# agent_13 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation prompt surface for RLCR setup. The directory recursively contains one included descendant, `prompt-template/plan/gen-plan-template.md`, a 74-line Markdown skeleton that defines the required shape of a human implementation plan before the loop state machine begins operating.
- algorithmic_behavior: The template forces plan authors or generation agents to produce deterministic acceptance criteria, explicit implementation boundaries, allowed choices, dependency sequencing, and code-style constraints. It is not executable code, but it shapes downstream algorithm behavior because RLCR hooks and stop hooks depend on plan files and goal trackers being structured enough for validation and review. Key sections are: plan title and goal description at `prompt-template/plan/gen-plan-template.md:1`, acceptance criteria with positive and negative tests at `:6`, path boundaries at `:25`, feasibility hints at `:46`, dependency sequencing at `:57`, and implementation notes at `:69`.
- inputs_outputs_state: Input is a draft goal or user intent supplied to the plan-generation step. Output is a Markdown plan with `Goal Description`, `Acceptance Criteria`, `Path Boundaries`, `Feasibility Hints and Suggestions`, `Dependencies and Sequence`, and `Implementation Notes`. It does not mutate runtime state directly; instead, it becomes or informs the plan file referenced by loop state frontmatter, such as `plan_file` parsed by `hooks/lib/loop-common.sh` at lines `190-240` and displayed by monitor parsing in `scripts/humanize.sh:349-365`.
- gates_or_invariants: The template requires each acceptance criterion to include positive and negative tests (`gen-plan-template.md:6-23`), defines upper and lower scope bounds (`:29-37`), and states that deterministic designs may collapse choice space (`:44`). It also bans plan-specific workflow terms such as `AC-`, `Milestone`, `Step`, and `Phase` from implementation code/comments (`:71-74`), preserving a separation between plan vocabulary and code vocabulary.
- dependencies_and_callers: The directory is part of the broader `prompt-template` tree consumed by `hooks/lib/template-loader.sh`, whose `get_template_dir` resolves `prompt-template` from `hooks/lib` (`template-loader.sh:24-31`) and whose `load_and_render_safe` provides fallback-safe rendering (`:167-203`). No direct code reference to `plan/gen-plan-template.md` appeared in the inspected hook grep, so it is likely used by setup/generation flows rather than validator hot paths.
- edge_cases_or_failure_modes: If this template is too vague, downstream plan/state files may lack deterministic tests or dependency ordering, weakening loop review. If the generated plan leaks plan-only terms into implementation, it violates the template’s own invariant. As a template, missing file handling depends on template-loader fallback behavior; missing templates produce warnings or fallback content rather than hard failures in `load_and_render_safe`.
- validation_or_tests: No direct test targets this exact plan template in the assigned set. Indirect validation comes from hook/state tests that expect state files to include plan references and valid loop structure. Recursive directory inspection found only `prompt-template/plan/gen-plan-template.md`, so the directory responsibility is narrow and fully covered.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-043 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor resilience around deletion of `.humanize/` during an active RLCR monitor session. It is not an automated gate, but it documents operator-observable behavior for a key edge case: graceful monitor exit and terminal restoration.
- algorithmic_behavior: The script supports three commands via a `case` dispatch at `tests/manual-monitor-test.sh:23-72`. `setup` creates a fake RLCR session directory `.humanize/rlcr/2026-01-16_99-99-99`, writes a minimal `state.md`, and writes a minimal `goal-tracker.md` (`:24-46`). `delete` removes `.humanize` to trigger monitor shutdown in another terminal (`:47-57`). `cleanup` removes leftover `.humanize` (`:58-62`). Any other argument prints usage and exits `1` (`:63-71`).
- inputs_outputs_state: Input is a single positional command: `setup`, `delete`, or `cleanup`. Outputs are console instructions and side effects on `.humanize/` in the project root. State created by `setup` includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` lines in `state.md` (`:27-32`), plus a minimal goal tracker with one acceptance criterion (`:33-38`). `delete` and `cleanup` both delete the local loop state directory.
- gates_or_invariants: The expected observable gate is written in the instructions and output text: after deletion, the monitor should show “Monitoring stopped: .humanize/rlcr directory no longer exists”, restore terminal scroll/cursor state, and avoid shell glob errors (`manual-monitor-test.sh:51-54`). The script assumes the user starts `source scripts/humanize.sh && humanize monitor rlcr` in another terminal (`:10-14`, `:43-45`).
- dependencies_and_callers: It depends on `scripts/humanize.sh` monitor behavior, particularly `_humanize_monitor_codex`, which exits when `.humanize/rlcr` is absent (`scripts/humanize.sh:230-235`) and uses `find` instead of unsafe globs for log discovery (`:280-339`). It also depends on the monitor’s state and goal-tracker parsers (`scripts/humanize.sh:32-112`, `:349-365`).
- edge_cases_or_failure_modes: The fixture `state.md` lacks YAML frontmatter separators and fields required by strict hook parsers, so it is tailored for monitor display, not hook validation. Running `delete` or `cleanup` is destructive to the local `.humanize` folder, so this script must only be used in a disposable project/session. The test is manual and can miss transient terminal rendering bugs unless the operator observes the second terminal.
- validation_or_tests: This manual spec overlaps with automated robustness coverage in `tests/robustness/test-hook-input-robustness.sh`, which invokes `_humanize_monitor_codex` with missing session directories, narrow terminals, and ANSI logs (`test-hook-input-robustness.sh:444-665`). The assigned script itself contains no assertions beyond operator instructions.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-073 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared algorithm library for RLCR and PR-loop hook validators. It centralizes hook input validation, active-loop discovery, state parsing, round/path matching, block-message rendering, cancel authorization, `.humanize` git protection, shell-command modification detection, loop termination, and PR goal-tracker updates.
- algorithmic_behavior: Hook input validation starts with `validate_hook_input`, which rejects null bytes, invalid UTF-8 when `iconv` is available, malformed JSON, and missing `tool_name` (`hooks/lib/loop-common.sh:58-95`). `require_tool_input_field` enforces required tool input fields (`:100-113`). `is_deeply_nested` computes JSON path depth with `jq` and blocks inputs deeper than the configured limit (`:118-132`). Runtime loop discovery uses `find_active_loop`, which checks only the newest timestamp-like child directory and treats either `state.md` or `finalize-state.md` as active (`:155-173`). State parsing has tolerant and strict paths: `parse_state_file` applies defaults for normal operation (`:207-240`), while `parse_state_file_strict` fails closed on missing frontmatter, missing required fields, nonnumeric rounds, nonnumeric max iterations, invalid `review_started`, or missing `base_branch` (`:251-323`). Review-loop continuation is determined by `detect_review_issues`, which scans Codex review logs for `[P?]` markers in the first 10 characters and extracts from the first issue line onward (`:343-386`).
- inputs_outputs_state: Inputs are JSON hook payloads, file paths, shell command strings, loop base directories, state files, review logs, and PR goal tracker files. Outputs are exported shell variables such as `VALIDATED_TOOL_NAME`, `VALIDATED_TOOL_INPUT`, and `STATE_*`; stdout path or parsed data; stderr block messages; and file mutations in selected state-changing helpers. `end_loop` renames `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`:1113-1139`). `update_pr_goal_tracker` mutates PR goal tracker Markdown by updating statistics, inserting issue-summary rows, and appending issue-log entries (`:1162-1309`).
- gates_or_invariants: The library enforces strict JSON and UTF-8 intake (`:61-82`), required hook fields (`:84-93`, `:100-113`), JSON depth limits (`:118-132`), active-loop freshness by newest directory only (`:150-173`), strict state schema for safety-sensitive validators (`:251-323`), round-file naming and extraction invariants (`:393-412`), explicit allowlist exceptions for specific todos/summary files (`:417-435`), and protected exit reason vocabulary for `end_loop` (`:1118-1126`). `is_cancel_authorized` is a security gate requiring `.cancel-requested`, rejecting command substitution, hidden `$` variables, command chaining, newlines, mixed quote styles, malformed argument counts, wrong source/destination paths, and symlinked source state files (`:545-704`).
- dependencies_and_callers: It sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` (`loop-common.sh:138-148`). It is consumed by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`, all of which call `validate_hook_input` and `is_deeply_nested` before tool-specific checks (`loop-read-validator.sh:25-45`, `loop-write-validator.sh:25-45`, `loop-bash-validator.sh:24-44`). It also supports `hooks/loop-edit-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-plan-file-validator.sh`, PR-loop stop hooks, setup scripts, and many robustness tests. Block message helpers route through template files under `prompt-template/block`, including the assigned `wrong-file-location.md`.
- edge_cases_or_failure_modes: Null byte detection is attempted before `jq`, but Bash command substitution can strip null bytes before functions see them, as the robustness test documents (`test-hook-input-robustness.sh:219-235`). `find_active_loop` intentionally ignores older active-looking directories if the newest directory lacks `state.md`/`finalize-state.md`, preventing stale “zombie” loop revival but making newest-directory naming important (`loop-common.sh:150-173`). Strict parsing accepts negative numeric rounds/max iterations because the regex is `^-?[0-9]+$` (`:302-310`); downstream policy must decide whether those values are semantically valid. `command_modifies_file` is regex-based and broad enough for common redirection/edit/move/delete forms (`:1065-1090`), but shell parsing remains inherently approximate and is backed by additional Bash-validator checks for wrappers and command segments.
- validation_or_tests: The assigned robustness test directly verifies valid/invalid JSON, required fields, deep nesting, non-UTF8 rejection, null-byte graceful behavior, and `command_modifies_file` patterns (`tests/robustness/test-hook-input-robustness.sh:35-235`, `:351-370`). Additional referenced suites cover cancel authorization, review issue detection, state parsing, active session selection, state transitions, command patterns, PR goal tracker updates, and `.humanize` git-add protection.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-103 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Hook block template for the RLCR read-path state machine when an agent attempts to read a round prompt/summary filename from outside the active loop directory. It turns a blocked transition into a corrective instruction.
- algorithmic_behavior: The template renders a `Wrong File Location` message with the attempted `{{FILE_PATH}}`, the active loop directory `{{ACTIVE_LOOP_DIR}}`, and current-round prompt/summary paths based on `{{CURRENT_ROUND}}` (`prompt-template/block/wrong-file-location.md:1-9`). It is invoked by `hooks/loop-read-validator.sh` after the validator confirms a round `summary` or `prompt` file target, finds an active loop, strictly parses state, and detects that the requested path is not inside `.humanize/rlcr/` (`loop-read-validator.sh:69-131`).
- inputs_outputs_state: Inputs are template variables supplied by `load_and_render_safe`: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` (`loop-read-validator.sh:127-130`). Output is a Markdown error message written to stderr before the validator exits `2` (`:127-131`). It does not change filesystem state.
- gates_or_invariants: The template supports the invariant that current round files must be accessed through the active loop directory, not copied/stale locations. The validator gate leading to this message only fires for `round-N-summary.md` or `round-N-prompt.md` paths (`loop-read-validator.sh:69-70`) and only after strict state parsing succeeds (`:94-99`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` single-pass rendering (`template-loader.sh:50-132`) and fallback-safe loading (`:167-203`). Its primary caller is `hooks/loop-read-validator.sh`. It coordinates with sibling block templates for wrong round (`prompt-template/block/wrong-round-file.md`) and wrong directory path (`prompt-template/block/wrong-directory-path.md`), which are used by later read-validation gates (`loop-read-validator.sh:138-167`).
- edge_cases_or_failure_modes: Line 9 says `If you need this file, use: cat {{FILE_PATH}}`, which could be confusing because this template is emitted when reading that path was blocked. The stronger corrective paths are listed at lines `5-7`, pointing to active-loop prompt/summary. If template loading fails, `loop-read-validator.sh` has a fallback message at lines `124-126`, but that fallback is less specific than the full template.
- validation_or_tests: Indirectly covered by hook validator tests that exercise wrong-location read behavior in broader suites. In the assigned robustness test, hook input parsing is validated for read payloads and non-loop paths (`test-hook-input-robustness.sh:35-45`, `:149-161`), but it does not specifically assert this template text.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-133 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Automated robustness specification for hook JSON intake, command-modification detection, monitor parser availability, and monitor crash resistance under missing sessions, narrow terminals, and ANSI logs. It is an executable contract for edge behavior around the RLCR hook and monitor system.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` and shared test helpers (`tests/robustness/test-hook-input-robustness.sh:14-19`), then runs grouped checks. Tests 1-10 validate production hook validators with JSON piped to `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh`: valid `Read`, `Write`, and `Bash` payloads should pass (`:35-69`); malformed JSON, missing `tool_name`, and missing `tool_input.file_path` should reject with non-signal nonzero exits (`:71-115`); long commands, special characters, Unicode paths, and unknown tool names should not be overblocked (`:117-175`). Tests 10a-10c cover deeply nested JSON rejection, non-UTF8 rejection with exact exit `1`, and null-byte graceful handling (`:177-235`). Monitor edge tests cover terminal width, log growth/deletion, ANSI stripping, binary log line counting, narrow/wide formatting, and rapid writes (`:245-341`). Command pattern tests call `command_modifies_file` for `sed -i` and redirection (`:351-370`). Integration tests source `scripts/humanize.sh` and check goal-tracker, git-status, and git-state helpers (`:381-433`). The final monitor tests invoke `_humanize_monitor_codex` in temporary runner scripts for missing sessions, narrow terminals, and ANSI-coded logs (`:444-665`).
- inputs_outputs_state: Inputs are generated JSON strings, temporary test directories from `setup_test_dir`, temporary fake HOME/cache directories, synthetic `.humanize/rlcr` sessions, synthetic goal trackers, and synthetic log files. Outputs are PASS/FAIL counters through `tests/test-helpers.sh`, child hook exit codes, captured hook/monitor output, and a final `print_test_summary` exit code (`test-hook-input-robustness.sh:666-671`). The script mutates only its temporary directories and generated runner scripts/logs under those temp roots.
- gates_or_invariants: Hook validators must fail closed on malformed input while not signal-crashing (`:80-84`, `:95-99`, `:110-114`). Deeply nested JSON above depth 30 must be rejected (`:177-199`), matching `is_deeply_nested` in `loop-common.sh:118-132`. Non-UTF8 binary input must return exit `1` (`test-hook-input-robustness.sh:201-217`), matching `validate_hook_input` UTF-8 validation (`loop-common.sh:68-75`). Unknown tools must pass through (`test-hook-input-robustness.sh:163-175`), matching validator early exits for nonmatching tool names (`loop-read-validator.sh:37-41`, `loop-write-validator.sh:37-41`, `loop-bash-validator.sh:36-40`). Monitor exits must remain below signal-crash threshold `128` for simulated edge cases (`test-hook-input-robustness.sh:573-578`, `:660-664`).
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, and `scripts/humanize.sh`. It exercises monitor helpers `humanize_parse_goal_tracker`, `humanize_parse_git_status`, `humanize_detect_git_state`, and `_humanize_monitor_codex`, defined in `scripts/humanize.sh:32-209` and `:219` onward.
- edge_cases_or_failure_modes: The test uses `echo "$JSON"` to pipe binary/null-containing strings, which can transform input; it explicitly acknowledges Bash strips null bytes before hook code can detect them (`test-hook-input-robustness.sh:219-230`). It assumes `jq` is available because production validators require it through `loop-common.sh`. Some monitor checks assert “does not crash” rather than exact UI content, so rendering regressions can pass if exit codes remain below `128`. The temporary runner scripts override `tput` and `clear`, which isolates terminal behavior but may not cover real terminal escape-state restoration as completely as the manual monitor test.
- validation_or_tests: This file is itself the validation artifact. It contains 26 named tests and ends by returning the aggregate helper summary (`:670-671`). I did not execute it in this read-only branch research pass; inspection shows it would create temp directories and run production hook/monitor scripts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 section headings present; each assigned item has exactly one Item Evidence section`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`