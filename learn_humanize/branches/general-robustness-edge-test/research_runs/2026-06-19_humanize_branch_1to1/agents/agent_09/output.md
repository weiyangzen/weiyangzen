# agent_09 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared algorithm library for RLCR/PR loop hooks. `hooks/lib/loop-common.sh` centralizes hook input validation, active-loop discovery, state parsing, file-access classification, cancel authorization, git-add protection, command write-detection, loop ending, review issue extraction, and PR goal tracker mutation. `hooks/lib/template-loader.sh` centralizes prompt/block template loading and single-pass rendering.
- algorithmic_behavior: `template-loader.sh` resolves `prompt-template` from the plugin root via `get_template_dir` at lines 26-31, loads template files at lines 36-48, renders `{{VAR}}` placeholders in one awk pass at lines 58-132, and provides fallback-safe rendering at lines 170-203. `loop-common.sh` validates hook JSON and tool fields at lines 58-113, rejects excessive JSON depth at lines 118-132, sources `template-loader.sh` and initializes `TEMPLATE_DIR` at lines 138-148, discovers the newest active RLCR loop at lines 155-173, parses tolerant and strict YAML frontmatter state at lines 207-324, detects Codex review issue markers at lines 343-386, classifies round/prompt/todos/state paths at lines 389-524, authorizes only narrowly-shaped cancel `mv` commands at lines 545-704, maps PR-loop bot/author names and PR-loop file types at lines 721-881, detects protected `.humanize` staging at lines 924-1020, detects shell commands that modify protected files at lines 1065-1090, ends loops by renaming `state.md` at lines 1113-1139, and updates PR goal tracker tables/logs idempotently at lines 1162-1309.
- inputs_outputs_state: Inputs are hook JSON from validators, command strings, file paths, `.humanize/rlcr/*/state.md` or `finalize-state.md`, `.humanize/pr-loop/*/state.md`, prompt templates, and Codex review logs. Outputs are shell return codes, rendered block messages to stderr/stdout, parsed global variables such as `STATE_CURRENT_ROUND`, `STATE_BASE_BRANCH`, and `VALIDATED_TOOL_NAME`, extracted review markdown files, renamed loop state files, and modified PR goal tracker files. State transitions include active-loop detection from `state.md`/`finalize-state.md`, `end_loop` renaming `state.md` to reason-specific state files, and `update_pr_goal_tracker` completing missing summary/log rows without double-counting.
- gates_or_invariants: Hook input must be valid JSON with `tool_name`; null bytes and invalid UTF-8 are rejected when detectable at lines 61-82. Deep JSON beyond default depth 30 is rejected. Strict state parsing requires YAML separators, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric rounds/iterations, and boolean `review_started` at lines 251-324. Todos/prompt/state/finalize-state/goal-tracker paths are classified by explicit filename patterns. Cancel is gated by `.cancel-requested`, no command substitution/backticks/newlines/shell operators, no residual `$`, exactly two args, expected source/destination paths, no mixed quote delimiters, no multiple trailing spaces, and no symlink source at lines 551-704. Template rendering is single-pass so replacement values containing placeholders are not recursively expanded.
- dependencies_and_callers: Sibling coordination is direct: `loop-common.sh` sources `template-loader.sh` at line 140 and all block-message helpers call `load_and_render_safe`. Parent hook callers include `hooks/loop-read-validator.sh` lines 17-35 and 55-62, `hooks/loop-write-validator.sh` lines 17-35 and 55-67, `hooks/loop-edit-validator.sh` lines 16-49, `hooks/loop-bash-validator.sh` lines 16-48 and 115-393, `hooks/loop-plan-file-validator.sh`, `hooks/pr-loop-stop-hook.sh`, and setup scripts. `scripts/portable-timeout.sh` is also sourced by setup/PR hooks rather than by this directory. Tests covering this directory include template reference validation, template robustness/stress tests, hook input robustness, state/session/transition robustness, cancel security robustness, humanize escape tests, and codex review merge tests.
- edge_cases_or_failure_modes: `find_active_loop` intentionally checks only the newest lexicographic child, so older active-looking loops are ignored. `parse_state_file` is tolerant but `parse_state_file_strict` fails closed for malformed state. `is_state_file_path` also matches `finalize-state.md`, so callers must check finalize first; several validators do. `git_adds_humanize` receives lowercased commands, so `-A` becomes `-a` and is treated accordingly. `command_modifies_file` is pattern-based and broad enough for redirects, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >`. Template loading warns and falls back rather than failing hard when templates are missing.
- validation_or_tests: `tests/test-template-references.sh` explicitly includes `hooks/lib/loop-common.sh` in its scan at lines 57-64 and checks common templates at lines 152-167. `tests/robustness/test-cancel-security-robustness.sh` sources `loop-common.sh` at line 17 and exercises `is_cancel_authorized`. `tests/test-helpers.sh` supplies `pass`, `fail`, `print_test_summary`, and temp dir setup at lines 30-89. Additional usage search shows state parsing, active-loop discovery, template fallback, `.humanize` git-add protection, and command modification helpers are covered by multiple `tests/robustness/*` and `tests/test-*` scripts.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-039 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Cross-platform timeout abstraction used by setup scripts, PR-loop hooks, GitHub/Git commands, and robustness tests so workflow operations are bounded on macOS and Linux.
- algorithmic_behavior: `detect_timeout_impl` selects the first available implementation in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` at lines 10-27. The detected value is stored once in `TIMEOUT_IMPL` at line 29. `run_with_timeout` accepts seconds plus command/args, dispatches to the selected implementation, preserves the child return code, and maps Python `TimeoutExpired` to exit 124 to match GNU timeout at lines 33-71.
- inputs_outputs_state: Input is `run_with_timeout <seconds> <command> [args...]`. Output is the wrapped command’s stdout/stderr and exit code, or 124 on Python-enforced timeout. State is the exported shell variable `TIMEOUT_IMPL` at lines 73-76; callers sourcing the file share that detection result for subsequent commands.
- gates_or_invariants: GNU `timeout` is accepted only if `timeout --version` succeeds, avoiding non-GNU/BSD ambiguity at lines 13-19. Python fallback runs `subprocess.run(sys.argv[1:], timeout=$timeout_secs)` without shell interpolation, preserving argument boundaries at lines 49-61. If no timeout tool exists, the script emits a warning and runs the command unbounded at lines 64-69.
- dependencies_and_callers: Depends on shell builtins, `command -v`, optional `gtimeout`/GNU `timeout`, and optional Python. Sourced by `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-plan-file-validator.sh`, and timeout/setup robustness tests. The usage search shows it wrapping `git`, `gh`, `codex exec`, and validation shell commands.
- edge_cases_or_failure_modes: Timeout seconds are not validated before interpolation into the Python snippet; invalid or malicious timeout strings would be interpreted as Python code if caller passes untrusted text. Empty command arrays fall through to the selected runner and fail according to that backend. With `none`, long-running commands can hang because the fallback is intentionally best-effort. `TIMEOUT_IMPL` is detected at source time; changes to PATH after sourcing are not reflected unless re-sourced.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` covers normal output, timeout exit behavior, argument preservation, nonzero command exit codes, shell pipelines, repeated calls, nonexistent/empty commands, special characters, trapped TERM behavior, and subshells. `tests/robustness/test-setup-scripts-robustness.sh` and base-branch detection tests exercise it in setup-style workflows.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-069 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that guards the template-rendering contract: validators must not reference missing block/prompt templates, and critical validators should use fallback-safe rendering so blocked actions always produce a meaningful message.
- algorithmic_behavior: Initializes `SCRIPT_DIR`, `PROJECT_ROOT`, and `TEMPLATE_DIR` at lines 14-17, defines local pass/fail/warn counters at lines 25-49, and scans a fixed set of hook scripts plus `hooks/lib/loop-common.sh` at lines 57-64. Section 1 extracts template names from non-comment lines matching `load_template`, `load_and_render`, or `load_and_render_safe` with `"$TEMPLATE_DIR"` and validates file existence at lines 74-111. Section 2 enumerates all markdown templates under `prompt-template` and warns on unreferenced templates at lines 121-142. Section 3 hard-codes common loop-common block templates, including `block/todos-file-access.md`, and verifies they exist at lines 149-167. Section 4 checks critical Read/Write/Edit validators for unsafe `load_and_render` calls not using `_safe` at lines 172-201. Summary exits 0 only when `TESTS_FAILED` is zero at lines 206-230.
- inputs_outputs_state: Inputs are repository shell scripts and `prompt-template/**/*.md`. Outputs are colored pass/fail/warn lines, aggregate counters, an optional missing-template list, and process exit status. State is in arrays `MISSING_TEMPLATES`, `TEMPLATE_FILES`, `UNREFERENCED`, and scalar counters.
- gates_or_invariants: Missing referenced templates are hard failures; unreferenced templates are warnings only. Critical validators are required to use `load_and_render_safe` for template rendering, preserving fallback behavior if a template disappears. The extractor skips commented lines and only recognizes direct quoted `$TEMPLATE_DIR` usage, so dynamic template references are outside the hard gate.
- dependencies_and_callers: Depends on Bash, `find`, `grep`, `sed`, and the repository structure `hooks/` plus `prompt-template/`. It validates the contract implemented by `hooks/lib/template-loader.sh` and message helpers in `hooks/lib/loop-common.sh`; it is included in the overall test suite list via `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The regex intentionally misses dynamically constructed template paths, alternate variable names, multiline function calls, or unquoted template arguments. Warnings for unreferenced templates may be benign because some templates are loaded dynamically or by setup scripts outside `hooks/`. `set -uo pipefail` omits `-e`, allowing the script to accumulate failures instead of stopping early.
- validation_or_tests: This file is itself the validation artifact for template references. Its strongest regression target is preventing an empty/errorless validator response when a template file is missing; lines 8-9 state that risk explicitly.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-099 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR loop invariant that agents should not create, read, or modify `round-*-todos.md` files directly and should use native TodoWrite state instead.
- algorithmic_behavior: The template is static markdown: title at line 1, direct prohibition at line 3, required alternative at line 5, and rationale at lines 7-8. It is loaded through `todos_blocked_message` in `hooks/lib/loop-common.sh` lines 437-446, which calls `load_and_render_safe` with a fallback if the template is missing.
- inputs_outputs_state: Input is a blocked Read/Write/Edit/Bash attempt against a todos file, routed through hook validators. Output is this markdown text to stderr as the reason for exit 2. The template carries no mutable state and has no placeholders.
- gates_or_invariants: Enforces a workflow boundary: file-based `round-*-todos.md` access is blocked except narrow allowlisted active-loop files. The native todo tool is the intended state surface, keeping task state visible to the host UI rather than hidden in markdown files.
- dependencies_and_callers: Called by `todos_blocked_message`; invoked by `hooks/loop-read-validator.sh` lines 55-62, `hooks/loop-write-validator.sh` lines 55-62, `hooks/loop-edit-validator.sh` lines 38-45, and `hooks/loop-bash-validator.sh` lines 385-391. Existence is asserted in `tests/test-template-references.sh` common templates at lines 152-167.
- edge_cases_or_failure_modes: Because the file has no variables, renderer failures are mainly missing-file or empty-file cases; `load_and_render_safe` supplies an inline fallback. The template text says “create or access,” while Bash validator specifically blocks modification unless the path is the active-loop allowlisted round-1/round-2 todos file; this is stricter as a user-facing policy than some legacy allowlist mechanics.
- validation_or_tests: `tests/test-template-references.sh` verifies the template exists. Hook robustness tests indirectly validate that todos access routes to this block message through read/write/edit/bash validators.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-129 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Security regression suite for `is_cancel_authorized`, proving that the only permitted state-file mutation bypass is an explicit cancel transition from `state.md` or `finalize-state.md` to `cancel-state.md` inside the active loop directory with a signal file present.
- algorithmic_behavior: Sets strict Bash mode, derives `PROJECT_ROOT`, sources `hooks/lib/loop-common.sh` and shared test helpers at lines 13-18, creates a temporary active loop with `state.md` at lines 20-30, then runs 26 positive and negative cases. Valid cases cover signal-file authorization for `state.md`, `finalize-state.md`, single quotes, and unquoted paths at lines 39-89. Negative cases cover missing signal, command substitution, backticks, semicolon/AND/OR/pipe chaining, wrong source/destination, extra arguments, newline injection, residual variable expansion, non-`mv`, mixed quote styles, IFS manipulation, multiple trailing spaces, non-standard source filenames, and symlink sources at lines 99-367. Regression cases ensure loop directories containing the word `finalize` do not bypass symlink checks for either source file at lines 369-405. It prints a shared summary and exits with the helper status at lines 407-412.
- inputs_outputs_state: Inputs are synthetic command strings lowercased before validation, synthetic `.cancel-requested` signal files, temporary `state.md`/`finalize-state.md` files, and symlinks. Outputs are pass/fail records and the final process exit code. Test state transitions repeatedly create/remove `.cancel-requested`, create/remove `finalize-state.md`, replace `state.md` with symlinks, and restore normal files between cases.
- gates_or_invariants: Positive authorization requires the signal file and exact command shape. Negative assertions encode the security contract from `loop-common.sh`: no shell chaining, no command substitution, no hidden variable expansion, no extra args, no wrong path, no symlink source, and no ambiguous quote style. The test explicitly validates `/./` path normalization as accepted at lines 265-276.
- dependencies_and_callers: Depends on `is_cancel_authorized` and `_normalize_path` behavior in `hooks/lib/loop-common.sh`, plus `setup_test_dir`, `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh` lines 30-89. It is part of the robustness suite for the Bash validator path that allows cancel operations in `hooks/loop-bash-validator.sh` lines 139-155 and 286-303.
- edge_cases_or_failure_modes: The test lowercases commands before calling `is_cancel_authorized`, matching validator behavior but making it insensitive to case-preservation issues in paths. It treats multiple trailing spaces as rejection and a single `/./` normalization as acceptance. It validates symlink rejection via filesystem state, including a historical substring bug where a directory name containing `finalize` could misclassify the source path.
- validation_or_tests: This file is the direct executable validation for cancel security. It covers both allowed transitions and bypass prevention, including regression tests for symlink bypasses and path-name confusion.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: GENERAL_ROBUSTNESS_EDGE_TEST-HZ-009, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-039, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-069, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-099, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-129
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`