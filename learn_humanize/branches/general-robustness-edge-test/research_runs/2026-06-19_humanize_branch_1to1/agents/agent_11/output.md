# agent_11 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing RLCR prompt fragments. This directory defines the worker/operator instructions emitted by `hooks/loop-codex-stop-hook.sh` when the RLCR loop transitions between implementation rounds, review-fix rounds, and finalize mode.
- algorithmic_behavior: Contains eight recursively inspected templates: `review-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `finalize-phase-prompt.md`, `next-round-prompt.md`, `push-every-round-note.md`, `goal-tracker-update-request.md`, `next-round-footer.md`, and `post-alignment-action-items.md`. `review-phase-prompt.md` turns Codex `[P0-9]` findings into a fix-only prompt and requires a summary path (`prompt-template/claude/review-phase-prompt.md:1-28`). `next-round-prompt.md` carries original plan, Codex review content, and read-only goal tracker rules into the next implementation round (`prompt-template/claude/next-round-prompt.md:1-31`). The finalize templates switch from implementation to refactor/simplification constraints after review pass or review skip (`prompt-template/claude/finalize-phase-prompt.md:1-51`, `prompt-template/claude/finalize-phase-skipped-prompt.md:1-50`).
- inputs_outputs_state: Inputs are template variables rendered by `load_and_render_safe`: `REVIEW_CONTENT`, `SUMMARY_FILE`, `NEXT_SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `BASE_BRANCH`, `START_BRANCH`, and `REVIEW_SKIP_REASON`. Outputs are markdown prompts written to `round-N-prompt.md` or returned as hook block reasons. State transitions are represented as prompt obligations: implementation round continues, review phase fixes issues, finalize phase writes `finalize-summary.md`.
- gates_or_invariants: The templates enforce loop invariants through instructions: create todos for all work, do not only address high-priority issues (`next-round-prompt.md:12-14`), treat `goal-tracker.md` as read-only after round 0 (`next-round-prompt.md:22-31`), use fix-only behavior in review phase (`review-phase-prompt.md:11-14`), and preserve functionality/tests during finalize simplification (`finalize-phase-prompt.md:16-24`; skipped variant at `finalize-phase-skipped-prompt.md:26-34`). `next-round-footer.md` explicitly forbids lying, editing loop state, or cancelling the loop to escape (`prompt-template/claude/next-round-footer.md:4-9`).
- dependencies_and_callers: Direct callers are in `hooks/loop-codex-stop-hook.sh`: skipped finalize loads `claude/finalize-phase-skipped-prompt.md` (`hooks/loop-codex-stop-hook.sh:1035-1041`), normal finalize loads `claude/finalize-phase-prompt.md` (`hooks/loop-codex-stop-hook.sh:1063-1068`), review issues load `claude/review-phase-prompt.md` (`hooks/loop-codex-stop-hook.sh:1114-1116`), and normal next-round prompt assembly loads `next-round-prompt.md`, optional post-alignment action items, footer, push note, and goal tracker update request (`hooks/loop-codex-stop-hook.sh:1489-1522`). Template loading is mediated by `hooks/lib/template-loader.sh` functions `load_template`, `render_template`, and `load_and_render_safe`.
- edge_cases_or_failure_modes: Missing templates are not fatal where `load_and_render_safe` has fallback text; missing optional fragments fall back to inline messages for push note and goal tracker update request (`hooks/loop-codex-stop-hook.sh:1508-1522`). Review-skipped finalize explicitly warns that implementation could not be fully validated and requires manual checks (`finalize-phase-skipped-prompt.md:3-14`). Post-alignment instructions are conditional only after every fifth-round alignment cadence (`hooks/loop-codex-stop-hook.sh:1494-1500`).
- validation_or_tests: `tests/test-template-references.sh` checks whether templates are referenced by hooks and verifies common template existence (`tests/test-template-references.sh:130-167`). Finalize prompt behavior is covered indirectly by `tests/test-finalize-phase.sh` per repository references, including finalize summary requirements and state transitions.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-041 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: RLCR loop bootstrap script. It validates command arguments, plan file safety, repository state, Codex availability, base branch resolution, and initializes `.humanize/rlcr/<timestamp>/` state, prompts, plan backup, and goal tracker.
- algorithmic_behavior: Parses positional or `--plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--track-plan-file`, and `--base-branch` (`scripts/setup-rlcr-loop.sh:111-195`). Blocks concurrent RLCR/PR loops by sourcing `hooks/lib/loop-common.sh` and calling `find_active_loop` / `find_active_pr_loop` (`scripts/setup-rlcr-loop.sh:203-232`). Validates git repo and commit presence through `run_with_timeout` (`scripts/setup-rlcr-loop.sh:257-267`). Validates plan paths against absolute paths, spaces, shell metacharacters, symlinks, missing dirs, path escape, and submodules (`scripts/setup-rlcr-loop.sh:273-374`). Enforces plan tracking policy (`scripts/setup-rlcr-loop.sh:380-425`), content sufficiency (`scripts/setup-rlcr-loop.sh:431-484`), Codex availability (`scripts/setup-rlcr-loop.sh:486-492`), YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:498-530`, `588-594`), base branch selection (`scripts/setup-rlcr-loop.sh:537-603`), then creates loop files (`scripts/setup-rlcr-loop.sh:610-807`) and prints the initial prompt plus completion requirements (`scripts/setup-rlcr-loop.sh:825-884`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR` or `pwd`, git metadata, the plan file content, local branch refs, `codex` command availability, and sourced `scripts/portable-timeout.sh`. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, `round-0-prompt.md`, and a rendered activation prompt to stdout. State file fields include `current_round: 0`, `max_iterations`, Codex settings, push policy, plan path/tracking mode, start/base branches, captured `base_commit`, `review_started: false`, and UTC timestamp (`scripts/setup-rlcr-loop.sh:628-644`).
- gates_or_invariants: Only one active loop may exist (`scripts/setup-rlcr-loop.sh:211-232`). Plan file must be relative, inside project, non-symlinked, readable, sufficiently detailed, and either gitignored by default or tracked-clean only with `--track-plan-file` (`scripts/setup-rlcr-loop.sh:273-425`). Git operations fail closed on timeout for status/tracking checks (`scripts/setup-rlcr-loop.sh:380-397`). User-provided base branch must exist locally; remote-only base branch is rejected because `codex review --base` needs a local ref (`scripts/setup-rlcr-loop.sh:537-553`). Base commit is captured at loop start to avoid diffing against a moving branch ref (`scripts/setup-rlcr-loop.sh:596-604`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` (`scripts/setup-rlcr-loop.sh:26-29`) and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:203-205`). Runtime dependencies include `git`, `codex`, shell tools (`grep`, `sed`, `awk`, `wc`, `date`, `cp`, `mkdir`), and repository-local hook conventions. The generated state/prompt files are later consumed by stop/write/read/bash validators and `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: Rejects multiple plan files, missing option arguments, non-numeric `--max` / `--codex-timeout`, unknown flags, missing plan, absolute paths, whitespace in path, shell metacharacters, file or parent symlinks, submodule-contained plans, tracked plan without `--track-plan-file`, untracked/dirty plan with `--track-plan-file`, low line count, low content count after ignoring blanks/comments/HTML comments, missing Codex, unsafe YAML chars in branch/model/effort, and inability to determine base branch. A notable current behavior is numeric zero accepted for `--codex-timeout` because validation only checks digits (`scripts/setup-rlcr-loop.sh:143-153`), which robustness tests document as current behavior.
- validation_or_tests: Covered by plan/path/setup robustness suites: `tests/test-plan-file-validation.sh` exercises absolute, missing, spaces, metacharacters, symlink, tracking, sparse/comment plans, `--plan-file`, and model validation; `tests/robustness/test-setup-scripts-robustness.sh` covers argument parsing, content rejection, symlink rejection, valid numeric args, timeout zero, and large timeout cases; `tests/robustness/test-base-branch-detection.sh` targets base branch detection; `tests/robustness/test-path-validation-robustness.sh` runs production setup path validation. `scripts/portable-timeout.sh` supplies portable timeout behavior using `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the hook helper that blocks loop exit when the latest Claude `TodoWrite` state contains incomplete todos.
- algorithmic_behavior: Creates a temp directory, feeds JSON hook input to the Python checker, captures exit code/output under `set +e`, and asserts expected outcomes with local `pass` / `fail` counters (`tests/test-todo-checker.sh:23-38`). Test Group 1 verifies hook input parsing and absent transcript handling (`tests/test-todo-checker.sh:45-97`). Test Group 2 verifies completed versus incomplete todo lists and output content (`tests/test-todo-checker.sh:99-157`). Test Group 3 verifies transcript format variations and latest-call semantics (`tests/test-todo-checker.sh:159-240`). Test Group 4 verifies missing status, empty content, and unicode-like content handling (`tests/test-todo-checker.sh:242-293`). It exits nonzero if any assertion failed (`tests/test-todo-checker.sh:295-313`).
- inputs_outputs_state: Inputs are synthetic JSON/JSONL transcripts written under `mktemp -d`, each passed as `{"transcript_path": ...}` to `python3 hooks/check-todos-from-transcript.py`. Outputs are PASS/FAIL lines and process exit 0/1 for the test script. The checker’s specified output states are exit `0` for all complete/no todos/no transcript, exit `1` for incomplete todos, and exit `2` for invalid hook input JSON, matching the Python docstring (`hooks/check-todos-from-transcript.py:7-13`).
- gates_or_invariants: The core invariant is that only the most recent non-empty `TodoWrite` list matters; older pending todos are superseded by later completed todos (`tests/test-todo-checker.sh:196-210`). Any status other than exact `completed` is incomplete, including `pending`, `in_progress`, and missing status (`tests/test-todo-checker.sh:121-157`, `249-263`). Invalid JSONL transcript lines are ignored, while invalid stdin JSON is a hard parse error (`tests/test-todo-checker.sh:51-73`, `179-194`).
- dependencies_and_callers: Depends on `python3`, `mktemp`, shell redirection, and `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:11-14`). The production checker parses Claude assistant message format, alternative `type: message`, and direct `tool_use` entries (`hooks/check-todos-from-transcript.py:51-85`), then prints `INCOMPLETE_TODOS` plus details before exiting 1 (`hooks/check-todos-from-transcript.py:113-126`).
- edge_cases_or_failure_modes: Nonexistent transcript files exit allow/0 (`tests/test-todo-checker.sh:87-97`; implementation returns empty list at `hooks/check-todos-from-transcript.py:25-26`). Empty transcript exits 0 (`tests/test-todo-checker.sh:166-177`). Empty todo content is still reported incomplete if status is pending (`tests/test-todo-checker.sh:265-278`). A malformed stdin JSON exits 2, but malformed JSONL lines inside an otherwise readable transcript are skipped (`hooks/check-todos-from-transcript.py:36-39`).
- validation_or_tests: This file is itself the validation suite for todo checker behavior. It does not invoke the full hook chain, but it directly exercises the production Python helper through subprocess calls and covers three transcript schemas plus error handling.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-101 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when an RLCR participant attempts to exit without writing the required round or finalize summary file.
- algorithmic_behavior: The template renders a concise block reason: exit attempted without work summary, required path in `{{SUMMARY_FILE}}`, expected summary contents, and instruction to retry exit after writing it (`prompt-template/block/work-summary-missing.md:1-16`). It is a gate message rather than executable logic.
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable supplied by `hooks/loop-codex-stop-hook.sh`. Output is markdown used as the `reason` field in a JSON hook block response. The target summary path depends on loop state: finalize phase uses `finalize-summary.md`, otherwise `round-${CURRENT_ROUND}-summary.md` (`hooks/loop-codex-stop-hook.sh:585-590`).
- gates_or_invariants: Enforces the invariant that every exit attempt must have a concrete summary artifact before Codex review, review-phase handling, or finalize completion proceeds. The hook checks file existence first, renders this template if absent, returns `decision: block`, and exits without advancing the loop (`hooks/loop-codex-stop-hook.sh:592-617`).
- dependencies_and_callers: Directly loaded by `load_and_render_safe "$TEMPLATE_DIR" "block/work-summary-missing.md"` with fallback text in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:596-600`). Related write/read validators also enforce correct summary paths and avoid stale/wrong-round summary edits through sibling block templates and `hooks/lib/loop-common.sh` helpers.
- edge_cases_or_failure_modes: If the template is missing or cannot render, the caller has a fallback markdown message (`hooks/loop-codex-stop-hook.sh:596-600`). In finalize phase, the same template still says "work summary" but the hook system message clarifies "Finalize Phase - summary file missing" (`hooks/loop-codex-stop-hook.sh:602-605`). The gate only checks existence here; content quality is left for later review stages.
- validation_or_tests: `tests/test-finalize-phase.sh` includes a negative case where finalize phase blocks exit when `finalize-summary.md` is missing, expecting a block mentioning summary. Template reference coverage appears in `tests/test-template-references.sh`, and broad summary-path behavior is covered by allowlist/write/read validator tests.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-131 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git status/state helpers in `scripts/humanize.sh`, especially `humanize_parse_git_status` and `humanize_detect_git_state`.
- algorithmic_behavior: Sources `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`, creates isolated git repositories, mutates repo state, calls production functions, parses pipe-delimited results, and asserts counts/states (`tests/robustness/test-git-operations-robustness.sh:13-62`). Positive tests cover clean repo, branch naming, untracked, modified, staged added, and insertion counts (`tests/robustness/test-git-operations-robustness.sh:68-158`). Negative/edge tests cover non-git dirs, detached HEAD, simultaneous file states, deletion, empty repo, feature branch, rename, many untracked files, filenames with spaces, binary files, staged+unstaged same file, and deletion counts (`tests/robustness/test-git-operations-robustness.sh:160-367`). Git state tests cover normal, detached, rebase, merge, shallow, non-git, and permission scenarios (`tests/robustness/test-git-operations-robustness.sh:369-507`).
- inputs_outputs_state: Inputs are temporary repo states created with git commands. `humanize_parse_git_status` returns `modified|added|deleted|untracked|insertions|deletions`, with an extra `|not a git repo` marker for non-repos (`scripts/humanize.sh:164-210`). `humanize_detect_git_state` returns `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo` (`scripts/humanize.sh:114-162`). Outputs are PASS/FAIL test summary via `print_test_summary`.
- gates_or_invariants: `humanize_parse_git_status` must not crash outside git; it emits zeros plus marker (`scripts/humanize.sh:167-170`). Porcelain XY states map to counts: `??` untracked, `A` added, `D` deleted, `M/MM` modified, and rename counts as modified (`scripts/humanize.sh:179-194`). Diffstat parsing must count insertion/deletion numbers from `git diff --shortstat HEAD` fallback (`scripts/humanize.sh:197-209`). State detection prioritizes rebase, then merge, then shallow, then detached, else normal (`scripts/humanize.sh:136-161`).
- dependencies_and_callers: Depends on git CLI, temp test helpers, portable timeout, and production `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:15-20`). The production helpers support monitor/status output paths in `scripts/humanize.sh`; `humanize_parse_git_status` is also exposed through `_parse_git_status` for compatibility (`scripts/humanize.sh:370`).
- edge_cases_or_failure_modes: Simulates detached HEAD via checkout of a commit (`tests/robustness/test-git-operations-robustness.sh:180-195`), rebase by creating `.git/rebase-merge` (`tests/robustness/test-git-operations-robustness.sh:403-432`), merge by creating `.git/MERGE_HEAD` (`tests/robustness/test-git-operations-robustness.sh:434-448`), shallow clone by touching `.git/shallow` (`tests/robustness/test-git-operations-robustness.sh:450-464`), and permission errors by chmodding `.git` to `000` while allowing either `permission_error` or `not_a_repo` as platform-dependent acceptable behavior (`tests/robustness/test-git-operations-robustness.sh:478-500`). File-name edge cases include spaces and binary files (`tests/robustness/test-git-operations-robustness.sh:301-329`).
- validation_or_tests: This file is the direct validation suite. It verifies production behavior rather than mocked functions because it sources `scripts/humanize.sh` and calls the actual helpers (`tests/robustness/test-git-operations-robustness.sh:17-33`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 section headers inspected; exact-once header count verified for all assigned rows.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`