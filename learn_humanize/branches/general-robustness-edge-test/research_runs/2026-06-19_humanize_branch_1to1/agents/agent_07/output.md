# agent_07 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable-specification tree for the Humanize loop algorithms: RLCR state handling, hook validators, prompt/template rendering, monitor behavior, PR review loop orchestration, cancellation, plan-file validation, git status parsing, and robustness edge cases. Inventory inspected recursively: 50 files, about 25,498 lines, including root shell suites, `tests/robustness/*.sh`, `tests/fixtures/*.json`, and `tests/mocks/gh`.
- algorithmic_behavior: `tests/run-all-tests.sh` is the suite coordinator. It declares ordered suites at `tests/run-all-tests.sh:33`, includes `test-state-exit-naming.sh` at `tests/run-all-tests.sh:39`, marks zsh-only suites at `tests/run-all-tests.sh:75`, strips ANSI before parsing pass/fail totals at `tests/run-all-tests.sh:125`, and exits nonzero on failed suites at `tests/run-all-tests.sh:155`. Shared helper behavior lives in `tests/test-helpers.sh:1`: counters, pass/fail/skip reporting, `setup_test_dir`, and temp git repo setup. Child suites cover hook/template algorithms (`tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-allowlist-validators.sh`), RLCR lifecycle (`tests/test-state-exit-naming.sh`, `tests/test-finalize-phase.sh`, `tests/test-cancel-signal-file.sh`, `tests/test-codex-review-merge.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-todo-checker.sh`), monitor behavior (`tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, `tests/manual-monitor-test.sh`), plan and gen-plan validation (`tests/test-plan-file-validation.sh`, `tests/test-gen-plan.sh`), and PR-loop behavior (`tests/test-pr-loop.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-lib.sh`).
- inputs_outputs_state: Inputs are repository scripts/hooks/templates, synthetic `.humanize/rlcr` and `.humanize/pr-loop` state directories, mock git repos, `gh` fixtures, JSON hook payloads, transcript JSONL, and temporary plan files. Outputs are pass/fail/skip counters, process exit codes, and temporary state transitions created under `mktemp` directories. `tests/setup-monitor-test-env.sh:20` creates canned PR-loop states for active bot variants; `tests/setup-fixture-mock-gh.sh:26` writes a fixture-backed `gh` shim; `tests/mocks/gh:1` is a reusable executable mock with environment-controlled PR state. Fixture inputs include issue comments, review comments, review submissions, reactions, and comment reactions in `tests/fixtures/*.json`.
- gates_or_invariants: Test invariants mirror runtime gates: only active state files keep loops alive, final-state files are terminal, protected loop files cannot be modified by Write/Edit/Bash validators, cancel renames require a signal file and exact safe command shape, template rendering must be safe against placeholder/metacharacter injection, plan-file setup must reject unsafe paths/branches/content, PR-loop approvals and timeouts must move bot state correctly, and monitor cleanup must restore terminal state. `tests/test-plan-file-validation.sh:86` starts path validation cases and later covers branch/model safety; `tests/test-cancel-signal-file.sh:3` covers cancellation authorization; `tests/test-humanize-escape.sh:41` covers `.humanize` git-add blocking; `tests/test-pr-loop-stophook.sh:20` covers force-push trigger rejection and related stop-hook states.
- dependencies_and_callers: The directory is called by developers or CI through `tests/run-all-tests.sh`. Suites source or execute `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/loop-*-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/humanize.sh`, PR scripts under `scripts/`, and fixtures/mocks under `tests/fixtures` and `tests/mocks`. `tests/test-pr-loop-system.sh:81` defines an internal `run_test` harness and exercises shared monitor helpers, PR phase detection, goal tracker parsing, approve-state detection, startup cases, and goal-tracker updates.
- edge_cases_or_failure_modes: The robustness subtree expands edge coverage without an artificial depth limit: state parsing corruption/missing fields/non-numeric values (`tests/robustness/test-state-file-robustness.sh:3`), state transitions and final/cancel handling (`tests/robustness/test-state-transition-robustness.sh:3`), concurrent state reads/writes, path traversal/symlink/submodule cases, hook input JSON/UTF-8/deep nesting, cancel security, git detached/rebase/merge/shallow states, template stress/error behavior, timeout fallback behavior, base-branch detection, setup-script failures, and PR-loop API handling. Some tests depend on local tools such as `git`, `jq`, `tput`, `zsh`, `python`, and shell features; `run-all-tests.sh` skips zsh-only coverage when zsh is unavailable.
- validation_or_tests: This directory is itself the validation surface. I inspected the runner, helpers, mocks, fixtures, root suites, and robustness suites recursively. I did not execute the suite in this read-only branch research run; many tests intentionally create temp repos/state and are outside the requested evidence-only scope.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell entrypoint for the interactive Humanize monitors. It defines public helper parsers and the `humanize monitor rlcr|pr` command dispatcher, then implements RLCR and PR-loop monitor state machines.
- algorithmic_behavior: The script sources shared monitor primitives from `scripts/lib/monitor-common.sh` at `scripts/humanize.sh:6`. Public helpers split pipe-delimited values for bash/zsh compatibility at `scripts/humanize.sh:18`, parse RLCR `goal-tracker.md` summary counts at `scripts/humanize.sh:32`, detect git repository states at `scripts/humanize.sh:116`, and parse porcelain/diffstat status at `scripts/humanize.sh:166`. `_humanize_monitor_codex` starts at `scripts/humanize.sh:219`: it requires `.humanize/rlcr`, finds the latest timestamped session through `monitor_find_latest_session`, resolves logs from `XDG_CACHE_HOME` or `$HOME/.cache/humanize` at `scripts/humanize.sh:250`, renders an 11-line status bar, tails log deltas, and switches to newer sessions/logs. The dispatcher at `scripts/humanize.sh:884` routes `humanize monitor rlcr` and `humanize monitor pr`. `_humanize_monitor_pr` starts at `scripts/humanize.sh:931`, supports `--once`, watches `.humanize/pr-loop`, selects the latest `round-*-pr-check.md`, `round-*-pr-feedback.md`, or `round-*-pr-comment.md`, and displays PR/bot/phase state.
- inputs_outputs_state: Inputs include the current working directory, `.humanize/rlcr` and `.humanize/pr-loop` directories, session `state.md` or terminal `*-state.md` files, `goal-tracker.md`, cached Codex logs, git status/diff output, terminal dimensions from `tput`, and command arguments. Outputs are terminal UI/status lines, tailed log content, one-shot PR summary text, and exit codes. The monitor mutates only process-local state such as `current_file`, `current_session_dir`, `last_size`, `TAIL_PID`, and cleanup flags; it does not intentionally modify loop state files.
- gates_or_invariants: Missing loop directories return errors at `scripts/humanize.sh:230` and `scripts/humanize.sh:955`. RLCR log selection validates that review logs have later round numbers than run logs; inconsistent ordering returns an error at `scripts/humanize.sh:330`. Status parsing prefers active `state.md` through shared helpers at `scripts/humanize.sh:383`. Terminal cleanup is idempotent and trap-backed at `scripts/humanize.sh:537`, with graceful stop on deleted `.humanize/rlcr` at `scripts/humanize.sh:563`. The log tail loop detects truncation/rotation and resets search at `scripts/humanize.sh:777`. PR `--once` exits with a summary instead of starting the tail loop at `scripts/humanize.sh:1215`.
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh`, `git`, `find`, `stat` with GNU/BSD fallbacks, `sed`, `grep`, `sort`, `tail`, `tput`, `basename`, and shell trap semantics. It is covered by monitor and PR-loop tests: shared monitor helper calls appear in `tests/test-pr-loop-system.sh:770`, state-file detection in `tests/test-pr-loop-system.sh:794`, file size in `tests/test-pr-loop-system.sh:826`, zsh monitor safety in `tests/test-zsh-monitor-safety.sh:63`, and monitor runtime cleanup assertions around `tests/test-monitor-runtime.sh:307`.
- edge_cases_or_failure_modes: Handles zsh array indexing and glob safety via `setopt localoptions ksharrays` and `find` loops. Handles missing logs by rendering a waiting state, deleted sessions by switching or waiting, truncated logs by resetting, missing state files by showing `N/A` or unknown state, non-git dirs with zero status values, and both GNU/macOS `stat`. One notable display edge: RLCR terminal state generated by `end_loop complete` is named `complete-state.md`, while the RLCR status color case recognizes `completed`; that can make a successful terminal state fall through to default coloring even though state detection still works.
- validation_or_tests: Relevant validation lives in `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, `tests/test-pr-loop-system.sh`, `tests/robustness/test-goal-tracker-robustness.sh`, and `tests/robustness/test-git-operations-robustness.sh`. Not executed during this evidence-only pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Focused executable spec for RLCR state-file exit naming and active-loop discovery semantics.
- algorithmic_behavior: The test creates a temp git repo and synthetic `.humanize/rlcr/<timestamp>` directories, sources `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:67`, then directly exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. It asserts terminal state files are not active, active `state.md` is detected, the newest active session wins, valid exit reasons rename `state.md` to `<reason>-state.md`, invalid reasons fail, missing state files fail gracefully, and legacy `.humanize-loop.local` paths are ignored.
- inputs_outputs_state: Inputs are temporary state files written by the test: `complete-state.md` at `tests/test-state-exit-naming.sh:52`, `state.md` at `tests/test-state-exit-naming.sh:78`, `cancel-state.md` at `tests/test-state-exit-naming.sh:100`, `unexpected-state.md` at `tests/test-state-exit-naming.sh:116`, `maxiter-state.md` at `tests/test-state-exit-naming.sh:131`, and `stop-state.md` at `tests/test-state-exit-naming.sh:147`. Outputs are pass/fail counters and process exit status `TESTS_FAILED` at `tests/test-state-exit-naming.sh:302`. State transitions occur only in the temp directory: `end_loop` renames temp `state.md` into terminal files in the loop at `tests/test-state-exit-naming.sh:217`.
- gates_or_invariants: Core invariant: `find_active_loop` must return a directory only when the newest timestamped RLCR directory contains `state.md` or `finalize-state.md`; terminal files such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` are not active. The production implementation confirms this at `hooks/lib/loop-common.sh:155` through `hooks/lib/loop-common.sh:173`. `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, and `unexpected`, enforced at `hooks/lib/loop-common.sh:1118`, and performs the rename at `hooks/lib/loop-common.sh:1130`.
- dependencies_and_callers: Depends on `git` for temp repo initialization, `hooks/lib/loop-common.sh`, and shell utilities. It is included in the master runner at `tests/run-all-tests.sh:39`.
- edge_cases_or_failure_modes: Negative cases cover invalid reason handling at `tests/test-state-exit-naming.sh:186`, missing state file handling at `tests/test-state-exit-naming.sh:232`, legacy path false positives at `tests/test-state-exit-naming.sh:258`, and legacy search isolation at `tests/test-state-exit-naming.sh:267`. This file does not cover `finalize-state.md` active behavior; that is handled in `tests/test-finalize-phase.sh`. The script uses `set -uo pipefail` rather than `set -e`, so failing assertions accumulate through counters rather than aborting immediately.
- validation_or_tests: The file is a validation script and exits with failure count. It verifies the same functions that state validators and stop hooks rely on. Not executed during this research pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template used by validators to deny direct modification of RLCR `state.md`, preserving loop state integrity.
- algorithmic_behavior: Static markdown block with the heading at `prompt-template/block/state-file-modification.md:1`, the denial message at `prompt-template/block/state-file-modification.md:3`, a short list of protected state contents at `prompt-template/block/state-file-modification.md:5`, and corruption rationale at `prompt-template/block/state-file-modification.md:10`. It is rendered by `state_file_blocked_message` in `hooks/lib/loop-common.sh:458`, which uses `load_and_render_safe` with this template path at `hooks/lib/loop-common.sh:463`.
- inputs_outputs_state: Inputs are only template-loading context from `TEMPLATE_DIR`; the file has no variables. Output is markdown sent to stderr/stdout by hook validators when an attempted Write/Edit/Bash operation targets `state.md`. It performs no state transition itself; its role is to block unsafe transitions before they can alter the managed state file.
- gates_or_invariants: Enforces the invariant that loop state is system-managed and not agent-editable. The associated path predicate is `is_state_file_path` at `hooks/lib/loop-common.sh:504`. The hook callers render this block from `hooks/loop-write-validator.sh:158`, `hooks/loop-edit-validator.sh:123`, and several Bash-validator branches including `hooks/loop-bash-validator.sh:154`, `hooks/loop-bash-validator.sh:301`, and `hooks/loop-bash-validator.sh:325`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` indirectly through `loop-common.sh`. Referenced by template coverage at `tests/test-template-references.sh:155`. Runtime callers are RLCR Write/Edit/Bash validators; separate templates exist for finalize-state and PR-loop state protection, so this file is specifically scoped to RLCR `state.md`.
- edge_cases_or_failure_modes: The block is intentionally minimal and variable-free, so rendering cannot fail due to placeholder substitution. If the template is missing, `state_file_blocked_message` supplies a shorter inline fallback. It does not itself mention cancel authorization or finalize-state, so those paths rely on separate validator logic/templates.
- validation_or_tests: Covered indirectly by template-reference and validator suites: `tests/test-template-references.sh`, `tests/test-allowlist-validators.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-cancel-signal-file.sh`, and finalize-phase tests for sibling protection. Not executed during this research pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-127 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for `scripts/humanize.sh`, centralizing session discovery, state-file status detection, terminal helpers, PR-loop phase detection, and goal-tracker parsing.
- algorithmic_behavior: Defines ANSI color helpers at `scripts/lib/monitor-common.sh:14`, cross-platform file-size lookup at `scripts/lib/monitor-common.sh:32`, timestamped latest-session discovery at `scripts/lib/monitor-common.sh:40`, terminal scroll-region setup/restore at `scripts/lib/monitor-common.sh:71` and `scripts/lib/monitor-common.sh:86`, status-to-color mapping at `scripts/lib/monitor-common.sh:130`, state-file detection at `scripts/lib/monitor-common.sh:154`, YAML frontmatter value extraction at `scripts/lib/monitor-common.sh:197`, timestamp formatting at `scripts/lib/monitor-common.sh:218`, string truncation at `scripts/lib/monitor-common.sh:233`, PR-loop phase detection at `scripts/lib/monitor-common.sh:267`, PR phase display text at `scripts/lib/monitor-common.sh:346`, RLCR goal-tracker parsing at `scripts/lib/monitor-common.sh:390`, and PR goal-tracker issue parsing at `scripts/lib/monitor-common.sh:468`.
- inputs_outputs_state: Inputs are loop/session directory paths, state files, `round-*-pr-check.md` files, goal tracker markdown, timestamps, strings, and status labels. Outputs are scalar shell strings: latest session path, `state_file|status`, phase keys, display text, counts, truncated strings, and formatted timestamps. The only persistent side effect is PR phase growth tracking: `get_pr_loop_phase` writes a size cache under `/tmp/humanize-phase-<session>-<round>.size` at `scripts/lib/monitor-common.sh:297` through `scripts/lib/monitor-common.sh:306`.
- gates_or_invariants: Latest sessions must match `YYYY-MM-DD_HH-MM-SS` at `scripts/lib/monitor-common.sh:55`. `monitor_find_state_file` gives active `state.md` priority at `scripts/lib/monitor-common.sh:162`, otherwise reports the first terminal `*-state.md` status at `scripts/lib/monitor-common.sh:168`, and returns unknown for missing inputs. PR phase detection checks terminal states first (`approve-state.md`, `cancel-state.md`, `maxiter-state.md`) at `scripts/lib/monitor-common.sh:272`, then treats growing or recently modified `round-*-pr-check.md` as Codex analysis at `scripts/lib/monitor-common.sh:315`, then falls back to `state.md` round/startup-case to distinguish initial review from reviewer polling at `scripts/lib/monitor-common.sh:321`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at `scripts/humanize.sh:6`. RLCR monitor uses `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` at `scripts/humanize.sh:239`, `scripts/humanize.sh:346`, and `scripts/humanize.sh:612`. PR monitor uses state/phase and goal-tracker helpers at `scripts/humanize.sh:964`, `scripts/humanize.sh:1017`, `scripts/humanize.sh:1101`, and `scripts/humanize.sh:1142`. Tests directly exercise these helpers in `tests/test-pr-loop-system.sh:770`, `tests/test-pr-loop-system.sh:794`, and `tests/test-pr-loop-system.sh:826`.
- edge_cases_or_failure_modes: Handles absent directories/files by returning empty or unknown values, GNU/BSD `stat` differences, missing goal trackers, no PR activity files, stale phase cache, terminal state precedence, and malformed or partial frontmatter by using simple grep/sed defaults. The `find` loops avoid shell glob failures in zsh and empty directories. Phase detection’s `/tmp` cache can be unwritable in restricted environments, but writes are ignored with `|| true`, leaving mtime as fallback.
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh` shared monitor tests and phase-output tests, `tests/test-zsh-monitor-safety.sh`, `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, and robustness goal-tracker/git suites through `scripts/humanize.sh`. Not executed during this evidence-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence headings present; each assigned item is represented once in `Item Evidence`.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`