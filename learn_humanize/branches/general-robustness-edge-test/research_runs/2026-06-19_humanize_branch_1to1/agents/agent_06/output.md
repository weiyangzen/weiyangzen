# agent_06 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is the repository’s executable backend for Humanize loop orchestration. It contains setup/cancel state-machine scripts for RLCR and PR loops, GitHub PR comment/review/reaction ingestion, reviewer-status classification, monitor rendering, gen-plan IO validation, and cross-platform timeout support. Command markdown files call into these scripts directly, for example `commands/start-pr-loop.md:4` allows only `scripts/setup-pr-loop.sh`, and `commands/start-rlcr-loop.md:4` allows only `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The directory splits into several coordinated child roles. `scripts/setup-rlcr-loop.sh` creates an RLCR loop session from a plan file, validates git and plan-file constraints, writes `.humanize/rlcr/<timestamp>/state.md`, backs up `plan.md`, initializes `goal-tracker.md`, and emits the round-0 prompt (`scripts/setup-rlcr-loop.sh:628`, `scripts/setup-rlcr-loop.sh:650`, `scripts/setup-rlcr-loop.sh:750`). `scripts/setup-pr-loop.sh` creates a PR review loop, resolves the PR/base repository, fetches initial comments, classifies startup case, optionally posts re-review triggers, writes `.humanize/pr-loop/<timestamp>/state.md`, creates a goal tracker, and emits the round-0 prompt (`scripts/setup-pr-loop.sh:283`, `scripts/setup-pr-loop.sh:412`, `scripts/setup-pr-loop.sh:419`, `scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:681`). `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh` implement the GitHub API data plane for PR-loop transitions. `scripts/humanize.sh` exposes the `humanize monitor rlcr|pr` dispatcher and renders status/tails for loop sessions (`scripts/humanize.sh:884`, `scripts/humanize.sh:931`). `scripts/lib/monitor-common.sh` contains reusable monitor parsers and PR phase detection (`scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:267`). `scripts/validate-gen-plan-io.sh` validates and composes draft input with the gen-plan template (`scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:148`).
- inputs_outputs_state: Inputs are command arguments, environment variables such as `CLAUDE_PROJECT_DIR`, git state, GitHub CLI/API responses, Codex availability, prompt templates, plan/draft files, and existing loop directories. Outputs are loop session directories under `.humanize/rlcr` or `.humanize/pr-loop`, state files (`state.md`, `finalize-state.md`, `approve-state.md`, `cancel-state.md`, `maxiter-state.md`, etc.), round prompt/comment/summary/check files, Markdown comments output, JSON poll/reviewer status output, terminal monitor displays, and gen-plan output files. State transitions are file-name based: setup creates active `state.md`; cancel scripts create `.cancel-requested` and rename active state to `cancel-state.md` (`scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`; `scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`); hooks later consume script-created state and move it to terminal state files such as `approve-state.md` or `complete-state.md` (`hooks/pr-loop-stop-hook.sh:476`, `hooks/loop-codex-stop-hook.sh:718`).
- gates_or_invariants: The directory enforces one active loop at a time across RLCR and PR loops by checking both loop roots before setup (`scripts/setup-pr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:211`). RLCR setup rejects absolute plan paths, whitespace, shell metacharacters, symlinks, path escape outside project, submodules, insufficient content, tracked/untracked policy violations, missing git commits, invalid model/effort, unsafe branch/base names, and missing local base branch (`scripts/setup-rlcr-loop.sh:273`, `scripts/setup-rlcr-loop.sh:287`, `scripts/setup-rlcr-loop.sh:299`, `scripts/setup-rlcr-loop.sh:347`, `scripts/setup-rlcr-loop.sh:400`, `scripts/setup-rlcr-loop.sh:431`, `scripts/setup-rlcr-loop.sh:537`). PR setup requires at least one bot, git repo, `gh`, authenticated GitHub CLI, `codex`, numeric PR, open PR, YAML-safe branch/model/effort, and for Case 4/5 trigger comments it aborts and removes the loop directory if trigger posting or Claude eyes verification fails (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:334`, `scripts/setup-pr-loop.sh:340`, `scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:449`, `scripts/setup-pr-loop.sh:501`, `scripts/setup-pr-loop.sh:507`). `portable-timeout.sh` centralizes timeout behavior with `gtimeout`, GNU `timeout`, Python, or a no-timeout fallback (`scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:33`).
- dependencies_and_callers: Command wrappers in `commands/*.md` invoke the setup/cancel/validate scripts. Stop hooks call `scripts/portable-timeout.sh`, PR poll/reaction/status scripts, and consume state created by setup scripts (`hooks/pr-loop-stop-hook.sh:58`, `hooks/pr-loop-stop-hook.sh:459`, `hooks/pr-loop-stop-hook.sh:801`, `hooks/pr-loop-stop-hook.sh:1534`; `hooks/loop-codex-stop-hook.sh:52`). Setup scripts source `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh` for active-loop checks, bot mention/YAML helpers, and safe template rendering (`scripts/setup-pr-loop.sh:38`, `scripts/setup-pr-loop.sh:211`; `scripts/setup-rlcr-loop.sh:204`). Tests reference almost every script path through PR-loop, plan-file, monitor, robustness, and gen-plan suites, including `tests/test-pr-loop.sh`, `tests/test-plan-file-validation.sh`, `tests/test-pr-loop-system.sh`, and robustness tests.
- edge_cases_or_failure_modes: Fork PR support is a recurring edge case: PR scripts resolve base/upstream repositories before API calls (`scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`, `scripts/check-pr-reviewer-status.sh:109`, `scripts/check-bot-reactions.sh:119`). Partial API failure is handled non-fatally in fetch/poll with retry counters and empty arrays (`scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:198`, `scripts/fetch-pr-comments.sh:445`; `scripts/poll-pr-reviews.sh:209`, `scripts/poll-pr-reviews.sh:230`). Timestamp filtering is lexical ISO comparison in several scripts; this assumes normalized ISO 8601 `Z` timestamps. Monitors must handle missing loop roots, deleted sessions, missing/truncated log files, zsh glob behavior, and terminal cleanup (`scripts/humanize.sh:623`, `scripts/humanize.sh:777`, `scripts/humanize.sh:797`, `scripts/humanize.sh:955`, `scripts/humanize.sh:1215`). `monitor-common.sh` writes temporary size cache files under `/tmp` for phase detection (`scripts/lib/monitor-common.sh:297`), which can fail silently in restricted environments.
- validation_or_tests: The directory is covered by script argument tests (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`), PR hook/state tests (`tests/test-pr-loop-hooks.sh:26`, `tests/test-pr-loop-hooks.sh:222`, `tests/test-pr-loop-hooks.sh:326`, `tests/test-pr-loop-hooks.sh:1242`), PR stop-hook integration tests (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:327`, `tests/test-pr-loop-stophook.sh:452`), gen-plan validation tests (`tests/test-gen-plan.sh:426`), plan/base/path validation suites, monitor tests, and robustness suites. I did not run them because the branch export is read-only and these tests create temp repos/files and mock binaries.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-036 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: Fetches and normalizes all PR feedback into a single Markdown artifact for PR-loop prompts. It is the first-round comment ingestion step called by `scripts/setup-pr-loop.sh:412`, and fixture tests exercise its output formatting through `tests/test-pr-loop-hooks.sh:1242`.
- algorithmic_behavior: The script parses `<pr_number> <output_file>`, optional `--after`, and optional `--bots` (`scripts/fetch-pr-comments.sh:27`). It validates required/numeric arguments and `gh`/`jq` availability (`scripts/fetch-pr-comments.sh:92`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:117`). It resolves the PR’s base repository, including fork fallback through parent repo lookup (`scripts/fetch-pr-comments.sh:126`, `scripts/fetch-pr-comments.sh:138`, `scripts/fetch-pr-comments.sh:142`). It fetches three endpoint families: issue comments, inline PR review comments, and PR reviews (`scripts/fetch-pr-comments.sh:211`, `scripts/fetch-pr-comments.sh:215`, `scripts/fetch-pr-comments.sh:219`). Each family is transformed into a common JSON object shape, including `type`, `id`, `author`, `author_type`, `created_at`, `body`, optional `path`/`line`, and optional `state` (`scripts/fetch-pr-comments.sh:260`, `scripts/fetch-pr-comments.sh:280`, `scripts/fetch-pr-comments.sh:302`). Empty PR review bodies are preserved as state placeholders instead of being dropped (`scripts/fetch-pr-comments.sh:300`, `scripts/fetch-pr-comments.sh:311`). Combined comments are deduplicated by `id`, optionally filtered by timestamp, sorted with humans before bots and newest first, and rendered as Markdown sections (`scripts/fetch-pr-comments.sh:321`, `scripts/fetch-pr-comments.sh:326`, `scripts/fetch-pr-comments.sh:336`, `scripts/fetch-pr-comments.sh:357`, `scripts/fetch-pr-comments.sh:394`).
- inputs_outputs_state: Inputs are PR number, output path, optional timestamp, optional comma-separated bot list, GitHub CLI auth/context, GitHub API JSON, `jq`, temp directory via `mktemp`, and system UTC date. Output is a Markdown file beginning with PR metadata and ending with `*End of comments*` (`scripts/fetch-pr-comments.sh:245`, `scripts/fetch-pr-comments.sh:442`). It does not mutate loop state directly, but the created Markdown file becomes `.humanize/pr-loop/<timestamp>/round-0-pr-comment.md` when called from setup. It writes warning text into the output if any endpoint exhausts retries (`scripts/fetch-pr-comments.sh:445`).
- gates_or_invariants: Hard gates include required PR number and output file, numeric PR number, `gh` installed, `jq` installed, current repo resolution, base repo resolution, and valid owner/name extraction (`scripts/fetch-pr-comments.sh:93`, `scripts/fetch-pr-comments.sh:98`, `scripts/fetch-pr-comments.sh:103`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:117`, `scripts/fetch-pr-comments.sh:131`, `scripts/fetch-pr-comments.sh:138`, `scripts/fetch-pr-comments.sh:158`). Fetch operations are not hard gates after setup: each API endpoint retries three times and then writes `[]` while incrementing `API_FAILURES` (`scripts/fetch-pr-comments.sh:175`, `scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:198`). Sorting invariant is human feedback first, then bot feedback, each newest first (`scripts/fetch-pr-comments.sh:333`). Bot grouping maps `codex` specially to `chatgpt-codex-connector[bot]`; other bots map to `<bot>[bot]` (`scripts/fetch-pr-comments.sh:381`, `scripts/fetch-pr-comments.sh:385`).
- dependencies_and_callers: Depends on `gh`, `jq`, `mktemp`, `sed`, `date`, and standard shell utilities. Called by PR setup to seed round-0 comments (`scripts/setup-pr-loop.sh:411`). Related behavior is mirrored by `scripts/poll-pr-reviews.sh`, which returns JSON instead of Markdown for later rounds. Tests invoke it directly for argument validation (`tests/test-pr-loop-scripts.sh:259`), fixture-backed comment coverage (`tests/test-pr-loop-hooks.sh:1242`), timestamp filtering (`tests/test-pr-loop-hooks.sh:1287`), and robustness/API failure behavior (`tests/robustness/test-pr-loop-api-robustness.sh`, referenced by `rg` output).
- edge_cases_or_failure_modes: Fork PRs require base repo resolution; failure to resolve current/parent repo exits before output completion. `--after` compares strings (`.created_at > $after`) and assumes normalized ISO timestamps (`scripts/fetch-pr-comments.sh:326`). `unique_by(.id)` can collapse different GitHub object types if numeric IDs collide across endpoint families (`scripts/fetch-pr-comments.sh:321`). API failures are non-fatal, so downstream prompts may be based on incomplete feedback; the file records this warning. Null `created_at` entries are discarded before sorting to avoid `fromdateiso8601` errors (`scripts/fetch-pr-comments.sh:335`). The helper `is_bot` and `format_timestamp` are defined but not used in final rendering (`scripts/fetch-pr-comments.sh:225`, `scripts/fetch-pr-comments.sh:237`).
- validation_or_tests: Argument gates are tested for help, missing PR number, missing output path, and invalid PR number (`tests/test-pr-loop-scripts.sh:268`, `tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`). Fixture-backed tests require that issue comments, inline comments, and approval-only reviews appear in output (`tests/test-pr-loop-hooks.sh:1242`) and that `--after` excludes early comments while preserving later approval-only reviews (`tests/test-pr-loop-hooks.sh:1287`). Unit-style tests assert timestamp sorting, human-before-bot sorting, deduplication, and empty-body review placeholders (`tests/test-pr-loop-hooks.sh:222`, `tests/test-pr-loop-hooks.sh:239`, `tests/test-pr-loop-hooks.sh:271`, `tests/test-pr-loop-hooks.sh:1160`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-066 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: This is the top-level executable specification for the PR loop. It is intentionally a thin runner that initializes a shared mock/test environment and delegates to script, hook, and stop-hook test modules (`tests/test-pr-loop.sh:18`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`).
- algorithmic_behavior: The runner sets strict shell mode, derives `SCRIPT_DIR` and `PROJECT_ROOT`, sources `test-helpers.sh` plus `test-pr-loop-lib.sh`, initializes mocks with `init_pr_loop_test_env`, sources the three PR-loop test modules, then calls `run_script_tests`, `run_hook_tests`, and `run_stophook_tests` in order (`tests/test-pr-loop.sh:13`, `tests/test-pr-loop.sh:15`, `tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`). It finishes by calling `print_test_summary`, whose return code reflects accumulated failures (`tests/test-pr-loop.sh:53`; `tests/test-pr-loop-lib.sh:131`).
- inputs_outputs_state: Inputs are the repository checkout, Bash, `jq`, git/gh/codex mocks created under the test temp directory, fixture JSON, production scripts/hooks, and helper functions. The runner’s output is TAP-like human-readable pass/fail logging via sourced helper functions and a final summary. It creates and mutates temp test directories through helpers, not production state. Because it sources modules rather than executing them as subprocesses, module functions share the same shell globals such as `TEST_DIR`, `PROJECT_ROOT`, and counters.
- gates_or_invariants: The runner requires successful sourcing of helpers/modules and a mock environment before executing tests. The delegated tests encode important PR-loop invariants: setup requires bot flags and parses `--max`, `--codex-model`, `--codex-timeout` (`tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:104`, `tests/test-pr-loop-scripts.sh:131`); cancel renames active state to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:211`); fetch/poll reject malformed arguments (`tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:366`); state/comment files are protected while `round-N-pr-resolve.md` is writable (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`); trigger comments, startup cases, unpushed commits, force-push detection, and bot timeouts are enforced by stop-hook tests (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:327`, `tests/test-pr-loop-stophook.sh:452`, `tests/test-pr-loop-stophook.sh:538`, `tests/test-pr-loop-stophook.sh:642`, `tests/test-pr-loop-stophook.sh:754`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh`. The library creates mock `gh` and `codex` commands and inserts them into `PATH` (`tests/test-pr-loop-lib.sh:27`, `tests/test-pr-loop-lib.sh:93`, `tests/test-pr-loop-lib.sh:111`). The modules directly invoke production scripts and hooks, including `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: Because modules are sourced into one shell, global variables and changed directories must be reset carefully; many tests `cd "$SCRIPT_DIR"` after temp work. The mock `gh` behavior is intentionally partial, so some tests validate parsing or block conditions rather than full GitHub parity. Some stop-hook tests use external `timeout` directly (`tests/test-pr-loop-stophook.sh:433`), which can be platform-sensitive compared with the project’s own `portable-timeout.sh`. The suite writes temp files and mock executables; it is unsuitable to run in the current read-only branch export.
- validation_or_tests: This file is itself the validation entry point. Its sourced script module covers argument validation and cancel/fetch/poll gates (`tests/test-pr-loop-scripts.sh:405`). Its hook module covers YAML bot lists, protected file writes, sorting, dedupe, bot-status extraction, trigger logic, approval-only review capture, fixture-backed fetch/poll behavior, and monitor once-mode (`tests/test-pr-loop-hooks.sh:26`, `tests/test-pr-loop-hooks.sh:222`, `tests/test-pr-loop-hooks.sh:326`, `tests/test-pr-loop-hooks.sh:1160`, `tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1558`). Its stop-hook module covers force-push rejection, Case 1 no-trigger exception, approve-state creation, dynamic startup-case updates, unpushed commit blocks, trigger requirements, and timeout handling (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:246`, `tests/test-pr-loop-stophook.sh:327`, `tests/test-pr-loop-stophook.sh:452`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-096 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for stale RLCR state schema detection. It is a small but core review/transition contract: when an active loop lacks required state fields, validators/hooks render this message and block or terminate the loop rather than continuing with ambiguous state.
- algorithmic_behavior: The template declares that the RLCR loop state file is missing required field `{{FIELD_NAME}}`, explains that the loop was started with an older Humanize version, and tells the user to cancel, update to version `1.1.2+`, and restart (`prompt-template/block/schema-outdated.md:1`, `prompt-template/block/schema-outdated.md:3`, `prompt-template/block/schema-outdated.md:7`). It uses the repository’s `{{VARIABLE_NAME}}` placeholder syntax rendered by `hooks/lib/template-loader.sh`, which performs single-pass substitution and leaves missing placeholders intact (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:170`).
- inputs_outputs_state: Input is `FIELD_NAME` supplied by the calling validator. Output is Markdown text embedded in a JSON block decision by callers such as `hooks/loop-plan-file-validator.sh:60`. It does not mutate state itself. Its operational effect is to stop a stale state machine from advancing: the validator returns `{"decision":"block"}` with the rendered reason (`hooks/loop-plan-file-validator.sh:71`).
- gates_or_invariants: The template supports the invariant that required schema fields must exist before hooks trust loop state. `hooks/loop-plan-file-validator.sh` uses it when `plan_tracked` or `start_branch` is missing (`hooks/loop-plan-file-validator.sh:79`). `hooks/lib/loop-common.sh` also defines strict schema checks for `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:284`). `hooks/loop-codex-stop-hook.sh` has inline schema-outdated blocks for missing `plan_tracked`/`start_branch`, invalid `review_started`, and missing `base_branch` (`hooks/loop-codex-stop-hook.sh:157`, `hooks/loop-codex-stop-hook.sh:176`, `hooks/loop-codex-stop-hook.sh:194`).
- dependencies_and_callers: Directly loaded by `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md" ... "FIELD_NAME=$field_name"` in `hooks/loop-plan-file-validator.sh:65`. Its template directory validity is checked by `hooks/lib/template-loader.sh:205`. It is referenced by search alongside other block templates, but this specific file is narrowly tied to schema validation.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline message with the same field-name variable (`hooks/loop-plan-file-validator.sh:62`, `hooks/lib/template-loader.sh:179`). If `FIELD_NAME` is not supplied, the placeholder remains visible, which is safer than silently naming the wrong field. The text mentions version `1.1.2+`, while some newer schema gates in `loop-codex-stop-hook.sh` refer to pre-`1.5.0` fields; this template is therefore accurate for the older `plan_tracked`/`start_branch` gate but not the only schema-outdated message in the system.
- validation_or_tests: Schema validation behavior is exercised indirectly by plan-file validator and hook tests. The key behavior is visible in `hooks/loop-plan-file-validator.sh:59`, where the template is rendered and JSON-escaped, then emitted as a block decision. Template loader tests cover safe fallback/substitution behavior elsewhere in the suite; this specific template is small enough that correctness is mostly governed by caller tests.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-126 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template for the “no review comments yet” startup path. It defines the initial agent contract when setup finds no existing PR feedback: do not trigger bots manually, write a round-0 resolution summary, then let the Stop Hook poll for automatic reviews.
- algorithmic_behavior: The template tells the agent that monitored bots `{{ACTIVE_BOTS_DISPLAY}}` will review automatically and that no comment is needed to trigger the first review (`prompt-template/pr-loop/round-0-task-no-comments.md:6`). It instructs the agent to write the initial summary to `@{{RESOLVE_PATH}}`, mark it as Round 0 awaiting initial bot reviews, and avoid modifying `.humanize/pr-loop` state files (`prompt-template/pr-loop/round-0-task-no-comments.md:8`, `prompt-template/pr-loop/round-0-task-no-comments.md:12`, `prompt-template/pr-loop/round-0-task-no-comments.md:18`). It also documents the subsequent Stop Hook transition: poll every 30 seconds up to 15 minutes per bot, validate approvals with local Codex, continue on issues, end on all approvals (`prompt-template/pr-loop/round-0-task-no-comments.md:26`).
- inputs_outputs_state: Inputs are template variables from `scripts/setup-pr-loop.sh`: PR number, branch, active bot display, resolve path, and bot mention string (`scripts/setup-pr-loop.sh:651`). Output is rendered Markdown appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` when `COMMENT_COUNT=0` (`scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:687`, `scripts/setup-pr-loop.sh:720`, `scripts/setup-pr-loop.sh:771`). The expected user/agent output is `round-0-pr-resolve.md`, which the PR stop hook later requires (`hooks/pr-loop-stop-hook.sh:262`).
- gates_or_invariants: This template encodes a key startup invariant: Case 1, round 0, with no prior comments does not require an explicit trigger comment. The stop-hook tests assert that exception (`tests/test-pr-loop-stophook.sh:140`), and `hooks/pr-loop-stop-hook.sh` documents trigger requirements by startup case and round (`hooks/pr-loop-stop-hook.sh:678`). It also reinforces system-owned state: agents may write the resolve summary but must not edit `.humanize/pr-loop` state/comment/prompt files, matching validator behavior that blocks state and comment writes while allowing `round-N-pr-resolve.md` (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`).
- dependencies_and_callers: Loaded by `scripts/setup-pr-loop.sh` via `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-task-no-comments.md" ...` (`scripts/setup-pr-loop.sh:720`). Template loading is provided by `hooks/lib/template-loader.sh`, sourced in setup (`scripts/setup-pr-loop.sh:38`). It pairs with `prompt-template/pr-loop/critical-requirements-no-comments.md`, which setup emits after the initial prompt for the same no-comments path (`scripts/setup-pr-loop.sh:812`, `scripts/setup-pr-loop.sh:834`).
- edge_cases_or_failure_modes: The setup script determines “no comments” by checking for the exact sentinel line `*No comments found.*` in the generated comment file (`scripts/setup-pr-loop.sh:643`). If comment fetch partially fails and returns an incomplete but non-empty file, setup may choose the has-comments template instead. If the no-comments template is missing, setup falls back to an inline equivalent (`scripts/setup-pr-loop.sh:689`, `scripts/setup-pr-loop.sh:720`). If bots do not auto-review, the Stop Hook timeout path removes timed-out bots from `active_bots`, and may eventually approve if no active bots remain (`hooks/pr-loop-stop-hook.sh:913`, `hooks/pr-loop-stop-hook.sh:1060`, `hooks/pr-loop-stop-hook.sh:1110`).
- validation_or_tests: Stop-hook tests verify the no-trigger exception for `startup_case=1`, `current_round=0` (`tests/test-pr-loop-stophook.sh:140`). Hook tests validate that `round-0` falls back to `started_at` when no trigger exists, but prefers `last_trigger_at` when present (`tests/test-pr-loop-hooks.sh:624`, `tests/test-pr-loop-hooks.sh:646`). File-protection tests verify the template’s “do not modify state files” contract by blocking state/comment writes and allowing resolve writes (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence sections; item identifiers are present only in their section headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`