# agent_02 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: The `agents` directory is a Claude/plugin subagent prompt surface. In this branch it contains one recursive child, `agents/draft-relevance-checker.md`, which defines the dedicated repository relevance classifier used by `/humanize:gen-plan`.
- algorithmic_behavior: `agents/draft-relevance-checker.md:1-6` declares agent metadata: `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep`. Its task contract at `agents/draft-relevance-checker.md:14-29` is a three-stage classifier: quickly inspect repository docs/structure, compare draft content against repo concepts/files/features, then return exactly one verdict prefix, `RELEVANT:` or `NOT_RELEVANT:`. The command surface invokes it in `commands/gen-plan.md:50-72`, where relevance failure stops plan generation and relevance success advances to deeper draft analysis.
- inputs_outputs_state: Input is draft document content supplied by the `gen-plan` command after IO validation, plus read-only repository exploration through the declared tools. Output is a textual verdict with a required prefix and brief explanation, specified at `agents/draft-relevance-checker.md:27-29`. There is no persistent state owned by this directory; it is a prompt-time decision gate.
- gates_or_invariants: The relevance gate is deliberately lenient: `agents/draft-relevance-checker.md:33-36` says informal, multilingual, or rough drafts are acceptable and doubt should resolve to relevant. The invariant is semantic relationship to the current repo, not exact path or syntax matching. The command depends on a `NOT_RELEVANT:` result to stop, as shown in `commands/gen-plan.md:67-72`.
- dependencies_and_callers: Primary caller is `commands/gen-plan.md:57-65`, which asks the Task tool to invoke `humanize:draft-relevance-checker` with model `haiku`. Tests in `tests/test-gen-plan.sh:111-156` validate that the agent file exists and has the expected name, model, and tools frontmatter. Additional checks in the same test file validate naming and frontmatter conventions later in the file, according to repository search results.
- edge_cases_or_failure_modes: Ambiguous drafts are intentionally biased toward `RELEVANT`, reducing false negatives but allowing potentially broad drafts through. A malformed response without the required verdict prefix would be risky because the command text expects prefix-based branching but does not define a robust parser in the prompt itself. Missing/renamed agent metadata would break the `humanize:draft-relevance-checker` invocation or fail tests. The agent has read/search tools only, so it cannot mutate repository state.
- validation_or_tests: `tests/test-gen-plan.sh:111-156` covers existence, `name`, `model`, and `tools`. The command also separately documents the invocation contract in `commands/gen-plan.md:57-65`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for PR loops. It creates the cancellation signal consumed by PR-loop polling and moves active loop state out of `state.md`, thereby transitioning the newest PR loop from active to cancelled.
- algorithmic_behavior: The script parses `--force` and help at `scripts/cancel-pr-loop.sh:24-66`; `--force` currently only toggles a variable and has no behavioral effect. It chooses the project root from `CLAUDE_PROJECT_DIR` or `pwd` at `scripts/cancel-pr-loop.sh:72`, then scans `.humanize/pr-loop/*/`, sorts descending, and takes the newest directory at `scripts/cancel-pr-loop.sh:73-76`. It requires `state.md` to exist at `scripts/cancel-pr-loop.sh:88-98`. On success it extracts `current_round`, `max_iterations`, and `pr_number` from state YAML-like lines at `scripts/cancel-pr-loop.sh:104-112`, touches `.cancel-requested`, moves `state.md` to `cancel-state.md`, and emits `CANCELLED` plus a human-readable summary at `scripts/cancel-pr-loop.sh:118-130`.
- inputs_outputs_state: Inputs are CLI flags, current working directory or `CLAUDE_PROJECT_DIR`, and the newest `.humanize/pr-loop/<timestamp>/state.md`. Outputs are stdout sentinel lines `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`; exit codes `0`, `1`, or `3`; filesystem state `.cancel-requested` and `cancel-state.md`. State transition is active PR loop `state.md` -> cancelled loop `cancel-state.md`, with `.cancel-requested` as an authorization/polling signal.
- gates_or_invariants: Only PR loops are affected; help text explicitly excludes `.humanize/rlcr/` at `scripts/cancel-pr-loop.sh:54-56`. A loop is active only if `state.md` exists in the newest PR-loop directory, matching `commands/cancel-pr-loop.md:21`. Unknown options exit `3` at `scripts/cancel-pr-loop.sh:60-64`. No directory means `NO_LOOP` and exit `1`; directory without `state.md` means `NO_ACTIVE_LOOP` and exit `1`.
- dependencies_and_callers: Slash-command wrapper `commands/cancel-pr-loop.md:1-25` allows running this script and instructs the assistant to branch on the first output line. `scripts/setup-pr-loop.sh:100`, `scripts/setup-pr-loop.sh:239`, and `scripts/setup-pr-loop.sh:801` reference `/humanize:cancel-pr-loop` as the operator path. `hooks/pr-loop-stop-hook.sh:941-945` observes `.cancel-requested` and exits its polling loop. The bash validator has a cancellation exception at `hooks/loop-bash-validator.sh:133-142`; detailed authorization lives in `hooks/lib/loop-common.sh:527-704`, requiring the signal file and a tightly shaped `mv state.md|finalize-state.md cancel-state.md` command.
- edge_cases_or_failure_modes: Newest-directory selection uses lexical reverse sort; it assumes timestamped directory names sort newest-first. If `state.md` is missing but `finalize-state.md` or `cancel-state.md` exists, PR cancel reports no active loop; unlike RLCR cancellation, this PR script does not handle `finalize-state.md`. `touch` can create the signal before `mv`; if `mv` fails under `set -e`, a stale `.cancel-requested` may remain. Metadata extraction is grep/sed-based and defaults missing fields to `?`. `--force` is documented as no-op. If `LOOP_BASE_DIR` has unusual names or spaces, the unquoted command substitution around `ls | sort | head` may be fragile, though later variable use is quoted.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:181-252` covers help output, no-loop behavior with `NO_LOOP`/exit `1`, and active cancellation moving `state.md` to `cancel-state.md`. Additional security-oriented tests around cancel authorization are referenced by repository search in `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, focused on validator acceptance of the signal-backed move.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-062 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared PR-loop test harness library. It supplies mock external commands, a common temporary test environment, and summary behavior for the PR-loop test runner and modules.
- algorithmic_behavior: The file is guarded by `TEST_PR_LOOP_LIB_LOADED` at `tests/test-pr-loop-lib.sh:10-12`, preventing duplicate definitions when sourced multiple times. It resolves `SCRIPT_DIR` and `PROJECT_ROOT` if unset at `tests/test-pr-loop-lib.sh:14-16` and sources `tests/test-helpers.sh` when `setup_test_dir` is not already defined at `tests/test-pr-loop-lib.sh:18-21`. `create_mock_gh` writes an executable mock `gh` at `tests/test-pr-loop-lib.sh:27-91`; the mock handles auth status, repo owner/name queries, PR view variants, PR commit timestamps, state queries, user API, and default empty API arrays. `create_mock_codex` writes an executable `codex` mock at `tests/test-pr-loop-lib.sh:93-104`. `init_pr_loop_test_env` builds a temp dir, prepends mock bin to `PATH`, creates mocks, and exports `MOCK_BIN_DIR` at `tests/test-pr-loop-lib.sh:110-124`. `print_test_summary` reports pass/fail counts and returns nonzero on failures at `tests/test-pr-loop-lib.sh:130-147`.
- inputs_outputs_state: Inputs are the test module environment and a caller-provided mock directory path for mock creation functions. Outputs are generated mock executables under `$TEST_DIR/mock_bin`, mutated `PATH`, exported `MOCK_BIN_DIR`, and shell functions in the sourcing process. Test counters `TESTS_PASSED` and `TESTS_FAILED` are read by `print_test_summary`. The harness state is process-local plus temporary filesystem state.
- gates_or_invariants: Idempotent loading is the main invariant. The mock `gh` returns deterministic JSON/timestamps so PR-loop tests do not depend on GitHub network state. Any unhandled `gh` command prints an error and exits `1` at `tests/test-pr-loop-lib.sh:87-88`, forcing tests to update mocks when production scripts add new CLI calls. The summary gate returns `1` if any test failed.
- dependencies_and_callers: Main caller is `tests/test-pr-loop.sh:18-34`, which sources helpers and this library, calls `init_pr_loop_test_env`, then sources PR-loop script/hook/stophook test modules. It depends on `tests/test-helpers.sh:84-89` for `setup_test_dir` and on `tests/test-helpers.sh:30-44` for pass/fail counters used by modules. The mock behavior supports script tests such as `tests/test-pr-loop-scripts.sh`, including cancellation tests at `tests/test-pr-loop-scripts.sh:181-252`.
- edge_cases_or_failure_modes: The mock `gh api user` returns plain `testuser`, not JSON, so it only supports production code expecting that shape. The PR view mock branches on substring matches in `$*`, so a new `gh pr view` query shape can accidentally hit a broader branch or fall through. `PATH` is prepended globally for the test process and is not restored in this library. The load guard also means changed function definitions are not refreshed if the file is sourced again in the same shell.
- validation_or_tests: This file is itself infrastructure rather than a direct test target. It is exercised transitively by `tests/test-pr-loop.sh:18-53`; successful PR-loop test execution depends on `init_pr_loop_test_env` and `print_test_summary`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-092 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR loop plan-file immutability gate. It explains why the stop hook blocks progress when a tracked plan file differs from its loop-start backup.
- algorithmic_behavior: The template renders `{{PLAN_FILE}}` and `{{BACKUP_PATH}}` into a concise block message. It states the plan file was modified since loop start at `prompt-template/block/plan-file-modified.md:3`, declares modification forbidden during active RLCR at `prompt-template/block/plan-file-modified.md:5`, and gives the required recovery sequence: cancel loop, update plan, restart loop with the plan path at `prompt-template/block/plan-file-modified.md:7-10`. It exposes backup location at `prompt-template/block/plan-file-modified.md:12`.
- inputs_outputs_state: Inputs are template variables `PLAN_FILE` and `BACKUP_PATH`. Output is rendered Markdown used as a stop-hook block reason. It does not mutate state; it documents a gate failure caused by state comparison elsewhere.
- gates_or_invariants: The invariant enforced by the caller is that the active RLCR loop’s plan file content must match the saved backup. In `hooks/loop-codex-stop-hook.sh:293-313`, a `diff -q` between `FULL_PLAN_PATH` and `BACKUP_PLAN` failing causes this template to render and returns JSON `{"decision":"block"}`. An earlier uncommitted git-status check also blocks at `hooks/loop-codex-stop-hook.sh:280-289`, but with an inline reason rather than this template.
- dependencies_and_callers: Called through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:307-309`, with an inline fallback at `hooks/loop-codex-stop-hook.sh:295-306` that mirrors this file. Rendering uses the template loader API referenced across hooks, including `hooks/lib/template-loader.sh:168-170`.
- edge_cases_or_failure_modes: If the template is missing, the fallback still preserves gate behavior. If variables are absent or malformed, the rendered remediation may contain unresolved placeholders while the JSON block still occurs. The gate is content-based and catches committed changes too, not just dirty working-tree changes, because it compares against the backup at `hooks/loop-codex-stop-hook.sh:293-294`.
- validation_or_tests: No direct test for this exact template was found in assigned evidence. It is indirectly covered by hook tests if they exercise plan backup mismatch behavior; the core enforcement line is `hooks/loop-codex-stop-hook.sh:293-313`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-122 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop critical-requirements template used when a newly opened PR has no fetched bot comments yet. It defines the worker completion contract for the initial review-waiting state.
- algorithmic_behavior: The template tells the worker that on completion they must write a resolution summary to `{{RESOLVE_PATH}}` at `prompt-template/pr-loop/critical-requirements-no-comments.md:6-13`, specifically noting this is Round 0 awaiting initial bot reviews and there are no issues yet. It then instructs the worker to try to exit so the Stop Hook can poll bot reviews at `prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`. It explicitly forbids commenting on the PR to trigger review at `prompt-template/pr-loop/critical-requirements-no-comments.md:17-18`.
- inputs_outputs_state: Input is `RESOLVE_PATH` supplied by PR-loop setup. Output is prompt text included in the generated round-0 prompt. It causes the worker to create/update the round-0 PR resolve file and then yield control to the stop hook; it does not itself perform filesystem mutation.
- gates_or_invariants: The invariant is that an initial no-comment PR loop should wait for automatic bot reviews rather than manually triggering review comments. In `scripts/setup-pr-loop.sh:812-835`, this template is selected only when `COMMENT_COUNT` is `0`. The setup path defines `RESOLVE_PATH="$LOOP_DIR/round-0-pr-resolve.md"` at `scripts/setup-pr-loop.sh:641` and includes it in `TEMPLATE_VARS` at `scripts/setup-pr-loop.sh:652-656`.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh:834` through `load_and_render_safe`, with an inline fallback at `scripts/setup-pr-loop.sh:813-833`. The paired branch for comments-present uses a different template at `scripts/setup-pr-loop.sh:835-860`, so this file specifically owns the no-comments initial-review contract. Stop-hook behavior later polls and can observe cancellation at `hooks/pr-loop-stop-hook.sh:941-945`.
- edge_cases_or_failure_modes: If `COMMENT_COUNT` is incorrectly computed as zero, the prompt may suppress a needed PR comment trigger and wait for automatic reviews. If `RESOLVE_PATH` is missing, unresolved placeholder text would be shown. The instruction “try to exit” relies on hook installation and correct stop-hook polling; without hooks, this template cannot enforce waiting. It also assumes review bots will automatically review a new PR.
- validation_or_tests: No direct template assertion was found. The setup branch and fallback are visible in `scripts/setup-pr-loop.sh:812-835`; PR-loop stop-hook tests may cover the broader no-comment polling path, but this assigned file’s exact text is not directly validated in the inspected tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 section headings matched the five assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`