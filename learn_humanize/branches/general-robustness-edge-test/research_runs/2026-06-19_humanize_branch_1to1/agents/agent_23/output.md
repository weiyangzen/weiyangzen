# agent_23 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook validator helper for the RLCR loop’s “all todos complete before exit/review/finalize” gate. It is intentionally narrow: it reads a Claude Code transcript path from hook input and finds the latest `TodoWrite` state so `hooks/loop-codex-stop-hook.sh` can block before expensive review work.
- algorithmic_behavior: `find_latest_todos()` streams transcript JSONL line by line, ignoring blank lines and malformed JSONL entries, and updates `latest_todos` whenever it sees a non-empty `TodoWrite` payload. It supports three transcript shapes: assistant message content blocks at lines 51-64, alternative `type: message` entries at lines 65-76, and direct `type: tool_use` entries at lines 78-85. `main()` reads hook input JSON from stdin, expands `transcript_path`, obtains the latest todo list, and treats any todo whose `status` is not exactly `completed` as incomplete at lines 90-129.
- inputs_outputs_state: Input is stdin JSON containing optional `transcript_path` as documented at lines 12-13 and read at lines 91-104. Secondary input is the transcript file itself. Output is process exit status plus stdout/stderr: exit 0 means no blocking todo evidence, exit 1 means incomplete todos and prints `INCOMPLETE_TODOS` plus bullet details at lines 121-126, and exit 2 means hook-input JSON parse failure with `PARSE_ERROR` on stderr at lines 92-97. It keeps no persistent state; the only state transition is selecting the latest observed todo list while scanning.
- gates_or_invariants: Missing transcript path exits allow at lines 99-101; missing transcript file returns no todos at lines 25-26; malformed transcript lines are ignored at lines 36-39; only the latest non-empty TodoWrite payload matters at lines 62-63, 75-76, and 84-85. The completion invariant is strict string equality to `completed` at lines 115-119, so missing, pending, or in-progress statuses all block.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` before git checks and Codex review. The stop hook sets `TODO_CHECKER` at line 321, executes it with the original hook input at lines 323-326, blocks on parse errors at lines 328-344, and blocks on incomplete todos at lines 347-368 using `prompt-template/block/incomplete-todos.md` fallback rendering. It is directly covered by `tests/test-todo-checker.sh`, which defines the checker path at line 13.
- edge_cases_or_failure_modes: Invalid stdin JSON is fail-closed for the stop hook because exit 2 becomes a block. Invalid JSONL inside the transcript is tolerated and skipped, so a partially corrupt transcript can still pass if no later incomplete TodoWrite is found. Empty or absent todo arrays are treated as no todo state and allow exit. A todo with empty content still blocks but produces a sparse bullet, because content defaults to empty string at lines 116-119. The helper trusts the transcript path from hook input after `~` expansion and does not enforce project-root containment.
- validation_or_tests: `tests/test-todo-checker.sh` covers invalid and empty hook input at lines 51-73, missing `transcript_path` at lines 75-85, completed todos at lines 106-119, pending and in-progress states at lines 121-157, latest TodoWrite selection at lines 196-210, direct and alternative transcript formats at lines 212-235, missing status at lines 249-263, empty content at lines 265-278, and Unicode/plain text handling at lines 280-290. `tests/test-finalize-phase.sh` also exercises it indirectly by creating an incomplete transcript and expecting finalize exit to block at lines 715-740.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR finalization, review, and completion state transitions. It codifies how `state.md`, `finalize-state.md`, `complete-state.md`, max-iteration state, todo completion, git cleanliness, and validators interact during the final stage of the loop.
- algorithmic_behavior: The test builds an isolated git repo under `mktemp`, sources `hooks/lib/loop-common.sh`, mocks `codex`, and drives actual hook scripts with JSON hook input. Setup functions create mock Codex executables at lines 53-142, initialize a repo and test plan at lines 144-178, and create an RLCR loop directory/state/frontmatter/goal tracker at lines 180-227. The assertions then walk through helper detection, finalize-state protection, finalize completion, COMPLETE-to-finalize transition, max-iteration bypass, review failure handling, empty review output handling, incomplete todo blocking, normal next-round behavior, and validator parsing while finalize-state is active.
- inputs_outputs_state: Inputs are mocked hook JSON, generated loop state frontmatter, temporary transcript JSONL files, mock Codex stdout/stderr behavior, and git worktree cleanliness. Outputs are pass/fail counters and script exit equal to failed test count at line 876. State transitions under test include `state.md` to `finalize-state.md` on successful COMPLETE/review path at lines 524-540, `finalize-state.md` to `complete-state.md` when final gates pass at lines 476-496, `state.md` to `maxiter-state.md` at lines 542-560, and normal next-round increment to round 4 at lines 791-797.
- gates_or_invariants: Finalize phase must be treated as active loop state, while complete-state must not revive a loop. The helper test asserts `find_active_loop` sees `finalize-state.md` at lines 293-299 and ignores `complete-state.md` at lines 314-320; this matches `hooks/lib/loop-common.sh`, where active loops are only newest directories containing `state.md` or `finalize-state.md` at lines 150-173. Finalize-state is protected from Write, Edit, Bash redirection, and Bash `mv`/`cp` source access at lines 345-415. Finalize completion still requires summary, clean git state, and complete todos before state rename, checked at lines 429-504 and 698-740. Finalize phase must not invoke Codex, asserted at lines 446-452 and 498-504.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at line 49 and calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-plan-file-validator.sh`. It relies on shared helper functions such as `is_finalize_state_file_path`, `is_finalize_summary_path`, `finalize_state_file_blocked_message`, `find_active_loop`, and `parse_state_file`. It is included in the aggregate runner `tests/run-all-tests.sh` at line 45.
- edge_cases_or_failure_modes: Review failure must block and preserve `state.md` without entering finalize at lines 566-619. Empty review stdout also blocks, keeps review state, and verifies an empty review log at lines 625-696. Manual toggling to review phase is guarded in the stop hook by requiring `.review-phase-started`; the hook’s consistency check is at `hooks/loop-codex-stop-hook.sh` lines 1412-1425. Malformed finalize-state must fail closed in validators: write/edit/read/plan validators parse `finalize-state.md` with strict state parsing, as seen in `hooks/loop-write-validator.sh` lines 132-143, `hooks/loop-edit-validator.sh` lines 99-108, `hooks/loop-read-validator.sh` lines 88-98, and `hooks/loop-plan-file-validator.sh` lines 38-48.
- validation_or_tests: This file is itself the validation artifact. It makes concrete assertions for helper recognition at lines 238-280, active-loop detection at lines 283-335, file protection at lines 337-415, finalize stop-hook gates at lines 417-504, COMPLETE and max-iteration routing at lines 506-560, review failure and empty-output gates at lines 562-696, todo-completion gate at lines 698-740, normal non-COMPLETE review loop behavior at lines 743-815, and finalize-state parser compatibility across validators at lines 816-865.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR bash validator’s default no-push gate. It is a small but algorithmically relevant policy surface: unless a loop was started with `--push-every-round`, direct `git push` commands are blocked.
- algorithmic_behavior: The template contains static markdown headed “Git Push Blocked” at lines 1-9. It tells the agent that commits should remain local and gives the opt-in startup flag `/humanize:start-rlcr-loop plan.md --push-every-round` at lines 6-9. No variables are rendered in this template.
- inputs_outputs_state: Input is the template loader request from a validator; output is a human-readable block reason. It changes no file or loop state by itself. The behavioral state it reflects is `push_every_round` from loop state: when false, `git push` is rejected; when true, the validator allows the push path and the stop hook later enforces unpushed-commit checks.
- gates_or_invariants: `hooks/loop-bash-validator.sh` parses loop state, reads `STATE_PUSH_EVERY_ROUND`, and blocks commands matching `git push` when the value is not true at lines 90-105. The gate is default-deny for pushes during an active RLCR loop unless explicitly opted into by state.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md"` in `hooks/loop-bash-validator.sh` line 103, with an inline fallback at lines 99-102. Template loading depends on `hooks/lib/template-loader.sh`; `load_template` reads a file if present at lines 36-47 and `load_and_render_safe` uses fallback only when the template is missing or renders empty at lines 170-203.
- edge_cases_or_failure_modes: The bash validator’s push detection is command-string based and matches only commands beginning with optional whitespace then `git push` at line 98; aliases, shell wrappers, or indirect push mechanisms are outside this template’s scope and would depend on other validator logic. Missing template does not disable the gate because the caller has a fallback. The template is advisory text only; enforcement lives in the bash validator.
- validation_or_tests: `tests/test-template-loader.sh` loads `block/git-push.md` and expects content containing “Git Push Blocked” at lines 55-64; it also verifies `load_and_render_safe` prefers the real template over fallback at lines 211-222. `tests/test-templates-comprehensive.sh` repeats the real-template-over-fallback check at lines 456-462. `tests/test-template-references.sh` scans hook references to ensure template files exist at lines 83-105.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt/footer template that adds next-round instructions when the loop is configured for remote push every round. It complements the push gate by making the required push action visible in generated prompts.
- algorithmic_behavior: The template is a two-line static note; line 2 says that because `--push-every-round` is enabled, commits must be pushed to remote after each round. It has no placeholders and no internal control flow.
- inputs_outputs_state: Input is loop state `push_every_round: true` as parsed by the stop hook into `PUSH_EVERY_ROUND`. Output is appended text in the generated next-round prompt file. It does not mutate loop state; it documents the already-selected policy for the next agent round.
- gates_or_invariants: In `hooks/loop-codex-stop-hook.sh`, after the next-round footer is written, the hook appends this note only when `PUSH_EVERY_ROUND` equals `true` at lines 1508-1515. Separately, the same stop hook blocks exit when this mode is enabled and the local branch is ahead of remote at lines 548-570, so the note aligns with an enforced gate rather than being merely decorative.
- dependencies_and_callers: Loaded with `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` in `hooks/loop-codex-stop-hook.sh` line 1510. If missing, the hook falls back to “Also push your changes after committing.” at lines 1511-1513. Template loading depends on `hooks/lib/template-loader.sh` lines 36-47.
- edge_cases_or_failure_modes: Missing template degrades to a shorter inline fallback, so prompt generation continues. Since the note is appended only during next-round prompt generation, it does not by itself block an exit attempt; the actual unpushed-commits enforcement is in the stop hook’s git-ahead check. The note has no branch/remote variables, so it cannot show branch-specific details.
- validation_or_tests: The file is covered indirectly by template reference scanning in `tests/test-template-references.sh`, which extracts `load_template`, `load_and_render`, and `load_and_render_safe` template paths from hooks and verifies referenced files exist at lines 83-105. The aggregate runner includes template reference tests at `tests/run-all-tests.sh` line 38.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-143 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the RLCR prompt/template rendering subsystem under large-value, large-template, high-substitution, and special-character conditions. It protects prompt/block template rendering used by validators and stop hooks.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` and shared test helpers at lines 14-17, creates an isolated temp directory at line 19, then runs 18 assertions. It first verifies ordinary substitution and fallback rendering at lines 33-52, then exercises file rendering with a generated standard template at lines 54-72. Stress checks cover 10KB variable values at lines 82-94, 100KB values at lines 96-109, 100KB template files at lines 111-132, and 50 substitutions at lines 134-151. Edge checks cover regex metacharacters, ampersands, backslashes, newlines, empty values, placeholder injection, dollar signs, repeated placeholders, boundary placeholders, and a template containing only a placeholder at lines 161-283.
- inputs_outputs_state: Inputs are inline template strings, generated temporary template files, and `VAR=value` arguments passed to `render_template`, `load_and_render`, and `load_and_render_safe`. Outputs are pass/fail messages and final exit status from `print_test_summary` at lines 289-290. Persistent repository state is not changed; temp files live under the test directory created by `setup_test_dir`.
- gates_or_invariants: The central invariant is single-pass placeholder substitution: a value containing another placeholder must remain literal, asserted at lines 218-228 and implemented by `hooks/lib/template-loader.sh` comments at lines 50-57 plus the AWK replacement loop at lines 71-129. Missing templates must use fallback via `load_and_render_safe`, asserted at lines 44-52 and implemented at `hooks/lib/template-loader.sh` lines 170-186. Unprovided placeholders remain intact, and replacement values must not be interpreted as sed/regex replacement syntax because the implementation uses environment-backed AWK lookup rather than sed substitution.
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`: `render_template` at lines 58-132, `load_and_render` at lines 136-147, and `load_and_render_safe` at lines 170-203. It also uses `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary` at lines 30-89. The aggregate runner includes this robustness test at `tests/run-all-tests.sh` line 62.
- edge_cases_or_failure_modes: The test deliberately stresses shell/environment size by passing 100KB values through environment variables at lines 96-103; extremely larger values may hit OS argument or environment limits outside the tested range. Multi-line values are checked only for presence of first/last lines, not exact full formatting, at lines 195-205. The “Unicode” test uses plain ASCII text despite the label at lines 230-239, so it is a weak Unicode coverage point. Variable-name validity is mostly covered elsewhere; this stress test assumes valid uppercase variable names.
- validation_or_tests: This file is itself a validation command and is listed in `tests/run-all-tests.sh` line 62. Related broader coverage appears in `tests/test-template-loader.sh`, which checks numerous special characters and injection cases, and `tests/robustness/test-template-error-robustness.sh`, which covers malformed names, missing directories, empty files, permission/symlink cases, and concurrent reads.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 unique assigned item headings present; assigned item_id strings are used once each in the five evidence headings.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`