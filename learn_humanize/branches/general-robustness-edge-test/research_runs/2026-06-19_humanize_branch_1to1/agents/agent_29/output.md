# agent_29 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse read gate for RLCR loop state. It prevents agents from reading stale or misplaced RLCR `round-N-summary.md`, `round-N-prompt.md`, and most `round-N-todos.md` files while allowing unrelated reads and PR-loop reads. The declared policy is in `hooks/loop-read-validator.sh:3`.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON and `tool_name` through shared helpers, exits immediately for non-`Read` tools, requires `tool_input.file_path`, lowercases the path for pattern matching, and applies RLCR-specific checks only for round summary/prompt/todos files. Todos are blocked unless they are explicitly allowlisted in the active loop directory, then summary/prompt files are checked against active loop, current round, and exact directory path. Main flow is visible in `hooks/loop-read-validator.sh:25`, `hooks/loop-read-validator.sh:39`, `hooks/loop-read-validator.sh:44`, `hooks/loop-read-validator.sh:55`, `hooks/loop-read-validator.sh:69`, `hooks/loop-read-validator.sh:122`, `hooks/loop-read-validator.sh:138`, and `hooks/loop-read-validator.sh:159`.
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path`. It reads active loop state from `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/rlcr/<newest>/state.md`, or `finalize-state.md` when present, then sets `CURRENT_ROUND` from strict state parsing. Output is exit `0` for allow, exit `1` for invalid/malformed input or state, and exit `2` with rendered stderr block text for policy denial. State lookup and finalize-state selection are at `hooks/loop-read-validator.sh:80` and `hooks/loop-read-validator.sh:88`.
- gates_or_invariants: Hook input must be valid JSON, contain no null/invalid UTF-8 via `validate_hook_input`, stay within nesting depth 30, and include `tool_input.file_path`; active state must pass `parse_state_file_strict`; readable round files must be in `.humanize/rlcr/`, match the current round unless allowlisted, and equal `$ACTIVE_LOOP_DIR/$basename`. Shared strict parsing requires `current_round`, `max_iterations`, `review_started`, and `base_branch`, and validates numeric/boolean fields in `hooks/lib/loop-common.sh:242`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and transitively `hooks/lib/template-loader.sh`; depends on `jq`, `grep`, `sed`, `basename`, `find_active_loop`, `is_round_file_type`, `extract_round_number`, `is_allowlisted_file`, `load_and_render_safe`, and block templates `block/wrong-file-location.md`, `block/wrong-round-file.md`, `block/wrong-directory-path.md`, and `block/todos-file-access.md`. Registered as a hook in `hooks/hooks.json:38`.
- edge_cases_or_failure_modes: No active loop allows reads, so stale-file protection is inactive without state. Malformed active state fails closed. Finalize phase uses `finalize-state.md`. Exact string path comparison can block semantically equivalent paths that do not exactly match the active loop path. Allowlist exceptions include `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` from `hooks/lib/loop-common.sh:421`. Legacy `.humanize-loop.local` is not treated as an RLCR loop because `is_in_humanize_loop_dir` only matches `.humanize/rlcr/` at `hooks/lib/loop-common.sh:706`.
- validation_or_tests: Related coverage includes allowlist read cases in `tests/test-allowlist-validators.sh:247`, wrong-round read denial in `tests/test-allowlist-validators.sh:273`, finalize-phase read validation in `tests/test-finalize-phase.sh:841`, and malformed/deep hook input robustness in `tests/robustness/test-hook-input-robustness.sh:39`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file immutability and hook safety. It validates the plan-file contract across UserPromptSubmit, Write, Edit, Bash, and Stop hooks, especially that the source plan and loop backup cannot be changed during an RLCR run.
- algorithmic_behavior: Creates temporary git repositories and `.humanize/rlcr/<timestamp>` loop state with `setup_test_loop`, then drives hooks by piping synthetic hook JSON and checking exit codes/output substrings. It accumulates pass/fail counts and exits with the number of failed tests. Harness setup and result functions are at `tests/test-plan-file-hooks.sh:26` and `tests/test-plan-file-hooks.sh:37`; final result exit is at `tests/test-plan-file-hooks.sh:1082`.
- inputs_outputs_state: Inputs are local hook scripts under `hooks/`, git repository state, generated `state.md`, `plan.md`, `goal-tracker.md`, and `round-0-summary.md` fixtures. Output is terminal PASS/FAIL/SKIP reporting and process exit `$TESTS_FAILED`. The test mutates only temporary directories created with `mktemp -d` and cleaned by trap at `tests/test-plan-file-hooks.sh:31`.
- gates_or_invariants: Enforces v1.5.0 state schema fields `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started`; branch must not change during a loop; loop `plan.md` backup is read-only; Bash cannot bypass protection with redirection, `rm`, command substitution, glob/brace expansion, pipes, or backticks; stop hook must detect source plan modification, deletion, missing backup, uncommitted tracked changes, and committed tracked changes that differ from backup; Round 0 goal tracker placeholders are reported per section. These gates are represented across `tests/test-plan-file-hooks.sh:123`, `tests/test-plan-file-hooks.sh:168`, `tests/test-plan-file-hooks.sh:200`, `tests/test-plan-file-hooks.sh:217`, `tests/test-plan-file-hooks.sh:234`, `tests/test-plan-file-hooks.sh:278`, `tests/test-plan-file-hooks.sh:483`, `tests/test-plan-file-hooks.sh:523`, `tests/test-plan-file-hooks.sh:576`, `tests/test-plan-file-hooks.sh:647`, `tests/test-plan-file-hooks.sh:671`, and `tests/test-plan-file-hooks.sh:755`.
- dependencies_and_callers: Directly exercises `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Those hooks depend on `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, template rendering, `git`, `jq`, `diff`, shell regexes, and Python todo checking when present. The Bash bypass model is backed by `command_modifies_file` in `hooks/lib/loop-common.sh:1065`.
- edge_cases_or_failure_modes: Handles default branch names dynamically for `main` versus `master`; verifies YAML-quoted `plan_file` and `start_branch` parsing; verifies hyphenated plan paths; deliberately allows legacy `.humanize-loop.local` paths as negative tests at `tests/test-plan-file-hooks.sh:1042`; stop-hook quote parsing test only asserts absence of YAML parse errors, allowing unrelated stop-hook blocks such as git cleanliness or Codex availability; repeated `setup_test_loop` appends to `.gitignore` to create commit-worthy changes on each reset.
- validation_or_tests: This file is itself the validation artifact. It covers prompt-submit schema/branch validation in tests 1-4, write/edit/bash plan backup protection in tests 5-8a, shell bypass prevention in tests 8.1-8.5, YAML quoting in tests 8.6-8.9, stop-hook plan integrity in tests 9-14, section-specific placeholder detection in tests 14.1-14.4, and legacy path allow behavior in tests 15-17.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-089 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Human-facing block template for the stop-hook large-file gate. It defines the remediation contract when changed code or documentation files exceed the configured line limit.
- algorithmic_behavior: Provides Markdown rendered by `load_and_render_safe` with `{{MAX_LINES}}` and `{{LARGE_FILES}}`. The stop hook builds the large-file list from `git status --porcelain`, classifies extensions as code or documentation, counts lines, and blocks exit when any changed/new matching file has more than `MAX_LINES`, currently 2000. Rendering is triggered in `hooks/loop-codex-stop-hook.sh:409`, `hooks/loop-codex-stop-hook.sh:456`, and `hooks/loop-codex-stop-hook.sh:470`.
- inputs_outputs_state: Inputs are template variables `MAX_LINES` and `LARGE_FILES`; output is Markdown beginning “Large Files Detected” with required actions. It does not mutate state itself; the stop hook wraps the rendered text in JSON with `"decision": "block"` and a system message at `hooks/loop-codex-stop-hook.sh:474`.
- gates_or_invariants: Encodes that code files must be split into modules smaller than the line limit with unchanged behavior, documentation must be split into logical sections with cross-references and narrative continuity, and changes must be committed before another stop attempt. The policy text is at `prompt-template/block/large-files.md:11`.
- dependencies_and_callers: Consumed by `hooks/loop-codex-stop-hook.sh`; rendered by `hooks/lib/template-loader.sh` single-pass placeholder substitution, which preserves missing placeholders rather than recursively expanding values. If the template is missing or empty, the stop hook fallback at `hooks/loop-codex-stop-hook.sh:463` is used.
- edge_cases_or_failure_modes: Template-only surface, so detection quality depends on the caller. The caller only scans files present in git status, skips deleted files, and ignores extensions outside its code/doc lists. Missing variables would leave literal placeholders because template rendering intentionally keeps unresolved placeholders. The optional `code-simplifier` instruction at `prompt-template/block/large-files.md:16` is advisory and depends on plugin availability.
- validation_or_tests: Referenced in template syntax/reference test suites via template loading patterns, and the stop-hook implementation provides the primary behavioral validation path. Search evidence shows the only runtime render path is the large-file check in `hooks/loop-codex-stop-hook.sh:470`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-119 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Canonical plan skeleton for `/humanize:gen-plan`. It defines the plan-generation contract that turns an unstructured draft into a deterministic implementation plan with goal, acceptance criteria, bounds, dependencies, and implementation notes.
- algorithmic_behavior: Static Markdown template copied into a new plan file, then the original draft is appended after separators. The IO validator locates this template through `CLAUDE_PLUGIN_ROOT` or a script-relative fallback, copies it to the output path, appends the draft, and exits successfully at `scripts/validate-gen-plan-io.sh:133` and `scripts/validate-gen-plan-io.sh:148`.
- inputs_outputs_state: Template has no `{{...}}` render variables; its placeholders are authoring prompts such as `<Plan Title>`. Runtime inputs are `--input <draft>` and `--output <plan>` to `scripts/validate-gen-plan-io.sh`; output is a new plan file containing this template plus original draft content. The script rejects missing/empty input, missing output directory, existing output, directory output, no write permission, invalid args, and missing template with exit codes documented at `scripts/validate-gen-plan-io.sh:4`.
- gates_or_invariants: Acceptance criteria must include positive and negative deterministic tests; path boundaries define upper/lower acceptable scope and allowed/prohibited choices; deterministic designs should collapse broad choice into fixed constraints; implementation code and comments must not contain plan workflow markers such as `AC-`, `Milestone`, `Step`, or `Phase`. These contracts appear in `prompt-template/plan/gen-plan-template.md:6`, `prompt-template/plan/gen-plan-template.md:25`, `prompt-template/plan/gen-plan-template.md:39`, `prompt-template/plan/gen-plan-template.md:44`, and `prompt-template/plan/gen-plan-template.md:69`.
- dependencies_and_callers: Used by `scripts/validate-gen-plan-io.sh`; command wiring references that validator from `commands/gen-plan.md:5` and `commands/gen-plan.md:35`. Structural and IO tests are in `tests/test-gen-plan.sh`, including validator argument/error cases beginning at `tests/test-gen-plan.sh:426`.
- edge_cases_or_failure_modes: Missing template is treated as plugin configuration error exit `7`; `realpath -m` is attempted but falls back to raw paths if unavailable; output must not preexist, so regeneration is intentionally non-overwriting; no template rendering occurs, so angle-bracket placeholders remain for the generation agent to fill.
- validation_or_tests: `tests/test-gen-plan.sh` validates command structure, frontmatter, naming, English-only content, and `validate-gen-plan-io.sh` error paths for missing args, missing input, empty input, missing output dir, existing output, directory output, valid paths, and help behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 section headings present, one section per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`