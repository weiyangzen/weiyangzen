# agent_01 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the Humanize Claude plugin surface for two loop state machines: RLCR and PR review loop. The root coordinates command entrypoints, hook wiring, shell state machines, prompt/block templates, monitoring helpers, fixtures, and test suites. `hooks/hooks.json:14-64` wires PreToolUse validators and Stop hooks; `README.md:155-163` describes the PR loop as detect PR, fetch reviews, fix, push, trigger review, poll, and validate with local Codex.
- algorithmic_behavior: `scripts/setup-pr-loop.sh` initializes PR loop sessions after mutual exclusion, git, `gh`, and `codex` checks (`scripts/setup-pr-loop.sh:220-277`), detects the PR/fork context (`283-359`), validates YAML-safe state fields (`361-388`), fetches initial comments (`402-412`), computes startup case (`415-428`), optionally posts trigger comments and verifies Claude eyes (`443-521`), writes `state.md` with `configured_bots`, `active_bots`, current round, commit, trigger, and timeout fields (`524-555`), then emits round prompt/critical instructions (`641-862`). `hooks/pr-loop-stop-hook.sh` then advances the PR loop on Stop attempts. RLCR sibling behavior is present via `setup-rlcr-loop.sh`, `loop-codex-stop-hook.sh`, validators, and RLCR prompt templates.
- inputs_outputs_state: Inputs include Claude hook JSON stdin, command args, `CLAUDE_PROJECT_DIR`, git state, GitHub CLI API data, Codex CLI output, plan files, PR comments/reviews/reactions, and `.humanize/.../state.md` frontmatter. Outputs include JSON hook block decisions, `.humanize/pr-loop/<timestamp>/round-N-*` artifacts, state renames such as `approve-state.md`, `closed-state.md`, `maxiter-state.md`, prompt files, feedback files, and monitor display data. `hooks/lib/loop-common.sh:789-808` locates active PR loops by newest `state.md`.
- gates_or_invariants: One active loop at a time (`scripts/setup-pr-loop.sh:220-240`); state/prompt/comment/check/feedback files are system-owned (`hooks/loop-write-validator.sh:70-94`, `hooks/loop-edit-validator.sh:54-76`); `round-N-pr-resolve.md` must match the current round (`hooks/lib/loop-common.sh:831-880`); bot names map consistently to GitHub authors (`hooks/lib/loop-common.sh:716-743`); `configured_bots` starts equal to active bots but remains the full polling universe (`scripts/setup-pr-loop.sh:528-533`).
- dependencies_and_callers: Command markdown under `commands/` calls setup/cancel scripts; `hooks/` call shared libraries and templates; `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` use `gh`/`jq` to normalize issue comments, review comments, and approval-only PR reviews; `scripts/lib/monitor-common.sh:255-383` derives PR loop phases; tests and fixtures exercise both pure parsing and mocked GitHub runtime.
- edge_cases_or_failure_modes: Fork PR base-repo lookup, PR merged/closed, force pushes, new commits after trigger, missing trigger, stale trigger, missing Claude eyes, Codex unavailable/empty/failing, uncommitted or unpushed work, approval-only reviews, paginated comments, per-bot timeout, wrong-round resolve files, malformed/deep hook JSON, shell bypass attempts, path traversal, macOS Bash compatibility, and monitor terminal/log edge cases are all represented in code/tests.
- validation_or_tests: `tests/run-all-tests.sh:32-72` includes template, hook, PR loop, system, and robustness suites. PR loop coverage spans `tests/test-pr-loop.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-stophook.sh`, fixture-backed `gh` mocks, and `tests/robustness/*`. I inspected these surfaces only; no tests were executed in this read-only research pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop hook state machine for active PR loops. It intercepts Claude exit attempts, verifies local work is ready for remote review, polls PR review bots, runs local Codex to classify bot feedback, and either blocks exit with feedback, advances the round, or terminates the loop.
- algorithmic_behavior: It reads hook stdin (`hooks/pr-loop-stop-hook.sh:42`), finds newest active PR loop (`64-75`), parses YAML frontmatter and bot lists (`87-174`), resolves PR base/lookup repository for forks (`196-237`), checks PR state (`239-255`), requires a current-round resolution file (`258-277`), enforces clean/pushed git state (`279-362`), detects force-push history rewrites and clears trigger state (`364-424`), handles max iterations (`426-435`), treats Codex `+1` reactions as approval in round zero case one (`438-502`), detects trigger comments (`519-590`), refreshes commit timestamps and trigger fields (`598-671`), gates trigger requirement by round/startup case (`673-735`), verifies Claude eyes when needed (`738-789`), polls configured bots (`791-1049`), runs Codex review of bot feedback (`1219-1344`), parses final markers (`1350-1399`), updates `active_bots` with approval/re-add/timeout logic (`1402-1481`), rewrites state (`1545-1567`), and emits next-round feedback (`1583-1621`).
- inputs_outputs_state: Inputs are `state.md`, `round-N-pr-resolve.md`, git status/ahead data, `gh pr view`, `gh api`, bot reactions, `scripts/poll-pr-reviews.sh` JSON, templates, and Codex output. Outputs are JSON hook decisions, stderr progress, updated `state.md`, terminal state renames (`merged-state.md`, `closed-state.md`, `approve-state.md`, `maxiter-state.md`, `usage-limit-state.md`), `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md`.
- gates_or_invariants: Required fields include `pr_number` and numeric `current_round` (`185-194`). Exit is blocked for missing resolution summary (`262-276`), dirty non-humanize files (`296-310`), unpushed commits (`313-360`), missing required trigger (`717-735`), missing Claude eyes (`757-779`), bot timeout/no responses (`1057-1165`), missing Codex (`1291-1306`), failing/empty Codex output (`1322-1344`), `WAITING_FOR_BOTS` (`1368-1388`), and issues remaining (`1614-1621`). Full approval moves state instead of blocking (`1355-1365`, `1569-1573`).
- dependencies_and_callers: Invoked by `hooks/hooks.json:52-64` as a Stop hook. Depends on `hooks/lib/loop-common.sh` for bot mapping, YAML list building, loop discovery, and goal tracker updates; `scripts/portable-timeout.sh` for bounded external calls; `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`; `jq`, `git`, `gh`, and `codex`; and prompt templates under `prompt-template/block` and `prompt-template/pr-loop`.
- edge_cases_or_failure_modes: Handles absent loop/state as pass-through (`67-75`), fork PR lookup (`196-237`), merged/closed PR (`246-255`), no upstream with origin/PR-head fallback (`318-345`), force push old commit not ancestor (`370-421`), new non-force commits invalidating stale triggers (`612-629`), paginated trigger comments (`535-543`), trigger filtering after latest commit (`563-576`), startup cases one through five (`677-707`, `814-836`), per-bot timeout anchored to trigger time (`858-890`), cancel signal during polling (`941-944`), Codex `+1` approval without comment (`974-1024`), duplicate comment IDs (`1029-1045`), previously approved bots re-added when new issues appear (`1418-1481`), and service rate-limit termination (`1391-1399`).
- validation_or_tests: Covered directly by `tests/test-pr-loop-hooks.sh`: trigger/user filtering (`538-586`), trigger refresh and missing trigger gates (`588-622`), round-zero timestamp priority (`624-665`), timeout anchoring (`667-696`), state/configured bot parsing (`698-724`), round file naming (`726-744`), E2E missing resolve/trigger/pagination/runtime priority (`766-1148`), approval-only reviews (`1160-1219`), fixture-backed fetch/poll (`1242-1419`), and wrong-round validation (`1431-1535`). Additional suite references include `tests/test-pr-loop-stophook.sh` and `tests/test-pr-loop-system.sh`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR loop hook/validator behavior. It is sourced by `tests/test-pr-loop.sh`, which calls `run_hook_tests` (`tests/test-pr-loop.sh:33-44`), and verifies the branch’s PR loop file protection, comment processing, Stop hook gates, polling integration, and monitor display behavior.
- algorithmic_behavior: Defines `run_hook_tests` (`tests/test-pr-loop-hooks.sh:15`) with grouped tests: PR loop validator protections (`26-210`), comment sorting (`222-259`), gatekeeper parsing/dedup/status extraction (`271-466`), Stop hook trigger logic and mocks (`478-754`), mocked end-to-end hook execution (`766-1148`), approval-only review handling (`1160-1219`), fixture-backed fetch/poll tests (`1231-1419`), wrong-round resolve validation (`1431-1535`), and monitor active/configured bot display (`1547-1621`).
- inputs_outputs_state: Inputs are `TEST_DIR`, `SCRIPT_DIR`, `PROJECT_ROOT`, helper functions from the parent test harness, mocked `.humanize/pr-loop/<timestamp>/state.md`, hook JSON payloads, fake `gh`/`git` scripts, fixture JSON files, and real project scripts. Outputs are pass/fail records through helper functions plus temporary loop files, generated mock binaries, fetched markdown, and poll JSON assertions.
- gates_or_invariants: Asserts `active_bots` YAML list format (`26-63`), PR loop `state.md` and generated comment files are blocked from direct writes (`68-130`), current-round `pr-resolve` writes are allowed while wrong-round writes/edits are blocked (`135-163`, `1431-1535`), Bash redirection cannot mutate PR loop state (`174-210`), human comments sort before bot comments and timestamps sort newest first (`222-259`), comments dedupe by ID (`271-286`), final markers `APPROVE` and `WAITING_FOR_BOTS` are recognized (`415-457`), triggers are current-user filtered (`538-586`), and per-bot timeout is anchored to trigger timestamp (`667-696`).
- dependencies_and_callers: Depends on `jq`, test helper functions, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/setup-fixture-mock-gh.sh`, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, and JSON fixtures under `tests/fixtures`.
- edge_cases_or_failure_modes: Tests user-filtered trigger selection, newer trigger replacement, no-trigger block for later rounds, round-zero trigger-vs-start timestamp priority, paginated comments where the trigger appears on a later page, approval-only PR reviews with empty bodies, timestamp filtering that excludes stale reviews, and active bot display when empty. One portability note: `test_bot_readd_logic` uses `declare -A` (`360-363`), while production code avoids associative arrays for macOS Bash 3.2; this test may require a newer Bash than the production hook itself.
- validation_or_tests: This file is itself validation. It is invoked from the PR loop aggregate runner (`tests/test-pr-loop.sh:33-44`) and complements `tests/test-pr-loop-system.sh`, which separately exercises mutual exclusion and reviewer status cases (`tests/test-pr-loop-system.sh:110-192`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Static block-message template for the RLCR plan backup invariant. It tells the agent that the loop-local `plan.md` is a backup of the original plan and cannot be modified.
- algorithmic_behavior: The template content is intentionally small: title (`prompt-template/block/plan-backup-protected.md:1`), backup immutability statement (`3`), integrity rationale (`5`), and read-only instruction (`7`). It does not compute state itself; it supplies the human-facing reason when validators reject plan backup writes or edits.
- inputs_outputs_state: No placeholders or runtime inputs. Output is markdown text used in a hook block response. The protected state is the RLCR session’s copied `plan.md`; the permitted behavior is read-only reference, not mutation.
- gates_or_invariants: Enforces “plan backup is immutable” as a validator contract. The write validator blocks `plan.md` inside `.humanize/rlcr` using this template path (`hooks/loop-write-validator.sh:176-188`), and the edit validator has the matching edit-side check (`hooks/loop-edit-validator.sh:128-141`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh`, specifically the safe template loading path (`hooks/lib/template-loader.sh:167-194`) so missing/empty templates can fall back to inline messages. Callers are RLCR write/edit validators, not the PR loop Stop hook.
- edge_cases_or_failure_modes: If the template is missing, validators still block through fallback text; if the path is outside `.humanize/rlcr`, the plan-backup condition should not apply. The template has no variable injection surface because it has no placeholders.
- validation_or_tests: Template loading and reference coverage are exercised by `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and robustness template tests in `tests/robustness/test-template-error-robustness.sh` and `tests/robustness/test-template-stress-robustness.sh`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-121 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR loop critical-requirements template for the “initial comments exist” startup path. It defines the work-completion contract the agent must satisfy before the Stop hook can successfully poll and validate bot re-review.
- algorithmic_behavior: Renders three required actions: commit and push changes (`prompt-template/pr-loop/critical-requirements-has-comments.md:6-10`), comment on the PR with `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review"` (`12-13`), and write a resolution summary to `{{RESOLVE_PATH}}` including issues, files, and tests (`15-21`). It closes by telling the user the Stop hook will poll bot reviews (`23`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`; output is rendered prompt text appended by setup. It does not directly update state, but it sets up the external preconditions that `hooks/pr-loop-stop-hook.sh` later verifies: current-round resolve file, clean/pushed work, trigger comment, and bot polling.
- gates_or_invariants: The template’s instructions line up with hard gates in the Stop hook: missing `round-N-pr-resolve.md` blocks (`hooks/pr-loop-stop-hook.sh:262-276`), dirty work blocks (`299-310`), unpushed commits block (`350-360`), and missing required trigger blocks (`717-735`). Setup selects this template only when fetched comments exist (`scripts/setup-pr-loop.sh:643-648`, `810-860`).
- dependencies_and_callers: Loaded by `scripts/setup-pr-loop.sh` via `load_and_render_safe` (`835-860`) after initial comment fetch and prompt assembly. Uses the shared template loader’s single-pass variable renderer (`hooks/lib/template-loader.sh:50-133`), which prevents placeholder injection through variable values.
- edge_cases_or_failure_modes: If `COMMENT_COUNT` is zero, the no-comments sibling template is used instead (`scripts/setup-pr-loop.sh:812-834`). If a bot list is empty, setup should already have failed the required bot flag check (`scripts/setup-pr-loop.sh:175-190`). Quoting around the `gh pr comment` body preserves a single command in the prompt, but actual trigger validity is still enforced by GitHub comment detection and bot reaction checks.
- validation_or_tests: Template reference/comprehensive suites cover template availability. Behavioral coverage appears through PR loop hook tests for missing resolve, trigger detection, unpushed/dirty gate logic by implication, and fixture-backed polling in `tests/test-pr-loop-hooks.sh:766-1148` and `1231-1419`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 section headers above, one per assigned row
- missing_items: none detected
- duplicate_items: none detected
- final_worker_status: `complete`