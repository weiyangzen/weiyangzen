# agent_28 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit gate for the RLCR loop. It prevents prompt submission from continuing when the active loop state is malformed, the user has switched branches, or the plan-file tracking contract no longer matches how the loop was started. It is registered in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/hooks.json:4) as the `UserPromptSubmit` command hook.
- algorithmic_behavior: The validator reads hook input from stdin but does not parse it for routing; its behavior is driven by project state. It resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, finds the newest active RLCR loop under `.humanize/rlcr`, prefers `finalize-state.md` over `state.md` if present, and runs strict state parsing before any git checks. No active loop exits cleanly with no output. Malformed state exits fail-closed with code `1` and stderr. Otherwise, it validates required loop fields, current branch equality, and plan-file git tracking state.
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md}`, state frontmatter fields `plan_file`, `plan_tracked`, and `start_branch`, plus git state for the project. Output is either no output with exit `0` for allow, JSON with `"decision": "block"` and exit `0` for user-correctable gates, or stderr plus exit `1` when strict state parsing fails. It reads `STATE_PLAN_TRACKED`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH` from `parse_state_file_strict` via [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:251).
- gates_or_invariants: Required fields `plan_tracked` and `start_branch` must be present after strict parsing. The strict parser also requires valid YAML frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, and validates numeric/boolean fields in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:259). Current git branch must match loop `start_branch`. If `plan_tracked: true`, the plan path must still be tracked and must have empty `git status --porcelain`. If `plan_tracked` is not true, the plan path must remain untracked/gitignored. Git timeouts and unexpected git errors block rather than allow.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, strict state parsing, field constants, and template loader initialization; depends on [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/scripts/portable-timeout.sh:33) for bounded git calls; depends on `jq` to JSON-escape rendered schema errors. Uses `load_and_render_safe` and `prompt-template/block/schema-outdated.md` for missing-field guidance. It coordinates with write/edit/bash/read validators that enforce related plan/state protections and with setup scripts that seed state fields such as `plan_tracked` and `start_branch`.
- edge_cases_or_failure_modes: Missing active loop is allowed. A newest timestamp directory without `state.md` or `finalize-state.md` is ignored by `find_active_loop`, so older loops are not revived. Malformed or legacy state is a hard safety block before more specific missing-field JSON can run. Detached HEAD or failed `rev-parse` blocks branch validation. Git timeout returns `124` and blocks. Exit-code variables such as `LS_FILES_EXIT` and `STATUS_EXIT` are defaulted with `${var:-0}` after each command, but because they are not explicitly unset before repeated use, stale values would be worth watching if the script ever grows additional repeated git probes in the same shell branch. `FULL_PLAN_PATH` is computed but unused.
- validation_or_tests: Direct coverage appears in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-plan-file-hooks.sh:93): valid state passes silently, missing schema fields block, branch switching blocks, quoted `plan_file` and `start_branch` are parsed, mismatched quoted branches block, paths with hyphens are accepted, and tracked plan-file mutation cases are covered for related stop-hook behavior. Robustness references also invoke this hook with `CLAUDE_PROJECT_DIR` and malformed state scenarios. The hook itself was inspected directly; no tests were executed in this read-only research pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR monitor runtime cleanup behavior. It asserts the monitor can stop cleanly when `.humanize/rlcr` disappears, restores terminal state, and handles repeated cleanup/signal paths without double-running cleanup logic.
- algorithmic_behavior: The script builds an isolated `/tmp/test-monitor-runtime-$$` workspace, writes small generated shell scripts, sources or greps `scripts/humanize.sh`, and uses `pass`/`fail` counters to enforce monitor behavior. It verifies `_graceful_stop` calls `_cleanup`, `_cleanup` calls `_restore_terminal`, `_restore_terminal` resets the terminal scroll region, loop-directory deletion maps to graceful stop, bash `INT/TERM` traps invoke cleanup, zsh `TRAPINT`/`TRAPTERM` handlers exist and can react to SIGINT, and cleanup resets traps to avoid re-triggering. It exits `0` only when all counted assertions pass.
- inputs_outputs_state: Inputs are repository source files, primarily [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/scripts/humanize.sh:523), optional `zsh`, and a writable temp directory. Outputs are colored PASS/FAIL/SKIP-style console lines plus final exit status. Runtime state is local to generated helper scripts: `cleanup_done`, `monitor_running`, `tail_pid`, `restore_called`, `cleanup_triggered`, and `call_count`.
- gates_or_invariants: `_cleanup` must be idempotent through `cleanup_done`. `_graceful_stop` must call `_cleanup` before printing the reason. `_restore_terminal` must reset scroll region with `printf "\033[r"`. Bash mode must install `trap '_cleanup' INT TERM`; zsh mode must define `TRAPINT()` and `TRAPTERM()`. `_cleanup` must clear traps with `trap - INT TERM`. The monitor loop must detect missing `.humanize/rlcr` and return cleanly, matching source behavior in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/scripts/humanize.sh:620).
- dependencies_and_callers: Listed in the aggregate test runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/run-all-tests.sh:50). It depends on shell utilities `grep`, `chmod`, `kill`, `sleep`, `rm`, and `zsh` if present. It depends on concrete function names and source formatting in `scripts/humanize.sh`; for example, one source grep expects `_cleanup()` with indentation at [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-monitor-runtime.sh:307).
- edge_cases_or_failure_modes: The test includes both behavior simulations and brittle source-text assertions. Some assertions grep only nearby source lines, so function refactors can fail the test even if runtime behavior remains equivalent. Test 4 contains a pipeline `grep -q '_restore_terminal' ... | grep -q '_cleanup'` whose first `grep -q` emits no stdout, making that nested check ineffective; the later explicit `grep -A30` assertion is the real coverage for `_cleanup -> _restore_terminal`. The zsh SIGINT test is skipped if zsh is absent. Non-interactive terminal behavior is only indirectly verified because actual `tput` terminal restoration is not exercised end-to-end.
- validation_or_tests: This file is itself validation. It maps to source requirements in `scripts/humanize.sh`: `_restore_terminal` at line 523, `_cleanup` at line 537, `_graceful_stop` at line 565, signal setup at lines 581-587, and deletion checks at lines 623-625 and 663-665. No tests were executed in this read-only research pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook blocking template for the “incomplete todos” gate. It defines the prompt-level contract that Claude must finish TodoWrite items before the loop can proceed into Codex review or stop.
- algorithmic_behavior: The template renders a fixed heading, inserts `{{INCOMPLETE_LIST}}`, and instructs the agent to complete all todos, mark each as completed with TodoWrite, and not proceed to Codex review until all todos are finished. It is selected by `loop-codex-stop-hook.sh` when the todo checker returns incomplete items.
- inputs_outputs_state: Input is `INCOMPLETE_LIST`, derived from `check-todos-from-transcript.py` output after stripping the `INCOMPLETE_TODOS` marker in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:347). Output is markdown text embedded as the JSON `reason` field in a block response. It does not mutate state directly; it gates the transition out of an implementation round.
- gates_or_invariants: The stop hook runs this before expensive review. `check-todos-from-transcript.py` returns `1` if any latest TodoWrite item has status other than `completed`, formatting each as `- [status] content` at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/check-todos-from-transcript.py:113). When incomplete todos exist, the hook emits `"decision": "block"` and system message “incomplete todos detected,” preventing Codex review at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:360).
- dependencies_and_callers: Depends on `load_and_render_safe` from [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/template-loader.sh:170), which uses single-pass `{{VAR}}` substitution and fallback text if the template is missing. Called only through the RLCR stop hook’s todo precheck path. The checker supports Claude Code transcript formats for assistant tool-use entries, alternate message entries, and direct tool-use entries.
- edge_cases_or_failure_modes: If transcript input is missing or no todos are found, the checker exits `0`, so this template is not used. If hook input JSON is malformed, a different parse-error block is emitted. Because rendering is single-pass, placeholder-like content inside todo text is not recursively expanded. The template assumes TodoWrite is the authoritative todo source; todos not present in the latest TodoWrite call are outside this gate.
- validation_or_tests: Behavior is covered indirectly by tests around todo checking and stop-hook blocking; direct references show the hook renders this template on `TODO_EXIT=1`. The template itself is small and declarative; no standalone execution was performed in this read-only research pass.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-118 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Main Codex review prompt template for regular RLCR review rounds. It defines the review contract that verifies Claude’s implementation against the original plan, prompt, summary, docs, and goal tracker, and it controls whether the loop can advance by requiring a final-line `COMPLETE` only under strict completion conditions.
- algorithmic_behavior: The template instructs Codex to first read `@{{PLAN_FILE}}`, compare Claude’s claimed work from `{{SUMMARY_CONTENT}}` and `@{{PROMPT_FILE}}`, perform a skeptical implementation review, check goal alignment using `@{{GOAL_TRACKER_FILE}}`, include a compact acceptance-criteria summary, and write review comments to `@{{REVIEW_RESULT_FILE}}` unless everything is complete. It treats deferred and unfinished work as incomplete and asks Codex to produce a singular implementation plan for pending work rather than accepting deferral. The last-line `COMPLETE` token is explicitly reserved for the all-done/no-deferral/no-pending state.
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`. Output is a rendered markdown review prompt written by the stop hook to `round-N-review-prompt.md` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:797). The subsequent review result file is expected at `round-N-review-result.md`.
- gates_or_invariants: All original plan tasks and acceptance criteria must be complete before `COMPLETE` is permitted. Deferred items and pending items are explicitly incomplete. The template requires review comments for blockers, mismatched claims, implementation plans for deferred/unfinished work, and goal alignment issues. This ties into stop-hook marker handling: the hook checks the last non-empty review-result line for exact `COMPLETE` or `STOP`, and the constant `MARKER_COMPLETE` is defined in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:30).
- dependencies_and_callers: Rendered via `load_and_render_safe` in the RLCR stop hook when `FULL_ALIGNMENT_CHECK` is not true. It depends on the template loader’s single-pass substitution semantics, so summary content containing `{{...}}` cannot inject new template variables. It coordinates with the stop hook’s review/finalize state machine: review prompts are generated before Codex review setup, Codex review runs against a base commit/branch, issue detection scans review logs for `[P?]` markers, and no-issue review results enter finalize phase through `enter_finalize_phase`.
- edge_cases_or_failure_modes: The template is stronger than the current `codex review` execution path in one important way: `run_codex_code_review` writes an audit prompt but comments that `codex review` does not accept prompt input, relying on `codex review --base` instead at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:901). That means this regular-review prompt is authoritative for prompt artifacts and older/alternate review flow expectations, but actual enforcement also depends on the stop hook’s result-file and `[P?]` detection paths. If required variables are missing, placeholders remain visible because missing variables are intentionally not replaced.
- validation_or_tests: Related tests in `tests/test-finalize-phase.sh` cover `COMPLETE` transition behavior, Codex review failure blocking, empty review output blocking, and non-complete review feedback producing a review result. `tests/test-codex-review-merge.sh` covers `[P?]` issue detection and result-file creation through `detect_review_issues`. No tests were executed in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item evidence headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`