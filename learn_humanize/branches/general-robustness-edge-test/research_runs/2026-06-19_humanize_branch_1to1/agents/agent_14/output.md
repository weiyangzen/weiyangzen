# agent_14 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: Prompt-contract directory for the PR review loop. It defines the round-0 operator prompt, initial PR goal tracker schema, critical completion requirements, and Codex goal-tracker update instructions. The directory is content-only, but it is core because `scripts/setup-pr-loop.sh` renders these templates into `.humanize/pr-loop/<timestamp>/goal-tracker.md`, `round-0-prompt.md`, and terminal activation output.
- algorithmic_behavior: The directory contains seven templates: `goal-tracker-initial.md`, `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, `critical-requirements-no-comments.md`, `critical-requirements-has-comments.md`, and `codex-goal-tracker-update.md`. Setup creates the tracker from `goal-tracker-initial.md` with PR number, branch, started time, active bots, and startup case fields (`prompt-template/pr-loop/goal-tracker-initial.md:5-13`). It then renders a common round-0 header (`round-0-header.md:3-15`), appends fetched PR comments, and selects a task template based on comment presence in `scripts/setup-pr-loop.sh:643-720` and `scripts/setup-pr-loop.sh:721-771`.
- inputs_outputs_state: Inputs are template variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTED_AT`, `STARTUP_CASE`, `RESOLVE_PATH`, and `BOT_MENTION_STRING`. Outputs are rendered markdown files/messages. State effect is indirect: the templates instruct the agent to write `round-0-pr-resolve.md`, push/comment when needed, and let `pr-loop-stop-hook.sh` poll and advance the loop (`round-0-task-has-comments.md:15-26`, `round-0-task-no-comments.md:8-15`).
- gates_or_invariants: Templates encode user-facing invariants: do not modify `.humanize/pr-loop/` state files, push fixes before asking bots to re-review when comments already exist, do not trigger initial review comments when no comments exist, and always write the resolution summary (`round-0-task-has-comments.md:30-35`, `round-0-task-no-comments.md:18-23`). The critical templates restate completion requirements at activation output time (`critical-requirements-has-comments.md:6-23`, `critical-requirements-no-comments.md:6-20`).
- dependencies_and_callers: Rendered with `load_and_render_safe` from `hooks/lib/template-loader.sh`. Direct call sites are `scripts/setup-pr-loop.sh:632-633` for goal tracker, `scripts/setup-pr-loop.sh:677-684` for header/comment prompt assembly, `scripts/setup-pr-loop.sh:687-771` for task selection, and `scripts/setup-pr-loop.sh:810-862` for critical requirements. Template existence is guarded by `tests/test-template-references.sh:121-142`, and PR-loop integration coverage includes goal tracker creation checks in `tests/test-pr-loop-system.sh` references around `prompt-template/pr-loop/goal-tracker-initial.md`.
- edge_cases_or_failure_modes: Missing templates degrade to inline fallbacks via `load_and_render_safe`, so setup can still produce prompts (`scripts/setup-pr-loop.sh:660-678`, `scripts/setup-pr-loop.sh:687-720`, `scripts/setup-pr-loop.sh:723-767`). Comment detection is sentinel-based: only a comment file containing exactly `*No comments found.*` selects the no-comment flow; any other content counts as comments (`scripts/setup-pr-loop.sh:643-649`). Template variables are single-pass rendered, so injected `{{...}}` in comments or values should remain literal.
- validation_or_tests: `tests/run-all-tests.sh` includes `test-pr-loop.sh`, `test-pr-loop-system.sh`, `test-template-references.sh`, and comprehensive template suites (`tests/run-all-tests.sh:38-40`, `tests/run-all-tests.sh:53-54`). `tests/test-template-references.sh` scans shell call sites for referenced templates and fails on missing files (`tests/test-template-references.sh:83-110`, `tests/test-template-references.sh:213-229`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-wide executable test orchestrator. It is not algorithm logic itself, but it is the primary specification surface that orders and aggregates validation for RLCR hooks, PR-loop behavior, template rendering, validators, monitor behavior, and robustness suites.
- algorithmic_behavior: Defines an ordered `TEST_SUITES` array and runs each existing suite from `tests/` (`tests/run-all-tests.sh:32-73`). It special-cases suites requiring zsh (`tests/run-all-tests.sh:75-79`, `tests/run-all-tests.sh:97-114`), captures suite output and exit code without aborting the harness (`tests/run-all-tests.sh:104-119`), strips ANSI escape sequences, extracts final `Passed:` and `Failed:` counts, and totals them (`tests/run-all-tests.sh:121-128`).
- inputs_outputs_state: Input is the repository test tree plus local availability of interpreters such as `zsh`. Output is console status per suite, aggregate pass/fail totals, a failed-suite list, and process exit code `0` only when no suite failed (`tests/run-all-tests.sh:141-158`). It does not mutate product state except by running child test scripts that create their own temporary directories.
- gates_or_invariants: Missing suites are skipped rather than failed (`tests/run-all-tests.sh:83-86`). A suite fails the aggregate if its exit code is nonzero or its parsed failed count is greater than zero (`tests/run-all-tests.sh:130-135`). For failed suites, only the last 30 output lines are shown to keep diagnostics bounded (`tests/run-all-tests.sh:133-134`).
- dependencies_and_callers: Depends on each listed test being executable or runnable via its shebang; zsh tests require `command -v zsh` or are skipped (`tests/run-all-tests.sh:106-113`). It includes the assigned robustness suite at `tests/robustness/test-hook-system-robustness.sh` (`tests/run-all-tests.sh:69`) and broad coverage for template loading, template references, PR loop, and hook robustness (`tests/run-all-tests.sh:34-72`).
- edge_cases_or_failure_modes: Count extraction assumes child suites print `Passed:` and `Failed:` summary lines; otherwise totals default to `0` via `|| echo "0"` (`tests/run-all-tests.sh:124-125`). Because missing tests are skipped, absence is visible but not a hard failure. A zsh-specific suite is skipped when zsh is absent, which may hide zsh-only regressions on minimal systems.
- validation_or_tests: This file is itself a harness rather than a unit under direct test. Its behavior is exercised by execution: `./tests/run-all-tests.sh` should return `0` for all passing suites and `1` if any child suite fails. I did not run it because this assignment is read-only research notes and the full suite may create extensive temporary execution.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-074 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared template engine for hook and setup messages. It locates `prompt-template/`, loads markdown templates, renders `{{VARIABLE}}` placeholders, provides safe fallback rendering, and validates expected template subdirectories.
- algorithmic_behavior: `get_template_dir` derives the plugin root from `hooks/lib` and returns `<plugin-root>/prompt-template` (`hooks/lib/template-loader.sh:24-31`). `load_template` emits file contents or an empty string with warning if missing (`hooks/lib/template-loader.sh:36-48`). `render_template` builds `TMPL_VAR_*` environment variables and uses an awk scanner to replace placeholders in a single pass (`hooks/lib/template-loader.sh:58-132`). `load_and_render` composes load plus render (`hooks/lib/template-loader.sh:136-147`), `append_template` concatenates another template (`hooks/lib/template-loader.sh:151-161`), and `load_and_render_safe` uses a fallback if loading or rendering is empty (`hooks/lib/template-loader.sh:170-203`).
- inputs_outputs_state: Inputs are a template directory, relative template name, fallback text when using the safe API, and `VAR=value` assignments. Output is rendered markdown on stdout; warnings/errors go to stderr for missing templates or invalid template directory. No persistent state is changed.
- gates_or_invariants: Rendering is intentionally single-pass so a variable value containing `{{OTHER_VAR}}` is not recursively expanded (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:117-125`). Missing variables remain as original placeholders (`hooks/lib/template-loader.sh:119-122`). `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`), though it does not require `pr-loop`.
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh:138-147`, which initializes `TEMPLATE_DIR` and warns without failing when validation fails. Used broadly by validators and stop hooks through `load_and_render_safe`, including `hooks/loop-read-validator.sh:127-149`, `hooks/loop-write-validator.sh:207-262`, `hooks/loop-codex-stop-hook.sh` template calls, and `scripts/setup-pr-loop.sh:632-862`.
- edge_cases_or_failure_modes: Variable names are not syntactically validated before becoming environment variable names; malformed names can be passed by callers, though normal templates use uppercase identifiers as documented (`hooks/lib/template-loader.sh:7-13`). Multiline values are carried through environment assignment and awk output; tests cover this. Empty existing templates fall back only through `load_and_render_safe`, not `load_and_render`.
- validation_or_tests: `tests/test-template-loader.sh` covers directory resolution, missing and existing loads, single/multiple/multiline substitutions, safe fallback, directory validation, shell metacharacters, placeholder injection prevention, empty values, Unicode, underscores, and numeric names (`tests/test-template-loader.sh:41-244`, `tests/test-template-loader.sh:247-641`). Additional stress/error coverage appears in `tests/test-templates-comprehensive.sh`, `tests/robustness/test-template-stress-robustness.sh`, and `tests/robustness/test-template-error-robustness.sh`; reference existence is checked by `tests/test-template-references.sh:83-110`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR read validation when an agent attempts to read a prompt or summary file from a non-current round.
- algorithmic_behavior: Renders the attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, states the authoritative `{{CURRENT_ROUND}}`, lists the current prompt and summary paths under `{{ACTIVE_LOOP_DIR}}`, and includes an explicit `cat {{FILE_PATH}}` instruction if the user truly needs that file (`prompt-template/block/wrong-round-file.md:1-9`).
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`. Output is a markdown reason emitted to stderr by the read validator; no state file changes occur.
- gates_or_invariants: This template supports the invariant that only current-round RLCR prompt/summary context should be read unless the file is allowlisted. The executable gate is in `hooks/loop-read-validator.sh:138-150`, which exits `2` after rendering this template when `CLAUDE_ROUND != CURRENT_ROUND` and `is_allowlisted_file` is false.
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-round-file.md"` in `hooks/loop-read-validator.sh:144-149`. The validator first confirms the active loop, parses `state.md` or `finalize-state.md` strictly, extracts the current round, classifies prompt/summary file type, and validates location before using this template (`hooks/loop-read-validator.sh:69-117`, `hooks/loop-read-validator.sh:122-132`).
- edge_cases_or_failure_modes: If the template is missing or empty, the read validator has an inline fallback (`hooks/loop-read-validator.sh:139-143`). If state parsing fails, the read validator blocks earlier with a malformed-state error rather than rendering this template (`hooks/loop-read-validator.sh:94-99`). If no active loop exists, the read is allowed (`hooks/loop-read-validator.sh:84-86`).
- validation_or_tests: `tests/test-template-loader.sh` validates the sibling wrong-round-number rendering path for real templates (`tests/test-template-loader.sh:147-164`). `tests/test-template-references.sh` verifies referenced templates exist. The assigned robustness suite exercises malformed state and read-validator JSON edge cases, including escaped null and nested JSON handling (`tests/robustness/test-hook-system-robustness.sh:637-664`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-134 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for hook behavior across edit, plan-file, bash, read/write, stop-hook state parsing, and JSON edge cases. It verifies that hook scripts fail closed where loop metadata is protected and fail gracefully where malformed inputs appear.
- algorithmic_behavior: Sets strict shell mode, sources shared loop functions, portable timeout, and test helpers, then creates an isolated temp directory (`tests/robustness/test-hook-system-robustness.sh:18-26`). It runs 21 numbered tests: edit JSON and protected state checks (`:40-155`), plan-file validator behavior (`:165-243`), state parsing tolerance (`:253-307`), bash command protection (`:317-382`), string/content edge cases (`:384-411`), concurrent read-validator invocations (`:421-451`), stop-hook state handling (`:461-613`), and large/null/nested JSON cases (`:623-664`).
- inputs_outputs_state: Inputs are synthetic JSON hook payloads piped to hook scripts, generated `.humanize/rlcr/.../state.md` files, mock git repositories, and a mock `codex` executable for stop-hook isolation (`tests/robustness/test-hook-system-robustness.sh:562-604`). Outputs are pass/fail counters through `tests/test-helpers.sh`, with final exit status from `print_test_summary` (`tests/robustness/test-hook-system-robustness.sh:667-672`).
- gates_or_invariants: Confirms state-file edits through Edit and Bash are blocked with nonzero or exit `2` (`tests/robustness/test-hook-system-robustness.sh:53-78`, `:317-350`), path traversal to RLCR `state.md` is blocked (`:124-155`), goal-tracker writes after round 0 are blocked (`:353-367`), unrelated filesystem/security concerns are delegated to the sandbox rather than these hooks (`:109-122`, `:369-382`), active valid loops block stop attempts with JSON `decision:block` (`:559-613`), and no-state conditions allow exit (`:461-490`).
- dependencies_and_callers: Calls `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. Depends on `hooks/lib/loop-common.sh` for `get_current_round`, `scripts/portable-timeout.sh` for `run_with_timeout`, `jq` indirectly through validators, and `git` for branch/plan validation setup.
- edge_cases_or_failure_modes: The suite explicitly accepts either parse-default or parsed value for malformed frontmatter in `get_current_round` (`tests/robustness/test-hook-system-robustness.sh:290-307`), requiring graceful behavior rather than one fixed policy. For missing `file_path`, malformed JSON, large JSON, escaped null, and nested JSON, the invariant is exit code below `128`, preventing crashes/signals (`:80-107`, `:623-664`). It also checks concurrent hook invocations do not corrupt state by launching ten read validators in background and re-reading current round (`:421-451`).
- validation_or_tests: Included in the all-suite runner at `tests/run-all-tests.sh:69`. It validates production hooks against concrete state machine behavior: strict parser semantics come from `hooks/lib/loop-common.sh:242-324`, PR loop stop missing-state pass-through comes from `hooks/pr-loop-stop-hook.sh:64-76`, and RLCR stop-hook unexpected-state handling comes from `hooks/loop-codex-stop-hook.sh:129-154`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5, each assigned item appears once as an `Item Evidence` heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`