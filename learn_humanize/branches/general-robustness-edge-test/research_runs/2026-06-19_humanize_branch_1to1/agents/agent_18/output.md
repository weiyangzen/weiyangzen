# agent_18 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for canceling the active PR loop. It is a thin command contract: the markdown delegates all cancellation logic to `scripts/cancel-pr-loop.sh` and constrains Claude to only invoke that script through the allowed tool declarations at `commands/cancel-pr-loop.md:3`.
- algorithmic_behavior: The command instructs a single script invocation, `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"`, at `commands/cancel-pr-loop.md:11-15`, then branches on the first output line at `commands/cancel-pr-loop.md:17-20`. `NO_LOOP` and `NO_ACTIVE_LOOP` map to a user-facing no-active-loop response; `CANCELLED` maps to reporting the script’s cancellation message.
- inputs_outputs_state: Inputs are the plugin root environment variable used to locate the script and the PR-loop state on disk under `.humanize/pr-loop/`. The command states the active-loop predicate: `state.md` exists in the newest PR loop directory at `commands/cancel-pr-loop.md:21`. The delegated script resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, selects the newest directory with `ls | sort -r | head -1`, and requires `state.md` at `scripts/cancel-pr-loop.sh:72-98`. On success it creates `.cancel-requested`, renames `state.md` to `cancel-state.md`, and emits `CANCELLED` with PR/round details at `scripts/cancel-pr-loop.sh:118-130`.
- gates_or_invariants: The command is PR-loop-only and explicitly excludes RLCR loops, with the RLCR alternative named at `commands/cancel-pr-loop.md:25`. It preserves loop artifacts for reference at `commands/cancel-pr-loop.md:23`. The command-level invariant is that the script owns cancellation semantics, not the assistant text flow, at `commands/cancel-pr-loop.md:21`. The script’s gate is active `state.md`; an existing newest loop directory without that file yields `NO_ACTIVE_LOOP` at `scripts/cancel-pr-loop.sh:91-98`.
- dependencies_and_callers: Depends on `scripts/cancel-pr-loop.sh`, Bash, `CLAUDE_PLUGIN_ROOT`, and the `.humanize/pr-loop/<session>/state.md` layout. Related setup/help text references `/humanize:cancel-pr-loop` in `commands/start-pr-loop.md:62` and `scripts/setup-pr-loop.sh` per the repository search. Behavioral tests for the script live in `tests/test-pr-loop-scripts.sh:181-252`.
- edge_cases_or_failure_modes: No loop directory produces `NO_LOOP` and exit 1 at `scripts/cancel-pr-loop.sh:78-82`. Newest loop directory present but inactive produces `NO_ACTIVE_LOOP` and exit 1 at `scripts/cancel-pr-loop.sh:91-98`. Missing frontmatter values default to `?` for round/max/PR output at `scripts/cancel-pr-loop.sh:104-112`. `--force` is allowed by the command metadata but currently has no additional behavioral effect in the script at `scripts/cancel-pr-loop.sh:24-30` and `scripts/cancel-pr-loop.sh:40`.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` checks help output, no-loop `NO_LOOP` behavior, and successful cancellation with state rename at `tests/test-pr-loop-scripts.sh:181-252`. The command markdown itself is not directly executable; its correctness is validated indirectly through the script and command reference consistency.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-048 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI escape stripping and pass/fail count extraction used by the aggregate test runner. The file explicitly says it tests the parsing used in `run-all-tests.sh` and compatibility with GNU and BSD `sed` at `tests/test-ansi-parsing.sh:3-6`.
- algorithmic_behavior: Defines local `pass` and `fail` counters at `tests/test-ansi-parsing.sh:18-31`, then runs eight cases over the same algorithm: set `esc=$'\033'`, strip SGR color sequences with `sed "s/${esc}\\[[0-9;]*m//g"`, and use `grep -oE` to extract numeric `Passed:` or `Failed:` counts. The production parser is in `tests/run-all-tests.sh:121-128`.
- inputs_outputs_state: Inputs are synthetic strings containing ANSI SGR sequences, plain text summaries, multiline summaries, and combined bold/color sequences. Outputs are incremented `TESTS_PASSED` or `TESTS_FAILED` counters and final process status 0 when all assertions pass, 1 otherwise at `tests/test-ansi-parsing.sh:163-178`. State is local to the script counters; no repository state is mutated.
- gates_or_invariants: The key invariant is portable escape construction with Bash ANSI-C quoting (`$'\033'`) rather than non-portable sed hex escapes, documented at `tests/test-ansi-parsing.sh:38-40`. The extraction invariant is that final counts must come from normalized plain text matching `Passed:[[:space:]]*[0-9]+` or `Failed:[[:space:]]*[0-9]+`, mirroring `tests/run-all-tests.sh:124-125`.
- dependencies_and_callers: Depends on Bash, `sed`, `grep -E`, `tail`, and shell ANSI-C quoting. It is included in the full test-suite list at `tests/run-all-tests.sh:32-44`. It validates the parser used after each suite execution in `tests/run-all-tests.sh:104-137`.
- edge_cases_or_failure_modes: Covers basic color stripping, multiple colored spans, colored pass count extraction, colored fail count extraction, zero fail count, multiline suite output, no ANSI codes, and bold+color combined sequences at `tests/test-ansi-parsing.sh:42-158`. The sed expression only targets SGR sequences ending in `m`; non-SGR terminal controls are outside this test’s scope.
- validation_or_tests: This file is itself a focused validation suite. It is also part of the aggregate runner, so failures in the parsing algorithm would affect suite totals and suite pass/fail classification in `tests/run-all-tests.sh:127-136`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the PR-loop force-push transition. It communicates that review state is reset because commit history changed non-fast-forward, and it instructs the user to post a fresh bot trigger comment.
- algorithmic_behavior: The template renders `{{OLD_COMMIT}}`, `{{NEW_COMMIT}}`, and `{{BOT_MENTION_STRING}}` into a block reason at `prompt-template/block/force-push-detected.md:1-17`. It defines the recovery sequence: state has been updated to the new commit SHA, a new trigger comment is required, and the comment must mention the configured bot mention string at `prompt-template/block/force-push-detected.md:7-15`.
- inputs_outputs_state: Inputs are template variables passed by the PR stop hook. The hook preserves the old SHA, obtains current HEAD, optionally fetches the new commit timestamp from GitHub, updates state fields `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id`, then renders this template at `hooks/pr-loop-stop-hook.sh:382-417`. Output is a JSON block decision with the rendered template as `reason` and a force-push system message at `hooks/pr-loop-stop-hook.sh:419-421`.
- gates_or_invariants: The gate triggers only when the stored PR commit SHA differs from current HEAD and the stored SHA is not an ancestor of current HEAD, i.e. non-fast-forward history rewrite, at `hooks/pr-loop-stop-hook.sh:370-380`. The invariant is that stale trigger state must be cleared before continuing, implemented by blanking both trigger timestamp and trigger comment id at `hooks/pr-loop-stop-hook.sh:396-410`.
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh:415-417` through `load_and_render_safe`. Rendering semantics come from `hooks/lib/template-loader.sh`, which performs single-pass `{{VAR}}` substitution and leaves missing variables intact at `hooks/lib/template-loader.sh:50-132`. The template loader’s fallback path is available through `load_and_render_safe` at `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If GitHub commit timestamp lookup fails, the hook falls back to current UTC time at `hooks/pr-loop-stop-hook.sh:388-394`. If the template is missing or renders empty, the hook uses its inline fallback message at `hooks/pr-loop-stop-hook.sh:412-417` and `hooks/lib/template-loader.sh:179-199`. The direct template reference scanner in `tests/test-template-references.sh` does not include `hooks/pr-loop-stop-hook.sh` in its checked script list at `tests/test-template-references.sh:56-64`, so this caller is less directly covered by that specific test.
- validation_or_tests: General template loading and syntax are covered by the comprehensive template test, which loads every markdown template and checks placeholder syntax at `tests/test-templates-comprehensive.sh:82-180`. `load_and_render_safe` fallback behavior is covered in `tests/test-template-loader.sh:197-220`. Force-push hook behavior is not directly isolated by the inspected tests.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template for the RLCR path when code review was skipped. It turns an incomplete review gate into a controlled finalize transition with manual-verification obligations.
- algorithmic_behavior: The template injects `{{REVIEW_SKIP_REASON}}`, `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`. It warns that validation was incomplete, asks for manual code review and tests, optionally allows simplification through the named subagent, imposes functionality-preserving constraints, and requires a finalize summary at `prompt-template/claude/finalize-phase-skipped-prompt.md:1-50`.
- inputs_outputs_state: Inputs are loop state variables and file paths passed by `enter_finalize_phase`. If `skip_reason` is non-empty, the stop hook renames `state.md` to `finalize-state.md`, chooses `finalize-summary.md`, renders this template, and returns a JSON block decision with the rendered prompt at `hooks/loop-codex-stop-hook.sh:1000-1041` and `hooks/loop-codex-stop-hook.sh:1071-1079`.
- gates_or_invariants: The template’s constraints are explicit: no functionality changes, no existing test failures, no new bugs, and only functionality-equivalent cleanup at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`. The finalize transition itself is gated by the hook’s state rename into `finalize-state.md`, which makes finalize phase detectable as active loop state by `find_active_loop` at `hooks/lib/loop-common.sh:150-173`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:1035-1041` through `load_and_render_safe`. Rendering depends on `hooks/lib/template-loader.sh` single-pass substitution rules at `hooks/lib/template-loader.sh:50-132`. The referenced plan and goal tracker paths are surfaced as `@{{PLAN_FILE}}` and `@{{GOAL_TRACKER_FILE}}` in the template at `prompt-template/claude/finalize-phase-skipped-prompt.md:35-39`.
- edge_cases_or_failure_modes: If the skipped-review reason is empty, the hook uses the normal finalize template instead at `hooks/loop-codex-stop-hook.sh:1042-1068`. If this skipped template is unavailable, an inline fallback with the same core warning and constraints is rendered at `hooks/loop-codex-stop-hook.sh:1008-1035`. Missing template variables remain as placeholders because the renderer intentionally preserves unresolved variables at `hooks/lib/template-loader.sh:119-122`.
- validation_or_tests: General template load and syntax coverage comes from `tests/test-templates-comprehensive.sh:82-180`; safe fallback behavior is covered at `tests/test-template-loader.sh:197-220`. Finalize-state detection and related validators are tested in `tests/test-finalize-phase.sh:286-355` and `tests/robustness/test-state-transition-robustness.sh:148-172`, but the inspected tests do not directly assert this skipped-review prompt’s exact rendered content.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-138 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for loop session discovery, specifically `find_active_loop` in `hooks/lib/loop-common.sh`. It covers concurrent/multiple sessions, deleted or missing state, odd directory names, symlinks, shallow-depth behavior, and terminal state files.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates a temporary test directory, then performs 18 assertions against `find_active_loop` at `tests/robustness/test-session-robustness.sh:14-20` and `tests/robustness/test-session-robustness.sh:33-317`. The function under test selects the lexicographically newest immediate child directory, then returns it only if it contains `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:150-173`.
- inputs_outputs_state: Inputs are synthetic `.humanize`-style session directory trees under `TEST_DIR/rlcr`. Outputs are pass/fail counter increments from `tests/test-helpers.sh:30-44`, a printed summary from `tests/test-helpers.sh:58-78`, and script exit status from `print_test_summary` at `tests/robustness/test-session-robustness.sh:322-323`. State transitions are test-local: each case removes and recreates `$TEST_DIR/rlcr` to model session lifecycle changes.
- gates_or_invariants: The central invariant is newest-only detection: older sessions are ignored even if they have `state.md`, preventing stale sessions from being revived. This is documented in the implementation at `hooks/lib/loop-common.sh:150-153` and asserted at `tests/robustness/test-session-robustness.sh:89-103` and `tests/robustness/test-session-robustness.sh:287-302`. Another invariant is that only `state.md` and `finalize-state.md` indicate active loops; `complete-state.md` and `cancel-state.md` do not, asserted at `tests/robustness/test-session-robustness.sh:259-285`.
- dependencies_and_callers: Depends on Bash, filesystem operations, `mktemp`, `ls`, `sort`, `head`, and the shared test helpers. `find_active_loop` is used by validators and setup/stop hooks across the repository, including `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and RLCR/PR setup scripts per the repository search. This robustness suite is included in the aggregate test runner at `tests/run-all-tests.sh:55-58`.
- edge_cases_or_failure_modes: Covers empty base directory, nonexistent base directory, base with files but no subdirectories, sessions without active state files, unexpected naming, symlinked directory, spaces in directory names, deep nested paths ignored beyond one level, rapid session creation, terminal complete/cancel states, mixed finished/newer versus older active sessions, and a nominal Unicode-name case at `tests/robustness/test-session-robustness.sh:128-317`. The test confirms implementation behavior but also exposes that ordering is lexicographic rather than timestamp-validated.
- validation_or_tests: This file is itself a validation suite and is run by `tests/run-all-tests.sh`. Related coverage exists in `tests/robustness/test-concurrent-state-robustness.sh` and `tests/robustness/test-state-transition-robustness.sh`, which also exercise `find_active_loop` around finalize, cancel, and invalid state transitions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections in Item Evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`