# agent_10 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Shared blocking-message template set for RLCR and PR-loop hooks. The directory is a flat recursive set of Markdown templates that turns detected guard conditions into actionable user-facing stop/read/write/bash validation messages.
- algorithmic_behavior: The templates encode hook outcomes for state protection, path/round validation, git cleanliness, PR-loop review transitions, schema failure, and summary/todo gates. Examples include state mutation blocks in `state-file-modification.md:1-10`, PR-loop state mutation blocks in `pr-loop-state-modification.md:1-12`, wrong-round/path guidance in `wrong-round-number.md:1-7` and `wrong-file-location.md:1-9`, git/push gates in `git-not-clean.md:1-16`, `git-push.md:1-9`, and remote-review transitions in `force-push-detected.md:1-17`, `no-trigger-comment.md:1-17`, `claude-eyes-timeout.md:1-18`, and `codex-review-failed.md:1-18`.
- inputs_outputs_state: Inputs are uppercase `{{PLACEHOLDER}}` values supplied by callers through `load_and_render_safe`; output is Markdown text returned to hooks as block reasons or system messages. The templates do not mutate state directly; they describe state invariants, required files, current round numbers, PR metadata, or commands the user must run.
- gates_or_invariants: Directory-wide placeholder syntax is expected to be uppercase identifiers. The template loader documents single-pass rendering and missing-variable preservation in `hooks/lib/template-loader.sh:7-13`, and `render_template` performs single-pass `awk` substitution in `hooks/lib/template-loader.sh:50-131`. Guard invariants expressed here include “state files are system-managed,” “prompt files are read-only,” “round files must match the current round,” “summaries must be written before exit,” “unpushed commits block exit when pushing is required,” and “force-push/reset cases require a fresh trigger comment.”
- dependencies_and_callers: Used through `hooks/lib/template-loader.sh` by hook and script callers. Concrete callers include common validator helpers referencing block templates in `hooks/lib/loop-common.sh:445-494`, bash validator use of `git-push.md` and `plan-backup-protected.md` in `hooks/loop-bash-validator.sh:103` and `hooks/loop-bash-validator.sh:338`, RLCR stop-hook use of `git-not-clean`, `large-files`, `work-summary-missing`, and `unpushed-commits` in `hooks/loop-codex-stop-hook.sh:470-599`, and PR stop-hook use of `unpushed-commits`, `force-push-detected`, `no-trigger-comment`, and `claude-eyes-timeout` in `hooks/pr-loop-stop-hook.sh:356-774`.
- edge_cases_or_failure_modes: If a template is missing or empty, callers using `load_and_render_safe` fall back to inline text (`hooks/lib/template-loader.sh:170-203`). If a placeholder value is absent, the placeholder remains literal, which is useful for safe degradation but can expose unresolved template variables. Runtime rendering does not enforce syntax validity; CI-style tests provide that validation.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads every `*.md` template under `prompt-template`, validates malformed placeholder patterns, renders real templates, and stress-renders all templates with dummy variables (`tests/test-templates-comprehensive.sh:90-194`, `tests/test-templates-comprehensive.sh:562-606`). `tests/test-template-references.sh:145-200` verifies important template references exist and that critical validators use safe rendering.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime setup script for `/humanize:start-pr-loop`; it initializes the PR review loop state machine that monitors remote bot reviewers, writes initial loop artifacts, and prepares the first Claude-facing prompt.
- algorithmic_behavior: Parses `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout` (`scripts/setup-pr-loop.sh:110-173`), requires at least one bot (`scripts/setup-pr-loop.sh:179-190`), builds active bot state (`scripts/setup-pr-loop.sh:192-202`), blocks if any RLCR or PR loop is already active (`scripts/setup-pr-loop.sh:221-241`), verifies git/gh/codex prerequisites (`scripts/setup-pr-loop.sh:243-277`), resolves the current branch and PR across current/parent/base repositories for fork support (`scripts/setup-pr-loop.sh:283-359`), validates YAML-safe state values (`scripts/setup-pr-loop.sh:361-388`), creates `.humanize/pr-loop/<timestamp>` (`scripts/setup-pr-loop.sh:394-400`), fetches initial comments (`scripts/setup-pr-loop.sh:406-412`), determines startup case (`scripts/setup-pr-loop.sh:418-435`), optionally posts a trigger comment for stale-review cases 4/5 (`scripts/setup-pr-loop.sh:443-459`), verifies Claude eyes reaction when required (`scripts/setup-pr-loop.sh:484-520`), then writes `state.md`, `goal-tracker.md`, `round-0-prompt.md`, and activation output (`scripts/setup-pr-loop.sh:535-865`).
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR`, template directory resolution, current git branch, GitHub PR metadata/comments/reactions, and Codex/GitHub CLI availability. Outputs are `.humanize/pr-loop/<timestamp>/state.md` with PR/bot/codex/polling/startup metadata (`scripts/setup-pr-loop.sh:535-555`), `goal-tracker.md` (`scripts/setup-pr-loop.sh:561-633`), `round-0-pr-comment.md` via fetch script, `round-0-pr-resolve.md` path, `round-0-prompt.md` (`scripts/setup-pr-loop.sh:641-771`), and stdout setup instructions plus the initial prompt (`scripts/setup-pr-loop.sh:780-862`).
- gates_or_invariants: Enforces one active loop at a time, numeric `--max` and timeout values, numeric PR number, open PR state, YAML-safe branch/model/effort strings, mandatory trigger-comment posting for cases 4/5, mandatory trigger-comment ID and Claude eyes reaction when `--claude` is involved in that path, and cleanup of the just-created loop directory on trigger/eyes failure (`scripts/setup-pr-loop.sh:457`, `scripts/setup-pr-loop.sh:500-518`).
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` for `run_with_timeout` (`scripts/setup-pr-loop.sh:33-35`; implementation in `scripts/portable-timeout.sh:31-71`) and `hooks/lib/template-loader.sh` (`scripts/setup-pr-loop.sh:37-43`). Sources `hooks/lib/loop-common.sh` for `find_active_loop`, `find_active_pr_loop`, `build_bot_mention_string`, and `build_yaml_list` (`scripts/setup-pr-loop.sh:210-214`; helpers in `hooks/lib/loop-common.sh:745-808`). Calls `scripts/fetch-pr-comments.sh` (`scripts/setup-pr-loop.sh:411-412`), `scripts/check-pr-reviewer-status.sh` (`scripts/setup-pr-loop.sh:418-429`), and `scripts/check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:505-507`).
- edge_cases_or_failure_modes: Handles fork PRs by trying current repo, parent repo, then base repo lookups (`scripts/setup-pr-loop.sh:295-359`). If reviewer-status probing fails, it defaults to startup case 1 with empty metadata (`scripts/setup-pr-loop.sh:419-422`). It intentionally avoids local-time fallback for trigger timestamps to prevent clock-skew filtering bugs (`scripts/setup-pr-loop.sh:479-482`). Comment presence is inferred from the exact sentinel `*No comments found.*` (`scripts/setup-pr-loop.sh:643-649`). The script uses `jq` directly for reviewer JSON parsing but does not perform its own explicit `jq` availability check before those calls.
- validation_or_tests: Argument behavior is covered by `tests/robustness/test-setup-scripts-robustness.sh:473-510`. PR-loop system coverage includes mutual exclusion (`tests/test-pr-loop-system.sh:120-127`), failure when a Case 4/5 Claude trigger comment ID cannot be retrieved (`tests/test-pr-loop-system.sh:1384-1410`), and goal-tracker creation through setup (`tests/test-pr-loop-system.sh:1453-1480`). I inspected statically only; I did not execute tests in this read-only branch export.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template subsystem, including directory structure, template loadability, placeholder syntax, fallback behavior, real-template rendering, and stress rendering.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and resolves `TEMPLATE_DIR` (`tests/test-templates-comprehensive.sh:15-59`). It verifies `prompt-template` contains `block`, `codex`, and `claude` subdirectories (`tests/test-templates-comprehensive.sh:64-80`), loads every Markdown template with `find ... -print0 | sort -z` (`tests/test-templates-comprehensive.sh:90-107`), validates placeholder syntax with regex checks for extra braces, wrong bracket types, unclosed candidates, spaces, and lowercase names (`tests/test-templates-comprehensive.sh:118-194`), self-tests malformed placeholder detection (`tests/test-templates-comprehensive.sh:204-259`), exercises render edge cases (`tests/test-templates-comprehensive.sh:266-438`), tests safe fallback behavior (`tests/test-templates-comprehensive.sh:446-490`), renders selected real templates (`tests/test-templates-comprehensive.sh:497-540`), then stress-tests rapid calls and all-template rendering with dummy placeholder values (`tests/test-templates-comprehensive.sh:547-606`).
- inputs_outputs_state: Inputs are all `prompt-template/**/*.md` files and the template-loader API. Output is colored PASS/FAIL/WARN console text plus process exit status. State is limited to counters `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` (`tests/test-templates-comprehensive.sh:26-50`) and temporary directories created for malformed/fallback checks.
- gates_or_invariants: Missing template directory is fatal (`tests/test-templates-comprehensive.sh:66-72`). Missing subdirectories, load failures, syntax errors, failed fallback behavior, failed real-template renders, and failed stress renders increment failures. Warnings are tracked but not fatal; final exit is `0` only when `TESTS_FAILED` is zero (`tests/test-templates-comprehensive.sh:611-625`). Placeholder convention is uppercase `{{VAR_NAME}}` (`tests/test-templates-comprehensive.sh:122-124`, `tests/test-templates-comprehensive.sh:171-175`).
- dependencies_and_callers: Depends directly on `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` from `hooks/lib/template-loader.sh`. It validates assigned template `prompt-template/block/unpushed-commits.md` as a real integration template in `tests/test-templates-comprehensive.sh:517-525`.
- edge_cases_or_failure_modes: Tests empty content, single/multiline content, empty-line preservation, regex-special values, backslashes, quotes, CJK-safe content, ASCII status patterns, Markdown formatting, long values, multiline values, variable positions, repeated variables, and adjacent variables (`tests/test-templates-comprehensive.sh:266-438`). It intentionally uses `set -uo pipefail` rather than `set -e`, allowing broad failure collection before final summary.
- validation_or_tests: This file is itself the validation harness. I did not execute it because it creates temporary files and the current task requested research notes only in a read-only branch export.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Blocking template for the “unpushed commits” invariant: a loop cannot stop successfully while required commits exist locally but have not reached the remote branch/PR head.
- algorithmic_behavior: Renders a Markdown block titled “Unpushed Commits Detected,” interpolating `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, then instructs the user to run `git push origin {{CURRENT_BRANCH}}` before retrying exit (`prompt-template/block/unpushed-commits.md:1-12`).
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`. Output is hook-facing Markdown. It does not mutate state; it represents a blocked transition from “work complete/attempting stop” to “exit allowed” until remote sync is satisfied.
- gates_or_invariants: In RLCR stop flow, the template is used only when `push_every_round` is true and `git status -sb` reports the local branch ahead (`hooks/loop-codex-stop-hook.sh:548-575`). In PR-loop stop flow, it is used when the local branch is ahead of the upstream/remote/PR head comparison (`hooks/pr-loop-stop-hook.sh:330-360`).
- dependencies_and_callers: Rendered through `load_and_render_safe` in both RLCR and PR stop hooks, with inline fallbacks if the template is unavailable (`hooks/loop-codex-stop-hook.sh:559-566`, `hooks/pr-loop-stop-hook.sh:350-358`). The renderer itself preserves missing placeholders and substitutes provided values in one pass (`hooks/lib/template-loader.sh:50-131`).
- edge_cases_or_failure_modes: If caller omits variables, placeholders remain visible. The template text specifically says “Since `--push-every-round` is enabled” (`prompt-template/block/unpushed-commits.md:5`), which is exact for RLCR push-every-round usage but less precise for PR-loop usage where pushing is generally required for bot review.
- validation_or_tests: Explicit real-template test checks the title, ahead count, and branch substitution in `tests/test-templates-comprehensive.sh:517-525`. It is also included in all-template load/render stress coverage (`tests/test-templates-comprehensive.sh:562-606`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-130 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for loop state parsing, active-loop discovery, concurrent state reads, atomic-write behavior, PR-loop state shape, stale-loop detection, and permission/format edge cases.
- algorithmic_behavior: Creates an isolated test directory (`tests/robustness/test-concurrent-state-robustness.sh:15-20`), defines `create_state_file` to write frontmatter state (`tests/robustness/test-concurrent-state-robustness.sh:30-50`), then exercises `get_current_round`, `find_active_loop`, `parse_state_file_strict`, and `find_active_pr_loop` across malformed, concurrent, stale, and PR-loop scenarios.
- inputs_outputs_state: Inputs are synthetic state files and directories under `TEST_DIR`, plus functions from `hooks/lib/loop-common.sh`. Outputs are PASS/FAIL lines and a summary exit via `print_test_summary` (`tests/robustness/test-concurrent-state-robustness.sh:559-560`). State changes are temporary file/directory creation, background writer processes, and one permission-mode mutation restored before cleanup (`tests/robustness/test-concurrent-state-robustness.sh:530-545`).
- gates_or_invariants: `get_current_round` should default or parse gracefully for whitespace, CRLF, empty values, comments, Unicode content, very large values, and zero (`tests/robustness/test-concurrent-state-robustness.sh:59-160`, `tests/robustness/test-concurrent-state-robustness.sh:374-388`, `tests/robustness/test-concurrent-state-robustness.sh:495-528`). `find_active_loop` should consider only the newest timestamp directory and require `state.md` or `finalize-state.md`, preventing old/zombie loops from reviving (`tests/robustness/test-concurrent-state-robustness.sh:170-240`, `tests/robustness/test-concurrent-state-robustness.sh:453-485`). Concurrent reads must all see the actual round value, not the default (`tests/robustness/test-concurrent-state-robustness.sh:251-278`), and atomic `mv` writes must keep every read numeric (`tests/robustness/test-concurrent-state-robustness.sh:280-323`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` for counters, temporary directory setup, and summary handling (`tests/test-helpers.sh:30-89`). Sources `hooks/lib/loop-common.sh`, where `find_active_loop` checks only the newest directory (`hooks/lib/loop-common.sh:150-173`), `get_current_round` parses frontmatter with sed/grep (`hooks/lib/loop-common.sh:175-188`), strict parsing returns distinct validation failures (`hooks/lib/loop-common.sh:242-324`), and `find_active_pr_loop` requires `state.md` in the newest PR-loop directory (`hooks/lib/loop-common.sh:789-808`).
- edge_cases_or_failure_modes: Covers leading/trailing whitespace, CRLF line endings, empty `current_round`, inline comments, nonexistent/empty loop dirs, newest dir missing state while older dirs contain state, `finalize-state.md`, malformed frontmatter, no frontmatter, YAML list formatting for PR bots, cancelled loops, huge/zero rounds, and unreadable state files. One test label says malformed YAML, but the strict parser is shell/regex-based rather than a YAML parser; malformed values are primarily rejected through required-field and numeric checks.
- validation_or_tests: This file is the validation artifact for concurrent state behavior. I did not execute it in this run because it writes temporary state and launches background writers, while the assignment requested read-only research notes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 Item Evidence sections present, one per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`