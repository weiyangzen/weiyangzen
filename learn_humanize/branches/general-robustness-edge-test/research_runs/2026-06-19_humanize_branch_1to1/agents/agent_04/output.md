# agent_04 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: Hook orchestration and enforcement layer for Humanize’s RLCR and PR review loops. `hooks/hooks.json:4-64` wires `UserPromptSubmit`, `PreToolUse`, and `Stop` hooks; the directory coordinates state protection, loop exit gating, Codex review transitions, PR bot polling, and generated feedback prompts.
- algorithmic_behavior: The directory recursively contains command hooks, shared parsers, prompt rendering helpers, and a transcript todo checker. `hooks/lib/loop-common.sh` supplies core primitives: hook JSON validation and nesting limits (`58-132`), active loop discovery (`155-173`, `792-808`), strict state parsing (`251-324`), bot author mapping (`721-771`), PR-loop readonly file classification (`810-829`), PR resolve round validation (`837-880`), protected `.humanize` git-add detection (`924-1020`), shell write detection (`1065-1090`), and goal-tracker updates (`1162+`). `hooks/lib/template-loader.sh:58-132` renders `{{VAR}}` placeholders in a single pass, and `170-203` provides fallback-backed template loading.
- inputs_outputs_state: Inputs arrive from Claude hook stdin JSON, `.humanize/rlcr/<timestamp>/state.md`, `.humanize/pr-loop/<timestamp>/state.md`, local git state, GitHub CLI responses, Codex outputs, and prompt templates. Outputs are hook decisions as JSON blocks, stderr block messages, generated loop files such as `round-N-prompt.md`, `round-N-pr-comment.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, and renamed terminal state files such as `approve-state.md`, `complete-state.md`, `maxiter-state.md`, `usage-limit-state.md`, or `closed-state.md`. PR stop-hook state parsing occurs at `hooks/pr-loop-stop-hook.sh:87-176`; active bot state is rewritten at `1483-1567`.
- gates_or_invariants: Read/write/edit/bash validators block wrong-round RLCR summaries/prompts, todo files, loop state files, plan backup edits, goal-tracker edits after round 0, PR loop generated files, and PR resolve files for the wrong round (`hooks/loop-read-validator.sh:55-167`, `hooks/loop-write-validator.sh:55-263`, `hooks/loop-edit-validator.sh:38-183`, `hooks/loop-bash-validator.sh:115-443`). Plan validation blocks stale schema, branch switching, and plan tracking drift (`hooks/loop-plan-file-validator.sh:44-229`). RLCR stop hook blocks malformed state, plan modifications, incomplete todos, dirty git, large files, missing summaries, missing goal tracker initialization, and failed Codex review (`hooks/loop-codex-stop-hook.sh:129-370`, `403-617`, `619-721`, `973-993`, `1129-1202`). PR stop hook blocks missing resolution summaries, uncommitted or unpushed work, missing triggers, Claude eyes timeout, Codex absence/failure, empty Codex output, and waiting bots (`hooks/pr-loop-stop-hook.sh:258-361`, `717-789`, `1291-1344`, `1368-1389`).
- dependencies_and_callers: Parent/sibling coordination is via `scripts/setup-pr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, and `prompt-template/**`. PR setup writes the initial state and prompt (`scripts/setup-pr-loop.sh:535-555`, `651-681`). PR stop hook calls reaction checks at `hooks/pr-loop-stop-hook.sh:459`, `766`, and `985`, polling at `951`, and startup-case recalculation at `1533-1541`. RLCR stop hook calls `hooks/check-todos-from-transcript.py` at `hooks/loop-codex-stop-hook.sh:321-370`. Tests cover directory behavior through `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-plan-file-hooks.sh`, robustness suites, and template reference checks.
- edge_cases_or_failure_modes: The directory explicitly handles corrupted YAML frontmatter, missing schema fields, invalid UTF-8/null-byte hook input, deeply nested JSON, branch drift, force-pushed PR heads, fork PR base repository resolution, stale trigger comments after new commits, bot timeouts, duplicate bot comments, Codex `APPROVE`/`WAITING_FOR_BOTS`/`USAGE_LIMIT_HIT` markers, missing Codex CLI, empty Codex output, shell bypasses using redirection or `sh -c`, and unauthorized state renames. One notable asymmetry: `loop-edit-validator.sh:24-32` reads JSON through direct `jq` instead of the shared `validate_hook_input`, while read/write/bash use the stricter helper.
- validation_or_tests: `hooks/check-todos-from-transcript.py:20-129` validates Claude TodoWrite completion before expensive RLCR review. PR stop-hook integration tests enumerate force-push, trigger, timeout, fork, and goal-tracker cases in `tests/test-pr-loop-stophook.sh:1772-1786`. Robustness suites referenced by search include hook input/system/API robustness and allowlist validators.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-034 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime GitHub reaction detector used by PR loop setup and stop-hook gates. It determines whether Codex approved with a `+1` reaction on the PR issue and whether Claude acknowledged a trigger comment with an `eyes` reaction.
- algorithmic_behavior: The script dispatches by command at `scripts/check-bot-reactions.sh:79-86`. `codex-thumbsup` parses a PR number and optional `--after` timestamp (`87-117`), resolves the PR base repo for fork support (`123-147`), fetches issue reactions with pagination and merges pages through `jq -s 'add // []'` (`149-159`), selects `chatgpt-codex-connector[bot]` with `+1` (`161-165`), applies the timestamp filter (`172-180`), and prints the JSON reaction (`182-184`). `claude-eyes` parses comment ID, optional PR number, retry count, and delay (`187-227`), resolves the base repo (`233-257`), sleeps and retries (`259-291`), fetches comment reactions (`264-270`), and selects `claude[bot]` with `eyes` (`275-284`).
- inputs_outputs_state: Inputs are CLI args, `gh repo view`, `gh pr view`, and GitHub API reaction endpoints. Output is a single reaction JSON object on stdout when found. Exit code `0` means found, `1` means not found or retries exhausted, and `2` means argument/API error (`13-16`, `298-302`). It is stateless except for waiting between retry attempts.
- gates_or_invariants: Requires a command and required positional identifiers; unknown options exit `2` (`98-100`, `208-210`). PR reactions and comment reactions are queried on the base repository rather than the fork (`119-121`, `229-231`). Pagination is required to avoid missing reactions beyond GitHub’s default page size (`149-153`, `264-267`). The `--after` gate uses ISO timestamp string ordering, so it assumes comparable UTC-style timestamps (`174-180`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` (`31-33`), `gh`, `jq`, and GitHub API availability. Called by `scripts/setup-pr-loop.sh:507` for mandatory Claude eyes verification during setup, by `hooks/pr-loop-stop-hook.sh:459` and `985` for Codex `+1` approval, and by `hooks/pr-loop-stop-hook.sh:766` for Claude eyes verification. Tested in `tests/test-pr-loop-system.sh:199-229`.
- edge_cases_or_failure_modes: If base repo resolution fails, it falls back to current repo (`144-147`, `254-257`), which can still fail if current repo is empty/unavailable. For `claude-eyes`, API failures during attempts are treated as retryable and eventually return not found (`270-295`), whereas `codex-thumbsup` treats reaction fetch failure as an error (`154-159`). The retry loop sleeps before every attempt (`260-263`), including the first. Numeric validation for `--retry`/`--delay` is implicit through `seq` and `sleep`, not prechecked.
- validation_or_tests: `tests/test-pr-loop-system.sh:199-216` verifies Codex `+1` detection and `--after` filtering; `223-229` verifies Claude eyes detection. Stop-hook tests indirectly exercise both reaction paths for case 1 approval and Claude eyes timeout.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-064 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for the PR loop stop-hook state machine. It uses mocked `gh`, `git`, and `codex` binaries to validate trigger gating, commit gates, bot timeouts, active bot mutation, fork support, and goal-tracker behavior.
- algorithmic_behavior: The test suite is wrapped in `run_stophook_tests()` at `tests/test-pr-loop-stophook.sh:15` and invokes all scenarios at `1772-1784`. Each test constructs `.humanize/pr-loop/<timestamp>/state.md`, writes the expected `round-N-pr-resolve.md`, prepends a mock binary directory to `PATH`, sets `CLAUDE_PROJECT_DIR`, runs `hooks/pr-loop-stop-hook.sh`, then asserts on output or mutated files.
- inputs_outputs_state: Inputs are synthetic state frontmatter fields (`current_round`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, `trigger_comment_id`, bot lists), mocked GitHub API data, mocked git branch/status/history, and mocked Codex analysis. Outputs are pass/fail calls from the external test harness, plus assertions on state transitions such as `state.md` to `approve-state.md`, updates to `active_bots`, and changed `startup_case`.
- gates_or_invariants: Tests encode key PR stop-hook gates: old trigger comments are rejected after newer commits (`20-138`), startup case 1 skips trigger requirement (`140-243`), empty `active_bots` approves (`247-324`), unpushed commits block exit (`452-536`), force-push history rewrites block and require retrigger (`538-640`), startup cases 4/5 require triggers (`642-752`), timeouts remove active bots (`755-894`), Codex `+1` approval completes single-bot case (`896-1010`), Claude eyes timeout blocks (`1011-1139`), fork PR lookup resolves parent repository (`1396-1522`), and mixed approval does not prematurely complete (`1524-1769`).
- dependencies_and_callers: Depends on `PROJECT_ROOT`, `TEST_DIR`, external `pass`/`fail` functions, shell `timeout`, `jq`, and the actual `hooks/pr-loop-stop-hook.sh`. It constructs local mocks instead of using network calls, so it is a deterministic behavioral spec for the hook. It also implicitly specifies interactions with `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, and `codex exec` by mocking their upstream command surfaces.
- edge_cases_or_failure_modes: The suite targets stale triggers, force-push ancestry failure, no trigger comments, short poll timeout, no bot responses, reaction-only approval, missing Claude eyes, fork PRs where the current repo lacks the PR, and mixed bot statuses where an approving bot is removed but an issue-reporting bot remains. It verifies the regression that resolved issue counts must not be inflated when only some bots approve (`1715-1767`).
- validation_or_tests: This item is itself validation. The scenario list at `1772-1784` is the suite’s coverage manifest. I did not execute it in this read-only research run; evidence is from direct inspection.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-094 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that `.humanize/pr-loop/**/state.md` is system-managed and must not be modified by the agent.
- algorithmic_behavior: The template provides the user-facing reason title and explanation at `prompt-template/block/pr-loop-state-modification.md:1-12`. `hooks/lib/loop-common.sh:883-890` loads it through `pr_loop_state_blocked_message()`, falling back to inline text if missing.
- inputs_outputs_state: No runtime variables are consumed by this template. Its output becomes stderr/block reason text when validators detect write/edit/bash attempts against PR loop state. It does not mutate state; it protects state from mutation.
- gates_or_invariants: The explicit invariant is that modifying PR loop `state.md` would corrupt current round, PR number/branch, active bot configuration, Codex configuration, and polling settings (`3-12`). The write validator invokes it for PR-loop `state.md` writes (`hooks/loop-write-validator.sh:76-80`), the edit validator invokes it for edits (`hooks/loop-edit-validator.sh:59-63`), and bash protection invokes it for both full-path and bare `state.md` shell modification patterns (`hooks/loop-bash-validator.sh:403-415`).
- dependencies_and_callers: Depends on `load_and_render_safe` and `TEMPLATE_DIR`. Called indirectly by Claude PreToolUse hooks through `hooks/hooks.json:14-50`.
- edge_cases_or_failure_modes: Because the template has no placeholders, rendering cannot lose dynamic data. If missing, `loop-common.sh` fallback still enforces a block, so template absence degrades message quality rather than policy.
- validation_or_tests: Template references are covered by template-reference tests found via `tests/test-template-references.sh`, and the state-file block behavior is exercised by PR loop hook/allowlist tests that invoke write/edit/bash validators.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-124 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: Initial PR loop prompt header that establishes Round 0 context and injects PR metadata before fetched review comments.
- algorithmic_behavior: The template starts the prompt with “Read and execute below with ultrathink,” declares “PR Review Loop (Round 0),” and renders `{{PR_NUMBER}}`, `{{START_BRANCH}}`, and `{{ACTIVE_BOTS_DISPLAY}}` (`prompt-template/pr-loop/round-0-header.md:1-15`). `scripts/setup-pr-loop.sh:651-681` supplies those variables, loads the template, writes it to `round-0-prompt.md`, and then appends fetched comments at `683-684`.
- inputs_outputs_state: Inputs are setup-time PR number, branch, and active bot display string. Output is the header portion of `.humanize/pr-loop/<timestamp>/round-0-prompt.md`. It follows state creation (`scripts/setup-pr-loop.sh:535-555`) and goal-tracker creation (`561-635`), so it is part of the transition from initialized loop state to first agent-facing instructions.
- gates_or_invariants: It is not a blocker by itself, but it preserves the Round 0 contract: the agent is monitoring remote review bots, not performing an unrelated task, and review comments follow immediately under the “Review Comments” section. Downstream task templates add the required resolution summary path and trigger/no-trigger instructions (`scripts/setup-pr-loop.sh:686-719`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` single-pass rendering and on `scripts/setup-pr-loop.sh` fallback header at `660-678`. It coordinates with `fetch-pr-comments.sh` output through the appended comment file and with the stop hook, which later requires `round-0-pr-resolve.md`.
- edge_cases_or_failure_modes: If variables are missing, unresolved `{{...}}` placeholders remain because rendering preserves unknown placeholders by design (`hooks/lib/template-loader.sh:115-122`). If the template is missing or empty, setup uses an inline fallback equivalent (`scripts/setup-pr-loop.sh:660-678`).
- validation_or_tests: Template reference tests include prompt-template coverage; PR loop system/setup tests exercise round-0 prompt creation indirectly. Direct evidence is the setup caller at `scripts/setup-pr-loop.sh:677-681`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections present above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`