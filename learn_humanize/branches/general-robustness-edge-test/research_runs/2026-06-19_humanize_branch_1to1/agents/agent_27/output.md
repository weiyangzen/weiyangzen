# agent_27 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-027 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse edit gate for RLCR and PR-loop managed files. It decides whether Claude’s `Edit` tool may touch loop artifacts, while leaving non-`Edit` tools and non-loop paths to other validators or the runtime sandbox (`hooks/loop-edit-validator.sh:24-33`, `hooks/loop-edit-validator.sh:83-85`).
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, lowercases the path, then applies ordered guards. It blocks round todos except allowlisted active-loop files, blocks round prompts, applies PR-loop protections, detects the active RLCR loop, strict-parses `state.md` or `finalize-state.md`, blocks state files, blocks RLCR `plan.md` backups, blocks `goal-tracker.md` after Round 0, and validates summary filename round numbers (`hooks/loop-edit-validator.sh:24-51`, `hooks/loop-edit-validator.sh:57-77`, `hooks/loop-edit-validator.sh:91-109`, `hooks/loop-edit-validator.sh:117-181`).
- inputs_outputs_state: Input is Claude hook JSON plus environment-derived roots: `PROJECT_ROOT`, `CLAUDE_PROJECT_DIR`, optional `LOOP_BASE_DIR`, and optional `LOOP_DIR`. Output is process status and stderr guidance: `0` allows, `2` policy-blocks with a rendered template message, and malformed active state fails closed with `1` after printing `Error: Malformed state file` (`hooks/loop-edit-validator.sh:95-109`). It does not mutate loop state; it is a gate over proposed edits.
- gates_or_invariants: Prompt files are generated/read-only; state files are hook/system-owned; `finalize-state.md` is checked before generic `state.md` because the generic matcher also matches it; `goal-tracker.md` becomes read-only to Claude after Round 0; wrong-round summaries are blocked unless explicitly allowlisted (`hooks/loop-edit-validator.sh:48-51`, `hooks/loop-edit-validator.sh:115-125`, `hooks/loop-edit-validator.sh:145-149`, `hooks/loop-edit-validator.sh:155-179`). Shared allowlist entries include `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`hooks/lib/loop-common.sh:417-435`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and its template-loader transitively (`hooks/loop-edit-validator.sh:16-18`, `hooks/lib/loop-common.sh:138-148`). Key helpers are `find_active_loop`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, `is_goal_tracker_path`, `is_in_pr_loop_dir`, `is_pr_loop_readonly_file`, and `validate_pr_resolve_round` (`hooks/lib/loop-common.sh:155-173`, `hooks/lib/loop-common.sh:251-323`, `hooks/lib/loop-common.sh:395-500`, `hooks/lib/loop-common.sh:777-880`).
- edge_cases_or_failure_modes: Invalid JSON exits through `jq` under `set -euo pipefail` rather than a custom validation path. Missing `file_path` becomes empty and passes unless another classifier matches. Path containment uses substring/pattern checks, not canonicalization, but basename protections still catch traversal ending in `state.md`; robustness tests explicitly expect outside-`.humanize` traversal to pass through to the sandbox and `.humanize/.../state.md` traversal to block (`tests/robustness/test-hook-system-robustness.sh:80-107`, `tests/robustness/test-hook-system-robustness.sh:109-155`). If no active RLCR loop exists, RLCR-specific path checks after the broad prompt/todos guards are skipped (`hooks/loop-edit-validator.sh:91-97`).
- validation_or_tests: Covered by allowlist tests for edit todos/summaries (`tests/test-allowlist-validators.sh:188-228`), finalize-state edit blocking (`tests/test-finalize-phase.sh:369-379`), PR wrong-round `pr-resolve` edit blocking (`tests/test-pr-loop-hooks.sh:1499-1527`), and hook-system robustness tests for malformed JSON, missing fields, state blocking, and traversal behavior (`tests/robustness/test-hook-system-robustness.sh:37-155`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable specification for real monitor resilience. It launches the actual `_humanize_monitor_codex` and `_humanize_monitor_pr` paths from `scripts/humanize.sh`, then verifies graceful behavior under loop-directory deletion, shell differences, and interrupts (`tests/test-monitor-e2e-real.sh:1-13`, `tests/test-monitor-e2e-real.sh:148-153`, `tests/test-monitor-e2e-real.sh:776-795`, `tests/test-monitor-e2e-real.sh:917-925`).
- algorithmic_behavior: Builds isolated fake projects under `/tmp/test-monitor-e2e-real-$$`, creates RLCR/PR loop session directories, writes representative `state.md` and `goal-tracker.md` files, mirrors monitor cache layout with sanitized project paths, stubs terminal commands where needed, runs monitors in background or one-shot mode, then deletes loop directories or sends SIGINT and waits with bounded polling (`tests/test-monitor-e2e-real.sh:48-56`, `tests/test-monitor-e2e-real.sh:64-108`, `tests/test-monitor-e2e-real.sh:160-186`, `tests/test-monitor-e2e-real.sh:692-805`).
- inputs_outputs_state: Inputs are generated loop state files, fake cache log files, shell availability, and runtime signals. Outputs are PASS/FAIL counters and exit `0` only when no test failed (`tests/test-monitor-e2e-real.sh:25-37`, `tests/test-monitor-e2e-real.sh:992-1017`). It mutates only temporary test directories and background processes; repo files are read-only dependencies.
- gates_or_invariants: RLCR deletion must exit cleanly, print `Monitoring stopped:` and `directory no longer exists`, avoid `no matches found` or `bad pattern`, restore terminal state via cleanup, and report `EXIT_CODE:0` for graceful deletion (`tests/test-monitor-e2e-real.sh:191-231`). SIGINT paths must either exit or produce cleanup/clean-exit evidence without glob errors (`tests/test-monitor-e2e-real.sh:492-549`, `tests/test-monitor-e2e-real.sh:647-682`). PR monitor paths must exit cleanly or prove they ran before interruption, again with no glob errors (`tests/test-monitor-e2e-real.sh:807-824`, `tests/test-monitor-e2e-real.sh:938-989`).
- dependencies_and_callers: Sources `scripts/humanize.sh`, which in turn sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6-10`). The tested RLCR monitor uses `find` instead of globs for sessions/logs, checks `.humanize/rlcr` existence in multiple loops, restores scroll region with `printf "\033[r"`, and defines cleanup/graceful-stop traps (`scripts/humanize.sh:230-235`, `scripts/humanize.sh:280-339`, `scripts/humanize.sh:523-587`, `scripts/humanize.sh:621-626`, `scripts/humanize.sh:662-665`, `scripts/humanize.sh:755-758`). PR monitor uses shared latest-session/state helpers, one-shot mode, alternate-screen cleanup, and continuous polling (`scripts/humanize.sh:930-1017`, `scripts/humanize.sh:1174-1213`, `scripts/humanize.sh:1215-1292`, `scripts/humanize.sh:1303-1355`; `scripts/lib/monitor-common.sh:32-63`, `scripts/lib/monitor-common.sh:154-188`).
- edge_cases_or_failure_modes: `zsh` tests are skipped when unavailable (`tests/test-monitor-e2e-real.sh:240-242`, `tests/test-monitor-e2e-real.sh:558-560`). Bash SIGINT delivery is treated permissively: if SIGINT does not stop the process, SIGTERM/SIGKILL cleanup can still count as pass for monitor liveness/cleanup coverage (`tests/test-monitor-e2e-real.sh:505-519`, `tests/test-monitor-e2e-real.sh:951-965`). PR `--once` deletion coverage is also permissive because one-shot mode may return before the delayed deletion, and the assertion accepts any clean `EXIT_CODE:0` (`tests/test-monitor-e2e-real.sh:784-817`). Timing uses fixed sleeps and bounded loops, so very slow machines can produce false failures.
- validation_or_tests: This file is itself part of the aggregate suite (`tests/run-all-tests.sh:32-52`). It complements lower-level runtime tests for graceful stop and cleanup idempotence (`tests/test-monitor-runtime.sh:56-134`, `tests/test-monitor-runtime.sh:137-178`) and shared monitor helper tests referenced in `tests/test-pr-loop-system.sh`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-087 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Blocking prompt used by the RLCR stop hook when Round 0 attempts to exit before the Goal Tracker’s required sections are initialized (`prompt-template/block/goal-tracker-not-initialized.md:1-18`).
- algorithmic_behavior: The stop hook scans `goal-tracker.md` sections for placeholder text in Ultimate Goal, Acceptance Criteria, and Active Tasks, builds `MISSING_ITEMS`, renders this template with `GOAL_TRACKER_FILE` and `MISSING_ITEMS`, then returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:640-681`, `hooks/loop-codex-stop-hook.sh:683-691`). The rendered message tells Claude to read the tracker, replace placeholders, define 3-7 testable acceptance criteria, populate active tasks mapped to ACs, and retry exit.
- inputs_outputs_state: Inputs are `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`. Output is human-facing block text embedded as the stop-hook `reason`; state remains in Round 0 until Claude updates `goal-tracker.md` and retries. It is loaded with `load_and_render_safe`, so missing template content falls back to an inline minimal message (`hooks/loop-codex-stop-hook.sh:675-681`; `hooks/lib/template-loader.sh:170-203`).
- gates_or_invariants: Enforces the Round 0-only initialization window: “IMMUTABLE SECTION can only be set in Round 0” (`prompt-template/block/goal-tracker-not-initialized.md:16`). This pairs with edit validators that block later Claude edits to `goal-tracker.md` after Round 0 (`hooks/loop-edit-validator.sh:141-149`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`; rendering semantics come from `hooks/lib/template-loader.sh`, whose substitution is single-pass and leaves missing variables intact (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-131`).
- edge_cases_or_failure_modes: The upstream placeholder detector looks for `\[To be [a-z]`, so empty or badly structured content without that placeholder may not trigger this exact block. The template states “3-7” ACs, but this file does not enforce count or testability by itself; it instructs the agent under a broader stop-hook gate.
- validation_or_tests: Indirectly validated through stop-hook behavior and template reference checks. `tests/test-template-references.sh` ensures critical scripts use safe rendering and referenced common templates exist, while comprehensive template tests exercise the same loader path for real templates (`tests/test-template-references.sh:172-190`, `tests/test-templates-comprehensive.sh:531-540`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-117 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section that transfers post-Round-0 Goal Tracker mutation responsibility from Claude to Codex, matching the validator invariant that Claude cannot directly edit `goal-tracker.md` after Round 0 (`prompt-template/codex/goal-tracker-update-section.md:1-17`).
- algorithmic_behavior: During stop-hook review prompt construction, the hook renders this section with `GOAL_TRACKER_FILE` and injects it into Codex review prompts. It tells Codex to evaluate any “Goal Tracker Update Request” in Claude’s summary, approve only justified changes serving the Ultimate Goal, update the mutable tracker sections itself, and explain rejections (`hooks/loop-codex-stop-hook.sh:739-745`, `hooks/loop-codex-stop-hook.sh:760-769`).
- inputs_outputs_state: Input is `{{GOAL_TRACKER_FILE}}`; output is prompt text, not direct shell output. If Codex follows it, mutable tracker state may transition tasks among Active, Completed and Verified, Explicitly Deferred, Plan Evolution Log, and Open Issues. The immutable Ultimate Goal and Acceptance Criteria must remain unchanged (`prompt-template/codex/goal-tracker-update-section.md:5-12`).
- gates_or_invariants: Preserves the immutable section after Round 0 and requires justification for plan evolution or deferral. It makes goal-tracker updates review-owned rather than implementer-owned, which is reinforced by the edit validator’s post-Round-0 block (`hooks/loop-edit-validator.sh:145-149`) and `goal_tracker_blocked_message` guidance to include update requests in summaries (`hooks/lib/loop-common.sh:1092-1104`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:741-745`). Rendering depends on the template loader’s single-pass placeholder substitution and fallback handling (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: The transition is judgment-based: the template says Codex must evaluate whether a request is justified, but no deterministic parser enforces the approval standard. If Claude omits the named update-request section, this prompt adds no tracker work. If Codex ignores the instruction, the mutable Goal Tracker can become stale even though direct Claude edits remain blocked.
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this exact template with a sample tracker path and asserts that both “Goal Tracker Update Requests” and the rendered path appear (`tests/test-templates-comprehensive.sh:531-540`). Reference search confirms it is wired into `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:741-745`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item evidence sections present; each assigned item_id appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`