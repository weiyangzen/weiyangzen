# agent_08 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: Public behavior contract for the Humanize plugin’s two main workflows: RLCR iterative implementation/review and PR-loop remote bot review handling.
- algorithmic_behavior: Defines the high-level state machine: Claude implements and summarizes, Codex reviews the summary, the loop repeats until Codex returns completion, then a review phase runs `codex review --base <branch>` and either returns issues to Claude or ends successfully (`README.md:60-72`). It also defines PR-loop progression: detect current branch PR, fetch bot review comments, have Claude fix issues, push changes, trigger re-review, poll every 30 seconds, run local Codex validation, and continue until all bots approve or max iterations is reached (`README.md:155-163`).
- inputs_outputs_state: Inputs include a plan/draft file for RLCR (`README.md:76-83`), command options such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, and `--base-branch` (`README.md:101-115`), plus PR-loop bot flags `--claude`/`--codex` (`README.md:139-152`). Outputs/state are `.humanize/rlcr/<timestamp>/` monitoring artifacts (`README.md:84`), local commits/pushes depending on mode, PR comments, bot review polling results, and final approval/issue loop status.
- gates_or_invariants: RLCR requires a summary before Codex review and treats review-phase severity markers `[P0-9]` as the issue signal (`README.md:70-72`). Base branch auto-detection priority is documented as remote default, then `main`, then `master` (`README.md:113-114`). PR-loop requires GitHub CLI, Codex CLI, and an associated open PR on the current branch (`README.md:165-168`). At least one PR-loop bot flag is required (`README.md:140-144`).
- dependencies_and_callers: User-facing commands map to implementation scripts/commands under `commands/`, `scripts/`, and hooks: `/humanize:start-rlcr-loop`, `/humanize:cancel-rlcr-loop`, `/humanize:gen-plan`, `/humanize:start-pr-loop`, and `/humanize:cancel-pr-loop` (`README.md:87-95`). The documented base-branch behavior corresponds to `scripts/setup-rlcr-loop.sh:532-586`; the PR polling behavior corresponds to `hooks/pr-loop-stop-hook.sh:791-900` and `scripts/poll-pr-reviews.sh`.
- edge_cases_or_failure_modes: Missing dependencies (`codex`, `gh`) block intended workflows (`README.md:52-55`, `README.md:165-167`). No associated open PR prevents PR-loop startup (`README.md:168`). Base-branch detection can fail if no usable local `main`/`master` or remote-default branch exists; production code also rejects remote-default branches that are not local refs (`scripts/setup-rlcr-loop.sh:557-584`).
- validation_or_tests: README behavior is exercised by the broader shell suite listed in `tests/run-all-tests.sh:32-73`, including template, plan-file, PR-loop, state, robustness, and base-branch tests. Specific coverage includes `tests/robustness/test-base-branch-detection.sh` for documented base-branch fallback behavior and PR-loop script/hook tests for bot polling.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-038 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop polling script that normalizes GitHub issue comments, inline review comments, and PR review submissions into one JSON stream filtered by watched bot authors and timestamp.
- algorithmic_behavior: Parses `<pr_number> --after <timestamp> --bots <bot1,bot2>` (`scripts/poll-pr-reviews.sh:19-81`), validates required arguments and numeric PR number (`scripts/poll-pr-reviews.sh:83-102`), checks `gh` and `jq` prerequisites (`scripts/poll-pr-reviews.sh:104-116`), resolves the PR base repo with fork fallback (`scripts/poll-pr-reviews.sh:122-157`), maps logical bot names to GitHub authors (`codex` to `chatgpt-codex-connector[bot]`, others to `<bot>[bot]`) (`scripts/poll-pr-reviews.sh:163-186`), fetches three API surfaces with retry (`scripts/poll-pr-reviews.sh:206-234`, `scripts/poll-pr-reviews.sh:239-293`), combines arrays (`scripts/poll-pr-reviews.sh:295-296`), filters by `created_at >= --after` and author regex (`scripts/poll-pr-reviews.sh:298-304`), and emits a final JSON object (`scripts/poll-pr-reviews.sh:310-325`).
- inputs_outputs_state: Inputs are CLI args, current GitHub repo context, PR number, timestamp string, bot list, `gh` auth/session state, network/API responses, and `jq`. Output is JSON with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:316-325`). Internal state is temporary files under `mktemp -d`, removed by trap (`scripts/poll-pr-reviews.sh:192-197`).
- gates_or_invariants: Hard validation rejects missing `--after`, missing `--bots`, missing/invalid PR number, missing `gh`, missing `jq`, and unresolved repo owner/name (`scripts/poll-pr-reviews.sh:83-116`, `scripts/poll-pr-reviews.sh:154-157`). API fetch failures are intentionally soft: after three retries, the function emits `[]` and returns success so polling can continue (`scripts/poll-pr-reviews.sh:199-234`). PR reviews with empty body are preserved by substituting `[Review state: <STATE>]` (`scripts/poll-pr-reviews.sh:275-293`), preventing approval-only reviews from disappearing.
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` via `POLL_SCRIPT="$PLUGIN_ROOT/scripts/poll-pr-reviews.sh"` (`hooks/pr-loop-stop-hook.sh:791-804`). It is part of the README PR-loop contract to poll bot reviews every 30 seconds (`README.md:155-163`). It depends on GitHub CLI endpoints for issue comments, pull review comments, and PR reviews (`scripts/poll-pr-reviews.sh:239-278`).
- edge_cases_or_failure_modes: Fork PRs require resolving the base repo; the script first tries the current repo and then parent repo (`scripts/poll-pr-reviews.sh:122-149`). API outages produce warnings on stderr and valid empty JSON data rather than process failure (`scripts/poll-pr-reviews.sh:220-233`). Filtering uses lexicographic ISO timestamp comparison, so malformed or non-normalized timestamps can misorder results. Bot regex construction escapes square brackets but does not otherwise quote arbitrary regex metacharacters in bot names (`scripts/poll-pr-reviews.sh:174-186`), acceptable for known bot flags but fragile for arbitrary names.
- validation_or_tests: Direct argument/help coverage appears in `tests/test-pr-loop-scripts.sh:329-399` and robustness coverage in `tests/robustness/test-pr-loop-api-robustness.sh:619-839`. Fixture-backed tests verify valid JSON output and approval-only review inclusion (`tests/test-pr-loop-hooks.sh:1322-1355`). Additional unit-style tests check empty-body PR review placeholders and bot regex matching (`tests/test-pr-loop-hooks.sh:1160-1219`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-068 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template rendering algorithm implemented in `hooks/lib/template-loader.sh`, especially safe single-pass substitution and literal handling of shell-sensitive content.
- algorithmic_behavior: Loads `hooks/lib/template-loader.sh` (`tests/test-template-loader.sh:10-12`), then tests path discovery, template loading, rendering, safe fallback rendering, directory validation, metacharacter rendering, placeholder-injection prevention, empty values, Unicode, and variable-name edge cases. The target implementation defines `{{VARIABLE_NAME}}` syntax, single-pass substitution, missing-placeholder preservation, and safe fallback behavior (`hooks/lib/template-loader.sh:7-21`, `hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:170-203`).
- inputs_outputs_state: Inputs are template directories/files under `prompt-template`, literal template strings, and `VAR=value` substitution arguments. Outputs are rendered strings and test pass/fail counters (`tests/test-template-loader.sh:20-34`). State is local test counters only; the script exits `0` if no failures and `1` otherwise (`tests/test-template-loader.sh:641-659`).
- gates_or_invariants: `get_template_dir` must resolve `prompt-template` from `hooks/lib` (`tests/test-template-loader.sh:41-52`). Missing templates return empty for `load_template` but fallback content for `load_and_render_safe` (`tests/test-template-loader.sh:67-78`, `tests/test-template-loader.sh:196-222`). Unused vars are ignored and unreplaced placeholders remain intact (`tests/test-template-loader.sh:166-194`). Values containing `&`, backslashes, `$`, backticks, pipes, semicolons, globs, quotes, redirection syntax, JSON-like text, regexes, and multiline content must render literally (`tests/test-template-loader.sh:247-486`). Injected `{{VAR}}` sequences inside values must not be recursively expanded (`tests/test-template-loader.sh:488-543`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`; indirectly validates templates used by validators and stop hooks through `load_and_render_safe`. The full suite includes this test first (`tests/run-all-tests.sh:32-35`). Cross-reference validation separately confirms `block/summary-bash-write.md` and other common templates exist (`tests/test-template-references.sh:145-167`).
- edge_cases_or_failure_modes: The main safety edge case is prompt corruption or injection if a value like reviewer content contains template syntax; tests 34-36 pin this as non-recursive single-pass behavior (`tests/test-template-loader.sh:499-543`). Shell metacharacters in replacement values are explicitly regression-tested so `awk` substitution cannot interpret `&` as matched text or backslashes as escapes (`tests/test-template-loader.sh:247-486`). Missing/invalid template directories must fail validation (`tests/test-template-loader.sh:224-244`).
- validation_or_tests: This file is itself the validator for `template-loader.sh`. It has 41 numbered tests plus subtests 38.1 and 39.1, and produces a summarized pass/fail result (`tests/test-template-loader.sh:641-659`). Related coverage exists in `tests/test-templates-comprehensive.sh`, `tests/robustness/test-template-error-robustness.sh`, and `tests/robustness/test-template-stress-robustness.sh`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-098 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by Bash validation to enforce the workflow invariant that round summary files must be modified through Write/Edit tools, not shell redirection or text-processing commands.
- algorithmic_behavior: Renders a short block message telling the agent not to use Bash commands for summary files and substitutes `{{CORRECT_PATH}}` into the required Write/Edit target (`prompt-template/block/summary-bash-write.md:1-8`). The message is loaded by `summary_bash_blocked_message`, which passes `CORRECT_PATH` through `load_and_render_safe` (`hooks/lib/loop-common.sh:475-484`).
- inputs_outputs_state: Input is `CORRECT_PATH`, typically computed as the current active RLCR summary path (`hooks/loop-bash-validator.sh:375-377`). Output is a block reason sent to stderr by the Bash validator before it exits with block code `2` (`hooks/loop-bash-validator.sh:375-378`). The template itself has no mutable state.
- gates_or_invariants: Enforces that Bash commands such as `cat`, `echo`, `sed`, and `awk` must not modify summary files because they bypass validation hooks (`prompt-template/block/summary-bash-write.md:3-8`). This preserves round-number and location validation performed by Write/Edit validators.
- dependencies_and_callers: Called by `hooks/lib/loop-common.sh:475-484`, which is in turn invoked by `hooks/loop-bash-validator.sh` when a Bash command modifies `round-[0-9]+-summary.md` (`hooks/loop-bash-validator.sh:375-378`). Template existence is checked by `tests/test-template-references.sh:152-167`. Rendering semantics depend on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `summary_bash_blocked_message` has a fallback that still includes the correct path (`hooks/lib/loop-common.sh:479-483`). If Bash modification detection misses a shell pattern, this template is not reached; that detection is owned by `command_modifies_file` and Bash validator pattern tests, not by the template. If `CORRECT_PATH` itself contains template syntax, the single-pass renderer keeps injected placeholders literal per `tests/test-template-loader.sh:488-543`.
- validation_or_tests: Template presence is listed in `tests/test-template-references.sh:152-167`. The loader test validates real templates and safe rendering (`tests/test-template-loader.sh:147-164`, `tests/test-template-loader.sh:196-222`). Bash command summary modification detection is covered by `tests/test-bash-validator-patterns.sh` patterns for `round-N-summary.md`, and runtime blocking is implemented in `hooks/loop-bash-validator.sh:375-378`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-128 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness test for RLCR code-review base branch auto-detection, covering missing-origin and local branch fallback behavior.
- algorithmic_behavior: Defines a local `detect_base_branch` helper that simulates the setup script’s branch detection: read `git remote show origin` HEAD branch, return it if non-empty and not `(unknown)`, else check local `main`, else local `master`, else return failure (`tests/robustness/test-base-branch-detection.sh:31-64`). It then creates isolated test repos for no-origin/main, no-origin/master, no-origin/develop-only, and origin-with-main cases (`tests/robustness/test-base-branch-detection.sh:66-191`).
- inputs_outputs_state: Inputs are temporary Git repositories under the test helper’s `TEST_DIR`, `git` command behavior, and `run_with_timeout` from `scripts/portable-timeout.sh` (`tests/robustness/test-base-branch-detection.sh:16-24`, `tests/robustness/test-base-branch-detection.sh:35-44`). Outputs are pass/fail messages through `test-helpers.sh` and final summary via `print_test_summary` (`tests/robustness/test-base-branch-detection.sh:193-198`).
- gates_or_invariants: Detection priority is remote default, local `main`, local `master`, then failure (`tests/robustness/test-base-branch-detection.sh:41-63`). Missing `origin` must not terminate under `set -euo pipefail`; the pipeline is guarded with `|| true` (`tests/robustness/test-base-branch-detection.sh:41-45`). Develop-only repos should fail rather than choosing an arbitrary branch (`tests/robustness/test-base-branch-detection.sh:124-149`).
- dependencies_and_callers: Included in the aggregate suite at `tests/run-all-tests.sh:55-67`. It references production logic in `scripts/setup-rlcr-loop.sh`, whose real auto-detection also verifies that the remote default branch exists as a local branch before selecting it (`scripts/setup-rlcr-loop.sh:556-566`), then falls back to local `main`/`master` and errors with fetch advice if needed (`scripts/setup-rlcr-loop.sh:567-584`).
- edge_cases_or_failure_modes: Covers no origin, local main fallback, local master fallback, no main/master failure, and origin remote behavior. Important drift: the test helper returns a non-unknown remote default immediately (`tests/robustness/test-base-branch-detection.sh:41-48`), while production requires that remote default to exist locally (`scripts/setup-rlcr-loop.sh:557-566`). As a result, this file may not catch failures where `origin/HEAD` points to a branch that has not been fetched as a local branch, even though production has explicit handling for that case (`scripts/setup-rlcr-loop.sh:575-584`).
- validation_or_tests: This file is the direct validator and exits via `print_test_summary`. It is part of the robustness suite (`tests/run-all-tests.sh:66`). Production explicit `--base-branch` validation additionally rejects missing local refs and remote-only refs with guidance to fetch (`scripts/setup-rlcr-loop.sh:537-554`), which is not directly exercised by this extracted helper.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`