# agent_15 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor utility layer for the humanize CLI and PR/RLCR monitor surfaces. The directory currently contains one recursive descendant, `scripts/lib/monitor-common.sh`, which is sourced by `scripts/humanize.sh` and tests rather than executed directly.
- algorithmic_behavior: `monitor-common.sh` provides reusable shell functions for terminal status rendering, session discovery, state-file classification, YAML/frontmatter extraction, PR loop phase detection, string/timestamp formatting, and goal-tracker parsing. Key sections are color constants at `scripts/lib/monitor-common.sh:14`, file/session utilities at `scripts/lib/monitor-common.sh:32` and `scripts/lib/monitor-common.sh:40`, terminal scroll-region helpers at `scripts/lib/monitor-common.sh:71`, state detection at `scripts/lib/monitor-common.sh:154`, frontmatter parsing at `scripts/lib/monitor-common.sh:197`, PR phase detection at `scripts/lib/monitor-common.sh:267`, display mapping at `scripts/lib/monitor-common.sh:346`, general goal parsing at `scripts/lib/monitor-common.sh:390`, and PR issue parsing at `scripts/lib/monitor-common.sh:468`.
- inputs_outputs_state: Inputs are filesystem paths, session directories, state markdown files, PR round artifacts, timestamps, status strings, and goal-tracker markdown. Outputs are shell strings such as file sizes, latest session path, `state_file|status`, ANSI color codes, readable timestamps, truncated labels, PR phases, phase descriptions, and pipe-delimited tracker summaries. The main persistent side effect is in `get_pr_loop_phase`, which caches the latest PR check file size under `/tmp/humanize-phase-${session_name}-${highest_round}.size` at `scripts/lib/monitor-common.sh:299` and updates it at `scripts/lib/monitor-common.sh:306`.
- gates_or_invariants: Session directories must match timestamp names `YYYY-MM-DD_HH-MM-SS` before `monitor_find_latest_session` considers them at `scripts/lib/monitor-common.sh:55`. State-file precedence is active `state.md` first, then the first `*-state.md` stop file found, else unknown at `scripts/lib/monitor-common.sh:162` and `scripts/lib/monitor-common.sh:181`. PR phase detection prioritizes terminal states `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` before file-growth detection at `scripts/lib/monitor-common.sh:272`. File-growth detection treats a larger `round-*-pr-check.md` size or an mtime younger than 10 seconds as `codex_analyzing` at `scripts/lib/monitor-common.sh:315`. Goal tracker row counts subtract markdown table header/separator rows and clamp negative active task counts to zero at `scripts/lib/monitor-common.sh:422` and `scripts/lib/monitor-common.sh:435`.
- dependencies_and_callers: `scripts/humanize.sh` sources this library when present at `scripts/humanize.sh:8` and uses functions for session lookup, status parsing, monitor sizing, PR loop phase display, and PR tracker stats. `tests/test-pr-loop-system.sh` sources it repeatedly for behavior checks around phase detection, state-file parsing, latest-session selection, file size handling, and tracker parsing. The directory coordinates upward with `scripts/humanize.sh` as a CLI support layer and sideways with hook/test suites that verify PR loop monitor behavior.
- edge_cases_or_failure_modes: Missing directories return empty or unknown rather than failing hard (`monitor_find_latest_session` at `scripts/lib/monitor-common.sh:44`, `monitor_find_state_file` at `scripts/lib/monitor-common.sh:157`). File size falls back across Linux/macOS `stat` forms and emits `0` if both fail at `scripts/lib/monitor-common.sh:34`. `monitor_get_yaml_value` uses a simple grep/sed key extraction and can match regex-significant key strings because it does not escape the key at `scripts/lib/monitor-common.sh:208`. `get_pr_loop_phase` writes to `/tmp`, so read-only or unwritable temp conditions are suppressed with `|| true` but may reduce growth detection accuracy. `parse_goal_tracker` treats markdown heading lines beginning with `#` as comments in surrounding validation logic elsewhere, and its section parsing is regex-boundary based, so malformed headings can undercount sections.
- validation_or_tests: Direct callers/tests found via repository search include `tests/test-pr-loop-system.sh` cases for `get_pr_loop_phase`, `parse_goal_tracker`, `humanize_parse_pr_goal_tracker`, `monitor_find_state_file`, `monitor_find_latest_session`, and `monitor_get_file_size`. Related robustness coverage exists in `tests/robustness/test-goal-tracker-robustness.sh`, but that file targets `scripts/humanize.sh` parsing behavior rather than this library alone.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Executable test fixture generator for GitHub CLI behavior used by PR loop API/comment/review tests. It creates a temporary `gh` executable that returns deterministic fixture JSON instead of calling GitHub.
- algorithmic_behavior: The script requires two positional inputs, `mock_bin_dir` and `fixtures_dir`, then writes `$mock_bin_dir/gh` using a heredoc and makes it executable. The generated mock dispatches on the first `gh` subcommand: `auth status` returns logged-in success; `repo view` returns JSON for owner/name/parent requests; `pr view` returns base repository, PR number, or state; `api user` returns `testuser`; and `api` endpoints containing `/issues/*/comments`, `/pulls/*/comments`, or `/pulls/*/reviews` stream fixture files. The dispatch table is in `tests/setup-fixture-mock-gh.sh:33` through `tests/setup-fixture-mock-gh.sh:96`.
- inputs_outputs_state: Inputs are a destination bin directory and a fixture directory with `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`. Output is an executable mock at `$MOCK_BIN_DIR/gh`, plus stdout containing `$MOCK_BIN_DIR` at `tests/setup-fixture-mock-gh.sh:104`. State transition is from no mock executable to a PATH-prependable `gh` shim; no repository files are intentionally modified.
- gates_or_invariants: The setup script fails closed when either argument is missing at `tests/setup-fixture-mock-gh.sh:19`. It creates the mock bin directory before writing at `tests/setup-fixture-mock-gh.sh:24`. The generated mock exits `0` for known commands and exits `1` with `Mock gh: unhandled command` for unknown command shapes at `tests/setup-fixture-mock-gh.sh:98`. Unknown `gh api` endpoints return an empty JSON array rather than failing at `tests/setup-fixture-mock-gh.sh:92`.
- dependencies_and_callers: Used by PR loop hook tests around fixture-backed `fetch-pr-comments.sh` and `poll-pr-reviews.sh`; repository search shows calls in `tests/test-pr-loop-hooks.sh:1237`, then fixture-backed executions of `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` around `tests/test-pr-loop-hooks.sh:1251` and `tests/test-pr-loop-hooks.sh:1332`. It depends on Bash, filesystem write access to a temporary bin directory, and fixtures under `tests/fixtures`.
- edge_cases_or_failure_modes: Matching is substring-based on the full argument string, so flags or query text containing `owner`, `name`, `parent`, `baseRepository`, `number`, or `state` steer the returned JSON. Missing fixture files cause the generated mock’s `cat` to fail. The mock covers only the GitHub API shapes required by the tests; any production change to `gh` invocation paths can surface as an unhandled command.
- validation_or_tests: It is itself a test helper and is validated indirectly by fixture tests such as `T-FIXTURE-1` through `T-FIXTURE-4` in `tests/test-pr-loop-hooks.sh`, covering all comment types, timestamp filtering, approval-only review JSON, and poll filtering.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-075 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking prompt/template body used when the PR loop requires Claude bot acknowledgement but the bot does not add an `eyes` reaction to the trigger comment within the retry window.
- algorithmic_behavior: The template renders a human-facing block reason with retry count and total wait seconds placeholders at `prompt-template/block/claude-eyes-timeout.md:3`, then enumerates likely configuration/permission/state causes and required operator actions. It is loaded by `hooks/pr-loop-stop-hook.sh` after `check-bot-reactions.sh claude-eyes` returns empty or `null`.
- inputs_outputs_state: Inputs are template variables `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`. Rendering is handled by `load_and_render_safe` from `hooks/lib/template-loader.sh`, whose placeholder substitution is single-pass and preserves unresolved placeholders (`hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`). Output is a markdown reason string embedded in a JSON hook response. State transition in the caller is from continued PR loop processing to a block decision: `{"decision":"block", ...}` at `hooks/pr-loop-stop-hook.sh:777`.
- gates_or_invariants: The caller only evaluates this gate when Claude is configured and trigger comments are required at `hooks/pr-loop-stop-hook.sh:757`; it only checks when a confirmed trigger comment ID exists at `hooks/pr-loop-stop-hook.sh:761`. The reaction check uses `--retry 3 --delay 5` at `hooks/pr-loop-stop-hook.sh:766`, and missing/`null` reaction is blocking at `hooks/pr-loop-stop-hook.sh:768`. If the template is missing or renders empty, the hook falls back to an inline message through `load_and_render_safe` at `hooks/pr-loop-stop-hook.sh:770` and `hooks/lib/template-loader.sh:170`.
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh:774`. It depends on `scripts/check-bot-reactions.sh`, the PR number, configured bot list, confirmed trigger comment ID, `jq`, and template-loader functions. It coordinates with PR loop trigger detection and bot response polling by stopping progression before later review polling if the Claude bot has not acknowledged the trigger.
- edge_cases_or_failure_modes: If `PR_TRIGGER_COMMENT_ID` is empty, the caller warns but does not block at `hooks/pr-loop-stop-hook.sh:783`, which means this template is bypassed for an inconsistent “trigger exists but no ID” state. If Claude is configured but trigger is not required, the eyes verification is skipped at `hooks/pr-loop-stop-hook.sh:787`. Because the template is operator-facing, its variables must be kept aligned with the caller’s retry/delay constants; currently the caller passes `RETRY_COUNT=3` and `TOTAL_WAIT_SECONDS=15`.
- validation_or_tests: Template loader behavior is covered by `tests/test-template-loader.sh` and comprehensive template tests generally; PR stop-hook behavior around bot reactions is exercised in PR loop hook/stophook tests. No direct line-specific test for this exact template was found besides the production load reference.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-105 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Blocking validation template for write/edit attempts targeting an RLCR round summary file whose encoded round number does not match the active loop state.
- algorithmic_behavior: The template interpolates attempted action, attempted round, file type, current round, and correct path. It explicitly instructs the agent not to increment the round number manually at `prompt-template/block/wrong-round-number.md:7`. It is used by write and edit validators when a summary path such as `round-N-summary.md` has a mismatched `N`.
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`, rendered through `load_and_render_safe`. Output is a markdown block message written to stderr by validators before returning exit code `2`. State transition is a tool operation denial: attempted write/edit is blocked, and the user/agent is redirected to the active round’s summary path.
- gates_or_invariants: `hooks/loop-write-validator.sh` extracts `CLAUDE_ROUND` from the filename and blocks only for summary files when the extracted round differs from `CURRENT_ROUND` and the path is not allowlisted at `hooks/loop-write-validator.sh:228` through `hooks/loop-write-validator.sh:244`. `hooks/loop-edit-validator.sh` applies the same invariant for edits at `hooks/loop-edit-validator.sh:155` through `hooks/loop-edit-validator.sh:178`. Allowlisted files bypass the mismatch block, preserving intentional exceptions.
- dependencies_and_callers: Direct callers are `hooks/loop-write-validator.sh:238` and `hooks/loop-edit-validator.sh:172`. It depends on helper functions such as `extract_round_number`, `is_round_file_type`, `is_allowlisted_file`, active state parsing from `hooks/lib/loop-common.sh`, and the template-loader. Nearby read validation uses a sibling template, `block/wrong-round-file.md`, not this template, for read attempts (`hooks/loop-read-validator.sh:138`).
- edge_cases_or_failure_modes: If the filename does not yield a round number, the validators do not apply this block. The write validator fixes `FILE_TYPE=summary`, so this template currently models summary writes/edits rather than every round artifact type. If template loading fails, fallback messages in the validators still block with exit code `2`, but the wording differs from this canonical template.
- validation_or_tests: Rendering is directly tested in `tests/test-template-loader.sh:151` and `tests/test-templates-comprehensive.sh:498`, which check placeholder substitution for a wrong-round summary path. Hook-level path/round validation is covered by loop write/edit validator tests and robustness hook-input tests.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-135 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for plan-file path and content validation in `scripts/setup-rlcr-loop.sh`. It encodes accepted and rejected path classes and verifies that validation fails before later runtime dependencies such as Codex matter.
- algorithmic_behavior: The script creates an isolated temporary git repo, commits a README, defines `create_valid_plan`, then repeatedly invokes `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"` through `test_path_validation`. The helper classifies production validation failures by matching setup script error text and classifies later `requires codex` or `must be gitignored` errors as proof that path/content validation passed. Positive tests accept ordinary relative paths, root filenames, dash/underscore names, nested directories, dotted filenames, long filenames, and deep nested paths. Negative tests reject absolute paths, whitespace, shell metacharacters, symlink files, parent directory symlinks, symlink chains, empty files, comment-only files, files with fewer than five lines, nonexistent files, and directories.
- inputs_outputs_state: Inputs are generated plan paths/content and the production setup script. Outputs are test pass/fail records via `pass`, `fail`, and final `print_test_summary` from `tests/test-helpers.sh`. State setup includes a temporary git repo at `TEST_DIR`, `.humanize/rlcr` cleanup before each subtest at `tests/robustness/test-path-validation-robustness.sh:62`, generated plan files, and symlinks. The script exits with the aggregate test status at `tests/robustness/test-path-validation-robustness.sh:465`.
- gates_or_invariants: The production validation being specified appears in `scripts/setup-rlcr-loop.sh`: relative-only path gate at `scripts/setup-rlcr-loop.sh:273`, whitespace rejection at `scripts/setup-rlcr-loop.sh:279`, shell metacharacter rejection at `scripts/setup-rlcr-loop.sh:287`, file symlink rejection at `scripts/setup-rlcr-loop.sh:299`, parent directory existence at `scripts/setup-rlcr-loop.sh:305`, parent symlink traversal rejection at `scripts/setup-rlcr-loop.sh:312`, file existence/readability at `scripts/setup-rlcr-loop.sh:335`, project containment at `scripts/setup-rlcr-loop.sh:347`, submodule rejection at `scripts/setup-rlcr-loop.sh:361`, tracking/gitignore policy at `scripts/setup-rlcr-loop.sh:400`, minimum five-line gate at `scripts/setup-rlcr-loop.sh:431`, and minimum three non-comment content lines at `scripts/setup-rlcr-loop.sh:441`.
- dependencies_and_callers: Depends on Bash, Git, `tests/test-helpers.sh`, filesystem support for symlinks, and `scripts/setup-rlcr-loop.sh`. It coordinates with broader path validation coverage in `tests/test-plan-file-validation.sh` and `tests/robustness/test-plan-file-robustness.sh`, but this file is the concentrated robustness matrix for path characters, traversal, symlinks, and content shape.
- edge_cases_or_failure_modes: `test_path_validation` relies on matching known production error strings at `tests/robustness/test-path-validation-robustness.sh:72`, so wording changes in `setup-rlcr-loop.sh` can turn expected validation failures into generic failures. Positive tests may still end in `requires codex` or `must be gitignored`, which the helper intentionally treats as validation success at `tests/robustness/test-path-validation-robustness.sh:76` and `tests/robustness/test-path-validation-robustness.sh:81`. Long filename testing is filesystem-dependent and skips as pass if creation fails at `tests/robustness/test-path-validation-robustness.sh:371`. Unicode/CJK/emoji paths are explicitly documented as allowed but are not tested in this file at `tests/robustness/test-path-validation-robustness.sh:393`.
- validation_or_tests: This file is the validation harness. It enumerates 31 numbered test scenarios and delegates final reporting to `print_test_summary`. I did not run it in this read-only research pass because it creates temporary repos/files and the user requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 unique Item Evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`