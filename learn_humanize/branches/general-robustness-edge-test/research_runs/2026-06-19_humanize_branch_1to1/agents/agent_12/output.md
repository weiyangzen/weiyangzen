# agent_12 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex review prompt template directory for the RLCR loop. It defines the prompt surfaces used when the Claude/Codex loop transitions from implementation output to review, goal alignment, and review-audit phases.
- algorithmic_behavior: Contains four runtime-rendered templates:
  - `prompt-template/codex/goal-tracker-update-section.md`: tells Codex to evaluate Claude’s requested `goal-tracker.md` edits, apply approved changes, update plan evolution/open issues/deferred sections, and never alter the immutable goal/AC section (`lines 1-17`).
  - `prompt-template/codex/regular-review.md`: standard review prompt requiring plan read, implementation review, goal alignment check, goal-tracker update handling, and `COMPLETE` only when all planned tasks and ACs are fully complete with no deferrals (`lines 1-57`).
  - `prompt-template/codex/full-alignment-review.md`: stricter every-5-round checkpoint requiring original plan read, per-AC audit, forgotten/deferred item audit, historical last-5-round stagnation review, and final `STOP` or `COMPLETE` control token (`lines 1-93`).
  - `prompt-template/codex/code-review-phase.md`: audit-only prompt documenting `codex review --base {{BASE_BRANCH}}`, severity marker scanning, generated cache artifacts, and transition rules (`lines 1-36`).
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `REVIEW_RESULT_FILE`, `LOOP_TIMESTAMP`, previous-round values, base branch, and timestamp. Outputs are rendered prompt/audit files such as `round-N-review-prompt.md` and `round-N-prompt.md` in the loop directory (`hooks/loop-codex-stop-hook.sh:735-812`, `914-920`). State transition effects are indirect but central: Codex’s final review output drives whether the loop continues, stops as complete, or stops for stagnation.
- gates_or_invariants: The templates encode hard review gates: original plan must be read before review (`regular-review.md:5-11`, `full-alignment-review.md:7-10`); deferred and pending work are incomplete (`regular-review.md:24-31`, `53-56`; `full-alignment-review.md:88-93`); immutable goal/AC section cannot be edited by goal-tracker update handling (`goal-tracker-update-section.md:10`); full alignment requires stagnation detection and `STOP` on stalled progress (`full-alignment-review.md:59-85`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh`, whose contract is `{{VAR}}` placeholders, single-pass substitution, missing variables preserved, and safe fallback behavior (`hooks/lib/template-loader.sh:7-20`, `50-57`, `167-180`). Runtime call sites are `hooks/loop-codex-stop-hook.sh:744-745` for goal-tracker section, `784-795` for full alignment, `800-812` for regular review, and `914-920` for code review audit. Template existence/rendering is covered by `tests/test-templates-comprehensive.sh` and `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: Missing or empty templates degrade through `load_and_render_safe` fallback rather than crashing (`hooks/lib/template-loader.sh:167-180`). Unresolved placeholders remain literal by loader design, which can preserve malformed prompt variables if callers omit them. Full-alignment depends on exact round modulo logic: `CURRENT_ROUND % 5 == 4` triggers full alignment (`hooks/loop-codex-stop-hook.sh:747-751`). `code-review-phase.md` is audit documentation because `codex review` does not accept prompt input (`code-review-phase.md:3-4`), so misunderstanding it as a prompt consumed by Codex would be a misuse.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and checks referenced templates exist. `tests/test-templates-comprehensive.sh` explicitly renders `codex/goal-tracker-update-section.md` (`search result lines around `531-539`) and then loads/renders all markdown templates via `find "$TEMPLATE_DIR" -name "*.md"` (`search result around `566-602`). Loader behavior is tested by `tests/test-template-loader.sh`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime IO validation and initial plan-file materialization script for `/humanize:gen-plan`. It is the command’s first workflow gate before relevance checking and draft analysis.
- algorithmic_behavior: Parses `--input` and `--output` arguments (`lines 29-56`), enforces required args (`58-67`), normalizes paths with `realpath -m` fallback (`69-72`), validates input existence and non-empty content (`79-93`), validates output directory existence (`95-101`), rejects directory/existing output paths (`103-116`), checks output directory write permission (`118-124`), resolves `prompt-template/plan/gen-plan-template.md` via `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`133-140`), then creates the output by copying the plan template and appending the original draft between explicit start/end markers (`148-157`).
- inputs_outputs_state: Inputs are CLI args `--input <draft.md>` and `--output <plan.md>`, plus optional `CLAUDE_PLUGIN_ROOT`. Outputs are stdout validation status messages and a newly created output plan file on success. State transition is from “no plan output” to “template plus original draft captured at output path”; the script exits `0` only after writing the combined file (`159-161`).
- gates_or_invariants: Exit-code contract is declared at `lines 4-12` and mirrored by the command docs in `commands/gen-plan.md:38-46`: `1` missing input, `2` empty input, `3` missing output directory, `4` output exists/is directory, `5` no write permission, `6` invalid arguments/help, `7` missing plan template. It deliberately refuses to overwrite an existing plan (`111-116`) and refuses to create missing directories (`95-100`), requiring the user/workflow to choose/create valid output location.
- dependencies_and_callers: Called directly by `commands/gen-plan.md:32-36`, and that command allowlists only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh:*)` for this phase (`commands/gen-plan.md:1-12`). Depends on shell builtins plus `realpath`, `dirname`, `wc`, `tr`, `cp`, and `cat`; `realpath` failure is tolerated by falling back to the original path string (`69-71`). Depends on `prompt-template/plan/gen-plan-template.md`.
- edge_cases_or_failure_modes: `set -e` means unexpected command failures after validation can abort the script, especially `cp`, append redirects, or `cat`. `realpath -m` is GNU-specific but guarded with fallback. `--help` exits as invalid-args code `6` by design (`48-50`, `16-24`). There is a time-of-check/time-of-use race between validating nonexistence of `OUTPUT_FILE` and `cp`. Permission check uses `-w`, which can be misleading on ACLs, network filesystems, or privileged users. If `CLAUDE_PLUGIN_ROOT` is wrong, the script exits `7` before writing.
- validation_or_tests: `tests/test-gen-plan.sh:426-542` exercises this script’s command surface: executable existence, missing option values, flag-as-value, unknown flag, missing input, empty input, missing output dir, existing output, output directory, valid paths, and help exit code. The gen-plan command itself documents the same phase and exit handling in `commands/gen-plan.md:19-47`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for zsh monitor safety. It guards against zsh `nomatch` failures in monitor/session/log discovery paths by requiring `find`-based iteration to work in empty, dotfile-only, and no-match directories.
- algorithmic_behavior: Runs under zsh (`#!/usr/bin/env zsh`, `lines 1-9`) with strict mode (`13`). Creates isolated `/tmp/test-zsh-humanize-$$` workspace and `XDG_CACHE_HOME` (`48-54`) and cleans it on exit (`56-60`). Test flow:
  - source `scripts/humanize.sh` and assert no “no matches found” error (`65-77`);
  - simulate latest session discovery for empty loop dir (`80-124`);
  - repeat with dotfiles only (`127-157`);
  - simulate state-file lookup with no `*-state.md` files (`160-205`);
  - scan empty cache for `round-*-codex-run.log` with `find` (`207-235`);
  - verify timestamp session iteration picks latest lexicographic timestamp (`240-273`);
  - assert zsh really is the shell (`276-286`);
  - demonstrate old glob pattern risk and new `find` safety (`288-321`);
  - summarize pass/fail and exit nonzero on failures (`323-341`).
- inputs_outputs_state: Inputs are the project root’s `scripts/humanize.sh`, temporary `.humanize/rlcr` fixture directories, and temporary cache files. Outputs are PASS/FAIL console lines and process exit `0` or `1`. State is isolated temp filesystem state, reset by `trap cleanup EXIT`.
- gates_or_invariants: The invariant is no runtime output containing zsh’s “no matches found” under no-match conditions. It also asserts timestamp directory names must match `YYYY-MM-DD_HH-MM-SS` before being considered sessions (`109`, `145`, `259`) and that latest session selection is lexicographic by timestamp string (`110-112`, `261-263`). The test assumes monitor discovery must use `find ... 2>/dev/null` instead of unguarded globs.
- dependencies_and_callers: Sources production `scripts/humanize.sh` (`72`, `92`), which in turn sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6-10`). The production helpers under test are `monitor_find_latest_session` (`scripts/lib/monitor-common.sh:40-63`) and `monitor_find_state_file` (`154-188`), wrapped by monitor functions in `scripts/humanize.sh:237-347`. Requires `zsh`, `find`, `mkdir`, `touch`, `rm`, and `/tmp` write access.
- edge_cases_or_failure_modes: Because it writes to `/tmp`, it can fail in a read-only or restricted sandbox even if production logic is correct. Some test snippets duplicate production logic rather than calling the exact helper, so they are strong behavioral regression specs for the find pattern but not pure unit tests for every production line. `setopt +o nomatch` in Test 8 intentionally relaxes zsh behavior to demonstrate old-code risk without failing the suite (`300-321`). It does not cover malformed numeric round extraction; that is elsewhere in monitor tests.
- validation_or_tests: The file itself is the validation. Its production basis is visible in `scripts/humanize.sh:280-339`, where `_find_latest_codex_log` uses `find` for session/cache discovery and performs a consistency guard that run-log rounds must be strictly before review-log rounds (`330-337`). Shared monitor helpers also use `find` rather than globs (`scripts/lib/monitor-common.sh:49-60`, `168-181`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Block-message template for path-boundary enforcement when Claude attempts to read or write a valid loop filename from the wrong active loop directory.
- algorithmic_behavior: Renders a short corrective message with `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` (`lines 1-6`). The message tells the agent which path is correct and includes a command-shaped hint for reading the requested file.
- inputs_outputs_state: Inputs are validator-provided variables. Output is stderr text emitted by read/write validators before they exit. No persistent state is changed by the template; the validator blocks the attempted operation by exiting `2`.
- gates_or_invariants: Enforces exact path equality: `FILE_PATH` must equal `ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`. In read validation, mismatch renders this template with `ACTION=read` and exits `2` (`hooks/loop-read-validator.sh:157-167`). In write validation, mismatch renders with `ACTION=write to` and exits `2` (`hooks/loop-write-validator.sh:252-262`). The invariant is that round files must live in the active loop directory, not an older/newer or arbitrary directory.
- dependencies_and_callers: Loaded by `load_and_render_safe` from `hooks/lib/template-loader.sh`. Direct callers are `hooks/loop-read-validator.sh:163-166` and `hooks/loop-write-validator.sh:258-261`. Related block templates handle wrong round and wrong file location; this one is specifically directory mismatch after filename/round checks.
- edge_cases_or_failure_modes: The final hint says `cat {{FILE_PATH}}` even though the “correct path” is `{{CORRECT_PATH}}` (`line 6`), which may be intentionally showing how to inspect what was attempted, but it can confuse recovery because the rejected path is not the allowed path. If the template is missing, both validators have a simpler fallback (`loop-read-validator.sh:160-162`, `loop-write-validator.sh:255-257`). Values are rendered single-pass, so unusual characters in paths are preserved rather than recursively interpreted by the template engine.
- validation_or_tests: Template reference validation finds the validators’ `wrong-directory-path.md` references. Template loader tests cover `load_and_render_safe` fallback and special character substitution. The path gate itself is exercised indirectly by read/write validator tests in the suite; direct call sites show exit code `2` on mismatch.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-132 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `humanize_parse_goal_tracker`, the production parser that converts `goal-tracker.md` markdown into monitor summary counts.
- algorithmic_behavior: Sources helpers and production `scripts/humanize.sh` (`lines 18-22`), creates an isolated test dir (`24`), defines `parse_result` to split the parser’s pipe-delimited output fields (`39-51`), then runs 18 cases over generated goal-tracker fixtures. It verifies AC counting in list and table formats (`60-116`), active task counting excluding completed/deferred statuses (`118-153`), completed task and unique completed AC counts (`155-202`), ultimate goal extraction (`204-227`), defaults for nonexistent/empty/malformed files (`237-256`, `304-326`), large AC counts (`258-279`), special characters (`281-302`), truncated/binary-touched files (`328-371`), open issues and deferred counts (`373-439`), headers-only zeros (`441-475`), mixed bold/sub-numbering AC formats (`477-499`), and long goal truncation to 60 chars (`501-524`).
- inputs_outputs_state: Inputs are temporary markdown files under `$TEST_DIR` and the production function. Expected parser output format is `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`35-36`). Outputs are PASS/FAIL results via shared test helpers and final `print_test_summary` exit status (`530-531`). State is only temporary fixture files.
- gates_or_invariants: Production parser invariants come from `scripts/humanize.sh:30-111`: missing tracker returns `0|0|0|0|0|0|No goal tracker` (`34-36`); ACs are counted only inside `### Acceptance Criteria` through `---` and match `AC-<digits>` or `AC<digits>` in list/table form (`48-54`); active task count equals Active Tasks table data rows minus completed/deferred status rows and never below zero (`56-84`); completed/deferred/open issue counts are markdown table data rows excluding header/separator (`39-46`, `86-102`); goal summary is the first nonempty content line under `### Ultimate Goal`, truncated with `cut -c1-60` (`104-109`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/humanize.sh`, and parser dependencies `sed`, `grep`, `sort`, `wc`, `tr`, `cut`, `head`. The parser is used by monitor status rendering through `_parse_goal_tracker() { humanize_parse_goal_tracker "$@"; }` in `scripts/humanize.sh:367-369`, with status bar consumers later splitting pipe-delimited values. Related robustness coverage appears in `tests/robustness/test-hook-input-robustness.sh`, which also calls `humanize_parse_goal_tracker`.
- edge_cases_or_failure_modes: Binary content is only required to return at least one parseable AC, not exact preservation after NUL bytes (`349-371`). Mixed subcriteria like `AC-3.1` are intentionally not counted by the current regex, while bold `**AC-2**` is accepted (`477-495`). Malformed files without proper `###` headers should produce zero ACs (`304-326`). Section parsing relies on heading text and `---` delimiters, so renamed headings or missing delimiters can change counts. Pipe-delimited return format can be fragile if goal summaries contain `|`, though the tests do not cover that.
- validation_or_tests: This file is the validation artifact. It directly tests the production implementation in `scripts/humanize.sh:30-111`, not a copied local parser. The summary gate at the end exits with the aggregate helper status (`530-531`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `GENERAL_ROBUSTNESS_EDGE_TEST-HZ-012`, `GENERAL_ROBUSTNESS_EDGE_TEST-HZ-042`, `GENERAL_ROBUSTNESS_EDGE_TEST-HZ-072`, `GENERAL_ROBUSTNESS_EDGE_TEST-HZ-102`, `GENERAL_ROBUSTNESS_EDGE_TEST-HZ-132`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`