# agent_19 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for canceling an active RLCR loop through the dedicated cancellation script, not by directly editing loop state. The command is hidden from the slash-command tool surface and restricts allowed tools to `scripts/cancel-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh --force`, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-5`).
- algorithmic_behavior: The command runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` first, then branches entirely on the first output line: `NO_LOOP` / `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:11-22`). For finalize-phase loops, it requires an explicit user confirmation before invoking `--force` (`commands/cancel-rlcr-loop.md:23-34`).
- inputs_outputs_state: Input is the current project’s `.humanize/rlcr` active-loop state as interpreted by the script plus, conditionally, a user confirmation choice. Output is a user-facing cancellation/no-loop/finalize-continuation report. State transition is delegated to `scripts/cancel-rlcr-loop.sh`, which finds the newest loop dir, detects `state.md` or `finalize-state.md`, touches `.cancel-requested`, and renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69-99`, `scripts/cancel-rlcr-loop.sh:134-151`).
- gates_or_invariants: The key invariant is stated directly: the script owns all cancellation logic; a loop is active only if `state.md` or `finalize-state.md` exists in the newest loop directory (`commands/cancel-rlcr-loop.md:36-38`). Finalize cancellation is guarded by a human confirmation unless forced after confirmation (`commands/cancel-rlcr-loop.md:23-34`).
- dependencies_and_callers: Depends on `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh`. Related surfaces refer users to this command from `commands/start-rlcr-loop.md`, `commands/cancel-pr-loop.md`, and several hook/template failure messages. The script itself uses `CLAUDE_PROJECT_DIR` or `pwd` to locate `.humanize/rlcr` (`scripts/cancel-rlcr-loop.sh:69-70`).
- edge_cases_or_failure_modes: Handles no loop directory (`NO_LOOP`), newest loop directory without active state (`NO_ACTIVE_LOOP`), normal loop cancellation, finalize-phase cancellation with confirmation, and unknown script options. A malformed state lacking round fields still cancels but reports `?` for round/max (`scripts/cancel-rlcr-loop.sh:105-111`).
- validation_or_tests: Cancellation signal behavior is covered elsewhere by `tests/test-cancel-signal-file.sh` per repository references. This assigned command file itself is a prompt/workflow contract rather than an executable test.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `command_modifies_file` in `hooks/lib/loop-common.sh`, the shared Bash-command classifier used by loop validators to block file modifications through Bash (`tests/test-bash-validator-patterns.sh:1-6`, `hooks/lib/loop-common.sh:1062-1090`).
- algorithmic_behavior: The test lowercases each command via `to_lower`, then asserts whether `command_modifies_file "$command_lower" "$pattern"` returns success or failure (`tests/test-bash-validator-patterns.sh:36-62`). It covers mutation patterns for redirection, `tee`, in-place `sed` / `awk` / `perl`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and file-descriptor `exec >` (`tests/test-bash-validator-patterns.sh:69-134`).
- inputs_outputs_state: Inputs are literal shell command strings plus a target regex, defaulting to `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:37-43`, `tests/test-bash-validator-patterns.sh:50-57`). Outputs are PASS/FAIL counters and process exit status 0 when all assertions pass, 1 otherwise (`tests/test-bash-validator-patterns.sh:20-34`, `tests/test-bash-validator-patterns.sh:197-214`). It mutates only in-memory test counters.
- gates_or_invariants: Positive invariant: destination-oriented writes to protected files must be detected. Negative invariant: read-only commands like `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` must not be false positives (`tests/test-bash-validator-patterns.sh:136-153`). Case-insensitivity is enforced by lowercasing and by the uppercase `ECHO X > GOAL-TRACKER.MD` case (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:80`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` from the repository root (`tests/test-bash-validator-patterns.sh:11-13`). The implementation’s regex list lives in `command_modifies_file` (`hooks/lib/loop-common.sh:1065-1089`) and is called by `hooks/loop-bash-validator.sh` for state files, plan files, goal tracker, prompt/summary/todo files, and PR-loop state files.
- edge_cases_or_failure_modes: Documents a known limitation: `cp file1.md file2.md goal-tracker.md` is not detected because the regex expects `cp src dest` shape (`tests/test-bash-validator-patterns.sh:165-168`). Variable destinations like `echo x > $FILE` are intentionally not matched because the target filename is not literal (`tests/test-bash-validator-patterns.sh:170-172`). It also tests tight redirection syntax `>goal-tracker.md` (`tests/test-bash-validator-patterns.sh:161-164`).
- validation_or_tests: The file is itself the validation harness. It additionally checks alternate protected patterns for `state\.md` and `round-[0-9]+-summary\.md` (`tests/test-bash-validator-patterns.sh:175-194`).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the `.humanize/` git-add protection gate. It explains why broad or forced staging commands are blocked when they would add local loop state (`prompt-template/block/git-add-humanize.md:1-7`).
- algorithmic_behavior: The template defines the remediation path after the validator blocks a staging command: use specific file paths, a directory path, or patch mode instead of broad staging (`prompt-template/block/git-add-humanize.md:8-15`). It enumerates blocked command shapes: direct `.humanize`, `git add -A`, `git add --all`, `git add .`, and `git add -f .` (`prompt-template/block/git-add-humanize.md:16-25`).
- inputs_outputs_state: Input is implicit validator context: `.humanize/` exists and the attempted `git add` would include it. Output is rendered Markdown guidance. No repository state is changed by the template; it is loaded by the message helper in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:1030-1059`).
- gates_or_invariants: `.humanize/` contains local loop state and must not be committed (`prompt-template/block/git-add-humanize.md:3-6`). If adding `.humanize*` to `.gitignore`, the allowed sequence stages only `.gitignore` and commits with a message that must not contain the literal `.humanize` string, avoiding recursive triggering of the protection (`prompt-template/block/git-add-humanize.md:26-34`).
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:1057-1059`). The actual detection/blocking logic is in the loop Bash/write validation layer, while this file is the explanatory contract shown when the gate fires.
- edge_cases_or_failure_modes: The template calls out `git add -f .` because force can bypass `.gitignore` (`prompt-template/block/git-add-humanize.md:24`). It also treats the commit message itself as part of the gate surface by warning not to include `.humanize` literally (`prompt-template/block/git-add-humanize.md:34`).
- validation_or_tests: Covered indirectly by template-loader tests and by any hook tests that verify git-add block messages. This assigned file is a prompt/template surface rather than executable logic.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-109 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude prompt-footer template that defines the sanctioned mechanism for requesting Goal Tracker changes instead of editing the tracker directly in later RLCR rounds (`prompt-template/claude/goal-tracker-update-request.md:2-14`).
- algorithmic_behavior: The template instructs Claude to include a `## Goal Tracker Update Request` section only if updates are needed, with `Requested Changes` and `Justification` subsections (`prompt-template/claude/goal-tracker-update-request.md:2-14`). It supports deferred tasks, open issues, task completion evidence, and plan evolution as structured request examples (`prompt-template/claude/goal-tracker-update-request.md:6-10`).
- inputs_outputs_state: Input is Claude’s discovered need to update planning state during a loop. Output is a Markdown request embedded in the round summary. State is not changed directly; Codex reviews the request and decides whether to update the Goal Tracker (`prompt-template/claude/goal-tracker-update-request.md:16`).
- gates_or_invariants: The invariant is separation of authority: Claude may request Goal Tracker updates, but Codex performs justified updates. This aligns with the stop hook appending the template to the next-round prompt (`hooks/loop-codex-stop-hook.sh:1517-1522`) after building the prompt and footer (`hooks/loop-codex-stop-hook.sh:1502-1506`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` using `load_template "$TEMPLATE_DIR" "claude/goal-tracker-update-request.md"`; if absent, the hook falls back to a one-line instruction (`hooks/loop-codex-stop-hook.sh:1517-1521`).
- edge_cases_or_failure_modes: Optional by design: “If Goal Tracker needs updates” avoids forcing noisy requests when no state change is needed (`prompt-template/claude/goal-tracker-update-request.md:2`). A weak or unjustified request may be ignored because Codex review is the acceptance gate (`prompt-template/claude/goal-tracker-update-request.md:16`).
- validation_or_tests: Indirectly validated by hook/template loading behavior. This file is a prompt contract and not a standalone executable test.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-139 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, focused on argument parsing, plan validation, git repository preconditions, YAML safety, mutual exclusion, symlink protection, dependency gates, and timeout/path portability (`tests/robustness/test-setup-scripts-robustness.sh:1-10`).
- algorithmic_behavior: The script creates isolated temp repositories, minimal plans, and wrapper functions that run setup scripts with `CLAUDE_PROJECT_DIR` set to the temp repo (`tests/robustness/test-setup-scripts-robustness.sh:20-87`). It then executes 35 labeled tests, each checking exit status plus expected diagnostic text, and finishes through `print_test_summary` (`tests/robustness/test-setup-scripts-robustness.sh:96-872`).
- inputs_outputs_state: Inputs are temporary git repos, generated `plan.md` files, mocked `codex`, `gh`, `timeout`, and `gtimeout` commands, plus setup-script CLI args. Outputs are PASS/FAIL/SKIP-style counters from `tests/test-helpers.sh` and final process status (`tests/test-helpers.sh:30-78`, `tests/robustness/test-setup-scripts-robustness.sh:872-873`). State transitions are confined to temp dirs created by `setup_test_dir` and cleaned by trap (`tests/test-helpers.sh:84-89`).
- gates_or_invariants: RLCR setup must reject missing/duplicate plan args, invalid `--max`, invalid `--codex-timeout`, missing `--codex-model` arg, unknown options, comment-only plans, short plans, paths with spaces, shell metacharacters, absolute paths, YAML-unsafe branches/models, non-git repos, repos without commits, and tracked plans without `--track-plan-file` (`tests/robustness/test-setup-scripts-robustness.sh:96-463`). These mirror production checks in `setup-rlcr-loop.sh` for option parsing, mutual exclusion, git validation, path validation, plan tracking, content validation, model/effort safety, and branch safety (`scripts/setup-rlcr-loop.sh:120-195`, `scripts/setup-rlcr-loop.sh:211-330`, `scripts/setup-rlcr-loop.sh:390-530`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `scripts/portable-timeout.sh` (`tests/robustness/test-setup-scripts-robustness.sh:15-19`). Calls `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh` through `run_rlcr_setup` and `run_pr_setup` (`tests/robustness/test-setup-scripts-robustness.sh:65-87`). Production setup scripts source `hooks/lib/loop-common.sh` for active-loop detection and shared helpers (`scripts/setup-rlcr-loop.sh:203-214`, `scripts/setup-pr-loop.sh:208-223`).
- edge_cases_or_failure_modes: Tests Git versions that may reject colon branches by treating branch creation variance as pass when the branch cannot exist (`tests/robustness/test-setup-scripts-robustness.sh:364-379`). Symlink tests skip/pass when symlink creation is unavailable (`tests/robustness/test-setup-scripts-robustness.sh:620-630`, `tests/robustness/test-setup-scripts-robustness.sh:648-658`). Timeout `0` and very large timeout are accepted under current non-negative numeric behavior despite the error wording saying positive integer (`tests/robustness/test-setup-scripts-robustness.sh:749-799`). Timeout handling is simulated by mocked `timeout` / `gtimeout` returning 124 for `git rev-parse` (`tests/robustness/test-setup-scripts-robustness.sh:801-856`). Portable path handling is checked by ensuring the mock does not hardcode `/usr/bin/git` (`tests/robustness/test-setup-scripts-robustness.sh:858-866`).
- validation_or_tests: The file is itself a validation suite. It also validates PR-loop setup basics: help text, required bot flag, unknown options, numeric `--max`, non-git rejection, valid args proceeding to `gh` auth, and PR `--codex-timeout` rejection (`tests/robustness/test-setup-scripts-robustness.sh:473-524`, `tests/robustness/test-setup-scripts-robustness.sh:714-779`). Mutual exclusion is validated by fake `.humanize/rlcr/.../state.md` and `.humanize/pr-loop/.../state.md` active-loop directories (`tests/robustness/test-setup-scripts-robustness.sh:535-595`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item-evidence headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`