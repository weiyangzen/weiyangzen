# agent_24 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude Code hook manifest for the Humanize RLCR/PR loop safety boundary. It wires lifecycle events to concrete validator/stop-hook scripts: `UserPromptSubmit` to `hooks/loop-plan-file-validator.sh`, `PreToolUse` for `Write`, `Edit`, `Read`, and `Bash`, and `Stop` to both RLCR and PR stop hooks. See [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/hooks.json:4) and [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/hooks.json:52).
- algorithmic_behavior: The manifest itself is declarative, but it defines the state-machine interception points. Prompt submission runs plan/branch/tracking validation before new user input is accepted. Tool-use hooks preflight file reads/writes/edits and shell commands against active `.humanize/rlcr` and `.humanize/pr-loop` state. Stop hooks intercept attempted exits and decide whether to allow completion, block with the next prompt, run Codex review, or continue the PR loop.
- inputs_outputs_state: Inputs are Claude hook events with tool names/tool inputs and environment `CLAUDE_PLUGIN_ROOT`; outputs are determined by the invoked validators, commonly exit `0` allow, exit `2` tool block, or JSON block decisions for stop/user-prompt hooks. State is externalized under `.humanize/rlcr` and `.humanize/pr-loop`; the manifest does not store state.
- gates_or_invariants: Requires `CLAUDE_PLUGIN_ROOT` to resolve all commands. PreToolUse matching is exact by tool type: `Write` -> write validator, `Edit` -> edit validator, `Read` -> read validator, `Bash` -> bash validator. Stop hooks have `timeout: 7200`, bounding long review/PR-loop transitions. The downstream strict state parser rejects malformed RLCR state, including missing `current_round`, `max_iterations`, `review_started`, or `base_branch`; see [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:251).
- dependencies_and_callers: Consumed by Claude plugin hook loading. Depends on scripts named in the manifest plus shared `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `jq`, `git`, and Codex for review behavior. Tests reference the same hook scripts in `tests/test-template-references.sh`, `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-pr-loop-hooks.sh`, and robustness tests.
- edge_cases_or_failure_modes: Misconfigured `CLAUDE_PLUGIN_ROOT` prevents hook command resolution. If both Stop hooks run for every stop event, RLCR and PR loop logic must internally no-op when inactive. Because this file only registers hooks, protection depends on each downstream script being executable and correct. Hook timeouts prevent indefinite Stop hook hangs but may still allow a long two-hour wait.
- validation_or_tests: `tests/test-template-references.sh` checks hook/template references. Hook behavior is exercised by `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-allowlist-validators.sh`, PR loop test suites, and robustness suites for hook input/system behavior.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:gen-plan` command contract and its I/O validator. It verifies command metadata, the relevance-checker agent metadata, version consistency, malformed fixture rejection, content hygiene, and the exit-code protocol for `scripts/validate-gen-plan-io.sh`.
- algorithmic_behavior: The script is a Bash test harness with counters (`pass`, `fail`) initialized at [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-gen-plan.sh:22). Positive tests PT-1 through PT-9 assert file presence, YAML fields, agent name/model/tools, and version consistency. Negative tests create temporary invalid markdown fixtures via `mktemp -d` and verify local validators reject bad names, missing frontmatter, malformed YAML, and invalid model aliases. The final section calls the actual I/O validator for argument and filesystem cases.
- inputs_outputs_state: Inputs are repository files: `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. Temporary fixtures are created under `mktemp -d` and cleaned by `trap`. Outputs are PASS/FAIL lines, summary counts, and process exit `0` only if `TESTS_FAILED == 0`; see [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-gen-plan.sh:554).
- gates_or_invariants: `gen-plan.md` must exist, include non-empty `description:`, `allowed-tools:`, and `argument-hint:` fields, and remain English-only/no emoji or CJK by the script’s `grep -P` content gate. `draft-relevance-checker.md` must exist, have `name: draft-relevance-checker`, `model: haiku`, and a `tools:` field. Version strings must match across plugin manifest, marketplace manifest, and README; this repository currently exposes `1.5.1` in all three places. Name validation accepts lowercase alphanumeric hyphen identifiers only: `^[a-z][a-z0-9-]*$`.
- dependencies_and_callers: Depends on standard shell tools (`sed`, `grep`, `awk`, `tr`, `mktemp`, `wc`) and the executable validator `scripts/validate-gen-plan-io.sh`. It matches the command workflow in `commands/gen-plan.md`, where phase 1 invokes the validator and maps exit codes `0` through `7`; see [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/commands/gen-plan.md:30). `tests/run-all-tests.sh` includes `test-gen-plan.sh`.
- edge_cases_or_failure_modes: The YAML syntax checker is intentionally lightweight and line-pattern based, so it is not a full YAML parser. Content hygiene uses `grep -P`; the script suppresses grep errors with `2>/dev/null`, which can make behavior platform-dependent if PCRE support is absent. `validate-gen-plan-io.sh` is not just a dry validator: after success it copies the plan template and writes the output plan file, so the “valid paths exits 0” test creates an output artifact in the temp directory. Multiple `trap` assignments overwrite earlier trap commands, but all temp paths are nested in disposable directories for this script’s current structure.
- validation_or_tests: This file is itself the validation. It checks `validate-gen-plan-io.sh` exit codes: invalid/missing args and `--help` exit `6`; missing input exit `1`; empty input exit `2`; missing output directory exit `3`; existing output or directory target exit `4`; valid input/output exit `0`. See [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-gen-plan.sh:438).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR stop hook’s fail-closed git-status gate. It tells Claude/user that repository state could not be verified, preventing loop exit or review progression when `git status --porcelain` fails or times out.
- algorithmic_behavior: The template has one substitution input, `{{GIT_STATUS_EXIT}}`, rendered into the failure message. It is loaded by `hooks/loop-codex-stop-hook.sh` when cached `git status` exits nonzero; the hook then emits a JSON block decision with system message `Loop: Blocked - git status failed`. See [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:386).
- inputs_outputs_state: Input is the numeric or shell exit code from `run_with_timeout "$GIT_TIMEOUT" git status --porcelain`. Output is rendered Markdown used as the JSON `reason`. It does not mutate state; it blocks progression before large-file checks, git-clean checks, Codex review, or finalize transition can proceed.
- gates_or_invariants: The invariant is fail-closed repository verification: if git status cannot be trusted, the loop must not continue. The template enumerates likely causes: lock contention, invalid repo state, or slow large repo. Rendering uses `load_and_render_safe`, so the hook has a fallback if the template is missing; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/template-loader.sh:170).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` in the “Cache Git Status Output” section. Depends indirectly on `scripts/portable-timeout.sh` for bounded git execution and `jq` for block JSON emission. The template reference is covered by `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: If `git rev-parse --git-dir` itself fails, the stop hook treats the directory as not a git repo and skips this particular status gate. If `git status` times out, the exit is normally `124`, but the template accepts any exit value. Missing template does not break the hook because a fallback string is embedded in the caller.
- validation_or_tests: Template existence/reference validation is in `tests/test-template-references.sh`. Stop-hook git failure behavior is indirectly covered by finalize/plan-file robustness tests that assert fail-closed behavior around git operations.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-114 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing review-phase prompt used when Codex review finds `[P0-9]` issues. It is the transition contract from implementation/finalization attempt back into a bounded “fix review findings only” loop.
- algorithmic_behavior: `hooks/loop-codex-stop-hook.sh` calls `continue_review_loop_with_issues(round, review_content)`, updates `current_round` in the active state file, writes `.humanize/rlcr/.../round-N-prompt.md`, and renders this template with `{{REVIEW_CONTENT}}` and `{{SUMMARY_FILE}}`; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:1084). It then emits a JSON block decision, making the rendered prompt the next instruction payload.
- inputs_outputs_state: Inputs are Codex review output starting at the first severity line detected by `detect_review_issues`, plus the computed summary path `round-N-summary.md`. Outputs are a review-fix prompt file and a stop-hook block response. State transition is `current_round -> round` while staying in review phase; the `COMPLETE` signal is explicitly declared ineffective during this phase in the template.
- gates_or_invariants: Claude must address all `[P0-9]` findings, avoid new features/unrelated changes, commit fixes, and write the summary to the exact `{{SUMMARY_FILE}}`. The loop continues until Codex review produces no severity markers. In review phase, `COMPLETE` is ignored and absence of `[P0-9]` findings is the completion gate; caller logic documents that at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:1362).
- dependencies_and_callers: Rendered by `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`; placeholder substitution is single-pass and therefore review content containing template-like braces is not recursively expanded. Depends on `hooks/lib/loop-common.sh::detect_review_issues`, which scans Codex review logs for severity markers in the first 10 characters of a line; see [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:335).
- edge_cases_or_failure_modes: If review content is huge, it is embedded directly into the prompt file and block reason. If Codex review fails or produces no output, this template is not used; the hard-failure block template path is used instead. If the template is missing, the caller fallback has fewer instructions but still blocks and requires fixes. Because completion ignores `COMPLETE`, a user/Claude attempt to force completion during review phase cannot advance the loop.
- validation_or_tests: `tests/test-finalize-phase.sh` exercises review-phase transitions, Codex review failure, empty output blocking, `review_started` preservation, review result creation, round incrementing, and feedback propagation. `tests/test-template-references.sh` verifies the template is referenced and safely rendered.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-144 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the portable timeout abstraction used by git validators and stop-hook review execution. It verifies that `run_with_timeout` behaves consistently across GNU `timeout`, Homebrew `gtimeout`, Python fallback, and no-timeout environments.
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh`, then runs 18 checks. It first validates detected implementation membership (`gtimeout|timeout|python3|python|none`), then checks normal command execution, timeout exit `124`, argument preservation, exit-code preservation, shell pipelines, short timeouts, zero timeout behavior, large output, rapid cycles, nonexistent command handling, empty command handling, special characters, signal handling, fallback detection, subshell execution, and exported `TIMEOUT_IMPL`.
- inputs_outputs_state: Inputs are local PATH/tool availability and shell commands executed through `run_with_timeout`. Outputs are PASS/FAIL counters from `test-helpers.sh` and final exit from `print_test_summary`; see [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/robustness/test-timeout-robustness.sh:278). No persistent repo state is modified.
- gates_or_invariants: Quick commands must return output and exit `0`; timed-out commands must return `124` when a timeout implementation exists; command arguments and special characters must be preserved; successful commands must preserve their own exit code; rapid invocations must not poison subsequent runs; fallback detection must never return an unknown implementation. In `none` mode, timeout-specific tests are skipped by passing with a skip message rather than failing.
- dependencies_and_callers: Direct dependency is `scripts/portable-timeout.sh`, where detection priority is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none`; see [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/scripts/portable-timeout.sh:10). The timeout helper is used by `hooks/loop-plan-file-validator.sh` for branch/tracking git checks and by `hooks/loop-codex-stop-hook.sh` for git status and Codex review execution.
- edge_cases_or_failure_modes: Python fallback uses `subprocess.run(..., timeout=N)` but does not capture child stdout/stderr, so normal streaming output still reaches the caller; timeout exits map to `124`. Zero timeout is intentionally loose: exit `0` or `124` is accepted because implementations vary. Signal-handling commands may exit `0` or `124`. In `none` mode, commands run unbounded and only emit a warning, which preserves functionality but weakens hang protection.
- validation_or_tests: This file is the validation for timeout behavior. Broader robustness suites also call hook validators through `run_with_timeout`, notably `tests/robustness/test-hook-system-robustness.sh`, which uses timeout wrapping around hook execution.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: GENERAL_ROBUSTNESS_EDGE_TEST-HZ-024, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-054, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-084, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-114, GENERAL_ROBUSTNESS_EDGE_TEST-HZ-144
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`