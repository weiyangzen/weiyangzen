# agent_20 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` command workflow that transforms a draft document into a structured implementation plan. It is a command-level algorithm spec for plan generation: validate IO, check repository relevance, analyze the draft, clarify issues, generate acceptance criteria and path boundaries, then write/review the final plan.
- algorithmic_behavior: The workflow is explicitly phased. Phase 1 runs `scripts/validate-gen-plan-io.sh` with `$ARGUMENTS` and maps exit codes 0-7 to stop/continue behavior at `commands/gen-plan.md:30-47`. Phase 2 reads the input draft and delegates a relevance check to `humanize:draft-relevance-checker`, expecting either `RELEVANT: <reason>` or `NOT_RELEVANT: <reason>` at `commands/gen-plan.md:50-72`. Phase 3 directs deep draft analysis across clarity, consistency, completeness, and functionality, using Explore agents to inspect affected code paths at `commands/gen-plan.md:76-108`. Phase 4 requires user clarification and explicitly preserves all original draft content as additive source material at `commands/gen-plan.md:111-139`. Phase 5 defines the generated plan schema and rules, including AC-X acceptance criteria, positive and negative tests, path boundaries, feasibility hints, dependencies, and implementation notes at `commands/gen-plan.md:142-248`. Phase 6 instructs the agent to update the pre-created output using Edit, review the complete plan, optionally unify language, and report results at `commands/gen-plan.md:251-303`.
- inputs_outputs_state: Inputs are command arguments `--input <draft.md> --output <plan.md>`, the draft file content, repository context, user clarifications, and the plugin root for the validation script. Output is a new plan file. The validator creates initial output by copying `prompt-template/plan/gen-plan-template.md` and appending the original draft between draft delimiters, as implemented in `scripts/validate-gen-plan-io.sh:133-160`. The state transition is: no output file exists, validation succeeds and creates seeded output, agent edits placeholders into a completed plan while retaining the original draft section.
- gates_or_invariants: IO validation is the first hard gate, with invalid inputs, empty drafts, missing output dirs, existing output paths, permission failures, and missing template treated as stop conditions (`commands/gen-plan.md:38-47`; `scripts/validate-gen-plan-io.sh:79-145`). Relevance is a second gate: completely unrelated drafts stop the command (`commands/gen-plan.md:67-72`). The draft preservation invariant is strong: clarifications supplement rather than replace original draft content (`commands/gen-plan.md:111-114`, `commands/gen-plan.md:247-248`). Generation invariants include no time estimates, no line-number references, AC-X formatting, deterministic positive and negative tests, affirmative path boundaries, and a code-style warning against embedding planning terminology in implementation code (`commands/gen-plan.md:225-248`). The final review gate requires reading the complete plan and fixing inconsistencies before completion (`commands/gen-plan.md:262-270`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, the plan template at `prompt-template/plan/gen-plan-template.md`, and the agent file `agents/draft-relevance-checker.md`. The command frontmatter authorizes Bash only for the validator plus Read/Glob/Grep/Task/Write/AskUserQuestion at `commands/gen-plan.md:1-12`. README documents the command usage at `README.md:118-127`. `tests/test-gen-plan.sh` validates command structure, allowed tools, argument hint, relevance agent metadata, and validator behavior at `tests/test-gen-plan.sh:55-157` and `tests/test-gen-plan.sh:426-541`.
- edge_cases_or_failure_modes: Handles missing input, empty input, missing output directory, existing output file or directory, no write permission, invalid arguments, and missing template via specific exit codes. The workflow also anticipates unclear drafts, quantitative metrics whose hard/soft status is ambiguous, mixed-language output, contradictions after generation, and inability to generate a complete plan (`commands/gen-plan.md:126-139`, `commands/gen-plan.md:272-303`). A subtle operational edge is that Phase 6 says to use the Edit tool even though frontmatter permits Write, so command execution relies on the agent respecting the textual workflow over the tool list.
- validation_or_tests: `tests/test-gen-plan.sh` covers command presence and frontmatter (`tests/test-gen-plan.sh:55-103`), relevance-agent name/model/tools (`tests/test-gen-plan.sh:107-157`), naming/frontmatter/YAML/content checks elsewhere in the same script, and validator exit-code behavior for argument and path failures (`tests/test-gen-plan.sh:426-541`). The validator itself documents and enforces exit codes 0-7 at `scripts/validate-gen-plan-io.sh:1-12`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancel mechanism around `.humanize/rlcr/.../state.md`. It proves that loop state cannot be modified directly, except for a narrowly authorized cancellation transition from `state.md` or `finalize-state.md` to `cancel-state.md` when a same-loop `.cancel-requested` signal file exists.
- algorithmic_behavior: The test builds temporary active loop directories with `state.md`, exports `CLAUDE_PROJECT_DIR`, constructs simulated Bash hook JSON with `jq`, and pipes that into `hooks/loop-bash-validator.sh` (`tests/test-cancel-signal-file.sh:42-73`). Positive cases allow `mv <loop>/state.md <loop>/cancel-state.md` with `.cancel-requested`, including normalized `./` paths and quoted paths (`tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-380`). The documented literal `${loop_dir}` command form is tested both directly against `is_cancel_authorized` and through the validator (`tests/test-cancel-signal-file.sh:442-486`). All other state mutations or broader shell constructs are expected to exit 2.
- inputs_outputs_state: Inputs are the command string under test, active loop directory discovered through the hook environment, and presence or absence of `.cancel-requested`. The validator output is exit status plus any blocked-message text; the test records pass/fail counters and exits with the number of failures (`tests/test-cancel-signal-file.sh:1349-1357`). The relevant state transition allowed by the spec is `state.md` or `finalize-state.md` renamed to `cancel-state.md`; the signal file is an authorization precondition, not a general permission to edit state.
- gates_or_invariants: The primary invariant is exact cancel shape: signal file exists in the active loop dir, command starts as `mv`, has exactly two arguments, source is active-loop `state.md` or `finalize-state.md`, destination is active-loop `cancel-state.md`, and the source is not a symlink. This is implemented in `hooks/lib/loop-common.sh:527-704`. The validator applies the exception only when an active loop exists and otherwise blocks state destinations and state sources via `command_modifies_file`, segment scanning, and shell-wrapper detection (`hooks/loop-bash-validator.sh:125-156`, `hooks/loop-bash-validator.sh:169-304`, `hooks/loop-bash-validator.sh:306-328`). It deliberately treats no active `state.md` as no active loop, so the command is allowed in that case (`tests/test-cancel-signal-file.sh:1229-1249`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and exercises `hooks/loop-bash-validator.sh` (`tests/test-cancel-signal-file.sh:32-33`, `tests/test-cancel-signal-file.sh:61-73`). It depends on `jq` to produce hook input JSON. It covers `is_cancel_authorized`, `_normalize_path`, state/finalize path recognition, and validator source/destination protections. It also indirectly depends on active-loop discovery from `CLAUDE_PROJECT_DIR` via loop-common.
- edge_cases_or_failure_modes: Extensive negative coverage blocks missing signal file, wrong signal directory, overwrite/edit/delete commands, wrong destination, arbitrary destination, source moved out to `/tmp`, command substitution with `$()` and backticks, embedded newline injection, hidden variables like `${IFS}`, extra arguments before or after the state path, options such as `mv --`, `cp -f`, `sudo`, `env`, `command --`, shell chaining via `;`, `&&`, `||`, `|`, `|&`, and `&`, shell wrappers `sh -c` and `bash -c`, leading redirection prefixes, quoted/escaped/ANSI-C redirection targets, unrelated `/tmp/state.md`, and direct helper false cases (`tests/test-cancel-signal-file.sh:121-357`, `tests/test-cancel-signal-file.sh:382-620`, `tests/test-cancel-signal-file.sh:628-1207`, `tests/test-cancel-signal-file.sh:1208-1343`). The helper itself returns distinct failure categories for missing signal, security violation, mixed quotes, trailing spaces, invalid structure, and symlink source (`hooks/lib/loop-common.sh:527-535`).
- validation_or_tests: This file is itself the validation suite. It verifies both end-to-end hook behavior and direct helper behavior. The matching implementation anchors are `is_cancel_authorized` in `hooks/lib/loop-common.sh:545-704` and RLCR state protection in `hooks/loop-bash-validator.sh:139-156`, source-bypass scanning in `hooks/loop-bash-validator.sh:169-304`, and shell-wrapper scanning in `hooks/loop-bash-validator.sh:306-328`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Small prompt block used as a special-case guard message when the loop detects untracked `.humanize*` artifacts in a dirty worktree. It reinforces the invariant that loop-local `.humanize/` state is generated operational data and should not be committed.
- algorithmic_behavior: The template emits a focused instruction: `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` with a short shell snippet (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). It does not perform logic itself; it is rendered into a larger blocked/dirty-worktree message.
- inputs_outputs_state: Input is only template loading from the stop hook. Output is human-facing markdown appended to `SPECIAL_NOTES` when untracked files include `.humanize` (`hooks/loop-codex-stop-hook.sh:501-510`). State transition requested of the user/agent is repository metadata hygiene: add `.humanize*` to `.gitignore` and stage `.gitignore`, while leaving `.humanize/` uncommitted.
- gates_or_invariants: Enforces a commit-surface invariant, not a code gate by itself: loop runtime state should remain local and ignored. If the template cannot be loaded, the stop hook falls back to a shorter note (`hooks/loop-codex-stop-hook.sh:506-509`), so the guard behavior degrades but still communicates the same policy.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` in `hooks/loop-codex-stop-hook.sh:504-510`. It is only relevant when `GIT_STATUS_CACHED` has uncommitted changes and the untracked subset contains `.humanize`.
- edge_cases_or_failure_modes: Misclassification risk is low but possible: the hook checks untracked paths with `grep '\.humanize'`, which can match any untracked path containing that substring, not only the root `.humanize/` directory (`hooks/loop-codex-stop-hook.sh:501-506`). The template’s suggested `echo '.humanize*' >> .gitignore` can duplicate an existing ignore rule if repeated. The block is advisory text; it does not itself prevent commits.
- validation_or_tests: No dedicated test was found for this exact template. Its caller path is covered indirectly only if stop-hook dirty-worktree prompt rendering is tested elsewhere. The template’s content is directly anchored at `prompt-template/block/git-not-clean-humanize-local.md:2-8`.
- skip_candidate: `yes: prompt advisory block for repository hygiene, not a standalone core algorithm; still relevant to RLCR guard messaging and commit-surface policy`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer appended to generated next-round RLCR prompts. It defines end-of-round behavioral constraints for the next Claude/Codex work round: do not escape the loop improperly, optionally run the code simplifier, commit changes, and write a round summary to the rendered summary path.
- algorithmic_behavior: The template injects a warning that the agent must not exit the RLCR loop by lying, editing the loop state file, or executing `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:4`). It then gives completion steps: use `code-simplifier` if installed, commit changes, and write work summary into `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:6-9`). The stop hook renders this variable after creating `NEXT_PROMPT_FILE` and `NEXT_SUMMARY_FILE` for the next round (`hooks/loop-codex-stop-hook.sh:1476-1506`).
- inputs_outputs_state: Input is the `NEXT_SUMMARY_FILE` value generated as `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` (`hooks/loop-codex-stop-hook.sh:1476-1479`). Output is appended markdown in the next-round prompt file. The state transition it influences is procedural: after the next agent completes work, there should be a commit plus a summary artifact in the loop directory, and the loop state file should remain under system control.
- gates_or_invariants: The key invariant is loop integrity: the worker must not fake completion, manually edit state, or invoke cancel to bypass the RLCR process. It also encodes a completion-surface invariant: commit and summary are required before exiting. The template is rendered with a fallback that only says to commit and write summary if the template is missing (`hooks/loop-codex-stop-hook.sh:1502-1506`), so the stronger anti-escape warning depends on this file being present.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-footer.md"` with `NEXT_SUMMARY_FILE` substitution at `hooks/loop-codex-stop-hook.sh:1502-1506`. It coordinates with the immediately preceding next-round prompt template and optional post-alignment action block (`hooks/loop-codex-stop-hook.sh:1480-1500`).
- edge_cases_or_failure_modes: It is prompt-level enforcement only; actual state-file protection is implemented separately by `hooks/loop-bash-validator.sh` and `hooks/lib/loop-common.sh`. If `code-simplifier` is unavailable, the instruction is conditional and should be skipped. If `NEXT_SUMMARY_FILE` rendering fails, the prompt may contain an unresolved placeholder, weakening the summary path instruction. If the template is missing, the fallback omits the explicit ban on lying/state edits/cancel commands.
- validation_or_tests: No dedicated template test was found. The caller lines in `hooks/loop-codex-stop-hook.sh:1502-1506` validate how it is rendered. The state-edit prohibition in this footer aligns with the executable cancel/state protections covered by `tests/test-cancel-signal-file.sh`.
- skip_candidate: `yes: prompt footer rather than executable algorithm; relevant because it encodes RLCR transition/review contract instructions consumed by the stop hook`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-140 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It exercises tolerant parsing, strict validation, defaulting behavior, quote handling, numeric parsing, malformed frontmatter handling, and file corruption tolerance for `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared test helpers, creates temporary state files, then calls `parse_state_file`, `parse_state_file_strict`, and `get_current_round` under positive and negative scenarios (`tests/robustness/test-state-file-robustness.sh:14-19`). Positive tests cover all required fields in a normal file, direct `current_round` extraction, ignoring extra fields, quoted string fields, and round zero (`tests/robustness/test-state-file-robustness.sh:33-139`). Negative/robustness tests cover missing YAML separators, non-numeric values, missing fields, empty files, partial YAML, malformed YAML, large/negative numbers, paths with spaces, missing files, binary trailing content, and CRLF (`tests/robustness/test-state-file-robustness.sh:149-397`).
- inputs_outputs_state: Inputs are markdown state files with YAML frontmatter. Outputs are global shell variables set by `parse_state_file` or `parse_state_file_strict`, and stdout from `get_current_round`. Tolerant parse sets `STATE_FRONTMATTER`, `STATE_PLAN_TRACKED`, `STATE_START_BRANCH`, `STATE_BASE_BRANCH`, `STATE_BASE_COMMIT`, `STATE_PLAN_FILE`, `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PUSH_EVERY_ROUND`, `STATE_CODEX_MODEL`, `STATE_CODEX_EFFORT`, `STATE_CODEX_TIMEOUT`, and `STATE_REVIEW_STARTED`, defaulting round to 0, max iterations to 10, and push-every-round to false (`hooks/lib/loop-common.sh:190-240`). Strict parse returns distinct validation failures for missing file, missing separators, missing required fields, non-numeric round/max, and invalid boolean review flag (`hooks/lib/loop-common.sh:242-323`).
- gates_or_invariants: Tolerant parsing should never crash on malformed content and should use defaults where appropriate. Strict parsing should reject absent frontmatter, missing required fields, and non-numeric numeric fields (`tests/robustness/test-state-file-robustness.sh:149-228`). The current implementation’s strict contract is stronger than some test fixtures imply: `parse_state_file_strict` requires `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:284-300`), while the tests describe only current round and max iterations as missing-field targets in comments (`tests/robustness/test-state-file-robustness.sh:201-214`). Numeric strict validation allows negative integers via `^-?[0-9]+$` (`hooks/lib/loop-common.sh:302-310`).
- dependencies_and_callers: Directly depends on `hooks/lib/loop-common.sh` for parsing functions and `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/robustness/test-state-file-robustness.sh:14-19`). The parser is used by `hooks/loop-bash-validator.sh` to validate active RLCR/finalize state before enforcing loop rules (`hooks/loop-bash-validator.sh:75-93`) and by broader loop logic for current round/state metadata.
- edge_cases_or_failure_modes: The test deliberately permits tolerant ambiguity for non-numeric `current_round`, partial YAML, negative numbers, and CRLF by checking that the parser handles them gracefully rather than enforcing one exact value (`tests/robustness/test-state-file-robustness.sh:190-199`, `tests/robustness/test-state-file-robustness.sh:242-257`, `tests/robustness/test-state-file-robustness.sh:297-312`, `tests/robustness/test-state-file-robustness.sh:386-397`). Binary content after frontmatter should not affect round extraction (`tests/robustness/test-state-file-robustness.sh:367-384`). Non-existent files should return non-zero from tolerant parse (`tests/robustness/test-state-file-robustness.sh:358-365`). A notable gap is no direct assertion for strict `review_started` boolean failure or missing `base_branch`, even though current strict parsing enforces both.
- validation_or_tests: This file is itself the validation suite and ends with `print_test_summary` and `exit $?` (`tests/robustness/test-state-file-robustness.sh:399-404`). Implementation anchors are `get_current_round` at `hooks/lib/loop-common.sh:175-188`, tolerant `parse_state_file` at `hooks/lib/loop-common.sh:190-240`, and strict `parse_state_file_strict` at `hooks/lib/loop-common.sh:242-323`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence headings, matching assigned count
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`