# agent_26 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Code Stop hook for the RLCR state machine. It intercepts exit attempts, checks loop safety gates, runs Codex review, advances rounds, enters review/finalize phases, or terminates the loop. The file declares its state surfaces at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:8) and finds the active loop at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:59).
- algorithmic_behavior: Reads hook stdin, locates `.humanize/rlcr/<timestamp>`, parses `state.md` or `finalize-state.md`, then applies fail-closed checks before Codex is invoked. Normal implementation rounds build `round-N-review-prompt.md` and run `codex exec`; a last non-empty line of `COMPLETE` starts the separate `codex review` phase, while `STOP` triggers the circuit breaker. Review phase runs `codex review --base <BASE_COMMIT|BASE_BRANCH>` until no severity markers are detected, then moves into finalize; finalize requires the final summary and all preliminary gates before preserving `complete-state.md`.
- inputs_outputs_state: Inputs include hook JSON on stdin, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr` state files, plan backup, summary files, `goal-tracker.md`, git status, TodoWrite transcript data, templates, and Codex CLI. Outputs include JSON block decisions, review prompts/results, debug files in `XDG_CACHE_HOME` or `$HOME/.cache`, next-round prompts, and state renames. Key transitions are `state.md` to `finalize-state.md` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:1002), `finalize-state.md` to `complete-state.md` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:716), and exit-reason state names through `end_loop` in [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:1106).
- gates_or_invariants: Requires schema fields and numeric rounds before proceeding; invalid model or effort ends as unexpected. Branch must match `start_branch`; plan backup must exist and match the original; incomplete TodoWrite items block; git status failures block; large changed code/docs files over 2000 lines block; dirty git state blocks; unpushed commits block when `push_every_round=true`; summary file must exist; Round 0 goal tracker placeholders block; max iterations block except review/finalize phases. Review phase also requires `.review-phase-started` to prevent manual `review_started=true` edits at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:1412).
- dependencies_and_callers: Intended as the Claude Stop hook. It sources `hooks/lib/loop-common.sh` for state parsing, markers, issue detection, templates, and end-loop behavior, plus `scripts/portable-timeout.sh` for bounded git/Codex calls. It calls `hooks/check-todos-from-transcript.py`, `git`, `jq`, `diff`, `awk`, `sed`, `wc`, and `codex`. Prompt dependencies include `prompt-template/codex/full-alignment-review.md`, regular review, goal-tracker update, block, next-round, review-phase, and finalize templates.
- edge_cases_or_failure_modes: No active loop or no state file allows exit. Tolerant parsing is followed by raw-field checks so truncated state cannot silently default to round 0. Codex failures, missing review result, or empty review result all block with retry instructions. `COMPLETE` must be the exact last non-empty line to avoid false positives such as “cannot complete”. `detect_review_issues` only treats severity markers in the first 10 characters of a line as review issues, so later markers are intentionally ignored by that detector. Several git/file checks use the current shell context while most branch/review operations use `PROJECT_ROOT`, so hook working directory remains an important runtime assumption.
- validation_or_tests: Covered by related suites such as `tests/test-finalize-phase.sh`, `tests/test-codex-review-merge.sh`, state-transition/concurrent robustness tests, todo-checker tests, and template-reference tests. I did not execute tests because this assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable shell specification for two robustness behaviors: `.humanize` git-add protection and glob-free directory iteration patterns that are safe in empty or dotfile-only scenarios. It sources `hooks/lib/loop-common.sh` at [test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-humanize-escape.sh:18).
- algorithmic_behavior: Defines `assert_blocks` and `assert_allows`, lowercases each command with `to_lower`, and calls `git_adds_humanize`. It then enumerates blocked command variants for direct, quoted, forced, all-scope, chained, and `git -C` cases, plus allowed variants for specific files, non-add git commands, patch mode, and similarly named non-`.humanize` files. The second half creates temp directories and validates `find`-based loops for empty dirs, dotfile-only dirs, `*-state.md` detection, and timestamped session selection.
- inputs_outputs_state: Inputs are literal command strings, sourced helper functions, and temporary directories under `/tmp`. The script mutates local counters `TESTS_PASSED` and `TESTS_FAILED`, temporarily changes directory for the `git add -A` cases, creates and removes temp directories, prints PASS/FAIL lines, and exits 0 only when no failures remain at [test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-humanize-escape.sh:344).
- gates_or_invariants: Direct `.humanize` path references must always block, including `./.humanize`, nested paths, and quotes. Force plus broad scope must block because it bypasses gitignore. `-A` and `--all` must block when `.humanize` exists. Safe commands must not over-block `.humanizeconfig`, `.humanize-backup`, `.humanizerc`, `git status`, `git diff`, `git log`, or patch mode. `find` loops must return zero results rather than triggering shell no-match errors.
- dependencies_and_callers: Depends on `to_lower` and `git_adds_humanize` in [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:902). `git_adds_humanize` is used by `hooks/loop-bash-validator.sh` to block dangerous Bash commands at [loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-bash-validator.sh:115). The suite is included in `tests/run-all-tests.sh` at [run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/run-all-tests.sh:48).
- edge_cases_or_failure_modes: The test simulates zsh-safe behavior with `find` loops but is itself a bash script, so it validates the pattern rather than executing zsh glob failure behavior. It uses `/tmp/test-humanize-*-$PID` without a trap, so an early abort could leave temp directories. The `git_adds_humanize` all-scope behavior is cwd-sensitive because `.humanize` existence is checked in the current directory; the test explicitly changes into a temp dir for those cases.
- validation_or_tests: This file is itself the validation asset for `git_adds_humanize` and find-based iteration. I inspected it directly but did not run it due to the read-only/no-modification research constraint.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the goal-tracker ownership invariant: after Round 0, Claude may not directly modify `goal-tracker.md`; updates must be requested in the round summary for Codex review.
- algorithmic_behavior: Renders a Markdown block headed with the current round, states the prohibition on direct Write/Edit/Bash modification, points to `{{SUMMARY_FILE}}`, and gives a required `Goal Tracker Update Request` structure with requested changes and justification. The template body is at [goal-tracker-modification.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/prompt-template/block/goal-tracker-modification.md:1).
- inputs_outputs_state: Inputs are `CURRENT_ROUND` and `SUMMARY_FILE` placeholders. Output is human-facing block text returned by validators on stderr or hook messages. It does not modify state directly; it redirects the state transition request into the summary file so Codex can apply justified tracker changes later.
- gates_or_invariants: Enforces “only Codex can modify the Goal Tracker” after Round 0. The invariant is implemented for Write at [loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-write-validator.sh:192), Edit at [loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-edit-validator.sh:145), and Bash at [loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-bash-validator.sh:349).
- dependencies_and_callers: Loaded through `goal_tracker_blocked_message` in [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/lib/loop-common.sh:1092). Rendering uses the single-pass template loader, which keeps unresolved placeholders literal. Related templates are `prompt-template/codex/goal-tracker-update-section.md` and `prompt-template/claude/goal-tracker-update-request.md`.
- edge_cases_or_failure_modes: If callers omit a variable, the placeholder remains because rendering is non-recursive and tolerant. Bash has stricter behavior in Round 0: direct Bash edits to `goal-tracker.md` are blocked even when Write/Edit are still allowed, with a separate bash-specific message. This template is policy text, not a parser, so it relies on validators to detect the attempted modification accurately.
- validation_or_tests: `tests/test-template-references.sh` explicitly checks that `block/goal-tracker-modification.md` exists in the common template set at [test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/tests/test-template-references.sh:152).
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-116 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory full-alignment Codex review contract used every fifth implementation round. It makes Codex audit the original plan, goal tracker, acceptance criteria, implementation reality, deferrals, forgotten work, and stagnation before allowing completion.
- algorithmic_behavior: Instructs Codex to read `@{{PLAN_FILE}}`, inspect `@{{GOAL_TRACKER_FILE}}`, evaluate every immutable acceptance criterion as MET/PARTIAL/NOT MET/DEFERRED, compare plan versus tracker for forgotten work, audit deferrals, review implementation claims, process goal-tracker update requests, inspect recent round history, and write `STOP` or `COMPLETE` as the final line only under strict conditions. The output rules are at [full-alignment-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/prompt-template/codex/full-alignment-review.md:86).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, iteration counts, loop timestamp, previous-round numbers, and `REVIEW_RESULT_FILE`. Output is the rendered `round-N-review-prompt.md`; Codex is expected to write findings to `round-N-review-result.md`. The stop hook selects this template when `CURRENT_ROUND % 5 == 4` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:747) and renders it at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:782).
- gates_or_invariants: `COMPLETE` is permitted only when all original plan tasks are done, all acceptance criteria are fully met, and there are no deferrals. Deferred criteria are explicitly incomplete. `STOP` is the circuit breaker for stagnation, repeated issues, circular rounds, no meaningful AC progress, or unaddressed recurring feedback. The hook later interprets exact final-line markers and terminates on `STOP` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-robustness-edge-test/hooks/loop-codex-stop-hook.sh:1437).
- dependencies_and_callers: Used by `hooks/loop-codex-stop-hook.sh` during implementation-phase `codex exec`. Depends on the template loader, goal-tracker update section, loop history files under `.humanize/rlcr/<timestamp>/`, the original plan, docs path, and Codex producing a review result file.
- edge_cases_or_failure_modes: The template gives examples for recent historical files and asks for the last five rounds, so Codex must generalize beyond the listed previous-two examples. If Codex writes `COMPLETE` with any deferred AC, the prompt contract is violated, but enforcement depends on Codex following the instruction; the hook only checks the marker. If output lacks an exact final marker, the hook treats it as feedback and continues the loop rather than completing.
- validation_or_tests: Directly referenced by the stop hook, and template loading/fallback behavior is covered by template-loader and template-reference suites. I inspected the template and caller but did not execute the review flow.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique Item Evidence sections above, matching the four assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`