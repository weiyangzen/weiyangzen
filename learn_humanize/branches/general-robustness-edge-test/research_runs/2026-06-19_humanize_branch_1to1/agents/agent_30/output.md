# agent_30 general-robustness-edge-test 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `4a4e59ce0cc5613c54d15754a29c7d2a2e9be058`

## Item Evidence

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-030 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` validator for the RLCR loop and PR loop. It protects loop-owned files under `.humanize/rlcr/` and `.humanize/pr-loop/` from direct Claude writes that would corrupt the loop state machine, round progression, generated prompts, todo files, or PR review artifacts.
- algorithmic_behavior: Reads hook JSON from stdin, validates it through `validate_hook_input`, rejects overly deep JSON with `is_deeply_nested`, ignores non-`Write` tools, and requires `tool_input.file_path` before proceeding (`hooks/loop-write-validator.sh:25-48`). It lowercases the target path for pattern checks, then applies ordered gates: block `round-N-todos.md` except a small active-loop allowlist (`55-63`), block `round-N-prompt.md` (`65-68`), enforce PR-loop file protection (`74-94`), classify RLCR summary/finalize/loop-internal paths (`100-117`), locate the active RLCR loop and strict-parse state (`123-145`), block state files (`152-160`), allow active-loop `finalize-summary.md` only at the exact active path (`168-173`), block `plan.md` backups (`179-185`), block `goal-tracker.md` after round 0 (`192-196`), block summary files outside `.humanize/rlcr/` (`202-210`), validate summary round number (`228-246`), and finally require the path to match the active loop directory (`252-263`).
- inputs_outputs_state: Input is hook JSON containing `tool_name` and `tool_input.file_path`. Output is exit status plus stderr block messages rendered through templates or fallbacks. Exit `0` means allow; exit `1` is malformed/unsafe input or malformed state; exit `2` is intentional policy block. It reads active-loop state from `.humanize/rlcr/<loop>/state.md` or `finalize-state.md` and uses `STATE_CURRENT_ROUND` as the state-machine cursor (`132-145`).
- gates_or_invariants: Only current-round summaries may be written unless allowlisted (`228-245`). `state.md` and `finalize-state.md` are system-managed and never directly writable (`152-160`). `goal-tracker.md` is writable by Claude only during round 0; later updates must be requested through summaries (`192-196`). PR-loop `state.md` and generated review/prompt/check/feedback files are read-only (`76-87`). `round-N-pr-resolve.md` is writable only for the active PR round (`89-92`). Summary files must live inside the active `.humanize/rlcr/<loop>/` directory (`202-210`, `252-263`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`18-19`). Uses helper validators and classifiers including `validate_hook_input`, `require_tool_input_field`, `find_active_loop`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, `is_in_pr_loop_dir`, `is_pr_loop_readonly_file`, and `validate_pr_resolve_round`. The helper library defines JSON/null-byte/UTF-8/depth checks (`hooks/lib/loop-common.sh:58-131`), active loop discovery (`155-173`), strict state parsing (`251-323`), round/allowlist helpers (`395-435`), PR-loop classifiers (`777-829`), and PR resolve round validation (`837-880`).
- edge_cases_or_failure_modes: Invalid JSON, missing `tool_name`, missing `file_path`, null bytes, invalid UTF-8, and nested JSON over depth 30 fail before path policy (`hooks/loop-write-validator.sh:27-48`; `hooks/lib/loop-common.sh:58-131`). If no active loop exists, RLCR summary/state-related writes are allowed after classification because there is no active state to protect (`128-130`). Malformed active state fails closed with “Malformed state file” (`140-144`). `finalize-state.md` must be checked before generic `state.md` because the generic matcher also catches it (`148-153`). The hardcoded allowlist permits only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop (`hooks/lib/loop-common.sh:417-435`), which is a deliberate bootstrap exception but a possible future maintenance hotspot.
- validation_or_tests: Behavior is indirectly covered by hook and template tests, with PR-loop missing-trigger tests referenced in `tests/test-pr-loop-hooks.sh:606-618`. Shared helper behavior is covered by template and hook tests found through repository references, but this assigned file itself is an executable hook rather than a standalone test.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-060 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file acceptance rules. It verifies the setup algorithm rejects unsafe plan paths, invalid repository states, incorrect tracking policy, weak plan content, YAML-unsafe branch/model values, and malformed CLI combinations before an RLCR loop can start.
- algorithmic_behavior: Creates isolated temp git repos, writes candidate plan files, invokes `scripts/setup-rlcr-loop.sh`, captures exit code/output, and asserts expected rejection or acceptance messages. It unsets `CLAUDE_PROJECT_DIR` so setup uses the test temp repo instead of the real project (`tests/test-plan-file-validation.sh:16-21`). It tracks pass/fail/skip counters and exits with the failure count (`23-38`, `743-752`). It also mocks `codex` when unavailable to let validation reach setup gates that occur before actual Codex use (`73-84`).
- inputs_outputs_state: Inputs are synthetic repo directories and plan paths generated inside `mktemp -d` (`37-70`). Outputs are colored PASS/FAIL/SKIP lines and a process exit equal to `TESTS_FAILED` (`32-34`, `743-752`). State transitions are test-environment transitions: initialize repo, add/ignore/track plan files, mutate branch names, make files unreadable where possible, run setup, then clean via trap (`36-39`).
- gates_or_invariants: The tested setup invariants map directly to `scripts/setup-rlcr-loop.sh`: plan path must be relative (`setup-rlcr-loop.sh:273-277`), contain no whitespace (`279-285`), contain no shell metacharacters (`287-294`), not be a symlink itself or through parent dirs (`299-333`), exist/read within project root (`335-359`), not be inside a submodule (`361-374`), satisfy tracking policy (`400-425`), have at least five lines and at least three meaningful non-comment content lines (`431-484`), run inside a git repo with at least one commit (`257-267`), use YAML-safe branch names (`504-512`), and use safe Codex model/effort fields (`514-520` and following validation for effort).
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh` repeatedly. That setup script sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:26-29`, `203-205`), checks mutual exclusion with active RLCR/PR loops (`211-232`), and then executes the validation layers that this test targets.
- edge_cases_or_failure_modes: Covers absolute paths (`89-102`), missing file (`104-114`), missing parent directory (`116-126`), spaces in dirs/files (`128-167`), many shell metacharacters (`169-187`), direct symlink (`189-200`), permission/path resolution errors with conditional skip if chmod is unsupported (`202-243`), `../` project escape (`245-272`), non-git repos (`274-297`), git repos with no commits (`299-323`), tracked plan without `--track-plan-file` (`329-359`), untracked plan with `--track-plan-file` (`361-393`), modified tracked plan (`395-426`), YAML-unsafe branch characters where git permits them (`432-512`), blank/sparse/comment-only content (`518-608`), valid content with single-line HTML comments (`610-661`), CLI ambiguity (`667-695`), and unsafe Codex parameters (`701-741`).
- validation_or_tests: This is itself the validation asset. It verifies expected setup behavior mostly by matching output substrings such as “relative path”, “not found”, “directory not found”, “cannot contain spaces”, “shell metacharacters”, “symbolic link”, “git repository”, “at least one commit”, “gitignored”, “tracked in git”, “clean”, “YAML-unsafe”, “insufficient content”, and “invalid characters”.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-090 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Block-message template for PR-loop trigger gating. It defines the user-facing reason when the PR loop cannot advance because the required bot-mention trigger comment is missing.
- algorithmic_behavior: Template contains placeholders for `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING` and tells the operator to post a comment mentioning the configured bots (`prompt-template/block/no-trigger-comment.md:3-17`). It does not mutate state itself; it is rendered into a hook block response.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/pr-loop-stop-hook.sh`: startup case, case description, current round, and bot mention string (`hooks/pr-loop-stop-hook.sh:717-731`). Output is rendered markdown placed into the JSON `reason` field of a block decision (`733-735`). The relevant state is PR loop round and trigger status: `REQUIRE_TRIGGER=true` and empty `PR_LAST_TRIGGER_AT` cause this template path (`684-718`).
- gates_or_invariants: Enforces that round greater than 0 always requires a trigger, new commits require a fresh trigger, and startup cases 4/5 require re-trigger on round 0 (`hooks/pr-loop-stop-hook.sh:684-707`). Trigger validation intentionally runs before Claude “eyes” verification to avoid checking stale trigger IDs (`710-717`, `738-747`).
- dependencies_and_callers: Called via `load_and_render_safe "$TEMPLATE_DIR" "block/no-trigger-comment.md"` in `hooks/pr-loop-stop-hook.sh:729-731`. The fallback message at `726-728` is used if template loading fails. `BOT_MENTION_STRING` is built by `build_bot_mention_string` in `hooks/lib/loop-common.sh:758-770`.
- edge_cases_or_failure_modes: Unknown startup cases default to not requiring a trigger in the earlier gate (`hooks/pr-loop-stop-hook.sh:703-705`), so this template is not rendered for those cases unless another condition sets `REQUIRE_TRIGGER=true`. Missing template files degrade to fallback text rather than failing the hook. The template assumes a usable bot mention string; if no active bots exist, the rendered action may be weak, but the caller normally builds this from configured bots.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` includes a missing-trigger test for round greater than 0, referenced at `tests/test-pr-loop-hooks.sh:606-618`. Repository search shows this template is rendered only by `hooks/pr-loop-stop-hook.sh:729`.
- skip_candidate: `no`

### GENERAL_ROBUSTNESS_EDGE_TEST-HZ-120 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: Prompt fragment inserted into the PR-loop Codex review prompt. It instructs Codex to update the PR loop `goal-tracker.md` after analyzing bot reviews, preserving PR-loop accounting across review rounds.
- algorithmic_behavior: Defines required updates to three tracker sections: add one Issue Summary row for `NEXT_ROUND`, update aggregate totals, and add an Issue Log entry for the round (`prompt-template/pr-loop/codex-goal-tracker-update.md:3-21`). It supplies an example markdown transformation (`22-55`) and rules that constrain edits to the summary/statistics/log sections while preserving headers and table structure (`57-64`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `NEXT_ROUND` placeholders (`3`, `9`, `18`). Output is rendered instruction text embedded inside `round-N-codex-prompt.md`; Codex then writes analysis to `round-N-pr-check.md` and may update `goal-tracker.md`. The caller constructs these variables at `hooks/pr-loop-stop-hook.sh:1239-1248` and appends the rendered instructions into the generated Codex prompt at `1288`.
- gates_or_invariants: Maintains tracker structure: append to existing Issue Summary, recompute `Total Issues Found`, `Total Issues Resolved`, and `Remaining`, add one round-scoped Issue Log entry, keep PR Information and Ultimate Goal unchanged, and use one row per reviewer (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`). The surrounding PR-loop prompt requires Codex to classify each expected bot as `APPROVE`, `ISSUES`, or `NO_RESPONSE`, then emit a final recommendation (`hooks/pr-loop-stop-hook.sh:1261-1287`), so tracker updates are coupled to per-bot review state.
- dependencies_and_callers: Rendered by `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/codex-goal-tracker-update.md"` in `hooks/pr-loop-stop-hook.sh:1248`. It depends on the PR-loop goal tracker created during setup and stored at `$LOOP_DIR/goal-tracker.md`. The generated prompt is executed through `codex exec` with `--full-auto -C "$PROJECT_ROOT"` (`hooks/pr-loop-stop-hook.sh:1309-1320`).
- edge_cases_or_failure_modes: The template’s example includes both issue and approval rows, but it relies on Codex to correctly infer counts from review content; bad or ambiguous bot reviews can cause tracker accounting drift. If the template cannot load, the fallback only says to update the tracker “with current status” (`hooks/pr-loop-stop-hook.sh:1245-1248`), which is less strict than this file. The hook later has automated `update_pr_goal_tracker` fallback paths (`hooks/pr-loop-stop-hook.sh:1359-1361`, `1514-1518`), so manual prompt compliance is not the only tracker update mechanism.
- validation_or_tests: Repository search shows direct template use in `hooks/pr-loop-stop-hook.sh:1248`; template-loader behavior is covered by template tests, but this specific prompt fragment has no standalone executable test found in the inspected references.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4 unique assigned item sections present above`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`