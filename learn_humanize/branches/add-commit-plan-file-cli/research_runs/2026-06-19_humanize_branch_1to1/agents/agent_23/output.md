# agent_23 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-023 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Cross-platform timeout adapter for long-running hook subprocesses. It normalizes macOS/Homebrew, GNU/Linux, Python, and no-timeout environments behind one Bash function, `run_with_timeout`, so the Codex stop hook can enforce `CODEX_TIMEOUT` without hard depending on GNU `timeout`. The script is intended to be sourced, not executed as a standalone command; usage is documented at `scripts/portable-timeout.sh:3-6`.
- algorithmic_behavior: On source, `detect_timeout_impl` probes timeout support in priority order: `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:9-27`). It stores the selected implementation in global `TIMEOUT_IMPL` (`scripts/portable-timeout.sh:29`). `run_with_timeout <seconds> <command> [args...]` shifts the timeout value, preserves the remaining command as a Bash array, then dispatches by `TIMEOUT_IMPL` (`scripts/portable-timeout.sh:31-71`). Native timeout modes call `gtimeout "$timeout_secs" "${cmd[@]}"` or `timeout "$timeout_secs" "${cmd[@]}"` and return the child exit code (`scripts/portable-timeout.sh:38-45`). Python fallback runs `subprocess.run(sys.argv[1:], timeout=<seconds>)`, exits with the child return code, and maps `subprocess.TimeoutExpired` to `124`, matching GNU timeout convention (`scripts/portable-timeout.sh:47-61`). If no implementation exists, it warns to stderr and executes the command directly (`scripts/portable-timeout.sh:64-68`).
- inputs_outputs_state: Inputs are the sourced shell environment, available commands on `PATH`, a timeout duration string, and the target command/args. Outputs are the target command stdout/stderr plus the returned exit status. State is limited to the exported `TIMEOUT_IMPL` variable (`scripts/portable-timeout.sh:73-76`) and the `run_with_timeout` shell function installed in the caller. The main observed consumer passes `CODEX_TIMEOUT` and a `codex exec` invocation via a pipeline: `printf ... | run_with_timeout "$CODEX_TIMEOUT" codex exec ...` in `hooks/loop-codex-stop-hook.sh:712-716`.
- gates_or_invariants: Selection invariant is first available supported timeout backend wins; BSD/non-GNU `timeout` is rejected by requiring `timeout --version` to succeed (`scripts/portable-timeout.sh:13-19`). Exit-code invariant is pass-through for normal child completion and `124` on Python-enforced timeout (`scripts/portable-timeout.sh:53-57`). Argument integrity is maintained with a Bash array (`scripts/portable-timeout.sh:36`), so whitespace-bearing command args are preserved by native and Python modes. The stop hook treats any nonzero Codex exit, including `124`, as a blocking review failure and emits retry diagnostics (`hooks/loop-codex-stop-hook.sh:754-771`).
- dependencies_and_callers: Depends on Bash arrays and `command -v`; optional external dependencies are `gtimeout`, GNU `timeout`, `python3`, or `python`. It is sourced by `hooks/loop-codex-stop-hook.sh` when `"$PLUGIN_ROOT/scripts/portable-timeout.sh"` exists (`hooks/loop-codex-stop-hook.sh:669-673`). The hook has a weaker inline fallback if the file is missing (`hooks/loop-codex-stop-hook.sh:674-687`), but that fallback checks `timeout` before `gtimeout` and has no Python timeout path. Repository search found no other direct caller except README tree documentation.
- edge_cases_or_failure_modes: With no backend, commands can run indefinitely despite a warning (`scripts/portable-timeout.sh:64-67`), which weakens the stop hook’s intended Codex review bound. Native timeout implementations may terminate process groups differently from Python `subprocess.run`; the Python fallback only starts and waits for the direct child command, so child process cleanup semantics may differ from GNU timeout. The Python code interpolates `$timeout_secs` directly into `timeout=$timeout_secs` (`scripts/portable-timeout.sh:54`); non-numeric or shell-hostile timeout values would cause Python syntax/runtime failure, returning `1` through the general exception path or earlier shell parsing behavior. Because `set -euo pipefail` is active in the stop hook (`hooks/loop-codex-stop-hook.sh:15`), the caller explicitly uses `|| CODEX_EXIT_CODE=$?` to keep nonzero timeout/command exits from aborting before diagnostics (`hooks/loop-codex-stop-hook.sh:714-716`).
- validation_or_tests: No dedicated test reference for `portable-timeout.sh` was found by repository search. Its behavior is indirectly exercised only when the Codex stop hook reaches the Codex review phase: the hook logs timeout configuration (`hooks/loop-codex-stop-hook.sh:697-709`), runs through `run_with_timeout` (`hooks/loop-codex-stop-hook.sh:715-716`), and then validates output/exits through hook failure handling (`hooks/loop-codex-stop-hook.sh:754-790`). The branch export is not a Git checkout in this sandbox, so git-based validation could not be run.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-053 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Prompt/error block used by RLCR loop validators when a round file path is inside a loop area but not the active loop directory path. It does not enforce the gate itself; it defines the operator-facing remediation message rendered by read/write validators before they block with exit code `2`.
- algorithmic_behavior: The template has three placeholders: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:3-4`). It tells the agent what action was attempted, shows the required active path, and adds a shell read hint (`prompt-template/block/wrong-directory-path.md:6`). `hooks/loop-read-validator.sh` computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and, when `FILE_PATH` differs exactly, renders this template with `ACTION=read` and exits `2` (`hooks/loop-read-validator.sh:122-132`). `hooks/loop-write-validator.sh` performs the same exact-path check for write targets, renders with `ACTION=write to`, and exits `2` (`hooks/loop-write-validator.sh:169-180`).
- inputs_outputs_state: Inputs are supplied by validators through `load_and_render_safe`: attempted action, attempted path, and computed correct active-loop path. Output is markdown written to stderr by the validator call sites (`hooks/loop-read-validator.sh:128-131`, `hooks/loop-write-validator.sh:175-178`). The template has no persistent state. The surrounding state comes from `.humanize-loop.local`: validators find the active loop directory with `find_active_loop`, which selects the newest loop directory containing `state.md` (`hooks/lib/loop-common.sh:23-44`), and current round comes from state frontmatter (`hooks/lib/loop-common.sh:46-58`).
- gates_or_invariants: Enforcement invariant is exact path equality after earlier file-type, location, and round checks pass. For reads, only `round-N-summary.md` and `round-N-prompt.md` are considered; other reads exit allowed (`hooks/loop-read-validator.sh:45-47`). For writes, non-summary files outside `.humanize-loop.local` are allowed, while summary/state/goal/plan-backup paths receive additional validation (`hooks/loop-write-validator.sh:51-67`). Directory-path blocking happens only after wrong-file-location and wrong-round checks have already been handled (`hooks/loop-read-validator.sh:83-116`, `hooks/loop-write-validator.sh:115-163`). Template rendering uses a single-pass placeholder substitution to avoid replacing placeholder-like content inside variable values (`hooks/lib/template-loader.sh:35-43`, `hooks/lib/template-loader.sh:56-116`).
- dependencies_and_callers: Direct callers are `hooks/loop-read-validator.sh:128` and `hooks/loop-write-validator.sh:175`. Rendering depends on `hooks/lib/template-loader.sh`, especially `load_and_render_safe` (`hooks/lib/template-loader.sh:152-188`), and on `TEMPLATE_DIR` initialization from `hooks/lib/loop-common.sh:11-20`. The validators depend on `jq` for hook JSON parsing (`hooks/loop-read-validator.sh:22-30`, `hooks/loop-write-validator.sh:23-31`) and shared path helpers such as `is_round_file_type`, `extract_round_number`, and `is_in_humanize_loop_dir` (`hooks/lib/loop-common.sh:65-84`, `hooks/lib/loop-common.sh:149-153`).
- edge_cases_or_failure_modes: The message says `cat {{FILE_PATH}}` even when the attempted action is `write to`, so the remediation is read-oriented for write failures too (`prompt-template/block/wrong-directory-path.md:6`). The exact string comparison means semantically equivalent paths can fail if one path is relative, has symlinks, or differs by normalization; there is no canonicalization in the checked branch (`hooks/loop-read-validator.sh:122-124`, `hooks/loop-write-validator.sh:169-171`). `is_in_humanize_loop_dir` is a substring regex check, so unusual paths containing `.humanize-loop.local/` in nonstandard locations can enter the stricter path flow (`hooks/lib/loop-common.sh:149-153`). If the template is missing or empty, both validators fall back to inline messages with the same variables, so enforcement continues (`hooks/loop-read-validator.sh:125-128`, `hooks/loop-write-validator.sh:172-175`, `hooks/lib/template-loader.sh:164-185`).
- validation_or_tests: Template availability and rendering are likely covered by the repository’s template-focused shell tests discovered under `tests/`, especially `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/test-templates-comprehensive.sh`. The direct gate behavior is in the read/write validators; repository search showed the template is referenced only by those two validators plus its own file. No test execution was run because this task required research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence headings present, matching the assigned table order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`