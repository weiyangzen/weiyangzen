# agent_14 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-014 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that gates RLCR loop exit on Claude native `TodoWrite` completion state. It prevents the expensive stop/review path from running while the latest transcript-visible todo list still has unfinished items.
- algorithmic_behavior: The main algorithm is `find_latest_todos(transcript_path)` in `hooks/check-todos-from-transcript.py:17`. It streams a JSONL transcript line by line, skips blank or malformed JSON lines, and keeps only the most recent non-empty `TodoWrite` todo list encountered. It recognizes three transcript shapes: Claude assistant messages with `message.content[]` tool-use blocks at `hooks/check-todos-from-transcript.py:48`, alternative `type: message` entries at `hooks/check-todos-from-transcript.py:62`, and direct `type: tool_use` entries at `hooks/check-todos-from-transcript.py:75`. The CLI `main()` reads hook JSON from stdin, extracts `transcript_path`, expands `~`, loads latest todos, and fails if any latest todo status is not exactly `completed` at `hooks/check-todos-from-transcript.py:87` and `hooks/check-todos-from-transcript.py:109`.
- inputs_outputs_state: Input is JSON on stdin with `transcript_path` (`hooks/check-todos-from-transcript.py:89` through `hooks/check-todos-from-transcript.py:100`) plus a JSONL transcript file. Output is process exit status and, on failure, stdout beginning with `INCOMPLETE_TODOS`, followed by formatted lines like `  - [status] content` (`hooks/check-todos-from-transcript.py:117` through `hooks/check-todos-from-transcript.py:122`). Exit `0` means no valid hook input, no transcript path, missing transcript, no latest todos, or all latest todos are `completed`; exit `1` means one or more latest todos are not `completed`. Internal state is only `latest_todos`, overwritten whenever a later non-empty `TodoWrite` call is found (`hooks/check-todos-from-transcript.py:25`, `hooks/check-todos-from-transcript.py:59`, `hooks/check-todos-from-transcript.py:72`, `hooks/check-todos-from-transcript.py:81`).
- gates_or_invariants: The gate invariant is “latest native todo list must contain only `completed` statuses.” Missing or unreadable-by-existence transcript is treated as no todos and therefore allowed (`hooks/check-todos-from-transcript.py:22`). Malformed transcript lines are ignored rather than fatal (`hooks/check-todos-from-transcript.py:33` through `hooks/check-todos-from-transcript.py:36`). Empty `todos` arrays do not replace the previous non-empty latest list, because updates happen only when `if todos` is true. The script does not parse RLCR checklist cursor syntax; it validates Claude `TodoWrite` item statuses.
- dependencies_and_callers: Depends only on Python stdlib `json`, `sys`, and `pathlib.Path` (`hooks/check-todos-from-transcript.py:12` through `hooks/check-todos-from-transcript.py:14`). It is called by the stop hook at `hooks/loop-codex-stop-hook.sh:224`; that caller passes the original hook input to Python at `hooks/loop-codex-stop-hook.sh:228`, checks exit code `1` at `hooks/loop-codex-stop-hook.sh:231`, extracts the todo list from stdout at `hooks/loop-codex-stop-hook.sh:234`, renders `prompt-template/block/incomplete-todos.md` at `hooks/loop-codex-stop-hook.sh:241`, and returns a block decision at `hooks/loop-codex-stop-hook.sh:244` through `hooks/loop-codex-stop-hook.sh:252`. The stop hook itself is registered in `hooks/hooks.json:52` through `hooks/hooks.json:59`. The rendered user-facing incomplete-todo instruction lives in `prompt-template/block/incomplete-todos.md:1` through `prompt-template/block/incomplete-todos.md:12`.
- edge_cases_or_failure_modes: Invalid stdin JSON silently permits stop (`hooks/check-todos-from-transcript.py:89` through `hooks/check-todos-from-transcript.py:93`), as does a missing `transcript_path` (`hooks/check-todos-from-transcript.py:95` through `hooks/check-todos-from-transcript.py:97`). A transcript that exists but cannot be opened due to permissions or IO errors is not caught and would raise. Since only non-empty `todos` lists replace `latest_todos`, a final explicit empty todo write would not clear an earlier incomplete list. A todo without `status` is treated as incomplete with an empty status (`hooks/check-todos-from-transcript.py:112` through `hooks/check-todos-from-transcript.py:115`). The script’s docstring says incomplete details go to stderr, but the implementation intentionally prints to stdout to preserve ordering (`hooks/check-todos-from-transcript.py:7`, `hooks/check-todos-from-transcript.py:118` through `hooks/check-todos-from-transcript.py:121`).
- validation_or_tests: I found integration-level wiring but no direct unit test for this Python script. Related validation is through the stop-hook path: `hooks/loop-codex-stop-hook.sh:219` through `hooks/loop-codex-stop-hook.sh:253` documents and implements the quick incomplete-todo gate, and `prompt-template/block/incomplete-todos.md:7` through `prompt-template/block/incomplete-todos.md:10` defines the required action shown to the agent. Template existence coverage in `tests/test-template-references.sh:56` through `tests/test-template-references.sh:110` scans hook template references, which indirectly protects the incomplete-todo rendered response from missing-template failures.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-044 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Prompt block used by RLCR write/edit/bash validators when an operation attempts to modify the loop-local `plan.md` backup. Its algorithmic role is a denial contract: the backup is read-only and exists to preserve plan integrity during an active RLCR loop.
- algorithmic_behavior: The file is a static markdown response template, not executable code. Its content states that the loop directory `plan.md` is a backup, cannot be modified, preserves plan integrity, and should only be read for reference (`prompt-template/block/plan-backup-protected.md:1` through `prompt-template/block/plan-backup-protected.md:7`). The behavior becomes active when validators detect writes/edits/bash commands targeting `.humanize-loop.local/.../plan.md`, then render this block with `load_and_render_safe`.
- inputs_outputs_state: Inputs are the validator-detected target path or bash command and the template loader fallback string. The output is the rendered markdown denial text emitted to stderr by the validators, followed by exit code `2` in PreToolUse validators. No mutable state is stored in the template. The protected state is external: the loop backup plan file at `.humanize-loop.local/.../plan.md`, plus the original plan file compared against it during stop-hook integrity validation.
- gates_or_invariants: The core invariant is that the backup `plan.md` inside `.humanize-loop.local` is immutable during the loop. `hooks/loop-write-validator.sh:96` through `hooks/loop-write-validator.sh:102` blocks Write operations when `IS_PLAN_BACKUP` is true and the path is under `.humanize-loop.local`. `hooks/loop-edit-validator.sh:81` through `hooks/loop-edit-validator.sh:88` blocks Edit operations for a basename of `plan.md` under `.humanize-loop.local`. `hooks/loop-bash-validator.sh:83` through `hooks/loop-bash-validator.sh:87` blocks bash commands that modify `.humanize-loop.local(/round-dir)?/plan.md`. Stop-hook integrity checks separately require the backup to exist (`hooks/loop-codex-stop-hook.sh:151` through `hooks/loop-codex-stop-hook.sh:162`) and require the original plan file to match the backup (`hooks/loop-codex-stop-hook.sh:196` through `hooks/loop-codex-stop-hook.sh:216`).
- dependencies_and_callers: Rendered by `load_and_render_safe` from three validators: bash at `hooks/loop-bash-validator.sh:84` through `hooks/loop-bash-validator.sh:86`, edit at `hooks/loop-edit-validator.sh:84` through `hooks/loop-edit-validator.sh:86`, and write at `hooks/loop-write-validator.sh:98` through `hooks/loop-write-validator.sh:100`. Those validators are registered as PreToolUse hooks in `hooks/hooks.json:14` through `hooks/hooks.json:50`. The same template file is covered by the template reference scanner, which extracts `load_template`, `load_and_render`, and `load_and_render_safe` references at `tests/test-template-references.sh:83` through `tests/test-template-references.sh:110`.
- edge_cases_or_failure_modes: Because this is a static template, failure modes come from caller path detection and template loading. Bash detection uses `command_modifies_file` with a regex for `.humanize-loop.local(/[^/]+)?/plan.md`, and tests explicitly cover direct `.humanize-loop.local/plan.md` paths to avoid a bypass (`tests/test-plan-file-hooks.sh:254` through `tests/test-plan-file-hooks.sh:264`). Write/Edit detection checks for `.humanize-loop.local` in the provided file path, so equivalent paths that avoid that literal string after normalization would depend on earlier validator path handling. If the template is missing, each caller supplies a concise fallback string, so blocking still works but user guidance becomes less specific (`hooks/loop-write-validator.sh:98`, `hooks/loop-edit-validator.sh:84`, `hooks/loop-bash-validator.sh:84`).
- validation_or_tests: Plan-backup blocking is covered in `tests/test-plan-file-hooks.sh`: Write blocking at `tests/test-plan-file-hooks.sh:194` through `tests/test-plan-file-hooks.sh:205`, Edit blocking at `tests/test-plan-file-hooks.sh:211` through `tests/test-plan-file-hooks.sh:222`, Bash redirect blocking at `tests/test-plan-file-hooks.sh:228` through `tests/test-plan-file-hooks.sh:239`, Bash `rm` blocking at `tests/test-plan-file-hooks.sh:241` through `tests/test-plan-file-hooks.sh:252`, and direct `.humanize-loop.local/plan.md` bash blocking at `tests/test-plan-file-hooks.sh:254` through `tests/test-plan-file-hooks.sh:264`. Template reference validation ensures referenced templates exist at `tests/test-template-references.sh:102` through `tests/test-template-references.sh:107`, and verifies critical validators use safe rendering at `tests/test-template-references.sh:172` through `tests/test-template-references.sh:200`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item sections present, one for HZ-014 and one for HZ-044
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`