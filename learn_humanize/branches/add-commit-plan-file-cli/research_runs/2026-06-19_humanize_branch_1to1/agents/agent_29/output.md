# agent_29 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-029 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt template loading/rendering algorithm in `hooks/lib/template-loader.sh`. It verifies path derivation, template file reads, `{{VAR}}` substitution, fallback behavior, directory validation, shell metacharacter safety, and placeholder-injection prevention.
- algorithmic_behavior: The script runs with `set -euo pipefail`, computes `SCRIPT_DIR` and `PROJECT_ROOT`, then sources `hooks/lib/template-loader.sh` before executing numbered assertions with local `pass`/`fail` counters (`tests/test-template-loader.sh:8-33`). Tests 1-14 cover baseline loader behavior: `get_template_dir` must resolve `$PROJECT_ROOT/prompt-template` (`tests/test-template-loader.sh:41-52`), `load_template` must return existing template content and empty output for missing templates (`tests/test-template-loader.sh:54-78`), `render_template` must handle single, multiple, multiline, unused, and unreplaced variables (`tests/test-template-loader.sh:80-144`, `tests/test-template-loader.sh:166-194`), `load_and_render` must integrate a real block template (`tests/test-template-loader.sh:146-164`), `load_and_render_safe` must choose fallback on missing templates but real content on existing templates (`tests/test-template-loader.sh:196-222`), and `validate_template_dir` must accept/reject valid/invalid template directories (`tests/test-template-loader.sh:224-244`).
- inputs_outputs_state: Inputs are the local repository layout, template names such as `block/git-push.md` and `block/wrong-round-number.md`, inline template strings, and `VAR=value` render arguments. Outputs are stdout PASS/FAIL lines plus process exit `0` when `TESTS_FAILED` is zero or `1` otherwise (`tests/test-template-loader.sh:545-563`). The only mutable state is in-process counters `TESTS_PASSED` and `TESTS_FAILED` (`tests/test-template-loader.sh:20-33`); no persistent files are intentionally written.
- gates_or_invariants: Rendering invariant: values must be literal data, not shell syntax. Tests 15-33 assert literal handling for `&`, backslashes, `$`, backticks, pipes, semicolons, globs, parentheses, redirection characters, quotes, comments, tilde, combined shell syntax, regex-like strings, JSON-like content, and multiline values (`tests/test-template-loader.sh:246-486`). Injection invariant: substituted values containing `{{OTHER_VAR}}` must not be rescanned or substituted later, including a realistic `REVIEW_CONTENT` case that references `{{GOAL_TRACKER_FILE}}` and `{{PLAN_FILE}}` (`tests/test-template-loader.sh:488-543`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, sourced at `tests/test-template-loader.sh:12`. The implementation under test defines `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` (`hooks/lib/template-loader.sh:11-207`). Runtime consumers source this through `hooks/lib/loop-common.sh:11-20`. A key caller is `hooks/loop-codex-stop-hook.sh`, which uses `load_and_render_safe` to build next-round prompts and append `claude/next-round-footer.md` (`hooks/loop-codex-stop-hook.sh:891-917`).
- edge_cases_or_failure_modes: Missing template files should warn on stderr but produce empty stdout, allowing callers to fall back (`hooks/lib/template-loader.sh:21-33`, exercised at `tests/test-template-loader.sh:68-78`). Empty or missing rendered content should trigger `load_and_render_safe` fallback (`hooks/lib/template-loader.sh:152-188`). The test also exposes environment sensitivity: `render_template` currently feeds content to `awk` via a here-string at `hooks/lib/template-loader.sh:59-114`; in this read-only sandbox, `bash tests/test-template-loader.sh` failed 31 render-related cases because the shell could not create a temp file for the here document/here-string at line 114. This appears environmental rather than a failing assertion about expected algorithm semantics.
- validation_or_tests: This file is itself the validation suite. I ran `bash tests/test-template-loader.sh`; result was 5 passed, 31 failed. The passing cases were path resolution, template loading existing/missing, and directory validation. The failures all depended on `render_template` and reported `cannot create temp file for here document: Operation not permitted` from `hooks/lib/template-loader.sh:114`, plus one fallback path hit `env_vars[@]: unbound variable` when rendering with no vars. The sandbox result should be recorded as constrained validation, not as a normal repo test result.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-059 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer template that defines the required end-of-round transition contract for the next RLCR worker: do not escape the loop dishonestly, optionally invoke `code-simplifier`, commit changes, and write the round summary to `{{NEXT_SUMMARY_FILE}}`.
- algorithmic_behavior: The template is static markdown with one variable placeholder. It starts with a separator, then states a loop-integrity gate: “MUST NOT try to exit the RLCR loop by lying, editing the loop state file, or executing `cancel-rlcr-loop`” (`prompt-template/claude/next-round-footer.md:2-4`). It then lists completion steps, including a conditional plugin review instruction, committing changes, and writing the summary into `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:6-9`).
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, supplied by the stop hook as `"NEXT_SUMMARY_FILE=$NEXT_SUMMARY_FILE"` (`hooks/loop-codex-stop-hook.sh:913-917`). Output is appended rendered markdown in the next round prompt file. Runtime state affected indirectly: it instructs the next agent to create a commit and write the summary file that the loop expects for handoff, but the template itself does not mutate state.
- gates_or_invariants: Main invariant is RLCR loop integrity: the next worker must not falsify completion, edit loop state to bypass control flow, or run `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:4`). Completion invariant: work is not considered ready until changes are committed and the summary is written to the rendered `NEXT_SUMMARY_FILE` path (`prompt-template/claude/next-round-footer.md:8-9`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` after it creates `NEXT_PROMPT_FILE` and `NEXT_SUMMARY_FILE` for `NEXT_ROUND` (`hooks/loop-codex-stop-hook.sh:887-889`). It is rendered through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-footer.md"` with fallback text if missing (`hooks/loop-codex-stop-hook.sh:913-917`). That safe loader comes from `hooks/lib/template-loader.sh:152-188` and keeps the loop operational even if this template is missing.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the hook falls back to a minimal “Before Exiting” footer that still tells the worker to commit and write the summary, but it omits the stronger anti-bypass language and optional `code-simplifier` step (`hooks/loop-codex-stop-hook.sh:913-917`). If `NEXT_SUMMARY_FILE` is unset or empty at the caller, the rendered `@{{NEXT_SUMMARY_FILE}}` target becomes wrong or empty; the template has no internal validation.
- validation_or_tests: Covered indirectly by `tests/test-template-loader.sh` through `load_and_render_safe` behavior, fallback behavior, and placeholder substitution semantics. The runtime call site demonstrates intended production rendering and append order after the next-round prompt body and before optional push/goal-tracker notes (`hooks/loop-codex-stop-hook.sh:900-933`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 item sections present above, one per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`