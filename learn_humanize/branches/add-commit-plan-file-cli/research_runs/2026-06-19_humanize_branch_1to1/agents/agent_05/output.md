# agent_05 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-005 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts/` is the user-facing setup and monitoring layer for the RLCR loop. It contains `setup-rlcr-loop.sh`, which initializes the loop state machine, `portable-timeout.sh`, which bounds Codex review execution across macOS/Linux, and `humanize.sh`, which monitors loop progress and Codex logs.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` parses `/humanize:start-rlcr-loop` arguments, validates the plan file and repo, creates `.humanize-loop.local/<timestamp>/`, snapshots `plan.md`, writes `state.md`, creates `goal-tracker.md`, and emits the Round 0 prompt (`scripts/setup-rlcr-loop.sh:97`, `:179`, `:220`, `:298`, `:388`, `:406`, `:428`, `:521`). It supports positional or `--plan-file`, `--track-plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--push-every-round` (`scripts/setup-rlcr-loop.sh:36`, `:97`). `scripts/portable-timeout.sh` selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback and exposes `run_with_timeout` (`scripts/portable-timeout.sh:10`, `:33`). `scripts/humanize.sh` implements `humanize monitor rlcr-loop`, locates newest timestamp session, finds newest cached Codex log, parses loop state, goal tracker, and git status, then renders a fixed terminal status bar while tailing logs (`scripts/humanize.sh:9`, `:24`, `:45`, `:99`, `:118`, `:202`, `:263`, `:456`, `:521`).
- inputs_outputs_state: Inputs are a relative markdown plan path, CLI options, current git repo/branch, Codex availability, and optional `CLAUDE_PROJECT_DIR`. Outputs are `.humanize-loop.local/<timestamp>/state.md`, backup `plan.md`, `goal-tracker.md`, `round-0-prompt.md`, and console instructions (`scripts/setup-rlcr-loop.sh:388`, `:397`, `:406`, `:425`, `:521`, `:586`). `state.md` stores `current_round`, `max_iterations`, Codex model/effort/timeout, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` (`scripts/setup-rlcr-loop.sh:406`). The stop hook later consumes these fields to enforce branch and plan immutability and to build review prompts (`hooks/loop-codex-stop-hook.sh:69`, `:74`, `:79`, `:547`). Cache logs live outside the project under `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/`, matching the monitor lookup (`scripts/humanize.sh:43`, `hooks/loop-codex-stop-hook.sh:655`).
- gates_or_invariants: Setup rejects non-git repos, repos without commits, absolute paths, paths with spaces, shell metacharacters, symlink plan files, missing parents, paths outside the project, plan files inside submodules, too-short plans, missing Codex, YAML-unsafe branch/model/effort names, tracked plans unless `--track-plan-file`, and dirty/untracked mismatch with the selected tracking mode (`scripts/setup-rlcr-loop.sh:204`, `:220`, `:227`, `:236`, `:246`, `:265`, `:279`, `:301`, `:332`, `:342`, `:354`, `:368`). The state directory invariant is that only newest `.humanize-loop.local/<timestamp>/state.md` means active loop; completed/cancelled/stopped states are renamed files handled by `end_loop` (`hooks/lib/loop-common.sh:27`, `:199`). `portable-timeout.sh` uses GNU-compatible exit `124` for Python timeout fallback (`scripts/portable-timeout.sh:47`). `humanize.sh` assumes timestamp directory names match `YYYY-MM-DD_HH-MM-SS` and uses pipe-delimited parser output, so unescaped `|` in extracted fields could split display columns (`scripts/humanize.sh:34`, `:113`, `:250`).
- dependencies_and_callers: `commands/start-rlcr-loop.md` invokes `scripts/setup-rlcr-loop.sh` with `$ARGUMENTS` and restricts allowed tools to that script (`commands/start-rlcr-loop.md:4`, `:12`). `hooks/loop-codex-stop-hook.sh` sources `scripts/portable-timeout.sh` before running `codex exec` (`hooks/loop-codex-stop-hook.sh:669`, `:715`). The README documents `scripts/` as setup scripts and the monitor utility (`README.md:213`, `:234`). Hook validators consume setup-created loop state to block invalid reads/writes/bash edits against loop artifacts (`hooks/loop-write-validator.sh:73`, `hooks/loop-read-validator.sh:56`, `hooks/loop-bash-validator.sh:36`, `hooks/loop-plan-file-validator.sh:22`).
- edge_cases_or_failure_modes: The branch export here did not expose usable git metadata under the restricted sandbox, so I did not execute repo-level git validation; source inspection shows setup would fail outside a git repo (`scripts/setup-rlcr-loop.sh:204`). If no timeout implementation exists, Codex review runs without a timeout warning (`scripts/portable-timeout.sh:64`). `humanize.sh` can wait indefinitely for the first Codex log and depends on terminal `tput`, `stat`, `tail`, and cache path conventions (`scripts/humanize.sh:434`, `:447`). Plan goal/AC extraction is heuristic and may leave placeholders for Round 0 initialization (`scripts/setup-rlcr-loop.sh:448`, `:470`). The setup writes state via here-doc, making the earlier path/model/branch character validation important for YAML safety (`scripts/setup-rlcr-loop.sh:406`).
- validation_or_tests: `tests/test-plan-file-validation.sh` covers path rejection, path escaping, non-git/no-commit repo, plan tracking mode, branch validation, CLI option conflicts, and Codex model/effort character validation (`tests/test-plan-file-validation.sh:85`, `:241`, `:270`, `:325`, `:357`, `:391`, `:428`, `:514`, `:547`). `tests/test-state-exit-naming.sh` verifies active-loop detection and `end_loop` rename behavior (`tests/test-state-exit-naming.sh:38`, `:202`). Template loading and cross-reference tests validate dependencies used by the hooks coordinating with setup state (`tests/test-template-references.sh:56`, `tests/test-templates-comprehensive.sh:61`).
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-035 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Specialized block-template note appended when the stop hook detects `.humanize-loop.local/` as an untracked artifact during the git-clean gate. Its role is to prevent users from committing loop-local state while still explaining why git is dirty.
- algorithmic_behavior: The template emits a short special-case instruction that `.humanize-loop.local/` is generated by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2`). It is not rendered with placeholders; it is loaded verbatim by the stop hook when untracked porcelain lines include `.humanize-loop.local` (`hooks/loop-codex-stop-hook.sh:355`, `:358`).
- inputs_outputs_state: Input is implicit: `git status --porcelain` output containing untracked `.humanize-loop.local` (`hooks/loop-codex-stop-hook.sh:350`, `:355`). Output is appended into `SPECIAL_NOTES`, which is then interpolated into the broader `block/git-not-clean.md` block response and returned as a JSON hook block reason (`hooks/loop-codex-stop-hook.sh:363`, `:386`, `:390`). It does not mutate state itself.
- gates_or_invariants: The gate remains “git must be clean before review”; this template only adds remediation context. The invariant is that `.humanize-loop.local/` is private loop state and should stay out of commits (`prompt-template/block/git-not-clean-humanize-local.md:3`). If this template is missing or empty, the hook falls back to a one-line note, so the block still functions (`hooks/loop-codex-stop-hook.sh:359`, `:360`).
- dependencies_and_callers: Caller is `hooks/loop-codex-stop-hook.sh` in the git clean quick-check. The template is loaded via `load_template` from `hooks/lib/template-loader.sh`, which returns empty content and warns on missing files (`hooks/lib/template-loader.sh:21`). Its content is nested inside `block/git-not-clean.md` through the `SPECIAL_NOTES` variable (`hooks/loop-codex-stop-hook.sh:380`, `:386`).
- edge_cases_or_failure_modes: Detection is substring-based over untracked lines using `grep '\.humanize-loop\.local'`, so any untracked path containing that component triggers the note (`hooks/loop-codex-stop-hook.sh:358`). The suggested command appends `.humanize*local*` to `.gitignore`; repeated blocks could encourage duplicate ignore entries if the user reruns it blindly (`prompt-template/block/git-not-clean-humanize-local.md:5`). Since the hook blocks all uncommitted changes, adding `.gitignore` still requires committing that change before Codex review can run.
- validation_or_tests: `tests/test-template-references.sh` scans hook template references and verifies referenced templates exist (`tests/test-template-references.sh:56`, `:93`). `tests/test-templates-comprehensive.sh` loads every template, checks placeholder syntax, fallback behavior, and renders all templates with dummy values (`tests/test-templates-comprehensive.sh:83`, `:114`, `:441`, `:563`).
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-065 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Primary Codex review contract for non-alignment RLCR rounds. It defines what Codex must inspect, how it compares Claude’s claimed work against the original plan and goal tracker, when it writes review feedback, and the exact completion sentinel that terminates the loop.
- algorithmic_behavior: The template tells Codex to read the original plan first via `@{{PLAN_FILE}}`, compare it with `@{{PROMPT_FILE}}` and Claude’s `{{SUMMARY_CONTENT}}`, conduct skeptical implementation review, reject deferrals as incomplete, and produce concrete implementation plans for pending/deferred work (`prompt-template/codex/regular-review.md:5`, `:11`, `:20`). It includes a mandatory goal alignment check against `@{{GOAL_TRACKER_FILE}}` and asks for an `ACs: X/Y addressed` summary (`prompt-template/codex/regular-review.md:33`, `:42`). It injects `{{GOAL_TRACKER_UPDATE_SECTION}}`, which makes Codex responsible for approved goal-tracker updates after Round 0 (`prompt-template/codex/regular-review.md:47`, `prompt-template/codex/goal-tracker-update-section.md:1`). Output rules require writing review comments to `@{{REVIEW_RESULT_FILE}}` when reality does not match claims or work remains; only a final standalone `COMPLETE` line stops Claude (`prompt-template/codex/regular-review.md:49`, `:52`, `:53`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, passed by `loop-codex-stop-hook.sh` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:612`, `:613`). Output is a rendered `round-N-review-prompt.md`, then Codex’s `round-N-review-result.md`; the hook reads the last non-empty line of that result to decide the state transition (`hooks/loop-codex-stop-hook.sh:547`, `:775`, `:828`, `:831`).
- gates_or_invariants: The key invariant is strict completion: no deferred work and no pending work may coexist with `COMPLETE` (`prompt-template/codex/regular-review.md:53`). The hook enforces the sentinel mechanically by requiring the last non-empty trimmed line to be exactly `COMPLETE`; otherwise it increments `current_round`, creates the next prompt, and blocks exit with feedback (`hooks/loop-codex-stop-hook.sh:831`, `:838`, `:882`, `:900`). The regular prompt is used only when `CURRENT_ROUND % 5 != 4`; every fifth review path uses full alignment instead (`hooks/loop-codex-stop-hook.sh:559`, `:594`, `:609`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, with template rendering supplied by `hooks/lib/template-loader.sh` single-pass substitution (`hooks/lib/template-loader.sh:35`, `:39`). It depends on setup-created `state.md`, plan backup, summary file, goal tracker, and prompt file (`scripts/setup-rlcr-loop.sh:406`, `:397`, `:425`, `:521`). It also depends on validators that prevent Claude from directly modifying post-Round 0 goal tracker state, making Codex’s update section authoritative (`hooks/loop-write-validator.sh:109`, `hooks/loop-bash-validator.sh:96`).
- edge_cases_or_failure_modes: If Codex writes output to stdout instead of `REVIEW_RESULT_FILE`, the hook copies stdout into the review file; if neither exists, or the file is empty, exit is blocked with debug-file paths (`hooks/loop-codex-stop-hook.sh:773`, `:791`, `:819`). A phrase such as “CANNOT COMPLETE” does not terminate because only a standalone last line does (`hooks/loop-codex-stop-hook.sh:831`). The template asks Codex to write to `@{{REVIEW_RESULT_FILE}}`, but the hook still tolerates stdout output. If the template is missing, a shorter fallback review prompt is used, reducing the strict detailed review instructions but preserving the review/result/completion shape (`hooks/loop-codex-stop-hook.sh:583`, `:612`).
- validation_or_tests: Template reference and comprehensive template tests cover existence, loadability, placeholder syntax, rendering edge cases, fallback behavior, and all-template renderability (`tests/test-template-references.sh:56`, `tests/test-templates-comprehensive.sh:83`, `:114`, `:261`, `:441`, `:563`). `tests/test-state-exit-naming.sh` validates loop-active state and rename conventions used after `COMPLETE`, `STOP`, max-iteration, cancellation, and unexpected exits (`tests/test-state-exit-naming.sh:38`, `:202`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 Item Evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`