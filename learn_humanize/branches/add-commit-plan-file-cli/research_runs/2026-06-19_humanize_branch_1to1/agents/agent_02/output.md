# agent_02 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-002 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command front door for RLCR loop lifecycle. [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/commands/start-rlcr-loop.md:1) starts the loop by delegating to `scripts/setup-rlcr-loop.sh`; [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/commands/cancel-rlcr-loop.md:1) cancels the newest active loop by renaming its `state.md`.
- algorithmic_behavior: `start-rlcr-loop.md` is a thin command wrapper with one allowed Bash entrypoint, `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS`, at lines 10-14. Its documented algorithm is iterative: Claude implements from a plan, writes a round summary, Codex reviews on exit, Codex feedback either blocks and continues, or `COMPLETE` terminates the loop at lines 16-23. It also defines the goal-tracker protocol: immutable goal/acceptance criteria, mutable active/completed/deferred/evolution sections, and full alignment checks after rounds 4, 9, 14, etc. at lines 24-41.
- inputs_outputs_state: Start input is `<path/to/plan.md> [--max N] [--codex-model MODEL:EFFORT] [--codex-timeout SECONDS] [--push-every-round]` from line 3. The delegated setup script validates the repo and plan file, creates `.humanize-loop.local/<timestamp>/state.md`, backs up `plan.md`, creates `goal-tracker.md`, and writes `round-0-prompt.md` and `round-0-summary.md` paths, with state creation visible in `scripts/setup-rlcr-loop.sh` lines 388-419 and prompt creation at lines 521-572. Cancel input is the current project’s `.humanize-loop.local/*/` directories; output is either “No active RLCR loop found” or `state.md -> cancel-state.md`, preserving the loop artifacts at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/commands/cancel-rlcr-loop.md:11).
- gates_or_invariants: Start exposes only the setup script as an allowed tool at line 4, so the command itself cannot directly mutate arbitrary files. It requires summaries, goal tracker maintenance, detailed implementation/test evidence, no state-file cheating, and no cancel-command escape in lines 43-49. Cancel’s invariant is “newest timestamp directory only” and “active only if `state.md` exists” at lines 11-33, matching `find_active_loop` behavior in `hooks/lib/loop-common.sh` lines 23-44.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, which parses the full option set, validates plan paths and git tracking, records branch/model/push settings, and initializes loop state. The command directory coordinates with hooks registered in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/hooks.json:1): plan-file validator on `UserPromptSubmit`, read/write/edit/bash validators on `PreToolUse`, and `loop-codex-stop-hook.sh` on `Stop`. It also depends on prompt templates under `prompt-template/claude`, `prompt-template/codex`, and `prompt-template/block`.
- edge_cases_or_failure_modes: Start delegates most failures to setup: missing plan, absolute path, spaces, shell metacharacters, symlink, nonexistent parent, outside project, submodule path, git tracking mismatch, too-short plan, missing Codex, unsafe branch/model/effort values. Those gates are in `scripts/setup-rlcr-loop.sh` lines 190-348 and 354-382. Cancel can report no active loop when no loop directory exists or when the newest loop lacks `state.md`, even if older loop directories contain state, because older directories are intentionally ignored.
- validation_or_tests: `tests/test-plan-file-validation.sh` covers setup plan-file validation, including absolute paths, nonexistent files, spaces, metacharacters, symlinks, outside-project paths, tracked/untracked policy, `--plan-file`, and model validation. `tests/test-state-exit-naming.sh` verifies active-loop detection and terminal state rename behavior for complete/cancel/maxiter/stop/unexpected states, especially lines 38-177 and 182-239.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-032 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared hook library for RLCR state-machine validation. It centralizes active-loop discovery, round parsing, protected path classification, template-backed block messages, shell write-detection patterns, and terminal state transitions.
- algorithmic_behavior: The library first resolves its own directory, sources `template-loader.sh`, derives `TEMPLATE_DIR`, and warns rather than hard-failing if templates are unavailable at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/lib/loop-common.sh:11). `find_active_loop` checks only the newest `.humanize-loop.local/*/` directory and returns it only when `state.md` exists, preventing older abnormal-exit directories from being revived at lines 23-44. `get_current_round` extracts YAML frontmatter and returns `current_round`, defaulting to `0` at lines 46-58. `is_round_file_type` and `extract_round_number` classify `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md` files at lines 65-84.
- inputs_outputs_state: Inputs are filesystem paths, lowercase command/path strings, state-file paths, current round values, summary paths, and loop end reasons. Outputs are stdout values for active loop path, current round, extracted round number, and rendered block text; return codes are used for predicates such as `is_goal_tracker_path`, `is_state_file_path`, `is_in_humanize_loop_dir`, and `command_modifies_file`. `end_loop` mutates state by renaming `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` at lines 199-232.
- gates_or_invariants: Only the newest loop directory can be active; only a file literally named `state.md` activates it. Round-file names must end in the exact `round-[0-9]+-{summary|prompt|todos}.md` pattern. State and goal-tracker path checks are suffix based. `end_loop` rejects unknown reasons and refuses to succeed if the state file is missing. `command_modifies_file` defines the Bash bypass-detection invariant: redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >` patterns are treated as writes at lines 155-183.
- dependencies_and_callers: Depends directly on [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/lib/template-loader.sh:1), specifically `get_template_dir`, `validate_template_dir`, and `load_and_render_safe`. Called by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh`, all of which source it near their startup. The hook registration is in `hooks/hooks.json` lines 4-62.
- edge_cases_or_failure_modes: Template directory failure only emits a warning and relies on inline fallbacks, so hooks can still block unsafe actions even if templates are missing. `find_active_loop` uses lexicographic sort of timestamp-named directories; incorrectly named directories could affect “newest” selection. `get_current_round` defaults missing or unparsable current round to `0`, which is permissive for validators but the stop hook later treats nonnumeric `current_round` as corrupted and ends unexpectedly. Bash write detection is regex based and can miss complex shell constructs or quoted/escaped forms, while also possibly matching benign commands that contain write-like substrings.
- validation_or_tests: `tests/test-state-exit-naming.sh` directly sources this file and validates active-loop detection and `end_loop` behavior at lines 64-177 and 182-239. `tests/test-template-references.sh` explicitly checks `loop-common.sh` template references and required common templates at lines 149-167. `tests/test-templates-comprehensive.sh` covers template loader behavior, including directory validation and real-template rendering at lines 480-606.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-062 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Claude prompt fragment that enforces the `--push-every-round` transition contract by adding an explicit push requirement to generated round prompts.
- algorithmic_behavior: The file is intentionally minimal: line 2 tells Claude that when `--push-every-round` is enabled, commits must be pushed to remote after each round. It does not parse state itself; it is loaded by the stop hook only when state frontmatter has `push_every_round: true`.
- inputs_outputs_state: Input is the boolean `PUSH_EVERY_ROUND` parsed from `state.md` by `hooks/loop-codex-stop-hook.sh` lines 79-94. Output is appended text in the next-round prompt at `loop-codex-stop-hook.sh` lines 919-926. The same instruction is also inlined for round 0 by `scripts/setup-rlcr-loop.sh` lines 574-580 when the loop is first created.
- gates_or_invariants: The template supports a hard workflow gate elsewhere: when `push_every_round` is false, `loop-bash-validator.sh` blocks `git push` at lines 53-64; when true, `loop-codex-stop-hook.sh` checks for unpushed commits with `git status -sb` and blocks exit if the branch is ahead of remote at lines 401-431. This template is therefore the user-facing instruction paired with that state gate.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` in `hooks/loop-codex-stop-hook.sh` line 921, with fallback `"Also push your changes after committing."` at lines 922-924. It depends on `template-loader.sh` resolution through `loop-common.sh`, not on variable substitution.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook appends the fallback push instruction. The template itself does not mention how to handle no upstream branch, detached HEAD, push failures, or remote divergence; the enforcement code only detects local ahead status, so other remote sync states would depend on git behavior outside this template.
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`, which scans hook template references and all prompt-template files. General load/render coverage for all templates is in `tests/test-templates-comprehensive.sh` lines 562-606. There is specific rendering coverage for `block/unpushed-commits.md` at lines 516-528, which validates the paired block message but not this note directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item evidence headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`