# agent_22 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell utility exposing the `humanize monitor rlcr-loop` command. Its algorithmic role is observability for an active RLCR loop: locate the newest loop session, find the latest Codex run log in the external cache, parse loop state and goal-tracker summaries, parse repository git status, and render a live terminal dashboard with log tailing.
- algorithmic_behavior: The top-level `humanize()` dispatcher reads the first argument and routes only `monitor rlcr-loop` to `_humanize_monitor_codex`; all other commands print usage and return failure at `scripts/humanize.sh:521` through `scripts/humanize.sh:553`. `_humanize_monitor_codex` starts by requiring `.humanize-loop.local` in the current directory, then searches timestamp-named session directories with pattern `YYYY-MM-DD_HH-MM-SS` at `scripts/humanize.sh:16` through `scripts/humanize.sh:41`. It maps the current project path to a sanitized cache directory under `$HOME/.cache/humanize` and selects the latest `round-*-codex-run.log` by newest session timestamp and highest round number at `scripts/humanize.sh:43` through `scripts/humanize.sh:96`. It repeatedly redraws a fixed status bar, prints the last 50 lines of the current log, streams appended bytes using file-size deltas, and switches to newer logs when detected at `scripts/humanize.sh:456` through `scripts/humanize.sh:513`.
- inputs_outputs_state: Inputs are the current working directory, `.humanize-loop.local/<timestamp>/state.md`, `.humanize-loop.local/<timestamp>/goal-tracker.md`, `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/round-N-codex-run.log`, terminal dimensions from `tput`, and the current git repository status. Outputs are terminal control sequences, dashboard text, streamed log content, usage/error messages, and process exit status. It does not mutate loop state files; runtime state is only local shell variables such as `current_file`, `current_session_dir`, `last_size`, `monitor_running`, and `tail_pid`.
- gates_or_invariants: Requires `.humanize-loop.local` to exist before monitoring; otherwise it returns `1` with a project-context error at `scripts/humanize.sh:16` through `scripts/humanize.sh:21`. Session directories must match the timestamp regex before being considered at `scripts/humanize.sh:33` through `scripts/humanize.sh:38` and `scripts/humanize.sh:64` through `scripts/humanize.sh:67`. Cache log discovery is scoped to sessions that also exist locally, preventing unrelated cache logs from another project/session from being selected at `scripts/humanize.sh:57` through `scripts/humanize.sh:73`. The terminal layout invariant is a 10-line fixed status area and a scroll region below it at `scripts/humanize.sh:13`, `scripts/humanize.sh:335` through `scripts/humanize.sh:388`.
- dependencies_and_callers: Documented as a user-facing shell utility in `README.md:88` through `README.md:110`, where users source `scripts/humanize.sh` and run `humanize monitor rlcr-loop`. The README identifies the loop state files and external cache layout at `README.md:242` through `README.md:265`, matching the script’s expected inputs. The cache files consumed by this monitor are written by `hooks/loop-codex-stop-hook.sh`, which places Codex command/stdout/stderr files under `$HOME/.cache/humanize/<project-path>/<timestamp>/` at `hooks/loop-codex-stop-hook.sh:655` through `hooks/loop-codex-stop-hook.sh:667`. State and goal-tracker files are created by `scripts/setup-rlcr-loop.sh`, including `goal-tracker.md` and the initial round prompt/summary path at `scripts/setup-rlcr-loop.sh:425` through `scripts/setup-rlcr-loop.sh:572`. External command dependencies include `basename`, `sed`, `grep`, `sort`, `wc`, `git`, `tput`, `tail`, `stat`, `seq`, `sleep`, and shell support for Bash/Zsh-local behavior.
- edge_cases_or_failure_modes: If no log exists initially, it prints a waiting message and polls until one appears or monitoring is interrupted at `scripts/humanize.sh:434` through `scripts/humanize.sh:441`. If `state.md` is missing, `_parse_state_md` returns `N/A` fields rather than failing at `scripts/humanize.sh:98` through `scripts/humanize.sh:114`. If `goal-tracker.md` is missing, `_parse_goal_tracker` returns zero counts and `No goal tracker` at `scripts/humanize.sh:116` through `scripts/humanize.sh:123`. If not in a git repo, `_parse_git_status` reports zero changes and `not a git repo` at `scripts/humanize.sh:200` through `scripts/humanize.sh:207`. Cross-platform file sizing falls back from GNU `stat -c%s` to macOS `stat -f%z` at `scripts/humanize.sh:447` through `scripts/humanize.sh:450`. Potential fragility: the status bar loop assumes `tput` works and the terminal has enough lines; the code does not explicitly guard non-interactive terminals. It also parses markdown tables with regexes, so major goal-tracker format drift can undercount tasks/ACs without a hard error.
- validation_or_tests: No test file directly targets `scripts/humanize.sh` in the inspected tree. Its data contracts are indirectly supported by setup/hook tests around state naming and loop files, especially `tests/test-state-exit-naming.sh`, `tests/test-plan-file-hooks.sh`, and plan-file validation tests referenced by `rg`. Direct runtime validation would require an interactive terminal plus fixture `.humanize-loop.local` and cache log files. In this read-only branch export I inspected behavior only; I did not execute the monitor because it is an interactive infinite/polling command.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-052 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Prompt/block template used as a stop-hook guard response when a worker/Claude attempts to exit an active RLCR loop without writing the required round summary file. It defines the human-readable block reason for a required workflow transition: exit is denied until `round-N-summary.md` exists.
- algorithmic_behavior: The template is static markdown with one substitution placeholder, `{{SUMMARY_FILE}}`, at `prompt-template/block/work-summary-missing.md:5` through `prompt-template/block/work-summary-missing.md:8`. It instructs the agent to write a summary covering implementation, changed files, tests, and remaining items at `prompt-template/block/work-summary-missing.md:10` through `prompt-template/block/work-summary-missing.md:14`, then retry exit at `prompt-template/block/work-summary-missing.md:16`. It does not compute state by itself; it is rendered by the template loader and embedded into a JSON hook response.
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable supplied by the caller. Output is rendered markdown text used as the hook `reason`. It has no persistent state and performs no file IO directly. The caller computes `SUMMARY_FILE="$LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` at `hooks/loop-codex-stop-hook.sh:438`, then passes `SUMMARY_FILE=$SUMMARY_FILE` into `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:447` through `hooks/loop-codex-stop-hook.sh:448`.
- gates_or_invariants: The guard condition is file existence: if the computed round summary file is absent, the stop hook blocks exit at `hooks/loop-codex-stop-hook.sh:440` through `hooks/loop-codex-stop-hook.sh:459`. The rendered message is wrapped in a JSON object with `"decision": "block"` and system message `Loop: Summary file missing for round $CURRENT_ROUND` at `hooks/loop-codex-stop-hook.sh:450` through `hooks/loop-codex-stop-hook.sh:457`. The template invariant is that `{{SUMMARY_FILE}}` must remain present so the agent is pointed to the exact required path.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe`. The template loader resolves the template root relative to the hook library at `hooks/lib/template-loader.sh:9` through `hooks/lib/template-loader.sh:16`, loads files at `hooks/lib/template-loader.sh:21` through `hooks/lib/template-loader.sh:33`, and performs single-pass `{{VAR}}` substitution to avoid placeholder injection at `hooks/lib/template-loader.sh:35` through `hooks/lib/template-loader.sh:117`. `load_and_render_safe` provides fallback rendering if the template is missing or empty at `hooks/lib/template-loader.sh:152` through `hooks/lib/template-loader.sh:188`. The expected summary file is also part of the initial loop prompt generated by `scripts/setup-rlcr-loop.sh:519` through `scripts/setup-rlcr-loop.sh:572`.
- edge_cases_or_failure_modes: If this template is missing or renders empty, the stop hook falls back to a shorter inline message defined at `hooks/loop-codex-stop-hook.sh:444` through `hooks/loop-codex-stop-hook.sh:446`. If `SUMMARY_FILE` contains unusual characters, rendering is still single-pass and does not recursively substitute placeholder-like text, because the loader writes values directly to output without rescanning at `hooks/lib/template-loader.sh:39` through `hooks/lib/template-loader.sh:42` and `hooks/lib/template-loader.sh:100` through `hooks/lib/template-loader.sh:107`. If the loop state has an incorrect `CURRENT_ROUND`, this template will accurately point to the computed but possibly wrong file; numeric validation of `CURRENT_ROUND` happens earlier in the stop hook at `hooks/loop-codex-stop-hook.sh:96` through `hooks/loop-codex-stop-hook.sh:105`.
- validation_or_tests: Template reference coverage is present through `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls for referenced templates at `tests/test-template-references.sh:83` through `tests/test-template-references.sh:99`, and checks critical scripts use safe rendering at `tests/test-template-references.sh:172` through `tests/test-template-references.sh:199`. Template-loader behavior is covered by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`, including safe fallback and real-template rendering patterns. I found no dedicated assertion for this exact template’s prose, only reference/existence and loader-path coverage.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item sections present, matching the assigned table order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`