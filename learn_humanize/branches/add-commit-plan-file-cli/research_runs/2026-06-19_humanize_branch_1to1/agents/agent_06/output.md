# agent_06 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the branch’s behavioral safety net for the RLCR loop algorithm. It validates setup-time plan-file admission, active-loop state lifecycle, hook-time plan immutability, git cleanliness gates, template rendering, and prompt-template inventory. The directory contains seven shell test suites totaling 96,907 bytes:
  - `tests/test-plan-file-validation.sh`
  - `tests/test-plan-file-hooks.sh`
  - `tests/test-state-exit-naming.sh`
  - `tests/test-template-loader.sh`
  - `tests/test-templates-comprehensive.sh`
  - `tests/test-template-references.sh`
  - `tests/test-error-scenarios.sh`
- algorithmic_behavior: The directory coordinates black-box and library-level tests around the loop’s state machine and prompt-generation contracts. `test-plan-file-validation.sh` drives `scripts/setup-rlcr-loop.sh` and asserts admission gates before a loop can start: relative plan paths only, existing parent directory/file, no spaces, no shell metacharacters, no symlinks, no project escape, git repo with at least one commit, tracking-mode consistency, YAML-safe branch/model/effort values, and `--plan-file` CLI parsing (`tests/test-plan-file-validation.sh:85`, `tests/test-plan-file-validation.sh:100`, `tests/test-plan-file-validation.sh:124`, `tests/test-plan-file-validation.sh:165`, `tests/test-plan-file-validation.sh:185`, `tests/test-plan-file-validation.sh:241`, `tests/test-plan-file-validation.sh:270`, `tests/test-plan-file-validation.sh:325`, `tests/test-plan-file-validation.sh:357`, `tests/test-plan-file-validation.sh:391`, `tests/test-plan-file-validation.sh:428`, `tests/test-plan-file-validation.sh:514`, `tests/test-plan-file-validation.sh:547`). These map directly to setup validation in `scripts/setup-rlcr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:226`, `scripts/setup-rlcr-loop.sh:234`, `scripts/setup-rlcr-loop.sh:246`, `scripts/setup-rlcr-loop.sh:252`, `scripts/setup-rlcr-loop.sh:265`, `scripts/setup-rlcr-loop.sh:298`, `scripts/setup-rlcr-loop.sh:356`, and `scripts/setup-rlcr-loop.sh:366`.
- algorithmic_behavior: `test-plan-file-hooks.sh` creates temporary repos and `.humanize-loop.local/<timestamp>/` state to exercise active hook enforcement during a loop. It verifies `hooks/loop-plan-file-validator.sh` accepts valid state, strips YAML quotes, blocks missing `plan_tracked`/`start_branch`, blocks branch changes, and preserves plan-file tracking invariants (`tests/test-plan-file-hooks.sh:91`, `tests/test-plan-file-hooks.sh:106`, `tests/test-plan-file-hooks.sh:121`, `tests/test-plan-file-hooks.sh:141`, `tests/test-plan-file-hooks.sh:164`). It then exercises PreToolUse hook guards: Write/Edit block the loop plan backup, Bash blocks plan backup modification/removal, and Bash pattern matching blocks bypasses via direct `.humanize-loop.local/plan.md`, command substitution, globs, brace expansion, pipes, and backticks (`tests/test-plan-file-hooks.sh:194`, `tests/test-plan-file-hooks.sh:211`, `tests/test-plan-file-hooks.sh:228`, `tests/test-plan-file-hooks.sh:241`, `tests/test-plan-file-hooks.sh:254`, `tests/test-plan-file-hooks.sh:272`, `tests/test-plan-file-hooks.sh:285`, `tests/test-plan-file-hooks.sh:298`, `tests/test-plan-file-hooks.sh:311`, `tests/test-plan-file-hooks.sh:324`). These assert `hooks/loop-bash-validator.sh:83` and `hooks/lib/loop-common.sh:158` resist command-based state/plan mutation.
- algorithmic_behavior: `test-plan-file-hooks.sh` also tests stop-hook plan integrity and state-schema behavior. It blocks when the project plan diverges from the backup, is deleted, the backup is missing, tracked plans have uncommitted changes, old state lacks required fields, and tracked plans are changed then committed so git status is clean but content differs (`tests/test-plan-file-hooks.sh:469`, `tests/test-plan-file-hooks.sh:509`, `tests/test-plan-file-hooks.sh:543`, `tests/test-plan-file-hooks.sh:562`, `tests/test-plan-file-hooks.sh:631`, `tests/test-plan-file-hooks.sh:655`). This maps to `hooks/loop-codex-stop-hook.sh:112`, `hooks/loop-codex-stop-hook.sh:148`, `hooks/loop-codex-stop-hook.sh:164`, `hooks/loop-codex-stop-hook.sh:180`, and `hooks/loop-codex-stop-hook.sh:196`.
- algorithmic_behavior: `test-state-exit-naming.sh` verifies the state-machine convention that only `state.md` marks an active loop, while terminal states are preserved as `<reason>-state.md` and ignored by active-loop detection (`tests/test-state-exit-naming.sh:38`, `tests/test-state-exit-naming.sh:74`, `tests/test-state-exit-naming.sh:93`, `tests/test-state-exit-naming.sh:110`, `tests/test-state-exit-naming.sh:125`, `tests/test-state-exit-naming.sh:141`). It also verifies newest active timestamp precedence and `end_loop` validation for allowed terminal reasons `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`tests/test-state-exit-naming.sh:157`, `tests/test-state-exit-naming.sh:182`, `tests/test-state-exit-naming.sh:202`, `tests/test-state-exit-naming.sh:228`). These assert `hooks/lib/loop-common.sh:27` and `hooks/lib/loop-common.sh:206`.
- algorithmic_behavior: `test-template-loader.sh` validates `hooks/lib/template-loader.sh` as the rendering engine for hook block messages and Codex/Claude prompts. It checks template root discovery, loading existing files, safe empty return for missing files, simple/multiple/multiline substitution, literal special characters, fallback rendering, directory validation, single-pass substitution, and placeholder-injection prevention (`tests/test-template-loader.sh:41`, `tests/test-template-loader.sh:54`, `tests/test-template-loader.sh:67`, `tests/test-template-loader.sh:80`, `tests/test-template-loader.sh:95`, `tests/test-template-loader.sh:110`, `tests/test-template-loader.sh:131`, `tests/test-template-loader.sh:196`, `tests/test-template-loader.sh:224`, `tests/test-template-loader.sh:246`, `tests/test-template-loader.sh:488`). These target `hooks/lib/template-loader.sh:11`, `hooks/lib/template-loader.sh:21`, `hooks/lib/template-loader.sh:43`, `hooks/lib/template-loader.sh:121`, `hooks/lib/template-loader.sh:155`, and `hooks/lib/template-loader.sh:193`.
- algorithmic_behavior: `test-templates-comprehensive.sh` recursively loads every `prompt-template/**/*.md`, validates required subdirectories `block`, `codex`, and `claude`, checks placeholder syntax, stress-renders templates with dummy values, verifies malformed placeholder detection, exercises rendering edge cases, and tests representative real templates (`tests/test-templates-comprehensive.sh:61`, `tests/test-templates-comprehensive.sh:82`, `tests/test-templates-comprehensive.sh:113`, `tests/test-templates-comprehensive.sh:196`, `tests/test-templates-comprehensive.sh:261`, `tests/test-templates-comprehensive.sh:440`, `tests/test-templates-comprehensive.sh:492`, `tests/test-templates-comprehensive.sh:542`). It protects the prompt contract used by `hooks/loop-codex-stop-hook.sh:556`, `hooks/loop-codex-stop-hook.sh:596`, `hooks/loop-codex-stop-hook.sh:612`, `hooks/loop-codex-stop-hook.sh:900`, and `hooks/loop-codex-stop-hook.sh:916`.
- algorithmic_behavior: `test-template-references.sh` scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails if a referenced template is missing (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:74`, `tests/test-template-references.sh:83`). It separately checks common message templates and enforces safe fallback usage for critical validators (`tests/test-template-references.sh:145`, `tests/test-template-references.sh:170`). This prevents a core failure mode where a blocking validator returns an empty message instead of actionable feedback.
- algorithmic_behavior: `test-error-scenarios.sh` is a manual/diagnostic safety script for template-loader failure modes. It documents that missing template files/directories, missing `load_and_render` targets, and empty content return empty strings without crashing, including under `set -euo pipefail` (`tests/test-error-scenarios.sh:21`, `tests/test-error-scenarios.sh:34`, `tests/test-error-scenarios.sh:47`, `tests/test-error-scenarios.sh:60`, `tests/test-error-scenarios.sh:86`). Its summary explicitly flags the risk of empty hook feedback (`tests/test-error-scenarios.sh:114`), which is why the other tests enforce `load_and_render_safe`.
- inputs_outputs_state: Inputs are temporary git repositories, generated plan files, `.gitignore`, `.humanize-loop.local/<timestamp>/state.md`, `plan.md` backups, hook JSON payloads for `Write`, `Edit`, and `Bash`, shell command strings, prompt templates, and mocked `codex` binaries when needed (`tests/test-plan-file-validation.sh:69`, `tests/test-plan-file-hooks.sh:37`, `tests/test-plan-file-hooks.sh:196`, `tests/test-template-loader.sh:12`). Outputs are shell exit statuses, stdout/stderr text, JSON hook block payloads, filesystem artifacts under temporary loop dirs, renamed terminal state files, and pass/fail counters. State transitions covered include no loop to active loop via `state.md`, active loop to terminal `<reason>-state.md`, current-branch consistency checks, plan backup consistency checks, and current-round based validation of summary/goal-tracker behavior.
- gates_or_invariants: Core invariants under test are: plan path must be repository-relative and shell/YAML safe; plan files cannot be symlinks or escape the project; plan tracking mode cannot drift; tracked plans must remain clean and content-identical to the backup; untracked plans must stay untracked unless `--track-plan-file` was used; `state.md` is the only active-loop sentinel; terminal state files are never active; required state fields `plan_tracked` and `start_branch` must exist; branch switching blocks progress; plan backup cannot be modified through Write/Edit/Bash; summary files must target the current round; Bash cannot bypass tool validators; prompt templates must exist or have safe fallbacks; template rendering must be literal and single-pass.
- dependencies_and_callers: The tests call production scripts and hooks directly: `scripts/setup-rlcr-loop.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/lib/loop-common.sh`, and `hooks/lib/template-loader.sh`. They rely on `git`, `jq`, `sed`, `grep`, `awk`, `wc`, `mktemp`, `chmod`, and `python3` indirectly for todo checking through the stop hook. They coordinate with sibling `prompt-template` by validating template presence, syntax, rendering, and hook references.
- edge_cases_or_failure_modes: Covered edge cases include absolute/nonexistent paths, missing parent directories, spaces, shell metacharacters, symlinks, unreadable directories, `../` project escape, non-git repos, repos without commits, tracked/untracked tracking-mode mismatches, dirty tracked plans, committed plan changes after backup, YAML-unsafe branch/model/effort strings, quoted YAML state fields, hyphenated plan paths, direct `.humanize-loop.local/plan.md` paths, shell expansion bypass attempts, missing templates, malformed placeholders, CJK/markdown/long/multiline values, repeated placeholders, adjacent placeholders, and placeholder injection through values.
- validation_or_tests: The directory is itself validation. I inspected all seven files recursively without a depth limit and traced their references into `hooks`, `scripts`, and `prompt-template`. I did not run the suites because this worker run requested research notes only and the branch export is read-only; several tests intentionally create files in temporary directories and invoke hooks, but no repository files would be modified by normal execution.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-036 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: This prompt block is an algorithmic sub-message used by the stop-hook git-clean gate when the working tree contains untracked files other than `.humanize-loop.local`. It teaches the blocked agent to classify likely generated artifacts separately from real source changes, steering toward `.gitignore` instead of blindly committing everything.
- algorithmic_behavior: The file is a static Markdown template with no placeholders. It starts with a note on untracked files (`prompt-template/block/git-not-clean-untracked.md:2`), explains that some untracked files may be build artifacts, test outputs, or runtime-generated files (`prompt-template/block/git-not-clean-untracked.md:3`), recommends `.gitignore` rather than committing them (`prompt-template/block/git-not-clean-untracked.md:4`), lists artifact categories such as build outputs, dependencies, IDE files, logs, caches, and temporary files (`prompt-template/block/git-not-clean-untracked.md:5` through `prompt-template/block/git-not-clean-untracked.md:8`), and ends by instructing review and `.gitignore` updates (`prompt-template/block/git-not-clean-untracked.md:10`).
- algorithmic_behavior: Runtime use occurs in `hooks/loop-codex-stop-hook.sh` during “Git Clean and Pushed?” quick-checks. The hook collects `git status --porcelain`, extracts untracked files with `grep '^??'`, excludes `.humanize-loop.local`, and if any other untracked entries remain, loads this template with `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` (`hooks/loop-codex-stop-hook.sh:349`, `hooks/loop-codex-stop-hook.sh:355`, `hooks/loop-codex-stop-hook.sh:367`, `hooks/loop-codex-stop-hook.sh:369`). The loaded text is appended to `SPECIAL_NOTES`, which is rendered into the broader `block/git-not-clean.md` response as `{{SPECIAL_NOTES}}` (`hooks/loop-codex-stop-hook.sh:373`, `hooks/loop-codex-stop-hook.sh:386`). If the template is missing or empty, the hook uses inline fallback text: “Review untracked files - add to .gitignore or commit them.” (`hooks/loop-codex-stop-hook.sh:370`).
- inputs_outputs_state: Inputs are untracked-file lines from `git status --porcelain` and the presence/readability of this template file. The template itself has no parameters and therefore no rendering state. Output is appended Markdown inside the JSON `reason` returned by the stop hook’s block decision. The state transition is not filesystem mutation; it changes the stop attempt from allowed review execution to a blocked hook response whenever uncommitted changes are present. The message is an explanatory supplement within the `Git Not Clean` block, not the primary gate condition.
- gates_or_invariants: The invariant supported by this template is that an RLCR loop stop attempt cannot proceed while the git worktree is dirty. For untracked files, the guidance distinguishes artifacts from commit-worthy changes. Combined with `prompt-template/block/git-not-clean.md`, it reinforces required actions: review untracked files, stage real changes, commit, avoid AI authorship/co-author metadata, and retry exit (`prompt-template/block/git-not-clean.md:5` through `prompt-template/block/git-not-clean.md:16`). The related `.humanize-loop.local` special case is handled by a sibling template instead (`prompt-template/block/git-not-clean-humanize-local.md:2` through `prompt-template/block/git-not-clean-humanize-local.md:7`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:369`. It depends on `hooks/lib/template-loader.sh:21` for `load_template`, which returns empty plus a warning if a file is missing. It is indirectly covered by template inventory tests: `tests/test-templates-comprehensive.sh` loads every `prompt-template/**/*.md` and validates placeholder syntax (`tests/test-templates-comprehensive.sh:82`, `tests/test-templates-comprehensive.sh:113`), while `tests/test-template-references.sh` scans hook references and fails on missing templates (`tests/test-template-references.sh:74`, `tests/test-template-references.sh:83`, `tests/test-template-references.sh:102`).
- edge_cases_or_failure_modes: If this file is missing, the stop hook falls back to a shorter inline note, so the gate still blocks but loses detailed artifact categorization. If both `.humanize-loop.local` and other untracked files exist, this note is concatenated after the `.humanize-loop.local` note without an inserted separator in hook code, relying on template leading/trailing blank lines for readable Markdown. Because the template has no placeholders, it cannot suffer variable substitution failures or placeholder injection, but broad wording means it does not inspect actual untracked filenames or decide whether each file should be ignored versus committed.
- validation_or_tests: This file is validated structurally by `tests/test-templates-comprehensive.sh`, which loads all Markdown templates and checks placeholder syntax. It is validated by reference existence through `tests/test-template-references.sh`, which extracts `load_template`/`load_and_render*` references from hooks and verifies referenced files exist. There is no dedicated behavioral test asserting the exact stop-hook output for this specific untracked-note branch.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 Item Evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`