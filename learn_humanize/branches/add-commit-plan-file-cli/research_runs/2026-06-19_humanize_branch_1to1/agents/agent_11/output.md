# agent_11 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-011 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex review prompt template directory for the RLCR loop. It defines the reviewer-side control prompts that evaluate Claude’s implementation against the original plan, the round prompt, goal tracker state, docs, and prior iteration history. The directory has three direct file descendants and no deeper descendants found by recursive inspection: `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`.
- algorithmic_behavior: `prompt-template/codex/regular-review.md:1-57` is the normal per-round review contract. It forces Codex to read the original plan first, compare Claude’s summary against both `{{PLAN_FILE}}` and `{{PROMPT_FILE}}`, inspect docs under `{{DOCS_PATH}}`, reject deferrals as incomplete, perform a goal alignment check, and write review findings unless every original task and acceptance criterion is complete. `prompt-template/codex/full-alignment-review.md:1-93` is the periodic comprehensive audit template. It adds acceptance-criteria status tabulation, forgotten-item detection, deferred-item audit, goal completion summary, and stagnation/circuit-breaker analysis over the last five rounds. `prompt-template/codex/goal-tracker-update-section.md:1-17` is an injectable subsection used by both review templates; it delegates post-Round-0 goal-tracker mutations to Codex, while preserving the immutable goal and acceptance-criteria section.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:596-607`, `hooks/loop-codex-stop-hook.sh:612-624`). Output is a rendered review prompt written to `$LOOP_DIR/round-${CURRENT_ROUND}-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:547-549`, `hooks/loop-codex-stop-hook.sh:596-607`, `hooks/loop-codex-stop-hook.sh:612-624`). The Codex process then writes or emits review content for `$REVIEW_RESULT_FILE`; stdout is copied into that file if Codex did not write it directly (`hooks/loop-codex-stop-hook.sh:773-789`). State transitions are driven by the last non-empty review-result line: `COMPLETE` ends the loop with complete state, `STOP` ends with stop state, and any other nonempty review advances `current_round` and generates the next prompt (`hooks/loop-codex-stop-hook.sh:831-885`).
- gates_or_invariants: Normal review invariant: `COMPLETE` is allowed only when all original plan tasks are complete, all acceptance criteria are met, and no deferred or unfinished work remains (`prompt-template/codex/regular-review.md:51-57`). Full-alignment invariant: `COMPLETE` is allowed only when all acceptance criteria are fully met with no deferrals, while detected stagnation must end with `STOP` (`prompt-template/codex/full-alignment-review.md:84-93`). Goal-tracker update invariant: Codex may update mutable sections, but must never modify Ultimate Goal or Acceptance Criteria after Round 0 (`prompt-template/codex/goal-tracker-update-section.md:3-11`). Scheduling gate: full alignment is selected every fifth round where `CURRENT_ROUND % 5 == 4`; otherwise the regular review template is used (`hooks/loop-codex-stop-hook.sh:559-563`, `hooks/loop-codex-stop-hook.sh:594-624`). Review-result gate requires the result file to exist and be nonempty, otherwise the hook blocks with Codex failure diagnostics (`hooks/loop-codex-stop-hook.sh:791-826`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, which builds `GOAL_TRACKER_UPDATE_SECTION` from `prompt-template/codex/goal-tracker-update-section.md`, then embeds that rendered section into either `full-alignment-review.md` or `regular-review.md` (`hooks/loop-codex-stop-hook.sh:553-557`, `hooks/loop-codex-stop-hook.sh:596-624`). Rendering is performed through `hooks/lib/template-loader.sh`, whose `load_and_render_safe` loads a template, substitutes variables, and falls back if missing or empty (`hooks/lib/template-loader.sh:155-188`). Template substitution is intentionally single-pass to prevent placeholder injection from values like `SUMMARY_CONTENT` or review text (`hooks/lib/template-loader.sh:35-43`, `hooks/lib/template-loader.sh:56-116`). The templates coordinate with sibling block templates through the same loader and template root, and with Claude-facing templates via the next-round prompt generated after review findings (`hooks/loop-codex-stop-hook.sh:891-903`).
- edge_cases_or_failure_modes: If a Codex template is missing or renders empty, the hook uses fallback prompt strings, reducing review specificity but preserving loop operation (`hooks/loop-codex-stop-hook.sh:572-592`, `hooks/lib/template-loader.sh:155-188`). If a variable is absent, the loader leaves the placeholder unchanged rather than failing (`hooks/lib/template-loader.sh:100-107`), which can leak unresolved `{{...}}` into prompts and weaken instruction clarity. Because rendering is single-pass, nested placeholders inside `SUMMARY_CONTENT` are not expanded, which is intentional injection resistance but may surprise template authors (`hooks/lib/template-loader.sh:39-42`). If Codex writes `STOP` during a non-full-alignment round, the hook still honors it as a stop transition, but logs it as unusual (`hooks/loop-codex-stop-hook.sh:848-875`). If Codex exits nonzero, does not create a result, or creates an empty result, the loop blocks rather than advancing (`hooks/loop-codex-stop-hook.sh:754-826`).
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` directly validates rendering of `codex/goal-tracker-update-section.md` with a concrete `GOAL_TRACKER_FILE`. Broader template loader behavior is exercised in `tests/test-template-loader.sh` references found for placeholder preservation and rendering, including goal-tracker variables. No direct test snippet was found for complete rendering of `full-alignment-review.md` or `regular-review.md`, so their validation appears mostly through hook integration and loader coverage rather than file-specific assertions.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-041 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Round 0 blocking/remediation template for an uninitialized goal tracker. It is a guard message, not an implementation prompt, and exists to prevent the loop from proceeding until the persistent goal anchor has real content.
- algorithmic_behavior: The template tells the agent that it is in Round 0 and the goal tracker has not been properly initialized (`prompt-template/block/goal-tracker-not-initialized.md:1-3`). It renders the specific missing goal-tracker sections through `{{MISSING_ITEMS}}` (`prompt-template/block/goal-tracker-not-initialized.md:5-6`). It then prescribes the recovery algorithm: read `{{GOAL_TRACKER_FILE}}`, replace placeholder text with actual Ultimate Goal content, define 3-7 testable Acceptance Criteria, populate Active Tasks with plan-derived tasks mapped to acceptance criteria, and write the updated goal tracker (`prompt-template/block/goal-tracker-not-initialized.md:8-15`). It closes with the critical Round 0 invariant that the immutable section can only be set during Round 0 (`prompt-template/block/goal-tracker-not-initialized.md:16-18`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `MISSING_ITEMS`, supplied by `hooks/loop-codex-stop-hook.sh` when it detects placeholder text in `$LOOP_DIR/goal-tracker.md` (`hooks/loop-codex-stop-hook.sh:465-509`). Output is rendered JSON `reason` text in a hook response with `"decision": "block"` and system message `"Loop: Goal Tracker not initialized in Round 0"` (`hooks/loop-codex-stop-hook.sh:511-518`). State transition is blocking: the hook exits before max-iteration checks, Codex review prompt generation, or round advancement, and the user/agent may retry exit after updating the goal tracker (`hooks/loop-codex-stop-hook.sh:502-520`).
- gates_or_invariants: This guard runs only when `CURRENT_ROUND` is `0` and the goal-tracker file exists (`hooks/loop-codex-stop-hook.sh:467`). The hook detects three placeholder families: Ultimate Goal placeholder text beginning `[To be extracted from plan`, Acceptance Criteria placeholder text beginning `[To be defined by Claude`, and Active Tasks placeholder text beginning `[To be populated by Claude` (`hooks/loop-codex-stop-hook.sh:471-485`). Any detected placeholder appends a corresponding missing-item bullet and triggers the block (`hooks/loop-codex-stop-hook.sh:487-502`). The template’s invariant is that the immutable goal/acceptance-criteria section must be initialized in Round 0 because later rounds treat it as read-only (`prompt-template/block/goal-tracker-not-initialized.md:16-18`).
- dependencies_and_callers: The placeholder values originate from `scripts/setup-rlcr-loop.sh`, which creates `goal-tracker.md` and inserts fallback placeholders when it cannot heuristically extract goal or acceptance criteria from the plan; it also seeds the Active Tasks placeholder row (`scripts/setup-rlcr-loop.sh:425-513`). The initial Round 0 prompt separately instructs Claude to initialize the tracker before implementation using the same semantic steps (`scripts/setup-rlcr-loop.sh:521-534`). The stop hook calls `load_and_render_safe` with this block template and a shorter fallback message (`hooks/loop-codex-stop-hook.sh:503-509`). Rendering behavior depends on `hooks/lib/template-loader.sh`, which uses single-pass variable substitution and fallback-on-empty behavior (`hooks/lib/template-loader.sh:35-43`, `hooks/lib/template-loader.sh:155-188`).
- edge_cases_or_failure_modes: If the goal tracker file is missing, this specific guard does not run because of the `-f` condition; behavior for missing loop artifacts is handled elsewhere, not by this template (`hooks/loop-codex-stop-hook.sh:467`). Detection is string-pattern based, so modified placeholder wording could bypass or falsely trigger the guard (`hooks/loop-codex-stop-hook.sh:475-484`). If setup heuristically extracts a weak or empty-looking goal/criteria section but does not leave the exact placeholder, this guard will not block. If the template is missing or empty, the fallback still blocks but omits the richer 3-7 acceptance-criteria and immutable-section instructions (`hooks/loop-codex-stop-hook.sh:503-509`). Multi-line `MISSING_ITEMS` is passed as an environment value into the renderer; the loader supports variable substitution, but unresolved placeholders are preserved rather than treated as hard errors (`hooks/lib/template-loader.sh:100-107`).
- validation_or_tests: I found direct references to this template in the stop hook and search results, but no file-specific test assertion analogous to the test for `codex/goal-tracker-update-section.md`. Related behavior is indirectly covered by setup/hook logic and the common template loader. The strongest concrete validation evidence is the hook’s explicit missing-item construction and block response path at `hooks/loop-codex-stop-hook.sh:487-520`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ADD_COMMIT_PLAN_FILE_CLI-HZ-011 exactly once; ADD_COMMIT_PLAN_FILE_CLI-HZ-041 exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`