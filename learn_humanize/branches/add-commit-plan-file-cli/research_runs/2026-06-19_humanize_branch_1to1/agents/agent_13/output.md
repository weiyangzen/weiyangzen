# agent_13 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-013 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command entrypoint for starting the RLCR loop. It is a thin command definition whose frontmatter exposes `/humanize:start-rlcr-loop`, constrains execution to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)`, hides the command from the slash-command tool, and declares the accepted argument shape at `commands/start-rlcr-loop.md:1-5`.
- algorithmic_behavior: The command delegates all executable setup behavior to `scripts/setup-rlcr-loop.sh $ARGUMENTS` at `commands/start-rlcr-loop.md:10-14`. Its embedded workflow defines the high-level RLCR cycle: implement the plan, write a round summary, stop hook invokes Codex review, Codex feedback either continues the loop or final `COMPLETE` exits at `commands/start-rlcr-loop.md:16-23`. It also defines Goal Tracker semantics: immutable ultimate goal / acceptance criteria, mutable task/progress/evolution sections, and full alignment checks at rounds 4, 9, 14, etc. at `commands/start-rlcr-loop.md:24-42`.
- inputs_outputs_state: Inputs are `<path/to/plan.md>` plus options shown in `argument-hint` at `commands/start-rlcr-loop.md:3`. The delegated setup script validates and expands those inputs: one plan path, optional `--plan-file`, `--track-plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round` at `scripts/setup-rlcr-loop.sh:97-173`. Outputs are a loop directory under `.humanize-loop.local/<timestamp>/`, a copied `plan.md`, `state.md`, `goal-tracker.md`, and `round-0-prompt.md` initialized by `scripts/setup-rlcr-loop.sh:388-419` and `scripts/setup-rlcr-loop.sh:421-572`. Runtime state advances through `current_round` in `state.md`; the stop hook renames `state.md` to `{complete,stop,maxiter,unexpected}-state.md` on terminal states via `hooks/lib/loop-common.sh:199-232` and `hooks/loop-codex-stop-hook.sh:837-875`.
- gates_or_invariants: The command-level rules require summaries, Goal Tracker maintenance, detailed implementation evidence, no loop-state cheating, and Codex trust at `commands/start-rlcr-loop.md:43-55`. The setup script enforces a git repo with at least one commit, relative plan paths, no spaces or shell metacharacters, no symlink plan, in-project path resolution, no submodule plan, valid tracking mode, at least 5 plan lines, Codex availability, YAML-safe branch/model/effort values, and branch capture at `scripts/setup-rlcr-loop.sh:200-382`. Stop-hook invariants include schema presence, same branch, plan backup/content integrity, all todos complete, no oversized changed files, clean git, optional pushed commits, required summary, initialized Goal Tracker in round 0, max-iteration termination, Codex result file existence/non-empty, and strict final-line `COMPLETE` / `STOP` matching at `hooks/loop-codex-stop-hook.sh:107-335`, `hooks/loop-codex-stop-hook.sh:339-521`, and `hooks/loop-codex-stop-hook.sh:754-885`.
- dependencies_and_callers: Called by Claude Code’s slash-command system using the frontmatter and script invocation in this file. It depends directly on `scripts/setup-rlcr-loop.sh`, which in turn depends on git, `codex`, shell tools, and `portable-timeout.sh` during review setup. The loop lifecycle depends on hooks registered in `hooks/hooks.json:4-63`, especially the Stop hook `hooks/loop-codex-stop-hook.sh`. README user docs mirror this entrypoint and options at `README.md:57-71` and `README.md:181-201`.
- edge_cases_or_failure_modes: Misuse is mostly rejected by setup before state creation: missing or duplicate plan path, unknown options, invalid numeric options, absolute paths, nonexistent parent/file, spaces, shell metacharacters, symlink, escaping project root, submodule path, wrong tracking mode, short plans, unavailable Codex, and unsafe branch/model strings. Runtime failure modes include outdated state schema, branch switching, plan deletion/modification, incomplete todos, large changed files, dirty git, missing summary, uninitialized Goal Tracker, Codex CLI missing/failing, empty/missing review output, max iterations, and unexpected `STOP` outside alignment rounds.
- validation_or_tests: Plan and setup validation are covered by `tests/test-plan-file-validation.sh`, whose stated scope includes absolute path, symlink, submodule, git repo, and plan tracking validation at `tests/test-plan-file-validation.sh:3-12`, with concrete cases for absolute paths, missing files, missing directories, spaces, metacharacters, symlinks, and resolution errors at `tests/test-plan-file-validation.sh:85-238`. Hook/state behavior is covered by `tests/test-plan-file-hooks.sh:3-10` and state terminal naming / active-loop detection by `tests/test-state-exit-naming.sh:3-10` and `tests/test-state-exit-naming.sh:38-177`.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-043 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the RLCR Stop hook when changed tracked/new code or documentation files exceed the configured line-count limit. It is not executable logic itself; it defines the human-facing remediation contract for the large-file gate.
- algorithmic_behavior: The template renders a stop message with `{{MAX_LINES}}` and `{{LARGE_FILES}}` placeholders at `prompt-template/block/large-files.md:1-4`. It then branches remediation by file category: code files must be split into smaller modules, preserve behavior, optionally invoke `code-simplifier`, and maintain module boundaries at `prompt-template/block/large-files.md:13-17`; documentation files must be split into logical sections, maintain cross-references and narrative flow, and update TOCs/navigation at `prompt-template/block/large-files.md:19-23`. It ends by requiring the user to commit and attempt exit again at `prompt-template/block/large-files.md:25`.
- inputs_outputs_state: Inputs are rendered by `load_and_render_safe` from the stop hook: `MAX_LINES=2000` and a newline list of offending files built as ``- `path`: N lines (code/documentation file)`` at `hooks/loop-codex-stop-hook.sh:262-324`. Output is a JSON hook block reason, with system message `"Loop: Blocked - large files detected..."`, emitted at `hooks/loop-codex-stop-hook.sh:326-334`. The gate does not mutate loop state; it exits before git-clean, summary, Codex-review, or round-advance processing.
- gates_or_invariants: The invariant is that any changed file recognized as code or documentation must be no more than `MAX_LINES` lines before the user can exit the RLCR loop. The hook scans `git status --porcelain`, normalizes rename targets, skips deleted files, filters extensions, lowercases extensions with `to_lower`, and counts lines with `wc -l` at `hooks/loop-codex-stop-hook.sh:264-312`. The check is positioned after incomplete-todo blocking and before git-clean/push checks, so oversized files block even if work is otherwise ready to commit/review at `hooks/loop-codex-stop-hook.sh:218-339`.
- dependencies_and_callers: Called only through `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` at `hooks/loop-codex-stop-hook.sh:322-324`. Template discovery/rendering is provided by `hooks/lib/template-loader.sh`, especially single-pass placeholder rendering at `hooks/lib/template-loader.sh:35-117` and fallback-safe loading at `hooks/lib/template-loader.sh:152-188`. `TEMPLATE_DIR` is initialized through `hooks/lib/loop-common.sh:11-21`.
- edge_cases_or_failure_modes: Untracked/modified rename records are handled by stripping `"old -> new"` to the destination path at `hooks/loop-codex-stop-hook.sh:276-279`. Deleted files are skipped at `hooks/loop-codex-stop-hook.sh:281-284`. Files with unsupported extensions are ignored at `hooks/loop-codex-stop-hook.sh:290-300`, so large JSON, YAML, binary, or extensionless files are not covered by this gate. If the template is missing or empty, the stop hook falls back to an inline shorter large-file message at `hooks/loop-codex-stop-hook.sh:315-324`; if placeholders are malformed or unavailable, `render_template` preserves unmatched placeholders instead of deleting them at `hooks/lib/template-loader.sh:100-107`.
- validation_or_tests: Template existence and reference coverage are checked by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails missing templates at `tests/test-template-references.sh:51-111`. Comprehensive template loading/syntax checks cover every `prompt-template/**/*.md`, including placeholder syntax and malformed placeholder detection at `tests/test-templates-comprehensive.sh:82-194` and `tests/test-templates-comprehensive.sh:196-260`. Rendering/fallback behavior is directly tested in `tests/test-template-loader.sh:41-78`, `tests/test-template-loader.sh:80-164`, and `tests/test-template-loader.sh:196-220`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 Item Evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`