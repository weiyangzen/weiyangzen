# agent_10 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-010 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: This directory contains the Claude-facing prompt fragments used to continue an RLCR round after Codex review finds remaining work. It is algorithmic prompt surface, not business logic: the files encode the next-round control flow, required planning behavior, goal-tracker read/update protocol, post-alignment priorities, commit/summary obligations, and optional push behavior.
- algorithmic_behavior: Recursive inspection found five files and no nested child directories: `prompt-template/claude/next-round-prompt.md`, `next-round-footer.md`, `goal-tracker-update-request.md`, `post-alignment-action-items.md`, and `push-every-round-note.md`. `hooks/loop-codex-stop-hook.sh` assembles them when Codex does not return `COMPLETE` or `STOP`: it increments `current_round` in `state.md`, creates `round-${NEXT_ROUND}-prompt.md`, renders `claude/next-round-prompt.md`, conditionally appends post-alignment instructions, appends the footer, conditionally appends the push note, and finally appends the goal-tracker update request block (`hooks/loop-codex-stop-hook.sh:882-933`). The prompt body forces the worker to reread the original plan, create todos for all discovered issues, consume Codex review output, and treat the goal tracker as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1-31`). The footer blocks loop escape by state tampering or `cancel-rlcr-loop`, asks for code simplifier review if installed, requires a commit, and requires summary output to the provided next summary file (`prompt-template/claude/next-round-footer.md:4-9`). The post-alignment fragment is only appended after full alignment rounds, based on `CURRENT_ROUND % 5 == 4`, and tells Claude to prioritize forgotten items, unmet acceptance criteria, and unjustified deferrals (`hooks/loop-codex-stop-hook.sh:905-910`, `prompt-template/claude/post-alignment-action-items.md:2-7`). The push note is appended only when `push_every_round: true` in loop state (`hooks/loop-codex-stop-hook.sh:919-925`, `prompt-template/claude/push-every-round-note.md:2`). The goal-tracker request fragment gives the structured summary section that Codex later reviews (`prompt-template/claude/goal-tracker-update-request.md:2-16`).
- inputs_outputs_state: Inputs are loop state fields and generated artifacts: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `NEXT_SUMMARY_FILE`, `CURRENT_ROUND`, and `PUSH_EVERY_ROUND` from the stop hook and `state.md` frontmatter (`hooks/loop-codex-stop-hook.sh:69-91`, `hooks/loop-codex-stop-hook.sh:828-834`, `hooks/loop-codex-stop-hook.sh:887-933`). Output is the next prompt file at `.humanize-loop.local/<loop>/round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:887-903`). State transition is handled by the caller: `current_round` is updated from the current round to `NEXT_ROUND` before prompt generation (`hooks/loop-codex-stop-hook.sh:882-885`). The directory templates do not directly mutate state; they define the worker contract for the next iteration.
- gates_or_invariants: The key invariant is that Codex owns goal-tracker mutation after round 0 while Claude may only request changes in its summary. The next-round prompt makes `goal-tracker.md` read-only after round 0 (`prompt-template/claude/next-round-prompt.md:22-31`), and the separate request fragment supplies the required request shape (`prompt-template/claude/goal-tracker-update-request.md:2-16`). The generated prompt also preserves the invariant that Claude must address all discovered issues, not just high-priority ones (`prompt-template/claude/next-round-prompt.md:12-14`). Loop integrity is reinforced by footer text forbidding false exits, state-file edits, or `cancel-rlcr-loop` execution (`prompt-template/claude/next-round-footer.md:4`). Push behavior is gated by `PUSH_EVERY_ROUND`; without that state flag the push note is not appended, and the Bash validator separately blocks `git push` when the flag is false (`hooks/loop-bash-validator.sh:49-64`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, specifically the review-failed continuation path (`hooks/loop-codex-stop-hook.sh:878-940`). Rendering depends on `hooks/lib/template-loader.sh`: `load_template`, `render_template`, and `load_and_render_safe` load Markdown fragments and substitute `{{VAR}}` placeholders (`hooks/lib/template-loader.sh:18-33`, `hooks/lib/template-loader.sh:35-117`, `hooks/lib/template-loader.sh:152-180`). Template resolution uses `get_template_dir` to locate `prompt-template` relative to `hooks/lib` (`hooks/lib/template-loader.sh:9-16`). Coordination with sibling template directories is explicit: Codex review templates in `prompt-template/codex` produce `REVIEW_CONTENT`; the Claude templates consume that content to continue the loop (`hooks/loop-codex-stop-hook.sh:553-624`, `hooks/loop-codex-stop-hook.sh:828-903`). The goal-tracker request emitted by this directory also coordinates with Codex’s `goal-tracker-update-section.md`, which tells Codex to apply justified requests during review (`hooks/loop-codex-stop-hook.sh:553-557`).
- edge_cases_or_failure_modes: Missing or empty critical templates degrade to inline fallbacks through `load_and_render_safe` for `next-round-prompt.md` and `next-round-footer.md` (`hooks/loop-codex-stop-hook.sh:891-917`, `hooks/lib/template-loader.sh:152-180`). Optional append-only fragments use `load_template`; if `post-alignment-action-items.md` is absent it is skipped, if `push-every-round-note.md` is absent a short fallback is appended, and if `goal-tracker-update-request.md` is absent a short fallback request sentence is appended (`hooks/loop-codex-stop-hook.sh:905-933`). Template substitution is single-pass, so placeholder-looking text inside `REVIEW_CONTENT` is preserved instead of being recursively substituted, avoiding prompt corruption or placeholder injection (`hooks/lib/template-loader.sh:39-42`, `hooks/lib/template-loader.sh:56-116`; test coverage at `tests/test-template-loader.sh:525-540`). A stale or malformed `state.md` could affect current round and push gating because these templates rely on values parsed upstream, but defaulting in the stop hook sets missing round/push values to `0` and `false` (`hooks/loop-codex-stop-hook.sh:88-91`).
- validation_or_tests: Static validation exists in `tests/test-template-references.sh`, which checks that template files are referenced by hooks and that common templates exist, though it treats some unreferenced templates as warnings (`tests/test-template-references.sh:130-167`). Rendering behavior is covered by `tests/test-template-loader.sh`, including real-template loading and injection prevention for review content containing `{{...}}` (`tests/test-template-loader.sh:147-164`, `tests/test-template-loader.sh:525-540`). Broader template directory and real-template integration tests are in `tests/test-templates-comprehensive.sh`, including directory structure, syntax, render edge cases, and real-template rendering (`tests/test-templates-comprehensive.sh:62-116`, `tests/test-templates-comprehensive.sh:262-545`). I did not execute tests in this read-only research pass.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-040 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: This block template is the user-facing enforcement message for a hard RLCR permission rule: after round 0, Claude cannot modify `goal-tracker.md` directly and must instead request changes in its current round summary. It is part of the hook validation algorithm because it is rendered by shared validator code when Write, Edit, or Bash attempts target the goal tracker after round 0.
- algorithmic_behavior: The template title includes the active round via `{{CURRENT_ROUND}}` (`prompt-template/block/goal-tracker-modification.md:1`). It states the rule that only Codex can modify the Goal Tracker after round 0 (`prompt-template/block/goal-tracker-modification.md:3`), forbids direct modification via Write, Edit, or Bash (`prompt-template/block/goal-tracker-modification.md:5`), and redirects the worker to include a `Goal Tracker Update Request` section in `{{SUMMARY_FILE}}` (`prompt-template/block/goal-tracker-modification.md:7-10`). It then defines the required Markdown structure with `Requested Changes` and `Justification` sections (`prompt-template/block/goal-tracker-modification.md:12-23`) and closes by stating Codex will review justified requests (`prompt-template/block/goal-tracker-modification.md:25`). The shared function `goal_tracker_blocked_message` renders this file with `CURRENT_ROUND` and `SUMMARY_FILE`; if the template is missing, it falls back to a shorter inline message (`hooks/lib/loop-common.sh:185-197`).
- inputs_outputs_state: Inputs are `current_round` and `summary_file` passed to `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:187-196`). Those values are computed by validators from the latest active `.humanize-loop.local` directory and `state.md` current round (`hooks/loop-write-validator.sh:73-82`, `hooks/loop-edit-validator.sh:58-67`, `hooks/loop-bash-validator.sh:36-46`). Output is a rendered Markdown block written to stderr by validators before they return a blocking hook result through exit code `2` (`hooks/loop-write-validator.sh:109-112`, `hooks/loop-edit-validator.sh:95-98`, `hooks/loop-bash-validator.sh:96-104`). The template itself performs no file mutation; the state transition is negative enforcement: attempted direct goal-tracker mutation is blocked, and desired goal-tracker changes are moved into the summary-review pathway.
- gates_or_invariants: The gate condition is `is_goal_tracker_path(...) && CURRENT_ROUND > 0` for Write and Edit operations (`hooks/loop-write-validator.sh:105-112`, `hooks/loop-edit-validator.sh:91-98`). Bash uses command pattern detection for writes to `goal-tracker.md`; in round 0 it blocks Bash and tells Claude to use Write/Edit, while in rounds greater than 0 it renders this template and blocks the command (`hooks/loop-bash-validator.sh:90-105`). The invariant enforced is separation of authority: Claude may initialize the goal tracker in round 0 through controlled tools, but after round 0 only Codex can apply accepted goal-tracker updates. This pairs with the next-round prompt’s read-only instruction (`prompt-template/claude/next-round-prompt.md:22-31`) and Codex review prompt behavior that can apply `Goal Tracker Update Request` sections (`hooks/loop-codex-stop-hook.sh:553-557`).
- dependencies_and_callers: Direct renderer is `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:185-197`), which depends on `load_and_render_safe` from `hooks/lib/template-loader.sh` (`hooks/lib/template-loader.sh:152-180`). Callers are `hooks/loop-write-validator.sh` for Write tool attempts (`hooks/loop-write-validator.sh:109-112`), `hooks/loop-edit-validator.sh` for Edit tool attempts (`hooks/loop-edit-validator.sh:95-98`), and `hooks/loop-bash-validator.sh` for shell command bypass attempts (`hooks/loop-bash-validator.sh:96-104`). Path detection depends on `is_goal_tracker_path`, which matches lowercase paths ending in `goal-tracker.md` (`hooks/lib/loop-common.sh:137-141`). Active-loop detection and round parsing depend on `find_active_loop` and `get_current_round` (`hooks/lib/loop-common.sh:23-58`).
- edge_cases_or_failure_modes: If no active loop exists, validators allow operations and this template is not rendered (`hooks/loop-write-validator.sh:73-79`, `hooks/loop-edit-validator.sh:58-64`, `hooks/loop-bash-validator.sh:36-43`). If `CURRENT_ROUND` parses as empty, `get_current_round` defaults to `0`, which means the after-round-0 block would not fire until state parsing yields a positive integer (`hooks/lib/loop-common.sh:46-58`). Bash detection is regex-based and covers common write/edit patterns such as redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, and `exec >` (`hooks/lib/loop-common.sh:155-183`), but unusual shell constructs outside those patterns could bypass this specific Bash gate. The file path matcher only checks paths ending in `goal-tracker.md`, so similarly named files are not blocked by this rule (`hooks/lib/loop-common.sh:137-141`). If the template file is missing or renders empty, validators still block via fallback text, preserving the enforcement behavior while reducing instruction detail (`hooks/lib/loop-common.sh:190-197`, `hooks/lib/template-loader.sh:152-180`).
- validation_or_tests: `tests/test-template-references.sh` includes `block/goal-tracker-modification.md` in the common-template existence list, validating that the shared hook message template is present (`tests/test-template-references.sh:149-167`). Search also shows all three validator callers route through `goal_tracker_blocked_message`, which statically confirms this template is on the enforcement path (`hooks/loop-write-validator.sh:111`, `hooks/loop-edit-validator.sh:97`, `hooks/loop-bash-validator.sh:102`). General template loader tests validate single-pass placeholder substitution and real-template rendering mechanics (`tests/test-template-loader.sh:147-164`, `tests/test-template-loader.sh:525-540`). I did not execute tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_COMMIT_PLAN_FILE_CLI-HZ-010`, `ADD_COMMIT_PLAN_FILE_CLI-HZ-040`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`