# agent_17 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook implementation for the RLCR state machine. It intercepts Claude Code `Stop` events, finds the active `.humanize-loop.local/<timestamp>/state.md`, enforces pre-review guards, invokes Codex as an external reviewer, and either ends the loop or blocks exit with next-round instructions. It is registered as the `Stop` hook in `hooks/hooks.json:52-59` with a 7200 second timeout.
- algorithmic_behavior: The script reads hook JSON from stdin into `HOOK_INPUT` (`hooks/loop-codex-stop-hook.sh:29`), resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` (`:43`), sources shared loop helpers (`:47-48`), and selects only the newest active loop directory via `find_active_loop` (`:52`; helper behavior in `hooks/lib/loop-common.sh:23-44`). It parses YAML-like frontmatter from `state.md` with `sed` and field-specific `grep`/`sed` extraction (`hooks/loop-codex-stop-hook.sh:69-86`), then applies defaults for round, max iterations, push policy, and Codex runtime (`:88-94`). The hook runs a sequence of cheap gates before Codex review: numeric state validation (`:96-105`), schema check for v1.1.2 fields (`:112-123`), branch consistency (`:130-142`), plan backup/original integrity (`:148-216`), incomplete TodoWrite check from transcript (`:224-253`), large changed-file check (`:262-335`), clean git status and optional pushed-state check (`:345-431`), summary file presence (`:438-459`), round-0 goal tracker initialization (`:465-520`), and max iteration termination (`:527-532`). If those pass, it renders either a regular Codex review prompt or every-fifth-round full-alignment review prompt (`:553-625`), runs `codex exec` with configured model/effort/full-auto/project root (`:631-716`), validates the Codex output file (`:754-826`), and interprets the last non-empty line as a strict state transition token: `COMPLETE` ends as complete (`:831-845`), `STOP` ends via circuit breaker (`:848-875`), anything else increments `current_round`, writes the next Claude prompt, and returns a JSON block (`:882-947`).
- inputs_outputs_state: Inputs are hook stdin JSON, especially `transcript_path` consumed by `hooks/check-todos-from-transcript.py` (`hooks/loop-codex-stop-hook.sh:224-229`; parser behavior in `hooks/check-todos-from-transcript.py:87-122`), environment variables `CLAUDE_PROJECT_DIR` and optionally `CODEX_TIMEOUT`, git repository state, current branch, tracked/untracked working tree state, the active loop `state.md`, backup `plan.md`, project plan file, round summary, and goal tracker. The state producer is `scripts/setup-rlcr-loop.sh`, which validates plan path/tracking/branch constraints (`scripts/setup-rlcr-loop.sh:220-326`, `:354-381`) and writes `state.md` fields including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, and `start_branch` (`:406-418`). Outputs are Claude hook JSON objects with `"decision": "block"` for recoverable gates, stderr diagnostics for Codex/debug paths, Codex debug files under `$HOME/.cache/humanize/<sanitized-project>/<loop_timestamp>/` (`hooks/loop-codex-stop-hook.sh:655-667`), review prompt/result files under the loop directory (`:547-549`), mutated `state.md` when continuing (`:882-885`), next-round prompt files (`:887-933`), or terminal state renames through `end_loop` to `complete-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md` (`hooks/lib/loop-common.sh:199-232`).
- gates_or_invariants: Active loop invariant is file-presence based: only the newest loop directory with `state.md` is active (`hooks/lib/loop-common.sh:23-44`); terminal states are represented by renaming `state.md` (`:221-225`). The hook intentionally ignores `stop_hook_active` so blocked-stop continuations still get reviewed (`hooks/loop-codex-stop-hook.sh:31-37`). Schema invariant requires `plan_tracked` and `start_branch` (`:112-123`); branch invariant forbids switching away from `start_branch` (`:130-142`). Plan invariant requires a loop backup at `$LOOP_DIR/plan.md`, an extant original project plan, clean tracked plan status when `plan_tracked=true`, and byte-equivalent original-vs-backup content even after committed plan changes (`:148-216`). Work-completion gates require all TodoWrite items completed before review (`:224-253`), no changed code/doc files over 2000 lines (`:262-335`), no uncommitted changes before exit (`:345-399`), no unpushed commits when `push_every_round=true` (`:405-430`), a round summary file (`:438-459`), initialized goal tracker in round 0 (`:465-520`), and not exceeding max iterations (`:527-532`). Codex review invariant requires installed `codex`, nonzero/empty/missing outputs are blocking failures (`:631-651`, `:754-826`), and completion is recognized only if the last non-empty line is exactly `COMPLETE`; this avoids false positives such as “CANNOT COMPLETE” (`:831-845`).
- dependencies_and_callers: Called by Claude Code hook configuration `hooks/hooks.json:52-59`. Depends on `hooks/lib/loop-common.sh` for active-loop discovery, template-derived block messages, lowercase conversion, and terminal-state renaming. Depends transitively on `hooks/lib/template-loader.sh`, whose single-pass `{{VAR}}` renderer prevents placeholder injection from substituted content (`hooks/lib/template-loader.sh:35-117`) and whose `load_and_render_safe` provides fallback messages for missing/empty templates (`:152-188`). Depends on `hooks/check-todos-from-transcript.py` for transcript TodoWrite parsing. Depends on prompt templates including `block/plan-file-modified.md`, `block/incomplete-todos.md`, `block/large-files.md`, `block/git-not-clean.md`, `block/unpushed-commits.md`, `block/work-summary-missing.md`, `block/goal-tracker-not-initialized.md`, `codex/full-alignment-review.md`, `codex/regular-review.md`, and Claude next-round templates as referenced in `hooks/loop-codex-stop-hook.sh:210-624` and `:900-933`. The state and initial round files are created by `scripts/setup-rlcr-loop.sh`, invoked through `commands/start-rlcr-loop.md:12-14`.
- edge_cases_or_failure_modes: Missing loop directory or missing `state.md` exits 0 and allows stop (`hooks/loop-codex-stop-hook.sh:52-67`). Corrupt nonnumeric `current_round` ends unexpectedly (`:96-100`), while invalid `max_iterations` falls back to 42 (`:103-105`). Outdated state schema currently blocks with JSON but does not call `end_loop`, despite the comment saying schema should terminate unexpectedly (`:107-123`); related prompt-submit validation uses the schema-outdated template separately. Plan path safety is mostly guaranteed by setup-time validation: relative path only, no spaces/metacharacters, no symlink, must resolve inside project, and not inside submodules (`scripts/setup-rlcr-loop.sh:220-291`). If setup invariants are bypassed, the stop hook still quotes path variables in file checks and git calls but does not independently re-run every setup path-safety check. Git checks use current working directory for `git rev-parse --git-dir` and some later commands, while branch checks use `git -C "$PROJECT_ROOT"`; under normal Claude hook execution this should match project root, but `CLAUDE_PROJECT_DIR` divergence is a subtle dependency. Codex failures, missing review files, empty review files, stdout-only output, missing `codex`, auth/network/timeouts, and permission/disk issues all convert to JSON blocks rather than nonzero hook exits (`hooks/loop-codex-stop-hook.sh:631-651`, `:728-826`). `STOP` is honored even outside full-alignment rounds, with an “unexpected circuit breaker” message (`:848-875`).
- validation_or_tests: Regression coverage is present in `tests/test-plan-file-hooks.sh`: stop hook accepts quoted `plan_file`/`start_branch` without YAML parse failures (`tests/test-plan-file-hooks.sh:388-429`), blocks modified plans (`:469-507`), deleted plans (`:509-540`), missing backups (`:543-560`), tracked-plan uncommitted modifications (`:562-629`), outdated schema as a JSON block (`:631-653`), and committed tracked-plan changes detected by content diff (`:655-730`). `tests/test-state-exit-naming.sh` covers `end_loop` valid/invalid terminal state renames (`tests/test-state-exit-naming.sh:182-235`). `tests/test-template-references.sh` scans `hooks/loop-codex-stop-hook.sh` and sibling validators for missing template references (`tests/test-template-references.sh:56-111`) and enforces safe template rendering patterns (`:172-199`). I did not run the test scripts because this assignment is read-only research; evidence is from direct file inspection.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-047 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR state-schema gate when an active loop was created by an older plugin and lacks required v1.1.2+ state fields. It defines the remediation contract shown to Claude/the operator when `plan_tracked` or `start_branch` is missing.
- algorithmic_behavior: The template is static Markdown with one variable, `{{FIELD_NAME}}`, rendered into “RLCR loop state file is missing required field: ...” (`prompt-template/block/schema-outdated.md:1-4`). It then instructs cancellation, plugin update to 1.1.2+, and loop restart (`:7-10`), and states that the loop will be terminated as `unexpected` to preserve state information (`:12`). The actual rendering path is in `hooks/loop-plan-file-validator.sh`, where `schema_validation_error()` calls `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md" ... "FIELD_NAME=$field_name"` and emits a JSON block (`hooks/loop-plan-file-validator.sh:46-64`). The validator iterates required fields `plan_tracked` and `start_branch` and invokes this template when either is empty (`:66-76`).
- inputs_outputs_state: Input is `FIELD_NAME`, derived from missing `state.md` frontmatter fields parsed by the prompt-submit validator (`hooks/loop-plan-file-validator.sh:36-40`, `:67-73`). Output is Markdown embedded as the `reason` value of a Claude hook JSON block (`:54-63`). It does not mutate state itself. Its state-machine meaning is advisory/blocking: it prevents further prompt submission when the active loop state schema is too old, telling the user to cancel or restart. The template’s terminal-state sentence says `unexpected`, but the caller shown here only blocks and exits 0; terminal rename to `unexpected-state.md` is implemented by `end_loop` in `hooks/lib/loop-common.sh:199-232` and is not invoked by `loop-plan-file-validator.sh`.
- gates_or_invariants: Encodes the invariant that active RLCR `state.md` must include newly required fields. This pairs with setup-time state creation in `scripts/setup-rlcr-loop.sh:406-418`, where `plan_tracked` and `start_branch` are written, and runtime validation in `hooks/loop-plan-file-validator.sh:66-76`. It also aligns with the stop hook’s inline schema gate, which requires the same fields and blocks stop when either is absent (`hooks/loop-codex-stop-hook.sh:107-123`), though the stop hook does not currently render this template.
- dependencies_and_callers: Direct caller is `hooks/loop-plan-file-validator.sh:52`. Rendering depends on `hooks/lib/template-loader.sh`, especially single-pass `render_template` (`hooks/lib/template-loader.sh:43-117`) and fallback-safe `load_and_render_safe` (`:155-188`). Template directory setup comes through `hooks/lib/loop-common.sh:11-21`. The template is referenced by repository-wide search only in the prompt-submit validator and template tests, not by the stop hook. It is part of the broader hook graph registered in `hooks/hooks.json`, where the prompt-submit validator runs on `UserPromptSubmit` (`hooks/hooks.json:4-12`) and the stop hook runs on `Stop` (`:52-59`).
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline message with the same `FIELD_NAME` variable (`hooks/loop-plan-file-validator.sh:49-52`; loader fallback behavior in `hooks/lib/template-loader.sh:164-187`). If the missing field name contains unexpected template syntax, rendering is single-pass so substituted content is not rescanned (`hooks/lib/template-loader.sh:39-42`, `:101-106`). The template text says the loop will be terminated as `unexpected`, but the direct caller only returns a block decision; this is a wording/behavior mismatch unless another hook path performs the rename. The stop hook separately has an inline combined message for missing `plan_tracked or start_branch`, so updates to this template will not change stop-hook wording (`hooks/loop-codex-stop-hook.sh:112-123`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers missing `plan_tracked` and missing `start_branch` through the prompt-submit validator (`tests/test-plan-file-hooks.sh:121-159`) and checks outdated-schema handling in the stop hook (`:631-653`). `tests/test-template-references.sh` discovers template references from hook scripts and fails if referenced templates are missing (`tests/test-template-references.sh:83-111`); it also lists directly referenced templates and safe render usage checks (`:130-199`). `tests/test-template-loader.sh` covers `load_and_render_safe` missing-template fallback and existing-template rendering behavior (`tests/test-template-loader.sh:197-221`, found by direct reference search). I did not run tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item headings present; each assigned item_id appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`