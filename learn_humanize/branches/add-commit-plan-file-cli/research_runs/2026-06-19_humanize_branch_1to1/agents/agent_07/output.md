# agent_07 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-007 `file` `README.md`
- cursor: `[_]`
- core_role: Public behavior contract for the Humanize/RLCR workflow. It defines the user-facing loop model where a plan file drives Claude implementation, Codex independently reviews each round, and loop artifacts/state are stored under `.humanize-loop.local/<timestamp>/`. The README is not executable, but it is behavior-defining documentation for command inputs, state files, exit states, monitoring, and quality gates.
- algorithmic_behavior: Documents the loop as a state machine: user creates a markdown plan, runs `/humanize:start-rlcr-loop <plan.md>`, Claude implements and writes summaries, Codex reviews, feedback either starts another round or terminates the loop. Lines 33-39 show the Plan -> Claude -> Codex -> feedback/done flow. Lines 41-55 define the plan-file minimum content contract, including clear description, acceptance criteria, technical approach as optional, and at least 5 lines. Lines 57-71 document runtime options for max iterations, Codex model, and timeout. Lines 112-129 define interrupt/resume/cancel behavior keyed solely by `state.md` presence in the newest loop directory.
- inputs_outputs_state: Inputs are a markdown plan path, optional `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round` arguments as documented at lines 190-200. Outputs are local loop artifacts listed at lines 242-253: `state.md`, backup `plan.md`, `goal-tracker.md`, round prompts, summaries, review prompts, and review results. Exit-state outputs are renamed state files at lines 255-259: `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md`. Cache outputs for Codex command/stdout/stderr live under `$HOME/.cache/humanize/...` per lines 262-266.
- gates_or_invariants: The README defines key invariants: active loop equals current `state.md` presence, cancellation renames state to `cancel-state.md`, and loop artifacts remain preserved for audit at lines 116-129. Goal tracker invariants are documented at lines 131-143: immutable goal/acceptance criteria are set in Round 0, mutable sections track active/completed/deferred work and plan evolution, and full alignment checks happen every 5 rounds starting at rounds 4, 9, 14. Circuit breaker behavior is documented at lines 145-152: Codex can emit `STOP` on stagnation. Installation/prerequisite gate: Codex CLI is required, lines 203-211.
- dependencies_and_callers: Public slash command is documented in the command table at lines 181-187 and wired by `commands/start-rlcr-loop.md`, which executes `scripts/setup-rlcr-loop.sh $ARGUMENTS` at command lines 10-14. The implementation adds CLI behavior beyond the README: `scripts/setup-rlcr-loop.sh` documents and parses `--plan-file` and `--track-plan-file` at script lines 47-55 and 145-155. Runtime hook dependencies are summarized by `hooks/hooks.json`, where `loop-plan-file-validator.sh` runs on `UserPromptSubmit` and `loop-codex-stop-hook.sh` runs on `Stop`.
- edge_cases_or_failure_modes: README omits two branch-relevant command options now implemented by the setup script: `--plan-file` and `--track-plan-file`. This creates documentation drift for this branch’s “add commit plan file CLI” behavior. Setup validation rejects absolute plan paths, paths with spaces, shell metacharacters, symlinks, paths outside the project, submodule paths, tracked plan files unless `--track-plan-file` is used, untracked plan files when `--track-plan-file` is used, dirty tracked plan files, too-short plans, missing Codex, YAML-unsafe branch names, and unsafe Codex model/effort strings; see `scripts/setup-rlcr-loop.sh` lines 181-240, 246-340, and 342-382. README only documents the simpler path and 5-line content expectations.
- validation_or_tests: README itself has no direct test. Its command and plan-file contract is indirectly covered by `tests/test-plan-file-validation.sh`: tracked plan without `--track-plan-file` rejected at lines 325-355, untracked plan with `--track-plan-file` rejected at lines 357-389, dirty tracked plan rejected at lines 391-422, `--plan-file` accepted at lines 514-529, and specifying both explicit and positional plan files rejected at lines 531-541. Stop-hook plan integrity behaviors corresponding to README’s backup/integrity state are tested in `tests/test-plan-file-hooks.sh` lines 469-560 and 562-731.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-037 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Prompt block used by the Stop hook as a hard exit gate when the repository has dirty git state. It converts a machine-detected git cleanliness failure into actionable instructions for Claude before Codex review is allowed to run.
- algorithmic_behavior: Template title and message are at lines 1-4. It interpolates `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then instructs the agent to review untracked files, update `.gitignore` for build artifacts, stage real changes, and commit with a descriptive message at lines 5-10. It also adds commit authorship constraints at lines 11-15, explicitly forbidding AI authorship/co-author attribution in commits.
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`. `GIT_ISSUES` is set by `hooks/loop-codex-stop-hook.sh` to `uncommitted changes` when `git status --porcelain` is non-empty, lines 346-353. `SPECIAL_NOTES` is built from optional companion templates when untracked `.humanize-loop.local` or other untracked files are detected, lines 354-374. Output is rendered markdown stored in the hook JSON `reason`, with `decision: block` and system message `Loop: Blocked - ... please commit first`, lines 386-397.
- gates_or_invariants: This block runs before expensive Codex review, so dirty git state prevents exit and prevents Codex review execution. The invariant is that all real work must be committed before attempting to exit the RLCR loop. If `--push-every-round` is enabled, a later gate checks unpushed commits after cleanliness passes, at `hooks/loop-codex-stop-hook.sh` lines 401-430.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, lines 338-399. Rendering uses `load_and_render_safe` from `hooks/lib/template-loader.sh`, which loads templates and falls back if missing or empty; see loader lines 152-188. Placeholder rendering is single-pass to prevent placeholder injection, lines 35-43 and 56-116. Companion note templates are `prompt-template/block/git-not-clean-humanize-local.md`, which tells users not to commit `.humanize-loop.local/`, and `prompt-template/block/git-not-clean-untracked.md`, which lists typical artifacts to ignore.
- edge_cases_or_failure_modes: If the template is missing, the hook falls back to an inline “Git Not Clean” message at `hooks/loop-codex-stop-hook.sh` lines 380-385. If untracked `.humanize-loop.local` is detected, the special note recommends adding `.humanize*local*` to `.gitignore`; this is helpful but broad and may ignore similarly named files. The template’s required action 0 references an optional `code-simplifier` plugin; absence of that plugin should not block the git-clean gate, because the actionable hard requirement is still to stage/commit real changes. The template does not enumerate the dirty files itself; the detector only passes a summarized issue label.
- validation_or_tests: Existence of this template as a referenced file is validated by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and checks each target file exists, lines 51-111. All templates are load-tested and syntax-checked by `tests/test-templates-comprehensive.sh` lines 82-194, including uppercase double-brace placeholder syntax. Template loader error behavior is explored in `tests/test-error-scenarios.sh`, which notes that missing templates return empty and warns that empty feedback is risky, lines 21-57 and 114-120. I found no dedicated assertion that the exact `git-not-clean.md` instructions mention `code-simplifier` or forbid AI co-author trailers.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique assigned item sections present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`