# agent_01 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin. It implements an RLCR loop: Claude works from a plan, commits and summarizes work, Codex reviews on Stop, and hooks enforce state/file/plan invariants. The root coordinates slash commands, setup scripts, lifecycle hooks, prompt templates, shell utilities, tests, and CI workflows.
- algorithmic_behavior: `commands/start-rlcr-loop.md:1-14` exposes the start command and delegates to `scripts/setup-rlcr-loop.sh`. Setup parses positional and `--plan-file` alternatives, `--track-plan-file`, `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round` in `scripts/setup-rlcr-loop.sh:97-173`. It validates git repository state, relative plan path constraints, shell metacharacter safety, symlink rejection, in-project resolution, optional submodule rejection, tracking/cleanliness policy, minimum plan size, Codex availability, YAML-safe branch/model/effort values, then creates `.humanize-loop.local/<timestamp>/` state, plan backup, goal tracker, and the round-0 prompt in `scripts/setup-rlcr-loop.sh:179-610`. `hooks/hooks.json:4-63` registers UserPromptSubmit, PreToolUse validators, and Stop hook entry points. `hooks/loop-codex-stop-hook.sh` is the main loop transition engine: it finds the newest active loop, parses state, checks schema and branch/plan integrity, blocks incomplete todos, large files, dirty git, missing summary, uninitialized round-0 goal tracker, max iterations, builds regular or full-alignment Codex prompts, runs `codex exec`, interprets strict terminal words `COMPLETE` or `STOP`, otherwise increments `current_round` and creates the next prompt. Shared helpers in `hooks/lib/loop-common.sh:27-44`, `48-58`, `199-232` locate active loops, read current round, and rename `state.md` into terminal state files. Template rendering is centralized in `hooks/lib/template-loader.sh:11-16`, `21-33`, `43-117`, `155-188`.
- inputs_outputs_state: Main user input is a markdown plan path plus CLI options. Setup rejects absolute paths at `scripts/setup-rlcr-loop.sh:220-224`, spaces at `226-232`, shell metacharacters at `234-241`, symlinks at `246-250`, missing parent/file at `252-263`, out-of-project paths at `265-277`, wrong tracking mode at `298-326`, short plans at `332-340`, missing Codex at `342-348`, unsafe branch/model/effort values at `354-382`. Outputs are loop files documented in `README.md:242-267`: `state.md`, copied `plan.md`, `goal-tracker.md`, round prompt/summary/review files, and cache logs under `$HOME/.cache/humanize`. State transitions use `current_round` in `state.md`; failed review updates it with `sed` and writes `round-N-prompt.md` in `hooks/loop-codex-stop-hook.sh:882-917`. Terminal states are produced by renaming state via `end_loop`, not by editing a status enum in place.
- gates_or_invariants: Only the newest timestamped directory with `state.md` is active, per `hooks/lib/loop-common.sh:23-44`; terminal files are ignored by that lookup. Plan file content must match its loop backup on every Stop, including committed changes for tracked plans, via `hooks/loop-codex-stop-hook.sh:148-216`. Active branch must equal `start_branch` in both prompt validation and Stop, from `hooks/loop-plan-file-validator.sh:82-91` and `hooks/loop-codex-stop-hook.sh:130-142`. Goal tracker immutable section can only be initialized during round 0; later direct writes/edits are blocked by `hooks/loop-write-validator.sh:105-113`, `hooks/loop-edit-validator.sh:91-99`, and `hooks/loop-bash-validator.sh:90-105`. Summary files must be current-round and in the active loop directory, enforced by `hooks/loop-write-validator.sh:115-180`, `hooks/loop-edit-validator.sh:101-131`, and `hooks/loop-read-validator.sh:83-133`. Prompt and state files are protected; todos markdown files are disallowed in favor of TodoWrite. Git push is blocked unless `push_every_round` is true in `hooks/loop-bash-validator.sh:48-65`; Stop blocks dirty git and optionally unpushed commits in `hooks/loop-codex-stop-hook.sh:338-432`.
- dependencies_and_callers: `commands/start-rlcr-loop.md` calls `scripts/setup-rlcr-loop.sh`; `commands/cancel-rlcr-loop.md:11-31` locates the newest loop and renames `state.md` to `cancel-state.md`. Hook scripts source `hooks/lib/loop-common.sh`, which sources `hooks/lib/template-loader.sh` at `hooks/lib/loop-common.sh:11-17`. Stop hook uses `hooks/check-todos-from-transcript.py` at `hooks/loop-codex-stop-hook.sh:224-253`, `jq` for hook JSON responses, git for branch/status checks, Codex CLI for review, and `scripts/portable-timeout.sh` for cross-platform timeouts. Templates in `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude` define validator responses and review/next-round prompts. CI routes algorithm coverage through `.github/workflows/plan-file-test.yml:33-51`, `.github/workflows/template-test.yml:26-63`, and `.github/workflows/shell-syntax-check.yml:19-61`.
- edge_cases_or_failure_modes: Missing or old state schema blocks with a schema message in `hooks/loop-plan-file-validator.sh:46-76` and Stop `hooks/loop-codex-stop-hook.sh:107-124`. Plan backup missing, original plan deleted, uncommitted tracked plan changes, or content mismatch all block before Codex review in `hooks/loop-codex-stop-hook.sh:151-216`. Incomplete TodoWrite state blocks before expensive review via `hooks/check-todos-from-transcript.py:17-84` and Stop `226-253`. Large modified code/docs files over 2000 lines block in `hooks/loop-codex-stop-hook.sh:256-336`. Codex failures, missing review file, or empty review file are converted to retryable block responses in `hooks/loop-codex-stop-hook.sh:726-826`. `STOP` is honored even outside full-alignment rounds, with a different warning path, in `hooks/loop-codex-stop-hook.sh:848-876`.
- validation_or_tests: Tests are shell-based and recursive across the plugin surface. `tests/test-plan-file-validation.sh` covers plan path, git, tracking, branch/model/effort validation. `tests/test-plan-file-hooks.sh` covers prompt-submit validation, plan backup protection, quoted YAML parsing, bypass attempts, and Stop-hook plan integrity checks. `tests/test-state-exit-naming.sh` covers active-loop discovery and terminal state rename names. `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh` cover template loading/rendering/fallback/syntax/reference behavior. I inspected tests but did not execute them, per the research-only instruction.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-031 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template subsystem, which is core because hook block messages, Codex review prompts, next-round prompts, and alignment action instructions are loaded/rendered through `hooks/lib/template-loader.sh`.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` at `tests/test-templates-comprehensive.sh:15-17`, computes `TEMPLATE_DIR`, tracks pass/fail/warning counters, and then validates the prompt-template tree in seven sections. It verifies required subdirectories `block`, `codex`, and `claude` at `64-80`; loads every markdown template with `load_template` at `85-107`; scans every template for malformed placeholder syntax at `116-194`; unit-tests malformed placeholder detection patterns at `199-259`; stress-tests `render_template` edge cases at `264-439`; verifies fallback and directory validation behavior at `443-490`; renders selected real templates at `495-540`; and stress-renders all templates with dummy values at `563-606`. It exits success only if `TESTS_FAILED` is zero at `619-625`.
- inputs_outputs_state: Inputs are the repository’s `prompt-template/**/*.md` files and `hooks/lib/template-loader.sh`. Temporary state is held in shell variables: `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, `TEMPLATE_COUNT`, `LOAD_FAILURES`, `syntax_errors`, and transient `TEMP_TEST_DIR`/`temp_dir`. Outputs are human-readable PASS/FAIL/WARN lines and a process exit status. It creates temporary directories with `mktemp -d` in malformed placeholder and incomplete-directory tests, then removes them at `258-259` and `490`.
- gates_or_invariants: Requires template directory and all three subdirectories to exist, otherwise fails early or records failures. Every `.md` template must load non-empty content unless only whitespace, which becomes a warning rather than hard failure at `96-105`. Placeholder syntax must use uppercase names inside `{{VAR}}` without extra braces or spaces; extra close/open braces, wrong bracket types, unclosed placeholders, spaced placeholders, and lowercase placeholders are treated as syntax errors at `128-175`. `load_and_render_safe` must use fallback for missing templates and real content for existing templates at `446-463`. `validate_template_dir` must reject missing or incomplete directories at `466-490`.
- dependencies_and_callers: Depends directly on `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` from `hooks/lib/template-loader.sh`. It is called by `.github/workflows/template-test.yml:34-40` after the smaller template-loader unit test and before template reference validation. It indirectly validates all block, Codex, and Claude templates consumed by `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: Covers empty content, single/multiple lines, empty-line preservation, regex-looking values, backslashes, quotes, CJK-like text using ASCII placeholder wording, ASCII status markers, markdown formatting, long 1000-character values, multiline values, variables at line start/end, repeated variables, adjacent variables, missing templates, incomplete template dirs, and rapid repeated calls. A notable test limitation appears around true non-ASCII coverage: the comment says CJK/emoji edge cases, but the actual values in `348-365` are ASCII-safe strings such as `Chinese Text Here` and `[OK] - Success!`. The script also tolerates unreplaced placeholders as warnings during all-template rendering at `590-597`, so not every missing dummy substitution is a hard failure.
- validation_or_tests: This file is itself the validation artifact. It includes integration checks for real templates: `block/wrong-round-number.md` at `498-514`, `block/unpushed-commits.md` at `517-528`, and `codex/goal-tracker-update-section.md` at `531-540`. The final summary and exit gate are at `608-625`. I inspected the file directly and did not run it.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-061 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Claude-facing prompt fragment appended to the next round after a full goal-alignment Codex review. It defines the post-alignment transition focus: address forgotten work, unmet acceptance criteria, and unjustified deferrals.
- algorithmic_behavior: The template is static markdown with no placeholders. Its content at `prompt-template/claude/post-alignment-action-items.md:2-7` adds a heading and three action categories: Forgotten Items, AC Status, and Deferred Items. It is appended by `hooks/loop-codex-stop-hook.sh:905-910` only when the round that just failed review is a full-alignment round, determined by `CURRENT_ROUND % 5 == 4` earlier in `hooks/loop-codex-stop-hook.sh:559-563`. This means rounds after 4, 9, 14, etc. receive extra corrective instructions.
- inputs_outputs_state: Input is implicit: the Stop hook’s current round number and the existence/content of this template file. Output is additional text appended to `round-${NEXT_ROUND}-prompt.md`. It does not mutate loop state directly; state advancement happens immediately before prompt creation via `sed`/`mv` on `state.md` in `hooks/loop-codex-stop-hook.sh:882-885`.
- gates_or_invariants: The append is conditional and tolerant. If `load_template` returns content, it is appended; if missing or empty, no fallback is used for this fragment and the next prompt proceeds without the section. The template relies on the Codex full-alignment review contract in `prompt-template/codex/full-alignment-review.md:19-48` and `59-93`, which asks Codex to audit acceptance criteria, forgotten items, deferred items, and stagnation. It does not use templated variables, so it is covered by syntax validation as a no-placeholder file.
- dependencies_and_callers: Sole direct caller found is `hooks/loop-codex-stop-hook.sh:905-910`. It is part of the broader Claude prompt chain with `prompt-template/claude/next-round-prompt.md` loaded at `hooks/loop-codex-stop-hook.sh:900-903`, `prompt-template/claude/next-round-footer.md` at `916-917`, optional push note at `920-925`, and goal tracker update request at `928-933`. It is validated by template discovery/loading tests in `tests/test-templates-comprehensive.sh:90-107` and syntax checks in `118-194`.
- edge_cases_or_failure_modes: Because it is advisory and static, failure mode is mainly absence: a missing or empty template silently omits post-alignment emphasis. The template also says “un-defer them now” at line `7`; this is intentionally directive but depends on Codex’s prior review being accurate and on later hook gates preventing direct goal-tracker modification after round 0.
- validation_or_tests: Covered generically by comprehensive template loading/syntax/render stress tests and by template reference scanning. The `rg` inspection showed it is directly referenced from the Stop hook append path. I inspected the file directly and did not execute tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_COMMIT_PLAN_FILE_CLI-HZ-001`, `ADD_COMMIT_PLAN_FILE_CLI-HZ-031`, `ADD_COMMIT_PLAN_FILE_CLI-HZ-061`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`