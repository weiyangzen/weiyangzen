# agent_19 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-019 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit gate for an active RLCR loop. It prevents continued prompting when the loop state schema is outdated, the git branch has changed, or the original plan-file tracking mode no longer matches the setup contract. Registered as the UserPromptSubmit hook in `hooks/hooks.json:4-10`.
- algorithmic_behavior: Reads hook stdin into `INPUT` but does not inspect it (`hooks/loop-plan-file-validator.sh:19-20`). Resolves `SCRIPT_DIR`, `PROJECT_ROOT`, sources `hooks/lib/loop-common.sh`, finds the newest active loop under `.humanize-loop.local`, and exits silently if no active loop exists (`hooks/loop-plan-file-validator.sh:13-29`; `hooks/lib/loop-common.sh:27-44`). Extracts YAML frontmatter from `state.md` with `sed`, then parses `plan_tracked`, `plan_file`, and `start_branch` via `grep` and `sed` (`hooks/loop-plan-file-validator.sh:31-40`).
- inputs_outputs_state: Inputs are stdin JSON from the Claude hook, `CLAUDE_PROJECT_DIR` or current working directory, `.humanize-loop.local/<timestamp>/state.md`, and git state for `HEAD`, `ls-files`, and `status --porcelain`. Outputs are either no output with exit 0 for allow, or JSON `{"decision":"block","reason":...}` with exit 0 for hook-level blocking (`hooks/loop-plan-file-validator.sh:58-63`, `84-89`, `105-120`, `128-133`). State is read-only; it does not mutate `state.md`, loop files, git index, or plan files.
- gates_or_invariants: Requires v1.1.2+ state fields `plan_tracked` and `start_branch`; missing fields render `block/schema-outdated.md` through `load_and_render_safe` and block (`hooks/loop-plan-file-validator.sh:42-76`). Enforces branch immutability by comparing `git rev-parse --abbrev-ref HEAD` to `start_branch` (`hooks/loop-plan-file-validator.sh:78-91`). If `plan_tracked:true`, the plan file must still be tracked and clean (`hooks/loop-plan-file-validator.sh:99-122`). Otherwise, the plan file must remain untracked/gitignored from the perspective of `git ls-files` (`hooks/loop-plan-file-validator.sh:123-136`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for active-loop lookup and template setup (`hooks/loop-plan-file-validator.sh:16-24`; `hooks/lib/loop-common.sh:11-21`). Depends transitively on `hooks/lib/template-loader.sh` for `load_and_render_safe`, which falls back if a template is missing or empty (`hooks/lib/template-loader.sh:155-188`). Depends on setup-time state written by `scripts/setup-rlcr-loop.sh`, especially `plan_file`, `plan_tracked`, and `start_branch` (`scripts/setup-rlcr-loop.sh:406-418`). Mirrors setup validation rules that `--track-plan-file` requires tracked and clean, while default mode rejects tracked plan files (`scripts/setup-rlcr-loop.sh:294-326`).
- edge_cases_or_failure_modes: No active loop is allowed silently, which avoids stale hook interference (`hooks/loop-plan-file-validator.sh:26-29`). Legacy quoted `plan_file` and `start_branch` values are stripped for backward compatibility (`hooks/loop-plan-file-validator.sh:33-40`). `plan_tracked` has whitespace removed but is not quote-stripped, so quoted boolean values would not equal `true` and would fall into the untracked branch. `FULL_PLAN_PATH` is computed but unused (`hooks/loop-plan-file-validator.sh:97`); this hook checks tracking cleanliness, not content integrity. Content drift, deleted original plan files, missing backups, and committed plan changes are handled later by the Stop hook via backup and diff checks (`hooks/loop-codex-stop-hook.sh:148-216`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` validates success with valid state, quoted `plan_file`, missing `plan_tracked`, missing `start_branch`, and branch switching (`tests/test-plan-file-hooks.sh:91-185`). It also covers quoted `start_branch`, mismatch under quoted branch, and hyphenated plan paths (`tests/test-plan-file-hooks.sh:341-460`). Setup-time tracking validation is covered by tracked-without-flag, untracked-with-flag, and modified-tracked-with-flag tests (`tests/test-plan-file-validation.sh:321-422`). Stop-hook integrity tests clarify that full plan content verification is outside this assigned hook (`tests/test-plan-file-hooks.sh:469-550`, `562-730`).
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-049 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Blocking-message template used when a Bash command attempts to modify `round-*-summary.md`. Its algorithmic role is policy communication for the Bash validator: summary files must be changed through Write/Edit so other round/path validators can enforce the RLCR state machine.
- algorithmic_behavior: The template is static Markdown with one placeholder, `{{CORRECT_PATH}}` (`prompt-template/block/summary-bash-write.md:1-8`). It tells the agent not to use Bash commands to modify summary files, names the correct path, and explains that shell writers such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/summary-bash-write.md:3-8`).
- inputs_outputs_state: Input is the `CORRECT_PATH` variable supplied by `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:117-124`). Render output is written to stderr by `hooks/loop-bash-validator.sh` before it exits with code 2 (`hooks/loop-bash-validator.sh:122-125`). The template itself has no state writes; it only shapes the denial message returned by the validator.
- gates_or_invariants: The gate is triggered when `command_modifies_file "$COMMAND_LOWER" "round-[0-9]+-summary\.md"` matches a Bash command (`hooks/loop-bash-validator.sh:117-126`). The invariant communicated by the template is that Bash must not be used for summary mutations; Write/Edit is required so correct-round and correct-location checks remain enforceable. `command_modifies_file` detects redirection, append, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns (`hooks/lib/loop-common.sh:155-180`).
- dependencies_and_callers: Direct caller path is `hooks/loop-bash-validator.sh:122-125`, which computes the current round’s expected summary path as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`. Message construction is centralized in `hooks/lib/loop-common.sh:115-124`. Rendering is handled by `load_and_render_safe`, which loads from `prompt-template/block/summary-bash-write.md` and uses the fallback text if the file is missing or empty (`hooks/lib/template-loader.sh:155-188`). Template existence is listed among common template references in `tests/test-template-references.sh:149-167`.
- edge_cases_or_failure_modes: If no active loop exists, the Bash validator exits before this template can be used (`hooks/loop-bash-validator.sh:36-43`). If the template is absent or empty, the fallback in `summary_bash_blocked_message` still blocks and includes `{{CORRECT_PATH}}` after safe rendering (`hooks/lib/loop-common.sh:117-124`; `hooks/lib/template-loader.sh:164-187`). Detection is string-pattern based on the Bash command text, so unusual shell constructs not covered by `command_modifies_file` may evade this specific message, though the helper covers the common write paths listed above. The template references “round number validation,” but the actual round-number enforcement for Write/Edit lives in sibling validators, while this Bash path blocks before any Bash write occurs (`hooks/loop-write-validator.sh:110-160`; `hooks/loop-edit-validator.sh:96-127`).
- validation_or_tests: Template reference coverage checks that `block/summary-bash-write.md` exists as a common template (`tests/test-template-references.sh:149-167`). Template rendering tests cover `CORRECT_PATH` substitution through real templates and the shared loader (`tests/test-template-loader.sh:151-164`; `tests/test-templates-comprehensive.sh:497-510`). Loader tests also exercise special-character substitution and safe fallback behavior (`tests/test-template-loader.sh:135-143`; `tests/test-template-loader.sh:197-221`). No dedicated test was found that invokes a Bash summary modification and asserts this exact template text; coverage is indirect through the caller/reference/rendering tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ADD_COMMIT_PLAN_FILE_CLI-HZ-019, ADD_COMMIT_PLAN_FILE_CLI-HZ-049
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`