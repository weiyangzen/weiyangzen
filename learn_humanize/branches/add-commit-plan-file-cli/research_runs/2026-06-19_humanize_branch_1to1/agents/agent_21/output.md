# agent_21 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-021 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Write gate for the RLCR loop. It prevents Claude Write-tool mutations of loop-owned control files and enforces that round summary writes target the active loop directory and current round. It is registered as the Write matcher in `hooks/hooks.json:14-21`, so it runs before Write tool calls rather than as a batch validator.
- algorithmic_behavior: The hook reads a JSON hook payload from stdin, extracts `.tool_name`, and exits allow-success for non-Write tools (`hooks/loop-write-validator.sh:23-28`). For Write calls, it extracts `.tool_input.file_path`, lowercases it, and applies a cascade of path gates (`hooks/loop-write-validator.sh:30-45`). It immediately blocks `round-*-todos.md` writes with `todos_blocked_message` and `round-*-prompt.md` writes with `prompt_write_blocked_message`, keeping todos in native TodoWrite and prompt files read-only (`hooks/loop-write-validator.sh:37-45`). It then classifies summary files and `.humanize-loop.local/` membership (`hooks/loop-write-validator.sh:51-57`), allowing unrelated writes outside loop state. For loop-local non-summary files, it only continues validation for `state.md`, `goal-tracker.md`, or `plan.md`; other loop-local files are allowed (`hooks/loop-write-validator.sh:59-67`). It locates the active loop via `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize-loop.local`, `find_active_loop`, and `get_current_round` (`hooks/loop-write-validator.sh:73-82`). If no active loop exists, it allows the write (`hooks/loop-write-validator.sh:77-79`).
- inputs_outputs_state: Input is a Claude hook JSON object on stdin with `tool_name` and `tool_input.file_path` (`hooks/loop-write-validator.sh:23-30`). Runtime state comes from `.humanize-loop.local/<active>/state.md`, specifically the `current_round` frontmatter parsed by `get_current_round` in `hooks/lib/loop-common.sh:46-58`. Outputs are process exit codes plus stderr explanations: exit `0` allows the Write; exit `2` blocks it with a rendered template or fallback message. It does not mutate files. Its state transition is purely gate-oriented: a proposed Write is allowed, blocked as loop-protected, blocked as wrong round, or blocked as wrong directory.
- gates_or_invariants: Todos files are never written via Write (`hooks/loop-write-validator.sh:37-40`). Prompt files are never written via Write (`hooks/loop-write-validator.sh:42-45`). `state.md` is immutable to Write (`hooks/loop-write-validator.sh:87-90`). `.humanize-loop.local/**/plan.md` backup is protected (`hooks/loop-write-validator.sh:96-102`). `goal-tracker.md` can only be written in round 0; after that, the hook redirects the agent to a round summary update request (`hooks/loop-write-validator.sh:109-113`). Summary files must be inside the active loop directory (`hooks/loop-write-validator.sh:119-127`), must use the current round number (`hooks/loop-write-validator.sh:145-163`), and must resolve to the exact active-loop path after extracting the relative filename (`hooks/loop-write-validator.sh:133-139`, `hooks/loop-write-validator.sh:169-180`).
- dependencies_and_callers: Depends on `jq`, `sed`, `basename`, `grep`, and Bash strict mode. It sources `hooks/lib/loop-common.sh` (`hooks/loop-write-validator.sh:15-17`), which in turn sources the template loader and initializes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:11-17`). Shared predicates include `is_round_file_type`, `extract_round_number`, `is_goal_tracker_path`, `is_state_file_path`, and `is_in_humanize_loop_dir` (`hooks/lib/loop-common.sh:65-84`, `hooks/lib/loop-common.sh:137-153`). Rendered block messages flow through `load_and_render_safe` from `hooks/lib/template-loader.sh:155-188`, with concrete templates such as `prompt-template/block/todos-file-access.md`, `prompt-template/block/prompt-file-write.md`, `prompt-template/block/state-file-modification.md`, `prompt-template/block/goal-tracker-modification.md`, `prompt-template/block/plan-backup-protected.md`, `prompt-template/block/wrong-summary-location.md`, `prompt-template/block/wrong-round-number.md`, and `prompt-template/block/wrong-directory-path.md`. Caller registration is `hooks/hooks.json:14-21`. The broader template-reference test includes this hook in its scanned scripts (`tests/test-template-references.sh:56-64`) and in critical `load_and_render_safe` coverage (`tests/test-template-references.sh:174-180`).
- edge_cases_or_failure_modes: If `jq` is unavailable or stdin is not valid JSON, strict mode makes extraction fail and the hook can fail hard rather than allow. If `.humanize-loop.local` has no newest directory with `state.md`, writes that would otherwise be protected by active-loop context are allowed after the no-active-loop branch (`hooks/loop-write-validator.sh:75-79`), while todos and prompt round-file gates still occur earlier. `find_active_loop` only checks the newest timestamp-like loop directory and ignores older loops, intentionally avoiding stale loop resurrection (`hooks/lib/loop-common.sh:23-44`). Path matching is text/regex based: `is_in_humanize_loop_dir` requires `.humanize-loop.local/` in the path (`hooks/lib/loop-common.sh:149-153`), so unusual relative paths or symlinked equivalents may bypass directory detection if the literal segment is absent. The directory equality check compares the raw `FILE_PATH` string to the computed `CORRECT_PATH` (`hooks/loop-write-validator.sh:169-180`), so semantically equivalent but non-normalized paths can be blocked. The `plan.md` backup gate is filename-based plus `.humanize-loop.local` substring (`hooks/loop-write-validator.sh:61-67`, `hooks/loop-write-validator.sh:96-102`), which is conservative for loop-local plan backups but does not affect non-loop `plan.md` files.
- validation_or_tests: `tests/test-plan-file-hooks.sh:194-205` directly verifies that `loop-write-validator.sh` blocks Write attempts to `.humanize-loop.local/**/plan.md` with exit `2` and a plan-related message. Template-reference coverage scans `loop-write-validator.sh` for referenced templates (`tests/test-template-references.sh:56-64`) and checks critical validators use safe rendering/fallbacks (`tests/test-template-references.sh:174-180`). I did not run tests in this read-only research pass; evidence is based on direct file inspection and referenced test code.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-051 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the RLCR stop hook when `--push-every-round` is active and the local branch has commits ahead of its upstream. It is a small message surface, not executable logic, but it defines the stop-gate instruction the agent receives when unpushed commits prevent loop exit.
- algorithmic_behavior: The template renders a heading, explains that stopping is blocked because `{{AHEAD_COUNT}}` commits on `{{CURRENT_BRANCH}}` are unpushed, states that `--push-every-round` requires pushing before exit, and provides the exact command shape `git push origin {{CURRENT_BRANCH}}` (`prompt-template/block/unpushed-commits.md:1-12`). The algorithmic decision happens in `hooks/loop-codex-stop-hook.sh`: after the clean-worktree check, if `PUSH_EVERY_ROUND` is true, the stop hook parses `git status -sb` for `ahead N`, extracts the count, gets the current branch, renders this template through `load_and_render_safe`, and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:401-429`).
- inputs_outputs_state: Template inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`, passed by `loop-codex-stop-hook.sh` at render time (`hooks/loop-codex-stop-hook.sh:417-419`). The rendered output becomes the JSON `reason` field returned by the stop hook, paired with a concise `systemMessage` such as a blocked-unpushed-commits message (`hooks/loop-codex-stop-hook.sh:421-428`). It does not read state itself and has no side effects. The state transition is a stop-attempt gate: when the local branch is ahead and push-every-round is enabled, the loop remains active and the agent is instructed to push; after the push removes the ahead status, a later stop attempt can progress to subsequent stop-hook checks.
- gates_or_invariants: The gate only applies when `PUSH_EVERY_ROUND` equals `true` (`hooks/loop-codex-stop-hook.sh:405`). It runs after dirty worktree detection, so uncommitted changes take priority over unpushed commits (`hooks/loop-codex-stop-hook.sh:349-399`). The template preserves the invariant that completed round work should be pushed before exit when push-every-round mode is active (`prompt-template/block/unpushed-commits.md:3-10`). Rendering uses the template loader’s single-pass substitution, so placeholder text in variable values is not recursively expanded (`hooks/lib/template-loader.sh:35-43`, `hooks/lib/template-loader.sh:56-116`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:417`, which uses `load_and_render_safe "$TEMPLATE_DIR" "block/unpushed-commits.md"` with fallback text. That loader comes from `hooks/lib/template-loader.sh:119-131` and safe fallback handling from `hooks/lib/template-loader.sh:155-188`. The stop hook is registered under the Stop hook with a 7200-second timeout in `hooks/hooks.json:52-59`. The template is discovered by repository-wide references in `rg`, including `tests/test-templates-comprehensive.sh:517-528`, which renders it with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`.
- edge_cases_or_failure_modes: Ahead detection relies on `git status -sb | grep -o 'ahead [0-9]*'` (`hooks/loop-codex-stop-hook.sh:407`), so it depends on Git’s short-branch status wording and upstream tracking information. If the branch has no upstream or Git reports status differently, the template may not be used even if commits are effectively unpushed. Detached HEAD falls back to branch name `unknown` only after ahead text is detected (`hooks/loop-codex-stop-hook.sh:409-410`). If the template file is missing or renders empty, `load_and_render_safe` falls back to an inline unpushed-commits message (`hooks/loop-codex-stop-hook.sh:412-419`, `hooks/lib/template-loader.sh:164-187`). The suggested command assumes the remote name is `origin`; this matches the template’s contract but may be wrong in repositories using a different push remote (`prompt-template/block/unpushed-commits.md:7-10`).
- validation_or_tests: `tests/test-templates-comprehensive.sh:517-528` explicitly renders `block/unpushed-commits.md` and checks for the heading, count text, and branch name. The stop-hook code path around `hooks/loop-codex-stop-hook.sh:401-429` supplies the variables and returns the rendered text as a JSON block. I did not find a focused end-to-end test in the inspected snippets that creates an ahead-of-remote branch and asserts this exact Stop response; coverage appears strongest at the template-render level.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item sections present once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`