# agent_28 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-028 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR loop state-file lifecycle. It verifies that an active loop is represented only by `.humanize-loop.local/<timestamp>/state.md`, and that terminal loop outcomes preserve state by renaming that file to `<reason>-state.md` rather than leaving it active.
- algorithmic_behavior: The test creates an isolated temp git repository, seeds loop directories under `.humanize-loop.local`, sources `hooks/lib/loop-common.sh`, and exercises two core functions: `find_active_loop` and `end_loop`. The first behavior block asserts that `find_active_loop` ignores terminal state files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md`, while detecting a literal `state.md` as active; see `tests/test-state-exit-naming.sh:38`, `tests/test-state-exit-naming.sh:51`, `tests/test-state-exit-naming.sh:74`, `tests/test-state-exit-naming.sh:93`, `tests/test-state-exit-naming.sh:110`, `tests/test-state-exit-naming.sh:125`, and `tests/test-state-exit-naming.sh:141`. It also verifies newest timestamp precedence when a later loop directory contains `state.md` at `tests/test-state-exit-naming.sh:157`. The second behavior block verifies that `end_loop` rejects unknown reasons, accepts only the five known reasons, renames `state.md` to the expected terminal filename, and fails cleanly if `state.md` is missing; see `tests/test-state-exit-naming.sh:182`, `tests/test-state-exit-naming.sh:202`, and `tests/test-state-exit-naming.sh:228`.
- inputs_outputs_state: Inputs are filesystem state under a loop base directory, loop timestamp directory names, a `state.md` file with YAML frontmatter, and an exit reason string. Outputs are stdout/stderr signals and file-system state changes. For `find_active_loop`, the output is either the newest active loop directory path or an empty string. The implementation only checks the newest timestamp-like child directory and returns it only if `${newest_dir}state.md` exists; see `hooks/lib/loop-common.sh:23` through `hooks/lib/loop-common.sh:44`. For `end_loop`, input reason values are constrained to `complete`, `cancel`, `maxiter`, `stop`, or `unexpected`; successful output is a move from `state.md` to `${reason}-state.md`, plus diagnostic stderr; see `hooks/lib/loop-common.sh:199` through `hooks/lib/loop-common.sh:231`. The state transition is active to inactive because the active sentinel `state.md` disappears.
- gates_or_invariants: The key invariant is "only `state.md` means active"; README documents this at `README.md:116` and the test enforces it with terminal filenames. A second invariant is newest-directory selection: older active-looking directories are ignored if the newest directory has no `state.md`, and the newest directory wins when it does; the implementation sorts loop directories descending by name at `hooks/lib/loop-common.sh:35` through `hooks/lib/loop-common.sh:40`, and the test covers the positive newest-active case at `tests/test-state-exit-naming.sh:157` through `tests/test-state-exit-naming.sh:175`. A third invariant is strict terminal reason vocabulary: invalid values must not create arbitrary terminal files, enforced in `end_loop` by the case statement at `hooks/lib/loop-common.sh:211` through `hooks/lib/loop-common.sh:219` and tested at `tests/test-state-exit-naming.sh:182` through `tests/test-state-exit-naming.sh:200`.
- dependencies_and_callers: The test depends on `bash`, `git`, `mktemp`, shell file operations, and `hooks/lib/loop-common.sh` sourced at `tests/test-state-exit-naming.sh:64` through `tests/test-state-exit-naming.sh:65`. Production callers of `find_active_loop` include read, write, edit, bash, plan-file, and Codex stop hooks: `hooks/loop-read-validator.sh:58`, `hooks/loop-write-validator.sh:75`, `hooks/loop-edit-validator.sh:60`, `hooks/loop-bash-validator.sh:38`, `hooks/loop-plan-file-validator.sh:24`, and `hooks/loop-codex-stop-hook.sh:52`. Production callers of `end_loop` are in `hooks/loop-codex-stop-hook.sh`: corrupted `current_round` ends as `unexpected` at line 99, max-iteration exhaustion ends as `maxiter` at line 531, Codex `COMPLETE` ends as `complete` at line 844, and Codex `STOP` ends as `stop` at line 874. Manual cancellation is documented separately as a direct rename to `cancel-state.md` in `commands/cancel-rlcr-loop.md:28` through `commands/cancel-rlcr-loop.md:33`.
- edge_cases_or_failure_modes: Empty or missing loop base dirs produce no active loop via `find_active_loop`; the implementation returns empty when the base directory is absent at `hooks/lib/loop-common.sh:30` through `hooks/lib/loop-common.sh:33`. A terminal state file in the newest loop directory must not revive a loop, which the tests cover for all five terminal names except that `complete-state.md` is specifically isolated before any active `state.md` exists. A missing `state.md` during `end_loop` emits "State file not found" and returns nonzero, tested at `tests/test-state-exit-naming.sh:228` through `tests/test-state-exit-naming.sh:239`. Invalid exit reasons emit "Invalid end_loop reason" and return nonzero, tested at `tests/test-state-exit-naming.sh:182` through `tests/test-state-exit-naming.sh:200`. One subtle production behavior is that `find_active_loop` only evaluates the newest loop directory; this intentionally prevents older directories from becoming active after abnormal exits, as documented in `hooks/lib/loop-common.sh:23` through `hooks/lib/loop-common.sh:26`.
- validation_or_tests: This file is itself the validation artifact. It increments pass/fail counters, reports per-case failures with expected and actual values, and exits with the number of failures at `tests/test-state-exit-naming.sh:241` through `tests/test-state-exit-naming.sh:250`. I did not execute it because the assignment asks for research notes only and the branch export is read-only; the file inspection was direct with line-numbered reads.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-058 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing prompt block that defines the sanctioned request path for changing `goal-tracker.md` after Round 0. It is not executable code, but it is algorithmically relevant because it encodes the handoff contract between Claude's work summary and Codex's later review/update authority.
- algorithmic_behavior: The template instructs Claude to include a `Goal Tracker Update Request` section in its summary only when tracker changes are needed. The required structure has `Requested Changes` bullets and a `Justification` paragraph; see `prompt-template/claude/goal-tracker-update-request.md:2` through `prompt-template/claude/goal-tracker-update-request.md:14`. The examples define allowed transition requests: mark task completed with evidence, add open issues, record plan evolution, or defer a task with acceptance-criteria impact. The final sentence states that Codex reviews the request and updates the tracker only if justified at `prompt-template/claude/goal-tracker-update-request.md:16`.
- inputs_outputs_state: Input is Claude's assessment that the Goal Tracker needs mutation after doing work or discovering new information. Output is a structured markdown section in a round summary, not a direct mutation. The state transition is indirect: Claude proposes changes; Codex later evaluates and, if approved, mutates `goal-tracker.md`. This is paired with `prompt-template/claude/next-round-prompt.md`, which tells Claude the tracker is read-only after Round 0 and to use the update-request section instead at lines 22 through 31. The same gate is reinforced when a direct modification is blocked: `prompt-template/block/goal-tracker-modification.md:3` through `prompt-template/block/goal-tracker-modification.md:25`.
- gates_or_invariants: The invariant is that after Round 0, Claude cannot directly modify `goal-tracker.md`; it must request changes through the summary. The template preserves a review gate by requiring justification tied to the Ultimate Goal, then deferring authority to Codex. The Codex-side companion template makes this explicit: Codex must evaluate whether the request is justified, update tracker sections if approved, never modify the immutable Ultimate Goal and Acceptance Criteria, or explain rejection; see `prompt-template/codex/goal-tracker-update-section.md:1` through `prompt-template/codex/goal-tracker-update-section.md:17`.
- dependencies_and_callers: The primary caller is `hooks/loop-codex-stop-hook.sh`, which loads `claude/goal-tracker-update-request.md` and appends it to the next round prompt after the footer and optional push note; see `hooks/loop-codex-stop-hook.sh:928` through `hooks/loop-codex-stop-hook.sh:933`. If the template cannot be loaded, the hook falls back to a one-line instruction at `hooks/loop-codex-stop-hook.sh:930` through `hooks/loop-codex-stop-hook.sh:931`. Related enforcement flows include validator messaging through `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:185` through `hooks/lib/loop-common.sh:197`, and the next-round prompt reference to read-only tracker behavior in `prompt-template/claude/next-round-prompt.md:22` through `prompt-template/claude/next-round-prompt.md:31`.
- edge_cases_or_failure_modes: If Claude omits the section despite needing a tracker update, Codex has no structured request to evaluate and the tracker can become stale. If the request lacks evidence or justification, the Codex-side gate can reject it under the companion instructions. If the template file is missing or empty, the stop hook degrades to a shorter fallback instruction, which preserves the existence of the request path but loses the richer examples and justification format. If Claude attempts direct writes instead, the block template redirects it back to the same request pattern.
- validation_or_tests: There is no direct test for this exact Claude template in the inspected test hits. The adjacent Codex-side goal-tracker update template is covered by `tests/test-templates-comprehensive.sh:531` through `tests/test-templates-comprehensive.sh:540`, which verifies rendering of `codex/goal-tracker-update-section.md` with a concrete tracker path. The production load path for this Claude template includes a fallback in `hooks/loop-codex-stop-hook.sh:928` through `hooks/loop-codex-stop-hook.sh:933`, which is the main runtime resilience mechanism.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item evidence sections, matching assigned_item_count
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`