# agent_30 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`
- scope_note: inspected assigned files directly under the read-only branch export; no files modified.

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-030 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable validation spec for prompt-template integrity. It verifies that hook scripts referencing templates point at existing markdown files, and that critical validators use safe template rendering so blocking messages do not collapse to empty output when a template is missing.
- algorithmic_behavior: The script has four ordered sections. Section 1 builds a fixed `SCRIPTS_TO_CHECK` list covering `hooks/loop-codex-stop-hook.sh`, read/write/edit/bash validators, and `hooks/lib/loop-common.sh` (`tests/test-template-references.sh:54-64`). It scans non-comment lines for `load_template`, `load_and_render`, or `load_and_render_safe` calls using literal `"$TEMPLATE_DIR"` and a quoted template path, extracts that second argument with `sed`, then checks `prompt-template/<path>` exists (`tests/test-template-references.sh:87-110`). Section 2 enumerates every `prompt-template/**/*.md` file and warns if a template is not directly referenced from `hooks/` (`tests/test-template-references.sh:119-142`). Section 3 hard-codes common block templates expected by `loop-common.sh` message functions and fails if any are absent (`tests/test-template-references.sh:147-167`). Section 4 inspects read/write/edit validators and fails if they use plain `load_and_render` instead of `load_and_render_safe` (`tests/test-template-references.sh:172-201`).
- inputs_outputs_state: Inputs are the repo root derived from the test file location, `prompt-template/`, and the fixed hook script paths (`tests/test-template-references.sh:14-17`, `tests/test-template-references.sh:57-64`). Mutable script-local state is held in counters `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, plus arrays `MISSING_TEMPLATES`, `TEMPLATE_FILES`, and `UNREFERENCED` (`tests/test-template-references.sh:25-27`, `tests/test-template-references.sh:71-72`, `tests/test-template-references.sh:121-132`). Outputs are colored PASS/FAIL/WARN lines, a summary, optional missing-template list, and process status: exit 0 only when `TESTS_FAILED` is zero; exit 1 otherwise (`tests/test-template-references.sh:206-230`).
- gates_or_invariants: Hard gates are: every direct template reference found in the configured hook scripts must exist; the six `COMMON_TEMPLATES` must exist; read/write/edit validators must use `load_and_render_safe` for all render calls. Soft gates are: missing hook scripts only warn and continue (`tests/test-template-references.sh:74-78`), and unreferenced templates only warn because some may be dynamic (`tests/test-template-references.sh:130-141`). The script directly protects the assigned `prompt-template/claude/next-round-prompt.md` because Section 1 finds its stop-hook reference and Section 2 verifies it is directly referenced.
- dependencies_and_callers: This is a standalone Bash validation script; no in-repo caller was found by `rg`, so it appears intended for manual or CI-style validation. It depends on Bash arrays/process substitution, `find`, `grep`, `sed`, `basename`, and the hook/template layout. It validates references to helpers implemented in `hooks/lib/template-loader.sh`: `load_template` returns empty on missing files (`hooks/lib/template-loader.sh:21-32`), `render_template` performs single-pass `{{VAR}}` replacement (`hooks/lib/template-loader.sh:43-117`), and `load_and_render_safe` falls back when a template is missing or renders empty (`hooks/lib/template-loader.sh:155-188`).
- edge_cases_or_failure_modes: The scanner only catches same-line calls with literal `"$TEMPLATE_DIR"` followed by a quoted static path, so dynamic template names, unquoted paths, or multi-line argument layouts can be missed. A missing script path does not fail the run, which avoids brittle tests but can hide coverage loss. Section 2 can count references from comments or non-call text because it uses a broad `grep -rq "\"$template\"" "$PROJECT_ROOT/hooks/"`. Empty existing templates are not caught by the existence checks, though runtime `load_and_render_safe` would fall back if rendering returns empty.
- validation_or_tests: I ran `bash tests/test-template-references.sh` in the branch export. Result: 74 passed, 0 failed, 1 warning. The warning was `block/codex-review-failed.md` not directly referenced, marked by the script as possibly OK if dynamic. The test explicitly passed `claude/next-round-prompt.md` as both existing and directly referenced.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-060 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR transition from a Codex review that found issues into the next Claude implementation round. It is not executable shell code, but it is algorithmically central because the stop hook renders it into the next round prompt that blocks exit and directs the next worker pass.
- algorithmic_behavior: The template first tells Claude that work is unfinished and requires “ultrathink” (`prompt-template/claude/next-round-prompt.md:1`). It forces the original plan to be reviewed through `@{{PLAN_FILE}}` before proceeding (`prompt-template/claude/next-round-prompt.md:3-8`). It then imposes a Todo policy: create Todos for all discovered issues, ordered by importance, and attempt every issue rather than only the highest-priority ones (`prompt-template/claude/next-round-prompt.md:12-14`). It embeds Codex feedback between explicit review-result markers using `{{REVIEW_CONTENT}}` (`prompt-template/claude/next-round-prompt.md:16-19`). Finally, it requires reading `@{{GOAL_TRACKER_FILE}}` before work and makes `goal-tracker.md` read-only after Round 0, with changes routed through a summary “Goal Tracker Update Request” (`prompt-template/claude/next-round-prompt.md:22-31`).
- inputs_outputs_state: Inputs are three render variables supplied by `hooks/loop-codex-stop-hook.sh`: `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` (`hooks/loop-codex-stop-hook.sh:900-903`). `PLAN_FILE` originates from state frontmatter (`hooks/loop-codex-stop-hook.sh:69-77`), `REVIEW_CONTENT` is read from the Codex review result file (`hooks/loop-codex-stop-hook.sh:828-829`), and `GOAL_TRACKER_FILE` is `$LOOP_DIR/goal-tracker.md` (`hooks/loop-codex-stop-hook.sh:465`). Output is rendered markdown written as the first body of `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:887-903`). The template itself does not mutate state; the caller increments `current_round` before rendering (`hooks/loop-codex-stop-hook.sh:882-885`).
- gates_or_invariants: The prompt-level invariants are plan alignment, exhaustive issue tracking, review-result consumption, goal-tracker awareness, and no direct goal-tracker modification after Round 0. Runtime template integrity is guarded by `load_and_render_safe`, which uses a fallback if this file is missing or empty (`hooks/loop-codex-stop-hook.sh:891-903`; `hooks/lib/template-loader.sh:155-188`). Placeholder injection is constrained by the loader’s single-pass renderer: content inserted through `REVIEW_CONTENT` is not rescanned for more `{{...}}` substitutions (`hooks/lib/template-loader.sh:39-42`, `hooks/lib/template-loader.sh:56-58`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which renders this template when Codex review found issues and the loop should continue. After this template is written, the hook may append post-alignment action items, always appends the next-round footer, conditionally appends a push note, and appends the goal-tracker update-request template (`hooks/loop-codex-stop-hook.sh:905-933`). The rendered prompt is then used as the JSON block reason sent back by the hook (`hooks/loop-codex-stop-hook.sh:935-940`). The template is validated by `tests/test-template-references.sh`, which found the stop-hook reference and confirmed the file exists.
- edge_cases_or_failure_modes: If the template is missing or empty at runtime, `load_and_render_safe` falls back to a much shorter generic prompt, which preserves loop continuity but loses the stricter all-issues Todo requirement and goal-tracker instructions. If a required render variable were not supplied, the loader would leave the placeholder literal because unknown placeholders are preserved (`hooks/lib/template-loader.sh:100-107`); the current stop-hook caller supplies all three. Very large `REVIEW_CONTENT` will expand the prompt directly. Because this is prompt text, enforcement depends partly on model compliance; separate validators and stop-hook gates enforce protected-file behavior around plan and goal-tracker writes.
- validation_or_tests: Direct file inspection confirms 31 lines and all three expected placeholders. The validation command `bash tests/test-template-references.sh` passed with 74 passed, 0 failed, and 1 warning unrelated to this template. `rg` found the only direct runtime render site at `hooks/loop-codex-stop-hook.sh:900`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`