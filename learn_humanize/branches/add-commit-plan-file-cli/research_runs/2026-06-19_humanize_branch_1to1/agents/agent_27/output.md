# agent_27 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-027 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable black-box specification for the `scripts/setup-rlcr-loop.sh` plan-file CLI validation pipeline. The script exercises the setup command as a user would invoke it, asserting accepted and rejected plan paths, git repository preconditions, tracking policy, branch-name safety, option parsing, and Codex model parameter validation. It is core algorithm evidence because it defines the expected validation contract around the new plan-file CLI surface rather than only testing presentation.

- algorithmic_behavior: The test constructs isolated temporary git repositories, invokes `scripts/setup-rlcr-loop.sh`, captures stdout/stderr plus exit code, and classifies each scenario through substring checks. Its helper setup creates a repo with one initial commit, a `plans/test-plan.md`, and a `.gitignore` entry for `plans/`, which models the default untracked/gitignored plan-file mode at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:36). It uses `mock_codex` to add a fake `codex` executable only when the real command is missing, so validation can proceed past CLI prerequisite checks without requiring Codex in the test environment at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:69). The underlying implementation under test parses positional and `--plan-file` inputs, then merges them with an exclusivity check at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:181), validates git repository and commit presence at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:204), validates plan path safety at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:220), enforces tracking policy at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:298), and only then creates loop state, plan backup, goal tracker, and round-0 prompt at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:388).

- inputs_outputs_state: Inputs are CLI invocations of `setup-rlcr-loop.sh` with combinations of positional plan paths, `--plan-file`, `--track-plan-file`, and `--codex-model`. Outputs observed by the test are process exit status and diagnostics such as “relative path”, “not found”, “directory not found”, “cannot contain spaces”, “shell metacharacters”, “symbolic link”, “git repository”, “at least one commit”, “gitignored”, “tracked in git”, “clean”, “YAML-unsafe”, and “invalid characters”. The successful setup path writes `.humanize-loop.local/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, and `round-0-prompt.md`; the implementation records `plan_file`, `plan_tracked`, `start_branch`, Codex config, and iteration limits in YAML frontmatter at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:406). The test itself maintains `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED`, then exits with the failure count at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:587).

- gates_or_invariants: The test encodes these invariants: plan paths must be relative, existing, non-symlink files under the project root; parent directories get a distinct missing-directory diagnostic; paths with whitespace or shell metacharacters are rejected; `../` escape is rejected through canonical path resolution; the current directory must be a git repo with at least one commit; default plan files must be untracked/gitignored; `--track-plan-file` flips the policy to require a tracked, clean plan file; `--plan-file` cannot be combined with a positional plan; branch names and Codex model/effort values must be YAML-safe. Corresponding implementation gates are visible in the absolute-path check at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:220), whitespace check at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:226), metacharacter check at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:234), canonical root containment check at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:265), submodule exclusion at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:279), and YAML-safety checks at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:354). The assigned test header lists submodule rejection as intended coverage at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:8), but this script does not include a concrete submodule setup case; the implementation has the gate.

- dependencies_and_callers: The test depends on `/bin/bash`, git, filesystem temp directories via `mktemp -d`, and optionally a real or mocked `codex` executable. Its direct target is `scripts/setup-rlcr-loop.sh`, referenced through `PROJECT_ROOT` computed from the test directory at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:16). The setup script then coordinates with loop hooks and templates by writing state that includes `plan_file` and `plan_tracked`; hook tests in `tests/test-plan-file-hooks.sh` validate those fields are later consumed, including quoted `plan_file` parsing at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-hooks.sh:75). Downstream hook files that use these state fields include `hooks/loop-plan-file-validator.sh` and `hooks/loop-codex-stop-hook.sh`, found by repository references.

- edge_cases_or_failure_modes: The test covers absolute-path input, nonexistent leaf file, nonexistent parent directory, directory and filename whitespace, a sweep of shell metacharacters, symlink plan file, permission/path-resolution failure, project-root escape using `../`, non-git directory, git repo with no commits, tracked plan file without `--track-plan-file`, untracked plan file with `--track-plan-file`, dirty tracked plan file with `--track-plan-file`, YAML-unsafe branch names when git permits creation, invalid Codex model/effort characters, and valid model names with dots and hyphens. The metacharacter loop currently calls the setup script with filenames that do not exist, but because implementation checks metacharacters before file existence, the expected diagnostic is still the security failure at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:176). A minor test-harness weakness is that this loop calls `pass "Path with shell metacharacters rejected"` after the loop even if an earlier iteration already called `fail` and broke, so a failing metacharacter can still add a pass count at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:183). Another coverage mismatch is the header’s “Submodule rejection” claim without a direct test case.

- validation_or_tests: This assigned file is itself validation. It declares the tested behaviors in comments at [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-validation.sh:5), runs numbered scenarios from Test 1 through Test 14, and reports pass/fail/skip totals. I inspected it directly and followed the implementation it targets, but did not execute it because the assignment requested research notes only and the branch export is read-only/no-modification.

- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-057 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Small prompt/block template used by the RLCR hook validation layer when a user attempts to write a round summary file outside the active loop directory. It is algorithm-adjacent rather than an algorithm implementation: the control flow lives in `hooks/loop-write-validator.sh`, while this file defines the rendered block text for that specific validation transition.

- algorithmic_behavior: The template contains a Markdown heading, one invariant sentence, and a `{{CORRECT_PATH}}` placeholder at [prompt-template/block/wrong-summary-location.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/prompt-template/block/wrong-summary-location.md:1). The caller detects a `Write` tool request, lowercases the target path, classifies `round-<n>-summary.md` filenames through shared helpers, checks whether the path is outside `.humanize-loop.local/`, computes the canonical active summary path as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`, renders this template with `CORRECT_PATH`, writes the rendered reason to stderr, and exits with code 2 at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:119). This makes the template part of a gate response: it does not decide the gate, but it names the invariant and directs the actor to the only acceptable output path.

- inputs_outputs_state: Template input is the single variable `CORRECT_PATH`, supplied by the hook caller at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:124). The value is derived from active-loop state: `find_active_loop` selects the newest `.humanize-loop.local/<timestamp>` directory containing `state.md`, and `get_current_round` reads `current_round` from that state file at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/lib/loop-common.sh:27) and [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/lib/loop-common.sh:48). Output is Markdown text headed “Wrong Summary Location” with the correct path substituted. The hook state transition is “allowed write” to “blocked write” for summary files outside the loop directory, represented by process exit code 2 rather than any file mutation.

- gates_or_invariants: The invariant is explicit: summary files must be in the loop directory. The gate activates only for `tool_name == "Write"` at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:23), only when the target matches the shared `round-[0-9]+-summary.md` pattern at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/lib/loop-common.sh:67), and only when the target is not inside `.humanize-loop.local/` as determined by `is_in_humanize_loop_dir` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/lib/loop-common.sh:149). If there is no active loop, the hook exits successfully without blocking at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:77). Inside the loop directory, later gates validate wrong round number and wrong directory path separately, so this template is specifically scoped to outside-loop summary writes.

- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh`, which sources `hooks/lib/loop-common.sh` and uses `load_and_render_safe` from the template-loader stack. The setup script creates the expected summary target as `round-0-summary.md` in the loop directory at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:519), then instructs the user to write the summary to that path at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/scripts/setup-rlcr-loop.sh:629). The template is also discoverable in repository-wide template tests by name, although the targeted real-template integration snippets I inspected focus on the neighboring `wrong-round-number.md` template rather than this specific file.

- edge_cases_or_failure_modes: If the template file is missing or invalid, `load_and_render_safe` falls back to an inline “Wrong Summary Location” message in the hook at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:121), so the validation gate can still block. If the path resembles a summary file but the round number differs while still in `.humanize-loop.local`, the wrong-round-number template handles it later at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:145). If the path is in `.humanize-loop.local` but not exactly the active loop directory, the wrong-directory-path gate handles it at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:169). Because `is_in_humanize_loop_dir` is a string match, a path containing `.humanize-loop.local/` is routed to deeper directory validation rather than this outside-location template.

- validation_or_tests: The assigned template’s path is referenced by the hook call at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/hooks/loop-write-validator.sh:124). Hook tests around the write validator verify related blocking behavior for protected loop files, including `Write` tool inputs and expected exit code 2 at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-plan-file-hooks.sh:194). Template-loader tests validate the shared rendering machinery with real block templates and variable substitution at [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-template-loader.sh:147) and [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-commit-plan-file-cli/tests/test-templates-comprehensive.sh:494). I did not find a direct test case that specifically writes `round-N-summary.md` outside `.humanize-loop.local` and asserts this exact template text, so this file is covered through caller presence and shared rendering tests more than through a dedicated behavioral assertion.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`