# agent_24 add-commit-plan-file-cli 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `20b3ebae90ba6b39c335a1dbdec89affc90c386e`

## Item Evidence

### ADD_COMMIT_PLAN_FILE_CLI-HZ-024 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes the RLCR loop runtime for `/humanize:start-rlcr-loop`. It is the command’s setup/activation engine: parses CLI options, validates the plan and repository boundary, creates a timestamped `.humanize-loop.local/<timestamp>/` state directory, backs up the plan, writes `state.md`, seeds `goal-tracker.md`, writes the initial `round-0-prompt.md`, and emits the prompt plus completion requirements to the current Claude session.
- algorithmic_behavior: The script starts with strict shell mode via `set -euo pipefail` at `scripts/setup-rlcr-loop.sh:11`, defines defaults for Codex model, effort, timeout, and max iterations at `scripts/setup-rlcr-loop.sh:17-21`, then parses positional and flag arguments in a single `while`/`case` loop at `scripts/setup-rlcr-loop.sh:97-173`. It accepts one plan file either positionally or through `--plan-file`, rejects duplicate plan sources at `scripts/setup-rlcr-loop.sh:181-188`, and requires a nonempty plan at `scripts/setup-rlcr-loop.sh:190-198`. After validation, it records the starting branch, creates `.humanize-loop.local/<timestamp>/`, copies the plan to `plan.md`, writes state frontmatter, heuristically extracts goal and acceptance criteria into a tracker, and builds the first implementation prompt from the backed-up plan at `scripts/setup-rlcr-loop.sh:388-572`. The command wrapper `commands/start-rlcr-loop.md:10-14` delegates directly to this script through `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS`, making the script the authoritative startup path.
- inputs_outputs_state: Inputs are shell arguments: `<path/to/plan.md>`, `--plan-file`, `--track-plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--push-every-round` at `scripts/setup-rlcr-loop.sh:40-56`. Runtime inputs also include `CLAUDE_PROJECT_DIR` or `pwd` for `PROJECT_ROOT` at `scripts/setup-rlcr-loop.sh:179`, current git repository state, the plan file content, the current branch, and availability of `codex`. Outputs are files under `.humanize-loop.local/<timestamp>/`: `plan.md` backup at `scripts/setup-rlcr-loop.sh:396-397`, `state.md` with `current_round`, `max_iterations`, Codex settings, push policy, plan path, tracking mode, start branch, and timestamp at `scripts/setup-rlcr-loop.sh:406-419`, `goal-tracker.md` at `scripts/setup-rlcr-loop.sh:425-513`, `round-0-summary.md` path reservation at `scripts/setup-rlcr-loop.sh:519`, and `round-0-prompt.md` at `scripts/setup-rlcr-loop.sh:521-572`. It also prints the setup summary and initial prompt to stdout at `scripts/setup-rlcr-loop.sh:586-638`. State transition is from no active timestamped loop to an active loop directory whose `state.md` starts at `current_round: 0`.
- gates_or_invariants: It requires a git repository and at least one commit at `scripts/setup-rlcr-loop.sh:204-214`. Plan paths must be relative at `scripts/setup-rlcr-loop.sh:220-224`, contain no spaces at `scripts/setup-rlcr-loop.sh:226-232`, contain no listed shell metacharacters at `scripts/setup-rlcr-loop.sh:234-241`, not be symlinks at `scripts/setup-rlcr-loop.sh:246-250`, have an existing parent directory and file at `scripts/setup-rlcr-loop.sh:252-263`, resolve inside `PROJECT_ROOT` at `scripts/setup-rlcr-loop.sh:265-277`, and not be inside a submodule at `scripts/setup-rlcr-loop.sh:279-292`. Tracking policy is bifurcated: with `--track-plan-file`, the plan must be tracked and clean at `scripts/setup-rlcr-loop.sh:301-315`; without it, the plan must not be tracked and should be gitignored at `scripts/setup-rlcr-loop.sh:316-326`. The plan must have at least five lines at `scripts/setup-rlcr-loop.sh:332-340`, `codex` must be available at `scripts/setup-rlcr-loop.sh:342-348`, branch names must avoid YAML-unsafe characters at `scripts/setup-rlcr-loop.sh:354-364`, Codex model names are restricted to alphanumeric, dot, underscore, and hyphen at `scripts/setup-rlcr-loop.sh:366-373`, and Codex effort is restricted to alphanumeric, underscore, and hyphen at `scripts/setup-rlcr-loop.sh:375-382`.
- dependencies_and_callers: Direct caller is the slash command file `commands/start-rlcr-loop.md`, which grants only the setup script as an allowed Bash tool at `commands/start-rlcr-loop.md:1-5` and executes it at `commands/start-rlcr-loop.md:10-14`. Runtime dependencies are `git`, `wc`, `dirname`, `basename`, `grep`, `sed`, `head`, `tail`, `date`, `mkdir`, `cp`, `cat`, and `codex`. The state it creates is consumed by hooks and loop controllers: `hooks/loop-read-validator.sh` finds the active loop under `.humanize-loop.local` and reads `current_round` from `state.md` at `hooks/loop-read-validator.sh:56-65`; shared helpers implement newest-loop selection and current-round extraction at `hooks/lib/loop-common.sh:23-58`; hook registration wires read, write, edit, bash, user-prompt, and stop hooks for this loop at `hooks/hooks.json:1-63`. The setup script’s generated state fields `plan_file`, `plan_tracked`, and `start_branch` are also expected by plan-file validation tests, which build representative `state.md` files at `tests/test-plan-file-hooks.sh:75-85`.
- edge_cases_or_failure_modes: The script fails early on unsupported CLI shapes: missing option arguments, nonnumeric `--max` or `--codex-timeout`, unknown options, multiple positional plan files, both `--plan-file` and positional path, or no plan file at `scripts/setup-rlcr-loop.sh:102-172` and `scripts/setup-rlcr-loop.sh:181-198`. Path hardening rejects absolute paths, whitespace, metacharacters, symlinks, missing parent dirs, missing files, path resolution failures, project escapes, and submodule-contained plans at `scripts/setup-rlcr-loop.sh:220-292`. Tracking mismatches are explicit: tracked plan without `--track-plan-file` fails, untracked plan with `--track-plan-file` fails, and modified tracked plan with `--track-plan-file` fails at `scripts/setup-rlcr-loop.sh:298-326`. Branch or Codex values that would make unquoted YAML unsafe are rejected at `scripts/setup-rlcr-loop.sh:354-382`. A notable behavior is that `MAX_ITERATIONS=0` is accepted by the regex because it checks digits only at `scripts/setup-rlcr-loop.sh:107-111`, despite the error text saying positive integer; that can create a loop whose stop hook may immediately hit the max-iteration path. Another operational edge is that `PROJECT_ROOT="${CLAUDE_PROJECT_DIR:-$(pwd)}"` is not canonicalized before the resolved path prefix check at `scripts/setup-rlcr-loop.sh:179` and `scripts/setup-rlcr-loop.sh:273-276`, so unusual symlinked project-root invocations could affect containment comparison.
- validation_or_tests: `tests/test-plan-file-validation.sh` directly exercises setup-script validation. It checks rejection of absolute paths, missing files, missing parent directories, paths with spaces, shell metacharacters, and symlinks at `tests/test-plan-file-validation.sh:85-196`; project escapes, non-git repositories, and repositories without commits at `tests/test-plan-file-validation.sh:241-319`; tracked/untracked/modified tracking policy at `tests/test-plan-file-validation.sh:321-422`; `--plan-file` behavior and duplicate plan source rejection at `tests/test-plan-file-validation.sh:520-540`; and Codex model/effort character validation at `tests/test-plan-file-validation.sh:547-585`. `tests/test-plan-file-hooks.sh` verifies downstream hook expectations for `state.md`, including `plan_file`, `plan_tracked`, and `start_branch`, with valid and missing-field cases at `tests/test-plan-file-hooks.sh:75-159`. I did not run these tests because this branch export is read-only and the user requested research notes only.
- skip_candidate: `no`

### ADD_COMMIT_PLAN_FILE_CLI-HZ-054 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: A runtime block-message template used by the RLCR read validator when a user or agent tries to read a round prompt or summary file from outside the active `.humanize-loop.local/<timestamp>/` loop directory. It is part of the guard surface that redirects reads to the authoritative current-round files.
- algorithmic_behavior: The template itself is declarative Markdown with three placeholders: `{{FILE_PATH}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{CURRENT_ROUND}}`. It tells the agent it is trying to read `{{FILE_PATH}}`, states that loop files are in `{{ACTIVE_LOOP_DIR}}/`, lists the current round prompt and summary paths, and provides a direct `cat {{FILE_PATH}}` fallback hint at `prompt-template/block/wrong-file-location.md:1-9`. The algorithmic behavior is activated in `hooks/loop-read-validator.sh`: for Read tool calls, the hook identifies prompt/summary round files, finds the active loop, gets `current_round`, and if the requested file is not under `.humanize-loop.local/`, renders this template through `load_and_render_safe` at `hooks/loop-read-validator.sh:87-96`, then exits with code `2` to block the read.
- inputs_outputs_state: Inputs are injected template variables from the read validator: `FILE_PATH` from hook JSON `.tool_input.file_path` at `hooks/loop-read-validator.sh:29-30`, `ACTIVE_LOOP_DIR` from `find_active_loop "$LOOP_BASE_DIR"` at `hooks/loop-read-validator.sh:56-59`, and `CURRENT_ROUND` from `get_current_round "$ACTIVE_LOOP_DIR/state.md"` at `hooks/loop-read-validator.sh:64`. Output is rendered Markdown written to stderr by the hook at `hooks/loop-read-validator.sh:92-95`, followed by a blocking exit status at `hooks/loop-read-validator.sh:96`. The template does not mutate state; it communicates the existing active-loop state derived from `.humanize-loop.local/<timestamp>/state.md`.
- gates_or_invariants: The template is only used after several gates pass. The hook ignores non-Read tools at `hooks/loop-read-validator.sh:23-27`, blocks todos file reads separately at `hooks/loop-read-validator.sh:32-39`, exits for non-prompt/non-summary paths at `hooks/loop-read-validator.sh:45-47`, exits if there is no active loop at `hooks/loop-read-validator.sh:56-62`, and exits if no round number can be extracted at `hooks/loop-read-validator.sh:70-73`. The wrong-location gate specifically requires `IN_HUMANIZE_LOOP_DIR=false`, computed through `is_in_humanize_loop_dir` at `hooks/loop-read-validator.sh:49-50` and implemented as a substring check for `.humanize-loop.local/` at `hooks/lib/loop-common.sh:149-153`. Rendering is safe-fallback guarded: if the template is missing or empty, `load_and_render_safe` substitutes an inline fallback instead of letting the hook fail at `hooks/lib/template-loader.sh:152-188`.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh` at `hooks/loop-read-validator.sh:87-96`. That hook is registered for Read PreToolUse events in `hooks/hooks.json:33-40`. Template loading depends on `hooks/lib/loop-common.sh`, which sources `hooks/lib/template-loader.sh` at `hooks/lib/loop-common.sh:11-17`; the template directory is resolved as `<plugin-root>/prompt-template` by `get_template_dir` at `hooks/lib/template-loader.sh:9-16`. Placeholder substitution uses `render_template`, a single-pass `awk` renderer designed to avoid placeholder injection from variable values at `hooks/lib/template-loader.sh:35-117`. The active-loop discovery and round parsing helpers are in `hooks/lib/loop-common.sh:23-84`.
- edge_cases_or_failure_modes: If no active loop exists, the template is never shown and the read is allowed at `hooks/loop-read-validator.sh:60-62`. If a wrong-location file path contains the substring `.humanize-loop.local/` but is not actually the active loop directory, it bypasses this specific template and may be handled by the later wrong-directory path gate at `hooks/loop-read-validator.sh:122-132`. If the requested filename lacks a parseable `round-N-summary.md` or `round-N-prompt.md` pattern, the hook exits at `hooks/loop-read-validator.sh:70-73`. The template’s final line says `If you need this file, use: cat {{FILE_PATH}}` at `prompt-template/block/wrong-file-location.md:9`; in the actual wrong-location case, that advice appears to repeat the blocked path rather than the computed `CORRECT_PATH`, although the preceding current-round list gives the active loop locations. If the template file is absent, malformed, or renders empty, the safe loader emits the fallback from `hooks/loop-read-validator.sh:89-91` through the fallback path in `hooks/lib/template-loader.sh:164-185`.
- validation_or_tests: Template infrastructure tests verify real-template rendering and placeholder behavior: `tests/test-template-loader.sh` exercises `load_and_render` with a real block template and placeholder substitution at `tests/test-template-loader.sh:147-164`; `tests/test-templates-comprehensive.sh` validates template directory checks and real template rendering at `tests/test-templates-comprehensive.sh:473-525`. Cross-reference tests ensure critical validators use `load_and_render_safe`, including `loop-read-validator.sh`, at `tests/test-template-references.sh:170-200`. Search evidence shows `prompt-template/block/wrong-file-location.md` is directly referenced only by `hooks/loop-read-validator.sh` at `hooks/loop-read-validator.sh:92`, matching its narrow guard role. I did not run tests because this branch export is read-only and the user requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item sections above; item IDs are present only in their Item Evidence headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`