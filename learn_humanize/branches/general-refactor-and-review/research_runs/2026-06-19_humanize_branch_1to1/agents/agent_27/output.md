# agent_27 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-027 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the repository test aggregator’s ANSI-aware result parsing. The file specifically validates the portable escape stripping used by `tests/run-all-tests.sh` before extracting `Passed:` and `Failed:` totals from colored test-suite output.
- algorithmic_behavior: The script defines colored local `pass`/`fail` reporters and counters at `tests/test-ansi-parsing.sh:13-30`, then runs eight fixtures through the same stripping and extraction pattern used by the aggregate runner. The central algorithm is `esc=$'\033'` followed by `sed "s/${esc}\\[[0-9;]*m//g"` at `tests/test-ansi-parsing.sh:45-48`, repeated through the cases. Count extraction uses `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'` for passed counts and the analogous `Failed:` pattern for failed counts at `tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`, and `tests/test-ansi-parsing.sh:124-125`.
- inputs_outputs_state: Inputs are synthetic Bash strings containing ANSI SGR sequences, plain text summary lines, and multi-line simulated suite output. Outputs are human-readable PASS/FAIL lines plus a final summary at `tests/test-ansi-parsing.sh:163-168`; process output is an exit status of `0` when `TESTS_FAILED` remains zero and `1` otherwise at `tests/test-ansi-parsing.sh:170-178`. Internal state is limited to `TESTS_PASSED` and `TESTS_FAILED`, incremented only by the reporter functions at `tests/test-ansi-parsing.sh:21-30`.
- gates_or_invariants: The invariant is that color-stripped content must preserve parseable `Passed:`/`Failed:` numeric summaries across GNU/Linux and BSD/macOS `sed`; the comments name this portability goal at `tests/test-ansi-parsing.sh:3-6` and `tests/test-ansi-parsing.sh:38-40`. Another gate is aggregation correctness: the runner should treat a suite as failed when either the suite exits nonzero or the parsed failed count is greater than zero, as implemented in `tests/run-all-tests.sh:58-79`.
- dependencies_and_callers: This file is listed in the aggregate test suite array at `tests/run-all-tests.sh:32-45`. The behavior under test is duplicated in the runner at `tests/run-all-tests.sh:64-68`, where aggregate output is stripped and the last `Passed:`/`Failed:` counts are extracted. Runtime dependencies are Bash, `sed`, `grep`, `tail`, and ANSI-C quoting support via Bash.
- edge_cases_or_failure_modes: Covered edge cases include multiple color spans in one line at `tests/test-ansi-parsing.sh:55-67`, colored numeric passed and failed counts at `tests/test-ansi-parsing.sh:69-95`, zero failed count at `tests/test-ansi-parsing.sh:97-109`, multi-line output with summary and colored failure text at `tests/test-ansi-parsing.sh:111-130`, no ANSI codes at `tests/test-ansi-parsing.sh:132-144`, and combined bold/color SGR codes at `tests/test-ansi-parsing.sh:146-158`. A residual limitation is that the sed regex only removes SGR color/style sequences ending in `m`; it does not attempt to strip non-SGR cursor/control sequences, which is consistent with the runner’s current parsing need.
- validation_or_tests: Static syntax validation passed with `bash -n tests/test-ansi-parsing.sh`. Direct execution of `tests/test-ansi-parsing.sh` completed with exit code `0`, reporting `Passed: 8` and `Failed: 0`. The file also self-validates as part of `tests/run-all-tests.sh` because it is included in the ordered `TEST_SUITES` array.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-057 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Block prompt template for the stop-hook gate that prevents loop exit when `--push-every-round` is enabled and the current branch has commits ahead of its remote. It is a policy/message component rather than executable logic, but it directly defines the user-facing remediation contract for an algorithmic transition gate.
- algorithmic_behavior: The template has two placeholders: `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:3` and `prompt-template/block/unpushed-commits.md:9`. When rendered, it explains that stopping is blocked, names the unpushed commit count and branch, states the `--push-every-round` requirement, and provides the exact `git push origin {{CURRENT_BRANCH}}` command at `prompt-template/block/unpushed-commits.md:5-10`.
- inputs_outputs_state: Inputs come from hook-derived variables: `AHEAD_COUNT` is parsed from `git status -sb` ahead text, and `CURRENT_BRANCH` is resolved with `git rev-parse --abbrev-ref HEAD` at `hooks/loop-codex-stop-hook.sh:477-491`. Output is markdown text assigned to `REASON`, then embedded in a JSON block decision as `reason` with `systemMessage` at `hooks/loop-codex-stop-hook.sh:493-500`. The template itself has no mutable state.
- gates_or_invariants: The gate only runs when `PUSH_EVERY_ROUND` is exactly `true` at `hooks/loop-codex-stop-hook.sh:477`; otherwise unpushed commits are allowed by this specific stop-hook check. `PUSH_EVERY_ROUND` originates as a CLI option defaulting to false in `scripts/setup-rlcr-loop.sh:34-42`, is documented at `scripts/setup-rlcr-loop.sh:54-63`, is persisted into loop state at `scripts/setup-rlcr-loop.sh:485-493`, and is parsed back from state with a false default at `hooks/lib/loop-common.sh:117-135`. The invariant is that a branch-ahead condition under this mode yields a block decision rather than permitting exit.
- dependencies_and_callers: The direct caller is `load_and_render_safe "$TEMPLATE_DIR" "block/unpushed-commits.md"` in `hooks/loop-codex-stop-hook.sh:489-491`. Rendering is handled by the template loader: `load_template` reads the file at `hooks/lib/template-loader.sh:36-47`, `render_template` performs single-pass `{{VAR}}` substitution at `hooks/lib/template-loader.sh:50-131`, and `load_and_render_safe` falls back if the template is missing or empty at `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: If this template is missing or renders empty, the stop hook falls back to the inline fallback message defined at `hooks/loop-codex-stop-hook.sh:484-488`. If `git status -sb` does not contain an `ahead N` fragment, no block is emitted by this gate. If branch resolution fails inside this block, `CURRENT_BRANCH` becomes `unknown` via the hook fallback at `hooks/loop-codex-stop-hook.sh:482`, and the rendered push command would target `unknown`; earlier branch consistency checks reduce but do not entirely eliminate that failure mode. The message assumes pushing to `origin` is correct; alternate upstream remote names are not represented in this template.
- validation_or_tests: Comprehensive template tests render this exact template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then assert the result contains the heading, `3 unpushed`, and the branch name at `tests/test-templates-comprehensive.sh:517-528`. Template reference validation scans hook references and verifies referenced template files exist at `tests/test-template-references.sh:56-107`; the same test also reports referenced template coverage across `prompt-template` at `tests/test-template-references.sh:121-142`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence headings present; each assigned item has one section`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`