# agent_24 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-024 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes an RLCR loop session for `/humanize:start-rlcr-loop`. It validates a user-provided implementation plan, captures loop configuration, creates `.humanize/rlcr/<timestamp>/` state artifacts, backs up the plan, seeds the goal tracker, and emits the first round prompt that drives Claude work plus later Codex review. The slash command wrapper calls this script directly from `commands/start-rlcr-loop.md:1-14`.
- algorithmic_behavior: The script is a strict bootstrap pipeline. It parses CLI flags for plan path, max iterations, Codex model/effort/timeout, tracked-plan behavior, and per-round push policy at `scripts/setup-rlcr-loop.sh:34-180`. It validates repository prerequisites, requiring a git repo and at least one commit at `scripts/setup-rlcr-loop.sh:186-220`. It validates the plan path as relative, space-free, shell-metacharacter-free, non-symlink, resolvable, inside the project, and outside submodules at `scripts/setup-rlcr-loop.sh:227-305`. It then validates plan tracking policy: without `--track-plan-file`, the plan must be untracked/gitignored; with `--track-plan-file`, it must be tracked and clean at `scripts/setup-rlcr-loop.sh:311-356`. Plan content must have at least 5 physical lines and at least 3 non-comment content lines, with blank lines, `#` comments, single-line HTML comments, and multi-line HTML comments excluded at `scripts/setup-rlcr-loop.sh:362-415`. It requires `codex` on `PATH` at `scripts/setup-rlcr-loop.sh:417-423`, records and sanitizes branch/model/effort fields for YAML state safety at `scripts/setup-rlcr-loop.sh:429-460`, then creates the loop directory, copies the plan backup, writes `state.md`, writes `goal-tracker.md`, writes `round-0-prompt.md`, and prints activation/work-completion instructions at `scripts/setup-rlcr-loop.sh:467-720`.
- inputs_outputs_state: Inputs are the positional or `--plan-file` relative markdown plan, optional `--track-plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--push-every-round`, plus environment `CLAUDE_PROJECT_DIR` for project root selection at `scripts/setup-rlcr-loop.sh:186`. The main outputs are `.humanize/rlcr/<timestamp>/plan.md` as immutable backup at `scripts/setup-rlcr-loop.sh:467-476`, `.humanize/rlcr/<timestamp>/state.md` with frontmatter fields `current_round: 0`, `max_iterations`, Codex config, push policy, plan file, tracking flag, start branch, and start time at `scripts/setup-rlcr-loop.sh:485-498`, `.humanize/rlcr/<timestamp>/goal-tracker.md` at `scripts/setup-rlcr-loop.sh:504-594`, and `.humanize/rlcr/<timestamp>/round-0-prompt.md` plus expected summary path `round-0-summary.md` at `scripts/setup-rlcr-loop.sh:600-653`. The primary state transition is “no active loop” to “active loop”: the presence of newest `.humanize/rlcr/<timestamp>/state.md` is later used by `find_active_loop` in `hooks/lib/loop-common.sh:58-79`. Later transitions are owned by the stop hook, which parses this state at `hooks/loop-codex-stop-hook.sh:66-89`, renames it through `end_loop` on terminal outcomes at `hooks/lib/loop-common.sh:301-334`, or increments `current_round` for the next blocked iteration at `hooks/loop-codex-stop-hook.sh:967-970`.
- gates_or_invariants: The script fails closed on missing plan, bad argument values, non-git repo, no commits, unsafe path forms, symlinked plan file, parent directory resolution failure, project-directory escape, submodule placement, disallowed tracking state, insufficient content, missing Codex CLI, YAML-unsafe branch/model/effort values, or timed-out git checks where explicitly detected. It enforces immutable-plan loop semantics by copying the plan into the loop directory and recording `plan_file`/`plan_tracked` in state; the stop hook later verifies backup presence and plan content equality at `hooks/loop-codex-stop-hook.sh:170-238`. It also embeds behavioral rules into the round-0 prompt: initialize the goal tracker before work, maintain task/AC mapping, commit changes, write the round summary, and do not exit by lying, editing state, or canceling the loop at `scripts/setup-rlcr-loop.sh:602-653`.
- dependencies_and_callers: Direct dependencies are Bash with `set -euo pipefail`, git, `wc`, `grep`, `sed`, `awk`, `dirname`, `date`, `cp`, `mkdir`, and Codex CLI. It sources `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:26-28`; that helper selects `gtimeout`, GNU `timeout`, Python timeout, or no-timeout fallback in `scripts/portable-timeout.sh:9-71`. The slash command allows only this setup script through `commands/start-rlcr-loop.md:1-14`. Downstream consumers include `hooks/lib/loop-common.sh`, which parses the generated state fields at `hooks/lib/loop-common.sh:96-137`; `hooks/loop-codex-stop-hook.sh`, which enforces branch consistency, plan integrity, work-summary presence, goal tracker initialization, Codex review, and round/termination transitions; and write/edit/bash/read validators that depend on the active-loop directory and current round.
- edge_cases_or_failure_modes: A plan path with spaces is rejected for YAML serialization consistency at `scripts/setup-rlcr-loop.sh:233-239`, so valid filesystem paths with spaces are intentionally unsupported. Paths containing shell metacharacters are rejected before file existence checks at `scripts/setup-rlcr-loop.sh:241-248`; this blocks injection but also disallows characters that could be harmless in filenames. Markdown headings beginning with `#` are treated as comments during content validation at `scripts/setup-rlcr-loop.sh:402-405`, so a heading-heavy plan needs at least three non-heading content lines. The HTML comment scanner handles single-line comments before entering multi-line mode at `scripts/setup-rlcr-loop.sh:391-400`, preventing a single closed comment from swallowing subsequent content. Git timeouts are checked for some commands, but if no timeout implementation exists, `portable-timeout.sh` runs commands without a timeout and prints a warning at `scripts/portable-timeout.sh:64-69`. The state file is generated as unquoted YAML scalar values at `scripts/setup-rlcr-loop.sh:485-498`, so branch/model/effort validation rejects unsafe characters up front; older quoted state files are still accepted downstream by `parse_state_file` quote stripping at `hooks/lib/loop-common.sh:119-129`.
- validation_or_tests: `tests/test-plan-file-validation.sh` directly targets this script’s path, git, tracking, content, option, branch, and Codex parameter validation. Representative coverage includes absolute/nonexistent paths, spaces, shell metacharacters, symlink rejection, project escape, non-git and no-commit repos at `tests/test-plan-file-validation.sh:89-323`; tracking policy at `tests/test-plan-file-validation.sh:329-426`; branch YAML-safety at `tests/test-plan-file-validation.sh:428-514`; content validation for blank, sparse, HTML-comment-only, `#`-comment-only, and valid plans at `tests/test-plan-file-validation.sh:515-661`; `--plan-file` parsing at `tests/test-plan-file-validation.sh:663-694`; and Codex model/effort character validation at `tests/test-plan-file-validation.sh:696-738`. `tests/test-plan-file-hooks.sh` verifies downstream state/plan integrity assumptions, including quoted state parsing and stop-hook plan modification/deletion/backup-missing blocks at `tests/test-plan-file-hooks.sh:91-185` and `tests/test-plan-file-hooks.sh:388-560`. I did not run tests because the branch export is read-only and even a `git rev-parse` probe attempted to write macOS toolchain cache files under `/tmp`, which the sandbox denied.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-054 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Defines the user-facing block message for attempts to modify RLCR loop `state.md`. It is a small prompt/template contract that protects the loop’s authoritative state cursor from direct user/agent mutation.
- algorithmic_behavior: The template itself is declarative, not executable. It states that `state.md` cannot be modified because the loop system manages it, and summarizes the protected contents as current round number, max iterations, and Codex configuration at `prompt-template/block/state-file-modification.md:1-10`. The executable behavior is provided by callers: `state_file_blocked_message` loads this template with fallback support at `hooks/lib/loop-common.sh:208-215`; write validation blocks `Write` to any `state.md` path under the active loop at `hooks/loop-write-validator.sh:91-98`; edit validation blocks `Edit` to state files at `hooks/loop-edit-validator.sh:75-82`; and bash validation blocks shell commands that appear to modify `state.md` at `hooks/loop-bash-validator.sh:71-79`. The path detector for `state.md` is `is_state_file_path` at `hooks/lib/loop-common.sh:245-249`.
- inputs_outputs_state: Inputs are implicit: a validator detects a tool action targeting `state.md` or a shell command that matches a state-file modification pattern. The template output is a markdown denial reason sent to stderr by PreToolUse validators, typically with exit code 2 in write/edit/bash validators. It does not update state; its purpose is to prevent unauthorized state transitions. Legitimate state transitions happen elsewhere: setup creates `state.md`; stop-hook issue loops rewrite `current_round` atomically with temp file plus `mv` at `hooks/loop-codex-stop-hook.sh:967-970`; `end_loop` renames `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` at `hooks/lib/loop-common.sh:301-334`.
- gates_or_invariants: The invariant is that loop state is system-owned. Direct edits to `state.md` are rejected because corrupting fields such as current round, iteration cap, or Codex config can bypass loop review or break round selection. The block pairs with schema and safety checks elsewhere: `loop-codex-stop-hook.sh` revalidates Codex model/effort from state at `hooks/loop-codex-stop-hook.sh:90-101`, validates numeric `current_round` and `max_iterations` at `hooks/loop-codex-stop-hook.sh:103-112`, requires newer state fields `plan_tracked` and `start_branch` at `hooks/loop-codex-stop-hook.sh:114-131`, and blocks branch drift at `hooks/loop-codex-stop-hook.sh:133-164`.
- dependencies_and_callers: The template depends on the template loader path resolution in `hooks/lib/template-loader.sh:24-31`, safe template loading/rendering at `hooks/lib/template-loader.sh:167-203`, and the shared message function in `hooks/lib/loop-common.sh:208-215`. Direct callers are `hooks/loop-write-validator.sh:95-97`, `hooks/loop-edit-validator.sh:79-81`, and `hooks/loop-bash-validator.sh:76-78`. The write/edit validators receive JSON hook input and inspect `.tool_input.file_path`; bash validation receives JSON hook input and inspects `.tool_input.command` through `command_modifies_file`, whose redirection/tee/sed/perl/mv/cp/rm/dd/truncate/exec patterns are defined at `hooks/lib/loop-common.sh:260-285`.
- edge_cases_or_failure_modes: The template is intentionally static and has no variables, so rendering cannot fail due to missing substitutions. If the template file is absent or empty, `load_and_render_safe` returns an inline fallback instead of failing the validator at `hooks/lib/template-loader.sh:176-203`; therefore the gate remains functional even if this file is missing. Detection is path/pattern based: `is_state_file_path` matches any lowercase path ending in `state.md`, while `command_modifies_file` has known limits for complex shell forms and variable-indirected filenames; tests explicitly note that some multi-source `cp` patterns are not detected for analogous filenames at `tests/test-bash-validator-patterns.sh:165-168`. Reads of `state.md` are not blocked by this template; read validation is focused on wrong round files, prompts/summaries, and todos at `hooks/loop-read-validator.sh:32-142`.
- validation_or_tests: `tests/test-template-references.sh` includes `block/state-file-modification.md` in the common-template existence set at `tests/test-template-references.sh:149-167`, proving the shared loader expects this file. `tests/test-bash-validator-patterns.sh` validates the lower-level command detection for state file modification patterns, including `echo x > state.md`, `sed -i ... state.md`, and non-modifying `cat state.md` at `tests/test-bash-validator-patterns.sh:175-183`. The broader validator tests exercise active-loop state parsing and file access rules, though I did not find a dedicated write/edit test case that asserts this exact template text is emitted for direct `state.md` Write/Edit attempts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 required item sections present; each assigned item header appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`