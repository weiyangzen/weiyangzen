# agent_01 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the full Humanize Claude Code plugin algorithm surface. It implements an RLCR loop where `/start-rlcr-loop` seeds a plan-backed loop, Claude works and writes round summaries, tool hooks enforce loop invariants, and a Stop hook invokes Codex review to either continue, complete, stop, or max out the loop.
- algorithmic_behavior: The command entrypoint `commands/start-rlcr-loop.md:1-14` delegates to `scripts/setup-rlcr-loop.sh`. Setup parses options and validates the plan path, Git repository, tracking policy, content, branch name, Codex model/effort, then creates `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:104-180`, `211-305`, `311-423`, `467-653`). Runtime hooks are registered in `hooks/hooks.json:4-63`: `UserPromptSubmit` validates state/plan consistency, `PreToolUse` validates Read/Write/Edit/Bash operations, and `Stop` runs the Codex review loop. The stop hook performs preflight gates, builds regular or full-alignment Codex prompts, runs `codex exec`, then treats exact final-line `COMPLETE` as success, exact final-line `STOP` as circuit breaker, otherwise increments `current_round` and writes the next prompt (`hooks/loop-codex-stop-hook.sh:114-238`, `240-606`, `608-710`, `716-930`, `933-1033`).
- inputs_outputs_state: Inputs are a relative plan file, command options (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--track-plan-file`), Claude hook JSON, transcript JSONL, Git state, templates, and Codex output. Outputs are loop artifacts under `.humanize/rlcr/<timestamp>/`, cache/debug files under `$HOME/.cache/humanize/...`, hook JSON block decisions, rendered Markdown block messages, terminal state files, and shell test pass/fail summaries. Loop state is active only while newest loop dir contains `state.md`; terminal transitions rename it via `end_loop` to `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md` (`hooks/lib/loop-common.sh:301-334`, `README.md:242-266`).
- gates_or_invariants: Plan intake rejects absolute paths, spaces, shell metacharacters, symlinks, missing/unreadable files, parent-dir escape, submodule paths, non-Git repos, repos without commits, invalid tracked/untracked mode, too-few lines/content lines, missing Codex, YAML-unsafe branches, and invalid model/effort (`scripts/setup-rlcr-loop.sh:211-423`, `429-461`). Runtime gates require schema fields `plan_tracked` and `start_branch`, unchanged branch, unchanged plan backup, completed native todos, successful Git status, no large changed code/docs files over 2000 lines, clean Git, pushed commits when configured, current-round summary, and initialized Round 0 goal tracker (`hooks/loop-codex-stop-hook.sh:119-238`, `248-326`, `336-503`, `512-606`). Tool guards keep prompt/state/plan backup files read-only or system-owned, block stale round files, and restrict goal-tracker edits after Round 0 (`hooks/loop-read-validator.sh:36-142`, `hooks/loop-write-validator.sh:37-190`, `hooks/loop-edit-validator.sh:36-140`, `hooks/loop-bash-validator.sh:59-146`).
- dependencies_and_callers: `commands/` are Claude slash-command surfaces. `scripts/portable-timeout.sh:10-71` abstracts `gtimeout`, GNU `timeout`, Python timeout, or no-timeout fallback. `hooks/lib/loop-common.sh:15-334` owns shared constants, state parsing, active-loop selection, filename parsing, allowlist logic, mutation detection, and terminal rename semantics. `hooks/lib/template-loader.sh:24-222` provides template directory discovery, single-pass `{{VAR}}` rendering via awk, safe fallbacks, and directory validation. `prompt-template/` defines Codex review contracts, Claude next-round prompts, and block messages. `hooks/check-todos-from-transcript.py:20-129` scans the latest TodoWrite call in multiple transcript formats. `.github/workflows/plan-file-test.yml:1-51`, `template-test.yml:1-65`, and `shell-syntax-check.yml:1-61` provide CI coverage; `tests/run-all-tests.sh:32-45` aggregates all local suites.
- edge_cases_or_failure_modes: Git operations are designed to fail closed on timeout/failure in setup and hooks. Stop-hook fallback copies Codex stdout into the expected review-result file if Codex did not create it directly (`hooks/loop-codex-stop-hook.sh:858-875`) and blocks on missing/empty review results (`876-911`). Latest timestamp wins for active loop discovery, so older directories with `state.md` are ignored (`hooks/lib/loop-common.sh:58-79`). Allowlisted historical files are narrow: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`hooks/lib/loop-common.sh:168-186`). `.gitignore:4-8` keeps `.humanize*` and Python cache artifacts local. Git metadata was not available in this read-only export, so source commit was taken from scheduler metadata.
- validation_or_tests: Tests cover template rendering, bash mutation-pattern detection, todo transcript parsing, plan-file validation, template reference existence, terminal state naming, comprehensive template validation, plan-file hooks, error fallbacks, ANSI parsing, and allowlist behavior (`tests/run-all-tests.sh:32-45`). CI runs focused plan-file tests and hook tests on relevant changes (`.github/workflows/plan-file-test.yml:1-51`) plus template and shell syntax tests. I did not execute tests because this branch export is read-only and the suites create temp repos/files.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-031 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan intake and startup validation. It codifies the boundary between acceptable loop requirements input and unsafe or ambiguous plan/state initialization.
- algorithmic_behavior: The test creates isolated temp Git repositories, generates plan files, mocks `codex` when absent, invokes `setup-rlcr-loop.sh` with different CLI arguments, captures stdout/stderr and exit code, and asserts expected accept/reject behavior (`tests/test-plan-file-validation.sh:36-84`). It groups cases into path validation, tracking validation, branch validation, content validation, CLI option handling, and Codex parameter validation (`86-738`).
- inputs_outputs_state: Inputs are synthetic repos, generated `plans/*.md`, Git tracked/untracked states, branch names, chmod/symlink fixtures, and command-line options. Outputs are colored PASS/FAIL/SKIP lines and final process exit equal to `TESTS_FAILED` (`tests/test-plan-file-validation.sh:23-34`, `740-749`). It unsets `CLAUDE_PROJECT_DIR` so setup uses the temp repo working directory instead of the real plugin root (`19-21`), then mutates temp repo state with `git init`, commits, `.gitignore`, branch checkouts, and file edits.
- gates_or_invariants: Path gates assert rejection of absolute paths, nonexistent files, nonexistent parent dirs, path/filename spaces, shell metacharacters, symlinks, permission/path-resolution errors, `../` escape, non-Git repos, and Git repos without commits (`tests/test-plan-file-validation.sh:89-323`). Tracking gates assert tracked plans require `--track-plan-file`, untracked plans cannot use it, and modified tracked plans are rejected (`326-426`). Branch gates assert YAML-unsafe names containing colon, hash, or quotes are rejected by either Git or setup (`429-513`). Content gates assert blank, sparse, HTML-comment-only, and hash-comment-only plans are rejected while content-bearing plans are not rejected for insufficient content (`515-661`). CLI/model gates cover `--plan-file`, mutually exclusive plan path forms, invalid Codex model/effort characters, and valid dotted/hyphenated model names (`664-738`).
- dependencies_and_callers: Depends directly on `scripts/setup-rlcr-loop.sh`, plus Git, `mktemp`, `chmod`, shell heredocs, and a mocked `codex` executable added to temp `PATH` when needed (`tests/test-plan-file-validation.sh:73-83`). The setup script under test depends on `scripts/portable-timeout.sh` and implements the corresponding validation logic at `scripts/setup-rlcr-loop.sh:211-423` and `429-461`. CI calls this test in `.github/workflows/plan-file-test.yml:38-42`; aggregate local runner calls it from `tests/run-all-tests.sh:32-45`.
- edge_cases_or_failure_modes: Some branch-name cases are accepted as pass if Git itself rejects the branch before setup can run (`tests/test-plan-file-validation.sh:460-475`, `477-513`). The chmod permission case may skip if permissions cannot be changed (`202-243`). “Accept” tests generally assert only absence of a specific validation error, allowing later unrelated failures such as Codex or environment issues (`610-661`, `727-738`). The header lists submodule rejection (`8-10`), but this file does not contain an explicit submodule fixture; submodule handling is implemented in setup at `scripts/setup-rlcr-loop.sh:292-305`, so this is a coverage gap. The metacharacter loop can emit a failure and then still print a pass summary for the group because line `187` is unconditional after the loop.
- validation_or_tests: This is itself a validation artifact. It verifies the plan-file startup contract against real setup-script execution in temp repos and returns nonzero on accumulated failures (`tests/test-plan-file-validation.sh:740-749`). Not executed here due read-only branch export.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-061 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template for the Read validator when Claude tries to read a prompt or summary file whose `round-N` does not match the active loop’s `current_round`.
- algorithmic_behavior: The template renders a Markdown warning with the attempted stale round/file type, the current round, and the active current prompt/summary paths (`prompt-template/block/wrong-round-file.md:1-9`). It is invoked by `hooks/loop-read-validator.sh` after the hook extracts `CLAUDE_ROUND`, determines `FILE_TYPE`, loads active state, and detects round mismatch (`hooks/loop-read-validator.sh:69-88`, `110-122`).
- inputs_outputs_state: Inputs are template variables `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH` supplied by the read validator (`hooks/loop-read-validator.sh:116-121`). Output is rendered Markdown written to stderr by the validator, followed by hook exit code `2` to block the Read tool (`hooks/loop-read-validator.sh:116-122`). The template itself does not mutate state.
- gates_or_invariants: Enforces the invariant that Claude reads only the active round’s prompt/summary, unless the exact path is on the small historical allowlist in `hooks/lib/loop-common.sh:168-186`. The read validator first blocks `round-*-todos.md` unless allowlisted, ignores non-round summary/prompt paths, blocks wrong locations, blocks wrong round via this template, and finally blocks wrong active-loop directory paths (`hooks/loop-read-validator.sh:36-142`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for single-pass `{{VAR}}` substitution and fallback behavior (`hooks/lib/template-loader.sh:50-132`, `167-203`). Called only through `load_and_render_safe` in the read validator. Template reference and render behavior are covered by the broader template suites, including reference scanning in `tests/test-template-references.sh` and comprehensive template validation in `tests/test-templates-comprehensive.sh`.
- edge_cases_or_failure_modes: If this template is missing or empty, the read validator uses its inline fallback at `hooks/loop-read-validator.sh:111-115`. The template’s final line says `cat {{FILE_PATH}}` (`prompt-template/block/wrong-round-file.md:9`); this avoids the Read tool path but may be confusing because it points back at the blocked stale file rather than only the current files. Directory mismatch is handled separately after round validation, so a same-basename file in an old loop dir is still blocked by `wrong-directory-path.md` if it passes the round check.
- validation_or_tests: Indirectly validated by template loader/reference tests and read-validator allowlist/blocking tests. Relevant test coverage includes read validator blocking non-allowlisted historical summaries in `tests/test-allowlist-validators.sh:229-282` and template reference validation patterns in `tests/test-template-references.sh:67-69`. Not executed here due read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`