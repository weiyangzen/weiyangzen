# agent_03 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-003 `directory` `hooks`
- cursor: `[_]`
- core_role: Runtime algorithm layer for the RLCR loop hooks. `hooks/hooks.json:1-64` registers the hook entrypoints for `UserPromptSubmit`, `PreToolUse` on `Write`/`Edit`/`Read`/`Bash`, and `Stop`; the shell/Python files enforce loop-state invariants, prevent protected-file bypasses, and drive the Codex review iteration.
- algorithmic_behavior: `hooks/lib/loop-common.sh:62-79` selects only the newest `.humanize/rlcr/*/` directory that still has `state.md`, preventing older loop directories from becoming active again. `parse_state_file` reads YAML frontmatter into `STATE_*` variables with defaults for `current_round`, `max_iterations`, and `push_every_round` (`hooks/lib/loop-common.sh:110-136`). Path classifiers detect round files, goal tracker, state, and loop-directory membership (`hooks/lib/loop-common.sh:144-186`, `239-255`). `command_modifies_file` matches shell write patterns such as redirection, `tee`, `sed -i`, `mv`, `rm`, `dd`, and `truncate` (`hooks/lib/loop-common.sh:257-285`).
- inputs_outputs_state: Inputs are Claude hook JSON on stdin, `.humanize/rlcr/<timestamp>/state.md`, plan backup `plan.md`, summaries, prompt/review files, transcript JSONL, git state, and prompt templates. Outputs are either exit `0` allow, exit `2` PreToolUse block messages on stderr, or JSON hook decisions with `"decision": "block"` for prompt/stop hooks. The stop hook mutates loop state only at controlled transitions: `end_loop` renames `state.md` to `<reason>-state.md` (`hooks/lib/loop-common.sh:301-334`), or the stop hook advances `current_round` and writes the next prompt (`hooks/loop-codex-stop-hook.sh:967-1033`).
- gates_or_invariants: Write/Edit/Read validators block `round-*-todos.md` except a narrow allowlist, block prompt writes, block state edits, block `plan.md` backup edits, and block goal tracker writes after round 0 (`hooks/loop-write-validator.sh:37-50`, `95-120`; `hooks/loop-edit-validator.sh:36-49`, `79-105`). Write additionally blocks summaries outside the loop directory using `block/wrong-summary-location.md` (`hooks/loop-write-validator.sh:127-134`) and validates exact active-loop path (`hooks/loop-write-validator.sh:177-188`). Read blocks stale prompt/summary access, wrong locations, wrong rounds, and old session directories (`hooks/loop-read-validator.sh:50-140`). Bash blocks protected shell writes and blocks `git push` unless `push_every_round` is true (`hooks/loop-bash-validator.sh:59-144`). Plan validation blocks old state schemas, branch switching, tracked-plan dirtiness, and plan tracking-mode drift (`hooks/loop-plan-file-validator.sh:52-222`).
- dependencies_and_callers: `hooks/hooks.json` is the Claude plugin binding surface. All shell validators source `hooks/lib/loop-common.sh`, which sources `hooks/lib/template-loader.sh` (`hooks/lib/loop-common.sh:46-55`). `loop-plan-file-validator.sh` and `loop-codex-stop-hook.sh` source `scripts/portable-timeout.sh` for bounded git/Codex calls (`hooks/loop-plan-file-validator.sh:19-24`; `hooks/loop-codex-stop-hook.sh:50-55`). `loop-codex-stop-hook.sh` calls `hooks/check-todos-from-transcript.py` before expensive review (`hooks/loop-codex-stop-hook.sh:246-294`). The setup path creates the loop directory, state file, plan backup, goal tracker, and initial prompt in `scripts/setup-rlcr-loop.sh:467-653`; `commands/start-rlcr-loop.md:1-14` invokes that setup script.
- edge_cases_or_failure_modes: `find_active_loop` ignores older directories even if they have valid state, so timestamp naming order is an invariant (`hooks/lib/loop-common.sh:70-78`). The validators generally fail open when no active loop exists, but fail closed for protected loop artifacts once active. `template-loader.sh` uses single-pass `awk` substitution so values containing `{{OTHER_VAR}}` are not recursively expanded (`hooks/lib/template-loader.sh:54-58`, `71-131`). Stop hook failure modes include invalid model/effort, corrupt numeric state, missing schema fields, branch change, missing/deleted/modified plan, incomplete todos, git status failure, large changed files over 2000 lines, dirty git state, unpushed commits when required, missing summary, uninitialized goal tracker placeholders, missing Codex CLI, nonzero Codex exit, missing/empty review result, `COMPLETE`, `STOP`, and max-iteration exit (`hooks/loop-codex-stop-hook.sh:90-238`, `246-617`, `716-960`).
- validation_or_tests: Related tests include template references, todo checker, allowlist validators, plan-file hooks, state-exit naming, bash validator patterns, and the assigned template-loader test discovered by `rg`. I ran `bash tests/test-template-loader.sh`; it failed in this managed read-only sandbox because `template-loader.sh:129` uses here-string input to `awk`, and Bash reported `cannot create temp file ... Operation not permitted`. The test still shows 5 initial non-rendering checks passed and 38 rendering checks failed due the sandbox temp-file restriction, not due an observed code edit.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-033 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, covering template path resolution, load behavior, render behavior, safe fallback behavior, directory validation, shell metacharacter safety, placeholder injection prevention, and additional Unicode/name edge cases.
- algorithmic_behavior: The test sources the real loader from `hooks/lib/template-loader.sh` (`tests/test-template-loader.sh:10-12`), tracks pass/fail counters (`20-34`), then runs 41 named assertions. It verifies `get_template_dir` maps `hooks/lib` to `prompt-template` (`41-52`), `load_template` loads an existing block template and returns empty for a missing template (`55-78`), `render_template` replaces single/multiple/multiline variables and preserves missing placeholders (`81-194`), `load_and_render` works against real templates (`147-164`), `load_and_render_safe` uses fallbacks or real templates appropriately (`197-222`), and `validate_template_dir` accepts/rejects required directory shapes (`225-244`).
- inputs_outputs_state: Inputs are repository templates under `prompt-template`, inline template strings, and `VAR=value` render arguments. Outputs are colored PASS/FAIL lines and process exit `0` if `TESTS_FAILED` is zero, else exit `1` (`tests/test-template-loader.sh:641-659`). State is local to counters and shell variables; it does not intentionally mutate repository files.
- gates_or_invariants: Rendering values must be literal even for shell metacharacters: ampersand, backslash, dollar, backticks, pipes, semicolons, glob syntax, parentheses, redirects, quotes, hash, tilde, combined shell syntax, Windows paths, regex fragments, JSON, and multiline values (`tests/test-template-loader.sh:247-486`). Placeholder injection must be prevented: placeholders embedded inside injected values must not be expanded later, including a realistic review-content case (`489-543`). Empty substitutions, non-ASCII template/value content, underscore-prefixed variables, and numeric variable names are expected to work (`546-639`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh` and real templates such as `prompt-template/block/git-push.md` and `prompt-template/block/wrong-round-number.md` (`tests/test-template-loader.sh:59`, `151-156`, `216`). It is listed in the repository’s aggregate runner `tests/run-all-tests.sh:34` according to repository search. It complements broader template tests such as `tests/test-error-scenarios.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: The test is sensitive to shell/temp-file availability because the loader implementation uses a Bash here-string into `awk` (`hooks/lib/template-loader.sh:74-129`). In this read-only managed sandbox, that produced `cannot create temp file ... Operation not permitted`, causing every rendering assertion to fail despite loader source inspection indicating the intended single-pass algorithm. Test 12 also exposed an `env_vars[@]: unbound variable` path when `load_and_render_safe` calls `render_template` with no render arguments under `set -u`; the assigned test treats existing-template/no-variable rendering as a supported case (`tests/test-template-loader.sh:211-222`).
- validation_or_tests: Direct validation command attempted: `bash tests/test-template-loader.sh`. Observed summary: 5 passed, 38 failed. Failure cause reported by the shell: temp-file creation denied for the loader here-string at `hooks/lib/template-loader.sh:129`; no files were modified. The test itself is the executable validation artifact for the loader contract.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-063 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Block-message template for the Write validator when an agent tries to create or write a `round-*-summary.md` file outside the active `.humanize/rlcr/<timestamp>/` loop directory.
- algorithmic_behavior: The template contains a heading, a short invariant statement, and a `{{CORRECT_PATH}}` placeholder (`prompt-template/block/wrong-summary-location.md:1-5`). `hooks/loop-write-validator.sh:127-134` renders it via `load_and_render_safe` after computing `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"`, then exits `2` to block the Write tool.
- inputs_outputs_state: Input variable is `CORRECT_PATH`, supplied by `loop-write-validator.sh`. Output is Markdown sent to stderr as the PreToolUse block reason. It does not mutate state; it guides the agent to the active loop summary path.
- gates_or_invariants: Enforces the invariant that summary files must live in the loop directory, not arbitrary project paths. This supports the stop hook’s later requirement that `SUMMARY_FILE="$LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` exists before Codex review (`hooks/loop-codex-stop-hook.sh:510-531`).
- dependencies_and_callers: Called by `hooks/loop-write-validator.sh:132`. Rendered through `hooks/lib/template-loader.sh` and the shared `load_and_render_safe` path, which preserves fallback behavior if templates are missing. Its sibling templates handle wrong round number, wrong directory path, wrong file location, and summary Bash writes.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the Write validator fallback still blocks with a message that includes `{{CORRECT_PATH}}` substitution (`hooks/loop-write-validator.sh:129-133`). If template rendering is unavailable due environment restrictions, the hook’s fallback path is intended to keep the gate functional, though the current loader’s here-string implementation can be affected by no-temp-file sandboxes.
- validation_or_tests: Covered indirectly by `tests/test-template-references.sh` template reference checks and by validator tests that exercise summary path restrictions. The assigned `tests/test-template-loader.sh` also validates `load_and_render_safe` behavior that this template relies on, but could not pass in this sandbox for the temp-file reason described above.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: GENERAL_REFACTOR_AND_REVIEW-HZ-003, GENERAL_REFACTOR_AND_REVIEW-HZ-033, GENERAL_REFACTOR_AND_REVIEW-HZ-063
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`