# agent_19 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-019 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit hook for the RLCR loop that fail-closes user prompt submission when the active loop state is outdated, the git branch changed, or the original plan-file tracking mode no longer matches the repository. It is registered as the `UserPromptSubmit` command hook in `hooks/hooks.json:4-10`, so its algorithmic role is a pre-turn safety gate rather than a mutating loop step.
- algorithmic_behavior: The hook reads all stdin into `INPUT` at `hooks/loop-plan-file-validator.sh:26-27` because Claude hook commands receive JSON input, but this implementation does not inspect the JSON payload. It derives `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` at line 14, finds the newest active loop under `.humanize/rlcr` with `find_active_loop` at lines 29-35, then parses `state.md` through `parse_state_file` at lines 38-46. If there is no active loop, it allows the prompt silently with exit 0. If a loop exists, it validates required schema fields at lines 52-82, checks the current branch against `start_branch` at lines 88-108, then enforces the plan tracking policy at lines 114-222. The tracking branch splits on `PLAN_TRACKED == true`: tracked plans must still be tracked and have clean `git status --porcelain` output at lines 116-185; untracked/gitignored plans must not become tracked at lines 186-221.
- inputs_outputs_state: Inputs are the active loop state file path discovered from `PROJECT_ROOT/.humanize/rlcr`, parsed fields `plan_tracked`, `plan_file`, and `start_branch`, plus git state from `git rev-parse --abbrev-ref HEAD`, `git ls-files --error-unmatch`, and `git status --porcelain`. Outputs are either no output with exit 0 for allow, or a JSON object with `"decision": "block"` and a human-readable `"reason"` for blocked states. It does not mutate state files, git state, or loop files. Its state transition is a gate transition only: active-loop user prompt submission remains allowed, or is blocked until branch/tracking/schema conditions are repaired.
- gates_or_invariants: Required schema invariant: `plan_tracked` and `start_branch` must be non-empty, enforced at `hooks/loop-plan-file-validator.sh:72-82`; missing fields render `prompt-template/block/schema-outdated.md` if available, with an inline fallback at lines 53-69. Branch invariant: current branch must match `start_branch`, enforced at lines 88-108. Plan tracking invariant: if the loop started with `--track-plan-file`, the plan must remain tracked and clean; if it started without tracking, the plan must remain untracked/gitignored. Git command timeouts and unexpected git errors fail closed with block JSON at lines 122-139, 147-164, 192-209.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file`, field constants, and `TEMPLATE_DIR`, sourced at `hooks/loop-plan-file-validator.sh:16-17`. `find_active_loop` chooses only the newest timestamp-named loop dir that contains `state.md` in `hooks/lib/loop-common.sh:58-79`, which prevents older loop dirs from reviving. `parse_state_file` extracts frontmatter and strips YAML quotes from `start_branch` and `plan_file` in `hooks/lib/loop-common.sh:110-136`. The hook depends on `scripts/portable-timeout.sh` for `run_with_timeout`, sourced at `hooks/loop-plan-file-validator.sh:19-24`; that wrapper prefers `gtimeout`, GNU `timeout`, Python subprocess timeout, then no-timeout fallback in `scripts/portable-timeout.sh:9-71`. It also depends on `jq` for JSON string escaping in `schema_validation_error` at `hooks/loop-plan-file-validator.sh:60-63`.
- edge_cases_or_failure_modes: No active loop exits silently, so stale repositories without `.humanize/rlcr/<timestamp>/state.md` are not blocked. If the newest loop directory lacks `state.md`, older loops are intentionally ignored by `find_active_loop` behavior in `hooks/lib/loop-common.sh:58-79`. If `state.md` is missing after `LOOP_DIR` is found, `parse_state_file` returns nonzero, and because the script has `set -e`, that would abort instead of returning block JSON; practically, `find_active_loop` only returns dirs with `state.md`, so this edge is constrained. YAML quote handling is intentionally supported for `plan_file` and `start_branch` via shared parsing, but `plan_tracked` is normalized by deleting spaces only, so values other than exact `true` take the untracked branch. `FULL_PLAN_PATH` is set at line 114 but unused; actual checks are git-index based. A missing `jq` would break block rendering because the hook assumes `jq` exists.
- validation_or_tests: `tests/test-plan-file-hooks.sh` directly exercises the UserPromptSubmit behavior. Covered cases include valid state allowing with no output at lines 91-104, YAML-quoted `plan_file` at lines 106-119, missing `plan_tracked` at lines 121-139, missing `start_branch` at lines 141-159, branch changes at lines 164-185, quoted `start_branch` pass/fail cases at lines 341-386, and hyphenated plan-file paths at lines 431-460. Related stop-hook tests cover plan integrity and tracked-plan race conditions at lines 469-653, showing the same state schema and parser contract is shared across hooks. I did not run the tests because this assignment is read-only research notes only.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-049 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Rendered prompt block for the RLCR Stop hook’s large-file gate. It is not executable code, but it defines the user-facing remediation contract when loop exit is blocked because changed code or documentation files exceed the configured line-count threshold.
- algorithmic_behavior: The template has two placeholders: `{{MAX_LINES}}` and `{{LARGE_FILES}}` at `prompt-template/block/large-files.md:3-4`, and repeats `{{MAX_LINES}}` in action items at lines 13-20. The Stop hook constructs `LARGE_FILES` by scanning cached `git status --porcelain` entries, classifying extensions as code or documentation, counting lines, and appending files whose line count is greater than `MAX_LINES`. That caller path is `hooks/loop-codex-stop-hook.sh:328-408`; the threshold is currently `MAX_LINES=2000` at line 334. When any large files are found, the Stop hook calls `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` with `MAX_LINES` and `LARGE_FILES` at lines 387-397, then emits JSON with `"decision": "block"`, rendered `"reason"`, and a short `"systemMessage"` at lines 399-407.
- inputs_outputs_state: Template inputs are `MAX_LINES`, supplied by the Stop hook, and `LARGE_FILES`, a newline-prefixed Markdown bullet list such as ``- `path`: N lines (code file)`` produced at `hooks/loop-codex-stop-hook.sh:381-383`. Output is Markdown embedded as the JSON `reason` field of the Stop hook block response. It does not mutate state; the state transition it participates in is Stop hook exit denial until large files are split, committed, and the user attempts to stop again.
- gates_or_invariants: The underlying gate only applies when the project is a git repo and `git status --porcelain` succeeds; the Stop hook fail-closes separately if git status fails at `hooks/loop-codex-stop-hook.sh:300-325`. The large-file gate inspects files in git status output, skips deleted files, handles rename syntax by using the destination path, limits classification to known code/doc extensions, validates that `wc -l` output is numeric, and blocks only when `line_count > MAX_LINES` at lines 339-385. The remediation invariant in the template is strict preservation of behavior for code splits at `prompt-template/block/large-files.md:13-17`, cross-reference and navigation maintenance for documentation splits at lines 19-23, and commit-before-exit at line 25.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` at lines 395-397. Template resolution depends on `TEMPLATE_DIR` initialized via `hooks/lib/loop-common.sh:46-55`, which delegates to `hooks/lib/template-loader.sh`. Rendering uses single-pass `{{VAR}}` substitution in `hooks/lib/template-loader.sh:50-132`; missing placeholders are preserved, and values are not recursively scanned, which is relevant because `LARGE_FILES` is arbitrary path-derived text. If this template is missing or renders empty, the Stop hook uses the inline fallback at `hooks/loop-codex-stop-hook.sh:388-394`.
- edge_cases_or_failure_modes: Because the scanner uses `git status --porcelain`, unchanged already-large tracked files are not listed unless they have status output; the gate is focused on changed or new files at stop time. Extension classification excludes many possible large assets or uncommon source formats, so those files pass through this specific gate. The gate compares strictly greater than the threshold, so exactly `MAX_LINES` lines is allowed while `MAX_LINES + 1` blocks. File paths are embedded in Markdown bullets; the template renderer avoids recursive placeholder expansion, but path text is still presented verbatim in the human-facing reason. The template recommends optional `code-simplifier` use at `prompt-template/block/large-files.md:16`, which is advisory and not a machine-checked dependency.
- validation_or_tests: I found direct template-reference coverage for template paths in `tests/test-template-references.sh`, and template-loader tests in `tests/test-template-loader.sh`, but no focused large-file Stop hook scenario in the searched test files. The caller logic is visible in `hooks/loop-codex-stop-hook.sh:328-408`; validation currently appears to be indirect through template existence/rendering checks rather than an end-to-end test that creates a >2000-line changed file and asserts the Stop hook renders this block. I did not run tests because this assignment is read-only research notes only.
- skip_candidate: `yes: template-only artifact, not a standalone algorithm implementation; still core-adjacent because it defines the Stop hook large-file gate contract and remediation instructions`

## Worker Self-Test
- assigned_items_seen: 2 evidence sections present, matching the two assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`