# agent_23 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-023 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Provides the repository’s cross-platform timeout abstraction for RLCR setup and hook workflows. It normalizes timeout behavior across macOS with Homebrew `gtimeout`, GNU/Linux `timeout`, Python fallback, or no timeout implementation, exposing a single `run_with_timeout <seconds> <command> [args...]` API.
- algorithmic_behavior: The file first detects the timeout backend in priority order: `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` at `scripts/portable-timeout.sh:10`. Detection checks `gtimeout` by command presence, checks `timeout --version` to avoid non-GNU/BSD incompatibility, and falls back to Python interpreters at `scripts/portable-timeout.sh:11` through `scripts/portable-timeout.sh:25`. The selected backend is captured once in global `TIMEOUT_IMPL` at `scripts/portable-timeout.sh:29`. `run_with_timeout` receives the timeout seconds, shifts it off, preserves the remaining command and arguments as a Bash array, and dispatches based on `TIMEOUT_IMPL` at `scripts/portable-timeout.sh:33` through `scripts/portable-timeout.sh:70`. Native timeout backends execute directly; Python fallback runs `subprocess.run(sys.argv[1:], timeout=...)` and maps `subprocess.TimeoutExpired` to exit code `124`, matching GNU timeout conventions at `scripts/portable-timeout.sh:47` through `scripts/portable-timeout.sh:61`.
- inputs_outputs_state: Inputs are a numeric timeout string and an arbitrary command argv vector. Output is the wrapped command’s stdout/stderr, except the Python fallback may emit `Error: ...` on unexpected Python/subprocess exceptions, and the `none` fallback emits a warning to stderr. Return code is the child command’s return code, `124` on timeout for GNU/Python backends, or `1` for Python wrapper exceptions. State is limited to exported `TIMEOUT_IMPL`, set during sourcing and exported at `scripts/portable-timeout.sh:76`; the function itself does not mutate repo state.
- gates_or_invariants: The key invariant is “callers can treat exit `124` as timeout” where GNU/Python backends are available. Bash array command capture at `scripts/portable-timeout.sh:36` preserves argument boundaries for native and Python execution. The detection gate intentionally rejects a `timeout` command that does not answer `--version`, so non-GNU implementations do not get used as if GNU-compatible. When no backend exists, the script fails open by running without a timeout and warning at `scripts/portable-timeout.sh:64` through `scripts/portable-timeout.sh:68`; callers that rely on hard timeout semantics must account for that degraded mode.
- dependencies_and_callers: The wrapper depends on Bash, `command -v`, optional `gtimeout`, optional GNU `timeout`, optional Python, and Python’s `subprocess` module. It is sourced by `scripts/setup-rlcr-loop.sh:28`, `hooks/loop-codex-stop-hook.sh:52`, and `hooks/loop-plan-file-validator.sh:21`. Setup uses it for git repository and plan-file validation such as `git rev-parse`, `git submodule status`, `git status --porcelain`, `git ls-files`, and branch capture at `scripts/setup-rlcr-loop.sh:212`, `scripts/setup-rlcr-loop.sh:218`, `scripts/setup-rlcr-loop.sh:295`, `scripts/setup-rlcr-loop.sh:313`, `scripts/setup-rlcr-loop.sh:323`, and `scripts/setup-rlcr-loop.sh:429`. The plan-file validator uses it to fail closed on git timeouts/errors during branch and plan tracking checks at `hooks/loop-plan-file-validator.sh:89`, `hooks/loop-plan-file-validator.sh:120`, `hooks/loop-plan-file-validator.sh:145`, and `hooks/loop-plan-file-validator.sh:190`. The stop hook uses it for branch checks, plan status, dirty tree checks, ahead checks, and the long-running Codex review invocation at `hooks/loop-codex-stop-hook.sh:138`, `hooks/loop-codex-stop-hook.sh:204`, `hooks/loop-codex-stop-hook.sh:307`, `hooks/loop-codex-stop-hook.sh:311`, `hooks/loop-codex-stop-hook.sh:479`, `hooks/loop-codex-stop-hook.sh:482`, and `hooks/loop-codex-stop-hook.sh:800`. The stop hook also has an inline fallback only if the file is unavailable, at `hooks/loop-codex-stop-hook.sh:754` through `hooks/loop-codex-stop-hook.sh:772`.
- edge_cases_or_failure_modes: If the timeout value is empty, non-numeric, negative, or otherwise invalid, this wrapper does not validate it; native timeout or Python will fail according to their own parsing/runtime behavior. If `TIMEOUT_IMPL` is detected once and the environment changes later, the function does not redetect. If no timeout backend is present, long-running git or Codex commands can hang because the `none` branch deliberately runs the command unbounded. The Python fallback does not explicitly kill process groups; it relies on `subprocess.run(..., timeout=...)` behavior for the direct child process. In callers that use pipelines, timeout status may be masked by downstream commands unless `pipefail` and explicit status capture are handled at the caller.
- validation_or_tests: I did not find a dedicated unit test for `scripts/portable-timeout.sh` itself. Its behavior is exercised indirectly through hook/setup paths that rely on timeout exit status `124` and command success/failure. Concrete caller validation exists in `hooks/loop-plan-file-validator.sh`, which checks timeout and unexpected git errors as blocking JSON responses at `hooks/loop-plan-file-validator.sh:122` through `hooks/loop-plan-file-validator.sh:164` and again at `hooks/loop-plan-file-validator.sh:192` through `hooks/loop-plan-file-validator.sh:210`. The stop-hook test suite includes scenarios that drive stop-hook validation paths, but not a backend-specific timeout matrix.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-053 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Defines the user-facing block message for an RLCR state schema gate when required state fields are missing. It is a prompt/block template, not executable code, but it participates in the validation contract for older loop state files.
- algorithmic_behavior: The template renders a fixed “State Schema Outdated” block with one substitution placeholder, `{{FIELD_NAME}}`, at `prompt-template/block/schema-outdated.md:3`. The message tells the user that the RLCR loop state file is missing a required field, attributes it to an older humanize version, lists remediation options, and states that the loop will be terminated as unexpected to preserve state information at `prompt-template/block/schema-outdated.md:5` through `prompt-template/block/schema-outdated.md:12`. Rendering is single-pass through the shared template loader: placeholders use `{{VARIABLE_NAME}}` syntax, missing variables remain unchanged, and replacement values are not rescanned, per `hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:13` and `hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:131`.
- inputs_outputs_state: Input is the `FIELD_NAME=<field>` render variable supplied by `hooks/loop-plan-file-validator.sh:58`. The output is markdown text consumed as the JSON `reason` string for a hook block response after newline escaping with `jq -Rs` at `hooks/loop-plan-file-validator.sh:60` through `hooks/loop-plan-file-validator.sh:68`. The template does not store state. It describes state preservation behavior, while actual loop termination or continuation is controlled by the hook invoking it.
- gates_or_invariants: The schema gate is triggered when parsed state frontmatter lacks required fields. `hooks/lib/loop-common.sh` defines required field constants `plan_tracked` and `start_branch` at `hooks/lib/loop-common.sh:16` through `hooks/lib/loop-common.sh:17`, parses them from `state.md` at `hooks/lib/loop-common.sh:117` through `hooks/lib/loop-common.sh:129`, and the plan-file validator checks them as required at `hooks/loop-plan-file-validator.sh:72` through `hooks/loop-plan-file-validator.sh:82`. If either field is empty, the validator emits the rendered block response and exits `0`, which means “hook handled by blocking JSON” rather than shell failure. The template loader has a fallback invariant: if this template is missing or empty, `load_and_render_safe` renders an inline fallback instead at `hooks/lib/template-loader.sh:170` through `hooks/lib/template-loader.sh:203`.
- dependencies_and_callers: Direct caller found: `hooks/loop-plan-file-validator.sh:58`. It depends on `TEMPLATE_DIR`, initialized through `hooks/lib/loop-common.sh:47` through `hooks/lib/loop-common.sh:55`, and on `load_and_render_safe` from `hooks/lib/template-loader.sh`. The same schema concept is duplicated in the stop hook as an inline JSON block for missing `plan_tracked` or `start_branch` at `hooks/loop-codex-stop-hook.sh:115` through `hooks/loop-codex-stop-hook.sh:131`, but that stop-hook branch does not call this specific template. Tests reference the stop-hook schema behavior in `tests/test-plan-file-hooks.sh:631` through `tests/test-plan-file-hooks.sh:650`.
- edge_cases_or_failure_modes: If `FIELD_NAME` is not passed, the placeholder remains visible as `{{FIELD_NAME}}` because the renderer preserves missing variables. If template loading fails, the plan validator silently degrades to its inline fallback and still blocks. The template text says the loop “will be terminated as 'unexpected',” but `hooks/loop-plan-file-validator.sh` only emits a block decision and exits; actual unexpected termination is more explicitly described in the stop-hook schema branch comments at `hooks/loop-codex-stop-hook.sh:115` through `hooks/loop-codex-stop-hook.sh:118`, though that branch also emits a block response rather than calling `end_loop` in the inspected range. There is also a specificity mismatch: this template reports one missing field per render, while the stop hook’s inline schema block groups `plan_tracked or start_branch`.
- validation_or_tests: The direct plan-file-validator template path does not appear to have a dedicated test assertion by template filename. Related schema validation is covered for the stop hook in `tests/test-plan-file-hooks.sh:631` through `tests/test-plan-file-hooks.sh:650`, where a legacy `state.md` without `plan_tracked` is expected to produce a JSON block mentioning schema, missing field, or `plan_tracked`. The template rendering mechanism itself has defensive behavior through `load_and_render_safe`, which provides fallback output on missing or empty templates.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections present, matching the two assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`