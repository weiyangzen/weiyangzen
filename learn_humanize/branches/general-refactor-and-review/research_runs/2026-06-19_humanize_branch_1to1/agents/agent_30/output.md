# agent_30 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-030 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR plan-file protection algorithm. It verifies that active loop metadata, branch continuity, immutable plan backups, shell bypass prevention, stop-hook plan integrity, and goal-tracker initialization gates behave as expected. The test is wired into the repo test runner and CI: `tests/run-all-tests.sh:41` includes it, and `.github/workflows/plan-file-test.yml:45-46` chmods and runs `./tests/test-plan-file-hooks.sh`.

- algorithmic_behavior: The script builds isolated throwaway git repositories under `TEST_DIR=$(mktemp -d)` and removes them with a trap on exit (`tests/test-plan-file-hooks.sh:30-32`). Its shared `setup_test_loop` routine initializes git, captures the default branch, creates `.humanize/rlcr/<timestamp>/`, writes a gitignored plan file, copies it to loop `plan.md` as the immutable backup, and writes `state.md` frontmatter with `current_round`, `max_iterations`, quoted `plan_file`, `plan_tracked`, and `start_branch` (`tests/test-plan-file-hooks.sh:37-85`). Each scenario then invokes the production hooks directly with JSON stdin or `{}` and asserts hook exit code plus selected output text.
  The first block exercises `hooks/loop-plan-file-validator.sh` as the `UserPromptSubmit` gate: valid state must return exit `0` with no output (`tests/test-plan-file-hooks.sh:91-104`); quoted `plan_file` must parse cleanly (`106-119`); missing `plan_tracked` and missing `start_branch` must produce block JSON mentioning those fields (`121-159`); branch drift must be detected after checkout to `feature-branch` (`164-185`).
  The next blocks exercise PreToolUse path gates. Write and Edit attempts targeting the loop backup `plan.md` must fail with exit `2` and a plan-related message (`194-221`). Bash attempts to write or remove plan backups must also fail (`228-252`), including the direct `.humanize/rlcr/plan.md` form without a timestamp directory (`254-266`).
  The bypass-prevention block asserts that the Bash validator detects command-substitution, glob, brace, pipe-to-tee, and backtick routes that still target `.humanize/rlcr/**/plan.md` (`272-335`). This maps to `hooks/lib/loop-common.sh:260-285`, where `command_modifies_file` matches redirection, `tee`, in-place edits, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf`, and descriptor redirection against a supplied file regex. The plan backup regex itself is applied in `hooks/loop-bash-validator.sh:87-91`.
  The YAML parsing block verifies quote stripping for `start_branch`, branch mismatch with a quoted value, stop-hook parsing of quoted `plan_file` and `start_branch`, and plan-file paths containing hyphens (`tests/test-plan-file-hooks.sh:341-460`). The underlying parser strips surrounding double quotes for `STATE_START_BRANCH` and `STATE_PLAN_FILE` in `hooks/lib/loop-common.sh:121-123`.
  The stop-hook plan integrity block verifies immutable plan semantics. The hook must block when the original plan diverges from the backup (`tests/test-plan-file-hooks.sh:469-507`), when the original plan is deleted (`509-541`), when the backup is missing (`543-560`), when a tracked plan has uncommitted changes (`562-629`), when old schema fields are absent (`631-653`), and when a tracked plan has been modified and committed so `git status` is clean but content differs from backup (`655-731`). The production stop hook implements these gates in `hooks/loop-codex-stop-hook.sh:119-130`, `154-164`, `170-238`.
  The section-specific placeholder block validates goal-tracker initialization diagnostics in round 0. It creates goal trackers with only Ultimate Goal, only Acceptance Criteria, only Active Tasks, or all three placeholder sections missing, and asserts the stop hook reports exactly the affected section names (`tests/test-plan-file-hooks.sh:737-1009`). The production algorithm extracts sections with `awk`, checks each section for a generic `[To be ...]` placeholder, builds `MISSING_ITEMS`, and blocks if non-empty (`hooks/loop-codex-stop-hook.sh:539-605`).
  The final negative tests assert that legacy `.humanize-loop.local/**/plan.md` is not treated as an RLCR loop directory anymore: Bash, Write, and Edit validators must allow it (`tests/test-plan-file-hooks.sh:1016-1054`). This depends on the current path classifier matching only `.humanize/rlcr/` (`hooks/lib/loop-common.sh:251-255`).

- inputs_outputs_state: Inputs are synthetic hook JSON payloads, git repository state, loop state frontmatter, active branch, plan file content, loop backup content, summary files, and goal tracker files. Hook invocations use `CLAUDE_PROJECT_DIR` to point production scripts at each temporary repo (`tests/test-plan-file-hooks.sh:93`, `619`, `720`, `794`, `863`, `930`, `997`). Outputs are pass/fail counters and process exit status; validator gates use exit `0` for allow or JSON block responses in prompt/stop hooks, and exit `2` for PreToolUse blocks (`194-252`, `272-335`). The test’s own state transitions are localized: setup creates a valid active loop, scenarios mutate `state.md`, git branch, plan content, backup presence, or goal tracker placeholders, then assert the corresponding gate. Test summary is printed at `tests/test-plan-file-hooks.sh:1056-1065`, and the script exits with `TESTS_FAILED`.

- gates_or_invariants: The central invariants are: an active loop requires `plan_tracked` and `start_branch`; branch must remain the branch that started the loop; `plan_file` and `start_branch` may be YAML-quoted; the loop backup `plan.md` is immutable; Bash must not bypass Write/Edit controls for loop-managed files; tracked plans must be both git-clean and content-identical to the backup; untracked/gitignored plans must still be content-identical to backup; a missing original plan or missing backup blocks stop; round-0 goal tracker placeholders are section-specific failures; `.humanize-loop.local` is not a protected loop path. These map to production guards in `hooks/loop-plan-file-validator.sh:72-108`, `116-222`; `hooks/loop-write-validator.sh:95-120`, `127-188`; `hooks/loop-edit-validator.sh:79-105`, `112-140`; `hooks/loop-bash-validator.sh:59-146`; and `hooks/loop-codex-stop-hook.sh:119-238`, `539-605`.

- dependencies_and_callers: Direct dependencies are Bash, git, jq, diff, grep/sed/awk, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `hooks/check-todos-from-transcript.py`, and the hook scripts under `hooks/`. Runtime hook registration is in `hooks/hooks.json`: `loop-plan-file-validator.sh` for `UserPromptSubmit` (`hooks/hooks.json:4-10`), Write/Edit/Read/Bash validators for `PreToolUse` (`14-50`), and `loop-codex-stop-hook.sh` for Stop with timeout `7200` (`52-59`). The tests are called by `tests/run-all-tests.sh:41` and the plan-file workflow at `.github/workflows/plan-file-test.yml:45-46`.

- edge_cases_or_failure_modes: Important edge cases covered are old state schema missing fields; branch drift; quoted YAML scalar parsing; hyphenated plan paths; plan backup missing; original plan deleted; tracked file race where the file changes after loop start; committed tracked changes that evade `git status`; plan writes through shell expansion, pipes, `tee`, and substitution; direct `.humanize/rlcr/plan.md` without a timestamp directory; and legacy `.humanize-loop.local` paths being intentionally unprotected. A notable test harness limitation is that `set -e` is toggled around hook calls but the script starts with `set -uo pipefail`, not `set -euo pipefail` (`tests/test-plan-file-hooks.sh:12`), so assertions rely on explicit exit-code checks rather than global fail-fast. Another practical dependency risk is that stop-hook tests may encounter earlier gates such as todo parsing, git cleanliness, missing Codex, or summary checks; the script creates summaries and goal trackers before plan-integrity assertions to force the intended gate ordering (`469-560`, `599-616`, `693-710`).

- validation_or_tests: This file is itself the validation asset. It contains 17 numbered tests plus subtests for YAML parsing, bypass prevention, stop-hook integrity, and placeholder detection. I did not execute it because this worker run requested read-only research notes and the environment is a read-only branch export. Static inspection confirms the script exercises the registered production hooks directly and asserts concrete exit codes/output patterns.

- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-060 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template used by the Read validator when an agent tries to read a round prompt or summary file from outside the active `.humanize/rlcr/<loop>/` directory. It is a small prompt contract, not an algorithm by itself, but it defines the corrective text surfaced by a core path-location gate.

- algorithmic_behavior: The template renders a “Wrong File Location” block with the attempted `{{FILE_PATH}}`, the authoritative `{{ACTIVE_LOOP_DIR}}`, and the current round’s prompt and summary paths (`prompt-template/block/wrong-file-location.md:1-7`). It then gives a fallback command to read the originally requested file if truly needed (`line 9`). The production call site is `hooks/loop-read-validator.sh:94-103`: when the requested file is a `round-*-summary.md` or `round-*-prompt.md` but `is_in_humanize_loop_dir` is false, the hook renders this template through `load_and_render_safe` and exits `2`. Template substitution is single-pass and missing placeholders are preserved by `hooks/lib/template-loader.sh:50-132`, with safe fallback behavior in `170-203`.

- inputs_outputs_state: Inputs are the template variables `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`, all supplied by `loop-read-validator.sh` (`hooks/loop-read-validator.sh:99-102`). The output is Markdown text sent to stderr before the hook blocks the Read tool with exit `2` (`hooks/loop-read-validator.sh:99-103`). The template itself has no persistent state transition; it participates in the validator’s state machine after the active loop is found, `state.md` is parsed, `CURRENT_ROUND` is loaded, and the requested round-file path is classified (`hooks/loop-read-validator.sh:54-88`).

- gates_or_invariants: The invariant encoded by the template is that current-round prompt/summary reads must come from the active loop directory, not from arbitrary locations or stale directories. The surrounding gate also validates current round number and exact active-directory path after this location check (`hooks/loop-read-validator.sh:106-140`). The template deliberately names only current-round prompt and summary files, reinforcing the active-loop source of truth.

- dependencies_and_callers: Called by `hooks/loop-read-validator.sh:99` through `load_and_render_safe`. The validator is registered for the `Read` PreToolUse hook in `hooks/hooks.json:33-40`. Shared dependencies are `hooks/lib/loop-common.sh` for active-loop selection, round-file detection, and state parsing (`hooks/lib/loop-common.sh:62-79`, `146-163`, `110-137`), plus `hooks/lib/template-loader.sh` for rendering. Related sibling block templates include `wrong-round-file.md`, `wrong-directory-path.md`, and `wrong-round-number.md`, which handle later path/round failures; `rg` found this template only at its file path and the single read-validator call site.

- edge_cases_or_failure_modes: If the template file is missing or renders empty, `load_and_render_safe` falls back to an inline wrong-location message (`hooks/loop-read-validator.sh:96-99`; `hooks/lib/template-loader.sh:170-203`). If one of the expected variables is missing, the renderer keeps the unresolved `{{VARIABLE}}` placeholder rather than recursively substituting or dropping it (`hooks/lib/template-loader.sh:12-13`, `116-122`). Because the template says “If you need this file, use: `cat {{FILE_PATH}}`,” it may look permissive, but the hook has already blocked the structured Read tool path; the intended transition is to guide the agent toward an explicit shell read when it truly wants that non-active file. This template is a skip candidate only if the core-algorithm subset is limited strictly to executable code, but it is still part of the validator’s user-facing gate behavior.

- validation_or_tests: No direct assertion for this exact template appears in `tests/test-plan-file-hooks.sh`, which focuses on plan-file hooks rather than Read validation. The read-validator call site is covered indirectly by template reference tests: `tests/test-template-references.sh:58-62` includes hook scripts in template-reference checks, and `tests/test-templates-comprehensive.sh:492-525` demonstrates real-template rendering integration for sibling templates. Static inspection confirms the template path matches the production call site exactly.

- skip_candidate: `yes: this is a message template rather than executable algorithm code, but it is still a core gate contract because the Read validator depends on its rendered block text for wrong-location failures.`

## Worker Self-Test
- assigned_items_seen: `GENERAL_REFACTOR_AND_REVIEW-HZ-030`, `GENERAL_REFACTOR_AND_REVIEW-HZ-060`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`