# agent_09 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`
- research_scope: read-only inspection of `prompt-template/block`, `prompt-template/block/codex-review-failed.md`, `prompt-template/codex/full-alignment-review.md`, plus their hook/template-loader references.
- note: no files were modified.

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-009 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: `prompt-template/block` is the reusable failure-message and guardrail-template directory for the RLCR loop hooks. Its templates define what the agent sees when read/write/edit/bash validators or the stop hook block an unsafe transition. The directory contains 25 markdown files totaling 9,932 bytes, matching the assignment metadata.
- algorithmic_behavior: The directory does not execute logic directly; it supplies rendered contract text to shell hook decisions. The active algorithm is: hook detects invalid state or forbidden action, calls `load_and_render_safe "$TEMPLATE_DIR" "block/<name>.md" ...`, receives markdown text with placeholders substituted, then either exits with a tool-block status or returns a JSON block decision. Template loading is implemented by `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, and `hooks/lib/template-loader.sh:170`.
- inputs_outputs_state: Inputs are hook-derived variables such as `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, `CORRECT_PATH`, `FILE_PATH`, `SUMMARY_FILE`, `GIT_ISSUES`, `SPECIAL_NOTES`, `MAX_LINES`, `LARGE_FILES`, `AHEAD_COUNT`, `CURRENT_BRANCH`, `PLAN_FILE`, `BACKUP_PATH`, and `MISSING_ITEMS`. Outputs are rendered markdown reasons sent to stderr by pre-tool validators or embedded as JSON `reason` fields by `hooks/loop-codex-stop-hook.sh`. The directory itself holds no mutable state; loop state is read from `.humanize/rlcr/<timestamp>/state.md` by `hooks/lib/loop-common.sh:110`.
- gates_or_invariants: The directory encodes several invariants: current-round files must be read/written only from the active loop directory (`wrong-file-location.md`, `wrong-round-file.md`, `wrong-directory-path.md`, `wrong-round-number.md`, `wrong-summary-location.md`); native TodoWrite must be used instead of `round-*-todos.md` files (`todos-file-access.md:1`); prompt and state files are system-managed (`prompt-file-write.md:1`, `state-file-modification.md:1`); the loop backup plan is read-only (`plan-backup-protected.md:1`); after Round 0 only Codex may update `goal-tracker.md` (`goal-tracker-modification.md:1`); stop attempts require complete todos, clean git state, summary presence, and line-limit compliance (`incomplete-todos.md:1`, `git-not-clean.md:1`, `work-summary-missing.md:1`, `large-files.md:1`).
- dependencies_and_callers: Main callers are `hooks/lib/loop-common.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. `loop-common.sh` wraps shared blocks for todos, prompt writes, state writes, summary bash writes, goal-tracker bash writes, and post-Round-0 goal-tracker blocking at `hooks/lib/loop-common.sh:188`, `hooks/lib/loop-common.sh:199`, `hooks/lib/loop-common.sh:208`, `hooks/lib/loop-common.sh:217`, `hooks/lib/loop-common.sh:228`, and `hooks/lib/loop-common.sh:287`. The read validator renders location/round blocks at `hooks/loop-read-validator.sh:94`, `hooks/loop-read-validator.sh:110`, and `hooks/loop-read-validator.sh:129`. Write/edit/bash validators render protected-surface blocks at `hooks/loop-write-validator.sh:95`, `hooks/loop-write-validator.sh:104`, `hooks/loop-write-validator.sh:117`, `hooks/loop-edit-validator.sh:79`, `hooks/loop-edit-validator.sh:88`, `hooks/loop-edit-validator.sh:102`, and `hooks/loop-bash-validator.sh:59`.
- edge_cases_or_failure_modes: Template rendering is deliberately single-pass, so placeholders embedded inside variable values are not recursively expanded (`hooks/lib/template-loader.sh:54`). Missing variables remain as literal placeholders (`hooks/lib/template-loader.sh:13`). Missing or empty templates fall back to inline messages through `load_and_render_safe` (`hooks/lib/template-loader.sh:170`). Some templates are compositional fragments rather than standalone block pages, notably `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md`, which are concatenated into `SPECIAL_NOTES` by `hooks/loop-codex-stop-hook.sh:429` and `hooks/loop-codex-stop-hook.sh:438`. `codex-review-failed.md` is present in this directory but appears unreferenced by the active hook path; details are covered in its file item.
- validation_or_tests: `tests/test-template-references.sh` scans hook scripts for template references and fails on missing referenced templates (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:83`, `tests/test-template-references.sh:102`). It also verifies shared `loop-common.sh` block templates exist (`tests/test-template-references.sh:149`) and that critical validators use safe rendering (`tests/test-template-references.sh:170`). `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` exercise template loading and substitution behavior; reference search shows direct checks for real block templates including `wrong-round-number.md`, `git-push.md`, and `unpushed-commits.md`.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-039 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: This file is an intended Codex-review failure block. It describes the failure condition, shows the Codex exit code, identifies the missing review result file, points to debug artifacts, and includes recent stderr content. Its placeholders are `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5`, `prompt-template/block/codex-review-failed.md:6`, `prompt-template/block/codex-review-failed.md:9`, `prompt-template/block/codex-review-failed.md:15`).
- algorithmic_behavior: As written, the template’s intended transition is “Codex review did not produce output” to “block loop exit and ask the user/agent to inspect debug files, then retry review on the next exit attempt.” It does not itself decide retry policy; it supplies the human-facing reason string. The final sentence establishes retry semantics: “The system will attempt another review when you exit” (`prompt-template/block/codex-review-failed.md:18`).
- inputs_outputs_state: Inputs are Codex execution metadata and captured stderr. Output is a rendered markdown block. No repository or loop state is mutated by the template. The logical state transition it supports is from failed review invocation to blocked stop attempt, preserving active loop state for another review attempt.
- gates_or_invariants: The template assumes a missing `REVIEW_RESULT_FILE` and therefore a failed Codex review result handoff (`prompt-template/block/codex-review-failed.md:6`). It also assumes debug files exist or are meaningful enough to inspect (`prompt-template/block/codex-review-failed.md:8`). It is narrower than the current stop-hook failure helper because it does not distinguish non-zero exit, missing result, empty result, or stdout-copy failures.
- dependencies_and_callers: Reference search found no active `load_template` or `load_and_render_safe` caller for `block/codex-review-failed.md`. The current active failure path is inline in `hooks/loop-codex-stop-hook.sh`: `codex_failure_exit()` constructs a markdown string directly at `hooks/loop-codex-stop-hook.sh:813`, with branches for non-zero exit (`hooks/loop-codex-stop-hook.sh:840`), failed stdout copy (`hooks/loop-codex-stop-hook.sh:865`), missing result file (`hooks/loop-codex-stop-hook.sh:877`), and empty result file (`hooks/loop-codex-stop-hook.sh:904`). The `rg` sweep found only the template itself plus inline stop-hook text under the same heading.
- edge_cases_or_failure_modes: Because this template is currently unreferenced, edits to it would not affect runtime behavior unless the stop hook is wired to load it. It also says “last 50 lines” (`prompt-template/block/codex-review-failed.md:13`), while the active stop hook tails 30 lines in its inline branches (`hooks/loop-codex-stop-hook.sh:843`, `hooks/loop-codex-stop-hook.sh:880`, `hooks/loop-codex-stop-hook.sh:885`), so even if wired in as-is, its text would not match current collection behavior unless the caller changed the tail count.
- validation_or_tests: The general template-reference test only fails for missing templates that are referenced by hooks; unreferenced templates are warnings, not hard failures (`tests/test-template-references.sh:130`, `tests/test-template-references.sh:139`). This file is therefore protected by directory existence only, not by an active caller contract test.
- skip_candidate: `yes: it is a plausible core failure-contract template, but current runtime code does not reference it and duplicates the failure message inline in the stop hook.`

### GENERAL_REFACTOR_AND_REVIEW-HZ-069 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: This is the mandatory periodic Codex audit prompt for RLCR goal alignment. It turns every fifth review cycle into a comprehensive checkpoint covering the original plan, goal tracker, implementation reality, deferred work, historical progress, and stagnation detection. The title marks the round number (`prompt-template/codex/full-alignment-review.md:1`), and the prompt states the checkpoint is mandatory every 5 rounds (`prompt-template/codex/full-alignment-review.md:3`).
- algorithmic_behavior: The prompt requires Codex to first read the original implementation plan (`prompt-template/codex/full-alignment-review.md:7`), inspect Claude’s inserted work summary (`prompt-template/codex/full-alignment-review.md:13`), audit every acceptance criterion in the goal tracker (`prompt-template/codex/full-alignment-review.md:19`), detect forgotten plan items (`prompt-template/codex/full-alignment-review.md:30`), re-evaluate deferrals (`prompt-template/codex/full-alignment-review.md:36`), perform a deep implementation review (`prompt-template/codex/full-alignment-review.md:50`), handle the rendered goal-tracker update section (`prompt-template/codex/full-alignment-review.md:57`), and review historical round files for stagnation (`prompt-template/codex/full-alignment-review.md:59`). It defines terminal markers: `STOP` for stagnation (`prompt-template/codex/full-alignment-review.md:84`) and `COMPLETE` only when all original plan tasks and acceptance criteria are fully met with no deferrals (`prompt-template/codex/full-alignment-review.md:91`).
- inputs_outputs_state: Inputs rendered by the stop hook include `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:681`). Output is a review prompt written to `.humanize/rlcr/<timestamp>/round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:632`). Codex is instructed to write findings to `REVIEW_RESULT_FILE` (`prompt-template/codex/full-alignment-review.md:88`). The stop hook then interprets the last non-empty line of that result as a transition marker (`hooks/loop-codex-stop-hook.sh:916`).
- gates_or_invariants: The scheduling gate is `CURRENT_ROUND % 5 == 4`, set in `hooks/loop-codex-stop-hook.sh:644`; only then does the hook render this template instead of `codex/regular-review.md` (`hooks/loop-codex-stop-hook.sh:679`). The acceptance gate is prompt-defined but hook-enforced only by the final marker: last line `COMPLETE` ends the loop as complete (`hooks/loop-codex-stop-hook.sh:923`), last line `STOP` ends the loop as stopped (`hooks/loop-codex-stop-hook.sh:933`), and any other final line creates the next round prompt and blocks exit (`hooks/loop-codex-stop-hook.sh:967`). The prompt explicitly treats deferred acceptance criteria as incomplete (`prompt-template/codex/full-alignment-review.md:92`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which builds review context after preliminary gates pass: state parsing and validation (`hooks/loop-codex-stop-hook.sh:77`), plan integrity (`hooks/loop-codex-stop-hook.sh:170`), todo completion (`hooks/loop-codex-stop-hook.sh:246`), git/large-file readiness (`hooks/loop-codex-stop-hook.sh:304`), summary existence (`hooks/loop-codex-stop-hook.sh:510`), and Round 0 goal-tracker initialization (`hooks/loop-codex-stop-hook.sh:539`). Template rendering uses the single-pass loader, so injected summary content cannot recursively expand template placeholders (`hooks/lib/template-loader.sh:54`). Historical files referenced by the prompt are under `.humanize/rlcr/{{LOOP_TIMESTAMP}}/round-*-summary.md` and `round-*-review-result.md` (`prompt-template/codex/full-alignment-review.md:63`).
- edge_cases_or_failure_modes: The hook accepts final marker strings mechanically; it does not parse the AC table or independently verify that no deferrals remain. A mistaken `COMPLETE` from Codex would end the loop despite the prompt’s stricter instruction. `STOP` is intended for full-alignment rounds, but the stop hook honors it even on non-alignment rounds (`hooks/loop-codex-stop-hook.sh:949`). Historical-file review assumes prior round artifacts exist; at the first full-alignment checkpoint, round history should be available from rounds 0 through 4, but missing or malformed files would be a Codex-review quality risk rather than a shell-level failure. Missing placeholder values would remain literal due to template-loader behavior (`hooks/lib/template-loader.sh:13`).
- validation_or_tests: `tests/test-template-references.sh` verifies that the referenced `codex/full-alignment-review.md` file exists when scanning `hooks/loop-codex-stop-hook.sh` (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:93`). `tests/test-state-exit-naming.sh` covers state-file naming outcomes for completion and stop markers. Reference search also shows `prompt-template/codex/regular-review.md` mirrors the final-marker contract for non-alignment rounds, providing a sibling template comparison point.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present; each assigned item_id appears in exactly one item heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`