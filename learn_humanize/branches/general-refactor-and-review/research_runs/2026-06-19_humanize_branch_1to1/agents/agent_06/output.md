# agent_06 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: Executable specification suite for the Humanize RLCR loop guard system. The directory recursively contains 12 shell specs and no nested test subdirectories: `run-all-tests.sh`, validator specs, template-rendering specs, plan-file lifecycle specs, state-exit specs, ANSI parsing specs, and the todo checker spec.
- algorithmic_behavior: The suite coordinates shell, Python, Git, `jq`, `sed`, `awk`, and template fixtures to assert the loop’s algorithmic contracts: safe template rendering, access-control hooks, plan immutability, active loop discovery, stop-hook review transitions, todo completion enforcement, and aggregate pass/fail accounting. The aggregate runner enumerates suites in a fixed order at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/run-all-tests.sh:32), captures each suite output at line 60, strips ANSI color before parsing counts at lines 64-68, and fails if any suite exits nonzero or reports failures at lines 73-80.
- inputs_outputs_state: Inputs are hook JSON payloads, temporary Git repositories, synthetic `.humanize/rlcr/<timestamp>/state.md` trees, prompt templates, and synthetic Claude transcript JSONL. Outputs are PASS/FAIL counters and process exit status. State transitions under test include `state.md` active-loop discovery versus terminal renamed state files in [tests/test-state-exit-naming.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-state-exit-naming.sh:38), current-round increments through the stop hook, and generated next-round prompts through `claude/next-round-prompt.md`.
- gates_or_invariants: Core invariants include: only `state.md` marks an active loop; legacy `.humanize-loop.local` paths are intentionally ignored; plan backup `plan.md` under `.humanize/rlcr` is read-only; `state.md` is hook-managed; round prompt files are read-only; todos files are generally blocked except a narrow active-loop allowlist; goal tracker edits are limited by round; Git branch and plan tracking state must remain stable; Codex model/effort and branch values must be YAML-safe.
- dependencies_and_callers: The tests cover `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/check-todos-from-transcript.py`, `hooks/loop-*-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, prompt templates under `prompt-template/`, and the portable timeout wrapper. `test-template-references.sh` explicitly scans hook scripts for referenced templates and requires critical validators to use safe fallback rendering at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-template-references.sh:174).
- edge_cases_or_failure_modes: The suite locks in edge behavior for shell metacharacters in plan paths, symlinks, path escapes, unreadable plan directories, no-Git and no-commit repos, dirty tracked plans, committed plan changes that differ from the loop backup, quoted YAML state fields, branch mismatch, command-substitution/glob/brace bypass attempts, missing templates, placeholder injection, ANSI-colored output parsing, malformed transcript lines, missing todo status, and incomplete `TodoWrite` states.
- validation_or_tests: Child roles inspected: `test-template-loader.sh` verifies single-pass placeholder rendering and shell metacharacter preservation; `test-templates-comprehensive.sh` validates all real templates, malformed placeholders, Unicode, multiline values, and load/render safety; `test-error-scenarios.sh` checks graceful missing-template fallback; `test-bash-validator-patterns.sh` exercises `command_modifies_file`; `test-allowlist-validators.sh` checks path-restricted allowlists; `test-plan-file-validation.sh` checks setup input gates; `test-plan-file-hooks.sh` checks prompt/stop hooks and plan integrity; `test-state-exit-naming.sh` checks terminal state naming; `test-template-references.sh` checks template references; `test-ansi-parsing.sh` checks runner parsing; `test-todo-checker.sh` checks transcript todo enforcement. I inspected recursively but did not execute the suite because the assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-036 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable spec for `hooks/check-todos-from-transcript.py`, the stop-hook quick gate that blocks RLCR loop exit when Claude’s latest `TodoWrite` still contains unfinished items.
- algorithmic_behavior: The spec sends JSON hook input to the Python checker and writes synthetic JSONL transcripts into a temporary directory. It tests parse handling first, then todo status interpretation, transcript format variants, and edge cases. Invalid hook stdin and empty stdin must exit `2` at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-todo-checker.sh:51) and line 63. Missing `transcript_path` and nonexistent transcript files are non-blocking exits `0` at lines 75-97.
- inputs_outputs_state: Inputs are stdin JSON payloads with optional `transcript_path` plus transcript JSONL entries in Claude assistant-message, direct `tool_use`, and alternative `message` formats. Outputs are exit status and, for incomplete todos, stdout containing an `INCOMPLETE_TODOS` marker plus todo detail. The underlying checker scans all JSONL lines, ignores malformed JSONL, keeps the most recent non-empty `TodoWrite.todos`, and treats only status `completed` as complete in [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/check-todos-from-transcript.py:20) and lines 113-126.
- gates_or_invariants: All todos in the latest `TodoWrite` call must be completed; earlier incomplete lists are superseded by later completed lists, as tested at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-todo-checker.sh:196). `pending`, `in_progress`, missing `status`, and empty-content pending todos are all incomplete gates. Empty transcript, absent transcript, invalid transcript lines with a valid completed todo, and no found todos are pass/skip cases.
- dependencies_and_callers: Depends on `python3` and `hooks/check-todos-from-transcript.py`. The stop hook calls this checker before expensive Codex review at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:246); parse exit `2` and incomplete exit `1` are converted into JSON block decisions at lines 253-293.
- edge_cases_or_failure_modes: Invalid stdin JSON is a hard parse failure; invalid JSONL transcript lines are ignored; missing `transcript_path` and nonexistent file fail open; direct `tool_use` and alternative `message` transcript shapes are accepted; missing todo status is treated as incomplete; Unicode content is accepted; only the latest non-empty todo list is authoritative.
- validation_or_tests: Sixteen numbered tests cover input parsing, all-complete versus incomplete lists, output detail, `in_progress`, empty/invalid transcript handling, latest-call precedence, three transcript shapes, missing status, empty content, and Unicode at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-todo-checker.sh:106) through line 293.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-066 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude next-round transition prompt template used when Codex review finds issues and the RLCR loop should continue rather than terminate.
- algorithmic_behavior: The template re-anchors the next Claude round to the original implementation plan, injects Codex’s review between explicit review markers, requires Claude to create todos for all discovered issues, and makes the goal tracker read-only after Round 0. It references `{{PLAN_FILE}}` at [prompt-template/claude/next-round-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/prompt-template/claude/next-round-prompt.md:5), injects `{{REVIEW_CONTENT}}` at lines 16-19, and references `{{GOAL_TRACKER_FILE}}` at lines 22-30.
- inputs_outputs_state: Inputs are template variables `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`, rendered via `load_and_render_safe` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:985). Output is `round-<next>-prompt.md`, after the stop hook updates `current_round` in `state.md` at lines 967-970 and before optional post-alignment, footer, push-note, and goal-tracker-update-request templates are appended at lines 990-1018.
- gates_or_invariants: The next round must not solve only the top issue; it must create todos for all discovered issues and attempt each one. The original plan remains the full-scope authority. The goal tracker can be read for Ultimate Goal, Acceptance Criteria, task status, plan evolution, and open issues, but direct modification after Round 0 is prohibited; updates must be requested in a summary rather than written directly.
- dependencies_and_callers: Called by the Codex stop hook’s “review found issues” branch. Rendering depends on `hooks/lib/template-loader.sh`, whose single-pass substitution prevents injected review content containing `{{...}}` from being re-expanded, as implemented at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/lib/template-loader.sh:50). The template’s existence is covered by broad template loading/reference tests, including all-template load/render coverage in `tests/test-templates-comprehensive.sh`.
- edge_cases_or_failure_modes: If this template is missing or renders empty, the stop hook has a fallback next-round prompt at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:977). If `REVIEW_CONTENT` contains placeholder-like text, single-pass rendering keeps it literal. If Claude needs goal-tracker changes after Round 0, the template intentionally routes that through a summary request rather than direct file modification.
- validation_or_tests: Template loading and placeholder behavior are covered by `test-template-loader.sh`, `test-templates-comprehensive.sh`, and `test-template-references.sh`; the stop-hook continuation path that renders `claude/next-round-prompt.md` is exercised indirectly by `test-plan-file-hooks.sh` through stop-hook state, plan, and goal-tracker gate scenarios.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `GENERAL_REFACTOR_AND_REVIEW-HZ-006`, `GENERAL_REFACTOR_AND_REVIEW-HZ-036`, `GENERAL_REFACTOR_AND_REVIEW-HZ-066`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`