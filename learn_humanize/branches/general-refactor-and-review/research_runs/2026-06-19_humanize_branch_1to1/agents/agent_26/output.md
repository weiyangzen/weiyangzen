# agent_26 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-026 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable regression specification for the RLCR loop hook allowlist policy. It verifies the deliberate exception set that lets validators access only a small set of generated loop files while preserving the broader rule that `round-*-todos.md` should normally be blocked in favor of native todo tooling. The script covers shared allowlist logic plus Read, Write, Edit, and Bash hook behavior.
- algorithmic_behavior: The test constructs an isolated temporary git-backed RLCR loop fixture under `.humanize/rlcr/2024-01-01_12-00-00`, writes a `state.md` frontmatter document with `current_round: 5`, `max_iterations: 42`, `plan_file`, `plan_tracked`, and `start_branch`, then drives the validators with JSON hook payloads on stdin. See fixture setup at `tests/test-allowlist-validators.sh:29-62`. The expected allowlist is narrow: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` are accepted only in the active loop directory; `round-3-todos.md`, `round-2-summary.md`, and same basenames in wrong directories are rejected at `tests/test-allowlist-validators.sh:70-124`. This corresponds directly to `is_allowlisted_file()` in `hooks/lib/loop-common.sh:165-186`, where the allowlist is exact path equality against `$active_loop_dir/$allowed`.
- inputs_outputs_state: Inputs are synthetic hook JSON objects such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}`, `Edit`, `Read`, and `Bash` command payloads, plus the environment variable `CLAUDE_PROJECT_DIR="$TEST_DIR"` so hooks discover the temporary project root (`tests/test-allowlist-validators.sh:130-137`). Outputs are validator exit codes and stderr/stdout messages captured into `RESULT`; success is `exit 0`, policy block is `exit 2`, and the test script exits with the aggregate failure count at `tests/test-allowlist-validators.sh:383-391`. State transitions are externalized through validator decisions rather than persistent mutation: the fixture state declares the active loop and current round, validators locate that active loop with `find_active_loop()` and parse `state.md`, and the tests assert whether each requested file access is permitted or blocked.
- gates_or_invariants: The primary invariant is exact active-loop path matching, not basename matching. Tests 7, 21, 23, 24, and 25 ensure that an allowlisted filename in `/other/path`, `/tmp`, a relative current directory, an old loop directory, or a different root with the same active-loop basename is blocked (`tests/test-allowlist-validators.sh:118-124`, `314-381`). The Write and Edit gates require allowlisted todos paths to pass but block non-allowlisted todos and wrong historical summaries (`tests/test-allowlist-validators.sh:133-183`, `189-226`). The Read gate similarly permits the historical summary/todo exceptions while blocking future/non-allowlisted round files (`tests/test-allowlist-validators.sh:232-282`). The Bash gate is stricter for shell writes: only commands containing the full active loop path and `round-[12]-todos.md` pass; basename-only or wrong-root commands fail (`tests/test-allowlist-validators.sh:288-381`, implemented at `hooks/loop-bash-validator.sh:136-144`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` at `tests/test-allowlist-validators.sh:15-17` and invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` as subprocesses. The shared library depends on `hooks/lib/template-loader.sh` for `load_and_render_safe()` and template discovery (`hooks/lib/loop-common.sh:46-56`, `hooks/lib/template-loader.sh:167-203`). Validator behavior depends on `jq` to parse hook payloads in each hook (`hooks/loop-write-validator.sh:23-31`, `hooks/loop-edit-validator.sh:22-30`, `hooks/loop-read-validator.sh:22-30`, `hooks/loop-bash-validator.sh:22-30`). The suite is part of the aggregate runner at `tests/run-all-tests.sh:32-45`, which includes `test-allowlist-validators.sh` as the final suite.
- edge_cases_or_failure_modes: The test intentionally covers several bypass classes: wrong active directory, old loop directory, same basename under a different project root, relative path write, and non-allowlisted future round todos. It also verifies that historical summaries may be allowlisted despite current round being `5`, while later summaries are blocked (`tests/test-allowlist-validators.sh:86-116`, `146-183`, `202-226`, `245-282`). One important failure mode is shell command detection fragility: Bash validation depends on regex matching in `command_modifies_file()` for redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv/cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` forms (`hooks/lib/loop-common.sh:257-285`). Commands outside those patterns may fall outside this specific test’s coverage. Another edge case is path case handling: Bash commands and active loop path are lowercased before matching, while `is_allowlisted_file()` uses exact case-sensitive equality (`hooks/loop-bash-validator.sh:29-30`, `136-140`; `hooks/lib/loop-common.sh:168-186`).
- validation_or_tests: The file itself defines 25 assertions. They are grouped into `is_allowlisted_file()` unit behavior (`tests/test-allowlist-validators.sh:64-124`), Write validator behavior (`127-183`), Edit validator behavior (`185-226`), Read validator behavior (`228-282`), and Bash validator path-restricted behavior (`284-381`). It uses `pass()` and `fail()` counters and exits with `TESTS_FAILED`, making it suitable for CI-style aggregation (`tests/test-allowlist-validators.sh:19-27`, `383-391`). I did not execute the suite because this branch export is read-only and the assignment asks for research notes only; the script is registered in the repository-wide test runner at `tests/run-all-tests.sh:32-45`.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-056 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Prompt/block template for the validator denial message used when a tool attempts to create, read, edit, or shell-modify blocked `round-*-todos.md` files. It is not algorithmic execution code itself, but it is part of the validator contract because `todos_blocked_message()` renders it as the user-facing gate explanation.
- algorithmic_behavior: The template is static Markdown with one policy instruction: do not create or access `round-*-todos.md` files, and use the native `TodoWrite` tool instead (`prompt-template/block/todos-file-access.md:1-8`). It contains no placeholders, so rendering is direct. `hooks/lib/loop-common.sh:188-197` wraps it in `todos_blocked_message()`, providing a fallback string if the template is missing or empty. The loading path uses `load_and_render_safe()`, which loads the template, falls back on missing/empty content, and otherwise renders variables in a single pass (`hooks/lib/template-loader.sh:167-203`).
- inputs_outputs_state: Inputs are indirect: validators call `todos_blocked_message "Read|Write|Edit|Bash"` after detecting a disallowed todos file access. The current template ignores the action argument, so the output is the same Markdown block for every action. The output is emitted to stderr by the hook before returning `exit 2`, as seen in Write at `hooks/loop-write-validator.sh:37-44`, Edit at `hooks/loop-edit-validator.sh:36-43`, Read at `hooks/loop-read-validator.sh:36-43`, and Bash at `hooks/loop-bash-validator.sh:136-143`. No persistent state is changed by the template; state transition is the hook’s decision to block the tool call.
- gates_or_invariants: The key invariant encoded by the template is that file-level todos access should be blocked in favor of native task tracking. The allowlist exceptions live outside the template in `is_allowlisted_file()` and validator conditionals, so this file only describes the blocked-path contract. The template also standardizes the message across validators, preventing divergent instructions between Read/Write/Edit/Bash gates. Its concise content means validator behavior is not coupled to dynamic template variables.
- dependencies_and_callers: Direct caller is `todos_blocked_message()` in `hooks/lib/loop-common.sh:188-197`. That function is called by `hooks/loop-write-validator.sh:41-43`, `hooks/loop-edit-validator.sh:40-42`, `hooks/loop-read-validator.sh:40-42`, and `hooks/loop-bash-validator.sh:140-142`. Template directory validation and rendering are provided by `hooks/lib/template-loader.sh`; `get_template_dir()` resolves `prompt-template` from `hooks/lib` (`hooks/lib/template-loader.sh:24-31`), while `validate_template_dir()` checks required subdirectories (`hooks/lib/template-loader.sh:205-222`). `tests/test-template-references.sh:149-167` lists `block/todos-file-access.md` among common templates expected to exist.
- edge_cases_or_failure_modes: If this template is missing, empty, or cannot be loaded, `todos_blocked_message()` falls back to an inline message (`hooks/lib/loop-common.sh:190-197`; fallback handling at `hooks/lib/template-loader.sh:176-203`). If the text drifts from validator behavior, users may receive misleading guidance, especially because the validators do allow specific migration/bootstrap exceptions for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` through shared allowlist logic. Another edge case is that the template references only `TodoWrite`; Read-only blocked access also receives the same instruction, which is coherent for task state but less action-specific.
- validation_or_tests: `tests/test-template-references.sh:152-167` checks that this common template exists. The allowlist validator tests indirectly depend on this template because blocked todos accesses are expected to return `exit 2` with output matching `todos` (`tests/test-allowlist-validators.sh:159-170`, `215-226`, `258-269`, `314-350`, `353-381`). The template loader tests elsewhere validate fallback-safe rendering generally, but this assigned file’s concrete integration point is the `todos_blocked_message()` path in `loop-common.sh`.
- skip_candidate: `yes: it is a static message template rather than a core algorithm implementation, but it remains relevant as part of the validator gate contract and blocked-access UX surface.`

## Worker Self-Test
- assigned_items_seen: `2 item sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`