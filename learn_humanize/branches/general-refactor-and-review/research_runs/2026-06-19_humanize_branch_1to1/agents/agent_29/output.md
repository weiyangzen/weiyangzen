# agent_29 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-029 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable negative-path specification for `hooks/lib/template-loader.sh`. It verifies that missing templates, invalid template directories, empty inputs, malformed placeholders, fallback rendering, and strict shell mode do not crash hook execution or produce misleading non-empty content. It is included in the full suite from `tests/run-all-tests.sh:32-45`.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` (`tests/test-error-scenarios.sh:11-15`), derives `TEMPLATE_DIR` through `get_template_dir`, then runs 12 assertion blocks. The first group asserts missing files/directories return empty output with success status via `load_template` and `load_and_render` (`tests/test-error-scenarios.sh:42-78`). Rendering behavior is checked for empty content, literal special regex characters, empty placeholders, and unclosed placeholders (`tests/test-error-scenarios.sh:80-106`, `183-206`). It also wraps a missing-template call inside `bash -c` with `set -euo pipefail` to prove hook scripts continue under strict mode (`tests/test-error-scenarios.sh:108-131`). Safe fallback behavior is checked both without and with variable substitution (`tests/test-error-scenarios.sh:133-158`). Directory validation is specified as a real gate: valid template roots return 0, invalid paths return 1 (`tests/test-error-scenarios.sh:160-180`).
- inputs_outputs_state: Inputs are the project root, `hooks/lib/template-loader.sh`, the resolved `prompt-template` directory, template names, inline template strings, and `VAR=value` render assignments. Outputs are colored PASS/FAIL lines plus counters in `TESTS_PASSED` and `TESTS_FAILED` (`tests/test-error-scenarios.sh:17-35`, `211-225`). The terminal state transition is exit 0 only when `TESTS_FAILED` remains 0; otherwise exit 1 (`tests/test-error-scenarios.sh:218-225`). No repository files are modified by the script.
- gates_or_invariants: The central invariant is graceful degradation: missing template material should yield empty content or fallback content, not shell failure. That matches `load_template`, which emits empty stdout on missing files (`hooks/lib/template-loader.sh:36-47`), `load_and_render`, which renders only non-empty loaded content (`hooks/lib/template-loader.sh:134-147`), and `load_and_render_safe`, which substitutes a fallback when load/render output is empty (`hooks/lib/template-loader.sh:170-203`). The directory invariant is stricter: a valid template root must contain `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`). The rendering invariant is single-pass placeholder substitution with unreplaced placeholders preserved (`hooks/lib/template-loader.sh:50-132`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`. Related shared initialization appears in `hooks/lib/loop-common.sh:46-56`, which sources the loader, initializes `TEMPLATE_DIR`, and warns rather than failing when validation fails. Broader validating coverage exists in `tests/test-template-loader.sh:197-244` and `tests/test-templates-comprehensive.sh:441-490`. The suite runner invokes this assigned test as one ordered suite entry (`tests/run-all-tests.sh:32-45`).
- edge_cases_or_failure_modes: Covered edge cases include missing template file, missing template directory, missing template under `load_and_render`, empty render input, regex-like path values, strict shell mode with missing templates, missing safe-template fallback, fallback variable substitution, invalid template root, empty placeholder `{{}}`, and unclosed placeholder text (`tests/test-error-scenarios.sh:42-206`). One observed environment-specific failure: running `./tests/test-error-scenarios.sh` in this managed read-only branch returned exit 1 because Bash/awk here-string evaluation in `render_template` hit `cannot create temp file for here document: Operation not permitted` at `hooks/lib/template-loader.sh:129`; this caused rendering assertions 5, 8, 11, and 12 to fail even though missing-template and directory-gate checks passed.
- validation_or_tests: Direct validation attempted: `./tests/test-error-scenarios.sh`. Result in this read-only sandbox: 8 passed, 4 failed, exit 1 due the sandbox temp-file restriction described above. The script itself is the assigned executable spec and is also wired into `tests/run-all-tests.sh`.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-059 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Prompt/block template for an RLCR hook blocking decision when a round file is in the wrong active loop directory. It is an algorithmic error surface: validators use it to tell the agent the attempted action/path and the exact canonical path to use.
- algorithmic_behavior: The template is a short Markdown block with three placeholders: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:1-6`). Runtime rendering is handled through `load_and_render_safe`, so missing or empty template content falls back to inline messages rather than emitting a blank blocker (`hooks/lib/template-loader.sh:170-203`). The template content is single-pass rendered by `render_template`, so inserted paths/actions are not recursively interpreted as template syntax (`hooks/lib/template-loader.sh:50-132`).
- inputs_outputs_state: Inputs are validator-provided `ACTION`, `FILE_PATH`, and `CORRECT_PATH` values. The read validator passes `ACTION=read` plus the attempted and canonical paths (`hooks/loop-read-validator.sh:129-139`). The write validator passes `ACTION=write to` plus the attempted and canonical paths (`hooks/loop-write-validator.sh:177-187`). Output is rendered Markdown on stderr immediately before the validators exit with code 2, preserving hook state by blocking the attempted tool use rather than mutating files.
- gates_or_invariants: The key invariant is exact path equality with the active loop directory. For reads, after validating file type, active loop, round number, and allowlist, the validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and blocks when `FILE_PATH` differs (`hooks/loop-read-validator.sh:125-140`). For writes, the same canonicalization/blocking gate applies after summary, state, plan backup, and goal-tracker protections (`hooks/loop-write-validator.sh:173-188`). The path gate depends on `find_active_loop`, `parse_state_file`, `extract_round_number`, `is_round_file_type`, and `is_allowlisted_file` from `hooks/lib/loop-common.sh:62-186`.
- dependencies_and_callers: Direct callers are `hooks/loop-read-validator.sh:135` and `hooks/loop-write-validator.sh:183`. Both source `hooks/lib/loop-common.sh`, which in turn sources `template-loader.sh` and initializes/validates `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:46-56`). Template existence and safe-render usage are cross-checked by `tests/test-template-references.sh`, which scans hook scripts for template references (`tests/test-template-references.sh:51-111`) and verifies critical validators use safe rendering (`tests/test-template-references.sh:170-201`).
- edge_cases_or_failure_modes: If the template is missing or empty, callers still emit the inline fallback for wrong directory path (`hooks/loop-read-validator.sh:131-138`, `hooks/loop-write-validator.sh:180-186`). If the active loop directory cannot be found, both validators exit 0 earlier, so this template is not used (`hooks/loop-read-validator.sh:61-67`, `hooks/loop-write-validator.sh:78-85`). If an attempted path is allowlisted or already equals the active-loop canonical path, the wrong-directory blocker is bypassed by earlier allowlist/path checks. A content nuance: line 6 says `cat {{FILE_PATH}}` even though the intended remediation likely should point to `{{CORRECT_PATH}}`; as written, the template’s human guidance may repeat the blocked path despite line 4 showing the correct path.
- validation_or_tests: Referenced by `rg` in both read/write validators. Existence is covered by `tests/test-template-references.sh:93-107`; safe usage is covered for critical validators by `tests/test-template-references.sh:172-201`. The template participates indirectly in validator behavior tests, including wrong-directory access blocking in `tests/test-allowlist-validators.sh:314-325` for related round-file directory protections.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`