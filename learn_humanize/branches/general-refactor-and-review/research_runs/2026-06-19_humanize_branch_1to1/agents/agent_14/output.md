# agent_14 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-014 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Early RLCR stop-hook validator that prevents the loop from advancing to expensive Codex review while the latest Claude `TodoWrite` state still contains unfinished todos. It is a small gate in the stop path, invoked from [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:246).
- algorithmic_behavior: The main algorithm is `find_latest_todos(transcript_path)` in [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/check-todos-from-transcript.py:20). It reads a JSONL transcript line by line, ignores blank lines and invalid JSONL records, and keeps replacing `latest_todos` whenever it finds a non-empty `TodoWrite` input. It recognizes three transcript shapes: Claude assistant messages with `message.content[]` tool blocks at lines 51-63, alternative `type: message` entries at lines 65-76, and direct `type: tool_use` records at lines 78-85. The command entrypoint reads hook input JSON from stdin at lines 90-97, extracts `transcript_path`, expands `~`, scans for the latest todos, then treats every latest todo whose `status` is not exactly `completed` as incomplete at lines 113-120.
- inputs_outputs_state: Input is a JSON object on stdin, expected to contain `transcript_path`; the transcript file itself is JSONL. Output is process exit status plus optional text. Exit `0` means no blocking condition: no transcript path, missing transcript file, no todos found, or all latest todos have `status: completed`. Exit `1` prints `INCOMPLETE_TODOS` followed by `  - [status] content` lines to stdout at lines 121-126. Exit `2` prints `PARSE_ERROR: ...` to stderr when the hook input JSON itself cannot be parsed at lines 92-97. The script does not persist state; its only state transition is local `latest_todos` being overwritten as later `TodoWrite` calls are discovered.
- gates_or_invariants: The gate is fail-open for absent data but fail-closed for explicit incomplete todo data. A missing `transcript_path` exits `0` at lines 99-101, a missing transcript path on disk returns an empty list at lines 25-26, and a transcript with no recognized todos exits `0` at lines 109-111. Once any latest todo exists, the invariant is strict: only literal `completed` passes; `pending`, `in_progress`, an empty status, or a missing status blocks at lines 115-119. Invalid JSONL transcript lines are ignored rather than blocking at lines 36-39.
- dependencies_and_callers: Depends only on Python standard library modules `json`, `sys`, and `pathlib.Path` at lines 15-17. The stop hook sets `TODO_CHECKER="$SCRIPT_DIR/check-todos-from-transcript.py"` and invokes it with the captured hook input at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:246). The caller maps exit `2` to a block response with system message `Loop: Blocked - todo checker parse error` at stop-hook lines 253-269, and maps exit `1` to an incomplete-todos template block at lines 272-293. README lists it as part of the lifecycle hook set at [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/README.md:224).
- edge_cases_or_failure_modes: Because missing transcript path, missing transcript file, empty transcript, no recognized todos, and malformed JSONL lines all pass, the gate depends on the caller supplying a valid `transcript_path` and on the transcript format matching one of the three recognized shapes. A `TodoWrite` call with an empty `todos` array is ignored and cannot clear an earlier non-empty todo list, because `latest_todos` is only updated when `todos` is truthy at lines 62-63, 75-76, and 84-85. Todo content is inserted directly into stdout, so multiline or unusually formatted content could affect the rendered incomplete list, though tests cover ordinary empty content and simple text.
- validation_or_tests: Covered by [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-todo-checker.sh:1). The test script validates invalid/empty hook input returning exit `2`, missing transcript path and nonexistent transcript passing, all-completed todos passing, incomplete and in-progress todos blocking, invalid JSONL lines being ignored, latest `TodoWrite` winning, direct `tool_use` and alternative `message` formats, missing status as incomplete, empty content as incomplete, and Unicode content passing when completed. The stop-hook integration path is directly visible in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:248).
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-044 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR stop hook’s fail-closed git status gate. It defines the message shown when repository state cannot be verified because `git status --porcelain` failed or timed out.
- algorithmic_behavior: The template is static Markdown with one variable, `{{GIT_STATUS_EXIT}}`, at [prompt-template/block/git-status-failed.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/prompt-template/block/git-status-failed.md:3). It is selected by `loop-codex-stop-hook.sh` after the hook confirms it is in a git repository, runs `git status --porcelain` through `run_with_timeout`, captures the exit code, and sees a nonzero result. The stop hook then calls `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md" ... "GIT_STATUS_EXIT=$GIT_STATUS_EXIT"` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:320).
- inputs_outputs_state: Input is the shell variable `GIT_STATUS_EXIT`, populated from the timeout-wrapped git status command at stop-hook lines 310-312. Output is rendered Markdown used as the JSON `reason` field in a stop-hook block decision at lines 322-323. The final hook output is a JSON object with `decision: block`, the rendered reason, and `systemMessage: Loop: Blocked - git status failed (exit N)`. The template stores no state; it describes a blocking state discovered by the hook.
- gates_or_invariants: This template supports a fail-closed invariant: if the hook cannot verify repository cleanliness, it blocks exit instead of continuing with stale or unknown git state. The relevant comment explicitly says not to use an empty fallback that would fail open at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:302). The template’s message names likely categories: git lock contention, invalid repository state, or slow large repository at lines 5-8.
- dependencies_and_callers: Loaded through the shared template system. `loop-common.sh` sources `template-loader.sh`, initializes `TEMPLATE_DIR`, and warns but degrades to inline fallbacks if the template directory is unavailable at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/lib/loop-common.sh:46). The call site uses `load_and_render_safe`, so a missing template falls back to the inline fallback text embedded in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:315). Template reference tests search for these template loader calls and verify template availability, including references under `prompt-template`.
- edge_cases_or_failure_modes: If `git rev-parse --git-dir` fails, this specific template is not used because the hook only enters the cached git status block when it has identified a repo at stop-hook line 307. If `git status` times out, returns nonzero due to lock contention, or is otherwise unable to inspect the worktree, the hook blocks. If the template file is missing or cannot render, `load_and_render_safe` uses the shorter inline fallback, preserving the blocking behavior but losing the more detailed bullet list from this file.
- validation_or_tests: The template is indirectly covered by the template reference/comprehensive tests discovered in `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`, which check that referenced prompt-template Markdown files exist and that `load_and_render_safe` paths are valid. The runtime behavior is exercised through the stop-hook git status gate at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:307).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections, matching the 2 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`