# agent_22 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Shell utility entrypoint for the Humanize RLCR loop monitor. It exposes `humanize monitor rlcr-loop`, finds the latest active `.humanize/rlcr/<timestamp>` session, tails the latest Codex run log from the external cache, and draws a live terminal dashboard with session, goal-tracker, and git status context. Main entrypoints are `_humanize_monitor_codex` at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:9) and `humanize` at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:521).
- algorithmic_behavior: The monitor first requires `.humanize/rlcr` in the current project, then chooses the lexicographically newest timestamp directory matching `YYYY-MM-DD_HH-MM-SS` via `_find_latest_session` ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:23)). `_find_latest_codex_log` derives a sanitized current project path, scans `$HOME/.cache/humanize/<sanitized-project>/<session>/round-*-codex-run.log`, and selects the newest session, then highest round number ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:43)). It parses `state.md`, `goal-tracker.md`, and `git status`, draws a fixed 10-line status region, prints the last 50 log lines, then incrementally emits appended bytes while polling for newer logs and switching when found ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:456)).
- inputs_outputs_state: Inputs are the current working directory, `.humanize/rlcr` session directories, per-session `state.md` and `goal-tracker.md`, cached Codex logs, terminal dimensions from `tput`, and git worktree state. Outputs are terminal UI only: status lines, tail output, switch notifications, usage text, and error messages. Runtime state is local shell state: `current_file`, `current_session_dir`, `last_size`, `monitor_running`, cleanup flags, and traps. It does not write repository files.
- gates_or_invariants: Requires an active loop directory before monitoring; otherwise returns with an explanatory error ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:16)). Session directories must match the timestamp regex. Log discovery is intentionally tied to the same cache layout used by `hooks/loop-codex-stop-hook.sh`, which writes logs under `$HOME/.cache/humanize/$SANITIZED_PROJECT_PATH/$LOOP_TIMESTAMP` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-codex-stop-hook.sh:740)). Terminal state is restored on `INT` or `TERM` through `_cleanup` and `_restore_terminal` ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:393), [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:406)).
- dependencies_and_callers: README instructs users to source this script from `.bashrc` or `.zshrc` and then run `humanize monitor rlcr-loop` ([README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/README.md:90)). It depends on POSIX-ish shell tools plus Bash/Zsh features: `git`, `grep`, `sed`, `sort`, `wc`, `tail`, `stat`, `tput`, `seq`, `basename`, and terminal escape sequences. It depends semantically on RLCR state files created by setup/loop hooks and on the cache naming convention from the Codex stop hook. No repository caller invokes it automatically; it is a sourced interactive utility.
- edge_cases_or_failure_modes: If no log exists yet, it waits until one appears ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:434)). Missing `state.md` yields `N/A` defaults; missing `goal-tracker.md` yields zero counts and "No goal tracker". Non-git directories produce a "not a git repo" git summary. The script handles GNU and BSD `stat` for file size ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/scripts/humanize.sh:447)). Potential edge risks include unquoted arithmetic comparisons if parsed counts are non-numeric, terminal capability assumptions around `tput`, and lexicographic session ordering depending on strict timestamp naming. The `tail_pid` cleanup path is mostly inert because the current implementation uses direct `tail` calls rather than a persistent background `tail`.
- validation_or_tests: The repo has CI shell syntax validation for all `.sh` files with both `bash -n` and `zsh -n` ([.github/workflows/shell-syntax-check.yml](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/.github/workflows/shell-syntax-check.yml:29)). I did not find a focused behavioral test for `humanize monitor rlcr-loop`; coverage appears syntax-level plus README usage documentation.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-052 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Guard-message template used when an actor attempts to write or edit `round-*-prompt.md`. It defines the review contract that prompt files are instructions from Codex to Claude and must be read, not modified ([prompt-template/block/prompt-file-write.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/prompt-template/block/prompt-file-write.md:1)).
- algorithmic_behavior: This file is static text, but it participates in enforcement through hook rendering. `prompt_write_blocked_message` loads it via `load_and_render_safe` with an inline fallback if the template is missing ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/lib/loop-common.sh:199)). The template loader uses single-pass substitution and falls back safely for missing templates; this particular template has no placeholders, so rendering is direct content output ([hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/lib/template-loader.sh:167)).
- inputs_outputs_state: Input is an attempted Bash, Write, or Edit operation targeting a prompt-round file. Output is a blocking diagnostic telling the user to read the current prompt, execute tasks, write the summary, and document prompt errors in the summary. It has no mutable state and no dynamic variables.
- gates_or_invariants: The invariant is that `round-N-prompt.md` files are read-only instruction sources. Bash command attempts matching `round-[0-9]+-prompt\.md` emit this block and exit `2` ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-bash-validator.sh:111)). Write and Edit validators also block files identified as round prompt files ([hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-write-validator.sh:47), [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/loop-edit-validator.sh:46)). The shared filename classifier recognizes `round-[0-9]+-prompt.md` endings ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/hooks/lib/loop-common.sh:144)).
- dependencies_and_callers: Direct caller is `prompt_write_blocked_message` in `hooks/lib/loop-common.sh`. Effective callers are `loop-bash-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh`. The README documents prompt files as "Instructions FROM Codex TO Claude", matching this template’s contract ([README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/README.md:250)).
- edge_cases_or_failure_modes: If the template file is missing or empty, the safe loader returns the fallback text, so enforcement remains active but the richer guidance is reduced. The guard depends on path matching; unusual prompt-file names outside the `round-N-prompt.md` convention would not be covered by this exact block. Bash validator detection is command-text based, so complex shell indirection may depend on `command_modifies_file` accuracy outside this template.
- validation_or_tests: `tests/test-template-references.sh` includes `block/prompt-file-write.md` in the common template existence checks ([tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/general-refactor-and-review/tests/test-template-references.sh:152)). The validator hooks reference the message function directly, and the repo has broader template and validator tests, but this file itself has no executable logic.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 item headings above; item IDs intentionally not repeated here`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`