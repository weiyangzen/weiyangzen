# agent_28 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-028 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash write-detection algorithm used by the RLCR hook layer. The file explicitly tests `command_modifies_file` from `hooks/lib/loop-common.sh`, which is the shared detector used by `hooks/loop-bash-validator.sh` to prevent shell commands from bypassing Write/Edit validation.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:11-13`, then defines two assertion paths: `assert_modifies` expects `command_modifies_file` to return success, while `assert_not_modifies` expects failure (`tests/test-bash-validator-patterns.sh:36-62`). Each tested command is normalized through `to_lower` before matching (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:54-57`), matching the production Bash hook flow where `COMMAND_LOWER=$(to_lower "$COMMAND")` is computed before validation (`hooks/loop-bash-validator.sh:29-30`). The implementation under test is regex-driven, iterating a fixed set of patterns for redirects, `tee`, in-place editors, `mv/cp/rm`, `dd`, `truncate`, `printf >`, and file-descriptor `exec >` (`hooks/lib/loop-common.sh:260-285`).
- inputs_outputs_state: Inputs are literal Bash command strings plus an optional target file regex. The default target pattern is `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:38-39`, `tests/test-bash-validator-patterns.sh:52-53`); later groups override this with `state\.md` and `round-[0-9]+-summary\.md` (`tests/test-bash-validator-patterns.sh:181-194`). Outputs are PASS/FAIL lines, `TESTS_PASSED` and `TESTS_FAILED` counter transitions (`tests/test-bash-validator-patterns.sh:20-33`), and final process status: exit `0` when failures are zero, exit `1` otherwise (`tests/test-bash-validator-patterns.sh:206-214`). The test mutates only in-memory shell counters and writes no repository state.
- gates_or_invariants: The central invariant is that modification-like Bash forms must match while read-only inspection commands must not. Covered positive gates include redirection and append (`tests/test-bash-validator-patterns.sh:75-80`), `tee` with and without `-a` (`tests/test-bash-validator-patterns.sh:89-93`), `sed/awk/perl` in-place edits (`tests/test-bash-validator-patterns.sh:102-107`), `mv/cp/rm` destination/destructive operations (`tests/test-bash-validator-patterns.sh:116-121`), and lower/upper-case command equivalence via normalization (`tests/test-bash-validator-patterns.sh:80`). Covered non-modifying invariants include `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, and `diff` against the target path (`tests/test-bash-validator-patterns.sh:142-152`). In production, these matches are gates that return hook exit `2` for blocked operations, including state file writes (`hooks/loop-bash-validator.sh:76-79`), goal tracker writes (`hooks/loop-bash-validator.sh:100-109`), prompt writes (`hooks/loop-bash-validator.sh:116-119`), summary writes (`hooks/loop-bash-validator.sh:126-129`), and most todos writes (`hooks/loop-bash-validator.sh:136-143`).
- dependencies_and_callers: Direct dependencies are Bash, `set -euo pipefail`, `to_lower`, and `command_modifies_file` from `hooks/lib/loop-common.sh`. `loop-common.sh` also sources `hooks/lib/template-loader.sh` during load (`hooks/lib/loop-common.sh:46-51`), so template-directory validation can warn during test startup if the template tree is broken (`hooks/lib/loop-common.sh:53-56`). The tested helper is called by `hooks/loop-bash-validator.sh` for `.humanize/rlcr` protected files and branch-loop artifacts (`hooks/loop-bash-validator.sh:76`, `hooks/loop-bash-validator.sh:87`, `hooks/loop-bash-validator.sh:100`, `hooks/loop-bash-validator.sh:116`, `hooks/loop-bash-validator.sh:126`, `hooks/loop-bash-validator.sh:136`).
- edge_cases_or_failure_modes: The test explicitly covers bare redirection without a command (`> goal-tracker.md`) and no-space redirection (`echo test >goal-tracker.md`) at `tests/test-bash-validator-patterns.sh:161-164`. It also asserts that variable-indirect writes such as `echo x > $FILE` are not detected (`tests/test-bash-validator-patterns.sh:170-172`), which is an important known blind spot because the regex matcher does not evaluate shell variables. The comments document another known limitation: multi-source `cp file1.md file2.md goal-tracker.md` is not detected because the pattern expects `cp src dest` shape (`tests/test-bash-validator-patterns.sh:165-168`). More generally, the production matcher is not a shell parser; it relies on regular expressions over the normalized command string, so quoting tricks, variable expansion, command substitution, or unusual whitespace/path forms may evade or overmatch outside the enumerated forms.
- validation_or_tests: I ran `bash tests/test-bash-validator-patterns.sh` in the read-only export. It completed successfully with `Passed: 48`, `Failed: 0`, and exit code `0`. The script itself is the validation harness for this algorithm and exercises target-specific patterns for `goal-tracker.md`, `state.md`, and `round-N-summary.md`.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-058 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: User-facing block template for the Codex stop hook when a worker/Claude attempts to exit an RLCR round without producing the required round summary file. It participates in the loop’s completion gate by supplying the rendered reason for a blocking decision.
- algorithmic_behavior: The template has one placeholder, `{{SUMMARY_FILE}}`, embedded in the required action block (`prompt-template/block/work-summary-missing.md:5-8`). It instructs the agent to write a work summary and lists required summary content: implementation, created/modified files, tests, and remaining items (`prompt-template/block/work-summary-missing.md:10-14`). The production stop hook constructs `SUMMARY_FILE="$LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` (`hooks/loop-codex-stop-hook.sh:510`) and, if the file does not exist, renders this template through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:512-520`).
- inputs_outputs_state: Input is the runtime `SUMMARY_FILE` variable passed as `SUMMARY_FILE=$SUMMARY_FILE` to the template renderer (`hooks/loop-codex-stop-hook.sh:519-520`). Output is rendered Markdown used as the JSON `reason` field in a stop-hook response with `"decision": "block"` and a concise `systemMessage` (`hooks/loop-codex-stop-hook.sh:522-529`). The template itself performs no state mutation. The state transition it participates in is external: exit is blocked while the expected round summary file is absent; after the file exists, the stop hook proceeds to later checks and summary review.
- gates_or_invariants: The invariant is one summary per current round at the exact path `round-${CURRENT_ROUND}-summary.md`. The gate condition is `[[ ! -f "$SUMMARY_FILE" ]]` (`hooks/loop-codex-stop-hook.sh:512`), so only file existence is checked at this stage, not content quality. Content quality is implied by the template’s checklist, then downstream logic reads `SUMMARY_CONTENT=$(cat "$SUMMARY_FILE")` after this gate (`hooks/loop-codex-stop-hook.sh:636`). If the template is missing or renders empty, `load_and_render_safe` falls back to an inline message and still substitutes variables (`hooks/lib/template-loader.sh:176-203`), preserving the blocking behavior.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh` at `hooks/loop-codex-stop-hook.sh:516-520`. Rendering depends on the template loader’s single-pass `{{VAR}}` substitution model, which leaves missing placeholders intact and avoids recursive expansion (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-58`). The template directory is resolved from `hooks/lib` to `prompt-template` by `get_template_dir` (`hooks/lib/template-loader.sh:24-31`), and `validate_template_dir` requires the `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-221`).
- edge_cases_or_failure_modes: Because the stop-hook gate only uses `-f`, an empty or low-quality summary file would satisfy this specific missing-summary check and move failure detection to later review stages. If `SUMMARY_FILE` contains unusual characters, rendering passes it as an environment-backed template value; the loader performs single-pass text replacement rather than shell evaluation, reducing placeholder-injection risk (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:71-129`). If this Markdown file is absent, the fallback at `hooks/loop-codex-stop-hook.sh:516-518` is shorter and omits the detailed summary checklist, but the JSON block decision still fires (`hooks/loop-codex-stop-hook.sh:522-530`).
- validation_or_tests: General template loading and fallback behavior are covered by `tests/test-template-loader.sh`, including existing-template use and fallback substitution (`tests/test-template-loader.sh:196-221`). Comprehensive template validation loads all Markdown templates under `prompt-template` and checks non-empty content (`tests/test-templates-comprehensive.sh:83-107`), so this file is included by directory traversal. Template reference checking searches hook references and would find direct use of `"block/work-summary-missing.md"` in `hooks/loop-codex-stop-hook.sh` (`tests/test-template-references.sh:130-140`), although the shown common-template list focuses on shared `loop-common.sh` message templates rather than this stop-hook-only block (`tests/test-template-references.sh:149-167`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2; item IDs appear in the two Item Evidence headings only`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`