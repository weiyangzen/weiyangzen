# agent_25 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-025 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-wide shell test orchestrator for the Humanize plugin. It is the executable aggregate validation surface for hook, template, todo, plan-file, state, ANSI parsing, and allowlist behavior. It does not implement the plugin algorithm directly, but it defines the ordered validation contract used to prove those algorithmic gates still behave as expected.
- algorithmic_behavior: The script initializes counters and a failed-suite accumulator, then iterates through a fixed ordered `TEST_SUITES` array at `tests/run-all-tests.sh:32-45`. For each suite, it builds `suite_path` from the script directory (`tests/run-all-tests.sh:14`, `tests/run-all-tests.sh:48`), skips missing files without failing (`tests/run-all-tests.sh:50-53`), executes the suite while capturing combined stdout/stderr (`tests/run-all-tests.sh:58-62`), strips ANSI color sequences from output (`tests/run-all-tests.sh:64-66`), extracts the final numeric `Passed:` and `Failed:` counts (`tests/run-all-tests.sh:67-68`), accumulates totals (`tests/run-all-tests.sh:70-71`), and records a failure if either the suite exit code is nonzero or the parsed failed count is greater than zero (`tests/run-all-tests.sh:73-78`). Successful suites print a pass line with parsed count (`tests/run-all-tests.sh:79`). Final state is summarized and converted into process exit status (`tests/run-all-tests.sh:84-102`).
- inputs_outputs_state: Inputs are the executable test suite files named in the local array: `test-template-loader.sh`, `test-bash-validator-patterns.sh`, `test-todo-checker.sh`, `test-plan-file-validation.sh`, `test-template-references.sh`, `test-state-exit-naming.sh`, `test-templates-comprehensive.sh`, `test-plan-file-hooks.sh`, `test-error-scenarios.sh`, `test-ansi-parsing.sh`, and `test-allowlist-validators.sh` (`tests/run-all-tests.sh:33-44`). It also depends on each suite emitting summary lines matching `Passed:[[:space:]]*[0-9]+` and `Failed:[[:space:]]*[0-9]+` (`tests/run-all-tests.sh:67-68`). Outputs are human-readable colored suite status lines, a total summary, and exit code `0` only if no failed suites were recorded (`tests/run-all-tests.sh:91-102`). Runtime state is held in `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` (`tests/run-all-tests.sh:28-30`), with per-suite `output`, `exit_code`, `passed`, and `failed` variables.
- gates_or_invariants: Missing suites are warning-level skips, not failures (`tests/run-all-tests.sh:50-53`), so the suite list is advisory unless the file exists. A present suite fails the aggregate gate if it exits nonzero even with zero parsed failures, or if it reports any parsed failures even with exit code zero (`tests/run-all-tests.sh:73`). ANSI stripping is an invariant of count parsing, using `$'\033'` plus `sed` rather than GNU-only escape syntax for portability (`tests/run-all-tests.sh:64-68`); `tests/test-ansi-parsing.sh` explicitly documents that this is intended to work on both GNU/Linux and BSD/macOS sed. The aggregate final success gate is `FAILED_SUITES` length equal to zero (`tests/run-all-tests.sh:91-102`).
- dependencies_and_callers: Direct dependencies are Bash, `sed`, `grep`, `tail`, executable permissions on child suites, and the child suites’ stable summary format. The child suites cover the main algorithmic guard surfaces: template loader/rendering behavior, bash write-detection regexes, todo transcript parsing, plan file validation, template reference completeness, loop state exit naming, comprehensive template syntax/rendering, plan-file hook enforcement, loader error scenarios, ANSI parsing, and allowlist behavior. Related implementation references include `hooks/lib/loop-common.sh`, where shared message and command-detection helpers live, and validator scripts such as `hooks/loop-bash-validator.sh`. The runner itself is likely called manually or by CI as `./tests/run-all-tests.sh` per usage comment (`tests/run-all-tests.sh:5`); no in-repo caller was found beyond references in test comments and search hits.
- edge_cases_or_failure_modes: If a suite is absent, the aggregate can still exit success because missing files are skipped (`tests/run-all-tests.sh:50-53`), which is useful for optional suites but weakens enforcement against accidental deletion unless CI separately checks file presence. If a suite changes its summary wording away from `Passed:`/`Failed:`, the parsed counts fall back to `0` because of `|| echo "0"` (`tests/run-all-tests.sh:67-68`), so exit code becomes the stronger signal in that case. If a suite emits multiple summary lines, only the last parsed count is used due to `tail -1` (`tests/run-all-tests.sh:67-68`). Because the script uses `set -uo pipefail` rather than global `set -e` initially (`tests/run-all-tests.sh:12`), it deliberately controls failure capture around suite execution with `set +e`/`set -e` (`tests/run-all-tests.sh:59-62`); after the first suite, `set -e` remains active, so unguarded command failures later in the loop could abort the runner. Count extraction is guarded with `|| echo "0"`, reducing that risk for expected no-match cases.
- validation_or_tests: The file is itself the aggregate validator. Its ANSI parsing mechanism is covered by `tests/test-ansi-parsing.sh`, which tests color stripping and count extraction behavior corresponding to `tests/run-all-tests.sh:64-68`. The listed suites provide coverage for the hook/template system: for example, `tests/test-template-references.sh` verifies common template references include `block/summary-bash-write.md` and that those files exist, while `tests/test-bash-validator-patterns.sh` verifies `command_modifies_file` recognizes write-like shell commands and avoids read-only commands. I did not execute the full runner in this read-only branch research pass; evidence is from direct file inspection and related reference tracing.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-055 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the summary-file Bash-write gate. It is a small prompt/template artifact, but it participates directly in the algorithmic transition from an invalid shell mutation attempt to a blocked hook response that instructs the agent to use validated Write/Edit tooling for the correct round summary path.
- algorithmic_behavior: The template emits a short block message titled `Bash Write Blocked: Use Write or Edit Tool` (`prompt-template/block/summary-bash-write.md:1`). It states that Bash commands must not modify summary files (`prompt-template/block/summary-bash-write.md:3`), renders a validated destination via `{{CORRECT_PATH}}` (`prompt-template/block/summary-bash-write.md:5`), and explains that shell commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/summary-bash-write.md:7-8`). The template is loaded by `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:217-226`, which passes `CORRECT_PATH=$correct_path` to `load_and_render_safe`.
- inputs_outputs_state: Input is a single template variable, `CORRECT_PATH`, supplied by `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:219-225`). In the Bash validator path, that value is computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-bash-validator.sh:126-128`). Output is rendered Markdown sent to stderr by the validator (`hooks/loop-bash-validator.sh:128`) before exiting with code `2` (`hooks/loop-bash-validator.sh:129`). The template itself has no mutable state; state comes from the active loop directory and parsed current round in `hooks/loop-bash-validator.sh:36-49`.
- gates_or_invariants: The invariant is that summary-file changes must go through Write/Edit hooks, not Bash, so round/path validation remains enforceable. The gate is triggered when `command_modifies_file "$COMMAND_LOWER" "round-[0-9]+-summary\.md"` returns true (`hooks/loop-bash-validator.sh:126`). The command detector recognizes redirections, `tee`, in-place `sed`/`awk`/`perl`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns (`hooks/lib/loop-common.sh:257-285`). The rendered message always points to the current round summary path, not necessarily the path attempted by the invalid Bash command (`hooks/loop-bash-validator.sh:126-128`), preserving round-number correctness.
- dependencies_and_callers: Direct caller is `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:217-226`. That function uses `load_and_render_safe`, with a fallback containing the same essential `{{CORRECT_PATH}}` placeholder (`hooks/lib/loop-common.sh:221-225`), so a missing template still yields a usable block message. The runtime gate is `hooks/loop-bash-validator.sh`, which parses hook JSON with `jq`, ignores non-Bash tool invocations (`hooks/loop-bash-validator.sh:22-27`), finds the active loop, parses state, and applies file-modification gates. Reference tests include `tests/test-template-references.sh`, where `block/summary-bash-write.md` is listed among common templates that must exist (`tests/test-template-references.sh:152-167`), and `tests/test-templates-comprehensive.sh`, which validates template directory structure, loading, syntax, rendering, and fallback behavior.
- edge_cases_or_failure_modes: If the template file is missing, `load_and_render_safe` should fall back to the inline message in `hooks/lib/loop-common.sh:221-225`; the user still receives a block reason, but wording may be less polished. If `CORRECT_PATH` is empty or wrong because active loop state parsing failed, the template will faithfully render bad guidance; upstream active-loop and state parsing are therefore critical. The Bash command detector is regex-based and has known pattern boundaries: `tests/test-bash-validator-patterns.sh` notes multi-source `cp file1 file2 goal-tracker.md` is not detected for that target class, though common destination forms are. For summary files, attempts that do not literally expose a `round-N-summary.md` target in the command string may evade this specific gate unless another validator catches them. Conversely, the detector intentionally blocks broad command classes, so some complex read-only commands containing write-like syntax near a matching filename could be false positives.
- validation_or_tests: Template existence is validated by `tests/test-template-references.sh`, which includes `block/summary-bash-write.md` in `COMMON_TEMPLATES` and fails if it is missing (`tests/test-template-references.sh:152-167`). Template rendering and fallback behavior are covered by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`, including real-template rendering patterns with `CORRECT_PATH` variables (`tests/test-template-loader.sh:146-164`, `tests/test-templates-comprehensive.sh:492-514`). Bash write detection feeding this template is covered by `tests/test-bash-validator-patterns.sh`, especially the summary-file pattern group for redirection and read-only `cat` behavior. I did not execute tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item evidence headings present; each assigned item is represented once.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`