# agent_17 general-refactor-and-review 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `1fea22e96345f1005992936983b85317a156e3d5`

## Item Evidence

### GENERAL_REFACTOR_AND_REVIEW-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Code `Stop` hook for the RLCR loop. It is the exit gate and reviewer transition driver: it finds the active `.humanize/rlcr/<timestamp>/state.md`, validates loop invariants, runs Codex review, then either allows exit by ending the loop or blocks exit with the next-round prompt. It is registered as the `Stop` hook in `hooks/hooks.json:52-59`.
- algorithmic_behavior: Reads hook JSON from stdin (`HOOK_INPUT`) for todo-transcript validation, sets `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, sources `hooks/lib/loop-common.sh` and `scripts/portable-timeout.sh`, and selects the newest active loop with `find_active_loop` (`hooks/loop-codex-stop-hook.sh:29-63`, `hooks/lib/loop-common.sh:58-79`). It parses YAML frontmatter from `state.md`, maps fields into local loop variables, validates Codex model/effort/current round, and enforces v1.1.2 schema fields (`hooks/loop-codex-stop-hook.sh:72-131`). It then runs ordered gates: branch consistency, plan backup/original integrity, native TodoWrite completion, git status availability, large changed-file limit, clean working tree, optional push requirement, summary-file existence, Round 0 goal-tracker initialization, and max-iteration cutoff (`hooks/loop-codex-stop-hook.sh:133-618`).
- inputs_outputs_state: Inputs are stdin hook payload, `.humanize/rlcr/<timestamp>/state.md`, loop `plan.md` backup, project plan file, `round-N-summary.md`, `goal-tracker.md`, git state, todo transcript, templates, and Codex CLI availability. Outputs are Claude hook JSON blocks such as `{"decision":"block","reason":...}`, stderr diagnostics, `round-N-review-prompt.md`, `round-N-review-result.md`, next `round-(N+1)-prompt.md`, cache logs under `$HOME/.cache/humanize/<sanitized-project>/<timestamp>/`, and `state.md` mutations. State transitions are: no active `state.md` -> allow exit; invalid model/effort/current round -> `end_loop ... unexpected`; max iteration exceeded -> `maxiter-state.md`; Codex final line `COMPLETE` -> `complete-state.md`; final line `STOP` -> `stop-state.md`; otherwise increment `current_round` and block exit with next-round instructions (`hooks/loop-codex-stop-hook.sh:90-112`, `hooks/loop-codex-stop-hook.sh:614-617`, `hooks/loop-codex-stop-hook.sh:923-970`, `hooks/lib/loop-common.sh:301-334`).
- gates_or_invariants: Active loop is defined solely by newest `.humanize/rlcr/*/state.md`, matching README contract (`README.md:114-118`, `README.md:242-260`). Branch must remain `start_branch`; plan file must exist and match the loop backup, with extra `git status` check when `plan_tracked=true` (`hooks/loop-codex-stop-hook.sh:137-238`). Git status failures block rather than fail open (`hooks/loop-codex-stop-hook.sh:297-326`). Changed code/docs files over 2000 lines block (`hooks/loop-codex-stop-hook.sh:328-409`). Any uncommitted changes block before Codex review, with optional push-ahead block when `push_every_round=true` (`hooks/loop-codex-stop-hook.sh:411-504`). Round 0 goal-tracker placeholders in Ultimate Goal, Acceptance Criteria, or Active Tasks block before Codex (`hooks/loop-codex-stop-hook.sh:533-606`). Codex completion markers are strict: last non-empty trimmed line must be exactly `COMPLETE` or `STOP`, preventing false positives such as “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:916-934`).
- dependencies_and_callers: Called by Claude Code via `hooks/hooks.json`. Depends on `hooks/lib/loop-common.sh` for state parsing, markers, active-loop selection, template directory setup, and `end_loop`; `hooks/lib/template-loader.sh` for single-pass `{{VAR}}` rendering and fallback templates; `scripts/portable-timeout.sh` for portable timeouts; `hooks/check-todos-from-transcript.py` for latest TodoWrite completion scan; `prompt-template/block/*`, `prompt-template/codex/*`, and `prompt-template/claude/*` for block/review/next-round messages; `scripts/setup-rlcr-loop.sh` for creating initial `state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:485-615`). Runtime commands include `git`, `diff`, `jq`, `awk`, `grep`, `sed`, `wc`, `python3`, `codex`, `tail`, `cp`, and `mkdir`.
- edge_cases_or_failure_modes: Missing active loop or missing `state.md` allows exit silently, by design (`hooks/loop-codex-stop-hook.sh:59-73`). Missing old-schema fields block with guidance instead of ending the loop (`hooks/loop-codex-stop-hook.sh:119-131`). Some git checks use `git -C "$PROJECT_ROOT"` while later cached status / push checks rely on current working directory, so hook cwd must match project root for those gates (`hooks/loop-codex-stop-hook.sh:137-204`, `hooks/loop-codex-stop-hook.sh:307-311`, `hooks/loop-codex-stop-hook.sh:477-482`). Round 0 goal-tracker validation only runs if `goal-tracker.md` exists and detects the generic pattern `\[To be [a-z]`; missing sections or differently worded placeholders may bypass that specific gate (`hooks/loop-codex-stop-hook.sh:539-570`). If Codex exits 0 but writes only stdout, stdout is copied as the review result; if stdout is chatter rather than a formal review, marker parsing still treats it as authoritative content (`hooks/loop-codex-stop-hook.sh:858-914`). Missing `jq` would prevent valid hook JSON because block responses are built with `jq`.
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers stop-hook parsing of quoted `plan_file`/`start_branch`, plan modification/deletion/backup gates, tracked-plan race conditions, old-schema JSON block behavior, and section-specific goal-tracker placeholder reporting (`tests/test-plan-file-hooks.sh:388-429`, `tests/test-plan-file-hooks.sh:469-653`, `tests/test-plan-file-hooks.sh:655-1010`). `tests/test-template-references.sh` scans `loop-codex-stop-hook.sh` template references and verifies referenced template files exist (`tests/test-template-references.sh:54-111`). `tests/test-state-exit-naming.sh` validates active-state detection and exit-state renaming. `tests/test-todo-checker.sh` exercises the transcript todo helper used before Codex review.
- skip_candidate: `no`

### GENERAL_REFACTOR_AND_REVIEW-HZ-047 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Block-message template for the Round 0 Goal Tracker initialization gate. It is not executable by itself, but it defines the contract shown to Claude when the stop hook detects placeholder content in required goal-tracker sections.
- algorithmic_behavior: The stop hook extracts three sections from `.humanize/rlcr/<timestamp>/goal-tracker.md`: `### Ultimate Goal`, `### Acceptance Criteria`, and `#### Active Tasks`. If any section still contains the placeholder pattern `\[To be [a-z]`, it builds `MISSING_ITEMS`, renders this template through `load_and_render_safe`, and returns a JSON block with system message `Loop: Goal Tracker not initialized in Round 0` (`hooks/loop-codex-stop-hook.sh:537-605`). The template then instructs Claude to read `{{GOAL_TRACKER_FILE}}`, replace placeholders with actual Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to ACs, then write the updated goal tracker (`prompt-template/block/goal-tracker-not-initialized.md:1-18`).
- inputs_outputs_state: Inputs are template variables `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, supplied by the stop hook (`hooks/loop-codex-stop-hook.sh:572-594`). Output is rendered markdown embedded as the `reason` field in the hook’s JSON block. The template does not directly mutate state, but it causes the stop attempt to remain blocked until Claude updates `goal-tracker.md` and exits again. It protects the Round 0-only mutable window for the immutable goal/AC sections.
- gates_or_invariants: The invariant is that the Goal Tracker’s immutable section must be concretely initialized before the first review loop can proceed. The template explicitly states the immutable section can only be set in Round 0 and becomes read-only afterward (`prompt-template/block/goal-tracker-not-initialized.md:16-18`). This aligns with setup’s generated placeholders and initial Round 0 prompt requirements (`scripts/setup-rlcr-loop.sh:507-579`, `scripts/setup-rlcr-loop.sh:605-615`), and with later validators/templates that block direct goal-tracker edits after Round 0 (`hooks/lib/loop-common.sh:287-299`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` in `hooks/loop-codex-stop-hook.sh:592`. Depends on the template loader’s single-pass placeholder renderer, which preserves missing variables and avoids recursive substitution (`hooks/lib/template-loader.sh:50-132`). It is coupled to setup-generated placeholder wording only loosely through the stop hook’s generic placeholder regex rather than exact template text.
- edge_cases_or_failure_modes: If this template is missing or renders empty, the hook falls back to an inline shorter message, so the gate still blocks but with less detailed remediation (`hooks/loop-codex-stop-hook.sh:587-594`, `hooks/lib/template-loader.sh:167-203`). If `MISSING_ITEMS` is empty, the hook does not render this template. If `goal-tracker.md` is absent, the hook’s Round 0 template path is skipped because the check is guarded by `[[ -f "$GOAL_TRACKER_FILE" ]]` (`hooks/loop-codex-stop-hook.sh:539`). Template wording says “3-7” ACs, but enforcement is instructional only; the hook only checks placeholder removal, not AC count or task-to-AC correctness.
- validation_or_tests: Section-specific tests verify the rendered block reports only the missing sections and all missing sections when all placeholders remain (`tests/test-plan-file-hooks.sh:733-1010`). Template existence is covered by the template-reference scanner, which includes `hooks/loop-codex-stop-hook.sh` in its checked scripts (`tests/test-template-references.sh:54-111`). Setup script generation provides the fixture placeholders that trigger this block (`scripts/setup-rlcr-loop.sh:537-579`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 headings present; each assigned item appears exactly once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`