# agent_17 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-017 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: This directory is the schema/template surface for Humanize plan generation and plan-refinement QA. Recursive inspection found two files: `prompt-template/plan/gen-plan-template.md` and `prompt-template/plan/refine-plan-qa-template.md`; there are no deeper descendants.
- algorithmic_behavior: `gen-plan-template.md` defines the canonical generated-plan structure: goal description, TDD-style acceptance criteria with positive and negative tests, path boundaries, feasibility hints, dependency/milestone sequencing, task breakdown, Claude-Codex deliberation, pending decisions, implementation notes, and translated-output rules (`prompt-template/plan/gen-plan-template.md:3`, `prompt-template/plan/gen-plan-template.md:6`, `prompt-template/plan/gen-plan-template.md:25`, `prompt-template/plan/gen-plan-template.md:57`, `prompt-template/plan/gen-plan-template.md:69`, `prompt-template/plan/gen-plan-template.md:80`, `prompt-template/plan/gen-plan-template.md:91`, `prompt-template/plan/gen-plan-template.md:99`, `prompt-template/plan/gen-plan-template.md:106`). `refine-plan-qa-template.md` defines the mandatory audit artifact for annotated plan refinement: summary, comment ledger, answers, research findings, applied plan changes, remaining decisions, and metadata (`prompt-template/plan/refine-plan-qa-template.md:3`, `prompt-template/plan/refine-plan-qa-template.md:7`, `prompt-template/plan/refine-plan-qa-template.md:19`, `prompt-template/plan/refine-plan-qa-template.md:38`, `prompt-template/plan/refine-plan-qa-template.md:60`, `prompt-template/plan/refine-plan-qa-template.md:86`, `prompt-template/plan/refine-plan-qa-template.md:108`).
- inputs_outputs_state: Inputs are draft requirements, Codex/Claude deliberation, extracted plan comments, and merged config values such as `alternative_plan_language`. Outputs are the main plan file, optional translated variant, and QA document. The directory does not persist runtime state; it defines artifact state shape and review contracts. The generation template requires variant filenames by inserting `_<code>` before the extension and preserving identifiers during translation (`prompt-template/plan/gen-plan-template.md:112`, `prompt-template/plan/gen-plan-template.md:114`, `prompt-template/plan/gen-plan-template.md:118`).
- gates_or_invariants: Every task must include exactly one routing tag, either `coding` or `analyze` (`prompt-template/plan/gen-plan-template.md:71`). Generated plans must include positive and negative tests per criterion (`prompt-template/plan/gen-plan-template.md:8`). Implementation code must not contain plan workflow terminology such as `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:101`). Translated variants are disabled when language config is empty, English, or unsupported (`prompt-template/plan/gen-plan-template.md:120`). The QA template requires disposition tracking for every comment and a convergence status (`prompt-template/plan/refine-plan-qa-template.md:11`, `prompt-template/plan/refine-plan-qa-template.md:117`).
- dependencies_and_callers: `commands/gen-plan.md` instructs the command to load merged config, use this plan structure, and conditionally write translated variants (`commands/gen-plan.md:62`, `commands/gen-plan.md:484`, `commands/gen-plan.md:569`). `commands/refine-plan.md` requires reading and fully populating `refine-plan-qa-template.md` (`commands/refine-plan.md:442`). `tests/test-gen-plan.sh` checks that the `gen-plan` command’s embedded plan structure matches `gen-plan-template.md`; `tests/test-refine-plan.sh` asserts refine-plan config and QA-template behavior.
- edge_cases_or_failure_modes: Deterministic drafts can collapse upper/lower path boundaries into the same allowed implementation (`prompt-template/plan/gen-plan-template.md:44`). Output paths with no extension become `output_<code>` (`prompt-template/plan/gen-plan-template.md:116`). Unsupported alternative languages intentionally produce no variant, which prevents accidental extra files (`prompt-template/plan/gen-plan-template.md:120`). Refinement can leave unresolved decisions explicitly pending rather than hiding them (`prompt-template/plan/refine-plan-qa-template.md:86`).
- validation_or_tests: Template reference coverage exists through `tests/test-template-references.sh` and comprehensive template checks; command-level tests cover `gen-plan` and `refine-plan` behavior. I did not execute tests because this task is research-only and the repository export is read-only.
- skip_candidate: `no`

### ASK_GEMINI-HZ-047 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: PostToolUse Bash hook for RLCR session binding. It records the Claude session id into the active RLCR `state.md` immediately after setup, so subsequent validators can filter by leader session and avoid applying loop restrictions to spawned team sessions (`hooks/loop-post-bash-hook.sh:3`, `hooks/loop-post-bash-hook.sh:20`).
- algorithmic_behavior: The hook reads hook JSON from stdin, resolves the project root from `CLAUDE_PROJECT_DIR` or `pwd`, and looks for `.humanize/.pending-session-id` (`hooks/loop-post-bash-hook.sh:26`, `hooks/loop-post-bash-hook.sh:30`, `hooks/loop-post-bash-hook.sh:33`). The signal file supplies the target state path and setup command signature (`hooks/loop-post-bash-hook.sh:40`). If a command signature exists, the hook extracts `.tool_input.command` with `jq` and only accepts a command that starts with the exact setup script path, quoted or unquoted, followed by end-of-string or whitespace (`hooks/loop-post-bash-hook.sh:60`, `hooks/loop-post-bash-hook.sh:70`, `hooks/loop-post-bash-hook.sh:82`). It then extracts `.session_id`, patches only an empty `session_id:` line with `awk`, moves the temp file over the state file, and removes the signal (`hooks/loop-post-bash-hook.sh:94`, `hooks/loop-post-bash-hook.sh:105`, `hooks/loop-post-bash-hook.sh:112`, `hooks/loop-post-bash-hook.sh:119`, `hooks/loop-post-bash-hook.sh:122`).
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/.pending-session-id`, the referenced RLCR `state.md`, and optionally `jq`. Outputs are an updated `session_id:` field and deletion of the one-shot signal file. State transition is pending setup signal -> bound state file -> signal consumed. Wrong-command and missing-session cases leave the signal intact for a later valid PostToolUse event (`hooks/loop-post-bash-hook.sh:88`, `hooks/loop-post-bash-hook.sh:100`).
- gates_or_invariants: No signal means no-op (`hooks/loop-post-bash-hook.sh:35`). Empty or missing state path causes signal cleanup and exit (`hooks/loop-post-bash-hook.sh:50`). Non-setup Bash commands must not consume the signal (`hooks/loop-post-bash-hook.sh:88`). Existing non-empty `session_id` is not overwritten (`hooks/loop-post-bash-hook.sh:107`). Replacement uses `awk -v` rather than `sed`, preserving session ids containing `/` or `&` (`hooks/loop-post-bash-hook.sh:110`).
- dependencies_and_callers: Registered as a Bash `PostToolUse` hook in `hooks/hooks.json:52`. Downstream session filtering is consumed by loop validators through shared session-id logic in `hooks/lib/loop-common.sh` and related validators. Tests in `tests/test-session-id.sh` cover recording, no-op behavior, command-boundary checks, special characters, false positives, quoted-prefix concatenation, and tab-delimited setup invocations (`tests/test-session-id.sh:110`, `tests/test-session-id.sh:464`, `tests/test-session-id.sh:531`, `tests/test-session-id.sh:826`, `tests/test-session-id.sh:883`, `tests/test-session-id.sh:966`).
- edge_cases_or_failure_modes: If `jq` is unavailable and a command signature is present, `HOOK_COMMAND` stays empty and the hook exits without consuming the signal (`hooks/loop-post-bash-hook.sh:62`, `hooks/loop-post-bash-hook.sh:66`). If the signal file omits the command signature, the command-boundary gate is skipped, making signal-file correctness important. The `awk` replacement matches exactly `session_id:`; a malformed line with trailing spaces would not be patched (`hooks/loop-post-bash-hook.sh:113`). Temp output is written beside the state file as `state.md.tmp.$$` before `mv` (`hooks/loop-post-bash-hook.sh:111`).
- validation_or_tests: Behavioral coverage is concentrated in `tests/test-session-id.sh`; hook installation/registration is covered by hook config tests. I did not run the tests because the branch export is read-only and some tests create or mutate `.humanize` test state.
- skip_candidate: `no`

### ASK_GEMINI-HZ-077 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI escape stripping and test-summary parsing. It protects the aggregate test runner’s ability to parse colored `Passed:` and `Failed:` lines on GNU/Linux and BSD/macOS sed (`tests/test-ansi-parsing.sh:3`, `tests/test-ansi-parsing.sh:5`).
- algorithmic_behavior: The script defines pass/fail counters, emits colored test status, then exercises the exact stripping primitive `sed "s/${esc}\\[[0-9;]*m//g"` where `esc=$'\033'` (`tests/test-ansi-parsing.sh:18`, `tests/test-ansi-parsing.sh:21`, `tests/test-ansi-parsing.sh:38`, `tests/test-ansi-parsing.sh:46`). Count extraction is specified via `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed:` followed by final numeric extraction (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`).
- inputs_outputs_state: Inputs are synthetic strings containing SGR color sequences, bold/color combinations, plain text, and multiline summaries. Outputs are PASS/FAIL lines, final counters, and exit code 0 when all cases pass or 1 otherwise (`tests/test-ansi-parsing.sh:163`, `tests/test-ansi-parsing.sh:170`).
- gates_or_invariants: The parser must remove basic color sequences, multiple colors in one line, bold+color sequences, and no-op clean plain text (`tests/test-ansi-parsing.sh:42`, `tests/test-ansi-parsing.sh:55`, `tests/test-ansi-parsing.sh:132`, `tests/test-ansi-parsing.sh:146`). It must correctly extract nonzero and zero failed counts and use the last count in multiline output (`tests/test-ansi-parsing.sh:83`, `tests/test-ansi-parsing.sh:97`, `tests/test-ansi-parsing.sh:112`).
- dependencies_and_callers: Depends on Bash ANSI-C quoting, `sed`, and `grep -E`. The comments tie it to `run-all-tests.sh` output parsing (`tests/test-ansi-parsing.sh:5`). It is part of the test suite rather than production runtime.
- edge_cases_or_failure_modes: The stripping regex only covers numeric semicolon SGR sequences ending in `m`; cursor movement, OSC sequences, bracketed paste, and non-SGR escapes are outside the contract. The script uses `set -uo pipefail` rather than `set -e`, allowing individual assertions to record failures instead of aborting immediately (`tests/test-ansi-parsing.sh:9`).
- validation_or_tests: The file is self-validating with eight deterministic scenarios and a final failure-count gate (`tests/test-ansi-parsing.sh:170`). I did not execute it because the assignment requested research notes only.
- skip_candidate: `no`

### ASK_GEMINI-HZ-107 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: Broad executable specification for the PR loop state machine, including setup mutual exclusion, reviewer-status classification, reaction gates, monitor phase detection, PR goal tracker parsing/updating, startup trigger rules, force-push/unpushed detection, and stop-hook integration.
- algorithmic_behavior: The test harness creates a temp repository, injects mock `gh` fixtures, sets `CLAUDE_PROJECT_DIR`, runs each test in a subshell, and reports pass/fail counters (`tests/test-pr-loop-system.sh:51`, `tests/test-pr-loop-system.sh:81`, `tests/test-pr-loop-system.sh:1761`). It verifies RLCR and PR loop mutual exclusion by creating active state directories and expecting setup scripts to reject the competing loop (`tests/test-pr-loop-system.sh:110`, `tests/test-pr-loop-system.sh:129`). Reviewer status cases are computed from issue comments, review comments, PR reviews, and latest commit timestamps: no comments, partial comments, all commented, all commented plus newer commits, and partial plus newer commits (`tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:173`, `tests/test-pr-loop-system.sh:236`, `tests/test-pr-loop-system.sh:493`, `tests/test-pr-loop-system.sh:1315`, `tests/test-pr-loop-system.sh:1347`).
- inputs_outputs_state: Inputs include JSON fixtures under `tests/fixtures`, env vars such as `MOCK_GH_PR_NUMBER`, `MOCK_GH_PR_STATE`, `MOCK_GH_LATEST_COMMIT_AT`, generated `.humanize/pr-loop/<timestamp>` state files, temporary git repos, and mock `gh`, `git`, and `codex` executables (`tests/test-pr-loop-system.sh:18`, `tests/test-pr-loop-system.sh:154`, `tests/test-pr-loop-system.sh:1396`, `tests/test-pr-loop-system.sh:1599`). Outputs include asserted JSON from helper scripts, monitor text, goal-tracker markdown updates, loop state files such as `state.md`, `approve-state.md`, `cancel-state.md`, and final test counters (`tests/test-pr-loop-system.sh:423`, `tests/test-pr-loop-system.sh:459`, `tests/test-pr-loop-system.sh:616`, `tests/test-pr-loop-system.sh:1889`).
- gates_or_invariants: Reaction gates require Codex `+1`, Claude `eyes`, and `--after` filtering to reject old reactions (`tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:207`, `tests/test-pr-loop-system.sh:223`). Phase gates distinguish approved, waiting for initial review, waiting for reviewer, cancelled, max-iteration, and Codex-analyzing states (`tests/test-pr-loop-system.sh:256`, `tests/test-pr-loop-system.sh:271`, `tests/test-pr-loop-system.sh:291`, `tests/test-pr-loop-system.sh:459`, `tests/test-pr-loop-system.sh:474`, `tests/test-pr-loop-system.sh:849`). Trigger gates require no trigger for startup cases 1-3 in round 0, but require a trigger for cases 4-5 and later rounds (`tests/test-pr-loop-system.sh:1112`). Missing trigger-comment id in case 4/5 setup is a hard failure and must clean up the loop directory (`tests/test-pr-loop-system.sh:1384`, `tests/test-pr-loop-system.sh:1431`).
- dependencies_and_callers: Calls `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, `hooks/lib/loop-common.sh`, and `hooks/pr-loop-stop-hook.sh` (`tests/test-pr-loop-system.sh:123`, `tests/test-pr-loop-system.sh:142`, `tests/test-pr-loop-system.sh:159`, `tests/test-pr-loop-system.sh:201`, `tests/test-pr-loop-system.sh:258`, `tests/test-pr-loop-system.sh:515`, `tests/test-pr-loop-system.sh:935`, `tests/test-pr-loop-system.sh:1723`). It depends on `jq`, `git`, `timeout`, and the mock `gh` fixture protocol.
- edge_cases_or_failure_modes: The test mutates shared fixture files and restores them in many paths; an interrupted failure can leave fixtures altered. Some portability-sensitive commands appear directly, such as `timeout` and GNU-style `touch -d` (`tests/test-pr-loop-system.sh:880`, `tests/test-pr-loop-system.sh:943`). Some tests explicitly verify only pattern mechanics rather than full remote behavior, for example unpushed commit simulation (`tests/test-pr-loop-system.sh:566`). Monitor “Codex analyzing” relies on file mtime/growth cache under `/tmp`, which can be sensitive to timestamp resolution (`tests/test-pr-loop-system.sh:864`, `tests/test-pr-loop-system.sh:887`).
- validation_or_tests: This file is itself the validation surface. It exposes filters for major groups in `main`, including `mutual_exclusion`, `reviewer_status`, `reactions`, `phase`, `goal_tracker`, `case_4_5`, `setup_failure`, and `goal_tracker_integration` (`tests/test-pr-loop-system.sh:1773`). I did not execute it because it writes temp repos and mutates repository fixtures, which conflicts with this read-only research task.
- skip_candidate: `no`

### ASK_GEMINI-HZ-137 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block appended to the RLCR/Codex stop gate when git is dirty due to untracked files outside `.humanize`. It turns a generic git-clean failure into actionable guidance about artifacts and `.gitignore`.
- algorithmic_behavior: The template has no placeholders. It tells the agent that untracked files may be build artifacts, test outputs, runtime-generated files, dependencies, editor files, logs, caches, or temp files, and says to review them and add appropriate `.gitignore` patterns (`prompt-template/block/git-not-clean-untracked.md:2`, `prompt-template/block/git-not-clean-untracked.md:10`).
- inputs_outputs_state: Input is only the loader decision to include the block. Output is markdown text appended to a larger git-not-clean blocking prompt. It does not mutate state directly.
- gates_or_invariants: The production gate distinguishes local `.humanize` untracked files from other untracked files. Other untracked files load this block as `UNTRACKED_NOTE`; if the template is missing, the hook uses a fallback note (`hooks/loop-codex-stop-hook.sh:647`, `hooks/loop-codex-stop-hook.sh:650`, `hooks/loop-codex-stop-hook.sh:651`). The actual block remains the dirty-git stop gate (`hooks/loop-codex-stop-hook.sh:658`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` (`hooks/loop-codex-stop-hook.sh:650`). Template loader behavior is covered by template loader/reference tests.
- edge_cases_or_failure_modes: It is advisory rather than prescriptive: some untracked files may actually need committing, but the block biases artifacts toward ignore patterns. `.humanize` untracked files are intentionally excluded from this block’s trigger path (`hooks/loop-codex-stop-hook.sh:630`). If the template cannot be loaded, behavior degrades to a one-line fallback.
- validation_or_tests: Covered indirectly by template reference/comprehensive tests and by stop-hook dirty-git paths. No direct dedicated test was found for this exact block.
- skip_candidate: `no`

### ASK_GEMINI-HZ-167 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Prompt block for the RLCR read validator when an agent attempts to read a round-specific file from a non-current round. It supports the active-round invariant of the loop.
- algorithmic_behavior: The block renders the attempted round/file type, the current round, and the current round’s prompt and summary paths, then gives an explicit command form for reading the requested file if truly needed (`prompt-template/block/wrong-round-file.md:1`, `prompt-template/block/wrong-round-file.md:3`, `prompt-template/block/wrong-round-file.md:5`, `prompt-template/block/wrong-round-file.md:9`).
- inputs_outputs_state: Inputs are template variables `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH` supplied by the read validator (`hooks/loop-read-validator.sh:277`). Output is a blocking stderr message. It does not update state.
- gates_or_invariants: `hooks/loop-read-validator.sh` triggers this block when the target round differs from `CURRENT_ROUND` and the file is not allowlisted (`hooks/loop-read-validator.sh:271`). The validator exits with code 2 after rendering the block (`hooks/loop-read-validator.sh:282`). This keeps prompt/summary reads aligned with active loop state.
- dependencies_and_callers: Rendered through `load_and_render_safe` in `hooks/loop-read-validator.sh` with an inline fallback (`hooks/loop-read-validator.sh:272`, `hooks/loop-read-validator.sh:277`). It depends on filename parsing in shared loop-common helpers for round and file type detection.
- edge_cases_or_failure_modes: Allowlisted files bypass the round mismatch gate (`hooks/loop-read-validator.sh:271`). The template’s “use cat” escape route is deliberate but shifts control to Bash-command validation rather than the read-tool validator, so callers must rely on Bash validator policy for any follow-up command. If placeholders are missing, fallback rendering still blocks but with less precise guidance.
- validation_or_tests: Covered indirectly by read-validator and template rendering tests. No direct dedicated test was found for this exact block.
- skip_candidate: `no`

### ASK_GEMINI-HZ-197 `file` `scripts/lib/config-loader.sh`
- cursor: `[_]`
- core_role: Shared configuration merger for Humanize runtime scripts and command prompts. It centralizes precedence and error semantics for default, user, and project configuration, including `HUMANIZE_CONFIG` override behavior.
- algorithmic_behavior: The script has a source guard and strict shell options (`scripts/lib/config-loader.sh:2`, `scripts/lib/config-loader.sh:6`). `load_merged_config <plugin_root> <project_root>` requires `jq`, locates required default config at `config/default_config.json`, optional user config under `${XDG_CONFIG_HOME:-$HOME/.config}/humanize/config.json`, and optional project config from `${HUMANIZE_CONFIG:-$project_root/.humanize/config.json}` (`scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:75`, `scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:78`, `scripts/lib/config-loader.sh:84`). Each layer is normalized to a JSON object or `{}` by `_config_loader_prepare_layer` (`scripts/lib/config-loader.sh:24`). A jq program recursively strips nulls, then merges empty/default/user/project layers in order so later layers win (`scripts/lib/config-loader.sh:113`, `scripts/lib/config-loader.sh:119`, `scripts/lib/config-loader.sh:128`). `get_config_value` extracts a top-level key, returning strings as-is, null/missing as empty, and other JSON scalar/object values via `tostring` (`scripts/lib/config-loader.sh:139`, `scripts/lib/config-loader.sh:148`).
- inputs_outputs_state: Inputs are filesystem config files plus env vars `XDG_CONFIG_HOME`, `HOME`, and `HUMANIZE_CONFIG`. Output is merged JSON on stdout or a single extracted value. Persistent state is not written; temp files are created in a `mktemp -d` directory and removed by trap (`scripts/lib/config-loader.sh:100`, `scripts/lib/config-loader.sh:101`).
- gates_or_invariants: Missing `jq` is fatal (`scripts/lib/config-loader.sh:17`). Missing or malformed required default config is fatal (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`). Missing optional configs become `{}` (`scripts/lib/config-loader.sh:48`). Malformed optional configs warn and become `{}` (`scripts/lib/config-loader.sh:57`). Config files must be JSON objects, not arrays or scalars (`scripts/lib/config-loader.sh:52`). Nulls in higher layers are stripped before merge, preventing null from erasing lower defaults (`scripts/lib/config-loader.sh:119`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` sources this loader and derives defaults for bitlesson, Codex model/effort, and agent teams (`hooks/lib/loop-common.sh:179`, `hooks/lib/loop-common.sh:189`, `hooks/lib/loop-common.sh:192`, `hooks/lib/loop-common.sh:198`, `hooks/lib/loop-common.sh:210`, `hooks/lib/loop-common.sh:221`). `scripts/bitlesson-select.sh`, `commands/gen-plan.md`, and `commands/refine-plan.md` also depend on the same semantics (`commands/gen-plan.md:62`, `commands/refine-plan.md:86`).
- edge_cases_or_failure_modes: The loader uses temp files, so a non-writable temp directory can fail before config merge. If `HOME` is unset and `XDG_CONFIG_HOME` is unset, the user config path resolves from an empty home prefix. `HUMANIZE_CONFIG` points to an optional project config path, so a nonexistent override path is ignored rather than fatal. Because `get_config_value` is top-level only, callers must not expect nested key lookup.
- validation_or_tests: `tests/test-config-merge.sh` covers default-only, project override, user/project precedence, additive merge, null stripping, `HUMANIZE_CONFIG`, and all-layer merge (`tests/test-config-merge.sh:35`, `tests/test-config-merge.sh:66`, `tests/test-config-merge.sh:91`, `tests/test-config-merge.sh:112`, `tests/test-config-merge.sh:134`, `tests/test-config-merge.sh:152`, `tests/test-config-merge.sh:175`). `tests/test-config-error-handling.sh` covers missing default fatal, malformed optional warnings/fallback, empty project config, and missing optional config paths (`tests/test-config-error-handling.sh:36`, `tests/test-config-error-handling.sh:55`, `tests/test-config-error-handling.sh:83`, `tests/test-config-error-handling.sh:112`, `tests/test-config-error-handling.sh:130`, `tests/test-config-error-handling.sh:152`). I did not execute these tests because the task is read-only research.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item sections above; item identifiers are intentionally not repeated here to preserve the exactly-once item-id constraint.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`