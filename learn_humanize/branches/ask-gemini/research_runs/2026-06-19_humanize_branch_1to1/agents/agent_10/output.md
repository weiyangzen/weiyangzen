# agent_10 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-010 `directory` `templates`
- cursor: `[_]`
- core_role: Project-level template seed directory for runtime memory artifacts. The directory currently contains one recursively discovered child, `templates/bitlesson.md`, which seeds `.humanize/bitlesson.md` for RLCR loops.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` resolves `PLUGIN_BITLESSON_TEMPLATE="$SCRIPT_DIR/../templates/bitlesson.md"` and calls `scripts/bitlesson-init.sh` before state creation (`scripts/setup-rlcr-loop.sh:867-873`). `bitlesson-init.sh` creates the project BitLesson file from the template only when missing, and never overwrites an existing knowledge base (`scripts/bitlesson-init.sh:10-14`, `scripts/bitlesson-init.sh:83-86`). This makes `templates` the immutable bootstrap source for project memory rather than the live store.
- inputs_outputs_state: Input is the plugin template file path plus `--project-root` and `--bitlesson-relpath`; output is a resolved BitLesson file path printed by `bitlesson-init.sh` (`scripts/bitlesson-init.sh:5-15`, `scripts/bitlesson-init.sh:88`). Runtime state is stored under `.humanize/bitlesson.md`, then referenced in loop state as `bitlesson_file` and `bitlesson_required` (`scripts/setup-rlcr-loop.sh:887-908`).
- gates_or_invariants: The directory has no depth limit concern in this branch because recursive inspection found only `templates/bitlesson.md`. The main invariant imposed by its child is strict entry field order. `bitlesson-init.sh` adds path safety gates: project root must exist, template must exist, relpath must be relative and must not contain `..`, and an existing BitLesson path must be a regular file (`scripts/bitlesson-init.sh:60-80`).
- dependencies_and_callers: Direct caller is `scripts/setup-rlcr-loop.sh`; follow-on consumers are `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, and `hooks/loop-codex-stop-hook.sh`. Documentation coordinates the same workflow in `docs/bitlesson.md:27-35` and `commands/start-rlcr-loop.md:159-171`.
- edge_cases_or_failure_modes: Missing template blocks initialization (`scripts/bitlesson-init.sh:65-68`). Existing non-file at target blocks (`scripts/bitlesson-init.sh:78-80`). Empty or no-lesson live BitLesson files cause selector behavior to return `LESSON_IDS: NONE` without model invocation (`scripts/bitlesson-select.sh:94-104`). Strict delta mode can block `Action: none` when the live knowledge base has no concrete lessons (`scripts/bitlesson-validate-delta.sh:254-267`).
- validation_or_tests: Template presence and references are covered by template-reference tests, including `block/summary-bash-write.md` but BitLesson itself is primarily covered through `tests/test-bitlesson-select-routing.sh`, `tests/test-bitlesson-validate-delta.sh`, and setup execution checks such as `tests/robustness/test-setup-scripts-robustness.sh:1089-1095`. I did not run tests because this task is a read-only research pass and the scripts create temporary repositories and loop state.
- skip_candidate: `no`

### ASK_GEMINI-HZ-040 `file` `docs/usage.md`
- cursor: `[_]`
- core_role: User-facing behavioral contract for Humanize workflows. It defines the RLCR two-phase loop, plan quiz, command surfaces, PR loop state machine, config precedence, model resolution, monitoring, cancellation, and sandbox environment behavior.
- algorithmic_behavior: The RLCR algorithm is documented as Implementation Phase followed by Review Phase, with Codex review severities driving continued iteration until acceptance or no issues (`docs/usage.md:7-12`). The plan quiz is advisory preflight: two multiple-choice questions verify plan understanding, but missed answers allow either proceed or stop-and-review (`docs/usage.md:26-35`). The PR loop algorithm detects current PR, fetches bot comments, lets Claude fix issues, pushes, triggers re-review, polls every 30 seconds up to 15 minutes per bot, performs local Codex validation, and repeats until bots approve or max iterations is reached (`docs/usage.md:230-238`).
- inputs_outputs_state: Inputs include plan paths, CLI flags, bot flags, model/timeouts, base branch, config files, and environment variables. Outputs include structured plans, QA ledgers, `.humanize/rlcr/<timestamp>/` progress state, `.humanize/skill/<timestamp>/` ask-codex records, and PR loop state (`docs/usage.md:43-69`, `docs/usage.md:262-263`, `docs/usage.md:334-335`). Config state is resolved through four layers: plugin defaults, user config, project config, then CLI flags (`docs/usage.md:267-272`).
- gates_or_invariants: `start-pr-loop` requires at least one bot flag and an open PR with `gh` and Codex available (`docs/usage.md:215-243`). `start-rlcr-loop` supports a max iteration cap, full review interval with minimum 2, optional base branch, push-every-round, skip implementation, and Plan Understanding Quiz skipping (`docs/usage.md:74-102`). `gen-plan --auto-start-rlcr-if-converged` starts RLCR only after convergence and no pending user decisions, as described in command docs referenced by usage (`docs/usage.md:112-128`).
- dependencies_and_callers: This doc maps to command markdown in `commands/start-rlcr-loop.md`, `commands/start-pr-loop.md`, `commands/gen-plan.md`, and scripts such as `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh`. It also references install docs and monitoring helper sourcing (`docs/usage.md:3`, `docs/usage.md:319-332`).
- edge_cases_or_failure_modes: The doc explicitly warns about plan misunderstanding causing loop drift (`docs/usage.md:20-24`), supports `--skip-impl` when plan file is optional (`docs/usage.md:91-92`), ignores legacy config keys (`docs/usage.md:314-316`), and flags `HUMANIZE_CODEX_BYPASS_SANDBOX` as a dangerous override with unrestricted filesystem access (`docs/usage.md:343-367`).
- validation_or_tests: The documented setup and PR-loop contracts are exercised by `tests/robustness/test-setup-scripts-robustness.sh`, `tests/test-pr-loop-1-scripts.sh`, `tests/test-pr-loop-scripts.sh`, model/config tests, and command markdown references. No tests were run in this read-only research pass.
- skip_candidate: `no`

### ASK_GEMINI-HZ-070 `file` `templates/bitlesson.md`
- cursor: `[_]`
- core_role: Strict schema template for the project BitLesson knowledge base, consumed by RLCR setup and later validated by selection/delta gates.
- algorithmic_behavior: The template defines a reusable memory entry format under `## Entry Template (Strict)`, requiring a markdown heading plus fields in fixed order: `Lesson ID`, `Scope`, `Problem Description`, `Root Cause`, `Solution`, `Constraints`, `Validation Evidence`, and `Source Rounds` (`templates/bitlesson.md:5-19`). Runtime setup copies this file to `.humanize/bitlesson.md` when missing (`scripts/setup-rlcr-loop.sh:867-873`, `scripts/bitlesson-init.sh:83-86`).
- inputs_outputs_state: Template input is static markdown. Output is a newly created live `.humanize/bitlesson.md` file. The live file becomes loop state through `bitlesson_file: .humanize/bitlesson.md` and controls stop-hook validation via `bitlesson_required` and `bitlesson_allow_empty_none` (`scripts/setup-rlcr-loop.sh:887-908`).
- gates_or_invariants: The strict field order is the schema invariant (`templates/bitlesson.md:5-19`). The validator later treats placeholder IDs like `<BL-YYYYMMDD-short-name>` as non-concrete and counts only real `Lesson ID:` values (`scripts/bitlesson-validate-delta.sh:227-240`). Selector logic treats a file with no `## Lesson:` heading as an empty knowledge base and returns `NONE` (`scripts/bitlesson-select.sh:100-104`).
- dependencies_and_callers: Called by `scripts/bitlesson-init.sh` from `scripts/setup-rlcr-loop.sh`. Runtime prompts instruct agents to read the live BitLesson file and run `bitlesson-selector` before tasks (`scripts/setup-rlcr-loop.sh:1361-1375`). Stop-hook validation calls `scripts/bitlesson-validate-delta.sh` for non-finalize rounds (`hooks/loop-codex-stop-hook.sh:790-808`).
- edge_cases_or_failure_modes: Existing BitLesson files are preserved, so template updates do not migrate old project files automatically. A whitespace-only live file is an error for selection (`scripts/bitlesson-select.sh:94-98`). `Action: add` or `Action: update` in a summary requires concrete IDs with format `BL-YYYYMMDD-short-name`, notes, and matching entries in the live file (`scripts/bitlesson-validate-delta.sh:269-384`).
- validation_or_tests: `docs/bitlesson.md:27-35` documents the initialization and stop-gate loop. BitLesson behavior is covered by `tests/test-bitlesson-select-routing.sh`, `tests/test-bitlesson-validate-delta.sh`, and setup robustness checks for summary scaffolding in `tests/robustness/test-setup-scripts-robustness.sh:1089-1095`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-100 `file` `tests/test-pr-loop-1-scripts.sh`
- cursor: `[_]`
- core_role: Parallel split test runner for PR loop script-level contracts. It is a thin executable specification entry point that isolates argument validation and basic script behavior from the larger PR loop suite.
- algorithmic_behavior: The runner enables strict Bash mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared test helpers and PR-loop library, initializes a mocked PR-loop test environment, sources `tests/test-pr-loop-scripts.sh`, runs `run_script_tests`, prints a named summary, and exits with the summary status (`tests/test-pr-loop-1-scripts.sh:9-24`).
- inputs_outputs_state: Inputs are the repository test scripts and the mocked environment created by `init_pr_loop_test_env`. The helper creates a temporary directory, prepends a mock bin dir to `PATH`, and installs mock `gh` and `codex` commands (`tests/test-pr-loop-lib.sh:107-120`). Outputs are pass/fail/skip counters printed by `print_test_summary` (`tests/test-helpers.sh:58-77`) and the process exit code.
- gates_or_invariants: The delegated tests enforce `setup-pr-loop.sh` help output, required bot flags, unknown-option rejection, recognition of `--claude`/`--codex`, combined bot flags, `--max`, `--codex-model`, and `--codex-timeout` parsing (`tests/test-pr-loop-scripts.sh:18-166`). They also exercise cancellation, fetch argument validation, and polling argument validation (`tests/test-pr-loop-scripts.sh:172-410`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, and scripts under `scripts/`: `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, `poll-pr-reviews.sh` (`tests/test-pr-loop-scripts.sh:3-10`). It is a split runner sibling of other PR loop test runners (`tests/test-pr-loop-2-hooks.sh`, `tests/test-pr-loop-3-stophook.sh`) listed by repository file discovery.
- edge_cases_or_failure_modes: Because it sources the delegated test file, missing helper or library files fail before any tests. Mocking `gh` and `codex` ensures argument parsing can be tested without real GitHub/Codex, but script tests that depend on current working directory or temp files can still be environment sensitive. The runner itself has no retry or skip logic beyond the delegated tests.
- validation_or_tests: This file is itself a validation entry point. It should be run from the repository as `bash tests/test-pr-loop-1-scripts.sh` in a writable environment. Not run here because the task forbids file modification and the test harness creates temporary directories and state.
- skip_candidate: `no`

### ASK_GEMINI-HZ-130 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking PR-loop prompt template emitted when Claude bot trigger acknowledgement fails. It translates a remote bot liveness gate into actionable user-facing stop-hook output.
- algorithmic_behavior: The template renders retry count and total wait seconds into a message saying the Claude bot did not respond with an eyes reaction (`prompt-template/block/claude-eyes-timeout.md:1-4`). `hooks/pr-loop-stop-hook.sh` loads it when Claude is configured, a trigger is required, a trigger comment ID exists, and `check-bot-reactions.sh claude-eyes ... --retry 3 --delay 5` returns no reaction (`hooks/pr-loop-stop-hook.sh:760-790`).
- inputs_outputs_state: Inputs are `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, rendered by `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:785-786`). Output is a JSON block decision with `systemMessage` `"PR Loop: Claude bot not responding - check bot configuration"` (`hooks/pr-loop-stop-hook.sh:788-790`). The template itself lists required human actions: verify bot installation, permissions, and PR state (`prompt-template/block/claude-eyes-timeout.md:10-18`).
- gates_or_invariants: The gate runs only after trigger validation, preventing stale-trigger checks (`hooks/pr-loop-stop-hook.sh:749-758`). It is blocking: failure to observe eyes after retries exits with a block decision (`hooks/pr-loop-stop-hook.sh:779-790`). Template rendering uses single-pass substitution through `hooks/lib/template-loader.sh`, preserving unresolved placeholders instead of recursive expansion (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:56-135`).
- dependencies_and_callers: Depends on `hooks/pr-loop-stop-hook.sh`, `scripts/check-bot-reactions.sh`, and `hooks/lib/template-loader.sh`. Setup-time Case 4/5 also verifies Claude eyes after posting/reusing trigger comments in `scripts/setup-pr-loop.sh:564-600`, but that setup path emits inline errors rather than this template.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` uses an inline fallback (`hooks/pr-loop-stop-hook.sh:781-786`, `hooks/lib/template-loader.sh:185-211`). If a trigger exists but no comment ID is available, the stop hook logs a warning and does not render this block (`hooks/pr-loop-stop-hook.sh:794-797`). If Claude is configured but trigger is not required, eyes verification is skipped (`hooks/pr-loop-stop-hook.sh:798-800`).
- validation_or_tests: Template existence/reference is discoverable by `tests/test-template-references.sh`. PR stop-hook behavior is covered by PR-loop hook tests, with the direct caller visible at `hooks/pr-loop-stop-hook.sh:785`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-160 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Bash-validator block template enforcing that round summaries are edited through Write/Edit tooling, not shell redirection or stream editing.
- algorithmic_behavior: The template tells the agent not to use Bash commands to modify summary files and renders `{{CORRECT_PATH}}` as the expected target (`prompt-template/block/summary-bash-write.md:1-8`). `summary_bash_blocked_message` loads it with the correct current summary path (`hooks/lib/loop-common.sh:823-832`). `hooks/loop-bash-validator.sh` calls that helper when `command_modifies_file` detects modification of `round-N-summary.md` (`hooks/loop-bash-validator.sh:517-525`).
- inputs_outputs_state: Input is a Bash command from hook input, lowered and pattern-matched by validators, plus active loop state/current round used to build `CORRECT_PATH`. Output is a stderr block message and exit code 2 from `loop-bash-validator.sh` (`hooks/loop-bash-validator.sh:522-525`). The template does not modify state; it prevents bypassing other validation hooks.
- gates_or_invariants: The invariant is that summary files must go through Write/Edit so round-number validation and summary content gates can run. The template explicitly names blocked Bash tools such as `cat`, `echo`, `sed`, and `awk` (`prompt-template/block/summary-bash-write.md:7`). The validator applies across all rounds for `round-[0-9]+-summary.md` modification patterns (`hooks/loop-bash-validator.sh:517-525`).
- dependencies_and_callers: Depends on `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, and `hooks/lib/template-loader.sh`. It coordinates with write/edit/read validators that allow or block summary files by round and allowlist; related references appear in `tests/test-allowlist-validators.sh` and `tests/test-bash-validator-patterns.sh`.
- edge_cases_or_failure_modes: Missing template falls back to an inline minimal message (`hooks/lib/loop-common.sh:827-831`). False positives/negatives depend on `command_modifies_file` recognizing shell syntax; tests explicitly check modifications like `echo x > round-5-summary.md` and reads like `cat round-5-summary.md` (`tests/test-bash-validator-patterns.sh:192-194`).
- validation_or_tests: Template existence is checked as a common template in `tests/test-template-references.sh:149-167`. Comprehensive template tests render `CORRECT_PATH` values (`tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` references found by search). Not run here due read-only constraints.
- skip_candidate: `no`

### ASK_GEMINI-HZ-190 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop Codex prompt section that forces local Codex review to update the PR loop goal tracker after analyzing bot feedback.
- algorithmic_behavior: The template instructs Codex to update `{{GOAL_TRACKER_FILE}}` by adding an Issue Summary row for `{{NEXT_ROUND}}`, updating totals, and adding an Issue Log section (`prompt-template/pr-loop/codex-goal-tracker-update.md:1-21`). It constrains valid statuses to `"Issues Found"`, `"All Resolved"`, or `"Approved"` (`prompt-template/pr-loop/codex-goal-tracker-update.md:9-10`) and gives a concrete markdown example (`prompt-template/pr-loop/codex-goal-tracker-update.md:22-55`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `NEXT_ROUND`, supplied by `hooks/pr-loop-stop-hook.sh:1253-1262`. Output is appended instruction text inside the generated local Codex prompt file `round-${NEXT_ROUND}-codex-prompt.md` (`hooks/pr-loop-stop-hook.sh:1243-1264`). The resulting intended state mutation is restricted to Issue Summary, Total Statistics, and Issue Log sections of the goal tracker (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`).
- gates_or_invariants: Invariants: preserve file structure, maintain markdown table formatting, do not modify header sections such as PR Information and Ultimate Goal, add rows rather than replacing tables, and create separate reviewer rows (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`). Statistics invariant is `Remaining = Total Found - Total Resolved` (`prompt-template/pr-loop/codex-goal-tracker-update.md:12-16`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh`, during local Codex review of bot feedback after fetching/saving comments (`hooks/pr-loop-stop-hook.sh:1233-1264`). It depends on template rendering in `hooks/lib/template-loader.sh`. The larger PR-loop workflow is documented in `docs/usage.md:230-238` and initialized by `scripts/setup-pr-loop.sh`.
- edge_cases_or_failure_modes: Missing or empty template falls back to `"## Goal Tracker Update"` with a brief instruction (`hooks/pr-loop-stop-hook.sh:1259-1262`). If Codex follows the fallback only, section-specific invariants are weaker, so this template is behavior-defining rather than decorative. Inaccurate issue counts or overwriting header sections would corrupt loop progress accounting.
- validation_or_tests: `tests/test-templates-comprehensive.sh` covers related goal-tracker templates; direct reference is visible in `hooks/pr-loop-stop-hook.sh:1262`. PR-loop stop-hook tests exercise prompt generation paths. Not run in this read-only research pass.
- skip_candidate: `no`

### ASK_GEMINI-HZ-220 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Large executable robustness specification for `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`. It encodes edge cases for argument parsing, plan validation, git prerequisites, YAML safety, loop mutual exclusion, symlink protection, skip-implementation state transitions, timeouts, and dependency checks.
- algorithmic_behavior: The script uses strict Bash, sources shared test helpers and portable timeout, creates an isolated temp test directory, defines helpers to create minimal plans and git repos, and wraps setup scripts with `CLAUDE_PROJECT_DIR` for isolation (`tests/robustness/test-setup-scripts-robustness.sh:13-104`). It then runs 49 numbered tests and exits through `print_test_summary` (`tests/robustness/test-setup-scripts-robustness.sh:113-1338`).
- inputs_outputs_state: Inputs are test-created repositories, mock `codex`, mock `gh`, restricted `PATH` bins, generated plan files, fake `.humanize/rlcr` and `.humanize/pr-loop` state directories, and CLI arguments. Outputs are pass/fail/skip counters and temporary loop artifacts used for assertions, such as `.review-phase-started`, `state.md`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, and `round-0-prompt.md` in skip-impl mode (`tests/robustness/test-setup-scripts-robustness.sh:1050-1128`).
- gates_or_invariants: RLCR parsing rejects missing plan unless `--skip-impl`, invalid `--max`, invalid timeout, missing `--codex-model` value, unknown options, and duplicate positional plus `--plan-file` (`tests/robustness/test-setup-scripts-robustness.sh:122-231`). Plan gates reject comment-only content, fewer than 5 lines, spaces, shell metacharacters, absolute paths, symlink plan files, symlink parent directories, tracked plans without `--track-plan-file`, non-git dirs, repos without commits, dirty `.humanizeconfig`, YAML-unsafe branch names, and model injection characters (`tests/robustness/test-setup-scripts-robustness.sh:241-503`, `tests/robustness/test-setup-scripts-robustness.sh:645-699`). PR setup gates require bot flags, reject unknown options and nonnumeric max, and require a git repo (`tests/robustness/test-setup-scripts-robustness.sh:513-565`).
- dependencies_and_callers: Direct targets are `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`. The tested implementation includes argument parsing in `scripts/setup-rlcr-loop.sh:188-320`, dependency checks in `scripts/setup-rlcr-loop.sh:330-359`, plan/path validation in `scripts/setup-rlcr-loop.sh:415-683`, YAML safety in `scripts/setup-rlcr-loop.sh:699-723`, clean tree/base branch gates in `scripts/setup-rlcr-loop.sh:725-823`, and skip-impl artifact creation in `scripts/setup-rlcr-loop.sh:927-1292`. PR setup parsing and prerequisites are in `scripts/setup-pr-loop.sh:120-277`.
- edge_cases_or_failure_modes: The script intentionally records current behavior that `--codex-timeout 0` is accepted because validation allows non-negative digits despite messaging saying positive (`tests/robustness/test-setup-scripts-robustness.sh:789-805`). Very large timeout values are accepted (`tests/robustness/test-setup-scripts-robustness.sh:821-839`). Timeout behavior is simulated by mock `timeout`/`gtimeout` returning 124 on `git rev-parse` (`tests/robustness/test-setup-scripts-robustness.sh:841-896`). Some dependency-missing tests include skip fallbacks if tools are installed in system paths (`tests/robustness/test-setup-scripts-robustness.sh:1205-1304`).
- validation_or_tests: This file is the validation suite. It verifies skip-impl creates review marker with `build_finish_round=0`, sets `review_started: true`, avoids placeholder goal-tracker text, creates BitLesson Delta summary defaults, creates round contract, and references the contract in prompt (`tests/robustness/test-setup-scripts-robustness.sh:1031-1128`). With a plan, skip-impl preserves plan goal, prompt `@plan.md`, and contract alignment with original plan (`tests/robustness/test-setup-scripts-robustness.sh:1131-1195`). Not executed here because it creates temp repos, commits, loop state, mocks, and cleanup artifacts; this branch export is read-only and the instruction is research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_GEMINI-HZ-010, ASK_GEMINI-HZ-040, ASK_GEMINI-HZ-070, ASK_GEMINI-HZ-100, ASK_GEMINI-HZ-130, ASK_GEMINI-HZ-160, ASK_GEMINI-HZ-190, ASK_GEMINI-HZ-220
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`