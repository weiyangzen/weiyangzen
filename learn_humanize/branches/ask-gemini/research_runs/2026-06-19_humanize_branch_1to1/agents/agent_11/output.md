# agent_11 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`
- inspection_scope: read-only filesystem inspection under `/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini`; no files modified and no write-producing tests executed.
- git_note: the branch export did not expose a usable `.git` checkout in this sandbox, so source commit identity is taken from run metadata.

## Item Evidence

### ASK_GEMINI-HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable-spec surface for the Humanize RLCR and PR-loop state machines. The directory validates hooks, prompt/template rendering, state parsing, monitor behavior, setup/cancel flows, PR review polling, model/provider routing, and security/robustness guards.
- algorithmic_behavior: `tests/run-all-tests.sh` enumerates the suite and runs isolated scripts in parallel with a bounded job count (`HUMANIZE_TEST_JOBS`, capped by default) while collecting pass/fail counts and logs; see [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/run-all-tests.sh:59). The directory splits PR-loop coverage into script, hook, and stop-hook partitions (`test-pr-loop-1-scripts.sh`, `test-pr-loop-2-hooks.sh`, `test-pr-loop-3-stophook.sh`) while `test-pr-loop.sh` runs the combined path; see [tests/test-pr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-pr-loop.sh:32).
- inputs_outputs_state: Inputs are temp repos, `.humanize/rlcr` and `.humanize/pr-loop` fixture state files, mocked `gh`/`codex` executables, JSON PR fixtures, environment overrides, and hook JSON payloads. Outputs are shell exit codes, pass/fail counters from `test-helpers.sh`, generated temp-loop artifacts, and captured command output. Shared helper state is `TEST_DIR` plus `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED`; see [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-helpers.sh:22).
- gates_or_invariants: The suite asserts fail-closed hook behavior for malformed JSON, wrong paths, wrong rounds, protected state files, plan backup writes, `.humanize` git-add attempts, unauthorized cancel moves, PR force-push detection, incomplete tasks, and Codex review failures. It also enforces portability invariants around Bash/zsh, BSD/GNU sed behavior, CRLF, JSON fixtures, template fallback behavior, and bounded parallelism.
- dependencies_and_callers: Coordinates with sibling paths `hooks/`, `scripts/`, `prompt-template/`, and `config/`. Fixture-backed GitHub behavior is supplied by `tests/setup-fixture-mock-gh.sh` and `tests/mocks/gh`; see [tests/setup-fixture-mock-gh.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/setup-fixture-mock-gh.sh:3) and [tests/mocks/gh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/mocks/gh:104). `tests/fixtures/*.json` covers issue comments, review comments, PR reviews, reactions, and approval-only cases.
- edge_cases_or_failure_modes: Covered recursively by robustness suites for base branch detection, cancel security, concurrent state access, git parsing, goal-tracker parsing, hook inputs, hook system behavior, path validation, plan-file validation, PR-loop API fetch/poll failures, session filtering, setup scripts, state transitions, template errors/stress, timeouts, and state-file parsing. Slow/interactive-sensitive paths are split or skipped in CI where appropriate; PR stop-hook split skips under `GITHUB_ACTIONS`.
- validation_or_tests: The directory is the validation layer. Top-level tests cover template loading/references, todo checking, plan-file hooks/validation, finalize phase, code-review issue detection, monitor runtime/e2e behavior, PR-loop scripts/hooks/stop hooks/system behavior, config merge/error handling, Codex hook installation, provider routing, session IDs, agent teams, and skill monitor behavior. Robustness tests add adversarial and edge-case coverage.
- skip_candidate: `no`

### ASK_GEMINI-HZ-041 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook preflight gate that blocks RLCR exit when Claude still has incomplete blocking/mainline work. It bridges legacy transcript `TodoWrite` state and the newer file-backed Claude Task system.
- algorithmic_behavior: `classify_lane()` reads a leading lane tag and defaults to blocking for safety; see [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/check-todos-from-transcript.py:24). `extract_tool_calls_from_entry()` supports assistant-message content arrays, generic message entries, and direct tool-use entries. `find_incomplete_todos_from_transcript()` tracks only the latest `TodoWrite` payload and emits incomplete items. `find_incomplete_tasks_from_directory()` reads authoritative JSON task files from `~/.claude/tasks/<session_id>/` or a test override.
- inputs_outputs_state: Input is hook JSON on stdin with optional `session_id`, `tasks_base_dir`, and `transcript_path`; task inputs are `*.json` files containing `status`, `subject`, and `description`; transcript input is JSONL. Output is exit `0` when clear or no task source exists, exit `1` with `INCOMPLETE_TODOS` plus formatted items when blockers exist, and exit `2` on invalid hook-input JSON; see [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/check-todos-from-transcript.py:173).
- gates_or_invariants: Only `completed` and `deleted` file-backed statuses are non-blocking. Legacy todos block unless status is `completed`. A leading `[queued]` lane is intentionally ignored as non-blocking; inline lane words do not downgrade the task. Missing stdin, missing transcript, missing session directory, malformed transcript lines, and malformed/unreadable task files are tolerated.
- dependencies_and_callers: Uses only Python stdlib (`json`, `re`, `sys`, `pathlib`). Called by `hooks/loop-codex-stop-hook.sh` before Codex review, where exit `1` becomes a hook block and exit `2` becomes a parse-error block; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/loop-codex-stop-hook.sh:394).
- edge_cases_or_failure_modes: Prevents “ghost task” transcript issues by reading the Task directory first. Ignores invalid JSONL transcript lines rather than failing the hook. If both Task files and legacy todos exist, incomplete items are aggregated. Missing task `status` defaults to `pending`; missing subject falls back to description or task id. Queued prefix matching is anchored to the start, preventing bypass by mentioning queued text mid-task.
- validation_or_tests: Covered directly by `tests/test-todo-checker.sh`, including invalid/empty input, nonexistent transcript, all-complete vs incomplete todos, multiple TodoWrite calls using latest state, direct and alternate transcript formats, missing status/content, queued lane bypass prevention, deleted tasks, mixed Task/TodoWrite sources, and Task ID output; see [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-todo-checker.sh:47).
- skip_candidate: `no`

### ASK_GEMINI-HZ-071 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor graceful-stop behavior when `.humanize/` disappears during an active monitor session.
- algorithmic_behavior: Provides three subcommands: `setup` creates a synthetic `.humanize/rlcr/2026-01-16_99-99-99` session with `state.md` and `goal-tracker.md`; `delete` removes `.humanize` to trigger monitor shutdown; `cleanup` removes leftovers; see [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/manual-monitor-test.sh:23).
- inputs_outputs_state: Input is one CLI argument: `setup`, `delete`, or `cleanup`. Output is terminal guidance and filesystem state under `.humanize/rlcr`. The synthetic state sets `current_round`, `max_iterations`, Codex config, `started_at`, and `plan_file`, while the tracker supplies minimal immutable goal/AC content.
- gates_or_invariants: Expected monitor invariant is a clean user-facing exit when `.humanize/rlcr` no longer exists, terminal restoration, and no shell glob errors. Usage without a valid subcommand exits `1`.
- dependencies_and_callers: Depends on `scripts/humanize.sh` monitor behavior, specifically the user running `source scripts/humanize.sh && humanize monitor rlcr` in another terminal; see [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/manual-monitor-test.sh:10). It coordinates with monitor runtime/e2e automated tests in `tests/test-monitor-runtime.sh` and `tests/test-monitor-e2e-real.sh`.
- edge_cases_or_failure_modes: The script intentionally deletes `.humanize`, so it is manual/destructive relative to loop-local state and should be used only in a disposable or expected test workspace. It does not self-assert pass/fail; success is observed in the monitor terminal.
- validation_or_tests: Acts as manual validation for monitor deletion behavior. Automated analogs exist in the monitor runtime and e2e test scripts.
- skip_candidate: `no`

### ASK_GEMINI-HZ-101 `file` `tests/test-pr-loop-2-hooks.sh`
- cursor: `[_]`
- core_role: Parallel split runner for PR-loop hook functionality tests. It is a dispatch wrapper, not the hook test implementation itself.
- algorithmic_behavior: Resolves `SCRIPT_DIR`/`PROJECT_ROOT`, sources shared test helpers and PR-loop library, initializes a mocked PR-loop test environment, sources `tests/test-pr-loop-hooks.sh`, invokes `run_hook_tests`, then prints a scoped summary; see [tests/test-pr-loop-2-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-pr-loop-2-hooks.sh:14).
- inputs_outputs_state: Inputs are the shell environment and repository test files. `init_pr_loop_test_env` creates a temp directory, mock `gh`, mock `codex`, and prepends mock bin to `PATH`; see [tests/test-pr-loop-lib.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-pr-loop-lib.sh:107). Output is a process exit code from `print_test_summary` plus pass/fail console output.
- gates_or_invariants: `set -euo pipefail` enforces runner failure on unhandled errors. Hook tests assert PR-loop validator protections, sorting/gatekeeper logic, trigger detection, approval-only review handling, fixture-backed fetch/poll behavior, wrong-round validation, and PR monitor bot display; see [tests/test-pr-loop-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-pr-loop-hooks.sh:15).
- dependencies_and_callers: Called by `tests/run-all-tests.sh` as the second PR-loop split. It depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-hooks.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/humanize.sh`.
- edge_cases_or_failure_modes: Because this wrapper exits with `$?` after `print_test_summary`, failures in sourced hook tests propagate cleanly. It relies on the sourced module defining `run_hook_tests`; a missing or broken source file fails early under `set -e`.
- validation_or_tests: The wrapper is itself a validation entry point for the hook subset; line 21 is the actual test invocation.
- skip_candidate: `no`

### ASK_GEMINI-HZ-131 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR review-phase failure when the nested Codex review command fails to produce the expected result artifact.
- algorithmic_behavior: Renders a failure message with placeholders for Codex exit code, missing review result file, debug command/stdout/stderr file paths, and the last stderr content; see [prompt-template/block/codex-review-failed.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/prompt-template/block/codex-review-failed.md:1). It tells the user the system will retry review on the next exit attempt.
- inputs_outputs_state: Inputs are `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT`. Output is a rendered block used as hook block reason. State remains in review/finalize gating rather than advancing to completion.
- gates_or_invariants: The corresponding stop-hook path treats review failure as a hard block, not a skippable review. `block_review_failure()` tails the review log and renders this template through `load_and_render_safe`; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/loop-codex-stop-hook.sh:1482).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` through `hooks/lib/template-loader.sh`, whose renderer uses single-pass `{{VAR}}` substitution and fallback behavior for missing templates; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/lib/template-loader.sh:188).
- edge_cases_or_failure_modes: If the template is missing or renders empty, the hook has a fallback. If variables are missing, unresolved placeholders remain by renderer design. The template is intentionally short; deeper failure diagnosis depends on debug files and tailed stderr.
- validation_or_tests: Template existence and loadability are checked by template reference/comprehensive tests. Review-failure behavior is exercised by finalize/review tests that mock Codex failures and empty output.
- skip_candidate: `no`

### ASK_GEMINI-HZ-161 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Shared validator block text that enforces the transition away from filesystem `round-*-todos.md` files toward native task tools.
- algorithmic_behavior: Emits an instruction block saying not to create or access `round-*-todos.md` and to use `TaskCreate`, `TaskUpdate`, and `TaskList`; see [prompt-template/block/todos-file-access.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/prompt-template/block/todos-file-access.md:1). The shared `todos_blocked_message()` function renders it for read/write/edit/bash validators; see [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/lib/loop-common.sh:770).
- inputs_outputs_state: Input is validator context: attempted tool/action and file path matching a todos round-file pattern. Output is stderr block text and validator exit `2` when blocked. It does not mutate loop state; it prevents stale file-based task state from becoming authoritative.
- gates_or_invariants: The invariant is that task progress belongs to native Task state, not ad hoc todo files. There is a narrow allowlist in `is_allowlisted_file()` for legacy/controlled loop files in the active loop directory; otherwise read/write/edit attempts are blocked; see [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/lib/loop-common.sh:747).
- dependencies_and_callers: Used by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, and indirectly bash-validator protections. Depends on template loader fallback behavior.
- edge_cases_or_failure_modes: Wrong directory, old loop directory, inactive loop, and generic relative todo filenames are blocked. Same basename under a different root is blocked. Allowlist checks are path-exact, so path normalization issues are covered by validator tests.
- validation_or_tests: `tests/test-allowlist-validators.sh` exercises read/write/edit/bash allow/block behavior for todo files and old/wrong directories. `tests/test-template-references.sh` explicitly checks that this common template exists; see [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/test-template-references.sh:152).
- skip_candidate: `no`

### ASK_GEMINI-HZ-191 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop startup contract block for the case where existing PR comments must be addressed before re-review.
- algorithmic_behavior: Renders critical completion requirements: commit and push, post a bot-triggering PR comment, and write a resolution summary to the current `round-0-pr-resolve.md` path; see [prompt-template/pr-loop/critical-requirements-has-comments.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/prompt-template/pr-loop/critical-requirements-has-comments.md:2).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`. Output is appended prompt text from setup-pr-loop. Expected state after user work is changed code on remote, a PR trigger comment, and a resolution file for the stop hook to inspect.
- gates_or_invariants: When setup detects existing comments, it selects this stricter contract instead of the no-comments variant. The invariant is that comment-addressing work must produce externally visible re-review evidence and a local resolution summary before stop-hook polling; see [scripts/setup-pr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/scripts/setup-pr-loop.sh:890).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` via `load_and_render_safe` after `COMMENT_COUNT` is derived from the fetched comment file sentinel; see [scripts/setup-pr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/scripts/setup-pr-loop.sh:721). Downstream behavior depends on `hooks/pr-loop-stop-hook.sh` polling bot reviews and checking trigger comments.
- edge_cases_or_failure_modes: If comments are misdetected, setup may choose the wrong contract. If placeholders are missing, the rendered prompt can contain unresolved variables. If the user skips commit/push or trigger comment, PR stop-hook paths block for unpushed commits or missing trigger.
- validation_or_tests: Covered by PR-loop setup/script tests and template load/render tests. Template syntax/loadability is covered by comprehensive template validation.
- skip_candidate: `no`

### ASK_GEMINI-HZ-221 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for RLCR state-file parsing, defaults, strict rejection, and newer state fields used by the loop state machine.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates temp state files, and exercises `parse_state_file`, `get_current_round`, and `parse_state_file_strict`; see [tests/robustness/test-state-file-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/robustness/test-state-file-robustness.sh:16). Positive cases parse full state, extra fields, quoted values, zero round, large round numbers, special characters, trailing whitespace, binary suffixes, `full_review_round`, and drift tracking fields. Negative/tolerant cases cover missing frontmatter, non-numeric rounds, missing required fields, empty files, partial YAML, malformed YAML, nonexistent files, negative values, and CRLF.
- inputs_outputs_state: Inputs are generated `state*.md` files under `TEST_DIR`. Output is pass/fail counters and process exit from `print_test_summary`. State variables under test include `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, `STATE_FULL_REVIEW_ROUND`, `STATE_MAINLINE_STALL_COUNT`, `STATE_LAST_MAINLINE_VERDICT`, and `STATE_DRIFT_STATUS`.
- gates_or_invariants: Tolerant parsing defaults `current_round` to `0`, `max_iterations` to `10`, `full_review_round` to `5`, and drift fields to safe defaults; see [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/lib/loop-common.sh:412). Strict parsing rejects missing frontmatter, missing required fields, non-numeric round/max values, and invalid `review_started`; see [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/hooks/lib/loop-common.sh:465).
- dependencies_and_callers: Depends on `loop-common.sh` parser helpers and test helper counters. The production parser is called by validators and stop hooks before allowing protected operations or loop transitions.
- edge_cases_or_failure_modes: The test confirms graceful behavior for partial writes and malformed content in tolerant call sites while expecting strict call sites to fail closed. It also documents compatibility for legacy states missing optional fields. The file’s header mentions partial writes; actual coverage is malformed/partial YAML rather than concurrent write races, which are handled by the separate concurrent-state robustness suite.
- validation_or_tests: This file is a direct validation suite. It is also included in `tests/run-all-tests.sh` robustness dispatch at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-gemini/tests/run-all-tests.sh:105).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 unique Item Evidence headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`