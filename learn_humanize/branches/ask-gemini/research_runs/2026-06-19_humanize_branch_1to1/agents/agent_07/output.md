# agent_07 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-007 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Repository-local prompt and block-message corpus for the Humanize RLCR, planning, Codex review, BitLesson, and PR-review loop algorithms. It is not executable by itself, but it defines the contracts that runtime hooks render into prompts, stop-hook block reasons, review instructions, and phase-transition instructions.
- algorithmic_behavior: The directory is recursively organized into five required subdirectories validated by `hooks/lib/template-loader.sh:224-235`: `block`, `claude`, `codex`, `plan`, and `pr-loop`. `block/` holds guard failure messages for protected state files, wrong round paths, git cleanliness, large-file gates, BitLesson validation, mainline drift, PR trigger waits, and force-push resets. `claude/` drives RLCR phase behavior: next round re-anchor, review phase, finalize phase, drift recovery, methodology analysis, and agent-team delegation. `codex/` drives local Codex review behavior, including regular review, full alignment audit, goal-tracker update instructions, and code-review phase prompts. `plan/` defines plan-generation and refine-plan QA output schemas. `pr-loop/` defines PR-loop round-zero prompts, critical completion requirements, initial goal tracker content, and Codex goal-tracker reconciliation instructions.
- inputs_outputs_state: Inputs are uppercase `{{VAR}}` placeholders rendered by `hooks/lib/template-loader.sh:56-135` with single-pass substitution; missing variables remain literal placeholders. Outputs are markdown prompts or block reasons consumed by hooks and scripts. State changes occur through callers: stop hooks write review prompts/results and choose continue/finalize/stop paths; validators output JSON block decisions; PR setup writes generated prompt and tracker files; plan scripts emit plan/QA documents.
- gates_or_invariants: Template loading is centralized through `load_template`, `render_template`, and `load_and_render_safe` in `hooks/lib/template-loader.sh:36-211`. Required directory shape is hard-gated by `validate_template_dir` at `hooks/lib/template-loader.sh:213-238`. Rendering is single-pass to prevent placeholder injection from values such as review content (`hooks/lib/template-loader.sh:52-55`). Many block templates encode fail-closed invariants: no state-file edits, no stale round files, no direct hook execution, no unpushed commits when push-every-round is enabled, no missing mainline verdict, no invalid BitLesson delta, and no PR review continuation without a trigger comment when required.
- dependencies_and_callers: `hooks/lib/loop-common.sh` resolves `TEMPLATE_DIR` and validates it (`hooks/lib/loop-common.sh:230-233`). `hooks/loop-codex-stop-hook.sh` renders regular/full alignment review prompts (`hooks/loop-codex-stop-hook.sh:991-1020`), finalize prompts (`hooks/loop-codex-stop-hook.sh:1254-1282`), drift/next-round prompts (`hooks/loop-codex-stop-hook.sh:1952-2065`), and many block reasons. `hooks/pr-loop-stop-hook.sh` renders PR-loop blocks such as unpushed commits, force push, no trigger comment, bot timeout, and Codex goal-tracker update instructions (`hooks/pr-loop-stop-hook.sh:356`, `415`, `740`, `785`, `1262`). `scripts/setup-pr-loop.sh` renders initial PR tracker and round-zero prompts (`scripts/setup-pr-loop.sh:712`, `758`, `800`, `847`, `914`, `940`). `scripts/bitlesson-validate-delta.sh` renders BitLesson block templates (`scripts/bitlesson-validate-delta.sh:196-217`, `250-306`, `349-383`). `scripts/install-skill.sh` bundles `prompt-template` with scripts/hooks/config (`scripts/install-skill.sh:162`).
- edge_cases_or_failure_modes: Missing template files fall back to caller-supplied fallback text through `load_and_render_safe` (`hooks/lib/template-loader.sh:188-211`). Missing subdirectories invalidate the template directory (`hooks/lib/template-loader.sh:224-235`). Placeholder values containing other placeholders are intentionally not expanded recursively. Block templates can become behaviorally stale if caller gates evolve but the markdown instruction does not; tests below mitigate missing references but do not prove semantic agreement for every prompt.
- validation_or_tests: `tests/test-template-references.sh` scans `load_template`, `load_and_render`, and `load_and_render_safe` references to ensure referenced templates exist (`tests/test-template-references.sh:83-100`) and also detects unused templates (`tests/test-template-references.sh:124-126`). `tests/test-templates-comprehensive.sh` verifies the template directory exists, required subdirs are present, all markdown templates load, and rendering handles special characters and stress cases (`tests/test-templates-comprehensive.sh:59-119`, `268-602`). `tests/test-template-loader.sh` covers core loader behavior including missing templates, unused variables, unreplaced variables, and special characters (`tests/test-template-loader.sh:45-229`, `261-633`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-037 `file` `config/codex-hooks.json`
- cursor: `[_]`
- core_role: Native Codex hook schema that registers Humanize Stop hooks for both RLCR and PR-loop workflows.
- algorithmic_behavior: Defines a `hooks.Stop` group with two command hooks: `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` and `{{HUMANIZE_RUNTIME_ROOT}}/hooks/pr-loop-stop-hook.sh`, each with `timeout: 7200` and a status message (`config/codex-hooks.json:3-18`). On Codex Stop events, this config routes control to the Humanize RLCR stop hook and PR stop hook so loop validation, review prompting, polling, and state transitions can run automatically.
- inputs_outputs_state: Input is the installed runtime root placeholder `{{HUMANIZE_RUNTIME_ROOT}}`. `scripts/install-codex-hooks.sh` reads this JSON as `HOOKS_TEMPLATE` (`scripts/install-codex-hooks.sh:14`), JSON-escapes the runtime root, replaces the placeholder, parses the template, and writes/merges the resulting Codex `hooks.json` (`scripts/install-codex-hooks.sh:94-163`). Output is an installed Codex hooks file under `CODEX_HOME` or a configured Codex config dir (`scripts/install-codex-hooks.sh:10-11`, `73`).
- gates_or_invariants: Install requires a Codex CLI exposing the `codex_hooks` feature (`scripts/install-codex-hooks.sh:75-83`) and can enable it (`scripts/install-codex-hooks.sh:166-175`). Existing hooks config must be a JSON object, `hooks` must be an object, and existing `Stop` must be a list (`scripts/install-codex-hooks.sh:111-128`). Prior managed Humanize hook commands are filtered before appending the current managed group, preventing duplicate managed hooks (`scripts/install-codex-hooks.sh:129-156`).
- dependencies_and_callers: Consumed by `scripts/install-codex-hooks.sh`; validated by hook-install tests that create old managed entries and expect both runtime-root commands to be installed (`tests/test-codex-hook-install.sh:91-96`, `192-193`). Runtime commands depend on `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: If `{{HUMANIZE_RUNTIME_ROOT}}` contains quotes or backslashes, installer uses JSON escaping before parsing (`scripts/install-codex-hooks.sh:104-109`). If existing `hooks.json` has invalid object shape, merge fails rather than corrupting config. If Codex lacks native hook support, installation fails. If either hook script is missing from the installed runtime, Codex Stop routing will invoke a nonexistent command even though the JSON schema itself is valid.
- validation_or_tests: `tests/test-codex-hook-install.sh` covers feature enabling, old hook replacement, runtime-root command insertion, and config merge behavior. `tests/test-unified-codex-config.sh` also references both stop-hook paths and PR-stop-hook defaults (`tests/test-unified-codex-config.sh:478`, `683-705`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-067 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Read-only Claude Code status-line renderer with an RLCR status field. It implements session-aware loop-state discovery and display routing for active Humanize sessions.
- algorithmic_behavior: Reads one JSON object from stdin (`scripts/statusline.sh:19`), extracts fields with `jq` (`scripts/statusline.sh:21-24`), formats duration and context usage, resolves session display names from transcript or `sessions-index.json`, checks fast mode in Claude settings, discovers git branch, and prints ANSI-colored fields packed to 75% of terminal width (`scripts/statusline.sh:169-403`). RLCR-specific behavior is in `_resolve_rlcr_display` and `get_rlcr_status`: methodology-analysis state displays `Analyzing`, finalize state `Finalizing`, active `state.md` as `Active`, terminal `*-state.md` as a capitalized terminal reason, otherwise `Off` (`scripts/statusline.sh:36-63`).
- inputs_outputs_state: Inputs include stdin JSON fields `.model.display_name`, `.cwd`, `.session_id`, `.transcript_path`, `.cost.*`, and `.context_window.used_percentage` (`scripts/statusline.sh:169-245`, `326-330`); filesystem state under `$CWD/.humanize/rlcr`; `$HOME/.claude` transcript/session index files; `$HOME/.claude/settings.json`; and `git -C "$CWD" branch --show-current`. Output is one or more terminal lines containing model, context bar, cost/duration, cwd/branch, line counts, session name, fast-mode, and RLCR state (`scripts/statusline.sh:351-403`). It does not mutate repository or loop state.
- gates_or_invariants: `get_rlcr_status` returns `Off` if the RLCR directory does not exist (`scripts/statusline.sh:71-74`). It pre-scans for `session_id` in state files; when no session id filter is provided and no session-aware files exist, it only checks the newest directory as zombie-loop protection (`scripts/statusline.sh:76-93`). When session-aware files exist, it ignores session-unaware entries (`scripts/statusline.sh:94-118`, `142-145`). With a session filter, it returns only matching session state (`scripts/statusline.sh:121-154`). RLCR colors are mapped by terminal status (`scripts/statusline.sh:157-167`).
- dependencies_and_callers: Depends on `jq`, `grep`, `awk`, `sed`, `cut`, `tr`, `git`, `stty`, and `tput`. No external caller or test references were found beyond the script itself via `rg` for `statusline`, so it appears intended for direct Claude status-line configuration rather than being invoked by Humanize hooks.
- edge_cases_or_failure_modes: If stdin is missing fields, defaults are used for most display values. If `/dev/tty` is unavailable, terminal width falls back through `tput` then `80` (`scripts/statusline.sh:379-382`). If transcript/session index is absent, the full session id is displayed (`scripts/statusline.sh:184-218`). If git branch lookup fails, branch is `?` (`scripts/statusline.sh:237-241`). Context percentage is rounded and not clamped, so abnormal input can distort the context bar math. `ls "$session_dir"/*-state.md | head -1` chooses the first terminal file lexically, which is acceptable for display but not authoritative loop control (`scripts/statusline.sh:47-58`, `127-135`).
- validation_or_tests: No direct statusline test was found. Adjacent monitor/runtime behavior is tested elsewhere, but this script’s RLCR display resolver and field-packing behavior appear untested directly.
- skip_candidate: `no`

### ASK_GEMINI-HZ-097 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for Humanize monitor runtime cleanup, graceful stop, terminal restoration, and signal handling contracts.
- algorithmic_behavior: Creates a temp test project under `/tmp/test-monitor-runtime-$$`, then generates small helper scripts to simulate or inspect monitor behavior (`tests/test-monitor-runtime.sh:45-53`, `61-109`). It verifies `_graceful_stop` calls cleanup, emits a user-friendly message, and reports loop-directory deletion (`tests/test-monitor-runtime.sh:56-134`). It verifies cleanup idempotence (`tests/test-monitor-runtime.sh:136-177`), simulated main-loop deletion detection (`tests/test-monitor-runtime.sh:179-251`), source-level terminal restore wiring (`tests/test-monitor-runtime.sh:253-311`), source-level `_graceful_stop -> _cleanup` wiring (`tests/test-monitor-runtime.sh:313-324`), bash signal cleanup (`tests/test-monitor-runtime.sh:326-381`), trap declarations (`tests/test-monitor-runtime.sh:383-423`), and optional zsh `TRAPINT` handling (`tests/test-monitor-runtime.sh:425-490`).
- inputs_outputs_state: Inputs are `scripts/humanize.sh`, a synthetic `.humanize/rlcr` directory, optional `zsh`, and shell signals. Outputs are PASS/FAIL lines, a summary, and exit `0` only when `TESTS_FAILED` is zero (`tests/test-monitor-runtime.sh:492-513`). Test-local state includes `cleanup_done`, `monitor_running`, and marker echo strings. It writes only its temp directory when run.
- gates_or_invariants: Core invariants are: cleanup must run once even under repeated calls (`tests/test-monitor-runtime.sh:143-177`); `_cleanup` must restore terminal state and reset traps; loop deletion must transition monitor to graceful stop; bash traps must include `INT TERM`; zsh trap functions must exist; and cleanup must reset traps to avoid recursive cleanup. Production source lines backing these contracts are `_restore_terminal` (`scripts/humanize.sh:707-712`), `_cleanup` (`scripts/humanize.sh:738-762`), `_graceful_stop` (`scripts/humanize.sh:764-777`), traps (`scripts/humanize.sh:784-795`), and loop deletion detection (`scripts/humanize.sh:838-844`).
- dependencies_and_callers: Depends on bash, grep, chmod, rm, kill, optional zsh, and `scripts/humanize.sh`. It is likely run by the repository test suite (`tests/run-all-tests.sh`) along with monitor tests, though this assigned file is also executable standalone.
- edge_cases_or_failure_modes: Several tests reconstruct simplified versions of functions rather than invoking the full monitor loop, so they prove the intended contracts but not every terminal behavior in a real TTY. The terminal reset check is source-grep based because `tput` is not reliable in non-interactive test mode (`tests/test-monitor-runtime.sh:260-284`). zsh signal handling is skipped when zsh is unavailable (`tests/test-monitor-runtime.sh:432-490`). The grep pipeline at `tests/test-monitor-runtime.sh:282` is ineffective because `grep -q` emits no output, but the real `_cleanup calls _restore_terminal` assertion is repeated correctly at `tests/test-monitor-runtime.sh:305-311`.
- validation_or_tests: This file is itself the validation asset. It cross-validates against `scripts/humanize.sh` implementation lines noted above and exits nonzero on any failed assertion.
- skip_candidate: `no`

### ASK_GEMINI-HZ-127 `file` `prompt-template/block/bitlesson-delta-invalid.md`
- cursor: `[_]`
- core_role: Stop-hook block template for invalid `## BitLesson Delta` action declarations in RLCR summaries.
- algorithmic_behavior: The template tells the agent that the `## BitLesson Delta` section exists but must contain exactly one action, limited to `none`, `add`, or `update` (`prompt-template/block/bitlesson-delta-invalid.md:1-7`). The production validator extracts the BitLesson Delta block while ignoring fenced code and HTML comments, then parses `Action:` lines case-insensitively (`scripts/bitlesson-validate-delta.sh:92-204`). If action count is not exactly one, or the action is outside the allowed set, it renders this template and returns a JSON block decision (`scripts/bitlesson-validate-delta.sh:206-218`).
- inputs_outputs_state: Inputs are summary file content, BitLesson file path, relative BitLesson path, `allow-empty-none`, template dir, and current round (`scripts/bitlesson-validate-delta.sh:4-14`, `17-67`). This specific template has no placeholders. Output is embedded as the `reason` in `{"decision":"block", ...}` via `block_exit` (`scripts/bitlesson-validate-delta.sh:78-90`). It does not mutate state; it prevents exit from advancing until the summary is corrected.
- gates_or_invariants: Enforces one and only one action declaration from the allowed vocabulary. The broader validator also requires a delta section, consistency between action and lesson IDs, non-placeholder notes for add/update, valid `BL-YYYYMMDD-short-name` IDs, and presence of referenced IDs in `.humanize/bitlesson.md` (`scripts/bitlesson-validate-delta.sh:181-385`).
- dependencies_and_callers: Called only through `scripts/bitlesson-validate-delta.sh`, which sources `hooks/lib/template-loader.sh` (`scripts/bitlesson-validate-delta.sh:74-77`). The RLCR stop hook invokes the validator with `--template-dir "$TEMPLATE_DIR"` around BitLesson summary validation (`hooks/loop-codex-stop-hook.sh:800`).
- edge_cases_or_failure_modes: Multiple valid `Action:` lines are invalid because count must equal one. Unknown action words are invalid after lowercase normalization. Missing action with a present section is invalid. If this template is missing, the validator uses a fallback message with the same allowed action set (`scripts/bitlesson-validate-delta.sh:207-216`). The extractor deliberately avoids matching headings inside fenced code or HTML comment blocks (`scripts/bitlesson-validate-delta.sh:95-179`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` is the direct test surface for this validator/template path, and template existence/reference coverage is also included in `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-157 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block template for RLCR state files missing required schema fields after a Humanize version upgrade.
- algorithmic_behavior: Renders the missing field name into a user-facing message, explains the state file was started with an older Humanize version, and directs the user to cancel, update to version `1.1.2+`, and restart (`prompt-template/block/schema-outdated.md:1-12`). The caller emits this as a JSON block reason when required state fields are empty.
- inputs_outputs_state: Input placeholder is `{{FIELD_NAME}}` (`prompt-template/block/schema-outdated.md:3`). `hooks/loop-plan-file-validator.sh` calls `load_and_render_safe` with `FIELD_NAME=$field_name`, JSON-escapes the rendered markdown, and outputs `{"decision":"block","reason":...}` (`hooks/loop-plan-file-validator.sh:59-77`). It does not repair state; it blocks the prompt-submit path so the active session does not continue with ambiguous schema.
- gates_or_invariants: The plan-file validator first locates the active loop by session id, resolves the active state file, and parses it strictly (`hooks/loop-plan-file-validator.sh:29-48`). It requires `plan_tracked` and `start_branch` values from the parsed state (`hooks/loop-plan-file-validator.sh:50-53`, `79-89`). After schema validation, the same validator checks branch consistency and plan-file tracking cleanliness (`hooks/loop-plan-file-validator.sh:91-130` and following).
- dependencies_and_callers: Used by `hooks/loop-plan-file-validator.sh`; template loading comes from `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. The field constants referenced by the validator come from loop-common (`hooks/loop-plan-file-validator.sh:79-80`).
- edge_cases_or_failure_modes: If strict state parsing fails, the validator exits nonzero before rendering this template (`hooks/loop-plan-file-validator.sh:44-48`). If the template is missing, the caller fallback still blocks with an older-version message (`hooks/loop-plan-file-validator.sh:61-66`). Template text says the session will be terminated as unexpected, while this caller’s immediate behavior is a block response; any terminal state transition would need to happen in adjacent loop handling, not in this template.
- validation_or_tests: Covered indirectly by hook-system and plan-file validator tests that exercise state parsing/schema behavior, plus template reference/comprehensive tests for existence and rendering.
- skip_candidate: `no`

### ASK_GEMINI-HZ-187 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular per-round Codex implementation review prompt for the RLCR loop. It defines the review contract that decides whether Claude continues, fixes issues, or may complete.
- algorithmic_behavior: Instructs Codex to read the original implementation plan first (`prompt-template/codex/regular-review.md:3-11`), compare Claude’s summary against plan and prompt, perform critical implementation review, reject deferrals as incomplete, and draft concrete implementation plans for unfinished work (`prompt-template/codex/regular-review.md:20-32`). It mandates a goal-alignment check against `goal-tracker.md` (`prompt-template/codex/regular-review.md:33-45`), classifies findings into Mainline Gaps, Blocking Side Issues, and Queued Side Issues (`prompt-template/codex/regular-review.md:47-52`), and requires a `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` line (`prompt-template/codex/regular-review.md:54-59`). It only permits final-line `COMPLETE` when all original tasks and acceptance criteria are fully done with no deferrals or pending work (`prompt-template/codex/regular-review.md:65-74`).
- inputs_outputs_state: Inputs are `{{CURRENT_ROUND}}`, `{{PLAN_FILE}}`, `{{PROMPT_FILE}}`, `{{SUMMARY_CONTENT}}`, `{{GOAL_TRACKER_FILE}}`, `{{DOCS_PATH}}`, `{{GOAL_TRACKER_UPDATE_SECTION}}`, and `{{REVIEW_RESULT_FILE}}`. `hooks/loop-codex-stop-hook.sh` renders it into `round-N-review-prompt.md` for non-full-alignment rounds (`hooks/loop-codex-stop-hook.sh:936-940`, `1006-1020`). Output is a review prompt consumed by Codex; Codex then writes `round-N-review-result.md`, which the stop hook uses to continue, finalize, stop, or block.
- gates_or_invariants: The prompt makes the mainline verdict mandatory, and the stop hook has a matching block path if the review omits that line (`hooks/loop-codex-stop-hook.sh:1355-1388`). The `COMPLETE` gate is strict: deferred or unfinished work must not be treated as complete (`prompt-template/codex/regular-review.md:70-74`). It also prevents queued side issues from taking over the next round by requiring lane classification and an explicit mainline progress verdict.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` after summary collection and goal-tracker update-section rendering (`hooks/loop-codex-stop-hook.sh:940-947`, `1008-1020`). Shares review infrastructure with `prompt-template/codex/full-alignment-review.md`, which is selected every configured full-review interval (`hooks/loop-codex-stop-hook.sh:948-1005`).
- edge_cases_or_failure_modes: If `FULL_REVIEW_ROUND` is invalid, the hook defaults to `5` before selecting regular versus full alignment review (`hooks/loop-codex-stop-hook.sh:948-958`). If this template is missing, the hook uses a much weaker fallback prompt (`hooks/loop-codex-stop-hook.sh:980-989`), so semantic coverage depends on this file being present. If Codex writes no result or omits the verdict, separate block paths prevent unsafe continuation.
- validation_or_tests: Covered by stop-hook and Codex-review tests, including review-result handling, missing verdict handling, and template reference/comprehensive tests. The required verdict block is implemented in `hooks/loop-codex-stop-hook.sh:1355-1388`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-217 `file` `tests/robustness/test-pr-loop-api-poll.sh`
- cursor: `[_]`
- core_role: Thin executable wrapper for the PR-loop stop-hook and polling robustness test group. It is part of a parallel split and runs tests 12-19 from the shared PR-loop API robustness suite.
- algorithmic_behavior: Resolves its own directory, sources `tests/robustness/test-pr-loop-api-robustness.sh`, calls `run_poll_tests`, prints a grouped summary, and exits with that summary status (`tests/robustness/test-pr-loop-api-poll.sh:11-18`). The sourced `run_poll_tests` covers PR stop-hook behavior and `scripts/poll-pr-reviews.sh` behavior (`tests/robustness/test-pr-loop-api-robustness.sh:560-855`).
- inputs_outputs_state: Inputs are the shared robustness library, mock git repos, mock `gh` commands, jq, and the production PR stop hook/poll script. Outputs are pass/fail assertions and final exit status. Test-local state includes temp repos under the shared test dir, synthetic `.humanize/pr-loop/.../state.md`, mock `gh` binaries, and generated `approve-state.md` path checks (`tests/robustness/test-pr-loop-api-robustness.sh:571-620`, `660-818`).
- gates_or_invariants: The test group asserts: no active PR loop should exit successfully (`tests/robustness/test-pr-loop-api-robustness.sh:571-585`); corrupted PR-loop state should not crash with signal-like exit code (`tests/robustness/test-pr-loop-api-robustness.sh:587-604`); `approve-state.md` should be writable in the expected loop session directory (`tests/robustness/test-pr-loop-api-robustness.sh:606-620`); poll help exits zero and displays usage (`tests/robustness/test-pr-loop-api-robustness.sh:630-641`); missing poll args are rejected (`tests/robustness/test-pr-loop-api-robustness.sh:643-655`); mocked valid polling returns JSON with boolean `has_new_comments` and comments array (`tests/robustness/test-pr-loop-api-robustness.sh:657-686`); slow API calls complete without hanging (`tests/robustness/test-pr-loop-api-robustness.sh:688-753`); API failure must still exit zero with valid JSON and boolean false `has_new_comments` (`tests/robustness/test-pr-loop-api-robustness.sh:755-854`).
- dependencies_and_callers: Depends on `hooks/pr-loop-stop-hook.sh`, `scripts/poll-pr-reviews.sh`, the helper functions in `tests/robustness/test-pr-loop-api-robustness.sh`, mock GitHub CLI creation, jq, and git fixture initialization. The production poll script resolves current/fork parent repo before polling (`scripts/poll-pr-reviews.sh:122-150`), maps configured bot names to GitHub bot authors (`scripts/poll-pr-reviews.sh:164-187`), retries API calls while degrading failures to empty arrays (`scripts/poll-pr-reviews.sh:200-235`), filters comments by timestamp and bot regex (`scripts/poll-pr-reviews.sh:299-305`), and emits final JSON (`scripts/poll-pr-reviews.sh:311-327`).
- edge_cases_or_failure_modes: This wrapper has no independent logic beyond sourcing and dispatch, so failures generally come from the shared library or production scripts. It deliberately checks graceful degradation on API failure instead of fail-fast polling. The API-failure test extracts JSON after warning lines by finding the first line beginning with `{`, so production warnings on stderr/stdout are tolerated as long as final JSON is parseable (`tests/robustness/test-pr-loop-api-robustness.sh:830-845`).
- validation_or_tests: This assigned file is itself the split validation entrypoint for PR-loop poll behavior. The substantive assertions live in `run_poll_tests` in the shared robustness file.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8/8 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`