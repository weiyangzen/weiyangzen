# agent_05 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: User-facing algorithm contract documentation for Humanize installation, RLCR/PR loop operation, monitoring, and BitLesson policy. The directory is small and was inspected recursively: `docs/bitlesson.md`, `docs/install-for-kimi.md`, `docs/install-for-codex.md`, `docs/install-for-claude.md`, `docs/usage.md`, `docs/images/rlcr-workflow.svg`, and `docs/images/monitor.png`.
- algorithmic_behavior: `docs/usage.md:5-12` defines the two-phase RLCR algorithm: implementation-summary review until COMPLETE, then `codex review --base <branch>` with severity markers. `docs/usage.md:16-41` documents the Plan Understanding Quiz and skip/yolo modes. `docs/usage.md:212-239` defines PR loop behavior: detect current PR, fetch bot comments, fix/push/trigger review, stop-hook polling, local Codex validation, repeat until approval or max iterations. `docs/bitlesson.md:25-35` defines BitLesson initialization, task selection, `NONE` recording, and stop-gate validation. `docs/images/rlcr-workflow.svg:5-17` and `:32-75` encode the visual state graph from plan to implementation, summary review, code review, done, and feedback loops.
- inputs_outputs_state: Inputs documented here include plan files, annotated plans, PR bot flags, Codex model/effort, config hierarchy, and shell environment. Outputs include generated plans, QA ledgers, loop directories under `.humanize/rlcr/<timestamp>/` or `.humanize/pr-loop/<timestamp>/`, cache logs under `~/.cache/humanize/...`, and installed skills/hooks. `docs/usage.md:267-312` documents config resolution: defaults, user config, project config, CLI flags. `docs/bitlesson.md:27` names `.humanize/bitlesson.md` as the project knowledge base.
- gates_or_invariants: `docs/bitlesson.md:36-51` specifies the required `## BitLesson Delta` summary block and strict action/id validation. `docs/install-for-codex.md:25-34` makes Codex install include native hooks, config seeding, provider mode, and Codex CLI version requirement. `docs/usage.md:341-376` documents the dangerous sandbox bypass environment variable and default safe mode. `docs/usage.md:308-312` defines Codex model precedence.
- dependencies_and_callers: The docs coordinate sibling runtime paths: `scripts/install-skills-codex.sh`, `scripts/install-skill.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/humanize.sh`, native hook files, and prompt templates. `docs/install-for-codex.md:48-68` lists bundled runtime directories copied into the Codex skill runtime. `docs/install-for-claude.md:58-72` and `docs/usage.md:319-334` document monitor setup using `scripts/humanize.sh`.
- edge_cases_or_failure_modes: Provider routing can fall back if the configured provider binary is missing (`docs/bitlesson.md:14-23`). `--yolo` bypasses the quiz and lets Claude answer Codex questions (`docs/usage.md:37-41`). PR loop requires `gh`, Codex, and an open PR (`docs/usage.md:240-244`). The monitor PNG is a visual asset only: `file` reports it as `900 x 173` PNG with no line-addressable algorithm.
- validation_or_tests: Documentation describes expected validation surfaces rather than executing them. The algorithmic contracts are backed elsewhere by scripts and tests assigned separately, especially PR loop setup, monitor e2e, BitLesson validation, and plan-file robustness.
- skip_candidate: `no`

### ASK_GEMINI-HZ-035 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Claude command workflow entry point for initializing the PR review loop.
- algorithmic_behavior: Frontmatter restricts the command’s tool surface to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)` at `commands/start-pr-loop.md:4`. The command body executes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh" $ARGUMENTS` at `:11-13`. The workflow described at `:15-23` chains PR detection, comment fetching, issue fixing, pushing, stop-hook polling, and local Codex validation.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, primarily `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout`. Outputs are delegated to the setup script: active PR-loop state directory, goal tracker, prompt, and terminal instructions. The command itself does not directly mutate state except by invoking the script.
- gates_or_invariants: At least one bot flag is required (`commands/start-pr-loop.md:24-28`). Human comments are prioritized over bots, with newest bot comments first (`:30-35`). The workflow requires commit/push, correct bot mention, summary write, and stop-hook-controlled polling (`:36-55`).
- dependencies_and_callers: Depends on Claude command execution, `${CLAUDE_PLUGIN_ROOT}`, `scripts/setup-pr-loop.sh`, PR stop hook, GitHub bot mentions, GitHub CLI, and Codex validation. It is the user-facing command counterpart to the script and hook system.
- edge_cases_or_failure_modes: Misuse is mostly surfaced by `setup-pr-loop.sh`: missing bot flags, no PR, missing `gh`, missing Codex, or active loop conflicts. The command also explicitly forbids cheating by editing state or running cancel commands to bypass the loop (`commands/start-pr-loop.md:54-55`).
- validation_or_tests: The command behavior is validated indirectly by PR loop script/hook tests, especially tests that inspect setup, prompt generation, stop-hook polling, and state protection.
- skip_candidate: `no`

### ASK_GEMINI-HZ-065 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for PR loop sessions. It creates `.humanize/pr-loop/<timestamp>/` state, fetches initial comments, decides startup behavior, optionally triggers bot re-review, creates goal tracker and round-0 prompt, and prints the initial instructions.
- algorithmic_behavior: Defaults are PR-specific: Codex effort `medium`, timeout `900`, max iterations `42`, poll interval `30`, poll timeout `900` (`scripts/setup-pr-loop.sh:17-31`). Argument parsing accepts bot flags and numeric/model settings, rejects unknown or positional arguments (`:49-174`). Active bots are normalized as `claude`/`codex` and rendered into mention strings via `build_bot_mention_string` (`:193-214`). The script enforces one active loop at a time by checking both RLCR and PR loop directories (`:222-241`), then validates git repo, commit existence, `gh`, `gh auth`, and `codex` (`:243-277`).
- inputs_outputs_state: Inputs include CLI flags, current branch, GitHub PR metadata, active bot set, reviewer status JSON, initial comments, and templates. Outputs include `round-0-pr-comment.md`, `state.md`, `goal-tracker.md`, `round-0-pr-resolve.md` path, and `round-0-prompt.md`. The state file records `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, Codex model/effort/timeout, polling settings, startup case, latest commit, latest commit time, `last_trigger_at`, and trigger comment id (`:615-635`).
- gates_or_invariants: PR lookup is fork-aware. It first uses `gh pr view --json number,url` and extracts the actual repo from the PR URL (`scripts/setup-pr-loop.sh:308-330`), then tries parent repo lookup with `FORK_OWNER:BRANCH` if needed (`:334-348`). It rejects missing, non-numeric, merged, or closed PRs (`:351-374`). It blocks YAML-unsafe branch/model/effort values (`:384-407`). For startup cases 4/5, it avoids duplicate trigger comments by searching comments after the latest commit and requiring all active bot mention patterns outside code/quotes (`:462-503`). If no reusable trigger exists, it posts a new trigger comment and aborts with cleanup if posting fails (`:522-538`). It intentionally avoids local timestamp fallback to prevent clock-skew filtering (`:558-561`). Claude mode additionally requires an eyes reaction on the trigger comment and cleans up on failure (`:564-600`).
- dependencies_and_callers: Direct dependencies are `scripts/portable-timeout.sh` for `run_with_timeout`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `git`, `gh`, `jq`, and `codex`. Template dependencies include `prompt-template/pr-loop/goal-tracker-initial.md`, `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, and critical requirements templates. Caller is `commands/start-pr-loop.md`.
- edge_cases_or_failure_modes: Handles fork PRs, renamed local branches, duplicate trigger spam, mentions inside code blocks/quotes, missing GitHub timestamps, PR state closed/merged, YAML injection characters, and SIGPIPE during final output via `trap 'exit 0' PIPE` (`scripts/setup-pr-loop.sh:857-858`). Implicit risk: `jq` is used heavily after prerequisites but is not explicitly checked in this script, so missing `jq` fails under `set -euo pipefail`.
- validation_or_tests: PR loop behavior is covered by script, hook, stop-hook, API robustness, and monitor tests. The setup script itself contains clear executable contracts through its error messages, state schema, and prompt/template selection logic.
- skip_candidate: `no`

### ASK_GEMINI-HZ-095 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: True end-to-end executable specification for monitor behavior using the real `scripts/humanize.sh` monitor functions rather than stubs.
- algorithmic_behavior: The test states its contract at `tests/test-monitor-e2e-real.sh:3-12`: clean exit when loop directories are deleted, no shell glob errors, terminal restoration, and bash/zsh compatibility. Test 1 creates a valid RLCR loop state and goal tracker, fake cache log, then sources the real monitor and deletes `.humanize/rlcr` while it runs (`:56-181`). It asserts user-friendly stop messages, no `no matches found`/`bad pattern`, cleanup message, scroll reset source, and exit code 0 (`:187-227`). Test 2 repeats RLCR deletion under zsh (`:233-370`). Tests 3 and 4 send SIGINT to RLCR monitors under bash/zsh and assert cleanup or clean exit plus no glob errors (`:377-550`, `:555-686`). Tests 5 and 6 cover PR monitor `--once` deletion race and continuous PR monitor SIGINT (`:691-829`, `:834-997`).
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/state.md`, `.humanize/pr-loop/<timestamp>/state.md`, goal trackers, fake HOME/XDG cache directories, tput/clear shims, zsh availability, and process signals. Outputs are pass/fail counters and captured monitor output. State transitions under test include active loop directory becoming absent, monitor process receiving SIGINT, and PR monitor once/continuous modes exiting cleanly.
- gates_or_invariants: The test enforces no zsh glob expansion failures and verifies monitor code uses `find`-based session discovery through behavior. It also checks RLCR monitor terminal cleanup by verifying `scripts/humanize.sh` contains `printf "\033[r"` (`tests/test-monitor-e2e-real.sh:215-219`), matching implementation in `scripts/humanize.sh:707-712`. The RLCR implementation explicitly calls graceful stop with `.humanize/rlcr directory no longer exists` when the directory disappears (`scripts/humanize.sh:840-843`, `:900-904`, `:1011-1015`).
- dependencies_and_callers: Depends on `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, bash, optional zsh, `tput` behavior, process signals, cache log naming, and the monitor command dispatcher. `monitor-common.sh:49-60` uses `find` instead of globs to avoid zsh no-match errors, which the test asserts.
- edge_cases_or_failure_modes: The bash SIGINT cases allow a SIGTERM fallback because process-group signal delivery can be unreliable in test shells (`tests/test-monitor-e2e-real.sh:505-518`, `:957-970`). PR deletion coverage is weaker than RLCR deletion coverage because PR `--once` exits after rendering status; the deletion happens in a short background race rather than through an explicit directory-deleted branch. This is useful evidence but not as strong as the RLCR deletion assertions.
- validation_or_tests: The file is itself the validation harness. It ends with a direct-run summary and exits nonzero on failures (`tests/test-monitor-e2e-real.sh:1002-1038`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-125 `file` `prompt-template/block/bitlesson-delta-empty-kb.md`
- cursor: `[_]`
- core_role: Stop-gate block message used when a round summary says `Action: none` while the BitLesson knowledge base has no concrete lesson entries.
- algorithmic_behavior: The template declares “BitLesson Recording Required” and tells the worker that `Action: none` is not allowed because `.humanize/bitlesson.md` has no concrete entries (`prompt-template/block/bitlesson-delta-empty-kb.md:1-5`). It instructs add/update of at least one reusable lesson when the round resolved issues.
- inputs_outputs_state: Inputs come from `scripts/bitlesson-validate-delta.sh`, which passes current round and BitLesson file variables to `load_and_render_safe`, although this specific template text is static. Output is a human-readable block reason used by the stop gate.
- gates_or_invariants: The enforcing condition is in `scripts/bitlesson-validate-delta.sh:242-267`: if action is `none`, lesson ids must be empty/NONE, and if concrete lesson count is zero while `BITLESSON_ALLOW_EMPTY_NONE` is not true, the hook blocks with this template. This preserves the invariant that an empty BitLesson KB cannot repeatedly produce no-op deltas unless explicitly allowed.
- dependencies_and_callers: Depends on `scripts/bitlesson-validate-delta.sh`, `hooks/lib/template-loader.sh`, `.humanize/bitlesson.md`, and the summary contract documented in `docs/bitlesson.md:36-51`.
- edge_cases_or_failure_modes: The gate is bypassed when `--allow-empty-bitlesson-none` or equivalent state allows empty none. If the template is missing or empty, the validator has a fallback block at `scripts/bitlesson-validate-delta.sh:255-261`.
- validation_or_tests: BitLesson behavior is covered by `tests/test-bitlesson-validate-delta.sh` and routing tests referenced by repository file inventory; the assigned template’s direct call site is line-referenced above.
- skip_candidate: `no`

### ASK_GEMINI-HZ-155 `file` `prompt-template/block/round-contract-bash-write.md`
- cursor: `[_]`
- core_role: Hook block template preventing Bash-based mutation of RLCR round contract files.
- algorithmic_behavior: The template says not to use Bash commands to modify round contract files and directs use of Write/Edit for `{{CORRECT_PATH}}` (`prompt-template/block/round-contract-bash-write.md:1-7`). It is not a standalone workflow prompt; it is an enforcement message.
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by the active loop and current round. Output is stderr block text from the Bash validator, followed by an exit code 2.
- gates_or_invariants: `hooks/loop-bash-validator.sh:528-542` detects commands that modify `round-[0-9]+-contract.md`, computes the current round contract path, renders this template, and exits 2. The invariant is that round contracts must be authored through Write/Edit so the write/edit validators can enforce active loop, correct round, and finalize-phase rules.
- dependencies_and_callers: Depends on `hooks/loop-bash-validator.sh`, active loop state from shared loop helpers, `command_modifies_file`, and `hooks/lib/template-loader.sh`. Related validators enforce contract read/write/edit round scoping and finalize-phase access separately.
- edge_cases_or_failure_modes: The Bash validator blocks any Bash mutation targeting a round contract, even if the target is the current round. That is intentional because current-round contract writes are allowed via Write/Edit validators, not shell redirection or shell editing commands.
- validation_or_tests: Contract access and Bash-block behavior are covered in validator tests such as allowlist/finalize/plan-file hook tests; the direct runtime call site is `hooks/loop-bash-validator.sh:534-542`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-185 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex review prompt template for mandatory full goal alignment checkpoints in RLCR.
- algorithmic_behavior: It requires Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), review Claude’s summary (`:13-16`), audit goal tracker acceptance criteria, forgotten items, deferred items, and completion summary (`:19-48`), then perform mainline drift audit (`:50-64`), implementation review (`:66-72`), goal tracker update instructions (`:73`), and stagnation analysis over historical round files (`:75-100`).
- inputs_outputs_state: Inputs are template variables: current round, plan file, summary content, goal tracker file, docs path, goal tracker update section, completed iteration count, loop timestamp, previous round numbers, and review result file. Output is a Codex-written review result at `{{REVIEW_RESULT_FILE}}`.
- gates_or_invariants: The template makes `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` mandatory (`prompt-template/codex/full-alignment-review.md:57-64`). It instructs STOP as the final line on stagnation (`:100`) and COMPLETE only when all original plan tasks and ACs are fully met with no deferrals (`:104-112`). The stop hook selects this template for full alignment rounds at `hooks/loop-codex-stop-hook.sh:948-1000`, and separately blocks missing verdicts.
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, `prompt-template/codex/goal-tracker-update-section.md`, regular review template, goal tracker state, plan backup/original plan, docs path, and historical round artifacts in `.humanize/rlcr/<timestamp>/`.
- edge_cases_or_failure_modes: Missing mainline verdict blocks the round. STOP is expected primarily during full alignment checks and triggers circuit-breaker termination. Deferred ACs are explicitly incomplete, so a model that writes COMPLETE with deferrals violates the contract. Historical file access may fail if prior artifacts are missing, which weakens stagnation analysis but does not remove the required verdict/output contract.
- validation_or_tests: Full alignment and stop-hook behavior are covered by stop/finalize/agent-team tests that assert COMPLETE, STOP, missing verdict, and mainline verdict behavior. The template’s enforced fields are also referenced by `hooks/lib/loop-common.sh` verdict extraction.
- skip_candidate: `no`

### ASK_GEMINI-HZ-215 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test harness for production plan-file validation in `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The test creates an isolated temp repo, mock Codex binary, and cache (`tests/robustness/test-plan-file-robustness.sh:18-40`, `:110-122`). `test_plan_validation` runs the production setup script with `CLAUDE_PROJECT_DIR` and classifies plan-validation errors, Codex availability, gitignore policy success, loop activation, or state file creation (`:46-105`). A local helper counts content lines by excluding blanks, HTML comments, multiline HTML comments, and lines starting `#` (`:129-169`).
- inputs_outputs_state: Inputs are generated plan files with varied content/path/file characteristics. Outputs are pass/fail results via `test-helpers.sh`. The production script, when validation passes far enough, may create `.humanize/rlcr/<timestamp>/state.md`; the test removes old loop dirs before each production invocation (`:56-62`).
- gates_or_invariants: Production gates being exercised are in `scripts/setup-rlcr-loop.sh`: relative path only, no spaces/metacharacters, no file or parent symlinks, existing readable file, resolved path inside project, not in submodule (`:462-563`); git tracking policy based on `--track-plan-file` (`:569-614`); at least five physical lines and at least three content lines, with `#` lines treated as comments (`:625-678`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, mock `codex`, `git`, `wc`, shell file permissions, and `tests/test-helpers.sh`. It is a robustness suite rather than a runtime component.
- edge_cases_or_failure_modes: Production-invoked cases include valid plan, exactly three content lines, standard size, rich markdown, empty file, comments-only file, large file over 1MB, mixed CRLF/LF endings, and disappearing/missing file (`tests/robustness/test-plan-file-robustness.sh:178-366`, `:527-551`). Several later checks are local sanity checks rather than production invocations: binary content, long lines, special characters, whitespace-only helper count, nested comments, nonexistent file existence, unreadable file, symlink, directory, and null bytes (`:368-525`). That distinction matters: they verify assumptions around shell/file handling but do not all prove `setup-rlcr-loop.sh` accepts/rejects those cases.
- validation_or_tests: The file is itself validation. It prints a summary and exits with the aggregated helper status (`tests/robustness/test-plan-file-robustness.sh:553-558`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8 unique assigned item headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`