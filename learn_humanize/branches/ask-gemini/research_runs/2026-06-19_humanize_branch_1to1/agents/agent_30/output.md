# agent_30 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-030 `file` `agents/plan-understanding-quiz.md`
- cursor: `[_]`
- core_role: Defines a specialized Claude agent prompt for validating whether a user understands an implementation plan before starting an RLCR loop. It is a policy/prompt file, not executable code, but it controls a pre-flight comprehension gate used by `commands/start-rlcr-loop.md`.
- algorithmic_behavior: The agent is instructed to read the provided plan, inspect repository context, and generate exactly two multiple-choice questions plus a short technical summary. The key algorithm is: analyze plan content and referenced repo files, derive two implementation-specific questions, generate four plausible options per question, choose exactly one correct option, and emit a strict 13-field flat format. The prompt explicitly rejects generic or trivial questions and requires questions to test "HOW things will be done" rather than title-level understanding (`agents/plan-understanding-quiz.md:16`, `agents/plan-understanding-quiz.md:29`, `agents/plan-understanding-quiz.md:36`).
- inputs_outputs_state: Input is the plan content supplied by the caller plus repository files the agent may read using `Read`, `Glob`, and `Grep` (`agents/plan-understanding-quiz.md:5`, `agents/plan-understanding-quiz.md:14`, `agents/plan-understanding-quiz.md:24`). Output is exactly: `QUESTION_1`, four `OPTION_1*` fields, `ANSWER_1`, `QUESTION_2`, four `OPTION_2*` fields, `ANSWER_2`, and `PLAN_SUMMARY` (`agents/plan-understanding-quiz.md:57`). It does not persist state itself; downstream command logic parses the output and drives user-question state.
- gates_or_invariants: The strict invariants are exactly two questions, exactly four options per question, exactly one correct answer, all 13 fields present, and answer letters limited to `A`, `B`, `C`, or `D` (`agents/plan-understanding-quiz.md:31`, `agents/plan-understanding-quiz.md:59`, `agents/plan-understanding-quiz.md:79`). It also requires randomizing correct-answer position and preserving the plan language (`agents/plan-understanding-quiz.md:81`).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md`, which says to invoke `humanize:plan-understanding-quiz` with the opus model, parse all 13 fields, ask the two questions via `AskUserQuestion`, and continue, stop, or warn depending on output quality and user answers (`commands/start-rlcr-loop.md:71`, `commands/start-rlcr-loop.md:86`, `commands/start-rlcr-loop.md:90`). It depends conceptually on the RLCR plan pre-check having already read plan content.
- edge_cases_or_failure_modes: If the plan is too short or underspecified, the prompt still requires deriving questions from available implementation hints (`agents/plan-understanding-quiz.md:84`). If output is malformed, the caller degrades to a warning and continues without the quiz (`commands/start-rlcr-loop.md:86`). If the user answers incorrectly, the caller shows `PLAN_SUMMARY` and asks whether to proceed or stop (`commands/start-rlcr-loop.md:100`).
- validation_or_tests: No direct test file was found specifically for this agent. Its behavioral contract is enforced at prompt/caller level by the strict output schema here and the parser requirements in `commands/start-rlcr-loop.md`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-060 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop script that fetches GitHub PR discussion surfaces, normalizes them into a unified comment stream, groups human and bot feedback, and writes the markdown comment bundle used to seed PR-resolution prompts.
- algorithmic_behavior: The script parses positional `pr_number` and `output_file`, plus `--after` and `--bots` options (`scripts/fetch-pr-comments.sh:22`). It validates required args and numeric PR number, requires `gh` and `jq`, resolves the correct base repository for fork PRs by trying current repo then parent repo, fetches issue comments, inline review comments, and PR reviews through paginated `gh api`, then normalizes all three APIs into common JSON objects (`scripts/fetch-pr-comments.sh:92`, `scripts/fetch-pr-comments.sh:126`, `scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:260`). It deduplicates by `id`, optionally filters by timestamp, sorts humans before bots and newest-first within those groups, then renders markdown (`scripts/fetch-pr-comments.sh:322`, `scripts/fetch-pr-comments.sh:327`, `scripts/fetch-pr-comments.sh:334`, `scripts/fetch-pr-comments.sh:345`).
- inputs_outputs_state: Inputs are GitHub CLI authentication/context, PR number, output path, optional ISO timestamp, and optional comma-separated active bot names (`scripts/fetch-pr-comments.sh:50`). It uses temp JSON files for issue comments, review comments, PR reviews, processed JSONL, combined JSON, and sorted JSON under `mktemp -d`, cleaned by `trap` (`scripts/fetch-pr-comments.sh:168`). Output is a markdown file headed with PR number, fetch timestamp, resolved repository, human comments, bot comments or grouped bot sections, and final sentinel `*End of comments*` (`scripts/fetch-pr-comments.sh:245`, `scripts/fetch-pr-comments.sh:357`, `scripts/fetch-pr-comments.sh:443`).
- gates_or_invariants: Hard gates include valid arguments, `gh`, `jq`, resolvable PR base repo, and parseable owner/name (`scripts/fetch-pr-comments.sh:103`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:117`, `scripts/fetch-pr-comments.sh:151`, `scripts/fetch-pr-comments.sh:159`). API endpoint failures are soft-gated: each endpoint retries three times, writes `[]` on terminal failure, increments `API_FAILURES`, and continues under `set -euo pipefail` (`scripts/fetch-pr-comments.sh:176`, `scripts/fetch-pr-comments.sh:190`, `scripts/fetch-pr-comments.sh:199`). The no-comments sentinel is exactly `*No comments found.*`, used later by setup logic (`scripts/fetch-pr-comments.sh:348`, `scripts/setup-pr-loop.sh:723`).
- dependencies_and_callers: Depends on `gh api`, `gh repo view`, `gh pr view`, `jq`, `date`, `mktemp`, and shell utilities. `scripts/setup-pr-loop.sh` calls it for round-0 comments with `--bots`, then uses the output file to decide startup prompt shape (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:725`). Tests reference it in PR loop script tests and robustness/API tests.
- edge_cases_or_failure_modes: Fork PRs are handled by parent repo fallback (`scripts/fetch-pr-comments.sh:142`). Empty PR reviews are preserved with a placeholder state body, so approval-only reviews are not dropped (`scripts/fetch-pr-comments.sh:300`). Entries with `null` timestamps are filtered before `fromdateiso8601` to avoid jq errors (`scripts/fetch-pr-comments.sh:336`). Bot grouping maps `codex` to `chatgpt-codex-connector[bot]`, while other bot names become `<bot>[bot]` (`scripts/fetch-pr-comments.sh:383`). Deduplication by only `.id` can collide across GitHub API domains if IDs overlap, because type is not included in the uniqueness key (`scripts/fetch-pr-comments.sh:322`).
- validation_or_tests: Covered by `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/robustness/test-pr-loop-api-robustness.sh`, including empty arrays, malformed/failed API paths, bot comments, multiple bots, Unicode and long bodies, approval-only reviews, and `--after` filtering. It is also indirectly validated by setup-pr-loop behavior that treats the no-comments sentinel as a state transition.
- skip_candidate: `no`

### ASK_GEMINI-HZ-090 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `gen-plan` command, its prompt template coupling, agent metadata conventions, and `validate-gen-plan-io.sh` input/output gate behavior.
- algorithmic_behavior: The script runs positive structural checks against `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin` metadata, and `prompt-template/plan/gen-plan-template.md`, then creates negative fixtures to verify validation helpers reject bad names, missing frontmatter, malformed YAML, and invalid model aliases. It then executes `scripts/validate-gen-plan-io.sh` across argument and filesystem scenarios and finally checks that the embedded markdown plan structure in `gen-plan.md` exactly matches the template file (`tests/test-gen-plan.sh:47`, `tests/test-gen-plan.sh:313`, `tests/test-gen-plan.sh:556`, `tests/test-gen-plan.sh:708`).
- inputs_outputs_state: Inputs are repository files under `commands`, `agents`, `prompt-template/plan`, `.claude-plugin`, `README.md`, and the validator script (`tests/test-gen-plan.sh:11`, `tests/test-gen-plan.sh:59`, `tests/test-gen-plan.sh:111`, `tests/test-gen-plan.sh:123`, `tests/test-gen-plan.sh:295`). It creates temp fixture directories with `mktemp -d` and writes invalid markdown fixtures plus temporary input/output files for validator tests (`tests/test-gen-plan.sh:322`, `tests/test-gen-plan.sh:386`, `tests/test-gen-plan.sh:566`). Output is PASS/FAIL console text and final exit code 0 if `TESTS_FAILED` is zero, otherwise 1 (`tests/test-gen-plan.sh:721`, `tests/test-gen-plan.sh:728`).
- gates_or_invariants: It requires `gen-plan.md` to exist with frontmatter description, `allowed-tools`, and `argument-hint` (`tests/test-gen-plan.sh:55`, `tests/test-gen-plan.sh:67`, `tests/test-gen-plan.sh:80`, `tests/test-gen-plan.sh:93`). It locks the Claude/Codex deliberation workflow: ask-codex script allowed, auto-start flag exposed, direct mode marked only partially converged, auto-start only in discussion mode, ultrathink required, pending decisions present, Phase 3 before Phase 4, and a maximum three-round convergence limit (`tests/test-gen-plan.sh:125`, `tests/test-gen-plan.sh:137`, `tests/test-gen-plan.sh:143`, `tests/test-gen-plan.sh:149`, `tests/test-gen-plan.sh:161`, `tests/test-gen-plan.sh:179`). It also enforces task tagging (`coding`/`analyze`) and pending decision consolidation sources (`tests/test-gen-plan.sh:225`, `tests/test-gen-plan.sh:243`).
- dependencies_and_callers: Uses shell, `grep`, `sed`, `awk`, `mktemp`, `wc`, and executable `scripts/validate-gen-plan-io.sh`. It is included in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:82`). It depends on `validate-gen-plan-io.sh` exit codes and flags, whose documented exit codes include 1 input missing, 2 empty input, 3 output dir missing, 4 output exists/is directory, 6 invalid args, and 7 missing template (`scripts/validate-gen-plan-io.sh:4`).
- edge_cases_or_failure_modes: The YAML syntax checker is intentionally lightweight and line-pattern based, not a full YAML parser (`tests/test-gen-plan.sh:435`). The content check uses `grep -P`, but redirects errors, so environments without PCRE-compatible grep may silently skip detection by falling into the pass path (`tests/test-gen-plan.sh:540`). Multiple `trap` assignments overwrite earlier cleanup traps, so only the last temp dir is guaranteed to be removed in this script (`tests/test-gen-plan.sh:324`, `tests/test-gen-plan.sh:567`). Validator tests intentionally use `/dev/null` for flag recognition and mutual exclusion checks, which exercises argument parsing without creating normal files (`tests/test-gen-plan.sh:672`).
- validation_or_tests: This file is itself the validation harness. It validates command schema, deliberation workflow ordering, template synchronization, agent metadata, version consistency, ASCII/English-only policy for selected files, and validator exit behavior. It exits nonzero on accumulated failures (`tests/test-gen-plan.sh:728`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-120 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: Executable specification for the migration to unified `codex_model` and `codex_effort` configuration across default config, config merging, RLCR state parsing, setup scripts, PR loop setup, stop hooks, and `ask-codex`.
- algorithmic_behavior: The test validates a layered configuration algorithm: default config provides `codex_model`/`codex_effort`, project config overrides defaults, caller-pre-set `DEFAULT_CODEX_*` overrides config, invalid or unsupported config values warn and fall back, state files override defaults for stop-hook execution, and stale `loop_reviewer_*` keys are ignored (`tests/test-unified-codex-config.sh:57`, `tests/test-unified-codex-config.sh:84`, `tests/test-unified-codex-config.sh:126`, `tests/test-unified-codex-config.sh:252`, `tests/test-unified-codex-config.sh:391`). Later sections run behavioral setup/PR/ask-codex scenarios with temp repos and mock binaries (`tests/test-unified-codex-config.sh:547`, `tests/test-unified-codex-config.sh:778`, `tests/test-unified-codex-config.sh:899`).
- inputs_outputs_state: Inputs include `config/default_config.json`, `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `hooks/pr-loop-stop-hook.sh`, and `scripts/ask-codex.sh` (`tests/test-unified-codex-config.sh:63`, `tests/test-unified-codex-config.sh:90`, `tests/test-unified-codex-config.sh:132`, `tests/test-unified-codex-config.sh:478`, `tests/test-unified-codex-config.sh:682`, `tests/test-unified-codex-config.sh:752`). It creates temporary `.humanize/config.json`, RLCR/PR state files, git repos, bare remotes, and mock `gh`/`codex` executables. Output is test-helper PASS/FAIL/SKIP text and `print_test_summary` exit status (`tests/test-unified-codex-config.sh:18`, `tests/test-unified-codex-config.sh:970`).
- gates_or_invariants: `default_config.json` must contain `codex_model: gpt-5.4` and `codex_effort: high`, and must not contain `loop_reviewer_*` keys (`tests/test-unified-codex-config.sh:68`). `loop-common.sh` must expose `DEFAULT_CODEX_MODEL`/`DEFAULT_CODEX_EFFORT`, not reviewer defaults (`tests/test-unified-codex-config.sh:143`, `tests/test-unified-codex-config.sh:155`). Valid effort values are exactly `xhigh`, `high`, `medium`, and `low` (`tests/test-unified-codex-config.sh:657`). `setup-rlcr-loop.sh` and generated state must avoid `loop_reviewer` fields (`tests/test-unified-codex-config.sh:385`, `tests/test-unified-codex-config.sh:622`). PR loop setup must not pre-set model but may pre-set effort to medium (`tests/test-unified-codex-config.sh:690`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for counters, temp dirs, temp git repos, and summary (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:86`, `tests/test-helpers.sh:93`). Depends on `jq` for JSON-based tests and skips parts when missing (`tests/test-unified-codex-config.sh:65`). The implementation under test uses `load_merged_config` and `get_config_value`, which merge `{}` + default + user + project layers and strip nulls (`scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:113`). Included in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:88`).
- edge_cases_or_failure_modes: Several heavy sections are conditional on local tools (`jq`, `codex`) and skip if unavailable, so full coverage depends on environment (`tests/test-unified-codex-config.sh:553`). The stop-hook invalid-effort test asserts that an invalid state value prevents even a stub `codex` invocation (`tests/test-unified-codex-config.sh:511`, `tests/test-unified-codex-config.sh:537`). It checks shell-metacharacter and whitespace rejection by regex directly, not by invoking every CLI parser (`tests/test-unified-codex-config.sh:642`). Mock `gh` covers only the setup-pr-loop calls needed for the e2e path and fails on unhandled args, which is useful as a contract detector (`tests/test-unified-codex-config.sh:824`).
- validation_or_tests: This file is the validation harness for unified Codex config. It directly executes loader and hook code, sources `loop-common.sh`, runs setup scripts in temp git repos, and checks generated state fields and runtime stderr for correct model/effort propagation. It finishes via `print_test_summary`, returning failure if any assertion failed (`tests/test-unified-codex-config.sh:970`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-150 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Prompt/block template used by RLCR validators to explain why `.humanize/rlcr/**/plan.md` backup files cannot be modified. It is a small policy template that supports plan-integrity gates.
- algorithmic_behavior: The template emits a deterministic denial message: `plan.md` in the loop directory is a backup, cannot be modified, exists to preserve plan integrity, and should be read rather than edited (`prompt-template/block/plan-backup-protected.md:1`). There is no branching logic in the template itself.
- inputs_outputs_state: Input is implicit: a validator detected an attempted write/edit/bash modification to a protected backup plan. Output is the rendered markdown reason text written to stderr by the validator. It carries no persistent state.
- gates_or_invariants: Invariant is read-only protection of RLCR loop backup plan files. The write validator blocks `IS_PLAN_BACKUP` inside `.humanize/rlcr/` and exits 2 after rendering this template (`hooks/loop-write-validator.sh:248`). The edit validator blocks basename `plan.md` under `.humanize/rlcr/` and exits 2 (`hooks/loop-edit-validator.sh:187`). The bash validator blocks commands that modify `.humanize/rlcr/.../plan.md` and exits 2 (`hooks/loop-bash-validator.sh:484`).
- dependencies_and_callers: Loaded via `load_and_render_safe` using template path `block/plan-backup-protected.md` from `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh`, each with an inline fallback if template loading fails (`hooks/loop-write-validator.sh:251`, `hooks/loop-edit-validator.sh:191`, `hooks/loop-bash-validator.sh:486`).
- edge_cases_or_failure_modes: If the template is missing or cannot render, callers use fallback strings, so enforcement still works but wording changes. The template says to read the backup instead of modifying it; it does not mention the original source plan path, so users must infer from loop context where edits should go.
- validation_or_tests: Protected plan behavior is likely covered by hook/plan-file robustness tests, and the template is directly referenced in the three validator implementations. No direct standalone template test was inspected for this specific file.
- skip_candidate: `no`

### ASK_GEMINI-HZ-180 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Single-line prompt template injected into the next Claude prompt when Codex review output contains open questions and `ask_codex_question` is enabled. It is an algorithmic transition notice: open questions must be clarified with the user before resolving other findings.
- algorithmic_behavior: The template instructs Claude that Codex found open questions and mandates `AskUserQuestion` clarification before proceeding to other Codex findings (`prompt-template/claude/open-question-notice.md:1`). The branching and injection logic live in the stop hook, not the template.
- inputs_outputs_state: Input condition is a Codex review result containing a short line with substring `Open Question`, while `ASK_CODEX_QUESTION` is true. Output is inserted text in the next round prompt, between the Codex review-result end marker/separator and the goal tracker section (`hooks/loop-codex-stop-hook.sh:2012`, `hooks/loop-codex-stop-hook.sh:2029`). No persistent state is stored by the template itself.
- gates_or_invariants: The invariant is priority ordering: clarify open questions first, before resolving other Codex findings. Detection is deliberately narrow: a line containing `Open Question` and total length under 40 characters (`hooks/loop-codex-stop-hook.sh:2013`). If detected, the hook logs the injection and inserts the notice (`hooks/loop-codex-stop-hook.sh:2023`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` via `load_template "$TEMPLATE_DIR" "claude/open-question-notice.md"` (`hooks/loop-codex-stop-hook.sh:2025`). The hook has an inline fallback with equivalent wording if the template is unavailable (`hooks/loop-codex-stop-hook.sh:2026`).
- edge_cases_or_failure_modes: The short-line detection can miss headings or labels longer than 39 characters, or open questions phrased without the exact substring. It can also trigger on any short line containing that substring, regardless of section semantics. If the prompt insertion marker layout changes, the awk insertion point may fail or place the notice incorrectly.
- validation_or_tests: No direct test for this exact template was inspected. The hook logic around open-question detection/injection is the enforcement point and should be the target for behavioral validation.
- skip_candidate: `no`

### ASK_GEMINI-HZ-210 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git-status and git-state helpers in `scripts/humanize.sh`, especially `humanize_parse_git_status` and `humanize_detect_git_state`.
- algorithmic_behavior: The script sources production code, creates a temp git repo, mutates repository state across many scenarios, calls `humanize_parse_git_status`, splits its pipe-delimited output into `modified|added|deleted|untracked|insertions|deletions`, and asserts expected counts. It then tests `humanize_detect_git_state` across normal, detached, rebase, merge, shallow, non-git, and permission-error states (`tests/robustness/test-git-operations-robustness.sh:18`, `tests/robustness/test-git-operations-robustness.sh:35`, `tests/robustness/test-git-operations-robustness.sh:71`, `tests/robustness/test-git-operations-robustness.sh:369`).
- inputs_outputs_state: Inputs are a temp directory from `setup_test_dir`, local git executable, production `scripts/humanize.sh`, `scripts/portable-timeout.sh`, and test helper functions (`tests/robustness/test-git-operations-robustness.sh:17`). It creates files, commits, branches, staged/unstaged changes, `.git/rebase-merge`, `.git/MERGE_HEAD`, `.git/shallow`, and unreadable `.git` directories as state fixtures (`tests/robustness/test-git-operations-robustness.sh:49`, `tests/robustness/test-git-operations-robustness.sh:418`, `tests/robustness/test-git-operations-robustness.sh:438`, `tests/robustness/test-git-operations-robustness.sh:455`, `tests/robustness/test-git-operations-robustness.sh:481`). Output is PASS/FAIL/SKIP-style test text and final summary exit code (`tests/robustness/test-git-operations-robustness.sh:506`).
- gates_or_invariants: `humanize_parse_git_status` must return nonempty parseable status in normal and empty repos, count untracked/modified/staged/deleted files, count insertions/deletions, handle detached HEAD, feature branches, renamed files as modified, filenames with spaces, binary files, and simultaneous staged plus unstaged changes (`tests/robustness/test-git-operations-robustness.sh:71`, `tests/robustness/test-git-operations-robustness.sh:197`, `tests/robustness/test-git-operations-robustness.sh:265`, `tests/robustness/test-git-operations-robustness.sh:301`, `tests/robustness/test-git-operations-robustness.sh:316`, `tests/robustness/test-git-operations-robustness.sh:331`). `humanize_detect_git_state` must identify `normal`, `detached`, `rebase`, `merge`, `shallow`, and `not_a_repo`, with `permission_error` or `not_a_repo` acceptable for the permission simulation (`tests/robustness/test-git-operations-robustness.sh:377`, `tests/robustness/test-git-operations-robustness.sh:478`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp dirs and summary, `scripts/portable-timeout.sh`, and production `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:17`). The production function `humanize_detect_git_state` returns state names based on `git rev-parse`, `.git` readability, rebase dirs, `MERGE_HEAD`, shallow marker, and detached HEAD (`scripts/humanize.sh:156`). The robustness test is included in the suite runner (`tests/run-all-tests.sh:109`).
- edge_cases_or_failure_modes: It assumes `master` or `main` exists and uses fallback checkouts (`tests/robustness/test-git-operations-robustness.sh:194`). Some simulated states are file/directory marker simulations rather than running actual conflicted rebase/merge flows, which keeps tests deterministic but does not cover every real git metadata layout. Permission behavior is intentionally flexible across systems (`tests/robustness/test-git-operations-robustness.sh:490`). The script mutates and restores the same repo repeatedly; a failed cleanup in an earlier test could cascade into later assertions.
- validation_or_tests: This file is itself the validation harness. It exercises 25 robustness cases and exits with the shared `print_test_summary` status (`tests/robustness/test-git-operations-robustness.sh:506`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`