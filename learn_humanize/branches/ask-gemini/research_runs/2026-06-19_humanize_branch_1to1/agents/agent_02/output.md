# agent_02 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`
- note: The branch export is read-only and appears not to include `.git`, so local commit verification via `git rev-parse` was not available. Evidence below is from direct file inspection only.

## Item Evidence

### ASK_GEMINI-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Defines specialized sub-agent prompt contracts used before and during RLCR/gen-plan workflows. The directory contains four prompt specs: `draft-relevance-checker.md`, `plan-compliance-checker.md`, `bitlesson-selector.md`, and `plan-understanding-quiz.md`.
- algorithmic_behavior: `draft-relevance-checker.md` performs lenient semantic relevance classification for draft-to-repo fit and emits exactly `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:14`, `agents/draft-relevance-checker.md:27`). `plan-compliance-checker.md` performs two pre-RLCR gates: repository relevance and branch-switch detection, then emits exactly one verdict: `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`agents/plan-compliance-checker.md:14`, `agents/plan-compliance-checker.md:31`, `agents/plan-compliance-checker.md:50`). `plan-understanding-quiz.md` generates two technical multiple-choice questions and a technical summary to verify user understanding before loop start (`agents/plan-understanding-quiz.md:29`, `agents/plan-understanding-quiz.md:57`). `bitlesson-selector.md` selects only directly relevant lessons from `.humanize/bitlesson.md`, returning `LESSON_IDS` and `RATIONALE` in a stable two-line format (`agents/bitlesson-selector.md:10`, `agents/bitlesson-selector.md:32`).
- inputs_outputs_state: Inputs are prompt-injected plan/draft/task text, related file paths, and repository context gathered via Read/Glob/Grep. Outputs are deterministic textual verdicts or structured fields consumed by slash command workflows. These files do not mutate state directly; they gate or annotate later setup/loop execution. `start-rlcr-loop.md` invokes `plan-compliance-checker` before setup and fail-closes malformed/non-pass output (`commands/start-rlcr-loop.md:41`, `commands/start-rlcr-loop.md:52`). It invokes `plan-understanding-quiz` as advisory preflight (`commands/start-rlcr-loop.md:75`, `commands/start-rlcr-loop.md:86`). `gen-plan.md` invokes `draft-relevance-checker` after IO validation (`commands/gen-plan.md:154`, `commands/gen-plan.md:160`). BitLesson selection is required per round by the RLCR command contract (`commands/start-rlcr-loop.md:164`).
- gates_or_invariants: Stable output schemas are explicit and narrow. Plan compliance is blocking and fail-closed on malformed output. The quiz is advisory: malformed output warns and continues. Branch-switch detection deliberately excludes safe patterns such as `git checkout -- <file>` and negated instructions (`agents/plan-compliance-checker.md:40`). BitLesson selection prioritizes precision and returns `NONE` when nothing is directly relevant (`agents/bitlesson-selector.md:26`).
- dependencies_and_callers: Called from `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, and `scripts/bitlesson-select.sh`. The BitLesson runtime routes through Codex or Claude depending on configured `bitlesson_model`; the selector spec documents this cross-agent runtime path (`agents/bitlesson-selector.md:19`, `scripts/bitlesson-select.sh:15`, `scripts/bitlesson-select.sh:26`).
- edge_cases_or_failure_modes: Relevance agents intentionally lean toward accepting ambiguous drafts/plans to avoid false negatives (`agents/draft-relevance-checker.md:33`, `agents/plan-compliance-checker.md:29`). Plan compliance also leans toward pass on ambiguous branch-switch evidence (`agents/plan-compliance-checker.md:60`). Quiz generation must always output all fields and handle short or non-English plans (`agents/plan-understanding-quiz.md:79`, `agents/plan-understanding-quiz.md:82`).
- validation_or_tests: `tests/test-gen-plan.sh` validates `draft-relevance-checker.md` existence, frontmatter, naming, model, tools, and English-only content; cross-references show these checks around agent metadata. Agent invocation behavior is embedded in command specs rather than unit-executed in this assigned set.
- skip_candidate: `no`

### ASK_GEMINI-HZ-032 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow wrapper for canceling active RLCR loops while preserving loop artifacts.
- algorithmic_behavior: The command delegates all state changes to `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh`, then interprets the first output line as a state-machine result (`commands/cancel-rlcr-loop.md:11`, `commands/cancel-rlcr-loop.md:17`). For normal, methodology-analysis, and forced finalize cancellations it reports script output. For finalize phase without force, it asks for explicit user confirmation before rerunning the script with `--force` (`commands/cancel-rlcr-loop.md:24`, `commands/cancel-rlcr-loop.md:31`).
- inputs_outputs_state: Input is an active loop directory under `.humanize/rlcr/` plus optional user confirmation. Output is user-facing status text derived from script sentinel lines: `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:18`). The underlying script creates `.cancel-requested`, removes pending setup/session markers, and renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:152`, `scripts/cancel-rlcr-loop.sh:162`).
- gates_or_invariants: The command itself is non-algorithmic by design: "The script handles all cancellation logic" (`commands/cancel-rlcr-loop.md:37`). Finalize cancellation is gated by a confirmation prompt because finalize completion would otherwise end the loop normally (`commands/cancel-rlcr-loop.md:26`). It treats `state.md`, `methodology-analysis-state.md`, and `finalize-state.md` as active-loop state files (`commands/cancel-rlcr-loop.md:37`).
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-loop.sh`, `AskUserQuestion`, and shared discovery logic from `hooks/lib/loop-common.sh` via the script. The script intentionally performs global newest-active-loop cancellation because slash commands lack hook `session_id` context (`scripts/cancel-rlcr-loop.sh:76`, `scripts/cancel-rlcr-loop.sh:87`).
- edge_cases_or_failure_modes: No active loop returns `NO_LOOP` or `NO_ACTIVE_LOOP` and exits nonzero. Finalize phase returns `FINALIZE_NEEDS_CONFIRM` unless `--force` is used (`scripts/cancel-rlcr-loop.sh:135`). The command preserves the loop directory, summaries, reviews, and state info for audit (`commands/cancel-rlcr-loop.md:39`).
- validation_or_tests: `tests/test-cancel-signal-file.sh`, `tests/test-finalize-phase.sh`, `tests/test-session-id.sh`, and cancel robustness tests reference cancellation behavior. The assigned command file itself is a declarative command contract, not an executable test.
- skip_candidate: `no`

### ASK_GEMINI-HZ-062 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop monitor helper that polls GitHub for new bot-authored PR feedback after a timestamp and returns normalized JSON.
- algorithmic_behavior: Parses `<pr_number> --after <timestamp> --bots <bot1,bot2>`, validates required args and numeric PR number, requires `gh` and `jq`, resolves the correct base repository for fork PRs, maps logical bot names to GitHub authors, fetches issue comments, review comments, and PR reviews, filters by timestamp and watched bot authors, then emits a JSON object (`scripts/poll-pr-reviews.sh:23`, `scripts/poll-pr-reviews.sh:99`, `scripts/poll-pr-reviews.sh:127`, `scripts/poll-pr-reviews.sh:241`, `scripts/poll-pr-reviews.sh:299`, `scripts/poll-pr-reviews.sh:317`).
- inputs_outputs_state: Inputs are PR number, ISO timestamp, comma-separated bot list, GitHub CLI auth/context, and repository/fork metadata. Output JSON contains `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:317`). Comment objects are normalized with `type`, `id`, `author`, `created_at`, and `body`; review comments also include `path` and `line`; PR reviews include `state` and synthesize a body for empty approval-only reviews (`scripts/poll-pr-reviews.sh:242`, `scripts/poll-pr-reviews.sh:259`, `scripts/poll-pr-reviews.sh:280`).
- gates_or_invariants: Fails on missing required CLI args, invalid PR number, missing `gh`, or missing `jq` (`scripts/poll-pr-reviews.sh:83`, `scripts/poll-pr-reviews.sh:108`). It resolves fork PRs by trying the current repo then parent repo before failing (`scripts/poll-pr-reviews.sh:132`, `scripts/poll-pr-reviews.sh:139`, `scripts/poll-pr-reviews.sh:147`). Bot author matching is regex-based and maps `codex` to `chatgpt-codex-connector[bot]` (`scripts/poll-pr-reviews.sh:164`).
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` as `POLL_SCRIPT="$PLUGIN_ROOT/scripts/poll-pr-reviews.sh"` (`hooks/pr-loop-stop-hook.sh:812`) inside a polling loop that tracks per-bot response and timeout state (`hooks/pr-loop-stop-hook.sh:881`, `hooks/pr-loop-stop-hook.sh:962`). Depends on GitHub REST API endpoints through `gh api --paginate` and `jq`.
- edge_cases_or_failure_modes: API fetches retry three times; on final failure the helper increments a diagnostic counter but returns `[]` and success so polling can continue through partial outages (`scripts/poll-pr-reviews.sh:200`, `scripts/poll-pr-reviews.sh:210`, `scripts/poll-pr-reviews.sh:231`). Approval-only PR reviews with empty body are preserved via a placeholder (`scripts/poll-pr-reviews.sh:276`). Timestamp filtering uses lexicographic ISO comparison, so callers must provide comparable ISO 8601 timestamps (`scripts/poll-pr-reviews.sh:300`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help and missing-arg validation. `tests/test-pr-loop-hooks.sh` covers approval-only review inclusion and `--after` filtering. `tests/robustness/test-pr-loop-api-robustness.sh` covers valid JSON output, slow API, and API failure returning boolean `has_new_comments:false`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-092 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable regression specification for `.humanize` escape prevention and shell-safe loop directory/state discovery patterns.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, then uses `git_adds_humanize` through `assert_blocks` and `assert_allows` to verify blocked and allowed `git add` command shapes (`tests/test-humanize-escape.sh:16`, `tests/test-humanize-escape.sh:44`, `tests/test-humanize-escape.sh:58`). It separately simulates find-based iteration patterns for empty dirs, dotfile-only dirs, no state files, state file discovery, and timestamped session directory selection (`tests/test-humanize-escape.sh:204`, `tests/test-humanize-escape.sh:225`, `tests/test-humanize-escape.sh:248`, `tests/test-humanize-escape.sh:303`).
- inputs_outputs_state: Inputs are shell command strings, temporary filesystem layouts under `/tmp`, and current working directory. Outputs are pass/fail counters and exit status 0 only if no failures (`tests/test-humanize-escape.sh:25`, `tests/test-humanize-escape.sh:344`). It creates and removes temporary directories for `.humanize` and session-layout simulations.
- gates_or_invariants: Blocks direct `.humanize`, `./.humanize`, nested paths ending in `.humanize`, quoted variants, forced broad adds, `-A/--all` when `.humanize` exists, chained commands, and `git -C` forms (`tests/test-humanize-escape.sh:83`, `tests/test-humanize-escape.sh:96`, `tests/test-humanize-escape.sh:109`, `tests/test-humanize-escape.sh:135`, `tests/test-humanize-escape.sh:153`, `tests/test-humanize-escape.sh:164`). Allows specific normal files, `.gitignore`, read-only git commands against `.humanize`, patch mode, and names that only start with `.humanize` but are not the directory (`tests/test-humanize-escape.sh:175`, `tests/test-humanize-escape.sh:184`, `tests/test-humanize-escape.sh:190`).
- dependencies_and_callers: Depends directly on `to_lower` and `git_adds_humanize` in `hooks/lib/loop-common.sh`. Its expected behavior maps to bash validators that protect loop state from accidental commits or broad staging.
- edge_cases_or_failure_modes: Explicitly guards against zsh/bash glob errors in empty or dotfile-only directories by using `find` instead of shell globs (`tests/test-humanize-escape.sh:201`). Ensures session detection ignores dotfiles like `.cancel-requested` and chooses the lexicographically latest timestamped session dir (`tests/test-humanize-escape.sh:308`, `tests/test-humanize-escape.sh:316`).
- validation_or_tests: This file is itself the validation. In this read-only research pass, it was inspected rather than executed because it creates `/tmp` directories and mutates local test fixtures.
- skip_candidate: `no`

### ASK_GEMINI-HZ-122 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR/PR-loop algorithm library used by validators, stop hooks, setup scripts, cancellation, Codex review logic, and monitoring (`hooks/lib/loop-common.sh:5`).
- algorithmic_behavior: Provides hook JSON validation, config-backed defaults, active-loop discovery, state frontmatter parsing/upserts, review issue extraction, protected file messaging, goal-tracker immutable-section checks, cancel authorization, PR-loop helpers, `.humanize` git-add protection, command mutation detection, loop termination, and PR goal tracker updates. Key functions include `validate_hook_input`, `find_active_loop`, `parse_state_file_strict`, `detect_review_issues`, `is_cancel_authorized`, `find_active_pr_loop`, `git_adds_humanize`, `command_modifies_file`, `end_loop`, and `update_pr_goal_tracker` (`hooks/lib/loop-common.sh:91`, `hooks/lib/loop-common.sh:307`, `hooks/lib/loop-common.sh:474`, `hooks/lib/loop-common.sh:671`, `hooks/lib/loop-common.sh:982`, `hooks/lib/loop-common.sh:1232`, `hooks/lib/loop-common.sh:1364`, `hooks/lib/loop-common.sh:1519`, `hooks/lib/loop-common.sh:1574`, `hooks/lib/loop-common.sh:1623`).
- inputs_outputs_state: Inputs include hook JSON, loop base dirs, loop state files, Codex review logs, file paths, bash command strings, PR bot JSON, and config files. Outputs include validated globals, parsed `STATE_*` variables, rendered block messages, state-file mutations via `upsert_state_fields` and `end_loop`, review result files, and PR goal-tracker updates. It loads merged config and defaults Codex model/effort/agent-team values with validation (`hooks/lib/loop-common.sh:185`, `hooks/lib/loop-common.sh:198`, `hooks/lib/loop-common.sh:209`, `hooks/lib/loop-common.sh:217`).
- gates_or_invariants: Hook input must reject null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, missing required tool fields, and too-deep JSON (`hooks/lib/loop-common.sh:91`, `hooks/lib/loop-common.sh:133`, `hooks/lib/loop-common.sh:151`). Active-loop discovery intentionally checks only the newest loop without a session filter and only the newest matching session with a filter to avoid reviving stale/zombie loops (`hooks/lib/loop-common.sh:289`, `hooks/lib/loop-common.sh:316`, `hooks/lib/loop-common.sh:333`). Strict state parsing requires frontmatter separators plus `current_round`, `max_iterations`, `review_started`, and `base_branch`, with numeric/boolean validation (`hooks/lib/loop-common.sh:482`, `hooks/lib/loop-common.sh:494`, `hooks/lib/loop-common.sh:512`, `hooks/lib/loop-common.sh:524`). Cancel authorization requires `.cancel-requested`, rejects substitutions/operators/remaining `$`, validates exact `mv` source/destination, and rejects symlinked sources (`hooks/lib/loop-common.sh:988`, `hooks/lib/loop-common.sh:993`, `hooks/lib/loop-common.sh:1112`, `hooks/lib/loop-common.sh:1128`).
- dependencies_and_callers: Sourced by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, `loop-codex-stop-hook.sh`, setup scripts, `humanize.sh`, `ask-codex.sh`, `bitlesson-select.sh`, PR loop setup/stop hooks, and `cancel-rlcr-loop.sh`. It depends on `jq`, `sed`, `awk`, `grep`, optional `iconv`, `perl` for edit previews, config loader, and template loader (`hooks/lib/loop-common.sh:179`, `hooks/lib/loop-common.sh:227`).
- edge_cases_or_failure_modes: Review issue detection returns 2 for missing/empty logs as a hard block, scans only last 50 lines for `[P?]` markers near line start to avoid false positives and huge-log issues (`hooks/lib/loop-common.sh:650`, `hooks/lib/loop-common.sh:676`, `hooks/lib/loop-common.sh:686`). `git_adds_humanize` lowercases input, splits shell segments, handles `git -C`, quotes, force/all combined flags, broad scopes, and `.gitignore` checks (`hooks/lib/loop-common.sh:1346`, `hooks/lib/loop-common.sh:1367`, `hooks/lib/loop-common.sh:1396`, `hooks/lib/loop-common.sh:1409`, `hooks/lib/loop-common.sh:1439`). PR goal tracker updates are idempotent by round plus reviewer, supporting multi-bot rounds (`hooks/lib/loop-common.sh:1639`, `hooks/lib/loop-common.sh:1660`).
- validation_or_tests: Covered by the assigned `tests/test-humanize-escape.sh` and `tests/robustness/test-hook-input-robustness.sh`, plus broader tests referenced in the tree: allowlist validators, bash validator patterns, Codex review merge, finalize phase, state naming, session id, PR loop system/hooks/scripts, unified Codex config, state/concurrent/session robustness, and cancel security robustness.
- skip_candidate: `no`

### ASK_GEMINI-HZ-152 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Block-message template enforcing PR-loop generated-file immutability.
- algorithmic_behavior: Renders the message used when a user or agent attempts to write generated `.humanize/pr-loop/round-*-pr-comment.md` or `round-*-prompt.md` files (`prompt-template/block/pr-loop-prompt-write.md:1`, `prompt-template/block/pr-loop-prompt-write.md:3`). It explains that these files are system-generated and read-only (`prompt-template/block/pr-loop-prompt-write.md:5`).
- inputs_outputs_state: Input is a call from `pr_loop_prompt_blocked_message`; output is a rendered markdown block message. It mutates no state. The loader path is `load_and_render_safe "$TEMPLATE_DIR" "block/pr-loop-prompt-write.md"` (`hooks/lib/loop-common.sh:1333`, `hooks/lib/loop-common.sh:1339`).
- gates_or_invariants: Preserves a critical invariant that PR-loop prompt/comment artifacts are generated by the loop system and must not be hand-edited. Related readonly detection covers `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` round files (`hooks/lib/loop-common.sh:1261`).
- dependencies_and_callers: Depends on template loader fallback behavior in `hooks/lib/loop-common.sh`. Used by write/edit validators through common block-message functions when PR-loop file protection triggers.
- edge_cases_or_failure_modes: The template mentions `pr-comment` and `prompt` specifically; the common readonly predicate protects additional generated PR-loop file types, so callers must rely on the predicate plus message rather than treating this template as an exhaustive list.
- validation_or_tests: `tests/test-template-references.sh` checks message-function template references, including `loop-common.sh` references. PR-loop hook tests exercise generated file protection indirectly.
- skip_candidate: `no`

### ASK_GEMINI-HZ-182 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt fragment appended to the next-round Claude prompt when RLCR is configured to push after every round.
- algorithmic_behavior: Contains a single instruction: when `--push-every-round` is enabled, the implementation agent must push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:2`). `loop-codex-stop-hook.sh` appends it only when `PUSH_EVERY_ROUND == true` (`hooks/loop-codex-stop-hook.sh:2063`, `hooks/loop-codex-stop-hook.sh:2069`).
- inputs_outputs_state: Input is the loop state/config field `push_every_round`; output is added text in `NEXT_PROMPT_FILE`. It does not mutate repository state itself; it changes the next-round instruction surface.
- gates_or_invariants: Ensures round prompts reflect the persistent push policy. If the template is unavailable, the stop hook falls back to "Also push your changes after committing" (`hooks/loop-codex-stop-hook.sh:2065`, `hooks/loop-codex-stop-hook.sh:2067`).
- dependencies_and_callers: Depends on template loading in the stop hook and the parsed `PUSH_EVERY_ROUND` value from RLCR state. The source field is defined as `FIELD_PUSH_EVERY_ROUND` in `loop-common.sh` (`hooks/lib/loop-common.sh:32`).
- edge_cases_or_failure_modes: The file has a leading blank line, so consumers append it as a paragraph separator. Behavior depends entirely on caller gating; if state parsing misreads `push_every_round`, the note may be omitted or incorrectly included.
- validation_or_tests: Template reference tests cover template availability. Push-every-round behavior is part of stop-hook prompt generation.
- skip_candidate: `no`

### ASK_GEMINI-HZ-212 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production hook input parsing and monitor resilience.
- algorithmic_behavior: Sources `loop-common.sh` and test helpers, creates an isolated test dir, then pipes JSON into real hook validators for Read, Write, and Bash tools (`tests/robustness/test-hook-input-robustness.sh:14`, `tests/robustness/test-hook-input-robustness.sh:35`, `tests/robustness/test-hook-input-robustness.sh:47`, `tests/robustness/test-hook-input-robustness.sh:59`). It tests malformed JSON, missing fields, long commands, special characters, Unicode paths, unknown tools, deep nesting, non-UTF8 bytes, and null-byte handling (`tests/robustness/test-hook-input-robustness.sh:71`, `tests/robustness/test-hook-input-robustness.sh:102`, `tests/robustness/test-hook-input-robustness.sh:117`, `tests/robustness/test-hook-input-robustness.sh:135`, `tests/robustness/test-hook-input-robustness.sh:149`, `tests/robustness/test-hook-input-robustness.sh:177`, `tests/robustness/test-hook-input-robustness.sh:201`, `tests/robustness/test-hook-input-robustness.sh:219`).
- inputs_outputs_state: Inputs are generated JSON payloads, temporary logs, temporary `.humanize/rlcr` session layouts, fake HOME/cache directories, and shimmed terminal functions. Outputs are pass/fail records through `print_test_summary` and test exit status (`tests/robustness/test-hook-input-robustness.sh:670`). The monitor integration tests generate temporary runner scripts and delete session directories to force graceful monitor exits (`tests/robustness/test-hook-input-robustness.sh:454`, `tests/robustness/test-hook-input-robustness.sh:531`, `tests/robustness/test-hook-input-robustness.sh:621`).
- gates_or_invariants: Valid tool JSON should pass for non-loop paths. Invalid JSON, missing `tool_name`, missing required `tool_input.file_path`, deep JSON, and non-UTF8 input must reject without signal crashes (`tests/robustness/test-hook-input-robustness.sh:80`, `tests/robustness/test-hook-input-robustness.sh:95`, `tests/robustness/test-hook-input-robustness.sh:110`, `tests/robustness/test-hook-input-robustness.sh:194`, `tests/robustness/test-hook-input-robustness.sh:210`). Monitor helpers must return pipe-delimited parse formats for goal tracker and git status (`tests/robustness/test-hook-input-robustness.sh:401`, `tests/robustness/test-hook-input-robustness.sh:415`). Monitor loop must exit with code below 128 under missing session, narrow terminal, and ANSI-log cases (`tests/robustness/test-hook-input-robustness.sh:481`, `tests/robustness/test-hook-input-robustness.sh:573`, `tests/robustness/test-hook-input-robustness.sh:660`).
- dependencies_and_callers: Depends on `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`, and `tests/test-helpers.sh`. It exercises `command_modifies_file` directly for sed-in-place and redirect patterns (`tests/robustness/test-hook-input-robustness.sh:351`, `tests/robustness/test-hook-input-robustness.sh:361`).
- edge_cases_or_failure_modes: Documents that Bash strips null bytes during command substitution, so the test only requires graceful handling below signal-exit range rather than strict detection (`tests/robustness/test-hook-input-robustness.sh:219`). Monitor tests simulate log deletion/growth, ANSI content, binary logs, rapid updates, very narrow and wide terminal widths (`tests/robustness/test-hook-input-robustness.sh:254`, `tests/robustness/test-hook-input-robustness.sh:268`, `tests/robustness/test-hook-input-robustness.sh:280`, `tests/robustness/test-hook-input-robustness.sh:293`, `tests/robustness/test-hook-input-robustness.sh:305`, `tests/robustness/test-hook-input-robustness.sh:317`).
- validation_or_tests: This file is itself the validation suite. It was inspected but not executed in this research pass because the provided branch export is read-only and the test writes temp fixtures, log files, and runner scripts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8/8 item evidence headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`