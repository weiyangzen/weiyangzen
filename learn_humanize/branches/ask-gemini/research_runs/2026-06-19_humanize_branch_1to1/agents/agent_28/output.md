# agent_28 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-028 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Specialized subagent prompt used by `gen-plan` to decide whether a user draft is semantically relevant to the current repository before creating a plan file. The file defines agent metadata (`name`, `description`, `model`, `tools`) at `agents/draft-relevance-checker.md:1-5` and the relevance contract at `agents/draft-relevance-checker.md:10-30`.
- algorithmic_behavior: The agent performs a lightweight repository exploration, then compares draft content against repository concepts, technologies, file paths, functions, or intended modifications. Its decision is deliberately permissive: if the draft could reasonably connect to the repository, it should return relevant. The required output is one of two machine-readable prefixes: `RELEVANT:` or `NOT_RELEVANT:` at `agents/draft-relevance-checker.md:27-30`.
- inputs_outputs_state: Input is the draft document content supplied by the invoking command. The agent may read repository docs and search files using `Read`, `Glob`, and `Grep` from the frontmatter. Output is a single verdict line with a brief explanation. It does not mutate repository state.
- gates_or_invariants: The gate is semantic relevance, not syntax matching. The prompt instructs the agent to be lenient, tolerate informal or multilingual drafts, focus on semantic relevance, and lean toward relevant when uncertain at `agents/draft-relevance-checker.md:31-36`.
- dependencies_and_callers: `commands/gen-plan.md` invokes `humanize:draft-relevance-checker` after IO validation and before creating the plan file; on `NOT_RELEVANT`, the command stops, and on `RELEVANT`, it copies the plan template and appends the draft (`commands/gen-plan.md:154-180`). `tests/test-gen-plan.sh` validates the agent name, model `haiku`, tools field, model validity, and English-only content (`tests/test-gen-plan.sh:250-287`, `tests/test-gen-plan.sh:522-552`).
- edge_cases_or_failure_modes: A false negative blocks plan creation even though downstream IO validation has already passed. A false positive allows unrelated content into a plan, but the prompt explicitly chooses this risk by requiring leniency. The agent can miss relevance if repository docs are absent or the draft references concepts indirectly.
- validation_or_tests: `tests/test-gen-plan.sh` covers existence and metadata shape, not the semantic classification behavior itself. The example outputs in the prompt provide only format examples (`agents/draft-relevance-checker.md:38-46`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-058 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime helper for PR loop orchestration. It queries GitHub reactions to determine whether Codex approved a PR body with `+1` or whether Claude acknowledged a trigger comment with an `eyes` reaction. The header defines purpose, commands, and exit-code contract at `scripts/check-bot-reactions.sh:3-16`.
- algorithmic_behavior: The script dispatches on `codex-thumbsup` or `claude-eyes` (`scripts/check-bot-reactions.sh:86-190`). For `codex-thumbsup`, it parses a PR number plus optional `--after`, resolves the correct base repository for fork PRs, fetches paginated issue reactions, filters for `chatgpt-codex-connector[bot]` with content `+1`, optionally rejects reactions older than the cutoff, and emits the matching reaction JSON (`scripts/check-bot-reactions.sh:92-187`). For `claude-eyes`, it parses a comment ID plus retry/delay/PR options, resolves the base repository when a PR number is supplied, loops with sleeps, fetches paginated comment reactions, filters for `claude[bot]` with content `eyes`, and emits the reaction JSON on success (`scripts/check-bot-reactions.sh:197-300`).
- inputs_outputs_state: Inputs are CLI arguments, GitHub CLI authentication/context, PR/comment IDs, optional timestamp cutoff, retry count, and retry delay. Output on success is JSON reaction data containing user/content/timestamp. Exit `0` means found, exit `1` means not found or timeout, and exit `2` means usage/API error. No local state is written.
- gates_or_invariants: Uses `set -euo pipefail` (`scripts/check-bot-reactions.sh:18`). Each GitHub operation is bounded by `GH_TIMEOUT=30` through `run_with_timeout` sourced from `scripts/portable-timeout.sh` (`scripts/check-bot-reactions.sh:24-33`). Fork PR support is an invariant: reactions are fetched from the PR base repository, found by checking current repo first and parent repo second (`scripts/check-bot-reactions.sh:119-150`, `scripts/check-bot-reactions.sh:232-262`). Pagination is required and merged with `jq -s 'add // []'` (`scripts/check-bot-reactions.sh:152-160`, `scripts/check-bot-reactions.sh:269-275`).
- dependencies_and_callers: Depends on `gh`, `jq`, network access, and `scripts/portable-timeout.sh`. `scripts/setup-pr-loop.sh` requires `claude-eyes` during setup when Claude is enabled and cleans up loop state if eyes verification fails (`scripts/setup-pr-loop.sh:564-600`). `hooks/pr-loop-stop-hook.sh` uses `codex-thumbsup` to remove Codex from active bots or approve a PR loop (`hooks/pr-loop-stop-hook.sh:665-717`) and uses `claude-eyes` as a blocking trigger acknowledgement gate (`hooks/pr-loop-stop-hook.sh:768-790`). The same hook also polls Codex `+1` during response monitoring (`hooks/pr-loop-stop-hook.sh:985-1004`).
- edge_cases_or_failure_modes: If current and parent repo resolution both fail, `PR_BASE_REPO` can fall back to an empty current repo, causing the API fetch to fail with exit `2`. `--after` comparison is lexicographic string comparison (`scripts/check-bot-reactions.sh:177-181`), so it assumes normalized ISO timestamps. Claude API fetch errors inside retry are silently retried (`scripts/check-bot-reactions.sh:273-277`), but Codex fetch errors fail immediately (`scripts/check-bot-reactions.sh:157-162`). Missing required arguments and duplicate positional IDs exit `2`.
- validation_or_tests: `tests/test-pr-loop-system.sh` and related PR-loop tests reference this helper. The assigned file itself encodes a clear operational contract through usage and exit codes. I did not run tests because this task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### ASK_GEMINI-HZ-088 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for error handling in `hooks/lib/template-loader.sh`. It asserts that missing templates, missing directories, empty content, strict shell mode, fallback rendering, directory validation, and malformed placeholders behave safely rather than crashing.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh`, resolves `TEMPLATE_DIR`, counts pass/fail results, and exits nonzero if any test fails (`tests/test-error-scenarios.sh:9-35`, `tests/test-error-scenarios.sh:208-226`). Tests 1-4 assert missing files/directories and empty render input return empty output with exit `0` (`tests/test-error-scenarios.sh:42-91`). Test 5 verifies regex-special characters in values render literally (`tests/test-error-scenarios.sh:93-106`). Test 6 runs a strict-mode subshell to confirm missing templates do not abort under `set -euo pipefail` (`tests/test-error-scenarios.sh:108-131`). Tests 7-10 cover safe fallback and template directory validation (`tests/test-error-scenarios.sh:133-180`). Tests 11-12 preserve empty and unclosed placeholders as literals (`tests/test-error-scenarios.sh:182-206`).
- inputs_outputs_state: Inputs are the repository template loader, template directory path, synthetic template strings, fallback strings, and invalid paths. Output is colored PASS/FAIL text plus an aggregate exit code. State is limited to shell variables; it creates no persistent fixtures.
- gates_or_invariants: Template loading must fail open to empty output for missing templates, while `load_and_render_safe` must supply fallback content. `render_template` must be single-pass and preserve unresolved malformed placeholders rather than raising errors. `validate_template_dir` must return success only for a real template directory.
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`, especially `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Those functions are implemented at `hooks/lib/template-loader.sh:26-237`. The behavior supports many hook callers that render block messages and prompts with fallbacks.
- edge_cases_or_failure_modes: Missing templates under strict mode are the primary failure target. Special regex metacharacters in values could corrupt replacement if substitution used regex replacement incorrectly, so the test guards literal rendering. Empty placeholder `{{}}` and unclosed `{{UNCLOSED` must remain unchanged, avoiding destructive partial substitutions.
- validation_or_tests: This file is itself the validation artifact. It overlaps with broader template coverage in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness tests under `tests/robustness/`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-118 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Comprehensive executable specification for the prompt-template system. It validates template directory structure, loading of every markdown template, placeholder syntax, rendering edge cases, fallback behavior, integration with real templates, and repeated rendering stress.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh`, derives `TEMPLATE_DIR`, and accumulates pass/fail/warning counters (`tests/test-templates-comprehensive.sh:13-59`). Section 1 requires subdirectories `block`, `codex`, and `claude` to exist (`tests/test-templates-comprehensive.sh:61-80`). Section 2 loads every `*.md` under the template tree and treats empty/whitespace-only templates as warnings or failures depending on load result (`tests/test-templates-comprehensive.sh:82-112`). Section 3 scans all templates for malformed double-brace placeholders, including extra braces, wrong brackets, spaces, and lowercase placeholder names (`tests/test-templates-comprehensive.sh:113-194`). Section 3.5 tests the malformed-placeholder regex detectors themselves (`tests/test-templates-comprehensive.sh:196-259`). Section 4 exercises render behavior across empty input, single/multiple lines, blank-line preservation, special characters, quotes, CJK-like values, markdown, long values, multiline values, start/end variables, repeated variables, and adjacent variables (`tests/test-templates-comprehensive.sh:261-439`). Section 5 checks fallback and directory validation (`tests/test-templates-comprehensive.sh:440-490`). Section 6 renders representative real templates with real variable sets (`tests/test-templates-comprehensive.sh:492-540`). Section 7 does 100 rapid calls and then loads/renders every template with dummy values (`tests/test-templates-comprehensive.sh:542-606`).
- inputs_outputs_state: Inputs are all markdown templates under `prompt-template/`, the template loader library, synthetic variables, temporary directories, and real template names. Outputs are PASS/FAIL/WARN lines and final process exit. Temporary test directories are created for malformed placeholder and incomplete directory checks and removed during the test (`tests/test-templates-comprehensive.sh:204-259`, `tests/test-templates-comprehensive.sh:481-490`).
- gates_or_invariants: Valid placeholder syntax is uppercase `{{VAR_NAME}}` only, matching the loader contract in `hooks/lib/template-loader.sh:7-13`. Rendering must be single-pass, preserve empty lines, handle shell/regex/markdown-like characters without crashing, and leave no unreplaced placeholders when dummy values are provided for every detected placeholder (`tests/test-templates-comprehensive.sh:575-597`). The script exits `1` on any failed test (`tests/test-templates-comprehensive.sh:619-625`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`. It validates templates consumed by loop validators, stop hooks, PR loop setup, PR loop stop hook, methodology analysis prompts, and next-round prompt assembly. The real-template integration checks cover `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:497-540`).
- edge_cases_or_failure_modes: The syntax scan can produce false positives for documentation-like examples, so the script explicitly disables a noisy single-brace check (`tests/test-templates-comprehensive.sh:160-164`). It treats CJK and emoji concerns conservatively with safe placeholder values rather than requiring terminal support for arbitrary Unicode (`tests/test-templates-comprehensive.sh:347-370`). Warnings for unreplaced placeholders do not fail the run, preserving flexibility for templates with intentional literals.
- validation_or_tests: This file is itself a CI-style validation suite for template behavior. It complements narrower tests in `tests/test-error-scenarios.sh`, `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and robustness tests.
- skip_candidate: `no`

### ASK_GEMINI-HZ-148 `file` `prompt-template/block/methodology-analysis-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template used when an agent attempts to modify `methodology-analysis-state.md`, which is reserved for loop-system control during the Methodology Analysis Phase.
- algorithmic_behavior: The template tells the agent the state file cannot be modified, explains that the phase runs before the loop fully exits, and redirects allowed work to spawning/reviewing methodology analysis, optionally filing an issue, and writing `methodology-analysis-done.md` (`prompt-template/block/methodology-analysis-state-file-modification.md:1-9`).
- inputs_outputs_state: It has no placeholders, so rendering takes no variables. Output is a markdown block message. It does not directly change state, but it protects the loop state transition that parks the active state as `methodology-analysis-state.md` until completion.
- gates_or_invariants: Invariant: `methodology-analysis-state.md` is system-managed and not agent-editable. The allowed completion signal is `methodology-analysis-done.md`, not editing the state file (`prompt-template/block/methodology-analysis-state-file-modification.md:3-9`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` wraps this template in `methodology_analysis_state_file_blocked_message` with a fallback (`hooks/lib/loop-common.sh:943-950`). `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` block direct writes/edits to this file (`hooks/loop-write-validator.sh:206-213`, `hooks/loop-edit-validator.sh:159-165`). `hooks/loop-bash-validator.sh` blocks bash commands that modify it unless the cancel authorization helper accepts the operation (`hooks/loop-bash-validator.sh:259-265`). The underlying phase transition is implemented by `hooks/lib/methodology-analysis.sh`, which renames active state to `methodology-analysis-state.md` and later renames it to a terminal state after done (`hooks/lib/methodology-analysis.sh:64-72`, `hooks/lib/methodology-analysis.sh:162-165`).
- edge_cases_or_failure_modes: If the template is missing, callers use a fallback string, but the detailed list of permitted methodology-phase actions is lost. If validators fail to detect a write path, an agent could corrupt the state handoff before terminalization.
- validation_or_tests: Covered indirectly by template load/syntax suites and by validator behavior around methodology-analysis state paths. The template contains no placeholders, so syntax risk is low.
- skip_candidate: `no`

### ASK_GEMINI-HZ-178 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Footer appended to the next-round Claude prompt, enforcing post-work completion behavior and forbidding unauthorized loop exit paths.
- algorithmic_behavior: The template appends a hard note that the agent must not exit by lying, editing loop state files, or executing `cancel-rlcr-loop`, then instructs the agent to optionally use `code-simplifier`, commit changes, and write a summary to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:1-9`).
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, rendered through the template loader. Output is markdown appended to the next prompt file. It influences future agent behavior rather than changing state itself.
- gates_or_invariants: The key invariant is that normal loop progression requires truthful completion artifacts, commits, and a round summary; loop state files and cancel scripts are not valid escape paths. The summary target must be substituted into the footer (`prompt-template/claude/next-round-footer.md:6-9`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` appends this footer using `load_and_render_safe`, with a shorter fallback if the template is missing, then appends task-tag routing and optional push instructions (`hooks/loop-codex-stop-hook.sh:2056-2070`). Rendering depends on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If `NEXT_SUMMARY_FILE` is missing or malformed upstream, the rendered prompt points the agent to the wrong summary path. If the template is unavailable, fallback still asks for commit and summary, but loses the explicit anti-tampering and optional simplifier instructions.
- validation_or_tests: Covered by the comprehensive template loader tests that load all templates, validate placeholder syntax, and render all templates with dummy placeholder values (`tests/test-templates-comprehensive.sh:90-107`, `tests/test-templates-comprehensive.sh:118-194`, `tests/test-templates-comprehensive.sh:563-606`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-208 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Security-focused executable specification for cancel authorization. It tests `is_cancel_authorized` from `hooks/lib/loop-common.sh` against valid cancel moves and a broad set of command/path injection attempts.
- algorithmic_behavior: The test creates a mock loop directory with `state.md`, uses `.cancel-requested` as the authorization signal, then calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` for positive and negative cases (`tests/robustness/test-cancel-security-robustness.sh:15-31`). Positive tests accept valid moves from `state.md` or `finalize-state.md` to `cancel-state.md`, with double quotes, single quotes, and unquoted paths (`tests/robustness/test-cancel-security-robustness.sh:39-89`). Negative tests reject missing signal files, command substitution, backticks, shell operators, wrong source/destination, extra args, newline injection, variable expansion, non-`mv` commands, mixed quote styles, IFS tricks, trailing spaces, nonstandard source names, symlinked state files, and path names containing `finalize` that previously risked substring misclassification (`tests/robustness/test-cancel-security-robustness.sh:99-405`).
- inputs_outputs_state: Inputs are generated command strings, a temporary loop directory, signal files, state/finalize files, and symlinks. Output is the shared PASS/FAIL summary from `tests/test-helpers.sh`, and the script exits with the helper summary code (`tests/robustness/test-cancel-security-robustness.sh:407-412`). Test state is isolated under `TEST_DIR`.
- gates_or_invariants: Cancel authorization requires `.cancel-requested`, command must be exactly `mv`, source must be loop `state.md`, `finalize-state.md`, or `methodology-analysis-state.md`, destination must be loop `cancel-state.md`, source must not be a symlink, and injection constructs must be rejected. The implementation documents these return-code categories and security rules at `hooks/lib/loop-common.sh:964-982`, validates signal/injection/operators at `hooks/lib/loop-common.sh:986-1025`, parses and rejects mixed quotes/extra args at `hooks/lib/loop-common.sh:1032-1110`, validates normalized source/destination paths at `hooks/lib/loop-common.sh:1112-1126`, and checks symlinks by exact source path at `hooks/lib/loop-common.sh:1128-1141`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`. Runtime callers include `hooks/loop-bash-validator.sh`, which permits otherwise-blocked state-file modifications only when `is_cancel_authorized` succeeds (`hooks/loop-bash-validator.sh:259-281`). The actual cancel script writes the signal and renames active state to `cancel-state.md`, including methodology-analysis state (`scripts/cancel-rlcr-loop.sh` references surfaced in search).
- edge_cases_or_failure_modes: The suite explicitly guards against shell injection via `$()`, backticks, `;`, `&&`, pipes, newlines, `${HOME}`, and `${IFS}`. It also guards path-bypass issues: wrong directory, wrong source filename, symlinked state/finalize files, `/./` normalization acceptance, mixed quotes rejection, and the regression where a loop directory containing `finalize` could bypass exact source classification (`tests/robustness/test-cancel-security-robustness.sh:265-405`).
- validation_or_tests: This file is the robustness validation. It complements the larger cancel signal suite in `tests/test-cancel-signal-file.sh`, which tests the same helper and broader cancel workflow paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`