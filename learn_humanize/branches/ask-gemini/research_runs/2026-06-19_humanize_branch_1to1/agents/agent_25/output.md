# agent_25 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-025 `directory` `skills/humanize-rlcr`
- cursor: `[_]`
- core_role: Codex skill packaging for the RLCR flow. Recursive inspection found this directory contains one child file only: `skills/humanize-rlcr/SKILL.md`. The directory therefore acts as the installable skill boundary for the RLCR algorithm entrypoint rather than a multi-file implementation subtree.
- algorithmic_behavior: The directory delegates all behavior to `SKILL.md`, which defines the required setup/work/stop-hook sequence. It coordinates with sibling runtime paths through the hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, especially `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, generated `.humanize/rlcr/<timestamp>/round-*-prompt.md`, generated summaries, and native Stop hooks.
- inputs_outputs_state: Input is the flow invocation arguments passed through `$ARGUMENTS` to `setup-rlcr-loop.sh` via `SKILL.md` lines 28-32. Runtime state is not stored in the skill directory; it is generated under `.humanize/rlcr/<timestamp>/` as prompt, summary, `state.md`, and `finalize-state.md` files per the child skill instructions at lines 40-45 and 71-75.
- gates_or_invariants: The directory-level invariant is that RLCR users enter through the skill and let native Humanize Stop hooks manage transitions. The child file explicitly forbids manual state edits, bypassing blocked hook results, ad-hoc `codex exec` / `codex review`, and non-generated source-of-truth files at `SKILL.md` lines 69-75.
- dependencies_and_callers: Consumed by the Codex skill/flow installer. It requires the Humanize runtime root to be hydrated into `{{HUMANIZE_RUNTIME_ROOT}}` and depends on native hook support, setup/cancel scripts, and generated loop files. Sibling/parent coordination is mostly through `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and `prompt-template/**`.
- edge_cases_or_failure_modes: If the runtime root placeholder is not hydrated, setup and cancel commands point at an invalid literal path. If setup exits non-zero, the flow must stop and report the error, per `SKILL.md` line 34. If the native Stop hook is unavailable or bypassed, most state, review, and completion gates described by the skill are not enforced.
- validation_or_tests: No directory-specific test file exists. Its behavior is indirectly exercised by RLCR setup/stop-hook tests such as `tests/test-task-tag-routing.sh`, which verifies generated RLCR prompts and stop-hook follow-up prompts, and by the child `SKILL.md` contract.
- skip_candidate: `no`

### ASK_GEMINI-HZ-055 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Stop-hook validator for required `## BitLesson Delta` summary sections. It enforces that RLCR round summaries declare whether reusable BitLesson knowledge was added, updated, or intentionally unchanged, and that the declaration matches the BitLesson file state.
- algorithmic_behavior: The script parses required CLI flags at lines 24-60 and rejects missing required arguments at lines 62-67. It verifies the summary file exists at lines 69-72, loads template rendering helpers at lines 74-76, and emits structured hook-block JSON through `block_exit` at lines 78-90. The core extractor uses `awk` to find a top-level `## BitLesson Delta` heading while ignoring headings inside fenced code blocks and HTML comments at lines 92-179. It then parses exactly one `Action:` value, normalizes it to lowercase, and allows only `none`, `add`, or `update` at lines 200-218.
- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` from lines 6-13. On success it exits `0` with no output at line 387. On validation failure it exits `0` with JSON containing `decision: block`, a rendered `reason`, and a `systemMessage` at lines 81-89. It reads, but does not mutate, the summary file, BitLesson file, and prompt templates.
- gates_or_invariants: A visible, non-comment, non-fenced `## BitLesson Delta` section is mandatory when invoked; missing section blocks at lines 181-198. Exactly one valid action is mandatory at lines 202-218. `Action: none` must not list concrete lesson IDs and may be blocked when the BitLesson file has no concrete lessons and `allow-empty-none` is not `true` at lines 242-267. `add` or `update` requires non-`NONE` IDs, non-placeholder `Notes:`, an existing BitLesson file, valid IDs matching `BL-YYYYMMDD-short-name`, and corresponding `Lesson ID:` entries in the BitLesson file at lines 269-384.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` for non-finalize rounds when `BITLESSON_REQUIRED=true` at lines 791-806. The stop hook derives and sanitizes `bitlesson_file` and `bitlesson_allow_empty_none` from state frontmatter at `hooks/loop-codex-stop-hook.sh` lines 101-153. The script depends on `jq`, `awk`, `sed`, `tr`, Bash arrays, and `hooks/lib/template-loader.sh`; missing templates fall back through `load_and_render_safe` in `hooks/lib/template-loader.sh` lines 185-210.
- edge_cases_or_failure_modes: Headings inside fenced Markdown or HTML comments are intentionally ignored by the extractor. Multiple or missing `Action:` fields block. `Lesson IDs:` is accepted as a fallback spelling when `Lesson ID(s):` is absent at lines 220-223. IDs are comma-split only, so semicolon/newline-separated IDs do not form multiple concrete IDs. Placeholder notes in square or angle brackets block at lines 281-296. If `jq` is missing, `block_exit` will fail because JSON emission depends on it.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` directly covers empty/placeholder notes, fenced/comment-hidden deltas, valid `add`, and normal visible sections at lines 101-152. The stop-hook integration path is visible at `hooks/loop-codex-stop-hook.sh` lines 794-806. Related templates include `prompt-template/block/bitlesson-delta-*.md`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-085 `file` `tests/test-config-error-handling.sh`
- cursor: `[_]`
- core_role: Executable specification for configuration-loading fault tolerance. It defines which config errors are fatal and which fall back to defaults for the Humanize/RLCR configuration layer.
- algorithmic_behavior: The test sources `scripts/lib/config-loader.sh` at lines 20-33, then drives `load_merged_config` and `get_config_value` across six scenarios. It verifies missing required default config fails at lines 40-53, malformed project config warns and falls back at lines 59-81, malformed user config warns and falls back at lines 87-110, empty project config uses defaults at lines 116-128, missing project config is non-fatal at lines 134-150, and missing user config is non-fatal at lines 156-172.
- inputs_outputs_state: Inputs are temporary fake plugin/project directories, project `.humanize/config.json`, optional XDG user config at `$XDG_CONFIG_HOME/humanize/config.json`, and the real repo default config. Outputs are pass/fail records from `tests/test-helpers.sh` and the final summary at line 174. The tests create temp files and repos when run, but this research pass did not execute them because the assigned export is read-only.
- gates_or_invariants: Required default config must exist and be a JSON object. Optional user/project config may be missing or malformed without aborting, but malformed optional config must emit a warning. Effective fallback value for `bitlesson_model` must be `haiku`, matching `config/default_config.json` lines 1-8 and asserted repeatedly at test lines 76-80, 105-109, 123-127, 141-146, and 163-168.
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary`. It validates `scripts/lib/config-loader.sh`, whose `_config_loader_prepare_layer` treats required missing/malformed layers as fatal and optional missing/malformed layers as `{}` with warnings at lines 24-61, and whose merge precedence is default then user then project at lines 113-132.
- edge_cases_or_failure_modes: If `jq` is unavailable, `load_merged_config` fails before these scenarios because `_config_loader_require_jq` enforces it at `scripts/lib/config-loader.sh` lines 17-22. The test intentionally captures stderr with `2>&1 >/dev/null`; it only requires broad warning keywords, not exact text, at lines 67 and 96. It does not test non-object JSON arrays for optional configs, though the loader treats them as malformed.
- validation_or_tests: This file is itself the validation. It is part of the broader shell test suite under `tests/`. It was inspected but not run in this read-only branch export.
- skip_candidate: `no`

### ASK_GEMINI-HZ-115 `file` `tests/test-task-tag-routing.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR task routing metadata. It ensures implementation tasks carry `coding` or `analyze` tags and that generated prompts preserve instructions for delegating analyze work through `/humanize:ask-codex`.
- algorithmic_behavior: The test creates a mock `codex` executable at lines 25-50 and fixture git repos/plans at lines 52-69. First, it runs `scripts/setup-rlcr-loop.sh` with a plan containing `coding` and `analyze` task tags at lines 75-95, then asserts that `round-0-prompt.md` includes `## Task Tag Routing (MUST FOLLOW)` and `/humanize:ask-codex` at lines 96-109. It also asserts the generated goal tracker includes `Tag` and `Owner` columns at lines 111-115. Second, it builds a stop-hook fixture with state, goal tracker, contract, and summary at lines 121-210, runs `hooks/loop-codex-stop-hook.sh`, and verifies `round-1-prompt.md` includes the routing reminder and ask-codex instruction at lines 212-237.
- inputs_outputs_state: Inputs include synthetic plans with a `Tag (coding/analyze)` column, a mock `codex` binary, fixture `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-0-contract.md`, `round-0-summary.md`, and hook JSON input at line 223. Outputs are generated RLCR prompt/tracker files and test pass/fail status.
- gates_or_invariants: Round-0 prompts must include routing rules from `scripts/setup-rlcr-loop.sh` lines 1342-1349. Goal tracker Active Tasks must include `| Task | Target AC | Status | Tag | Owner | Notes |`, generated by `scripts/setup-rlcr-loop.sh` lines 1135-1139. Follow-up prompts must append the stop-hook reminder from `hooks/loop-codex-stop-hook.sh` lines 1301-1314. The routing invariant is `coding -> Claude` and `analyze -> /humanize:ask-codex` plus owner metadata alignment.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `tests/test-helpers.sh`, Git test repo helpers, and a mock `codex` placed at the front of `PATH`. Related plan-generation templates also define the tag contract in `prompt-template/plan/gen-plan-template.md`, referenced by `rg` results.
- edge_cases_or_failure_modes: The mock `codex` only supports `exec` and `review`; unexpected subcommands fail at lines 42-47. The stop-hook fixture omits `codex_model` intentionally and relies on config defaults at lines 153-156. The assertions check literal prompt text, so copy changes in setup/stop-hook templates can fail tests even if behavior remains semantically equivalent.
- validation_or_tests: This file is itself the regression test for setup-time and stop-hook-time routing surfaces. It was inspected but not run in this read-only branch export.
- skip_candidate: `no`

### ASK_GEMINI-HZ-145 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the RLCR Stop hook when changed tracked/new files exceed the configured line-count threshold. It converts a mechanical file-size gate into actionable user instructions.
- algorithmic_behavior: The template renders `{{MAX_LINES}}` and `{{LARGE_FILES}}` into a block message at lines 1-4, explains maintainability/review risks at lines 6-9, and gives separate required actions for code and documentation files at lines 11-24. The final instruction requires committing the split changes before attempting exit again at line 25.
- inputs_outputs_state: Inputs are template variables supplied by the stop hook: `MAX_LINES` and `LARGE_FILES`. Output is Markdown prompt text embedded as the `reason` in a Stop-hook JSON block response. The template itself has no state and no side effects.
- gates_or_invariants: The caller sets `MAX_LINES=2000` at `hooks/loop-codex-stop-hook.sh` line 510, scans changed git-status paths, classifies code/documentation extensions at lines 543-554, counts lines at lines 556-560, and blocks when any file exceeds the threshold at lines 562-588. The invariant is that oversized code/docs files in the changed set must be split before loop exit.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` at `hooks/loop-codex-stop-hook.sh` lines 576-578. Rendering behavior comes from `hooks/lib/template-loader.sh`, especially single-pass placeholder replacement at lines 56-135 and safe fallback at lines 185-210.
- edge_cases_or_failure_modes: The stop hook scans `GIT_STATUS_CACHED`, so behavior depends on what that status variable contains before this gate. Deleted files are skipped at lines 534-537. Unsupported file extensions are ignored at lines 551-553. The rendered `LARGE_FILES` entries include absolute `$PROJECT_ROOT/$filename` paths at stop-hook lines 529-564, which may expose local paths in the block message.
- validation_or_tests: Located by `rg` as used only by `hooks/loop-codex-stop-hook.sh`. No direct template-specific test was assigned or found in the inspected paths; broader stop-hook/template tests likely cover template loading mechanics.
- skip_candidate: `no`

### ASK_GEMINI-HZ-175 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template for the degraded path where implementation completes but Codex code review is skipped. It defines the manual verification and summary contract before RLCR can finish.
- algorithmic_behavior: The template warns that code review was skipped and renders `{{REVIEW_SKIP_REASON}}` at lines 1-5. It requires manual verification steps at lines 7-13, permits optional simplification via `code-simplifier:code-simplifier` at lines 15-24, imposes non-negotiable behavior-preservation/test constraints at lines 26-34, references plan and goal tracker files at lines 35-39, and requires task completion, commit, and `{{FINALIZE_SUMMARY_FILE}}` at lines 40-51.
- inputs_outputs_state: Inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`, supplied by `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh` lines 1254-1260. Output is Markdown used as the Stop-hook block `reason`; the state transition itself is handled by the caller moving `state.md` to `finalize-state.md` at lines 1217-1224.
- gates_or_invariants: The finalize-skip invariant is that skipped review does not equal validated completion. The prompt requires manual checks and preserving existing functionality/tests. The caller enters this skipped path when `BASE_BRANCH` is empty, setting `REVIEW_SKIPPED=true` and reason `No base_branch configured for code review` at `hooks/loop-codex-stop-hook.sh` lines 1778-1787.
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-skipped-prompt.md"` at `hooks/loop-codex-stop-hook.sh` lines 1254-1260. Depends on `hooks/lib/template-loader.sh` rendering/fallback semantics. It coordinates with normal finalize prompt `prompt-template/claude/finalize-phase-prompt.md` through the same `enter_finalize_phase` function.
- edge_cases_or_failure_modes: If any render variable is missing, `render_template` keeps the placeholder unchanged per `hooks/lib/template-loader.sh` lines 7-13 and 115-121. The template text asks for tests “if possible” in the summary at lines 47-51, so enforcement of actual test execution is procedural rather than machine-validated here. If the template is missing/empty, the stop hook uses an inline fallback at lines 1227-1253.
- validation_or_tests: `rg` found the only direct caller in `hooks/loop-codex-stop-hook.sh`. No dedicated assigned test directly asserts this template; finalize behavior is covered more broadly by RLCR stop-hook/finalize tests in the suite.
- skip_candidate: `no`

### ASK_GEMINI-HZ-205 `file` `skills/humanize-rlcr/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction file defining the Codex-facing RLCR control protocol. It is the algorithmic contract for starting the loop, performing rounds, writing summaries, and yielding control to the native Stop hook.
- algorithmic_behavior: The frontmatter marks it as a non-user-invocable flow with model invocation disabled at lines 1-7. It defines the runtime root placeholder at lines 14-23, starts setup through `"{{HUMANIZE_RUNTIME_ROOT}}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at lines 26-34, and defines the repeated round algorithm at lines 36-49: read current prompt, implement, commit, write summary, stop/exit, then obey Stop-hook instructions if blocked.
- inputs_outputs_state: Inputs are `$ARGUMENTS` passed to setup, typically a plan file or options listed at lines 76-95. Outputs are commits plus required summary files: normal summaries at `.humanize/rlcr/<timestamp>/round-<N>-summary.md` and finalize summary at `.humanize/rlcr/<timestamp>/finalize-summary.md`, specified at lines 43-45. Loop state lives in generated `.humanize/rlcr/<timestamp>/` files, not in the skill file.
- gates_or_invariants: The file enumerates Stop-hook enforcement surfaces at lines 50-68: state/schema validation, branch consistency, plan integrity, incomplete Task/Todo blocking, git clean, push-every-round, summary presence, max iterations, full-alignment rounds, COMPLETE/STOP marker handling, review transition marker, code-review gating on `[P0-9]`, review failure/empty-output blocks, and open-question handling. Critical rules at lines 69-75 forbid manual state edits, bypassing hook blocks, ad-hoc Codex review/exec, and ignoring generated source-of-truth files.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, generated prompts/summaries, native Humanize Stop hooks, and Codex review/exec behavior. The option table at lines 80-97 maps skill-level flags to setup-script behavior, including model/effort, base branch, review cadence, skip implementation, push enforcement, and agent teams.
- edge_cases_or_failure_modes: Setup non-zero is terminal for the flow at line 34. If a worker edits `state.md`/`finalize-state.md` manually, the skill considers that invalid and the stop hook is expected to guard it. If the user runs ad-hoc `codex exec` or `codex review`, they can desynchronize generated prompts/reviews from hook-managed state, which is explicitly prohibited at line 73.
- validation_or_tests: Indirectly validated by setup and stop-hook tests, especially `tests/test-task-tag-routing.sh` for prompt generation and follow-up routing, plus broader RLCR tests under `tests/test-rlcr-*`/`tests/test-stop-hook-*` discovered in the repository listing. The file itself is an instruction contract rather than executable code.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique Item Evidence headings above; IDs are not repeated here to preserve exactly-once heading evidence.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`