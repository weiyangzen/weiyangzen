# agent_29 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-029 `file` `agents/plan-compliance-checker.md`
- cursor: `[_]`
- core_role: Agent prompt/policy for the RLCR pre-flight plan gate. It validates that a user-supplied implementation plan is relevant to the current repository and does not require branch switching before `/humanize:start-rlcr-loop` proceeds.
- algorithmic_behavior: The prompt defines two checks: repository relevance by inspecting docs, structure, technologies, paths, and plan substance at `agents/plan-compliance-checker.md:16-30`; branch-switch detection by scanning for branch checkout/switch/create/worktree instructions at `agents/plan-compliance-checker.md:31-46`. It is intentionally lenient on relevance and branch ambiguity, leaning toward pass unless clearly unsafe.
- inputs_outputs_state: Input is plan-file content plus repository context explored through `Read`, `Glob`, and `Grep` tools declared at `agents/plan-compliance-checker.md:4-5`. Output must be exactly one verdict line: `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` as specified at `agents/plan-compliance-checker.md:48-55`. It does not mutate repo state itself; its output gates setup.
- gates_or_invariants: Always output one verdict only, never zero or multiple; relevance false positives are avoided by "if in doubt ... PASS"; branch-switch false positives are also avoided unless there is a clear instruction. Safe patterns include file restore, negated branch-switch language, descriptive branch references, and `--base-branch` config at `agents/plan-compliance-checker.md:40-45`.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md` before setup. That command extracts a relative safe plan path, reads it, invokes `humanize:plan-compliance-checker`, and fail-closes malformed output at `commands/start-rlcr-loop.md:13-57`.
- edge_cases_or_failure_modes: Empty or near-empty plans can fail relevance; plans in any language are allowed; branch terms in descriptive or negated contexts should not fail. A malformed agent response causes the caller to stop rather than continue.
- validation_or_tests: No direct shell test file was assigned for this prompt, but its caller includes explicit parsing rules and fail-closed behavior in `commands/start-rlcr-loop.md:52-57`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-059 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop classifier that determines startup/re-review case from GitHub PR commits and bot review/comment activity. Its JSON drives whether the PR loop can process reviews immediately or must require a fresh trigger comment.
- algorithmic_behavior: Parses `<pr_number> --bots <bot1,bot2>` at `scripts/check-pr-reviewer-status.sh:49-85`; maps logical bot names to GitHub authors, with Codex mapped to `chatgpt-codex-connector[bot]` at `scripts/check-pr-reviewer-status.sh:91-100`; resolves current vs parent repo for fork PRs at `scripts/check-pr-reviewer-status.sh:113-140`; fetches latest commit, issue comments, inline review comments, and PR review submissions at `scripts/check-pr-reviewer-status.sh:142-168`; classifies each bot as commented, missing, or stale by comparing that bot's newest comment/review time to latest commit time at `scripts/check-pr-reviewer-status.sh:174-205`; emits case 1, 2, 3, 4, or 5 at `scripts/check-pr-reviewer-status.sh:211-245`.
- inputs_outputs_state: Inputs are GitHub CLI/API data, `jq`, `portable-timeout.sh`, PR number, and comma-separated bot names. Output is JSON with `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` at `scripts/check-pr-reviewer-status.sh:259-275`. It does not persist state directly, but callers copy the case and commit timestamps into PR-loop state.
- gates_or_invariants: Missing PR number or `--bots` exits with usage at `scripts/check-pr-reviewer-status.sh:75-85`. Fetching commit info is a hard error at `scripts/check-pr-reviewer-status.sh:142-148`. Comment fetch failures degrade to empty arrays. Staleness is per-bot, not global-newest, so one fresh bot does not hide another stale bot at `scripts/check-pr-reviewer-status.sh:192-200`.
- dependencies_and_callers: `scripts/setup-pr-loop.sh` calls it to seed `startup_case`, latest commit SHA/date, and trigger behavior at `scripts/setup-pr-loop.sh:437-447`. `hooks/pr-loop-stop-hook.sh` re-evaluates startup case after rounds at `hooks/pr-loop-stop-hook.sh:1562-1574`. The no-trigger gate uses these cases at `hooks/pr-loop-stop-hook.sh:620-747`.
- edge_cases_or_failure_modes: Fork PR lookup falls back from current repo to parent repo; unresolved repo falls back to current repo with a warning. Case 5 covers partial reviews plus newer commits. String timestamp comparison assumes ISO-8601 UTC timestamps. Empty bot list is not explicitly rejected after `--bots`; it would produce zero total bots and case behavior based on empty arrays.
- validation_or_tests: `tests/test-pr-loop-system.sh` validates case 1 no comments and case 2 partial comments at `tests/test-pr-loop-system.sh:152-193`, valid dynamic startup JSON at `tests/test-pr-loop-system.sh:661-675`, case 4 all-reviewed-stale behavior at `tests/test-pr-loop-system.sh:1318-1340`, and case 5 partial-stale behavior at `tests/test-pr-loop-system.sh:1347-1370`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-089 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR finalize-phase behavior, covering state transitions, hook gates, write/edit/bash/read validators, review failure behavior, and anti-drift state machine behavior.
- algorithmic_behavior: Builds an isolated git repo and loop directory with synthetic `state.md`, plan, goal tracker, and round contract at `tests/test-finalize-phase.sh:172-268`. It mocks `codex exec` and `codex review` in several modes at `tests/test-finalize-phase.sh:51-170`. It then exercises helper functions, validators, and `hooks/loop-codex-stop-hook.sh` through positive and negative scenarios.
- inputs_outputs_state: Inputs are mock Codex outputs, hook JSON payloads, transcript JSONL task/todo data, and synthetic loop state. Outputs are pass/fail counters and real filesystem state transitions inside the temp repo. Important state transitions include `state.md` to `finalize-state.md` on completion review success, `finalize-state.md` to `complete-state.md` on finalize completion, `state.md` to `maxiter-state.md` at max iterations, and `state.md` to `stop-state.md` under drift breaker.
- gates_or_invariants: Finalize state must be detected as active while complete state is not at `tests/test-finalize-phase.sh:324-361`. `finalize-summary.md` writes are allowed but `finalize-state.md` and round contracts are blocked through Write/Edit/Bash validators at `tests/test-finalize-phase.sh:379-480`. Finalize exit requires summary, clean git, and completed todos at `tests/test-finalize-phase.sh:494-569` and `tests/test-finalize-phase.sh:770-812`. Finalize must not invoke Codex at `tests/test-finalize-phase.sh:511-517` and `tests/test-finalize-phase.sh:563-569`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-finalize-phase.sh:48-49`; invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. It constrains implementation sections such as active state resolution in `hooks/lib/loop-common.sh:245-365`, finalize protection helpers at `hooks/lib/loop-common.sh:799-821`, and stop-hook phase detection at `hooks/loop-codex-stop-hook.sh:71-88`.
- edge_cases_or_failure_modes: Codex review non-zero exit must block and preserve `state.md` with `review_started: true` at `tests/test-finalize-phase.sh:630-688`. Empty review stdout must also block, create an empty review log, and preserve review state at `tests/test-finalize-phase.sh:690-767`. Max iterations bypass finalize. Normal non-complete rounds remain unaffected, increment the round, write review result, and include feedback at `tests/test-finalize-phase.sh:814-887`.
- validation_or_tests: The file is itself the validation spec. It also tests drift recovery: two stalled rounds generate a drift recovery prompt and set `drift_status: replan_required`, missing verdict blocks without state mutation, and third stalled/regressed round creates `stop-state.md` at `tests/test-finalize-phase.sh:889-1039`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-119 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable contract for `hooks/check-todos-from-transcript.py`, the stop-hook pre-review gate that blocks exit while unfinished mainline/blocking work remains.
- algorithmic_behavior: Runs Python checker with hook JSON on stdin and synthesized transcript/task files. It covers input parsing, legacy `TodoWrite` transcript parsing, latest-todo selection, direct tool-use/message transcript variants, and file-based Task system reads.
- inputs_outputs_state: Inputs include hook JSON with optional `transcript_path`, `session_id`, and `tasks_base_dir`; transcript JSONL; and task JSON files under a mock tasks directory. Outputs are process exit codes: invalid hook JSON exits 2; incomplete work exits 1 with `INCOMPLETE_TODOS`; all complete/no data exits 0. The checker implementation declares this contract at `hooks/check-todos-from-transcript.py:9-15`.
- gates_or_invariants: Missing transcript path, nonexistent transcript, empty input, empty transcript, and nonexistent task session do not block at `tests/test-todo-checker.sh:64-99`, `tests/test-todo-checker.sh:197-225`, and `tests/test-todo-checker.sh:526-536`. For legacy todos, only the latest `TodoWrite` list matters at `tests/test-todo-checker.sh:227-241`. `pending`, `in_progress`, missing status, and empty content are incomplete unless the task is lane-prefixed as queued at the beginning.
- dependencies_and_callers: Directly tests `hooks/check-todos-from-transcript.py`; `hooks/loop-codex-stop-hook.sh` invokes that checker before expensive review at `hooks/loop-codex-stop-hook.sh:387-443`. The Python checker combines task-directory results and transcript results at `hooks/check-todos-from-transcript.py:186-199`.
- edge_cases_or_failure_modes: Invalid JSONL transcript lines are ignored, not fatal. Direct `tool_use` entries and alternate `message` format are accepted at `tests/test-todo-checker.sh:243-271`. Lane tag matching is intentionally prefix-only: a queued tag in the middle of content must not downgrade a blocker at `tests/test-todo-checker.sh:175-188` and `tests/test-todo-checker.sh:424-439`. Deleted task JSON files are ignored at `tests/test-todo-checker.sh:481-496`.
- validation_or_tests: Test groups cover input handling, todo detection, transcript variations, legacy edge cases, and new Task system behavior. The checker implementation mirrors these expectations with `LANE_PREFIX_PATTERN` at `hooks/check-todos-from-transcript.py:24-35`, TodoWrite parsing at `hooks/check-todos-from-transcript.py:73-120`, task directory parsing at `hooks/check-todos-from-transcript.py:123-170`, and formatted blocking output at `hooks/check-todos-from-transcript.py:204-221`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-149 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Block-message template for the PR-loop trigger-comment gate. It turns the algorithmic `REQUIRE_TRIGGER` decision into actionable hook output when no qualifying bot mention exists.
- algorithmic_behavior: Renders a short explanation that the PR loop is waiting for a trigger comment, includes startup case and case description, names the current round, and shows an example comment containing the bot mention string at `prompt-template/block/no-trigger-comment.md:1-17`.
- inputs_outputs_state: Template inputs are `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING`. Output is markdown embedded into a JSON block decision by the PR stop hook; it does not mutate state.
- gates_or_invariants: The template is only rendered when `REQUIRE_TRIGGER=true` and `PR_LAST_TRIGGER_AT` is empty. The hook computes this for subsequent rounds, new commits during polling, and startup cases 4/5 at `hooks/pr-loop-stop-hook.sh:620-644`, then blocks at `hooks/pr-loop-stop-hook.sh:728-747`.
- dependencies_and_callers: Called through `load_and_render_safe` in `hooks/pr-loop-stop-hook.sh:737-742`. Its case values come from PR-loop state seeded by `scripts/setup-pr-loop.sh:437-447` and refreshed by `hooks/pr-loop-stop-hook.sh:1562-1592`.
- edge_cases_or_failure_modes: If the template cannot load, the caller has an inline fallback at `hooks/pr-loop-stop-hook.sh:737-739`. It assumes the caller has already stripped stale/invalid trigger timestamps and computed a correct bot mention string.
- validation_or_tests: The assigned file has no direct standalone test, but trigger gating is tied to PR-loop status tests and stop-hook behavior. The caller intentionally validates trigger before Claude-eyes checks to avoid stale approvals at `hooks/pr-loop-stop-hook.sh:721-755`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-179 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Prompt template defining the next RLCR implementation round contract after Codex review feedback. It establishes the required re-anchor, task lanes, BitLesson flow, goal-tracker maintenance, and mainline guardrails before code changes resume.
- algorithmic_behavior: Requires re-reading the plan, goal tracker, and recent summaries/reviews; then writing a round contract before implementation at `prompt-template/claude/next-round-prompt.md:12-28`. Defines Task-system lane tags `[mainline]`, `[blocking]`, and `[queued]` with routing rules at `prompt-template/claude/next-round-prompt.md:29-46`. Injects Codex review content at `prompt-template/claude/next-round-prompt.md:48-51` and requires the mutable goal tracker to stay current at `prompt-template/claude/next-round-prompt.md:54-65`. Mainline guardrails prevent queued issues from taking over at `prompt-template/claude/next-round-prompt.md:67-75`.
- inputs_outputs_state: Inputs are rendered placeholders: `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `BITLESSON_FILE`, and `REVIEW_CONTENT`. Output is `round-N-prompt.md`, created by `hooks/loop-codex-stop-hook.sh:1961-1970`. It indirectly requires writing `round-N-contract.md` before implementation and updating the mutable section of `goal-tracker.md`.
- gates_or_invariants: Implementation must not start until the round contract exists. Exactly one mainline objective is required. Blocking work is allowed only if it truly blocks that objective; queued work must be documented but cannot replace mainline. Each task/sub-task must read the BitLesson file and run `bitlesson-selector`.
- dependencies_and_callers: Rendered by the RLCR stop hook when drift replan is not required at `hooks/loop-codex-stop-hook.sh:1961-1970`. The stop hook may append BitLesson enforcement if needed at `hooks/loop-codex-stop-hook.sh:1973-1985`, inject Agent Teams warning at `hooks/loop-codex-stop-hook.sh:1988-2010`, and inject open-question notice based on Codex review content at `hooks/loop-codex-stop-hook.sh:2012-2045`.
- edge_cases_or_failure_modes: If review output lacks clear classification, the next worker must classify findings into mainline gaps, blocking side issues, and queued side issues. If tracker reconciliation cannot be done safely, the prompt allows a "Goal Tracker Update Request" instead of silent mutation. Drift mode bypasses this template in favor of `claude/drift-replan-prompt.md`.
- validation_or_tests: `tests/test-finalize-phase.sh` verifies the normal/stalled round prompt path and drift alternative around `tests/test-finalize-phase.sh:848-887` and `tests/test-finalize-phase.sh:921-946`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-209 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for concurrent and malformed RLCR/PR state-file access. It constrains state parsing, active-loop detection, zombie-loop prevention, atomic write assumptions, strict parsing, and permission handling.
- algorithmic_behavior: Creates synthetic state files and loop directories, sources `hooks/lib/loop-common.sh`, then exercises `get_current_round`, `find_active_loop`, `find_active_pr_loop`, and `parse_state_file_strict` under edge and concurrency scenarios.
- inputs_outputs_state: Inputs are temporary `state.md`, `finalize-state.md`, `cancel-state.md`, PR loop state, and concurrent background writes. Outputs are pass/fail counters via shared test helpers. It mutates only temp test directories.
- gates_or_invariants: `get_current_round` must tolerate trailing whitespace, leading whitespace, CRLF, empty values, YAML comments, unicode content, zero, and large round values without crashing at `tests/robustness/test-concurrent-state-robustness.sh:59-160` and `tests/robustness/test-concurrent-state-robustness.sh:494-527`. Without a session filter, `find_active_loop` checks only the newest directory to prevent stale older loops from reviving, as asserted at `tests/robustness/test-concurrent-state-robustness.sh:170-240` and `tests/robustness/test-concurrent-state-robustness.sh:452-466`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` at `tests/robustness/test-concurrent-state-robustness.sh:15-18`. The implementation under test resolves active states and protects zombie loops in `hooks/lib/loop-common.sh:245-365`; `get_current_round` is implemented at `hooks/lib/loop-common.sh:368-381`; strict parser checks frontmatter and required fields at `hooks/lib/loop-common.sh:465-540`.
- edge_cases_or_failure_modes: Multiple concurrent reads must all observe the real nonzero round value at `tests/robustness/test-concurrent-state-robustness.sh:250-277`. Reads during atomic temp-file-then-rename writes must all return numeric values at `tests/robustness/test-concurrent-state-robustness.sh:279-322`. Malformed YAML and missing frontmatter must be rejected by strict parsing at `tests/robustness/test-concurrent-state-robustness.sh:332-371`. `cancel-state.md` is terminal and must not count as active at `tests/robustness/test-concurrent-state-robustness.sh:468-484`. Permission denial should degrade to empty or zero, not crash, at `tests/robustness/test-concurrent-state-robustness.sh:529-552`.
- validation_or_tests: The file is itself the validation spec. Related session-filter behavior is covered elsewhere, but this assigned test specifically establishes no-filter newest-directory behavior, finalize-state active detection, PR-loop detection, and atomic-read expectations.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7 unique item sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`