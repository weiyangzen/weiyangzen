# agent_09 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-009 `directory` `skills`
- cursor: `[_]`
- core_role: `skills/` is the installed skill entrypoint layer. It maps human/agent-facing flows to the repository runtime scripts, templates, and hook-managed state machines without implementing most of the shell logic itself.
- algorithmic_behavior: The directory contains six skill manifests:
  - `skills/humanize/SKILL.md`: umbrella Humanize workflow documentation and command map. It defines RLCR phases, PR-loop phases, gen-plan, ask-codex, goal tracker discipline, `.humanize/` state layout, and exit code contracts. See `skills/humanize/SKILL.md:30`, `skills/humanize/SKILL.md:32`, `skills/humanize/SKILL.md:50`, `skills/humanize/SKILL.md:62`, `skills/humanize/SKILL.md:188`, `skills/humanize/SKILL.md:216`, `skills/humanize/SKILL.md:255`.
  - `skills/ask-codex/SKILL.md`: one-shot Codex consultation wrapper. It requires quoted free-form input, routes to `scripts/ask-codex.sh`, and defines stdout/stderr and exit-code interpretation. See `skills/ask-codex/SKILL.md:14`, `skills/ask-codex/SKILL.md:18`, `skills/ask-codex/SKILL.md:38`, `skills/ask-codex/SKILL.md:46`.
  - `skills/ask-gemini/SKILL.md`: one-shot Gemini consultation wrapper with web-research expectation. It mirrors ask-codex quoting rules and routes to `scripts/ask-gemini.sh`. See `skills/ask-gemini/SKILL.md:10`, `skills/ask-gemini/SKILL.md:17`, `skills/ask-gemini/SKILL.md:21`, `skills/ask-gemini/SKILL.md:41`.
  - `skills/humanize-gen-plan/SKILL.md`: flow spec for draft-to-plan generation. Its Mermaid state machine gates on `scripts/validate-gen-plan-io.sh`, repository relevance, issue resolution, metric confirmation, plan generation, internal review, and language unification. See `skills/humanize-gen-plan/SKILL.md:19`, `skills/humanize-gen-plan/SKILL.md:49`, `skills/humanize-gen-plan/SKILL.md:55`, `skills/humanize-gen-plan/SKILL.md:96`.
  - `skills/humanize-refine-plan/SKILL.md`: flow spec for annotated-plan refinement. It gates on `scripts/validate-refine-plan-io.sh`, extracts `CMT:` / `ENDCMT` blocks, classifies comments, optionally asks the user, preserves gen-plan sections, generates QA from `prompt-template/plan/refine-plan-qa-template.md`, and writes atomically. See `skills/humanize-refine-plan/SKILL.md:18`, `skills/humanize-refine-plan/SKILL.md:50`, `skills/humanize-refine-plan/SKILL.md:67`, `skills/humanize-refine-plan/SKILL.md:88`, `skills/humanize-refine-plan/SKILL.md:131`.
  - `skills/humanize-rlcr/SKILL.md`: Codex entrypoint for RLCR using native Stop hooks. It defines the required round sequence: run setup, read generated prompt, implement, commit, write summary, exit normally, and let the Stop hook enforce transitions. See `skills/humanize-rlcr/SKILL.md:24`, `skills/humanize-rlcr/SKILL.md:36`, `skills/humanize-rlcr/SKILL.md:50`, `skills/humanize-rlcr/SKILL.md:69`.
- inputs_outputs_state: Inputs are skill arguments and runtime-root placeholders such as `{{HUMANIZE_RUNTIME_ROOT}}` or `${CLAUDE_PLUGIN_ROOT}`. Outputs are not direct file writes by the skill manifests; they delegate to scripts that create `.humanize/rlcr/<timestamp>/`, `.humanize/pr-loop/<timestamp>/`, `.humanize/skill/<timestamp>/`, generated plan files, QA files, or consultation outputs. State references are explicit in `skills/humanize/SKILL.md:218` and `skills/humanize-rlcr/SKILL.md:40`.
- gates_or_invariants: Key invariants include quoted shell argument handling for one-shot commands (`skills/ask-codex/SKILL.md:14`, `skills/ask-gemini/SKILL.md:17`), no direct ad-hoc replacement for Stop-hook phase transitions (`skills/humanize-rlcr/SKILL.md:71`), gen-plan schema preservation in refine-plan (`skills/humanize-refine-plan/SKILL.md:71`), task routing tags restricted to `coding` or `analyze` (`skills/humanize-refine-plan/SKILL.md:84`), and required summary/goal tracker discipline (`skills/humanize/SKILL.md:202`).
- dependencies_and_callers: The skills coordinate with `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/validate-refine-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/ask-gemini.sh`, `prompt-template/plan/refine-plan-qa-template.md`, and native hooks under `hooks/`. The manifests are installed/wired by the plugin installation path, and tests reference `humanize-refine-plan` installation in `tests/test-refine-plan.sh`.
- edge_cases_or_failure_modes: Missing runtime commands or non-zero script exits must be reported, not bypassed. `ask-*` skills warn against unquoted `$ARGUMENTS`, because shell metacharacters can break execution before the wrapper starts. RLCR explicitly forbids editing state files, skipping hook blocks, or running manual `codex exec` / `codex review` in place of managed transitions.
- validation_or_tests: Relevant tests include `tests/test-refine-plan.sh` for refine-plan skill/validator/template wiring, `tests/test-plan-file-validation.sh` for RLCR setup plan gates, `tests/test-bitlesson-select-routing.sh` for BitLesson selection routing, and Stop-hook/validator suites under `tests/test-stop-hook-legacy-compat.sh`, `tests/test-pr-loop-*.sh`, and `tests/robustness/`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-039 `file` `docs/bitlesson.md`
- cursor: `[_]`
- core_role: Behavior-defining documentation for the BitLesson knowledge-capture subsystem used by RLCR rounds.
- algorithmic_behavior: Defines model routing, initialization, lesson selection, and stop-gate delta validation. Config is read from default, user, project, and CLI layers (`docs/bitlesson.md:5`). Provider routing maps OpenAI-style model names to Codex and Claude-style names to Claude (`docs/bitlesson.md:14`). Codex-only installs force Codex/OpenAI routing before provider resolution (`docs/bitlesson.md:21`). RLCR startup initializes `.humanize/bitlesson.md`, runs `scripts/bitlesson-select.sh` per task/sub-task, records selected IDs or `NONE`, and requires a `## BitLesson Delta` in each round summary (`docs/bitlesson.md:25`).
- inputs_outputs_state: Inputs are merged config key `bitlesson_model`, provider binaries, `templates/bitlesson.md`, task/sub-task descriptions, related paths, `.humanize/bitlesson.md`, and round summaries. Outputs are selected lesson IDs, updated knowledge-base entries, and a summary delta block with `Action`, `Lesson ID(s)`, and `Notes` (`docs/bitlesson.md:36`).
- gates_or_invariants: `Action: none` requires `Lesson ID(s): NONE` or empty; `add`/`update` require concrete `BL-YYYYMMDD-short-name` IDs that exist in `.humanize/bitlesson.md`; `--require-bitlesson-entry-for-none` can forbid repeated empty-KB `none` deltas (`docs/bitlesson.md:47`).
- dependencies_and_callers: Implemented by `scripts/bitlesson-init.sh`, `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, `scripts/setup-rlcr-loop.sh`, and `hooks/loop-codex-stop-hook.sh`. Setup initializes the BitLesson file and records state fields at `scripts/setup-rlcr-loop.sh:867` and `scripts/setup-rlcr-loop.sh:906`. Stop hook calls the delta validator for non-finalize rounds when `bitlesson_required=true` at `hooks/loop-codex-stop-hook.sh:794`.
- edge_cases_or_failure_modes: Missing configured provider falls back to default Codex so the loop can proceed (`docs/bitlesson.md:19`). Codex-only mode prevents older Claude defaults such as `haiku` from routing to Claude on Codex-only installs (`docs/bitlesson.md:21`). Empty BitLesson knowledge bases may return `NONE`, unless stricter enforcement is enabled.
- validation_or_tests: Covered by `tests/test-bitlesson-select-routing.sh`, `tests/test-bitlesson-validate-delta.sh`, `tests/test-stop-hook-legacy-compat.sh`, and setup scaffold checks in `tests/test-unified-codex-config.sh` and `tests/robustness/test-setup-scripts-robustness.sh`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-069 `file` `scripts/validate-refine-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime validator for the refine-plan flow. It validates CLI arguments, annotated-plan input shape, required gen-plan sections, output writability, QA directory writability/creation, and reports stable exit codes 0-7.
- algorithmic_behavior: Uses two awk scanners plus a bash validation layer. `scan_cmt_blocks` walks the file as a state machine over normal text, fenced code, HTML comments, and active CMT blocks (`scripts/validate-refine-plan-io.sh:16`). It ignores markers inside fences and HTML comments, counts only non-empty blocks, rejects nested `CMT:`, stray `ENDCMT`, and missing `ENDCMT` with line/column/heading/context diagnostics (`scripts/validate-refine-plan-io.sh:48`, `scripts/validate-refine-plan-io.sh:168`, `scripts/validate-refine-plan-io.sh:226`, `scripts/validate-refine-plan-io.sh:235`). `scan_sections` similarly builds visible heading lines while excluding fenced, HTML-commented, and CMT regions (`scripts/validate-refine-plan-io.sh:245`, `scripts/validate-refine-plan-io.sh:305`, `scripts/validate-refine-plan-io.sh:367`). The main layer parses `--input`, `--output`, `--qa-dir`, `--discussion`, and `--direct`, rejects unknown flags, and makes `--discussion`/`--direct` mutually exclusive (`scripts/validate-refine-plan-io.sh:394`, `scripts/validate-refine-plan-io.sh:439`).
- inputs_outputs_state: Inputs are an annotated plan path, optional output path, optional QA directory, and mode flags. If `--output` is omitted, output defaults to input for in-place mode (`scripts/validate-refine-plan-io.sh:451`). Paths are normalized with `realpath -m` when available (`scripts/validate-refine-plan-io.sh:456`). Outputs are validation status lines, specific `VALIDATION_ERROR:*` markers, auto-created QA directory when missing, and exit code. It does not write the refined plan; it only prepares the IO gate.
- gates_or_invariants: Exit codes are declared at `scripts/validate-refine-plan-io.sh:4`: 1 missing input, 2 empty input, 3 no valid/malformed CMT, 4 missing required sections, 5 output/input-dir write failure, 6 QA-dir failure, 7 invalid args. Required sections are exact checks for `## Goal Description`, `## Acceptance Criteria`, `## Path Boundaries`, `## Feasibility Hints`, `## Dependencies and Sequence`, `## Task Breakdown`, `## Claude-Codex Deliberation`, `## Pending User Decisions`, and `## Implementation Notes` (`scripts/validate-refine-plan-io.sh:501`). It requires at least one valid non-empty CMT block (`scripts/validate-refine-plan-io.sh:484`) and excludes markers inside ignored regions (`scripts/validate-refine-plan-io.sh:495`).
- dependencies_and_callers: Called by `skills/humanize-refine-plan/SKILL.md:22` and documented/tested by the refine-plan command suite. Depends on POSIX-ish shell utilities: `awk`, `grep`, `realpath`, `dirname`, `mkdir`, `wc`, `tr`.
- edge_cases_or_failure_modes: Empty CMT blocks do not count; inline comments are accepted; code-fenced and HTML-comment markers are ignored; nested/stray/unclosed markers fail as malformed; required headings hidden inside fences/comments/CMT blocks do not satisfy schema; output directory must preexist for new-file mode; in-place mode requires input directory writable; QA directory is auto-created but a non-directory or non-writable path fails.
- validation_or_tests: `tests/test-refine-plan.sh` explicitly covers validator exit codes, ignored marker regions, empty/nested/unclosed CMTs, missing sections, output/QA directory errors, in-place and new-file success, and success count reporting. See `tests/test-refine-plan.sh:1044`, `tests/test-refine-plan.sh:1123`, `tests/test-refine-plan.sh:1156`, `tests/test-refine-plan.sh:1195`, `tests/test-refine-plan.sh:1222`, `tests/test-refine-plan.sh:1290`, `tests/test-refine-plan.sh:1328`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-099 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file, branch, CLI, and Codex-parameter validation behavior.
- algorithmic_behavior: Creates isolated temporary git repos, mocks `codex`, unsets `CLAUDE_PROJECT_DIR`, and invokes `scripts/setup-rlcr-loop.sh` with controlled plan paths and flags (`tests/test-plan-file-validation.sh:16`, `tests/test-plan-file-validation.sh:40`, `tests/test-plan-file-validation.sh:73`). It asserts rejection or acceptance by checking exit codes and diagnostic substrings.
- inputs_outputs_state: Inputs are generated plan files, git repo states, branch names, CLI flags, and mocked PATH. Outputs are PASS/FAIL/SKIP counters and process exit equal to failed test count (`tests/test-plan-file-validation.sh:742`). State is created under `mktemp -d` and removed by trap (`tests/test-plan-file-validation.sh:36`).
- gates_or_invariants: The test specifies that setup must reject absolute paths, missing files, missing parent dirs, paths or filenames with spaces, shell metacharacters, symlink files, inaccessible path resolution, `../` project escape, non-git repos, repos without commits, tracked plans unless `--track-plan-file`, untracked or dirty plans when `--track-plan-file` is used, YAML-unsafe branch names, blank/sparse/comment-only plans, conflicting `--plan-file` plus positional plan, and invalid Codex model/effort strings. See `tests/test-plan-file-validation.sh:88`, `tests/test-plan-file-validation.sh:115`, `tests/test-plan-file-validation.sh:127`, `tests/test-plan-file-validation.sh:168`, `tests/test-plan-file-validation.sh:188`, `tests/test-plan-file-validation.sh:244`, `tests/test-plan-file-validation.sh:273`, `tests/test-plan-file-validation.sh:328`, `tests/test-plan-file-validation.sh:431`, `tests/test-plan-file-validation.sh:517`, `tests/test-plan-file-validation.sh:683`, `tests/test-plan-file-validation.sh:700`.
- dependencies_and_callers: Exercises `scripts/setup-rlcr-loop.sh` production code. The corresponding implementation rejects absolute/unsafe paths at `scripts/setup-rlcr-loop.sh:462`, symlinks at `scripts/setup-rlcr-loop.sh:488`, parent symlinks at `scripts/setup-rlcr-loop.sh:501`, project escapes at `scripts/setup-rlcr-loop.sh:536`, submodules at `scripts/setup-rlcr-loop.sh:550`, tracking mismatches at `scripts/setup-rlcr-loop.sh:569`, insufficient content at `scripts/setup-rlcr-loop.sh:625`, branch/model YAML issues at `scripts/setup-rlcr-loop.sh:699`.
- edge_cases_or_failure_modes: Some branch-name tests accept either git rejection or script rejection. Permission-based path-resolution tests may skip if chmod cannot make the directory inaccessible (`tests/test-plan-file-validation.sh:225`). The “valid plan accepted” cases only assert absence of the specific validation error, because later setup may fail for unrelated runtime reasons.
- validation_or_tests: This is itself validation. It is complemented by `tests/robustness/test-plan-file-robustness.sh`, which stress-tests large files, line endings, file disappearance, and content-line counting through production setup.
- skip_candidate: `no`

### ASK_GEMINI-HZ-129 `file` `prompt-template/block/bitlesson-delta-missing.md`
- cursor: `[_]`
- core_role: Stop-hook block message template used when an RLCR round summary omits the required `## BitLesson Delta` section.
- algorithmic_behavior: Provides a minimal remediation contract: add a `## BitLesson Delta` section with `Action: none|add|update`, `Lesson ID(s): <IDs or NONE>`, and `Notes: <what changed and why>` (`prompt-template/block/bitlesson-delta-missing.md:1`).
- inputs_outputs_state: Input is the template loader request from `scripts/bitlesson-validate-delta.sh`; output is the block reason text embedded into a JSON decision. The validator uses this template via `load_and_render_safe` when delta detection fails (`scripts/bitlesson-validate-delta.sh:181`, `scripts/bitlesson-validate-delta.sh:196`).
- gates_or_invariants: The template encodes the section-name invariant and required field shape. It does not validate values itself; value validation happens later in `scripts/bitlesson-validate-delta.sh:202`, `scripts/bitlesson-validate-delta.sh:242`, and `scripts/bitlesson-validate-delta.sh:269`.
- dependencies_and_callers: Called through `hooks/loop-codex-stop-hook.sh:794`, which invokes `scripts/bitlesson-validate-delta.sh` for non-finalize rounds with BitLesson enforcement enabled. Setup enables that state for normal RLCR at `scripts/setup-rlcr-loop.sh:884` and stores `bitlesson_required` at `scripts/setup-rlcr-loop.sh:906`.
- edge_cases_or_failure_modes: If this template is missing or unreadable, the validator has an inline fallback with equivalent content (`scripts/bitlesson-validate-delta.sh:182`). A BitLesson Delta heading inside fenced code or HTML comments still counts as missing because the extractor ignores those regions (`scripts/bitlesson-validate-delta.sh:92`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` covers missing delta and ignored-region cases; `tests/test-template-references.sh` and template-loader tests cover template reference integrity.
- skip_candidate: `no`

### ASK_GEMINI-HZ-159 `file` `prompt-template/block/stop-hook-direct-execution.md`
- cursor: `[_]`
- core_role: Bash-validator block template preventing agents from manually executing hook/stop-gate scripts during active loops.
- algorithmic_behavior: Tells the worker that direct hook execution through Bash is blocked, hooks are managed automatically, and the correct transition is to finish work and end the response (`prompt-template/block/stop-hook-direct-execution.md:1`).
- inputs_outputs_state: Input is a blocked Bash command matching hook script execution patterns. Output is the stderr block message and validator exit 2. `hooks/lib/loop-common.sh` exposes `stop_hook_direct_execution_blocked_message`, loading this template with an inline fallback (`hooks/lib/loop-common.sh:1502`). `hooks/loop-bash-validator.sh` calls it when the command matches direct execution of `loop-codex-stop-hook.sh`, `pr-loop-stop-hook.sh`, or `rlcr-stop-gate.sh` even through wrappers such as `env`, `command`, `timeout`, `nice`, `nohup`, `strace`, shell invocation, source, or dot (`hooks/loop-bash-validator.sh:167`).
- gates_or_invariants: During an active RLCR or PR loop, workers must not manually run hook scripts to force phase transitions. The hook system owns stop-gate invocation. This protects state-machine integrity and prevents bypassing summary, git-clean, BitLesson, review, and PR polling gates.
- dependencies_and_callers: Depends on active-loop detection in `hooks/loop-bash-validator.sh:161` and shared template loading from `hooks/lib/loop-common.sh:1513`. Related RLCR flow rules also appear in `skills/humanize-rlcr/SKILL.md:71`.
- edge_cases_or_failure_modes: The matcher intentionally covers common wrappers and direct/relative/absolute script paths. It only runs when a loop is active; if no loop is active, Bash commands are allowed (`hooks/loop-bash-validator.sh:161`). If the template is unavailable, the fallback message is used.
- validation_or_tests: Covered by bash-validator and stop-hook tests, especially direct hook execution protection in hook-system and stop-gate test suites.
- skip_candidate: `no`

### ASK_GEMINI-HZ-189 `file` `prompt-template/plan/refine-plan-qa-template.md`
- cursor: `[_]`
- core_role: QA ledger template for the refine-plan flow. It defines the required evidence surface for comment processing, research, plan changes, unresolved decisions, and metadata.
- algorithmic_behavior: Structures output into `Summary`, `Comment Ledger`, `Answers`, `Research Findings`, `Plan Changes Applied`, `Remaining Decisions`, and `Refinement Metadata` (`prompt-template/plan/refine-plan-qa-template.md:1`). The ledger requires CMT ID, classification, location, original excerpt, and disposition (`prompt-template/plan/refine-plan-qa-template.md:7`). Separate sections capture answers for question comments, repo research findings, applied plan changes, cross-reference updates, pending decisions, and convergence metadata.
- inputs_outputs_state: Inputs are extracted raw comments, classifications (`question`, `change_request`, `research_request`), source locations, research scope/findings, applied changes, unresolved decisions, paths, counts, modified sections, convergence status, and date. Output is a QA markdown document generated alongside the refined plan. The refine-plan skill explicitly uses this template after plan validation succeeds (`skills/humanize-refine-plan/SKILL.md:41`).
- gates_or_invariants: The template enforces traceability: every `CMT-N` should have a disposition; research must record scope and findings; plan changes must list affected sections and cross-reference updates; metadata must include input/output/QA paths, counts by classification, modified sections, convergence status, and refinement date (`prompt-template/plan/refine-plan-qa-template.md:108`).
- dependencies_and_callers: Used by the refine-plan flow described in `skills/humanize-refine-plan/SKILL.md`. It is paired with `scripts/validate-refine-plan-io.sh`, which guarantees the input has valid CMT blocks and required gen-plan sections before QA generation.
- edge_cases_or_failure_modes: The template supports partial convergence through `Remaining Decisions` and `Convergence Status: <converged | partially_converged>`. It also supports deferred dispositions when user input or further clarification remains. It is not executable and does not itself enforce classification correctness; the command/flow and tests enforce that.
- validation_or_tests: `tests/test-refine-plan.sh` asserts this template exists and includes all required sections, ledger columns, path metadata, and convergence status fields (`tests/test-refine-plan.sh:805`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-219 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable specification for active RLCR session discovery under concurrent and corrupted session-directory scenarios.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and tests `find_active_loop` against synthetic `.humanize/rlcr`-like directory trees (`tests/robustness/test-session-robustness.sh:14`). The function under test returns the newest active loop directory or empty string (`hooks/lib/loop-common.sh:307`). Without a session filter, it checks only the single newest directory to prevent reviving stale older loops (`hooks/lib/loop-common.sh:316`). With a session filter, it scans newest-to-oldest for the matching session ID and returns only if that session’s newest loop is active (`hooks/lib/loop-common.sh:333`).
- inputs_outputs_state: Inputs are base directories containing session subdirectories and state-marker files: `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`. Output is either a session directory path or empty. Test output is PASS/FAIL summary via `print_test_summary` (`tests/robustness/test-session-robustness.sh:322`).
- gates_or_invariants: Active means `state.md` or `finalize-state.md` is resolvable; terminal sessions with only `complete-state.md` or `cancel-state.md` are ignored. The newest directory dominates; if newest lacks active state or is terminal, older active-looking directories must not be revived. The function checks immediate subdirectories only, not deeply nested paths.
- dependencies_and_callers: `find_active_loop` is shared by hook validators, stop hook, setup, and cancel scripts. Callers include `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `scripts/setup-rlcr-loop.sh`.
- edge_cases_or_failure_modes: Tests cover empty/missing base dirs, files without subdirs, missing state files, non-standard names, symlinked session dirs, spaces in names, deep paths, rapid creation, completed/cancelled sessions, mixed terminal/newer and active/older sessions, and many sessions with a performance threshold below 1000ms. See `tests/robustness/test-session-robustness.sh:33`, `tests/robustness/test-session-robustness.sh:89`, `tests/robustness/test-session-robustness.sh:128`, `tests/robustness/test-session-robustness.sh:194`, `tests/robustness/test-session-robustness.sh:224`, `tests/robustness/test-session-robustness.sh:259`, `tests/robustness/test-session-robustness.sh:287`.
- validation_or_tests: This file is the validation. It proves zombie-loop protection, finalize-phase detection, terminal-state ignoring, and resilience to unusual directory names.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item evidence sections present, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`