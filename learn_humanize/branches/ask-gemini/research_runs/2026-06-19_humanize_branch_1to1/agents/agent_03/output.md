# agent_03 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command workflow layer for plan generation/refinement, RLCR startup/cancel, and PR-loop startup/cancel. Recursively inspected all six files: `commands/cancel-pr-loop.md`, `commands/cancel-rlcr-loop.md`, `commands/gen-plan.md`, `commands/refine-plan.md`, `commands/start-pr-loop.md`, `commands/start-rlcr-loop.md`.
- algorithmic_behavior: `gen-plan.md` defines a strict phase pipeline from mode/config setup through IO validation, relevance check, Codex first-pass analysis, Claude plan synthesis, Claude/Codex convergence, pending-decision consolidation, final schema generation, optional translation, and optional RLCR start (`commands/gen-plan.md:32`, `commands/gen-plan.md:49`, `commands/gen-plan.md:62`, `commands/gen-plan.md:132`, `commands/gen-plan.md:154`, `commands/gen-plan.md:184`, `commands/gen-plan.md:255`, `commands/gen-plan.md:301`, `commands/gen-plan.md:372`, `commands/gen-plan.md:533`). `refine-plan.md` defines an annotated-plan refinement state machine: extract `CMT:` blocks, classify/process comments, preserve the gen-plan schema, produce QA, then atomically write outputs (`commands/refine-plan.md:30`, `commands/refine-plan.md:184`, `commands/refine-plan.md:261`, `commands/refine-plan.md:317`, `commands/refine-plan.md:380`, `commands/refine-plan.md:442`, `commands/refine-plan.md:493`). `start-rlcr-loop.md` gates loop startup through plan compliance and an advisory quiz before delegating to setup (`commands/start-rlcr-loop.md:13`, `commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:109`). `start-pr-loop.md` delegates PR-loop setup and documents review polling/approval mechanics (`commands/start-pr-loop.md:9`, `commands/start-pr-loop.md:15`). Cancel commands delegate state mutation to scripts, including finalize confirmation for RLCR (`commands/cancel-rlcr-loop.md:11`, `commands/cancel-rlcr-loop.md:24`; `commands/cancel-pr-loop.md:11`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, project config, draft/plan files, annotated comments, PR/RLCR loop state, and bot/Codex review output. Outputs are plan artifacts, QA ledgers, translated variants, loop directories/state files under `.humanize`, summaries, prompts, and user-facing status. State transitions are mostly delegated to scripts: RLCR setup creates active `state.md`; cancel scripts rename active state to cancel state; gen-plan may enter RLCR only when convergence and decision gates pass.
- gates_or_invariants: Command directory enforces planning-only for plan commands (`commands/gen-plan.md:20`, `commands/refine-plan.md:19`), sequential phase execution (`commands/gen-plan.md:34`, `commands/refine-plan.md:32`), no branch-switch plans before RLCR (`commands/start-rlcr-loop.md:48`), task routing tags limited to `coding`/`analyze` (`commands/gen-plan.md:449`, `commands/refine-plan.md:411`), and no manual state-file cheating (`commands/start-rlcr-loop.md:151`, `commands/start-pr-loop.md:49`).
- dependencies_and_callers: Commands call scripts under `${CLAUDE_PLUGIN_ROOT}/scripts`: `validate-gen-plan-io.sh`, `ask-codex.sh`, `setup-rlcr-loop.sh`, `validate-refine-plan-io.sh`, `setup-pr-loop.sh`, `cancel-rlcr-loop.sh`, and `cancel-pr-loop.sh` (`commands/gen-plan.md:5`, `commands/refine-plan.md:5`, `commands/start-pr-loop.md:4`, `commands/cancel-rlcr-loop.md:3`). They also depend on prompt templates such as `prompt-template/plan/gen-plan-template.md` and `prompt-template/plan/refine-plan-qa-template.md`.
- edge_cases_or_failure_modes: Mixed `--discussion`/`--direct` stops (`commands/gen-plan.md:56`, `commands/refine-plan.md:63`); invalid config can warn or stop depending on layer (`commands/gen-plan.md:74`, `commands/refine-plan.md:98`); Codex unavailable forces retry-or-Claude-only choice (`commands/gen-plan.md:208`); refine parse failures include nested CMT, stray ENDCMT, and missing ENDCMT (`commands/refine-plan.md:229`); RLCR compliance checker malformed output fails closed (`commands/start-rlcr-loop.md:52`); finalize cancellation needs explicit user confirmation (`commands/cancel-rlcr-loop.md:24`).
- validation_or_tests: Structural and validator coverage appears in `tests/test-gen-plan.sh`, `tests/test-refine-plan.sh`, `tests/test-plan-file-validation.sh`, `tests/test-pr-loop-system.sh`, and `tests/robustness/test-setup-scripts-robustness.sh`. I did not execute tests because this branch export is read-only and these suites create temp files/repos.
- skip_candidate: `no`

### ASK_GEMINI-HZ-033 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Primary plan-generation algorithm contract and RLCR auto-start gate. It turns a user draft into a structured implementation plan with acceptance criteria, path boundaries, dependency sequencing, routing tags, Claude/Codex deliberation history, and pending decisions (`commands/gen-plan.md:30`, `commands/gen-plan.md:376`).
- algorithmic_behavior: The workflow is explicitly ordered: mode setup, merged config, IO validation, relevance check, Codex first-pass analysis, Claude candidate plan, up to three Claude/Codex convergence rounds, issue/disagreement resolution, final plan generation, write/report (`commands/gen-plan.md:32`). Codex v1 must produce structured sections such as `CORE_RISKS`, `MISSING_REQUIREMENTS`, and `QUESTIONS_FOR_USER` (`commands/gen-plan.md:198`). The convergence loop runs a second Codex review with `AGREE`, `DISAGREE`, `REQUIRED_CHANGES`, and `UNRESOLVED`, then Claude revises and updates a convergence matrix (`commands/gen-plan.md:261`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, input draft, output path, merged config, Codex analysis, Claude analysis, and user answers. Outputs are the plan file, optional translated variant, convergence metadata, pending decisions, and optional RLCR setup invocation. It creates the output initially by copying `gen-plan-template.md` and appending the original draft (`commands/gen-plan.md:176`), then edits placeholders while preserving the draft appendix (`commands/gen-plan.md:537`).
- gates_or_invariants: No coding during planning (`commands/gen-plan.md:20`); phases must not be parallelized (`commands/gen-plan.md:34`); IO validator exit codes 1-7 are mapped to hard stops (`commands/gen-plan.md:140`); direct mode is always only `partially_converged` and cannot auto-start (`commands/gen-plan.md:257`); original draft content must be preserved and final plan must be a superset of it (`commands/gen-plan.md:303`, `commands/gen-plan.md:523`); every task needs exactly one `coding` or `analyze` tag (`commands/gen-plan.md:449`, `commands/gen-plan.md:529`).
- dependencies_and_callers: Allows and calls `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh`, and optionally `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/gen-plan.md:5`, `commands/gen-plan.md:137`, `commands/gen-plan.md:192`, `commands/gen-plan.md:613`). Reads config through `scripts/lib/config-loader.sh` semantics (`commands/gen-plan.md:64`). Uses `humanize:draft-relevance-checker` via Task (`commands/gen-plan.md:160`).
- edge_cases_or_failure_modes: Unsupported alternative language disables variant with a warning (`commands/gen-plan.md:118`); optional malformed config warns while malformed required default config stops (`commands/gen-plan.md:74`, `commands/gen-plan.md:126`); Codex failures trigger AskUserQuestion retry/continue choice (`commands/gen-plan.md:208`); unresolved decisions block auto-start because Phase 8 requires no `PENDING` decisions (`commands/gen-plan.md:596`); auto-start failure reports exact manual fallback (`commands/gen-plan.md:616`).
- validation_or_tests: `tests/test-gen-plan.sh` checks command presence, frontmatter, auto-start flag, direct-mode convergence behavior, Codex-first ordering, convergence limit, task tags, pending-decision consolidation, validator exit codes, and template parity. Related validator is `scripts/validate-gen-plan-io.sh`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-063 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Shared runtime timeout abstraction for macOS/Linux compatibility. It bounds external GitHub, git, Codex, Claude, Gemini, and test operations without each caller needing platform-specific timeout logic.
- algorithmic_behavior: `detect_timeout_impl` chooses the first available implementation in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` (`scripts/portable-timeout.sh:9`). `run_with_timeout` dispatches to that implementation, preserving the command argument vector via Bash arrays for native timeout implementations (`scripts/portable-timeout.sh:31`). Python fallback runs `subprocess.run(..., timeout=N)`, returns the child exit status, and maps `TimeoutExpired` to exit `124` to match GNU timeout (`scripts/portable-timeout.sh:47`).
- inputs_outputs_state: Input is `timeout_seconds command [args...]`; output is the command stdout/stderr and exit code. Script-level state is `TIMEOUT_IMPL`, computed at source time and exported (`scripts/portable-timeout.sh:29`, `scripts/portable-timeout.sh:73`).
- gates_or_invariants: GNU `timeout` is accepted only when `timeout --version` works, avoiding BSD/non-GNU ambiguity (`scripts/portable-timeout.sh:13`). Exit code `124` is the invariant timeout signal for native and Python paths (`scripts/portable-timeout.sh:56`). If no timeout exists, it explicitly warns and runs the command unbounded (`scripts/portable-timeout.sh:64`).
- dependencies_and_callers: Sourced by RLCR/PR setup and stop hooks, ask scripts, PR polling/check scripts, BitLesson selection, and robustness tests. Examples: `hooks/loop-codex-stop-hook.sh:51`, `hooks/pr-loop-stop-hook.sh:59`, `scripts/setup-rlcr-loop.sh:27`, `scripts/setup-pr-loop.sh:35`, `scripts/ask-codex.sh:29`, `scripts/ask-gemini.sh:30`, `tests/robustness/test-hook-system-robustness.sh:23`.
- edge_cases_or_failure_modes: Empty command arrays are not prevalidated; behavior falls through to the selected implementation. Python fallback interpolates `timeout_secs` directly into Python code, so callers must pass numeric timeout values. `none` mode is intentionally fail-open on timeout enforcement while warning to stderr.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` covers implementation detection, quick completion, timeout exit `124`, argument preservation, child exit propagation, pipelines, zero timeout, large output, rapid cycles, missing commands, empty command, special characters, trap behavior, and subshells. `tests/robustness/test-hook-system-robustness.sh:812` uses it to bound a large JSON validator call.
- skip_candidate: `no`

### ASK_GEMINI-HZ-093 `file` `tests/test-model-router.sh`
- cursor: `[_]`
- core_role: Executable specification for provider routing and effort normalization used by shared `scripts/lib/model-router.sh`, especially for BitLesson/model-provider selection.
- algorithmic_behavior: The test sources the router (`tests/test-model-router.sh:8`) and verifies `detect_provider` routes `gpt-*` and `o[0-9]*` names to `codex` (`tests/test-model-router.sh:37`, `tests/test-model-router.sh:54`, `tests/test-model-router.sh:71`, `tests/test-model-router.sh:88`, `tests/test-model-router.sh:105`), and Claude aliases/names to `claude` (`tests/test-model-router.sh:122`, `tests/test-model-router.sh:139`, `tests/test-model-router.sh:156`, `tests/test-model-router.sh:173`, `tests/test-model-router.sh:190`). It then verifies dependency checks with mock binaries (`tests/test-model-router.sh:239`, `tests/test-model-router.sh:273`) and effort mapping (`tests/test-model-router.sh:311`, `tests/test-model-router.sh:329`, `tests/test-model-router.sh:346`, `tests/test-model-router.sh:363`, `tests/test-model-router.sh:380`).
- inputs_outputs_state: Inputs are model strings, provider names, effort strings, and controlled `PATH` values. Outputs are provider names, normalized effort names, stderr diagnostics, and pass/fail summary from `print_test_summary` (`tests/test-model-router.sh:426`). Test state uses `setup_test_dir`, `SAFE_BASE_PATH`, and helper-created mock binaries (`tests/test-model-router.sh:10`, `tests/test-model-router.sh:17`).
- gates_or_invariants: Unknown and empty models must exit nonzero with an error (`tests/test-model-router.sh:198`, `tests/test-model-router.sh:216`). Missing provider binaries must fail with provider-specific stderr (`tests/test-model-router.sh:250`, `tests/test-model-router.sh:284`). `xhigh` maps to `high` only for Claude, with an info log (`tests/test-model-router.sh:300`); `xhigh` passes through for Codex (`tests/test-model-router.sh:337`). Unknown efforts fail for both providers (`tests/test-model-router.sh:388`, `tests/test-model-router.sh:405`).
- dependencies_and_callers: Tests implementation in `scripts/lib/model-router.sh`, whose core functions are `detect_provider`, `check_provider_dependency`, and `map_effort` (`scripts/lib/model-router.sh:10`, `scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:62`). Runtime caller found in `scripts/bitlesson-select.sh`, which detects provider and checks dependency before invoking Codex or Claude.
- edge_cases_or_failure_modes: Case-insensitive Claude detection is tested by `claude-3-OPUS-20240229` (`tests/test-model-router.sh:181`). The test deliberately isolates PATH with `/usr/bin:/bin:/usr/sbin:/sbin` to avoid false positives (`tests/test-model-router.sh:10`). Duplicate numbering comments around tests 10/10 are cosmetic and not algorithmic.
- validation_or_tests: This file is itself the validation artifact and is registered in `tests/run-all-tests.sh`. I inspected but did not run it due to read-only export constraints.
- skip_candidate: `no`

### ASK_GEMINI-HZ-123 `file` `hooks/lib/methodology-analysis.sh`
- cursor: `[_]`
- core_role: RLCR stop-hook sub-state machine for Methodology Analysis Phase, inserted before terminal loop exit so an independent analysis can produce methodology improvement suggestions.
- algorithmic_behavior: `enter_methodology_analysis_phase` checks privacy/re-entry/done guards, renames active `state.md` to `methodology-analysis-state.md`, records `.methodology-exit-reason`, creates a placeholder done file, renders a prompt, and emits a JSON block decision (`hooks/lib/methodology-analysis.sh:38`, `hooks/lib/methodology-analysis.sh:42`, `hooks/lib/methodology-analysis.sh:48`, `hooks/lib/methodology-analysis.sh:64`, `hooks/lib/methodology-analysis.sh:69`, `hooks/lib/methodology-analysis.sh:72`, `hooks/lib/methodology-analysis.sh:81`, `hooks/lib/methodology-analysis.sh:89`). `complete_methodology_analysis` requires non-empty done and report files, validates exit reason, renames methodology state to `<reason>-state.md`, and removes the marker (`hooks/lib/methodology-analysis.sh:114`, `hooks/lib/methodology-analysis.sh:118`, `hooks/lib/methodology-analysis.sh:129`, `hooks/lib/methodology-analysis.sh:142`, `hooks/lib/methodology-analysis.sh:152`, `hooks/lib/methodology-analysis.sh:162`, `hooks/lib/methodology-analysis.sh:167`). `block_methodology_analysis_incomplete` emits a JSON block with required completion steps (`hooks/lib/methodology-analysis.sh:180`).
- inputs_outputs_state: Inputs are exit reason (`complete`, `stop`, `maxiter`), exit description, `PRIVACY_MODE`, `STATE_FILE`, `LOOP_DIR`, round fields, and template directory. Outputs are renamed state files, `.methodology-exit-reason`, `methodology-analysis-done.md`, `methodology-analysis-report.md`, and JSON hook responses.
- gates_or_invariants: Privacy mode skips analysis (`hooks/lib/methodology-analysis.sh:42`); re-entry and already-done states are skipped (`hooks/lib/methodology-analysis.sh:48`, `hooks/lib/methodology-analysis.sh:54`); completion fails closed on empty done file, missing/empty report, missing marker, or invalid exit reason (`hooks/lib/methodology-analysis.sh:118`, `hooks/lib/methodology-analysis.sh:129`, `hooks/lib/methodology-analysis.sh:142`, `hooks/lib/methodology-analysis.sh:152`).
- dependencies_and_callers: Sourced by `hooks/loop-codex-stop-hook.sh:54`. Stop hook detects methodology phase (`hooks/loop-codex-stop-hook.sh:86`), handles completion before git-clean checks (`hooks/loop-codex-stop-hook.sh:603`), and enters methodology analysis on max iterations, finalize-complete, complete-at-max, and STOP/drift exits (`hooks/loop-codex-stop-hook.sh:901`, `hooks/loop-codex-stop-hook.sh:917`, `hooks/loop-codex-stop-hook.sh:1771`, `hooks/loop-codex-stop-hook.sh:1875`). It depends on `load_and_render_safe` from template loader/loop common and `jq` for JSON.
- edge_cases_or_failure_modes: If the placeholder done file remains empty, exit stays blocked. If the report is omitted or empty, completion blocks even if done marker exists. If `.methodology-exit-reason` is tampered with or absent, completion blocks rather than guessing terminal state. State rename via `mv` is not guarded for filesystem failure in this library, so caller context must treat any abnormal shell failure as hook failure.
- validation_or_tests: Related tests and guards appear across hook validators: `hooks/loop-edit-validator.sh:163`, `hooks/loop-bash-validator.sh:259`, and `hooks/lib/loop-common.sh:937` block methodology state modification. Template `prompt-template/claude/methodology-analysis-prompt.md` defines report/done obligations. Robustness coverage is in hook-system/state-transition/finalize tests.
- skip_candidate: `no`

### ASK_GEMINI-HZ-153 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Safety prompt block for PR-loop state-file mutation attempts. It is a small but core guard message template used by validators to explain why direct edits to PR-loop state are forbidden.
- algorithmic_behavior: The template states that `.humanize/pr-loop/state.md` is managed by the PR loop system and lists the fields whose integrity matters: round number, PR number/branch, active bots, Codex configuration, and polling settings (`prompt-template/block/pr-loop-state-modification.md:1`, `prompt-template/block/pr-loop-state-modification.md:3`, `prompt-template/block/pr-loop-state-modification.md:5`).
- inputs_outputs_state: Input is a validator decision to block a PR-loop state write/edit/bash mutation. Output is user-facing block text; it does not mutate state itself.
- gates_or_invariants: Invariant is that PR loop state cannot be modified manually because it would corrupt the loop state (`prompt-template/block/pr-loop-state-modification.md:12`).
- dependencies_and_callers: Loaded through template rendering helpers from hook validators when a path targets PR-loop `state.md`. Related implementation patterns are in `hooks/lib/loop-common.sh` template block loaders and PR hook validators; tests for PR loop state protection appear in PR-loop hook/system suites.
- edge_cases_or_failure_modes: Template is intentionally generic and does not mention bypasses or authorized cancel flows. If template loading fails, validators should fall back to hard-coded messages via `load_and_render_safe` patterns.
- validation_or_tests: `tests/test-template-references.sh` covers template reference presence broadly. PR-loop system and hook tests exercise state-file detection/cancellation behavior.
- skip_candidate: `no`

### ASK_GEMINI-HZ-183 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: RLCR Review Phase prompt contract. It directs Claude after Codex code review finds issues, preserving the mainline objective and classifying review findings before fixes.
- algorithmic_behavior: The prompt forces re-anchoring on the original plan, goal tracker, and round contract before code changes (`prompt-template/claude/review-phase-prompt.md:5`). It injects review content via `{{REVIEW_CONTENT}}` (`prompt-template/claude/review-phase-prompt.md:14`). It classifies findings as blocking or queued (`prompt-template/claude/review-phase-prompt.md:18`) and requires lane tags `[blocking]`/`[queued]` (`prompt-template/claude/review-phase-prompt.md:26`). Instructions then refresh the round contract, address blocking issues first, avoid unrelated features, commit, and write the summary (`prompt-template/claude/review-phase-prompt.md:34`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, `{{ROUND_CONTRACT_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{SUMMARY_FILE}}`. Outputs are an updated round contract, code fixes, commit, summary, and potentially goal tracker reconciliation notes.
- gates_or_invariants: Review findings do not automatically become the new mainline objective (`prompt-template/claude/review-phase-prompt.md:12`); queued issues must not take over the round (`prompt-template/claude/review-phase-prompt.md:24`); no new `[mainline]` task unless review proves previous objective incomplete (`prompt-template/claude/review-phase-prompt.md:32`); `COMPLETE` is ignored during review phase (`prompt-template/claude/review-phase-prompt.md:55`).
- dependencies_and_callers: Rendered by review-phase handling in `hooks/loop-codex-stop-hook.sh`, especially after implementation `COMPLETE` transitions to review phase and repeated review runs (`hooks/loop-codex-stop-hook.sh:1760`, `hooks/loop-codex-stop-hook.sh:1818`). It relies on state field `review_started` and `.review-phase-started` marker (`hooks/loop-codex-stop-hook.sh:1790`, `hooks/loop-codex-stop-hook.sh:1821`).
- edge_cases_or_failure_modes: Prevents review drift by requiring blocking/queued classification and stable mainline. If review phase is toggled manually without `.review-phase-started`, stop hook blocks as inconsistent state (`hooks/loop-codex-stop-hook.sh:1819`). If blocking issues remain, loop continues until no `[P0-9]` issues are found.
- validation_or_tests: Review/finalize behavior is covered by `tests/test-finalize-phase.sh`, `tests/test-codex-review-merge.sh`, PR/RLCR stop hook tests, and hook robustness checks for active review state.
- skip_candidate: `no`

### ASK_GEMINI-HZ-213 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for hook validators and stop hooks. It verifies state protection, JSON handling, command filtering, goal-tracker immutability rules, concurrency tolerance, and active-loop blocking.
- algorithmic_behavior: The suite sources loop-common and portable timeout (`tests/robustness/test-hook-system-robustness.sh:22`) then exercises edit validator behavior, plan-file validator behavior, state parser tolerance, bash/write/read validators, concurrent reads, stop hooks, and JSON edge cases. It builds synthetic `.humanize/rlcr` sessions with state frontmatter and goal trackers, then invokes hook scripts with JSON tool payloads (`tests/robustness/test-hook-system-robustness.sh:40`, `tests/robustness/test-hook-system-robustness.sh:165`, `tests/robustness/test-hook-system-robustness.sh:253`, `tests/robustness/test-hook-system-robustness.sh:317`, `tests/robustness/test-hook-system-robustness.sh:605`, `tests/robustness/test-hook-system-robustness.sh:645`, `tests/robustness/test-hook-system-robustness.sh:807`).
- inputs_outputs_state: Inputs are JSON hook payloads for `Edit`, `Write`, `Read`, and `Bash`; temporary project roots via `CLAUDE_PROJECT_DIR`; mock state files; mock git repos; and mock `codex`. Outputs are hook exit codes, JSON block/no-block decisions, renamed terminal state files, and pass/fail summary (`tests/robustness/test-hook-system-robustness.sh:855`).
- gates_or_invariants: Direct edits to RLCR `state.md` are blocked (`tests/robustness/test-hook-system-robustness.sh:53`), including traversal paths (`tests/robustness/test-hook-system-robustness.sh:124`). Plan-file edits are allowed only through the plan validator’s branch/tracked-file semantics (`tests/robustness/test-hook-system-robustness.sh:165`). Bash attempts to mutate `state.md` are blocked (`tests/robustness/test-hook-system-robustness.sh:317`). After round 0, bash writes to `goal-tracker.md` are blocked, mutable writes/edits are allowed, immutable-section changes are blocked, and old-session goal trackers are unreadable (`tests/robustness/test-hook-system-robustness.sh:403`, `tests/robustness/test-hook-system-robustness.sh:419`, `tests/robustness/test-hook-system-robustness.sh:465`, `tests/robustness/test-hook-system-robustness.sh:497`, `tests/robustness/test-hook-system-robustness.sh:525`, `tests/robustness/test-hook-system-robustness.sh:539`). Active loops must block exit (`tests/robustness/test-hook-system-robustness.sh:743`).
- dependencies_and_callers: Exercises `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/lib/loop-common.sh`, and `scripts/portable-timeout.sh`. It also requires `jq`, `git`, and a mocked `codex` for active-loop stop-hook behavior (`tests/robustness/test-hook-system-robustness.sh:776`).
- edge_cases_or_failure_modes: Malformed JSON must fail gracefully below signal/crash exits (`tests/robustness/test-hook-system-robustness.sh:80`); missing `file_path` may pass or fail gracefully (`tests/robustness/test-hook-system-robustness.sh:94`); malformed state can default or parse what it can (`tests/robustness/test-hook-system-robustness.sh:290`); missing critical state fields end loop unexpectedly or allow exit rather than crash (`tests/robustness/test-hook-system-robustness.sh:700`); large, null-containing, and deeply nested JSON must not crash (`tests/robustness/test-hook-system-robustness.sh:807`, `tests/robustness/test-hook-system-robustness.sh:821`, `tests/robustness/test-hook-system-robustness.sh:835`). Some dangerous unrelated shell commands are intentionally passed through because sandbox enforcement is out of scope (`tests/robustness/test-hook-system-robustness.sh:553`).
- validation_or_tests: This file is the validation artifact. I inspected it directly but did not run it because the branch export is read-only and the suite creates temp directories, git repos, and files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 unique assigned item headings above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`