# agent_13 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-013 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for the RLCR and PR-loop hook state machines. The directory has three files: `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/methodology-analysis.sh`. Together they provide hook input hardening, loop discovery, state-file parsing, transition helpers, path/file guards, prompt-template rendering, PR-loop tracker helpers, and the methodology-analysis pre-exit phase.
- algorithmic_behavior: `loop-common.sh` is the main coordinator. It defines canonical state fields and exit reasons at `hooks/lib/loop-common.sh:24`, validates hook JSON and rejects null bytes, invalid UTF-8, invalid JSON, or missing `tool_name` at `hooks/lib/loop-common.sh:91`, and rejects overly deep JSON at `hooks/lib/loop-common.sh:151`. It resolves active state files with priority `methodology-analysis-state.md`, `finalize-state.md`, then `state.md` at `hooks/lib/loop-common.sh:249`, and has zombie-loop protection in `find_active_loop` by considering only the newest unfiltered loop or the newest session-matching loop at `hooks/lib/loop-common.sh:307`. It parses YAML frontmatter in tolerant and strict modes at `hooks/lib/loop-common.sh:435` and `hooks/lib/loop-common.sh:474`; strict mode requires frontmatter, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric round/iteration fields, and boolean `review_started`. It implements review-output issue extraction by scanning only the final 50 log lines for `[P?]` markers at `hooks/lib/loop-common.sh:671`. It supplies file-protection predicates and prompt renderers for todos, prompts, state files, finalize-state, contracts, summaries, goal trackers, PR-loop state, PR-loop generated files, git-add protection, and direct hook execution blocks at `hooks/lib/loop-common.sh:770`, `hooks/lib/loop-common.sh:800`, `hooks/lib/loop-common.sh:1232`, `hooks/lib/loop-common.sh:1323`, and `hooks/lib/loop-common.sh:1502`. `template-loader.sh` implements template discovery, load, single-pass variable substitution, fallback rendering, append, and directory validation at `hooks/lib/template-loader.sh:26`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:56`, `hooks/lib/template-loader.sh:188`, and `hooks/lib/template-loader.sh:216`. `methodology-analysis.sh` adds a terminal-analysis state transition: it renames the active state file to `methodology-analysis-state.md`, records `.methodology-exit-reason`, creates a done placeholder, renders a block prompt, and later requires non-empty done and report artifacts before renaming to the final terminal state at `hooks/lib/methodology-analysis.sh:38` and `hooks/lib/methodology-analysis.sh:114`.
- inputs_outputs_state: Inputs include hook JSON from validators, `.humanize/rlcr/*/{state.md,finalize-state.md,methodology-analysis-state.md}`, `.humanize/pr-loop/*/state.md`, Codex review logs under `$CACHE_DIR`, template files under `prompt-template`, merged config from `scripts/lib/config-loader.sh`, and loop-session identifiers. Outputs include exported/assigned shell variables such as `VALIDATED_TOOL_NAME`, `STATE_CURRENT_ROUND`, `STATE_BASE_BRANCH`, normalized verdict/status values, rendered markdown block messages, JSON block objects through callers, updated state frontmatter via `upsert_state_fields`, renamed terminal state files through `end_loop`, and PR-loop goal-tracker updates through `update_pr_goal_tracker`.
- gates_or_invariants: The libraries fail closed on malformed hook JSON and malformed strict state files. Active loop selection intentionally avoids reviving stale older sessions. State-file path resolution prioritizes methodology and finalize phases before normal round state. Template rendering is single-pass so substituted values containing `{{...}}` are not recursively expanded (`hooks/lib/template-loader.sh:52`). Missing templates use fallback messages via `load_and_render_safe`, preventing hook crashes when prompt files are absent. Cancel authorization requires a `.cancel-requested` signal, exact `mv` shape, no shell operators, no command substitution, no extra args, exact state source and `cancel-state.md` destination, and non-symlink source (`hooks/lib/loop-common.sh:982`). `.humanize` git staging is guarded against direct paths, forced broad adds, and all-file adds (`hooks/lib/loop-common.sh:1364`). Goal-tracker immutable sections must remain unchanged after round 0 (`hooks/lib/loop-common.sh:880`).
- dependencies_and_callers: All main validators source `loop-common.sh`: `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. Setup and cancellation scripts also consume the same helpers through `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, and `scripts/cancel-rlcr-loop.sh`. `loop-common.sh` sources `scripts/lib/config-loader.sh` and `template-loader.sh` at `hooks/lib/loop-common.sh:179` and `hooks/lib/loop-common.sh:227`. `methodology-analysis.sh` is sourced by `hooks/loop-codex-stop-hook.sh` and depends on `load_and_render_safe` and `jq`.
- edge_cases_or_failure_modes: Bad JSON, missing `tool_name`, deep JSON, invalid UTF-8, missing strict-state fields, nonnumeric state values, malformed `review_started`, unsupported configured Codex model/effort, missing template subdirectories, missing/empty Codex review logs, stale terminal sessions, symlinked cancel source files, command-injection shapes in cancel commands, direct hook execution, and broad `.humanize` staging are all handled explicitly. `update_pr_goal_tracker` is idempotent per round plus reviewer and repairs partial tracker rows/log entries rather than double-counting (`hooks/lib/loop-common.sh:1639`).
- validation_or_tests: Covered by many executable specs: `tests/test-template-loader.sh`, `tests/robustness/test-template-error-robustness.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-session-id.sh`, `tests/robustness/test-state-file-robustness.sh`, `tests/robustness/test-state-transition-robustness.sh`, `tests/test-finalize-phase.sh`, `tests/test-pr-loop-system.sh`, and `tests/test-pr-loop-hooks.sh`. The assigned PR-loop and template robustness tests directly exercise key functions from this directory.
- skip_candidate: `no`

### ASK_GEMINI-HZ-043 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash validator for RLCR and PR-loop sessions. It prevents shell-command bypasses around the Write/Edit validators and enforces state-machine file ownership, phase restrictions, and protected generated artifacts.
- algorithmic_behavior: The script reads hook JSON from stdin, validates it with `validate_hook_input`, rejects overly deep JSON, and exits early unless `tool_name` is `Bash` (`hooks/loop-bash-validator.sh:24`, `hooks/loop-bash-validator.sh:31`, `hooks/loop-bash-validator.sh:38`). It extracts `.tool_input.command`, lowercases it, finds the session-aware active RLCR loop and newest active PR loop, then applies phase-specific checks (`hooks/loop-bash-validator.sh:47`, `hooks/loop-bash-validator.sh:54`, `hooks/loop-bash-validator.sh:61`, `hooks/loop-bash-validator.sh:65`). During methodology analysis it allows only leading `cancel-rlcr-loop.sh` and blocks git writes, file mutations, in-place edits, interpreters, shell-script launchers, build tools, source/dot commands, direct script execution, and file redirection (`hooks/loop-bash-validator.sh:87`). Outside methodology analysis, it blocks direct execution of stop hooks and stop gates through wrapper-aware regexes covering `env`, `command`, `timeout`, `nice`, `nohup`, `strace`, and shells (`hooks/loop-bash-validator.sh:172`). For active RLCR loops it parses strict state, blocks `git push` unless `push_every_round` is true, blocks `.humanize` staging, blocks state/finalize/methodology-state modification as destination, source, or shell-wrapper payload, and blocks Bash modifications to plan backup, goal tracker, prompt, summary, contract, and todos files (`hooks/loop-bash-validator.sh:197`, `hooks/loop-bash-validator.sh:216`, `hooks/loop-bash-validator.sh:235`, `hooks/loop-bash-validator.sh:259`, `hooks/loop-bash-validator.sh:484`, `hooks/loop-bash-validator.sh:497`, `hooks/loop-bash-validator.sh:512`, `hooks/loop-bash-validator.sh:522`, `hooks/loop-bash-validator.sh:534`, `hooks/loop-bash-validator.sh:549`). For active PR loops it blocks PR-loop `state.md` and generated read-only round files both by full path and bare basename to catch relative-path bypasses (`hooks/loop-bash-validator.sh:567`).
- inputs_outputs_state: Input is a Claude hook JSON object with `tool_name`, `tool_input.command`, and optional `session_id`. State inputs are active RLCR/PR-loop directories under `.humanize`, state frontmatter, `.cancel-requested`, git ignore state for `.humanize`, and prompt templates via shared block renderers. Output is exit code `0` to allow, `1` for malformed/safety failures, or `2` to block; block reasons are written to stderr, often using rendered markdown templates.
- gates_or_invariants: Non-Bash tools are ignored. No active loop means Bash is allowed. Malformed RLCR state blocks for safety. Cancel-by-rename is the only allowed mutation of active state files and must pass `is_cancel_authorized`. `finalize-state.md` and `methodology-analysis-state.md` are checked before generic `state.md` because their names also match the generic suffix. PR-loop protections must work even without an RLCR loop, as specified by `tests/test-pr-loop-hooks.sh:174`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, loop discovery, state parsing, command mutation detection, cancel authorization, `.humanize` git-add protection, and rendered block messages. It is registered as a hook implementation by the hook system and is tested through JSON-fed shell invocations in `tests/test-pr-loop-hooks.sh` and pattern-level tests in `tests/test-bash-validator-patterns.sh`.
- edge_cases_or_failure_modes: Handles command chaining, `mv/cp` source-side bypasses, shell-wrapper bypasses, leading redirections, quoted/ANSI-C/escaped redirection targets, broad `git add` shapes, `git -C` add variants, forced add-all, state-source symlinks during cancel, mixed quote cancel commands, and PR-loop relative paths from inside `.humanize/pr-loop`. A known limitation is explicit in methodology-analysis comments: spawned agents in different sessions are not restricted by this session-matched hook (`hooks/loop-bash-validator.sh:78`).
- validation_or_tests: `tests/test-pr-loop-hooks.sh:174` verifies PR-loop Bash protection without an RLCR loop. `tests/test-bash-validator-patterns.sh` tests `command_modifies_file` shapes consumed by this validator. Robustness tests under `tests/robustness/test-hook-system-robustness.sh` and cancel-security tests exercise state and cancel protections. PR-loop wrong-round write/edit behavior is tested through sibling validators at `tests/test-pr-loop-hooks.sh:1431`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-073 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Test fixture generator for a deterministic mock `gh` CLI used by PR-loop fetch and poll tests. It turns fixture JSON files into repeatable GitHub API responses.
- algorithmic_behavior: The script accepts `<mock_bin_dir>` and `<fixtures_dir>`, validates both are provided, creates the mock bin directory, writes an executable `gh` script, and prints the mock directory path (`tests/setup-fixture-mock-gh.sh:16`, `tests/setup-fixture-mock-gh.sh:19`, `tests/setup-fixture-mock-gh.sh:24`, `tests/setup-fixture-mock-gh.sh:99`). The generated mock handles `gh auth status`, `gh repo view`, `gh pr view`, and `gh api` endpoints. For API calls, it returns `testuser` for `user`, fixture `issue_comments.json` for `/issues/*/comments`, fixture `review_comments.json` for `/pulls/*/comments`, fixture `pr_reviews.json` for `/pulls/*/reviews`, and `[]` by default (`tests/setup-fixture-mock-gh.sh:33`, `tests/setup-fixture-mock-gh.sh:64`).
- inputs_outputs_state: Inputs are a writable mock-bin directory and a fixture directory containing `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`. Output is `$MOCK_BIN_DIR/gh`, an executable command shim. It does not alter repository algorithm state; it builds an isolated test PATH dependency.
- gates_or_invariants: Fails with usage and exit `1` if either argument is missing. The mock must mirror enough of GitHub CLI semantics for `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` to exercise repository owner/name detection, PR number/state lookup, current user lookup, issue comments, inline review comments, and PR reviews.
- dependencies_and_callers: Called by `setup_fixture_mock_gh` inside `tests/test-pr-loop-hooks.sh:1231`. The produced mock is placed first in `PATH` for fixture tests of `scripts/fetch-pr-comments.sh` at `tests/test-pr-loop-hooks.sh:1251` and `scripts/poll-pr-reviews.sh` at `tests/test-pr-loop-hooks.sh:1332`. It depends on the fixture files in `tests/fixtures`.
- edge_cases_or_failure_modes: Unhandled commands print `Mock gh: unhandled command` and exit `1` (`tests/setup-fixture-mock-gh.sh:95`). Endpoint matching is substring-based, so it intentionally supports the script paths under test rather than all `gh` options. If fixture filenames are absent or malformed, downstream fetch/poll scripts fail or produce incomplete output.
- validation_or_tests: Its behavior is validated indirectly by fixture-backed tests: fetch output must include human issue comments, inline review comments, and approval-only PR reviews with placeholders (`tests/test-pr-loop-hooks.sh:1242`); filtering after a timestamp must exclude early comments (`tests/test-pr-loop-hooks.sh:1287`); poll JSON must include approval-only reviews and bot response information (`tests/test-pr-loop-hooks.sh:1322`, `tests/test-pr-loop-hooks.sh:1371`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-103 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable PR-loop behavior specification. It covers validators, PR comment sorting and deduplication, gate-keeper logic, stop-hook integration, end-to-end stop-hook behavior with mocked `gh`/`git`, approval-only reviews, fixture-backed fetch/poll behavior, wrong-round protection, and monitor output.
- algorithmic_behavior: The file defines `run_hook_tests()` and nested test functions (`tests/test-pr-loop-hooks.sh:15`). Validator tests require PR-loop `active_bots` to be YAML list format, block writes to PR-loop `state.md`, block writes to generated `round-N-pr-comment.md`, allow `round-N-pr-resolve.md`, and ensure Bash protection works without any RLCR loop (`tests/test-pr-loop-hooks.sh:26`, `tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`, `tests/test-pr-loop-hooks.sh:174`). Sorting tests specify newest-first ISO sorting and human-before-bot ordering (`tests/test-pr-loop-hooks.sh:222`, `tests/test-pr-loop-hooks.sh:239`). Gate tests specify comment dedupe by `id`, YAML configured-bot parsing, Codex per-bot status extraction, re-adding previously approved bots when new issues appear, trigger mention timestamp selection, and final marker parsing for `APPROVE` and `WAITING_FOR_BOTS` (`tests/test-pr-loop-hooks.sh:271`, `tests/test-pr-loop-hooks.sh:288`, `tests/test-pr-loop-hooks.sh:326`, `tests/test-pr-loop-hooks.sh:353`, `tests/test-pr-loop-hooks.sh:390`, `tests/test-pr-loop-hooks.sh:415`, `tests/test-pr-loop-hooks.sh:437`). Stop-hook integration tests specify trigger detection by current user, timestamp refresh when newer, missing trigger blocking after round 0, round 0 preference for `last_trigger_at`, fallback to `started_at`, trigger-anchored timeouts, configured/active bot state tracking, and consistent next-round file naming (`tests/test-pr-loop-hooks.sh:538`, `tests/test-pr-loop-hooks.sh:588`, `tests/test-pr-loop-hooks.sh:606`, `tests/test-pr-loop-hooks.sh:624`, `tests/test-pr-loop-hooks.sh:646`, `tests/test-pr-loop-hooks.sh:667`, `tests/test-pr-loop-hooks.sh:698`, `tests/test-pr-loop-hooks.sh:726`). E2E tests execute `hooks/pr-loop-stop-hook.sh` with mocked CLI dependencies to verify missing resolve files block, trigger comments are detected, paginated API output is combined, and round 0 uses `last_trigger_at` for `--after` (`tests/test-pr-loop-hooks.sh:766`, `tests/test-pr-loop-hooks.sh:845`, `tests/test-pr-loop-hooks.sh:947`, `tests/test-pr-loop-hooks.sh:1052`). Later tests specify approval-only review placeholders, fixture-backed fetch/poll behavior, wrong-round pr-resolve blocking, and monitor parsing/display (`tests/test-pr-loop-hooks.sh:1160`, `tests/test-pr-loop-hooks.sh:1231`, `tests/test-pr-loop-hooks.sh:1431`, `tests/test-pr-loop-hooks.sh:1547`).
- inputs_outputs_state: Inputs are temporary test repos, `.humanize/pr-loop/<timestamp>/state.md`, generated mock `gh` and `git` commands, fixture JSON, hook JSON payloads for Write/Edit/Bash validators, and scripts under `hooks` and `scripts`. Outputs are pass/fail assertions through shared test helpers, generated loop files such as `round-N-pr-resolve.md`, `round-N-pr-comment.md`, and state transitions or block messages emitted by hooks.
- gates_or_invariants: Generated PR-loop files are read-only except current-round `pr-resolve`. Wrong-round `pr-resolve` writes/edits block. Trigger comments are filtered to the current GitHub user. For round 0, `last_trigger_at` wins over `started_at` when present. Subsequent rounds require a trigger. Poll timeouts are anchored to trigger time, not poll-start time. All configured bots are polled so previously approved bots can be re-added if they produce new issues. Approval-only PR reviews with empty body must not be dropped; they receive a `[Review state: ...]` placeholder.
- dependencies_and_callers: Sources or is run by the broader test harness via `tests/run-all-tests.sh`. Uses `init_test_git_repo`, `pass`, and `fail` from test helpers. Calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, and `tests/setup-monitor-test-env.sh`.
- edge_cases_or_failure_modes: Exercises no RLCR loop present, no resolve file, no trigger after round 0, new trigger newer than old, paginated `gh api` output, approval-only empty/null review bodies, timestamp filters excluding old reviews, wrong round numbers, empty active-bot lists, and mock CLI behavior. It also embeds macOS Bash compatibility concerns indirectly by testing YAML-list parsing and avoiding associative arrays in production hook logic.
- validation_or_tests: This file is itself the validation surface. It gives concrete expected hook exit codes, required block text, required output files, and JSON shape checks for poll results. It is one of the most comprehensive executable specs for PR-loop state transitions and gates.
- skip_candidate: `no`

### ASK_GEMINI-HZ-133 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: User-facing block template for attempts to modify `finalize-state.md`, the RLCR Finalize Phase state file managed by the loop system.
- algorithmic_behavior: The template states that `finalize-state.md` cannot be modified and directs the agent to focus on code simplification, committing changes, and writing `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1`). It is loaded by `finalize_state_file_blocked_message()` in `hooks/lib/loop-common.sh:800`, which is called by Bash, Write, and Edit validators when a command or tool targets `finalize-state.md` (`hooks/loop-bash-validator.sh:268`, `hooks/loop-write-validator.sh:215`, `hooks/loop-edit-validator.sh:168`).
- inputs_outputs_state: Input is only template rendering through `load_and_render_safe`; it has no variables. Output is markdown block reason text, normally sent to stderr by validators or embedded in hook block responses.
- gates_or_invariants: Reinforces that finalize-state is system-owned and that the only writable finalize artifact for the worker is `finalize-summary.md`. It supports the invariant that state transitions are performed by the stop hook or loop system, not by direct tool writes.
- dependencies_and_callers: Depends on `template-loader.sh` fallback-safe loading. Called via `loop-common.sh` helper from `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-edit-validator.sh`. Covered by `tests/test-finalize-phase.sh`, which checks the helper exists and finalize protections are active.
- edge_cases_or_failure_modes: If the template is missing, callers fall back to inline text from `loop-common.sh:801`. Because the template has no placeholders, rendering cannot fail due to variable substitution. Misleading direct edits are still blocked by validators before this text is emitted.
- validation_or_tests: Template reference coverage appears in `tests/test-template-references.sh`; finalize-state protection is exercised in finalize-phase and hook-system tests. Template-loader robustness guarantees missing-template fallback behavior.
- skip_candidate: `no`

### ASK_GEMINI-HZ-163 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Stop-hook block template requiring a worker to write the round or finalize work summary before exiting an RLCR loop.
- algorithmic_behavior: The template tells the agent it attempted to exit without a work summary, renders the required `{{SUMMARY_FILE}}`, and lists required summary contents: implemented work, files created/modified, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:1`). `hooks/loop-codex-stop-hook.sh` selects `finalize-summary.md` in Finalize Phase or `round-${CURRENT_ROUND}-summary.md` otherwise, and if the file is missing it renders this template into a JSON block response (`hooks/loop-codex-stop-hook.sh:721`, `hooks/loop-codex-stop-hook.sh:730`, `hooks/loop-codex-stop-hook.sh:737`).
- inputs_outputs_state: Input variable is `SUMMARY_FILE`. The state input is `IS_FINALIZE_PHASE` and `CURRENT_ROUND` from the active state file. Output is a stop-hook block JSON with `decision: block`, the rendered reason, and a system message such as “summary file missing for round N” or “Finalize Phase - summary file missing” (`hooks/loop-codex-stop-hook.sh:740`).
- gates_or_invariants: No RLCR round or finalize phase may exit without an explicit summary artifact. The summary file path is phase-specific and must be written before the agent attempts to exit again. This is a review-contract gate: Codex review and later transitions rely on a concrete summary file.
- dependencies_and_callers: Loaded with `load_and_render_safe` by `hooks/loop-codex-stop-hook.sh`. It depends on `loop-common.sh` for template loader setup and state parsing earlier in the stop hook. Its behavior is related to summary Bash/write validators that force summaries to be created through controlled Write/Edit paths rather than shell redirection.
- edge_cases_or_failure_modes: If the template is missing, the stop hook uses the inline fallback at `hooks/loop-codex-stop-hook.sh:734`. In Finalize Phase, the required summary file changes to `finalize-summary.md`; outside Finalize Phase, it is the current round summary. An empty file is not checked at this exact gate; this specific template handles absence.
- validation_or_tests: Covered through stop-hook tests that assert missing required artifacts block exit; template reference and fallback behavior are covered by template tests. The assigned template robustness test confirms missing templates safely fall back.
- skip_candidate: `no`

### ASK_GEMINI-HZ-193 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR-loop goal tracker template. It seeds the persistent `goal-tracker.md` for PR review loops with PR metadata, monitored bots, goal, issue summary table, counters, and initial log entry.
- algorithmic_behavior: The template renders PR number, branch, start time, monitored bots, startup case, an ultimate goal requiring all monitored bot reviewers to approve, an issue summary table initialized with round 0, total issue counters initialized to zero, and an issue log awaiting initial reviews (`prompt-template/pr-loop/goal-tracker-initial.md:1`). `scripts/setup-pr-loop.sh` constructs `GOAL_TRACKER_VARS` including `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `BOT_AC_ROWS`, and `STARTED_AT`, renders this template with fallback, and writes it to `$LOOP_DIR/goal-tracker.md` (`scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:658`, `scripts/setup-pr-loop.sh:712`).
- inputs_outputs_state: Inputs are PR setup state: PR number, starting branch, active bot list display, startup case, and start timestamp. Output is `.humanize/pr-loop/<timestamp>/goal-tracker.md`. This tracker is later parsed by monitor/status code and updated by PR-loop stop-hook/Codex review flows.
- gates_or_invariants: The goal tracker begins with zero issues found/resolved and remaining zero, ensuring later stop-hook updates can increment from known counters. The ultimate goal is approval from all monitored bots. The template uses rendered variables but no mutable/immutable split; PR-loop tracker update consistency is handled by later stop-hook logic and helper functions.
- dependencies_and_callers: Primary caller is `scripts/setup-pr-loop.sh`. PR-loop stop hook later references `$LOOP_DIR/goal-tracker.md` for update instructions (`hooks/pr-loop-stop-hook.sh:1254`). Monitor parsing of PR-loop goal trackers occurs in `scripts/humanize.sh` and `scripts/lib/monitor-common.sh`. Tests in `tests/test-pr-loop-system.sh` verify setup creates this file and that it contains expected structure and PR number.
- edge_cases_or_failure_modes: If the template is absent or empty, setup uses `FALLBACK_GOAL_TRACKER` in `scripts/setup-pr-loop.sh:668`, which includes a broader acceptance-criteria/current-status/log structure. A notable mismatch is that setup computes `BOT_AC_ROWS`, but this specific assigned template does not use `{{BOT_AC_ROWS}}`; that variable only affects the fallback. This is not necessarily a bug, but it means the normal rendered tracker is issue-summary oriented rather than acceptance-criteria oriented.
- validation_or_tests: `tests/test-pr-loop-system.sh` includes checks that setup creates `goal-tracker.md`, includes `Issue Summary`, and includes the PR number. `tests/test-pr-loop-hooks.sh` monitor tests validate YAML bot state display alongside tracker use. Template reference tests ensure the file is loadable from the configured template directory.
- skip_candidate: `no`

### ASK_GEMINI-HZ-223 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for the prompt template system, especially `hooks/lib/template-loader.sh` behavior under missing files, malformed placeholders, unusual variables, file-system edge cases, and concurrent loads.
- algorithmic_behavior: The script sources `template-loader.sh` and shared test helpers, creates a test directory, then runs 20 tests (`tests/robustness/test-template-error-robustness.sh:15`, `tests/robustness/test-template-error-robustness.sh:17`, `tests/robustness/test-template-error-robustness.sh:20`). Missing template and missing directory cases must return fallback content through `load_and_render_safe` (`tests/robustness/test-template-error-robustness.sh:34`, `tests/robustness/test-template-error-robustness.sh:43`). Empty templates may return empty or fallback but must not crash (`tests/robustness/test-template-error-robustness.sh:53`). Malformed templates with unclosed, nested, only-opening, and empty placeholders must produce non-empty results without crashing (`tests/robustness/test-template-error-robustness.sh:73`, `tests/robustness/test-template-error-robustness.sh:84`, `tests/robustness/test-template-error-robustness.sh:95`, `tests/robustness/test-template-error-robustness.sh:106`). Invalid variable names with spaces, special characters, or numeric-only names are tolerated (`tests/robustness/test-template-error-robustness.sh:125`). Values containing template syntax must not recursively expand, preserving `{{OTHER}}` literally (`tests/robustness/test-template-error-robustness.sh:165`). Newlines and very long variable names are tested (`tests/robustness/test-template-error-robustness.sh:176`, `tests/robustness/test-template-error-robustness.sh:188`). File-system tests cover BOM templates, whitespace-only files, filenames with spaces, permission-denied reads, concurrent loads, subdirectory templates, and symlinks (`tests/robustness/test-template-error-robustness.sh:208`, `tests/robustness/test-template-error-robustness.sh:220`, `tests/robustness/test-template-error-robustness.sh:228`, `tests/robustness/test-template-error-robustness.sh:245`, `tests/robustness/test-template-error-robustness.sh:270`, `tests/robustness/test-template-error-robustness.sh:309`, `tests/robustness/test-template-error-robustness.sh:320`).
- inputs_outputs_state: Inputs are synthetic templates in `$TEST_DIR`, direct calls to `render_template` and `load_and_render_safe`, generated permission states, background concurrent subshells, and symlinked files. Outputs are pass/fail records from test helpers and the final test summary at `tests/robustness/test-template-error-robustness.sh:340`. It does not mutate production state.
- gates_or_invariants: Template failures must degrade to fallback or harmless output, not abort hook execution. Variable substitution must be single-pass. Missing placeholders remain safe. Concurrent render calls must all succeed and preserve the source template file. Filesystem oddities such as BOM, whitespace-only content, special filenames, permission failures, and symlinks must not compromise the loader.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` and `tests/test-helpers.sh`. It is part of the robustness suite and is listed by `tests/run-all-tests.sh` through the robustness test collection. Its assertions directly cover block templates used by RLCR and PR-loop validators.
- edge_cases_or_failure_modes: Explicitly explores missing files/directories, empty content, malformed placeholder syntax, placeholder names outside the documented uppercase pattern, variable values containing newlines or template syntax, long names, BOM bytes, no permissions, parallel reads, nested subdirectories, and symlink behavior. Some outcomes are intentionally flexible, such as permission handling varying by system and empty templates accepting either empty output or fallback.
- validation_or_tests: This file is itself the validation. It complements `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-template-references.sh` by focusing on error resilience rather than normal rendering only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_GEMINI-HZ-013, ASK_GEMINI-HZ-043, ASK_GEMINI-HZ-073, ASK_GEMINI-HZ-103, ASK_GEMINI-HZ-133, ASK_GEMINI-HZ-163, ASK_GEMINI-HZ-193, ASK_GEMINI-HZ-223
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`