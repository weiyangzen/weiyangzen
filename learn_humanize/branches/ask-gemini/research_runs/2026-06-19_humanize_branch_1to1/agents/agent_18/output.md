# agent_18 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-018 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR-loop prompt contract directory. It defines the user-facing and Codex-facing markdown fragments that initialize `.humanize/pr-loop/<timestamp>/goal-tracker.md`, build `round-0-prompt.md`, and tell the Stop Hook/Codex how to continue the PR review state machine.
- algorithmic_behavior: `goal-tracker-initial.md:1-33` seeds PR metadata, ultimate goal, initial issue table row, totals, and round-0 log. `round-0-header.md:1-15` starts the round-0 prompt before fetched PR comments are appended by `scripts/setup-pr-loop.sh:758-764`. `round-0-task-no-comments.md:4-30` handles the fresh-PR path: write an initial resolution and exit without triggering bot review manually. `round-0-task-has-comments.md:4-43` handles the comments-present path: prioritize human comments, fix issues, commit, push, comment with bot mentions, and write a resolution. `critical-requirements-no-comments.md:2-20` and `critical-requirements-has-comments.md:2-24` duplicate the completion gate as prominent final instructions. `codex-goal-tracker-update.md:1-64` is injected into the local Codex review prompt so Codex records per-round reviewer statuses, issue totals, and issue-log entries.
- inputs_outputs_state: Inputs are template variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTED_AT`, `STARTUP_CASE`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are rendered markdown files and prompt text: `setup-pr-loop.sh` renders the goal tracker at `scripts/setup-pr-loop.sh:712-713`, renders the round-0 header at `scripts/setup-pr-loop.sh:758-761`, appends comments at `scripts/setup-pr-loop.sh:763-764`, selects the no-comments task at `scripts/setup-pr-loop.sh:767-800`, selects the has-comments task at `scripts/setup-pr-loop.sh:801-847`, and prints critical requirements at `scripts/setup-pr-loop.sh:890-942`. `pr-loop-stop-hook.sh` renders the Codex tracker-update instructions at `hooks/pr-loop-stop-hook.sh:1253-1263`.
- gates_or_invariants: The no-comments path forbids manually commenting to trigger first review; the has-comments path requires commit, push, and bot mention before waiting for re-review. Both task templates forbid modifying `.humanize/pr-loop/` state files. The goal tracker format is an invariant consumed by later prompt and monitor logic: per-reviewer rows, total statistics, and round issue log must remain structured.
- dependencies_and_callers: Rendered through `hooks/lib/template-loader.sh`, whose placeholder syntax is single-pass `{{VAR}}` substitution (`hooks/lib/template-loader.sh:7-20`, `hooks/lib/template-loader.sh:48-56`). Main callers are `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh`; the template directory is required by `hooks/lib/template-loader.sh` validation, which expects a `pr-loop` subdir.
- edge_cases_or_failure_modes: If templates are missing, callers provide inline fallbacks, so behavior degrades but does not stop setup. If PR comments contain the exact no-comments sentinel, setup chooses the automatic-review path; otherwise any non-sentinel content is treated as comments present (`scripts/setup-pr-loop.sh:723-729`). Placeholder omissions remain unreplaced by design, which can leak unresolved `{{...}}` into prompts if caller variables are incomplete.
- validation_or_tests: Covered by the PR-loop test runner `tests/test-pr-loop.sh:32-47`, system/fixture tests referenced from `tests/run-all-tests.sh`, and template-reference checks that scan `load_and_render_safe` usages. `tests/test-pr-loop-hooks.sh` validates PR-loop state protection, comment handling, trigger logic, and wrong-round behaviors that rely on these prompt contracts.
- skip_candidate: `no`

### ASK_GEMINI-HZ-048 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: Claude PreToolUse `Read` gate for RLCR loop files. It prevents reading stale, wrong-round, wrong-location, or privacy-sensitive loop artifacts while explicitly allowing normal reads outside restricted RLCR surfaces. PR-loop files are generally readable by design (`hooks/loop-read-validator.sh:12-13`).
- algorithmic_behavior: The hook reads JSON from stdin, validates structure and nesting, exits early for non-`Read` tools, then requires `tool_input.file_path` (`hooks/loop-read-validator.sh:26-49`). It blocks `round-*-todos.md` reads unless the path is allowlisted in the active loop (`hooks/loop-read-validator.sh:59-67`). During methodology-analysis phase, it canonicalizes file and loop paths and permits only methodology artifacts from the loop while blocking other project files (`hooks/loop-read-validator.sh:79-154`). It identifies restricted RLCR reads as `goal-tracker.md` or round summary/prompt/contract files (`hooks/loop-read-validator.sh:160-174`), locates the active session loop, parses strict state, blocks contract reads during finalize phase, validates the active goal tracker path, validates current round number, and finally enforces exact directory path (`hooks/loop-read-validator.sh:182-303`).
- inputs_outputs_state: Inputs are hook JSON, `session_id`, `tool_name`, `tool_input.file_path`, and environment roots such as `CLAUDE_PROJECT_DIR`, `PROJECT_ROOT`, and `LOOP_BASE_DIR`. Outputs are exit status plus stderr block messages. Exit `0` allows, exit `1` rejects malformed hook input or malformed state, and exit `2` blocks a policy violation with a rendered template. State read includes active loop discovery via `find_active_loop`, active state resolution, and `CURRENT_ROUND` from strict state parsing.
- gates_or_invariants: Only the active session-matched loop can influence methodology restrictions; the code comments explicitly avoid unfiltered fallback for methodology mode (`hooks/loop-read-validator.sh:81-85`). Malformed state fails closed (`hooks/loop-read-validator.sh:196-200`). Wrong-round summary/prompt/contract files are blocked unless allowlisted (`hooks/loop-read-validator.sh:271-284`). `goal-tracker.md` must be exactly the active loop tracker (`hooks/loop-read-validator.sh:212-230`). Finalize phase has no active contract, so contract reads are blocked (`hooks/loop-read-validator.sh:203-205`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-read-validator.sh:18-20`) for JSON validation, active loop resolution, state parsing, path classifiers, allowlist, normalization, and block renderers. It depends on `jq`, `realpath` when available, shell path tools, and templates such as `block/wrong-file-location.md`, `block/wrong-round-file.md`, and `block/wrong-directory-path.md`. It is registered in `hooks/hooks.json` as the read validator.
- edge_cases_or_failure_modes: Non-`Read` tools are ignored. Missing `file_path`, invalid JSON, invalid UTF-8/null bytes, or deeply nested JSON block before path logic. If no active loop exists, restricted reads are allowed because there is no authoritative current round. Nonexistent methodology paths use parent-directory realpath fallback for BSD/macOS compatibility (`hooks/loop-read-validator.sh:91-114`). Spawned agents with different `session_id` are not hook-restricted during methodology analysis; the prompt must enforce sanitization (`hooks/loop-read-validator.sh:83-84`, `hooks/loop-read-validator.sh:121-123`).
- validation_or_tests: Directly exercised by `tests/test-allowlist-validators.sh`, `tests/robustness/test-hook-system-robustness.sh`, `tests/robustness/test-hook-input-robustness.sh`, and finalize-phase tests. `tests/test-template-references.sh` also checks that validators use safe template rendering.
- skip_candidate: `no`

### ASK_GEMINI-HZ-078 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/ask-codex.sh`, the one-shot Codex consultation workflow used by `/humanize:ask-codex` and analyze-task delegation.
- algorithmic_behavior: The test creates an isolated git project, mock `codex` binary, and helper wrapper that runs `ask-codex.sh` with `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and mock `PATH` (`tests/test-ask-codex.sh:29-75`). It verifies validation failures for empty questions, help, unknown flags, missing option arguments, nonnumeric timeout, and unsafe model/effort characters (`tests/test-ask-codex.sh:84-154`). It verifies success output, project-local `.humanize/skill/<id>/output.md`, `metadata.md` status, `input.md` capture, and exit `0` (`tests/test-ask-codex.sh:164-210`). It covers Codex process errors, empty responses, timeout exit `124`, unique concurrent directories, model/effort parsing, `--` separator behavior, cache files, and skill guidance against bare `$ARGUMENTS` expansion (`tests/test-ask-codex.sh:220-433`).
- inputs_outputs_state: Inputs are command-line flags, free-form question text, and mock env vars `MOCK_CODEX_EXIT_CODE`, `MOCK_CODEX_STDOUT`, and `MOCK_CODEX_STDERR`. Expected outputs are stdout containing Codex response, stderr status including `ask-codex: cache=...`, local artifacts under `.humanize/skill`, cache artifacts `codex-run.cmd` and `codex-run.out`, and metadata statuses `success`, `error`, `empty_response`, or `timeout`.
- gates_or_invariants: `ask-codex.sh` must reject empty work, unknown options, unsafe model/effort characters, and bad timeout values. It must propagate non-timeout Codex exit codes, convert empty stdout to exit `1`, preserve timeout as exit `124`, and keep concurrent invocations from colliding. The skill docs must instruct callers to quote free-form user text and avoid `"${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh" $ARGUMENTS`.
- dependencies_and_callers: Tests `scripts/ask-codex.sh`, whose implementation parses args at `scripts/ask-codex.sh:89-144`, validates prerequisites at `scripts/ask-codex.sh:153-186`, creates storage at `scripts/ask-codex.sh:202-218`, saves input at `scripts/ask-codex.sh:224-238`, builds/runs Codex at `scripts/ask-codex.sh:244-298`, handles errors at `scripts/ask-codex.sh:310-385`, and emits success at `scripts/ask-codex.sh:391-415`. It also tests `skills/ask-codex/SKILL.md:14-34` for safe invocation guidance.
- edge_cases_or_failure_modes: The mock consumes stdin to avoid broken pipes (`tests/test-ask-codex.sh:49-50`). Concurrent calls are launched in the background and must produce at least two unique skill/cache directories (`tests/test-ask-codex.sh:286-320`). The test assumes default effort `high` for model-without-effort under a clean mock project (`tests/test-ask-codex.sh:341-350`), which depends on config-backed defaults resolving to fallback values.
- validation_or_tests: This file is itself the validation suite and is included in `tests/run-all-tests.sh`. I inspected it but did not execute tests because the assignment requested read-only research notes.
- skip_candidate: `no`

### ASK_GEMINI-HZ-108 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Combined executable test runner for the PR review loop state machine. It is a coordinator rather than an assertion-heavy module.
- algorithmic_behavior: It sources generic helpers and the PR-loop shared test library (`tests/test-pr-loop.sh:14-20`), initializes a mock PR-loop test environment (`tests/test-pr-loop.sh:26`), sources script, hook, and stop-hook test modules (`tests/test-pr-loop.sh:32-34`), then runs `run_script_tests`, `run_hook_tests`, and `run_stophook_tests` before printing a summary (`tests/test-pr-loop.sh:40-53`).
- inputs_outputs_state: Inputs are the repository test scripts and local shell environment. The sourced library creates temp dirs, mock `gh`, mock `codex`, and a modified `PATH` (`tests/test-pr-loop-lib.sh:27-121`). Outputs are pass/fail lines and a final PR Loop Tests summary. State is shared through global counters from `tests/test-helpers.sh` and test temp directories.
- gates_or_invariants: `set -euo pipefail` makes sourcing or runner failures abort (`tests/test-pr-loop.sh:13`). All three child suites must pass for the combined summary to return success. This top-level file preserves the invariant that PR-loop behavior is validated as scripts plus validators plus Stop Hook, not as isolated helpers only.
- dependencies_and_callers: Child script suite checks `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh` argument/validation behavior (`tests/test-pr-loop-scripts.sh:1-11`, `tests/test-pr-loop-scripts.sh:405-410`). Hook suite checks state protection, read-only comment/prompt protections, allowed resolve writes, Bash protection, sorting, dedupe, configured bot parsing, trigger detection, fixture API behavior, wrong-round resolve writes/edits, and monitor parsing (`tests/test-pr-loop-hooks.sh:15-23`, plus test list around `tests/test-pr-loop-hooks.sh:1432-1621`). Stop-hook suite checks trigger recency, startup-case exceptions, approval state, unpushed commits, force-push detection, missing triggers, bot timeout auto-removal, reaction approvals, fork PR lookup, and mixed approval (`tests/test-pr-loop-stophook.sh:1-15`, `tests/test-pr-loop-stophook.sh:1769-1780`).
- edge_cases_or_failure_modes: Because it sources modules into one shell, global variables and PATH mutations from earlier suites can affect later suites. The runner has no test isolation beyond what the helper functions and child modules provide. A failure in module sourcing prevents later suites from running.
- validation_or_tests: This file is the validation orchestrator for the PR-loop algorithm. It is also decomposed into `test-pr-loop-1-scripts.sh`, `test-pr-loop-2-hooks.sh`, and `test-pr-loop-3-stophook.sh` in the broader test runner.
- skip_candidate: `no`

### ASK_GEMINI-HZ-138 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: RLCR Stop Hook block template for the dirty-git gate. It tells the agent why it cannot stop and what cleanup/commit actions are required before retrying.
- algorithmic_behavior: The template renders `{{GIT_ISSUES}}` into the headline (`prompt-template/block/git-not-clean.md:1-4`), includes optional `{{SPECIAL_NOTES}}`, then requires reviewing untracked files, staging real changes, and committing with project conventions (`prompt-template/block/git-not-clean.md:5-10`). It also requires no AI authorship/co-author attribution in commits (`prompt-template/block/git-not-clean.md:11-16`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`, assembled by `hooks/loop-codex-stop-hook.sh` when `git status` shows uncommitted or untracked work. Output is a rendered block reason used in the Stop Hook JSON block decision; the state machine stays in the current loop state until the worktree is cleaned and committed.
- gates_or_invariants: Stop is blocked when uncommitted changes or untracked files are detected. The invariant is that loop completion cannot proceed with a dirty worktree, and commits must follow local authorship rules with no AI co-author trailer.
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh:622-675` through `load_and_render_safe`; that caller may also load specialized notes such as `git-not-clean-humanize-local.md` or `git-not-clean-untracked.md` before rendering. Rendering depends on `hooks/lib/template-loader.sh` single-pass substitution.
- edge_cases_or_failure_modes: If `SPECIAL_NOTES` is empty, the template still renders cleanly. If the template is missing, the Stop Hook has an inline fallback. The code-simplifier instruction is conditional on plugin installation, so absence of that plugin is not itself a blocker.
- validation_or_tests: Template presence and safe-rendering usage are covered by template reference tests. Dirty-git behavior is part of Stop Hook test coverage in the broader RLCR test suite, though this exact assigned file is a template rather than executable code.
- skip_candidate: `no`

### ASK_GEMINI-HZ-168 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: RLCR write/edit block template for wrong-round artifacts. It prevents agents from writing or editing `round-N-summary.md`, `round-N-prompt.md`, or `round-N-contract.md` for any round other than the active one.
- algorithmic_behavior: The template reports the attempted action, attempted round, file type, active `CURRENT_ROUND`, and `CORRECT_PATH`, then explicitly says not to increment the round number manually (`prompt-template/block/wrong-round-number.md:1-7`).
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Outputs are stderr block text from validators and exit `2` from the calling validator. It does not mutate state.
- gates_or_invariants: The invariant is that the state file owns the current round. Agents may only write/edit current-round artifacts, unless an explicit allowlist applies. They must not self-advance by creating a later round file.
- dependencies_and_callers: Used by `hooks/loop-write-validator.sh:326-341` and `hooks/loop-edit-validator.sh:251-266` after the validators parse the filename round and strict loop state. Rendering depends on `load_and_render_safe` and template-loader placeholder substitution.
- edge_cases_or_failure_modes: If no round number can be extracted, callers skip this template. Allowlisted historical files bypass the wrong-round block. If variables are missing, unresolved placeholders remain by template-loader design, which is safer than recursively substituting attacker-controlled content.
- validation_or_tests: Direct template rendering is covered by `tests/test-template-loader.sh:151-156` and `tests/test-templates-comprehensive.sh:498-504`. Wrong-round validator behavior is also covered by hook tests.
- skip_candidate: `no`

### ASK_GEMINI-HZ-198 `file` `scripts/lib/model-router.sh`
- cursor: `[_]`
- core_role: Shared provider-routing helper for scripts that need to dispatch model names to Codex or Claude and normalize effort values across providers.
- algorithmic_behavior: Source guard prevents double-loading (`scripts/lib/model-router.sh:6-8`). `detect_provider` rejects empty input, routes `gpt-*` and `o[0-9]*` to `codex`, routes `claude-*` or names containing `haiku`, `sonnet`, or `opus` case-insensitively to `claude`, and errors on unknown names (`scripts/lib/model-router.sh:10-30`). `check_provider_dependency` maps provider to binary, validates only `codex` or `claude`, checks `PATH`, and prints install guidance on failure (`scripts/lib/model-router.sh:32-60`). `map_effort` accepts only providers `codex|claude`, accepts only `xhigh|high|medium|low`, maps `xhigh` to `high` for Claude with an info log, and otherwise passes effort through (`scripts/lib/model-router.sh:62-91`).
- inputs_outputs_state: Inputs are function arguments and current `PATH`. Outputs are stdout provider/effort values, stderr error/info messages, and shell return codes. It keeps no persistent state beyond `_MODEL_ROUTER_LOADED`.
- gates_or_invariants: Unknown model names are hard failures, so callers cannot silently choose an arbitrary provider. Unknown providers and unknown efforts are hard failures. Claude never receives `xhigh`; Codex can receive `xhigh` unchanged.
- dependencies_and_callers: Tested by `tests/test-model-router.sh:1-426`. Used by `scripts/bitlesson-select.sh`, which sources it at `scripts/bitlesson-select.sh:9-12`, detects provider at `scripts/bitlesson-select.sh:106-115`, and checks/falls back on provider dependency at `scripts/bitlesson-select.sh:117-126`. It depends only on Bash, `grep`, and `command -v`.
- edge_cases_or_failure_modes: `o3-mini`, `o1-pro`, and `o4-mini` route to Codex because the pattern is `o` plus digit. A model containing `sonnet`, `haiku`, or `opus` anywhere routes to Claude, which is intentionally broad but can classify custom names unexpectedly. `provider_mode=codex-only` fallback is implemented in the caller, not this library.
- validation_or_tests: `tests/test-model-router.sh` covers Codex and Claude model detection, unknown/empty model errors, dependency success/failure with mock binaries, `xhigh` mapping for Claude, pass-through efforts, and invalid effort errors.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_GEMINI-HZ-018, ASK_GEMINI-HZ-048, ASK_GEMINI-HZ-078, ASK_GEMINI-HZ-108, ASK_GEMINI-HZ-138, ASK_GEMINI-HZ-168, ASK_GEMINI-HZ-198
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`