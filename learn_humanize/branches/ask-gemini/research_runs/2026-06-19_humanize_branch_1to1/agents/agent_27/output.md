# agent_27 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-027 `file` `agents/bitlesson-selector.md`
- cursor: `[_]`
- core_role: Agent prompt specification for selecting which BitLesson entries apply to a concrete sub-task before execution. It is a policy/prompt surface rather than executable shell logic.
- algorithmic_behavior: The prompt constrains the selector to inspect a sub-task description, related paths, and BitLesson content, then choose only directly relevant lessons. The decision policy prioritizes precision, rejects weak matches, and returns `NONE` when no lesson is directly relevant (`agents/bitlesson-selector.md:12`, `agents/bitlesson-selector.md:26`).
- inputs_outputs_state: Inputs are current sub-task description, related file paths, and project BitLesson content, normally `.humanize/bitlesson.md` (`agents/bitlesson-selector.md:14`). Output is exactly two stable lines: `LESSON_IDS: ...` and `RATIONALE: ...` (`agents/bitlesson-selector.md:32`). No persistent state is changed by this prompt file itself.
- gates_or_invariants: Output format is intentionally stable and forbids extra sections (`agents/bitlesson-selector.md:32`). The key invariant is deterministic, minimal selection so later Claude/Codex review can quickly validate the decision (`agents/bitlesson-selector.md:23`).
- dependencies_and_callers: Runtime execution is delegated to `scripts/bitlesson-select.sh`, which routes to Codex CLI for `gpt-*` models or Claude CLI for Claude-family models based on `bitlesson_model` (`agents/bitlesson-selector.md:21`). The runtime script loads merged config, validates required CLI args and BitLesson file presence, constructs the selector prompt, then enforces the same two-line output contract (`scripts/bitlesson-select.sh:15`, `scripts/bitlesson-select.sh:71`, `scripts/bitlesson-select.sh:237`). Loop prompts inject `bitlesson-selector` discipline into later work rounds when required (`hooks/loop-codex-stop-hook.sh:1455`, `hooks/loop-codex-stop-hook.sh:1927`).
- edge_cases_or_failure_modes: Empty or missing BitLesson files fail in the runtime script, while a non-empty file with no `## Lesson:` headings produces `LESSON_IDS: NONE` without invoking a model (`scripts/bitlesson-select.sh:89`, `scripts/bitlesson-select.sh:100`). Unsupported or unavailable providers fall back through model-router/dependency checks; unexpected model output fails closed with raw output shown on stderr (`scripts/bitlesson-select.sh:121`, `scripts/bitlesson-select.sh:253`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` covers Codex routing for `gpt-*`, stdin prompt passing via trailing `-`, and Claude routing for `haiku` (`tests/test-bitlesson-select-routing.sh:147`, `tests/test-bitlesson-select-routing.sh:165`, `tests/test-bitlesson-select-routing.sh:200`). Installer tests also verify a PATH-ready `bitlesson-selector` shim and dispatch behavior.
- skip_candidate: `no`

### ASK_GEMINI-HZ-057 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation command for active RLCR loops. It converts an active loop into a terminal cancellation state by writing a cancel signal and renaming the active state file to `cancel-state.md`.
- algorithmic_behavior: The script parses `--force` and help, locates the project from `CLAUDE_PROJECT_DIR` or `pwd`, sources `hooks/lib/loop-common.sh`, finds the newest active loop globally, detects which active phase file exists, optionally blocks finalize cancellation without force, then performs cancellation (`scripts/cancel-rlcr-loop.sh:24`, `scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:87`, `scripts/cancel-rlcr-loop.sh:104`, `scripts/cancel-rlcr-loop.sh:135`, `scripts/cancel-rlcr-loop.sh:152`).
- inputs_outputs_state: Inputs are optional `--force`, current project directory, and active loop files under `.humanize/rlcr`. State transitions are `state.md`, `methodology-analysis-state.md`, or `finalize-state.md` to `cancel-state.md`; it also creates `.cancel-requested`, removes `.humanize/.pending-session-id`, and removes `.methodology-exit-reason` (`scripts/cancel-rlcr-loop.sh:99`, `scripts/cancel-rlcr-loop.sh:152`). Outputs include sentinel strings such as `NO_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, and `CANCELLED_FINALIZE` with round metadata (`scripts/cancel-rlcr-loop.sh:89`, `scripts/cancel-rlcr-loop.sh:136`, `scripts/cancel-rlcr-loop.sh:168`).
- gates_or_invariants: Finalize phase requires `--force`; otherwise exit code 2 blocks cancellation (`scripts/cancel-rlcr-loop.sh:135`). The command intentionally ignores session filtering because slash-command cancellation is a global user action in the current project (`scripts/cancel-rlcr-loop.sh:76`). The active loop finder has zombie-loop protection: without a session filter it only checks the newest loop directory and does not revive older active directories if a newer terminal directory exists (`hooks/lib/loop-common.sh:289`, `hooks/lib/loop-common.sh:316`).
- dependencies_and_callers: Depends on `find_active_loop` and active-state resolution from `hooks/lib/loop-common.sh` (`scripts/cancel-rlcr-loop.sh:72`, `hooks/lib/loop-common.sh:245`, `hooks/lib/loop-common.sh:307`). The bash validator has an explicit allowlist for `cancel-rlcr-loop.sh` and for the state-file rename when `.cancel-requested` exists; `is_cancel_authorized` validates that the move is exactly from an active state file to `cancel-state.md`, rejects shell injection and extra args, and rejects symlink sources (`hooks/lib/loop-common.sh:964`, `hooks/lib/loop-common.sh:988`, `hooks/lib/loop-common.sh:1112`).
- edge_cases_or_failure_modes: No active loop returns code 1. A loop directory with no active state file also returns code 1 (`scripts/cancel-rlcr-loop.sh:89`, `scripts/cancel-rlcr-loop.sh:113`). Unknown args return code 3. Finalize without force returns code 2. Because `set -euo pipefail` is active, failures in `touch`, `rm`, or `mv` abort as “other error” behavior even though there is no custom trap (`scripts/cancel-rlcr-loop.sh:18`). If a newer terminal loop exists, global cancellation reports no active loop rather than cancelling an older stale loop, as tested (`tests/test-session-id.sh:420`).
- validation_or_tests: Session tests assert cancellation works regardless of stored `session_id` and renames state to `cancel-state.md` (`tests/test-session-id.sh:368`). The same suite asserts zombie-loop protection when the newest loop is completed (`tests/test-session-id.sh:386`). `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh` cover the cancel signal and validator authorization boundary.
- skip_candidate: `no`

### ASK_GEMINI-HZ-087 `file` `tests/test-disable-nested-codex-hooks.sh`
- cursor: `[_]`
- core_role: Executable regression spec ensuring Humanize’s nested Codex calls disable native Codex hooks, preventing recursive hook invocation during RLCR stop/review flows and PR review loops.
- algorithmic_behavior: The test builds temporary git repos, creates mock loop state, injects a mock `codex` binary that records arguments, invokes `hooks/loop-codex-stop-hook.sh`, then checks whether implementation-phase `codex exec` and review-phase `codex review` include `--disable codex_hooks` before the subcommand (`tests/test-disable-nested-codex-hooks.sh:34`, `tests/test-disable-nested-codex-hooks.sh:67`, `tests/test-disable-nested-codex-hooks.sh:155`, `tests/test-disable-nested-codex-hooks.sh:179`, `tests/test-disable-nested-codex-hooks.sh:190`).
- inputs_outputs_state: Inputs are mock repo directories, mock Codex behavior, and loop state files containing `review_started` true or false (`tests/test-disable-nested-codex-hooks.sh:100`). The test mutates only temporary files under `mktemp -d` and exports isolated `XDG_CACHE_HOME` (`tests/test-disable-nested-codex-hooks.sh:34`, `tests/test-disable-nested-codex-hooks.sh:37`). Outputs are PASS/FAIL counters and final exit code 1 on failure (`tests/test-disable-nested-codex-hooks.sh:204`).
- gates_or_invariants: Required invariant is that nested Codex calls use the disable-hooks arg when the CLI supports it. The stop hook probes `codex --help`, caches support per loop, and prepends `CODEX_DISABLE_HOOKS_ARGS` to both `codex review` and `codex exec` invocations (`hooks/loop-codex-stop-hook.sh:1065`, `hooks/loop-codex-stop-hook.sh:1162`, `hooks/loop-codex-stop-hook.sh:1591`). The PR stop hook performs the same guard for `codex exec` (`hooks/pr-loop-stop-hook.sh:1337`, `hooks/pr-loop-stop-hook.sh:1350`).
- dependencies_and_callers: Calls `hooks/loop-codex-stop-hook.sh` and scans `hooks/pr-loop-stop-hook.sh` source. It depends on git availability, writable temp/cache directories, shell PATH overriding, and the loop-state schema fields required by the stop hook (`tests/test-disable-nested-codex-hooks.sh:40`, `tests/test-disable-nested-codex-hooks.sh:111`).
- edge_cases_or_failure_modes: If Codex help output does not advertise `--disable`, the hook intentionally omits the arg for old CLI compatibility, so this test’s mock must expose `--disable` in help-like output indirectly through the mock’s accepted argv path. The test records `$*` as a single line, so it validates ordering by substring rather than structured argv. Hook failures are surfaced with captured output (`tests/test-disable-nested-codex-hooks.sh:164`).
- validation_or_tests: This file is itself validation. It covers implementation-phase stop hook, review-phase stop hook, and PR stop hook source pattern (`tests/test-disable-nested-codex-hooks.sh:175`, `tests/test-disable-nested-codex-hooks.sh:186`, `tests/test-disable-nested-codex-hooks.sh:197`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-117 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable template integrity spec ensuring shell validators and stop hooks do not reference missing prompt/block templates and that critical validators use fallback-safe rendering.
- algorithmic_behavior: The script scans a fixed set of hook shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls with `$TEMPLATE_DIR`, extracts the referenced template paths, verifies files exist under `prompt-template`, inventories all template `.md` files, validates a curated set of common block templates, and checks critical validators for unsafe `load_and_render` use (`tests/test-template-references.sh:54`, `tests/test-template-references.sh:74`, `tests/test-template-references.sh:121`, `tests/test-template-references.sh:152`, `tests/test-template-references.sh:172`).
- inputs_outputs_state: Inputs are shell scripts under `hooks/`, templates under `prompt-template/`, and static arrays of scripts/templates (`tests/test-template-references.sh:16`, `tests/test-template-references.sh:57`). Outputs are PASS/FAIL/WARN counts and a nonzero exit when missing templates or unsafe critical rendering are found (`tests/test-template-references.sh:25`, `tests/test-template-references.sh:221`).
- gates_or_invariants: Hard gate: every directly referenced template in scanned scripts must exist. Critical validators `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh` must use `load_and_render_safe` for all template rendering so missing template files degrade to fallback messages instead of empty block reasons (`tests/test-template-references.sh:174`, `tests/test-template-references.sh:176`, `tests/test-template-references.sh:196`).
- dependencies_and_callers: Depends on `find`, `grep`, `sed`, bash arrays, and the template-loader naming convention. It directly aligns with `hooks/lib/template-loader.sh`, where `load_and_render_safe` exists to render templates with fallback content. It is included in the repo’s broader test suite list (`tests/run-all-tests.sh:99` references nearby suites, while this script is standalone executable).
- edge_cases_or_failure_modes: The scanner only catches direct quoted template paths after `"$TEMPLATE_DIR"`; dynamic paths may warn as unreferenced or escape direct validation. Warnings for unreferenced templates are informational and do not fail the test (`tests/test-template-references.sh:130`, `tests/test-template-references.sh:139`). Missing scanned scripts produce warnings, not failures (`tests/test-template-references.sh:74`).
- validation_or_tests: This file is itself the validation gate. It specifically protects against validator block messages becoming empty when a template is missing, as stated in the header (`tests/test-template-references.sh:8`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-147 `file` `prompt-template/block/mainline-verdict-missing.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop hook when implementation review output omits the mandatory mainline progress verdict line.
- algorithmic_behavior: The template tells the agent that the review output is missing `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED`, explains that Humanize cannot safely update drift state or choose the next-round prompt, and instructs retrying exit so Codex reruns the implementation review (`prompt-template/block/mainline-verdict-missing.md:1`, `prompt-template/block/mainline-verdict-missing.md:3`, `prompt-template/block/mainline-verdict-missing.md:7`).
- inputs_outputs_state: Inputs are render variables `REVIEW_RESULT_FILE` and `REVIEW_PROMPT_FILE`, which are included in the block message for audit/debug (`prompt-template/block/mainline-verdict-missing.md:11`). The template itself changes no state; the calling hook emits a JSON block decision.
- gates_or_invariants: The mandatory verdict line is the invariant. The hook uses this template in `block_missing_mainline_verdict`, emits `"decision": "block"`, and exits without advancing to the next prompt when the verdict is absent (`hooks/loop-codex-stop-hook.sh:1355`, `hooks/loop-codex-stop-hook.sh:1375`, `hooks/loop-codex-stop-hook.sh:1379`). This prevents drift counters and prompt routing from being updated on ambiguous review output.
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe` with an inline fallback (`hooks/loop-codex-stop-hook.sh:1361`, `hooks/loop-codex-stop-hook.sh:1375`). The missing-verdict path is triggered after implementation review parsing detects no mainline verdict (`hooks/loop-codex-stop-hook.sh:1723`). Review prompt templates explicitly tell Codex the verdict line is mandatory (`prompt-template/codex/full-alignment-review.md:59`, `prompt-template/codex/regular-review.md:56`).
- edge_cases_or_failure_modes: If this template is missing, the safe loader fallback still blocks with equivalent text (`hooks/loop-codex-stop-hook.sh:1361`). If Codex returns useful review text but no verdict, the system intentionally blocks rather than inferring a verdict from prose.
- validation_or_tests: `tests/test-finalize-phase.sh` has a negative test where mocked Codex review omits the verdict; expected behavior is a block decision mentioning verdict and no creation of the next round prompt (`tests/test-finalize-phase.sh:948`, `tests/test-finalize-phase.sh:976`). `tests/test-template-references.sh` validates the template exists where referenced.
- skip_candidate: `no`

### ASK_GEMINI-HZ-177 `file` `prompt-template/claude/methodology-analysis-prompt.md`
- cursor: `[_]`
- core_role: Prompt template for the RLCR methodology-analysis phase that runs when the loop reaches an exit point, before final terminal state is accepted.
- algorithmic_behavior: The prompt instructs Claude to spawn an Opus agent, analyze loop records from a methodology-only perspective, write `methodology-analysis-report.md`, then derive all user-facing content solely from that sanitized report (`prompt-template/claude/methodology-analysis-prompt.md:12`, `prompt-template/claude/methodology-analysis-prompt.md:16`, `prompt-template/claude/methodology-analysis-prompt.md:43`). It enumerates sanitization rules forbidding project-identifying details and defines optional GitHub issue flow if improvements are found (`prompt-template/claude/methodology-analysis-prompt.md:22`, `prompt-template/claude/methodology-analysis-prompt.md:51`).
- inputs_outputs_state: Render inputs are `EXIT_REASON`, `EXIT_REASON_DESCRIPTION`, `CURRENT_ROUND`, `MAX_ITERATIONS`, and `LOOP_DIR` (`prompt-template/claude/methodology-analysis-prompt.md:5`, `hooks/lib/methodology-analysis.sh:82`). Required outputs are `methodology-analysis-report.md` and meaningful content in `methodology-analysis-done.md` (`prompt-template/claude/methodology-analysis-prompt.md:20`, `prompt-template/claude/methodology-analysis-prompt.md:71`).
- gates_or_invariants: Sanitization is the central invariant: no paths, identifiers, domain terms, snippets, raw errors, or project-specific URLs in the report (`prompt-template/claude/methodology-analysis-prompt.md:22`). Completion marker content is mandatory (`prompt-template/claude/methodology-analysis-prompt.md:73`). The library enforces that both done marker and report exist with content before converting methodology state to terminal state (`hooks/lib/methodology-analysis.sh:118`, `hooks/lib/methodology-analysis.sh:129`).
- dependencies_and_callers: Rendered by `enter_methodology_analysis_phase` in `hooks/lib/methodology-analysis.sh`, which renames the active state file to `methodology-analysis-state.md`, records `.methodology-exit-reason`, touches a placeholder done file, and returns a block JSON containing this prompt (`hooks/lib/methodology-analysis.sh:64`, `hooks/lib/methodology-analysis.sh:68`, `hooks/lib/methodology-analysis.sh:71`, `hooks/lib/methodology-analysis.sh:89`). The stop hook enters this phase for max-iteration, completion, and circuit-breaker exits (`hooks/loop-codex-stop-hook.sh:901`, `hooks/loop-codex-stop-hook.sh:917`, `hooks/loop-codex-stop-hook.sh:1875`).
- edge_cases_or_failure_modes: Privacy mode skips the phase (`hooks/lib/methodology-analysis.sh:42`). Re-entry is skipped if methodology state is already active or a non-empty done marker already exists (`hooks/lib/methodology-analysis.sh:48`, `hooks/lib/methodology-analysis.sh:54`). Completion fails closed if the report is missing or empty, if `.methodology-exit-reason` is missing, or if exit reason is not one of `complete`, `stop`, or `maxiter` (`hooks/lib/methodology-analysis.sh:131`, `hooks/lib/methodology-analysis.sh:142`, `hooks/lib/methodology-analysis.sh:152`).
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`. Stop-hook integration references are visible in `rg` results and line-level callers; methodology completion gates are implemented in `hooks/lib/methodology-analysis.sh:114`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-207 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for base-branch auto-detection used by RLCR setup before Codex review can compare implementation changes against a stable base.
- algorithmic_behavior: The test defines a local `detect_base_branch` helper that attempts remote default branch from `git remote show origin`, falls back to local `main`, then local `master`, and fails when none are available (`tests/robustness/test-base-branch-detection.sh:35`, `tests/robustness/test-base-branch-detection.sh:41`, `tests/robustness/test-base-branch-detection.sh:50`, `tests/robustness/test-base-branch-detection.sh:56`). It creates temporary repos for no-origin/main, no-origin/master, no-origin/develop-only, and simulated origin cases (`tests/robustness/test-base-branch-detection.sh:70`, `tests/robustness/test-base-branch-detection.sh:100`, `tests/robustness/test-base-branch-detection.sh:129`, `tests/robustness/test-base-branch-detection.sh:156`).
- inputs_outputs_state: Inputs are generated git repositories, optional bare remote, and `git_timeout`. Outputs are PASS/FAIL counters via `test-helpers.sh` and the detected branch name or failure (`tests/robustness/test-base-branch-detection.sh:18`, `tests/robustness/test-base-branch-detection.sh:24`, `tests/robustness/test-base-branch-detection.sh:197`). State is confined to the temporary test directory.
- gates_or_invariants: Detection priority is user-specified base branch in the real setup path, then remote default, local `main`, local `master`, then hard failure (`scripts/setup-rlcr-loop.sh:753`). The real script additionally validates user-specified branch existence locally and checks whether a remote-only branch needs fetching (`scripts/setup-rlcr-loop.sh:755`, `scripts/setup-rlcr-loop.sh:761`). For auto-detected remote default, current setup accepts it only if that branch exists locally, then falls back to local `main`/`master`; if no usable local branch exists it errors and suggests `--base-branch` or fetching (`scripts/setup-rlcr-loop.sh:778`, `scripts/setup-rlcr-loop.sh:781`, `scripts/setup-rlcr-loop.sh:794`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, git, and writable temp repos (`tests/robustness/test-base-branch-detection.sh:18`). It mirrors part of `scripts/setup-rlcr-loop.sh`, which later writes `base_branch` into loop state and validates YAML safety (`scripts/setup-rlcr-loop.sh:806`, `scripts/setup-rlcr-loop.sh:899`).
- edge_cases_or_failure_modes: Missing origin is deliberately guarded with `|| true` so `pipefail` does not terminate detection (`tests/robustness/test-base-branch-detection.sh:41`, `scripts/setup-rlcr-loop.sh:777`). No local `main`/`master` fails (`tests/robustness/test-base-branch-detection.sh:144`). There is a nuance: the test helper says it replicates setup logic, but it does not verify remote default local existence before echoing it; the current real setup does (`tests/robustness/test-base-branch-detection.sh:35`, `scripts/setup-rlcr-loop.sh:780`). The simulated-origin test accepts either remote detection or local fallback (`tests/robustness/test-base-branch-detection.sh:181`).
- validation_or_tests: This file is the validation spec. It covers no-origin fallback to `main`, fallback to `master`, failure with only `develop`, and remote/default behavior (`tests/robustness/test-base-branch-detection.sh:88`, `tests/robustness/test-base-branch-detection.sh:117`, `tests/robustness/test-base-branch-detection.sh:144`, `tests/robustness/test-base-branch-detection.sh:181`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item headings in Item Evidence; each assigned item appears exactly once there
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`

Verification note: I did not modify files. A `git status --short` check could not complete because this read-only branch export is not a git checkout with `.git`, and the sandbox also blocked Xcode cache writes under `/tmp`.