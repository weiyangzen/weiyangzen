# agent_15 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Phase-specific Claude prompt templates for the RLCR loop. The directory defines what Claude is allowed and expected to do in initial implementation, continuation, review, drift recovery, finalize, and methodology-analysis phases.
- algorithmic_behavior: The directory has no nested subdirectories; all 14 files were inspected. `agent-teams-core.md` makes Claude a coordinator-only team leader, forbidding direct implementation and requiring Task-tool delegation, file ownership boundaries, progress monitoring, review, and commits by team members (`prompt-template/claude/agent-teams-core.md:1`, `prompt-template/claude/agent-teams-core.md:16`, `prompt-template/claude/agent-teams-core.md:18`, `prompt-template/claude/agent-teams-core.md:27`). `agent-teams-instructions.md` is injected into round 0 when agent teams are enabled and points to the plan, goal tracker, and summary contract (`prompt-template/claude/agent-teams-instructions.md:1`, `prompt-template/claude/agent-teams-instructions.md:5`). `agent-teams-continue.md` re-creates agent teams after a prior round, explicitly warning that old teammates no longer exist and that new members are cold-started (`prompt-template/claude/agent-teams-continue.md:5`, `prompt-template/claude/agent-teams-continue.md:7`, `prompt-template/claude/agent-teams-continue.md:10`).
- inputs_outputs_state: Templates are rendered by setup/stop hooks with variables such as `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, `CURRENT_ROUND`, `BASE_BRANCH`, `START_BRANCH`, and `FINALIZE_SUMMARY_FILE`. `scripts/setup-rlcr-loop.sh` appends `agent-teams-instructions.md` and `agent-teams-core.md` into `round-0-prompt.md` when `AGENT_TEAMS=true` (`scripts/setup-rlcr-loop.sh:1377`). `hooks/loop-codex-stop-hook.sh` renders `drift-replan-prompt.md` or `next-round-prompt.md` into the next round prompt (`hooks/loop-codex-stop-hook.sh:1951`, `hooks/loop-codex-stop-hook.sh:1962`), then appends footer, push note, goal tracker request, and optional agent-teams continuation (`hooks/loop-codex-stop-hook.sh:2056`, `hooks/loop-codex-stop-hook.sh:2063`, `hooks/loop-codex-stop-hook.sh:2072`, `hooks/loop-codex-stop-hook.sh:2079`).
- gates_or_invariants: The templates enforce a single mainline objective per round (`prompt-template/claude/next-round-prompt.md:20`), classification of tasks into `[mainline]`, `[blocking]`, and `[queued]` lanes (`prompt-template/claude/next-round-prompt.md:29`), stable immutable goal tracker content after round 0 (`prompt-template/claude/next-round-prompt.md:63`), review-phase fixes without unrelated features (`prompt-template/claude/review-phase-prompt.md:34`), and functionality-equivalent finalize changes only (`prompt-template/claude/finalize-phase-prompt.md:16`). Drift recovery requires re-reading plan/tracker/history, rewriting the round contract, and proving mainline advancement (`prompt-template/claude/drift-replan-prompt.md:17`, `prompt-template/claude/drift-replan-prompt.md:25`, `prompt-template/claude/drift-replan-prompt.md:63`). Methodology analysis must be sanitized and derived from report content only (`prompt-template/claude/methodology-analysis-prompt.md:22`, `prompt-template/claude/methodology-analysis-prompt.md:45`).
- dependencies_and_callers: Main callers are `scripts/setup-rlcr-loop.sh` for initial prompt generation and `hooks/loop-codex-stop-hook.sh` for continuation/review/finalize prompts. `hooks/lib/methodology-analysis.sh` renders `methodology-analysis-prompt.md` after moving the loop to methodology-analysis state (`hooks/lib/methodology-analysis.sh:64`, `hooks/lib/methodology-analysis.sh:82`). Tests in `tests/test-agent-teams.sh` verify the agent-team templates exist and are injected only in the intended phases (`tests/test-agent-teams.sh:331`, `tests/test-agent-teams.sh:593`, `tests/test-agent-teams.sh:704`).
- edge_cases_or_failure_modes: If templates are absent, setup and stop hooks have fallback prompt blocks, so the loop remains operable but with less detailed guidance (`scripts/setup-rlcr-loop.sh:1381`, `scripts/setup-rlcr-loop.sh:1387`, `hooks/loop-codex-stop-hook.sh:2084`, `hooks/loop-codex-stop-hook.sh:2090`). Agent-teams continuation is deliberately not appended in review phase, preventing review remediation from being reframed as general team implementation (`hooks/loop-codex-stop-hook.sh:2081`). `open-question-notice.md` is injected only when review content has a short “Open Question” line and question routing is enabled (`hooks/loop-codex-stop-hook.sh:2012`, `hooks/loop-codex-stop-hook.sh:2023`).
- validation_or_tests: `tests/test-agent-teams.sh` validates `--agent-teams` gating, state parsing, round-0 prompt injection, template existence, implementation-phase continuation, drift recovery continuation, and review-phase exclusion (`tests/test-agent-teams.sh:54`, `tests/test-agent-teams.sh:117`, `tests/test-agent-teams.sh:267`, `tests/test-agent-teams.sh:350`, `tests/test-agent-teams.sh:596`, `tests/test-agent-teams.sh:635`, `tests/test-agent-teams.sh:707`). Template reference tests also scan template-loader usage (`tests/test-template-references.sh:83`, `tests/test-template-references.sh:172`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-045 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Edit` validator for RLCR and PR-loop controlled files. It blocks edits to system-owned prompt/state files, enforces correct round/session targeting, protects goal-tracker immutable content, and constrains methodology-analysis edits.
- algorithmic_behavior: The hook reads JSON from stdin, exits immediately unless `.tool_name` is `Edit`, extracts `.tool_input.file_path`, lowercases it, and filters by `session_id` (`hooks/loop-edit-validator.sh:25`, `hooks/loop-edit-validator.sh:28`, `hooks/loop-edit-validator.sh:32`, `hooks/loop-edit-validator.sh:36`). It blocks `round-*-todos.md` edits unless allowlisted in the active loop (`hooks/loop-edit-validator.sh:42`), blocks prompt-file writes (`hooks/loop-edit-validator.sh:52`), applies PR-loop state/read-only restrictions (`hooks/loop-edit-validator.sh:61`, `hooks/loop-edit-validator.sh:65`, `hooks/loop-edit-validator.sh:71`), and validates PR resolve-file round numbers (`hooks/loop-edit-validator.sh:77`).
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name`, `tool_input.file_path`, and optionally `old_string`, `new_string`, `replace_all`, and `session_id`. Output is either exit 0 to allow, exit 2 with a block message to deny an unsafe edit, or exit 1 for malformed required data or malformed loop state. State is read from `.humanize/rlcr/*/{state.md,finalize-state.md,methodology-analysis-state.md}` via `find_active_loop` and `resolve_active_state_file`, with strict parsing into `CURRENT_ROUND` (`hooks/loop-edit-validator.sh:136`, `hooks/loop-edit-validator.sh:145`, `hooks/loop-edit-validator.sh:151`).
- gates_or_invariants: During methodology analysis, only `methodology-analysis-report.md` and `methodology-analysis-done.md` may be edited; all other edits are blocked before the general `.humanize/rlcr` early exit (`hooks/loop-edit-validator.sh:84`, `hooks/loop-edit-validator.sh:98`, `hooks/loop-edit-validator.sh:109`, `hooks/loop-edit-validator.sh:117`). State files are blocked in specificity order: methodology analysis state, finalize state, then generic state (`hooks/loop-edit-validator.sh:159`, `hooks/loop-edit-validator.sh:163`, `hooks/loop-edit-validator.sh:168`, `hooks/loop-edit-validator.sh:173`). Finalize phase blocks historical contract edits (`hooks/loop-edit-validator.sh:178`). `plan.md` backups under `.humanize/rlcr` are read-only (`hooks/loop-edit-validator.sh:187`). After round 0, goal tracker edits must preserve the immutable section by previewing the Edit result and comparing immutable content (`hooks/loop-edit-validator.sh:211`, `hooks/loop-edit-validator.sh:225`, `hooks/loop-edit-validator.sh:230`).
- dependencies_and_callers: Registered in `hooks/hooks.json` as the PreToolUse hook for the `Edit` matcher (`hooks/hooks.json:24`). Depends heavily on `hooks/lib/loop-common.sh` for active-loop lookup, state parsing, file-type predicates, PR-loop helpers, render-safe block messages, edit preview, and immutable tracker comparison (`hooks/loop-edit-validator.sh:18`, `hooks/lib/loop-common.sh:307`, `hooks/lib/loop-common.sh:474`, `hooks/lib/loop-common.sh:728`, `hooks/lib/loop-common.sh:882`, `hooks/lib/loop-common.sh:899`, `hooks/lib/loop-common.sh:1218`, `hooks/lib/loop-common.sh:1277`).
- edge_cases_or_failure_modes: Malformed state fails closed with exit 1 (`hooks/loop-edit-validator.sh:151`). If there is no active loop, edits under `.humanize/rlcr` are allowed rather than globally blocked (`hooks/loop-edit-validator.sh:140`). Methodology-analysis filtering is session-aware and intentionally does not restrict spawned agents with different session IDs; the prompt handles their sanitization (`hooks/loop-edit-validator.sh:92`, `hooks/loop-edit-validator.sh:94`). If an Edit request to goal tracker after round 0 lacks `old_string` or `new_string`, it returns exit 1 rather than attempting a blind allow (`hooks/loop-edit-validator.sh:211`).
- validation_or_tests: Covered by finalize-phase tests for blocking finalize contract edits (`tests/test-finalize-phase.sh:425`, `tests/test-finalize-phase.sh:437`), plan-file hook tests (`tests/test-plan-file-hooks.sh:273`), allowlist validator tests (`tests/test-allowlist-validators.sh:229`), PR-loop hook tests (`tests/test-pr-loop-hooks.sh:1520`), and robustness hook-system tests that call this validator directly with JSON (`tests/robustness/test-hook-system-robustness.sh:44`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-075 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR `--agent-teams` feature, spanning CLI validation, state persistence, parser behavior, prompt generation, continuation behavior, drift recovery, and review-phase exclusion.
- algorithmic_behavior: The test creates temporary Git repos and plan files, invokes `scripts/setup-rlcr-loop.sh` with and without `--agent-teams`, and checks state/prompt artifacts under `.humanize/rlcr` (`tests/test-agent-teams.sh:35`, `tests/test-agent-teams.sh:56`, `tests/test-agent-teams.sh:104`). It sources `hooks/lib/loop-common.sh` to test `parse_state_file` and `find_active_loop` directly (`tests/test-agent-teams.sh:20`, `tests/test-agent-teams.sh:207`, `tests/test-agent-teams.sh:409`). Later sections synthesize stop-hook loop directories and mock `codex` output to force implementation-phase continuation, drift recovery, no-agent-teams continuation, and review-phase prompting (`tests/test-agent-teams.sh:431`, `tests/test-agent-teams.sh:543`, `tests/test-agent-teams.sh:567`, `tests/test-agent-teams.sh:596`, `tests/test-agent-teams.sh:635`, `tests/test-agent-teams.sh:675`, `tests/test-agent-teams.sh:707`).
- inputs_outputs_state: Inputs are environment variables (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `CLAUDE_PROJECT_DIR`, `PATH` for mock `codex`), setup arguments, synthetic state frontmatter, and mocked Codex review text. Outputs are pass/fail test assertions plus generated state files and prompts. The key state field is `agent_teams`, expected to be true when enabled and false by default or missing (`tests/test-agent-teams.sh:117`, `tests/test-agent-teams.sh:149`, `tests/test-agent-teams.sh:199`, `tests/test-agent-teams.sh:234`).
- gates_or_invariants: `--agent-teams` must fail unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; values like `0`, `false`, `yes`, and `true` are rejected (`tests/test-agent-teams.sh:54`, `tests/test-agent-teams.sh:71`). Project config can enable agent teams only if the experimental environment is set (`tests/test-agent-teams.sh:172`, `tests/test-agent-teams.sh:179`). Initial prompts must include team-leader guidance when enabled and omit it when disabled (`tests/test-agent-teams.sh:245`, `tests/test-agent-teams.sh:294`). Implementation-phase and drift-recovery next prompts must include continuation guidance, while review phase must not (`tests/test-agent-teams.sh:593`, `tests/test-agent-teams.sh:632`, `tests/test-agent-teams.sh:704`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/setup-rlcr-loop.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, and templates under `prompt-template/claude` (`tests/test-agent-teams.sh:17`, `tests/test-agent-teams.sh:21`, `tests/test-agent-teams.sh:29`, `tests/test-agent-teams.sh:427`, `tests/test-agent-teams.sh:334`, `tests/test-agent-teams.sh:350`, `tests/test-agent-teams.sh:372`). This file is included in `tests/run-all-tests.sh` under “Session ID and Agent Teams tests” (`tests/run-all-tests.sh:93`).
- edge_cases_or_failure_modes: It guards against stale teammate assumptions by checking continuation-specific template content (`tests/test-agent-teams.sh:380`). It exercises drift behavior by mutating `mainline_stall_count` and `last_mainline_verdict` before stop-hook execution (`tests/test-agent-teams.sh:635`, `tests/test-agent-teams.sh:636`). It verifies `find_active_loop` still works when `agent_teams` appears in state frontmatter, preventing new schema fields from breaking active-loop discovery (`tests/test-agent-teams.sh:391`).
- validation_or_tests: This is itself the validation artifact. It asserts template existence/size, state parser defaults, setup CLI behavior, stop-hook prompt generation, and phase routing (`tests/test-agent-teams.sh:331`, `tests/test-agent-teams.sh:350`, `tests/test-agent-teams.sh:369`, `tests/test-agent-teams.sh:417`, `tests/test-agent-teams.sh:740`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-105 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop script argument parsing and basic state transitions across setup, cancellation, comment fetching, and review polling.
- algorithmic_behavior: Defines four grouped runners: `run_setup_tests`, `run_cancel_tests`, `run_fetch_tests`, and `run_poll_tests`, then dispatches them from `run_script_tests` (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`, `tests/test-pr-loop-scripts.sh:405`). Each group shells out to the corresponding script and checks exit codes or diagnostic text. Setup tests validate `--help`, required bot flags, invalid options, `--claude`, `--codex`, both bots, `--max`, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:27`, `tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:52`, `tests/test-pr-loop-scripts.sh:66`, `tests/test-pr-loop-scripts.sh:80`, `tests/test-pr-loop-scripts.sh:92`, `tests/test-pr-loop-scripts.sh:104`, `tests/test-pr-loop-scripts.sh:131`, `tests/test-pr-loop-scripts.sh:143`).
- inputs_outputs_state: Inputs are command-line arguments to `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`, plus temporary `.humanize/pr-loop/<timestamp>/state.md` for cancellation. Outputs are pass/fail assertions. The cancellation test expects `cancel-pr-loop.sh` to create `cancel-state.md` and remove `state.md` from the active loop directory (`tests/test-pr-loop-scripts.sh:217`, `tests/test-pr-loop-scripts.sh:222`, `tests/test-pr-loop-scripts.sh:236`).
- gates_or_invariants: Setup requires at least one bot flag and rejects unknown options (`tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:52`). Numeric arguments must be integers (`tests/test-pr-loop-scripts.sh:117`). Fetch requires a numeric PR number and output file (`tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`). Poll requires PR number, `--after`, and `--bots` (`tests/test-pr-loop-scripts.sh:352`, `tests/test-pr-loop-scripts.sh:366`, `tests/test-pr-loop-scripts.sh:380`).
- dependencies_and_callers: Intended to be sourced by a broader harness because the header notes “source test-pr-loop-scripts.sh && run_script_tests” (`tests/test-pr-loop-scripts.sh:11`). It assumes `PROJECT_ROOT`, `TEST_DIR`, and test helper functions such as `pass` and `fail` exist. It exercises `scripts/setup-pr-loop.sh`, whose argument parser and prompt builder are at `scripts/setup-pr-loop.sh:111` and `scripts/setup-pr-loop.sh:718`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`.
- edge_cases_or_failure_modes: The setup flag-recognition tests intentionally allow later failure from non-git context but assert the parser did not fail with “at least one bot flag” (`tests/test-pr-loop-scripts.sh:66`, `tests/test-pr-loop-scripts.sh:80`). The cancellation tests distinguish no loop from an inactive loop directory without `state.md` (`scripts/cancel-pr-loop.sh:78`, `scripts/cancel-pr-loop.sh:91`). Fetch and poll tests stop before real GitHub API dependencies by targeting early validation failures.
- validation_or_tests: This file is the validation. It pins the CLI surface and basic PR-loop cancellation state transition; deeper API and hook behavior is covered in related PR-loop robustness/API tests, but this assigned file specifically covers script-level parsing and early validation.
- skip_candidate: `no`

### ASK_GEMINI-HZ-135 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Block-message template for the Bash validator’s `.humanize` commit-surface protection. It prevents local loop state from being accidentally staged or force-added.
- algorithmic_behavior: The template explains that `.humanize/` contains local loop state, that broad or forced `git add` commands were blocked, and gives allowed alternatives using specific paths or patch mode (`prompt-template/block/git-add-humanize.md:1`, `prompt-template/block/git-add-humanize.md:3`, `prompt-template/block/git-add-humanize.md:8`). It lists blocked examples: direct `.humanize`, `git add -A`, `git add --all`, `git add .`, and `git add -f .` (`prompt-template/block/git-add-humanize.md:16`).
- inputs_outputs_state: Inputs are none beyond being loaded as a static template by `git_add_humanize_blocked_message`; output is the stderr reason emitted by the Bash validator when `git_adds_humanize` detects a risky command. The helper falls back to equivalent inline content if the template is unavailable (`hooks/lib/loop-common.sh:1462`, `hooks/lib/loop-common.sh:1499`).
- gates_or_invariants: The invariant is that `.humanize/` loop artifacts remain local and out of version control, even when a user tries broad or force staging. The template also warns that adding `.humanize*` to `.gitignore` should be staged as `.gitignore` only, and that the commit message must avoid the literal protected string to avoid triggering protection (`prompt-template/block/git-add-humanize.md:26`, `prompt-template/block/git-add-humanize.md:34`).
- dependencies_and_callers: Called from `hooks/lib/loop-common.sh` by `git_add_humanize_blocked_message` (`hooks/lib/loop-common.sh:1462`). That helper is used by `hooks/loop-bash-validator.sh` after the command matcher `git_adds_humanize` identifies a risky git-add invocation (`hooks/loop-bash-validator.sh:229`, `hooks/loop-bash-validator.sh:235`). Template existence and safe rendering are covered by template reference/comprehensive tests (`tests/test-template-references.sh:172`, `tests/test-templates-comprehensive.sh:75`).
- edge_cases_or_failure_modes: The protection is intentionally conservative around broad add commands because `.humanize` may be ignored normally but `git add -f` bypasses ignore rules (`hooks/loop-bash-validator.sh:230`). The message’s commit-message warning is an edge case for validator pattern matching: a corrective `.gitignore` commit should not itself contain the protected literal in the message (`prompt-template/block/git-add-humanize.md:34`).
- validation_or_tests: Bash-validator tests and template tests indirectly validate this surface. The relevant validator path is `hooks/loop-bash-validator.sh:229`; the template loader safe fallback path is `hooks/lib/loop-common.sh:1499`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-165 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Block-message template for wrong-loop-directory access. It tells Claude the exact active-loop path it should use when attempting to read or write a round file from the wrong directory.
- algorithmic_behavior: The template renders `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`, then gives a shell read fallback for the attempted path (`prompt-template/block/wrong-directory-path.md:1`, `prompt-template/block/wrong-directory-path.md:3`, `prompt-template/block/wrong-directory-path.md:6`). It is a small diagnostic contract rather than a validator itself.
- inputs_outputs_state: Inputs are the action string and both paths. Output is a block reason emitted by read/write validators after they compute the expected active-loop path. No persistent state is written by the template; the validators only read state to derive the active loop and current round.
- gates_or_invariants: For RLCR round files, the active loop directory is authoritative. Read/write operations on a matching round filename in a stale or wrong directory are blocked unless the path exactly equals `ACTIVE_LOOP_DIR/<filename>` (`hooks/loop-read-validator.sh:290`, `hooks/loop-read-validator.sh:292`, `hooks/loop-write-validator.sh:350`, `hooks/loop-write-validator.sh:352`). This prevents stale loop artifacts from being used as active contract/summary/prompt files.
- dependencies_and_callers: Loaded with `load_and_render_safe` in `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh` (`hooks/loop-read-validator.sh:296`, `hooks/loop-write-validator.sh:356`). It depends on common helpers that identify round files and active loops (`hooks/lib/loop-common.sh:728`, `hooks/lib/loop-common.sh:307`). The assigned `hooks/loop-edit-validator.sh` validates round number for Edit but does not use this specific wrong-directory template; wrong-directory enforcement is read/write oriented.
- edge_cases_or_failure_modes: If the user attempts to access a file with the correct round filename but in an inactive loop directory, the validators construct the correct path from the active loop and block (`hooks/loop-read-validator.sh:287`, `hooks/loop-write-validator.sh:347`). The template includes a `cat {{FILE_PATH}}` hint for cases where the attempted path only needs inspection, but the validator still denies the controlled tool access.
- validation_or_tests: Path and hook robustness tests exercise wrong-location behavior across validators, including path-validation and plan-file hook tests. The direct template reference is discoverable in `tests/test-template-references.sh`, which scans `load_and_render_safe` calls (`tests/test-template-references.sh:83`, `tests/test-template-references.sh:172`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-195 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 task template used when initial PR comments already exist. It defines Claude’s first action contract for analyzing reviewer feedback, fixing issues, pushing changes, triggering bot re-review, and writing a resolution summary.
- algorithmic_behavior: The template orders comment analysis with human comments first and bot comments newest first (`prompt-template/pr-loop/round-0-task-has-comments.md:6`). It then instructs code inspection, fixes, and tests as needed (`prompt-template/pr-loop/round-0-task-has-comments.md:10`). After fixes, it requires commit, push, and a `gh pr comment` command using `{{PR_NUMBER}}` and `{{BOT_MENTION_STRING}}` to trigger re-review (`prompt-template/pr-loop/round-0-task-has-comments.md:15`, `prompt-template/pr-loop/round-0-task-has-comments.md:19`). It requires a resolution summary at `@{{RESOLVE_PATH}}` with issues addressed, files modified, and tests added (`prompt-template/pr-loop/round-0-task-has-comments.md:23`).
- inputs_outputs_state: Inputs are rendered variables from `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, and `BOT_MENTION_STRING` (`scripts/setup-pr-loop.sh:731`). The output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after fetched comments (`scripts/setup-pr-loop.sh:760`, `scripts/setup-pr-loop.sh:763`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:851`). It does not mutate state directly; it instructs Claude to produce commits, pushes, comments, and a `round-0-pr-resolve.md` summary.
- gates_or_invariants: The template forbids modifying `.humanize/pr-loop/` state files, requires pushing fixes so bots can review them, requires the correct bot mention format, and tells Claude to address all valid concerns (`prompt-template/pr-loop/round-0-task-has-comments.md:30`). It also explains the stop-hook gate: poll every 30 seconds for up to 15 minutes per bot, validate review content with local Codex, continue on issues, and end only when all bots approve (`prompt-template/pr-loop/round-0-task-has-comments.md:39`).
- dependencies_and_callers: Selected by `scripts/setup-pr-loop.sh` when fetched comments do not equal the “No comments found” sentinel (`scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:725`, `scripts/setup-pr-loop.sh:801`, `scripts/setup-pr-loop.sh:847`). It coordinates with `scripts/fetch-pr-comments.sh` for the comment file, `hooks/pr-loop-stop-hook.sh` for polling and validation, and PR-loop validators that block state/read-only file modification (`hooks/loop-edit-validator.sh:61`, `hooks/lib/loop-common.sh:1261`).
- edge_cases_or_failure_modes: If no comments are present, setup uses `round-0-task-no-comments.md` instead, and the “do not comment to trigger first review” flow applies (`scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:800`). If the template is missing, setup uses an inline fallback with equivalent task semantics (`scripts/setup-pr-loop.sh:803`, `scripts/setup-pr-loop.sh:847`). The required push can be blocked by dirty/unpushed checks in `pr-loop-stop-hook.sh`, so the prompt’s push requirement aligns with later stop-hook gates.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` validates setup argument parsing, while PR-loop hook/system tests cover the later stop-hook behavior. The template is also referenced by setup and scanned by template reference tests (`tests/test-template-references.sh:83`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-225 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the portable timeout abstraction used around git, GitHub CLI, Codex, Gemini, and bot operations throughout RLCR/PR-loop scripts.
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh`, checks `TIMEOUT_IMPL`, then exercises `run_with_timeout` across normal completion, timeout exit code, argument handling, exit-code preservation, pipelines, large output, rapid cycles, nonexistent command, empty command, special characters, signal handling, fallback detection, subshell execution, and export visibility (`tests/robustness/test-timeout-robustness.sh:16`, `tests/robustness/test-timeout-robustness.sh:31`, `tests/robustness/test-timeout-robustness.sh:51`, `tests/robustness/test-timeout-robustness.sh:62`, `tests/robustness/test-timeout-robustness.sh:79`, `tests/robustness/test-timeout-robustness.sh:89`, `tests/robustness/test-timeout-robustness.sh:102`, `tests/robustness/test-timeout-robustness.sh:157`, `tests/robustness/test-timeout-robustness.sh:170`, `tests/robustness/test-timeout-robustness.sh:188`, `tests/robustness/test-timeout-robustness.sh:201`, `tests/robustness/test-timeout-robustness.sh:211`, `tests/robustness/test-timeout-robustness.sh:221`, `tests/robustness/test-timeout-robustness.sh:235`, `tests/robustness/test-timeout-robustness.sh:249`, `tests/robustness/test-timeout-robustness.sh:259`).
- inputs_outputs_state: Inputs are timeout seconds and command argv vectors. Expected outputs include stdout preservation for successful commands, exit code 124 on timeout when an implementation exists, original command exit codes on normal failure, and nonzero exit for missing commands. `TIMEOUT_IMPL` is detected as one of `gtimeout`, `timeout`, `python3`, `python`, or `none` (`tests/robustness/test-timeout-robustness.sh:42`).
- gates_or_invariants: The wrapper must prefer Homebrew `gtimeout`, then GNU `timeout`, then Python, then no-timeout fallback (`scripts/portable-timeout.sh:6`, `scripts/portable-timeout.sh:10`). Timeout implementations must normalize timeout exits to 124, including Python’s `subprocess.TimeoutExpired` path (`scripts/portable-timeout.sh:47`, `scripts/portable-timeout.sh:56`). Successful command exit codes must pass through unchanged (`scripts/portable-timeout.sh:53`, `scripts/portable-timeout.sh:55`, `tests/robustness/test-timeout-robustness.sh:89`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` and `tests/test-helpers.sh` (`tests/robustness/test-timeout-robustness.sh:16`). The timeout wrapper is used by `hooks/loop-codex-stop-hook.sh` for git/Codex operations (`hooks/loop-codex-stop-hook.sh:49`, `hooks/loop-codex-stop-hook.sh:276`, `hooks/loop-codex-stop-hook.sh:1162`, `hooks/loop-codex-stop-hook.sh:1591`), `hooks/pr-loop-stop-hook.sh` for GitHub and Codex operations (`hooks/pr-loop-stop-hook.sh:56`, `hooks/pr-loop-stop-hook.sh:206`, `hooks/pr-loop-stop-hook.sh:1350`), setup scripts, `ask-codex.sh`, `ask-gemini.sh`, and bot-status scripts.
- edge_cases_or_failure_modes: The test accepts either success or timeout for zero-second timeout because platform implementations vary (`tests/robustness/test-timeout-robustness.sh:139`). Signal-handling commands may return either 124 or 0 depending on whether the wrapper or trapped process wins the race (`tests/robustness/test-timeout-robustness.sh:221`). If no timeout implementation exists, timeout tests are skipped or the wrapper runs without enforcement while warning to stderr (`scripts/portable-timeout.sh:64`, `scripts/portable-timeout.sh:66`, `tests/robustness/test-timeout-robustness.sh:75`).
- validation_or_tests: This file is the validation. It ends with `print_test_summary` and exits with the accumulated test status (`tests/robustness/test-timeout-robustness.sh:274`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item headers above; each assigned item is represented once as a `###` evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`