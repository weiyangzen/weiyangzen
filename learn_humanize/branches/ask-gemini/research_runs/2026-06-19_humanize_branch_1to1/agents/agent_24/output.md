# agent_24 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-024 `directory` `skills/humanize-refine-plan`
- cursor: `[_]`
- core_role: Skill bundle for the refine-plan flow. Recursive inspection found one contained file: `skills/humanize-refine-plan/SKILL.md`.
- algorithmic_behavior: The directory packages the algorithm contract for converting an annotated `gen-plan` document containing `CMT:` / `ENDCMT` blocks into a comment-free refined plan plus QA ledger. Since it contains only `SKILL.md`, all directory-level behavior is delegated to that skill file.
- inputs_outputs_state: Input is an annotated plan path plus optional output path, QA directory, discussion/direct mode, and optional alternate language. Outputs are a refined plan, QA ledger, optional translated variants, and a success report with paths/counts/mode/convergence status. State transitions are declared as parse args -> load config -> validate IO -> extract/classify/process comments -> refine plan -> validate refined plan -> QA generation -> optional variants -> atomic write.
- gates_or_invariants: Directory-level invariant is that this skill remains a flow definition, not a source-code implementation path. The child skill requires preserving `gen-plan` schema, removing all resolved comment blocks, using only `coding` or `analyze` task routing tags, and writing output atomically.
- dependencies_and_callers: Installed by `scripts/install-skill.sh` as one of the synced skills; tests assert the skill exists and has `user-invocable: false` in `tests/test-refine-plan.sh:820-836`. Related command surface is `commands/refine-plan.md`, which gives the executable phase-by-phase instructions and allows the validator at `commands/refine-plan.md:1-12`.
- edge_cases_or_failure_modes: Directory has no extra assets/templates, so missing `SKILL.md` would make the whole skill empty. Behavioral edge cases are inherited from `SKILL.md`: malformed/no comment blocks, missing required sections, invalid argument combinations, non-writable output/QA paths, unsupported alternate language, ambiguous comment classification.
- validation_or_tests: `tests/test-refine-plan.sh:820-836` covers installation/wiring; `tests/test-refine-plan.sh:1044-1343` covers validator behavior backing the skill contract.
- skip_candidate: `no`

### ASK_GEMINI-HZ-054 `file` `scripts/bitlesson-select.sh`
- cursor: `[_]`
- core_role: Runtime selector for BitLesson discipline. It routes a task/path scope plus BitLesson knowledge base through a model provider and returns the minimal lesson ID set to apply before implementation.
- algorithmic_behavior: Parses `--task`, `--paths`, and `--bitlesson-file` (`scripts/bitlesson-select.sh:30-69`); validates required values and file existence/non-empty content (`:71-98`); short-circuits to `LESSON_IDS: NONE` when the BitLesson file lacks `## Lesson:` entries (`:100-104`). It loads merged config, resolves `bitlesson_model`, `codex_model`, and `provider_mode` (`:13-21`), detects provider (`:110`), forces Codex when `provider_mode=codex-only` and a Claude model was configured (`:112-115`), falls back to Codex if the configured provider binary is unavailable (`:121-126`), builds a constrained selector prompt (`:143-175`), invokes Codex or Claude with a 120-second timeout (`:181-217`), then extracts and re-emits only `LESSON_IDS:` and `RATIONALE:` lines (`:237-262`).
- inputs_outputs_state: Inputs are task text, comma-separated paths, BitLesson markdown, merged config, PATH binaries, and git root detection from the BitLesson file directory (`:132-137`). Output is exactly two stable lines: `LESSON_IDS: <ids|NONE>` and `RATIONALE: <sentence>`. Internal state transitions are config-loaded -> provider-detected -> dependency-checked/fallback -> prompt-built -> model-run -> output-normalized.
- gates_or_invariants: Hard output contract at `:35-38` and `:168-173`; prompt forbids tools/repo inspection and requires using only prompt content (`:161-167`). Codex path runs read-only with low effort and project root (`:200-205`), conditionally disables Codex hooks and uses `--skip-git-repo-check` / `--ephemeral` when supported (`:187-199`). Missing model output lines are a hard format error (`:253-258`).
- dependencies_and_callers: Depends on `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh` (`:9-28`). Provider detection is implemented in `scripts/lib/model-router.sh:10-30`; dependency checks in `:32-60`; timeout fallback in `scripts/portable-timeout.sh:9-71`. Installed as a PATH shim named `bitlesson-selector` by `scripts/install-skill.sh:334-379`. Stop-hook follow-up prompts require the shim when BitLesson is enabled (`hooks/loop-codex-stop-hook.sh:1455-1467`).
- edge_cases_or_failure_modes: Unknown args or missing required args exit 1. Missing/empty BitLesson file exits 1. Placeholder/no-lesson BitLesson returns `NONE` without invoking a model. Unknown model fails via `detect_provider`. Missing Claude can fall back to Codex, but missing Codex after fallback remains fatal. Timeout exits 124 (`scripts/bitlesson-select.sh:222-225`). Unexpected model output is fatal even if the model command exits 0.
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` verifies routing to Codex and Claude, stdin prompt handoff, unknown model error, missing Codex failure, Claude-to-Codex fallback, codex-only forcing, placeholder short-circuit, and direct-helper Codex flags (`tests/test-bitlesson-select-routing.sh:137-496`). Install shim dispatch is covered in `tests/test-codex-hook-install.sh` per references from `rg`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-084 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for `detect_review_issues()`, the review-phase parser that decides whether Codex review found actionable `[P0-9]` issues or may advance to finalize.
- algorithmic_behavior: The test declares the algorithm explicitly: scan the last 50 log lines, find the first line where `[P?]` appears in the first 10 characters, extract from that line to EOF, return 0 when found and 1 when not found (`tests/test-codex-review-merge.sh:11-15`). It sources `hooks/lib/loop-common.sh` where the implementation lives (`:41-42`), creates isolated `LOOP_DIR` and `CACHE_DIR` (`:47-54`), then exercises 12 scenarios.
- inputs_outputs_state: Input fixture files are `round-N-codex-review.log` under `$CACHE_DIR`. Output is function stdout plus optional `$LOOP_DIR/round-N-review-result.md`. Exit state is part of the contract: 0 issues found, 1 no issues, 2 missing/empty log.
- gates_or_invariants: `[P0-9]` counts only when it occurs in `substr(line,1,10)`, so mid-line mentions are ignored (`:83-107`). Only the last 50 lines are scanned, so old debug output is ignored (`:199-227`) while late findings are detected (`:171-197`). Extraction starts at the first matching issue and includes subsequent content (`:229-255`).
- dependencies_and_callers: Tests `detect_review_issues()` in `hooks/lib/loop-common.sh:650-719`. Stop hook calls it in `run_and_handle_code_review()` at `hooks/loop-codex-stop-hook.sh:1196-1212`; exit 0 continues review loop, exit 1 enters finalize, exit 2 blocks as review failure.
- edge_cases_or_failure_modes: Missing log and empty log are hard errors returning 2 (`tests/test-codex-review-merge.sh:133-169`). Exactly 50 lines with first-line issue must detect (`:332-354`). A dash prefix such as `- [P1]` must detect because marker starts within first 10 chars (`:280-303`). Result file creation is required when issues are found (`:305-330`).
- validation_or_tests: The file is itself the validation surface and exits 1 if any assertions fail (`:356-368`). I inspected it only; I did not run tests in this read-only research pass.
- skip_candidate: `no`

### ASK_GEMINI-HZ-114 `file` `tests/test-stop-hook-legacy-compat.sh`
- cursor: `[_]`
- core_role: Executable specification for legacy compatibility in the RLCR stop hook, guarding git-dirty filtering and BitLesson default behavior for older loop state files.
- algorithmic_behavior: Builds temporary git repositories with `.gitignore`, a plan backup, `state.md`, round summary, and goal tracker (`tests/test-stop-hook-legacy-compat.sh:40-118`). It installs a mock `codex` binary that writes a marker when invoked (`:25-38`) and runs `hooks/loop-codex-stop-hook.sh` with hook JSON from inside each fixture repo (`:120-136`).
- inputs_outputs_state: Inputs are fixture repos, loop state frontmatter, untracked files, optional `.humanize/bitlesson.md`, and mock Codex output. Outputs are hook exit code, captured hook output, and marker file presence proving whether Codex review was reached.
- gates_or_invariants: Test 1 proves untracked `.humanize-old/` must not block the clean-tree gate (`:145-160`). Test 1b proves `.humanizeconfig` remains a normal untracked dirty file and must block, while still including both humanize-runtime and generic untracked-file notes (`:162-183`). Test 2 proves legacy states omitting `bitlesson_required` default to disabled even if `.humanize/bitlesson.md` exists (`:185-197`).
- dependencies_and_callers: The behavior under test is in `hooks/loop-codex-stop-hook.sh`: BitLesson defaults to false unless raw state contains `bitlesson_required:` (`hooks/loop-codex-stop-hook.sh:130-133`); git dirty filtering excludes only `^\?\? \.humanize[-/]` (`:625-631`) but leaves `.humanizeconfig` outside that pattern; notes are loaded from `prompt-template/block/git-not-clean-humanize-local.md` and `prompt-template/block/git-not-clean-untracked.md` (`:638-654`).
- edge_cases_or_failure_modes: Over-broad filtering would wrongly ignore `.humanizeconfig`; under-broad filtering would block on legacy `.humanize-*` directories. A regressed BitLesson fallback would block legacy loops before Codex because the fixture summary intentionally omits a BitLesson Delta section (`tests/test-stop-hook-legacy-compat.sh:87-93`).
- validation_or_tests: The script uses shared test helpers and exits with `print_test_summary` result (`:199-200`). I inspected it only; I did not run tests in this read-only research pass.
- skip_candidate: `no`

### ASK_GEMINI-HZ-144 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the pre-review incomplete-task gate. It prevents expensive Codex review and loop exit while Claude still has active/incomplete tasks.
- algorithmic_behavior: The template receives `{{INCOMPLETE_LIST}}` and renders a blocking instruction headed “Incomplete Tasks Detected” (`prompt-template/block/incomplete-todos.md:1-5`). It requires completing all remaining tasks, marking each with `TaskUpdate` status `completed`, and only then proceeding to summary/stop (`:7-10`). It explicitly forbids proceeding to Codex review until tasks are finished (`:12`).
- inputs_outputs_state: Input is the incomplete task list produced by `hooks/check-todos-from-transcript.py` via the stop hook. Output is a rendered JSON `reason` string from `hooks/loop-codex-stop-hook.sh` when TODO checker exits 1 (`hooks/loop-codex-stop-hook.sh:394-442`). State transition is attempted stop -> transcript task scan -> block before Codex review if incomplete.
- gates_or_invariants: Incomplete tasks are a hard pre-review gate. The template enforces TaskUpdate completion rather than allowing summary-only closure.
- dependencies_and_callers: Loaded by `load_and_render_safe "$TEMPLATE_DIR" "block/incomplete-todos.md"` in `hooks/loop-codex-stop-hook.sh:420-431`. Falls back to inline `# Incomplete Tasks` text if the file is unavailable (`:425-429`).
- edge_cases_or_failure_modes: If the TODO checker parse fails, the stop hook uses a separate parse-error block instead of this template (`hooks/loop-codex-stop-hook.sh:401-418`). If the template is missing, fallback text is less strict and omits the TaskUpdate-specific details.
- validation_or_tests: Covered indirectly by stop-hook/task-checker behavior; `rg` shows the template is consumed only by the stop hook. No direct assigned test file targets this template.
- skip_candidate: `no`

### ASK_GEMINI-HZ-174 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Prompt template that defines the RLCR transition after Code Review passes and before final loop completion. It gates final exit through a simplification/refactor phase.
- algorithmic_behavior: Declares Codex review passed and instructs Claude to use the `code-simplifier:code-simplifier` agent via Task (`prompt-template/claude/finalize-phase-prompt.md:1-14`). It constrains changes to functionality-equivalent simplification and requires tests not fail (`:16-24`). It focuses review on recent branch changes between `{{BASE_BRANCH}}` and `{{START_BRANCH}}` (`:25-34`), references plan and goal tracker placeholders (`:36-40`), and requires completion of `[mainline]` and `[blocking]` tasks, committing changes, and writing `{{FINALIZE_SUMMARY_FILE}}` (`:41-52`).
- inputs_outputs_state: Inputs are template variables `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a rendered stop-hook block reason that keeps the loop active in finalize state. State transition occurs when review has no `[P?]` issues: stop hook renames `state.md` to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:1215-1224`) and renders this template on the non-skipped path (`:1262-1288`).
- gates_or_invariants: Finalize changes must preserve functionality, pass existing tests, avoid new bugs, and be simplification-only. Final exit later requires `finalize-summary.md` and a clean/pushed repo; the finalize phase itself skips further Codex review and completes when all checks pass (`hooks/loop-codex-stop-hook.sh:721-755`, `:908-924`).
- dependencies_and_callers: Called by `enter_finalize_phase()` via `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-prompt.md"` (`hooks/loop-codex-stop-hook.sh:1282-1287`). The non-skipped path is reached from `run_and_handle_code_review()` when `detect_review_issues` returns no issues (`:1208-1212`) or initial review passes after COMPLETE (`:1760-1805`).
- edge_cases_or_failure_modes: A separate skipped-review template is used when code review cannot run (`hooks/loop-codex-stop-hook.sh:1227-1260`), so this template specifically represents validated-review success. If `code-simplifier` is unavailable, the template still requires the agent by name; fallback text in the hook is weaker but equivalent in phase purpose.
- validation_or_tests: Finalize behavior is covered broadly by `tests/test-finalize-phase.sh` per search references; this specific template is referenced by stop-hook line coverage and prompt-template lookup. I inspected the template and caller, not runtime execution.
- skip_candidate: `no`

### ASK_GEMINI-HZ-204 `file` `skills/humanize-refine-plan/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction file defining the refine-plan algorithm contract: convert annotated implementation plans into clean `gen-plan` artifacts plus QA evidence.
- algorithmic_behavior: Frontmatter names the skill, marks it as a `flow`, and sets `user-invocable: false` (`skills/humanize-refine-plan/SKILL.md:1-6`). The Mermaid flow defines setup, config load, validator call, CMT extraction, parse failure reporting, classification, optional user question in discussion mode, processing, plan validation/repair, QA generation, optional variants, atomic write, and success report (`:18-48`). Workflow guarantees require preserving schema, removing all resolved CMT blocks, preserving required/optional sections, restricting routing tags, generating QA from the shipped template, and atomic writes (`:67-87`).
- inputs_outputs_state: Required input is `--input <annotated-plan>` with optional `--output`, `--qa-dir`, `--alt-language`, `--discussion`, and `--direct` (`:50-61`). Output is a refined plan plus QA ledger recording each raw `CMT-N`, classification, answers, research, applied changes, remaining decisions, metadata, and convergence (`:88-107`). Alternate-language state maps names/codes to suffixes `_zh` through `_ar`, with English as no-op (`:108-130`).
- gates_or_invariants: `--discussion` and `--direct` are mutually exclusive; the validator must not receive `--alt-language`; in-place output is allowed but still writes QA separately (`:62-65`). Required sections include Goal Description, Acceptance Criteria, Path Boundaries, Feasibility Hints and Suggestions, Dependencies and Sequence, Task Breakdown, Claude-Codex Deliberation, Pending User Decisions, and Implementation Notes (`:71-82`). Exit-code contract for validator is `0` success, `1` missing input, `2` empty input, `3` no CMT blocks, `4` missing sections, `5` bad output dir, `6` bad QA dir, `7` invalid args (`:131-143`).
- dependencies_and_callers: Declared validator dependency is `{{HUMANIZE_RUNTIME_ROOT}}/scripts/validate-refine-plan-io.sh` (`:20-23`). Command implementation details in `commands/refine-plan.md` mirror the skill: allowed validator tool (`commands/refine-plan.md:1-12`), sequential phases (`:30-43`), validator exclusion of `--alt-language` (`:72-77`), config precedence (`:86-99`), exit handling (`:157-180`), and stateful scanner requirements (`:184-220`). Runtime validator implements CMT/section scanning in `scripts/validate-refine-plan-io.sh:16-373` and path/exit-code checks in `:375-585`.
- edge_cases_or_failure_modes: Malformed/nested/unterminated CMT blocks, CMT markers inside HTML comments or fences, empty CMT blocks, missing schema sections inside ignored regions, invalid language values, invalid argument pairs, unwritable in-place input directory, unwritable/missing output directory, and invalid QA path. Ambiguous classifications route to user question in discussion mode or smallest safe assumption in direct mode with QA recording (`skills/humanize-refine-plan/SKILL.md:30-35`, `:59-60`).
- validation_or_tests: `tests/test-refine-plan.sh:820-836` verifies skill install/wiring; `tests/test-refine-plan.sh:1044-1343` verifies validator exit codes, ignored regions, malformed CMT diagnostics, required sections, writable directories, QA auto-create, mode reporting, and valid in-place/new-file invocations.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`