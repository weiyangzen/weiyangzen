# agent_16 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-016 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side RLCR review prompt templates. This directory defines the reviewer contract used when the loop asks Codex to audit Claude’s implementation, detect drift, classify issues, update goal-tracker state, and decide whether the loop can terminate.
- algorithmic_behavior: Contains four templates:
  - `prompt-template/codex/code-review-phase.md:1` documents the `codex review --base {{BASE_BRANCH}}` phase, where output is scanned for `[P0-9]` severity markers and the loop either returns remediation feedback or transitions to finalize (`lines 12-18`).
  - `prompt-template/codex/regular-review.md:1` is the normal round review prompt. It requires reading the original plan (`lines 3-11`), performing implementation review (`lines 20-31`), auditing the goal tracker (`lines 33-45`), classifying findings into Mainline Gaps, Blocking Side Issues, and Queued Side Issues (`lines 47-52`), and emitting the mandatory `Mainline Progress Verdict` (`lines 54-59`).
  - `prompt-template/codex/full-alignment-review.md:1` is the periodic deep audit. It adds acceptance-criteria tabulation (`lines 19-29`), forgotten/deferred item checks (`lines 30-48`), mainline drift audit (`lines 50-64`), historical progress/stagnation analysis (`lines 75-100`), and stricter terminal output rules (`lines 102-112`).
  - `prompt-template/codex/goal-tracker-update-section.md:1` is an inserted responsibility block telling Codex to mutate only the mutable section of `goal-tracker.md`, never the immutable section (`lines 3-18`).
- inputs_outputs_state: Inputs are template variables such as `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, `{{SUMMARY_CONTENT}}`, `{{DOCS_PATH}}`, `{{CURRENT_ROUND}}`, `{{REVIEW_RESULT_FILE}}`, `{{LOOP_TIMESTAMP}}`, `{{PREV_ROUND}}`, and `{{GOAL_TRACKER_UPDATE_SECTION}}`. Outputs are review text written to `@{{REVIEW_RESULT_FILE}}`, optional goal-tracker edits to `@{{GOAL_TRACKER_FILE}}`, a final sentinel line (`COMPLETE` or `STOP`), and classified action items that feed the next Claude round.
- gates_or_invariants: `COMPLETE` is permitted only when all original plan tasks/ACs are fully met with no deferrals or pending work (`regular-review.md:70-74`, `full-alignment-review.md:104-112`). `STOP` is reserved for stagnation in full alignment rounds (`full-alignment-review.md:92-100`). The `Mainline Progress Verdict` line is mandatory and enforced by the Humanize stop hook (`regular-review.md:54-59`, `full-alignment-review.md:57-64`). Deferrals are treated as incomplete, not accepted progress (`regular-review.md:24-31`, `full-alignment-review.md:110-112`). The goal tracker immutable section must not be modified (`goal-tracker-update-section.md:5-10`).
- dependencies_and_callers: These templates are loaded through the template system (`hooks/lib/template-loader.sh` includes `codex` as a required subdir) and used by RLCR stop/review phases. They coordinate with sibling `prompt-template/claude` prompts by defining what Codex sends back to Claude, and with `.humanize/rlcr/<timestamp>/` artifacts: `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, and `goal-tracker.md` (`full-alignment-review.md:79-89`).
- edge_cases_or_failure_modes: The templates explicitly handle claimed completion that does not match code reality, deferred work, pending work, forgotten plan items, repeated review findings, circular progress, and no-code-change stagnation. A missing `Mainline Progress Verdict` is a stop-hook failure. A false `COMPLETE` would prematurely stop the loop, so both review templates make completion conditions narrow and explicit.
- validation_or_tests: Template reference/comprehensive tests exist elsewhere, and stop-hook behavior refers to the required verdict line. For this assigned directory, the strongest executable connection is that `regular-review.md` and `full-alignment-review.md` specify sentinel outputs and verdict strings consumed by RLCR stop-hook logic.
- skip_candidate: `no`

### ASK_GEMINI-HZ-046 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit hook that guards active RLCR sessions before the next prompt proceeds. It validates state schema, branch immutability, and plan-file tracking cleanliness so the loop cannot continue from stale or inconsistent plan state.
- algorithmic_behavior: Reads hook JSON from stdin (`line 27`), extracts `session_id` (`line 30`), finds the active session-matched RLCR loop under `.humanize/rlcr` (`lines 32-34`), and exits allow when no active loop exists (`lines 36-39`). It resolves the active state file (`line 42`), parses it strictly and fail-closed on malformed state (`lines 44-48`), maps parsed state fields (`lines 50-53`), checks required schema fields `plan_tracked` and `start_branch` (`lines 79-89`), verifies the current git branch matches `start_branch` (`lines 95-115`), then enforces one of two plan-file modes: tracked-and-clean (`lines 123-192`) or untracked/gitignored (`lines 193-228`).
- inputs_outputs_state: Inputs are hook stdin JSON, `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr/<loop>/state.md`/`finalize-state.md`/`methodology-analysis-state.md`, git branch/tracking/status, and fields parsed by `parse_state_file_strict`: `STATE_PLAN_TRACKED`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`. Outputs are either silent `exit 0` allow, shell `exit 1` on malformed state, or JSON `{"decision":"block","reason":...}` for user-correctable gates. The hook does not mutate state.
- gates_or_invariants: Active loop state must be strict-parseable (`lines 44-48`). `plan_tracked` and `start_branch` must be present (`lines 79-89`). Branch switching during an RLCR loop is forbidden (`lines 107-115`). If `plan_tracked=true`, the plan file must still be tracked and have empty `git status --porcelain` (`lines 123-192`). If `plan_tracked=false`, the plan file must not become tracked (`lines 193-228`). Git timeouts or unexpected git errors block rather than allow (`lines 95-105`, `129-147`, `154-172`, `199-216`).
- dependencies_and_callers: Registered in `hooks/hooks.json` under `UserPromptSubmit`. Sources `hooks/lib/loop-common.sh` (`line 17`) for session extraction, active-loop resolution, constants, strict parsing, and template directory state; sources `scripts/portable-timeout.sh` (`line 21`) for bounded git operations. Uses `prompt-template/block/schema-outdated.md` through `load_and_render_safe` (`lines 60-76`). Setup-side fields are produced by `scripts/setup-rlcr-loop.sh`, which writes `plan_tracked` and `start_branch` into state.
- edge_cases_or_failure_modes: No active loop is allowed to pass. Malformed state exits with stderr and code 1 rather than a JSON block (`lines 44-48`). Missing schema fields return a JSON block and `exit 0` (`lines 60-88`). Git command timeout uses exit `124` and produces a timeout-specific block. A tracked plan file removed from git, a tracked plan with local modifications, and an untracked-mode plan that becomes tracked each block with distinct messages. Quoted `start_branch` and `plan_file` are handled in shared parsing (`hooks/lib/loop-common.sh:390-395`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers this hook extensively, including missing schema fields, branch mismatch, quoted branches, and plan tracking. `tests/robustness/test-hook-system-robustness.sh` also invokes this validator for active-loop and malformed-input behavior. The assigned hook itself is implementation, not just tests.
- skip_candidate: `no`

### ASK_GEMINI-HZ-076 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR validator allowlist behavior. It defines which legacy/generated round files may bypass normal round-file blocking across Read, Write, Edit, and Bash validators, and verifies direct stop-hook execution cannot be invoked manually through Bash.
- algorithmic_behavior: Creates a temporary git repo and active RLCR state with `current_round: 5`, `plan_tracked: false`, `start_branch`, `base_branch`, and `review_started: false` (`lines 29-64`). First directly tests `is_allowlisted_file()` (`lines 66-134`). Then runs actual hook scripts with JSON PreToolUse payloads for Write (`lines 137-219`), Edit (`lines 221-288`), Read (`lines 290-370`), Bash path-restricted todo writes (`lines 372-482`), and Bash direct hook wrapper blocking (`lines 484-575`). It exits with the number of failures (`lines 577-585`).
- inputs_outputs_state: Inputs are synthetic hook JSON payloads, a temporary `.humanize/rlcr/2024-01-01_12-00-00/state.md`, `CLAUDE_PROJECT_DIR`, and the repository hook scripts. Outputs are PASS/FAIL lines and final process exit code equal to `TESTS_FAILED`. The test mutates only its temp directory.
- gates_or_invariants: The allowlist is exact-path, active-loop scoped. Allowed by `is_allowlisted_file()` are only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop dir (`tests lines 72-102`; implementation `hooks/lib/loop-common.sh:754-768`). `round-3-todos.md`, `round-2-summary.md`, `round-0-contract.md`, and matching basenames in wrong directories must be blocked (`lines 104-134`). Current round contracts are allowed through Write/Edit/Read (`lines 169-180`, `251-262`, `320-331`), but stale contracts are blocked (`lines 208-219`, `264-275`, `359-370`). Bash may modify only `round-1/2-todos.md` with a full active-loop absolute path (`lines 376-399`) and must block contracts, wrong dirs, stale todos, relative basenames, old loop dirs, and same-basename different roots (`lines 402-482`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` directly (`line 17`) and invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh`. The expected implementation branches are `loop-write-validator.sh:59-67` and `329-343`, `loop-edit-validator.sh:42-50` and `254-268`, `loop-read-validator.sh:59-67` and `271-284`, and `loop-bash-validator.sh:534-557`. The direct hook execution block is implemented by `loop-bash-validator.sh:172-189` with a shared message in `hooks/lib/loop-common.sh:1502-1514`.
- edge_cases_or_failure_modes: Security edge cases include allowlisted basename in wrong directory, old loop directory, same timestamp basename under a different root, generic relative filenames, and multiple command wrappers around direct hook execution (`env`, `command`, assignment prefixes, `timeout`, `nice`, `nohup`, `strace`, `ltrace`, combinations). The file also implicitly guards macOS/BSD portability by using shell/json hook execution rather than framework-specific mocks.
- validation_or_tests: This file is itself the validation artifact. It tests 39 named behaviors (`lines 72-575`) and fails the suite if any pass/fail expectation fails (`line 585`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-106 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for the PR-loop Stop Hook state machine. It models PR review polling, trigger validation, force-push protection, bot timeout handling, dynamic startup-case changes, fork PR lookup, and per-bot active-bot transitions.
- algorithmic_behavior: Defines `run_stophook_tests()` (`line 15`) and then creates isolated `.humanize/pr-loop/<timestamp>/state.md` fixtures plus mocked `gh`, `git`, and sometimes `codex` binaries. The invoked tests at `lines 1768-1780` cover force-push old-trigger rejection, startup Case 1 no-trigger exception, empty `active_bots` approval, unpushed-commit block, history-rewrite force-push block, missing trigger block, timeout auto-removal, Codex `+1` approval, Claude eyes timeout, dynamic `startup_case` persistence/change, fork PR base repo resolution, and mixed approval/issue behavior.
- inputs_outputs_state: Inputs are PR loop state frontmatter fields: `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, `codex_timeout`, `poll_interval`, `poll_timeout`, `started_at`, `last_trigger_at`, `trigger_comment_id`, `startup_case`, `latest_commit_sha`, and `latest_commit_at`. The hook consumes mocked GitHub API responses, local git state, `round-N-pr-resolve.md`, bot comments/reactions, and Codex validation output. Outputs are JSON block/allow decisions, terminal state file renames such as `approve-state.md`, updated `state.md`, generated `round-N-pr-check.md`/feedback files, and goal-tracker updates.
- gates_or_invariants: The spec asserts the hook must reject trigger comments older than `latest_commit_at` after force push (`lines 20-135`), allow round 0 Case 1 without a trigger while waiting for automatic review (`lines 140-241`), create `approve-state.md` when `active_bots` is empty (`lines 246-324`), block unpushed commits before polling (`lines 452-535`), block history rewrites and require re-trigger (`lines 538-640`), require trigger comments for startup Case 4/5 or later rounds (`lines 642-752`), remove timed-out bots and complete if none remain (`lines 754-893`), treat Codex `+1` as approval (`lines 896-1009`), require Claude eyes reaction after a trigger when Claude is configured (`lines 1011-1143`), preserve/update `startup_case` after new comments (`lines 1145-1388`), resolve fork PRs through parent/base repo (`lines 1390-1517`), and not mark mixed approvals as fully resolved (`lines 1519-1763`).
- dependencies_and_callers: Executes `hooks/pr-loop-stop-hook.sh`. That hook parses state (`hooks/pr-loop-stop-hook.sh:88-177`), resolves fork/base repo (`198-237`), checks PR state (`239-256`), requires resolve file (`258-277`), checks clean/pushed git state (`279-361`), detects force push (`364-424`), detects/freshens trigger comments (`450-607`), decides trigger requirements (`609-644`), handles Codex `+1` (`646-719`, `985-1039`), validates Claude eyes (`749-800`), polls via `scripts/poll-pr-reviews.sh` (`802-1063`), writes comments and runs Codex (`1182-1375`), and updates active bots/state/goal tracker (`1433-1605`). It uses helper mappings from `hooks/lib/loop-common.sh` and reaction/poll scripts.
- edge_cases_or_failure_modes: Covers stale triggers after force push, new commits invalidating old triggers, missing trigger comments, unpushed commits, no upstream/PR head fallback, GitHub fork PR lookup, bot timeout completion, Codex approval by reaction instead of comment, Claude bot missing eyes reaction, comments arriving after loop startup, mixed approval where one bot approves and another has issues, and preventing resolved-count inflation when any issues remain (`hooks/pr-loop-stop-hook.sh:1536-1542`).
- validation_or_tests: This file is the validation artifact. It invokes 12 test functions (`lines 1768-1780`). One earlier helper-like test `test_stophook_dynamic_startup_case()` is defined at `lines 327-450` but is not invoked; the later `test_stophook_dynamic_startup_case_update()` is the active dynamic-case test.
- skip_candidate: `no`

### ASK_GEMINI-HZ-136 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Block-message template for a special git-cleanliness case where `.humanize/` is present and should be ignored rather than committed.
- algorithmic_behavior: Provides human-facing remediation text: `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` (`lines 2-8`). It is a decision-support template, not an executable gate by itself.
- inputs_outputs_state: Inputs are none in this concrete file; it has no template variables. Output is markdown text embedded in a blocking response by a hook or gate when local `.humanize` dirtiness is detected.
- gates_or_invariants: The invariant is that `.humanize/` runtime state is private loop state and must not enter git history. The template recommends adding `.humanize*` to `.gitignore` and staging only `.gitignore`, preserving loop artifacts as uncommitted local state.
- dependencies_and_callers: Related to git-cleanliness checks in RLCR/PR hooks and shared git-add-humanize messaging. Nearby shared messaging in `hooks/lib/loop-common.sh:1490-1499` reinforces the same invariant and warns about commit-message triggering. This assigned template is a prompt/block surface used by the template loader when the corresponding block key is requested.
- edge_cases_or_failure_modes: If this message is surfaced at the wrong time, an agent might try to commit `.humanize` artifacts. The command snippet stages `.gitignore` but intentionally does not commit, leaving final commit policy to the surrounding loop. It also uses `.humanize*`, which covers `.humanize` and similarly prefixed local folders.
- validation_or_tests: Template-reference tests elsewhere likely ensure block templates are loadable. This specific file is tiny and declarative; its behavior is validated indirectly by hooks that choose this block.
- skip_candidate: `no`

### ASK_GEMINI-HZ-166 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR Read validator path correction when an agent tries to read a round file from the wrong location.
- algorithmic_behavior: Tells the agent it is trying to read `{{FILE_PATH}}` while loop files are in `{{ACTIVE_LOOP_DIR}}/`, lists current prompt and summary paths for `{{CURRENT_ROUND}}`, and gives a direct `cat {{FILE_PATH}}` instruction if the file is still needed (`lines 1-9`).
- inputs_outputs_state: Inputs are `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Output is markdown used as stderr/block reason. It does not mutate state.
- gates_or_invariants: Enforces that current round files are canonical under the active loop directory, not stale or copied paths. In implementation, `hooks/loop-read-validator.sh` invokes this template when a round prompt/summary/contract path is outside `.humanize/rlcr` (`loop-read-validator.sh:255-264`), and then separately validates round number unless allowlisted (`loop-read-validator.sh:271-284`).
- dependencies_and_callers: Called through `load_and_render_safe` in `hooks/loop-read-validator.sh:260-263`. It coordinates with sibling block templates such as `wrong-round-file.md`, `wrong-round-number.md`, and location templates for writes/contracts. It depends on active-loop discovery and current round parsing from `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: The wording says “If you need this file, use: `cat {{FILE_PATH}}`” even though the validator has just blocked a Read tool attempt; this is useful for Bash/manual access contexts but could be confusing if the same path remains guarded by another validator. It is a skip candidate only if assessing pure algorithm code, but it is core to the hook’s correction contract.
- validation_or_tests: `tests/test-templates-comprehensive.sh` references wrong-file-location rendering, and read-validator tests exercise wrong-location blocks. The assigned file is declarative but part of the validation surface for path-gate UX.
- skip_candidate: `no`

### ASK_GEMINI-HZ-196 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 task template for startup Case 1: no review comments exist yet, so the worker should not manually trigger bots and should let the Stop Hook poll for automatic bot reviews.
- algorithmic_behavior: Instructs the agent that monitored bots `{{ACTIVE_BOTS_DISPLAY}}` will automatically review and that no trigger comment is needed (`lines 4-10`). Requires writing an initial summary to `@{{RESOLVE_PATH}}` noting round 0 is awaiting reviews (`lines 12-15`). Forbids commenting to trigger review and forbids modifying `.humanize/pr-loop/` state files (`lines 18-23`). Explains the Stop Hook will poll every 30 seconds up to 15 minutes per bot, run local Codex validation on approvals, continue on issues, and end on all approvals (`lines 26-30`).
- inputs_outputs_state: Inputs are `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH`. Output is the agent’s `round-0-pr-resolve.md` summary at `RESOLVE_PATH`; subsequent state transitions are handled by the Stop Hook. The template itself does not mutate state.
- gates_or_invariants: Invariant: first reviews are automatic in no-comment startup, so user/agent must not add a PR comment to trigger bots (`lines 6-10`, `20`). State files under `.humanize/pr-loop/` are system-managed and must not be modified (`line 21`). Stop Hook is the only component that polls and decides loop continuation/completion (`lines 22`, `26-30`).
- dependencies_and_callers: Generated by PR loop setup for round 0 when no comments exist. It aligns with `hooks/pr-loop-stop-hook.sh` trigger-requirement logic: round 0 with `startup_case=1` does not require a trigger (`pr-loop-stop-hook.sh:613-633`), and the assigned test confirms this exception (`tests/test-pr-loop-stophook.sh:140-241`). It coordinates with `scripts/setup-pr-loop.sh`, sibling templates for “has comments” and critical requirements, and the PR stop hook’s polling/Codex validation flow.
- edge_cases_or_failure_modes: If bots do not auto-review, the Stop Hook may time out and block or auto-remove timed-out bots depending state and timeout handling. If an agent comments anyway, it may alter startup-case/trigger semantics. If the agent edits state files, validators should block those writes, but this template prevents the behavior at prompt level.
- validation_or_tests: `tests/test-pr-loop-stophook.sh` validates the no-trigger Case 1 behavior (`lines 140-241`) and broader Stop Hook polling behavior. PR loop validators also protect state files and read-only PR-loop artifacts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ASK_GEMINI-HZ-016`, `ASK_GEMINI-HZ-046`, `ASK_GEMINI-HZ-076`, `ASK_GEMINI-HZ-106`, `ASK_GEMINI-HZ-136`, `ASK_GEMINI-HZ-166`, `ASK_GEMINI-HZ-196`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`