# agent_19 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-019 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer used by Humanize commands, hooks, monitors, and model-selection helpers. Recursive inventory under `scripts/lib` contains exactly four files: `config-loader.sh`, `model-router.sh`, `monitor-common.sh`, and `monitor-skill.sh`.
- algorithmic_behavior: `config-loader.sh` performs ordered JSON config merging via `jq`: required default config, optional user config, optional project config, with null stripping and later-layer override semantics at `scripts/lib/config-loader.sh:63-136`. `model-router.sh` maps model names to Codex or Claude providers and validates provider binaries/effort mappings at `scripts/lib/model-router.sh:10-90`. `monitor-common.sh` provides reusable terminal, state, YAML, phase, and tracker parsers at `scripts/lib/monitor-common.sh:32-546`. `monitor-skill.sh` builds a live or one-shot monitor for `.humanize/skill` invocations, including tool filtering, cache lookup, status aggregation, and output tailing at `scripts/lib/monitor-skill.sh:18-527`.
- inputs_outputs_state: Inputs are shell arguments, config files, `.humanize/*` runtime state directories, frontmatter markdown files, and CLI availability. Outputs are merged JSON, provider names, display strings, pipe-delimited monitor counters, rendered terminal status, and selected monitored file paths. State is read from timestamped session dirs, `metadata.md`, `input.md`, `output.md`, cache logs, `state.md`, and `goal-tracker.md`; this directory mostly reads state rather than mutating it.
- gates_or_invariants: Config loading requires `jq` and treats the default config as mandatory/malformed-fatal while optional configs warn and degrade to `{}` at `scripts/lib/config-loader.sh:40-60`, `scripts/lib/config-loader.sh:109-132`. Provider routing rejects unknown model names and invalid effort values at `scripts/lib/model-router.sh:13-29`, `scripts/lib/model-router.sh:75-82`. Skill monitor accepts only timestamp-shaped invocation directories and applies optional `codex`/`gemini` filtering at `scripts/lib/monitor-skill.sh:77-90`.
- dependencies_and_callers: `scripts/humanize.sh` sources `monitor-common.sh` on startup and `monitor-skill.sh` near the end at `scripts/humanize.sh:6-16`, `scripts/humanize.sh:1660-1663`; its monitor subcommands call `_humanize_monitor_skill` with optional Codex/Gemini filters at `scripts/humanize.sh:1171-1190`. `scripts/bitlesson-select.sh` sources `config-loader.sh` and `model-router.sh`, then uses `load_merged_config`, `get_config_value`, and `detect_provider` at `scripts/bitlesson-select.sh:9-21`, `scripts/bitlesson-select.sh:110-125`. `hooks/lib/loop-common.sh` also sources `config-loader.sh` for hook/runtime defaults at `hooks/lib/loop-common.sh:171-209`.
- edge_cases_or_failure_modes: Missing/malformed required default config stops config loading; malformed optional configs warn and are ignored. `model-router.sh` is limited to Codex/Claude naming and rejects newer/other provider names. Monitor parsing uses simple frontmatter and table heuristics, so malformed markdown can produce zero/default counts. `monitor-skill.sh` falls back through global cache, local cache, and project-local files, and treats legacy unknown tool invocations as Codex when filtering at `scripts/lib/monitor-skill.sh:66-75`, `scripts/lib/monitor-skill.sh:181-217`.
- validation_or_tests: Direct tests include `tests/test-config-merge.sh`, `tests/test-config-error-handling.sh`, `tests/test-model-router.sh`, `tests/test-skill-monitor.sh`, and `tests/test-pr-loop-system.sh`; repository search shows those tests source or assert against this directory’s functions. I did not run tests because this assignment requested read-only research notes.
- skip_candidate: `no`

### ASK_GEMINI-HZ-049 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse write gate for RLCR and PR loop state machines. It validates Claude `Write` tool destinations before file creation/modification and blocks writes that would corrupt loop state, generated prompts, summaries, contracts, todos, goal trackers, methodology-analysis state, or PR loop protected files.
- algorithmic_behavior: Reads JSON hook input from stdin, validates JSON/tool structure, rejects overly deep JSON, exits early for non-`Write` tools, and requires `tool_input.file_path` for writes at `hooks/loop-write-validator.sh:26-49`. It lowercases the path, extracts `session_id`, and uses session-aware active-loop discovery at `hooks/loop-write-validator.sh:49-53`, `hooks/loop-write-validator.sh:107-115`, `hooks/loop-write-validator.sh:182-185`. It blocks round todos unless allowlisted, blocks prompt writes, handles PR loop write protection, restricts methodology-analysis writes to two allowed artifact names, parses current loop state, blocks state-file writes, handles finalize-summary exceptions, blocks plan backup writes, enforces goal-tracker location/immutable-section rules, blocks summaries/contracts outside the active loop, validates round numbers, then validates exact directory placement at `hooks/loop-write-validator.sh:59-363`.
- inputs_outputs_state: Input is a Claude hook JSON object with `tool_name`, `tool_input.file_path`, optional `tool_input.content`, and optional `session_id`. Outputs are exit codes: `0` allow, `1` malformed/unsafe hook input or malformed state, `2` blocked operation with rendered explanatory message. Runtime state is read from `$CLAUDE_PROJECT_DIR/.humanize/rlcr`, `.humanize/pr-loop`, `state.md`, `finalize-state.md`, `methodology-analysis-state.md`, and `goal-tracker.md`.
- gates_or_invariants: Only active-loop paths may receive summaries/contracts; current round must match the active state unless the path is allowlisted at `hooks/loop-write-validator.sh:325-343`. State files are system-managed and always blocked for `Write` at `hooks/loop-write-validator.sh:210-223`. Finalize phase has no active contract, so contract writes are blocked at `hooks/loop-write-validator.sh:238-242`. Goal tracker writes after round 0 must target the active loop’s tracker and preserve the immutable section at `hooks/loop-write-validator.sh:261-283`. During methodology analysis, only `methodology-analysis-report.md` and `methodology-analysis-done.md` are writable at `hooks/loop-write-validator.sh:115-152`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-write-validator.sh:18-20`. Core dependencies include `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, path classifiers, `goal_tracker_mutable_update_allowed`, PR loop classifiers, and `load_and_render_safe`. These are defined in `hooks/lib/loop-common.sh`, including hook JSON validation at `hooks/lib/loop-common.sh:91-165`, active-loop discovery at `hooks/lib/loop-common.sh:245-365`, strict state parsing at `hooks/lib/loop-common.sh:474-540`, round/allowlist helpers at `hooks/lib/loop-common.sh:721-768`, goal-tracker immutability at `hooks/lib/loop-common.sh:845-894`, and PR loop helpers at `hooks/lib/loop-common.sh:1217-1315`. It is registered as a hook command in `hooks/hooks.json:20` per search evidence.
- edge_cases_or_failure_modes: Fail-closed behavior occurs for malformed hook JSON, missing `file_path`, deep JSON, malformed active state, and missing `content` when validating mutable goal-tracker writes. Path resolution handles missing target files by resolving parent directories when `realpath` cannot resolve the full path at `hooks/loop-write-validator.sh:116-139`. A known limitation is spawned agents with different `session_id` are not restricted by this methodology-analysis hook path and rely on prompt sanitization, documented at `hooks/loop-write-validator.sh:109-113`. Exact string path comparison for finalize summary and final directory validation can reject semantically equivalent but differently normalized paths except where `_normalize_path` is explicitly used for goal trackers.
- validation_or_tests: Direct and indirect tests call this file in `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-allowlist-validators.sh`, `tests/test-pr-loop-hooks.sh`, and robustness tests, per repository search. `tests/test-template-references.sh` also verifies validators use safe template rendering. I inspected but did not run tests.
- skip_candidate: `no`

### ASK_GEMINI-HZ-079 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `command_modifies_file`, the shell-command write detector used by `hooks/loop-bash-validator.sh` to block Bash-based modifications to protected loop files.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, lowercases each command, and asserts whether `command_modifies_file "$command_lower" "$pattern"` should match at `tests/test-bash-validator-patterns.sh:11-62`. It enumerates command classes: redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec`, benign read commands, and file-type-specific patterns for state and summary files at `tests/test-bash-validator-patterns.sh:69-194`.
- inputs_outputs_state: Inputs are hardcoded command strings and regex file patterns, defaulting to `goal-tracker\.md`. Outputs are PASS/FAIL lines and process exit `0` only when all assertions pass at `tests/test-bash-validator-patterns.sh:197-214`. It does not create loop state; it only evaluates pattern logic.
- gates_or_invariants: The spec requires literal writes such as `echo x > goal-tracker.md`, `tee -a goal-tracker.md`, `sed -i`, `perl -i`, `mv src goal-tracker.md`, `rm goal-tracker.md`, and `dd of=goal-tracker.md` to be treated as modifications at `tests/test-bash-validator-patterns.sh:75-133`. It requires read-only commands like `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` not to match at `tests/test-bash-validator-patterns.sh:142-152`.
- dependencies_and_callers: Depends on `to_lower` and `command_modifies_file` from `hooks/lib/loop-common.sh`, where the detector’s regex list is implemented at `hooks/lib/loop-common.sh:1516-1544`. Runtime caller is `hooks/loop-bash-validator.sh`, which invokes this detector to protect state files, plan backup, goal tracker, prompt, summary, contract, todos, and PR loop files at `hooks/loop-bash-validator.sh:259-278`, `hooks/loop-bash-validator.sh:484-549`, `hooks/loop-bash-validator.sh:571-602`.
- edge_cases_or_failure_modes: The test documents a known limitation: multi-source copy such as `cp file1.md file2.md goal-tracker.md` is not detected because the pattern expects `cp src dest` format at `tests/test-bash-validator-patterns.sh:165-168`. Variable-based paths such as `echo x > $FILE` intentionally do not match because the detector needs a literal protected filename at `tests/test-bash-validator-patterns.sh:170-172`.
- validation_or_tests: This file is itself the validation artifact. It ends with a failure count gate and exits nonzero if any pattern expectation fails at `tests/test-bash-validator-patterns.sh:197-214`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-109 `file` `tests/test-refine-plan.sh`
- cursor: `[_]`
- core_role: Large executable contract for the hidden `refine-plan` command, its IO validator, comment scanning/classification model, QA template coverage, language variant rules, atomic write requirements, and install/packaging wiring.
- algorithmic_behavior: Defines assertion helpers and fixture generators for valid plans, no-comment plans, missing-section plans, section headings hidden inside code fences/HTML comments, mixed real/ignored sections, and goal-body variants at `tests/test-refine-plan.sh:63-421`. It includes a reference comment scanner that tracks fenced code, HTML comments, active `CMT:` blocks, inline preservation, and non-empty comment counting at `tests/test-refine-plan.sh:427-530`. It defines reference classifiers and language/path helpers at `tests/test-refine-plan.sh:532-622`.
- inputs_outputs_state: Inputs are repository files such as `commands/refine-plan.md`, `prompt-template/plan/refine-plan-qa-template.md`, `scripts/validate-refine-plan-io.sh`, skill/install docs, plugin metadata, and generated temp fixtures. Outputs are PASS/FAIL lines and exit `0` only if all assertions pass at `tests/test-refine-plan.sh:1345-1363`. Temporary state is created under `mktemp -d` and cleaned by trap at `tests/test-refine-plan.sh:624-625`.
- gates_or_invariants: Positive tests require command metadata, allowed tools, hidden slash-command flag, planning-only scope, sequential phases, validator exit-code documentation, comment extraction requirements, classification/disposition vocabulary, gen-plan section preservation, alternative-language mapping, atomic temp-file write rules, QA template sections, installation wiring, Kimi bundle-copy instructions, and version consistency at `tests/test-refine-plan.sh:632-880`. Reference behavior requires markers inside fences/HTML comments to be ignored, inline and multiline comments to be removed while preserving surrounding text, research intent to dominate change intent, change intent to dominate question intent, English to be a no-op, unsupported languages to be rejected, and variant paths to insert suffixes before the final extension at `tests/test-refine-plan.sh:891-1040`.
- dependencies_and_callers: Main implementation targets are `commands/refine-plan.md`, which defines the phased workflow and planning-only constraint at `commands/refine-plan.md:19-43`, config/language resolution at `commands/refine-plan.md:86-153`, IO validation handling at `commands/refine-plan.md:157-180`, scanner/classifier semantics at `commands/refine-plan.md:184-315`, refinement/QA/atomic write semantics at `commands/refine-plan.md:380-557`; `scripts/validate-refine-plan-io.sh`, which implements CMT scanning, section scanning, argument parsing, exit codes, path writability checks, QA directory creation, and success reporting at `scripts/validate-refine-plan-io.sh:1-585`; and `prompt-template/plan/refine-plan-qa-template.md`, whose required QA sections and metadata appear at `prompt-template/plan/refine-plan-qa-template.md:1-119`.
- edge_cases_or_failure_modes: Validator failures are mapped to exit codes: missing input `1`, empty input `2`, invalid/no comments `3`, missing required sections `4`, output/input-dir writability `5`, QA-dir failure `6`, invalid arguments `7`, asserted at `tests/test-refine-plan.sh:1051-1283`. It explicitly tests ignored markers in HTML/fences, empty CMT blocks, unterminated and nested CMT blocks with context output, required headings hidden in ignored regions, non-writable directories, mutual exclusion of `--discussion` and `--direct`, and rejection of unexpected `--alt-language` in the validator at `tests/test-refine-plan.sh:1051-1343`.
- validation_or_tests: This file is itself the top-level validation harness for refine-plan behavior. I did not execute it because the branch export is read-only and the assignment asks for research notes only; the test is designed to write temporary fixtures and exercise filesystem writability cases.
- skip_candidate: `no`

### ASK_GEMINI-HZ-139 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: User-facing block template for the Bash validator’s git-push gate. It tells the agent that current commits stay local unless the loop was started with `--push-every-round`.
- algorithmic_behavior: Static markdown message with title, local-commit policy, and the enabling command example at `prompt-template/block/git-push.md:1-9`. It is loaded through `load_and_render_safe` by `hooks/loop-bash-validator.sh` when `git push` is attempted and push-every-round is not enabled, as shown at `hooks/loop-bash-validator.sh:223`.
- inputs_outputs_state: Inputs are none beyond template loading; no placeholders are present. Output is a rendered markdown block sent to stderr by the validator. No state is mutated.
- gates_or_invariants: The invariant is “no remote push during loop unless configured at loop start.” The template’s concrete instruction is to use `/humanize:start-rlcr-loop plan.md --push-every-round` if remote pushes are needed at `prompt-template/block/git-push.md:6-9`.
- dependencies_and_callers: Depends on the hook template loader path and `load_and_render_safe` from `hooks/lib/template-loader.sh`. Caller is `hooks/loop-bash-validator.sh`, found by search at the `git_push_blocked_message` load site.
- edge_cases_or_failure_modes: Since it has no variables, rendering cannot fail due to missing substitutions. If the template is missing, callers use their inline fallback through `load_and_render_safe`, so behavior degrades to a fallback block rather than allowing the push.
- validation_or_tests: `tests/test-template-references.sh` scans template references and verifies critical validators use safe rendering; `tests/test-templates-comprehensive.sh` includes `block/git-push.md` in render-safety coverage per search evidence. This item is a static policy template rather than algorithmic code.
- skip_candidate: `yes: static prompt block, but it participates in the Bash validator’s git-push gate contract`

### ASK_GEMINI-HZ-169 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: User-facing block template for write attempts targeting a round summary file outside the active `.humanize/rlcr/<session>` loop directory.
- algorithmic_behavior: Static markdown with one placeholder, `{{CORRECT_PATH}}`, rendered into the correct active-loop summary destination at `prompt-template/block/wrong-summary-location.md:1-5`. `hooks/loop-write-validator.sh` calls this template when a summary or contract-like protected file is outside `.humanize/rlcr`, and specifically uses it for summaries at `hooks/loop-write-validator.sh:290-305`.
- inputs_outputs_state: Input is the active loop’s computed `CORRECT_PATH`, built as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` at `hooks/loop-write-validator.sh:299-304`. Output is a rendered markdown block to stderr followed by validator exit `2`. No persistent state is changed.
- gates_or_invariants: Summary files must be written only in the active loop directory, not elsewhere. The template supports the path-location invariant after the validator has already resolved the active loop and current round.
- dependencies_and_callers: Depends on `load_and_render_safe` and template variable substitution. Caller is `hooks/loop-write-validator.sh`; fallback content is embedded next to the call so missing template still blocks the write at `hooks/loop-write-validator.sh:300-304`.
- edge_cases_or_failure_modes: If `CORRECT_PATH` is not supplied, the rendered message would expose an unsubstituted placeholder, but the validator always passes it in the observed call. If no active loop exists, `loop-write-validator.sh` exits earlier and this template is not used at `hooks/loop-write-validator.sh:187-189`.
- validation_or_tests: `tests/test-template-references.sh` and comprehensive template tests cover referenced templates. `tests/test-allowlist-validators.sh` and hook tests exercise wrong-location write blocking paths per search evidence.
- skip_candidate: `yes: static prompt block, but it is part of the write-validator transition feedback contract`

### ASK_GEMINI-HZ-199 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor/parser library for Humanize loop monitoring. It provides reusable status colors, file/session utilities, terminal layout helpers, state-file discovery, YAML frontmatter extraction, PR-loop phase detection, and goal-tracker statistics.
- algorithmic_behavior: File/session utilities include cross-platform file size and latest timestamped session discovery at `scripts/lib/monitor-common.sh:32-63`. Terminal helpers manage fixed header scroll regions at `scripts/lib/monitor-common.sh:69-92`. Status helpers map loop states to ANSI colors at `scripts/lib/monitor-common.sh:128-141`. State-file discovery prioritizes methodology-analysis and active `state.md`, then terminal `*-state.md` files, returning `path|status` at `scripts/lib/monitor-common.sh:147-192`. YAML frontmatter values are extracted by key at `scripts/lib/monitor-common.sh:198-213`. PR-loop phase detection checks terminal states, detects active Codex analysis by latest `round-*-pr-check.md` growth/mtime, then derives waiting phase from `state.md` frontmatter at `scripts/lib/monitor-common.sh:271-346`. Goal-tracker parsers count acceptance criteria, active/completed/deferred tasks, issue breakdown, and PR issue totals at `scripts/lib/monitor-common.sh:391-546`.
- inputs_outputs_state: Inputs are directories and markdown files such as `.humanize/rlcr/<session>`, `.humanize/pr-loop/<session>`, `state.md`, `methodology-analysis-state.md`, `*-state.md`, `goal-tracker.md`, and `round-*-pr-check.md`. Outputs are plain strings used by monitors: colors, formatted timestamps, truncated strings, phase names, phase display messages, and pipe-delimited counters. It writes only a small `/tmp/humanize-phase-<session>-<round>.size` cache when detecting whether a PR check file is growing at `scripts/lib/monitor-common.sh:301-310`.
- gates_or_invariants: Session names must match `YYYY-MM-DD_HH-MM-SS` for latest-session discovery at `scripts/lib/monitor-common.sh:54-60`. Active state priority treats `methodology-analysis-state.md` as more specific than `state.md`, and terminal-state status is derived from `*-state.md` filenames at `scripts/lib/monitor-common.sh:162-190`. PR phase detection treats final states before active analysis, then falls back to state metadata at `scripts/lib/monitor-common.sh:276-345`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at `scripts/humanize.sh:6-10`; `monitor-skill.sh` depends on `monitor_get_yaml_value` and `monitor_format_timestamp` at `scripts/lib/monitor-skill.sh:8-10`, `scripts/lib/monitor-skill.sh:246-280`. Tests source it directly in `tests/test-pr-loop-system.sh` per search evidence.
- edge_cases_or_failure_modes: YAML parsing is simple grep/sed frontmatter extraction and does not handle nested YAML, duplicate keys, or quoted colons robustly. PR phase “Codex analyzing” depends on file-size cache and 10-second mtime fallback, so first observation or recently touched files can classify as analyzing at `scripts/lib/monitor-common.sh:296-322`. Goal-tracker counts rely on markdown section names and table row counts; malformed tables can undercount or default to zero.
- validation_or_tests: `tests/test-pr-loop-system.sh` exercises state-file discovery, PR goal-tracker parsing, and phase helpers. Robustness tests for goal trackers also validate related parsing behavior, though some use equivalent functions surfaced by `scripts/humanize.sh`. I did not execute tests in the read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ASK_GEMINI-HZ-019`, `ASK_GEMINI-HZ-049`, `ASK_GEMINI-HZ-079`, `ASK_GEMINI-HZ-109`, `ASK_GEMINI-HZ-139`, `ASK_GEMINI-HZ-169`, `ASK_GEMINI-HZ-199`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`