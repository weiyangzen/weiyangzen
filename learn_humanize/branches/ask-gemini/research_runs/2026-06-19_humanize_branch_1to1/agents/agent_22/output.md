# agent_22 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-022 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: Top-level Humanize skill bundle documentation and routing surface. Recursive inspection found one child file, `skills/humanize/SKILL.md`, so this directory’s behavior is entirely defined by that skill descriptor.
- algorithmic_behavior: `skills/humanize/SKILL.md:1-6` declares the skill as `humanize`, non-user-invocable, and model-invocation disabled, making it a shared runtime/reference skill rather than a direct command endpoint. It defines the RLCR algorithm as two phases: implementation summary plus Codex completeness review, then code review through `codex review --base <branch>` until no severity issues remain (`skills/humanize/SKILL.md:32-49`). It also defines PR-loop behavior: detect PR, fetch bot reviews, fix, push, trigger re-review, poll, locally validate, and repeat until approval or max iterations (`skills/humanize/SKILL.md:50-60`).
- inputs_outputs_state: Inputs are implementation plans, optional RLCR/PR command flags, bot selectors, base branch, model/timeout settings, and one-shot consultation questions. Outputs/state live under `.humanize/`, with RLCR session state, summaries, reviews, goal tracker, finalize/methodology files, PR-loop state, and one-shot skill outputs listed at `skills/humanize/SKILL.md:216-243`.
- gates_or_invariants: Key invariants are explicit summary writing, goal tracker maintenance, no state-file cheating, normal stop/exit through native hooks, and external review as a gate (`skills/humanize/SKILL.md:202-209`). Goal tracker governance splits immutable Ultimate Goal/Acceptance Criteria from mutable Active/Completed/Deferred/Evolution sections (`skills/humanize/SKILL.md:188-200`).
- dependencies_and_callers: Coordinates runtime scripts through hydrated `{{HUMANIZE_RUNTIME_ROOT}}`: `setup-rlcr-loop.sh`, `cancel-rlcr-loop.sh`, `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `validate-gen-plan-io.sh`, `ask-codex.sh`, and `humanize.sh` monitor commands (`skills/humanize/SKILL.md:73-145`, `skills/humanize/SKILL.md:245-253`). Install docs copy this bundle plus scripts/hooks/templates/config into user skill locations, confirming it is the shared runtime root for sibling skills.
- edge_cases_or_failure_modes: Max-iteration termination is part of both RLCR and PR options (`skills/humanize/SKILL.md:85-99`, `skills/humanize/SKILL.md:122-125`). The directory itself is a skip candidate for direct execution because `user-invocable: false`, but not a skip candidate for core algorithm review because it defines the overall state machine and command contracts.
- validation_or_tests: Related tests exercise the runtime contracts described here, especially state lifecycle and hook protections in `tests/test-state-exit-naming.sh` and `tests/test-cancel-signal-file.sh`. Install/reference searches show this directory is copied as the runtime dependency bundle in Codex/Kimi install flows.
- skip_candidate: `no`

### ASK_GEMINI-HZ-052 `file` `scripts/ask-gemini.sh`
- cursor: `[_]`
- core_role: Runtime workflow script for `/humanize:ask-gemini`, implementing a one-shot Gemini CLI consultation with persistent invocation records and cache/log routing.
- algorithmic_behavior: Parses `--gemini-model`, `--gemini-timeout`, `--`, and free-form question parts (`scripts/ask-gemini.sh:85-136`); validates `gemini` exists, a question is present, and model name matches a restricted character set (`scripts/ask-gemini.sh:142-165`); detects project root via git or `CLAUDE_PROJECT_DIR` (`scripts/ask-gemini.sh:171-175`); creates unique project-local and cache directories (`scripts/ask-gemini.sh:181-196`); records input (`scripts/ask-gemini.sh:202-215`); builds Gemini args and an augmented prompt that requires Google Search (`scripts/ask-gemini.sh:221-238`); runs `gemini` through `run_with_timeout` and writes stdout/stderr cache files (`scripts/ask-gemini.sh:278-280`); classifies timeout, process error, empty response, or success (`scripts/ask-gemini.sh:291-385`).
- inputs_outputs_state: Inputs are shell arguments, `HUMANIZE_GEMINI_YOLO`, `XDG_CACHE_HOME`, `HOME`, git/project cwd, and the external `gemini` CLI. Outputs are Gemini response on stdout, status/debug on stderr, `.humanize/skill/<unique-id>/input.md`, `output.md`, `metadata.md`, and cache files `gemini-run.cmd`, `.out`, `.log` (`scripts/ask-gemini.sh:16-18`, `scripts/ask-gemini.sh:244-258`, `scripts/ask-gemini.sh:365-385`).
- gates_or_invariants: Default model is `gemini-3.1-pro-preview`, default timeout is 3600 seconds (`scripts/ask-gemini.sh:36-40`). Timeout must be numeric but the regex allows `0`, despite the error text saying positive (`scripts/ask-gemini.sh:110-119`). Model names are limited to alphanumeric, dot, underscore, hyphen (`scripts/ask-gemini.sh:159-165`). Tool approval mode is sandbox by default and `--yolo` only when `HUMANIZE_GEMINI_YOLO` is true or 1 (`scripts/ask-gemini.sh:223-228`).
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` (`scripts/ask-gemini.sh:27-31`), whose timeout priority is `gtimeout`, GNU `timeout`, Python, then no-timeout fallback (`scripts/portable-timeout.sh:9-27`). The skill descriptor allows Bash execution of this script only (`skills/ask-gemini/SKILL.md:5`). Monitoring consumes its `.humanize/skill` metadata and `gemini-run.*` cache files (`scripts/lib/monitor-skill.sh:50-64`, `scripts/lib/monitor-skill.sh:165-199`).
- edge_cases_or_failure_modes: Missing Gemini exits 1 with install guidance (`scripts/ask-gemini.sh:142-148`); empty question exits 1 (`scripts/ask-gemini.sh:150-157`); invalid model exits 1 (`scripts/ask-gemini.sh:159-165`); unwritable home cache falls back to local `SKILL_DIR/cache` (`scripts/ask-gemini.sh:192-196`); timeout exits 124 and writes timeout metadata (`scripts/ask-gemini.sh:291-310`); nonzero Gemini exit preserves last stderr lines and metadata (`scripts/ask-gemini.sh:313-334`); empty stdout becomes status `empty_response` with exit 1 (`scripts/ask-gemini.sh:337-358`).
- validation_or_tests: No dedicated `ask-gemini.sh` test was found in assigned items. Indirect contract is specified by `skills/ask-gemini/SKILL.md`, README usage, and monitor support for Gemini invocation records.
- skip_candidate: `no`

### ASK_GEMINI-HZ-082 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR cancellation authorization and Bash-hook state-file protection.
- algorithmic_behavior: Creates isolated loop directories with `.humanize/rlcr/<timestamp>/state.md`, exports `CLAUDE_PROJECT_DIR`, then feeds synthetic Bash hook JSON into `hooks/loop-bash-validator.sh` (`tests/test-cancel-signal-file.sh:28-74`). It verifies that `mv state.md cancel-state.md` is allowed only when `.cancel-requested` exists in the active loop directory (`tests/test-cancel-signal-file.sh:80-115`) and blocked without that signal (`tests/test-cancel-signal-file.sh:121-135`).
- inputs_outputs_state: Inputs are command strings encoded via `jq`, active loop state file, optional `.cancel-requested`, and sourced `hooks/lib/loop-common.sh` (`tests/test-cancel-signal-file.sh:32-33`, `tests/test-cancel-signal-file.sh:42-59`, `tests/test-cancel-signal-file.sh:63-74`). Outputs are pass/fail counters and process exit equal to failed test count at the tail of the script.
- gates_or_invariants: The invariant under test is narrow: only `mv` from active `state.md`, `finalize-state.md`, or `methodology-analysis-state.md` to active `cancel-state.md` may bypass the state-file mutation block, and only with the signal file. The implementation gate is `is_cancel_authorized`, which rejects missing signal, command substitution, newlines, shell chaining, hidden variables, extra args, wrong source/destination, and symlink sources (`hooks/lib/loop-common.sh:964-982`, `hooks/lib/loop-common.sh:988-1143`).
- dependencies_and_callers: Exercises `loop-bash-validator.sh`, especially protected state-file checks that call `is_cancel_authorized` before exiting 0 or blocking with exit 2 (`hooks/loop-bash-validator.sh:259-285`, `hooks/loop-bash-validator.sh:416-443`, `hooks/loop-bash-validator.sh:446-476`). Depends on `jq`, temp directories, Bash, and `loop-common.sh` helpers including `to_lower`.
- edge_cases_or_failure_modes: Covers wrong signal directory (`tests/test-cancel-signal-file.sh:201-217`), other state mutations despite signal (`tests/test-cancel-signal-file.sh:141-175`, `tests/test-cancel-signal-file.sh:223-237`), wrong destination and arbitrary destination (`tests/test-cancel-signal-file.sh:181-195`, `tests/test-cancel-signal-file.sh:283-297`), command injection via `&&`, `;`, `$()`, backticks, newline, chained operators, pipes, background operators, shell wrappers, redirection prefixes, hidden `${IFS}`, command prefixes such as `sudo`, `env`, and `command --` (`tests/test-cancel-signal-file.sh:243-357`, `tests/test-cancel-signal-file.sh:530-946`). Positive coverage includes quoted paths and literal `${loop_dir}` syntax normalization (`tests/test-cancel-signal-file.sh:365-380`, `tests/test-cancel-signal-file.sh:450-486`).
- validation_or_tests: This file is itself the validation suite for the cancel signal mechanism. Expected pass condition is all scenario exit codes matching 0 for authorized cancel and 2 for blocked bypass attempts.
- skip_candidate: `no`

### ASK_GEMINI-HZ-112 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR active-loop discovery and terminal state-file naming.
- algorithmic_behavior: Builds a temporary git repo and RLCR session directories, sources `loop-common.sh`, and tests that only a literal `state.md` identifies an active loop (`tests/test-state-exit-naming.sh:31-74`, `tests/test-state-exit-naming.sh:76-95`). Terminal files `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` must not be treated as active loops (`tests/test-state-exit-naming.sh:97-159`). Newer active loop directories take precedence (`tests/test-state-exit-naming.sh:161-180`).
- inputs_outputs_state: Inputs are temporary `.humanize/rlcr` directories, state markdown files, `CLAUDE_PROJECT_DIR`, and `loop-common.sh`. Outputs are pass/fail/skip counters and exit status equal to failed test count (`tests/test-state-exit-naming.sh:293-302`).
- gates_or_invariants: `find_active_loop` returns only the newest loop directory whose resolved active state file exists; with no session filter it checks the single newest directory for zombie-loop protection (`hooks/lib/loop-common.sh:307-330`). `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, or `unexpected`; it renames `state.md` to `<reason>-state.md` and fails if the reason is invalid or the source state file is missing (`hooks/lib/loop-common.sh:1567-1600`).
- dependencies_and_callers: Directly depends on `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` from `hooks/lib/loop-common.sh`. It validates the path migration invariant that only `.humanize/rlcr/` is a Humanize loop path and legacy `.humanize-loop.local` is not (`tests/test-state-exit-naming.sh:249-291`, `hooks/lib/loop-common.sh:1146-1150`).
- edge_cases_or_failure_modes: Invalid end-loop reasons must error (`tests/test-state-exit-naming.sh:186-204`); all valid reasons must create matching terminal files (`tests/test-state-exit-naming.sh:206-230`); missing state file must fail gracefully with a not-found warning (`tests/test-state-exit-naming.sh:232-243`); legacy directories must not be searched when using the new base path (`tests/test-state-exit-naming.sh:267-291`).
- validation_or_tests: This file is the validation suite. Its expected final condition is zero failures after 13 tests covering active detection, terminal naming, path matching, and legacy-path exclusion.
- skip_candidate: `no`

### ASK_GEMINI-HZ-142 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template for goal-tracker write violations after RLCR Round 0.
- algorithmic_behavior: Renders a user-facing block message telling the agent it may update only the mutable section of the active goal tracker after Round 0 (`prompt-template/block/goal-tracker-modification.md:1-13`). It names the correct tracker path through `{{CORRECT_PATH}}` and includes the active round through `{{CURRENT_ROUND}}`.
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `CORRECT_PATH`; output is a rendered Markdown block emitted by hook logic. The corresponding function `goal_tracker_blocked_message` passes those variables to `load_and_render_safe` and has an inline fallback if the template is unavailable (`hooks/lib/loop-common.sh:1546-1565`).
- gates_or_invariants: Enforces three invariants: immutable section unchanged, no Bash modification of `goal-tracker.md`, and no writes to an old loop session’s tracker (`prompt-template/block/goal-tracker-modification.md:8-13`). It also directs unresolved tracker drift into an optional `Goal Tracker Update Request` in the round summary rather than unsafe direct mutation (`prompt-template/block/goal-tracker-modification.md:14`).
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh` message rendering. Cross-reference validation explicitly requires this template path in `tests/test-template-references.sh:152-159`, ensuring the hook’s referenced block template exists.
- edge_cases_or_failure_modes: If missing, the fallback in `loop-common.sh` preserves the essential block behavior but loses the exact template wording. A stale `CORRECT_PATH` would misroute edits, so active-loop detection/session matching upstream is part of the safety boundary.
- validation_or_tests: `tests/test-template-references.sh` includes `block/goal-tracker-modification.md` in the common template existence list (`tests/test-template-references.sh:149-167`).
- skip_candidate: `no`

### ASK_GEMINI-HZ-172 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Prompt header that switches the initial RLCR round into Agent Teams mode and defines the leader’s coordination responsibilities.
- algorithmic_behavior: Tells the agent it is the Team Leader in an RLCR development cycle, must read the implementation plan thoroughly before creating the team, and must account for the plan file, goal tracker, and final work summary (`prompt-template/claude/agent-teams-instructions.md:1-8`).
- inputs_outputs_state: Input is the initial RLCR prompt generation path with `--agent-teams` enabled. Output is appended Markdown in `round-0-prompt.md`. `setup-rlcr-loop.sh` appends this header plus `agent-teams-core.md` when both files exist (`scripts/setup-rlcr-loop.sh:1377-1386`), otherwise it emits a fallback Agent Teams block (`scripts/setup-rlcr-loop.sh:1387-1397`).
- gates_or_invariants: The template establishes initial-round-only coordination: read plan and goal tracker before splitting tasks, then write the designated summary after teammates finish. It is a prompt-level gate, not a shell/runtime validator.
- dependencies_and_callers: Direct caller is `scripts/setup-rlcr-loop.sh` during initial prompt construction. `tests/test-agent-teams.sh` verifies this template exists and has content (`tests/test-agent-teams.sh:330-344`), and separately checks non-agent-teams prompts avoid Team Leader wording (`tests/test-agent-teams.sh:320-328`).
- edge_cases_or_failure_modes: If the header or core template is missing, setup falls back to a smaller embedded instruction set, so teams mode still exists but loses the richer reusable prompt content (`scripts/setup-rlcr-loop.sh:1387-1397`). Misclassification risk is moderate because this is prompt text, but it controls task-splitting behavior for a core workflow mode.
- validation_or_tests: `tests/test-agent-teams.sh` checks file existence/content size for this template and behavior differences with and without `--agent-teams`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-202 `file` `skills/ask-gemini/SKILL.md`
- cursor: `[_]`
- core_role: User-invocable skill contract for safely calling `scripts/ask-gemini.sh` and interpreting its result.
- algorithmic_behavior: Front matter names the skill, describes Gemini consultation with deep web research, advertises supported flags, and restricts allowed tools to Bash execution of `${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh:*` (`skills/ask-gemini/SKILL.md:1-6`). Usage rules require quoting free-form user text as a single shell argument and preserving recognized flags as separate arguments (`skills/ask-gemini/SKILL.md:15-31`). It explicitly forbids unsafe unquoted `$ARGUMENTS` expansion because the shell would re-parse metacharacters (`skills/ask-gemini/SKILL.md:33-39`).
- inputs_outputs_state: Inputs are optional `--gemini-model`, optional `--gemini-timeout`, and the question/task. Outputs are script stdout for Gemini’s answer and stderr for status (`skills/ask-gemini/SKILL.md:41-47`). Persistent output is `.humanize/skill/<timestamp>/output.md` (`skills/ask-gemini/SKILL.md:57-61`), matching the script’s storage behavior.
- gates_or_invariants: The critical invariant is shell safety: never pass free-form text unquoted, especially when it may contain spaces or shell metacharacters (`skills/ask-gemini/SKILL.md:15-18`, `skills/ask-gemini/SKILL.md:33-39`). It also requires consuming Gemini stdout carefully and reporting nonzero exit errors (`skills/ask-gemini/SKILL.md:41-47`).
- dependencies_and_callers: Depends on `scripts/ask-gemini.sh` for execution and on Gemini CLI availability through that script. README advertises `/humanize:ask-gemini` as the deep web research command (`README.md:58-60`), and monitor support recognizes Gemini invocation metadata/logs (`scripts/lib/monitor-skill.sh:16-17`, `scripts/lib/monitor-skill.sh:176-199`).
- edge_cases_or_failure_modes: Exit-code interpretation is part of the contract: 1 for validation errors, 124 for timeout with larger-timeout suggestion, other for Gemini process error (`skills/ask-gemini/SKILL.md:48-55`). Misquoting arguments is the primary edge case because the script may never start or may receive altered input.
- validation_or_tests: No direct unit test found for this skill file. Its behavior is cross-validated by consistency with `scripts/ask-gemini.sh` help/defaults and monitor support for `.humanize/skill` records.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_GEMINI-HZ-022, ASK_GEMINI-HZ-052, ASK_GEMINI-HZ-082, ASK_GEMINI-HZ-112, ASK_GEMINI-HZ-142, ASK_GEMINI-HZ-172, ASK_GEMINI-HZ-202
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`