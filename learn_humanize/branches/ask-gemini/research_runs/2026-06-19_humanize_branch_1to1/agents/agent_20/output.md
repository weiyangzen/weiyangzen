# agent_20 ask-gemini 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `883e3f5bb8106cea4153d9f5e469b2fa7a8d6849`

## Item Evidence

### ASK_GEMINI-HZ-020 `directory` `skills/ask-codex`
- cursor: `[_]`
- core_role: Claude skill wrapper for the one-shot Codex consultation path. The directory recursively contains one file, `skills/ask-codex/SKILL.md`, which defines the slash-skill contract and delegates execution to `scripts/ask-codex.sh`.
- algorithmic_behavior: The skill does not implement the Codex runner itself; it constrains how Claude must invoke the runner. It requires free-form user text to be passed as one quoted shell argument when no flags are present, and requires flag reconstruction when `--codex-model` or `--codex-timeout` is supplied so flags remain separate arguments while the question stays quoted. The unsafe `$ARGUMENTS` form is explicitly forbidden because shell reparsing can break on spaces or metacharacters.
- inputs_outputs_state: Inputs are skill arguments of the form `[--codex-model MODEL:EFFORT] [--codex-timeout SECONDS] [question or task]`. Output is the stdout/stderr behavior of `scripts/ask-codex.sh`; `SKILL.md` states stdout contains the Codex response, stderr contains status, and responses are persisted under `.humanize/skill/<timestamp>/output.md`.
- gates_or_invariants: Main invariant is argument-boundary preservation. `skills/ask-codex/SKILL.md:14-22` requires quoted free-form text and separated flags; `skills/ask-codex/SKILL.md:30-36` forbids unquoted `$ARGUMENTS`. Allowed tooling is restricted to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh:*)` at `skills/ask-codex/SKILL.md:5`.
- dependencies_and_callers: Delegates to `scripts/ask-codex.sh` at `skills/ask-codex/SKILL.md:19` and `skills/ask-codex/SKILL.md:27`. Cross-references show it is used by planning and RLCR flows, including `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, and `scripts/setup-rlcr-loop.sh`, where `analyze` tasks are routed to `/humanize:ask-codex`.
- edge_cases_or_failure_modes: Documented nonzero cases are validation error, timeout, and Codex process errors. `skills/ask-codex/SKILL.md:46-51` maps exit code `1` to missing Codex/empty question/invalid flags, `124` to timeout, and other nonzero codes to process errors. The most important shell edge case is user text containing metacharacters such as `(`, `)`, `;`, `#`, `*`, or `[`.
- validation_or_tests: `tests/test-ask-codex.sh` is the direct behavioral test file for this wrapper and runner path. Cross-reference assertions include checks that the unsafe unquoted form exists only as a warning and that the safe quoted form is present.
- skip_candidate: `no`

### ASK_GEMINI-HZ-050 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook state machine for the PR loop. It intercepts exit attempts, verifies PR loop prerequisites, polls configured remote review bots, runs local Codex to classify bot feedback, and either terminates the loop or blocks exit with next-round instructions.
- algorithmic_behavior: The hook starts by finding the newest active PR loop under `.humanize/pr-loop` via `find_active_pr_loop`, then parses `state.md` frontmatter into scalar fields and YAML arrays for `configured_bots` and `active_bots` (`hooks/pr-loop-stop-hook.sh:65-177`). It resolves the PR lookup repository, including fork parent fallback (`hooks/pr-loop-stop-hook.sh:205-237`), exits cleanly for merged or closed PRs (`hooks/pr-loop-stop-hook.sh:244-255`), requires a round resolution file (`hooks/pr-loop-stop-hook.sh:262-276`), checks clean git status and pushed commits (`hooks/pr-loop-stop-hook.sh:283-361`), detects force pushes by reachability of `latest_commit_sha` and clears trigger state (`hooks/pr-loop-stop-hook.sh:370-421`), then advances into trigger detection, bot polling, Codex validation, and state rewrite.
- inputs_outputs_state: Inputs are hook JSON on stdin, PR-loop `state.md`, round resolution files, git state, GitHub PR comments/reviews/reactions via `gh`, and bot helper scripts. Outputs are either no-op exit, terminal state file renames such as `merged-state.md`, `closed-state.md`, `approve-state.md`, `usage-limit-state.md`, or JSON stop-hook decisions with `decision: block`. It writes round artifacts: `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md`.
- gates_or_invariants: Required fields include `pr_number` and numeric `current_round` (`hooks/pr-loop-stop-hook.sh:186-195`). The hook blocks if the resolution summary is missing, git status fails, non-`.humanize` changes are uncommitted, or commits are unpushed (`hooks/pr-loop-stop-hook.sh:262-361`). It enforces max iterations by moving to `maxiter-state.md` (`hooks/pr-loop-stop-hook.sh:430-435`). Trigger rules depend on round and startup case: later rounds always require a fresh trigger; cases `4` and `5` require trigger in round zero; new commits override earlier no-trigger cases (`hooks/pr-loop-stop-hook.sh:613-644`). If Claude is configured and a trigger is required, an eyes reaction is mandatory before polling proceeds (`hooks/pr-loop-stop-hook.sh:768-790`).
- dependencies_and_callers: Sourced helpers include `hooks/lib/loop-common.sh` for loop discovery, bot mapping, YAML list construction, templates, and goal-tracker update; `scripts/portable-timeout.sh` for bounded `git`, `gh`, and `codex` calls. Runtime helper scripts include `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/check-pr-reviewer-status.sh`. The hook is installed through hook configuration: `hooks/hooks.json` and `config/codex-hooks.json` reference `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: Fork PRs are handled by resolving the PR in current repo first, then parent repo. Force-push protection rejects stale trigger comments by updating `latest_commit_at`, clearing `last_trigger_at` and `trigger_comment_id`, and requiring a new `@bot` comment (`hooks/pr-loop-stop-hook.sh:396-421`). Existing trigger timestamps are refreshed so old triggers after new commits are not reused (`hooks/pr-loop-stop-hook.sh:541-565`). Startup cases `2` and `3` use all historical comments and anchor timeout to the current time to avoid instant timeout (`hooks/pr-loop-stop-hook.sh:869-879`). Codex `+1` reactions can approve Codex without a comment, but only after trigger validation rules are satisfied (`hooks/pr-loop-stop-hook.sh:662-718`, `hooks/pr-loop-stop-hook.sh:985-1039`). Bot timeouts remove timed-out bots from `active_bots`, and if all active bots time out the loop is approved (`hooks/pr-loop-stop-hook.sh:1071-1126`).
- validation_or_tests: Direct test coverage is broad. `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-system.sh`, and robustness tests invoke this hook with mocked state and PR conditions. `tests/test-unified-codex-config.sh` asserts model default behavior for this hook. The assigned `prompt-template/block/git-status-failed.md` is related to the git-status failure gate, though this hook currently has an inline fallback for that specific gate at `hooks/pr-loop-stop-hook.sh:287-293`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-080 `file` `tests/test-bitlesson-select-routing.sh`
- cursor: `[_]`
- core_role: Executable specification for the BitLesson selector provider-routing algorithm implemented by `scripts/bitlesson-select.sh` and `scripts/lib/model-router.sh`.
- algorithmic_behavior: The test builds temporary BitLesson fixtures and mock `codex`/`claude` binaries, then verifies routing by configured `bitlesson_model`. It asserts `gpt-*` models route to Codex, Claude-family names and `haiku`/`sonnet`/`opus` route to Claude, unknown model names fail with a clear error, missing Codex for a Codex route fails, missing Claude falls back to Codex, `provider_mode: codex-only` forces Codex, placeholder BitLesson files short-circuit to `LESSON_IDS: NONE`, and Codex selector calls are direct helper calls that disable hooks and avoid `--full-auto`.
- inputs_outputs_state: Inputs are temporary `.humanize/config.json` files, temporary `bitlesson.md` content, CLI arguments `--task`, `--paths`, and `--bitlesson-file`, and mocked provider binaries in an isolated `PATH`. Expected output is exactly the stable selector format with `LESSON_IDS:` and `RATIONALE:` lines, or nonzero stderr for invalid routes.
- gates_or_invariants: The test enforces strict PATH isolation for missing-binary cases with `SAFE_BASE_PATH` (`tests/test-bitlesson-select-routing.sh:9-11`). It requires Codex to be invoked with trailing `-` and nonempty stdin (`tests/test-bitlesson-select-routing.sh:67-90`, `tests/test-bitlesson-select-routing.sh:164-197`). It requires placeholder files without a `## Lesson:` entry to return `NONE` without invoking a model (`tests/test-bitlesson-select-routing.sh:416-437`). It captures Codex args and requires `--disable codex_hooks`, `--skip-git-repo-check`, `--ephemeral`, read-only sandboxing, and absence of `--full-auto` (`tests/test-bitlesson-select-routing.sh:440-494`).
- dependencies_and_callers: Tests `scripts/bitlesson-select.sh`, which sources `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh`. Provider detection comes from `detect_provider`: `gpt-*` and `o[0-9]*` map to Codex, `claude-*`, `haiku`, `sonnet`, and `opus` map to Claude, otherwise error (`scripts/lib/model-router.sh:10-30`). Provider dependency checks require the matching binary (`scripts/lib/model-router.sh:32-60`).
- edge_cases_or_failure_modes: Covers unknown model names (`tests/test-bitlesson-select-routing.sh:286-310`), missing Codex binary for a Codex-selected route (`tests/test-bitlesson-select-routing.sh:312-346`), missing Claude fallback to Codex (`tests/test-bitlesson-select-routing.sh:348-383`), case-insensitive `OPUS` matching (`tests/test-bitlesson-select-routing.sh:257-284`), and model-free placeholder no-op behavior.
- validation_or_tests: This file is itself the validation asset. It contains ten numbered scenarios and ends with `print_test_summary "Bitlesson Select Routing Test Summary"` at `tests/test-bitlesson-select-routing.sh:496`.
- skip_candidate: `no`

### ASK_GEMINI-HZ-110 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR session scoping, session-id capture, loop discovery, and cancellation behavior across concurrent or stale RLCR loop directories.
- algorithmic_behavior: The test verifies that setup creates a `session_id:` field in `state.md`, leaves it initially empty, and creates `.humanize/.pending-session-id` with a state path and setup command signature (`tests/test-session-id.sh:30-107`). It then verifies `hooks/loop-post-bash-hook.sh` records the `session_id` from PostToolUse JSON only when the Bash command is a real setup invocation, removes the signal after successful capture, and preserves it on unrelated commands (`tests/test-session-id.sh:109-200`, `tests/test-session-id.sh:463-528`). It exercises `find_active_loop` with matching, nonmatching, empty, absent, and terminal-session scenarios (`tests/test-session-id.sh:202-347`, `tests/test-session-id.sh:576-784`).
- inputs_outputs_state: Inputs are temporary RLCR loop directories with `state.md`, `finalize-state.md`, `complete-state.md`, or `cancel-state.md`; mocked hook JSON containing `tool_input.command` and `session_id`; setup/cancel scripts; and the shared `loop-common.sh` functions. Outputs are pass/fail assertions, patched `state.md` fields, removed or preserved `.pending-session-id`, returned loop directory paths, and cancel-state renames.
- gates_or_invariants: `find_active_loop` without a session filter must only inspect the newest directory to prevent stale-loop revival (`hooks/lib/loop-common.sh:291-294`, tested at `tests/test-session-id.sh:385-436`). With a session filter, it must find the newest directory belonging to that session by checking any `*state.md`, then return it only if active; if that newest session directory is terminal, it returns empty and does not fall through to an older active stale loop (`hooks/lib/loop-common.sh:296-305`, `hooks/lib/loop-common.sh:333-360`). Empty stored `session_id` matches any filter for backward compatibility (`hooks/lib/loop-common.sh:349-350`). PostToolUse capture requires the command to start with the full setup script path, quoted or unquoted, followed by end-of-string or POSIX whitespace (`hooks/loop-post-bash-hook.sh:56-91`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file`, and `extract_session_id`; `scripts/setup-rlcr-loop.sh` for state creation and signal file creation (`scripts/setup-rlcr-loop.sh:890-925`); `hooks/loop-post-bash-hook.sh` for session capture; and `scripts/cancel-rlcr-loop.sh`, which intentionally cancels globally without session filtering because it has no hook JSON session context (`scripts/cancel-rlcr-loop.sh:76-87`).
- edge_cases_or_failure_modes: Covers special characters in captured session IDs including `/`, `&`, and `.` (`tests/test-session-id.sh:530-574`), YAML-safe lookup characters such as dash/underscore/dot (`tests/test-session-id.sh:438-461`), false-positive command strings like `echo /path/setup-rlcr-loop.sh` and `cat setup-rlcr-loop.sh` (`tests/test-session-id.sh:825-880`), quoted-prefix concatenation attacks like `"setup-rlcr-loop.sh"foo` (`tests/test-session-id.sh:882-925`), tab-delimited quoted/unquoted invocations (`tests/test-session-id.sh:965-1053`), `finalize-state.md` matching (`tests/test-session-id.sh:288-318`), and `cancel-state.md` blocking stale revival (`tests/test-session-id.sh:747-784`).
- validation_or_tests: This file is the validation suite for the session-id feature and ends with `print_test_summary "Session ID Feature Tests"` at `tests/test-session-id.sh:1055-1059`. It indirectly validates `loop-common.sh`, `loop-post-bash-hook.sh`, setup, cancel, and bash-validator session filtering.
- skip_candidate: `no`

### ASK_GEMINI-HZ-140 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Prompt/block template for a hook gate where git status cannot be verified. It is a small failure message surface used to explain that repository state cannot be trusted.
- algorithmic_behavior: The template takes `{{GIT_STATUS_EXIT}}` and renders a concise block telling the agent/user that `git status` failed or timed out, lists likely causes, and instructs manual verification. It does not itself run git or decide state transitions.
- inputs_outputs_state: Input is the git-status exit code template variable. Output is Markdown beginning with `# Git Status Failed`, followed by possible causes such as lock contention, invalid repository state, or large repository slowness (`prompt-template/block/git-status-failed.md:1-10`). No persistent state changes occur in the template.
- gates_or_invariants: Invariant is fail-closed messaging: if repository status cannot be verified, the loop should not proceed. The template supports a block decision in hooks that must avoid approving or advancing with unknown repo cleanliness.
- dependencies_and_callers: Related template-loading infrastructure is in `hooks/lib/loop-common.sh` through template directory setup and safe rendering. Cross-reference found `hooks/loop-codex-stop-hook.sh` loading this exact template for a git-status failure gate. The assigned `hooks/pr-loop-stop-hook.sh` has a related git-status gate at `hooks/pr-loop-stop-hook.sh:283-293`, but in the inspected version it uses an inline fallback rather than this template.
- edge_cases_or_failure_modes: Main edge case is timeout or failure of `git status` itself. The template intentionally avoids prescribing destructive cleanup; it asks for manual status inspection.
- validation_or_tests: Indirectly covered by hook tests that assert block messages on git-status failures. No dedicated test file for this template was identified in the assigned set.
- skip_candidate: `no`

### ASK_GEMINI-HZ-170 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Claude prompt template for continuing Agent Teams mode across RLCR rounds after Codex feedback. It shapes team-leader behavior in the next iteration rather than implementing shell logic.
- algorithmic_behavior: The template instructs the leader to treat prior teammates as gone, review Codex feedback before spawning new members, avoid redoing already-correct work, cold-start new team members with enough current-state context, prioritize critical findings if one round is insufficient, and inspect current file state before assigning ownership (`prompt-template/claude/agent-teams-continue.md:1-12`).
- inputs_outputs_state: Input is the previous round context and Codex review feedback placed above the template by the caller. Output is an instruction block that influences the next Claude continuation prompt. It does not write loop state directly, but affects RLCR task allocation and acceptance criteria for the next round.
- gates_or_invariants: Key invariants are no reliance on old team members, review-before-spawn, no redundant rework, cold-start context for spawned members, and state awareness before file ownership assignment. These act as behavioral gates to reduce stale context and repeated work between RLCR rounds.
- dependencies_and_callers: Cross-reference shows `hooks/loop-codex-stop-hook.sh` loads this template for agent-teams continuation. `tests/test-agent-teams.sh` asserts that the template exists, has substantive content, and includes a “Continuation Context” section.
- edge_cases_or_failure_modes: Addresses partial changes and pattern drift left by previous rounds; warns that new members have no conversation history and therefore need explicit context. A likely failure mode is over-spawning without first understanding Codex feedback, which this template directly forbids.
- validation_or_tests: `tests/test-agent-teams.sh` checks this template around lines that verify size and continuation-specific guidance. The assigned file itself is not executable.
- skip_candidate: `no`

### ASK_GEMINI-HZ-200 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Runtime monitor implementation for `.humanize/skill` invocations, covering ask-codex and ask-gemini runs. It provides `_humanize_monitor_skill`, sourced by `scripts/humanize.sh`, and supports aggregate status display plus live output tailing.
- algorithmic_behavior: The function parses `--once` and optional `--tool-filter <codex|gemini>`, validates `.humanize/skill`, identifies invocation tool from `metadata.md` or `input.md`, filters directories, sorts timestamped invocation directories newest-first, chooses the newest invocation with watchable content, counts statuses, selects the best output/log file, and either prints a one-shot summary or enters an interactive terminal monitor with a fixed status bar and live `tail -f`.
- inputs_outputs_state: Inputs are `.humanize/skill/<timestamp>/` directories, `metadata.md`, `input.md`, `output.md`, local `cache/`, and global cache under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/skill-<id>`. Outputs are terminal text/status bars and live tailed output; it does not mutate invocation state except managing its own terminal and background tail process.
- gates_or_invariants: Directory names must begin with `YYYY-MM-DD_HH-MM-SS` to be considered valid (`scripts/lib/monitor-skill.sh:77-90`). Tool filtering accepts exact `codex` or `gemini`, with legacy `unknown` treated as Codex when filtering for Codex (`scripts/lib/monitor-skill.sh:66-75`). Running invocations prefer live cache logs; completed invocations prefer nonempty `output.md`, then cache outputs/logs (`scripts/lib/monitor-skill.sh:165-217`). Status counts classify `success`, `error`, `timeout`, `empty_response`, and running when no metadata exists (`scripts/lib/monitor-skill.sh:119-143`).
- dependencies_and_callers: Depends on `monitor-common.sh` helpers such as `monitor_get_yaml_value` and `monitor_format_timestamp`, and on `humanize_split_to_array` from `humanize.sh` (`scripts/lib/monitor-skill.sh:8-10`). `scripts/humanize.sh` sources this file at `scripts/humanize.sh:1660-1663` and routes `humanize monitor skill`, `humanize monitor codex`, and `humanize monitor gemini` to `_humanize_monitor_skill` with optional filters (`scripts/humanize.sh:1171-1199`).
- edge_cases_or_failure_modes: If `.humanize/skill` is absent, it returns an error and suggests running ask-codex or ask-gemini first (`scripts/lib/monitor-skill.sh:43-48`). If no filtered invocation exists in `--once`, it returns nonzero with a filter-specific message (`scripts/lib/monitor-skill.sh:339-347`). If content is unavailable, it reports “Still running” or “No output available” (`scripts/lib/monitor-skill.sh:394-400`). Interactive mode handles deleted skill directories, changing focused invocations, terminal cleanup, and background tail cleanup with bash/zsh-compatible traps (`scripts/lib/monitor-skill.sh:428-527`).
- validation_or_tests: Cross-reference shows `tests/test-skill-monitor.sh` sources `humanize.sh`, which sources this monitor file. The monitor is also user-facing through `scripts/humanize.sh` command routing and usage text.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7 unique item sections above; item identifiers are not repeated here to preserve the exactly-once evidence invariant`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`