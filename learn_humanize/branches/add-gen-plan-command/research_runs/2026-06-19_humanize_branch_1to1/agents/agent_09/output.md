# agent_09 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-009 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing behavior contract for the Humanize plugin. It defines the RLCR state-machine at a high level, documents the public command surface, and adds the branch-relevant `/humanize:gen-plan` workflow to the command table and options section.
- algorithmic_behavior: The README describes RLCR as an iterative loop: user plan enters Claude implementation, Claude writes progress, Codex reviews, feedback returns to Claude, and the loop exits only when Codex reports completion or max iterations is reached (`README.md:58-66`). It also documents the `gen-plan` algorithm as a five-step transformation: validate paths, check repository relevance, analyze draft quality, ask the user to resolve issues, and generate a structured `plan.md` with `AC-X` acceptance criteria (`README.md:103-120`).
- inputs_outputs_state: Inputs are a plan path for `/humanize:start-rlcr-loop <path/to/plan.md>` (`README.md:70-75`, `README.md:90-101`) or `--input <draft.md> --output <plan.md>` for `/humanize:gen-plan` (`README.md:103-111`). Outputs include `.humanize/rlcr/<timestamp>/` loop artifacts for RLCR monitoring (`README.md:75`) and a structured implementation plan for gen-plan (`README.md:112-120`). The loop state is conceptually round-based: Claude implements, Codex reviews, then the loop either feeds back, completes, or stops at max iteration (`README.md:60-66`).
- gates_or_invariants: README declares Codex CLI as a prerequisite (`README.md:52-55`). RLCR requires a clear plan with acceptance criteria before starting (`README.md:70-73`). The start command exposes bounded iteration via `--max`, model/effort selection via `--codex-model <MODEL:EFFORT>`, Codex timeout, and optional `--push-every-round` persistence gate (`README.md:90-100`). Gen-plan requires both input and output paths (`README.md:106-110`) and promises repository relevance, clarity, consistency, completeness, and functionality review before writing the output (`README.md:114-119`).
- dependencies_and_callers: The README command table maps public slash commands to command wrappers: `/start-rlcr-loop`, `/cancel-rlcr-loop`, and `/gen-plan` (`README.md:78-85`). `commands/gen-plan.md` delegates to `humanize:gen-plan`, whose skill invokes `scripts/validate-gen-plan-io.sh`, then relevance checking, draft analysis, user issue resolution, and plan writing (`commands/gen-plan.md:1-19`, `skills/gen-plan/SKILL.md:19-45`, `skills/gen-plan/SKILL.md:48-72`, `skills/gen-plan/SKILL.md:75-125`). `commands/start-rlcr-loop.md` delegates to `humanize:start-rlcr-loop`, whose skill invokes `scripts/setup-rlcr-loop.sh` and defines the Codex review feedback loop and goal tracker (`commands/start-rlcr-loop.md:1-19`, `skills/start-rlcr-loop/SKILL.md:11-24`, `skills/start-rlcr-loop/SKILL.md:25-56`). `commands/cancel-rlcr-loop.md` delegates to `humanize:cancel-rlcr-loop`, which operates on `.humanize/rlcr/*/state.md` or `finalize-state.md` (`commands/cancel-rlcr-loop.md:1-17`, `skills/cancel-rlcr-loop/SKILL.md:18-60`).
- edge_cases_or_failure_modes: README-level failure modes include missing Codex CLI (`README.md:52-55`), hitting maximum iterations instead of Codex completion (`README.md:64-65`, `README.md:93-99`), user cancellation (`README.md:76`), and gen-plan validation failures. The concrete gen-plan validator maps failures to deterministic exit codes: missing input, empty input, missing output directory, existing output path, no write permission, or invalid arguments (`scripts/validate-gen-plan-io.sh:4-11`, `scripts/validate-gen-plan-io.sh:78-123`).
- validation_or_tests: README is documentation, not directly executable, but its documented command flow is backed by command wrappers and skill contracts. The gen-plan IO gate has explicit validator output and exit code behavior (`scripts/validate-gen-plan-io.sh:73-130`). RLCR behavior is further exercised by the repository’s hook and runtime tests listed under `tests/`, including template, hook, state, cancellation, and finalize-phase tests.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-039 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable negative-path specification for the template loading/rendering subsystem used by RLCR hooks. It defines how validators should behave when templates or directories are missing, when Bash strict mode is active, and when placeholders are malformed or contain special characters.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh`, derives `TEMPLATE_DIR` through `get_template_dir`, then runs twelve assertions with pass/fail counters (`tests/test-error-scenarios.sh:11-24`, `tests/test-error-scenarios.sh:25-35`). Each test validates a specific contract: missing template returns empty and exit 0, missing template directory returns empty and exit 0, `load_and_render` missing template returns empty, empty content renders empty, regex-like characters are preserved as literal values, strict-mode callers continue, safe rendering falls back, fallback variables are substituted, valid template directory is accepted, invalid directory is rejected, empty placeholders stay literal, and unclosed placeholders stay literal (`tests/test-error-scenarios.sh:42-206`).
- inputs_outputs_state: Inputs are template directory paths, template names, template strings, and `VAR=value` render assignments (`tests/test-error-scenarios.sh:46-59`, `tests/test-error-scenarios.sh:72-100`, `tests/test-error-scenarios.sh:139-152`). Outputs are command substitution results and exit codes stored in `CONTENT`, `RESULT`, `EXIT_CODE`, and `SCRIPT_OUTPUT`, plus aggregate `TESTS_PASSED` and `TESTS_FAILED` counters (`tests/test-error-scenarios.sh:22-35`, `tests/test-error-scenarios.sh:46-52`, `tests/test-error-scenarios.sh:211-225`). The terminal state transition is pass if failures are zero, exit 0; otherwise exit 1 (`tests/test-error-scenarios.sh:218-225`).
- gates_or_invariants: The central invariant is fail-soft template loading: missing templates and missing directories must not crash validators or strict-mode scripts (`tests/test-error-scenarios.sh:42-78`, `tests/test-error-scenarios.sh:108-131`). Safe rendering must provide a usable fallback when the template is absent and must still substitute variables inside the fallback (`tests/test-error-scenarios.sh:133-158`). Rendering must not treat replacement values as regex syntax (`tests/test-error-scenarios.sh:93-106`), and malformed placeholder syntax must remain unchanged (`tests/test-error-scenarios.sh:182-206`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh` (`tests/test-error-scenarios.sh:13`). The tested functions are implemented there: `load_template` returns empty for missing files while warning to stderr (`hooks/lib/template-loader.sh:36-47`), `render_template` performs single-pass awk replacement from `TMPL_VAR_` environment variables (`hooks/lib/template-loader.sh:50-132`), `load_and_render` renders only non-empty loaded content (`hooks/lib/template-loader.sh:134-147`), `load_and_render_safe` falls back on missing or empty template output (`hooks/lib/template-loader.sh:167-203`), and `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`). Callers include RLCR hook validators and `hooks/loop-codex-stop-hook.sh`, which rely on safe template messages for block reasons.
- edge_cases_or_failure_modes: The script intentionally covers missing files, missing directories, strict mode with `set -euo pipefail`, empty template content, special characters including brackets, parentheses, and asterisks, missing safe-render templates, invalid template roots, `{{}}`, and unclosed `{{` placeholders (`tests/test-error-scenarios.sh:42-206`). A notable environment-sensitive failure exists in read-only execution: `render_template` uses a Bash here-string into awk (`hooks/lib/template-loader.sh:74-129`), and in this read-only sandbox that produced `cannot create temp file for here document: Operation not permitted`, causing render-dependent tests to return empty.
- validation_or_tests: I ran `bash tests/test-error-scenarios.sh` from the read-only branch export. Result: exit 1, with 8 passed and 4 failed. Failures were Test 5 special regex characters, Test 8 fallback variable substitution, Test 11 empty placeholder, and Test 12 unclosed placeholder. Each failure was preceded by the here-document temp-file permission error from `hooks/lib/template-loader.sh:129`, so the observed failure appears tied to the read-only sandbox’s inability to create the temporary file needed by Bash here-string rendering, not to the test file mutating state.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-069 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop hook’s large-file gate. It turns detected oversized changed files into a user-facing “stop blocked” reason with file list, line limit, and remediation requirements.
- algorithmic_behavior: The template accepts `{{MAX_LINES}}` and `{{LARGE_FILES}}`, explains why oversized files block stopping, then branches remediation instructions by file type: code files must be split into smaller modules while preserving behavior; documentation files must be split into logical sections with cross-references and narrative flow; after splitting, changes must be committed and exit attempted again (`prompt-template/block/large-files.md:1-25`). It is rendered by `load_and_render_safe` when the stop hook detects changed code or documentation files over the configured line threshold (`hooks/loop-codex-stop-hook.sh:399-409`).
- inputs_outputs_state: Inputs are `MAX_LINES`, currently set to `2000` in the stop hook (`hooks/loop-codex-stop-hook.sh:346`), and `LARGE_FILES`, accumulated from git status entries whose line count exceeds the threshold (`hooks/loop-codex-stop-hook.sh:349-397`). The rendered output becomes `REASON`, then the hook emits JSON with `"decision": "block"`, the rendered reason, and a system message (`hooks/loop-codex-stop-hook.sh:407-418`). State transition: an attempted stop is converted into a blocked stop until oversized changed files are split below the threshold and committed.
- gates_or_invariants: The gate only runs inside a Git repository after `git status --porcelain` succeeds (`hooks/loop-codex-stop-hook.sh:320-348`). It considers tracked or new files reported by git status, skips empty status lines, normalizes rename targets, skips deleted files, and only checks recognized code/documentation extensions (`hooks/loop-codex-stop-hook.sh:351-385`). It validates numeric `wc -l` output before comparison and blocks only when `line_count > MAX_LINES` (`hooks/loop-codex-stop-hook.sh:387-399`). The template itself enforces two invariants: each split file should be below `{{MAX_LINES}}` lines, and code functionality must remain strictly unchanged (`prompt-template/block/large-files.md:13-18`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh` in the “Large File Detection” quick check (`hooks/loop-codex-stop-hook.sh:340-421`). Rendering depends on `hooks/lib/template-loader.sh` and its `load_and_render_safe` fallback semantics (`hooks/lib/template-loader.sh:167-203`). The template reference is discoverable by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails if referenced template files are missing (`tests/test-template-references.sh:52-111`, `tests/test-template-references.sh:221-230`).
- edge_cases_or_failure_modes: Files with unsupported extensions are ignored, even if very large (`hooks/loop-codex-stop-hook.sh:370-385`). Deleted files are ignored (`hooks/loop-codex-stop-hook.sh:365-368`). Renames use only the new path after `" -> "` (`hooks/loop-codex-stop-hook.sh:360-363`). Non-numeric or failed line counts are skipped (`hooks/loop-codex-stop-hook.sh:387-391`). If the template is missing or renders empty, the hook uses an inline fallback block with the same core variables (`hooks/loop-codex-stop-hook.sh:399-409`), preserving fail-closed behavior for the gate.
- validation_or_tests: The assigned template was inspected directly (`prompt-template/block/large-files.md:1-25`). Its caller path was verified by repository search and the stop-hook line references. `tests/test-template-references.sh` includes a static validation algorithm that would catch a missing `block/large-files.md` reference because it scans `hooks/loop-codex-stop-hook.sh` and checks the referenced file exists (`tests/test-template-references.sh:56-107`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item sections present in `Item Evidence`; exact set matches the assignment table
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`