# agent_10 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-010 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared RLCR hook library directory. It contains the reusable state, path-classification, command-detection, template-loading, and loop-exit primitives used by the read/write/edit/bash validators, plan-file validator, and Codex stop hook. Recursive inspection found exactly `hooks/lib/template-loader.sh` and `hooks/lib/loop-common.sh`.
- algorithmic_behavior: `template-loader.sh` resolves the plugin prompt-template directory from the hook library location, loads templates, and renders `{{VAR}}` placeholders in a single awk pass so substituted values are not rescanned (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`). Missing variables remain as placeholders, and missing templates return empty content or fallback-backed content through `load_and_render_safe` (`hooks/lib/template-loader.sh:119`, `hooks/lib/template-loader.sh:170`). `loop-common.sh` sources the template loader, initializes `TEMPLATE_DIR`, validates expected template subdirectories, finds the newest active loop directory, parses YAML-like state frontmatter, classifies protected paths, detects bash writes, authorizes cancel renames, protects `.humanize`, and renames state on loop end (`hooks/lib/loop-common.sh:46`, `hooks/lib/loop-common.sh:63`, `hooks/lib/loop-common.sh:112`, `hooks/lib/loop-common.sh:284`, `hooks/lib/loop-common.sh:443`, `hooks/lib/loop-common.sh:574`, `hooks/lib/loop-common.sh:622`).
- inputs_outputs_state: Inputs are shell arguments from hook scripts: loop base directories, state file paths, command strings already lowercased by validators, template names, and `VAR=value` template bindings. Outputs are stdout path/content values, stderr block messages, function return codes, and state-file renames via `end_loop`. Parsed state is exported through caller-visible globals such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_PLAN_TRACKED`, `STATE_START_BRANCH`, `STATE_PUSH_EVERY_ROUND`, `STATE_CODEX_MODEL`, `STATE_CODEX_EFFORT`, and `STATE_CODEX_TIMEOUT` (`hooks/lib/loop-common.sh:99`).
- gates_or_invariants: Active loop discovery only accepts the newest timestamp directory and only when it contains `state.md` or `finalize-state.md`, preventing older loop revival (`hooks/lib/loop-common.sh:58`, `hooks/lib/loop-common.sh:75`). State parsing defaults `current_round` to `0`, `max_iterations` to `10`, and `push_every_round` to `false` (`hooks/lib/loop-common.sh:133`). `is_finalize_state_file_path` and `is_finalize_summary_path` use suffix matching for finalize-specific gates (`hooks/lib/loop-common.sh:262`, `hooks/lib/loop-common.sh:268`). Cancel authorization requires `.cancel-requested`, rejects command substitution, rejects chaining/newlines/residual `$`, requires exactly two mv args, and restricts source/destination to the active loop state/finalize-state and cancel-state paths (`hooks/lib/loop-common.sh:288`, `hooks/lib/loop-common.sh:295`, `hooks/lib/loop-common.sh:328`, `hooks/lib/loop-common.sh:396`). `git_adds_humanize` blocks direct `.humanize` staging, broad force-adds, and broad add-all when `.humanize` exists (`hooks/lib/loop-common.sh:421`, `hooks/lib/loop-common.sh:480`, `hooks/lib/loop-common.sh:511`, `hooks/lib/loop-common.sh:523`).
- dependencies_and_callers: `loop-common.sh` is sourced by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The validators use `find_active_loop`, `parse_state_file`, path classifiers, `command_modifies_file`, and block-message helpers to enforce the same state model across tool hooks. `loop-codex-stop-hook.sh` uses `parse_state_file` for phase state and `end_loop` for `complete`, `maxiter`, `stop`, and `unexpected` exits (`hooks/loop-codex-stop-hook.sh:88`, `hooks/loop-codex-stop-hook.sh:641`, `hooks/loop-codex-stop-hook.sh:1049`).
- edge_cases_or_failure_modes: Template directory validation only warns, allowing inline fallbacks to keep hooks operational when templates are missing (`hooks/lib/loop-common.sh:53`). `render_template` is deliberately single-pass to avoid placeholder injection from review content or other substituted values (`hooks/lib/template-loader.sh:54`). `find_active_loop` ignores older directories even if they contain state files, so an abnormal latest directory without state causes no active loop to be found (`hooks/lib/loop-common.sh:71`). `is_state_file_path` matches `finalize-state.md` because it ends in `state.md`, so callers must check finalize-state first; the hook scripts call this out and do so (`hooks/loop-write-validator.sh:103`, `hooks/loop-edit-validator.sh:84`, `hooks/loop-bash-validator.sh:91`). `command_modifies_file` is regex-based and covers common redirection, tee, sed/awk/perl inplace, mv/cp/rm, dd, truncate, printf, and exec redirection forms, but it is not a shell parser (`hooks/lib/loop-common.sh:571`).
- validation_or_tests: Covered by several focused shell specs: `tests/test-finalize-phase.sh` for finalize-state detection/protection and state parsing; `tests/test-cancel-signal-file.sh` for `is_cancel_authorized`; `tests/test-state-exit-naming.sh` for `find_active_loop` and `end_loop`; `tests/test-humanize-escape.sh` for `.humanize` git-add variants; `tests/test-bash-validator-patterns.sh` for `command_modifies_file`; `tests/test-template-loader.sh` and `tests/test-template-references.sh` for template loading/reference integrity. I did not run the full tests in this read-only export; an attempted git status failed because the export is not a git checkout and git/Xcode tried to write cache files outside the allowed filesystem.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-040 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the Finalize Phase algorithm. It defines positive and negative cases for transition from normal RLCR rounds into finalize, finalize completion, validator handling of finalize-state, and preservation of normal round behavior (`tests/test-finalize-phase.sh:5`).
- algorithmic_behavior: The test builds a temporary git repo and loop directory, sources `hooks/lib/loop-common.sh`, mocks `codex`, writes `state.md`, `plan.md`, and `goal-tracker.md`, then drives hook scripts with JSON hook input (`tests/test-finalize-phase.sh:40`, `tests/test-finalize-phase.sh:44`, `tests/test-finalize-phase.sh:48`, `tests/test-finalize-phase.sh:83`, `tests/test-finalize-phase.sh:118`). It first verifies helper predicates for `finalize-state.md` and `finalize-summary.md`, then exercises active-loop detection, validators, stop-hook quick checks, Codex COMPLETE handling, max-iteration behavior, todo gating, and normal non-COMPLETE review flow.
- inputs_outputs_state: Inputs include mock Codex review text ending in `COMPLETE` or non-complete feedback, generated state frontmatter fields, synthetic hook JSON for Write/Edit/Bash/Read/UserPromptSubmit/Stop hooks, a temporary transcript JSONL containing TodoWrite states, and filesystem mutations inside the temporary repo. Expected outputs are hook exit codes, JSON block decisions, block reasons containing finalize/summary/git/todo text, created review-result files, and state-file transitions among `state.md`, `finalize-state.md`, `complete-state.md`, and `maxiter-state.md`.
- gates_or_invariants: `find_active_loop` must treat `finalize-state.md` as active and `complete-state.md` as inactive (`tests/test-finalize-phase.sh:226`, `tests/test-finalize-phase.sh:241`). Write validator must allow active-loop `finalize-summary.md` but Write/Edit/Bash must block `finalize-state.md` modification, including bash redirection and mv/cp from finalize-state (`tests/test-finalize-phase.sh:281`, `tests/test-finalize-phase.sh:293`, `tests/test-finalize-phase.sh:305`, `tests/test-finalize-phase.sh:317`, `tests/test-finalize-phase.sh:329`, `tests/test-finalize-phase.sh:341`). Finalize completion requires a finalize summary, clean git state, and completed todos, and it must not invoke Codex again (`tests/test-finalize-phase.sh:365`, `tests/test-finalize-phase.sh:382`, `tests/test-finalize-phase.sh:390`, `tests/test-finalize-phase.sh:412`, `tests/test-finalize-phase.sh:434`, `tests/test-finalize-phase.sh:521`). Normal COMPLETE enters finalize unless current round is already at max iterations (`tests/test-finalize-phase.sh:460`, `tests/test-finalize-phase.sh:478`).
- dependencies_and_callers: The spec drives `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh` (`tests/test-finalize-phase.sh:284`, `tests/test-finalize-phase.sh:308`, `tests/test-finalize-phase.sh:320`, `tests/test-finalize-phase.sh:372`, `tests/test-finalize-phase.sh:630`, `tests/test-finalize-phase.sh:643`, `tests/test-finalize-phase.sh:657`). Its assertions map to stop-hook implementation branches: finalize-state detection at `hooks/loop-codex-stop-hook.sh:72`, finalize summary selection at `hooks/loop-codex-stop-hook.sh:522`, finalize completion without Codex at `hooks/loop-codex-stop-hook.sh:651`, COMPLETE-to-finalize rename at `hooks/loop-codex-stop-hook.sh:961`, and finalize prompt generation at `hooks/loop-codex-stop-hook.sh:983`.
- edge_cases_or_failure_modes: The test includes missing finalize summary, dirty git status, incomplete todos, unauthorized direct writes/edits/bash mutations of `finalize-state.md`, mv/cp source protection, `complete-state.md` not being active, max-iteration COMPLETE bypassing finalize and ending as maxiter, and normal non-COMPLETE review preserving `state.md` and incrementing `current_round` (`tests/test-finalize-phase.sh:238`, `tests/test-finalize-phase.sh:365`, `tests/test-finalize-phase.sh:390`, `tests/test-finalize-phase.sh:478`, `tests/test-finalize-phase.sh:515`, `tests/test-finalize-phase.sh:575`, `tests/test-finalize-phase.sh:591`).
- validation_or_tests: This file is itself validation. It exits with `TESTS_FAILED`, increments pass/fail counters through helper functions, and prints a summary (`tests/test-finalize-phase.sh:36`, `tests/test-finalize-phase.sh:667`). I inspected it directly but did not execute it because the branch export is read-only and local git operations already failed in this environment due external cache writes and missing `.git`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-070 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Template-backed guard message for the plan backup invariant. It defines the user-facing block reason when a hook detects an attempted modification of `.humanize/rlcr/.../plan.md`.
- algorithmic_behavior: The template states that `plan.md` in the loop directory is a backup of the original plan and cannot be modified, because it preserves plan integrity during the RLCR loop; it instructs consumers to read the plan instead of modifying it (`prompt-template/block/plan-backup-protected.md:1`, `prompt-template/block/plan-backup-protected.md:3`, `prompt-template/block/plan-backup-protected.md:5`, `prompt-template/block/plan-backup-protected.md:7`). It contains no variables, so rendering is pass-through unless a missing-template fallback is used.
- inputs_outputs_state: Input is the template name `block/plan-backup-protected.md` requested through `load_and_render_safe`. Output is a markdown block reason printed to stderr by pre-tool validators. It does not mutate state itself; the state invariant it supports is that the loop’s backup `plan.md` remains byte-stable for later integrity checks against the original plan file.
- gates_or_invariants: The guarded invariant is backup immutability. Write validator blocks Write to any `plan.md` inside `.humanize/rlcr` (`hooks/loop-write-validator.sh:132`); edit validator blocks Edit similarly (`hooks/loop-edit-validator.sh:100`); bash validator blocks shell modifications targeting `.humanize/rlcr(/timestamp)?/plan.md` (`hooks/loop-bash-validator.sh:295`). The stop hook later relies on this invariant by requiring the backup to exist and diff-clean against the original plan before exit (`hooks/loop-codex-stop-hook.sh:182`, `hooks/loop-codex-stop-hook.sh:230`).
- dependencies_and_callers: Direct callers are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`, each through `load_and_render_safe` from `hooks/lib/template-loader.sh` (`hooks/loop-write-validator.sh:135`, `hooks/loop-edit-validator.sh:104`, `hooks/loop-bash-validator.sh:297`). Template reference coverage appears in `tests/test-template-references.sh`, and behavior coverage for the three hook surfaces is listed in `tests/test-plan-file-hooks.sh` (`tests/test-plan-file-hooks.sh:195`, `tests/test-plan-file-hooks.sh:212`, `tests/test-plan-file-hooks.sh:229`).
- edge_cases_or_failure_modes: Because the template has no placeholders, variable substitution cannot corrupt it. If the template is absent or empty, callers use fallback messages such as “Writing to plan.md backup is not allowed during RLCR loop” (`hooks/loop-write-validator.sh:134`, `hooks/loop-edit-validator.sh:103`, `hooks/loop-bash-validator.sh:296`). The bash gate depends on `command_modifies_file`, so it covers many common write forms but inherits regex-parser limitations from the shared library.
- validation_or_tests: The plan-backup block is indirectly validated by plan-file hook tests for Write/Edit/Bash and by template-reference tests ensuring message functions point at existing templates. Its operational counterpart is stop-hook plan integrity validation, which blocks when the backup is missing or differs from the original plan (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:231`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_GEN_PLAN_COMMAND-HZ-010`, `ADD_GEN_PLAN_COMMAND-HZ-040`, `ADD_GEN_PLAN_COMMAND-HZ-070`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`