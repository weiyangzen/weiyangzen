# agent_18 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-018 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Thin slash-command workflow definition for cancelling an active RLCR loop. The file is not the cancellation algorithm itself; it is the command-facing dispatch surface that restricts execution to `Skill(humanize:cancel-rlcr-loop:*)` and tells the caller to invoke `humanize:cancel-rlcr-loop` (`commands/cancel-rlcr-loop.md:1-17`).
- algorithmic_behavior: On command invocation, Claude Code should use the Skill tool rather than inline shell logic. The command delegates discovery, active-loop classification, cancel signal creation, state-file renaming, finalize-phase confirmation, and reporting to `skills/cancel-rlcr-loop/SKILL.md`. The referenced skill selects the newest `.humanize/rlcr/*/` directory, treats `state.md` as a normal active loop, treats `finalize-state.md` as Finalize Phase, writes `.cancel-requested`, and renames active state to `cancel-state.md` (`skills/cancel-rlcr-loop/SKILL.md:20-58`).
- inputs_outputs_state: Input is the user's `/humanize:cancel-rlcr-loop` command with no explicit arguments in this wrapper. Output is a delegated Skill invocation block (`commands/cancel-rlcr-loop.md:11-15`). State mutation is intentionally outside the command file: the skill may create `.humanize/rlcr/<timestamp>/.cancel-requested` and move `state.md` or `finalize-state.md` to `cancel-state.md` (`skills/cancel-rlcr-loop/SKILL.md:37-54`).
- gates_or_invariants: The command frontmatter limits allowed tools to the cancellation skill only (`commands/cancel-rlcr-loop.md:1-5`). The skill invariant is that the active loop is always the newest timestamped loop directory and is active only when `state.md` or `finalize-state.md` exists (`skills/cancel-rlcr-loop/SKILL.md:58`). Finalize Phase adds a user-confirmation gate before cancellation (`skills/cancel-rlcr-loop/SKILL.md:43-56`).
- dependencies_and_callers: This command depends directly on `skills/cancel-rlcr-loop/SKILL.md`. It is referenced as the cancellation path from setup/help surfaces in `scripts/setup-rlcr-loop.sh` and README usage docs, and it is tested for Skill delegation by `tests/test-skill-structure.sh:180-188` plus more detailed invocation tests in `tests/test-skill-invocation.sh:110-161`.
- edge_cases_or_failure_modes: If no `.humanize/rlcr/*/` directory exists, the delegated skill reports no active loop (`skills/cancel-rlcr-loop/SKILL.md:20-35`). If a loop directory exists without `state.md` or `finalize-state.md`, it reports that the loop has already ended or was not properly started (`skills/cancel-rlcr-loop/SKILL.md:29-35`). Because this wrapper only delegates, any missing or malformed skill definition breaks actual cancellation even if the command file exists.
- validation_or_tests: `tests/test-skill-structure.sh` checks `commands/cancel-rlcr-loop.md` exists and contains `Skill(humanize:cancel-rlcr-loop` in allowed tools (`tests/test-skill-structure.sh:180-188`). `tests/test-skill-invocation.sh` also checks discovery, thin-wrapper behavior, allowed tools, and command-to-skill separation. I did not execute tests in this read-only research run.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-048 `file` `tests/test-skill-structure.sh`
- cursor: `[_]`
- core_role: Executable specification for plugin structural contracts and new `gen-plan` IO validation behavior. It is a Bash test runner, not production runtime code, but it defines pass/fail gates for skill metadata, command delegation, version consistency, fixture-based negative validation, and `scripts/validate-gen-plan-io.sh` exit-code semantics (`tests/test-skill-structure.sh:1-9`, `tests/test-skill-structure.sh:682-798`).
- algorithmic_behavior: The test initializes repo paths from its own location (`tests/test-skill-structure.sh:11-14`), tracks counters through `pass`/`fail` helpers (`tests/test-skill-structure.sh:22-40`), runs positive checks PT-1 through PT-9, then creates temporary invalid fixtures for negative checks NT-1 through NT-6 (`tests/test-skill-structure.sh:47-249`). It ends by executing the `gen-plan` validator with controlled argument/path cases and exits `0` only if `TESTS_FAILED` is zero (`tests/test-skill-structure.sh:800-818`).
- inputs_outputs_state: Inputs are the repository files under `skills/`, `commands/`, `.claude-plugin/`, README, and `scripts/validate-gen-plan-io.sh`. It creates temp fixture directories with `mktemp -d` and writes invalid `SKILL.md` files inside them (`tests/test-skill-structure.sh:257-259`, `tests/test-skill-structure.sh:281-289`, `tests/test-skill-structure.sh:333-371`). Outputs are colored PASS/FAIL lines, aggregate counters, and shell exit status. Internal state is `TESTS_PASSED`, `TESTS_FAILED`, temp fixture paths, extracted metadata values, and per-command `EXIT_CODE`.
- gates_or_invariants: Skill directories must contain `SKILL.md`, names must match lowercase kebab-case, required frontmatter must include `name` and `description`, `model` must be present for actual skills, `allowed-tools` must exist, `context: fork` is forbidden, and skills must be user-invocable by default (`tests/test-skill-structure.sh:55-223`). Version must match across `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and README (`tests/test-skill-structure.sh:226-246`). `gen-plan` validator must map invalid arguments to exit `6`, missing input to `1`, empty input to `2`, missing output directory to `3`, existing output or directory output to `4`, and valid paths to `0` (`tests/test-skill-structure.sh:694-794`).
- dependencies_and_callers: Depends on Bash, standard tools (`sed`, `grep`, `awk`, `mktemp`, `touch`, `mkdir`, `wc` indirectly through the validator), and repo layout. It directly tests `skills/start-rlcr-loop/SKILL.md`, `skills/cancel-rlcr-loop/SKILL.md`, `skills/gen-plan/SKILL.md`, `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, metadata JSON files, README, and `scripts/validate-gen-plan-io.sh`. It is included in the aggregate suite by `tests/run-all-tests.sh:51`. The `gen-plan` validator being specified is used by `skills/gen-plan/SKILL.md:29-45`, while the command wrapper delegates via `commands/gen-plan.md:1-19`.
- edge_cases_or_failure_modes: YAML validation is intentionally lightweight and pattern-based: `check_yaml_syntax` only verifies broad `key:` or list-line shapes inside frontmatter and does not parse full YAML (`tests/test-skill-structure.sh:398-412`). It creates an unclosed-frontmatter fixture but does not directly assert that fixture fails before moving on to actual skill YAML checks (`tests/test-skill-structure.sh:432-447`). The model validator accepts exact `opus|sonnet|haiku` or prefixes `claude-`, `gpt-`, `o[0-9]`, `gemini-`, so it is a naming heuristic rather than capability validation (`tests/test-skill-structure.sh:584-592`). The `trap` is assigned twice, so the later trap for script-test temp cleanup replaces the earlier fixture cleanup trap (`tests/test-skill-structure.sh:257-259`, `tests/test-skill-structure.sh:689-692`).
- validation_or_tests: This file is itself a validation entry. Its `gen-plan` section exercises `scripts/validate-gen-plan-io.sh`, whose documented exit codes and checks are implemented at `scripts/validate-gen-plan-io.sh:4-11`, argument parsing at `scripts/validate-gen-plan-io.sh:28-66`, path normalization at `scripts/validate-gen-plan-io.sh:68-76`, input/output gates at `scripts/validate-gen-plan-io.sh:78-123`, and success output at `scripts/validate-gen-plan-io.sh:125-131`. I inspected but did not execute the script.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-078 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR stop-hook gate that blocks loop exit when the current work summary file has not been written. It supplies the user-facing remediation text and injects the expected `{{SUMMARY_FILE}}` path (`prompt-template/block/work-summary-missing.md:1-16`).
- algorithmic_behavior: The template is rendered by `hooks/loop-codex-stop-hook.sh` after the hook identifies the active loop, phase, and expected summary path. Normal rounds require `round-${CURRENT_ROUND}-summary.md`; Finalize Phase requires `finalize-summary.md` (`hooks/loop-codex-stop-hook.sh:522-527`). If the file does not exist, the hook renders `block/work-summary-missing.md` with `SUMMARY_FILE=$SUMMARY_FILE`, returns JSON with `"decision": "block"`, and exits before Codex review or further loop progression (`hooks/loop-codex-stop-hook.sh:529-553`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable. The template output tells the agent it attempted to exit without a summary, shows the exact required path, and requires content covering implementation, created/modified files, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:3-16`). State is not mutated by the template; the next state transition is external: the agent writes the missing summary and retries exit.
- gates_or_invariants: The summary file existence gate is hard: missing summary blocks normal and finalize exits. The stop hook uses `load_and_render_safe`, so if the template is missing or empty, a fallback message still blocks exit (`hooks/loop-codex-stop-hook.sh:533-537`; `hooks/lib/template-loader.sh:167-203`). Rendering is single-pass, leaving missing placeholders literal and avoiding recursive placeholder expansion (`hooks/lib/template-loader.sh:50-132`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`. Rendering depends on `hooks/lib/template-loader.sh`, sourced indirectly through loop common setup (`hooks/loop-codex-stop-hook.sh:46-58`). The hook itself depends on active loop discovery and state parsing before it reaches this gate (`hooks/loop-codex-stop-hook.sh:59-89`). Related tests verify missing-summary blocking in Finalize Phase (`tests/test-finalize-phase.sh:365-379`) and broader template-reference safety patterns (`tests/test-template-references.sh:172-200`).
- edge_cases_or_failure_modes: If `SUMMARY_FILE` contains unusual characters, rendering passes it through as an environment-backed value into the template; single-pass rendering prevents placeholder injection but does not otherwise sanitize display (`hooks/lib/template-loader.sh:62-80`, `hooks/lib/template-loader.sh:116-129`). The gate checks only file existence at this point, not summary quality or required section completeness (`hooks/loop-codex-stop-hook.sh:529`). If no active loop or no state file exists, the stop hook exits before this template is used (`hooks/loop-codex-stop-hook.sh:59-82`).
- validation_or_tests: `tests/test-finalize-phase.sh` explicitly removes `finalize-summary.md`, invokes the stop hook, and expects a blocking decision containing “summary” (`tests/test-finalize-phase.sh:365-379`). Template loading behavior is covered by template-loader tests and reference checks, including safe fallback expectations (`tests/test-template-references.sh:172-200`). I inspected these references without executing tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item sections present above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`