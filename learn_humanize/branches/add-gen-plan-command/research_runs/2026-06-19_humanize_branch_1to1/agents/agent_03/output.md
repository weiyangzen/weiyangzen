# agent_03 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Thin slash-command dispatch layer for the user-facing Humanize workflow commands. The directory contains exactly three files and no nested directories: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, and `commands/cancel-rlcr-loop.md`.
- algorithmic_behavior: Each command file exposes metadata and delegates execution to a corresponding skill rather than embedding the workflow algorithm. `commands/gen-plan.md:1-5` declares the command description, argument hint, and `Skill(humanize:gen-plan:*)` allowed tool, then instructs invocation of `humanize:gen-plan` with `$ARGUMENTS` at `commands/gen-plan.md:12-17`. `commands/start-rlcr-loop.md:1-5` similarly delegates to `humanize:start-rlcr-loop` with loop arguments at `commands/start-rlcr-loop.md:12-17`. `commands/cancel-rlcr-loop.md:1-4` delegates cancellation to `humanize:cancel-rlcr-loop`, with no user arguments, at `commands/cancel-rlcr-loop.md:11-15`.
- inputs_outputs_state: Inputs are slash-command arguments passed as `$ARGUMENTS` for plan generation and loop start; cancellation takes the active loop state from `.humanize/rlcr` via the skill rather than command arguments. Outputs are skill invocations, not direct file mutations. The dispatch files carry no state themselves; state transitions occur in target skills and scripts, especially `skills/start-rlcr-loop/SKILL.md`, `scripts/setup-rlcr-loop.sh`, and `skills/cancel-rlcr-loop/SKILL.md`.
- gates_or_invariants: Command-level invariant is enforced by `allowed-tools`: command bodies may invoke only the matching `Skill(...)` namespace. `hide-from-slash-command-tool: "true"` appears in all three command files, making these internal slash command wrappers rather than broad tool menus. The command layer intentionally does not include operational script paths or `.humanize/rlcr` mutation logic.
- dependencies_and_callers: Depends on `skills/gen-plan/SKILL.md`, `skills/start-rlcr-loop/SKILL.md`, and `skills/cancel-rlcr-loop/SKILL.md`. `skills/gen-plan/SKILL.md:21-25` defines validation, relevance checking, draft analysis, clarification, and plan generation. `skills/start-rlcr-loop/SKILL.md:11-15` calls `scripts/setup-rlcr-loop.sh`, and `skills/start-rlcr-loop/SKILL.md:17-24` describes the iterative work, summary, Codex review, feedback, and completion loop. `skills/cancel-rlcr-loop/SKILL.md:18-60` defines active-loop discovery and state renaming.
- edge_cases_or_failure_modes: Misrouting risk is low because each command names exactly one skill. If the skill is missing or renamed, the command has no fallback. If `$ARGUMENTS` are invalid for `gen-plan` or `start-rlcr-loop`, validation happens downstream, not here. The cancel command has no argument hint, so all ambiguity is resolved by newest active loop selection in the skill.
- validation_or_tests: `tests/test-skill-invocation.sh:125-143` verifies `start-rlcr-loop` is a thin wrapper whose full script reference lives in the skill, not the command. `tests/test-skill-invocation.sh:146-163` verifies `cancel-rlcr-loop` is also a thin wrapper and that `.humanize/rlcr` logic lives in the skill. `tests/test-skill-invocation.sh:330-342` and `tests/test-skill-invocation.sh:345-360` verify the start and cancel skills retain required instruction elements.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-033 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor graceful-stop behavior, focused on AC-1.1 and AC-1.3. It creates and destroys `.humanize` state to let a human verify monitor behavior in another terminal.
- algorithmic_behavior: The script dispatches on `${1:-}` via a `case` at `tests/manual-monitor-test.sh:23-72`. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99`, writes a minimal `state.md`, and writes a minimal `goal-tracker.md` at `tests/manual-monitor-test.sh:24-38`. `delete` removes `.humanize` at `tests/manual-monitor-test.sh:47-56` and tells the tester to confirm a graceful monitor stop. `cleanup` removes `.humanize` at `tests/manual-monitor-test.sh:58-61`. Unknown commands print usage and exit nonzero at `tests/manual-monitor-test.sh:63-70`.
- inputs_outputs_state: Input is one positional subcommand: `setup`, `delete`, or `cleanup`. Outputs are filesystem fixtures under `.humanize/rlcr/<timestamp>/` and console instructions. State transition sequence is: no fixture -> active synthetic loop state (`state.md` plus `goal-tracker.md`) -> `.humanize` removed -> monitor should exit cleanly and restore terminal state.
- gates_or_invariants: Uses `set -euo pipefail` at `tests/manual-monitor-test.sh:17`, computes `PROJECT_ROOT` relative to the script at `tests/manual-monitor-test.sh:19-21`, and always operates from repo root. The expected monitor command is fixed: `source scripts/humanize.sh && humanize monitor rlcr-loop` at `tests/manual-monitor-test.sh:10-12` and `tests/manual-monitor-test.sh:44-45`. The explicit expected stop message is `Monitoring stopped: .humanize/rlcr directory no longer exists` at `tests/manual-monitor-test.sh:51-53`.
- dependencies_and_callers: Depends on `scripts/humanize.sh` monitor behavior. The monitor checks for `.humanize/rlcr` before startup at `scripts/humanize.sh:20-25`, repeatedly detects deletion at `scripts/humanize.sh:547-552`, `scripts/humanize.sh:584-592`, and `scripts/humanize.sh:677-685`, and calls `_graceful_stop`. `_graceful_stop` calls `_cleanup` and prints the stop message at `scripts/humanize.sh:499-512`; `_cleanup` kills tail, restores terminal state, and prints stopped status at `scripts/humanize.sh:478-497`.
- edge_cases_or_failure_modes: This is a manual two-terminal test, so it does not assert output automatically. It removes the whole `.humanize` directory rather than only `.humanize/rlcr`, which exercises the same missing-loop-dir guard but is broader than the exact monitor path. Because the fixture timestamp is fixed in the future-looking synthetic name `2026-01-16_99-99-99`, it may dominate other local sessions if run in a real project with existing loop state. It intentionally checks for no shell glob errors at `tests/manual-monitor-test.sh:52-54`; the monitor avoids zsh no-match failures by using `find` instead of globs in `scripts/humanize.sh:27-49` and `scripts/humanize.sh:71-108`.
- validation_or_tests: Automated companion tests exist in `tests/test-monitor-runtime.sh`. That file verifies `_graceful_stop` emits the expected message and calls terminal restoration at `tests/test-monitor-runtime.sh:56-120`, and separately verifies directory deletion detection at `tests/test-monitor-runtime.sh:180-240`. `tests/test-monitor-e2e-real.sh` also exercises real monitor deletion behavior, but this assigned script is the manual specification used for human observation.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-063 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Bash pre-tool gate that prevents `git push` during a loop unless the loop was started with `--push-every-round`.
- algorithmic_behavior: The template itself is static markdown: title at `prompt-template/block/git-push.md:1`, policy text at `prompt-template/block/git-push.md:3-4`, and remediation guidance at `prompt-template/block/git-push.md:6-9`. It defines the user-facing output emitted when the algorithm blocks a push.
- inputs_outputs_state: No template variables are used. Runtime inputs come from hook state parsing: `hooks/loop-bash-validator.sh:56-73` checks `STATE_PUSH_EVERY_ROUND`; when the parsed value is not `true` and the lowercased command matches `git push`, it renders this template to stderr and exits `2`. State is read from active loop `state.md` or `finalize-state.md`; `hooks/loop-bash-validator.sh:45-53` chooses the state file and parses it. `scripts/setup-rlcr-loop.sh:148-150` sets `PUSH_EVERY_ROUND=true` when the flag is present, and `scripts/setup-rlcr-loop.sh:485-498` writes `push_every_round: $PUSH_EVERY_ROUND` into loop state.
- gates_or_invariants: Default invariant is local-only commits during RLCR unless `push_every_round` is true. `hooks/lib/loop-common.sh:121-137` parses `push_every_round` and defaults it to `false` when absent, so legacy or incomplete state files block pushes by default. The hook only detects commands beginning with optional whitespace plus `git push` at `hooks/loop-bash-validator.sh:63-65`, so the gate is specific to direct Bash tool push commands.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md" "$FALLBACK"` in `hooks/loop-bash-validator.sh:66-71`. Template resolution comes from `hooks/lib/template-loader.sh`: `get_template_dir` maps the hook library to `prompt-template` at `hooks/lib/template-loader.sh:24-31`, and `load_and_render_safe` falls back if the template is missing or empty at `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If the template is missing, the hook still blocks pushes using the inline fallback at `hooks/loop-bash-validator.sh:66-69`. If `push_every_round` has any value other than exact `true`, including empty or malformed values, pushes are blocked. Commands not matching the simple prefix pattern, such as shell wrappers or aliases, may fall outside this specific gate depending on how the Bash validator receives the command. The template’s remediation references `/humanize:start-rlcr-loop plan.md --push-every-round`, aligning with `commands/start-rlcr-loop.md:3`.
- validation_or_tests: `tests/test-template-loader.sh:55-65` verifies `block/git-push.md` loads and contains “Git Push Blocked”. `tests/test-template-loader.sh:211-222` verifies `load_and_render_safe` uses the existing template rather than fallback. `tests/test-templates-comprehensive.sh` also references the template in its comprehensive rendering coverage.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-093 `file` `skills/cancel-rlcr-loop/SKILL.md`
- cursor: `[_]`
- core_role: Authoritative skill workflow for cancelling the active RLCR loop while preserving loop state and avoiding unauthorized direct edits to loop state files.
- algorithmic_behavior: The skill first finds the newest timestamped loop directory with `ls -1d .humanize/rlcr/*/ | sort -r | head -1` at `skills/cancel-rlcr-loop/SKILL.md:20-25`. If no directory is found, it reports no active loop at `skills/cancel-rlcr-loop/SKILL.md:27`. It then classifies the newest loop as `NORMAL_LOOP`, `FINALIZE_PHASE`, or `NO_ACTIVE_LOOP` based on `state.md` or `finalize-state.md` at `skills/cancel-rlcr-loop/SKILL.md:29-35`. Normal cancellation reads state, creates `.cancel-requested`, then renames `state.md` to `cancel-state.md` at `skills/cancel-rlcr-loop/SKILL.md:37-41`. Finalize cancellation asks for confirmation before creating the signal and renaming `finalize-state.md` at `skills/cancel-rlcr-loop/SKILL.md:43-56`.
- inputs_outputs_state: Inputs are the `.humanize/rlcr/*/` directory set, active `state.md` or `finalize-state.md`, and optional user confirmation during Finalize Phase. Outputs are `.cancel-requested`, `cancel-state.md`, and user-facing status messages. Main state transitions are `state.md -> cancel-state.md` for normal loops and `finalize-state.md -> cancel-state.md` for Finalize Phase, with the cancel signal created before the rename.
- gates_or_invariants: The newest timestamped loop directory is the only current loop, as stated at `skills/cancel-rlcr-loop/SKILL.md:58`. A loop is active only if `state.md` or `finalize-state.md` exists. The “signal first, rename second” invariant at `skills/cancel-rlcr-loop/SKILL.md:39-40` and `skills/cancel-rlcr-loop/SKILL.md:52-53` coordinates with the Bash validator: `hooks/loop-bash-validator.sh:98-115` blocks state-file modification unless `is_cancel_authorized` returns true. `hooks/lib/loop-common.sh:274-413` requires `.cancel-requested`, rejects command substitution/chaining, and allows only an exact `mv` from active `state.md` or `finalize-state.md` to active `cancel-state.md`.
- dependencies_and_callers: Exposed through `commands/cancel-rlcr-loop.md:7-15`. Depends on allowed tools listed in skill frontmatter: restricted Bash patterns for listing loops, touching cancel signal, moving state files, reading state files, plus `Read` and `AskUserQuestion` at `skills/cancel-rlcr-loop/SKILL.md:5-13`. Runtime hook compatibility depends on `hooks/lib/loop-common.sh` cancellation authorization and `hooks/loop-bash-validator.sh` state-file protections.
- edge_cases_or_failure_modes: If `.humanize/rlcr` has directories but the newest lacks active state files, the skill reports that no active loop exists and ignores older directories, matching `find_active_loop` behavior in `hooks/lib/loop-common.sh:58-81`. If the loop is in Finalize Phase and the user declines, no state transition occurs at `skills/cancel-rlcr-loop/SKILL.md:55-56`. If the signal file is missing or the rename command differs from the exact authorized shape, the Bash validator blocks the operation. Race conditions are possible if the active loop changes between newest-directory discovery and state rename, because the skill’s commands are separate shell steps.
- validation_or_tests: `tests/test-skill-invocation.sh:146-163` verifies the command delegates and the skill contains the `.humanize/rlcr` logic. `tests/test-skill-invocation.sh:345-360` verifies required cancel instruction elements. `tests/test-cancel-signal-file.sh:75-113` verifies `mv state.md` to `cancel-state.md` is allowed with a signal file. `tests/test-cancel-signal-file.sh:116-153` verifies the rename is blocked without signal and that other writes are blocked even with signal. Additional negative cases in the same test cover wrong destinations and wrong signal location.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`