# agent_20 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-020 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Thin slash-command wrapper that exposes the RLCR loop entrypoint and delegates the actual workflow to the `humanize:start-rlcr-loop` skill. The command itself is intentionally not the implementation surface: its frontmatter declares `allowed-tools: ["Skill(humanize:start-rlcr-loop:*)"]` at `commands/start-rlcr-loop.md:4`, hides the wrapper from the slash-command tool at `commands/start-rlcr-loop.md:5`, and forwards `$ARGUMENTS` to the skill at `commands/start-rlcr-loop.md:14-17`.
- algorithmic_behavior: Runtime behavior is a two-stage dispatch. First, the command receives `<path/to/plan.md> [--max N] [--codex-model MODEL:EFFORT] [--codex-timeout SECONDS] [--push-every-round]` from its argument hint at `commands/start-rlcr-loop.md:3`. Second, it invokes the Skill tool with `Skill: humanize:start-rlcr-loop` and unmodified `Arguments: $ARGUMENTS` at `commands/start-rlcr-loop.md:14-17`. The delegated skill then runs `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` per `skills/start-rlcr-loop/SKILL.md:11-15`.
- inputs_outputs_state: Input is the user-provided plan path plus optional loop controls. The wrapper output is not a generated file or state mutation; it is a Skill invocation. State creation happens downstream in `scripts/setup-rlcr-loop.sh`, which validates arguments and writes loop files under `.humanize/rlcr/...`: it initializes defaults for max iterations, Codex model/effort/timeout, and push policy at `scripts/setup-rlcr-loop.sh:17-41`; emits a `goal-tracker.md` structure at `scripts/setup-rlcr-loop.sh:521-594`; writes the initial `round-0-prompt.md` and `round-0-summary.md` path at `scripts/setup-rlcr-loop.sh:600-653`; then prints activation metadata including plan file, branch, max iterations, Codex settings, and loop directory at `scripts/setup-rlcr-loop.sh:667-684`.
- gates_or_invariants: Wrapper invariant is strict delegation: command is a small adapter, while the skill owns execution tools. This is validated by tests that require the wrapper to include `Skill(humanize:start-rlcr-loop` and avoid embedding the setup script directly: `tests/test-skill-invocation.sh:95-105` and `tests/test-skill-invocation.sh:125-143`. Downstream gates include plan file presence at `scripts/setup-rlcr-loop.sh:197-205`, git repository and commit existence at `scripts/setup-rlcr-loop.sh:211-220`, relative path-only plan input at `scripts/setup-rlcr-loop.sh:227-230`, no spaces in the plan path at `scripts/setup-rlcr-loop.sh:233-235`, minimum plan content at `scripts/setup-rlcr-loop.sh:410-415`, Codex CLI availability at `scripts/setup-rlcr-loop.sh:417-423`, branch-name YAML safety at `scripts/setup-rlcr-loop.sh:435-443`, and Codex model character safety at `scripts/setup-rlcr-loop.sh:445-452`.
- dependencies_and_callers: Depends on the `humanize:start-rlcr-loop` skill via the Skill tool. The skill depends on `scripts/setup-rlcr-loop.sh` through an allowed Bash tool declaration at `skills/start-rlcr-loop/SKILL.md:5-7`. Related public documentation names the command in `README.md` and setup script help, but the assigned wrapper’s direct contract is covered by `tests/test-skill-invocation.sh` and `tests/test-skill-structure.sh:164-178`.
- edge_cases_or_failure_modes: Wrapper-level failure modes are mostly structural: missing Skill delegation, accidentally embedding full setup logic in the command, or allowing tools other than the Skill delegation. Downstream failures include no plan file, invalid `--max`, missing Codex, non-git repository, unsafe branch/model strings, absolute plan paths, paths with spaces, or too little meaningful plan content. The wrapper does not validate `$ARGUMENTS`; malformed input is intentionally passed through to the skill/setup script for validation.
- validation_or_tests: Static tests assert command-to-skill delegation at `tests/test-skill-invocation.sh:95-105`, thin-wrapper behavior at `tests/test-skill-invocation.sh:125-143`, delegation pattern with skill execution tools at `tests/test-skill-invocation.sh:205-222`, and command structure at `tests/test-skill-structure.sh:164-178`. I did not run the full skill test suite because the assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-050 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template loading/rendering subsystem used by RLCR hooks. It sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12`, then validates path resolution, template loading, single-pass placeholder substitution, fallback behavior, template directory validation, shell metacharacter literal rendering, placeholder-injection prevention, Unicode handling, empty values, and variable-name edge cases.
- algorithmic_behavior: The test harness initializes strict Bash mode at `tests/test-template-loader.sh:8`, computes `SCRIPT_DIR` and `PROJECT_ROOT`, sources the implementation, and tracks pass/fail counters at `tests/test-template-loader.sh:20-33`. It verifies `get_template_dir` maps `hooks/lib` to `prompt-template` at `tests/test-template-loader.sh:41-52`. It verifies `load_template` returns real template contents for `block/git-push.md` and empty output for missing files at `tests/test-template-loader.sh:54-78`. It verifies `render_template` replaces `{{VAR}}` placeholders for single, multiple, and multiline templates at `tests/test-template-loader.sh:80-129`, while unused variables are ignored and missing variables remain literal at `tests/test-template-loader.sh:166-194`. Integration tests verify `load_and_render` with `block/wrong-round-number.md` at `tests/test-template-loader.sh:147-164` and `load_and_render_safe` fallback/template selection at `tests/test-template-loader.sh:196-222`. Directory validation is specified at `tests/test-template-loader.sh:224-244`.
- inputs_outputs_state: Inputs are in-memory template strings, relative template paths under `prompt-template`, and `VAR=value` assignment arguments. Outputs are rendered strings and process exit status. The test state is local to `TESTS_PASSED` and `TESTS_FAILED`; each `pass()` or `fail()` call mutates those counters at `tests/test-template-loader.sh:23-34`. Final state transition occurs at `tests/test-template-loader.sh:641-659`: print summary, exit `0` when no failures, exit `1` otherwise.
- gates_or_invariants: Key invariants are inherited from `hooks/lib/template-loader.sh`: template variables use `{{VARIABLE_NAME}}` syntax, missing variables remain as placeholders, and substitution is single-pass to prevent injected `{{...}}` in values from being rescanned (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-57`). The implementation builds `TMPL_VAR_` environment variables from assignments at `hooks/lib/template-loader.sh:62-69`, uses an AWK scan over each line to locate `{{` and `}}` at `hooks/lib/template-loader.sh:71-129`, replaces known names from the environment at `hooks/lib/template-loader.sh:115-122`, and echoes the rendered result at `hooks/lib/template-loader.sh:131`. The test asserts literal handling for shell metacharacters such as ampersand, backslash, dollar signs, backticks, pipes, semicolons, globs, parentheses, redirection, quotes, hash, tilde, combined command-like strings, regex-like values, JSON-like values, and multiline metacharacter values at `tests/test-template-loader.sh:246-487`. Placeholder injection prevention is specified at `tests/test-template-loader.sh:488-543`.
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`. The loader is sourced by `hooks/lib/loop-common.sh:46-55`, and loop-common is used by validators such as `hooks/loop-read-validator.sh:14-16`. The loader’s public functions are widely called by hook validators and stop hooks; examples include wrong-file-location rendering in `hooks/loop-read-validator.sh:100-109`, todo/prompt/state block messages in `hooks/lib/loop-common.sh:190-237`, and many stop-hook prompt sections found by repository search.
- edge_cases_or_failure_modes: The spec covers missing templates, invalid directories, empty substitution values, unreplaced placeholders, multiline values, literal shell metacharacters, placeholder-looking content inside injected values, non-ASCII input, underscore-prefixed variable names, and numeric variable names. A notable implementation-sensitive edge case is calling `render_template` with no `VAR=value` arguments: `env "${env_vars[@]}" awk ...` at `hooks/lib/template-loader.sh:74` can interact poorly with `set -u` when the local array is empty, as observed during `load_and_render_safe` with an existing template and no substitutions. Another environment edge case is Bash here-string use at `hooks/lib/template-loader.sh:129`; in this read-only sandbox it attempted to create a temp file and failed with `Operation not permitted`, causing render tests to fail for sandbox reasons.
- validation_or_tests: I ran `bash tests/test-template-loader.sh` in the assigned read-only branch export. Observed result: 5 passed, 38 failed, exit code `1`. The failures consistently reported `hooks/lib/template-loader.sh: line 129: cannot create temp file for here document: Operation not permitted` for render paths, plus one `env_vars[@]: unbound variable` case for rendering an existing template with no variable assignments. Because the repository export is read-only and the shell here-string needs a writable temp area, this run is useful as environment evidence but not a clean semantic validation of expected rendering behavior.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-080 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block prompt template for the RLCR read-access validator when an agent tries to read a round prompt or summary file from outside the active `.humanize/rlcr/...` loop directory. It communicates the active loop directory and current-round prompt/summary paths.
- algorithmic_behavior: The template is rendered with `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Its static title is `# Wrong File Location` at `prompt-template/block/wrong-file-location.md:1`. It reports the attempted path and active loop root at `prompt-template/block/wrong-file-location.md:3`, lists current prompt and summary paths at `prompt-template/block/wrong-file-location.md:5-7`, and ends with an instruction using the attempted file path at `prompt-template/block/wrong-file-location.md:9`. In the actual validator, this template is selected when the requested path is a round prompt/summary file but `is_in_humanize_loop_dir` returns false: `hooks/loop-read-validator.sh:54-55` computes the file/location condition, `hooks/loop-read-validator.sh:100-109` renders this template through `load_and_render_safe`, writes it to stderr, and exits `2` to block the read.
- inputs_outputs_state: Inputs are the attempted `FILE_PATH` from hook JSON (`hooks/loop-read-validator.sh:22-30`), the active loop directory discovered from `LOOP_BASE_DIR` (`hooks/loop-read-validator.sh:61-64`), and `CURRENT_ROUND` parsed from `state.md` or `finalize-state.md` (`hooks/loop-read-validator.sh:69-77`). Output is a rendered human-readable block message on stderr. State transition is a hook decision transition: allowed read exits `0`; wrong-location read exits `2` at `hooks/loop-read-validator.sh:105-109`.
- gates_or_invariants: The template only applies after the validator confirms the tool is `Read` at `hooks/loop-read-validator.sh:22-27`, the path targets a round summary or prompt at `hooks/loop-read-validator.sh:47-52`, and an active loop exists at `hooks/loop-read-validator.sh:61-67`. It is bypassed for non-round files, for no active loop, and for files already inside the humanize loop directory. If the template is missing or empty, the validator uses an inline fallback defined at `hooks/loop-read-validator.sh:102-104`, preserving the block behavior even when prompt templates are unavailable.
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh` through `load_and_render_safe` at `hooks/loop-read-validator.sh:105`. Rendering depends on `TEMPLATE_DIR`, initialized and validated by `hooks/lib/loop-common.sh:46-55`, and on `hooks/lib/template-loader.sh` for fallback rendering and placeholder substitution. The template path is also part of the broader template reference surface found by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` usage patterns.
- edge_cases_or_failure_modes: If the attempted path is outside `.humanize/rlcr` but has a valid current-round filename, the wrong-location gate fires before wrong-round or wrong-directory gates. If no active loop is found, the validator exits `0` rather than blocking. If the path is a todos file, that earlier gate can block at `hooks/loop-read-validator.sh:32-44`. The final line says `cat {{FILE_PATH}}` even though the hook is blocking the current read; this appears to be intended as a concrete path hint, but it can be confusing because the earlier text says loop files are in `{{ACTIVE_LOOP_DIR}}/`.
- validation_or_tests: Covered indirectly by the template loader and reference tests. `tests/test-template-loader.sh` does not render this exact file, but it exercises the same `load_and_render_safe` and placeholder mechanics. `hooks/loop-read-validator.sh:100-109` is the direct runtime validation path. I did not run a full hook simulation because creating `.humanize/rlcr` state files would modify the read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item sections present under Item Evidence, matching assigned_item_count
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`