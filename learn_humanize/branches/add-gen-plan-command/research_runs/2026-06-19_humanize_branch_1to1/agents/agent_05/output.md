# agent_05 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Template corpus for the RLCR control loop’s user-facing and agent-facing state transitions. It does not execute logic itself; it defines the contractual messages rendered by `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, the PreToolUse validators, and `hooks/loop-codex-stop-hook.sh`. The directory has three required subdirectories: `block/`, `claude/`, and `codex/`; the loader validates that exact structure in `hooks/lib/template-loader.sh:205-221`.
- algorithmic_behavior: `prompt-template/block/` contains denial and repair messages used when validators reject unsafe or out-of-protocol actions. Examples include state protection (`block/state-file-modification.md`, `block/finalize-state-file-modification.md`), wrong round/path protection (`block/wrong-round-number.md`, `block/wrong-round-file.md`, `block/wrong-directory-path.md`, `block/wrong-summary-location.md`), loop artifact protection (`block/prompt-file-write.md`, `block/plan-backup-protected.md`, `block/todos-file-access.md`), git policy gates (`block/git-add-humanize.md`, `block/git-push.md`, `block/git-not-clean.md`, `block/unpushed-commits.md`), and goal-tracker gates (`block/goal-tracker-bash-write.md`, `block/goal-tracker-modification.md`, `block/goal-tracker-not-initialized.md`). `prompt-template/claude/` defines the next-round and finalize prompts that drive Claude-side loop work: `next-round-prompt.md` tells the worker to re-read the original plan and Codex review, create todos for all discovered issues, and treat `goal-tracker.md` as read-only after Round 0 (`prompt-template/claude/next-round-prompt.md:1-31`); `next-round-footer.md` requires commit plus summary output (`prompt-template/claude/next-round-footer.md:4-9`); `finalize-phase-prompt.md` defines the post-Codex simplification phase and summary requirements (`prompt-template/claude/finalize-phase-prompt.md:1-50`). `prompt-template/codex/` defines review-side gates: `regular-review.md` requires skeptical verification against the plan and forbids `COMPLETE` unless all tasks are done with no deferrals (`prompt-template/codex/regular-review.md:20-57`); `full-alignment-review.md` adds periodic acceptance-criteria audit, forgotten-item detection, deferred-item audit, stagnation detection, and `STOP`/`COMPLETE` terminal markers (`prompt-template/codex/full-alignment-review.md:19-93`); `goal-tracker-update-section.md` delegates post-Round-0 goal-tracker mutation authority to Codex only (`prompt-template/codex/goal-tracker-update-section.md:1-17`).
- inputs_outputs_state: Inputs are Markdown templates plus `{{VARIABLE_NAME}}` substitutions. The loader documents uppercase placeholder syntax, single-pass substitution, and preserving missing placeholders in `hooks/lib/template-loader.sh:7-13`. Rendering is done by `render_template`, which prefixes substitution values with `TMPL_VAR_`, scans lines once with `awk`, and deliberately does not re-scan replaced values (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`). Outputs are Markdown instructions/errors sent to stderr by validators or embedded into prompt/review files by the stop hook. Missing template state is handled by `load_and_render_safe`, which falls back to inline messages (`hooks/lib/template-loader.sh:167-203`), so template absence degrades message quality rather than immediately disabling validation.
- gates_or_invariants: The directory must contain `block/`, `codex/`, and `claude/` (`hooks/lib/template-loader.sh:216-218`). Template rendering is single-pass to prevent placeholder injection from review content or other untrusted values (`hooks/lib/template-loader.sh:54-57`). Validator messages should use `load_and_render_safe` for critical blocks; `tests/test-template-references.sh` scans validator scripts and fails unsafe/missing references (`tests/test-template-references.sh:51-111`, `tests/test-template-references.sh:170-200`). The semantic invariants encoded in templates include: prompt files are instructions from Codex and cannot be modified (`prompt-template/block/prompt-file-write.md:3-10`); `state.md` is loop-managed (`prompt-template/block/state-file-modification.md:1-9`); `.humanize/` must not be committed (`prompt-template/block/git-add-humanize.md:1-24`); todos should use native TodoWrite except the explicit allowlist cases enforced by hooks (`prompt-template/block/todos-file-access.md:1-8`); goal-tracker immutable sections are only initialized in Round 0 (`prompt-template/block/goal-tracker-not-initialized.md:1-18`).
- dependencies_and_callers: `hooks/lib/template-loader.sh` resolves this directory from the plugin root (`hooks/lib/template-loader.sh:24-31`). `hooks/lib/loop-common.sh` loads the template loader and initializes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:46-55`), then wraps common block templates in functions such as `todos_blocked_message`, `prompt_write_blocked_message`, `state_file_blocked_message`, `summary_bash_blocked_message`, `goal_tracker_bash_blocked_message`, and `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:190-248`, `hooks/lib/loop-common.sh:601-613`). `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh` call those wrappers or direct `load_and_render_safe` paths to emit blocking messages (`hooks/loop-read-validator.sh:100-145`, `hooks/loop-write-validator.sh:105-216`, `hooks/loop-edit-validator.sh:86-147`, `hooks/loop-bash-validator.sh:63-352`). `hooks/loop-codex-stop-hook.sh` references the Codex and Claude prompt templates for review/follow-up flow, as shown by repository reference hits to `codex/goal-tracker-update-section.md`, `claude/goal-tracker-update-request.md`, and next-round/finalize templates.
- edge_cases_or_failure_modes: Missing templates are not silent: `load_template` warns and returns empty (`hooks/lib/template-loader.sh:33-48`), while `load_and_render_safe` substitutes a fallback (`hooks/lib/template-loader.sh:167-203`). Missing variables remain as placeholders (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:119-122`), which can surface incomplete caller substitutions to users. Content with nested `{{...}}` is intentionally not recursively expanded, avoiding prompt corruption or variable injection. Empty or whitespace-only templates are detected as warnings by comprehensive tests (`tests/test-templates-comprehensive.sh:90-107`). A stale or missing template can still cause lower-quality guidance if the fallback is too terse, so `tests/test-template-references.sh` treats missing referenced templates as critical (`tests/test-template-references.sh:213-229`).
- validation_or_tests: `tests/test-template-loader.sh` covers directory resolution, existing/missing template loading, single/multiple/multiline substitution, unreplaced variables, safe fallback behavior, and directory validation (`tests/test-template-loader.sh:41-230`). `tests/test-template-references.sh` scans shell scripts for template references, verifies referenced files exist, verifies common template files including `block/goal-tracker-bash-write.md`, and enforces safe rendering in critical validators (`tests/test-template-references.sh:51-167`, `tests/test-template-references.sh:170-200`). `tests/test-templates-comprehensive.sh` loads every template under `prompt-template/`, checks subdirectory structure, validates placeholder syntax, and renders real templates including `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:61-120`, `tests/test-templates-comprehensive.sh:500-545`).
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-035 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR allowlist exception that permits a narrow set of historical/generated round files while the general validators block `round-*-todos.md` and wrong-round summaries. It directly tests `is_allowlisted_file()` plus the Read/Write/Edit/Bash validators that call it.
- algorithmic_behavior: The test builds an isolated git repo in a temp directory, commits an initial file, creates `.humanize/rlcr/2024-01-01_12-00-00`, and writes a `state.md` with `current_round: 5`, `max_iterations: 42`, a plan file, `plan_tracked: false`, and `start_branch` (`tests/test-allowlist-validators.sh:29-62`). It then sets `ACTIVE_LOOP_DIR` or `CLAUDE_PROJECT_DIR` and feeds JSON hook payloads to validators. The expected allowlist is hard-coded in production as exactly `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop directory (`hooks/lib/loop-common.sh:167-188`). Tests 1-7 exercise the helper directly for allowed names, disallowed names, and wrong directories (`tests/test-allowlist-validators.sh:64-125`). Tests 8-11 verify Write behavior: allowed files exit 0, blocked todos/wrong summaries exit 2 with relevant error text (`tests/test-allowlist-validators.sh:127-183`). Tests 12-14 do the same for Edit (`tests/test-allowlist-validators.sh:185-225`). Tests 15-18 do the same for Read (`tests/test-allowlist-validators.sh:228-282`). Tests 19-25 cover Bash command detection and path restriction (`tests/test-allowlist-validators.sh:284-381`).
- inputs_outputs_state: Inputs are synthetic hook JSON such as `{"tool_name":"Write","tool_input":{"file_path":"$LOOP_DIR/round-1-todos.md"}}` and Bash command strings like `echo test > $LOOP_DIR/round-1-todos.md`. The validators consume `CLAUDE_PROJECT_DIR` to locate `.humanize/rlcr`, derive the active loop via `find_active_loop`, parse `state.md`, and return process exit status plus stderr. Expected outputs are exit `0` for allowed access and exit `2` for blocked hook usage. Test state is local to `TEST_DIR`, cleaned with a trap (`tests/test-allowlist-validators.sh:29-31`), and pass/fail counts are accumulated before the script exits with `TESTS_FAILED` (`tests/test-allowlist-validators.sh:383-391`).
- gates_or_invariants: The central invariant is full path equality against the active loop directory; a matching basename alone is insufficient (`hooks/lib/loop-common.sh:181-187`). Read/Write/Edit validators only bypass todo/wrong-round blocks if `is_allowlisted_file "$FILE_PATH" "$ACTIVE_LOOP_DIR"` succeeds (`hooks/loop-read-validator.sh:36-43`, `hooks/loop-read-validator.sh:116-129`, `hooks/loop-write-validator.sh:37-45`, `hooks/loop-write-validator.sh:181-198`, `hooks/loop-edit-validator.sh:36-44`, `hooks/loop-edit-validator.sh:124-149`). Bash validator adds a stronger command-string gate: `round-*-todos.md` writes are blocked unless the command contains the lowercased, regex-escaped active loop path followed by `round-[12]-todos.md` (`hooks/loop-bash-validator.sh:344-352`). The test encodes security cases for wrong directory, relative path, old loop directory, and same basename under a different root (`tests/test-allowlist-validators.sh:314-381`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` directly (`tests/test-allowlist-validators.sh:15-17`) and invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` through hook JSON. Those validators are wired as Claude Code PreToolUse hooks in `hooks/hooks.json:14-50`. Production helper dependencies include `jq` in validators for JSON extraction (`hooks/loop-read-validator.sh:22-30`, `hooks/loop-write-validator.sh:23-31`, `hooks/loop-edit-validator.sh:22-30`, `hooks/loop-bash-validator.sh:22-30`), git for test setup (`tests/test-allowlist-validators.sh:36-43`), and the template/block messages emitted through `loop-common.sh`.
- edge_cases_or_failure_modes: The test intentionally covers only a small allowed set; `round-3-todos.md`, `round-2-summary.md`, `round-4-todos.md`, and `round-3-summary.md` must remain blocked even though their filenames match round-file patterns (`tests/test-allowlist-validators.sh:102-116`, `tests/test-allowlist-validators.sh:159-183`, `tests/test-allowlist-validators.sh:215-225`, `tests/test-allowlist-validators.sh:258-282`). The direct helper is case-sensitive and path-string-based; it does not canonicalize paths, so symlinks or alternate textual paths are outside its allow condition. Bash validator matching is command-text based and lowercases the command; it protects against same-basename different-root and old-loop bypasses, but its correctness depends on `command_modifies_file` recognizing the modification pattern. The test does not cover quoted active loop paths with spaces, although setup code rejects plan paths with spaces elsewhere and loop timestamps do not contain spaces.
- validation_or_tests: This file is itself the validation surface. It exits nonzero on any failed assertion (`tests/test-allowlist-validators.sh:383-391`). It validates production helper behavior plus all four hook validators across 25 explicit cases. Related broader coverage exists in `tests/test-bash-validator-patterns.sh`, which exercises `command_modifies_file` for redirection, tee, in-place edit, mv/cp/rm, `dd`, `truncate`, `exec`, and non-modifying reads, including summary-file patterns; that complements this allowlist test’s Bash path-restriction checks.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-065 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Blocking-message template for the Round 0 goal-tracker Bash write gate. Its job is to stop shell redirection/edit bypasses and force the agent to use Write/Edit tools so the normal goal-tracker validators can enforce path and round rules.
- algorithmic_behavior: The template emits a Markdown denial headed “Bash Write Blocked: Use Write or Edit Tool” (`prompt-template/block/goal-tracker-bash-write.md:1`). It explicitly says not to use Bash to modify `goal-tracker.md` (`prompt-template/block/goal-tracker-bash-write.md:3`), renders the correct path via `{{CORRECT_PATH}}` (`prompt-template/block/goal-tracker-bash-write.md:5`), and explains that shell tools such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:7-8`). The production wrapper is `goal_tracker_bash_blocked_message`, which calls `load_and_render_safe` with `CORRECT_PATH` (`hooks/lib/loop-common.sh:239-248`). The Bash validator uses this template only when `command_modifies_file "$COMMAND_LOWER" "goal-tracker\.md"` is true and `CURRENT_ROUND` is `0` (`hooks/loop-bash-validator.sh:302-317`).
- inputs_outputs_state: Input is `CORRECT_PATH`, normally `$ACTIVE_LOOP_DIR/goal-tracker.md` from `hooks/loop-bash-validator.sh:309-311`. Output is rendered Markdown to stderr, and the validator exits with code `2` after writing it (`hooks/loop-bash-validator.sh:308-317`). It does not mutate state; it preserves the intended state transition by preventing Bash from writing the initial Goal Tracker and routing the user back to tool-mediated modification.
- gates_or_invariants: Round 0 is special: direct Write/Edit can initialize `goal-tracker.md`, but Bash writes are still blocked because they bypass validation hooks. After Round 0, the sibling template `block/goal-tracker-modification.md` is used instead, and direct goal-tracker modification is denied in favor of a summary-file update request (`hooks/loop-bash-validator.sh:309-315`, `hooks/lib/loop-common.sh:601-613`). The invariant encoded here is tool-channel integrity: initialization must occur through Write/Edit, not through shell commands.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for `{{CORRECT_PATH}}` rendering and fallback behavior. Called by `hooks/lib/loop-common.sh:241-248`; reachable from `hooks/loop-bash-validator.sh:308-317`. Its existence is explicitly checked by `tests/test-template-references.sh` as one of the common templates (`tests/test-template-references.sh:152-158`). It is also referenced in the assigned directory-level template corpus and the Bash validator’s goal-tracker path.
- edge_cases_or_failure_modes: If this file is missing, `goal_tracker_bash_blocked_message` falls back to an inline message with the same `CORRECT_PATH` placeholder (`hooks/lib/loop-common.sh:241-248`), so enforcement still works but the explanatory prompt may be degraded. If `command_modifies_file` fails to identify a shell mutation pattern, this template is not emitted and the Bash command may escape the specific gate; that detection is tested separately in `tests/test-bash-validator-patterns.sh`. If `CURRENT_ROUND` is parsed incorrectly as `0`, the Round 0 Bash-write message may be shown when the stricter post-Round-0 update-request message should be used.
- validation_or_tests: `tests/test-template-references.sh` checks that `block/goal-tracker-bash-write.md` exists (`tests/test-template-references.sh:152-158`). `tests/test-bash-validator-patterns.sh` validates many `command_modifies_file` cases for `goal-tracker.md`, including redirection, tee, `sed -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, and `exec`, while ensuring read-only commands are not false positives. The assigned `tests/test-allowlist-validators.sh` does not target this exact template, but it exercises the same Bash validator infrastructure and exit-code contract.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-095 `file` `skills/start-rlcr-loop/SKILL.md`
- cursor: `[_]`
- core_role: Skill-level workflow entrypoint for starting an RLCR loop. It is a thin operational instruction file that grants exactly the setup script tool and defines the human/Claude/Codex loop contract: implement plan, summarize work, receive Codex review, continue on findings, complete only on Codex `COMPLETE`.
- algorithmic_behavior: The frontmatter names the skill, describes when it should be used, sets model `opus`, and restricts allowed tools to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` (`skills/start-rlcr-loop/SKILL.md:1-7`). The body instructs execution of `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` (`skills/start-rlcr-loop/SKILL.md:11-15`). It defines the loop sequence: work on the implementation plan, write a summary, Codex reviews on exit, feedback causes another round, and `COMPLETE` ends the loop (`skills/start-rlcr-loop/SKILL.md:17-24`). It also introduces the Goal Tracker state model: immutable Ultimate Goal and Acceptance Criteria, mutable Active/Completed/Deferred/Evolution sections, explicit deferrals, and full alignment checks after every four work rounds (`skills/start-rlcr-loop/SKILL.md:25-38`). The command wrapper delegates to this skill rather than duplicating setup logic (`commands/start-rlcr-loop.md:1-19`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` passed to `scripts/setup-rlcr-loop.sh`; supported setup arguments include a relative plan path, `--plan-file`, `--track-plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--push-every-round` (`scripts/setup-rlcr-loop.sh:43-100`, `scripts/setup-rlcr-loop.sh:104-180`). The setup script validates git repo state, plan path safety, plan tracking/cleanliness, plan content, Codex availability, branch/model YAML safety, then creates `.humanize/rlcr/<timestamp>/` with `state.md`, `plan.md` backup, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:207-498`, `scripts/setup-rlcr-loop.sh:500-653`). Initial `state.md` records `current_round: 0`, `max_iterations`, Codex model/effort/timeout, push policy, plan path, plan tracking mode, start branch, and start timestamp (`scripts/setup-rlcr-loop.sh:481-498`). Outputs to the user include activation metadata and the initial prompt, followed by completion requirements to commit and write `round-0-summary.md` (`scripts/setup-rlcr-loop.sh:667-720`).
- gates_or_invariants: The skill restricts executable access to the setup script only (`skills/start-rlcr-loop/SKILL.md:5-7`). The loop requires summaries before exit (`skills/start-rlcr-loop/SKILL.md:46-48`), forbids cheating by editing state or cancel commands (`skills/start-rlcr-loop/SKILL.md:49`), and makes Codex `COMPLETE` the normal completion signal (`skills/start-rlcr-loop/SKILL.md:52-56`). Setup gates enforce one plan file, relative path only, no spaces, no shell metacharacters, no symlink, inside project, not inside a submodule, proper tracked/gitignored policy, at least 5 lines and at least 3 content lines, Codex CLI present, and YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:186-205`, `scripts/setup-rlcr-loop.sh:227-305`, `scripts/setup-rlcr-loop.sh:307-423`, `scripts/setup-rlcr-loop.sh:429-461`). Round 0 Goal Tracker initialization is explicitly required before implementation (`scripts/setup-rlcr-loop.sh:602-615`), and later direct goal-tracker modification is controlled by validators and Codex update sections.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md`, whose allowed tool is `Skill(humanize:start-rlcr-loop:*)` (`commands/start-rlcr-loop.md:1-17`). The skill depends on `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`, which in turn sources `scripts/portable-timeout.sh` (`scripts/setup-rlcr-loop.sh:26-29`) and uses git, `codex`, `grep`, `sed`, `wc`, `date`, and filesystem writes. Downstream runtime depends on hooks declared in `hooks/hooks.json`, especially PreToolUse validators and the Stop hook (`hooks/hooks.json:14-63`). The prompts/templates in `prompt-template/` supply later review and block messaging during the loop.
- edge_cases_or_failure_modes: Missing or non-executable setup script prevents the skill from starting the loop; this is tested. Invalid arguments fail early: missing plan, both positional and `--plan-file`, multiple plan files, invalid numeric options, unknown options, absolute/space/metacharacter paths, missing/unreadable/too-small plan, tracked plan without `--track-plan-file`, untracked plan with `--track-plan-file`, missing Codex CLI, unsafe branch name, or unsafe Codex model/effort all abort before loop state is created. The setup script writes `.humanize/rlcr` state; if hooks are not installed/enabled, later loop invariants rely on user compliance. Goal extraction and acceptance-criteria extraction are heuristic (`scripts/setup-rlcr-loop.sh:527-557`), so Round 0 explicitly asks Claude to refine placeholders and populate tasks (`scripts/setup-rlcr-loop.sh:605-615`).
- validation_or_tests: `tests/test-skill-invocation.sh` verifies the skill is discoverable, the command delegates via Skill tool, the command is a thin wrapper, the skill has allowed tools with `setup-rlcr-loop.sh`, the setup script exists and is executable, `bash -n` passes, `--help` works, auto-invocation keywords exist, and instruction content includes setup script, Goal Tracker, summary, and Codex (`tests/test-skill-invocation.sh:58-145`, `tests/test-skill-invocation.sh:173-270`, `tests/test-skill-invocation.sh:282-342`). Related structure coverage appears in `tests/test-skill-structure.sh`. Plan input validation is covered by `tests/test-plan-file-validation.sh`, and hook protections for generated loop state are covered by validator tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 4 assigned item sections are present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`