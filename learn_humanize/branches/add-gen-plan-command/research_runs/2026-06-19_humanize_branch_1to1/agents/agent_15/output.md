# agent_15 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-015 `directory` `skills/gen-plan`
- cursor: `[_]`
- core_role: Defines the `humanize:gen-plan` skill, which is the command’s core algorithm for converting a draft document into a structured implementation plan. The directory recursively contains one file, `skills/gen-plan/SKILL.md`, so the directory-level responsibility is concentrated in that skill spec.
- algorithmic_behavior: The workflow is a staged pipeline: IO validation, repository relevance check, draft analysis, user issue resolution, plan generation, and final write/report. `skills/gen-plan/SKILL.md:19-26` lists the phases. Phase 1 delegates validation to `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` and branches by explicit exit code meanings at `skills/gen-plan/SKILL.md:29-45`. Phase 2 reads the draft and invokes `draft-relevance-checker` with a required `RELEVANT:` or `NOT_RELEVANT:` verdict at `skills/gen-plan/SKILL.md:48-72`. Phases 3-4 analyze clarity, consistency, completeness, functionality, and ask the user to resolve issues before generation at `skills/gen-plan/SKILL.md:75-120`. Phase 5 specifies the exact generated plan shape at `skills/gen-plan/SKILL.md:123-197`.
- inputs_outputs_state: Inputs are command arguments forwarded from `commands/gen-plan.md:12-17`, specifically `--input <draft.md> --output <plan.md>`, plus repository context for relevance and exploration. Output is a newly written plan file with Goal Description, Acceptance Criteria, Path Boundaries, Feasibility Hints, and Dependencies/Sequence per `skills/gen-plan/SKILL.md:127-197`. State is mostly transient: validated paths, draft content, relevance verdict, discovered issues, user clarifications, and final AC count. The skill forbids overwriting existing output through the validator’s output-exists gate, referenced in `skills/gen-plan/SKILL.md:40-43` and implemented in `scripts/validate-gen-plan-io.sh:102-115`.
- gates_or_invariants: Required gates include input file exists, input nonempty, output directory exists, output file absent, output directory writable, and valid argument shape. The script implements these at `scripts/validate-gen-plan-io.sh:57-123`. Relevance is a hard semantic gate: `NOT_RELEVANT` stops plan generation at `skills/gen-plan/SKILL.md:66-72`. Generated plans must be English-only, avoid time estimates and line numbers, use AC-X or AC-X.Y criteria, include positive and negative tests for every AC, use affirmative path boundaries, and respect deterministic draft designs at `skills/gen-plan/SKILL.md:199-219`.
- dependencies_and_callers: The slash command wrapper is `commands/gen-plan.md`, whose allowed tool is only `Skill(humanize:gen-plan:*)` at `commands/gen-plan.md:1-5`. The skill depends on `scripts/validate-gen-plan-io.sh`, `agents/draft-relevance-checker.md`, repository exploration tools, `AskUserQuestion`, and `Write`. The relevance agent is defined with Read/Glob/Grep tools and a lenient semantic policy at `agents/draft-relevance-checker.md:1-37`.
- edge_cases_or_failure_modes: Missing or malformed args exit 6; nonexistent input exits 1; empty input exits 2; missing output directory exits 3; existing output path or directory exits 4; non-writable output directory exits 5. Those codes are documented in `scripts/validate-gen-plan-io.sh:4-11` and consumed by the skill at `skills/gen-plan/SKILL.md:37-45`. Relevance false positives are intentionally preferred over false negatives because the checker is told to be lenient and mark relevant when in doubt at `agents/draft-relevance-checker.md:31-37`. A deterministic draft may collapse upper/lower path boundaries and constrain “Allowed Choices,” explicitly handled at `skills/gen-plan/SKILL.md:171-173` and `skills/gen-plan/SKILL.md:219`.
- validation_or_tests: The core IO validator is tested indirectly by `tests/test-skill-structure.sh` references found in the repository cross-reference scan, including missing values, unknown options, input not found, empty input, output dir missing, output exists, output-is-directory, valid paths, and help behavior. The executable implementation of those cases is `scripts/validate-gen-plan-io.sh:28-130`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-045 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file immutability and hook safety. It verifies that active loop state, plan backups, summary/goal-tracker files, and tool-specific validators maintain the algorithm’s control boundary while an RLCR loop is running.
- algorithmic_behavior: The script builds isolated temporary git repos and loop directories under `.humanize/rlcr/<timestamp>`, writes `state.md`, seeds a gitignored project plan, copies it to `plan.md` backup, then feeds JSON hook inputs into hook scripts. Setup lives at `tests/test-plan-file-hooks.sh:30-86`. It tests `loop-plan-file-validator.sh` as a UserPromptSubmit gate for schema fields, branch consistency, quoted YAML parsing, and plan tracking status at `tests/test-plan-file-hooks.sh:91-185` and `tests/test-plan-file-hooks.sh:341-459`. It tests `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh` as PreToolUse gates against protected `plan.md` backup writes/edits/shell modifications at `tests/test-plan-file-hooks.sh:187-239`.
- inputs_outputs_state: Inputs are synthetic hook JSON objects, git state, `CLAUDE_PROJECT_DIR`, loop state frontmatter, project plan files, backup files, round summaries, and goal trackers. Outputs are process exit codes and hook response text/JSON. Test counters mutate through `pass`, `fail`, and `skip` helpers at `tests/test-plan-file-hooks.sh:17-28`; the final process exits with `TESTS_FAILED` at `tests/test-plan-file-hooks.sh:1056-1065`. Active loop state includes `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, and `start_branch` written at `tests/test-plan-file-hooks.sh:77-85`.
- gates_or_invariants: Required state schema includes `plan_tracked` and `start_branch`; missing either must block with a schema-related message, specified at `tests/test-plan-file-hooks.sh:121-159` and implemented in `hooks/loop-plan-file-validator.sh:52-86`. The current git branch must match `start_branch`, tested at `tests/test-plan-file-hooks.sh:164-185` and implemented in `hooks/loop-plan-file-validator.sh:88-112`. Plan backup writes and edits must exit 2, tested at `tests/test-plan-file-hooks.sh:194-221`, with implementation gates in `hooks/loop-write-validator.sh:128-139` and `hooks/loop-edit-validator.sh:96-108`. Bash mutations to `.humanize/rlcr/.../plan.md` must exit 2, including direct paths, `rm`, command substitution, globs, braces, pipes, and backticks at `tests/test-plan-file-hooks.sh:228-335`; implementation uses `command_modifies_file` in `hooks/loop-bash-validator.sh:289-300`.
- dependencies_and_callers: The tested hook registration surface is `hooks/hooks.json`: UserPromptSubmit calls `loop-plan-file-validator.sh`, PreToolUse calls Write/Edit/Read/Bash validators, and Stop calls `loop-codex-stop-hook.sh` at `hooks/hooks.json:4-63`. Shared parsing and message behavior comes from `hooks/lib/loop-common.sh`, especially `parse_state_file` quote stripping at `hooks/lib/loop-common.sh:119-138`, active-loop discovery at `hooks/lib/loop-common.sh:58-81`, summary bash rendering at `hooks/lib/loop-common.sh:228-237`, and shell modification detection at `hooks/lib/loop-common.sh:571-599`.
- edge_cases_or_failure_modes: The test covers YAML-quoted `plan_file` and `start_branch`, quoted branch mismatch, plan paths with hyphens, deleted plan files, missing backups, tracked plan uncommitted modifications, tracked plan committed modifications where git status is clean, and old schema state files at `tests/test-plan-file-hooks.sh:337-731`. Stop-hook integrity is implemented by checking backup presence, original plan existence, tracked-file git status, and content diff against backup at `hooks/loop-codex-stop-hook.sh:182-250`. Legacy `.humanize-loop.local` paths are negative tests and must be allowed because they are no longer loop directories, specified at `tests/test-plan-file-hooks.sh:1012-1054`.
- validation_or_tests: This file itself is validation. It expects valid hooks to pass with exit 0 and no output for clean state at `tests/test-plan-file-hooks.sh:91-119`; blocking PreToolUse validators to exit 2 at `tests/test-plan-file-hooks.sh:194-239`; and Stop hook blocks to return JSON with a `decision` field and relevant reason text at `tests/test-plan-file-hooks.sh:469-731`. It also specifies section-specific placeholder detection for `goal-tracker.md`: only Ultimate Goal, only Acceptance Criteria, only Active Tasks, or all three placeholders should be reported as applicable at `tests/test-plan-file-hooks.sh:733-1010`. The implementation extracts and checks those sections in `hooks/loop-codex-stop-hook.sh:556-630`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-075 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template used when Bash attempts to modify round summary files. It is a user-facing gate contract that redirects the actor from shell writes to validated Write/Edit tool paths.
- algorithmic_behavior: The template states the block reason, names the correct path using `{{CORRECT_PATH}}`, and explains that Bash commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks. Its content is `prompt-template/block/summary-bash-write.md:1-8`. It is loaded by `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:228-237`, and that message is emitted by the Bash validator when `command_modifies_file` detects a `round-N-summary.md` modification at `hooks/loop-bash-validator.sh:329-338`.
- inputs_outputs_state: Input is the rendered variable `CORRECT_PATH`, passed from `loop-bash-validator.sh` as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` at `hooks/loop-bash-validator.sh:334-336`. Output is blocking text sent to stderr before the validator exits 2. State read upstream includes active loop dir and `current_round`, parsed by `parse_state_file` in `hooks/loop-bash-validator.sh:45-53`.
- gates_or_invariants: The invariant is that summary files must be modified through Write or Edit, not shell commands. That preserves round-number and directory validation performed by Write/Edit validators. The template itself names the required tool path at `prompt-template/block/summary-bash-write.md:3-8`; the gate is enforced when Bash commands match redirect, tee, sed-in-place, awk-in-place, perl-in-place, move/copy, remove, dd, truncate, printf redirect, or exec redirect patterns in `hooks/lib/loop-common.sh:578-591`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` through `load_and_render_safe`, initialized by `hooks/lib/loop-common.sh:46-56`. Direct caller is `summary_bash_blocked_message` at `hooks/lib/loop-common.sh:228-237`; runtime caller is `loop-bash-validator.sh` at `hooks/loop-bash-validator.sh:334-337`. Template reference coverage is also listed in `tests/test-template-references.sh` per repository cross-reference.
- edge_cases_or_failure_modes: If the template is missing or fails to render, `summary_bash_blocked_message` has an inline fallback at `hooks/lib/loop-common.sh:232-236`, so the block still works. The message is only as accurate as `CORRECT_PATH`; active loop resolution uses newest `.humanize/rlcr` directory with state/finalize-state at `hooks/lib/loop-common.sh:58-81`. Bash detection is regex-based and broad but not a full shell parser, so unusual shell syntax outside `command_modifies_file` patterns may be a residual bypass risk.
- validation_or_tests: The hook behavior is covered by Bash validator tests in the broader test suite; this assigned test file specifically verifies Bash validator plan-backup protections, while `tests/test-template-references.sh` includes `block/summary-bash-write.md` in template-reference checks. The exact summary Bash gate is implemented in `hooks/loop-bash-validator.sh:329-338`, and the template’s required `CORRECT_PATH` variable is wired at `hooks/lib/loop-common.sh:236`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`