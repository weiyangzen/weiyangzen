# agent_23 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-023 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: Bash `PreToolUse` gate for the RLCR loop. It prevents shell-command bypasses around loop state, plan backups, prompt/summary/todo files, goal tracker policy, `.humanize` private state, and optional remote push behavior. It is registered as the Bash hook in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/hooks.json:43).
- algorithmic_behavior: Reads hook JSON from stdin, exits early unless `.tool_name == "Bash"`, lowercases `.tool_input.command`, locates the newest active loop under `.humanize/rlcr`, chooses `finalize-state.md` over `state.md` when present, parses YAML-ish frontmatter through `parse_state_file`, then applies ordered block gates. Key order: git push when `push_every_round` is false at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-bash-validator.sh:63), `.humanize` git-add protection at line 81, state/finalize-state write and move/copy protection at lines 98-115 and 128-287, plan backup protection at line 295, goal tracker policy at line 308, prompt writes at line 324, summary writes at line 334, and todo writes at line 344.
- inputs_outputs_state: Input is Claude hook JSON on stdin, plus `CLAUDE_PROJECT_DIR` or `pwd` for `PROJECT_ROOT`; active loop state comes from `.humanize/rlcr/<loop>/state.md` or `finalize-state.md`. Outputs are process exit codes and stderr block messages: `0` allows the Bash command, `2` blocks it. It does not mutate loop state itself except allowing a narrowly authorized cancel rename command to proceed.
- gates_or_invariants: Invariant: loop control files are system-owned. `state.md` and `finalize-state.md` cannot be written, deleted, copied from, or moved except through authorized cancel flow guarded by `.cancel-requested`. `round-*-prompt.md` is read-only. `round-*-summary.md` cannot be modified through Bash; agents must use Write/Edit so other validators can check location and round. `goal-tracker.md` is Bash-blocked in all rounds, with different guidance for round 0 versus later rounds. `.humanize` must not be staged through direct or broad `git add` forms. Todo files are generally blocked, with a narrow active-loop full-path exception for `round-1-todos.md` and `round-2-todos.md`.
- dependencies_and_callers: Sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/lib/loop-common.sh:46) for `find_active_loop`, `parse_state_file`, `to_lower`, `git_adds_humanize`, `is_cancel_authorized`, `command_modifies_file`, and rendered block messages. Depends on `jq`, `sed`, `grep`, `git check-ignore`, shell regexes, and `prompt-template/block/*.md` through `load_and_render_safe`. Covered indirectly by test suites referencing this hook, including finalize phase, cancel signal, allowlist, plan file, hook preservation, and template reference tests surfaced by repository search.
- edge_cases_or_failure_modes: The script has substantial hardening for shell operator splitting, `|&`, `&&`, `||`, `;`, background `&` while preserving redirection operators, leading redirections before `mv/cp`, quoted paths, ANSI-C/locale shell quoting, shell wrappers like `sh -c`, and `sudo/env/command` prefixes. Remaining risk is regex-based shell parsing: complex shell syntax, newlines, process substitution, or unusual quoting could be over-blocked or under-detected. The direct `command_modifies_file` helper is intentionally broad but path-token based, so spaces in target paths may be imperfect. If `jq` is missing or hook input is malformed, `set -euo pipefail` can make the hook fail closed or unexpectedly rather than cleanly allow.
- validation_or_tests: Dedicated pattern tests exist for `command_modifies_file` in `tests/test-bash-validator-patterns.sh`; cancel authorization is directly tested in `tests/test-cancel-signal-file.sh`; `.humanize` staging behavior is tested in `tests/test-humanize-escape.sh`; Bash hook invocations are present in finalize, allowlist, plan-file, and hook-preservation tests. Template reference coverage includes this script in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-template-references.sh:57).
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-053 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/check-todos-from-transcript.py:1), the stop-hook preflight that prevents expensive Codex review while Claude still has incomplete `TodoWrite` tasks. The checker is invoked by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-codex-stop-hook.sh:258).
- algorithmic_behavior: Creates a temp directory, feeds hook JSON to the Python checker, captures stdout/stderr and exit code under `set +e`, and asserts expected pass/fail semantics across 16 cases. It validates input parsing first, then todo-state interpretation, transcript format variations, and edge cases. The checker’s algorithm is: parse stdin JSON, read `transcript_path`, scan JSONL for the most recent non-empty `TodoWrite` list, treat any status other than `completed` as incomplete, print `INCOMPLETE_TODOS`, and return `1`.
- inputs_outputs_state: Inputs are synthetic JSON hook payloads and temporary JSONL transcript files. Outputs are TAP-like colored `PASS`/`FAIL` lines, counters, and final script exit `0` when no assertions fail or `1` when any fail. The test mutates only its temp dir via `mktemp -d` and cleans it with `trap`.
- gates_or_invariants: Invalid hook JSON and empty stdin must exit `2`; missing `transcript_path` and nonexistent transcript files must exit `0`. All completed todos must exit `0`; pending, in-progress, missing status, and empty-content pending todos must exit `1`. The latest `TodoWrite` call wins over earlier calls. Invalid JSONL lines inside a transcript are ignored, not fatal. No todos found means allow.
- dependencies_and_callers: Depends on `python3`, Bash, `mktemp`, `grep`, and the Python checker path resolved from repo root at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-todo-checker.sh:11). The production caller uses the checker’s exit code `2` as a parse-error block and `1` as incomplete-todos block before Codex review in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-codex-stop-hook.sh:265).
- edge_cases_or_failure_modes: The test confirms malformed JSONL transcript lines are skipped, but malformed top-level hook input blocks. It also confirms direct `tool_use`, `type: message`, and normal Claude assistant-message transcript structures. The “Unicode” case currently uses ASCII text, so it names but does not truly stress non-ASCII decoding. Empty `todos: []` is implicitly treated like no todos because the checker only updates `latest_todos` when the list is truthy.
- validation_or_tests: This file is itself the validation harness. Important assertions are invalid input at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-todo-checker.sh:51), all-complete at line 106, incomplete/pending at line 121, latest-call behavior at line 196, transcript format variants at lines 212 and 227, missing status at line 249, and final aggregate exit at line 305.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-083 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR write validator when an agent tries to create or edit a round summary outside the active loop directory. It defines the correction message used to preserve the loop’s summary-file location invariant.
- algorithmic_behavior: The template is static Markdown with a `{{CORRECT_PATH}}` placeholder. [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-write-validator.sh:155) computes the active loop summary path as `round-${CURRENT_ROUND}-summary.md`, renders this template through `load_and_render_safe`, writes it to stderr, and exits `2`.
- inputs_outputs_state: Input is a single render variable, `CORRECT_PATH`. Output is Markdown headed “Wrong Summary Location” and the exact path the agent should use. The file has no state and no executable logic.
- gates_or_invariants: Enforces that summary files must live in the active `.humanize/rlcr/<loop>/` directory, not arbitrary repo paths or stale loop directories. It complements the Bash validator’s separate summary gate, which blocks Bash modification of any `round-*-summary.md` and points to the current loop summary path at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-bash-validator.sh:334).
- dependencies_and_callers: Direct caller found in `loop-write-validator.sh`; template existence is covered by the template reference scanner, whose script list includes write/edit/read/bash validators and common hook library at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-template-references.sh:57). Depends on `template-loader.sh` behavior through `load_and_render_safe`.
- edge_cases_or_failure_modes: If the template is missing or placeholder rendering fails, `load_and_render_safe` falls back to inline text in `loop-write-validator.sh`. If `CORRECT_PATH` is wrong because active-loop discovery or round parsing is wrong, the template faithfully prints the wrong path; the template itself cannot validate that value.
- validation_or_tests: Covered structurally by template-reference tests; behavior is likely covered through write-validator summary-location tests if present. The file itself is only five lines at [prompt-template/block/wrong-summary-location.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/prompt-template/block/wrong-summary-location.md:1).
- skip_candidate: `yes: prompt text is part of the guard contract, but it is not algorithmic by itself; its core relevance comes from the write-validator gate that renders it.`

## Worker Self-Test
- assigned_items_seen: `3/3 evidence sections present; item IDs are intentionally not repeated here to preserve the exact-once constraint`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`