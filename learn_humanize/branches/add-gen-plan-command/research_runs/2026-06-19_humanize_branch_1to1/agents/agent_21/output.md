# agent_21 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-021 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that enforces the loop’s “no unfinished native TodoWrite todos before exit” gate. It is not the whole RLCR state machine, but it is a validator in the stop transition path: `hooks/loop-codex-stop-hook.sh` invokes it before later git/status/review checks (`hooks/loop-codex-stop-hook.sh:260`, `hooks/loop-codex-stop-hook.sh:284`).
- algorithmic_behavior: The main algorithm is `find_latest_todos(transcript_path)` (`hooks/check-todos-from-transcript.py:20`). It streams a Claude transcript JSONL file line by line, ignores blank lines and malformed JSONL records (`hooks/check-todos-from-transcript.py:31`, `hooks/check-todos-from-transcript.py:38`), and keeps only the most recent non-empty `TodoWrite` payload (`hooks/check-todos-from-transcript.py:62`, `hooks/check-todos-from-transcript.py:75`, `hooks/check-todos-from-transcript.py:84`). It recognizes three transcript shapes: Claude assistant messages containing `message.content[].tool_use` (`hooks/check-todos-from-transcript.py:52`), an alternative top-level `message.content[]` shape (`hooks/check-todos-from-transcript.py:66`), and direct top-level `tool_use` entries (`hooks/check-todos-from-transcript.py:79`).
- inputs_outputs_state: Input is hook JSON on stdin with optional `transcript_path` (`hooks/check-todos-from-transcript.py:91`, `hooks/check-todos-from-transcript.py:99`). The transcript path is `~` expanded before reading (`hooks/check-todos-from-transcript.py:103`). Output is mostly exit code state: `0` means no todos or all latest todos completed, `1` means incomplete todos, and `2` means hook-input JSON parse error (`hooks/check-todos-from-transcript.py:7`). On incomplete todos it prints marker `INCOMPLETE_TODOS` followed by `  - [status] content` lines to stdout (`hooks/check-todos-from-transcript.py:121`, `hooks/check-todos-from-transcript.py:124`). State transition is derived from latest TodoWrite snapshot only: any todo whose `status` is not exactly `completed` is treated as unfinished (`hooks/check-todos-from-transcript.py:115`, `hooks/check-todos-from-transcript.py:118`).
- gates_or_invariants: The hook fails open for missing `transcript_path`, nonexistent transcript files, and transcripts with no TodoWrite calls (`hooks/check-todos-from-transcript.py:100`, `hooks/check-todos-from-transcript.py:25`, `hooks/check-todos-from-transcript.py:109`). It fails closed only for malformed hook input JSON, returning parse error exit `2` (`hooks/check-todos-from-transcript.py:92`). The stop hook maps exit `2` to a blocking JSON decision with system message “todo checker parse error” (`hooks/loop-codex-stop-hook.sh:265`, `hooks/loop-codex-stop-hook.sh:273`) and maps exit `1` to an immediate stop block before Codex review (`hooks/loop-codex-stop-hook.sh:284`, `hooks/loop-codex-stop-hook.sh:297`). The invariant is that loop exit cannot proceed while the latest native TodoWrite state contains `pending`, `in_progress`, missing status, or any non-`completed` value.
- dependencies_and_callers: Uses only Python stdlib modules `json`, `sys`, and `pathlib.Path` (`hooks/check-todos-from-transcript.py:15`). Primary caller is `hooks/loop-codex-stop-hook.sh`, which sets `TODO_CHECKER="$SCRIPT_DIR/check-todos-from-transcript.py"` (`hooks/loop-codex-stop-hook.sh:258`) and pipes the original hook input into Python (`hooks/loop-codex-stop-hook.sh:262`). Its blocking message depends on `prompt-template/block/incomplete-todos.md` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:294`). It also coordinates with read/write/edit validators that forbid file-based `round-*-todos.md` access and direct agents toward native TodoWrite instead (`hooks/loop-read-validator.sh:36`, `hooks/lib/loop-common.sh:196`).
- edge_cases_or_failure_modes: Malformed transcript JSONL lines are ignored, so corrupt individual transcript rows do not block if later valid TodoWrite data exists (`hooks/check-todos-from-transcript.py:36`). Empty TodoWrite arrays are ignored and do not replace the previous latest non-empty todo list (`hooks/check-todos-from-transcript.py:62`). This means a later explicit empty todo list is not treated as “all cleared”; it is treated as absent. Missing `status` becomes `""` and blocks (`hooks/check-todos-from-transcript.py:116`). Missing `content` becomes an empty display string, still blocking if status is incomplete (`hooks/check-todos-from-transcript.py:117`). Nonexistent transcript path exits `0`, which avoids false blocking when Claude omits transcript data but can miss unfinished todos if the path is wrong (`hooks/check-todos-from-transcript.py:25`).
- validation_or_tests: Dedicated coverage is in `tests/test-todo-checker.sh`. It asserts invalid or empty stdin exits `2` (`tests/test-todo-checker.sh:51`, `tests/test-todo-checker.sh:63`), missing `transcript_path` and nonexistent transcript files exit `0` (`tests/test-todo-checker.sh:75`, `tests/test-todo-checker.sh:87`), completed todos exit `0` (`tests/test-todo-checker.sh:106`), pending and in-progress todos exit `1` (`tests/test-todo-checker.sh:121`, `tests/test-todo-checker.sh:144`), invalid JSONL lines are ignored (`tests/test-todo-checker.sh:179`), multiple TodoWrite calls use the latest snapshot (`tests/test-todo-checker.sh:196`), direct and alternative transcript formats are accepted (`tests/test-todo-checker.sh:212`, `tests/test-todo-checker.sh:227`), and missing status/empty content behavior is specified (`tests/test-todo-checker.sh:249`, `tests/test-todo-checker.sh:265`).
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-051 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable validation spec for the prompt-template reference graph used by RLCR hooks. It prevents validator block messages from silently becoming empty when a shell hook references a missing template. This is core to gate reliability rather than algorithm execution state itself.
- algorithmic_behavior: The script computes `PROJECT_ROOT` from its own location and fixes `TEMPLATE_DIR` to `prompt-template` (`tests/test-template-references.sh:14`, `tests/test-template-references.sh:16`). It then runs four validation passes: scan selected shell scripts for literal template loads, enumerate all template files and classify direct references, check a curated list of common block templates, and enforce safe rendering in critical validators. Its scanner reads each target script line by line (`tests/test-template-references.sh:74`, `tests/test-template-references.sh:87`), skips comment lines (`tests/test-template-references.sh:88`), matches calls to `load_template`, `load_and_render`, or `load_and_render_safe` whose first argument is literal `"$TEMPLATE_DIR"` (`tests/test-template-references.sh:93`), extracts the second quoted argument with `sed` (`tests/test-template-references.sh:96`), and tests for file existence under `prompt-template` (`tests/test-template-references.sh:100`, `tests/test-template-references.sh:102`).
- inputs_outputs_state: Input is the checked-out repository filesystem; it has no CLI arguments. Its internal state is three counters, `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` (`tests/test-template-references.sh:25`). It also accumulates `MISSING_TEMPLATES`, `TEMPLATE_FILES`, and `UNREFERENCED` arrays (`tests/test-template-references.sh:71`, `tests/test-template-references.sh:122`, `tests/test-template-references.sh:132`). Output is colored PASS/FAIL/WARN text and process exit status. It exits `0` only if `TESTS_FAILED == 0` (`tests/test-template-references.sh:221`); otherwise it prints the missing template list and exits `1` (`tests/test-template-references.sh:213`, `tests/test-template-references.sh:225`).
- gates_or_invariants: Hard invariant: every literal template path referenced by the selected hook scripts must exist (`tests/test-template-references.sh:102`). Warnings do not fail the run: missing checked scripts are warnings (`tests/test-template-references.sh:75`) and unreferenced templates are warnings because dynamic use may be valid (`tests/test-template-references.sh:139`). Another hard invariant is that critical validators `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh` must not call unsafe `load_and_render` directly; they must use `load_and_render_safe` so fallback messages exist if the template is missing (`tests/test-template-references.sh:176`, `tests/test-template-references.sh:185`, `tests/test-template-references.sh:196`). The curated common-template invariant covers shared messages exposed from `hooks/lib/loop-common.sh`, including todos, prompt writes, state modification, summary bash writes, and goal tracker gates (`tests/test-template-references.sh:152`).
- dependencies_and_callers: Depends on Bash, `find`, `grep`, `sed`, and repository path conventions. It checks these scripts explicitly: `hooks/loop-codex-stop-hook.sh`, read/write/edit/bash validators, and `hooks/lib/loop-common.sh` (`tests/test-template-references.sh:57`). It is coupled to the template loader API in `hooks/lib/template-loader.sh`, especially `load_template`, `load_and_render`, and `load_and_render_safe` (`hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:136`, `hooks/lib/template-loader.sh:170`). It is not itself invoked in the inspected files, but it is a test entrypoint for template-reference integrity.
- edge_cases_or_failure_modes: The scanner only recognizes calls with first argument literally quoted as `"$TEMPLATE_DIR"` (`tests/test-template-references.sh:94`), so dynamically constructed template directory arguments or unquoted variants can evade Section 1. It extracts only the second quoted argument after that literal (`tests/test-template-references.sh:96`), so multiline calls or computed template names are not fully modeled. Section 2 searches only `hooks/` for exact quoted template names (`tests/test-template-references.sh:136`), so references from tests or non-hook scripts do not count for the informational “referenced” classification. `set -u` is active but `set -e` is intentionally absent (`tests/test-template-references.sh:12`), allowing the script to accumulate all failures before deciding final exit. Missing scripts warn and do not fail, which is helpful for portability but can hide a removed validator from this particular test.
- validation_or_tests: This file is itself validation. Related coverage exists in `tests/test-template-loader.sh`, which verifies real template rendering and safe fallback behavior (`tests/test-template-loader.sh:151`, `tests/test-template-loader.sh:202`, `tests/test-template-loader.sh:216`), and in `tests/test-templates-comprehensive.sh`, which performs broader template directory and real-template integration checks (`tests/test-templates-comprehensive.sh:485`, `tests/test-templates-comprehensive.sh:499`). The assigned script specifically protects the stop-hook and validator block surfaces found by `rg`, including `block/incomplete-todos.md`, `block/wrong-round-file.md`, and other block templates referenced by shell validators.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-081 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: User-facing block template for the read-validator transition that prevents an agent from reading stale prompt/summary files from a non-current RLCR round. It is a small template, but it participates directly in a state gate: wrong-round reads are rejected and the agent is redirected to current-round files.
- algorithmic_behavior: The template renders a deterministic block reason using `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{FILE_PATH}}` (`prompt-template/block/wrong-round-file.md:3`). It announces the current round files for prompt and summary (`prompt-template/block/wrong-round-file.md:5`) and gives a direct `cat {{FILE_PATH}}` escape hatch for intentional access (`prompt-template/block/wrong-round-file.md:9`). The actual substitution is handled by `load_and_render_safe`, which performs single-pass `{{VAR}}` replacement and leaves missing placeholders unreplaced (`hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`, `hooks/lib/template-loader.sh:120`).
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-read-validator.sh` when `CLAUDE_ROUND != CURRENT_ROUND` for a read of a round prompt/summary that is not allowlisted (`hooks/loop-read-validator.sh:116`, `hooks/loop-read-validator.sh:122`). Rendered output goes to stderr and the validator exits `2`, which is the hook-level block signal (`hooks/loop-read-validator.sh:122`, `hooks/loop-read-validator.sh:128`). There is no persisted state in the template; it reflects current loop state read from `state.md` or `finalize-state.md` by the validator (`hooks/loop-read-validator.sh:70`, `hooks/loop-read-validator.sh:76`).
- gates_or_invariants: The gate is read-only and applies only after several preconditions: tool must be `Read` (`hooks/loop-read-validator.sh:25`), target must be a round prompt or summary (`hooks/loop-read-validator.sh:50`), an active RLCR loop directory must exist (`hooks/loop-read-validator.sh:63`), a round number must be extractable from the filename (`hooks/loop-read-validator.sh:83`), the file must be inside `.humanize/rlcr` or pass earlier location checks (`hooks/loop-read-validator.sh:100`), and the path must not be allowlisted (`hooks/loop-read-validator.sh:116`). The template’s invariant is that the error message names the stale requested round and the actual current round, then points to the current prompt/summary file pair.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`, where it is referenced as `"block/wrong-round-file.md"` (`hooks/loop-read-validator.sh:122`). It depends on shared path and state helpers from `hooks/lib/loop-common.sh`, sourced by the read validator (`hooks/loop-read-validator.sh:15`), and on the shared template loader from `hooks/lib/template-loader.sh` via `load_and_render_safe` (`hooks/lib/template-loader.sh:170`). It is included in the repository-wide template reference graph checked by `tests/test-template-references.sh`, whose Section 1 finds `loop-read-validator.sh` template loads (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:94`).
- edge_cases_or_failure_modes: If this template is missing or renders empty, the caller’s inline fallback is used instead (`hooks/loop-read-validator.sh:117`, `hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:192`), so the gate still blocks but with less complete guidance. If any variable is not supplied, the placeholder remains visible because rendering leaves unknown placeholders unchanged (`hooks/lib/template-loader.sh:120`). The final line says “If you need this file, use: `cat {{FILE_PATH}}`”; because this is emitted during a `Read` block, it guides users toward explicit shell access when intentional, which may be acceptable as an escape path but is also a policy distinction worth noting. Write/edit wrong-round blocks use the sibling `block/wrong-round-number.md`, not this file (`hooks/loop-write-validator.sh:191`, `hooks/loop-edit-validator.sh:141`).
- validation_or_tests: `tests/test-template-references.sh` validates that this exact referenced template exists because `loop-read-validator.sh` uses `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-round-file.md"` (`tests/test-template-references.sh:94`, `hooks/loop-read-validator.sh:122`). Broader template rendering tests exercise the same loader behavior on sibling wrong-round templates, including variable replacement and fallback behavior (`tests/test-template-loader.sh:151`, `tests/test-template-loader.sh:202`). I did not find a dedicated behavioral test that renders `block/wrong-round-file.md` specifically; its coverage is primarily existence/reference validation plus shared loader tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections present; each assigned item is represented once as a section heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`