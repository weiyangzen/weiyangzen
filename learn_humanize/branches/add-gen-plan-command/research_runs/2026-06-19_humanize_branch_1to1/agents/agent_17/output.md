# agent_17 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-017 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Specialized subagent prompt/policy for Phase 2 of `gen-plan`, deciding whether an input draft is semantically relevant to the current repository before expensive plan analysis/generation proceeds. The caller is `skills/gen-plan/SKILL.md:48-72`, which invokes subagent type `draft-relevance-checker` via Task and branches on `RELEVANT:` vs `NOT_RELEVANT:`.
- algorithmic_behavior: The agent prompt defines a three-step classifier: first explore repository context through docs and structure (`agents/draft-relevance-checker.md:16-20`), then compare draft content against repo concepts, technologies, paths, functions, and intended modifications/learning goals (`agents/draft-relevance-checker.md:21-25`), then emit exactly one verdict prefix with a brief explanation (`agents/draft-relevance-checker.md:27-29`). It is deliberately permissive: semantic relevance is prioritized over syntactic matching, informal or multilingual drafts are accepted, and uncertainty should bias to relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft document content supplied by the invoking `gen-plan` skill after IO validation and file read (`skills/gen-plan/SKILL.md:50-58`). Repository state is read-only context discovered through `Read`, `Glob`, and `Grep` tools declared in frontmatter (`agents/draft-relevance-checker.md:4-5`). Output is textual only: `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` (`agents/draft-relevance-checker.md:27-29`). No persistent state is written by this agent; it controls the caller's phase transition.
- gates_or_invariants: The key invariant is the output prefix contract. `gen-plan` stops on `NOT_RELEVANT` and continues on `RELEVANT` (`skills/gen-plan/SKILL.md:66-72`). The gate is intentionally low-friction: "if in doubt" means continue (`agents/draft-relevance-checker.md:36`), reducing false negatives but allowing weakly related drafts through.
- dependencies_and_callers: Depends on local repo documentation and file tree being readable. Direct caller is the `gen-plan` skill, whose command wrapper delegates through `commands/gen-plan.md:10-17` and whose allowed tools include `Task`, `Read`, `Glob`, `Grep`, `Write`, and `AskUserQuestion` (`skills/gen-plan/SKILL.md:5-12`). The prompt explicitly names this agent file as the designed relevance checker (`skills/gen-plan/SKILL.md:64`).
- edge_cases_or_failure_modes: Ambiguous drafts, rough ideas, or non-English text are expected and should lean relevant (`agents/draft-relevance-checker.md:33-36`). A malformed verdict prefix could make caller behavior ambiguous because the skill text only specifies branching by prefix, not fallback handling. A shallow repository exploration could misclassify drafts that reference indirect concepts rather than exact paths.
- validation_or_tests: No direct shell test targets this agent prompt in the inspected suite. The broader `gen-plan` validation is covered through `scripts/validate-gen-plan-io.sh` and `tests/test-skill-structure.sh:682-797`, but that tests IO exit codes rather than Task subagent relevance behavior. The prompt examples at `agents/draft-relevance-checker.md:38-46` serve as embedded output examples, not executable validation.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-047 `file` `tests/test-skill-invocation.sh`
- cursor: `[_]`
- core_role: Executable specification for command-to-skill delegation, skill discoverability, allowed tool scoping, wrapper thinness, setup script viability, auto-invocation descriptions, and minimum instruction completeness for the RLCR start/cancel skills. It is included in the aggregate test runner at `tests/run-all-tests.sh:51-53`.
- algorithmic_behavior: The script is a Bash test harness with strict mode (`tests/test-skill-invocation.sh:12`), root/path derivation (`tests/test-skill-invocation.sh:14-18`), pass/fail counters (`tests/test-skill-invocation.sh:26-44`), grouped validation phases, and a final nonzero exit if any assertion failed (`tests/test-skill-invocation.sh:367-380`). It performs static grep/wc checks plus one script execution check for `setup-rlcr-loop.sh --help` (`tests/test-skill-invocation.sh:258-271`).
- inputs_outputs_state: Inputs are repository files under `skills/`, `commands/`, and `scripts/`: `skills/start-rlcr-loop/SKILL.md`, `skills/cancel-rlcr-loop/SKILL.md`, `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, and `scripts/setup-rlcr-loop.sh` (`tests/test-skill-invocation.sh:16-18`, `61`, `76`, `98`, `113`, `236`). Outputs are colored PASS/FAIL lines and final process exit status. Internal state is only `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: Discovery requires skill frontmatter with `---`, `name:`, and `description:` (`tests/test-skill-invocation.sh:61-68`, `76-82`). Commands must delegate via `Skill(humanize:start-rlcr-loop` and `Skill(humanize:cancel-rlcr-loop` in allowed-tools (`tests/test-skill-invocation.sh:95-123`). Wrappers must remain under 20 lines and omit full operational details while skills retain them (`tests/test-skill-invocation.sh:125-163`). Skills must declare appropriate allowed tools (`tests/test-skill-invocation.sh:173-203`). The setup script must exist, be executable, pass `bash -n`, and support `--help` with exit 0 and usage-like text (`tests/test-skill-invocation.sh:233-272`). Description keyword thresholds enforce auto-invocation discoverability (`tests/test-skill-invocation.sh:282-317`). Completeness requires key strings such as `setup-rlcr-loop.sh`, `Goal Tracker`, `.humanize/rlcr`, `state.md`, `cancel-state.md`, and `AskUserQuestion` (`tests/test-skill-invocation.sh:327-361`).
- dependencies_and_callers: Depends on POSIX/Bash utilities: `grep`, `sed`, `wc`, executable permission bits, and the setup script being runnable. The start skill's actual frontmatter contains `allowed-tools` limited to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` (`skills/start-rlcr-loop/SKILL.md:5-7`), while its command wrapper delegates through `Skill(humanize:start-rlcr-loop:*)` (`commands/start-rlcr-loop.md:1-5`). The cancel skill allows a narrow set of `.humanize/rlcr` state commands plus `Read` and `AskUserQuestion` (`skills/cancel-rlcr-loop/SKILL.md:5-14`), and its command wrapper similarly delegates (`commands/cancel-rlcr-loop.md:1-15`).
- edge_cases_or_failure_modes: The test is intentionally string-based, so behavior can pass with matching text even if runtime semantics later drift. `grep "allowed-tools" | head -1` in Test 9 assumes the wrapper declares tools on a single visible line (`tests/test-skill-invocation.sh:210-216`). Keyword thresholds can fail on harmless description wording changes. The `setup-rlcr-loop.sh --help` execution can fail due to environment or permissions even when content is syntactically valid. The test does not validate actual Skill tool invocation in a Claude runtime; it validates repository contracts that make delegation possible.
- validation_or_tests: This file is itself validation. It exits 0 only if `TESTS_FAILED` remains zero (`tests/test-skill-invocation.sh:373-380`). It complements `tests/test-skill-structure.sh`, which separately validates `validate-gen-plan-io.sh` exit codes for the new gen-plan flow (`tests/test-skill-structure.sh:682-797`), but this assigned script's focus is RLCR skill invocation/delegation.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-077 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the Codex stop hook when `--push-every-round` is enabled and local commits are ahead of the remote. It communicates the required transition: push before exit can proceed.
- algorithmic_behavior: The template renders a markdown block titled "Unpushed Commits Detected" (`prompt-template/block/unpushed-commits.md:1`), substitutes `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into the user-facing stop reason (`prompt-template/block/unpushed-commits.md:3`), states the active policy gate (`prompt-template/block/unpushed-commits.md:5`), and provides the exact command `git push origin {{CURRENT_BRANCH}}` (`prompt-template/block/unpushed-commits.md:7-10`). The final line instructs the user to retry exit after pushing (`prompt-template/block/unpushed-commits.md:12`).
- inputs_outputs_state: Inputs are template variables `AHEAD_COUNT` and `CURRENT_BRANCH`. In the caller, `AHEAD_COUNT` is parsed from `git status -sb` ahead text and `CURRENT_BRANCH` from `git rev-parse --abbrev-ref HEAD` (`hooks/loop-codex-stop-hook.sh:489-495`). Output is rendered markdown placed into the JSON `reason` field for a blocking hook decision (`hooks/loop-codex-stop-hook.sh:501-512`). It does not mutate state; it explains a state gate detected by Git.
- gates_or_invariants: The template is only used when `PUSH_EVERY_ROUND == true` and unpushed commits are detected (`hooks/loop-codex-stop-hook.sh:489-493`). The hook then returns `decision: "block"` and a system message requiring push first (`hooks/loop-codex-stop-hook.sh:505-512`). The invariant is that exit is blocked until local ahead count is cleared by pushing.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, through `load_and_render_safe "$TEMPLATE_DIR" "block/unpushed-commits.md"` with a fallback template (`hooks/loop-codex-stop-hook.sh:496-503`). The rendering subsystem is covered by `tests/test-templates-comprehensive.sh`, which loads this real template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch` (`tests/test-templates-comprehensive.sh:517-520`).
- edge_cases_or_failure_modes: If the template file is missing or rendering fails, the hook has a fallback reason with the same two variables (`hooks/loop-codex-stop-hook.sh:496-503`). If branch detection fails, the caller passes `unknown` as branch (`hooks/loop-codex-stop-hook.sh:494`). The rendered command assumes `origin` is the correct remote and that branch name is pushable as-is. Ahead parsing relies on `git status -sb` containing `ahead N`, so unusual Git status formats could skip or miscount the gate.
- validation_or_tests: `tests/test-templates-comprehensive.sh:517-528` verifies the rendered template contains the heading, `3 unpushed`, and `feature-branch`. The hook caller also includes a fallback for resilience, but no inspected test directly simulates the full Git ahead blocking path for this exact template.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique assigned item sections present above, matching the 3 requested rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`