# agent_19 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-019 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Slash-command entrypoint for the gen-plan workflow. The file is a command facade, not the implementation engine: it exposes metadata and delegates all substantive draft-to-plan behavior to the `humanize:gen-plan` skill.
- algorithmic_behavior: Lines 1-6 define command metadata: description, required argument shape, allowed tool boundary, and `hide-from-slash-command-tool`. Lines 10-17 specify the deterministic dispatch pattern: invoke `Skill: humanize:gen-plan` with `Arguments: $ARGUMENTS`. The command itself performs no parsing, validation, relevance scoring, plan synthesis, or writing.
- inputs_outputs_state: Input is the raw slash-command argument string, expected by line 3 to look like `--input <path/to/draft.md> --output <path/to/plan.md>`. Output is indirect: the delegated skill validates paths, reads the draft, generates a structured plan, and writes it to the requested output path. This command does not maintain loop state or mutate repository files directly.
- gates_or_invariants: The main invariant is tool confinement: line 4 allows only `Skill(humanize:gen-plan:*)`, so the command layer cannot directly call Bash, Read, Write, or Task. The delegated skill adds the actual gates: `skills/gen-plan/SKILL.md` lines 29-45 require IO validation and map validation exit codes to stop conditions; `scripts/validate-gen-plan-io.sh` lines 78-123 enforce input exists, input non-empty, output directory exists, output target absent, and output directory writable.
- dependencies_and_callers: Direct dependency is `skills/gen-plan/SKILL.md`, referenced by `commands/gen-plan.md` lines 10 and 15. The skill depends on `scripts/validate-gen-plan-io.sh` via `skills/gen-plan/SKILL.md` lines 31-35, and on `agents/draft-relevance-checker.md` via `skills/gen-plan/SKILL.md` lines 52-64. The relevance agent is constrained to Read/Glob/Grep and returns `RELEVANT:` or `NOT_RELEVANT:` per `agents/draft-relevance-checker.md` lines 27-29.
- edge_cases_or_failure_modes: Since this command only delegates, failures occur mainly if `$ARGUMENTS` is malformed, the skill is unavailable, or allowed-tool routing fails. Delegated IO validation reports invalid arguments with exit code 6 for missing flags, missing values, unknown flags, or help; missing input is exit 1; empty input is exit 2; missing output directory is exit 3; existing output path or directory target is exit 4; non-writable output directory is exit 5. A draft judged unrelated stops after relevance check instead of producing a plan.
- validation_or_tests: Direct tests for this exact command facade were not found in the inspected references. The delegated IO gate has executable coverage in `tests/test-skill-structure.sh` lines 681-798, including missing flag values, unknown options, missing input, empty input, missing output directory, existing output, directory-as-output, valid paths, and help. The delegated skill structure itself is inspected by the same test file’s skill metadata sections around lines 650-679.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-049 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop state finalization and active-loop discovery. It verifies that only live state files keep a loop active, and that terminal conditions rename `state.md` into reason-specific terminal state files.
- algorithmic_behavior: The test bootstraps an isolated git repo in a temp dir at lines 31-47, creates RLCR loop directories under `.humanize/rlcr`, sources `hooks/lib/loop-common.sh` at line 65, and then exercises shared functions rather than shelling through higher-level commands. Tests 1-7 assert active-loop discovery semantics; tests 8-10 assert `end_loop` terminal rename behavior; tests 11-13 assert new `.humanize/rlcr` path detection and legacy path exclusion.
- inputs_outputs_state: Inputs are synthetic loop directories and state files with YAML frontmatter. The primary observed outputs are function return strings, exit codes, and filesystem renames. `find_active_loop "$TEST_DIR/.humanize/rlcr"` returns either the active loop directory path or an empty string. `end_loop "$loop_dir" "$state_file" "$reason"` moves `state.md` to `<reason>-state.md` on success and returns nonzero for invalid reasons or missing state file.
- gates_or_invariants: The key invariant is that `state.md` is the active-loop marker; terminal files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` must not reactivate a loop. The helper itself also treats `finalize-state.md` as active per `hooks/lib/loop-common.sh` lines 61 and 75-77, although this assigned test focuses on terminal state names and normal `state.md`. Another invariant is newest-directory precedence: line 171 expects `find_active_loop` to choose the lexicographically newest timestamp directory with an active state marker. Path invariant: `.humanize/rlcr/` matches, `.humanize-loop.local/` does not.
- dependencies_and_callers: The test depends directly on `hooks/lib/loop-common.sh`, especially `find_active_loop` at lines 63-81, `is_in_humanize_loop_dir` at lines 415-419, and `end_loop` at lines 622-648. `loop-common.sh` also sources `hooks/lib/template-loader.sh` at line 48, so tests indirectly require template loader availability. The behavior under test is used by RLCR validators and monitors that need to distinguish active loops from archived terminal loops.
- edge_cases_or_failure_modes: Covered failures include archived terminal files being incorrectly detected as active, older loop directories being revived, invalid `end_loop` reasons being accepted, missing `state.md` being silently ignored as success, and legacy `.humanize-loop.local` paths being matched. A notable edge is that `find_active_loop` only checks the newest timestamp directory, as documented in `hooks/lib/loop-common.sh` lines 58-60; if the newest directory lacks active state while an older one has `state.md`, the older one is intentionally ignored to prevent stale revival.
- validation_or_tests: This file is itself the validation asset. It defines pass/fail counters at lines 23-29 and exits with `TESTS_FAILED` at line 298. Specific assertions include completed state not active at lines 51-72, live `state.md` active at lines 74-91, each terminal reason not active at lines 93-155, newest active directory precedence at lines 157-176, invalid `end_loop` reason rejection at lines 182-200, valid terminal rename coverage for `complete cancel maxiter stop unexpected` at lines 202-226, missing state-file handling at lines 228-239, and new-vs-legacy path detection at lines 245-287.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-079 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR hook gates when a tool targets a round file in the wrong loop directory. It shapes the remediation message after validators compute the active loop path and expected file path.
- algorithmic_behavior: The template is a four-field rendered message: title at line 1, attempted action and attempted path at line 3, corrected path at line 4, and a command hint at line 6. It uses `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` placeholders. It does not decide whether to block; it is rendered only after read/write validators have already classified a path as wrong.
- inputs_outputs_state: Inputs are template variables supplied through `load_and_render_safe`: action text such as `read` or `write to`, the requested file path, and the active-loop-correct path. Output is markdown sent to stderr by hook validators before exiting with block code 2. No persistent state is written by the template.
- gates_or_invariants: The governing gate is directory equality: after extracting the requested round filename, validators compute `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and block if the requested `FILE_PATH` differs. `hooks/loop-read-validator.sh` lines 131-146 applies this to reads of round prompt/summary files. `hooks/loop-write-validator.sh` lines 201-216 applies this to writes after summary round-number validation. The invariant is that current round artifacts must be read or written only under the newest active `.humanize/rlcr/<timestamp>/` directory.
- dependencies_and_callers: Called through `load_and_render_safe` from `hooks/loop-read-validator.sh` lines 141-144 and `hooks/loop-write-validator.sh` lines 211-214. Rendering depends on `hooks/lib/template-loader.sh`: `render_template` performs single-pass `{{VAR}}` substitution at lines 50-132, and `load_and_render_safe` provides fallback handling at lines 167-203. Active-loop and path helpers come from `hooks/lib/loop-common.sh`, including `find_active_loop` lines 63-81 and `is_in_humanize_loop_dir` lines 415-419.
- edge_cases_or_failure_modes: If there is no active loop, read/write validators exit without rendering this block. If the template is missing or empty, `load_and_render_safe` falls back to an inline message. Unprovided placeholders remain unreplaced because the renderer intentionally keeps missing variables as-is. One message-level quirk: line 6 says `cat {{FILE_PATH}}`; because `FILE_PATH` is the attempted path, the hint may repeat the wrong path even though line 4 provides `CORRECT_PATH`. That is a content-risk candidate, not a state-transition bug.
- validation_or_tests: Template rendering behavior is covered generally by `tests/test-template-loader.sh` lines 146-164 and `tests/test-templates-comprehensive.sh` lines 492-510 using real block templates, though those examples exercise `wrong-round-number.md` rather than this exact file. The exact wrong-directory template is referenced by validators and covered indirectly when validator tests exercise wrong active-loop paths; direct line-specific coverage for `wrong-directory-path.md` was not found in the inspected test snippets.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item sections present; each assigned section heading appears once.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`