# agent_24 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-024 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook controller for the RLCR loop. It intercepts Claude stop attempts, validates the loop state and worktree, runs Codex review in normal rounds, advances to Finalize Phase on Codex `COMPLETE`, stops on `STOP`, or blocks Claude with next-round instructions when review finds issues. Direct file evidence: hook purpose and state artifacts are declared at `hooks/loop-codex-stop-hook.sh:3`-`13`; active loop discovery starts at `hooks/loop-codex-stop-hook.sh:43`-`63`.
- algorithmic_behavior: Reads hook JSON from stdin, locates `.humanize/rlcr/<timestamp>`, decides whether the active state is normal `state.md` or finalize `finalize-state.md`, parses state frontmatter, and emits Claude-hook JSON decisions. The normal path creates a review prompt, invokes `codex exec`, validates review output, then chooses one of three transitions: `COMPLETE` enters Finalize Phase, `STOP` terminates with stop state, anything else increments `current_round` and blocks exit with next-round feedback. Key transition lines: phase detection at `hooks/loop-codex-stop-hook.sh:72`-`82`; prompt generation at `hooks/loop-codex-stop-hook.sh:671`-`749`; Codex execution at `hooks/loop-codex-stop-hook.sh:813`-`840`; marker parsing at `hooks/loop-codex-stop-hook.sh:955`-`960`; `COMPLETE` handling at `hooks/loop-codex-stop-hook.sh:961`-`1021`; `STOP` handling at `hooks/loop-codex-stop-hook.sh:1023`-`1050`; continue-loop behavior at `hooks/loop-codex-stop-hook.sh:1057`-`1123`.
- inputs_outputs_state: Inputs are stdin hook input, `CLAUDE_PROJECT_DIR` or cwd, `.humanize/rlcr` loop directory, `state.md` or `finalize-state.md`, plan backup `plan.md`, original plan file, `goal-tracker.md`, current round summary, optional transcript path, git status, and Codex CLI output. Outputs are JSON hook decisions such as `{"decision":"block",...}`, stderr diagnostic lines, generated `round-N-review-prompt.md`, `round-N-review-result.md`, next `round-(N+1)-prompt.md`, cached Codex command/stdout/stderr under `$HOME/.cache/humanize/...`, and state-file renames. State fields map from `STATE_*` to local variables at `hooks/loop-codex-stop-hook.sh:88`-`100`. Finalize completion renames `finalize-state.md` to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:651`-`656`. Normal issue feedback rewrites `current_round` via temp file at `hooks/loop-codex-stop-hook.sh:1057`-`1060`.
- gates_or_invariants: The hook fail-closes on malformed Codex model/effort, nonnumeric `current_round`, missing required schema fields, branch drift, plan backup deletion, plan content drift, incomplete todos, git status failure, large changed files over 2000 lines, uncommitted changes, unpushed commits when configured, missing summary file, and uninitialized round-0 goal tracker. Concrete gates: model/effort validation at `hooks/loop-codex-stop-hook.sh:102`-`113`; schema gate at `hooks/loop-codex-stop-hook.sh:127`-`143`; branch gate at `hooks/loop-codex-stop-hook.sh:149`-`176`; plan backup/integrity gates at `hooks/loop-codex-stop-hook.sh:182`-`250`; todo gate at `hooks/loop-codex-stop-hook.sh:258`-`306`; git status fail-closed gate at `hooks/loop-codex-stop-hook.sh:316`-`337`; large-file gate at `hooks/loop-codex-stop-hook.sh:346`-`420`; clean/pushed gates at `hooks/loop-codex-stop-hook.sh:430`-`515`; summary gate at `hooks/loop-codex-stop-hook.sh:522`-`554`; max-iterations gate at `hooks/loop-codex-stop-hook.sh:636`-`642`. Marker invariant is strict last non-empty line equals exactly `COMPLETE` or `STOP`, preventing false positives such as embedded phrases; see `hooks/loop-codex-stop-hook.sh:955`-`960`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file`, markers, template loader setup, and `end_loop` at `hooks/loop-codex-stop-hook.sh:46`-`49`; sources `scripts/portable-timeout.sh` for bounded git/Codex commands at `hooks/loop-codex-stop-hook.sh:50`-`55`. `loop-common.sh` defines marker constants at `hooks/lib/loop-common.sh:26`-`29`, active loop detection at `hooks/lib/loop-common.sh:58`-`81`, state parsing at `hooks/lib/loop-common.sh:98`-`139`, and state finalization via `end_loop` at `hooks/lib/loop-common.sh:616`-`648`. It depends on templates under `prompt-template/codex/*`, `prompt-template/claude/*`, and `prompt-template/block/*`; notably `prompt-template/claude/finalize-phase-prompt.md` is rendered at `hooks/loop-codex-stop-hook.sh:1005`-`1008`. It calls `hooks/check-todos-from-transcript.py`, whose exit-code contract is documented at `hooks/check-todos-from-transcript.py:7`-`10` and whose incomplete-todo output marker is at `hooks/check-todos-from-transcript.py:121`-`126`.
- edge_cases_or_failure_modes: No active loop exits silently with code 0; missing `state.md` and missing `finalize-state.md` also allow exit (`hooks/loop-codex-stop-hook.sh:59`-`82`). Outdated state schema blocks with operator instructions (`hooks/loop-codex-stop-hook.sh:131`-`143`). Git operation timeout blocks instead of allowing an unsafe exit (`hooks/loop-codex-stop-hook.sh:149`-`164`, `316`-`337`). For tracked plans, both uncommitted git status and content diff against backup are enforced (`hooks/loop-codex-stop-hook.sh:214`-`231`). Finalize Phase skips Codex review after all gates pass (`hooks/loop-codex-stop-hook.sh:646`-`657`). If Codex returns `COMPLETE` at or beyond max iterations, the hook skips Finalize Phase and records max-iteration termination (`hooks/loop-codex-stop-hook.sh:963`-`969`). Codex not found, nonzero exit, missing review result file, empty review file, or failure copying stdout all block with debug file references (`hooks/loop-codex-stop-hook.sh:755`-`775`, `850`-`950`).
- validation_or_tests: Finalize behavior is covered by `tests/test-finalize-phase.sh`: test headings enumerate positive and negative cases at `tests/test-finalize-phase.sh:5`-`19`; finalize state detection is asserted at `tests/test-finalize-phase.sh:219`-`255`; missing finalize summary and “Codex not called” in finalize are asserted at `tests/test-finalize-phase.sh:365`-`388`; clean-git and complete-state rename are asserted at `tests/test-finalize-phase.sh:390`-`428`; `COMPLETE` entering Finalize Phase and max-iteration skip are asserted at `tests/test-finalize-phase.sh:460`-`495`; incomplete todos block finalize at `tests/test-finalize-phase.sh:521`-`540`. Hook JSON schema preservation is checked at `tests/test-hook-preservation.sh:483`-`532`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-054 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh-specific safety specification for the humanize monitor runtime. Its algorithmic role is to prevent zsh `nomatch` glob failures in monitor/session discovery paths, which would otherwise break inspection of RLCR loop state, state files, and Codex logs. It states the acceptance criteria directly at `tests/test-zsh-monitor-safety.sh:3`-`10`.
- algorithmic_behavior: Creates an isolated `/tmp/test-zsh-humanize-$$` workspace, sources `scripts/humanize.sh` under zsh, then exercises test-local copies of the monitor discovery algorithms against empty directories, dotfile-only directories, no matching state files, empty cache directories, and multiple timestamped sessions. Setup and cleanup are at `tests/test-zsh-monitor-safety.sh:44`-`56`. The pass/fail counters and final exit contract are at `tests/test-zsh-monitor-safety.sh:24`-`36` and `tests/test-zsh-monitor-safety.sh:326`-`344`.
- inputs_outputs_state: Inputs are the repository root resolved from the test path, the current zsh runtime, temporary `.humanize/rlcr` directories, dotfiles such as `.cancel-requested`, timestamp-named session directories, and `$HOME/.cache/humanize/test-project/...`. Outputs are PASS/FAIL lines and process exit 0 when all assertions pass, exit 1 when any assertion fails. It mutates only temporary test state under `/tmp/test-zsh-humanize-$$` and removes it via trap (`tests/test-zsh-monitor-safety.sh:48`-`56`).
- gates_or_invariants: The core invariant is “monitor discovery must use `find` streams and directory existence checks rather than unmatched globs.” Test 1 fails if sourcing `humanize.sh` prints `no matches found` (`tests/test-zsh-monitor-safety.sh:61`-`73`). Test 2 requires latest-session discovery to return empty safely for an empty loop dir (`tests/test-zsh-monitor-safety.sh:82`-`120`). Test 3 requires dotfiles not to trigger glob errors or fake sessions (`tests/test-zsh-monitor-safety.sh:129`-`160`). Test 4 requires no `*-state.md` match to return `|unknown` safely (`tests/test-zsh-monitor-safety.sh:169`-`208`). Test 5 requires an empty Codex cache to count zero logs without error (`tests/test-zsh-monitor-safety.sh:217`-`238`). Test 6 requires valid timestamped sessions to be counted and latest selected lexicographically by timestamp (`tests/test-zsh-monitor-safety.sh:249`-`276`). Test 7 proves the suite is actually running under zsh (`tests/test-zsh-monitor-safety.sh:279`-`289`). Test 8 demonstrates the old glob pattern hazard and the new `find` pattern (`tests/test-zsh-monitor-safety.sh:292`-`324`).
- dependencies_and_callers: The suite sources `scripts/humanize.sh` directly at `tests/test-zsh-monitor-safety.sh:67`-`72` and is executed by the aggregate runner specifically under zsh. `tests/run-all-tests.sh` lists it in `TEST_SUITES` at `tests/run-all-tests.sh:32`-`54`, marks it as zsh-only at `tests/run-all-tests.sh:56`-`59`, and dispatches it with `zsh "$suite_path"` when available at `tests/run-all-tests.sh:85`-`90`. The implementation being specified lives in `scripts/humanize.sh`: `_humanize_monitor_codex` enables zsh `ksharrays` at `scripts/humanize.sh:9`-`13`; `_find_latest_session` uses `find` at `scripts/humanize.sh:27`-`49`; `_find_latest_codex_log` uses `find` for both sessions and cached logs at `scripts/humanize.sh:51`-`108`; `_find_state_file` uses `find` for stopped-state files at `scripts/humanize.sh:111`-`149`.
- edge_cases_or_failure_modes: Covers exactly the cases that would make zsh abort with `no matches found`: empty `.humanize/rlcr`, dotfile-only loop dirs, missing `*-state.md`, empty `round-*-codex-run.log` cache, and old direct glob iteration. A limitation is that several checks duplicate the relevant helper logic inside the test instead of calling private nested monitor helpers directly; this is still useful because it mirrors the concrete shell patterns and separately verifies that sourcing the real script does not emit glob errors.
- validation_or_tests: This file is itself the validation asset. It also aligns with broader monitor safety tests referenced by `rg`: `tests/test-monitor-e2e-real.sh` checks for `no matches found|bad pattern`, while this assigned file is the zsh-focused unit/runtime spec. The aggregate runner’s zsh dispatch is important because running this file under bash would not exercise the same `nomatch` behavior.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-084 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude prompt template for the Finalize Phase transition after Codex has already accepted the implementation. It defines the post-review contract: simplify/refactor only, preserve behavior, run tests, commit, and write `finalize-summary.md`. The hook renders this template when Codex review’s last non-empty line is `COMPLETE`; see `hooks/loop-codex-stop-hook.sh:983`-`1019`.
- algorithmic_behavior: Provides the instruction payload returned as the `reason` of a blocking stop-hook JSON decision. That block intentionally prevents final exit, moves the loop into `finalize-state.md`, and asks Claude to run the `code-simplifier:code-simplifier` agent before the final completion attempt. Template title and phase premise are at `prompt-template/claude/finalize-phase-prompt.md:1`-`5`; agent invocation requirement is at `prompt-template/claude/finalize-phase-prompt.md:7`-`14`; the final summary destination placeholder is at `prompt-template/claude/finalize-phase-prompt.md:40`-`45`.
- inputs_outputs_state: Inputs are template variables `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`, passed by the stop hook at `hooks/loop-codex-stop-hook.sh:1005`-`1008`. Output is rendered markdown embedded into stop-hook JSON as a block reason (`hooks/loop-codex-stop-hook.sh:1012`-`1019`). The state transition around it is `state.md` renamed to `finalize-state.md` before rendering (`hooks/loop-codex-stop-hook.sh:979`-`981`); on the next stop attempt, if all gates pass, `finalize-state.md` becomes `complete-state.md` (`hooks/loop-codex-stop-hook.sh:651`-`656`).
- gates_or_invariants: The template’s invariants are non-negotiable: do not change existing functionality, do not fail existing tests, do not introduce bugs, and only perform functionality-equivalent simplification (`prompt-template/claude/finalize-phase-prompt.md:16`-`24`). It scopes work to code changed during the RLCR session and allows complexity reduction, readability improvements, duplicate consolidation, control-flow simplification, and dead-code removal (`prompt-template/claude/finalize-phase-prompt.md:25`-`34`). It requires all todos completed, changes committed, and finalize summary written before exiting (`prompt-template/claude/finalize-phase-prompt.md:40`-`50`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe`, with inline fallback if the template is missing (`hooks/loop-codex-stop-hook.sh:985`-`1008`). Its operational siblings include validator protections for `finalize-state.md` and allowance for `finalize-summary.md`: `hooks/loop-write-validator.sh` detects finalize phase and allows only the exact finalize summary path at `hooks/loop-write-validator.sh:88`-`123`; `hooks/lib/loop-common.sh` identifies finalize paths at `hooks/lib/loop-common.sh:219`-`225` and `hooks/lib/loop-common.sh:262`-`271`. The template also references the original plan and goal tracker via rendered file mentions at `prompt-template/claude/finalize-phase-prompt.md:35`-`39`.
- edge_cases_or_failure_modes: If the template is unavailable, the stop hook uses an inline fallback with equivalent constraints and summary destination (`hooks/loop-codex-stop-hook.sh:985`-`1004`). If Claude enters Finalize Phase but omits `finalize-summary.md`, the stop hook blocks (`hooks/loop-codex-stop-hook.sh:522`-`554`). If the worktree is dirty, finalize still blocks before completion (`hooks/loop-codex-stop-hook.sh:430`-`483`). If todos remain incomplete, finalize blocks before any final completion (`hooks/loop-codex-stop-hook.sh:258`-`306`). If `COMPLETE` happens at max iterations, this prompt is skipped and the loop ends as max-iteration state (`hooks/loop-codex-stop-hook.sh:963`-`969`).
- validation_or_tests: `tests/test-finalize-phase.sh` asserts the prompt path indirectly: when mock Codex outputs `COMPLETE`, the hook must block, create `finalize-state.md`, remove normal `state.md`, and return a prompt mentioning simplification (`tests/test-finalize-phase.sh:460`-`475`). It also asserts finalize completion renames to `complete-state.md` and does not call Codex during finalize (`tests/test-finalize-phase.sh:382`-`388`, `tests/test-finalize-phase.sh:412`-`428`). Validator tests in the same file assert `finalize-summary.md` is writable but `finalize-state.md` is protected (`tests/test-finalize-phase.sh:279`-`350`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_GEN_PLAN_COMMAND-HZ-024`, `ADD_GEN_PLAN_COMMAND-HZ-054`, `ADD_GEN_PLAN_COMMAND-HZ-084`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`