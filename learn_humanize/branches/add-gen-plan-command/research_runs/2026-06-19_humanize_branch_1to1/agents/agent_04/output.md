# agent_04 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: Runtime enforcement layer for the Humanize RLCR loop. The directory wires Claude Code hook events to validators and the Codex stop/review state machine. `hooks/hooks.json:4-63` registers `UserPromptSubmit`, `PreToolUse` for `Write`, `Edit`, `Read`, `Bash`, and `Stop` with a 7200s timeout for `loop-codex-stop-hook.sh`.
- algorithmic_behavior: The directory coordinates several guard algorithms. `hooks/lib/template-loader.sh:24-222` resolves templates, loads them, performs single-pass `{{VAR}}` substitution, provides fallbacks, and validates required template subdirectories. `hooks/lib/loop-common.sh:63-81` selects only the newest active `.humanize/rlcr/<timestamp>` loop, `112-139` parses YAML-like state frontmatter, `146-188` classifies round files and allowlisted files, `274-413` authorizes cancellation only through an exact signal-file-backed `mv`, `443-539` detects dangerous `git add` patterns for `.humanize`, `571-599` detects shell file modification patterns, and `615-648` performs terminal loop state renames. `loop-read-validator.sh:36-146` blocks stale or misplaced reads of todos, prompts, and summaries. `loop-write-validator.sh:37-216` blocks protected writes and enforces active-loop path/round invariants, with a special finalize-summary allowance at `121-125`. `loop-edit-validator.sh:36-150` mirrors edit-time constraints. `loop-bash-validator.sh:63-354` blocks push policy violations, `.humanize` adds, state/plan/goal/prompt/summary/todos shell modification bypasses, including chained commands, shell wrappers, and redirection-heavy segments. `loop-plan-file-validator.sh:52-226` validates state schema, branch consistency, and tracked/untracked plan-file invariants before user prompts. `check-todos-from-transcript.py:20-129` scans transcript JSONL for the latest `TodoWrite` across three known transcript formats and fails on incomplete todos. `loop-codex-stop-hook.sh:59-1123` is the main stop-hook state machine: it finds the loop, validates state, branch, plan integrity, todos, git state, summary files, goal tracker initialization, max iterations, Codex availability/output, and then either enters finalize, completes finalize, stops, maxiters, or generates the next round prompt.
- inputs_outputs_state: Inputs are Claude hook JSON on stdin, `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_ROOT`, `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md,plan.md,goal-tracker.md,round-*}`, git status/branch output, transcript JSONL, prompt templates, and Codex CLI output. PreToolUse validators allow with exit `0` or block with stderr plus exit `2`; `UserPromptSubmit` and `Stop` hooks emit JSON decisions such as `{"decision":"block","reason":...}` and exit `0` per hook protocol. Runtime state transitions include no-active-loop allow, `state.md -> finalize-state.md` on strict `COMPLETE` (`loop-codex-stop-hook.sh:961-1021`), `finalize-state.md -> complete-state.md` after finalize checks (`651-657`), `state.md -> stop-state.md` on strict `STOP` (`1023-1051`), `state.md -> maxiter-state.md` on max iteration (`638-642`), and current-round increment plus next prompt creation when Codex returns issues (`1057-1123`).
- gates_or_invariants: Uses latest loop only to avoid reviving old loops (`loop-common.sh:58-81`). Protects state files, finalize state, prompt files, todos, plan backup, and goal tracker after round 0. Requires branch to remain unchanged (`loop-plan-file-validator.sh:88-112`, `loop-codex-stop-hook.sh:145-176`), plan backup to exist and match (`182-250`), todos to be complete before exit (`258-306`), `git status` to succeed fail-closed (`316-337`), modified files to stay under 2000 lines (`340-421`), git worktree to be clean before Codex review (`423-483`), unpushed commits to be pushed when configured (`485-515`), and a round/finalize summary to exist (`518-554`). Codex review result must exist, be nonempty, and end with a standalone `COMPLETE` or `STOP` marker for those transitions (`897-960`).
- dependencies_and_callers: Called by Claude Code through `hooks/hooks.json`. Depends on `jq`, `git`, `python3`, `awk`, `sed`, `grep`, `wc`, `diff`, `tail`, `codex`, and `scripts/portable-timeout.sh`. Depends on `prompt-template/block/*`, `prompt-template/codex/*`, and `prompt-template/claude/*`; the assigned `prompt-template/block/git-status-failed.md` is invoked from `loop-codex-stop-hook.sh:327-334`. The stop hook calls `hooks/check-todos-from-transcript.py` at `258-263`.
- edge_cases_or_failure_modes: No active loop allows all hooks. Missing or outdated state schema blocks. Invalid model/effort or corrupted numeric state ends unexpected (`loop-codex-stop-hook.sh:102-124`). `finalize-state.md` is checked before generic `state.md` patterns to avoid false matches in write/edit/bash validators. Bash bypass handling covers chained commands, wrapper shells, source and destination `mv/cp`, leading redirections, ANSI-C quotes, escaped paths, and cancellation only with exact two-argument `mv` plus `.cancel-requested`. Todo checker ignores malformed transcript lines but exits `2` on malformed hook input. Codex absent, nonzero, missing result, stdout-only result-copy failure, and empty result all block with debug paths (`755-950`). `git status` failure blocks because repository state cannot be verified.
- validation_or_tests: `tests/run-all-tests.sh:33-54` includes suites covering templates, bash patterns, todo checker, plan-file validation, hook preservation, allowlists, finalize phase, cancel signal, and related monitor/runtime behavior. Targeted test references include `tests/test-plan-file-hooks.sh` invoking plan/write/edit/bash/stop hooks, `tests/test-hook-preservation.sh` validating hook files and `hooks.json`, `tests/test-allowlist-validators.sh` exercising allowlist behavior, `tests/test-finalize-phase.sh` exercising finalize transitions, `tests/test-todo-checker.sh` covering transcript parsing, and `tests/test-template-loader.sh` covering template rendering.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-034 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Executable aggregate validation gate for the plugin. It defines the ordered test contract that branch changes are expected to satisfy.
- algorithmic_behavior: Initializes counters and failed-suite tracking (`tests/run-all-tests.sh:28-30`), declares the ordered `TEST_SUITES` array (`33-54`), declares zsh-only suites (`57-59`), iterates each suite (`61-120`), skips missing files (`64-67`), dispatches zsh-only suites under `zsh` when available (`78-95`), runs other suites as executables (`96-99`), strips ANSI sequences (`102-104`), extracts final `Passed:` and `Failed:` counts (`105-106`), accumulates totals (`108-109`), and marks a suite failed if its exit code is nonzero or parsed failed count is greater than zero (`111-118`).
- inputs_outputs_state: Inputs are executable files under `tests/`, interpreter availability, suite stdout/stderr, suite exit codes, and summary lines matching `Passed:` / `Failed:`. Outputs are console progress, per-suite pass/fail/skip messages, final totals, failed suite names, and process exit `0` when no failed suites are recorded or `1` otherwise (`129-139`). State is local shell variables only.
- gates_or_invariants: A nonzero suite exit always fails the aggregate even if parsed failed count is zero. A parsed `Failed: N` greater than zero also fails even if exit code is zero. Missing suites are skipped rather than failed (`64-67`). `test-zsh-monitor-safety.sh` is required to run under zsh, but absence of zsh skips that suite (`87-95`). The runner depends on each suite printing recognizable `Passed:` / `Failed:` summary lines for accurate aggregate counts.
- dependencies_and_callers: Depends on `bash`, optional `zsh`, `sed`, `grep`, `tail`, and executable permissions on test suites. It coordinates the suites listed at `33-54`, including template, hook, plan-file, finalize, cancel, monitor, skill structure, hook preservation, and skill invocation tests.
- edge_cases_or_failure_modes: A missing test silently reduces coverage by reporting `SKIP`. If zsh is unavailable, zsh safety coverage is skipped. If a suite does not emit expected `Passed:` / `Failed:` text, the aggregate count defaults to `0` via `|| echo "0"` even though the suite can still fail through exit code. Only the last 30 lines of a failed suite are shown (`114-115`), so earlier diagnostics may be hidden.
- validation_or_tests: This file is itself the top-level validation harness. It does not appear to have a separate self-test; its correctness is implied by executable use and by the individual suites it invokes.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-064 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Block-message template for the stop hook’s fail-closed git-status gate.
- algorithmic_behavior: Provides a markdown reason headed `# Git Status Failed` and interpolates `{{GIT_STATUS_EXIT}}` into the failure sentence (`prompt-template/block/git-status-failed.md:1-3`). It explains that repository state cannot be verified and lists likely classes of cause: lock contention, invalid repository state, or a large/slow repository (`5-9`). `loop-codex-stop-hook.sh:316-337` caches `git status --porcelain`, treats timeout/failure as blocking, renders this template with `GIT_STATUS_EXIT`, and emits a JSON block decision.
- inputs_outputs_state: Input is `GIT_STATUS_EXIT` supplied by the stop hook’s `load_and_render_safe` call. Output is human-facing markdown embedded into the hook JSON `reason`. It has no persistent state.
- gates_or_invariants: The associated invariant is fail-closed: if `git status` cannot complete, the loop must not proceed to large-file checks, clean-worktree checks, Codex review, or loop completion because repository state cannot be proven. If the template is missing or empty, the stop hook has an inline fallback at `loop-codex-stop-hook.sh:327-331`.
- dependencies_and_callers: Called only through the template loader from `loop-codex-stop-hook.sh:327-334`. Depends on `hooks/lib/template-loader.sh` single-pass placeholder rendering; if the variable is absent, the placeholder remains literal by design (`template-loader.sh:116-122`).
- edge_cases_or_failure_modes: Covers both timeout and generic nonzero git failures under the same text. It intentionally avoids prescribing automatic recovery, instead instructing manual `git status` verification. If `GIT_STATUS_EXIT` is unset or malformed before rendering, the template text can retain or display a confusing placeholder/value, but the gate still blocks.
- validation_or_tests: Template reference coverage is included by `tests/run-all-tests.sh:38` via `test-template-references.sh` and broader template coverage by `tests/run-all-tests.sh:40` via `test-templates-comprehensive.sh`. The stop-hook caller path is visible at `loop-codex-stop-hook.sh:327-334`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-094 `file` `skills/gen-plan/SKILL.md`
- cursor: `[_]`
- core_role: Skill-level algorithmic workflow for `/humanize:gen-plan`, converting a rough draft into a structured implementation plan with goals, AC-style acceptance criteria, path boundaries, feasibility hints, and dependency sequencing.
- algorithmic_behavior: Frontmatter declares the skill name, purpose, model, and allowed tools (`skills/gen-plan/SKILL.md:1-13`). Phase 1 runs `scripts/validate-gen-plan-io.sh` with `$ARGUMENTS` and maps exit codes `0-6` to stop/continue behavior (`29-45`). Phase 2 reads the draft and invokes `draft-relevance-checker` through `Task` using a haiku model, requiring `RELEVANT:` or `NOT_RELEVANT:` and stopping on not relevant (`48-71`); the referenced agent defines repository exploration and verdict rules at `agents/draft-relevance-checker.md:14-30` and lenient relevance policy at `31-37`. Phase 3 analyzes clarity, consistency, completeness, and functionality, using Explore agents for codebase investigation (`75-107`). Phase 4 uses `AskUserQuestion` until significant issues are resolved or acknowledged (`110-120`). Phase 5 emits the plan structure and generation rules (`123-220`). Phase 6 writes the generated plan and reports path, summary, and AC count (`223-229`). Error handling requires clarification or an explanation of missing information (`232-240`).
- inputs_outputs_state: Inputs are command arguments, an existing nonempty draft file, an output path that must not already exist, repository structure/content discovered through reads/searches/subagents, and user clarification answers. Output is a newly written plan file plus a final report containing generated path, included summary, and acceptance-criteria count. The workflow has no long-lived state except the output file it creates.
- gates_or_invariants: IO validation blocks missing input, empty input, missing output directory, existing output file/path-as-directory, no output write permission, and invalid arguments. The concrete validator implements these checks at `scripts/validate-gen-plan-io.sh:28-130`. Relevance check blocks unrelated drafts. Plan-generation invariants include English only, no emoji/CJK, no line numbers, no time estimates, AC-X or AC-X.Y format, both positive and negative tests for each AC, affirmative upper/lower path boundaries, conceptual not prescriptive suggestions, dependency relationships rather than schedules, and narrowed boundaries for deterministic designs (`skills/gen-plan/SKILL.md:199-220`).
- dependencies_and_callers: `commands/gen-plan.md:1-17` is a thin command shim allowing only `Skill(humanize:gen-plan:*)` and passing `$ARGUMENTS` to this skill. The skill depends on `scripts/validate-gen-plan-io.sh`, `agents/draft-relevance-checker.md`, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion`. README usage references `/gen-plan --input <draft.md> --output <plan.md>` at `README.md:84` and describes the command at `README.md:103-119`.
- edge_cases_or_failure_modes: The validator treats `--help`, unknown options, missing flag values, and flags where values are expected as invalid arguments with exit `6` (`scripts/validate-gen-plan-io.sh:15-66`). It uses `realpath -m` when available and falls back to the original path (`68-71`). An output path that is a directory and an already-existing file both map to exit `4` (`102-115`). Relevance checking is intentionally lenient and marks doubtful cases relevant, reducing false negatives but permitting weakly related drafts. Semantic quality gates for clarity/completeness/functionality depend on agent judgment and user clarification rather than a deterministic script.
- validation_or_tests: `tests/test-skill-structure.sh:682-797` tests `validate-gen-plan-io.sh` exit behavior for missing values, unknown flags, nonexistent input, empty input, missing output directory, existing output, output directory path, valid paths, and help. `tests/run-all-tests.sh:51-53` includes `test-skill-structure.sh`, `test-hook-preservation.sh`, and `test-skill-invocation.sh`. There is visible validation for IO and skill metadata, but I did not find an automated test that verifies the generated plan’s semantic quality, relevance-check decision quality, or required AC/path-boundary content.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_GEN_PLAN_COMMAND-HZ-004`, `ADD_GEN_PLAN_COMMAND-HZ-034`, `ADD_GEN_PLAN_COMMAND-HZ-064`, `ADD_GEN_PLAN_COMMAND-HZ-094`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`