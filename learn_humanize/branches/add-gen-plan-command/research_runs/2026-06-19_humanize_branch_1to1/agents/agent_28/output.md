# agent_28 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-028 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: Claude Code `PreToolUse` Write-path guard for the RLCR loop. It protects loop-owned control files and enforces that Claude writes only the correct per-round/finalize artifacts in the active `.humanize/rlcr/<timestamp>` directory. It is registered as the `Write` matcher in `hooks/hooks.json:14-21`.
- algorithmic_behavior: The hook reads hook JSON from stdin, extracts `.tool_name`, and exits immediately unless the tool is `Write` (`hooks/loop-write-validator.sh:23-28`). For Write calls, it extracts `.tool_input.file_path`, lowercases it for pattern matching, and applies ordered gates: block non-allowlisted `round-*-todos.md` writes (`37-45`), block all `round-*-prompt.md` writes (`47-50`), classify summary/finalize-summary/humanize-loop paths (`56-58`), allow unrelated paths (`60-63`), narrow `.humanize/rlcr` handling to protected control files (`67-73`), find the active loop and parse current round from `state.md` or `finalize-state.md` (`79-98`), block `finalize-state.md` before generic `state.md` because the generic matcher also catches it (`101-113`), allow only the active loop’s `finalize-summary.md` (`121-126`), block `plan.md` backups inside RLCR (`132-138`), block `goal-tracker.md` after round 0 (`145-149`), block summary files outside `.humanize/rlcr` (`155-163`), validate round numbers for summary writes (`181-199`), then enforce exact active-loop directory placement (`205-216`).
- inputs_outputs_state: Input is Claude hook JSON on stdin with `tool_name` and `tool_input.file_path`. Environment input is `CLAUDE_PROJECT_DIR`; if absent, it falls back to `pwd` for project root (`38`, `80`). State input is the newest active loop under `$PROJECT_ROOT/.humanize/rlcr`, discovered through `find_active_loop` (`40`, `82`). Current round is parsed from frontmatter in `state.md` or `finalize-state.md` via `parse_state_file` (`91-98`). Output is process exit: `0` allows the Write; `2` blocks and emits a rendered template message on stderr. The hook itself does not mutate repository or loop state.
- gates_or_invariants: Prompt files are read-only artifacts generated for Claude, so Write always blocks them (`47-50`). Todos files must use native TodoWrite except a narrow allowlist in the active loop (`37-45`); the allowlist is defined in `hooks/lib/loop-common.sh:170-188` as `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`. `state.md` and `finalize-state.md` are system-managed and cannot be written directly (`105-113`). `finalize-summary.md` is writable only when the target path is exactly `$ACTIVE_LOOP_DIR/finalize-summary.md` (`121-124`). `goal-tracker.md` is mutable through Write only during round 0; after that, updates must be requested through the summary path (`145-148`). Summary writes must target the current round unless allowlisted (`181-198`) and must live in the active loop directory (`205-216`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`15-17`), which in turn sources `template-loader.sh` and initializes `TEMPLATE_DIR` from the plugin root (`hooks/lib/loop-common.sh:46-55`). Key helper dependencies include `find_active_loop` (`63-81`), `parse_state_file` (`112-139`), `is_round_file_type` (`148-153`), `extract_round_number` (`158-165`), `is_allowlisted_file` (`170-188`), `is_goal_tracker_path` (`251-254`), `is_state_file_path` (`257-260`), `is_finalize_state_file_path` (`263-266`), `is_finalize_summary_path` (`269-272`), and `is_in_humanize_loop_dir` (`416-419`). It uses block templates through `load_and_render_safe`, including `block/finalize-state-file-modification.md`, `block/wrong-summary-location.md`, `block/wrong-round-number.md`, `block/wrong-directory-path.md`, and `block/plan-backup-protected.md`. Sibling validators enforce related behavior for Edit, Read, and Bash; the Edit validator mirrors the finalize/state block ordering at `hooks/loop-edit-validator.sh:81-94`, and Bash protects direct shell modifications at `hooks/loop-bash-validator.sh:88-115` plus source-file move/copy bypass checks at `240-286`.
- edge_cases_or_failure_modes: If `tool_name` is not `Write`, the hook allows silently (`26-28`). If no active loop is found, protected path checks after classification mostly no-op and exit allow (`84-86`), which prevents stale or completed loops from blocking normal work. `find_active_loop` checks only the newest timestamp directory and only considers `state.md` or `finalize-state.md` active, intentionally ignoring older “zombie” loop directories (`hooks/lib/loop-common.sh:58-81`). `is_state_file_path` matches any path ending in `state.md`, including `finalize-state.md`; this is why the hook checks `is_finalize_state_file_path` first (`hooks/loop-write-validator.sh:103-113`, helper definitions at `hooks/lib/loop-common.sh:257-266`). Case-insensitive file matching is achieved by lowercasing the path (`30-31`), but exact path equality for active-loop placement uses the original path string (`123`, `184`, `207`), so casing or symlink-normalized path variants can fail exact placement. The allowlist is filename and active-loop-directory exact, so an allowlisted filename outside the active loop still blocks. If `jq` is unavailable or malformed JSON is passed, `set -euo pipefail` means the hook can fail instead of returning a structured block reason.
- validation_or_tests: Direct behavioral test coverage is present but not executed in this read-only research pass. `tests/test-allowlist-validators.sh:64-124` validates allowlist behavior, and `133-183` verifies Write allow/block outcomes for allowlisted todos and summaries. `tests/test-finalize-phase.sh:174-216` validates finalize helper detection, `219-256` validates active-loop detection with `finalize-state.md` and exclusion of `complete-state.md`, and `281-303` validates Write allowing `finalize-summary.md` while blocking `finalize-state.md`. `tests/test-hook-preservation.sh:98-167` validates `PreToolUse` matcher configuration and `409-426` validates the Write hook fires and blocks `plan.md`. Template reference and safe-render usage are checked in `tests/test-template-references.sh:52-105` and `172-202`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-058 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for attempts to modify `finalize-state.md`, the RLCR loop’s Finalize Phase state file. It is not an algorithm by itself, but it is a user-facing enforcement contract for the validator state machine.
- algorithmic_behavior: The template states that `finalize-state.md` cannot be modified because it is managed by the loop system during Finalize Phase (`prompt-template/block/finalize-state-file-modification.md:1-3`). It then redirects the actor to the valid Finalize Phase work: run the code simplifier, commit changes, and write the finalize summary to `finalize-summary.md` (`5-8`). The enforcement algorithm is in callers: `finalize_state_file_blocked_message` loads this template through `load_and_render_safe` (`hooks/lib/loop-common.sh:219-226`); Write and Edit validators call that helper before the broader `state.md` check (`hooks/loop-write-validator.sh:101-113`, `hooks/loop-edit-validator.sh:81-94`); Bash validator calls it for shell writes/moves/copies involving `finalize-state.md` (`hooks/loop-bash-validator.sh:98-104`, `245-252`, `268-276`).
- inputs_outputs_state: It takes no template variables and has no dynamic placeholders. Input is simply the template path requested by the shared template loader. Output is Markdown text emitted to stderr by PreToolUse validators when blocking, or embedded in the hook block response depending on caller. State transition context comes from `loop-codex-stop-hook.sh`: when Codex review ends with `COMPLETE`, the loop renames `state.md` to `finalize-state.md` and returns a block prompt for Finalize Phase (`hooks/loop-codex-stop-hook.sh:961-1020`); when Finalize Phase checks pass, the stop hook renames that state to `complete-state.md` (`651-656`).
- gates_or_invariants: The invariant is that `finalize-state.md` is system-owned and not an allowed Claude Write/Edit/Bash target. The only user-write artifact named by the template is `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:8`), matching the write validator’s special allow path at `hooks/loop-write-validator.sh:116-126`. This preserves the state-machine boundary between Finalize Phase control state and Claude’s final evidence/summary output.
- dependencies_and_callers: Loaded by `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:219-226`. That helper is used by `hooks/loop-write-validator.sh:105-107`, `hooks/loop-edit-validator.sh:86-88`, and `hooks/loop-bash-validator.sh:98-104`, `245-252`, `268-276`. It depends on `hooks/lib/template-loader.sh`, where `get_template_dir` resolves `prompt-template` from the plugin root (`24-31`) and `load_and_render_safe` falls back to an inline message if the template is missing or empty (`167-185`).
- edge_cases_or_failure_modes: If this template is missing, the shared helper falls back to a shorter inline block message (`hooks/lib/loop-common.sh:221-225`; safe fallback behavior at `hooks/lib/template-loader.sh:176-185`), so enforcement still works but guidance loses the explicit numbered Finalize Phase actions. Because the file has no variables, placeholder substitution cannot corrupt it. The template’s instructions assume Finalize Phase is active; callers also use it for any detected modification of a file named `finalize-state.md`, including Bash move/copy source protection, so it may appear during bypass attempts rather than normal Write/Edit operations.
- validation_or_tests: Template existence/load coverage is included in `tests/test-template-references.sh:52-105`, which scans shell script template references, and `tests/test-templates-comprehensive.sh:92-107`, which loads every Markdown template and warns/fails on empty loads. Syntax/render checks for all templates are in `tests/test-templates-comprehensive.sh:116-125` and `566-602`. Behavioral coverage for this exact template path is indirect through finalize tests: `tests/test-finalize-phase.sh:211-216` checks the helper exists, `293-303` checks Write blocks `finalize-state.md`, `305-315` checks Edit blocks it, and `317-350` checks Bash modification/source bypasses are blocked with finalize-related output.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-088 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Claude next-round prompt fragment appended immediately after a Full Goal Alignment Check. It nudges the worker side of the RLCR loop to convert alignment findings into concrete next-round work.
- algorithmic_behavior: The template adds a “Post-Alignment Check Action Items” section (`prompt-template/claude/post-alignment-action-items.md:2`). It instructs Claude to pay special attention to forgotten items, acceptance criteria marked NOT MET, and unjustified deferrals (`4-7`). The stop hook decides when to append it: Full Alignment Check rounds are those where `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:683-687`). If Codex review does not complete or stop the loop, the stop hook increments `current_round`, renders the next-round base prompt, then loads this template and appends it only for that post-alignment case (`1053-1086`).
- inputs_outputs_state: The template has no variables; input is only the file content loaded from `prompt-template/claude/post-alignment-action-items.md`. Output is appended text in `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:1062-1085`). It participates in state progression after a review-found-issues branch: `current_round` is updated in the state file (`1057-1060`), a new prompt file is created (`1062-1078`), this post-alignment fragment may be appended (`1080-1086`), then footer/push/goal-tracker request sections are appended (`1088-1108`).
- gates_or_invariants: It is gated by the previous round being a Full Alignment Check, not by template content. The condition is deterministic and round-based: every fifth round with zero-based index ending in 4 (`683-687`, `1080-1086`). It is skipped if the template load returns empty (`1082-1085`), so a missing template degrades by omission rather than blocking. It only applies when the loop continues after review issues; if Codex review ends with `COMPLETE`, the hook enters Finalize Phase instead (`961-1020`), and if it ends with `STOP`, it terminates the loop (`1023-1051`).
- dependencies_and_callers: Loaded by `load_template` in `hooks/loop-codex-stop-hook.sh:1082`. It is adjacent to the base next-round prompt template rendered at `1075-1078` from `prompt-template/claude/next-round-prompt.md`, whose content already requires reading the original plan, creating todos for all discovered issues, reviewing Codex feedback, and treating `goal-tracker.md` as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1-31`). It is conceptually paired with the Full Alignment review prompt built earlier in the stop hook (`hooks/loop-codex-stop-hook.sh:718-731`), which generates the review content that this post-alignment fragment asks Claude to act on.
- edge_cases_or_failure_modes: Because the template has no placeholders and is loaded with `load_template`, a missing file produces an empty string plus warning, and the stop hook simply does not append it (`hooks/loop-codex-stop-hook.sh:1082-1085`; loader behavior at `hooks/lib/template-loader.sh:33-47`). That means the loop continues but loses targeted post-alignment remediation guidance. The template relies on Codex review wording such as forgotten items, NOT MET acceptance criteria, and unjustified deferrals; if the review uses different labels, Claude still receives the base review content but this fragment may be less directly actionable. Since it appends only after review-found-issues flow, it does not affect Finalize Phase or STOP termination branches.
- validation_or_tests: Template-wide load/syntax/render coverage applies through `tests/test-templates-comprehensive.sh:92-125` and `566-602`. Reference coverage is included in `tests/test-template-references.sh:52-105`, which scans hook scripts for template references. The conditional append path is visible in `hooks/loop-codex-stop-hook.sh:1080-1086`; I found no narrow test that asserts this specific fragment appears in a generated next-round prompt after a Full Alignment Check.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections, matching the 3 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`