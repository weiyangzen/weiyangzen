# agent_07 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-007 `directory` `skills`
- cursor: `[_]`
- core_role: The `skills` directory is the plugin’s skill entrypoint layer. It contains three recursive descendants only: `skills/gen-plan/SKILL.md`, `skills/start-rlcr-loop/SKILL.md`, and `skills/cancel-rlcr-loop/SKILL.md`. These files move algorithmic command instructions out of thin slash-command wrappers and into discoverable Skill definitions. The command wrappers delegate through `allowed-tools: ["Skill(humanize:...:*)"]` in [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/commands/gen-plan.md:4), [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/commands/start-rlcr-loop.md:4), and [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/commands/cancel-rlcr-loop.md:3).
- algorithmic_behavior: `skills/gen-plan/SKILL.md` defines a six-phase draft-to-plan algorithm: validate IO, check repository relevance, analyze draft quality, ask clarifying questions, generate a structured plan, then write/report output. Its IO validation phase delegates to `scripts/validate-gen-plan-io.sh` and maps exit codes 0-6 to stop/continue behavior at [skills/gen-plan/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/skills/gen-plan/SKILL.md:29). Its relevance phase invokes the `draft-relevance-checker` subagent and accepts only `RELEVANT` verdicts before continuing at [skills/gen-plan/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/skills/gen-plan/SKILL.md:48). Its generation phase requires English-only output, AC-X/AC-X.Y criteria, positive and negative tests, path boundaries, feasibility hints, and dependency sequence at [skills/gen-plan/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/skills/gen-plan/SKILL.md:123). `skills/start-rlcr-loop/SKILL.md` delegates loop initialization to `scripts/setup-rlcr-loop.sh`, then describes the iterative Claude/Codex review state machine and goal-tracker contract at [skills/start-rlcr-loop/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/skills/start-rlcr-loop/SKILL.md:11). `skills/cancel-rlcr-loop/SKILL.md` defines a cancellation algorithm over newest `.humanize/rlcr/*/` loop directory, with different transitions for `state.md` and `finalize-state.md` at [skills/cancel-rlcr-loop/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/skills/cancel-rlcr-loop/SKILL.md:20).
- inputs_outputs_state: `gen-plan` input is `$ARGUMENTS` containing `--input <draft>` and `--output <plan>`; output is a new plan file plus a summary of path, contents, and AC count. Its validator requires input exists and is nonempty, output directory exists, output path does not already exist, and output directory is writable, with machine-readable exit codes documented in [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/scripts/validate-gen-plan-io.sh:4). `start-rlcr-loop` input is a plan path plus options such as `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, and `--track-plan-file`; it creates `.humanize/rlcr/<timestamp>/state.md`, copies `plan.md`, creates `goal-tracker.md`, and writes `round-0-prompt.md`/`round-0-summary.md` paths, as shown in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/scripts/setup-rlcr-loop.sh:470). `cancel-rlcr-loop` input is the current filesystem state; outputs are `.cancel-requested` plus either `cancel-state.md` from `state.md` or `finalize-state.md` after confirmation.
- gates_or_invariants: The skill layer enforces tool boundaries through `allowed-tools`: `gen-plan` permits the validator script plus Read/Glob/Grep/Task/Write/AskUserQuestion, while `start-rlcr-loop` permits only its setup script and `cancel-rlcr-loop` permits only loop inspection, signal creation, state renames, Read, and AskUserQuestion. `gen-plan` must not overwrite an existing plan, must stop on irrelevant drafts, must use AC-X format, must include both positive and negative tests, must avoid time estimates and line-number references, and must write English only. `start-rlcr-loop` requires a git repo with at least one commit, a relative non-symlink plan path inside the project, no spaces or shell metacharacters, a sufficiently detailed plan, Codex CLI availability, YAML-safe branch/model/effort names, and either gitignored plan file or clean tracked plan when `--track-plan-file` is set, enforced in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/scripts/setup-rlcr-loop.sh:211) and [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/scripts/setup-rlcr-loop.sh:227). `cancel-rlcr-loop` invariant: newest timestamped loop is the only active candidate; cancellation preserves state by rename, not deletion.
- dependencies_and_callers: Slash commands call skills, not scripts directly. `gen-plan` depends on `scripts/validate-gen-plan-io.sh` and `agents/draft-relevance-checker.md`; the subagent returns strict `RELEVANT:` or `NOT_RELEVANT:` verdicts at [agents/draft-relevance-checker.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/agents/draft-relevance-checker.md:27). `start-rlcr-loop` depends on `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, git, Codex CLI, and later hook behavior under `hooks/`. `cancel-rlcr-loop` coordinates with `hooks/lib/loop-common.sh` cancellation authorization: Bash state-file mutation is only allowed when a cancel signal exists, referenced by the validator at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-bash-validator.sh:98).
- edge_cases_or_failure_modes: `gen-plan` fails closed for missing/empty input, missing output directory, existing output path including directories, no output write permission, invalid/missing flags, and irrelevant drafts. Its content generation can still depend on model judgment after validation. `start-rlcr-loop` rejects absolute paths, paths escaping project root, symlinks, submodule paths, tracked plans without `--track-plan-file`, untracked plans with `--track-plan-file`, plans under 5 total lines or fewer than 3 non-comment content lines, unavailable Codex, and YAML-unsafe branch/model values. `cancel-rlcr-loop` can report no active loop if newest loop directory lacks `state.md`/`finalize-state.md`; finalize-phase cancellation intentionally requires user confirmation.
- validation_or_tests: Skill structure and delegation are covered by tests in [tests/test-skill-structure.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-skill-structure.sh:70), including skill names, descriptions, model fields, allowed tools, and command-to-skill delegation. `start-rlcr-loop` invocation/delegation and setup script checks are covered in [tests/test-skill-invocation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-skill-invocation.sh:58) and [tests/test-skill-invocation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-skill-invocation.sh:233). `validate-gen-plan-io.sh` exit codes are executable-spec tested in [tests/test-skill-structure.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-skill-structure.sh:682). README documents `gen-plan` as a five-step workflow at [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/README.md:103).
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-037 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the `command_modifies_file` detection helper used by the RLCR Bash PreToolUse hook. It defines which shell command shapes count as protected-file writes and which are allowed read-only operations. The test imports the real helper from `hooks/lib/loop-common.sh` at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-bash-validator-patterns.sh:11), so it validates the production regex contract rather than a duplicate implementation.
- algorithmic_behavior: The test lowercases each candidate command with `to_lower`, calls `command_modifies_file "$command_lower" "$pattern"`, and increments pass/fail counters through `assert_modifies` and `assert_not_modifies` at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-bash-validator-patterns.sh:36). Test groups cover redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec`, false positives, edge spacing, state-file patterns, and summary-file regex patterns. The production helper checks an array of ERE patterns for destination redirection, append redirection, `tee -a`, in-place editors, two-argument copy/move, removal, `dd of=`, truncate, printf redirection, and file-descriptor `exec` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/lib/loop-common.sh:574).
- inputs_outputs_state: Inputs are literal command strings plus a file regex pattern such as `goal-tracker\.md`, `state\.md`, or `round-[0-9]+-summary\.md`. Output is the script exit code: `0` if every assertion matched expected behavior, `1` if any assertion failed. Internal state is only `TESTS_PASSED`/`TESTS_FAILED`; it does not create repository state. The helper itself returns shell status `0` for “modifies” and `1` for “does not modify.”
- gates_or_invariants: The invariant under test is that Bash write bypasses to protected RLCR files must be detected consistently while read-only commands are not blocked. The test asserts writes through `>`, `>>`, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, destination `mv`/`cp`, `rm`, `dd of=`, `truncate`, and `exec 3>` are positive cases. It asserts `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo <filename>`, `ls`, `file`, `stat`, and `diff` are false-positive guards at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-bash-validator-patterns.sh:136). These patterns feed hook gates that block `state.md`, `finalize-state.md`, plan backup, `goal-tracker.md`, prompt, summary, and todos file Bash writes in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-bash-validator.sh:87) and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-bash-validator.sh:302).
- dependencies_and_callers: The file depends on Bash, `hooks/lib/loop-common.sh`, `to_lower`, and `command_modifies_file`. Its downstream caller is `hooks/loop-bash-validator.sh`, which reads JSON hook input with `jq`, finds the active loop, parses `state.md`, and invokes `command_modifies_file` for protected write classes. The test is likely run by the project’s test suite through shell scripts, but this file is also directly executable because it has a shebang and self-contained summary.
- edge_cases_or_failure_modes: The test documents a known limitation: `cp file1.md file2.md goal-tracker.md` is not detected because the production pattern expects `cp src dest` format, noted at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-bash-validator-patterns.sh:165). Literal variable targets such as `echo x > $FILE` are intentionally not detected. Matching is regex/string based, so complex quoting, multi-source copy, shell expansion, command substitution, or obfuscated writes may evade this helper unless separately handled by `loop-bash-validator.sh`’s additional state-source and wrapper checks. Conversely, matching any path suffix with the protected filename can overblock commands whose destination merely contains that basename.
- validation_or_tests: The file is itself the validation artifact. It validates production regexes for `goal-tracker.md`, `state.md`, and `round-N-summary.md` patterns, then prints pass/fail totals and exits nonzero on failures at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-bash-validator-patterns.sh:197). The broader hook uses the same helper for Bash enforcement, including Round 0 goal-tracker Bash write guidance and post-Round 0 update-request enforcement at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-bash-validator.sh:308).
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-067 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: This template is a Stop-hook blocking message for the Round 0 goal-tracker initialization gate. It tells the agent that the immutable goal-tracker fields are still placeholders and that exit is blocked until the goal, acceptance criteria, and active tasks are initialized. The file is directly rendered by `hooks/loop-codex-stop-hook.sh` when placeholder content remains in `goal-tracker.md`.
- algorithmic_behavior: The hook computes `GOAL_TRACKER_FILE="$LOOP_DIR/goal-tracker.md"` and only runs this gate when not in finalize phase, `CURRENT_ROUND` is 0, and the file exists at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-codex-stop-hook.sh:560). It extracts three sections with `awk`: `### Ultimate Goal`, `### Acceptance Criteria`, and `#### Active Tasks`. Each section is checked for a generic placeholder pattern `\[To be [a-z]`; missing-section messages are assembled into `MISSING_ITEMS`; then `load_and_render_safe` renders this template with `GOAL_TRACKER_FILE` and `MISSING_ITEMS` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/hooks/loop-codex-stop-hook.sh:596). The rendered message is returned in a JSON block decision with system message `Loop: Goal Tracker not initialized in Round 0`.
- inputs_outputs_state: Inputs are template variables `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, plus the current loop state from `state.md`/`finalize-state.md`. Output is human-facing Markdown embedded in hook JSON. The required state transition is external: the agent must read the goal tracker, replace placeholder text with real Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to ACs, then write `goal-tracker.md`. The template itself does not mutate state, but it gates exit until the file state changes.
- gates_or_invariants: The main invariant is that the immutable section can only be set in Round 0; after Round 0 it is read-only. The template states this explicitly at [prompt-template/block/goal-tracker-not-initialized.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/prompt-template/block/goal-tracker-not-initialized.md:16). This pairs with setup-time placeholder generation in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/scripts/setup-rlcr-loop.sh:507) and the initial Round 0 prompt requiring goal-tracker initialization before implementation at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/scripts/setup-rlcr-loop.sh:605). It also pairs with Bash write-blocking: Round 0 Bash writes to `goal-tracker.md` are blocked so updates go through safer Write/Edit paths, while later rounds require a summary-based update request.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe`; fallback text exists inline if template rendering fails. It depends on `hooks/lib/template-loader.sh` semantics for replacing `{{...}}` placeholders, and on `setup-rlcr-loop.sh` creating predictable headings and placeholder text. Related Codex review templates also use `GOAL_TRACKER_FILE` for regular and full alignment reviews, but this specific block is only the Round 0 uninitialized-exit gate.
- edge_cases_or_failure_modes: Because the hook detects placeholders with a generic `"[To be "` regex only inside extracted sections, a user could leave incomplete non-placeholder content and pass this specific gate. If headings are renamed or malformed, extraction may miss content and also miss placeholders. If setup successfully extracts a goal or AC section from the plan, those sections may not contain placeholders, so the gate can focus only on remaining placeholder sections. The gate is skipped in finalize phase and for rounds other than 0, so later drift is handled by other review/alignment mechanisms, not this template.
- validation_or_tests: Template existence and rendering are covered by comprehensive template tests referenced for `GOAL_TRACKER_FILE`, including [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-templates-comprehensive.sh:533), and template-loader tests cover placeholder substitution behavior around `GOAL_TRACKER_FILE` in [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-gen-plan-command/tests/test-template-loader.sh:530). The hook-side behavior is specified by the line-level implementation in `loop-codex-stop-hook.sh`; I did not execute the test suite because this task requested read-only research notes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections present, matching the assigned table order exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`