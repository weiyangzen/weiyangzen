# agent_01 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`
- inspection_note: recursive filesystem inventory found 78 files across the export; Git metadata was unavailable in this read-only branch export, so evidence is based on `rg --files`, `find`, `wc`, and line-numbered source reads, not repository history.

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is a Claude Code plugin named Humanize, versioned in `.claude-plugin/plugin.json:1`, whose core algorithms are two command workflows: RLCR, an iterative Claude implementation loop gated by Codex review, and `gen-plan`, a draft-to-plan generation workflow. `README.md:58` describes the RLCR flow as plan -> Claude implementation/summary -> Codex review -> feedback or done.
- algorithmic_behavior: Root coordination is plugin-driven. `commands/*.md` are thin slash-command wrappers delegating to skills, for example `commands/start-rlcr-loop.md:10` and `commands/gen-plan.md:10`. `skills/start-rlcr-loop/SKILL.md:11` delegates setup to `scripts/setup-rlcr-loop.sh`, while `skills/gen-plan/SKILL.md:19` defines a six-phase prompt algorithm: IO validation, repository relevance, draft analysis, issue resolution, plan generation, and write/complete. Runtime enforcement is hook-driven through `hooks/hooks.json:14`, which registers pre-tool validators for Write/Edit/Read/Bash and a Stop hook at `hooks/hooks.json:52`.
- inputs_outputs_state: Inputs include user plan markdown, draft markdown for `gen-plan`, Claude hook JSON, transcript paths, Git status, Codex CLI output, and template variables. RLCR outputs `.humanize/rlcr/<timestamp>/` state, backup `plan.md`, `goal-tracker.md`, round prompts, summaries, review prompts/results, and terminal state files. `setup-rlcr-loop.sh:485` creates YAML-frontmatter `state.md`; `loop-codex-stop-hook.sh:1057` advances rounds by rewriting `current_round`; `loop-codex-stop-hook.sh:980` transitions to `finalize-state.md` after exact terminal `COMPLETE`; `loop-common.sh:622` terminally renames state files to reason-specific names.
- gates_or_invariants: Root invariants include exactly one active newest loop directory via `loop-common.sh:63`, protected state/finalize files, plan backup immutability, branch consistency, plan-file integrity, TodoWrite completion before Stop review, git clean state, optional push requirements, large-file detection, and post-Round 0 goal tracker write restrictions. `prompt-template/block/*` supplies user-facing gate text, `prompt-template/codex/*` supplies Codex review contracts, and `prompt-template/claude/*` supplies next-round/finalize instructions.
- dependencies_and_callers: External dependencies are Claude Code plugin infrastructure, Bash/zsh-compatible shell scripts, `git`, `jq`, `python3`, `codex`, `awk/sed/grep`, and optional `timeout`/`gtimeout` through `scripts/portable-timeout.sh`. `hooks/lib/template-loader.sh:58` is the shared prompt renderer; `hooks/lib/loop-common.sh:46` sources it for validators and Stop logic. `agents/draft-relevance-checker.md:27` is used by `gen-plan` to classify drafts as `RELEVANT` or `NOT_RELEVANT`.
- edge_cases_or_failure_modes: Covered cases include missing Git repo or initial commit, plan path escaping, symlinks, spaces/metacharacters, tracked-vs-gitignored plan policy, outdated state schema, switched branch, missing plan backup, modified plan content, incomplete TodoWrite items, missing summary files, Codex absence/failure/empty output, same-second loop timestamp collision risk, and missing templates falling back to inline messages. `validate-gen-plan-io.sh:68` uses `realpath -m` when available but falls back to raw paths, so portability depends on shell environment.
- validation_or_tests: `tests/run-all-tests.sh:32` lists 20 suites covering template loading, validators, todo parsing, plan-file validation, finalize phase, cancellation, monitors, skill structure, hook preservation, and invocation. `tests/test-plan-file-validation.sh:1` directly targets setup path validation; `tests/test-template-references.sh:83` scans shell template references for missing files; `tests/test-skill-structure.sh:682` validates `gen-plan` IO script exit codes. Tests were inspected but not executed in this research pass.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-031 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script for `/humanize:start-rlcr-loop`; it validates a single implementation plan, records loop configuration, seeds goal tracking, creates the first Claude prompt, and hands control to Claude plus Stop-hook review. Defaults are declared at `scripts/setup-rlcr-loop.sh:17`, including Codex model, effort, timeout, max iterations, and Git timeout.
- algorithmic_behavior: Parses positional plan and options from `scripts/setup-rlcr-loop.sh:104`, including `--plan-file`, `--track-plan-file`, `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round`. It rejects missing or duplicate plan sources at `scripts/setup-rlcr-loop.sh:188`, validates Git repo and commit presence at `scripts/setup-rlcr-loop.sh:211`, validates path safety at `scripts/setup-rlcr-loop.sh:227`, validates plan tracking policy at `scripts/setup-rlcr-loop.sh:311`, validates plan content at `scripts/setup-rlcr-loop.sh:362`, and validates Codex availability at `scripts/setup-rlcr-loop.sh:417`.
- inputs_outputs_state: Inputs are CLI arguments, `CLAUDE_PROJECT_DIR` or current directory, the plan file, Git repository state, branch name, and Codex availability. Outputs are `.humanize/rlcr/<timestamp>/plan.md` backup at `scripts/setup-rlcr-loop.sh:475`, `state.md` frontmatter at `scripts/setup-rlcr-loop.sh:485`, `goal-tracker.md` at `scripts/setup-rlcr-loop.sh:504`, and `round-0-prompt.md` plus activation text at `scripts/setup-rlcr-loop.sh:602`.
- gates_or_invariants: Enforces relative-only plan paths, no spaces, no shell metacharacters, no symlink plan file, existing readable file inside project, no submodule path, and stable tracking mode. Without `--track-plan-file`, the plan must be untracked/gitignored; with it, the plan must be tracked and clean. It records YAML-safe branch/model/effort values at `scripts/setup-rlcr-loop.sh:435` and requires at least five total lines plus at least three non-comment content lines at `scripts/setup-rlcr-loop.sh:362`.
- dependencies_and_callers: Called by `skills/start-rlcr-loop/SKILL.md:14`, reached through `commands/start-rlcr-loop.md:10`. It sources `scripts/portable-timeout.sh:28`, shells out to Git, `wc`, `grep`, `sed`, `awk`, `date`, `dirname`, `basename`, `cp`, and requires `codex` in PATH. Later hooks consume the state fields it writes through `hooks/lib/loop-common.sh:112`.
- edge_cases_or_failure_modes: Fails closed on Git timeouts for status/tracking checks, rejects inaccessible parent directories, treats Markdown headings beginning with `#` as comments for content counting, rejects YAML-unsafe branch names when Git allows them, and falls back to placeholder goal/acceptance criteria when heuristic extraction fails. A same-second repeated start can target the same timestamp directory because `TIMESTAMP` only has second precision at `scripts/setup-rlcr-loop.sh:470`.
- validation_or_tests: `tests/test-plan-file-validation.sh:89` through later sections covers absolute paths, missing files, parent directory errors, spaces, shell metacharacters, symlinks, path escapes, non-Git repos, no-commit repos, tracking policy, branch safety, content validation, `--plan-file`, duplicate plan source rejection, and Codex parameter validation. `tests/test-skill-invocation.sh:233` checks the setup script exists, is executable, has valid Bash syntax, and `--help` works.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-061 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Advisory block template appended to the Stop-hook git-clean gate when non-Humanize untracked files are present. It explains that untracked files may be build artifacts, outputs, dependencies, editor files, logs, caches, or temporary files and should often be ignored rather than committed.
- algorithmic_behavior: The template itself has no variables or executable behavior. It is loaded at `hooks/loop-codex-stop-hook.sh:453` only after cached `git status --porcelain` contains `??` entries not matching `.humanize`, then concatenated into `SPECIAL_NOTES` and rendered into the broader `block/git-not-clean.md` failure response at `hooks/loop-codex-stop-hook.sh:470`.
- inputs_outputs_state: Input is the Stop hook’s classification of untracked files, not the template body. Output is Markdown text beginning at `prompt-template/block/git-not-clean-untracked.md:2` and ending with an instruction to review untracked files and add appropriate `.gitignore` patterns at `prompt-template/block/git-not-clean-untracked.md:10`. It does not mutate state.
- gates_or_invariants: Does not create the block condition by itself; the invariant is enforced by the Stop hook’s git-clean check at `hooks/loop-codex-stop-hook.sh:430`. The template refines the remediation path so Claude does not blindly commit generated artifacts.
- dependencies_and_callers: Depends on `load_template` from `hooks/lib/template-loader.sh:36`; caller supplies a fallback string at `hooks/loop-codex-stop-hook.sh:455` if the template is missing. It coordinates with `.gitignore:4`, which ignores `.humanize*`, and with `prompt-template/block/git-not-clean-humanize-local.md` for Humanize-local untracked state.
- edge_cases_or_failure_modes: Since it is generic and contains no actual untracked path list, the user still needs Git status details to decide whether to ignore or commit files. If both Humanize-local and other untracked notes are present, they are string-concatenated; the leading blank line in this template helps preserve readability.
- validation_or_tests: `tests/test-template-references.sh:83` scans template references in shell scripts and verifies the referenced file exists, which covers this Stop-hook reference. The broader git-clean path is exercised by Stop-hook and template test suites, but this template does not appear to have a dedicated content-render assertion.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-091 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt subtemplate that defines how post-Round 0 Goal Tracker changes are mediated. It makes Codex responsible for evaluating “Goal Tracker Update Request” sections in Claude summaries because Claude is blocked from direct modification after Round 0.
- algorithmic_behavior: Loaded and rendered by `hooks/loop-codex-stop-hook.sh:677` with `GOAL_TRACKER_FILE`, then injected into both full-alignment and regular Codex review prompts at `hooks/loop-codex-stop-hook.sh:720` and `hooks/loop-codex-stop-hook.sh:736`. It tells Codex to evaluate justification, update the tracker if approved, reject with reasons if not, and handle task completion, new issues, plan changes, and deferrals.
- inputs_outputs_state: Input is the rendered `{{GOAL_TRACKER_FILE}}` placeholder at `prompt-template/codex/goal-tracker-update-section.md:6` plus Claude’s summary content in the parent review prompt. Output is instructions embedded into `round-N-review-prompt.md`; approved actions may update `goal-tracker.md` by moving tasks, adding Plan Evolution Log entries, adding Open Issues, or adding Explicitly Deferred items.
- gates_or_invariants: Preserves the immutable Ultimate Goal and Acceptance Criteria by explicit instruction at `prompt-template/codex/goal-tracker-update-section.md:10`. Requires justification before approval at `prompt-template/codex/goal-tracker-update-section.md:5`, rejection rationale at `prompt-template/codex/goal-tracker-update-section.md:11`, and strong justification for deferrals at `prompt-template/codex/goal-tracker-update-section.md:17`. This is primarily a prompt-level contract; Claude-side Write/Edit/Bash validators enforce post-Round 0 write blocking, but Codex’s own adherence is not separately diff-validated by a shell gate.
- dependencies_and_callers: Depends on `load_and_render_safe` and single-pass placeholder rendering from `hooks/lib/template-loader.sh:170`. The Stop hook has a fallback section at `hooks/loop-codex-stop-hook.sh:678` if the template is missing. The section complements `prompt-template/claude/goal-tracker-update-request.md`, which instructs Claude to request rather than directly apply post-Round 0 tracker changes.
- edge_cases_or_failure_modes: If `GOAL_TRACKER_FILE` is missing from render inputs, the placeholder remains literal because the renderer preserves missing placeholders. If Claude’s summary asks for changes that conflict with the immutable section, only the prompt instruction says to refuse; there is no programmatic immutable-section checksum.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531` directly renders this real template and asserts it contains “Goal Tracker Update Requests” plus the supplied goal tracker path. Template loading behavior is also covered by `tests/test-template-loader.sh` and missing-reference scanning by `tests/test-template-references.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item sections above, matching the 4 assigned rows
- missing_items: none
- duplicate_items: none; each assigned identifier is used only in its item heading
- final_worker_status: `complete`