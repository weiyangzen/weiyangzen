# agent_06 add-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `ac298f55075b28c65b2e76f8f73831cf6fd5be2f`

## Item Evidence

### ADD_GEN_PLAN_COMMAND-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is a flat executable helper layer for Humanize commands and hooks. It contains the direct gen-plan IO gate, RLCR loop bootstrapper, portable timeout abstraction, and RLCR monitor UI. Directory contents inspected recursively: `scripts/humanize.sh`, `scripts/portable-timeout.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`.
- algorithmic_behavior: `validate-gen-plan-io.sh` is the branch’s gen-plan preflight gate: parse `--input/--output`, normalize paths with `realpath -m`, validate input existence/non-empty state, output parent existence, output nonexistence, and output-dir writability, then emit typed validation messages and exit codes 0-6 (`scripts/validate-gen-plan-io.sh`:15-23, 28-55, 68-130). `setup-rlcr-loop.sh` bootstraps an RLCR state machine from a plan file: parse options, validate git repo/commit, constrain plan paths, validate tracked-or-ignored plan policy, require substantive content, validate Codex model/effort strings, create `.humanize/rlcr/<timestamp>/state.md`, copy the plan, create `goal-tracker.md`, and emit round-0 prompt plus completion requirements (`scripts/setup-rlcr-loop.sh`:104-180, 211-356, 362-423, 429-498, 504-653, 667-720). `portable-timeout.sh` chooses `gtimeout`, GNU `timeout`, Python subprocess timeout, or no-timeout fallback and exposes `run_with_timeout` (`scripts/portable-timeout.sh`:10-29, 33-71). `humanize.sh` implements `humanize monitor rlcr-loop`, discovering latest RLCR session and Codex log, parsing state/goal/git status, rendering a fixed terminal status bar, following log growth, and switching on newer/deleted/truncated sessions/logs (`scripts/humanize.sh`:9-109, 152-314, 316-453, 547-799, 805-838).
- inputs_outputs_state: Inputs include command arguments, repo cwd/`CLAUDE_PROJECT_DIR`, plan/draft paths, git state, `codex` availability, `.humanize/rlcr` state directories, cache logs under `$HOME/.cache/humanize`, and terminal dimensions. Outputs include validation status text and exit codes, generated `.humanize/rlcr/<timestamp>` files, `state.md` fields (`current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, `started_at`), `goal-tracker.md`, `round-0-prompt.md`, and monitor terminal output.
- gates_or_invariants: Gen-plan validation uses stable exit codes: 1 input missing, 2 empty input, 3 missing output dir, 4 output exists/is directory, 5 no write permission, 6 invalid args (`scripts/validate-gen-plan-io.sh`:4-11). RLCR setup rejects absolute plan paths, paths with spaces, shell metacharacters, symlinks, missing/unreadable files, path escape outside project, submodule paths, tracked plans unless `--track-plan-file`, untracked/dirty tracked plans when `--track-plan-file`, plans under 5 lines, plans with fewer than 3 content lines, missing Codex CLI, YAML-unsafe branch/model/effort values (`scripts/setup-rlcr-loop.sh`:227-305, 311-356, 362-461). Timeout behavior preserves GNU timeout exit 124 for Python timeout expiration (`scripts/portable-timeout.sh`:53-58). Monitor avoids zsh glob failures by using `find`, handles missing loop/log/session states, restores terminal scroll region on cleanup, and treats no-git as a nonfatal display state (`scripts/humanize.sh`:10-12, 27-49, 51-109, 254-300, 465-497).
- dependencies_and_callers: `/humanize:gen-plan` delegates to `skills/gen-plan/SKILL.md`, whose allowed tools include `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh:*)` and whose Phase 1 requires executing that script (`commands/gen-plan.md`:1-19; `skills/gen-plan/SKILL.md`:1-13, 29-45). `/humanize:start-rlcr-loop` delegates to `skills/start-rlcr-loop/SKILL.md`, whose allowed tool is `setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md`:1-19; `skills/start-rlcr-loop/SKILL.md`:1-15). `portable-timeout.sh` is sourced by `setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/loop-plan-file-validator.sh` (`scripts/setup-rlcr-loop.sh`:26-29; `hooks/loop-codex-stop-hook.sh`:46-52, 793-811; `hooks/loop-plan-file-validator.sh`:13-24). `humanize.sh` is covered by monitor tests and intended to be sourced before `humanize monitor rlcr-loop`.
- edge_cases_or_failure_modes: `validate-gen-plan-io.sh` relies on `realpath -m`; if unavailable it falls back to original path, so behavior is path-string dependent but guarded by file checks (`scripts/validate-gen-plan-io.sh`:68-71). `setup-rlcr-loop.sh --help` could not execute in this read-only sandbox because Bash was denied temp-file creation for a here-doc, but `bash -n` passed. Git status inspection was also environment-limited because this branch export lacks usable `.git` metadata and macOS temp/cache writes were denied. Monitor has explicit handling for missing `.humanize/rlcr`, no sessions, no logs, deleted sessions/logs, and truncated/rotated logs (`scripts/humanize.sh`:20-25, 521-525, 568-667, 704-714, 724-758).
- validation_or_tests: Ran `bash -n scripts/humanize.sh scripts/portable-timeout.sh scripts/setup-rlcr-loop.sh scripts/validate-gen-plan-io.sh` successfully. Ran `bash tests/test-ansi-parsing.sh`: 8 passed, 0 failed. Ran `scripts/validate-gen-plan-io.sh --help; printf 'exit=%s\n' $?`: printed usage and documented exit `6`. `tests/test-skill-structure.sh` exercises `validate-gen-plan-io.sh` exit codes 1-6 and success (`tests/test-skill-structure.sh`:681-798). `tests/test-skill-invocation.sh` checks `setup-rlcr-loop.sh` executable state, syntax, help behavior, and command/skill delegation (`tests/test-skill-invocation.sh`:125-143, 173-221, 233-270, 327-340).
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-036 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the aggregate test runner’s ANSI-stripping and pass/fail count extraction algorithm. It validates the parsing logic used by `tests/run-all-tests.sh` before totals are accumulated.
- algorithmic_behavior: Defines colored pass/fail helpers and eight cases. The core transform is `esc=$'\033'` followed by `sed "s/${esc}\\[[0-9;]*m//g"` to remove SGR color sequences, then `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'` or the analogous `Failed` expression to extract counters (`tests/test-ansi-parsing.sh`:38-48, 70-90). It verifies basic color stripping, multiple colored spans, colored passed count, colored failed count, zero failed count, multi-line suite output using `tail -1`, plain text, and bold+color sequences (`tests/test-ansi-parsing.sh`:42-158).
- inputs_outputs_state: Inputs are synthetic strings containing plain text and ANSI SGR escape sequences. Outputs are printed `PASS`/`FAIL` lines and final `Passed:`/`Failed:` counts derived from `TESTS_PASSED` and `TESTS_FAILED` (`tests/test-ansi-parsing.sh`:13-31, 163-178). State is only in the two shell counters.
- gates_or_invariants: The parser must be portable across GNU/Linux and BSD/macOS sed by using ANSI-C quoting for ESC rather than GNU-only syntax (`tests/test-ansi-parsing.sh`:5-6, 38-40). It must preserve non-ANSI plain text, handle multiple SGR spans, bold/color combinations, zero counts, and select the last summary count in multi-line output (`tests/test-ansi-parsing.sh`:116-125, 137-153).
- dependencies_and_callers: The file is included in the aggregate suite list as `test-ansi-parsing.sh` (`tests/run-all-tests.sh`:32-54). It specifies the exact logic that the aggregate runner applies after each suite: strip ANSI, extract latest `Passed` and `Failed`, accumulate totals, and classify suite failure by exit code or failed count (`tests/run-all-tests.sh`:85-118).
- edge_cases_or_failure_modes: It covers only SGR sequences ending in `m` with numeric/semicolon parameters; cursor movement, OSC, erase-line, and other CSI forms are outside this contract. Count extraction depends on labels exactly matching `Passed:` and `Failed:` with numeric suffixes. If a suite emits unrelated matching labels later, `tail -1` will select that later occurrence.
- validation_or_tests: Ran `bash tests/test-ansi-parsing.sh`: all eight cases passed and final summary reported `Passed: 8`, `Failed: 0`.
- skip_candidate: `no`

### ADD_GEN_PLAN_COMMAND-HZ-066 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Prompt block template for the RLCR guardrail that forbids direct `goal-tracker.md` mutation after Round 0 and redirects state changes into a reviewable “Goal Tracker Update Request” in the current round summary.
- algorithmic_behavior: The template renders a round-specific block with `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`, states that after Round 0 only Codex can modify the Goal Tracker, forbids direct Write/Edit/Bash modification of `goal-tracker.md`, and defines the required markdown request section with requested changes and justification (`prompt-template/block/goal-tracker-modification.md`:1-25).
- inputs_outputs_state: Inputs are template variables from `goal_tracker_blocked_message "$current_round" "$summary_file"` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh`:601-613). Output is a rendered block message used by validators when they block prohibited edits; it does not mutate state itself.
- gates_or_invariants: The invariant is ownership separation: Round 0 may initialize the goal tracker, later rounds must not directly modify it; requests must be placed in `{{SUMMARY_FILE}}` for Codex review. Rendering uses `load_and_render_safe`, so if the template is missing or empty the fallback still blocks and includes round/summary context (`hooks/lib/loop-common.sh`:603-613; `hooks/lib/template-loader.sh`:167-203).
- dependencies_and_callers: The template is referenced by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh`:610-612). That message is used by edit/write validators for after-Round-0 direct goal tracker edits and by the bash validator for shell attempts to modify `goal-tracker.md` (`hooks/loop-edit-validator.sh`:111-116; `hooks/loop-write-validator.sh`:142-147; `hooks/loop-bash-validator.sh`:303-314). `tests/test-template-references.sh` requires the common template file to exist (`tests/test-template-references.sh`:152-167).
- edge_cases_or_failure_modes: The block is advisory text rendered by guard scripts; enforcement depends on validator detection. Bash modification detection is regex-based in `command_modifies_file`, covering common redirection, `tee`, in-place editors, copy/move/remove, `dd`, `truncate`, and `exec >` patterns, but known complex shell patterns can depend on the validator’s regex coverage (`hooks/lib/loop-common.sh`:571-599; `tests/test-bash-validator-patterns.sh`:75-168).
- validation_or_tests: Direct file inspection confirmed placeholders and request schema. Reference validation exists in `tests/test-template-references.sh` (`tests/test-template-references.sh`:152-167). Broader behavior is exercised by bash/edit/write validator tests and plan-file hook tests that create goal trackers and verify blocking paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present; checklist cursor uses only [_]`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`