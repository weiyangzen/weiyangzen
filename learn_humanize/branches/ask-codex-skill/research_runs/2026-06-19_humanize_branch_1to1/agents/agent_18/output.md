# agent_18 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-018 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness specification for the Humanize loop algorithms. The directory contains 19 shell test files covering loop state parsing, session discovery, cancel authorization, hook input validation, hook state protection, template loading/rendering, setup argument/path/plan validation, git status/state detection, PR API fetch/poll behavior, timeout portability, base branch detection, and concurrent state access. It is not implementation code, but it is a broad executable contract for algorithmic safety boundaries.
- algorithmic_behavior: The child scripts exercise production libraries and scripts, usually by sourcing `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, or `scripts/portable-timeout.sh`, then creating isolated temp repositories through `tests/test-helpers.sh`. Key contained roles:
  - `test-state-file-robustness.sh`: strict versus tolerant parsing of RLCR `state.md`, required fields, `current_round`, `full_review_round`, malformed YAML, missing frontmatter, binary content, CRLF, zero/negative/large rounds.
  - `test-session-robustness.sh`: active loop selection by newest timestamped directory, `state.md` and `finalize-state.md` activity, terminal `complete-state.md` and `cancel-state.md`, symlink and unusual directory names.
  - `test-concurrent-state-robustness.sh`: concurrent reads, atomic-write simulation, stale/zombie loop protection, strict parser rejection paths, PR loop state detection, YAML list handling.
  - `test-state-transition-robustness.sh`: valid round progression, finalize precedence, cancel inactivity, missing required fields, out-of-range rounds, lexicographic timestamp ordering.
  - `test-cancel-security-robustness.sh`: authorization grammar for moving `state.md` or `finalize-state.md` to `cancel-state.md`; rejects command substitution, backticks, shell chaining, pipes, wrong src/dest, extra args, newlines, mixed quotes, IFS manipulation, trailing spaces, symlinked state files.
  - `test-hook-input-robustness.sh`: validator behavior for JSON inputs, malformed/deep/binary JSON, large commands, Unicode, missing tool fields, monitor rendering/log handling, and availability of production goal/git parsers.
  - `test-hook-system-robustness.sh`: edit/write/bash/plan validators, state and goal-tracker mutation blocks, path traversal to protected state files, stop hook behavior with missing/corrupt/outdated state, large/nested/null JSON.
  - `test-template-error-robustness.sh` and `test-template-stress-robustness.sh`: missing template fallbacks, empty/malformed placeholders, special filenames, symlinks, concurrent loads, large templates/values, many variables, ampersands, backslashes, newlines, Unicode, placeholder injection prevention.
  - `test-path-validation-robustness.sh`: production `setup-rlcr-loop.sh` plan path validation against absolute paths, spaces, metacharacters, symlinks, symlink chains, deep/long paths, empty/comment-only/short/nonexistent/directory plans.
  - `test-plan-file-robustness.sh`: production plan content validation for enough meaningful content, large files, mixed line endings, markdown richness, binary/null content, comments-only/whitespace-only inputs, symlinks, unreadable files, disappearing files.
  - `test-setup-scripts-robustness.sh`: `setup-rlcr-loop.sh` and `setup-pr-loop.sh` argument validation, active-loop blocking, git repo prerequisites, timeout/model/full-review options, symlink rejection, `--skip-impl` state effects.
  - `test-git-operations-robustness.sh`: `humanize_parse_git_status` and git state detection for clean/dirty/untracked/staged/deleted/renamed/binary/space-containing files, detached HEAD, rebase, merge, shallow/non-git states.
  - `test-goal-tracker-robustness.sh`: `humanize_parse_goal_tracker` over markdown lists/tables, active/completed/deferred/open counts, unique completed ACs, malformed/truncated/binary files, long goals.
  - `test-timeout-robustness.sh`: `run_with_timeout` and `detect_timeout_impl` for available backend selection, timeout exit code, pipelines, args, zero timeout, large output, rapid cycles, missing commands, signal handling.
  - `test-base-branch-detection.sh`: base branch discovery from local `main`/`master` or simulated origin default.
  - `test-pr-loop-api-robustness.sh`: shared mock-`gh` harness for PR loop state, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, bot comment parsing, API/rate/network failure handling. `test-pr-loop-api-fetch.sh` and `test-pr-loop-api-poll.sh` split this shared suite into parallel-safe entry points.
- inputs_outputs_state: Inputs are temp directories, synthetic git repos, synthetic `.humanize/rlcr` and `.humanize/pr-loop` state trees, mock `gh` binaries, hook JSON payloads, generated plan files, template files, and shell commands. Outputs are pass/fail/skip counters through `tests/test-helpers.sh`, generated temp state files, fetched comment markdown files, poll JSON, and script exit codes. Persistent repository state is not required; all tests use temp dirs or mocks. The primary state transitions under test are `state.md -> finalize-state.md -> complete-state.md`, `state.md/finalize-state.md -> cancel-state.md`, session lookup active/terminal filtering, setup-created pending session signal files, and hook block/pass decisions.
- gates_or_invariants: Major invariants include: strict parser rejects missing required schema while tolerant reads default safely; active-loop discovery must avoid zombie revival; cancel authorization must be a narrow `mv` grammar and reject symlink state files; plan paths must be relative, simple, nonsymlinked, and meaningful; hooks must block protected loop state edits but pass unrelated sandbox-scoped operations; PR polling must return valid JSON with `has_new_comments:false` on API failure; timeout wrapper must not hang; setup must refuse invalid args and active loop conflicts. Useful line anchors: directory file list from `find tests/robustness -type f` showed all 19 scripts; `tests/run-all-tests.sh:63-80` includes most robustness scripts in the aggregate suite; `tests/robustness/test-cancel-security-robustness.sh:39-392` enumerates cancel security cases; `tests/robustness/test-setup-scripts-robustness.sh:96-1051` enumerates setup validation cases; `tests/robustness/test-pr-loop-api-robustness.sh:34-239` builds the `gh` mock and `:282-558`, `:560-855` split fetch and poll behaviors.
- dependencies_and_callers: Called directly by `tests/run-all-tests.sh` for most scripts at `tests/run-all-tests.sh:63-80`; PR API split wrappers source the shared robustness file at `tests/robustness/test-pr-loop-api-fetch.sh:14-18` and `tests/robustness/test-pr-loop-api-poll.sh:13-18`. Shared dependencies include `tests/test-helpers.sh` for counters/temp repos (`:30-78`, `:86-104`), `hooks/lib/loop-common.sh` for state/session/cancel/hook helpers, `hooks/lib/template-loader.sh` for template rendering, `scripts/humanize.sh` for monitor/git/goal parsing, `scripts/portable-timeout.sh`, setup/cancel scripts, hook validators, stop hooks, and PR scripts.
- edge_cases_or_failure_modes: The suite specifically targets malformed JSON/YAML, missing fields, non-UTF/binary/null bytes, CRLF, Unicode, deeply nested JSON, huge inputs, disappearing/unreadable files, symlinks and symlink chains, shell metacharacter injection, stale older sessions, terminal newest sessions, API rate limits/network failure/auth failure, long bot comments, slow API calls, nonexistent commands, empty commands, detached/rebase/merge git states, and active-loop collision.
- validation_or_tests: This item is itself validation. It is also covered by the aggregate runner, which launches listed suites in parallel, collects exit files and pass/fail counts, sorts by elapsed time, prints failure tails, and exits nonzero on any failed suite (`tests/run-all-tests.sh:111-147`, `:149-193`, `:214-230`). I did not run the tests because the assignment is read-only research and the tests create temp state and subprocesses.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-048 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level executable test orchestrator for the Humanize plugin. It defines the canonical test suite set, executes suites in parallel, aggregates pass/fail totals, and returns the repository-level validation status.
- algorithmic_behavior: The script starts with `set -uo pipefail` rather than `set -e`, allowing it to wait for all parallel jobs even when one suite fails (`tests/run-all-tests.sh:15`). It defines `TEST_SUITES` with core template, hook, monitor, plan, PR-loop, session-id, agent-teams, ask-codex, and robustness scripts (`:31-81`). It marks `test-zsh-monitor-safety.sh` as zsh-only (`:83-86`). It creates an output temp dir and cleanup trap (`:88-90`), decides shell selection with `needs_zsh` (`:92-101`), formats durations with `format_ms` (`:103-109`), spawns every available suite in the background (`:111-147`), waits and parses each suite’s output for final `Passed:` and `Failed:` counters after stripping ANSI escape codes (`:149-180`), records failed suites and last 30 lines of detail (`:182-188`), sorts display lines by elapsed time (`:203-204`), then exits `1` if any suite failed or `0` otherwise (`:214-230`).
- inputs_outputs_state: Inputs are executable test scripts under `tests/`, optional `zsh`, and each suite’s stdout/stderr. Outputs are console summary lines, temp files per suite (`*.out`, `*.exit`, `*.time`, failure `*.detail`), aggregate pass/fail counts, and process exit code. State is limited to associative arrays `PIDS`, `SKIPPED`, `FAILED_SUITES`, aggregate counters, and the temp output directory.
- gates_or_invariants: Missing suites are skipped rather than failing (`:122-125`); zsh-only tests are skipped if zsh is unavailable (`:127-131`); every non-skipped suite gets a background PID (`:132-146`); failed status is determined by either nonzero exit or parsed failed count greater than zero (`:182-184`); details are retained only for failed suites (`:186-187`); final exit is nonzero iff `FAILED_SUITES` is nonempty (`:214-230`).
- dependencies_and_callers: Depends on shell arrays and associative arrays, `mktemp`, `date +%s%3N`, `sed`, `grep`, `sort`, `cut`, `tail`, and test scripts being executable. It coordinates with all listed child test paths, including the assigned `tests/test-session-id.sh` at `:57-59` and robustness scripts at `:62-80`.
- edge_cases_or_failure_modes: If a suite exits before writing its exit/time file, the runner defaults missing exit to `1` and elapsed to `0` (`:169-171`). Pass/fail parsing assumes child summaries contain `Passed:` and `Failed:` tokens (`:174-177`); suites without those tokens contribute zero counts while still failing on nonzero exit. If `date +%s%3N` is not portable on a platform, duration math may degrade. Parallel execution can interleave only through temp files, not stdout, because each suite redirects independently.
- validation_or_tests: This is the validation entry point rather than a tested unit. Its behavior is implicitly validated whenever the full suite is invoked; its declared usage and exit code contract are documented in comments at `:3-12`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-078 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR `session_id` ownership and session-aware loop selection. It validates that setup creates a session binding placeholder, the PostToolUse hook records the actual Claude Code session ID only after a real setup command, and loop discovery/cancel behavior remains safe across concurrent or stale sessions.
- algorithmic_behavior: The script sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` (`tests/test-session-id.sh:15-20`), then uses temp git repos and hand-authored state files. It verifies setup creates `state.md` with a `session_id:` field (`:54-65`), that the initial value is empty (`:72-80`), and that `.humanize/.pending-session-id` exists and references `state.md` (`:83-107`). It exercises `hooks/loop-post-bash-hook.sh` with synthetic PostToolUse JSON to bind `session_id` into `state.md` (`:113-160`), verifies no-op behavior without the signal file (`:167-199`), and checks `find_active_loop` behavior for matching, nonmatching, empty stored, backward-compatible no-filter, and `finalize-state.md` cases (`:202-318`). It verifies `parse_state_file` exports `STATE_SESSION_ID` (`:320-346`) and that cancel is global rather than session-filtered (`:348-383`). It then covers zombie-loop prevention when newer terminal dirs exist (`:385-436`, `:633-684`, `:747-784`), safe characters in IDs (`:438-461`), rejection of non-setup commands while preserving the signal (`:463-509`), acceptance of real setup commands (`:511-528`), special characters in stored IDs (`:530-574`), filter-first behavior across sessions (`:576-631`, `:686-745`), signal-file command-marker format (`:786-823`), rejection of substring/basename/quoted-prefix false positives (`:825-925`), and acceptance of unquoted and tab-delimited setup invocations (`:927-1053`).
- inputs_outputs_state: Inputs are mock setup command paths, generated state files containing YAML frontmatter, `.pending-session-id` files with two-line content, synthetic hook JSON with `session_id`, and production scripts/hooks. Outputs are pass/fail/skip counters and mutations in temp state files: empty `session_id:` becomes a concrete ID only on accepted setup invocation; the pending signal file is removed only after successful binding; cancel renames active state to `cancel-state.md`.
- gates_or_invariants: The binding hook must be one-shot and command-sensitive: no pending signal means no mutation (`:187-197`), non-setup commands preserve empty ID and the signal (`:487-505`), false-positive textual references do not consume the signal (`:848-879`), quoted-prefix concatenation is rejected (`:905-925`), but quoted/unquoted and tab-delimited real setup invocations are accepted (`:950-1007`, `:1033-1052`). `find_active_loop` must match session IDs when present, treat empty stored IDs as backward-compatible matches (`:238-261`), allow no-filter lookup (`:263-286`), and avoid reviving stale older loops when a session’s newest dir is terminal or cancelled (`:633-684`, `:747-784`).
- dependencies_and_callers: Depends on `setup-rlcr-loop.sh`, `loop-post-bash-hook.sh`, `cancel-rlcr-loop.sh`, and `loop-common.sh` functions `find_active_loop` and `parse_state_file`. It uses helper functions `setup_test_dir` and `init_test_git_repo` from `tests/test-helpers.sh`. The aggregate runner includes it as a primary suite at `tests/run-all-tests.sh:57-59`.
- edge_cases_or_failure_modes: Covers absent hook file with skips, missing signal file, existing session IDs, session IDs with slash/ampersand/dot, empty stored IDs, terminal newest loop directories, concurrent sessions where one is terminal and another active, cancel-state terminal blocking, command marker as substring, basename-only commands, quoted path concatenation without delimiter, tabs as delimiters, and stale older active loop directories.
- validation_or_tests: The file is a test suite and ends with `print_test_summary "Session ID Feature Tests"` (`tests/test-session-id.sh:1055-1059`). It provides a direct behavioral contract for the session-id algorithm and is included in the full test runner.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-108 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Blocking prompt template for state-schema validation failures. It tells the agent/user that a required field is missing from the state file and terminates the session as unexpected to preserve state information.
- algorithmic_behavior: The template renders a single placeholder, `{{FIELD_NAME}}`, into a message saying the state file is missing that required field (`prompt-template/block/schema-outdated.md:1-4`). It diagnoses the session as started with an older Humanize version (`:5`) and presents recovery options: cancel the RLCR loop, update the plugin to version `1.1.2+`, and restart (`:7-10`). It defines the resulting transition: terminate the session as `unexpected` to preserve state (`:12`).
- inputs_outputs_state: Input is `FIELD_NAME`, supplied by `hooks/loop-plan-file-validator.sh` when required fields are absent. Output is rendered markdown used as a block reason. The state transition is not performed by this template directly, but the message declares the validator’s preservation behavior.
- gates_or_invariants: Required state fields are hard gates. Missing schema is not auto-repaired inside the hook; the user is instructed to cancel/update/restart, and the session is terminated as unexpected. The short explicit line at `:3` is the key diagnostic contract.
- dependencies_and_callers: Referenced by `hooks/loop-plan-file-validator.sh:65`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md" ... "FIELD_NAME=$field_name"`. The template depends on `hooks/lib/template-loader.sh` placeholder substitution. Template reference coverage is checked by the broader template tests, including `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: If the template is missing, the caller’s `load_and_render_safe` fallback is used. If `FIELD_NAME` is empty or malformed, the rendered diagnostic loses specificity but still blocks. The template itself contains no conditional logic and no external command.
- validation_or_tests: Covered indirectly by template-loading/reference suites and hook validator tests that exercise schema-outdated paths. The direct line reference for the placeholder is `prompt-template/block/schema-outdated.md:3`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-138 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 completion contract template for the no-existing-comments startup case. It tells the worker what artifact must be written before trying to exit and explicitly prevents manual PR review triggering.
- algorithmic_behavior: The template renders `{{RESOLVE_PATH}}` as the required resolution summary destination (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-10`). It requires the summary to say this is Round 0 awaiting initial bot reviews and that there are no issues yet (`:11-14`). It then instructs the agent to try to exit so the Stop Hook can poll for bot reviews (`:15-16`, `:20`) and forbids commenting on the PR to trigger review because bots will review automatically for a new PR (`:17-18`).
- inputs_outputs_state: Input is `RESOLVE_PATH`, prepared by `scripts/setup-pr-loop.sh` as the Round 0 resolve file path. Output is prompt text appended to the startup task. Expected state effect is creation/update of the resolve summary file, followed by Stop Hook-driven polling rather than agent-driven PR comments.
- gates_or_invariants: Before exit, the worker must write the resolution summary to the rendered path. In the no-comments branch, the summary content must indicate Round 0 and no current issues. The invariant is “do not comment on the PR to trigger review”; review initiation belongs to the automated Stop Hook polling path.
- dependencies_and_callers: Referenced by `scripts/setup-pr-loop.sh:914`, which uses `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-no-comments.md" "$FALLBACK_CRITICAL" "${TEMPLATE_VARS[@]}"`. The same setup script sets `RESOLVE_PATH` at `scripts/setup-pr-loop.sh:721` and includes it in template variables at `:736`. The sibling `critical-requirements-has-comments.md` handles the alternate startup branch.
- edge_cases_or_failure_modes: If `RESOLVE_PATH` is missing, the prompt would render an unusable destination. If the template is unavailable, the setup script’s fallback critical content is used. The template has no script logic, so enforcement depends on PR-loop setup and stop-hook behavior.
- validation_or_tests: Covered indirectly by template reference/comprehensive template suites and PR-loop setup tests. The key direct lines are `prompt-template/pr-loop/critical-requirements-no-comments.md:8-18`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence headings present; each assigned item appears once as a heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`