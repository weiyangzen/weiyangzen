# agent_10 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-010 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for RLCR and PR-loop hooks. The directory contains exactly two files, `hooks/lib/template-loader.sh` and `hooks/lib/loop-common.sh`, totaling 60,912 bytes. It centralizes template discovery/rendering, hook input validation, loop state parsing, active-loop discovery, review issue detection, protected-path classifiers, cancel authorization, git-add protection, PR loop helpers, and goal tracker mutation helpers.
- algorithmic_behavior: `template-loader.sh` resolves `prompt-template` relative to the hook library, loads Markdown templates, and performs single-pass `{{VAR}}` substitution using `awk` and `TMPL_VAR_` environment variables so substituted values are emitted without re-scanning. Key lines: `get_template_dir` at `hooks/lib/template-loader.sh:26`, `load_template` at `:36`, `render_template` at `:58`, fallback rendering at `:170`, and directory validation at `:208`. `loop-common.sh` sources the template loader at `hooks/lib/loop-common.sh:157`, establishes constants for state field names and exit reasons at `:24`, validates hook JSON at `:76`, parses loop state at `:352` and `:386`, detects active loop directories at `:233`, extracts Codex review findings at `:472`, enforces cancel-command structure at `:679`, maps PR-loop bot authors at `:855`, and updates PR goal tracking at `:1296`.
- inputs_outputs_state: Inputs include hook JSON payloads, loop directories under `.humanize/rlcr` and `.humanize/pr-loop`, state frontmatter fields, Codex review logs in `CACHE_DIR`, prompt templates under `prompt-template`, shell commands submitted to Bash validators, and goal tracker Markdown. Outputs include global parse variables such as `STATE_CURRENT_ROUND`, `STATE_BASE_BRANCH`, and `VALIDATED_TOOL_NAME`; rendered block messages; detected active loop paths; review result files like `round-N-review-result.md`; boolean shell return codes; and mutated loop or tracker files via helpers such as `end_loop` and `update_pr_goal_tracker`. State transition helpers prefer `finalize-state.md` over `state.md` in `resolve_active_state_file` at `hooks/lib/loop-common.sh:180`, treat terminal `*-state.md` files as non-active during active-loop lookup via `resolve_any_state_file` at `:197`, and rename active state files to terminal names in `end_loop` at `:1247`.
- gates_or_invariants: JSON hook input must be valid UTF-8 JSON, contain `tool_name`, and reject null bytes, as enforced at `hooks/lib/loop-common.sh:76`. Strict state parsing requires YAML separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`, with numeric round and max checks at `:394` to `:439`. Active loop discovery intentionally checks the newest directory only when no session filter is supplied, avoiding stale loop revival, and with a session filter it stops at the newest directory for that session even if terminal, described at `:215` to `:231` and implemented at `:242` to `:290`. Cancel authorization requires a `.cancel-requested` signal file, rejects command substitution, shell operators, hidden variables, mixed quote styles, extra arguments, wrong source/destination paths, and symlinked source files at `:679` to `:837`. `git_adds_humanize` blocks direct or broad `.humanize` staging, including force-add broad scopes, at `:1058` to `:1154`.
- dependencies_and_callers: The directory is sourced by `scripts/humanize.sh`, `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and all loop validators and stop hooks. Concrete callers include `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. `loop-codex-stop-hook.sh` uses `load_and_render_safe` with `block/incomplete-todos.md` at `hooks/loop-codex-stop-hook.sh:385`, calls `detect_review_issues` through `run_and_handle_code_review` at `:1050`, and renders `claude/review-phase-prompt.md` at `:1183`. Tests source these libraries heavily, including `tests/test-template-loader.sh`, `tests/test-codex-review-merge.sh`, `tests/test-session-id.sh`, `tests/test-humanize-escape.sh`, and robustness tests under `tests/robustness`.
- edge_cases_or_failure_modes: Template loading warns and returns empty content for missing templates, with safe fallback rendering used to avoid blank hook reasons. Template substitution is intentionally single-pass; placeholder-like text inside substituted content remains literal. Strict state parsing rejects missing separators and malformed required fields, but it permits negative numeric rounds syntactically; enforcement of semantic round bounds lives elsewhere. `detect_review_issues` scans only the last 50 log lines and only markers in the first 10 characters, which avoids debug false positives but can miss earlier true findings. Active-loop discovery uses lexicographic directory order rather than validating timestamp syntax. Cancel authorization lowercases commands and paths, which supports validator flow but can be sensitive on case-sensitive filesystems. Goal tracker updating uses `sed`/`awk` table insertion and temporary files; partial update repair is supported, but malformed tracker sections may reduce precision.
- validation_or_tests: `tests/test-template-loader.sh`, `tests/test-error-scenarios.sh`, `tests/robustness/test-template-error-robustness.sh`, and `tests/robustness/test-template-stress-robustness.sh` cover template functions. `tests/test-codex-review-merge.sh` covers review issue extraction. `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh` exercise cancel authorization. `tests/test-humanize-escape.sh` covers `.humanize` git-add detection. `tests/test-pr-loop-system.sh` covers `update_pr_goal_tracker`. The assigned `tests/robustness/test-state-transition-robustness.sh` directly validates `parse_state_file_strict`, `get_current_round`, and `find_active_loop`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-040 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop ingestion script that fetches GitHub PR discussion surfaces and normalizes them into a Markdown comment file consumed by setup and review workflow prompts. It is core to PR-loop state initialization because `scripts/setup-pr-loop.sh` writes `round-0-pr-comment.md` from this script before deciding whether comments exist.
- algorithmic_behavior: The script parses positional `<pr_number> <output_file>` plus `--after` and `--bots` options at `scripts/fetch-pr-comments.sh:22` to `:90`, validates PR number and output path at `:92` to `:106`, checks `gh` and `jq` prerequisites at `:112` to `:120`, resolves the base repository by trying the current repo first and then parent repo for forks at `:126` to `:154`, and derives owner/name at `:156` to `:162`. It creates a temp directory and three JSON files for issue comments, review comments, and PR reviews at `:168` to `:175`. `fetch_with_retry` retries each `gh api` endpoint up to three times, emits warnings, writes `[]` on final endpoint failure, and increments `API_FAILURES` without aborting at `:183` to `:208`. It transforms each JSON source to a common object shape with `jq` at `:260` to `:320`, combines and deduplicates with `unique_by(.id)` at `:322` to `:324`, optionally filters by timestamp at `:326` to `:332`, sorts humans before bots and newest first at `:334` to `:343`, then writes grouped Markdown sections at `:345` to `:443`.
- inputs_outputs_state: Inputs are the PR number, output file path, optional ISO-8601 `--after` timestamp, optional comma-separated active bot names, the current git/GitHub repo context from `gh repo view`, PR availability from `gh pr view`, and API payloads from `/issues/{n}/comments`, `/pulls/{n}/comments`, and `/pulls/{n}/reviews`. Output is a Markdown file headed with PR number, fetch timestamp, and resolved repository at `scripts/fetch-pr-comments.sh:245` to `:254`, followed by human comments and either grouped or ungrouped bot comments. The script also writes a warning footer if one or more endpoints failed at `:445` to `:450`. State is temporary and confined to `mktemp -d`, cleaned through `trap` at `:168` to `:170`.
- gates_or_invariants: The script fails fast for missing/invalid arguments, unknown options, absent `gh` or `jq`, inability to resolve current or parent repository, and inability to parse owner/name. It deliberately treats endpoint fetch failures as non-fatal once the repo and PR are resolved, preserving a partial output file and warning instead of aborting the loop. It only includes comments with non-null `created_at` in sorting at `:337` to `:343`. PR reviews with empty bodies are preserved by substituting a state placeholder at `:300` to `:320`, preventing approval-only reviews from disappearing.
- dependencies_and_callers: Depends on GitHub CLI, `jq`, `mktemp`, `sed`, `tr`, `date`, and POSIX shell utilities. Called by `scripts/setup-pr-loop.sh` at `scripts/setup-pr-loop.sh:430` to create the initial comment file with active bot grouping. The setup script later checks for the exact `*No comments found.*` sentinel at `scripts/setup-pr-loop.sh:723` to `:729` to decide if comments exist. Test mocks are supplied by `tests/setup-fixture-mock-gh.sh`, whose fixture-backed endpoints are declared at `tests/setup-fixture-mock-gh.sh:71` to `:86`.
- edge_cases_or_failure_modes: Fork PR handling falls back to the parent repo only if the PR is not found in the current repo. Timestamp filtering uses string comparison at `scripts/fetch-pr-comments.sh:326` to `:332`; this is reliable for normalized ISO-8601 UTC timestamps but not arbitrary date strings. Deduplication by ID across comment types can collapse different GitHub object types if IDs collide globally. The `is_bot` helper at `:226` is defined but not used; bot classification is done inline in `jq` and shell grouping. Active bot mapping special-cases `codex` to `chatgpt-codex-connector[bot]` at `:386` to `:392`, while all other names become `<bot>[bot]`. API endpoint failures can produce successful exit with incomplete comments, flagged only by stderr and output warning.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` validates help, missing PR number, missing output, and invalid PR number at `tests/test-pr-loop-scripts.sh:259` to `:325`. `tests/test-pr-loop-hooks.sh` fixture tests assert all comment types are included, approval-only reviews get `[Review state: APPROVED]`, and `--after` filtering excludes older comments at `tests/test-pr-loop-hooks.sh:1242` to `:1318`. `tests/robustness/test-pr-loop-api-robustness.sh` covers empty arrays, rate-limit/network behavior, Claude and Codex bot parsing, multiple bots, Unicode, and long bodies at `tests/robustness/test-pr-loop-api-robustness.sh:362` to `:550`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-070 `file` `tests/test-pr-loop-2-hooks.sh`
- cursor: `[_]`
- core_role: Split test runner for PR-loop hook functionality. It is not an algorithm implementation itself, but it is an executable specification entrypoint for the hook and PR comment behavior covered by `tests/test-pr-loop-hooks.sh`.
- algorithmic_behavior: The runner sets strict shell mode at `tests/test-pr-loop-2-hooks.sh:9`, calculates `SCRIPT_DIR` and `PROJECT_ROOT` at `:11` to `:12`, sources shared test helpers and PR-loop test library at `:14` to `:15`, initializes a mocked PR-loop environment with `init_pr_loop_test_env` at `:17`, sources the hook test definitions at `:19`, invokes `run_hook_tests` at `:21`, and returns the aggregate test status from `print_test_summary` at `:23` to `:24`.
- inputs_outputs_state: Inputs are the repository checkout, helper scripts under `tests`, hook scripts under `hooks`, PR-loop scripts under `scripts`, and mocks created by `init_pr_loop_test_env`. Outputs are TAP-like shell log lines through `pass`/`fail` helpers and a process exit code equal to the test summary result. State is created in the temporary test directory initialized by `setup_test_dir` through `tests/test-pr-loop-lib.sh:107` to `:121`; the runner itself does not mutate repository files.
- gates_or_invariants: Because of `set -euo pipefail`, sourcing failures, missing helpers, or failed unguarded commands abort the runner. All actual assertions live in `run_hook_tests`; this wrapper’s invariant is that the hook test suite runs in an isolated mock environment before summary. It relies on `print_test_summary` returning nonzero when any test failed at `tests/test-pr-loop-lib.sh:127` to `:144`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and `tests/test-pr-loop-hooks.sh`. It is part of a parallel split family noted by the header, alongside `tests/test-pr-loop.sh` and sibling split runners. `tests/test-pr-loop-hooks.sh` includes validator protections, sorting checks, fixture-backed fetch checks, and monitor-related hook tests; its `run_hook_tests` function starts at `tests/test-pr-loop-hooks.sh:15`.
- edge_cases_or_failure_modes: Misclassified as “core algorithm” if interpreted as production logic; it is a harness. Failures can arise from missing executable bits on hook scripts, missing `jq`/shell utilities, broken mocks, or global `TESTS_PASSED`/`TESTS_FAILED` state from helpers. Since this is a coarse runner, it does not allow selecting individual hook tests unless the sourced library supports it.
- validation_or_tests: This file is the validation entrypoint. The specific assigned behavior tied to PR comment fetching is tested in the sourced hook test file at `tests/test-pr-loop-hooks.sh:1242` to `:1318`, and validator protections are defined earlier in the same file at `:26` to `:210`.
- skip_candidate: `yes: executable harness rather than production algorithm, but it is still relevant as a specification runner for core hook behavior`

### ASK_CODEX_SKILL-HZ-100 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the RLCR stop hook when a user attempts to stop while native task/todo items remain incomplete. It defines a gate in the loop transition from active work to Codex review or stopping.
- algorithmic_behavior: The template emits a “Incomplete Tasks Detected” block at `prompt-template/block/incomplete-todos.md:1`, injects `{{INCOMPLETE_LIST}}` at `:5`, and instructs the agent to complete remaining tasks, mark each task completed using `TaskUpdate`, and only then proceed to summary/stop at `:7` to `:10`. It explicitly blocks progression to Codex review until all tasks are finished at `:12`.
- inputs_outputs_state: Input is the `INCOMPLETE_LIST` variable supplied by the stop hook after task checker output is parsed. Output is a Markdown reason string passed to the hook decision JSON. It does not modify state directly; it drives a block decision that prevents the stop/review transition.
- gates_or_invariants: The invariant is “no review/stop while tasks remain incomplete.” In `hooks/loop-codex-stop-hook.sh`, when `TODO_EXIT` equals 1, the hook extracts incomplete tasks at `hooks/loop-codex-stop-hook.sh:375` to `:379`, renders this template at `:385` to `:386`, returns a JSON decision of `block` at `:388` to `:395`, and exits before review logic.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh`. Direct caller is `hooks/loop-codex-stop-hook.sh:385`. It depends on the stop hook’s task checker path producing an incomplete-list payload; if the template is absent or empty, the hook has an inline fallback at `hooks/loop-codex-stop-hook.sh:380` to `:384`.
- edge_cases_or_failure_modes: If `INCOMPLETE_LIST` contains template-looking `{{...}}` text, the single-pass renderer in `hooks/lib/template-loader.sh:58` to `:131` prevents recursive substitution. If the task checker gives malformed output, a separate parse-error block path is used before this template. The template says `TaskUpdate` status should be `completed`; if the external task tool vocabulary changes, this prompt could become stale.
- validation_or_tests: Template reference coverage is checked by `tests/test-template-references.sh`, and comprehensive template loading by `tests/test-templates-comprehensive.sh`. The behavioral gate is exercised through stop-hook tests where incomplete task checker exit status causes a block before Codex review.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-130 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Review-phase prompt template generated when Codex review detects severity-marked issues. It encodes the loop transition from “implementation complete” back to “fix review findings,” and constrains the next worker action to targeted fixes.
- algorithmic_behavior: The template announces the Review Phase at `prompt-template/claude/review-phase-prompt.md:1` to `:3`, injects review findings through `{{REVIEW_CONTENT}}` at `:5` to `:7`, instructs the agent to address all `[P0-9]` findings, avoid unrelated changes, commit fixes, and write a summary to `{{SUMMARY_FILE}}` at `:9` to `:15`. It also defines summary contents at `:16` to `:21` and clarifies that the COMPLETE signal is ignored in this phase, with repeated Codex reviews until no severity findings remain at `:23` to `:28`.
- inputs_outputs_state: Inputs are `REVIEW_CONTENT`, generated by `detect_review_issues`, and `SUMMARY_FILE`, computed as `round-N-summary.md` by the stop hook. Output is a rendered prompt file `round-N-prompt.md`, written by `hooks/loop-codex-stop-hook.sh:1183` to `:1185`, and then returned as the hook block reason at `:1187` to `:1192`. State transition side effect occurs in the caller: the hook updates `current_round` in `state.md` before prompt generation at `hooks/loop-codex-stop-hook.sh:1159` to `:1162`.
- gates_or_invariants: The template preserves review-loop gating: COMPLETE has no effect, all severity findings must be fixed, and no new feature work should be introduced. The caller enters this path only when `detect_review_issues` returns findings in `run_and_handle_code_review` at `hooks/loop-codex-stop-hook.sh:1046` to `:1057`; no findings transition to finalize instead at `:1059` to `:1062`.
- dependencies_and_callers: Rendered through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:1183`; fallback content is embedded at `:1168` to `:1181`. It depends on `hooks/lib/loop-common.sh:472` to identify issue content and on `hooks/lib/template-loader.sh` for single-pass variable replacement. Tests around review detection and merge behavior live in `tests/test-codex-review-merge.sh`, while `tests/test-agent-teams.sh` notes this template is used for review-phase prompts and should not include agent-team instructions around `tests/test-agent-teams.sh:614`.
- edge_cases_or_failure_modes: Review findings are only as complete as `detect_review_issues`, which scans the tail of the review log and markers near line starts. If review content contains literal template placeholders, single-pass rendering prevents accidental expansion. The template requires a commit after fixes, so a review-only or explanation-only response is intentionally noncompliant. If `SUMMARY_FILE` is wrong due to caller state, the template will faithfully point to the wrong file; path correctness is owned by the stop hook.
- validation_or_tests: `tests/test-codex-review-merge.sh` validates issue marker detection behavior that feeds this template. Template existence and reference integrity are covered by `tests/test-template-references.sh` and comprehensive template tests. Stop-hook review transition behavior is indirectly covered by tests that exercise `detect_review_issues` outcomes and review-loop prompting.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-160 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test specification for RLCR loop state parsing, active-loop discovery, terminal-state handling, and schema validation. It directly exercises core functions from `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The script enables strict shell mode at `tests/robustness/test-state-transition-robustness.sh:12`, resolves project paths, sources `loop-common.sh` and test helpers at `:14` to `:17`, and creates a temp test directory at `:19`. It defines fixtures for full active state, finalize state, and cancel state at `:30` to `:89`. Tests 1 to 3 validate normal round parsing and max-round values at `:98` to `:138`. Tests 4 and 5 verify finalize detection and precedence at `:148` to `:172`. Tests 6 and 7 verify terminal cancel state is not active and a newer active state after cancel is selected at `:182` to `:207`. Tests 8 to 10 cover negative, over-max, and non-numeric rounds at `:217` to `:282`. Tests 11 to 13 validate required schema fields at `:292` to `:361`. Tests 14 to 16 cover loop-directory discovery, lexicographic ordering, and ignoring nested state files at `:371` to `:417`.
- inputs_outputs_state: Inputs are temporary `.humanize/rlcr/<timestamp>` directories and synthetic `state.md`, `finalize-state.md`, and `cancel-state.md` files. The tested functions are `parse_state_file_strict`, `get_current_round`, and `find_active_loop`. Outputs are pass/fail records through `pass` and `fail`, followed by `print_test_summary` at `:423` and process exit based on failure count at `:424`. State remains in the temporary test directory managed by `setup_test_dir`.
- gates_or_invariants: Active state files require strict frontmatter fields, including `current_round`, `max_iterations`, `base_branch`, and `review_started`, as checked by `parse_state_file_strict`. `finalize-state.md` is considered active and preferred over `state.md`; `cancel-state.md` is terminal and not active. Newer active directories supersede older terminal ones, but if the newest relevant directory is terminal under session-filter logic, stale revival is prevented in `loop-common.sh`. Directory discovery is deliberately one level deep and lexicographic, not recursive or timestamp-validating.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, especially `parse_state_file_strict` at `hooks/lib/loop-common.sh:386`, `get_current_round` at `:296`, `resolve_active_state_file` at `:180`, `resolve_any_state_file` at `:197`, and `find_active_loop` at `:233`. Depends on `tests/test-helpers.sh` for temp setup and result accounting. This test complements `tests/robustness/test-state-file-robustness.sh`, `tests/robustness/test-concurrent-state-robustness.sh`, `tests/robustness/test-session-robustness.sh`, and `tests/test-state-exit-naming.sh`.
- edge_cases_or_failure_modes: The test documents accepted-but-suspicious behavior: negative rounds parse because strict parser accepts signed integers, and rounds above max are parsed while enforcement is elsewhere. It also documents lexicographic directory selection and lack of recursive discovery. The fixture writer omits some optional fields in invalid cases to isolate schema validation behavior. Because the script writes test files with heredocs, failures can arise if helper temp setup points at a non-writable location, but not from repository writes.
- validation_or_tests: This file is itself validation. It covers 16 named scenarios and exits with the shared test summary result. It is likely included in broader robustness runners such as `tests/run-all-tests.sh` or manual robustness sweeps.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6 section headings present; each assigned row represented once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`