# agent_05 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Central prompt/block corpus for the Humanize RLCR, PR-loop, gen-plan, and ask/review workflows. The directory contains 56 markdown templates totaling 46,698 bytes across `block/` 32 files, `claude/` 12, `codex/` 4, `plan/` 1, and `pr-loop/` 7. It is loaded through [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/template-loader.sh:24), which resolves the plugin root’s `prompt-template` path.
- algorithmic_behavior: Templates are not passive docs; they encode runtime gates and transition contracts. `block/` files are validator rejection messages for protected paths, git hygiene, stale schema, wrong rounds, large files, missing work summaries, and PR-loop state protection. `claude/` files build implementation/finalization/next-round prompts and footer obligations. `codex/` files define review criteria, full-alignment checks, code-review output contracts, and goal-tracker update requests. `pr-loop/` files seed PR-loop prompts, round-0 tasks, critical requirements, and PR goal-tracker state. `plan/gen-plan-template.md` defines the structured plan-generation contract.
- inputs_outputs_state: Inputs are template names plus `{{VAR}}` substitutions. `render_template` performs single-pass substitution and leaves missing placeholders intact, which protects review content from being recursively interpreted as template syntax; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/template-loader.sh:50) lines 50-57 and 71-131. Outputs are rendered prompts or block messages consumed by hooks and setup scripts. Runtime state affected indirectly includes generated `.humanize/rlcr/.../round-N-prompt.md`, PR-loop prompts, goal trackers, and validator stderr.
- gates_or_invariants: `validate_template_dir` requires the directory plus `block/`, `codex/`, and `claude/` subdirectories; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/template-loader.sh:205) lines 205-221. `load_and_render_safe` falls back when a template is missing or renders empty; lines 170-203. The corpus uses `{{UPPERCASE_VAR}}` placeholders, and comprehensive tests validate loadability and syntax.
- dependencies_and_callers: Main callers include RLCR stop-hook prompt assembly in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:1550), validators such as [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-bash-validator.sh:89), shared block-message helpers in `hooks/lib/loop-common.sh`, PR-loop setup in `scripts/setup-pr-loop.sh`, RLCR setup in `scripts/setup-rlcr-loop.sh`, and gen-plan validation in `scripts/validate-gen-plan-io.sh`.
- edge_cases_or_failure_modes: Missing templates degrade through fallback messages where callers use `load_and_render_safe`; direct `load_template` callers may receive empty content and must handle it. Missing variables remain visible as `{{VAR}}`, which is useful for debugging but can leak placeholders into generated prompts. The validation function only requires `block`, `codex`, and `claude`, so `plan` and `pr-loop` completeness is enforced mainly by tests rather than `validate_template_dir`.
- validation_or_tests: [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-template-references.sh:56) scans shell scripts for template references, fails missing files, checks common templates, and verifies critical validators use safe rendering. [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-templates-comprehensive.sh:90) loads every template, validates placeholder syntax, tests fallback behavior, and renders real templates including `block/git-push.md`. Not executed in this research pass.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-035 `file` `scripts/ask-codex.sh`
- cursor: `[_]`
- core_role: One-shot Codex consultation runtime. It is the executable backing the `ask-codex` skill, sending a prompt to `codex exec`, persisting input/output/metadata, and returning Codex stdout to the caller. The skill contract in [skills/ask-codex/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/skills/ask-codex/SKILL.md:15) invokes this script directly.
- algorithmic_behavior: The script runs strict bash mode at [scripts/ask-codex.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/ask-codex.sh:20), sources `portable-timeout.sh` and `loop-common.sh` for timeout/default model handling, then parses `--codex-model`, `--codex-timeout`, `--`, and positional question text. It validates `codex` availability, rejects empty questions, validates model/effort characters, discovers the project root via git or `CLAUDE_PROJECT_DIR`, creates a unique `.humanize/skill/<id>/` record, creates a cache directory, builds `codex exec` args, runs Codex through `run_with_timeout`, and classifies result state as timeout, error, empty response, or success.
- inputs_outputs_state: Inputs are CLI args, stdin only indirectly through the generated prompt pipe, `HUMANIZE_CODEX_BYPASS_SANDBOX`, `XDG_CACHE_HOME`, `HOME`, `CLAUDE_PROJECT_DIR`, the current git worktree, and `codex` on `PATH`. Outputs are clean Codex response on stdout, status/error/debug lines on stderr, `.humanize/skill/<unique-id>/input.md`, `output.md`, `metadata.md`, and cache files `codex-run.cmd`, `codex-run.out`, `codex-run.log`. Success exits 0; validation and empty response generally exit 1; timeout exits 124; other Codex failures propagate Codex’s exit code.
- gates_or_invariants: Argument parsing stops option handling after the first positional token or explicit `--` lines 86-147. Timeout must match numeric regex lines 120-130. Model allows alphanumeric, dot, underscore, hyphen lines 172-178; effort allows alphanumeric, underscore, hyphen lines 180-186. `codex` must exist lines 153-160, question must be non-empty lines 162-170, stdout must be non-empty lines 358-381. The sandbox posture defaults to `--full-auto`; dangerous bypass only occurs when `HUMANIZE_CODEX_BYPASS_SANDBOX` is `true` or `1`, lines 249-253.
- dependencies_and_callers: Depends on [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/portable-timeout.sh:10) for GNU/macOS/Python/no-timeout selection and [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/loop-common.sh:42) for default `gpt-5.3-codex:xhigh`. Documented in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/README.md:203) lines 203-221. Called through the Claude skill allowlist in `skills/ask-codex/SKILL.md`.
- edge_cases_or_failure_modes: If no git root exists, project root falls back to `CLAUDE_PROJECT_DIR` or `pwd`. If home cache is not writable, cache falls back into `.humanize/skill/<id>/cache`. If no timeout implementation exists, `portable-timeout.sh` warns and runs without a hard timeout. The timeout validator accepts `0` because it checks digits, not greater-than-zero, despite the error text saying positive integer. Non-zero Codex exits include the last 20 stderr lines in the user-facing error. Empty successful stdout is treated as failure with `status: empty_response`.
- validation_or_tests: [tests/test-ask-codex.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-ask-codex.sh:83) covers empty questions, help, unknown options, missing option args, invalid model/effort, success output and persisted files, metadata states, timeout handling, concurrent unique directories, argument separator handling, and cache file creation. Not executed in this research pass.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-065 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: Narrow executable spec for monitor SIGINT behavior. It is a parallel split wrapper that sources the real end-to-end monitor test library and runs only the SIGINT-related cases.
- algorithmic_behavior: [tests/test-monitor-e2e-sigint.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-monitor-e2e-sigint.sh:1) enables strict bash mode, sources `test-monitor-e2e-real.sh` at line 5, prints a section header, invokes `monitor_test_bash_sigint`, `monitor_test_zsh_sigint`, and `monitor_test_pr_sigint` at lines 12-14, then prints pass/fail counters and exits 0 only when `TESTS_FAILED` is zero at lines 20-22.
- inputs_outputs_state: Inputs are the sourced helper functions, temp test projects, shell availability, `scripts/humanize.sh`, fake `HOME` and cache paths, mock `.humanize/rlcr` and `.humanize/pr-loop` state trees created by the helper. Outputs are PASS/FAIL lines and process exit status. The helper creates and cleans `/tmp/test-monitor-e2e-real-$$` using an EXIT trap.
- gates_or_invariants: The wrapper’s invariant is “all three SIGINT specs must pass or the script exits non-zero.” In the helper, bash RLCR SIGINT sets up a real monitor runner, sends SIGINT to the monitor process group, bounds wait loops, and verifies cleanup/no glob errors; see [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-monitor-e2e-real.sh:377) lines 377-550. The zsh variant skips when zsh is unavailable and otherwise checks clean exit/no glob errors lines 555-686. The PR monitor variant runs `humanize monitor pr`, sends SIGINT, and verifies cleanup/no glob errors lines 834-997.
- dependencies_and_callers: Depends on `test-monitor-e2e-real.sh`, which explicitly says it uses the real `_humanize_monitor_codex` and `_humanize_monitor_pr` paths rather than stubs lines 3-12. Those helpers source [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/humanize.sh:1) inside generated runner scripts.
- edge_cases_or_failure_modes: SIGINT delivery is recognized as shell/process-group sensitive. The bash and PR helper paths tolerate fallback termination after SIGTERM when the monitor was running but SIGINT delivery is unreliable. The tests guard against hanging with bounded wait loops and force-kill cleanup, and specifically reject zsh/bash glob errors such as “no matches found” or “bad pattern.”
- validation_or_tests: This file is itself the validation wrapper. The sourced helper validates real graceful-interrupt behavior, terminal cleanup indications, no glob expansion failures, and PR monitor behavior with and without one-shot mode. Not executed in this research pass.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-095 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Runtime block message for the RLCR bash validator when a user attempts `git push` while the loop is configured to keep commits local.
- algorithmic_behavior: [prompt-template/block/git-push.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/block/git-push.md:1) emits a fixed “Git Push Blocked” message, states that commits should stay local, and tells the user to start the loop with `--push-every-round` when per-round pushes are desired.
- inputs_outputs_state: The template has no placeholders, so its only input is existence/loadability. Output goes to stderr from the bash validator. It does not mutate state directly; the state input is `push_every_round` read from the RLCR state file by the caller.
- gates_or_invariants: The active gate is in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-bash-validator.sh:89) lines 89-104. After strict state parsing, if `PUSH_EVERY_ROUND` is not `true` and `COMMAND_LOWER` starts with `git push`, the validator renders this template and exits 2. The invariant is that normal RLCR rounds do not push unless the loop was explicitly configured to do so.
- dependencies_and_callers: Loaded through `load_and_render_safe`, so a fallback block message exists if the template is missing. It depends on `FIELD_PUSH_EVERY_ROUND` from `loop-common.sh` and the validator’s normalized command string. The README documents `--push-every-round` as requiring git push after each round at [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/README.md:156).
- edge_cases_or_failure_modes: The validator regex is anchored to commands beginning with `git push`, so chained forms such as `cd repo && git push` may not be covered by this specific gate unless another validator path catches them. If the state file is malformed, strict parsing blocks before this template is reached. Because the template has no variables, rendering cannot fail due to missing substitutions.
- validation_or_tests: [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-templates-comprehensive.sh:456) verifies `load_and_render_safe` uses the real `block/git-push.md` template when present. Template reference and load tests also cover existence/loadability. Not executed in this research pass.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-125 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Next-round Claude prompt footer that encodes completion obligations after Codex requests another iteration.
- algorithmic_behavior: [prompt-template/claude/next-round-footer.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/claude/next-round-footer.md:1) appends a separator, forbids false exits, state-file edits, and `cancel-rlcr-loop`, conditionally asks for `code-simplifier` review if installed, then requires a descriptive commit and a work summary at `@{{NEXT_SUMMARY_FILE}}`.
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, computed by the stop hook as `.humanize/rlcr/<timestamp>/round-N-summary.md`. Output is appended to the generated next-round prompt file. The caller first advances `current_round` in the state file, then creates `round-N-prompt.md` and `round-N-summary.md` path variables.
- gates_or_invariants: In [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:1541), the hook updates `current_round`, builds `NEXT_PROMPT_FILE` and `NEXT_SUMMARY_FILE`, renders the main next-round prompt, injects optional open-question/post-alignment notices, then appends this footer via `load_and_render_safe` at lines 1608-1612. The invariant is that every follow-up implementation prompt carries commit and summary obligations.
- dependencies_and_callers: Depends on the template loader and the RLCR stop-hook’s state variables. It coordinates with `claude/next-round-prompt.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md`, appended nearby in the same prompt assembly flow lines 1614-1628.
- edge_cases_or_failure_modes: If `NEXT_SUMMARY_FILE` is missing from render variables, the placeholder remains visible because rendering intentionally preserves undefined placeholders. If the template is missing, the fallback still requires commit and summary but loses the stronger “do not lie/edit state/cancel” wording and optional code-simplifier instruction. The code-simplifier instruction is conditional on plugin availability and does not define the plugin detection itself.
- validation_or_tests: Covered by directory-wide load and syntax tests in `tests/test-templates-comprehensive.sh` and template-reference scanning. The stop-hook prompt assembly path references the template directly, so missing-file tests should catch removal. Not executed in this research pass.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-155 `file` `tests/robustness/test-pr-loop-api-poll.sh`
- cursor: `[_]`
- core_role: Parallel split wrapper for PR-loop stop-hook and `poll-pr-reviews.sh` robustness tests. It narrows the larger API robustness suite to tests 12-19.
- algorithmic_behavior: [tests/robustness/test-pr-loop-api-poll.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-pr-loop-api-poll.sh:1) enables strict bash mode, sources `test-pr-loop-api-robustness.sh`, invokes `run_poll_tests`, prints the “PR Loop API Poll Tests” summary, and exits with the summary status.
- inputs_outputs_state: Inputs are the shared robustness helper, temp git repos, generated `.humanize/pr-loop/.../state.md`, mock `gh` binaries, `hooks/pr-loop-stop-hook.sh`, `scripts/poll-pr-reviews.sh`, and `jq`. Outputs are PASS/FAIL diagnostics and exit status. The shared file initializes `PROJECT_ROOT`, sources `loop-common.sh` and `test-helpers.sh`, and sets up a temp test directory at [tests/robustness/test-pr-loop-api-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-pr-loop-api-robustness.sh:16) lines 16-21.
- gates_or_invariants: `run_poll_tests` starts at line 561. It requires the stop hook to pass with no active PR loop, handle corrupted state without crashing with signal-style exit codes, and allow the `approve-state.md` path. It then requires `poll-pr-reviews.sh --help` to succeed, missing args to fail, mocked review polling to return JSON with `has_new_comments` as a boolean and `comments` as an array when available, slow API calls to complete, and API failure to return exit 0 plus parseable JSON with boolean `has_new_comments:false`.
- dependencies_and_callers: Depends on `create_mock_gh`, `create_pr_loop_state`, and `init_basic_git_repo` from the shared robustness file. The mock `gh` covers repo view, PR view, issue comments, rate/network/auth failures, mixed bot comments, Unicode, and long comments. This wrapper is likely used by parallelized CI to run only the polling half of the larger suite.
- edge_cases_or_failure_modes: Explicitly exercises no active loop, corrupted state, missing CLI args, slow API, and API failure. The API-failure check is strict: non-zero exit fails, missing JSON fails, invalid JSON fails, and string `"false"` fails because the test checks for boolean false through `jq`.
- validation_or_tests: This file is the executable validation entry point. The concrete assertions live in [tests/robustness/test-pr-loop-api-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-pr-loop-api-robustness.sh:571) lines 571-855. Not executed in this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 item section headings above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`