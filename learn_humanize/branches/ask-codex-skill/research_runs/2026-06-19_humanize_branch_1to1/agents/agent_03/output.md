# agent_03 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command entrypoint layer for the plugin’s three main workflows: plan generation, RLCR loop start/cancel, and PR review loop start/cancel. The directory itself is declarative markdown, but it is core because these files define allowed tools, argument surfaces, user-facing state transitions, and which scripts are invoked.
- algorithmic_behavior: `commands/gen-plan.md` defines a six-phase plan-generation pipeline: validate IO, relevance-check draft, analyze clarity/consistency/completeness/functionality, resolve issues with user input, generate a structured plan, then edit/review the output file; see `commands/gen-plan.md:19-27`, `commands/gen-plan.md:30-47`, `commands/gen-plan.md:50-72`, `commands/gen-plan.md:76-109`, `commands/gen-plan.md:111-139`, `commands/gen-plan.md:142-248`, `commands/gen-plan.md:251-293`. `commands/start-rlcr-loop.md` delegates startup to `scripts/setup-rlcr-loop.sh`, then describes the RLCR phases: implementation summary review, review phase via `codex review --base`, and finalize; see `commands/start-rlcr-loop.md:10-25`, `commands/start-rlcr-loop.md:60-67`. `commands/start-pr-loop.md` delegates startup to `scripts/setup-pr-loop.sh` and defines the PR bot-monitor loop: fetch comments, fix issues, push, tag bots, stop hook polls, local Codex validates, then repeats or exits; see `commands/start-pr-loop.md:10-24`, `commands/start-pr-loop.md:37-47`. The two cancel commands delegate all mutation to cancel scripts and require interpreting first-line status codes; see `commands/cancel-rlcr-loop.md:17-36` and `commands/cancel-pr-loop.md:17-25`.
- inputs_outputs_state: `gen-plan` input is `--input` draft and `--output` plan path, validated by `scripts/validate-gen-plan-io.sh`, which rejects missing/empty input, missing output dir, existing output, no write permission, invalid args, or missing template, then copies the plan template and appends original draft content; see `scripts/validate-gen-plan-io.sh:29-67`, `scripts/validate-gen-plan-io.sh:79-124`, `scripts/validate-gen-plan-io.sh:133-160`. RLCR startup accepts a plan path or `--plan-file`, iteration/model/timeout/base-branch/review options, and `--skip-impl`; it creates `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md`; see `scripts/setup-rlcr-loop.sh:300-328`, `scripts/setup-rlcr-loop.sh:725-787`, `scripts/setup-rlcr-loop.sh:809-928`, `scripts/setup-rlcr-loop.sh:934-1065`. PR startup accepts `--claude` and/or `--codex`, resolves the PR, fetches comments, creates `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-pr-resolve.md`, and `round-0-prompt.md`; see `scripts/setup-pr-loop.sh:255-277`, `scripts/setup-pr-loop.sh:295-379`, `scripts/setup-pr-loop.sh:430-635`, `scripts/setup-pr-loop.sh:641-851`. Cancel scripts rename active state to `cancel-state.md` and create `.cancel-requested`; RLCR also handles finalize-state with confirmation unless forced; see `scripts/cancel-rlcr-loop.sh:99-169` and `scripts/cancel-pr-loop.sh:88-130`.
- gates_or_invariants: Commands restrict tool access to the intended setup/cancel scripts; see `commands/gen-plan.md:4-12`, `commands/start-rlcr-loop.md:1-5`, `commands/start-pr-loop.md:1-5`, `commands/cancel-rlcr-loop.md:1-4`, `commands/cancel-pr-loop.md:1-4`. RLCR startup enforces git repository with at least one commit, relative plan paths, no spaces or shell metacharacters, no symlink file or symlink parent traversal, file exists/readable, path stays in project, no submodule plan, tracked/clean plan only with `--track-plan-file`, otherwise plan must not be tracked, minimum 5 lines and at least 3 content lines, and Codex availability; see `scripts/setup-rlcr-loop.sh:334-344`, `scripts/setup-rlcr-loop.sh:356-457`, `scripts/setup-rlcr-loop.sh:463-508`, `scripts/setup-rlcr-loop.sh:519-585`. PR startup gates on git, `gh`, auth, Codex, PR existence, PR not closed/merged, and YAML-safe branch/model/effort values; see `scripts/setup-pr-loop.sh:250-277`, `scripts/setup-pr-loop.sh:351-390`, `scripts/setup-pr-loop.sh:393-407`.
- dependencies_and_callers: Command files are invoked by Claude slash command infrastructure and depend on `${CLAUDE_PLUGIN_ROOT}` resolving to the plugin root. They call `scripts/validate-gen-plan-io.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, and `scripts/cancel-pr-loop.sh`. Those scripts depend on shared hook utilities in `hooks/lib/loop-common.sh` and template rendering in `hooks/lib/template-loader.sh`; PR startup also depends on `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`, `gh`, `jq`, and GitHub API behavior.
- edge_cases_or_failure_modes: `gen-plan` can fail after creating output if the template exists but later analysis cannot complete; command text requires preserving original draft content and using Edit rather than Write after validation; see `commands/gen-plan.md:113-114`, `commands/gen-plan.md:247-260`. RLCR `--skip-impl` permits no plan, creates placeholder plan state, and starts with `review_started` true; see `scripts/setup-rlcr-loop.sh:309-320`, `scripts/setup-rlcr-loop.sh:733-765`, `scripts/setup-rlcr-loop.sh:800-803`, `scripts/setup-rlcr-loop.sh:936-970`. PR startup case 4/5 avoids duplicate bot trigger comments and verifies Claude eyes reaction, failing closed and removing the loop dir if the trigger or reaction cannot be verified; see `scripts/setup-pr-loop.sh:457-602`. Cancel commands preserve loop directories and state history but are intentionally broad: RLCR cancel is global to newest active loop because slash command lacks session id; see `scripts/cancel-rlcr-loop.sh:76-87`.
- validation_or_tests: Covered by many tests under `tests/`, especially plan validation robustness and PR/RLCR system tests. Directly relevant assigned tests include `tests/robustness/test-plan-file-robustness.sh`, which runs `setup-rlcr-loop.sh` against realistic plan files, and `tests/test-monitor-e2e-deletion.sh`, which validates monitor deletion behavior. Hook registration is defined in `hooks/hooks.json:14-79`, connecting Write/Edit/Read/Bash validators and both stop hooks.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-033 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Write gate for RLCR and PR loop state machines. It prevents Claude from corrupting generated loop state, prompt, todos, round-specific summary/resolution files, plan backup files, goal tracker ownership, and protected PR-loop artifacts.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON syntax/UTF-8/null-byte safety through `validate_hook_input`, rejects overly deep JSON via `is_deeply_nested`, then exits early unless `.tool_name` is `Write`; see `hooks/loop-write-validator.sh:25-45` and helper behavior in `hooks/lib/loop-common.sh:76-150`. It extracts `.tool_input.file_path`, lowercases for pattern tests, and extracts session id for active-loop filtering; see `hooks/loop-write-validator.sh:48-52`. It blocks `round-N-todos.md` writes except allowlisted files in the active loop, blocks all `round-N-prompt.md` writes, applies PR-loop protections, classifies RLCR summary/finalize summary/loop-dir paths, locates the active RLCR loop, parses state strictly, blocks state files, conditionally allows the active finalize summary, blocks `plan.md` backup writes, blocks goal tracker writes after round 0, blocks summaries outside `.humanize/rlcr`, validates summary round number, and finally validates the exact active-loop directory path; see `hooks/loop-write-validator.sh:54-71`, `hooks/loop-write-validator.sh:77-97`, `hooks/loop-write-validator.sh:103-120`, `hooks/loop-write-validator.sh:126-147`, `hooks/loop-write-validator.sh:154-265`.
- inputs_outputs_state: Input is Claude hook JSON with `tool_name`, `tool_input.file_path`, and optional `session_id`. Output is process exit status plus stderr block messages. Exit 0 allows Write, exit 1 indicates malformed hook/state input or safety failure, and exit 2 blocks the tool with a rendered template reason. It does not mutate files; it reads state and templates only. It derives `CURRENT_ROUND` from active `state.md` or `finalize-state.md` through `parse_state_file_strict`; see `hooks/loop-write-validator.sh:135-147`.
- gates_or_invariants: Only the active loop’s current round summary can be written, with a small legacy allowlist for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`; see `hooks/lib/loop-common.sh:548-569`. Prompt files are read-only generated instructions; see `hooks/lib/loop-common.sh:582-589`. `state.md` and `finalize-state.md` are system-owned and blocked, with finalize-state checked first because the generic state pattern also matches it; see `hooks/loop-write-validator.sh:150-162`. Finalize summary is allowed only at `$ACTIVE_LOOP_DIR/finalize-summary.md`; see `hooks/loop-write-validator.sh:164-175`. Goal tracker writes are allowed during round 0 but blocked after round 0, redirecting work into the current summary; see `hooks/loop-write-validator.sh:190-198`. Directory invariant requires target path to equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`; see `hooks/loop-write-validator.sh:250-265`.
- dependencies_and_callers: Registered for PreToolUse matcher `Write` in `hooks/hooks.json:14-22`. Depends on `hooks/lib/loop-common.sh` for JSON validation, path classifiers, active-loop lookup, strict state parsing, PR-loop classifiers, and block-message helpers; see `hooks/loop-write-validator.sh:17-20`. Also depends on templates under `prompt-template/block/` through `load_and_render_safe` in `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: Malformed JSON, null bytes, invalid UTF-8, missing `tool_input.file_path`, or deeply nested JSON fail closed. If there is no active loop, most RLCR protections exit 0 so normal project writes are not blocked; see `hooks/loop-write-validator.sh:131-133`. PR-loop file paths are handled before RLCR summary logic to avoid cross-loop misclassification; see `hooks/loop-write-validator.sh:73-97`. Finalize-state detection sets `IS_FINALIZE_PHASE`, but the current file does not use that variable beyond classification; effective finalize behavior is enforced by active state resolution and finalize summary path checks. Path matching uses string and regex checks, so unusual equivalent paths not normalized in this script may be caught by the final exact-path comparison rather than accepted.
- validation_or_tests: Covered by `hooks/hooks.json` integration and tests such as `tests/test-allowlist-validators.sh`, `tests/test-plan-file-hooks.sh`, `tests/robustness/test-hook-input-robustness.sh`, and broader hook/system tests listed under `tests/`. The validator’s helper functions have explicit template fallbacks and JSON validation in `hooks/lib/loop-common.sh:76-150` and `hooks/lib/template-loader.sh:54-131`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-063 `file` `tests/test-monitor-e2e-deletion.sh`
- cursor: `[_]`
- core_role: Thin executable test shard for monitor deletion behavior. It is core as an end-to-end specification that `.humanize` loop monitor functions must stop cleanly when their backing loop directories disappear.
- algorithmic_behavior: Sources `tests/test-monitor-e2e-real.sh`, prints a section header, runs three deletion tests, then exits 0 only if `TESTS_FAILED` is zero; see `tests/test-monitor-e2e-deletion.sh:4-22`. The sourced tests run real monitor functions from `scripts/humanize.sh`, not stubs, for bash RLCR deletion, zsh RLCR deletion, and PR-loop deletion; see `tests/test-monitor-e2e-real.sh:3-13`, `tests/test-monitor-e2e-real.sh:56-228`, `tests/test-monitor-e2e-real.sh:233-370`, `tests/test-monitor-e2e-real.sh:691-820`.
- inputs_outputs_state: Inputs are test-local fake projects under `/tmp/test-monitor-e2e-real-$$`, generated `.humanize/rlcr/.../state.md` or `.humanize/pr-loop/.../state.md`, generated goal trackers, fake cache directories, and sourced `scripts/humanize.sh`. Outputs are pass/fail counters printed to stdout and shell exit status. During tests, the backing `.humanize/rlcr` or `.humanize/pr-loop/<timestamp>` directory is deleted to trigger monitor shutdown; see `tests/test-monitor-e2e-real.sh:60-81`, `tests/test-monitor-e2e-real.sh:164-181`, `tests/test-monitor-e2e-real.sh:241-263`, `tests/test-monitor-e2e-real.sh:332-348`, `tests/test-monitor-e2e-real.sh:696-720`, `tests/test-monitor-e2e-real.sh:790-813`.
- gates_or_invariants: Monitors must exit within bounded wait loops, emit “Monitoring stopped:” or an equivalent clean stop signal, explain that the directory no longer exists for RLCR deletion, avoid shell glob errors such as “no matches found” or “bad pattern”, restore terminal cleanup state, and exit with status 0; see `tests/test-monitor-e2e-real.sh:167-227` and `tests/test-monitor-e2e-real.sh:351-370`. The bash RLCR test also checks that `scripts/humanize.sh` contains a scroll-region reset escape; see `tests/test-monitor-e2e-real.sh:215-220`.
- dependencies_and_callers: Depends on `tests/test-monitor-e2e-real.sh`, `scripts/humanize.sh`, bash, optionally zsh, terminal-command shims for noninteractive testing, and fake HOME/XDG cache layout. It is likely called by broader test runners such as `tests/run-all-tests.sh` or split CI invocations; the file itself is explicitly labeled “parallel split 1/3” at `tests/test-monitor-e2e-deletion.sh:2`.
- edge_cases_or_failure_modes: If zsh is unavailable, the zsh deletion test reports skip inside the sourced function rather than failing; see `tests/test-monitor-e2e-real.sh:238-240`. If a monitor hangs after deletion, the test kills it and fails; see `tests/test-monitor-e2e-real.sh:174-180`, `tests/test-monitor-e2e-real.sh:342-348`. PR deletion test accepts a broader clean-exit indication because it checks for `Stopped`, `gracefully`, or `EXIT_CODE:0`; see `tests/test-monitor-e2e-real.sh:811-820`.
- validation_or_tests: This file is itself validation. It exercises real monitor behavior across bash, zsh, and PR-loop paths and enforces user-friendly deletion shutdown plus terminal cleanup.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-093 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Block-message template fragment used when the RLCR stop hook detects untracked files that are not `.humanize` local state. It shapes the gate response for the “git not clean” invariant before expensive Codex review or loop advancement.
- algorithmic_behavior: The template contains a short note telling the user that untracked files may be build artifacts, test outputs, runtime files, dependency directories, IDE files, logs, caches, or temp files, and that appropriate patterns should usually be added to `.gitignore`; see `prompt-template/block/git-not-clean-untracked.md:2-10`. It has no placeholders and no executable logic. The caller loads it only after cached git status reports untracked files excluding `.humanize`; see `hooks/loop-codex-stop-hook.sh:549-568`.
- inputs_outputs_state: Input is implicit template loading from `$TEMPLATE_DIR/block/git-not-clean-untracked.md` via `load_template`; output is markdown text appended to `SPECIAL_NOTES` in the stop hook. It does not mutate state. If the template is missing or empty, the stop hook falls back to “Review untracked files - add to .gitignore or commit them.”; see `hooks/loop-codex-stop-hook.sh:563-568`.
- gates_or_invariants: Reinforces the clean-git gate: if `GIT_STATUS_CACHED` is non-empty, `GIT_ISSUES` becomes “uncommitted changes” and the hook blocks progression; untracked files add explanatory notes, not a separate bypass; see `hooks/loop-codex-stop-hook.sh:540-573`. The template distinguishes generated artifacts from source changes and nudges toward ignore rules rather than blanket committing.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_template`, which is sourced from `hooks/lib/template-loader.sh`. Related sibling templates include `git-not-clean.md` for the overall block and `git-not-clean-humanize-local.md` for `.humanize` local state.
- edge_cases_or_failure_modes: Because this template is generic and has no variable interpolation, it cannot name the actual untracked paths. It is appended only for “other untracked” after `.humanize` entries are filtered out; if both `.humanize` and other untracked files exist, both special notes can be appended.
- validation_or_tests: Referenced by template coverage tests such as `tests/test-template-references.sh` and dirty-git behavior tests like `tests/robustness/test-git-operations-robustness.sh`. Direct caller path is evidenced in `hooks/loop-codex-stop-hook.sh:561-568`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-123 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template for an exceptional RLCR transition where code review was skipped. It defines the operator contract for manual verification and functionality-equivalent cleanup before final loop completion.
- algorithmic_behavior: Renders a “Finalize Phase (Review Skipped)” prompt with `{{REVIEW_SKIP_REASON}}`, tells the user the implementation could not be fully validated, asks for manual review/tests/common quality checks, optionally suggests the `code-simplifier:code-simplifier` agent, constrains changes to behavior-preserving cleanup, references original plan and goal tracker, and requires finalize summary output at `{{FINALIZE_SUMMARY_FILE}}`; see `prompt-template/claude/finalize-phase-skipped-prompt.md:1-50`.
- inputs_outputs_state: Inputs are template variables `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a rendered prompt returned as a stop-hook block reason. The caller first renames `state.md` to `finalize-state.md`, then renders this template when `skip_reason` is non-empty; see `hooks/loop-codex-stop-hook.sh:1065-1110`. Finalize completion later renames `finalize-state.md` to `complete-state.md` after checks pass; see `hooks/loop-codex-stop-hook.sh:762-773`.
- gates_or_invariants: Non-negotiable constraints are no functionality changes, no existing test failures, no new bugs, and only functionality-equivalent changes; see `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`. Before exit, all tasks must be completed, changes committed, and finalize summary written; see `prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`. The stop hook’s normal review path does not skip review on command failure: review failure blocks; skipped-finalize is a distinct branch entered only when the caller passes a skip reason; see `hooks/loop-codex-stop-hook.sh:1040-1062`, `hooks/loop-codex-stop-hook.sh:1077-1110`.
- dependencies_and_callers: Called by `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh`. It depends on template rendering from `hooks/lib/template-loader.sh`, the active RLCR loop state variables, and sibling normal finalize template `prompt-template/claude/finalize-phase-prompt.md`.
- edge_cases_or_failure_modes: If the template is missing, the stop hook has an inline fallback with the same key constraints and summary requirement; see `hooks/loop-codex-stop-hook.sh:1077-1104`. Because this path warns that review was skipped, it creates residual risk: the prompt relies on manual verification and available tests instead of an enforced successful code review.
- validation_or_tests: Covered by finalize-phase tests such as `tests/test-finalize-phase.sh`, template reference tests, and stop-hook tests. Direct runtime integration is in `hooks/loop-codex-stop-hook.sh:1065-1145`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-153 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR plan-file validation in `scripts/setup-rlcr-loop.sh`. It ensures startup accepts practical valid plans, rejects insufficient plans, and handles path/content edge cases without fragile shell failures.
- algorithmic_behavior: Creates an isolated temp directory, isolated XDG cache, mock `codex`, mock git repo with a committed README and `.gitignore`, then defines `test_plan_validation` to run production `setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR=$TEST_DIR`; see `tests/robustness/test-plan-file-robustness.sh:15-40`, `tests/robustness/test-plan-file-robustness.sh:46-106`, `tests/robustness/test-plan-file-robustness.sh:108-123`. The helper classifies validation failure by matching production error messages, classifies success by Codex availability/gitignore/loop-created markers, and treats nonzero unexpected exits as failure; see `tests/robustness/test-plan-file-robustness.sh:64-105`.
- inputs_outputs_state: Inputs are generated plan files under `$TEST_DIR`, production script `scripts/setup-rlcr-loop.sh`, mock `codex`, and git state. Outputs are pass/fail messages through `test-helpers.sh` and the final summary exit code; see `tests/robustness/test-plan-file-robustness.sh:124-127`, `tests/robustness/test-plan-file-robustness.sh:553-558`. It repeatedly removes `$TEST_DIR/.humanize/rlcr` for consistent validation state; see `tests/robustness/test-plan-file-robustness.sh:56-62`.
- gates_or_invariants: Production must accept correctly formatted plans, minimum-content plans with exactly 3 content lines, standard 5KB plans, rich markdown, large 1MB-plus plans, and mixed line endings; see `tests/robustness/test-plan-file-robustness.sh:178-290`, `tests/robustness/test-plan-file-robustness.sh:325-366`. It must reject empty plans, comments-only plans, and missing files via production validation; see `tests/robustness/test-plan-file-robustness.sh:300-323`, `tests/robustness/test-plan-file-robustness.sh:527-551`. Local helper checks specify how content lines exclude blanks, single-line and multiline HTML comments, and shell/YAML-style comments; see `tests/robustness/test-plan-file-robustness.sh:129-169`. Production counterpart enforces the same minimum line and content-line logic in `scripts/setup-rlcr-loop.sh:519-572`.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `tests/test-helpers.sh`, git, bash, `mktemp`, and the mock `codex` injected into PATH. It indirectly exercises setup-script dependencies such as `hooks/lib/loop-common.sh` and project git status.
- edge_cases_or_failure_modes: Some edge tests are observational helper checks rather than full production validation: binary content, very long lines, special shell characters in content, whitespace-only content count, nested HTML comments, unreadable file detection, symlink detection, directory instead of file, and null-byte line counting; see `tests/robustness/test-plan-file-robustness.sh:368-525`. The unreadable-file case tolerates root-like environments where the file remains readable; see `tests/robustness/test-plan-file-robustness.sh:477-489`. The large-file test protects against SIGPIPE from `sed | head` style pipelines; see `tests/robustness/test-plan-file-robustness.sh:325-327`. The production script still rejects symlink plan files and symlink parents directly; see `scripts/setup-rlcr-loop.sh:382-416`.
- validation_or_tests: This file is validation. It provides positive, negative, and edge coverage for plan validation and should be run as part of robustness test suites. In this read-only branch export I inspected it but did not execute it or modify any files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: six item sections above, one per assigned row, in the scheduler-provided order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`