# agent_14 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-014 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation template directory. Recursively contains one child, `prompt-template/plan/gen-plan-template.md`, which defines the canonical structure that `gen-plan` seeds before the draft is analyzed and rewritten into a final implementation plan.
- algorithmic_behavior: The template prescribes required plan sections: goal description, acceptance criteria with deterministic positive/negative tests, path boundaries, feasibility hints, dependencies/sequence, and implementation notes (`prompt-template/plan/gen-plan-template.md:1`, `:6`, `:25`, `:46`, `:57`, `:69`). It is not executable itself, but it is algorithmically active because `scripts/validate-gen-plan-io.sh` locates this exact file, copies it to the requested output, and appends the original draft between sentinel markers (`scripts/validate-gen-plan-io.sh:133`, `:148`, `:151`).
- inputs_outputs_state: Input is a user draft passed through `commands/gen-plan.md` and validated by `scripts/validate-gen-plan-io.sh`; output is a new plan file initialized from `gen-plan-template.md` plus the original draft content (`commands/gen-plan.md:30`, `scripts/validate-gen-plan-io.sh:149`, `:153`). No persistent state is stored in this directory; state transition happens in the output file from “nonexistent” to “template plus draft” after IO validation succeeds.
- gates_or_invariants: The template encodes invariants expected downstream: ACs must include positive and negative deterministic tests (`prompt-template/plan/gen-plan-template.md:6`), path boundaries must constrain acceptable implementation scope (`:25`), deterministic designs should narrow allowed choices (`:44`), feasibility hints are non-prescriptive (`:46`), and implementation code/comments must not contain plan workflow markers like `AC-`, `Milestone`, `Step`, or `Phase` (`:71`).
- dependencies_and_callers: Primary caller is `scripts/validate-gen-plan-io.sh`, invoked by `commands/gen-plan.md` Phase 1 (`commands/gen-plan.md:30`, `:35`). The command then performs relevance checking, draft analysis, clarification, and final plan generation (`commands/gen-plan.md:50`, `:76`, `:111`, `:142`). Tests for the surrounding command and validator live in `tests/test-gen-plan.sh`, including validation-script success and failure cases (`tests/test-gen-plan.sh:426`, `:523`).
- edge_cases_or_failure_modes: Missing template is treated as plugin configuration error exit code 7 (`scripts/validate-gen-plan-io.sh:142`). The validator blocks missing input, empty input, nonexistent output directory, existing output path, output path as directory, and unwritable output directory before the template is copied (`scripts/validate-gen-plan-io.sh:79`, `:87`, `:95`, `:103`, `:111`, `:118`). `realpath -m` is attempted but falls back to the original path if unavailable (`:69`).
- validation_or_tests: `tests/test-gen-plan.sh` validates command structure and script behavior, including invalid argument exits, missing input, empty input, nonexistent output dir, existing output, directory output, and valid path success (`tests/test-gen-plan.sh:438`, `:474`, `:483`, `:493`, `:503`, `:513`, `:523`). It does not deeply validate the semantic contents of `gen-plan-template.md`; it validates the pipeline around it.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-044 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for `/humanize:start-pr-loop`. It creates the `.humanize/pr-loop/<timestamp>/` state surface, detects the active PR, fetches current review context, decides startup behavior, optionally posts bot re-review triggers, and emits the first round prompt.
- algorithmic_behavior: The script sets PR-loop defaults for Codex model, effort, timeout, max iterations, polling interval, and Git/GitHub timeouts (`scripts/setup-pr-loop.sh:17`, `:23`, `:27`). It parses `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout`, rejecting unknown or malformed arguments (`:111`, `:124`, `:136`, `:151`, `:163`). It requires at least one bot flag and builds active bot state (`:180`, `:197`). It enforces mutual exclusion with active RLCR or PR loops before initialization (`:220`, `:225`, `:234`). It validates git, `gh`, authentication, and `codex` availability (`:243`, `:249`, `:255`, `:263`, `:271`). PR detection resolves fork/base-repo context by trying `gh pr view`, parsing the PR URL, and falling back to parent repo with `FORK_OWNER:BRANCH` lookup (`:283`, `:296`, `:312`, `:323`, `:337`, `:344`). Closed or merged PRs are rejected (`:365`, `:367`, `:371`).
- inputs_outputs_state: Inputs are CLI flags, current branch, GitHub CLI repository/PR metadata, active bot list, template files, and output from helper scripts. Outputs are `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-pr-resolve.md` path contract, `round-0-prompt.md`, and terminal prompt text (`scripts/setup-pr-loop.sh:413`, `:425`, `:615`, `:641`, `:721`, `:761`, `:860`). The state file records `current_round: 0`, `max_iterations`, `pr_number`, `start_branch`, configured and active bot lists, Codex settings, poll settings, startup case, latest commit metadata, trigger timestamp, and trigger comment id (`:615`-`:634`).
- gates_or_invariants: Branch names cannot contain YAML-unsafe characters `: # " ' \`` or newline before being written to YAML (`scripts/setup-pr-loop.sh:384`). Codex model and effort are restricted to safe character classes (`:393`, `:401`). Bot mention detection strips fenced code, tilde fences, inline code, indented code, and quoted lines before deciding whether an existing trigger mentions all active bots (`:478`-`:499`). Duplicate trigger comments are avoided when a valid existing trigger is found after the latest commit (`:518`). New trigger comment failure aborts and removes the just-created loop directory to avoid orphaned state (`:525`, `:536`). Claude mode requires a verified eyes reaction on the trigger comment; missing trigger id or missing eyes reaction also cleans up and exits (`:564`, `:568`, `:587`, `:596`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` for `run_with_timeout` (`scripts/setup-pr-loop.sh:33`; implementation in `scripts/portable-timeout.sh:33`), `hooks/lib/template-loader.sh` for `get_template_dir` and `load_and_render_safe` (`scripts/setup-pr-loop.sh:37`; `hooks/lib/template-loader.sh:26`, `:170`), and `hooks/lib/loop-common.sh` for active-loop discovery, YAML lists, and bot mention strings (`scripts/setup-pr-loop.sh:39`; `hooks/lib/loop-common.sh:883`, `:895`, `:926`). It invokes `scripts/fetch-pr-comments.sh` to create the initial comment markdown (`scripts/setup-pr-loop.sh:430`), `scripts/check-pr-reviewer-status.sh` to classify startup case (`:437`), and `scripts/check-bot-reactions.sh` for Claude eyes validation (`:587`). PR-loop templates replace inline fallbacks for goal tracker, round-0 header/task, and critical requirements (`:712`, `:758`, `:800`, `:847`, `:914`, `:940`).
- edge_cases_or_failure_modes: No bot flag, unknown option, missing option argument, and nonnumeric numeric options are hard failures (`scripts/setup-pr-loop.sh:124`, `:151`, `:163`, `:180`). Git command timeout or missing commit fails setup (`:243`, `:249`). Missing PR fails with creation guidance (`:351`). Invalid PR number from `gh` is detected twice, both during auto-detection and after fallbacks (`:318`, `:359`). Fork PRs are explicitly supported through PR URL parsing and parent repo lookup (`:292`, `:323`, `:334`). Startup status failure degrades to Case 1 with a warning rather than aborting (`:437`-`:441`). If GitHub timestamp fetch for a posted trigger fails, it deliberately does not fall back to local time to avoid clock-skew filtering bugs (`:558`). Comment existence is binary, based on the exact `*No comments found.*` sentinel in the fetched comment file (`:723`).
- validation_or_tests: Direct script argument tests are in `tests/test-pr-loop-scripts.sh`: help output, missing bot flag, invalid bot flag, `--claude`, `--codex`, both bots, `--max`, invalid `--max`, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:27`, `:38`, `:52`, `:66`, `:80`, `:92`, `:104`, `:117`, `:131`, `:143`). The split runner `tests/test-pr-loop-1-scripts.sh` sources the harness, initializes mocks, sources this test file, and runs `run_script_tests` (`tests/test-pr-loop-1-scripts.sh:14`, `:17`, `:19`, `:21`). Mocks for `gh` and `codex` are provided by `tests/test-pr-loop-lib.sh` (`tests/test-pr-loop-lib.sh:27`, `:90`, `:107`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-074 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable specification for basic PR-loop script CLI parsing and validation. It covers `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh` at the command boundary.
- algorithmic_behavior: Defines `run_setup_tests`, `run_cancel_tests`, `run_fetch_tests`, `run_poll_tests`, and aggregate `run_script_tests` (`tests/test-pr-loop-scripts.sh:18`, `:172`, `:259`, `:332`, `:405`). Each nested test runs a real script, captures stdout/stderr and exit code, then calls shared `pass` or `fail` helpers according to text and exit-code expectations (`:29`, `:40`, `:54`, `:183`, `:281`, `:355`). It intentionally tolerates later environmental failures for positive setup flag parsing by asserting that the specific parse error did not occur, rather than requiring full setup success (`:66`, `:80`, `:92`, `:104`, `:131`, `:143`).
- inputs_outputs_state: Inputs are `PROJECT_ROOT`, `TEST_DIR`, shared `pass`/`fail` functions, and scripts under `$PROJECT_ROOT/scripts` (`tests/test-pr-loop-scripts.sh:25`, `:179`, `:266`, `:339`). Outputs are test-result counter mutations through `pass`/`fail` and console diagnostics; the file itself does not exit. For cancel tests, it mutates a temporary `.humanize/pr-loop/<timestamp>/state.md` fixture and expects it to become `cancel-state.md` (`:217`, `:222`, `:236`).
- gates_or_invariants: Setup script must reject missing bot flags and unknown options (`tests/test-pr-loop-scripts.sh:38`, `:52`); valid bot flags must suppress the missing-bot error (`:66`, `:80`, `:92`); `--max abc` must fail with an integer error (`:117`); cancel with no active loop must return exit 1 and `NO_LOOP` (`:192`); cancel with active loop must return `CANCELLED` and rename state (`:211`, `:236`); fetch must reject missing PR number, missing output file, and invalid PR number (`:279`, `:293`, `:307`); poll must reject missing PR number, missing `--after`, and missing `--bots` (`:352`, `:366`, `:380`).
- dependencies_and_callers: Intended to be sourced, not executed standalone: header usage says `source test-pr-loop-scripts.sh && run_script_tests` (`tests/test-pr-loop-scripts.sh:11`). It is sourced by the full PR-loop runner `tests/test-pr-loop.sh` (`tests/test-pr-loop.sh:32`, `:41`) and split runner `tests/test-pr-loop-1-scripts.sh` (`tests/test-pr-loop-1-scripts.sh:19`, `:21`). It depends on harness setup in `tests/test-pr-loop-lib.sh`, which creates mock `gh` and `codex` and exports them on `PATH` (`tests/test-pr-loop-lib.sh:107`, `:112`, `:117`), plus shared `pass` and `fail` from `tests/test-helpers.sh` (`tests/test-helpers.sh:30`, `:35`).
- edge_cases_or_failure_modes: Several positive setup tests only prove argument parsing proceeds past a specific parse gate; they do not prove full PR-loop setup succeeds, because the script may fail later due to git/PR/GitHub context (`tests/test-pr-loop-scripts.sh:68`). The mock `gh` implementation in the harness is shallow and may not emulate all JSON shapes used by newer setup paths (`tests/test-pr-loop-lib.sh:43`-`:80`). Fetch and poll tests only cover argument validation, not API pagination, deduplication, bot grouping, or timestamp filtering.
- validation_or_tests: This file is itself validation. It maps to CLI contracts in the scripts: `cancel-pr-loop.sh` help/no-loop/active-loop behavior (`scripts/cancel-pr-loop.sh:32`, `:78`, `:91`, `:118`), `fetch-pr-comments.sh` required args and numeric PR validation (`scripts/fetch-pr-comments.sh:45`, `:92`, `:98`, `:103`), and `poll-pr-reviews.sh` required `--after`, `--bots`, and numeric PR validation (`scripts/poll-pr-reviews.sh:41`, `:83`, `:89`, `:94`, `:99`). No tests were run in this read-only research turn.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-104 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Blocking message template for RLCR plan-file integrity violations. It defines the user-facing gate when the active plan differs from the backup captured at loop start.
- algorithmic_behavior: The template renders `{{PLAN_FILE}}` and `{{BACKUP_PATH}}`, states that modifying plan files is forbidden during an active session, and prescribes the cancel-update-restart sequence (`prompt-template/block/plan-file-modified.md:3`, `:5`, `:7`, `:12`). It has no branching itself; behavior is determined by the stop hook that loads it.
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH` variables supplied by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:332`-`:334`). Output is a markdown block embedded into a JSON hook response with decision `block` and system message `Loop: Blocked - plan file modified` (`hooks/loop-codex-stop-hook.sh:335`). State does not advance; the active loop remains blocked until cancellation or plan restoration.
- gates_or_invariants: Two integrity checks lead into this gate. For tracked plans started with `--track-plan-file`, uncommitted changes block first using an inline reason (`hooks/loop-codex-stop-hook.sh:302`-`:315`). Then a content diff between the full plan path and backup plan blocks via this template (`:318`-`:337`). The invariant is that plan requirements cannot mutate while an RLCR loop is active.
- dependencies_and_callers: Loaded through `load_and_render_safe`, so if the template is missing the hook uses an inline fallback with the same core message (`hooks/loop-codex-stop-hook.sh:320`-`:332`; loader at `hooks/lib/template-loader.sh:170`). It coordinates with `scripts/setup-rlcr-loop.sh`, which creates the loop’s plan backup and state, and with plan-file validators that prevent tool writes elsewhere in the hook system.
- edge_cases_or_failure_modes: The surrounding hook comment notes “Plan changes are now allowed: plan.md is a symlink to the original, so this diff always passes” immediately before the diff gate (`hooks/loop-codex-stop-hook.sh:318`). That comment appears inconsistent with the actual `diff -q` block that still renders this template on differences (`:319`-`:337`), so this path is a potential stale-comment/confusing-invariant area. If template rendering fails, fallback preserves the block behavior (`:320`-`:332`).
- validation_or_tests: Plan-file validation tests exist in the suite, including `tests/test-plan-file-validation.sh` and `tests/test-plan-file-hooks.sh` discovered in the repository, but this turn inspected the direct caller rather than executing tests. The key runtime validation is in `hooks/loop-codex-stop-hook.sh:302`-`:337`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-134 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex regular-review prompt template for RLCR stop-hook review rounds that are not full alignment checkpoints. It defines the review contract that determines whether Claude continues work or the loop may progress toward completion.
- algorithmic_behavior: The template instructs Codex to read the original plan first (`prompt-template/codex/regular-review.md:3`-`:9`), compare Claude’s completion claim against the plan and current prompt (`:11`), conduct a skeptical implementation review for gaps, deferrals, and incomplete work (`:20`-`:31`), perform a mandatory goal alignment check against `goal-tracker.md` (`:33`-`:45`), apply the inserted goal-tracker update section (`:47`), and write findings unless everything is complete (`:49`-`:57`). The last-line `COMPLETE` marker is explicitly reserved for the all-done/no-deferrals/no-pending condition (`:53`-`:57`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, passed by the stop hook (`hooks/loop-codex-stop-hook.sh:860`-`:872`). Output is `round-N-review-prompt.md`, which drives Codex review output into `round-N-review-result.md` (`:787`-`:789`, `:860`-`:872`). If review finds issues, the loop continues with a new prompt and updated round; if Codex review phase passes with no issues, the hook enters finalize phase (`hooks/loop-codex-stop-hook.sh:1055`-`:1062`, `:1067`-`:1072`).
- gates_or_invariants: The prompt treats deferred and unfinished work as incomplete and forbids `COMPLETE` when any such work remains (`prompt-template/codex/regular-review.md:24`, `:28`, `:53`-`:56`). It requires AC accounting, forgotten-item detection, deferral audit, and plan-evolution validation (`:35`-`:45`). The stop hook separately decides regular versus full-alignment review based on `current_round % full_review_round`, defaulting invalid `full_review_round` to 5 (`hooks/loop-codex-stop-hook.sh:799`-`:809`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` when `FULL_ALIGNMENT_CHECK` is false (`hooks/loop-codex-stop-hook.sh:842`, `:857`, `:860`). It embeds `prompt-template/codex/goal-tracker-update-section.md`, which makes Codex responsible for evaluating and applying justified goal-tracker update requests while never modifying the immutable goal/AC section (`prompt-template/codex/goal-tracker-update-section.md:1`-`:11`). It is a sibling/variant of `prompt-template/codex/full-alignment-review.md`, which is used at scheduled comprehensive checkpoints (`prompt-template/codex/full-alignment-review.md:1`, `:57`).
- edge_cases_or_failure_modes: The prompt assumes Codex can inspect the plan, prompt, goal tracker, docs, and codebase; missing or stale files would weaken the review but are not handled inside the template. The template says review comments can be written to `@{{REVIEW_RESULT_FILE}}`, but later hook logic also runs automated `codex review --base` and detects severity markers rather than relying only on the textual prompt (`hooks/loop-codex-stop-hook.sh:951`, `:1012`, `:1046`). This creates a split contract: the prompt governs human-readable review expectations, while the hook’s final transition depends on the hook’s review-detection path.
- validation_or_tests: Template-reference and comprehensive-template tests exist in `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`; direct render/caller behavior is anchored in `hooks/loop-codex-stop-hook.sh:793`-`:872`. No tests were run in this read-only research turn.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_CODEX_SKILL-HZ-014, ASK_CODEX_SKILL-HZ-044, ASK_CODEX_SKILL-HZ-074, ASK_CODEX_SKILL-HZ-104, ASK_CODEX_SKILL-HZ-134
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`