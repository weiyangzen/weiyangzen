# agent_11 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`
- note: local export is read-only and does not include `.git`; source commit is taken from scheduler metadata.

## Item Evidence

### ASK_CODEX_SKILL-HZ-011 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Block-message template directory for hook-enforced workflow gates. These markdown files are not executable by themselves, but they define the user-facing contract when validators, stop hooks, and PR-loop hooks block or redirect an action.
- algorithmic_behavior: The directory contains 32 direct child `.md` files and no nested descendants. Templates are loaded through `hooks/lib/template-loader.sh`, where `load_template` reads `prompt-template/<name>` and `load_and_render_safe` falls back if the template is missing or renders empty (`hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:170`). Placeholders use `{{VAR}}` single-pass substitution (`hooks/lib/template-loader.sh:58`).
- inputs_outputs_state: Inputs are template variables supplied by hook code, such as `CURRENT_ROUND`, `CORRECT_PATH`, `MAX_LINES`, `LARGE_FILES`, `AHEAD_COUNT`, `CURRENT_BRANCH`, `BOT_MENTION_STRING`, and `SUMMARY_FILE`. Output is rendered markdown inserted into JSON hook responses as `reason` or printed from validators. State changes are made by callers, not templates; examples include PR state rewrites in `hooks/pr-loop-stop-hook.sh:396` and RLCR state transitions in `hooks/loop-codex-stop-hook.sh:1071`.
- gates_or_invariants: Directory-level invariant is that every referenced block template should exist and be safe to render. `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:208`). `tests/test-template-references.sh` checks referenced templates and safe loader usage. The actual gates include wrong round/path, read-only loop files, `.humanize` git-add protection, dirty git state, unpushed commits, large files, missing trigger comments, force-push reset, Claude-eyes timeout, and Codex review failure.
- dependencies_and_callers: Core callers include `hooks/loop-read-validator.sh` for wrong file/round/directory read blocks; `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` for write/edit path guards; `hooks/loop-bash-validator.sh` and `hooks/lib/loop-common.sh` for bash/git/goal-tracker blocks; `hooks/loop-codex-stop-hook.sh` for stop, git, large-file, summary, goal-tracker, and review-failure blocks; and `hooks/pr-loop-stop-hook.sh` for PR-trigger, force-push, unpushed, and Claude-eyes blocks.
- edge_cases_or_failure_modes: Missing or empty templates fall back through `load_and_render_safe` (`hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:192`). Placeholder values are not recursively expanded, preventing injected `{{...}}` in review content from corrupting prompts (`hooks/lib/template-loader.sh:54`). Some templates are advisory notes loaded with `load_template`, so if missing they degrade to hardcoded notes, for example `.humanize` and untracked-file notes in `hooks/loop-codex-stop-hook.sh:554` and `hooks/loop-codex-stop-hook.sh:564`.
- validation_or_tests: Template coverage is exercised by `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and robustness coverage in `tests/robustness/test-template-error-robustness.sh`. Specific stop-hook consumers are exercised by `tests/test-pr-loop-3-stophook.sh` through `tests/test-pr-loop-stophook.sh`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-041 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell utility exposing `humanize monitor rlcr` and `humanize monitor pr`. It implements live monitoring, status parsing, terminal rendering, log following, session switching, and cleanup for RLCR and PR loop workflows.
- algorithmic_behavior: Starts by sourcing shared monitor and loop libraries (`scripts/humanize.sh:6`, `scripts/humanize.sh:12`). Public helpers parse pipe-delimited arrays, goal-tracker state, git repository state, and git status (`scripts/humanize.sh:24`, `scripts/humanize.sh:38`, `scripts/humanize.sh:122`, `scripts/humanize.sh:172`). `_humanize_monitor_codex` locates the latest `.humanize/rlcr` session, finds the matching cache log, draws a fixed status bar, tails log deltas, and switches to newer sessions/logs (`scripts/humanize.sh:225`, `scripts/humanize.sh:255`, `scripts/humanize.sh:775`). `_humanize_monitor_pr` performs the analogous PR-loop monitor with optional `--once` (`scripts/humanize.sh:1151`, `scripts/humanize.sh:1162`, `scripts/humanize.sh:1431`).
- inputs_outputs_state: Inputs are the current working directory, `.humanize/rlcr` or `.humanize/pr-loop`, loop `state.md`/terminal state files, `goal-tracker.md`, project cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, terminal size, git status, and CLI args (`monitor rlcr`, `monitor pr`, `--once`). Outputs are terminal UI/status lines and tailed log content. It does not write authoritative loop state; it reads state and performs terminal cleanup.
- gates_or_invariants: RLCR log lookup is constrained to the current latest session to avoid showing stale logs (`scripts/humanize.sh:261`). It rejects inconsistent phase ordering when a `codex-run` round is greater than or equal to a review round (`scripts/humanize.sh:328`). It requires sufficient terminal height before split-view rendering (`scripts/humanize.sh:600`). Signal handlers must restore terminal state and kill background tails (`scripts/humanize.sh:675`, `scripts/humanize.sh:1392`).
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for session discovery, file size, state-file detection, PR phase labels, and PR goal-tracker parsing. Depends on `hooks/lib/loop-common.sh` for constants such as `DEFAULT_CODEX_MODEL` when available. Exposed command dispatch is `humanize monitor <rlcr|pr>` (`scripts/humanize.sh:1104`).
- edge_cases_or_failure_modes: Handles missing loop dirs with explicit errors (`scripts/humanize.sh:236`, `scripts/humanize.sh:1175`); missing logs with centered waiting messages (`scripts/humanize.sh:820`); deleted loop/session/log paths with graceful stop or session switch (`scripts/humanize.sh:777`, `scripts/humanize.sh:1012`); truncated/rotated logs by resetting search state (`scripts/humanize.sh:994`); terminal resize via deferred `SIGWINCH` flag to avoid rendering races (`scripts/humanize.sh:716`); bash/zsh trap differences (`scripts/humanize.sh:724`, `scripts/humanize.sh:1420`).
- validation_or_tests: Monitor behavior is covered by `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-monitor-e2e-deletion.sh`, `tests/test-monitor-e2e-sigint.sh`, `tests/test-zsh-monitor-safety.sh`, and inclusion in `tests/run-all-tests.sh`. Goal parsing has robustness coverage in `tests/robustness/test-goal-tracker-robustness.sh`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-071 `file` `tests/test-pr-loop-3-stophook.sh`
- cursor: `[_]`
- core_role: Split test runner for the PR-loop stop-hook integration suite. It exists to run the slow stop-hook subset separately from the broader PR-loop test suite.
- algorithmic_behavior: Establishes strict shell mode (`tests/test-pr-loop-3-stophook.sh:10`), resolves `SCRIPT_DIR` and `PROJECT_ROOT` (`tests/test-pr-loop-3-stophook.sh:12`), sources shared test helpers and PR-loop test library (`tests/test-pr-loop-3-stophook.sh:15`), initializes mocks/temp environment (`tests/test-pr-loop-3-stophook.sh:18`), sources the substantive stop-hook tests (`tests/test-pr-loop-3-stophook.sh:20`), invokes `run_stophook_tests` (`tests/test-pr-loop-3-stophook.sh:22`), then prints a suite summary and exits with its status (`tests/test-pr-loop-3-stophook.sh:24`).
- inputs_outputs_state: Inputs are local test files, temporary directories from `setup_test_dir`, mock `gh` and `codex` binaries from `init_pr_loop_test_env`, and the real `hooks/pr-loop-stop-hook.sh` invoked by sourced tests. Outputs are PASS/FAIL lines and a process exit code. It mutates only temporary test directories.
- gates_or_invariants: Uses `set -euo pipefail` so missing sourced dependencies or failing unhandled commands abort the runner. It assumes `tests/test-pr-loop-stophook.sh` defines `run_stophook_tests`.
- dependencies_and_callers: Depends directly on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and `tests/test-pr-loop-stophook.sh`. The sourced suite exercises PR stop-hook behavior: trigger validation, force-push detection, unpushed commits, bot timeout/removal, Codex thumbs-up approval, Claude eyes timeout, fork PR resolution, and mixed approval goal-tracker handling (`tests/test-pr-loop-stophook.sh:1769`).
- edge_cases_or_failure_modes: This runner has minimal logic; failures mostly come from missing helper files, failed mock setup, absent dependencies like `jq`, or stop-hook behavioral regressions in the sourced suite. It is a skip candidate only if the scheduler wants implementation code rather than executable specification; as a core-algorithm spec it is relevant.
- validation_or_tests: It is itself validation. Its substantive assertions include old trigger rejection after force-push timestamps (`tests/test-pr-loop-stophook.sh:129`), no-trigger exception for startup case 1 (`tests/test-pr-loop-stophook.sh:231`), unpushed commit blocking (`tests/test-pr-loop-stophook.sh:529`), force-push blocking (`tests/test-pr-loop-stophook.sh:633`), missing trigger blocking (`tests/test-pr-loop-stophook.sh:745`), Claude eyes timeout blocking (`tests/test-pr-loop-stophook.sh:1136`), and mixed approval non-completion (`tests/test-pr-loop-stophook.sh:1740`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-101 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR large-file gate. It defines the remediation contract when staged code or documentation exceeds the line-count limit.
- algorithmic_behavior: Template renders a heading, the `{{MAX_LINES}}` threshold, a rendered `{{LARGE_FILES}}` list, and separate required actions for code and documentation splitting (`prompt-template/block/large-files.md:1`, `prompt-template/block/large-files.md:3`, `prompt-template/block/large-files.md:13`, `prompt-template/block/large-files.md:19`). The stop hook builds `LARGE_FILES` by inspecting cached git status paths, classifying extensions as code or documentation, counting lines, and comparing against `MAX_LINES=2000` (`hooks/loop-codex-stop-hook.sh:457`, `hooks/loop-codex-stop-hook.sh:486`, `hooks/loop-codex-stop-hook.sh:499`, `hooks/loop-codex-stop-hook.sh:504`).
- inputs_outputs_state: Inputs are `MAX_LINES` and newline-separated file entries such as ``- `path`: N lines (code file)``. Output is a rendered markdown `reason` in a JSON block decision (`hooks/loop-codex-stop-hook.sh:518`, `hooks/loop-codex-stop-hook.sh:522`). The caller exits before Codex review or finalize progression.
- gates_or_invariants: The invariant is “no staged code/doc file over threshold before exit.” The template requires functionality to remain strictly unchanged after splitting (`prompt-template/block/large-files.md:15`) and asks documentation splits to preserve cross-references and narrative continuity (`prompt-template/block/large-files.md:20`).
- dependencies_and_callers: Called only through `hooks/loop-codex-stop-hook.sh` large-file validation. Uses `load_and_render_safe`, so the gate has a hardcoded fallback if this template is unavailable (`hooks/loop-codex-stop-hook.sh:511`).
- edge_cases_or_failure_modes: The hook ignores unknown extensions, files it cannot read, nonnumeric `wc -l` results, and paths absent from cached status. It uses line counts, not file size or generated-file classification. Because this is a stop gate, a false positive blocks loop exit until files are split or unstaged.
- validation_or_tests: Template existence/reference is covered by template-reference/comprehensive tests. The exact gate path is in `hooks/loop-codex-stop-hook.sh:480-531`; broader stop-hook behavior is tested in RLCR hook suites such as `tests/test-plan-file-hooks.sh` and robustness hook tests.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-131 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit template for the RLCR code-review phase. It records how `codex review` is invoked and how the loop interprets review output.
- algorithmic_behavior: Documents that `codex review` does not consume prompt input and reviews git diff directly (`prompt-template/codex/code-review-phase.md:3`). It records base branch, review round, and timestamp (`prompt-template/codex/code-review-phase.md:6`). It defines transition rules: run `codex review --base`, scan for `[P0-9]`, return a fix prompt on issues, otherwise transition to Finalize (`prompt-template/codex/code-review-phase.md:12`). It also declares generated debug/audit files (`prompt-template/codex/code-review-phase.md:30`).
- inputs_outputs_state: Inputs are `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP`; callers also pass `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE`, but this template does not currently display those extra variables. Output is `round-<round>-review-prompt.md` in the loop directory (`hooks/loop-codex-stop-hook.sh:968`, `hooks/loop-codex-stop-hook.sh:983`).
- gates_or_invariants: The review-phase invariant is that Codex review failure is blocking and cannot be skipped. `run_and_handle_code_review` blocks on nonzero `codex review`, blocks if review output is missing, continues the loop if `[P0-9]` issues are found, and enters finalize only when no issues are detected (`hooks/loop-codex-stop-hook.sh:1040`, `hooks/loop-codex-stop-hook.sh:1052`, `hooks/loop-codex-stop-hook.sh:1055`, `hooks/loop-codex-stop-hook.sh:1060`).
- dependencies_and_callers: Consumed by `run_codex_code_review` in `hooks/loop-codex-stop-hook.sh`. That function chooses a fixed base commit when available to avoid comparing a branch to itself (`hooks/loop-codex-stop-hook.sh:956`), writes command/debug files (`hooks/loop-codex-stop-hook.sh:965`, `hooks/loop-codex-stop-hook.sh:993`), and runs `codex review --base "$review_base"` under timeout from `PROJECT_ROOT` (`hooks/loop-codex-stop-hook.sh:1012`).
- edge_cases_or_failure_modes: `codex review` writes to stderr/log rather than a prompt result, so the audit file is informational. Missing template falls back to a shorter generated audit prompt (`hooks/loop-codex-stop-hook.sh:971`). Review failure is surfaced through `prompt-template/block/codex-review-failed.md` via `block_review_failure` (`hooks/loop-codex-stop-hook.sh:1201`, `hooks/loop-codex-stop-hook.sh:1252`).
- validation_or_tests: Review-phase and finalize behavior are covered by `tests/test-codex-review-merge.sh`, `tests/test-finalize-phase.sh`, template-reference tests, and comprehensive template rendering tests. The expected `[P0-9]` format is embedded in this template (`prompt-template/codex/code-review-phase.md:19`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-161 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the template loader under failure and malformed-input conditions. It protects the hook system from template errors becoming hook crashes.
- algorithmic_behavior: Runs in strict mode, sources `hooks/lib/template-loader.sh` and shared test helpers, creates a temporary test directory, then executes 20 tests grouped by missing templates, malformed syntax, invalid names, variable-value edge cases, filesystem edge cases, concurrent loading, and discovery (`tests/robustness/test-template-error-robustness.sh:13`, `tests/robustness/test-template-error-robustness.sh:17`, `tests/robustness/test-template-error-robustness.sh:20`).
- inputs_outputs_state: Inputs are synthetic template strings/files and variable assignments. Outputs are PASS/FAIL counters through `pass`, `fail`, and final `print_test_summary` (`tests/robustness/test-template-error-robustness.sh:340`). State is confined to `TEST_DIR`, including generated empty, BOM, whitespace, permission, concurrent, subdirectory, and symlink templates.
- gates_or_invariants: Missing files and missing directories must use fallback content (`tests/robustness/test-template-error-robustness.sh:34`, `tests/robustness/test-template-error-robustness.sh:43`). Malformed placeholders must not crash and should return nonempty content (`tests/robustness/test-template-error-robustness.sh:73`). Template syntax inside variable values must not recursively expand (`tests/robustness/test-template-error-robustness.sh:165`). Concurrent reads must all succeed and preserve the source file (`tests/robustness/test-template-error-robustness.sh:270`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` functions `load_and_render_safe` and `render_template`, plus `tests/test-helpers.sh`. It complements normal template tests in `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and stress tests in `tests/robustness/test-template-stress-robustness.sh`.
- edge_cases_or_failure_modes: Explicitly covers unclosed/nested/empty placeholders, names with spaces/special chars/numbers/500-character length, values with `{{OTHER}}` and newlines, UTF-8 BOM, whitespace-only content, filenames with spaces, permission-denied files, 10 concurrent loads, subdirectory lookup, and symlinked templates (`tests/robustness/test-template-error-robustness.sh:73`, `tests/robustness/test-template-error-robustness.sh:125`, `tests/robustness/test-template-error-robustness.sh:188`, `tests/robustness/test-template-error-robustness.sh:208`, `tests/robustness/test-template-error-robustness.sh:245`, `tests/robustness/test-template-error-robustness.sh:320`).
- validation_or_tests: This file is executable validation and exits with the helper summary status (`tests/robustness/test-template-error-robustness.sh:341`). It should be run from the repository test harness or directly when changing `hooks/lib/template-loader.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ASK_CODEX_SKILL-HZ-011`, `ASK_CODEX_SKILL-HZ-041`, `ASK_CODEX_SKILL-HZ-071`, `ASK_CODEX_SKILL-HZ-101`, `ASK_CODEX_SKILL-HZ-131`, `ASK_CODEX_SKILL-HZ-161`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`