# agent_09 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-009 `file` `README.md`
- cursor: `[_]`
- core_role: Behavior-defining user documentation for the Humanize Claude Code plugin. It defines the RLCR loop, PR review loop, `gen-plan`, and `ask-codex` user-facing workflows rather than implementing them directly.
- algorithmic_behavior: Defines RLCR as an iterative state machine: user plan -> Claude implementation and summary -> Codex summary review -> repeat until `COMPLETE` -> `codex review --base <branch>` review phase -> issues loop back to Claude or finish when no issues remain (`README.md:99-112`). Defines `gen-plan` as validation/relevance analysis -> user issue resolution -> structured plan with AC acceptance criteria (`README.md:166-183`). Defines PR loop as PR detection, bot comment fetch, Claude fixes, push/re-trigger, stop-hook polling, local Codex validation, and loop termination on bot approval or max iterations (`README.md:223-231`).
- inputs_outputs_state: Inputs include plan/draft files, command options, bot flags, base branch, max iterations, Codex model/effort, timeout, push-every-round, and PR context (`README.md:143-164`, `README.md:185-201`). Outputs include loop artifacts under `.humanize/rlcr/<timestamp>/` and one-shot ask-codex artifacts under `.humanize/skill/<timestamp>/` with `input.md`, `output.md`, and `metadata.md` (`README.md:123-127`, `README.md:220-221`). State transitions include implementation phase, review phase, cancellation, max-iteration stop, skip-implementation direct review, and PR approval completion.
- gates_or_invariants: Requires Codex CLI for review (`README.md:52-55`), GitHub CLI/auth/open PR for PR loop (`README.md:233-236`), Codex `COMPLETE` before review phase, severity markers from `codex review` for issue detection (`README.md:109-112`), and explicit bot selection for PR loop (`README.md:188-193`). `--push-every-round` changes the commit/push contract (`README.md:156`).
- dependencies_and_callers: Describes command entry points `/humanize:start-rlcr-loop`, `/humanize:cancel-rlcr-loop`, `/humanize:gen-plan`, `/humanize:start-pr-loop`, `/humanize:cancel-pr-loop`, and `/humanize:ask-codex` (`README.md:130-140`). Runtime dependencies are Claude Code plugin loading, Codex CLI, GitHub CLI, git branches, and monitor helper sourcing (`README.md:43-55`, `README.md:123-127`).
- edge_cases_or_failure_modes: Documents dangerous sandbox bypass via `HUMANIZE_CODEX_BYPASS_SANDBOX`, including unrestricted filesystem/command access when enabled (`README.md:60-93`). Base branch auto-detection has fallback priority remote default -> `main` -> `master` (`README.md:157-158`). `--skip-impl` makes the plan optional and jumps to review (`README.md:161-162`). PR loop fails preconditions without `gh`, Codex, or an associated open PR (`README.md:233-236`).
- validation_or_tests: README itself is documentation; behavior is validated indirectly by command, hook, and robustness tests elsewhere. The documented PR-loop command surface is specifically covered by `tests/test-pr-loop-1-scripts.sh` through delegated script argument tests.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-039 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop classifier that determines startup/review status cases by inspecting the entire PR comment/review history for configured bots.
- algorithmic_behavior: Parses `<pr_number> --bots <bot1,bot2>` and rejects missing/unknown/multiple PR args (`scripts/check-pr-reviewer-status.sh:49-85`). Maps logical bot names to GitHub authors, with `codex` mapped to `chatgpt-codex-connector[bot]` and others to `<name>[bot]` (`scripts/check-pr-reviewer-status.sh:91-100`). Resolves the PR base repo by trying current repo, then parent repo for fork PRs, then falling back with a warning (`scripts/check-pr-reviewer-status.sh:113-140`). Fetches latest commit metadata, issue comments, review comments, and PR review submissions, normalizes them into author/timestamp/body records, and combines all streams (`scripts/check-pr-reviewer-status.sh:142-168`). For each configured bot it detects commented/missing status, newest review timestamp, and stale review status using `LATEST_COMMIT_AT > BOT_NEWEST` (`scripts/check-pr-reviewer-status.sh:179-205`). Case classification is: no comments -> 1; partial fresh -> 2; all fresh -> 3; all with any stale -> 4; partial with any stale -> 5 (`scripts/check-pr-reviewer-status.sh:220-245`).
- inputs_outputs_state: Inputs are PR number, comma-separated bot list, current GitHub repo context, GitHub API data, and `gh`/`jq` output. Output is JSON containing `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` (`scripts/check-pr-reviewer-status.sh:259-275`). It does not write repository state; callers consume its JSON to update PR-loop state.
- gates_or_invariants: Hard-gates on required PR number and `--bots` (`scripts/check-pr-reviewer-status.sh:75-85`) and on successful commit-info fetch (`scripts/check-pr-reviewer-status.sh:142-148`). Comment fetch failures degrade to empty arrays (`scripts/check-pr-reviewer-status.sh:156-165`). Per-bot staleness is used instead of a global newest-review-only check (`scripts/check-pr-reviewer-status.sh:192-200`, `scripts/check-pr-reviewer-status.sh:235-240`).
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` for bounded `gh` calls (`scripts/check-pr-reviewer-status.sh:35-40`; timeout implementation in `scripts/portable-timeout.sh:9-71`). Called during PR-loop setup to pick startup behavior and default to case 1 on failure (`scripts/setup-pr-loop.sh:437-447`). Called again by the PR stop hook to dynamically update `startup_case` before rewriting state (`hooks/pr-loop-stop-hook.sh:1554-1565`).
- edge_cases_or_failure_modes: Fork PR support depends on parent repo discovery; if both repo lookups fail, `PR_BASE_REPO` can fall back to an empty/current value and subsequent commit fetch may fail. Timestamp comparison assumes sortable ISO-8601 strings. Case 5 implementation and tests mean “partial commented plus stale,” while the header comment describes case 5 as future distinction similar to case 4 (`scripts/check-pr-reviewer-status.sh:22-28`, `scripts/check-pr-reviewer-status.sh:223-229`). Empty or malformed bot names are not deeply validated after comma splitting.
- validation_or_tests: `tests/test-pr-loop-system.sh` validates cases 1, 2, and 3 (`tests/test-pr-loop-system.sh:152-185`, `tests/test-pr-loop-system.sh:493-506`), PR review submissions counting as comments (`tests/test-pr-loop-system.sh:236-249`), and explicit case 4/case 5 stale scenarios (`tests/test-pr-loop-system.sh:1315-1369`). Setup and stop-hook callers are also covered by broader PR-loop tests.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-069 `file` `tests/test-pr-loop-1-scripts.sh`
- cursor: `[_]`
- core_role: Executable validation entry point for the first PR-loop test split, focused on script argument parsing and validation behavior.
- algorithmic_behavior: Initializes script/project roots, sources shared test helpers and PR-loop test library, creates the mock PR-loop environment, loads `tests/test-pr-loop-scripts.sh`, runs `run_script_tests`, prints a summary, and exits with the aggregate test status (`tests/test-pr-loop-1-scripts.sh:11-24`). It delegates all substantive assertions.
- inputs_outputs_state: Inputs are the repository scripts under test and the shell environment. `init_pr_loop_test_env` creates a temporary directory and prepends mock `gh` and `codex` binaries to `PATH` (`tests/test-pr-loop-lib.sh:107-121`). Outputs are PASS/FAIL lines, a test summary, and process exit status. Temporary state includes mock loop directories, state files, and environment variables such as `CLAUDE_PROJECT_DIR`.
- gates_or_invariants: Uses `set -euo pipefail` to fail on command errors/unbound variables (`tests/test-pr-loop-1-scripts.sh:9`). The delegated suite asserts setup requires at least one bot flag, rejects unknown flags and invalid `--max`, recognizes `--claude`, `--codex`, combined bot flags, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:27-165`). It also checks cancel/fetch/poll validation gates for missing loop, active loop cancellation, missing PR/output/after/bots, and invalid PR number (`tests/test-pr-loop-scripts.sh:172-399`).
- dependencies_and_callers: Included by the aggregate test runner at `tests/run-all-tests.sh:53`. Depends on `tests/test-helpers.sh` for counters/temp directory cleanup (`tests/test-helpers.sh:22-89`) and `tests/test-pr-loop-lib.sh` for mock command creation (`tests/test-pr-loop-lib.sh:27-121`).
- edge_cases_or_failure_modes: This file is a thin runner; failures mostly arise in sourced tests or environment setup. Because delegated tests execute real scripts with mocked `gh`/`codex`, missing shell tools, path issues, or mock coverage gaps can produce false failures. Some tests intentionally expect scripts to fail later for environmental reasons while proving option parsing progressed past a specific gate.
- validation_or_tests: It is itself the validation harness. The covered behavior is script CLI validation for `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh` (`tests/test-pr-loop-scripts.sh:5-11`, `tests/test-pr-loop-scripts.sh:405-410`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-099 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Stop-hook block template that enforces Round 0 goal-tracker initialization before RLCR progress can continue.
- algorithmic_behavior: Renders a user-facing block reason when the Goal Tracker still contains placeholders. The template names the missing items, instructs Claude to read `{{GOAL_TRACKER_FILE}}`, define/extract the Ultimate Goal, define 3-7 testable Acceptance Criteria, populate Active Tasks mapped to ACs, and write the updated `goal-tracker.md` (`prompt-template/block/goal-tracker-not-initialized.md:3-16`).
- inputs_outputs_state: Inputs are template variables `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}` (`prompt-template/block/goal-tracker-not-initialized.md:5-6`). Output is Markdown embedded into the stop-hook JSON `reason`. It does not mutate state directly; it blocks exit and requires the agent to update the goal tracker, after which the agent may attempt exit again (`prompt-template/block/goal-tracker-not-initialized.md:18`).
- gates_or_invariants: Enforces that the immutable section can only be set in Round 0 and becomes read-only afterward (`prompt-template/block/goal-tracker-not-initialized.md:16`). The caller detects placeholders in Ultimate Goal, Acceptance Criteria, and Active Tasks, builds `MISSING_ITEMS`, renders this template, returns decision `block`, and exits successfully from the hook protocol (`hooks/loop-codex-stop-hook.sh:700-743`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` with an inline fallback (`hooks/loop-codex-stop-hook.sh:726-733`). Depends on template-loader behavior inherited through `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: If the template is missing or fails to render, the hook uses a shorter fallback. The placeholder detector is pattern-based, so unusual placeholder text or malformed headings could evade or over-trigger the gate. The template assumes Round 0; using it outside that context would conflict with the immutable-section rule.
- validation_or_tests: Template reference coverage is present via `tests/test-template-references.sh`, and goal-tracker behavior has dedicated robustness tests elsewhere. The direct caller path is visible in `hooks/loop-codex-stop-hook.sh:726-743`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-129 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt contract fragment appended to the next Claude prompt when the RLCR loop was started with `--push-every-round`.
- algorithmic_behavior: Adds a concise instruction that commits must be pushed to remote after each round (`prompt-template/claude/push-every-round-note.md:2`). It is conditional guidance, not an enforcement script.
- inputs_outputs_state: Input is the parsed `PUSH_EVERY_ROUND` state flag. Output is appended text in the next round prompt. The hook loads this template only when `PUSH_EVERY_ROUND == true`; otherwise no push instruction is appended (`hooks/loop-codex-stop-hook.sh:1614-1620`).
- gates_or_invariants: Maintains the workflow invariant that push behavior follows the loop’s startup flag. The complementary bash validator blocks `git push` when `push_every_round` is not true (`hooks/loop-bash-validator.sh:81-104`), while this template instructs the positive case.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` with fallback text `Also push your changes after committing.` if the template is absent (`hooks/loop-codex-stop-hook.sh:1616-1619`). Depends on `load_template` and state parsing earlier in the stop hook.
- edge_cases_or_failure_modes: The template has a leading blank line, so rendering preserves spacing in the generated prompt. It does not verify that a push happened; enforcement must come from later git state checks, prompt compliance, or external review.
- validation_or_tests: Indirectly validated by stop-hook/prompt generation tests and template reference checks. The direct append path is `hooks/loop-codex-stop-hook.sh:1614-1620`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-159 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing, covering tolerant parsing, strict validation, defaults, malformed files, and newer optional fields.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and shared test helpers, creates a temporary test directory, writes many synthetic state files, calls `parse_state_file`, `parse_state_file_strict`, and `get_current_round`, records pass/fail counts, and exits with the summary status (`tests/robustness/test-state-file-robustness.sh:12-23`, `tests/robustness/test-state-file-robustness.sh:476-481`). The parser under test extracts YAML frontmatter fields into `STATE_*` globals and applies tolerant defaults for current round, max iterations, push flag, full review interval, ask-codex question, and agent teams (`hooks/lib/loop-common.sh:312-374`). Strict parsing requires frontmatter separators and required fields, validates numeric values and boolean `review_started`, and applies only optional defaults (`hooks/lib/loop-common.sh:377-449`).
- inputs_outputs_state: Inputs are generated `state*.md` fixtures with YAML frontmatter variations. Outputs are shell assertions, global parser variables such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, and `STATE_FULL_REVIEW_ROUND`, plus process exit status. The test writes only to its temp directory and cleans it via `setup_test_dir` trap (`tests/test-helpers.sh:84-89`).
- gates_or_invariants: Positive gates verify valid current/max rounds, extra fields ignored, quoted strings unquoted, round zero preserved, large/negative values handled, special-character paths, binary tail content, and `full_review_round` parsing/default/min value (`tests/robustness/test-state-file-robustness.sh:33-139`, `tests/robustness/test-state-file-robustness.sh:277-474`). Negative gates verify strict rejection of missing frontmatter, non-numeric `current_round`, missing required fields, and nonexistent files, while tolerant paths default or avoid crashes (`tests/robustness/test-state-file-robustness.sh:149-228`, `tests/robustness/test-state-file-robustness.sh:358-397`).
- dependencies_and_callers: Included by aggregate test runner at `tests/run-all-tests.sh:63`. Depends on `hooks/lib/loop-common.sh` parser functions (`tests/robustness/test-state-file-robustness.sh:14-17`). Hook validators use the strict parser to fail closed on malformed active loop state, for example read validator and bash validator (`hooks/loop-read-validator.sh:91-99`, `hooks/loop-bash-validator.sh:77-86`).
- edge_cases_or_failure_modes: The suite intentionally allows tolerant parser ambiguity for partial YAML and CRLF/no-crash cases (`tests/robustness/test-state-file-robustness.sh:242-257`, `tests/robustness/test-state-file-robustness.sh:386-397`). Some “valid” tolerant fixtures omit fields that strict parsing now requires, which is acceptable for tolerant-mode tests but means the label “all required fields” is not strict-schema accurate. Because parsing is grep/sed based, duplicate keys, nested YAML, comments, or unusual quoting are not deeply parsed.
- validation_or_tests: This file is the validation surface. It checks 21 robustness cases and exits through `print_test_summary` (`tests/robustness/test-state-file-robustness.sh:33-481`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 unique item evidence headings above; all assigned rows represented once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`