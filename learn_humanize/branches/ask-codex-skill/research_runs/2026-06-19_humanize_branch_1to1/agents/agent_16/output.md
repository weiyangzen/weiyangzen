# agent_16 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-016 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor utility directory. It currently contains one descendant, `scripts/lib/monitor-common.sh`, which centralizes read-only loop/session observation helpers used by the shell monitor layer. `scripts/humanize.sh` sources this library at `scripts/humanize.sh:6-10`, then uses its PR loop state, phase, and tracker helpers in the PR monitor display path at `scripts/humanize.sh:1231-1248`, `scripts/humanize.sh:1313-1319`, and `scripts/humanize.sh:1355-1363`.
- algorithmic_behavior: `monitor-common.sh` provides cross-platform file and display primitives plus loop-status parsers. Key sections are color helpers at `scripts/lib/monitor-common.sh:14-23`, file size detection via Linux/macOS `stat` at `scripts/lib/monitor-common.sh:32-35`, latest timestamped session selection at `scripts/lib/monitor-common.sh:40-63`, terminal split-view setup/restore at `scripts/lib/monitor-common.sh:71-92`, status-color mapping at `scripts/lib/monitor-common.sh:130-141`, state-file discovery at `scripts/lib/monitor-common.sh:154-188`, YAML frontmatter scalar extraction at `scripts/lib/monitor-common.sh:197-209`, timestamp/string formatting at `scripts/lib/monitor-common.sh:218-252`, PR loop phase detection at `scripts/lib/monitor-common.sh:267-342`, phase display mapping at `scripts/lib/monitor-common.sh:346-381`, generic goal tracker parsing at `scripts/lib/monitor-common.sh:390-463`, and PR goal tracker issue parsing at `scripts/lib/monitor-common.sh:468-498`.
- inputs_outputs_state: Inputs are session directories, loop directories, markdown state files, goal tracker markdown, terminal dimensions from `tput`, file mtimes/sizes, and optional state/cache files. Outputs are pipe-delimited status strings, phase names, display strings, ANSI codes, parsed count summaries, and terminal escape sequences. It does not mutate authoritative `.humanize` state files. Its only persistent side effect is the PR phase size cache written under `/tmp/humanize-phase-<session>-<round>.size` at `scripts/lib/monitor-common.sh:297-307`; it also changes terminal scroll state when setup/restore helpers are called.
- gates_or_invariants: `monitor_find_latest_session` only considers direct child directories whose basename matches `YYYY-MM-DD_HH-MM-SS` and chooses the lexicographically latest timestamp at `scripts/lib/monitor-common.sh:54-60`. `monitor_find_state_file` treats `state.md` as active with priority over terminal `*-state.md` files at `scripts/lib/monitor-common.sh:162-181`. `get_pr_loop_phase` checks terminal PR outcomes first: `approve-state.md`, `cancel-state.md`, `maxiter-state.md` at `scripts/lib/monitor-common.sh:272-275`; only then does it infer `codex_analyzing` from the latest `round-*-pr-check.md` growth or recent mtime at `scripts/lib/monitor-common.sh:277-318`; otherwise it maps `current_round: 0` plus `startup_case: 1` to `waiting_initial_review` and other active states to `waiting_reviewer` at `scripts/lib/monitor-common.sh:321-338`.
- dependencies_and_callers: The directory coordinates upward with `scripts/humanize.sh`, especially PR monitor status bar rendering, and with the test suite. `tests/test-pr-loop-system.sh` directly sources `scripts/lib/monitor-common.sh` for phase tests at `tests/test-pr-loop-system.sh:256-309`, tracker parsing at `tests/test-pr-loop-system.sh:315-417`, state detection at `tests/test-pr-loop-system.sh:423-453`, terminal-state phases at `tests/test-pr-loop-system.sh:459-487`, shared monitor helpers at `tests/test-pr-loop-system.sh:770-843`, and file-growth phase behavior at `tests/test-pr-loop-system.sh:849-899`.
- edge_cases_or_failure_modes: Multiple terminal `*-state.md` files are found with unsorted `find`, so the selected terminal status is not deterministic if a session has more than one terminal marker. `monitor_find_state_file` returns raw filename status such as `approve`, and `scripts/humanize.sh` maps that to `approved` for PR display at `scripts/humanize.sh:1241-1247`. The phase cache key uses only session basename plus round, so identical session names in different projects can collide in `/tmp`. A new or recently touched `round-*-pr-check.md` reports `codex_analyzing` even if no process is live, because size growth or mtime age under 10 seconds is sufficient at `scripts/lib/monitor-common.sh:315-318`. The markdown parsers are regex/table-shape dependent and do not parse full YAML or markdown; malformed or differently headed trackers fall back to zero/default counts.
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh` phase, monitor, state, file-size, latest-session, and goal-tracker parser tests: `tests/test-pr-loop-system.sh:256-417`, `tests/test-pr-loop-system.sh:423-487`, `tests/test-pr-loop-system.sh:770-899`, and monitor output assertions at `tests/test-pr-loop-system.sh:905-1106`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-046 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Validation and bootstrap script for `/humanize:gen-plan`. It gates draft input/output paths, locates the plan template, creates the target plan file, and appends the original draft behind explicit delimiters. The command wrapper calls it in Phase 1 at `commands/gen-plan.md:30-47`.
- algorithmic_behavior: It parses only `--input`, `--output`, and `-h|--help` at `scripts/validate-gen-plan-io.sh:29-56`; enforces required arguments at `scripts/validate-gen-plan-io.sh:58-67`; canonicalizes paths with `realpath -m` when available at `scripts/validate-gen-plan-io.sh:69-72`; prints validation context at `scripts/validate-gen-plan-io.sh:74-77`; performs ordered filesystem gates at `scripts/validate-gen-plan-io.sh:79-124`; reports `VALIDATION_SUCCESS` and line count at `scripts/validate-gen-plan-io.sh:126-131`; resolves `prompt-template/plan/gen-plan-template.md` from `CLAUDE_PLUGIN_ROOT` or script-relative fallback at `scripts/validate-gen-plan-io.sh:133-140`; verifies template existence at `scripts/validate-gen-plan-io.sh:142-146`; then copies the template and appends the draft content at `scripts/validate-gen-plan-io.sh:148-157`.
- inputs_outputs_state: Inputs are CLI args, the draft file, the output path, the output directory permissions, optional `CLAUDE_PLUGIN_ROOT`, and `prompt-template/plan/gen-plan-template.md`. Output is stdout/stderr validation text plus a newly created plan file. State transition on success is: absent output path becomes a file containing the template, a blank separator, `--- Original Design Draft Start ---`, the full draft content, and `--- Original Design Draft End ---`. It refuses to overwrite existing output paths at `scripts/validate-gen-plan-io.sh:103-116`.
- gates_or_invariants: Exit code contract is declared at `scripts/validate-gen-plan-io.sh:4-12` and mirrored by `commands/gen-plan.md:38-47`: `1` input missing, `2` input empty, `3` output directory missing, `4` output exists or is a directory, `5` output dir not writable, `6` invalid args/help, `7` missing template. It requires a non-empty input file, an existing writable output directory, a non-existing file output path, and an installed template before generation.
- dependencies_and_callers: Called by `commands/gen-plan.md:32-35`; the command frontmatter allowlists the script invocation at `commands/gen-plan.md:4-5`. It depends on standard shell utilities: `realpath`, `dirname`, `wc`, `cp`, `cat`, and shell redirection. The tests in `tests/test-gen-plan.sh` exercise its argument and filesystem exit-code behavior at `tests/test-gen-plan.sh:426-541`.
- edge_cases_or_failure_modes: `realpath -m` is GNU-specific; on systems without it, paths fall back to the provided string, so `OUTPUT_DIR` may be relative. There is a time-of-check/time-of-use race between the non-existence check and `cp`; if another process creates the output path after validation, `cp` may overwrite. If `cp` succeeds but a later append fails, a partial output file can remain because `set -e` exits without cleanup. `CLAUDE_PLUGIN_ROOT` takes precedence; if set incorrectly, the script exits `7` even if the script-relative template exists. Help intentionally exits `6`, which is tested but means help is treated as invalid by the command phase contract.
- validation_or_tests: `tests/test-gen-plan.sh` checks missing values, flag-as-value, unknown option, input not found, empty input, missing output dir, existing output, directory output, valid path success, and help behavior at `tests/test-gen-plan.sh:438-538`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-076 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: Executable specification for PR loop behavior. It validates the PR loop’s scheduler-facing and hook-facing algorithm: mutual exclusion with RLCR, reviewer case classification, reaction checks, PR review ingestion, monitor phase derivation, goal tracker parsing/mutation, trigger requirements, startup case evolution, setup cleanup failure paths, setup-created tracker state, and stop-hook tracker updates.
- algorithmic_behavior: The runner uses strict shell mode at `tests/test-pr-loop-system.sh:13`, sets project/mock/fixture paths at `tests/test-pr-loop-system.sh:15-23`, manages pass/fail counters at `tests/test-pr-loop-system.sh:30-48`, creates an isolated temp git repo and mock GitHub environment per test at `tests/test-pr-loop-system.sh:50-71`, runs each test in a subshell at `tests/test-pr-loop-system.sh:80-104`, and dispatches named filter groups in `main` at `tests/test-pr-loop-system.sh:1761-1904`. The test body specifies behavior for setup scripts, reviewer-status scripts, reaction scripts, monitor library functions, tracker update helpers, and the stop hook.
- inputs_outputs_state: Inputs are mock fixture JSON under `tests/fixtures`, mock `gh` under `tests/mocks`, environment variables such as `MOCK_GH_PR_NUMBER`, `MOCK_GH_PR_STATE`, `MOCK_GH_LATEST_COMMIT_AT`, `MOCK_GH_HEAD_SHA`, `CLAUDE_PROJECT_DIR`, and optional test filter argument. Outputs are colored pass/fail logs and process exit `1` if any test fails at `tests/test-pr-loop-system.sh:1899-1901`. State is created under `TEST_TEMP_DIR`, including `.humanize/rlcr/.../state.md`, `.humanize/pr-loop/.../state.md`, terminal marker files, temporary git commits, mock binaries, and generated tracker files.
- gates_or_invariants: Mutual exclusion requires active RLCR to block PR setup and active PR loop to block RLCR setup at `tests/test-pr-loop-system.sh:110-146`. Reviewer status cases distinguish no comments, partial comments, all comments, and comments followed by newer commits at `tests/test-pr-loop-system.sh:152-192`, `tests/test-pr-loop-system.sh:493-507`, and `tests/test-pr-loop-system.sh:1315-1378`. PR review submissions count as reviewer activity at `tests/test-pr-loop-system.sh:236-250`. Startup cases `1|2|3` in round 0 do not require a trigger, while cases `4|5` and all later rounds do at `tests/test-pr-loop-system.sh:1112-1174`. Missing trigger comment ID for case 4/5 setup must fail and clean up loop state at `tests/test-pr-loop-system.sh:1384-1447`.
- dependencies_and_callers: It depends on `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/lib/monitor-common.sh`, `scripts/humanize.sh`, `hooks/lib/loop-common.sh`, and `hooks/pr-loop-stop-hook.sh`. It also uses `jq`, `git`, `timeout`, `date`, `touch`, and dynamically generated mock `gh`, `git`, and `codex` binaries in the stop-hook integration path at `tests/test-pr-loop-system.sh:1595-1715`.
- edge_cases_or_failure_modes: The file deliberately mutates shared fixture files during tests and restores them manually; interruption or early failure can leave fixture drift. Some tests are partial specifications: unpushed commits detection notes it cannot truly simulate upstream status at `tests/test-pr-loop-system.sh:566-571`, and the early startup case 4/5 test only asserts valid JSON at `tests/test-pr-loop-system.sh:661-675`, with stronger case 4/5 assertions later. The phase analyzing test uses `touch -d` at `tests/test-pr-loop-system.sh:878-881` without a BSD/macOS fallback, unlike other date code in the file, so it may fail on macOS. Running the suite requires a writable temp area and functional `git`; I did not run it in this read-only research pass.
- validation_or_tests: The file is itself validation. Major covered surfaces include phase detection at `tests/test-pr-loop-system.sh:256-309` and `tests/test-pr-loop-system.sh:459-487`, goal tracker parsing/update at `tests/test-pr-loop-system.sh:315-417`, `tests/test-pr-loop-system.sh:513-538`, `tests/test-pr-loop-system.sh:681-764`, table insertion and partial repair at `tests/test-pr-loop-system.sh:1180-1309`, monitor output assertions at `tests/test-pr-loop-system.sh:905-1106`, setup-created tracker verification at `tests/test-pr-loop-system.sh:1453-1539`, and stop-hook tracker update verification at `tests/test-pr-loop-system.sh:1541-1755`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-106 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Block-message template for PR loop state file protection. It defines the user-facing contract that `.humanize/pr-loop/.../state.md` is system-managed and must not be modified manually.
- algorithmic_behavior: The template is static markdown. It emits a heading, states that `state.md` in `.humanize/pr-loop/` cannot be modified, lists the state fields it protects, and explains that manual edits corrupt PR loop state at `prompt-template/block/pr-loop-state-modification.md:1-12`.
- inputs_outputs_state: It has no template variables. Input is only the template loader request from `pr_loop_state_blocked_message`; output is markdown written to validator stderr/block reason. It performs no state mutation itself.
- gates_or_invariants: The protected invariant is that PR loop `state.md` remains owned by the PR loop system because it stores current round, PR number/branch, active bot configuration, Codex configuration, and polling settings at `prompt-template/block/pr-loop-state-modification.md:5-10`. The loader fallback for this message is defined in `hooks/lib/loop-common.sh:1017-1024`.
- dependencies_and_callers: Called through `pr_loop_state_blocked_message` in `hooks/lib/loop-common.sh:1018-1023`. Write validation blocks direct PR loop `state.md` writes at `hooks/loop-write-validator.sh:77-84`; edit validation blocks edits at `hooks/loop-edit-validator.sh:60-67`; bash validation blocks command-based modifications, including full path and bare `state.md` patterns, at `hooks/loop-bash-validator.sh:403-414`.
- edge_cases_or_failure_modes: Because it is only a message template, enforcement depends on validators correctly detecting modifications. The bash validator includes a broad bare `state.md` check when an active PR loop exists, which improves relative-path protection but can also block commands that modify an unrelated bare `state.md` while in that protected context. The template only names `state.md`; terminal files such as `approve-state.md` are governed by other state-discovery/transition logic rather than this specific message.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` includes PR loop state protection checks: write validator block at `tests/test-pr-loop-hooks.sh:85-96` and bash redirect block at `tests/test-pr-loop-hooks.sh:193-194`. The larger PR loop system test depends on protected state semantics when it creates and observes `state.md`, `approve-state.md`, `cancel-state.md`, and `maxiter-state.md`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-136 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: Prompt block inserted into the PR loop Codex review prompt to make Codex update the PR loop goal tracker after review analysis. It defines the intended transition rules for the tracker’s Issue Summary, Total Statistics, and Issue Log sections.
- algorithmic_behavior: The template instructs Codex to update `{{GOAL_TRACKER_FILE}}` after analysis at `prompt-template/pr-loop/codex-goal-tracker-update.md:1-4`; add one Issue Summary row for `{{NEXT_ROUND}}` with reviewer, issues found, issues resolved, and status at `prompt-template/pr-loop/codex-goal-tracker-update.md:7-10`; increment cumulative totals and recompute Remaining at `prompt-template/pr-loop/codex-goal-tracker-update.md:12-15`; add a round log entry at `prompt-template/pr-loop/codex-goal-tracker-update.md:17-20`; and preserve section boundaries and table shape at `prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`.
- inputs_outputs_state: Inputs are template variables `GOAL_TRACKER_FILE` and `NEXT_ROUND`, provided by `hooks/pr-loop-stop-hook.sh:1253-1258`. Output is natural-language instruction embedded in the generated Codex prompt at `hooks/pr-loop-stop-hook.sh:1262-1303`. The desired state transition is a tracker mutation: append/insert round-specific review evidence, increase cumulative found/resolved counts, and set Remaining to Found minus Resolved.
- gates_or_invariants: Required tracker invariants are: keep markdown table formatting, modify only Issue Summary/Total Statistics/Issue Log, do not modify PR Information or Ultimate Goal, add to existing tables instead of replacing them, and use one row per reviewer at `prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`. Initial tracker structure is created from `prompt-template/pr-loop/goal-tracker-initial.md:15-30` by `scripts/setup-pr-loop.sh:641-713`.
- dependencies_and_callers: Loaded with fallback by `hooks/pr-loop-stop-hook.sh:1253-1263` and inserted into the Codex validation prompt at `hooks/pr-loop-stop-hook.sh:1302`. The stop hook backstops or repairs tracker updates with `update_pr_goal_tracker` from `hooks/lib/loop-common.sh:1296-1442`, calling it on full approve at `hooks/pr-loop-stop-hook.sh:1381-1385` and on continuing issues at `hooks/pr-loop-stop-hook.sh:1536-1542`.
- edge_cases_or_failure_modes: The template is advisory; if Codex ignores it or writes a malformed table, enforcement falls to `update_pr_goal_tracker`. There is a status vocabulary mismatch risk: the prompt allows `"All Resolved"` at `prompt-template/pr-loop/codex-goal-tracker-update.md:10`, while `update_pr_goal_tracker` emits `"Resolved"` when only resolved count is positive at `hooks/lib/loop-common.sh:1371-1379`. Reviewer casing can affect idempotency because `update_pr_goal_tracker` checks exact round+reviewer rows at `hooks/lib/loop-common.sh:1317-1331`; the stop hook explicitly uses lowercase `codex` to avoid duplicate rows at `hooks/pr-loop-stop-hook.sh:1536-1540`. The prompt does not define a machine-readable schema for individual issue identity, so “new issues” and “resolved issues” counts remain heuristic.
- validation_or_tests: Tracker schema and mutation behavior are specified in `tests/test-pr-loop-system.sh`: schema checks at `tests/test-pr-loop-system.sh:642-655`, basic update at `tests/test-pr-loop-system.sh:513-538`, add-row and idempotency checks at `tests/test-pr-loop-system.sh:681-764`, in-table insertion at `tests/test-pr-loop-system.sh:1180-1236`, partial update repair at `tests/test-pr-loop-system.sh:1242-1309`, setup-created tracker integration at `tests/test-pr-loop-system.sh:1453-1539`, and stop-hook tracker update integration at `tests/test-pr-loop-system.sh:1541-1755`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ASK_CODEX_SKILL-HZ-016`, `ASK_CODEX_SKILL-HZ-046`, `ASK_CODEX_SKILL-HZ-076`, `ASK_CODEX_SKILL-HZ-106`, `ASK_CODEX_SKILL-HZ-136`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`