# agent_17 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-017 `directory` `skills/ask-codex`
- cursor: `[_]`
- core_role: This directory is the Claude skill declaration for the ask-codex feature. Recursive inspection found one descendant, `skills/ask-codex/SKILL.md`, which binds the user-facing skill to the executable script `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh` through the `allowed-tools` declaration at `skills/ask-codex/SKILL.md:6` and the usage command at `skills/ask-codex/SKILL.md:15-19`.
- algorithmic_behavior: The directory does not implement the Codex call itself; it defines the skill contract and dispatch path. The behavior is one-shot consultation: send arguments to `ask-codex.sh`, read Codex stdout, incorporate that answer, and report non-zero exit failures. The skill explicitly disables direct model invocation at `skills/ask-codex/SKILL.md:5`, so the allowed execution path is the shell script, not freeform model delegation.
- inputs_outputs_state: Inputs are user-provided `$ARGUMENTS`, optionally including `--codex-model MODEL:EFFORT`, `--codex-timeout SECONDS`, and question/task text as documented at `skills/ask-codex/SKILL.md:4` and `skills/ask-codex/SKILL.md:17-19`. Outputs are divided by stream: Codex response on stdout, status on stderr at `skills/ask-codex/SKILL.md:21-24`. Persistent output is saved under `.humanize/skill/<timestamp>/output.md` per `skills/ask-codex/SKILL.md:38-40`; the implementation confirms project-local `input.md`, `output.md`, and `metadata.md` storage plus cache logs in `scripts/ask-codex.sh:15-17`, `scripts/ask-codex.sh:205-217`, and `scripts/ask-codex.sh:387-400`.
- gates_or_invariants: The skill’s exit-code contract is its main gate: `0` means usable stdout, `1` means validation error, `124` means timeout, and other values mean Codex process failure at `skills/ask-codex/SKILL.md:27-34`. The underlying script enforces `codex` availability, non-empty question, safe model characters, and safe effort characters at `scripts/ask-codex.sh:153-186`. It also rejects timeout, non-zero Codex exit, and empty stdout before emitting a response at `scripts/ask-codex.sh:308-381`.
- dependencies_and_callers: The skill depends on `scripts/ask-codex.sh`, which depends on `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout execution and default Codex model/effort at `scripts/ask-codex.sh:26-33`. Repository references identify README documentation and `tests/test-ask-codex.sh` as caller-facing and validation surfaces. The script builds `codex exec` arguments with model, effort config, sandbox/automation flag, and project root at `scripts/ask-codex.sh:243-255`, then runs `printf '%s' "$QUESTION" | run_with_timeout ... codex exec ... -` at `scripts/ask-codex.sh:293-297`.
- edge_cases_or_failure_modes: Misconfiguration can fail before Codex invocation: missing CLI, empty question, invalid model/effort tokens, unknown flags, non-numeric timeout. Runtime failures include timeout exit `124`, non-zero Codex exit with stderr tail, empty response, non-writable home cache falling back to `.humanize/skill/<id>/cache`, and optional dangerous sandbox bypass when `HUMANIZE_CODEX_BYPASS_SANDBOX` is true or `1` at `scripts/ask-codex.sh:249-253`.
- validation_or_tests: Validation is covered by `tests/test-ask-codex.sh` per repository references and by the skill’s own documented exit-code table. The script is testable with a mocked `codex` binary because all external behavior is behind `command -v codex` plus `codex exec` invocation and stream/log files. The directory itself is not a runnable algorithm, but it is a core dispatch contract for the algorithm implemented in `scripts/ask-codex.sh`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-047 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: This is a manual executable specification for RLCR monitor cleanup behavior, especially graceful exit when `.humanize/` is deleted while `humanize monitor rlcr` is running. The intent is stated at `tests/manual-monitor-test.sh:5-7`: deleting `.humanize/` should produce a clean exit and restore terminal state.
- algorithmic_behavior: The script is a three-command state-machine harness: `setup` creates a fake active RLCR session, `delete` removes `.humanize` to trigger the monitor’s missing-directory path, and `cleanup` removes leftovers. The usage flow at `tests/manual-monitor-test.sh:9-15` coordinates two terminals: one running the monitor and one mutating the backing state directory.
- inputs_outputs_state: Input is positional command `${1:-}` parsed by the case statement at `tests/manual-monitor-test.sh:23`. `setup` writes `.humanize/rlcr/2026-01-16_99-99-99/state.md` and `goal-tracker.md` with minimal loop metadata and acceptance criteria at `tests/manual-monitor-test.sh:24-38`. `delete` removes `.humanize` at `tests/manual-monitor-test.sh:47-56`. `cleanup` also removes `.humanize` at `tests/manual-monitor-test.sh:58-61`. Outputs are human-readable instructions and expected observations on stdout.
- gates_or_invariants: `set -euo pipefail` at `tests/manual-monitor-test.sh:17` makes failed setup/delete operations abort. The manual acceptance gate is observational: after deletion, the monitor should print the exact clean-exit reason “Monitoring stopped: .humanize/rlcr directory no longer exists,” restore scroll region/cursor, and avoid shell glob errors as listed at `tests/manual-monitor-test.sh:51-54`. The monitor implementation’s deletion check is in `scripts/humanize.sh:775-780` and repeated inside the no-log polling loop at `scripts/humanize.sh:832-840`.
- dependencies_and_callers: It depends on `scripts/humanize.sh` exposing `humanize monitor rlcr`, as instructed at `tests/manual-monitor-test.sh:11` and echoed at `tests/manual-monitor-test.sh:43-45`. The underlying monitor cleanup path tracks `tail_pid`, `monitor_running`, and `cleanup_done`, kills the tail process, restores the terminal, and prints “Stopped monitoring.” at `scripts/humanize.sh:668-699`. `_graceful_stop` then calls `_cleanup` before reporting the missing-directory reason at `scripts/humanize.sh:701-710`.
- edge_cases_or_failure_modes: This is manual, so it does not automatically assert outcomes or capture terminal escape-state regressions. It mutates the real project-local `.humanize` directory, so running it in a repository with active loop state would be destructive to that local state. The fixture timestamp `2026-01-16_99-99-99` is intentionally synthetic and may sort unexpectedly against real sessions if real `.humanize/rlcr` content exists. The script also assumes a usable interactive terminal and sourced plugin command in the second terminal.
- validation_or_tests: The script itself is validation evidence for monitor lifecycle behavior rather than an automated unit test. Related automated coverage appears in monitor-focused tests such as `tests/test-zsh-monitor-safety.sh`, `tests/test-monitor-e2e-real.sh`, and shared monitor helpers in `scripts/lib/monitor-common.sh`, where terminal restoration primitives reset the scroll region and move the cursor at `scripts/lib/monitor-common.sh:84-92`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-077 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: This is the top-level PR-loop test runner. It is an executable specification that composes script, hook, and stop-hook test modules into a single PR-loop validation entrypoint. The file’s module list is documented at `tests/test-pr-loop.sh:5-8`.
- algorithmic_behavior: The runner initializes a shared test environment, sources three modules, runs their exported test suites in a fixed order, then prints a summary. The sequence is: source helpers and library at `tests/test-pr-loop.sh:18-20`, initialize mocks at `tests/test-pr-loop.sh:26`, source modules at `tests/test-pr-loop.sh:32-34`, run script/hook/stop-hook tests at `tests/test-pr-loop.sh:40-47`, and print the summary at `tests/test-pr-loop.sh:53`.
- inputs_outputs_state: Inputs are the repository’s test scripts and the ambient shell environment. `init_pr_loop_test_env` creates an isolated test directory, creates mock `gh` and `codex` binaries, and prepends the mock directory to `PATH` at `tests/test-pr-loop-lib.sh:107-121`. Test state is held in shared helper counters such as `TESTS_PASSED` and `TESTS_FAILED`; the final output summary returns non-zero if any tests failed at `tests/test-pr-loop-lib.sh:127-143`.
- gates_or_invariants: `set -euo pipefail` at `tests/test-pr-loop.sh:13` makes missing modules or failed commands terminate unless test functions intentionally handle errors. The gate is aggregate: `print_test_summary` returns failure when `TESTS_FAILED > 0` at `tests/test-pr-loop-lib.sh:137-143`. Module ordering is an invariant because script tests validate setup/cancel/fetch/poll surfaces before hook and stop-hook integration surfaces are run.
- dependencies_and_callers: It depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh`. The script module’s entrypoint runs setup, cancel, fetch, and poll tests at `tests/test-pr-loop-scripts.sh:405-409`. The hook module covers validators, comment processing, fixture tests, E2E behavior, and monitor parsing as introduced at `tests/test-pr-loop-hooks.sh:3-12`. The stop-hook module covers force-push detection, trigger validation, bot timeout handling, state file management, and dynamic startup-case updates at `tests/test-pr-loop-stophook.sh:3-12`.
- edge_cases_or_failure_modes: Because all modules are sourced into one shell, global variables, current working directory, function names, and modified `PATH` are shared; a module that fails to restore `cd "$SCRIPT_DIR"` or overwrites shared variables can affect later suites. Mock `gh` returns simplified JSON and empty arrays for many API paths at `tests/test-pr-loop-lib.sh:27-87`, so this runner validates deterministic contract behavior but cannot fully prove live GitHub API behavior. A module source failure prevents later tests from running because of `set -e`.
- validation_or_tests: This file is itself the validation orchestrator. It is also included in the broader runner list at `tests/run-all-tests.sh` references, while split wrappers such as `test-pr-loop-1-scripts.sh`, `test-pr-loop-2-hooks.sh`, and `test-pr-loop-3-stophook.sh` provide narrower entrypoints. The main runner’s value is integrated coverage across PR setup scripts, validators, PR comment/review polling, and stop-hook lifecycle behavior.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-107 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: This template defines the blocking message shown when an agent attempts to write to `round-*-prompt.md` files. It is a prompt-contract guard: prompt files are instructions to the agent and must not be modified by the agent itself.
- algorithmic_behavior: The template is static content with no placeholders. It tells the agent the write is blocked, explains that prompt files contain instructions from Codex to Claude, and redirects the allowed workflow to reading the prompt, executing tasks, and writing results to the summary file at `prompt-template/block/prompt-file-write.md:1-12`.
- inputs_outputs_state: Input is an attempted write/edit/bash operation targeting a prompt file in the active loop. Output is this rendered block message returned by validators. There is no local state mutation in the template itself. The loader path is `prompt_write_blocked_message`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/prompt-file-write.md"` at `hooks/lib/loop-common.sh:582-589`.
- gates_or_invariants: The invariant is instruction immutability: `round-*-prompt.md` is read-only from the agent’s perspective. If prompt content is wrong, the agent must document the problem in the summary rather than edit the prompt at `prompt-template/block/prompt-file-write.md:12`. Write/edit/bash validators consume this message for prompt-write violations: `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh` reference `prompt_write_blocked_message` according to repository search results.
- dependencies_and_callers: It depends on the shared template system in `hooks/lib/template-loader.sh`; missing templates fall back to an embedded shorter message in `hooks/lib/loop-common.sh:583-588`. The shared loader supports `load_and_render_safe`, and template reference coverage includes `tests/test-template-references.sh`, which lists `block/prompt-file-write.md` as an expected referenced template.
- edge_cases_or_failure_modes: Because the template has no variables, rendering is low risk. If the template is missing or unreadable, the fallback still preserves the core behavior but loses the more detailed numbered workflow. A PR-loop-specific prompt/comment write guard uses a different template, `block/pr-loop-prompt-write.md`, so this file primarily covers the general RLCR prompt file write block rather than every prompt-like file in PR-loop state.
- validation_or_tests: Validated indirectly by template reference tests and comprehensive template loader tests. Behavioral validation comes from hook tests that assert protected files are blocked and correct round/summary paths are allowed. The important validation property is that validators render the message through `load_and_render_safe`, so missing-template failures degrade to a safe denial rather than allowing prompt mutation.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-137 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: This template is the critical completion contract inserted into a PR-loop prompt when fetched PR comments exist. It defines the required transition from local issue resolution to remote bot re-review: commit/push, comment on the PR, then write the resolution summary.
- algorithmic_behavior: The template renders a strict ordered checklist. On completion, the agent must commit and push changes, run `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review"`, and write a summary to `{{RESOLVE_PATH}}` including issues addressed, files modified, and tests added at `prompt-template/pr-loop/critical-requirements-has-comments.md:6-23`.
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`. `scripts/setup-pr-loop.sh` constructs these variables in `TEMPLATE_VARS` at `scripts/setup-pr-loop.sh:731-738`; `RESOLVE_PATH` is set to `$LOOP_DIR/round-0-pr-resolve.md` at `scripts/setup-pr-loop.sh:721`. Output is appended to the generated prompt after the header, fetched comments, and task section.
- gates_or_invariants: This template is selected only for the “has comments” branch. `scripts/setup-pr-loop.sh` detects comments by the absence of the sentinel `*No comments found.*` at `scripts/setup-pr-loop.sh:723-729`; when `COMMENT_COUNT` is non-zero, it uses the has-comments task branch at `scripts/setup-pr-loop.sh:801-847` and then loads this critical requirements template at `scripts/setup-pr-loop.sh:916-940`. The invariant is that the stop hook should poll only after the required trigger comment and pushed commits exist; the template explicitly states that the Stop Hook polls for bot reviews at `prompt-template/pr-loop/critical-requirements-has-comments.md:23`.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` via `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-has-comments.md" ...` at `scripts/setup-pr-loop.sh:940`. It coordinates with `hooks/pr-loop-stop-hook.sh`, which enforces downstream gates such as unpushed commits, force-push detection, and missing trigger comments through block templates referenced in that hook. Tests for those gates are aggregated by `tests/test-pr-loop.sh` and implemented in stop-hook cases such as force-push rejection, unpushed commits, missing trigger, bot timeout, and approval handling listed in `tests/test-pr-loop-stophook.sh:20-40` and the function index from the inspected test file.
- edge_cases_or_failure_modes: If the comment-detection sentinel is malformed, the setup script could choose the wrong critical template. If `BOT_MENTION_STRING` or `PR_NUMBER` is empty or incorrect, the generated `gh pr comment` command may fail to trigger re-review. If the agent writes the summary without committing/pushing or without posting the trigger comment, stop-hook validation should block progress. Missing template falls back to an embedded equivalent at `scripts/setup-pr-loop.sh:916-939`.
- validation_or_tests: Covered by template reference tests, PR-loop setup tests, and stop-hook integration tests. Specific related gates include missing trigger blocking, force-push detection, unpushed commit detection, bot timeout handling, and approval capture in `tests/test-pr-loop-stophook.sh`. This template is core because it encodes the required handoff between the prompt-generation phase and the stop-hook polling/validation phase.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item evidence headers present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`