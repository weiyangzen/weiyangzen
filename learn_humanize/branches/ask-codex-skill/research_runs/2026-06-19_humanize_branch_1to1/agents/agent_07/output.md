# agent_07 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-007 `directory` `skills`
- cursor: `[_]`
- core_role: The `skills` directory is the command-facing skill registry surface for this branch. Recursive inspection shows one child, `skills/ask-codex/SKILL.md`, a Claude skill manifest that exposes `/humanize:ask-codex` as a one-shot Codex consultation path and delegates all executable behavior to `scripts/ask-codex.sh`.
- algorithmic_behavior: The manifest defines the skill identity and routing metadata: `name: ask-codex`, a one-shot consultation description, argument hint, `disable-model-invocation: true`, and a restricted tool allowlist for only `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh:*` (`skills/ask-codex/SKILL.md:1-6`). Its operational algorithm is deliberately thin: execute the script with `$ARGUMENTS`, read stdout as Codex’s answer, and treat stderr as status/debug information (`skills/ask-codex/SKILL.md:13-25`).
- inputs_outputs_state: Inputs are the user’s free-form question/task plus optional `--codex-model MODEL:EFFORT` and `--codex-timeout SECONDS` arguments (`skills/ask-codex/SKILL.md:4`, `scripts/ask-codex.sh:86-147`). Output is Codex stdout to the caller, with status/debug on stderr (`skills/ask-codex/SKILL.md:21-25`). Persistent state is created by the delegated script under `.humanize/skill/<unique-id>/{input.md,output.md,metadata.md}` and cache files under `~/.cache/humanize/...` or a project-local fallback (`scripts/ask-codex.sh:202-218`, `scripts/ask-codex.sh:224-237`, `scripts/ask-codex.sh:387-401`).
- gates_or_invariants: The skill-level invariant is tool confinement: the skill may only call the ask-codex Bash script (`skills/ask-codex/SKILL.md:5-6`). The delegated script gates on Codex CLI availability, non-empty question, safe model characters, safe effort characters, and numeric timeout (`scripts/ask-codex.sh:153-186`). It maps exit code `0` to success, `1` to validation/empty response errors, `124` to timeout, and other codes to Codex process failure (`skills/ask-codex/SKILL.md:27-34`, `scripts/ask-codex.sh:308-380`).
- dependencies_and_callers: The directory coordinates with `scripts/ask-codex.sh`, which sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout handling and default Codex model/effort (`scripts/ask-codex.sh:26-43`). `tests/test-ask-codex.sh` mocks Codex and verifies validation, success, error, timeout, directory uniqueness, argument parsing, and cache artifacts (`tests/test-ask-codex.sh:1-17`, `tests/test-ask-codex.sh:80-153`, `tests/test-ask-codex.sh:163-210`, `tests/test-ask-codex.sh:219-275`, `tests/test-ask-codex.sh:285-405`). README references this as a user-facing command (`README.md:203-216` from search result context).
- edge_cases_or_failure_modes: Missing `codex`, empty question, unknown flags, missing option values, non-numeric timeout, unsafe model/effort strings, Codex timeout, non-zero Codex exit, and empty stdout all become explicit failures (`scripts/ask-codex.sh:153-186`, `scripts/ask-codex.sh:308-380`). Concurrent invocations avoid directory collision with timestamp, pid, and random suffix in `UNIQUE_ID` (`scripts/ask-codex.sh:202-207`).
- validation_or_tests: `tests/test-ask-codex.sh` provides direct executable coverage with a mock Codex binary, including stdout passthrough, metadata statuses `success/error/empty_response/timeout`, cache debug files, `--` separator handling, `MODEL:EFFORT` parsing, and concurrent unique directories (`tests/test-ask-codex.sh:38-74`, `tests/test-ask-codex.sh:163-210`, `tests/test-ask-codex.sh:219-405`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-037 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for active RLCR loops. It transitions the newest active RLCR session from `state.md` or `finalize-state.md` to `cancel-state.md` and creates a `.cancel-requested` signal so hook-level state-file protections can recognize the cancellation path.
- algorithmic_behavior: The script parses only `--force`, `-h`, and `--help`; unknown options exit `3` (`scripts/cancel-rlcr-loop.sh:24-63`). It resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, builds `.humanize/rlcr`, sources `hooks/lib/loop-common.sh`, and calls `find_active_loop` without a session filter (`scripts/cancel-rlcr-loop.sh:69-87`). It detects normal vs finalize phase by checking `state.md` first, then `finalize-state.md` (`scripts/cancel-rlcr-loop.sh:99-113`), extracts `current_round` and `max_iterations` for reporting (`scripts/cancel-rlcr-loop.sh:119-125`), blocks finalize cancellation unless `--force` is present (`scripts/cancel-rlcr-loop.sh:131-142`), then touches `.cancel-requested`, removes `.humanize/.pending-session-id`, and moves the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:148-155`).
- inputs_outputs_state: Inputs are command-line flags, `CLAUDE_PROJECT_DIR`, the newest active loop directory, and active state fields. Outputs are stable first-line markers: `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, or `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:89-92`, `scripts/cancel-rlcr-loop.sh:110-112`, `scripts/cancel-rlcr-loop.sh:131-141`, `scripts/cancel-rlcr-loop.sh:161-169`). State transition is `state.md -> cancel-state.md` for normal loops or `finalize-state.md -> cancel-state.md` for forced finalize cancellation, plus `.cancel-requested` creation (`scripts/cancel-rlcr-loop.sh:148-155`).
- gates_or_invariants: Cancellation is intentionally global because a slash command has no session id; the comments state this is a product decision so user-initiated cancel cancels whichever loop is active in the project (`scripts/cancel-rlcr-loop.sh:76-87`). Finalize phase is protected by an interactive confirmation path unless `--force` is passed (`scripts/cancel-rlcr-loop.sh:131-142`). The sibling command wrapper requires `AskUserQuestion` confirmation when the first output line is `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:17-35`).
- dependencies_and_callers: Depends on `find_active_loop` from `hooks/lib/loop-common.sh`, whose no-filter mode checks only the single newest directory and returns it only if `state.md` or `finalize-state.md` exists (`hooks/lib/loop-common.sh:215-257`). It is called by the slash command spec in `commands/cancel-rlcr-loop.md`, whose key principle repeats that active means `state.md` or `finalize-state.md` in the newest loop directory (`commands/cancel-rlcr-loop.md:36-38`). Hook protection is coordinated with `is_cancel_authorized`, which accepts only a signal-file-backed `mv state.md|finalize-state.md cancel-state.md` operation and rejects injection or wrong paths (`hooks/lib/loop-common.sh:661-838`, `hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:286-303`).
- edge_cases_or_failure_modes: No loop base or newest inactive loop yields `NO_LOOP`; a directory with no active state yields `NO_ACTIVE_LOOP` (`scripts/cancel-rlcr-loop.sh:89-113`). If current/max round fields are missing, report values degrade to `?` rather than blocking cancellation (`scripts/cancel-rlcr-loop.sh:119-125`). Finalize cancellation exits `2` without `--force`; unknown arguments exit `3`; filesystem failures in `touch`, `rm`, or `mv` abort under `set -euo pipefail`.
- validation_or_tests: `tests/test-cancel-signal-file.sh` verifies that only signal-file-backed `mv state.md cancel-state.md` is allowed, while arbitrary state modifications, wrong destinations, wrong signal directory, and command injection are blocked (`tests/test-cancel-signal-file.sh:80-135`, `tests/test-cancel-signal-file.sh:141-257`, `tests/test-cancel-signal-file.sh:263-546`). `tests/robustness/test-cancel-security-robustness.sh` further covers finalize-state source, quoting, wrong source/destination, extra args, newline/operator injection, variable expansion, mixed quotes, symlinks, and paths containing `finalize` (`tests/robustness/test-cancel-security-robustness.sh:39-89`, `tests/robustness/test-cancel-security-robustness.sh:99-276`, `tests/robustness/test-cancel-security-robustness.sh:278-405`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-067 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file and loop-file protection. It validates the hook state machine around `state.md`, project plan immutability, `plan.md` backups, branch locking, Bash/Write/Edit bypass prevention, stop-hook plan integrity, and goal-tracker initialization.
- algorithmic_behavior: The test builds an isolated temp git repo, installs a mock `codex`, creates `.humanize/rlcr/<timestamp>`, a gitignored `plans/test-plan.md`, a loop `plan.md` backup, and a v1.5.0-style `state.md` containing `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` (`tests/test-plan-file-hooks.sh:30-58`, `tests/test-plan-file-hooks.sh:63-114`). It then feeds JSON hook inputs into `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` and checks exit codes/output text.
- inputs_outputs_state: Inputs are generated hook JSON such as `{"tool_name":"Write","tool_input":{"file_path":".../plan.md"}}`, Bash commands targeting protected files, temporary state files with varied schema/branch/plan settings, and mock Codex output (`tests/test-plan-file-hooks.sh:124-146`, `tests/test-plan-file-hooks.sh:228-265`, `tests/test-plan-file-hooks.sh:538-575`). Outputs are `PASS`/`FAIL` counters and process exit status equal to failure count (`tests/test-plan-file-hooks.sh:26-32`, `tests/test-plan-file-hooks.sh:1108-1117`). Test state transitions repeatedly reset loop state, branch state, plan backups, tracked/untracked plan files, and goal-tracker content.
- gates_or_invariants: Enforces strict state schema handling: missing v1.5.0 fields must block as malformed (`tests/test-plan-file-hooks.sh:149-188`), quoted YAML values must be parsed correctly (`tests/test-plan-file-hooks.sh:134-147`, `tests/test-plan-file-hooks.sh:373-423`), and branch switching must block (`tests/test-plan-file-hooks.sh:194-217`). It verifies `plan.md` backup cannot be written, edited, removed, or modified through Bash (`tests/test-plan-file-hooks.sh:226-299`) and tests command-substitution, glob, brace, pipe, and backtick bypass attempts (`tests/test-plan-file-hooks.sh:300-367`).
- dependencies_and_callers: Directly depends on all RLCR hook validators and the stop hook. The exercised implementations include strict state parsing in `parse_state_file_strict` (`hooks/lib/loop-common.sh:377-449`), branch/plan tracking checks in `loop-plan-file-validator.sh` (`hooks/loop-plan-file-validator.sh:44-115`, `hooks/loop-plan-file-validator.sh:121-180`), Bash plan/goal protection (`hooks/loop-bash-validator.sh:336-357`), Write/Edit plan backup protection (`hooks/loop-write-validator.sh:181-187`, `hooks/loop-edit-validator.sh:131-138`), and stop-hook plan integrity checks (`hooks/loop-codex-stop-hook.sh:260-340`).
- edge_cases_or_failure_modes: Covers direct `.humanize/rlcr/plan.md` without timestamp subdir (`tests/test-plan-file-hooks.sh:286-299`), hyphenated `plan_file` paths (`tests/test-plan-file-hooks.sh:469-500`), tracked plan uncommitted modifications (`tests/test-plan-file-hooks.sh:602-671`), tracked plan committed changes despite clean git status (`tests/test-plan-file-hooks.sh:697-775`), missing plan backup (`tests/test-plan-file-hooks.sh:583-600`), deleted project plan (`tests/test-plan-file-hooks.sh:549-581`), old schema JSON block behavior (`tests/test-plan-file-hooks.sh:673-695`), and legacy `.humanize-loop.local` paths intentionally allowed (`tests/test-plan-file-hooks.sh:1064-1106`).
- validation_or_tests: This file is itself the validation artifact. It also specifies section-specific goal-tracker placeholder detection: only the section containing a placeholder should be reported, while all three should be reported when all placeholders remain (`tests/test-plan-file-hooks.sh:777-853`, `tests/test-plan-file-hooks.sh:855-924`, `tests/test-plan-file-hooks.sh:926-1062`). The stop-hook implementation extracts the `Ultimate Goal`, `Acceptance Criteria`, and `Active Tasks` sections independently and looks for `[To be ...]` only inside each section (`hooks/loop-codex-stop-hook.sh:669-730`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-097 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR Bash validator’s Round 0 goal-tracker write gate. It provides the user-facing reason when a Bash command attempts to modify `goal-tracker.md` and directs the agent to use Write/Edit so normal hook validation applies.
- algorithmic_behavior: The template emits a Markdown block headed “Bash Write Blocked: Use Write or Edit Tool,” states that Bash commands must not modify `goal-tracker.md`, renders `{{CORRECT_PATH}}`, and names shell tools such as `cat`, `echo`, `sed`, and `awk` as bypassing validation hooks (`prompt-template/block/goal-tracker-bash-write.md:1-8`).
- inputs_outputs_state: Input is the `CORRECT_PATH` variable supplied by `goal_tracker_bash_blocked_message`; output is a rendered block message sent to stderr by the Bash validator. It has no durable state and no state transition itself.
- gates_or_invariants: The invariant is that Bash writes to `goal-tracker.md` are not accepted as legitimate goal-tracker updates. In Round 0, `hooks/loop-bash-validator.sh` routes matching Bash modifications through `goal_tracker_bash_blocked_message` and exits `2` (`hooks/loop-bash-validator.sh:343-357`). The shared renderer uses this template and falls back to an inline equivalent if missing (`hooks/lib/loop-common.sh:620-629`).
- dependencies_and_callers: Loaded via `load_and_render_safe` from `hooks/lib/template-loader.sh`, which performs single-pass `{{VAR}}` substitution and keeps missing placeholders literal (`hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:170-203`). Caller path is `hooks/loop-bash-validator.sh` after `command_modifies_file "$COMMAND_LOWER" "goal-tracker\.md"` matches (`hooks/loop-bash-validator.sh:349-357`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, `goal_tracker_bash_blocked_message` falls back to a shorter inline message (`hooks/lib/loop-common.sh:623-629`). For rounds greater than 0, the Bash validator does not use this template; it points the agent to the round summary instead (`hooks/loop-bash-validator.sh:349-356`). The template itself is advisory text, so enforcement depends on the caller returning exit `2`.
- validation_or_tests: `tests/test-plan-file-hooks.sh` verifies Bash validator blocking behavior around protected loop files and goal-tracker initialization indirectly through the same hook stack (`tests/test-plan-file-hooks.sh:256-367`, `tests/test-plan-file-hooks.sh:777-1062`). `tests/test-bash-validator-patterns.sh` enumerates write-like Bash forms for `goal-tracker.md`, including redirects, `tee`, in-place edits, `mv`, `cp`, `rm`, `dd`, `truncate`, and file-descriptor redirects, while allowing read-only commands (`tests/test-bash-validator-patterns.sh:75-168` from search result context).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-127 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Prompt template that enforces an interaction gate after Codex review detects open questions. It instructs Claude to ask the user before resolving any other Codex findings.
- algorithmic_behavior: The template is a single notice: Codex found open questions, and Claude “must use `AskUserQuestion`” to clarify with the user before proceeding (`prompt-template/claude/open-question-notice.md:1`). `hooks/loop-codex-stop-hook.sh` scans the Codex review result and sets `HAS_OPEN_QUESTION=true` if any line shorter than 40 characters contains `Open Question` (`hooks/loop-codex-stop-hook.sh:1568-1573`). When true, it loads this template and injects it into the next prompt after the Codex review result end marker and separator (`hooks/loop-codex-stop-hook.sh:1575-1592`).
- inputs_outputs_state: Input is the Codex review result file and the template content. Output is additional prompt text inserted into the next-round prompt. State transition is not a file-state rename; it is a conversation-control transition that changes the next Claude turn from “resolve findings” to “clarify with user first.”
- gates_or_invariants: The gate is ordering: open questions from Codex must be clarified before other remediation. The detection deliberately requires a short line containing `Open Question`, which reduces false positives in long prose but may miss differently formatted headings (`hooks/loop-codex-stop-hook.sh:1568-1573`).
- dependencies_and_callers: Depends on `load_template` from `hooks/lib/template-loader.sh`; if loading fails, the stop hook uses an inline fallback with the same `AskUserQuestion` instruction (`hooks/loop-codex-stop-hook.sh:1577-1580`, `hooks/lib/template-loader.sh:33-48`). It is called only from the RLCR stop-hook review feedback path.
- edge_cases_or_failure_modes: Missing template is non-fatal because of the inline fallback (`hooks/loop-codex-stop-hook.sh:1577-1580`). Detection can under-match if Codex formats open questions in a line of 40+ characters or without the exact `Open Question` phrase. It can over-match short incidental lines containing that phrase.
- validation_or_tests: The search surface shows this template is only referenced by `hooks/loop-codex-stop-hook.sh`, so validation is through stop-hook behavior and template-loading tests rather than a dedicated assigned test (`hooks/loop-codex-stop-hook.sh:1575-1592`, `hooks/lib/template-loader.sh:33-48`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-157 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR active-session discovery. It tests `find_active_loop` behavior under multiple sessions, terminal states, missing directories, unusual names, symlinks, rapid creation, and stale-loop/zombie-loop cases.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates a temp test directory, then repeatedly constructs `rlcr` session directories and calls `find_active_loop "$TEST_DIR/rlcr"` (`tests/robustness/test-session-robustness.sh:14-20`, `tests/robustness/test-session-robustness.sh:33-46`). The implementation under test checks only the newest directory when no session filter is provided and returns it only if `state.md` or `finalize-state.md` exists (`hooks/lib/loop-common.sh:215-257`).
- inputs_outputs_state: Inputs are filesystem layouts: session directories containing `state.md`, `finalize-state.md`, `complete-state.md`, `cancel-state.md`, no state file, symlinked directories, paths with spaces, and deep nested directories (`tests/robustness/test-session-robustness.sh:33-318`). Outputs are pass/fail lines and the helper `print_test_summary` exit status (`tests/robustness/test-session-robustness.sh:318-323`). State transitions are simulated by deleting and recreating `rlcr` trees between tests.
- gates_or_invariants: Newest active session wins when it has `state.md` (`tests/robustness/test-session-robustness.sh:33-46`); `finalize-state.md` is active (`tests/robustness/test-session-robustness.sh:48-61`); many sessions should resolve in under one second (`tests/robustness/test-session-robustness.sh:63-87`); stale older active states must not revive if the newest directory lacks active state or is terminal (`tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:287-302`).
- dependencies_and_callers: Depends on `find_active_loop`, `resolve_active_state_file`, and `resolve_any_state_file` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:176-213`, `hooks/lib/loop-common.sh:233-291`). Callers that rely on this behavior include `scripts/cancel-rlcr-loop.sh` for global cancel discovery (`scripts/cancel-rlcr-loop.sh:69-87`), `hooks/loop-plan-file-validator.sh` for session-aware validation (`hooks/loop-plan-file-validator.sh:29-38`), and `hooks/loop-codex-stop-hook.sh` for active stop-hook routing (`hooks/loop-codex-stop-hook.sh:58-66`).
- edge_cases_or_failure_modes: Empty/nonexistent base directories and base directories with only files return empty (`tests/robustness/test-session-robustness.sh:128-162`). Directories without active state return empty (`tests/robustness/test-session-robustness.sh:164-177`). Non-standard names and spaces are accepted (`tests/robustness/test-session-robustness.sh:179-222`). Deep nested state files are ignored because discovery is one level only (`tests/robustness/test-session-robustness.sh:224-238`). Sessions with only `complete-state.md` or `cancel-state.md` are ignored (`tests/robustness/test-session-robustness.sh:259-285`). The “Unicode” test label does not create a Unicode path; it creates `session-test`, so that case is only a graceful-name smoke test (`tests/robustness/test-session-robustness.sh:304-316`).
- validation_or_tests: This file is itself the validation artifact. It also documents intended zombie-loop protection that directly explains why older active-looking directories are ignored when the newest session is missing state or terminal (`tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:287-302`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6/6 evidence headings present, one per assigned row, in assignment order`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`