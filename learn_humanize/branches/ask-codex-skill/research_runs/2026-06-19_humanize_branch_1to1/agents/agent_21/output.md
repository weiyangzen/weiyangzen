# agent_21 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-021 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow for cancelling the active RLCR loop. It is a thin command contract around `scripts/cancel-rlcr-loop.sh`, with a human confirmation branch for Finalize Phase cancellation.
- algorithmic_behavior: The command allows only three tools: normal cancel script, forced cancel script, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:3`). The required flow is: run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` (`commands/cancel-rlcr-loop.md:11-15`), inspect the first output line, map `NO_LOOP`/`NO_ACTIVE_LOOP` to a no-loop response, map `CANCELLED`/`CANCELLED_FINALIZE` to reporting script output, and branch on `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:17-22`). In the confirmation branch, a user "Yes" triggers the same script with `--force`; "No" leaves Finalize Phase running (`commands/cancel-rlcr-loop.md:23-34`).
- inputs_outputs_state: Inputs are the script output status token, the plugin root path, and optionally the user's confirmation. The command itself does not decide active-loop semantics; it delegates state mutation to the script (`commands/cancel-rlcr-loop.md:36`). The referenced script finds the newest active RLCR loop under `${CLAUDE_PROJECT_DIR:-$(pwd)}/.humanize/rlcr`, creates `.cancel-requested`, removes `.humanize/.pending-session-id`, and renames `state.md` or `finalize-state.md` to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69-75`, `scripts/cancel-rlcr-loop.sh:87-112`, `scripts/cancel-rlcr-loop.sh:148-155`). Script outputs include `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, and `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:89-92`, `scripts/cancel-rlcr-loop.sh:110-112`, `scripts/cancel-rlcr-loop.sh:131-141`, `scripts/cancel-rlcr-loop.sh:161-168`).
- gates_or_invariants: The Finalize Phase gate is explicit: non-forced cancellation of `finalize-state.md` must ask the user first (`commands/cancel-rlcr-loop.md:21-34`; `scripts/cancel-rlcr-loop.sh:131-141`). Active loop detection is centralized in `find_active_loop`, which checks only the newest loop without a session filter to avoid reviving stale older loops (`hooks/lib/loop-common.sh:215-256`). The script intentionally cancels globally without session filtering because slash commands lack hook `session_id` context (`scripts/cancel-rlcr-loop.sh:76-87`). State-file protection elsewhere only allows the cancel move after `.cancel-requested` authorizes it (`hooks/lib/loop-common.sh:809-837`; `hooks/loop-bash-validator.sh:139-145`).
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-loop.sh`, `hooks/lib/loop-common.sh::find_active_loop`, and `AskUserQuestion`. It is referenced as the operator escape path by RLCR setup and stop-hook messages, for example setup instructions mention `/humanize:cancel-rlcr-loop` (`scripts/setup-rlcr-loop.sh:116`) and stop-hook blocks direct users to cancel when needed (`hooks/loop-codex-stop-hook.sh:172`, `hooks/loop-codex-stop-hook.sh:191`, `hooks/loop-codex-stop-hook.sh:205`).
- edge_cases_or_failure_modes: No `.humanize/rlcr` active newest loop returns `NO_LOOP`; a loop directory without `state.md` or `finalize-state.md` returns `NO_ACTIVE_LOOP` (`scripts/cancel-rlcr-loop.sh:89-112`). Finalize cancellation without `--force` exits with code 2 and requires confirmation (`scripts/cancel-rlcr-loop.sh:11-15`, `scripts/cancel-rlcr-loop.sh:131-141`). Unknown script options exit 3 (`scripts/cancel-rlcr-loop.sh:57-60`). Round metadata may be absent, in which case user-facing output uses `?` for round/max (`scripts/cancel-rlcr-loop.sh:119-125`).
- validation_or_tests: The command file is a procedural spec rather than a test. Its behavior is covered indirectly by script state protections and cancel logic. Relevant validation hooks protect state files while permitting authorized cancel moves via `.cancel-requested` (`hooks/loop-bash-validator.sh:133-145`; `hooks/lib/loop-common.sh:809-837`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-051 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR `--agent-teams` algorithm: CLI gating, state serialization, state parsing, initial prompt injection, continuation prompt injection, and phase-specific exclusion in review phase.
- algorithmic_behavior: The test first asserts that `setup-rlcr-loop.sh --agent-teams` fails unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, and rejects other values such as `0`, `false`, `yes`, and `true` (`tests/test-agent-teams.sh:31-79`). It then asserts successful setup creates `state.md` when the env var is exactly `1` (`tests/test-agent-teams.sh:81-111`). It verifies `agent_teams: true` is written with the flag and `agent_teams: false` is the default without the flag (`tests/test-agent-teams.sh:113-153`). It directly tests `parse_state_file` with present and missing `agent_teams` fields (`tests/test-agent-teams.sh:155-209`). Prompt tests require initial team-leader guidance when enabled, no team-leader text when disabled, and existence/content thresholds for `agent-teams-instructions.md`, `agent-teams-core.md`, and `agent-teams-continue.md` (`tests/test-agent-teams.sh:211-355`). Stop-hook integration tests mock `codex` and assert next-round implementation prompts include agent-teams continuation only when `agent_teams=true`, while review-phase prompts exclude it (`tests/test-agent-teams.sh:384-630`).
- inputs_outputs_state: Inputs include a temporary git repo, a gitignored `temp/plan.md`, `CLAUDE_PROJECT_DIR`, optional `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, a synthetic `state.md`, and mocked `codex` commands. Outputs are pass/fail assertions, generated `.humanize/rlcr/*/state.md`, `round-0-prompt.md`, and next-round prompts such as `round-4-prompt.md`/`round-6-prompt.md`. The implementation records `agent_teams: $AGENT_TEAMS` in state (`scripts/setup-rlcr-loop.sh:767-785`), parses it into `STATE_AGENT_TEAMS` (`hooks/lib/loop-common.sh:320-330`), and defaults missing values to `false` in tolerant and strict parsers (`hooks/lib/loop-common.sh:363-372`, `hooks/lib/loop-common.sh:442-447`). The stop hook maps it to `AGENT_TEAMS` (`hooks/loop-codex-stop-hook.sh:104-119`).
- gates_or_invariants: The main gate is exact env enablement: `--agent-teams` requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; any other value fails (`scripts/setup-rlcr-loop.sh:225-228`, `scripts/setup-rlcr-loop.sh:286-297`). State invariant: the field must exist for new loops and missing historical fields parse as `false`. Prompt invariant: initial implementation prompts include the agent-teams header/core only when `AGENT_TEAMS=true` (`scripts/setup-rlcr-loop.sh:1002-1023`). Continuation invariant: next-round agent-teams guidance is appended only during implementation phase, not review phase, via `[[ "$AGENT_TEAMS" == "true" ]] && [[ "$REVIEW_STARTED" != "true" ]]` (`hooks/loop-codex-stop-hook.sh:1630-1640`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` (`tests/test-agent-teams.sh:17-23`). Exercises `scripts/setup-rlcr-loop.sh` (`tests/test-agent-teams.sh:29`) and `hooks/loop-codex-stop-hook.sh` (`tests/test-agent-teams.sh:394-395`). Depends on templates `prompt-template/claude/agent-teams-instructions.md`, `agent-teams-core.md`, and `agent-teams-continue.md` (`tests/test-agent-teams.sh:301-355`). The templates define team-leader responsibilities, Task tool usage with `team_name`, file ownership, coordination, cold-start context, and continuation-specific reset of prior team members (`prompt-template/claude/agent-teams-core.md:1-30`; `prompt-template/claude/agent-teams-continue.md:1-12`).
- edge_cases_or_failure_modes: Missing or non-`1` env var must fail setup. Missing templates are tolerated by runtime fallback prompt content (`scripts/setup-rlcr-loop.sh:1006-1022`; `hooks/loop-codex-stop-hook.sh:1635-1644`), but the test separately expects real template files with minimum size and key headings (`tests/test-agent-teams.sh:297-355`). Review phase is a critical negative case: even with `agent_teams=true`, review-phase prompts must not include agent-teams continuation (`tests/test-agent-teams.sh:599-630`). Older state files without the field must not break parsing and must default to false (`tests/test-agent-teams.sh:184-209`).
- validation_or_tests: This file is itself the validation asset. It builds throwaway repos, commits `.gitignore`, runs setup scripts, searches generated loop artifacts, calls parser functions, mocks `codex exec`/`codex review`, invokes the real stop hook, and finishes via `print_test_summary "Agent Teams Feature Tests"` (`tests/test-agent-teams.sh:24-29`, `tests/test-agent-teams.sh:398-530`, `tests/test-agent-teams.sh:632-636`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-081 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable reference-integrity gate for prompt-template loading. It prevents validators and stop hooks from silently rendering empty or missing block messages.
- algorithmic_behavior: Section 1 scans a fixed set of shell scripts that use templates: stop hook, read/write/edit/bash validators, and `loop-common.sh` (`tests/test-template-references.sh:51-64`). It extracts calls matching `load_template`, `load_and_render`, or `load_and_render_safe` with literal `"$TEMPLATE_DIR"` and a quoted template path, then checks each referenced file exists under `prompt-template/` (`tests/test-template-references.sh:66-111`). Section 2 enumerates every `*.md` template and warns, but does not fail, if the template is not directly referenced by hooks (`tests/test-template-references.sh:116-142`). Section 3 hard-codes common block templates, including `block/todos-file-access.md`, and requires their existence (`tests/test-template-references.sh:145-167`). Section 4 requires critical validators to use `load_and_render_safe` rather than unsafe `load_and_render` (`tests/test-template-references.sh:169-201`). The script exits 0 only when no failures are recorded (`tests/test-template-references.sh:221-230`).
- inputs_outputs_state: Inputs are repository paths resolved from `SCRIPT_DIR`, the `prompt-template` tree, and a fixed script allowlist (`tests/test-template-references.sh:14-17`, `tests/test-template-references.sh:57-64`). Outputs are colored PASS/FAIL/WARN counters, a list of missing templates, and process exit status. It does not mutate repository state. Its internal state is counters plus arrays: `MISSING_TEMPLATES`, `TEMPLATE_FILES`, and `UNREFERENCED` (`tests/test-template-references.sh:25-27`, `tests/test-template-references.sh:71-72`, `tests/test-template-references.sh:121-132`).
- gates_or_invariants: Every literal template reference found in critical hook scripts must resolve to a file. Missing common templates are failures, not warnings (`tests/test-template-references.sh:152-166`). Unreferenced templates are only warnings because dynamic use is allowed (`tests/test-template-references.sh:130-140`). Critical validators `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh` must use `load_and_render_safe` for all render calls, preserving fallback messages when a template is missing (`tests/test-template-references.sh:176-200`). The underlying loader’s safe behavior is to render the fallback when a template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`).
- dependencies_and_callers: Depends on shell, `find`, `grep`, `sed`, and the repository template loader naming conventions. It verifies references used by `hooks/loop-codex-stop-hook.sh`, RLCR validators, and `hooks/lib/loop-common.sh`. It specifically covers `todos_blocked_message`, which renders `block/todos-file-access.md` with fallback text (`hooks/lib/loop-common.sh:571-580`).
- edge_cases_or_failure_modes: The extraction only catches literal quoted paths after `"$TEMPLATE_DIR"`; dynamic template names are not guaranteed to be validated and may appear as unreferenced warnings. Missing scripts are warnings, not failures (`tests/test-template-references.sh:74-78`). Comments are skipped to avoid false positives (`tests/test-template-references.sh:87-91`). Unsafe render detection is line-based and can miss unusual multiline calls, but it is adequate for the repository’s current style (`tests/test-template-references.sh:185-194`).
- validation_or_tests: This is a validation script. Success condition: `TESTS_FAILED == 0`; failure lists missing templates and exits 1 (`tests/test-template-references.sh:213-230`). It validates the existence of the assigned `prompt-template/block/todos-file-access.md` through the common-template list (`tests/test-template-references.sh:152-164`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-111 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR guardrails that prohibit direct `round-*-todos.md` file access and force use of the native task system.
- algorithmic_behavior: The template message is short and deterministic: "Do NOT create or access `round-*-todos.md` files" and "Use the native Task tools instead (TaskCreate, TaskUpdate, TaskList)" (`prompt-template/block/todos-file-access.md:1-8`). It is rendered by `todos_blocked_message`, whose fallback has the same semantic content (`hooks/lib/loop-common.sh:571-580`).
- inputs_outputs_state: It has no variables or placeholders; rendering output is static markdown. It is invoked as a stderr block reason by Read, Write, and Edit validators when a path is recognized as a `todos` round file and is not allowlisted for the active loop (`hooks/loop-read-validator.sh:54-65`; `hooks/loop-write-validator.sh:54-65`; `hooks/loop-edit-validator.sh:37-48`). No state is mutated by the template itself.
- gates_or_invariants: Invariant: agents should not create/read/edit `round-*-todos.md`; task state must live in native Task tools. The guard allows only allowlisted files in the active loop; otherwise it exits with code 2 after emitting the message (`hooks/loop-read-validator.sh:58-65`, `hooks/loop-write-validator.sh:58-65`, `hooks/loop-edit-validator.sh:41-48`). Template availability is a common-template requirement in `tests/test-template-references.sh` (`tests/test-template-references.sh:152-164`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh::load_and_render_safe` via `hooks/lib/loop-common.sh::todos_blocked_message`. Called by `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh`. Related prompt templates also reinforce Task usage, such as `prompt-template/claude/next-round-prompt.md` and finalize prompts, but this block is the guardrail surface.
- edge_cases_or_failure_modes: If the file is missing or empty, `load_and_render_safe` emits the fallback instead of an empty block (`hooks/lib/template-loader.sh:170-203`). The `action` argument passed into `todos_blocked_message` is unused, so Read/Write/Edit get identical message content (`hooks/lib/loop-common.sh:573-580`). Bash validator protection for todos files is less direct than Read/Write/Edit; this assigned block primarily backs tool-path validators.
- validation_or_tests: Directly validated as a common required template by `tests/test-template-references.sh` (`tests/test-template-references.sh:152-164`). Indirectly covered by template loader robustness tests that assert safe fallback behavior for missing templates (`hooks/lib/template-loader.sh:167-203`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-141 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template for the startup case where existing PR comments are already present. It defines the agent’s first remediation cycle before the PR-loop stop hook polls for re-review.
- algorithmic_behavior: The template instructs the agent to analyze existing comments with precedence rules: human comments first, bot comments newest first (`prompt-template/pr-loop/round-0-task-has-comments.md:4-9`). It then requires code reads, fixes, and tests as needed (`prompt-template/pr-loop/round-0-task-has-comments.md:10-14`). After fixing, the agent must commit, push, and post a PR comment that tags the bot mention string to trigger re-review (`prompt-template/pr-loop/round-0-task-has-comments.md:15-21`). Finally it must write a resolution summary to `@{{RESOLVE_PATH}}` listing addressed issues, modified files, and tests (`prompt-template/pr-loop/round-0-task-has-comments.md:23-27`). Important rules prohibit modifying `.humanize/pr-loop/` state files, require pushed fixes, require the correct trigger-comment format, and require addressing valid reviewer concerns (`prompt-template/pr-loop/round-0-task-has-comments.md:30-35`). The note defines the stop-hook transition: poll every 30 seconds up to 15 minutes per bot, validate approvals with local Codex, continue if issues remain, and end if all bots approve (`prompt-template/pr-loop/round-0-task-has-comments.md:39-43`).
- inputs_outputs_state: Template inputs are `{{PR_NUMBER}}`, `{{BOT_MENTION_STRING}}`, and `{{RESOLVE_PATH}}`; they are passed by `setup-pr-loop.sh` in `TEMPLATE_VARS` (`scripts/setup-pr-loop.sh:731-738`). Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the header and fetched comments (`scripts/setup-pr-loop.sh:757-764`, `scripts/setup-pr-loop.sh:847-851`). The intended state transition is external: the agent produces code commits, pushes to remote, posts a GitHub trigger comment, and writes `round-0-pr-resolve.md`; the stop hook later polls and may update `active_bots`, create next-round artifacts, or rename `state.md` to `approve-state.md`.
- gates_or_invariants: This template is selected only when fetched PR comments are not the exact sentinel `*No comments found.*`; setup maps that to `COMMENT_COUNT=1` and loads `pr-loop/round-0-task-has-comments.md` (`scripts/setup-pr-loop.sh:721-729`, `scripts/setup-pr-loop.sh:766-848`). The re-review trigger invariant is `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review the latest changes"` (`prompt-template/pr-loop/round-0-task-has-comments.md:18-21`). PR-loop state files are system-managed and protected by validators (`prompt-template/pr-loop/round-0-task-has-comments.md:32`; `hooks/loop-write-validator.sh:73-97`; `hooks/loop-edit-validator.sh:56-80`). The stop hook separately blocks missing required trigger comments, verifies Claude eyes reaction when relevant, polls bot reviews, deduplicates comments, handles Codex +1 approval, and updates active bot state (`hooks/pr-loop-stop-hook.sh:728-747`, `hooks/pr-loop-stop-hook.sh:768-800`, `hooks/pr-loop-stop-hook.sh:802-867`, `hooks/pr-loop-stop-hook.sh:961-1043`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe` when comments exist (`scripts/setup-pr-loop.sh:801-848`). Depends on `fetch-pr-comments.sh` output semantics for the no-comments sentinel (`scripts/setup-pr-loop.sh:723-729`), GitHub CLI `gh pr comment`, and stop-hook scripts that poll/validate bot feedback. Related templates include `round-0-header.md`, `round-0-task-no-comments.md`, and critical-requirements templates.
- edge_cases_or_failure_modes: If the template is missing, setup uses an embedded fallback with the same task contract (`scripts/setup-pr-loop.sh:803-847`). If the user does not post the bot trigger comment when one is required, the stop hook blocks with `block/no-trigger-comment.md` (`hooks/pr-loop-stop-hook.sh:728-747`). If Claude is configured but does not react with eyes to the trigger, the stop hook blocks after retry (`hooks/pr-loop-stop-hook.sh:768-790`). If no bot comments arrive, timed-out bots may be removed from `active_bots`; if none remain, the loop writes `approve-state.md`, otherwise it blocks with timeout guidance (`hooks/pr-loop-stop-hook.sh:1065-1179`). If `codex` is unavailable for local validation, the stop hook blocks (`hooks/pr-loop-stop-hook.sh:1305-1321`).
- validation_or_tests: Template references are covered by `tests/test-template-references.sh` because `setup-pr-loop.sh` is not in that script’s fixed scan list, so this exact template is not guaranteed by that test. PR-loop behavior is covered by PR-loop test suites referenced in `tests/run-all-tests.sh` and `tests/test-pr-loop-*`, while this file itself functions as the runtime prompt contract.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ASK_CODEX_SKILL-HZ-021`, `ASK_CODEX_SKILL-HZ-051`, `ASK_CODEX_SKILL-HZ-081`, `ASK_CODEX_SKILL-HZ-111`, `ASK_CODEX_SKILL-HZ-141`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`