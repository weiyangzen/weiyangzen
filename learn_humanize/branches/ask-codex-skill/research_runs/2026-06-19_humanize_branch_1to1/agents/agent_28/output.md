# agent_28 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-028 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude stop hook that implements the RLCR loop’s exit gate. It intercepts exit attempts, finds the active `.humanize/rlcr/<timestamp>` loop, validates loop/repo state, runs Codex review, and either blocks Claude with next-round instructions or transitions the loop toward review/finalize/completion. The file declares this purpose directly at lines 3-12.
- algorithmic_behavior: The hook reads hook JSON from stdin, extracts `session_id`, and uses `find_active_loop` plus `resolve_active_state_file` to locate `state.md` or `finalize-state.md` (lines 28-82). It parses YAML frontmatter, maps shared `STATE_*` fields into local variables, and validates Codex model/effort, required round fields, numeric round fields, schema version fields, branch consistency, plan integrity, todo completion, git cleanliness, summary existence, goal tracker initialization, and max iterations before invoking expensive review work (lines 89-159, 166-224, 230-340, 349-398, 428-629, 642-760). It then builds either a regular or full-alignment Codex prompt based on `current_round % full_review_round` (lines 787-873), runs `codex exec` during implementation phase (lines 1278-1308), interprets `COMPLETE`/`STOP` markers only when they appear as the last non-empty line (lines 1423-1430, 1507-1534), and creates the next Claude prompt when review issues remain (lines 1541-1666).
- inputs_outputs_state: Inputs are hook stdin JSON, `CLAUDE_PROJECT_DIR` or cwd, `.humanize/rlcr` state files, round summaries, plan backup, git status, Codex CLI availability, templates, and environment knobs like `HUMANIZE_CODEX_BYPASS_SANDBOX` (lines 28, 42-61, 91-119, 927-932). Outputs are hook JSON decisions with `decision: block` for all repair/continuation paths, debug files under `$XDG_CACHE_HOME/humanize/<project>/<loop>` (lines 903-913), Codex command/stdout/stderr logs (lines 1285-1312), `round-N-review-prompt.md`, `round-N-review-result.md`, `round-N-prompt.md`, and state-file renames or edits. State transitions include `state.md -> finalize-state.md` after Codex review success (lines 1067-1074), `finalize-state.md -> complete-state.md` when finalize checks pass (lines 768-773), `state.md -> maxiter/stop/unexpected-state.md` through `end_loop`, and `review_started: false -> true` plus `.review-phase-started` marker when implementation phase reaches `COMPLETE` (lines 1456-1465).
- gates_or_invariants: The hook fails closed on corrupt or outdated schema, missing `current_round`, missing `max_iterations`, invalid `review_started`, missing `base_branch`, git command timeout/failure, changed branch, modified plan backup, incomplete todos, large changed files over 2000 lines, uncommitted changes, unpushed commits when `push_every_round=true`, missing summary, uninitialized goal tracker placeholders, and invalid review-phase marker (lines 121-159, 166-211, 230-257, 273-340, 351-397, 428-447, 457-531, 541-627, 642-744, 1482-1498). Marker parsing is intentionally strict: `COMPLETE` and `STOP` must be the entire last non-empty line to avoid false positives such as “CANNOT COMPLETE” (lines 1423-1427). Review phase ignores a literal `COMPLETE` marker and instead requires no `[P0-9]` findings from `codex review` (lines 1430-1434, 1476-1504).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for constants, `extract_session_id`, active loop discovery, state parsing, `detect_review_issues`, `end_loop`, and lowercase helpers; `loop-common.sh` documents shared use by this stop hook at lines 5-12 and defines markers/defaults at lines 42-61. Depends on `hooks/lib/template-loader.sh` for `load_template` and `load_and_render_safe`, whose fallback behavior and single-pass rendering are defined at lines 36-47, 58-132, and 170-203. Depends on `scripts/portable-timeout.sh` for bounded git/Codex calls, `hooks/check-todos-from-transcript.py` for task completion gating, `git`, `jq`, `python3`, `awk`, `sed`, `diff`, and `codex`. The hook is invoked by Claude Code’s Stop hook mechanism rather than by another repo shell script.
- edge_cases_or_failure_modes: No active loop or no active state file exits 0 and allows exit (lines 61-77). Missing Codex CLI blocks with install/cancel instructions (lines 881-900). `codex exec` nonzero, missing result file, copy failure from stdout, or empty review result all block with debug paths and retry instructions (lines 1318-1418). `codex review` failure or empty log is a hard retry block, not a skipped gate (lines 1040-1055, 1198-1271). Review-phase manual tampering is guarded by requiring `.review-phase-started` (lines 1482-1498). Max iterations terminates before running another implementation round except in review/finalize phase (lines 751-759). `STOP` on non-alignment rounds is honored but labeled unexpected (lines 1522-1534).
- validation_or_tests: This file’s behavior is covered indirectly by extensive shell tests elsewhere, including template failure tests and finalize/review tests referenced by grep results such as `tests/test-finalize-phase.sh` checking review result file creation. The assigned robustness tests validate two dependencies that this hook relies on: template fallback behavior and git status/state parsing. I did not execute tests because the task asked for research notes only and the branch export is read-only.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-058 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable error-path specification for the template loader used by RLCR hooks. It verifies that missing or malformed template situations degrade to empty output or fallback text instead of crashing hook execution.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh`, derives `TEMPLATE_DIR`, then runs 12 sequential assertions with local `pass`/`fail` counters (lines 11-35, 42-226). It exercises `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir`.
- inputs_outputs_state: Inputs are the project root, the real prompt template directory returned by `get_template_dir`, synthetic missing paths, synthetic template strings, and fallback strings. Outputs are colored PASS/FAIL lines and process exit code 0 when `TESTS_FAILED=0`, otherwise 1 (lines 17-35, 211-226). State is limited to `TESTS_PASSED` and `TESTS_FAILED`; it does not mutate repository files.
- gates_or_invariants: Missing template file and missing template directory must return empty string with exit 0 for `load_template` (lines 42-65). `load_and_render` with a missing template and `render_template` with empty content must return empty output without failure (lines 67-91). Special regex characters in values must render literally (lines 93-106). Under `set -euo pipefail`, missing templates must not abort a caller script (lines 108-131). `load_and_render_safe` must use fallback text and substitute variables inside fallback text (lines 133-158). A valid template directory must pass validation, an invalid directory must fail validation (lines 160-180). Empty placeholders and unclosed placeholders must remain unchanged (lines 182-206).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`. The production functions under test are used heavily by `hooks/loop-codex-stop-hook.sh` for blocking messages and prompt generation, for example `block/git-status-failed.md`, `block/large-files.md`, `block/git-not-clean.md`, `block/work-summary-missing.md`, and `block/codex-review-failed.md` call sites in the stop hook at lines 443, 518, 583, 649, and 1252. The template loader’s single-pass substitution and fallback semantics are defined in `hooks/lib/template-loader.sh` lines 50-132 and 167-203.
- edge_cases_or_failure_modes: Tests missing template file, missing template directory, missing template under strict shell mode, empty template content, special regex characters in variable values, invalid template directory, `{{}}`, and unclosed `{{UNCLOSED`. These edge cases matter because the stop hook must still block safely even if optional prompt templates are absent.
- validation_or_tests: The file is itself the validation. It declares success only when all 12 checks pass and exits nonzero otherwise (lines 218-226). No additional test runner wrapper is needed beyond executing the script in a writable test environment.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-088 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR review phase when Codex review fails to produce usable output. It is part of the stop-hook gating contract: review failure blocks exit and tells Claude to retry rather than allowing completion.
- algorithmic_behavior: The template renders a short failure report containing exit code, expected review result path, command/stdout/stderr debug file paths, and the last stderr content (lines 1-18). It has no executable logic itself; algorithmic behavior comes from being rendered by `load_and_render_safe`.
- inputs_outputs_state: Inputs are template variables `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (lines 5-15). The output is Markdown used as the `reason` in a hook JSON block decision. It does not update state directly.
- gates_or_invariants: The invariant encoded by the text is that Codex review output is mandatory. The final sentence says the system will attempt another review when exiting again (line 18), matching the stop hook’s hard-block behavior in `block_review_failure`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` inside `block_review_failure` via `load_and_render_safe "$TEMPLATE_DIR" "block/codex-review-failed.md"` at lines 1201-1260, then returned as JSON block reason at lines 1262-1270. The stop hook passes additional variables such as `FAILURE_REASON`, `ROUND_NUMBER`, `BASE_BRANCH`, and `EXIT_CODE`; this concrete template currently uses the narrower variable set above, so some caller-provided values are ignored when the file exists.
- edge_cases_or_failure_modes: Because this template expects `CODEX_EXIT_CODE` but the `block_review_failure` caller passes `EXIT_CODE` rather than `CODEX_EXIT_CODE` at lines 1252-1257, the rendered template may leave `{{CODEX_EXIT_CODE}}` unresolved for `codex review` failures. Similarly, the caller passes `CODEX_LOG_FILE` but this template expects `CODEX_STDERR_FILE`; that can leave `{{CODEX_STDERR_FILE}}` unresolved. The fallback embedded in the hook includes the caller’s variable names and richer context, so the mismatch is specific to the external template path.
- validation_or_tests: Template reference coverage is implied by `tests/test-template-references.sh` and comprehensive template tests, but this assigned file is not directly exercised in the provided line reads. Its behavior depends on `template-loader.sh` preserving unknown placeholders as-is, which is specified at lines 115-122 of that loader and tested by `tests/test-error-scenarios.sh` lines 182-206.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-118 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Block-message template used by write validation to enforce the RLCR invariant that round summary files must be written inside the active loop directory, not arbitrary project paths.
- algorithmic_behavior: The template emits a concise Markdown block with a title, the invariant, and the correct path variable (lines 1-5). It has no executable logic by itself.
- inputs_outputs_state: Input is `CORRECT_PATH`, rendered into the correct loop-local summary path on line 5. Output is Markdown sent to stderr by the write validator; no state mutation occurs.
- gates_or_invariants: Enforces “summary files MUST be in the loop directory” (line 3). The production validator applies this when a write target matches `round-N-summary.md` but is not under `.humanize/rlcr` (validator lines 200-211).
- dependencies_and_callers: Called by `hooks/loop-write-validator.sh` at lines 204-211. That validator computes file type and loop membership with shared helpers, finds the active loop for the hook session, parses active state strictly, and sets `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` before rendering this template. It depends on `loop-common.sh` path classifiers and `template-loader.sh`.
- edge_cases_or_failure_modes: If no active loop exists, the write validator exits 0 before this gate (validator lines 126-133). Finalize summaries are separately allowed only at `$ACTIVE_LOOP_DIR/finalize-summary.md` during finalize phase (validator lines 164-175), so this template is for regular round summaries only. If the template is missing, the validator fallback still blocks and prints a correct path (validator lines 205-210).
- validation_or_tests: Covered indirectly by template-loader tests for fallback rendering and by write-validator tests if present. The direct call site is clear and deterministic in `hooks/loop-write-validator.sh`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-148 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for git-state and git-status helper behavior used by RLCR tooling. It validates that git summary and state detection remain stable across normal repositories, dirty worktrees, detached HEAD, rebase/merge markers, shallow clones, non-git directories, and permission-like failures.
- algorithmic_behavior: The script sources `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`, creates isolated temporary repositories, then calls production functions `humanize_parse_git_status` and `humanize_detect_git_state` across 25 cases (lines 13-21, 28-62, 71-507). `parse_result` extracts pipe-delimited fields from the status summary (lines 35-47), and `init_test_repo` creates a committed baseline repo with local git config (lines 49-62).
- inputs_outputs_state: Inputs are temporary git repos and file mutations created during the test: untracked files, modified files, staged additions, deletions, renamed files, files with spaces, binary files, staged+unstaged changes, branch changes, manual `.git/rebase-merge`, `.git/MERGE_HEAD`, `.git/shallow`, and unreadable `.git` simulation. Outputs are PASS/FAIL assertions plus `print_test_summary` and the final exit code (lines 502-507). State is confined to the test temp directory, but it performs real git writes inside that temp workspace.
- gates_or_invariants: `humanize_parse_git_status` must return counts as `modified|added|deleted|untracked|insertions|deletions` for clean, untracked, modified, staged, insertion, deletion, mixed-state, rename, many untracked, space-containing filenames, binary, and same-file staged+unstaged cases (test lines 71-367). `humanize_detect_git_state` must return `normal`, `detached`, `rebase`, `merge`, `shallow`, `not_a_repo`, or tolerate `permission_error/not_a_repo` depending on platform behavior (test lines 377-500). The production implementation under test counts porcelain statuses and diffstat in `scripts/humanize.sh` lines 170-215, and detects git states in priority order at lines 120-168.
- dependencies_and_callers: Depends on real `git`, shell helpers from `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and production `scripts/humanize.sh`. The git helpers are relevant to RLCR state machine behavior because stop hooks and validators rely on git state/cleanliness gates before allowing loop exit or completion. The assigned stop hook has its own direct `git status --porcelain` gate at lines 428-629, while broader CLI/monitor surfaces use `humanize_parse_git_status` and `humanize_detect_git_state`.
- edge_cases_or_failure_modes: Handles non-git directories by expecting a non-crashing string containing `not a git repo` from status parsing (lines 168-177). Detached HEAD must still parse status as clean and state as `detached` (lines 180-195, 388-401). Rebase, merge, and shallow states are simulated through marker files/directories rather than full operations (lines 403-463). Permission simulation allows either `permission_error` or `not_a_repo` for platform variance (lines 478-500). Empty repo with no commits must return a non-empty result rather than crash (lines 233-246).
- validation_or_tests: The file is itself the validation suite. It exits with the result of `print_test_summary`, so any failed assertion fails the script (lines 502-507). I did not execute it in this read-only branch export because it creates temporary repos and writes files; the requested deliverable is research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections above; each assigned row is represented once as its own `###` section.
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`