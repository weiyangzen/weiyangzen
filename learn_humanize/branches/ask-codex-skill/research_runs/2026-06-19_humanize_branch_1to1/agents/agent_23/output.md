# agent_23 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-023 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow entry for starting the PR review loop with remote bot monitoring. The file is intentionally a thin command surface; it delegates execution to `${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh` with user arguments at `commands/start-pr-loop.md:12-14`.
- algorithmic_behavior: The documented loop is: detect current branch PR, fetch selected bot reviews, have the agent fix issues, push changes, comment with bot mentions, require a resolution summary, then let the Stop Hook poll every 30 seconds with a 15-minute timeout and invoke local Codex validation (`commands/start-pr-loop.md:16-24`, `commands/start-pr-loop.md:37-45`). Comment ordering gives human comments priority over bot comments, then processes newest bot comments first (`commands/start-pr-loop.md:31-35`).
- inputs_outputs_state: Inputs are required bot flags `--claude` or `--codex` and optional loop/model controls from the argument hint (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-29`). The delegated setup script validates bot flags, prerequisites, PR state, YAML-safe branch/model values, and creates `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-pr-loop.sh:180-190`, `scripts/setup-pr-loop.sh:220-277`, `scripts/setup-pr-loop.sh:283-390`, `scripts/setup-pr-loop.sh:615-635`, `scripts/setup-pr-loop.sh:641-713`, `scripts/setup-pr-loop.sh:721-851`).
- gates_or_invariants: At least one bot flag is mandatory; summaries must be written, fixes must be pushed, bot mentions must match the setup prompt, and users are told not to bypass the loop by editing state or cancel files (`commands/start-pr-loop.md:50-56`). The setup script also enforces one active RLCR/PR loop at a time and refuses merged/closed PRs (`scripts/setup-pr-loop.sh:220-241`, `scripts/setup-pr-loop.sh:365-374`).
- dependencies_and_callers: Depends directly on `scripts/setup-pr-loop.sh` via the allowed Bash tool declaration (`commands/start-pr-loop.md:4`). Runtime dependencies flow through the setup script: `git`, `gh`, authenticated GitHub CLI, `codex`, `portable-timeout.sh`, `template-loader.sh`, `loop-common.sh`, `fetch-pr-comments.sh`, and optionally `check-bot-reactions.sh` for Claude eyes confirmation (`scripts/setup-pr-loop.sh:33-40`, `scripts/setup-pr-loop.sh:255-277`, `scripts/setup-pr-loop.sh:564-600`).
- edge_cases_or_failure_modes: Missing bot flags, unknown args, no active PR, fork PR lookup, unsafe branch/model strings, missing GitHub auth, missing Codex CLI, inability to post or locate trigger comments, and missing Claude eyes reaction all block setup (`scripts/setup-pr-loop.sh:111-174`, `scripts/setup-pr-loop.sh:337-356`, `scripts/setup-pr-loop.sh:384-407`, `scripts/setup-pr-loop.sh:520-600`). Loop termination is by max iterations, all monitored bots approving, or `/humanize:cancel-pr-loop` (`commands/start-pr-loop.md:58-62`).
- validation_or_tests: PR-loop setup and monitor behavior are covered by `tests/test-pr-loop-system.sh` and related PR-loop test suites; the command wrapper itself is mostly validated through setup-script tests such as help/argument and integration paths referenced in `tests/test-pr-loop-system.sh:1385-1483`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-053 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the test aggregator’s ANSI-stripping and pass/fail count parsing logic. It protects `tests/run-all-tests.sh` from miscounting colored suite summaries on GNU/Linux and BSD/macOS sed.
- algorithmic_behavior: The test defines `esc=$'\033'`, strips SGR color sequences with `sed "s/${esc}\\[[0-9;]*m//g"`, then extracts counts using `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed:[[:space:]]*[0-9]+` followed by a numeric suffix grep (`tests/test-ansi-parsing.sh:38-47`, `tests/test-ansi-parsing.sh:74-90`). The production aggregator uses the same algorithm at `tests/run-all-tests.sh:157-177`.
- inputs_outputs_state: Inputs are synthetic strings containing basic color, multiple colors, bold/color combinations, plain text, zero counts, and a multiline suite summary (`tests/test-ansi-parsing.sh:45-153`). Outputs are colored PASS/FAIL lines plus a final Passed/Failed summary; state is maintained only in `TESTS_PASSED` and `TESTS_FAILED` counters (`tests/test-ansi-parsing.sh:18-31`, `tests/test-ansi-parsing.sh:163-178`).
- gates_or_invariants: Stripping ANSI codes must preserve visible text and numeric counts; zero must remain a real count; plain text without ANSI must continue to parse; multiline summaries must choose the final extracted count using `tail -1` (`tests/test-ansi-parsing.sh:98-129`, `tests/test-ansi-parsing.sh:132-158`).
- dependencies_and_callers: Depends on bash ANSI-C quoting, `sed`, `grep`, and `echo -e`. It is included in the aggregate suite list at `tests/run-all-tests.sh:31-42`, and that runner consumes suite summaries using the same regex extraction.
- edge_cases_or_failure_modes: The stripper targets only CSI SGR sequences ending in `m` with digits/semicolons, so non-SGR terminal controls are outside scope. Count extraction assumes summary labels contain `Passed:` and `Failed:` followed by integers. The test explicitly covers zero failed counts and no-ANSI input but not malformed labels or localized output.
- validation_or_tests: Contains eight focused assertions and exits `0` only if `TESTS_FAILED` is zero (`tests/test-ansi-parsing.sh:41-178`). This research run inspected the executable spec but did not run it because the task requested notes only in a read-only branch export.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-083 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the Stop Hook gate that prevents loop exit while Claude legacy TodoWrite items or file-backed Task-system items remain incomplete.
- algorithmic_behavior: The script feeds hook JSON into the Python checker and asserts exit-code semantics: invalid hook JSON returns `2`, empty/no transcript/nonexistent transcript returns `0`, completed todos return `0`, incomplete/pending/in-progress todos return `1`, and output includes incomplete task details (`tests/test-todo-checker.sh:52-158`). It also verifies transcript variants: invalid JSONL lines are ignored, the latest TodoWrite snapshot wins, direct `tool_use` entries work, and `type: message` entries work (`tests/test-todo-checker.sh:167-241`).
- inputs_outputs_state: Inputs are temporary JSONL transcripts and mock task files under a generated `tasks_base_dir`; state lives only in `TEST_DIR`, removed by the EXIT trap (`tests/test-todo-checker.sh:37-39`, `tests/test-todo-checker.sh:305-308`). Outputs are PASS/FAIL lines and the checker’s exit code/output captured in `RESULT` and `EXIT_CODE` (`tests/test-todo-checker.sh:21-35`).
- gates_or_invariants: For legacy TodoWrite, any status other than `completed` is incomplete, including missing status and empty content (`tests/test-todo-checker.sh:250-279`). For file-backed Tasks, missing status defaults to pending, `completed` passes, `deleted` is ignored, and mixed legacy/file inputs must still block if any Task is incomplete (`tests/test-todo-checker.sh:309-443`).
- dependencies_and_callers: Depends on `python3`, `mktemp`, and `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:12-15`). The checker’s production algorithms extract tool calls from assistant/message/direct entries, track the latest TodoWrite list, read `~/.claude/tasks/<session_id>`, and format `INCOMPLETE_TODOS` output (`hooks/check-todos-from-transcript.py:23-55`, `hooks/check-todos-from-transcript.py:58-101`, `hooks/check-todos-from-transcript.py:104-147`, `hooks/check-todos-from-transcript.py:150-196`).
- edge_cases_or_failure_modes: Nonexistent session directories and nonexistent transcript files allow exit, malformed transcript lines are ignored, and malformed/unreadable task JSON is skipped by implementation (`hooks/check-todos-from-transcript.py:64-65`, `hooks/check-todos-from-transcript.py:76-80`, `hooks/check-todos-from-transcript.py:121-145`). A parse error in hook input is distinct and blocks via exit code `2`.
- validation_or_tests: The production Stop Hook invokes this checker before expensive Codex review; exit `2` becomes a parse-error block and exit `1` renders `block/incomplete-todos.md` with the incomplete list (`hooks/loop-codex-stop-hook.sh:349-397`). The assigned test script defines 25 tests and exits `0` only when all pass (`tests/test-todo-checker.sh:457-475`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-113 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the “summary file missing” gate in the Codex/RLCR Stop Hook. It defines the human-facing contract for what must be written before the agent may exit.
- algorithmic_behavior: The template renders `{{SUMMARY_FILE}}` and tells the agent that exit was attempted without a work summary, then requires a summary covering implementation, created/modified files, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:1-16`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` template variable. Output is a markdown reason string returned inside the Stop Hook’s JSON block response; the template itself performs no state mutation.
- gates_or_invariants: The gate is existence-based in the hook: normal rounds require `round-${CURRENT_ROUND}-summary.md`, finalize phase requires `finalize-summary.md`, and missing files block exit (`hooks/loop-codex-stop-hook.sh:631-667`). The template’s invariant is that the exact required path is shown to the agent.
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/work-summary-missing.md"` with a fallback message (`hooks/loop-codex-stop-hook.sh:646-650`). It depends on the template loader and the Stop Hook’s `SUMMARY_FILE` calculation.
- edge_cases_or_failure_modes: If the template is missing or fails to render, the Stop Hook has a fallback reason (`hooks/loop-codex-stop-hook.sh:646-650`). The gate checks file existence, not semantic completeness of the summary contents; the bullets are a review contract for the agent rather than parser-enforced fields.
- validation_or_tests: `tests/test-template-references.sh` scans shell template references and verifies referenced template files exist, including block templates used by hooks (`tests/test-template-references.sh:56-110`, `tests/test-template-references.sh:172-200`). Stop-hook behavior around this gate is covered indirectly by hook and template tests.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-143 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for RLCR and PR-loop monitoring, sourced rather than executed. It centralizes terminal colors, file/session/state detection, PR-loop phase inference, and markdown goal-tracker parsing (`scripts/lib/monitor-common.sh:3-6`).
- algorithmic_behavior: Functions emit values through stdout: color helpers return ANSI codes (`scripts/lib/monitor-common.sh:14-23`), `monitor_get_file_size` uses GNU/BSD `stat` fallbacks (`scripts/lib/monitor-common.sh:29-35`), `monitor_find_latest_session` selects the lexicographically latest timestamp directory matching `YYYY-MM-DD_HH-MM-SS` (`scripts/lib/monitor-common.sh:37-63`), and `monitor_find_state_file` prioritizes live `state.md` over terminal `*-state.md` files (`scripts/lib/monitor-common.sh:147-188`).
- inputs_outputs_state: Inputs are loop directories, session directories, markdown state files, PR check files, YAML frontmatter, and goal tracker markdown. Outputs include `path|status`, phase names, display strings, truncated strings, and pipe-delimited statistics (`scripts/lib/monitor-common.sh:197-252`, `scripts/lib/monitor-common.sh:267-381`, `scripts/lib/monitor-common.sh:390-498`). The only persistent side effect is PR phase cache writes to `/tmp/humanize-phase-${session_name}-${highest_round}.size` (`scripts/lib/monitor-common.sh:297-307`); terminal helpers alter scroll region/cursor state (`scripts/lib/monitor-common.sh:69-92`).
- gates_or_invariants: PR phase detection checks final states first (`approve-state.md`, `cancel-state.md`, `maxiter-state.md`), then treats the latest `round-*-pr-check.md` as `codex_analyzing` if file size grows or mtime is under 10 seconds, else falls back to `waiting_initial_review` for `current_round: 0` plus `startup_case: 1`, or `waiting_reviewer` (`scripts/lib/monitor-common.sh:272-341`). Status display maps phase/status to stable human-readable text (`scripts/lib/monitor-common.sh:344-381`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at startup (`scripts/humanize.sh:6-10`). The monitor uses `monitor_get_file_size` for incremental log reading (`scripts/humanize.sh:765-768`), `get_pr_loop_phase` and `get_pr_loop_phase_display` for PR monitor headers (`scripts/humanize.sh:1313-1335`, `scripts/humanize.sh:1451-1457`), and `humanize_parse_pr_goal_tracker` for PR issue stats (`scripts/humanize.sh:1355-1363`). Shell dependencies are `find`, `stat`, `sed`, `grep`, `sort`, `wc`, `date`, `tput`, and writable `/tmp` for phase cache.
- edge_cases_or_failure_modes: Missing directories return empty or `unknown`; malformed/missing tracker files return zeroed records (`scripts/lib/monitor-common.sh:44-47`, `scripts/lib/monitor-common.sh:157-160`, `scripts/lib/monitor-common.sh:392-394`, `scripts/lib/monitor-common.sh:470-472`). YAML parsing is grep/sed based, not full YAML (`scripts/lib/monitor-common.sh:194-209`). `*-state.md` selection depends on `find` order after `state.md` is absent, cache keys can collide for sessions with the same basename, and first observation of an old non-empty `pr-check` file can be classified as analyzing because previous cached size defaults to zero.
- validation_or_tests: `tests/test-pr-loop-system.sh` directly sources this library and tests approved/cancelled/maxiter/waiting/codex-analyzing phases, state-file detection, latest-session selection, file-size fallback, and goal-tracker parsers (`tests/test-pr-loop-system.sh:256-371`, `tests/test-pr-loop-system.sh:377-417`, `tests/test-pr-loop-system.sh:423-452`, `tests/test-pr-loop-system.sh:770-843`, `tests/test-pr-loop-system.sh:849-899`). No files were modified during this research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item evidence headings present, one per assigned row; item identifiers are not repeated outside their headings.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`