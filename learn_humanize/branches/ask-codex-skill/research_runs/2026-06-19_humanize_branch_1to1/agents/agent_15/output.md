# agent_15 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-015 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: Template pack for the PR review loop’s generated user prompts and tracker-update contract. The directory contains seven prompt fragments: `goal-tracker-initial.md`, `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, `critical-requirements-no-comments.md`, `critical-requirements-has-comments.md`, and `codex-goal-tracker-update.md`.
- algorithmic_behavior: `scripts/setup-pr-loop.sh` renders these files with loop variables to create `.humanize/pr-loop/<timestamp>/goal-tracker.md` and `round-0-prompt.md`. The setup script builds initial state and bot display data at `scripts/setup-pr-loop.sh:608`, creates goal tracker variables at `scripts/setup-pr-loop.sh:658`, renders `goal-tracker-initial.md` at `scripts/setup-pr-loop.sh:712`, renders the round-0 header at `scripts/setup-pr-loop.sh:758`, chooses no-comments vs has-comments task templates at `scripts/setup-pr-loop.sh:766`, and renders matching critical completion requirements at `scripts/setup-pr-loop.sh:890`. `hooks/pr-loop-stop-hook.sh` separately renders `codex-goal-tracker-update.md` into the local Codex review prompt at `hooks/pr-loop-stop-hook.sh:1253` and appends it at `hooks/pr-loop-stop-hook.sh:1302`.
- inputs_outputs_state: Inputs are template variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `STARTED_AT`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are rendered prompt/tracker text under `.humanize/pr-loop/<timestamp>/`. The initial tracker records PR number, branch, monitored bots, startup case, an initial issue summary row, and totals in `goal-tracker-initial.md:3` through `prompt-template/pr-loop/goal-tracker-initial.md:33`. Round-0 prompts embed fetched PR comments after `round-0-header.md:12`, then add either wait-for-auto-review instructions from `round-0-task-no-comments.md:4` or issue-fixing instructions from `round-0-task-has-comments.md:4`.
- gates_or_invariants: The no-comments path explicitly forbids triggering review by comment because first reviews are automatic at `prompt-template/pr-loop/round-0-task-no-comments.md:20`. The has-comments path requires analyzing human comments first, then bot comments newest first at `prompt-template/pr-loop/round-0-task-has-comments.md:6`, and requires commit, push, PR comment trigger, and resolution summary at `prompt-template/pr-loop/round-0-task-has-comments.md:15`. Both paths prohibit modifying `.humanize/pr-loop/` state files at `prompt-template/pr-loop/round-0-task-has-comments.md:32` and `prompt-template/pr-loop/round-0-task-no-comments.md:21`. The tracker-update template requires preserving tracker structure and only updating issue summary, total statistics, and issue log at `prompt-template/pr-loop/codex-goal-tracker-update.md:57`.
- dependencies_and_callers: Main caller is `scripts/setup-pr-loop.sh` for startup prompt generation. Runtime caller is `hooks/pr-loop-stop-hook.sh` for Codex tracker-update instructions. The template loader is reached through `load_and_render_safe`; call sites were found in `scripts/setup-pr-loop.sh:712`, `scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:800`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:914`, `scripts/setup-pr-loop.sh:940`, and `hooks/pr-loop-stop-hook.sh:1262`.
- edge_cases_or_failure_modes: The directory encodes two round-0 branches: no fetched comments uses passive waiting and no trigger comment; existing comments uses active fix/commit/push/re-trigger. If templates are missing or fail to load, `scripts/setup-pr-loop.sh` has inline fallbacks for each fragment at `scripts/setup-pr-loop.sh:668`, `scripts/setup-pr-loop.sh:740`, `scripts/setup-pr-loop.sh:769`, `scripts/setup-pr-loop.sh:803`, `scripts/setup-pr-loop.sh:893`, and `scripts/setup-pr-loop.sh:916`. A subtle behavior is that `goal-tracker-initial.md` is issue-summary oriented, while setup’s inline fallback includes acceptance criteria rows, so template/fallback shape differs.
- validation_or_tests: Direct template coverage appears in `tests/test-pr-loop-system.sh:644` for `goal-tracker-initial.md`. Runtime behavior is covered indirectly by stop-hook tests that inspect tracker updates and active bot state, especially mixed approval at `tests/test-pr-loop-stophook.sh:1520` and assertions at `tests/test-pr-loop-stophook.sh:1711`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-045 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Startup script for the RLCR loop, a local iterative “Claude implements, Codex reviews” workflow. It validates inputs and repository state, creates `.humanize/rlcr/<timestamp>/` loop state, initializes goal tracking, and emits the initial round-0 prompt.
- algorithmic_behavior: The script parses CLI options at `scripts/setup-rlcr-loop.sh:133`, including max iterations, Codex model/effort, timeout, push policy, base branch, full review interval, skip-implementation mode, Codex-question handling, and agent teams. It enforces mutual exclusion with active RLCR/PR loops at `scripts/setup-rlcr-loop.sh:259`, validates agent-teams env at `scripts/setup-rlcr-loop.sh:286`, validates plan path and content at `scripts/setup-rlcr-loop.sh:346` and `scripts/setup-rlcr-loop.sh:512`, checks Codex availability at `scripts/setup-rlcr-loop.sh:579`, requires a clean git working tree at `scripts/setup-rlcr-loop.sh:626`, resolves base branch at `scripts/setup-rlcr-loop.sh:648`, captures `base_commit` at `scripts/setup-rlcr-loop.sh:711`, then writes state and prompts under `.humanize/rlcr`.
- inputs_outputs_state: Inputs are a relative plan path or `--skip-impl`, plus options such as `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--track-plan-file`, `--claude-answer-codex`, and `--agent-teams`. It uses `CLAUDE_PROJECT_DIR` or `pwd` for project root at `scripts/setup-rlcr-loop.sh:251`. Outputs include `state.md` with fields `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, `ask_codex_question`, `session_id`, `agent_teams`, and `started_at` at `scripts/setup-rlcr-loop.sh:767`; `.humanize/.pending-session-id` at `scripts/setup-rlcr-loop.sh:789`; optional `.review-phase-started` for skip-impl at `scripts/setup-rlcr-loop.sh:800`; `goal-tracker.md` at `scripts/setup-rlcr-loop.sh:809`; and `round-0-prompt.md` at `scripts/setup-rlcr-loop.sh:934`.
- gates_or_invariants: Only one loop can be active at a time (`scripts/setup-rlcr-loop.sh:259`). Plan paths must be relative, space-free, shell-metacharacter-free, non-symlinked, inside the project, and not inside a submodule (`scripts/setup-rlcr-loop.sh:356`, `scripts/setup-rlcr-loop.sh:362`, `scripts/setup-rlcr-loop.sh:370`, `scripts/setup-rlcr-loop.sh:382`, `scripts/setup-rlcr-loop.sh:430`, `scripts/setup-rlcr-loop.sh:444`). Plan tracking policy is strict: tracked and clean only with `--track-plan-file`; otherwise the plan must be untracked/gitignored (`scripts/setup-rlcr-loop.sh:483`). Content must have at least five lines and at least three non-comment content lines (`scripts/setup-rlcr-loop.sh:519`, `scripts/setup-rlcr-loop.sh:567`). Branch/model/effort fields are YAML-safety validated at `scripts/setup-rlcr-loop.sh:597`, `scripts/setup-rlcr-loop.sh:607`, and `scripts/setup-rlcr-loop.sh:616`.
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:26`, `hooks/lib/loop-common.sh` at `scripts/setup-rlcr-loop.sh:30`, Git commands via `run_with_timeout`, Codex CLI, and optional Claude Code Agent Teams templates under `$TEMPLATE_DIR/claude/` at `scripts/setup-rlcr-loop.sh:1003`. Shared defaults come from `hooks/lib/loop-common.sh`, which defines default Codex model/effort at `hooks/lib/loop-common.sh:42`.
- edge_cases_or_failure_modes: Skip-impl mode without a plan creates a placeholder plan and starts directly in review mode (`scripts/setup-rlcr-loop.sh:309`, `scripts/setup-rlcr-loop.sh:733`, `scripts/setup-rlcr-loop.sh:764`). Git timeouts fail closed for repo, status, and tracking checks. Remote-only base branches are rejected because `codex review --base` needs a local ref (`scripts/setup-rlcr-loop.sh:652`). If no local main/master/default exists, setup fails at `scripts/setup-rlcr-loop.sh:690`. Output SIGPIPE is trapped only after setup work is complete at `scripts/setup-rlcr-loop.sh:1071`, avoiding partial internal-pipeline masking.
- validation_or_tests: The file is not itself a test, but it creates state consumed by RLCR hooks and monitoring. Related validation is reflected in plan-file hook and robustness tests found by repository search, including `tests/robustness/test-setup-scripts-robustness.sh` and `tests/test-plan-file-hooks.sh`. In this read-only export, I did not execute tests.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-075 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/pr-loop-stop-hook.sh`, focused on PR-loop exit blocking, polling, trigger freshness, bot approval/removal, timeout behavior, startup-case re-evaluation, fork PR lookup, and goal-tracker correctness.
- algorithmic_behavior: Defines `run_stophook_tests()` at `tests/test-pr-loop-stophook.sh:15`, with isolated test functions that create synthetic `.humanize/pr-loop/<timestamp>/state.md`, write the expected `round-N-pr-resolve.md`, inject mock `gh`, `git`, and sometimes `codex` binaries into `PATH`, set `CLAUDE_PROJECT_DIR`, and run `hooks/pr-loop-stop-hook.sh` with JSON stdin. Registered tests run at `tests/test-pr-loop-stophook.sh:1768`.
- inputs_outputs_state: Inputs are state fields such as `current_round`, `configured_bots`, `active_bots`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, `trigger_comment_id`, `poll_interval`, and `poll_timeout`, plus mocked GitHub API/repo responses. Expected outputs are hook JSON blocks, messages, state-file rewrites, `approve-state.md`, `round-1-pr-check.md`, and updated `active_bots`. Examples: empty active bots should create `approve-state.md` at `tests/test-pr-loop-stophook.sh:246`; bot timeout should create approval state and empty active bots at `tests/test-pr-loop-stophook.sh:754`; mixed approval should keep Codex active and remove Claude at `tests/test-pr-loop-stophook.sh:1746`.
- gates_or_invariants: Old trigger comments before `latest_commit_at` must not satisfy force-push/new-commit trigger requirements (`tests/test-pr-loop-stophook.sh:20`). Round 0 startup case 1 must not require a trigger (`tests/test-pr-loop-stophook.sh:140`). Unpushed commits must block exit (`tests/test-pr-loop-stophook.sh:452`). History rewrite must block and require re-trigger (`tests/test-pr-loop-stophook.sh:538`). Startup case 4/5 missing trigger must block (`tests/test-pr-loop-stophook.sh:642`). Claude bot trigger flow requires eyes reaction or blocks (`tests/test-pr-loop-stophook.sh:1011`). Mixed approval must not prematurely complete (`tests/test-pr-loop-stophook.sh:1734`).
- dependencies_and_callers: Sourced by `tests/test-pr-loop.sh:34` and `tests/test-pr-loop-3-stophook.sh:20`; both call `run_stophook_tests`. It exercises `hooks/pr-loop-stop-hook.sh`, which implements trigger detection at `hooks/pr-loop-stop-hook.sh:466`, trigger-required logic at `hooks/pr-loop-stop-hook.sh:613`, Codex thumbs-up handling at `hooks/pr-loop-stop-hook.sh:647`, Claude eyes verification at `hooks/pr-loop-stop-hook.sh:750`, polling at `hooks/pr-loop-stop-hook.sh:806`, Codex validation prompt construction at `hooks/pr-loop-stop-hook.sh:1243`, and active-bot state updates at `hooks/pr-loop-stop-hook.sh:1425`.
- edge_cases_or_failure_modes: Covers pagination/JQ shape differences in mocked `gh` output, old triggers after force push, round-0 no-trigger exception, no active bots, comments arriving after loop start, unpushed local commits, missing trigger, no bot responses, Codex approval via PR reaction, Claude bot not responding with eyes, dynamic timestamps for polling, fork repository PR resolution, and mixed approve/issues result. One defined test, `test_stophook_dynamic_startup_case` at `tests/test-pr-loop-stophook.sh:328`, is not included in the final registration list; the later `test_stophook_dynamic_startup_case_update` is registered instead at `tests/test-pr-loop-stophook.sh:1778`.
- validation_or_tests: This file is the validation. It asserts `T-STOPHOOK-1` through `T-STOPHOOK-13c`, including force-push validation (`tests/test-pr-loop-stophook.sh:132`), case-1 trigger exemption (`tests/test-pr-loop-stophook.sh:233`), approve-state creation (`tests/test-pr-loop-stophook.sh:314`), timeout state cleanup (`tests/test-pr-loop-stophook.sh:877`), fork PR support (`tests/test-pr-loop-stophook.sh:1504`), and mixed approval active-bot correctness (`tests/test-pr-loop-stophook.sh:1752`). I inspected but did not execute it in this read-only branch export.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-105 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Hook-block message template for attempts to write generated PR-loop prompt/comment files.
- algorithmic_behavior: The file states that `round-*-pr-comment.md` and `round-*-prompt.md` under `.humanize/pr-loop/` are generated and read-only. Its content is concise: the title at `prompt-template/block/pr-loop-prompt-write.md:1`, the blocked path patterns at `prompt-template/block/pr-loop-prompt-write.md:3`, and generated-file explanations at `prompt-template/block/pr-loop-prompt-write.md:5`.
- inputs_outputs_state: It has no dynamic placeholders. Output is a rendered block message returned by validators when a user/tool attempts to mutate PR-loop generated prompt/comment artifacts. It does not mutate state itself.
- gates_or_invariants: Enforces an invariant that PR-loop generated prompts and fetched-comment files are system-owned, while user work should go into the round resolve file instead. This supports the broader state-file and prompt-file immutability model.
- dependencies_and_callers: Loaded through `pr_loop_prompt_blocked_message()` in `hooks/lib/loop-common.sh:1026`; that function calls `load_and_render_safe "$TEMPLATE_DIR" "block/pr-loop-prompt-write.md"` at `hooks/lib/loop-common.sh:1033`. `loop-common.sh` initializes the template loader and directory at `hooks/lib/loop-common.sh:156`.
- edge_cases_or_failure_modes: If the template cannot be loaded, `loop-common.sh` supplies a fallback with the same core message at `hooks/lib/loop-common.sh:1028`. The template only covers writes to generated prompt/comment files; separate block templates cover PR-loop state mutation.
- validation_or_tests: Indirectly covered by hook/validator tests that exercise PR-loop file protections. The direct call-site evidence is in `hooks/lib/loop-common.sh:1017` through `hooks/lib/loop-common.sh:1034`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-135 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Structural template for `/humanize:gen-plan`; it defines the canonical plan shape that later loop setup and plan validators expect.
- algorithmic_behavior: The template requires a title, goal description, acceptance criteria with positive and negative tests, path boundaries, feasibility hints, dependencies/sequence, and implementation notes. Acceptance criteria are explicitly TDD-oriented at `prompt-template/plan/gen-plan-template.md:6`; path boundaries define upper/lower acceptable scope and allowed choices at `prompt-template/plan/gen-plan-template.md:25`; dependencies are milestone/sequence based at `prompt-template/plan/gen-plan-template.md:57`; implementation notes prohibit leaking plan workflow labels into implementation code at `prompt-template/plan/gen-plan-template.md:69`.
- inputs_outputs_state: Consumed by `scripts/validate-gen-plan-io.sh`, which validates `--input` and `--output`, locates the template via `CLAUDE_PLUGIN_ROOT` or script-relative fallback at `scripts/validate-gen-plan-io.sh:133`, copies it to the output path at `scripts/validate-gen-plan-io.sh:148`, then appends the original draft between delimiters at `scripts/validate-gen-plan-io.sh:151`. The generated plan becomes an input to RLCR setup, whose plan parser extracts goal/objective/purpose and acceptance/criteria/requirements sections at `scripts/setup-rlcr-loop.sh:857` and `scripts/setup-rlcr-loop.sh:881`.
- gates_or_invariants: The template enforces deterministic acceptance criteria by requiring positive and negative tests. It also bounds implementation scope through upper/lower bound sections and allowed/prohibited choices. It instructs that highly deterministic designs should make upper and lower bounds converge when choices are fixed at `prompt-template/plan/gen-plan-template.md:44`. It prohibits implementation code/comments from containing plan-specific workflow markers at `prompt-template/plan/gen-plan-template.md:71`.
- dependencies_and_callers: Main caller is `scripts/validate-gen-plan-io.sh`, invoked by `commands/gen-plan.md` as shown by repository search at `commands/gen-plan.md:35`. Related tests for the command and IO validator are in `tests/test-gen-plan.sh`, especially the script tests starting around `tests/test-gen-plan.sh:426`.
- edge_cases_or_failure_modes: If the template file is missing, `validate-gen-plan-io.sh` exits with code 7 as plugin configuration error at `scripts/validate-gen-plan-io.sh:142`. IO validation rejects missing input, empty input, missing output directory, existing output file, output directory paths, and unwritable output directories at `scripts/validate-gen-plan-io.sh:79` through `scripts/validate-gen-plan-io.sh:123`. The template itself is a prompt scaffold, so correctness depends on downstream model filling placeholders with concrete plan content.
- validation_or_tests: `tests/test-gen-plan.sh` validates command structure and IO behavior, including missing arg exit code 6, input-not-found exit 1, empty input exit 2, missing output dir exit 3, existing output exit 4, directory output exit 4, valid paths exit 0, and help exit 6, as referenced in search hits around `tests/test-gen-plan.sh:426`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, each represented once as a heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`