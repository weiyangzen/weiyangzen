# agent_29 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-029 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for `Edit` calls during RLCR and PR-loop operation. It protects loop-owned files and enforces the RLCR state machine boundary for editable artifacts.
- algorithmic_behavior: Reads hook JSON from stdin, exits immediately unless `.tool_name` is `Edit`, extracts `.tool_input.file_path`, lowercases the path, and optionally extracts `session_id` for session-aware active-loop lookup at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:24). It blocks round todo edits unless allowlisted, blocks round prompt edits, applies PR-loop protections, then only applies RLCR rules when the target path is under `.humanize/rlcr/` at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:41) and [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:86).
- inputs_outputs_state: Input is Claude hook JSON. Output is no stdout on allow, diagnostic text on stderr on block, and exit code `0` for allow, `2` for policy block, `1` for malformed active state. State read is the active loop directory under `$CLAUDE_PROJECT_DIR/.humanize/rlcr`, filtered by hook `session_id`; `resolve_active_state_file` prefers `finalize-state.md` before `state.md` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/loop-common.sh:180), and `find_active_loop` intentionally checks only the newest active loop unless session-filtered at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/loop-common.sh:233).
- gates_or_invariants: State files are hook-owned: `finalize-state.md` is checked before generic `state.md` because the generic predicate also matches it at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:113). Strict state parsing must succeed before protected RLCR edits proceed at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:102). `goal-tracker.md` is editable only in round `0`; later edits must go through summary-driven update flow at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:141). Summary edits must target the current round unless allowlisted at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:155).
- dependencies_and_callers: Depends on `jq` and `hooks/lib/loop-common.sh` helpers: `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, `is_finalize_state_file_path`, `is_state_file_path`, PR-loop predicates, and template rendering. Directly validated by finalize tests at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-finalize-phase.sh:369), allowlist tests for edit validator behavior, plan-file hook tests, and robustness hook tests at [tests/robustness/test-hook-system-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-hook-system-robustness.sh:40).
- edge_cases_or_failure_modes: Malformed JSON can fail via `jq` under `set -euo pipefail`; robustness tests expect non-crash non-128 failure behavior at [tests/robustness/test-hook-system-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-hook-system-robustness.sh:80). Paths outside `.humanize/rlcr` are allowed and delegated to sandbox enforcement. Path traversal that still lands on `.humanize/rlcr/.../state.md` is blocked. Missing active loop means allow. Malformed active state fails closed.
- validation_or_tests: `tests/test-finalize-phase.sh` asserts edit attempts on `finalize-state.md` return exit `2` with a finalize message. `tests/robustness/test-hook-system-robustness.sh` covers valid JSON, malformed JSON, missing `file_path`, outside-loop paths, traversal to `state.md`, and embedded newlines in `old_string`/`new_string`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-059 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase: entry after Codex `COMPLETE`, protected finalize state, required final summary, todo/git-clean gates, no Codex invocation during finalize completion, and normal-round non-regression.
- algorithmic_behavior: Builds an isolated git repo in `mktemp`, sources production `hooks/lib/loop-common.sh`, creates mock `codex` binaries for success/failure/empty-review scenarios, writes loop state fixtures under `.humanize/rlcr/<timestamp>`, and invokes production validators plus `loop-codex-stop-hook.sh` with hook JSON. Setup functions create canonical state frontmatter including `current_round`, `max_iterations`, `review_started`, `plan_file`, `start_branch`, and `base_branch` at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-finalize-phase.sh:180).
- inputs_outputs_state: Inputs are synthetic hook JSON, mock Codex stdout/stderr behavior, fake transcript JSONL for todo status, and loop files. Outputs are pass/fail counters and shell exit equal to `TESTS_FAILED`. State transitions asserted include `state.md -> finalize-state.md` on successful complete/review path, `finalize-state.md -> complete-state.md` on finalization, `state.md -> maxiter-state.md` when complete occurs at max iterations, and preservation of `state.md` with `review_started: true` on review failures.
- gates_or_invariants: `finalize-state.md` is active but not user-modifiable; `complete-state.md` is terminal and not active. Finalize completion requires `finalize-summary.md`, clean git status, and completed todos; it bypasses Codex review because Codex already confirmed completion. Max-iteration handling takes precedence over entering Finalize Phase. Review failures and empty review output are hard blocks, not shortcuts to finalize, matching production `run_and_handle_code_review` failure paths at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:1034).
- dependencies_and_callers: Depends on `git`, `jq`, `python3` todo checker through the stop hook, production validators `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-read-validator.sh`, `loop-plan-file-validator.sh`, and shared helper predicates. It verifies stop-hook behavior whose phase detection is defined at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:68), summary gate at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:632), finalize completion at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:763), and complete/review transition at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:1429).
- edge_cases_or_failure_modes: Covers missing summary, dirty git tree, incomplete todos, finalize-state write/edit/bash bypasses including `mv`/`cp` from the state file, review command nonzero, empty review stdout, max iterations, terminal `complete-state.md` not revived as active, and normal RLCR rounds retaining round increment/review-result behavior.
- validation_or_tests: The file is itself the validation artifact. Major sections start with helper predicate tests at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-finalize-phase.sh:238), finalize-state active-loop tests at line 283, write/edit/bash protection tests at line 337, stop-hook finalize gates at line 417, complete/max-iteration tests at line 506, review failure tests at line 562, empty-review tests at line 621, todo gate tests at line 698, and validator parsing tests at line 816.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-089 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: User-facing block template for attempts to modify `finalize-state.md`; it is part of the validator contract for Finalize Phase state ownership.
- algorithmic_behavior: Provides the rendered policy reason: `finalize-state.md` is loop-system-managed during Finalize Phase, and the actor should instead run simplification, commit changes, and write `finalize-summary.md` at [prompt-template/block/finalize-state-file-modification.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/block/finalize-state-file-modification.md:1). It has no branching logic itself; behavior is selected by `finalize_state_file_blocked_message`.
- inputs_outputs_state: Input is implicit template lookup from validator code. Output is Markdown text emitted to stderr by edit/write/bash validators. State is not mutated; the template reinforces that only the loop system may mutate `finalize-state.md`.
- gates_or_invariants: The invariant is that `finalize-state.md` must not be modified via user tools. Shared helper `finalize_state_file_blocked_message` loads this exact block and falls back to an inline equivalent if missing at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/loop-common.sh:600). Validators deliberately check finalize-specific paths before generic `state.md` matching.
- dependencies_and_callers: Called via `load_and_render_safe "$TEMPLATE_DIR" "block/finalize-state-file-modification.md"` in `hooks/lib/loop-common.sh`. Used by `loop-edit-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh`; bash protection covers direct writes, destination matches, source `mv/cp`, command segments, and shell-wrapper bypasses at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-bash-validator.sh:139) and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-bash-validator.sh:286).
- edge_cases_or_failure_modes: If the template is unavailable or template directory validation fails, the helper emits the fallback block rather than allowing the modification. The message must remain specific to finalize state; a generic state block would be less precise because `state.md` pattern matching also catches `finalize-state.md`.
- validation_or_tests: `tests/test-finalize-phase.sh` asserts write, edit, bash redirect, `mv FROM`, and `cp FROM` modifications to `finalize-state.md` are blocked with finalize wording at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-finalize-phase.sh:357). Template reference coverage also searches validator/template references.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-119 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Continuation prompt block for Agent Teams mode in subsequent RLCR implementation rounds after Codex feedback. It defines how team-leader behavior carries forward across rounds without assuming old subagents persist.
- algorithmic_behavior: Instructs the leader to create a new team, analyze Codex review before spawning members, avoid redoing already-correct work, provide cold-start context to new members, prioritize critical findings if work exceeds one round, and inspect current file state before assigning ownership at [prompt-template/claude/agent-teams-continue.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/claude/agent-teams-continue.md:1).
- inputs_outputs_state: Input is prior-round Codex feedback and the current loop state where `agent_teams: true` and `review_started != true`. Output is appended Markdown in the next round prompt file. It does not mutate state directly, but it shapes worker/team orchestration for the next RLCR cycle.
- gates_or_invariants: Only used for implementation continuation, not review phase. The stop hook appends this continuation block plus `agent-teams-core.md` only when `AGENT_TEAMS == true` and `REVIEW_STARTED != true`; otherwise it omits it or uses a shorter fallback if templates are missing at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:1630).
- dependencies_and_callers: Loaded by `loop-codex-stop-hook.sh` when building `NEXT_PROMPT_FILE` after Codex found issues. Coordinates with `prompt-template/claude/agent-teams-core.md`, `goal-tracker-update-request.md`, next-round footer, and review feedback handling. Dedicated tests verify the file exists, has sufficient content, and contains the `Continuation Context` section at [tests/test-agent-teams.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-agent-teams.sh:335).
- edge_cases_or_failure_modes: Missing continuation or core template degrades to a fallback that preserves basic delegation guidance but loses the richer invariants about cold-start members and not reusing stale teammates. Misuse during review phase would be wrong because Codex review, not team implementation, owns that phase.
- validation_or_tests: `tests/test-agent-teams.sh` checks file size and continuation-specific guidance. `rg` references show production loading at `hooks/loop-codex-stop-hook.sh:1633` and no direct parser dependency beyond template loading.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-149 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for `humanize_parse_goal_tracker`, the production parser that summarizes RLCR `goal-tracker.md` progress for monitor/status surfaces.
- algorithmic_behavior: Sources `tests/test-helpers.sh`, then production `scripts/humanize.sh`, creates temporary goal-tracker variants, invokes `humanize_parse_goal_tracker`, and validates parsed fields via `parse_result` at [tests/robustness/test-goal-tracker-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-goal-tracker-robustness.sh:38). The production function returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/humanize.sh:36).
- inputs_outputs_state: Inputs are markdown files with sections such as `### Acceptance Criteria`, `#### Active Tasks`, `### Completed and Verified`, `### Explicitly Deferred`, `### Open Issues`, and `### Ultimate Goal`. Output is the pipe-delimited summary string. No persistent state is mutated beyond temporary test files.
- gates_or_invariants: Parser must tolerate list and table AC formats, count active tasks excluding completed/deferred rows, count completed task rows and unique completed AC IDs, count deferred/open issue tables, return defaults for missing files, and avoid failing on empty, malformed, truncated, binary-mixed, special-character, or large files. Production section boundaries and regexes are sed/grep based at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/humanize.sh:54), active-task counting at line 62, completed/deferred/open issue counting at line 92, and goal summary extraction at line 110.
- dependencies_and_callers: Depends on `scripts/humanize.sh`, which sources monitor utilities and `hooks/lib/loop-common.sh`. Test helpers provide `setup_test_dir`, `pass`, `fail`, and `print_test_summary`. The parser is consumed by humanize monitor/status code to summarize goal tracker health; nearby code aliases `_parse_goal_tracker` to `humanize_parse_goal_tracker` at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/humanize.sh:371).
- edge_cases_or_failure_modes: Decimal AC IDs such as `AC-3.1` are intentionally not counted by the simple AC regex. Table row counting subtracts two header rows, so malformed tables can undercount or zero out. Pipe-delimited output can be fragile if goal summaries contain pipes, although this test suite does not cover that. Binary content is expected to yield at least parseable leading ACs rather than crash.
- validation_or_tests: The file defines 18 tests: standard list ACs, table ACs, active/completed/unique AC counts, goal summary extraction, missing/empty files, 60 ACs, special characters, malformed/truncated/binary content, open issues, deferred tasks, headers-only files, mixed bold/sub-numbered ACs, and long goal summaries at [tests/robustness/test-goal-tracker-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-goal-tracker-robustness.sh:60).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 section headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`