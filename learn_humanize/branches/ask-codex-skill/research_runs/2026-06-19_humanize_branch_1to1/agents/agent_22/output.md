# agent_22 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-022 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Command workflow definition for transforming a user draft into a structured implementation plan with goals, AC-format acceptance criteria, path boundaries, dependencies, feasibility notes, and preserved original draft content.
- algorithmic_behavior: The workflow is staged: IO validation, relevance check, draft analysis, issue resolution, plan generation, write/review completion (`commands/gen-plan.md:19-26`). Phase 1 delegates argument and path checks to `scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:30-47`), which copies `prompt-template/plan/gen-plan-template.md` to the output and appends the original draft (`scripts/validate-gen-plan-io.sh:133-160`). Phase 2 asks `humanize:draft-relevance-checker` to return `RELEVANT` or `NOT_RELEVANT` (`commands/gen-plan.md:56-72`). Phase 3 requires repository exploration for clarity, consistency, completeness, and functional feasibility (`commands/gen-plan.md:76-108`). Phase 4 resolves analysis issues and explicitly classifies numeric metrics as hard requirements or optimization directions (`commands/gen-plan.md:111-139`). Phase 5 defines the output schema and generation rules (`commands/gen-plan.md:142-248`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, `--input` draft path, `--output` plan path, repository context, user clarifications, and the plan template. Output is a new plan file containing generated structured plan content plus the original draft retained at the bottom (`commands/gen-plan.md:251-260`). State transition is draft file -> validated output shell/template seed -> analyzed requirements -> edited completed plan -> optional language-unified plan -> user-facing report (`commands/gen-plan.md:262-292`).
- gates_or_invariants: Hard gates include input exists/non-empty, output directory exists, output path does not already exist, writable output directory, and plan template presence via exit codes 1-7 (`commands/gen-plan.md:38-47`; `scripts/validate-gen-plan-io.sh:79-146`). Plan invariants require AC-X/AC-X.Y criteria, positive and negative tests, no time estimates, no code line references, all draft information preserved, deterministic designs respected, and no plan-progress terms in implementation code/comments (`commands/gen-plan.md:225-248`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, `prompt-template/plan/gen-plan-template.md`, `Task`, `Read`, `Glob`, `Grep`, `AskUserQuestion`, and intended file editing. The validation script depends on `CLAUDE_PLUGIN_ROOT` or script-relative template lookup (`scripts/validate-gen-plan-io.sh:133-140`).
- edge_cases_or_failure_modes: Stop conditions include irrelevant drafts, unresolved/acknowledged issues needing user clarification, missing quantitative metric classification, template configuration failure, and multilingual output requiring optional user-guided language unification (`commands/gen-plan.md:67-70`, `commands/gen-plan.md:126-138`, `commands/gen-plan.md:272-284`, `commands/gen-plan.md:296-304`). Notable contract mismatch: frontmatter allows `Write` but not `Edit` (`commands/gen-plan.md:4-11`), while Phase 6 explicitly requires the Edit tool, not Write (`commands/gen-plan.md:257`).
- validation_or_tests: IO validation is implemented by `scripts/validate-gen-plan-io.sh`. Template-wide validation in `tests/test-templates-comprehensive.sh` loads every `prompt-template/**/*.md` file (`tests/test-templates-comprehensive.sh:90-107`), validates placeholder syntax (`tests/test-templates-comprehensive.sh:118-194`), and stress-renders all templates with dummy values (`tests/test-templates-comprehensive.sh:563-606`), covering `prompt-template/plan/gen-plan-template.md`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-052 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR allowlist behavior across shared allowlist logic and Read/Write/Edit/Bash hook validators.
- algorithmic_behavior: Builds a temp git repo and active loop directory `.humanize/rlcr/2024-01-01_12-00-00`, seeds `state.md` with `current_round: 5`, `max_iterations: 42`, and `review_started: false` (`tests/test-allowlist-validators.sh:29-64`), then feeds JSON hook payloads to validator scripts. It first tests `is_allowlisted_file` directly (`tests/test-allowlist-validators.sh:66-126`), then verifies Write, Edit, Read, and Bash validators (`tests/test-allowlist-validators.sh:129-383`).
- inputs_outputs_state: Inputs are synthetic tool events such as `{"tool_name":"Write","tool_input":{"file_path":...}}`, `CLAUDE_PROJECT_DIR`, temp loop state, and active loop path. Outputs are PASS/FAIL lines and process exit equal to `TESTS_FAILED` (`tests/test-allowlist-validators.sh:385-393`). State is local to the temp repo and cleaned with a trap (`tests/test-allowlist-validators.sh:29-31`).
- gates_or_invariants: The allowlist is exact-path and exact-basename: only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` under the active loop dir pass (`hooks/lib/loop-common.sh:551-569`). Wrong rounds, wrong directories, generic relative paths, old loop dirs, and same-basename different roots must block. Bash is stricter for todos: command text must contain the active loop dir and `round-[12]-todos.md` (`hooks/loop-bash-validator.sh:385-391`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`tests/test-allowlist-validators.sh:15-17`) and calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh`. Those validators depend on active-loop discovery, strict state parsing, path-type detection, and rendered block messages from `prompt-template/block/*`.
- edge_cases_or_failure_modes: Security-centered cases include wrong active directory (`tests/test-allowlist-validators.sh:120-126`), old loop directory (`tests/test-allowlist-validators.sh:355-368`), same basename under `/tmp` (`tests/test-allowlist-validators.sh:370-383`), and bare `round-1-todos.md` in Bash (`tests/test-allowlist-validators.sh:342-353`). Historical summaries are intentionally allowed even though current round is 5 (`tests/test-allowlist-validators.sh:88-102`, `tests/test-allowlist-validators.sh:148-159`, `tests/test-allowlist-validators.sh:204-215`).
- validation_or_tests: This file is itself the validation harness. Expected allow decisions use exit code 0; expected blocks use exit code 2 plus message text containing `todos` or `round` (`tests/test-allowlist-validators.sh:168-184`, `tests/test-allowlist-validators.sh:267-283`, `tests/test-allowlist-validators.sh:323-382`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-082 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Comprehensive executable specification for prompt-template directory validity, template placeholder syntax, single-pass rendering behavior, fallback behavior, and broad corpus renderability.
- algorithmic_behavior: Resolves the template directory through `get_template_dir`, verifies required subdirectories (`block`, `codex`, `claude`) (`tests/test-templates-comprehensive.sh:59-80`), loads every markdown template (`tests/test-templates-comprehensive.sh:90-107`), applies regex-based syntax checks for malformed `{{VAR}}` placeholders (`tests/test-templates-comprehensive.sh:118-194`), runs explicit malformed placeholder detection checks (`tests/test-templates-comprehensive.sh:197-259`), exercises rendering edge cases (`tests/test-templates-comprehensive.sh:261-439`), validates fallback mechanics (`tests/test-templates-comprehensive.sh:440-490`), tests selected real templates (`tests/test-templates-comprehensive.sh:492-540`), and stress-renders all templates (`tests/test-templates-comprehensive.sh:542-606`).
- inputs_outputs_state: Inputs are `prompt-template/**/*.md`, `hooks/lib/template-loader.sh`, temporary malformed-template fixtures, and variable assignments like `VAR=value`. Outputs are colored PASS/FAIL/WARN lines and exit 0 only when `TESTS_FAILED == 0` (`tests/test-templates-comprehensive.sh:608-625`). Temporary malformed/incomplete dirs are created and removed during execution (`tests/test-templates-comprehensive.sh:204-259`, `tests/test-templates-comprehensive.sh:481-490`).
- gates_or_invariants: Placeholders must use uppercase `{{VAR_NAME}}` style, with no extra braces, wrong bracket types, spaces, or lowercase names (`tests/test-templates-comprehensive.sh:122-176`). Empty templates warn rather than fail (`tests/test-templates-comprehensive.sh:96-102`). Missing templates must fall back through `load_and_render_safe` (`tests/test-templates-comprehensive.sh:445-453`). Warnings for unreplaced placeholders in the all-template stress pass do not fail the suite (`tests/test-templates-comprehensive.sh:590-597`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh` (`tests/test-templates-comprehensive.sh:15-18`). The loader defines single-pass substitution and missing-variable preservation (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:50-132`), safe fallback rendering (`hooks/lib/template-loader.sh:167-203`), and directory validation (`hooks/lib/template-loader.sh:205-222`).
- edge_cases_or_failure_modes: Tests many edge cases but not all with exact equality: backslashes, quotes, multiline values, and markdown values are mostly “no crash” assertions (`tests/test-templates-comprehensive.sh:328-345`, `tests/test-templates-comprehensive.sh:373-402`). The “CJK” section uses ASCII placeholder values in this export (`tests/test-templates-comprehensive.sh:347-360`). Syntax detection is regex-based and intentionally skips noisy single-brace checks (`tests/test-templates-comprehensive.sh:160-164`).
- validation_or_tests: Real-template assertions include `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:497-540`). The assigned `prompt-template/pr-loop/round-0-task-no-comments.md` is covered by all-template load/syntax/render loops, while `prompt-template/block/unpushed-commits.md` also has a dedicated assertion (`tests/test-templates-comprehensive.sh:517-528`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-112 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when a loop cannot exit because local commits have not been pushed.
- algorithmic_behavior: Renders a stop-block message stating the ahead count and current branch, explains that `--push-every-round` requires pushing before exit, and provides `git push origin {{CURRENT_BRANCH}}` as the required action (`prompt-template/block/unpushed-commits.md:1-12`).
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH` placeholders (`prompt-template/block/unpushed-commits.md:3`, `prompt-template/block/unpushed-commits.md:9`). Output is a markdown block reason consumed by hook JSON responses. The template itself is stateless; loop state transition is handled by stop hooks that block exit until pushed.
- gates_or_invariants: The gate is active only when caller logic detects unpushed commits. In the RLCR stop hook, it runs only when `PUSH_EVERY_ROUND == true`, `git status -sb` reports `ahead N`, and the hook returns JSON with `"decision": "block"` (`hooks/loop-codex-stop-hook.sh:598-626`). In PR loop, any positive `AHEAD_COUNT` blocks because bots must review pushed commits (`hooks/pr-loop-stop-hook.sh:350-360`).
- dependencies_and_callers: Rendered through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:614-616`) and `hooks/pr-loop-stop-hook.sh` (`hooks/pr-loop-stop-hook.sh:356-357`). Rendering semantics come from `hooks/lib/template-loader.sh`, which preserves missing placeholders and performs single-pass substitution (`hooks/lib/template-loader.sh:50-132`).
- edge_cases_or_failure_modes: If `CURRENT_BRANCH` cannot be resolved in RLCR, caller passes `unknown` (`hooks/loop-codex-stop-hook.sh:607`). The template inserts the branch directly into a shell command without shell quoting beyond markdown formatting, so unusual branch names may produce a command users should inspect before copying. In PR-loop context, the same template mentions `--push-every-round`, although PR-loop caller blocks for reviewability even outside that RLCR flag context.
- validation_or_tests: Dedicated comprehensive template test renders it with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserts the title, count text, and branch appear (`tests/test-templates-comprehensive.sh:517-528`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-142 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop task prompt fragment for Round 0 when the fetched PR comments file indicates there are no review comments yet.
- algorithmic_behavior: Instructs the agent to wait for automatic monitored-bot reviews, write an initial summary to `@{{RESOLVE_PATH}}`, avoid comments that would manually trigger review, avoid modifying `.humanize/pr-loop/` state files, and rely on the Stop Hook for polling and validation (`prompt-template/pr-loop/round-0-task-no-comments.md:4-29`).
- inputs_outputs_state: Inputs are `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH` (`prompt-template/pr-loop/round-0-task-no-comments.md:6`, `prompt-template/pr-loop/round-0-task-no-comments.md:12`). Output is prompt content appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` by setup code. The intended state transition is Round 0 initialized with no comments -> agent writes `round-0-pr-resolve.md` -> Stop Hook polls bots -> issues continue the loop or approvals end it (`prompt-template/pr-loop/round-0-task-no-comments.md:26-29`).
- gates_or_invariants: Core invariant is “do not comment to trigger review”; first reviews are automatic (`prompt-template/pr-loop/round-0-task-no-comments.md:18-22`). The resolve summary path must be the provided current-round resolve file. PR-loop validators separately block state-file and read-only prompt/comment modifications while allowing current-round `round-N-pr-resolve.md` writes through round validation (`hooks/loop-write-validator.sh:77-96`, `hooks/loop-edit-validator.sh:60-79`).
- dependencies_and_callers: `scripts/setup-pr-loop.sh` chooses this template when `COMMENT_FILE` contains the exact sentinel `*No comments found.*` (`scripts/setup-pr-loop.sh:721-729`, `scripts/setup-pr-loop.sh:766-800`). It renders with `load_and_render_safe` and the variables built at setup time (`scripts/setup-pr-loop.sh:731-738`, `scripts/setup-pr-loop.sh:800`).
- edge_cases_or_failure_modes: Comment detection depends on the sentinel line; malformed or nonstandard comment fetch output could route to the wrong task template (`scripts/setup-pr-loop.sh:723-729`). If bots do not review within the stop-hook polling window stated in the prompt, the loop remains waiting rather than resolving immediately (`prompt-template/pr-loop/round-0-task-no-comments.md:26-29`). Missing variables would remain as placeholders under the loader’s missing-variable behavior (`hooks/lib/template-loader.sh:7-14`).
- validation_or_tests: Covered by the comprehensive template suite’s all-template load, syntax, and dummy render passes (`tests/test-templates-comprehensive.sh:90-107`, `tests/test-templates-comprehensive.sh:118-194`, `tests/test-templates-comprehensive.sh:563-606`). Setup call-site fallback text mirrors the template content, giving a runtime fallback if the file is unavailable (`scripts/setup-pr-loop.sh:769-800`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique Item Evidence headings present; each assigned row is represented once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`