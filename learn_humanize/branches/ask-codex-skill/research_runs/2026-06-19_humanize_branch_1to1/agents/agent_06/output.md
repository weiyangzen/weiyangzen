# agent_06 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts/` tree is the command/runtime layer for Humanize loop workflows. It contains startup, cancellation, GitHub PR polling, Codex invocation, terminal monitoring, timeout portability, and plan-generation IO validation. Recursive inspection found 13 files: 12 top-level shell scripts plus `scripts/lib/monitor-common.sh`; no deeper child directories beyond `scripts/lib`.
- algorithmic_behavior: `setup-rlcr-loop.sh` bootstraps RLCR state from a plan, enforces mutual exclusion against PR loops, validates plan paths, captures base commit, and writes `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:259`, `scripts/setup-rlcr-loop.sh:767`, `scripts/setup-rlcr-loop.sh:934`). `setup-pr-loop.sh` bootstraps PR review loops, resolves fork/base repo context, fetches initial comments, classifies startup cases, and creates `.humanize/pr-loop/<timestamp>/state.md`, goal tracker, prompt, and critical requirements (`scripts/setup-pr-loop.sh:259`, `scripts/setup-pr-loop.sh:433`, `scripts/setup-pr-loop.sh:608`, `scripts/setup-pr-loop.sh:721`). `humanize.sh` exposes monitors for RLCR and PR loops, parsing state/goal/git status and rendering fixed terminal status bars with live tailing (`scripts/humanize.sh:222`, `scripts/humanize.sh:1150`). `ask-codex.sh` is the one-shot Codex consultation path with input/output/metadata capture and timeout-wrapped `codex exec` (`scripts/ask-codex.sh:153`, `scripts/ask-codex.sh:243`, `scripts/ask-codex.sh:296`).
- inputs_outputs_state: Inputs include slash-command arguments, `CLAUDE_PROJECT_DIR`, Git repository state, GitHub CLI API data, Codex CLI availability, plan files, PR numbers, bot selections, and existing `.humanize` loop state. Outputs include prompt files, state files, goal trackers, PR comment markdown, JSON poll results, `.cancel-requested` signals, terminal monitor displays, Codex output files, and exit/status messages. State transitions include active loop creation, round incrementing by hooks, terminal state files such as `cancel-state.md` or `approve-state.md`, and monitor phase detection via state files and `round-*-pr-check.md` growth.
- gates_or_invariants: The tree enforces one active loop type at a time (`setup-rlcr-loop.sh:259`, `setup-pr-loop.sh:259`), rejects unsafe plan paths and YAML-unsafe branch/model values (`setup-rlcr-loop.sh:356`, `setup-pr-loop.sh:347`), requires `gh`, `jq`, `git`, and `codex` where needed, validates PR existence/state before PR-loop startup (`setup-pr-loop.sh:206`, `setup-pr-loop.sh:331`), uses per-command timeout wrappers (`scripts/portable-timeout.sh:33`), and fail-closes on missing required arguments. PR polling intentionally returns valid JSON with `has_new_comments:false` on API failure so the outer loop can continue (`scripts/poll-pr-reviews.sh:207`, `scripts/poll-pr-reviews.sh:317`).
- dependencies_and_callers: Scripts depend heavily on `hooks/lib/loop-common.sh` for shared constants, loop discovery, bot mapping, YAML list construction, and template rendering (`hooks/lib/loop-common.sh:854`, `hooks/lib/loop-common.sh:883`, `hooks/lib/loop-common.sh:926`). PR runtime coordination happens with `hooks/pr-loop-stop-hook.sh`, which consumes PR state, checks triggers/reactions, polls `scripts/poll-pr-reviews.sh`, calls `scripts/check-bot-reactions.sh`, and updates `active_bots` (`hooks/pr-loop-stop-hook.sh:65`, `hooks/pr-loop-stop-hook.sh:680`, `hooks/pr-loop-stop-hook.sh:883`). RLCR next-round coordination happens with `hooks/loop-codex-stop-hook.sh` and `prompt-template/claude/*` (`hooks/loop-codex-stop-hook.sh:1546`). GitHub API scripts coordinate as a pipeline: `check-pr-reviewer-status.sh` classifies startup cases, `fetch-pr-comments.sh` builds readable review context, `poll-pr-reviews.sh` returns machine JSON, and `check-bot-reactions.sh` handles Codex `+1` and Claude `eyes`.
- edge_cases_or_failure_modes: PR fork handling is repeatedly encoded because comments/reactions live on the base repo, not necessarily the current checkout (`scripts/fetch-pr-comments.sh:110`, `scripts/poll-pr-reviews.sh:122`, `scripts/check-bot-reactions.sh:101`). API failures are often downgraded to warnings or empty arrays, while missing local prerequisites fail immediately. Monitor code must restore terminal scroll regions and traps even on deletion or signals (`scripts/humanize.sh:675`, `scripts/humanize.sh:703`, `scripts/humanize.sh:1392`). Some scripts rely on `grep | sed | tr` under `set -euo pipefail`, so “default if not found” code may be unreachable if the pipeline fails, notably in cancellation metadata extraction patterns.
- validation_or_tests: Shell syntax validation passed for assigned and key referenced shell files: `bash -n scripts/cancel-pr-loop.sh`, `tests/test-monitor-runtime.sh`, `tests/robustness/test-pr-loop-api-robustness.sh`, `scripts/lib/monitor-common.sh`, `scripts/poll-pr-reviews.sh`, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh`. Behavioral tests are specified in `tests/test-monitor-runtime.sh` and `tests/robustness/test-pr-loop-api-robustness.sh`; I did not run full behavioral tests because this branch export is read-only and those tests create temp repos/mock executables.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-036 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Implements the PR-loop cancellation transition for `.humanize/pr-loop/`, distinct from RLCR cancellation. It is a runtime state mutation script for stopping an active PR loop.
- algorithmic_behavior: Parses `--force` and help, though `--force` currently has no behavioral effect (`scripts/cancel-pr-loop.sh:24`, `scripts/cancel-pr-loop.sh:40`). Resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, selects the newest loop directory under `.humanize/pr-loop` with `ls -1d | sort -r | head -1`, requires `state.md`, extracts round/max/PR number, touches `.cancel-requested`, then renames `state.md` to `cancel-state.md` (`scripts/cancel-pr-loop.sh:72`, `scripts/cancel-pr-loop.sh:76`, `scripts/cancel-pr-loop.sh:88`, `scripts/cancel-pr-loop.sh:105`, `scripts/cancel-pr-loop.sh:119`, `scripts/cancel-pr-loop.sh:122`).
- inputs_outputs_state: Inputs are CLI flags, filesystem state under `.humanize/pr-loop`, and YAML-like state fields `current_round`, `max_iterations`, and `pr_number`. Outputs are stdout status tokens `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`, exit codes `0`, `1`, or `3`, a `.cancel-requested` signal file, and a terminal `cancel-state.md` preserving the prior active state.
- gates_or_invariants: It only operates on PR loops, not `.humanize/rlcr` (`scripts/cancel-pr-loop.sh:55`). It requires the newest PR-loop directory to contain `state.md`; otherwise it refuses to cancel and exits `1` (`scripts/cancel-pr-loop.sh:78`, `scripts/cancel-pr-loop.sh:91`). Unknown options fail with exit `3` (`scripts/cancel-pr-loop.sh:60`).
- dependencies_and_callers: It is designed for the `/humanize:cancel-pr-loop` user path. Runtime consumers include PR-loop poll logic that checks for `.cancel-requested` while waiting for bots (`hooks/pr-loop-stop-hook.sh:946`) and monitor code that displays `cancel-state.md` as cancelled through `monitor_find_state_file` and PR-specific status mapping (`scripts/lib/monitor-common.sh:154`, `scripts/humanize.sh:1234`).
- edge_cases_or_failure_modes: The newest-directory selection is lexical timestamp sorting and ignores older active loops by design. If metadata fields are missing, the script intends to default to `?`, but the extraction pipelines use `grep` under `set -euo pipefail`; a missing field can terminate before defaults are applied (`scripts/cancel-pr-loop.sh:105`). Race conditions between state check and `mv` are fail-stop because `set -e` is active. `--force` may mislead operators because help says it exists but has no extra effect.
- validation_or_tests: `bash -n scripts/cancel-pr-loop.sh` passed. No direct behavioral cancellation test was found in the assigned tests; PR-loop robustness tests cover adjacent terminal-state path writability and active-loop discovery rather than this script’s cancel path.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-066 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for monitor cleanup and terminal restoration behavior, primarily validating the RLCR monitor portions of `scripts/humanize.sh`.
- algorithmic_behavior: Creates a temp test workspace under `/tmp/test-monitor-runtime-$$`, then runs staged tests that source or mimic monitor functions. It verifies `_graceful_stop` emits a user-facing deletion/cancel message and calls cleanup/terminal restore (`tests/test-monitor-runtime.sh:61`, `tests/test-monitor-runtime.sh:94`), cleanup idempotency via `cleanup_done` (`tests/test-monitor-runtime.sh:143`), loop directory deletion detection (`tests/test-monitor-runtime.sh:186`), source-level terminal reset/restore wiring (`tests/test-monitor-runtime.sh:263`), signal trap behavior in bash and zsh (`tests/test-monitor-runtime.sh:333`, `tests/test-monitor-runtime.sh:432`), and trap reset in `_cleanup` (`tests/test-monitor-runtime.sh:418`).
- inputs_outputs_state: Inputs are the repository root, `scripts/humanize.sh`, shell availability, optional `zsh`, and writable `/tmp`. Outputs are colored PASS/FAIL lines, a final summary, and exit `0` only if `TESTS_FAILED` is zero (`tests/test-monitor-runtime.sh:496`, `tests/test-monitor-runtime.sh:502`). Test state is held in `cleanup_done`, `monitor_running`, `restore_called`, `cleanup_triggered`, and generated helper scripts in the temp directory.
- gates_or_invariants: `_cleanup` must be idempotent, must reset traps, must restore terminal state, and must be called by `_graceful_stop` before printing the stop reason. The implementation under test matches this contract in `scripts/humanize.sh:675` through `scripts/humanize.sh:713`. Bash traps must include `trap '_cleanup' INT TERM`; zsh must define `TRAPINT()` and `TRAPTERM()` (`tests/test-monitor-runtime.sh:390`, `tests/test-monitor-runtime.sh:397`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` and the monitor terminal implementation; indirectly overlaps with `scripts/lib/monitor-common.sh` terminal primitives. It is a regression suite for user-facing monitor behavior when `.humanize/rlcr` disappears or the user sends `Ctrl+C`.
- edge_cases_or_failure_modes: Some checks are source-grep based because terminal scroll-region behavior is hard to exercise non-interactively (`tests/test-monitor-runtime.sh:270`). Zsh signal behavior is optional and skipped when `zsh` is unavailable (`tests/test-monitor-runtime.sh:432`). The test manually redefines simplified `_cleanup` and `_graceful_stop` in early cases, so it validates the behavioral contract as much as the exact sourced implementation.
- validation_or_tests: The file itself is the validation asset. `bash -n tests/test-monitor-runtime.sh` passed. I did not run the full test in this read-only export because it writes temp scripts and manipulates shell signals, but its intended assertions and implementation references were inspected directly.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-096 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Prompt block for the RLCR stop hook’s fail-closed git-status gate. It tells Claude/operator that repository state cannot be verified.
- algorithmic_behavior: Renders a short markdown reason headed “Git Status Failed,” interpolating `{{GIT_STATUS_EXIT}}`, listing likely causes, and instructing manual git-status inspection (`prompt-template/block/git-status-failed.md:1`). It is used when `git status --porcelain` fails or times out in `hooks/loop-codex-stop-hook.sh`.
- inputs_outputs_state: Input is the template variable `GIT_STATUS_EXIT`. Output is a markdown reason string embedded in hook JSON `{decision:"block", reason, systemMessage}` (`hooks/loop-codex-stop-hook.sh:431`, `hooks/loop-codex-stop-hook.sh:443`). It does not mutate repository state itself; it supports a blocked transition.
- gates_or_invariants: The invariant is fail-closed: if git status cannot be trusted, the loop must not proceed to completion or review decisions. The hook explicitly captures status exit code without fail-open fallback, attempts stale index lock cleanup, then blocks (`hooks/loop-codex-stop-hook.sh:430`, `hooks/loop-codex-stop-hook.sh:435`).
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` in `hooks/loop-codex-stop-hook.sh:443`. Depends on the shared template loader and the hook’s `run_with_timeout` wrapper.
- edge_cases_or_failure_modes: If `GIT_STATUS_EXIT` is not provided, the placeholder would remain unresolved unless the template loader handles it. The block is intentionally generic; it does not diagnose index locks beyond naming possible lock contention.
- validation_or_tests: Direct file inspection confirms the template is present and only 10 lines. Its call site in `loop-codex-stop-hook.sh` was inspected. No separate template rendering test was run.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-126 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: RLCR continuation prompt template used when Codex finds issues and the loop advances to the next Claude work round.
- algorithmic_behavior: Instructs Claude that work is unfinished, requires re-reading the original plan via `@{{PLAN_FILE}}`, requires Task system tracking for all discovered issues, embeds Codex review content between explicit markers, and provides a read-only Goal Tracker reference via `@{{GOAL_TRACKER_FILE}}` (`prompt-template/claude/next-round-prompt.md:1`, `prompt-template/claude/next-round-prompt.md:12`, `prompt-template/claude/next-round-prompt.md:16`, `prompt-template/claude/next-round-prompt.md:22`). The template is rendered after `current_round` is incremented and before footer instructions are appended (`hooks/loop-codex-stop-hook.sh:1541`, `hooks/loop-codex-stop-hook.sh:1559`).
- inputs_outputs_state: Inputs are `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`. Output is `round-N-prompt.md` in the active RLCR loop directory (`hooks/loop-codex-stop-hook.sh:1547`). State transition is “Codex found issues” to “next Claude round blocked prompt”; commit/summary obligations are appended later by `next-round-footer.md` using `NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:1608`).
- gates_or_invariants: Enforces full-scope remediation rather than only addressing top-priority issues (`prompt-template/claude/next-round-prompt.md:13`). Treats `goal-tracker.md` as read-only after round 0 and requires update requests in the summary instead of direct edits (`prompt-template/claude/next-round-prompt.md:30`). The stop hook may inject an Open Question notice between the Codex review marker and goal tracker reference when Codex raises questions (`hooks/loop-codex-stop-hook.sh:1564`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` with `load_and_render_safe`. It is paired with `prompt-template/claude/next-round-footer.md`, optional `open-question-notice.md`, optional `post-alignment-action-items.md`, and `goal-tracker-update-request.md`.
- edge_cases_or_failure_modes: Large or adversarial `REVIEW_CONTENT` is inserted directly into the prompt; boundaries are marker comments, not escaping. The template alone does not mention the next summary path, so correct behavior depends on the footer append step. If the footer render fails, the fallback still provides commit/summary instructions (`hooks/loop-codex-stop-hook.sh:1609`).
- validation_or_tests: Direct file inspection confirms placeholders and constraints. The exact hook render call and footer pairing were inspected. No standalone prompt render test was run.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-156 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for PR-loop API handling, mocked GitHub CLI behavior, state discovery, stop-hook resilience, and poll/fetch JSON contracts.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, sets up a temp test directory, and defines a configurable mock `gh` command supporting repository/PR lookup plus behavior-specific API responses (`tests/robustness/test-pr-loop-api-robustness.sh:18`, `tests/robustness/test-pr-loop-api-robustness.sh:32`). `run_fetch_tests` covers active PR-loop detection, YAML-list `active_bots`, missing `pr_number`, `fetch-pr-comments.sh` empty/rate-limit/network behavior, bot comment formatting for Claude/Codex/mixed responses, and JSON edge cases for Unicode and long comments (`tests/robustness/test-pr-loop-api-robustness.sh:282`, `tests/robustness/test-pr-loop-api-robustness.sh:362`, `tests/robustness/test-pr-loop-api-robustness.sh:442`, `tests/robustness/test-pr-loop-api-robustness.sh:518`). `run_poll_tests` covers stop-hook no-loop/corrupted-state behavior, approve-state path writability, `poll-pr-reviews.sh` help/argument validation, valid JSON output, slow API handling, and API failure semantics (`tests/robustness/test-pr-loop-api-robustness.sh:560`, `tests/robustness/test-pr-loop-api-robustness.sh:571`, `tests/robustness/test-pr-loop-api-robustness.sh:630`, `tests/robustness/test-pr-loop-api-robustness.sh:755`).
- inputs_outputs_state: Inputs are mocked `gh` behaviors, generated temp Git repos, synthetic `.humanize/pr-loop/<timestamp>/state.md`, bot names, PR number `123`, and the project’s actual scripts. Outputs are PASS/FAIL assertions and a helper summary. It creates and inspects generated `comments.md`, JSON poll output, state files, and hook exit codes.
- gates_or_invariants: API failures must not crash polling; `poll-pr-reviews.sh` must exit `0`, output parseable JSON, and set boolean `has_new_comments` exactly to false on API failure (`tests/robustness/test-pr-loop-api-robustness.sh:822`). Missing required poll args must be rejected (`tests/robustness/test-pr-loop-api-robustness.sh:643`). Stop hook must pass with no active PR loop and not crash with corrupted state (`tests/robustness/test-pr-loop-api-robustness.sh:571`, `tests/robustness/test-pr-loop-api-robustness.sh:587`). Fetch should create structured markdown for empty arrays and preserve bot/severity markers (`tests/robustness/test-pr-loop-api-robustness.sh:373`, `tests/robustness/test-pr-loop-api-robustness.sh:476`).
- dependencies_and_callers: Directly exercises `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `hooks/pr-loop-stop-hook.sh`, and `find_active_pr_loop` from `hooks/lib/loop-common.sh`. It also depends on `git`, `jq`, bash, and helper functions from `tests/test-helpers.sh`.
- edge_cases_or_failure_modes: Mock coverage includes rate limiting, network errors, bot approvals/issues, mixed bots, Unicode, long bodies, slow API, and API failure with warnings before JSON. The `auth_failure` mock behavior is defined but not used by the visible tests. Because it initializes temp Git repos and writes executable mock binaries, the full test is environment-sensitive despite being self-contained.
- validation_or_tests: `bash -n tests/robustness/test-pr-loop-api-robustness.sh` passed. Full behavioral execution was not run in this read-only branch export; the file’s executable spec and all major assertion blocks were inspected directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_CODEX_SKILL-HZ-006, ASK_CODEX_SKILL-HZ-036, ASK_CODEX_SKILL-HZ-066, ASK_CODEX_SKILL-HZ-096, ASK_CODEX_SKILL-HZ-126, ASK_CODEX_SKILL-HZ-156
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`