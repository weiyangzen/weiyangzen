# agent_12 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-012 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt templates for the RLCR state machine. They define implementation-round continuation, review-fix rounds, finalize rounds, goal-tracker update requests, agent-teams delegation rules, open-question blocking notices, and push/summary footer obligations.
- algorithmic_behavior: The directory is consumed by setup and stop hooks through the template loader. Initial agent-teams instructions are appended during setup when `AGENT_TEAMS=true` in `scripts/setup-rlcr-loop.sh:1002-1011`. Later rounds are built in `hooks/loop-codex-stop-hook.sh:1559-1628`, with optional open-question injection at `hooks/loop-codex-stop-hook.sh:1564-1597`, post-alignment action items at `hooks/loop-codex-stop-hook.sh:1600-1606`, push notes at `hooks/loop-codex-stop-hook.sh:1614-1621`, goal-tracker update instructions at `hooks/loop-codex-stop-hook.sh:1623-1628`, and agent-teams continuation at `hooks/loop-codex-stop-hook.sh:1630-1639`.
- inputs_outputs_state: Inputs are rendered variables such as `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, `{{GOAL_TRACKER_FILE}}`, `{{NEXT_SUMMARY_FILE}}`, `{{FINALIZE_SUMMARY_FILE}}`, `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, and `{{REVIEW_SKIP_REASON}}`. Outputs are round prompt files and block reasons returned to Claude, especially `round-N-prompt.md` and finalize/review prompts.
- gates_or_invariants: `review-phase-prompt.md:11-14` requires all severity-marked findings to be addressed, fixes only, a commit, and a summary. `finalize-phase-prompt.md:18-23` and `finalize-phase-skipped-prompt.md:28-33` forbid behavior changes, failing tests, and bug-introducing refactors. `next-round-footer.md:4-9` forbids false loop exits and state-file tampering. `open-question-notice.md:1` forces user clarification before other findings when the stop hook detects an open question.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, whose renderer is single-pass and leaves missing placeholders unchanged (`hooks/lib/template-loader.sh:50-131`). Direct callers include `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and tests in `tests/test-agent-teams.sh:298-354` plus continuation behavior tests around `tests/test-agent-teams.sh:548-620`.
- edge_cases_or_failure_modes: Missing templates fall back to embedded hook text via `load_and_render_safe` (`hooks/lib/template-loader.sh:170-203`). Agent-teams prompt injection has a fallback if either header/core template is missing (`scripts/setup-rlcr-loop.sh:1011-1021`, `hooks/loop-codex-stop-hook.sh:1640-1645`). Open-question insertion depends on a short line containing `Open Question`, so unusual review formatting may skip the notice (`hooks/loop-codex-stop-hook.sh:1564-1573`).
- validation_or_tests: `tests/test-agent-teams.sh:301-354` checks agent-teams templates exist, meet minimum size expectations, and contain required sections. Template reference and robustness tests exercise template loading generally. Directory content inspected recursively: `agent-teams-continue.md`, `agent-teams-core.md`, `agent-teams-instructions.md`, `finalize-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `goal-tracker-update-request.md`, `next-round-footer.md`, `next-round-prompt.md`, `open-question-notice.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, and `review-phase-prompt.md`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-042 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime polling adapter that converts GitHub issue comments, inline PR review comments, and PR reviews into a normalized JSON event stream for the PR loop.
- algorithmic_behavior: Parses `pr_number`, `--after`, and `--bots` (`scripts/poll-pr-reviews.sh:23-81`), validates required arguments and numeric PR numbers (`scripts/poll-pr-reviews.sh:83-102`), requires `gh` and `jq` (`scripts/poll-pr-reviews.sh:108-116`), resolves the PR base repository including fork-parent fallback (`scripts/poll-pr-reviews.sh:126-158`), maps logical bot names to GitHub authors with a special Codex mapping (`scripts/poll-pr-reviews.sh:164-187`), fetches three GitHub API endpoints with retries (`scripts/poll-pr-reviews.sh:200-235`), normalizes records (`scripts/poll-pr-reviews.sh:240-294`), filters by timestamp and bot author regex (`scripts/poll-pr-reviews.sh:299-305`), and emits final JSON (`scripts/poll-pr-reviews.sh:311-326`).
- inputs_outputs_state: Inputs are GitHub CLI auth/context, PR number, ISO timestamp, and comma-separated bot names. Output JSON contains `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:317-326`). State is temporary only: it creates a `mktemp` directory and cleans it with a trap (`scripts/poll-pr-reviews.sh:193-199`).
- gates_or_invariants: It must never silently run without all required arguments. API fetch failures are nonfatal after three attempts and return an empty array so the outer poll loop can continue (`scripts/poll-pr-reviews.sh:207-235`). Approval-only PR reviews are preserved by replacing empty/null review bodies with a state placeholder (`scripts/poll-pr-reviews.sh:276-289`). The filter requires `created_at >= --after` and matching bot author (`scripts/poll-pr-reviews.sh:299-305`).
- dependencies_and_callers: Depends on `gh`, `jq`, GitHub REST endpoints, and current or parent repo metadata. Called by `hooks/pr-loop-stop-hook.sh:812` inside the PR stop-hook polling loop; its `bots_responded` authors are mapped back to bot names at `hooks/pr-loop-stop-hook.sh:961-983`, and its `comments` are deduplicated and accumulated at `hooks/pr-loop-stop-hook.sh:1043-1059`.
- edge_cases_or_failure_modes: Missing `gh`/`jq`, invalid PR number, PR not found in current or parent repo, malformed GitHub payloads, comma-separated bot whitespace, API warnings before JSON, fork PRs, and approval reviews without bodies. The script uses lexicographic ISO timestamp comparison, so non-normalized timestamps could misfilter. Warnings may precede JSON during API failure, which tests handle by extracting from the first JSON line.
- validation_or_tests: Direct script robustness is covered by `tests/robustness/test-pr-loop-api-robustness.sh:630-855`: help, missing args, valid mocked JSON, slow API, and API failure with boolean false `has_new_comments`. Fixture-backed behavior is covered by `tests/test-pr-loop-hooks.sh:1322-1411`, using `tests/setup-fixture-mock-gh.sh:71-86` and fixtures that include issue comments, inline comments, approvals, and changes-requested reviews.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-072 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR loop validators, trigger detection, polling integration, bot-status gates, approval-only review handling, wrong-round protections, and monitor display.
- algorithmic_behavior: Defines `run_hook_tests()` (`tests/test-pr-loop-hooks.sh:15`) and executes grouped tests in order. It validates YAML state shape and write protections (`tests/test-pr-loop-hooks.sh:26-210`), sorting/deduplication and gate parsing (`tests/test-pr-loop-hooks.sh:222-466`), stop-hook trigger/timeouts/state behavior (`tests/test-pr-loop-hooks.sh:478-754`), end-to-end mocked stop-hook runs (`tests/test-pr-loop-hooks.sh:766-1148`), approval-only review normalization (`tests/test-pr-loop-hooks.sh:1160-1219`), fixture-backed fetch/poll behavior (`tests/test-pr-loop-hooks.sh:1231-1419`), wrong-round write/edit blocking (`tests/test-pr-loop-hooks.sh:1431-1535`), and monitor output (`tests/test-pr-loop-hooks.sh:1547-1621`).
- inputs_outputs_state: Inputs are temporary `.humanize/pr-loop/<timestamp>/state.md` files, generated hook JSON events, mock `gh` and `git` binaries, fixture JSON, and helper scripts. Outputs are pass/fail counters via `pass`/`fail`, hook decisions, generated prompt/comment files, and mutated temporary state files. It depends on `TEST_DIR`, `SCRIPT_DIR`, `PROJECT_ROOT`, and shared test helpers.
- gates_or_invariants: State files must store bots as YAML lists; `state.md` and `round-N-pr-comment.md` writes are blocked, while current-round `round-N-pr-resolve.md` writes are allowed (`tests/test-pr-loop-hooks.sh:26-166`). Human comments sort before bot comments and timestamps sort newest first (`tests/test-pr-loop-hooks.sh:222-255`). Previously approved bots must be re-added if new issues appear (`tests/test-pr-loop-hooks.sh:353-388`). Missing trigger comments block later rounds (`tests/test-pr-loop-hooks.sh:606-621`). Round 0 prefers `last_trigger_at` over `started_at` when available (`tests/test-pr-loop-hooks.sh:624-665`). Timeout math is anchored to the trigger timestamp (`tests/test-pr-loop-hooks.sh:667-696`).
- dependencies_and_callers: Sourced by `tests/test-pr-loop.sh:33` and `tests/test-pr-loop-2-hooks.sh:19`. It calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, `tests/setup-monitor-test-env.sh`, and fixture JSON under `tests/fixtures`.
- edge_cases_or_failure_modes: Covers user-filtered trigger detection, newer trigger refresh, paginated trigger discovery, missing resolve file, raw vs transformed GitHub API mock shapes, approval reviews with empty/null bodies, timestamp filters that exclude early reviews, wrong-round resolve file edits/writes, empty `active_bots`, and configured-vs-active bot display. Some tests simulate logic inline rather than invoking the production function, so they are contract checks as well as regression tests.
- validation_or_tests: This file is itself the validation suite. It is also indirectly included in broader PR loop test runners. Its fixture-backed polling tests assert valid JSON and approval placeholders at `tests/test-pr-loop-hooks.sh:1343-1367` and timestamp exclusion at `tests/test-pr-loop-hooks.sh:1392-1411`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-102 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Blocking message template for the PR loop when an explicit bot-triggering PR comment is required but absent.
- algorithmic_behavior: Renders a human-readable block reason with startup case, current round, required mention string, and an example trigger comment (`prompt-template/block/no-trigger-comment.md:1-17`). It is loaded in `hooks/pr-loop-stop-hook.sh:740-742` when `REQUIRE_TRIGGER=true` and `PR_LAST_TRIGGER_AT` is empty (`hooks/pr-loop-stop-hook.sh:728-747`).
- inputs_outputs_state: Inputs are `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING`. Output is the `reason` field in a JSON block decision with system message `PR Loop: Missing trigger comment - please @mention bots first` (`hooks/pr-loop-stop-hook.sh:744-746`). It does not mutate state.
- gates_or_invariants: Enforces the invariant that polling and Claude eyes verification do not proceed until a qualifying trigger exists. The caller validates the trigger before Claude eyes checks to avoid using stale trigger IDs (`hooks/pr-loop-stop-hook.sh:721-727`).
- dependencies_and_callers: Depends on `load_and_render_safe` and the template directory rooted at `prompt-template`. Caller fallback text exists at `hooks/pr-loop-stop-hook.sh:737-739` if the template is missing.
- edge_cases_or_failure_modes: If variables are missing, unresolved placeholders remain because rendering is single-pass and missing variables are preserved (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:119-122`). Startup cases outside 4/5 render as “Subsequent round requires trigger” by caller logic (`hooks/pr-loop-stop-hook.sh:729-735`).
- validation_or_tests: Missing-trigger behavior is covered by inline logic test `tests/test-pr-loop-hooks.sh:606-621` and stop-hook API robustness test paths in `tests/robustness/test-pr-loop-api-robustness.sh:577-595`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-132 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex review prompt template for scheduled full goal-alignment checkpoints in the RLCR loop.
- algorithmic_behavior: Forces Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), inspect Claude’s summary (`prompt-template/codex/full-alignment-review.md:13-16`), audit every acceptance criterion (`prompt-template/codex/full-alignment-review.md:19-29`), detect forgotten/deferred work (`prompt-template/codex/full-alignment-review.md:30-41`), perform an implementation review against docs (`prompt-template/codex/full-alignment-review.md:50-55`), check progress stagnation using historical round files (`prompt-template/codex/full-alignment-review.md:59-84`), and write strict terminal signals (`prompt-template/codex/full-alignment-review.md:86-93`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE`, all passed by `hooks/loop-codex-stop-hook.sh:842-855`. Output is `round-N-review-prompt.md`, later consumed by Codex to produce `round-N-review-result.md`.
- gates_or_invariants: The full-alignment checkpoint is selected when `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1`, after validating `FULL_REVIEW_ROUND` as an integer of at least 2 (`hooks/loop-codex-stop-hook.sh:799-809`). The template says deferred acceptance criteria are incomplete and only permits `COMPLETE` when all original plan tasks and ACs are met with no deferrals (`prompt-template/codex/full-alignment-review.md:88-93`). Stagnation requires `STOP` as the last line (`prompt-template/codex/full-alignment-review.md:84-90`).
- dependencies_and_callers: Uses the same template loader as other prompts. It coordinates with sibling Codex templates, especially `codex/goal-tracker-update-section.md` and `codex/regular-review.md`, and with Claude next-round templates because full-alignment findings can inject post-alignment action items into the next Claude prompt (`hooks/loop-codex-stop-hook.sh:1600-1606`).
- edge_cases_or_failure_modes: Invalid/corrupt `FULL_REVIEW_ROUND` silently defaults to 5 with a warning (`hooks/loop-codex-stop-hook.sh:801-805`). Previous-round indices are clamped to zero to avoid negative historical file references (`hooks/loop-codex-stop-hook.sh:814-817`). If the template is missing, the hook falls back to a much thinner alignment prompt (`hooks/loop-codex-stop-hook.sh:820-829`), reducing audit coverage.
- validation_or_tests: Template loading is covered through template reference tests and stop-hook prompt generation paths. The template’s behavioral contract is also exercised indirectly by RLCR stop-hook tests that validate review/finalize loop transitions.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-162 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for the template rendering subsystem under large content and escaping stress.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and `tests/test-helpers.sh` (`tests/robustness/test-template-stress-robustness.sh:14-17`), creates a temporary test directory (`tests/robustness/test-template-stress-robustness.sh:19`), then runs 18 tests covering normal substitution, fallback rendering, 1KB/100KB templates, 10KB/100KB variable values, 50 variables, regex metacharacters, ampersands, backslashes, newlines, empty values, placeholder injection prevention, dollar signs, repeated variables, boundary placeholders, and placeholder-only templates (`tests/robustness/test-template-stress-robustness.sh:33-283`).
- inputs_outputs_state: Inputs are in-memory template strings and temporary files under `TEST_DIR`. Outputs are `pass`/`fail` counters and a final summary (`tests/robustness/test-template-stress-robustness.sh:285-290`). Temporary files are cleaned by the helper trap (`tests/test-helpers.sh:84-89`).
- gates_or_invariants: Confirms renderer behavior promised in `hooks/lib/template-loader.sh`: single-pass substitution, missing template fallback, no placeholder injection, preservation of special characters, and tolerance for large values/templates. The injection test specifically requires `VAR_A=contains {{VAR_B}}` to remain literal while `VAR_B` in the original template is replaced (`tests/robustness/test-template-stress-robustness.sh:218-228`).
- dependencies_and_callers: Depends on Bash, `awk` through `render_template`, `grep`, `wc`, `date`, and test helpers. It is listed in the aggregate runner at `tests/run-all-tests.sh:63-70`.
- edge_cases_or_failure_modes: Large environment-variable substitution can stress shell/env size limits; large templates stress the `awk` renderer. Multiline values, backslashes, dollar signs, ampersands, and regex metacharacters guard against the historical kinds of replacement corruption common in sed-style renderers. The “Unicode” test currently uses basic ASCII text despite its label (`tests/robustness/test-template-stress-robustness.sh:230-239`), so true non-ASCII coverage is weak.
- validation_or_tests: The file is self-validating and exits with `print_test_summary` status (`tests/robustness/test-template-stress-robustness.sh:289-290`). It directly validates `render_template`, `load_and_render`, and `load_and_render_safe`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 item sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`