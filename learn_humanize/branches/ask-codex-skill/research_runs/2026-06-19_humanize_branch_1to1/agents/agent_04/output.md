# agent_04 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: The `hooks` directory is the Claude Code hook layer for two loop state machines: RLCR and PR loop. It registers prompt, pre-tool, post-tool, and stop hooks in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/hooks.json:1), then delegates common state parsing, template rendering, path guards, and stop-loop transitions to shell/Python helpers under `hooks/` and `hooks/lib/`.
- algorithmic_behavior: Recursive inventory found only `hooks/` and `hooks/lib/`, with 12 files total. `hooks.json` wires `UserPromptSubmit` to `loop-plan-file-validator.sh`, `PreToolUse` to read/write/edit/bash validators, `PostToolUse` to `loop-post-bash-hook.sh`, and `Stop` to both stop hooks with 7200s timeout at [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/hooks.json:10). `loop-codex-stop-hook.sh` drives RLCR review rounds; `pr-loop-stop-hook.sh` drives PR review polling and per-bot approval; validators prevent direct mutation of generated state/prompt files.
- inputs_outputs_state: Inputs are Claude hook JSON from stdin, `.humanize/rlcr/<timestamp>/state.md` or `.humanize/pr-loop/<timestamp>/state.md`, generated round files, git status, GitHub CLI results, Codex CLI results, task/todo state, and prompt templates. Outputs are either exit `0` allow/pass, exit `2` tool block for pre-tool validators, or JSON `{"decision":"block"}` for stop hooks; state outputs include renamed terminal state files such as `complete-state.md`, `approve-state.md`, `closed-state.md`, `maxiter-state.md`, plus generated review/prompt/feedback files.
- gates_or_invariants: Common guards reject invalid hook JSON, null bytes, invalid UTF-8, overly deep JSON, missing tool fields, malformed state frontmatter, wrong round files, direct `.humanize` git-adds, protected state/prompt writes, goal-tracker mutation after round 0, branch drift, plan-file drift, dirty git, unpushed PR commits, missing summaries, incomplete tasks, Codex failures, and missing bot trigger/response conditions. The shared strict parser requires frontmatter and core numeric fields at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/loop-common.sh:386).
- dependencies_and_callers: `hooks/lib/loop-common.sh` supplies state discovery/parsing, path classification, git-add detection, command mutation detection, loop termination, bot mapping, and PR goal-tracker updates; `hooks/lib/template-loader.sh` supplies single-pass `{{VAR}}` rendering at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/lib/template-loader.sh:58). Sibling dependencies include `scripts/portable-timeout.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `prompt-template/**`, and monitor/setup/cancel scripts.
- edge_cases_or_failure_modes: The directory explicitly handles multi-session filtering, stale terminal state files, finalize phase, macOS Bash 3.2 compatibility, zsh/bash differences, fork PR repository lookup, missing templates via fallbacks, malformed hook input, DoS-like nested JSON, shell wrapper bypasses, chained bash redirections, stale `.git/index.lock`, and no active loop as an allow/exit path.
- validation_or_tests: Related executable specs include hook robustness/input tests, PR hook tests, stop-hook tests, template reference tests, todo checker tests, monitor E2E tests, and PR API robustness tests. The assigned monitor and API wrapper tests are direct coverage for monitor and PR API paths.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-034 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: PR-loop stop hook. It intercepts Claude exit attempts while `.humanize/pr-loop/<timestamp>/state.md` is active, checks whether PR review obligations are complete, polls configured bots, asks local Codex to classify bot feedback, and either terminates the loop or blocks exit with next-round instructions.
- algorithmic_behavior: The hook reads stdin but primarily uses `CLAUDE_PROJECT_DIR`, discovers the active PR loop at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:49), parses state YAML fields and bot lists at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:88), resolves the base repo for fork PRs at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:197), checks PR state, requires a round resolution file, enforces clean and pushed git state, detects force-push/new-commit trigger invalidation, polls bots, generates comment/check/feedback files, runs `codex exec`, parses final markers, and updates active bot state.
- inputs_outputs_state: Inputs include `state.md` fields such as `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id`; the current git branch/head/status; GitHub PR/comments/reactions via `gh`; bot polling JSON; and Codex output. Outputs include block JSON, generated `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, mutations to `state.md`, goal-tracker updates, and terminal renames to `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, or `usage-limit-state.md`.
- gates_or_invariants: It blocks if the resolution summary is missing at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:258), git status fails or non-`.humanize` files are dirty at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:280), local commits are unpushed at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:313), a new force-push/new-commit needs a fresh bot trigger at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:364), required trigger comments are absent at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:721), Claude lacks an eyes reaction when required, Codex is absent/fails/empty, or Codex returns `WAITING_FOR_BOTS`.
- dependencies_and_callers: Registered as a Stop hook in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/hooks.json:68). It sources `hooks/lib/loop-common.sh` and `scripts/portable-timeout.sh`, uses `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, `prompt-template/pr-loop/codex-goal-tracker-update.md`, block templates, `git`, `gh`, `jq`, `codex`, `date`, `awk`, and `sed`.
- edge_cases_or_failure_modes: Handles absent loop/state by allowing exit, missing `pr_number` or invalid `current_round` by graceful exit, merged/closed PRs as terminal state, fork PR comments on base repo, startup cases 1-5, old trigger reuse after force-push/new commits, Codex +1 approval reactions before/during polling, per-bot timeouts, bot re-addition after previously approved bots report new issues, comment deduplication by id, API/poll transient failure retry, and macOS Bash without associative arrays via dynamic `_map_*` variables at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/pr-loop-stop-hook.sh:887).
- validation_or_tests: Covered by PR stop-hook tests and robustness suites. The assigned fetch wrapper covers shared API behavior; `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/robustness/test-pr-loop-api-robustness.sh` invoke or model this hook’s state/API paths. Related monitor E2E also creates PR loop state and exercises PR monitor state display.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-064 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for real monitor behavior. It sources `scripts/humanize.sh` and exercises `_humanize_monitor_codex` and `_humanize_monitor_pr` under actual bash/zsh shell flows rather than stubs.
- algorithmic_behavior: The test creates temporary `.humanize/rlcr` and `.humanize/pr-loop` sessions, valid state files, goal trackers, and fake cache log directories, then starts real monitor functions in background or one-shot mode. It deletes loop directories or sends SIGINT/SIGTERM, then asserts graceful termination, clean messages, no shell glob errors, restored terminal behavior, and expected exit codes. The direct RLCR deletion test starts at [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-monitor-e2e-real.sh:56); PR deletion starts at [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-monitor-e2e-real.sh:691).
- inputs_outputs_state: Inputs are generated temp project trees under `/tmp/test-monitor-e2e-real-$$`, fake `HOME`/`XDG_CACHE_HOME`, synthetic `state.md`, synthetic `goal-tracker.md`, fake log files, shell availability (`bash`, optional `zsh`), and terminal command shims. Outputs are PASS/FAIL counters, captured monitor output files, process lifecycle assertions, and final exit `0` only if `TESTS_FAILED=0`.
- gates_or_invariants: Monitors must exit after deletion within bounded waits, print `Monitoring stopped:` / stop messages, include deletion reason for RLCR, avoid `no matches found` and `bad pattern`, restore scroll region/source cleanup, and return `EXIT_CODE:0` for graceful deletion paths. SIGINT tests require cleanup indication or clean exit; bash SIGINT has a tolerant fallback because signal delivery can be complex.
- dependencies_and_callers: Depends on `scripts/humanize.sh` monitor functions. `_humanize_monitor_codex` checks `.humanize/rlcr`, locates cache logs, traps INT/TERM/WINCH, and calls `_graceful_stop` when the directory disappears at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/humanize.sh:775). `_humanize_monitor_pr` supports `--once`, status display, latest PR round file tailing, and trap cleanup at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/scripts/humanize.sh:1151).
- edge_cases_or_failure_modes: Covers bash and zsh monitor compatibility, missing zsh as skip not failure, directory deletion during no-log and active-log states, background monitor that refuses to exit, terminal escape handling in non-interactive mode, PR monitor with and without `--once`, and signal cleanup paths that may require TERM/KILL fallback in tests.
- validation_or_tests: The file is itself a validation entry point and prints a summary at [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/test-monitor-e2e-real.sh:999). It is also sourced by `tests/test-monitor-e2e-sigint.sh` and `tests/test-monitor-e2e-deletion.sh`, making its functions reusable specs.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-094 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop hook’s dirty git gate. It converts a detected unclean working tree into operator-facing required actions before exit/review can proceed.
- algorithmic_behavior: Template variables `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` are filled by the single-pass template loader, then emitted as the stop-hook block reason. It instructs the user to review untracked files, ignore build artifacts, stage real changes, and commit before attempting exit again at [prompt-template/block/git-not-clean.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/block/git-not-clean.md:1).
- inputs_outputs_state: Inputs are the concrete git issue summary and optional special notes such as `.humanize` local-state or untracked-file warnings. Output is Markdown text embedded in `{"decision":"block"}` by `loop-codex-stop-hook.sh`; it does not mutate state itself.
- gates_or_invariants: Encodes the invariant that Claude cannot stop while real project changes are uncommitted. It also forbids AI authorship attribution in commits and says no `Co-Authored-By: Claude` or similar at [prompt-template/block/git-not-clean.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/block/git-not-clean.md:11).
- dependencies_and_callers: Called by `loop-codex-stop-hook.sh` through `load_and_render_safe` when `git status --porcelain` finds relevant changes; the caller constructs notes from adjacent templates and supplies `GIT_ISSUES`/`SPECIAL_NOTES` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:535).
- edge_cases_or_failure_modes: The template assumes the caller has already filtered local `.humanize` artifacts and distinguished untracked notes. If the template is missing, the hook has an inline fallback; if variables are missing, `render_template` leaves placeholders unchanged by design.
- validation_or_tests: Template coverage is provided by template reference/comprehensive tests that scan real hook references. The functional gate is indirectly tested through stop-hook tests that exercise dirty git blocking.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-124 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt template that routes post-round goal-tracker changes through Claude’s summary instead of direct file edits. It is part of the RLCR goal-tracker ownership algorithm: Claude proposes changes; Codex reviews and applies justified updates.
- algorithmic_behavior: The template tells Claude to include a `## Goal Tracker Update Request` section with requested changes and justification if tracker changes are needed at [prompt-template/claude/goal-tracker-update-request.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/prompt-template/claude/goal-tracker-update-request.md:2). `loop-codex-stop-hook.sh` appends it to next-round instructions after Codex finds issues, using a fallback if absent at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-codex-stop-hook.sh:1624).
- inputs_outputs_state: It has no dynamic placeholders. Input is the surrounding next-round prompt context; output is a Markdown contract embedded into Claude instructions. Downstream state effect is indirect: Claude includes the request in `round-N-summary.md`, then Codex may update `goal-tracker.md`.
- gates_or_invariants: Supports the invariant enforced by validators: direct `goal-tracker.md` Write/Edit/Bash changes are blocked after round 0, while summary-mediated requests remain allowed. The write/edit/bash gates are implemented in [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-write-validator.sh:190), [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-edit-validator.sh:141), and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/hooks/loop-bash-validator.sh:343).
- dependencies_and_callers: Used by RLCR stop-hook prompt assembly, alongside next-round prompt/footer templates and optional agent-team instructions. It complements `prompt-template/codex/goal-tracker-update-section.md`, which tells Codex how to process these requests.
- edge_cases_or_failure_modes: If Claude omits the section, no tracker update request is carried forward. If the template file is missing, the hook injects a simpler fallback. If Claude tries to bypass this contract with direct file writes, pre-tool validators block.
- validation_or_tests: Covered by template reference and comprehensive template tests, plus finalize/plan-file hook tests that exercise goal-tracker mutation blocking and next-round prompt generation.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-154 `file` `tests/robustness/test-pr-loop-api-fetch.sh`
- cursor: `[_]`
- core_role: Thin executable wrapper for the PR loop API fetch robustness subset. It isolates Tests 1-11 from the larger PR API robustness suite for parallel or targeted execution.
- algorithmic_behavior: The wrapper resolves `SCRIPT_DIR`, sources `test-pr-loop-api-robustness.sh`, calls `run_fetch_tests`, prints a named summary, and exits with the summary status at [tests/robustness/test-pr-loop-api-fetch.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-pr-loop-api-fetch.sh:14).
- inputs_outputs_state: Inputs are the shared robustness library, temp test directory helpers, mocked `gh` behaviors, git temp repos, `scripts/fetch-pr-comments.sh`, and shared loop-common functions. Outputs are PASS/FAIL lines, a `PR Loop API Fetch Tests` summary, and process exit status reflecting failures.
- gates_or_invariants: Inherited tests verify active PR loop discovery, YAML bot-list state handling, missing `pr_number` tolerance, empty PR comment output structure, API rate-limit/network behavior, Claude/Codex/multiple-bot parsing, Unicode comment handling, and long comment bodies. The wrapped function starts at [tests/robustness/test-pr-loop-api-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/ask-codex-skill/tests/robustness/test-pr-loop-api-robustness.sh:282).
- dependencies_and_callers: The wrapper depends on `tests/robustness/test-pr-loop-api-robustness.sh`, which builds a mock `gh`, initializes git repos, calls `scripts/fetch-pr-comments.sh`, and uses `find_active_pr_loop` from `hooks/lib/loop-common.sh`. `tests/run-all-tests.sh` includes this split API fetch test.
- edge_cases_or_failure_modes: The wrapper itself can fail if the shared suite cannot be sourced. The covered edge cases include empty arrays, rate limit stderr/nonzero API calls, network errors, bot severity markers, multiple bots, Unicode, and 10KB comment bodies. Since it only runs fetch tests, poll/stop-hook Tests 12-19 live in the sibling wrapper/suite path rather than this file.
- validation_or_tests: This file is the validation entry point for the fetch half of PR API robustness. It is not a skip despite being small, because it defines the executable split boundary used by the test runner.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6 unique Item Evidence headings present above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`