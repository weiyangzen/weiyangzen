# agent_24 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-024 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is a thin command wrapper around `scripts/setup-rlcr-loop.sh`, with allowed tool scope restricted to that script only at `commands/start-rlcr-loop.md:3-14`.
- algorithmic_behavior: Defines the high-level RLCR state machine: implementation rounds, summary writing, Codex summary review, transition to review phase on Codex `COMPLETE`, `codex review --base <branch>`, issue repair for `[P0-9]` findings, then finalize phase when review has no issues at `commands/start-rlcr-loop.md:16-25` and `commands/start-rlcr-loop.md:60-67`.
- inputs_outputs_state: Inputs are a plan path or `--plan-file`, iteration/model/timeout/tracking/push/base/review interval flags, `--skip-impl`, `--claude-answer-codex`, and `--agent-teams` at `commands/start-rlcr-loop.md:3`. Output is activation of a loop session generated by the setup script. Actual state materialized by the dependency script includes `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-0-prompt.md`, `round-0-summary.md`, `.pending-session-id`, and optional `.review-phase-started` for skip-impl mode in `scripts/setup-rlcr-loop.sh:725-803`, `scripts/setup-rlcr-loop.sh:809-928`, and `scripts/setup-rlcr-loop.sh:934-1055`.
- gates_or_invariants: The command text requires summaries before exit, current `goal-tracker.md`, detailed evidence, and forbids state-file/cancel-command cheating at `commands/start-rlcr-loop.md:46-52`. Goal tracking has an immutable section for ultimate goal and acceptance criteria plus mutable active/completed/deferred/evolution sections at `commands/start-rlcr-loop.md:27-45`. Setup enforces one active loop, clean git tree, valid model/effort, valid base branch, and plan-path/content gates in `scripts/setup-rlcr-loop.sh:260-280`, `scripts/setup-rlcr-loop.sh:330-585`, and `scripts/setup-rlcr-loop.sh:626-720`.
- dependencies_and_callers: Calls `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` directly at `commands/start-rlcr-loop.md:12-14`. That script depends on `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` at `scripts/setup-rlcr-loop.sh:25-31`. README exposes the same user-facing command contract at `README.md:143-164`.
- edge_cases_or_failure_modes: Stop conditions are max iteration count, successful completion plus clean code review, or explicit cancel at `commands/start-rlcr-loop.md:54-58`. Skip-implementation mode allows no plan and jumps directly to code review at `commands/start-rlcr-loop.md:69-86`; setup creates a placeholder plan and sets initial `review_started` from the skip flag at `scripts/setup-rlcr-loop.sh:309-327`, `scripts/setup-rlcr-loop.sh:733-755`, and `scripts/setup-rlcr-loop.sh:764-787`. Other failures include dirty working tree, invalid/untracked/tracked plan mismatch, bad base branch, missing Codex, branch/model YAML-unsafe characters, and insufficient plan content.
- validation_or_tests: This command is indirectly specified by setup tests referenced in the repo, especially `tests/test-plan-file-validation.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, `tests/robustness/test-base-branch-detection.sh`, `tests/test-session-id.sh`, and `tests/test-agent-teams.sh` from the search evidence. This assigned file itself is prose plus invocation metadata, so its executable behavior is in `scripts/setup-rlcr-loop.sh`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-054 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable specification for the one-shot Ask Codex algorithm. It mocks the `codex` binary and validates argument parsing, output capture, metadata persistence, error handling, uniqueness, and cache behavior for `scripts/ask-codex.sh`.
- algorithmic_behavior: Test setup builds an isolated mock git repo and a mock `codex` executable controlled by `MOCK_CODEX_EXIT_CODE`, `MOCK_CODEX_STDOUT`, and `MOCK_CODEX_STDERR` at `tests/test-ask-codex.sh:24-74`. The test invokes the production script through `run_ask_codex`, setting `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and `PATH` to control project root, writable cache, and Codex behavior.
- inputs_outputs_state: Inputs covered include empty question, `--help`, unknown option, `--codex-model`, `--codex-timeout`, `--` separator, and arbitrary question text at `tests/test-ask-codex.sh:83-153` and `tests/test-ask-codex.sh:329-371`. Expected outputs include stdout containing Codex response, stderr with status/cache path, `.humanize/skill/<unique>/input.md`, `output.md`, `metadata.md`, and cache files `codex-run.cmd` and `codex-run.out` at `tests/test-ask-codex.sh:163-204` and `tests/test-ask-codex.sh:381-404`.
- gates_or_invariants: Validation gates require non-empty question, installed `codex`, known options only, numeric timeout, and model/effort character safety at `tests/test-ask-codex.sh:83-153`; implementation enforces these in `scripts/ask-codex.sh:89-186`. The script must preserve clean stdout for the response while status/debug goes to stderr, matching the skill contract in `skills/ask-codex/SKILL.md:21-25`.
- dependencies_and_callers: Direct dependency under test is `scripts/ask-codex.sh` at `tests/test-ask-codex.sh:17`. The test uses `tests/test-helpers.sh` for `setup_test_dir`, `init_test_git_repo`, `pass`, `fail`, and summary reporting at `tests/test-helpers.sh:30-104`. Production dependencies include `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` at `scripts/ask-codex.sh:26-34`.
- edge_cases_or_failure_modes: Covers Codex non-zero exit propagation and error metadata at `tests/test-ask-codex.sh:219-237`, empty Codex response as script exit 1 plus `empty_response` metadata at `tests/test-ask-codex.sh:239-256`, timeout exit 124 plus user guidance and `timeout` metadata at `tests/test-ask-codex.sh:258-275`, and concurrent invocations creating distinct project and cache directories at `tests/test-ask-codex.sh:277-319`. It also verifies no-effort model uses default `xhigh` and `--` treats later dash-prefixed tokens as question text.
- validation_or_tests: This file is itself the validation suite. It maps to implementation lines that create unique IDs and storage in `scripts/ask-codex.sh:202-218`, save input in `scripts/ask-codex.sh:224-237`, build/run Codex in `scripts/ask-codex.sh:243-297`, classify failures in `scripts/ask-codex.sh:308-380`, and copy successful output plus metadata in `scripts/ask-codex.sh:387-410`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-084 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable runtime-safety specification for monitor logic under zsh. It protects the RLCR monitor from zsh `nomatch` glob failures when session, state, or cache directories are empty or contain only non-matching entries.
- algorithmic_behavior: Runs under zsh (`#!/usr/bin/env zsh`) and asserts the environment is actually zsh at `tests/test-zsh-monitor-safety.sh:1-13` and `tests/test-zsh-monitor-safety.sh:276-286`. It creates isolated `.humanize/rlcr` and cache trees under `/tmp`, sources `scripts/humanize.sh`, and exercises find-based directory/file iteration patterns instead of brittle globs at `tests/test-zsh-monitor-safety.sh:48-60`, `tests/test-zsh-monitor-safety.sh:68-77`, and `tests/test-zsh-monitor-safety.sh:90-124`.
- inputs_outputs_state: Inputs are synthetic RLCR session directories, dotfiles, `state.md`/`*-state.md` absence or presence, cache directories, and valid timestamp-named sessions. Outputs are pass/fail counters and process exit 0 only when all cases avoid glob errors and produce expected session/log/state detection results at `tests/test-zsh-monitor-safety.sh:323-341`.
- gates_or_invariants: Key invariant is that monitor scanning must use `find ... 2>/dev/null` loops and handle empty reads, non-directories, and absent files without zsh `no matches found`. This mirrors shared monitor helpers: `monitor_find_latest_session` uses `find` and timestamp regex at `scripts/lib/monitor-common.sh:37-63`; `monitor_find_state_file` returns `state.md|active`, `<reason>-state.md|reason`, or `|unknown` at `scripts/lib/monitor-common.sh:147-188`.
- dependencies_and_callers: Sources `scripts/humanize.sh` at `tests/test-zsh-monitor-safety.sh:72` and models internals used by `_humanize_monitor_codex`, whose wrapper enables zsh-compatible array behavior and delegates latest session/state lookup to shared helpers at `scripts/humanize.sh:225-246` and `scripts/humanize.sh:341-344`. The Codex log scanner in `scripts/humanize.sh:255-339` is the related production behavior for cache log discovery.
- edge_cases_or_failure_modes: Explicit cases: empty loop directory, dotfiles only, no `*-state.md`, empty cache directory, multiple timestamp sessions selecting latest lexicographically, and demonstration that the old `for x in .humanize/rlcr/*` pattern would be unsafe under strict zsh while the find pattern is safe at `tests/test-zsh-monitor-safety.sh:80-157`, `tests/test-zsh-monitor-safety.sh:160-235`, `tests/test-zsh-monitor-safety.sh:240-273`, and `tests/test-zsh-monitor-safety.sh:288-321`. Production log scanner also detects inconsistent implementation/review log ordering and returns an error if run-round is not before review-round at `scripts/humanize.sh:328-335`.
- validation_or_tests: This file is the validation asset. It does not assert visual monitor rendering; it specifically validates shell-safety and discovery primitives that the monitor depends on.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-114 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Prompt/block template for path-correction failures in RLCR read/write validators. It communicates that an attempted read or write targeted the wrong directory and supplies the canonical path.
- algorithmic_behavior: Template accepts placeholders `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`, producing a short blocking message with attempted action, supplied path, correct path, and a command hint at `prompt-template/block/wrong-directory-path.md:1-6`.
- inputs_outputs_state: Inputs are rendered by hook validators: read validator passes `ACTION=read`, attempted `FILE_PATH`, and computed `CORRECT_PATH` at `hooks/loop-read-validator.sh:157-166`; write validator passes `ACTION=write to` and the same path pair at `hooks/loop-write-validator.sh:254-263`. Output is stderr guidance followed by validator exit code 2 in both callers.
- gates_or_invariants: Invariant is that RLCR files must be accessed through the active loop directory, not a stale or sibling path. Read validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and blocks when attempted path differs at `hooks/loop-read-validator.sh:153-168`; write validator applies the same rule at `hooks/loop-write-validator.sh:250-265`.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh`, which does single-pass `{{VAR}}` replacement to avoid placeholder injection and uses caller-provided fallback text if the template is missing or renders empty at `hooks/lib/template-loader.sh:50-132` and `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If this template is missing, callers still block with inline fallback text at `hooks/loop-read-validator.sh:160-166` and `hooks/loop-write-validator.sh:257-263`. If variables contain template-like braces, the template renderer intentionally does not rescan substituted values, avoiding secondary substitution corruption at `hooks/lib/template-loader.sh:54-57` and `hooks/lib/template-loader.sh:71-129`.
- validation_or_tests: Related behavior is covered by hook/path validation suites surfaced in repository search, especially tests around plan/path validation and session behavior; no test file assigned here directly targets this exact template content. The template is algorithmically small but core to validator UX and transition blocking.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-144 `file` `skills/ask-codex/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction for one-shot Codex consultation. It defines the user-facing Ask Codex workflow and delegates all execution to `scripts/ask-codex.sh`.
- algorithmic_behavior: The skill disables model invocation for the skill wrapper, restricts allowed tools to the ask-codex script, and instructs the agent to run `"${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh" $ARGUMENTS` at `skills/ask-codex/SKILL.md:1-19`. It is explicitly non-iterative, unlike the RLCR loop, at `skills/ask-codex/SKILL.md:36-40`.
- inputs_outputs_state: Inputs are optional `--codex-model MODEL:EFFORT`, optional `--codex-timeout SECONDS`, and question/task text at `skills/ask-codex/SKILL.md:4`. Successful output is Codex stdout, while status goes to stderr and response is saved to `.humanize/skill/<timestamp>/output.md` at `skills/ask-codex/SKILL.md:21-25` and `skills/ask-codex/SKILL.md:36-40`. Production script persists `input.md`, `output.md`, `metadata.md`, and cache logs as shown in `scripts/ask-codex.sh:202-218`, `scripts/ask-codex.sh:224-237`, and `scripts/ask-codex.sh:387-410`.
- gates_or_invariants: Exit-code contract is part of the skill: 0 means response in stdout, 1 means validation error, 124 means timeout, and other codes are Codex process errors at `skills/ask-codex/SKILL.md:27-34`. Implementation gates include installed `codex`, non-empty question, safe model characters, safe effort characters, numeric timeout, and unknown-option rejection in `scripts/ask-codex.sh:89-186`.
- dependencies_and_callers: Direct dependency is `scripts/ask-codex.sh`; README documents the same command and use case at `README.md:203-220`. The script shares defaults with `hooks/lib/loop-common.sh`, where default model and effort are `gpt-5.3-codex` and `xhigh` at `hooks/lib/loop-common.sh:42-45`.
- edge_cases_or_failure_modes: Timeout must be reported with guidance to increase timeout, and the script exits 124 at `scripts/ask-codex.sh:308-330`. Non-zero Codex exits propagate with stderr tail and `status: error` metadata at `scripts/ask-codex.sh:332-356`. Empty successful stdout is considered failure with `status: empty_response` at `scripts/ask-codex.sh:358-380`. Cache directory creation falls back to project-local cache if the home cache is unwritable at `scripts/ask-codex.sh:209-218`.
- validation_or_tests: `tests/test-ask-codex.sh` is the assigned executable specification for this skill. It validates the skill contract end to end with a mocked Codex binary, including stdout/stderr separation, metadata, timeout, errors, argument parsing, uniqueness, and cache artifacts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ASK_CODEX_SKILL-HZ-024, ASK_CODEX_SKILL-HZ-054, ASK_CODEX_SKILL-HZ-084, ASK_CODEX_SKILL-HZ-114, ASK_CODEX_SKILL-HZ-144
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`