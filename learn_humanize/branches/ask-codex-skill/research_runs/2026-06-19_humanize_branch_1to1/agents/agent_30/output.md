# agent_30 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-030 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit guard for an active RLCR loop. It enforces loop-state schema validity, branch immutability, and plan-file tracking invariants before user prompt handling continues. It is registered as the UserPromptSubmit hook in `hooks/hooks.json:4-10`.
- algorithmic_behavior: Reads hook stdin into `INPUT`, extracts `session_id`, locates the newest active `.humanize/rlcr` loop for that session, and exits allow/no-op if no active loop exists (`hooks/loop-plan-file-validator.sh:26-39`). It resolves `finalize-state.md` before `state.md`, strict-parses state, then maps parsed fields into local `PLAN_TRACKED`, `PLAN_FILE`, and `START_BRANCH` (`hooks/loop-plan-file-validator.sh:41-53`). It validates required schema fields, verifies the current git branch equals the loop start branch, and then checks whether the plan file remains in the expected tracked or untracked state (`hooks/loop-plan-file-validator.sh:79-229`).
- inputs_outputs_state: Input is Claude hook JSON on stdin, only used for `session_id` filtering via shared `extract_session_id`. Runtime state comes from `$CLAUDE_PROJECT_DIR` or cwd, `.humanize/rlcr/<loop>/state.md` or `finalize-state.md`, and git state in `PROJECT_ROOT`. Output is either silent exit 0 for allow/no-op, or JSON with `"decision": "block"` and a reason for schema, branch, git, or plan-tracking violations. The script does not mutate loop state.
- gates_or_invariants: Fails closed on malformed state (`parse_state_file_strict`) with stderr error and exit 1 (`hooks/loop-plan-file-validator.sh:44-48`). Requires nonempty `plan_tracked` and `start_branch` after strict parsing (`hooks/loop-plan-file-validator.sh:79-89`). Blocks branch switching during an active loop (`hooks/loop-plan-file-validator.sh:95-115`). If `plan_tracked=true`, the plan file must be tracked and clean (`hooks/loop-plan-file-validator.sh:123-192`). Otherwise the plan file must remain untracked/gitignored from the loop’s perspective (`hooks/loop-plan-file-validator.sh:193-228`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for constants, session filtering, active-loop resolution, strict state parsing, and template loading (`hooks/loop-plan-file-validator.sh:16-18`). Depends on `scripts/portable-timeout.sh` for `run_with_timeout` around git commands (`hooks/loop-plan-file-validator.sh:19-24`). `parse_state_file_strict` requires frontmatter separators, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric round/iteration values, and boolean `review_started` (`hooks/lib/loop-common.sh:386-448`). The timeout wrapper returns GNU-style 124 on timeout when possible (`scripts/portable-timeout.sh:31-71`).
- edge_cases_or_failure_modes: No active loop exits 0 without output. Malformed or older state blocks for safety. Git branch lookup failure or timeout blocks because branch consistency cannot be proven (`hooks/loop-plan-file-validator.sh:95-106`). Git `ls-files` timeout or unexpected error blocks; normal exit 1 means untracked and is handled explicitly (`hooks/loop-plan-file-validator.sh:125-148`, `196-218`). Tracked-plan mode blocks if plan becomes untracked or dirty. Untracked-plan mode blocks if the plan becomes tracked. Template failure for schema messages falls back through `load_and_render_safe`, so missing templates do not disable blocking (`hooks/loop-plan-file-validator.sh:59-77`; `hooks/lib/template-loader.sh:170-203`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers valid state pass, quoted `plan_file`, malformed/missing schema fields, and branch changes (`tests/test-plan-file-hooks.sh:119-217`). Later tests cover quoted `start_branch`, quoted branch mismatch, and hyphenated `plan_file` paths (`tests/test-plan-file-hooks.sh:380-500`). `tests/robustness/test-hook-system-robustness.sh` verifies plan-file edits and non-plan edits do not produce block decisions from this UserPromptSubmit validator, plus empty JSON robustness (`tests/robustness/test-hook-system-robustness.sh:199-240`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-060 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable contract for the `/gen-plan` command surface and its IO validator. It is not the RLCR state machine itself, but it defines deterministic behavior for turning a draft into a plan entrypoint, including command metadata, agent metadata, naming, model acceptance, content constraints, and path validation.
- algorithmic_behavior: Initializes pass/fail counters and checks repository files under `commands/` and `agents/` (`tests/test-gen-plan.sh:11-40`). Positive tests require `commands/gen-plan.md`, nonempty description, `allowed-tools`, `argument-hint`, a `draft-relevance-checker` agent, correct agent name, `haiku` model, tools field, and version consistency across plugin metadata and README (`tests/test-gen-plan.sh:54-180`). Negative tests build temporary invalid fixtures for name, frontmatter, malformed YAML, and model validation (`tests/test-gen-plan.sh:191-401`). It then runs `scripts/validate-gen-plan-io.sh` against argument and path scenarios, asserting specific exit codes (`tests/test-gen-plan.sh:425-541`).
- inputs_outputs_state: Inputs are repository files plus temporary fixtures created under `mktemp -d`. Outputs are colored PASS/FAIL lines, counters, and final process exit 0 when all tests pass or 1 when any fail (`tests/test-gen-plan.sh:544-562`). Temporary fixture cleanup is via `trap`, though the script assigns multiple traps, so later trap definitions supersede earlier ones in bash.
- gates_or_invariants: Command names must match `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:195-199`). Command frontmatter must start with `---` and contain `description`; agent frontmatter must additionally contain `name` (`tests/test-gen-plan.sh:201-214`). Model names must be exact `opus|sonnet|haiku` aliases or full IDs prefixed by `claude-`, `gpt-`, `o[0-9]`, or `gemini-`; partial aliases like `opus-v2` are rejected (`tests/test-gen-plan.sh:350-389`). `gen-plan.md` and the relevance agent must not contain CJK or common emoji ranges (`tests/test-gen-plan.sh:403-423`).
- dependencies_and_callers: Tests `commands/gen-plan.md`, whose workflow validates IO, performs a repository relevance check through `humanize:draft-relevance-checker`, analyzes draft clarity/consistency/completeness/functionality, resolves issues through `AskUserQuestion`, generates AC-style plan content, then updates the preseeded output with Edit (`commands/gen-plan.md:19-47`, `50-72`, `76-139`, `142-304`). Tests `scripts/validate-gen-plan-io.sh`, whose exit-code contract is documented in lines 4-12 and implemented through argument, input, output, permission, template, copy, and append checks (`scripts/validate-gen-plan-io.sh:4-12`, `29-160`).
- edge_cases_or_failure_modes: The YAML syntax checker is deliberately lightweight and regex-based, so it validates local expected shapes rather than full YAML semantics (`tests/test-gen-plan.sh:304-319`). `grep -P` is used for emoji/CJK checks and is guarded with `2>/dev/null`; environments without PCRE may silently treat the content as acceptable. The version check requires all three metadata files to exist and have the same version string (`tests/test-gen-plan.sh:160-180`). IO validator cases assert: missing option value, flag-as-value, unknown option, missing input, empty input, missing output directory, existing output, output path as directory, valid paths, and help (`tests/test-gen-plan.sh:438-539`).
- validation_or_tests: This file is itself the validation artifact. It is included in `tests/run-all-tests.sh` as `test-gen-plan.sh` (`tests/run-all-tests.sh:52`). The command under test allows only specific tools including the validator script, Read, Glob, Grep, Task, Write, and AskUserQuestion (`commands/gen-plan.md:1-12`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-090 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: PR-loop block template for a non-fast-forward commit transition. It defines the review-state reset contract when force push is detected.
- algorithmic_behavior: Presents a block reason headed “Force Push Detected,” interpolates old and new commit SHAs, states that force pushes reset review state, and gives the required human action: post a new trigger comment mentioning the bot (`prompt-template/block/force-push-detected.md:1-17`).
- inputs_outputs_state: Inputs are template variables `OLD_COMMIT`, `NEW_COMMIT`, and `BOT_MENTION_STRING`; the stop hook also passes `PR_NUMBER`, but this template does not consume it (`hooks/pr-loop-stop-hook.sh:415-417`). Output is rendered markdown embedded into a JSON hook block response.
- gates_or_invariants: The operational invariant is that rewritten commit history invalidates prior trigger/review state. The template does not decide that condition itself; it encodes the message used after the caller has already updated tracking to the new commit.
- dependencies_and_callers: Called from `hooks/pr-loop-stop-hook.sh` after detecting that the previous PR latest commit SHA is no longer reachable from current HEAD (`hooks/pr-loop-stop-hook.sh:380-421`). The caller preserves `OLD_COMMIT_SHA`, fetches the new head commit timestamp, rewrites `latest_commit_sha` and `latest_commit_at`, clears `last_trigger_at` and `trigger_comment_id`, updates local variables, renders this template with `load_and_render_safe`, then emits `{"decision":"block"}` (`hooks/pr-loop-stop-hook.sh:382-421`).
- edge_cases_or_failure_modes: If the new commit timestamp cannot be read from GitHub, the caller falls back to current UTC time before rendering (`hooks/pr-loop-stop-hook.sh:388-394`). If the template is missing or empty, `load_and_render_safe` uses an inline fallback that still blocks and asks for a new trigger (`hooks/pr-loop-stop-hook.sh:412-420`). The template’s wording says the loop “has updated its tracking,” which must remain true because the caller mutates the PR-loop state before rendering (`hooks/pr-loop-stop-hook.sh:396-410`).
- validation_or_tests: Direct reference was found in `hooks/pr-loop-stop-hook.sh:415`. Template rendering safety is provided by the single-pass template loader, which preserves missing placeholders and avoids recursive placeholder expansion (`hooks/lib/template-loader.sh:50-132`). No focused test file for this exact template was read in the assigned set.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-120 `file` `prompt-template/claude/agent-teams-core.md`
- cursor: `[_]`
- core_role: Shared prompt contract for Claude “Agent Teams” mode in the RLCR loop. It turns the main Claude instance into a coordination-only team leader and defines delegation, conflict-prevention, progress-monitoring, quality, commit, and plan-approval rules.
- algorithmic_behavior: The template forbids direct implementation by the team leader and constrains the leader to split tasks, create agent teams via Task tool with `team_name`, coordinate ownership, monitor progress, and wait for teammates rather than coding (`prompt-template/claude/agent-teams-core.md:1-12`). It then gives seven guidelines covering parallel task splitting, cold-start context for agents, strict file ownership or dependency sequencing, TaskList coordination, review of teammate output, teammate commits, and optional plan approval for risky work (`prompt-template/claude/agent-teams-core.md:14-23`). The Important section repeats Task tool usage, reassignment, merge/conflict resolution, no direct code, and interpreting idle teammates as waiting for leader response (`prompt-template/claude/agent-teams-core.md:24-30`).
- inputs_outputs_state: Input is the `AGENT_TEAMS=true` state/config flag in RLCR setup or continuation. Output is static markdown appended into generated round prompts. It does not read or write repository state itself, but it changes the behavioral policy of subsequent agent execution.
- gates_or_invariants: The key invariant is strict separation of coordinator and implementers: “team leader” must not write code or edit files (`prompt-template/claude/agent-teams-core.md:1-4`, `24-30`). Another invariant is non-overlapping file ownership because parallel edits to the same file are treated as silent overwrite risk, not merge-conflict-safe collaboration (`prompt-template/claude/agent-teams-core.md:16-19`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh` appends this core template to `round-0-prompt.md` when `AGENT_TEAMS=true`, after `agent-teams-instructions.md` (`scripts/setup-rlcr-loop.sh:1002-1011`). `hooks/loop-codex-stop-hook.sh` appends it for next-round continuation when agent teams are enabled and review has not started (`hooks/loop-codex-stop-hook.sh:1630-1640`). Both callers have fallbacks if templates are missing (`scripts/setup-rlcr-loop.sh:1011-1015`; `hooks/loop-codex-stop-hook.sh:1640-1649`).
- edge_cases_or_failure_modes: Missing template degrades to a shorter fallback that still preserves the no-direct-implementation rule. The policy assumes teammates can commit their own changes and that the leader can merge/resolve conflicts; if the runtime lacks those tools or permissions, the generated prompt could instruct behavior the environment cannot perform.
- validation_or_tests: `tests/test-agent-teams.sh` asserts this template exists, is at least 500 bytes, and contains “Your Role,” “Guidelines,” and “Important” sections (`tests/test-agent-teams.sh:317-330`). Setup and continuation call sites verify this is a shared template rather than duplicated prompt text.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-150 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for hook JSON parsing, command-pattern detection, monitor helper behavior, and monitor-loop edge cases. It validates safety properties around malformed input and hostile-ish terminal/log conditions that could affect the RLCR hook/monitor control plane.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and shared test helpers, creates a test directory, then pipes JSON into real hook validators (`loop-read-validator.sh`, `loop-write-validator.sh`, `loop-bash-validator.sh`) and checks exit-code behavior (`tests/robustness/test-hook-input-robustness.sh:14-40`). It covers valid tool inputs, invalid JSON, missing fields, long commands, special chars, Unicode, unknown tools, deep nesting, non-UTF8 bytes, and null-byte handling (`tests/robustness/test-hook-input-robustness.sh:35-235`). It then exercises monitor/log primitives, command modification detection, production goal/git parser functions from `scripts/humanize.sh`, and real `_humanize_monitor_codex` invocations under fake HOME/cache/session setups (`tests/robustness/test-hook-input-robustness.sh:237-665`).
- inputs_outputs_state: Inputs are generated JSON strings, temporary log/state/goal-tracker files, fake project directories, fake cache directories, and shims for `tput` and `clear`. Outputs are pass/fail records through `pass`, `fail`, and final `print_test_summary`, with process exit based on accumulated failures (`tests/robustness/test-hook-input-robustness.sh:666-671`).
- gates_or_invariants: Generic hook validators must accept well-formed valid JSON for the correct tool, reject malformed JSON and missing required fields with non-signal nonzero exits, ignore unrecognized tools, reject depth over 30, reject invalid UTF-8 with exit 1, and avoid signal crashes on null-byte-stripped input (`tests/robustness/test-hook-input-robustness.sh:35-235`). Monitor code must tolerate missing session dirs, narrow terminals, ANSI logs, binary logs, rapid log growth, and deleted logs without signal crashes (`tests/robustness/test-hook-input-robustness.sh:245-342`, `444-665`). Command pattern logic must detect `sed -i` and redirect writes through `command_modifies_file` (`tests/robustness/test-hook-input-robustness.sh:351-369`).
- dependencies_and_callers: Relies on `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, and `command_modifies_file` from `hooks/lib/loop-common.sh`. The production JSON validator rejects null bytes, optionally validates UTF-8 through `iconv`, validates JSON with `jq`, requires `tool_name`, and extracts `tool_input` (`hooks/lib/loop-common.sh:76-113`). `loop-read-validator.sh` and `loop-bash-validator.sh` show the tested integration pattern: validate JSON, reject deep nesting, check tool name, require tool-specific fields, then apply RLCR path/state rules (`hooks/loop-read-validator.sh:25-49`; `hooks/loop-bash-validator.sh:24-48`).
- edge_cases_or_failure_modes: The null-byte case documents a bash limitation: command substitution strips null bytes, so the test only asserts graceful handling rather than guaranteed rejection (`tests/robustness/test-hook-input-robustness.sh:219-235`). Non-UTF8 rejection depends on `iconv` availability in the shared validator (`hooks/lib/loop-common.sh:86-94`). Monitor tests spawn background deletion processes and use bounded wait loops; expected success is “no signal crash,” not necessarily semantic monitor completion (`tests/robustness/test-hook-input-robustness.sh:531-560`, `621-647`). The test writes temporary runner scripts and fake state files, so it is a robustness harness rather than a pure unit test.
- validation_or_tests: This file is itself listed in `tests/run-all-tests.sh` as `robustness/test-hook-input-robustness.sh` (`tests/run-all-tests.sh:68`). It complements `tests/robustness/test-hook-system-robustness.sh`, which covers the plan-file validator and state parsing robustness around active loop contexts (`tests/robustness/test-hook-system-robustness.sh:180-240`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique Item Evidence headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`