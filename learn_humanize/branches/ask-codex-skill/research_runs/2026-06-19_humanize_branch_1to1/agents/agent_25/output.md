# agent_25 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-025 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook task completion gate for the RLCR loop. It prevents transition into expensive Codex review/finalization while Claude still has incomplete work, supporting both legacy transcript `TodoWrite` state and file-backed Claude Task state.
- algorithmic_behavior: `extract_tool_calls_from_entry()` recognizes three transcript shapes: assistant message content blocks, generic `message` content blocks, and direct `tool_use` entries (`hooks/check-todos-from-transcript.py:23-55`). `find_incomplete_todos_from_transcript()` reads JSONL, ignores malformed lines, keeps only the latest non-empty `TodoWrite.todos`, and treats every status other than `completed` as incomplete (`:58-101`). `find_incomplete_tasks_from_directory()` scans `~/.claude/tasks/<session_id>/*.json` or a test override, defaults missing task status to `pending`, and treats only `completed` and `deleted` as terminal (`:104-147`). `main()` combines both sources and emits `INCOMPLETE_TODOS` plus formatted rows when anything remains open (`:150-196`).
- inputs_outputs_state: Input is hook JSON on stdin with optional `session_id`, `tasks_base_dir`, and `transcript_path` (`:151-175`). Outputs are exit `0` for no tasks/no data, exit `1` with stdout details for incomplete tasks, and exit `2` with stderr `PARSE_ERROR` for invalid hook JSON (`:9-12`, `:152-161`, `:177-196`). It reads task/transcript files but does not mutate RLCR state directly; the state transition is enforced by its caller blocking or allowing the stop hook.
- gates_or_invariants: Empty stdin, absent transcript, missing task directory, malformed task files, and malformed transcript JSONL lines are fail-open except invalid top-level hook JSON (`:154-161`, `:64-65`, `:76-79`, `:121-145`). Latest legacy `TodoWrite` wins, preventing stale transcript tasks from blocking once replaced (`:67-88`). File-backed tasks are authoritative when `session_id` exists and are merged with legacy transcript findings, so either source can block (`:163-175`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` before Codex review (`hooks/loop-codex-stop-hook.sh:342-397`). Exit `2` becomes a JSON hook block for parse failure, and exit `1` becomes an incomplete-task block rendered from `block/incomplete-todos.md` (`:356-397`). Test coverage is in `tests/test-todo-checker.sh`, which exercises invalid input, empty input, absent transcript, all-complete, incomplete, in-progress, malformed JSONL, latest `TodoWrite`, alternate transcript formats, missing status, empty content, task directory statuses, deleted tasks, and mixed legacy/task cases (`tests/test-todo-checker.sh:52-455`).
- edge_cases_or_failure_modes: Invalid top-level JSON blocks stop processing with exit `2`; invalid transcript lines and unreadable task JSON are silently skipped, which avoids false blocking but can miss corrupted task evidence (`hooks/check-todos-from-transcript.py:76-79`, `:143-145`). Missing `status` is incomplete for both todos and tasks (`:92-95`, `:130-132`). If both file tasks and transcript todos exist, a completed legacy list does not override an incomplete file task (`tests/test-todo-checker.sh:417-435`).
- validation_or_tests: Direct executable spec exists in `tests/test-todo-checker.sh`; not executed in this read-only branch export because the test creates temporary files/directories. The stop-hook integration path was inspected at `hooks/loop-codex-stop-hook.sh:349-397`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-055 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the `command_modifies_file()` mutation detector in `hooks/lib/loop-common.sh`. It validates the shell-pattern gate used by Bash hooks to block indirect writes to RLCR/PR loop control files.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, lowercases each sample command through `to_lower()`, then asserts whether `command_modifies_file "$command_lower" "$pattern"` returns true (`tests/test-bash-validator-patterns.sh:11-62`). The spec covers redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec`, read-only commands, edge forms like `>goal-tracker.md`, state files, and round summary patterns (`:69-194`).
- inputs_outputs_state: Input is the fixed list of command strings and target regexes in the script. Output is colored pass/fail lines plus process exit `0` when all assertions pass, `1` otherwise (`:20-34`, `:197-214`). It does not alter RLCR state; it validates the classifier used by runtime hooks.
- gates_or_invariants: Read-only commands such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` must not be classified as writes (`:136-153`). Literal target names should match across case after lowercasing (`:75-80`). Variable indirection such as `echo x > $FILE` is intentionally not matched (`:170-172`). The script documents a known limitation: multi-source `cp file1 file2 goal-tracker.md` is not detected because the pattern expects `cp src dest` (`:165-168`).
- dependencies_and_callers: Depends on `loop-common.sh` functions `to_lower()` and `command_modifies_file()` (`tests/test-bash-validator-patterns.sh:13`, `:40-43`, `:54-58`). The validated function is consumed by `hooks/loop-bash-validator.sh` to block writes to `state.md`, `finalize-state.md`, plan backups, `goal-tracker.md`, prompts, summaries, todos, and PR-loop files (`hooks/loop-bash-validator.sh:139-156`, `:336-390`, `:407-440`).
- edge_cases_or_failure_modes: The test intentionally confirms case normalization and destination-position matching but does not cover quoted paths with spaces, shell wrappers, or chained command parsing; those are handled elsewhere in `loop-bash-validator.sh` for state-file source moves (`hooks/loop-bash-validator.sh:169-328`). The known multi-source copy gap remains a documented false negative in this classifier.
- validation_or_tests: This file is itself the validation asset and is included in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:34`). Not executed here because the task requested research notes only and the environment is read-only.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-085 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR/PR loop algorithm library. It centralizes hook input validation, loop/session discovery, state parsing, Codex review result detection, file/path classification, protected-action messages, cancel authorization, git-add protection, mutation detection, loop termination, and PR goal-tracker updates.
- algorithmic_behavior: Defines canonical state fields and Codex defaults (`hooks/lib/loop-common.sh:24-45`). Validates hook JSON with null-byte, UTF-8, `jq`, and required `tool_name` checks (`:67-113`), plus field and depth helpers (`:115-150`). Resolves active/terminal state files and finds the newest active RLCR loop with optional `session_id`, deliberately avoiding revival of older “zombie” loops (`:176-291`). Parses state frontmatter in tolerant and strict modes; strict mode requires `current_round`, `max_iterations`, `review_started`, and `base_branch`, validates numeric and boolean fields, then defaults optional fields (`:308-449`). Detects Codex review issues by scanning only the last 50 log lines for `[P?]` markers in the first 10 characters and writes an audit result file when issues are found (`:451-520`). Provides path classifiers, allowlist checks, message renderers, cancel validation, PR-loop helpers, git-add protection, generic shell mutation detection, loop ending, and PR tracker mutation (`:522-1443`).
- inputs_outputs_state: Inputs include hook JSON strings, `.humanize/rlcr/*/state.md` or `finalize-state.md`, Codex review logs under `$CACHE_DIR`, command strings, file paths, loop directories, and PR bot result JSON. Outputs are shell return codes, stdout paths/parsed values, stderr diagnostics/messages, and in mutating helpers: review result files (`:509-511`), renamed terminal state files via `end_loop()` (`:1240-1273`), and updated PR goal trackers via temp-file replacement (`:1296-1443`).
- gates_or_invariants: Hook input must be valid JSON, UTF-8, non-null-byte, and include `tool_name` (`:76-113`). Deep JSON above depth 30 can be rejected by callers (`:133-150`). Active-loop discovery checks only the newest directory without a session filter and, with a session filter, stops at that session’s newest loop even if terminal, preventing stale loop revival (`:215-291`). Strict state parsing fails closed on malformed/missing schema, including missing `base_branch` (`:386-449`). Cancel authorization requires `.cancel-requested`, rejects command substitution, shell chaining, residual variables, mixed quotes, extra args, wrong source/destination, and symlink sources (`:661-838`). `git_adds_humanize()` blocks direct `.humanize`, force broad adds, `-A/--all`, and broad `.`/`*` adds when `.humanize` is not ignored (`:1036-1154`). `command_modifies_file()` classifies common shell write forms via regex array (`:1196-1224`).
- dependencies_and_callers: Sourced by RLCR validators and stop/setup/cancel scripts per header (`:5-13`), including `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, `loop-codex-stop-hook.sh`, and `scripts/humanize.sh`. It sources `hooks/lib/template-loader.sh` and uses `load_and_render_safe` templates for user-facing block messages (`:156-165`, `:571-629`, `:1156-1194`, `:1226-1238`). Runtime enforcement examples: read validator uses active-loop, strict state, round parsing, allowlist, and wrong-location templates (`hooks/loop-read-validator.sh:27-150`); bash validator uses strict state, cancel authorization, git-add and mutation detection (`hooks/loop-bash-validator.sh:24-445`).
- edge_cases_or_failure_modes: `resolve_any_state_file()` uses the first `*-state.md` from `ls | head`, so terminal-state choice is filesystem sort dependent if multiple terminal files exist (`hooks/lib/loop-common.sh:209-212`). `parse_state_file()` is tolerant and defaults some fields, while strict parsing is required for fail-closed hooks (`:333-449`). `detect_review_issues()` can miss markers outside the final 50 log lines by design (`:460-468`, `:487-520`). `command_modifies_file()` is broad but regex-based and documented by tests as not catching all shell write forms. Cancel authorization lowercases commands, which is acceptable for expected state filenames but can matter for case-sensitive unusual paths.
- validation_or_tests: Covered by many test suites: `tests/test-bash-validator-patterns.sh` for mutation patterns; `tests/test-allowlist-validators.sh` for allowlist and loop file protection; `tests/test-session-id.sh` for session-aware loop filtering; robustness suites for hook input, state files, state transitions, cancel security, PR-loop API, and concurrent state. Not executed here due read-only research constraints.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-115 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: User-facing block template for the read validator when a prompt/summary round file is requested from outside the active RLCR loop directory.
- algorithmic_behavior: Renders `{{FILE_PATH}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{CURRENT_ROUND}}` into a concise correction message (`prompt-template/block/wrong-file-location.md:1-9`). The runtime path is `loop-read-validator.sh`: after detecting a round summary/prompt read, it finds the active loop, strict-parses state, derives current round and file type, and if the file is not under `.humanize/rlcr/`, renders this template and exits `2` (`hooks/loop-read-validator.sh:72-131`).
- inputs_outputs_state: Inputs are template variables supplied by `load_and_render_safe`: requested file path, active loop directory, and current round (`hooks/loop-read-validator.sh:127-130`). Output is a block reason on stderr; no persistent state changes.
- gates_or_invariants: Enforces location correctness for round prompt/summary reads: active-loop files must come from `{{ACTIVE_LOOP_DIR}}/`, not an arbitrary same-named file elsewhere (`prompt-template/block/wrong-file-location.md:3-7`). It is paired with wrong-round and wrong-directory checks in `loop-read-validator.sh`, so location is one gate in a broader round-file access state machine (`hooks/loop-read-validator.sh:118-169`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh` via `load_and_render_safe`, with fallback text in `hooks/loop-read-validator.sh:124-130`. Related templates include `wrong-round-file.md` and `wrong-directory-path.md` for adjacent read/write path errors.
- edge_cases_or_failure_modes: The final line says `cat {{FILE_PATH}}` (`prompt-template/block/wrong-file-location.md:9`), which repeats the blocked path rather than the computed active-loop path; this may be intentionally saying “if you need this exact file, use Bash cat” because the hook is a Read-tool validator, but it can also be read as contradictory guidance. The template does not include `CORRECT_PATH`, although the caller computes it at `hooks/loop-read-validator.sh:123`.
- validation_or_tests: Template reference tests inspect common template wiring, though this exact template was found through the read-validator reference rather than the `COMMON_TEMPLATES` list (`tests/test-template-references.sh:149-167`). Runtime coverage is implied by read-validator path tests in the allowlist/validator suites.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-145 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for RLCR setup’s base-branch auto-detection. This protects the review-base selection used later by Codex review and state parsing.
- algorithmic_behavior: Defines `detect_base_branch()` as an isolated reproduction of setup logic: query `git remote show origin` for `HEAD branch`, ignore empty or `(unknown)`, then fall back to local `main`, then local `master`, else return failure (`tests/robustness/test-base-branch-detection.sh:35-64`). It uses `run_with_timeout` from `scripts/portable-timeout.sh` for macOS/Linux compatibility (`:18-19`, `scripts/portable-timeout.sh:31-71`). The real setup script has the same priority but additionally verifies remote default exists locally and validates user-specified branches (`scripts/setup-rlcr-loop.sh:647-700`).
- inputs_outputs_state: Inputs are temporary git repositories created by the test: no-origin with `main`, no-origin with `master`, no `main`/`master`, and a bare `origin` simulation (`tests/robustness/test-base-branch-detection.sh:70-191`). Outputs are pass/fail counters through `tests/test-helpers.sh` and summary return status (`tests/test-helpers.sh:30-77`, `tests/robustness/test-base-branch-detection.sh:193-198`). The test mutates only temporary repositories under `TEST_DIR`.
- gates_or_invariants: Priority is remote default, then local `main`, then local `master`, matching command docs that state auto-detection order (`commands/start-rlcr-loop.md:67`). Missing origin must not fail the function because the remote query is guarded with `|| true` under `pipefail` (`tests/robustness/test-base-branch-detection.sh:41-45`). A repo with only `develop` must fail (`:124-149`). The production setup also blocks remote-only user-specified base branches because `codex review --base` needs a local ref (`scripts/setup-rlcr-loop.sh:652-668`), rejects YAML-unsafe base names (`:703-709`), and captures a fixed base commit SHA to avoid self-diff drift (`:711-719`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp directory and result accounting and `scripts/portable-timeout.sh` for timeout abstraction (`tests/robustness/test-base-branch-detection.sh:16-24`). It is listed in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:73`). Its behavioral parent is `scripts/setup-rlcr-loop.sh`, which writes `base_branch` and `base_commit` into RLCR state; `loop-common.sh` later requires `base_branch` in strict state parsing (`hooks/lib/loop-common.sh:419-421`).
- edge_cases_or_failure_modes: The test’s helper accepts any non-unknown remote default without verifying local existence (`tests/robustness/test-base-branch-detection.sh:43-48`), while production setup requires the remote default branch to exist locally before selecting it (`scripts/setup-rlcr-loop.sh:675-680`). Test 4 is intentionally tolerant: it passes any detected branch unless detection fully fails, because `git remote show` may fall back to local `main` in the simulated remote case (`tests/robustness/test-base-branch-detection.sh:181-190`).
- validation_or_tests: This file is the validation spec. Not executed here because it creates and commits temporary git repositories, and the assigned branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ASK_CODEX_SKILL-HZ-025`, `ASK_CODEX_SKILL-HZ-055`, `ASK_CODEX_SKILL-HZ-085`, `ASK_CODEX_SKILL-HZ-115`, `ASK_CODEX_SKILL-HZ-145`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`