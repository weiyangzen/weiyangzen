# agent_27 ask-codex-skill 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `caf375a20530c8bb81e8e8e103a8598c25c11bb0`

## Item Evidence

### ASK_CODEX_SKILL-HZ-027 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash guard for active RLCR and PR-loop state machines. It prevents shell commands from bypassing the Write/Edit/Read validators and protects loop-owned state, prompts, summaries, todos, plan backups, PR comments, and generated review artifacts.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON and depth, exits early for non-`Bash` tools, then extracts `.tool_input.command` (`hooks/loop-bash-validator.sh:24-48`). It locates the active RLCR loop by `session_id` and active PR loop from `.humanize/*` directories (`hooks/loop-bash-validator.sh:54-69`). For RLCR, it resolves `finalize-state.md` before `state.md`, strictly parses state, and uses `current_round` plus `push_every_round` to gate actions (`hooks/loop-bash-validator.sh:77-106`). It then runs ordered block checks: `git add` of `.humanize`, state/finalize-state writes or moves, plan backup writes, `goal-tracker.md` writes, round prompt/summary/todos writes, and PR-loop protected file writes (`hooks/loop-bash-validator.sh:115-445`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name`, `tool_input.command`, and optional `session_id`. Output is process exit status plus stderr block text for normal hook blocks. Exit `0` allows, `1` rejects malformed hook/state input, and `2` blocks valid-but-forbidden Bash operations. It does not mutate state itself; it derives state from `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md}` and `.humanize/pr-loop/<timestamp>/state.md`.
- gates_or_invariants: Fails closed on invalid hook JSON, missing Bash command, excessive JSON depth, or malformed RLCR state (`hooks/loop-bash-validator.sh:26-45`, `hooks/loop-bash-validator.sh:81-85`). Blocks `git push` unless `push_every_round: true` (`hooks/loop-bash-validator.sh:96-105`). Blocks forced or broad `git add` paths that would stage `.humanize` (`hooks/loop-bash-validator.sh:115-118`). Protects `finalize-state.md` before generic `state.md` to avoid pattern shadowing (`hooks/loop-bash-validator.sh:139-156`). Allows the special state-to-cancel-state rename only if `is_cancel_authorized` accepts a signal-file-backed command (`hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:286-327`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `validate_hook_input`, `is_deeply_nested`, `extract_session_id`, active-loop lookup, strict state parsing, cancellation authorization, `.humanize` git-add detection, command modification detection, and block messages (`hooks/lib/loop-common.sh:76-149`, `hooks/lib/loop-common.sh:233-306`, `hooks/lib/loop-common.sh:386-448`, `hooks/lib/loop-common.sh:679-720`, `hooks/lib/loop-common.sh:1058-1095`, `hooks/lib/loop-common.sh:1199-1224`). It also uses `load_and_render_safe` templates through the shared library for human-facing block reasons.
- edge_cases_or_failure_modes: Explicitly handles chained commands, shell wrappers, `mv`/`cp` source-side state-file bypasses, leading redirections, append redirections, descriptor redirections, `&>`/`&>>`, quoted paths, ANSI-C quoting, escaped spaces, and command segments split by `;`, `&&`, `||`, pipes, and background operators (`hooks/loop-bash-validator.sh:169-328`). Potential false positives are intentional around bare `state.md` in PR-loop mode to catch relative-path bypasses (`hooks/loop-bash-validator.sh:403-415`). Todo write protection only allows active-loop full-path `round-[12]-todos.md`, so later rounds or same-basename outside active loop are blocked (`hooks/loop-bash-validator.sh:385-392`).
- validation_or_tests: Covered indirectly by pattern tests such as `tests/test-bash-validator-patterns.sh`, `.humanize` escape tests, cancel-signal tests, hook robustness tests, and the concurrent state robustness tests assigned below. The assigned validator itself delegates most pattern semantics to shared helpers, especially `command_modifies_file` (`hooks/lib/loop-common.sh:1199-1224`) and `git_adds_humanize` (`hooks/lib/loop-common.sh:1058-1095`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-057 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for Codex review-log issue extraction, specifically `detect_review_issues()` in `hooks/lib/loop-common.sh`.
- algorithmic_behavior: Creates an isolated test directory and `XDG_CACHE_HOME`, sources `hooks/lib/loop-common.sh`, then writes synthetic `round-N-codex-review.log` files into `CACHE_DIR` and invokes `detect_review_issues N` (`tests/test-codex-review-merge.sh:33-43`, `tests/test-codex-review-merge.sh:47-54`). The algorithm under test scans only the last 50 lines, looks for `[P0-9]` in the first 10 characters of a line, extracts from the first match through EOF, and returns no-issue/error codes otherwise (`tests/test-codex-review-merge.sh:11-15`).
- inputs_outputs_state: Inputs are round numbers and log files at `$CACHE_DIR/round-${round}-codex-review.log`. On match, `detect_review_issues()` writes `$LOOP_DIR/round-${round}-review-result.md`, emits a `## Codex Review Issues` section on stdout, and returns `0` (`hooks/lib/loop-common.sh:472-515`). It returns `1` for no issue marker and `2` for missing or empty log (`hooks/lib/loop-common.sh:477-520`). Test output is PASS/FAIL counts and exit `1` if any assertion fails (`tests/test-codex-review-merge.sh:357-368`).
- gates_or_invariants: `[P?]` must appear within `substr($0, 1, 10)` of a last-50-lines line; mentions later in a line do not count (`tests/test-codex-review-merge.sh:83-107`, `hooks/lib/loop-common.sh:491-498`). Logs longer than 50 lines ignore early issue-looking text outside the window (`tests/test-codex-review-merge.sh:199-227`). Missing and empty logs are hard errors, not “no issues” (`tests/test-codex-review-merge.sh:133-169`).
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`; production caller is `hooks/loop-codex-stop-hook.sh`, which treats exit `0` as continue review loop, `1` as proceed toward finalize, and `2` as retry/block hard error (`hooks/loop-codex-stop-hook.sh:1021-1050`). Review-result files are referenced by Codex prompt templates and setup docs as loop artifacts.
- edge_cases_or_failure_modes: Tests issue markers at position `0`, after dash prefix, multiple issue markers where extraction starts at the first, exactly 50-line logs, late markers in long logs, early markers outside the scan window, markers in the middle of lines, no markers, missing logs, and empty logs (`tests/test-codex-review-merge.sh:56-354`). A real issue emitted earlier than the last 50 lines is intentionally ignored to avoid stale debug false positives.
- validation_or_tests: This file is itself a runnable validation script. It has 12 assertions and exits nonzero on failure (`tests/test-codex-review-merge.sh:56-368`).
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-087 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Block-message template for the PR-loop transition that waits for Claude bot acknowledgment. It defines the operator-facing contract when Claude does not add an `eyes` reaction after bounded retries.
- algorithmic_behavior: The template renders `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}`, explains likely causes, lists required checks, and tells the operator to post a new trigger comment after configuration is fixed (`prompt-template/block/claude-eyes-timeout.md:1-18`). It does not execute logic; it is selected by the PR stop hook when bot acknowledgement fails.
- inputs_outputs_state: Inputs are template variables `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, supplied by `hooks/pr-loop-stop-hook.sh` as `3` and `15` after a `check-bot-reactions.sh claude-eyes ... --retry 3 --delay 5` call (`hooks/pr-loop-stop-hook.sh:775-786`). Output is rendered markdown embedded in a hook JSON block decision (`hooks/pr-loop-stop-hook.sh:785-790`).
- gates_or_invariants: The gate only runs after trigger validation succeeds, so the hook does not waste the eyes check on missing or stale trigger data (`hooks/pr-loop-stop-hook.sh:725-747`). It is blocking only when Claude is in configured bots, a trigger is required, and a trigger comment ID is available (`hooks/pr-loop-stop-hook.sh:760-779`). Missing or `null` eyes reaction blocks progression (`hooks/pr-loop-stop-hook.sh:779-790`).
- dependencies_and_callers: Consumed by `load_and_render_safe "$TEMPLATE_DIR" "block/claude-eyes-timeout.md"` in `hooks/pr-loop-stop-hook.sh` (`hooks/pr-loop-stop-hook.sh:785-786`). Rendering semantics come from `hooks/lib/template-loader.sh`: placeholders use `{{VAR}}`, missing variables remain unchanged, and substitution is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:7-20`, `hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-190`).
- edge_cases_or_failure_modes: If the template is missing or empty, the caller’s fallback message is rendered instead (`hooks/pr-loop-stop-hook.sh:781-786`, `hooks/lib/template-loader.sh:176-186`). If trigger exists but no comment ID is available, the hook warns and skips this template rather than blocking with the timeout (`hooks/pr-loop-stop-hook.sh:794-797`).
- validation_or_tests: Template-reference and template-loader tests cover existence and fallback behavior broadly. PR-loop behavior tests reference wrong-trigger and bot-review gates, while this template’s direct production path is the Claude eyes verification block in `hooks/pr-loop-stop-hook.sh`.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-117 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR round-number invariants. It prevents workers from writing or editing a stale/future `round-N-summary.md` instead of the current round’s summary.
- algorithmic_behavior: Renders a concise markdown block with the attempted action, attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, current round, correct path, and the invariant “Do NOT increment the round number yourself” (`prompt-template/block/wrong-round-number.md:1-7`).
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Output is stderr block text from Write/Edit validators with exit `2` when the attempted summary file’s round differs from state current round (`hooks/loop-write-validator.sh:230-246`, `hooks/loop-edit-validator.sh:162-178`).
- gates_or_invariants: The invariant is `CLAUDE_ROUND == CURRENT_ROUND` for summary files unless the file is allowlisted (`hooks/loop-write-validator.sh:230-246`, `hooks/loop-edit-validator.sh:165-178`). The correct path is always derived from `ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`, not from user-provided path text (`hooks/loop-write-validator.sh:233-245`, `hooks/loop-edit-validator.sh:165-177`).
- dependencies_and_callers: Called by both `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` via `load_and_render_safe` (`hooks/loop-write-validator.sh:240-245`, `hooks/loop-edit-validator.sh:172-177`). Template rendering uses the shared single-pass loader (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-190`). Tests render it directly in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`.
- edge_cases_or_failure_modes: If round extraction fails (`CLAUDE_ROUND` empty), the wrong-round gate does not fire, so other file-location/type gates must handle invalid paths. If the template is unavailable, validators use inline fallback text with the same variables (`hooks/loop-write-validator.sh:235-245`, `hooks/loop-edit-validator.sh:167-177`).
- validation_or_tests: Template-loader tests explicitly render `block/wrong-round-number.md`; Write/Edit validator tests cover stale summary path blocking through the validator paths noted above.
- skip_candidate: `no`

### ASK_CODEX_SKILL-HZ-147 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for concurrent and malformed state access in the RLCR/PR-loop state machine.
- algorithmic_behavior: Sources shared test helpers and `hooks/lib/loop-common.sh`, then creates many synthetic `.md` state files and loop directory layouts under a temp directory (`tests/robustness/test-concurrent-state-robustness.sh:15-50`, `tests/robustness/test-concurrent-state-robustness.sh:71-73`). It validates tolerant parsing (`get_current_round`), active loop detection (`find_active_loop` and `find_active_pr_loop`), concurrent reads, atomic write/read consistency, strict parser failures, stale terminal-state behavior, and edge-value handling.
- inputs_outputs_state: Inputs are temp state files with YAML frontmatter, timestamped loop directories, simulated PR-loop state, concurrent background writer activity, and file permissions. Outputs are pass/fail helper calls and final `print_test_summary`, exiting with helper-provided aggregate status (`tests/robustness/test-concurrent-state-robustness.sh:554-559`). It mutates only temp test directories.
- gates_or_invariants: `get_current_round` should parse expected numeric rounds or degrade to `0`/empty on whitespace, CRLF, empty values, comments, permission errors, and missing parseability (`tests/robustness/test-concurrent-state-robustness.sh:59-160`, `tests/robustness/test-concurrent-state-robustness.sh:529-552`). `find_active_loop` returns only the newest active directory in no-filter mode, detects `finalize-state.md`, ignores non-existent/empty bases, and does not revive older stale loops when newer directories are terminal or missing active state (`tests/robustness/test-concurrent-state-robustness.sh:170-240`, `tests/robustness/test-concurrent-state-robustness.sh:452-484`). Atomic writes via temp file plus `mv` must produce 20/20 valid reads during concurrent access (`tests/robustness/test-concurrent-state-robustness.sh:281-322`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and summary output (`tests/robustness/test-concurrent-state-robustness.sh:17-19`). Exercises `hooks/lib/loop-common.sh` functions: `get_current_round` (`hooks/lib/loop-common.sh:296-306`), `find_active_loop` (`hooks/lib/loop-common.sh:233-291`), `parse_state_file_strict` (`hooks/lib/loop-common.sh:386-448`), and `find_active_pr_loop` (`hooks/lib/loop-common.sh:926-942`).
- edge_cases_or_failure_modes: Covers trailing/leading whitespace, CRLF line endings, empty current round, YAML inline comments, no frontmatter, malformed YAML-like syntax, unicode content, YAML list content in PR state, old loops, cancelled loops, large round numbers, zero round, and unreadable state files (`tests/robustness/test-concurrent-state-robustness.sh:59-160`, `tests/robustness/test-concurrent-state-robustness.sh:332-552`). One notable tension: the test’s `parse_state_file_strict` fixtures omit newer required fields such as `review_started` and `base_branch`, while current strict parser requires them (`hooks/lib/loop-common.sh:415-421`); the tests still expect nonzero for malformed/missing-frontmatter cases, so this strengthens failure but may mask which strict-field gate fired.
- validation_or_tests: This file is itself a validation script with 22 named tests across parsing, detection, concurrency, strict validation, PR loop state, stale loop detection, and permission/edge cases (`tests/robustness/test-concurrent-state-robustness.sh:56-559`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 unique item sections; every assigned row represented once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`