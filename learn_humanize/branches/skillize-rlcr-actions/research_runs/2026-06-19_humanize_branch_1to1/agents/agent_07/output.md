# agent_07 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Executable specification corpus for the RLCR/Humanize loop. `tests/run-all-tests.sh:32-54` orders the main suites, and `tests/run-all-tests.sh:56-59` declares zsh-specific execution for runtime glob-safety coverage. The directory has 22 shell files at root level and no nested test subdirectories.
- algorithmic_behavior: The directory verifies the hook-driven algorithm around loop startup, active-loop discovery, state transitions, protected file access, prompt/template rendering, Codex review/stop handling, monitor shutdown, skill packaging, and cancel authorization. The suite is shell-first, with Python coverage for todo transcript parsing via `tests/test-todo-checker.sh:13` and `python3 "$TODO_CHECKER"` calls such as `tests/test-todo-checker.sh:54`.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/` loop trees, hook JSON payloads, temp git repos, generated state/frontmatter, mock `codex` binaries, transcript JSONL, and prompt templates. Outputs are process exit codes, hook JSON block decisions, pass/fail counters, and renamed state files. Test-local state is usually under `mktemp -d` or `/tmp`, for example `tests/test-plan-file-hooks.sh:31-32`, `tests/test-cancel-signal-file.sh:29-33`, and `tests/test-monitor-e2e-real.sh:48`.
- gates_or_invariants: The directory encodes hard gates: plan files must be relative, in-project, non-symlink, contentful, git-state-compatible, and YAML-safe (`tests/test-plan-file-validation.sh:89-101`, `:169-187`, `:518-658`, `:700-735`). Loop backups/state/prompt files are protected from Write/Edit/Bash mutation (`tests/test-plan-file-hooks.sh:194-239`, `tests/test-finalize-phase.sh:293-350`). Cancel requires an active-loop `.cancel-requested` signal plus an exact `mv state.md|finalize-state.md cancel-state.md` shape (`tests/test-cancel-signal-file.sh:74-113`, `:1257-1340`). Monitor iteration must use `find` patterns rather than unsafe globs in bash/zsh (`tests/test-humanize-escape.sh:201-236`, `tests/test-zsh-monitor-safety.sh:5-9`).
- dependencies_and_callers: The tests coordinate with sibling implementation paths: `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/loop-*-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/check-todos-from-transcript.py`, `scripts/setup-rlcr-loop.sh`, `scripts/humanize.sh`, `prompt-template/**`, `commands/**`, `skills/**`, and `.claude-plugin/**`. Example cross-reference scans are explicit in `tests/test-template-references.sh:56-64`; hook preservation checks `hooks/hooks.json` and hook scripts in `tests/test-hook-preservation.sh:54-249`; setup validation calls `scripts/setup-rlcr-loop.sh` throughout `tests/test-plan-file-validation.sh`.
- edge_cases_or_failure_modes: Coverage is heavy on bypasses: quoted and path-variant `.humanize` adds, force/all git-add flags, chained commands, `git -C`, empty and dotfile-only loop dirs, no `*-state.md`, stale/legacy `.humanize-loop.local`, command substitution, glob/brace/backtick bypasses, hidden shell variables, newline injection, missing templates, placeholder injection, non-ASCII/special template values, malformed placeholders, incomplete TodoWrite state, dirty git state, max iteration exit, and finalize-phase no-Codex behavior. Representative lines: `tests/test-humanize-escape.sh:83-192`, `tests/test-plan-file-hooks.sh:272-335`, `tests/test-cancel-signal-file.sh:320-500`, `tests/test-template-loader.sh:247-542`, `tests/test-finalize-phase.sh:390-435`.
- validation_or_tests: Child roles by suite: `test-template-loader.sh`, `test-error-scenarios.sh`, `test-template-references.sh`, and `test-templates-comprehensive.sh` validate template loading/rendering/fallbacks/reference integrity; `test-bash-validator-patterns.sh`, `test-humanize-escape.sh`, `test-allowlist-validators.sh`, `test-plan-file-hooks.sh`, `test-cancel-signal-file.sh`, and `test-finalize-phase.sh` validate hook gates and state transitions; `test-plan-file-validation.sh` validates setup input gates; `test-todo-checker.sh` validates transcript/TodoWrite interpretation; `test-monitor-runtime.sh`, `test-monitor-e2e-real.sh`, `test-zsh-monitor-safety.sh`, and `manual-monitor-test.sh` validate monitor shutdown and shell compatibility; `test-hook-preservation.sh`, `test-skill-invocation.sh`, and `test-skill-structure.sh` validate packaging, hooks, skills, and command delegation; `test-ansi-parsing.sh` validates the runner’s colored output parsing. I inspected only; I did not execute the suite.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-037 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Focused executable regression spec for `.humanize` escape prevention and zsh/bash-safe directory iteration. Its header ties the file to AC-1.2 “no matches found” safety and AC-2.1 `.humanize` git-add blocking at `tests/test-humanize-escape.sh:5-12`.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` (`tests/test-humanize-escape.sh:16-18`), defines `assert_blocks` and `assert_allows` wrappers over `to_lower` plus `git_adds_humanize` (`tests/test-humanize-escape.sh:45-70`), then runs eight groups of positive/negative cases. It validates path variants, quoted paths, force/all flags, chained commands, `git -C` forms, allowed false positives, and `find`-based iteration patterns.
- inputs_outputs_state: Inputs are literal shell command strings and temporary directories under `/tmp`. The `git_adds_humanize` tests pass lowercased command strings into the shared parser. The `git add -A/--all` group creates a real `.humanize` directory before asserting blocks (`tests/test-humanize-escape.sh:128-144`) because the implementation only blocks all-add forms when `.humanize` exists. Outputs are colored PASS/FAIL lines and an exit code of 0 if `TESTS_FAILED` remains zero (`tests/test-humanize-escape.sh:337-352`).
- gates_or_invariants: Direct or nested `.humanize` paths must block, including `./.humanize`, `path/to/.humanize`, quoted forms, force flags, chained commands, and git options (`tests/test-humanize-escape.sh:83-166`). Specific safe adds must remain allowed, including `.gitignore`, `src/.gitkeep`, patch mode, and similarly prefixed non-target files like `.humanizeconfig` (`tests/test-humanize-escape.sh:175-192`). Directory iteration must return zero cleanly for empty dirs, dotfiles-only dirs, and no `*-state.md` files (`tests/test-humanize-escape.sh:204-269`).
- dependencies_and_callers: Depends directly on `to_lower` and `git_adds_humanize` from `hooks/lib/loop-common.sh`. The implementation splits command segments, detects `git ... add`, strips quotes, blocks direct `.humanize` refs, force+broad scope, all-add when `.humanize` exists, and broad scope when not gitignored at `hooks/lib/loop-common.sh:443-539`. It also validates the same `find` idiom used by `scripts/humanize.sh` monitor logic.
- edge_cases_or_failure_modes: Important edge cases include lowercasing `-A` into `-a`, quoted `.humanize`, `git -C /path add`, `git --git-dir=/repo add`, `-fA`/`-Af`, broad `*`, false positive avoidance for `.humanize-backup`, and zsh empty-glob failures avoided by `find` process substitution (`tests/test-humanize-escape.sh:109-119`, `:153-192`, `:201-236`). Known limitation inherited from parser style: this test exercises regex command recognition rather than a shell AST.
- validation_or_tests: The file is itself the validation artifact. It covers helper behavior without invoking the full hook JSON path; full Bash hook integration is covered by sibling suites such as `tests/test-plan-file-hooks.sh` and `tests/test-cancel-signal-file.sh`.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-067 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Blocking-message template for the invariant that `round-*-prompt.md` files are generated instructions and cannot be modified by Claude/user-agent work. The template states the prohibition at `prompt-template/block/prompt-file-write.md:1-7` and redirects work to summaries at `:8-12`.
- algorithmic_behavior: This is a static prompt/block template consumed by the shared `prompt_write_blocked_message` function. The function loads it via `load_and_render_safe "$TEMPLATE_DIR" "block/prompt-file-write.md"` at `hooks/lib/loop-common.sh:201-208`; if missing, an inline fallback preserves the same gate. The template has no variables, so rendering is identity.
- inputs_outputs_state: Input is an attempted Write/Edit/Bash modification targeting `round-[0-9]+-prompt.md`. Output is the block text sent on stderr or hook response reason, telling the agent to read the prompt, execute tasks, and write results to the summary file. It mutates no repository or loop state.
- gates_or_invariants: Prompt files are immutable because they contain instructions “FROM Codex TO you” (`prompt-template/block/prompt-file-write.md:5`). Write validator blocks prompt paths at `hooks/loop-write-validator.sh:47-49`; Edit validator blocks at `hooks/loop-edit-validator.sh:46-49`; Bash validator blocks modifying commands matching `round-[0-9]+-prompt\.md` at `hooks/loop-bash-validator.sh:319-326`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for `load_and_render_safe`, whose fallback behavior protects against empty/missing templates (`hooks/lib/template-loader.sh:167-203`). It is included in template-reference validation as a required common template at `tests/test-template-references.sh:152-155`, and the scan verifies referenced template files exist at `tests/test-template-references.sh:83-110`.
- edge_cases_or_failure_modes: The main failure mode is template disappearance or empty content; the loader fallback prevents a silent empty block message. Bash detection is regex-based via `command_modifies_file`, so prompt protection depends on matching common mutation forms like redirect, tee, sed/perl/awk in-place edit, mv/cp/rm, dd, truncate, printf redirect, and exec redirect (`hooks/lib/loop-common.sh:571-599`).
- validation_or_tests: `tests/test-template-references.sh` explicitly cross-references `block/prompt-file-write.md` (`tests/test-template-references.sh:152-155`) and checks critical validators use safe rendering (`tests/test-template-references.sh:172-200`). Broader prompt/template loading and fallback behavior are exercised by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`. I inspected; I did not execute tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `SKILLIZE_RLCR_ACTIONS-HZ-007`, `SKILLIZE_RLCR_ACTIONS-HZ-037`, `SKILLIZE_RLCR_ACTIONS-HZ-067`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`