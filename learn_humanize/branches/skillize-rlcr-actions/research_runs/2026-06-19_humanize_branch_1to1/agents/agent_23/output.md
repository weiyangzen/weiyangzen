# agent_23 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-023 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Read gate for RLCR loop artifacts. It prevents Claude from reading stale, misplaced, or system-managed loop files: old round prompts/summaries, round files outside the active `.humanize/rlcr/<loop>/` directory, old session directories, and most `round-*-todos.md` files. It is registered as the Read hook in `hooks/hooks.json:33-39`.
- algorithmic_behavior: The script reads hook JSON from stdin, extracts `.tool_name`, and exits immediately unless the tool is `Read` (`hooks/loop-read-validator.sh:22-27`). It lowercases the requested path (`:29-31`), first handles `round-N-todos.md` reads (`:36-43`), then ignores all non-summary/non-prompt paths (`:50-52`). For prompt/summary reads, it resolves the active loop, chooses `finalize-state.md` over `state.md` when present (`:61-76`), parses the current round (`:76-78`), extracts the round number from the target filename (`:83-86`), classifies the file as `summary` or `prompt` (`:88-94`), then enforces location, round, and active-directory correctness (`:100-146`). Allowed reads exit `0`; blocked reads write an explanatory template-rendered message to stderr and exit `2`.
- inputs_outputs_state: Inputs are hook JSON on stdin, `CLAUDE_PROJECT_DIR`/`PROJECT_ROOT`/`LOOP_BASE_DIR`/`LOOP_DIR` environment context, active loop files under `.humanize/rlcr`, and templates under `prompt-template/block`. Outputs are only process status and stderr guidance; the validator does not mutate loop state. The only state transition it observes is normal-loop versus Finalize Phase state source selection: `state.md` is parsed unless `finalize-state.md` exists, in which case `finalize-state.md` becomes authoritative for `current_round` (`hooks/loop-read-validator.sh:69-78`).
- gates_or_invariants: Non-Read tools are never gated by this script (`hooks/loop-read-validator.sh:25-27`). Todos reads are blocked unless the requested file is in the explicit active-loop allowlist (`:36-43`). Summary/prompt reads must be inside `.humanize/rlcr/` (`:100-110`), must match `current_round` unless allowlisted (`:116-129`), and must point exactly at `$ACTIVE_LOOP_DIR/$filename` (`:135-146`). Shared invariants come from `hooks/lib/loop-common.sh`: active loop is the newest timestamped directory containing `state.md` or `finalize-state.md` (`:58-81`), round files match `round-[0-9]+-{summary|prompt|todos}.md` (`:146-153`), and the allowlist is limited to `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop (`:167-188`).
- dependencies_and_callers: Depends on `jq`, Bash strict mode, `hooks/lib/loop-common.sh`, and transitively `hooks/lib/template-loader.sh`. Key shared functions are `to_lower`, `is_round_file_type`, `find_active_loop`, `is_allowlisted_file`, `todos_blocked_message`, `is_in_humanize_loop_dir`, `parse_state_file`, `extract_round_number`, and `load_and_render_safe` (`hooks/lib/loop-common.sh:112-199`, `:415-419`). Block templates referenced directly are `block/wrong-file-location.md`, `block/wrong-round-file.md`, and `block/wrong-directory-path.md` (`hooks/loop-read-validator.sh:105`, `:122`, `:141`); todos blocking goes through `block/todos-file-access.md` (`hooks/lib/loop-common.sh:190-199`).
- edge_cases_or_failure_modes: If there is no active loop, summary/prompt reads are allowed (`hooks/loop-read-validator.sh:65-67`), but todos reads are blocked because `LOOP_DIR` is empty (`:36-43`). Empty or malformed `file_path` falls through as non-round content and is allowed. A malformed state file defaults `STATE_CURRENT_ROUND` to `0` via `parse_state_file` (`hooks/lib/loop-common.sh:133-138`), which can affect wrong-round decisions. Path matching is string-based: `is_in_humanize_loop_dir` only checks for the `.humanize/rlcr/` substring (`hooks/lib/loop-common.sh:415-419`), while the final directory gate requires exact equality with `$ACTIVE_LOOP_DIR/$filename` (`hooks/loop-read-validator.sh:135-146`), so symlinked or relative aliases to the active file can be blocked. Old loop directories are blocked by the final exact-path gate even when the round number matches. Allowlisted historical summaries/todos bypass only when the exact path is under the active loop.
- validation_or_tests: Template reference coverage includes this validator in `tests/test-template-references.sh:57-64` and verifies critical validators use `load_and_render_safe` rather than unsafe rendering (`:176-200`). Allowlist tests cover allowed reads for `round-1-todos.md` and `round-0-summary.md`, and blocked reads for non-allowlisted `round-3-todos.md` and `round-3-summary.md` (`tests/test-allowlist-validators.sh:232-281`). Finalize Phase parsing is checked by moving `state.md` to `finalize-state.md` and confirming the read validator allows the current round summary (`tests/test-finalize-phase.sh:639-650`). Hook-preservation testing confirms the Read hook fires and allows an ordinary plan-file read (`tests/test-hook-preservation.sh:446-463`). Tests were inspected, not executed in this read-only research pass.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-053 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that `finalize-state.md` is loop-system-owned during Finalize Phase. It tells the agent that the file cannot be modified and redirects work toward the valid finalize actions: run the code-simplifier agent, commit changes, and write `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1-8`).
- algorithmic_behavior: The template itself has no placeholders and no branching. It is loaded through `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:220-225`, which calls `load_and_render_safe` with an inline fallback. The loaded text is emitted to stderr when Write, Edit, or Bash validators detect attempted modification of `finalize-state.md`.
- inputs_outputs_state: Input is the template file resolved from `$TEMPLATE_DIR/block/finalize-state-file-modification.md`. Output is human-facing block guidance. It performs no state mutation, but protects the Finalize Phase state file from user/agent mutation so the loop controller remains the sole authority for finalize-state lifecycle transitions.
- gates_or_invariants: The protected invariant is “Claude must not modify `finalize-state.md`.” Write and Edit validators check `finalize-state.md` before generic `state.md` because the generic `state\.md` pattern also matches `finalize-state.md` (`hooks/loop-write-validator.sh:101-108`, `hooks/loop-edit-validator.sh:82-89`). Bash validation uses the same ordering for redirects, move/copy source detection, and shell-wrapper bypass checks (`hooks/loop-bash-validator.sh:86-104`, `:245-252`, `:268-276`). This preserves a specific Finalize Phase error path rather than falling through to the less-specific `state.md` block message.
- dependencies_and_callers: Direct loader is `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:220-225`. Callers include `hooks/loop-write-validator.sh:105-107`, `hooks/loop-edit-validator.sh:86-88`, and `hooks/loop-bash-validator.sh:98-104`, `:245-252`, `:270-276`. Rendering depends on `load_and_render_safe`, which substitutes placeholders if present and falls back if the template is missing or empty (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If the template is missing or empty, the common helper degrades to the inline fallback “You cannot modify finalize-state.md...” (`hooks/lib/loop-common.sh:220-225`), so the gate remains functional but loses the richer Finalize Phase instructions. Bash has an intentional exception: an authorized cancel move from `state.md` or `finalize-state.md` to `cancel-state.md` may pass when `.cancel-requested` exists and the command exactly matches the validated form (`hooks/lib/loop-common.sh:274-412`; Bash callers check this before blocking at `hooks/loop-bash-validator.sh:98-104`, `:245-252`, `:270-276`). Because this template has no variables, renderer injection or unresolved-placeholder behavior is not relevant here.
- validation_or_tests: Template reference validation scans `hooks/lib/loop-common.sh` along with validators and stop hooks (`tests/test-template-references.sh:57-64`) and extracts `load_template`/`load_and_render_safe` references to ensure files exist (`:87-110`). The Finalize Phase test suite verifies the shared `finalize_state_file_blocked_message` function is present (`tests/test-finalize-phase.sh:211-216`). Tests were inspected, not executed in this read-only research pass.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-083 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt fragment appended to the next Claude round after a Full Goal Alignment Check. It converts alignment-review findings into prioritized next-round work: forgotten items, unmet acceptance criteria, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2-7`).
- algorithmic_behavior: During stop-hook continuation after Codex finds issues, the stop hook increments `current_round`, writes the next round prompt, then conditionally appends this template when the round that just completed satisfies `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:1057-1086`). This means the prompt fragment is added to the round immediately after the periodic alignment check. The template is loaded with `load_template`; if non-empty, its contents are appended verbatim to `round-${NEXT_ROUND}-prompt.md` (`:1080-1085`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `NEXT_PROMPT_FILE`, `$TEMPLATE_DIR`, and the existing review-continuation context in the stop hook. Output is appended markdown in the next round prompt. The template does not itself mutate state, but it is part of the state transition from a completed reviewed round to the next actionable round: the hook first updates `state.md` to `NEXT_ROUND` (`hooks/loop-codex-stop-hook.sh:1057-1060`), creates the new prompt (`:1062-1078`), then adds these post-alignment priorities when applicable (`:1080-1085`).
- gates_or_invariants: This is a soft prompt gate, not a hard validator. Its invariant is process-oriented: after a Full Goal Alignment Check, the next Claude pass should not continue ordinary issue-fixing while ignoring alignment-specific findings. The trigger cadence is encoded as `CURRENT_ROUND % 5 == 4`, matching the inline comment “round after every 5th” (`hooks/loop-codex-stop-hook.sh:1080-1081`). The template emphasizes three review-contract categories: forgotten work, acceptance criteria marked not met, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:4-7`).
- dependencies_and_callers: Sole direct caller found is `hooks/loop-codex-stop-hook.sh:1082`. It depends on `load_template` from `hooks/lib/template-loader.sh:33-48`, which returns empty output when a template is missing. The surrounding next-prompt assembly also depends on `claude/next-round-prompt.md`, `claude/next-round-footer.md`, optional push notes, and goal-tracker update request templates (`hooks/loop-codex-stop-hook.sh:1066-1108`).
- edge_cases_or_failure_modes: If this template is missing or empty, the hook silently skips the post-alignment block because stderr is redirected and append happens only when `POST_ALIGNMENT` is non-empty (`hooks/loop-codex-stop-hook.sh:1082-1085`); unlike block templates, there is no inline fallback. If the modulo cadence is misaligned with the actual Full Goal Alignment Check cadence, the fragment would be appended after the wrong rounds or omitted after real alignment checks. The template contains no variables, so placeholder rendering is not involved.
- validation_or_tests: Template reference validation includes `hooks/loop-codex-stop-hook.sh` in the script scan (`tests/test-template-references.sh:57-64`) and verifies referenced template files exist by extracting `load_template`/`load_and_render_safe` calls (`:87-110`). No direct behavioral test was found for the modulo trigger or content-specific append behavior. Tests were inspected, not executed in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 section headings present; each assigned heading occurs once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`