# agent_29 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-029 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable test aggregator for the Humanize/RLCR plugin. It is the broad validation gate that serially runs the plugin’s shell test suites and converts their per-suite `Passed:` / `Failed:` summaries into one aggregate pass/fail exit code.
- algorithmic_behavior: The script initializes counters and an ordered `TEST_SUITES` array at `tests/run-all-tests.sh:28-54`, then iterates through each suite at `tests/run-all-tests.sh:61-120`. For each suite it resolves the path relative to `SCRIPT_DIR`, skips missing files rather than failing (`tests/run-all-tests.sh:64-67`), detects whether the suite must run under zsh (`tests/run-all-tests.sh:69-76`), executes the suite while temporarily disabling immediate exit (`tests/run-all-tests.sh:85-100`), strips ANSI color escapes, extracts the final numeric `Passed:` and `Failed:` counts, and accumulates totals (`tests/run-all-tests.sh:102-109`). A suite is marked failed if its process exits nonzero or reports any failed tests (`tests/run-all-tests.sh:111-118`). Final process state is derived solely from whether `FAILED_SUITES` is empty (`tests/run-all-tests.sh:129-140`).
- inputs_outputs_state: Inputs are executable test files under `tests/`, the host shell environment, optional `zsh`, and each child suite’s stdout/stderr summary format. Outputs are human-readable progress logs, aggregate totals, a list of failed suite names, and exit code `0` for all suites passing or `1` if any executed suite fails. Internal state is `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES`, all maintained in-memory during the run. Missing suites are skipped and do not affect totals or failure state.
- gates_or_invariants: The suite order is hard-coded and therefore defines the repository’s regression gate ordering (`tests/run-all-tests.sh:32-54`). The zsh-only invariant is represented by `ZSH_TESTS`, currently only `test-zsh-monitor-safety.sh` (`tests/run-all-tests.sh:56-59`). The runner assumes each child suite prints parseable `Passed:` and `Failed:` lines; if those are absent, counts default to zero because of `|| echo "0"` (`tests/run-all-tests.sh:105-106`). A nonzero child exit code is still a hard failure even if parsed failed count is zero (`tests/run-all-tests.sh:111`).
- dependencies_and_callers: Direct dependencies are Bash, optional zsh, `sed`, `grep`, `tail`, and every listed test executable. The included suites cover template loading, validator pattern detection, todo checking, plan-file validation, template references, state exit naming, comprehensive templates, plan-file hooks, error scenarios, ANSI parsing, allowlist validators, finalize phase, cancel signal files, humanize escape behavior, zsh monitor safety, monitor runtime/e2e behavior, skill structure, hook preservation, and skill invocation (`tests/run-all-tests.sh:33-54`). `tests/test-ansi-parsing.sh` explicitly validates the ANSI stripping and count extraction behavior used here, including BSD/macOS-safe `sed` handling.
- edge_cases_or_failure_modes: If zsh is missing, zsh-required tests are skipped rather than failing (`tests/run-all-tests.sh:87-95`), which can hide zsh regressions on hosts without zsh. Missing suite files are also skipped (`tests/run-all-tests.sh:64-67`), so accidental deletion only appears as a skip line. The runner strips only CSI color patterns matching `ESC[[0-9;]*m` (`tests/run-all-tests.sh:103-104`), so other ANSI sequences would remain. Because counts are extracted from the last matching lines (`tests/run-all-tests.sh:105-106`), noisy child output with later misleading `Passed:` / `Failed:` text could distort totals, though process exit status still guards hard failures.
- validation_or_tests: This file is itself the top-level validation script. It indirectly validates all listed test assets. Its parsing algorithm is specifically tested by `tests/test-ansi-parsing.sh`, which states it tests “the portable ANSI stripping used in run-all-tests.sh” and exercises `Passed:` / `Failed:` extraction. Related structural coverage comes from `tests/test-skill-invocation.sh`, `tests/test-skill-structure.sh`, `tests/test-hook-preservation.sh`, and template tests that the runner includes.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-059 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Fail-closed prompt block used when the stop hook cannot verify repository cleanliness because `git status --porcelain` failed or timed out. It defines the user-facing gate message for an unsafe repository-state transition.
- algorithmic_behavior: The template is static Markdown with one placeholder, `{{GIT_STATUS_EXIT}}`, at `prompt-template/block/git-status-failed.md:3`. It explains that git status failed, lists possible causes such as lock contention, invalid repository state, or slow repository operations (`prompt-template/block/git-status-failed.md:5-8`), and instructs manual verification (`prompt-template/block/git-status-failed.md:10`). The actual transition occurs in `hooks/loop-codex-stop-hook.sh:319-337`: the hook first confirms it is in a git repo, runs `git status --porcelain` with `run_with_timeout`, captures `GIT_STATUS_EXIT`, and if nonzero renders this template through `load_and_render_safe`, returning JSON with `"decision": "block"`.
- inputs_outputs_state: Input is the integer exit code stored in `GIT_STATUS_EXIT`. Output is rendered Markdown included in the stop-hook JSON `reason`, plus a `systemMessage` of the form `Loop: Blocked - git status failed (exit N)` from `hooks/loop-codex-stop-hook.sh:334-335`. It does not mutate repository state. It only influences hook control flow by making the exit attempt block when git state cannot be trusted.
- gates_or_invariants: The invariant is fail-closed repository verification. The stop hook comments explicitly state “Fail-closed on git failures to prevent bypassing checks” at `hooks/loop-codex-stop-hook.sh:314`, and the failure branch blocks exit rather than assuming a clean tree (`hooks/loop-codex-stop-hook.sh:325-337`). Template rendering uses the repository template loader’s single-pass substitution model, which keeps missing placeholders intact and prevents recursive placeholder injection (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-131`).
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` at `hooks/loop-codex-stop-hook.sh:332-333`. Rendering depends on `hooks/lib/template-loader.sh`, especially `load_and_render_safe` fallback behavior at `hooks/lib/template-loader.sh:170-203`. The template participates in the same block-template family as git cleanliness, untracked-file, unpushed-commit, and large-file guard messages under `prompt-template/block/`.
- edge_cases_or_failure_modes: If the template file is missing or empty, the hook has an inline fallback at `hooks/loop-codex-stop-hook.sh:327-331`, so the block still emits a reason. If `{{GIT_STATUS_EXIT}}` is not supplied, the loader would leave the placeholder literal rather than blanking it, due to missing-variable preservation in `hooks/lib/template-loader.sh:119-122`. The stop hook only enters this path after `git rev-parse --git-dir` succeeds; outside a git repo, this specific template is not used. Timeouts and non-timeout git failures are collapsed into the same user-facing transition, distinguished only by exit code.
- validation_or_tests: `tests/test-template-references.sh` scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and verifies the referenced files exist (`tests/test-template-references.sh:56-110`), which covers the stop-hook reference to this block. `tests/test-templates-comprehensive.sh` loads every `prompt-template/**/*.md` file and checks nonempty content (`tests/test-templates-comprehensive.sh:83-107`), then validates placeholder syntax (`tests/test-templates-comprehensive.sh:114-140`). The direct `rg` evidence shows this block is referenced only by `hooks/loop-codex-stop-hook.sh`.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-089 `file` `skills/start-rlcr-loop/SKILL.md`
- cursor: `[_]`
- core_role: Skill-level workflow entry point for starting the RLCR loop. It delegates execution to `scripts/setup-rlcr-loop.sh`, constrains allowed tool use to that setup script, and documents the iterative Codex-review state machine and Goal Tracker contract.
- algorithmic_behavior: Frontmatter declares the skill name, trigger description, model, and a single allowed Bash tool for `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`skills/start-rlcr-loop/SKILL.md:1-7`). The body instructs the agent to execute that script with `$ARGUMENTS` (`skills/start-rlcr-loop/SKILL.md:11-15`). It defines the loop sequence: implementation work, summary-file write, Codex review on exit, feedback continuation when issues exist, and termination when Codex outputs `COMPLETE` (`skills/start-rlcr-loop/SKILL.md:17-23`). It then adds the Goal Tracker drift-prevention contract: immutable ultimate goal and acceptance criteria, mutable active/completed/deferred/log sections, per-task AC mapping, plan-evolution logging, explicit deferrals, and periodic full alignment checks at rounds 4, 9, 14, and so on (`skills/start-rlcr-loop/SKILL.md:25-38`).
- inputs_outputs_state: Inputs are slash-command arguments passed through as `$ARGUMENTS`, normally including a relative plan file and optional flags. The skill output is whatever `scripts/setup-rlcr-loop.sh` creates and prints: a `.humanize/rlcr/<timestamp>/` loop directory, `state.md`, a copied `plan.md`, `goal-tracker.md`, `round-0-prompt.md`, and `round-0-summary.md` path guidance. The setup script initializes state fields such as `current_round: 0`, `max_iterations`, Codex model/effort/timeout, plan tracking mode, start branch, and start time at `scripts/setup-rlcr-loop.sh:485-498`. It creates the Goal Tracker structure at `scripts/setup-rlcr-loop.sh:504-594` and emits the initial prompt and completion requirements at `scripts/setup-rlcr-loop.sh:602-719`.
- gates_or_invariants: The skill’s main invariant is controlled setup through the script, not ad hoc loop creation. The command wrapper delegates to the skill rather than embedding setup logic (`commands/start-rlcr-loop.md:1-19`). The backing setup script enforces a git repository with at least one commit (`scripts/setup-rlcr-loop.sh:211-220`), relative non-space plan paths without shell metacharacters (`scripts/setup-rlcr-loop.sh:227-248`), no symlink plan file (`scripts/setup-rlcr-loop.sh:253-257`), plan file inside the project (`scripts/setup-rlcr-loop.sh:278-290`), no plan inside a submodule (`scripts/setup-rlcr-loop.sh:292-305`), plan tracking policy depending on `--track-plan-file` (`scripts/setup-rlcr-loop.sh:311-356`), at least five lines and at least three meaningful content lines (`scripts/setup-rlcr-loop.sh:362-415`), Codex CLI availability (`scripts/setup-rlcr-loop.sh:417-423`), YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:429-461`), and Round 0-only initialization of immutable Goal Tracker sections (`scripts/setup-rlcr-loop.sh:605-615`).
- dependencies_and_callers: The caller surface is `commands/start-rlcr-loop.md`, whose allowed tools contain `Skill(humanize:start-rlcr-loop:*)` and whose body delegates to this skill. Runtime dependencies include `${CLAUDE_PLUGIN_ROOT}`, Bash, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, git, Codex CLI, `date`, `cp`, `wc`, `grep`, and `sed`. The ongoing loop then depends on hooks such as `hooks/loop-codex-stop-hook.sh`, validator hooks, prompt templates, and Goal Tracker update templates. `tests/test-skill-invocation.sh` checks discovery, command-to-skill delegation, allowed-tools, setup script executability, and syntax around `tests/test-skill-invocation.sh:58-270`.
- edge_cases_or_failure_modes: The skill text says completion can happen when Codex confirms all ACs are met or validly deferred (`skills/start-rlcr-loop/SKILL.md:55`), but the Codex review templates are stricter: deferred items are treated as incomplete and should not produce `COMPLETE` unless all original tasks/ACs are met without deferrals. That is a semantic tension between skill-level stopping prose and review-template policy. Setup can fail before loop creation for missing git repo, no commits, unsafe paths, tracked/untracked plan mismatch, insufficient plan content, missing Codex CLI, unsafe branch names, or invalid model/effort strings. The skill command uses unquoted `$ARGUMENTS` in the displayed shell snippet (`skills/start-rlcr-loop/SKILL.md:14`), but the setup script rejects spaces and metacharacters in the plan path and validates model/effort values, reducing injection risk at the implementation layer.
- validation_or_tests: `tests/test-skill-invocation.sh` validates that the start skill has frontmatter, command delegation through the Skill tool, allowed-tools including the setup script, executable setup script, and valid Bash syntax (`tests/test-skill-invocation.sh:58-270`). `tests/test-skill-structure.sh` and `tests/test-hook-preservation.sh` also check skill/command structure and preservation of hook behavior after skillification. `tests/test-plan-file-validation.sh` exercises setup-script plan validation cases including absolute paths, missing files, spaces, symlinks, submodules, tracking policy, model argument validation, and insufficient content. `tests/run-all-tests.sh` includes all of these in the aggregate suite.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 evidence sections present for the 3 assigned rows
- missing_items: none
- duplicate_items: none detected in Item Evidence headings
- final_worker_status: `complete`