# agent_06 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-006 `directory` `skills`
- cursor: `[_]`
- core_role: Skillification surface for the RLCR loop commands. The directory contains exactly two recursive descendants: `skills/start-rlcr-loop/SKILL.md` and `skills/cancel-rlcr-loop/SKILL.md`. It moves the substantive command procedures out of thin slash-command wrappers and into Skill definitions while leaving hooks and scripts as the runtime enforcement layer.
- algorithmic_behavior: `start-rlcr-loop` declares a Skill with `name`, `description`, `model`, and allowed execution of `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` only; its body instructs the agent to execute that setup script with `$ARGUMENTS` and describes the iterative work-summary-review loop, Goal Tracker structure, alignment audits, and stop conditions (`skills/start-rlcr-loop/SKILL.md:1`, `skills/start-rlcr-loop/SKILL.md:11`, `skills/start-rlcr-loop/SKILL.md:25`, `skills/start-rlcr-loop/SKILL.md:52`). `cancel-rlcr-loop` declares a Skill for cancellation with allowed Bash/Read/AskUserQuestion operations, selects the newest `.humanize/rlcr/*/` directory, detects `state.md` versus `finalize-state.md`, writes `.cancel-requested`, and renames active state to `cancel-state.md` (`skills/cancel-rlcr-loop/SKILL.md:1`, `skills/cancel-rlcr-loop/SKILL.md:20`, `skills/cancel-rlcr-loop/SKILL.md:29`, `skills/cancel-rlcr-loop/SKILL.md:37`, `skills/cancel-rlcr-loop/SKILL.md:43`).
- inputs_outputs_state: Start input is the slash-command argument string passed through to `setup-rlcr-loop.sh`; the setup script validates a relative plan file, git repository state, optional `--track-plan-file`, Codex model/effort/timeout, and then creates `.humanize/rlcr/<timestamp>/state.md`, a protected `plan.md` backup, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:104`, `scripts/setup-rlcr-loop.sh:186`, `scripts/setup-rlcr-loop.sh:227`, `scripts/setup-rlcr-loop.sh:331`, `scripts/setup-rlcr-loop.sh:467`, `scripts/setup-rlcr-loop.sh:485`, `scripts/setup-rlcr-loop.sh:504`, `scripts/setup-rlcr-loop.sh:602`). Cancel input is the latest loop directory plus user confirmation during finalize; output is preserved cancellation state, not deletion (`skills/cancel-rlcr-loop/SKILL.md:51`, `skills/cancel-rlcr-loop/SKILL.md:58`).
- gates_or_invariants: Start forbids shortcuts and requires summaries plus `goal-tracker.md` maintenance (`skills/start-rlcr-loop/SKILL.md:44`). Setup rejects missing plans, absolute paths, spaces, shell metacharacters, symlinks, paths outside project root, submodule paths, dirty tracked plans, tracked plans without `--track-plan-file`, too-simple plans, YAML-unsafe branches, and unsafe Codex model/effort strings (`scripts/setup-rlcr-loop.sh:197`, `scripts/setup-rlcr-loop.sh:227`, `scripts/setup-rlcr-loop.sh:233`, `scripts/setup-rlcr-loop.sh:241`, `scripts/setup-rlcr-loop.sh:253`, `scripts/setup-rlcr-loop.sh:278`, `scripts/setup-rlcr-loop.sh:292`, `scripts/setup-rlcr-loop.sh:331`, `scripts/setup-rlcr-loop.sh:346`, `scripts/setup-rlcr-loop.sh:362`, `scripts/setup-rlcr-loop.sh:435`, `scripts/setup-rlcr-loop.sh:445`). Cancel’s invariant is that only newest timestamped loop can be active, and only `state.md` or `finalize-state.md` qualifies (`skills/cancel-rlcr-loop/SKILL.md:58`).
- dependencies_and_callers: `commands/start-rlcr-loop.md` and `commands/cancel-rlcr-loop.md` are thin wrappers that delegate through `Skill(humanize:...)` rather than carrying full behavior (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:10`, `commands/cancel-rlcr-loop.md:3`, `commands/cancel-rlcr-loop.md:9`). Runtime enforcement depends on `hooks/hooks.json`, the hook scripts, shared `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and the prompt templates. Tests `tests/test-skill-structure.sh` and `tests/test-skill-invocation.sh` assert names, descriptions, models, allowed tools, no `context: fork`, command-to-skill delegation, and wrapper thinness (`tests/test-skill-structure.sh:77`, `tests/test-skill-structure.sh:165`, `tests/test-skill-invocation.sh:95`, `tests/test-skill-invocation.sh:125`).
- edge_cases_or_failure_modes: No active loop returns a user-facing “No active RLCR loop found” path; a loop directory with no active state is treated as already ended or invalid (`skills/cancel-rlcr-loop/SKILL.md:27`, `skills/cancel-rlcr-loop/SKILL.md:35`). Finalize cancellation is deliberately interactive to avoid aborting the almost-finished phase accidentally (`skills/cancel-rlcr-loop/SKILL.md:43`). Start can fail before state creation on git timeout, path validation, plan tracking mismatch, unsafe branch/model fields, or missing Codex dependency (`scripts/setup-rlcr-loop.sh:211`, `scripts/setup-rlcr-loop.sh:311`, `scripts/setup-rlcr-loop.sh:419`).
- validation_or_tests: Recursive inspection found only the two Skill files. File modes show Skill markdown is non-executable while referenced scripts and tests are executable. Static syntax validation of `tests/test-hook-preservation.sh` passed with `bash -n`. Skill-specific coverage exists in `tests/test-skill-structure.sh` and `tests/test-skill-invocation.sh`; hook preservation coverage also checks that skills do not override `hooks.json` and that start skill references `setup-rlcr-loop.sh` (`tests/test-hook-preservation.sh:263`, `tests/test-hook-preservation.sh:281`, `tests/test-hook-preservation.sh:296`).
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-036 `file` `tests/test-hook-preservation.sh`
- cursor: `[_]`
- core_role: Executable specification proving the hook system still behaves identically after converting commands to Skills. It validates both static hook registration and live hook behavior against generated test RLCR state.
- algorithmic_behavior: The test initializes pass/fail counters under `set -euo pipefail`, resolves `PROJECT_ROOT`, `HOOKS_DIR`, and `HOOKS_JSON`, then runs 33 numbered checks (`tests/test-hook-preservation.sh:15`, `tests/test-hook-preservation.sh:17`, `tests/test-hook-preservation.sh:28`, `tests/test-hook-preservation.sh:53`, `tests/test-hook-preservation.sh:391`, `tests/test-hook-preservation.sh:614`). Static checks assert `hooks/hooks.json` exists, parses with `jq`, has `.hooks`, registers `UserPromptSubmit`, `PreToolUse`, `Stop`, includes Write/Edit/Read/Bash matchers, uses `${CLAUDE_PLUGIN_ROOT}` in every hook command, and gives Stop a positive timeout (`tests/test-hook-preservation.sh:56`, `tests/test-hook-preservation.sh:68`, `tests/test-hook-preservation.sh:79`, `tests/test-hook-preservation.sh:90`, `tests/test-hook-preservation.sh:102`, `tests/test-hook-preservation.sh:114`, `tests/test-hook-preservation.sh:126`, `tests/test-hook-preservation.sh:205`, `tests/test-hook-preservation.sh:219`). Script checks assert six hook scripts and two main scripts exist and are executable, hook scripts parse with `bash -n`, and shared hook libraries exist (`tests/test-hook-preservation.sh:176`, `tests/test-hook-preservation.sh:185`, `tests/test-hook-preservation.sh:231`, `tests/test-hook-preservation.sh:250`, `tests/test-hook-preservation.sh:314`).
- inputs_outputs_state: Inputs are repository files plus transient JSON hook payloads. `setup_test_loop` creates a temp git repo, writes a gitignored `plans/test-plan.md`, copies it to `.humanize/rlcr/2024-01-01_12-00-00/plan.md`, and writes state frontmatter with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, and `start_branch` (`tests/test-hook-preservation.sh:340`, `tests/test-hook-preservation.sh:344`, `tests/test-hook-preservation.sh:359`, `tests/test-hook-preservation.sh:363`, `tests/test-hook-preservation.sh:376`, `tests/test-hook-preservation.sh:380`). Outputs are colored PASS/FAIL lines and final exit 0 only when `TESTS_FAILED` is zero (`tests/test-hook-preservation.sh:32`, `tests/test-hook-preservation.sh:37`, `tests/test-hook-preservation.sh:607`, `tests/test-hook-preservation.sh:614`).
- gates_or_invariants: The hook topology under test is exactly `UserPromptSubmit -> loop-plan-file-validator.sh`, `PreToolUse` matchers for Write/Edit/Read/Bash, and `Stop -> loop-codex-stop-hook.sh` with timeout 7200 (`hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`). All hook commands must use `CLAUDE_PLUGIN_ROOT`, preserving plugin relocatability (`tests/test-hook-preservation.sh:201`). Skills must be separate from hooks and must not place `hooks.json` in `skills/` (`tests/test-hook-preservation.sh:263`). Commands must delegate through Skill tool and start skill must still reference the setup script (`tests/test-hook-preservation.sh:281`, `tests/test-hook-preservation.sh:296`).
- dependencies_and_callers: Depends on `jq`, `git`, `bash`, `mktemp`, hook scripts, `hooks/hooks.json`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `commands/start-rlcr-loop.md`, `skills/start-rlcr-loop/SKILL.md`, `scripts/setup-rlcr-loop.sh`, and `scripts/humanize.sh`. The hook scripts themselves depend on shared loop parsing and template rendering; `find_active_loop` chooses the newest active loop directory and supports normal/finalize state (`hooks/lib/loop-common.sh:58`, `hooks/lib/loop-common.sh:112`).
- edge_cases_or_failure_modes: The test intentionally accepts two valid Stop-hook outcomes: exit 0 with no output, or valid JSON with `decision` equal to block/allow (`tests/test-hook-preservation.sh:514`). It verifies branch switching blocks with JSON `decision: block` and a reason mentioning branch (`tests/test-hook-preservation.sh:570`, `tests/test-hook-preservation.sh:589`). It asserts Write/Edit/Bash block the protected loop `plan.md` backup but Read allows the original plan file (`tests/test-hook-preservation.sh:414`, `tests/test-hook-preservation.sh:433`, `tests/test-hook-preservation.sh:451`, `tests/test-hook-preservation.sh:470`). Safe non-plan writes and safe Bash commands must pass (`tests/test-hook-preservation.sh:534`, `tests/test-hook-preservation.sh:552`).
- validation_or_tests: Static syntax validation of this file passed with `bash -n tests/test-hook-preservation.sh`. I did not run the full test because this branch export is read-only and the full script creates temp git state, commits, and executes repository hooks; additionally `git status --short` in this environment failed before repository discovery due sandboxed Xcode/git cache writes and then “not a git repository.” The file itself is executable (`-rwxr-xr-x`) and directly encodes its own pass/fail summary and exit gate (`tests/test-hook-preservation.sh:614`).
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-066 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Block-message template for the plan integrity gate. It defines the user-facing contract when the original plan file no longer matches the loop’s backup.
- algorithmic_behavior: The template renders `{{PLAN_FILE}}` and `{{BACKUP_PATH}}`, states that plan files cannot be modified during an active RLCR loop, and gives the required recovery transition: cancel current loop, update plan file, start a new loop with the same plan path (`prompt-template/block/plan-file-modified.md:1`, `prompt-template/block/plan-file-modified.md:3`, `prompt-template/block/plan-file-modified.md:5`, `prompt-template/block/plan-file-modified.md:7`, `prompt-template/block/plan-file-modified.md:12`).
- inputs_outputs_state: Inputs are template variables supplied by `loop-codex-stop-hook.sh`: `PLAN_FILE=$PLAN_FILE` and `BACKUP_PATH=$BACKUP_PLAN` (`hooks/loop-codex-stop-hook.sh:244`). Output is Markdown embedded as the JSON `reason` for a Stop-hook block decision (`hooks/loop-codex-stop-hook.sh:247`).
- gates_or_invariants: The invariant is strict plan immutability after loop start. The stop hook first requires backup existence, then requires the project plan file to exist, then uses `diff -q "$FULL_PLAN_PATH" "$BACKUP_PLAN"` to detect content drift for both tracked and gitignored plans (`hooks/loop-codex-stop-hook.sh:182`, `hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:198`, `hooks/loop-codex-stop-hook.sh:230`). Tracked plans also fail earlier on uncommitted git status (`hooks/loop-codex-stop-hook.sh:214`).
- dependencies_and_callers: Loaded through `load_and_render_safe`, whose renderer performs single-pass `{{VAR}}` substitution and falls back if a template is missing or empty (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:167`). The same template is referenced by search only in the stop-hook plan diff block and in template-reference tests; related setup code creates the backup copy at loop start (`scripts/setup-rlcr-loop.sh:475`).
- edge_cases_or_failure_modes: If the template is missing, the stop hook has an inline fallback with the same essential message and transition (`hooks/loop-codex-stop-hook.sh:232`). If backup is missing, deletion occurs, tracked status is dirty, or git checks fail, other block paths fire before this template (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:198`, `hooks/loop-codex-stop-hook.sh:214`). Template variables are single-pass, so placeholder-like text inside values is not recursively expanded (`hooks/lib/template-loader.sh:54`).
- validation_or_tests: Covered indirectly by hook preservation’s Stop-hook schema test and by broader plan-file hook tests that check Stop-hook blocking on modified, deleted, and missing backup cases (`tests/test-hook-preservation.sh:483`, `tests/test-plan-file-hooks.sh:466`, `tests/test-plan-file-hooks.sh:469`, `tests/test-plan-file-hooks.sh:509`, `tests/test-plan-file-hooks.sh:543`). Template reference tests also include this template family through hook/template scanning.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`