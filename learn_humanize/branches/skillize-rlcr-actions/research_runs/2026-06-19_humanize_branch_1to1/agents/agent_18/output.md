# agent_18 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-018 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude Code hook manifest for the RLCR loop guard surface. It does not implement guard logic itself; it routes lifecycle events to the validator and Stop-hook scripts that enforce the loop state machine.
- algorithmic_behavior: [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/hooks.json:4) maps `UserPromptSubmit` to `loop-plan-file-validator.sh`, `PreToolUse` matchers `Write`, `Edit`, `Read`, and `Bash` to their respective validators, and `Stop` to `loop-codex-stop-hook.sh`. The Stop hook gets a long `7200` second timeout at lines 52-59, which allows Codex review orchestration to run after Claude attempts to stop.
- inputs_outputs_state: Input is Claude Code hook dispatch metadata and tool/stop events. Output is command execution through `${CLAUDE_PLUGIN_ROOT}/hooks/...`; the called scripts decide whether to pass, block with exit code or JSON, or continue the RLCR loop. State transitions are indirect: prompt submission can validate plan-file invariants, tool use can be blocked before mutation, and stop attempts can be allowed or converted into another loop iteration.
- gates_or_invariants: The manifest enforces event coverage at the boundary: prompt validation before user prompt acceptance, mutation/read command validation before tool execution, and final completion review before stop. Every command path is plugin-root-relative, so the installed plugin can move without hardcoded absolute paths.
- dependencies_and_callers: Called by the Claude Code hook engine. Depends on six scripts registered in lines 9, 20, 29, 38, 47, and 57. Those scripts in turn use shared helpers such as `hooks/lib/loop-common.sh` and templates under `prompt-template/`.
- edge_cases_or_failure_modes: If `${CLAUDE_PLUGIN_ROOT}` is unset or points at the wrong plugin root, every registered command path fails. If any script is missing or not executable, the corresponding guard surface silently loses enforcement at runtime. The manifest only matches `Write`, `Edit`, `Read`, and `Bash`; other tool names would need explicit registration if they can mutate or bypass loop constraints.
- validation_or_tests: [tests/test-hook-preservation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-hook-preservation.sh:64) validates that `hooks.json` is valid JSON, contains `hooks`, configures `UserPromptSubmit`, `PreToolUse`, and `Stop`, includes the expected matchers, uses `CLAUDE_PLUGIN_ROOT` for all commands, and has a positive Stop-hook timeout. Lines 176-223 also check all referenced hook scripts exist and are executable.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-048 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the todo-completion gate used before expensive RLCR Stop-hook review. It defines when Claude may proceed toward review versus when the loop blocks because native TodoWrite state is unfinished.
- algorithmic_behavior: [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-todo-checker.sh:51) creates JSON/JSONL fixtures in a temp directory, pipes hook JSON into the Python checker, captures exit codes, and increments pass/fail counters. It verifies invalid hook JSON and empty stdin return exit `2`; missing `transcript_path` and nonexistent transcript files return exit `0`; completed todos return exit `0`; `pending`, `in_progress`, missing-status, and empty-content todos return exit `1`.
- inputs_outputs_state: Inputs are synthetic Claude transcript JSONL files and hook input JSON carrying `transcript_path`. Outputs are test PASS/FAIL lines and final process exit `0` only when all assertions pass. The checker’s contract, confirmed by [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:7), is exit `0` for all-completed or no todos, exit `1` for incomplete todos with an `INCOMPLETE_TODOS` marker, and exit `2` for hook-input parse errors.
- gates_or_invariants: The key invariant is “latest TodoWrite wins.” Lines 196-210 create two TodoWrite calls and expect the second completed list to supersede the earlier pending list. Lines 212-240 specify accepted transcript shapes: nested Claude assistant messages, direct `tool_use` entries, and `type: message` entries. Lines 179-194 assert malformed JSONL transcript lines are ignored rather than failing the gate.
- dependencies_and_callers: Direct dependency is `python3 "$TODO_CHECKER"` at line 54 and throughout. The production caller is [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:258), which runs the checker before Codex review. Stop-hook exit handling blocks on parse error at lines 265-281 and blocks immediately on incomplete todos at lines 284-305 using `prompt-template/block/incomplete-todos.md`.
- edge_cases_or_failure_modes: Nonexistent transcript files and no todos are fail-open by design, so missing transcript metadata will not block Stop review. Invalid hook JSON is fail-closed with exit `2`. The Python checker only updates `latest_todos` when the TodoWrite list is non-empty, so an empty latest TodoWrite would not overwrite an earlier non-empty list; this behavior is visible in `if todos:` checks in [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:62) but is not directly covered by this test. The “Unicode” test at lines 280-293 uses ASCII text, so true non-ASCII content is not actually exercised.
- validation_or_tests: The script is itself the validation surface and ends with an aggregate gate: if `TESTS_FAILED` is zero it prints “All tests passed!” and exits `0`; otherwise it exits `1` at lines 295-313. It covers 16 scenarios across input parsing, todo-state interpretation, transcript format variations, and edge cases.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-078 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: User-facing block template for the summary-file location invariant. It is not executable code, but it defines the exact contract message emitted when an RLCR actor tries to write a round summary outside the active loop directory.
- algorithmic_behavior: [prompt-template/block/wrong-summary-location.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/block/wrong-summary-location.md:1) renders a short Markdown error headed “Wrong Summary Location” and includes `{{CORRECT_PATH}}`. It is consumed by [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-write-validator.sh:155), which detects `round-N-summary.md` writes outside `.humanize/rlcr`, renders this template with the active loop’s `round-${CURRENT_ROUND}-summary.md`, prints to stderr, and exits `2`.
- inputs_outputs_state: Input is the `CORRECT_PATH` variable supplied by the write validator. Output is a rendered Markdown block telling the agent where the summary must go. No persistent state is mutated by the template; the state transition is in the validator: attempted invalid summary write becomes blocked tool use.
- gates_or_invariants: The invariant is that summary files must live in the active loop directory and correspond to the current round. The surrounding validator first finds the active loop and current round at lines 79-99, protects state/finalize-state/plan backup files, allows the valid finalize summary path at lines 121-125, then blocks wrong summary location before doing deeper directory/round checks.
- dependencies_and_callers: Depends on `load_and_render_safe` from [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:170). That renderer uses single-pass placeholder substitution and falls back to an inline message if the template is missing or renders empty. The active-loop and round-parsing context comes from `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: If the template is missing, the validator still emits its fallback text, so enforcement survives missing template files. If `CORRECT_PATH` is not supplied, the placeholder remains visible because missing variables are intentionally preserved. Bash-based summary writes are not handled by this template; they are blocked by the Bash validator through a separate summary-bash template. Exact path comparison later in the write validator means path normalization or symlink aliases are not resolved by this template layer.
- validation_or_tests: [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-references.sh:52) scans hook scripts for template references and fails if a referenced template is missing. Lines 172-200 additionally require critical validators to use `load_and_render_safe`, which covers this template’s fail-safe rendering path. `rg` also shows the template is directly referenced only by `loop-write-validator.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 required item sections present; each assigned section heading used once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`