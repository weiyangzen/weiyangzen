# agent_08 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: Public behavior contract for the RLCR workflow. It defines Humanize as an iterative Claude implementation plus independent Codex review loop, with goal tracking, review feedback, audit trail, and termination once acceptance criteria are met or iteration limits are reached. Key contract lines: `README.md:7-13` introduces RLCR, `README.md:15-27` states the iteration philosophy and audit/quality goals, and `README.md:58-66` gives the workflow state diagram.
- algorithmic_behavior: The documented state machine is: user supplies a plan file, Claude implements and summarizes, Codex reviews independently, Codex feedback loops back into Claude, and the loop reaches Done when Codex reports completion or the max iteration limit is reached (`README.md:60-66`). Quick Start exposes the primary transition trigger as `/humanize:start-rlcr-loop docs/my-feature-plan.md` (`README.md:70-75`) and cancellation as `/humanize:cancel-rlcr-loop` (`README.md:76`). The lower-level setup script confirms the same flow: plan input, Codex review on exit attempt, feedback block on issues, completion when Codex outputs `COMPLETE`, or stop at max iterations (`scripts/setup-rlcr-loop.sh:65-79`, `scripts/setup-rlcr-loop.sh:86-89`).
- inputs_outputs_state: Inputs are a markdown plan file with clear description, acceptance criteria, and technical approach (`README.md:70`), plus options for max iterations, Codex model/effort, Codex timeout, and push-every-round (`README.md:87-98`). The setup implementation adds stricter input constraints: relative plan path only, no spaces, no shell metacharacters, existing git repo with at least one commit (`scripts/setup-rlcr-loop.sh:197-248`). Outputs are an RLCR session directory under `.humanize/rlcr/<timestamp>/` for monitoring (`README.md:75`), with `state.md`, a copied `plan.md`, `goal-tracker.md`, and round prompt/summary/review artifacts generated by setup (`scripts/setup-rlcr-loop.sh:467-507`, `scripts/setup-rlcr-loop.sh:648-653`). The generated state schema includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` (`scripts/setup-rlcr-loop.sh:485-498`).
- gates_or_invariants: Prerequisite gate: Codex CLI must be available (`README.md:52-55`). Workflow gate: Codex review must either feed issues back into another implementation round or produce completion/max-iteration termination (`README.md:60-66`). Plan quality gate is implied by “clear description, acceptance criteria, and technical approach” (`README.md:70`) and enforced more strictly by setup path validation and git validation (`scripts/setup-rlcr-loop.sh:211-248`). State invariant: the active session lives under `.humanize/rlcr/<timestamp>/`, which monitor and hooks treat as the loop root (`README.md:75`, `hooks/lib/loop-common.sh:58-81`).
- dependencies_and_callers: README points users at Claude Code plugin commands (`README.md:78-84`). The command surface delegates through `commands/start-rlcr-loop.md` and `commands/cancel-rlcr-loop.md`, while `skills/start-rlcr-loop/SKILL.md` invokes `scripts/setup-rlcr-loop.sh`. Runtime monitoring is implemented by `scripts/humanize.sh`, whose CLI exposes `humanize monitor rlcr-loop` (`scripts/humanize.sh:805-820`). Hooks under `hooks/` consume the generated `.humanize/rlcr` state, especially `hooks/loop-codex-stop-hook.sh` for Codex review and `hooks/loop-plan-file-validator.sh` for schema/branch/plan gates.
- edge_cases_or_failure_modes: README is intentionally high-level and omits several executable constraints: plan paths cannot be absolute, contain spaces, or contain shell metacharacters (`scripts/setup-rlcr-loop.sh:227-248`); the loop requires a git repo and a first commit (`scripts/setup-rlcr-loop.sh:211-220`); state schema freshness is enforced by hooks via `plan_tracked` and `start_branch` (`hooks/loop-plan-file-validator.sh:76-86`). There is a documentation inconsistency in command spelling: Quick Start uses `/humanize:start-rlcr-loop` (`README.md:73`), while the command table lists `/start-rlcr-loop` and `/cancel-rlcr-loop` without the namespace (`README.md:80-83`). The README does not document monitor behavior when `.humanize/rlcr` is deleted, which is specified by assigned test `tests/test-monitor-e2e-real.sh`.
- validation_or_tests: This file is documentation, so it is not directly executable. Its behavioral claims are covered indirectly by setup/help text (`scripts/setup-rlcr-loop.sh:43-100`), skill/command delegation tests in `tests/test-skill-invocation.sh`, plan validation tests in `tests/test-plan-file-validation.sh`, and monitor behavior tests including `tests/test-monitor-e2e-real.sh`. I did not run tests in this read-only research pass.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-038 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for monitor lifecycle behavior when the active `.humanize/rlcr` directory is removed. It validates the real `_humanize_monitor_codex` function rather than a stub (`tests/test-monitor-e2e-real.sh:3-6`) against acceptance criteria AC-1.1 through AC-1.4 (`tests/test-monitor-e2e-real.sh:8-12`).
- algorithmic_behavior: The test creates an isolated temp project and loop session, launches the real monitor in the background, deletes `.humanize/rlcr`, waits for monitor termination, and asserts graceful cleanup and shell compatibility. Bash path: create state and goal tracker (`tests/test-monitor-e2e-real.sh:64-91`), create matching cache log in fake HOME (`tests/test-monitor-e2e-real.sh:93-101`), generate a runner that sources `scripts/humanize.sh` and calls `_humanize_monitor_codex` (`tests/test-monitor-e2e-real.sh:103-149`), run it in the background (`tests/test-monitor-e2e-real.sh:153-156`), delete `.humanize/rlcr` (`tests/test-monitor-e2e-real.sh:158-162`), then wait up to 20 half-second polls (`tests/test-monitor-e2e-real.sh:164-179`). Zsh repeats the same real-function path if `zsh` exists (`tests/test-monitor-e2e-real.sh:233-359`).
- inputs_outputs_state: Inputs are synthetic but realistic loop artifacts: `state.md` with `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` (`tests/test-monitor-e2e-real.sh:69-76`, `tests/test-monitor-e2e-real.sh:241-248`); `goal-tracker.md` with immutable/mutable sections and acceptance criteria (`tests/test-monitor-e2e-real.sh:78-91`, `tests/test-monitor-e2e-real.sh:250-263`); and cached `round-1-codex-run.log` under `$HOME/.cache/humanize/<sanitized-project>/<timestamp>/` (`tests/test-monitor-e2e-real.sh:98-101`, `tests/test-monitor-e2e-real.sh:269-273`). Outputs are captured monitor text files, pass/fail counters, and process exit codes. Success requires `EXIT_CODE:0` in both bash and zsh paths (`tests/test-monitor-e2e-real.sh:219-224`, `tests/test-monitor-e2e-real.sh:354-358`).
- gates_or_invariants: Main gates: monitor process must exit after loop directory deletion within the bounded wait (`tests/test-monitor-e2e-real.sh:164-179`, `tests/test-monitor-e2e-real.sh:323-337`); output must include “Monitoring stopped:” and the deletion reason (`tests/test-monitor-e2e-real.sh:184-195`, `tests/test-monitor-e2e-real.sh:341-346`); output must not include zsh/bash glob errors such as “no matches found” or “bad pattern” (`tests/test-monitor-e2e-real.sh:197-202`, `tests/test-monitor-e2e-real.sh:348-352`); cleanup must display “Stopped monitoring” (`tests/test-monitor-e2e-real.sh:204-210`); and source must contain the scroll-region reset escape `printf "\033[r"` (`tests/test-monitor-e2e-real.sh:212-217`). The implementation supports these gates by using `find` rather than shell globs for sessions/logs (`scripts/humanize.sh:27-49`, `scripts/humanize.sh:51-109`), resetting terminal scroll region in `_restore_terminal` (`scripts/humanize.sh:465-471`), and routing deletion through `_graceful_stop` (`scripts/humanize.sh:499-512`).
- dependencies_and_callers: Direct dependency is `scripts/humanize.sh`; the generated runner sources it and calls `_humanize_monitor_codex` (`tests/test-monitor-e2e-real.sh:141-145`, `tests/test-monitor-e2e-real.sh:300-305`). The test also depends on shell utilities `bash`, optionally `zsh`, `sed`, `grep`, `kill`, `sleep`, `pkill`, `mkdir`, `rm`, and temp filesystem access under `/tmp` (`tests/test-monitor-e2e-real.sh:48-56`). It shims `tput` and `clear` to make terminal operations deterministic and noninteractive (`tests/test-monitor-e2e-real.sh:119-139`, `tests/test-monitor-e2e-real.sh:289-298`). Related monitor implementation includes initial missing-directory failure (`scripts/humanize.sh:20-25`), active-loop directory checks in the main monitor loop (`scripts/humanize.sh:547-553`, `scripts/humanize.sh:584-593`, `scripts/humanize.sh:677-686`), and the CLI wrapper (`scripts/humanize.sh:810-820`).
- edge_cases_or_failure_modes: The test intentionally uses minimal `state.md`; it omits newer setup fields such as `plan_tracked`, `start_branch`, `codex_timeout`, and `push_every_round`, which is valid for monitor parsing because `_parse_state_md` only reads display fields (`scripts/humanize.sh:152-168`). The bash runner accepts an `OUTPUT_FILE` argument but does not use it internally (`tests/test-monitor-e2e-real.sh:109-113`), because redirection is handled by the parent launch (`tests/test-monitor-e2e-real.sh:155`). The zsh path is skipped, not failed, when `zsh` is unavailable (`tests/test-monitor-e2e-real.sh:233-235`). Timing is polling-based: the monitor gets a fixed two-second startup window before deletion (`tests/test-monitor-e2e-real.sh:158-162`, `tests/test-monitor-e2e-real.sh:317-321`), so very slow startup could cause noisy failure. The test validates deletion of the whole `.humanize/rlcr` base, but not all monitor branches, such as switching to a newer session when only one session directory is deleted (`scripts/humanize.sh:620-648`) or handling log truncation/rotation (`scripts/humanize.sh:704-715`).
- validation_or_tests: This file is itself the real e2e validator and reports AC-1.1 through AC-1.4 as verified only when no failures occur (`tests/test-monitor-e2e-real.sh:371-384`). Related narrower tests cover graceful-stop cleanup and deletion detection in `tests/test-monitor-runtime.sh:94-134` and `tests/test-monitor-runtime.sh:180-235`, plus zsh no-glob safety in `tests/test-zsh-monitor-safety.sh:58-160`. I inspected but did not execute the test in this read-only research pass.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-068 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Prompt/block template for the outdated RLCR state-schema gate. It supplies the user-facing block reason when an active loop’s state file is missing a required schema field (`prompt-template/block/schema-outdated.md:1-4`), specifically for state files created before the `plan_tracked` and `start_branch` fields became required.
- algorithmic_behavior: The template has one substitution input, `{{FIELD_NAME}}`, embedded in the missing-field message (`prompt-template/block/schema-outdated.md:3`). `hooks/loop-plan-file-validator.sh` renders it through `load_and_render_safe` with `FIELD_NAME=$field_name` (`hooks/loop-plan-file-validator.sh:56-63`), JSON-escapes the rendered Markdown with `jq -Rs`, and returns a hook response with `"decision": "block"` (`hooks/loop-plan-file-validator.sh:64-73`). The loop over required fields checks `plan_tracked` and `start_branch`; the first empty value triggers this schema block and exits the hook successfully with a block decision (`hooks/loop-plan-file-validator.sh:76-86`).
- inputs_outputs_state: Inputs are the active loop state file selected from `.humanize/rlcr`, parsed via `parse_state_file` (`hooks/loop-plan-file-validator.sh:29-45`), and the field name selected from `FIELD_PLAN_TRACKED` or `FIELD_START_BRANCH` (`hooks/loop-plan-file-validator.sh:76-83`). Field constants live in `hooks/lib/loop-common.sh:15-24`, and `parse_state_file` extracts frontmatter values into `STATE_PLAN_TRACKED` and `STATE_START_BRANCH` (`hooks/lib/loop-common.sh:119-131`). Output is a rendered Markdown reason with options to cancel, update Humanize to version `1.1.2+`, and restart the loop (`prompt-template/block/schema-outdated.md:5-10`), wrapped in JSON for the hook consumer (`hooks/loop-plan-file-validator.sh:68-73`). Normal setup creates both required fields in new state files (`scripts/setup-rlcr-loop.sh:485-496`), so this template is mainly a migration/compatibility gate.
- gates_or_invariants: The invariant is that an active RLCR loop state must include non-empty `plan_tracked` and `start_branch` values before plan-file prompt validation continues (`hooks/loop-plan-file-validator.sh:76-86`). Template rendering is deliberately resilient: `load_and_render_safe` uses a fallback if the template is missing or renders empty (`hooks/lib/template-loader.sh:170-203`). Rendering is single-pass, so substituted values are not rescanned as template syntax, and undefined placeholders remain literal (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:111-128`). Template directory validation warns rather than hard-fails, preserving fallback behavior (`hooks/lib/loop-common.sh:50-56`).
- dependencies_and_callers: Direct caller is `hooks/loop-plan-file-validator.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md"` (`hooks/loop-plan-file-validator.sh:56-63`). Indirect dependencies are `hooks/lib/template-loader.sh` for rendering and fallback behavior, `hooks/lib/loop-common.sh` for field constants/state parsing, and `scripts/setup-rlcr-loop.sh` for writing the modern schema. Related but not direct: `hooks/loop-codex-stop-hook.sh` performs a similar outdated-schema block with an inline plural message rather than this template (`hooks/loop-codex-stop-hook.sh:126-142`).
- edge_cases_or_failure_modes: If the template file is missing, the validator falls back to an inline message with the same field placeholder (`hooks/loop-plan-file-validator.sh:56-63`, `hooks/lib/template-loader.sh:176-186`). If `FIELD_NAME` were not passed, the placeholder would remain as written because missing variables are preserved by the renderer (`hooks/lib/template-loader.sh:120-122`). There is a contract mismatch risk: the template says the loop “will be terminated as ‘unexpected’” (`prompt-template/block/schema-outdated.md:12`), but the direct caller returns a JSON block decision and exits without calling the loop-ending path (`hooks/loop-plan-file-validator.sh:68-85`). The stop hook’s related schema check also returns a block JSON response and does not call its `end_loop` helper in that branch (`hooks/loop-codex-stop-hook.sh:131-142`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` verifies the plan-file hook blocks when `plan_tracked` is missing and when `start_branch` is missing (`tests/test-plan-file-hooks.sh:121-159`). The same test file verifies the stop hook returns a JSON block for an outdated schema (`tests/test-plan-file-hooks.sh:631-650`), though that path uses the inline stop-hook message rather than this template. Template existence and safe rendering behavior are covered by `tests/test-template-references.sh:83-111` and fallback/render tests in `tests/test-templates-comprehensive.sh:445-463`. I inspected but did not run these tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`