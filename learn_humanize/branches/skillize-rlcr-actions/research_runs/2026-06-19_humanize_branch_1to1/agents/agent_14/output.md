# agent_14 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-014 `directory` `skills/start-rlcr-loop`
- cursor: `[_]`
- core_role: Skill entrypoint for starting the RLCR loop. The directory recursively contains one file, `skills/start-rlcr-loop/SKILL.md`; it is not the implementation itself, but it is the command-facing dispatcher that authorizes and invokes the setup algorithm in `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: `SKILL.md` declares `name: start-rlcr-loop`, describes “Start iterative development loop with Codex review,” selects `claude-opus-4-5-20250514`, and allows only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` in frontmatter at `skills/start-rlcr-loop/SKILL.md:1-7`. Its executable instruction is a single script call with `$ARGUMENTS` at `skills/start-rlcr-loop/SKILL.md:11-15`. The skill documents the loop cycle: implement plan, write summary, Codex reviews on exit, continue on issues, end on `COMPLETE` at `skills/start-rlcr-loop/SKILL.md:17-24`.
- inputs_outputs_state: Input is the slash-command argument string passed through `$ARGUMENTS`; the wrapper delegates all parsing to `setup-rlcr-loop.sh`. The setup script accepts a relative markdown plan plus options such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, and `--push-every-round` at `scripts/setup-rlcr-loop.sh:43-180`. Outputs are `.humanize/rlcr/<timestamp>/state.md`, a copied `plan.md`, `goal-tracker.md`, and `round-0-prompt.md`, created at `scripts/setup-rlcr-loop.sh:467-653`, followed by an activation message and initial prompt at `scripts/setup-rlcr-loop.sh:667-720`.
- gates_or_invariants: The skill-level gate is narrow tool permission: only the setup script may run. Implementation gates in the delegated script include git repo and HEAD existence checks at `scripts/setup-rlcr-loop.sh:211-221`, relative/no-space/no-metacharacter plan paths at `scripts/setup-rlcr-loop.sh:227-248`, no symlink and in-project resolution at `scripts/setup-rlcr-loop.sh:253-290`, submodule exclusion at `scripts/setup-rlcr-loop.sh:292-305`, plan tracking/cleanliness policy at `scripts/setup-rlcr-loop.sh:311-356`, minimum plan size/content at `scripts/setup-rlcr-loop.sh:362-415`, Codex CLI availability at `scripts/setup-rlcr-loop.sh:417-423`, and YAML-safe branch/model/effort fields at `scripts/setup-rlcr-loop.sh:429-461`.
- dependencies_and_callers: `commands/start-rlcr-loop.md` delegates to `Skill(humanize:start-rlcr-loop:*)` at `commands/start-rlcr-loop.md:1-17`. The setup script depends on `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:26-29`, git, Codex CLI, and `CLAUDE_PROJECT_DIR` fallback to `pwd` at `scripts/setup-rlcr-loop.sh:186`. Structural tests assert the skill name at `tests/test-skill-structure.sh:77-91`, command delegation at `tests/test-skill-structure.sh:165-178`, and preservation of the setup-script reference at `tests/test-hook-preservation.sh:280-306`.
- edge_cases_or_failure_modes: Fails closed for missing/multiple plan files, absolute paths, spaces, shell metacharacters, symlinks, missing parent/file/readability, escaping outside project, submodule paths, git timeouts, tracked plan without `--track-plan-file`, untracked plan with `--track-plan-file`, insufficient content, missing Codex, unsafe branch/model/effort values. Timestamp collision is theoretically possible because `LOOP_DIR` uses second precision at `scripts/setup-rlcr-loop.sh:469-473`; the script does not explicitly check for an existing same-second loop directory.
- validation_or_tests: Directory-level behavior is covered by skill structure and hook-preservation tests above. The actual setup algorithm has many implied validation surfaces through `setup-rlcr-loop.sh`; this assigned directory is a dispatcher plus operator contract, so most behavioral validation is necessarily against the delegated script and hooks rather than the one-line skill invocation.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-044 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR state-file lifecycle naming. It verifies that active loops are represented only by `state.md` in this test’s normal-loop scope, and terminal states are preserved as reason-prefixed `*-state.md` files rather than being mistaken for live sessions.
- algorithmic_behavior: The test creates a temp git repo, sources `hooks/lib/loop-common.sh`, then exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` directly at `tests/test-state-exit-naming.sh:31-67`. It checks inactive terminal names `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` at `tests/test-state-exit-naming.sh:51-155`; positive active `state.md` detection at `tests/test-state-exit-naming.sh:74-91`; newest-directory precedence at `tests/test-state-exit-naming.sh:157-176`; valid and invalid `end_loop` reasons at `tests/test-state-exit-naming.sh:182-239`; and new-vs-legacy path detection at `tests/test-state-exit-naming.sh:245-287`.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/...` files and helper function calls. Outputs are PASS/FAIL counters and process exit equal to `TESTS_FAILED` at `tests/test-state-exit-naming.sh:289-298`. State transitions under test are `state.md -> <reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` via `end_loop`.
- gates_or_invariants: `find_active_loop` must ignore terminal files and return only the newest directory containing an active indicator; production code also treats `finalize-state.md` as active at `hooks/lib/loop-common.sh:58-81`. `end_loop` accepts only five exact reasons, otherwise returns nonzero with “Invalid end_loop reason” at `hooks/lib/loop-common.sh:615-648`. Missing state file must return nonzero with a warning. Path recognition must match `.humanize/rlcr/` and intentionally not match legacy `.humanize-loop.local` at `hooks/lib/loop-common.sh:415-419`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, which itself sources `hooks/lib/template-loader.sh` at `hooks/lib/loop-common.sh:46-55`. Production callers include `hooks/loop-codex-stop-hook.sh`, which finds active loops at `hooks/loop-codex-stop-hook.sh:59-82`, marks invalid state as `unexpected` at `hooks/loop-codex-stop-hook.sh:102-119`, marks max-iteration exits at `hooks/loop-codex-stop-hook.sh:636-642`, enters finalize on `COMPLETE` at `hooks/loop-codex-stop-hook.sh:961-1021`, and marks `STOP` at `hooks/loop-codex-stop-hook.sh:1023-1050`.
- edge_cases_or_failure_modes: The test’s comment says “Only state.md indicates active loop,” but current `find_active_loop` also treats `finalize-state.md` as active in production (`hooks/lib/loop-common.sh:61-76`), so this test is a normal-loop naming spec rather than the complete active-state universe. The newest-directory rule means an older directory with `state.md` is ignored if the newest session lacks an active indicator. The test does not cover `finalize-state.md`; related finalize tests cover that at `tests/test-finalize-phase.sh:226-255` and completion to `complete-state.md` at `tests/test-finalize-phase.sh:420-495`.
- validation_or_tests: The file is itself validation. It uses `set -uo pipefail` initially, toggles `set +e`/`set -e` around expected failures, and exits nonzero on any failed assertion. It validates both positive transitions and negative non-active terminal-state detection, including legacy path isolation.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-074 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Prompt/block template used by validation hooks when a round file is in the correct filename/round family but the wrong session directory. It is part of the guard contract that keeps agents interacting with the active `.humanize/rlcr/<timestamp>` directory.
- algorithmic_behavior: Template content is four short lines at `prompt-template/block/wrong-directory-path.md:1-6`. It renders `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`, telling the agent what operation was attempted and the canonical active-loop path. The final instruction says `cat {{FILE_PATH}}` at `prompt-template/block/wrong-directory-path.md:6`; based on callers, this is used for both read and write blocks, so that line is helpful for reads but potentially awkward for write attempts.
- inputs_outputs_state: Inputs are template variables supplied by hooks. Read validator passes `ACTION=read`, attempted file path, and computed active-loop path at `hooks/loop-read-validator.sh:135-145`. Write validator passes `ACTION=write to`, attempted file path, and computed active-loop path at `hooks/loop-write-validator.sh:205-215`. Output is rendered markdown written to stderr; hook exit code is `2`, which blocks the tool call.
- gates_or_invariants: The gate fires only after an active loop is found, the file is a relevant round summary/prompt or protected `.humanize/rlcr` file, and earlier validations have not already blocked by file type, round number, state-file protection, goal-tracker rules, or wrong location. For reads, the final directory equality check is `FILE_PATH == ACTIVE_LOOP_DIR/<filename>` at `hooks/loop-read-validator.sh:131-146`. For writes, the same invariant appears at `hooks/loop-write-validator.sh:201-216`.
- dependencies_and_callers: Loaded through `load_and_render_safe`, whose template root is resolved to `prompt-template` at `hooks/lib/template-loader.sh:24-31`. Rendering is single-pass, keeps missing placeholders literal, and avoids recursive placeholder expansion at `hooks/lib/template-loader.sh:50-132`; fallback behavior for missing templates is at `hooks/lib/template-loader.sh:167-203`. Callers are `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`; `rg` found no bash-validator use of this exact template.
- edge_cases_or_failure_modes: Exact string comparison means symlinked, relative, differently normalized, or case-varied paths may be blocked even if they resolve to the same file. The template’s final `cat {{FILE_PATH}}` suggests reading the attempted path, not the `{{CORRECT_PATH}}`; that appears inconsistent with the preceding “Correct path” instruction and can be misleading, especially when `ACTION=write to`. If the template is absent or empty, hooks fall back to inline wrong-directory messages.
- validation_or_tests: No direct test file was found for this specific template text. Indirect validation exists through hook tests that exercise read/write validator behavior, plus `template-loader.sh` tests elsewhere in the suite if present. The concrete block behavior is anchored by the two hook call sites above.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 item sections present; each assigned item_id appears once as an Item Evidence heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`