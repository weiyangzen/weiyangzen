# agent_13 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-013 `directory` `skills/cancel-rlcr-loop`
- cursor: `[_]`
- core_role: This directory is the cancellation skill package for the RLCR loop. Recursive inspection found one child, `skills/cancel-rlcr-loop/SKILL.md`, which defines the skill identity, trigger description, model, allowed tool surface, and the cancellation state transition recipe. Its command wrapper is intentionally thin: `commands/cancel-rlcr-loop.md:1` delegates to `Skill(humanize:cancel-rlcr-loop:*)` and says the skill contains the full cancellation instructions at `commands/cancel-rlcr-loop.md:9`.
- algorithmic_behavior: The skill selects the current loop by newest timestamp directory under `.humanize/rlcr/*/` using `ls -1d ... | sort -r | head -1` at `skills/cancel-rlcr-loop/SKILL.md:20`. It then classifies the loop as `NORMAL_LOOP`, `FINALIZE_PHASE`, or `NO_ACTIVE_LOOP` by checking for `state.md` first and `finalize-state.md` second at `skills/cancel-rlcr-loop/SKILL.md:29`. For normal loops, it reads state, creates `.cancel-requested`, then renames `state.md` to `cancel-state.md` at `skills/cancel-rlcr-loop/SKILL.md:37`. For finalize phase, it reads `finalize-state.md`, asks the user for confirmation, and only on “Yes, cancel now” creates `.cancel-requested` and renames `finalize-state.md` to `cancel-state.md` at `skills/cancel-rlcr-loop/SKILL.md:43`.
- inputs_outputs_state: Inputs are the `.humanize/rlcr/` directory set, the newest loop’s `state.md` or `finalize-state.md`, current round/max-iteration fields inside that state file, and possibly a user confirmation during finalize phase. Outputs are a cancel signal file at `${LOOP_DIR}.cancel-requested`, a preserved terminal state file named `cancel-state.md`, and user-facing status messages. State transitions are `state.md -> cancel-state.md` for normal active loops and `finalize-state.md -> cancel-state.md` for confirmed finalize cancellation. If no active state marker exists, no filesystem transition is performed.
- gates_or_invariants: The skill’s key invariant is explicit at `skills/cancel-rlcr-loop/SKILL.md:58`: the current loop is always the newest timestamped directory, and active means `state.md` or `finalize-state.md` exists. The cancel signal must be created before the state-file move at `skills/cancel-rlcr-loop/SKILL.md:39` and `skills/cancel-rlcr-loop/SKILL.md:52`. The allowed tool list is intentionally narrow: list loop dirs, touch `.cancel-requested`, move state/finalize state to cancel state, read state files, and ask the user at `skills/cancel-rlcr-loop/SKILL.md:5`. Runtime hooks harden the same invariant: `hooks/lib/loop-common.sh:274` implements `is_cancel_authorized`, requiring the signal file, rejecting command substitution, shell operators, hidden variables, extra args, and requiring exactly `mv <active state/finalize-state> <active cancel-state>`. `hooks/loop-bash-validator.sh:98` and `hooks/loop-bash-validator.sh:108` allow state modifications only when that authorization returns true.
- dependencies_and_callers: The direct caller is the slash-command wrapper at `commands/cancel-rlcr-loop.md:1`. The lifecycle sibling is `skills/start-rlcr-loop/SKILL.md`, whose stopping conditions include user cancellation at `skills/start-rlcr-loop/SKILL.md:52`. Shared hook logic mirrors the skill’s active-loop detection in `hooks/lib/loop-common.sh:58`, including newest directory selection at `hooks/lib/loop-common.sh:71` and active marker detection at `hooks/lib/loop-common.sh:75`. Bash validation depends on the `.cancel-requested` signal and authorized move logic at `hooks/loop-bash-validator.sh:98` and `hooks/loop-bash-validator.sh:245`. The full test suite includes this area via `tests/run-all-tests.sh:45` for finalize behavior, `tests/run-all-tests.sh:46` for cancel-signal behavior, `tests/run-all-tests.sh:51` for skill structure, and `tests/run-all-tests.sh:53` for skill invocation.
- edge_cases_or_failure_modes: No loop directories produce “No active RLCR loop found” at `skills/cancel-rlcr-loop/SKILL.md:27`. A newest loop directory without `state.md` or `finalize-state.md` is treated as already ended or improperly started at `skills/cancel-rlcr-loop/SKILL.md:35`, even if older directories contain state files; this matches `hooks/lib/loop-common.sh:59`, which avoids reviving older “zombie” loops. Finalize cancellation can be declined, leaving `finalize-state.md` in place at `skills/cancel-rlcr-loop/SKILL.md:55`. Unauthorized or malformed state moves are blocked by `is_cancel_authorized`, including missing signal, wrong command, extra arguments, hidden variables, command substitution, chained commands, wrong source directory, or wrong destination at `hooks/lib/loop-common.sh:288`.
- validation_or_tests: `tests/test-skill-structure.sh` validates the skill exists, has the correct `name`, has a description, has `allowed-tools`, is user-invocable, has a valid model, has valid YAML/frontmatter, and contains no emoji/CJK. Relevant checks include cancel name validation at `tests/test-skill-structure.sh:95`, allowed tools at `tests/test-skill-structure.sh:191`, and model validation at `tests/test-skill-structure.sh:148`. `tests/test-skill-invocation.sh` adds behavior-specific checks: discoverability at `tests/test-skill-invocation.sh:73`, command delegation at `tests/test-skill-invocation.sh:110`, thin-wrapper separation at `tests/test-skill-invocation.sh:146`, allowed Bash tools at `tests/test-skill-invocation.sh:189`, auto-invocation keywords at `tests/test-skill-invocation.sh:301`, and content completeness at `tests/test-skill-invocation.sh:345`. `tests/test-cancel-signal-file.sh` validates the runtime authorization mechanism, including positive allowed move with signal at `tests/test-cancel-signal-file.sh:78` and direct helper checks at `tests/test-cancel-signal-file.sh:1257`.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-043 `file` `tests/test-skill-structure.sh`
- cursor: `[_]`
- core_role: This executable shell test is the structural specification for skill packaging and metadata. It does not execute RLCR loop transitions itself; instead, it gates whether skill directories, frontmatter, command delegation, model declarations, version metadata, and content constraints remain valid. It is included in the aggregate runner at `tests/run-all-tests.sh:51`.
- algorithmic_behavior: The script computes `SCRIPT_DIR`, `PROJECT_ROOT`, `SKILLS_DIR`, and `COMMANDS_DIR` at `tests/test-skill-structure.sh:11`, initializes counters, and reports pass/fail through helper functions at `tests/test-skill-structure.sh:25`. Positive tests PT-1 through PT-9 validate actual repository artifacts: skill directory count at `tests/test-skill-structure.sh:55`, `start-rlcr-loop` name at `tests/test-skill-structure.sh:76`, `cancel-rlcr-loop` name at `tests/test-skill-structure.sh:94`, descriptions at `tests/test-skill-structure.sh:111`, no `context: fork` at `tests/test-skill-structure.sh:128`, model fields at `tests/test-skill-structure.sh:147`, command-to-skill delegation at `tests/test-skill-structure.sh:164`, allowed tools at `tests/test-skill-structure.sh:191`, user-invocable status at `tests/test-skill-structure.sh:208`, and version consistency at `tests/test-skill-structure.sh:225`. Negative tests NT-1 through NT-6 create temporary invalid fixtures and verify helper validators reject invalid names, missing frontmatter, malformed YAML, wrong locations, duplicate names, and invalid models starting at `tests/test-skill-structure.sh:248`.
- inputs_outputs_state: Inputs are the repository’s `skills/*/SKILL.md`, `commands/*.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md`. Temporary negative fixtures are created under `mktemp -d` at `tests/test-skill-structure.sh:257` and removed by a trap at `tests/test-skill-structure.sh:259`. Outputs are colored PASS/FAIL lines, summary counts, and process exit code `0` if `TESTS_FAILED == 0`, otherwise `1`, at `tests/test-skill-structure.sh:658`. Internal state consists of `TESTS_PASSED`, `TESTS_FAILED`, temporary fixture paths, and arrays such as `SKILL_NAMES` at `tests/test-skill-structure.sh:560`.
- gates_or_invariants: The skill directory invariant is `skills/<name>/SKILL.md`; PT-1 counts actual skill files at `tests/test-skill-structure.sh:59`, and NT-4 verifies missing/wrong-location behavior at `tests/test-skill-structure.sh:455`. Skill names must match `^[a-z][a-z0-9-]*$` at `tests/test-skill-structure.sh:261`. Required frontmatter must start with `---` and include `name:` and `description:` at `tests/test-skill-structure.sh:267`. Model names must match one of `claude-`, `gpt-`, `o[0-9]`, or `gemini-` at `tests/test-skill-structure.sh:584`. The test also encodes a version invariant across plugin, marketplace, and README metadata at `tests/test-skill-structure.sh:234`; current inspected values are `1.2.3` in `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md`.
- dependencies_and_callers: The script depends on Bash, `sed`, `grep`, `awk`, `mktemp`, `tr`, `wc`, associative arrays, and `grep -P` for the emoji/CJK content check at `tests/test-skill-structure.sh:649`. It is called by `tests/run-all-tests.sh`, whose test-suite array includes it at `tests/run-all-tests.sh:51`. It couples to actual package paths: `skills/start-rlcr-loop/SKILL.md`, `skills/cancel-rlcr-loop/SKILL.md`, `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md`.
- edge_cases_or_failure_modes: The YAML checker is intentionally lightweight: `check_yaml_syntax` only validates line shapes inside frontmatter and does not parse full YAML semantics at `tests/test-skill-structure.sh:398`. It creates an unclosed-frontmatter fixture at `tests/test-skill-structure.sh:432`, but the visible assertion path only checks existing skills afterward at `tests/test-skill-structure.sh:442`; this is a coverage gap for that specific malformed fixture. The version extractor uses grep patterns tailored to `"version": "..."` and `Current Version: N.N.N` at `tests/test-skill-structure.sh:235`, so nonstandard formatting could produce empty values. The emoji/CJK check depends on PCRE Unicode support from `grep -P`; the script redirects grep errors to `/dev/null` at `tests/test-skill-structure.sh:650`, which can hide unsupported-PCRE behavior as a pass. In the current read-only environment I did not execute the script because negative fixtures require writing to a temporary directory and the assignment requested research notes only.
- validation_or_tests: This file is itself a validation suite. Its positive branch tests repository conformance, while its negative branch tests validator behavior with generated bad fixtures. It finishes with an explicit pass/fail summary and exits nonzero on any failure at `tests/test-skill-structure.sh:668`. It overlaps with, but is narrower than, `tests/test-skill-invocation.sh`; that companion test validates actual command/skill delegation and content completeness, while this structure script enforces general packaging and metadata rules.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-073 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: This prompt block is a guard message template used when the RLCR stop hook detects that the worker tried to exit without writing the required round or finalize summary. It defines a blocking transition contract: write the summary to the rendered path, then retry exit.
- algorithmic_behavior: The template is static Markdown with one placeholder, `{{SUMMARY_FILE}}`, at `prompt-template/block/work-summary-missing.md:7`. The stop hook computes the required summary path: `finalize-summary.md` during finalize phase, otherwise `round-${CURRENT_ROUND}-summary.md`, at `hooks/loop-codex-stop-hook.sh:522`. If that file is absent, the hook renders this template with `SUMMARY_FILE=$SUMMARY_FILE` via `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:529`. Rendering is single-pass placeholder replacement implemented in `hooks/lib/template-loader.sh:50`, which prevents nested placeholder expansion in variable values.
- inputs_outputs_state: Input is the computed summary path from hook state. In normal rounds the path is `$LOOP_DIR/round-${CURRENT_ROUND}-summary.md`; in finalize phase it is `$LOOP_DIR/finalize-summary.md`. Output is a user-facing block titled “Work Summary Missing” with required action and required summary contents. No repository state is changed by the template itself; the state transition is behavioral: exit remains blocked until the user creates the expected summary file.
- gates_or_invariants: The invariant is that every attempted stop must have a summary file before review/finalization can proceed. The template specifies required summary contents: implemented work, files created/modified, tests added/passed, and remaining items at `prompt-template/block/work-summary-missing.md:10`. The hook sets the system message to “Finalize Phase - summary file missing” or “Summary file missing for round N” at `hooks/loop-codex-stop-hook.sh:539`, preserving different phase semantics while using the same template.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh:536`. Template discovery is initialized through `hooks/lib/loop-common.sh:46`, which sources `hooks/lib/template-loader.sh`, computes `TEMPLATE_DIR`, and validates the prompt-template directory at `hooks/lib/loop-common.sh:50`. `load_and_render_safe` falls back to an inline message if the template is missing or empty at `hooks/lib/template-loader.sh:167`. The template lives beside sibling block templates under `prompt-template/block/`, which are used by the same hook and validators for other loop gates.
- edge_cases_or_failure_modes: If the template file is missing or empty, the hook uses the fallback message defined at `hooks/loop-codex-stop-hook.sh:533`, so the gate still blocks but loses the richer checklist of summary contents. If `SUMMARY_FILE` contains unresolved template-looking text, single-pass rendering avoids second-order substitution, per `hooks/lib/template-loader.sh:54`. If the user writes the wrong summary file, this gate remains active because the hook checks exact path existence at `hooks/loop-codex-stop-hook.sh:529`.
- validation_or_tests: The template-loading system is exercised by the broader test suite through template-loader and template-reference tests listed in `tests/run-all-tests.sh:34` and `tests/run-all-tests.sh:38`. Direct usage is visible in the stop hook at `hooks/loop-codex-stop-hook.sh:536`. The assigned `tests/test-skill-structure.sh` does not validate this template; this file is core because it defines an algorithmic stop gate contract, not because it is a skill metadata artifact.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `SKILLIZE_RLCR_ACTIONS-HZ-013`, `SKILLIZE_RLCR_ACTIONS-HZ-043`, `SKILLIZE_RLCR_ACTIONS-HZ-073`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`