# agent_27 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-027 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script for the RLCR workflow. It is the executable behind `humanize:start-rlcr-loop`, creating the per-session `.humanize/rlcr/<timestamp>/` state bundle, seeding the goal tracker, backing up the plan, and emitting the initial Claude prompt. The command wrapper delegates to the skill in `commands/start-rlcr-loop.md:1`, and the skill invokes this script in `skills/start-rlcr-loop/SKILL.md:11`.
- algorithmic_behavior: Parses CLI options and a single plan path at `scripts/setup-rlcr-loop.sh:104`; validates repo, path, tracking, content, branch, and Codex settings; creates `LOOP_DIR` at `scripts/setup-rlcr-loop.sh:467`; copies the plan backup at `scripts/setup-rlcr-loop.sh:475`; writes `state.md` frontmatter at `scripts/setup-rlcr-loop.sh:485`; builds `goal-tracker.md` at `scripts/setup-rlcr-loop.sh:504`; writes `round-0-prompt.md` at `scripts/setup-rlcr-loop.sh:602`; then prints the activation banner and initial prompt at `scripts/setup-rlcr-loop.sh:667`.
- inputs_outputs_state: Inputs are `PLAN_FILE` or `--plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--track-plan-file`, `CLAUDE_PROJECT_DIR`, cwd, git state, and `codex` availability. Outputs are `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, `round-0-prompt.md`, and the required `round-0-summary.md` path. Initial state sets `current_round: 0`, max iterations, Codex model/effort/timeout, push policy, plan path/tracking mode, start branch, and UTC start time at `scripts/setup-rlcr-loop.sh:485`.
- gates_or_invariants: Requires git repo and existing `HEAD` with timeout wrapping at `scripts/setup-rlcr-loop.sh:211`; rejects absolute paths, spaces, shell metacharacters, symlinks, missing files, unreadable files, parent-resolution failures, project escapes, and submodule-contained plans at `scripts/setup-rlcr-loop.sh:227`. Tracking gate requires tracked and clean only with `--track-plan-file`, otherwise rejects tracked plans at `scripts/setup-rlcr-loop.sh:331`. Content gate requires at least five lines and at least three non-comment content lines at `scripts/setup-rlcr-loop.sh:362`. Branch/model/effort YAML-safety gates are at `scripts/setup-rlcr-loop.sh:435`. Codex CLI must exist at `scripts/setup-rlcr-loop.sh:417`.
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:28`; that helper chooses `gtimeout`, GNU `timeout`, Python, or no timeout in `scripts/portable-timeout.sh:10`. Runtime consumers are the stop hook and validators: `hooks/loop-codex-stop-hook.sh:88` parses `state.md`, verifies branch and plan integrity at `hooks/loop-codex-stop-hook.sh:145`, then later advances or renames state files at `hooks/loop-codex-stop-hook.sh:961` and `hooks/loop-codex-stop-hook.sh:1057`. Write/bash validators protect generated loop files, e.g. state-file and goal-tracker rules in `hooks/loop-write-validator.sh:101` and `hooks/loop-bash-validator.sh:86`.
- edge_cases_or_failure_modes: `--max` and `--codex-timeout` accept numeric zero even though error text says positive, so zero can trigger immediate max-iteration behavior or no practical Codex timeout. Default tracking text says the plan must be gitignored, but the implemented check only verifies it is not tracked at `scripts/setup-rlcr-loop.sh:346`. Markdown headings are treated as comment lines for content validation at `scripts/setup-rlcr-loop.sh:402`, so heading-only plans are rejected. If no timeout implementation exists, git checks run without timeout per `scripts/portable-timeout.sh:64`. The goal/AC extraction is heuristic and leaves placeholders for Round 0 if headings are absent at `scripts/setup-rlcr-loop.sh:527`.
- validation_or_tests: Read-only syntax check passed with `bash -n scripts/setup-rlcr-loop.sh`. Test coverage includes script existence/syntax/help in `tests/test-skill-invocation.sh:233`, plan path/tracking/content/model validation in `tests/test-plan-file-validation.sh:89`, `tests/test-plan-file-validation.sh:329`, `tests/test-plan-file-validation.sh:514`, and `tests/test-plan-file-validation.sh:696`, plus downstream state transition coverage in `tests/test-finalize-phase.sh:460` and `tests/test-finalize-phase.sh:575`. A local `git rev-parse` verification was not usable in this branch export because the export is not a git checkout in the sandbox and macOS cache creation was denied.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-057 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the “repository is dirty” gate. It turns a detected git-clean violation into instructions that prevent loop exit until changes are reviewed, staged, and committed.
- algorithmic_behavior: The template itself is declarative, but it participates in a hard gate. `hooks/loop-codex-stop-hook.sh:319` caches `git status --porcelain`; if any dirty output exists, the hook sets `GIT_ISSUES="uncommitted changes"` at `hooks/loop-codex-stop-hook.sh:435`, augments `SPECIAL_NOTES` for `.humanize` or other untracked files at `hooks/loop-codex-stop-hook.sh:438`, renders this template at `hooks/loop-codex-stop-hook.sh:470`, and returns a JSON block decision at `hooks/loop-codex-stop-hook.sh:474`.
- inputs_outputs_state: Inputs are `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` from the stop hook. Output is the rendered human-facing reason string embedded in the hook JSON response. No file state advances; the active `state.md` or `finalize-state.md` remains in place and the exit attempt is blocked.
- gates_or_invariants: Requires commit hygiene before exit: review untracked files, ignore artifacts, stage real changes, commit with project conventions, and avoid AI authorship trailers. It also suggests optional `code-simplifier` use before committing at `prompt-template/block/git-not-clean.md:5`. The hook fails closed if `git status` itself fails before this template is used, via `hooks/loop-codex-stop-hook.sh:325`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh:170` for safe rendering with fallback, and on special-note templates `prompt-template/block/git-not-clean-humanize-local.md:2` and `prompt-template/block/git-not-clean-untracked.md:2`. Its only direct runtime caller found is the stop hook at `hooks/loop-codex-stop-hook.sh:470`.
- edge_cases_or_failure_modes: If `.humanize` is the dirty source, this template’s generic “stage real changes” instruction is softened by `SPECIAL_NOTES`, which says `.humanize/` should not be committed. The template does not list actual dirty paths, so the user must inspect git status separately. It mentions `git add -A`, but active-loop bash validation can block broad adds that would include `.humanize`, through `hooks/lib/loop-common.sh:443` and `hooks/loop-bash-validator.sh:81`. Push checks are separate and rendered by `prompt-template/block/unpushed-commits.md`.
- validation_or_tests: Template existence is covered indirectly by `tests/test-template-references.sh:56`, which scans hook template references and fails on missing files. Dirty-git blocking behavior is exercised in finalize phase by `tests/test-finalize-phase.sh:390`. I found no direct unit test asserting the exact rendered text of this specific template.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-087 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex review contract for normal RLCR rounds. It tells Codex how to audit Claude’s implementation against the original plan, goal tracker, docs, summary, and acceptance criteria, and defines the `COMPLETE` termination marker contract for non-full-alignment rounds.
- algorithmic_behavior: The stop hook renders this template for rounds where `CURRENT_ROUND % 5 != 4`; full-alignment rounds use a different template. Rendering happens at `hooks/loop-codex-stop-hook.sh:733`, with placeholders for round, plan, current prompt, summary, goal tracker, docs path, goal-tracker update section, and review result path. The resulting prompt is written to `round-N-review-prompt.md`, sent to `codex exec`, and Codex output is expected at `round-N-review-result.md`.
- inputs_outputs_state: Inputs are `{{CURRENT_ROUND}}`, `{{PLAN_FILE}}`, `{{PROMPT_FILE}}`, `{{SUMMARY_CONTENT}}`, `{{DOCS_PATH}}`, `{{GOAL_TRACKER_FILE}}`, `{{GOAL_TRACKER_UPDATE_SECTION}}`, and `{{REVIEW_RESULT_FILE}}`. Output is a review instruction prompt. Downstream state depends on Codex’s last non-empty review line: exact `COMPLETE` enters Finalize Phase by renaming `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:961`; any non-marker review increments `current_round` and creates the next prompt at `hooks/loop-codex-stop-hook.sh:1057`.
- gates_or_invariants: Requires Codex to read the plan first at `prompt-template/codex/regular-review.md:5`, audit for implementation gaps at `prompt-template/codex/regular-review.md:20`, reject deferred or pending work at `prompt-template/codex/regular-review.md:24`, verify goal alignment at `prompt-template/codex/regular-review.md:33`, and only use `COMPLETE` when all original plan tasks and ACs are done with no deferrals or pending work at `prompt-template/codex/regular-review.md:53`. It requires an AC summary format at `prompt-template/codex/regular-review.md:42`.
- dependencies_and_callers: Depends on the stop hook’s template rendering and Codex invocation path at `hooks/loop-codex-stop-hook.sh:718` and `hooks/loop-codex-stop-hook.sh:838`. Its Part 3 content is injected from `prompt-template/codex/goal-tracker-update-section.md:1`, which gives Codex authority to evaluate and apply requested goal-tracker updates after Round 0. It indirectly relies on setup-created `goal-tracker.md` and `round-N-summary.md`, and on validators that block Claude from editing goal-tracker after Round 0, e.g. `hooks/loop-write-validator.sh:145`.
- edge_cases_or_failure_modes: The template is stricter than the start-skill prose, because it treats all deferrals as incomplete for `COMPLETE`. The injected goal-tracker section begins with its own heading, so `## Part 3: {{GOAL_TRACKER_UPDATE_SECTION}}` can render as a nested-looking heading line. Unlike `full-alignment-review.md`, this template does not instruct Codex to use `STOP`; the stop hook still handles `STOP` during regular rounds as unusual at `hooks/loop-codex-stop-hook.sh:1023`. If Codex exits successfully but writes only stdout, the hook copies stdout to the review result file at `hooks/loop-codex-stop-hook.sh:897`; empty or missing review results block exit at `hooks/loop-codex-stop-hook.sh:915`.
- validation_or_tests: Template reference coverage is provided by `tests/test-template-references.sh:56`. Stop-hook behavior around this contract is covered by `tests/test-finalize-phase.sh:460` for `COMPLETE` entering Finalize Phase and `tests/test-finalize-phase.sh:575` for non-`COMPLETE` feedback incrementing the round. Read-only syntax checks passed for the stop hook and shared libraries that render and consume this template. I found no direct unit test asserting the fully rendered `regular-review.md` prompt.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence headers present; each assigned item id appears once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`