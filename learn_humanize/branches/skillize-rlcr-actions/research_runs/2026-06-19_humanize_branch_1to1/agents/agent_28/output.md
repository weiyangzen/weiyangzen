# agent_28 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-028 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor deletion handling, specifically AC-1.1 clean/user-friendly exit and AC-1.3 terminal restoration. It creates a minimal active-loop fixture, asks the operator to run `source scripts/humanize.sh && humanize monitor rlcr-loop`, then deletes `.humanize` to force the monitor’s graceful-stop path.
- algorithmic_behavior: The script dispatches on one positional command via `case "${1:-}"` at `tests/manual-monitor-test.sh:23`. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99`, writes a minimal `state.md`, writes `goal-tracker.md`, and prints the next manual steps at `tests/manual-monitor-test.sh:24-46`. `delete` removes `.humanize` and states the expected monitor outputs at `tests/manual-monitor-test.sh:47-57`. `cleanup` removes leftovers at `tests/manual-monitor-test.sh:58-62`. Invalid input prints usage and exits nonzero at `tests/manual-monitor-test.sh:63-70`.
- inputs_outputs_state: Input is one of `setup`, `delete`, or `cleanup`. State transition is absent/dirty local `.humanize` state -> synthetic active session -> deleted `.humanize` -> cleaned local state. Outputs are filesystem side effects under `.humanize/rlcr/...` plus operator-facing stdout. The fixture fields in `state.md` include `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` at `tests/manual-monitor-test.sh:27-32`, matching monitor parsing expectations in `scripts/humanize.sh:152-168`.
- gates_or_invariants: Uses `set -euo pipefail` and roots itself at the repository top using `SCRIPT_DIR`/`PROJECT_ROOT` at `tests/manual-monitor-test.sh:17-21`. The invariant under test is that deleting `.humanize` while `humanize monitor rlcr-loop` is running should produce `Monitoring stopped: .humanize/rlcr directory no longer exists`, reset the terminal scroll region/cursor, and avoid shell glob errors; those expectations are printed at `tests/manual-monitor-test.sh:51-54`. The corresponding monitor implementation uses `find` guards to avoid zsh glob failures at `scripts/humanize.sh:27-49` and resets the terminal in `_restore_terminal` with `printf "\033[r"` at `scripts/humanize.sh:465-471`.
- dependencies_and_callers: Depends on the shell monitor entrypoint `humanize monitor rlcr-loop`, dispatched in `scripts/humanize.sh:805-837`. It exercises `_humanize_monitor_codex`, which requires `.humanize/rlcr` at startup (`scripts/humanize.sh:20-25`), tracks latest timestamped sessions (`scripts/humanize.sh:27-49`), reads state files (`scripts/humanize.sh:111-168`), and routes deletion through `_graceful_stop`/`_cleanup` (`scripts/humanize.sh:478-512`). It is not called by `tests/run-all-tests.sh` in the inspected references; it is a human-driven validation aid.
- edge_cases_or_failure_modes: `delete` and `cleanup` both run `rm -rf .humanize`, so running this in a real project with useful loop history will remove local loop artifacts. The fixture timestamp is fixed, so repeated setup overwrites the same synthetic session. The script does not create a cached Codex log under `$HOME/.cache/humanize`, so the monitor may show “waiting for log file” while still testing deletion handling. It requires two terminals and human observation, so terminal restoration is not automatically asserted here.
- validation_or_tests: This file is itself a manual test. Similar behavior is automated in `tests/test-monitor-runtime.sh:94-134`, which asserts `_graceful_stop` calls cleanup and prints `Monitoring stopped`, and in `tests/test-monitor-runtime.sh:253-305`, which checks terminal restore source markers. End-to-end coverage in `tests/test-monitor-e2e-real.sh:184-209` checks graceful-stop text, deletion reason, absence of `no matches found|bad pattern`, and cleanup output.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-058 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR Bash validator’s git-push gate. It defines the user-facing contract when `git push` is disallowed because the loop is configured for local commits until completion.
- algorithmic_behavior: The template is static Markdown: title at `prompt-template/block/git-push.md:1`, local-only policy at `prompt-template/block/git-push.md:3-4`, and remediation via `/humanize:start-rlcr-loop plan.md --push-every-round` at `prompt-template/block/git-push.md:6-9`. The actual algorithm is in `hooks/loop-bash-validator.sh`: it parses hook input, finds the active loop, parses state, checks `STATE_PUSH_EVERY_ROUND`, and if false blocks commands matching leading `git push` with this template at `hooks/loop-bash-validator.sh:55-72`.
- inputs_outputs_state: Template input is implicit: it is loaded by `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md" "$FALLBACK"` at `hooks/loop-bash-validator.sh:70`. Runtime inputs are the Bash command from hook JSON (`hooks/loop-bash-validator.sh:22-30`) and `push_every_round` from the active loop state (`hooks/loop-bash-validator.sh:51-63`). Output is stderr Markdown plus validator exit code `2`, which blocks the tool call. No repository state is changed by the template itself.
- gates_or_invariants: The invariant is default no-push behavior: if `push_every_round` is anything other than literal `true`, direct `git push` is blocked (`hooks/loop-bash-validator.sh:63-72`). `push_every_round` is initialized from the startup flag: `--push-every-round` sets `PUSH_EVERY_ROUND="true"` in `scripts/setup-rlcr-loop.sh:148-150`, and the value is persisted into `state.md` at `scripts/setup-rlcr-loop.sh:485-498`. Missing legacy state defaults to false in `parse_state_file` at `hooks/lib/loop-common.sh:121-137`.
- dependencies_and_callers: Depends on the template loader via `hooks/lib/loop-common.sh`, where `TEMPLATE_DIR` is initialized and fallback behavior is allowed at `hooks/lib/loop-common.sh:50-56`. The startup command advertises the enabling option in `commands/start-rlcr-loop.md:1-4`. The user-facing README also documents `--push-every-round`, but the enforcement path is the Bash validator plus this template.
- edge_cases_or_failure_modes: The validator pattern only matches commands beginning with optional whitespace then `git push`; aliases, subshells, or chained forms not starting with `git push` depend on other validator coverage and are not described by this template. If the template is missing or invalid, the validator falls back to inline text at `hooks/loop-bash-validator.sh:66-70`, so blocking still occurs but wording changes. If `push_every_round` is malformed, the strict comparison treats it as false and blocks pushes.
- validation_or_tests: `tests/test-template-loader.sh:55-65` verifies the template loads and contains “Git Push Blocked”. `tests/test-template-loader.sh:211-222` verifies `load_and_render_safe` uses this real template instead of fallback. `tests/test-templates-comprehensive.sh:458-462` also checks the real template path is selected. The behavioral gate is indirectly covered through Bash validator tests that exercise protected command blocking.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-088 `file` `skills/cancel-rlcr-loop/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction defining the canonical RLCR cancellation workflow. It tells the agent how to identify the newest active loop, authorize cancellation by writing `.cancel-requested`, preserve state by renaming active state to `cancel-state.md`, and handle Finalize Phase with explicit user confirmation.
- algorithmic_behavior: Frontmatter declares skill identity, trigger description, model, and allowed tools at `skills/cancel-rlcr-loop/SKILL.md:1-14`. The workflow selects the newest loop directory with `ls -1d .humanize/rlcr/*/ | sort -r | head -1` at `skills/cancel-rlcr-loop/SKILL.md:20-25`, returns “No active RLCR loop found” when absent at `skills/cancel-rlcr-loop/SKILL.md:27`, classifies state as `NORMAL_LOOP`, `FINALIZE_PHASE`, or `NO_ACTIVE_LOOP` at `skills/cancel-rlcr-loop/SKILL.md:29-35`, and then transitions normal/finalize states to cancellation at `skills/cancel-rlcr-loop/SKILL.md:37-57`.
- inputs_outputs_state: Inputs are the current `.humanize/rlcr/*/` directories, the active state file (`state.md` or `finalize-state.md`), and in Finalize Phase the user’s confirmation answer. Outputs are `.cancel-requested` plus `cancel-state.md`, with the original `state.md` or `finalize-state.md` removed by `mv`. Normal transition: active `state.md` -> signal file -> `cancel-state.md` at `skills/cancel-rlcr-loop/SKILL.md:37-41`. Finalize transition after confirmation: active `finalize-state.md` -> signal file -> `cancel-state.md` at `skills/cancel-rlcr-loop/SKILL.md:43-57`.
- gates_or_invariants: Newest timestamped loop is authoritative; older loops are ignored, stated at `skills/cancel-rlcr-loop/SKILL.md:58`. A loop is active only if `state.md` or `finalize-state.md` exists. The signal file must be created before renaming state in both paths (`skills/cancel-rlcr-loop/SKILL.md:39-40`, `52-53`) because Bash validator authorization requires `.cancel-requested`. The shared enforcement function `is_cancel_authorized` rejects missing signal, command substitution, newlines, shell chaining, remaining `$`, non-`mv`, extra args, wrong source, and wrong destination at `hooks/lib/loop-common.sh:274-413`.
- dependencies_and_callers: `/humanize:cancel-rlcr-loop` is a thin command wrapper that delegates to this skill via `Skill(humanize:cancel-rlcr-loop:*)` at `commands/cancel-rlcr-loop.md:1-17`. Runtime validators identify active loops using the same “newest only” model in `find_active_loop` at `hooks/lib/loop-common.sh:58-81`. `hooks/loop-bash-validator.sh:86-95` documents the state-file modification block and cancellation exception, relying on `is_cancel_authorized` for the safe rename path.
- edge_cases_or_failure_modes: If `.humanize/rlcr` has no directories, the skill reports no active loop. If the newest directory exists but has neither `state.md` nor `finalize-state.md`, it reports that the loop already ended or was not properly started. If both state files exist, the classification command checks `state.md` first, so it chooses `NORMAL_LOOP`. Finalize cancellation is gated by `AskUserQuestion`; selecting “No, let it finish” makes no filesystem change. The allowed-tools glob commands are broad, but hook-side authorization narrows the permitted state rename to the active loop’s exact source and `cancel-state.md` destination.
- validation_or_tests: `tests/test-skill-invocation.sh:73-84` verifies the cancel skill exists and has required frontmatter; `tests/test-skill-invocation.sh:110-122` verifies the command delegates via Skill tool. `tests/test-cancel-signal-file.sh:1-10` describes the signal-file test matrix, and `tests/test-cancel-signal-file.sh:74-92` verifies the positive case where `mv state.md cancel-state.md` is allowed only after `.cancel-requested` exists. The same test file includes extensive negative cases for wrong paths, chaining, shell wrappers, extra args, and hidden variables.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`