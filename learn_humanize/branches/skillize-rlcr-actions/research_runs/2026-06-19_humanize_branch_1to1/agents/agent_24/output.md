# agent_24 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-024 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` gate for RLCR loop state. It prevents Claude from writing protected loop artifacts directly, enforces current-round summary paths, and preserves the loop’s state-machine ownership boundaries. It is registered as the `Write` matcher command in `hooks/hooks.json:14-21`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits immediately for non-`Write` tools (`hooks/loop-write-validator.sh:23-30`). It normalizes the target path to lowercase for pattern checks (`line 31`), blocks `round-*-todos.md` except allowlisted loop files (`lines 37-44`), blocks all `round-*-prompt.md` writes (`lines 47-50`), classifies summary/finalize/loop-dir targets (`lines 56-58`), and allows unrelated writes outside `.humanize/rlcr/` (`lines 60-63`). For loop-owned control files it finds the active loop, chooses `finalize-state.md` over `state.md` when present, parses current round, then blocks writes to state files, plan backup, and post-round-0 goal tracker (`lines 79-149`). Summary writes are checked for correct location and current round (`lines 155-198`) before final directory equality validation (`lines 205-216`).
- inputs_outputs_state: Input is Claude hook JSON on stdin plus environment `CLAUDE_PROJECT_DIR` or `pwd` for project root resolution (`lines 23-40`, `80-82`). It reads `.humanize/rlcr/<active-loop>/state.md` or `finalize-state.md` through `parse_state_file` to derive `STATE_CURRENT_ROUND` (`lines 88-99`). Output is process exit status: `0` allow, `2` block. Block reasons are written to stderr using rendered templates via shared helpers, for example todos, prompt, state, wrong summary location, wrong round, wrong directory, and goal tracker messages (`lines 41-49`, `105-112`, `132-147`, `160-196`, `211-214`).
- gates_or_invariants: The active loop is the newest directory under `.humanize/rlcr` containing `state.md` or `finalize-state.md`; if none exists, protected-path validation is skipped (`hooks/lib/loop-common.sh:63-81`, `hooks/loop-write-validator.sh:84-86`). `state.md` and `finalize-state.md` are system-owned and always blocked, with finalize checked first because `state.md` matching also catches `finalize-state.md` (`hooks/loop-write-validator.sh:101-113`). `finalize-summary.md` is writable only inside the active loop and only at exactly `$ACTIVE_LOOP_DIR/finalize-summary.md` (`lines 116-126`). `goal-tracker.md` is writable only through round 0; later rounds must request updates in the current round summary (`lines 141-149`). Summary filenames must match the parsed current round unless the file is allowlisted (`lines 181-198`), and any protected loop file must resolve to the active-loop directory (`lines 205-216`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for template loading, active loop detection, state parsing, path classifiers, allowlist logic, round extraction, and block-message rendering (`hooks/loop-write-validator.sh:15-17`). Key shared definitions include `TEMPLATE_DIR` initialization (`hooks/lib/loop-common.sh:46-55`), `is_round_file_type` (`148-153`), `extract_round_number` (`158-165`), `is_allowlisted_file` (`170-188`), state/finalize/goal/finalize-summary classifiers (`250-272`), and `.humanize/rlcr` membership (`416-419`). The template-reference test scans this validator as a critical template user (`tests/test-template-references.sh:56-64`, `176-180`).
- edge_cases_or_failure_modes: If hook input lacks `tool_name` or `file_path`, empty defaults are used and most checks allow the write unless a later path classifier matches (`hooks/loop-write-validator.sh:24-31`). If no active loop is found, writes to otherwise protected filenames are allowed after the initial todos/prompt checks (`lines 84-86`). The allowlist intentionally permits `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` only in the active loop (`hooks/lib/loop-common.sh:174-187`), so historical bootstrap files can be preserved while other non-current files are blocked. Legacy `.humanize-loop.local` paths are not treated as active RLCR loop paths; tests expect those writes to be allowed (`tests/test-plan-file-hooks.sh:1030-1041`). Directory validation uses string equality against `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`, so alternate path spellings or symlinked equivalents can be blocked even if they point to the same file.
- validation_or_tests: Direct tests cover finalize behavior: allowing `finalize-summary.md` and blocking `finalize-state.md` (`tests/test-finalize-phase.sh:281-303`). Allowlist tests cover permitted `round-1-todos.md` and historical `round-0-summary.md`, plus blocked `round-3-todos.md` and non-allowlisted `round-2-summary.md` (`tests/test-allowlist-validators.sh:133-183`). Plan backup protection is tested for Write hooks (`tests/test-plan-file-hooks.sh:194-205`). Template-reference tests ensure referenced templates exist and critical validators use safe rendering with fallbacks (`tests/test-template-references.sh:51-69`, `169-190`). I inspected tests but did not execute them because this run requested research notes only in a read-only branch export.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-054 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the `.humanize/` Git staging invariant. It does not detect violations by itself; it supplies the reason text when the Bash validator determines a `git add` command would stage local loop state.
- algorithmic_behavior: The template states that `.humanize/` contains local loop state and should not be committed (`prompt-template/block/git-add-humanize.md:3-6`). It then gives allowed narrower staging patterns (`lines 8-14`) and examples of blocked commands that would stage `.humanize` directly or through broad/forced staging (`lines 16-24`). Enforcement is performed by `git_adds_humanize` in `hooks/lib/loop-common.sh:421-539`, which is called by the Bash PreToolUse validator at `hooks/loop-bash-validator.sh:75-84`.
- inputs_outputs_state: Template input is only the template loader lookup path; this file has no placeholders, so rendering is static. Runtime input comes from Bash hook JSON `.tool_input.command`, lowercased in `hooks/loop-bash-validator.sh:22-31`. If `git_adds_humanize` returns true, `git_add_humanize_blocked_message` renders this template or a fallback to stderr and the Bash validator exits `2` (`hooks/loop-bash-validator.sh:81-83`, `hooks/lib/loop-common.sh:541-568`).
- gates_or_invariants: The invariant is that `.humanize/` remains uncommitted local RLCR state. The detector blocks direct `.humanize` path references, including quoted and nested/path-prefixed variants, without over-blocking similarly named files like `.humanizeconfig` (`hooks/lib/loop-common.sh:475-485`). It blocks forced broad staging such as `-f .`, `--force *`, and combined force/all flags because force bypasses `.gitignore` (`lines 488-516`). It blocks `git add -A` or `--all` when `.humanize` exists (`lines 518-527`). It blocks broad `git add .` or `git add *` only if `.humanize` exists and is not protected by `git check-ignore` (`lines 529-534`).
- dependencies_and_callers: The template is loaded by `git_add_humanize_blocked_message` (`hooks/lib/loop-common.sh:543-568`). That helper is used by `hooks/loop-bash-validator.sh` after active-loop discovery and push-policy checks (`hooks/loop-bash-validator.sh:32-84`). The validator is registered for `Bash` hooks in `hooks/hooks.json:42-48`. The detector supports chained shell segments split on `&&`, `||`, `|`, and `;`, and `git -C`/git-option forms before `add` (`hooks/lib/loop-common.sh:446-473`).
- edge_cases_or_failure_modes: Because command text is lowercased before analysis, `-A` is treated as `-a`; the detector explicitly accounts for this (`hooks/lib/loop-common.sh:425-427`, `496-502`). Direct `.humanize` references are blocked even if no `.humanize` directory currently exists, but `-A`/`--all` blocking depends on `.humanize` being present in the current working directory (`lines 480-527`). Broad non-force staging is allowed when `.humanize` is ignored by Git, matching the template’s note that `.humanize/` is already in `.gitignore` (`prompt-template/block/git-add-humanize.md:4`, `hooks/lib/loop-common.sh:529-534`). The detector is regex/shell-text based, so highly unusual quoting or command construction could evade detection if the literal command no longer contains recognizable `git ... add ...` text.
- validation_or_tests: `tests/test-humanize-escape.sh` directly exercises `git_adds_humanize`: direct and quoted `.humanize` paths (`83-100`), force and broad-scope flags (`109-119`), `-A`/`--all` with a real `.humanize` directory (`128-140`), chained commands (`153-155`), `git -C`/`--git-dir` forms (`164-167`), and allowed commands including specific paths, patch mode, and similarly named files (`175-192`). Template-reference tests scan `hooks/lib/loop-common.sh` and validators for template references (`tests/test-template-references.sh:56-64`). I inspected but did not run these tests.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-084 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt footer fragment for the RLCR push policy. It instructs Claude that when `--push-every-round` is enabled, commits must be pushed after each round.
- algorithmic_behavior: The file is a static one-sentence note (`prompt-template/claude/push-every-round-note.md:2`). It is appended to the generated next-round prompt only when parsed loop state has `push_every_round: true`; otherwise it is omitted (`hooks/loop-codex-stop-hook.sh:1094-1101`). This is a prompt-level transition aid rather than a validator, but it coordinates with enforcement that blocks unpushed exits under the same state flag.
- inputs_outputs_state: Its direct input is the `PUSH_EVERY_ROUND` shell variable, derived from `STATE_PUSH_EVERY_ROUND` after `parse_state_file`; the shared parser defaults missing `push_every_round` to `false` (`hooks/lib/loop-common.sh:112-138`). Output is appended text in `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md` during stop-hook prompt generation (`hooks/loop-codex-stop-hook.sh:1062-1101`). If the template cannot be loaded, the stop hook falls back to `Also push your changes after committing.` (`hooks/loop-codex-stop-hook.sh:1096-1099`).
- gates_or_invariants: When `push_every_round` is true, the next-round instruction surface must include a push requirement (`hooks/loop-codex-stop-hook.sh:1094-1101`). Enforcement is paired with stop-hook Git status validation: if local branch status is ahead of remote, the stop hook blocks exit with an unpushed-commits message (`hooks/loop-codex-stop-hook.sh:485-503`). Conversely, when `push_every_round` is not true, the Bash validator blocks `git push` and tells the user to start the loop with the flag if pushing is required (`hooks/loop-bash-validator.sh:56-72`; `prompt-template/block/git-push.md:1-9`).
- dependencies_and_callers: Called only from `hooks/loop-codex-stop-hook.sh` via `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` (`hooks/loop-codex-stop-hook.sh:1094-1101`). The flag is documented in `README.md:96` and represented in shared state parsing as `FIELD_PUSH_EVERY_ROUND` (`hooks/lib/loop-common.sh:21`, `107-136`). Related block template `prompt-template/block/unpushed-commits.md` provides the hard-stop message when the policy is violated (`prompt-template/block/unpushed-commits.md:1-10`).
- edge_cases_or_failure_modes: This template does not itself check remote state, current branch, or whether `git push` succeeds; it only changes instructions. If the template is missing, runtime still emits a fallback push instruction, so the prompt gate degrades gracefully (`hooks/loop-codex-stop-hook.sh:1096-1099`). If `push_every_round` is absent or malformed and not exactly `true`, it defaults effectively to the local-only policy and this note will not be appended (`hooks/lib/loop-common.sh:128-136`, `hooks/loop-codex-stop-hook.sh:1094-1101`).
- validation_or_tests: Runtime references were found in the stop hook and push-policy templates. `tests/test-finalize-phase.sh:135` includes state fixture coverage with `push_every_round: false`, and template-reference tests scan `hooks/loop-codex-stop-hook.sh` for template paths (`tests/test-template-references.sh:56-64`). I found no direct test that asserts this exact note is appended when `push_every_round` is true; enforcement coverage appears to live primarily in the stop hook’s unpushed-commits gate and Bash push block.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 assigned item headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`