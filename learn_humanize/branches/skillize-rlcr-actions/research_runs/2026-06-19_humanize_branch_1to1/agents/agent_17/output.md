# agent_17 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-017 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Fast stop-hook validator for Claude Code TodoWrite completion. It reads a Claude transcript and determines whether the latest todo state allows the RLCR loop to proceed into heavier stop-hook validation/review. The file documents its contract at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:7): exit `0` means no incomplete todos, exit `1` means incomplete todos exist, exit `2` means hook input JSON parse failure.
- algorithmic_behavior: `find_latest_todos()` line-scans a UTF-8 JSONL transcript, skipping blank lines and malformed JSONL entries, and updates `latest_todos` whenever it finds a non-empty `TodoWrite` input list. It recognizes three transcript shapes: Claude assistant messages with `message.content[]` tool-use blocks at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:51), alternative `type: message` entries at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:65), and direct `type: tool_use` entries at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:78). The main routine parses hook JSON from stdin, expands `~` in `transcript_path`, extracts latest todos, and treats every todo whose `status` is not exactly `completed` as incomplete at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/check-todos-from-transcript.py:113).
- inputs_outputs_state: Input is one JSON object on stdin, normally containing `transcript_path`; the secondary input is the transcript JSONL file. Output is either no stdout on success, `PARSE_ERROR: ...` on stderr for invalid hook JSON, or stdout beginning with `INCOMPLETE_TODOS` followed by one line per unfinished todo. The script does not mutate repository or RLCR state; it converts volatile transcript state into a stop-hook gate result. Missing `transcript_path`, a nonexistent transcript path, no TodoWrite calls, or no todos all transition to allow/proceed via exit `0`.
- gates_or_invariants: The invariant is exact TodoWrite completion: only `status == "completed"` is accepted. Any other status, including empty/missing status, is a blocking incomplete todo. The latest non-empty TodoWrite list wins, so earlier incomplete todos are ignored if a later TodoWrite list is fully completed. Invalid JSONL transcript lines are ignored rather than blocking, while invalid hook-input JSON is a hard parse gate.
- dependencies_and_callers: Depends only on Python standard-library `json`, `sys`, and `pathlib`. Its primary caller is `hooks/loop-codex-stop-hook.sh`, which sets `TODO_CHECKER` and invokes it before expensive Codex review at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:258). Exit `2` is converted into a JSON block decision at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:265); exit `1` renders `prompt-template/block/incomplete-todos.md` through `load_and_render_safe` and blocks at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:284).
- edge_cases_or_failure_modes: A later TodoWrite call with an empty `todos` array does not clear prior latest todos because assignment only happens when `todos` is truthy. Todo entries are assumed to be dictionaries; malformed todo elements could raise during `todo.get`. `transcript_path` is not type-checked before `Path(...).expanduser()`. File permission/read errors are not caught. Status comparison is case-sensitive and schema-specific; this checker does not understand branch checklist cursors, only Claude TodoWrite statuses.
- validation_or_tests: Covered by `tests/test-todo-checker.sh`, which exercises invalid/empty hook JSON, missing transcript path, nonexistent transcript file, all-completed todos, pending/in-progress todos, empty transcript, invalid JSONL lines, latest TodoWrite precedence, direct tool-use format, alternative message format, missing status, empty content, and Unicode content. Representative tests for latest TodoWrite precedence are at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-todo-checker.sh:196); missing status as incomplete is at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-todo-checker.sh:249).
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-047 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template subsystem used by RLCR hooks. It validates that all `prompt-template/**/*.md` files are discoverable, syntactically compatible with the `{{VAR}}` renderer, render under edge-case substitutions, and support fallback behavior. It is wired into CI by `.github/workflows/template-test.yml`, which runs `./tests/test-templates-comprehensive.sh` at [.github/workflows/template-test.yml](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/.github/workflows/template-test.yml:39).
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:17), resolves `TEMPLATE_DIR` through `get_template_dir`, and tracks `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` counters. It runs seven validation sections: directory structure, load all templates, syntax validation, malformed-placeholder detector checks, render edge cases, fallback/integration behavior, and stress tests. It exits `0` only if `TESTS_FAILED` remains zero at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:619).
- inputs_outputs_state: Inputs are the repository’s `prompt-template` tree and the shell functions from `hooks/lib/template-loader.sh`. Temporary directories are created with `mktemp -d` for malformed-placeholder and incomplete-directory checks, then removed during normal flow. Outputs are colored pass/fail/warn lines and a final process exit code. Internal state is limited to shell counters and transient variables; no repository files are modified.
- gates_or_invariants: Template root must exist and contain `block/`, `codex/`, and `claude/` subdirectories at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:64). Every `.md` template must load and be non-empty/non-whitespace to avoid failure or warning. Placeholder syntax is constrained to uppercase identifiers in double braces; the script flags extra open/close braces, wrong bracket types, spaces inside placeholders, lowercase placeholders, and heuristic unclosed placeholders at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:118). Render behavior must preserve empty lines, replace repeated/adjacent placeholders, tolerate special characters, and handle long/multiline values without crashing. Fallback behavior must use fallback text for missing templates and real template text when present.
- dependencies_and_callers: Depends on Bash, `find`, `sort`, `grep`, `sed`, `wc`, `tr`, `mktemp`, and the loader functions `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Those functions are implemented in `hooks/lib/template-loader.sh`; the renderer’s single-pass substitution contract is documented at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:50). `tests/run-all-tests.sh` includes this script in the broader test list at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/run-all-tests.sh:40).
- edge_cases_or_failure_modes: The script uses `set -uo pipefail` but not `set -e`, so most validation failures are accumulated rather than aborting immediately. Missing template root is a hard early exit, but missing subdirectories increment failures and continue. Syntax regexes are heuristic and can produce false positives/negatives for unusual markdown examples. The “CJK” test uses ASCII placeholder content rather than actual CJK characters in this file, so it primarily checks non-crashing substitution flow. Remaining placeholders during the all-template render sweep only warn at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:591), so warnings alone do not fail CI. Temp cleanup is inline, not trap-protected, for the temporary directories created inside this script.
- validation_or_tests: This file is itself validation. It also tests assigned template `prompt-template/block/wrong-round-number.md` by rendering `ACTION=edit`, `CLAUDE_ROUND=3`, `FILE_TYPE=summary`, `CURRENT_ROUND=5`, and `CORRECT_PATH=/tmp/round-5-summary.md`, then checking for the title, rendered wrong path, current round, and correct path at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:498). Related narrower coverage exists in `tests/test-template-loader.sh`, which has a real-template integration test for the same wrong-round template at [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-loader.sh:147).
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-077 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR round-number invariant. It explains that an attempted action targets `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md` while the active round is `{{CURRENT_ROUND}}`, gives the computed correct path, and forbids manually incrementing the round number.
- algorithmic_behavior: The file is a pure markdown template with five placeholders: `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. It renders through the shared single-pass template loader, producing a deterministic block message. The title and invariant sentence are defined at [prompt-template/block/wrong-round-number.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/block/wrong-round-number.md:1); the corrective path is emitted at [prompt-template/block/wrong-round-number.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/block/wrong-round-number.md:5).
- inputs_outputs_state: Inputs are supplied by edit/write validators after they parse the attempted path and current loop round: attempted action label, extracted attempted round, target file type, current round, and active-loop correct path. Output is markdown sent to stderr by validators. The template itself has no persistent state; it participates in a state transition where an invalid attempted edit/write is blocked and the agent is redirected to the current round’s summary file.
- gates_or_invariants: The enforced invariant is “summary file round must equal current RLCR round unless allowlisted.” `hooks/loop-edit-validator.sh` applies this when editing summary files: it extracts the filename, derives `CLAUDE_ROUND`, compares it to `CURRENT_ROUND`, computes `CORRECT_PATH`, renders this template, and exits `2` at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-edit-validator.sh:124). `hooks/loop-write-validator.sh` applies the same gate for write operations at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-write-validator.sh:181).
- dependencies_and_callers: Rendered through `load_and_render_safe` in both `hooks/loop-edit-validator.sh` and `hooks/loop-write-validator.sh`, with inline fallback messages available if the template is missing. It depends on caller-side helpers such as `is_round_file_type`, `extract_round_number`, `is_allowlisted_file`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` to decide when the template should be shown. It is directly exercised by `tests/test-templates-comprehensive.sh` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-templates-comprehensive.sh:498) and `tests/test-template-loader.sh` at [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-loader.sh:151).
- edge_cases_or_failure_modes: If caller extraction fails and `CLAUDE_ROUND` is empty, validators do not render this block. If `is_allowlisted_file` returns true, the wrong-round gate is bypassed. If a caller omits a placeholder assignment, the loader leaves that placeholder literal in the output. The template says not to increment the round number manually but does not itself explain how `CURRENT_ROUND` is derived; correctness depends on upstream loop state and path normalization.
- validation_or_tests: Comprehensive template tests assert that rendering this template includes “Wrong Round Number,” the attempted `round-3-summary` path, the current round value, and `/tmp/round-5-summary.md`. The general syntax sweep also validates that its placeholders conform to the uppercase `{{VAR}}` syntax. The narrower loader test checks for `round-3-summary.md` and the rendered current round.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item evidence headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`