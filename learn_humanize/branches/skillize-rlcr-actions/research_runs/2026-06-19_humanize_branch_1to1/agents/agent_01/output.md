# agent_01 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is a Claude Code plugin implementing the Humanize RLCR loop: Claude executes a plan, local hooks enforce loop invariants, Codex independently reviews each attempted exit, and loop state advances through round, finalize, completion, stop, cancel, or max-iteration outcomes. Recursive inventory found plugin metadata, CI, commands, skills, setup/monitor scripts, hook libraries, hook entrypoints, prompt templates, and executable tests, totaling 16,647 inspected lines.
- algorithmic_behavior: `commands/start-rlcr-loop.md` delegates to `skills/start-rlcr-loop/SKILL.md`, which runs `scripts/setup-rlcr-loop.sh`; that script parses options, validates plan/git/codex prerequisites, creates `.humanize/rlcr/<timestamp>/state.md`, backs up `plan.md`, initializes `goal-tracker.md`, and emits `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:104`, `scripts/setup-rlcr-loop.sh:211`, `scripts/setup-rlcr-loop.sh:485`, `scripts/setup-rlcr-loop.sh:602`). `hooks/hooks.json` wires `UserPromptSubmit`, `PreToolUse` for Read/Write/Edit/Bash, and Stop (`hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`). `hooks/loop-codex-stop-hook.sh` is the transition engine: validate state, block on unfinished work, build a Codex prompt, run Codex, parse final markers, and either enter finalize, stop, or generate the next round (`hooks/loop-codex-stop-hook.sh:252`, `hooks/loop-codex-stop-hook.sh:695`, `hooks/loop-codex-stop-hook.sh:839`, `hooks/loop-codex-stop-hook.sh:962`, `hooks/loop-codex-stop-hook.sh:1024`, `hooks/loop-codex-stop-hook.sh:1057`).
- inputs_outputs_state: Inputs are a relative plan file plus options, Claude tool-hook JSON, transcript JSONL, git status, `state.md` frontmatter, `goal-tracker.md`, round summaries, and Codex review output. Outputs are loop files under `.humanize/rlcr/<timestamp>/`, rendered block JSON for hooks, Codex debug logs under `$HOME/.cache/humanize/...`, monitor UI output, and renamed state files such as `finalize-state.md`, `complete-state.md`, `stop-state.md`, `maxiter-state.md`, or `cancel-state.md`. State parsing is centralized in `hooks/lib/loop-common.sh:112`; active-loop discovery selects only the newest timestamp directory with active state (`hooks/lib/loop-common.sh:63`).
- gates_or_invariants: Plan path must be relative, inside the project, non-symlinked, no spaces/metacharacters, readable, and contentful (`scripts/setup-rlcr-loop.sh:227`, `scripts/setup-rlcr-loop.sh:278`, `scripts/setup-rlcr-loop.sh:362`). Branch, plan tracking, and plan backup integrity are rechecked during prompts and stop handling (`hooks/loop-plan-file-validator.sh:52`, `hooks/loop-codex-stop-hook.sh:145`, `hooks/loop-codex-stop-hook.sh:178`). Tool validators block stale round files, prompt/state edits, todos file bypasses, goal-tracker writes after round 0, Bash redirection bypasses, `.humanize` git staging, and push unless enabled (`hooks/loop-read-validator.sh:36`, `hooks/loop-write-validator.sh:105`, `hooks/loop-edit-validator.sh:86`, `hooks/loop-bash-validator.sh:63`, `hooks/loop-bash-validator.sh:81`, `hooks/loop-bash-validator.sh:98`, `hooks/loop-bash-validator.sh:308`). Stop hook blocks incomplete todos, dirty git state, large changed files, missing summaries, and uninitialized round-0 goal tracker before Codex review (`hooks/loop-codex-stop-hook.sh:258`, `hooks/loop-codex-stop-hook.sh:316`, `hooks/loop-codex-stop-hook.sh:346`, `hooks/loop-codex-stop-hook.sh:529`, `hooks/loop-codex-stop-hook.sh:563`).
- dependencies_and_callers: External dependencies are Claude Code plugin hooks, `jq`, `git`, `codex`, `python3`, POSIX shell tools, and optional `zsh`. `scripts/portable-timeout.sh` abstracts `gtimeout`, GNU `timeout`, or Python timeout fallback (`scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:33`). `hooks/lib/template-loader.sh` provides single-pass `{{VAR}}` rendering to avoid placeholder injection (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:71`). `prompt-template/{block,claude,codex}` supplies all user-facing transition prompts; `tests/` validates hooks, parser behavior, templates, monitor safety, skills, and lifecycle rules; CI invokes targeted test suites in `.github/workflows/*`.
- edge_cases_or_failure_modes: Missing active loop exits cleanly; older timestamp directories are ignored to avoid reviving stale sessions (`hooks/lib/loop-common.sh:58`). Git timeout/failure is fail-closed in plan and stop checks (`hooks/loop-plan-file-validator.sh:92`, `hooks/loop-codex-stop-hook.sh:319`). Bash validation has special handling for state-file source moves, shell wrappers, redirections, ANSI-C quoting, and authorized cancellation via `.cancel-requested` (`hooks/loop-bash-validator.sh:128`, `hooks/loop-bash-validator.sh:167`, `hooks/lib/loop-common.sh:274`). Codex failure modes include missing command, nonzero exit, absent review file, empty file, and stdout fallback (`hooks/loop-codex-stop-hook.sh:755`, `hooks/loop-codex-stop-hook.sh:878`, `hooks/loop-codex-stop-hook.sh:897`, `hooks/loop-codex-stop-hook.sh:943`). This branch export has no usable `.git` checkout for git inspection, so supplied commit metadata was treated as authoritative.
- validation_or_tests: `tests/run-all-tests.sh` orchestrates 20 suites and includes the assigned ANSI parser suite (`tests/run-all-tests.sh:32`, `tests/run-all-tests.sh:43`, `tests/run-all-tests.sh:102`). I did not run the full suite because many tests create fixtures and this run is notes-only in a read-only export. I did run the assigned non-mutating executable spec: `bash tests/test-ansi-parsing.sh`, result `Passed: 8`, `Failed: 0`, exit 0.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-031 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the aggregate test runner’s ANSI-stripping parser. It protects `tests/run-all-tests.sh`, which captures colored child-suite output and extracts numeric pass/fail totals after stripping color codes (`tests/run-all-tests.sh:102`).
- algorithmic_behavior: The test defines colored output helpers, then repeatedly feeds strings containing ESC SGR sequences through `sed "s/${esc}\\[[0-9;]*m//g"` using `esc=$'\033'` (`tests/test-ansi-parsing.sh:38`, `tests/test-ansi-parsing.sh:45`, `tests/test-ansi-parsing.sh:47`). It validates direct stripping and the downstream count extraction pipeline using `grep -oE 'Passed:[[:space:]]*[0-9]+'`, `grep -oE '[0-9]+$'`, and in multiline cases `tail -1` (`tests/test-ansi-parsing.sh:74`, `tests/test-ansi-parsing.sh:88`, `tests/test-ansi-parsing.sh:116`).
- inputs_outputs_state: Inputs are hard-coded colored and plain strings for pass/fail summaries. Outputs are colored `PASS` or `FAIL` lines plus a final summary. Internal state is limited to `TESTS_PASSED` and `TESTS_FAILED`, incremented in `pass()` and `fail()` (`tests/test-ansi-parsing.sh:18`, `tests/test-ansi-parsing.sh:21`, `tests/test-ansi-parsing.sh:26`). The process exits 0 only when the failure counter remains zero (`tests/test-ansi-parsing.sh:170`).
- gates_or_invariants: Expected stripped output must match exact strings for basic and multi-color inputs; count extraction must return the expected numeric value for pass counts, failure counts, zero, multiline output, plain text, and bold-plus-color SGR codes (`tests/test-ansi-parsing.sh:49`, `tests/test-ansi-parsing.sh:63`, `tests/test-ansi-parsing.sh:77`, `tests/test-ansi-parsing.sh:91`, `tests/test-ansi-parsing.sh:105`, `tests/test-ansi-parsing.sh:126`, `tests/test-ansi-parsing.sh:140`, `tests/test-ansi-parsing.sh:154`).
- dependencies_and_callers: Depends on Bash ANSI-C quoting, portable BSD/GNU `sed`, `grep -E`, and `echo -e`. Called by the aggregate runner at `tests/run-all-tests.sh:43`; CI indirectly exercises it when the full test runner is used, while related workflow gates also run targeted template/skill/plan suites.
- edge_cases_or_failure_modes: The stripping regex handles CSI SGR forms with digits and semicolons ending in `m`; it intentionally does not cover non-SGR escape sequences such as cursor motion or erase-line controls. The parser defaults to `0` if grep finds no count, which avoids shell failure but can mask malformed output unless the suite exit code or expected assertion catches it (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`).
- validation_or_tests: Direct run completed successfully: all 8 subtests passed and 0 failed. The covered cases are basic color, multiple colors, passed count, failed count, zero count, multiline suite summary, no ANSI codes, and bold/color combined.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-061 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Block-template contract for the goal-tracker ownership gate after round 0. It tells Claude that direct `goal-tracker.md` mutation is forbidden and converts attempted mutation into a structured request that Codex may evaluate.
- algorithmic_behavior: Template variables are `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`. Rendered content states that after round 0 only Codex can modify the Goal Tracker, direct Write/Edit/Bash mutation is disallowed, and Claude must add a `Goal Tracker Update Request` section to the current summary with requested changes plus justification (`prompt-template/block/goal-tracker-modification.md:1`, `prompt-template/block/goal-tracker-modification.md:3`, `prompt-template/block/goal-tracker-modification.md:9`, `prompt-template/block/goal-tracker-modification.md:14`, `prompt-template/block/goal-tracker-modification.md:21`).
- inputs_outputs_state: Input is a blocked Write/Edit/Bash attempt targeting `goal-tracker.md` after `current_round` is greater than zero. Output is Markdown sent to stderr by PreToolUse validators; it does not mutate state itself. The state transition is procedural: Claude records a request in `round-N-summary.md`, then Codex receives responsibility to approve, reject, or apply changes during review.
- gates_or_invariants: The invariant is role separation: Claude may initialize or directly maintain the tracker only in round 0, but later rounds require Codex-mediated updates. The immutable Goal/Acceptance Criteria section remains protected by the Codex review template as well (`prompt-template/codex/goal-tracker-update-section.md:3`, `prompt-template/codex/goal-tracker-update-section.md:10`).
- dependencies_and_callers: Rendered by `goal_tracker_blocked_message()` with safe fallback in `hooks/lib/loop-common.sh:603`. Called by Write validation at `hooks/loop-write-validator.sh:145`, Edit validation at `hooks/loop-edit-validator.sh:114`, and Bash validation at `hooks/loop-bash-validator.sh:308`. The downstream Codex prompt section that handles requests is loaded into both regular and full-alignment reviews at `hooks/loop-codex-stop-hook.sh:677`.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` emits a fallback that preserves the same core instruction (`hooks/lib/loop-common.sh:606`). Bash in round 0 uses a different block that instructs Claude to use Write/Edit instead of Bash (`hooks/loop-bash-validator.sh:308`, `prompt-template/block/goal-tracker-bash-write.md:1`). Write/Edit round-0 direct edits are allowed so initial tracker setup can complete.
- validation_or_tests: Template reference coverage explicitly includes `block/goal-tracker-modification.md` (`tests/test-template-references.sh:157`). Comprehensive template tests validate real Codex goal-tracker update rendering (`tests/test-templates-comprehensive.sh:531`). The related validator behavior is exercised through write/edit/bash and goal-tracker pattern suites, including modification detection for redirection, `tee`, in-place editors, file operations, and false-positive read commands (`tests/test-bash-validator-patterns.sh:70`, `tests/test-bash-validator-patterns.sh:83`, `tests/test-bash-validator-patterns.sh:96`, `tests/test-bash-validator-patterns.sh:110`, `tests/test-bash-validator-patterns.sh:136`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique assigned item headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`