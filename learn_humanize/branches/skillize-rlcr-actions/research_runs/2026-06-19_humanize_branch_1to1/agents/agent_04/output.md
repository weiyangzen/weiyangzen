# agent_04 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-004 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Declarative prompt and gate-message corpus for the RLCR loop’s state machine. The directory supplies the markdown payloads that validators and the Codex stop hook render into block reasons, Codex review prompts, Claude next-round prompts, and finalize-phase instructions. The runtime loader resolves this directory from `hooks/lib` to `prompt-template` in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:24), and requires the `block`, `codex`, and `claude` subdirectories in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:208).
- algorithmic_behavior: The `block/` children encode hard-stop transitions and validator feedback: wrong round/path, forbidden state/prompt/summary/goal-tracker writes, plan mutation, incomplete todos, git status/clean/push gates, unpushed commits, large files, schema drift, and missing summaries. The `codex/` children encode review policy: regular review, full alignment every 5 rounds, and Codex-owned goal-tracker updates; `regular-review.md` only allows `COMPLETE` when all original tasks are fully complete with no deferrals in [prompt-template/codex/regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/codex/regular-review.md:49), while `full-alignment-review.md` adds acceptance-criteria audit, forgotten-item detection, deferred-item audit, and a stagnation `STOP` circuit breaker in [prompt-template/codex/full-alignment-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/codex/full-alignment-review.md:19). The `claude/` children encode forward transitions: next-round work prompt, footer, post-alignment action items, push-every-round note, goal-tracker update request format, and finalize-phase simplification/refactor instructions.
- inputs_outputs_state: Inputs are `{{VAR}}` substitutions such as `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `REVIEW_RESULT_FILE`, `MAX_LINES`, and `LARGE_FILES`. Syntax and single-pass semantics are documented in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:7); rendering is implemented with environment-prefixed variables plus awk scanning in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:58). Outputs are markdown strings returned as hook block reasons or written to loop files such as `round-N-review-prompt.md` and `round-N-prompt.md`; the stop hook builds Codex review prompts in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:671), writes next-round prompts after incrementing `current_round` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:1057), and emits finalize-phase block JSON in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:1005).
- gates_or_invariants: Template directory integrity requires `block/`, `codex/`, and `claude/`; missing templates must not produce empty critical user guidance because callers use `load_and_render_safe` with fallbacks in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:170). Rendering is intentionally single-pass to prevent injected `{{OTHER_VAR}}` content from being recursively substituted. Review templates enforce “no deferrals as completion,” goal-tracker immutability for Claude after Round 0, and `STOP` only as a circuit-breaker marker. Block templates enforce filesystem and workflow boundaries before the loop can proceed.
- dependencies_and_callers: Primary loader is `hooks/lib/template-loader.sh`; shared message wrappers are in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/loop-common.sh:190). Direct callers include `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh`. The user-facing RLCR flow is described as Claude implements, Codex reviews, and feedback loops until completion in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/README.md:58); setup initializes the plan/model/round state consumed by templates in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/setup-rlcr-loop.sh:43).
- edge_cases_or_failure_modes: Missing template file or directory falls back or returns empty depending on loader function; empty/unclosed placeholders remain literal; regex and shell metacharacters in values must render literally; multiline substitution is expected to work; unavailable template subdirectories degrade common hooks to inline fallback warnings. Template reference drift is high risk because validators may block an action but return unclear or empty reasons; this is why `tests/test-template-references.sh` scans shell-script references and checks critical validators use `load_and_render_safe` in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-references.sh:172).
- validation_or_tests: Coverage includes `tests/test-template-loader.sh` for loader/render behavior, `tests/test-template-references.sh` for missing-reference detection, `tests/test-templates-comprehensive.sh` for fallback and real-template integration, and `tests/test-error-scenarios.sh` for graceful error behavior. `tests/run-all-tests.sh` includes `test-error-scenarios.sh` in the suite order at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/run-all-tests.sh:32). I did static inspection only and did not execute tests in this read-only research run.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-034 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for error-tolerant template loading and rendering. It protects the RLCR hook layer from crashing or emitting empty/garbled guidance when a template is missing, a directory is invalid, placeholders are malformed, or strict shell mode is active.
- algorithmic_behavior: The test sources the template loader and computes `TEMPLATE_DIR` in [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-error-scenarios.sh:11), then drives 12 scenarios with manual pass/fail counters. Tests 1-3 assert missing files/directories and missing `load_and_render` templates return empty output with exit 0. Test 4 asserts empty input renders empty. Test 5 verifies special regex characters in values render literally. Test 6 runs an isolated `set -euo pipefail` subshell and expects `SCRIPT_COMPLETED`, proving missing templates do not abort strict-mode hook scripts. Tests 7-8 verify safe fallback and fallback variable substitution. Tests 9-10 validate template-dir accept/reject behavior. Tests 11-12 assert empty `{{}}` and unclosed `{{` placeholders remain unchanged.
- inputs_outputs_state: Inputs are `PROJECT_ROOT`, loaded `hooks/lib/template-loader.sh`, computed `TEMPLATE_DIR`, in-memory template strings, fallback strings, and `VAR=value` render assignments. Outputs are colored `PASS`/`FAIL` lines, a summary, and process exit 0 if `TESTS_FAILED == 0` or exit 1 otherwise in [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-error-scenarios.sh:218). State is limited to `TESTS_PASSED` and `TESTS_FAILED` counters updated by helper functions in [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-error-scenarios.sh:22).
- gates_or_invariants: Missing non-safe loads must be nonfatal and empty; safe loads must use fallback text; fallback text must itself be renderable; invalid template directories must return nonzero; malformed placeholders must not be consumed or partially rewritten. The strict-mode invariant in Test 6 is especially important because production hooks commonly run under `set -euo pipefail`, and a nonzero missing-template path would abort the validator instead of returning a controlled block reason.
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`, especially `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. It is included in the aggregate test harness in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/run-all-tests.sh:42). It complements broader positive-path and stress coverage in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`.
- edge_cases_or_failure_modes: Catches regressions where `cat`/loader failures leak nonzero exits, where fallback rendering is skipped, where regex-like values such as `[test] (foo) *bar*` are interpreted instead of literal, where strict-mode subshells terminate early, or where placeholder parsing corrupts `{{}}` / `{{UNCLOSED`. The test uses `set -uo pipefail` at top level rather than `set -e`, so individual assertions can record failures and continue; the strict-mode behavior is isolated inside the subshell at [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-error-scenarios.sh:114).
- validation_or_tests: This file is itself validation. It asserts expected output and exit codes after every call, then fails the suite if any counter is nonzero. I inspected it directly but did not run it.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-064 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Prompt/block template for the large-file exit gate. It converts a detected list of oversized changed files into a blocking instruction that requires modularization before the RLCR loop can stop or proceed to expensive Codex review.
- algorithmic_behavior: The template renders a title, the `{{MAX_LINES}}` threshold, a multiline `{{LARGE_FILES}}` list, rationale, and required actions. For code files it requires splitting into smaller modules, preserving behavior, optional `code-simplifier` review, and clear module boundaries in [prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/block/large-files.md:13). For documentation files it requires logical splits, cross-references, narrative continuity, and TOC/navigation updates in [prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/prompt-template/block/large-files.md:19).
- inputs_outputs_state: Inputs are `MAX_LINES` and `LARGE_FILES`, supplied by `hooks/loop-codex-stop-hook.sh`. The stop hook hardcodes `MAX_LINES=2000`, scans cached `git status --porcelain`, classifies file extensions as code or documentation, counts lines, appends bullets for files above the limit, renders this template via `load_and_render_safe`, and outputs JSON with `"decision": "block"` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:346). The template itself has no persistent state; the state transition is that exit/review is blocked until the oversized changed files are split and committed.
- gates_or_invariants: Gate applies only when `GIT_IS_REPO == true` and changed/new files from `GIT_STATUS_CACHED` exceed 2000 lines. The hook skips empty status lines, deleted files, unsupported extensions, and nonnumeric `wc -l` results; it handles rename status by keeping the new path in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:360). The template-level invariant is behavior preservation for code splits and reference/navigation preservation for documentation splits.
- dependencies_and_callers: Called only through the Codex stop hook’s large-file quick check in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:340). It depends on `load_and_render_safe` fallback behavior from `hooks/lib/template-loader.sh`; if this markdown file is absent, the hook uses an inline fallback but loses the richer remediation instructions.
- edge_cases_or_failure_modes: The gate does not inspect unchanged large files, non-git contexts, unsupported extensions, or generated files that do not appear in porcelain status. A `git status` timeout/failure is handled earlier as a fail-closed git-status block, not by this template. Large paths with unusual porcelain quoting may depend on the hook’s simple `${line#???}` parsing. Multiline `LARGE_FILES` must render intact, which is covered by template-rendering tests for multiline values.
- validation_or_tests: Template reference existence is covered by `tests/test-template-references.sh`; loader fallback and real-template rendering behavior is covered by `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and the assigned error-scenario test. I did not find a dedicated test that fabricates a >2000-line changed file and asserts this exact block template path is used.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item headings present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`