# agent_19 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-019 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for RLCR Bash commands. It is registered for the `Bash` matcher in `hooks/hooks.json:42-48` and acts as a command-level guard that preserves RLCR loop state, prompt, summary, todo, goal-tracker, and local `.humanize` invariants before a shell command can run.
- algorithmic_behavior: Reads JSON hook input from stdin, exits immediately unless `.tool_name` is `Bash`, extracts `.tool_input.command`, lowercases it, finds the newest active loop under `.humanize/rlcr`, parses `state.md` or `finalize-state.md`, then applies ordered block checks (`hooks/loop-bash-validator.sh:22-54`). Its block decisions use exit code `2` plus rendered template/fallback text; allow decisions exit `0`.
- inputs_outputs_state: Input is Claude hook JSON plus environment-derived project root via `CLAUDE_PROJECT_DIR` or `pwd` (`hooks/loop-bash-validator.sh:22-38`). State input is the active loop frontmatter parsed through `parse_state_file`, especially `current_round` and `push_every_round` (`hooks/loop-bash-validator.sh:45-63`). Output is either no stdout/stderr and exit `0`, or an explanatory stderr block and exit `2`.
- gates_or_invariants: Blocks `git push` unless `push_every_round: true` (`hooks/loop-bash-validator.sh:60-72`); blocks `git add` patterns that would include `.humanize` (`hooks/loop-bash-validator.sh:75-84`); blocks writes/removes/copies/moves involving `finalize-state.md` before generic `state.md` because the generic pattern also matches it (`hooks/loop-bash-validator.sh:87-115`). It also blocks source-side `mv`/`cp` of state files by splitting shell operators and stripping leading redirections before matching (`hooks/loop-bash-validator.sh:117-263`), then catches `sh -c` / `bash -c` wrappers (`hooks/loop-bash-validator.sh:265-287`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file`, `to_lower`, `git_adds_humanize`, `command_modifies_file`, `is_cancel_authorized`, and rendered block messages (`hooks/loop-bash-validator.sh:14-17`; helpers at `hooks/lib/loop-common.sh:63-143`, `274-413`, `443-613`). It depends on `jq`, `grep`, `sed`, `find`, and template loading via `load_and_render_safe`.
- edge_cases_or_failure_modes: No active loop means all commands are allowed (`hooks/loop-bash-validator.sh:40-43`). Cancel is the only authorized state-file move, requiring `.cancel-requested`, no command substitution/backticks/newlines/chaining, exactly two args, source exactly active `state.md` or `finalize-state.md`, and destination exactly active `cancel-state.md` (`hooks/lib/loop-common.sh:274-413`). The source-side state-file bypass detector handles quoted relative names, sudo/env/command prefixes, shell chaining, wrappers, and redirection-prefixed commands (`hooks/loop-bash-validator.sh:117-287`). A notable limitation inherited from `command_modifies_file`: common two-argument `cp src target` is covered, but multi-source copy detection is intentionally incomplete in tests (`tests/test-bash-validator-patterns.sh:162-168`).
- validation_or_tests: Pattern-level coverage lives in `tests/test-bash-validator-patterns.sh`, including redirects, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, and read-only false positives (`tests/test-bash-validator-patterns.sh:75-194`). Cancel/state-file bypass coverage is extensive in `tests/test-cancel-signal-file.sh`, including quoted paths, sudo/env/command prefixes, chained operators, shell wrappers, redirection prefixes, ANSI-C quoting, wrong-directory cancel moves, and no-active-loop behavior. Finalize-state parsing for this validator is covered in `tests/test-finalize-phase.sh:626-637`.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-049 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh safety specification for the humanize monitor path. It verifies that monitor helper algorithms avoid zsh `nomatch` failures when RLCR session/cache directories are empty, hidden-only, or lacking expected files.
- algorithmic_behavior: Runs under zsh with `set -euo pipefail`, creates an isolated `/tmp/test-zsh-humanize-$$` project, sources `scripts/humanize.sh`, then exercises local copies of monitor helper loops that intentionally use `find` instead of shell globs (`tests/test-zsh-monitor-safety.sh:1-15`, `48-73`, `86-120`, `132-160`, `172-208`, `220-238`). It tracks pass/fail counters and exits nonzero if any assertion fails (`tests/test-zsh-monitor-safety.sh:24-36`, `329-344`).
- inputs_outputs_state: Inputs are the repository under test, zsh runtime, temporary `.humanize/rlcr` directories, dotfiles, timestamped session directories, and a temporary `$HOME/.cache/humanize/test-project/...` cache path (`tests/test-zsh-monitor-safety.sh:48-56`, `129-130`, `217-240`, `249-276`). Output is colored PASS/FAIL console evidence plus final exit code.
- gates_or_invariants: The central invariant is that empty or nonmatching directories must produce empty results or zero counts, not zsh “no matches found” errors. It verifies sourcing `humanize.sh`, latest-session discovery, dotfile-only handling, missing `*-state.md`, missing `round-*-codex-run.log`, chronological latest-session selection, actual zsh execution, and a demonstration that the old glob pattern is unsafe while the new `find` pattern is safe (`tests/test-zsh-monitor-safety.sh:58-324`).
- dependencies_and_callers: Directly depends on `scripts/humanize.sh`, especially `_find_latest_session`, `_find_latest_codex_log`, and `_find_state_file`. The real implementation uses `find "$loop_dir" -mindepth 1 -maxdepth 1 -type d`, project-cache lookup, and `find "$session_dir" -maxdepth 1 -name '*-state.md' -type f` (`scripts/humanize.sh:27-49`, `51-109`, `111-150`). The test mirrors these internals instead of invoking `_humanize_monitor_codex` interactively, which makes it suitable for CI-style validation of helper behavior.
- edge_cases_or_failure_modes: Empty loop dir, hidden-file-only loop dir, no state files, empty cache dir, multiple timestamped session dirs, and old glob behavior are explicitly modeled. The test also validates that a non-zsh invocation would fail the shell identity gate (`tests/test-zsh-monitor-safety.sh:279-289`). It cleans up its temp project and cache path on exit (`tests/test-zsh-monitor-safety.sh:52-56`, `240`).
- validation_or_tests: This file is itself the validation asset. Related broader coverage appears in `tests/test-monitor-e2e-real.sh`, which checks monitor output for `no matches found|bad pattern`, and `tests/test-humanize-escape.sh`, which separately validates `find`-based `*-state.md` iteration.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-079 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude prompt template for the RLCR Finalize Phase. It defines the algorithmic transition contract after Codex review reports `COMPLETE`: refactor/simplify only, preserve behavior, run tests, complete todos, commit, and write `finalize-summary.md`.
- algorithmic_behavior: The template tells Claude that Codex review has passed and the implementation/acceptance criteria are complete, then delegates simplification to `code-simplifier:code-simplifier` via the Task tool (`prompt-template/claude/finalize-phase-prompt.md:1-14`). It constrains the phase to functionality-equivalent cleanup and names the expected evidence surfaces: todos completed, descriptive commit, summary file, simplification notes, modified files, and test confirmation (`prompt-template/claude/finalize-phase-prompt.md:16-49`).
- inputs_outputs_state: Inputs are rendered placeholders `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:35-44`). The caller sets `FINALIZE_SUMMARY_FILE="$LOOP_DIR/finalize-summary.md"` and renders the prompt with those variables (`hooks/loop-codex-stop-hook.sh:983-1008`). Expected output state is a written finalize summary and committed cleanup changes; loop state is already converted from `state.md` to `finalize-state.md` by the caller before the prompt is returned (`hooks/loop-codex-stop-hook.sh:971-981`).
- gates_or_invariants: Non-negotiable constraints are no functionality change, no failing tests, no new bugs, and cleanup-only refactoring (`prompt-template/claude/finalize-phase-prompt.md:16-24`). The downstream stop hook treats Finalize Phase as special: once `finalize-state.md` exists, it expects `finalize-summary.md` instead of a round summary and can complete the loop only after checks pass (`hooks/loop-codex-stop-hook.sh:522-526`, `646-655`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` when the last non-empty Codex review line is exactly `COMPLETE`, unless current round has reached max iterations (`hooks/loop-codex-stop-hook.sh:955-969`). Rendering goes through `load_and_render_safe`, with an inline fallback prompt if the template is missing (`hooks/loop-codex-stop-hook.sh:983-1008`). It also depends procedurally on the availability of a Task tool and `code-simplifier:code-simplifier` agent, though the template does not enforce agent availability itself.
- edge_cases_or_failure_modes: If `COMPLETE` arrives at max iterations, Finalize Phase is skipped and the loop ends as max-iteration state (`hooks/loop-codex-stop-hook.sh:961-969`). If the template is missing or invalid, fallback text preserves the essential finalize contract (`hooks/loop-codex-stop-hook.sh:985-1005`). The prompt can over-assume the simplifier agent exists; the only explicit instruction is to use it, not a fallback if unavailable.
- validation_or_tests: `tests/test-finalize-phase.sh` verifies that `COMPLETE` triggers `state.md -> finalize-state.md`, returns a blocking finalize prompt mentioning simplification, and skips finalize at max iterations (`tests/test-finalize-phase.sh:460-496`). The same suite validates Finalize Phase todo gating and validator compatibility with `finalize-state.md` (`tests/test-finalize-phase.sh:499-530`, `617-665`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence headings present; each assigned item appears once as a heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`