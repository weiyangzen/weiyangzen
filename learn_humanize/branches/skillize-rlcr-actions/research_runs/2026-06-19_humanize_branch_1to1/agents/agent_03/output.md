# agent_03 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-003 `directory` `hooks`
- cursor: `[_]`
- core_role: Claude Code hook layer for the RLCR loop. `hooks/hooks.json:4-63` wires `UserPromptSubmit`, `PreToolUse` validators for `Write`, `Edit`, `Read`, `Bash`, and a long-running `Stop` hook. The directory enforces loop-state integrity, current-round file routing, protected local state, cancellation safety, pre-stop quality gates, and Codex review-driven round transitions.
- algorithmic_behavior: Shared functions in `hooks/lib/loop-common.sh` locate only the newest active `.humanize/rlcr/<timestamp>` loop with `state.md` or `finalize-state.md` (`63-81`), parse YAML-like frontmatter fields (`112-139`), identify round files (`148-165`), enforce a small allowlist (`170-188`), authorize exactly-scoped cancel moves (`274-413`), detect unsafe `git add` of `.humanize` (`443-539`), classify shell file modifications (`571-599`), and terminate by renaming state to a reason-specific state file (`622-648`). `hooks/lib/template-loader.sh` resolves `prompt-template`, renders single-pass `{{VAR}}` placeholders to avoid placeholder injection (`58-132`), and falls back when templates are missing (`170-203`).
- inputs_outputs_state: Inputs are Claude hook JSON from stdin, `CLAUDE_PROJECT_DIR`, Git state, `.humanize/rlcr` state files, loop prompt/summary/todo files, transcript JSONL, and prompt templates. Outputs are either exit `0` allow decisions, exit `2` blocking stderr for `PreToolUse`, JSON `{decision:"block", reason, systemMessage}` for prompt/stop hooks, generated review prompts/results, next-round prompts, and state-file renames. `check-todos-from-transcript.py` reads `transcript_path`, finds the latest `TodoWrite` payload across supported transcript shapes (`20-87`), exits `1` with `INCOMPLETE_TODOS` when any status is not `completed` (`113-126`), and exits `2` on hook-input parse error (`90-97`).
- gates_or_invariants: `loop-read-validator.sh` blocks todo reads unless allowlisted (`36-43`), blocks stale/wrong-location prompt or summary reads (`100-145`). `loop-write-validator.sh` blocks todo and prompt writes (`37-50`), state/finalize-state writes (`105-113`), `.humanize/rlcr/plan.md` backup writes (`132-138`), post-round-zero goal tracker writes (`145-149`), and summary writes outside the active loop/current round (`155-216`). `loop-edit-validator.sh` applies the same edit-side protections for todos/prompts/state/plan/goal tracker/current summary (`36-149`). `loop-bash-validator.sh` blocks `git push` unless enabled (`63-72`), `.humanize` git-add leakage (`81-84`), state/finalize-state modifications (`98-115`), state-file source moves/copies via segmented shell parsing (`128-287`), plan/goal/prompt/summary/todo modifications via shell (`295-352`), and only permits cancel when `is_cancel_authorized` accepts the exact move.
- dependencies_and_callers: `hooks.json` is the caller map consumed by Claude plugin hook registration. All shell validators source `hooks/lib/loop-common.sh`; common sources `template-loader.sh`. `loop-plan-file-validator.sh` and `loop-codex-stop-hook.sh` additionally source `scripts/portable-timeout.sh` for bounded Git/Codex operations. The stop hook calls `check-todos-from-transcript.py` before expensive review (`hooks/loop-codex-stop-hook.sh:258-306`). Templates under `prompt-template/block`, `prompt-template/claude`, and `prompt-template/codex` provide user-facing block and review prompt text. Related tests include `tests/test-cancel-signal-file.sh`, `tests/test-hook-preservation.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-todo-checker.sh`, and `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: Cancel authorization rejects command substitution, backticks, newlines, shell operators, extra args, remaining `$` variables after `$loop_dir` normalization, wrong source/destination paths, and wrong signal-file location (`loop-common.sh:290-410`). Bash validator strips leading redirections including `&>`, `&>>`, fd redirections, quoted targets, escaped spaces, and ANSI-C quoted targets before detecting `mv`/`cp` state-file sources (`loop-bash-validator.sh:128-243`). Stop hook fails closed on invalid model/effort/state numbers (`loop-codex-stop-hook.sh:102-124`), outdated state schema (`131-143`), branch drift (`149-176`), missing/modified/deleted plan backup or source (`182-250`), todo checker parse errors (`265-281`), Git status failure (`316-337`), large changed files over 2000 lines (`346-420`), dirty/unpushed Git state (`430-516`), missing summaries (`522-554`), uninitialized Round 0 goal tracker placeholders (`563-630`), Codex command absence (`755-775`), Codex timeout/failure or missing/empty review file (`838-950`).
- validation_or_tests: Directory behavior is covered by shell tests referenced above. Hook registration is asserted in `tests/test-hook-preservation.sh` per cross-reference output; template references are checked by `tests/test-template-references.sh`; todo checker behavior by `tests/test-todo-checker.sh`; plan/finalize behavior by `tests/test-plan-file-hooks.sh` and `tests/test-finalize-phase.sh`; cancellation hardening by the assigned `tests/test-cancel-signal-file.sh`. I did not execute tests because this task requested research notes only in a read-only branch export.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-033 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the cancel-signal security boundary around `loop-bash-validator.sh` and `is_cancel_authorized`. It proves that cancellation is allowed only through the intended signal-file-plus-state-rename path and that state-file tampering remains blocked even when a signal exists.
- algorithmic_behavior: The test creates a disposable fake project under `mktemp -d`, sources `hooks/lib/loop-common.sh`, and creates `.humanize/rlcr/2024-01-01_12-00-00/state.md` with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, and `start_branch` (`42-56`). `run_bash_validator` builds Claude-style hook JSON using `jq`, pipes it into `hooks/loop-bash-validator.sh`, and returns the validator exit code through `PIPESTATUS` (`61-71`). Each scenario resets loop state, optionally touches `.cancel-requested`, runs a command string, and asserts exit `0` for allowed cancel or exit `2` for blocked attempts.
- inputs_outputs_state: Inputs are synthesized Bash commands and generated hook JSON. Runtime state is `CLAUDE_PROJECT_DIR`, fake loop `state.md`, and optional `.cancel-requested`. Outputs are colored PASS/FAIL lines, `TESTS_PASSED`, `TESTS_FAILED`, and final process exit equal to failure count (`1347-1355`). The validator under test may emit block text to stderr, captured into `OUTPUT`.
- gates_or_invariants: Positive cases require `.cancel-requested` inside the active loop and exact `mv <loop>/state.md <loop>/cancel-state.md`, allowing path normalization with `/./`, single-quoted paths, and documented literal `${loop_dir}` syntax (`78-113`, `363-484`). Negative cases prove no signal means blocked (`119-133`), wrong signal directory is blocked (`199-215`), and non-cancel state mutations like redirect, `sed -i`, wrong destination, `rm`, arbitrary destination, `cp`, or extra args are blocked (`139-235`, `281-295`, `384-438`, `491-545`).
- dependencies_and_callers: Depends on Bash, `jq`, `mktemp`, core shell utilities, `hooks/lib/loop-common.sh`, and `hooks/loop-bash-validator.sh`. It indirectly covers `is_cancel_authorized` in `loop-common.sh` and Bash validator state-file modification detection. It is listed in `tests/run-all-tests.sh` per cross-reference output, so it participates in the broader repository test suite.
- edge_cases_or_failure_modes: Injection and bypass coverage is extensive: `&&`, semicolon, `$()`, backticks, newlines (`241-355`); `sudo`, `env`, `command --`, leading whitespace (`550-644`); shell chaining with `;`, `&&`, `||`, `|`, background `&`, no-space `&`, `|&` (`650-804`); `sh -c` and `bash -c` wrappers (`810-844`); redirection-prefixed commands including `2>`, `>`, `2>&1`, spaced redirects, `&>`, `>>`, `2>>`, double/single quoted targets, escaped-space targets, and ANSI-C quoted targets (`850-1204`). It also asserts unrelated `/tmp/state.md` paths are blocked despite a valid loop signal (`1210-1225`) and that commands are allowed when no active loop state exists (`1231-1247`).
- validation_or_tests: The file is itself validation. Direct helper tests verify `is_cancel_authorized` true/false behavior for correct signal and command, missing signal, wrong command, three args, quoted paths, extra arg before state, and hidden `${ifs}` variables (`1258-1341`). I did not execute it in this read-only research pass; execution would create temporary files and invoke the real validator.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-063 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Block template used by the stop hook to prevent Codex review and loop exit while Claude’s native TodoWrite list still has unfinished items.
- algorithmic_behavior: The template renders an “Incomplete Todos Detected” message, inserts `{{INCOMPLETE_LIST}}`, and instructs the agent to complete remaining todos, mark each todo completed with TodoWrite, and only then proceed to summary/stop (`prompt-template/block/incomplete-todos.md:1-12`). It is loaded in `hooks/loop-codex-stop-hook.sh:289-305` after `check-todos-from-transcript.py` returns incomplete todo evidence.
- inputs_outputs_state: Input variable is `INCOMPLETE_LIST`, produced from the todo checker output by dropping the first marker line (`loop-codex-stop-hook.sh:284-295`). Output is Markdown text embedded as the `reason` in a JSON block response with system message “Loop: Blocked - incomplete todos detected, please finish all tasks first” (`loop-codex-stop-hook.sh:297-304`). It does not mutate files or loop state directly.
- gates_or_invariants: Enforces a pre-review invariant: no stop/Codex review while TodoWrite contains any item whose status is not `completed`. The related checker treats absent transcript/todos as allow (`check-todos-from-transcript.py:99-111`) but blocks on any incomplete status (`113-126`). The template’s required action is procedural rather than parser-enforced, but it is the user-facing contract for that gate.
- dependencies_and_callers: Called by `load_and_render_safe` from `template-loader.sh`, through `loop-codex-stop-hook.sh`. Depends on `hooks/check-todos-from-transcript.py` for `INCOMPLETE_LIST` construction and on the transcript format containing TodoWrite tool calls.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` uses the inline fallback in the stop hook (`loop-codex-stop-hook.sh:289-295`). If hook input JSON cannot be parsed, stop hook emits a separate parse-error block instead of this template (`265-281`). If no todos are found, the gate allows review, so this template is not used.
- validation_or_tests: Covered by the stop hook’s todo-check path and by `tests/test-todo-checker.sh` per repository cross-reference. Template path validity is also in scope for `tests/test-template-references.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `SKILLIZE_RLCR_ACTIONS-HZ-003`, `SKILLIZE_RLCR_ACTIONS-HZ-033`, `SKILLIZE_RLCR_ACTIONS-HZ-063`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`