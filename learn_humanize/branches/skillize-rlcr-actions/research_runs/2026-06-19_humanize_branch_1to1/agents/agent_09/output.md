# agent_09 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared algorithm library for RLCR hook enforcement. The directory contains exactly two files: [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/loop-common.sh:1), which centralizes loop state parsing, active-loop discovery, path classification, shell-command mutation detection, cancel authorization, block-message rendering, and loop termination state transitions; and [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:1), which loads and renders prompt/block templates used by validators and stop hooks.
- algorithmic_behavior: [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/loop-common.sh:15) defines canonical state field names and exit reasons, then sources the template loader and resolves `TEMPLATE_DIR` with validation that warns but does not fail closed. `find_active_loop` selects only the newest timestamped loop directory and treats it as active only when `state.md` or `finalize-state.md` exists, intentionally ignoring older loop directories and renamed terminal state files. `parse_state_file` extracts YAML-like frontmatter into `STATE_*` globals with defaults for current round, max iterations, and push policy. Path helpers classify round prompt/summary/todos files, state/finalize files, goal tracker, finalize summary, and `.humanize/rlcr` membership. Message helpers render block templates with safe fallbacks. `command_modifies_file` heuristically identifies shell writes through redirection, `tee`, in-place editors, `mv/cp`, `rm`, `dd`, `truncate`, and `exec` redirection. `git_adds_humanize` detects direct or broad `git add` operations that could stage `.humanize`. `is_cancel_authorized` permits the narrow state-to-cancel rename only when `.cancel-requested` exists and the command has exactly the expected shape. `end_loop` renames the active state file to a reason-specific terminal state file. [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:24) resolves `prompt-template`, reads templates, performs single-pass `{{VAR}}` replacement via `awk` and `TMPL_VAR_` environment variables, preserves unknown placeholders, avoids recursive placeholder expansion, supplies fallback rendering through `load_and_render_safe`, and validates required template subdirectories.
- inputs_outputs_state: Inputs include hook JSON-derived paths/commands from sibling validators, active loop base directories, `state.md` or `finalize-state.md` frontmatter, `.cancel-requested`, `.humanize` existence/gitignore state, shell command strings lowercased by callers, and template names/variable assignments. Outputs are stdout values such as active loop path/current round/rendered template text; global `STATE_*` variables after parsing; shell exit status decisions used by callers; stderr block messages; and, for `end_loop`, a filesystem state transition from `state.md` to `<reason>-state.md`. The directory does not own hook JSON parsing itself; sibling scripts call it after reading hook input.
- gates_or_invariants: The newest loop directory is the only candidate active loop, preventing older directories from reviving. Terminal state files such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` are intentionally not active-loop markers. State modification blocks distinguish `finalize-state.md` before `state.md` because the latter pattern can match the former. Cancel authorization requires a signal file, rejects command substitution, backticks, newlines, shell chaining, hidden variables, extra args, and wrong source/destination paths. `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, or `unexpected`. Template rendering is single-pass so injected content containing `{{OTHER_VAR}}` is not re-expanded.
- dependencies_and_callers: Sibling validators source `loop-common.sh`: [loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-read-validator.sh:16), [loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-write-validator.sh:17), [loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-edit-validator.sh:16), [loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-bash-validator.sh:16), [loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-plan-file-validator.sh:17), and [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-codex-stop-hook.sh:48). The read/write/edit validators use active-loop selection, current-round parsing, allowlists, path classifiers, and block template rendering. The Bash validator uses `git_adds_humanize`, `command_modifies_file`, state/finalize block messages, cancel authorization, and todos allowlist enforcement. The plan-file validator uses state schema fields and safe template fallback for schema/branch/tracking gates. The Codex stop hook uses parsing, template rendering, and `end_loop` for unexpected/max-iteration/stop transitions.
- edge_cases_or_failure_modes: `parse_state_file` is a simple grep/sed frontmatter parser, not a general YAML parser; callers perform deeper schema/numeric validation. `command_modifies_file` is regex-based shell detection, not a shell AST; tests explicitly note multi-source `cp file1 file2 goal-tracker.md` is a known limitation. `git_adds_humanize` depends on current working directory for `.humanize` existence and `git check-ignore`. `load_template` returns empty on missing templates, so correctness depends on callers using `load_and_render_safe` for blocking messages. Template variable names are documented as uppercase/numeric/underscore, but `render_template` mechanically uses the exact placeholder content to look up environment variables rather than enforcing the documented grammar.
- validation_or_tests: Template behavior is covered by [test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-loader.sh:41) for directory resolution, loading, rendering, fallback behavior, and real-template integration; placeholder-injection prevention is covered at [test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-loader.sh:488). Error/fallback behavior is covered by [test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-error-scenarios.sh:42). Bash mutation detection is covered by [test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-bash-validator-patterns.sh:69). Active-loop and `end_loop` behavior are covered by [test-state-exit-naming.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-state-exit-naming.sh:64). Allowlist behavior is covered by [test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-allowlist-validators.sh:64). I inspected these files but did not execute tests in this read-only research run.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-039 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable behavioral specification for the runtime monitor’s graceful-stop and terminal-restoration contract. It targets acceptance criteria named in the file header: clean exit when `.humanize/rlcr` is deleted, and terminal state restoration after graceful stop.
- algorithmic_behavior: The script runs under strict shell mode, creates an isolated temp directory under `/tmp`, and cleans it on exit. Test 1 writes a temporary script that sources [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:1), then defines a minimal monitor environment plus stubbed `_restore_terminal`, `_cleanup`, and `_graceful_stop` functions to verify the intended call order and user-facing message. It expects restore, cleanup, `"Monitoring stopped:"`, and `"directory no longer exists"` markers. Test 2 verifies idempotency by guarding `_cleanup` with `cleanup_done` and asserting repeated graceful-stop/cleanup calls increment only once. Test 3 simulates the main loop’s directory-existence check, removes `.humanize/rlcr`, and expects the stop path to run only after deletion. Test 4 statically checks that `scripts/humanize.sh` defines `_restore_terminal`, resets the scroll region with `printf "\033[r"`, and that `_cleanup` calls `_restore_terminal`. Test 5 statically checks that `_graceful_stop` calls `_cleanup`.
- inputs_outputs_state: Inputs are the repository root, a temp workspace path, the current contents of [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:466), and temporary `.humanize/rlcr/.../state.md` scaffolding. Outputs are PASS/FAIL lines, counters, and final exit status. Internal test state includes `cleanup_done`, `monitor_running`, `tail_pid`, and `call_count`; filesystem state changes are confined to the temp directory and generated temporary shell scripts.
- gates_or_invariants: `_cleanup` must be idempotent. `_graceful_stop` must invoke cleanup before emitting the specific stop explanation. Directory deletion of `.humanize/rlcr` must trigger graceful stop and return success from the simulated check. Terminal restoration must include scroll-region reset, and `_cleanup` must call `_restore_terminal`. The production monitor implements the same core path at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:478) through [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:512), and polls for directory deletion in the main/no-log/incremental loops at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:548), [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:589), and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:682).
- dependencies_and_callers: Depends on `bash`, `grep`, `mkdir`, `rm`, `chmod`, temporary script execution, and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/scripts/humanize.sh:9). It is included in the repository test surface and describes monitor runtime behavior rather than being called by production hooks.
- edge_cases_or_failure_modes: Several checks are simulated rather than executing the real nested monitor functions because `_restore_terminal`, `_cleanup`, and `_graceful_stop` are defined inside `_humanize_monitor_codex`. Test 4 acknowledges that it cannot actually test `tput` in non-interactive mode and therefore greps source text. The line that pipes `grep -q '_restore_terminal'` into another `grep -q '_cleanup'` is ineffective because `grep -q` emits no stdout; the later direct source check for `_cleanup` calling `_restore_terminal` is the effective assertion. Static grep windows can become brittle if function bodies are refactored without changing behavior.
- validation_or_tests: The file is itself a validation script. Its summary exits zero only when `TESTS_FAILED` is zero and reports the AC-1.1 and AC-1.3 verification messages. I inspected the script and its production target but did not execute it in this read-only research run.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-069 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Static block-message template for the state-file protection gate. It defines the user-facing contract shown when a tool attempts to modify `state.md`, explaining that the loop system owns this file and that manual edits would corrupt loop state.
- algorithmic_behavior: The template has no variables and renders as fixed Markdown: title, prohibition on modifying `state.md`, a short list of state contents, and a corruption warning. It is loaded by `state_file_blocked_message` in [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/loop-common.sh:211), which uses `load_and_render_safe` with a shorter fallback if the template is missing.
- inputs_outputs_state: Input is the template path requested through `TEMPLATE_DIR` plus `block/state-file-modification.md`; there are no placeholder inputs. Output is rendered Markdown to stderr through validator callers. It does not mutate state directly; its algorithmic significance is that it is the explanatory surface for a hard gate that prevents direct state mutation.
- gates_or_invariants: The message is emitted when write/edit validators detect `state.md`, and when the Bash validator detects shell commands that modify `state.md`, unless the Bash path is the explicitly authorized cancel rename. This keeps current round, iteration limits, Codex configuration, and other parsed state fields under loop-system control rather than agent/user tool control. `finalize-state.md` has a separate template and is intentionally checked first by callers.
- dependencies_and_callers: Called through `state_file_blocked_message` in [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/loop-common.sh:211). Production callers include [loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-write-validator.sh:110), [loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-edit-validator.sh:91), and [loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/loop-bash-validator.sh:108). Rendering depends on [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/hooks/lib/template-loader.sh:170).
- edge_cases_or_failure_modes: Because the file is static, rendering cannot fail due to missing variables. If it is missing, the fallback still blocks but loses detail. The content lists only current round, max iterations, and Codex configuration, while `parse_state_file` also handles fields such as plan tracking, start branch, plan file, push policy, effort, and timeout; this is a documentation precision gap, not a gate failure.
- validation_or_tests: Template existence and safe reference use are covered indirectly by template loading/reference tests, including [test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-template-loader.sh:197) for fallback/template rendering and [test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/skillize-rlcr-actions/tests/test-error-scenarios.sh:134) for missing-template fallback. State block behavior is exercised through validators that call `state_file_blocked_message`. I inspected, not executed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item sections present; each assigned item appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`