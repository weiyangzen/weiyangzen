# agent_20 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-020 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook orchestrator for the RLCR state machine. It intercepts Claude Code stop attempts, determines the active RLCR session under `.humanize/rlcr`, validates loop invariants, optionally invokes Codex as independent reviewer, and either allows termination by retiring state or blocks exit with the next instruction payload. It is registered as the Claude Code `Stop` hook in `hooks/hooks.json:52-59`.
- algorithmic_behavior: Reads hook JSON/stdin into `HOOK_INPUT` at `hooks/loop-codex-stop-hook.sh:29`, locates the newest active loop via `find_active_loop` at `hooks/loop-codex-stop-hook.sh:59`, then selects normal `state.md` or finalize `finalize-state.md` phase at `hooks/loop-codex-stop-hook.sh:72-82`. It parses state frontmatter through `parse_state_file` at `hooks/loop-codex-stop-hook.sh:88-100`, revalidates Codex model/effort syntax at `hooks/loop-codex-stop-hook.sh:102-113`, and validates numeric round state at `hooks/loop-codex-stop-hook.sh:115-124`. Before running Codex, it performs fast failure gates: state schema fields at `hooks/loop-codex-stop-hook.sh:131-143`, current branch consistency at `hooks/loop-codex-stop-hook.sh:149-176`, plan backup/existence/content integrity at `hooks/loop-codex-stop-hook.sh:182-250`, native todo completion via `check-todos-from-transcript.py` at `hooks/loop-codex-stop-hook.sh:258-307`, fail-closed git status at `hooks/loop-codex-stop-hook.sh:316-338`, large changed file detection at `hooks/loop-codex-stop-hook.sh:346-421`, clean worktree and optional push-every-round enforcement at `hooks/loop-codex-stop-hook.sh:430-516`, required summary file presence at `hooks/loop-codex-stop-hook.sh:522-554`, and Round 0 goal-tracker initialization at `hooks/loop-codex-stop-hook.sh:560-630`. It blocks exit by printing JSON with `"decision": "block"` and a rendered reason; otherwise, terminal conditions rename state files or exit normally.
- inputs_outputs_state: Inputs are Claude Code stop hook stdin, `CLAUDE_PROJECT_DIR` or current directory, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, the plan backup `plan.md`, round summary files, goal tracker, git status, and templates under `prompt-template`. Outputs include block JSON payloads, generated `round-N-review-prompt.md`, `round-N-review-result.md`, `round-(N+1)-prompt.md`, cache/debug files in `$HOME/.cache/humanize/...`, and state renames. Normal active state is `state.md`; Codex `COMPLETE` transitions to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:961-981`; finalize success transitions to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:651-656`; max-iteration and stop paths call `end_loop` at `hooks/loop-codex-stop-hook.sh:638-642`, `hooks/loop-codex-stop-hook.sh:963-968`, and `hooks/loop-codex-stop-hook.sh:1023-1050`. If Codex reports unresolved issues, the hook increments `current_round` with `sed`, writes the next prompt, and blocks exit at `hooks/loop-codex-stop-hook.sh:1057-1122`.
- gates_or_invariants: The hook intentionally ignores `stop_hook_active` so each blocked continuation can be reviewed again; termination is controlled by active loop absence, Codex marker, or max iterations (`hooks/loop-codex-stop-hook.sh:31-37`). Required state schema fields `plan_tracked` and `start_branch` must exist (`hooks/loop-codex-stop-hook.sh:131-143`). The git branch may not change during a loop (`hooks/loop-codex-stop-hook.sh:166-176`). The plan file must still exist and match the loop backup (`hooks/loop-codex-stop-hook.sh:182-250`). Exit is blocked if todos remain incomplete (`hooks/loop-codex-stop-hook.sh:284-305`), if git status fails (`hooks/loop-codex-stop-hook.sh:325-337`), if changed code/docs files exceed 2000 lines (`hooks/loop-codex-stop-hook.sh:346-421`), if the worktree is dirty (`hooks/loop-codex-stop-hook.sh:461-483`), or if `push_every_round` is true and commits are unpushed (`hooks/loop-codex-stop-hook.sh:489-514`). Review completion is strict: only the last non-empty line exactly equal to `COMPLETE` or `STOP` has semantic effect (`hooks/loop-codex-stop-hook.sh:955-960`). Every fifth round, where `CURRENT_ROUND % 5 == 4`, becomes a full-alignment review (`hooks/loop-codex-stop-hook.sh:683-687`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for active-loop lookup, state parsing, marker constants, template directory, and terminal `end_loop` behavior (`hooks/loop-codex-stop-hook.sh:46-49`). Depends on `scripts/portable-timeout.sh` for bounded git/Codex operations (`hooks/loop-codex-stop-hook.sh:50-55`; implementation priority at `scripts/portable-timeout.sh:9-27`). Uses `jq`, `git`, `diff`, `wc`, `python3`, `codex`, and `load_and_render_safe` templates. State is created by `scripts/setup-rlcr-loop.sh`, which writes the same frontmatter fields at `scripts/setup-rlcr-loop.sh:485-498`. It consumes `prompt-template/codex/full-alignment-review.md`, `prompt-template/codex/regular-review.md`, `prompt-template/codex/goal-tracker-update-section.md`, `prompt-template/claude/finalize-phase-prompt.md`, `prompt-template/claude/next-round-prompt.md`, `prompt-template/claude/next-round-footer.md`, and `prompt-template/claude/goal-tracker-update-request.md` at `hooks/loop-codex-stop-hook.sh:677-681`, `hooks/loop-codex-stop-hook.sh:718-748`, `hooks/loop-codex-stop-hook.sh:1005-1008`, and `hooks/loop-codex-stop-hook.sh:1075-1108`.
- edge_cases_or_failure_modes: No active loop or missing state file allows exit (`hooks/loop-codex-stop-hook.sh:61-82`). Invalid `codex_model`, invalid `codex_effort`, or corrupt `current_round` retires the loop as unexpected through `end_loop` (`hooks/loop-codex-stop-hook.sh:102-119`). Missing/invalid `max_iterations` defaults to 42 (`hooks/loop-codex-stop-hook.sh:122-124`). Git command timeout/failure blocks instead of allowing a bypass (`hooks/loop-codex-stop-hook.sh:149-164`, `hooks/loop-codex-stop-hook.sh:319-337`). Todo checker parse errors block with diagnostic context (`hooks/loop-codex-stop-hook.sh:265-281`). If `codex` is absent, it blocks with install/cancel guidance (`hooks/loop-codex-stop-hook.sh:755-775`). Codex nonzero exit, missing review result, failed stdout copy, or empty review file all block via `codex_failure_exit` and reference debug files (`hooks/loop-codex-stop-hook.sh:852-950`). If Codex says `COMPLETE` at or beyond max iterations, the hook records max-iteration exit rather than entering finalize (`hooks/loop-codex-stop-hook.sh:961-968`). `STOP` is honored even on non-alignment rounds, but called unusual in stderr (`hooks/loop-codex-stop-hook.sh:1023-1050`).
- validation_or_tests: Hook registration preservation is covered in `tests/test-hook-preservation.sh` according to references from `rg`, including direct stop hook invocation. Plan-file gates are exercised in `tests/test-plan-file-hooks.sh`, with direct stop-hook calls visible in search results around cases invoking `hooks/loop-codex-stop-hook.sh`. Finalize phase tests verify `finalize-state.md` active-loop detection and `complete-state.md` non-active behavior at `tests/test-finalize-phase.sh:219-270`. State exit naming tests assert only active files are detected, terminal state files are ignored, newer loop precedence works, and `end_loop` writes valid terminal state files at `tests/test-state-exit-naming.sh:38-225`. Template reference completeness includes the stop hook in its scan list at `tests/test-template-references.sh:56-64`.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-050 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR hook library defining the state vocabulary, active-loop detection, state frontmatter parser, path/file classifiers, standard block messages, cancel authorization, `.humanize` git-add protection, Bash modification detection, and terminal state renaming. It is foundational for the stop hook plus read/write/edit/bash/plan validators.
- algorithmic_behavior: Defines canonical state field names at `hooks/lib/loop-common.sh:15-24`, Codex markers `COMPLETE` and `STOP` at `hooks/lib/loop-common.sh:26-28`, and terminal exit reasons at `hooks/lib/loop-common.sh:30-40`. It loads template support from `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:46-56`. `find_active_loop` chooses the newest timestamp-named loop directory and returns it only when `state.md` or `finalize-state.md` exists, preventing older terminal loops from becoming active again (`hooks/lib/loop-common.sh:58-81`). `parse_state_file` extracts YAML-like frontmatter and sets `STATE_*` variables with defaults for round, max iterations, and push behavior (`hooks/lib/loop-common.sh:98-139`). File classifiers normalize or detect round files, goal tracker, state/finalize state, finalize summary, and `.humanize/rlcr` paths (`hooks/lib/loop-common.sh:141-188`, `hooks/lib/loop-common.sh:250-272`, `hooks/lib/loop-common.sh:415-419`). Message helpers render templates or fallbacks for todos, prompt writes, state files, finalize state, summaries, goal tracker bash writes, git-add protection, and post-Round-0 goal-tracker edits (`hooks/lib/loop-common.sh:190-248`, `hooks/lib/loop-common.sh:541-568`, `hooks/lib/loop-common.sh:601-613`). `command_modifies_file` classifies shell commands that write, edit in place, move/copy to, remove, truncate, `dd`, or redirect into a protected filename (`hooks/lib/loop-common.sh:571-599`). `end_loop` validates the exit reason and renames the state file to `<reason>-state.md` (`hooks/lib/loop-common.sh:615-648`).
- inputs_outputs_state: Inputs are path strings, command strings, active loop directory paths, state file paths, and template directories. Outputs are stdout return values for lookup/parsing helpers, shell return codes for predicates, global `STATE_*` variables from `parse_state_file`, rendered block-message text, and terminal state files from `end_loop`. The active-state invariant is encoded in `find_active_loop`: only `state.md` and `finalize-state.md` are active; `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` are terminal by omission. `is_cancel_authorized` provides a narrow transition path from `state.md` or `finalize-state.md` to `cancel-state.md` when an authorization signal exists (`hooks/lib/loop-common.sh:274-413`).
- gates_or_invariants: Template rendering is fail-soft: missing templates warn and message helpers use inline fallbacks through `load_and_render_safe`; the underlying template renderer is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:167-203`). `find_active_loop` intentionally checks only the newest directory to avoid reviving stale loops (`hooks/lib/loop-common.sh:58-61`). `is_cancel_authorized` requires `.cancel-requested`, rejects command substitution/backticks, newlines, shell chaining operators, residual `$` variables, non-`mv` commands, wrong source/destination paths, and extra arguments (`hooks/lib/loop-common.sh:288-413`). `git_adds_humanize` blocks direct `.humanize` adds, force broad adds, all-scope adds when `.humanize` exists, and broad `.`/`*` adds when `.humanize` is not ignored (`hooks/lib/loop-common.sh:421-539`). `end_loop` only accepts `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`hooks/lib/loop-common.sh:627-635`).
- dependencies_and_callers: Sourced by `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-plan-file-validator.sh`, as shown by caller references. Concrete examples: write validator uses `find_active_loop`, `parse_state_file`, path classifiers, and goal-tracker/state messages at `hooks/loop-write-validator.sh:15-17`, `hooks/loop-write-validator.sh:37-49`, `hooks/loop-write-validator.sh:79-149`; edit validator uses the same state/path checks at `hooks/loop-edit-validator.sh:14-17`, `hooks/loop-edit-validator.sh:63-118`; bash validator uses `to_lower`, `find_active_loop`, `parse_state_file`, `git_adds_humanize`, `command_modifies_file`, and `is_cancel_authorized` at `hooks/loop-bash-validator.sh:14-17`, `hooks/loop-bash-validator.sh:29-53`, `hooks/loop-bash-validator.sh:81-114`, and `hooks/loop-bash-validator.sh:245-352`. It depends directly on `hooks/lib/template-loader.sh`, whose `get_template_dir`, `load_template`, `render_template`, and safe fallback functions are defined at `hooks/lib/template-loader.sh:24-203`.
- edge_cases_or_failure_modes: If the loop base directory is absent, no active loop is returned (`hooks/lib/loop-common.sh:66-69`). `parse_state_file` returns failure on missing file but also defaults missing `current_round`, `max_iterations`, and `push_every_round` after parsing (`hooks/lib/loop-common.sh:115-138`). `is_state_file_path` uses `state\.md$`, so `finalize-state.md` also matches; callers compensate by checking finalize-specific paths first, explicitly documented in write/edit/bash validators (`hooks/loop-write-validator.sh:101-108`, `hooks/loop-edit-validator.sh:81-94`, `hooks/loop-bash-validator.sh:87-105`). `git_adds_humanize` lowercases input, so its comments note that flags like `-A` become `-a` and are matched accordingly (`hooks/lib/loop-common.sh:425-427`, `hooks/lib/loop-common.sh:496-503`). The command modification detector is regex-based and intentionally conservative for common write forms; unusual shell constructs outside its patterns may be handled by higher-level validators or may be residual risk. `end_loop` returns nonzero if the state file is already missing (`hooks/lib/loop-common.sh:639-647`).
- validation_or_tests: `tests/test-template-references.sh` scans `loop-common.sh` and related hooks for template references and checks common block templates at `tests/test-template-references.sh:56-64` and `tests/test-template-references.sh:149-167`. `tests/test-bash-validator-patterns.sh` directly tests `command_modifies_file` with redirection and `tee` cases at `tests/test-bash-validator-patterns.sh:1-95`. `tests/test-finalize-phase.sh` verifies finalize path helpers and active-loop behavior at `tests/test-finalize-phase.sh:174-270`. `tests/test-state-exit-naming.sh` validates active versus terminal state discovery and `end_loop` accepted reasons at `tests/test-state-exit-naming.sh:38-225`. `tests/test-cancel-signal-file.sh` directly exercises `is_cancel_authorized`, including signal present/absent, wrong commands, extra args, quoted paths, and hidden variables at `tests/test-cancel-signal-file.sh:1250-1341`. `tests/test-humanize-escape.sh` is referenced as covering `git_adds_humanize` path variants and force/all behaviors.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-080 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing prompt fragment that defines the sanctioned request contract for goal-tracker changes after direct edits are restricted. It is not executable code, but it participates in the RLCR transition/gate design by routing goal-tracker mutations through Codex review instead of allowing Claude to edit `goal-tracker.md` directly after Round 0.
- algorithmic_behavior: The template tells Claude to include a `Goal Tracker Update Request` section in its summary only when goal-tracker changes are needed (`prompt-template/claude/goal-tracker-update-request.md:2-4`). It specifies two structured fields: `Requested Changes` with examples for marking tasks complete, adding open issues, recording plan evolution, and deferring tasks (`prompt-template/claude/goal-tracker-update-request.md:6-10`), plus `Justification` explaining why the changes serve the ultimate goal (`prompt-template/claude/goal-tracker-update-request.md:12-13`). The final line states that Codex will review and update the tracker if justified (`prompt-template/claude/goal-tracker-update-request.md:16`). The stop hook appends this fragment to generated next-round prompts at `hooks/loop-codex-stop-hook.sh:1103-1108`.
- inputs_outputs_state: Input is a next-round Claude prompt being generated after Codex found issues. Output is instruction text appended to `round-(N+1)-prompt.md`, which influences Claude’s next summary format. The state transition it supports is indirect: after Round 0, Claude cannot mutate `goal-tracker.md`; Claude may instead emit a structured request in `round-N-summary.md`; Codex receives the paired Codex-side instructions and may update `goal-tracker.md` during review. The companion Codex template makes that responsibility explicit at `prompt-template/codex/goal-tracker-update-section.md:1-17`, and the stop hook injects it into both regular and full-alignment Codex review prompts at `hooks/loop-codex-stop-hook.sh:677-681` and `hooks/loop-codex-stop-hook.sh:718-748`.
- gates_or_invariants: The template reinforces the invariant enforced by validators: after Round 0, Claude must not directly modify `goal-tracker.md`. The next-round prompt states this at `prompt-template/claude/next-round-prompt.md:22-31`; write and edit validators block post-Round-0 goal-tracker writes/edits at `hooks/loop-write-validator.sh:141-149` and `hooks/loop-edit-validator.sh:110-118`; bash validator blocks shell-based goal-tracker modification and routes Round 0 versus later-round messaging at `hooks/loop-bash-validator.sh:302-317`. The Codex-side companion constrains updates further by instructing Codex never to modify the immutable Ultimate Goal and Acceptance Criteria section (`prompt-template/codex/goal-tracker-update-section.md:5-11`).
- dependencies_and_callers: Loaded by `load_template "$TEMPLATE_DIR" "claude/goal-tracker-update-request.md"` in the stop hook at `hooks/loop-codex-stop-hook.sh:1104`; if missing, the hook falls back to a one-line request instruction at `hooks/loop-codex-stop-hook.sh:1105-1107`. The template relies on the broader template loader behavior where missing templates return empty and safe loaders provide fallbacks (`hooks/lib/template-loader.sh:33-48`, `hooks/lib/template-loader.sh:167-203`). It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, `prompt-template/claude/next-round-prompt.md`, and the validator block message `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:601-613`.
- edge_cases_or_failure_modes: Because this is a static prompt fragment, enforcement depends on validators and Codex review, not on the template itself. If the template is missing, next-round prompts still include a fallback but lose the structured examples (`hooks/loop-codex-stop-hook.sh:1104-1108`). If Claude omits the request section, Codex has no structured update request to evaluate. If Claude requests changes that conflict with immutable goal/acceptance sections, the companion Codex instructions require rejection or selective handling (`prompt-template/codex/goal-tracker-update-section.md:5-11`). The template does not specify exact evidence schema beyond examples, so evidence quality depends on Claude summary discipline and Codex judgment.
- validation_or_tests: Template reference coverage scans shell scripts for template paths and includes the stop hook plus common template checks in `tests/test-template-references.sh:56-110`. The companion Codex update-section template has a direct rendering test in `tests/test-templates-comprehensive.sh:531-539`. The functional enforcement around this prompt is covered through write/edit/bash validators, including post-Round-0 goal-tracker blocking in the hook code cited above. I did not find a direct dedicated test for the exact `prompt-template/claude/goal-tracker-update-request.md` body; coverage appears indirect through template reference scanning and next-round prompt generation paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 assigned item sections are present above, each with one `###` heading and one `cursor: [_]` line.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`