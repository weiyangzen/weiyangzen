# agent_15 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-015 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Thin slash-command workflow definition for cancelling an active RLCR loop. It is not the cancellation algorithm itself; it is the command-layer dispatch surface that exposes the operation while delegating execution to the namespaced skill.
- algorithmic_behavior: The command frontmatter declares description, allowed Skill tool scope, and hidden slash-command metadata at `commands/cancel-rlcr-loop.md:1-5`. The body instructs the runtime to invoke `Skill: humanize:cancel-rlcr-loop` at `commands/cancel-rlcr-loop.md:9-15`, explicitly keeping the operational logic out of the command file. The actual cancel flow lives in `skills/cancel-rlcr-loop/SKILL.md`, which finds the newest `.humanize/rlcr/*/` directory, checks for `state.md` or `finalize-state.md`, writes `.cancel-requested`, and renames the active state file to `cancel-state.md`.
- inputs_outputs_state: Input is a user command invocation plus permission to call `Skill(humanize:cancel-rlcr-loop:*)` from `commands/cancel-rlcr-loop.md:3`. Output is not direct file mutation by this wrapper; the delegated skill produces user-facing status and state transitions. In the skill, normal-loop cancellation transitions `.humanize/rlcr/<loop>/state.md` to `cancel-state.md` after creating `.cancel-requested`; finalize-phase cancellation transitions `finalize-state.md` to `cancel-state.md` only after confirmation.
- gates_or_invariants: The command invariant is delegation only: the command must stay under the Skill tool boundary and must not duplicate full cancellation instructions. The delegated skill invariant is that the active loop is the newest timestamped loop directory and is active only if `state.md` or `finalize-state.md` exists. The skill also preserves loop artifacts rather than deleting the loop directory.
- dependencies_and_callers: The command depends on `skills/cancel-rlcr-loop/SKILL.md` for the real algorithm. Template blocks point users to this command when loop state is stale or plan files are modified, including `prompt-template/block/plan-file-modified.md:8` and `prompt-template/block/schema-outdated.md:8`. Setup and hook text also refer to cancellation paths, such as `scripts/setup-rlcr-loop.sh:87`, `scripts/setup-rlcr-loop.sh:684`, and `hooks/loop-codex-stop-hook.sh:766`.
- edge_cases_or_failure_modes: If no newest loop directory exists, the skill reports no active loop. If a loop directory exists but neither `state.md` nor `finalize-state.md` exists, the skill reports that the loop already ended or was not properly started. Finalize phase is special: cancellation is gated by an explicit user confirmation because the loop would otherwise complete without returning to review.
- validation_or_tests: `tests/test-skill-invocation.sh` verifies the command delegates through `Skill(humanize:cancel-rlcr-loop...)` at lines `110-123`, verifies it remains a thin wrapper without `.humanize/rlcr` implementation details at lines `146-163`, and verifies the cancel skill has Bash-capable allowed tools at lines `189-202`. `tests/test-skill-structure.sh` also checks command delegation in the command structure tests at lines `180-188` per reference search.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-045 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR prompt template loading and rendering algorithm implemented by `hooks/lib/template-loader.sh`. It defines expected path resolution, missing-template behavior, single-pass placeholder replacement, shell metacharacter safety, placeholder injection prevention, fallback behavior, and template directory validation.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12`, then runs 41 numbered checks with pass/fail counters. Tests 1-14 cover baseline API behavior: `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` at `tests/test-template-loader.sh:41-244`. Tests 15-33 assert literal rendering of shell metacharacters and multiline values at `tests/test-template-loader.sh:247-486`. Tests 34-36 assert single-pass placeholder injection prevention at `tests/test-template-loader.sh:488-543`. Tests 37-41 cover empty values, Unicode, underscore-prefixed variables, and numeric variable names at `tests/test-template-loader.sh:545-639`. Final exit status is derived from accumulated failures at `tests/test-template-loader.sh:641-659`.
- inputs_outputs_state: Inputs are the repository root, template directory resolved from `hooks/lib`, real templates such as `block/git-push.md` and `block/wrong-round-number.md`, inline template strings, and `VAR=value` render assignments. Outputs are terminal pass/fail lines, incremented `TESTS_PASSED` and `TESTS_FAILED` counters, and process exit code `0` if no failures or `1` otherwise. No persistent repository state is intentionally modified.
- gates_or_invariants: Key invariants enforced by the test are: missing templates return empty content for `load_template`; missing placeholders remain unchanged; unused variables are ignored; values are rendered literally without shell interpretation; values containing `{{OTHER_VAR}}` are not recursively expanded; safe loading uses fallback content when a template is missing or renders empty; template directory validation requires expected subdirectories. The most important algorithmic gate is single-pass substitution, matching implementation comments in `hooks/lib/template-loader.sh:50-57` and the awk scanner in `hooks/lib/template-loader.sh:71-129`.
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, which provides `get_template_dir` at lines `24-31`, `load_template` at lines `33-48`, `render_template` at lines `50-132`, `load_and_render` at lines `134-147`, `load_and_render_safe` at lines `167-203`, and `validate_template_dir` at lines `205-222`. The tested loader is sourced transitively by RLCR hooks through `hooks/lib/loop-common.sh`, and used directly by hook scripts such as `hooks/loop-read-validator.sh`. `tests/run-all-tests.sh:34` includes this script in the full test suite per reference search.
- edge_cases_or_failure_modes: This spec explicitly covers absent files, invalid template directories, unprovided variables, empty variable values, shell-sensitive characters including `&`, backslashes, `$`, backticks, pipes, semicolons, globs, redirection syntax, quotes, comments, tildes, JSON-like text, regex-like text, multiline values, Unicode text, underscore-prefixed variables, and numeric variable names. One implementation-sensitive edge case is awk newline handling: multiline template input is read line-by-line and printed line-by-line, while multiline environment values can be injected into a single placeholder and are expected to preserve embedded newlines.
- validation_or_tests: The file is itself the validation suite. It uses `set -euo pipefail` at `tests/test-template-loader.sh:8`, but individual checks use conditionals and explicit `fail` bookkeeping, so expected negative cases can be tested without aborting. The summary gate at `tests/test-template-loader.sh:651-659` exits nonzero if any assertion failed. Additional related validation exists in `tests/test-error-scenarios.sh` for template-loader error behavior and `tests/test-template-references.sh` for verifying that referenced template files exist and critical validators use safe rendering.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-075 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Error-message block template for the RLCR read-validation algorithm. It is emitted when an agent attempts to read a round prompt or summary file from outside the active `.humanize/rlcr/` loop directory.
- algorithmic_behavior: The template declares the block title at `prompt-template/block/wrong-file-location.md:1`, reports the attempted `{{FILE_PATH}}` and required `{{ACTIVE_LOOP_DIR}}/` at line `3`, lists the current round prompt and summary paths at lines `5-7`, and gives a final direct `cat {{FILE_PATH}}` instruction at line `9`. It does not make decisions itself; it defines the structured remediation text rendered by the hook.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-read-validator.sh`: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` at `hooks/loop-read-validator.sh:105-108`. Output is a rendered markdown error message written to stderr when the hook blocks a Read tool call. State transition is external: the hook exits `2`, which signals a blocked tool use, while the RLCR loop state files remain unchanged.
- gates_or_invariants: The gate is location-based. `hooks/loop-read-validator.sh` first ignores non-Read tools at lines `22-27`, ignores non-round prompt/summary files at lines `50-52`, finds the active loop and current round at lines `61-78`, extracts a round number at lines `83-86`, and then blocks if `IN_HUMANIZE_LOOP_DIR` is false at lines `100-109`. The invariant is that current round prompt and summary reads must come from the active loop directory unless another allowlist path applies earlier or later in the hook flow.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`, which renders this template through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-file-location.md"` at lines `105-108`. That hook depends on `hooks/lib/loop-common.sh`, which loads the template-loader library and shared loop helpers. The template’s availability is covered by template reference scanning in `tests/test-template-references.sh`, which extracts `load_template`, `load_and_render`, and `load_and_render_safe` calls at lines `83-110`.
- edge_cases_or_failure_modes: If no active loop directory exists, the read validator exits without blocking at `hooks/loop-read-validator.sh:65-67`, so this template is not used. If the filename does not contain an extractable round number, the hook exits without blocking at lines `83-86`. If the template file is missing or empty, `load_and_render_safe` falls back to an inline wrong-location message defined at `hooks/loop-read-validator.sh:102-104`. The template’s final instruction says `cat {{FILE_PATH}}`, which may be intentionally permissive for direct shell inspection even though the original Read tool access is blocked; this is a UX contract worth keeping aligned with policy.
- validation_or_tests: `tests/test-template-references.sh` protects against missing template references by scanning hook calls and checking referenced markdown files exist at lines `74-110`. `tests/test-template-loader.sh` validates the safe fallback path used by this caller through `load_and_render_safe` tests at lines `196-222`. The read validator also embeds a fallback, so missing-template failure should still produce a nonempty block message instead of an empty stderr response.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`