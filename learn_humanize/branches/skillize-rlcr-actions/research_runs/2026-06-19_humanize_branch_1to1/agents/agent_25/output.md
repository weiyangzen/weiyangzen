# agent_25 skillize-rlcr-actions 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `0a9187b8478dd3ed9c0cf58a4615c61e188fd444`

## Item Evidence

### SKILLIZE_RLCR_ACTIONS-HZ-025 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Provides the user-facing `humanize monitor rlcr-loop` command and the internal `_humanize_monitor_codex` terminal monitor. It is not the loop scheduler or validator; it is an operational observer for RLCR loop state, goal-tracker progress, git status, and Codex review logs.
- algorithmic_behavior: The monitor requires `.humanize/rlcr` in the current project (`scripts/humanize.sh:14-25`), selects the newest timestamp-shaped session directory with `find` and lexical timestamp comparison (`scripts/humanize.sh:28-49`), selects the newest cached Codex log from `$HOME/.cache/humanize/<sanitized-project-path>/<session>/round-*-codex-run.log` (`scripts/humanize.sh:52-108`), parses active/stopped state from `state.md` or `*-state.md` (`scripts/humanize.sh:111-150`), parses loop metadata and goal-tracker counts (`scripts/humanize.sh:153-252`), and draws an 11-line status bar above an incrementally updated log tail (`scripts/humanize.sh:316-453`, `scripts/humanize.sh:547-799`). The public dispatcher exposes only `monitor rlcr-loop` and usage errors (`scripts/humanize.sh:806-838`).
- inputs_outputs_state: Inputs are the current working directory, `.humanize/rlcr/<timestamp>/`, session `state.md` or terminal `*-state.md`, `goal-tracker.md`, cached Codex stderr log files, terminal dimensions via `tput`, and git status/diff data (`scripts/humanize.sh:256-300`). Outputs are terminal-rendered status/log text, exit status `1` for missing loop/session/usage, and exit status `0` for graceful monitor stop. Internal state includes `current_session_dir`, `current_file`, `current_loop_status`, `last_size`, `last_no_log_status`, `monitor_running`, and `cleanup_done`; it does not mutate RLCR loop files.
- gates_or_invariants: Session directories must match `YYYY-MM-DD_HH-MM-SS` (`scripts/humanize.sh:41-46`, `scripts/humanize.sh:77-80`). `state.md` has priority and means `active`; `*-state.md` supplies a terminal reason such as completed/failed/cancelled (`scripts/humanize.sh:123-149`). Bash/zsh compatibility is guarded by zsh `ksharrays` and by `find` instead of unmatched globs (`scripts/humanize.sh:10-12`, `scripts/humanize.sh:35-47`). Cleanup must reset terminal scroll region and traps (`scripts/humanize.sh:465-497`). Loop directory deletion is a graceful-stop path, not a crash (`scripts/humanize.sh:499-512`, `scripts/humanize.sh:549-553`, `scripts/humanize.sh:682-686`).
- dependencies_and_callers: The cached log layout is produced by `hooks/loop-codex-stop-hook.sh`, which writes Codex command/stdout/stderr under the same sanitized project cache path (`hooks/loop-codex-stop-hook.sh:779-791`). The script depends on `find`, `grep`, `sed`, `basename`, `stat`, `tail`, `git`, `tput`, `clear`, `seq`, `sort`, and `wc`. Tests and manual checks source or exercise it from `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, and `tests/manual-monitor-test.sh`.
- edge_cases_or_failure_modes: Missing `.humanize/rlcr` blocks monitor start (`scripts/humanize.sh:20-25`); no session returns a specific “Start an RLCR loop” message (`scripts/humanize.sh:521-526`); no log enters a wait/poll branch (`scripts/humanize.sh:568-667`); deleted session/log switches to a newer one or waits (`scripts/humanize.sh:620-648`, `scripts/humanize.sh:724-759`); truncated/rotated logs reset current-file tracking (`scripts/humanize.sh:697-715`). Potential weak spots: `round_num` is compared numerically after parsing any `round-*-codex-run.log` name, so malformed nonnumeric names can produce shell comparison errors; pipe characters in parsed state/goal fields can confuse the pipe-delimited `_split_to_array` transport; very narrow terminals can make truncation math fragile.
- validation_or_tests: `tests/test-monitor-runtime.sh` verifies graceful stop messaging and that cleanup calls terminal restore (`tests/test-monitor-runtime.sh:1-9`, `tests/test-monitor-runtime.sh:263-323`). `tests/test-monitor-e2e-real.sh` runs the real monitor under bash, deletes `.humanize/rlcr`, checks graceful exit, no glob errors, cleanup message, and exit code `0` (`tests/test-monitor-e2e-real.sh:59-225`). `tests/test-zsh-monitor-safety.sh` covers zsh source safety, empty loop dirs, dotfile-only dirs, missing state files, and empty log caches (`tests/test-zsh-monitor-safety.sh:59-238`).
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-055 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Small prompt block used by the stop-hook git-clean gate to explain that `.humanize/` is local RLCR state and should be ignored rather than committed.
- algorithmic_behavior: The stop hook computes cached git porcelain status, detects uncommitted changes, extracts untracked files, and if any untracked path contains `.humanize`, loads this block as `HUMANIZE_LOCAL_NOTE` (`hooks/loop-codex-stop-hook.sh:430-448`). That note is appended into the larger git-not-clean blocking reason before the hook emits a JSON `"decision": "block"` response (`hooks/loop-codex-stop-hook.sh:461-482`). The block itself instructs the agent to append `.humanize*` to `.gitignore` and stage `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`).
- inputs_outputs_state: Inputs are the static template content plus the caller’s untracked-file detection. There are no template variables. Output is human-readable remediation text embedded into the stop hook’s block reason. It does not edit `.gitignore` itself and does not change loop state.
- gates_or_invariants: The invariant is that `.humanize/` loop state is local and must not be committed (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). The enclosing gate still blocks exit while the repo has uncommitted changes; this block only provides a special-case remediation note (`hooks/loop-codex-stop-hook.sh:461-482`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` (`hooks/loop-codex-stop-hook.sh:441-447`). Template resolution is provided by `hooks/lib/template-loader.sh`, where `load_template` returns file contents or an empty warning path (`hooks/lib/template-loader.sh:33-48`). Related protection exists in `hooks/lib/loop-common.sh` for blocking `git add .humanize` variants, but this item is specifically the stop-hook clean-state note.
- edge_cases_or_failure_modes: The caller checks `grep -q '\.humanize'`, so the special note can trigger for broader names containing that substring, not only the exact `.humanize/` directory (`hooks/loop-codex-stop-hook.sh:439-443`). The suggested ignore pattern `.humanize*` intentionally covers `.humanize/` and legacy variants, but it may also ignore similarly named local files. If the template is missing, the hook falls back to a terse inline note rather than failing (`hooks/loop-codex-stop-hook.sh:443-446`).
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for template references and verifies referenced template files exist (`tests/test-template-references.sh:51-111`). Broader `.humanize` add/ignore behavior is covered by hook and bash-validator tests such as `tests/test-humanize-escape.sh`, which exercises direct, quoted, forced, and aggregate `git add` variants.
- skip_candidate: `no`

### SKILLIZE_RLCR_ACTIONS-HZ-085 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory every-fifth-round Codex review contract. It turns a normal code review into a full goal-alignment audit that can continue the loop, enter finalize, or trip the stagnation circuit breaker through strict terminal markers.
- algorithmic_behavior: The stop hook marks a full-alignment round when `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:683-687`), renders this template into `round-N-review-prompt.md` with plan, summary, goal-tracker, docs, historical round, and review-result variables (`hooks/loop-codex-stop-hook.sh:718-731`), then runs `codex exec` with the rendered prompt (`hooks/loop-codex-stop-hook.sh:813-840`). The prompt requires Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), audit every acceptance criterion (`prompt-template/codex/full-alignment-review.md:19-29`), detect forgotten/deferred items (`prompt-template/codex/full-alignment-review.md:30-41`), review implementation reality (`prompt-template/codex/full-alignment-review.md:50-55`), inspect recent history for stagnation (`prompt-template/codex/full-alignment-review.md:59-84`), and end with `STOP`, `COMPLETE`, or actionable findings (`prompt-template/codex/full-alignment-review.md:86-93`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:720-731`). Output is a review prompt file that should cause Codex to write `REVIEW_RESULT_FILE`. The stop hook then reads the review file, takes the last non-empty trimmed line, and interprets exact `COMPLETE` or `STOP` markers (`hooks/loop-codex-stop-hook.sh:952-960`).
- gates_or_invariants: `COMPLETE` is allowed only when all original plan tasks and acceptance criteria are fully met with no deferrals (`prompt-template/codex/full-alignment-review.md:88-93`). `STOP` is the stagnation/circuit-breaker marker, especially for repeated issues, no meaningful AC progress, circular review feedback, or no new code changes (`prompt-template/codex/full-alignment-review.md:74-84`). The hook avoids false positives by requiring the marker to be the last non-empty line exactly, so phrases like “CANNOT COMPLETE” do not count (`hooks/loop-codex-stop-hook.sh:955-960`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`; template rendering uses `load_and_render_safe`, which gives an inline fallback if the template is missing (`hooks/loop-codex-stop-hook.sh:695-731`). Rendering semantics come from `hooks/lib/template-loader.sh`, which performs single-pass `{{VAR}}` substitution and deliberately does not re-expand placeholders inside variable values (`hooks/lib/template-loader.sh:50-131`). The prompt relies on historical files under `.humanize/rlcr/<timestamp>/` (`prompt-template/codex/full-alignment-review.md:63-73`).
- edge_cases_or_failure_modes: If Codex exits nonzero, creates no review file, or creates an empty review file, the hook blocks with debug paths instead of advancing state (`hooks/loop-codex-stop-hook.sh:878-950`). If Codex writes review text to stdout rather than the target file, the hook copies stdout into the review result file (`hooks/loop-codex-stop-hook.sh:897-913`). If `COMPLETE` appears at or beyond max iterations, the hook terminates as max-iteration rather than entering finalize (`hooks/loop-codex-stop-hook.sh:961-969`). If `STOP` appears outside a full-alignment round, the hook still honors it but logs it as unusual (`hooks/loop-codex-stop-hook.sh:1023-1050`). The prompt is policy-heavy; the final marker is trusted after Codex review, with no independent machine proof that every AC is actually met.
- validation_or_tests: `tests/test-template-references.sh` verifies referenced templates exist (`tests/test-template-references.sh:51-111`). `tests/test-finalize-phase.sh` exercises `COMPLETE` handling into finalize-state behavior, and `tests/test-state-exit-naming.sh` covers terminal state naming for `COMPLETE`/`STOP`. There does not appear to be a focused test that validates the full text of this template’s AC/deferred/stagnation instructions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item sections emitted, matching the 3 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`