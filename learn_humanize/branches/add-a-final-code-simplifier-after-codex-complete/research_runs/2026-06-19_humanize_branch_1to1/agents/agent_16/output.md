# agent_16 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-016 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash validator for the RLCR loop. It is registered as the Bash matcher hook in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/hooks.json:42), and protects loop-owned control files plus the default local-only commit policy while an active `.humanize/rlcr` loop exists.
- algorithmic_behavior: The script reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.command` with `jq`, and exits immediately unless the tool is Bash ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:22)). It lowercases the command for case-insensitive matching, finds the newest active loop under `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/rlcr`, and allows all commands if no loop is active ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:36)). If `finalized-state.md` exists it becomes the parsed state file instead of `state.md`, which makes finalize-phase protection share the same state parser ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:45)).
- inputs_outputs_state: Inputs are the Claude hook JSON payload, `CLAUDE_PROJECT_DIR`, the active loop directory, state frontmatter fields, and prompt templates loaded through `loop-common.sh`. The main state input is `push_every_round`, parsed by `parse_state_file`; its default is `false` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/lib/loop-common.sh:128). Outputs are hook exit status plus stderr block messages: `0` allows the Bash command, `2` blocks it. The validator itself does not mutate loop state. The only state transition it deliberately permits is an authorized cancel move from `state.md` to `cancel-state.md`, delegated to `is_cancel_authorized` when `.cancel-requested` exists and the command shape is exact ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/lib/loop-common.sh:274)).
- gates_or_invariants: The first gate blocks direct `git push` when `push_every_round` is not exactly `true`; it matches only commands beginning with `git push`, renders `block/git-push.md`, and exits `2` ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:63)). The state-file gates protect both destination writes and source moves/copies for `finalized-state.md` and `state.md`, checking finalized state first because `state\.md` would also match that filename suffix ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:87)). The source-protection section splits shell operators, strips leading redirections, then detects `mv`/`cp` from state files, including wrapper forms like `sh -c` and `bash -c` for move/copy payloads ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:113)). Later gates block Bash writes to `.humanize/rlcr/.../plan.md`, `goal-tracker.md`, `round-N-prompt.md`, and `round-N-summary.md`; goal tracker behavior depends on `CURRENT_ROUND`, using a direct Write/Edit instruction in round 0 and summary-request flow after round 0 ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:272)). Todo-file writes are special-cased: modifications to `round-N-todos.md` are blocked unless the command contains the full active loop path and the basename is `round-1-todos.md` or `round-2-todos.md` ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:321)).
- dependencies_and_callers: Direct dependencies are `jq`, Bash, `sed`, `grep`, and shared functions from [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/lib/loop-common.sh:46), especially `find_active_loop`, `parse_state_file`, `to_lower`, `command_modifies_file`, `is_cancel_authorized`, and template-backed block-message helpers. `command_modifies_file` contains the core write-detection regex set for redirects, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/lib/loop-common.sh:420)). `push_every_round` is authored by setup via `--push-every-round` in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/scripts/setup-rlcr-loop.sh:148). The complementary stop-hook behavior checks for unpushed commits only when that flag is true, so the policy is: default blocks early pushes; opt-in mode permits pushes and later requires them before exit ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-codex-stop-hook.sh:486)).
- edge_cases_or_failure_modes: With no active loop, all Bash commands are allowed, so protection depends on `find_active_loop` selecting the newest loop directory with state. The `git push` gate is intentionally narrow: `git push` at command start is blocked, but prefixed/chained forms are not covered by that regex. The Bash parser is regex-based rather than a full shell parser; it handles many redirection variants through placeholders and cleanup, but quoted shell operators or complex shell syntax can still create false positives or missed cases. `command_modifies_file` does not detect variable-target writes such as `echo x > $FILE`, and tests document a known limitation for multi-source `cp` into the protected destination ([tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-bash-validator-patterns.sh:165)). Invalid hook JSON or missing `jq` would fail under `set -euo pipefail` rather than producing a friendly block message.
- validation_or_tests: Existing tests cover the delegated modification patterns, including redirects, `tee`, in-place editors, file operations, read-only false positives, and state/summary patterns in [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-bash-validator-patterns.sh:69). Finalize-phase tests verify Bash blocks writes and source `mv`/`cp` from `finalized-state.md`, and that `finalized-state.md` parsing works for normal Bash commands ([tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-finalize-phase.sh:317)). Cancel-signal tests cover the exact allowed cancel move plus blocked cases without the signal and non-cancel state modifications with the signal ([tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-cancel-signal-file.sh:78)). Allowlist tests cover active-loop todo path restrictions and wrong-root rejection ([tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-allowlist-validators.sh:288)). I did not execute the test suite because this assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-046 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: User-facing block template for the Bash validator’s default “commits stay local” gate. It is small, but it is part of the algorithmic contract because it defines the recovery instruction shown when a disallowed `git push` is attempted during an RLCR loop.
- algorithmic_behavior: The template contains a static markdown block headed “Git Push Blocked” and tells the agent that current commits should remain local, with the opt-in command example `/humanize:start-rlcr-loop plan.md --push-every-round` ([prompt-template/block/git-push.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/prompt-template/block/git-push.md:1)). It has no placeholders, so rendering is a load-only operation in practice.
- inputs_outputs_state: Inputs are indirect: `hooks/loop-bash-validator.sh` chooses this template only when `STATE_PUSH_EVERY_ROUND` is not `true` and the command starts with `git push`. Output is markdown written to stderr by `load_and_render_safe` before the hook exits `2` ([hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/loop-bash-validator.sh:66)). It does not read or write state; the relevant state is set upstream by the setup script’s default `PUSH_EVERY_ROUND=false` and `--push-every-round` option ([scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/scripts/setup-rlcr-loop.sh:41)).
- gates_or_invariants: The template reinforces the invariant that remote pushes are not part of the default per-round loop path. The escape hatch is explicit and must be chosen when starting the loop, not after the block fires. This pairs with the stop-hook’s opposite gate for opt-in mode, where unpushed commits block exit until pushed ([prompt-template/block/unpushed-commits.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/prompt-template/block/unpushed-commits.md:1)).
- dependencies_and_callers: Loaded through `load_and_render_safe` from [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/lib/template-loader.sh:170), called only by the Bash validator’s git-push block path. The template directory is resolved by `get_template_dir` from the hook library root to `prompt-template` ([hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/hooks/lib/template-loader.sh:24)).
- edge_cases_or_failure_modes: If the template is missing or empty, the validator falls back to its inline “Git Push Blocked” message, so missing template content degrades but does not remove the gate. Because the template is static, it cannot show the current branch, active loop path, or actual `push_every_round` value. Its instruction is only useful for future loop starts; it does not provide an in-loop override.
- validation_or_tests: Template loading is covered by [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-template-loader.sh:59), and `load_and_render_safe` is checked to prefer this real template over a fallback in [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-template-loader.sh:216). The comprehensive template test repeats the same fallback-vs-real-template behavior ([tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-a-final-code-simplifier-after-codex-complete/tests/test-templates-comprehensive.sh:455)). I did not find a targeted test that executes a `git push` Bash hook payload and asserts this exact template is emitted.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`