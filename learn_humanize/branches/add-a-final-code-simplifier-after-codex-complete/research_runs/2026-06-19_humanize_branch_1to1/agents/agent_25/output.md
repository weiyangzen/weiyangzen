# agent_25 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-025 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable validation aggregator for the Humanize plugin test suite. It is not production runtime code, but it is a core executable specification because it defines which behavior suites must pass and how suite pass/fail signals are interpreted. The declared usage and exit contract are in `tests/run-all-tests.sh:3-9`.

- algorithmic_behavior: The script resolves its own directory via `SCRIPT_DIR` so test suite paths are stable relative to `tests/`, independent of caller cwd (`tests/run-all-tests.sh:14`). It initializes aggregate counters and a failed-suite array (`tests/run-all-tests.sh:28-30`), then runs an ordered suite list (`tests/run-all-tests.sh:32-47`). For each suite, it computes `suite_path`, skips only if the file is absent, runs the suite with stdout and stderr captured, records the exit code, strips ANSI color codes, extracts the last reported `Passed:` and `Failed:` counts, and adds them to totals (`tests/run-all-tests.sh:49-73`). A suite is failed if either its process exit code is nonzero or its parsed failed-count is greater than zero (`tests/run-all-tests.sh:75-82`). At the end, it reports totals and exits `1` when any suite failed, otherwise exits `0` (`tests/run-all-tests.sh:86-104`).

- inputs_outputs_state: Inputs are implicit: the executable test files listed in `TEST_SUITES`, their stdout/stderr, their exit statuses, and their conventional summary lines matching `Passed:[[:space:]]*[0-9]+` and `Failed:[[:space:]]*[0-9]+` (`tests/run-all-tests.sh:33-47`, `tests/run-all-tests.sh:62-70`). There are no CLI arguments. Outputs are colored console status lines, a final summary, and process exit code `0` or `1`. Internal state transitions are shell-local: `TOTAL_PASSED` and `TOTAL_FAILED` monotonically accumulate parsed counts, while `FAILED_SUITES` records suite filenames only when a suite trips the failure gate (`tests/run-all-tests.sh:72-77`). The script does not write repository files itself, although child test suites may create their own temporary state.

- gates_or_invariants: The suite order is fixed by the literal `TEST_SUITES` array, so adding a test file under `tests/` does not include it unless this list is updated (`tests/run-all-tests.sh:33-47`). Existing suites are required to be runnable as commands via `"$suite_path"` (`tests/run-all-tests.sh:62`). Missing listed suites are treated as `SKIP`, not as failures (`tests/run-all-tests.sh:52-55`). Failure is intentionally dual-sourced: a nonzero exit code fails even if the parsed failed-count is zero, and a positive parsed failed-count fails even if the suite exits successfully (`tests/run-all-tests.sh:75`). Only failed suites have their captured output surfaced, truncated to the last 30 lines (`tests/run-all-tests.sh:78-79`).

- dependencies_and_callers: Runtime dependencies are Bash, standard Unix utilities `sed`, `grep`, and `tail`, plus executable permission on test scripts. The file itself has executable mode in the branch export. Direct text references found in the repo are mostly self/documentation and a focused ANSI parser test: `tests/test-ansi-parsing.sh` states it tests the portable ANSI stripping used by this runner (`tests/test-ansi-parsing.sh:3-6`). The included child suites cover template loading, bash validator patterns, todo checking, plan validation/hooks, template references, state exit naming, comprehensive templates, error scenarios, ANSI parsing, allowlist validators, finalize phase, and cancel signal behavior (`tests/run-all-tests.sh:33-47`). No production hook directly calls this runner in the inspected references; it is intended as a manually or CI-invoked validation entrypoint.

- edge_cases_or_failure_modes: A listed but missing suite is skipped, which can mask missing coverage if a test file is accidentally removed (`tests/run-all-tests.sh:52-55`). Count extraction depends on exact English labels `Passed:` and `Failed:`; suites with different summary wording contribute zero counts unless their exit status fails (`tests/run-all-tests.sh:69-70`). The ANSI stripper only removes CSI color sequences ending in `m`, so other terminal control sequences remain in parsed output (`tests/run-all-tests.sh:67-68`). Because it captures full child output into a shell variable before printing anything, very large suite output can increase memory use and hides progress until each suite exits (`tests/run-all-tests.sh:62`). Non-executable test files that exist are attempted and should fail through the captured command exit code path (`tests/run-all-tests.sh:62-76`). If a suite emits multiple summary blocks, the runner uses the last matched `Passed:` and `Failed:` lines via `tail -1` (`tests/run-all-tests.sh:69-70`).

- validation_or_tests: The runner is itself the top-level validation command: `./tests/run-all-tests.sh`. Its parsing behavior is specifically regression-tested by `tests/test-ansi-parsing.sh`, including ANSI stripping with `$'\033'` and `sed` (`tests/test-ansi-parsing.sh:38-48`), extraction of colored pass/fail counts (`tests/test-ansi-parsing.sh:73-90`), multiline output simulation using `tail -1` (`tests/test-ansi-parsing.sh:115-129`), plain text parsing (`tests/test-ansi-parsing.sh:133-144`), and combined bold/color sequences (`tests/test-ansi-parsing.sh:147-158`). The aggregator also includes `test-finalize-phase.sh`, which is relevant to branch behavior around finalization (`tests/run-all-tests.sh:45`).

- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-055 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Static block-message template for the prompt-file immutability gate. It defines the user-facing contract emitted when an agent attempts to write, edit, or shell-modify a generated `round-*-prompt.md` instruction file. This is algorithmically relevant because it is part of the hook enforcement path that prevents workers from altering their own Codex-provided instructions.

- algorithmic_behavior: The template itself is declarative markdown: title, prohibition, rationale, required workflow, and error-handling instruction (`prompt-template/block/prompt-file-write.md:1-12`). Its active behavior comes from callers that load and print it when prompt-file modification is detected. `prompt_write_blocked_message` supplies this template name to `load_and_render_safe` with an inline fallback (`hooks/lib/loop-common.sh:201-207`). Write and Edit validators lower-case the target path, check whether it is a prompt round file, emit this message, and exit with blocking status `2` (`hooks/loop-write-validator.sh:29-49`, `hooks/loop-edit-validator.sh:29-48`). The Bash validator similarly blocks shell commands that modify `round-[0-9]+-prompt.md`, emits the same message, and exits `2` (`hooks/loop-bash-validator.sh:296-303`).

- inputs_outputs_state: The template has no variables or placeholders; its only input is successful template loading from `prompt-template/block/prompt-file-write.md`. The rendered output is the fixed markdown message. In the hook flow, relevant inputs are the tool name and target path for Write/Edit hooks, or a normalized shell command for Bash validation. The state transition is an attempted prompt-file mutation becoming a blocked tool action: validator prints the block message to stderr and exits `2`, leaving the target prompt file unchanged. The template tells the worker to read the current prompt, execute the tasks, and write results to the summary file instead (`prompt-template/block/prompt-file-write.md:7-10`).

- gates_or_invariants: The main invariant is that `round-*-prompt.md` files are instruction sources “FROM Codex TO you” and cannot be modified by the receiving agent (`prompt-template/block/prompt-file-write.md:3-7`). Error correction is routed through the summary file rather than prompt mutation (`prompt-template/block/prompt-file-write.md:12`). The shared matcher for Write/Edit paths accepts lowercased paths ending in `round-[0-9]+-prompt.md` (`hooks/lib/loop-common.sh:146-153`), so the hook gate is numeric-round-specific. The Bash validator uses a similar numeric-round regex for commands (`hooks/loop-bash-validator.sh:301`). The loader has a fallback invariant: if the template is missing or renders empty, `load_and_render_safe` returns the inline fallback rather than allowing a blank block reason (`hooks/lib/template-loader.sh:170-202`).

- dependencies_and_callers: The template depends on the template loader stack: `loop-common.sh` sources `template-loader.sh`, resolves `TEMPLATE_DIR`, and warns rather than hard-failing if template directory validation fails (`hooks/lib/loop-common.sh:46-55`). Its primary wrapper is `prompt_write_blocked_message` (`hooks/lib/loop-common.sh:201-207`). Direct runtime callers are `hooks/loop-write-validator.sh:47-49`, `hooks/loop-edit-validator.sh:46-48`, and `hooks/loop-bash-validator.sh:301-303`. Generation code creates prompt files separately in the Codex stop hook, including current and next round prompt paths (`hooks/loop-codex-stop-hook.sh:671-672`, `hooks/loop-codex-stop-hook.sh:1063-1075`), which explains why they are protected as generated inputs rather than worker outputs.

- edge_cases_or_failure_modes: If this template is absent or empty, the blocking behavior still functions through the fallback text in `prompt_write_blocked_message`, but the richer instruction list from the template is lost (`hooks/lib/loop-common.sh:203-207`, `hooks/lib/template-loader.sh:179-186`). The template wording says `round-*-prompt.md`, while the actual Write/Edit matcher only detects numeric round filenames ending exactly in `round-[0-9]+-prompt.md`; similarly named files outside that pattern are not covered by this specific gate (`hooks/lib/loop-common.sh:148-152`). Review prompt files such as `round-N-review-prompt.md` are generated elsewhere but do not match this exact prompt-file regex, so protection for those would need to come from another rule if required. Because the template has no variables, rendering cannot adapt the message to include the exact offending path.

- validation_or_tests: `tests/test-template-references.sh` includes `block/prompt-file-write.md` in the common-template existence set and fails if it is missing (`tests/test-template-references.sh:149-166`). The same test scans templates for shell-script references and treats direct references as pass-worthy informational evidence (`tests/test-template-references.sh:130-141`). Hook behavior around prompt-file blocking is covered indirectly by the validator-focused suites included in the top-level runner, especially `test-bash-validator-patterns.sh`, `test-plan-file-hooks.sh`, `test-template-references.sh`, and `test-templates-comprehensive.sh` as listed by `tests/run-all-tests.sh:35-41`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence headings present exactly once`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`