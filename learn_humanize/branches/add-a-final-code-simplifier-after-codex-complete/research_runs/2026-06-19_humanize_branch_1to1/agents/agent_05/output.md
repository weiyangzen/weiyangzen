# agent_05 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-005 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory contains the shell entry points that initialize and observe the RLCR loop state machine. It has exactly three recursive children: `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`. `setup-rlcr-loop.sh` creates the loop session, state files, goal tracker, and round-0 prompt; `portable-timeout.sh` normalizes timeout behavior across macOS/Linux; `humanize.sh` provides a monitor UI for active or completed loop sessions.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` parses loop configuration (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--plan-file`, `--track-plan-file`) in lines 43-180, validates the project and plan file in lines 186-423, records branch/model safety constraints in lines 429-461, then creates `.humanize/rlcr/<timestamp>/` with `state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` in lines 467-653. The branch-specific final simplifier behavior is present in the generated round-0 prompt: line 649 tells the worker to use `code-simplifier` if available before commit. `scripts/portable-timeout.sh` chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback in lines 10-29 and exposes `run_with_timeout` in lines 33-71. `scripts/humanize.sh` finds the latest timestamped RLCR session in lines 23-41, finds matching Codex logs under `$HOME/.cache/humanize/<sanitized-project>/<timestamp>/` in lines 43-96, parses loop state in lines 98-154, parses goal tracker progress in lines 156-238, parses git status in lines 240-286, renders a fixed terminal status bar in lines 302-440, and switches between sessions/log files in the monitor loop in lines 489-679.
- inputs_outputs_state: Inputs include the plan path and flags passed through `/humanize:start-rlcr-loop`, the current git repository, `codex` availability, the current branch name, `.gitmodules`, the plan file content, `.humanize/rlcr` state folders, `$HOME/.cache/humanize` Codex logs, and current git status. Outputs from `setup-rlcr-loop.sh` are the loop directory, backup plan, YAML-front-matter `state.md`, initialized `goal-tracker.md`, `round-0-prompt.md`, and the activation prompt printed to stdout. `portable-timeout.sh` exports `TIMEOUT_IMPL` and returns the wrapped command’s status, using `124` for Python timeout expiry to match GNU timeout. `humanize.sh` does not mutate loop state; it outputs a terminal monitor with session, round, model, status, goal progress, git status, and log tail.
- gates_or_invariants: `setup-rlcr-loop.sh` fails closed on missing plan, multiple plan file inputs, non-git repo, repo without commits, absolute plan path, spaces in path, shell metacharacters in path, symlink plan, missing/unreadable file, path escaping project root, submodule-contained plan, timed-out git checks, tracked-vs-gitignored mismatch, too few lines, insufficient content lines, missing `codex`, YAML-unsafe branch/model/effort, or unsafe model/effort characters. Key references: path checks at lines 227-290, submodule check at lines 292-305, tracking policy at lines 311-356, content checks at lines 362-415, `codex` gate at lines 417-423, and YAML safety at lines 435-461. `humanize.sh` requires `.humanize/rlcr` in lines 16-21 and at least one valid timestamped session in lines 493-498. `portable-timeout.sh` preserves command exit status and warns before running without a timeout in lines 64-68.
- dependencies_and_callers: `commands/start-rlcr-loop.md` restricts the slash command to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` and invokes that script with `$ARGUMENTS` at lines 1-14. `setup-rlcr-loop.sh` sources `scripts/portable-timeout.sh` at lines 26-28. `hooks/loop-codex-stop-hook.sh` also sources `portable-timeout.sh` at line 52 and references it again at line 794 for Codex timeout behavior. The final simplifier flow coordinates with `hooks/loop-codex-stop-hook.sh`: on a Codex `COMPLETE`, the hook renames `state.md` to `finalized-state.md`, renders `prompt-template/claude/finalize-phase-prompt.md`, and blocks exit with a simplification prompt in lines 961-1020. The finalize prompt itself requires `code-simplifier:code-simplifier` at `prompt-template/claude/finalize-phase-prompt.md` lines 7-14. `README.md` references `humanize monitor rlcr-loop` and lists the script files, but the executable algorithm lives in the scripts and hooks.
- edge_cases_or_failure_modes: Timeout fallback can silently degrade to no timeout if neither GNU timeout nor Python is available, though it emits a warning. `setup-rlcr-loop.sh` treats Markdown heading lines starting with `#` as comments during content-line counting, so a plan made mostly of headings can fail the “meaningful content” gate. Branch names with `:`, `#`, quotes, backticks, or newline are rejected to keep generated YAML safe. Plan paths with spaces are rejected explicitly for YAML serialization consistency. The monitor handles no-log sessions by rendering a waiting/status message and polling for logs; it also switches to newer timestamped sessions even before a log exists. The monitor assumes terminal capabilities such as `tput`, scroll regions, and a usable terminal width; those are operational dependencies rather than repository state.
- validation_or_tests: Read-only validation performed: `bash -n scripts/humanize.sh scripts/portable-timeout.sh scripts/setup-rlcr-loop.sh tests/test-template-loader.sh hooks/lib/template-loader.sh hooks/loop-edit-validator.sh hooks/loop-write-validator.sh hooks/loop-codex-stop-hook.sh` completed with exit 0. Related executable coverage includes `tests/test-plan-file-validation.sh`, which calls `scripts/setup-rlcr-loop.sh` across path, tracking, content, and model validation scenarios, and `tests/test-finalize-phase.sh`, where lines 460-470 assert that Codex `COMPLETE` enters Finalize Phase and the prompt mentions simplification. I did not modify or run state-creating setup flows because this branch export is read-only and the assignment requested research notes only.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-035 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: This is an executable specification for the prompt-template rendering layer used by RLCR validators and Codex/Claude prompt assembly. It directly sources `hooks/lib/template-loader.sh` at line 12 and verifies path resolution, template loading, single-pass rendering, fallback rendering, template directory validation, literal handling of shell metacharacters, placeholder-injection prevention, and real rendering of `prompt-template/block/wrong-round-number.md`.
- algorithmic_behavior: The test harness initializes `PROJECT_ROOT` from the test location in lines 10-12, uses `set -euo pipefail` at line 8, and tracks pass/fail counters in lines 20-34. Tests 1-3 cover template directory resolution and missing/existing template loads in lines 42-78. Tests 4-10 cover basic rendering semantics: single variable, multiple variables, multiline templates, path-like values, unused variables, and unreplaced placeholders in lines 81-194. Test 8 is the assigned real-template integration case: it renders `block/wrong-round-number.md` with `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`, then expects the title, `round-3-summary.md`, and current round `5` text in lines 147-164. Tests 11-14 cover safe fallback rendering and template directory validation in lines 197-244. Tests 15-33 cover literal preservation for shell metacharacters and structured values in lines 247-486. Tests 34-36 assert single-pass rendering prevents injected `{{...}}` placeholders inside values from being re-expanded in lines 488-543. Tests 37-41 cover empty values, ASCII/non-ASCII Unicode, underscore-prefixed names, and numbered variable names in lines 545-639. The summary gate exits 0 only if `TESTS_FAILED == 0` in lines 641-659.
- inputs_outputs_state: Inputs are the repository’s `hooks/lib/template-loader.sh`, the `prompt-template` directory, especially `prompt-template/block/git-push.md` and `prompt-template/block/wrong-round-number.md`, and inline template/value fixtures inside the test file. Outputs are colored PASS/FAIL lines plus a final summary. State is limited to `TESTS_PASSED` and `TESTS_FAILED`. The test intentionally uses command substitutions like `RESULT=$(render_template ...)` to assert exact stdout rendering.
- gates_or_invariants: The core invariant under test is that `render_template` performs literal, single-pass replacement. The implementation documents and implements this in `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}` syntax, missing variables stay as-is, and values containing `{{OTHER_VAR}}` are not rescanned, described in lines 7-13 and implemented with an `awk` scan in lines 58-132. The test also asserts that missing templates return empty for `load_template`, while `load_and_render_safe` falls back to a caller-provided message. `validate_template_dir` must require `block`, `codex`, and `claude` subdirectories, matching `hooks/lib/template-loader.sh` lines 208-222.
- dependencies_and_callers: This test depends on `hooks/lib/template-loader.sh` functions `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. The same loader is used by loop validators and hooks through `hooks/lib/loop-common.sh` and direct hook calls. Relevant consumers include `hooks/loop-edit-validator.sh` lines 141-146 and `hooks/loop-write-validator.sh` lines 191-196 for the wrong-round-number block, plus `hooks/loop-codex-stop-hook.sh` lines 1005-1008 for the Finalize Phase prompt. `tests/run-all-tests.sh` includes `test-template-loader.sh` in its test list.
- edge_cases_or_failure_modes: The test explicitly covers missing template files, unused substitutions, unreplaced placeholders, empty substitution values, multiple variable replacement, multiline templates and values, path separators, Windows-style backslashes, regex text, JSON text, Markdown text, shell-sensitive characters (`&`, `$`, backticks, pipes, semicolons, glob characters, parentheses, redirections, quotes, hash, tilde), and prompt-corrupting placeholder injection. An observed environment-specific failure is that `render_template` uses a Bash here-string into `awk` at `hooks/lib/template-loader.sh` line 129; in this read-only sandbox, Bash reported “cannot create temp file for here document: Operation not permitted,” causing rendering tests to fail despite file syntax being valid. A separate observed edge in this environment was `env_vars[@]: unbound variable` when rendering an existing template with no substitutions under `set -u`, reached by test 12.
- validation_or_tests: `bash -n tests/test-template-loader.sh hooks/lib/template-loader.sh` passed as part of the syntax validation command. Running `bash tests/test-template-loader.sh` in the read-only branch export produced 5 passes and 38 failures. The failures were dominated by the sandbox temp-file restriction at `hooks/lib/template-loader.sh` line 129, so this run is not a clean functional validation of the renderer under normal writable-temp conditions. The successful portions before renderer execution verified directory resolution, load of an existing template, missing-template empty behavior, and valid/invalid template directory checks.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-065 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: This block template defines the user-facing rejection message for attempts to edit or write a round summary file with a round number that does not match the active RLCR state. It is small, but it is algorithmically relevant because validators use it as the block reason when enforcing the current-round invariant.
- algorithmic_behavior: The template title is `Wrong Round Number` at line 1. Line 3 renders the attempted action, the attempted file name `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, and the authoritative `{{CURRENT_ROUND}}`. Line 5 renders the correct target path, and line 7 tells the worker not to increment the round number manually. It has no control flow itself; its behavior is parameterized by `load_and_render_safe` in validator scripts.
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Output is Markdown block text printed to stderr by validators and returned inside hook block decisions. It has no persistent state. When rendered by the assigned test with `ACTION=edit`, `CLAUDE_ROUND=3`, `FILE_TYPE=summary`, `CURRENT_ROUND=5`, and `CORRECT_PATH=/tmp/round-5-summary.md`, the expected output includes `round-3-summary.md`, current round `5`, and the correct path.
- gates_or_invariants: The invariant is that summary files must use the active round number. `hooks/loop-edit-validator.sh` enforces this for edit operations: if the target is a summary file, it extracts `CLAUDE_ROUND`, compares it to `CURRENT_ROUND`, excludes allowlisted files, renders this template, and exits 2 in lines 124-148. `hooks/loop-write-validator.sh` enforces the same invariant for write operations in lines 181-198. The block message also reinforces a process rule: the worker should not independently advance the round number.
- dependencies_and_callers: Direct callers are `hooks/loop-edit-validator.sh` lines 141-146 and `hooks/loop-write-validator.sh` lines 191-196, both through `load_and_render_safe`. The loader dependency is `hooks/lib/template-loader.sh`, particularly `load_and_render_safe` lines 170-203. It is directly exercised by `tests/test-template-loader.sh` lines 147-164 and by `tests/test-templates-comprehensive.sh` around its real-template coverage. The validators depend on shared helpers such as `extract_round_number`, `is_round_file_type`, `is_allowlisted_file`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `TEMPLATE_DIR`, supplied by the hook environment/common library.
- edge_cases_or_failure_modes: If the template is missing or empty, both validators use fallback messages embedded near their call sites, so blocking still occurs. If a filename lacks an extractable round number, the wrong-round gate is skipped. If the file is allowlisted, mismatched round numbers are not blocked. If `CURRENT_ROUND` is stale or incorrectly parsed upstream, this template will faithfully report the wrong authoritative round because it does not validate the state itself. The template is summary-oriented in current callers because `FILE_TYPE=summary`; using another file type depends on caller-provided values.
- validation_or_tests: `tests/test-template-loader.sh` test 8 verifies the template renders with real substitutions and expected content. `tests/test-templates-comprehensive.sh` also references this block as a real template. In the read-only sandbox run, test 8 failed because the renderer could not create the shell temporary file required by the here-string, not because this Markdown template was malformed. Direct inspection confirms the five placeholders used by callers are present and spelled consistently.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3` section headers present, one per assigned item
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`