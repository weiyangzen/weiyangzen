# agent_30 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-030 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable negative-path specification for `hooks/lib/template-loader.sh`; it defines how prompt template loading/rendering must behave under missing files, malformed placeholders, strict shell mode, and fallback paths.
- algorithmic_behavior: The script resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources `hooks/lib/template-loader.sh`, computes `TEMPLATE_DIR`, then runs 12 isolated assertions with `pass`/`fail` counters (`tests/test-error-scenarios.sh:11-35`). It verifies missing template files and directories return empty output with exit 0 (`:45-65`), `load_and_render` returns empty for missing templates (`:71-78`), empty template content returns empty (`:83-91`), special regex-looking characters in values render literally (`:97-106`), missing templates do not terminate a nested `set -euo pipefail` script (`:112-131`), `load_and_render_safe` falls back and substitutes variables in fallback text (`:137-158`), template directory validation accepts/rejects expected paths (`:164-180`), and malformed placeholders stay unchanged (`:186-206`). Final status exits 0 only when `TESTS_FAILED == 0` (`:218-225`).
- inputs_outputs_state: Inputs are the repository filesystem, the sourced loader functions, `TEMPLATE_DIR`, inline template strings, and `VAR=value` substitution arguments. Outputs are colored PASS/FAIL lines, a summary count, and process exit code. State is limited to in-process `TESTS_PASSED`/`TESTS_FAILED` increments; there is no persistent write state. The script deliberately uses `set -uo pipefail` instead of global `set -e`, allowing all cases to run and aggregate failures.
- gates_or_invariants: Missing templates are non-fatal and should produce empty content, not hard errors. `load_and_render_safe` must provide an operator-facing fallback when templates are absent. Rendering must be single-pass and literal-safe, preserving special characters and malformed placeholders. `validate_template_dir` is expected to require the `block`, `codex`, and `claude` subdirectories as implemented in `hooks/lib/template-loader.sh:208-221`. The suite gate is binary: any failed assertion forces exit 1.
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`, sourced at `tests/test-error-scenarios.sh:13`. It exercises `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`, implemented in `hooks/lib/template-loader.sh:26-221`. It is included in the aggregate suite list by `tests/run-all-tests.sh:32-47`, specifically `tests/run-all-tests.sh:42`.
- edge_cases_or_failure_modes: Covered edge cases include missing files, missing directories, missing templates under strict mode, empty content, values containing spaces/brackets/parentheses/asterisks, fallback strings with placeholders, invalid template roots, empty `{{}}`, and unclosed `{{...` placeholders. A sandbox-specific failure appeared when running this test in the read-only branch export: `render_template` uses a here-string into `awk` at `hooks/lib/template-loader.sh:74-129`, and Bash could not create the temporary here-document file under the current filesystem restrictions, causing the render-dependent checks to return empty.
- validation_or_tests: I ran `bash tests/test-error-scenarios.sh` from the repository root. Result in this managed read-only environment: 8 passed, 4 failed. The failures were the render-dependent cases for special-character substitution, fallback substitution, empty placeholder preservation, and unclosed placeholder preservation; each showed `cannot create temp file for here document: Operation not permitted` from `hooks/lib/template-loader.sh:129`. This validates the test’s failure-reporting behavior but not normal writable-shell behavior.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-060 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-hook blocking prompt template for the `--push-every-round` gate. It is not executable code, but it defines the operator-facing contract used when the loop detects local commits ahead of the remote.
- algorithmic_behavior: The template renders a Markdown block headed “Unpushed Commits Detected” (`prompt-template/block/unpushed-commits.md:1`), interpolates `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into the blocking explanation (`:3`), states that `--push-every-round` requires pushing before exit (`:5`), and emits the required command `git push origin {{CURRENT_BRANCH}}` (`:7-10`). In the stop hook, this rendered text becomes the JSON `reason` for a `"decision": "block"` response (`hooks/loop-codex-stop-hook.sh:501-512`).
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`, supplied by `hooks/loop-codex-stop-hook.sh` after parsing `git status -sb` for `ahead N` and reading the current branch (`hooks/loop-codex-stop-hook.sh:491-494`). Output is rendered Markdown included in the hook response. The template itself has no state transitions; the state transition happens in the hook, from normal exit flow to blocked stop decision when unpushed commits are found.
- gates_or_invariants: The template is only consulted when `PUSH_EVERY_ROUND` is `"true"` (`hooks/loop-codex-stop-hook.sh:489`). The gate condition is a non-empty ahead marker from `git status -sb` (`:491-492`). The invariant it enforces is: when push-every-round is enabled, the worker may not exit while the local branch is ahead of remote. If the template is missing, the hook still blocks using a fallback message (`:496-503`), so template absence should not bypass the gate.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh:486-514`, through `load_and_render_safe`. Rendering semantics come from `hooks/lib/template-loader.sh:170-203`. The feature is user-facing in CLI documentation at `README.md:199`, which describes `--push-every-round` as requiring git push after each round. Related prompt surface exists at `prompt-template/claude/push-every-round-note.md`, referenced by `hooks/loop-codex-stop-hook.sh:1094-1096`.
- edge_cases_or_failure_modes: If branch detection fails, the hook falls back to `unknown` for `CURRENT_BRANCH` (`hooks/loop-codex-stop-hook.sh:494`), which would render `git push origin unknown`; that is safe as a block reason but may be less actionable. The ahead check depends on English `git status -sb` text matching `ahead [0-9]*`; unusual Git output, no upstream, or localized output could avoid this specific template path. Branch names are rendered into text, not executed by the hook, so shell-sensitive characters are advisory risk for the user-entered command rather than hook execution risk.
- validation_or_tests: `tests/test-templates-comprehensive.sh:517-525` renders this real template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserts the output contains the heading, the count phrase, and the branch name. The same rendering path is indirectly protected by the template-loader tests and the safe fallback behavior covered by `tests/test-error-scenarios.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 unique Item Evidence headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`