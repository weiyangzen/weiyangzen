# agent_11 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-011 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side review prompt template bundle for the RLCR loop. It defines the review agent’s contract after each Claude implementation round, including regular review, mandatory full alignment review every fifth round, goal-tracker mutation authority, completion gate semantics, and stagnation circuit-breaking. The directory contains exactly three files: `prompt-template/codex/regular-review.md`, `prompt-template/codex/full-alignment-review.md`, and `prompt-template/codex/goal-tracker-update-section.md`; `wc -c` reports 9101 bytes total.
- algorithmic_behavior: `regular-review.md` is the normal review state contract. It tells Codex to read `@{{PLAN_FILE}}`, compare Claude’s claimed work against `@{{PROMPT_FILE}}`, inspect implementation/docs under `@{{DOCS_PATH}}`, reject deferrals, force pending work into review comments, audit `@{{GOAL_TRACKER_FILE}}`, and only end with `COMPLETE` when all original tasks and ACs are fully complete with no deferrals or pending work (`prompt-template/codex/regular-review.md:3`, `prompt-template/codex/regular-review.md:20`, `prompt-template/codex/regular-review.md:33`, `prompt-template/codex/regular-review.md:49`). `full-alignment-review.md` is the stricter round `CURRENT_ROUND % 5 == 4` checkpoint. It requires a comprehensive audit of immutable ACs, forgotten tasks, deferred items, implementation reality, and the last five rounds for stagnation, with `STOP` as the circuit-breaker marker and `COMPLETE` only when all ACs are met with no deferrals (`prompt-template/codex/full-alignment-review.md:1`, `prompt-template/codex/full-alignment-review.md:19`, `prompt-template/codex/full-alignment-review.md:59`, `prompt-template/codex/full-alignment-review.md:86`). `goal-tracker-update-section.md` is injected into both review modes and gives Codex, not Claude, post-Round-0 authority to update `goal-tracker.md` when Claude requests justified tracker changes (`prompt-template/codex/goal-tracker-update-section.md:1`).
- inputs_outputs_state: Inputs are rendered placeholders supplied by `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:671`, `hooks/loop-codex-stop-hook.sh:680`, `hooks/loop-codex-stop-hook.sh:720`, `hooks/loop-codex-stop-hook.sh:736`). Output is a rendered review prompt at `.humanize/rlcr/<timestamp>/round-N-review-prompt.md`, then Codex is expected to write `round-N-review-result.md` or stdout is copied there as fallback (`hooks/loop-codex-stop-hook.sh:671`, `hooks/loop-codex-stop-hook.sh:899`). State transitions are driven by the last non-empty review line: `COMPLETE` renames `state.md` to `finalized-state.md` and enters the final code-simplifier phase; `STOP` calls `end_loop` with the stop exit; any other substantive review content advances to the next round and creates `round-(N+1)-prompt.md` (`hooks/loop-codex-stop-hook.sh:958`, `hooks/loop-codex-stop-hook.sh:962`, `hooks/loop-codex-stop-hook.sh:980`, `hooks/loop-codex-stop-hook.sh:1023`, `hooks/loop-codex-stop-hook.sh:1057`).
- gates_or_invariants: Regular review’s invariant is no `COMPLETE` unless all original-plan tasks are complete, all ACs are met, and there are no deferrals or pending items (`prompt-template/codex/regular-review.md:53`). Full alignment strengthens that to all original plan tasks done, all ACs met, and no deferrals, with deferred ACs explicitly incomplete (`prompt-template/codex/full-alignment-review.md:88`). The full alignment template adds a stagnation detector over historical round files and instructs `STOP` as the final line if development is stalled (`prompt-template/codex/full-alignment-review.md:63`, `prompt-template/codex/full-alignment-review.md:74`, `prompt-template/codex/full-alignment-review.md:84`). The hook uses strict final-line matching, so “CANNOT COMPLETE” does not trigger finalization; only a trimmed final line exactly equal to the configured marker does (`hooks/loop-codex-stop-hook.sh:955`).
- dependencies_and_callers: The directory is loaded by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` from `hooks/lib/template-loader.sh` (`hooks/loop-codex-stop-hook.sh:680`, `hooks/loop-codex-stop-hook.sh:720`, `hooks/loop-codex-stop-hook.sh:736`). Rendering is single-pass placeholder substitution; missing variables remain literal, preventing injected template content from being recursively expanded (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`). `validate_template_dir` treats `prompt-template/codex` as a required subdirectory alongside `block` and `claude` (`hooks/lib/template-loader.sh:205`). Downstream, full-alignment rounds also affect Claude’s next prompt by appending `prompt-template/claude/post-alignment-action-items.md` after Codex review issues (`hooks/loop-codex-stop-hook.sh:1080`).
- edge_cases_or_failure_modes: If any Codex template is missing or renders empty, the hook falls back to embedded fallback prompt strings, weakening but preserving loop operation (`hooks/loop-codex-stop-hook.sh:696`, `hooks/loop-codex-stop-hook.sh:707`, `hooks/lib/template-loader.sh:167`). If Codex exits nonzero, creates no review file, or writes an empty file, the hook blocks exit and asks for retry rather than advancing the loop (`hooks/loop-codex-stop-hook.sh:878`, `hooks/loop-codex-stop-hook.sh:915`, `hooks/loop-codex-stop-hook.sh:943`). The full-alignment template uses `PREV_PREV_ROUND`; for early rounds this can be negative, but the template is only selected when `CURRENT_ROUND % 5 == 4`, so the first use is round 4 and both previous indices are valid (`hooks/loop-codex-stop-hook.sh:683`, `hooks/loop-codex-stop-hook.sh:692`). If Codex emits `STOP` on a non-alignment round, the hook still honors it but labels it unexpected (`hooks/loop-codex-stop-hook.sh:1039`). Because replacement is single-pass, `SUMMARY_CONTENT` or `REVIEW_CONTENT` containing `{{...}}` cannot corrupt prompt variables, but genuinely missing variables remain visible in the final prompt.
- validation_or_tests: `tests/test-templates-comprehensive.sh` directly tests `codex/goal-tracker-update-section.md` rendering with `GOAL_TRACKER_FILE`, asserting both the heading and rendered path appear (`tests/test-templates-comprehensive.sh:531`). General template-loader tests cover real template rendering and stress repeated substitutions, but there is no observed direct test for full `regular-review.md` or `full-alignment-review.md` marker semantics beyond hook logic. I could not use `git status` for repository validation because this read-only branch export does not expose usable git metadata and macOS toolchain cache writes were denied.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-041 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Failure diagnostic block template intended to report a Codex review execution failure: exit code, missing review result file, debug command/stdout/stderr paths, and the tail of stderr (`prompt-template/block/codex-review-failed.md:1`). It is a user-facing failure contract rather than a review algorithm itself.
- algorithmic_behavior: The template describes the failed Codex-review transition when no review result is produced. It has placeholders for `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5`). Its operational instruction is to inspect debug files and retry review when the agent exits (`prompt-template/block/codex-review-failed.md:18`).
- inputs_outputs_state: Inputs are failure metadata from the Codex invocation path: exit status, expected output file, debug artifact paths, and stderr tail. Output would be a rendered Markdown block suitable for a hook block reason. Intended state transition is “block current exit and retry later,” matching the hook’s actual `codex_failure_exit` behavior that emits JSON with `"decision": "block"` and exits 0 for the Claude Code hook protocol (`hooks/loop-codex-stop-hook.sh:850`, `hooks/loop-codex-stop-hook.sh:869`).
- gates_or_invariants: The failure gate is triggered when Codex exits nonzero, produces no review result file after stdout fallback, or creates an empty review result (`hooks/loop-codex-stop-hook.sh:878`, `hooks/loop-codex-stop-hook.sh:899`, `hooks/loop-codex-stop-hook.sh:915`, `hooks/loop-codex-stop-hook.sh:943`). The invariant is that the loop must not advance without a non-empty `REVIEW_RESULT_FILE`; missing output is treated as infrastructure failure, not review success.
- dependencies_and_callers: Search found no runtime caller for `prompt-template/block/codex-review-failed.md`; only the file itself matched its distinctive placeholders. The actual hook currently builds similar failure text inline in `codex_failure_exit` rather than calling `load_and_render_safe` on this template (`hooks/loop-codex-stop-hook.sh:856`). Related debug files are created under `$HOME/.cache/humanize/<sanitized-project>/<loop_timestamp>/` before Codex runs (`hooks/loop-codex-stop-hook.sh:779`, `hooks/loop-codex-stop-hook.sh:789`).
- edge_cases_or_failure_modes: As a template, if it were used, large stderr is bounded by precomputed `STDERR_CONTENT`, and the file labels `REVIEW_RESULT_FILE` as “not created,” which is accurate only for the no-result case, not nonzero exit with a partial or empty file (`prompt-template/block/codex-review-failed.md:6`). Since the active hook hardcodes a different failure body and uses the last 30 lines of stderr, this template can drift from real behavior; it says “last 50 lines” while the hook tails 30 lines (`prompt-template/block/codex-review-failed.md:13`, `hooks/loop-codex-stop-hook.sh:882`).
- validation_or_tests: No direct test reference was found for this template in `tests/test-templates-comprehensive.sh` or hook call sites. The covered behavior is the hook’s inline failure path, not this Markdown file. Direct inspection confirms all placeholders are plain `{{VAR}}` syntax compatible with `hooks/lib/template-loader.sh`.
- skip_candidate: `yes: currently appears unused by runtime callers; the active Codex failure path is hardcoded in hooks/loop-codex-stop-hook.sh, so this file is likely dead or legacy unless loaded by an external path not present in this branch export`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-071 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Claude-side prompt appendix used immediately after a full Codex goal-alignment review. It translates Codex’s full-alignment findings into next-round implementation priorities: forgotten items, unmet ACs, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2`).
- algorithmic_behavior: The file is static Markdown, not parameterized. It instructs the next Claude round to pay special attention to a preceding Full Goal Alignment Check, address forgotten tasks, prioritize ACs marked `NOT MET`, and un-defer unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:4`). It acts as a bridge from Codex’s audit classifications in `full-alignment-review.md` back into Claude’s next implementation prompt.
- inputs_outputs_state: Input is implicit loop state: the current review just completed on a full-alignment round where `CURRENT_ROUND % 5 == 4`. Output is appended text in the next round prompt `round-(N+1)-prompt.md` after `claude/next-round-prompt.md` is rendered with review feedback and before the final footer/push/update-request templates are appended (`hooks/loop-codex-stop-hook.sh:1062`, `hooks/loop-codex-stop-hook.sh:1075`, `hooks/loop-codex-stop-hook.sh:1080`). It does not mutate files directly; it changes the next agent instruction surface.
- gates_or_invariants: The append gate is exact modulo scheduling: only after a full alignment check, `if [[ $((CURRENT_ROUND % 5)) -eq 4 ]]` (`hooks/loop-codex-stop-hook.sh:1080`). The hook also gates on non-empty template load before appending, so a missing or empty file silently skips this appendix rather than blocking the loop (`hooks/loop-codex-stop-hook.sh:1082`). It is only reached when Codex did not emit `COMPLETE` or `STOP`; those markers exit earlier into finalize or stopped states (`hooks/loop-codex-stop-hook.sh:962`, `hooks/loop-codex-stop-hook.sh:1023`, `hooks/loop-codex-stop-hook.sh:1053`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which uses `load_template`, not `load_and_render_safe`, so no placeholder rendering occurs (`hooks/loop-codex-stop-hook.sh:1082`). It depends semantically on `prompt-template/codex/full-alignment-review.md`, whose audit sections produce the forgotten-item, AC-status, and deferral findings this appendix tells Claude to address (`prompt-template/codex/full-alignment-review.md:30`, `prompt-template/codex/full-alignment-review.md:36`, `prompt-template/codex/full-alignment-review.md:42`). It coordinates with `claude/next-round-prompt.md`, `claude/next-round-footer.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md` in the next-prompt assembly sequence (`hooks/loop-codex-stop-hook.sh:1075`, `hooks/loop-codex-stop-hook.sh:1091`, `hooks/loop-codex-stop-hook.sh:1095`, `hooks/loop-codex-stop-hook.sh:1103`).
- edge_cases_or_failure_modes: If the full-alignment review found stagnation and ended with `STOP`, this appendix is never appended because the hook terminates first (`hooks/loop-codex-stop-hook.sh:1023`). If full-alignment review found all work complete and ended with `COMPLETE`, the loop enters finalization instead, so this appendix also does not apply (`hooks/loop-codex-stop-hook.sh:971`). Because the template is static and loaded without rendering, any future placeholders added to the file would remain literal. If the file is missing, the loop loses a useful prioritization hint but continues without warning because stderr is redirected and only non-empty content is appended (`hooks/loop-codex-stop-hook.sh:1082`).
- validation_or_tests: No direct test reference was found for this Claude appendix. Its behavior is covered only indirectly by hook control flow. Direct inspection confirms it has no variables and no file-writing instructions beyond implementation prioritization.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-011`, `ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-041`, `ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-071`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`