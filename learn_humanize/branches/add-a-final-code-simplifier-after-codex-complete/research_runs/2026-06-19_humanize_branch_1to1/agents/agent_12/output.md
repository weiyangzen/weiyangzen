# agent_12 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-012 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: User-facing slash-command workflow for terminating the active RLCR loop. It is a control-plane command, not an implementation algorithm, but it is core to the loop state machine because it defines the only documented manual cancellation path.
- algorithmic_behavior: The command selects the newest `.humanize/rlcr/*/` directory with `ls -1d ... | sort -r | head -1` (`commands/cancel-rlcr-loop.md:11-16`). If no directory exists, it reports no active loop (`commands/cancel-rlcr-loop.md:18`). If the newest loop lacks `state.md`, it treats the loop as already ended or malformed and refuses cancellation (`commands/cancel-rlcr-loop.md:20-27`). If active, it reads state for round/max metadata, creates `.cancel-requested` first, then renames `state.md` to `cancel-state.md` (`commands/cancel-rlcr-loop.md:28-32`).
- inputs_outputs_state: Input is the repository-local loop directory tree under `.humanize/rlcr/`, with the newest timestamped directory treated as authoritative. Output is a preserved terminal state file `cancel-state.md` plus a `.cancel-requested` signal file. State transition is `state.md -> cancel-state.md`; `cancel-state.md` is not considered active by the shared active-loop finder, which only recognizes `state.md` and `finalized-state.md` (`hooks/lib/loop-common.sh:58-80`).
- gates_or_invariants: The markdown command itself limits allowed tools to listing loop directories, touching `.cancel-requested`, moving `state.md` to `cancel-state.md`, reading state, and `Read` (`commands/cancel-rlcr-loop.md:3`). Runtime enforcement is stricter in `is_cancel_authorized`: the signal file must exist, command substitutions/backticks/newlines/shell chaining are rejected, the command must be `mv`, it must have exactly two args, and args must normalize exactly to active-loop `state.md` and `cancel-state.md` (`hooks/lib/loop-common.sh:274-411`). The Bash validator permits state-file moves only if this authorization passes (`hooks/loop-bash-validator.sh:92-99`, `hooks/loop-bash-validator.sh:236-242`).
- dependencies_and_callers: Related start command documents cancellation as one stop condition (`commands/start-rlcr-loop.md:51-55`). `find_active_loop` in `hooks/lib/loop-common.sh` defines the “newest directory only” active-loop rule used by validators (`hooks/lib/loop-common.sh:58-80`). `loop-bash-validator.sh` is the enforcement caller that allows the cancellation move only when the command matches the signal-file protocol (`hooks/loop-bash-validator.sh:75-99`, `hooks/loop-bash-validator.sh:246-263`). README mirrors the same cancellation behavior and terminal filename convention (`README.md:118-126`, `README.md:256-257`).
- edge_cases_or_failure_modes: If an older loop has `state.md` but the newest loop does not, cancellation reports no active loop because the system intentionally ignores older “zombie” loops. If `.cancel-requested` is missing, in the wrong directory, or paired with a wrong `mv`, the Bash validator blocks. Commands with extra args, wrong source/destination directories, shell wrappers, substitutions, command chaining, or hidden variables are expected to fail authorization. The command workflow only handles normal `state.md`; a loop already in Finalize Phase with `finalized-state.md` is not cancellable through this exact markdown sequence unless a separate path handles it.
- validation_or_tests: `tests/test-cancel-signal-file.sh` explicitly covers the signal-file mechanism: allowed `mv state.md -> cancel-state.md` with signal (`tests/test-cancel-signal-file.sh:78-92`), blocked move without signal (`tests/test-cancel-signal-file.sh:119-130`), wrong-directory move blocked even with signal (`tests/test-cancel-signal-file.sh:1206-1225`), no-active-loop allowed path (`tests/test-cancel-signal-file.sh:1227-1247`), and direct helper tests for `is_cancel_authorized` (`tests/test-cancel-signal-file.sh:1249-1288`). I did not execute tests; this is read-only research evidence.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-042 `file` `prompt-template/block/finalized-state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when an agent tries to modify `finalized-state.md`. It defines the user-visible contract for Finalize Phase state ownership and redirects work toward code simplification, committing, and `finalize-summary.md`.
- algorithmic_behavior: The template states that `finalized-state.md` is managed by the loop system during Finalize Phase and lists the allowed next actions: run the code-simplifier agent, commit changes, and write `finalize-summary.md` (`prompt-template/block/finalized-state-file-modification.md:1-8`). It does not implement detection itself; it supplies the block reason rendered by hook logic.
- inputs_outputs_state: Input is a validator-detected attempted Write/Edit/Bash modification involving `finalized-state.md`. Output is a blocking message. The broader state transition is entered when Codex returns `COMPLETE`: the stop hook renames `state.md` to `finalized-state.md` and blocks exit with a Finalize prompt (`hooks/loop-codex-stop-hook.sh:961-1012`). After all Finalize checks pass, the stop hook renames `finalized-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:645-656`).
- gates_or_invariants: `finalized-state.md` must be checked before generic `state.md` because `state.md` matching also catches the suffix (`hooks/loop-write-validator.sh:101-108`, `hooks/loop-edit-validator.sh:81-93`, `hooks/loop-bash-validator.sh:75-90`). `is_finalized_state_file_path` matches only paths ending in `finalized-state.md` (`hooks/lib/loop-common.sh:262-266`). Write validator allows `finalize-summary.md` in the active loop during Finalize Phase, but still blocks `finalized-state.md` itself (`hooks/loop-write-validator.sh:115-125`). The Bash validator blocks writes, redirects, and mv/cp source or destination attempts involving `finalized-state.md` (`hooks/loop-bash-validator.sh:87-90`, `hooks/loop-bash-validator.sh:230-253`).
- dependencies_and_callers: `finalized_state_file_blocked_message` loads this template with a fallback (`hooks/lib/loop-common.sh:219-226`). `load_and_render_safe` falls back when a template is missing or empty (`hooks/lib/template-loader.sh:168-184`). Callers include write, edit, and bash validators (`hooks/loop-write-validator.sh:105-107`, `hooks/loop-edit-validator.sh:86-88`, `hooks/loop-bash-validator.sh:87-90`). The Finalize Phase itself is created and consumed by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:66-89`, `hooks/loop-codex-stop-hook.sh:979-1008`).
- edge_cases_or_failure_modes: Because generic `state.md` patterns also match `finalized-state.md`, ordering is critical; reversed checks would show the wrong block message or potentially route through generic state handling. Missing template content is not fatal because the common helper has a fallback, but the fallback is shorter and omits the code-simplifier/finalize-summary action list. Bash source-protection covers copying or moving `finalized-state.md` away, not just writing to it.
- validation_or_tests: `tests/test-finalize-phase.sh` covers helper matching for `finalized-state.md` and `finalize-summary.md` (`tests/test-finalize-phase.sh:174-205`), Write/Edit/Bash blocking for `finalized-state.md` (`tests/test-finalize-phase.sh:293-350`), Finalize summary allowance (`tests/test-finalize-phase.sh:279-290`), missing finalize summary blocking (`tests/test-finalize-phase.sh:365-380`), Finalize completion rename to `complete-state.md` (`tests/test-finalize-phase.sh:412-440`), and `COMPLETE` triggering Finalize Phase with a simplification prompt (`tests/test-finalize-phase.sh:460-475`). Template reference tests scan `load_template` and `load_and_render_safe` calls for missing template paths (`tests/test-template-references.sh:83-105`). I did not execute tests.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-072 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Small Claude prompt fragment appended when the RLCR loop is configured with `--push-every-round`. It is a policy reminder surface for the git push invariant, not a standalone algorithm.
- algorithmic_behavior: The template contains one instruction: when `--push-every-round` is enabled, commits must be pushed to remote after each round (`prompt-template/claude/push-every-round-note.md:1-2`). The stop hook conditionally appends this template to the next round prompt when `STATE_PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:1088-1101`). The setup script similarly appends equivalent text to the round-0 prompt when the startup flag is parsed (`scripts/setup-rlcr-loop.sh:148-150`, `scripts/setup-rlcr-loop.sh:655-660`).
- inputs_outputs_state: Input is the parsed `push_every_round` state field from `state.md`/`finalized-state.md`; output is additional prompt text written to the next Claude prompt. The flag originates from `/humanize:start-rlcr-loop ... --push-every-round` (`commands/start-rlcr-loop.md:1-4`) and is parsed by the setup script into `PUSH_EVERY_ROUND=true` (`scripts/setup-rlcr-loop.sh:148-150`).
- gates_or_invariants: Prompting is paired with enforcement. If `push_every_round` is false, `loop-bash-validator.sh` blocks `git push` and points users to the flag (`hooks/loop-bash-validator.sh:56-72`). If true, the stop hook checks for an ahead-of-remote branch and blocks exit until pushed (`hooks/loop-codex-stop-hook.sh:485-514`). Thus the note is advisory, but the invariant is enforced at exit.
- dependencies_and_callers: Loaded through `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` in the stop hook (`hooks/loop-codex-stop-hook.sh:1094-1101`). `load_template` returns file content or an empty string with a warning if missing (`hooks/lib/template-loader.sh:34-48`), and the stop hook falls back to “Also push your changes after committing.” if the template is absent (`hooks/loop-codex-stop-hook.sh:1096-1099`).
- edge_cases_or_failure_modes: Missing template does not break the loop because of the inline fallback. The note alone cannot prove a push occurred; enforcement depends on `git status -sb` showing an ahead count, so detached/headless remotes or unusual status output could affect detection. Round 0 uses equivalent hardcoded setup-script text rather than this template, so wording can drift between initial and later prompts.
- validation_or_tests: Template reference coverage checks that referenced templates exist (`tests/test-template-references.sh:83-105`). Push enforcement is covered indirectly by hook logic: false blocks direct `git push` (`hooks/loop-bash-validator.sh:63-72`), true blocks exit with ahead commits (`hooks/loop-codex-stop-hook.sh:489-514`). I did not execute tests.
- skip_candidate: `yes: this file is a prompt note with no internal branching or state mutation; it is core only as a policy surface consumed by the RLCR prompt-generation algorithm.`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present; IDs are listed only in section headers to preserve exact-once evidence sections`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`