# agent_15 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-015 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Central Claude plugin hook registry for the RLCR state machine. It is not itself executable business logic, but it is the dispatch table that determines which lifecycle events enforce RLCR validation and review. The file declares "Humanize Plugin Hooks - Validation hooks and Stop hooks for /start-rlcr-loop" at `hooks/hooks.json:2`.
- algorithmic_behavior: Registers three lifecycle groups. `UserPromptSubmit` invokes `${CLAUDE_PLUGIN_ROOT}/hooks/loop-plan-file-validator.sh` before submitted prompts proceed (`hooks/hooks.json:4-10`). `PreToolUse` binds tool-specific validators for `Write`, `Edit`, `Read`, and `Bash` (`hooks/hooks.json:14-50`). `Stop` invokes `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` with a long timeout (`hooks/hooks.json:52-59`). This means RLCR behavior is event driven: prompt submission checks loop invariants, tool calls validate file/command access, and stop attempts become the Codex review/finalize gate.
- inputs_outputs_state: Inputs are Claude hook event payloads passed by the host to the configured command hooks, plus environment variables such as `CLAUDE_PLUGIN_ROOT` for script resolution and `CLAUDE_PROJECT_DIR` consumed by the target scripts. Outputs are produced by the target validators, not by this JSON file. The stop hook emits JSON block/allow decisions and mutates `.humanize/rlcr/...` state; for example it finds the active loop from `.humanize/rlcr` and uses `state.md` or `finalized-state.md` as phase indicators in `hooks/loop-codex-stop-hook.sh:45-72`.
- gates_or_invariants: The registry enforces that bypass surfaces are covered before or at the relevant event: plan/branch integrity on user prompt submit; write/edit/read/bash path control before tool use; final Codex review on stop. The stop hook timeout is `7200` seconds (`hooks/hooks.json:57-58`), making long Codex review an explicit allowed gate rather than a default hook timeout. The registry assumes every referenced script exists and is executable via `CLAUDE_PLUGIN_ROOT`.
- dependencies_and_callers: Called by Claude Code's hook system, as documented by the lifecycle layout in `README.md:220-230`. Depends on five scripts: `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, and the stop hook `hooks/loop-codex-stop-hook.sh`. The validators share `hooks/lib/loop-common.sh`, which defines marker constants including `COMPLETE` and `STOP` (`hooks/lib/loop-common.sh:26-29`) and discovers active loop directories (`hooks/lib/loop-common.sh:58-81`).
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is unset or wrong, all configured command paths resolve incorrectly. If a new tool can mutate files but is not listed under `PreToolUse`, it may bypass the current validator set. If the Stop hook exceeds `7200` seconds, the host may terminate it. If `hooks.json` is installed without referenced template files, validators still generally have inline fallbacks, but the user-facing block messages can degrade.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts that load templates and fails missing referenced templates, explicitly to avoid "empty error messages" (`tests/test-template-references.sh:3-10`, `tests/test-template-references.sh:56-69`). `tests/test-finalize-phase.sh` verifies that a Codex `COMPLETE` result causes `state.md -> finalized-state.md` and blocks with a finalize prompt (`tests/test-finalize-phase.sh:460-470`). README’s flow diagram describes the high-level loop: Claude implements and summarizes, Codex reviews, and `COMPLETE` or max iterations ends the loop (`README.md:31-39`).
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-045 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-gate prompt template for the "git working tree is not clean" invariant. It is rendered when an active RLCR loop reaches a stop attempt but the repository has staged, unstaged, or untracked changes. This template is directly part of the completion gate because the stop hook blocks exit before Codex review when work has not been committed.
- algorithmic_behavior: The template tells the agent it is "trying to stop" with `{{GIT_ISSUES}}` (`prompt-template/block/git-not-clean.md:3`), injects `{{SPECIAL_NOTES}}` for special untracked-file cases (`prompt-template/block/git-not-clean.md:4`), then gives required actions: run the code-simplifier if installed, inspect untracked files, stage real changes, and commit (`prompt-template/block/git-not-clean.md:5-9`). It also enforces commit authorship rules forbidding AI attribution (`prompt-template/block/git-not-clean.md:11-14`) and only permits another exit attempt after committing (`prompt-template/block/git-not-clean.md:16`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`. The stop hook sets `GIT_ISSUES="uncommitted changes"` when cached git porcelain status is non-empty (`hooks/loop-codex-stop-hook.sh:434-437`). It derives `SPECIAL_NOTES` from untracked `.humanize*` directories and other untracked files (`hooks/loop-codex-stop-hook.sh:438-458`). The rendered output becomes the JSON `reason` in a hook response with `"decision": "block"` and system message `"Loop: Blocked - ... please commit first"` (`hooks/loop-codex-stop-hook.sh:470-481`). State does not advance; the loop remains active and the agent must commit before trying to stop again.
- gates_or_invariants: The invariant is "no uncommitted changes before expensive Codex review and before exit." The check runs only inside a Git repo (`hooks/loop-codex-stop-hook.sh:430`) and blocks before unpushed-commit checking and before Codex prompt generation. The template adds a branch-specific new requirement from this branch: line `prompt-template/block/git-not-clean.md:6` asks the agent to invoke the `code-simplifier` plugin before committing, aligning with the branch goal to add a final simplifier step after Codex completion, but at this earlier dirty-tree gate it is conditional on plugin availability.
- dependencies_and_callers: Rendered by `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` in `hooks/loop-codex-stop-hook.sh:470-472`. Rendering behavior comes from `hooks/lib/template-loader.sh`, which supports `{{VARIABLE_NAME}}` placeholders, leaves missing variables intact, and performs single-pass substitution to prevent placeholder injection (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-57`). Sibling templates can contribute special notes: `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md` are loaded at `hooks/loop-codex-stop-hook.sh:442-457`.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook uses an inline fallback that lacks the code-simplifier instruction (`hooks/loop-codex-stop-hook.sh:464-472`), so this branch’s simplifier guidance is weakened on template lookup failure. If `SPECIAL_NOTES` concatenates multiple templates without explicit separators, readability depends on those sibling templates including their own spacing. Because missing template variables are preserved by the renderer, a caller bug can expose raw `{{GIT_ISSUES}}` or `{{SPECIAL_NOTES}}` text rather than failing closed.
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`, which scans template-loading calls in `loop-codex-stop-hook.sh` and validators (`tests/test-template-references.sh:56-64`) and checks each referenced template path exists (`tests/test-template-references.sh:93-106`). Git-clean behavior is indirectly covered by stop-hook tests around blocking conditions in `tests/test-plan-file-hooks.sh`, while the exact code-simplifier sentence in this template did not appear to have a dedicated assertion in the inspected test snippets.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-075 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular Codex review prompt template for non-full-alignment RLCR rounds. It defines the review contract Codex must apply before allowing the loop to advance toward completion. It is a core transition template because its final-line `COMPLETE` instruction is consumed by the stop hook as the success marker that enters Finalize Phase.
- algorithmic_behavior: The template opens with `# Code Review - Round {{CURRENT_ROUND}}` (`prompt-template/codex/regular-review.md:1`), requires Codex to read the original implementation plan at `@{{PLAN_FILE}}` before reviewing (`prompt-template/codex/regular-review.md:5-9`), and asks it to compare Claude’s claimed completion against both the plan and `@{{PROMPT_FILE}}` (`prompt-template/codex/regular-review.md:11`). It injects Claude’s summary between explicit markers (`prompt-template/codex/regular-review.md:14-17`). Part 1 directs a skeptical implementation review, including finding plan-design gaps and forcing completion of deferred work (`prompt-template/codex/regular-review.md:20-31`). Part 2 mandates goal alignment checks against `@{{GOAL_TRACKER_FILE}}` (`prompt-template/codex/regular-review.md:33-45`). Part 3 inserts `{{GOAL_TRACKER_UPDATE_SECTION}}` (`prompt-template/codex/regular-review.md:47`). Part 4 defines output behavior: write issues to `@{{REVIEW_RESULT_FILE}}`, and only output `COMPLETE` as the last line if all original work and acceptance criteria are done with no deferrals or pending work (`prompt-template/codex/regular-review.md:49-57`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, passed by the stop hook at `hooks/loop-codex-stop-hook.sh:736-748`. The rendered template is written to `round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:671-673`, `hooks/loop-codex-stop-hook.sh:736-748`). Codex is then run with this prompt via `codex exec`, and its expected output is `round-N-review-result.md` (`hooks/loop-codex-stop-hook.sh:671-673`, `hooks/loop-codex-stop-hook.sh:770-789`). If Codex’s last non-empty trimmed line is exactly `COMPLETE`, the stop hook renames `state.md` to `finalized-state.md` and blocks with a finalize prompt (`hooks/loop-codex-stop-hook.sh:955-981`, `hooks/loop-codex-stop-hook.sh:983-1020`).
- gates_or_invariants: The review contract rejects completion if any original plan task is deferred, unfinished, or if any acceptance criteria are unmet (`prompt-template/codex/regular-review.md:53-56`). The executable gate is strict final-line matching: the stop hook checks only the last non-empty line and requires it to equal the `COMPLETE` marker, avoiding false positives such as "CANNOT COMPLETE" (`hooks/loop-codex-stop-hook.sh:955-962`). Every fifth round uses `full-alignment-review.md` instead; regular review is selected only when `CURRENT_ROUND % 5 != 4` (`hooks/loop-codex-stop-hook.sh:683-687`, `hooks/loop-codex-stop-hook.sh:718-748`). If `COMPLETE` happens at or beyond max iterations, Finalize Phase is skipped and the loop terminates as max-iteration instead (`hooks/loop-codex-stop-hook.sh:961-969`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` after summary existence, goal-tracker initialization, max-iteration, and finalize-phase checks have passed. It depends on `hooks/lib/template-loader.sh` single-pass placeholder rendering (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-131`) and on loop constants from `hooks/lib/loop-common.sh`, especially `MARKER_COMPLETE="COMPLETE"` (`hooks/lib/loop-common.sh:26-28`). It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, loaded into `GOAL_TRACKER_UPDATE_SECTION` at `hooks/loop-codex-stop-hook.sh:677-681`, and with `prompt-template/claude/finalize-phase-prompt.md`, which is loaded after successful `COMPLETE` at `hooks/loop-codex-stop-hook.sh:1005-1008`.
- edge_cases_or_failure_modes: If this template is missing or empty, `load_and_render_safe` falls back to a much shorter regular review prompt (`hooks/loop-codex-stop-hook.sh:707-716`), losing the detailed plan/goal-alignment/deferred-work contract. If Codex writes output to stdout instead of the review result path, the stop hook copies stdout to `REVIEW_RESULT_FILE` when possible (`hooks/loop-codex-stop-hook.sh:892-905`). Missing, empty, or unwritable review result files cause JSON block failures with debug paths (`hooks/loop-codex-stop-hook.sh:920-950`). Placeholder injection from summary content is mitigated by single-pass rendering; any `{{...}}` inside `SUMMARY_CONTENT` is not recursively expanded (`hooks/lib/template-loader.sh:54-57`). A review that says all work is done but leaves commentary after `COMPLETE` will not pass unless `COMPLETE` remains the last non-empty line.
- validation_or_tests: `tests/test-template-references.sh` validates that `codex/regular-review.md` exists when referenced by shell scripts (`tests/test-template-references.sh:93-106`). `tests/test-finalize-phase.sh` verifies the downstream transition from a mocked `COMPLETE` review into Finalize Phase and checks that the resulting block prompt mentions simplification (`tests/test-finalize-phase.sh:460-470`). The README documents the review loop as Claude implementation, Codex critique, and termination on `COMPLETE` or max iterations (`README.md:31-39`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`