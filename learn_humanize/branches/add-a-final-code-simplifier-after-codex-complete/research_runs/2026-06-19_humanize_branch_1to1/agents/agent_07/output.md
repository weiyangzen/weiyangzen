# agent_07 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-007 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing behavioral contract for the RLCR state machine. It defines the loop concept, command interface, expected artifacts, monitoring flow, cancellation model, and exit-state vocabulary. It is not executable, but it describes behavior implemented by `commands/start-rlcr-loop.md`, `scripts/setup-rlcr-loop.sh`, `hooks/hooks.json`, and `hooks/loop-codex-stop-hook.sh`.
- algorithmic_behavior: The documented flow is plan-driven iteration: user supplies a plan, Claude implements and writes a summary, Codex reviews, feedback loops back to Claude, and the loop ends on completion or max iterations (`README.md:31-39`). The plan requirements are explicit: clear task description, acceptance criteria, optional approach, and at least 5 lines (`README.md:41-55`). The command starts via `/humanize:start-rlcr-loop <plan.md>` with controls for max iterations, Codex model/effort, timeout, and push behavior (`README.md:57-70`, `README.md:188-201`). Goal tracking is part of the algorithm, with immutable goal/AC sections and mutable progress/deferred/task sections (`README.md:131-144`).
- inputs_outputs_state: Inputs are a markdown plan file, optional `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round` arguments (`README.md:59-70`, `README.md:190-200`). Outputs are loop artifacts under `.humanize/rlcr/<timestamp>/`, including `state.md`, `plan.md`, `goal-tracker.md`, round prompts, round summaries, review prompts, and review results (`README.md:242-254`). State is presence-based: `state.md` means active, and ending renames it to one of `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md` (`README.md:112-129`, `README.md:255-260`). Debug/cache output is outside the project under `$HOME/.cache/humanize/...` (`README.md:262-266`).
- gates_or_invariants: The README defines interruptibility as a state-file invariant: resume works by restarting Claude while `state.md` exists, cancel works by renaming `state.md` to `cancel-state.md` (`README.md:114-127`). It defines the review gate as Codex feedback/confirmation and the circuit breaker gate as `STOP` on stagnation (`README.md:145-152`). It also describes complete audit trail and goal drift prevention as design constraints (`README.md:23-27`, `README.md:268-274`). Cross-check: actual hook registration wires `Stop` to `hooks/loop-codex-stop-hook.sh` with a 7200-second timeout (`hooks/hooks.json:52-59`), and the start command delegates to `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:1-14`).
- dependencies_and_callers: README command docs map to the slash command metadata and setup script: `commands/start-rlcr-loop.md` allows only the setup script and documents the same flow (`commands/start-rlcr-loop.md:1-23`). Setup enforces the plan path, git repo, plan-file tracking, content, Codex availability, YAML-safe model/effort, and state-file creation (`scripts/setup-rlcr-loop.sh:104-180`, `scripts/setup-rlcr-loop.sh:211-423`, `scripts/setup-rlcr-loop.sh:485-498`). Hook orchestration is installed through `hooks/hooks.json`, which registers validators for prompt, write, edit, read, bash, and stop events (`hooks/hooks.json:4-63`).
- edge_cases_or_failure_modes: The README’s completion wording is partially stale for this branch: it says Codex `COMPLETE` or max iterations goes to “Done” (`README.md:33-39`) and says `complete-state.md` is created when Codex confirms all goals (`README.md:255-257`), but the branch now inserts a Finalize Phase after `COMPLETE` unless the max-iteration edge case is hit. The implemented hook renames `state.md` to `finalized-state.md`, blocks with a simplification prompt, then later renames to `complete-state.md` after finalize checks pass (`hooks/loop-codex-stop-hook.sh:961-1020`, `hooks/loop-codex-stop-hook.sh:646-656`). Other documented edge cases: cancellation preserves loop directory (`README.md:129`), max iteration emits `maxiter-state.md`, STOP emits `stop-state.md`, and schema/integrity failures emit `unexpected-state.md` (`README.md:255-260`).
- validation_or_tests: README behavior is covered indirectly by integration-style tests and hook tests. `tests/run-all-tests.sh` includes state naming, template, validator, finalize, and cancel-signal suites (`tests/run-all-tests.sh:32-47`). `tests/test-finalize-phase.sh` specifically verifies the branch’s newer finalize semantics: `COMPLETE` enters Finalize Phase, max iterations skip it, finalize requires git clean/summary/todos, and finalize does not invoke Codex (`tests/test-finalize-phase.sh:5-20`, `tests/test-finalize-phase.sh:354-493`). I did not execute tests because this assignment requested read-only research notes only.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-037 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template loading/rendering subsystem. This matters to core RLCR behavior because templates carry hook block messages, Codex review prompts, Claude next-round prompts, and the new finalize-phase prompt. If template discovery, syntax, or substitution fails, state-machine transitions can block with missing or corrupted instructions.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh`, resolves `TEMPLATE_DIR`, and runs ordered sections that validate directory shape, load every markdown template, scan placeholder syntax, test malformed placeholder detection, render edge cases, verify fallback behavior, render real templates, and stress all templates with dummy values (`tests/test-templates-comprehensive.sh:15-18`, `tests/test-templates-comprehensive.sh:59-80`, `tests/test-templates-comprehensive.sh:85-112`, `tests/test-templates-comprehensive.sh:116-194`, `tests/test-templates-comprehensive.sh:261-439`, `tests/test-templates-comprehensive.sh:440-490`, `tests/test-templates-comprehensive.sh:492-606`). The script tracks `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` and exits nonzero if any failure exists (`tests/test-templates-comprehensive.sh:26-29`, `tests/test-templates-comprehensive.sh:611-625`).
- inputs_outputs_state: Inputs are all `*.md` files under `prompt-template/`, plus the functions `get_template_dir`, `load_template`, `render_template`, `load_and_render_safe`, and `validate_template_dir` from `hooks/lib/template-loader.sh` (`tests/test-templates-comprehensive.sh:17`, `tests/test-templates-comprehensive.sh:90-107`). Temporary state is created with `mktemp -d` for malformed-placeholder and incomplete-directory tests, then removed (`tests/test-templates-comprehensive.sh:204-210`, `tests/test-templates-comprehensive.sh:258-259`, `tests/test-templates-comprehensive.sh:482-490`). Outputs are colored PASS/FAIL/WARN lines and exit code 0 or 1 (`tests/test-templates-comprehensive.sh:31-57`, `tests/test-templates-comprehensive.sh:619-625`).
- gates_or_invariants: Template directory must exist and contain `block/`, `codex/`, and `claude/` (`tests/test-templates-comprehensive.sh:66-80`). Placeholders must use `{{UPPER_CASE}}`-style syntax; checks flag extra closing braces, extra opening braces, wrong brackets, likely unclosed placeholders, spaces inside placeholders, and lowercase placeholders (`tests/test-templates-comprehensive.sh:122-175`). Rendering invariants include preserving empty lines, replacing repeated/adjacent variables, tolerating special regex characters, quotes, backslashes, CJK-like text, markdown formatting, long values, and multiline values without crashing (`tests/test-templates-comprehensive.sh:266-439`). Fallback invariant: `load_and_render_safe` must substitute fallback variables when a template is missing and must prefer a real template when present (`tests/test-templates-comprehensive.sh:445-463`).
- dependencies_and_callers: The tested implementation is `hooks/lib/template-loader.sh`. Its documented semantics are single-pass placeholder substitution, missing variables remain as-is, and variable values containing `{{OTHER_VAR}}` are not expanded (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:50-58`). The loader computes template root relative to `hooks/lib`, loads files by path, and validates required subdirectories (`hooks/lib/template-loader.sh:24-48`, `hooks/lib/template-loader.sh:205-222`). Template users include `hooks/loop-codex-stop-hook.sh` for block messages, review prompts, next-round prompts, and finalize prompts (`hooks/loop-codex-stop-hook.sh:536-548`, `hooks/loop-codex-stop-hook.sh:678-748`, `hooks/loop-codex-stop-hook.sh:1005-1019`, `hooks/loop-codex-stop-hook.sh:1075-1092`).
- edge_cases_or_failure_modes: The syntax scanner is regex-based and intentionally skips noisy single-brace checks (`tests/test-templates-comprehensive.sh:160-164`), so it is a guardrail rather than a parser. The “unclosed” check can have false positives and applies a secondary var-name check (`tests/test-templates-comprehensive.sh:146-158`). The CJK section currently uses ASCII replacement text for its first assertion and then a simple “CJK:” presence check, so it mostly validates non-crash behavior rather than exact Unicode preservation (`tests/test-templates-comprehensive.sh:348-360`). Backslash, quotes, markdown, and multiline tests also often assert “no crash” or prefix presence instead of exact equality (`tests/test-templates-comprehensive.sh:328-345`, `tests/test-templates-comprehensive.sh:373-402`). All-template rendering warns on unreplaced placeholders but does not fail, which means optional or intentionally missing variables do not break CI (`tests/test-templates-comprehensive.sh:590-597`).
- validation_or_tests: This file is itself a test suite and is included in the aggregate runner (`tests/run-all-tests.sh:32-47`). It directly validates real templates including `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:497-540`). It does not directly render `claude/finalize-phase-prompt.md` by name, but the “all templates can be loaded and rendered” stress loop covers it generically by discovering every `*.md` template and substituting dummy values for all detected placeholders (`tests/test-templates-comprehensive.sh:562-606`). I did not execute it due to the research-only/read-only instruction.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-067 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing prompt template for the new Finalize Phase inserted after Codex declares RLCR implementation complete. It defines the final simplification contract, the required `code-simplifier:code-simplifier` delegation, allowed scope of refactoring, and completion artifacts before the loop can move from `finalized-state.md` to `complete-state.md`.
- algorithmic_behavior: The template states that Codex review has passed and all acceptance criteria are met, then directs Claude into a Finalize Phase for simplification/refactoring (`prompt-template/claude/finalize-phase-prompt.md:1-6`). Its required action is to use the Task tool with `subagent_type="code-simplifier:code-simplifier"` (`prompt-template/claude/finalize-phase-prompt.md:7-14`). It constrains the simplifier to code changed during the RLCR session and to cleanup that preserves behavior (`prompt-template/claude/finalize-phase-prompt.md:25-33`). Before exiting, Claude must complete todos, commit changes, and write `{{FINALIZE_SUMMARY_FILE}}` with simplification details, modified files, test confirmation, and refactoring notes (`prompt-template/claude/finalize-phase-prompt.md:40-50`).
- inputs_outputs_state: Template inputs are `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:35-44`). The stop hook supplies these values when rendering after `COMPLETE`: `FINALIZE_SUMMARY_FILE="$LOOP_DIR/finalize-summary.md"`, plus `PLAN_FILE` and `GOAL_TRACKER_FILE` (`hooks/loop-codex-stop-hook.sh:983-1008`). Output is a JSON block decision whose `reason` is the rendered finalize prompt and whose system message is “Loop: Finalize Phase - Simplify and refactor code before completion” (`hooks/loop-codex-stop-hook.sh:1010-1019`).
- gates_or_invariants: The template’s non-negotiable invariants are no functionality changes, no existing test failures, no new bugs, and only functionality-equivalent cleanup (`prompt-template/claude/finalize-phase-prompt.md:16-24`). The surrounding state-machine gate is strict: only a last non-empty Codex review line exactly equal to `COMPLETE` enters Finalize Phase (`hooks/loop-codex-stop-hook.sh:955-963`). At max iterations, `COMPLETE` skips Finalize Phase and ends as `maxiter-state.md` instead (`hooks/loop-codex-stop-hook.sh:963-969`). During Finalize Phase, the stop hook expects `finalize-summary.md` instead of `round-N-summary.md` (`hooks/loop-codex-stop-hook.sh:522-527`), skips Codex review after all checks pass, and renames `finalized-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:646-656`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, via `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-prompt.md"` (`hooks/loop-codex-stop-hook.sh:1005-1008`). Rendering semantics come from `hooks/lib/template-loader.sh`, especially single-pass `{{VAR}}` substitution and fallback behavior (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-203`). Active-loop detection and validator behavior come from `hooks/lib/loop-common.sh`, which treats both `state.md` and `finalized-state.md` as active loop indicators (`hooks/lib/loop-common.sh:58-81`). Write/edit/bash validators protect `finalized-state.md` while allowing the active loop’s `finalize-summary.md` write path (`hooks/loop-write-validator.sh:88-125`, `hooks/loop-edit-validator.sh:71-93`, `hooks/loop-bash-validator.sh:45-89`).
- edge_cases_or_failure_modes: If this template is missing or renders empty, the hook uses an inline fallback prompt with the same core constraints and summary path (`hooks/loop-codex-stop-hook.sh:985-1005`). If `finalize-summary.md` is missing, exit is blocked before completion (`hooks/loop-codex-stop-hook.sh:522-554`). If todos are incomplete, the shared todo checker blocks before Codex/finalize completion (`hooks/loop-codex-stop-hook.sh:252-307`). If git is dirty, the normal git-clean gate still blocks in Finalize Phase (`hooks/loop-codex-stop-hook.sh:423-520`). One implementation nuance: the prompt says “Commit your changes,” but `finalize-summary.md` lives under `.humanize/rlcr/...`, which is typically local loop state; the hook only checks that it exists, not that the summary itself is committed.
- validation_or_tests: `tests/test-finalize-phase.sh` is the direct executable spec for this prompt’s state transition. It verifies `COMPLETE` triggers state rename to `finalized-state.md` and returns a block prompt mentioning simplification (`tests/test-finalize-phase.sh:460-476`), max iterations skip Finalize (`tests/test-finalize-phase.sh:478-496`), finalize requires summary/git clean/todos (`tests/test-finalize-phase.sh:365-407`, `tests/test-finalize-phase.sh:499-541`), finalize completion renames to `complete-state.md` without invoking Codex (`tests/test-finalize-phase.sh:412-440`), and validators parse `finalized-state.md` (`tests/test-finalize-phase.sh:617-665`). Generic template loading/rendering coverage also includes this file through the all-template discovery loop in `tests/test-templates-comprehensive.sh` (`tests/test-templates-comprehensive.sh:562-606`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3, exactly the three Item Evidence headings above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`