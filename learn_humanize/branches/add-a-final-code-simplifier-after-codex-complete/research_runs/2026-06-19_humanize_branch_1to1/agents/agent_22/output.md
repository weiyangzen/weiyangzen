# agent_22 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell utility exposing the user-facing `humanize monitor rlcr-loop` command. Its algorithmic responsibility is read-only RLCR loop observability: find the newest loop session, find the newest Codex stderr log in the external cache, parse session state and goal tracker summaries, parse git status, and render a fixed terminal dashboard while tailing live log output. It is not the scheduler, stop hook, or completion gate itself. Entrypoint routing is in `scripts/humanize.sh:681`.
- algorithmic_behavior: `_humanize_monitor_codex` starts by requiring `.humanize/rlcr` in the current project (`scripts/humanize.sh:10`, `scripts/humanize.sh:16`). `_find_latest_session` chooses the lexicographically latest timestamp directory matching `YYYY-MM-DD_HH-MM-SS` (`scripts/humanize.sh:23`). `_find_latest_codex_log` reconstructs the cache path as `$HOME/.cache/humanize/<sanitized-project-path>/<session>/round-*-codex-run.log`, then selects highest session and round (`scripts/humanize.sh:43`). `_find_state_file` maps active sessions to `state.md|active`, otherwise first `*-state.md|<reason>`, otherwise unknown (`scripts/humanize.sh:98`). `_parse_state_md`, `_parse_goal_tracker`, and `_parse_git_status` turn files/commands into pipe-delimited summary records (`scripts/humanize.sh:138`, `scripts/humanize.sh:156`, `scripts/humanize.sh:240`). `_draw_status_bar` formats those records into an 11-line fixed header (`scripts/humanize.sh:302`). The main monitor loop redraws status, handles no-log waiting, tails the last 50 lines, emits incremental bytes with `tail -c`, and switches when newer sessions or round logs appear (`scripts/humanize.sh:519`).
- inputs_outputs_state: Inputs are current working directory, `.humanize/rlcr/<timestamp>/` session folders, session files such as `state.md`, `*-state.md`, and `goal-tracker.md`, the project git worktree, terminal capabilities from `tput`, and cached Codex logs under `$HOME/.cache/humanize` (`README.md:244`, `README.md:262`). Output is terminal UI/log text only; the script does not write loop state or project files. Runtime state is held in locals: `current_session_dir`, `current_file`, `current_loop_status`, `last_size`, `last_no_log_status`, `tail_pid`, and cleanup flags (`scripts/humanize.sh:460`, `scripts/humanize.sh:514`). State transitions are monitor-local: no session to error, active session to no-log wait, no-log to tailing when a log appears, current log to newer log/round, current session to newer timestamp session, and signal cleanup to restored terminal (`scripts/humanize.sh:489`, `scripts/humanize.sh:534`, `scripts/humanize.sh:607`, `scripts/humanize.sh:637`, `scripts/humanize.sh:465`).
- gates_or_invariants: Requires `.humanize/rlcr` to exist and at least one valid timestamp session before monitoring (`scripts/humanize.sh:16`, `scripts/humanize.sh:493`). Treats `state.md` presence as active and `*-state.md` as terminal status (`scripts/humanize.sh:110`). Session names must match the timestamp regex (`scripts/humanize.sh:34`, `scripts/humanize.sh:65`). Log discovery is limited to cache sessions that also exist locally, preventing unrelated cache logs from being shown (`scripts/humanize.sh:57`). Terminal scroll region is reserved below the status bar and restored on INT/TERM (`scripts/humanize.sh:442`, `scripts/humanize.sh:452`, `scripts/humanize.sh:486`). Command routing only accepts `humanize monitor rlcr-loop`; other commands print usage and return failure (`scripts/humanize.sh:686`).
- dependencies_and_callers: Intended to be sourced from shell rc files; README shows discovering the plugin root and `source "$HUMANIZE_PLUGIN_ROOT/scripts/humanize.sh"`, then running `humanize monitor rlcr-loop` (`README.md:90`, `README.md:103`). Depends on standard shell tools: `basename`, `sed`, `grep`, `sort`, `wc`, `git`, `stat`, `tail`, `tput`, `seq`, `kill`, `wait`, and terminal ANSI support. Its cache naming intentionally matches `hooks/loop-codex-stop-hook.sh`, which writes `round-N-codex-run.log` under the same sanitized project cache path (`hooks/loop-codex-stop-hook.sh:779`, `hooks/loop-codex-stop-hook.sh:789`). It also depends on loop artifacts created by `scripts/setup-rlcr-loop.sh` and documented in README (`README.md:247`).
- edge_cases_or_failure_modes: Missing `.humanize/rlcr` or no timestamp sessions returns a user-facing error (`scripts/humanize.sh:17`, `scripts/humanize.sh:494`). Missing log files are handled by a polling wait and status-only display (`scripts/humanize.sh:534`). Missing state or goal tracker degrades to `unknown` or zero/N/A summaries (`scripts/humanize.sh:103`, `scripts/humanize.sh:139`, `scripts/humanize.sh:158`). Git outside a repository returns zero counts and a sentinel message (`scripts/humanize.sh:243`). `stat` is cross-platform via GNU `-c%s`, BSD `-f%z`, then zero fallback (`scripts/humanize.sh:509`). Potential fragility: `_find_state_file` picks the first `*-state.md` by glob order rather than newest or priority (`scripts/humanize.sh:120`); `goal_summary` is pipe-delimited and can be split incorrectly if source text contains `|` (`scripts/humanize.sh:237`, `scripts/humanize.sh:288`); zsh compatibility relies on `setopt localoptions ksharrays` inside drawing but other nested helpers use bash-style locals and regex conditionals.
- validation_or_tests: No direct automated test references for `scripts/humanize.sh` were found by repository search. Validation is mostly implicit via README usage documentation and compatibility comments. Related producer behavior is covered elsewhere: cache/state names are produced by setup and stop hooks, while the monitor consumes them. Manual validation path is to source the script and run `humanize monitor rlcr-loop` from a project with `.humanize/rlcr` and cache logs (`README.md:90`, `README.md:103`).
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-052 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Prompt block for the stop-hook large-file gate. It defines the human/agent remediation contract when a session tries to stop while staged/tracked changed files exceed the configured line threshold. It is core to validation behavior because the hook renders it into the block reason returned to Claude/Codex tooling (`prompt-template/block/large-files.md:1`).
- algorithmic_behavior: The template is passive markdown with two placeholders: `{{MAX_LINES}}` and `{{LARGE_FILES}}` (`prompt-template/block/large-files.md:3`). Runtime behavior is implemented in `hooks/loop-codex-stop-hook.sh`: it sets `MAX_LINES=2000`, scans `GIT_STATUS_CACHED`, extracts filenames, normalizes rename paths, skips deleted/nonexistent files, classifies extensions into code or documentation, counts lines with `wc -l`, appends files above threshold to `LARGE_FILES`, then renders this template with `load_and_render_safe` and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:346`, `hooks/loop-codex-stop-hook.sh:351`, `hooks/loop-codex-stop-hook.sh:393`, `hooks/loop-codex-stop-hook.sh:407`, `hooks/loop-codex-stop-hook.sh:411`). The template’s instructions distinguish code splitting from documentation splitting and add a branch-specific simplification handoff: use `code-simplifier` if installed (`prompt-template/block/large-files.md:13`, `prompt-template/block/large-files.md:19`).
- inputs_outputs_state: Inputs are rendered variables from the stop hook: numeric line cap and markdown bullet list of offending files with line counts and file type. Output is the `reason` string embedded in the hook’s JSON response; `systemMessage` separately says the loop is blocked because large files were detected (`hooks/loop-codex-stop-hook.sh:411`). The template itself stores no state. The resulting state transition is external: stop attempt proceeds to blocked decision rather than allowing loop completion until files are split and committed (`prompt-template/block/large-files.md:25`).
- gates_or_invariants: Hard gate triggers only in a git repository and only from cached git status input (`hooks/loop-codex-stop-hook.sh:348`, `hooks/loop-codex-stop-hook.sh:397`). Only selected extensions are checked: common code extensions plus markdown/text documentation (`hooks/loop-codex-stop-hook.sh:375`). Files must be strictly greater than the limit, not equal (`hooks/loop-codex-stop-hook.sh:393`). Required remediation invariant is each split file below `{{MAX_LINES}}` lines while preserving code behavior or documentation flow (`prompt-template/block/large-files.md:14`, `prompt-template/block/large-files.md:20`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` (`hooks/loop-codex-stop-hook.sh:407`). Rendering depends on `hooks/lib/template-loader.sh`, whose placeholder syntax is `{{VARIABLE_NAME}}`, single-pass, preserving missing placeholders (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:50`). Safe rendering falls back to an inline large-file message if the template is missing or renders empty (`hooks/lib/template-loader.sh:170`, `hooks/loop-codex-stop-hook.sh:400`). Related branch behavior is echoed by other prompt blocks and finalize prompts that reference `code-simplifier`, but this file is the large-file-specific contract.
- edge_cases_or_failure_modes: Untracked files are only caught if present in the cached git status used by the stop hook; deleted files are skipped (`hooks/loop-codex-stop-hook.sh:365`). Filenames with rename syntax are reduced to the destination path (`hooks/loop-codex-stop-hook.sh:360`). Unsupported large binary or uncommon source extensions are ignored (`hooks/loop-codex-stop-hook.sh:382`). `wc -l` failures or nonnumeric counts are skipped (`hooks/loop-codex-stop-hook.sh:388`). The template tells agents to commit and retry exit, so a dirty-but-unsplit state remains blocked by the hook rather than self-resolving (`prompt-template/block/large-files.md:25`).
- validation_or_tests: Template reference existence is covered by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls and verifies referenced files exist (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:93`). Repository search found no dedicated behavioral test that constructs an over-2000-line changed file and asserts this exact block response; coverage is reference/existence plus hook-level fallback mechanics.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`