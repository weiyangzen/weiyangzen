# agent_26 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-026 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR hook allowlist exception path. The test suite defines which loop-generated files may bypass otherwise strict hook blocking: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`, but only inside the current active loop directory. It protects the validator contract used by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`.
- algorithmic_behavior: The suite constructs a temporary git repository and active RLCR loop directory, writes a minimal `.humanize/rlcr/<timestamp>/state.md`, then exercises allow/deny decisions through both the shared predicate and hook entrypoints. `setup_test_loop` initializes git state and writes frontmatter with `current_round: 5`, `max_iterations: 42`, `plan_file`, `plan_tracked`, and `start_branch` at `tests/test-allowlist-validators.sh:33-61`. Direct predicate tests verify `is_allowlisted_file "$file" "$ACTIVE_LOOP_DIR"` allows only the four exact files at `tests/test-allowlist-validators.sh:70-124`. Hook tests then feed JSON hook payloads on stdin and assert exit code `0` for allowlisted cases and exit code `2` plus relevant block text for denied cases.
- inputs_outputs_state: Inputs are synthetic Claude hook JSON objects such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}` and Bash command payloads such as `echo test > <loop>/round-1-todos.md`. The test sets `CLAUDE_PROJECT_DIR="$TEST_DIR"` so hooks resolve the active loop under the temp repository at `tests/test-allowlist-validators.sh:130-131`. Observable outputs are shell exit codes, stdout/stderr text captured in `RESULT`, and aggregate counters `TESTS_PASSED` / `TESTS_FAILED` at `tests/test-allowlist-validators.sh:23-27`. The final process exit is the failed-test count at `tests/test-allowlist-validators.sh:383-391`.
- gates_or_invariants: The key invariant is exact path equality, not filename-only matching. The shared implementation hardcodes the allowlist and compares `"$file_path" == "$active_loop_dir/$allowed"` in `hooks/lib/loop-common.sh:170-187`. The test explicitly guards wrong-directory bypasses at `tests/test-allowlist-validators.sh:118-124`, generic relative Bash writes at `tests/test-allowlist-validators.sh:340-351`, old loop directories at `tests/test-allowlist-validators.sh:353-366`, and same-basename different roots at `tests/test-allowlist-validators.sh:368-381`. For Bash, the invariant is narrower: only full active-loop paths to `round-1-todos.md` or `round-2-todos.md` may pass, enforced by `hooks/loop-bash-validator.sh:321-329`.
- dependencies_and_callers: The suite sources `hooks/lib/loop-common.sh` at `tests/test-allowlist-validators.sh:15-17`, which in turn loads the template loader and defines `find_active_loop`, `parse_state_file`, `is_round_file_type`, `extract_round_number`, `is_allowlisted_file`, message renderers, and command/path helpers. It invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` directly. The suite is included in the aggregate runner’s ordered test list at `tests/run-all-tests.sh:32-47`. Hook dependencies include `jq`, `git`, `mktemp`, `sed`, `grep`, and Bash.
- edge_cases_or_failure_modes: The tests cover non-current but intentionally allowed historical files: `round-0-summary.md` and `round-1-summary.md` are accepted even when `current_round` is `5`, as shown at `tests/test-allowlist-validators.sh:146-157`, `tests/test-allowlist-validators.sh:202-213`, and `tests/test-allowlist-validators.sh:245-256`. They also cover disallowed future or non-allowlisted rounds: `round-3-todos.md`, `round-4-todos.md`, `round-2-summary.md`, and `round-3-summary.md`. A notable implementation edge is that direct `Write` / `Edit` / `Read` allowlisting permits `round-0-summary.md` and `round-1-summary.md` through `is_allowlisted_file`, while the Bash validator intentionally blocks all summary modifications via shell and only provides the todos-specific full-path exception at `hooks/loop-bash-validator.sh:311-329`. Another edge is the tests use exact, unquoted paths in Bash command strings; `command_modifies_file` handles many write forms, including redirection, `tee`, in-place edits, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf`, and `exec` at `hooks/lib/loop-common.sh:423-448`, but this suite specifically validates the allowlist boundary rather than every shell parsing variant.
- validation_or_tests: This file is itself the validation artifact. It contains 25 numbered assertions: 7 for `is_allowlisted_file`, 4 for `Write`, 3 for `Edit`, 4 for `Read`, and 7 for path-restricted Bash behavior. The expected success path is exit `0`; expected hook blocks use exit `2` and look for diagnostic words such as `todos` or `round`. I did not execute the test suite because this assignment requested read-only research notes and no file modification; inspection was direct with line-numbered reads.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-056 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: User-facing block template for an RLCR state schema compatibility gate. It is rendered when an active loop’s `state.md` or `finalized-state.md` is missing required fields introduced in humanize `1.1.2+`, preventing the loop from proceeding with ambiguous or stale state.
- algorithmic_behavior: The template declares the failure title, names the missing field via `{{FIELD_NAME}}`, explains that the loop was started with an older humanize version, lists remediation options, and states that the loop will be terminated as `unexpected` to preserve state information. The placeholder appears at `prompt-template/block/schema-outdated.md:3`; remediation guidance is at `prompt-template/block/schema-outdated.md:7-10`; the termination contract is at `prompt-template/block/schema-outdated.md:12`.
- inputs_outputs_state: The template input is a single render variable, `FIELD_NAME`. `hooks/loop-plan-file-validator.sh` builds this value from missing required fields and calls `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md" "$fallback" "FIELD_NAME=$field_name"` at `hooks/loop-plan-file-validator.sh:56-63`. The output is Markdown text embedded as the JSON `reason` for a UserPromptSubmit hook block at `hooks/loop-plan-file-validator.sh:64-73`. Template rendering is single-pass and preserves unknown placeholders according to `hooks/lib/template-loader.sh:50-132`, limiting placeholder injection from supplied values.
- gates_or_invariants: The corresponding schema gate requires `plan_tracked` and `start_branch` in parsed state frontmatter. `hooks/loop-plan-file-validator.sh:76-86` iterates over those fields and blocks when a field value is empty. The invariant is that newer plan-file and branch-consistency validation cannot safely run unless the state records whether the plan is tracked and which branch the loop started on. The template’s text also encodes a state-preservation invariant: outdated loops should terminate as `unexpected`, not silently continue or mutate state under an old schema.
- dependencies_and_callers: Primary caller is `hooks/loop-plan-file-validator.sh`, which sources `hooks/lib/loop-common.sh`, parses the active loop state, validates schema, then emits JSON. The template loader dependency is `hooks/lib/template-loader.sh`; it resolves template root via `get_template_dir`, loads `prompt-template/block/schema-outdated.md`, substitutes `{{FIELD_NAME}}`, and falls back if the template is missing. A related but not identical schema check exists in `hooks/loop-codex-stop-hook.sh:127-143`; that stop hook emits an inline block reason for missing `plan_tracked` or `start_branch` rather than rendering this template.
- edge_cases_or_failure_modes: If the template is absent or renders empty, `load_and_render_safe` falls back to an inline message in `hooks/loop-plan-file-validator.sh:58-63` and `hooks/lib/template-loader.sh:170-203`. If multiple fields are missing, the plan-file validator reports the first missing field encountered by the `REQUIRED_FIELDS` array at `hooks/loop-plan-file-validator.sh:76-86`; it does not aggregate all missing fields in one response. The rendered text says the loop will be terminated as `unexpected`, but the plan-file validator itself returns a JSON block and exits `0` at `hooks/loop-plan-file-validator.sh:68-84`; actual termination behavior is handled elsewhere, with the stop hook’s inline outdated-schema path also returning a block at `hooks/loop-codex-stop-hook.sh:131-142`.
- validation_or_tests: The template is indirectly covered by tests referenced in the repository search results: `tests/test-plan-file-hooks.sh` includes a case named “Stop hook returns JSON block for outdated schema” around `tests/test-plan-file-hooks.sh:631-650`, and template directory/rendering behavior is covered by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`. I inspected the direct template and its render call path; I did not run tests due the read-only research-only instruction.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item sections present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`