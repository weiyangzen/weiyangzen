# agent_18 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-018 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse gate for Claude `Edit` calls during the RLCR loop. It protects loop-owned files under `.humanize/rlcr` and enforces round-aware edit routing so Claude edits summaries through the current round surface, not internal state, prompts, plan backups, todos, or post-round-0 goal tracker state. The hook is registered for `Edit` in `hooks/hooks.json:24-30`.
- algorithmic_behavior: The script reads hook JSON from stdin, extracts `.tool_name`, and exits allowfully unless the tool is exactly `Edit` (`hooks/loop-edit-validator.sh:22-27`). For `Edit`, it extracts `.tool_input.file_path`, lowercases it via shared helper `to_lower`, then applies staged gates: todos-file gate before loop-dir filtering (`lines 29-43`), prompt-file write block (`lines 46-49`), skip for paths outside `.humanize/rlcr` (`lines 55-57`), active-loop and current-round discovery (`lines 63-79`), state/finalized-state blocks (`lines 86-94`), plan backup block (`lines 100-108`), post-round-0 goal tracker block (`lines 114-118`), and wrong-round summary validation (`lines 124-150`).
- inputs_outputs_state: Inputs are the hook payload on stdin, `CLAUDE_PROJECT_DIR`, optional `PROJECT_ROOT`, optional `LOOP_BASE_DIR`, optional `LOOP_DIR`, and files under `.humanize/rlcr`. It sources `hooks/lib/loop-common.sh` for path classifiers, state parsing, active-loop lookup, and templated block messages (`hooks/loop-edit-validator.sh:15-16`). Outputs are process exit status and optional stderr block text: `0` permits the edit; `2` rejects it with a rendered/fallback message. It does not mutate repository or loop state. It derives current state by selecting `finalized-state.md` over `state.md` when present (`lines 71-75`) and parsing frontmatter into `STATE_CURRENT_ROUND` (`lines 77-79`).
- gates_or_invariants: The validator preserves these invariants: non-Edit tools are ignored; prompt files matching `round-N-prompt.md` are read-only (`lines 46-49`); state files are hook/system-owned (`lines 86-94`); `plan.md` backups under `.humanize/rlcr` are read-only (`lines 100-108`); `goal-tracker.md` becomes Codex-owned after round 0 (`lines 114-118`); summary edits must target `round-${CURRENT_ROUND}-summary.md` unless the path is explicitly allowlisted (`lines 124-148`). Todos files are blocked unless they are in the active-loop allowlist (`lines 36-43`), with the allowlist defined in `hooks/lib/loop-common.sh:170-188` as exact active-loop paths for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`.
- dependencies_and_callers: Direct caller is Claude hook dispatch from `hooks/hooks.json`. Shared dependencies include `jq` for parsing hook JSON (`hooks/loop-edit-validator.sh:23,29`), bash, standard shell utilities, and `hooks/lib/loop-common.sh`. Key shared helpers are `find_active_loop` (`hooks/lib/loop-common.sh:63-81`), `parse_state_file` (`lines 112-139`), `is_round_file_type` (`lines 148-153`), `extract_round_number` (`lines 158-165`), `is_allowlisted_file` (`lines 170-188`), `is_in_humanize_loop_dir` (`lines 415-418`), `is_goal_tracker_path` (`lines 251-254`), and templated message functions for todos, prompts, state, finalized state, and goal tracker blocks (`lines 190-248`, `450-462`). It coordinates with sibling validators: `loop-write-validator.sh`, `loop-read-validator.sh`, and `loop-bash-validator.sh`; the Bash sibling handles command-pattern mutation detection for goal tracker and other protected files, including the template covered by HZ-048 (`hooks/loop-bash-validator.sh:285-293`).
- edge_cases_or_failure_modes: If `jq` is unavailable or hook JSON is malformed, `set -euo pipefail` makes the hook fail rather than silently allow. If the path is a todos file but no active loop exists, the script still blocks because the todos gate runs before loop-dir early return and treats empty active loop as non-allowlisted (`hooks/loop-edit-validator.sh:36-43`). If the edited file is under legacy `.humanize-loop.local`, it is outside the recognized `.humanize/rlcr/` path and is allowed, matching regression tests (`tests/test-plan-file-hooks.sh:1043-1054`). If no active loop is found for a path inside `.humanize/rlcr`, the validator exits allowfully after active-loop discovery (`hooks/loop-edit-validator.sh:67-69`). The finalized phase is parsed from `finalized-state.md` when present, and `finalized-state.md` is checked before generic `state.md` because the generic regex also matches it (`lines 71-75`, `82-88`). Wrong-round summary extraction depends on `.humanize/rlcr/<loop>/...` path shape; if extraction fails, that specific gate is skipped (`lines 124-131`).
- validation_or_tests: Covered by targeted shell tests rather than inline test code. `tests/test-allowlist-validators.sh:189-226` verifies edit allowlist behavior for allowed todos/summary files and blocks a non-allowlisted todos file. `tests/test-plan-file-hooks.sh:211-222` verifies edit blocking for loop `plan.md`, and `tests/test-plan-file-hooks.sh:1043-1054` verifies legacy `.humanize-loop.local` edit allowance. `tests/test-finalize-phase.sh:305-315` verifies edit blocking for `finalized-state.md`; `tests/test-finalize-phase.sh:620-650` verifies finalized-state parsing in sibling validators. `tests/test-template-references.sh:176-180` treats `loop-edit-validator.sh` as a critical validator that should use safe template rendering.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-048 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR Bash validator’s round-0 goal tracker mutation gate. It is not executable algorithm code, but it is part of the validation contract: when Bash attempts to modify `goal-tracker.md` during round 0, the system must redirect Claude to use the Write/Edit hook path so normal validators can inspect the change.
- algorithmic_behavior: The template renders a short denial message: Bash must not modify `goal-tracker.md`; use Write or Edit at `{{CORRECT_PATH}}`; Bash commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:1-8`). The template itself has one variable, `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh:241-248`.
- inputs_outputs_state: Input is the `CORRECT_PATH` template variable, normally set by `hooks/loop-bash-validator.sh` to `$ACTIVE_LOOP_DIR/goal-tracker.md` when `CURRENT_ROUND` is 0 (`hooks/loop-bash-validator.sh:285-288`). Output is stderr text returned through `goal_tracker_bash_blocked_message`; the Bash validator exits with status `2` after rendering (`hooks/loop-bash-validator.sh:285-293`). The template does not read or mutate state. It reflects the round state indirectly because only the round-0 branch uses this template; later rounds use `block/goal-tracker-modification.md` through `goal_tracker_blocked_message` (`hooks/loop-bash-validator.sh:289-292`, `hooks/lib/loop-common.sh:450-462`).
- gates_or_invariants: The invariant encoded by the template is tool-routing, not file ownership: during round 0, goal tracker updates are allowed only through Write/Edit, because Bash redirection and in-place mutation patterns bypass structured validation. This pairs with `command_modifies_file` pattern detection for redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec fd>` patterns in `hooks/lib/loop-common.sh:423-448`.
- dependencies_and_callers: Primary caller is `goal_tracker_bash_blocked_message` (`hooks/lib/loop-common.sh:241-248`), which loads `block/goal-tracker-bash-write.md` via `load_and_render_safe`. Runtime trigger is `hooks/loop-bash-validator.sh:285-293`. Template existence is checked as a common template reference in `tests/test-template-references.sh:152-159`. It coordinates with the HZ-018 edit validator because the template tells Claude to use Write/Edit; those tools are then guarded by `loop-write-validator.sh` and `loop-edit-validator.sh` for round-aware goal tracker rules.
- edge_cases_or_failure_modes: If the template is missing, the shared function has an inline fallback with the same `CORRECT_PATH` variable (`hooks/lib/loop-common.sh:241-248`), so blocking still works but message consistency regresses. If `CORRECT_PATH` is empty or stale because active-loop discovery failed upstream, the rendered guidance becomes less useful; the gate itself still exits blocked once the Bash validator reaches this branch. The text says Bash commands bypass validation hooks; that is specifically true for direct shell file writes, and the validator compensates by pattern matching known write forms. Detection is regex-based and has known limits documented in tests, for example multi-source `cp` is noted as not detected while common `cp src goal-tracker.md` is detected (`tests/test-bash-validator-patterns.sh:166-168` from search results).
- validation_or_tests: `tests/test-template-references.sh:152-159` checks this template exists as one of the common block templates. Bash mutation detection for `goal-tracker.md` is broadly covered by `tests/test-bash-validator-patterns.sh`, including redirection, `tee`, in-place editors, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec`, and read-only non-modifying commands, as shown by the matched test references. The runtime caller branch is visible in `hooks/loop-bash-validator.sh:285-293`.
- skip_candidate: `yes: template-only evidence surface rather than core algorithm implementation; still relevant because it defines the operator-facing validation contract for a core Bash mutation gate`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections; each assigned row represented once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`