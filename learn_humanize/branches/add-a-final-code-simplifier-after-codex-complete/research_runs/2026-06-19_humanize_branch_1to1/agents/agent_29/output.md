# agent_29 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-029 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR loop cancellation path. It verifies that an active loop can only be cancelled by the intended two-step protocol: create `.cancel-requested`, then rename the active loop’s `state.md` to `cancel-state.md`. The file is part of the aggregate suite in `tests/run-all-tests.sh:32-47`, where `test-cancel-signal-file.sh` is run as a named suite.
- algorithmic_behavior: The script builds a temporary loop root under `$TEST_DIR/.humanize/rlcr/2024-01-01_12-00-00`, writes a minimal active `state.md`, exports `CLAUDE_PROJECT_DIR`, and invokes the real Bash hook through JSON input assembled with `jq` (`tests/test-cancel-signal-file.sh:42-71`). Its positive cases confirm authorized cancellation for exact active-loop paths, normalized `/./` paths, quoted paths, and literal `${loop_dir}` command syntax (`tests/test-cancel-signal-file.sh:78-113`, `363-484`). Its negative cases assert that similar-looking state-file mutations are rejected unless they match the strict cancel shape: no signal file, wrong destination, wrong signal directory, `rm`, `sed -i`, arbitrary destinations, unrelated `/tmp/state.md`, command substitution, backticks, newlines, extra args, command prefixes, wrappers, chaining, pipes, background operators, and redirection-prefixed commands (`tests/test-cancel-signal-file.sh:119-355`, `384-644`, `650-1204`, `1210-1247`). The tail calls `is_cancel_authorized` directly for helper-level truth table coverage (`tests/test-cancel-signal-file.sh:1257-1341`) and exits with the number of failures (`tests/test-cancel-signal-file.sh:1347-1355`).
- inputs_outputs_state: Inputs are Bash command strings passed to `run_bash_validator`, plus active-loop filesystem state created by `setup_test_loop`. The hook input shape is `{"tool_name":"Bash","tool_input":{"command":...}}` (`tests/test-cancel-signal-file.sh:61-70`). Expected output is mostly process status: exit `0` means the hook allows the command; exit `2` means the hook blocks it. State preconditions are created per test by adding or omitting `$LOOP_DIR/.cancel-requested` and by controlling whether `$LOOP_DIR/state.md` exists. The script does not perform the actual rename; it validates the pre-tool decision gate. The underlying active-loop detector only treats the newest loop dir as active if `state.md` or `finalized-state.md` exists (`hooks/lib/loop-common.sh:58-80`), explaining the “no active loop” allow case in `tests/test-cancel-signal-file.sh:1227-1247`.
- gates_or_invariants: The cancellation authorization invariant is implemented in `hooks/lib/loop-common.sh:is_cancel_authorized`: signal file must exist in the active loop directory (`hooks/lib/loop-common.sh:288-293`), command substitutions/backticks/newlines/chaining operators are rejected (`295-308`), `$loop_dir` and `${loop_dir}` are normalized to the active-loop path (`310-322`), any remaining `$` is rejected to block hidden variables (`323-326`), the command must start exactly with `mv` (`328-331`), exactly two args must parse with no remainder (`333-387`), and normalized source/destination must equal active-loop `state.md` and `cancel-state.md` (`389-409`). The Bash validator applies this exception before blocking state file modifications (`hooks/loop-bash-validator.sh:75-100`) and again for `mv` or `cp` source-side state-file attempts (`102-244`). It also detects `sh -c` and `bash -c` wrapper payloads (`hooks/loop-bash-validator.sh:246-264`). Redirection prefix cleaning is intentionally broad, including `&>`, `&>>`, quoted targets, ANSI-C quoting, and escaped-space targets (`hooks/loop-bash-validator.sh:118-225`).
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh` sourced at `tests/test-cancel-signal-file.sh:32-33`, `hooks/loop-bash-validator.sh` executed by `run_bash_validator` (`61-71`), and `jq` for robust JSON encoding of shell command strings (`64-69`). The algorithm being tested is documented for users in `commands/cancel-rlcr-loop.md:28-32`, which instructs creating `.cancel-requested` before `mv`, and in `README.md:116-127`, which describes loop state as controlled by `state.md` and cancellation as renaming it. The validator itself depends on `find_active_loop`, `to_lower`, `command_modifies_file`, and `is_cancel_authorized` from the common library (`hooks/lib/loop-common.sh:58-80`, `141-144`, `274-412`, `420-448`).
- edge_cases_or_failure_modes: The test suite is dominated by bypass prevention. It covers false authorization when `.cancel-requested` is absent or placed in the parent directory (`tests/test-cancel-signal-file.sh:119-133`, `199-215`); command payloads that append extra commands with `&&`, semicolon, newline, pipe, `||`, `|&`, or background `&` (`241-355`, `650-804`); substitutions using `$()` or backticks (`301-335`); hidden variable expansion with `${ifs}` (`531-544`, `1330-1341`); wrong command prefixes such as `sudo`, `env`, `command --`, leading whitespace, and shell wrappers (`550-644`, `810-844`); and source or destination path abuse such as `mv /tmp/state.md /tmp/cancel-state.md`, extra args before/after source, `mv --`, or an arbitrary destination (`281-295`, `384-438`, `491-524`, `1210-1225`). It also validates a permissive edge: if no active loop exists, the Bash hook allows the command because the loop guard is inactive (`1231-1247`).
- validation_or_tests: This file is itself the validation suite. It records pass/fail counts through local `pass` and `fail` helpers (`tests/test-cancel-signal-file.sh:17-26`) and exits with the failure count (`1347-1355`). It is registered in the repository’s full shell test runner (`tests/run-all-tests.sh:32-47`). I did not execute it in this read-only branch research run; the analysis is based on direct inspection of the test file and the hook/library sections it exercises.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-059 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Prompt/block template that defines the user-facing denial message when an agent attempts to create, read, write, edit, or shell-access forbidden `round-*-todos.md` files. It is small, but algorithmically relevant because it is the rendered contract for todo-state access gates: use native TodoWrite instead of file-based todos.
- algorithmic_behavior: The template emits a titled block, forbids access to `round-*-todos.md`, and redirects the agent to the native TodoWrite tool (`prompt-template/block/todos-file-access.md:1-8`). It is not an algorithm by itself; it is a policy payload loaded by `todos_blocked_message` in `hooks/lib/loop-common.sh:190-199`. The active validators call that message when a todo file path matches the `round-[0-9]+-todos.md` pattern and is not explicitly allowlisted for the active loop.
- inputs_outputs_state: Inputs are validator events for `Read`, `Write`, `Edit`, or `Bash` tool usage and a target path/command. `is_round_file_type` identifies todo files by suffix (`hooks/lib/loop-common.sh:146-153`). `find_active_loop` supplies the current loop directory (`hooks/lib/loop-common.sh:58-80`). `is_allowlisted_file` allows only a narrow set including `round-1-todos.md` and `round-2-todos.md` in the active loop directory (`hooks/lib/loop-common.sh:167-188`). Output is the rendered block text from this file; if the template cannot load, `todos_blocked_message` falls back to an inline equivalent (`hooks/lib/loop-common.sh:190-199`). State transition is a pre-tool gate: validators return exit `2` to block disallowed access, leaving todo state unchanged.
- gates_or_invariants: Read, Write, and Edit validators all check todo file names early and block unless the path is allowlisted in the active loop directory (`hooks/loop-read-validator.sh:32-44`, `hooks/loop-write-validator.sh:33-45`, `hooks/loop-edit-validator.sh:32-44`). Bash access is caught through `command_modifies_file` for todo filenames; the command must include the full active loop directory path and target only `round-1-todos.md` or `round-2-todos.md`, otherwise the template is rendered and the hook exits `2` (`hooks/loop-bash-validator.sh:317-329`). This preserves a single todo state mechanism: native TodoWrite owns task tracking, while round todo files are generally protected from ad hoc file manipulation.
- dependencies_and_callers: Primary caller is `todos_blocked_message` in `hooks/lib/loop-common.sh:190-199`. That shared helper is used by `hooks/loop-read-validator.sh:36-42`, `hooks/loop-write-validator.sh:37-43`, `hooks/loop-edit-validator.sh:36-42`, and `hooks/loop-bash-validator.sh:321-327`. Template presence is cross-checked in `tests/test-template-references.sh:149-167`, where `block/todos-file-access.md` is listed as a required common template. Allowlist behavior around todo files is tested directly in `tests/test-allowlist-validators.sh:70-108`, which confirms `round-1-todos.md` and `round-2-todos.md` are allowed only in the active loop context and later rounds are not.
- edge_cases_or_failure_modes: If no active loop is found, Read/Write/Edit todo checks block non-allowlisted todo files by rendering this template (`hooks/loop-read-validator.sh:36-42`, `hooks/loop-write-validator.sh:37-43`, `hooks/loop-edit-validator.sh:36-42`). If the template file is missing or template directory validation fails, `todos_blocked_message` uses its inline fallback, so the gate still communicates the same policy (`hooks/lib/loop-common.sh:190-199`). For Bash, same-basename bypasses are guarded by requiring the active-loop full path, preventing commands against `/tmp/round-1-todos.md`, old loop dirs, or same-basename paths in different roots from being accepted (`hooks/loop-bash-validator.sh:321-329`).
- validation_or_tests: `tests/test-template-references.sh:149-167` validates that this template exists as one of the common templates referenced by message functions. `tests/test-allowlist-validators.sh:70-108` validates allowlist classification for todo files; the same test file also exercises Write/Edit/Read/Bash validator behavior for allowed and blocked todo paths later in the suite. I did not run these tests in the read-only research environment; evidence is from direct file inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence sections present, one section per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`