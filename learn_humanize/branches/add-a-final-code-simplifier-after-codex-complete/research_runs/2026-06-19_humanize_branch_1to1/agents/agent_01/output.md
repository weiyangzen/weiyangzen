# agent_01 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin. The core algorithm is an RLCR loop: initialize a plan-backed loop, constrain Claude tool access through hooks, require summaries/todos/git cleanliness, ask Codex to review each round, and on Codex `COMPLETE` enter a new Finalize Phase before terminal completion.
- algorithmic_behavior: `commands/start-rlcr-loop.md:10-14` delegates to `scripts/setup-rlcr-loop.sh`, which validates plan path/content/git state/Codex availability, records the start branch, then creates `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:211-240`, `scripts/setup-rlcr-loop.sh:307-423`, `scripts/setup-rlcr-loop.sh:467-653`). `hooks/hooks.json:4-62` wires `UserPromptSubmit`, `PreToolUse`, and `Stop` events to validators and the stop hook. `hooks/lib/loop-common.sh:58-81` treats newest `state.md` or `finalized-state.md` as active. `hooks/loop-codex-stop-hook.sh:59-82` detects normal versus finalize phase, then runs ordered gates before either running Codex or completing finalization.
- inputs_outputs_state: Inputs are plan markdown, command options (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`), Claude hook JSON, transcript JSONL, templates, git status, and Codex CLI output. Outputs are loop artifacts (`state.md`, prompts, summaries, review prompts/results, `goal-tracker.md`), JSON hook block decisions, Codex debug files under `$HOME/.cache/humanize/...`, and terminal state-file renames (`complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, `unexpected-state.md`). The branch adds `finalized-state.md` as an active post-Codex cursor (`hooks/loop-codex-stop-hook.sh:72-79`, `hooks/loop-codex-stop-hook.sh:979-981`).
- gates_or_invariants: Setup rejects non-relative plan paths, spaces, shell metacharacters, symlinks, out-of-project paths, submodule paths, insufficient content, missing Codex, YAML-unsafe branch/model/effort, and invalid tracking state (`scripts/setup-rlcr-loop.sh:227-305`, `scripts/setup-rlcr-loop.sh:331-370`, `scripts/setup-rlcr-loop.sh:417-461`). Runtime gates check schema, branch consistency, plan backup integrity, todos, git status, large files, push policy, summary presence, goal-tracker initialization, and max iterations (`hooks/loop-codex-stop-hook.sh:131-176`, `hooks/loop-codex-stop-hook.sh:182-250`, `hooks/loop-codex-stop-hook.sh:252-307`, `hooks/loop-codex-stop-hook.sh:316-516`, `hooks/loop-codex-stop-hook.sh:522-643`). Finalize invariant: all those pre-review gates still apply, but Codex review is skipped and `finalized-state.md` is renamed to `complete-state.md` once checks pass (`hooks/loop-codex-stop-hook.sh:646-657`).
- dependencies_and_callers: Shell scripts depend on `bash`, `git`, `jq`, `codex`, `python3`, and timeout implementations from `scripts/portable-timeout.sh:9-76`. Template rendering uses `hooks/lib/template-loader.sh:24-31` for locating templates and `hooks/lib/template-loader.sh:50-132` for single-pass `{{VAR}}` substitution. `hooks/check-todos-from-transcript.py:20-87` parses the latest `TodoWrite` call. `scripts/humanize.sh:6-95` monitors loop/cache output, and `README.md:213-267` documents repository/state layout. The `prompt-template` tree is the review/block contract surface.
- edge_cases_or_failure_modes: Git failures and timeouts fail closed in stop/setup/plan validators (`hooks/loop-codex-stop-hook.sh:149-164`, `hooks/loop-codex-stop-hook.sh:319-337`). Codex failures block with debug paths; stdout can be copied to the expected review result if Codex did not write the file (`hooks/loop-codex-stop-hook.sh:878-950`). `COMPLETE` and `STOP` are only recognized as the last non-empty line after trimming (`hooks/loop-codex-stop-hook.sh:955-962`, `hooks/loop-codex-stop-hook.sh:1023-1050`). If `COMPLETE` arrives at max iterations, finalize is skipped and `maxiter-state.md` is created (`hooks/loop-codex-stop-hook.sh:961-969`). Documentation drift: `README.md:255-260` lists terminal state files but not the new active `finalized-state.md`, and `commands/start-rlcr-loop.md:51-55` still says `COMPLETE` ends the loop directly.
- validation_or_tests: `tests/run-all-tests.sh:32-47` includes template, validator, state naming, finalize, cancel, and plan-file suites. Finalize-specific coverage is in `tests/test-finalize-phase.sh`. CI runs shell syntax checks (`.github/workflows/shell-syntax-check.yml:19-61`), template tests (`.github/workflows/template-test.yml:26-65`), and plan-file/state/cancel suites (`.github/workflows/plan-file-test.yml:42-60`). I did not run tests because the export is read-only and the suites create/mutate temporary git repositories; direct inspection was used as requested.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-031 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable regression specification for the branch’s Finalize Phase. It proves that Codex `COMPLETE` no longer directly finishes the loop; instead the loop enters a protected simplification phase, then completes only after summary/todo/git checks pass.
- algorithmic_behavior: The test harness creates a temp git repo, mock plan, mock loop directory, mock `codex`, and state files (`tests/test-finalize-phase.sh:40-163`). It imports shared helpers from `hooks/lib/loop-common.sh` (`tests/test-finalize-phase.sh:44-45`). It verifies path helper functions for `finalized-state.md` and `finalize-summary.md` (`tests/test-finalize-phase.sh:174-216`), active-loop detection for `finalized-state.md` but not `complete-state.md` (`tests/test-finalize-phase.sh:219-256`), normal `state.md` behavior (`tests/test-finalize-phase.sh:258-271`), write/edit/bash validator behavior (`tests/test-finalize-phase.sh:273-351`), stop-hook finalize completion (`tests/test-finalize-phase.sh:353-440`), `COMPLETE` entry behavior (`tests/test-finalize-phase.sh:442-496`), incomplete todos (`tests/test-finalize-phase.sh:498-541`), normal non-complete review flow (`tests/test-finalize-phase.sh:543-615`), and validator parsing of `finalized-state.md` (`tests/test-finalize-phase.sh:617-665`).
- inputs_outputs_state: Inputs are hook JSON snippets, synthetic transcript JSONL, synthetic loop state frontmatter, temporary plan/goal tracker files, mock Codex stdout, and git dirty/clean state. Outputs are PASS/FAIL counters, hook exit codes, hook JSON block decisions, and filesystem assertions on state files. Key transitions asserted: `state.md` to `finalized-state.md` on Codex `COMPLETE` (`tests/test-finalize-phase.sh:460-475`), `finalized-state.md` to `complete-state.md` after finalize checks (`tests/test-finalize-phase.sh:412-431`), and `state.md` to `maxiter-state.md` when current round equals max (`tests/test-finalize-phase.sh:478-496`).
- gates_or_invariants: Finalize phase must require `finalize-summary.md` (`tests/test-finalize-phase.sh:365-380`), keep Codex uninvolved during finalization (`tests/test-finalize-phase.sh:382-388`, `tests/test-finalize-phase.sh:434-440`), require clean git state (`tests/test-finalize-phase.sh:390-407`), block incomplete todos (`tests/test-finalize-phase.sh:521-540`), allow writing only the active `finalize-summary.md` (`tests/test-finalize-phase.sh:281-291`), and block any direct modifications or source copies/moves of `finalized-state.md` (`tests/test-finalize-phase.sh:293-351`).
- dependencies_and_callers: Depends on `git`, `mktemp`, `jq` indirectly through hooks, `python3` through todo checking, the stop hook, validators, and shared helpers. It mutates PATH to install a mock `codex` (`tests/test-finalize-phase.sh:47-81`) and relies on helper-created loop files with state frontmatter matching runtime schema (`tests/test-finalize-phase.sh:118-163`).
- edge_cases_or_failure_modes: Covers max-iteration bypass of Finalize Phase, `complete-state.md` not being active, normal RLCR rounds staying unaffected, Codex not being invoked in finalize, and validators parsing `finalized-state.md` where older logic may have assumed `state.md`. Test fragility: because it creates commits and temp repos, it requires a writable temp area and functioning git config; the read-only branch export is unsuitable for executing it here.
- validation_or_tests: This file is itself validation. It exits with the failed-test count (`tests/test-finalize-phase.sh:667-676`) and is included by the aggregate runner (`tests/run-all-tests.sh:45`). Its behavioral coverage maps directly to the stop-hook implementation at `hooks/loop-codex-stop-hook.sh:646-657` and `hooks/loop-codex-stop-hook.sh:961-1020`.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-061 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when the stop hook detects that the required work summary file is absent. It is a gate contract: no summary, no exit/review/finalization.
- algorithmic_behavior: The template renders a Markdown block headed `Work Summary Missing`, tells the agent it attempted to exit without a summary, interpolates `{{SUMMARY_FILE}}`, lists expected summary contents, and instructs retry after writing it (`prompt-template/block/work-summary-missing.md:1-16`). It has no branching logic; control behavior comes from callers.
- inputs_outputs_state: Input variable is `SUMMARY_FILE`. Output is a Markdown reason string that the stop hook embeds into a JSON block decision. In normal phase, the caller sets `SUMMARY_FILE` to `round-${CURRENT_ROUND}-summary.md`; in Finalize Phase, it sets it to `finalize-summary.md` (`hooks/loop-codex-stop-hook.sh:522-537`). The template does not mutate state.
- gates_or_invariants: Enforces the invariant that a summary file must exist before the loop can continue past stop-hook gates. Because this check occurs before Codex prompt construction and before finalize completion, it blocks both regular review and final completion (`hooks/loop-codex-stop-hook.sh:529-553`). In Finalize Phase, the system message changes to “Finalize Phase - summary file missing” while the rendered body remains generic (`hooks/loop-codex-stop-hook.sh:539-543`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in the stop hook (`hooks/loop-codex-stop-hook.sh:533-537`) and rendered by the single-pass template loader (`hooks/lib/template-loader.sh:170-203`). Its existence is protected by template-reference tests that scan hook references (`tests/test-template-references.sh:56-111`).
- edge_cases_or_failure_modes: If `SUMMARY_FILE` is missing from substitutions, the placeholder is intentionally preserved by the template renderer rather than expanded recursively (`hooks/lib/template-loader.sh:111-122`). The content is generic and says “What was implemented”; for Finalize Phase the dedicated prompt asks for simplification details, so this block is a skip candidate only for wording refinement, not for algorithmic relevance.
- validation_or_tests: Directly exercised by the finalize test’s missing-summary assertion (`tests/test-finalize-phase.sh:365-380`). Template loader behavior and fallback handling are tested in `tests/test-template-loader.sh:196-220`, and aggregate execution includes template reference validation (`tests/run-all-tests.sh:34-45`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 evidence headings present; IDs are not repeated here to preserve one occurrence per assigned item.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`