# agent_20 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-020 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: Claude `PreToolUse` read gate for RLCR loop-controlled files. It prevents the agent from reading stale, misplaced, or wrong-round loop artifacts, and redirects todo access away from `round-*-todos.md` toward native TodoWrite. It is installed for the `Read` matcher in `hooks/hooks.json:33-39`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name`, and exits immediately unless the tool is `Read` (`hooks/loop-read-validator.sh:22-27`). It extracts `.tool_input.file_path`, lowercases it via `to_lower`, then checks three classes of controlled paths: todos, summaries, and prompts (`hooks/loop-read-validator.sh:29-50`). For `round-*-todos.md`, it finds the active `.humanize/rlcr` loop and blocks unless the exact file is allowlisted in the active loop (`hooks/loop-read-validator.sh:36-43`). For summary/prompt reads, it finds the active loop, parses `state.md` or `finalized-state.md`, extracts current round, extracts the requested file’s round number, classifies file type, then applies location, round, and exact-directory checks (`hooks/loop-read-validator.sh:54-146`).
- inputs_outputs_state: Input is Claude hook JSON on stdin with `tool_name` and `tool_input.file_path` (`hooks/loop-read-validator.sh:22-30`). External state is resolved from `PROJECT_ROOT`, `CLAUDE_PROJECT_DIR`, optional `LOOP_BASE_DIR`/`LOOP_DIR`, active loop directory, and state frontmatter (`hooks/loop-read-validator.sh:37-39`, `61-76`). Output is either silent allow with exit `0`, or a rendered block message on stderr plus exit `2` for `PreToolUse` denial (`hooks/loop-read-validator.sh:41-43`, `105-109`, `122-128`, `141-145`). It does not mutate repo or loop state.
- gates_or_invariants: Non-Read tools are ignored (`hooks/loop-read-validator.sh:25-27`). Only filenames matching `round-[0-9]+-{summary,prompt,todos}.md` are controlled; other reads pass (`hooks/lib/loop-common.sh:146-153`, `hooks/loop-read-validator.sh:50-52`). The active loop is the newest timestamped directory containing `state.md` or `finalized-state.md`, preventing old loop revival (`hooks/lib/loop-common.sh:58-81`). Summary/prompt reads must be inside `.humanize/rlcr/`, must match the current round unless allowlisted, and must equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` exactly (`hooks/loop-read-validator.sh:100-109`, `116-128`, `135-145`). Allowlist is path-exact and limited to `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop (`hooks/lib/loop-common.sh:167-188`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh`, which also sources `hooks/lib/template-loader.sh` and establishes `TEMPLATE_DIR` (`hooks/loop-read-validator.sh:14-16`, `hooks/lib/loop-common.sh:46-52`). Depends on `jq`, `basename`, `find_active_loop`, `is_round_file_type`, `is_allowlisted_file`, `extract_round_number`, `parse_state_file`, `is_in_humanize_loop_dir`, `todos_blocked_message`, and `load_and_render_safe`. Block copy uses `prompt-template/block/todos-file-access.md`, `wrong-file-location.md`, `wrong-round-file.md`, and `wrong-directory-path.md` with inline fallbacks (`hooks/loop-read-validator.sh:41`, `105`, `122`, `141`). Direct caller is Claude hook wiring in `hooks/hooks.json:33-39`.
- edge_cases_or_failure_modes: If no active loop exists, todos are blocked unless allowlisted check passes against an active loop, but summary/prompt checks exit allow because no current loop can be established (`hooks/loop-read-validator.sh:36-43`, `65-67`). In Finalize Phase, `finalized-state.md` supersedes `state.md` for current-round parsing (`hooks/loop-read-validator.sh:69-76`). A malformed round filename that matched summary/prompt type but yields no round number is allowed (`hooks/loop-read-validator.sh:83-86`). The directory invariant is string-exact, so equivalent relative paths, symlinked paths, or normalized-but-not-identical paths can be blocked even if they resolve to the same file (`hooks/loop-read-validator.sh:135-145`). Case handling is asymmetric: type/round detection lowercases the path, but exact path comparison remains case-sensitive (`hooks/loop-read-validator.sh:29-30`, `137`). Template absence degrades to fallback messages through `load_and_render_safe`, preserving blocking behavior.
- validation_or_tests: `tests/test-allowlist-validators.sh:232-281` exercises read-validator allowlist behavior: allows `round-1-todos.md` and historical `round-0-summary.md`, blocks non-allowlisted `round-3-todos.md` and stale `round-3-summary.md`. `tests/test-finalize-phase.sh:639-650` verifies the read validator parses `finalized-state.md` and allows the current round summary during Finalize Phase. `tests/test-template-references.sh:56-64` includes this validator in template-reference scanning, and `tests/test-template-references.sh:174-190` checks critical validators use safe template rendering. Related shared allowlist tests appear in `tests/test-allowlist-validators.sh:70-121`.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-050 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR Round 0 goal-tracker initialization gate. It is not executable code, but it defines the remediation contract shown when the loop refuses to advance because `goal-tracker.md` still contains placeholders in required sections.
- algorithmic_behavior: The template renders a hard-stop explanation headed “Goal Tracker Not Initialized,” interpolates the path `{{GOAL_TRACKER_FILE}}`, interpolates a generated missing-item list `{{MISSING_ITEMS}}`, then instructs the agent to read the tracker, replace placeholders, define the Ultimate Goal, define 3-7 testable Acceptance Criteria, populate Active Tasks, write the updated file, and retry exit (`prompt-template/block/goal-tracker-not-initialized.md:1-18`). The actual gate lives in `hooks/loop-codex-stop-hook.sh:557-630`: only in non-Finalize Round 0, it extracts the `### Ultimate Goal`, `### Acceptance Criteria`, and `#### Active Tasks` sections, searches each section for the generic placeholder pattern `\[To be [a-z]`, builds section-specific missing lines, renders this template, and returns a JSON stop-hook block.
- inputs_outputs_state: Template inputs are `GOAL_TRACKER_FILE` and `MISSING_ITEMS` (`prompt-template/block/goal-tracker-not-initialized.md:5-6`, `9`). It is rendered by `load_and_render_safe` with values from `$LOOP_DIR/goal-tracker.md` and the dynamically built missing list (`hooks/loop-codex-stop-hook.sh:560`, `596-618`). The rendered content becomes the JSON `reason`, with `decision: "block"` and system message `Loop: Goal Tracker not initialized in Round 0` (`hooks/loop-codex-stop-hook.sh:620-627`). The template itself has no persistent state, but its rendered instructions govern the transition out of Round 0.
- gates_or_invariants: The gate is skipped in Finalize Phase and only runs when `CURRENT_ROUND == 0` and `goal-tracker.md` exists (`hooks/loop-codex-stop-hook.sh:557-563`). Missing initialization is detected per section, not by scanning the entire file, avoiding false positives from placeholder wording elsewhere (`hooks/loop-codex-stop-hook.sh:564-590`). The immutable-section invariant is explicit in the template: the immutable section can only be set in Round 0 and becomes read-only afterward (`prompt-template/block/goal-tracker-not-initialized.md:16`). Because this block runs after summary-file existence and before max-iteration/Codex-review transitions, unresolved placeholders prevent normal Round 0 exit (`hooks/loop-codex-stop-hook.sh:529-554`, `611-630`, `636-657`).
- dependencies_and_callers: Called directly from `hooks/loop-codex-stop-hook.sh:616-618` through `load_and_render_safe`. Rendering semantics come from `hooks/lib/template-loader.sh`: templates use `{{VARIABLE_NAME}}` syntax, missing variables remain as placeholders, and substitution is single-pass to prevent placeholder injection (`hooks/lib/template-loader.sh:7-14`, `50-58`, `71-131`). Initial placeholder content is created by `scripts/setup-rlcr-loop.sh`: goal placeholder fallback at `537`, acceptance criteria placeholder at `556`, active-task placeholder row at `574-579`; the initial Round 0 prompt requires initializing the tracker before implementation and restates the immutable-section rule (`scripts/setup-rlcr-loop.sh:605-615`, `648-653`).
- edge_cases_or_failure_modes: If `goal-tracker.md` is absent, this particular initialization check does not run because the condition requires `-f "$GOAL_TRACKER_FILE"` (`hooks/loop-codex-stop-hook.sh:563`); another gate would need to catch absence. Placeholder detection is generic and lowercase-specific (`\[To be [a-z]`), so differently phrased placeholders or capitalized text after “To be” may evade this gate, while legitimate bracketed text beginning with that pattern inside the target sections could be flagged (`hooks/loop-codex-stop-hook.sh:576-593`). Section extraction stops on any heading beginning with `##`; because `#### Active Tasks` also matches the stop pattern after capture starts, unusual nested heading structure can truncate detection (`hooks/loop-codex-stop-hook.sh:588-590`). If the template is missing or empty, `load_and_render_safe` falls back to a shorter inline message but preserves the block decision (`hooks/loop-codex-stop-hook.sh:612-618`).
- validation_or_tests: `tests/test-plan-file-hooks.sh:737-807`, `809-876`, `878-943`, and `945-1010` verify section-specific reporting for only Ultimate Goal missing, only Acceptance Criteria missing, only Active Tasks missing, and all three missing. `tests/test-templates-comprehensive.sh:118-194` validates template placeholder syntax across all markdown templates, and `tests/test-templates-comprehensive.sh:575-605` verifies all templates can be loaded and rendered with dummy values. Related initialization source behavior is covered by setup script lines that seed placeholders and Round 0 instructions (`scripts/setup-rlcr-loop.sh:507-579`, `602-653`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2` item evidence sections present; item ids are intentionally not repeated here to preserve the exactly-once output constraint
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`