# agent_04 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-004 `directory` `prompt-template`
- cursor: `[_]`
- core_role: `prompt-template` is the declarative prompt and block-message layer for the RLCR loop algorithm. It is not executable by itself, but it defines the text contracts that validators, stop hooks, Codex review prompts, Claude continuation prompts, and the new Finalize Phase use to steer state transitions and enforce gates. The directory was inspected recursively: `block/`, `claude/`, and `codex/`, 29 files total, 23,764 bytes.
- algorithmic_behavior: `hooks/lib/template-loader.sh` resolves this directory from `hooks/lib` to `prompt-template` at lines 24-31, loads files by relative name at lines 33-47, and renders `{{VAR}}` placeholders in a single pass at lines 50-132. Single-pass rendering is an invariant: placeholders injected through variable values are intentionally not rescanned, preventing prompt corruption for content-heavy values such as review summaries. `load_and_render_safe` at lines 167-203 provides graceful fallback behavior when a template is missing or renders empty. `validate_template_dir` requires the `block`, `codex`, and `claude` subdirectories at lines 205-221.
- inputs_outputs_state: Inputs are template names plus shell-provided substitution pairs such as `CURRENT_ROUND`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `SUMMARY_CONTENT`, `REVIEW_RESULT_FILE`, and `FINALIZE_SUMMARY_FILE`. Outputs are rendered Markdown strings emitted either to stderr for PreToolUse blockers, to JSON `reason` fields for stop/user-prompt hooks, or to loop files like `round-N-review-prompt.md` and `round-N-prompt.md`. State effects are indirect but central: rendered templates decide whether a hook blocks, whether Codex is instructed to emit `COMPLETE` or `STOP`, whether `state.md` advances rounds, and whether `state.md` is renamed into `finalized-state.md` or completion terminal state files.
- gates_or_invariants: The `block/` templates are guardrail messages for invalid operations: wrong round/location reads (`wrong-round-file.md`, `wrong-file-location.md`, `wrong-directory-path.md`), wrong summary writes (`wrong-round-number.md`, `wrong-summary-location.md`), state mutation (`state-file-modification.md`, `finalized-state-file-modification.md`), todo/prompt file access, dirty git state, unpushed commits, large files, stale schema, missing summaries, and goal-tracker misuse. The `codex/regular-review.md` template requires Codex to read the original plan and only output `COMPLETE` when all tasks are complete with no deferrals at lines 3-11 and 49-57. `codex/full-alignment-review.md` adds every-fifth-round goal alignment, stagnation review, and `STOP` circuit-breaker instructions at lines 59-93. The branch-specific Finalize Phase contract lives in `claude/finalize-phase-prompt.md`: after Codex passes, Claude must run `code-simplifier:code-simplifier`, preserve functionality, keep tests passing, avoid new bugs, and write `finalize-summary.md` at lines 1-50.
- dependencies_and_callers: `loop-common.sh` sources `template-loader.sh`, validates the directory, and exposes standard blocker renderers at lines 46-56 and 190-247. `loop-read-validator.sh` uses `block/wrong-file-location.md`, `block/wrong-round-file.md`, and `block/wrong-directory-path.md` for read gates at lines 100-145. `loop-write-validator.sh` and `loop-edit-validator.sh` consume state, finalized-state, plan backup, goal-tracker, summary, and wrong-round templates at `hooks/loop-write-validator.sh` lines 100-216 and `hooks/loop-edit-validator.sh` lines 81-150. `loop-codex-stop-hook.sh` consumes most of the directory: block templates for pre-review gates at lines 230-553 and 611-618, Codex review prompts at lines 677-748, Finalize Phase prompt at lines 983-1019, and next-round Claude prompts/footers at lines 1066-1108.
- edge_cases_or_failure_modes: Missing template files do not hard-fail most hooks because callers use `load_and_render_safe`; this preserves gate behavior with inline fallbacks. Missing subdirectories trigger a warning in `loop-common.sh` but allow graceful degradation. Unrendered placeholders remain visible by design when variables are absent. `finalized-state-file-modification.md` is important because `is_state_file_path` also matches `finalized-state.md`; callers must check finalized state first, as documented in write/edit validators. `block/codex-review-failed.md` exists but the current stop hook uses an inline `codex_failure_exit` message for several Codex failure paths, so that template may be partly legacy or reserved rather than the live path for all Codex failures.
- validation_or_tests: Template behavior is covered by `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh`. The Finalize Phase prompt and state transition path are validated by `tests/test-finalize-phase.sh`, especially the assertions that `COMPLETE` creates `finalized-state.md` and a simplifier prompt at lines 460-476, Finalize completion renames to `complete-state.md` at lines 412-439, and Finalize skips Codex review at lines 382-388 and 434-440.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-034 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: This shell test is an executable specification for loop-state file naming and active-loop detection. It validates the distinction between active state files (`state.md`, and by related code also `finalized-state.md`) and terminal state files (`complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, `unexpected-state.md`).
- algorithmic_behavior: The test creates a temporary git repo, creates RLCR loop directories under `.humanize/rlcr/<timestamp>`, sources `hooks/lib/loop-common.sh`, and exercises three core helpers: `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. It asserts that terminal state files are not treated as active loops at lines 51-72 and 93-155, while `state.md` is active at lines 74-91. It verifies newest timestamp precedence at lines 157-176. It then validates `end_loop` rejects invalid reasons at lines 182-200 and maps valid reasons to `${reason}-state.md` at lines 202-226.
- inputs_outputs_state: Inputs are synthetic loop directories and small YAML-frontmatter state files written into the temp repo. Function outputs are captured as shell variables (`ACTIVE_LOOP`, `RESULT`, `EXIT_CODE`) and file existence checks. The expected state transition is a rename from `state.md` to one of `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`; missing state files should cause nonzero return and a warning rather than silent success.
- gates_or_invariants: Only `state.md` should mean a normal active loop in this test; finished/cancelled/stopped/unexpected/max-iteration states must be archival and must not re-enter the loop. `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, and `unexpected`, matching `loop-common.sh` lines 464-497. New active-loop discovery is scoped to `.humanize/rlcr`, not legacy `.humanize-loop.local`, which is asserted at lines 245-287.
- dependencies_and_callers: The test depends directly on `hooks/lib/loop-common.sh` at line 65. It also depends on git for temp repo initialization at lines 40-46, `mktemp` cleanup at lines 31-33, and shell utilities such as `grep`, `rm`, and `mkdir`. It is indirectly coupled to `loop-codex-stop-hook.sh`, whose terminal paths call `end_loop`: unexpected state on bad Codex config/current_round at `hooks/loop-codex-stop-hook.sh` lines 102-119, max iterations at lines 638-642 and 961-968, and `STOP` at lines 1023-1050.
- edge_cases_or_failure_modes: The test covers invalid `end_loop` reason strings, missing `state.md`, stale terminal files, timestamp ordering, and legacy path isolation. One subtle edge: this test’s header says `complete-state.md` on `COMPLETE`, but in this branch `COMPLETE` first enters Finalize Phase as `finalized-state.md`; `complete-state.md` is created after Finalize checks pass. That branch behavior is covered separately in `tests/test-finalize-phase.sh`. The test also predates or intentionally excludes `finalized-state.md` active detection; `tests/test-finalize-phase.sh` fills that gap at lines 219-235.
- validation_or_tests: The script is itself the validation asset and exits with `TESTS_FAILED` at line 298. It reports pass/fail counters at lines 289-298. It should be run as part of the shell test suite, likely via `tests/run-all-tests.sh`. I did not execute it because the assignment requested read-only research notes and the test creates temp repos/files outside the branch export.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-064 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: This block template is the user-facing read gate for attempts to read a round prompt or summary whose `round-N` number does not match the active loop’s current round. It prevents agents from acting on stale instructions or summaries from old rounds.
- algorithmic_behavior: The template says the attempted file `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md` is invalid because the current round is `{{CURRENT_ROUND}}`, then lists the active current prompt and summary paths under `{{ACTIVE_LOOP_DIR}}` and gives a direct `cat {{FILE_PATH}}` escape hatch for intentional access. Its complete contents are nine lines, with placeholders on lines 3 and 6-9.
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, supplied by `hooks/loop-read-validator.sh` lines 122-127. Output is rendered Markdown written to stderr, followed by hook exit code `2`, which blocks the read. The template itself mutates no state; it preserves algorithmic state by preventing reads from a non-current round unless explicitly allowlisted.
- gates_or_invariants: The active invariant is in `loop-read-validator.sh`: after extracting `CLAUDE_ROUND` from the basename at lines 83-86 and determining file type at lines 88-94, the hook blocks when `CLAUDE_ROUND != CURRENT_ROUND` and the path is not allowlisted at lines 116-128. This preserves a single-current-round model and avoids stale prompt/summary context.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh` lines 116-128 through `load_and_render_safe`. It depends on helper functions from `loop-common.sh`: `is_round_file_type` at lines 146-153, `extract_round_number` at lines 155-165, `find_active_loop` at lines 58-81, `parse_state_file` at lines 98-139, and `is_allowlisted_file` at lines 167-188. It is validated structurally by template-reference tests and behaviorally by read-validator tests.
- edge_cases_or_failure_modes: If there is no active loop, read validation exits open before this template is used. If the filename does not match a round file pattern, the read is not blocked by this gate. A small allowlist permits historical files such as early todos/summaries. In Finalize Phase, the validator parses `finalized-state.md` when present, so wrong-round reads remain tied to the preserved current round rather than disabled.
- validation_or_tests: `tests/test-template-loader.sh` exercises nearby real template rendering using `wrong-round-number.md`, while `tests/test-template-references.sh` scans hook references to ensure this file exists. `tests/test-finalize-phase.sh` confirms the read validator parses `finalized-state.md` and allows the current round summary at lines 639-651, covering the same current-round machinery this template depends on.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item headings present exactly once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`