# agent_27 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-027 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the test harness’s ANSI-stripping and pass/fail count extraction algorithm. It verifies that colored test-suite summaries can be normalized into plain text before `Passed:` and `Failed:` totals are parsed.
- algorithmic_behavior: The script defines `pass()` and `fail()` helpers that print colored PASS/FAIL lines and mutate `TESTS_PASSED` / `TESTS_FAILED` counters (`tests/test-ansi-parsing.sh:18-31`). Each test builds a hard-coded input string, strips ANSI SGR escape sequences with `sed "s/${esc}\\[[0-9;]*m//g"`, then compares either the stripped string or extracted numeric counts against an expected value (`tests/test-ansi-parsing.sh:45-53`, `tests/test-ansi-parsing.sh:74-80`, `tests/test-ansi-parsing.sh:88-94`). Count extraction is done with two `grep -oE` stages: first matching `Passed:` / `Failed:` plus digits, then taking the trailing integer (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`). Multi-line summary parsing uses `tail -1` to select the final matching count (`tests/test-ansi-parsing.sh:123-125`).
- inputs_outputs_state: Inputs are local fixture strings embedded in the script: basic colored counts, multiple color spans, plain text, bold+color, zero counts, and a simulated multi-line summary (`tests/test-ansi-parsing.sh:45`, `tests/test-ansi-parsing.sh:60`, `tests/test-ansi-parsing.sh:102`, `tests/test-ansi-parsing.sh:116-122`, `tests/test-ansi-parsing.sh:137`, `tests/test-ansi-parsing.sh:151`). Outputs are stdout diagnostics plus a final colored summary showing `Passed:` and `Failed:` totals (`tests/test-ansi-parsing.sh:163-168`). Internal state transitions are limited to incrementing pass/fail counters. The terminal state is process exit `0` when `TESTS_FAILED == 0`, otherwise exit `1` (`tests/test-ansi-parsing.sh:170-178`).
- gates_or_invariants: The central invariant is that ANSI SGR wrappers must not affect parsing of numeric summary values. The sed expression must be portable across GNU and BSD sed by using Bash ANSI-C quoting to create `esc=$'\033'` rather than relying on GNU-only escape handling (`tests/test-ansi-parsing.sh:38-47`). The parser expects labels exactly matching `Passed:` or `Failed:` followed by optional whitespace and digits. The suite itself is part of the top-level ordered test list in `tests/run-all-tests.sh:32-47`, and that harness uses the same stripping/parsing logic at `tests/run-all-tests.sh:66-70`.
- dependencies_and_callers: Depends on Bash, `sed`, `grep -oE`, `tail`, arithmetic expansion, and terminal ANSI conventions. It is called by `tests/run-all-tests.sh`, which captures each suite’s output, strips ANSI codes, extracts the final `Passed:` / `Failed:` counts, aggregates totals, and marks the suite failed if the child exits nonzero or reports failed tests (`tests/run-all-tests.sh:60-81`). No production hook calls this test directly; its algorithmic value is guarding the test runner’s aggregation behavior.
- edge_cases_or_failure_modes: The stripper only removes SGR sequences ending in `m` with numeric/semicolon parameters, so other ANSI control sequences are outside the covered grammar (`tests/test-ansi-parsing.sh:47`). The count extractor is label-sensitive and will miss lowercase or renamed labels. In single-line tests without `tail -1`, multiple matches could return multiple lines, though current fixtures provide one match. In the harness, `tail -1` intentionally handles repeated summaries (`tests/run-all-tests.sh:69-70`). The fallback `|| echo "0"` means a missed count becomes zero rather than a hard parse failure (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`), which can hide malformed output if the child also exits successfully.
- validation_or_tests: This file is itself the validation asset. It contains eight explicit cases: basic color stripping, multiple colors, passed-count extraction, failed-count extraction, zero failed count, complex multi-line summary, no ANSI codes, and bold+color combined (`tests/test-ansi-parsing.sh:42-158`). `tests/run-all-tests.sh` includes it as `test-ansi-parsing.sh` in the full suite list (`tests/run-all-tests.sh:43`). I inspected the file and its caller; I did not execute the test suite because this worker task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-057 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template used as the user-facing denial message when the loop validators detect an attempted modification to `state.md`. It defines the review contract that `state.md` is loop-owned state and must not be edited by an agent or tool call.
- algorithmic_behavior: The template is static Markdown with a heading, a prohibition, and the rationale that `state.md` contains current round number, max iterations, and Codex configuration (`prompt-template/block/state-file-modification.md:1-10`). It has no placeholders or dynamic rendering fields. The block is loaded by `state_file_blocked_message()` through `load_and_render_safe "$TEMPLATE_DIR" "block/state-file-modification.md" "$fallback"` (`hooks/lib/loop-common.sh:210-217`). If the file is missing or empty, the inline fallback still blocks with a shorter equivalent message (`hooks/lib/loop-common.sh:211-216`).
- inputs_outputs_state: Input is the validator’s detection that a requested path or shell command targets `state.md`. Output is the rendered Markdown message written to stderr by validators before exiting with code `2` (`hooks/loop-write-validator.sh:110-112`, `hooks/loop-edit-validator.sh:91-93`, `hooks/loop-bash-validator.sh:93-99`). The template itself does not mutate state. The guarded state is external: `state.md` frontmatter stores fields parsed by the loop system, including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, and related fields (`hooks/lib/loop-common.sh:15-24`, `hooks/lib/loop-common.sh:98-111`).
- gates_or_invariants: The invariant is that normal tool paths cannot modify the active loop’s `state.md`; only loop-controlled transitions should rename or update state. Direct Write and Edit validators classify `state.md` paths using `is_state_file_path`, after checking the more specific finalized-state path first (`hooks/loop-write-validator.sh:100-113`, `hooks/loop-edit-validator.sh:81-94`). Bash validation blocks destination writes and redirects to `state.md`, then separately checks `mv` / `cp` source operands so moving or copying from `state.md` is also blocked (`hooks/loop-bash-validator.sh:75-116`). The main exception is the authorized cancel transition: `mv active_loop/state.md active_loop/cancel-state.md` is allowed only when `.cancel-requested` exists and the command passes strict normalization, no substitution, no shell operators, exactly two args, and exact active-loop source/destination checks (`hooks/lib/loop-common.sh:274-412`).
- dependencies_and_callers: Depends on the template loader in `hooks/lib/template-loader.sh`, especially `load_and_render_safe`, which loads a template and falls back when missing (`hooks/lib/template-loader.sh:167-180`). Primary callers are `state_file_blocked_message()` in `hooks/lib/loop-common.sh:210-217`, `hooks/loop-write-validator.sh:110-112`, `hooks/loop-edit-validator.sh:91-93`, and `hooks/loop-bash-validator.sh:93-99`. Template existence/reference coverage includes `tests/test-template-references.sh`, where `block/state-file-modification.md` is listed as a common template expected to exist (`tests/test-template-references.sh:149-167`).
- edge_cases_or_failure_modes: Because `is_state_file_path` matches `state.md$`, it would also match `finalized-state.md`; validators avoid the wrong message by checking `is_finalized_state_file_path` first (`hooks/loop-write-validator.sh:101-110`, `hooks/loop-edit-validator.sh:81-92`, `hooks/loop-bash-validator.sh:79-88`). The template text names `state.md` only; finalized state has a separate template. Bash command detection is regex-based and therefore backed by many bypass tests, including redirection prefixes, chained commands, shell wrappers, quoted paths, ANSI-C quoted redirection targets, and unrelated `/tmp/state.md` paths (`tests/test-cancel-signal-file.sh:136-173`, `tests/test-cancel-signal-file.sh:218-235`, `tests/test-cancel-signal-file.sh:1206-1225`). If the template file is absent, the system still blocks via fallback, but the richer explanation about state contents is lost (`hooks/lib/loop-common.sh:211-216`).
- validation_or_tests: `tests/test-template-references.sh` validates this template exists in the common template set (`tests/test-template-references.sh:152-167`). State-file modification behavior is covered indirectly by Bash-validator cancel-signal negative tests, such as blocking `echo > state.md`, `sed -i state.md`, `rm state.md`, and unrelated `/tmp/state.md` moves even with a cancel signal (`tests/test-cancel-signal-file.sh:139-152`, `tests/test-cancel-signal-file.sh:159-172`, `tests/test-cancel-signal-file.sh:221-234`, `tests/test-cancel-signal-file.sh:1210-1224`). Finalized-state sibling behavior is separately tested in `tests/test-finalize-phase.sh:293-327`. I inspected the template and its call sites; I did not execute tests because this worker task requested research notes only and no file modification.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-027`, `ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-057`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`