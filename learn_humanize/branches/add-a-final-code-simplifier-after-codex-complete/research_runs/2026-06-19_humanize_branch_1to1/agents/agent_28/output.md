# agent_28 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-028 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash modification detector used by RLCR loop validators. The file directly sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:13` and tests `command_modifies_file`, the shared helper that decides whether a Bash command attempts to modify guarded loop files.
- algorithmic_behavior: The test converts every sample command through `to_lower` before detection, matching the production normalization path in `hooks/loop-bash-validator.sh:29-30`. It defines two assertion paths: `assert_modifies` expects `command_modifies_file` to return success, and `assert_not_modifies` expects no match (`tests/test-bash-validator-patterns.sh:36-62`). The covered detector patterns correspond to implementation regexes in `hooks/lib/loop-common.sh:423-448`: redirects, append redirects, `tee`, in-place `sed`, `awk`, `perl`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>`.
- inputs_outputs_state: Input is a shell command string plus an optional target filename regex. Default target is `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:39` and `tests/test-bash-validator-patterns.sh:53`); later groups pass `state\.md` and `round-[0-9]+-summary\.md` (`tests/test-bash-validator-patterns.sh:181-194`). Output is process status: zero failures exits `0`; any failed expectation increments `TESTS_FAILED` and exits `1` (`tests/test-bash-validator-patterns.sh:20-33`, `tests/test-bash-validator-patterns.sh:206-214`). Internal state is only test counters, not repo state.
- gates_or_invariants: The detector must identify writes to protected names while avoiding false positives for read-only shell commands. Positive gates include simple redirection (`tests/test-bash-validator-patterns.sh:75-80`), `tee` writes (`tests/test-bash-validator-patterns.sh:89-93`), in-place editors (`tests/test-bash-validator-patterns.sh:102-107`), file operations (`tests/test-bash-validator-patterns.sh:116-121`), and lower-level writers such as `dd`, `truncate`, and `exec` (`tests/test-bash-validator-patterns.sh:130-133`). Negative gates ensure `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, and `diff` do not count as modification (`tests/test-bash-validator-patterns.sh:142-152`). This invariant is important because `hooks/loop-bash-validator.sh` uses the helper as a blocking gate for state, plan, goal tracker, prompt, summary, and todos files.
- dependencies_and_callers: Depends on Bash, `set -euo pipefail`, and shared functions from `hooks/lib/loop-common.sh`. The tested helper is called by `hooks/loop-bash-validator.sh` for `finalized-state.md`, `state.md`, plan backups, `goal-tracker.md`, round prompt files, round summary files, and round todos files (`hooks/loop-bash-validator.sh:87-93`, `hooks/loop-bash-validator.sh:272`, `hooks/loop-bash-validator.sh:285`, `hooks/loop-bash-validator.sh:301`, `hooks/loop-bash-validator.sh:311`, `hooks/loop-bash-validator.sh:321`). The suite is part of the aggregate runner at `tests/run-all-tests.sh:32-47`, specifically listed at `tests/run-all-tests.sh:35`.
- edge_cases_or_failure_modes: The file explicitly tests case-insensitivity by lowercasing before detection and using an uppercase command sample (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:80`). It covers redirect spacing variants such as `> goal-tracker.md` and `>goal-tracker.md` (`tests/test-bash-validator-patterns.sh:161-164`). It documents a known limitation: multi-source copy like `cp file1.md file2.md goal-tracker.md` is not detected because the implementation pattern expects `cp src dest` (`tests/test-bash-validator-patterns.sh:165-168`; implementation at `hooks/lib/loop-common.sh:434`). It also confirms variable targets such as `$FILE` are not matched unless the literal protected path appears (`tests/test-bash-validator-patterns.sh:170-172`), so shell expansion bypass protection is handled elsewhere by higher-level hook tests rather than this helper-only suite.
- validation_or_tests: I ran `bash tests/test-bash-validator-patterns.sh` from the branch export. Result: 48 passed, 0 failed, exit code 0. Static validation also confirms this suite is included in `tests/run-all-tests.sh`.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-058 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the Bash validator’s summary-file write gate. It is the rendered contract shown when a Bash command attempts to modify `round-*-summary.md`; the algorithmic role is to redirect the actor from shell writes to validated Write/Edit-tool paths.
- algorithmic_behavior: The template states that Bash commands must not modify summary files (`prompt-template/block/summary-bash-write.md:3`), renders a validated target through `{{CORRECT_PATH}}` (`prompt-template/block/summary-bash-write.md:5`), and explains that Bash commands bypass validation hooks (`prompt-template/block/summary-bash-write.md:7-8`). The placeholder is rendered by `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:228-237`, which uses `load_and_render_safe` with a fallback containing the same `CORRECT_PATH` variable. Template rendering is single-pass via `hooks/lib/template-loader.sh:58-132`, so injected placeholder text inside a variable value is not recursively expanded.
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by the Bash validator as `"$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` when a summary-file modification is detected (`hooks/loop-bash-validator.sh:311-314`). Output is markdown written to stderr, followed by hook exit code `2` from the validator (`hooks/loop-bash-validator.sh:311-315`). The template itself has no mutable state; it participates in a validator state transition from allowed Bash command to blocked pre-tool-use action.
- gates_or_invariants: The gate invariant is that `round-[0-9]+-summary\.md` modification attempts through Bash are blocked and must use Write/Edit instead. Detection is delegated to `command_modifies_file` (`hooks/loop-bash-validator.sh:311`), while this template defines the remediation message. `load_and_render_safe` guarantees a non-empty fallback if the template is missing or renders empty (`hooks/lib/template-loader.sh:170-202`), preventing silent validator blocks.
- dependencies_and_callers: Direct caller is `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:230-237`. That helper is called only by `hooks/loop-bash-validator.sh` in the summary-file modification section (`hooks/loop-bash-validator.sh:306-315`). Template existence is checked by `tests/test-template-references.sh`, which includes `block/summary-bash-write.md` in the common template list (`tests/test-template-references.sh:152-157`) and scans shell template references (`tests/test-template-references.sh:56-110`).
- edge_cases_or_failure_modes: If the template file is missing, empty, or cannot be rendered, `load_and_render_safe` falls back to an inline message (`hooks/lib/loop-common.sh:232-236`; `hooks/lib/template-loader.sh:179-200`). If `CORRECT_PATH` is malformed, the template will still render that value literally because rendering does not validate filesystem semantics. The message says Bash commands like `cat`, `echo`, `sed`, and `awk` bypass validation hooks; detection coverage for modifying forms is specified separately by `tests/test-bash-validator-patterns.sh`, including the summary regex cases at `tests/test-bash-validator-patterns.sh:186-194`.
- validation_or_tests: Static inspection confirms the template exists and is referenced by the shared message helper. I also ran `bash tests/test-template-references.sh | sed -n '145,170p;203,225p'`; the command completed with exit code 0. The targeted Bash detector suite separately verified that Bash writes to `round-5-summary.md` and `round-10-summary.md` are detected while `cat round-5-summary.md` is ignored (`tests/test-bash-validator-patterns.sh:192-194`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item evidence sections present; each assigned item is represented once in `## Item Evidence`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`