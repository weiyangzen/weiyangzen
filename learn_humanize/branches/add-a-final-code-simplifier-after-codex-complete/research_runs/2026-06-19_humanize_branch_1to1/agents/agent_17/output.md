# agent_17 add-a-final-code-simplifier-after-codex-complete 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `71ed63c83360c154ed316e8e38853af41ffc3b7e`

## Item Evidence

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop hook implementation for the RLCR state machine. It intercepts Claude Code Stop events, validates loop invariants, runs Codex as an external reviewer during normal rounds, blocks exit with next-round feedback when review is not complete, and implements the new post-Codex Finalize Phase where code simplification/refactoring is required before loop completion. The hook is registered as the Claude Code `Stop` hook in `hooks/hooks.json:52-59` with a 7200-second timeout.
- algorithmic_behavior: The hook reads hook JSON from stdin (`hooks/loop-codex-stop-hook.sh:29`), locates the active loop under `.humanize/rlcr` (`hooks/loop-codex-stop-hook.sh:43-60`), then chooses either normal state (`state.md`) or finalized state (`finalized-state.md`) (`hooks/loop-codex-stop-hook.sh:72-82`). It parses state frontmatter through `parse_state_file` (`hooks/loop-codex-stop-hook.sh:88-100`; dependency definition in `hooks/lib/loop-common.sh:98-139`), validates model/effort and numeric round data (`hooks/loop-codex-stop-hook.sh:102-124`), then runs a fail-closed gate stack before allowing any reviewer transition. Normal rounds build a Codex review prompt (`hooks/loop-codex-stop-hook.sh:671-749`), execute `codex exec` in full-auto mode against the project root (`hooks/loop-codex-stop-hook.sh:813-840`), validate the review artifact (`hooks/loop-codex-stop-hook.sh:878-950`), and inspect the last non-empty review line for exact `COMPLETE` or `STOP` markers (`hooks/loop-codex-stop-hook.sh:955-1051`). If neither marker appears, it increments `current_round`, writes the next Claude prompt from templates, and returns JSON with `"decision": "block"` (`hooks/loop-codex-stop-hook.sh:1057-1123`).
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR` or current directory, `.humanize/rlcr/<timestamp>/state.md` or `finalized-state.md`, the plan file and backup plan, Claude transcript path, current git state, round summary files, prompt templates, and Codex CLI availability. Outputs are Claude Code hook JSON decisions, stderr diagnostic logs, per-round prompt/review files in the loop directory, Codex debug files in `$HOME/.cache/humanize/...` (`hooks/loop-codex-stop-hook.sh:779-833`), and state-file renames. State transitions are: no active loop allows exit (`hooks/loop-codex-stop-hook.sh:61-64`); normal review issues advance `current_round` and block with next instructions (`hooks/loop-codex-stop-hook.sh:1057-1123`); `STOP` calls `end_loop ... stop` (`hooks/loop-codex-stop-hook.sh:1023-1051`); invalid state calls `end_loop ... unexpected` (`hooks/loop-codex-stop-hook.sh:104-119`); max iteration exhaustion calls `end_loop ... maxiter` (`hooks/loop-codex-stop-hook.sh:636-643` and `963-969`); Codex `COMPLETE` before max iterations renames `state.md` to `finalized-state.md` and blocks with the final simplifier prompt (`hooks/loop-codex-stop-hook.sh:971-1020`); a later finalized-phase stop with all gates passing renames `finalized-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:651-657`).
- gates_or_invariants: The hook deliberately ignores `stop_hook_active` so each blocked retry can run another review (`hooks/loop-codex-stop-hook.sh:31-38`). It requires v1.1.2+ state fields `plan_tracked` and `start_branch` (`hooks/loop-codex-stop-hook.sh:126-143`), forbids branch switching (`hooks/loop-codex-stop-hook.sh:149-176`), requires the plan backup and exact plan content integrity (`hooks/loop-codex-stop-hook.sh:182-250`), requires all native TodoWrite todos to be complete before expensive review (`hooks/loop-codex-stop-hook.sh:252-307`; parser behavior in `hooks/check-todos-from-transcript.py:90-129`), fails closed if `git status --porcelain` fails or times out (`hooks/loop-codex-stop-hook.sh:316-338`), blocks large changed code/doc files above 2000 lines (`hooks/loop-codex-stop-hook.sh:340-421`), blocks uncommitted changes and optionally unpushed commits (`hooks/loop-codex-stop-hook.sh:423-516`), requires the expected round summary or `finalize-summary.md` (`hooks/loop-codex-stop-hook.sh:518-554`), checks Round 0 goal tracker initialization (`hooks/loop-codex-stop-hook.sh:556-630`), and skips Codex entirely during Finalize Phase after preflight gates pass (`hooks/loop-codex-stop-hook.sh:645-657`). Review marker detection is strict: last non-empty line only, trimmed, exactly equal to the marker, preventing accidental matches inside prose such as “cannot complete” (`hooks/loop-codex-stop-hook.sh:955-962`).
- dependencies_and_callers: Called by Claude Code’s Stop hook registration in `hooks/hooks.json:52-59`. Sources `hooks/lib/loop-common.sh` for active-loop discovery, state parsing, marker constants, template loader setup, lowercase helper, and `end_loop` (`hooks/lib/loop-common.sh:26-40`, `58-81`, `98-144`, `464-497`). Sources `scripts/portable-timeout.sh` for portable timeout execution; that wrapper chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`). Uses `hooks/check-todos-from-transcript.py` to inspect the latest TodoWrite call in a transcript and return exit codes 0, 1, or 2 (`hooks/check-todos-from-transcript.py:1-13`, `90-129`). Uses the template loader’s single-pass safe rendering and fallback behavior (`hooks/lib/template-loader.sh:50-132`, `167-203`). Calls external commands `git`, `diff`, `python3`, `jq`, `codex`, `sed`, `awk`, `grep`, `tail`, `wc`, and `cp/mv`. The new simplifier handoff is rendered from `prompt-template/claude/finalize-phase-prompt.md` when present, otherwise the inline fallback explicitly instructs use of the `code-simplifier:code-simplifier` agent (`hooks/loop-codex-stop-hook.sh:983-1008`).
- edge_cases_or_failure_modes: If no active loop or no usable state file exists, the hook exits without blocking (`hooks/loop-codex-stop-hook.sh:61-82`). Invalid `codex_model` or `codex_effort` terminates as unexpected to avoid unsafe YAML/config use (`hooks/loop-codex-stop-hook.sh:102-113`). Non-numeric `current_round` terminates unexpectedly; non-numeric `max_iterations` falls back to 42 (`hooks/loop-codex-stop-hook.sh:115-124`). Git branch lookup or status failures block rather than allowing completion (`hooks/loop-codex-stop-hook.sh:149-164`, `316-338`). Plan deletion, uncommitted plan modification, or committed content drift blocks (`hooks/loop-codex-stop-hook.sh:185-250`). Todo parser JSON errors block, while missing transcript path or no todos allow progress (`hooks/loop-codex-stop-hook.sh:260-307`; `hooks/check-todos-from-transcript.py:92-111`). In Finalize Phase, missing `finalize-summary.md`, dirty git, or incomplete todos still block before completion (`hooks/loop-codex-stop-hook.sh:522-554`, `423-516`, `252-307`). If Codex is absent, nonzero, produces no review file, or produces an empty review file, the hook blocks with debug paths for retry (`hooks/loop-codex-stop-hook.sh:755-775`, `852-950`). If Codex reports `COMPLETE` at or beyond max iterations, the hook records max-iteration termination instead of entering Finalize Phase (`hooks/loop-codex-stop-hook.sh:961-969`).
- validation_or_tests: `tests/test-finalize-phase.sh` directly exercises the new Finalize Phase behavior: missing finalize summary blocks and Codex is not invoked (`tests/test-finalize-phase.sh:354-388`), dirty git blocks in finalize (`tests/test-finalize-phase.sh:390-407`), all finalize gates passing renames to `complete-state.md` (`tests/test-finalize-phase.sh:412-430`), Codex `COMPLETE` enters Finalize Phase and the prompt mentions simplification (`tests/test-finalize-phase.sh:460-475`), max-iteration `COMPLETE` skips finalize and records max iteration (`tests/test-finalize-phase.sh:478-496`), and incomplete todos still block in Finalize Phase (`tests/test-finalize-phase.sh:499-530`). `tests/test-plan-file-hooks.sh` contains stop-hook plan integrity and git-gate coverage by invoking this hook in multiple plan-file scenarios, as found by references at `tests/test-plan-file-hooks.sh:421`, `466-499`, and later invocations. `tests/test-template-references.sh:56-64` includes this hook in the scan for shell-script template references, guarding against missing prompt-template files.
- skip_candidate: `no`

### ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-047 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Prompt/block template for the hook’s fail-closed git-status gate. It is not an algorithm implementation by itself, but it is part of the RLCR guard contract because it defines the message shown when repository state cannot be verified.
- algorithmic_behavior: The template renders a short block headed “Git Status Failed” and interpolates `{{GIT_STATUS_EXIT}}` into the failure sentence (`prompt-template/block/git-status-failed.md:1-3`). The body explains likely causes: git not responding, invalid repository state, or large repository slowness (`prompt-template/block/git-status-failed.md:5-8`). It ends by instructing manual git-status inspection and retry (`prompt-template/block/git-status-failed.md:10`). The template is loaded only in `hooks/loop-codex-stop-hook.sh` when cached `git status --porcelain` exits nonzero (`hooks/loop-codex-stop-hook.sh:322-335`).
- inputs_outputs_state: Input is the template variable `GIT_STATUS_EXIT`, supplied by the stop hook after `run_with_timeout "$GIT_TIMEOUT" git status --porcelain` fails (`hooks/loop-codex-stop-hook.sh:322-333`). Output is rendered Markdown embedded as the `reason` field of the Claude Code hook JSON block decision (`hooks/loop-codex-stop-hook.sh:332-335`). It does not read or mutate state files directly; its state effect is indirect, because the failed gate prevents RLCR exit/completion while leaving the loop state active for retry.
- gates_or_invariants: The key invariant is fail-closed repository verification: if git status cannot be obtained, downstream checks for large files, clean tree, pushed commits, Codex review, and completion are not trusted. The stop hook’s fallback text has the same required variable and is used if this template is missing or empty (`hooks/loop-codex-stop-hook.sh:327-333`; safe fallback behavior in `hooks/lib/template-loader.sh:167-203`). Rendering is single-pass, so variable content is inserted without recursive placeholder expansion (`hooks/lib/template-loader.sh:50-132`).
- dependencies_and_callers: Sole direct caller found is `hooks/loop-codex-stop-hook.sh:332`. Template path resolution comes from `hooks/lib/template-loader.sh`, which maps the plugin root to `prompt-template` (`hooks/lib/template-loader.sh:24-31`) and validates that `block`, `codex`, and `claude` subdirectories exist (`hooks/lib/template-loader.sh:205-221`). `tests/test-template-references.sh:56-70` scans this hook and related scripts for template references, which indirectly covers this template path’s existence.
- edge_cases_or_failure_modes: If the template is missing, empty, or renders empty, `load_and_render_safe` returns the inline fallback from the stop hook (`hooks/lib/template-loader.sh:167-203`), so the gate still blocks. If `GIT_STATUS_EXIT` is unavailable or malformed, the template would render the supplied string as-is; in normal hook flow it is initialized to 0 and overwritten only by the failing command’s exit code (`hooks/loop-codex-stop-hook.sh:322-325`). The text is intentionally generic and does not differentiate timeout exit code 124 from other git failures, so remediation remains manual inspection and retry.
- validation_or_tests: Direct reference discovery shows this template is matched only by the stop hook (`hooks/loop-codex-stop-hook.sh:332`) and by the template file itself. `tests/test-template-references.sh:56-70` is the repository-level guard that scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and reports missing templates. I did not find a dedicated test asserting the exact rendered prose for `git-status-failed.md`; its behavior is covered structurally by the hook’s fail-closed path plus template-reference validation.
- skip_candidate: `yes: this file is a small Markdown rendering template, not executable algorithm code; keep it in scope because it defines the user-facing contract for an algorithmic git-status failure transition.`

## Worker Self-Test
- assigned_items_seen: ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-017, ADD_A_FINAL_CODE_SIMPLIFIER_AFTER_CODEX_COMPLETE-HZ-047
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`