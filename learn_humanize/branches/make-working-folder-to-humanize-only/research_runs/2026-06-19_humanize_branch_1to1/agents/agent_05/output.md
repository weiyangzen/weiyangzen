# agent_05 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-005 `directory` `scripts`
- cursor: `[_]`
- core_role: Provides the executable shell layer for the RLCR loop: loop initialization, portable review timeouts, and operator monitoring. Recursive inspection found three direct children only: `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`. README documents this directory as “Setup scripts” and identifies these exact files at `README.md:234-237`.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` is the primary bootstrap algorithm for `/humanize:start-rlcr-loop`. It parses CLI flags, validates the plan/repository boundary, creates `.humanize/rlcr/<timestamp>/`, copies the plan backup, writes state and goal-tracker files, and emits the round-0 implementation prompt. Its command wrapper is `commands/start-rlcr-loop.md:1-14`, which only allows Bash execution of this setup script. `scripts/portable-timeout.sh` is a sourced utility used by the stop hook to execute Codex review with a platform-compatible timeout. `scripts/humanize.sh` is an interactive monitor command that discovers RLCR sessions, locates current Codex logs in `$HOME/.cache/humanize`, parses loop state/goal tracker/git status, and continuously redraws a terminal dashboard.
- inputs_outputs_state: `setup-rlcr-loop.sh` accepts a required relative plan path plus options including `--plan-file`, `--track-plan-file`, `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round` at `scripts/setup-rlcr-loop.sh:97-173`. Inputs become state fields in `.humanize/rlcr/<timestamp>/state.md`, including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` at `scripts/setup-rlcr-loop.sh:406-419`. It also writes `goal-tracker.md` and `round-0-prompt.md` at `scripts/setup-rlcr-loop.sh:425-572`, and copies the original plan to `plan.md` at `scripts/setup-rlcr-loop.sh:396-397`. `portable-timeout.sh` exposes `run_with_timeout <seconds> <command> ...` and exports `TIMEOUT_IMPL` after selecting `gtimeout`, GNU `timeout`, Python, or no timeout at `scripts/portable-timeout.sh:10-29` and `scripts/portable-timeout.sh:33-76`. `humanize.sh` reads `.humanize/rlcr`, state files, goal trackers, git status, and cached logs; it outputs terminal status/log rendering only, with no RLCR state writes in the inspected code.
- gates_or_invariants: `setup-rlcr-loop.sh` requires a Git repo and at least one commit at `scripts/setup-rlcr-loop.sh:204-214`. It forbids absolute plan paths, whitespace, shell metacharacters, symlink plan files, paths outside the project, and plans inside submodules at `scripts/setup-rlcr-loop.sh:220-292`. It enforces plan tracking mode: with `--track-plan-file`, the plan must be tracked and clean; without it, the plan must not be tracked at `scripts/setup-rlcr-loop.sh:298-326`. It rejects plans shorter than five lines at `scripts/setup-rlcr-loop.sh:332-340`, requires Codex CLI at `scripts/setup-rlcr-loop.sh:342-348`, validates branch/model/effort values for YAML safety at `scripts/setup-rlcr-loop.sh:354-382`, and uses `set -euo pipefail` at `scripts/setup-rlcr-loop.sh:11`. `portable-timeout.sh` preserves GNU timeout exit-code convention by returning `124` on Python `TimeoutExpired` at `scripts/portable-timeout.sh:53-58`. `humanize.sh` requires `.humanize/rlcr` to exist before monitoring at `scripts/humanize.sh:16-21`, filters session directories to timestamp format at `scripts/humanize.sh:33-40`, and dispatches only `humanize monitor rlcr-loop` at `scripts/humanize.sh:520-553`.
- dependencies_and_callers: `setup-rlcr-loop.sh` is invoked by `commands/start-rlcr-loop.md:12-14`; its generated state and prompt files are consumed by hooks, especially `hooks/loop-codex-stop-hook.sh`. The stop hook later sources `scripts/portable-timeout.sh` if present at `hooks/loop-codex-stop-hook.sh:669-687` and then pipes the rendered Codex review prompt through `run_with_timeout` at `hooks/loop-codex-stop-hook.sh:714-716`. `humanize.sh` is intended to be sourced in a shell profile; README shows sourcing it from `HUMANIZE_PLUGIN_ROOT/scripts/humanize.sh` and running `humanize monitor rlcr-loop` at `README.md:90-103`. `humanize.sh` mirrors stop-hook cache naming by sanitizing `pwd` with `sed`, explicitly noted at `scripts/humanize.sh:51-55`, matching the stop hook’s cache path logic at `hooks/loop-codex-stop-hook.sh:655-663`.
- edge_cases_or_failure_modes: `setup-rlcr-loop.sh` fails closed for ambiguous plan specification, unknown flags, missing option arguments, non-numeric `--max`/`--codex-timeout`, multiple plan files, inaccessible parent directories, invalid Codex model/effort strings, and unsupported plan location/tracking states. It assumes plan paths have no spaces because state serialization is unquoted; that is intentional and enforced at `scripts/setup-rlcr-loop.sh:226-232`. The plan goal/acceptance extraction is heuristic: it tries common headings and otherwise writes placeholders for Round 0 initialization at `scripts/setup-rlcr-loop.sh:448-476`. `portable-timeout.sh` falls back to running without timeout if no supported timeout tool or Python exists at `scripts/portable-timeout.sh:64-68`, which preserves function but weakens the review duration gate. `humanize.sh` can wait indefinitely for the first log at `scripts/humanize.sh:434-441`; status parsing is grep/sed based and can degrade to `N/A` when fields are missing at `scripts/humanize.sh:98-114`. Its terminal rendering depends on `tput`, `tail`, `stat`, `sed`, and a real terminal.
- validation_or_tests: `tests/test-plan-file-validation.sh` directly exercises `setup-rlcr-loop.sh` path and parameter validation, including absolute paths, nonexistent files, nonexistent parent directories, whitespace, shell metacharacters, symlinks, both `--plan-file` and positional arguments, and Codex model/effort character validation at `tests/test-plan-file-validation.sh:85-183` and `tests/test-plan-file-validation.sh:531-585`. `tests/test-plan-file-validation.sh` uses the setup script throughout, showing coverage for tracked/untracked plan modes and plan file constraints via repeated direct invocations. The timeout utility is indirectly validated through stop-hook integration paths, not by a dedicated timeout test found in assigned inspection. `humanize.sh` is documented in README at `README.md:90-108`; I did not find a direct automated monitor test in the inspected references. A read-only `git status --short` verification attempt failed because this branch export is not a normal writable Git checkout under the sandbox, not due to edits.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-035 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Specialized block-template fragment used when the RLCR stop hook detects untracked `.humanize` artifacts during the git-clean gate. It gives Claude a precise remediation instruction: `.humanize/` is local loop state and should be ignored, not committed.
- algorithmic_behavior: The template is not standalone logic; it is loaded as part of stop-hook construction of a blocking message. The file states that `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and tells the agent to add `.humanize*` to `.gitignore` plus stage `.gitignore` at `prompt-template/block/git-not-clean-humanize-local.md:2-8`. In the stop hook, uncommitted changes set `GIT_ISSUES="uncommitted changes"` at `hooks/loop-codex-stop-hook.sh:349-353`; untracked files are extracted at `hooks/loop-codex-stop-hook.sh:354-355`; if those untracked paths match `.humanize`, this template is loaded and appended to `SPECIAL_NOTES` at `hooks/loop-codex-stop-hook.sh:357-364`. The notes are then injected into the broader `block/git-not-clean.md` message at `hooks/loop-codex-stop-hook.sh:377-389`.
- inputs_outputs_state: Input is the current `git status --porcelain` output from the project root. The specific trigger is an untracked line matching `.humanize` in `UNTRACKED`. Output is a Markdown note embedded inside the JSON hook block reason, not a file write. It does not alter loop state directly. If present, it changes the guidance shown to the agent when exit is blocked for dirty git state.
- gates_or_invariants: The gate invariant is that RLCR work cannot exit to Codex review while the Git tree has uncommitted changes. `.humanize*` artifacts are treated as special untracked local state: the hook still blocks, but this template steers the fix toward `.gitignore` rather than committing `.humanize`. The template path itself is optional at runtime; if loading returns empty, the hook falls back to “Note: .humanize* directories are intentionally untracked.” at `hooks/loop-codex-stop-hook.sh:359-362`.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` from `hooks/loop-codex-stop-hook.sh:359`. The template loader resolves files under `prompt-template` and returns file contents when found at `hooks/lib/template-loader.sh:21-33`. Existence of this template is covered by the template reference scanner, which checks `hooks/loop-codex-stop-hook.sh` among scripts using template functions at `tests/test-template-references.sh:51-64` and extracts template references at `tests/test-template-references.sh:83-90`.
- edge_cases_or_failure_modes: The hook uses `grep -q '\.humanize'`, so any untracked path containing that substring can trigger the local-state note, not just a root `.humanize/` directory. The template recommends appending `.humanize*` to `.gitignore`; repeated application could duplicate ignore lines because the prompt block is instructional, not idempotent automation. If `.humanize` is tracked or modified rather than untracked, this special note is not reached through the `UNTRACKED` path, though the broader dirty git gate still blocks. If the template is missing or empty, the fallback note is less actionable but the gate remains active.
- validation_or_tests: `tests/test-template-references.sh` is designed to prevent missing template references from causing empty block messages at `tests/test-template-references.sh:3-10`; it scans `hooks/loop-codex-stop-hook.sh` for template calls at `tests/test-template-references.sh:57-64`. `tests/test-templates-comprehensive.sh` loads all Markdown templates under `prompt-template` and fails empty load cases at `tests/test-templates-comprehensive.sh:82-107`. No dedicated assertion for the exact `.humanize*` text was found in the inspected excerpts.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-065 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: The standard Codex review prompt for non-full-alignment RLCR rounds. It defines the review contract that decides whether Claude’s implementation can terminate the loop or must continue with findings.
- algorithmic_behavior: The template instructs Codex to first read the original plan at `@{{PLAN_FILE}}`, compare the plan and `@{{PROMPT_FILE}}` against Claude’s claimed summary, and conduct a skeptical implementation review at `prompt-template/codex/regular-review.md:1-23`. It rejects deferrals as incomplete and asks Codex to turn deferred or unfinished tasks into definitive implementation plans at `prompt-template/codex/regular-review.md:24-31`. It then mandates a goal alignment check against `@{{GOAL_TRACKER_FILE}}`, including acceptance criteria progress, forgotten items, unjustified deferrals, and plan evolution validity at `prompt-template/codex/regular-review.md:33-45`. The output contract is explicit: write review comments to `@{{REVIEW_RESULT_FILE}}` if claims do not match reality or pending work remains, and only place `COMPLETE` as the final line when all original tasks and acceptance criteria are done with no deferrals or pending work at `prompt-template/codex/regular-review.md:49-57`.
- inputs_outputs_state: Inputs are template variables: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`. The stop hook sets `PROMPT_FILE`, `REVIEW_PROMPT_FILE`, and `REVIEW_RESULT_FILE` at `hooks/loop-codex-stop-hook.sh:547-549`, reads the summary into `SUMMARY_CONTENT` at `hooks/loop-codex-stop-hook.sh:551`, renders a goal-tracker update section at `hooks/loop-codex-stop-hook.sh:553-557`, and renders `regular-review.md` for non-alignment rounds at `hooks/loop-codex-stop-hook.sh:609-624`. Output is the rendered `round-N-review-prompt.md`, which is piped to `codex exec`; Codex is expected to create or stdout the review result file. The hook copies stdout to `REVIEW_RESULT_FILE` if Codex did not create it but stdout has content at `hooks/loop-codex-stop-hook.sh:773-789`.
- gates_or_invariants: This template enforces the regular-round acceptance gate: no `COMPLETE` unless all original plan tasks are fully completed, all acceptance criteria are met, and there are no deferred or unfinished tasks. It also makes the final-line `COMPLETE` token the loop termination signal. The stop hook independently validates that `REVIEW_RESULT_FILE` exists and is non-empty at `hooks/loop-codex-stop-hook.sh:791-826`, then reads the last non-empty line and only accepts exactly `COMPLETE` or `STOP` after trimming whitespace at `hooks/loop-codex-stop-hook.sh:828-846`. For normal regular rounds, anything other than final-line `COMPLETE` or `STOP` advances the loop by incrementing `current_round` and creating the next prompt at `hooks/loop-codex-stop-hook.sh:878-947`.
- dependencies_and_callers: Called only on non-full-alignment rounds; every fifth review round where `CURRENT_ROUND % 5 == 4` uses `prompt-template/codex/full-alignment-review.md` instead at `hooks/loop-codex-stop-hook.sh:559-563` and `hooks/loop-codex-stop-hook.sh:594-624`. Rendering uses `load_and_render_safe`, which substitutes variables in a single pass to prevent placeholder injection from variable values at `hooks/lib/template-loader.sh:35-43` and `hooks/lib/template-loader.sh:155-188`. The rendered prompt is sent to Codex with model/effort from state through `codex exec`, rooted at `PROJECT_ROOT`, with timeout protection at `hooks/loop-codex-stop-hook.sh:689-716`.
- edge_cases_or_failure_modes: If the template file is missing or renders empty, the hook uses a much shorter fallback prompt at `hooks/loop-codex-stop-hook.sh:583-592`; this keeps the loop functional but loses the detailed regular-review contract. If `SUMMARY_CONTENT` contains placeholder-like text, template rendering is single-pass, so those embedded placeholders are not recursively expanded. If Codex writes `COMPLETE` anywhere except the last non-empty line, the hook does not accept completion. The prompt itself tells Codex to write to `@{{REVIEW_RESULT_FILE}}`, but hook logic tolerates stdout output by copying stdout into that file. The template treats any deferral as incomplete, which is stricter than the start-command README wording that says completion can occur when all ACs are met or validly deferred at `commands/start-rlcr-loop.md:51-55`; the regular-review template is the stricter operational gate for this file.
- validation_or_tests: `tests/test-template-references.sh` checks that shell template references exist, including `hooks/loop-codex-stop-hook.sh` at `tests/test-template-references.sh:57-64`. `tests/test-templates-comprehensive.sh` verifies the template directory structure, loads all `prompt-template/**/*.md` files, and validates non-empty template content at `tests/test-templates-comprehensive.sh:61-107`. Template loader tests also cover variable substitution generally; reference search showed placeholder-oriented tests in `tests/test-template-loader.sh`, including `PLAN_FILE` and `GOAL_TRACKER_FILE` substitutions. I did not run tests because the assignment required read-only research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 unique Item Evidence sections present; exact item identifiers are only used in their section headings to preserve single-occurrence evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`