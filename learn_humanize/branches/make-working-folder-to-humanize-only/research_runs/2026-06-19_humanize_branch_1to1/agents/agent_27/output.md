# agent_27 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-027 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable black-box specification for `/humanize:start-rlcr-loop` setup validation. The file tests `scripts/setup-rlcr-loop.sh`, which is the command invoked by `commands/start-rlcr-loop.md:12-13`, and focuses on rejecting unsafe or ambiguous plan-file inputs before loop state is created.
- algorithmic_behavior: The script creates isolated temporary repositories with `mktemp -d`, cleans them with a trap, and runs the setup script under many filesystem/git/CLI states. Helpers at `tests/test-plan-file-validation.sh:28-30` record pass/fail/skip counts. `setup_test_repo` at `tests/test-plan-file-validation.sh:36-67` initializes a git repo, commits a baseline, writes `plans/test-plan.md`, and gitignores `plans/`. `mock_codex` at `tests/test-plan-file-validation.sh:69-80` injects a fake `codex` binary only when the real CLI is absent, allowing validation paths to advance past the codex availability gate.
- inputs_outputs_state: Inputs are CLI invocations to `scripts/setup-rlcr-loop.sh` plus controlled repo state: path strings, plan contents, `.gitignore`, git tracking status, branch names, chmod permissions, and codex model arguments. Outputs are terminal `PASS`/`FAIL`/`SKIP` lines, counters at `tests/test-plan-file-validation.sh:587-596`, and final process exit equal to `TESTS_FAILED`. The production setup script, when validation succeeds, would create `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` per `scripts/setup-rlcr-loop.sh:388-572`; this test mostly asserts early failures before that state is reached.
- gates_or_invariants: The test encodes these startup invariants: plan file must be provided as a relative path (`tests/test-plan-file-validation.sh:85-98`; implementation `scripts/setup-rlcr-loop.sh:220-224`); parent directory and file must exist (`100-122`; implementation `252-263`); spaces and shell metacharacters are rejected (`124-183`; implementation `226-241`); symlink plan files are rejected (`185-196`; implementation `246-250`); unreadable/unresolvable directories fail clearly (`198-239`; implementation `265-272`); `../` project escapes are rejected (`241-268`; implementation `273-277`); caller must be inside a git repo with at least one commit (`270-319`; implementation `204-214`); tracked plans require `--track-plan-file`, untracked/gitignored plans must omit it, and tracked plans must be clean (`325-422`; implementation `298-326`); branch names and codex parameters must be YAML-safe (`428-585`; implementation `354-381`); `--plan-file` and positional plan args are mutually exclusive (`514-541`; implementation `181-188`).
- dependencies_and_callers: Depends on Bash, git, `mktemp`, `grep`, `chmod`, `wc`, and either installed `codex` or the mock binary. The direct target is `scripts/setup-rlcr-loop.sh`. The user-facing caller is the slash command in `commands/start-rlcr-loop.md:1-14`. The tested validation gates protect state serialization in `scripts/setup-rlcr-loop.sh:406-419`, where branch, plan path, model, effort, timeout, and tracking mode are written into YAML frontmatter.
- edge_cases_or_failure_modes: Covers absolute paths, missing files, missing parent directories, whitespace in directory or filename, shell metacharacters `; & | $ \` < > ( ) { } [ ] ! # ~ * ?`, symlinks, chmod-based path resolution failures, path traversal outside the project, non-git directories, git repos without commits, tracked/untracked mismatch with `--track-plan-file`, dirty tracked plans, YAML-unsafe branch names containing colon/hash/quotes, invalid codex model/effort characters, and valid codex model names with dots/hyphens. Notable coverage gaps: the header lists “Submodule rejection” at `tests/test-plan-file-validation.sh:8`, and the implementation has a submodule gate at `scripts/setup-rlcr-loop.sh:279-292`, but this file does not include an explicit submodule scenario. The metacharacter loop at `tests/test-plan-file-validation.sh:176-183` records a `FAIL` on first bad result but still records the final aggregate `PASS`, so diagnostics can contain both for that section.
- validation_or_tests: This assigned file is itself the validation test. It asserts exact-ish error substrings such as “relative path”, “not found”, “directory not found”, “cannot contain spaces”, “shell metacharacters”, “symbolic link”, “gitignored”, “tracked in git”, “clean”, “YAML-unsafe”, and “invalid characters”. I inspected it read-only and did not execute it, consistent with the “research notes only / do not modify files” instruction.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-057 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Blocking-message template used by RLCR hook validation when an agent tries to write a round summary outside the active loop directory. It is small, but algorithmically relevant because it communicates the enforced summary-location invariant to the blocked writer.
- algorithmic_behavior: The template content at `prompt-template/block/wrong-summary-location.md:1-5` renders a Markdown block headed “Wrong Summary Location”, states that summaries must be in the loop directory, and substitutes `{{CORRECT_PATH}}` into the “Correct path” line. It has no executable logic or local state; its behavior is determined by the hook and template renderer.
- inputs_outputs_state: Input is one render variable, `CORRECT_PATH`, computed by `hooks/loop-write-validator.sh:119-125` as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`. Output is a rendered stderr message. The hook exits with code `2` after rendering at `hooks/loop-write-validator.sh:124-126`, which makes this template part of a blocking state transition: attempted invalid `Write` action becomes rejected tool use with corrective path guidance.
- gates_or_invariants: The gate fires only for `Write` tool calls (`hooks/loop-write-validator.sh:23-28`) whose path is recognized as a summary filename by `is_round_file_type` (`hooks/lib/loop-common.sh:65-72`) and is not inside `.humanize/rlcr/` per `is_in_humanize_loop_dir` (`hooks/lib/loop-common.sh:149-153`). The active loop and current round are resolved first (`hooks/loop-write-validator.sh:73-82`); if no active loop exists, the hook exits without blocking (`77-79`). The invariant enforced is: summary files must be written as `round-<current_round>-summary.md` in the newest active loop directory.
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh:119-126`. Rendering is provided by `load_and_render_safe` in `hooks/lib/template-loader.sh:155-188`, using single-pass `{{VAR}}` substitution from `render_template` at `hooks/lib/template-loader.sh:35-117`. The hook loads shared path/round helpers through `hooks/lib/loop-common.sh:11-17`. Template existence is covered generically by `tests/test-template-references.sh:57-64` and `tests/test-template-references.sh:93-107`, which scan hook scripts for referenced templates and fail if files are missing.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline message containing `{{CORRECT_PATH}}` and still renders variables (`hooks/lib/template-loader.sh:164-187`), so blocking still produces useful guidance. If the attempted file is inside `.humanize/rlcr/` but in the wrong loop directory, this template is not used; the later wrong-directory-path gate handles that case at `hooks/loop-write-validator.sh:169-179`. If a user modifies summaries through Bash, a different message path is used: `summary_bash_blocked_message` in `hooks/loop-bash-validator.sh:122-125`. If a filename does not exactly match `round-[0-9]+-summary.md`, it is not classified as a summary by this gate.
- validation_or_tests: The file is validated indirectly by the template-reference test, which confirms every `load_template`, `load_and_render`, and `load_and_render_safe` reference in hook scripts has a real template file. I did not find a dedicated rendering test for this exact template; rendering behavior is covered by the shared template loader tests and by the hook’s fallback behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence sections present, one per assigned row`
- missing_items: `none`
- duplicate_items: `none detected`
- final_worker_status: `complete`