# agent_13 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-013 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. This file is the Claude/plugin command surface: it exposes the command metadata, restricts allowed execution to the setup script, delegates runtime setup to `scripts/setup-rlcr-loop.sh`, and documents the user/agent loop contract. It is not the implementation body itself; it is the entrypoint and behavioral contract for the plan/RLCR algorithm.
- algorithmic_behavior: The command frontmatter declares the user-facing description, accepted argument shape, and tool boundary: `allowed-tools` permits only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` at `commands/start-rlcr-loop.md:2-5`. The command body then executes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at `commands/start-rlcr-loop.md:10-14`. Its documented algorithm is an iterative cycle: implement from the plan, write a summary, attempt exit, have Codex review the summary, continue on issues, and terminate when Codex returns `COMPLETE` at `commands/start-rlcr-loop.md:16-23`. It also introduces the Goal Tracker as the anti-drift mechanism, with immutable goal/acceptance-criteria state and mutable round-by-round task/evolution state at `commands/start-rlcr-loop.md:24-42`.
- inputs_outputs_state: Primary inputs are `<path/to/plan.md>`, `--max N`, `--codex-model MODEL:EFFORT`, `--codex-timeout SECONDS`, and `--push-every-round`, as declared in `commands/start-rlcr-loop.md:3`. The delegated setup script expands those into concrete state: defaults for Codex model, effort, timeout, and max iterations at `scripts/setup-rlcr-loop.sh:17-22`; argument parsing at `scripts/setup-rlcr-loop.sh:97-180`; creation of `.humanize/rlcr/<timestamp>/` at `scripts/setup-rlcr-loop.sh:388-394`; backup copy of the plan to `plan.md` at `scripts/setup-rlcr-loop.sh:396-397`; state frontmatter containing `current_round`, `max_iterations`, Codex settings, push mode, plan path/tracking mode, start branch, and timestamp at `scripts/setup-rlcr-loop.sh:406-419`; initial `goal-tracker.md` at `scripts/setup-rlcr-loop.sh:425-513`; and `round-0-prompt.md` plus `round-0-summary.md` path expectations at `scripts/setup-rlcr-loop.sh:519-572`. The stop hook consumes this state: it finds the active loop, parses `state.md`, and controls iteration with `CURRENT_ROUND`, `MAX_ITERATIONS`, Codex config, and push mode at `hooks/loop-codex-stop-hook.sh:52-95`. It advances the loop by writing the next round back into `state.md` around `hooks/loop-codex-stop-hook.sh:884`.
- gates_or_invariants: The markdown command declares behavioral invariants: summaries must be written before exit, the Goal Tracker must stay current, implementation summaries must include details/files/tests, state-file or cancel-command cheating is forbidden, and Codex review is authoritative for continuation/completion at `commands/start-rlcr-loop.md:43-49`. The delegated setup script enforces stronger gates before state creation: repository must be a Git repo with at least one commit at `scripts/setup-rlcr-loop.sh:204-214`; plan path must be relative, space-free, shell-metacharacter-free, non-symlink, inside the project, outside submodules, and existing at `scripts/setup-rlcr-loop.sh:220-292`; untracked/gitignored plan behavior is required by default, while `--track-plan-file` requires tracked and clean plan state at `scripts/setup-rlcr-loop.sh:298-326`; plan must have at least five lines at `scripts/setup-rlcr-loop.sh:332-340`; `codex` must exist at `scripts/setup-rlcr-loop.sh:342-348`; branch/model/effort values must be YAML-safe at `scripts/setup-rlcr-loop.sh:354-380`. The stop hook adds runtime gates: missing active loop permits exit, corrupted `current_round` ends unexpectedly, missing v1.1.2 state fields blocks with schema guidance, Round 0 Goal Tracker placeholders block exit, max iterations ends the loop, and only a final-line `COMPLETE` from Codex is accepted as successful completion (`hooks/loop-codex-stop-hook.sh:31-37`, `hooks/loop-codex-stop-hook.sh:96-120`, `hooks/loop-codex-stop-hook.sh:500-533`, `hooks/loop-codex-stop-hook.sh:831-838`).
- dependencies_and_callers: Called by the `/humanize:start-rlcr-loop` slash-command mechanism described in README examples and command listings (`README.md:61-70`, `README.md:185-191`). Direct executable dependency is `scripts/setup-rlcr-loop.sh`, named by both allowed-tools and the body (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:13`). The loop then depends on hooks, especially `hooks/loop-codex-stop-hook.sh`, which reviews stop attempts, and plan-file/edit/write/bash validators surfaced by references in `tests/test-plan-file-hooks.sh:5-9`. Runtime dependencies include Git, Codex CLI, shell utilities such as `wc`, `grep`, `sed`, and `jq` in the hook paths, plus templates under `prompt-template/codex`, `prompt-template/claude`, and `prompt-template/block`, loaded through `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: Misuse cases include missing plan argument, conflicting explicit and positional plan arguments, absolute paths, paths with spaces or shell metacharacters, symlink plans, missing parent directories, nonexistent files, `../` escape, submodule plans, tracked plan without `--track-plan-file`, untracked plan with `--track-plan-file`, dirty tracked plan, too-short plan, missing Codex binary, YAML-unsafe branch/model/effort, older loop state missing `plan_tracked` or `start_branch`, corrupted numeric `current_round`, uninitialized Goal Tracker placeholders in Round 0, max-iteration exhaustion, and Codex result text that mentions completion but does not put `COMPLETE` as the exact final non-empty line. A notable documentation/implementation edge: `commands/start-rlcr-loop.md:36` says full alignment checks occur at rounds 4, 9, 14, while `hooks/loop-codex-stop-hook.sh:559-563` implements this as `CURRENT_ROUND % 5 == 4`, which matches those examples if Round 0 is counted.
- validation_or_tests: Direct tests exist for the delegated setup behavior in `tests/test-plan-file-validation.sh`, including absolute path rejection, nonexistent file/directory rejection, spaces, metacharacters, symlink/submodule/path escape behavior, tracking-mode rules, and Codex model validation (`tests/test-plan-file-validation.sh:1-12`, `tests/test-plan-file-validation.sh:85-130`, later matches from the same file). Hook/state tests exist in `tests/test-plan-file-hooks.sh`, covering valid state, YAML-quoted `plan_file`, missing `plan_tracked`, plan-file validators, summary and Goal Tracker scenarios (`tests/test-plan-file-hooks.sh:1-9`, `tests/test-plan-file-hooks.sh:75-140`). State exit naming/completion behavior is covered by `tests/test-state-exit-naming.sh` according to references for `complete-state.md`, `maxiter`, and `current_round` cases. I did not execute the test suite because this branch export is read-only and the task requested research notes only; a lightweight `git` verification attempt also failed because this export is not a Git checkout with writable tool caches.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-043 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the RLCR stop hook when changed code/documentation files exceed the configured line-count threshold. It defines the remediation contract presented to the agent/user before the loop may proceed to expensive Codex review or exit.
- algorithmic_behavior: The template renders a block headed `Large Files Detected`, interpolating `{{MAX_LINES}}` and `{{LARGE_FILES}}` at `prompt-template/block/large-files.md:1-4`. It explains why oversized files are unacceptable for maintainability/reviewability/error risk at `prompt-template/block/large-files.md:6-9`. It then branches required remediation by file type: code files must be split into smaller modules, preserve behavior strictly, optionally invoke `code-simplifier`, and maintain module boundaries at `prompt-template/block/large-files.md:13-17`; documentation files must be split into logical sections/chapters, preserve cross-references and narrative flow, and update TOC/navigation at `prompt-template/block/large-files.md:19-23`. The final instruction requires committing changes and attempting exit again at `prompt-template/block/large-files.md:25`.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `MAX_LINES=2000` at `hooks/loop-codex-stop-hook.sh:262`, and `LARGE_FILES`, assembled from `git status --porcelain` entries whose files still exist, match code/doc extensions, and exceed the threshold (`hooks/loop-codex-stop-hook.sh:264-312`). Output is rendered markdown assigned to `REASON` through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` with fallback support at `hooks/loop-codex-stop-hook.sh:314-324`. That reason becomes a JSON block decision with `"decision": "block"` and a system message telling the user to split files at `hooks/loop-codex-stop-hook.sh:326-334`. This template does not mutate state itself; the hook exits after emitting the block JSON, leaving the active loop state unchanged until the user/agent remediates and retries.
- gates_or_invariants: The gate is threshold-based and runs before the Git clean/push and Codex-review phases in the stop hook. Only changed tracked/new files listed by `git status --porcelain` are inspected; deleted files are skipped at `hooks/loop-codex-stop-hook.sh:281-284`. Only recognized code extensions and documentation extensions are considered; other extensions are ignored at `hooks/loop-codex-stop-hook.sh:286-301`. Files block only when `line_count > MAX_LINES`, not `>=`, at `hooks/loop-codex-stop-hook.sh:303-307`. The template’s invariant is behavior-preserving decomposition: code functionality must remain strictly unchanged after splitting (`prompt-template/block/large-files.md:14-16`), docs must preserve references/flow (`prompt-template/block/large-files.md:20-23`), and each resulting file should be under the same max-line limit.
- dependencies_and_callers: Sole direct caller found is `hooks/loop-codex-stop-hook.sh`, which loads it through the shared template loader (`hooks/loop-codex-stop-hook.sh:322-324`). Rendering uses `load_and_render_safe` from `hooks/lib/template-loader.sh`; that function loads the file, performs single-pass `{{VAR}}` substitution, and falls back if the template is missing or empty (`hooks/lib/template-loader.sh:152-188`). The template also names an optional external/local capability, `code-simplifier`, and gives invocation forms (`prompt-template/block/large-files.md:16`), but that plugin is advisory rather than required for the hook decision.
- edge_cases_or_failure_modes: If `git` is unavailable or the current directory is not a Git repository, the large-file gate is skipped entirely because the hook wraps it in a Git availability/repo check (`hooks/loop-codex-stop-hook.sh:264`). If a file exceeds 2000 lines but has an unrecognized extension, it will not be included. If a changed file path appears in Git rename form, the hook uses the destination path (`hooks/loop-codex-stop-hook.sh:276-279`). If a file is exactly 2000 lines, it passes because the comparison is strictly greater than the limit. If template rendering fails or the file is missing, the fallback still blocks with a shorter large-file message (`hooks/loop-codex-stop-hook.sh:315-324`). Because `LARGE_FILES` is built from status output and line counts, unusual path formats may be constrained by Git porcelain parsing; the code uses `${line#???}` and does not implement full porcelain `-z` parsing, so paths with unusual characters could be fragile even though earlier setup strongly rejects spaces/metacharacters for plan paths, not general changed files.
- validation_or_tests: Reference search found direct coverage of the template loader infrastructure and template-reference integrity in `tests/test-templates-comprehensive.sh` and `tests/test-template-references.sh`, but no dedicated large-file behavioral test in `tests/`. The large-file block is indirectly validated by the hook path that loads it and emits JSON. Relevant observable implementation lines are `hooks/loop-codex-stop-hook.sh:257-335`. I did not run tests because the instruction was research-only in a read-only export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 evidence sections; both assigned rows represented once in `## Item Evidence`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`