# agent_29 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-029 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, the RLCR prompt-template loading/rendering library. The test asserts that hook prompts can be loaded from `prompt-template/`, rendered with `{{VAR}}` substitutions, and protected against shell metacharacter interpretation and placeholder injection.
- algorithmic_behavior: The script enables strict Bash mode with `set -euo pipefail`, resolves `PROJECT_ROOT` from its own path, sources `hooks/lib/template-loader.sh`, and runs 36 ordered assertions with `pass`/`fail` counters. Key sections are path resolution and sourcing at `tests/test-template-loader.sh:8-12`, helper/counter functions at `tests/test-template-loader.sh:20-34`, base loader/render tests at `tests/test-template-loader.sh:41-244`, shell metacharacter tests at `tests/test-template-loader.sh:246-486`, placeholder-injection tests at `tests/test-template-loader.sh:488-543`, and final exit gating at `tests/test-template-loader.sh:545-563`.
- inputs_outputs_state: Inputs are the repository-local template directory, concrete template names, inline template strings, and `VAR=value` render assignments. Outputs are console PASS/FAIL lines, incremented `TESTS_PASSED`/`TESTS_FAILED`, and process exit `0` only when `TESTS_FAILED` is zero. The test validates `get_template_dir` maps `hooks/lib` to `prompt-template` at `tests/test-template-loader.sh:44-52`; `load_template` returns real content for `block/git-push.md` and empty content for missing templates at `tests/test-template-loader.sh:57-78`; `render_template` maps placeholders to values at `tests/test-template-loader.sh:83-144`; `load_and_render` composes load plus render against `block/wrong-round-number.md` at `tests/test-template-loader.sh:149-164`; `load_and_render_safe` chooses fallback or real template at `tests/test-template-loader.sh:200-221`; and `validate_template_dir` accepts/rejects required directory structure at `tests/test-template-loader.sh:227-244`.
- gates_or_invariants: The test encodes these invariants: missing templates are non-fatal and produce empty/fallback output; unused render variables are ignored; unreplaced placeholders remain literal; shell metacharacters in values are rendered literally, including `&`, backslash, `$`, backticks, pipes, semicolons, globs, redirections, quotes, hashes, tildes, JSON, regex, and multiline content; injected `{{VAR}}` sequences inside values are not rescanned or replaced. The injection invariants are explicit at `tests/test-template-loader.sh:491-492` and exercised at `tests/test-template-loader.sh:499-543`.
- dependencies_and_callers: The direct dependency is `hooks/lib/template-loader.sh`, sourced at `tests/test-template-loader.sh:12`. The functions under test are implemented in `hooks/lib/template-loader.sh`: `get_template_dir` at `hooks/lib/template-loader.sh:11-16`, `load_template` at `hooks/lib/template-loader.sh:21-33`, `render_template` at `hooks/lib/template-loader.sh:43-117`, `load_and_render` at `hooks/lib/template-loader.sh:121-132`, `load_and_render_safe` at `hooks/lib/template-loader.sh:155-188`, and `validate_template_dir` at `hooks/lib/template-loader.sh:193-207`. Real template dependencies include `prompt-template/block/git-push.md` and `prompt-template/block/wrong-round-number.md`; structural validation requires `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude` per `hooks/lib/template-loader.sh:201`.
- edge_cases_or_failure_modes: The core failure modes covered are missing template files, missing template directories, missing required subdirectories, empty render output triggering fallback, no variable assignments, variables not present in templates, placeholders without supplied values, shell metacharacter preservation, multiline values, and placeholder-injection prevention. A notable implementation-sensitive edge is `render_template`’s use of an `awk` here-string at `hooks/lib/template-loader.sh:59-114`; in this read-only sandbox, that here-string could not create a temporary file. Another edge surfaced by the run is zero render assignments under `set -u`, where `env_vars[@]` became an unbound variable in the active Bash environment when `load_and_render_safe` rendered an existing template without variables.
- validation_or_tests: I ran `bash tests/test-template-loader.sh` from the branch export. Result: 5 passed, 31 failed. The failures were dominated by sandbox/runtime restrictions: `hooks/lib/template-loader.sh: line 114: cannot create temp file for here document: Operation not permitted`, which prevents `render_template` from receiving content through the here-string. Test 12 additionally reported `env_vars[@]: unbound variable` for the no-variable render path. No files were modified.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-059 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Claude next-round prompt footer template. It defines the closing contract appended to a blocked RLCR loop continuation prompt: do not escape the loop through prohibited actions, optionally run code simplification, commit work, and write the next round summary to the rendered summary path.
- algorithmic_behavior: The template is static Markdown with one placeholder, `{{NEXT_SUMMARY_FILE}}`, at `prompt-template/claude/next-round-footer.md:9`. It starts with a separator at `prompt-template/claude/next-round-footer.md:2`, states the loop-escape prohibition at `prompt-template/claude/next-round-footer.md:4`, and lists completion steps at `prompt-template/claude/next-round-footer.md:6-9`. It is not executable by itself; its behavior comes from rendering through `load_and_render_safe` and appending to the generated next-round prompt.
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, computed by the stop hook as `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` at `hooks/loop-codex-stop-hook.sh:888-890`. Output is appended prompt text written into `NEXT_PROMPT_FILE` at `hooks/loop-codex-stop-hook.sh:916-917`. The state transition it participates in is review-found-issues continuation: the hook updates `current_round` in the state file at `hooks/loop-codex-stop-hook.sh:882-885`, creates the next prompt and summary paths at `hooks/loop-codex-stop-hook.sh:887-890`, writes the next-round prompt body at `hooks/loop-codex-stop-hook.sh:900-903`, appends this footer at `hooks/loop-codex-stop-hook.sh:913-917`, then returns a JSON block decision with the generated prompt as `reason` at `hooks/loop-codex-stop-hook.sh:939-946`.
- gates_or_invariants: The footer enforces a behavioral invariant for the agent: it must not exit the RLCR loop by lying, editing loop state, or running `cancel-rlcr-loop` at `prompt-template/claude/next-round-footer.md:4`. It also requires durable completion artifacts: a descriptive commit at line 8 and a summary written to the rendered `@{{NEXT_SUMMARY_FILE}}` path at line 9. The hook has a fallback footer, `"Commit your changes and write summary to {{NEXT_SUMMARY_FILE}}"`, at `hooks/loop-codex-stop-hook.sh:914-917`, so the commit/summary gate persists even if this template is missing.
- dependencies_and_callers: The only direct caller found is `hooks/loop-codex-stop-hook.sh:916`, which renders `claude/next-round-footer.md` through `load_and_render_safe`. That function depends on `hooks/lib/template-loader.sh:155-188`, which loads the template, substitutes `NEXT_SUMMARY_FILE`, and falls back if the template is missing or renders empty. The footer is sequenced after `prompt-template/claude/next-round-prompt.md` and optional `post-alignment-action-items.md`, and before optional `push-every-round-note.md` and `goal-tracker-update-request.md` in `hooks/loop-codex-stop-hook.sh:891-933`.
- edge_cases_or_failure_modes: If the template is missing or empty, the hook uses the fallback footer and still includes the summary path. If `NEXT_SUMMARY_FILE` is not substituted, the prompt would contain a literal placeholder and weaken the summary-location contract. Because the template includes `@{{NEXT_SUMMARY_FILE}}`, malformed paths or unescaped special characters in `NEXT_SUMMARY_FILE` are handled by the loader’s single-pass literal substitution model, but rendering can fail in environments where the loader’s here-string cannot create temporary files, as seen during the test run for the assigned test item.
- validation_or_tests: This exact template is not directly asserted by `tests/test-template-loader.sh`, but the rendering path it uses is covered by the loader tests for `load_and_render_safe`, real-template rendering, fallback behavior, multiline content, and placeholder-injection prevention. Direct reference search found one call site: `hooks/loop-codex-stop-hook.sh:916`. File inspection confirms the placeholder appears exactly once at `prompt-template/claude/next-round-footer.md:9`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 unique item evidence headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`