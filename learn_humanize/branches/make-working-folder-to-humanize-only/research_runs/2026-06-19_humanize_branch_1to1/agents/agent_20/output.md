# agent_20 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-020 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Claude `Read` calls in the RLCR loop. It is the read-side gate that prevents Claude from consuming stale round prompt/summary files, round files outside the active `.humanize/rlcr` session, and file-based round todos. It is registered under the `Read` matcher in [hooks/hooks.json:33-39](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/hooks.json:33).
- algorithmic_behavior: The hook reads JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits allow-mode for non-`Read` tools ([hooks/loop-read-validator.sh:22-30](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-read-validator.sh:22)). It lowercases the path for suffix matching, blocks any `round-N-todos.md` read immediately, then only applies deeper validation to `round-N-summary.md` and `round-N-prompt.md` files ([hooks/loop-read-validator.sh:36-47](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-read-validator.sh:36)). For prompt/summary files, it finds the active loop, reads `current_round`, extracts the requested round from the filename, derives file type, and enforces location, round number, and exact active-directory path checks ([hooks/loop-read-validator.sh:56-135](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-read-validator.sh:56)).
- inputs_outputs_state: Inputs are hook stdin JSON, `CLAUDE_PROJECT_DIR` or `pwd`, `FILE_PATH`, `.humanize/rlcr/<timestamp>/state.md`, and templates under `prompt-template`. Outputs are process exit status plus stderr block messages; exit `0` allows the read and exit `2` blocks it. The script does not mutate RLCR state; it reads active-loop state through `find_active_loop` and `get_current_round` from [hooks/lib/loop-common.sh:23-58](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:23).
- gates_or_invariants: `round-*-todos.md` files are never readable through filesystem `Read`; agents must use native todo tooling. Prompt and summary reads are allowed only when the file is in the active loop directory, its embedded round equals `current_round`, and the literal path equals `$ACTIVE_LOOP_DIR/<basename>` ([hooks/loop-read-validator.sh:87-132](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-read-validator.sh:87)). If no active loop is found, prompt/summary validation is inert and the read is allowed, while the todos-file block still applies because it runs earlier.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for lowercase conversion, round-file matching, active-loop discovery, round extraction, `.humanize/rlcr` membership, and todos block rendering ([hooks/lib/loop-common.sh:60-95](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:60), [hooks/lib/loop-common.sh:149-153](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:149)). That library depends on `hooks/lib/template-loader.sh`, whose safe rendering path falls back to inline messages when a template is missing or renders empty ([hooks/lib/template-loader.sh:155-188](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/template-loader.sh:155)). The validator emits `wrong-file-location`, `wrong-round-file`, and `wrong-directory-path` block templates through `load_and_render_safe`.
- edge_cases_or_failure_modes: Missing or malformed `jq` input can terminate the hook because `set -euo pipefail` is active. `find_active_loop` intentionally considers only the newest loop directory; if the newest directory lacks `state.md`, older state files are ignored and prompt/summary reads are allowed as if no active loop exists. The final exact string comparison can block legitimate non-canonical paths, symlinked paths, or relative paths if the hook input does not match the active loop path format. The `.humanize/rlcr` membership check is substring-based, but the later exact-path check prevents cross-session or cross-project prompt/summary reads when an active loop exists.
- validation_or_tests: Template-reference coverage includes this script in the scanned validator set ([tests/test-template-references.sh:56-64](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:56)) and checks that critical validators use `load_and_render_safe` rather than unsafe rendering ([tests/test-template-references.sh:172-200](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:172)). I found template existence validation but no direct behavioral test matrix for the read validator’s allow/block cases.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-050 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Static block-message template for the RLCR invariant that `round-*-todos.md` files must not be created, read, edited, or modified as filesystem artifacts. It is not executable logic, but it is the user-facing contract emitted by the validator gate.
- algorithmic_behavior: The template contains no variables or branching. When rendered, it tells the agent not to create or access `round-*-todos.md` files and to use the native `TodoWrite` tool instead ([prompt-template/block/todos-file-access.md:1-8](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/prompt-template/block/todos-file-access.md:1)). The effective transition is validator-driven: a matching attempted access produces this message on stderr and the hook exits blocked.
- inputs_outputs_state: Inputs are only the template loader request from `todos_blocked_message`; there are no placeholders or runtime substitutions. Output is markdown text. The template carries no persistent state and does not read or write any RLCR files.
- gates_or_invariants: The explicit invariant is that round todo state must live in Claude Code’s native todo system, not in `round-*-todos.md` files. The rationale in the template is that native todo tools provide visible state tracking and task-management integration ([prompt-template/block/todos-file-access.md:5-8](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/prompt-template/block/todos-file-access.md:5)).
- dependencies_and_callers: Loaded by `todos_blocked_message` in [hooks/lib/loop-common.sh:86-95](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:86), which provides an inline fallback with the same core directive if the template is missing. The assigned read validator calls that helper before active-loop discovery when the requested file path matches `round-N-todos.md` ([hooks/loop-read-validator.sh:36-39](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-read-validator.sh:36)). Repository search also shows the same helper is used by write/edit/bash validators, so this one template centralizes the todos-file block across access modes.
- edge_cases_or_failure_modes: Because the template is static, rendering cannot fail due to missing variables. If the file is removed or empty, `load_and_render_safe` falls back to a shorter inline message, so the block still has a usable explanation. A content regression here would not change the validator’s exit code, but it could weaken or confuse the recovery instruction shown to the agent.
- validation_or_tests: `tests/test-template-references.sh` explicitly lists `block/todos-file-access.md` as a required common template and fails if it is absent ([tests/test-template-references.sh:149-166](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:149)). The same test scans shell scripts for template references and verifies referenced template files exist ([tests/test-template-references.sh:51-115](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:51)).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item sections present above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`