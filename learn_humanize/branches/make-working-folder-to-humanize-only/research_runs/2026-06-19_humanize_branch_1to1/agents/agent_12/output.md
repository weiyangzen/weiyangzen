# agent_12 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-012 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: User-facing slash-command workflow for terminating the active RLCR loop without deleting evidence. It defines the manual cancellation algorithm: locate the newest loop directory under `.humanize/rlcr/`, verify it is active by the presence of `state.md`, then rename that state file to `cancel-state.md`. The command is hidden from slash-command tooling metadata but remains the documented user cancellation path; see `commands/cancel-rlcr-loop.md:1-5`, `commands/cancel-rlcr-loop.md:7-35`.
- algorithmic_behavior: The command treats the newest timestamped directory as the only candidate loop. It computes `LOOP_DIR=$(ls -1d .humanize/rlcr/*/ 2>/dev/null | sort -r | head -1)` and prints `NONE` when no loop directory exists, then checks `"${LOOP_DIR}state.md"` before any mutation; see `commands/cancel-rlcr-loop.md:11-24`. If active, it reads state for the current round and max iteration metadata, renames `state.md` to `cancel-state.md`, and reports cancellation with preserved state; see `commands/cancel-rlcr-loop.md:28-31`.
- inputs_outputs_state: Input is the filesystem state under `.humanize/rlcr/*/`, specifically timestamped loop directories and the active `state.md`. Output is either a no-active-loop message or a filesystem state transition from `.humanize/rlcr/<newest>/state.md` to `.humanize/rlcr/<newest>/cancel-state.md`. The loop’s summaries, prompts, review results, and state remain in place for later inspection; see `commands/cancel-rlcr-loop.md:33-35`. This matches the README’s state model: active loops are controlled solely by `state.md` in the newest loop directory, and cancellation is implemented by renaming it to `cancel-state.md`; see `README.md:112-129`.
- gates_or_invariants: The central invariant is that only `state.md` means active; renamed terminal files are preserved but not active. The command explicitly says the current loop is always the newest timestamp and an active loop requires `state.md`; see `commands/cancel-rlcr-loop.md:33`. Shared hook logic reinforces this with `find_active_loop`, which inspects only the newest directory and returns it only if `state.md` exists; see `hooks/lib/loop-common.sh:23-44`. There is also a behavioral boundary: normal loop participants are told not to escape by editing state files or running cancellation themselves; see `commands/start-rlcr-loop.md:43-55`, `scripts/setup-rlcr-loop.sh:563-566`, and `prompt-template/claude/next-round-footer.md:1-5`.
- dependencies_and_callers: The command depends on shell primitives allowed in its frontmatter: listing loop directories, reading `state.md`, and moving `state.md` to `cancel-state.md`; see `commands/cancel-rlcr-loop.md:3`. It is referenced as the user cancellation path in the start command (`commands/start-rlcr-loop.md:51-55`), setup output (`scripts/setup-rlcr-loop.sh:598-604`), and README command table/state documentation (`README.md:120-127`, `README.md:255-258`). It aligns with the shared `end_loop` helper, which supports `cancel` as one of the valid terminal reasons and renames `state.md` to `<reason>-state.md`; see `hooks/lib/loop-common.sh:199-232`.
- edge_cases_or_failure_modes: If no `.humanize/rlcr/*/` directory exists, the command reports no active loop; see `commands/cancel-rlcr-loop.md:18`. If the newest loop directory exists but lacks `state.md`, it also reports no active loop and does not inspect older directories, preventing older loops from being revived; see `commands/cancel-rlcr-loop.md:20-27` and `hooks/lib/loop-common.sh:23-44`. If `LOOP_DIR` is empty but later steps are executed anyway, `"${LOOP_DIR}state.md"` resolves to `state.md` in the current directory, so the numbered workflow depends on the agent honoring the `If NONE` gate before continuing. The direct `mv` also overwrites only according to normal shell behavior; there is no explicit collision guard if `cancel-state.md` already exists in the newest directory.
- validation_or_tests: State-exit tests cover the same active-loop naming contract: `cancel-state.md` must not be detected as active, while `state.md` must be detected; see `tests/test-state-exit-naming.sh:38-108`. The same test verifies `end_loop` rejects invalid reasons and creates the expected terminal state file for valid reasons including `cancel`; see `tests/test-state-exit-naming.sh:182-225`. `hooks/loop-bash-validator.sh` protects ordinary Bash attempts to modify `state.md` during active loops, making this explicit cancel command a controlled exception rather than general write permission; see `hooks/loop-bash-validator.sh:67-75`. I inspected tests and callers only; I did not execute tests because this assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-042 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Prompt block template for the RLCR stop hook’s early incomplete-todo gate. It is not the todo parser itself; it is the rendered blocking message shown when the latest TodoWrite state still contains unfinished work. The template tells the agent it is trying to stop with incomplete todos, inserts `{{INCOMPLETE_LIST}}`, and requires completion through TodoWrite before summary/stop/review; see `prompt-template/block/incomplete-todos.md:1-12`.
- algorithmic_behavior: The stop hook runs this gate before invoking expensive Codex review. It calls `hooks/check-todos-from-transcript.py`, captures exit code `1` as “incomplete todos found,” extracts the list after the marker line, renders `block/incomplete-todos.md`, then returns a JSON decision with `"decision": "block"` and a system message; see `hooks/loop-codex-stop-hook.sh:220-253`. The template’s functional role is to convert parser output into a blocking instruction: complete all todos, mark each as completed using TodoWrite, and only then write the summary and stop; see `prompt-template/block/incomplete-todos.md:7-12`.
- inputs_outputs_state: Input to the template is `INCOMPLETE_LIST`, supplied by the stop hook from the todo checker output; see `hooks/loop-codex-stop-hook.sh:233-242`. The upstream checker reads hook JSON from stdin, uses `transcript_path`, scans the transcript for the most recent TodoWrite tool call across supported transcript shapes, and emits incomplete entries formatted as `- [status] content`; see `hooks/check-todos-from-transcript.py:87-122`. Output is a rendered Markdown reason embedded in a JSON block response from the stop hook, not a file mutation; see `hooks/loop-codex-stop-hook.sh:244-252`.
- gates_or_invariants: The invariant is that RLCR cannot proceed to Codex review while TodoWrite has any status other than `completed`. The checker treats no transcript, invalid hook JSON, missing transcript path, missing transcript file, or no TodoWrite state as pass-through/no block; see `hooks/check-todos-from-transcript.py:22-23`, `hooks/check-todos-from-transcript.py:87-107`. For found todos, any `status != "completed"` is incomplete; see `hooks/check-todos-from-transcript.py:109-122`. The larger system also blocks `round-*-todos.md` file access and directs agents to native TodoWrite instead, keeping the state source in the transcript/UI rather than ad hoc files; see `hooks/lib/loop-common.sh:86-95`, `hooks/loop-bash-validator.sh:128-135`, and `prompt-template/block/todos-file-access.md:1-8`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, via `load_and_render_safe "$TEMPLATE_DIR" "block/incomplete-todos.md"`; see `hooks/loop-codex-stop-hook.sh:236-242`. Rendering depends on `hooks/lib/template-loader.sh`, whose `render_template` performs single-pass `{{VAR}}` substitution to avoid placeholder injection, and `load_and_render_safe`, which falls back to an inline message if the template is missing or empty; see `hooks/lib/template-loader.sh:35-117` and `hooks/lib/template-loader.sh:152-180`. The checker dependency is `hooks/check-todos-from-transcript.py`; see `hooks/loop-codex-stop-hook.sh:224-229`.
- edge_cases_or_failure_modes: If `hooks/check-todos-from-transcript.py` is absent, the stop hook skips this gate entirely because the block is inside `if [[ -f "$TODO_CHECKER" ]]`; see `hooks/loop-codex-stop-hook.sh:224-254`. If the transcript JSON is malformed or absent, the checker exits successfully and allows review; see `hooks/check-todos-from-transcript.py:87-107`. If a TodoWrite entry lacks `status`, it is treated as incomplete because empty status differs from `completed`; see `hooks/check-todos-from-transcript.py:111-115`. The hook captures checker stderr/stdout together (`2>&1`), so unexpected checker diagnostics on failure could be folded into `INCOMPLETE_LIST`; see `hooks/loop-codex-stop-hook.sh:227-235`. Template rendering is single-pass, so todo text containing `{{...}}` is preserved rather than recursively expanded; see `hooks/lib/template-loader.sh:39-42`.
- validation_or_tests: Existing references show this template is part of the stop-hook path, but the template-reference test’s common-template list does not include `block/incomplete-todos.md`, so coverage appears indirect through hook execution rather than explicit cross-reference enumeration; see `tests/test-template-references.sh:149-167`. The parser has clear internal behavior for three transcript formats: assistant-message tool use, alternative message format, and direct tool-use entry; see `hooks/check-todos-from-transcript.py:38-83`. I inspected the implementation and references only; I did not execute tests because this assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-012`, `MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-042`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`