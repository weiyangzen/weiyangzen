# agent_22 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell utility entrypoint for live RLCR loop monitoring. It does not start, mutate, or validate loop execution; it reads loop/cache artifacts and renders a terminal dashboard around the newest Codex run log. The exported command is `humanize`, with the supported route `humanize monitor rlcr-loop` dispatching to `_humanize_monitor_codex` at `scripts/humanize.sh:521` and `scripts/humanize.sh:529`.
- algorithmic_behavior: `_humanize_monitor_codex` first requires `.humanize/rlcr` in the current project and fails early if absent (`scripts/humanize.sh:10`, `scripts/humanize.sh:16`). It discovers the latest session by lexicographically comparing timestamp-shaped directories matching `YYYY-MM-DD_HH-MM-SS` (`scripts/humanize.sh:23`, `scripts/humanize.sh:34`). It then discovers the newest Codex log by mapping the current project path to a sanitized `$HOME/.cache/humanize/<project>/<session>/` cache path, scanning `round-*-codex-run.log`, and ordering by session timestamp then round number (`scripts/humanize.sh:43`, `scripts/humanize.sh:49`, `scripts/humanize.sh:53`, `scripts/humanize.sh:75`, `scripts/humanize.sh:84`). Once a log is selected, it reserves a top terminal status region, tails the last 50 log lines, streams appended bytes by file-size offsets, and switches automatically when a newer log appears (`scripts/humanize.sh:383`, `scripts/humanize.sh:444`, `scripts/humanize.sh:456`, `scripts/humanize.sh:471`, `scripts/humanize.sh:484`, `scripts/humanize.sh:499`).
- inputs_outputs_state: Inputs are the caller’s current working directory, local `.humanize/rlcr/<timestamp>/` session directories, each session’s `state.md` and `goal-tracker.md`, git repository status, terminal dimensions, and cache logs under `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/`. Parsed `state.md` fields are `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` (`scripts/humanize.sh:98`, `scripts/humanize.sh:106`). Parsed goal-tracker signals include acceptance criteria counts, active/completed/deferred task counts, open issue count, and goal summary (`scripts/humanize.sh:116`, `scripts/humanize.sh:137`, `scripts/humanize.sh:150`, `scripts/humanize.sh:174`, `scripts/humanize.sh:178`, `scripts/humanize.sh:184`, `scripts/humanize.sh:188`, `scripts/humanize.sh:191`). Output is terminal UI only: a fixed status bar plus live log stream (`scripts/humanize.sh:262`, `scripts/humanize.sh:339`, `scripts/humanize.sh:352`, `scripts/humanize.sh:358`, `scripts/humanize.sh:372`). Internal runtime state is local shell variables: `current_file`, `current_session_dir`, `tail_pid`, `monitor_running`, `cleanup_done`, `last_size`, and `file_size` (`scripts/humanize.sh:11`, `scripts/humanize.sh:401`, `scripts/humanize.sh:452`).
- gates_or_invariants: The monitor only runs inside a project with `.humanize/rlcr`; otherwise it returns nonzero with a human-readable error (`scripts/humanize.sh:16`). Session directory names must match the timestamp regex before they are considered (`scripts/humanize.sh:34`, `scripts/humanize.sh:65`). Cache lookup must use the same project-path sanitization convention documented in `loop-codex-stop-hook.sh`, replacing non-safe characters with `-` and collapsing repeated hyphens (`scripts/humanize.sh:51`, `scripts/humanize.sh:54`; corresponding cache creation is in `hooks/loop-codex-stop-hook.sh:655` and `hooks/loop-codex-stop-hook.sh:661`). The shell compatibility invariant is explicit: it supports bash and zsh, using `setopt localoptions ksharrays` for zsh array indexing and a custom `_split_to_array` for pipe-delimited parser output (`scripts/humanize.sh:4`, `scripts/humanize.sh:248`, `scripts/humanize.sh:264`). Terminal state is restored on interrupt or termination through `_cleanup` and traps (`scripts/humanize.sh:393`, `scripts/humanize.sh:406`, `scripts/humanize.sh:427`).
- dependencies_and_callers: The README instructs users to source this file from `.bashrc` or `.zshrc` and run `humanize monitor rlcr-loop` in the project directory (`README.md:90`, `README.md:95`, `README.md:100`, `README.md:103`). The monitored cache path is produced by the Codex stop hook, which writes `round-N-codex-run.cmd`, `.out`, and `.log` under `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/` (`hooks/loop-codex-stop-hook.sh:655`, `hooks/loop-codex-stop-hook.sh:662`, `hooks/loop-codex-stop-hook.sh:665`). It depends on standard CLI tools and terminal capabilities: `basename`, `sed`, `grep`, `sort`, `wc`, `git`, `stat`, `tail`, `tput`, `seq`, `kill`, and `sleep`. It has no direct test file found for the monitor behavior, but the repository docs describe the expected artifacts and dashboard fields (`README.md:75`, `README.md:106`).
- edge_cases_or_failure_modes: If `.humanize/rlcr` is missing, monitoring cannot start (`scripts/humanize.sh:17`). If no log exists yet, it waits indefinitely until one appears or a signal flips `monitor_running` (`scripts/humanize.sh:434`, `scripts/humanize.sh:436`). If `state.md` is missing, state display falls back to `N/A` fields (`scripts/humanize.sh:101`). If `goal-tracker.md` is missing, progress display falls back to zero counts and `No goal tracker` (`scripts/humanize.sh:120`). If not in a git repo, git status reports zero counts and `not a git repo` (`scripts/humanize.sh:203`). The parser uses pipe-delimited strings, so unescaped pipe characters in extracted fields such as goal summaries or plan paths can shift array positions during `_split_to_array` (`scripts/humanize.sh:197`, `scripts/humanize.sh:248`). Very narrow terminals can produce negative truncation arithmetic because `max_display_len` is `term_width - 12` and then subtracts again for suffix/prefix lengths (`scripts/humanize.sh:311`). It computes `tail_pid` cleanup state but does not actually background a persistent `tail` process in the shown monitor loop, so cleanup’s `tail_pid` branch is mostly defensive (`scripts/humanize.sh:401`, `scripts/humanize.sh:416`).
- validation_or_tests: No dedicated automated test for `scripts/humanize.sh` was found in repository scans. Related validation is indirect: README documents expected monitor setup and displayed data (`README.md:88`, `README.md:106`), while tests cover RLCR state-directory conventions and active loop semantics that the monitor relies on, including `.humanize/rlcr` and `state.md` behavior (`tests/test-state-exit-naming.sh` was referenced by repository search around state naming and active-loop detection). Practical validation for this file would require creating a synthetic `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, and matching cache log, then running `humanize monitor rlcr-loop` in an interactive terminal and confirming status rendering, byte-offset streaming, and auto-switch to a newer round log.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-052 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the stop-hook gate that prevents an RLCR round from exiting without a required work summary. It is algorithmically small, but core to the review-loop transition contract because the stop hook cannot proceed to Codex review until `round-<current>-summary.md` exists.
- algorithmic_behavior: The template renders a concise corrective instruction: the agent attempted to exit without writing the work summary, must write it to `{{SUMMARY_FILE}}`, should include implementation, changed files, tests, and remaining items, then may attempt exit again (`prompt-template/block/work-summary-missing.md:3`, `prompt-template/block/work-summary-missing.md:5`, `prompt-template/block/work-summary-missing.md:10`). It contains one placeholder, `{{SUMMARY_FILE}}`, which is substituted by the template loader’s single-pass renderer (`hooks/lib/template-loader.sh:35`, `hooks/lib/template-loader.sh:39`, `hooks/lib/template-loader.sh:56`, `hooks/lib/template-loader.sh:101`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` value supplied by `hooks/loop-codex-stop-hook.sh`, built as `$LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-codex-stop-hook.sh:438`). Output is rendered Markdown used as the `reason` field in a JSON block decision returned by the stop hook (`hooks/loop-codex-stop-hook.sh:447`, `hooks/loop-codex-stop-hook.sh:450`). The template itself stores no state; the state transition it enforces is external: missing summary keeps the stop hook in a blocked decision instead of proceeding to review-prompt construction and `SUMMARY_CONTENT=$(cat "$SUMMARY_FILE")` (`hooks/loop-codex-stop-hook.sh:440`, `hooks/loop-codex-stop-hook.sh:453`, `hooks/loop-codex-stop-hook.sh:551`).
- gates_or_invariants: The invariant is direct: the current round summary file must exist before the stop hook can continue (`hooks/loop-codex-stop-hook.sh:440`). If it does not exist, the hook returns JSON with `"decision": "block"` and a system message identifying the missing round summary (`hooks/loop-codex-stop-hook.sh:450`, `hooks/loop-codex-stop-hook.sh:452`). The template must remain present and nonempty; if it is missing or renders empty, `load_and_render_safe` falls back to a built-in shorter message (`hooks/loop-codex-stop-hook.sh:444`, `hooks/loop-codex-stop-hook.sh:447`; fallback behavior at `hooks/lib/template-loader.sh:152`, `hooks/lib/template-loader.sh:164`, `hooks/lib/template-loader.sh:177`). Single-pass rendering is an invariant of the loader, so injected placeholder-like content inside variable values is not recursively substituted (`hooks/lib/template-loader.sh:39`, `hooks/lib/template-loader.sh:102`).
- dependencies_and_callers: The concrete caller is `hooks/loop-codex-stop-hook.sh`, in the “Check Summary File Exists” gate (`hooks/loop-codex-stop-hook.sh:434`). The rendering dependency is `hooks/lib/template-loader.sh`, especially `load_and_render_safe` (`hooks/lib/template-loader.sh:155`). Template existence is checked by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails when a referenced template file is missing (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:57`, `tests/test-template-references.sh:93`, `tests/test-template-references.sh:102`). Repository search also found the same `SUMMARY_FILE` path used by edit/write/bash validators to guide blocked goal-tracker changes toward summary-based update requests, which makes the summary file a shared loop communication surface (`hooks/loop-edit-validator.sh:96`, `hooks/loop-write-validator.sh:110`, `hooks/loop-bash-validator.sh:101`).
- edge_cases_or_failure_modes: If the template file is deleted, renamed, or empty, the hook still blocks using the fallback message, but the agent receives less complete summary guidance (`hooks/loop-codex-stop-hook.sh:444`, `hooks/lib/template-loader.sh:164`). If `SUMMARY_FILE` contains unusual characters or embedded newlines, substitution is performed through environment variables into awk; the loader avoids recursive placeholder substitution but does not semantically validate the path (`hooks/lib/template-loader.sh:49`, `hooks/lib/template-loader.sh:59`). The gate checks only file existence at this point, not content quality or required sections; a blank or low-quality summary can pass this specific gate and be consumed later as `SUMMARY_CONTENT` (`hooks/loop-codex-stop-hook.sh:440`, `hooks/loop-codex-stop-hook.sh:551`). The template text says “After writing the summary, you may attempt to exit again,” so it assumes the surrounding stop-hook loop will re-run after the agent writes the file (`prompt-template/block/work-summary-missing.md:16`).
- validation_or_tests: `tests/test-template-references.sh` validates that referenced template paths exist, including templates loaded from `loop-codex-stop-hook.sh` (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:83`). Broader template rendering behavior is covered by the template-loader tests found in the repository scan (`tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`), and the stop-hook path is exercised in plan-file hook tests that run `hooks/loop-codex-stop-hook.sh` against synthetic `.humanize/rlcr` directories. For this specific file, the key validation target is: with no `round-N-summary.md`, the stop hook returns a block decision whose reason includes the rendered summary path from this template.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`