# agent_14 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-014 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Lightweight Stop-hook gate that prevents the RLCR loop from advancing to expensive Codex review while Claude still has unfinished native TodoWrite tasks. It is invoked from `hooks/loop-codex-stop-hook.sh` before large-file detection and Codex review, at `hooks/loop-codex-stop-hook.sh:218-254`.
- algorithmic_behavior: The script reads JSON hook input from stdin, extracts `transcript_path`, parses that transcript as JSONL, tracks the most recent non-empty TodoWrite payload, and exits according to whether every todo has status `completed`. Main parser entrypoint is `find_latest_todos(transcript_path)` at `hooks/check-todos-from-transcript.py:17-84`; hook entrypoint is `main()` at `hooks/check-todos-from-transcript.py:87-125`.
- inputs_outputs_state: Input is a JSON object on stdin containing `transcript_path`, documented at `hooks/check-todos-from-transcript.py:9-10` and consumed at `hooks/check-todos-from-transcript.py:89-100`. Secondary input is the transcript file, read line-by-line as JSONL at `hooks/check-todos-from-transcript.py:27-36`. Output is process status plus optional stdout: exit `0` means no blocking todos were found or all latest todos are complete; exit `1` prints `INCOMPLETE_TODOS` and one line per incomplete todo at `hooks/check-todos-from-transcript.py:117-122`. Internal state is only `latest_todos`, overwritten whenever a later TodoWrite payload with a non-empty `todos` array is found at `hooks/check-todos-from-transcript.py:25`, `hooks/check-todos-from-transcript.py:59-60`, `hooks/check-todos-from-transcript.py:72-73`, and `hooks/check-todos-from-transcript.py:81-82`.
- gates_or_invariants: The gate checks only the latest TodoWrite list, not all historical todos. A todo is accepted only when `todo["status"] == "completed"`; any other status is incomplete and becomes a blocking line at `hooks/check-todos-from-transcript.py:109-115`. Missing transcript, invalid stdin JSON, empty `transcript_path`, unreadable absent transcript path, malformed JSONL lines, or no TodoWrite entries all resolve to allow/exit `0` at `hooks/check-todos-from-transcript.py:22-23`, `hooks/check-todos-from-transcript.py:33-36`, `hooks/check-todos-from-transcript.py:89-97`, and `hooks/check-todos-from-transcript.py:105-107`.
- dependencies_and_callers: Depends only on Python standard library modules `json`, `sys`, and `pathlib.Path` at `hooks/check-todos-from-transcript.py:12-14`. Direct caller is `hooks/loop-codex-stop-hook.sh`, which sets `TODO_CHECKER="$SCRIPT_DIR/check-todos-from-transcript.py"` and runs `python3 "$TODO_CHECKER"` with the original hook input at `hooks/loop-codex-stop-hook.sh:224-229`. On exit `1`, the stop hook strips the marker line, renders `prompt-template/block/incomplete-todos.md`, and emits a blocking JSON decision at `hooks/loop-codex-stop-hook.sh:231-252`. Hook registration places `loop-codex-stop-hook.sh` under the Claude Stop event with a long timeout in `hooks/hooks.json:52-59`.
- edge_cases_or_failure_modes: The checker is intentionally fail-open for invalid or missing hook metadata, nonexistent transcript files, malformed JSONL entries, and empty TodoWrite arrays. It recognizes three transcript shapes: Claude assistant messages with `message.content` tool blocks at `hooks/check-todos-from-transcript.py:48-61`, alternative message entries with top-level `content` at `hooks/check-todos-from-transcript.py:62-73`, and direct tool_use entries at `hooks/check-todos-from-transcript.py:75-82`. It ignores TodoWrite calls whose `todos` array is empty, so an empty latest write does not erase a previous non-empty state. It formats missing todo fields as blank status/content rather than failing, because it uses `.get()` at `hooks/check-todos-from-transcript.py:111-115`. The module docstring says details go to stderr at `hooks/check-todos-from-transcript.py:6-7`, but implementation prints both marker and details to stdout to avoid buffering order issues at `hooks/check-todos-from-transcript.py:117-121`.
- validation_or_tests: I found no dedicated test file for this Python helper. Integration behavior is partially covered through the Stop hook path: the stop hook checks `TODO_EXIT == 1`, renders `prompt-template/block/incomplete-todos.md`, and blocks at `hooks/loop-codex-stop-hook.sh:231-252`. Related hook inventory lists the helper as part of lifecycle hooks in `README.md:224-232`. The incomplete-todos block itself requires the user to complete todos and mark them completed using TodoWrite at `prompt-template/block/incomplete-todos.md:1-12`.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-044 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Shared prompt-template block for the RLCR plan-integrity gate. It is not executable code, but it defines the user-facing blocking contract when an agent attempts to modify `.humanize/rlcr/.../plan.md`, which is treated as the loop’s backup copy of the original plan.
- algorithmic_behavior: The template states the transition rule directly: the loop-directory `plan.md` is a backup and cannot be modified; agents may read it for reference instead. The whole contract is in `prompt-template/block/plan-backup-protected.md:1-7`, especially the read-only assertion at `prompt-template/block/plan-backup-protected.md:3` and the read-instead instruction at `prompt-template/block/plan-backup-protected.md:7`.
- inputs_outputs_state: Input is indirect: validators choose this template when hook input targets the backup plan path. Output is rendered text returned by `load_and_render_safe`, then sent to stderr by write/edit/bash validators. It does not mutate state itself. The protected state is the active loop backup file `plan.md` under `.humanize/rlcr`, which the Stop hook later requires to exist and match the original plan file for integrity verification at `hooks/loop-codex-stop-hook.sh:144-216`.
- gates_or_invariants: The invariant is that the loop backup plan is read-only for the duration of the RLCR loop. The Write validator identifies `plan.md` in `.humanize/rlcr` and blocks with this template at `hooks/loop-write-validator.sh:93-102`. The Edit validator applies the same gate at `hooks/loop-edit-validator.sh:77-88`. The Bash validator blocks shell commands matching `.humanize/rlcr(/[^/]+)?/plan.md`, including writes/removals, at `hooks/loop-bash-validator.sh:77-88`. The Stop hook independently enforces plan backup existence and content equality against the original plan at `hooks/loop-codex-stop-hook.sh:151-216`.
- dependencies_and_callers: Rendered by `load_and_render_safe`, sourced through `hooks/lib/loop-common.sh` and its template loader at `hooks/lib/loop-common.sh:11-17`. Direct references are `hooks/loop-write-validator.sh:98-100`, `hooks/loop-edit-validator.sh:84-86`, and `hooks/loop-bash-validator.sh:84-86`. The reference search found no other template consumers beyond those validators.
- edge_cases_or_failure_modes: The template contains no variables, so rendering does not depend on substitution data. Each caller supplies an inline fallback string if template loading fails, so the gate remains active even if the template cannot be read: write fallback at `hooks/loop-write-validator.sh:98-99`, edit fallback at `hooks/loop-edit-validator.sh:84-85`, bash fallback at `hooks/loop-bash-validator.sh:84-85`. Path sensitivity differs by hook: Write/Edit check the basename `plan.md` plus `.humanize/rlcr` in the path, while Bash uses a regex helper against the lowercased command string. A same-named `plan.md` outside `.humanize/rlcr` is not blocked by this template path in the inspected validators.
- validation_or_tests: Plan-backup protection is tested in `tests/test-plan-file-hooks.sh`. Test 5 verifies Write blocks loop `plan.md` at `tests/test-plan-file-hooks.sh:194-205`; Test 6 verifies Edit blocks it at `tests/test-plan-file-hooks.sh:211-222`; Test 7 verifies Bash write redirection is blocked at `tests/test-plan-file-hooks.sh:228-239`; Test 8 verifies Bash removal is blocked at `tests/test-plan-file-hooks.sh:241-249`. Stop-hook backup-missing behavior is tested at `tests/test-plan-file-hooks.sh:543-559`, confirming the broader plan-integrity contract around this protected backup.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-014`, `MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-044`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`