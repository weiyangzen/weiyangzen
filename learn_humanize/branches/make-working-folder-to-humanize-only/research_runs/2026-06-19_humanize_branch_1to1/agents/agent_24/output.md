# agent_24 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-024 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes the RLCR loop state for `/humanize:start-rlcr-loop`. It is the setup entrypoint that converts one implementation-plan markdown file into a timestamped `.humanize/rlcr/<timestamp>/` loop session with copied plan, persisted state, goal tracker, and round-0 prompt. The command wrapper at `commands/start-rlcr-loop.md:1-13` invokes this script directly.
- algorithmic_behavior: Parses loop options and enforces single-plan input in `scripts/setup-rlcr-loop.sh:36-173`, including `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--plan-file`, and `--track-plan-file`. It validates the repository and plan path before any loop state is created in `scripts/setup-rlcr-loop.sh:179-348`. After validation, it records the starting branch and safe Codex settings in `scripts/setup-rlcr-loop.sh:354-382`, creates `.humanize/rlcr/<timestamp>/` in `scripts/setup-rlcr-loop.sh:388-397`, writes YAML-like state frontmatter in `scripts/setup-rlcr-loop.sh:406-419`, builds `goal-tracker.md` in `scripts/setup-rlcr-loop.sh:425-513`, and renders `round-0-prompt.md` in `scripts/setup-rlcr-loop.sh:519-580`. It then prints activation metadata and the initial prompt to stdout in `scripts/setup-rlcr-loop.sh:586-638`.
- inputs_outputs_state: Inputs are CLI arguments, `CLAUDE_PROJECT_DIR` or current working directory, the relative plan file, current git repository state, current branch name, and the presence of the `codex` executable. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `.humanize/rlcr/<timestamp>/state.md`, `.humanize/rlcr/<timestamp>/goal-tracker.md`, and `.humanize/rlcr/<timestamp>/round-0-prompt.md`. The state transition is from no initialized loop session to an active round-0 session with `current_round: 0`, max iteration count, Codex model/effort/timeout, push policy, plan path, plan tracking mode, start branch, and UTC start time recorded in `state.md` at `scripts/setup-rlcr-loop.sh:406-419`. The summary file path is declared as `.humanize/rlcr/<timestamp>/round-0-summary.md` at `scripts/setup-rlcr-loop.sh:519`, but the script does not create the summary file; it instructs the worker to write it later.
- gates_or_invariants: Requires exactly one plan source: positional and `--plan-file` are mutually exclusive in `scripts/setup-rlcr-loop.sh:181-188`, and multiple positional files fail in `scripts/setup-rlcr-loop.sh:162-169`. Requires a git repository with at least one commit in `scripts/setup-rlcr-loop.sh:204-214`. Requires the plan path to be relative, space-free, shell-metacharacter-free, not a symlink, under an existing parent directory, existing as a regular file, within `PROJECT_ROOT`, and outside submodules in `scripts/setup-rlcr-loop.sh:220-292`. Plan tracking has two modes: without `--track-plan-file`, the plan must not be tracked; with it, the plan must be tracked and clean in `scripts/setup-rlcr-loop.sh:298-326`. The plan must have at least five lines in `scripts/setup-rlcr-loop.sh:332-340`. `codex` must be available in `scripts/setup-rlcr-loop.sh:342-348`. Branch/model/effort are constrained for frontmatter safety in `scripts/setup-rlcr-loop.sh:354-382`.
- dependencies_and_callers: Called by the slash-command definition in `commands/start-rlcr-loop.md:1-13`. Depends on Bash with `set -euo pipefail`, `git`, `wc`, `tr`, `date`, `mkdir`, `cp`, `grep`, `sed`, `head`, `tail`, `awk`, and `codex`. Its generated files are consumed by RLCR hooks and validators: for example, `hooks/loop-read-validator.sh:56-64` locates `.humanize/rlcr` and reads the active loop’s `state.md`; `hooks/loop-codex-stop-hook.sh` references current round summaries, prompts, review prompts, and review results throughout the round lifecycle, as shown by references around `hooks/loop-codex-stop-hook.sh:438-549` and `hooks/loop-codex-stop-hook.sh:888-929`.
- edge_cases_or_failure_modes: The initial git repository checks use `git rev-parse` without `-C "$PROJECT_ROOT"` in `scripts/setup-rlcr-loop.sh:204-214`, while later git operations use `git -C "$PROJECT_ROOT"`; if `CLAUDE_PROJECT_DIR` differs from the process cwd, validation may check the wrong directory first. The non-tracked plan mode says the file must be gitignored, but the implementation only rejects tracked files in `scripts/setup-rlcr-loop.sh:316-325`; it does not run `git check-ignore`. Numeric options accept `0` because they validate only `^[0-9]+$` in `scripts/setup-rlcr-loop.sh:107-111` and `scripts/setup-rlcr-loop.sh:134-139`, despite user-facing text saying positive. Goal and acceptance extraction are heuristic: `grep`/`sed` scan common headings in `scripts/setup-rlcr-loop.sh:448-476`, so unusual headings fall back to placeholders. `PLAN_CONTENT` is assigned in `scripts/setup-rlcr-loop.sh:426` but not used. The script writes under `.humanize/rlcr`; in this read-only branch research run, it was inspected but not executed.
- validation_or_tests: `tests/test-plan-file-validation.sh` is the direct validation test suite. It covers absolute paths, nonexistent files, missing parent directories, spaces, shell metacharacters, symlinks, path resolution errors, escaping outside the project, non-git repos, and repos with no commits in `tests/test-plan-file-validation.sh:85-319`. It covers plan tracking rules in `tests/test-plan-file-validation.sh:325-422`, YAML-unsafe branch names in `tests/test-plan-file-validation.sh:428-508`, CLI option conflict handling in `tests/test-plan-file-validation.sh:514-541`, and Codex model/effort validation in `tests/test-plan-file-validation.sh:547-585`. Tests were read for evidence; they were not run because the assignment requested research notes only.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-054 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: User-facing guard template for the RLCR read-access validator. It explains why reading a round prompt or summary from a non-loop location is blocked and redirects the agent toward the active loop directory.
- algorithmic_behavior: The template itself is declarative markdown, but it participates in the algorithmic gate implemented by `hooks/loop-read-validator.sh`. The read validator handles only `Read` tool events in `hooks/loop-read-validator.sh:22-30`, ignores non-round files in `hooks/loop-read-validator.sh:41-47`, finds the active loop and current round in `hooks/loop-read-validator.sh:56-64`, and when a round prompt/summary is outside `.humanize/rlcr`, renders this block via `load_and_render_safe` and exits with status `2` in `hooks/loop-read-validator.sh:87-97`. The template content defines the message: attempted `{{FILE_PATH}}`, active `{{ACTIVE_LOOP_DIR}}`, current prompt path, and current summary path in `prompt-template/block/wrong-file-location.md:1-9`.
- inputs_outputs_state: Inputs are placeholder variables supplied by the validator: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` in `hooks/loop-read-validator.sh:92-95`. Output is markdown written to stderr as the hook rejection reason. It has no persistent state and does not mutate loop files. Its runtime state dependency is the active loop selected from `.humanize/rlcr` and the current round read from `state.md`.
- gates_or_invariants: The invariant is that round prompt/summary reads must come from the active `.humanize/rlcr/<timestamp>/` directory, not arbitrary project paths or stale copied files. The template reinforces that the current prompt is `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-prompt.md` and the current summary is `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-summary.md` in `prompt-template/block/wrong-file-location.md:5-7`. This gate is only reached after the filename is recognized as a round prompt or summary and an active loop exists.
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh:92`. Rendering depends on `load_and_render_safe`, referenced across validators and defined in the template loader. The read validator depends on shared loop functions from `hooks/lib/loop-common.sh`, loaded at `hooks/loop-read-validator.sh:14-16`, for round-file classification, active-loop discovery, current-round extraction, and path classification. `hooks/hooks.json:38` registers `hooks/loop-read-validator.sh` as a hook command.
- edge_cases_or_failure_modes: If no active loop exists, the validator exits without rendering this block in `hooks/loop-read-validator.sh:60-62`. If the requested file is not a recognized round prompt or summary, it exits without blocking in `hooks/loop-read-validator.sh:45-47`. The validator computes `CORRECT_PATH` in `hooks/loop-read-validator.sh:88`, but this specific template does not receive or display that variable; instead it lists current round prompt/summary paths. The last line says to use `cat {{FILE_PATH}}` in `prompt-template/block/wrong-file-location.md:9`, which can be ambiguous because it repeats the blocked path rather than the computed active-loop path; the preceding lines provide the intended active loop location.
- validation_or_tests: Template references are covered by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` references and includes `hooks/loop-read-validator.sh` in its critical validator set, as shown by references around `tests/test-template-references.sh:59-69` and `tests/test-template-references.sh:172-199`. Broader template-loader behavior, including safe fallback rendering, is covered in `tests/test-template-loader.sh:197-221` and `tests/test-templates-comprehensive.sh:446-467`. No direct content-specific assertion for this exact block was observed during inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`