# agent_04 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-004 `directory` `prompt-template`
- cursor: `[_]`
- core_role: `prompt-template/` is the RLCR loop’s prompt and guard-message contract layer. It does not execute logic itself, but it defines the rendered Markdown instructions that drive loop blocking, Codex review, Claude next-round continuation, goal-tracker ownership, and several stop/validation gates. The runtime loader is `hooks/lib/template-loader.sh`, whose `get_template_dir` resolves this directory at `hooks/lib/template-loader.sh:11-16`, and whose `validate_template_dir` requires the three child folders `block`, `codex`, and `claude` at `hooks/lib/template-loader.sh:193-206`.
- algorithmic_behavior: The directory has 32 Markdown templates, 514 total lines, 21,442 bytes. It is recursively split into:
  - `prompt-template/block/`: blocking and remediation messages for hooks and validators. Examples include wrong round/path, protected plan backup, prompt/state/summary write protection, dirty git, unpushed commits, incomplete todos, large file limits, goal-tracker modification, and Codex-review failure messaging. These templates usually become JSON hook `reason` strings, causing Claude Code to block the attempted action and show corrective instructions.
  - `prompt-template/codex/`: Codex review prompt contracts. `regular-review.md` defines normal round review; `full-alignment-review.md` defines every-fifth-round goal alignment and stagnation audit; `goal-tracker-update-section.md` is injected into both review modes.
  - `prompt-template/claude/`: continuation prompt fragments for the next Claude round after Codex finds issues, plus post-alignment action items, push-every-round note, and a goal-tracker update request format.
  Runtime behavior is single-pass placeholder rendering. `render_template` scans `{{VAR}}` tokens and substitutes only the variables supplied in the current render call, leaving unknown placeholders intact and avoiding recursive placeholder injection from user/model content (`hooks/lib/template-loader.sh:35-117`). `load_and_render_safe` falls back to an inline fallback message if a template is missing or renders empty (`hooks/lib/template-loader.sh:152-188`).
- inputs_outputs_state: Inputs are loop state fields, hook input, active loop paths, current round, plan path, summary content, Codex output paths, git state, todo state, and render variables such as `CURRENT_ROUND`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `SUMMARY_CONTENT`, `REVIEW_RESULT_FILE`, `CORRECT_PATH`, `GIT_ISSUES`, and similar placeholders. Outputs are rendered Markdown blocks, either embedded in hook JSON block decisions or written to round files such as `round-N-review-prompt.md` and `round-N+1-prompt.md`. The stop hook renders the goal-tracker update section at `hooks/loop-codex-stop-hook.sh:553-557`, chooses full-alignment versus regular review at `hooks/loop-codex-stop-hook.sh:559-624`, and writes next-round prompts at `hooks/loop-codex-stop-hook.sh:887-917`. State transitions are prompt-driven: guard failures block and do not advance the round; review issues update `current_round` and create the next prompt (`hooks/loop-codex-stop-hook.sh:882-885`); successful review completion ends the loop through the stop-hook path.
- gates_or_invariants: Required subdirectories are `block`, `codex`, and `claude` (`hooks/lib/template-loader.sh:201-203`). The template renderer’s main invariant is single-pass substitution to prevent injected content from being reinterpreted as template syntax (`hooks/lib/template-loader.sh:39-42`). Guard templates encode invariants around active loop directory usage, current round number, protected `state.md`, protected prompt files, plan backup integrity, required summaries, native TodoWrite usage, goal-tracker immutability after Round 0, git cleanliness, push requirements, and large-file limits. Codex prompt templates encode review gates: no deferrals as completion, no pending work as completion, all acceptance criteria must be met, and full-alignment rounds may emit a stagnation stop signal.
- dependencies_and_callers: Primary callers are shell hooks and validators. `hooks/loop-codex-stop-hook.sh` renders block templates for plan-file modification, incomplete todos, large files, dirty git, unpushed commits, missing summaries, and uninitialized goal tracker; then renders Codex and Claude round prompts. `hooks/lib/loop-common.sh` centralizes common block messages, including goal-tracker modification blocking at `hooks/lib/loop-common.sh:185-197`. Validators call templates through the same loader: `loop-read-validator.sh` uses wrong-file and wrong-round blocks; `loop-write-validator.sh` and `loop-edit-validator.sh` use plan backup, summary location, wrong round, and wrong directory blocks; `loop-bash-validator.sh` uses git-push and plan-backup blocks; `loop-plan-file-validator.sh` uses schema-outdated. Test dependencies include `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-template-loader.sh`.
- edge_cases_or_failure_modes: Missing templates are intended to degrade to fallback messages through `load_and_render_safe`; direct `load_template` callers may receive empty content and must handle it. Unknown placeholders remain literal, which is safer than destructive substitution but can leak unresolved variables into prompts. Multi-line injected fragments can make Markdown structure awkward; for example `regular-review.md:47` and `full-alignment-review.md:57` put `{{GOAL_TRACKER_UPDATE_SECTION}}` after a heading prefix, while the injected section itself starts with a heading. `prompt-template/block/codex-review-failed.md` is present but not directly referenced by the current hook path; the hook builds a similar failure reason inline at `hooks/loop-codex-stop-hook.sh:726-752`. This creates drift risk between the template inventory and active failure behavior.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for template references and verifies referenced files exist (`tests/test-template-references.sh:51-116`), then warns on unreferenced templates (`tests/test-template-references.sh:119-142`). `tests/test-templates-comprehensive.sh` validates directory shape, loads every template, checks placeholder syntax, tests fallback behavior, and renders real templates (`tests/test-templates-comprehensive.sh:61-180`, `tests/test-templates-comprehensive.sh:446-488`, `tests/test-templates-comprehensive.sh:531-540`). `tests/test-template-loader.sh` covers loader behavior including missing templates, safe fallback, and directory validation. I inspected these tests but did not execute them, because this task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-034 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: This file is a short block template for the “Codex review failed to produce output” path. It is designed to communicate that the review could not complete, show the Codex exit code and expected review-result path, point to debug files, include stderr tail content, and tell the operator the system will retry review on the next exit attempt (`prompt-template/block/codex-review-failed.md:1-18`).
- algorithmic_behavior: If wired into the stop hook, this template would be rendered after Codex execution fails or fails to create the expected review-result file. It does not decide the failure itself; it formats failure evidence for the hook’s block response. The effective transition implied by the text is: Codex review did not yield a usable result, so the loop blocks termination and waits for another review attempt on the next exit (`prompt-template/block/codex-review-failed.md:18`).
- inputs_outputs_state: Inputs are `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5-15`). Output is rendered Markdown suitable for a hook block reason. The intended state effect is no successful round advancement and no completion acceptance; the user/agent is expected to inspect debug artifacts and retry.
- gates_or_invariants: The gate represented is “review result file was not created” or otherwise no usable Codex review output exists. The template assumes command/stdout/stderr debug artifacts are available and that stderr content has already been truncated before substitution. It says “last 50 lines” at `prompt-template/block/codex-review-failed.md:13`; the active hook’s inline failure path currently tails 30 lines in several failure branches (`hooks/loop-codex-stop-hook.sh:756-770`, `hooks/loop-codex-stop-hook.sh:793-816`), so the template wording and active implementation are not aligned.
- dependencies_and_callers: Direct search found no active `load_template` or `load_and_render_safe` caller for `block/codex-review-failed.md`. The real Codex execution failure handling is currently inline inside `hooks/loop-codex-stop-hook.sh`: debug paths are created at `hooks/loop-codex-stop-hook.sh:655-667`, the command is saved at `hooks/loop-codex-stop-hook.sh:697-709`, Codex is run at `hooks/loop-codex-stop-hook.sh:714-716`, and `codex_failure_exit` builds its own block reason at `hooks/loop-codex-stop-hook.sh:726-752`. Failure checks cover nonzero exit, missing review result, stdout-copy failure, and empty result (`hooks/loop-codex-stop-hook.sh:754-825`).
- edge_cases_or_failure_modes: Because it appears unreferenced, changes to this file may not affect runtime behavior. If later wired in, it lacks the active inline path’s richer `Error Type` distinction and does not mention stdout-copy failures or empty review-result files. It also assumes `REVIEW_RESULT_FILE` was not created, which is not true for every Codex failure mode in the active hook.
- validation_or_tests: The comprehensive template test loads every template under `prompt-template/` (`tests/test-templates-comprehensive.sh:83-107`), so this file is covered for existence/loadability and syntax as part of the directory-wide scan. `tests/test-template-references.sh` warns on templates not directly referenced (`tests/test-template-references.sh:119-142`); based on the current search result, this file would fall into that category. I did not execute tests.
- skip_candidate: `yes: current runtime uses an inline Codex failure formatter instead of this template, so the file is likely a stale or reserved block template rather than active core algorithm behavior in this branch`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-064 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: This is an active Codex review prompt fragment that establishes Codex, not Claude, as the post-Round-0 authority for `goal-tracker.md` updates. It instructs Codex to evaluate any “Goal Tracker Update Request” in Claude’s summary, apply justified updates to the tracker, or explain rejection in the review (`prompt-template/codex/goal-tracker-update-section.md:1-12`).
- algorithmic_behavior: During stop-hook review prompt construction, the hook renders this file with `GOAL_TRACKER_FILE` and stores it in `GOAL_TRACKER_UPDATE_SECTION` (`hooks/loop-codex-stop-hook.sh:553-557`). That rendered section is then injected into both review modes: regular review at `prompt-template/codex/regular-review.md:47` and full-alignment review at `prompt-template/codex/full-alignment-review.md:57`. The section asks Codex to evaluate whether a requested tracker change is justified and serves the Ultimate Goal, then either mutate the tracker or reject the request with a reason.
- inputs_outputs_state: Inputs are the rendered `GOAL_TRACKER_FILE` path, Claude’s summary content, and any summary subsection named “Goal Tracker Update Request.” Approved outputs are modifications to the tracker: move tasks between Active, Completed, and Deferred sections; add Plan Evolution Log entries; add Open Issues; or record deferrals (`prompt-template/codex/goal-tracker-update-section.md:6-17`). Rejected outputs are review comments explaining why the request was rejected (`prompt-template/codex/goal-tracker-update-section.md:11`). The state transitions are goal-tracker section changes, not loop-state frontmatter changes.
- gates_or_invariants: The key gate is Codex evaluation: the change must be justified and serve the Ultimate Goal (`prompt-template/codex/goal-tracker-update-section.md:5`). The hard invariant is that the IMMUTABLE SECTION, meaning Ultimate Goal and Acceptance Criteria, must never be modified (`prompt-template/codex/goal-tracker-update-section.md:10`). Deferrals are allowed only with strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`). This pairs with enforcement templates that tell Claude it cannot modify the goal tracker directly after Round 0 (`prompt-template/block/goal-tracker-modification.md:1-25`) and with next-round instructions reminding Claude to submit update requests instead of editing the file (`prompt-template/claude/next-round-prompt.md:22-31`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:556`. It depends on the template loader’s safe rendering behavior, especially fallback handling if the template is missing. It coordinates with `prompt-template/claude/goal-tracker-update-request.md`, which gives Claude the request format (`prompt-template/claude/goal-tracker-update-request.md:2-16`), and with `hooks/lib/loop-common.sh:185-197`, which formats the direct-modification block message. Validation coverage explicitly renders this template with a sample tracker path in `tests/test-templates-comprehensive.sh:531-540`.
- edge_cases_or_failure_modes: If Claude does not include a request section, this fragment effectively becomes a no-op review responsibility. If the request asks to alter Ultimate Goal or Acceptance Criteria, Codex must reject that portion because the immutable section is protected. If a deferral is weak or conflicts with acceptance criteria, the section tells Codex to reject or constrain it. Markdown structure is slightly brittle because caller templates prefix the injected content with `## Part 3:`, while this file starts with its own `##` heading; the rendered first line can become an awkward combined heading, though the instruction remains readable.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-540` specifically renders this template and checks for the “Goal Tracker Update Requests” heading and substituted tracker path. Directory-level tests also load all templates and validate placeholder syntax. `tests/test-template-references.sh` finds active references to this template from `hooks/loop-codex-stop-hook.sh`. I inspected the tests but did not execute them.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-004; MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-034; MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-064
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`