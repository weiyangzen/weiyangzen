# agent_19 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-019 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit validator for an active RLCR loop. It enforces that the loop continues on the branch and plan-file tracking mode captured when the loop began. It is registered in `hooks/hooks.json:4-10` as the UserPromptSubmit command hook.
- algorithmic_behavior: The hook consumes stdin hook input but does not inspect its JSON payload (`hooks/loop-plan-file-validator.sh:19-20`). It locates the active loop under `.humanize/rlcr` using `find_active_loop` (`hooks/loop-plan-file-validator.sh:22-24`), which only returns the newest loop directory if it has `state.md` (`hooks/lib/loop-common.sh:23-44`). If no active loop exists, it permits the prompt with silent `exit 0` (`hooks/loop-plan-file-validator.sh:26-29`). For an active loop, it extracts YAML frontmatter from `state.md`, parses `plan_tracked`, `plan_file`, and `start_branch`, then applies schema, branch, and plan tracking checks (`hooks/loop-plan-file-validator.sh:31-40`).
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or current working directory as `PROJECT_ROOT` (`hooks/loop-plan-file-validator.sh:13-14`), active loop state at `$PROJECT_ROOT/.humanize/rlcr/*/state.md`, and git state for the project branch and plan file (`hooks/loop-plan-file-validator.sh:82`, `hooks/loop-plan-file-validator.sh:101-102`, `hooks/loop-plan-file-validator.sh:125`). Outputs are either no stdout and `exit 0` for allow, or a JSON object with `"decision": "block"` and a reason for hook-level denial (`hooks/loop-plan-file-validator.sh:58-63`, `hooks/loop-plan-file-validator.sh:84-89`, `hooks/loop-plan-file-validator.sh:105-110`, `hooks/loop-plan-file-validator.sh:115-120`, `hooks/loop-plan-file-validator.sh:128-133`). It does not mutate state.
- gates_or_invariants: Required state schema fields are `plan_tracked` and `start_branch`; missing values trigger `schema_validation_error` (`hooks/loop-plan-file-validator.sh:46-76`). The current git branch must equal `start_branch`; otherwise prompt submission is blocked (`hooks/loop-plan-file-validator.sh:78-91`). If `plan_tracked:true`, the plan file must still be tracked and clean in git (`hooks/loop-plan-file-validator.sh:99-122`). If not tracked mode, the plan file must remain untracked/gitignored; becoming tracked blocks the loop (`hooks/loop-plan-file-validator.sh:123-136`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `load_and_render_safe`, and `TEMPLATE_DIR` initialization (`hooks/loop-plan-file-validator.sh:16-17`; `hooks/lib/loop-common.sh:11-17`). Depends on `hooks/lib/template-loader.sh` safe rendering behavior for schema-block templates and fallbacks (`hooks/lib/template-loader.sh:152-188`). Depends on `jq` for JSON-string escaping of schema reasons (`hooks/loop-plan-file-validator.sh:54-57`) and `git`, `sed`, `grep`, `tr`. Called by Claude hook registration at `hooks/hooks.json:4-10`. Behavior is tested through `tests/test-plan-file-hooks.sh`, which names it as the UserPromptSubmit hook (`tests/test-plan-file-hooks.sh:3-7`).
- edge_cases_or_failure_modes: Missing or old-schema `state.md` frontmatter blocks if required fields are absent (`hooks/loop-plan-file-validator.sh:66-76`). YAML quotes are stripped from `plan_file` and `start_branch` for backward compatibility (`hooks/loop-plan-file-validator.sh:34-40`); tests cover quoted `plan_file`, quoted branch, and hyphenated paths (`tests/test-plan-file-hooks.sh:106-119`, `tests/test-plan-file-hooks.sh:341-386`, `tests/test-plan-file-hooks.sh:431-460`). If `git rev-parse` fails, `CURRENT_BRANCH` becomes empty, which will block whenever `start_branch` is non-empty (`hooks/loop-plan-file-validator.sh:82-90`). The hook sets `FULL_PLAN_PATH` but does not use it (`hooks/loop-plan-file-validator.sh:97`), so all plan validation is git-index/status based. A committed tracked-plan content change with clean git status is not detected here; related stop-hook tests cover that stronger integrity check instead (`tests/test-plan-file-hooks.sh:655-720`).
- validation_or_tests: Direct tests cover valid state pass, quoted `plan_file` parsing, missing `plan_tracked`, missing `start_branch`, and branch change blocking (`tests/test-plan-file-hooks.sh:91-185`). Additional tests cover quoted `start_branch` and hyphenated plan file paths (`tests/test-plan-file-hooks.sh:341-386`, `tests/test-plan-file-hooks.sh:431-460`). I did not execute the test suite in this read-only research pass.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-049 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Bash validator when a user attempts to modify `round-*-summary.md` through shell commands instead of Write/Edit. It is a small prompt contract that explains the gate and supplies the correct summary path.
- algorithmic_behavior: The template content tells the agent: do not use Bash to modify summary files; use the Write or Edit tool at `{{CORRECT_PATH}}`; Bash commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/summary-bash-write.md:1-8`). Its single placeholder is rendered by `summary_bash_blocked_message`, which passes `CORRECT_PATH=$correct_path` (`hooks/lib/loop-common.sh:115-124`).
- inputs_outputs_state: Input is the current round summary path computed by the Bash validator as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-bash-validator.sh:122-124`). Output is rendered markdown emitted to stderr by `loop-bash-validator.sh`, followed by hook denial with exit code `2` (`hooks/loop-bash-validator.sh:122-125`). It does not read or write persistent state; it formats the reason for an existing Bash gate.
- gates_or_invariants: The invariant is that summary files must be created or changed via Write/Edit so downstream validation hooks can inspect file path, round number, and loop semantics. The Bash validator detects command patterns against `round-[0-9]+-summary\.md` (`hooks/loop-bash-validator.sh:117-125`) using `command_modifies_file`, whose patterns cover redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf`, and `exec >` forms (`hooks/lib/loop-common.sh:155-183`).
- dependencies_and_callers: Loaded indirectly through `hooks/lib/loop-common.sh:117-124`, which calls `load_and_render_safe` with this template and an inline fallback. The safe loader uses the plugin `prompt-template` directory (`hooks/lib/template-loader.sh:9-16`) and falls back if a template is missing or renders empty (`hooks/lib/template-loader.sh:152-188`). The direct runtime caller is `hooks/loop-bash-validator.sh:122-125`, registered as the Bash PreToolUse hook in `hooks/hooks.json:42-48`. Template existence is checked in `tests/test-template-references.sh:152-167`.
- edge_cases_or_failure_modes: If the template is missing, the fallback message still includes `{{CORRECT_PATH}}` substitution through `load_and_render_safe` (`hooks/lib/loop-common.sh:119-123`; `hooks/lib/template-loader.sh:164-180`). Detection depends on regex matching shell text rather than shell parsing, so unusual quoting, variables, command construction, or paths with spaces may evade or overmatch. The template names common bypass commands but enforcement comes from `command_modifies_file`; the message itself is advisory, not a parser.
- validation_or_tests: Cross-reference tests require the template to exist as a common block template (`tests/test-template-references.sh:152-167`). The Bash validator tests exercise related bypass-blocking behavior for protected RLCR files, including command redirection and substitution patterns (`tests/test-plan-file-hooks.sh:224-337`), though the returned text is generally asserted by protected file category rather than this exact template body. I did not execute the test suite in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`