# agent_10 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-010 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Provides the Claude-facing prompt fragments used by the RLCR loop to continue work after Codex review, preserve the original plan/goal-tracker context, require summaries, and route Goal Tracker changes through Codex instead of direct file edits.
- algorithmic_behavior: The directory is a small prompt-fragment state machine for later rounds. `prompt-template/claude/next-round-prompt.md:1` restarts Claude with “Your work is not finished,” then requires review of `@{{PLAN_FILE}}` at lines 3-8, todo creation for all discovered issues at lines 12-14, inclusion of Codex review content through `{{REVIEW_CONTENT}}` at lines 16-19, and read-only Goal Tracker reference at lines 22-31. `prompt-template/claude/next-round-footer.md:4-9` blocks improper loop exit behavior and requires commit plus summary output to `@{{NEXT_SUMMARY_FILE}}`. `prompt-template/claude/goal-tracker-update-request.md:2-14` appends the structured summary section Claude must use when Goal Tracker updates are needed. `prompt-template/claude/post-alignment-action-items.md:2-7` injects extra priorities after full alignment checks: forgotten items, unmet acceptance criteria, and unjustified deferrals. `prompt-template/claude/push-every-round-note.md:2` adds a push-after-each-round instruction when enabled.
- inputs_outputs_state: Inputs are template variables rendered by hook code: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, and `NEXT_SUMMARY_FILE` for next-round prompt assembly in `hooks/loop-codex-stop-hook.sh:900-917`. `PUSH_EVERY_ROUND` gates whether `push-every-round-note.md` is appended at `hooks/loop-codex-stop-hook.sh:919-925`. Full-alignment state is computed from `CURRENT_ROUND % 5 == 4` at `hooks/loop-codex-stop-hook.sh:905-910`, which conditionally appends `post-alignment-action-items.md`. The output is the next prompt file written to `$NEXT_PROMPT_FILE`, then included as the stop-hook response reason at `hooks/loop-codex-stop-hook.sh:938-940`.
- gates_or_invariants: The main invariant is that after Round 0, Claude reads Goal Tracker state but does not directly modify it; `next-round-prompt.md:22-31` states this, and `goal-tracker-update-request.md:2-16` gives the request protocol. Another invariant is comprehensive task tracking: `next-round-prompt.md:12-14` prohibits only addressing the highest-priority issue and requires todos for all discovered issues. The footer enforces loop integrity by prohibiting false exit or state tampering at `next-round-footer.md:4`.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, which loads and renders `claude/next-round-prompt.md`, appends `claude/post-alignment-action-items.md` conditionally, appends `claude/next-round-footer.md`, optionally appends `claude/push-every-round-note.md`, and always appends `claude/goal-tracker-update-request.md` at `hooks/loop-codex-stop-hook.sh:891-933`. The directory coordinates with sibling `prompt-template/codex/goal-tracker-update-section.md`, whose Codex-side instructions make Codex evaluate Claude’s requested Goal Tracker changes and avoid immutable sections at `prompt-template/codex/goal-tracker-update-section.md:1-17`.
- edge_cases_or_failure_modes: If a Claude template is missing, `loop-codex-stop-hook.sh` has fallbacks for the next prompt, footer, push note, and goal-update request at lines 892-899, 914-917, 921-924, and 929-932. The post-alignment fragment has no explicit fallback beyond omission if empty at lines 907-910, so a missing file silently removes that extra guidance. If `PUSH_EVERY_ROUND` is false, the push note is intentionally omitted. If `CURRENT_ROUND` math is wrong upstream, post-alignment priorities may be appended on the wrong round.
- validation_or_tests: Static reference validation exists in `tests/test-template-references.sh:149-167` for common/block templates, but that snippet does not enumerate the Claude directory fragments. Comprehensive rendering coverage is visible for the related Codex Goal Tracker section in `tests/test-templates-comprehensive.sh:531-540`; this indirectly validates the paired Codex side of the Claude request flow. I did not run tests because this is a read-only research pass; I inspected the referenced scripts and templates directly.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-040 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Defines the bash-validator block message used when a non-Codex actor attempts to modify `goal-tracker.md` after Round 0. It is a core gate template for preserving Goal Tracker authority and forcing state changes through reviewable summary requests.
- algorithmic_behavior: The template declares “Goal Tracker Modification Blocked” with `{{CURRENT_ROUND}}` in the heading at `prompt-template/block/goal-tracker-modification.md:1`. Lines 3-5 define the transition rule: after Round 0, only Codex may modify the Goal Tracker, and direct modification via Write, Edit, or Bash is prohibited. Lines 7-23 define the replacement protocol: include a `Goal Tracker Update Request` section in `{{SUMMARY_FILE}}`, with requested changes and justification. Line 25 assigns Codex the review-and-apply role if the request is justified.
- inputs_outputs_state: Inputs are `CURRENT_ROUND` and `SUMMARY_FILE`, supplied by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:187-197`. In the validator path, when `command_modifies_file "$COMMAND_LOWER" "goal-tracker\.md"` matches and `CURRENT_ROUND` is greater than 0, `hooks/loop-bash-validator.sh:96-104` computes `SUMMARY_FILE="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"`, renders this template, writes it to stderr, and exits with status 2. The state transition is not a file mutation; it is an attempted direct write being converted into a summary-request workflow.
- gates_or_invariants: The invariant is direct Goal Tracker writes are blocked after Round 0. Round 0 has a separate behavior: `loop-bash-validator.sh:97-100` calls `goal_tracker_bash_blocked_message` instead, while later rounds call this template at lines 101-102. The command detector in `hooks/lib/loop-common.sh:160-183` recognizes redirects, `tee`, in-place editors, copy/move/removal, `dd of=`, `truncate`, `printf >`, and file-descriptor redirects against the target pattern. The template also preserves a review gate: Codex must judge whether requested changes serve the Ultimate Goal before applying them, matching the Codex-side responsibility in `prompt-template/codex/goal-tracker-update-section.md:3-17`.
- dependencies_and_callers: Direct caller is `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:185-197`, which uses `load_and_render_safe` with a short fallback if this template is unavailable. Runtime caller is `hooks/loop-bash-validator.sh:90-105`, which detects attempted `goal-tracker.md` modification and emits the rendered block. The workflow pairs with `prompt-template/claude/goal-tracker-update-request.md:2-16`, which tells Claude to include the same structured request in its summary, and with `hooks/loop-codex-stop-hook.sh:553-557`, which loads the Codex-side section telling Codex to process those requests.
- edge_cases_or_failure_modes: Coverage depends on the shell-command pattern detector; unusual write methods not matching `command_modifies_file` could bypass this specific bash validation layer. The fallback in `hooks/lib/loop-common.sh:190-196` preserves the core block but loses the full request format if this template is missing. Commands matching `goal-tracker.md` for benign reads should not be blocked unless they match one of the modification patterns. Round 0 intentionally uses a different Goal Tracker block path, so this file’s detailed request flow applies only after the initial setup round.
- validation_or_tests: `tests/test-template-references.sh:152-159` explicitly requires `block/goal-tracker-modification.md` to exist as one of the common templates referenced by `loop-common.sh`. The broader rendering tests in `tests/test-templates-comprehensive.sh:531-540` cover the reciprocal Codex Goal Tracker update section, including successful interpolation of `GOAL_TRACKER_FILE`. I did not execute tests in this read-only branch export; validation here is direct inspection of template, hook call sites, and test references.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 Item Evidence headings present, each with one assigned research item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`