# agent_21 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-021 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` gate for the RLCR loop. It prevents Claude from writing to scheduler-owned loop files, enforces round-scoped summary paths, and preserves RLCR state integrity before a write reaches the filesystem. It is wired in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/hooks.json:14) with matcher `Write` and command `hooks/loop-write-validator.sh`.
- algorithmic_behavior: The hook reads JSON hook input from stdin, extracts `.tool_name`, and exits allow-pass for any non-`Write` tool at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-write-validator.sh:23). For `Write`, it lowercases `.tool_input.file_path`, immediately blocks `round-*-todos.md` and `round-*-prompt.md`, classifies summary files and `.humanize/rlcr/` paths, finds the active loop, reads `current_round`, then applies protected-file and canonical-path checks.
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path`; implicit inputs are `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr/<timestamp>/state.md`, and prompt templates. Output is exit `0` to allow or exit `2` plus a Markdown block reason on stderr to block. The script itself does not mutate state; it derives current state from the newest active loop directory returned by `find_active_loop` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:27) and current round from frontmatter via `get_current_round` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:48).
- gates_or_invariants: Todos files are blocked because the loop expects native TodoWrite instead of `round-*-todos.md` writes at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-write-validator.sh:37). Prompt files are read-only instructions generated for Claude, blocked at line 42. `state.md` writes are always blocked after an active loop is found at line 87. `plan.md` under `.humanize/rlcr/` is treated as a protected backup at line 96. `goal-tracker.md` is writable only during round 0; after that the hook directs updates into the current summary at line 109. Summary files must be inside `.humanize/rlcr/`, must use the current round number, and must be written to the active loop directory, enforced at lines 119, 145, and 171.
- dependencies_and_callers: Depends on `jq`, `sed`, `basename`, and shared functions from [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:11). Message rendering goes through `load_and_render_safe`, whose fallback behavior is defined in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/template-loader.sh:155). It consumes block templates including `plan-backup-protected.md`, `wrong-summary-location.md`, `wrong-round-number.md`, and `wrong-directory-path.md`. It is a sibling to read/edit/bash validators listed in `hooks/hooks.json`; those share the same path predicates and message helpers.
- edge_cases_or_failure_modes: If no active loop exists, it allows writes at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-write-validator.sh:77), so protections only apply while a valid newest loop has `state.md`. Non-summary files outside `.humanize/rlcr/` are allowed at line 55. Other non-controlled files inside `.humanize/rlcr/` are allowed unless they are `state.md`, `goal-tracker.md`, or `plan.md`, at line 63. Legacy `.humanize-loop.local` paths are not treated as active RLCR paths because the loop-dir predicate only matches `.humanize/rlcr/` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:149). A malformed or missing `current_round` defaults to `0` via helper behavior rather than failing closed. Path equality is literal, so symlinks, relative-vs-absolute differences, or trailing path normalization differences could trigger a wrong-directory block.
- validation_or_tests: Tests directly cover blocking writes to loop `plan.md` in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-plan-file-hooks.sh:194) and allowing legacy `.humanize-loop.local` writes in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-plan-file-hooks.sh:751). Template-reference tests include this hook in the scan set at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:57) and check critical validators use safe rendering at line 174. I inspected tests and code only; I did not execute the test suite.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-051 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR stop gate that enforces `push_every_round`. It does not compute the gate; it defines the exact remediation prompt shown when local commits are ahead of the remote.
- algorithmic_behavior: The template renders two placeholders, `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, into a Markdown block headed “Unpushed Commits Detected” at [prompt-template/block/unpushed-commits.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/prompt-template/block/unpushed-commits.md:1). It tells the agent that stopping is blocked with unpushed commits and gives the concrete command `git push origin {{CURRENT_BRANCH}}` at line 9. Rendering is single-pass placeholder substitution, so placeholder-like text inside values is not recursively expanded, per [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/template-loader.sh:39).
- inputs_outputs_state: Inputs are `AHEAD_COUNT` parsed from `git status -sb` and `CURRENT_BRANCH` from `git rev-parse --abbrev-ref HEAD`, both passed by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:407). Output is a rendered Markdown reason embedded in JSON `{ "decision": "block", "reason": ..., "systemMessage": ... }` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:421). The template itself has no state transition; the stop hook blocks exit until pushing changes makes the ahead count disappear.
- gates_or_invariants: The associated gate only runs when `push_every_round` in loop state frontmatter resolves to `true`; the stop hook parses it at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:81) and defaults to `false` at line 91. The unpushed-commit gate runs after the dirty-worktree gate, so uncommitted changes block before unpushed commits at line 377. The invariant is: if stop is attempted during an active loop, git is clean, `push_every_round=true`, and local branch status reports `ahead N`, exit is blocked until the branch is pushed.
- dependencies_and_callers: Called only by the Codex stop hook’s unpushed-commit section through `load_and_render_safe` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:417). Template lookup and fallback behavior come from [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/template-loader.sh:152). The fallback message is inline in the stop hook at line 412, so a missing template degrades to a simpler block message rather than allowing exit.
- edge_cases_or_failure_modes: Detection depends on `git status -sb` containing the English text `ahead N`; unusual Git porcelain localization or detached/no-upstream states may not produce that pattern. `AHEAD_COUNT` uses `grep -o '[0-9]*'`, so it assumes the matched status fragment contains only the intended ahead count. If `CURRENT_BRANCH` cannot be resolved, the hook passes `unknown`, which would render a less useful push command. Since the template says `git push origin <branch>`, it assumes `origin` is the correct remote and the current branch should be pushed there.
- validation_or_tests: Template rendering is covered in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-templates-comprehensive.sh:517), which verifies the heading, ahead count, and branch name render. Template-reference scanning includes `loop-codex-stop-hook.sh` in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:57). I inspected these tests only; I did not execute them.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 section headers present for the 2 assigned rows`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`