# agent_06 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the repository’s shell-based validation suite for the RLCR loop’s core control-plane behavior. It does not implement the runtime algorithm directly, but it codifies the expected gates for template loading/rendering, plan-file setup validation, hook enforcement, loop state discovery/termination, and prompt/template reference integrity. The directory is flat, with seven shell scripts: `tests/test-error-scenarios.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-plan-file-validation.sh`, `tests/test-state-exit-naming.sh`, `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/test-templates-comprehensive.sh`.
- algorithmic_behavior: `tests/test-template-loader.sh` validates `hooks/lib/template-loader.sh`: `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` are exercised for normal loading, missing templates, literal shell metacharacter handling, multiline values, and placeholder-injection prevention (`tests/test-template-loader.sh:41`, `tests/test-template-loader.sh:197`, `tests/test-template-loader.sh:247`, `tests/test-template-loader.sh:488`). The underlying loader uses a single-pass `awk` scan so values containing `{{OTHER_VAR}}` are emitted without a second substitution pass (`hooks/lib/template-loader.sh:35`, `hooks/lib/template-loader.sh:56`, `hooks/lib/template-loader.sh:100`).
- algorithmic_behavior: `tests/test-templates-comprehensive.sh` expands that coverage across all `prompt-template/**/*.md` files. It verifies required template subdirectories, loads every Markdown template, validates placeholder syntax for uppercase `{{VAR_NAME}}`-style placeholders, tests malformed placeholder detection, renders edge cases, checks safe fallback rendering, and stress-renders every template with dummy placeholder values (`tests/test-templates-comprehensive.sh:61`, `tests/test-templates-comprehensive.sh:83`, `tests/test-templates-comprehensive.sh:113`, `tests/test-templates-comprehensive.sh:196`, `tests/test-templates-comprehensive.sh:440`, `tests/test-templates-comprehensive.sh:542`).
- algorithmic_behavior: `tests/test-template-references.sh` scans runtime hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references, fails on missing referenced templates, warns about unreferenced templates, verifies common templates used by `loop-common.sh`, and asserts critical validators use `load_and_render_safe` rather than unsafe direct rendering (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:116`, `tests/test-template-references.sh:144`, `tests/test-template-references.sh:169`). This protects the block-message path from silent empty output when a validator blocks an action.
- algorithmic_behavior: `tests/test-plan-file-validation.sh` exercises the setup command `scripts/setup-rlcr-loop.sh` as a validation gate. It asserts rejection of absolute paths, missing files, missing parent directories, spaces, shell metacharacters, symlinks, project-directory escapes, non-git repos, git repos without commits, wrong plan tracking state, unsafe branch names, invalid Codex model/effort strings, and conflicting CLI plan arguments (`tests/test-plan-file-validation.sh:85`, `tests/test-plan-file-validation.sh:100`, `tests/test-plan-file-validation.sh:124`, `tests/test-plan-file-validation.sh:165`, `tests/test-plan-file-validation.sh:185`, `tests/test-plan-file-validation.sh:241`, `tests/test-plan-file-validation.sh:270`, `tests/test-plan-file-validation.sh:325`, `tests/test-plan-file-validation.sh:428`, `tests/test-plan-file-validation.sh:531`, `tests/test-plan-file-validation.sh:547`). These tests map directly to setup validations in `scripts/setup-rlcr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:226`, `scripts/setup-rlcr-loop.sh:234`, `scripts/setup-rlcr-loop.sh:246`, `scripts/setup-rlcr-loop.sh:265`, `scripts/setup-rlcr-loop.sh:279`, `scripts/setup-rlcr-loop.sh:298`, and `scripts/setup-rlcr-loop.sh:356`.
- algorithmic_behavior: `tests/test-plan-file-hooks.sh` validates the active-loop hook protocol around immutable plan inputs. It sets up `.humanize/rlcr/<timestamp>/state.md`, a plan backup, and a gitignored source plan, then checks that `loop-plan-file-validator.sh` enforces `plan_tracked` and `start_branch`, parses quoted YAML scalar values, blocks branch changes, and validates plan tracking consistency (`tests/test-plan-file-hooks.sh:37`, `tests/test-plan-file-hooks.sh:91`, `tests/test-plan-file-hooks.sh:121`, `tests/test-plan-file-hooks.sh:141`, `tests/test-plan-file-hooks.sh:164`). It also verifies write/edit/bash validators block modifications to loop `plan.md`, including direct `.humanize/rlcr/plan.md`, command substitution, glob, brace expansion, pipe-to-tee, and backtick bypass forms (`tests/test-plan-file-hooks.sh:194`, `tests/test-plan-file-hooks.sh:211`, `tests/test-plan-file-hooks.sh:228`, `tests/test-plan-file-hooks.sh:254`, `tests/test-plan-file-hooks.sh:272`, `tests/test-plan-file-hooks.sh:285`, `tests/test-plan-file-hooks.sh:298`, `tests/test-plan-file-hooks.sh:311`, `tests/test-plan-file-hooks.sh:324`).
- algorithmic_behavior: `tests/test-plan-file-hooks.sh` also validates stop-hook plan integrity. It expects the stop hook to block when the source plan differs from the loop backup, when the source plan is deleted, when the backup is missing, when tracked plans have uncommitted changes, when old state schemas omit required fields, and when tracked plans are modified and committed after loop start (`tests/test-plan-file-hooks.sh:469`, `tests/test-plan-file-hooks.sh:509`, `tests/test-plan-file-hooks.sh:543`, `tests/test-plan-file-hooks.sh:562`, `tests/test-plan-file-hooks.sh:631`, `tests/test-plan-file-hooks.sh:655`). The corresponding stop hook parses these state fields at `hooks/loop-codex-stop-hook.sh:69`, blocks outdated schemas at `hooks/loop-codex-stop-hook.sh:112`, checks branch consistency at `hooks/loop-codex-stop-hook.sh:130`, checks tracked plan status at `hooks/loop-codex-stop-hook.sh:180`, and performs backup content diffing at `hooks/loop-codex-stop-hook.sh:196`.
- algorithmic_behavior: `tests/test-state-exit-naming.sh` validates active-loop state-machine semantics. Only a directory containing `state.md` is active; terminal state files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active (`tests/test-state-exit-naming.sh:38`, `tests/test-state-exit-naming.sh:93`, `tests/test-state-exit-naming.sh:110`, `tests/test-state-exit-naming.sh:125`, `tests/test-state-exit-naming.sh:141`). It asserts newest active loop precedence (`tests/test-state-exit-naming.sh:157`), valid `end_loop` reasons and target filenames (`tests/test-state-exit-naming.sh:179`), and the new `.humanize/rlcr` path boundary versus legacy `.humanize-loop.local` (`tests/test-state-exit-naming.sh:241`). These map to `find_active_loop`, `is_in_humanize_loop_dir`, and `end_loop` in `hooks/lib/loop-common.sh:23`, `hooks/lib/loop-common.sh:149`, and `hooks/lib/loop-common.sh:199`.
- algorithmic_behavior: `tests/test-error-scenarios.sh` is an observational error-mode script for `template-loader.sh`. It records that missing templates, missing directories, empty content, and `set -euo pipefail` contexts return empty output without crashing, but explicitly flags the risk that empty feedback can reach the agent (`tests/test-error-scenarios.sh:21`, `tests/test-error-scenarios.sh:34`, `tests/test-error-scenarios.sh:47`, `tests/test-error-scenarios.sh:86`, `tests/test-error-scenarios.sh:107`). This supports the later suite’s emphasis on `load_and_render_safe`.
- inputs_outputs_state: Inputs are shell script paths, prompt templates under `prompt-template`, hook JSON payloads, temporary git repositories, synthetic plan files, `.humanize/rlcr/<timestamp>` loop directories, `state.md` frontmatter, and command-line options to `scripts/setup-rlcr-loop.sh`. Outputs are pass/fail counters and process exit codes; most scripts exit with the number of failures or `0` on success, while hook tests expect validator block exit code `2` for tool-use validators and JSON `"decision": "block"` output for stop/user-prompt hooks. Temporary state is created under `mktemp -d` test directories (`tests/test-plan-file-validation.sh:32`, `tests/test-plan-file-hooks.sh:30`, `tests/test-state-exit-naming.sh:31`).
- inputs_outputs_state: The tests validate state transitions from setup into active loop artifacts: `scripts/setup-rlcr-loop.sh` creates `.humanize/rlcr/<timestamp>/state.md`, copies the plan backup to `plan.md`, creates `goal-tracker.md`, and emits `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:388`, `scripts/setup-rlcr-loop.sh:396`, `scripts/setup-rlcr-loop.sh:406`, `scripts/setup-rlcr-loop.sh:421`, `scripts/setup-rlcr-loop.sh:516`). The exit naming tests validate transitions from active `state.md` to terminal `<reason>-state.md` via `end_loop` (`hooks/lib/loop-common.sh:221`).
- gates_or_invariants: Template invariants include existing `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude` subdirectories (`tests/test-templates-comprehensive.sh:74`), non-empty loadable Markdown templates (`tests/test-templates-comprehensive.sh:90`), uppercase no-space placeholder syntax (`tests/test-templates-comprehensive.sh:122`), and safe fallback for critical validators (`tests/test-template-references.sh:172`). Rendering invariant: substitution is single-pass and preserves placeholders embedded inside variable values (`hooks/lib/template-loader.sh:39`, `tests/test-template-loader.sh:488`).
- gates_or_invariants: Plan-file setup invariants include relative paths only, no spaces or shell metacharacters, no symlink plan files, real path under project root, no submodule path, correct tracked/untracked mode, at least five plan lines, available `codex`, YAML-safe branch names, and YAML-safe Codex model/effort values (`scripts/setup-rlcr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:226`, `scripts/setup-rlcr-loop.sh:234`, `scripts/setup-rlcr-loop.sh:246`, `scripts/setup-rlcr-loop.sh:265`, `scripts/setup-rlcr-loop.sh:279`, `scripts/setup-rlcr-loop.sh:301`, `scripts/setup-rlcr-loop.sh:332`, `scripts/setup-rlcr-loop.sh:342`, `scripts/setup-rlcr-loop.sh:356`, `scripts/setup-rlcr-loop.sh:366`).
- gates_or_invariants: Hook invariants include no branch switching during an active loop, required state schema fields `plan_tracked` and `start_branch`, source plan tracking consistency, source plan content equality to backup, immutable plan backup, no Bash modification bypasses for protected files, and active-loop detection only through `.humanize/rlcr/*/state.md` (`hooks/loop-plan-file-validator.sh:66`, `hooks/loop-plan-file-validator.sh:82`, `hooks/loop-plan-file-validator.sh:99`, `hooks/loop-codex-stop-hook.sh:196`, `hooks/loop-bash-validator.sh:72`, `hooks/loop-bash-validator.sh:83`, `hooks/lib/loop-common.sh:35`).
- dependencies_and_callers: The tests depend on Bash, git, `jq` for hook JSON construction/parsing, `python3` indirectly through `loop-codex-stop-hook.sh` todo checking, `codex` or a mock `codex` shim in plan-file validation tests, and standard Unix tools including `sed`, `awk`, `grep`, `find`, `sort`, `wc`, `mktemp`, `chmod`, and `diff`. Runtime files under test include `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, and the full `prompt-template` tree.
- dependencies_and_callers: Coordination with sibling/parent paths is test-driven: `tests` is the validation sibling of `hooks`, `scripts`, and `prompt-template`. It verifies that setup writes loop artifacts consumed by hooks, that hooks render block messages from templates, and that shared library logic in `hooks/lib` is reused consistently by write/edit/bash/read/stop validators. `tests/test-template-references.sh` explicitly scans hook scripts and `hooks/lib/loop-common.sh` to enforce template dependency integrity (`tests/test-template-references.sh:56`).
- edge_cases_or_failure_modes: Covered template failure modes include missing template files returning empty output, incomplete template directories, malformed placeholders, values containing shell metacharacters, Windows paths, JSON-looking values, multiline values, and placeholder injection in review content (`tests/test-error-scenarios.sh:21`, `tests/test-template-loader.sh:247`, `tests/test-template-loader.sh:473`, `tests/test-template-loader.sh:488`, `tests/test-templates-comprehensive.sh:196`).
- edge_cases_or_failure_modes: Covered plan/setup failure modes include absolute or escaping paths, spaces, shell metacharacters, symlinks, unreadable path resolution, non-git repos, repos with no commits, tracked plan without `--track-plan-file`, untracked plan with `--track-plan-file`, modified tracked plans, YAML-unsafe branch/model/effort data, and duplicate plan-file CLI specification (`tests/test-plan-file-validation.sh:85`, `tests/test-plan-file-validation.sh:198`, `tests/test-plan-file-validation.sh:357`, `tests/test-plan-file-validation.sh:391`, `tests/test-plan-file-validation.sh:428`, `tests/test-plan-file-validation.sh:531`).
- edge_cases_or_failure_modes: Covered hook bypass cases include command substitution, glob expansion, brace expansion, piped `tee`, backtick substitution, quoted YAML state fields, branch mismatch under quoted state values, legacy `.humanize-loop.local` false positives, tracked-plan committed modifications that leave git status clean, and outdated state schema (`tests/test-plan-file-hooks.sh:272`, `tests/test-plan-file-hooks.sh:285`, `tests/test-plan-file-hooks.sh:298`, `tests/test-plan-file-hooks.sh:311`, `tests/test-plan-file-hooks.sh:324`, `tests/test-plan-file-hooks.sh:341`, `tests/test-plan-file-hooks.sh:365`, `tests/test-plan-file-hooks.sh:631`, `tests/test-plan-file-hooks.sh:655`, `tests/test-state-exit-naming.sh:254`).
- validation_or_tests: This assigned item is itself the validation surface. I inspected all seven files recursively under `tests` and followed their direct references to loader, loop common, setup, plan-file validator, stop hook, bash validator, and related prompt templates. I did not execute the test suite because the assignment requested research notes only and the branch export/sandbox is read-only; the scripts create temporary git repositories, write loop artifacts, and run setup/hook commands.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-036 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: This file is a prompt block appended to the stop-hook’s git-cleanliness gate when non-humanize untracked files are present. Its algorithmic role is advisory but gate-relevant: it specializes the generic “Git Not Clean” block by steering the agent to classify untracked files as generated artifacts versus real changes before committing. The template is loaded by `hooks/loop-codex-stop-hook.sh` when `git status --porcelain` includes `??` entries outside `.humanize*` (`hooks/loop-codex-stop-hook.sh:354`, `hooks/loop-codex-stop-hook.sh:366`).
- algorithmic_behavior: The template contains static Markdown only. It declares that untracked files may be build artifacts, test outputs, or runtime-generated files; says those should “typically” be added to `.gitignore`; lists common generated categories; and instructs review plus `.gitignore` pattern updates (`prompt-template/block/git-not-clean-untracked.md:2`, `prompt-template/block/git-not-clean-untracked.md:3`, `prompt-template/block/git-not-clean-untracked.md:4`, `prompt-template/block/git-not-clean-untracked.md:10`). It has no `{{PLACEHOLDER}}` variables, so `load_template` reads it verbatim and no render substitution is required.
- inputs_outputs_state: Input is the stop hook’s computed `OTHER_UNTRACKED`, derived by filtering untracked porcelain rows to exclude `.humanize` paths (`hooks/loop-codex-stop-hook.sh:355`, `hooks/loop-codex-stop-hook.sh:367`). If non-empty, the hook loads this template into `UNTRACKED_NOTE`; if the file is missing or empty, it falls back to a short inline message (`hooks/loop-codex-stop-hook.sh:369`, `hooks/loop-codex-stop-hook.sh:370`). Output is appended to `SPECIAL_NOTES`, which is rendered into `prompt-template/block/git-not-clean.md` through `SPECIAL_NOTES={{SPECIAL_NOTES}}` and returned as a JSON block reason (`hooks/loop-codex-stop-hook.sh:373`, `hooks/loop-codex-stop-hook.sh:386`; `prompt-template/block/git-not-clean.md:3`).
- inputs_outputs_state: The state transition is not a file mutation; it is a validation-result transition. Dirty git state sets `GIT_ISSUES="uncommitted changes"`, optional untracked notes enrich `SPECIAL_NOTES`, and the stop hook blocks the exit with `"decision": "block"` until changes are committed or ignored (`hooks/loop-codex-stop-hook.sh:349`, `hooks/loop-codex-stop-hook.sh:351`, `hooks/loop-codex-stop-hook.sh:377`, `hooks/loop-codex-stop-hook.sh:390`).
- gates_or_invariants: The invariant is that an RLCR loop cannot exit while git has uncommitted changes, including untracked files. This template does not decide the block; the gate is enforced by `GIT_STATUS` being non-empty (`hooks/loop-codex-stop-hook.sh:350`, `hooks/loop-codex-stop-hook.sh:378`). The file’s advisory invariant is that generated untracked files should be ignored rather than committed, while real changes still need explicit staging and commit through the parent `git-not-clean.md` action list (`prompt-template/block/git-not-clean-untracked.md:4`; `prompt-template/block/git-not-clean.md:5`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`. It depends on `hooks/lib/template-loader.sh` through `load_template`, which returns template content or an empty string with a warning if missing (`hooks/lib/template-loader.sh:18`, `hooks/lib/template-loader.sh:21`). It composes with sibling templates `prompt-template/block/git-not-clean.md`, which provides the main block and action list, and `prompt-template/block/git-not-clean-humanize-local.md`, which handles `.humanize*` untracked files separately (`hooks/loop-codex-stop-hook.sh:357`, `hooks/loop-codex-stop-hook.sh:359`, `prompt-template/block/git-not-clean-humanize-local.md:2`).
- dependencies_and_callers: Validation coverage comes from `tests/test-template-references.sh`, which scans template references in hooks and fails if referenced templates are missing (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:93`), and `tests/test-templates-comprehensive.sh`, which loads every Markdown template and validates placeholder syntax (`tests/test-templates-comprehensive.sh:83`, `tests/test-templates-comprehensive.sh:113`). Because this template has no placeholders, it falls into the “syntax valid, no placeholders” path (`tests/test-templates-comprehensive.sh:190`).
- edge_cases_or_failure_modes: If the template file is missing, the stop hook falls back to `Review untracked files - add to .gitignore or commit them.` (`hooks/loop-codex-stop-hook.sh:370`). If untracked paths include both `.humanize*` and other files, the hook concatenates the humanize-local note and this untracked note without adding an explicit separator at concatenation time (`hooks/loop-codex-stop-hook.sh:363`, `hooks/loop-codex-stop-hook.sh:373`); both templates begin with leading blank lines, reducing but not structurally guaranteeing clean formatting. If all untracked files are `.humanize*`, this template is not used (`hooks/loop-codex-stop-hook.sh:367`).
- edge_cases_or_failure_modes: The template intentionally does not enumerate actual untracked filenames. That avoids long prompt output, but means the agent must run or inspect git status separately to classify each file. The language says generated files should “typically” be ignored, so the parent gate still requires judgment: real source files should be committed, while artifacts should be added to `.gitignore` (`prompt-template/block/git-not-clean-untracked.md:3`, `prompt-template/block/git-not-clean-untracked.md:4`; `prompt-template/block/git-not-clean.md:7`).
- validation_or_tests: Inspected directly with line numbers. Relevant lines are `prompt-template/block/git-not-clean-untracked.md:2` through `prompt-template/block/git-not-clean-untracked.md:10`. I did not execute tests; validation evidence is static inspection of the template and its direct caller/test references in the read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item sections present; item IDs are intentionally shown only in their section headings to preserve the exact-once output invariant`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`