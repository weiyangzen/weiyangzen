# agent_17 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook implementation for the RLCR loop. It is registered as the Claude Code `Stop` hook in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/hooks.json:52), with command `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` and a `7200` second hook timeout. Its job is to intercept attempted exits from an active `.humanize/rlcr/<timestamp>/state.md` loop, run fast local gates first, then run Codex review and either end the loop or block Claude with next-round feedback.
- algorithmic_behavior: The hook reads the full hook JSON input from stdin at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:29), locates the newest active loop via `find_active_loop`, and exits normally if no active loop/state exists at lines 52-67. It parses YAML frontmatter fields from `state.md` using `sed`/`grep`: integrity fields `plan_tracked`, `start_branch`, `plan_file`; iteration fields `current_round`, `max_iterations`, `push_every_round`; Codex fields `codex_model`, `codex_effort`, `codex_timeout` at lines 69-94. It validates `current_round`, defaulting/failing conservatively for malformed state at lines 96-105. It then performs pre-Codex gates in this order: schema, branch consistency, plan backup/original integrity, incomplete TodoWrite transcript status, large changed file detection, git cleanliness, optional unpushed-commit check, summary-file existence, round-0 goal-tracker initialization, and max-iteration stop. Only after those pass does it build a Codex review prompt and execute `codex exec` at lines 547-716.
- inputs_outputs_state: Inputs are hook stdin, `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr/<newest>/state.md`, backup plan `.humanize/rlcr/<newest>/plan.md`, original `$PLAN_FILE`, `round-N-summary.md`, optional `goal-tracker.md`, git status, transcript path inside hook input, prompt templates, Codex CLI, and optional `scripts/portable-timeout.sh`. Outputs include JSON hook decisions on stdout, stderr diagnostic messages, `round-N-review-prompt.md`, `round-N-review-result.md`, next `round-(N+1)-prompt.md`, debug files under `$HOME/.cache/humanize/<sanitized-project>/<loop-timestamp>/`, and state transitions through `state.md` updates or renames. The normal continuation state transition is `current_round: N` to `current_round: N+1` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:882), followed by a block JSON carrying the next prompt at lines 935-947. Terminal transitions call `end_loop` from [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:199), which renames `state.md` to `<reason>-state.md`; reasons used here include `unexpected`, `maxiter`, `complete`, and `stop`.
- gates_or_invariants: The hook intentionally ignores `stop_hook_active`, so repeated blocked-stop attempts still trigger review each iteration, documented at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:31). Required state schema fields are `plan_tracked` and `start_branch`; missing either returns JSON block with system message “state schema outdated” at lines 112-123. Branch switching is forbidden by comparing current git branch to `start_branch` at lines 130-142. Plan mutation is forbidden: missing backup blocks at lines 148-162, deleted original blocks at lines 164-175, tracked uncommitted plan changes block at lines 180-193, and content diff from loop backup blocks at lines 196-216. Incomplete Claude todos block before expensive Codex review at lines 224-253, using [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/check-todos-from-transcript.py:17), which returns exit `1` if latest TodoWrite entries are not `completed`. Changed code/docs files over `2000` lines block at lines 262-335. Any uncommitted git changes block at lines 345-399, and `push_every_round=true` additionally blocks ahead-of-remote commits at lines 405-430. The round summary must exist at lines 438-459. Round 0 goal tracker placeholders must be replaced before exit at lines 465-520. The max iteration gate computes `NEXT_ROUND=CURRENT_ROUND+1` and ends as `maxiter` when beyond limit at lines 527-532.
- dependencies_and_callers: Called by Claude Code via the hook registry, not by normal shell flow. It depends on [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/loop-common.sh:11), which sources the template loader, resolves `TEMPLATE_DIR`, finds the newest active loop, provides `to_lower`, and provides `end_loop`. It depends on [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/template-loader.sh:155) for `load_and_render_safe`; that renderer uses single-pass `{{VAR}}` substitution to avoid placeholder injection at lines 35-43. It depends on [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/scripts/portable-timeout.sh) when present, otherwise defines an inline `run_with_timeout` at hook lines 669-687. Initial state is created by [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/scripts/setup-rlcr-loop.sh:406), which writes `current_round`, `max_iterations`, Codex config, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at`. Related validators share the same loop state; for example [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-plan-file-validator.sh:42) enforces schema/branch/plan tracking at prompt-submit time.
- edge_cases_or_failure_modes: No active loop or missing `state.md` silently allows exit. Corrupt nonnumeric `current_round` ends as `unexpected` at lines 96-100, while nonnumeric `max_iterations` defaults to `42` at lines 103-105. Old schema state blocks as JSON but, despite the nearby comment “terminate loop as unexpected,” does not call `end_loop` in this hook at lines 110-123, so the active `state.md` remains. If `codex` is unavailable, the hook blocks with installation guidance at lines 631-650. Codex nonzero exit, missing review file, failed stdout copy, or empty result all use `codex_failure_exit` and block with debug-file paths at lines 728-826. If Codex writes output only to stdout, the hook copies stdout into `round-N-review-result.md` at lines 773-789. Completion detection is deliberately strict: only the last non-empty line exactly equal to `COMPLETE` ends successfully, avoiding false positives such as “CANNOT COMPLETE,” at lines 831-845. Last line `STOP` triggers circuit-breaker termination even outside full-alignment rounds, with different stderr messaging, at lines 848-875. Full-alignment review is every fifth round when `CURRENT_ROUND % 5 == 4`, affecting prompt template and STOP semantics at lines 559-563 and 848-872.
- validation_or_tests: Focused tests in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-plan-file-hooks.sh:389) cover quoted `plan_file`/`start_branch` parsing for the stop hook; lines 469-560 cover plan modified, deleted, and backup-missing blocks; lines 562-629 cover tracked plan uncommitted modification detection; lines 631-653 cover outdated schema JSON block behavior; lines 655-730 cover committed plan changes that leave git status clean but differ from backup. [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:56) scans this hook and sibling validators for missing template references. README documents `loop-codex-stop-hook.sh` as a lifecycle hook and Codex as a prerequisite at [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/README.md:203).
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-047 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR state-schema gate. It defines the user-facing block reason when an active loop’s `state.md` lacks a required v1.1.2+ field. The template is short but algorithmically relevant because it standardizes the recovery contract for stale loop state.
- algorithmic_behavior: The template at [prompt-template/block/schema-outdated.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/prompt-template/block/schema-outdated.md:1) renders a “State Schema Outdated” message. It interpolates `{{FIELD_NAME}}` into “RLCR loop state file is missing required field” at line 3, explains the loop was started with an older humanize version at line 5, gives three remediation options at lines 7-10, and states the loop will be terminated as `unexpected` to preserve state at line 12.
- inputs_outputs_state: Input is a single template variable, `FIELD_NAME`. Rendering is performed through `load_and_render_safe`, which does single-pass `{{VAR}}` substitution via [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/lib/template-loader.sh:43). The main caller is [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-plan-file-validator.sh:47): `schema_validation_error` receives `field_name`, renders this template with `FIELD_NAME=$field_name` at line 52, JSON-escapes the rendered reason with `jq -Rs` at lines 54-56, and emits `{ "decision": "block", "reason": ... }` at lines 58-63. State is not directly mutated by the template. It describes a terminal/repair expectation; actual mutation must happen in hook logic.
- gates_or_invariants: This template participates in the invariant that active RLCR loops require `plan_tracked` and `start_branch` fields. The plan-file validator declares those required fields at [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-plan-file-validator.sh:67) and emits this block when either value is empty at lines 68-75. The stop hook enforces the same schema gate independently at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-codex-stop-hook.sh:112), but uses an inline combined message for missing `plan_tracked` or `start_branch` instead of this template.
- dependencies_and_callers: Direct caller found: `hooks/loop-plan-file-validator.sh`. Template existence is indirectly guarded by [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-template-references.sh:83), which scans `load_template`, `load_and_render`, and `load_and_render_safe` references in hook scripts and fails missing templates. Repository-wide search found no direct call from `loop-codex-stop-hook.sh`; its schema text is duplicated inline. This means wording can drift between prompt-submit validation and stop-hook validation unless kept synchronized manually.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to an inline string in [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/hooks/loop-plan-file-validator.sh:49). If `FIELD_NAME` is empty, the rendered message would name an empty field, but the current caller only passes keys from a fixed `REQUIRED_FIELDS` array. The template says the loop “will be terminated as 'unexpected',” but the observed direct caller only blocks and does not call `end_loop`; the stop hook’s inline schema gate also blocks without renaming `state.md`. That wording is therefore stronger than the current caller behavior and may be a skip/repair signal for documentation accuracy rather than algorithm execution.
- validation_or_tests: [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/make-working-folder-to-humanize-only/tests/test-plan-file-hooks.sh:121) covers missing `plan_tracked` via the prompt-submit validator and expects `plan_tracked` in the result; lines 141-158 cover missing `start_branch`. The stop-hook-specific outdated schema test at lines 631-653 validates JSON block behavior for old schema state, but that path uses inline hook text rather than this assigned template. Template reference validation includes the plan-file validator and would catch this file being absent.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 Item Evidence headings; each assigned item id is used only in its own heading`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`