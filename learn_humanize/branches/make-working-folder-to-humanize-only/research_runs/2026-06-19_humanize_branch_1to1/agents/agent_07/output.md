# agent_07 make-working-folder-to-humanize-only 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `823cb32d01ae021ec8a0eb3dc1f18af901d2ba65`

## Item Evidence

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-007 `file` `README.md`
- cursor: `[_]`
- core_role: Behavior-defining top-level documentation for the Humanize/RLCR state machine. It defines the intended loop model: user plan -> Claude implementation and summary -> Codex independent review -> either feedback loop or terminal state. The workflow diagram in `README.md:31-39` is the compact algorithm contract, and `README.md:242-266` defines the persistent loop artifact schema.
- algorithmic_behavior: Documents an iterative development loop where `/humanize:start-rlcr-loop <plan.md>` initializes state, Claude implements against a plan, Claude writes `round-N-summary.md`, Codex reviews via `round-N-review-result.md`, and the hook either returns feedback for the next round or exits when Codex says `COMPLETE` or a stop/max/cancel condition applies. The README states plan requirements at `README.md:41-56`, command invocation and options at `README.md:57-71` and `README.md:181-201`, monitoring behavior at `README.md:73-110`, pause/resume/cancel behavior at `README.md:112-129`, goal tracking at `README.md:131-152`, and terminal state file naming at `README.md:255-260`.
- inputs_outputs_state: Primary input is a markdown plan file with clear task description, acceptance criteria, optional technical approach, and at least 5 lines (`README.md:51-55`). Setup creates `.humanize/rlcr/<timestamp>/` (`README.md:242-244`) with `state.md`, backup `plan.md`, `goal-tracker.md`, per-round prompts, summaries, review prompts, and review results (`README.md:246-253`). The loop is active when the newest loop directory contains `state.md` (`README.md:116-118`). Exit states are represented by renaming `state.md` to `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md` (`README.md:255-260`). Runtime Codex command logs are intentionally stored outside the project under `$HOME/.cache/humanize/...` (`README.md:262-266`).
- gates_or_invariants: Key invariants are: acceptance criteria are anchored in the Goal Tracker and should not be forgotten (`README.md:139-143`); the Goal Tracker has an immutable goal/AC section and mutable work-tracking section (`README.md:135-138`); deferred tasks require justification and impact analysis (`README.md:140-143`, `README.md:272-274`); every fourth work interval full-alignment audit can trigger a circuit breaker (`README.md:143-152`); active loop detection is based on `state.md` presence only (`README.md:116-118`). The implementation reinforces these gates: `scripts/setup-rlcr-loop.sh:190-348` validates plan path, git repo, file existence, line count, tracking policy, branch/model YAML safety, and Codex availability; `hooks/loop-codex-stop-hook.sh:52-57` allows exit if no active loop exists; `hooks/loop-codex-stop-hook.sh:96-124` blocks corrupted/outdated state; `hooks/loop-codex-stop-hook.sh:349-399` blocks dirty git before review; `hooks/loop-codex-stop-hook.sh:440-459` blocks missing summaries.
- dependencies_and_callers: The README points to slash commands in `commands/start-rlcr-loop.md` and `commands/cancel-rlcr-loop.md` (`README.md:221-223`). The start command delegates to `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:10-14`), which materializes state and the initial prompt (`scripts/setup-rlcr-loop.sh:388-419`, `scripts/setup-rlcr-loop.sh:521-572`). The stop-hook implements the review transition and terminal state changes (`hooks/loop-codex-stop-hook.sh:831-875`, `hooks/loop-codex-stop-hook.sh:882-940`). Shared active-loop discovery comes from `hooks/lib/loop-common.sh:23-44`, matching the README’s newest-directory/`state.md` rule. Shell monitoring is implemented by `scripts/humanize.sh`, referenced in `README.md:88-110`.
- edge_cases_or_failure_modes: The README’s cancel path renames only the newest loop’s `state.md` to `cancel-state.md` (`README.md:120-127`), so older loop directories are preserved and ignored by active-loop discovery. Documented abnormal outcomes include `unexpected-state.md` for schema/integrity issues (`README.md:260`) and `stop-state.md` for Codex circuit breaker (`README.md:259`). Implementation-level failure modes behind the README include rejecting absolute plan paths, spaces, shell metacharacters, symlinks, paths outside the project, submodule-contained plans, too-short plans, missing Codex, unsafe branch/model values, and untracked/tracked plan policy violations (`scripts/setup-rlcr-loop.sh:220-348`). The README command table omits implementation-supported `--plan-file` and `--track-plan-file`, visible in `scripts/setup-rlcr-loop.sh:47-56`, so the README is a high-level contract rather than a complete CLI reference.
- validation_or_tests: The README itself is documentation, but its described behavior is covered by adjacent command/hook tests. State naming and active-loop path semantics are covered by `tests/test-state-exit-naming.sh` references surfaced in search, including checks that `.humanize/rlcr` is recognized and legacy `.humanize-loop.local` is not. Plan validation is covered by `tests/test-plan-file-validation.sh` and plan-file hook protection by `tests/test-plan-file-hooks.sh`. Template availability for hook messages is covered by `tests/test-template-references.sh:51-115`, and full template loading/rendering behavior by `tests/test-templates-comprehensive.sh:1-10`.
- skip_candidate: `no`

### MAKE_WORKING_FOLDER_TO_HUMANIZE_ONLY-HZ-037 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Prompt block template for the stop-hook dirty-git gate. It defines the user-facing block reason when Claude attempts to exit an active RLCR loop while the repository has uncommitted changes. It is a small file, but algorithmically important because it is the rendered instruction payload for a state-machine transition from “exit attempted” back to “must commit and continue.”
- algorithmic_behavior: The template receives `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` placeholders (`prompt-template/block/git-not-clean.md:3-4`) and renders required actions: optionally run `code-simplifier`, review untracked files, stage real changes, and commit with a descriptive project-convention message (`prompt-template/block/git-not-clean.md:5-10`). It also defines commit-authorship rules forbidding AI attribution (`prompt-template/block/git-not-clean.md:11-14`) and tells the agent to retry exit only after committing (`prompt-template/block/git-not-clean.md:16`).
- inputs_outputs_state: Inputs are template variables passed by the stop hook: `GIT_ISSUES=$GIT_ISSUES` and `SPECIAL_NOTES=$SPECIAL_NOTES` at `hooks/loop-codex-stop-hook.sh:386-388`. `GIT_ISSUES` is set to `uncommitted changes` when `git status --porcelain` is nonempty (`hooks/loop-codex-stop-hook.sh:349-353`). `SPECIAL_NOTES` is assembled from optional `.humanize*` and other-untracked-file templates (`hooks/loop-codex-stop-hook.sh:354-374`). Output is the rendered block assigned to `REASON`, then emitted in hook JSON as `{ "decision": "block", "reason": ..., "systemMessage": ... }` (`hooks/loop-codex-stop-hook.sh:390-397`). State transition is non-terminal: the active `state.md` remains in place and the exit attempt is blocked before Codex review.
- gates_or_invariants: This template enforces the invariant that review/exit cannot proceed while there are staged, unstaged, or untracked changes. The hook runs this check before summary validation and before expensive Codex review (`hooks/loop-codex-stop-hook.sh:338-399`), so dirty git has priority over later review transitions. The template also encodes repository hygiene invariants: distinguish build artifacts from real changes, commit real changes, follow project commit conventions, and avoid AI co-author metadata (`prompt-template/block/git-not-clean.md:7-14`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which loads it through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:386-388`). Template loading is provided by `hooks/lib/template-loader.sh`: template paths are rooted at `prompt-template` (`hooks/lib/template-loader.sh:9-16`), loaded by filename (`hooks/lib/template-loader.sh:21-33`), rendered with single-pass `{{VAR}}` replacement to prevent placeholder injection (`hooks/lib/template-loader.sh:35-117`), and protected by fallback behavior if a template is missing or renders empty (`hooks/lib/template-loader.sh:152-185`). `hooks/lib/loop-common.sh:11-20` wires the loader into all loop hooks and warns rather than failing hard if templates are unavailable.
- edge_cases_or_failure_modes: If `.humanize*` is untracked, the hook appends a special note loaded from `block/git-not-clean-humanize-local.md`, warning that local Humanize state should not be committed (`hooks/loop-codex-stop-hook.sh:357-364`). If other untracked files exist, it appends `block/git-not-clean-untracked.md` or an inline fallback (`hooks/loop-codex-stop-hook.sh:366-374`). If `git-not-clean.md` is missing or empty, `load_and_render_safe` renders an inline fallback beginning `# Git Not Clean` (`hooks/loop-codex-stop-hook.sh:380-388`, `hooks/lib/template-loader.sh:164-185`). Because replacement is single-pass, malicious or accidental placeholder text inside `SPECIAL_NOTES` is not recursively expanded (`hooks/lib/template-loader.sh:39-42`, `hooks/lib/template-loader.sh:56-114`).
- validation_or_tests: Template reference existence is validated by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails missing referenced templates (`tests/test-template-references.sh:51-115`). General template loading/rendering, edge cases, fallback behavior, and placeholder syntax are covered by `tests/test-templates-comprehensive.sh:1-10` and its template directory/load-all checks at `tests/test-templates-comprehensive.sh:59-90`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item evidence headings present, each with separate cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`