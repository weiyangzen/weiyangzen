# agent_22 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is the user-facing entry point that delegates execution to `scripts/setup-rlcr-loop.sh` via the only allowed tool command at `commands/start-rlcr-loop.md:3-14`.
- algorithmic_behavior: Defines the two-stage RLCR algorithm: implementation rounds reviewed by Codex, then review phase using `codex review --base <branch>`, then finalize after no `[P0-9]` findings. The command text documents the transition sequence at `commands/start-rlcr-loop.md:16-25` and restates the two phases at `commands/start-rlcr-loop.md:60-67`.
- inputs_outputs_state: Inputs are `$ARGUMENTS` forwarded to `setup-rlcr-loop.sh`, including plan path, `--plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, and `--skip-impl` from `commands/start-rlcr-loop.md:3`. The setup script creates `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and round prompt/summary files; relevant state fields include `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `push_every_round`, `full_review_round`, `plan_file`, `start_branch`, `base_branch`, `base_commit`, and `review_started` at `scripts/setup-rlcr-loop.sh:709-725`.
- gates_or_invariants: The command requires summaries before exit, goal tracker maintenance, thorough evidence, no state-file/cancel cheating, and trusting Codex feedback at `commands/start-rlcr-loop.md:46-52`. The underlying setup enforces numeric option validation for `--max`, `--codex-timeout`, and `--full-review-round` at `scripts/setup-rlcr-loop.sh:125-160` and `scripts/setup-rlcr-loop.sh:188-202`; plan-file presence is required unless `--skip-impl` is used at `scripts/setup-rlcr-loop.sh:272-290`.
- dependencies_and_callers: Depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:12-14`). Runtime loop transitions depend on `hooks/loop-codex-stop-hook.sh`, which computes full-alignment rounds at `hooks/loop-codex-stop-hook.sh:773-783`, enters code review after implementation completion at `hooks/loop-codex-stop-hook.sh:1394-1433`, reruns code review during review phase at `hooks/loop-codex-stop-hook.sh:1441-1468`, and appends next-round prompts at `hooks/loop-codex-stop-hook.sh:1507-1559`.
- edge_cases_or_failure_modes: `--skip-impl` bypasses plan requirements and starts directly in review mode, documented at `commands/start-rlcr-loop.md:69-86` and implemented with placeholder plan/state setup at `scripts/setup-rlcr-loop.sh:272-281`, `scripts/setup-rlcr-loop.sh:675-731`, and `scripts/setup-rlcr-loop.sh:864-898`. Base branch detection can fail if no local base branch exists; the setup script validates explicit and auto-detected base branches at `scripts/setup-rlcr-loop.sh:590-660`. Plan path validation rejects absolute paths, spaces, metacharacters, symlinks, missing/unreadable files, out-of-project paths, and submodule paths at `scripts/setup-rlcr-loop.sh:320-421`.
- validation_or_tests: Robustness coverage exists around setup options and skip-impl behavior in `tests/robustness/test-setup-scripts-robustness.sh` per repository references, including `--full-review-round` and `--skip-impl` cases. The command itself is metadata plus delegation; validation is primarily in `scripts/setup-rlcr-loop.sh` and stop-hook tests.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for template-loader error handling. It verifies prompt-template loading/rendering fails gracefully rather than crashing hooks that gate RLCR loop transitions.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` at `tests/test-error-scenarios.sh:13`, resolves the template directory through `get_template_dir` at `tests/test-error-scenarios.sh:15`, and runs 12 focused assertions. The behavior under test maps to loader functions defined in `hooks/lib/template-loader.sh`: `load_template` returns empty plus warning for missing files at `hooks/lib/template-loader.sh:36-48`, `render_template` performs single-pass `{{VAR}}` substitution via awk at `hooks/lib/template-loader.sh:58-132`, `load_and_render_safe` falls back on missing/empty templates at `hooks/lib/template-loader.sh:170-203`, and `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:208-222`.
- inputs_outputs_state: Inputs are template directory paths, template names, fallback strings, and `VAR=value` assignments. Outputs are rendered strings, warnings on stderr, pass/fail console output, and final process exit status. Internal state is only `TESTS_PASSED` and `TESTS_FAILED` counters initialized at `tests/test-error-scenarios.sh:22-23` and updated by `pass`/`fail` at `tests/test-error-scenarios.sh:25-35`.
- gates_or_invariants: Missing templates and missing directories must return an empty string with exit 0 for unsafe loader calls (`tests/test-error-scenarios.sh:45-78`). Empty template content must render empty without failure (`tests/test-error-scenarios.sh:83-91`). Strict-mode callers must continue when a template is missing (`tests/test-error-scenarios.sh:111-131`). Safe loading must use fallback text and substitute variables inside fallback text (`tests/test-error-scenarios.sh:137-158`). Valid template dirs must pass and invalid dirs must fail (`tests/test-error-scenarios.sh:163-180`).
- dependencies_and_callers: Depends on Bash, the plugin root layout, and `hooks/lib/template-loader.sh`. It indirectly protects all hook call sites that render blocking prompts through `load_and_render_safe`, such as the git-clean stop gate at `hooks/loop-codex-stop-hook.sh:557-559` and next-round prompt rendering at `hooks/loop-codex-stop-hook.sh:1523-1539`.
- edge_cases_or_failure_modes: Explicitly covers special regex characters in variable values (`tests/test-error-scenarios.sh:94-105`), missing templates under `set -euo pipefail` (`tests/test-error-scenarios.sh:109-131`), empty placeholder `{{}}` left unchanged (`tests/test-error-scenarios.sh:182-193`), and unclosed placeholder `{{UNCLOSED` left unchanged (`tests/test-error-scenarios.sh:195-206`). One subtle contract difference: this script uses `set -uo pipefail` at `tests/test-error-scenarios.sh:9` and separately spawns a strict-mode subshell for `set -euo pipefail`.
- validation_or_tests: Self-validating test runner; prints summary at `tests/test-error-scenarios.sh:211-217` and exits 0 only when `TESTS_FAILED` is zero at `tests/test-error-scenarios.sh:218-225`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-082 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR stop hook’s git-clean invariant. It tells the agent why exit is blocked and what must be committed before the loop can advance.
- algorithmic_behavior: Template accepts `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` placeholders at `prompt-template/block/git-not-clean.md:3-4`. It emits required actions: use `code-simplifier` if available, review untracked files, stage real changes, and commit with a descriptive message at `prompt-template/block/git-not-clean.md:5-10`. It also enforces no AI authorship attribution in commit messages at `prompt-template/block/git-not-clean.md:11-15`.
- inputs_outputs_state: Input values are produced by `hooks/loop-codex-stop-hook.sh`: `GIT_ISSUES` is set to `"uncommitted changes"` when cached `git status --porcelain` is non-empty, and `SPECIAL_NOTES` is assembled from `.humanize` and untracked-file notes at `hooks/loop-codex-stop-hook.sh:516-545`. Output is a rendered markdown block inserted into the stop-hook JSON `reason`, with system message `"Loop: Blocked - $GIT_ISSUES detected, please commit first"` at `hooks/loop-codex-stop-hook.sh:557-568`.
- gates_or_invariants: Prevents exit before expensive Codex review if the working tree has staged, unstaged, or untracked changes. This is a persistence gate: commit real changes first, do not commit local `.humanize` artifacts unless intentional, and do not include AI co-author trailers. The stop hook exits with a block decision at `hooks/loop-codex-stop-hook.sh:548-570`, so this template participates directly in the implementation-to-review/finalize transition guard.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` in `hooks/loop-codex-stop-hook.sh:557`. Uses `hooks/lib/template-loader.sh` single-pass rendering, so `SPECIAL_NOTES` content is inserted without recursive placeholder expansion.
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook falls back to an inline `# Git Not Clean` message at `hooks/loop-codex-stop-hook.sh:551-556`, so the gate remains active. Special notes can include `.humanize*` local-directory guidance from `block/git-not-clean-humanize-local.md` or untracked-file guidance from `block/git-not-clean-untracked.md` via `hooks/loop-codex-stop-hook.sh:529-544`.
- validation_or_tests: Template existence and safe rendering are covered by the template reference/comprehensive tests in the repository. The specific missing-template fallback contract is exercised by `tests/test-error-scenarios.sh:137-158` and the loader implementation at `hooks/lib/template-loader.sh:170-203`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-112 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Next-round instruction fragment appended after a Full Goal Alignment Check. It turns Codex’s alignment findings into action priorities for the following Claude round.
- algorithmic_behavior: The template contains a fixed markdown section telling the next round to address forgotten items, unmet acceptance criteria, and unjustified deferrals at `prompt-template/claude/post-alignment-action-items.md:2-7`. It has no variables; it is conditionally appended verbatim.
- inputs_outputs_state: Input is the stop hook’s computed `FULL_ALIGNMENT_CHECK` boolean. The stop hook computes it from `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` at `hooks/loop-codex-stop-hook.sh:773-783`. Output is appended to `round-${NEXT_ROUND}-prompt.md` only when the check was full-alignment and the template loads non-empty at `hooks/loop-codex-stop-hook.sh:1528-1534`.
- gates_or_invariants: Enforces goal-drift correction after full alignment reviews. It does not block by itself; instead it changes the next-round work priority so ignored tasks, unmet ACs, and bad deferrals are pulled back into the active implementation loop.
- dependencies_and_callers: Called only from `hooks/loop-codex-stop-hook.sh` after `claude/next-round-prompt.md` is rendered at `hooks/loop-codex-stop-hook.sh:1514-1526`. The full-alignment review prompt itself is selected at `hooks/loop-codex-stop-hook.sh:816-829` when the same `FULL_ALIGNMENT_CHECK` flag is true.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook simply skips appending it because it uses `load_template` and checks `[[ -n "$POST_ALIGNMENT" ]]` at `hooks/loop-codex-stop-hook.sh:1530-1533`; this avoids crashes but weakens post-alignment guidance. Invalid or corrupted `full_review_round` values are defaulted to 5 before computing the alignment flag at `hooks/loop-codex-stop-hook.sh:775-779`.
- validation_or_tests: Covered indirectly by template reference tests and setup robustness for `--full-review-round`. Runtime behavior is constrained by the stop-hook conditional append at `hooks/loop-codex-stop-hook.sh:1528-1534`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-142 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Broader robustness test suite for template-system failure modes under strict Bash execution. It expands the core template-loader contract beyond simple missing-file checks into malformed syntax, invalid names, filesystem edge cases, and concurrent loading.
- algorithmic_behavior: Runs with `set -euo pipefail` at `tests/robustness/test-template-error-robustness.sh:13`, sources the template loader and shared test helpers at `tests/robustness/test-template-error-robustness.sh:15-18`, creates an isolated temp directory via `setup_test_dir` at `tests/robustness/test-template-error-robustness.sh:20`, then executes 20 scenario tests. Shared pass/fail/summary behavior comes from `tests/test-helpers.sh:30-78`, and temp cleanup is installed by `tests/test-helpers.sh:86-89`.
- inputs_outputs_state: Inputs are generated temporary template files, fallback strings, unusual placeholder names, unusual variable values, file permissions, symlinks, and concurrent shell jobs. Outputs are pass/fail records and a final exit status from `print_test_summary` at `tests/robustness/test-template-error-robustness.sh:340-341`. State includes `TEST_DIR`, pass/fail counters from the helper, and local arrays/PIDs for concurrency at `tests/robustness/test-template-error-robustness.sh:274-292`.
- gates_or_invariants: Missing template/file directory must use fallback (`tests/robustness/test-template-error-robustness.sh:34-51`). Empty templates must be handled gracefully (`tests/robustness/test-template-error-robustness.sh:53-63`). Malformed placeholders must not crash and should yield non-empty output (`tests/robustness/test-template-error-robustness.sh:73-115`). Template syntax in variable values must not recursively expand (`tests/robustness/test-template-error-robustness.sh:165-174`). Concurrent loads must all produce expected per-job output while preserving the source file (`tests/robustness/test-template-error-robustness.sh:270-299`).
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh` functions and `tests/test-helpers.sh`. The tests correspond to loader implementation choices: single-pass awk scanning at `hooks/lib/template-loader.sh:71-129`, fallback logic at `hooks/lib/template-loader.sh:176-203`, and plain file loading through `cat` at `hooks/lib/template-loader.sh:41-47`.
- edge_cases_or_failure_modes: Covers unclosed placeholders, nested braces, only opening braces, empty placeholder names, spaces/special characters/numeric-only variable names, multiline values, very long variable names, UTF-8 BOM files, whitespace-only templates, filenames with spaces, permission denied, concurrent access, subdirectory loading, and symlinked templates at `tests/robustness/test-template-error-robustness.sh:73-334`. Some cases intentionally accept variable system behavior, such as empty template returning empty or fallback at `tests/robustness/test-template-error-robustness.sh:57-60`, permission handling variance at `tests/robustness/test-template-error-robustness.sh:247-260`, and symlink handling variance at `tests/robustness/test-template-error-robustness.sh:320-334`.
- validation_or_tests: This file is itself the validation harness. It exits with the helper summary status, so any failed assertion yields nonzero exit through `exit $?` at `tests/robustness/test-template-error-robustness.sh:340-341`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FIX_PR_LOOP-HZ-022`, `FIX_PR_LOOP-HZ-052`, `FIX_PR_LOOP-HZ-082`, `FIX_PR_LOOP-HZ-112`, `FIX_PR_LOOP-HZ-142`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`