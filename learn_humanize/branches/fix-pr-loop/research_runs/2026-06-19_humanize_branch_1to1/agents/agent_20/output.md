# agent_20 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` command workflow that converts a draft document into a structured implementation plan. It is a command-level orchestration spec, not shell implementation code. Its stated phases are IO validation, repository relevance check, draft analysis, issue resolution, plan generation, and final write/report, at `commands/gen-plan.md:19`.
- algorithmic_behavior: The command first executes `scripts/validate-gen-plan-io.sh` with `$ARGUMENTS` and branches on exit codes 0 through 7, at `commands/gen-plan.md:30` and `commands/gen-plan.md:38`. After validation, it reads the input draft and delegates repository relevance to `humanize:draft-relevance-checker` with model `haiku`, expecting either `RELEVANT:` or `NOT_RELEVANT:`, at `commands/gen-plan.md:56`. If relevant, it performs multidimensional analysis for clarity, consistency, completeness, and functionality, including Explore subagents for related code paths, at `commands/gen-plan.md:76` and `commands/gen-plan.md:101`. It then asks user questions for unresolved issues and numeric metric hardness, at `commands/gen-plan.md:111` and `commands/gen-plan.md:126`.
- inputs_outputs_state: Inputs are `--input <draft.md>` and `--output <plan.md>` as declared in frontmatter, at `commands/gen-plan.md:3`. The validator normalizes paths, verifies the draft exists and is non-empty, verifies output directory/writeability, rejects existing output, copies `prompt-template/plan/gen-plan-template.md`, and appends the original draft; that companion script behavior is in `scripts/validate-gen-plan-io.sh:69`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:118`, `scripts/validate-gen-plan-io.sh:142`, and `scripts/validate-gen-plan-io.sh:148`. Output is a plan containing goal description, AC-formatted acceptance criteria, path boundaries, feasibility hints, dependency sequence, and implementation notes, specified at `commands/gen-plan.md:148`. State transition is draft plus template to completed plan file, with the original draft preserved at the bottom, required at `commands/gen-plan.md:253`.
- gates_or_invariants: Hard IO gates are exit-code driven: missing input, empty input, missing output directory, existing output, no write permission, invalid args, and missing template stop the workflow, at `commands/gen-plan.md:38`. Relevance is a gate: `NOT_RELEVANT` stops command execution, at `commands/gen-plan.md:67`. Draft content is invariant: original draft content must not be discarded or overridden, at `commands/gen-plan.md:113`, and final plan must be a superset of draft plus clarifications, at `commands/gen-plan.md:247`. Acceptance criteria must use AC-X or AC-X.Y and include both positive and negative tests, at `commands/gen-plan.md:235` and `commands/gen-plan.md:239`. Generation rules forbid time estimates and line-number references, at `commands/gen-plan.md:227` and `commands/gen-plan.md:229`.
- dependencies_and_callers: The command frontmatter permits the validator script, Read/Glob/Grep/Task/Write/AskUserQuestion, at `commands/gen-plan.md:4`. It depends on `agents/draft-relevance-checker.md`, whose required outputs are `RELEVANT:` or `NOT_RELEVANT:`, at `agents/draft-relevance-checker.md:27`. It depends on the plan template at `prompt-template/plan/gen-plan-template.md`, whose skeleton matches the command’s plan shape at `prompt-template/plan/gen-plan-template.md:1`. It is referenced in README user workflow and command table, e.g. `README.md:76` and `README.md:122`. Structural and script validation exists in `tests/test-gen-plan.sh`, especially validator exit-code tests at `tests/test-gen-plan.sh:426`.
- edge_cases_or_failure_modes: The validator rejects `--input` or `--output` without values, unknown flags, non-existent or empty input, missing output directory, output path as directory, and pre-existing output; these are implemented in `scripts/validate-gen-plan-io.sh:29`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:87`, `scripts/validate-gen-plan-io.sh:95`, and `scripts/validate-gen-plan-io.sh:103`. Mixed-language plans trigger an optional AskUserQuestion language-unification path, at `commands/gen-plan.md:272`. If generation cannot be completed, the command must explain missing information and suggest draft improvements, at `commands/gen-plan.md:302`.
- validation_or_tests: `tests/test-gen-plan.sh` validates command/agent frontmatter, naming conventions, YAML syntax, language constraints, and the validator script’s exit codes. Script tests cover invalid arg cases, missing/empty input, missing dir, existing output, output directory, valid path success, and help behavior at `tests/test-gen-plan.sh:438` through `tests/test-gen-plan.sh:536`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR cancellation authorization algorithm. It verifies that a loop state file may only transition to `cancel-state.md` through a very narrow signal-file-authorized `mv`, and that other state file modifications are blocked by the Bash validator.
- algorithmic_behavior: The test constructs an active RLCR loop under a temp project, writes `.humanize/rlcr/<timestamp>/state.md`, sets `CLAUDE_PROJECT_DIR`, and invokes `hooks/loop-bash-validator.sh` through JSON hook input, at `tests/test-cancel-signal-file.sh:42` and `tests/test-cancel-signal-file.sh:63`. Positive cases require `.cancel-requested` in the active loop dir and a command that moves the active state file to `cancel-state.md`, at `tests/test-cancel-signal-file.sh:80` and `tests/test-cancel-signal-file.sh:365`. Negative cases verify the validator blocks writes, in-place edits, wrong destinations, wrong signal directory, removal, injected commands, arbitrary destinations, wrappers, prefixes, redirection prefixes, unrelated paths, and malformed argument shapes.
- inputs_outputs_state: Inputs are synthetic Bash commands wrapped as Claude hook JSON with `tool_name: Bash` and `tool_input.command`, at `tests/test-cancel-signal-file.sh:66`. The state under test is `state.md` plus optional `.cancel-requested`; the authorized transition is `state.md` to `cancel-state.md`. Expected output is validator exit code 0 for allowed cancellation and exit code 2 for blocked state-file modification. The helper-level tests call `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` directly and expect success/failure returns, at `tests/test-cancel-signal-file.sh:1251`.
- gates_or_invariants: The invariant is that `.cancel-requested` alone is not enough; the command must exactly be an `mv` from active-loop `state.md` or `finalize-state.md` to active-loop `cancel-state.md`. The shared implementation documents and enforces this in `hooks/lib/loop-common.sh:533`, including required signal, rejection of command substitution/backticks/newlines/operators, normalization of `$loop_dir`, rejection of remaining `$`, exact argument count, exact source/destination paths, and symlink rejection through `hooks/lib/loop-common.sh:557` through `hooks/lib/loop-common.sh:705`. The Bash validator applies this exception before blocking state modification, at `hooks/loop-bash-validator.sh:139` and `hooks/loop-bash-validator.sh:149`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `to_lower`, and `is_cancel_authorized`, and on `hooks/loop-bash-validator.sh` for end-to-end hook behavior. The real producer is `scripts/cancel-rlcr-loop.sh`, which finds newest loop, creates `.cancel-requested`, and renames active `state.md` or `finalize-state.md` to `cancel-state.md`, at `scripts/cancel-rlcr-loop.sh:85`, `scripts/cancel-rlcr-loop.sh:134`, and `scripts/cancel-rlcr-loop.sh:137`.
- edge_cases_or_failure_modes: Covered edge cases include relative-style `./state.md` normalization at `tests/test-cancel-signal-file.sh:100`, wrong destination at `tests/test-cancel-signal-file.sh:181`, signal in parent directory at `tests/test-cancel-signal-file.sh:201`, `&&` and semicolon injection at `tests/test-cancel-signal-file.sh:243` and `tests/test-cancel-signal-file.sh:263`, command substitution and backticks at `tests/test-cancel-signal-file.sh:300` and `tests/test-cancel-signal-file.sh:320`, newline injection at `tests/test-cancel-signal-file.sh:340`, option/prefix bypasses at `tests/test-cancel-signal-file.sh:383`, `tests/test-cancel-signal-file.sh:549`, and `tests/test-cancel-signal-file.sh:589`, shell wrappers at `tests/test-cancel-signal-file.sh:808`, redirection-prefix variants at `tests/test-cancel-signal-file.sh:848`, unrelated `/tmp/state.md` at `tests/test-cancel-signal-file.sh:1208`, and no-active-loop permissiveness at `tests/test-cancel-signal-file.sh:1229`.
- validation_or_tests: This file is itself the validation suite. It reports pass/fail counters and exits with `TESTS_FAILED`, at `tests/test-cancel-signal-file.sh:1349`. It validates both end-to-end hook exit codes and direct helper behavior. Important coverage includes positive allowed signal cases at `tests/test-cancel-signal-file.sh:76`, broad blocked command families at `tests/test-cancel-signal-file.sh:117`, and direct `is_cancel_authorized` cases at `tests/test-cancel-signal-file.sh:1251`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Small block template used inside the RLCR stop hook’s git-clean gate when untracked `.humanize*` files are detected. It is not the gate implementation, but it affects the remediation contract emitted by that gate.
- algorithmic_behavior: The template emits a special-case note saying `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore`, at `prompt-template/block/git-not-clean-humanize-local.md:2`. The algorithmic caller is the stop hook: when cached git status contains untracked entries and those entries include `.humanize`, it loads this template into `SPECIAL_NOTES`, at `hooks/loop-codex-stop-hook.sh:525` and `hooks/loop-codex-stop-hook.sh:529`. The final block reason is rendered through `block/git-not-clean.md` with `GIT_ISSUES` and `SPECIAL_NOTES`, at `hooks/loop-codex-stop-hook.sh:557`.
- inputs_outputs_state: Inputs are the stop hook’s cached git status and untracked-file classification; this template has no variables. Output is Markdown appended to the stop-hook block reason. State transition is not direct filesystem mutation; it guides the user to append `.humanize*` to `.gitignore` and stage `.gitignore`, at `prompt-template/block/git-not-clean-humanize-local.md:4`.
- gates_or_invariants: The surrounding invariant is that an RLCR loop may not exit into expensive review/completion while the git worktree has uncommitted changes. The stop hook sets `GIT_ISSUES="uncommitted changes"` for non-empty git status and returns a JSON block decision, at `hooks/loop-codex-stop-hook.sh:521` and `hooks/loop-codex-stop-hook.sh:561`. This template narrows the remediation for the `.humanize` local-artifact case and reinforces that `.humanize/` should not be committed.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"`, at `hooks/loop-codex-stop-hook.sh:530`. Template loading behavior comes from `hooks/lib/template-loader.sh`, where `load_template` returns file content or empty on missing template, at `hooks/lib/template-loader.sh:36`. The stop hook has a fallback `"Note: .humanize* directories are intentionally untracked."` if this template is missing, at `hooks/loop-codex-stop-hook.sh:531`.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook substitutes a shorter fallback note, at `hooks/loop-codex-stop-hook.sh:531`. If there are other untracked files as well, a separate untracked note is appended after filtering out `.humanize`, at `hooks/loop-codex-stop-hook.sh:537`. The template’s command uses `echo '.humanize*' >> .gitignore`; repeated application could duplicate the ignore line, but the template is advisory text rather than enforced logic.
- validation_or_tests: No direct test file for this template was found in assigned scope. Its behavior is indirectly covered by stop-hook git-clean blocking tests, e.g. `tests/test-finalize-phase.sh` includes a git-not-clean blocking case at `tests/test-finalize-phase.sh:454`. Template loading safety is covered by `load_template`/fallback behavior in `hooks/lib/template-loader.sh:36` and stop-hook fallback at `hooks/loop-codex-stop-hook.sh:531`.
- skip_candidate: `yes: template is a remediation message plugged into a core git-clean gate, not an independent algorithm`

### FIX_PR_LOOP-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Footer template appended to the generated next-round Claude prompt after Codex review finds issues. It defines behavioral constraints for the next worker round: do not fake loop exit, do not edit loop state files, do not run cancellation, commit changes, and write the summary file.
- algorithmic_behavior: The stop hook increments `current_round`, writes the next prompt path and summary path, renders the next-round prompt body, optionally appends post-alignment instructions, then appends this footer with `NEXT_SUMMARY_FILE` substitution, at `hooks/loop-codex-stop-hook.sh:1505`, `hooks/loop-codex-stop-hook.sh:1511`, and `hooks/loop-codex-stop-hook.sh:1536`. The footer text explicitly forbids lying, editing loop state files, or executing `cancel-rlcr-loop`, at `prompt-template/claude/next-round-footer.md:4`. It then instructs the worker to commit and write its summary into `@{{NEXT_SUMMARY_FILE}}`, at `prompt-template/claude/next-round-footer.md:6`.
- inputs_outputs_state: Input variable is `NEXT_SUMMARY_FILE`, passed by the stop hook as the active loop’s `round-${NEXT_ROUND}-summary.md`, at `hooks/loop-codex-stop-hook.sh:1512` and `hooks/loop-codex-stop-hook.sh:1539`. Output is Markdown appended to `round-${NEXT_ROUND}-prompt.md`. State transition is Codex-review-failed round continuation: `state.md` `current_round` is rewritten to the next round before this template is appended, at `hooks/loop-codex-stop-hook.sh:1506`.
- gates_or_invariants: This is a prompt-level invariant, not a validator: the next Claude round must not terminate the loop by falsifying completion or mutating state. Enforcement is backed elsewhere by hook protections against state-file writes and Bash modification patterns in `hooks/loop-bash-validator.sh:127`, `hooks/loop-write-validator.sh`, and shared state-file messages in `hooks/lib/loop-common.sh:467`. The required summary file path ties into later stop-hook validation that blocks missing work summaries, referenced by the stop hook’s `work-summary-missing` block at `hooks/loop-codex-stop-hook.sh:623`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-footer.md"`, at `hooks/loop-codex-stop-hook.sh:1539`. Rendering uses single-pass template substitution so injected `{{...}}` in values is not recursively expanded, at `hooks/lib/template-loader.sh:54` and `hooks/lib/template-loader.sh:71`.
- edge_cases_or_failure_modes: If missing or empty, the stop hook fallback only says to commit changes and write the summary, at `hooks/loop-codex-stop-hook.sh:1537`; that fallback omits the stronger “do not lie / do not edit state / do not cancel” language. The template references `code-simplifier` conditionally if installed, at `prompt-template/claude/next-round-footer.md:7`; this is advisory and depends on plugin availability.
- validation_or_tests: No direct test was found for this exact template. It is indirectly exercised when the stop hook creates a next-round prompt after review issues, in the code path beginning `Review Found Issues - Continue Loop` at `hooks/loop-codex-stop-hook.sh:1501`. Template rendering behavior is covered by the shared loader semantics in `hooks/lib/template-loader.sh:50`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-140 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It validates tolerant parsing, strict parsing, defaulting behavior, and resilience to malformed or unusual `state.md` content.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared test helpers, creates a temp directory, writes many synthetic state files, and calls `parse_state_file`, `parse_state_file_strict`, and `get_current_round`, at `tests/robustness/test-state-file-robustness.sh:16`, `tests/robustness/test-state-file-robustness.sh:19`, and `tests/robustness/test-state-file-robustness.sh:52`. Positive tests verify normal frontmatter parsing, `current_round` extraction, ignored extra fields, quoted values, zero values, large values, special characters, full-review interval parsing, and default full-review interval. Negative/robustness tests verify strict rejection and tolerant fallback behavior for missing frontmatter, non-numeric fields, missing required fields, empty files, partial YAML, malformed YAML, nonexistent files, binary suffixes, and CRLF.
- inputs_outputs_state: Inputs are temporary Markdown state files with YAML frontmatter. `parse_state_file` sets global variables such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, and `STATE_FULL_REVIEW_ROUND`, described in `hooks/lib/loop-common.sh:191`. `get_current_round` outputs a scalar round number, defaulting to 0, at `hooks/lib/loop-common.sh:176`. `parse_state_file_strict` returns nonzero validation codes for missing file/frontmatter/required fields/non-numeric fields, described at `hooks/lib/loop-common.sh:246`.
- gates_or_invariants: Tolerant parsing applies defaults for missing `current_round`, `max_iterations`, `push_every_round`, and `full_review_round`, at `hooks/lib/loop-common.sh:234`. Strict parsing requires at least two `---` separators, requires `current_round`, `max_iterations`, `review_started`, and `base_branch`, and validates numeric round/iteration fields and boolean `review_started`, at `hooks/lib/loop-common.sh:263`, `hooks/lib/loop-common.sh:289`, and `hooks/lib/loop-common.sh:307`. This matters because hooks use strict parsing to fail closed for active loop protection, e.g. `hooks/loop-bash-validator.sh:81`.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh` parser functions and `tests/test-helpers.sh` counters/summary, at `tests/robustness/test-state-file-robustness.sh:16` and `tests/test-helpers.sh:58`. Parser functions are shared by read/write/bash validators and stop hooks; the common library header lists `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh`, at `hooks/lib/loop-common.sh:5`.
- edge_cases_or_failure_modes: Strict parser rejects missing YAML separators, non-numeric `current_round`, and missing required fields, at `tests/robustness/test-state-file-robustness.sh:149`, `tests/robustness/test-state-file-robustness.sh:173`, and `tests/robustness/test-state-file-robustness.sh:201`. Tolerant parser defaults missing fields to `current_round=0` and `max_iterations=10`, at `tests/robustness/test-state-file-robustness.sh:217`. Malformed YAML without a colon returns default 0 for `get_current_round`, at `tests/robustness/test-state-file-robustness.sh:259`. Binary content after YAML preserves parsed round, at `tests/robustness/test-state-file-robustness.sh:367`. CRLF is only required not to crash, at `tests/robustness/test-state-file-robustness.sh:386`. Minimum valid `full_review_round=2` and default 5 are tested at `tests/robustness/test-state-file-robustness.sh:425` and `tests/robustness/test-state-file-robustness.sh:450`.
- validation_or_tests: This file is itself the robustness suite. It prints a summary and exits through `print_test_summary`, at `tests/robustness/test-state-file-robustness.sh:480`. It asserts both parser return codes and parsed variable values across 21 named tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence sections present; each assigned item appears once as an Item Evidence heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`