# agent_21 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-021 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the PR review loop. The file is not the implementation itself; it authorizes and invokes `${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh` with user arguments (`commands/start-pr-loop.md:3-14`), then documents the PR-loop algorithm that the setup script and stop hook enforce.
- algorithmic_behavior: Defines the high-level loop: detect the PR for the current branch, fetch monitored bot review comments, require the agent to analyze/fix issues, push changes, trigger bot re-review with the provided mentions, write a resolution summary, then let the Stop Hook poll and run local Codex validation (`commands/start-pr-loop.md:16-24`, `commands/start-pr-loop.md:37-46`). It also specifies priority order: human comments first, then newest bot comments (`commands/start-pr-loop.md:31-35`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, primarily `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout` (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). The invoked setup script creates `.humanize/pr-loop/<timestamp>/state.md` with round, max iterations, PR number, configured/active bots, Codex settings, polling settings, startup case, latest commit, trigger timestamp, and trigger comment id (`scripts/setup-pr-loop.sh:554-573`). It also writes the initial prompt and goal tracker (`scripts/setup-pr-loop.sh:651-652`, `scripts/setup-pr-loop.sh:697-790`) and prints the activation payload (`scripts/setup-pr-loop.sh:799-827`).
- gates_or_invariants: At least one bot flag is mandatory (`commands/start-pr-loop.md:25-30`; enforced in `scripts/setup-pr-loop.sh:179-190`). The workflow requires summaries before exit, pushed fixes, correct bot mentions, no state-file tampering, and Stop Hook ownership of polling/validation (`commands/start-pr-loop.md:50-56`). The setup script gates on no active RLCR/PR loop (`scripts/setup-pr-loop.sh:220-241`), valid git repo and commit (`scripts/setup-pr-loop.sh:243-253`), authenticated `gh` and available `codex` (`scripts/setup-pr-loop.sh:255-277`), an open PR (`scripts/setup-pr-loop.sh:351-374`), YAML-safe branch/model/effort values (`scripts/setup-pr-loop.sh:384-407`), and optional Claude eyes-reaction confirmation when a re-review trigger comment is posted (`scripts/setup-pr-loop.sh:503-540`).
- dependencies_and_callers: Depends directly on `scripts/setup-pr-loop.sh`. That script sources `hooks/lib/loop-common.sh` for `find_active_loop`, `find_active_pr_loop`, `build_bot_mention_string`, and YAML-list helpers (`scripts/setup-pr-loop.sh:210-214`; `hooks/lib/loop-common.sh:764-814`). It also coordinates with `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, Git, GitHub CLI, Codex CLI, prompt templates under `prompt-template/pr-loop/`, and the Stop Hook.
- edge_cases_or_failure_modes: No bot flag fails early. Existing RLCR/PR loops block new setup. Fork PRs are handled by resolving the PR lookup/base repo before API calls (`scripts/setup-pr-loop.sh:290-379`). A merged/closed PR blocks. Case 4/5 startup posts a trigger comment and aborts with cleanup if the comment or required Claude eyes reaction cannot be verified (`scripts/setup-pr-loop.sh:462-540`). If there are no comments, Round 0 instructs the agent not to trigger review manually because first reviews are automatic (`scripts/setup-pr-loop.sh:705-740`).
- validation_or_tests: Covered by PR-loop integration and setup-script tests referenced in the repo search surface, including `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-scripts.sh`, and `tests/robustness/test-setup-scripts-robustness.sh`. This assigned command file is declarative; the executable validation lives around the setup script and hooks.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-051 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the Codex review-log merge/detection algorithm implemented by `detect_review_issues()` in `hooks/lib/loop-common.sh`. The test header states the expected algorithm: scan the whole log, find the first line with `[P<digit>]` in the first 10 characters, extract from that line to EOF, and return no-issue status if absent (`tests/test-codex-review-merge.sh:5-15`).
- algorithmic_behavior: Creates isolated `LOOP_DIR` and `CACHE_DIR`, sources `loop-common.sh`, writes synthetic `round-N-codex-review.log` files, invokes `detect_review_issues N`, and asserts return codes plus output/result-file side effects (`tests/test-codex-review-merge.sh:37-54`, `tests/test-codex-review-merge.sh:72-80`). The implementation uses `awk` over `substr($0, 1, 10)` to locate the first `[P0-9]`, then `sed` to extract from that line through the end (`hooks/lib/loop-common.sh:349-387`).
- inputs_outputs_state: Input is a round number and the globals `CACHE_DIR` and `LOOP_DIR`. The input log path is `$CACHE_DIR/round-${round}-codex-review.log`; the audit output path is `$LOOP_DIR/round-${round}-review-result.md` (`hooks/lib/loop-common.sh:349-352`). Exit `0` means issues found and emits a `## Codex Review Issues` block; exit `1` means no issue markers; exit `2` means missing or empty log and is a hard error (`hooks/lib/loop-common.sh:332-337`, `hooks/lib/loop-common.sh:354-392`).
- gates_or_invariants: Only `[P0-9]` markers in the first 10 characters count. Markers later in a line must be ignored (`tests/test-codex-review-merge.sh:83-107`). The scan must cover the entire file, not a tail window, including markers after line 50 and markers near the beginning of long logs (`tests/test-codex-review-merge.sh:171-227`). Extraction starts at the first matching marker and includes all following content (`tests/test-codex-review-merge.sh:229-255`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` (`tests/test-codex-review-merge.sh:41-42`) plus standard shell tools. The main caller is `hooks/loop-codex-stop-hook.sh`, where `run_and_handle_code_review()` runs `codex review`, calls `detect_review_issues`, blocks on exit `2`, continues the loop on exit `0`, and enters finalize on no issues (`hooks/loop-codex-stop-hook.sh:999-1027`).
- edge_cases_or_failure_modes: Tests missing log and empty log as hard errors (`tests/test-codex-review-merge.sh:133-169`), marker at position zero (`tests/test-codex-review-merge.sh:257-278`), dash-prefixed marker (`tests/test-codex-review-merge.sh:280-303`), exactly 50 lines (`tests/test-codex-review-merge.sh:332-354`), multiple issue lines where the first marker controls the extraction start (`tests/test-codex-review-merge.sh:229-255`), and result-file creation when issues are found (`tests/test-codex-review-merge.sh:305-330`).
- validation_or_tests: The file is itself the validation suite. It tracks pass/fail counters and exits nonzero on any failure (`tests/test-codex-review-merge.sh:356-368`). It specifically protects against regressions where Codex review output is truncated, tail-only scanned, or treated as approval when the log is missing.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-081 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block used by the Stop Hook’s git-clean gate when untracked files other than `.humanize*` are present. It is a small but core transition template because it explains how the agent should resolve a blocked exit caused by untracked artifacts.
- algorithmic_behavior: The template warns that untracked files may be build artifacts, test outputs, runtime files, dependencies, IDE files, logs, caches, or temp files, and instructs the agent to review them and add appropriate `.gitignore` patterns (`prompt-template/block/git-not-clean-untracked.md:2-10`). It does not parse files itself; it is injected into the larger git-not-clean block.
- inputs_outputs_state: Input is the Stop Hook’s derived `OTHER_UNTRACKED` set from cached `git status` lines beginning with `??` but excluding `.humanize` paths (`hooks/loop-codex-stop-hook.sh:521-545`). Output is text appended into `SPECIAL_NOTES`, then rendered into the Stop Hook JSON block reason (`hooks/loop-codex-stop-hook.sh:548-568`). No state file changes occur; the state transition is a blocked exit before expensive review/finalize work.
- gates_or_invariants: Any nonempty git status marks `GIT_ISSUES="uncommitted changes"` and blocks exit until committed or otherwise resolved (`hooks/loop-codex-stop-hook.sh:521-570`). `.humanize*` untracked files are treated separately with a local-note template; other untracked files trigger this assigned block (`hooks/loop-codex-stop-hook.sh:528-545`). The invariant is that Codex review should not run against unsaved/uncommitted work.
- dependencies_and_callers: Loaded with `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` from `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:537-545`). Template loading is provided by `hooks/lib/template-loader.sh`, where missing templates return empty content and safe rendering can fall back (`hooks/lib/template-loader.sh:33-48`, `hooks/lib/template-loader.sh:167-180`).
- edge_cases_or_failure_modes: If this template is missing, the stop hook falls back to a terse “Review untracked files - add to .gitignore or commit them.” note (`hooks/loop-codex-stop-hook.sh:540-543`). The template is advisory and does not list concrete paths; the parent git-clean block supplies the blocked decision. Untracked source files that should be committed are not distinguished here from generated artifacts, so the agent must inspect them before choosing `.gitignore` versus commit.
- validation_or_tests: Validation is indirect through Stop Hook behavior and template-loading tests. The relevant runtime check is the git-clean gate in `hooks/loop-codex-stop-hook.sh:510-570`; this template defines the explanatory branch for untracked-file resolution.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-111 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing next-round prompt template for continuing the RLCR loop after Codex finds issues. It is a transition contract between Codex review output and the agent’s next implementation pass.
- algorithmic_behavior: Instructs the agent that work is not finished, requires reading the original plan, requires creating Todos for all discovered issues instead of only the most important ones, embeds Codex’s review result, and requires reading the goal tracker without modifying it after Round 0 (`prompt-template/claude/next-round-prompt.md:1-31`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{GOAL_TRACKER_FILE}}` (`prompt-template/claude/next-round-prompt.md:3-31`). The Stop Hook updates `current_round` to `NEXT_ROUND`, writes `round-${NEXT_ROUND}-prompt.md`, renders this template with those variables, appends optional post-alignment instructions, footer, push note, and goal-tracker update-request template, then returns a JSON block decision with the prompt as `reason` (`hooks/loop-codex-stop-hook.sh:1505-1569`).
- gates_or_invariants: The template gates the next round on alignment with the original plan (`prompt-template/claude/next-round-prompt.md:3-8`), exhaustive Todo creation for all discovered issues (`prompt-template/claude/next-round-prompt.md:12-13`), and read-only treatment of `goal-tracker.md` after Round 0 (`prompt-template/claude/next-round-prompt.md:22-31`). The template loader uses single-pass substitution so review content containing `{{...}}` is not recursively expanded or corrupted (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-129`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` after review issues are found (`hooks/loop-codex-stop-hook.sh:1501-1527`). It depends on `hooks/lib/template-loader.sh`, the Codex review/detection path, and companion templates such as `claude/next-round-footer.md`, `claude/post-alignment-action-items.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md` (`hooks/loop-codex-stop-hook.sh:1528-1556`).
- edge_cases_or_failure_modes: If the template is missing, the Stop Hook fallback only includes a short next-round instruction, Codex review content, and references to the plan/goal tracker (`hooks/loop-codex-stop-hook.sh:1514-1527`), so loss of this template weakens the exhaustive-Todo and goal-tracker-readonly guidance. If goal tracker changes are needed, the agent must request them in the summary rather than directly editing the file (`prompt-template/claude/next-round-prompt.md:30-31`).
- validation_or_tests: Validated indirectly by Stop Hook prompt-generation behavior and template-loader tests. The assigned template’s functional contract is enforced at runtime by the Stop Hook’s blocked exit response for “Codex found issues to address” (`hooks/loop-codex-stop-hook.sh:1558-1569`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-141 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state transition and discovery behavior. It verifies valid state progression, finalize/cancel handling, strict schema rejection, and active-loop directory selection (`tests/robustness/test-state-transition-robustness.sh:3-9`).
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers, creates isolated loop directories, writes `state.md`, `finalize-state.md`, and `cancel-state.md`, then exercises `parse_state_file_strict`, `get_current_round`, and `find_active_loop` (`tests/robustness/test-state-transition-robustness.sh:14-19`, `tests/robustness/test-state-transition-robustness.sh:30-89`). The implementation selects only the lexicographically newest direct child loop directory and treats `state.md` or `finalize-state.md` as active indicators (`hooks/lib/loop-common.sh:151-174`).
- inputs_outputs_state: Inputs are generated YAML-frontmatter state files. Outputs are parsed globals/round values, active-loop paths, pass/fail lines, and a final test summary (`tests/robustness/test-state-transition-robustness.sh:423-424`). State transitions modeled include normal round 0 and later rounds, max round, move into finalize state, cancellation ending activity, and a newer loop after cancellation (`tests/robustness/test-state-transition-robustness.sh:98-207`).
- gates_or_invariants: Strict parser requires at least two YAML separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`; it rejects nonnumeric `current_round`/`max_iterations` and non-boolean `review_started` (`hooks/lib/loop-common.sh:263-329`). The tests assert missing `current_round`, `max_iterations`, and `base_branch` are rejected (`tests/robustness/test-state-transition-robustness.sh:292-361`). Cancel-state alone is not active; finalize-state is active (`tests/robustness/test-state-transition-robustness.sh:148-191`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-state-transition-robustness.sh:14-18`). The tested helpers are used by setup scripts and hook validators, including read/write/edit/bash validators and the Stop Hook, to locate active loops and reject corrupted state before allowing tool actions.
- edge_cases_or_failure_modes: Negative rounds are parsed as numeric and accepted by `get_current_round`, even though they may indicate an invalid state (`tests/robustness/test-state-transition-robustness.sh:217-236`; `hooks/lib/loop-common.sh:307-310`). Rounds exceeding `max_iterations` are parsed, with enforcement documented as elsewhere (`tests/robustness/test-state-transition-robustness.sh:238-257`). Non-numeric rounds are rejected by strict parsing (`tests/robustness/test-state-transition-robustness.sh:259-282`). Active-loop discovery is lexicographic, not timestamp-validated, and ignores nested state files in the wrong location (`tests/robustness/test-state-transition-robustness.sh:371-417`).
- validation_or_tests: The file is itself the validation suite, ending with `print_test_summary` and `exit $?` (`tests/robustness/test-state-transition-robustness.sh:419-424`). It protects state-machine behavior that would otherwise revive stale loops, miss finalize mode, treat canceled sessions as active, or accept malformed state schema.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item evidence headers present; each assigned row is represented once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`