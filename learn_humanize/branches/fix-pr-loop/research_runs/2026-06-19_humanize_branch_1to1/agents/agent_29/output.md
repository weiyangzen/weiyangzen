# agent_29 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Read` gate for the RLCR file-state machine. It prevents agents from reading stale or misplaced round prompt/summary/todos files while allowing ordinary reads and PR-loop reads outside the RLCR round-file patterns.
- algorithmic_behavior: Reads hook JSON from stdin, validates structure/depth, exits immediately for non-`Read` tools, then requires `tool_input.file_path` (`hooks/loop-read-validator.sh:25-49`). It first blocks `round-*-todos.md` reads unless the file is allowlisted for the active loop (`hooks/loop-read-validator.sh:55-63`). For `round-*-summary.md` and `round-*-prompt.md`, it locates the active RLCR loop, parses `state.md` or `finalize-state.md`, extracts the current round, checks the file's round number, and enforces exact active-loop path equality (`hooks/loop-read-validator.sh:69-170`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.file_path`. Output is normally no stdout/stderr and exit `0` for allowed reads; exit `1` for malformed hook input or malformed state; exit `2` with rendered block text for user-correctable policy violations. State inputs are `$CLAUDE_PROJECT_DIR`, optional `$PROJECT_ROOT`, optional `$LOOP_BASE_DIR`, optional `$LOOP_DIR`, active `.humanize/rlcr/<session>/state.md`, and optional `finalize-state.md` (`hooks/loop-read-validator.sh:80-96`).
- gates_or_invariants: Requires valid JSON, non-deeply-nested input, and a non-empty `file_path` for `Read` (`hooks/loop-read-validator.sh:27-46`). Todos are native TodoWrite-only except a small allowlist (`hooks/loop-read-validator.sh:55-61`). Round prompt/summary reads must be in `.humanize/rlcr`, must match the current round unless allowlisted, and must equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` exactly (`hooks/loop-read-validator.sh:122-168`). Malformed state fails closed (`hooks/loop-read-validator.sh:94-98`).
- dependencies_and_callers: Registered as the `Read` PreToolUse hook in `hooks/hooks.json:33-40`. Depends on `hooks/lib/loop-common.sh` for `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, `to_lower`, `is_round_file_type`, `find_active_loop`, `parse_state_file_strict`, `extract_round_number`, `is_allowlisted_file`, and `todos_blocked_message`. Uses rendered block templates through `load_and_render_safe` for wrong location, wrong round, and wrong directory responses (`hooks/loop-read-validator.sh:127-130`, `144-149`, `163-166`).
- edge_cases_or_failure_modes: If there is no active loop, summary/prompt round-file reads are allowed because there is no current RLCR state to compare against (`hooks/loop-read-validator.sh:84-86`). Files that look like round summaries/prompts but lack a parseable round number are allowed (`hooks/loop-read-validator.sh:105-108`). `finalize-state.md` overrides `state.md`, so finalize phase can use a distinct current round (`hooks/loop-read-validator.sh:88-92`). Exact string comparison for the final path gate means symlinks, relative aliases, or path normalization differences can block even if they resolve to the same file (`hooks/loop-read-validator.sh:157-168`).
- validation_or_tests: Directly covered by allowlist tests: `round-1-todos.md` and `round-0-summary.md` are allowed, while `round-3-todos.md` and `round-3-summary.md` are blocked (`tests/test-allowlist-validators.sh:234-281`). Supporting allowlist semantics are tested at function level, including wrong-directory rejection (`tests/test-allowlist-validators.sh:72-126`). Hook robustness tests also exercise invalid JSON, missing fields, deep nesting, and concurrent read validation paths.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file immutability and loop-state safety across UserPromptSubmit, Write, Edit, Bash, and Stop hooks.
- algorithmic_behavior: Builds temporary git repositories and active `.humanize/rlcr/<timestamp>` loop directories, then feeds synthetic Claude hook payloads into hook scripts (`tests/test-plan-file-hooks.sh:30-88`). It verifies the UserPromptSubmit plan validator accepts valid state, parses quoted YAML values, rejects missing required state fields, and blocks branch switching (`tests/test-plan-file-hooks.sh:93-191`). It verifies write/edit/bash validators block mutation of the loop `plan.md` backup and direct `.humanize/rlcr/plan.md` paths (`tests/test-plan-file-hooks.sh:197-272`). It then exercises bypass prevention, stop-hook plan integrity, tracked/untracked plan file rules, goal-tracker placeholder detection, and legacy path handling (`tests/test-plan-file-hooks.sh:274-1091`).
- inputs_outputs_state: Inputs are generated hook JSON strings for `Write`, `Edit`, `Bash`, and empty `{}` for UserPromptSubmit/Stop. The test mutates git branch state, tracked status, plan file content, loop `state.md`, loop `plan.md`, `round-0-summary.md`, and `goal-tracker.md`. Outputs are `PASS`/`FAIL`/`SKIP` counters and final process exit equal to `$TESTS_FAILED` (`tests/test-plan-file-hooks.sh:26-32`, `1082-1091`).
- gates_or_invariants: Active loop state must contain v1.5.0-style fields including `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` (`tests/test-plan-file-hooks.sh:75-87`). The plan validator must fail closed on malformed schema and branch mismatch (`tests/test-plan-file-hooks.sh:123-191`). The plan backup in `.humanize/rlcr/.../plan.md` is immutable to Write/Edit/Bash (`tests/test-plan-file-hooks.sh:200-258`). Stop hook must block if the source plan is modified, deleted, missing backup, dirty when tracked, or content-diverged even after committed changes (`tests/test-plan-file-hooks.sh:483-749`). Goal-tracker required sections must be populated with section-specific placeholder diagnostics (`tests/test-plan-file-hooks.sh:751-1036`).
- dependencies_and_callers: Calls `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` via `$PROJECT_ROOT` (`tests/test-plan-file-hooks.sh:99`, `204`, `221`, `238`, `433`). Uses git commands for repository setup, branch switching, tracking status, and committed-change simulations. Depends on temporary directories and `CLAUDE_PROJECT_DIR` to isolate hook state from the real checkout (`tests/test-plan-file-hooks.sh:31-32`, `95`, `635`, `651`, `814`).
- edge_cases_or_failure_modes: Covers YAML-quoted `plan_file` and `start_branch`, quoted branch mismatch, hyphenated plan paths, and stop-hook quote parsing (`tests/test-plan-file-hooks.sh:108-121`, `347-474`). Covers Bash bypass attempts using command substitution, glob expansion, brace expansion, pipes through `tee`, and backticks (`tests/test-plan-file-hooks.sh:278-341`). Covers tracked-plan race conditions where git status is dirty and where content changes are already committed so git status is clean (`tests/test-plan-file-hooks.sh:576-749`). Confirms legacy `.humanize-loop.local` is no longer treated as an active loop path and is allowed by Bash/Write/Edit validators (`tests/test-plan-file-hooks.sh:1041-1080`).
- validation_or_tests: The file is itself the validation script. It encodes expected exits: `0` with no output for allowed UserPromptSubmit cases, `0` plus JSON/block text for prompt-submit policy blocks, `2` for PreToolUse mutation blocks, and JSON `"decision": "block"` for stop-hook integrity failures (`tests/test-plan-file-hooks.sh:97-120`, `182-189`, `203-210`, `512-554`, `660-668`). It exits nonzero if any assertion fails (`tests/test-plan-file-hooks.sh:1082-1091`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-089 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the large-file quality gate. It defines the user-facing remediation contract when modified code or documentation files exceed the configured line limit.
- algorithmic_behavior: Provides a markdown block with placeholders `{{MAX_LINES}}` and `{{LARGE_FILES}}`, explains why large files are blocked, and gives separate required actions for code and documentation files (`prompt-template/block/large-files.md:1-25`). It does not execute logic itself; it is rendered by the stop hook after file scanning detects violations.
- inputs_outputs_state: Inputs are template variables supplied by `load_and_render_safe`: maximum allowed line count and a rendered list of offending files. Output is markdown used as the JSON `reason` in a stop-hook block response (`hooks/loop-codex-stop-hook.sh:486-505`).
- gates_or_invariants: Requires code files to be split into smaller modules under the line limit with strictly unchanged functionality (`prompt-template/block/large-files.md:13-17`). Requires documentation files to be split into logical sections with cross-references, narrative continuity, and updated navigation (`prompt-template/block/large-files.md:19-23`). Final instruction requires committing the split before trying to exit again (`prompt-template/block/large-files.md:25`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` during “Quick Check: Large File Detection.” The hook scans changed git-status entries, classifies extensions as code or documentation, counts lines, appends offenders to `LARGE_FILES`, then renders `block/large-files.md` (`hooks/loop-codex-stop-hook.sh:427-496`).
- edge_cases_or_failure_modes: The caller only checks files present in `GIT_STATUS_CACHED` and skips deleted files, unsupported extensions, non-numeric `wc -l` output, and non-git contexts (`hooks/loop-codex-stop-hook.sh:435-480`). The template’s “commit the changes” instruction is generic; the actual algorithmic block is the stop-hook JSON decision, not this file.
- validation_or_tests: Template reference coverage exists through template-reference tests, and runtime rendering is tied to the stop-hook large-file branch at `hooks/loop-codex-stop-hook.sh:486-505`. I did not find a dedicated large-file fixture in the assigned test file; coverage appears indirect via template existence/rendering and stop-hook path scanning.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-119 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Plan-generation schema template that constrains how a draft becomes a deterministic `plan.md` for RLCR execution. It defines acceptance criteria, scope bounds, allowed choices, sequencing, and code-style constraints.
- algorithmic_behavior: Establishes required plan sections: title, goal description, acceptance criteria with positive and negative tests, path boundaries, feasibility hints, dependencies/sequence, and implementation notes (`prompt-template/plan/gen-plan-template.md:1-74`). The acceptance-criteria structure drives deterministic verification by requiring pass and fail/rejection examples per criterion (`prompt-template/plan/gen-plan-template.md:6-23`). Path boundaries define upper/lower acceptable scope and explicit allowed/prohibited choices (`prompt-template/plan/gen-plan-template.md:25-44`).
- inputs_outputs_state: Input is a design draft passed to the gen-plan flow. `scripts/validate-gen-plan-io.sh` validates input/output paths, copies this template, then appends the original draft between `--- Original Design Draft Start ---` and `--- Original Design Draft End ---` (`scripts/validate-gen-plan-io.sh:133-160`). Output is the combined prompt/input artifact used to generate a concrete plan.
- gates_or_invariants: Acceptance criteria must include positive and negative tests for deterministic verification (`prompt-template/plan/gen-plan-template.md:6-23`). Deterministic designs with no real implementation choice should make upper/lower bounds converge and mark choices as fixed (`prompt-template/plan/gen-plan-template.md:39-44`). Feasibility hints are explicitly non-prescriptive (`prompt-template/plan/gen-plan-template.md:46-55`). Implementation code/comments must not leak plan workflow markers such as `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:69-74`).
- dependencies_and_callers: Used by `scripts/validate-gen-plan-io.sh`, resolved from `$CLAUDE_PLUGIN_ROOT/prompt-template/plan/gen-plan-template.md` or script-relative fallback (`scripts/validate-gen-plan-io.sh:133-149`). Its structure is mirrored in `commands/gen-plan.md` Phase 5 plan-generation instructions (`commands/gen-plan.md:142-185`).
- edge_cases_or_failure_modes: If the template file is missing, `validate-gen-plan-io.sh` treats it as a plugin configuration error and exits `7` (`scripts/validate-gen-plan-io.sh:142-145`). The template is a schema/prompt contract rather than an executable validator; downstream compliance depends on the generator and later hooks/tests interpreting the produced plan.
- validation_or_tests: `scripts/validate-gen-plan-io.sh` validates IO preconditions before copying the template and appending the draft (`scripts/validate-gen-plan-io.sh:120-160`). Related plan robustness tests inspect generated plan shape and comments, but this assigned file itself is not executable.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none detected
- final_worker_status: `complete`