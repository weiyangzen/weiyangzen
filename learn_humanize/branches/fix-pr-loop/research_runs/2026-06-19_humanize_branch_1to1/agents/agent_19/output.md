# agent_19 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow contract for canceling the active RLCR loop. The frontmatter restricts execution to `scripts/cancel-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh --force`, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-5`), making the markdown file the operator-facing state-transition policy rather than the implementation itself.
- algorithmic_behavior: The command first runs the cancel script (`commands/cancel-rlcr-loop.md:11-15`), branches only on the first output line (`commands/cancel-rlcr-loop.md:17-22`), reports normal cancellation statuses directly, and enters a confirmation sub-flow only for `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:23-34`). The underlying script finds the newest `.humanize/rlcr` directory, treats `state.md` as normal loop and `finalize-state.md` as Finalize Phase, then creates `.cancel-requested` and renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69-99`, `scripts/cancel-rlcr-loop.sh:134-151`).
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT`, the active loop files under `.humanize/rlcr/<timestamp>/`, the script's first-line status, and a user confirmation answer during Finalize Phase. Outputs are the user-facing cancellation/no-loop message plus optional forced script execution. State transitions are `state.md -> cancel-state.md` for normal cancellation and `finalize-state.md -> cancel-state.md` only after `--force` or script-level force path.
- gates_or_invariants: The key invariant is explicit: all cancellation logic belongs to the script, and active-loop detection depends on `state.md` or `finalize-state.md` in the newest loop directory (`commands/cancel-rlcr-loop.md:36-38`). Finalize Phase is protected by a human confirmation gate unless `--force` is selected (`commands/cancel-rlcr-loop.md:23-34`; `scripts/cancel-rlcr-loop.sh:117-127`).
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-loop.sh` for all filesystem mutation and status codes. The command is paired with `/humanize:start-rlcr-loop`, whose help points users to `/humanize:cancel-rlcr-loop` as the stopping path (`scripts/setup-rlcr-loop.sh:102-105`, `scripts/setup-rlcr-loop.sh:1000`).
- edge_cases_or_failure_modes: `NO_LOOP` covers no loop directory; `NO_ACTIVE_LOOP` covers a loop directory without active state (`scripts/cancel-rlcr-loop.sh:75-99`). Finalize cancellation without force exits with status code 2 and does not mutate state (`scripts/cancel-rlcr-loop.sh:117-127`). Unknown cancel-script options exit as other error (`scripts/cancel-rlcr-loop.sh:57-60`).
- validation_or_tests: Direct behavior is embodied by `scripts/cancel-rlcr-loop.sh`; broader cancellation and finalize behavior have related test surfaces in the repository such as `tests/test-cancel-signal-file.sh` and `tests/test-finalize-phase.sh`. I did not execute tests because this run is research-only and the branch export is read-only.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `command_modifies_file` in `hooks/lib/loop-common.sh`; it defines which Bash command strings count as unauthorized file modifications for protected RLCR artifacts (`tests/test-bash-validator-patterns.sh:3-13`).
- algorithmic_behavior: The test lowercases each command through `to_lower`, calls `command_modifies_file`, and increments pass/fail counters through `assert_modifies` and `assert_not_modifies` (`tests/test-bash-validator-patterns.sh:24-62`). It covers redirection, `tee`, in-place editors, `mv`/`cp`/`rm`, `dd`, `truncate`, file-descriptor `exec`, read-only false positives, and alternate target regexes for `state.md` and `round-N-summary.md` (`tests/test-bash-validator-patterns.sh:69-194`).
- inputs_outputs_state: Inputs are literal shell command strings plus a target regex, defaulting to `goal-tracker\\.md` (`tests/test-bash-validator-patterns.sh:37-55`). Outputs are colored PASS/FAIL lines and a final exit status of 0 only when `TESTS_FAILED` remains zero (`tests/test-bash-validator-patterns.sh:197-214`). Test state is limited to `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: The central invariant is high recall for write-like commands while avoiding read-only false positives. Read-only commands such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` must not trigger (`tests/test-bash-validator-patterns.sh:136-152`). Case-insensitivity is enforced by lowercasing before matching (`tests/test-bash-validator-patterns.sh:40-43`, `hooks/lib/loop-common.sh:394-397`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`tests/test-bash-validator-patterns.sh:11-13`). The implementation under test is the regex list in `command_modifies_file`, including redirect, `tee`, editor, file operation, `dd`, `truncate`, `printf`, and `exec` patterns (`hooks/lib/loop-common.sh:1068-1095`).
- edge_cases_or_failure_modes: Explicitly tests redirection with no command and no space (`tests/test-bash-validator-patterns.sh:161-163`). It documents a known limitation: multi-source `cp file1 file2 goal-tracker.md` is not detected because the implementation expects `cp src dest` shape (`tests/test-bash-validator-patterns.sh:165-168`). Variable indirection like `echo x > $FILE` is intentionally not matched (`tests/test-bash-validator-patterns.sh:170-172`).
- validation_or_tests: This file is itself the focused validation suite for the Bash modification detector. I inspected it but did not execute it during this read-only research pass.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Block-message template for the Bash validator when a `git add` command could stage `.humanize/` local loop state. It is a policy surface for repository hygiene and loop-state isolation (`prompt-template/block/git-add-humanize.md:1-7`).
- algorithmic_behavior: The template explains why the command was blocked, directs the agent toward specific file adds or patch mode (`prompt-template/block/git-add-humanize.md:8-15`), lists broad/direct add commands that are unsafe (`prompt-template/block/git-add-humanize.md:16-25`), and provides a safe `.gitignore` update sequence (`prompt-template/block/git-add-humanize.md:26-34`).
- inputs_outputs_state: Input is a blocked Bash command classified by `git_adds_humanize`. Output is rendered markdown returned by `git_add_humanize_blocked_message`, loaded from this template when available (`hooks/lib/loop-common.sh:1028-1065`). The state transition is a control-flow block: the attempted `git add` does not proceed, and the user is redirected to narrower staging or `.gitignore` repair.
- gates_or_invariants: The invariant is that `.humanize/` is local loop state and must not enter version control (`prompt-template/block/git-add-humanize.md:1-6`). The backing detector blocks direct `.humanize` references, force-add broad scopes, `git add -A/--all` when `.humanize` exists, and broad adds when `.humanize` is not ignored (`hooks/lib/loop-common.sh:908-1026`).
- dependencies_and_callers: Called through `git_add_humanize_blocked_message` in `hooks/lib/loop-common.sh:1028-1065`, and triggered by `hooks/loop-bash-validator.sh` when `git_adds_humanize` reports a match (`hooks/loop-bash-validator.sh:115`). Related path-variant coverage exists in `tests/test-humanize-escape.sh`, which targets quoted paths, `./.humanize`, nested `.humanize`, and broad add behavior.
- edge_cases_or_failure_modes: The template warns that even the commit message for adding `.humanize*` to `.gitignore` must avoid the literal protected string, because the protection can trigger on command text (`prompt-template/block/git-add-humanize.md:26-34`). The implementation avoids over-blocking names like `.humanizeconfig` by requiring `.humanize` path-boundary matches (`hooks/lib/loop-common.sh:967-972`).
- validation_or_tests: Template loading is validated indirectly by template-reference/comprehensive suites, and behavioral detection is covered by `tests/test-humanize-escape.sh`. I inspected the template and the detector call path, not the full test execution.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-109 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing prompt block that defines the sanctioned handoff format for Goal Tracker changes after direct modification is no longer allowed (`prompt-template/claude/goal-tracker-update-request.md:2-16`).
- algorithmic_behavior: The block instructs Claude to include a `Goal Tracker Update Request` section only when updates are needed, with requested changes and justification (`prompt-template/claude/goal-tracker-update-request.md:2-14`). It makes Codex the reviewer/updater of the Goal Tracker rather than letting Claude mutate it directly (`prompt-template/claude/goal-tracker-update-request.md:16`).
- inputs_outputs_state: Input is Claude's assessment that task status, open issues, plan evolution, or deferrals need to be reflected. Output is a markdown section in Claude's round summary. State transition is indirect: requested changes move from Claude summary to Codex review, then Codex may update `goal-tracker.md` if justified.
- gates_or_invariants: The gate is justification tied to the Ultimate Goal (`prompt-template/claude/goal-tracker-update-request.md:12-16`). This supports the broader invariant that after Round 0 Claude requests Goal Tracker changes rather than directly editing the protected file; `prompt-template/claude/next-round-prompt.md:30-31` repeats that rule.
- dependencies_and_callers: Loaded into next-round prompts by `hooks/loop-codex-stop-hook.sh`, which reads this template and appends it to `NEXT_PROMPT_FILE` (`hooks/loop-codex-stop-hook.sh:1552-1556`). The Codex-side companion responsibility appears in `prompt-template/codex/goal-tracker-update-section.md`, and direct-write blocks reference the same request mechanism (`hooks/lib/loop-common.sh:1098-1107`).
- edge_cases_or_failure_modes: If no update is needed, the section should be omitted. If requested changes lack a justification or do not serve the Ultimate Goal, the template explicitly leaves acceptance to Codex review (`prompt-template/claude/goal-tracker-update-request.md:12-16`). If the template is unavailable, the stop hook falls back to a short instruction (`hooks/loop-codex-stop-hook.sh:1552-1555`).
- validation_or_tests: Template presence and cross-template references are part of the repository template suites, with `tests/test-templates-comprehensive.sh` checking related Goal Tracker update wording. I inspected the template and load site only.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-139 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, covering argument parsing, plan validation, git constraints, YAML safety, mutual exclusion, symlink protection, timeout behavior, full-review-round bounds, and skip-implementation mode (`tests/robustness/test-setup-scripts-robustness.sh:3-10`).
- algorithmic_behavior: The script creates isolated temp repos and mocks, then runs 45 named cases. Helpers create minimal plans, initialize git repos, and wrap setup calls with `CLAUDE_PROJECT_DIR` to isolate state (`tests/robustness/test-setup-scripts-robustness.sh:15-87`). Test groups cover RLCR args (`:96-214`), plan validation (`:224-341`), YAML safety (`:351-402`), git repo edge cases (`:412-463`), PR-loop args (`:473-525`), mutual exclusion (`:535-595`), symlink protection (`:605-658`), positive parse paths (`:668-739`), timeout scenarios (`:749-866`), full-review-round (`:876-965`), and skip-impl mode (`:975-1067`).
- inputs_outputs_state: Inputs include CLI args, temporary markdown plans, fake active `.humanize/rlcr` and `.humanize/pr-loop` state files, symlinks, mock `codex`, mock `gh`, and mock `timeout`/`gtimeout`. Outputs are PASS/FAIL counters from `tests/test-helpers.sh` and final `print_test_summary`/exit code (`tests/robustness/test-setup-scripts-robustness.sh:1069-1074`; `tests/test-helpers.sh:30-78`). State transitions under test include creation of `.humanize/rlcr/<timestamp>/state.md`, `.review-phase-started`, and skip-impl `goal-tracker.md`.
- gates_or_invariants: RLCR setup must reject invalid numeric args, unknown options, duplicate plan-file forms, missing plans unless skip-impl, non-git repos, repos without commits, unsafe plan paths, symlink plan paths, tracked plans without `--track-plan-file`, unsafe branch/model values, and already-active loops. These gates map to implementation lines in `scripts/setup-rlcr-loop.sh:120-220`, `:240-307`, `:320-472`, `:483-536`, and `:561-587`. PR setup similarly requires bot selection, git repo, `gh`, auth, and `codex` (`scripts/setup-pr-loop.sh:110-190`, `:243-277`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for counters/temp cleanup and `scripts/portable-timeout.sh` for timeout-compatible command execution (`tests/robustness/test-setup-scripts-robustness.sh:17-20`; `scripts/portable-timeout.sh:9-76`). Primary implementation dependencies are `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `hooks/lib/loop-common.sh` active-loop detection, and system `git`.
- edge_cases_or_failure_modes: Negative `--max` is tested in both `--max=-5` and `--max -5` forms because shell option parsing can treat `-5` as a flag (`tests/robustness/test-setup-scripts-robustness.sh:129-153`). Branch names with colons may be skipped as environment-dependent if Git refuses to create them (`:351-380`). Symlink tests pass as environment-dependent if symlink creation is unsupported (`:620-630`, `:648-658`). Timeout `0` and very large timeout values are accepted by current regex behavior despite the "positive integer" message wording (`:749-799`).
- validation_or_tests: This file is itself the validation suite. I did not execute it because it intentionally creates temp repos, files, symlinks, and mock executables, while this assignment requires research notes only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 of 5 assigned section headers present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`