# agent_18 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling an active PR loop. It is a thin command surface that delegates all operational behavior to `scripts/cancel-pr-loop.sh`; it does not implement cancellation itself.
- algorithmic_behavior: The command restricts allowed tools to two Bash invocations of the cancellation script, normal and `--force`, in `commands/cancel-pr-loop.md:3`. The workflow instructs the operator/agent to run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"` and branch on the script’s first output line in `commands/cancel-pr-loop.md:11-19`. Its state model is explicitly file-based: a PR loop is active when `state.md` exists in the newest `.humanize/pr-loop/` loop directory, stated in `commands/cancel-pr-loop.md:21`.
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT`, the slash-command invocation, and the current project’s `.humanize/pr-loop/` state directory. Outputs are human-facing responses derived from script sentinels: `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED` per `commands/cancel-pr-loop.md:17-19`. The underlying script confirms the concrete state transition: it resolves `PROJECT_ROOT="${CLAUDE_PROJECT_DIR:-$(pwd)}"`, uses `.humanize/pr-loop`, picks the lexicographically newest loop directory, creates `.cancel-requested`, and renames `state.md` to `cancel-state.md` in `scripts/cancel-pr-loop.sh:72-122`.
- gates_or_invariants: The command-level invariant is that cancellation logic must stay inside the script, not inside prompt reasoning, per `commands/cancel-pr-loop.md:21`. Another invariant is loop-domain separation: this command only affects PR loops and must not cancel RLCR loops, stated in `commands/cancel-pr-loop.md:25` and echoed by `scripts/cancel-pr-loop.sh:54-56`. The script gates active cancellation on the presence of `state.md`; if the newest loop directory exists but has no active state file, it emits `NO_ACTIVE_LOOP` and exits nonzero in `scripts/cancel-pr-loop.sh:88-98`.
- dependencies_and_callers: Depends directly on `scripts/cancel-pr-loop.sh` through the allowed-tools declaration and workflow body. Related command surface is `commands/cancel-rlcr-loop.md`, referenced as the RLCR-specific alternative in `commands/cancel-pr-loop.md:25`. The broader PR-loop setup and stop-hook messaging reference this command as the operator cancellation path, e.g. `scripts/setup-pr-loop.sh:100` and `hooks/pr-loop-stop-hook.sh:1174`.
- edge_cases_or_failure_modes: If `.humanize/pr-loop` has no child loop directory, script emits `NO_LOOP` and exits `1` (`scripts/cancel-pr-loop.sh:76-82`). If the newest directory lacks `state.md`, script emits `NO_ACTIVE_LOOP` and exits `1` (`scripts/cancel-pr-loop.sh:91-98`). `--force` is accepted but currently has no additional effect (`scripts/cancel-pr-loop.sh:24-31`, `scripts/cancel-pr-loop.sh:39-41`). Unknown options exit `3` (`scripts/cancel-pr-loop.sh:60-64`). Round, max-iteration, and PR number metadata default to `?` if missing, keeping cancellation available even with partial state metadata (`scripts/cancel-pr-loop.sh:104-112`).
- validation_or_tests: The assigned command is covered by PR-loop script tests: `tests/test-pr-loop-scripts.sh` includes a cancel-pr-loop section and checks help behavior, no-loop sentinel behavior, and active cancellation behavior according to the `rg` references at `tests/test-pr-loop-scripts.sh:169-243`. I also performed read-only syntax validation with `bash -n scripts/cancel-pr-loop.sh`, which returned success.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-048 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable regression specification for portable ANSI escape stripping and numeric pass/fail extraction in the aggregate test runner.
- algorithmic_behavior: The file models the parsing algorithm used by `tests/run-all-tests.sh`: assign `esc=$'\033'`, strip SGR color sequences with `sed "s/${esc}\\[[0-9;]*m//g"`, then extract summary counts with `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed...` followed by `grep -oE '[0-9]+$'`. This exact production algorithm appears in `tests/run-all-tests.sh:121-125`, while this test states its portability purpose in `tests/test-ansi-parsing.sh:3-6` and uses the same escape variable in `tests/test-ansi-parsing.sh:45-47`.
- inputs_outputs_state: Inputs are synthetic colored and plain strings representing test-suite output. Outputs are pass/fail counters in `TESTS_PASSED` and `TESTS_FAILED`, initialized in `tests/test-ansi-parsing.sh:18-19`, updated by local `pass` and `fail` functions in `tests/test-ansi-parsing.sh:21-31`, and summarized at `tests/test-ansi-parsing.sh:163-178`. Exit status is `0` only when no local failures occurred; otherwise `1`.
- gates_or_invariants: The parser must work with Bash ANSI-C quoted escape bytes and BSD/GNU `sed`, as documented in `tests/test-ansi-parsing.sh:38-40`. It must remove color-only sequences without damaging payload text, parse colored numeric counts, preserve zero counts, handle multiline summaries with `tail -1`, work on plain text without ANSI sequences, and handle combined bold/color attributes. These invariants are encoded across tests 1-8 in `tests/test-ansi-parsing.sh:42-158`.
- dependencies_and_callers: Production caller is `tests/run-all-tests.sh`, which includes this suite in `TEST_SUITES` at `tests/run-all-tests.sh:32-44` and applies the parsing method to each child suite’s captured output at `tests/run-all-tests.sh:104-128`. It depends on standard shell tools `sed`, `grep`, and `tail`; no repo library is sourced. It intentionally duplicates small local `pass`/`fail` helpers rather than using `tests/test-helpers.sh`.
- edge_cases_or_failure_modes: The ANSI pattern only strips SGR sequences ending in `m`, so non-SGR control sequences are out of scope. Count extraction defaults to `0` only through `|| echo "0"` when grep fails, as seen in `tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`, and `tests/test-ansi-parsing.sh:124-125`. The multiline case uses `tail -1`, guarding against earlier incidental count lines in the same captured output (`tests/test-ansi-parsing.sh:124-125`). The script uses `set -uo pipefail` rather than `set -e`, so individual failed comparisons accumulate into the summary instead of aborting immediately (`tests/test-ansi-parsing.sh:9`).
- validation_or_tests: This file is itself the validation asset. It is also enrolled in the full test runner at `tests/run-all-tests.sh:43`. I performed read-only syntax validation with `bash -n tests/test-ansi-parsing.sh`, which returned success.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when a PR loop detects a non-fast-forward branch rewrite. It defines the human-facing transition contract after force-push detection.
- algorithmic_behavior: The template explains that the tracked commit SHA changed from `{{OLD_COMMIT}}` to `{{NEW_COMMIT}}` in a non-fast-forward manner (`prompt-template/block/force-push-detected.md:3`) and states the algorithmic consequence: force pushes reset review state (`prompt-template/block/force-push-detected.md:5`). It then instructs the next required transition: the loop has updated tracking, the user must post a new trigger comment, and the comment should mention `{{BOT_MENTION_STRING}}` (`prompt-template/block/force-push-detected.md:7-15`).
- inputs_outputs_state: Inputs are template variables `OLD_COMMIT`, `NEW_COMMIT`, and `BOT_MENTION_STRING`; `PR_NUMBER` is also passed by the call site but not consumed by this template. The rendered output becomes the `reason` field in a stop-hook JSON block decision. At the call site, force-push handling updates state fields `latest_commit_sha`, `latest_commit_at`, clears `last_trigger_at`, and clears `trigger_comment_id` before rendering this template (`hooks/pr-loop-stop-hook.sh:395-409`).
- gates_or_invariants: The gate is non-fast-forward detection: current `HEAD` must differ from the stored PR latest commit, and the stored commit must not be an ancestor of the current head. That check is implemented with `git merge-base --is-ancestor` in `hooks/pr-loop-stop-hook.sh:372-379`. Once triggered, stale trigger state is invalidated before any further review-cycle progress (`hooks/pr-loop-stop-hook.sh:395-409`), and the hook exits with `decision: block` to require an explicit new trigger (`hooks/pr-loop-stop-hook.sh:418-420`).
- dependencies_and_callers: Primary caller is `hooks/pr-loop-stop-hook.sh`, which loads it through `load_and_render_safe "$TEMPLATE_DIR" "block/force-push-detected.md"` at `hooks/pr-loop-stop-hook.sh:414-416`. Rendering depends on `hooks/lib/template-loader.sh`; its template engine uses single-pass `{{VARIABLE}}` substitution and preserves missing placeholders, documented in `hooks/lib/template-loader.sh:7-14` and implemented in `hooks/lib/template-loader.sh:50-131`.
- edge_cases_or_failure_modes: If the PR latest commit SHA is empty, force-push detection is skipped (`hooks/pr-loop-stop-hook.sh:369`). If current head cannot be resolved, detection is skipped because `CURRENT_HEAD` is empty (`hooks/pr-loop-stop-hook.sh:370-374`). If GitHub commit timestamp lookup fails, the hook falls back to current UTC time (`hooks/pr-loop-stop-hook.sh:387-393`), which may conservatively affect trigger-comment timestamp validation. If the template is missing or renders empty, `load_and_render_safe` falls back to an inline force-push message (`hooks/pr-loop-stop-hook.sh:411-416`; fallback behavior in `hooks/lib/template-loader.sh:167-203`).
- validation_or_tests: Template loader behavior is covered by `tests/test-template-loader.sh` according to repository references, and the force-push call path is part of `hooks/pr-loop-stop-hook.sh`. No direct dedicated test for this exact template was found in the inspected references, but the rendering mechanism and stop-hook force-push branch provide the validation surface.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template for RLCR sessions where code review was skipped. It encodes a degraded-validation transition and the manual verification contract before loop completion.
- algorithmic_behavior: The template begins with a warning parameterized by `{{REVIEW_SKIP_REASON}}` and states that implementation could not be fully validated (`prompt-template/claude/finalize-phase-skipped-prompt.md:1-5`). It then instructs manual verification steps: review code changes, run available tests, and check common quality issues (`prompt-template/claude/finalize-phase-skipped-prompt.md:7-13`). It permits optional simplification via the `code-simplifier:code-simplifier` Task subagent (`prompt-template/claude/finalize-phase-skipped-prompt.md:15-24`) but constrains it to functionality-equivalent cleanup (`prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`). Before exiting, it requires completed todos, a commit, and a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`).
- inputs_outputs_state: Template inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`; these are supplied by `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh:1069-1075`. The output is a rendered blocking reason returned to Claude in JSON, with `decision: block` and a phase-specific system message (`hooks/loop-codex-stop-hook.sh:1105-1110` and continuation immediately after).
- gates_or_invariants: The transition to this skipped-review finalization path happens only when `enter_finalize_phase` receives a non-empty skip reason (`hooks/loop-codex-stop-hook.sh:1042-1075`). One concrete skip path is missing `base_branch`, which sets `REVIEW_SKIPPED="true"` and `REVIEW_SKIP_REASON="No base_branch configured for code review"` in `hooks/loop-codex-stop-hook.sh:1413-1418`. The prompt’s invariants are non-negotiable: do not change existing functionality, do not fail existing tests, do not introduce bugs, and only perform functionality-equivalent changes (`prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, specifically `enter_finalize_phase` (`hooks/loop-codex-stop-hook.sh:1030-1103`). Rendering depends on `load_and_render_safe` and the single-pass template renderer in `hooks/lib/template-loader.sh:50-131` and `hooks/lib/template-loader.sh:167-203`. It references session artifacts `@{{PLAN_FILE}}` and `@{{GOAL_TRACKER_FILE}}` in `prompt-template/claude/finalize-phase-skipped-prompt.md:35-38`, meaning downstream agent behavior depends on those files existing and being readable.
- edge_cases_or_failure_modes: If this template is missing, the hook uses an inline fallback with similar instructions (`hooks/loop-codex-stop-hook.sh:1043-1067`). If any variable is missing, the template engine preserves the unresolved placeholder rather than recursively substituting or deleting it, per `hooks/lib/template-loader.sh:7-14` and `hooks/lib/template-loader.sh:115-122`. The prompt acknowledges reduced assurance because review was skipped; therefore its output relies on manual verification and available tests rather than the normal code-review gate.
- validation_or_tests: Finalize-phase behavior has a dedicated suite referenced by `rg`, including multiple `find_active_loop` checks in `tests/test-finalize-phase.sh`. The hook contains both normal finalize and skipped-review rendering paths (`hooks/loop-codex-stop-hook.sh:1042-1103`). Template-rendering semantics are covered by `tests/test-template-loader.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-138 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for active session detection, specifically `find_active_loop` behavior under multiple sessions, missing state, finalized/cancelled sessions, unusual names, symlinks, and shallow-vs-deep directory layout.
- algorithmic_behavior: The test imports the production function from `hooks/lib/loop-common.sh` (`tests/robustness/test-session-robustness.sh:14-17`) and repeatedly constructs temporary `.humanize/rlcr`-like directory trees under `TEST_DIR`. The production algorithm being specified is `find_active_loop`: if the base directory does not exist, return empty; otherwise list immediate child directories, reverse-sort them, select the newest, and return it only if it contains `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151-174`).
- inputs_outputs_state: Inputs are filesystem states under `$TEST_DIR/rlcr`, created by `setup_test_dir` from `tests/test-helpers.sh:84-89`. Outputs are the path string printed by `find_active_loop` and local pass/fail counters managed by `tests/test-helpers.sh:30-44`. The script exits with the status from `print_test_summary` at `tests/robustness/test-session-robustness.sh:322-323`, whose implementation returns nonzero when failures exist (`tests/test-helpers.sh:58-77`).
- gates_or_invariants: Active loops are represented only by `state.md` or `finalize-state.md`, not by `complete-state.md` or `cancel-state.md`. That invariant is tested positively in `tests/robustness/test-session-robustness.sh:33-61` and negatively in `tests/robustness/test-session-robustness.sh:259-285`. The function intentionally checks only the newest immediate child directory, ignoring older active-looking directories if the newest lacks an active state or is finished (`tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:287-302`). It must not require timestamp-formatted names (`tests/robustness/test-session-robustness.sh:179-192`) and must not recurse deeply (`tests/robustness/test-session-robustness.sh:224-238`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop` and `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and summary output (`tests/robustness/test-session-robustness.sh:16-19`). The function is used by RLCR validators and hooks, including `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` per repository references. The test is included in the aggregate runner at `tests/run-all-tests.sh:55-58`.
- edge_cases_or_failure_modes: Covered edge cases include empty base directory (`tests/robustness/test-session-robustness.sh:128-138`), nonexistent base directory (`tests/robustness/test-session-robustness.sh:140-148`), files without subdirectories (`tests/robustness/test-session-robustness.sh:150-162`), directories with no state files (`tests/robustness/test-session-robustness.sh:164-177`), symlinked session directories (`tests/robustness/test-session-robustness.sh:194-208`), spaces in names (`tests/robustness/test-session-robustness.sh:210-222`), rapid creation (`tests/robustness/test-session-robustness.sh:240-257`), and a placeholder Unicode/naming grace case that actually uses `session-test` (`tests/robustness/test-session-robustness.sh:304-316`). The production algorithm’s reliance on `ls | sort -r | head -1` means “newest” is lexicographic by path/name, not filesystem mtime; this is consistent with the tests.
- validation_or_tests: This file is the validation asset and is enrolled in `tests/run-all-tests.sh:57`. I performed read-only syntax validation with `bash -n tests/robustness/test-session-robustness.sh`, which returned success. I did not execute the full test because it creates temporary fixtures and the assignment requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`