# agent_16 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness specification suite for the RLCR loop, PR loop, hook validators, monitor parsers, template loader, timeout wrapper, git state parser, setup scripts, and state-machine helpers. The directory has no subdirectories and contains 17 executable shell specs totaling 8,611 lines / 267,229 bytes.
- algorithmic_behavior: Each script creates isolated temp fixtures through `tests/test-helpers.sh` (`setup_test_dir`, `pass`, `fail`, `print_test_summary`; lines 30-88) and then either invokes production scripts directly or sources production helper libraries. Child roles are:
  - `test-setup-scripts-robustness.sh`: setup CLI parsing and startup gates for `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, including `--max`, `--codex-timeout`, `--codex-model`, `--full-review-round`, `--skip-impl`, active loop collision, git repo requirements, and plan tracking/path safety.
  - `test-hook-input-robustness.sh`: JSON input and monitor edge cases for `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-bash-validator.sh`, plus monitor terminal/log handling and parser availability.
  - `test-hook-system-robustness.sh`: validator and stop-hook behavior for edit/write/plan/bash hooks, protected `state.md`, `goal-tracker.md`, summary paths, corrupted state, missing state, large/deep/null JSON payloads, and concurrent hook invocations.
  - `test-pr-loop-api-robustness.sh`: PR-loop state discovery, mocked `gh` API failures, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, bot comment formatting, rate-limit/network errors, JSON output contracts, and approval directory layout.
  - `test-path-validation-robustness.sh`: direct production path-validation tests against `setup-rlcr-loop.sh`, covering relative allowed paths and rejecting absolute paths, spaces, shell metacharacters, symlink files, symlink parents, empty/comment-only/short/nonexistent/directory plans.
  - `test-plan-file-robustness.sh`: assigned separately below; validates plan content and file edge cases through production setup.
  - `test-state-file-robustness.sh`: tolerant vs strict state parsing; validates YAML frontmatter, required fields, numeric fields, defaults, CRLF, binary content, `full_review_round`.
  - `test-state-transition-robustness.sh`: RLCR state-machine phases and directory selection: `state.md`, `finalize-state.md`, `cancel-state.md`, round values, missing fields, lexicographic newest-session selection.
  - `test-concurrent-state-robustness.sh`: concurrent state reads, read-during-write behavior, active-loop lookup race boundaries, malformed frontmatter rejection, PR-loop state YAML list handling.
  - `test-session-robustness.sh`: `find_active_loop` newest-directory semantics, finalize detection, finished/cancelled-loop exclusion, symlink/special-name/deep path behavior.
  - `test-cancel-security-robustness.sh`: cancel authorization command grammar, `.cancel-requested` signal requirement, injection rejection, path normalization, symlink rejection.
  - `test-git-operations-robustness.sh`: `humanize_parse_git_status` and `humanize_detect_git_state` for clean/dirty/staged/untracked/deleted/renamed/binary paths, detached HEAD, merge/rebase/shallow/non-git states.
  - `test-goal-tracker-robustness.sh`: `humanize_parse_goal_tracker` counts ACs/tasks/issues/deferred/completed states, goal-summary extraction/truncation, malformed/truncated/binary markdown.
  - `test-template-error-robustness.sh` and `test-template-stress-robustness.sh`: `render_template` / `load_and_render_safe` missing files, malformed placeholders, invalid variable names, BOM, permission, symlink, concurrency, large values/templates, regex/sed-special characters, single-pass placeholder injection prevention.
  - `test-timeout-robustness.sh`: `scripts/portable-timeout.sh` implementation detection, exit-code preservation, timeout code 124, zero timeout, bad commands, signal behavior, pipelines, subshells.
  - `test-base-branch-detection.sh`: extracted setup branch-detection algorithm: remote default, local `main`, local `master`, no-branch failure.
- inputs_outputs_state: Inputs are shell CLI args, temp git repos, synthetic `.humanize/rlcr/*` and `.humanize/pr-loop/*` state trees, mocked `gh`, mocked `codex`, JSON hook payloads, plan files, templates, and terminal/log fixtures. Outputs are process exit codes, stdout/stderr messages, generated `.humanize` loop state files, approval/finalize/cancel markers, rendered block messages, parsed pipe-delimited monitor values, JSON from PR polling, and test summary counters.
- gates_or_invariants: The suite encodes fail-closed behavior for plan path/content gates in `scripts/setup-rlcr-loop.sh` lines 320-390 and 483-532; newest-session-only lookup in `hooks/lib/loop-common.sh` lines 151-174; strict state parser requirements in lines 246-320; hook block messages/protected state paths in lines 463-500; summary-location enforcement in `hooks/loop-write-validator.sh` lines 198-209; and template single-pass substitution in `hooks/lib/template-loader.sh` lines 50-131.
- dependencies_and_callers: Depends on Bash, git, `jq` where PR polling output is validated, `scripts/portable-timeout.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, hook scripts under `hooks/`, `hooks/lib/loop-common.sh`, and `hooks/lib/template-loader.sh`. The suite coordinates with parent `tests/` via `tests/test-helpers.sh`; it complements broader system tests such as `tests/test-pr-loop-hooks.sh`, which also calls `tests/setup-monitor-test-env.sh`.
- edge_cases_or_failure_modes: Covers injection strings, command substitution, `&&`, `||`, pipes, semicolons, newlines, mixed quotes, `IFS`, absolute/escaped/symlinked paths, binary/null bytes, Unicode, CRLF, very large files, very long lines, missing/corrupt YAML, missing required state fields, detached/shallow/non-git repositories, API failures/timeouts, concurrent reads, concurrent template loads, narrow/wide terminals, ANSI/binary logs, and missing or stale active-loop directories.
- validation_or_tests: The directory itself is the validation surface. I inspected recursively with `rg --files tests/robustness`, `find tests/robustness -type f`, line counts, headers, test markers, and production references. I did not execute the full suite because the task is read-only research notes and the specs create temp repos/mocks; no repository files were modified.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Fixture generator for PR-loop monitor tests. It creates `.humanize/pr-loop/<timestamp>/state.md` layouts that encode bot-list parsing states used by monitor and PR-loop display logic.
- algorithmic_behavior: The script requires a `test_dir` positional arg and optional scenario name (`default` if omitted) at lines 12-18. A `case` dispatch at lines 20-89 creates one of three fixtures: `yaml_list` with both `configured_bots` and `active_bots` containing `claude` and `codex` (lines 21-41), `configured` with configured bots `claude,codex` but active bot only `codex` for partial approval (lines 43-62), and `empty` with `active_bots:` present but no list entries for all-approved display (lines 64-82). Unknown scenario names fail with usage-like options and exit 1 at lines 84-88.
- inputs_outputs_state: Input `$1` is the test project directory; `$2` selects scenario. Output is the test directory path printed on stdout at line 91. State output is a YAML-frontmatter `state.md` containing `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- gates_or_invariants: Enforces `set -euo pipefail` at line 10, refuses empty `TEST_DIR` at lines 15-18, uses fixed timestamps to make monitor selection deterministic, and preserves YAML list shape for `configured_bots` / `active_bots`. The empty-active-bots fixture deliberately writes `active_bots:` with no child list entries (lines 77-78).
- dependencies_and_callers: Called by `tests/test-pr-loop-hooks.sh` monitor cases around `test_monitor_yaml_list_parsing`, `test_monitor_configured_bots`, and `test_monitor_empty_active_bots`. Production parser consumers include `scripts/humanize.sh` PR monitor parsing of `configured_bots` and `active_bots` around lines 1042-1059 and display around lines 1132-1136 / 1256-1257.
- edge_cases_or_failure_modes: Unknown scenario names, missing required directory arg, YAML list parsing versus empty-list behavior, and partial-approval state where configured bots differ from active bots.
- validation_or_tests: The helper underpins monitor assertions in `tests/test-pr-loop-hooks.sh` lines 1547-1621. It is an executable specification for PR-loop monitor state format rather than production code.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR Codex review failures. It defines the user-facing transition when Codex review cannot produce a valid review result and the loop must retry instead of exiting.
- algorithmic_behavior: Template content is a failure block headed `# Codex Review Failed` (line 1), states that Codex produced no output (line 3), renders exit code and missing review result path placeholders (lines 5-6), lists debug files for command/stdout/stderr (lines 8-11), embeds a stderr excerpt in a fenced block (lines 13-16), and instructs that another review will be attempted on exit (line 18).
- inputs_outputs_state: Inputs are placeholder bindings supplied through the template loader: `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT`. Output is rendered Markdown, typically placed in a hook-block JSON reason or stderr block.
- gates_or_invariants: It is a fail-closed review gate: absence of review output is not treated as success. The template’s contract is that `REVIEW_RESULT_FILE` is explicitly marked “not created” at line 6 and stderr is limited to recent diagnostic content at lines 13-16.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/codex-review-failed.md"` at lines 1217-1225. That function builds a blocking JSON response immediately afterward at lines 1227-1230. Rendering uses `hooks/lib/template-loader.sh`; its single-pass `render_template` prevents placeholder injection by not rescanning substituted values (lines 50-57 and 116-125).
- edge_cases_or_failure_modes: Missing template falls back to inline failure text in `loop-codex-stop-hook.sh` lines 1179-1214. Caller also has later hard failures for non-zero Codex exit, missing review result, empty review result, and failed stdout-copy fallback around lines 1312-1379.
- validation_or_tests: Covered indirectly by template loader tests for fallback and substitution behavior in `tests/robustness/test-template-error-robustness.sh` and stress behavior in `tests/robustness/test-template-stress-robustness.sh`; Codex review failure paths are exercised by stop-hook robustness around `tests/robustness/test-hook-system-robustness.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Blocking prompt template for summary files written outside the active loop directory. It encodes the invariant that round summaries must live in `.humanize/rlcr/<session>/`.
- algorithmic_behavior: The template emits `# Wrong Summary Location` (line 1), states the invariant “Summary files MUST be in the loop directory” (line 3), and renders the required destination as `{{CORRECT_PATH}}` (line 5).
- inputs_outputs_state: Input placeholder is `CORRECT_PATH`, computed by hook validators from the active loop directory and current round. Output is a Markdown block shown to the agent/user when a write/edit targets a summary filename outside the allowed loop path.
- gates_or_invariants: Enforces summary path ownership: round summaries are loop state artifacts, not arbitrary project-root files. `hooks/loop-write-validator.sh` applies this when `IS_SUMMARY_FILE=true` and `IN_HUMANIZE_LOOP_DIR=false`, sets `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"`, renders this template, then exits 2 at lines 198-209.
- dependencies_and_callers: Primary caller is `hooks/loop-write-validator.sh` lines 202-208. `hooks/loop-edit-validator.sh` has related wrong-location behavior for edits around its summary checks, and `hooks/lib/loop-common.sh` provides related block-message helpers for state, finalize, summary-bash, and goal-tracker-bash protections at lines 463-500.
- edge_cases_or_failure_modes: Missing template uses the inline fallback in `loop-write-validator.sh` lines 204-206. The key edge case is an otherwise valid summary filename written outside the active `.humanize/rlcr` session; it is blocked even if content is valid.
- validation_or_tests: Template rendering and `CORRECT_PATH` substitution are covered by template tests such as `tests/test-template-loader.sh` and comprehensive template tests; hook-level behavior is exercised in robustness hook specs, especially write/edit validator cases in `tests/robustness/test-hook-input-robustness.sh` and `tests/robustness/test-hook-system-robustness.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-136 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness contract for production plan-file validation in `scripts/setup-rlcr-loop.sh`. It verifies that RLCR loop setup accepts sufficiently meaningful implementation plans and rejects unsafe or invalid file states.
- algorithmic_behavior: The central helper `test_plan_validation` at lines 24-82 runs `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"` and a supplied plan path, captures output in a temp file, removes any previous test loop dir, and classifies production outcomes. It returns failure for validation errors matching “Plan is too simple”, “insufficient content”, “not found”, “not readable”, or “symbolic link” at lines 40-44; returns success when setup reaches later gates such as missing Codex, gitignore/tracking gate, activation text, or actual `.humanize/rlcr/*/state.md` creation at lines 46-71. The file then creates an isolated git repo at lines 88-96 and runs positive and negative cases.
- inputs_outputs_state: Inputs are generated plan files under `$TEST_DIR`, a temporary git repository, and production setup script behavior. Outputs are pass/fail counters via `tests/test-helpers.sh`, setup stdout/stderr classifications, optional `.humanize/rlcr` state directories, line/content counts, and final exit status from `print_test_summary` at lines 531-532.
- gates_or_invariants: Mirrors production gates in `scripts/setup-rlcr-loop.sh`: relative paths only, no spaces/metacharacters, no symlink file or symlink parent, file exists/readable and stays within project (production lines 320-406); plan must have at least 5 physical lines (production lines 483-491); plan must have at least 3 content lines after skipping blanks, `#` comments, and HTML comments (production lines 493-532). The local `count_content_lines` helper at lines 103-143 intentionally mirrors the content-line algorithm for expected-value checks.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, Bash, git, `scripts/setup-rlcr-loop.sh`, and indirectly `scripts/portable-timeout.sh` as sourced by setup. It coordinates with sibling `test-path-validation-robustness.sh`, which stresses path syntax and symlink traversal more deeply, and `test-setup-scripts-robustness.sh`, which covers broader setup argument gates.
- edge_cases_or_failure_modes: Positive cases include valid standard plan (lines 152-178), exactly-minimum content after comments (lines 180-206), 5KB standard plan (lines 208-225), rich Markdown with code/table/list/link formatting (lines 227-264), 1MB+ plan for SIGPIPE regression (lines 299-327), and mixed CRLF/LF (lines 329-340). Negative or diagnostic cases include empty plan (lines 274-281), comments-only (lines 283-297), binary content line-count handling (lines 342-362), very long lines (lines 364-381), shell-special characters in content (lines 383-404), whitespace-only (lines 406-416), nested/complex comments (lines 418-440), nonexistent file (lines 442-449), unreadable file (lines 451-463), symlink file (lines 465-475), directory instead of file (lines 477-486), null bytes (lines 488-499), and disappeared file rejected by production (lines 501-525).
- validation_or_tests: This file is the test. I inspected it directly in full with line numbers. I did not execute it during research because it creates temp repos and invokes setup behavior; the requested artifact is evidence notes, not a test run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 unique item sections above`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`