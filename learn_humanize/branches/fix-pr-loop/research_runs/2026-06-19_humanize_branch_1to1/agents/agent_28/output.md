# agent_28 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit safety gate for active RLCR loops. It preserves loop invariants before user prompts proceed: state schema compatibility, no branch switching, and stable plan-file tracking mode. Registered in `hooks/hooks.json:4-10`.
- algorithmic_behavior: Reads hook stdin but does not inspect it (`hooks/loop-plan-file-validator.sh:26-27`); finds the newest active loop via `find_active_loop` (`:29-35`); prefers `finalize-state.md` over `state.md` during Finalize Phase (`:38-42`); parses state strictly (`:44-48`); then runs schema, branch, and plan-tracking checks. Allowed path is silent `exit 0` (`:231`); blocked paths emit Claude hook JSON with `"decision": "block"`.
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or current directory, `.humanize/rlcr/*/{state.md,finalize-state.md}`, Git branch/status, `plan_tracked`, `plan_file`, `start_branch`, templates, `jq`, and `run_with_timeout`. Outputs are JSON block reasons for policy violations or no output on allow. It does not mutate loop state or files.
- gates_or_invariants: Requires non-empty `plan_tracked` and `start_branch` after strict parsing (`:79-89`); fails closed on malformed state with exit 1 (`:44-48`); blocks if current branch differs from `start_branch` (`:95-115`); for `plan_tracked: true`, plan must remain tracked and clean (`:123-192`); otherwise plan must remain untracked/gitignored (`:193-228`); Git timeout/error paths block (`:129-146`, `:154-171`, `:199-216`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for field constants, template loading, active-loop selection, and strict state parsing (`hooks/lib/loop-common.sh:15-29`, `:139-174`, `:246-330`); depends on `scripts/portable-timeout.sh:31-71` for bounded Git commands; uses `prompt-template/block/schema-outdated.md` through `load_and_render_safe` if old schema fields are missing (`hooks/loop-plan-file-validator.sh:59-76`).
- edge_cases_or_failure_modes: If no active loop exists, it allows immediately (`:33-35`). It ignores older loop directories by design because `find_active_loop` only considers the newest timestamped directory (`hooks/lib/loop-common.sh:151-174`). `plan_file` is parsed but not included in the validator’s explicit required-field list, so a missing `plan_file` degrades into Git-command failure handling rather than a schema-specific message. `FULL_PLAN_PATH` is computed but unused (`hooks/loop-plan-file-validator.sh:121`). In untracked mode, deletion of the plan file is not detected here because `git ls-files --error-unmatch` still reports “not tracked”; later stop-hook plan integrity checks cover file deletion.
- validation_or_tests: `tests/test-plan-file-hooks.sh:93-190` covers valid state, quoted `plan_file`, malformed schema, missing `start_branch`, and branch changes. `tests/test-plan-file-hooks.sh:347-474` covers quoted `start_branch` and hyphenated plan paths. `tests/test-finalize-phase.sh:853-865` verifies the validator can parse `finalize-state.md`. Robustness coverage includes empty JSON and pass-through behavior in `tests/robustness/test-hook-system-robustness.sh:199-243`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for the RLCR monitor cleanup path in `scripts/humanize.sh`: graceful stop on deleted `.humanize/rlcr`, terminal restoration, idempotent cleanup, and signal handling.
- algorithmic_behavior: Creates an isolated `/tmp/test-monitor-runtime-$$` workspace (`tests/test-monitor-runtime.sh:45-53`), generates small helper scripts, and checks observable output with `grep`. It exercises modeled `_graceful_stop`, `_cleanup`, directory-deletion detection, terminal restore source patterns, Bash traps, and zsh `TRAPINT`/`TRAPTERM` behavior (`:56-490`).
- inputs_outputs_state: Inputs are the repo root, temporary `.humanize/rlcr` state, generated shell scripts, `scripts/humanize.sh`, and optional `zsh`. Outputs are PASS/FAIL lines and final exit code 0 only when `TESTS_FAILED` is zero (`:492-513`). Internal state variables include `cleanup_done`, `monitor_running`, `tail_pid`, `restore_called`, and synthetic signal flags.
- gates_or_invariants: Cleanup must be idempotent (`:143-177`); deleted loop directory must trigger `_graceful_stop` and stop monitoring (`:180-251`); terminal restore must reset scroll region and be called from cleanup (`:254-311`); `_graceful_stop` must call `_cleanup` (`:313-324`); Bash trap must bind `INT TERM` to `_cleanup`, zsh must define `TRAPINT` and `TRAPTERM`, and cleanup must reset traps (`:326-423`).
- dependencies_and_callers: Mirrors `scripts/humanize.sh` monitor internals: `_restore_terminal` resets scroll region (`scripts/humanize.sh:523-528`), `_cleanup` flips flags, resets traps, kills `tail_pid`, restores terminal, and prints stop text (`:537-561`), `_graceful_stop` delegates to `_cleanup` and prints the deletion reason (`:563-576`), signal handlers are installed at `:578-587`, and the monitor checks loop-dir existence in three polling paths (`:620-626`, `:657-666`, `:750-758`). Included in `tests/run-all-tests.sh:50`.
- edge_cases_or_failure_modes: Several tests are model-based rather than truly invoking the nested runtime functions: Test 1 redefines `_cleanup` and `_graceful_stop` after sourcing `humanize.sh` (`tests/test-monitor-runtime.sh:78-101`), so it validates intended behavior rather than the actual closure in `scripts/humanize.sh`. Test 5 uses `grep -A5 "_graceful_stop()"` (`:320`), but the real `_cleanup` call is at `scripts/humanize.sh:571`, just beyond five following lines from the function header at `:565`; this can create a false negative. Test 4 has a dead pipeline check because `grep -q` emits no output before piping to another `grep` (`tests/test-monitor-runtime.sh:282-287`), though the later `grep -A30` check covers the real cleanup call (`:305-311`). zsh runtime coverage is skipped when zsh is unavailable (`:432-490`).
- validation_or_tests: The file is itself the validation artifact. It additionally cross-checks static source strings for `_restore_terminal`, scroll-region reset, `_cleanup -> _restore_terminal`, trap installation, and trap reset (`:274-311`, `:390-423`). Related end-to-end/manual references appear in `tests/test-monitor-e2e-real.sh:198-201` and `tests/manual-monitor-test.sh:52`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the pre-review TodoWrite completeness gate. It prevents the RLCR loop from advancing to Codex review while Claude still has incomplete todos.
- algorithmic_behavior: Renders a Markdown block headed “Incomplete Todos Detected” with `{{INCOMPLETE_LIST}}`, then instructs the agent to complete all todos, mark each via TodoWrite, and only then write the summary and stop (`prompt-template/block/incomplete-todos.md:1-12`).
- inputs_outputs_state: Input is `INCOMPLETE_LIST`, derived from the todo checker output after the `INCOMPLETE_TODOS` marker (`hooks/loop-codex-stop-hook.sh:371-382`). Output is a rendered `reason` field in a JSON block decision with system message “Loop: Blocked - incomplete todos detected...” (`:384-392`). It does not modify loop state; it blocks the Stop event before expensive review work.
- gates_or_invariants: Any todo whose status is not exactly `completed` is incomplete (`hooks/check-todos-from-transcript.py:118-131`). If incomplete todos exist, the stop hook exits after emitting the block JSON, so Codex review prompt construction and review execution do not occur (`hooks/loop-codex-stop-hook.sh:371-393`).
- dependencies_and_callers: Called only through `hooks/loop-codex-stop-hook.sh` quick-check section (`:340-394`), using `hooks/check-todos-from-transcript.py` for transcript parsing and `load_and_render_safe` for template fallback rendering. Template rendering is single-pass and missing variables are preserved by `hooks/lib/template-loader.sh:50-132`.
- edge_cases_or_failure_modes: Empty hook input or missing `transcript_path` allows the stop to proceed because the checker exits 0 (`hooks/check-todos-from-transcript.py:90-106`). Transcript parse errors use a separate JSON block path, not this template (`hooks/loop-codex-stop-hook.sh:352-369`). If the template is missing or empty, the stop hook falls back to a smaller inline “Incomplete Todos” message (`:376-382`). The checker only considers the most recent TodoWrite call and treats unknown statuses as incomplete (`hooks/check-todos-from-transcript.py:20-87`, `:118-131`).
- validation_or_tests: Direct template-specific tests were not found in the inspected set, but the caller path is explicit in `hooks/loop-codex-stop-hook.sh:340-394` and the checker behavior is fully defined in `hooks/check-todos-from-transcript.py:90-135`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-118 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Standard Codex review prompt for non-full-alignment RLCR rounds. It defines the review contract that decides whether Claude may stop or must continue implementing plan gaps.
- algorithmic_behavior: Requires Codex to read the original implementation plan at `@{{PLAN_FILE}}` first (`prompt-template/codex/regular-review.md:3-10`), compare Claude’s summary against the plan and prompt (`:11-18`), perform implementation review (`:20-31`), run a mandatory goal-alignment check against `@{{GOAL_TRACKER_FILE}}` (`:33-45`), process the injected goal-tracker update section (`:47`), and obey strict output rules (`:49-57`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`; these are passed by `hooks/loop-codex-stop-hook.sh:761-846`. Output target is `@{{REVIEW_RESULT_FILE}}`; Codex must write findings when work is incomplete and may put `COMPLETE` as the last line only when all original tasks and ACs are done with no deferrals or pending work (`prompt-template/codex/regular-review.md:51-57`).
- gates_or_invariants: Deferrals are considered incomplete and must be forced back into implementation (`:24-31`, `:53-56`). The goal alignment summary must report AC coverage, forgotten items, and unjustified deferrals (`:33-45`). The hook later treats only an exact last non-empty `COMPLETE` or `STOP` marker as special, avoiding false positives such as prose containing “complete” (`hooks/loop-codex-stop-hook.sh:1388-1395`).
- dependencies_and_callers: Generated by `hooks/loop-codex-stop-hook.sh` when the round is not a full-alignment check (`:773-783`, `:816-846`). Shares `GOAL_TRACKER_UPDATE_SECTION` with full-alignment prompts (`:767-771`). Uses `hooks/lib/template-loader.sh` single-pass substitution semantics, so placeholders inside substituted values are not recursively expanded (`hooks/lib/template-loader.sh:50-132`). It competes with `prompt-template/codex/full-alignment-review.md`, selected every `FULL_REVIEW_ROUND` interval.
- edge_cases_or_failure_modes: If `full_review_round` is invalid or less than 2, the hook defaults it to 5 before choosing the prompt (`hooks/loop-codex-stop-hook.sh:773-783`). If the regular template is missing, an inline fallback prompt is used (`:805-814`, `:831-846`). Missing substitution variables remain as literal placeholders per the loader (`hooks/lib/template-loader.sh:12-14`, `:119-122`). The template says `COMPLETE` “will stop Claude,” but hook semantics are phase-sensitive: in review phase, `COMPLETE` is ignored and review logic determines exit (`hooks/loop-codex-stop-hook.sh:1394-1399`).
- validation_or_tests: The hook validates review-result creation by copying stdout to the expected file if needed, then failing on missing or empty results (`hooks/loop-codex-stop-hook.sh:1330-1383`). COMPLETE/finalize behavior is covered by finalize-phase tests around `tests/test-finalize-phase.sh:510-536` and failure paths around `:566-651`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`