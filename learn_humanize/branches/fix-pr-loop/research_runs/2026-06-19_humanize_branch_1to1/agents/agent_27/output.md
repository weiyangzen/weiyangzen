# agent_27 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-027 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: Claude `PreToolUse` Edit-path validator for RLCR and PR-loop state machines. It prevents direct edits to generated prompts, state files, protected loop artifacts, and post-Round-0 goal tracker state before the Edit tool mutates the filesystem.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and only acts when `tool_name == "Edit"` (`hooks/loop-edit-validator.sh:24-32`). It first blocks RLCR round todos unless the file is in the small active-loop allowlist (`:38-45`), blocks round prompt edits (`:48-51`), then applies PR-loop protections before RLCR protections (`:57-77`). For RLCR files it finds the active loop, chooses `finalize-state.md` over `state.md` when finalization is active, parses state strictly, then evaluates state, plan backup, goal tracker, and summary-round gates (`:91-181`).
- inputs_outputs_state: Input is Claude hook JSON plus environment roots such as `CLAUDE_PROJECT_DIR`, `PROJECT_ROOT`, `LOOP_BASE_DIR`, and optionally `LOOP_DIR`. Outputs are exit code `0` for allow, `2` for policy block with rendered stderr guidance, and `1` for malformed state fail-closed (`:105-109`). It reads `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`; no state is written by this script.
- gates_or_invariants: Prompt files are read-only (`:48-51`). PR-loop `state.md` and read-only PR artifacts are blocked (`:60-70`). `round-N-pr-resolve.md` must match current PR round (`:72-75`). RLCR `state.md` and `finalize-state.md` are blocked, with finalize checked first because `state.md` matching also catches `finalize-state.md` (`:113-125`). RLCR `plan.md` backups under `.humanize/rlcr/` are blocked (`:131-138`). `goal-tracker.md` is editable only through Round 0; after that Claude must request Codex updates through the round summary (`:145-149`). Summary edits must target the current round unless allowlisted (`:155-179`).
- dependencies_and_callers: Registered as the Edit hook in `hooks/hooks.json:24-30`. It sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16-18`) for predicates and messages: active-loop selection only considers the newest timestamp dir with `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151-174`), strict state parsing requires frontmatter plus `current_round`, `max_iterations`, `review_started`, and `base_branch` (`:255-329`), and the active-loop allowlist permits only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`:423-441`). PR-loop helpers define read-only PR files and wrong-round checks (`:783-887`).
- edge_cases_or_failure_modes: Non-Edit hook invocations are ignored (`hooks/loop-edit-validator.sh:27-29`). Paths outside `.humanize/rlcr/` are allowed after PR-loop checks, delegating general sandboxing elsewhere (`:83-85`). If no active RLCR loop exists, RLCR path edits are allowed (`:95-97`). Malformed active state blocks even unrelated RLCR loop-file edits for safety (`:105-109`). The path checks are string/regex based (`hooks/lib/loop-common.sh:713-716`, `:783-787`), so canonicalization is not performed here.
- validation_or_tests: Edit hook behavior is covered by allowlist tests for allowed historical todos/summaries and blocked future todos (`tests/test-allowlist-validators.sh:191-228`), finalize-state edit blocking (`tests/test-finalize-phase.sh:369-379`), PR wrong-round resolve blocking (`tests/test-pr-loop-hooks.sh:1515-1524`), and robustness cases for valid JSON, malformed JSON, missing `file_path`, state blocking, and outside-loop path delegation (`tests/robustness/test-hook-system-robustness.sh:40-120`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for real monitor behavior in RLCR and PR-loop modes, especially graceful shutdown when loop directories disappear and signal handling occurs.
- algorithmic_behavior: Builds isolated temp projects under `/tmp/test-monitor-e2e-real-$$`, creates minimal `.humanize/rlcr` or `.humanize/pr-loop` session state plus goal tracker files, points `HOME` and `XDG_CACHE_HOME` at fake cache roots, sources the real `scripts/humanize.sh`, and invokes real monitor functions rather than stubs (`tests/test-monitor-e2e-real.sh:48-56`, `:64-163`, `:243-329`, `:382-479`, `:692-805`, `:833-933`). It then deletes loop directories or sends SIGINT and checks output and exit behavior.
- inputs_outputs_state: Inputs are generated test state files, generated goal trackers, fake cache logs like `round-1-codex-run.log`, shell availability (`bash`, optional `zsh`), terminal command shims, and the repository’s real monitor code. Outputs are PASS/FAIL counters and process exit `0` only when no failures occurred (`:25-37`, `:995-1017`). It creates and removes `/tmp` test assets through `cleanup_test` (`:48-56`).
- gates_or_invariants: RLCR monitor must exit after `.humanize/rlcr` deletion, show `Monitoring stopped:` and a deletion reason, avoid zsh/bash glob errors, restore terminal scroll region, and return exit code `0` (`:191-230`). The source must contain the scroll reset escape in `_restore_terminal` (`:219-224`; implementation at `scripts/humanize.sh:522-528`). zsh deletion behavior has the same message, no-glob, and exit-code gates (`tests/test-monitor-e2e-real.sh:355-372`). SIGINT gates require cleanup text or clean exit evidence and no glob errors for bash/zsh (`:532-549`, `:672-681`). PR monitor gates require graceful or clean exit and no glob errors for once and continuous modes (`:807-824`, `:973-990`).
- dependencies_and_callers: Targets `scripts/humanize.sh`, which sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6-10`). RLCR monitor uses `monitor_find_latest_session`, cache-based Codex log discovery with `find` to avoid shell glob failures, terminal setup/restoration, traps, and deletion checks (`scripts/humanize.sh:219-239`, `:246-328`, `:512-588`, `:620-665`, `:750-758`). PR monitor uses `monitor_find_latest_session`, `monitor_find_state_file`, PR phase helpers, goal tracker parsing, `--once` mode, and tailing latest PR files (`scripts/humanize.sh:931-1017`, `:1030-1060`, `:1169-1213`, `:1215-1293`, `:1303-1355`).
- edge_cases_or_failure_modes: zsh is optional; tests skip zsh-specific cases if unavailable (`tests/test-monitor-e2e-real.sh:240-242`, `:558-560`). SIGINT delivery can be unreliable in non-interactive bash, so the test accepts SIGTERM fallback as a pass if the monitor was running (`:492-519`, `:951-965`). PR `--once` test deletes the session in a background process, but `--once` may complete before deletion; it therefore accepts clean exit evidence (`:784-816`). The test is not purely read-only because it writes temp files and spawns/kills processes.
- validation_or_tests: This file is itself the validation harness. I did not execute it in this research run because the branch export and task instructions are read-only, while the script creates `/tmp` trees, generated runners, cache logs, and background processes.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-087 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Stop-hook block template that enforces the Round 0 initialization gate for RLCR `goal-tracker.md`.
- algorithmic_behavior: Renders a blocking instruction when the goal tracker still has placeholder content. It names the missing sections through `{{MISSING_ITEMS}}` and tells Claude to read `{{GOAL_TRACKER_FILE}}`, replace placeholders with actual Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to criteria, then write the updated tracker (`prompt-template/block/goal-tracker-not-initialized.md:1-18`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `MISSING_ITEMS` template variables. Output is a rendered Markdown reason inserted into the Stop-hook JSON block response. The template itself mutates no state; it directs Claude to update `goal-tracker.md` during Round 0.
- gates_or_invariants: The key invariant is that the immutable section can only be set in Round 0 and becomes read-only afterward (`:16`). The corresponding Stop hook only runs this check outside Finalize Phase and Review Phase, when `CURRENT_ROUND` is `0`, `review_started` is not true, and the tracker file exists (`hooks/loop-codex-stop-hook.sh:647-652`). It detects placeholders section-by-section for Ultimate Goal, Acceptance Criteria, and Active Tasks, builds missing-item bullets, and blocks exit when any remain (`:662-718`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` (`hooks/loop-codex-stop-hook.sh:700-707`). Initial tracker placeholders are generated by `scripts/setup-rlcr-loop.sh`: Ultimate Goal and Acceptance Criteria placeholders may be inserted when plan extraction is insufficient (`scripts/setup-rlcr-loop.sh:782-817`), and Active Tasks starts with a placeholder row (`:834-839`).
- edge_cases_or_failure_modes: The detector looks for the generic pattern `[To be ` inside extracted sections, not specific English placeholder strings, reducing coupling but missing nonmatching placeholder wording (`hooks/loop-codex-stop-hook.sh:653-683`). Skip-implementation mode creates a simplified tracker without placeholders and bypasses normal tracker use (`scripts/setup-rlcr-loop.sh:739-760`). If the goal tracker file is absent, this particular gate does not block because the Stop-hook condition requires `-f "$GOAL_TRACKER_FILE"` (`hooks/loop-codex-stop-hook.sh:652`).
- validation_or_tests: The template is indirectly tested by Stop-hook behavior around Round 0 initialization; direct references show it is rendered with `GOAL_TRACKER_FILE` and `MISSING_ITEMS` in `hooks/loop-codex-stop-hook.sh:700-707`. No direct comprehensive-template assertion for this exact block appeared in the targeted search.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-117 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section that gives Codex ownership of post-Round-0 goal tracker updates requested by Claude summaries.
- algorithmic_behavior: Instructs Codex to evaluate any “Goal Tracker Update Request” in Claude’s summary, decide whether it serves the Ultimate Goal, update `@{{GOAL_TRACKER_FILE}}` if approved, or explain rejection in the review if not (`prompt-template/codex/goal-tracker-update-section.md:1-12`). Approved updates include moving tasks among Active/Completed/Deferred, adding Plan Evolution Log entries with round and justification, and adding Open Issues (`:6-17`).
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE`; the rendered section is embedded into Codex review prompts. Output is either direct Codex edits to `goal-tracker.md` or a rejection explanation in the review result. It explicitly forbids modifying the immutable Ultimate Goal and Acceptance Criteria (`:10`).
- gates_or_invariants: This template operationalizes the same post-Round-0 ownership gate enforced by the Edit validator: Claude cannot directly edit `goal-tracker.md` after Round 0 (`hooks/loop-edit-validator.sh:145-149`), so update requests must flow through summary to Codex. Deferrals require strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`), and immutable content is never to be changed (`:10`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` renders this template once per review prompt (`hooks/loop-codex-stop-hook.sh:767-771`) and injects it into both full-alignment and regular Codex review templates (`:816-846`). Those review templates also instruct Codex to read the goal tracker and verify goal alignment (`prompt-template/codex/full-alignment-review.md:21`; `prompt-template/codex/regular-review.md:35` from search results).
- edge_cases_or_failure_modes: Codex is given judgment authority; unjustified requests can be rejected rather than blindly applied (`prompt-template/codex/goal-tracker-update-section.md:5`, `:11`). The template assumes Claude uses the expected “Goal Tracker Update Request” section name; if Claude buries changes elsewhere, Codex may not treat them as actionable. It also assumes the goal tracker structure contains Active, Completed, Deferred, Plan Evolution Log, and Open Issues sections generated by setup (`scripts/setup-rlcr-loop.sh:826-854`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this template with a sample `GOAL_TRACKER_FILE` and checks that the heading and substituted path appear (`tests/test-templates-comprehensive.sh:531-539`). It is also exercised indirectly whenever the Stop hook builds regular or full-alignment Codex review prompts (`hooks/loop-codex-stop-hook.sh:816-846`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections present; headings correspond to the four scheduler-assigned paths in order.
- missing_items: none
- duplicate_items: none in Item Evidence headings
- final_worker_status: `complete`