# agent_14 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR-loop prompt template set for round-0 initialization, initial goal tracking, completion instructions, and Codex goal-tracker update instructions. It is not executable code, but it defines the behavioral contract that `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh` render into loop artifacts.
- algorithmic_behavior: The directory has seven templates: `goal-tracker-initial.md`, `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, `critical-requirements-no-comments.md`, `critical-requirements-has-comments.md`, and `codex-goal-tracker-update.md`. `setup-pr-loop.sh` renders `goal-tracker-initial.md` to `.humanize/pr-loop/<timestamp>/goal-tracker.md` using PR number, branch, bot display, startup case, and started timestamp at `scripts/setup-pr-loop.sh:597-652`. It then renders `round-0-header.md`, appends fetched comments, and selects `round-0-task-no-comments.md` or `round-0-task-has-comments.md` based on whether the fetched comment file contains the no-comments sentinel at `scripts/setup-pr-loop.sh:662-790`. It also prints matching critical requirements at `scripts/setup-pr-loop.sh:829-881`. `pr-loop-stop-hook.sh` injects `codex-goal-tracker-update.md` into the local Codex validation prompt so Codex updates Issue Summary, Total Statistics, and Issue Log after per-bot analysis at `hooks/pr-loop-stop-hook.sh:1252-1302`.
- inputs_outputs_state: Inputs are template variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `STARTUP_CASE`, `STARTED_AT`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are rendered markdown prompts, critical instruction text, and goal-tracker instructions. The state surface coordinated by these templates is `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-prompt.md`, and `round-N-pr-resolve.md`; round transitions later use `round-N-pr-comment.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md` in the stop hook.
- gates_or_invariants: The no-comment path explicitly says not to trigger first review manually and only to write the initial summary (`round-0-task-no-comments.md:6-14`, `critical-requirements-no-comments.md:8-18`). The has-comments path requires analyzing human comments first, fixing issues, committing, pushing, commenting with bot mentions, and writing the resolution summary (`round-0-task-has-comments.md:6-27`, `critical-requirements-has-comments.md:8-23`). Both round-0 task templates forbid modifying `.humanize/pr-loop/` state files (`round-0-task-has-comments.md:30-35`, `round-0-task-no-comments.md:18-23`). The goal-tracker update template requires table rows per reviewer, total statistics updates, and issue log entries while preserving headers (`codex-goal-tracker-update.md:7-20`, `codex-goal-tracker-update.md:57-64`).
- dependencies_and_callers: Direct callers are `scripts/setup-pr-loop.sh` for all round-0 and critical templates (`scripts/setup-pr-loop.sh:651`, `scripts/setup-pr-loop.sh:697`, `scripts/setup-pr-loop.sh:739`, `scripts/setup-pr-loop.sh:786`, `scripts/setup-pr-loop.sh:853`, `scripts/setup-pr-loop.sh:879`) and `hooks/pr-loop-stop-hook.sh` for `codex-goal-tracker-update.md` (`hooks/pr-loop-stop-hook.sh:1261`). Rendering depends on `hooks/lib/template-loader.sh`, sourced via `scripts/setup-pr-loop.sh:39` and `hooks/lib/loop-common.sh:139-144`. Runtime semantics depend on GitHub CLI scripts for comment/review polling and on Codex CLI validation in `pr-loop-stop-hook.sh:811-1230` and `pr-loop-stop-hook.sh:1322-1334`.
- edge_cases_or_failure_modes: Missing templates do not hard-fail because callers use `load_and_render_safe` with inline fallbacks. The no-comment detection is sentinel-based: only a line exactly matching `*No comments found.*` makes `COMMENT_COUNT=0`; otherwise setup treats comments as present (`scripts/setup-pr-loop.sh:662-668`). If template variables are missing, placeholders remain, because the renderer preserves unknown placeholders (`hooks/lib/template-loader.sh:115-122`). If variable values contain template syntax, single-pass rendering prevents recursive substitution (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:71-129`).
- validation_or_tests: Covered by `tests/run-all-tests.sh`, which includes `test-template-loader.sh`, `test-template-references.sh`, `test-templates-comprehensive.sh`, `test-pr-loop.sh`, `test-pr-loop-system.sh`, and PR-loop/hook robustness suites (`tests/run-all-tests.sh:32-73`). Template reference validation scans scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and checks that referenced templates exist (`tests/test-template-references.sh:83-110`). The loader tests cover real template rendering, fallback behavior, unreplaced variables, and placeholder-injection prevention (`tests/test-template-loader.sh:147-164`, `tests/test-template-loader.sh:196-222`, `tests/test-template-loader.sh:488-525`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level executable specification and aggregator for Humanize plugin behavior, including RLCR loop, PR-loop, template loading, validators, monitor runtime, and robustness suites.
- algorithmic_behavior: Defines ordered `TEST_SUITES` at `tests/run-all-tests.sh:32-73`, detects suites that require zsh at `tests/run-all-tests.sh:75-78`, executes each test script under bash or zsh, captures output and exit code, strips ANSI escape codes, extracts the last reported `Passed:` and `Failed:` counts, aggregates totals, and fails the overall run if any suite exits nonzero or reports failures (`tests/run-all-tests.sh:80-139`, `tests/run-all-tests.sh:121-136`).
- inputs_outputs_state: Input is the repository test tree relative to `SCRIPT_DIR` (`tests/run-all-tests.sh:14`). Outputs are console summaries for each suite, aggregate pass/fail totals, failed suite names, and process exit status 0 or 1 (`tests/run-all-tests.sh:141-159`). Internal state is `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES`.
- gates_or_invariants: Missing test files are skipped rather than failing (`tests/run-all-tests.sh:83-86`). `test-zsh-monitor-safety.sh` must run under zsh if zsh is available; if zsh is missing, it is skipped (`tests/run-all-tests.sh:97-114`). Any nonzero suite exit or parsed failed count greater than zero marks the suite failed (`tests/run-all-tests.sh:130-134`). Overall success requires `FAILED_SUITES` to be empty (`tests/run-all-tests.sh:148-158`).
- dependencies_and_callers: Depends on every suite listed in `TEST_SUITES`, including the assigned `tests/robustness/test-hook-system-robustness.sh` at `tests/run-all-tests.sh:69`. Depends on standard shell utilities `sed`, `grep`, and `tail` for parsing and failure output (`tests/run-all-tests.sh:121-134`). It is likely a human/CI entrypoint; no internal caller was required to understand its behavior.
- edge_cases_or_failure_modes: If a suite does not print `Passed:` or `Failed:`, parsed counts default to zero (`tests/run-all-tests.sh:124-125`), so exit code is the reliable backstop. If zsh is unavailable, the zsh-only suite is skipped, not failed (`tests/run-all-tests.sh:108-113`). Because `set -e` is disabled only around suite execution and restored afterward (`tests/run-all-tests.sh:105-119`), unexpected failures outside that block can still abort the aggregator.
- validation_or_tests: The file is itself a validation harness. It validates the assigned algorithm subset by including template, hook, PR-loop, state transition, API, setup script, timeout, path validation, concurrent state, and hook-system robustness suites (`tests/run-all-tests.sh:34-72`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-074 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared template loading and single-pass rendering library for loop hooks and setup scripts. It converts static prompt/block markdown templates into runtime messages without recursive placeholder expansion.
- algorithmic_behavior: `get_template_dir` derives the repository `prompt-template` path from a caller-provided `hooks/lib` directory by going up two levels (`hooks/lib/template-loader.sh:24-31`). `load_template` returns file contents or warns and returns an empty string when missing (`hooks/lib/template-loader.sh:33-48`). `render_template` takes content plus `VAR=value` assignments, exports them with a `TMPL_VAR_` prefix, and runs an awk scanner that finds `{{...}}` placeholders and replaces only those present in the environment lookup (`hooks/lib/template-loader.sh:58-132`). `load_and_render` composes loading and rendering but returns nothing for empty content (`hooks/lib/template-loader.sh:134-147`). `append_template` concatenates base and additional template content (`hooks/lib/template-loader.sh:149-161`). `load_and_render_safe` renders a template when available, otherwise renders or echoes a fallback message (`hooks/lib/template-loader.sh:167-203`). `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`).
- inputs_outputs_state: Inputs are a template directory, relative template path, template content, and `VAR=value` substitution pairs. Outputs are rendered markdown/string content on stdout and warnings/errors on stderr. It does not persist state, but all higher-level state files and hook block responses depend on its rendered output.
- gates_or_invariants: Rendering is intentionally single-pass: replacement values are written to output and not re-scanned (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:115-125`). Missing variables keep the original placeholder (`hooks/lib/template-loader.sh:119-122`). Missing template files return empty in `load_template` but safe callers fall back (`hooks/lib/template-loader.sh:41-47`, `hooks/lib/template-loader.sh:179-187`). Template directory validation is structural and requires core template subdirs, not every individual template (`hooks/lib/template-loader.sh:211-221`).
- dependencies_and_callers: Uses shell, `cat`, `cd`, `pwd`, `env`, and `awk`. It is sourced by `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:139-144`), which makes `TEMPLATE_DIR` available to validators and stop hooks. Direct functional usage appears across `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and `scripts/setup-pr-loop.sh`.
- edge_cases_or_failure_modes: Variable names are not validated before export; invalid shell environment assignments could make `env` fail if callers pass malformed `VAR=value`, though normal templates use uppercase identifiers. Newlines and special characters in values are routed through environment variables and awk output, avoiding sed replacement escaping issues. Empty template content causes `load_and_render_safe` to use fallback after rendering result is empty (`hooks/lib/template-loader.sh:189-200`). Unclosed `{{` is treated literally and parsing continues (`hooks/lib/template-loader.sh:102-109`).
- validation_or_tests: `tests/test-template-loader.sh` verifies path derivation, existing/missing loads, single/multiple/multiline substitution, unreplaced variables, safe fallback, and real template rendering (`tests/test-template-loader.sh:41-222`). It also verifies placeholder injection prevention (`tests/test-template-loader.sh:488-525`). `tests/test-template-references.sh` checks referenced templates exist and critical validators use safe rendering (`tests/test-template-references.sh:83-110`, `tests/test-template-references.sh:172-200`). Robustness suites cover missing directories, malformed placeholders, long values, large templates, newlines, and placeholder syntax in values (`tests/robustness/test-template-error-robustness.sh:34-63`, `tests/robustness/test-template-error-robustness.sh:73-115`, `tests/robustness/test-template-stress-robustness.sh:82-129`, `tests/robustness/test-template-stress-robustness.sh:218-228`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block message template for RLCR read validation when an agent attempts to read a round prompt or summary from a different round than the active loop state.
- algorithmic_behavior: Defines a concise diagnostic headed “Wrong Round File,” names the attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, states the current round, lists the active prompt and summary paths, and gives an explicit `cat {{FILE_PATH}}` escape hatch if the blocked historical file is truly needed (`prompt-template/block/wrong-round-file.md:1-9`).
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, supplied by `hooks/loop-read-validator.sh` when it detects a wrong-round read. Output is markdown written to stderr, then the read validator exits with code 2.
- gates_or_invariants: The gating invariant is current-round coherence: if the requested round extracted from the filename differs from the parsed active `current_round`, and the file is not allowlisted, reads are blocked (`hooks/loop-read-validator.sh:138-150`). The validator separately blocks wrong loop locations before wrong round checks (`hooks/loop-read-validator.sh:122-132`) and wrong active directory paths after round validation (`hooks/loop-read-validator.sh:157-168`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-round-file.md"` in `hooks/loop-read-validator.sh:144-149`. The validator depends on `extract_round_number`, `is_round_file_type`, `is_allowlisted_file`, `find_active_loop`, and `parse_state_file_strict` from `hooks/lib/loop-common.sh` (`hooks/loop-read-validator.sh:69-105`, `hooks/lib/loop-common.sh:408-440`).
- edge_cases_or_failure_modes: The template is for RLCR files named `round-N-summary.md` or `round-N-prompt.md`, not PR-loop `round-N-pr-resolve.md`. If the active state is malformed, the read validator fails closed before rendering this message (`hooks/loop-read-validator.sh:94-99`). Some historical files are allowlisted, such as `round-0-summary.md` and `round-1-summary.md`, so this block does not fire for those exact active-loop paths (`hooks/lib/loop-common.sh:420-440`).
- validation_or_tests: Template existence and reference safety are covered by `tests/test-template-references.sh`. Real rendering of adjacent round-block templates is covered in `tests/test-template-loader.sh:147-164`; wrong-round read behavior is exercised by hook tests such as `tests/test-finalize-phase.sh`, which constructs a read of `round-5-summary.md` while current state differs (`tests/test-finalize-phase.sh:841`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-134 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for hook-level safety across edit, plan-file, bash, write/read validators, shared state parsing, concurrent hook invocation, and stop-hook active/missing/corrupt-state behavior.
- algorithmic_behavior: Sets up a disposable test directory, sources `loop-common.sh`, `portable-timeout.sh`, and `test-helpers.sh`, then runs 21 numbered tests (`tests/robustness/test-hook-system-robustness.sh:18-31`, `tests/robustness/test-hook-system-robustness.sh:40-671`). It invokes hooks by piping synthetic Claude tool JSON into hook scripts with `CLAUDE_PROJECT_DIR` pointed at fixture workspaces. It verifies valid Edit JSON passes, state file edits are blocked, malformed JSON exits below crash range, non-loop path traversal is delegated to sandbox, path traversal into `.humanize/rlcr/.../state.md` is blocked, plan-file edits are allowed when valid, and non-plan edits pass through (`tests/robustness/test-hook-system-robustness.sh:40-230`).
- inputs_outputs_state: Inputs are generated JSON payloads for `Edit`, `Bash`, `Write`, and `Read`; generated `.humanize/rlcr/<timestamp>/state.md` files; temporary git repos for plan and stop-hook tests; and a mock `codex` binary for active loop stop-hook testing. Outputs are pass/fail counters through `test-helpers.sh`, hook exit-code assertions, hook JSON decision checks, and the final test summary exit status (`tests/robustness/test-hook-system-robustness.sh:592-613`, `tests/robustness/test-hook-system-robustness.sh:667-672`).
- gates_or_invariants: Hook validators must not crash on malformed, empty, deeply nested, null-byte, binary-looking, newline, large, or missing-field JSON (`tests/robustness/test-hook-system-robustness.sh:80-107`, `tests/robustness/test-hook-system-robustness.sh:384-411`, `tests/robustness/test-hook-system-robustness.sh:623-665`). State files are protected from Edit and Bash modification (`tests/robustness/test-hook-system-robustness.sh:53-78`, `tests/robustness/test-hook-system-robustness.sh:317-351`). Goal tracker writes are blocked after round 0 (`tests/robustness/test-hook-system-robustness.sh:353-367`). Stop hooks must allow exit when no active state exists, handle corrupted or incomplete state without crashing, and block active loops with a JSON `decision: block` (`tests/robustness/test-hook-system-robustness.sh:461-613`).
- dependencies_and_callers: Depends on `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, shared parsing in `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, git, jq, and test helpers (`tests/robustness/test-hook-system-robustness.sh:20-24`). It is included in the global harness at `tests/run-all-tests.sh:69`.
- edge_cases_or_failure_modes: The test explicitly documents that unrelated dangerous shell commands and paths outside `.humanize` pass through because sandboxing owns that broader security boundary (`tests/robustness/test-hook-system-robustness.sh:109-122`, `tests/robustness/test-hook-system-robustness.sh:369-382`). The corrupted state test allows either a decision-bearing response or no active-loop detection, but insists on a non-crash exit (`tests/robustness/test-hook-system-robustness.sh:493-514`). The active-loop stop-hook test must mock Codex because a valid active state can trigger `codex exec` (`tests/robustness/test-hook-system-robustness.sh:592-605`).
- validation_or_tests: This file is a validation suite. Its final gate calls `print_test_summary "Hook System Robustness Test Summary"` and exits with that status (`tests/robustness/test-hook-system-robustness.sh:667-672`). The overall aggregator runs it as one of the robustness suites (`tests/run-all-tests.sh:69`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 Item Evidence headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`