# agent_26 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: RLCR Stop hook registered as a Claude `Stop` hook with a 7200s timeout in `hooks/hooks.json:52-59`; it intercepts exit attempts, runs Codex validation, and either permits loop termination by retiring state files or blocks exit with the next instruction prompt.
- algorithmic_behavior: Reads hook stdin but intentionally ignores `stop_hook_active` so every iterative stop is reviewed (`hooks/loop-codex-stop-hook.sh:29-37`). It locates the newest active `.humanize/rlcr/<timestamp>` loop through `find_active_loop`, detects normal vs finalize phase by `state.md` or `finalize-state.md`, parses YAML frontmatter, then runs ordered quick checks before invoking Codex (`hooks/loop-codex-stop-hook.sh:43-82`, `88-156`). In implementation phase it renders either `codex/full-alignment-review.md` or `codex/regular-review.md`, executes `codex exec`, validates the output file, and interprets only a final standalone `COMPLETE` or `STOP` marker (`761-847`, `1271-1395`). A `COMPLETE` transitions to review phase, runs `codex review --base`, and only absence of `[P0-9]` issues enters finalize (`1395-1469`). Non-complete findings increment `current_round`, write `round-N-prompt.md`, and block exit with the next-round prompt (`1505-1571`).
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or `pwd`, hook JSON stdin, `.humanize/rlcr/<timestamp>/state.md`, `round-N-summary.md`, `goal-tracker.md`, plan backup, git state, Codex CLI, templates, and optional `BASE_COMMIT`/`BASE_BRANCH`. Outputs include JSON hook decisions via `jq`, review prompt/result files, cache debug files under `XDG_CACHE_HOME` or `$HOME/.cache`, mutated state frontmatter through `sed`, `.review-phase-started`, `finalize-state.md`, and terminal state names such as `maxiter-state.md`, `stop-state.md`, `unexpected-state.md`, or `complete-state.md` (`877-887`, `1036-1037`, `1421-1428`, `1505-1508`; terminal rename helper in `hooks/lib/loop-common.sh:1119-1139`).
- gates_or_invariants: Fails closed on invalid `codex_model`/`codex_effort`, missing `current_round`/`max_iterations`, nonnumeric round, old schema missing `plan_tracked`, `start_branch`, `review_started`, or `base_branch`, branch mismatch, missing/modified plan backup, incomplete todos, failed `git status`, large changed files over 2000 lines, dirty git, unpushed commits when `push_every_round=true`, missing summaries, uninitialized Round 0 goal tracker placeholders, exceeded max iterations, missing Codex CLI, failed Codex execution, missing/empty review result, and invalid review-phase marker (`118-208`, `223-337`, `347-425`, `433-603`, `616-733`, `855-875`, `1311-1383`, `1446-1462`). `COMPLETE` is strict: last non-empty line only (`1388-1395`); review phase ignores `COMPLETE` and relies on `[P0-9]` absence (`1395-1399`, `1440-1469`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `hooks/check-todos-from-transcript.py`, `git`, `jq`, `python3`, `diff`, `awk`, `sed`, `wc`, and `codex` (`46-52`, `345-349`, `406-410`, `856-875`). Shared functions supply markers, active loop discovery, tolerant parsing, review issue extraction, lowercase conversion, and terminal state naming (`hooks/lib/loop-common.sh:30-44`, `156-174`, `209-244`, `349-392`, `395-397`, `1119-1139`). Template rendering is single-pass and fallback-safe (`hooks/lib/template-loader.sh:50-58`, `170-203`).
- edge_cases_or_failure_modes: Missing active loop or missing state file allows exit (`59-63`, `76-82`), while corrupted active state usually ends as unexpected or blocks. `max_iterations` nonnumeric is silently reset to `42`, unlike missing/non-numeric `current_round` (`147-156`). Missing `full_review_round` is a warning with default `5`, and invalid values below `2` default to `5` (`216-220`, `773-779`). `find_active_loop` still uses `ls "$loop_base_dir"/*/`, so shell behavior around empty globs remains relevant despite tests modeling safer `find` iteration (`hooks/lib/loop-common.sh:156-170`). A `STOP` outside full-alignment rounds is honored but labeled unexpected (`1471-1498`). In this read-only branch export, git metadata was unavailable, so I did not execute the hook end-to-end.
- validation_or_tests: Direct coverage appears in `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, and `tests/robustness/test-hook-system-robustness.sh`, which invoke this hook via `echo "$HOOK_INPUT" | .../loop-codex-stop-hook.sh`; template reference coverage includes `tests/test-template-references.sh`. Review issue detection is delegated to `detect_review_issues`, which scans Codex review logs for `[P0-9]` within the first ten characters and writes extracted findings to `round-N-review-result.md` (`hooks/lib/loop-common.sh:340-392`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for `.humanize` escape protections and shell-safe directory iteration; it verifies the shared helper behavior that prevents RLCR local state from being accidentally or forcibly committed.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, lowercases test commands through `to_lower`, and asserts `git_adds_humanize` returns block/allow decisions (`tests/test-humanize-escape.sh:16-19`, `44-70`). It groups coverage for direct `.humanize` path variants, quoted variants, force/all flags, chained commands, `git -C`/`--git-dir`, allowed non-add or specific-file commands, and find-based empty/dotfile/session iteration patterns (`78-192`, `195-332`).
- inputs_outputs_state: Inputs are hard-coded shell command strings plus temporary directories under `/tmp` for `.humanize` existence and directory iteration tests (`130-144`, `204-324`). Outputs are colored PASS/FAIL lines, `TESTS_PASSED`/`TESTS_FAILED`, and exit `0` only when no failures occurred (`20-38`, `337-352`). It does not mutate repo files; it creates and removes transient `/tmp/test-humanize-*` directories.
- gates_or_invariants: Direct `.humanize`, `./.humanize`, nested `path/to/.humanize`, and quoted forms must block (`83-100`). Force plus broad scope or all flags must block, including combined `-fA`/`-Af` (`109-119`). `git add -A`/`--all` must block when `.humanize` exists even if a narrower path is supplied (`128-140`). Chained and configured-git forms must still be parsed (`153-166`). Allowed cases include specific files, `.gitignore`, non-add git commands, patch mode, and similar names like `.humanizeconfig` (`175-192`).
- dependencies_and_callers: Depends on `git_adds_humanize` and `to_lower` from `hooks/lib/loop-common.sh`. The implementation under test splits chained commands on shell operators, detects `git ... add`, strips quotes, blocks `.humanize` path tokens without over-blocking `.humanizeconfig`, detects `--force`, combined `-f`, `--all`, lowercased `-A`, broad `.`/`*`, and consults `git check-ignore` for broad non-force adds (`hooks/lib/loop-common.sh:930-1025`). It is included in the aggregate runner as `test-humanize-escape.sh` (`tests/run-all-tests.sh:48`).
- edge_cases_or_failure_modes: The test intentionally distinguishes `.humanize` from names that merely start with that string (`189-192`). It covers `.humanize` existence-sensitive behavior for `-A`/`--all`, but the current helper blocks `git add -A src/` whenever `.humanize` exists because `has_all=true` is enough (`135-140`; implementation `1010-1014`). It tests a `find`-based iteration pattern for empty directories and latest timestamp selection (`204-332`), while the actual shared `find_active_loop` implementation uses `ls` globbing (`hooks/lib/loop-common.sh:164-170`), so this file is a spec for the intended safe pattern, not direct coverage of that helper.
- validation_or_tests: This file is itself a test script. I did not execute it because the current task is research-only and the sandbox is read-only; the script writes under `/tmp` and removes those directories. Static inspection shows it would fail on any assertion by incrementing `TESTS_FAILED` and exiting `1` (`34-38`, `344-352`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template enforcing the RLCR invariant that after Round 0 only Codex may modify `goal-tracker.md`.
- algorithmic_behavior: Renders a round-specific denial headed `Goal Tracker Modification Blocked (Round {{CURRENT_ROUND}})`, tells Claude not to modify `goal-tracker.md` through Write, Edit, or Bash, and redirects the state transition into a structured `Goal Tracker Update Request` embedded in the current summary file (`prompt-template/block/goal-tracker-modification.md:1-25`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `SUMMARY_FILE`; output is human-readable block text returned by `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:1098-1110`). The state effect is indirect: Claude must write a request to `round-N-summary.md`, after which Codex review may update the tracker through review instructions.
- gates_or_invariants: The invariant is “After Round 0, only Codex can modify the Goal Tracker” (`3-5`). The allowed alternative format requires `## Goal Tracker Update Request`, `### Requested Changes`, and `### Justification` sections (`9-23`). This prevents unilateral mutation while preserving a reviewed change path.
- dependencies_and_callers: Called through `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1098-1110`. Write and Edit validators invoke it when `is_goal_tracker_path` and `CURRENT_ROUND > 0` (`hooks/loop-write-validator.sh:188-196`, `hooks/loop-edit-validator.sh:141-148`). Bash validator invokes it for command-level `goal-tracker.md` modification attempts after Round 0, while Round 0 Bash attempts get a different “use Write/Edit” message (`hooks/loop-bash-validator.sh:343-357`).
- edge_cases_or_failure_modes: Does not itself validate whether the requested update is justified; it only shifts authority to Codex. It relies on validators correctly detecting path/command attempts and on the stop-hook prompt including goal-tracker update handling. Missing template falls back to a shorter inline message, so behavior remains blocking even if the file is absent (`hooks/lib/template-loader.sh:170-203`, `hooks/lib/loop-common.sh:1103-1109`).
- validation_or_tests: Template reference coverage explicitly includes this path in `tests/test-template-references.sh`; validator behavior is exercised through allowlist and hook tests that cover protected RLCR files. The stop hook also injects Codex-side goal tracker update instructions into review prompts (`hooks/loop-codex-stop-hook.sh:767-771`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-116 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex full-alignment checkpoint template for RLCR. It defines the periodic audit contract that can continue implementation, enter final completion only under strict criteria, or trigger the `STOP` circuit breaker for stagnation.
- algorithmic_behavior: Requires Codex to read the original implementation plan first (`prompt-template/codex/full-alignment-review.md:5-10`), inspect Claude’s summary (`13-16`), audit every immutable acceptance criterion with MET/PARTIAL/NOT MET/DEFERRED statuses (`19-29`), compare the plan against the goal tracker for forgotten work (`30-35`), re-evaluate deferred items (`36-41`), summarize remaining work (`42-48`), perform implementation review against reality and docs (`50-55`), process the injected goal-tracker update section (`57`), and inspect recent round history for stagnation (`59-84`).
- inputs_outputs_state: Inputs are rendered variables `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE`; these are supplied by `hooks/loop-codex-stop-hook.sh:785-829`. Output is a Codex-written `round-N-review-result.md` whose final non-empty line controls hook state: `COMPLETE`, `STOP`, or findings that generate the next round.
- gates_or_invariants: Full alignment is scheduled when `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1`; invalid intervals default to `5` (`hooks/loop-codex-stop-hook.sh:773-783`). `STOP` must be a single last line if stagnation is detected (`prompt-template/codex/full-alignment-review.md:84`, `90`). `COMPLETE` is allowed only when all original plan tasks are done, all ACs are fully met, and there are no deferrals (`88-93`); deferred ACs are explicitly incomplete (`91-93`).
- dependencies_and_callers: Rendered only by the stop hook during full-alignment rounds through `load_and_render_safe "$TEMPLATE_DIR" "codex/full-alignment-review.md"` (`hooks/loop-codex-stop-hook.sh:816-829`). It incorporates `codex/goal-tracker-update-section.md` as Part 3 and relies on template-loader single-pass substitution to keep embedded summary/review content from re-expanding placeholders (`hooks/lib/template-loader.sh:50-58`, `71-132`).
- edge_cases_or_failure_modes: If Codex writes `STOP` on a non-full-alignment round, the hook still honors it but reports it as unexpected (`hooks/loop-codex-stop-hook.sh:1471-1498`). If Codex includes “COMPLETE” in prose rather than as the final standalone line, the hook treats it as not complete (`1388-1395`). The template requires historical files by computed paths; early rounds clamp previous indices to 0 to avoid negative references (`hooks/loop-codex-stop-hook.sh:788-791`). Missing template falls back to a much shorter full-alignment prompt, reducing audit strictness but keeping the loop functional (`793-803`, `818-829`).
- validation_or_tests: Template existence and safe rendering are covered by `tests/test-template-references.sh` and template-loader tests. Behavior is indirectly covered by stop-hook tests around review prompt generation, `COMPLETE`, `STOP`, finalize, and max-iteration transitions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections present; each assigned row is represented once as its own `###` section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`