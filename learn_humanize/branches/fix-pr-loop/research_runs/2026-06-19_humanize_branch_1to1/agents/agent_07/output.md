# agent_07 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`
- note: repository export was inspected read-only; no files were modified and no test suites were executed.

## Item Evidence

### FIX_PR_LOOP-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification tree for the Humanize RLCR and PR loop algorithms. It is not runtime code, but it defines the expected behavior for loop setup, state transitions, monitor display, hook validators, PR reviewer polling, template loading, cancellation safety, and robustness edge cases.
- algorithmic_behavior: `tests/run-all-tests.sh` is the aggregate runner: it orders core suites at `tests/run-all-tests.sh:32-73`, runs zsh-only tests separately at `tests/run-all-tests.sh:75-78`, strips ANSI before pass/fail aggregation at `tests/run-all-tests.sh:121-128`, and exits nonzero on any failed suite at `tests/run-all-tests.sh:148-159`. `tests/test-pr-loop.sh` composes the PR loop suite from scripts, hook, and stop-hook modules at `tests/test-pr-loop.sh:5-8` and sources those modules at `tests/test-pr-loop.sh:19-34`. The `tests/robustness` subtree expands coverage across malformed state files, concurrent state access, session selection, setup script edge cases, hook JSON parsing, PR API failures, timeout behavior, template errors/stress, cancel security, git operations, and base branch detection. Fixture JSON under `tests/fixtures` and `tests/mocks/gh` drive deterministic GitHub API behavior for PR comments, reviews, reactions, PR state, and repo metadata.
- inputs_outputs_state: Inputs are temp git repos, mocked GitHub responses, fixture JSON, synthetic `.humanize/rlcr` and `.humanize/pr-loop` session directories, `state.md` and terminal state files, environment variables such as `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and mock `PATH` entries. Outputs are pass/fail counters, exit codes, captured hook/script output, and temporary loop/session files. Most tests clean via temp directories; some PR system tests temporarily rewrite fixture JSON and restore it.
- gates_or_invariants: The tree treats validator exit code `2` as a block, zero as allow, and nonzero suite status as failure. It asserts that terminal state files are not active loops, active loop selection is timestamp-ordered, PR and RLCR loops are mutually exclusive, protected files cannot be modified through Read/Write/Edit/Bash bypasses, and template references must exist so blocked actions receive concrete messages.
- dependencies_and_callers: Depends on `bash`, `zsh` for the zsh monitor suite, `git`, `jq`, `timeout` or `scripts/portable-timeout.sh`, production scripts under `scripts/`, production hooks under `hooks/`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/lib/monitor-common.sh`, and the mock `gh` executable in `tests/mocks/gh`.
- edge_cases_or_failure_modes: Covered edge cases include zsh no-match globs, shell command chaining and redirection bypasses, command substitution in cancel operations, symlinked state sources, malformed or deeply nested hook JSON, invalid UTF-8/null bytes, missing templates, large template values, partial state writes, stale sessions, rapid session deletion, force-push detection, PR API rate/failure behavior, terminal cleanup after monitor interruption, and git detached/rebase/merge/shallow states.
- validation_or_tests: The directory is the validation surface. Key high-level suites include state exit naming, finalize phase, cancel signal security, monitor runtime/e2e/zsh safety, PR loop scripts/hooks/stophook/system, template loader/reference/comprehensive tests, todo checker tests, and robustness suites listed by the aggregate runner.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell entrypoint and monitor runtime for Humanize loops. It routes `humanize monitor rlcr` and `humanize monitor pr`, renders live loop status, tails activity logs, parses goal/git/state metadata, and delegates shared monitor primitives to `scripts/lib/monitor-common.sh`.
- algorithmic_behavior: The script sources shared monitor helpers at `scripts/humanize.sh:6-10`. It provides bash/zsh-compatible pipe splitting at `scripts/humanize.sh:16-28`, RLCR goal tracker parsing at `scripts/humanize.sh:30-112`, git special-state detection at `scripts/humanize.sh:114-162`, and git status/diffstat parsing at `scripts/humanize.sh:164-210`. `_humanize_monitor_codex` watches `.humanize/rlcr` from `scripts/humanize.sh:219`, locates the latest session through the shared helper at `scripts/humanize.sh:237-240`, finds cached Codex run/review logs with project-path sanitization at `scripts/humanize.sh:242-341`, parses state fields at `scripts/humanize.sh:349-365`, draws a fixed status bar at `scripts/humanize.sh:373-510`, and handles log growth, truncation, deletion, session switching, and graceful shutdown at `scripts/humanize.sh:620-871`. `humanize()` dispatches monitor subcommands at `scripts/humanize.sh:883-924`. `_humanize_monitor_pr` watches `.humanize/pr-loop`, supports `--once` at `scripts/humanize.sh:942-953`, selects latest PR check/feedback/comment files at `scripts/humanize.sh:967-1009`, maps state names for display at `scripts/humanize.sh:1011-1028`, parses PR state YAML at `scripts/humanize.sh:1030-1059`, renders PR status at `scripts/humanize.sh:1062-1167`, and tails or snapshots recent files at `scripts/humanize.sh:1215-1364`.
- inputs_outputs_state: Inputs are `.humanize/rlcr`, `.humanize/pr-loop`, state frontmatter, `goal-tracker.md`, round files, cached Codex logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, terminal dimensions via `tput`, and current git status. Outputs are terminal status bars, rolling log tails, one-shot PR monitor text, and command exit statuses. It keeps in-process monitor state such as current session, current file, tail pid, last file size, and cleanup flags; it does not modify loop state files.
- gates_or_invariants: Monitor commands require the corresponding loop directory to exist, otherwise return failure. RLCR log discovery enforces run/review round consistency by rejecting a session where the max run round is greater than or equal to the first review round at `scripts/humanize.sh:330-337`. Signal traps must restore terminal state and stop background tails. PR `--once` requires a session and state file at `scripts/humanize.sh:1215-1230`.
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh`, shell utilities (`find`, `stat`, `sed`, `grep`, `tail`, `tput`, `date`, `git`), the Humanize session directory schema, and the `humanize monitor <rlcr|pr>` command surface. Tests exercise it through monitor runtime/e2e/zsh suites, PR system monitor output tests, goal tracker robustness, git operation robustness, and hook-input monitor edge tests.
- edge_cases_or_failure_modes: Handles missing loop directories, no sessions, no logs yet, deleted sessions/logs, truncated or rotated logs, newer sessions appearing mid-monitor, non-git directories, missing state fields defaulting to `N/A` or fallback values, narrow terminals, bash/zsh array differences, and terminal cleanup on interrupts. One mismatch to watch: RLCR completion files are named `complete-state.md` by `end_loop`, while some monitor color/status helpers use `completed`, so completed-loop display can fall through to a default color/status label.
- validation_or_tests: Covered by `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, `tests/test-pr-loop-system.sh` monitor phase/shared-monitor cases, `tests/robustness/test-hook-input-robustness.sh`, `tests/robustness/test-goal-tracker-robustness.sh`, and `tests/robustness/test-git-operations-robustness.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Focused executable spec for loop state file naming and active-loop detection. It verifies that only active state files keep a loop discoverable and that `end_loop` preserves terminal state under reason-specific filenames.
- algorithmic_behavior: The test creates an isolated temp git repo and loop directory at `tests/test-state-exit-naming.sh:31-49`, then sources `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:64-67`. Tests 1-6 assert that `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active while `state.md` is active at `tests/test-state-exit-naming.sh:38-159`. Test 7 asserts newest session precedence at `tests/test-state-exit-naming.sh:161-180`. Tests 8-10 verify `end_loop` rejects invalid reasons, creates each valid `{reason}-state.md`, and fails clearly when the source state file is missing at `tests/test-state-exit-naming.sh:186-243`. Tests 11-13 assert `.humanize/rlcr` path recognition and legacy `.humanize-loop.local` separation at `tests/test-state-exit-naming.sh:249-291`.
- inputs_outputs_state: Inputs are synthetic loop directories, hand-written YAML frontmatter, valid/invalid end reasons, and path strings. Outputs are pass/fail counters and terminal state files such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md`. The state transition under test is `state.md` renamed to `{reason}-state.md`.
- gates_or_invariants: The production `find_active_loop` only checks the newest session and treats `state.md` or `finalize-state.md` as active at `hooks/lib/loop-common.sh:151-170`. `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` at `hooks/lib/loop-common.sh:1124-1132`, renames only if the state file exists at `hooks/lib/loop-common.sh:1134-1144`, and fails otherwise. Legacy loop paths must not match `is_in_humanize_loop_dir`, which is scoped to `.humanize/rlcr/` at `hooks/lib/loop-common.sh:712-716`.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`, plus `git`, `mktemp`, and shell file operations. It is included in the aggregate test order at `tests/run-all-tests.sh:32-40`.
- edge_cases_or_failure_modes: Guards against zombie loops where only terminal state files remain, accidental resurrection of older sessions, invalid end reasons, missing state files, and legacy path confusion. It does not validate `finalize-state.md` active detection; that is covered in finalize/state robustness suites.
- validation_or_tests: This file is itself the validation. It exits with `TESTS_FAILED` at `tests/test-state-exit-naming.sh:293-302`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template defining the user-facing contract when an agent attempts to modify RLCR `state.md`. It is part of the validation surface because hook blockers render this message when protecting loop state.
- algorithmic_behavior: The template emits a titled block at `prompt-template/block/state-file-modification.md:1`, states that `state.md` is loop-system-managed at `prompt-template/block/state-file-modification.md:3`, names representative state contents at `prompt-template/block/state-file-modification.md:5-8`, and explains corruption risk at `prompt-template/block/state-file-modification.md:10`. `state_file_blocked_message` loads it through `load_and_render_safe` at `hooks/lib/loop-common.sh:463-470`.
- inputs_outputs_state: Input is the static markdown template under `prompt-template/block`. Output is the rendered block message, typically sent to stderr or hook response when a state modification is blocked. It has no mutable state and no placeholders.
- gates_or_invariants: The gate is message availability and clarity: blocked `state.md` writes must produce a concrete explanation. `load_and_render_safe` falls back to an inline message if the template is missing or renders empty at `hooks/lib/template-loader.sh:170-202`. Bash validator paths call the message after detecting state modification attempts at `hooks/loop-bash-validator.sh:148-155`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, and the hook validators that protect Write/Edit/Bash operations. Related templates split specialized cases, such as `prompt-template/block/finalize-state-file-modification.md` and `prompt-template/block/pr-loop-state-modification.md`.
- edge_cases_or_failure_modes: The template is intentionally generic to `state.md`; it does not cover `finalize-state.md` or PR loop state files. Its content list is representative rather than a complete schema, so schema additions must not rely on this text as the authoritative field list.
- validation_or_tests: Template existence and renderability are covered by `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-template-loader.sh`, and template robustness suites.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-127 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for RLCR and PR loop status displays. It centralizes file/session/state detection, ANSI/status formatting, YAML frontmatter extraction, PR phase classification, and goal tracker parsing.
- algorithmic_behavior: Color helpers are defined at `scripts/lib/monitor-common.sh:14-23`, cross-platform file sizing at `scripts/lib/monitor-common.sh:29-35`, timestamp-named latest-session selection at `scripts/lib/monitor-common.sh:37-63`, terminal setup/restore helpers at `scripts/lib/monitor-common.sh:69-92`, and status color mapping at `scripts/lib/monitor-common.sh:128-141`. `monitor_find_state_file` prioritizes `state.md` as active, otherwise returns the first `*-state.md` and derives status from the filename at `scripts/lib/monitor-common.sh:147-188`. `monitor_get_yaml_value`, timestamp formatting, and truncation helpers live at `scripts/lib/monitor-common.sh:194-252`. `get_pr_loop_phase` classifies PR loops by terminal state files, growing `round-*-pr-check.md` files, and state frontmatter at `scripts/lib/monitor-common.sh:258-342`; display strings are mapped at `scripts/lib/monitor-common.sh:344-381`. Goal tracker parsers return pipe-delimited metrics for RLCR and PR flows at `scripts/lib/monitor-common.sh:387-498`.
- inputs_outputs_state: Inputs are loop/session directories, state files, PR check files, goal tracker markdown, filesystem timestamps/sizes, and the current clock. Outputs are simple strings such as `path|status`, phase names, display text, ANSI escape codes, and pipe-delimited metric tuples. The only external state write is the PR phase size cache at `/tmp/humanize-phase-${session_name}-${highest_round}.size` in `scripts/lib/monitor-common.sh:297-307`.
- gates_or_invariants: Session discovery only accepts names matching `YYYY-MM-DD_HH-MM-SS` at `scripts/lib/monitor-common.sh:54-60`. `state.md` always wins over terminal state files at `scripts/lib/monitor-common.sh:162-165`. PR phase detection checks final states before activity heuristics at `scripts/lib/monitor-common.sh:272-275`, selects the highest numeric check round at `scripts/lib/monitor-common.sh:281-290`, and treats growth or recent mtime as Codex analysis at `scripts/lib/monitor-common.sh:292-318`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at `scripts/humanize.sh:6-10` and directly sourced in PR system tests. Depends on `find`, `stat` GNU/BSD variants, `sed`, `grep`, `sort`, `wc`, `date`, and writable `/tmp` for phase caching.
- edge_cases_or_failure_modes: Handles missing loop/session/state files by returning blank or `unknown`. `monitor_find_state_file` uses unsorted `find` for terminal state files, so multiple terminal files in one session have nondeterministic selection. It returns raw reasons such as `complete` from `complete-state.md`, while some display color maps use `completed`; callers may need normalization. `/tmp` cache writes are ignored on failure, which avoids monitor crashes but can weaken growth detection. Clock skew or stale mtime can misclassify `codex_analyzing`.
- validation_or_tests: Covered by shared monitor tests in `tests/test-pr-loop-system.sh` for latest session, state file, file size, PR phase, and monitor output; also indirectly by monitor runtime/e2e/zsh tests and state-exit/finalize/state robustness suites.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 of 5 item sections present exactly once in Item Evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`