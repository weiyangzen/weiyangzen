# agent_12 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-facing review prompt template directory for the RLCR loop. It defines the review contracts used when Claude implementation rounds transition into Codex review, full alignment audit, and goal-tracker reconciliation.
- algorithmic_behavior: The directory contains four templates: `code-review-phase.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`. `regular-review.md` requires reading the original plan, comparing Claude claims to actual implementation, checking goal tracker alignment, and only ending with `COMPLETE` when all original plan tasks and acceptance criteria are fully complete with no deferrals or pending work (`prompt-template/codex/regular-review.md:5`, `prompt-template/codex/regular-review.md:20`, `prompt-template/codex/regular-review.md:33`, `prompt-template/codex/regular-review.md:51`). `full-alignment-review.md` adds a mandatory checkpoint audit for every acceptance criterion, forgotten items, deferred items, progress stagnation, and STOP/COMPLETE terminal decisions (`prompt-template/codex/full-alignment-review.md:19`, `prompt-template/codex/full-alignment-review.md:30`, `prompt-template/codex/full-alignment-review.md:36`, `prompt-template/codex/full-alignment-review.md:59`, `prompt-template/codex/full-alignment-review.md:86`). `goal-tracker-update-section.md` delegates post-round tracker edits to Codex, while preserving the immutable Ultimate Goal and Acceptance Criteria section (`prompt-template/codex/goal-tracker-update-section.md:1`, `prompt-template/codex/goal-tracker-update-section.md:3`, `prompt-template/codex/goal-tracker-update-section.md:6`, `prompt-template/codex/goal-tracker-update-section.md:10`). `code-review-phase.md` is an audit artifact for `codex review --base`, including expected severity marker parsing and generated files (`prompt-template/codex/code-review-phase.md:3`, `prompt-template/codex/code-review-phase.md:14`, `prompt-template/codex/code-review-phase.md:15`, `prompt-template/codex/code-review-phase.md:30`).
- inputs_outputs_state: Inputs are template variables such as `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, previous-round indices, `REVIEW_RESULT_FILE`, `BASE_BRANCH`, `BASE_COMMIT`, and timestamps. Outputs are rendered prompt/audit markdown files in the loop directory, especially `round-N-review-prompt.md`, `round-N-review-result.md`, and `round-N-codex-review.*` audit/cache artifacts. Runtime selection happens in `hooks/loop-codex-stop-hook.sh`: it renders the shared goal-tracker section first (`hooks/loop-codex-stop-hook.sh:767`), selects full alignment rounds by modulo of `CURRENT_ROUND` and `FULL_REVIEW_ROUND` (`hooks/loop-codex-stop-hook.sh:773`), then renders either `codex/full-alignment-review.md` or `codex/regular-review.md` (`hooks/loop-codex-stop-hook.sh:816`, `hooks/loop-codex-stop-hook.sh:831`). The review-phase audit template is rendered before `codex review` invocation (`hooks/loop-codex-stop-hook.sh:930`, `hooks/loop-codex-stop-hook.sh:948`).
- gates_or_invariants: The templates encode strict terminal gates: no COMPLETE if any task is deferred or unfinished (`prompt-template/codex/regular-review.md:53`, `prompt-template/codex/regular-review.md:54`, `prompt-template/codex/regular-review.md:55`, `prompt-template/codex/full-alignment-review.md:88`, `prompt-template/codex/full-alignment-review.md:90`, `prompt-template/codex/full-alignment-review.md:91`). Full alignment includes a stagnation circuit breaker that writes `STOP` as the final line when repeated issues or circular progress are detected (`prompt-template/codex/full-alignment-review.md:76`, `prompt-template/codex/full-alignment-review.md:84`). Goal-tracker updates must not modify immutable goal/AC content (`prompt-template/codex/goal-tracker-update-section.md:10`).
- dependencies_and_callers: Called through `hooks/loop-codex-stop-hook.sh`, using `load_and_render_safe` and fallbacks. Template rendering is provided by `hooks/lib/template-loader.sh`, which defines `{{VARIABLE_NAME}}` syntax, single-pass replacement, and preservation of missing placeholders (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`). Template coverage also appears in `tests/test-templates-comprehensive.sh`, which checks `codex/goal-tracker-update-section.md` rendering with `GOAL_TRACKER_FILE` (`tests/test-templates-comprehensive.sh:531`, `tests/test-templates-comprehensive.sh:535`).
- edge_cases_or_failure_modes: Missing templates fall back to inline prompt bodies in the stop hook (`hooks/loop-codex-stop-hook.sh:768`, `hooks/loop-codex-stop-hook.sh:794`, `hooks/loop-codex-stop-hook.sh:805`, `hooks/loop-codex-stop-hook.sh:936`), so template absence degrades prompt richness but does not necessarily stop execution. Missing variables remain as placeholders due to loader behavior (`hooks/lib/template-loader.sh:13`, `hooks/lib/template-loader.sh:120`), which can surface unresolved `{{...}}` text in prompts. `code-review-phase.md` mentions base branch but the hook also passes base commit/review base variables; the template does not display all of those extra variables, while the fallback does (`hooks/loop-codex-stop-hook.sh:941`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` validates real template rendering for `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:531`). Runtime behavior is indirectly covered by the stop hook’s use of `load_and_render_safe`; no direct test for every Codex template was found in the inspected references.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime validator and generator bootstrap for the `gen-plan` command. It enforces input/output file safety, locates the plan template, and materializes the initial plan file by combining the template with the user draft.
- algorithmic_behavior: Parses `--input` and `--output` flags, rejects missing values and unknown options, canonicalizes paths with `realpath -m`, validates input existence/non-emptiness, validates output directory existence, rejects existing output paths and directory outputs, checks write permission, then copies `prompt-template/plan/gen-plan-template.md` and appends the original draft under explicit start/end separators (`scripts/validate-gen-plan-io.sh:29`, `scripts/validate-gen-plan-io.sh:69`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:87`, `scripts/validate-gen-plan-io.sh:95`, `scripts/validate-gen-plan-io.sh:103`, `scripts/validate-gen-plan-io.sh:118`, `scripts/validate-gen-plan-io.sh:133`, `scripts/validate-gen-plan-io.sh:148`, `scripts/validate-gen-plan-io.sh:151`).
- inputs_outputs_state: Inputs are CLI args `--input <draft.md>` and `--output <plan.md>`, plus optional `CLAUDE_PLUGIN_ROOT`. Output is either a validation error with a stable exit code or a newly written plan file. The script emits status lines including absolute input/output paths, `VALIDATION_SUCCESS`, line count, and final write location (`scripts/validate-gen-plan-io.sh:74`, `scripts/validate-gen-plan-io.sh:126`, `scripts/validate-gen-plan-io.sh:127`, `scripts/validate-gen-plan-io.sh:159`). It mutates filesystem state only by creating the output file after all IO checks pass.
- gates_or_invariants: Exit codes are part of the command protocol: input not found `1`, empty input `2`, missing output directory `3`, existing output or output directory `4`, no write permission `5`, invalid args/help `6`, missing plan template `7` (`scripts/validate-gen-plan-io.sh:4`). The output path must not exist before generation (`scripts/validate-gen-plan-io.sh:111`). The template must exist, otherwise generation stops as a plugin configuration error (`scripts/validate-gen-plan-io.sh:142`).
- dependencies_and_callers: `commands/gen-plan.md` allows this script as a Bash tool and makes it Phase 1 of the command workflow (`commands/gen-plan.md:4`, `commands/gen-plan.md:30`, `commands/gen-plan.md:35`). The command maps the script’s exit codes to user-facing stop/continue behavior (`commands/gen-plan.md:38`). The script depends on `realpath`, `dirname`, `wc`, `cp`, `cat`, and `prompt-template/plan/gen-plan-template.md`; it resolves the template through `CLAUDE_PLUGIN_ROOT` first, then script-relative fallback (`scripts/validate-gen-plan-io.sh:135`, `scripts/validate-gen-plan-io.sh:138`).
- edge_cases_or_failure_modes: `set -e` makes unhandled command failures abort the script, but most expected checks are explicit. `realpath -m` fallback preserves the original path if `realpath` is unavailable or fails (`scripts/validate-gen-plan-io.sh:70`). Permission check uses `-w`, which may be environment-dependent under elevated or unusual filesystem permissions. The script copies the template before appending the draft; if append fails after copy, a partially created output could remain because there is no rollback. `usage` exits `6` even for `--help`, matching tests but making help a non-success path (`scripts/validate-gen-plan-io.sh:16`, `scripts/validate-gen-plan-io.sh:48`).
- validation_or_tests: `tests/test-gen-plan.sh` exercises argument errors, missing input, empty input, missing output directory, output exists, output is directory, valid success, and help exit code (`tests/test-gen-plan.sh:438`, `tests/test-gen-plan.sh:447`, `tests/test-gen-plan.sh:456`, `tests/test-gen-plan.sh:465`, `tests/test-gen-plan.sh:474`, `tests/test-gen-plan.sh:483`, `tests/test-gen-plan.sh:493`, `tests/test-gen-plan.sh:503`, `tests/test-gen-plan.sh:513`, `tests/test-gen-plan.sh:523`, `tests/test-gen-plan.sh:532`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh safety specification for monitor path/session/log discovery. It protects the monitor algorithm from zsh `nomatch` failures on empty directories, dotfile-only directories, and missing log/state files.
- algorithmic_behavior: The test requires zsh (`#!/usr/bin/env zsh`) and runs with `set -euo pipefail` (`tests/test-zsh-monitor-safety.sh:1`, `tests/test-zsh-monitor-safety.sh:13`). It creates an isolated `/tmp/test-zsh-humanize-$$` workspace, sets `XDG_CACHE_HOME` under that workspace, and cleans up on exit (`tests/test-zsh-monitor-safety.sh:48`, `tests/test-zsh-monitor-safety.sh:53`, `tests/test-zsh-monitor-safety.sh:56`). It sources `scripts/humanize.sh` and fails if sourcing emits `no matches found` (`tests/test-zsh-monitor-safety.sh:72`). It then validates safe `find`-based session iteration with empty loop dirs, dotfiles only, no `*-state.md`, empty Codex cache, valid timestamp sessions, shell verification, and old-glob-vs-new-find behavior (`tests/test-zsh-monitor-safety.sh:80`, `tests/test-zsh-monitor-safety.sh:127`, `tests/test-zsh-monitor-safety.sh:160`, `tests/test-zsh-monitor-safety.sh:208`, `tests/test-zsh-monitor-safety.sh:240`, `tests/test-zsh-monitor-safety.sh:276`, `tests/test-zsh-monitor-safety.sh:289`).
- inputs_outputs_state: Inputs are temporary `.humanize/rlcr` session directories, dotfiles, state files, and cache directories. Outputs are PASS/FAIL lines and final exit code based on `TESTS_FAILED` (`tests/test-zsh-monitor-safety.sh:27`, `tests/test-zsh-monitor-safety.sh:32`, `tests/test-zsh-monitor-safety.sh:333`). State transitions are test-local: creates/removes `.humanize/rlcr`, creates timestamped session dirs, creates cache paths under `XDG_CACHE_HOME`, and verifies computed latest session/log counts.
- gates_or_invariants: Invariant is no zsh `no matches found` error under monitor discovery conditions. The production invariant is implemented by `monitor_find_latest_session`, which uses `find` instead of glob and returns an empty string when the loop dir is absent or contains no valid timestamp dirs (`scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:44`, `scripts/lib/monitor-common.sh:49`, `scripts/lib/monitor-common.sh:60`). State detection similarly prioritizes `state.md`, then first `*-state.md`, else `|unknown` using `find` (`scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:162`, `scripts/lib/monitor-common.sh:168`, `scripts/lib/monitor-common.sh:181`). Codex log discovery in `scripts/humanize.sh` also uses `find` for sessions and cache logs (`scripts/humanize.sh:280`, `scripts/humanize.sh:328`, `scripts/humanize.sh:339`).
- dependencies_and_callers: Depends on zsh, `scripts/humanize.sh`, monitor utilities sourced by that script, `find`, `basename`, `mkdir`, `touch`, and temp filesystem access. Production monitor entrypoint is `_humanize_monitor_codex`, which wraps `monitor_find_latest_session`, `monitor_find_state_file`, and `humanize_parse_goal_tracker` (`scripts/humanize.sh:219`, `scripts/humanize.sh:237`, `scripts/humanize.sh:344`, `scripts/humanize.sh:367`).
- edge_cases_or_failure_modes: The test catches a historical glob failure pattern by demonstrating the old `.humanize/rlcr/*` loop under zsh (`tests/test-zsh-monitor-safety.sh:295`, `tests/test-zsh-monitor-safety.sh:307`) and comparing it with `find` (`tests/test-zsh-monitor-safety.sh:314`). Several tests duplicate helper logic locally rather than invoking the private helper functions inside `_humanize_monitor_codex`, so they verify the intended algorithmic pattern plus source-time safety, not every production closure directly. `find` output order for `*-state.md` is not sorted in the production helper or copied test body, so multiple stopped state files may select filesystem order’s first match (`scripts/lib/monitor-common.sh:172`).
- validation_or_tests: This file is itself the validation surface. It exits zero only when all eight zsh safety tests pass (`tests/test-zsh-monitor-safety.sh:323`, `tests/test-zsh-monitor-safety.sh:333`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Blocking prompt template for read/write path validators when Claude targets the wrong loop directory path. It is a guard-contract template, not executable logic by itself.
- algorithmic_behavior: Renders a short diagnostic with attempted action, requested file path, and correct path (`prompt-template/block/wrong-directory-path.md:1`, `prompt-template/block/wrong-directory-path.md:3`, `prompt-template/block/wrong-directory-path.md:4`). It is selected when validator-computed `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` differs from the requested `FILE_PATH` (`hooks/loop-read-validator.sh:157`, `hooks/loop-read-validator.sh:159`, `hooks/loop-write-validator.sh:252`, `hooks/loop-write-validator.sh:254`).
- inputs_outputs_state: Inputs are `ACTION`, `FILE_PATH`, and `CORRECT_PATH`. Output is rendered markdown to stderr through `load_and_render_safe`; validators then exit with code `2` (`hooks/loop-read-validator.sh:163`, `hooks/loop-read-validator.sh:167`, `hooks/loop-write-validator.sh:258`, `hooks/loop-write-validator.sh:262`). It does not mutate repository state.
- gates_or_invariants: Enforces active-loop-directory isolation: only the exact active loop path for the current round filename is accepted. The template participates in a hard block; mismatched paths do not continue to the requested read/write action.
- dependencies_and_callers: Called by both `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`. Rendering uses the shared template loader’s `{{VAR}}` single-pass placeholder substitution contract (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`).
- edge_cases_or_failure_modes: The final instruction says `If you need this file, use: cat {{FILE_PATH}}` (`prompt-template/block/wrong-directory-path.md:6`). Because this template is specifically emitted when `FILE_PATH` is wrong and `CORRECT_PATH` is known, this appears contradictory; it likely should point to `{{CORRECT_PATH}}`. As written, the diagnostic can re-instruct the agent to read the rejected path.
- validation_or_tests: No direct test for this exact template was found in the inspected test references. Related template rendering tests cover other block templates and the loader behavior (`tests/test-templates-comprehensive.sh:500`, `tests/test-templates-comprehensive.sh:506`), but not this wrong-directory-path output specifically.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-132 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the production `humanize_parse_goal_tracker` parser, which feeds monitor status summaries from `goal-tracker.md`.
- algorithmic_behavior: Sources test helpers and production `scripts/humanize.sh`, then calls `humanize_parse_goal_tracker` against many generated tracker variants (`tests/robustness/test-goal-tracker-robustness.sh:18`, `tests/robustness/test-goal-tracker-robustness.sh:21`, `tests/robustness/test-goal-tracker-robustness.sh:24`). The parser returns seven pipe-delimited fields: `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`tests/robustness/test-goal-tracker-robustness.sh:35`, `scripts/humanize.sh:30`, `scripts/humanize.sh:111`). The test helper `parse_result` extracts fields with `cut` (`tests/robustness/test-goal-tracker-robustness.sh:39`).
- inputs_outputs_state: Inputs are temporary markdown files representing goal trackers in list, table, malformed, truncated, binary-mixed, and section-only forms. Outputs are PASS/FAIL assertions and final summary via `print_test_summary` (`tests/robustness/test-goal-tracker-robustness.sh:530`). The production parser reads the tracker file, counts ACs between `### Acceptance Criteria` and `---`, counts Active Tasks table rows minus completed/deferred statuses, counts Completed/Deferred/Open Issues table rows, counts unique completed ACs, and extracts a 60-character Ultimate Goal summary (`scripts/humanize.sh:48`, `scripts/humanize.sh:56`, `scripts/humanize.sh:86`, `scripts/humanize.sh:90`, `scripts/humanize.sh:96`, `scripts/humanize.sh:100`, `scripts/humanize.sh:104`).
- gates_or_invariants: Nonexistent tracker must return `0|0|0|0|0|0|No goal tracker` (`tests/robustness/test-goal-tracker-robustness.sh:237`, `scripts/humanize.sh:34`). Empty or headers-only files should produce zero counts (`tests/robustness/test-goal-tracker-robustness.sh:246`, `tests/robustness/test-goal-tracker-robustness.sh:441`). Malformed files without proper headers should not produce false AC counts (`tests/robustness/test-goal-tracker-robustness.sh:304`). Long goal summaries should be truncated to at most 60 chars (`tests/robustness/test-goal-tracker-robustness.sh:501`, `scripts/humanize.sh:108`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/humanize.sh`, and shell utilities `sed`, `grep`, `sort`, `wc`, `cut`, `seq`, and `printf`. Production caller is the Codex monitor status bar: `_draw_status_bar` reads `$session_dir/goal-tracker.md`, splits the parser output, and displays total/completed ACs, active/completed/deferred tasks, open issues, and goal summary (`scripts/humanize.sh:400`, `scripts/humanize.sh:401`, `scripts/humanize.sh:402`, `scripts/humanize.sh:403`).
- edge_cases_or_failure_modes: The parser’s pipe-delimited output can be fragile if `goal_summary` itself contains `|`, because `parse_result` and monitor splitting use `|` as a delimiter. AC regex intentionally counts `AC-1` and bold `AC-2` but not decimal subcriteria like `AC-3.1`, documented by the mixed-format test (`tests/robustness/test-goal-tracker-robustness.sh:477`, `tests/robustness/test-goal-tracker-robustness.sh:494`). Table counting assumes two header rows and section delimiters matching `^###`; unusual markdown table structures can undercount or overcount. Binary-mixed content is expected to degrade gracefully with at least parseable ACs counted (`tests/robustness/test-goal-tracker-robustness.sh:349`).
- validation_or_tests: This file defines 18 robustness tests covering standard list ACs, table ACs, active task exclusion, completed task and unique AC counts, goal extraction, nonexistent/empty/malformed/truncated/binary files, open issues, deferred tasks, headers-only files, mixed AC formats, and long goal truncation (`tests/robustness/test-goal-tracker-robustness.sh:60`, `tests/robustness/test-goal-tracker-robustness.sh:89`, `tests/robustness/test-goal-tracker-robustness.sh:118`, `tests/robustness/test-goal-tracker-robustness.sh:155`, `tests/robustness/test-goal-tracker-robustness.sh:194`, `tests/robustness/test-goal-tracker-robustness.sh:204`, `tests/robustness/test-goal-tracker-robustness.sh:237`, `tests/robustness/test-goal-tracker-robustness.sh:258`, `tests/robustness/test-goal-tracker-robustness.sh:373`, `tests/robustness/test-goal-tracker-robustness.sh:411`, `tests/robustness/test-goal-tracker-robustness.sh:477`, `tests/robustness/test-goal-tracker-robustness.sh:501`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 section headers present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`