# agent_02 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: The `agents` directory is a subagent-spec surface. Recursive inspection found one child, `agents/draft-relevance-checker.md`, which defines the `draft-relevance-checker` agent used as a lightweight relevance gate for `gen-plan`.
- algorithmic_behavior: `draft-relevance-checker.md` declares frontmatter fields `name: draft-relevance-checker`, `description`, `model: haiku`, and `tools: Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-6`. Its algorithm is prompt-defined: inspect repository docs/structure, compare draft content to repository concepts/files/features, then emit exactly one verdict shape, `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` at `agents/draft-relevance-checker.md:12-29`. It uses a lenient semantic policy: informal drafts are acceptable, syntactic similarity is not required, and uncertainty should lean relevant at `agents/draft-relevance-checker.md:31-36`.
- inputs_outputs_state: Input is draft document content plus read-only repository context gathered through `Read`, `Glob`, and `Grep`. Output is a single textual verdict consumed by the caller. There is no persistent state, no file mutation, and no checklist cursor state inside this directory.
- gates_or_invariants: The gate invariant is binary relevance classification with a deliberately low rejection threshold. The agent contract requires one of the two verdict prefixes, making downstream command routing simple. Frontmatter is also part of the invariant because tests validate name/model/tools.
- dependencies_and_callers: `commands/gen-plan.md` invokes this agent through the Task tool after IO validation, with haiku model and a prompt requiring repo exploration, relation analysis, and a `RELEVANT`/`NOT_RELEVANT` return at `commands/gen-plan.md:50-65`. If the verdict is `NOT_RELEVANT`, `gen-plan` stops and reports the reason at `commands/gen-plan.md:67-70`. This directory coordinates with sibling `commands/` as command-time validation metadata, not with PR-loop runtime hooks.
- edge_cases_or_failure_modes: The lenient policy can allow weakly related drafts through, by design. Conversely, because the contract is textual, malformed output without the expected prefix could break caller interpretation. The tools are read/search only, so the agent cannot directly alter repository state.
- validation_or_tests: `tests/test-gen-plan.sh` validates the agent file exists at `tests/test-gen-plan.sh:106-116`, verifies the name at `tests/test-gen-plan.sh:119-129`, verifies `model: haiku` at `tests/test-gen-plan.sh:132-143`, checks a `tools:` field at `tests/test-gen-plan.sh:146-156`, validates naming at `tests/test-gen-plan.sh:241-245`, checks required frontmatter at `tests/test-gen-plan.sh:289-295`, checks model validity at `tests/test-gen-plan.sh:391-399`, and checks English/no emoji/CJK content at `tests/test-gen-plan.sh:417-422`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for active PR loops. It terminates the active-loop state by creating a cancel signal and renaming `state.md` to `cancel-state.md`, preserving loop artifacts while making the loop no longer active.
- algorithmic_behavior: The script runs under `set -euo pipefail` at `scripts/cancel-pr-loop.sh:18`. It parses `--force`, `-h`, and `--help`, with unknown options exiting `3` at `scripts/cancel-pr-loop.sh:24-66`. `--force` currently only sets `FORCE=true` and has no behavioral effect. It resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, then sets `LOOP_BASE_DIR="$PROJECT_ROOT/.humanize/pr-loop"` at `scripts/cancel-pr-loop.sh:72-73`. It selects the lexicographically newest loop directory using `ls -1d ... | sort -r | head -1` at `scripts/cancel-pr-loop.sh:75-76`. If no directory exists, it prints `NO_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:78-82`. If the newest directory lacks `state.md`, it prints `NO_ACTIVE_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:88-98`. On active state, it extracts `current_round`, `max_iterations`, and `pr_number` from YAML-like lines at `scripts/cancel-pr-loop.sh:104-112`, touches `.cancel-requested` at `scripts/cancel-pr-loop.sh:118-119`, moves `state.md` to `cancel-state.md` at `scripts/cancel-pr-loop.sh:121-122`, then prints `CANCELLED` plus human-readable summary at `scripts/cancel-pr-loop.sh:128-132`.
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR`, current working directory fallback, `.humanize/pr-loop/<timestamp>/state.md`, and metadata keys inside that state file. Outputs are stdout status tokens, exit codes, `.humanize/pr-loop/<timestamp>/.cancel-requested`, and `.humanize/pr-loop/<timestamp>/cancel-state.md`. State transition is `state.md` present -> `.cancel-requested` present and `cancel-state.md` present, with `state.md` removed by `mv`.
- gates_or_invariants: A PR loop is cancelable only when the newest PR-loop directory has `state.md`. The command wrapper explicitly states that active status is defined by `state.md` in the newest `.humanize/pr-loop/` directory at `commands/cancel-pr-loop.md:17-25`. The script intentionally targets only PR loops and not `.humanize/rlcr`, matching help text at `scripts/cancel-pr-loop.sh:48-56`.
- dependencies_and_callers: The slash-command wrapper permits only `scripts/cancel-pr-loop.sh` and `scripts/cancel-pr-loop.sh --force` at `commands/cancel-pr-loop.md:1-4`, runs the script at `commands/cancel-pr-loop.md:9-15`, and interprets first-line status tokens at `commands/cancel-pr-loop.md:17-19`. `scripts/setup-pr-loop.sh` blocks new PR loop setup when `find_active_pr_loop` reports an active PR loop and tells the user to cancel first at `scripts/setup-pr-loop.sh:220-240`; the helper `find_active_pr_loop` uses the same newest-directory plus `state.md` active invariant at `hooks/lib/loop-common.sh:795-814`. The stop hook observes `.cancel-requested` while polling and exits its poll loop at `hooks/pr-loop-stop-hook.sh:951-955`.
- edge_cases_or_failure_modes: Because the script inspects only the newest PR-loop directory, a stale newest directory without `state.md` causes `NO_ACTIVE_LOOP` even if an older directory still has `state.md`. Missing metadata keys are intended to default to `?`, but under `set -euo pipefail`, a failed `grep` in assignments at `scripts/cancel-pr-loop.sh:104-107` can terminate before defaults at `scripts/cancel-pr-loop.sh:109-112`. `--force` is documented as having no additional effect. It does not handle `finalize-state.md`, unlike RLCR cancellation. The `mv` is not guarded against races with the stop hook or preexisting `cancel-state.md`.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` exercises help output at `tests/test-pr-loop-scripts.sh:181-190`, no-loop behavior with `CLAUDE_PROJECT_DIR` pointed at a temp dir and expected `exit=1` plus `NO_LOOP` at `tests/test-pr-loop-scripts.sh:192-209`, and active cancellation with mock `state.md`, expected `CANCELLED`, `cancel-state.md` present, and `state.md` absent at `tests/test-pr-loop-scripts.sh:211-253`. Broader state naming tests treat `cancel-state.md` as a non-active terminal state, visible in search references under `tests/test-state-exit-naming.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-062 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared executable test library for PR-loop test modules. It creates a controlled test environment with mock external dependencies and provides the PR-loop-specific test summary gate.
- algorithmic_behavior: The library uses a load guard, `TEST_PR_LOOP_LIB_LOADED`, to avoid redefining functions on repeated source at `tests/test-pr-loop-lib.sh:10-13`. It initializes `SCRIPT_DIR` and `PROJECT_ROOT` from caller context or file location at `tests/test-pr-loop-lib.sh:14-17`. If `setup_test_dir` is not already declared, it sources `tests/test-helpers.sh` at `tests/test-pr-loop-lib.sh:18-21`. `create_mock_gh` writes an executable `gh` mock that handles `auth status`, `repo view --json owner/name`, `pr view` for commits/number/state, and `api user`, returning empty arrays for other API calls at `tests/test-pr-loop-lib.sh:27-88`. `create_mock_codex` writes a `codex` mock that prints `Mock codex output` and exits `0` at `tests/test-pr-loop-lib.sh:90-101`. `init_pr_loop_test_env` creates a temp test dir, prepends a mock binary directory to `PATH`, installs both mocks, and exports `MOCK_BIN_DIR` at `tests/test-pr-loop-lib.sh:107-121`. `print_test_summary` reports pass/fail counts and returns nonzero if any failures occurred at `tests/test-pr-loop-lib.sh:127-144`.
- inputs_outputs_state: Inputs are the sourcing shell environment, optional existing `SCRIPT_DIR`/`PROJECT_ROOT`, and helper functions from `test-helpers.sh`. Outputs are shell functions, exported `MOCK_BIN_DIR`, mutated `PATH`, a temp `TEST_DIR`, executable mock binaries, and summary return status. Test counters `TESTS_PASSED` and `TESTS_FAILED` are shared with `test-helpers.sh`.
- gates_or_invariants: The idempotent source guard prevents duplicate setup. The mocks define stable GitHub/Codex behavior so PR-loop tests avoid network/model dependencies. `print_test_summary` is a validation gate: any failed assertion makes the test run return `1`.
- dependencies_and_callers: Main runner `tests/test-pr-loop.sh` sources `test-helpers.sh` and this library at `tests/test-pr-loop.sh:18-20`, calls `init_pr_loop_test_env` before sourcing test modules at `tests/test-pr-loop.sh:22-34`, runs script/hook/stophook tests at `tests/test-pr-loop.sh:37-47`, then calls `print_test_summary` at `tests/test-pr-loop.sh:49-53`. The helper dependency `tests/test-helpers.sh` defines `pass`, `fail`, `skip`, `setup_test_dir`, and git test setup at `tests/test-helpers.sh:22-105`.
- edge_cases_or_failure_modes: The `gh` mock is intentionally partial and exits `1` for unhandled commands at `tests/test-pr-loop-lib.sh:82-85`, so tests requiring unmocked `gh` behavior will fail unless they extend the mock. The mock API returns `[]` for most API calls, which is useful for empty-review/comment paths but can mask response-shape assumptions. `print_test_summary` here does not include skipped count, unlike the generic helper summary in `tests/test-helpers.sh:58-78`.
- validation_or_tests: This file is itself validation infrastructure. Its behavior is exercised indirectly by `tests/test-pr-loop.sh`, which requires it before all PR-loop script, hook, and stop-hook tests. The cancel-script tests above depend on this library for `TEST_DIR`, mock binary setup, and pass/fail accounting.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-092 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR loop plan-file integrity gate. It explains why execution is blocked when the original plan file diverges from the loop backup.
- algorithmic_behavior: The template renders a `Plan File Modified` block and states that `{{PLAN_FILE}}` changed since session start at `prompt-template/block/plan-file-modified.md:1-5`. It declares plan modification forbidden during an active session and gives the recovery sequence: cancel current RLCR loop, update plan, restart RLCR loop with the same plan path at `prompt-template/block/plan-file-modified.md:7-10`. It also emits the backup path via `{{BACKUP_PATH}}` at `prompt-template/block/plan-file-modified.md:12`.
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH` template variables. Output is a rendered human-facing block reason embedded in hook JSON. The template does not mutate state; the state transition is enforced by the caller, which blocks the stop-hook exit rather than advancing the loop.
- gates_or_invariants: The invariant is that active RLCR plan contents must match the loop backup. If the plan file has uncommitted tracked changes, `loop-codex-stop-hook.sh` blocks with inline JSON reason at `hooks/loop-codex-stop-hook.sh:300-311`. If content differs from backup, it uses this template through `load_and_render_safe` and returns `{"decision":"block"}` at `hooks/loop-codex-stop-hook.sh:315-334`.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` is the direct consumer at `hooks/loop-codex-stop-hook.sh:329-331`. Setup-side plan tracking rules are enforced by `scripts/setup-rlcr-loop.sh`, which rejects invalid `--track-plan-file` combinations and modified tracked files according to search references at `scripts/setup-rlcr-loop.sh:450-469`. `hooks/loop-plan-file-validator.sh` separately blocks plan tracking state changes and uncommitted modifications according to search references at `hooks/loop-plan-file-validator.sh:178-224`.
- edge_cases_or_failure_modes: This is RLCR-oriented, not PR-loop-oriented. The wording says “session” at `prompt-template/block/plan-file-modified.md:3`, while the fallback in the hook says “RLCR loop” at `hooks/loop-codex-stop-hook.sh:317-328`; that is a minor consistency risk. If template rendering fails, the hook has an inline fallback. If variables are missing, unresolved placeholders could leak into the block unless `load_and_render_safe` rejects them.
- validation_or_tests: `tests/test-plan-file-hooks.sh` includes a stop-hook test that blocks when the plan file is modified, referenced at `tests/test-plan-file-hooks.sh:483-518`. `tests/test-plan-file-validation.sh` validates setup rejection of tracked/untracked/modified plan-file cases at `tests/test-plan-file-validation.sh:329-423`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-122 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 prompt block for the “no existing comments” startup path. It prevents premature PR comments and routes the user toward writing the round-0 resolution summary before the stop hook polls for bot reviews.
- algorithmic_behavior: The template emits a critical requirements banner at `prompt-template/pr-loop/critical-requirements-no-comments.md:2-4`. It requires the worker/user to write the resolution summary to `{{RESOLVE_PATH}}` at `prompt-template/pr-loop/critical-requirements-no-comments.md:6-10`, with required content noting round 0 is awaiting initial bot reviews and that there are no issues yet at `prompt-template/pr-loop/critical-requirements-no-comments.md:11-14`. It then tells the user to try to exit so the Stop Hook can poll at `prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`. The key negative command is explicit: do not comment on the PR to trigger review at `prompt-template/pr-loop/critical-requirements-no-comments.md:17-18`.
- inputs_outputs_state: Input is `RESOLVE_PATH`, provided by `scripts/setup-pr-loop.sh` as `$LOOP_DIR/round-0-pr-resolve.md` at `scripts/setup-pr-loop.sh:660` and included in `TEMPLATE_VARS` at `scripts/setup-pr-loop.sh:670-677`. Output is rendered prompt text printed after the round-0 prompt. Intended state effect is creation of `round-0-pr-resolve.md` by the user/agent, followed by stop-hook polling rather than immediate PR-comment-triggered review.
- gates_or_invariants: Selection gate is `COMMENT_COUNT == 0`. `scripts/setup-pr-loop.sh` computes that by looking for the exact sentinel `*No comments found.*` in the fetched comment file at `scripts/setup-pr-loop.sh:662-668`. If zero comments, setup loads this template with a fallback at `scripts/setup-pr-loop.sh:829-854`; otherwise it uses `critical-requirements-has-comments.md` at `scripts/setup-pr-loop.sh:855-879`. The invariant is that a brand-new PR should wait for automatic bot reviews rather than posting a manual trigger comment.
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh`, which prints the initial round prompt, then prints this critical block at `scripts/setup-pr-loop.sh:826-881`. It coordinates with `fetch-pr-comments.sh` through the no-comments sentinel and with `hooks/pr-loop-stop-hook.sh`, which polls for bot reviews after the user exits. The setup output also tells users the loop flow and cancellation command at `scripts/setup-pr-loop.sh:800-820`.
- edge_cases_or_failure_modes: The no-comments branch depends on an exact sentinel match; any deviation in the comments file causes the has-comments path. The template is hardcoded for round 0, so reuse outside initial startup would be misleading. If `RESOLVE_PATH` is missing or renderer fails, setup uses an inline fallback with the same placeholder text. The prompt cannot itself enforce writing the file; enforcement depends on stop-hook behavior and downstream tests.
- validation_or_tests: No direct test specifically targeting this template name was found in the inspected references. It is indirectly covered through `scripts/setup-pr-loop.sh` prompt construction and fallback loading. The setup script’s argument and startup behavior are part of `tests/test-pr-loop-scripts.sh`, while stop-hook behavior is run by `tests/test-pr-loop.sh` through `run_stophook_tests` at `tests/test-pr-loop.sh:40-47`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FIX_PR_LOOP-HZ-002`, `FIX_PR_LOOP-HZ-032`, `FIX_PR_LOOP-HZ-062`, `FIX_PR_LOOP-HZ-092`, `FIX_PR_LOOP-HZ-122`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`