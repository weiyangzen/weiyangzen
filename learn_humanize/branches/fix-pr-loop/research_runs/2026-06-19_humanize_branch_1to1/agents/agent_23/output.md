# agent_23 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook validation helper for the RLCR todo-completion gate. It reads Claude Code transcript JSONL and decides whether the current stop attempt can continue based on the latest `TodoWrite` state. Direct implementation is in [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/hooks/check-todos-from-transcript.py:20); caller is [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/hooks/loop-codex-stop-hook.sh:345).
- algorithmic_behavior: `find_latest_todos()` streams the transcript line by line, ignores blank and invalid JSONL rows, and updates `latest_todos` whenever it finds a non-empty `TodoWrite` payload. It supports three transcript shapes: Claude assistant message content blocks, alternative `type: message` entries, and direct `type: tool_use` entries at lines 51-85. `main()` parses hook stdin, expands `~`, obtains latest todos, and reports incomplete todos when `status != "completed"` at lines 90-134.
- inputs_outputs_state: Input is stdin JSON containing optional `transcript_path`; secondary input is the transcript JSONL file. Output is process exit status plus stdout/stderr text. Exit `0` means no blocking todos, no transcript, no todos, missing transcript, or empty stdin. Exit `1` prints `INCOMPLETE_TODOS` plus formatted rows like `- [in_progress] Task`. Exit `2` prints `PARSE_ERROR` on stderr for malformed hook input JSON. No repository state is mutated.
- gates_or_invariants: The only completed state accepted is exact string `completed` at line 123. Missing `status`, empty `status`, `pending`, and `in_progress` are all incomplete. The latest non-empty `TodoWrite` call wins, so older unfinished todo lists do not block if superseded by a later completed list. Invalid transcript JSONL lines are non-fatal, but invalid hook-input JSON is fatal to the hook gate.
- dependencies_and_callers: Called by `loop-codex-stop-hook.sh` before expensive review or finalize completion. The stop hook treats exit `2` as a parse-error block and exit `1` as an immediate incomplete-todos block using `prompt-template/block/incomplete-todos.md` lines 347-390 in the stop hook. Dedicated coverage lives in [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/tests/test-todo-checker.sh:51), and finalize-phase behavior embeds this checker at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/tests/test-finalize-phase.sh:715).
- edge_cases_or_failure_modes: A nonexistent transcript path returns success, so absence of transcript cannot block exit. Malformed transcript rows are ignored individually. An empty latest `todos` array is ignored because updates only happen when `todos` is truthy, so the previous non-empty TodoWrite can remain authoritative. Tool name matching is exact `TodoWrite`; other todo-like tools are ignored. Output ordering is deliberately stdout-only for incomplete todos to avoid mixed buffering lines 126-130.
- validation_or_tests: `tests/test-todo-checker.sh` covers invalid input, empty input, missing transcript path, nonexistent files, all-completed and incomplete todos, in-progress status, invalid JSONL rows, latest-call precedence, direct/alternative transcript formats, missing status, empty content, and Unicode lines 51-293. `tests/test-finalize-phase.sh` verifies incomplete finalize todos block before Codex can run lines 715-741.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR `COMPLETE -> review -> finalize -> complete-state` state machine and the validators that protect finalize artifacts. It creates isolated repos and mock Codex binaries, then drives real hooks such as `loop-codex-stop-hook.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-read-validator.sh`, and `loop-plan-file-validator.sh`.
- algorithmic_behavior: Setup builds a temporary git repo, loop directory, `state.md`, plan backup, and goal tracker lines 40-226. Helper tests prove `is_finalize_state_file_path`, `is_finalize_summary_path`, and `finalize_state_file_blocked_message` exist and match intended filenames lines 238-280. Active-loop detection treats `finalize-state.md` as active but `complete-state.md` as inactive lines 283-320. Validator tests prove `finalize-summary.md` is writable while `finalize-state.md` is protected across Write/Edit/Bash, including source-side `mv` and `cp` attempts lines 338-415.
- inputs_outputs_state: Inputs are generated hook JSON payloads, synthetic transcript files, mocked `codex` outputs, git cleanliness, and loop state frontmatter. Outputs are pass/fail counters and hook exit/JSON behavior. State transitions under test include `state.md -> finalize-state.md` on passing review, `finalize-state.md -> complete-state.md` after finalize gates pass, `state.md -> maxiter-state.md` at max iterations, and state preservation when review fails.
- gates_or_invariants: Finalize completion still requires clean git, complete todos, and `finalize-summary.md`; it does not call Codex after entering finalize lines 429-504 and 698-741. Max-iteration termination bypasses finalize and writes `maxiter-state.md` lines 542-560. Review failures and empty review stdout must block and preserve `state.md` with `review_started: true`, never silently proceed to finalize lines 562-696. Normal RLCR rounds with non-`COMPLETE` review feedback must keep `state.md`, increment rounds, persist review result, and surface feedback lines 743-815.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at line 49. Uses stop-hook phase detection implemented at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/hooks/loop-codex-stop-hook.sh:67), todo checker invocation at line 345 there, finalize completion at lines 737-748, review handling at lines 999-1027, and finalize entry at lines 1032-1113. It also depends on strict state parsing and finalization path helpers in `loop-common.sh` lines 191-330 and 515-525.
- edge_cases_or_failure_modes: Tests guard against manual or accidental `complete-state.md` revival, malformed finalize validator parsing, bypass via Bash redirects or source-file moves/copies, dirty git exit, missing summary, incomplete todos, review command nonzero exit, empty review logs, max-iteration conflict with `COMPLETE`, and Codex invocation during finalize. The test uses `set -uo pipefail` rather than `set -e`, so assertions accumulate failures instead of aborting early.
- validation_or_tests: This file is itself the validation artifact. It enumerates positive and negative cases in the header lines 5-19 and exits with `TESTS_FAILED` at line 876. It cross-checks validators still parse `finalize-state.md` near the end: Bash, Read, and plan-file validators lines 817-865.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR push gate when `push_every_round` is disabled. It defines the message emitted when Bash validation rejects `git push` during a local-only loop.
- algorithmic_behavior: Static Markdown tells the agent that commits should stay local and that the loop handles local commits until completion. It offers the transition flag `--push-every-round` as the opt-in path lines 1-9. No variables are rendered in this template.
- inputs_outputs_state: Input is only template loading via `load_and_render_safe`; output is the rendered block reason. No state changes occur. The effective state input comes from parsed loop state field `push_every_round`.
- gates_or_invariants: The Bash validator checks `PUSH_EVERY_ROUND`; when it is not `true`, any command matching leading `git push` is blocked with this template and exit `2` in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/hooks/loop-bash-validator.sh:88). This preserves the invariant that standard RLCR loops do not push remote commits unless started with the explicit flag.
- dependencies_and_callers: Called by `loop-bash-validator.sh` lines 96-104. Template location depends on `hooks/lib/template-loader.sh`, where `get_template_dir` resolves `prompt-template` lines 24-31 and `load_and_render_safe` provides fallback behavior lines 170-203.
- edge_cases_or_failure_modes: Only Bash commands beginning with `git push` are covered by the caller’s regex; aliases, wrapper scripts, or indirect shell functions are outside this template’s scope. The template has no placeholders, so rendering failure risk is limited to missing-file fallback. It is RLCR-specific; PR-loop unpushed-commit blocking uses `prompt-template/block/unpushed-commits.md` instead of this template.
- validation_or_tests: Template rendering smoke coverage appears in `tests/test-template-loader.sh` for existing-template safe rendering and in `tests/test-templates-comprehensive.sh`; direct behavior is covered through Bash-validator tests elsewhere. The assigned template’s exact text is lines 1-9.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Next-round instruction fragment appended when the RLCR state has `push_every_round: true`. It turns the otherwise local-only workflow into a per-round remote-push obligation.
- algorithmic_behavior: Static note says that because `--push-every-round` is enabled, commits must be pushed after each round line 2. It is appended to the generated next-round prompt only inside the stop-hook’s next-round construction path.
- inputs_outputs_state: Input is `PUSH_EVERY_ROUND` parsed from state frontmatter. Output is an appended Markdown note in `round-${NEXT_ROUND}-prompt.md`; no direct state mutation occurs in the template itself. The caller writes the next prompt and appends this note at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/hooks/loop-codex-stop-hook.sh:1542).
- gates_or_invariants: It pairs with the stop-hook unpushed-commit gate. When `push_every_round` is true, the stop hook blocks exit if local branch is ahead of remote lines 572-602. This template is the proactive instruction; `block/unpushed-commits.md` is the reactive block if the instruction is not followed.
- dependencies_and_callers: Loaded with `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` in `loop-codex-stop-hook.sh` lines 1542-1548, with inline fallback `Also push your changes after committing.` if missing. The state parser defines `push_every_round` as a tracked field and defaults it to false in `hooks/lib/loop-common.sh` lines 23 and 227-240.
- edge_cases_or_failure_modes: Because this is appended only during next-round prompt generation, it does not affect the current stop attempt. If the template is missing or empty, the fallback still preserves the push instruction. The note has no placeholders, so it cannot reflect branch name or remote details.
- validation_or_tests: `tests/test-finalize-phase.sh` uses `push_every_round: false` in generated state lines 190-199, so finalize coverage verifies the default no-push path indirectly. The push-enabled append behavior is specified by the stop-hook branch at lines 1542-1549.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-143 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the prompt template rendering subsystem used by RLCR/PR hooks. It stress-tests `render_template`, `load_and_render`, and `load_and_render_safe`, which are central to producing block prompts, review prompts, finalize prompts, and next-round instructions.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and shared test helpers lines 14-18, then checks normal substitution, safe fallback rendering, generated file rendering, large values, large templates, many substitutions, special shell/regex characters, newlines, empty values, placeholder injection prevention, boundary placeholders, and placeholder-only templates lines 33-289. The underlying renderer is a single-pass awk scanner in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-pr-loop/hooks/lib/template-loader.sh:50).
- inputs_outputs_state: Inputs are in-memory template strings, temporary template files under `$TEST_DIR`, and `VAR=value` substitution arguments. Outputs are rendered strings plus pass/fail counters from `tests/test-helpers.sh`. Temporary files are created outside the repo via `setup_test_dir`; no repository state is modified.
- gates_or_invariants: Rendering must preserve values containing regex operators, ampersands, backslashes, dollar signs, and newlines; missing templates must render fallback; missing variables remain untouched per loader contract; values containing `{{OTHER}}` must not trigger second-pass substitution. Size behavior is explicitly tested with 10KB and 100KB variable values and a roughly 100KB template.
- dependencies_and_callers: Direct dependency is `template-loader.sh`; `load_and_render_safe` is used widely by stop hooks and validators for block messages and phase prompts. Shared assertions come from `tests/test-helpers.sh` lines 30-78. Related template reference coverage is mentioned in `tests/test-template-references.sh`, and broader special-character cases appear in `tests/test-template-loader.sh`, but this assigned file focuses on stress and injection-like robustness.
- edge_cases_or_failure_modes: The test documents that circular references are out of scope because templates do not support variable-to-variable references lines 5-9. It intentionally expects placeholder injection prevention lines 218-228. Unicode coverage is reduced to basic ASCII text despite the test label lines 230-239, so it is not strong multilingual coverage. Very large stress is bounded at about 100KB, not multi-megabyte templates.
- validation_or_tests: This file is self-validating and exits with the shared summary result lines 285-290. It validates the template loader behavior used by assigned prompt templates `block/git-push.md` and `claude/push-every-round-note.md`, plus other hook-generated prompts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections present for the 5 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`