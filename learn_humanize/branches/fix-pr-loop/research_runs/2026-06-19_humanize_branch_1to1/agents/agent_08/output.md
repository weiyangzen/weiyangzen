# agent_08 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing workflow contract for the RLCR and PR-loop state machines. It defines the two-phase RLCR algorithm and the PR bot review loop at a behavior level, including commands, options, prerequisites, and monitoring surfaces.
- algorithmic_behavior: The README describes RLCR as an iterative cycle where Claude implements, Codex reviews summaries, Claude responds to feedback, then a separate `codex review --base <branch>` review phase gates completion. The Mermaid flow at `README.md:60`-`README.md:68` shows the transition from plan to implementation to summary review, then to code review and final done state. The phase split at `README.md:70`-`README.md:72` is behavior-defining: implementation feedback loops until COMPLETE, then review phase checks quality with severity markers. The PR loop section at `README.md:159`-`README.md:167` defines a separate remote bot workflow: detect PR, fetch bot comments, fix issues, push, trigger re-review, poll every 30 seconds with a 15-minute timeout per bot, validate with local Codex, and continue until all bots approve or max iterations is reached.
- inputs_outputs_state: Inputs include a plan file for `/humanize:start-rlcr-loop` (`README.md:101`-`README.md:119`), a draft and output path for `/humanize:gen-plan` (`README.md:124`-`README.md:139`), and bot flags for `/humanize:start-pr-loop --claude|--codex` (`README.md:143`-`README.md:156`). Output/state surfaces include `.humanize/rlcr/<timestamp>/` monitoring artifacts (`README.md:84`) and `humanize monitor pr` (`README.md:174`-`README.md:177`). RLCR state transitions are plan -> implementation summaries -> Codex feedback loop -> code review -> done or back to Claude. PR-loop state transitions are PR detection -> comment fetch -> fix -> push -> trigger -> poll -> local validation -> approve or iterate.
- gates_or_invariants: Required external gate is Codex CLI (`README.md:52`-`README.md:55`, `README.md:169`-`README.md:171`). RLCR code review uses `codex review --base <branch>` and severity markers (`README.md:70`-`README.md:72`). Base branch auto-detection priority is remote default, then `main`, then `master` (`README.md:113`-`README.md:115`). PR loop requires an associated open PR and authenticated GitHub CLI (`README.md:169`-`README.md:172`). PR-loop completion requires all configured bots to approve or max iterations to be reached (`README.md:167`).
- dependencies_and_callers: The README maps directly to scripts and hooks: `/humanize:start-rlcr-loop` is implemented by the RLCR setup/stop-hook flow; `/humanize:start-pr-loop` is implemented by PR loop setup and stop hook scripts. It names the external `codex`, `gh`, and Claude Code plugin environment dependencies. It also defines user-visible command names in the commands table at `README.md:87`-`README.md:95`.
- edge_cases_or_failure_modes: Max iteration limits are explicit (`README.md:106`, `README.md:151`). PR loop can be canceled (`README.md:95`), and monitoring implies long-running asynchronous state. Base-branch auto-detection can be overridden with `--base-branch` (`README.md:113`). The PR loop has a timeout per bot (`README.md:165`) to prevent indefinite waits.
- validation_or_tests: README behavior is covered indirectly by tests for the underlying scripts: PR polling tests validate `poll-pr-reviews.sh` behavior; base branch robustness tests validate the `--base-branch` and auto-detection contract documented at `README.md:113`-`README.md:115`; template-loader tests validate prompt/template rendering used by hook messages referenced by the workflows.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-038 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop polling script. It is the data acquisition and normalization stage that turns GitHub PR issue comments, review comments, and PR reviews into a single JSON event stream for the stop hook.
- algorithmic_behavior: The script parses `<pr_number> --after <timestamp> --bots <bot1,bot2>` (`scripts/poll-pr-reviews.sh:23`-`scripts/poll-pr-reviews.sh:81`), validates required arguments and numeric PR number (`scripts/poll-pr-reviews.sh:83`-`scripts/poll-pr-reviews.sh:102`), checks `gh` and `jq` prerequisites (`scripts/poll-pr-reviews.sh:104`-`scripts/poll-pr-reviews.sh:116`), resolves the correct repository for PR operations in fork scenarios (`scripts/poll-pr-reviews.sh:122`-`scripts/poll-pr-reviews.sh:150`), maps logical bot names to GitHub authors (`scripts/poll-pr-reviews.sh:164`-`scripts/poll-pr-reviews.sh:173`), fetches three GitHub API streams with retries, normalizes them, combines them, filters by timestamp and bot author, then emits one JSON object (`scripts/poll-pr-reviews.sh:240`-`scripts/poll-pr-reviews.sh:326`).
- inputs_outputs_state: Inputs are PR number, ISO-ish `--after` timestamp, and comma-separated bot list. It also depends on the current GitHub repo context from `gh repo view`. Outputs are JSON with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:317`-`scripts/poll-pr-reviews.sh:326`). Internal transient state lives in a `mktemp -d` directory and is removed by trap (`scripts/poll-pr-reviews.sh:193`-`scripts/poll-pr-reviews.sh:199`). API failure count is tracked diagnostically but does not currently appear in output (`scripts/poll-pr-reviews.sh:204`-`scripts/poll-pr-reviews.sh:206`).
- gates_or_invariants: Required tools are `gh` and `jq`; missing either exits nonzero. PR operations must resolve to either current repo or parent repo (`scripts/poll-pr-reviews.sh:132`-`scripts/poll-pr-reviews.sh:150`). Bot names are converted to author regexes, with special handling for `codex` -> `chatgpt-codex-connector[bot]` (`scripts/poll-pr-reviews.sh:167`-`scripts/poll-pr-reviews.sh:171`). Filtering requires `created_at >= $after` and author matching the generated regex (`scripts/poll-pr-reviews.sh:299`-`scripts/poll-pr-reviews.sh:305`). PR reviews with empty body are still included using a state placeholder (`scripts/poll-pr-reviews.sh:276`-`scripts/poll-pr-reviews.sh:294`), preserving approval-only reviews as review events.
- dependencies_and_callers: Main caller is `hooks/pr-loop-stop-hook.sh`, which sets `POLL_SCRIPT="$PLUGIN_ROOT/scripts/poll-pr-reviews.sh"` and invokes it with `--after "$AFTER_TIMESTAMP"` and waiting bots (`hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:960`-`hooks/pr-loop-stop-hook.sh:965`). The caller reads `.bots_responded[]` to mark per-bot responses (`hooks/pr-loop-stop-hook.sh:967`-`hooks/pr-loop-stop-hook.sh:982`) and `.comments` to accumulate deduplicated comments (`hooks/pr-loop-stop-hook.sh:1042`-`hooks/pr-loop-stop-hook.sh:1058`). The script depends on GitHub API endpoints for issues comments, pull review comments, and pull reviews (`scripts/poll-pr-reviews.sh:241`, `scripts/poll-pr-reviews.sh:258`, `scripts/poll-pr-reviews.sh:279`).
- edge_cases_or_failure_modes: Fork PR lookup is handled by trying current repo, then parent repo (`scripts/poll-pr-reviews.sh:135`-`scripts/poll-pr-reviews.sh:143`). API fetch failures are retried three times, then converted to an empty array while returning success (`scripts/poll-pr-reviews.sh:210`-`scripts/poll-pr-reviews.sh:235`), preventing transient GitHub failures from crashing the polling loop. Non-array API responses normalize to empty arrays in each jq block (`scripts/poll-pr-reviews.sh:242`-`scripts/poll-pr-reviews.sh:255`, `scripts/poll-pr-reviews.sh:259`-`scripts/poll-pr-reviews.sh:274`, `scripts/poll-pr-reviews.sh:280`-`scripts/poll-pr-reviews.sh:294`). Empty bot names after comma trimming are not explicitly rejected; they can produce an author pattern of `[bot]`, which is a minor edge in bot input validation. Timestamp comparison is string-based in jq; this works for normalized UTC ISO timestamps but may be fragile for mixed offsets or non-normalized values.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:341`-`tests/test-pr-loop-scripts.sh:389` covers help and missing required arguments. `tests/robustness/test-pr-loop-api-robustness.sh:646`-`tests/robustness/test-pr-loop-api-robustness.sh:675` validates parseable JSON and required fields with mocked `gh`; `tests/robustness/test-pr-loop-api-robustness.sh:677`-`tests/robustness/test-pr-loop-api-robustness.sh:742` validates slow API handling; `tests/robustness/test-pr-loop-api-robustness.sh:744`-`tests/robustness/test-pr-loop-api-robustness.sh:843` requires API failures to exit zero with boolean `has_new_comments:false`. Fixture-backed tests validate approval-only review placeholders and timestamp filtering at `tests/test-pr-loop-hooks.sh:1322`-`tests/test-pr-loop-hooks.sh:1411`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-068 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template loading/rendering subsystem used by validators and stop hooks. It defines expected behavior for path resolution, missing templates, fallback rendering, single-pass substitution, literal handling of shell metacharacters, and placeholder-injection prevention.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` (`tests/test-template-loader.sh:10`-`tests/test-template-loader.sh:12`) and exercises each public loader function. It verifies `get_template_dir` maps from `hooks/lib` to `prompt-template` (`tests/test-template-loader.sh:41`-`tests/test-template-loader.sh:52`), `load_template` returns content for existing templates and empty output for missing ones (`tests/test-template-loader.sh:55`-`tests/test-template-loader.sh:78`), `render_template` performs single and multiple substitutions, handles multiline content, ignores unused variables, and leaves unreplaced placeholders intact (`tests/test-template-loader.sh:81`-`tests/test-template-loader.sh:194`). It also validates `load_and_render` integration with a real block template and `load_and_render_safe` fallback behavior (`tests/test-template-loader.sh:147`-`tests/test-template-loader.sh:222`).
- inputs_outputs_state: Inputs are template strings, template file names under `prompt-template`, and `VAR=value` substitution pairs. Outputs are rendered strings and process exit status. Test state is only counters `TESTS_PASSED` and `TESTS_FAILED` (`tests/test-template-loader.sh:20`-`tests/test-template-loader.sh:34`), with final exit zero only when no failures occur (`tests/test-template-loader.sh:641`-`tests/test-template-loader.sh:659`). The production implementation uses environment variables prefixed with `TMPL_VAR_` and a single awk pass (`hooks/lib/template-loader.sh:62`-`hooks/lib/template-loader.sh:129`).
- gates_or_invariants: Core invariant is single-pass substitution: injected values containing `{{OTHER_VAR}}` must not be rescanned or expanded. This is tested directly for later and earlier variables (`tests/test-template-loader.sh:499`-`tests/test-template-loader.sh:523`) and in a realistic `REVIEW_CONTENT` prompt-corruption scenario (`tests/test-template-loader.sh:525`-`tests/test-template-loader.sh:543`). Literal rendering is required for shell metacharacters including ampersands, backslashes, dollar signs, backticks, pipes, semicolons, globs, parentheses, redirection, quotes, hashes, tildes, JSON, regex-like strings, and multiline content (`tests/test-template-loader.sh:246`-`tests/test-template-loader.sh:487`). `validate_template_dir` requires the template root plus `block`, `codex`, and `claude` subdirectories as implemented in `hooks/lib/template-loader.sh:208`-`hooks/lib/template-loader.sh:222`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`. The loader is used widely by hook scripts; examples include `hooks/lib/loop-common.sh` for blocked messages, including summary bash write blocks (`hooks/lib/loop-common.sh:481`-`hooks/lib/loop-common.sh:490`), and PR/RLCR stop hooks for rendering dynamic prompts. The tests reference real templates such as `block/git-push.md` and `block/wrong-round-number.md` (`tests/test-template-loader.sh:59`, `tests/test-template-loader.sh:151`-`tests/test-template-loader.sh:156`).
- edge_cases_or_failure_modes: Missing templates must not hard-fail `load_template`; they produce empty content with warning behavior in implementation (`hooks/lib/template-loader.sh:36`-`hooks/lib/template-loader.sh:48`). Missing variables remain as placeholders (`tests/test-template-loader.sh:181`-`tests/test-template-loader.sh:194`). Empty variables substitute to empty strings (`tests/test-template-loader.sh:555`-`tests/test-template-loader.sh:565`). Variable names with underscore prefix and numbers are accepted (`tests/test-template-loader.sh:617`-`tests/test-template-loader.sh:639`). Non-ASCII content in template and values is explicitly covered (`tests/test-template-loader.sh:579`-`tests/test-template-loader.sh:615`). A notable implementation boundary is that `template-loader.sh` documentation says variable names are uppercase letters, numbers, and underscores (`hooks/lib/template-loader.sh:7`-`hooks/lib/template-loader.sh:13`), while the awk implementation actually looks up whatever text appears between braces; stricter invalid-name behavior is handled by separate robustness tests, not this file.
- validation_or_tests: This file is itself the validation surface. It has 41 named tests and exits nonzero on any failure (`tests/test-template-loader.sh:641`-`tests/test-template-loader.sh:659`). It is also listed in the aggregate runner (`tests/run-all-tests.sh:34` from search results), so it participates in broader test execution.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-098 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template for a write-validation gate. It tells the agent that Bash-based modification of summary files is blocked and routes the agent to the proper Write/Edit tool path.
- algorithmic_behavior: The template has one dynamic placeholder, `{{CORRECT_PATH}}`, rendered into the instruction “Use the Write or Edit tool instead” (`prompt-template/block/summary-bash-write.md:1`-`prompt-template/block/summary-bash-write.md:8`). It is not executable logic itself, but it defines the user-visible block reason emitted when the Bash validator detects an attempted summary-file write via shell commands.
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by `summary_bash_blocked_message "$correct_summary_path"` in `hooks/lib/loop-common.sh:481`-`hooks/lib/loop-common.sh:490`. Output is the rendered block message. There is no persistent state in the template; state transition effect is external: validator returns a blocked decision and keeps the loop from accepting a summary mutation through Bash.
- gates_or_invariants: Invariant is that summary files must not be modified with shell commands because Bash commands bypass validation hooks (`prompt-template/block/summary-bash-write.md:3`-`prompt-template/block/summary-bash-write.md:8`). The path shown to the agent must be the validated correct summary path. The rendering must preserve literal command names and substitute only `CORRECT_PATH`, relying on the single-pass loader behavior described in `hooks/lib/template-loader.sh:50`-`hooks/lib/template-loader.sh:57`.
- dependencies_and_callers: Called through `summary_bash_blocked_message()` in `hooks/lib/loop-common.sh:481`-`hooks/lib/loop-common.sh:490`. The function uses `load_and_render_safe`, so if this template is missing or empty, a fallback still emits a similar block message (`hooks/lib/loop-common.sh:485`-`hooks/lib/loop-common.sh:489`). Template reference tests include `block/summary-bash-write.md` as a common template and check reference consistency in the template system (`tests/test-template-references.sh:152`-`tests/test-template-references.sh:162` from search results).
- edge_cases_or_failure_modes: If `CORRECT_PATH` contains characters meaningful to shell or template syntax, safe rendering should keep it literal because the loader uses single-pass awk substitution. If the template is missing, the caller fallback loses the explanatory sentence about bypassing validation hooks but still blocks and provides the correct path. If validators accidentally allow Bash writes to summaries, this template is never reached; its correctness depends on upstream command classification in loop validation.
- validation_or_tests: Covered indirectly by template-loader tests for safe rendering and fallback behavior, especially `load_and_render_safe` (`tests/test-template-loader.sh:197`-`tests/test-template-loader.sh:222`) and shell metacharacter literal rendering (`tests/test-template-loader.sh:246`-`tests/test-template-loader.sh:487`). Template reference checks explicitly include this file as a common template in `tests/test-template-references.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-128 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR base branch auto-detection. It models the decision order used by `scripts/setup-rlcr-loop.sh` so code review uses a suitable local base ref for `codex review`.
- algorithmic_behavior: The test defines a local `detect_base_branch()` helper that mirrors the production priority: remote default from `git remote show origin`, then local `main`, then local `master`, otherwise failure (`tests/robustness/test-base-branch-detection.sh:31`-`tests/robustness/test-base-branch-detection.sh:64`). It sets up isolated git repositories under a test directory, commits minimal content, then validates fallback behavior when origin is absent and when specific local branches exist (`tests/robustness/test-base-branch-detection.sh:70`-`tests/robustness/test-base-branch-detection.sh:149`). It also creates a bare remote and local repo to simulate origin-based detection (`tests/robustness/test-base-branch-detection.sh:151`-`tests/robustness/test-base-branch-detection.sh:191`).
- inputs_outputs_state: Inputs are temporary git repositories with controlled branch and remote state, plus a timeout argument passed through `run_with_timeout` from `scripts/portable-timeout.sh` (`tests/robustness/test-base-branch-detection.sh:16`-`tests/robustness/test-base-branch-detection.sh:20`). Output is pass/fail test reporting through `test-helpers.sh`, with final summary at `tests/robustness/test-base-branch-detection.sh:193`-`tests/robustness/test-base-branch-detection.sh:198`. Internal state includes created repos `REPO1` through `REPO4` and a bare remote.
- gates_or_invariants: Detection priority is remote default, local `main`, local `master`, then error. The helper explicitly guards the `remote show origin | grep | sed` pipeline with `|| true` to prevent `set -euo pipefail` from terminating the script when origin is missing (`tests/robustness/test-base-branch-detection.sh:41`-`tests/robustness/test-base-branch-detection.sh:45`). Production logic also requires remote default to exist as a local branch before selecting it (`scripts/setup-rlcr-loop.sh:613`-`scripts/setup-rlcr-loop.sh:623`), falls back to local `main`/`master` (`scripts/setup-rlcr-loop.sh:624`-`scripts/setup-rlcr-loop.sh:630`), and emits actionable errors if no local base exists (`scripts/setup-rlcr-loop.sh:632`-`scripts/setup-rlcr-loop.sh:642`). Production additionally validates YAML-unsafe base branch characters and captures the base commit SHA (`scripts/setup-rlcr-loop.sh:645`-`scripts/setup-rlcr-loop.sh:661`).
- dependencies_and_callers: The test targets `scripts/setup-rlcr-loop.sh` base branch behavior, with supporting helpers from `tests/test-helpers.sh` and `scripts/portable-timeout.sh`. The user-facing option is documented in `README.md:113`-`README.md:115`. Production setup uses this branch to seed loop state and later drive Codex code review against a stable base commit.
- edge_cases_or_failure_modes: Missing `origin` must gracefully fall back to local branches (`tests/robustness/test-base-branch-detection.sh:70`-`tests/robustness/test-base-branch-detection.sh:122`). A repo with neither `main` nor `master` must fail (`tests/robustness/test-base-branch-detection.sh:128`-`tests/robustness/test-base-branch-detection.sh:149`). Remote default can be unavailable or `(unknown)`, in which case fallback applies (`tests/robustness/test-base-branch-detection.sh:43`-`tests/robustness/test-base-branch-detection.sh:48`). The test’s helper is a simplified mirror: it does not cover user-specified `--base-branch`, remote-only branch diagnostics, YAML safety validation, or base commit capture, all of which exist in production `scripts/setup-rlcr-loop.sh:594`-`scripts/setup-rlcr-loop.sh:661`. The comment at `tests/robustness/test-base-branch-detection.sh:35` references old production line numbers and should be treated as descriptive, not exact.
- validation_or_tests: This file is the validation surface for the main fallback cases. It exercises no-origin main, no-origin master, no qualifying branch failure, and origin simulation (`tests/robustness/test-base-branch-detection.sh:70`-`tests/robustness/test-base-branch-detection.sh:191`). It should be complemented by direct tests for explicit `--base-branch`, remote-only branch error text, YAML-unsafe branch rejection, and base commit capture if those production paths are considered core.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5, each present once as an Item Evidence heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`