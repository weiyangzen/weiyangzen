# agent_24 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude plugin hook registry for the RLCR and PR-loop state machine. It binds Claude hook events to the repository’s validator and stop-hook executables, making this file the dispatch table that turns user prompts, tool calls, and session stops into loop gate checks.
- algorithmic_behavior: The registry wires `UserPromptSubmit` to `hooks/loop-plan-file-validator.sh`, so loop setup prompts are validated before execution (`hooks/hooks.json:4-13`). It wires `PreToolUse` to tool-specific validators for `Write`, `Edit`, `Read`, and `Bash` (`hooks/hooks.json:14-51`). It wires `Stop` to two long-running stop hooks, `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh`, each with `timeout: 7200`, allowing RLCR and PR-loop stop processing to run through review, polling, and transition logic (`hooks/hooks.json:52-67`).
- inputs_outputs_state: Inputs are Claude hook events plus hook payloads on stdin to each referenced script. Outputs are the hook scripts’ JSON decisions or shell exit behavior, not direct output from `hooks.json`. State transitions are delegated: plan-file validation gates loop entry; pre-tool validators gate filesystem and command access while the loop is active; stop hooks advance loop rounds, review phases, PR polling, completion, or blocking states.
- gates_or_invariants: Tool validators are scoped by matcher, so each Claude tool gets the intended guard (`Write` to write validator, `Edit` to edit validator, `Read` to read validator, `Bash` to bash validator). The stop hook includes both RLCR and PR-loop processors, so a Claude stop event is a shared transition point for both modes. The registry assumes `${CLAUDE_PLUGIN_ROOT}` is set correctly; otherwise all command paths fail at runtime.
- dependencies_and_callers: Depends on executable scripts under `hooks/`: `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, `loop-bash-validator.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh` (`hooks/hooks.json:9`, `:20`, `:29`, `:38`, `:47`, `:57`, `:62`). The referenced scripts exist and are executable in this export. Cross-reference tests cover these hook scripts in `tests/test-template-references.sh:57-63`, plan-file tests in `tests/test-plan-file-hooks.sh`, PR-loop tests in `tests/test-pr-loop-hooks.sh`, and robustness tests in `tests/robustness/test-hook-system-robustness.sh:3-15`.
- edge_cases_or_failure_modes: Missing or unset `CLAUDE_PLUGIN_ROOT` breaks all hook command resolution. A missing executable means Claude can no longer enforce the relevant transition gate. The two stop hooks share the same `Stop` event, so each must internally no-op or block safely when its loop state is absent. Long stop-hook work is allowed up to 7200 seconds; shorter external hook managers could still interrupt these transitions.
- validation_or_tests: `tests/robustness/test-hook-system-robustness.sh` focuses on JSON parsing, injection prevention, state parsing, and concurrent access for the hook family (`tests/robustness/test-hook-system-robustness.sh:11-15`). `tests/test-template-references.sh` scans critical hook scripts for template references and safe fallback usage (`tests/test-template-references.sh:52-69`, `:169-184`). Numerous PR/RLCR tests invoke the referenced validators and stop hooks directly, providing executable coverage for the dispatch targets.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/gen-plan` command’s structural and IO validation behavior. It is not the PR-loop stop algorithm itself, but it validates the plan-generation command that can produce structured implementation plans consumed by later loop workflows.
- algorithmic_behavior: The script initializes project paths for `commands`, `agents`, and plugin metadata (`tests/test-gen-plan.sh:11-15`), tracks pass/fail counters (`:22-40`), then runs positive and negative checks. Positive tests require `commands/gen-plan.md` to exist with description, allowed tools, and argument hint (`:54-104`), require `agents/draft-relevance-checker.md` to exist with name, model, and tools (`:107-157`), and require version consistency across plugin JSON and README (`:160-180`). Negative tests create temporary malformed fixtures to ensure helper validators reject invalid names, missing frontmatter, malformed YAML, and invalid model aliases (`:191-399`). It then checks content policy for no emoji/CJK in the command and agent files (`:403-423`) and exercises `scripts/validate-gen-plan-io.sh` exit-code behavior (`:425-541`).
- inputs_outputs_state: Inputs are repository files plus temporary fixtures created under `mktemp -d` (`tests/test-gen-plan.sh:191-193`, `:435-436`). Outputs are colored PASS/FAIL lines and process exit `0` only when `TESTS_FAILED` is zero (`:547-562`). The test mutates only temporary fixture directories during normal execution, with cleanup registered through `trap`.
- gates_or_invariants: Command and agent names must match `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:195-199`). Command frontmatter must start with `---` and include `description:` (`:201-206`). Agent frontmatter must include `name:` and `description:` (`:208-214`). Model names must be exact short aliases `opus`, `sonnet`, `haiku`, or full IDs with accepted prefixes (`:350-358`), explicitly rejecting partial alias prefixes like `opus-v2`, `haiku123`, and `sonnet-fast` (`:372-389`). `validate-gen-plan-io.sh` must return stable exit codes: invalid args `6`, missing input `1`, empty input `2`, missing output directory `3`, output conflict `4`, valid paths `0` (`:438-539`).
- dependencies_and_callers: Depends on `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. The command under test declares `validate-gen-plan-io.sh`, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` as allowed tools (`commands/gen-plan.md:1-12`). Its workflow gates IO validation, relevance checking, draft analysis, user issue resolution, plan generation, and write/complete phases (`commands/gen-plan.md:19-27`). The agent under test is a lenient relevance classifier using `haiku` and `Read, Glob, Grep` (`agents/draft-relevance-checker.md:1-6`, `:27-36`).
- edge_cases_or_failure_modes: The YAML syntax checker is intentionally simple and line-oriented, so it can validate this repository’s frontmatter shape but is not a full YAML parser (`tests/test-gen-plan.sh:304-319`). The test has two `trap` registrations, and the later one for script tests replaces the earlier fixture cleanup trap in plain bash execution (`:193`, `:436`), leaving a possible temp cleanup gap. Version extraction uses regexes tailored to current file formats (`:168-177`). `grep -P` may be unavailable on some platforms, but the script suppresses stderr and treats no match as pass for content checks (`:409-421`).
- validation_or_tests: This file is itself a validation harness. It also validates the concrete IO validator implementation, whose documented exit codes are in `scripts/validate-gen-plan-io.sh:4-12`; the script parses `--input` and `--output`, rejects missing values and unknown options (`scripts/validate-gen-plan-io.sh:29-67`), checks input existence/non-emptiness and output target safety (`:79-124`), then copies `prompt-template/plan/gen-plan-template.md` and appends the draft on success (`:133-160`).
- skip_candidate: `yes: It is core to gen-plan command validation and planning workflow quality, but only indirectly related to the RLCR/PR-loop state-machine algorithm named by this branch subset.`

### FIX_PR_LOOP-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Fail-closed prompt block for the RLCR stop hook when `git status --porcelain` fails or times out. It defines the human-facing block reason used when repository cleanliness cannot be verified.
- algorithmic_behavior: The template renders a short blocked-state explanation with one variable, `{{GIT_STATUS_EXIT}}`, reporting the failed git status exit code (`prompt-template/block/git-status-failed.md:1-10`). In `hooks/loop-codex-stop-hook.sh`, git status is cached once for later checks; if the command exits nonzero, the hook renders this template and returns a JSON decision of `"block"` with system message `"Loop: Blocked - git status failed..."` (`hooks/loop-codex-stop-hook.sh:396-423`).
- inputs_outputs_state: Input is `GIT_STATUS_EXIT`, supplied by the stop hook after `run_with_timeout "$GIT_TIMEOUT" git status --porcelain` fails (`hooks/loop-codex-stop-hook.sh:409-420`). Output is rendered markdown embedded into the hook’s JSON `reason` field (`hooks/loop-codex-stop-hook.sh:421-422`). State transition is fail-closed: the loop does not proceed to large-file checks, clean-worktree checks, Codex execution, review, or finalize when git status cannot be trusted.
- gates_or_invariants: The invariant is that repository state must be verifiable before the stop hook can continue. The hook explicitly avoids a fail-open fallback, noting that `|| echo ""` would bypass checks (`hooks/loop-codex-stop-hook.sh:401-410`). The template’s guidance instructs manual `git status` inspection rather than continuing automatically (`prompt-template/block/git-status-failed.md:5-10`).
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` in `hooks/loop-codex-stop-hook.sh:419-420`. Rendering uses `hooks/lib/template-loader.sh`, whose placeholder syntax is `{{VARIABLE_NAME}}`, single-pass substitution, and preservation of missing placeholders (`hooks/lib/template-loader.sh:7-14`, `:50-58`). `load_and_render_safe` uses fallback text if the template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If the template is missing, the hook has an inline fallback with the same core message (`hooks/loop-codex-stop-hook.sh:414-418`). If `GIT_STATUS_EXIT` is unavailable or malformed, the rendered message would contain inaccurate status detail, but the hook still blocks. If git itself is absent or the directory is not a git repository, this specific block is bypassed because the enclosing condition requires both `command -v git` and `git rev-parse --git-dir` to pass (`hooks/loop-codex-stop-hook.sh:406`).
- validation_or_tests: Template reference coverage scans hook scripts for template loads in `tests/test-template-references.sh:52-69`. The broader hook robustness suite exercises stop-hook state parsing and hook failure safety (`tests/robustness/test-hook-system-robustness.sh:3-15`). Timeout behavior underlying this gate is validated separately by `tests/robustness/test-timeout-robustness.sh`.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-114 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Review-phase repair prompt for the RLCR state machine. It defines what Claude must do after Codex review finds priority findings, keeping the loop in review-fix mode until those findings clear.
- algorithmic_behavior: The template injects `{{REVIEW_CONTENT}}` into a review-results section and `{{SUMMARY_FILE}}` into the instruction to write a round summary (`prompt-template/claude/review-phase-prompt.md:5-15`). It orders Claude to address all `[P0-9]` findings, limit changes to fixes, commit changes, and write a summary (`:9-15`). It states that the COMPLETE signal has no effect during review phase and that Codex will review again until no priority findings remain (`:23-28`).
- inputs_outputs_state: Inputs are `review_content` from `detect_review_issues` and `next_summary_file` from the stop hook (`hooks/loop-codex-stop-hook.sh:1129-1150`). Output is the next round prompt file at `$LOOP_DIR/round-${round}-prompt.md`; the hook then returns a JSON block decision whose reason is that prompt (`hooks/loop-codex-stop-hook.sh:1130-1159`). State transition is from a failed Codex review into a blocked review-fix round rather than finalization.
- gates_or_invariants: Review phase can only complete when Codex review has no priority findings. The stop hook runs `codex review --base`, captures output, and calls `detect_review_issues`; issues cause `continue_review_loop_with_issues`, no issues proceed to finalize, and missing/empty review output blocks as a hard error (`hooks/loop-codex-stop-hook.sh:999-1027`). The hook ignores COMPLETE while already in review phase, so users cannot bypass review by writing the completion marker (`hooks/loop-codex-stop-hook.sh:1394-1399`). A marker file `.review-phase-started` is required when `review_started=true`, preventing manual state toggles (`hooks/loop-codex-stop-hook.sh:1446-1462`).
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "claude/review-phase-prompt.md"` in `hooks/loop-codex-stop-hook.sh:1148-1150`. The review issue detector lives in `hooks/lib/loop-common.sh:349-392`; it scans the full Codex review log for `[P0-9]` markers within the first 10 characters of a line, extracts from the first matching line to the end, writes `round-N-review-result.md`, and returns issue/no-issue/error codes. The review command audit and execution path are in `hooks/loop-codex-stop-hook.sh:916-984`.
- edge_cases_or_failure_modes: If the template is absent, the stop hook uses an inline fallback prompt (`hooks/loop-codex-stop-hook.sh:1133-1146`). If review output contains priority markers only later than the first 10 characters, `detect_review_issues` intentionally ignores them (`hooks/lib/loop-common.sh:341-345`, `:364-372`), so prompt generation may not trigger. Empty or missing review logs return detector code `2` and block rather than finalizing (`hooks/lib/loop-common.sh:354-358`; `hooks/loop-codex-stop-hook.sh:1017-1019`). Template rendering is single-pass, so review content containing template-like braces is not recursively substituted (`hooks/lib/template-loader.sh:50-58`).
- validation_or_tests: `tests/test-codex-review-merge.sh` is the executable spec for the priority-marker detector; it verifies full-file scanning, first-10-character detection, extraction from first finding to end, result-file creation, and no-issue/error returns (`tests/test-codex-review-merge.sh:3-15`, `:57-107`, `:232-330`). `tests/test-finalize-phase.sh` includes review-phase blocking scenarios, and template-loader tests cover safe placeholder rendering.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-144 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout abstraction used by setup scripts, validators, and stop hooks to keep git, gh, Codex, and hook subprocesses bounded.
- algorithmic_behavior: The script sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-17`), then verifies timeout implementation detection and command execution behavior. It accepts `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` as valid implementations (`:31-49`). It checks quick command success, timeout exit code `124`, argument preservation, command exit-code preservation, and pipeline execution through `sh -c` (`:51-110`). Negative/edge tests cover short timeouts, zero timeout behavior, large output, rapid cycles, nonexistent commands, empty command handling, special characters, signal-trapping commands, fallback detection, subshells, and exported `TIMEOUT_IMPL` (`:120-272`).
- inputs_outputs_state: Inputs are the host environment’s available timeout tools and shell commands under test. Outputs are PASS/FAIL counters via `tests/test-helpers.sh` and final exit status from `print_test_summary` (`tests/robustness/test-timeout-robustness.sh:274-279`; `tests/test-helpers.sh:58-78`). It does not mutate repository state.
- gates_or_invariants: `detect_timeout_impl` must choose implementations in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:6-27`). `run_with_timeout` must return `124` on timeout for real timeout implementations (`tests/robustness/test-timeout-robustness.sh:62-77`, `:120-137`) and preserve ordinary command exit codes (`:89-100`). The abstraction must pass arguments as an array, preserving spaces and special characters (`scripts/portable-timeout.sh:33-46`; `tests/robustness/test-timeout-robustness.sh:79-87`, `:211-219`).
- dependencies_and_callers: Directly depends on `scripts/portable-timeout.sh`, whose `run_with_timeout` dispatches to `gtimeout`, `timeout`, Python `subprocess.run(..., timeout=seconds)`, or no-timeout fallback (`scripts/portable-timeout.sh:33-71`), and exports `TIMEOUT_IMPL` (`:73-76`). Many core scripts source this abstraction: `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-plan-file-validator.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, and PR polling/check scripts, as shown by repository cross-references to `run_with_timeout`.
- edge_cases_or_failure_modes: When `TIMEOUT_IMPL=none`, timeout tests skip timeout-specific assertions and the wrapper runs commands unbounded with a warning (`scripts/portable-timeout.sh:64-69`; `tests/robustness/test-timeout-robustness.sh:75-77`, `:135-137`). Python fallback does not capture child output itself, relying on inherited stdout/stderr, and exits `124` on `subprocess.TimeoutExpired` (`scripts/portable-timeout.sh:47-61`). Zero timeout is explicitly allowed to either succeed or timeout because platform behavior varies (`tests/robustness/test-timeout-robustness.sh:139-155`). Empty command handling is accepted without strict assertion, only requiring the wrapper not to crash the test harness (`:201-209`).
- validation_or_tests: This assigned file is the validation harness. Related robustness coverage also uses `run_with_timeout` for large hook JSON payload handling in `tests/robustness/test-hook-system-robustness.sh:623-635` and for base-branch/git-operation robustness in other `tests/robustness` scripts. The timeout abstraction is central to fail-closed git status behavior in `hooks/loop-codex-stop-hook.sh:406-423`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: FIX_PR_LOOP-HZ-024, FIX_PR_LOOP-HZ-054, FIX_PR_LOOP-HZ-084, FIX_PR_LOOP-HZ-114, FIX_PR_LOOP-HZ-144
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`