# agent_15 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`
- notes: Repository Git metadata was not usable in this read-only export, so the scheduler-provided source commit is treated as authoritative. No files were modified.

## Item Evidence

### FIX_PR_LOOP-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitoring support library for RLCR and PR-loop user-facing monitor commands. The directory currently contains one recursive child file, `scripts/lib/monitor-common.sh`, which is sourced by `scripts/humanize.sh` when present (`scripts/humanize.sh:6-10`). It centralizes cross-platform file sizing, session discovery, terminal handling, state-file detection, PR-loop phase detection, and goal-tracker parsing.
- algorithmic_behavior: `monitor_get_file_size` abstracts Linux/macOS `stat` variants and returns `0` if both fail (`scripts/lib/monitor-common.sh:32-35`). `monitor_find_latest_session` scans immediate timestamp-named session directories matching `YYYY-MM-DD_HH-MM-SS`, compares basenames lexicographically, and returns the latest path or empty string (`scripts/lib/monitor-common.sh:40-63`). `monitor_find_state_file` prefers active `state.md`, otherwise returns the first `*-state.md` discovered with stop reason extracted from the filename, else `|unknown` (`scripts/lib/monitor-common.sh:154-188`). `get_pr_loop_phase` first checks terminal files `approve-state.md`, `cancel-state.md`, and `maxiter-state.md`; then detects active Codex analysis by highest-numbered `round-*-pr-check.md` growth or recent mtime; then falls back to `state.md` `current_round` and `startup_case` to classify waiting state (`scripts/lib/monitor-common.sh:267-342`). `parse_goal_tracker` and `humanize_parse_pr_goal_tracker` transform markdown trackers into pipe-delimited counters for monitor display (`scripts/lib/monitor-common.sh:390-463`, `scripts/lib/monitor-common.sh:468-498`).
- inputs_outputs_state: Inputs are filesystem paths to loop directories, session directories, state files, PR check files, and goal trackers. Outputs are simple strings intended for shell parsing: paths, `path|status`, phase names, display text, or pipe-delimited statistics. `get_pr_loop_phase` writes a cache file under `/tmp/humanize-phase-${session_name}-${highest_round}.size` to compare PR check growth across calls (`scripts/lib/monitor-common.sh:297-307`). Terminal helpers mutate the active terminal scroll region with ANSI/tput commands (`scripts/lib/monitor-common.sh:71-92`).
- gates_or_invariants: Session selection accepts only timestamp basename format and ignores non-directories or unrelated names (`scripts/lib/monitor-common.sh:54-60`). State-file resolution treats `state.md` as active with priority over stopped state files (`scripts/lib/monitor-common.sh:162-166`). PR-loop phase classification prioritizes final states before transient Codex activity (`scripts/lib/monitor-common.sh:272-276`). Round file parsing only accepts numeric round suffixes when locating latest PR check (`scripts/lib/monitor-common.sh:281-290`). The “Codex analyzing” gate is true if file size grew since cache or mtime age is under 10 seconds (`scripts/lib/monitor-common.sh:310-318`).
- dependencies_and_callers: `scripts/humanize.sh` sources this library and wraps `monitor_find_latest_session` for RLCR monitor session selection (`scripts/humanize.sh:237-240`) and PR-loop monitor session selection (`scripts/humanize.sh:962-965`). It uses `monitor_find_state_file` for RLCR state display (`scripts/humanize.sh:344-347`) and PR-loop status mapping (`scripts/humanize.sh:1011-1028`). PR monitor display uses `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` (`scripts/humanize.sh:1097-1148`, `scripts/humanize.sh:1235-1241`). System tests source it directly for phase detection, latest-session detection, state-file detection, and file-size checks (`tests/test-pr-loop-system.sh:256-309`, `tests/test-pr-loop-system.sh:770-843`, `tests/test-pr-loop-system.sh:849-895`).
- edge_cases_or_failure_modes: Missing loop/session directories return empty or `unknown` instead of hard failure (`scripts/lib/monitor-common.sh:44-47`, `scripts/lib/monitor-common.sh:157-160`, `scripts/lib/monitor-common.sh:270`). The `*-state.md` scan stops at the first `find` result, so multiple stopped state files have nondeterministic winner unless filesystem ordering is stable (`scripts/lib/monitor-common.sh:172-181`). YAML parsing uses simple `grep`/`sed` frontmatter extraction and does not parse nested or quoted YAML semantics beyond stripping quotes (`scripts/lib/monitor-common.sh:197-209`, `scripts/lib/monitor-common.sh:321-330`). PR phase cache writes to `/tmp`; if the write fails, the function continues, but growth detection may degrade to mtime-based behavior (`scripts/lib/monitor-common.sh:305-318`). Old files with unchanged size fall back to waiting state.
- validation_or_tests: `tests/test-pr-loop-system.sh` covers approved, initial waiting, reviewer waiting, and Codex-analyzing phase detection (`tests/test-pr-loop-system.sh:256-309`, `tests/test-pr-loop-system.sh:849-895`). It also verifies timestamp session selection, state-file priority/status extraction, and cross-platform file-size behavior (`tests/test-pr-loop-system.sh:770-843`). `tests/robustness/test-goal-tracker-robustness.sh` and `tests/robustness/test-hook-input-robustness.sh` reference goal tracker parsing behavior, while `scripts/humanize.sh` retains a separate `humanize_parse_goal_tracker` implementation for backward-compatible RLCR monitor parsing (`scripts/humanize.sh:30-112`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Fixture-backed mock of the GitHub CLI used by PR-loop integration tests to specify GitHub API behavior deterministically. It is an executable test support script for comment fetching, polling, and review-status behavior, especially approval-only reviews.
- algorithmic_behavior: The script requires two inputs, `<mock_bin_dir>` and `<fixtures_dir>`, creates the mock bin directory, writes an executable `gh` shim, and prints the mock bin path (`tests/setup-fixture-mock-gh.sh:16-24`, `tests/setup-fixture-mock-gh.sh:27-101`). The generated mock dispatches on the first `gh` subcommand: `auth status` succeeds, `repo view` emits JSON for owner/name/parent combinations, `pr view` emits JSON for number or state, and `api` maps endpoint substrings to fixture files (`tests/setup-fixture-mock-gh.sh:33-92`). Unhandled commands fail with stderr and exit `1` (`tests/setup-fixture-mock-gh.sh:95-96`).
- inputs_outputs_state: Inputs are the output directory where `gh` should be generated and the directory containing fixtures. The generated executable embeds `FIXTURES_DIR` as a literal variable (`tests/setup-fixture-mock-gh.sh:31`). Outputs are a created executable at `$MOCK_BIN_DIR/gh`, fixture JSON on stdout for matching endpoints, and `$MOCK_BIN_DIR` from the setup script. It reads `issue_comments.json`, `review_comments.json`, and `pr_reviews.json` (`tests/setup-fixture-mock-gh.sh:71-87`).
- gates_or_invariants: Missing setup arguments are rejected with usage and exit `1` (`tests/setup-fixture-mock-gh.sh:19-22`). Endpoint matching is order-sensitive: issue comments are matched before PR review comments, and PR review comments before PR reviews (`tests/setup-fixture-mock-gh.sh:71-87`). Unknown `gh api` endpoints intentionally return `[]` and success to allow tests to focus on selected API surfaces (`tests/setup-fixture-mock-gh.sh:89-91`). Unknown non-API commands fail closed.
- dependencies_and_callers: `tests/test-pr-loop-hooks.sh` wraps this setup in `setup_fixture_mock_gh`, prepends the generated mock directory to `PATH`, and invokes `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` (`tests/test-pr-loop-hooks.sh:1231-1252`, `tests/test-pr-loop-hooks.sh:1322-1334`). The target scripts query `gh repo view`, `gh pr view`, and the three comment/review API endpoints the mock implements (`scripts/fetch-pr-comments.sh:130-153`, `scripts/fetch-pr-comments.sh:210-220`, `scripts/poll-pr-reviews.sh:126-150`, `scripts/poll-pr-reviews.sh:240-294`).
- edge_cases_or_failure_modes: The mock only implements JSON shapes and command flags needed by existing tests; unsupported `gh repo view` field combinations can emit nothing but still exit success if they enter the `repo view` branch. The fixture filenames use underscore names; dash-named fixture files also exist but are not read by this mock. API pagination flags are ignored because matching is based on endpoint string in `$2`, which remains valid for the current target scripts. The mock does not validate PR numbers, repository names, auth context, or `--repo` values.
- validation_or_tests: Fixture tests assert that `fetch-pr-comments.sh` includes human issue comments, inline review comments, and approval-only PR reviews represented by `[Review state: APPROVED]` (`tests/test-pr-loop-hooks.sh:1242-1283`). They assert `--after` filtering excludes older comments while preserving late approvals (`tests/test-pr-loop-hooks.sh:1287-1318`). Polling tests validate JSON output, placeholder inclusion for empty approval bodies, `bots_responded`, and timestamp filtering (`tests/test-pr-loop-hooks.sh:1322-1419`). The target scripts explicitly include all PR reviews even with empty bodies and synthesize state placeholders (`scripts/fetch-pr-comments.sh:300-320`, `scripts/poll-pr-reviews.sh:276-294`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-075 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking prompt/template used when a PR-loop Claude trigger comment does not receive the required `eyes` reaction after retries. It defines the human-readable reason returned by the stop hook when Claude bot responsiveness/configuration cannot be confirmed.
- algorithmic_behavior: The template renders a “Claude Bot Not Responding” block with two variables: `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}` (`prompt-template/block/claude-eyes-timeout.md:1-3`). It then lists likely causes, required operator actions, and a restart instruction (`prompt-template/block/claude-eyes-timeout.md:5-18`). Rendering uses the shared template loader’s single-pass `{{VAR}}` substitution, preserving missing placeholders if not supplied (`hooks/lib/template-loader.sh:50-131`).
- inputs_outputs_state: Inputs are retry count and total wait seconds passed by `hooks/pr-loop-stop-hook.sh` via `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:778-785`). Output is a markdown reason string assigned to `REASON`, then embedded in a JSON object with decision `block` and a system message (`hooks/pr-loop-stop-hook.sh:787-789`). The template itself has no state; the state transition is in the stop hook, which blocks loop exit rather than advancing the PR loop.
- gates_or_invariants: The template is used only if Claude is configured and trigger verification is required, there is a confirmed trigger comment ID, and `check-bot-reactions.sh claude-eyes` returns empty or `null` after retries (`hooks/pr-loop-stop-hook.sh:767-779`). The reaction checker sleeps between attempts, fetches comment reactions from the PR base repo, and looks specifically for `claude[bot]` with content `eyes` (`scripts/check-bot-reactions.sh:264-300`). `load_and_render_safe` provides a fallback message if the template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh` in the Claude trigger verification gate (`hooks/pr-loop-stop-hook.sh:771-789`). That caller depends on `scripts/check-bot-reactions.sh`, `jq`, template loading, and PR-loop state variables such as configured bots, startup case, trigger requirement, trigger comment ID, and PR number. The checker supports fork PRs by resolving the base repository before fetching reactions (`scripts/check-bot-reactions.sh:232-263`, `scripts/check-bot-reactions.sh:269-275`).
- edge_cases_or_failure_modes: If the trigger exists but no comment ID is available, the hook only logs a warning and does not render this block (`hooks/pr-loop-stop-hook.sh:793-796`). If the template is missing, the fallback still blocks with the same decision shape, but less detailed instructions (`hooks/pr-loop-stop-hook.sh:780-789`). The reaction checker waits before each attempt, so configured `3 x 5s` implies three sleeps and three checks (`scripts/check-bot-reactions.sh:264-267`). API errors during reaction fetch cause retry continuation and eventual not-found exit rather than immediate hard error (`scripts/check-bot-reactions.sh:273-278`).
- validation_or_tests: Template loader behavior is covered by tests for safe fallback and variable substitution (`tests/test-template-loader.sh:197-221`, `tests/test-templates-comprehensive.sh:446-458`). PR-loop API robustness tests exercise stop-hook scenarios around bot API behavior, while `check-bot-reactions.sh` documents exit code `1` for timeout/not found and `2` for API/argument errors (`scripts/check-bot-reactions.sh:13-16`). No direct isolated test for this exact template was observed, but it is part of the referenced template set discovered by template reference tests (`tests/test-template-references.sh:69-94`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-105 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Blocking template for RLCR loop validators when an agent tries to edit or write a round summary file whose round number does not match the active loop round. It enforces the loop invariant that Claude must write only the current round’s summary path and must not self-increment round numbers.
- algorithmic_behavior: The template renders action, attempted round, file type, current round, and corrected path: `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-round-number.md:1-7`). The final instruction explicitly tells the agent not to increment the round number itself (`prompt-template/block/wrong-round-number.md:7`). Rendering is single-pass through `load_and_render_safe`, so variables inside values are not recursively expanded (`hooks/lib/template-loader.sh:50-131`, `hooks/lib/template-loader.sh:167-203`).
- inputs_outputs_state: Inputs come from validators after extracting the filename and round number from `.humanize/rlcr/.../round-N-summary.md`. `hooks/loop-edit-validator.sh` passes `ACTION=edit`, extracted `CLAUDE_ROUND`, `FILE_TYPE=summary`, current state round, and the computed correct path (`hooks/loop-edit-validator.sh:155-178`). `hooks/loop-write-validator.sh` passes similar values with `ACTION=write to` (`hooks/loop-write-validator.sh:228-244`). Output is markdown written to stderr, then the validator exits `2` to block the operation.
- gates_or_invariants: The gate runs only for summary file types; it extracts a round number and compares it to `CURRENT_ROUND` (`hooks/loop-edit-validator.sh:155-166`, `hooks/loop-write-validator.sh:224-232`). Allowlisted files bypass the block (`hooks/loop-edit-validator.sh:165`, `hooks/loop-write-validator.sh:231`). The invariant enforced is that `round-${CURRENT_ROUND}-summary.md` under the active loop directory is the only acceptable current summary target, preventing premature or stale round state transitions.
- dependencies_and_callers: Direct callers are `hooks/loop-edit-validator.sh` and `hooks/loop-write-validator.sh`. Both depend on loop-common utilities for active loop discovery, file-type classification, round extraction, and allowlist behavior before invoking the template. The template loader resolves templates relative to `prompt-template` and substitutes variables (`hooks/lib/template-loader.sh:24-31`, `hooks/lib/template-loader.sh:136-147`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: If no round can be extracted, the validators do not render this template and allow later checks to decide (`hooks/loop-edit-validator.sh:162-180`, `hooks/loop-write-validator.sh:228-246`). If the template is missing or empty, each caller has a fallback message with the same key data and still exits `2` (`hooks/loop-edit-validator.sh:167-178`, `hooks/loop-write-validator.sh:233-244`). Because the template is summary-specific in observed callers, other round file types would need separate caller wiring to use it.
- validation_or_tests: Template integration tests render this real template with sample values and assert title, attempted `round-3-summary`, current round `5`, and corrected path appear (`tests/test-template-loader.sh:145-164`, `tests/test-templates-comprehensive.sh:498-514`). `tests/test-pr-loop-hooks.sh` has a later “Wrong-Round Validation Tests” section starting after fixture tests (`tests/test-pr-loop-hooks.sh:1421-1425`). Template reference tests also check that critical validators use `load_and_render_safe` rather than unsafe direct rendering (`tests/test-template-references.sh:172-199`).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-135 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `scripts/setup-rlcr-loop.sh` plan-file path and content validation. It defines accepted path syntax, rejected shell/path hazards, symlink traversal constraints, long-path behavior, and minimum plan content requirements.
- algorithmic_behavior: The test creates an isolated temp directory, initializes a Git repo with one commit, and defines `create_valid_plan` to produce a markdown plan with at least five lines and meaningful content (`tests/robustness/test-path-validation-robustness.sh:12-44`). `test_path_validation` removes any active RLCR state, invokes the production setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"`, captures output/exit, and classifies validation failure by matching production error patterns; reaching the later “requires codex” or “must be gitignored” errors is interpreted as path/content validation success (`tests/robustness/test-path-validation-robustness.sh:55-92`).
- inputs_outputs_state: Inputs are generated plan paths and file contents inside the temp repo. Outputs are pass/fail counters through `tests/test-helpers.sh` (`tests/test-helpers.sh:30-78`) and a final process exit from `print_test_summary` (`tests/robustness/test-path-validation-robustness.sh:461-466`). The test mutates only its temp repo and cleans it by the helper’s EXIT trap (`tests/test-helpers.sh:84-89`). It intentionally depends on production setup side effects only up to validation gates, deleting `.humanize/rlcr` before each case to avoid active-loop interference (`tests/robustness/test-path-validation-robustness.sh:62-67`).
- gates_or_invariants: Positive cases require acceptance of normal relative paths, root filenames, dash/underscore names, nested paths, dotted filenames, long filenames, and deep nesting (`tests/robustness/test-path-validation-robustness.sh:101-148`, `tests/robustness/test-path-validation-robustness.sh:366-391`). Negative cases require rejection of absolute paths, spaces, shell metacharacters, glob characters, tilde, brackets/braces, hash, bang, direct file symlinks, parent directory symlinks, symlink chains, empty files, comment-only files, short files, missing files, and directories (`tests/robustness/test-path-validation-robustness.sh:158-356`, `tests/robustness/test-path-validation-robustness.sh:404-459`). Unicode/CJK/emoji paths are explicitly allowed by policy note, though not directly tested in this file (`tests/robustness/test-path-validation-robustness.sh:393-395`).
- dependencies_and_callers: The production target is `scripts/setup-rlcr-loop.sh`, specifically its plan path validation and content validation sections. The implementation rejects absolute paths (`scripts/setup-rlcr-loop.sh:320-324`), whitespace (`scripts/setup-rlcr-loop.sh:326-332`), shell metacharacters (`scripts/setup-rlcr-loop.sh:334-341`), direct symlink files (`scripts/setup-rlcr-loop.sh:346-350`), nonexistent parent directories (`scripts/setup-rlcr-loop.sh:352-357`), symlinked parent segments (`scripts/setup-rlcr-loop.sh:359-380`), missing/unreadable files (`scripts/setup-rlcr-loop.sh:382-392`), paths resolving outside the project (`scripts/setup-rlcr-loop.sh:394-406`), submodule paths (`scripts/setup-rlcr-loop.sh:408-421`), tracked plan files without `--track-plan-file` (`scripts/setup-rlcr-loop.sh:447-472`), files under five lines (`scripts/setup-rlcr-loop.sh:483-491`), and files with fewer than three content lines after excluding blanks/comments (`scripts/setup-rlcr-loop.sh:493-536`).
- edge_cases_or_failure_modes: The test’s success classifier treats downstream missing Codex CLI and gitignore enforcement as proof that path/content validation passed (`tests/robustness/test-path-validation-robustness.sh:76-84`), so failures after validation but before those messages could be misclassified as rejection. Some negative tests pass paths that do not exist, so production may reject by metacharacter before file existence; the test is validating rejection category broadly, not exact first error for every case. Long filename handling is conditional: if the filesystem refuses the file, the test records a pass as skipped-by-filesystem rather than failing (`tests/robustness/test-path-validation-robustness.sh:366-379`). The test uses `touch -d` nowhere in this file and should be portable except for Git availability and temp filesystem symlink support.
- validation_or_tests: This file is itself the validation suite, with 31 numbered tests and a summary exit (`tests/robustness/test-path-validation-robustness.sh:101-466`). It is complemented by `tests/robustness/test-plan-file-robustness.sh` and `tests/test-plan-file-validation.sh`, which target nearby plan-file validation scenarios and use similar production-message classification (`tests/robustness/test-plan-file-robustness.sh:37-53`, `tests/test-plan-file-validation.sh:122-125`, `tests/test-plan-file-validation.sh:190-199`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 section headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`