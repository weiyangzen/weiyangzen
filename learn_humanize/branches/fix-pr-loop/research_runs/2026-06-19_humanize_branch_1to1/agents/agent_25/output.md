# agent_25 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`

## Item Evidence

### FIX_PR_LOOP-HZ-025 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash gate for RLCR and PR loop protected state surfaces. It prevents Bash commands from bypassing Write/Edit/Read validators for loop-managed files and local `.humanize` state. Registered for Bash in `hooks/hooks.json:42-48`; shared helpers come from `hooks/lib/loop-common.sh`, sourced at `hooks/loop-bash-validator.sh:16-18`.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON shape/encoding/depth, exits early for non-Bash tools, then lowercases `tool_input.command` for pattern checks (`hooks/loop-bash-validator.sh:24-49`). It discovers active RLCR and PR loops under `$CLAUDE_PROJECT_DIR/.humanize/rlcr` and `.humanize/pr-loop`, allowing all commands when neither loop is active (`hooks/loop-bash-validator.sh:54-67`). For active RLCR loops it selects `finalize-state.md` over `state.md`, parses frontmatter strictly, and uses current round plus `push_every_round` to gate later checks (`hooks/loop-bash-validator.sh:74-106`). It then applies ordered block rules for `git push`, `git add .humanize`, RLCR state files, plan backup, goal tracker, prompt files, summary files, todos files, and PR-loop read-only/state files (`hooks/loop-bash-validator.sh:96-445`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.command`. Output is process exit status plus stderr block message. Exit `0` means allow, exit `1` means malformed/unsafe hook input or malformed active state, exit `2` means policy block. State is read-only: active loop detection uses newest timestamp directory via `find_active_loop`/`find_active_pr_loop` (`hooks/lib/loop-common.sh:156-174`, `hooks/lib/loop-common.sh:798-814`); strict RLCR state parsing requires `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:255-330`). It does not mutate loop state.
- gates_or_invariants: Fails closed on invalid JSON, invalid UTF-8, excessive JSON nesting, missing Bash command, or malformed active RLCR state (`hooks/loop-bash-validator.sh:26-45`, `hooks/lib/loop-common.sh:59-133`). Blocks `git push` unless `push_every_round: true` (`hooks/loop-bash-validator.sh:93-105`). Blocks force/broad `.humanize` staging through `git_adds_humanize` (`hooks/loop-bash-validator.sh:109-118`, `hooks/lib/loop-common.sh:930-1066`). Protects RLCR `state.md` and `finalize-state.md` as both destination and `mv/cp` source, including chained shell segments and shell wrappers (`hooks/loop-bash-validator.sh:139-328`). Allows only tightly authorized cancel moves to `cancel-state.md` when `.cancel-requested` exists (`hooks/lib/loop-common.sh:551-710`). For `round-[0-9]+-todos.md`, only full paths to active loop `round-1-todos.md` or `round-2-todos.md` are allowed; wrong directory, old loop, relative basename, and other rounds block (`hooks/loop-bash-validator.sh:385-393`). PR loop protection blocks active PR loop `state.md` and generated read-only files, including bare filenames from inside the loop dir (`hooks/loop-bash-validator.sh:403-442`).
- dependencies_and_callers: Depends on `jq`, `sed`, `grep`, `find`/`ls` style filesystem discovery through common helpers, template rendering via `load_and_render_safe`, and optional `iconv` for UTF-8 validation. The hook message for round-0 goal tracker Bash writes is `prompt-template/block/goal-tracker-bash-write.md`, rendered by `goal_tracker_bash_blocked_message` (`hooks/lib/loop-common.sh:493-501`). PR/RLCR block messages are templates under `prompt-template/block`. It is invoked by Claude hook configuration, not directly by application code (`hooks/hooks.json:42-48`).
- edge_cases_or_failure_modes: The command scanner lowercases the full command, so case-sensitive paths are normalized for matching but exact filesystem semantics are not modeled. It uses regex heuristics rather than a shell parser, but has explicit coverage for command chaining, `sudo`/`env`/`command` prefixes, `sh -c` and `bash -c`, `|&`, `&&`, `||`, pipes, semicolons, leading redirections, quoted redirection targets, ANSI-C quoting, escaped spaces, and append redirections (`hooks/loop-bash-validator.sh:169-284`, `hooks/loop-bash-validator.sh:306-328`). `command_modifies_file` catches common write forms but can only recognize enumerated patterns (`hooks/lib/loop-common.sh:1071-1096`). Cancel authorization rejects shell substitution, remaining `$`, chained operators, extra args, mixed quote delimiters, wrong source/destination, and symlink source files (`hooks/lib/loop-common.sh:551-710`).
- validation_or_tests: Direct tests cover finalize-state Bash blocking and parsing (`tests/test-finalize-phase.sh:381-415`, `tests/test-finalize-phase.sh:826-837`), PR loop Bash protection without RLCR (`tests/test-pr-loop-hooks.sh:174-210`), todos allowlist path restriction (`tests/test-allowlist-validators.sh:290-383`), plan backup Bash blocks and bypass forms (`tests/test-plan-file-hooks.sh:234-338`), hook JSON robustness including non-UTF8/deep nesting/long commands (`tests/robustness/test-hook-input-robustness.sh:35-217`), state/goal-tracker Bash protection (`tests/robustness/test-hook-system-robustness.sh:317-364`), and extensive cancel/redirection/shell-wrapper security cases (`tests/test-cancel-signal-file.sh`, especially cases around `sh -c`, `bash -c`, and redirection-prefixed `mv` at lines 815-1205).
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-055 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared Bash test harness for the loop validator/robustness executable specifications. It is not production algorithm code, but it standardizes test pass/fail counters, cleanup, and mock git repo setup used by many core algorithm tests.
- algorithmic_behavior: Defines color constants and global counters (`tests/test-helpers.sh:13-24`), then exposes `pass`, `fail`, and `skip` functions that print status and increment counters (`tests/test-helpers.sh:30-52`). `print_test_summary` reports totals and returns `0` only when `TESTS_FAILED == 0`, making it the shell-suite success gate (`tests/test-helpers.sh:58-78`). `setup_test_dir` creates a temp directory and installs an EXIT trap to remove it (`tests/test-helpers.sh:84-89`). `init_test_git_repo` creates a minimal git repository, disables GPG signing, writes `file.txt`, and commits it (`tests/test-helpers.sh:91-104`).
- inputs_outputs_state: Inputs are shell function arguments: labels for `pass`/`fail`/`skip`, optional expected/got text, optional summary title, temp target path for git repo initialization. Outputs are console lines plus changed shell globals `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, and `TEST_DIR`. It mutates only temporary test directories and mock git repos when called by tests.
- gates_or_invariants: The summary gate is binary: any failed assertion makes `print_test_summary` return `1` (`tests/test-helpers.sh:71-77`). Temp cleanup is bound to a single `trap "rm -rf $TEST_DIR" EXIT`, so later calls replace the trap rather than stacking multiple cleanup handlers (`tests/test-helpers.sh:86-89`). `init_test_git_repo` assumes `git` exists and that `cd "$dir"` succeeds; it restores the prior directory using `cd -` (`tests/test-helpers.sh:93-104`).
- dependencies_and_callers: Used by PR-loop tests, robustness tests, and parser/validator suites, including `tests/test-pr-loop.sh`, `tests/robustness/test-hook-input-robustness.sh`, `tests/robustness/test-cancel-security-robustness.sh`, `tests/robustness/test-goal-tracker-robustness.sh`, and others. It depends on POSIX-ish shell utilities plus `mktemp`, `git`, and Bash arithmetic.
- edge_cases_or_failure_modes: `trap "rm -rf $TEST_DIR" EXIT` is not quoted defensively at trap execution time; paths from `mktemp -d` are expected to be safe, but repeated setup calls overwrite cleanup. `cd "$dir"` inside `init_test_git_repo` affects caller process state during execution and relies on `cd -` to restore. The helper has no assertion framework isolation; all counters are global, so sourced tests share state by design.
- validation_or_tests: No direct tests for this helper were found; it is indirectly validated by every shell test that sources it. `tests/robustness/test-goal-tracker-robustness.sh:18-24` is a representative caller that sources the helper, calls `setup_test_dir`, and then uses `pass`/`fail` for production parser tests.
- skip_candidate: `yes: support test harness rather than core runtime algorithm, but it is still relevant as executable specification infrastructure for the validators`

### FIX_PR_LOOP-HZ-085 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for a specific RLCR state-machine gate: in round 0, Bash writes to `goal-tracker.md` are rejected and the agent is redirected to Write/Edit so validation hooks can see the edit.
- algorithmic_behavior: Static Markdown template with one placeholder, `{{CORRECT_PATH}}`, identifying the intended goal tracker path (`prompt-template/block/goal-tracker-bash-write.md:1-8`). It explains that Bash commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks and instructs using Write/Edit instead.
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message "$GOAL_TRACKER_PATH"` when `CURRENT_ROUND == 0` and `command_modifies_file` detects `goal-tracker.md` (`hooks/loop-bash-validator.sh:349-357`, `hooks/lib/loop-common.sh:493-501`). Output is rendered Markdown on stderr, followed by validator exit `2`. The template carries no mutable state.
- gates_or_invariants: The invariant it supports is tool-surface integrity: goal tracker modifications in round 0 must go through Write/Edit, not Bash, so the corresponding validators can enforce path/round rules. After round 0, the same Bash detection path uses `goal_tracker_blocked_message` instead, telling the agent to put update requests in the round summary (`hooks/loop-bash-validator.sh:349-356`, `hooks/lib/loop-common.sh:1100-1109`).
- dependencies_and_callers: Rendered through `load_and_render_safe`, which does single-pass `{{VAR}}` substitution and falls back to an inline message if the file is missing or renders empty (`hooks/lib/template-loader.sh:58-132`, `hooks/lib/template-loader.sh:170-203`). Template existence is checked by `tests/test-template-references.sh`, including explicit common-template coverage for `block/goal-tracker-bash-write.md` (`tests/test-template-references.sh:152-167`).
- edge_cases_or_failure_modes: If `CORRECT_PATH` is missing, the placeholder remains literal because the renderer keeps unresolved placeholders (`hooks/lib/template-loader.sh:119-122`). If the template file is missing, behavior degrades to the fallback message in `goal_tracker_bash_blocked_message` (`hooks/lib/loop-common.sh:494-500`). The text names typical Bash write commands but enforcement depends on `command_modifies_file`, not the wording.
- validation_or_tests: Reference validation scans `hooks/loop-bash-validator.sh` and `hooks/lib/loop-common.sh` for template references and verifies this file exists (`tests/test-template-references.sh:56-64`, `tests/test-template-references.sh:83-110`, `tests/test-template-references.sh:152-167`). Bash blocking for post-round-0 goal tracker writes is exercised in `tests/robustness/test-hook-system-robustness.sh:353-364`; round-0 uses this specific message path via the same validator branch.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-115 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit prompt template for the RLCR Code Review Phase. It records that the loop is invoking `codex review --base {{BASE_BRANCH}}`, then documents the transition contract: review findings with `[P0-9]` markers send the loop back for remediation; no findings transition to Finalize Phase.
- algorithmic_behavior: The template is rendered as `round-{{REVIEW_ROUND}}-review-prompt.md` and records base branch, review round, timestamp, expected `codex review` output marker format, and generated artifact names (`prompt-template/codex/code-review-phase.md:1-36`). It is documentation/audit input, not a prompt consumed by `codex review`; the template explicitly states `codex review` does not accept prompt input (`prompt-template/codex/code-review-phase.md:3-4`).
- inputs_outputs_state: Inputs are template variables `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP` as defined in the template (`prompt-template/codex/code-review-phase.md:1-10`). The caller also passes `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE`, but this template does not currently reference them (`hooks/loop-codex-stop-hook.sh:948-954`). Output is the audit file at `$LOOP_DIR/round-${round}-review-prompt.md`; cache outputs include command and log files (`hooks/loop-codex-stop-hook.sh:930-968`).
- gates_or_invariants: The operational gate is in `run_and_handle_code_review`: `codex review` failure is blocking, empty/missing output is blocking, `[P0-9]` findings continue the review loop, and no findings enter finalize (`hooks/loop-codex-stop-hook.sh:999-1025`). The template mirrors that contract in prose (`prompt-template/codex/code-review-phase.md:12-18`). Review base selection prefers captured `BASE_COMMIT` over `BASE_BRANCH` to avoid comparing a branch to itself after commits advance (`hooks/loop-codex-stop-hook.sh:921-928`), although the current template only prints `BASE_BRANCH`.
- dependencies_and_callers: Rendered by `run_codex_code_review` using `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:916-954`). The actual review command is `codex review --base "$review_base"` with Codex model/review_model config args (`hooks/loop-codex-stop-hook.sh:899-905`, `hooks/loop-codex-stop-hook.sh:973-978`). Review issue detection uses `detect_review_issues` from `hooks/lib/loop-common.sh`, referenced by the stop hook (`hooks/loop-codex-stop-hook.sh:986-1015`).
- edge_cases_or_failure_modes: Since this is an audit file, stale/missing fields do not directly alter `codex review`; the stop hook fallback contains richer base-commit/review-base detail than the template, so rendered audit output can omit the actual commit base even though the command uses it (`hooks/loop-codex-stop-hook.sh:936-954`). If the template is missing, `load_and_render_safe` writes the fallback audit content instead (`hooks/lib/template-loader.sh:170-203`). If `codex review` exits nonzero or produces no usable output, `block_review_failure` prevents loop exit and requires retry (`hooks/loop-codex-stop-hook.sh:1163-1207`).
- validation_or_tests: Template reference validation includes `hooks/loop-codex-stop-hook.sh` among scanned scripts and checks all `load_and_render_safe` template paths exist (`tests/test-template-references.sh:56-64`, `tests/test-template-references.sh:83-110`). Finalize/review tests mock codex review success/failure/empty output and assert blocking behavior for failure modes (`tests/test-finalize-phase.sh:566-651`). The README and setup scripts also document the same review transition, but the stop hook is the authoritative runtime caller.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item sections present; each assigned item_id appears exactly once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`