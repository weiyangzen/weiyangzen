# agent_01 fix-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `81f6f49d816a90a0e719db41ce15c4636ab9858a`
- notes: read-only branch export inspected recursively; no files modified. The export does not include usable git metadata, so the source commit is taken from scheduler metadata.

## Item Evidence

### FIX_PR_LOOP-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude plugin. It contains the RLCR loop and PR-loop algorithms, hook registration, slash-command entrypoints, prompt/block templates, GitHub polling scripts, monitor/cancel helpers, and shell test suites. Recursive inspection found hidden plugin metadata, GitHub workflow files, commands, hooks, templates, scripts, fixtures, mocks, and robustness tests.
- algorithmic_behavior: The top-level workflow is command-driven and hook-gated. `/humanize:start-pr-loop` delegates to `scripts/setup-pr-loop.sh` through `commands/start-pr-loop.md:1-14`; setup detects the PR, validates prerequisites, creates `.humanize/pr-loop/<timestamp>/state.md`, fetches initial comments, derives startup case, creates a goal tracker, and emits the initial prompt (`scripts/setup-pr-loop.sh:204-241`, `scripts/setup-pr-loop.sh:409-574`, `scripts/setup-pr-loop.sh:657-884`). Hook registration wires pre-tool validators and both Stop hooks through `hooks/hooks.json:14-64`. At Stop time, `hooks/pr-loop-stop-hook.sh` polls remote reviews, updates PR-loop state, and blocks or releases the session. Shared helpers in `hooks/lib/loop-common.sh` provide state parsing, bot mapping, active-loop discovery, read-only file classification, and wrong-round validation (`hooks/lib/loop-common.sh:722-887`). `prompt-template/` supplies the user-facing transition contracts and block messages loaded by `hooks/lib/template-loader.sh:24-222`.
- inputs_outputs_state: Inputs are slash-command arguments, Claude hook JSON, local git state, GitHub CLI PR/review/comment/reaction data, Codex CLI output, prompt templates, and loop state files. Outputs are `.humanize/rlcr` and `.humanize/pr-loop` state directories, generated prompt/comment/check/feedback/summary files, JSON hook decisions, PR trigger comments, and renamed terminal state files such as `approve-state.md`, `closed-state.md`, `merged-state.md`, `maxiter-state.md`, and `usage-limit-state.md`. PR-loop state centers on `current_round`, `configured_bots`, `active_bots`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id`.
- gates_or_invariants: Only one RLCR or PR loop may be active (`scripts/setup-pr-loop.sh:217-241`). Bot flags are mandatory (`scripts/setup-pr-loop.sh:175-190`). GitHub CLI, Codex, git repo, open PR, YAML-safe branch/model/effort, and fork-aware PR lookup are setup gates (`scripts/setup-pr-loop.sh:243-407`). PreToolUse validators protect `.humanize` state, generated prompts/comments/checks/feedback, wrong-round resolve files, and git-add attempts against loop internals (`hooks/loop-write-validator.sh:71-94`, `hooks/loop-bash-validator.sh:397-443`). Stop hook gates include required resolution summary, clean/pushed git state, trigger freshness, Claude eyes reaction when required, per-bot review timeout, Codex marker parsing, and max-iteration termination.
- dependencies_and_callers: `commands/` are Claude slash-command surfaces; `hooks/hooks.json` is the caller registry; `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh` implement PR-loop GitHub IO; `hooks/lib/template-loader.sh` resolves prompt/block templates; tests in `tests/` call hooks/scripts with mocks and fixtures. External dependencies are `bash`, `jq`, `git`, `gh`, `codex`, and portable `timeout` behavior via `scripts/portable-timeout.sh`.
- edge_cases_or_failure_modes: Fork PRs are explicitly resolved to the base repository in setup and polling. Stale trigger comments after force pushes or new commits are rejected by clearing trigger fields and filtering comments after `latest_commit_at`. Empty-body approval reviews are represented with `[Review state: ...]` placeholders in polling. macOS Bash 3.2 compatibility is handled by avoiding associative arrays in production PR-loop code and by using dynamic variable maps. The root test suite also covers malformed hook JSON, path traversal/validation, concurrent state, cancellation security, template failure, timeout behavior, and PR-loop API failures.
- validation_or_tests: Test coverage is broad: `tests/test-pr-loop-hooks.sh` is the assigned executable specification for PR-loop hooks; `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-scripts.sh`, and `tests/robustness/test-pr-loop-api-robustness.sh` cover adjacent behavior; fixtures and mock `gh` data live under `tests/fixtures` and `tests/mocks`. I inspected tests but did not execute them because this run requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Main PR-loop Stop hook and transition controller. It intercepts Claude exit attempts during an active `.humanize/pr-loop` session, verifies local and remote prerequisites, polls configured PR review bots, asks local Codex to classify bot feedback, mutates loop state, and either blocks Claude with next-round feedback or ends the loop.
- algorithmic_behavior: The script reads hook stdin, resolves `PROJECT_ROOT`, sources `loop-common.sh` and `portable-timeout.sh`, finds the newest active PR loop, and parses `state.md` frontmatter into scalar fields plus YAML-list arrays (`hooks/pr-loop-stop-hook.sh:42-76`, `hooks/pr-loop-stop-hook.sh:87-176`). It resolves the PR base repository for fork support, checks PR `MERGED` or `CLOSED`, requires the current round resolve file, enforces git cleanliness and pushed commits, detects force pushes, handles max iterations and empty `active_bots`, detects fresh trigger comments from the current GitHub user, validates trigger requirements by startup case/round, checks Codex thumbs-up reactions, verifies Claude eyes reactions when required, polls for bot reviews, writes the next round comment/check/feedback files, and runs Codex to classify final status (`hooks/pr-loop-stop-hook.sh:204-255`, `hooks/pr-loop-stop-hook.sh:261-360`, `hooks/pr-loop-stop-hook.sh:367-447`, `hooks/pr-loop-stop-hook.sh:454-746`, `hooks/pr-loop-stop-hook.sh:752-799`, `hooks/pr-loop-stop-hook.sh:805-1636`).
- inputs_outputs_state: Inputs are active `state.md`, round resolve file, local git status and HEAD, GitHub PR state/comments/commits/reactions, configured templates, `poll-pr-reviews.sh` JSON, `check-bot-reactions.sh` JSON, and Codex output. Outputs include JSON hook block decisions, stderr diagnostics, rewritten `state.md`, renamed terminal state files, `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, and optional goal tracker updates. Primary state transitions are: active `state.md` to `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, or `usage-limit-state.md`; otherwise `current_round` increments and `active_bots` is rewritten based on Codex classification.
- gates_or_invariants: Required state fields include numeric `current_round` and `pr_number`; missing or invalid fields exit without taking over other hooks (`hooks/pr-loop-stop-hook.sh:185-194`). A resolution summary for the current round is mandatory before exit (`hooks/pr-loop-stop-hook.sh:261-276`). Non-`.humanize` tracked changes and unpushed commits block exit (`hooks/pr-loop-stop-hook.sh:282-360`). Trigger comments must be from the current user, mention configured bots, and be newer than the latest commit timestamp when available (`hooks/pr-loop-stop-hook.sh:465-520`, `hooks/pr-loop-stop-hook.sh:566-599`). `REQUIRE_TRIGGER` is true for later rounds, startup cases 4/5, and newly detected commits (`hooks/pr-loop-stop-hook.sh:612-643`). Claude eyes reaction is blocking after required trigger detection (`hooks/pr-loop-stop-hook.sh:767-789`). Codex output must be non-empty and end in an accepted marker: `APPROVE`, `WAITING_FOR_BOTS`, `USAGE_LIMIT_HIT`, or implied issues remaining (`hooks/pr-loop-stop-hook.sh:1347-1412`).
- dependencies_and_callers: Called by Claude Stop hook registration after `loop-codex-stop-hook.sh` (`hooks/hooks.json:52-64`). It depends on `hooks/lib/loop-common.sh` for bot mapping, YAML list building, active-loop lookup, and goal tracker update helpers; `hooks/lib/template-loader.sh` indirectly for rendered block messages; `scripts/portable-timeout.sh`; `scripts/poll-pr-reviews.sh`; `scripts/check-bot-reactions.sh`; `scripts/check-pr-reviewer-status.sh`; plus external `gh`, `git`, `jq`, `codex`, `awk`, `sed`, and `date`.
- edge_cases_or_failure_modes: Fork PR lookup falls back from current repo to parent repo (`hooks/pr-loop-stop-hook.sh:204-236`). Force pushes clear stale trigger fields and require fresh bot mention (`hooks/pr-loop-stop-hook.sh:367-420`). New non-force commits refresh `latest_commit_at` and clear triggers (`hooks/pr-loop-stop-hook.sh:540-564`). Pagination is handled by `gh api --paginate --jq ... | jq -s` in trigger detection (`hooks/pr-loop-stop-hook.sh:470-478`). Per-bot timeout is anchored to trigger time, except startup cases 2/3 collect historical comments and anchor timeout to poll start (`hooks/pr-loop-stop-hook.sh:868-878`). The hook polls all configured bots, not only currently active bots, so a previously approved bot can be re-added if it posts new issues (`hooks/pr-loop-stop-hook.sh:880-901`, `hooks/pr-loop-stop-hook.sh:1431-1494`). Codex thumbs-up can remove only Codex from active bots before or during polling (`hooks/pr-loop-stop-hook.sh:661-718`, `hooks/pr-loop-stop-hook.sh:984-1038`). Cancel signal exits the poll loop (`hooks/pr-loop-stop-hook.sh:951-955`).
- validation_or_tests: Directly exercised by `tests/test-pr-loop-hooks.sh` E2E and gate tests, including missing resolve, trigger detection, pagination, trigger priority, timeout anchoring, configured bot parsing, and wrong-round resolve validation. Additional focused coverage exists in `tests/test-pr-loop-stophook.sh` and broader PR-loop/system suites. Not executed in this research pass.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop hook behavior. It defines `run_hook_tests`, then groups unit, integration, fixture-backed, wrong-round, and monitor tests that codify the expected PR-loop state machine and validator contracts.
- algorithmic_behavior: The test runner begins with validator expectations for YAML-list `active_bots`, protected PR-loop state/comment files, allowed current-round resolve writes, and Bash bypass blocking (`tests/test-pr-loop-hooks.sh:15-210`). It then tests sorting and gate logic: newest-first timestamps, human-before-bot ordering, comment deduplication by ID, configured bot parsing, Codex status extraction, bot re-add logic, trigger detection, `APPROVE` marker parsing, and `WAITING_FOR_BOTS` marker parsing (`tests/test-pr-loop-hooks.sh:223-466`). Stop-hook integration tests use mocked `gh` and `git` binaries to validate current-user trigger filtering, trigger refresh, missing-trigger blocking, round-0 timestamp priority, trigger-anchored timeouts, configured bot state, and round file naming (`tests/test-pr-loop-hooks.sh:478-754`). E2E sections create temporary `.humanize/pr-loop` states and invoke `hooks/pr-loop-stop-hook.sh` for missing resolve, trigger detection, paginated comments, and round-0 `last_trigger_at` precedence (`tests/test-pr-loop-hooks.sh:766-1148`). Later sections cover approval-only reviews, fixture-backed fetch/poll scripts, wrong-round resolve validator behavior, and monitor parsing (`tests/test-pr-loop-hooks.sh:1160-1621`).
- inputs_outputs_state: Inputs are global test harness variables such as `TEST_DIR`, `PROJECT_ROOT`, `SCRIPT_DIR`, and pass/fail helpers; temporary loop directories and `state.md` files; mocked `gh`/`git`; JSON fixtures; and scripts/hooks under test. Outputs are pass/fail records through the harness, generated temporary mock binaries, temporary PR-loop files, and captured hook/script output. State transitions are simulated through temporary `state.md` frontmatter and direct invocation of validators/stop hook.
- gates_or_invariants: The tests assert that PR loop stores bots as YAML lists, protects generated state/comment/check/prompt/feedback files, only allows current-round `round-N-pr-resolve.md`, blocks Bash bypasses, prioritizes `last_trigger_at` over `started_at` when present, filters triggers by current user, anchors timeouts to trigger time, collects all configured bots separately from active bots, captures approval-only reviews, filters comments by `--after`, and displays monitor active/configured bots correctly.
- dependencies_and_callers: Intended to be sourced by a broader shell test harness and invoked via `run_hook_tests` (`tests/test-pr-loop-hooks.sh:12-15`). It depends on helper functions such as `init_test_git_repo`, `pass`, and `fail`; project scripts including `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `humanize.sh`, validators, and stop hook; fixture setup via `tests/setup-fixture-mock-gh.sh`; monitor setup via `tests/setup-monitor-test-env.sh`; and external `jq`, `bash`, plus mock `gh`/`git`.
- edge_cases_or_failure_modes: Covers comments from non-current users, pagination where the trigger appears on a later page, old `started_at` versus newer trigger timestamp, missing resolve summary, missing trigger on later rounds, approval reviews with empty or null body, fixture data with mixed issue/review/approval comments, after-filter exclusion, and wrong-round write/edit attempts. A notable portability caveat is that one unit test uses `declare -A` for simulated maps (`tests/test-pr-loop-hooks.sh:354-364`), while production code avoids associative arrays for macOS Bash 3.2 compatibility.
- validation_or_tests: This file is itself validation. It was inspected directly and mapped to the implementation but not executed in the read-only research pass.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR plan backup invariant. It tells Claude that `.humanize/rlcr/<loop>/plan.md` is a backup of the original plan and must be read-only.
- algorithmic_behavior: The template is a static rendered message. It states that `plan.md` in the loop directory cannot be modified, exists to preserve plan integrity, and should be read rather than edited (`prompt-template/block/plan-backup-protected.md:1-7`). It is loaded as a safe template fallback surface by write and Bash validators when a tool attempts to modify the RLCR loop plan backup (`hooks/loop-write-validator.sh:175-185`, `hooks/loop-bash-validator.sh:330-340`).
- inputs_outputs_state: It has no template variables. Input is a validator-detected attempt to modify the backup plan. Output is user-facing block text emitted to stderr by the validator, with exit code 2 from the caller. State is intentionally unchanged; the invariant is preservation of the backup plan file.
- gates_or_invariants: The invariant is plan immutability during an RLCR loop. `loop-write-validator.sh` blocks direct Write attempts to `.humanize/rlcr/.../plan.md`; `loop-bash-validator.sh` blocks shell-level modifications targeting `.humanize/rlcr/.../plan.md`.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh`. Callers are `hooks/loop-write-validator.sh` and `hooks/loop-bash-validator.sh`, both registered in `hooks/hooks.json` as PreToolUse hooks for Write and Bash.
- edge_cases_or_failure_modes: If the template is missing or renders empty, callers provide a fallback message. Because this template has no variables, there is no variable injection surface. It is RLCR-focused, not PR-loop-specific, but it participates in the same hook protection layer that prevents loop state tampering.
- validation_or_tests: Covered indirectly by plan-file and validator suites, especially tests around plan backup protection and Bash bypass patterns. Not executed in this pass.
- skip_candidate: `no`

### FIX_PR_LOOP-HZ-121 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt contract for round-0 sessions that already have comments. It tells Claude the exact completion side effects required before attempting to exit: commit and push, comment on the PR to trigger re-review, and write the current resolve summary.
- algorithmic_behavior: The template renders `{{PR_NUMBER}}`, `{{BOT_MENTION_STRING}}`, and `{{RESOLVE_PATH}}` into a critical requirements block (`prompt-template/pr-loop/critical-requirements-has-comments.md:1-24`). `scripts/setup-pr-loop.sh` selects this template only when initial fetched comments exist, after writing the initial prompt and before final command output (`scripts/setup-pr-loop.sh:829-881`). The resulting instructions align with Stop hook gates: the push requirement is enforced by the unpushed commit check, the trigger comment is enforced by trigger detection when required, and the resolve path is enforced by the missing resolve summary gate.
- inputs_outputs_state: Inputs are template variables from setup: PR number, active bot mention string, and `round-0-pr-resolve.md` path (`scripts/setup-pr-loop.sh:670-677`, `scripts/setup-pr-loop.sh:855-879`). Output is appended command output shown to Claude, not a separate state file. It influences subsequent state by directing the user to create a resolve file, push commits, and post a trigger comment before Stop hook polling.
- gates_or_invariants: Defines the human/agent contract that the Stop hook later validates. It encodes that changes must be visible remotely before bot review, that re-review must be explicitly triggered with the configured bot mentions, and that the current round has a resolution summary at the expected path. These are mirrored by `hooks/pr-loop-stop-hook.sh:261-276`, `hooks/pr-loop-stop-hook.sh:312-360`, and `hooks/pr-loop-stop-hook.sh:727-746`.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `scripts/setup-pr-loop.sh`; rendered by `hooks/lib/template-loader.sh`, whose single-pass substitution leaves unknown placeholders untouched (`hooks/lib/template-loader.sh:50-132`). It coordinates with `prompt-template/pr-loop/round-0-task-has-comments.md`, the generated `round-0-prompt.md`, and Stop hook enforcement.
- edge_cases_or_failure_modes: If initial comments are absent, setup uses `critical-requirements-no-comments.md` instead. If template variables are missing, unresolved placeholders remain visible rather than being recursively expanded. If bot flags are invalid or absent, setup fails before this template is rendered, so the mention string should correspond to configured bots.
- validation_or_tests: Template presence/reference is covered by template reference/comprehensive tests, and the behavioral contract is covered by PR-loop hook tests for missing resolve, trigger requirements, and unpushed commit gates. Not executed in this pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 Item Evidence sections, one per assigned row, in assigned order; item identifiers are present in section headings only
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`