# agent_21 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-021 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: Skill-facing workflow contract for the Humanize runtime. The directory recursively contains only `skills/humanize/SKILL.md`, so its algorithmic role is declarative: it describes RLCR phases, command entry points, state files, and runtime boundaries rather than implementing executable shell logic.
- algorithmic_behavior: `skills/humanize/SKILL.md:32` defines the RLCR two-phase loop: implementation summaries are reviewed until Codex returns `COMPLETE`, then a code-review/finalize phase runs. `skills/humanize/SKILL.md:48` documents the native Codex `Stop` hook path for automatic exit gating. `skills/humanize/SKILL.md:50` also documents plan generation as a structured transformation from draft to implementation plan.
- inputs_outputs_state: Inputs are plan files or review-only mode via `setup-rlcr-loop.sh` (`skills/humanize/SKILL.md:61`), optional command flags (`skills/humanize/SKILL.md:73`), and generated plan drafts (`skills/humanize/SKILL.md:97`). Outputs and state are `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-N-summary.md`, review results, finalize artifacts, and `.humanize/skill/<timestamp>/output.md` (`skills/humanize/SKILL.md:180`).
- gates_or_invariants: Requires summaries before exit (`skills/humanize/SKILL.md:168`), goal tracker maintenance (`skills/humanize/SKILL.md:169`), no state-file cheating/cancel-command bypass (`skills/humanize/SKILL.md:171`), and normal stop/exit so the Stop hook can gate progress (`skills/humanize/SKILL.md:172`).
- dependencies_and_callers: Documents runtime commands under `{{HUMANIZE_RUNTIME_ROOT}}/scripts/`, especially `setup-rlcr-loop.sh`, `cancel-rlcr-loop.sh`, `validate-gen-plan-io.sh`, `ask-codex.sh`, and `humanize.sh` monitor entry points (`skills/humanize/SKILL.md:63`, `skills/humanize/SKILL.md:91`, `skills/humanize/SKILL.md:99`, `skills/humanize/SKILL.md:107`, `skills/humanize/SKILL.md:210`). It coordinates with sibling installed runtime folders (`scripts`, `hooks`, `prompt-template`, `templates`, `config`, `agents`) referenced by install docs, but those are not physically under this assigned directory.
- edge_cases_or_failure_modes: Misclassification risk: this directory is included as a core ancestor but does not itself contain runtime algorithms beyond the skill manifest. It can still affect behavior if installed with an unresolved `{{HUMANIZE_RUNTIME_ROOT}}` placeholder or stale command defaults, because users and agents would invoke wrong paths or models.
- validation_or_tests: No direct tests for this exact manifest were inspected. Installation docs and broad references expect `skills/humanize/SKILL.md` to exist, and runtime scripts/tests validate the behavior it describes.
- skip_candidate: `yes: directory contains only SKILL.md declarative workflow documentation; core algorithm descendants are referenced, not nested in this directory`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-051 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Stop-gate validator for required `## BitLesson Delta` reporting in non-finalize RLCR rounds. It converts invalid summary/knowledge-base alignment into a JSON `"decision": "block"` response consumed by the Codex stop hook.
- algorithmic_behavior: Parses required CLI args (`scripts/bitlesson-validate-delta.sh:24`), verifies the summary file exists (`scripts/bitlesson-validate-delta.sh:69`), loads template rendering helpers (`scripts/bitlesson-validate-delta.sh:74`), and emits block JSON through `block_exit` (`scripts/bitlesson-validate-delta.sh:78`). `extract_bitlesson_delta_block` scans markdown while ignoring fenced code blocks and HTML comments (`scripts/bitlesson-validate-delta.sh:92`), detects a real heading matching `## BitLesson Delta` (`scripts/bitlesson-validate-delta.sh:153`), and extracts its body until the next level-2 heading (`scripts/bitlesson-validate-delta.sh:161`).
- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` (`scripts/bitlesson-validate-delta.sh:6`). Output is empty stdout with exit 0 when validation passes, or a JSON block object with `decision`, `reason`, and `systemMessage` when the loop must stop and prompt for repair (`scripts/bitlesson-validate-delta.sh:81`). It does not mutate state or files.
- gates_or_invariants: Requires exactly one `Action:` with value `none`, `add`, or `update` (`scripts/bitlesson-validate-delta.sh:202`). `Action: none` requires `Lesson ID(s): NONE` or empty IDs (`scripts/bitlesson-validate-delta.sh:242`) and may be blocked when the BitLesson KB has no concrete entries and empty-none is disabled (`scripts/bitlesson-validate-delta.sh:254`). `add`/`update` require concrete IDs (`scripts/bitlesson-validate-delta.sh:269`), non-empty non-placeholder `Notes:` (`scripts/bitlesson-validate-delta.sh:281`), an existing BitLesson file (`scripts/bitlesson-validate-delta.sh:299`), valid ID format `BL-YYYYMMDD-short-name` (`scripts/bitlesson-validate-delta.sh:322`), and matching `Lesson ID:` entries in the KB (`scripts/bitlesson-validate-delta.sh:328`).
- dependencies_and_callers: Depends on `jq`, `awk`, `sed`, and `hooks/lib/template-loader.sh` via `load_and_render_safe` (`scripts/bitlesson-validate-delta.sh:76`). Main caller is `hooks/loop-codex-stop-hook.sh:823`, which runs it for non-finalize rounds when BitLesson is required and exits early if the script emits a block object. Uses prompt templates such as `prompt-template/block/bitlesson-delta-missing-notes.md`.
- edge_cases_or_failure_modes: Missing required args exit 1 (`scripts/bitlesson-validate-delta.sh:62`); missing summary exits 1 (`scripts/bitlesson-validate-delta.sh:69`). A `## BitLesson Delta` heading inside code fences or HTML comments is intentionally ignored (`scripts/bitlesson-validate-delta.sh:104`, `scripts/bitlesson-validate-delta.sh:133`). Multiple or absent `Action:` lines block. Comma-separated IDs are trimmed but only comma delimiters are supported (`scripts/bitlesson-validate-delta.sh:315`). Placeholder lesson IDs in the KB are excluded from concrete-count logic (`scripts/bitlesson-validate-delta.sh:234`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` directly covers whitespace notes, bracket/angle placeholder notes, fenced/commented delta headings, and valid add/update flows (`tests/test-bitlesson-validate-delta.sh:101`, `tests/test-bitlesson-validate-delta.sh:116`, `tests/test-bitlesson-validate-delta.sh:130`, `tests/test-bitlesson-validate-delta.sh:144`). Stop-hook invocation is covered by the call site in `hooks/loop-codex-stop-hook.sh:823`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-081 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared shell-test harness for executable specs. It is not product runtime, but it controls test pass/fail accounting and temporary git-repo fixtures used by algorithm tests.
- algorithmic_behavior: Defines color constants (`tests/test-helpers.sh:13`), initializes global counters (`tests/test-helpers.sh:22`), and exposes `pass`, `fail`, `skip`, `print_test_summary`, `setup_test_dir`, and `init_test_git_repo`. `print_test_summary` returns success only when `TESTS_FAILED` is zero (`tests/test-helpers.sh:71`).
- inputs_outputs_state: Inputs are assertion names and optional expected/actual strings. Outputs are colored status lines and a summary block. State is held in shell globals `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, and `TEST_DIR`; `setup_test_dir` registers an EXIT trap that removes the temp directory (`tests/test-helpers.sh:86`).
- gates_or_invariants: Any call to `fail` increments `TESTS_FAILED`; `print_test_summary` is the suite-level gate that returns 1 when failures exist (`tests/test-helpers.sh:74`). `init_test_git_repo` creates a minimal repository with user identity and unsigned initial commit (`tests/test-helpers.sh:93`).
- dependencies_and_callers: Used broadly by shell tests, including `tests/test-bitlesson-validate-delta.sh:10`, `tests/test-config-merge.sh:18`, `tests/test-config-error-handling.sh:18`, and robustness tests under `tests/robustness/`. Depends on `mktemp`, `trap`, `git`, and standard shell builtins.
- edge_cases_or_failure_modes: `setup_test_dir` overwrites any prior `TEST_DIR` and replaces the EXIT cleanup trap, so repeated calls in one script intentionally clean only the latest directory at process exit. `init_test_git_repo` changes directories internally and returns with `cd -`, so failures before the final `cd -` can strand the shell in the fixture repo under `set -e`.
- validation_or_tests: This file is itself test infrastructure rather than a target with independent unit tests. Its behavior is indirectly exercised by every sourced shell test that relies on counters and `print_test_summary`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-111 `file` `prompt-template/block/bitlesson-delta-missing-notes.md`
- cursor: `[_]`
- core_role: Block-message template for the BitLesson delta notes gate. It defines the human-facing repair instruction when `Action: add` or `Action: update` lacks a real `Notes:` explanation.
- algorithmic_behavior: Provides a short Markdown reason with `{{ACTION}}` substitution (`prompt-template/block/bitlesson-delta-missing-notes.md:3`) and explicitly rejects empty or placeholder notes (`prompt-template/block/bitlesson-delta-missing-notes.md:5`).
- inputs_outputs_state: Input is the template variable `ACTION`, supplied by `scripts/bitlesson-validate-delta.sh:294`. Output is rendered Markdown embedded as the JSON `reason` field in the stop-gate block response. No state mutation.
- gates_or_invariants: Enforces the review contract that add/update declarations must explain what changed and why. The actual condition is evaluated in the validator: empty notes or notes matching bracket/angle placeholders block (`scripts/bitlesson-validate-delta.sh:281`).
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "block/bitlesson-delta-missing-notes.md"` from `scripts/bitlesson-validate-delta.sh:294`; rendered by `hooks/lib/template-loader.sh` single-pass substitution (`hooks/lib/template-loader.sh:56`).
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline message in the validator (`scripts/bitlesson-validate-delta.sh:286`). Missing `ACTION` would leave the placeholder unresolved by design, because the renderer preserves unknown placeholders (`hooks/lib/template-loader.sh:119`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh:75` asserts blocked output mentions `Notes`; cases at `tests/test-bitlesson-validate-delta.sh:101`, `tests/test-bitlesson-validate-delta.sh:106`, and `tests/test-bitlesson-validate-delta.sh:111` exercise this template path.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-141 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template for preventing direct `round-*-todos.md` file access in RLCR loops, steering agents to native task tooling instead.
- algorithmic_behavior: Emits a Markdown block reason: do not create or access `round-*-todos.md`; use `TaskCreate`, `TaskUpdate`, `TaskList` (`prompt-template/block/todos-file-access.md:3`). It is presentation logic for validators that detect disallowed todo-file read/write/edit/bash access.
- inputs_outputs_state: No dynamic variables are used. Output is rendered Markdown returned by `todos_blocked_message` (`hooks/lib/loop-common.sh:828`) and printed to stderr by validators. No state mutation.
- gates_or_invariants: The invariant is that todo state should be managed through native task tools, except allowlisted early-round files. `hooks/lib/loop-common.sh:806` defines `is_allowlisted_file`; only `round-1-todos.md` and `round-2-todos.md` in the active loop dir are allowlisted (`hooks/lib/loop-common.sh:810`). Non-allowlisted todo file edits block in `hooks/loop-edit-validator.sh:40`; read/write/bash validators also call `todos_blocked_message` per the reference map.
- dependencies_and_callers: Loaded by `hooks/lib/loop-common.sh:834` using `load_and_render_safe`. Called from `hooks/loop-edit-validator.sh:45`, `hooks/loop-read-validator.sh:61`, `hooks/loop-write-validator.sh:62`, and `hooks/loop-bash-validator.sh:548`.
- edge_cases_or_failure_modes: If no active loop is found or the path is outside the active allowlist, validators block. Allowlisting is exact path based, so `round-1-todos.md` in the wrong directory or without a full active-loop path is blocked. Missing template falls back to an inline message in `hooks/lib/loop-common.sh:830`.
- validation_or_tests: `tests/test-template-references.sh:152` checks `block/todos-file-access.md` exists as a common template. `tests/test-allowlist-validators.sh` covers allowed `round-1/2-todos.md` and blocked later/wrong-directory/generic todo paths, including edit, read, write, and bash paths.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-171 `file` `scripts/lib/config-loader.sh`
- cursor: `[_]`
- core_role: Shared configuration merge library for Humanize commands. It centralizes config precedence and value extraction so commands do not hand-roll `.humanize/config.json` parsing.
- algorithmic_behavior: Source-guard prevents double loading (`scripts/lib/config-loader.sh:2`), then strict shell mode is enabled (`scripts/lib/config-loader.sh:6`). `_config_loader_require_jq` fails if `jq` is unavailable (`scripts/lib/config-loader.sh:17`). `_config_loader_prepare_layer` validates each layer as a JSON object, emits `{}` for absent optional layers, fatals on missing/malformed required layers, and warns while ignoring malformed optional layers (`scripts/lib/config-loader.sh:24`). `load_merged_config` resolves default, user, and project paths (`scripts/lib/config-loader.sh:77`), creates temp normalized layer files, strips nulls recursively, and merges layers with later layers winning (`scripts/lib/config-loader.sh:113`). `get_config_value` returns a scalar/stringified value or empty when missing/null (`scripts/lib/config-loader.sh:139`).
- inputs_outputs_state: Inputs are `<plugin_root>` and `<project_root>` plus environment variables `XDG_CONFIG_HOME`, `HOME`, and `HUMANIZE_CONFIG`. Default config is `$plugin_root/config/default_config.json`; user config is `${XDG_CONFIG_HOME:-$HOME/.config}/humanize/config.json`; project config is `${HUMANIZE_CONFIG:-$project_root/.humanize/config.json}` (`scripts/lib/config-loader.sh:77`). Output is merged JSON on stdout, warning/error text on stderr, and nonzero status for fatal conditions. Temporary files are cleaned by a subshell EXIT trap (`scripts/lib/config-loader.sh:100`).
- gates_or_invariants: `jq` is mandatory; default config is required and must be a JSON object (`scripts/lib/config-loader.sh:109`). Optional user/project configs must also be JSON objects to participate, but malformed optional configs are ignored with warnings (`scripts/lib/config-loader.sh:52`). Nulls never override lower-layer values because `strip_nulls` removes them before merge (`scripts/lib/config-loader.sh:119`).
- dependencies_and_callers: Used by `scripts/bitlesson-select.sh:10`, which reads `bitlesson_model`, `codex_model`, and `provider_mode`. Command specs for gen-plan/refine-plan require this loader and its precedence (`commands/gen-plan.md:64`, `commands/refine-plan.md:88`). Default values include `codex_model`, `codex_effort`, `bitlesson_model`, `agent_teams`, `alternative_plan_language`, and `gen_plan_mode` in `config/default_config.json:1`.
- edge_cases_or_failure_modes: Missing args return usage failure (`scripts/lib/config-loader.sh:70`). Missing `HOME` with no `XDG_CONFIG_HOME` yields `/.config/humanize/config.json` as the optional user path, which is ignored if absent. Because `_config_loader_prepare_layer` may `exit 1` inside the subshell, fatal required-layer failures propagate out of `load_merged_config`. `get_config_value` only supports top-level keys and stringifies non-string scalars/objects.
- validation_or_tests: `tests/test-config-merge.sh` covers default-only, project override, project-over-user priority, additive merge, null stripping, `HUMANIZE_CONFIG`, and all-layer merge (`tests/test-config-merge.sh:43`, `tests/test-config-merge.sh:70`, `tests/test-config-merge.sh:95`, `tests/test-config-merge.sh:138`, `tests/test-config-merge.sh:156`). `tests/test-config-error-handling.sh` covers missing default fatal, malformed optional warnings/fallbacks, empty config, missing project config, and missing user config (`tests/test-config-error-handling.sh:40`, `tests/test-config-error-handling.sh:59`, `tests/test-config-error-handling.sh:87`, `tests/test-config-error-handling.sh:116`, `tests/test-config-error-handling.sh:134`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-021`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-051`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-081`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-111`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-141`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-171`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`