# agent_11 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification suite for the Humanize RLCR state machine, validators, setup/cancel flows, monitor behavior, template rendering, config/model routing, BitLesson routing, and robustness/security edge cases. The tree contains 43 top-level test/support files, 16 `tests/robustness` suites, 8 fixture JSON files, and 1 `tests/mocks/gh` mock.
- algorithmic_behavior: `tests/run-all-tests.sh:60-118` defines the suite matrix and includes stop hook, cancellation, finalize, session, setup, state, path, hook, timeout, template, config, monitor, and robustness suites. It executes suites in parallel with bounded concurrency from `HUMANIZE_TEST_JOBS` or CPU count capped at 8 (`tests/run-all-tests.sh:19-37`, `tests/run-all-tests.sh:161-217`), captures per-suite output and exit status, strips ANSI for pass/fail aggregation, and prints detailed failing logs (`tests/run-all-tests.sh:228-302`). Individual suites construct isolated temp repos and mock tools to exercise production scripts rather than only unit stubs.
- inputs_outputs_state: Inputs are shell scripts, generated temp git repositories, mock `codex` and `gh`, JSON fixtures under `tests/fixtures`, and hook JSON payloads. Outputs are pass/fail counters and process exit `0` or `1`; `tests/test-helpers.sh:30-78` centralizes `pass`, `fail`, `skip`, and summary behavior for most suites. State under test is usually `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md,methodology-analysis-state.md,*-summary.md,*-prompt.md,goal-tracker.md}` plus git state.
- gates_or_invariants: The directory asserts strict state protection, session isolation, zombie-loop prevention, cancellation authorization, background-task stop short-circuiting, dependency preflight, path safety, template availability, and no direct hook execution. `tests/run-all-tests.sh:129-140` installs a dummy `codex` only when absent so dependency gates can run in CI-like environments while behavior-specific tests still supply stronger mocks.
- dependencies_and_callers: Sibling paths under test include `hooks/*.sh`, `hooks/lib/*.sh`, `hooks/check-todos-from-transcript.py`, `scripts/*.sh`, `scripts/lib/*.sh`, `prompt-template/**`, `config/**`, and command/skill files. `tests/mocks/gh:27-220` routes `gh pr`, `gh api`, and `gh repo` calls to fixtures for GitHub-related tests. `tests/setup-monitor-test-env.sh:1-28` is currently a placeholder helper with no implemented scenarios.
- edge_cases_or_failure_modes: The tree covers malformed JSON, invalid UTF-8/null-byte hook inputs, command injection, shell wrappers, redirection variants, symlinks, submodules, dirty git trees, missing dependencies, timed-out git commands, corrupted state frontmatter, stale loop directories, concurrent state access, missing templates, huge template variables, and zsh glob behavior.
- validation_or_tests: The directory is itself the validation surface. `tests/run-all-tests.sh` is the aggregate runner; individual assigned specs below cover cancellation, todo/task completion, and setup robustness in detail.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-041 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Primary RLCR Stop hook. It decides whether Claude may stop naturally, should be blocked with next-round instructions, should transition to review/finalize/methodology phases, or should terminally end the loop. It is central to this branch because it now short-circuits before all expensive gates when background work is still running.
- algorithmic_behavior: The hook reads hook JSON from stdin (`hooks/loop-codex-stop-hook.sh:28`), resolves project and loop context, extracts `session_id`, and finds the active loop with background-marker fallback (`hooks/loop-codex-stop-hook.sh:42-64`). The first substantive gate delegates to `handle_bg_task_short_circuit` (`hooks/loop-codex-stop-hook.sh:71-83`), which checks ambiguous callers, cross-session parked loops, pending background tasks, and stale same-session markers before any state parsing, git checks, summaries, or Codex calls. If no short-circuit fires, it resolves phase from `state.md`, `finalize-state.md`, or `methodology-analysis-state.md` (`hooks/loop-codex-stop-hook.sh:91-101`) and runs the normal RLCR gate chain.
- inputs_outputs_state: Inputs include hook JSON, `.humanize/rlcr` state files, transcript path, task files via the todo checker, git status/log, summary/contract/goal-tracker files, templates, BitLesson files, and Codex CLI output. Outputs are Claude hook JSON blocks, plain exit `0` allows, debug files in `XDG_CACHE_HOME` (`hooks/loop-codex-stop-hook.sh:1117-1128`), review prompts/results, next-round prompts/summaries, and state renames. State transitions include `state.md -> finalize-state.md` on passed implementation plus code review (`hooks/loop-codex-stop-hook.sh:1283-1365`), `finalize-state.md -> complete-state.md` after finalize gates (`hooks/loop-codex-stop-hook.sh:943-952`), `state.md -> stop-state.md` on STOP/drift (`hooks/loop-codex-stop-hook.sh:1914-1945`), and methodology interposition through `enter_methodology_analysis_phase` (`hooks/loop-codex-stop-hook.sh:929-946`, `hooks/lib/methodology-analysis.sh:38-99`).
- gates_or_invariants: It validates required state fields before trusting defaults (`hooks/loop-codex-stop-hook.sh:186-199`), validates numeric rounds and Codex model/effort (`hooks/loop-codex-stop-hook.sh:172-184`, `hooks/loop-codex-stop-hook.sh:201-218`), blocks outdated schemas (`hooks/loop-codex-stop-hook.sh:220-270`), enforces branch consistency and plan integrity before review (`hooks/loop-codex-stop-hook.sh:285-399`), blocks incomplete TodoWrite/Task state before Codex (`hooks/loop-codex-stop-hook.sh:401-456`), fail-closes on git status errors and large files (`hooks/loop-codex-stop-hook.sh:485-604`), requires summary/contract/BitLesson/goal-tracker readiness (`hooks/loop-codex-stop-hook.sh:746-916`), and requires a mainline verdict unless Codex emits STOP (`hooks/loop-codex-stop-hook.sh:1785-1791`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `hooks/lib/loop-bg-tasks.sh`, `hooks/lib/methodology-analysis.sh`, `scripts/portable-timeout.sh`, `hooks/check-todos-from-transcript.py`, `scripts/bitlesson-validate-delta.sh`, `jq`, `git`, and `codex`. The background helper computes pending ids from transcript launch and completion events (`hooks/lib/loop-bg-tasks.sh:97-193`) and emits the parked-loop system message while creating `bg-pending.marker` (`hooks/lib/loop-bg-tasks.sh:320-332`).
- edge_cases_or_failure_modes: Missing transcript or unreadable/malformed transcript does not falsely allow marker cleanup; helper failures fall through to normal gates (`hooks/lib/loop-bg-tasks.sh:143-148`, `hooks/lib/loop-bg-tasks.sh:356-361`). Cross-session parked loops return a system message and leave artifacts untouched (`hooks/lib/loop-bg-tasks.sh:282-293`). `review_started=true` without `.review-phase-started` is blocked as state tampering (`hooks/loop-codex-stop-hook.sh:1884-1900`). Codex failures, missing/empty review result files, no stdout from `codex review`, and absent `codex` binary all block rather than completing (`hooks/loop-codex-stop-hook.sh:1095-1115`, `hooks/loop-codex-stop-hook.sh:1551-1621`, `hooks/loop-codex-stop-hook.sh:1696-1768`).
- validation_or_tests: Directly covered by `tests/test-stop-hook-bg-allow.sh`, `tests/test-stop-hook-legacy-compat.sh`, `tests/test-finalize-phase.sh`, `tests/test-state-exit-naming.sh`, `tests/test-codex-review-merge.sh`, `tests/test-todo-checker.sh`, and multiple robustness suites. The branch-specific background criteria are enumerated in `tests/test-stop-hook-bg-allow.sh:12-34`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-071 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the cancel signal authorization contract. It pins the only Bash-mediated state mutation allowed during an active loop: an authorized move from active state file to `cancel-state.md`.
- algorithmic_behavior: The test builds isolated loop state in `setup_test_loop` (`tests/test-cancel-signal-file.sh:42-59`), sources `loop-common.sh`, and routes candidate Bash commands through `hooks/loop-bash-validator.sh` using JSON (`tests/test-cancel-signal-file.sh:61-74`). Positive cases allow `mv <loop>/state.md <loop>/cancel-state.md` only when `<loop>/.cancel-requested` exists, including `/./` path normalization and quoted paths (`tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-380`). Literal `${loop_dir}` command syntax is tested both directly and through the validator (`tests/test-cancel-signal-file.sh:450-486`).
- inputs_outputs_state: Inputs are generated `state.md`, optional `.cancel-requested`, and candidate Bash command strings. The expected output is validator exit `0` for authorized cancel or exit `2` for blocked mutations. The test itself does not perform the move; it validates the pre-tool gate that would allow or deny it.
- gates_or_invariants: No signal means block (`tests/test-cancel-signal-file.sh:121-135`). A signal in the wrong directory does not authorize (`tests/test-cancel-signal-file.sh:201-217`). Any other mutation to `state.md`, including `echo`, `sed -i`, `rm`, wrong destination, or arbitrary backup path, is blocked even with a signal (`tests/test-cancel-signal-file.sh:141-195`, `tests/test-cancel-signal-file.sh:223-297`). The implementation gate is `is_cancel_authorized`, which requires the signal, rejects substitutions/operators/extra variables, enforces exactly two args, normalizes paths, and validates source/destination (`hooks/lib/loop-common.sh:1020-1200`).
- dependencies_and_callers: Calls `hooks/loop-bash-validator.sh`, which checks active loop state, strict state parsing, and state-file command patterns (`hooks/loop-bash-validator.sh:191-279`, `hooks/loop-bash-validator.sh:292-470`). It also exercises `is_cancel_authorized` directly in helper tests (`tests/test-cancel-signal-file.sh:1252-1343`).
- edge_cases_or_failure_modes: Covers command substitution `$()` and backticks, newline injection, hidden `${ifs}`, `sudo`/`env`/`command` prefixes, leading whitespace, semicolon/and/or/pipe/background operators, `sh -c` and `bash -c` wrappers, options and extra args, unrelated `/tmp/state.md`, and redirection-prefixed variants including quoted, escaped-space, and ANSI-C quoted targets (`tests/test-cancel-signal-file.sh:243-357`, `tests/test-cancel-signal-file.sh:386-607`, `tests/test-cancel-signal-file.sh:628-1227`). No active loop allows commands because RLCR protection is not in scope (`tests/test-cancel-signal-file.sh:1230-1249`).
- validation_or_tests: Self-contained test exits with the number of failures (`tests/test-cancel-signal-file.sh:1345-1357`) and is included in the aggregate runner (`tests/run-all-tests.sh:77`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-101 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook pre-Codex gate that prevents exit while blocking/mainline tasks remain incomplete.
- algorithmic_behavior: The test drives the Python checker with crafted hook JSON on stdin. It verifies parse behavior, legacy TodoWrite transcript parsing, direct/newer transcript formats, malformed JSONL tolerance, and file-based Claude Task state. The implementation treats prefixed lane tags `mainline`, `blocking`, and `queued` specially, defaulting to blocking for safety (`hooks/check-todos-from-transcript.py:24-35`).
- inputs_outputs_state: Inputs are `transcript_path`, `session_id`, and optional `tasks_base_dir` in hook JSON. Legacy TodoWrite state comes from the latest TodoWrite call in transcript JSONL (`hooks/check-todos-from-transcript.py:73-120`). New Task state comes from `~/.claude/tasks/<session_id>/*.json` or test override (`hooks/check-todos-from-transcript.py:123-170`). Outputs are exit `0` for no blocking incomplete tasks, exit `1` plus `INCOMPLETE_TODOS` details for blockers, and exit `2` for invalid hook JSON (`hooks/check-todos-from-transcript.py:173-221`).
- gates_or_invariants: Empty input, missing transcript path, and nonexistent transcript are non-blocking (`tests/test-todo-checker.sh:52-98`). Pending or in-progress TodoWrite items block; completed items pass (`tests/test-todo-checker.sh:107-158`). A leading `queued` lane marker does not block, but an inline mention of `queued` does not downgrade a blocker (`tests/test-todo-checker.sh:160-188`). File tasks with status other than `completed` or `deleted` block unless leading `queued` (`tests/test-todo-checker.sh:339-439`, `hooks/check-todos-from-transcript.py:149-165`).
- dependencies_and_callers: Called by the stop hook before git/summary/Codex work (`hooks/loop-codex-stop-hook.sh:408-456`). Depends only on Python standard library. The test uses temp transcript and task directories, no external service.
- edge_cases_or_failure_modes: Invalid JSON input exits `2`; invalid JSONL lines inside transcript are ignored (`tests/test-todo-checker.sh:52-62`, `tests/test-todo-checker.sh:210-225`). Multiple TodoWrite calls use the latest todo list (`tests/test-todo-checker.sh:227-241`). Direct `tool_use` and alternate `message` formats are supported (`tests/test-todo-checker.sh:243-271`). Missing status is incomplete; empty content still blocks; deleted file-based tasks are ignored; mixed legacy and file-based tasks are combined (`tests/test-todo-checker.sh:280-324`, `tests/test-todo-checker.sh:481-524`).
- validation_or_tests: The test exits `0` only when all assertions pass (`tests/test-todo-checker.sh:538-556`) and is listed in the aggregate runner (`tests/run-all-tests.sh:63`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-131 `file` `prompt-template/block/methodology-analysis-state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking message template for attempted modification of `methodology-analysis-state.md`, the state file used after RLCR has passed or stopped but before final terminal state is written.
- algorithmic_behavior: The template tells the agent that `methodology-analysis-state.md` is loop-managed and redirects work to the methodology analysis workflow: spawn an Opus agent, review sanitized report, optionally help file a GitHub issue, and write `methodology-analysis-done.md` (`prompt-template/block/methodology-analysis-state-file-modification.md:1-9`).
- inputs_outputs_state: No variables are required by this template. Input is an attempted write/edit/Bash mutation detected by validators. Output is markdown reason text loaded into a hook error/block response. It does not perform state transitions; it preserves the state machine invariant that only loop code can rename or complete this phase.
- gates_or_invariants: `methodology_analysis_state_file_blocked_message` loads this template with a fallback (`hooks/lib/loop-common.sh:999-1005`). `loop-bash-validator.sh` blocks destination and source modifications of `methodology-analysis-state.md` unless the exact cancel authorization path is valid (`hooks/loop-bash-validator.sh:253-259`, `hooks/loop-bash-validator.sh:410-418`, `hooks/loop-bash-validator.sh:443-451`).
- dependencies_and_callers: Used by `hooks/lib/loop-common.sh` and validators. It aligns with `hooks/lib/methodology-analysis.sh`, where entering analysis renames the active state to `methodology-analysis-state.md` and creates `methodology-analysis-done.md` (`hooks/lib/methodology-analysis.sh:64-73`), while completion requires non-empty done and report files before renaming to terminal state (`hooks/lib/methodology-analysis.sh:114-173`).
- edge_cases_or_failure_modes: Template absence is tolerated by fallback text in `loop-common.sh`, but a missing or empty template would reduce guidance quality. It is intentionally narrow: it blocks state-file mutation, not read-only inspection or the allowed cancel path.
- validation_or_tests: Referenced by template validation suites through template loader/reference coverage; methodology state protection is exercised by hook/finalize/state robustness tests and Bash validator logic.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-161 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Claude-facing prompt fragment appended after Full Goal Alignment Check rounds, turning Codex alignment findings into next-round prioritization constraints.
- algorithmic_behavior: It adds a `Post-Alignment Check Action Items` section telling the next round to address forgotten items, unmet acceptance criteria, unjustified deferrals, and to keep non-blocking queued issues queued unless they now block mainline progress (`prompt-template/claude/post-alignment-action-items.md:2-8`).
- inputs_outputs_state: No variables are required. Input condition is `FULL_ALIGNMENT_CHECK=true`, computed from `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` (`hooks/loop-codex-stop-hook.sh:977-987`). Output is appended markdown in the next-round prompt (`hooks/loop-codex-stop-hook.sh:2114-2120`). It does not modify state directly.
- gates_or_invariants: Reinforces the mainline/queued separation after alignment review. The invariant is behavioral: unmet ACs and forgotten blocking work outrank cleanup, while queued issues remain non-blocking unless their status changes.
- dependencies_and_callers: Loaded by `load_template` from the stop hook (`hooks/loop-codex-stop-hook.sh:2114-2119`) after the next prompt is rendered and before footer/task-routing additions (`hooks/loop-codex-stop-hook.sh:2122-2127`).
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook silently skips appending it because this path uses `load_template` and checks non-empty rather than a fallback (`hooks/loop-codex-stop-hook.sh:2115-2119`). Invalid `full_review_round` values are normalized to 5 earlier (`hooks/loop-codex-stop-hook.sh:979-983`).
- validation_or_tests: Covered by template reference/comprehensive suites and indirectly by stop-hook prompt generation tests, including task-tag/commit-history/full-alignment related prompt checks.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-191 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for `scripts/setup-rlcr-loop.sh`, covering initialization gates that create valid RLCR state and prevent unsafe or ambiguous loop starts.
- algorithmic_behavior: The test creates minimal plans, temp git repositories, and mock runtime bins (`tests/robustness/test-setup-scripts-robustness.sh:31-93`). It drives setup through argument parsing, prerequisite checks, plan validation, git validation, YAML safety, mutual exclusion, symlink protection, timeout handling, full-review-round validation, skip-impl initialization, and dependency reporting.
- inputs_outputs_state: Inputs are CLI flags to `setup-rlcr-loop.sh`, plan files, git repos with controlled status, mock `codex`, mock `jq`, and mock `timeout/gtimeout`. Outputs are setup exit codes, stderr messages, and created RLCR artifacts such as `state.md`, `.review-phase-started`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, and `round-0-prompt.md`.
- gates_or_invariants: The test pins parser rejection for missing/invalid `--max`, `--codex-timeout`, `--codex-model`, unknown options, and duplicate plan inputs (`tests/robustness/test-setup-scripts-robustness.sh:102-220`), matching setup parser checks (`scripts/setup-rlcr-loop.sh:188-320`). It asserts dependency preflight lists `codex`, `jq`, and `git` together (`scripts/setup-rlcr-loop.sh:331-359`). It validates relative path only, no spaces, no shell metacharacters, no symlink file or symlink parent, project-contained path, submodule exclusion, clean/tracked-plan rules, and meaningful content (`scripts/setup-rlcr-loop.sh:441-667`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `tests/test-helpers.sh`, git, and generated mocks. It verifies setup behavior that later stop hooks depend on: state fields written at `scripts/setup-rlcr-loop.sh:876-903`, pending session-id signal at `scripts/setup-rlcr-loop.sh:905-914`, base branch/commit capture at `scripts/setup-rlcr-loop.sh:740-811`, and initial prompt/summary/contract scaffolding around `scripts/setup-rlcr-loop.sh:1191-1504`.
- edge_cases_or_failure_modes: Covers comment-only and too-short plans, absolute/metacharacter/space paths, YAML-unsafe branch/model names, non-git repos, repos without commits, dirty `.humanizeconfig`, tracked plans without `--track-plan-file`, active loop mutual exclusion, plan symlinks, parent symlinks, git timeout simulation, timeout value `0` and very large values, `--full-review-round` min/non-numeric/missing, missing dependencies, and environment-dependent skips when a system binary cannot be hidden (`tests/robustness/test-setup-scripts-robustness.sh:230-1192`).
- validation_or_tests: The script ends through shared `print_test_summary` and exits with its result (`tests/robustness/test-setup-scripts-robustness.sh:1187-1192`). It is included in `tests/run-all-tests.sh:113`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item-evidence headings above; each assigned item_id appears exactly once in Item Evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`