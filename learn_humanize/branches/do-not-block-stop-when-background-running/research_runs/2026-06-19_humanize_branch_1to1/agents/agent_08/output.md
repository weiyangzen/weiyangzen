# agent_08 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-008 `directory` `scripts`
- cursor: `[_]`
- core_role: Operator/runtime command layer for Humanize RLCR and supporting skills. It bootstraps loop state, wraps the stop hook for non-hook callers, validates plan/refine IO, runs one-shot model consultations, manages BitLesson selection/delta gates, installs runtime hooks/skills, and renders terminal/status monitoring.
- algorithmic_behavior: Recursively inspected `scripts` and `scripts/lib`: 20 files total. The central RLCR bootstrap is `scripts/setup-rlcr-loop.sh`, which validates dependencies (`codex`, `jq`, `git`) at `scripts/setup-rlcr-loop.sh:336`, rejects concurrent active loops at `scripts/setup-rlcr-loop.sh:365`, validates plan path/content at `scripts/setup-rlcr-loop.sh:451` through `scripts/setup-rlcr-loop.sh:667`, records branch/base commit at `scripts/setup-rlcr-loop.sh:682` through `scripts/setup-rlcr-loop.sh:811`, then writes `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, round contract, summary scaffold, and initial prompt at `scripts/setup-rlcr-loop.sh:817` through `scripts/setup-rlcr-loop.sh:1537`. The branch-specific non-blocking stop behavior is reached from this directory through `scripts/rlcr-stop-gate.sh`, which builds Stop-hook JSON and maps hook JSON without `decision` to `ALLOW` exit 0 at `scripts/rlcr-stop-gate.sh:95` through `scripts/rlcr-stop-gate.sh:160`.
- inputs_outputs_state: Inputs include CLI flags (`--plan-file`, `--skip-impl`, `--codex-model`, `--push-every-round`, `--base-branch`), project git state, config JSON, prompt templates, Claude hook JSON, model CLI output, and BitLesson markdown. Outputs include `.humanize/rlcr/<timestamp>/state.md`, prompt/summary/contract/goal files, `.humanize/.pending-session-id`, `.humanize/skill/<id>` skill records, cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, installer writes under target skill/config dirs, and terminal/statusline output.
- gates_or_invariants: `setup-rlcr-loop.sh` requires relative plan paths, no spaces, no shell metacharacters, no symlink file/parent directory traversal, in-project path, not in submodule, adequate content, clean git worktree, YAML-safe branch/model values, valid `codex_effort`, and a local base branch (`scripts/setup-rlcr-loop.sh:451`, `scripts/setup-rlcr-loop.sh:490`, `scripts/setup-rlcr-loop.sh:558`, `scripts/setup-rlcr-loop.sh:624`, `scripts/setup-rlcr-loop.sh:688`, `scripts/setup-rlcr-loop.sh:715`, `scripts/setup-rlcr-loop.sh:744`). `rlcr-stop-gate.sh` requires executable stop hook and `jq`, treats hook script failure/non-JSON/unexpected decisions as wrapper errors, and converts hook `block` to exit 10 (`scripts/rlcr-stop-gate.sh:76`, `scripts/rlcr-stop-gate.sh:117`, `scripts/rlcr-stop-gate.sh:129`, `scripts/rlcr-stop-gate.sh:139`). `portable-timeout.sh` normalizes timeouts with `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:33`).
- dependencies_and_callers: Depends heavily on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-bg-tasks.sh`, `hooks/loop-codex-stop-hook.sh`, `prompt-template/**`, `templates/bitlesson.md`, `config/default_config.json`, `codex`, `gemini`, `claude`, `jq`, `git`, `awk`, `sed`, `perl`, `python3`, and optional `rsync`. `scripts/lib/config-loader.sh` merges default/user/project JSON configs using `jq` with null stripping at `scripts/lib/config-loader.sh:63` through `scripts/lib/config-loader.sh:137`. `scripts/lib/model-router.sh` maps model names to `codex` or `claude` providers and maps unsupported `xhigh` to `high` for Claude at `scripts/lib/model-router.sh:10` and `scripts/lib/model-router.sh:62`.
- edge_cases_or_failure_modes: Plan validation has explicit failures for absolute paths, metacharacters, parent symlinks, unreadable files, submodules, dirty tracked/untracked work, missing local base branches, invalid model/effort, and insufficient plan content. `cancel-rlcr-loop.sh` is intentionally global, not session-filtered, and renames active state to `cancel-state.md`; it refuses finalize cancellation without `--force` (`scripts/cancel-rlcr-loop.sh:76`, `scripts/cancel-rlcr-loop.sh:135`, `scripts/cancel-rlcr-loop.sh:152`). `ask-codex.sh` and `ask-gemini.sh` record timeout/error/empty_response metadata and return nonzero on invalid outputs (`scripts/ask-codex.sh:309`, `scripts/ask-gemini.sh:291`). `install-skill.sh` uses temp-copy fallback when `rsync` is unavailable to avoid half-destroyed targets (`scripts/install-skill.sh:120` through `scripts/install-skill.sh:145`). `statusline.sh` is mostly reference UI, with only the RLCR field tied to this repo (`scripts/statusline.sh:7` through `scripts/statusline.sh:14`).
- validation_or_tests: Relevant executable specs include assigned `tests/robustness/test-path-validation-robustness.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-template-loader.sh`, plus non-assigned `tests/test-stop-hook-bg-allow.sh`, which directly exercises the background-stop allow path. I did not execute tests because this branch export is read-only and the test suites create temp repos/files.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-038 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-gate helper that determines whether Claude still has incomplete blocking/mainline work before allowing loop exit. It supports legacy `TodoWrite` transcript state and the newer Claude task directory state.
- algorithmic_behavior: Parses hook input JSON from stdin, then checks `~/.claude/tasks/<session_id>/*.json` and/or a `transcript_path`. `extract_tool_calls_from_entry()` recognizes assistant/message content blocks and direct `tool_use` entries (`hooks/check-todos-from-transcript.py:38` through `hooks/check-todos-from-transcript.py:70`). `find_incomplete_todos_from_transcript()` keeps only the latest TodoWrite list from JSONL transcript entries, then emits items whose status is not `completed` and whose lane is not `queued` (`hooks/check-todos-from-transcript.py:73` through `hooks/check-todos-from-transcript.py:120`). `find_incomplete_tasks_from_directory()` reads task JSON files and treats any status other than `completed` or `deleted` as incomplete, again skipping `queued` lane tasks (`hooks/check-todos-from-transcript.py:123` through `hooks/check-todos-from-transcript.py:170`).
- inputs_outputs_state: Input is stdin JSON with optional `session_id`, `tasks_base_dir`, and `transcript_path` (`hooks/check-todos-from-transcript.py:173` through `hooks/check-todos-from-transcript.py:198`). Output is exit 0 when no incomplete work is found, exit 1 with stdout marker `INCOMPLETE_TODOS` plus formatted task/todo lines, or exit 2 on invalid hook input JSON (`hooks/check-todos-from-transcript.py:200` through `hooks/check-todos-from-transcript.py:221`). Internal state is transient except for reading the live Claude task directory.
- gates_or_invariants: Lane prefixes are restricted to `[mainline]`, `[blocking]`, and `[queued]`; default lane is `blocking` for safety (`hooks/check-todos-from-transcript.py:24` through `hooks/check-todos-from-transcript.py:35`). `queued` tasks are explicitly non-blocking and ignored. Malformed transcript lines and malformed task JSON files are skipped rather than fatal after the top-level hook input parses.
- dependencies_and_callers: Python stdlib only: `json`, `re`, `sys`, `Path`, and typing. Used by stop-hook style todo enforcement; the comments identify Claude Code TodoWrite transcript parsing and `~/.claude/tasks/<session_id>/` as supported state sources (`hooks/check-todos-from-transcript.py:3` through `hooks/check-todos-from-transcript.py:15`).
- edge_cases_or_failure_modes: Missing transcript or missing tasks directory returns no incomplete items, so absence of task state is allow-by-default (`hooks/check-todos-from-transcript.py:79`, `hooks/check-todos-from-transcript.py:140`). Multiple sources are concatenated without deduplication, so a still-present task and TodoWrite item could both surface. Defaulting untagged tasks to `blocking` prevents untagged unfinished work from being silently ignored.
- validation_or_tests: Covered by non-assigned todo checker tests in the tree and by stop-hook behavior that consumes `INCOMPLETE_TODOS`. The assigned evidence is direct source inspection only.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-068 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `command_modifies_file()` in `hooks/lib/loop-common.sh`, which feeds RLCR Bash validators that block shell-based mutation of protected loop artifacts.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, lowercases candidate commands via `to_lower`, then asserts whether `command_modifies_file "$command_lower" "$pattern"` detects a file modification (`tests/test-bash-validator-patterns.sh:11` through `tests/test-bash-validator-patterns.sh:62`). The tested production patterns are defined in `hooks/lib/loop-common.sh:1435` through `hooks/lib/loop-common.sh:1460`.
- inputs_outputs_state: Inputs are literal shell command strings and target regex patterns, defaulting to `goal-tracker\.md`. Outputs are colored pass/fail lines and process exit 0 if `TESTS_FAILED == 0`, else exit 1 (`tests/test-bash-validator-patterns.sh:20` through `tests/test-bash-validator-patterns.sh:34`, `tests/test-bash-validator-patterns.sh:197` through `tests/test-bash-validator-patterns.sh:214`).
- gates_or_invariants: Commands that must be detected include redirections, `tee`, in-place `sed`/`awk`/`perl`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec N>` (`tests/test-bash-validator-patterns.sh:69` through `tests/test-bash-validator-patterns.sh:134`). Commands that must not be detected include read-only `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `file`, `stat`, and `diff` (`tests/test-bash-validator-patterns.sh:136` through `tests/test-bash-validator-patterns.sh:153`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, especially `to_lower()` at `hooks/lib/loop-common.sh:777` and `command_modifies_file()` at `hooks/lib/loop-common.sh:1435`. Production caller examples include `hooks/loop-bash-validator.sh`, which blocks state, plan backup, goal tracker, prompt, summary, contract, and todo file mutations using the same helper (`hooks/loop-bash-validator.sh:253`, `hooks/loop-bash-validator.sh:478`, `hooks/loop-bash-validator.sh:491`, `hooks/loop-bash-validator.sh:506`, `hooks/loop-bash-validator.sh:516`, `hooks/loop-bash-validator.sh:528`).
- edge_cases_or_failure_modes: The test documents a known limitation: `cp file1.md file2.md goal-tracker.md` is not detected because the regex expects simple `cp src dest` (`tests/test-bash-validator-patterns.sh:165` through `tests/test-bash-validator-patterns.sh:168`). Variables are not resolved, so `echo x > $FILE` is intentionally not matched (`tests/test-bash-validator-patterns.sh:170` through `tests/test-bash-validator-patterns.sh:172`). Detection is pattern-based, not a full shell parser.
- validation_or_tests: This file is itself the validation. It covers generic protected-file matching plus state and summary patterns (`tests/test-bash-validator-patterns.sh:175` through `tests/test-bash-validator-patterns.sh:194`). Not executed due read-only instruction.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-098 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template loading/rendering engine used by hooks and scripts to generate block reasons and next-round prompts.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, then verifies template directory resolution, file loading, variable rendering, load-and-render integration, safe fallback, template-dir validation, shell-metacharacter literal handling, placeholder-injection prevention, empty substitutions, Unicode, underscore-prefixed variables, and numeric variable names (`tests/test-template-loader.sh:10` through `tests/test-template-loader.sh:659`).
- inputs_outputs_state: Inputs are synthetic template strings, real template names, fallback strings, and `VAR=value` substitution pairs. Outputs are pass/fail lines and exit 0/1 based on `TESTS_FAILED` (`tests/test-template-loader.sh:20` through `tests/test-template-loader.sh:34`, `tests/test-template-loader.sh:641` through `tests/test-template-loader.sh:659`). No persistent state.
- gates_or_invariants: The production renderer is intentionally single-pass: `{{VAR}}` inside a substituted value is not expanded later (`hooks/lib/template-loader.sh:48` through `hooks/lib/template-loader.sh:56`). Tests enforce literal rendering of `&`, backslash, `$`, backticks, pipes, semicolons, glob syntax, parentheses, redirections, quotes, hash, tilde, JSON-like content, and multiline values (`tests/test-template-loader.sh:247` through `tests/test-template-loader.sh:486`). Placeholder-injection tests ensure review text containing `{{GOAL_TRACKER_FILE}}` remains literal rather than corrupting prompts (`tests/test-template-loader.sh:488` through `tests/test-template-loader.sh:543`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, whose key functions are `get_template_dir()` (`hooks/lib/template-loader.sh:26`), `load_template()` (`hooks/lib/template-loader.sh:36`), `render_template()` (`hooks/lib/template-loader.sh:56`), `load_and_render_safe()` (`hooks/lib/template-loader.sh:188`), and `validate_template_dir()` (`hooks/lib/template-loader.sh:216`). Callers include stop hook block templates, BitLesson delta validation, loop-common block messages, and next-round footer rendering.
- edge_cases_or_failure_modes: Missing templates return empty plus stderr warning in `load_template()` but `load_and_render_safe()` falls back (`hooks/lib/template-loader.sh:41` through `hooks/lib/template-loader.sh:45`, `hooks/lib/template-loader.sh:194` through `hooks/lib/template-loader.sh:210`). Missing variables stay unreplaced (`tests/test-template-loader.sh:181` through `tests/test-template-loader.sh:194`). The renderer relies on environment variables prefixed `TMPL_VAR_`, so tests cover unsafe shell-looking values to ensure no command interpretation.
- validation_or_tests: This file is the validation suite for template safety. Not executed because running it would be outside the requested read-only research-only mode.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-128 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block-reason template for the large-file gate that prevents exiting the loop when staged/changed code or documentation files exceed the configured line threshold.
- algorithmic_behavior: Template receives `{{MAX_LINES}}` and `{{LARGE_FILES}}`, explains why oversized files are blocked, and gives separate remediation instructions for code and documentation (`prompt-template/block/large-files.md:1` through `prompt-template/block/large-files.md:25`). The stop hook loads this template when it has accumulated oversized files at `hooks/loop-codex-stop-hook.sh:582` through `hooks/loop-codex-stop-hook.sh:603`.
- inputs_outputs_state: Inputs are rendered variables from the stop hook: maximum allowed line count and bullet list of offending files. Output is a Markdown reason string embedded in hook JSON as `.reason`, with `.decision = "block"` and system message `Loop: Blocked - large files detected...` (`hooks/loop-codex-stop-hook.sh:590` through `hooks/loop-codex-stop-hook.sh:601`). No file state is mutated by the template.
- gates_or_invariants: It requires code files to be split into modules under `MAX_LINES` without behavior change, documentation files to be split into coherent sections, and then changes committed before another exit attempt (`prompt-template/block/large-files.md:11` through `prompt-template/block/large-files.md:25`). It also optionally invokes `code-simplifier` after code splitting (`prompt-template/block/large-files.md:16`).
- dependencies_and_callers: Rendered by `load_and_render_safe()` from `hooks/lib/template-loader.sh`. Caller is `hooks/loop-codex-stop-hook.sh` large file check, which classifies code/documentation extensions, counts lines with `wc -l`, and records each file in `LARGE_FILES` (`hooks/loop-codex-stop-hook.sh:560` through `hooks/loop-codex-stop-hook.sh:579`).
- edge_cases_or_failure_modes: If the template is missing, stop hook uses an inline fallback with the same variables (`hooks/loop-codex-stop-hook.sh:582` through `hooks/loop-codex-stop-hook.sh:589`). The template assumes splitting is always the correct remedy; unusual generated or vendored files would rely on the upstream gate’s file selection rather than template nuance.
- validation_or_tests: Template rendering safety is covered by `tests/test-template-loader.sh`; large-file behavior is tied to stop-hook tests outside this assigned set. Direct validation here is source inspection.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-158 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Next-round prompt footer that preserves RLCR loop integrity by instructing Claude not to fake exit/cancel state and to commit plus write the next summary before stopping.
- algorithmic_behavior: The template is appended when the stop hook generates the next round prompt. It inserts `{{NEXT_SUMMARY_FILE}}` into the final summary instruction (`prompt-template/claude/next-round-footer.md:1` through `prompt-template/claude/next-round-footer.md:9`). The call site is `hooks/loop-codex-stop-hook.sh:2122` through `hooks/loop-codex-stop-hook.sh:2127`.
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`; output is Markdown appended to `round-N-prompt.md`. It does not mutate state directly but shapes the next state transition by requiring a commit and summary before another stop attempt.
- gates_or_invariants: Explicit anti-bypass invariant: do not exit by lying, editing loop state files, or executing `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:4`). Operational invariant: after work, optionally use `code-simplifier`, commit changes, and write work summary to the rendered next summary path (`prompt-template/claude/next-round-footer.md:6` through `prompt-template/claude/next-round-footer.md:9`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` rendering semantics. Caller is the stop hook’s next-round prompt assembly path, followed by task-tag routing notes, optional push note, goal tracker update request, and optional agent-team continuation instructions (`hooks/loop-codex-stop-hook.sh:2125` through `hooks/loop-codex-stop-hook.sh:2145`).
- edge_cases_or_failure_modes: If missing, the fallback is much shorter and only says to commit and write the summary (`hooks/loop-codex-stop-hook.sh:2122` through `hooks/loop-codex-stop-hook.sh:2125`), so the explicit anti-bypass and code-simplifier instructions would be weaker. Rendering safety for the summary path depends on single-pass template rendering.
- validation_or_tests: Covered indirectly by template loader tests and stop-hook next-round prompt generation tests. Direct validation here is source inspection.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-188 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for plan path and content validation in `scripts/setup-rlcr-loop.sh`, protecting the RLCR bootstrap from path traversal, shell injection, symlink escape, and low-signal plan inputs.
- algorithmic_behavior: Creates an isolated test git repo, mock `codex`, and valid plan helper, then invokes the production setup script with varied plan paths. `test_path_validation()` deletes prior loop state, runs `scripts/setup-rlcr-loop.sh`, and interprets known validation errors as rejection while treating downstream `codex`/gitignore failures as proof that path validation passed (`tests/robustness/test-path-validation-robustness.sh:16` through `tests/robustness/test-path-validation-robustness.sh:113`).
- inputs_outputs_state: Inputs are synthetic plan file paths and content fixtures. Outputs are pass/fail lines through `tests/test-helpers.sh` and final `print_test_summary` exit code (`tests/robustness/test-path-validation-robustness.sh:482` through `tests/robustness/test-path-validation-robustness.sh:487`). If executed, it writes temp repos, git commits, symlinks, and plan files under the test temp dir.
- gates_or_invariants: Positive cases: normal relative path, simple root filename, dash/underscore, nested directories, dots in filename, long filenames if filesystem allows, and 10-level nested path (`tests/robustness/test-path-validation-robustness.sh:122` through `tests/robustness/test-path-validation-robustness.sh:169`, `tests/robustness/test-path-validation-robustness.sh:387` through `tests/robustness/test-path-validation-robustness.sh:412`). Negative cases: absolute paths, spaces, semicolon, pipe, dollar, backticks, angle brackets, ampersand, glob characters, backslash, tilde, parentheses, braces, brackets, hash, exclamation, symlink file, symlink parent directory, symlink chain, empty file, comments-only file, fewer than five lines, nonexistent file, and directory path (`tests/robustness/test-path-validation-robustness.sh:179` through `tests/robustness/test-path-validation-robustness.sh:480`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `git`, `jq` indirectly through setup, and production `scripts/setup-rlcr-loop.sh`. The tested production checks are located at `scripts/setup-rlcr-loop.sh:451` through `scripts/setup-rlcr-loop.sh:537` for path/symlink/project validation and `scripts/setup-rlcr-loop.sh:611` through `scripts/setup-rlcr-loop.sh:667` for plan content validation.
- edge_cases_or_failure_modes: The suite notes Unicode/CJK/emoji paths are allowed (`tests/robustness/test-path-validation-robustness.sh:414` through `tests/robustness/test-path-validation-robustness.sh:416`). `test_path_validation()` pattern-matches error text, so if production error strings change without behavior change, the test can misclassify. It also treats gitignore/tracking errors as validation pass, isolating path/content validation from later bootstrap gates (`tests/robustness/test-path-validation-robustness.sh:97` through `tests/robustness/test-path-validation-robustness.sh:105`).
- validation_or_tests: This file is the validation suite for setup path robustness. It was not executed because the assignment explicitly says not to modify files and the test creates repositories/files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item evidence sections above; each assigned row is represented once as its own section.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`