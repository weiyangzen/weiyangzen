# agent_25 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-025 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness suite for the RLCR/Humanize control plane. It stress-tests state parsing, active-loop discovery, hook input validation, read/write/bash guard behavior, setup argument validation, plan/path validation, template rendering, timeout portability, git status parsing, monitor behavior, and cancellation authorization. It is not runtime code, but it is executable specification for core algorithm invariants.
- algorithmic_behavior: The directory contains 16 shell tests:
  - `tests/robustness/test-base-branch-detection.sh`: exercises setup-time base branch selection when no origin exists, local `main`/`master` fallback, missing default branch behavior, and origin default branch detection at lines 5, 37, 67, 96, 125, and 152.
  - `tests/robustness/test-cancel-security-robustness.sh`: verifies strict authorized cancel command recognition and path bypass prevention. It covers valid signal-file moves, `finalize-state.md` cancellation, quote variants, missing signal file, command substitution/backticks/semicolon/pipe/AND/OR/newline/IFS/variable-expansion injection, wrong source/destination, extra args, `/./` normalization, mixed quotes, symlink source files, and paths containing `finalize` at lines 5, 39, 51, 99, 109, 122, 135, 148, 161, 174, 187, 200, 213, 226, 239, 252, 265, 278, 291, 304, 317, 332, 353, 369, and 390.
  - `tests/robustness/test-concurrent-state-robustness.sh`: models concurrent and partial state-file reads, timestamped loop selection, finalize/cancel handling, malformed YAML, unicode, very large/zero rounds, and permission cases. It sources `tests/test-helpers.sh` and later `hooks/lib/loop-common.sh`; key cases are at lines 5, 30, 59, 100, 170, 203, 250, 279, 332, 353, 373, 397, 413, 439, 456, and 474.
  - `tests/robustness/test-git-operations-robustness.sh`: tests production `humanize_parse_git_status` and `humanize_detect_git_state` from `scripts/humanize.sh`, including clean/dirty repos, untracked/staged/modified/deleted/renamed/binary files, detached HEAD, empty/non-git repos, rebase, merge, shallow clone, and permission handling at lines 5, 19, 36, 50, 71, 180, 233, 265, 316, 331, 377, 388, 403, 434, 450, 466, and 478.
  - `tests/robustness/test-goal-tracker-robustness.sh`: tests production `humanize_parse_goal_tracker` and issue-count parsing. It covers AC list/table formats, active/completed/deferred/open issue counts, missing/empty/truncated/binary content, malformed tables, new blocking/queued issue schema, legacy fallback, mixed formats, and long goal summaries at lines 5, 22, 39, 53, 70, 99, 128, 165, 204, 214, 247, 256, 268, 291, 314, 338, 359, 383, 421, 451, 487, 500, 536, and 560.
  - `tests/robustness/test-hook-input-robustness.sh`: pipes JSON into production validators and monitors. It checks valid Read/Write/Bash input, invalid/missing/deep/binary/null JSON, long commands, Unicode, unrecognized tools, terminal width/log handling, file modification detection, production goal/git parser availability, and monitor graceful exit/narrow terminal/ANSI logs at lines 5, 16, 35, 47, 59, 71, 87, 102, 117, 149, 177, 201, 219, 245, 328, 351, 381, 410, 424, 444, 488, and 580.
  - `tests/robustness/test-hook-system-robustness.sh`: tests hook validators beyond raw input parsing. It covers edit JSON acceptance/rejection, state and goal-tracker protection, malformed/missing paths, path traversal inside `.humanize`, plan file validation, mutable vs immutable goal-tracker edits after round 0, old-session goal tracker reads, bash validator behavior, stop-hook missing/corrupted/valid state transitions, and large/null/deep JSON at lines 5, 21, 39, 52, 79, 108, 123, 164, 316, 402, 418, 464, 496, 510, 538, 552, 604, 644, 660, 683, 726, 790, 804, and 818.
  - `tests/robustness/test-path-validation-robustness.sh`: executes `scripts/setup-rlcr-loop.sh` with plan paths to test accepted relative forms and rejected absolute paths, spaces, shell metacharacters, globs, symlinks, long/deep paths, empty/comment-only/insufficient/nonexistent/directory plan files at lines 5, 22, 50, 78, 122, 179, 188, 199, 208, 217, 226, 235, 244, 253, 262, 271, 280, 289, 298, 307, 316, 325, 342, 352, 367, 387, 402, 425, 434, 450, 463, and 472.
  - `tests/robustness/test-plan-file-robustness.sh`: tests production plan validation by running setup, with helper `test_plan_validation`, mock codex setup, content-line counting, valid markdown, comments, large files, CRLF, binary/null content, long lines, HTML comments, nonexistent/unreadable/symlink/directory/disappearing files at lines 5, 27, 48, 130, 178, 206, 234, 253, 300, 309, 325, 355, 368, 390, 409, 432, 444, 468, 477, 491, 503, 514, and 527.
  - `tests/robustness/test-session-robustness.sh`: tests active-loop session discovery under many timestamped directories, finalize precedence, zombie-loop protection, empty/missing bases, no state files, unexpected names, symlink directories, special characters, deep paths, rapid creation, complete/cancel terminal states, and Unicode names at lines 5, 33, 48, 63, 89, 105, 128, 140, 150, 164, 179, 194, 210, 224, 240, 259, 273, 287, and 304.
  - `tests/robustness/test-setup-scripts-robustness.sh`: broad setup script edge-case suite. It covers CLI parsing, `--max`, `--codex-timeout`, `--codex-model`, plan validation, path/symlink rejection, branch/model validation, dirty/non-git/no-commit guards, existing loop blocking, valid setup, timeout simulation, portable git path handling, `--full-review-round`, `--skip-impl`, scaffold generation, and dependency checks for codex/jq at lines 5, 31, 51, 67, 84, 102, 111, 124, 135, 172, 183, 194, 205, 230, 260, 286, 309, 329, 357, 388, 418, 431, 454, 473, 502, 540, 567, 603, 629, 657, 675, 695, 752, 770, 788, 807, 825, 843, 869, 885, 916, 929, 943, 959, 972, 985, 1059, 1088, 1118, and 1160.
  - `tests/robustness/test-state-file-robustness.sh`: tests `parse_state_file` and `parse_state_file_strict`, including required fields, quoted values, defaults, missing frontmatter, non-numeric/negative/large rounds, missing fields, malformed YAML, binary/CRLF, `full_review_round`, and drift fields at lines 5, 16, 33, 100, 149, 163, 173, 190, 201, 217, 230, 242, 259, 277, 297, 314, 338, 358, 367, 386, 399, 425, 450, 476, and 502.
  - `tests/robustness/test-state-transition-robustness.sh`: tests valid/invalid state transitions and active-loop selection: round 0, round progression, max round, finalize precedence, cancel inactivity, newer loop after cancel, invalid negative/excess/non-numeric/missing fields, newest directory selection, lexicographic ordering, and ignoring invalid nested state at lines 5, 30, 54, 74, 98, 114, 127, 148, 160, 182, 194, 217, 238, 259, 292, 315, 339, 371, 385, and 404.
  - `tests/robustness/test-template-error-robustness.sh`: tests `hooks/lib/template-loader.sh` under missing dirs/files, empty templates, malformed placeholders, unusual variable names/values, BOM, whitespace-only files, special filenames, permission denied, concurrent loads, subdirectories, and symlinked templates at lines 5, 17, 34, 43, 53, 73, 84, 95, 106, 125, 135, 146, 165, 176, 188, 208, 220, 228, 245, 270, 309, and 320.
  - `tests/robustness/test-template-stress-robustness.sh`: tests template renderer stress behavior: normal substitution, fallback values, 10KB/100KB values, 100KB templates, many variables, regex/sed-special values, newlines, empty values, placeholder-in-value injection prevention, Unicode, dollar signs, repeated variables, and placeholder-only content at lines 5, 16, 33, 44, 54, 82, 96, 111, 134, 161, 172, 183, 195, 207, 218, 230, 241, 252, 263, and 274.
  - `tests/robustness/test-timeout-robustness.sh`: tests `scripts/portable-timeout.sh` fallback detection and command execution behavior for GNU/Brew/Python/no-timeout paths, quick commands, timeout exit code 124, args, pipelines, short/zero timeouts, large output, retries, missing/empty commands, special chars, signal handling, subshells, and exported `TIMEOUT_IMPL` at lines 5, 16, 31, 39, 51, 62, 79, 89, 102, 120, 139, 157, 170, 188, 201, 211, 221, 235, 249, and 259.
- inputs_outputs_state: Inputs are temporary test repositories, mock `codex` binaries, synthetic `.humanize/rlcr/<timestamp>` state trees, JSON hook payloads, generated plan files, fake `$HOME`/`XDG_CACHE_HOME`, and subprocess signals. Outputs are pass/fail counters via `tests/test-helpers.sh`, hook exit codes, validator stderr messages, generated temporary state files, and mock command logs. State transitions tested include `state.md` active loop, `finalize-state.md` active finalize phase, terminal `cancel-state.md` and `complete-state.md`, `methodology-analysis-state.md`, and setup-created scaffolds.
- gates_or_invariants: The suite encodes several hard invariants: malformed hook JSON fails closed; state writes are blocked; active loop selection uses newest/session-aware semantics and avoids zombie revival; finalize phase forbids historical round contract access; goal-tracker immutable sections are protected after round 0; setup rejects unsafe plan paths, symlinks, dirty repos, invalid model/branch values, and missing dependencies; timeout wrappers preserve 124 on timeout; template substitution is single-pass; cancel authorization requires exact safe move patterns and signal files.
- dependencies_and_callers: Tests source or execute production code in `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/humanize.sh`, and `scripts/portable-timeout.sh`. They share helpers from `tests/test-helpers.sh`. This directory complements sibling top-level tests such as `tests/test-stop-hook-bg-allow.sh`, `tests/test-stop-gate.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-ask-codex.sh`, and the aggregate `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The directory is deliberately edge-heavy: non-UTF8/null JSON, deeply nested JSON, binary files, symlink chains, spaces/metacharacters/globs in paths, concurrent/partial writes, missing/corrupt state, old sessions, terminal sessions, zsh/bash differences, missing command dependencies, timeouts, large outputs, malformed markdown tables, and unsafe shell command forms.
- validation_or_tests: This item is itself test coverage. The files are shell tests with line-count total 8,145 lines under `tests/robustness`, discovered recursively via `rg --files tests/robustness`. I did not execute them because this task requests research notes only and the branch export is read-only.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-055 `file` `scripts/rlcr-stop-gate.sh`
- cursor: `[_]`
- core_role: Runtime wrapper that lets non-hook workflows run the same RLCR stop-hook gate as Claude Code Stop events. It is directly related to the branch behavior because it maps stop-hook background-task short-circuit responses into an “allow” result rather than treating them as a blocking loop failure.
- algorithmic_behavior: The script establishes `HUMANIZE_ROOT`, default `PROJECT_ROOT`, and `HOOK_SCRIPT` at lines 19-22, parses `--session-id`, `--transcript-path`, `--project-root`, and `--json` at lines 43-74, validates hook script executability and `jq` availability at lines 76-84, builds Stop-hook JSON with `hook_event_name: "Stop"`, `stop_hook_active: false`, `cwd`, model, permission mode, `session_id`, and `transcript_path` at lines 95-110, invokes `hooks/loop-codex-stop-hook.sh` with `CLAUDE_PROJECT_DIR="$PROJECT_ROOT"` at line 115, then normalizes hook output into wrapper exit codes and human-readable output at lines 117-165.
- inputs_outputs_state: Inputs are CLI flags, environment defaults `CLAUDE_PROJECT_DIR`, `CLAUDE_SESSION_ID`, `CLAUDE_TRANSCRIPT_PATH`, `CODEX_MODEL`, and `CODEX_PERMISSION_MODE`, plus active `.humanize/rlcr` state discovered by the hook. Outputs are stdout/stderr messages and exit codes: `0` for allow, `10` for hook decision `block`, and `20` for wrapper/runtime errors, documented at lines 8-15. It does not directly mutate state, but the called stop hook may create or clean `bg-pending.marker` through `handle_bg_task_short_circuit` in `hooks/lib/loop-bg-tasks.sh`.
- gates_or_invariants: The wrapper requires an executable hook script and `jq` before running. It intentionally emits explicit JSON `null` for empty `session_id` or `transcript_path` rather than dropping sibling fields, preventing the jq object-collapse bug described at lines 90-94. It treats empty hook output as allow at lines 123-127, validates non-empty hook output as JSON at lines 129-133, treats `decision: block` as exit 10 at lines 139-147, and treats JSON with no `decision` as allow at lines 149-160. That no-decision allow path is important for background-task short-circuit messages emitted by `hooks/lib/loop-bg-tasks.sh` lines 330-332.
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, `jq`, and Stop-hook JSON schema. `hooks/loop-bash-validator.sh` blocks manual Bash execution of this script together with the stop hook at lines 166-183, so it is intended for controlled skill/workflow invocation rather than user bypass. Related tests include `tests/test-stop-gate.sh` for project-root handling and JSON forwarding, and `tests/test-stop-hook-bg-allow.sh` for pending background task allow behavior; references appear in repository search hits around `tests/test-stop-gate.sh:217-277` and `tests/test-stop-hook-bg-allow.sh:470-579`.
- edge_cases_or_failure_modes: Unknown or missing option values exit 20 at lines 45-72. Missing hook or missing `jq` exit 20 at lines 76-84. Hook nonzero exit maps to wrapper error at lines 117-121. Non-JSON hook output maps to wrapper error at lines 129-133. Unexpected non-empty `decision` values also map to 20 at lines 163-165. If invoked without `--session-id` while a background marker exists, the hook’s ambiguous-caller guard exits silently via `hooks/lib/loop-bg-tasks.sh` lines 251-263, and this wrapper reports allow.
- validation_or_tests: `tests/test-stop-gate.sh` covers default cwd project root, `--project-root`, tracked `.humanize` blocking, inactive loop allow, and the jq object-collapse fix. `tests/test-stop-hook-bg-allow.sh` covers forwarding transcript paths and allow behavior when background tasks are pending.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-085 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for the real monitor loop `_humanize_monitor_codex` in `scripts/humanize.sh`. It verifies graceful monitor termination when RLCR state disappears or the process receives SIGINT, across bash and zsh.
- algorithmic_behavior: The header states it runs the real `_humanize_monitor_codex` and validates deletion handling, no shell glob errors, terminal restoration, and bash/zsh behavior at lines 3-12. It creates isolated `/tmp/test-monitor-e2e-real-$$` state at lines 43-51. Test 1 creates a fake project, valid `state.md`, `goal-tracker.md`, fake cache log, and a runner that sources `scripts/humanize.sh` and calls `_humanize_monitor_codex` at lines 56-158; it deletes `.humanize/rlcr` at line 165 and waits for bounded exit at lines 167-181. Test 2 repeats the deletion case under zsh at lines 233-371. Test 3 runs bash SIGINT handling at lines 377-550. Test 4 runs zsh SIGINT handling at lines 555-685. The direct execution block runs all four tests and summarizes pass/fail counts at lines 691-724.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/state.md` files with fields such as `current_round`, `max_iterations`, `codex_model`, `plan_file`, `start_branch`, `base_branch`, and `review_started` at lines 65-78, 246-259, 387-400, and 568-581; synthetic `goal-tracker.md`; fake cache logs in `$XDG_CACHE_HOME`; bash/zsh shells; terminal shim functions for `tput` and `clear`; and process signals. Outputs are captured monitor logs, pass/fail counters, process exit status, and summary text.
- gates_or_invariants: The monitor must exit within a bounded wait after `.humanize/rlcr` deletion at lines 167-181 and 335-349, print `Monitoring stopped:` and a “directory no longer exists” reason at lines 187-198, avoid `no matches found` or `bad pattern` at lines 200-205 and 360-364, restore terminal state or at least include cleanup messaging at lines 207-220, and return exit code 0 on graceful deletion at lines 222-227 and 366-370. SIGINT paths must terminate or be cleaned up with bounded force-kill fallback and must not produce glob errors at lines 492-549 and 649-684.
- dependencies_and_callers: Depends on production `scripts/humanize.sh` and, through it, `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh`. The production monitor begins at `scripts/humanize.sh` lines 261-360, finding sessions and cache logs. Sibling wrappers `tests/test-monitor-e2e-deletion.sh` and `tests/test-monitor-e2e-sigint.sh` source this file to run focused subsets.
- edge_cases_or_failure_modes: Covers state directory deletion during monitor polling, zsh glob behavior, SIGINT delivery differences between shells/process groups, terminal command availability via shims, missing zsh skip paths, cache directory lookup via sanitized project path, and prevention of orphaned monitor processes through `pkill -f "test-monitor-e2e-real-$$"` at lines 46-49.
- validation_or_tests: This file is a test. I inspected but did not run it. It validates the real monitor rather than stubs, as emphasized at lines 5-6 and line 106.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-115 `file` `prompt-template/block/finalize-contract-access.md`
- cursor: `[_]`
- core_role: Block-message template for the Finalize Phase invariant that historical `round-N-contract.md` files are no longer active contract surfaces.
- algorithmic_behavior: The template renders a title, states there is no active `round-N-contract.md` during Finalize Phase, and instructs the agent not to `{{ACTION}}` historical round contracts while using `finalize-summary.md` and `goal-tracker.md` instead at lines 1-7.
- inputs_outputs_state: Input is the `ACTION` substitution variable. Output is the rendered markdown block. No state mutation occurs in the template itself. The runtime state transition that activates it is detection of `finalize-state.md` as the active state file.
- gates_or_invariants: Enforces that Finalize Phase has no active round contract. `hooks/lib/loop-common.sh` defines `finalize_contract_blocked_message` at lines 865-877, with this template as the primary message and an inline fallback. `hooks/loop-read-validator.sh` blocks contract reads in finalize phase at lines 217-219. `hooks/loop-write-validator.sh` blocks contract writes in finalize phase at lines 218-221. `hooks/loop-edit-validator.sh` also calls this helper for edit attempts, per repository search result `hooks/loop-edit-validator.sh:170`.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/lib/template-loader.sh`, whose safe loader falls back if the template is missing or empty at lines 188-210. Called by read/write/edit validators through `finalize_contract_blocked_message`.
- edge_cases_or_failure_modes: If the template is missing, empty, or render output is empty, `load_and_render_safe` uses the fallback message at `hooks/lib/loop-common.sh` lines 868-873. If `ACTION` is absent, the placeholder remains literal because the renderer preserves unknown placeholders, per `hooks/lib/template-loader.sh` lines 115-121.
- validation_or_tests: `tests/robustness/test-hook-system-robustness.sh` covers finalize and state-file hook behaviors generally, and validator references prove this template participates in read/write/edit gate enforcement. Template rendering fallbacks and placeholder behavior are covered by `tests/robustness/test-template-error-robustness.sh` and `tests/robustness/test-template-stress-robustness.sh`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-145 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Block-message template for path-canonicality enforcement when an agent targets a valid round file name from the wrong RLCR session directory or path.
- algorithmic_behavior: The template reports attempted action/path, provides the correct path, and oddly suggests `cat {{FILE_PATH}}` if the file is needed at lines 1-6. It is used after validators have already determined the active loop directory and current round are otherwise valid but the concrete path does not exactly match the active session path.
- inputs_outputs_state: Inputs are `ACTION`, `FILE_PATH`, and `CORRECT_PATH`. Output is a rendered markdown block. No state mutation occurs. It is a terminal validator response that exits with block code 2 in the calling hook.
- gates_or_invariants: `hooks/loop-read-validator.sh` sets `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and renders this template when `FILE_PATH != CORRECT_PATH` at lines 304-314. `hooks/loop-write-validator.sh` performs the analogous write-path check at lines 330-340. The invariant is that round summaries/prompts/contracts must be accessed via the active loop directory, not old-session or alternate paths.
- dependencies_and_callers: Depends on the template renderer in `hooks/lib/template-loader.sh` lines 56-135 and safe fallback logic at lines 188-210. Callers are read and write validators; search showed direct uses at `hooks/loop-read-validator.sh:310` and `hooks/loop-write-validator.sh:336`.
- edge_cases_or_failure_modes: The template’s last line says `cat {{FILE_PATH}}`, which appears semantically inconsistent with the “Correct path” line and with read/write validators blocking the original path. It may be a skip/repair candidate for wording, but it is still core because it controls block guidance. Unknown placeholders remain literal if a caller omits a variable, by renderer behavior at `hooks/lib/template-loader.sh` lines 115-121.
- validation_or_tests: Path/session/round file restrictions are covered by `tests/robustness/test-hook-system-robustness.sh`, especially wrong path/location cases around lines 108, 123, 538, and related validator behavior. Template behavior is covered by the template robustness tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-175 `file` `skills/ask-codex/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction defining the workflow for one-shot Codex consultation through `scripts/ask-codex.sh`. It is algorithmic workflow metadata because it constrains command construction, quoting, output interpretation, and failure reporting for `/humanize:ask-codex`.
- algorithmic_behavior: The frontmatter names the skill, describes it as sending a task to `codex exec`, declares argument hints, and restricts allowed tools to the plugin script at lines 1-5. The usage section requires quoting free-form user text and shell metacharacters at lines 12-15. It gives the safe no-flag invocation `"${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh" "$ARGUMENTS"` at lines 16-20. If flags are supplied, it requires reconstructing them as separate shell arguments with the free-form task as one quoted final argument at lines 22-28. It explicitly forbids the unsafe unquoted `$ARGUMENTS` form at lines 30-36.
- inputs_outputs_state: Inputs are optional `--codex-model MODEL:EFFORT`, optional `--codex-timeout SECONDS`, and a free-form question/task. Output is Codex stdout for the caller to read, with status information on stderr, as documented at lines 38-42. Persistent state/output is saved under `.humanize/skill/<timestamp>/output.md` per line 55, implemented by `scripts/ask-codex.sh` lines 205-238 and 391-408.
- gates_or_invariants: The skill-level invariant is safe shell argument handling: flags stay separate and the prompt is one quoted argument. Runtime validation lives in `scripts/ask-codex.sh`: it parses options at lines 89-144, rejects missing invalid `--codex-model` and `--codex-timeout` at lines 105-130, requires `codex` in PATH and non-empty question at lines 153-170, validates model and effort characters at lines 172-186, detects project root at lines 192-196, writes input/debug files at lines 224-276, invokes `codex exec` under `run_with_timeout` at lines 294-298, maps timeout to exit 124 at lines 309-331, maps nonzero/empty responses to errors at lines 334-385, and returns stdout on success at lines 391-415.
- dependencies_and_callers: The skill delegates to `scripts/ask-codex.sh`, which sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` at lines 26-33 for timeout and config-backed defaults. It is referenced by planning/routing surfaces: `prompt-template/plan/gen-plan-template.md`, `scripts/setup-rlcr-loop.sh`, `commands/start-rlcr-loop.md`, `commands/gen-plan.md`, and `hooks/loop-codex-stop-hook.sh` around its task-routing prompt text. Tests include `tests/test-ask-codex.sh`, `tests/test-task-tag-routing.sh`, `tests/test-gen-plan.sh`, and `tests/test-unified-codex-config.sh`.
- edge_cases_or_failure_modes: Unsafe shell expansion can break or mis-execute before `ask-codex.sh` starts, which the skill explicitly warns against at lines 30-36. Runtime failures include missing Codex CLI, empty question, invalid flags, timeout exit 124, Codex nonzero exit, and empty stdout. The script falls back to project-local cache if home cache is not writable at lines 213-218.
- validation_or_tests: `tests/test-ask-codex.sh` directly checks the skill file contains the unsafe-form warning and safe quoted invocation, per repository search hits around lines 416-423. `tests/test-unified-codex-config.sh` checks config-backed default model/effort behavior for `ask-codex.sh`. `tests/test-task-tag-routing.sh` checks prompts include `/humanize:ask-codex` for `analyze` tasks.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all six required item sections are present above, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`