# agent_12 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-012 `file` `README.md`
- cursor: `[_]`
- core_role: Top-level behavior documentation for Humanize/RLCR. It defines the system as an iterative Claude implementation plus independent Codex review loop, not as a one-shot generator. Key behavior references: RLCR definition in `README.md:9-12`, core loop concepts in `README.md:13-18`, and two-phase workflow in `README.md:20-27`.
- algorithmic_behavior: Documents the intended state-machine rhythm: generate/refine a plan, start an RLCR loop, Claude implements, Codex reviews summaries/code, issues feed back into later implementation rounds until acceptance criteria are met. Quick-start commands at `README.md:42-62` name the user-facing entrypoints: `/humanize:gen-plan`, `/humanize:refine-plan`, `/humanize:start-rlcr-loop`, and `/humanize:ask-gemini`.
- inputs_outputs_state: Inputs are draft/plan files and user commands. Outputs are generated/refined plans, active RLCR loop artifacts, Codex review feedback, and monitor views. Runtime state is only described indirectly through “Implementation” and “Code Review” phases at `README.md:26`; operational state files are implemented elsewhere under `.humanize/rlcr`.
- gates_or_invariants: Documents two major gates: human plan understanding before loop start at `README.md:18`, and Codex review feedback as the independent acceptance/refinement gate at `README.md:16` and `README.md:26`.
- dependencies_and_callers: Depends on Claude Code plugin installation and Codex CLI for review. Install instructions are at `README.md:29-40`; monitor dependencies are described at `README.md:64-71`. It points readers to detailed docs in `docs/usage.md`, install guides, and configuration docs at `README.md:79-86`.
- edge_cases_or_failure_modes: README itself does not define low-level failure handling. Possible user-facing gaps are installation prerequisites and external CLI availability, especially Codex required at `README.md:40`.
- validation_or_tests: No direct tests for README behavior were inspected; the executable contracts live in shell tests and hooks. README is documentation-level behavioral context rather than an executable spec.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-042 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: Claude `PreToolUse` validator for `Edit` operations inside active RLCR loops. It protects loop-owned control files, round artifacts, state files, plan backups, and goal tracker immutability. The stated blocked classes are listed in `hooks/loop-edit-validator.sh:3-10`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name`, `.tool_input.file_path`, lowercases the path, and extracts `session_id` (`hooks/loop-edit-validator.sh:23-35`). Non-`Edit` tools pass immediately (`hooks/loop-edit-validator.sh:26-28`). It blocks todos file edits unless allowlisted (`hooks/loop-edit-validator.sh:40-48`), blocks prompt edits (`hooks/loop-edit-validator.sh:50-53`), applies methodology-analysis restrictions before the normal loop-dir early exit (`hooks/loop-edit-validator.sh:55-113`), exits for paths outside `.humanize/rlcr` (`hooks/loop-edit-validator.sh:115-121`), resolves the session-matched active loop (`hooks/loop-edit-validator.sh:127-133`), parses strict state (`hooks/loop-edit-validator.sh:135-147`), blocks state files (`hooks/loop-edit-validator.sh:149-167`), blocks finalize-phase contract edits (`hooks/loop-edit-validator.sh:169-172`), blocks loop `plan.md` backups (`hooks/loop-edit-validator.sh:174-186`), enforces goal-tracker path and immutable-section rules (`hooks/loop-edit-validator.sh:188-228`), and rejects wrong-round summary/contract edits (`hooks/loop-edit-validator.sh:230-261`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name`, `tool_input.file_path`, optional `old_string`, `new_string`, `replace_all`, and `session_id`. Output is exit `0` allow, exit `2` block with a rendered prompt/template message, or exit `1` for malformed/missing required edit-state fields. State read includes `.humanize/rlcr/<loop>/state.md`, `finalize-state.md`, or `methodology-analysis-state.md`.
- gates_or_invariants: Session isolation is enforced by `find_active_loop "$LOOP_BASE_DIR" "$HOOK_SESSION_ID"` without background-marker fallback, so validators do not adopt another session’s parked loop. Strict state parsing fails closed on malformed state (`hooks/loop-edit-validator.sh:142-146`; implementation in `hooks/lib/loop-common.sh:520-587`). Methodology-analysis phase allows only `methodology-analysis-report.md` and `methodology-analysis-done.md` (`hooks/loop-edit-validator.sh:100-112`). Goal tracker edits after round 0 must provide literal edit fields and preserve the immutable section via `preview_edit_result` and `goal_tracker_mutable_update_allowed` (`hooks/loop-edit-validator.sh:202-224`; helper logic in `hooks/lib/loop-common.sh:936-979`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:15-17`), which provides path classifiers, active-loop discovery, strict/tolerant state parsing, template rendering, and goal-tracker helpers. Template dependency includes `prompt-template/block/plan-backup-protected.md`, loaded at `hooks/loop-edit-validator.sh:181-183`. It is called by Claude hook wiring for pre-tool edit validation.
- edge_cases_or_failure_modes: If no active loop is found, edits pass (`hooks/loop-edit-validator.sh:131-133`). If `realpath` fails during methodology analysis, it resolves parent paths; if unresolved paths contain traversal segments, it blocks (`hooks/loop-edit-validator.sh:70-91`). Spawned agents with different `session_id` are explicitly not restricted by this hook and must be sanitized by the analysis prompt (`hooks/loop-edit-validator.sh:64-67`). Missing `old_string`/`new_string` for protected goal tracker edits returns exit `1` (`hooks/loop-edit-validator.sh:202-211`).
- validation_or_tests: Related executable coverage includes robustness tests for strict state parsing in `tests/robustness/test-state-file-robustness.sh`, especially missing frontmatter/fields and non-numeric values at `tests/robustness/test-state-file-robustness.sh:149-228`. Background-marker validator isolation is covered by `tests/test-stop-hook-bg-allow.sh:1030-1072`, where default `find_active_loop` ignores a foreign `bg-pending.marker`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-072 `file` `tests/test-codex-hook-install.sh`
- cursor: `[_]`
- core_role: Executable specification for installing native Codex Stop hooks and Codex-target Humanize runtime assets. It verifies merge/idempotency behavior around `scripts/install-skill.sh` and `scripts/install-codex-hooks.sh`.
- algorithmic_behavior: Creates a fake Codex binary that supports `features list`, `features enable codex_hooks`, and `exec` (`tests/test-codex-hook-install.sh:40-68`). Seeds an existing `hooks.json` with unrelated hooks plus a stale Humanize Stop hook (`tests/test-codex-hook-install.sh:71-108`). Runs `scripts/install-skill.sh --target codex` with isolated config/skills/bin paths (`tests/test-codex-hook-install.sh:110-116`). Then asserts installed skills, hook file creation, feature enablement, user config creation, and shim creation (`tests/test-codex-hook-install.sh:118-152`). A Python inspection verifies the managed Stop hook is present, unrelated hooks remain, stale managed hook commands are removed, SessionStart is preserved, and exactly one managed Humanize Stop hook exists (`tests/test-codex-hook-install.sh:168-226`). A second install validates idempotency (`tests/test-codex-hook-install.sh:251-288`). Unsupported Codex builds without `codex_hooks` must fail with explanatory output (`tests/test-codex-hook-install.sh:290-332`).
- inputs_outputs_state: Inputs are fake `CODEX_HOME`, `XDG_CONFIG_HOME`, `--codex-config-dir`, `--codex-skills-dir`, and `--command-bin-dir`. Outputs are `hooks.json`, installed skill bundles, `.codex-hooks-enabled`, Humanize user config, and `bitlesson-selector` shim. The test observes installer logs and JSON state.
- gates_or_invariants: Native hooks support is mandatory; installer must reject Codex builds where `codex features list` lacks `codex_hooks` (`tests/test-codex-hook-install.sh:298-327`; implementation in `scripts/install-codex-hooks.sh:75-83`). Hook merge must remove old Humanize-managed hook paths but preserve unrelated Stop and SessionStart hooks. The resulting managed Stop hook count must be exactly one (`tests/test-codex-hook-install.sh:222-226`). Codex install must set `provider_mode` to `codex-only` and seed `bitlesson_model` with the default Codex/OpenAI model (`tests/test-codex-hook-install.sh:154-166`).
- dependencies_and_callers: Depends on `scripts/install-skill.sh` (`tests/test-codex-hook-install.sh:12`), `scripts/install-codex-hooks.sh`, `config/codex-hooks.json`, `jq`, `python3`, and `tests/test-helpers.sh`. The hook merge implementation quotes runtime paths and replaces `{{HUMANIZE_RUNTIME_ROOT}}` in `scripts/install-codex-hooks.sh:94-119`, filters managed Stop commands at `scripts/install-codex-hooks.sh:132-165`, and enables the feature at `scripts/install-codex-hooks.sh:175-184`.
- edge_cases_or_failure_modes: Fails fast if installer or `python3` is unavailable (`tests/test-codex-hook-install.sh:19-27`). Existing malformed/non-object `hooks.json` would be rejected by the Python merge path (`scripts/install-codex-hooks.sh:120-136`). Runtime roots with spaces/quotes are protected by JSON escaping plus shell quoting (`scripts/install-codex-hooks.sh:105-119`). Unsupported Codex versions must fail rather than silently installing inert hooks.
- validation_or_tests: The file is itself the validation entrypoint and ends with `print_test_summary "Codex Hook Install Tests"` (`tests/test-codex-hook-install.sh:334`). It validates Codex install idempotency, hook merge correctness, feature gate behavior, and shim dispatch to installed runtime (`tests/test-codex-hook-install.sh:235-249`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-102 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: Executable specification for unified `codex_model`/`codex_effort` configuration across defaults, config merging, state parsing, setup, stop hook, and `ask-codex`. It ensures the system no longer uses stale `loop_reviewer_*` fields.
- algorithmic_behavior: Validates `config/default_config.json` has `codex_model=gpt-5.4` and `codex_effort=high`, with reviewer keys absent (`tests/test-unified-codex-config.sh:61-80`). Exercises config-loader merge defaults and project overrides (`tests/test-unified-codex-config.sh:88-122`). Sources `loop-common.sh` to verify config-backed `DEFAULT_CODEX_MODEL`/`DEFAULT_CODEX_EFFORT`, caller preset precedence, invalid model/effort fallback, and unsupported non-Codex model fallback (`tests/test-unified-codex-config.sh:126-247`). Tests stop-hook fallback chain from `STATE_CODEX_*` to `DEFAULT_CODEX_*` (`tests/test-unified-codex-config.sh:252-373`). Checks setup script fields and stale reviewer-key ignoring (`tests/test-unified-codex-config.sh:377-468`). Runs a stop-hook invalid-effort behavior test to ensure Codex is not invoked (`tests/test-unified-codex-config.sh:472-543`). Runs setup and `ask-codex` runtime tests (`tests/test-unified-codex-config.sh:547-771`).
- inputs_outputs_state: Inputs include JSON configs in `config/default_config.json`, `.humanize/config.json`, state frontmatter fields, CLI flags such as `--codex-model model:effort`, and mock Codex binaries. Outputs are default variables, parsed `STATE_CODEX_MODEL`/`STATE_CODEX_EFFORT`, generated `state.md`, setup console output, stop-hook error behavior, and `ask-codex` stderr model/effort reporting.
- gates_or_invariants: Model values must match shell-safe regex and be Codex/OpenAI-style prefixes (`gpt-` or `o[0-9]`) when config-backed; invalid values warn and fall back (`hooks/lib/loop-common.sh:198-217`). Effort must be one of `xhigh`, `high`, `medium`, `low` (`tests/test-unified-codex-config.sh:657-672`). State-provided values override defaults; bare state falls back to config-backed defaults (`tests/test-unified-codex-config.sh:261-372`). Stop hook rejects invalid state effort before invoking Codex (`tests/test-unified-codex-config.sh:489-542`; implementation in `hooks/loop-codex-stop-hook.sh:172-184`). Setup state template must write `codex_model` and `codex_effort` (`scripts/setup-rlcr-loop.sh:876-883`) and no reviewer fields.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/ask-codex.sh`, `jq`, `codex`, `git`, and `timeout`. `ask-codex.sh` uses `CODEX_MODEL="$DEFAULT_CODEX_MODEL"` and `CODEX_EFFORT="$DEFAULT_CODEX_EFFORT"` (`scripts/ask-codex.sh:41-42`), then passes effort into `codex exec` args (`scripts/ask-codex.sh:245-247`).
- edge_cases_or_failure_modes: Skips portions when `jq`, `codex`, or required files are missing. Invalid config characters such as `haiku!` and invalid effort `superhigh` must not poison runtime defaults (`tests/test-unified-codex-config.sh:196-222`). Shell-safe but unsupported models like `haiku`, `false`, and `claude-3` also fall back (`tests/test-unified-codex-config.sh:223-247`). Stale reviewer keys in config/state are ignored without setting `STATE_LOOP_REVIEWER_*` (`tests/test-unified-codex-config.sh:400-467`).
- validation_or_tests: The whole file is the validation. It ends with `print_test_summary "Unified Codex Config Test Summary"` (`tests/test-unified-codex-config.sh:775-779`). It combines static assertions, direct function calls, generated-state inspection, and mock-runtime behavior.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-132 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template used when an edit targets the loop’s backup `plan.md`. It defines the user-facing contract that the copied plan inside `.humanize/rlcr` is read-only.
- algorithmic_behavior: Static template says the loop-directory `plan.md` backup cannot be modified, exists to preserve plan integrity, and should be read rather than edited (`prompt-template/block/plan-backup-protected.md:1-7`). The edit validator loads it when `basename "$FILE_PATH"` is `plan.md` and the path is under `/.humanize/rlcr/` (`hooks/loop-edit-validator.sh:178-184`).
- inputs_outputs_state: Input is a blocked Edit operation against loop backup `plan.md`. Output is this rendered message on stderr, followed by validator exit `2`. The protected state is the original plan backup copied into the loop directory.
- gates_or_invariants: Invariant: loop backup `plan.md` is immutable. The template reinforces that active implementation should reference it, not mutate it.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-backup-protected.md"` in `hooks/loop-edit-validator.sh:181-183`. Depends on `hooks/lib/template-loader.sh` through `loop-common.sh`.
- edge_cases_or_failure_modes: If the template cannot load, the validator uses inline fallback text at `hooks/loop-edit-validator.sh:181`. The template applies only to the loop backup path, not arbitrary project files named `plan.md`.
- validation_or_tests: Covered indirectly by edit-validator tests/template-reference tests. The direct enforcement path is visible in `hooks/loop-edit-validator.sh:174-186`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-162 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt fragment that instructs Claude to push commits after each round when `--push-every-round` is enabled.
- algorithmic_behavior: Static note: “Since `--push-every-round` is enabled, you must push your commits to remote after each round” (`prompt-template/claude/push-every-round-note.md:1-2`). The setup script has an equivalent inline append to `round-0-prompt.md` when `PUSH_EVERY_ROUND` is true (`scripts/setup-rlcr-loop.sh:1429-1435`).
- inputs_outputs_state: Input is configuration state/CLI flag `push_every_round: true`, persisted in state at `scripts/setup-rlcr-loop.sh:880-884`. Output is prompt text instructing remote push behavior. State affected indirectly: stop hook and bash validator enforce push policy based on `STATE_PUSH_EVERY_ROUND`.
- gates_or_invariants: Invariant: when push-every-round mode is enabled, each completed round must be pushed to remote before the loop proceeds. The stop hook has a push-related gate area at `hooks/loop-codex-stop-hook.sh:714`, and bash validation references blocking `git push` when `push_every_round` is false via `hooks/loop-bash-validator.sh:203`.
- dependencies_and_callers: Template belongs to Claude prompt assembly. Related setup state is written by `scripts/setup-rlcr-loop.sh`; prompt-generation code may use this template or the inline equivalent shown above.
- edge_cases_or_failure_modes: Skip candidate as core algorithm because this specific file is only a prompt note and no direct caller was confirmed in inspected code; however, the behavior it encodes is algorithmically relevant to round transition/push policy.
- validation_or_tests: Related config parsing for `push_every_round` is covered by state parsing in `hooks/lib/loop-common.sh:443` and defaults in `hooks/lib/loop-common.sh:497`. No direct test of this exact template file was identified in assigned reads.
- skip_candidate: `yes: static prompt fragment; behavior is enforced by state/hooks elsewhere, and this exact template may be redundant with inline setup prompt text`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-192 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR state file parsing under valid, malformed, partial, legacy, and edge-case frontmatter. It verifies the contract used by validators and stop hooks when reading `.humanize/rlcr/*state.md`.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers (`tests/robustness/test-state-file-robustness.sh:14-17`). Positive tests cover complete state parsing (`tests/robustness/test-state-file-robustness.sh:33-60`), `get_current_round` extraction (`tests/robustness/test-state-file-robustness.sh:62-70`), extra fields ignored (`tests/robustness/test-state-file-robustness.sh:72-98`), quoted values (`tests/robustness/test-state-file-robustness.sh:100-122`), and round zero (`tests/robustness/test-state-file-robustness.sh:124-139`). Negative/edge tests cover missing frontmatter, non-numeric values, missing required fields, empty files, partial YAML, malformed lines, large/negative numbers, special characters, trailing whitespace, missing files, binary suffixes, CRLF, `full_review_round`, and drift fields (`tests/robustness/test-state-file-robustness.sh:149-523`).
- inputs_outputs_state: Inputs are temporary `state*.md` files with YAML frontmatter variants. Outputs are pass/fail test counters and parsed globals such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_FULL_REVIEW_ROUND`, `STATE_MAINLINE_STALL_COUNT`, `STATE_LAST_MAINLINE_VERDICT`, and `STATE_DRIFT_STATUS`.
- gates_or_invariants: Tolerant `parse_state_file` should return defaults for non-critical missing fields: current round `0`, max iterations `10`, full review round `5`, drift stall count `0`, last verdict `unknown`, drift status `normal` (`hooks/lib/loop-common.sh:481-508`). Strict `parse_state_file_strict` rejects missing frontmatter, missing required `current_round`, `max_iterations`, `review_started`, or `base_branch`, non-numeric current/max values, and non-boolean `review_started` (`hooks/lib/loop-common.sh:520-587`). `get_current_round` defaults to `0` on unreadable or absent parsed value (`hooks/lib/loop-common.sh:417-427`).
- dependencies_and_callers: Depends on state parsing helpers in `hooks/lib/loop-common.sh`. Those helpers are called by `hooks/loop-edit-validator.sh` for fail-closed edit validation (`hooks/loop-edit-validator.sh:142-147`) and by `hooks/loop-codex-stop-hook.sh` for tolerant parse plus explicit schema validation (`hooks/loop-codex-stop-hook.sh:119-184`).
- edge_cases_or_failure_modes: The test intentionally accepts graceful-but-not-strict behavior for tolerant paths: non-numeric current round may return raw or empty without crashing (`tests/robustness/test-state-file-robustness.sh:190-199`), partial YAML can return `0` or `5` (`tests/robustness/test-state-file-robustness.sh:242-257`), CRLF handling only must not crash (`tests/robustness/test-state-file-robustness.sh:386-397`), and negative numbers are “handled gracefully” even though strict parser allows signed numeric patterns (`hooks/lib/loop-common.sh:558-567`).
- validation_or_tests: The file is the validation entrypoint and ends with `print_test_summary "State File Robustness Test Summary"` plus exit status propagation (`tests/robustness/test-state-file-robustness.sh:525-530`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-012, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-042, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-072, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-102, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-132, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-162, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-192
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`