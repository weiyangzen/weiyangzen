# agent_03 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Declarative slash-command workflow surface for Humanize planning and RLCR control. Recursive contents are four command specs: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, `commands/refine-plan.md`, and `commands/cancel-rlcr-loop.md`.
- algorithmic_behavior: `gen-plan.md` converts a draft into a structured plan through fixed sequential phases: config merge, IO validation, relevance check, Codex first-pass critique, Claude plan synthesis, optional Claude/Codex convergence, user-decision consolidation, final plan write, optional language variant, and optional RLCR auto-start when converged (`commands/gen-plan.md:32`, `commands/gen-plan.md:49`, `commands/gen-plan.md:132`, `commands/gen-plan.md:184`, `commands/gen-plan.md:255`, `commands/gen-plan.md:594`). `start-rlcr-loop.md` gates startup through plan compliance and a non-blocking understanding quiz before delegating loop creation to `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:13`, `commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:109`). `refine-plan.md` refines annotated plans with a stateful comment scanner, classification, processing, schema preservation, QA generation, and atomic output replacement (`commands/refine-plan.md:30`, `commands/refine-plan.md:184`, `commands/refine-plan.md:276`, `commands/refine-plan.md:395`, `commands/refine-plan.md:508`). `cancel-rlcr-loop.md` delegates all cancellation semantics to `scripts/cancel-rlcr-loop.sh`, including finalize confirmation (`commands/cancel-rlcr-loop.md:11`, `commands/cancel-rlcr-loop.md:24`, `commands/cancel-rlcr-loop.md:37`).
- inputs_outputs_state: Inputs are command arguments and plan/draft files. Outputs are plan files, QA ledgers, translated variants, initialized RLCR loop state, or cancellation messages. The command layer itself mostly does not mutate state directly; it calls scripts such as `validate-gen-plan-io.sh`, `ask-codex.sh`, `setup-rlcr-loop.sh`, `validate-refine-plan-io.sh`, and `cancel-rlcr-loop.sh` declared in frontmatter allowed tools (`commands/gen-plan.md:4`, `commands/refine-plan.md:4`, `commands/cancel-rlcr-loop.md:3`).
- gates_or_invariants: The directory enforces planning-only boundaries for plan generation/refinement (`commands/gen-plan.md:20`, `commands/refine-plan.md:19`), sequential phase ordering (`commands/gen-plan.md:34`, `commands/refine-plan.md:32`), plan schema requirements including AC format and task routing tags (`commands/gen-plan.md:501`, `commands/refine-plan.md:399`), branch-switch prevention before RLCR (`commands/start-rlcr-loop.md:41`), and cancellation confirmation in Finalize Phase (`commands/cancel-rlcr-loop.md:24`).
- dependencies_and_callers: These files are slash-command definitions consumed by the Humanize command system. They coordinate with `scripts/setup-rlcr-loop.sh`, which writes `.humanize/rlcr/<timestamp>/state.md` with fields such as `current_round`, `max_iterations`, `review_started`, `session_id`, and drift counters (`scripts/setup-rlcr-loop.sh:876`), and with hook libraries such as `hooks/lib/loop-common.sh` and `hooks/lib/loop-bg-tasks.sh` for runtime loop selection and stop handling.
- edge_cases_or_failure_modes: Directory-level edge handling includes unsupported or malformed configs, missing or unsafe IO paths, malformed plan-review agent output, unresolved user decisions, unsupported language variants, missing comment blocks, malformed comment markers, and cancellation during Finalize requiring confirmation instead of direct rename.
- validation_or_tests: Related tests cover command IO and runtime surfaces: `tests/test-gen-plan.sh`, `tests/test-refine-plan.sh`, `tests/test-state-exit-naming.sh`, `tests/test-stop-hook-bg-allow.sh`, and robustness tests under `tests/robustness/`. The assigned state-exit and concurrent-state tests directly validate downstream state semantics used by commands that start/cancel RLCR.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-033 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command contract for starting the RLCR loop, including preflight plan compliance, optional user comprehension quiz, and delegation to executable loop setup.
- algorithmic_behavior: The command first extracts a plan path unless `--skip-impl` or help mode is present (`commands/start-rlcr-loop.md:17`, `commands/start-rlcr-loop.md:21`). It runs a plan-compliance agent that must return `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH`; malformed output is fail-closed (`commands/start-rlcr-loop.md:41`, `commands/start-rlcr-loop.md:52`). It then optionally runs a plan-understanding quiz and lets the user proceed or stop after wrong answers (`commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:86`, `commands/start-rlcr-loop.md:96`). If gates pass, it runs `scripts/setup-rlcr-loop.sh` with the original arguments (`commands/start-rlcr-loop.md:109`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, a relative safe plan path, and optional flags such as `--max`, `--codex-model`, `--codex-timeout`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--agent-teams`, `--yolo`, `--skip-quiz`, and `--privacy` (`commands/start-rlcr-loop.md:3`). Output is an initialized loop in `.humanize/rlcr/<timestamp>/`, produced by the setup script. The setup script writes the active `state.md` fields, including `current_round`, `max_iterations`, `full_review_round`, plan metadata, branch metadata, `review_started`, empty `session_id`, BitLesson fields, mainline drift counters, and `started_at` (`scripts/setup-rlcr-loop.sh:876`).
- gates_or_invariants: Plan path pre-check only runs for safe relative paths with no traversal and limited characters (`commands/start-rlcr-loop.md:28`). Branch-switching instructions in a plan are hard-blocked because RLCR requires branch stability (`commands/start-rlcr-loop.md:52`). The quiz is advisory, not an unconditional blocker; wrong answers trigger user choice (`commands/start-rlcr-loop.md:62`, `commands/start-rlcr-loop.md:100`). A round means “the agent believes the entire plan is finished,” not a task boundary (`commands/start-rlcr-loop.md:130`).
- dependencies_and_callers: Depends on Task agents `humanize:plan-compliance-checker` and `humanize:plan-understanding-quiz`, then on `scripts/setup-rlcr-loop.sh`. Runtime behavior then flows into stop hooks and shared loop libraries: `hooks/loop-codex-stop-hook.sh` locates active loops through `find_active_loop "$LOOP_BASE_DIR" "$HOOK_SESSION_ID" true` and runs the background-task guard before normal gates (`hooks/loop-codex-stop-hook.sh:61`, `hooks/loop-codex-stop-hook.sh:64`, `hooks/loop-codex-stop-hook.sh:71`).
- edge_cases_or_failure_modes: No plan path means pre-check is skipped and the setup script owns the error (`commands/start-rlcr-loop.md:26`). Unsafe absolute/traversal paths skip pre-check and rely on setup validation (`commands/start-rlcr-loop.md:30`). Malformed compliance output stops the command (`commands/start-rlcr-loop.md:56`). Malformed quiz output warns and continues (`commands/start-rlcr-loop.md:86`). In `--skip-impl`, setup uses review mode and initializes `review_started` accordingly (`scripts/setup-rlcr-loop.sh:868`).
- validation_or_tests: The startup state contract is validated indirectly by `tests/test-state-exit-naming.sh` for active versus terminal state names, `tests/test-session-id.sh` for session-id behavior, and `tests/test-stop-hook-bg-allow.sh` for background stop short-circuiting after startup.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-063 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Placeholder helper intended to set up monitor test fixtures, but it currently contains no supported fixture cases.
- algorithmic_behavior: Accepts `<test_dir>` and optional `<test_name>`, enforces `set -euo pipefail`, rejects missing `TEST_DIR`, then falls through a `case` statement whose only branch rejects every test name as unknown (`tests/setup-monitor-test-env.sh:10`, `tests/setup-monitor-test-env.sh:12`, `tests/setup-monitor-test-env.sh:15`, `tests/setup-monitor-test-env.sh:20`).
- inputs_outputs_state: Input is positional argument one as target test directory and positional argument two as fixture name defaulting to `default` (`tests/setup-monitor-test-env.sh:12`). Successful output would echo `TEST_DIR`, but the only implemented path exits before success (`tests/setup-monitor-test-env.sh:20`, `tests/setup-monitor-test-env.sh:28`). It creates no directories and writes no state files despite header comments claiming that purpose (`tests/setup-monitor-test-env.sh:3`).
- gates_or_invariants: Missing `TEST_DIR` is a usage error with exit 1 (`tests/setup-monitor-test-env.sh:15`). Any `TEST_NAME` is rejected with “Available: (none currently)” and exit 1 (`tests/setup-monitor-test-env.sh:20`).
- dependencies_and_callers: No internal dependencies. Recursive test search did not show this helper being central to the assigned state or background-stop tests; many tests instead use `tests/test-helpers.sh` and local fixture builders.
- edge_cases_or_failure_modes: All real invocations currently fail unless the script is expanded with named cases. The default `TEST_NAME=default` also fails because no default fixture is implemented.
- validation_or_tests: No direct positive validation exists in the inspected assigned set. This file is itself a negative executable specification showing monitor fixtures are unavailable.
- skip_candidate: `yes: classified as executable core behavior, but the implementation is a no-op placeholder that rejects every test name and does not exercise RLCR state or background-stop logic.`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-093 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR state-file lifecycle naming: active loops use `state.md`, while terminal outcomes rename that file to reason-specific `*-state.md` names.
- algorithmic_behavior: Creates a temporary git repo, sources `hooks/lib/loop-common.sh`, and validates `find_active_loop`, `end_loop`, and `.humanize/rlcr` path detection (`tests/test-state-exit-naming.sh:31`, `tests/test-state-exit-naming.sh:66`). It asserts `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active loop indicators (`tests/test-state-exit-naming.sh:51`, `tests/test-state-exit-naming.sh:97`, `tests/test-state-exit-naming.sh:114`, `tests/test-state-exit-naming.sh:129`, `tests/test-state-exit-naming.sh:145`). It asserts `state.md` is active (`tests/test-state-exit-naming.sh:76`) and newer active loop directories take precedence (`tests/test-state-exit-naming.sh:161`).
- inputs_outputs_state: Inputs are temporary `.humanize/rlcr/<timestamp>/` directories with synthetic state files. Outputs are pass/fail counters and process exit equal to failure count (`tests/test-state-exit-naming.sh:293`, `tests/test-state-exit-naming.sh:302`). It mutates only temporary fixture state files. It calls `end_loop "$dir" "$dir/state.md" "$reason"` and expects terminal files named `${reason}-state.md` (`tests/test-state-exit-naming.sh:206`).
- gates_or_invariants: `find_active_loop` must not revive terminal states. `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, and `unexpected`, rejecting invalid reasons (`tests/test-state-exit-naming.sh:186`). Missing state file must fail gracefully (`tests/test-state-exit-naming.sh:232`). `.humanize/rlcr` is the canonical loop path; legacy `.humanize-loop.local` must not match (`tests/test-state-exit-naming.sh:249`, `tests/test-state-exit-naming.sh:258`, `tests/test-state-exit-naming.sh:267`).
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`. The implemented `end_loop` validates reasons and renames the active state file to `${reason}-state.md` (`hooks/lib/loop-common.sh:1483`, `hooks/lib/loop-common.sh:1495`, `hooks/lib/loop-common.sh:1505`). `find_active_loop` checks active state files and prevents stale revival (`hooks/lib/loop-common.sh:295`).
- edge_cases_or_failure_modes: Invalid reason, missing state file, terminal states in the newest directory, and legacy path confusion are explicitly covered. A noteworthy difference from newer code is that `find_active_loop` now also treats `finalize-state.md` and `methodology-analysis-state.md` as active via `resolve_active_state_file`, while this older naming test focuses on `state.md` plus terminal files.
- validation_or_tests: This is a runnable test and exits with `$TESTS_FAILED`. It validates the exact terminal-state naming contract that stop/cancel/finalize paths rely on.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-123 `file` `prompt-template/block/git-tracked-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the stop-hook git hygiene gate when local Humanize runtime state under `.humanize/` is tracked or staged.
- algorithmic_behavior: Renders a blocking explanation that tracked or staged `.humanize/` files are local loop state and must remain outside version control (`prompt-template/block/git-tracked-humanize.md:1`, `prompt-template/block/git-tracked-humanize.md:3`). It instructs the user to remove that state from the index with `git rm --cached -r .humanize`, keep only real project files staged, and retry the stop action (`prompt-template/block/git-tracked-humanize.md:7`).
- inputs_outputs_state: Input is the stop hook detecting tracked `.humanize` paths. Output is a Markdown reason loaded by `git_tracked_humanize_blocked_message`, then placed into JSON block output by `hooks/loop-codex-stop-hook.sh` at the git-clean check (`hooks/loop-codex-stop-hook.sh:639`, `hooks/loop-codex-stop-hook.sh:641`). The gate does not repair state itself.
- gates_or_invariants: `.humanize/` runtime artifacts must not enter version control. The template explicitly forbids `git add -f` on Humanize state and committing RLCR trackers, summaries, contracts, or cancel/finalize markers (`prompt-template/block/git-tracked-humanize.md:16`).
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh` via `git_tracked_humanize_blocked_message`, which falls back to equivalent inline text if template loading fails (`hooks/lib/loop-common.sh:1392`, `hooks/lib/loop-common.sh:1415`). Detection is `git_has_tracked_humanize_state`, scoped to `git ls-files -- .humanize` (`hooks/lib/loop-common.sh:1368`, `hooks/lib/loop-common.sh:1385`).
- edge_cases_or_failure_modes: Only tracked/staged `.humanize/` is blocked; untracked local state is allowed by the detector’s stated contract (`hooks/lib/loop-common.sh:1368`). Files staged for removal via `git rm --cached` are intentionally omitted so the user can unstick the loop (`hooks/lib/loop-common.sh:1375`).
- validation_or_tests: Related behavior is covered by bash/git hygiene tests such as `tests/test-bash-validator-patterns.sh`, `tests/test-plan-file-hooks.sh`, and broader stop-hook gate tests. The assigned file itself is a render template, not an executable test.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-153 `file` `prompt-template/claude/drift-replan-prompt.md`
- cursor: `[_]`
- core_role: Next-round prompt template for drift recovery when Codex reports repeated stalled or regressed mainline progress.
- algorithmic_behavior: Forces the next round into “Drift Recovery Mode,” injecting `{{STALL_COUNT}}`, `{{LAST_MAINLINE_VERDICT}}`, the original `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, `{{ROUND_CONTRACT_FILE}}`, and `{{REVIEW_CONTENT}}` (`prompt-template/claude/drift-replan-prompt.md:3`, `prompt-template/claude/drift-replan-prompt.md:7`, `prompt-template/claude/drift-replan-prompt.md:14`, `prompt-template/claude/drift-replan-prompt.md:49`). It requires the agent to re-read plan/tracker/recent evidence and rewrite the round contract before implementation (`prompt-template/claude/drift-replan-prompt.md:17`). It constrains work into `[mainline]`, `[blocking]`, and `[queued]` task lanes so queued cleanup cannot replace recovered mainline movement (`prompt-template/claude/drift-replan-prompt.md:35`).
- inputs_outputs_state: Inputs are rendered variables from stop-hook drift state. Output is the next `round-N-prompt.md` content used to guide Claude after drift status becomes `replan_required`. The stop hook updates drift fields and renders this template around `hooks/loop-codex-stop-hook.sh:2018`, passing stall count and verdict variables around `hooks/loop-codex-stop-hook.sh:2025`.
- gates_or_invariants: The recovery contract must contain exactly one recovered mainline objective, one or two target ACs, root cause, blocking versus queued issue classification, and success criteria for returning to `ADVANCED` (`prompt-template/claude/drift-replan-prompt.md:25`). It forbids normal issue-clearing behavior, broadening scope, and spending the round mostly on queued cleanup (`prompt-template/claude/drift-replan-prompt.md:10`, `prompt-template/claude/drift-replan-prompt.md:63`).
- dependencies_and_callers: Depends on drift fields initialized by setup (`scripts/setup-rlcr-loop.sh:898`) and parsed/normalized by `loop-common.sh` (`hooks/lib/loop-common.sh:453`, `hooks/lib/loop-common.sh:605`). The stop hook increments stalled/regressed counters and sets `drift_status` to `replan_required` after repeated failures (`hooks/loop-codex-stop-hook.sh:1779`, `hooks/loop-codex-stop-hook.sh:1800`, `hooks/loop-codex-stop-hook.sh:1802`).
- edge_cases_or_failure_modes: If the agent cannot produce a credible recovered mainline objective, the prompt requires saying so with concrete blockers (`prompt-template/claude/drift-replan-prompt.md:67`). If drift keeps accumulating, the stop hook can trigger a circuit breaker and terminate the loop with `stop-state.md` rather than rendering another recovery prompt (`hooks/loop-codex-stop-hook.sh:1910`).
- validation_or_tests: `tests/test-finalize-phase.sh` validates two stalled rounds trigger drift recovery state and prompt generation, while a third stalled/regressed round stops the loop with drift state preserved (`tests/test-finalize-phase.sh:893`, `tests/test-finalize-phase.sh:941`, `tests/test-finalize-phase.sh:1027`). `tests/test-agent-teams.sh` also checks drift recovery prompt generation under agent-team mode.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-183 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for concurrent and malformed RLCR state access, especially shared helpers used by stop hooks and validators.
- algorithmic_behavior: Sources `tests/test-helpers.sh`, creates temporary state fixtures, and exercises `get_current_round`, `find_active_loop`, and `parse_state_file_strict` under parsing edge cases, loop detection cases, concurrent reads, atomic writes, malformed frontmatter, stale loops, and permission failures (`tests/robustness/test-concurrent-state-robustness.sh:15`, `tests/robustness/test-concurrent-state-robustness.sh:30`, `tests/robustness/test-concurrent-state-robustness.sh:56`, `tests/robustness/test-concurrent-state-robustness.sh:166`, `tests/robustness/test-concurrent-state-robustness.sh:246`, `tests/robustness/test-concurrent-state-robustness.sh:328`).
- inputs_outputs_state: Inputs are generated `state.md` files with YAML frontmatter. Outputs are pass/fail counters summarized by `print_test_summary`, with process exit derived from that summary (`tests/robustness/test-concurrent-state-robustness.sh:503`). It writes only under `TEST_DIR`, created by `setup_test_dir` (`tests/test-helpers.sh:86`).
- gates_or_invariants: `get_current_round` must default or degrade gracefully for leading whitespace, CRLF, empty values, comments, unicode, huge numbers, zero, and unreadable files (`tests/robustness/test-concurrent-state-robustness.sh:80`, `tests/robustness/test-concurrent-state-robustness.sh:100`, `tests/robustness/test-concurrent-state-robustness.sh:123`, `tests/robustness/test-concurrent-state-robustness.sh:141`, `tests/robustness/test-concurrent-state-robustness.sh:373`, `tests/robustness/test-concurrent-state-robustness.sh:439`, `tests/robustness/test-concurrent-state-robustness.sh:456`, `tests/robustness/test-concurrent-state-robustness.sh:474`). `find_active_loop` must return the newest active loop, avoid stale revival when a newer directory lacks active state, detect `finalize-state.md`, and ignore terminal `cancel-state.md` (`tests/robustness/test-concurrent-state-robustness.sh:170`, `tests/robustness/test-concurrent-state-robustness.sh:186`, `tests/robustness/test-concurrent-state-robustness.sh:203`, `tests/robustness/test-concurrent-state-robustness.sh:413`). Atomic `mv` writes must keep all concurrent reads valid (`tests/robustness/test-concurrent-state-robustness.sh:279`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, where `get_current_round` extracts `current_round` from frontmatter and defaults to zero (`hooks/lib/loop-common.sh:414`), `find_active_loop` implements newest-directory and session-aware zombie-loop protection (`hooks/lib/loop-common.sh:295`), and `parse_state_file_strict` rejects missing/malformed required fields (`hooks/lib/loop-common.sh:511`). It also indirectly pulls in `hooks/lib/loop-bg-tasks.sh` because `loop-common.sh` sources it at the bottom (`hooks/lib/loop-common.sh:1518`).
- edge_cases_or_failure_modes: The strict YAML malformed test expects nonzero return for an invalid array and missing frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:332`, `tests/robustness/test-concurrent-state-robustness.sh:353`). There is a potential fixture/schema mismatch: current `parse_state_file_strict` requires `review_started` and `base_branch` in addition to `current_round` and `max_iterations` (`hooks/lib/loop-common.sh:549`, `hooks/lib/loop-common.sh:553`), while this test’s malformed fixtures omit those fields; the negative cases still pass because they expect rejection, but positive strict parsing is not exercised here.
- validation_or_tests: This file is itself a runnable robustness test and covers 22 named checks ending with `Concurrent State Access Robustness Test Summary` (`tests/robustness/test-concurrent-state-robustness.sh:21`, `tests/robustness/test-concurrent-state-robustness.sh:503`). Branch-specific background behavior is validated more directly in `tests/test-stop-hook-bg-allow.sh`, where pending background tasks write `bg-pending.marker`, cross-session parked loops are protected, same-session stale markers are removed after verified completion, malformed transcripts preserve markers, and pre-loop launches are filtered by timestamp (`tests/test-stop-hook-bg-allow.sh:696`, `tests/test-stop-hook-bg-allow.sh:771`, `tests/test-stop-hook-bg-allow.sh:1030`, `tests/test-stop-hook-bg-allow.sh:1133`, `tests/test-stop-hook-bg-allow.sh:1183`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 7 assigned item sections are present exactly once in `## Item Evidence`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`