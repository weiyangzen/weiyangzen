# agent_09 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-009 `directory` `skills`
- cursor: `[_]`
- core_role: The `skills` directory is the user-facing and flow-level instruction layer for Humanize/RLCR. It does not implement the shell state machine directly, but it defines the permitted entrypoints, round workflow, command contracts, and safety rules that drive the runtime scripts and hooks. Recursively inspected children: `skills/ask-codex/SKILL.md`, `skills/ask-gemini/SKILL.md`, `skills/humanize/SKILL.md`, `skills/humanize-gen-plan/SKILL.md`, `skills/humanize-refine-plan/SKILL.md`, and `skills/humanize-rlcr/SKILL.md`.
- algorithmic_behavior: `skills/humanize/SKILL.md` describes the two-phase RLCR algorithm: implementation rounds, Codex summary review, transition on `COMPLETE`, then code-review phase using `[P0-9]` findings before finalize (`skills/humanize/SKILL.md:32`, `skills/humanize/SKILL.md:43`). It also points users at `setup-rlcr-loop.sh` and cancellation commands (`skills/humanize/SKILL.md:61`, `skills/humanize/SKILL.md:89`). `skills/humanize-rlcr/SKILL.md` is the Codex-native flow entrypoint: run setup, read the generated round prompt, implement, commit, write the required summary, then stop normally so the native Stop hook controls review/continuation (`skills/humanize-rlcr/SKILL.md:24`, `skills/humanize-rlcr/SKILL.md:36`). `humanize-gen-plan` and `humanize-refine-plan` define upstream plan production/refinement flows; they shape valid plan inputs that later become RLCR anchors (`skills/humanize-gen-plan/SKILL.md:19`, `skills/humanize-refine-plan/SKILL.md:18`).
- inputs_outputs_state: Inputs are user flow invocations and arguments, such as plan paths for RLCR (`skills/humanize-rlcr/SKILL.md:80`) or draft/output paths for plan generation (`skills/humanize-gen-plan/SKILL.md:49`). Outputs are not state files directly from these docs, but prescribed artifacts under `.humanize/rlcr/<timestamp>/`, including `state.md`, `goal-tracker.md`, round prompts, summaries, review results, finalize state/summary, and completion state (`skills/humanize/SKILL.md:180`). The `humanize-rlcr` skill documents the round artifacts as source of truth (`skills/humanize-rlcr/SKILL.md:40`, `skills/humanize-rlcr/SKILL.md:74`).
- gates_or_invariants: Key invariants: never manually edit state files, never bypass a blocked hook result, never substitute ad-hoc Codex commands for hook-managed transitions, and always use generated prompt/review files as source of truth (`skills/humanize-rlcr/SKILL.md:69`). The broader skill reinforces summaries, goal tracker maintenance, no state-file cheating, and reliance on the native Stop hook (`skills/humanize/SKILL.md:166`). Plan generation/refinement invariants require valid IO, relevant drafts, preserved schema, removal of resolved `CMT:` blocks, valid task routing tags, and atomic writes (`skills/humanize-gen-plan/SKILL.md:21`, `skills/humanize-refine-plan/SKILL.md:67`).
- dependencies_and_callers: The skills coordinate with sibling runtime paths: `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/ask-codex.sh`, `scripts/ask-gemini.sh`, `scripts/validate-gen-plan-io.sh`, and prompt templates. `ask-codex` and `ask-gemini` require wrapper scripts through `${CLAUDE_PLUGIN_ROOT}/scripts/...` and emphasize quoted argument passing (`skills/ask-codex/SKILL.md:14`, `skills/ask-gemini/SKILL.md:17`). `humanize-rlcr` delegates state/schema/branch/plan/git/summary/review gates to installed Stop hooks (`skills/humanize-rlcr/SKILL.md:50`).
- edge_cases_or_failure_modes: `ask-codex`/`ask-gemini` explicitly reject unsafe unquoted `$ARGUMENTS` usage because shell metacharacters can break or alter execution (`skills/ask-codex/SKILL.md:30`, `skills/ask-gemini/SKILL.md:33`). RLCR setup failures must stop immediately (`skills/humanize-rlcr/SKILL.md:34`). The flow-level docs identify timeout, validation, missing tool, missing plan, and output collision cases via exit-code tables (`skills/humanize-gen-plan/SKILL.md:96`, `skills/humanize-refine-plan/SKILL.md:131`, `skills/ask-codex/SKILL.md:44`, `skills/ask-gemini/SKILL.md:48`).
- validation_or_tests: Validation is mostly delegated: setup validates RLCR prerequisites and Stop hook gates; plan generation uses `validate-gen-plan-io.sh`; refine-plan uses `validate-refine-plan-io.sh`; expert-consultation skills depend on wrapper script exit codes. These skill docs are indirectly covered by tests that assert setup, routing, Stop hook, and template behavior; they are not themselves executable tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-039 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: `hooks/hooks.json` is the hook registration map for the RLCR state machine. It binds Claude Code hook events to the validator and Stop-hook scripts that enforce file access, state transitions, git/plan gates, and review continuation.
- algorithmic_behavior: The file registers `UserPromptSubmit` to `loop-plan-file-validator.sh` (`hooks/hooks.json:4`), `PreToolUse` validators for `Write`, `Edit`, `Read`, and `Bash` (`hooks/hooks.json:14`), a `PostToolUse` hook for Bash (`hooks/hooks.json:52`), and the long-running `Stop` hook to `loop-codex-stop-hook.sh` with a 7200-second timeout (`hooks/hooks.json:63`). This makes Stop the central transition point from “round work attempted to exit” into “validate, review, block or advance”.
- inputs_outputs_state: Inputs are Claude hook event payloads for prompt submission, tool use, post-Bash events, and stop events. Outputs are determined by each hook script: exit 0 allow/no-op, exit 2 or JSON block decisions for tool validators, session-id/state updates in post-Bash, and Stop-hook JSON responses containing `decision`, `reason`, and `systemMessage`.
- gates_or_invariants: The invariant is that all write/edit/read/bash operations during an active RLCR loop pass through the validators before tool execution, while Stop attempts pass through `loop-codex-stop-hook.sh`. The config itself has no conditionals; correctness depends on each matcher name matching the tool/event names exactly (`hooks/hooks.json:16`, `hooks/hooks.json:25`, `hooks/hooks.json:34`, `hooks/hooks.json:43`).
- dependencies_and_callers: Depends on scripts under `hooks/`: `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, `loop-bash-validator.sh`, `loop-post-bash-hook.sh`, and `loop-codex-stop-hook.sh`. Those scripts in turn share `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is unset or wrong, every command path resolves incorrectly (`hooks/hooks.json:9`, `hooks/hooks.json:68`). If the Stop hook exceeds 7200 seconds, Claude Code will time it out. If a matcher name drifts from actual tool names, the corresponding safety gate silently stops applying.
- validation_or_tests: `tests/test-template-references.sh` statically scans critical hook scripts for template references and safe fallback rendering. Broader hook behavior is covered by tests such as `tests/test-stop-hook-bg-allow.sh`, `tests/test-plan-file-hooks.sh`, and robustness hook tests; this item specifically is the dispatch configuration that makes those scripts active.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-069 `file` `tests/test-bitlesson-select-routing.sh`
- cursor: `[_]`
- core_role: This executable spec validates provider routing and safe invocation behavior for `scripts/bitlesson-select.sh`, which is part of the RLCR per-task lesson-selection loop. It ensures task prompts choose the configured model provider without accidentally triggering hooks or full automation.
- algorithmic_behavior: The test creates isolated temp directories, mock BitLesson knowledge bases, and mock `codex`/`claude` binaries (`tests/test-bitlesson-select-routing.sh:18`, `tests/test-bitlesson-select-routing.sh:67`, `tests/test-bitlesson-select-routing.sh:119`). It verifies `gpt-*` models route to Codex (`tests/test-bitlesson-select-routing.sh:136`), Codex receives prompt content through stdin with trailing `-` (`tests/test-bitlesson-select-routing.sh:164`), `haiku`/`sonnet`/`opus` route to Claude with case-insensitive model detection (`tests/test-bitlesson-select-routing.sh:199`, `tests/test-bitlesson-select-routing.sh:228`, `tests/test-bitlesson-select-routing.sh:257`), unknown models fail with a clear error (`tests/test-bitlesson-select-routing.sh:286`), missing Codex fails when Codex is required (`tests/test-bitlesson-select-routing.sh:312`), missing Claude falls back to Codex (`tests/test-bitlesson-select-routing.sh:348`), `provider_mode: codex-only` forces Codex despite a Claude model (`tests/test-bitlesson-select-routing.sh:390`), placeholder BitLesson files short-circuit to `NONE` (`tests/test-bitlesson-select-routing.sh:416`), and Codex selector calls disable hooks, use read-only mode, use ephemeral/skip-git-repo-check when available, and avoid `--full-auto` (`tests/test-bitlesson-select-routing.sh:440`).
- inputs_outputs_state: Inputs to the tested selector are `--task`, `--paths`, `--bitlesson-file`, `CLAUDE_PROJECT_DIR`, `XDG_CONFIG_HOME`, `.humanize/config.json`, and `PATH` ordering. Expected stdout has exactly `LESSON_IDS:` and `RATIONALE:` lines. The test mutates only temp fixtures and captures mock CLI args/stdin for assertions (`tests/test-bitlesson-select-routing.sh:93`, `tests/test-bitlesson-select-routing.sh:469`).
- gates_or_invariants: The selector must reject missing task/path/file arguments and missing BitLesson files; production code enforces those at `scripts/bitlesson-select.sh:71`, `scripts/bitlesson-select.sh:77`, and `scripts/bitlesson-select.sh:83`. A BitLesson file without lesson entries returns `LESSON_IDS: NONE` without invoking a model (`scripts/bitlesson-select.sh:100`). Provider routing is delegated to `detect_provider`, where `gpt-*`/`oN*` means Codex and `claude-*`/`haiku`/`sonnet`/`opus` means Claude (`scripts/lib/model-router.sh:18`, `scripts/lib/model-router.sh:23`). Stable output parsing requires both fields, otherwise it fails with raw output (`scripts/bitlesson-select.sh:237`, `scripts/bitlesson-select.sh:253`).
- dependencies_and_callers: Depends on `scripts/bitlesson-select.sh`, `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh` (`scripts/bitlesson-select.sh:9`, `scripts/bitlesson-select.sh:23`, `scripts/bitlesson-select.sh:27`). The next-round prompt requires running `bitlesson-selector` before each task/sub-task (`prompt-template/claude/next-round-prompt.md:42`), and Stop-hook follow-up prompts append the same requirement when needed (`hooks/loop-codex-stop-hook.sh:2039`).
- edge_cases_or_failure_modes: Unknown model names return non-zero (`scripts/lib/model-router.sh:28`). If configured provider binary is missing, production first falls back to Codex, then errors if Codex is also unavailable (`scripts/bitlesson-select.sh:121`). Codex invocation probes CLI support for `--disable`, `--skip-git-repo-check`, and `--ephemeral`, then runs with `-s read-only` and stdin prompt (`scripts/bitlesson-select.sh:187`). Timeouts return 124 (`scripts/bitlesson-select.sh:222`). Bad model output lacking `LESSON_IDS` or `RATIONALE` is rejected (`scripts/bitlesson-select.sh:253`).
- validation_or_tests: This file is itself a validation asset. It ends with `print_test_summary "Bitlesson Select Routing Test Summary"` (`tests/test-bitlesson-select-routing.sh:496`). I did not execute it because the branch export is read-only and the test creates temp repos/binaries; inspection confirms the intended executable specification.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-099 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: This executable spec protects the hook/template boundary. Its core purpose is to prevent validators from blocking an action with an empty or missing message because a referenced prompt template file is absent.
- algorithmic_behavior: Section 1 iterates a fixed set of hook/library scripts and extracts direct calls to `load_template`, `load_and_render`, and `load_and_render_safe` with `$TEMPLATE_DIR` plus a quoted template path (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:87`). Each extracted template must exist under `prompt-template` or the test records a failure (`tests/test-template-references.sh:100`). Section 2 enumerates all `.md` templates and warns if not directly referenced by hook scripts (`tests/test-template-references.sh:121`). Section 3 asserts common block templates exist (`tests/test-template-references.sh:149`). Section 4 requires critical read/write/edit validators to use `load_and_render_safe` rather than unsafe rendering (`tests/test-template-references.sh:172`).
- inputs_outputs_state: Inputs are repository paths: `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, and `prompt-template/**/*.md`. Outputs are PASS/FAIL/WARN counters and exit code 0 only when no failures occurred (`tests/test-template-references.sh:206`, `tests/test-template-references.sh:221`).
- gates_or_invariants: All statically referenced templates must exist; critical validators must use `load_and_render_safe` for all rendering. This maps to production behavior in `hooks/lib/template-loader.sh`, where `load_template` warns and returns empty for missing files (`hooks/lib/template-loader.sh:36`), while `load_and_render_safe` emits a fallback when the template or rendered result is empty (`hooks/lib/template-loader.sh:188`).
- dependencies_and_callers: Depends on shell utilities `find`, `grep`, `sed`, and the repo’s prompt template layout. It validates templates consumed by Stop-hook gates such as plan modification, incomplete todos, git-not-clean, work-summary missing, round-contract missing, mainline drift, and code review failure (`hooks/loop-codex-stop-hook.sh:391`, `hooks/loop-codex-stop-hook.sh:444`, `hooks/loop-codex-stop-hook.sh:698`, `hooks/loop-codex-stop-hook.sh:766`, `hooks/loop-codex-stop-hook.sh:804`, `hooks/loop-codex-stop-hook.sh:1403`, `hooks/loop-codex-stop-hook.sh:1602`).
- edge_cases_or_failure_modes: The extraction is intentionally static and only catches quoted direct references after `$TEMPLATE_DIR`; dynamic references can be warned as unreferenced rather than failed. Missing critical templates fail because they would otherwise create poor or empty block output. Unsafe `load_and_render` use in critical validators fails because it lacks fallback behavior.
- validation_or_tests: This file is itself the template reference validation. It prints missing templates explicitly before failing (`tests/test-template-references.sh:213`) and exits 1 on any failure (`tests/test-template-references.sh:225`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-129 `file` `prompt-template/block/mainline-drift-stop.md`
- cursor: `[_]`
- core_role: This block template is the user-facing circuit-breaker message emitted when the RLCR implementation phase repeatedly fails to advance the mainline. It defines the terminal replan contract for drift/stagnation.
- algorithmic_behavior: The template states the loop stopped because implementation failed to advance for `{{STALL_COUNT}}` consecutive rounds, reports `{{LAST_VERDICT}}`, points at `{{PLAN_FILE}}`, sets drift status to `replan_required`, and instructs the user to re-read the plan, diagnose stalls/regressions, and start a fresh narrower RLCR loop (`prompt-template/block/mainline-drift-stop.md:1`).
- inputs_outputs_state: Inputs are template variables `STALL_COUNT`, `LAST_VERDICT`, and `PLAN_FILE`. The rendered output becomes the Stop-hook JSON `reason` when `stop_for_mainline_drift` fires (`hooks/loop-codex-stop-hook.sh:1385`). The Stop hook first upserts state fields: `mainline_stall_count`, `last_mainline_verdict`, and `drift_status=replan_required` (`hooks/loop-codex-stop-hook.sh:1389`), then ends the loop as `stop-state.md` through `end_loop`.
- gates_or_invariants: The circuit breaker is driven by extracted `Mainline Progress Verdict` values. `ADVANCED` resets counters; `STALLED` or `REGRESSED` increments them; two consecutive stalled/regressed rounds mark `replan_required`, and three trigger the stop circuit breaker (`hooks/loop-codex-stop-hook.sh:1793`, `hooks/loop-codex-stop-hook.sh:1802`, `hooks/loop-codex-stop-hook.sh:1808`). Missing verdicts block rather than guessing (`hooks/loop-codex-stop-hook.sh:1788`). `COMPLETE` suppresses drift stop and transitions toward review (`hooks/loop-codex-stop-hook.sh:1817`).
- dependencies_and_callers: Called by `stop_for_mainline_drift` in `hooks/loop-codex-stop-hook.sh`, rendered through `load_and_render_safe` with an inline fallback (`hooks/loop-codex-stop-hook.sh:1394`, `hooks/loop-codex-stop-hook.sh:1403`). Verdict normalization/extraction is in `hooks/lib/loop-common.sh`: `extract_mainline_progress_verdict` accepts only one unambiguous `ADVANCED`, `STALLED`, or `REGRESSED` verdict (`hooks/lib/loop-common.sh:621`).
- edge_cases_or_failure_modes: If the template is missing, the Stop hook uses an inline fallback. If Codex outputs multiple verdict words in one line, extraction returns `unknown`, which triggers the missing-verdict block rather than mutating drift state (`hooks/lib/loop-common.sh:637`). If `STOP` is the explicit last line, the normal STOP path is honored separately (`hooks/loop-codex-stop-hook.sh:1914`).
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`. Drift behavior is exercised in related tests referenced by the repo search: two stalled rounds produce drift recovery state and a third stalled/regressed round stops with drift messaging in `tests/test-finalize-phase.sh`; the exact template is part of the Stop-hook rendering path.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-159 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: This template defines the standard continuation prompt after an implementation-phase Codex review finds issues but does not require drift recovery or terminal stop. It is the main “next round” contract that keeps RLCR focused on mainline progress.
- algorithmic_behavior: The prompt orders the agent to re-read the original plan, goal tracker, recent round history, and then write the current round contract before coding (`prompt-template/claude/next-round-prompt.md:12`). The round contract must contain exactly one mainline objective, 1-2 target ACs, blocking issues, queued out-of-scope issues, and success criteria (`prompt-template/claude/next-round-prompt.md:20`). It defines task lanes `[mainline]`, `[blocking]`, and `[queued]`, and forbids queued work from replacing the round objective (`prompt-template/claude/next-round-prompt.md:29`). It embeds Codex’s review content between stable delimiters (`prompt-template/claude/next-round-prompt.md:48`) and instructs the agent to keep `goal-tracker.md` mutable section updated without changing the immutable section (`prompt-template/claude/next-round-prompt.md:54`).
- inputs_outputs_state: Inputs are `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `BITLESSON_FILE`, and `REVIEW_CONTENT`. Output is `.humanize/rlcr/<timestamp>/round-N-prompt.md`, generated by the Stop hook after state advances to the next round (`hooks/loop-codex-stop-hook.sh:1952`, `hooks/loop-codex-stop-hook.sh:1960`, `hooks/loop-codex-stop-hook.sh:2028`). The next summary scaffold is created alongside it if missing (`hooks/loop-codex-stop-hook.sh:1961`).
- gates_or_invariants: The template enforces “round contract before implementation” and stable mainline focus. It also requires reading the BitLesson file and running `bitlesson-selector` before each task/sub-task (`prompt-template/claude/next-round-prompt.md:42`). Stop-hook validation enforces the existence of the round contract for anti-drift loops before review can proceed (`hooks/loop-codex-stop-hook.sh:789`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe` when `DRIFT_REPLAN_REQUIRED` is false (`hooks/loop-codex-stop-hook.sh:2027`). The Stop hook later appends footer, task-tag routing reminders, push notes if enabled, goal-tracker update request guidance, and agent-team continuation instructions as applicable (`hooks/loop-codex-stop-hook.sh:2122`, `hooks/loop-codex-stop-hook.sh:2127`, `hooks/loop-codex-stop-hook.sh:2130`, `hooks/loop-codex-stop-hook.sh:2138`, `hooks/loop-codex-stop-hook.sh:2147`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, the Stop hook uses an inline fallback that still points to plan, goal tracker, round contract, BitLesson, and review content (`hooks/loop-codex-stop-hook.sh:1986`). If drift is already `replan_required`, the Stop hook uses `claude/drift-replan-prompt.md` instead (`hooks/loop-codex-stop-hook.sh:2017`). Open Questions in Codex review inject an additional notice when `ASK_CODEX_QUESTION=true` (`hooks/loop-codex-stop-hook.sh:2078`).
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`. Task lane enforcement is supported by `hooks/check-todos-from-transcript.py`, whose lane-prefix pattern accepts `[mainline]`, `[blocking]`, and `[queued]`; Stop hook blocks incomplete tasks before review (`hooks/loop-codex-stop-hook.sh:408`, `hooks/loop-codex-stop-hook.sh:434`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-189 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: This executable robustness spec validates production plan-file handling in `scripts/setup-rlcr-loop.sh`. It ensures RLCR startup rejects unsafe or meaningless plan files while accepting realistic large/rich plans.
- algorithmic_behavior: The test creates an isolated temp project, mock Codex binary, local git repo, clean commit, and `.gitignore` arrangement (`tests/robustness/test-plan-file-robustness.sh:18`, `tests/robustness/test-plan-file-robustness.sh:26`, `tests/robustness/test-plan-file-robustness.sh:112`). Its `test_plan_validation` helper invokes the production setup script with a plan path and classifies validation failure by known error messages such as “Plan is too simple”, “insufficient content”, “not found”, “not readable”, or “symbolic link” (`tests/robustness/test-plan-file-robustness.sh:46`, `tests/robustness/test-plan-file-robustness.sh:61`, `tests/robustness/test-plan-file-robustness.sh:65`).
- inputs_outputs_state: Inputs are generated plan files with different content/metadata shapes. Outputs are PASS/FAIL counters and the production side effect of setup creating `.humanize/rlcr/<timestamp>/state.md` for accepted plans (`tests/robustness/test-plan-file-robustness.sh:88`). The helper treats “requires codex”, “must be gitignored”, or successful activation/state creation as validation having passed (`tests/robustness/test-plan-file-robustness.sh:70`, `tests/robustness/test-plan-file-robustness.sh:76`, `tests/robustness/test-plan-file-robustness.sh:82`).
- gates_or_invariants: Production setup rejects absolute paths, spaces, shell metacharacters, symlink plan files, symlink parent path segments, missing files, unreadable files, paths outside project, submodule plans, dirty git status, tracked plans unless `--track-plan-file`, and plans with fewer than five lines or fewer than three content lines (`scripts/setup-rlcr-loop.sh:451`, `scripts/setup-rlcr-loop.sh:457`, `scripts/setup-rlcr-loop.sh:465`, `scripts/setup-rlcr-loop.sh:477`, `scripts/setup-rlcr-loop.sh:490`, `scripts/setup-rlcr-loop.sh:513`, `scripts/setup-rlcr-loop.sh:519`, `scripts/setup-rlcr-loop.sh:525`, `scripts/setup-rlcr-loop.sh:539`, `scripts/setup-rlcr-loop.sh:578`, `scripts/setup-rlcr-loop.sh:614`, `scripts/setup-rlcr-loop.sh:624`). The test’s local `count_content_lines` mirrors the production rule that blank lines, HTML comments, multiline HTML comments, and `#` lines are non-content (`tests/robustness/test-plan-file-robustness.sh:129`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, git, shell utilities, and `tests/test-helpers.sh`. It indirectly verifies that setup can proceed far enough to create RLCR state files with branch/base/model/drift fields (`scripts/setup-rlcr-loop.sh:876`, `scripts/setup-rlcr-loop.sh:898`) after plan validation succeeds.
- edge_cases_or_failure_modes: Positive cases include a normal plan, exactly three content lines, standard 5KB files, rich Markdown with code/tables/lists/links, >1MB files, and mixed CRLF/LF line endings (`tests/robustness/test-plan-file-robustness.sh:178`, `tests/robustness/test-plan-file-robustness.sh:206`, `tests/robustness/test-plan-file-robustness.sh:234`, `tests/robustness/test-plan-file-robustness.sh:253`, `tests/robustness/test-plan-file-robustness.sh:325`, `tests/robustness/test-plan-file-robustness.sh:355`). Negative/edge coverage includes empty files, comments-only files, binary/null bytes, very long lines, special shell characters in content, whitespace-only files, nested/complex comments, nonexistent files, unreadable files, symlinks, directories-as-files, and disappearance during validation (`tests/robustness/test-plan-file-robustness.sh:300`, `tests/robustness/test-plan-file-robustness.sh:309`, `tests/robustness/test-plan-file-robustness.sh:368`, `tests/robustness/test-plan-file-robustness.sh:390`, `tests/robustness/test-plan-file-robustness.sh:409`, `tests/robustness/test-plan-file-robustness.sh:432`, `tests/robustness/test-plan-file-robustness.sh:444`, `tests/robustness/test-plan-file-robustness.sh:468`, `tests/robustness/test-plan-file-robustness.sh:477`, `tests/robustness/test-plan-file-robustness.sh:491`, `tests/robustness/test-plan-file-robustness.sh:503`, `tests/robustness/test-plan-file-robustness.sh:514`, `tests/robustness/test-plan-file-robustness.sh:527`).
- validation_or_tests: This file is itself the validation spec and ends with `print_test_summary "Plan File Robustness Test Summary"` (`tests/robustness/test-plan-file-robustness.sh:557`). I did not run it because the assigned export is read-only and the test initializes git repos and writes fixtures; inspection was direct and complete.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-009`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-039`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-069`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-099`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-129`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-159`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-189`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`