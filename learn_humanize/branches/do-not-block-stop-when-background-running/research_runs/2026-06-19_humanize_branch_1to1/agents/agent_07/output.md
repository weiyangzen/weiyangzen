# agent_07 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-007 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Repository-wide prompt/message template corpus for RLCR state-machine transitions, stop-hook block reasons, Codex review contracts, Claude continuation/finalize prompts, methodology-analysis prompts, and plan/refine-plan schemas. It is loaded through `hooks/lib/template-loader.sh` via `load_template` / `load_and_render_safe`, with fallbacks in callers such as `hooks/loop-codex-stop-hook.sh` and `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The directory is split into four algorithmic subtrees. `prompt-template/block/*` defines guard failure messages and required recovery actions, including incomplete tasks, protected state files, git hygiene, wrong round files, BitLesson delta validation, mainline drift, and summary/contract location errors. `prompt-template/claude/*` defines Claude-side phase prompts: round continuation, drift recovery, review phase, finalize phase, agent teams, open-question notices, push reminders, and methodology analysis. `prompt-template/codex/*` defines Codex-side audit/review instructions, including regular review, full alignment review, code-review audit, commit-history context, and goal-tracker update responsibilities. `prompt-template/plan/*` defines plan generation and refine-plan QA output schemas.
- inputs_outputs_state: Inputs are placeholder variables rendered by shell callers, such as `{{INCOMPLETE_LIST}}`, `{{PLAN_FILE}}`, `{{CURRENT_ROUND}}`, `{{REVIEW_CONTENT}}`, `{{GOAL_TRACKER_FILE}}`, `{{ROUND_CONTRACT_FILE}}`, `{{BASE_BRANCH}}`, `{{EXIT_REASON}}`, and `{{LOOP_DIR}}`. Outputs are markdown prompt bodies or block reasons embedded into Claude Code hook JSON (`decision: block`, `reason`, `systemMessage`) or written as round prompt/review files. State is not stored in templates themselves, but templates describe valid transitions among `state.md`, `finalize-state.md`, `methodology-analysis-state.md`, and terminal `*-state.md` files.
- gates_or_invariants: Block templates enforce that `.humanize/` state is not committed (`block/git-add-humanize.md:1`, `block/git-tracked-humanize.md:1`), prompt/state/plan backups are read-only (`block/prompt-file-write.md:1`, `block/state-file-modification.md:1`, `block/plan-backup-protected.md:1`), native Task tools replace `round-*-todos.md` files (`block/todos-file-access.md:1`), round summaries/contracts stay in the active loop/current round (`block/wrong-round-number.md:1`, `block/wrong-summary-location.md:1`, `block/wrong-contract-location.md:1`), and mainline review verdicts are mandatory (`block/mainline-verdict-missing.md:1`). Codex templates require `COMPLETE` only when all original work is fully done with no deferrals (`codex/regular-review.md:72`, `codex/full-alignment-review.md:112`) and require a `Mainline Progress Verdict` (`codex/regular-review.md:56`, `codex/full-alignment-review.md:59`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` resolves common block messages through `load_and_render_safe` for todos, prompt writes, state-file modification, finalize contract access, summary/goal-tracker Bash writes, git-add protection, tracked `.humanize`, direct hook execution, and goal-tracker mutation (`hooks/lib/loop-common.sh:828`, `838`, `847`, `856`, `866`, `881`, `892`, `1000`, `1330`, `1394`, `1420`, `1464`). `hooks/loop-codex-stop-hook.sh` renders stop gates, Codex prompts, review/finalize prompts, drift prompts, and task-tag reminders (`hooks/loop-codex-stop-hook.sh:444`, `590`, `766`, `804`, `974`, `1057`, `1073`, `1202`, `1320`, `1348`, `2018`, `2028`, `2125`). `hooks/lib/methodology-analysis.sh` renders `claude/methodology-analysis-prompt.md` on terminal transition (`hooks/lib/methodology-analysis.sh:82`).
- edge_cases_or_failure_modes: Missing templates are intentionally not fatal where callers use `load_and_render_safe`; inline fallbacks keep hooks functional. Placeholder drift can produce confusing block text if caller variable names diverge. `test-template-references.sh` and `test-templates-comprehensive.sh` cover missing/invalid templates, while some templates are intentionally “not directly referenced” because callers load dynamically. Long prompt bodies are file-backed to avoid shell command substitution size limits in setup (`scripts/setup-rlcr-loop.sh:1342`).
- validation_or_tests: Recursive inspection found 1,426 template lines across block, Claude, Codex, and plan subtrees. `tests/test-template-references.sh` scans shell references to verify referenced template files exist. `tests/test-templates-comprehensive.sh` loads/renders templates and exercises placeholder rendering. Assigned tests also validate selected template effects: `tests/test-task-tag-routing.sh:99` checks round-0 routing text, and `tests/robustness/test-hook-system-robustness.sh:391` etc. checks hook block behavior that uses block templates.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-037 `file` `docs/usage.md`
- cursor: `[_]`
- core_role: User-facing behavior contract for Humanize commands and the RLCR/ask-codex workflows. It documents the state-machine shape that scripts/tests enforce.
- algorithmic_behavior: Defines RLCR as a two-phase loop: Implementation Phase, where Claude executes a plan and Codex reviews summaries until `COMPLETE`, then Review Phase, where `codex review --base <branch>` checks `[P0-9]` findings (`docs/usage.md:7-12`). It defines Plan Understanding Quiz as advisory pre-flight friction, not a hard gate (`docs/usage.md:16-35`). It documents the typical `gen-plan -> refine-plan -> start-rlcr-loop` flow (`docs/usage.md:43-56`) and command options for start, gen, refine, and ask-codex (`docs/usage.md:58-247`).
- inputs_outputs_state: Inputs include plan paths, `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--agent-teams`, `--skip-quiz`, `--yolo`, and ask-codex question text (`docs/usage.md:70-100`, `236-247`). Outputs include `.humanize/rlcr/<timestamp>/` progress data for loops (`docs/usage.md:323`) and `.humanize/skill/<timestamp>/{input.md,output.md,metadata.md}` for ask-codex (`docs/usage.md:253-254`).
- gates_or_invariants: The loop continues until all acceptance criteria are met or no issues remain (`docs/usage.md:12`). `--yolo` composes `--skip-quiz` and `--claude-answer-codex` (`docs/usage.md:96-98`). Base branch auto-detection order is user input, remote default, `main`, then `master` (`docs/usage.md:84-85`). Config precedence is defaults, user config, project config, CLI flags (`docs/usage.md:256-263`), while Codex model resolution prioritizes CLI, feature defaults, config defaults, then hardcoded fallback (`docs/usage.md:299-303`).
- dependencies_and_callers: Mirrors `scripts/setup-rlcr-loop.sh` CLI/state setup, `scripts/ask-codex.sh` one-shot behavior, `scripts/validate-gen-plan-io.sh`, `scripts/validate-refine-plan-io.sh`, `hooks/loop-codex-stop-hook.sh`, and `scripts/humanize.sh` monitoring. Config keys align with `config/default_config.json` and `hooks/lib/loop-common.sh` default loading (`docs/usage.md:264-307`).
- edge_cases_or_failure_modes: Quiz can be skipped intentionally, and auto-started gen-plan flows skip it because convergence discussion already checked understanding (`docs/usage.md:37-41`). `--skip-impl` makes plan optional and moves directly to code review (`docs/usage.md:89-90`). Legacy `loop_reviewer_model` keys are ignored in favor of `codex_model`/`codex_effort` (`docs/usage.md:305-307`). `HUMANIZE_CODEX_BYPASS_SANDBOX` is a dangerous mode that switches Codex sandbox behavior (`docs/usage.md:331-364`).
- validation_or_tests: `tests/test-ask-codex.sh` validates the documented ask-codex outputs and metadata. `tests/test-task-tag-routing.sh` and `tests/robustness/test-hook-system-robustness.sh` validate documented routing/state-machine behavior. `tests/test-unified-codex-config.sh` and config tests cover shared model config implied by `docs/usage.md:276-307`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-067 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/ask-codex.sh`, the one-shot Codex consultation path used directly by `/humanize:ask-codex` and indirectly by analyze-task routing.
- algorithmic_behavior: Builds a mock git project and mock `codex` binary, then exercises validation, success, error, concurrency, argument parsing, cache, and skill-guidance behavior (`tests/test-ask-codex.sh:29-75`). The script’s tested target reads CLI args, validates model/effort/timeout/question, creates `.humanize/skill/<unique-id>/`, writes `input.md`, cache debug files, runs `codex exec`, then writes `output.md` and `metadata.md` (`scripts/ask-codex.sh:86-238`, `262-298`, `391-415`).
- inputs_outputs_state: Inputs are mock env vars `MOCK_CODEX_EXIT_CODE`, `MOCK_CODEX_STDOUT`, `MOCK_CODEX_STDERR` (`tests/test-ask-codex.sh:5-10`), CLI args such as `--codex-model`, `--codex-timeout`, `--`, and question text. Outputs verified are stdout answer (`tests/test-ask-codex.sh:164-172`), project-local `output.md`, `metadata.md`, `input.md` (`tests/test-ask-codex.sh:174-199`), unique `.humanize/skill` directories (`tests/test-ask-codex.sh:286-306`), and cache files `codex-run.cmd` / `.out` (`tests/test-ask-codex.sh:382-405`).
- gates_or_invariants: Empty question exits 1 (`tests/test-ask-codex.sh:84-91`), help exits 0 (`93-100`), unknown option exits 1 (`102-109`), missing option args and non-numeric timeout fail (`111-136`), model/effort unsafe characters fail (`138-154`), Codex non-zero exit propagates exactly (`220-230`), empty Codex stdout exits 1 (`240-257`), timeout exit 124 remains 124 and records timeout metadata (`259-276`). `scripts/ask-codex.sh` enforces these with explicit regexes and status handling (`scripts/ask-codex.sh:153-186`, `309-385`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, git fixture helpers, `scripts/ask-codex.sh`, `skills/ask-codex/SKILL.md`, mock `codex`, `jq` indirectly through shared libraries, and `hooks/lib/loop-common.sh` for default Codex config (`scripts/ask-codex.sh:31-33`). It also validates skill documentation that warns against unsafe bare `$ARGUMENTS` expansion (`tests/test-ask-codex.sh:415-433`).
- edge_cases_or_failure_modes: Concurrent rapid calls must create distinct project-local and cache dirs (`tests/test-ask-codex.sh:286-320`), avoiding timestamp-only collisions through `UNIQUE_ID="${TIMESTAMP}-$$-random"` in the script (`scripts/ask-codex.sh:202-204`). Cache falls back to project-local cache when home cache is not writable (`scripts/ask-codex.sh:209-218`). The `--` separator treats later flag-shaped tokens as question text (`tests/test-ask-codex.sh:352-361`).
- validation_or_tests: This file is itself the executable validation. It does not call real Codex; the mock consumes stdin and emits configured stdout/stderr (`tests/test-ask-codex.sh:39-52`). It uses `print_test_summary` at the end (`tests/test-ask-codex.sh:440`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-097 `file` `tests/test-task-tag-routing.sh`
- cursor: `[_]`
- core_role: Executable specification that plan task routing metadata (`coding` vs `analyze`) survives from generated round-0 prompts into later stop-hook continuation prompts.
- algorithmic_behavior: Creates two fixture repos with plans containing a `Task Breakdown` table and a `Tag (coding/analyze)` column (`tests/test-task-tag-routing.sh:75-90`, `126-140`). First path runs `scripts/setup-rlcr-loop.sh` and inspects generated `round-0-prompt.md` plus `goal-tracker.md` (`tests/test-task-tag-routing.sh:93-114`). Second path creates a synthetic active loop and state, runs `hooks/loop-codex-stop-hook.sh`, then inspects `round-1-prompt.md` (`tests/test-task-tag-routing.sh:121-237`).
- inputs_outputs_state: Inputs are plan markdown, mock Codex output, `state.md` fields including current round, max iterations, plan file, branches, review flags, and goal tracker rows (`tests/test-task-tag-routing.sh:153-190`). Outputs are `round-0-prompt.md`, `goal-tracker.md`, and `round-1-prompt.md`. The tested setup prompt has a “Task Tag Routing (MUST FOLLOW)” section and maps `analyze` tasks to `/humanize:ask-codex` (`scripts/setup-rlcr-loop.sh:1331-1338`). Continuation prompts append “Task Tag Routing Reminder” via `append_task_tag_routing_note` (`hooks/loop-codex-stop-hook.sh:1369-1383`, `2127`).
- gates_or_invariants: Round-0 prompt must include routing section (`tests/test-task-tag-routing.sh:99-103`), round-0 prompt must mention `/humanize:ask-codex` for analyze tasks (`105-109`), goal tracker Active Tasks table must include `Tag` and `Owner` columns (`111-115`), stop-hook follow-up prompt must include routing reminder and ask-codex instruction (`227-237`). If task lacks an explicit tag, setup prompt says default to `coding` (`scripts/setup-rlcr-loop.sh:1338`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `tests/test-helpers.sh`, fixture git repos, and a mock `codex` binary that supports both `exec` and `review` subcommands (`tests/test-task-tag-routing.sh:25-50`). It validates behavior described in `docs/usage.md:91-97` and `prompt-template/plan/gen-plan-template.md:71-78`.
- edge_cases_or_failure_modes: The second fixture intentionally omits `codex_model` in `state.md`; the stop hook should derive config defaults rather than dropping routing behavior (`tests/test-task-tag-routing.sh:155`). Non-`COMPLETE` Codex output containing `CONTINUE` should block exit and generate next-round instructions, not transition to review/finalize (`tests/test-task-tag-routing.sh:214-225`).
- validation_or_tests: This file is the test. It confirms routing in both setup-generated first prompt and stop-hook-generated follow-up prompt. It ends with `print_test_summary "Task Tag Routing Tests"` (`tests/test-task-tag-routing.sh:239`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-127 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the “incomplete native tasks” gate, preventing expensive Codex review before the current Task system work is marked complete.
- algorithmic_behavior: Renders a block reason headed “Incomplete Tasks Detected”, inserts `{{INCOMPLETE_LIST}}`, and instructs Claude to complete remaining tasks via `TaskUpdate` before stopping (`prompt-template/block/incomplete-todos.md:1-10`). The stop hook runs `hooks/check-todos-from-transcript.py` before Codex review (`hooks/loop-codex-stop-hook.sh:401-413`) and, when incomplete tasks exist, renders this template (`hooks/loop-codex-stop-hook.sh:444`).
- inputs_outputs_state: Input placeholder is `{{INCOMPLETE_LIST}}` (`prompt-template/block/incomplete-todos.md:5`), populated from transcript-derived task state. Output is a markdown block message embedded as the hook JSON `reason`, with `decision: block` and a system message indicating incomplete tasks. State transition is intentionally “no transition”: loop remains active and does not run Codex review.
- gates_or_invariants: All remaining tasks must be completed and marked completed using `TaskUpdate` before stop (`prompt-template/block/incomplete-todos.md:7-10`). The template explicitly says not to proceed to Codex review until all tasks are finished (`prompt-template/block/incomplete-todos.md:12`), preserving the invariant that Codex reviews completed summaries rather than in-progress task state.
- dependencies_and_callers: Depends on `hooks/check-todos-from-transcript.py` for task extraction, `hooks/loop-codex-stop-hook.sh` for gate sequencing, and `hooks/lib/template-loader.sh` for render/fallback. It complements `prompt-template/block/todos-file-access.md`, which forbids `round-*-todos.md` and routes work to native Task tools.
- edge_cases_or_failure_modes: If the transcript checker is missing, the stop hook skips this specific gate; if checker output misclassifies tasks, the hook may block too aggressively or too weakly. This gate must run before Codex review to avoid token-wasting review on incomplete work, and it must not block `[queued]` tasks if they are allowed elsewhere by lane semantics.
- validation_or_tests: Covered indirectly by stop-hook tests and template tests. `tests/test-templates-comprehensive.sh` ensures the template loads/renders. Stop-hook robustness includes active-loop blocking behavior before normal exit review (`tests/robustness/test-hook-system-robustness.sh:726-780`), though this assigned robustness file primarily focuses broader hook resilience.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-157 `file` `prompt-template/claude/methodology-analysis-prompt.md`
- cursor: `[_]`
- core_role: Terminal-phase prompt template for methodology improvement analysis, inserted after loop success/stop/max-iteration exit but before the loop is fully terminalized.
- algorithmic_behavior: Instructs Claude to spawn an Opus agent to analyze `round-*-summary.md` and `round-*-review-result.md` from a methodology-only perspective, write `methodology-analysis-report.md`, derive all user-facing content only from that report, optionally ask the user about filing a sanitized GitHub issue, and write `methodology-analysis-done.md` before exiting (`prompt-template/claude/methodology-analysis-prompt.md:12-73`).
- inputs_outputs_state: Inputs are `{{EXIT_REASON}}`, `{{EXIT_REASON_DESCRIPTION}}`, `{{CURRENT_ROUND}}`, `{{MAX_ITERATIONS}}`, and `{{LOOP_DIR}}` (`prompt-template/claude/methodology-analysis-prompt.md:5-20`). Outputs are `methodology-analysis-report.md`, optional GitHub issue draft/create command, and `methodology-analysis-done.md` (`prompt-template/claude/methodology-analysis-prompt.md:20`, `62-69`, `71-73`). State transition is implemented by `enter_methodology_analysis_phase`, which renames active state to `methodology-analysis-state.md`, records `.methodology-exit-reason`, touches an empty done marker, and blocks with this prompt (`hooks/lib/methodology-analysis.sh:38-99`).
- gates_or_invariants: Sanitization is strict: no file paths, function/class/variable names, branch names, hashes, domain/product terms, snippets, raw errors, URLs, or project-identifying info (`prompt-template/claude/methodology-analysis-prompt.md:22-30`). User-facing content must be derived only from the sanitized report, not raw records (`prompt-template/claude/methodology-analysis-prompt.md:43-46`). Completion marker must contain meaningful content before exit (`prompt-template/claude/methodology-analysis-prompt.md:71-73`); implementation additionally requires a non-empty report and valid saved exit reason before renaming to terminal state (`hooks/lib/methodology-analysis.sh:114-173`).
- dependencies_and_callers: Rendered by `hooks/lib/methodology-analysis.sh:82` and entered from stop-hook paths on `complete`, `stop`, or `maxiter` when privacy mode allows (`hooks/loop-codex-stop-hook.sh:1837`, `1941`; code-review success enters finalize then methodology depending phase flow). Protected by edit/write/bash validators that allow only methodology artifacts during this phase (`hooks/loop-edit-validator.sh:70-112`, `hooks/loop-write-validator.sh:87-131`, `hooks/loop-bash-validator.sh:81-153`).
- edge_cases_or_failure_modes: `privacy_mode: true` skips analysis (`hooks/lib/methodology-analysis.sh:42-46`). Re-entry is avoided if `methodology-analysis-state.md` already exists or a non-empty done file exists (`hooks/lib/methodology-analysis.sh:48-61`). Missing/empty report, empty done marker, missing `.methodology-exit-reason`, or invalid exit reason blocks terminalization (`hooks/lib/methodology-analysis.sh:118-162`). Spawned agents may not share the same session id, so validators note sanitization is enforced by prompt rather than hook restrictions for spawned agents (`hooks/loop-edit-validator.sh:64-67`, `hooks/loop-bash-validator.sh:72-75`).
- validation_or_tests: `tests/robustness/test-hook-system-robustness.sh` validates methodology-phase write/edit/bash restrictions indirectly through validators; template loading is covered by comprehensive template tests. The methodology library itself enforces non-empty completion/report checks at `hooks/lib/methodology-analysis.sh:118-143`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-187 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for the RLCR hook system, especially JSON handling, protected loop state, goal-tracker immutability, command-injection resistance, concurrent hook invocations, and stop-hook behavior on missing/corrupt/active state.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and helpers (`tests/robustness/test-hook-system-robustness.sh:19-23`), then exercises `loop-edit-validator.sh`, `loop-plan-file-validator.sh`, `loop-bash-validator.sh`, `loop-write-validator.sh`, `loop-read-validator.sh`, and `loop-codex-stop-hook.sh`. It creates fixture `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, plans, mock git repos, and mock Codex binaries to drive realistic hook paths.
- inputs_outputs_state: Inputs are Claude hook JSON payloads for `Edit`, `Write`, `Bash`, and `Read` (`tests/robustness/test-hook-system-robustness.sh:41`, `67`, `96`, `386`, `453`, `541`), state-file frontmatter fixtures (`57-65`, `128-142`, `182-196`, `731-745`), and mock Codex output (`761-767`). Outputs under test are hook exit codes, block JSON with `decision: block`, preserved/renamed state files such as `unexpected-state.md`, and unchanged state after concurrent reads.
- gates_or_invariants: Edit to `state.md` is blocked (`tests/robustness/test-hook-system-robustness.sh:52-77`), malformed JSON exits non-crashing (`79-91`), path traversal to `state.md` is blocked (`123-154`), valid plan file edits pass while non-plan file edits pass through (`164-229`), Bash cannot modify `state.md` (`316-400`) or `goal-tracker.md` after round 0 (`402-416`), Write/Edit may update only the mutable goal-tracker section after round 0 (`418-536`), old-session goal tracker reads are blocked (`538-550`), unrelated dangerous Bash commands are allowed through for sandbox responsibility (`552-565`), and binary/large/nested JSON cases do not crash (`567-832`).
- dependencies_and_callers: Directly exercises validators whose shared logic lives in `hooks/lib/loop-common.sh`: JSON validation (`validate_hook_input`, `is_deeply_nested` at `hooks/lib/loop-common.sh:91-165`), state parsing (`get_current_round`, `parse_state_file_strict` at `417-587`), goal-tracker immutable extraction and preview editing (`910-955`), state-path detection (`982-1000`), and command modification detection (`1435-1460`). It also exercises the stop hook’s active-loop detection and schema validation (`hooks/loop-codex-stop-hook.sh:61-83`, `107-270`).
- edge_cases_or_failure_modes: Missing state directory allows exit with no block (`tests/robustness/test-hook-system-robustness.sh:644-658`). Corrupted state should not crash (`660-681`). Missing critical fields should end/allow gracefully as unexpected rather than looping forever (`683-724`). Complete valid active loop must block exit and continue the review flow (`726-780`). Concurrent validator invocations must not corrupt state (`604-634`). Large JSON is capped by timeout and should exit below 128 (`790-802`); escaped null and nested JSON must be handled (`804-832`).
- validation_or_tests: This file is itself a validation suite. It ends with `print_test_summary "Hook System Robustness Test Summary"` and exits with the summary status (`tests/robustness/test-hook-system-robustness.sh:834-839`). Related branch-specific background-stop behavior is further specified by `tests/test-stop-hook-bg-allow.sh`, whose helper implementation is in `hooks/lib/loop-bg-tasks.sh`; the stop hook runs that short-circuit before normal gates (`hooks/loop-codex-stop-hook.sh:72-83`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7/7 item evidence sections present; item IDs appear only in their section headings`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`