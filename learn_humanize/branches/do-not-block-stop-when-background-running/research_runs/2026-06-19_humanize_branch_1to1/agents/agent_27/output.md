# agent_27 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-027 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Lightweight policy agent for the `gen-plan` workflow. It decides whether a user draft is semantically related to the current repository before plan generation proceeds.
- algorithmic_behavior: The prompt defines a three-step classifier: explore repo docs/structure, compare draft content against repo concepts/components/files/features, then emit a strict verdict prefix: `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:14-29`). Its decision policy is intentionally permissive: informal drafts and multilingual rough ideas are acceptable, and uncertainty should resolve to relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft document content supplied by the caller plus repository-readable context via `Read`, `Glob`, and `Grep` tools (`agents/draft-relevance-checker.md:5`). Output is a single verdict line with a short explanation (`agents/draft-relevance-checker.md:27-29`). It does not write state or transition repo files.
- gates_or_invariants: Main invariant is strict output contract: downstream logic distinguishes `RELEVANT` from `NOT_RELEVANT`. The classifier gate is lenient by design, blocking only drafts that are clearly unrelated. It uses the `haiku` model for low-cost fast classification (`agents/draft-relevance-checker.md:4`).
- dependencies_and_callers: Called by `commands/gen-plan.md` Phase 2 after IO validation (`commands/gen-plan.md:154-169`). If `NOT_RELEVANT`, the command reports the reason and stops (`commands/gen-plan.md:171-174`). If relevant, the plan template is copied and original draft appended before Codex first-pass analysis (`commands/gen-plan.md:176-180`). Upstream IO validation is performed by `scripts/validate-gen-plan-io.sh`, which validates required args, input existence/non-empty, output dir, output nonexistence, write permission, and template existence with exit codes 1-7 (`scripts/validate-gen-plan-io.sh:35-79`, `scripts/validate-gen-plan-io.sh:108-178`).
- edge_cases_or_failure_modes: A semantically related but syntactically different draft should pass. A draft about unrelated real-world topics should fail. Failure mode is output not matching the required prefixes, because the caller’s branch logic depends on those labels. Another edge is overblocking: the prompt explicitly biases against it.
- validation_or_tests: `tests/test-gen-plan.sh` verifies the agent file exists (`tests/test-gen-plan.sh:106-116`), has expected name (`tests/test-gen-plan.sh:248-260`), uses `haiku` (`tests/test-gen-plan.sh:263-274`), follows naming convention (`tests/test-gen-plan.sh:365-376`), has required frontmatter (`tests/test-gen-plan.sh:420-426`), has a valid model (`tests/test-gen-plan.sh:522-531`), and contains English-only/no emoji/CJK (`tests/test-gen-plan.sh:535-553`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-057 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Status-line renderer for Claude Code sessions, with one Humanize-specific field: `RLCR`. It is mostly generic session/cost/context display; the file itself states only the final `RLCR` field is repository-related (`scripts/statusline.sh:7-14`).
- algorithmic_behavior: Reads one JSON object from stdin (`scripts/statusline.sh:19`), extracts model/cwd/session/cost/context fields through `jq` (`scripts/statusline.sh:21-24`, `scripts/statusline.sh:169-174`), derives git branch (`scripts/statusline.sh:237-241`), formats cost/duration/line deltas (`scripts/statusline.sh:247-255`), resolves custom session names from transcript JSONL or sessions index (`scripts/statusline.sh:178-218`), resolves fast mode from `~/.claude/settings.json` (`scripts/statusline.sh:220-232`), computes an RLCR display state, then greedily wraps colored fields to 75% terminal width (`scripts/statusline.sh:376-403`).
- inputs_outputs_state: Inputs are stdin JSON fields `.model.display_name`, `.cwd`, `.session_id`, `.transcript_path`, `.cost.*`, and `.context_window.used_percentage`; local files under `$CWD/.humanize/rlcr`; git branch from `$CWD`; Claude transcript/session index files under `$HOME/.claude/projects`; and `~/.claude/settings.json`. Output is one or more terminal-formatted status lines with ANSI color escapes. It does not mutate state.
- gates_or_invariants: RLCR display precedence is `methodology-analysis-state.md` => `Analyzing`, `finalize-state.md` => `Finalizing`, `state.md` => `Active`, any other `*-state.md` => capitalized terminal reason, otherwise `Off` (`scripts/statusline.sh:36-63`). Session-aware filtering invariant: if any state file contains `session_id`, session-unaware entries are ignored; with a supplied session id, newest directories are scanned until matching frontmatter is found (`scripts/statusline.sh:76-155`). Without a supplied session id and no session-aware files, only the newest directory is considered for zombie-loop protection (`scripts/statusline.sh:82-92`). Colors encode state severity/status (`scripts/statusline.sh:157-167`).
- dependencies_and_callers: Depends on `jq`, `git`, `grep`, `awk`, `sed`, `stty`, `tput`, and shell filesystem globbing. The RLCR logic comments say it mirrors `find_active_loop()` from `hooks/lib/loop-common.sh` (`scripts/statusline.sh:65-67`). Repository search found no caller or installer reference beyond this script path; it appears to be an optional status-line script rather than core hook runtime.
- edge_cases_or_failure_modes: Missing or invalid JSON values become empty/default via `jq ... // empty`; missing `.humanize` yields `RLCR: Off` (`scripts/statusline.sh:257-262`). Missing transcript falls back to a derived `$HOME/.claude/projects/<cwd>/<sid>.jsonl` path (`scripts/statusline.sh:184-191`). Session id frontmatter parsing strips spaces only (`scripts/statusline.sh:110-112`, `scripts/statusline.sh:138-140`), so unusual YAML formatting may not match. `grep -rqE "$rlcr_dir"/*/*.md` can silently fail when no files match, which falls back to non-session-aware behavior (`scripts/statusline.sh:76-80`). Terminal width fallback is 80 cols when `/dev/tty`/`tput` fail (`scripts/statusline.sh:379-382`).
- validation_or_tests: No direct test file or install caller for `scripts/statusline.sh` was found. Indirectly, the state naming it consumes (`state.md`, `finalize-state.md`, `methodology-analysis-state.md`, terminal `*-state.md`) is central to RLCR monitor/loop tests, but this renderer itself is not asserted in the searched test suite.
- skip_candidate: `yes: mostly generic/optional status-line renderer; algorithmic relevance is limited to read-only RLCR state classification and display, not workflow mutation or validation gates`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-087 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for monitor cleanup, graceful stop, directory deletion detection, terminal restoration, and signal handling in `scripts/humanize.sh`.
- algorithmic_behavior: Creates a temp test workspace under `/tmp/test-monitor-runtime-$$`, installs cleanup trap, then runs nine shell-level tests with pass/fail counters (`tests/test-monitor-runtime.sh:45-53`, `tests/test-monitor-runtime.sh:22-34`). It builds small generated scripts that source or model parts of `scripts/humanize.sh` and verifies observable output/grep contracts.
- inputs_outputs_state: Inputs are the repository root, generated `.humanize/rlcr/.../state.md`, generated temporary test scripts, and `scripts/humanize.sh`. Outputs are PASS/FAIL lines, final summary counts, and exit code 0 only when `TESTS_FAILED` is zero (`tests/test-monitor-runtime.sh:492-513`). State is limited to the temp directory, removed on EXIT (`tests/test-monitor-runtime.sh:49-53`).
- gates_or_invariants: Cleanup must be idempotent via `cleanup_done` (`tests/test-monitor-runtime.sh:143-177`). `_graceful_stop` must call `_cleanup`, print `Monitoring stopped:`, and include the deletion reason (`tests/test-monitor-runtime.sh:94-134`). Main loop must continue while `.humanize/rlcr` exists and stop after deletion (`tests/test-monitor-runtime.sh:186-251`). `_restore_terminal` must exist and reset scroll region with `printf "\033[r"` (`tests/test-monitor-runtime.sh:263-303`). `_cleanup` must call `_restore_terminal` and reset INT/TERM traps (`tests/test-monitor-runtime.sh:305-323`, `tests/test-monitor-runtime.sh:411-423`). Bash and zsh signal handlers must trigger cleanup (`tests/test-monitor-runtime.sh:326-409`, `tests/test-monitor-runtime.sh:426-490`).
- dependencies_and_callers: Included in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:70-82`). Depends on Bash, optional zsh, grep, chmod, sleep, kill, and the implementation in `scripts/humanize.sh`. The implementation under test defines `_restore_terminal`, `_cleanup`, `_graceful_stop`, bash traps, zsh `TRAPINT`/`TRAPTERM`, and loop-dir deletion check (`scripts/humanize.sh:706-777`, `scripts/humanize.sh:784-795`, `scripts/humanize.sh:838-844`).
- edge_cases_or_failure_modes: Test 4 contains a pipeline `grep ... | grep ...` whose first grep output is only a boolean stream, so that inner conditional is ineffective; the later direct `grep -A30` check covers the same invariant (`tests/test-monitor-runtime.sh:282-287`, `tests/test-monitor-runtime.sh:305-311`). zsh test is skipped when zsh is unavailable (`tests/test-monitor-runtime.sh:432-490`). Noninteractive terminal behavior is not fully exercised; scroll reset is verified by source grep rather than actual terminal state.
- validation_or_tests: This file is itself validation. It validates the branch behavior implied by “do not block stop when background running”: graceful stop and cleanup are allowed to complete cleanly when runtime state disappears or signals occur, restoring terminal state instead of hanging.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-117 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Rendered block message for the Bash/git-add guard that prevents local `.humanize/` loop state from entering version control.
- algorithmic_behavior: The template explains that `.humanize/` is local loop state, already gitignored, and the attempted command was blocked because it would add those files (`prompt-template/block/git-add-humanize.md:1-7`). It gives allowed alternatives using specific paths or patch mode (`prompt-template/block/git-add-humanize.md:8-15`) and enumerates blocked broad/direct commands (`prompt-template/block/git-add-humanize.md:16-25`).
- inputs_outputs_state: Inputs are none at render time for this template; it is static Markdown loaded by `load_and_render_safe`. Output is a human-facing blocking diagnostic printed to stderr by the guard. It does not update state.
- gates_or_invariants: The behavioral gate is implemented in `git_adds_humanize`: detect `git add` segments, strip quotes, block direct `.humanize` path references without overblocking `.humanizeconfig`/`.humanize-backup`, detect force/all/broad scope flags, block `git add -A/--all` when `.humanize` exists, and block broad `.`/`*` when `.humanize` is not ignored (`hooks/lib/loop-common.sh:1250-1326`). The template preserves a user escape path for intentionally adding `.humanize*` to `.gitignore`, but warns the commit message must not contain the literal protected string (`prompt-template/block/git-add-humanize.md:26-34`).
- dependencies_and_callers: Rendered by `git_add_humanize_blocked_message()` via `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` (`hooks/lib/loop-common.sh:1328-1366`). The blocking function is called by `hooks/loop-bash-validator.sh` when a command attempts a protected git add (`hooks/loop-bash-validator.sh:229-230`). Related tracked-state guard uses `git_has_tracked_humanize_state` to block already staged/tracked `.humanize` during stop-hook processing (`hooks/lib/loop-common.sh:1368-1388`, `hooks/loop-codex-stop-hook.sh:639-641`).
- edge_cases_or_failure_modes: Force flags plus broad scope are blocked even if gitignore would otherwise protect `.humanize` because force bypasses ignores (`hooks/lib/loop-common.sh:1275-1303`). Non-force broad add is allowed when `.humanize` does not exist or is ignored (`hooks/lib/loop-common.sh:1305-1321`). Pattern intentionally allows similarly named non-state paths (`.humanizeconfig`, `.humanize-backup`) (`hooks/lib/loop-common.sh:1267-1272`). Chained commands are split and analyzed segment-by-segment (`hooks/lib/loop-common.sh:1234-1246`).
- validation_or_tests: `tests/test-humanize-escape.sh` covers path variants, quoted `.humanize`, RLCR subpaths, force/all flags, chained commands, allowed project paths, `.gitignore`, and non-state lookalike filenames (`tests/test-humanize-escape.sh:41-90`, `tests/test-humanize-escape.sh:124-159`, `tests/test-humanize-escape.sh:181-196`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-147 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Rendered block message for the read validator when an agent tries to read a stale/non-current RLCR round prompt/summary/contract file.
- algorithmic_behavior: The template reports the requested `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, states the current round, lists current prompt and summary paths, and gives an explicit `cat {{FILE_PATH}}` override if the old file is truly needed (`prompt-template/block/wrong-round-file.md:1-9`). It is a human-facing explanation for a hard hook exit.
- inputs_outputs_state: Inputs are template variables `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, supplied by the read validator (`hooks/loop-read-validator.sh:291-296`). Output is rendered Markdown to stderr; the validator exits with code 2 (`hooks/loop-read-validator.sh:285-298`). No state mutation.
- gates_or_invariants: The read hook only applies to `Read` tool calls after JSON validation and required `file_path` check (`hooks/loop-read-validator.sh:23-47`). It extracts filename/round/type, validates file location first, then blocks when the requested round differs from `CURRENT_ROUND` unless the file is allowlisted (`hooks/loop-read-validator.sh:250-298`). The invariant is that active loop agents should read current round files by default, preventing stale prompt/summary/contract context from driving behavior.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-read-validator.sh` (`hooks/loop-read-validator.sh:291`). Depends on shared helpers from `hooks/lib/loop-common.sh`, including hook input validation, active loop detection, file type checks, round extraction, and allowlist logic (`hooks/loop-read-validator.sh:15-18`, `hooks/loop-read-validator.sh:49-82`). Related write/edit validators use a separate `wrong-round-number.md` template for mutation attempts, so this file is read-specific.
- edge_cases_or_failure_modes: If the path is outside `.humanize/rlcr`, `wrong-file-location.md` is rendered first instead of this template (`hooks/loop-read-validator.sh:265-279`). If the filename has no parseable round number, the validator exits 0 (`hooks/loop-read-validator.sh:250-253`). Allowlisted historical files can bypass wrong-round blocking (`hooks/loop-read-validator.sh:285`). The override suggestion uses `cat`, which shifts the user into Bash command validation rather than the Read hook.
- validation_or_tests: `tests/test-allowlist-validators.sh` verifies read validator allows allowlisted historical round summaries/current contracts and blocks non-allowlisted stale summaries with a round error (`tests/test-allowlist-validators.sh:307-356`). Template rendering is also checked by template loader/comprehensive tests for wrong-round-number style templates, while this specific read template is referenced directly by the hook.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-177 `file` `skills/humanize-gen-plan/SKILL.md`
- cursor: `[_]`
- core_role: Flow-level skill definition for generating a structured implementation plan from a draft, including IO validation, relevance gating, issue clarification, metric confirmation, plan writing, review, and language unification.
- algorithmic_behavior: The Mermaid flow starts with `validate-gen-plan-io.sh --input <draft> --output <plan>`, stops on validation failure, reads the draft, checks repository relevance, stops if unrelated, analyzes for clarity/consistency/completeness/functionality, loops through user questions until issues are resolved, confirms quantitative metrics when present, generates the plan, writes it using an edit-preserving step, reviews/fixes inconsistencies, asks for language unification if multiple languages are present, then reports success (`skills/humanize-gen-plan/SKILL.md:19-47`).
- inputs_outputs_state: Required inputs are `--input <path/to/draft.md>` and `--output <path/to/plan.md>` (`skills/humanize-gen-plan/SKILL.md:49-54`). Output is a `plan.md` with title, goal, AC-X acceptance criteria with positive/negative tests, path boundaries, allowed/prohibited choices, dependencies/milestones, and implementation notes (`skills/humanize-gen-plan/SKILL.md:55-94`). The installer hydrates `{{HUMANIZE_RUNTIME_ROOT}}` as an absolute runtime root (`skills/humanize-gen-plan/SKILL.md:13-17`).
- gates_or_invariants: Validation exit codes 0-7 are part of the skill contract (`skills/humanize-gen-plan/SKILL.md:96-108`). Output structure must include acceptance criteria and boundaries, and code should not contain plan terminology (`skills/humanize-gen-plan/SKILL.md:65-94`). The flow is user-invocable false, model invocation disabled, and intended as a controlled flow definition rather than freeform generation (`skills/humanize-gen-plan/SKILL.md:1-7`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh` for path/template gates and likely on the `draft-relevance-checker` agent described above for the relevance decision. Installed/synced by `scripts/install-skill.sh`, which includes `humanize-gen-plan` in the skill list and validates expected skill roots (`scripts/install-skill.sh:6`, `scripts/install-skill.sh:41`, `scripts/install-skill.sh:106-110`). Documentation references installation and usage in Codex/Kimi skill dirs.
- edge_cases_or_failure_modes: Validation failure stops before reading/writing. Existing output path is rejected, preventing overwrite. Drafts with unresolved ambiguity create a user-question loop. Quantitative metrics require confirmation as hard requirement vs trend. Multiple-language output requires user unification before success. Unsupported/absent translated variant config is handled in the broader `commands/gen-plan.md` flow, which defaults to no translated variant when `alternative_plan_language` is empty/English/unsupported (`commands/gen-plan.md:120-128`).
- validation_or_tests: `tests/test-gen-plan.sh` validates related gen-plan command/agent frontmatter, naming, model, and content conventions; `scripts/validate-gen-plan-io.sh` provides executable validation for the skill’s first gate. Install docs and `scripts/install-skill.sh` provide installation surface validation for the skill package.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`