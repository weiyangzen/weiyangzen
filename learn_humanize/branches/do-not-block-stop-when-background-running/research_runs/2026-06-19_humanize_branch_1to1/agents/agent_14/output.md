# agent_14 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-014 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Central catalog of human-facing block/guard templates for RLCR stop, read/write/edit/bash validators, review failure handling, drift control, git hygiene, BitLesson validation, and loop state protection. The directory is not executable logic, but it defines the reason payloads that algorithmic gates return to Claude when a transition is blocked.
- algorithmic_behavior: The templates are loaded by `hooks/lib/template-loader.sh` through `load_template`, `load_and_render`, and especially `load_and_render_safe`, which substitutes `{{VAR}}` placeholders and falls back to embedded messages when a template is absent or empty. Key caller examples include stop-hook gates for incomplete tasks, git status failure, large files, dirty git state, unpushed commits, missing summaries/contracts, Codex review failure, and mainline drift in `hooks/loop-codex-stop-hook.sh`; access and mutation blockers in `hooks/lib/loop-common.sh`; bash command blockers in `hooks/loop-bash-validator.sh`; and BitLesson delta failures in `scripts/bitlesson-validate-delta.sh`.
- inputs_outputs_state: Inputs are template names plus caller-provided variables such as `MAX_LINES`, `LARGE_FILES`, `GIT_STATUS_EXIT`, `GIT_ISSUES`, `SPECIAL_NOTES`, `SUMMARY_FILE`, `ROUND_CONTRACT_FILE`, `PLAN_FILE`, `STALL_COUNT`, `LAST_VERDICT`, and `CORRECT_PATH`. Outputs are Markdown reason strings consumed by hooks that emit JSON block decisions or stderr validator failures. The templates do not mutate state directly; their rendered output drives upstream state transitions such as “stop blocked,” “loop stopped for drift,” or “validator rejects write/bash operation.”
- gates_or_invariants: The directory encodes invariants including prompt files are read-only, `state.md`/finalize/methodology state are system-managed, `.humanize/` state must not be staged or committed, broad `git add` patterns are unsafe, summaries/contracts must be written through Write/Edit rather than Bash, current-round file access must match the active loop directory, git pushes are blocked unless push-every-round is enabled, and Codex review must be retried when the review output is missing or malformed. Examples: `prompt-template/block/prompt-file-write.md:1`, `prompt-template/block/state-file-modification.md:1`, `prompt-template/block/git-add-humanize.md:1`, `prompt-template/block/git-not-clean.md:1`, `prompt-template/block/mainline-verdict-missing.md:1`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, whose `render_template` is single-pass and keeps missing placeholders unchanged (`hooks/lib/template-loader.sh:48-136`) and whose safe loader emits fallbacks on missing/empty templates (`hooks/lib/template-loader.sh:185-211`). The directory has 42 files and no subdirectories. Caller coverage is broad: `hooks/loop-codex-stop-hook.sh:391`, `hooks/loop-codex-stop-hook.sh:444`, `hooks/loop-codex-stop-hook.sh:510`, `hooks/loop-codex-stop-hook.sh:590`, `hooks/loop-codex-stop-hook.sh:698`, `hooks/loop-codex-stop-hook.sh:766`, `hooks/loop-codex-stop-hook.sh:804`, `hooks/loop-codex-stop-hook.sh:1403`, `hooks/loop-codex-stop-hook.sh:1441`, `hooks/loop-codex-stop-hook.sh:1602`; `hooks/lib/loop-common.sh:826-898`, `hooks/lib/loop-common.sh:1392-1429`, `hooks/lib/loop-common.sh:1478`; and `hooks/loop-bash-validator.sh:217`, `hooks/loop-bash-validator.sh:480`, `hooks/loop-bash-validator.sh:534`.
- edge_cases_or_failure_modes: Missing templates are intentionally non-fatal because callers provide fallbacks; malformed placeholders remain literal or render non-empty rather than crashing; empty templates may cause fallback behavior; variable values containing `{{OTHER}}` are not recursively expanded, preventing prompt injection via substituted content. The directory also has a stale-template risk: if template text diverges from caller fallback text, user-facing instructions may differ while the gate logic stays the same.
- validation_or_tests: Template presence/reference tests include `tests/test-template-references.sh`, comprehensive loading/rendering tests include `tests/test-templates-comprehensive.sh`, and failure-mode coverage is in `tests/robustness/test-template-error-robustness.sh:34-340`. `tests/test-template-loader.sh` exercises real block templates such as `block/git-push.md` and `block/wrong-round-number.md`. Not executed in this read-only research run; inspected as executable specifications.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-044 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: PostToolUse Bash hook that records the Claude Code `session_id` into the active RLCR `state.md` immediately after `setup-rlcr-loop.sh` finishes. This is part of session isolation: later validators and stop hooks can distinguish the team leader/main session from spawned agents or other Claude sessions.
- algorithmic_behavior: Reads hook JSON from stdin, resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, then checks `.humanize/.pending-session-id` (`hooks/loop-post-bash-hook.sh:26-35`). The signal file must contain a state-file path and command signature (`hooks/loop-post-bash-hook.sh:40-48`). If present, the hook verifies that the Bash command actually starts with the exact setup script path, quoted or unquoted, followed by end-of-string or whitespace (`hooks/loop-post-bash-hook.sh:56-101`). It normalizes repeated slashes in both command and signature to avoid false negatives from plugin-root path concatenation (`hooks/loop-post-bash-hook.sh:70-78`). It then extracts `.session_id` with `jq`, replaces an empty `session_id:` line using `awk`, and removes the signal (`hooks/loop-post-bash-hook.sh:104-133`).
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/.pending-session-id`, and the target `state.md`. Outputs are a patched `state.md` where `session_id:` becomes `session_id: <hook session id>`, plus deletion of `.humanize/.pending-session-id`. State transition is one-shot: `pending signal + blank state session_id` to `recorded session_id + no signal`.
- gates_or_invariants: It only consumes the signal for a real setup invocation. Non-setup Bash commands, echoing the setup path, basename-only references, quoted-prefix concatenation such as `"setup.sh"foo`, missing `tool_input.command`, missing `session_id`, or an already-populated `session_id` must not overwrite state. It cleans up only invalid signals that point to a missing/empty state path (`hooks/loop-post-bash-hook.sh:50-53`). It uses `awk -v` rather than sed replacement so session ids containing `/` or `&` are safe (`hooks/loop-post-bash-hook.sh:120-128`).
- dependencies_and_callers: Registered as a PostToolUse Bash hook in `hooks/hooks.json:52-58`. The setup script creates the signal after writing `state.md`; `scripts/setup-rlcr-loop.sh:892` writes the blank `session_id:` field and `scripts/setup-rlcr-loop.sh:905-914` writes `.pending-session-id` with the state path and resolved setup script path. Downstream session-aware filtering is implemented in `hooks/lib/loop-common.sh:237-242` and `hooks/lib/loop-common.sh:295-372`, and used by bash/read/write/edit/plan/stop validators.
- edge_cases_or_failure_modes: If `jq` is unavailable, command/session extraction fails and the signal is left for a later valid hook event. If the signal references a missing state file, the signal is removed to avoid a permanent stale block. If no `session_id` exists in hook input, the signal remains. If a current state already has a session id, the hook removes the signal but does not overwrite. Boundary checks prevent race/false-positive consumption by unrelated Bash commands.
- validation_or_tests: `tests/test-session-id.sh` covers setup signal creation (`tests/test-session-id.sh:84-91`), successful recording and signal removal (`tests/test-session-id.sh:110-160`), no-op without signal (`tests/test-session-id.sh:168-199`), rejection of non-setup commands (`tests/test-session-id.sh:464-507`), acceptance of setup commands (`tests/test-session-id.sh:512-527`), special-character session ids (`tests/test-session-id.sh:531-572`), false-positive path echoes and basename-only commands (`tests/test-session-id.sh:826-879`), quoted-prefix boundary bypass (`tests/test-session-id.sh:883-923`), unquoted setup invocation (`tests/test-session-id.sh:928-962`), and tab-delimited quoted/unquoted invocations (`tests/test-session-id.sh:966-1051`). Not executed in this read-only research run.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-074 `file` `tests/test-commit-history-section.sh`
- cursor: `[_]`
- core_role: Executable specification for the “Integral Context” component of RLCR review prompts: Codex review should see accumulated commit history plus recent round summary/review references, with robust fallback behavior when the base commit is unavailable.
- algorithmic_behavior: Builds a temporary git repo, sources `hooks/lib/template-loader.sh`, constructs `COMMIT_HISTORY` and `RECENT_ROUND_FILES`, renders `prompt-template/codex/commit-history-section.md` with `load_and_render_safe`, and asserts expected content for several round/base-commit states (`tests/test-commit-history-section.sh:14-20`, `tests/test-commit-history-section.sh:53-60`). It mirrors the stop-hook logic at `hooks/loop-codex-stop-hook.sh:997-1026`.
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `BASE_COMMIT`, git log output from the temp repo, `LOOP_TIMESTAMP`, and the template file. Outputs are test pass/fail records plus a final test summary. It mutates only its temp repo by creating commits and an orphan branch (`tests/test-commit-history-section.sh:69-74`, `tests/test-commit-history-section.sh:155-162`).
- gates_or_invariants: Round 0 must render `(no commits yet)` and `(first round, no prior history)` (`tests/test-commit-history-section.sh:33-61`). Round 3 must include all three commits and prior round 0-2 summary/review files (`tests/test-commit-history-section.sh:76-114`). The algorithm must use `git merge-base --is-ancestor` before `BASE_COMMIT..HEAD`, not merely trust that the object exists (`tests/test-commit-history-section.sh:124-132`). Missing template fallback must still include development history, recent round files, and “Read these files” directive (`tests/test-commit-history-section.sh:181-222`). Round 1 must reference only round 0 (`tests/test-commit-history-section.sh:224-244`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, git, and `hooks/lib/template-loader.sh`. It validates the stop-hook code that computes `COMMIT_HISTORY`, annotates base fallback, builds up to three prior round file refs, and uses `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:997-1026`).
- edge_cases_or_failure_modes: Corrupted base commit (`deadbeef...`) must not crash under `set -e`; unrelated valid base commit on an orphan branch must be treated as unavailable; empty legacy base commit must trigger annotated recent-history fallback; missing template must not remove the whole section; round index generation must not reference future/current rounds.
- validation_or_tests: This file is itself the validation artifact. It covers six named tests and ends with `print_test_summary "Commit History Section (I Component) Tests"` (`tests/test-commit-history-section.sh:268-271`). Not executed in this read-only research run.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-104 `file` `hooks/lib/loop-bg-tasks.sh`
- cursor: `[_]`
- core_role: Core implementation for this branch’s behavior: allow a natural stop when the active Claude session still has background Agent/Bash work running, without advancing or terminating the RLCR loop. It also owns parked-loop recovery and cross-session safety around `bg-pending.marker`.
- algorithmic_behavior: Provides helpers for transcript path extraction and background task accounting. `expand_leading_tilde` safely expands only `~` and `~/...` (`hooks/lib/loop-bg-tasks.sh:29-36`). `extract_transcript_path` reads `.transcript_path` via `jq` and applies tilde expansion (`hooks/lib/loop-bg-tasks.sh:41-46`). `derive_loop_start_iso_ts` converts the loop directory’s local-wall-clock basename (`YYYY-MM-DD_HH-MM-SS`) to a UTC ISO timestamp for transcript filtering (`hooks/lib/loop-bg-tasks.sh:65-95`). `list_pending_background_task_ids` scans transcript JSONL for launches via `toolUseResult.isAsync == true` with `agentId`, or `toolUseResult.backgroundTaskId`, filters launches before the loop start, unions completion events from modern `system/task_notification` records and legacy queue-operation XML, then emits `launched - completed` via `comm -23` (`hooks/lib/loop-bg-tasks.sh:134-193`). `has_pending_background_tasks` and `count_pending_background_tasks` wrap that list for boolean/count consumers (`hooks/lib/loop-bg-tasks.sh:195-225`).
- inputs_outputs_state: Inputs are `LOOP_DIR`, hook JSON input, hook `session_id`, transcript path, transcript event records, stored state `session_id`, and `bg-pending.marker`. Outputs are either pending task IDs/counts, no output with return status, or JSON `systemMessage` responses emitted by `handle_bg_task_short_circuit`. State mutation is deliberately narrow: pending in-loop work creates/touches `bg-pending.marker` (`hooks/lib/loop-bg-tasks.sh:320-332`); same-session verified completion removes a stale marker only after a successful readable transcript scan with empty pending output (`hooks/lib/loop-bg-tasks.sh:356-360`). It does not rename `state.md`, increment rounds, run Codex, or close the loop.
- gates_or_invariants: The stop hook calls `handle_bg_task_short_circuit` before phase detection, state parsing, task checks, git checks, summary checks, max-iteration logic, or Codex review (`hooks/loop-codex-stop-hook.sh:71-83`). Guard order is fixed: ambiguous caller with marker and no `session_id` exits silently (`hooks/lib/loop-bg-tasks.sh:250-264`); foreign session with marker and mismatched stored `session_id` exits with a parked-loop message while preserving artifacts (`hooks/lib/loop-bg-tasks.sh:266-293`); current-session pending background tasks exits 0 with a visible “loop active” message and writes marker (`hooks/lib/loop-bg-tasks.sh:297-333`); same-session stale-marker cleanup runs only after authoritative “no pending” proof (`hooks/lib/loop-bg-tasks.sh:335-362`). Missing transcript, non-file transcript, missing `jq`, jq parse errors, and malformed transcripts are fail-closed: they do not short-circuit as pending and do not erase existing parked markers.
- dependencies_and_callers: Depends on `jq`, `date`, `sort`, `sed`, `grep`, `comm`, and process substitution. It also depends on `FIELD_SESSION_ID` and `resolve_active_state_file` from `hooks/lib/loop-common.sh`, and is sourced from there (`hooks/lib/loop-common.sh:1518-1528`). The direct caller is `hooks/loop-codex-stop-hook.sh:83`; wrapper behavior is exercised through `scripts/rlcr-stop-gate.sh`.
- edge_cases_or_failure_modes: Handles `~/...` transcript paths, pre-loop session-wide background launches, time-zone conversion from local loop directory names to UTC transcript timestamps, mixed modern/legacy completion formats, no-active-loop path, finalize phase, wrappers that omit `session_id`, foreign sessions, unreadable/missing/malformed transcripts, stale marker cleanup, and exact session preference over marker fallback. A notable design risk is that completion matching uses task IDs only; if transcript formats change again, pending detection may fail closed and fall through to normal gates rather than parking.
- validation_or_tests: `tests/test-stop-hook-bg-allow.sh` is the main executable spec. It lists acceptance criteria AC-1 through AC-20 in its header (`tests/test-stop-hook-bg-allow.sh:12-34`) and extends through AC-22 later. It covers no background tasks reaching Codex, pending subagent/shell stop allowance, completed launches returning to normal review, multiple pending counts, missing transcript fail-closed, finalize phase, wrapper forwarding, tilde expansion (`tests/test-stop-hook-bg-allow.sh:498-545`), modern task notifications and mixed legacy/SDK completions (`tests/test-stop-hook-bg-allow.sh:879-931`), marker preservation on unreadable/missing/malformed transcripts (`tests/test-stop-hook-bg-allow.sh:933-1028`, `tests/test-stop-hook-bg-allow.sh:1133-1181`), validator isolation (`tests/test-stop-hook-bg-allow.sh:1030-1072`), ambiguous no-session marker handling (`tests/test-stop-hook-bg-allow.sh:1074-1085`), and since-timestamp filtering/time-zone derivation (`tests/test-stop-hook-bg-allow.sh:1183-1288`). Not executed in this read-only research run.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-134 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Specific block template enforcing the invariant that `round-*-prompt.md` files are instructions from Codex/the loop to Claude and must not be modified by the agent.
- algorithmic_behavior: Renders a Markdown block headed “Prompt File Write Blocked” and instructs the agent to read the current prompt, execute the tasks, and write results to the summary file; if the prompt has errors, those must be documented in the summary (`prompt-template/block/prompt-file-write.md:1-12`).
- inputs_outputs_state: No dynamic placeholders are used. Output is a static reason string returned by `prompt_write_blocked_message`. It does not directly mutate state; validators consume it to reject a Write/Edit/Bash attempt with a block/error result.
- gates_or_invariants: The key invariant is prompt-file immutability: the current round prompt is an input contract, not a writable work product. This prevents an agent from rewriting its own instructions and corrupting the RLCR review loop.
- dependencies_and_callers: Loaded by `prompt_write_blocked_message` in `hooks/lib/loop-common.sh:837-844`. Called from Bash validator when a command modifies `round-[0-9]+-prompt.md` (`hooks/loop-bash-validator.sh:501-508`), from write validator (`hooks/loop-write-validator.sh:68`), and from edit validator (`hooks/loop-edit-validator.sh:51`). Referenced in template reference tests (`tests/test-template-references.sh:154`).
- edge_cases_or_failure_modes: Since it has no placeholders, rendering is low-risk. If missing or empty, callers fall back to a shorter embedded message in `hooks/lib/loop-common.sh:839-843`. The broader edge case is path matching: this template only explains the block; actual correctness depends on validators reliably detecting prompt-file writes across Write/Edit/Bash tool payloads.
- validation_or_tests: Covered indirectly by `tests/test-template-references.sh` for template existence and by validator suites for prompt-file access/write blocking. The template-loader robustness tests ensure missing/empty template fallback remains safe. Not executed in this read-only research run.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-164 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit template for the RLCR Code Review Phase. It documents the `codex review` invocation and the transition contract: issues found return remediation to Claude; no issues move the loop to Finalize Phase.
- algorithmic_behavior: Produces `round-{{REVIEW_ROUND}}-review-prompt.md`, but explicitly states that `codex review` does not accept prompt input and reviews based on git diff (`prompt-template/codex/code-review-phase.md:1-5`). It records base branch, review round, timestamp (`prompt-template/codex/code-review-phase.md:6-10`), describes the phase steps (`prompt-template/codex/code-review-phase.md:12-18`), provides the expected `[P0-9]` issue format (`prompt-template/codex/code-review-phase.md:19-28`), and names generated command/output/debug files (`prompt-template/codex/code-review-phase.md:30-36`).
- inputs_outputs_state: Inputs are `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP`. The caller also passes `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE`, but this template does not currently render those variables; the fallback text in the caller includes them (`hooks/loop-codex-stop-hook.sh:1190-1208`). Output is an audit Markdown file in the loop directory, not stdin to Codex. State transition is indirect: the review phase later scans Codex output for `[P0-9]` and either continues remediation or transitions to finalize.
- gates_or_invariants: Captures the invariant that review phase cannot be skipped and completion is determined by absence of `[P0-9]` review markers, not by a normal implementation `COMPLETE` marker. The stop hook notes that review phase runs `codex review` every exit attempt until no `[P0-9]` patterns are found (`hooks/loop-codex-stop-hook.sh:1881-1882`).
- dependencies_and_callers: Generated by the stop hook’s review-prompt helper at `hooks/loop-codex-stop-hook.sh:1187-1208`. The review command builder and runner are nearby (`hooks/loop-codex-stop-hook.sh:1157`, `hooks/loop-codex-stop-hook.sh:1225-1262`). Issue detection helpers live in `hooks/lib/loop-common.sh:706-725`. Tests around issue extraction include `tests/test-codex-review-merge.sh`, and finalize/review failure behavior appears in `tests/test-finalize-phase.sh:633-722`.
- edge_cases_or_failure_modes: The template is an audit artifact rather than enforcement logic, so stale wording would not directly change gate behavior. However, it currently omits some variables passed by the caller (`BASE_COMMIT`, review base/type), which can make audit output less complete than the fallback. If Codex review fails, returns empty output, or writes severity markers only in stderr/log windows, the enforcement path is handled outside this template.
- validation_or_tests: Template existence is covered by template reference/comprehensive tests. Review marker extraction is tested in `tests/test-codex-review-merge.sh`, including `[P?]` markers near line starts, late markers in long logs, multi-issue extraction, and dash-prefixed markers. Failure to produce valid review output is covered by `tests/test-finalize-phase.sh:633-722`. Not executed in this read-only research run.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-194 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the RLCR template system under missing files, malformed placeholders, unusual variable names/values, filesystem edge cases, and concurrent loads. This protects the block-template gate surface from crashing hooks or dropping fallback messages.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and test helpers, creates a temp directory, then runs 20 tests grouped by missing templates, malformed templates, invalid variable names, variable value edge cases, filesystem edge cases, concurrent loading, and template discovery (`tests/robustness/test-template-error-robustness.sh:13-25`, `tests/robustness/test-template-error-robustness.sh:27-340`).
- inputs_outputs_state: Inputs are generated temp template files, direct calls to `load_and_render_safe` and `render_template`, shell-created values including multiline values, long names, BOM bytes, symlinks, permission bits, and concurrent background jobs. Outputs are pass/fail records and a final summary exit code. State is confined to the test temp directory, with temporary chmod changes restored before cleanup (`tests/robustness/test-template-error-robustness.sh:245-260`).
- gates_or_invariants: Missing template or missing directory must use fallback (`tests/robustness/test-template-error-robustness.sh:34-51`). Empty templates must be handled gracefully (`tests/robustness/test-template-error-robustness.sh:53-63`). Malformed placeholders, nested braces, only-opening braces, and empty placeholder names must not crash (`tests/robustness/test-template-error-robustness.sh:73-115`). Values containing template syntax must not recursively expand (`tests/robustness/test-template-error-robustness.sh:165-174`). Newlines must be preserved enough for content to survive (`tests/robustness/test-template-error-robustness.sh:176-186`). Ten concurrent safe loads must all succeed without deleting the file (`tests/robustness/test-template-error-robustness.sh:270-299`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, `tests/test-helpers.sh`, `id`, `chmod`, `ln`, `seq`, background job `wait`, and shell utilities like `head`/`tr`. It validates behavior that all `prompt-template/block` and `prompt-template/codex` callers rely on, especially `load_and_render_safe` fallback semantics in hook blockers.
- edge_cases_or_failure_modes: Covers permission denied if not root, UTF-8 BOM, whitespace-only files, filenames with spaces, nested subdirectories, symlink templates, long variable names, special-character variable names, numeric variable names, and concurrent rendering. Some assertions intentionally accept multiple safe outcomes, for example empty-template output may be empty or fallback, and permission handling may vary by system.
- validation_or_tests: This file is itself the validation suite and ends with `print_test_summary "Template Error Robustness Test Summary"` plus `exit $?` (`tests/robustness/test-template-error-robustness.sh:336-341`). Not executed in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7 unique item sections above; count matches assigned_item_count`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`