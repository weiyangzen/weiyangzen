# agent_30 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-030 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow wrapper for cancelling the active RLCR loop. It is intentionally model-free (`disable-model-invocation: true`) and delegates all state mutation to `scripts/cancel-rlcr-loop.sh`; see `commands/cancel-rlcr-loop.md:1-5` and the “Key principle” at `commands/cancel-rlcr-loop.md:37`.
- algorithmic_behavior: The command runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` first, then branches exclusively on the first output line: `NO_LOOP`/`NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM`; see `commands/cancel-rlcr-loop.md:11-23`. If finalize confirmation is required, it asks the user and only runs `--force` when the user explicitly chooses immediate cancellation; see `commands/cancel-rlcr-loop.md:24-35`.
- inputs_outputs_state: Input is the current project’s newest RLCR loop directory discovered by the script, plus a possible user confirmation for finalize phase. Output is a human-facing cancellation/no-loop/finalize-continuation message. State transitions are performed by `scripts/cancel-rlcr-loop.sh`: active `state.md`, `methodology-analysis-state.md`, or `finalize-state.md` becomes `cancel-state.md`, and `.cancel-requested` is created; the markdown explicitly states active-loop detection at `commands/cancel-rlcr-loop.md:37-39`. The implementation confirms the script statuses and phase files at `scripts/cancel-rlcr-loop.sh:99-117`, finalize guard at `scripts/cancel-rlcr-loop.sh:135-145`, and state rename/output at `scripts/cancel-rlcr-loop.sh:152-180`.
- gates_or_invariants: Finalize phase has a confirmation gate: unforced cancellation returns `FINALIZE_NEEDS_CONFIRM` and must not proceed without the user selecting “Yes, cancel now”; see `commands/cancel-rlcr-loop.md:22-35`. The command’s allowlist limits Bash invocations to the cancel script with and without `--force`, plus `AskUserQuestion`; see `commands/cancel-rlcr-loop.md:3`.
- dependencies_and_callers: Depends directly on `scripts/cancel-rlcr-loop.sh` and indirectly on `hooks/lib/loop-common.sh` because the script sources it for `find_active_loop`; see `scripts/cancel-rlcr-loop.sh:72-87`. Related workflow references appear in `skills/humanize/SKILL.md:89-95`, `commands/start-rlcr-loop.md:186`, setup prompts such as `scripts/setup-rlcr-loop.sh:1419`, and many stop-hook recovery messages.
- edge_cases_or_failure_modes: No active loop maps to first-line status `NO_LOOP` or `NO_ACTIVE_LOOP` and should be reported as “No active RLCR loop found”; see `commands/cancel-rlcr-loop.md:17-18`. A loop directory with no active state file is handled separately by the script as `NO_ACTIVE_LOOP`; see `scripts/cancel-rlcr-loop.sh:113-117`. Unknown script options exit with error code 3; see `scripts/cancel-rlcr-loop.sh:57-61`. Finalize cancellation without force is a soft stop requiring confirmation, not a direct cancel; see `scripts/cancel-rlcr-loop.sh:135-145`.
- validation_or_tests: Read-only syntax validation passed for `scripts/cancel-rlcr-loop.sh` using `bash -n`. The markdown behavior is backed by script implementation line checks above. I did not execute destructive cancellation behavior because this branch export is read-only and no active loop should be mutated during research.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-060 `file` `templates/bitlesson.md`
- cursor: `[_]`
- core_role: Seed template for the project-local BitLesson knowledge base at `.humanize/bitlesson.md`. It defines the strict schema that the RLCR memory/lesson loop later selects from and validates against; see `templates/bitlesson.md:1-23`.
- algorithmic_behavior: Provides a deterministic entry format with required field order: `Lesson ID`, `Scope`, `Problem Description`, `Root Cause`, `Solution`, `Constraints`, `Validation Evidence`, and `Source Rounds`; see `templates/bitlesson.md:5-19`. The setup algorithm initializes the project file from this template via `scripts/setup-rlcr-loop.sh:853-862`, and `scripts/bitlesson-init.sh` copies it only when the target file does not already exist; see `scripts/bitlesson-init.sh:83-86`.
- inputs_outputs_state: Input is the template file and a target project root/relative path supplied to `bitlesson-init.sh`; see `scripts/bitlesson-init.sh:18-38`. Output is `.humanize/bitlesson.md`, printed as an absolute resolved path by `bitlesson-init.sh`; see `scripts/bitlesson-init.sh:75-88`. The state seeded here later becomes project memory consumed by `bitlesson-selector` and checked in round summaries through `## BitLesson Delta`.
- gates_or_invariants: Template enforces precise, reusable entries and exact field order; see `templates/bitlesson.md:3-8`. `bitlesson-init.sh` refuses absolute or parent-traversing `--bitlesson-relpath`, refuses non-directory project roots, refuses missing templates, and refuses existing non-file targets; see `scripts/bitlesson-init.sh:60-80`. It does not overwrite existing knowledge bases; see `scripts/bitlesson-init.sh:10-14` and `scripts/bitlesson-init.sh:83-86`.
- dependencies_and_callers: Called by setup at `scripts/setup-rlcr-loop.sh:856-862`. The seeded file path is recorded into RLCR state as `bitlesson_file: .humanize/bitlesson.md`; see `scripts/setup-rlcr-loop.sh:895-897`. The stop hook validates summaries with `scripts/bitlesson-validate-delta.sh` when BitLesson is required; see `hooks/loop-codex-stop-hook.sh:820-835`. The next prompt instructs agents to read the file and run `bitlesson-selector`; see `scripts/setup-rlcr-loop.sh:1345-1361`.
- edge_cases_or_failure_modes: Empty template-only knowledge bases can cause selector short-circuit behavior to `NONE` in selector tests, while stricter loop modes can block repeated `Action: none`; related rules are referenced in `commands/start-rlcr-loop.md:179-180` and docs. If the template is missing or the target path is unsafe, initialization fails before loop state creation; see `scripts/bitlesson-init.sh:54-72`.
- validation_or_tests: Read-only syntax validation passed for `scripts/bitlesson-init.sh` using `bash -n`. Template behavior is covered by BitLesson-related tests such as `tests/test-bitlesson-select-routing.sh`, `tests/test-bitlesson-validate-delta.sh`, and setup checks referenced by `tests/robustness/test-setup-scripts-robustness.sh`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-090 `file` `tests/test-refine-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `refine-plan` command structure, validator contract, comment extraction/classification reference behavior, language variant path rules, atomic write requirements, QA template coverage, and install/documentation wiring. The header states its coverage at `tests/test-refine-plan.sh:3-11`.
- algorithmic_behavior: The script sets repository paths for `commands/refine-plan.md`, `prompt-template/plan/refine-plan-qa-template.md`, `scripts/validate-refine-plan-io.sh`, docs, skills, and plugin metadata at `tests/test-refine-plan.sh:16-34`. It defines reusable assertions (`assert_file_contains`, regex, line order, equals) at `tests/test-refine-plan.sh:63-115`, then fixtures for valid and invalid annotated plans at `tests/test-refine-plan.sh:159-421`.
- inputs_outputs_state: Inputs are repository files plus temporary fixture plans created under `TEST_FIXTURES_DIR`; see `tests/test-refine-plan.sh:624-625`. Outputs are PASS/FAIL lines and final process exit 0/1 based on `TESTS_FAILED`; see `tests/test-refine-plan.sh:1345-1363`. State is local to test counters and temp fixtures. It does not update repo state unless invoked in an environment where referenced scripts create temp QA directories under the temp fixture root.
- gates_or_invariants: Static gates require `refine-plan.md` metadata, allowed tools, hidden slash-tool flag, ultrathink instruction, planning-only constraint, sequential phase ordering, exact validator exit-code documentation, extraction/classification requirements, config precedence, language mappings, atomic write temp-file rules, QA template sections, and installation wiring; see `tests/test-refine-plan.sh:640-880`. It enforces line ordering between extraction, classification, processing, QA, and atomic write phases at `tests/test-refine-plan.sh:699-701`.
- dependencies_and_callers: Primary targets are `commands/refine-plan.md`, `scripts/validate-refine-plan-io.sh`, and `prompt-template/plan/refine-plan-qa-template.md`; see `tests/test-refine-plan.sh:25-27`. It also depends on `skills/humanize-refine-plan/SKILL.md`, `scripts/install-skill.sh`, docs for Claude/Codex/Kimi installation, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md`; see `tests/test-refine-plan.sh:28-34`. It is listed by the test suite through `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The reference scanner ignores markers inside fenced code blocks and HTML comments, preserves non-comment text around inline comments, counts only non-empty comments, and removes multiline comments from the working plan; see `tests/test-refine-plan.sh:427-529` and reference cases at `tests/test-refine-plan.sh:891-953`. Classifier precedence is research request over change request over question; see `tests/test-refine-plan.sh:547-558` and `tests/test-refine-plan.sh:955-985`. Language normalization handles whitespace, names/codes, English no-op, unsupported values, suffix-before-extension, extensionless outputs, and QA path derivation from input basename; see `tests/test-refine-plan.sh:560-622` and `tests/test-refine-plan.sh:987-1040`. Validator edge tests require exit 7 for invalid args/modes, 1 for missing input, 2 for empty input, 3 for no/ignored/empty/unterminated/nested comments, 4 for missing required sections or sections hidden in ignored regions, 5 for output/input dir write failures, 6 for invalid QA dir, and 0 for valid modes; see `tests/test-refine-plan.sh:1051-1343`.
- validation_or_tests: Read-only syntax validation passed for `tests/test-refine-plan.sh` and `scripts/validate-refine-plan-io.sh` using `bash -n`. I did not run the full test script because it creates temp fixtures/directories and invokes validator behavior; this branch export is restricted to read-only research. Direct file inspection covered all sections from line 1 through line 1363.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-120 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template used when an RLCR agent tries to stop while Git has uncommitted tracked/staged/unstaged changes or relevant untracked files. It forms the user-facing remediation contract for the “git clean before exit/review” gate; see `prompt-template/block/git-not-clean.md:1-18`.
- algorithmic_behavior: The template renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then tells the agent to review/simplify code if the code-simplifier plugin exists, review untracked files, stage specific paths only, and commit with project conventions; see `prompt-template/block/git-not-clean.md:3-10`. The stop hook computes `GIT_ISSUES` and `SPECIAL_NOTES`, renders this template, and returns a JSON block decision; see `hooks/loop-codex-stop-hook.sh:654-710`.
- inputs_outputs_state: Inputs are `GIT_ISSUES` such as “uncommitted changes” and optional notes for `.humanize/` local state or other untracked files; see `hooks/loop-codex-stop-hook.sh:636-684`. Output is a rendered block reason and system message: `decision: block` with “Loop: Blocked - … detected, please commit first”; see `hooks/loop-codex-stop-hook.sh:698-709`. It does not mutate state itself, but blocks the RLCR transition to review/finalize until the Git tree is clean enough.
- gates_or_invariants: Hard invariant: no broad staging commands during active RLCR (`git add -A`, `git add --all`, `git add .`), never stage `.humanize/` or legacy `.humanize-*` artifacts, follow commit conventions, and avoid AI authorship/co-author trailers; see `prompt-template/block/git-not-clean.md:11-17`. The stop hook excludes untracked `.humanize/` paths from the dirty determination while still adding notes when appropriate; see `hooks/loop-codex-stop-hook.sh:654-680`.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"`; see `hooks/loop-codex-stop-hook.sh:692-700`. Rendering semantics come from `hooks/lib/template-loader.sh`, with usage examples around `hooks/lib/template-loader.sh:139`. Related special-note templates include `prompt-template/block/git-not-clean-humanize-local.md` and `prompt-template/block/git-not-clean-untracked.md`.
- edge_cases_or_failure_modes: If `.humanize/` is untracked, it should not block by itself, but a note may be included; see `hooks/loop-codex-stop-hook.sh:654-673`. Other untracked files add artifact-review notes; see `hooks/loop-codex-stop-hook.sh:676-684`. If Humanize state is tracked, a separate stronger block happens before this template; see `hooks/loop-codex-stop-hook.sh:639-652`. If the template is unavailable, the hook has a fallback block body; see `hooks/loop-codex-stop-hook.sh:692-697`.
- validation_or_tests: Template rendering path is exercised by stop-hook tests and comprehensive template tests. Direct inspection confirms placeholders and rules. No execution was needed because this item is a static prompt contract.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-150 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Continuation prompt fragment for RLCR Agent Teams mode after a Codex review round. It defines how the next team leader should reset team membership, consume review feedback, and avoid redoing completed work; see `prompt-template/claude/agent-teams-continue.md:1-12`.
- algorithmic_behavior: The template instructs the leader to create a brand-new team because prior teammates no longer exist, review Codex feedback before spawning agents, inspect previous summaries/goal tracker, give cold-start context and acceptance criteria to new members, prioritize critical feedback if one round is insufficient, and verify current file state before assigning ownership; see `prompt-template/claude/agent-teams-continue.md:5-12`.
- inputs_outputs_state: Inputs are the prior Codex review feedback, goal tracker/prior summaries, and current repository state. Output is next-round prompt guidance appended to the generated prompt file. It does not mutate loop state directly, but it changes the behavioral state of the next implementation round by forcing fresh team construction and review-driven allocation.
- gates_or_invariants: Invariant: old team members must not be messaged or referenced; new members have no conversation history and require complete task context; see `prompt-template/claude/agent-teams-continue.md:7-10`. Invariant: do not redo correctly completed work; address review findings and gaps only; see `prompt-template/claude/agent-teams-continue.md:8-10`. It also requires state verification before file ownership assignment; see `prompt-template/claude/agent-teams-continue.md:12`.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` when `AGENT_TEAMS=true` and review phase has not started, then combined with `prompt-template/claude/agent-teams-core.md`; see `hooks/loop-codex-stop-hook.sh:2145-2155`. If unavailable, a much shorter fallback is emitted; see `hooks/loop-codex-stop-hook.sh:2156-2163`. It is tested by `tests/test-agent-teams.sh:372-387`.
- edge_cases_or_failure_modes: If this template or the core template is missing/empty, the fallback lacks detailed cold-start, priority, and state-awareness rules; see `hooks/loop-codex-stop-hook.sh:2150-2163`. If agents reuse stale team context despite the prompt, work can be misassigned because previous teammates are gone and new members lack conversation history; this is explicitly guarded at `prompt-template/claude/agent-teams-continue.md:7-10`.
- validation_or_tests: Direct inspection confirms required continuation guidance. Existing test coverage checks the template exists and contains continuation-specific guidance; see `tests/test-agent-teams.sh:372-387`. No execution was needed because this item is a static prompt fragment.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-180 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: High-level Humanize skill contract for iterative RLCR development, plan generation, cancellation, one-shot Codex consultation, plan structure, goal tracking, monitoring, data layout, and exit codes. It is non-user-invocable and model-free, acting as installed runtime instructions; see `skills/humanize/SKILL.md:1-8`.
- algorithmic_behavior: Defines the RLCR two-phase loop: implementation rounds produce summaries reviewed by Codex until `COMPLETE`, then code review uses `codex review --base <branch>` and severity markers until no issues remain, then finalize phase completes the loop; see `skills/humanize/SKILL.md:30-49`. It also defines generate-plan behavior, command entrypoints, common setup options, cancellation, ask-codex, and plan-file schema; see `skills/humanize/SKILL.md:50-150`.
- inputs_outputs_state: Inputs include an implementation plan file or `--skip-impl`, setup options like max iterations/model/base branch/full-review interval/agent teams/privacy, rough draft documents for plan generation, and user questions for `ask-codex`; see `skills/humanize/SKILL.md:61-109`. Outputs include `.humanize/rlcr/<timestamp>/` loop state, summaries, review results, goal tracker, finalize/methodology/complete state files, and `.humanize/skill/<timestamp>/` one-shot skill outputs; see `skills/humanize/SKILL.md:180-203`. State transitions include implementation -> review -> finalize/complete, cancellation, methodology analysis, and goal-tracker mutation; see `skills/humanize/SKILL.md:34-48` and `skills/humanize/SKILL.md:152-165`.
- gates_or_invariants: The skill requires summaries before exit, up-to-date goal tracker, thorough implementation/test details, no cheating by editing state/cancel files, and use of the native Codex Stop hook after writing summaries; see `skills/humanize/SKILL.md:166-174`. Goal tracker invariants split immutable ultimate goal/acceptance criteria from mutable tasks/completed/deferred/evolution log; see `skills/humanize/SKILL.md:152-164`. Plan structure invariants require AC-format acceptance criteria, positive/negative tests, boundaries, milestones, and no plan terminology leaking into code; see `skills/humanize/SKILL.md:111-150`.
- dependencies_and_callers: Entry scripts include `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, and `scripts/ask-codex.sh`; see `skills/humanize/SKILL.md:61-109`. Monitoring depends on `scripts/humanize.sh` and `humanize monitor rlcr`; see `skills/humanize/SKILL.md:205-212`. Runtime state layout coordinates with hooks, templates, and tests that consume `.humanize/rlcr` files; see `skills/humanize/SKILL.md:180-203`.
- edge_cases_or_failure_modes: Missing `codex` violates prerequisite; see `skills/humanize/SKILL.md:175-178`. Max-iteration, full-review, push-every-round, quiz, yolo, privacy, skip-impl, and agent-team options alter loop gates and prompts; see `skills/humanize/SKILL.md:73-88`. Cancel during finalize requires special handling through the cancel command/script; see `skills/humanize/SKILL.md:89-95` and `commands/cancel-rlcr-loop.md:24-35`. Legacy or malformed state can trigger hook/template blocks elsewhere because the skill’s directory layout and state filenames are contractually expected.
- validation_or_tests: Direct inspection covered the complete file. Related behavior is broadly tested through setup, stop-hook, monitor, plan-file, goal-tracker, BitLesson, and cancellation tests. I also syntax-checked referenced shell scripts where used in this evidence (`setup` was inspected by line reference; `cancel`, `bitlesson-init`, and refine validator were checked with `bash -n`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6/6 item headings present, one per assigned row; item IDs intentionally not repeated here to preserve exact-once heading occurrence.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`