# agent_04 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-004 `directory` `config`
- cursor: `[_]`
- core_role: `config` is the runtime configuration seed directory. It contains the hook installation template that routes Codex/Claude Stop events into the RLCR stop hook, and the default configuration file that feeds model, effort, BitLesson, agent-teams, and planning defaults into the shared runtime loader.
- algorithmic_behavior: Recursive inspection found two files only: `config/codex-hooks.json` and `config/default_config.json`. `config/codex-hooks.json:3-16` defines a `Stop` hook command that resolves through `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh`; `config/default_config.json:2-7` sets default model/feature flags consumed by config merging. Runtime install paths hydrate these defaults: `scripts/install-codex-hooks.sh` references the hook template and replaces `{{HUMANIZE_RUNTIME_ROOT}}`; `scripts/lib/config-loader.sh:63-87` treats `config/default_config.json` as the mandatory base layer for merged configuration.
- inputs_outputs_state: Inputs are static JSON configuration plus optional user/project overrides. Outputs are installed hook configuration for Codex/Claude environments and merged config values exposed through `get_config_value`. State transitions happen indirectly: defaults become `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, `DEFAULT_AGENT_TEAMS`, then setup writes those into loop state files such as `state.md` (`scripts/setup-rlcr-loop.sh:880-893`).
- gates_or_invariants: `config/default_config.json` must exist; config robustness tests assert missing default config is fatal. Model defaults are validated by `hooks/lib/loop-common.sh:198-217`, which rejects invalid characters and non-Codex model prefixes. `agent_teams` defaults to false unless overridden by caller/project/user config (`hooks/lib/loop-common.sh:219-222`).
- dependencies_and_callers: `scripts/lib/config-loader.sh` loads defaults; `hooks/lib/loop-common.sh` consumes merged values; `scripts/setup-rlcr-loop.sh` uses these defaults to create loop state and initial prompts; `scripts/install-codex-hooks.sh` consumes `config/codex-hooks.json`; docs and tests reference this hierarchy (`tests/test-config-merge.sh`, `tests/test-unified-codex-config.sh`, `docs/usage.md`).
- edge_cases_or_failure_modes: Bad JSON or missing `default_config.json` blocks config loading. Invalid `codex_effort` values fall back to `high`; unsupported `codex_model` values fall back to `gpt-5.4`. A non-hydrated `{{HUMANIZE_RUNTIME_ROOT}}` placeholder would route Stop hooks to a nonexistent command after install.
- validation_or_tests: `tests/test-unified-codex-config.sh` validates default keys, absence of stale reviewer keys, project override precedence, invalid model fallback, and state fallback behavior. `tests/test-config-merge.sh` validates layer merge behavior including `agent_teams`. `tests/test-codex-hook-install.sh` validates hook installation behavior.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-034 `file` `config/codex-hooks.json`
- cursor: `[_]`
- core_role: This file is the native Stop-hook routing template. It connects the Codex/Claude hook system to the repository’s RLCR stop algorithm by invoking `hooks/loop-codex-stop-hook.sh`.
- algorithmic_behavior: The JSON declares `hooks.Stop[0].hooks[0]` as a command hook (`config/codex-hooks.json:3-9`). The command path is templated as `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` (`config/codex-hooks.json:9`), so installation must replace the placeholder with the runtime root. The hook timeout is `7200` seconds (`config/codex-hooks.json:10`), allowing long stop-gate transitions such as review, summary, validation, and next-round prompt generation. `statusMessage` labels the hook as `humanize RLCR stop hook` (`config/codex-hooks.json:11`).
- inputs_outputs_state: Input is a Stop event from the host hook runner. Output is not produced by the JSON itself; it delegates to `loop-codex-stop-hook.sh`, which emits Stop-hook JSON decisions such as `decision: block` or no decision/empty output for allow. State changes occur in the delegated hook, including updating loop state, generating next prompts, or parking background-running loops.
- gates_or_invariants: The hook command must resolve to an executable file after placeholder hydration. The Stop hook must be installed in the host’s expected JSON schema: `hooks` -> `Stop` -> list -> `hooks` -> command hook. The timeout must be long enough for RLCR loop review behavior; changing it too low would cause false failures in stop transitions.
- dependencies_and_callers: Installed by `scripts/install-codex-hooks.sh`, which references `config/codex-hooks.json` and performs runtime-root replacement. The command target depends on `hooks/loop-codex-stop-hook.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/loop-bg-tasks.sh`, templates under `prompt-template/`, and tools such as `jq`, `git`, and `codex` depending on phase.
- edge_cases_or_failure_modes: If `{{HUMANIZE_RUNTIME_ROOT}}` remains literal, Stop events cannot reach the hook. If the runtime root points to a stale install, the wrong hook implementation may run. If the host runner interprets `timeout` differently or ignores it, long Codex review/finalize transitions could be interrupted.
- validation_or_tests: Hook behavior is indirectly covered by stop-hook tests such as `tests/test-stop-gate.sh`, `tests/test-stop-hook-bg-allow.sh`, `tests/test-stop-hook-legacy-compat.sh`, and install tests such as `tests/test-codex-hook-install.sh`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-064 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: This is an executable specification for the RLCR Agent Teams feature. It verifies CLI gating, config propagation into loop state, state parsing, prompt injection, active-loop discovery, and phase-specific continuation behavior.
- algorithmic_behavior: The test sources shared helpers and `hooks/lib/loop-common.sh` (`tests/test-agent-teams.sh:17-23`). It exercises setup through `scripts/setup-rlcr-loop.sh` (`tests/test-agent-teams.sh:29`) and the actual stop hook through `hooks/loop-codex-stop-hook.sh` (`tests/test-agent-teams.sh:427-428`). It constructs temporary git repositories, writes plan files, initializes `.humanize/rlcr/.../state.md`, mocks `codex`, and checks generated prompt files. The main algorithmic branches are: reject `--agent-teams` unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`tests/test-agent-teams.sh:54-79`); record `agent_teams: true/false` in state (`tests/test-agent-teams.sh:114-153`); allow `.humanize/config.json` to enable teams (`tests/test-agent-teams.sh:156-186`); parse missing `agent_teams` as false (`tests/test-agent-teams.sh:218-242`); include team instructions in initial and implementation prompts but not review prompts (`tests/test-agent-teams.sh:245-328`, `593-734`).
- inputs_outputs_state: Inputs include CLI flag `--agent-teams`, environment variable `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, project config `.humanize/config.json`, state frontmatter, plan files, mocked Codex review/exec output, and hook JSON. Outputs are pass/fail test assertions, created `state.md`, `round-0-prompt.md`, next-round prompts such as `round-4-prompt.md`, and hook JSON block decisions. State transitions include setup creating loop state with `agent_teams`, stop hook moving from round `N` to `N+1` and adding continuation instructions when implementation needs another round.
- gates_or_invariants: The hard gate is that Agent Teams mode requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; values like `0`, `false`, `yes`, and `true` are rejected (`tests/test-agent-teams.sh:71-79`). State parser invariant: `STATE_AGENT_TEAMS` must be `true` when present and default to `false` when absent (`tests/test-agent-teams.sh:207-239`). Prompt invariant: initial and implementation-phase prompts include Agent Teams guidance only when enabled; review-phase prompts must not include that continuation (`tests/test-agent-teams.sh:704-734`). Template invariant: `agent-teams-core.md` must include role/guidelines/important sections and minimum content size (`tests/test-agent-teams.sh:347-366`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `prompt-template/claude/agent-teams-instructions.md`, `agent-teams-core.md`, `agent-teams-continue.md`, `git`, and `jq`. Production support is in `hooks/lib/loop-common.sh:451-500` for parsing and defaults, `scripts/setup-rlcr-loop.sh:381-392` for env gating, `scripts/setup-rlcr-loop.sh:880-893` for state writing, and `hooks/loop-codex-stop-hook.sh:2145-2165` for continuation prompt injection.
- edge_cases_or_failure_modes: Missing env var blocks setup; non-`1` env values block setup. Missing `agent_teams` in state must not break older loops. Agent Teams text leaking into review phase would confuse the role contract. Missing template files fall back to inline text in setup/stop hook, but tests still expect template files to exist with meaningful content where present.
- validation_or_tests: The file is itself the validator and ends with `print_test_summary "Agent Teams Feature Tests"` (`tests/test-agent-teams.sh:740`). It validates both setup-time and stop-hook-time behavior using temporary repos and mocked Codex commands.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-094 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: This is an executable specification for `scripts/rlcr-stop-gate.sh`, the wrapper that lets non-hook workflows reuse the same stop-hook decision logic as native Stop events. It is directly related to the branch topic because it verifies stop-gate allow/block behavior, project-root routing, tracked-state blocking, and correct forwarding of `transcript_path` when `session_id` is empty.
- algorithmic_behavior: The test defines `setup_active_loop_fixture` to create a minimal active loop that should block on missing summary (`tests/test-stop-gate.sh:18-59`). It then verifies default project root is caller cwd (`tests/test-stop-gate.sh:64-89`), `--project-root` overrides from outside the repository (`tests/test-stop-gate.sh:91-116`), tracked `.humanize/rlcr` state blocks before normal loop validation (`tests/test-stop-gate.sh:118-145`), unrelated `.humanize-backup` and `.humanizeconfig` tracked files do not trigger the Humanize-state block (`tests/test-stop-gate.sh:147-178`), no active loop allows exit 0 (`tests/test-stop-gate.sh:180-204`), and empty `session_id` does not collapse the generated JSON object or drop `transcript_path` (`tests/test-stop-gate.sh:206-279`).
- inputs_outputs_state: Inputs are active loop directories, state frontmatter, git-tracked or untracked `.humanize`-adjacent files, wrapper args `--project-root`, `--transcript-path`, `--json`, and mock hook JSON output. Outputs are wrapper exit codes and stdout/stderr. Expected exit codes are 0 for allow, 10 for gate block, and not 20 for the object-collapse regression path. The wrapper itself constructs Stop-hook JSON with `cwd`, `model`, `permission_mode`, `session_id`, and `transcript_path` (`scripts/rlcr-stop-gate.sh:95-110`).
- gates_or_invariants: Exit-code invariant: `scripts/rlcr-stop-gate.sh` documents 0 allow, 10 block, 20 wrapper/runtime error (`scripts/rlcr-stop-gate.sh:8-11`). Block output must include `BLOCK:` when the hook returns `decision: block` (`scripts/rlcr-stop-gate.sh:139-146`). Allow output must include `ALLOW:` when no active loop exists or hook returns no decision (`scripts/rlcr-stop-gate.sh:123-160`). JSON construction must use explicit `if/then/else` null conversion rather than `select(length > 0)`, preserving `transcript_path` when `session_id` is empty (`scripts/rlcr-stop-gate.sh:90-110`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/loop-codex-stop-hook.sh`, `jq`, `git`, and temporary mock hook installation. Production wrapper delegates to `hooks/loop-codex-stop-hook.sh` with `CLAUDE_PROJECT_DIR="$PROJECT_ROOT"` (`scripts/rlcr-stop-gate.sh:112-120`). Background-running semantics are handled downstream in `hooks/lib/loop-bg-tasks.sh`, especially early allow with a system message when pending background tasks exist (`hooks/lib/loop-bg-tasks.sh:297-332`).
- edge_cases_or_failure_modes: Running the gate from the plugin install directory instead of project cwd would miss active loops, hence the cwd test. Empty `session_id` previously risked object collapse and losing `transcript_path`, causing bogus allow or wrapper errors. Tracked `.humanize` state must block, but similarly named dotfiles must not be false positives. A non-JSON hook response is a wrapper error (`scripts/rlcr-stop-gate.sh:129-132`).
- validation_or_tests: The file validates wrapper behavior end to end and ends with `print_test_summary "RLCR Stop Gate Wrapper Test Summary"` (`tests/test-stop-gate.sh:281`). It includes a mock hook that always returns `decision:"block"` to isolate JSON-forwarding behavior (`tests/test-stop-gate.sh:219-228`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-124 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: This is a block-message template used by write/bash validation to enforce that `goal-tracker.md` is modified through validated tool paths rather than shell redirection or text-processing commands.
- algorithmic_behavior: The template emits a clear block title (`prompt-template/block/goal-tracker-bash-write.md:1`), tells the agent not to use Bash commands to modify `goal-tracker.md` (`prompt-template/block/goal-tracker-bash-write.md:3`), injects the corrected path through `{{CORRECT_PATH}}` (`prompt-template/block/goal-tracker-bash-write.md:5`), and explains that `cat`, `echo`, `sed`, `awk`, etc. bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:7-8`).
- inputs_outputs_state: Input is the `CORRECT_PATH` template variable supplied by the validator/template loader. Output is a user-facing/system-facing block prompt. It does not mutate state itself; it prevents invalid state mutation by steering edits to Write/Edit tools that are hook-visible.
- gates_or_invariants: Invariant: Goal tracker changes must pass through Write/Edit validation hooks. Bash-side writes are blocked because they bypass the validation path. The message depends on a valid correct path; if `{{CORRECT_PATH}}` is not hydrated, the block would be less actionable.
- dependencies_and_callers: Loaded through the repository’s prompt-template system (`hooks/lib/template-loader.sh` via `load_template` used throughout hook libraries). Likely consumed by bash/write validators around goal-tracker protections; related block templates in the same directory cover `summary-bash-write.md`, `round-contract-bash-write.md`, and state-file modification blocks.
- edge_cases_or_failure_modes: False positives could block harmless Bash reads if routing mistakes classify a read as write. False negatives would allow shell writes to goal tracker, bypassing status/task validation. Missing path substitution would still communicate the rule but lose exact remediation.
- validation_or_tests: The specific template is covered indirectly by validator/template coverage such as `tests/test-bash-validator-patterns.sh`, `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness tests for hook/template behavior. This file itself is only 8 lines and contains no executable logic.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-154 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: This template defines the Finalize Phase prompt after Codex review has passed. It instructs the agent to perform functionality-equivalent simplification/refactoring before declaring loop completion, making it part of the RLCR phase-transition contract.
- algorithmic_behavior: The template states that Codex review has passed and implementation/acceptance criteria are complete (`prompt-template/claude/finalize-phase-prompt.md:1-5`). It directs use of the `code-simplifier:code-simplifier` subagent through Task (`prompt-template/claude/finalize-phase-prompt.md:7-14`). It imposes non-negotiable constraints: no functionality changes, no existing test failures, no new bugs, and only equivalent cleanup (`prompt-template/claude/finalize-phase-prompt.md:16-23`). It focuses simplification on recent branch changes between `{{BASE_BRANCH}}` and `{{START_BRANCH}}` (`prompt-template/claude/finalize-phase-prompt.md:25-34`) and references the original plan and goal tracker via placeholders (`prompt-template/claude/finalize-phase-prompt.md:36-40`).
- inputs_outputs_state: Inputs are template placeholders `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`. Output is the finalization prompt that asks the agent to run a simplification pass, commit changes, and write a finalize summary. State transition: RLCR moves from review-passed implementation into Finalize Phase, requiring summary evidence before final completion.
- gates_or_invariants: Completion gate requires all `[mainline]` and `[blocking]` tasks completed, `[queued]` tasks only as non-blocking follow-up, a descriptive commit, and a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:41-52`). Refactoring gate prohibits behavioral changes and requires tests to still pass (`prompt-template/claude/finalize-phase-prompt.md:20-23`, `51`).
- dependencies_and_callers: Consumed by stop-hook/finalize-phase logic through the template loader. Related tests include `tests/test-finalize-phase.sh` and template coverage tests. It references Task tooling and the `code-simplifier:code-simplifier` subagent by name, so it depends on that agent being available in the Claude environment.
- edge_cases_or_failure_modes: Missing subagent support could make the prompt impossible to follow literally. Placeholder hydration failures would leave raw `{{...}}` paths/branches. A refactor that changes behavior violates the finalize gate even if tests pass. If queued tasks are blocking but mislabeled non-blocking, completion could be premature.
- validation_or_tests: `tests/test-finalize-phase.sh` likely verifies finalize-state/prompt behavior; broad template tests validate that prompt templates exist and render. The template’s final summary requirements provide the validation evidence contract for finalize output.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-184 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: This is an executable robustness specification for git status and repository-state parsing used by Humanize monitoring/gating. It protects core algorithms that decide whether the worktree is clean, dirty, detached, rebasing, merging, shallow, inaccessible, or non-git.
- algorithmic_behavior: The test sources `scripts/humanize.sh` and `scripts/portable-timeout.sh` (`tests/robustness/test-git-operations-robustness.sh:15-20`). It validates `humanize_parse_git_status`, whose contract is `modified|added|deleted|untracked|insertions|deletions` (`tests/robustness/test-git-operations-robustness.sh:32-46`; implementation at `scripts/humanize.sh:206-252`). Positive cases cover clean repos, branch name detection, untracked, modified, staged added, insertion counts, deleted files, feature branches, renamed files, many untracked files, spaces in filenames, binary files, staged+unstaged same file, and deletion counts (`tests/robustness/test-git-operations-robustness.sh:71-368`). It also validates `humanize_detect_git_state` for normal, detached, rebase, merge, shallow, non-git, and permission-error conditions (`tests/robustness/test-git-operations-robustness.sh:369-500`; implementation at `scripts/humanize.sh:156-204`).
- inputs_outputs_state: Inputs are temporary git repositories with controlled filesystem/git states: porcelain status changes, commits, branch checkouts, simulated `.git/rebase-merge`, `.git/MERGE_HEAD`, `.git/shallow`, and unreadable `.git`. Outputs are parsed count strings from `humanize_parse_git_status` and enum strings from `humanize_detect_git_state`. The test also mutates temporary repos to move through git states and then cleans them up.
- gates_or_invariants: `humanize_parse_git_status` must return count fields in stable pipe-delimited order. Non-git directories must return a non-empty result containing `not a git repo` (`tests/robustness/test-git-operations-robustness.sh:168-177`; implementation `scripts/humanize.sh:209-212`). Renames count as modified (`scripts/humanize.sh:229`). Rebase is detected before merge/shallow/detached (`scripts/humanize.sh:178-204`), so state precedence is explicit. Permission problems may return `permission_error` or `not_a_repo` depending on platform behavior (`tests/robustness/test-git-operations-robustness.sh:478-500`).
- dependencies_and_callers: Depends on `git`, shell filesystem operations, `scripts/humanize.sh`, `scripts/portable-timeout.sh`, and `tests/test-helpers.sh`. Production callers include the Humanize monitor/statusline and setup/stop gates that need safe dirty-worktree and git-state decisions before proceeding.
- edge_cases_or_failure_modes: Empty repos without commits must not crash (`tests/robustness/test-git-operations-robustness.sh:233-245`). Filenames with spaces must not break count parsing (`tests/robustness/test-git-operations-robustness.sh:301-314`). Binary files should be counted as untracked without diff parsing errors (`tests/robustness/test-git-operations-robustness.sh:316-329`). Simultaneous staged and unstaged changes should not double-break parsing (`tests/robustness/test-git-operations-robustness.sh:331-347`). Platform-specific permission behavior is tolerated.
- validation_or_tests: The file is the validator and ends with `print_test_summary "Git Operations Robustness Test Summary"` (`tests/robustness/test-git-operations-robustness.sh:502-507`). It directly tests the production functions rather than mocks, so failures indicate regressions in git-state parsing used by runtime gates.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7/7 item sections present; each assigned item heading appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`