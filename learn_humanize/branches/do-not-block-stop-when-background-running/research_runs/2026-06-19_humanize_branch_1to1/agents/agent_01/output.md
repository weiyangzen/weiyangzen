# agent_01 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin. It coordinates RLCR planning, loop setup, stop-hook gating, session isolation, monitor display, prompt templates, skills, installer assets, and shell-based validation. Recursive inventory found top-level roles: `commands/` command workflows, `scripts/` runtime entrypoints and helpers, `hooks/` Claude hook validators and stop logic, `prompt-template/` rendered block/review/team prompts, `tests/` executable specs, `config/` defaults, `skills/` installable skill wrappers, `agents/` task agents, `docs/`, `.github/`, and plugin metadata.
- algorithmic_behavior: The repository implements a plan-to-RLCR pipeline. `commands/gen-plan.md` creates structured plans; `commands/start-rlcr-loop.md` and `scripts/setup-rlcr-loop.sh` initialize `.humanize/rlcr/<timestamp>/state.md`; `hooks/loop-codex-stop-hook.sh` enforces stop-gate review behavior; `hooks/lib/loop-bg-tasks.sh` adds the branch-relevant short-circuit for pending background work before normal gates; `hooks/lib/loop-common.sh` centralizes state parsing, session lookup, and template blocks. The root README describes the two-phase RLCR loop: Claude implements, Codex reviews, and review feedback drives iteration (`README.md:20-27`).
- inputs_outputs_state: Inputs include draft/plan files, hook JSON from Claude Code, git state, `.humanize/rlcr` runtime state, transcript JSONL paths, config JSON layers, prompt templates, and CLI flags. Outputs include plan documents, `.humanize/rlcr/<timestamp>` state/summary/review files, hook JSON system messages, monitor output, block prompts, and optional local commits/pushes. Main state transitions use `state.md`, `finalize-state.md`, `methodology-analysis-state.md`, terminal `*-state.md`, and for this branch `bg-pending.marker` parking state.
- gates_or_invariants: The root enforces clean git setup before RLCR start, YAML-safe state fields, base branch detection and local branch existence, no direct state-file edits via hooks, session-aware loop selection, no stale older loop revival, stop-hook Codex review before exit, and background-task fail-closed behavior. The branch-specific invariant is that a natural stop while async Agent/Bash work is still pending must not run summary/git/BitLesson/Codex gates; it exits 0 with a system message and leaves loop state unchanged (`hooks/lib/loop-bg-tasks.sh:297-333`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh` sources `hooks/lib/loop-common.sh` and `scripts/portable-timeout.sh`; stop hooks source `loop-common.sh`, which sources `loop-bg-tasks.sh`; monitor code sources `scripts/lib/monitor-common.sh`; command specs call validators and setup scripts. Tests cover the repository via `tests/run-all-tests.sh`, with focused specs for gen-plan, session ID, monitor runtime, stop gates, background tasks, and base branch detection.
- edge_cases_or_failure_modes: Root-level failure modes include non-git or dirty repos, missing `jq`/Codex/git, invalid config JSON, malformed hook JSON, deleted `.humanize/rlcr` while monitoring, remote-only base branches, stale loop directories, terminal newest loop blocking stale revival, missing transcript path, malformed transcript JSONL, foreign-session parked loops, and wrapper calls that omit session IDs. The git status command could not run in this read-only branch export because this export has no `.git` directory and sandboxed macOS tooling could not create cache files; file inspection remained read-only.
- validation_or_tests: Recursive inventory covered all repository files visible under the branch export. Relevant tests include `tests/test-stop-hook-bg-allow.sh` for pending background stop short-circuit behavior, `tests/test-session-id.sh` for session scoping, `tests/manual-monitor-test.sh` and monitor E2E/runtime tests for graceful monitor stops, `tests/robustness/test-base-branch-detection.sh` for base branch fallback behavior, and `tests/test-gen-plan.sh` plus `scripts/validate-gen-plan-io.sh` for gen-plan workflow validation. I did not run mutating tests because the assigned export is read-only and the user requested research notes only.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-031 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Declarative command workflow for generating an implementation plan from a draft and optionally starting RLCR when the plan has converged. It is core to the plan/RLCR algorithm because it defines allowed tools, phase ordering, convergence gates, output schema, task routing tags, and direct-start conditions.
- algorithmic_behavior: The workflow is strictly sequential (`commands/gen-plan.md:32-45`). Phase 0 parses `--auto-start-rlcr-if-converged`, `--discussion`, and `--direct`, with discussion/direct mutually exclusive (`commands/gen-plan.md:49-58`). Phase 0.5 loads merged config through `scripts/lib/config-loader.sh`, not direct file reads (`commands/gen-plan.md:62-74`). Phase 1 runs `scripts/validate-gen-plan-io.sh` and maps exit codes 0-7 to command behavior (`commands/gen-plan.md:132-150`). Phase 2 runs a relevance-checker agent before creating the output plan (`commands/gen-plan.md:154-180`). Phase 3 asks Codex for first-pass planning risks and gaps before Claude synthesizes (`commands/gen-plan.md:184-213`). Phase 5 runs up to 3 Claude/Codex convergence rounds unless direct mode skips it (`commands/gen-plan.md:255-298`). Phase 6 consolidates user decisions, asks where required, and preserves draft content (`commands/gen-plan.md:301-369`). Phase 7 defines the final plan schema, including ACs, path boundaries, dependencies, task tags, deliberation, pending decisions, and implementation notes (`commands/gen-plan.md:372-530`). Phase 8 writes the plan, checks consistency, optionally translates, and optionally starts RLCR (`commands/gen-plan.md:533-647`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, `--input`, `--output`, config values `alternative_plan_language`, deprecated `chinese_plan`, `gen_plan_mode`, the draft file, repository context, Codex output, Claude analysis, and user answers. Outputs are the main plan file, optional translated variant, internal status variables such as `PLAN_CONVERGENCE_STATUS` and `HUMAN_REVIEW_REQUIRED`, and optionally an RLCR loop setup call. The file itself permits writes only to the output plan and translated variant before any optional auto-start (`commands/gen-plan.md:20-28`).
- gates_or_invariants: No coding during plan generation (`commands/gen-plan.md:20-28`); no phase parallelization (`commands/gen-plan.md:32-35`); config malformed default is fatal while malformed optional configs warn and continue (`commands/gen-plan.md:70-74`, `commands/gen-plan.md:126`); unsupported language disables translation with warning (`commands/gen-plan.md:115-118`); direct mode is never considered converged for auto-start (`commands/gen-plan.md:255-258`); pending user decisions block auto-start (`commands/gen-plan.md:596-600`); every task must be tagged `coding` or `analyze` (`commands/gen-plan.md:447-457`, `commands/gen-plan.md:529`); generated implementation code should not carry plan workflow terminology (`commands/gen-plan.md:477-482`).
- dependencies_and_callers: Allowed tools include `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` (`commands/gen-plan.md:4-13`). It depends on `prompt-template/plan/gen-plan-template.md`, whose required sections mirror the command (`prompt-template/plan/gen-plan-template.md:1-120`), and `agents/draft-relevance-checker.md`. It can call `/humanize:start-rlcr-loop --skip-quiz <output-plan-path>` or fallback to `scripts/setup-rlcr-loop.sh --skip-quiz --plan-file <output-plan-path>` only after convergence gates pass (`commands/gen-plan.md:594-619`).
- edge_cases_or_failure_modes: Invalid/missing input/output paths; output already exists; missing template; absent or malformed config; missing Codex CLI or timeout, where the user chooses retry or Claude-only planning (`commands/gen-plan.md:208-213`); both mode flags set; unsupported alternate language; mixed-language plan requiring user decision; unresolved Claude/Codex disagreement; auto-start requested but direct mode, partial convergence, or pending decisions prevent it.
- validation_or_tests: `scripts/validate-gen-plan-io.sh` implements argument and path validation with documented exit codes and emits `TEMPLATE_FILE:` on success (`scripts/validate-gen-plan-io.sh:1-12`, `scripts/validate-gen-plan-io.sh:155-178`). `tests/test-gen-plan.sh` asserts command metadata, allowed tool references, auto-start option, direct-mode partial convergence, discussion-mode auto-start gate, ultrathink instruction, Phase 3 before Phase 4, convergence limits, required template sections, and mandatory `coding`/`analyze` tags (`tests/test-gen-plan.sh:47-248`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-061 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable spec for monitor graceful-stop behavior when `.humanize/` is deleted while `humanize monitor rlcr` is running. It is core-adjacent to loop monitoring and state lifecycle behavior, not the stop-hook background short-circuit itself.
- algorithmic_behavior: The script has three commands. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99/state.md` with minimal loop fields and `goal-tracker.md` with a minimal immutable goal/AC (`tests/manual-monitor-test.sh:23-46`). `delete` removes `.humanize` to trigger monitor shutdown and tells the tester to verify the monitor’s user-facing message and terminal restoration (`tests/manual-monitor-test.sh:47-57`). `cleanup` removes `.humanize` (`tests/manual-monitor-test.sh:58-62`). Invalid usage prints `setup|delete|cleanup` and exits 1 (`tests/manual-monitor-test.sh:63-72`).
- inputs_outputs_state: Input is the first positional argument. Output is filesystem state under `.humanize/rlcr/2026-01-16_99-99-99/`, plus user-readable instructions. State transition under test is active monitor state present -> `.humanize` removed -> monitor exits cleanly with restored terminal. This script itself mutates `.humanize` when run.
- gates_or_invariants: The monitor should detect directory disappearance and stop with the exact expected text “Monitoring stopped: .humanize/rlcr directory no longer exists” (`tests/manual-monitor-test.sh:51-54`). It should restore scroll region/cursor and avoid shell glob errors after deletion. The script assumes it is run from repository root by resolving `SCRIPT_DIR/..` and `cd`ing there (`tests/manual-monitor-test.sh:19-21`).
- dependencies_and_callers: The manual workflow depends on `scripts/humanize.sh` and specifically `humanize monitor rlcr` (`tests/manual-monitor-test.sh:9-14`, `tests/manual-monitor-test.sh:43-45`). The monitor implementation uses `scripts/lib/monitor-common.sh`, whose `monitor_find_state_file` classifies active and terminal state files (`scripts/lib/monitor-common.sh:147-185`) and terminal utilities restore the terminal (`scripts/lib/monitor-common.sh:69-86`).
- edge_cases_or_failure_modes: If run in a real project, it deletes `.humanize`, so it is unsafe outside a controlled test. The minimal state lacks newer fields such as `session_id`, `base_branch`, and review fields; that is acceptable for monitor display but not for strict stop-hook parsing. It does not automatically verify output; a human must observe the monitor terminal. It is a skip candidate for automated core-algorithm execution but not for research because it specifies expected behavior.
- validation_or_tests: Manual validation steps are embedded in comments and output (`tests/manual-monitor-test.sh:9-14`, `tests/manual-monitor-test.sh:51-56`). Automated sibling coverage exists in monitor runtime/E2E tests that delete `.humanize/rlcr` and verify graceful exit messages, but this assigned file itself is intentionally manual.
- skip_candidate: `yes: manual helper/spec rather than an automated algorithm implementation; still relevant as monitor lifecycle evidence`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-091 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR session identity. It validates that setup records an initially empty `session_id`, PostToolUse fills it only for the setup invocation, loop discovery is session-aware, terminal newest loops block stale revival, and cancel remains global.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` (`tests/test-session-id.sh:18-20`) and exercises `scripts/setup-rlcr-loop.sh`, `hooks/loop-post-bash-hook.sh`, `find_active_loop`, `parse_state_file`, and `scripts/cancel-rlcr-loop.sh`. It first verifies setup creates `state.md` containing an empty `session_id:` and `.humanize/.pending-session-id` (`tests/test-session-id.sh:31-107`). It then writes a two-line pending signal and feeds a mock PostToolUse JSON so the hook patches `session_id` and removes the signal (`tests/test-session-id.sh:109-165`). It verifies no-op behavior without a signal (`tests/test-session-id.sh:167-200`). It tests `find_active_loop` exact match, mismatch, empty stored session fallback, no-filter backward compatibility, and `finalize-state.md` matching (`tests/test-session-id.sh:202-318`). It confirms `parse_state_file` sets `STATE_SESSION_ID` (`tests/test-session-id.sh:320-346`). It verifies cancel ignores session IDs and renames active state to `cancel-state.md` (`tests/test-session-id.sh:348-383`). Later tests validate stale-loop protection and boundary-safe PostToolUse command matching.
- inputs_outputs_state: Inputs are temporary git repos, mock plan files, state files with YAML frontmatter, `.pending-session-id`, mock hook JSON, and session IDs. Outputs are pass/fail test results, patched `state.md`, removed or preserved signal files, return values from `find_active_loop`, and renamed `cancel-state.md`. State transitions include `session_id:` empty -> populated, active `state.md` -> `cancel-state.md`, and terminal newest session directories causing `find_active_loop` to return empty.
- gates_or_invariants: PostToolUse may consume `.pending-session-id` only when the Bash command starts with the full setup script path, quoted or unquoted, followed by end-of-string or whitespace; false positives such as `echo <path>`, basename-only `cat setup-rlcr-loop.sh`, and quoted-prefix concatenation must not consume it (`tests/test-session-id.sh:463-528`, `tests/test-session-id.sh:825-925`). It must handle special characters in session IDs with awk replacement (`tests/test-session-id.sh:530-574`). `find_active_loop` must prefer exact session matches over marker fallback, support empty stored session IDs for backward compatibility, and prevent stale older active loops when a newer terminal state exists for the same session (`tests/test-session-id.sh:581-784`). Signal files must contain both state path and absolute setup script signature (`tests/test-session-id.sh:786-823`).
- dependencies_and_callers: Production dependencies under test include `hooks/lib/loop-common.sh` constants/functions, `hooks/loop-post-bash-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and `tests/test-helpers.sh`. Implementation lines show `FIELD_SESSION_ID` in `loop-common.sh` (`hooks/lib/loop-common.sh:24-40`), `extract_session_id` (`hooks/lib/loop-common.sh:237-243`), active/terminal state resolution and `find_active_loop` (`hooks/lib/loop-common.sh:251-412`), and `_parse_state_fields` reading `STATE_SESSION_ID` (`hooks/lib/loop-common.sh:433-455`). Setup writes `session_id:` empty and signal file (`scripts/setup-rlcr-loop.sh:876-914`). Post hook performs the boundary-aware command check and safe patch (`hooks/loop-post-bash-hook.sh:56-133`).
- edge_cases_or_failure_modes: Missing `jq` prevents hook command/session extraction; missing or stale signal file is removed only if state path is invalid, otherwise preserved for later valid setup hook; non-setup Bash events leave signal intact; terminal `complete-state.md`/`cancel-state.md` prevents stale loop revival; cancel checks only newest loop without session filter; special characters `/` and `&` in session IDs must not break replacement; tab-delimited setup invocations must be accepted.
- validation_or_tests: This file is the validation suite. It prints a summary via `print_test_summary` (`tests/test-session-id.sh:1055-1059`). Sibling production comments also document the same algorithms: `find_active_loop` session-filter semantics (`hooks/lib/loop-common.sh:295-318`) and PostToolUse one-shot mechanism (`hooks/loop-post-bash-hook.sh:8-21`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-121 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Block prompt template for the RLCR git push policy. It is a small guardrail message used when push is not allowed in the current loop configuration.
- algorithmic_behavior: The template tells Claude/current actor that commits should stay local and remote push is unnecessary (`prompt-template/block/git-push.md:1-4`). It provides the explicit transition to enable push behavior: restart the RLCR loop with `--push-every-round` (`prompt-template/block/git-push.md:6-9`).
- inputs_outputs_state: Inputs are the template loader and a blocking condition from hook/bash validators that detect disallowed `git push` behavior. Output is rendered Markdown shown to the model/user. It does not mutate state. The relevant state field is `push_every_round` in `state.md`, written by setup (`scripts/setup-rlcr-loop.sh:876-884`) and parsed by loop common (`hooks/lib/loop-common.sh:441-447`).
- gates_or_invariants: Invariant is “do not push unless the loop was started with `--push-every-round`.” `scripts/setup-rlcr-loop.sh` documents the flag (`scripts/setup-rlcr-loop.sh:108-111`) and writes `push_every_round: $PUSH_EVERY_ROUND` to state (`scripts/setup-rlcr-loop.sh:883`). The template itself is intentionally terse and does not encode detection logic.
- dependencies_and_callers: Rendered through the shared template loader used by hook common (`hooks/lib/loop-common.sh:227-235`). Likely caller is the Bash validator path that blocks protected commands and uses block templates. It depends on the `block/git-push.md` template path being available under `prompt-template/block/`.
- edge_cases_or_failure_modes: If rendered outside RLCR context, it may lack dynamic details such as current state value or exact blocked command; that is by design. If the loop state incorrectly says push is false, the message instructs restart with the flag rather than ad hoc push.
- validation_or_tests: Covered by template comprehensive tests and Bash validator pattern tests in the repository, and indirectly by setup state tests that ensure `push_every_round` is present. This assigned file itself is a static template with no executable tests.
- skip_candidate: `yes: static block text, not algorithm code; still defines an algorithmic gate contract for push behavior`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-151 `file` `prompt-template/claude/agent-teams-core.md`
- cursor: `[_]`
- core_role: Prompt template defining the team-leader contract for Claude Agent Teams mode. It is core to parallel execution semantics because it constrains delegation, file ownership, sequencing, review, and commit coordination.
- algorithmic_behavior: The template makes the leader coordination-only: split tasks, spawn teams with `team_name`, prevent overlap, monitor progress, resolve blockers, wait for teammates, and never implement directly (`prompt-template/claude/agent-teams-core.md:1-12`). Guidelines require independent parallelizable units, cold-start prompts with sufficient context, strict file ownership, sequencing via dependencies when two tasks touch the same file, progress tracking via TaskList, review of teammate outputs, member commits, optional plan approval, and BitLesson selection before subtasks (`prompt-template/claude/agent-teams-core.md:14-23`). The final section reiterates Task tool usage, monitoring/reassignment, merging/conflict resolution, no direct code edits, and interpreting idle teammates as waiting for response (`prompt-template/claude/agent-teams-core.md:25-31`).
- inputs_outputs_state: Inputs are an RLCR plan/task set, current work status, file paths, team/task boundaries, and Task tool capability. Outputs are delegated subagent tasks, TaskList/progress state, team commits, merged work, and final summary. The template itself has no filesystem state; it shapes agent behavior during RLCR when `agent_teams` is enabled.
- gates_or_invariants: Leader must not write code or edit files directly (`prompt-template/claude/agent-teams-core.md:3`, `prompt-template/claude/agent-teams-core.md:30`). File conflicts are treated as destructive silent overwrites, so file ownership boundaries are mandatory and same-file work must be sequenced with `blockedBy` dependencies (`prompt-template/claude/agent-teams-core.md:18`). Team outputs must be reviewed before tasks are complete (`prompt-template/claude/agent-teams-core.md:20`). BitLesson discipline is required before each subtask (`prompt-template/claude/agent-teams-core.md:23`).
- dependencies_and_callers: Called by RLCR setup/prompt generation when Agent Teams mode is enabled. `scripts/setup-rlcr-loop.sh` exposes `--agent-teams`, validates the experimental env requirement in help text, stores `agent_teams: $AGENT_TEAMS` in state (`scripts/setup-rlcr-loop.sh:125-127`, `scripts/setup-rlcr-loop.sh:893`), and later appends agent-team instructions requiring delegation rather than direct implementation (`scripts/setup-rlcr-loop.sh:1383-1406` from search evidence). It coordinates with `prompt-template/claude/agent-teams-instructions.md` and continuation templates.
- edge_cases_or_failure_modes: If tasks are not split by file ownership, teammate changes can overwrite each other. If leader implements while waiting, it violates role separation. If cold-start prompts omit plan/current state/file boundaries, teammates may make conflicting or irrelevant changes. If multiple subtasks require the same file, missing dependencies can lose work.
- validation_or_tests: `tests/test-agent-teams.sh` validates Agent Teams prompt/state behavior in the repository. Static assertions also search this template and setup output for team-mode instructions. This file itself is static prompt content.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-181 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness test for setup-time base branch detection. It validates the branch-selection algorithm used by `scripts/setup-rlcr-loop.sh` before RLCR code review can compare against a stable local base.
- algorithmic_behavior: The file defines a local `detect_base_branch` helper that replicates setup logic using `run_with_timeout` (`tests/robustness/test-base-branch-detection.sh:31-64`). Priority is remote default branch from `git remote show origin`, then local `main`, then local `master`, then failure (`tests/robustness/test-base-branch-detection.sh:41-63`). Test 1 creates a repo with local `main` and no origin and expects `main` (`tests/robustness/test-base-branch-detection.sh:66-93`). Test 2 creates local `master`, deletes `main`, and expects `master` (`tests/robustness/test-base-branch-detection.sh:95-122`). Test 3 creates only `develop` and expects failure (`tests/robustness/test-base-branch-detection.sh:124-149`). Test 4 creates a bare origin and local `main`, pushes, and accepts remote/default or local `main` behavior (`tests/robustness/test-base-branch-detection.sh:151-191`).
- inputs_outputs_state: Inputs are temporary git repositories, optional origin remote, local branch refs, and timeout seconds. Outputs are detected base branch string or nonzero failure. State created during tests includes temp repos, commits, branches, remotes, and pushed refs. The production setup then captures `base_commit` from the selected branch (`scripts/setup-rlcr-loop.sh:803-811`) and writes `base_branch`/`base_commit` to state (`scripts/setup-rlcr-loop.sh:887-890`).
- gates_or_invariants: Production setup priority is user-specified base branch > remote default if it exists locally > local `main` > local `master` > error (`scripts/setup-rlcr-loop.sh:740-793`). User-specified base branches must exist locally; remote-only branches produce a clear error and fetch instruction (`scripts/setup-rlcr-loop.sh:744-760`). Base branch names must be YAML-safe (`scripts/setup-rlcr-loop.sh:795-800`). Base commit SHA is captured at loop start so reviewing while on the base branch does not later diff against an advanced self-ref (`scripts/setup-rlcr-loop.sh:803-811`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and git CLI. Production caller is `scripts/setup-rlcr-loop.sh`, which runs this branch selection during RLCR initialization after git clean checks (`scripts/setup-rlcr-loop.sh:714-793`).
- edge_cases_or_failure_modes: Missing origin must not abort under `set -euo pipefail`; the helper guards remote detection with `|| true` (`tests/robustness/test-base-branch-detection.sh:41-44`). Remote default may be unknown or remote-only; production rejects remote-only local refs. No `main`/`master` local branch fails. Git operation timeouts or sandbox restrictions can cause detection failure. The test helper comments reference older setup line numbers, but the behavior matches current setup lines 740-793.
- validation_or_tests: This assigned file is the executable validation. It prints a base branch detection summary via `print_test_summary` (`tests/robustness/test-base-branch-detection.sh:193-198`). I did not run it because it creates temp repos and commits, while this branch export is read-only/research-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 item-evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none in Item Evidence headings
- final_worker_status: `complete`