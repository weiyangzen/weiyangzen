# agent_18 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-018 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell-library runtime for Humanize CLI monitoring, configuration resolution, and model/provider routing. This directory is not the branch-specific background-stop implementation; that lives in `hooks/lib/loop-bg-tasks.sh`, sourced through `hooks/lib/loop-common.sh`. `scripts/lib` still provides core support algorithms used by sibling scripts and the CLI monitor surface.
- algorithmic_behavior: Recursively inspected all direct descendants: `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/lib/monitor-common.sh`, and `scripts/lib/monitor-skill.sh`. There are no nested subdirectories under `scripts/lib`.
- algorithmic_behavior: `config-loader.sh` provides guarded, strict-mode config merging. It requires `jq`, prepares default/user/project layers as valid JSON objects, strips nulls recursively, and merges `{}` + default + user + project using jq object multiplication, so later layers override earlier layers (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:24`, `scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:113`). Missing/malformed required default config is fatal; missing/malformed optional user/project config becomes `{}` with warning (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`). `get_config_value` reads one top-level key, stringifies non-string scalar values, and emits empty for missing/null (`scripts/lib/config-loader.sh:139`).
- algorithmic_behavior: `model-router.sh` classifies models into `codex` or `claude`, checks matching CLI dependencies, and maps effort names across providers. Codex is selected for `gpt-*` or `o[0-9]*`; Claude for `claude-*`, `haiku`, `sonnet`, or `opus` (`scripts/lib/model-router.sh:10`). Unknown model/provider/effort values return non-zero with explicit stderr (`scripts/lib/model-router.sh:28`, `scripts/lib/model-router.sh:44`, `scripts/lib/model-router.sh:79`). `xhigh` maps to `high` only for Claude (`scripts/lib/model-router.sh:84`).
- algorithmic_behavior: `monitor-common.sh` contains terminal/status utilities, state-file discovery, YAML frontmatter parsing, and goal-tracker table/list counters. It finds active `methodology-analysis-state.md` before `state.md`, otherwise derives stop reason from `*-state.md` (`scripts/lib/monitor-common.sh:154`). It parses frontmatter with sed/grep (`scripts/lib/monitor-common.sh:201`) and derives goal progress counts by scanning goal-tracker sections (`scripts/lib/monitor-common.sh:262`, `scripts/lib/monitor-common.sh:300`).
- algorithmic_behavior: `monitor-skill.sh` implements `_humanize_monitor_skill`, a live/once monitor for `.humanize/skill` invocations from ask-codex/ask-gemini. It filters by tool, finds latest/best invocation directories, counts status categories, locates global/local cache output/log files, renders a fixed status bar, tails live output, and cleans up background tail processes on signals (`scripts/lib/monitor-skill.sh:18`, `scripts/lib/monitor-skill.sh:53`, `scripts/lib/monitor-skill.sh:79`, `scripts/lib/monitor-skill.sh:121`, `scripts/lib/monitor-skill.sh:169`, `scripts/lib/monitor-skill.sh:339`, `scripts/lib/monitor-skill.sh:428`).
- inputs_outputs_state: `config-loader.sh` input is plugin root, project root, optional `XDG_CONFIG_HOME`, `HOME`, and `HUMANIZE_CONFIG`; output is merged JSON on stdout and warnings/errors on stderr. It creates temporary files and removes them with trap (`scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:100`).
- inputs_outputs_state: `model-router.sh` input is model/provider/effort strings and `PATH`; output is provider/effort strings on stdout or diagnostics on stderr. It has no persistent state.
- inputs_outputs_state: `monitor-common.sh` input is loop/session paths, state markdown, goal tracker markdown, terminal size; output is formatted status values/strings. It does not mutate loop state.
- inputs_outputs_state: `monitor-skill.sh` input is `.humanize/skill` plus optional `--once` and `--tool-filter`; output is terminal UI or once-mode text summary. It maintains local loop variables and a `tail -f` PID but does not change invocation metadata; cleanup kills/disowns the tail process (`scripts/lib/monitor-skill.sh:31`, `scripts/lib/monitor-skill.sh:440`).
- gates_or_invariants: Source guards prevent double sourcing in config/model libraries (`scripts/lib/config-loader.sh:3`, `scripts/lib/model-router.sh:7`). Config default layer is required and must be a JSON object; optional layers degrade to `{}`. Template/state monitors avoid zsh glob failures by using `find` instead of unguarded globs (`scripts/lib/monitor-common.sh:49`, `scripts/lib/monitor-skill.sh:88`). Skill directories must timestamp-prefix as `YYYY-MM-DD_HH-MM-SS` to participate in sorting/counting (`scripts/lib/monitor-skill.sh:77`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` sources `scripts/lib/config-loader.sh` and reads `bitlesson_model`, `codex_model`, `codex_effort`, and `agent_teams` from merged config (`hooks/lib/loop-common.sh:179`, `hooks/lib/loop-common.sh:189`). `scripts/bitlesson-select.sh` sources both config loader and model router to choose/validate provider (`scripts/bitlesson-select.sh:10`, `scripts/bitlesson-select.sh:110`). `scripts/humanize.sh` sources monitor common at startup and monitor skill near the command dispatch path (`scripts/humanize.sh:8`, `scripts/humanize.sh:1244`), then exposes `humanize monitor skill|codex|gemini` (`scripts/humanize.sh:1204`).
- edge_cases_or_failure_modes: Missing `jq` is fatal for config loading. Malformed optional config is ignored with warning, so stale bad user/project config does not block normal operation. `model-router.sh` has a narrow provider vocabulary and rejects Gemini/other model names. `monitor_get_yaml_value` is simple grep/sed frontmatter extraction and can misread duplicate keys or complex YAML. Skill monitor legacy behavior treats invocations without tool tags as Codex when filtering for Codex (`scripts/lib/monitor-skill.sh:72`).
- validation_or_tests: `tests/test-config-merge.sh` and `tests/test-config-error-handling.sh` cover merge precedence, null stripping, malformed optional configs, and missing required defaults. `tests/test-model-router.sh` covers provider detection, dependency checks, and effort mapping. `tests/test-skill-monitor.sh` covers once-mode skill monitor behavior, cache lookup, and tool filtering. I did not run tests; this is read-only evidence gathering.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-048 `file` `scripts/ask-gemini.sh`
- cursor: `[_]`
- core_role: Runtime script for `/humanize:ask-gemini`, implementing one-shot Gemini CLI consultation with persistent invocation records and monitor-readable cache artifacts. It supports workflow state visibility but is not the branch’s background-stop short-circuit itself.
- algorithmic_behavior: Parses flags `--gemini-model`, `--gemini-timeout`, `--`, and free-form question parts (`scripts/ask-gemini.sh:85`). Validates `gemini` CLI availability, non-empty question, and a safe model-name character set (`scripts/ask-gemini.sh:142`, `scripts/ask-gemini.sh:150`, `scripts/ask-gemini.sh:159`). Detects project root using `git rev-parse` or `CLAUDE_PROJECT_DIR`/`pwd` fallback (`scripts/ask-gemini.sh:171`). Creates a project-local invocation directory and a global cache directory keyed by sanitized project path (`scripts/ask-gemini.sh:181`, `scripts/ask-gemini.sh:185`, `scripts/ask-gemini.sh:189`). Writes `input.md`, debug command/prompt file, runs Gemini through `run_with_timeout`, classifies result, writes `metadata.md`, copies successful stdout to `output.md`, and finally prints Gemini stdout (`scripts/ask-gemini.sh:202`, `scripts/ask-gemini.sh:248`, `scripts/ask-gemini.sh:279`, `scripts/ask-gemini.sh:299`, `scripts/ask-gemini.sh:365`, `scripts/ask-gemini.sh:385`).
- inputs_outputs_state: Inputs are CLI args, env vars `HUMANIZE_GEMINI_YOLO`, `XDG_CACHE_HOME`, `HOME`, `CLAUDE_PROJECT_DIR`, current Git repo, and Gemini CLI behavior. Outputs: Gemini response to stdout; status/log pointers to stderr; `.humanize/skill/<unique-id>/input.md`, `output.md`, `metadata.md`; cache files `gemini-run.cmd`, `gemini-run.out`, `gemini-run.log` under `~/.cache/humanize/<sanitized-project>/skill-<unique-id>/` or local fallback `SKILL_DIR/cache` (`scripts/ask-gemini.sh:16`, `scripts/ask-gemini.sh:244`).
- inputs_outputs_state: Metadata state transition is absent/running while Gemini is executing, then terminal `status: timeout`, `error`, `empty_response`, or `success` is written (`scripts/ask-gemini.sh:299`, `scripts/ask-gemini.sh:323`, `scripts/ask-gemini.sh:347`, `scripts/ask-gemini.sh:367`). `scripts/lib/monitor-skill.sh` interprets missing metadata as running and uses `Tool: gemini` from `input.md` until metadata exists (`scripts/lib/monitor-skill.sh:53`, `scripts/lib/monitor-skill.sh:174`).
- gates_or_invariants: Default model is `gemini-3.1-pro-preview`, default timeout `3600` seconds (`scripts/ask-gemini.sh:36`). Timeout must be digits only, although the message says positive integer and `0` technically matches (`scripts/ask-gemini.sh:115`). Model may contain only alphanumeric, dot, underscore, hyphen (`scripts/ask-gemini.sh:159`). Approval mode is `--yolo` only when `HUMANIZE_GEMINI_YOLO=true|1`; otherwise `--sandbox` (`scripts/ask-gemini.sh:223`). Prompt is always augmented with a Google Search instruction (`scripts/ask-gemini.sh:233`).
- dependencies_and_callers: Sources `scripts/portable-timeout.sh`, whose `run_with_timeout` chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/ask-gemini.sh:30`; `scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:33`). Skill metadata in `skills/ask-gemini/SKILL.md` allows Bash execution of this script and specifically warns callers to quote free-form user text (`skills/ask-gemini/SKILL.md:5`, `skills/ask-gemini/SKILL.md:17`). The humanize skill monitor recognizes Gemini invocation files and cache prefixes (`scripts/lib/monitor-skill.sh:168`, `scripts/lib/monitor-skill.sh:179`).
- edge_cases_or_failure_modes: If home cache cannot be created, the script falls back to project-local `SKILL_DIR/cache` and warns (`scripts/ask-gemini.sh:192`). If no timeout implementation exists, portable-timeout warns and runs without timeout (`scripts/portable-timeout.sh:64`). Timeout exit code `124` is handled distinctly with retry guidance and terminal metadata (`scripts/ask-gemini.sh:291`). Non-zero Gemini exit includes last 20 stderr lines when available (`scripts/ask-gemini.sh:313`). Empty stdout after exit 0 is treated as `empty_response` and exits 1 (`scripts/ask-gemini.sh:337`). The debug command stores prompt content in cache, which is useful for audit but can persist sensitive question text.
- validation_or_tests: No dedicated `test-ask-gemini.sh` appeared in the assigned list; repository references cover the skill wrapper and monitor behavior. `skills/ask-gemini/SKILL.md` documents expected exit-code semantics (`skills/ask-gemini/SKILL.md:48`). I did not execute the script because it requires Gemini CLI/network and would write state.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-078 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for resilient template-loader behavior under error conditions. This is a support contract for hook/block prompt rendering: missing templates and malformed placeholder cases should not crash strict-mode loop scripts.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, resolves `TEMPLATE_DIR`, then runs 12 tests with pass/fail counters (`tests/test-error-scenarios.sh:11`, `tests/test-error-scenarios.sh:15`, `tests/test-error-scenarios.sh:22`). It validates `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir`.
- inputs_outputs_state: Inputs are repository template files and functions from `hooks/lib/template-loader.sh`. Outputs are colored PASS/FAIL lines and process exit 0 iff all checks pass (`tests/test-error-scenarios.sh:25`, `tests/test-error-scenarios.sh:218`). It creates no persistent files; several tests run command substitutions and one isolated `bash -c` strict-mode subshell (`tests/test-error-scenarios.sh:114`).
- gates_or_invariants: Missing template file returns empty string and exit 0 (`tests/test-error-scenarios.sh:45`). Missing template directory also returns empty string and exit 0 (`tests/test-error-scenarios.sh:58`). `load_and_render` with missing template returns empty without failing (`tests/test-error-scenarios.sh:71`). Empty content renders empty (`tests/test-error-scenarios.sh:84`). Special regex characters in replacement values are preserved literally (`tests/test-error-scenarios.sh:97`). Strict-mode scripts continue after missing template rendering (`tests/test-error-scenarios.sh:112`). Safe renderer uses fallback and substitutes fallback variables (`tests/test-error-scenarios.sh:137`, `tests/test-error-scenarios.sh:150`). Valid template dir accepted, invalid rejected (`tests/test-error-scenarios.sh:164`, `tests/test-error-scenarios.sh:175`). Empty and unclosed placeholders stay literal (`tests/test-error-scenarios.sh:186`, `tests/test-error-scenarios.sh:199`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`. That loader defines single-pass `{{VAR}}` substitution via awk so replacement values are not rescanned, preventing placeholder injection/corruption (`hooks/lib/template-loader.sh:48`, `hooks/lib/template-loader.sh:69`). `load_and_render_safe` suppresses loader/render failures and emits fallback when content/result is empty (`hooks/lib/template-loader.sh:188`). Many hooks use `load_and_render_safe`, including `state_file_blocked_message`, BitLesson delta validation, and Codex review prompt generation (`hooks/lib/loop-common.sh:847`; `scripts/bitlesson-validate-delta.sh:263`; `hooks/loop-codex-stop-hook.sh:1073`).
- edge_cases_or_failure_modes: The tests deliberately encode graceful degradation rather than fail-fast behavior for missing templates. This means a missing block template can silently fall back or empty-output depending on caller API; critical validators must use `load_and_render_safe`, which is separately checked by `tests/test-template-references.sh`. `set -uo pipefail` is used in this test, not `set -e`; strict-mode behavior is tested inside a subshell (`tests/test-error-scenarios.sh:9`, `tests/test-error-scenarios.sh:114`).
- validation_or_tests: This file itself is validation. It is included in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:72`). I did not execute it in this read-only research run.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-108 `file` `prompt-template/block/bitlesson-delta-empty-kb.md`
- cursor: `[_]`
- core_role: Stop/block prompt text used when BitLesson delta validation rejects `Action: none` because the knowledge base has no concrete lesson entries. It is a gate message template for forcing reusable lesson capture after issue-resolution rounds.
- algorithmic_behavior: Static markdown with no placeholders in the file itself. It tells the agent that `Action: none` is disallowed while `.humanize/bitlesson.md` has no concrete lesson entries, and requires `Action: add` or `Action: update` when previous-round issues are resolved (`prompt-template/block/bitlesson-delta-empty-kb.md:1`).
- inputs_outputs_state: Input is only template loading by `load_and_render_safe`. Output is a markdown block passed as the reason/system instruction to `block_exit` in `scripts/bitlesson-validate-delta.sh`. It does not mutate state itself.
- gates_or_invariants: The consuming gate calculates `CONCRETE_BITLESSON_COUNT` by scanning `Lesson ID:` entries and excluding placeholders (`scripts/bitlesson-validate-delta.sh:227`). If `BITLESSON_ACTION == none`, there are zero concrete lesson entries, and `BITLESSON_ALLOW_EMPTY_NONE != true`, this template is rendered and the hook blocks (`scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:254`, `scripts/bitlesson-validate-delta.sh:263`, `scripts/bitlesson-validate-delta.sh:266`).
- dependencies_and_callers: Direct caller is `scripts/bitlesson-validate-delta.sh`, which uses `load_and_render_safe "$TEMPLATE_DIR" "block/bitlesson-delta-empty-kb.md"` with fallback text containing `CURRENT_ROUND` and `BITLESSON_FILE` variables (`scripts/bitlesson-validate-delta.sh:255`). The stop hook passes `--allow-empty-none "$BITLESSON_ALLOW_EMPTY_NONE"` to that validator (`hooks/loop-codex-stop-hook.sh:828`), and derives `BITLESSON_ALLOW_EMPTY_NONE` from state/env with defensive defaulting (`hooks/loop-codex-stop-hook.sh:159`).
- edge_cases_or_failure_modes: Because this concrete template has no `{{CURRENT_ROUND}}` or `{{BITLESSON_FILE}}` placeholders, rendered output is less specific than the fallback. Missing template would still block via fallback because the caller uses `load_and_render_safe`. If `BITLESSON_ALLOW_EMPTY_NONE=true`, the empty-KB `Action: none` path is allowed and this template is bypassed.
- validation_or_tests: `tests/test-stop-hook-legacy-compat.sh` covers legacy loops where BitLesson-required behavior should remain disabled if state omits it, avoiding accidental blocks. Template existence/reference checks include block template references in `tests/test-template-references.sh`. I did not run tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-138 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Canonical block message for attempts to modify `state.md`, the RLCR loop state file. It protects scheduler-owned state transitions from direct agent writes.
- algorithmic_behavior: Static markdown block states that `state.md` cannot be modified because it is managed by the loop system, and instructs the agent to stop if it believes work is done so review can be auto-triggered (`prompt-template/block/state-file-modification.md:1`).
- inputs_outputs_state: Input is template loading via `state_file_blocked_message`; output is stderr block text from write/edit/bash validators. No direct state changes.
- gates_or_invariants: `hooks/lib/loop-common.sh` wraps this template in `state_file_blocked_message`, with inline fallback if the template is missing (`hooks/lib/loop-common.sh:847`). Validators check more specific state names before generic `state.md` because generic matching could catch `finalize-state.md`/methodology state (`hooks/loop-write-validator.sh:188`, `hooks/loop-edit-validator.sh:152`). Write validator blocks state paths with exit 2 after printing this message (`hooks/loop-write-validator.sh:200`). Edit validator does the same (`hooks/loop-edit-validator.sh:164`). Bash validator blocks direct and source-side modifications/copy/move patterns with this message (`hooks/loop-bash-validator.sh:277`, `hooks/loop-bash-validator.sh:435`, `hooks/loop-bash-validator.sh:467`).
- dependencies_and_callers: Depends on `load_and_render_safe` from `hooks/lib/template-loader.sh`. Used indirectly by loop write/edit/bash validators through `hooks/lib/loop-common.sh`. The file is explicitly listed in template-reference tests (`tests/test-template-references.sh:155`).
- edge_cases_or_failure_modes: Template only mentions `state.md`; separate templates/functions handle `finalize-state.md` and `methodology-analysis-state.md`. Generic state matching must be ordered after specific state matches to avoid wrong guidance. Bash command detection must distinguish reads like `cat state.md` from writes, which is covered by validator pattern tests (`tests/test-bash-validator-patterns.sh:181`).
- validation_or_tests: `tests/robustness/test-hook-system-robustness.sh` includes a Bash validator state modification test (`tests/robustness/test-hook-system-robustness.sh:316`). `tests/test-cancel-signal-file.sh` verifies unrelated `state.md` modifications remain blocked even when cancel signal behavior exists. `tests/test-finalize-phase.sh` separately validates finalize-state protection. I did not run tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-168 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex review prompt contract for normal RLCR review rounds. It defines the review algorithm Codex must follow before allowing Claude to stop: compare implementation to original plan and goal tracker, classify findings, require a mainline verdict, and only end with `COMPLETE` when all work is truly complete.
- algorithmic_behavior: Template variables include `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `COMMIT_HISTORY_SECTION`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`. It instructs Codex to read the original plan first (`prompt-template/codex/regular-review.md:5`), review Claude’s summary against plan/prompt (`prompt-template/codex/regular-review.md:11`), treat deferrals/pending tasks as incomplete and include implementation plans for them (`prompt-template/codex/regular-review.md:26`, `prompt-template/codex/regular-review.md:30`), perform mandatory goal alignment checks (`prompt-template/codex/regular-review.md:35`), classify findings into Mainline Gaps, Blocking Side Issues, and Queued Side Issues (`prompt-template/codex/regular-review.md:49`), and emit a mandatory `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` line (`prompt-template/codex/regular-review.md:56`).
- inputs_outputs_state: Input is rendered context from the stop hook and prior round artifacts. Output is a prompt file written to `REVIEW_PROMPT_FILE`; Codex’s later review result is expected at `REVIEW_RESULT_FILE`. This template does not mutate state directly, but it controls whether downstream Codex output may stop the loop by ending with `COMPLETE` (`prompt-template/codex/regular-review.md:71`, `prompt-template/codex/regular-review.md:72`).
- gates_or_invariants: The contract says `COMPLETE` as the last line is allowed only if all original plan tasks are fully complete, all ACs are met, and no deferrals/pending work remain (`prompt-template/codex/regular-review.md:72`). It explicitly says deferred and unfinished items are incomplete (`prompt-template/codex/regular-review.md:73`). Missing the mainline verdict line is itself a stop-hook blocker by contract (`prompt-template/codex/regular-review.md:61`). It also prevents queued side issues from hijacking the mainline by requiring a progress verdict and classification lanes (`prompt-template/codex/regular-review.md:63`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` when `FULL_ALIGNMENT_CHECK` is not true, via `load_and_render_safe "$TEMPLATE_DIR" "codex/regular-review.md"` into `REVIEW_PROMPT_FILE` (`hooks/loop-codex-stop-hook.sh:1071`). Fallback prompt exists but is much shorter and lacks the full classification/goal-alignment contract (`hooks/loop-codex-stop-hook.sh:1042`). Template rendering uses the single-pass loader in `hooks/lib/template-loader.sh`, so summary content containing `{{...}}` will not be recursively substituted (`hooks/lib/template-loader.sh:52`).
- edge_cases_or_failure_modes: If the template is missing, the fallback still asks for review and `COMPLETE`, but loses mandatory detailed sections; this makes template-reference validation important. If Codex omits the verdict, the prompt states the stop hook will block and require rerun (`prompt-template/codex/regular-review.md:61`). The template’s strict “no deferrals” rule can intentionally keep the loop open even when implementation is mostly done.
- validation_or_tests: `tests/test-codex-review-merge.sh`, `tests/test-finalize-phase.sh`, and stop-hook tests exercise Codex review result handling, including COMPLETE/failure paths. `tests/test-template-references.sh` checks that referenced templates exist and critical validators use safe rendering. I did not run tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-018, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-048, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-078, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-108, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-138, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-168
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`