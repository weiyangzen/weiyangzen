# agent_23 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-023 `directory` `skills/humanize-refine-plan`
- cursor: `[_]`
- core_role: Defines the non-user-invocable `humanize-refine-plan` flow skill. The directory contains one file, `skills/humanize-refine-plan/SKILL.md`, and acts as the agent-facing algorithm contract for refining annotated generated plans into a cleaned plan plus QA ledger.
- algorithmic_behavior: The flow is explicitly staged: parse arguments and paths, load merged config, run IO validation, extract `CMT:` / `ENDCMT` blocks with a stateful scanner, classify comments, process comments, generate a comment-free refined plan, validate the refined plan, generate QA from `prompt-template/plan/refine-plan-qa-template.md`, optionally translate variants, then atomically write outputs. See `skills/humanize-refine-plan/SKILL.md:18-47`.
- inputs_outputs_state: Inputs are `--input`, optional `--output`, optional `--qa-dir`, optional `--alt-language`, and mutually exclusive `--discussion` / `--direct` mode flags (`skills/humanize-refine-plan/SKILL.md:50-65`). Outputs are the refined plan, a QA ledger, and optional language variants (`skills/humanize-refine-plan/SKILL.md:96-107`). State is mostly document state: extracted raw comment records, classification/disposition state, convergence status, and optional pending decisions.
- gates_or_invariants: The skill preserves the `gen-plan` schema and required sections, removes all resolved CMT blocks, preserves optional sections, restricts routing tags to `coding` or `analyze`, and writes atomically (`skills/humanize-refine-plan/SKILL.md:67-87`). It forbids passing `--alt-language` to `validate-refine-plan-io.sh` because the validator rejects it (`skills/humanize-refine-plan/SKILL.md:62-65`). Validation exit codes 0-7 are part of the flow contract (`skills/humanize-refine-plan/SKILL.md:131-142`).
- dependencies_and_callers: Depends on `scripts/validate-refine-plan-io.sh` for input/output validation (`skills/humanize-refine-plan/SKILL.md:22`), `prompt-template/plan/refine-plan-qa-template.md` for ledger structure (`skills/humanize-refine-plan/SKILL.md:41`), and config-loader semantics as mirrored in `commands/refine-plan.md:86-153`. Installation/test wiring is covered by `tests/test-refine-plan.sh:820-855`.
- edge_cases_or_failure_modes: Ambiguous comments route to user confirmation in discussion mode or smallest safe assumptions in direct mode (`skills/humanize-refine-plan/SKILL.md:31-34`, `commands/refine-plan.md:306-312`). Unsupported or redundant alternate language values are normalized or skipped; English is a no-op (`skills/humanize-refine-plan/SKILL.md:108-129`). Blocking inconsistencies after plan validation stop the flow instead of inventing requirements (`skills/humanize-refine-plan/SKILL.md:36-40`).
- validation_or_tests: `tests/test-refine-plan.sh` verifies command metadata, section preservation, CMT extraction requirements, alt-language handling, atomic-write requirements, QA template coverage, and skill install wiring (`tests/test-refine-plan.sh:650-855`). Validator behavior is specified in script tests for exit codes, malformed comments, ignored code-fence/HTML markers, writable directories, QA directory creation, and mode reporting (`tests/test-refine-plan.sh:1044-1348`). I did not execute tests; this was read-only research.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-053 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Provides the interactive `humanize` shell function, especially `humanize monitor rlcr`, plus parsing helpers used by monitor/status behavior. It is core runtime observability for RLCR loop sessions and relevant to this branch because it handles live monitoring while stop-hook background-task behavior may leave loops parked/resumable.
- algorithmic_behavior: On load, it sources `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh` if present (`scripts/humanize.sh:6-16`). It exposes helpers for pipe splitting, goal-tracker parsing, git-state detection, and git status summaries (`scripts/humanize.sh:24-252`). `_humanize_monitor_codex` discovers the latest `.humanize/rlcr` session, maps it to cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, and renders a fixed terminal status bar plus incremental log tailing (`scripts/humanize.sh:261-375`, `scripts/humanize.sh:414-649`, `scripts/humanize.sh:838-1179`).
- inputs_outputs_state: Inputs include current project directory, `.humanize/rlcr` session dirs, state files (`state.md`, `methodology-analysis-state.md`, `*-state.md`), goal tracker, git status, terminal dimensions, and cached `round-*-codex-run.log` / `round-*-codex-review.log` files (`scripts/humanize.sh:266-405`). Output is terminal UI text and log streaming; internal state tracks `current_session_dir`, `current_file`, `last_size`, `monitor_running`, `tail_pid`, `cleanup_done`, and resize flags (`scripts/humanize.sh:731-838`).
- gates_or_invariants: Requires `.humanize/rlcr` to exist before starting (`scripts/humanize.sh:272-277`). It only searches logs for the current session, preventing accidental display of stale logs from another session (`scripts/humanize.sh:291-301`). It validates log phase ordering defensively: max codex-run round must be strictly less than min codex-review round when both exist, otherwise it reports inconsistent log state (`scripts/humanize.sh:334-371`). Terminal resize handling is delayed to safe points so signal handlers only set a flag (`scripts/humanize.sh:779-796`, `scripts/humanize.sh:851-873`, `scripts/humanize.sh:1033-1063`).
- dependencies_and_callers: Calls shared monitor helpers from `scripts/lib/monitor-common.sh` for latest-session discovery, state-file discovery, file sizes, and parse support (`scripts/humanize.sh:279-282`, `scripts/humanize.sh:377-380`, `scripts/humanize.sh:828-831`). Sources `scripts/lib/monitor-skill.sh` to provide `humanize monitor skill|codex|gemini` (`scripts/humanize.sh:1190-1246`). Runtime logs are produced by stop hook / ask-codex paths such as `hooks/loop-codex-stop-hook.sh:1635-1637` and `scripts/ask-codex.sh:262-264`.
- edge_cases_or_failure_modes: If the RLCR root is deleted while monitoring, `_graceful_stop` calls cleanup, restores terminal state, and exits with a user-facing message (`scripts/humanize.sh:764-777`, `scripts/humanize.sh:841-844`, `scripts/humanize.sh:1027-1030`). If no log exists yet, it displays a centered waiting message and continues polling (`scripts/humanize.sh:883-1008`). It switches to newer sessions/logs, handles session/log deletion, detects truncation/rotation, and resets read position (`scripts/humanize.sh:1081-1177`). Signal cleanup is guarded to run once and kill any tracked background tail process (`scripts/humanize.sh:736-762`).
- validation_or_tests: Monitor runtime and E2E tests cover graceful stop on directory deletion and terminal restoration (`tests/test-monitor-runtime.sh:5-10`, `tests/test-monitor-e2e-real.sh:156-190`), plus SIGINT cleanup behavior (`tests/test-monitor-e2e-sigint.sh:12-13`, `tests/test-monitor-e2e-real.sh:476-540`). Zsh safety is referenced by `tests/test-zsh-monitor-safety.sh`. I did not execute tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-083 `file` `tests/test-model-router.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/lib/model-router.sh`, which routes model names to providers, validates required provider binaries, and maps reasoning effort values across Codex/Claude providers. This supports workflow routing in scripts such as `scripts/bitlesson-select.sh`.
- algorithmic_behavior: The test sources `tests/test-helpers.sh` and `scripts/lib/model-router.sh`, then runs 20 numbered checks (`tests/test-model-router.sh:1-8`, `tests/test-model-router.sh:29-426`). It creates mock provider binaries in temporary directories to isolate `PATH` behavior (`tests/test-model-router.sh:17-27`, `tests/test-model-router.sh:239-277`). Each check captures function stdout/stderr and exit code, then calls `pass` or `fail`.
- inputs_outputs_state: Inputs are model names, provider names, effort strings, and controlled `PATH` values. Outputs are expected provider strings (`codex` / `claude`), effort strings (`xhigh`, `high`, `medium`, `low`), nonzero error states, and test summary counts via `print_test_summary` (`tests/test-model-router.sh:35-230`, `tests/test-model-router.sh:243-426`). Temporary test state is created by `setup_test_dir` from `tests/test-helpers.sh:84-89`.
- gates_or_invariants: `detect_provider` must route `gpt-*` and `o[0-9]*` model names to Codex, while `haiku`, `sonnet`, `opus`, and `claude-*` names route to Claude (`tests/test-model-router.sh:30-196`; implementation in `scripts/lib/model-router.sh:10-30`). Unknown and empty model names must fail with error text (`tests/test-model-router.sh:198-230`). `check_provider_dependency` must require matching `codex` or `claude` binaries (`tests/test-model-router.sh:232-298`; implementation in `scripts/lib/model-router.sh:32-60`). `map_effort` accepts only `xhigh|high|medium|low` and maps `xhigh` to `high` for Claude with an info log (`tests/test-model-router.sh:300-420`; implementation in `scripts/lib/model-router.sh:62-91`).
- dependencies_and_callers: Direct dependency is `scripts/lib/model-router.sh`. Known caller `scripts/bitlesson-select.sh` loads the router, detects the configured BitLesson model provider, optionally overrides Claude to Codex in `codex-only` mode, checks CLI dependencies, and then dispatches either `codex exec` or `claude --print` (`scripts/bitlesson-select.sh:9-24`, `scripts/bitlesson-select.sh:106-126`, `scripts/bitlesson-select.sh:183-217`).
- edge_cases_or_failure_modes: The test constrains missing provider binaries to fail loudly and mention the missing binary (`tests/test-model-router.sh:250-264`, `tests/test-model-router.sh:283-298`). It also fixes error behavior for invalid efforts on both providers (`tests/test-model-router.sh:388-420`). Case-insensitive Claude family detection is covered by `claude-3-OPUS-20240229` (`tests/test-model-router.sh:181-196`).
- validation_or_tests: This file is itself the validator and is listed in `tests/run-all-tests.sh:98`. I did not execute it in this read-only pass.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-113 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Block-message template for a GitHub Claude-bot review path where no `eyes` reaction appears after retries. It is a prompt/block template defining a failure gate response, but within this branch export I did not find a live shell-script call site that renders this exact template.
- algorithmic_behavior: The template reports that the Claude bot did not respond after `{{RETRY_COUNT}}` retries and `{{TOTAL_WAIT_SECONDS}}` total seconds, lists possible causes, gives required actions, and tells the user to post a new trigger comment once configured (`prompt-template/block/claude-eyes-timeout.md:1-18`).
- inputs_outputs_state: Inputs are template variables `RETRY_COUNT` and `TOTAL_WAIT_SECONDS` (`prompt-template/block/claude-eyes-timeout.md:3`). Output is a human-readable block reason. It does not mutate state itself; any state transition would be owned by the caller rendering it.
- gates_or_invariants: The invariant is retry exhaustion without an `eyes` reaction: the gate should not silently continue as if Claude GitHub App review started. The message steers toward configuration/permission checks (`prompt-template/block/claude-eyes-timeout.md:5-18`).
- dependencies_and_callers: Depends on the generic template loader if used: `hooks/lib/template-loader.sh` provides `load_template`, `render_template`, and `load_and_render_safe` (`hooks/lib/template-loader.sh:17-20`, referenced by search). Repository fixtures include `tests/fixtures/comment-reactions.json` with an `eyes` reaction, but no direct `load_and_render_safe "$TEMPLATE_DIR" "block/claude-eyes-timeout.md"` call was found in inspected shell/scripts. Template reference tests scan for missing referenced templates generally (`tests/test-template-references.sh:51-111`), but this template appears unreferenced or dynamically referenced.
- edge_cases_or_failure_modes: If a caller expects this template and it is missing, `load_and_render_safe` would fall back if the caller supplies a fallback; unsafe rendering could produce an empty reason. The template itself assumes retry counts were computed by caller logic and does not encode escalation beyond user configuration steps.
- validation_or_tests: Covered indirectly by template existence/renderability suites such as `tests/test-templates-comprehensive.sh` and reference scanning in `tests/test-template-references.sh:121-142`, where unreferenced templates may warn rather than fail. I did not execute tests.
- skip_candidate: `yes: appears to be a valid block template, but no direct runtime caller was found in this branch export, so it may be legacy/dormant relative to the current core stop/background algorithm.`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-143 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the mandatory per-round or finalize work-summary gate. It directly participates in RLCR loop state transitions by blocking exit until the required summary file exists.
- algorithmic_behavior: The template tells the agent it attempted to exit without writing the work summary, renders the required path via `{{SUMMARY_FILE}}`, lists summary contents, and says the agent may retry exit afterward (`prompt-template/block/work-summary-missing.md:1-16`).
- inputs_outputs_state: Input variable is `SUMMARY_FILE` (`prompt-template/block/work-summary-missing.md:5-8`). Output is a block reason consumed by JSON emitted by the stop hook. The relevant state is the active loop phase and current round: normal phase expects `.humanize/rlcr/<session>/round-<N>-summary.md`; finalize phase expects `finalize-summary.md` (`hooks/loop-codex-stop-hook.sh:750-757`).
- gates_or_invariants: The stop hook checks summary-file existence before proceeding to later round-contract/Codex review gates. If absent, it renders this template with `load_and_render_safe`, returns JSON decision `block`, and exits 0 as a hook-controlled block (`hooks/loop-codex-stop-hook.sh:759-783`). In finalize phase the system message changes to “Finalize Phase - summary file missing”; otherwise it includes the current round (`hooks/loop-codex-stop-hook.sh:769-773`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:763-767`. Rendering depends on `hooks/lib/template-loader.sh` via `loop-common.sh`. The template is part of the wider prompt-template reference set checked by `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: Missing template file is mitigated by an inline fallback in the stop hook (`hooks/loop-codex-stop-hook.sh:763-767`). If background work is pending, this gate is intentionally bypassed because `handle_bg_task_short_circuit` runs before normal gate logic and exits 0 with no state termination (`hooks/loop-codex-stop-hook.sh:71-83`, `hooks/lib/loop-bg-tasks.sh:297-333`). That ordering is central to the branch behavior: do not block natural stop while async work is still running.
- validation_or_tests: Template reference validation ensures referenced templates exist (`tests/test-template-references.sh:51-111`) and critical validators prefer safe rendering (`tests/test-template-references.sh:170-200`). Background short-circuit tests verify pending background tasks skip normal gates and preserve state (`tests/test-stop-hook-bg-allow.sh:5-10`, `tests/test-stop-hook-bg-allow.sh:343-410`). I did not execute tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-173 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared utility library for humanize monitor functions. It centralizes terminal colors, file/session/state helpers, frontmatter extraction, display formatting, and goal-tracker parsing used by `scripts/humanize.sh` and `scripts/lib/monitor-skill.sh`.
- algorithmic_behavior: Provides color-code functions (`scripts/lib/monitor-common.sh:12-23`), cross-platform file size via GNU/BSD `stat` fallback (`scripts/lib/monitor-common.sh:29-35`), latest timestamped session lookup (`scripts/lib/monitor-common.sh:37-63`), terminal setup/restore helpers (`scripts/lib/monitor-common.sh:69-92`), status color mapping (`scripts/lib/monitor-common.sh:128-141`), state-file discovery (`scripts/lib/monitor-common.sh:147-192`), YAML frontmatter value extraction (`scripts/lib/monitor-common.sh:198-213`), timestamp/string formatting (`scripts/lib/monitor-common.sh:219-256`), and goal-tracker issue/progress parsing (`scripts/lib/monitor-common.sh:262-383`).
- inputs_outputs_state: Inputs are file paths for loop dirs, session dirs, state files, metadata files, and goal trackers. Outputs are simple strings: latest session path, `state_file|status`, YAML values, formatted timestamps, truncated strings, and pipe-delimited goal summaries. It does not write state and is designed to be sourced, not executed (`scripts/lib/monitor-common.sh:3-6`).
- gates_or_invariants: `monitor_find_latest_session` only accepts immediate child dirs named `YYYY-MM-DD_HH-MM-SS`, avoiding arbitrary directory selection (`scripts/lib/monitor-common.sh:49-60`). `monitor_find_state_file` prioritizes `methodology-analysis-state.md`, then active `state.md`, then any `*-state.md` stop-state file (`scripts/lib/monitor-common.sh:162-190`). Missing tracker files produce zeroed summaries instead of errors (`scripts/lib/monitor-common.sh:265-270`, `scripts/lib/monitor-common.sh:300-305`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh:6-10`; `_humanize_monitor_codex` wraps its latest-session, state-file, and file-size helpers (`scripts/humanize.sh:279-282`, `scripts/humanize.sh:377-380`, `scripts/humanize.sh:828-831`). `scripts/lib/monitor-skill.sh` depends on `monitor_get_yaml_value` and `monitor_format_timestamp` for skill invocation metadata/status displays (`scripts/lib/monitor-skill.sh:8-10`, `scripts/lib/monitor-skill.sh:56-63`, `scripts/lib/monitor-skill.sh:250-280`).
- edge_cases_or_failure_modes: Frontmatter extraction is simple grep/sed and strips double quotes, so complex YAML, duplicate keys, or values containing quotes are not robustly parsed (`scripts/lib/monitor-common.sh:207-212`). The `_count_table_rows` helper is defined inside both goal parser functions; in shell this may leak/redefine a global function when sourced, but behavior is consistent for the current use (`scripts/lib/monitor-common.sh:272-279`, `scripts/lib/monitor-common.sh:307-315`). Goal-tracker parsing is table/heading-pattern dependent and may undercount malformed or schema-drifted trackers.
- validation_or_tests: Runtime monitor tests indirectly cover these helpers through `scripts/humanize.sh` (`tests/test-monitor-e2e-real.sh:144-149`, `tests/test-monitor-e2e-real.sh:464-469`). Robustness tests target the production goal-tracker parser in `scripts/humanize.sh`, whose logic overlaps this library (`tests/robustness/test-goal-tracker-robustness.sh:5-21`). Skill monitor tests exercise YAML metadata parsing through `_humanize_monitor_skill` (`tests/test-skill-monitor.sh`, referenced by search). I did not execute tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6/6 unique item headings; each assigned item appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`