# agent_05 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: Documentation contract surface for Humanize RLCR setup, runtime behavior, configuration precedence, BitLesson selection, and user-facing command semantics. It is not executable code, but several files define the intended algorithm and deployment paths used by hooks/scripts.
- algorithmic_behavior: `docs/usage.md` defines the RLCR loop as an implementation phase followed by Codex summary/code review, continuing until acceptance criteria are met or issues are gone (`docs/usage.md:5`, `docs/usage.md:7`, `docs/usage.md:12`). It specifies pre-flight Plan Understanding Quiz behavior, including advisory skip modes (`docs/usage.md:16`, `docs/usage.md:33`, `docs/usage.md:37`). It also documents command contracts for `start-rlcr-loop`, `gen-plan`, `refine-plan`, and `ask-codex`, including flags such as `--skip-impl`, `--agent-teams`, `--yolo`, and `--skip-quiz` (`docs/usage.md:70`, `docs/usage.md:89`, `docs/usage.md:93`, `docs/usage.md:96`). `docs/bitlesson.md` defines BitLesson model/provider routing, KB initialization, per-task selection, and required `## BitLesson Delta` summary contract (`docs/bitlesson.md:7`, `docs/bitlesson.md:14`, `docs/bitlesson.md:27`, `docs/bitlesson.md:34`, `docs/bitlesson.md:40`). Install guides define where runtime bundle, hooks, and config files are copied for Codex/Kimi/Claude (`docs/install-for-codex.md:25`, `docs/install-for-kimi.md:26`, `docs/install-for-claude.md:47`).
- inputs_outputs_state: Inputs documented here include plan paths, command flags, config layers, provider binaries, and hook install locations. `docs/usage.md` defines config layer order: plugin default, user config, project config, CLI flags (`docs/usage.md:256`, `docs/usage.md:258`). Outputs/state include `.humanize/rlcr/<timestamp>/` progress data (`docs/usage.md:323`), `.humanize/skill/<timestamp>/` ask-codex artifacts (`docs/usage.md:253`), `.humanize/bitlesson.md` (`docs/bitlesson.md:27`), installed Codex hook config `${CODEX_HOME:-~/.codex}/hooks.json` (`docs/install-for-codex.md:28`, `docs/install-for-codex.md:67`), and skill/runtime directories under Codex/Kimi homes (`docs/install-for-codex.md:25`, `docs/install-for-kimi.md:27`).
- gates_or_invariants: Usage docs specify that gen-plan validates input/output paths and optionally auto-starts RLCR only when convergence conditions are met (`docs/usage.md:120`, `docs/usage.md:126`). Refine-plan ignores comment markers inside fenced code/HTML comments and requires at least one non-empty comment block (`docs/usage.md:209`, `docs/usage.md:211`). BitLesson summary validation is strict: `Action: none` requires `NONE`/empty IDs, while add/update must reference concrete `BL-YYYYMMDD-short-name` IDs present in `.humanize/bitlesson.md` (`docs/bitlesson.md:47`, `docs/bitlesson.md:49`, `docs/bitlesson.md:50`). Install docs require Codex CLI `0.114.0` or newer for native hooks (`docs/install-for-codex.md:34`).
- dependencies_and_callers: These docs coordinate with `config/default_config.json` by documenting matching defaults (`docs/usage.md:266`, `docs/usage.md:268`; `docs/bitlesson.md:9`). They coordinate with `scripts/install-skill.sh`, `scripts/install-skills-codex.sh`, `scripts/install-skills-kimi.sh`, `scripts/install-codex-hooks.sh`, and copied runtime bundle contents (`docs/install-for-codex.md:16`, `docs/install-for-codex.md:22`, `docs/install-for-codex.md:99`, `docs/install-for-kimi.md:23`, `docs/install-for-kimi.md:33`). `docs/images/rlcr-workflow.svg` is a static diagram of Plan -> Implement -> Review Summary -> Code Review -> Done with feedback loops (`docs/images/rlcr-workflow.svg:4`, `docs/images/rlcr-workflow.svg:13`, `docs/images/rlcr-workflow.svg:16`). `docs/images/monitor.png` is a static monitor screenshot asset.
- edge_cases_or_failure_modes: Documentation calls out unstable dev branch use (`docs/install-for-claude.md:31`, `docs/install-for-claude.md:45`), missing provider binaries falling back for BitLesson (`docs/bitlesson.md:19`), dangerous sandbox bypass mode (`docs/usage.md:331`, `docs/usage.md:333`, `docs/usage.md:337`), malformed/unsupported alternative language no-op behavior (`docs/usage.md:144`, `docs/usage.md:145`), and output directories needing writeability (`docs/install-for-kimi.md:259`, `docs/install-for-kimi.md:265`). `docs/images/monitor.png` and `docs/images/rlcr-workflow.svg` are passive documentation assets, not algorithmic executables.
- validation_or_tests: The docs are indirectly validated by tests that grep/document config and template expectations, including `tests/test-unified-codex-config.sh` for defaults/config migration and `tests/test-template-references.sh` for template references. I inspected recursively: `docs/bitlesson.md`, `docs/install-for-claude.md`, `docs/install-for-codex.md`, `docs/install-for-kimi.md`, `docs/usage.md`, `docs/images/monitor.png`, and `docs/images/rlcr-workflow.svg`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-035 `file` `config/default_config.json`
- cursor: `[_]`
- core_role: Lowest-priority default configuration layer for runtime routing, model defaults, BitLesson provider choice, agent teams default, alternate plan language, and gen-plan mode.
- algorithmic_behavior: Provides default keys: `codex_model: gpt-5.4`, `codex_effort: high`, `bitlesson_model: haiku`, `agent_teams: false`, `alternative_plan_language: ""`, and `gen_plan_mode: discussion` (`config/default_config.json:2` through `config/default_config.json:7`). These defaults are loaded by `scripts/lib/config-loader.sh`, which treats `$plugin_root/config/default_config.json` as required (`scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:109`) and overlays user/project configs via jq object merge (`scripts/lib/config-loader.sh:113`, `scripts/lib/config-loader.sh:128`).
- inputs_outputs_state: Input is static JSON. Output is the merged config JSON returned from `load_merged_config`, then queried by `get_config_value` (`scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:139`). Runtime state derived from these values includes hook defaults in `loop-common.sh`: `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, and `DEFAULT_AGENT_TEAMS` (`hooks/lib/loop-common.sh:191`, `hooks/lib/loop-common.sh:198`, `hooks/lib/loop-common.sh:209`, `hooks/lib/loop-common.sh:221`). `setup-rlcr-loop.sh` writes resolved model/effort/agent teams into state files (`scripts/setup-rlcr-loop.sh:880`, `scripts/setup-rlcr-loop.sh:881`, `scripts/setup-rlcr-loop.sh:893`).
- gates_or_invariants: The default config file is required; missing/malformed required default config is fatal in `_config_loader_prepare_layer` (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`). Optional user/project malformed configs are ignored with warnings (`scripts/lib/config-loader.sh:57`). `loop-common.sh` validates `codex_model` syntax and requires Codex model prefixes `gpt-` or `o[0-9]`, otherwise warns and falls back (`hooks/lib/loop-common.sh:199`, `hooks/lib/loop-common.sh:203`). `codex_effort` must be one of `xhigh|high|medium|low` (`hooks/lib/loop-common.sh:211`).
- dependencies_and_callers: Called by `scripts/lib/config-loader.sh`; consumed by `hooks/lib/loop-common.sh`, `scripts/bitlesson-select.sh`, install scripts, ask-codex/setup paths, and docs. `scripts/bitlesson-select.sh` reads `bitlesson_model`, `codex_model`, and optional `provider_mode` from the merged config (`scripts/bitlesson-select.sh:15`, `scripts/bitlesson-select.sh:16`, `scripts/bitlesson-select.sh:18`, `scripts/bitlesson-select.sh:20`). `scripts/lib/model-router.sh` maps `gpt-*`/`o[N]*` to Codex and `claude-*`/`haiku`/`sonnet`/`opus` to Claude (`scripts/lib/model-router.sh:18`, `scripts/lib/model-router.sh:23`).
- edge_cases_or_failure_modes: Nulls are stripped from overlay config layers before merging, so a project/user null does not erase a lower default (`scripts/lib/config-loader.sh:119`, `scripts/lib/config-loader.sh:121`). Missing `jq` is fatal for config loading (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:19`). Legacy reviewer keys are documented as ignored in favor of `codex_model`/`codex_effort` (`docs/usage.md:305`, `docs/usage.md:307`). Default `bitlesson_model: haiku` routes to Claude unless `provider_mode: codex-only` is seeded on Codex install, which docs and installer describe (`docs/bitlesson.md:16`, `docs/bitlesson.md:21`; `scripts/install-skill.sh:313`, `scripts/install-skill.sh:317`).
- validation_or_tests: `tests/test-unified-codex-config.sh` asserts `default_config.json` has `codex_model=gpt-5.4`, `codex_effort=high`, and no legacy `loop_reviewer_*` keys. `tests/test-config-merge.sh` covers default-only, user/project override, null stripping, and `HUMANIZE_CONFIG` override behavior. `tests/test-config-error-handling.sh` covers missing/malformed default config and optional layer handling. I inspected but did not execute tests in the read-only export.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-065 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR hook allowlist behavior across read/write/edit/bash validators. It defines what loop artifacts may be accessed directly and what must be routed through safer Task/Write/Edit interfaces.
- algorithmic_behavior: Sets up an isolated git repo and active loop dir with `state.md` at current round 5 (`tests/test-allowlist-validators.sh:33`, `tests/test-allowlist-validators.sh:49`, `tests/test-allowlist-validators.sh:53`). It directly tests `is_allowlisted_file()` from `hooks/lib/loop-common.sh` (`tests/test-allowlist-validators.sh:17`, `tests/test-allowlist-validators.sh:66`). Allowlisted direct paths are exactly `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, `round-1-summary.md`, plus the active current-round contract when validator-specific current-round checks permit it (`tests/test-allowlist-validators.sh:72`, `tests/test-allowlist-validators.sh:80`, `tests/test-allowlist-validators.sh:88`, `tests/test-allowlist-validators.sh:96`, `tests/test-allowlist-validators.sh:169`). Non-allowlisted stale/future todos, summaries, and contracts are expected to block (`tests/test-allowlist-validators.sh:104`, `tests/test-allowlist-validators.sh:112`, `tests/test-allowlist-validators.sh:120`).
- inputs_outputs_state: Inputs are synthetic hook JSON payloads for `Write`, `Edit`, `Read`, and `Bash` (`tests/test-allowlist-validators.sh:145`, `tests/test-allowlist-validators.sh:227`, `tests/test-allowlist-validators.sh:296`, `tests/test-allowlist-validators.sh:378`). Outputs are hook exit codes and stderr messages. Exit `0` means allowed; exit `2` means user-facing block. The test increments pass/fail counters and exits with `TESTS_FAILED` (`tests/test-allowlist-validators.sh:26`, `tests/test-allowlist-validators.sh:27`, `tests/test-allowlist-validators.sh:581`, `tests/test-allowlist-validators.sh:585`).
- gates_or_invariants: `is_allowlisted_file()` in `loop-common.sh` enforces both basename and active loop directory equality; it hardcodes the four allowlisted historical/current files (`hooks/lib/loop-common.sh:806`, `hooks/lib/loop-common.sh:811`). Todos access through Read/Write/Edit/Bash is blocked unless allowlisted (`hooks/loop-write-validator.sh:57`, `hooks/loop-read-validator.sh:56`, `hooks/loop-edit-validator.sh:40`, `hooks/loop-bash-validator.sh:543`). Bash is more restrictive: it allows only active-loop `round-[12]-todos.md` path writes, blocks contracts via Bash, blocks generic relative `round-1-todos.md`, wrong root, old loop dirs, and same-basename different roots (`tests/test-allowlist-validators.sh:402`, `tests/test-allowlist-validators.sh:415`, `tests/test-allowlist-validators.sh:441`, `tests/test-allowlist-validators.sh:454`, `tests/test-allowlist-validators.sh:469`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, `jq`, `git`, and the test helper conventions. It exercises `todos_blocked_message`, `finalize_contract_blocked_message`, `summary_bash_blocked_message`, and direct-execution block templates through validator output paths (`hooks/lib/loop-common.sh:826`, `hooks/lib/loop-common.sh:864`, `hooks/lib/loop-common.sh:879`, `hooks/lib/loop-common.sh:1418`).
- edge_cases_or_failure_modes: Wrong directory is blocked even with an allowlisted filename (`tests/test-allowlist-validators.sh:128`). Current-round contract is allowed via Write/Edit/Read but blocked via Bash because contract files must use structured tools (`tests/test-allowlist-validators.sh:169`, `tests/test-allowlist-validators.sh:251`, `tests/test-allowlist-validators.sh:320`, `tests/test-allowlist-validators.sh:402`). Direct manual stop-hook/stop-gate execution is blocked even under wrappers: bare `bash`, `zsh`, `env`, `command`, assignment prefix, `timeout`, `nice`, `nohup`, `strace`, and combinations (`tests/test-allowlist-validators.sh:507`, `tests/test-allowlist-validators.sh:517`, `tests/test-allowlist-validators.sh:527`, `tests/test-allowlist-validators.sh:542`, `tests/test-allowlist-validators.sh:552`, `tests/test-allowlist-validators.sh:567`). Implementation uses wrapper regexes in `loop-bash-validator.sh` (`hooks/loop-bash-validator.sh:166`, `hooks/loop-bash-validator.sh:179`, `hooks/loop-bash-validator.sh:181`).
- validation_or_tests: This file is itself the validation spec. It covers `is_allowlisted_file`, all three file validators, Bash path restrictions, and hook-wrapper blocking (`tests/test-allowlist-validators.sh:5` through `tests/test-allowlist-validators.sh:10`). I inspected it directly; not executed due read-only research requirement.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-095 `file` `tests/test-stop-hook-bg-allow.sh`
- cursor: `[_]`
- core_role: Branch-specific executable specification for “do not block stop when background running.” It verifies that the RLCR Stop hook exits naturally with a system message, does not run normal gates/Codex review, and leaves loop state intact while background Agent/Bash work is still pending.
- algorithmic_behavior: The test describes the target behavior in its header: pending background Agent or Bash work must make `loop-codex-stop-hook.sh` exit `0` with a user-facing `systemMessage`, without mutating on-disk state, so a later stop can resume normal review flow (`tests/test-stop-hook-bg-allow.sh:3`, `tests/test-stop-hook-bg-allow.sh:5`, `tests/test-stop-hook-bg-allow.sh:8`). It creates isolated repos that satisfy pre-Codex gates (`tests/test-stop-hook-bg-allow.sh:81`, `tests/test-stop-hook-bg-allow.sh:111`) and fake Codex to detect whether the hook reached review (`tests/test-stop-hook-bg-allow.sh:62`, `tests/test-stop-hook-bg-allow.sh:66`). Core implementation is `handle_bg_task_short_circuit` in `hooks/lib/loop-bg-tasks.sh`, called before phase/state/gate logic (`hooks/loop-codex-stop-hook.sh:71`, `hooks/loop-codex-stop-hook.sh:83`). That helper lists launched but incomplete background IDs from transcript events and exits early with JSON when pending IDs exist (`hooks/lib/loop-bg-tasks.sh:97`, `hooks/lib/loop-bg-tasks.sh:150`, `hooks/lib/loop-bg-tasks.sh:320`, `hooks/lib/loop-bg-tasks.sh:330`).
- inputs_outputs_state: Inputs are hook JSON fields `transcript_path` and optional `session_id` (`tests/test-stop-hook-bg-allow.sh:339`, `tests/test-stop-hook-bg-allow.sh:631`), plus transcript JSONL records representing async Agent launch (`toolUseResult.isAsync` + `agentId`) and background shell launch (`backgroundTaskId`) (`tests/test-stop-hook-bg-allow.sh:188`, `tests/test-stop-hook-bg-allow.sh:204`). Outputs are either no JSON/Codex marker for normal allow, Codex marker for normal review, or JSON with `systemMessage` and no `decision` for allow-with-message (`tests/test-stop-hook-bg-allow.sh:270`, `tests/test-stop-hook-bg-allow.sh:310`). State includes `state.md`/`finalize-state.md` hashes, `bg-pending.marker`, and stored `session_id` (`tests/test-stop-hook-bg-allow.sh:273`, `tests/test-stop-hook-bg-allow.sh:595`, `tests/test-stop-hook-bg-allow.sh:618`).
- gates_or_invariants: No background dispatch reaches Codex (`tests/test-stop-hook-bg-allow.sh:332`, `tests/test-stop-hook-bg-allow.sh:340`). Pending Agent and pending shell short-circuit with one background task in the message and state untouched (`tests/test-stop-hook-bg-allow.sh:344`, `tests/test-stop-hook-bg-allow.sh:359`). Multiple pending tasks count correctly (`tests/test-stop-hook-bg-allow.sh:390`, `tests/test-stop-hook-bg-allow.sh:408`). Missing/empty/no `transcript_path` fails closed into normal Codex flow, not false short-circuit (`tests/test-stop-hook-bg-allow.sh:412`, `tests/test-stop-hook-bg-allow.sh:420`, `tests/test-stop-hook-bg-allow.sh:427`). No active loop exits silently without Codex (`tests/test-stop-hook-bg-allow.sh:435`). Finalize phase still short-circuits before finalize gates (`tests/test-stop-hook-bg-allow.sh:455`). The `rlcr-stop-gate.sh` wrapper must forward transcript path and map JSON-with-systemMessage to `ALLOW` (`tests/test-stop-hook-bg-allow.sh:469`, `scripts/rlcr-stop-gate.sh:95`, `scripts/rlcr-stop-gate.sh:152`).
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, `hooks/lib/loop-bg-tasks.sh`, `hooks/lib/loop-common.sh`, `scripts/rlcr-stop-gate.sh`, `jq`, `git`, `sha256sum`, and `tests/test-helpers.sh`. `loop-common.sh` sources the background helper for all consumers (`hooks/lib/loop-common.sh:245`, `hooks/lib/loop-common.sh:1528`). `find_active_loop` has an opt-in marker fallback only for stop hook callers (`hooks/lib/loop-common.sh:312`, `hooks/lib/loop-common.sh:390`, `hooks/loop-codex-stop-hook.sh:64`).
- edge_cases_or_failure_modes: The test covers tilde transcript paths, direct helper tilde expansion, and wrapper tilde paths (`tests/test-stop-hook-bg-allow.sh:498`, `tests/test-stop-hook-bg-allow.sh:518`, `tests/test-stop-hook-bg-allow.sh:529`, `tests/test-stop-hook-bg-allow.sh:552`; implementation `expand_leading_tilde` at `hooks/lib/loop-bg-tasks.sh:23`). It covers cross-session parked loops: foreign session sees a “parked” system message and cannot mutate marker/state (`tests/test-stop-hook-bg-allow.sh:584`, `tests/test-stop-hook-bg-allow.sh:636`), while no-marker cross-session remains isolated (`tests/test-stop-hook-bg-allow.sh:648`). It verifies short-circuit writes `bg-pending.marker` (`tests/test-stop-hook-bg-allow.sh:696`), exact session match wins over newer foreign marker (`tests/test-stop-hook-bg-allow.sh:720`), same-session resume removes stale marker only after readable transcript proves no pending IDs (`tests/test-stop-hook-bg-allow.sh:771`, `hooks/lib/loop-bg-tasks.sh:356`), and foreign/ambiguous callers preserve marker/session (`tests/test-stop-hook-bg-allow.sh:823`, `tests/test-stop-hook-bg-allow.sh:1074`). Completion recognition handles both current SDK `system/task_notification` and legacy queue-operation XML (`tests/test-stop-hook-bg-allow.sh:879`, `tests/test-stop-hook-bg-allow.sh:904`; implementation `hooks/lib/loop-bg-tasks.sh:173`). Malformed/missing transcript preserves markers (fail closed) (`tests/test-stop-hook-bg-allow.sh:933`, `tests/test-stop-hook-bg-allow.sh:986`, `tests/test-stop-hook-bg-allow.sh:1133`). Pre-loop transcript launches are filtered using a loop-start timestamp derived from local loop dir names and converted to UTC, including UTC/JST/PST checks (`tests/test-stop-hook-bg-allow.sh:1183`, `tests/test-stop-hook-bg-allow.sh:1221`, `tests/test-stop-hook-bg-allow.sh:1235`, `tests/test-stop-hook-bg-allow.sh:1251`; implementation `hooks/lib/loop-bg-tasks.sh:48`, `hooks/lib/loop-bg-tasks.sh:75`).
- validation_or_tests: This file is the validation spec for AC-1 through AC-22b (`tests/test-stop-hook-bg-allow.sh:12` through `tests/test-stop-hook-bg-allow.sh:33`, plus later AC-21/AC-22 additions). It includes both helper-level and end-to-end hook/wrapper assertions. I inspected the full file and related implementations; not executed due read-only research requirement.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-125 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: User-facing block template for goal-tracker write/edit violations after Round 0. It encodes the contract that only the active tracker’s mutable section may be updated and Bash must not modify `goal-tracker.md`.
- algorithmic_behavior: Template renders a block headed `Goal Tracker Update Blocked (Round {{CURRENT_ROUND}})` (`prompt-template/block/goal-tracker-modification.md:1`). It instructs that after Round 0 only the `MUTABLE SECTION` of the active goal tracker may be updated (`prompt-template/block/goal-tracker-modification.md:3`) and points the user to `{{CORRECT_PATH}}` (`prompt-template/block/goal-tracker-modification.md:5`, `prompt-template/block/goal-tracker-modification.md:6`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `CORRECT_PATH`, supplied by `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:1464`, `hooks/lib/loop-common.sh:1478`). Output is rendered markdown to stderr by validators when blocking a tool call. No persistent state is directly modified by the template.
- gates_or_invariants: The rendered rules are the invariant: immutable section unchanged, no Bash modification, and no old-session tracker writes (`prompt-template/block/goal-tracker-modification.md:8` through `prompt-template/block/goal-tracker-modification.md:12`). Enforcement occurs in Write/Edit validators: path must normalize to active loop `goal-tracker.md`, and for rounds greater than zero the resulting content must pass `goal_tracker_mutable_update_allowed` (`hooks/loop-write-validator.sh:241`, `hooks/loop-write-validator.sh:246`, `hooks/loop-write-validator.sh:251`, `hooks/loop-write-validator.sh:257`; `hooks/loop-edit-validator.sh:192`, `hooks/loop-edit-validator.sh:197`, `hooks/loop-edit-validator.sh:202`, `hooks/loop-edit-validator.sh:221`).
- dependencies_and_callers: Called by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:1462` through `hooks/lib/loop-common.sh:1480`), which is used by `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh`. Bash-specific goal tracker blocks use separate message paths, but this template explicitly tells users not to use Bash (`prompt-template/block/goal-tracker-modification.md:11`).
- edge_cases_or_failure_modes: If a user needs Codex to correct tracker drift that cannot be safely resolved, the template instructs them to include an optional `Goal Tracker Update Request` in the summary rather than unsafe tracker mutation (`prompt-template/block/goal-tracker-modification.md:14`). If `load_and_render_safe` cannot load the template, `loop-common.sh` has an inline fallback with the same variables/rules (`hooks/lib/loop-common.sh:1467`).
- validation_or_tests: Goal-tracker validator behavior is covered elsewhere in hook tests; this assigned template itself is referenced through `goal_tracker_blocked_message`. `tests/robustness/test-goal-tracker-robustness.sh` is parser/monitor focused rather than this mutation gate. I inspected the template and caller paths directly.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-155 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Claude prompt template for Finalize Phase when code review was skipped. It defines a conservative finalization contract after incomplete validation.
- algorithmic_behavior: The template warns that review was skipped for `{{REVIEW_SKIP_REASON}}` and says implementation could not be fully validated (`prompt-template/claude/finalize-phase-skipped-prompt.md:1`, `prompt-template/claude/finalize-phase-skipped-prompt.md:3`, `prompt-template/claude/finalize-phase-skipped-prompt.md:5`). It asks Claude to manually verify changes, run available tests, and check common quality issues before finalizing (`prompt-template/claude/finalize-phase-skipped-prompt.md:7` through `prompt-template/claude/finalize-phase-skipped-prompt.md:13`). It optionally allows a code-simplifier agent, focused on diff from `{{BASE_BRANCH}}` to `{{START_BRANCH}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:15`, `prompt-template/claude/finalize-phase-skipped-prompt.md:17`, `prompt-template/claude/finalize-phase-skipped-prompt.md:19`).
- inputs_outputs_state: Inputs are template variables `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE` (`prompt-template/claude/finalize-phase-skipped-prompt.md:3`, `prompt-template/claude/finalize-phase-skipped-prompt.md:19`, `prompt-template/claude/finalize-phase-skipped-prompt.md:37`, `prompt-template/claude/finalize-phase-skipped-prompt.md:38`, `prompt-template/claude/finalize-phase-skipped-prompt.md:45`). Output is a prompt directing Claude’s finalization behavior and requiring a finalize summary at the given path.
- gates_or_invariants: Non-negotiable constraints: preserve existing functionality, keep existing tests passing, avoid new bugs, and only make functionality-equivalent cleanup changes (`prompt-template/claude/finalize-phase-skipped-prompt.md:26` through `prompt-template/claude/finalize-phase-skipped-prompt.md:33`). Before exit, `[mainline]` and `[blocking]` tasks must be completed, `[queued]` tasks may remain only as documented non-blocking follow-up, changes must be committed, and finalize summary must be written (`prompt-template/claude/finalize-phase-skipped-prompt.md:40` through `prompt-template/claude/finalize-phase-skipped-prompt.md:45`). Summary must include work done, files modified, tests pass confirmation if possible, and manual verification notes (`prompt-template/claude/finalize-phase-skipped-prompt.md:47` through `prompt-template/claude/finalize-phase-skipped-prompt.md:51`).
- dependencies_and_callers: Related to stop-hook finalize phase transitions and templates under `prompt-template/claude/finalize-phase-prompt.md`. Stop-hook tests reference finalize phase behavior through `finalize-state.md` and `finalize-summary.md` paths; for example background short-circuit still applies in finalize phase before normal finalize gates (`tests/test-stop-hook-bg-allow.sh:455`, `hooks/loop-codex-stop-hook.sh:86`, `hooks/loop-codex-stop-hook.sh:97`). This template likely loads through template-loader paths used by setup/stop hook finalize generation, although the assigned file itself is data-only.
- edge_cases_or_failure_modes: Main risk is skipped review: the template compensates by requiring manual verification and preserving existing functionality (`prompt-template/claude/finalize-phase-skipped-prompt.md:9`, `prompt-template/claude/finalize-phase-skipped-prompt.md:30`). It permits queued follow-ups but only if non-blocking and documented (`prompt-template/claude/finalize-phase-skipped-prompt.md:43`). It does not itself enforce those constraints; enforcement depends on prompt adherence plus stop/finalize validators.
- validation_or_tests: `tests/test-finalize-phase.sh` covers finalize phase behavior broadly, and `tests/test-stop-hook-bg-allow.sh` confirms pending background work short-circuits even in finalize phase. I inspected this template directly; not executed.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-185 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for production goal-tracker parsing used by the Humanize monitor/progress display. It validates parser behavior across normal, malformed, large, and mixed-schema trackers.
- algorithmic_behavior: Sources production `scripts/humanize.sh` and tests `humanize_parse_goal_tracker` plus `humanize_parse_goal_tracker_issue_counts` (`tests/robustness/test-goal-tracker-robustness.sh:21`, `tests/robustness/test-goal-tracker-robustness.sh:35`, `tests/robustness/test-goal-tracker-robustness.sh:477`). Parser output contract is `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`tests/robustness/test-goal-tracker-robustness.sh:36`; implementation `scripts/humanize.sh:69`, `scripts/humanize.sh:153`). Issue breakdown contract is `blocking_issues|queued_issues|open_issues` (`tests/robustness/test-goal-tracker-robustness.sh:53`; implementation `scripts/humanize.sh:36`, `scripts/humanize.sh:66`).
- inputs_outputs_state: Inputs are temporary `goal-tracker.md` variants written into `$TEST_DIR`: list-format ACs, table-format ACs, active task tables, completed/verified sections, deferred sections, issue sections, malformed files, binary-containing files, and long goals (`tests/robustness/test-goal-tracker-robustness.sh:72`, `tests/robustness/test-goal-tracker-robustness.sh:102`, `tests/robustness/test-goal-tracker-robustness.sh:131`, `tests/robustness/test-goal-tracker-robustness.sh:168`, `tests/robustness/test-goal-tracker-robustness.sh:294`, `tests/robustness/test-goal-tracker-robustness.sh:362`, `tests/robustness/test-goal-tracker-robustness.sh:563`). Outputs are pipe-delimited parse strings and pass/fail counts.
- gates_or_invariants: AC counting extracts unique `AC-?[0-9]+(\.[0-9]+)?` IDs from the Acceptance Criteria section (`scripts/humanize.sh:87`, `scripts/humanize.sh:91`). Active tasks are data rows under `#### Active Tasks` minus rows whose status column is `completed` or `deferred` (`scripts/humanize.sh:102`, `scripts/humanize.sh:109`, `scripts/humanize.sh:115`, `scripts/humanize.sh:121`). Completed tasks are table rows in `### Completed and Verified`; completed ACs are unique AC IDs in that section (`scripts/humanize.sh:125`, `scripts/humanize.sh:129`). Deferred tasks are rows in `### Explicitly Deferred` (`scripts/humanize.sh:137`). New issue schema separates `### Blocking Side Issues` and `### Queued Side Issues`; legacy `### Open Issues` falls back to blocking for safety (`scripts/humanize.sh:56`, `scripts/humanize.sh:57`, `scripts/humanize.sh:60`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` and `scripts/humanize.sh` (`tests/robustness/test-goal-tracker-robustness.sh:19`, `tests/robustness/test-goal-tracker-robustness.sh:22`). Runtime caller is the monitor display path in `scripts/humanize.sh`, which parses the active goal tracker and prints progress, AC counts, active/done/deferred tasks, and issue counts/colors (`scripts/humanize.sh:449`, `scripts/humanize.sh:451`, `scripts/humanize.sh:459`, `scripts/humanize.sh:607`, `scripts/humanize.sh:615`, `scripts/humanize.sh:617`).
- edge_cases_or_failure_modes: Nonexistent tracker returns `0|0|0|0|0|0|No goal tracker` (`tests/robustness/test-goal-tracker-robustness.sh:247`, `scripts/humanize.sh:73`). Empty and header-only files return zero counts (`tests/robustness/test-goal-tracker-robustness.sh:256`, `tests/robustness/test-goal-tracker-robustness.sh:500`). Malformed files without proper `###` sections count zero ACs gracefully (`tests/robustness/test-goal-tracker-robustness.sh:314`). Truncated files and binary content still return parseable AC counts rather than crashing (`tests/robustness/test-goal-tracker-robustness.sh:338`, `tests/robustness/test-goal-tracker-robustness.sh:359`). Large 60-AC trackers must not overflow (`tests/robustness/test-goal-tracker-robustness.sh:268`). Special characters in AC descriptions must not affect ID counting (`tests/robustness/test-goal-tracker-robustness.sh:291`). Mixed AC format accepts standard and bold formats, with the test expecting at least two matches (`tests/robustness/test-goal-tracker-robustness.sh:536`). Very long goal summaries are returned without parser truncation; truncation is display-layer behavior (`tests/robustness/test-goal-tracker-robustness.sh:560`, `scripts/humanize.sh:493`).
- validation_or_tests: This file is itself the robustness test for the parser/monitor contract. It has 18 numbered test groups and exits through `print_test_summary` (`tests/robustness/test-goal-tracker-robustness.sh:70`, `tests/robustness/test-goal-tracker-robustness.sh:589`). I inspected it and the production parser directly; not executed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-005`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-035`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-065`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-095`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-125`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-155`, `DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-185`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`