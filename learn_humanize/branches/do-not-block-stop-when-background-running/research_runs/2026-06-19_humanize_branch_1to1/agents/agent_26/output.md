# agent_26 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-026 `file` `agents/bitlesson-selector.md`
- cursor: `[_]`
- core_role: Prompt/policy specification for the BitLesson selector agent. It defines how an implementation sub-task is mapped to a minimal set of lessons from `.humanize/bitlesson.md`.
- algorithmic_behavior: The agent receives a sub-task, related paths, and BitLesson content, then performs relevance filtering. Its decision rules require direct relevance to scope/failure mode, prefer precision over recall, and return `NONE` when no lesson applies (`agents/bitlesson-selector.md:26-30`). It has a stable two-line output contract: `LESSON_IDS` plus one-sentence `RATIONALE` (`agents/bitlesson-selector.md:32-41`).
- inputs_outputs_state: Inputs are current sub-task description, related file paths, and configured BitLesson content (`agents/bitlesson-selector.md:12-18`). Output is deterministic plain text consumed by later Claude/Codex review stages (`agents/bitlesson-selector.md:19-24`). It does not mutate state directly; state impact occurs indirectly when selected IDs are followed and when round summaries record BitLesson Delta.
- gates_or_invariants: The main invariant is stable output shape with no extra sections (`agents/bitlesson-selector.md:34-41`). The selection gate is intentionally conservative: weakly related lessons must be excluded (`agents/bitlesson-selector.md:28-30`). This prevents stale or broad lessons from polluting task execution.
- dependencies_and_callers: The file documents runtime dispatch through `scripts/bitlesson-select.sh`, which routes by configured `bitlesson_model` to Codex CLI for `gpt-*` models or Claude CLI for `haiku`/`sonnet`/`opus` models (`agents/bitlesson-selector.md:21-22`). The setup script injects BitLesson selection into round-0 prompts and requires running `bitlesson-selector` before each task/sub-task (`scripts/setup-rlcr-loop.sh:1350-1364`). Next-round prompt generation also references this discipline through hook-generated prompts (`hooks/loop-codex-stop-hook.sh` references found by search).
- edge_cases_or_failure_modes: If no lesson is directly relevant, the only valid result is `NONE`, not a broad catch-all (`agents/bitlesson-selector.md:28-30`). The prompt itself is deterministic, but runtime validation lives in `scripts/bitlesson-select.sh`: empty BitLesson files fail, files with no recorded lessons return `LESSON_IDS: NONE`, selector timeouts exit 124, and unexpected output format exits 1 (`scripts/bitlesson-select.sh:89-104`, `scripts/bitlesson-select.sh:181-230`, `scripts/bitlesson-select.sh:237-262`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` covers provider routing and output behavior for the runtime script. The setup script initializes `.humanize/bitlesson.md` via `scripts/bitlesson-init.sh` (`scripts/setup-rlcr-loop.sh:856-862`), and `scripts/bitlesson-init.sh` validates project/template paths, refuses absolute/`..` relpaths, and creates the file only if missing (`scripts/bitlesson-init.sh:70-88`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-056 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Main bootstrap/runtime setup script for the RLCR loop. It validates invocation, repository/plan state, loop exclusivity, model and branch configuration, then creates the active `.humanize/rlcr/<timestamp>` loop state, initial prompt, round contract, goal tracker, BitLesson file, summary scaffold, and user-facing activation output.
- algorithmic_behavior: The script starts with strict Bash mode and shared libraries (`scripts/setup-rlcr-loop.sh:11-33`). It parses options such as `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--agent-teams`, BitLesson behavior flags, and `--privacy` (`scripts/setup-rlcr-loop.sh:188-320`). It then enforces dependency presence, active-loop mutual exclusion, optional Agent Teams environment readiness, plan-file normalization, Git repository validity, plan path/content/tracking gates, clean working tree, base branch discovery, state directory creation, and prompt generation.
- inputs_outputs_state: Primary inputs are a relative plan file or `--skip-impl`, command flags, environment variables including `CLAUDE_PROJECT_DIR` and `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, the Git repo state, merged Codex defaults from `hooks/lib/loop-common.sh`, and templates under the plugin tree. Outputs are `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, `round-0-prompt.md`, optional `.review-phase-started`, `.humanize/.pending-session-id`, and terminal activation text (`scripts/setup-rlcr-loop.sh:817-919`, `scripts/setup-rlcr-loop.sh:925-1152`, `scripts/setup-rlcr-loop.sh:1191-1437`, `scripts/setup-rlcr-loop.sh:1443-1537`). State transitions include `review_started: false` in normal mode and `review_started: true` in skip-impl mode (`scripts/setup-rlcr-loop.sh:868-890`).
- gates_or_invariants: It requires `codex`, `jq`, and `git` before setup proceeds (`scripts/setup-rlcr-loop.sh:336-359`). Only one active RLCR loop may exist (`scripts/setup-rlcr-loop.sh:365-375`). Plan files must be relative, have no spaces or shell metacharacters, not be symlinks, stay inside the project, not live under submodules, and satisfy tracking rules (`scripts/setup-rlcr-loop.sh:451-603`). Content gates require at least five lines and at least three meaningful content lines excluding blanks/comments (`scripts/setup-rlcr-loop.sh:614-667`). YAML safety gates reject unsafe branch/model characters and invalid Codex effort (`scripts/setup-rlcr-loop.sh:688-712`, `scripts/setup-rlcr-loop.sh:795-800`). The working tree must be clean except ignored `.humanize` runtime paths (`scripts/setup-rlcr-loop.sh:719-737`).
- dependencies_and_callers: Called by the `/humanize:start-rlcr-loop` command (`commands/start-rlcr-loop.md:109-115`) and as a fallback from `gen-plan` auto-start (`commands/gen-plan.md:610-614`). It sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:25-33`), uses `find_active_loop` from the common library, invokes `scripts/bitlesson-init.sh` (`scripts/setup-rlcr-loop.sh:856-862`), and consumes Agent Teams templates when enabled (`scripts/setup-rlcr-loop.sh:1366-1387`). The post-Bash hook consumes `.humanize/.pending-session-id`; tests assert its signature behavior (`tests/test-session-id.sh` references found by search).
- edge_cases_or_failure_modes: Missing dependencies produce a consolidated error list (`scripts/setup-rlcr-loop.sh:336-359`). `--skip-impl` without a plan creates an internal placeholder and skips plan validation, while skip-impl with a plan keeps the plan as an anchor (`scripts/setup-rlcr-loop.sh:404-423`, `scripts/setup-rlcr-loop.sh:825-845`, `scripts/setup-rlcr-loop.sh:927-1053`). Git operations are wrapped with timeouts and fail closed on timeout (`scripts/setup-rlcr-loop.sh:558-575`, `scripts/setup-rlcr-loop.sh:719-724`). Base branch auto-detection can fail if no local main/master/default branch exists, or if a remote default exists but lacks a local ref (`scripts/setup-rlcr-loop.sh:744-793`). Output phase traps SIGPIPE only after setup work is complete to avoid marking internal pipeline failures as success (`scripts/setup-rlcr-loop.sh:1443-1445`).
- validation_or_tests: Plan path/content/security behavior is covered by `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, and `tests/robustness/test-path-validation-robustness.sh`. Base-branch behavior is covered by `tests/robustness/test-base-branch-detection.sh`; setup edge cases by `tests/robustness/test-setup-scripts-robustness.sh`; Agent Teams by `tests/test-agent-teams.sh`; session ID signaling by `tests/test-session-id.sh`; Codex config integration by `tests/test-unified-codex-config.sh`. `tests/run-all-tests.sh` includes related suites.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-086 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: Thin executable specification for monitor SIGINT behavior. It runs only the SIGINT subset of the broader real monitor e2e tests.
- algorithmic_behavior: The script enables strict mode, sources `tests/test-monitor-e2e-real.sh`, prints a SIGINT-suite heading, executes `monitor_test_bash_sigint` and `monitor_test_zsh_sigint`, then exits 0 only when `TESTS_FAILED` is zero (`tests/test-monitor-e2e-sigint.sh:1-21`).
- inputs_outputs_state: Inputs are the sourced real monitor test definitions and the environment’s shell/tool availability, especially `zsh` for the zsh path. Output is pass/fail terminal text plus process exit code. State is held in sourced global counters `TESTS_PASSED` and `TESTS_FAILED` (`tests/test-monitor-e2e-sigint.sh:19-21`).
- gates_or_invariants: The hard gate is `[[ $TESTS_FAILED -eq 0 ]] && exit 0 || exit 1` (`tests/test-monitor-e2e-sigint.sh:21`). The sourced bash SIGINT test creates a synthetic active loop, starts `_humanize_monitor_codex`, sends SIGINT to the monitor process group where possible, waits with a bounded loop, escalates to TERM/KILL only after timeout, and accepts cleanup text or clean exit code while rejecting glob errors (`tests/test-monitor-e2e-real.sh:377-550`). The zsh SIGINT test skips if zsh is unavailable, sends SIGINT to the zsh monitor process, requires exit/cleanup indication, and rejects zsh glob errors (`tests/test-monitor-e2e-real.sh:555-685`).
- dependencies_and_callers: Depends directly on `tests/test-monitor-e2e-real.sh` (`tests/test-monitor-e2e-sigint.sh:4-5`). That file sources `scripts/humanize.sh` inside generated runner scripts and calls the real `_humanize_monitor_codex` function (`tests/test-monitor-e2e-real.sh:464-468`, `tests/test-monitor-e2e-real.sh:632-635`). The global test runner includes this script in the monitor group (`tests/run-all-tests.sh:79-83`).
- edge_cases_or_failure_modes: The bash test treats SIGINT process-group delivery as complex; if SIGINT does not stop the monitor but TERM does, it records a pass for handled termination rather than failing the whole suite (`tests/test-monitor-e2e-real.sh:492-519`). The zsh test is stricter: if the monitor remains after SIGINT and must be killed, it fails (`tests/test-monitor-e2e-real.sh:650-667`). Both tests explicitly fail on shell glob errors such as `no matches found` or `bad pattern` (`tests/test-monitor-e2e-real.sh:544-549`, `tests/test-monitor-e2e-real.sh:680-684`).
- validation_or_tests: This file is itself validation. It is a focused split of `test-monitor-e2e-real.sh`, whose direct execution runs deletion and SIGINT coverage for bash and zsh and reports verified monitor properties (`tests/test-monitor-e2e-real.sh:691-719`).
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-116 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Guard message template used when an agent attempts to modify `finalize-state.md`. It is a policy block for Finalize Phase state ownership.
- algorithmic_behavior: The template informs the agent that `finalize-state.md` is loop-managed and redirects work to the allowed Finalize Phase actions: run code simplifier, commit changes, and write `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1-8`).
- inputs_outputs_state: It has no dynamic placeholders. Input is the validator’s decision that a finalize-state modification was attempted. Output is a rendered Markdown block emitted to stderr by the hook. It preserves loop state by preventing direct edits to `finalize-state.md`.
- gates_or_invariants: The invariant is that `finalize-state.md` is owned by the loop system during Finalize Phase (`prompt-template/block/finalize-state-file-modification.md:3`). The permitted user/agent state output is `finalize-summary.md`, not direct state-file mutation (`prompt-template/block/finalize-state-file-modification.md:5-8`).
- dependencies_and_callers: Loaded through `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh`, which uses `load_and_render_safe` with this template path and a fallback message (`hooks/lib/loop-common.sh:855-862`). It is used by write/edit validators that share common block-message functions.
- edge_cases_or_failure_modes: If the template is unavailable or cannot render, `load_and_render_safe` has a fallback defined in the common library (`hooks/lib/loop-common.sh:857-861`). Because the template has no placeholders, rendering risk is low; the main failure mode is missing template or validator misclassification.
- validation_or_tests: Finalize-phase hook behavior is covered by `tests/test-finalize-phase.sh` as part of the suite (`tests/run-all-tests.sh:74-76`). The common-library fallback provides defensive behavior if template loading fails.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-146 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Guard message template for read attempts targeting the wrong loop file location. It enforces that current-round loop files are read from the active loop directory.
- algorithmic_behavior: The template renders the attempted `{{FILE_PATH}}`, points to `{{ACTIVE_LOOP_DIR}}/`, lists the current round prompt and summary, and says to use `cat {{FILE_PATH}}` if that exact file is needed (`prompt-template/block/wrong-file-location.md:1-9`). It is designed as a user-facing correction from validators.
- inputs_outputs_state: Inputs are template variables `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`; `CORRECT_PATH` may also be passed by callers but is not consumed by this template. Output is Markdown guidance. It does not mutate state; it blocks unsafe or stale file access by causing validator exit 2.
- gates_or_invariants: The invariant is that loop files are scoped to the active loop directory and current round (`prompt-template/block/wrong-file-location.md:3-7`). `hooks/loop-read-validator.sh` invokes this block when a goal tracker path under a loop directory is not the active goal tracker, or when a round prompt/summary/contract read occurs outside the active loop dir (`hooks/loop-read-validator.sh:226-240`, `hooks/loop-read-validator.sh:269-278`).
- dependencies_and_callers: Rendered through `load_and_render_safe` from `hooks/loop-read-validator.sh` (`hooks/loop-read-validator.sh:235-239`, `hooks/loop-read-validator.sh:274-277`). The validator first parses active state strictly and derives `CURRENT_ROUND` before location validation (`hooks/loop-read-validator.sh:210-216`).
- edge_cases_or_failure_modes: If the state file is malformed, the read validator fails closed before rendering the wrong-location message (`hooks/loop-read-validator.sh:210-214`). If a file is historical or allowlisted, later validator logic may permit it; otherwise wrong-location reads are blocked. The final template line can be confusing because it says `cat {{FILE_PATH}}` even though the validator just blocked reading that path, but algorithmically it still prevents silent access to stale loop artifacts.
- validation_or_tests: Covered by read-validator and allowlist test suites, including `tests/test-allowlist-validators.sh` in the aggregate runner (`tests/run-all-tests.sh:74`). The behavior is also exercised indirectly by finalize and monitor/runtime suites that depend on active loop directory correctness.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-176 `file` `skills/ask-gemini/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction contract for consulting Gemini CLI as an independent, web-research-backed expert. It defines safe invocation, flag handling, output interpretation, and exit-code semantics for `/humanize:ask-gemini`.
- algorithmic_behavior: The skill requires using `${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh` with the question quoted as a single shell argument when no flags are supplied (`skills/ask-gemini/SKILL.md:15-23`). If flags such as `--gemini-model` or `--gemini-timeout` are supplied, it requires reconstructing arguments so flags remain separate and free-form text is one quoted final argument (`skills/ask-gemini/SKILL.md:25-31`). It explicitly forbids unquoted `$ARGUMENTS` expansion because shell reparsing can break before the script starts (`skills/ask-gemini/SKILL.md:33-39`).
- inputs_outputs_state: Inputs are optional model/timeout flags plus a question or task. Output from the script is Gemini’s response on stdout and status information on stderr (`skills/ask-gemini/SKILL.md:41-46`). Persistent state is a saved response under `.humanize/skill/<timestamp>/output.md` (`skills/ask-gemini/SKILL.md:57-60`). The backing script also stores input, output, metadata, and cache logs (`scripts/ask-gemini.sh:16-18`, `scripts/ask-gemini.sh:181-215`, `scripts/ask-gemini.sh:361-385`).
- gates_or_invariants: The key invariant is shell-safe quoting for arbitrary user text containing spaces or metacharacters (`skills/ask-gemini/SKILL.md:17-39`). Exit-code interpretation is part of the contract: 0 success, 1 validation error, 124 timeout, other values as Gemini process errors (`skills/ask-gemini/SKILL.md:48-55`). The backing script validates `gemini` availability, non-empty question, numeric timeout, and model-name safety (`scripts/ask-gemini.sh:102-165`).
- dependencies_and_callers: The skill’s allowed tool is the Bash invocation of `${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh` (`skills/ask-gemini/SKILL.md:5`). The script depends on `scripts/portable-timeout.sh` (`scripts/ask-gemini.sh:27-31`) and the external `gemini` CLI (`scripts/ask-gemini.sh:142-148`). User-facing monitor support references ask-gemini skill invocations through `scripts/humanize.sh` and `scripts/lib/monitor-skill.sh` search results.
- edge_cases_or_failure_modes: Unknown or missing flags fail validation in the script (`scripts/ask-gemini.sh:102-126`). Missing Gemini CLI exits 1 with installation guidance (`scripts/ask-gemini.sh:142-147`). Timeout exits 124 and records timeout metadata (`scripts/ask-gemini.sh:291-310`). Non-zero Gemini exit records error metadata and includes stderr tail (`scripts/ask-gemini.sh:313-334`). Empty stdout exits 1 with `empty_response` metadata (`scripts/ask-gemini.sh:337-358`). If home cache is unwritable, the script falls back to project-local cache (`scripts/ask-gemini.sh:188-196`).
- validation_or_tests: The skill itself defines the operator contract. Runtime validation is implemented in `scripts/ask-gemini.sh`; monitor tooling can inspect `.humanize/skill` outputs. I did not find a dedicated `test-ask-gemini.sh` in the searched references, so residual risk is mainly around CLI version drift and argument reconstruction by callers.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6/6 item evidence sections present; assigned IDs are used once as section headers`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`