# agent_10 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-010 `directory` `templates`
- cursor: `[_]`
- core_role: Seed-template directory for non-prompt runtime artifacts. Recursive inspection found one child, `templates/bitlesson.md`, which is the canonical initial BitLesson knowledge-base scaffold copied into project state as `.humanize/bitlesson.md`.
- algorithmic_behavior: `templates/bitlesson.md:5-19` defines a strict entry schema and field order for reusable lessons: lesson heading, `Lesson ID`, scope, problem, root cause, solution, constraints, validation evidence, and source rounds. `templates/bitlesson.md:21-23` leaves the KB empty except for the insertion marker. Setup wires this template through `scripts/setup-rlcr-loop.sh:856-862`, which passes it to `scripts/bitlesson-init.sh`; the init script creates the target from the template only when missing at `scripts/bitlesson-init.sh:83-86`.
- inputs_outputs_state: Inputs are project root, template path, and relative BitLesson path. Output is a project-local `.humanize/bitlesson.md` file initialized from the template. State is initialized but not advanced by the template itself; later rounds mutate the project-local KB and summaries reference those lesson IDs.
- gates_or_invariants: The template establishes structural invariants used by later validation: concrete lesson IDs must not remain as `<BL-YYYYMMDD-short-name>` placeholders, and the expected concrete format is `BL-YYYYMMDD-short-name`, matching the validator’s ID check in `scripts/bitlesson-validate-delta.sh:322-336`. `scripts/bitlesson-init.sh:70-72` rejects absolute or `..` relative BitLesson paths, and `scripts/bitlesson-init.sh:78-80` rejects non-regular existing targets.
- dependencies_and_callers: Called by RLCR setup via `scripts/setup-rlcr-loop.sh:858-862`; implemented by `scripts/bitlesson-init.sh:1-88`; validated later by `scripts/bitlesson-validate-delta.sh`, which counts concrete entries at `scripts/bitlesson-validate-delta.sh:227-240`.
- edge_cases_or_failure_modes: If the target exists, `bitlesson-init.sh` preserves it and does not overwrite user/project lessons. If the template path is missing, init fails at `scripts/bitlesson-init.sh:65-68`. An empty KB can become a gate depending on `--allow-empty-none`; when false, `Action: none` blocks if no concrete lesson exists at `scripts/bitlesson-validate-delta.sh:254-266`.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` creates a temporary `.humanize/bitlesson.md` with a concrete `Lesson ID` at `tests/test-bitlesson-validate-delta.sh:21-34` and verifies summary-to-KB validation. `tests/test-templates-comprehensive.sh` does not inspect `templates/`; it validates `prompt-template/`, so this directory’s direct coverage is primarily through setup/init and BitLesson delta tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-040 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: Claude PreToolUse Bash guard for the RLCR state machine. It prevents Bash commands from bypassing Write/Edit validators, protects loop-owned files, blocks unsafe state transitions, and keeps unrelated sessions from being restricted by stale active-loop state.
- algorithmic_behavior: The hook reads hook JSON from stdin at `hooks/loop-bash-validator.sh:22`, validates structure through `validate_hook_input` at `hooks/loop-bash-validator.sh:24-27`, rejects deep JSON at `hooks/loop-bash-validator.sh:29-32`, and exits immediately for non-Bash tools at `hooks/loop-bash-validator.sh:36-38`. For Bash, it requires `tool_input.command` at `hooks/loop-bash-validator.sh:40-45`, lowercases it, derives `PROJECT_ROOT`, extracts `session_id`, and finds a session-matched active loop at `hooks/loop-bash-validator.sh:52-60`. With no active loop it allows all Bash commands at `hooks/loop-bash-validator.sh:155-158`.
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr` state files, and command text. Outputs are exit `0` allow, exit `1` malformed/unsafe hook input or state parse failure, and exit `2` block with human-facing message on stderr. The hook does not mutate loop state; it enforces state-file ownership and command routing.
- gates_or_invariants: Methodology analysis phase is the strictest gate: when `methodology-analysis-state.md` exists, only a leading unchained `cancel-rlcr-loop.sh` is allowed, while git write commands, file manipulation, interpreters, shell scripts, build tools, sourcing, direct script execution, and file redirection are blocked at `hooks/loop-bash-validator.sh:81-153`. During active RLCR it blocks direct execution of `loop-codex-stop-hook.sh` and `rlcr-stop-gate.sh`, including wrappers like `env`, `command`, `timeout`, `nice`, `nohup`, `strace`, and `ltrace`, via `hooks/loop-bash-validator.sh:166-183`. It blocks `git push` when `push_every_round` is not true at `hooks/loop-bash-validator.sh:210-219`, blocks `git add` commands that would stage `.humanize` at `hooks/loop-bash-validator.sh:229-232`, and protects `methodology-analysis-state.md`, `finalize-state.md`, and `state.md` from destination or source-side edits at `hooks/loop-bash-validator.sh:253-279` and `hooks/loop-bash-validator.sh:292-470`. It also blocks plan backup, goal tracker, prompt, summary, round contract, and most todos writes at `hooks/loop-bash-validator.sh:478-550`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-bash-validator.sh:15-17`. Key delegated behavior includes JSON validation in `hooks/lib/loop-common.sh:91-128`, required field checks in `hooks/lib/loop-common.sh:133-146`, active-loop lookup in `hooks/lib/loop-common.sh:319-412`, strict state parsing in `hooks/lib/loop-common.sh:520-587`, `.humanize` git-add detection in `hooks/lib/loop-common.sh:1230-1326`, protected-file command detection in `hooks/lib/loop-common.sh:1435-1460`, and cancel authorization in `hooks/lib/loop-common.sh:1038-1075`. Template messages are loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: The hook intentionally uses session-matched active-loop detection; it calls `find_active_loop "$LOOP_BASE_DIR" "$HOOK_SESSION_ID"` without marker fallback, so validators do not adopt another session’s `bg-pending.marker`. Malformed state blocks fail closed at `hooks/loop-bash-validator.sh:195-199`. The state-file source detection includes careful shell segment splitting and redirection stripping at `hooks/loop-bash-validator.sh:292-438`, covering chained commands and leading redirections. Authorized cancellation is the only state-file rename exception and requires `.cancel-requested` plus a constrained command shape.
- validation_or_tests: `tests/robustness/test-session-robustness.sh` validates the `find_active_loop` behavior this hook depends on. `tests/test-templates-comprehensive.sh` validates the template loading/rendering path used for block messages. Direct hook-script bypass patterns are referenced in broader validator tests such as `tests/test-allowlist-validators.sh` per repository search results, but this assigned file itself is the implementation under inspection.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-070 `file` `tests/test-bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Executable regression specification for `scripts/bitlesson-validate-delta.sh`, focused on the `## BitLesson Delta` summary contract and Notes-field enforcement.
- algorithmic_behavior: The test creates a temporary BitLesson KB with a concrete lesson ID via `make_bitlesson_file` at `tests/test-bitlesson-validate-delta.sh:21-34`, creates round summaries via `make_summary_file` at `tests/test-bitlesson-validate-delta.sh:36-49`, and invokes the validator with fixed arguments at `tests/test-bitlesson-validate-delta.sh:51-62`. It asserts blocking by parsing JSON `.decision == "block"` with `jq` at `tests/test-bitlesson-validate-delta.sh:64-73`, asserts Notes-specific block messages at `tests/test-bitlesson-validate-delta.sh:75-84`, and treats empty stdout as pass at `tests/test-bitlesson-validate-delta.sh:86-95`.
- inputs_outputs_state: Inputs are generated markdown summaries, generated `.humanize/bitlesson.md`, `prompt-template`, current round `1`, and `--allow-empty-none false`. Outputs are test pass/fail counters and validator stdout. Temporary filesystem state is created under `TEST_DIR` by `tests/test-helpers.sh:86-89`.
- gates_or_invariants: `Action: add` and `Action: update` require a non-empty, non-placeholder `Notes:` field. The test covers whitespace-only Notes at `tests/test-bitlesson-validate-delta.sh:101-104`, square-bracket placeholder Notes at `tests/test-bitlesson-validate-delta.sh:106-109`, and angle-bracket placeholder Notes at `tests/test-bitlesson-validate-delta.sh:111-114`. It also verifies that a BitLesson Delta section inside a fenced code block is ignored and therefore blocks as missing at `tests/test-bitlesson-validate-delta.sh:116-128`, and that a section inside an HTML comment is ignored at `tests/test-bitlesson-validate-delta.sh:130-142`.
- dependencies_and_callers: Depends on `scripts/bitlesson-validate-delta.sh`, `tests/test-helpers.sh`, `jq`, Bash, and the `prompt-template` block messages. The validator’s relevant implementation parses outside fences/comments at `scripts/bitlesson-validate-delta.sh:92-179`, validates `Action` at `scripts/bitlesson-validate-delta.sh:202-218`, validates Notes at `scripts/bitlesson-validate-delta.sh:281-297`, and validates referenced lesson IDs at `scripts/bitlesson-validate-delta.sh:312-384`.
- edge_cases_or_failure_modes: The test is deliberately scoped to false positives around quoted template text: fenced and commented examples must not count as a real summary delta. It also ensures normal text still counts at `tests/test-bitlesson-validate-delta.sh:149-152`, preventing the fence/comment fix from over-filtering valid summaries.
- validation_or_tests: This file is itself the validation. It ends with `print_test_summary "BitLesson Delta Validator Test Summary"` at `tests/test-bitlesson-validate-delta.sh:154`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-100 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Comprehensive executable spec for prompt-template directory integrity and `hooks/lib/template-loader.sh` rendering behavior, ensuring block/prompt contracts are loadable and safe under edge inputs.
- algorithmic_behavior: The test resolves `TEMPLATE_DIR` using `get_template_dir` at `tests/test-templates-comprehensive.sh:59`, checks the directory and core subdirectories at `tests/test-templates-comprehensive.sh:64-80`, then loads every `*.md` template under `prompt-template` with `find ... -print0` at `tests/test-templates-comprehensive.sh:90-107`. It validates placeholder syntax for extra braces, wrong bracket types, unclosed placeholders, spaces, and lowercase placeholders at `tests/test-templates-comprehensive.sh:118-194`. It then exercises rendering edge cases, fallback behavior, real templates, stress loops, and all-template rendering.
- inputs_outputs_state: Inputs are `prompt-template/**/*.md`, `hooks/lib/template-loader.sh`, generated temporary malformed templates, and dummy render variables. Outputs are colored pass/fail/warn counts and process exit `0` only when `TESTS_FAILED == 0` at `tests/test-templates-comprehensive.sh:619-625`. Temporary state is created with `mktemp -d` at `tests/test-templates-comprehensive.sh:205-207` and `tests/test-templates-comprehensive.sh:484-490`.
- gates_or_invariants: The placeholder grammar is uppercase `{{VAR_NAME}}` with no interior spaces or lowercase names, asserted at `tests/test-templates-comprehensive.sh:122-175`. Rendering must preserve empty lines, support special regex characters, quotes, long values, multiline values without crashing, variable positions, repeated variables, and adjacent variables at `tests/test-templates-comprehensive.sh:266-439`. Missing templates must use fallback via `load_and_render_safe` at `tests/test-templates-comprehensive.sh:445-453`; existing templates must override fallback at `tests/test-templates-comprehensive.sh:455-463`; invalid/incomplete template dirs must be rejected at `tests/test-templates-comprehensive.sh:466-490`.
- dependencies_and_callers: Sources `hooks/lib/template-loader.sh` at `tests/test-templates-comprehensive.sh:17`. That loader defines `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` in `hooks/lib/template-loader.sh:26-238`. The underlying renderer is single-pass awk substitution, so inserted values containing `{{...}}` are not re-expanded, as documented and implemented at `hooks/lib/template-loader.sh:48-136`.
- edge_cases_or_failure_modes: Syntax checks intentionally skip noisy single-brace placeholder checks at `tests/test-templates-comprehensive.sh:160-164`. The CJK section uses safe ASCII stand-ins rather than actual CJK characters at `tests/test-templates-comprehensive.sh:348-360`, so it is more of a no-crash encoding smoke test than full Unicode semantic coverage. Missing placeholder variables remain unreplaced by design; the final all-template loop warns about unreplaced placeholders rather than failing at `tests/test-templates-comprehensive.sh:590-597`.
- validation_or_tests: This file is a CI-oriented validation harness. It tests real templates including `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at `tests/test-templates-comprehensive.sh:497-540`, then renders every template with dummy placeholder assignments at `tests/test-templates-comprehensive.sh:562-606`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-130 `file` `prompt-template/block/mainline-verdict-missing.md`
- cursor: `[_]`
- core_role: Block-message template for the stop hook when Codex implementation review output omits the mandatory mainline progress verdict. This is a drift-state safety gate.
- algorithmic_behavior: The template states the required line exactly as `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` at `prompt-template/block/mainline-verdict-missing.md:3-5`, explains that Humanize cannot safely update drift state or select the next-round prompt without it at `prompt-template/block/mainline-verdict-missing.md:7-9`, and includes file placeholders for review result and review prompt at `prompt-template/block/mainline-verdict-missing.md:11-13`.
- inputs_outputs_state: Inputs are `{{REVIEW_RESULT_FILE}}` and `{{REVIEW_PROMPT_FILE}}`. Output is a rendered block reason embedded in stop-hook JSON. State transition is negative: the RLCR loop does not advance because the implementation review cannot be classified as advanced, stalled, or regressed.
- gates_or_invariants: The invariant is that every implementation review must include one parseable mainline verdict. The stop hook’s `block_missing_mainline_verdict` function loads this template at `hooks/loop-codex-stop-hook.sh:1421-1443`, emits JSON with `"decision": "block"` and system message `"Loop: Blocked - implementation review missing Mainline Progress Verdict"` at `hooks/loop-codex-stop-hook.sh:1445-1452`, and exits without proceeding at `hooks/loop-codex-stop-hook.sh:1453`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe`, with an inline fallback that mirrors this file at `hooks/loop-codex-stop-hook.sh:1427-1439`. The verdict vocabulary is backed by constants in `hooks/lib/loop-common.sh:46-48` and normalized by `normalize_mainline_progress_verdict` at `hooks/lib/loop-common.sh:590-603`.
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook uses its inline fallback, so the block still happens. If placeholders are not supplied, template-loader semantics preserve unresolved `{{...}}`, but the current caller supplies both paths. The parsing side requires an actual verdict token; a review that discusses verdicts but omits the exact line still blocks.
- validation_or_tests: `tests/test-templates-comprehensive.sh` validates that all prompt templates load and render. Repository references show `tests/test-finalize-phase.sh` includes a negative case for “Missing Mainline Progress Verdict blocks exit” around lines 948-979.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-160 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Next-round prompt injection notice that turns Codex “Open Question” findings into a user-clarification gate before Claude proceeds with other remediation.
- algorithmic_behavior: The file is a single-line notice requiring `AskUserQuestion` before resolving any other Codex findings. It has no placeholders and is intended to be inserted verbatim into the next Claude prompt.
- inputs_outputs_state: Input is only the presence of an Open Question in `REVIEW_RESULT_FILE` and `ASK_CODEX_QUESTION=true`; the template itself takes no variables. Output is additional prompt text in `NEXT_PROMPT_FILE`. The state transition is behavioral: the next round must clarify user-facing open questions before continuing implementation work.
- gates_or_invariants: The stop hook scans review result lines only when `ASK_CODEX_QUESTION` is true at `hooks/loop-codex-stop-hook.sh:2078-2080`. Detection requires a line shorter than 40 characters containing `Open Question` at `hooks/loop-codex-stop-hook.sh:2081-2087`. On detection, it loads this template at `hooks/loop-codex-stop-hook.sh:2089-2093` and inserts it after the Codex review result end marker and following `---` separator at `hooks/loop-codex-stop-hook.sh:2095-2110`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` using `load_template`. If missing, the hook falls back to the same notice text inline at `hooks/loop-codex-stop-hook.sh:2091-2094`. The injected instruction depends on the availability of the Claude `AskUserQuestion` tool in the interactive environment.
- edge_cases_or_failure_modes: Detection is intentionally narrow: a long line containing “Open Question” or a differently worded question heading will not trigger injection. If `ASK_CODEX_QUESTION` is false, no notice is inserted. Because the template has no variables, template syntax risk is minimal.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads all templates and validates syntax, so this file is covered as a no-placeholder template. The stop-hook path is visible in `hooks/loop-codex-stop-hook.sh:2078-2110`.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-190 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for active RLCR session discovery, especially zombie-loop prevention and concurrent-session-safe loop selection used by validators and stop-hook infrastructure.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared helpers at `tests/robustness/test-session-robustness.sh:14-18`, creates a temp test root at `tests/robustness/test-session-robustness.sh:19`, then repeatedly builds `.humanize/rlcr`-like directory layouts and calls `find_active_loop`. Positive cases verify newest active `state.md` selection at `tests/robustness/test-session-robustness.sh:33-46`, `finalize-state.md` detection at `tests/robustness/test-session-robustness.sh:48-61`, many-session performance at `tests/robustness/test-session-robustness.sh:63-87`, zombie-loop protection at `tests/robustness/test-session-robustness.sh:89-103`, and both active state files at `tests/robustness/test-session-robustness.sh:105-118`.
- inputs_outputs_state: Inputs are generated session directories and marker files such as `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`. Output is pass/fail status from `test-helpers.sh` and the path or empty string returned by `find_active_loop`. State is temporary filesystem state only; each scenario resets `TEST_DIR/rlcr`.
- gates_or_invariants: Without a session filter, `find_active_loop` checks only the single newest immediate child directory and returns it only if it has an active state file, as implemented in `hooks/lib/loop-common.sh:329-343`. The tests enforce that older active sessions are not revived when the newest directory has no active state or is terminal: see `tests/robustness/test-session-robustness.sh:89-103` and `tests/robustness/test-session-robustness.sh:287-302`. Completed and canceled loops are ignored at `tests/robustness/test-session-robustness.sh:259-285`.
- dependencies_and_callers: Depends on `find_active_loop`, `resolve_active_state_file`, and `resolve_any_state_file` from `hooks/lib/loop-common.sh`. These functions are used by `hooks/loop-bash-validator.sh` to decide whether RLCR-specific Bash protections apply and by other validators/stop hooks for session-aware loop routing.
- edge_cases_or_failure_modes: Covered edge cases include empty/nonexistent bases at `tests/robustness/test-session-robustness.sh:128-148`, files without subdirectories at `tests/robustness/test-session-robustness.sh:150-162`, sessions lacking state files at `tests/robustness/test-session-robustness.sh:164-177`, non-standard names at `tests/robustness/test-session-robustness.sh:179-192`, symlinked session dirs at `tests/robustness/test-session-robustness.sh:194-208`, spaces in names at `tests/robustness/test-session-robustness.sh:210-222`, deep nested state ignored because only immediate children are checked at `tests/robustness/test-session-robustness.sh:224-238`, rapid creation at `tests/robustness/test-session-robustness.sh:240-257`, and graceful handling of the final naming scenario at `tests/robustness/test-session-robustness.sh:304-316`.
- validation_or_tests: This file is itself the robustness test and ends by printing `Session Robustness Test Summary` at `tests/robustness/test-session-robustness.sh:318-322`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence headings present; each assigned item is represented once in the Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`