# agent_06 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-006 `directory` `hooks`
- cursor: `[_]`
- core_role: Runtime enforcement engine for the Humanize RLCR loop. The directory registers Claude hooks, validates tool use, selects the active loop, protects loop-managed artifacts, performs Stop-hook state-machine transitions, and implements this branch’s background-task stop short-circuit.
- algorithmic_behavior: Recursive contents inspected: `hooks/hooks.json`, top-level validators, `check-todos-from-transcript.py`, `loop-codex-stop-hook.sh`, and `hooks/lib/*`. `hooks/hooks.json:4-69` wires `UserPromptSubmit`, `PreToolUse` for Write/Edit/Read/Bash, `PostToolUse` for Bash, and a 7200s `Stop` hook. `hooks/loop-codex-stop-hook.sh:61-84` extracts `session_id`, finds the active loop with background-marker opt-in, then calls `handle_bg_task_short_circuit` before all normal gates. `hooks/lib/loop-bg-tasks.sh:97-193` computes pending async work as launched task IDs minus completion IDs. `hooks/lib/loop-bg-tasks.sh:227-363` implements the four ordered background guards: ambiguous caller, cross-session parked loop, pending background short-circuit, and same-session stale-marker cleanup.
- inputs_outputs_state: Inputs are hook JSON on stdin, `CLAUDE_PROJECT_DIR`, optional `session_id` and `transcript_path`, `.humanize/rlcr/<timestamp>/*` state artifacts, git status, summaries, goal tracker, BitLesson file, and Codex CLI outputs. Outputs include validator exit codes (`0` allow, `1` malformed/safety error, `2` block), Stop-hook JSON objects with `decision: block` or only `systemMessage`, generated round/review/finalize prompts, `bg-pending.marker`, `.review-phase-started`, and state-file renames. Major state transitions are active `state.md` to review (`review_started=true` plus marker), to `finalize-state.md`, to terminal `complete-state.md`/`stop-state.md`/`maxiter-state.md`, or to `methodology-analysis-state.md`; see `hooks/loop-codex-stop-hook.sh:1826-1907` and `hooks/lib/methodology-analysis.sh:38-99`.
- gates_or_invariants: Shared input validation rejects null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, and deeply nested JSON (`hooks/lib/loop-common.sh:91-165`). Active-loop discovery is session-aware and only the Stop hook can opt into marker fallback (`hooks/lib/loop-common.sh:295-412`). Tool validators protect todos, prompts, state files, plan backups, wrong-round summaries/contracts, old-session goal trackers, and post-Round-0 immutable goal-tracker sections (`hooks/loop-read-validator.sh:56-64`, `hooks/loop-write-validator.sh:237-264`, `hooks/loop-edit-validator.sh:188-228`). Bash blocks manual stop-hook execution, git pushes when disabled, `.humanize` staging, and shell-based modifications to protected files (`hooks/loop-bash-validator.sh:160-184`, `hooks/loop-bash-validator.sh:223-231`, `hooks/loop-bash-validator.sh:241-553`). Stop gates enforce schema, branch consistency, plan integrity, incomplete tasks, git cleanliness, large files, summary presence, round contract, BitLesson delta, Round 0 goal tracker initialization, max iterations, Codex review, drift, and finalize completion.
- dependencies_and_callers: Callers are Claude Code’s hook system via `hooks.json`, and the non-hook wrapper `scripts/rlcr-stop-gate.sh`, which forwards Stop-hook JSON and maps no-decision JSON to `ALLOW` (`scripts/rlcr-stop-gate.sh:95-160`). Dependencies include `jq`, `git`, `codex`, `python3`, `awk/sed/perl`, `scripts/portable-timeout.sh`, `scripts/bitlesson-validate-delta.sh`, `scripts/lib/config-loader.sh`, `scripts/humanize.sh`, and `prompt-template/*`. `check-todos-from-transcript.py` supports both legacy `TodoWrite` transcript parsing and `~/.claude/tasks/<session_id>` task files (`hooks/check-todos-from-transcript.py:73-170`).
- edge_cases_or_failure_modes: Branch-specific background behavior fails closed on missing transcript, missing file, unreadable parse, or missing `jq`; in those cases it does not infer “no pending” and does not clean markers (`hooks/lib/loop-bg-tasks.sh:143-148`, `hooks/lib/loop-bg-tasks.sh:344-360`). Pending background tasks write `bg-pending.marker` and exit 0 with a system message instead of running git/summary/Codex gates (`hooks/lib/loop-bg-tasks.sh:320-332`). Cross-session parked loops return a parked system message and leave state untouched (`hooks/lib/loop-bg-tasks.sh:282-293`). Empty-session callers with an existing marker exit silently (`hooks/lib/loop-bg-tasks.sh:262-264`). Loop-start timestamps are converted from local wall-clock directory names to UTC to avoid pre-loop transcript launches pinning a loop (`hooks/lib/loop-bg-tasks.sh:48-95`). The stop hook also handles stale git `index.lock`, missing Codex CLI, review failures/no output, absent review-phase marker, and malformed state.
- validation_or_tests: Relevant executable specs include the assigned `tests/test-stop-hook-legacy-compat.sh`, assigned `tests/robustness/test-hook-input-robustness.sh`, and supporting `tests/test-stop-hook-bg-allow.sh`. The background test documents acceptance criteria for pending subagents/shells, completion notifications, marker preservation/cleanup, session isolation, tilde paths, malformed transcripts, timezone filtering, and wrapper behavior (`tests/test-stop-hook-bg-allow.sh:3-34`, `tests/test-stop-hook-bg-allow.sh:343-418`, `tests/test-stop-hook-bg-allow.sh:586-820`, `tests/test-stop-hook-bg-allow.sh:937-1330`). Tests were inspected, not executed, because this task requested research notes only.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-036 `file` `docs/bitlesson.md`
- cursor: `[_]`
- core_role: Behavior-defining documentation for BitLesson, the RLCR knowledge-capture contract that feeds task selection and Stop-hook summary validation.
- algorithmic_behavior: Defines model/provider routing from config into BitLesson selection. Config precedence is default config, user config, project config, then CLI flags (`docs/bitlesson.md:7-12`). Provider routing maps `gpt-*`/`o[N]-*` to Codex and `claude-*`/`haiku`/`sonnet`/`opus` to Claude (`docs/bitlesson.md:14-18`). Codex-only provider mode forces selection onto Codex/OpenAI before provider resolution (`docs/bitlesson.md:21-23`). Workflow initializes `.humanize/bitlesson.md`, runs `scripts/bitlesson-select.sh` per task/sub-task, applies selected IDs or `NONE`, and validates `## BitLesson Delta` in every round summary (`docs/bitlesson.md:25-35`).
- inputs_outputs_state: Inputs are merged config (`bitlesson_model`), provider availability, project `.humanize/bitlesson.md`, task/sub-task descriptions, and round summary content. Outputs are selected lesson IDs, `NONE`, initialized BitLesson knowledge base content, and Stop-hook block/allow decisions based on the summary delta. State evolves through project-local knowledge entries and per-round summary deltas.
- gates_or_invariants: Required summary contract is exactly a `## BitLesson Delta` section with `Action: none|add|update`, `Lesson ID(s): <IDs or NONE>`, and `Notes` (`docs/bitlesson.md:36-45`). Strict rules require `Action: none` to use `NONE` or an empty ID field, while `add`/`update` must reference concrete `BL-YYYYMMDD-short-name` IDs that exist in `.humanize/bitlesson.md` (`docs/bitlesson.md:47-51`). Implementation backs this with `scripts/bitlesson-validate-delta.sh`, which detects the section outside fences/comments, validates one action, checks IDs, rejects placeholder notes, and blocks via JSON (`scripts/bitlesson-validate-delta.sh:92-217`, `scripts/bitlesson-validate-delta.sh:220-387`).
- dependencies_and_callers: Called from `scripts/setup-rlcr-loop.sh` for initialization/state fields and from `hooks/loop-codex-stop-hook.sh:820-838` for non-finalize summary validation when `bitlesson_required: true`. Depends on `scripts/bitlesson-init.sh`, `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, `templates/bitlesson.md`, config loading, and provider routing helpers. README links this doc as the Bitter Lesson workflow reference (`README.md:86`).
- edge_cases_or_failure_modes: If the configured provider binary is missing, selection falls back to default Codex so the loop can proceed (`docs/bitlesson.md:19`). Legacy loops without `bitlesson_required` must remain disabled even when `.humanize/bitlesson.md` exists; this is explicitly tested in `tests/test-stop-hook-legacy-compat.sh:185-197`. Empty knowledge bases can be prevented from endlessly reporting `none` with `--require-bitlesson-entry-for-none` (`docs/bitlesson.md:51`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` covers the validator broadly, and the assigned legacy compatibility test asserts missing `bitlesson_required` does not accidentally enable the gate. Documentation itself was inspected directly and cross-checked against the validator implementation.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-066 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI escape stripping used by the test runner’s output parsing, ensuring pass/fail counts remain correct on GNU/Linux and BSD/macOS sed.
- algorithmic_behavior: Defines colored `PASS`/`FAIL` helper output and tests a sed expression using ANSI-C quoted ESC: `sed "s/${esc}\\[[0-9;]*m//g"` (`tests/test-ansi-parsing.sh:38-47`). It strips color/bold sequences, then extracts numeric counts with portable `grep -oE` and tailing numeric matches (`tests/test-ansi-parsing.sh:73-90`, `tests/test-ansi-parsing.sh:123-125`).
- inputs_outputs_state: Inputs are hard-coded strings containing ANSI SGR sequences, plain text, multi-line summary output, and zero-count cases. Outputs are colored pass/fail lines, integer `TESTS_PASSED` and `TESTS_FAILED` counters, and final process exit `0` if no test failed or `1` otherwise (`tests/test-ansi-parsing.sh:160-178`).
- gates_or_invariants: The stripping expression must preserve visible text and remove only ANSI color/bold markers. Count extraction must work for `Passed:` and `Failed:` labels, including zero, multi-line summaries, and no-color inputs (`tests/test-ansi-parsing.sh:42-158`). The test is registered in the full suite at `tests/run-all-tests.sh:73`.
- dependencies_and_callers: Depends only on Bash, `sed`, `grep`, and standard shell arithmetic. It validates behavior consumed by `tests/run-all-tests.sh`, whose output uses ANSI color constants (`tests/run-all-tests.sh:46-51`) and aggregates suite results.
- edge_cases_or_failure_modes: Covers multiple ANSI sequences in one line, bold plus color, plain text without ANSI, zero failed count, and multi-line summaries where the last count is selected. It does not cover non-SGR ANSI escape families such as cursor movement; the algorithm under test is intentionally scoped to SGR color/bold sequences.
- validation_or_tests: The file is self-validating with eight tests and final summary/exit code logic. It was inspected, not executed.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-096 `file` `tests/test-stop-hook-legacy-compat.sh`
- cursor: `[_]`
- core_role: Executable specification for legacy compatibility in `hooks/loop-codex-stop-hook.sh`, especially git-dirty filtering for Humanize local folders and BitLesson opt-in behavior for old state files.
- algorithmic_behavior: Builds isolated git repos, installs a mock `codex` binary that writes an invocation marker, creates a minimal active RLCR loop fixture, and invokes the real Stop hook with empty JSON (`tests/test-stop-hook-legacy-compat.sh:25-38`, `tests/test-stop-hook-legacy-compat.sh:40-136`). The fixture intentionally omits `bitlesson_required` and omits a `## BitLesson Delta` in `round-0-summary.md` so regressions in BitLesson fallback would block before Codex (`tests/test-stop-hook-legacy-compat.sh:67-93`).
- inputs_outputs_state: Inputs are temp repos, `.gitignore`, untracked `.humanize-old`, untracked `.humanizeconfig`, optional `.humanize/bitlesson.md`, generated `state.md`, summary, goal tracker, and hook stdin `{}`. Outputs are `RUN_EXIT_CODE`, captured hook output, and `codex-called.marker` indicating the hook reached the Codex review path.
- gates_or_invariants: Test 1 asserts untracked legacy `.humanize-*` paths are ignored by git-dirty checks (`tests/test-stop-hook-legacy-compat.sh:145-160`). Test 1b asserts `.humanizeconfig` is treated as a real untracked file and blocks before Codex while still surfacing Humanize-local and generic untracked-file notes (`tests/test-stop-hook-legacy-compat.sh:162-183`). Test 2 asserts legacy state missing `bitlesson_required` leaves BitLesson validation disabled even when `.humanize/bitlesson.md` exists (`tests/test-stop-hook-legacy-compat.sh:185-197`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp repo setup and counters, `git`, mock `codex`, `jq` indirectly through the hook, `XDG_CACHE_HOME`, and the real Stop hook at `hooks/loop-codex-stop-hook.sh`. It targets stop-hook logic around `BITLESSON_REQUIRED` parsing (`hooks/loop-codex-stop-hook.sh:115-167`) and untracked Humanize filtering (`hooks/loop-codex-stop-hook.sh:654-685`). Registered in the full suite at `tests/run-all-tests.sh:70`.
- edge_cases_or_failure_modes: Captures compatibility edge cases where global gitignore may already ignore `.humanize/`, so `.humanize-old/` is used to guarantee a visible local Humanize note (`tests/test-stop-hook-legacy-compat.sh:166-170`). The mock Codex allows the test to distinguish “blocked before review” from “review path reached” without external Codex dependency.
- validation_or_tests: This file is the validation. It was inspected, not executed.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-126 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Block template for the Round 0 Goal Tracker initialization gate. It defines the corrective prompt when the Stop hook detects placeholder content in the immutable goal/task sections.
- algorithmic_behavior: Renders a block reason with `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, tells the agent to read the tracker, replace placeholder text, define an Ultimate Goal, define 3-7 testable acceptance criteria, populate Active Tasks, and write the updated file (`prompt-template/block/goal-tracker-not-initialized.md:1-18`). It states the immutable section can only be set in Round 0 (`prompt-template/block/goal-tracker-not-initialized.md:16`).
- inputs_outputs_state: Inputs are template variables from the Stop hook after it scans goal tracker sections. Output is a rendered markdown block embedded in Stop-hook JSON `reason`, with `systemMessage` “Goal Tracker not initialized in Round 0” (`hooks/loop-codex-stop-hook.sh:897-914`). State transition is not direct file mutation by the template; it blocks exit until the agent updates `goal-tracker.md` and retries Stop.
- gates_or_invariants: The corresponding gate runs only outside Finalize Phase, before review has started, in Round 0, and when `goal-tracker.md` exists (`hooks/loop-codex-stop-hook.sh:846-849`). The hook extracts Ultimate Goal, Acceptance Criteria, and Active Tasks sections and flags generic `[To be ...]` placeholders (`hooks/loop-codex-stop-hook.sh:855-895`). The template’s invariant is that the immutable section must be initialized in Round 0 before later rounds make it read-only.
- dependencies_and_callers: Loaded by `load_and_render_safe` through the template loader’s single-pass substitution (`hooks/lib/template-loader.sh:48-136`). The broader immutability contract is enforced by write/edit validators using `goal_tracker_mutable_update_allowed` (`hooks/lib/loop-common.sh:907-950`, `hooks/loop-write-validator.sh:241-264`, `hooks/loop-edit-validator.sh:192-228`).
- edge_cases_or_failure_modes: If the template is missing or empty, the Stop hook has an inline fallback with the same variable contract (`hooks/loop-codex-stop-hook.sh:897-904`). Placeholder detection is section-scoped to avoid blocking on stray mentions elsewhere in the file. If the file itself is missing, this specific initialization gate does not fire; other setup/summary gates may still block.
- validation_or_tests: Template references are covered by template reference/comprehensive tests; setup robustness also checks skip-implementation goal trackers lack placeholder text. This template was inspected directly and cross-referenced to its Stop-hook call site.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-156 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing continuation template that defines the optional summary-side protocol for requesting Goal Tracker reconciliation when direct mutable-section edits are unsafe or blocked.
- algorithmic_behavior: Provides a markdown section skeleton headed `## Goal Tracker Update Request`, with `Requested Changes` and `Justification` subsections (`prompt-template/claude/goal-tracker-update-request.md:1-17`). Suggested request categories include marking tasks completed with evidence, adding blocking or queued side issues, recording plan evolution, and deferring tasks with AC impact.
- inputs_outputs_state: Input is static template content loaded during next-round prompt generation. Output is appended to `round-N-prompt.md` after Codex review finds issues and the loop is continuing (`hooks/loop-codex-stop-hook.sh:2138-2143`). State transition is indirect: Claude may include a matching section in the next summary, and Codex review prompt instructions can reconcile justified changes into the active `goal-tracker.md`.
- gates_or_invariants: The template reinforces that direct tracker updates are optional only when unsafe; changes must be justified and tied to the Ultimate Goal. It complements the hard validator invariant that after Round 0 only the mutable section may change and Bash cannot modify the tracker (`hooks/lib/loop-common.sh:936-950`, `hooks/loop-bash-validator.sh:485-499`).
- dependencies_and_callers: Called by `loop-codex-stop-hook.sh` after building the next prompt (`hooks/loop-codex-stop-hook.sh:2122-2143`). It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, whose fallback tells Codex to apply requested tracker changes when present in Claude’s summary (`hooks/loop-codex-stop-hook.sh:971-975`), and with read/write/edit validators that scope tracker access to the active loop.
- edge_cases_or_failure_modes: If the template is absent, the hook appends a short fallback sentence instead (`hooks/loop-codex-stop-hook.sh:2139-2142`). Requests can become stale or overbroad if not justified; the design routes them through Codex review instead of granting unrestricted immutable-section mutation.
- validation_or_tests: Template reference and comprehensive template tests reference goal-tracker update templates, and hook-system robustness covers mutable versus immutable tracker edits after Round 0. This file was inspected directly and cross-checked to its call site.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-186 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production hook input parsing, shared command-pattern helpers, and monitor behavior under hostile or unusual inputs.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, then pipes synthetic JSON into real validators (`loop-read-validator.sh`, `loop-write-validator.sh`, `loop-bash-validator.sh`) to verify acceptance and rejection behavior (`tests/robustness/test-hook-input-robustness.sh:12-40`). It later sources `scripts/humanize.sh` and invokes production monitor helper functions and `_humanize_monitor_codex` through fixture runner scripts (`tests/robustness/test-hook-input-robustness.sh:381-471`).
- inputs_outputs_state: Inputs include valid Read/Write/Bash hook JSON, invalid JSON, `{}`, missing `tool_input.file_path`, a 10KB command, shell metacharacters, Unicode paths, unknown tool names, 50-level nested JSON, binary invalid UTF-8 bytes, null-byte JSON, ANSI/binary/rapidly updated logs, and fake monitor session directories. Outputs are pass/fail counters, validator exit codes, parser return strings, monitor exit markers, and final test summary/exit (`tests/robustness/test-hook-input-robustness.sh:666-671`).
- gates_or_invariants: Valid JSON for non-loop paths must pass (`tests/robustness/test-hook-input-robustness.sh:35-69`). Invalid JSON, empty JSON, missing required fields, deep nesting, and invalid UTF-8 must reject without signal crashes (`tests/robustness/test-hook-input-robustness.sh:71-217`). Unknown tools should pass through (`tests/robustness/test-hook-input-robustness.sh:163-175`). `command_modifies_file` must detect `sed -i` and redirects (`tests/robustness/test-hook-input-robustness.sh:351-370`). Monitor helpers must return pipe-delimited parse values and tolerate no-session, narrow terminal, ANSI log, and deleted-session scenarios (`tests/robustness/test-hook-input-robustness.sh:381-664`).
- dependencies_and_callers: Depends on production hooks, `scripts/humanize.sh`, test helpers, Bash, `jq`, `sed`, `wc`, `mktemp`, and terminal shims for `tput`/`clear`. Registered in the full suite at `tests/run-all-tests.sh:107`. It directly validates shared routines implemented in `hooks/lib/loop-common.sh:91-165` and `hooks/lib/loop-common.sh:1432-1460`.
- edge_cases_or_failure_modes: Notes that Bash command substitution strips null bytes before the hook can see them, so the expected invariant is graceful non-signal behavior rather than deterministic rejection (`tests/robustness/test-hook-input-robustness.sh:219-235`). Monitor fixtures delete `.humanize/rlcr` while the monitor is running to verify graceful exit under disappearing state (`tests/robustness/test-hook-input-robustness.sh:531-560`). It also covers ANSI log rendering and narrow terminal width to prevent UI/parser crashes.
- validation_or_tests: The file is itself a robustness suite. It was inspected, not executed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 section headings above, matching the assigned count
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`