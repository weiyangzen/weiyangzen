# agent_16 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-016 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-facing review template set for the RLCR stop-hook algorithm. The directory contains exactly five files totaling 12,759 bytes: `regular-review.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, `commit-history-section.md`, and `code-review-phase.md`.
- algorithmic_behavior: `regular-review.md` defines the normal implementation review contract: read the plan, compare Claude’s claimed work to actual implementation, classify findings into mainline gaps/blocking side issues/queued side issues, emit a mandatory `Mainline Progress Verdict`, and only end with `COMPLETE` when all planned work is fully done with no deferrals (`prompt-template/codex/regular-review.md:1-75`). `full-alignment-review.md` is the heavier periodic checkpoint template with acceptance-criteria audit, forgotten-item detection, deferred-item audit, drift audit, implementation review, stagnation check, and strict output rules (`prompt-template/codex/full-alignment-review.md:21-114`). `goal-tracker-update-section.md` delegates mutable goal-tracker repair to Codex while forbidding immutable-section edits (`prompt-template/codex/goal-tracker-update-section.md:1-18`). `commit-history-section.md` injects accumulated commits and recent round files as integral context (`prompt-template/codex/commit-history-section.md:1-12`). `code-review-phase.md` documents the audit record for `codex review --base ...` and its P-severity issue scan (`prompt-template/codex/code-review-phase.md:1-36`).
- inputs_outputs_state: Inputs are template variables rendered by `hooks/loop-codex-stop-hook.sh`: plan path, prompt path, summary content, goal tracker, docs path, commit history section, loop timestamp, previous round indices, review result path, base branch, and timestamps (`hooks/loop-codex-stop-hook.sh:971-1087`, `hooks/loop-codex-stop-hook.sh:1185-1208`). Outputs are rendered review/audit prompt files under the active `.humanize/rlcr/<loop>/` directory. The templates do not mutate state directly, but their required Codex outputs drive stop-hook state transitions into next-round blocking, review phase, finalize phase, stop, or completion.
- gates_or_invariants: The directory encodes several algorithmic gates: deferrals are incomplete, `COMPLETE` is allowed only when all original plan tasks/ACs are met, full-alignment rounds must include a mainline verdict, and full-alignment stagnation can require `STOP` (`regular-review.md:49-75`, `full-alignment-review.md:52-67`, `full-alignment-review.md:94-114`). Template rendering is single-pass so injected values containing `{{...}}` are not recursively expanded (`hooks/lib/template-loader.sh:48-56`, `hooks/lib/template-loader.sh:69-135`).
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`, which loads the goal-tracker update section (`:971-975`), commit history section (`:1017-1026`), either full-alignment or regular review prompt (`:1055-1087`), and the code-review audit prompt (`:1202-1208`). Rendering uses `hooks/lib/template-loader.sh:188-210`. Review-result interpretation depends on `extract_mainline_progress_verdict` (`hooks/lib/loop-common.sh:621-649`) and code-review issue detection (`hooks/lib/loop-common.sh:706-775`).
- edge_cases_or_failure_modes: Missing templates fall back through `load_and_render_safe` instead of crashing (`hooks/lib/template-loader.sh:188-210`). Missing variables remain as placeholders by design (`hooks/lib/template-loader.sh:7-14`). Invalid `full_review_round` defaults to 5 before selecting full-alignment mode (`hooks/loop-codex-stop-hook.sh:977-987`). A missing or non-ancestor `BASE_COMMIT` falls back to recent branch commits with an annotation (`hooks/loop-codex-stop-hook.sh:997-1007`). The templates are prescriptive text, so enforcement depends on downstream parser/gate code noticing missing verdicts, `COMPLETE`, `STOP`, or P-severity review findings.
- validation_or_tests: `tests/test-template-references.sh` scans hook scripts for template references and verifies referenced template files exist (`tests/test-template-references.sh:51-111`). `tests/test-commit-history-section.sh` verifies commit-history rendering for round 0, later rounds, corrupted base commits, and fallback behavior (`tests/test-commit-history-section.sh:1-115`). `tests/test-templates-comprehensive.sh` directly renders `codex/goal-tracker-update-section.md` and checks expected content (`tests/test-templates-comprehensive.sh:531-539`). I did not execute tests because this assignment is a read-only research pass.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-046 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Write-path validator for RLCR loop state. It prevents Claude from corrupting generated prompts, todos, loop state files, wrong-round summaries/contracts, plan backups, and immutable goal-tracker content.
- algorithmic_behavior: The hook reads JSON from stdin, validates structure/depth, exits early unless `tool_name` is `Write`, requires `tool_input.file_path`, lowercases the path for type checks, and extracts `session_id` for session-aware active-loop lookup (`hooks/loop-write-validator.sh:24-52`). It blocks `round-*-todos.md` except a small allowlist, blocks all prompt-file writes, enforces methodology-analysis write restrictions before normal file-type exits, classifies summary/contract/finalize/state/goal-tracker paths, resolves the active loop and current round, blocks managed state-file writes, allows only active-loop `finalize-summary.md` in finalize phase, blocks contracts in finalize phase, protects `plan.md` loop backups, validates goal-tracker path and immutable-section preservation, then checks summary/contract location, round number, and active-loop directory (`hooks/loop-write-validator.sh:57-343`).
- inputs_outputs_state: Input is Claude hook JSON. Output is either exit 0 for allowed writes/non-Write tools, exit 1 for malformed hook input or malformed state, or exit 2 plus a rendered blocking message on stderr for policy violations. It reads active loop state from `.humanize/rlcr`, parses `current_round` via strict state parsing, and compares proposed goal-tracker content when the current round is greater than 0 (`hooks/loop-write-validator.sh:178-184`, `hooks/loop-write-validator.sh:241-263`). It does not intentionally mutate files.
- gates_or_invariants: Valid JSON and shallow nesting are required (`:26-34`). `file_path` is required for Write (`:42-49`). Prompt files are read-only (`:67-70`). During methodology analysis, only `methodology-analysis-report.md` and `methodology-analysis-done.md` may be written (`:87-132`). `state.md`, `finalize-state.md`, and `methodology-analysis-state.md` are blocked (`:190-203`). `goal-tracker.md` must be the active loop’s tracker, and after round 0 its immutable section must remain unchanged (`:241-263`). Summary/contract writes outside `.humanize/rlcr` or to the wrong round/directory are blocked (`:270-341`).
- dependencies_and_callers: Registered as the `Write` PreToolUse hook in `hooks/hooks.json:14-21`. It depends on `hooks/lib/loop-common.sh` for JSON validation, loop lookup, path classifiers, allowlist, strict state parsing, goal-tracker immutable extraction, and message rendering (`hooks/loop-write-validator.sh:16-19`; helper ranges include `hooks/lib/loop-common.sh:91-165`, `:295-412`, `:511-587`, `:782-849`, `:901-950`, `:981-1018`). It also depends on `jq`, `realpath` when available, and `prompt-template/block/*` templates through `load_and_render_safe`.
- edge_cases_or_failure_modes: If no active loop matches the session, the validator allows writes (`hooks/loop-write-validator.sh:167-169`). Methodology-analysis lookup intentionally avoids unfiltered fallback so unrelated sessions are not restricted, but a comment notes spawned agents with different session IDs are not restricted by hooks and rely on the analysis prompt (`:81-84`). If `realpath` cannot resolve a methodology-phase write path and the path contains `..`, it fails closed (`:88-111`). Legacy/non-loop files are allowed unless they match protected round/finalize/state/goal-tracker patterns (`:143-156`). The round-contract/summary gate checks path and round, not content quality.
- validation_or_tests: `tests/test-finalize-phase.sh` verifies finalize-summary writes are allowed, finalize-state writes are blocked, and finalize-phase round-contract writes are blocked (`tests/test-finalize-phase.sh:386-420`). `tests/test-template-references.sh` includes this validator in critical template-reference checks (`tests/test-template-references.sh:57-64`, `:176-190`). Search evidence also shows `tests/test-allowlist-validators.sh` exercises write-validator allowlist and wrong-round behavior. I did not execute tests in the read-only export.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-076 `file` `tests/test-config-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the repository’s configuration merge hierarchy, which feeds RLCR defaults through `scripts/lib/config-loader.sh` and `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The test sources `scripts/lib/config-loader.sh`, builds temporary project/user config layouts, calls `load_merged_config "$PROJECT_ROOT" "$PROJECT_DIR"` with controlled `XDG_CONFIG_HOME` and `HUMANIZE_CONFIG`, then reads keys with `get_config_value` and records pass/fail assertions (`tests/test-config-merge.sh:16-34`, `:39-200`).
- inputs_outputs_state: Inputs are default config from `config/default_config.json`, optional user config at `$XDG_CONFIG_HOME/humanize/config.json`, optional project config at `$PROJECT_DIR/.humanize/config.json`, and optional `HUMANIZE_CONFIG`. Outputs are test result lines and the final `print_test_summary "Config Merge Tests"` result (`tests/test-config-merge.sh:200`). Test state is temporary directories and JSON files created during each case.
- gates_or_invariants: It asserts default-only values for `bitlesson_model`, `agent_teams`, and `gen_plan_mode` (`:39-64`); project config overrides defaults (`:70-89`); project config wins over user config (`:95-109`); user keys not overridden by project remain additive (`:115-132`); null in a higher layer does not erase a lower-layer value (`:138-150`); `HUMANIZE_CONFIG` replaces the default project config path (`:156-173`); and all layers contribute distinct keys additively (`:179-198`).
- dependencies_and_callers: Direct dependency is `scripts/lib/config-loader.sh`, especially `load_merged_config` layer setup and jq merge (`scripts/lib/config-loader.sh:63-137`) and `get_config_value` conversion (`scripts/lib/config-loader.sh:139-159`). Shared test helpers provide `setup_test_dir`, `pass`, `fail`, and summary behavior (`tests/test-helpers.sh:30-89`). Production consumers include `hooks/lib/loop-common.sh`, which calls `load_merged_config` and derives defaults for BitLesson, Codex model/effort, and agent teams (`hooks/lib/loop-common.sh:179-223`).
- edge_cases_or_failure_modes: This test covers null stripping but not malformed optional config, missing default config, non-object JSON, or missing `jq`; those are handled by separate config-error tests. It assumes temp directory creation is available. Because `load_merged_config` uses `mktemp -d` and jq slurpfiles, failures in temp/file creation or missing jq become configuration-loader failures rather than assertion failures.
- validation_or_tests: This file is itself the validation asset. It maps directly to loader internals: required default config is prepared as required, optional user/project layers degrade to `{}`, and jq merges stripped-null layers in order empty/default/user/project (`scripts/lib/config-loader.sh:24-61`, `:100-132`). I inspected but did not run it due the read-only research constraint.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-106 `file` `hooks/lib/methodology-analysis.sh`
- cursor: `[_]`
- core_role: Terminal-phase library for RLCR methodology analysis. It inserts a methodology-improvement checkpoint before the loop fully exits, without treating project implementation work as the analysis subject.
- algorithmic_behavior: `enter_methodology_analysis_phase` skips when privacy mode is true, when methodology analysis is already active, or when a prior done marker contains content; otherwise it renames the active state file to `methodology-analysis-state.md`, writes `.methodology-exit-reason`, creates an empty done placeholder, renders `claude/methodology-analysis-prompt.md`, and outputs a JSON block telling Claude to perform the analysis (`hooks/lib/methodology-analysis.sh:38-100`). `complete_methodology_analysis` requires a non-empty done marker, a non-empty report, a valid stored exit reason, then renames `methodology-analysis-state.md` to `<reason>-state.md` and removes the marker (`:114-174`). `block_methodology_analysis_incomplete` emits a JSON block explaining how to finish the phase (`:183-206`).
- inputs_outputs_state: Inputs are function arguments `exit_reason` and description plus globals `PRIVACY_MODE`, `STATE_FILE`, `LOOP_DIR`, `CURRENT_ROUND`, `MAX_ITERATIONS`, and `TEMPLATE_DIR` (`:22-33`, `:107-109`). Outputs are JSON objects with `decision: block`, stderr diagnostics, `methodology-analysis-state.md`, `.methodology-exit-reason`, `methodology-analysis-report.md`, and `methodology-analysis-done.md`. State transitions are active/finalize state to methodology-analysis state, then methodology-analysis state to `complete-state.md`, `stop-state.md`, or `maxiter-state.md`.
- gates_or_invariants: Privacy mode disables the phase (`:42-46`). Re-entry is blocked if methodology-analysis state already exists (`:48-52`). A non-empty previous done marker skips duplicate analysis (`:54-62`). Completion requires meaningful done content, meaningful report content, an existing exit-reason marker, and exit reason in `complete|stop|maxiter` (`:118-163`). Invalid or missing evidence fails closed.
- dependencies_and_callers: Sourced by the stop hook at `hooks/loop-codex-stop-hook.sh:53-54`. The finalize-success path tries to enter methodology analysis before writing `complete-state.md` (`hooks/loop-codex-stop-hook.sh:943-952`). The rendered prompt is `prompt-template/claude/methodology-analysis-prompt.md`, which requires a sanitized Opus-agent report and a meaningful completion marker (`prompt-template/claude/methodology-analysis-prompt.md:12-31`, `:43-73`). Write restrictions are reinforced by `hooks/loop-write-validator.sh:87-132`; read/bash/edit validators also reference methodology-analysis state.
- edge_cases_or_failure_modes: The empty placeholder done file intentionally does not complete the phase (`hooks/lib/methodology-analysis.sh:71-73`, `:123-129`). Missing or whitespace-only reports block completion (`:131-143`). Missing or invalid `.methodology-exit-reason` blocks terminal-state selection (`:145-163`). If the library returns 1 from enter because privacy/skip/re-entry applies, the caller proceeds with its normal terminal transition. If filesystem renames fail, the caller’s `set -euo pipefail` context is expected to stop execution.
- validation_or_tests: The prompt and validators encode most of the tested contract; `prompt-template/claude/methodology-analysis-prompt.md:20-31` specifies sanitized report requirements, and `hooks/loop-write-validator.sh:87-132` enforces that only methodology report/done artifacts can be written during this phase. I did not find a dedicated assigned test file for this library in the inspected set, and I did not run tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-136 `file` `prompt-template/block/round-contract-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the anti-drift round-contract gate. It tells Claude that a round cannot exit until the current round contract exists.
- algorithmic_behavior: The template renders a target `{{ROUND_CONTRACT_FILE}}` and requires the contract to restate the single mainline objective, target ACs, truly blocking issues, queued/out-of-scope issues, and concrete success criteria (`prompt-template/block/round-contract-missing.md:1-13`).
- inputs_outputs_state: Input is `ROUND_CONTRACT_FILE` supplied by the stop hook. Output is a rendered blocking reason inside a jq JSON response. The template itself does not mutate state; it blocks normal stop-hook progression until the file exists.
- gates_or_invariants: The gate is active only for non-finalize loops whose raw state contains `drift_status`; legacy loops without that field bypass it (`hooks/loop-codex-stop-hook.sh:786-793`). If the contract file is absent, stop hook returns a block decision with system message `Loop: Round contract missing for round ...` (`hooks/loop-codex-stop-hook.sh:793-815`). Write-validator rules separately ensure contract writes target the active loop and current round (`hooks/loop-write-validator.sh:305-341`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/round-contract-missing.md"` (`hooks/loop-codex-stop-hook.sh:804-805`). Rendering depends on `hooks/lib/template-loader.sh:188-210`.
- edge_cases_or_failure_modes: Missing template falls back to inline text in the stop hook (`hooks/loop-codex-stop-hook.sh:794-803`). The stop hook checks only file existence, not whether the contract actually contains the required sections. Finalize phase has no active round contract and contract writes are blocked (`hooks/loop-write-validator.sh:218-222`). Legacy loops without `drift_status` skip this anti-drift gate by design.
- validation_or_tests: Setup robustness tests create and check `round-0-contract.md` in skip-implementation scenarios, and stop-hook tests reference round-contract behavior; the direct inspected enforcement is in `hooks/loop-codex-stop-hook.sh:786-817`. I did not run tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-166 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Periodic full-goal alignment review template. It is the strongest Codex review contract for detecting drift, forgotten work, unjustified deferrals, and stagnation across RLCR rounds.
- algorithmic_behavior: It requires Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), review Claude’s summary and commit history (`:13-20`), audit each immutable acceptance criterion (`:21-31`), compare plan to goal tracker for forgotten items (`:32-37`), audit deferred items (`:38-43`), summarize completion (`:44-50`), classify mainline drift (`:52-67`), perform implementation review (`:68-74`), apply goal-tracker update instructions (`:75`), inspect recent historical files for stagnation (`:77-103`), and write findings/STOP/COMPLETE according to strict rules (`:104-114`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMMIT_HISTORY_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, previous-round indices, and `REVIEW_RESULT_FILE` supplied by `hooks/loop-codex-stop-hook.sh:1055-1069`. Output is Codex’s review result file and stdout/stderr captures from `codex exec` (`hooks/loop-codex-stop-hook.sh:1633-1658`).
- gates_or_invariants: The mandatory `Mainline Progress Verdict` is required; omission causes the Humanize stop hook to block and rerun review (`full-alignment-review.md:52-67`, stop-hook block function at `hooks/loop-codex-stop-hook.sh:1421-1454`). If stagnation is detected, the last line must be `STOP` (`full-alignment-review.md:94-103`). `COMPLETE` is legal only when all original plan tasks and ACs are fully met with no deferrals (`:106-114`).
- dependencies_and_callers: Selected every `FULL_REVIEW_ROUND` rounds using modulo logic in the stop hook (`hooks/loop-codex-stop-hook.sh:977-987`). It embeds the commit-history template output from `prompt-template/codex/commit-history-section.md` and the goal-tracker update section from `prompt-template/codex/goal-tracker-update-section.md` (`hooks/loop-codex-stop-hook.sh:971-1026`). Review verdict extraction depends on `hooks/lib/loop-common.sh:621-649`.
- edge_cases_or_failure_modes: Invalid or too-small `full_review_round` defaults to 5 (`hooks/loop-codex-stop-hook.sh:979-983`). Previous-round indices are clamped to avoid negative file references (`hooks/loop-codex-stop-hook.sh:989-995`). Historical files are prompt references, so missing files can degrade review quality but are not a shell-level failure at render time. There is a minor textual mismatch: line 111 says “see Part 4” for stagnation even though stagnation is Part 5; the operative STOP rule remains explicit.
- validation_or_tests: Template existence is covered by template-reference scanning (`tests/test-template-references.sh:51-111`). The semantics are primarily prompt-level gates enforced downstream by stop-hook parsing, verdict extraction, and final marker handling. I inspected but did not execute tests.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-196 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for `scripts/portable-timeout.sh`, the timeout adapter used by RLCR setup, stop-hook Git/Codex operations, ask-codex, ask-gemini, and BitLesson selection. This matters to the branch theme because stop hooks must not hang indefinitely while gating work.
- algorithmic_behavior: The test sources `portable-timeout.sh`, checks `TIMEOUT_IMPL`, validates the implementation enum, then exercises `run_with_timeout` across quick completion, timeout exit code, argument passing, exit-code preservation, pipelines, short timeouts, zero timeout, large output, rapid cycles, nonexistent command, empty command, special characters, signal handling, fallback detection, subshell execution, and export visibility (`tests/robustness/test-timeout-robustness.sh:14-278`).
- inputs_outputs_state: Inputs are detected commands on PATH (`gtimeout`, `timeout`, `python3`, `python`), shell utilities (`sleep`, `date`, `seq`, `wc`, `sh`, `printf`), and the sourced `TIMEOUT_IMPL`. Outputs are pass/fail test lines and `print_test_summary` exit status (`tests/robustness/test-timeout-robustness.sh:274-279`). The test has no durable repo state.
- gates_or_invariants: Valid implementations are `gtimeout`, `timeout`, `python3`, `python`, or `none` (`:39-49`). Timeout-capable implementations should return 124 on timed-out commands (`:62-77`, `:120-137`). Successful commands preserve stdout and exit status (`:51-60`, `:89-110`). Special characters are passed as arguments without shell expansion (`:211-219`). Fallback detection must return a valid enum (`:235-247`).
- dependencies_and_callers: Direct dependency is `scripts/portable-timeout.sh`, whose detection order is `gtimeout > timeout > python3 > python > none` (`scripts/portable-timeout.sh:9-29`) and whose `run_with_timeout` dispatches to GNU/Homebrew timeout, Python `subprocess.run(..., timeout=...)`, or no-timeout fallback (`scripts/portable-timeout.sh:31-76`). Stop-hook usage includes `codex review` and `codex exec` calls bounded by `run_with_timeout` (`hooks/loop-codex-stop-hook.sh:1227-1229`, `:1656-1658`).
- edge_cases_or_failure_modes: If no timeout implementation exists, timeout-specific cases are skipped/passed as skipped by message but still recorded via `pass`, so the suite validates graceful degradation rather than enforcing hard timeout availability (`tests/robustness/test-timeout-robustness.sh:65-77`, `:122-155`). Zero timeout accepts either immediate success or timeout (`:139-155`). Empty command handling is not strict; it always records pass with the observed exit code (`:201-209`). The signal-handling case accepts 124 or 0, so it does not fully assert kill behavior for commands that ignore termination (`:221-233`).
- validation_or_tests: This file is the validation asset for `portable-timeout.sh`. I inspected it and the implementation, but did not execute it because the task is read-only research and several cases intentionally sleep or depend on local timeout tools.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-016, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-046, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-076, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-106, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-136, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-166, DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-196
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`