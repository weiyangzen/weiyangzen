# agent_15 do-not-block-stop-when-background-running 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `3711e5fd9059584c7bf98cf1d19ee02dcf5bef48`
- notes: inspected assigned paths only in the read-only branch export; no files modified. `git` metadata was unavailable in this export, so source commit is taken from scheduler metadata.

## Item Evidence

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt templates define RLCR behavioral transitions: initial/continuing implementation rounds, review rounds, drift recovery, finalize phase, methodology analysis, open-question handling, post-alignment handling, push reminders, goal-tracker reconciliation, and Agent Teams delegation.
- algorithmic_behavior: Recursively inspected all 14 files: `agent-teams-continue.md`, `agent-teams-core.md`, `agent-teams-instructions.md`, `drift-replan-prompt.md`, `finalize-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `goal-tracker-update-request.md`, `methodology-analysis-prompt.md`, `next-round-footer.md`, `next-round-prompt.md`, `open-question-notice.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, `review-phase-prompt.md`. `next-round-prompt.md:12-27` requires re-reading plan/tracker/recent records and writing a round contract before coding. `drift-replan-prompt.md:17-33` forces a recovery contract after stalled/regressed rounds. `review-phase-prompt.md:18-32` classifies findings into blocking versus queued issues. `finalize-phase-prompt.md:16-24` and `finalize-phase-skipped-prompt.md:26-34` constrain finalize changes to functionality-equivalent simplification. `methodology-analysis-prompt.md:12-73` spawns a sanitized methodology analysis flow.
- inputs_outputs_state: Inputs are rendered placeholders such as `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, `STALL_COUNT`, `LAST_MAINLINE_VERDICT`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, and `START_BRANCH`. Outputs are prompt files or stop-hook block reasons, rendered through `load_template` / `load_and_render_safe`. These templates do not mutate state themselves; they instruct Claude to update summaries, contracts, trackers, or methodology artifacts through the proper loop flow.
- gates_or_invariants: The templates preserve key RLCR invariants: one mainline objective per round, no queued cleanup taking over mainline work, round contract before implementation, goal tracker mutable/immutable discipline, AskUserQuestion before resolving open questions (`open-question-notice.md:1`), and no direct loop cancellation or state-file manipulation (`next-round-footer.md:4-9`). Agent Teams mode forbids the team leader from implementing directly (`agent-teams-core.md:1-12`) and emphasizes file ownership to avoid silent overwrites (`agent-teams-core.md:16-23`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh:1366-1387` appends initial Agent Teams templates. `hooks/loop-codex-stop-hook.sh:1320-1353` renders finalize prompts, `1513-1520` renders review prompts, `2018-2036` chooses drift-replan versus next-round prompts, `2091-2110` injects open-question notice, `2116-2119` appends post-alignment actions, `2125-2135` appends footer and push note, `2139-2143` appends goal-tracker update request, and `2147-2155` appends Agent Teams continuation templates. `hooks/lib/methodology-analysis.sh:81-87` renders methodology-analysis prompt.
- edge_cases_or_failure_modes: Missing templates are generally covered by inline fallbacks through `load_and_render_safe`, so prompt generation can continue with reduced formatting. Review-skipped finalize has a separate template and stricter manual-verification language. Drift recovery replaces ordinary issue-clearing behavior when `DRIFT_REPLAN_REQUIRED` is true. Open-question detection is separate and inserted only when review content contains an open-question signal and the state enables asking Codex questions.
- validation_or_tests: `tests/test-agent-teams.sh:334-385` checks Agent Teams template existence/content. `tests/test-template-references.sh:54-110` scans shell scripts for referenced templates and `172-201` checks critical validators use safe rendering. `tests/test-finalize-phase.sh:1064-1088` validates finalize-state read restrictions that interact with finalize prompts.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-045 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Read validator for RLCR loop files. It prevents Claude from reading stale, wrong-round, wrong-directory, todos, or methodology-sensitive loop artifacts.
- algorithmic_behavior: Reads hook JSON from stdin (`hooks/loop-read-validator.sh:23`), validates JSON and nesting via shared helpers (`26-33`), exits for non-Read tools (`37-39`), requires `tool_input.file_path` (`41-46`), extracts session id (`49-50`), and then applies ordered read gates. Todos reads are blocked unless allowlisted in the active loop (`56-64`). Methodology-analysis restrictions run before general round-file logic (`66-168`), allowing only methodology artifacts inside the loop and blocking project files during that phase. Round/goal-tracker handling starts at `174-188`, resolves the session-matched active loop (`197`), parses state strictly (`210-215`), blocks round contracts during finalize (`217-220`), validates active goal tracker path (`226-244`), extracts round number/type (`250-263`), then enforces active loop directory, current round, allowlist, and exact path (`269-315`).
- inputs_outputs_state: Inputs are JSON fields `tool_name`, `tool_input.file_path`, optional `session_id`, plus environment `CLAUDE_PROJECT_DIR`, `PROJECT_ROOT`, `LOOP_BASE_DIR`, and optional `LOOP_DIR`. Outputs are exit status `0` for allowed reads, `1` for malformed input/state safety failure, and `2` for blocked reads with a rendered stderr message. It does not write state.
- gates_or_invariants: Enforces valid JSON, max depth 30, required file path, session-aware active loop selection, strict state parsing before current-round decisions, no historical round-contract reads during finalize, correct active goal tracker path, current-round-only prompt/summary/contract reads unless explicitly allowlisted, and methodology-analysis privacy isolation with path canonicalization.
- dependencies_and_callers: Registered for `Read` in `hooks/hooks.json:33-39`. Depends on `hooks/lib/loop-common.sh` for `validate_hook_input`, `is_deeply_nested`, `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, path classifiers, allowlists, and message renderers. Uses `jq`, `realpath`, `basename`, `dirname`, and `load_and_render_safe` templates such as `block/wrong-file-location.md`, `block/wrong-round-file.md`, and `block/wrong-directory-path.md`.
- edge_cases_or_failure_modes: No active loop allows reads (`199-201`). Malformed state fails closed (`210-214`). Methodology mode rejects unresolved traversal or symlink paths when canonicalization is unavailable (`98-115`). Files outside project root are allowed during methodology analysis, while project-root files are blocked (`151-166`). Spawned agents are noted as not restricted by these hooks and instead rely on the methodology prompt’s sanitization rule (`80-81`, `136-137`).
- validation_or_tests: `tests/robustness/test-hook-input-robustness.sh:35-115` covers valid/invalid JSON and missing fields; `180-235` covers deep nesting, non-UTF8, and null-byte handling. `tests/test-allowlist-validators.sh:320-370` verifies current round contract allowed and stale round contract blocked. `tests/test-finalize-phase.sh:1064-1088` verifies finalize-state parsing and finalize contract blocking.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-075 `file` `tests/test-config-error-handling.sh`
- cursor: `[_]`
- core_role: Executable specification for config-loader failure behavior. It guards the configuration layer that `loop-common.sh` uses to derive Codex/BitLesson/Agent Teams defaults.
- algorithmic_behavior: Sources `scripts/lib/config-loader.sh` (`tests/test-config-error-handling.sh:20-33`) and runs six cases. Missing required `default_config.json` must be fatal (`40-53`). Malformed project config emits a warning and falls back to defaults (`59-81`). Malformed user config emits a warning and falls back (`87-110`). Empty project config is valid and uses defaults (`116-128`). Missing project config is nonfatal (`134-150`). Missing user config directory is nonfatal (`156-172`).
- inputs_outputs_state: Inputs are temporary plugin/project/user config directories, `.humanize/config.json`, `XDG_CONFIG_HOME`, and the repository default config. Outputs are pass/fail records through `test-helpers.sh` and the final `print_test_summary` (`174`). State is limited to temporary test directories and config fixtures.
- gates_or_invariants: Required default config is a hard gate; optional user/project config layers are soft gates and must degrade to `{}` on absence or malformed JSON. Default `bitlesson_model` is asserted as `haiku` after fallback (`76-80`, `105-109`, `123-127`, `141-146`, `163-168`).
- dependencies_and_callers: Directly depends on `scripts/lib/config-loader.sh`. Related implementation: `_config_loader_prepare_layer` requires JSON object layers and warns for malformed optional layers (`scripts/lib/config-loader.sh:24-60`); `load_merged_config` merges empty, default, user, and project layers in order (`63-137`); `get_config_value` extracts scalar/string values (`139-159`). `hooks/lib/loop-common.sh:185-223` consumes this loader and validates merged `codex_model`, `codex_effort`, and `agent_teams`.
- edge_cases_or_failure_modes: A missing required default config must return nonzero. Malformed optional config must not poison the loop; stderr must contain warning-like text. Empty `{}` project config must not be treated as malformed. Missing optional paths should still produce merged defaults.
- validation_or_tests: This file is itself the validation. Related broader coverage appears in `tests/test-config-merge.sh` and `tests/test-unified-codex-config.sh` through additional `load_merged_config` calls.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-105 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR hook library. It centralizes hook input validation, active-loop/session selection, state parsing, review issue detection, path classifiers, rendered block messages, goal-tracker guards, cancel security checks, git hygiene checks, command write detection, terminal state transition, and background-task helper loading.
- algorithmic_behavior: Defines state fields, mainline verdict enums, drift status enums, markers, and exit reasons (`hooks/lib/loop-common.sh:24-76`). `validate_hook_input` rejects null bytes, invalid UTF-8 where `iconv` exists, invalid JSON, and missing `tool_name` (`91-128`); `require_tool_input_field` and `is_deeply_nested` add per-tool and DoS gates (`130-165`). Config/template setup loads merged config and validates Codex defaults (`171-235`). Active-loop logic resolves methodology/finalize/state priority (`251-267`), resolves terminal states (`269-293`), and session-filters loop directories with zombie-loop protection and optional background-marker fallback (`295-412`). State parsing has tolerant and strict modes (`481-587`). Review issue extraction scans the last 50 log lines for `[P?]` markers near line starts and writes a review-result file (`706-775`). Path/message helpers cover round files, allowlists, goal tracker immutability, state/finalize/methodology paths, cancel authorization, `.humanize` git protection, command write detection, and loop termination (`777-1516`).
- inputs_outputs_state: Inputs include hook JSON strings, state/frontmatter files, loop base directories, session ids, Codex review logs, command strings, goal tracker content, git working tree state, and template/config files. Outputs include exported/global `STATE_*` variables, return codes, rendered stderr/stdout messages, review-result files, updated YAML frontmatter through `upsert_state_fields`, and terminal state renames through `end_loop`. At the bottom it sources `loop-bg-tasks.sh` (`1518-1528`), making background-task helpers available to stop-hook callers.
- gates_or_invariants: Critical invariants include strict state schema for safety-sensitive operations (`520-587`), newest-directory zombie-loop protection (`329-343`), exact session isolation with terminal newest-loop suppression (`346-384`), marker fallback only for opt-in stop-hook callers (`390-407`), normalized mainline verdict/drift enums (`589-619`), immutable goal tracker preservation after Round 0 (`907-950`), exact cancel command structure and symlink rejection (`1020-1200`), `.humanize` never being broadly/force-added to git (`1208-1366`), tracked `.humanize` state blocking (`1368-1416`), and Bash write detection for protected files (`1432-1460`).
- dependencies_and_callers: Called by all core hooks listed in its header (`5-13`), including read/write/edit/bash validators and stop hook. It sources `scripts/lib/config-loader.sh` (`179`) and `hooks/lib/template-loader.sh` (`227`). Stop-hook uses the loaded background helper at `hooks/loop-codex-stop-hook.sh:83`. `hooks/loop-bash-validator.sh:528-536` uses `command_modifies_file` plus templates for round-contract Bash blocking.
- edge_cases_or_failure_modes: If `iconv` is absent, UTF-8 rejection relies on `jq`. `parse_state_file` is tolerant and applies defaults, while `parse_state_file_strict` fails closed for missing required fields or nonnumeric values. `upsert_state_fields` splits assignments on spaces, so it is suited to simple scalar field updates. Review issue detection intentionally scans only the last 50 lines, reducing false positives but missing issues emitted earlier. `git_adds_humanize` lowercases commands, so it explicitly treats `-A` as `-a` (`1212-1214`). The sourced background helper detects pending async Agent/Bash transcript events and exits stop-hook naturally while writing `bg-pending.marker`; cleanup only happens after a readable transcript proves no pending tasks (`hooks/lib/loop-bg-tasks.sh:239-363`).
- validation_or_tests: `tests/robustness/test-concurrent-state-robustness.sh:170-219` covers active-loop detection and finalize state; `332-370` covers strict parsing failures. `tests/robustness/test-hook-input-robustness.sh:35-235` covers JSON/depth/encoding gates. `tests/test-stop-hook-bg-allow.sh:1-34` lists background-task acceptance criteria and later verifies marker fallback, pending task detection, unreadable/malformed transcript fail-closed behavior, and transcript timestamp filtering. `tests/test-allowlist-validators.sh:320-370` verifies read allow/block behavior built on common helpers.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-135 `file` `prompt-template/block/round-contract-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that round contracts must be written through structured file tools, not shell redirection or command-line mutation.
- algorithmic_behavior: The template has a fixed heading and instruction (`prompt-template/block/round-contract-bash-write.md:1-5`) plus `{{CORRECT_PATH}}` (`7`). It is rendered when Bash command analysis detects an attempted modification of `round-[0-9]+-contract.md`.
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as the active loop’s current round contract path by `hooks/loop-bash-validator.sh:528-535`. Output is a human-facing block reason on stderr and validator exit code `2`; no state mutation.
- gates_or_invariants: Enforces round-contract provenance: Bash cannot create, overwrite, append, move, copy, remove, truncate, or otherwise modify a contract file when `command_modifies_file` matches protected patterns from `loop-common.sh:1432-1460`. The user must use Write/Edit tooling so other validators can enforce current loop path, round number, and immutable/mutable policy.
- dependencies_and_callers: Called by `hooks/loop-bash-validator.sh:528-536` through `load_and_render_safe`. Depends on `TEMPLATE_DIR` from `loop-common.sh`/`template-loader.sh`, and uses fallback text if the template is missing.
- edge_cases_or_failure_modes: Detection is regex-based over a lowercased shell command, so complex shell constructs outside `command_modifies_file` patterns could be missed. The validator still has a fallback block message if this file is absent. The template itself is intentionally tiny and carries no branching logic.
- validation_or_tests: `tests/test-template-references.sh:54-110` checks referenced templates exist. Bash validator behavior is indirectly covered by validator suites that exercise protected round files and allowlists.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-165 `file` `prompt-template/codex/commit-history-section.md`
- cursor: `[_]`
- core_role: Codex review prompt fragment providing integral development history: commits since loop start plus recent round summaries/review files. It helps Codex detect recurring issues, stalled progress, and mainline drift.
- algorithmic_behavior: Renders `{{COMMIT_HISTORY}}` inside a fenced block (`prompt-template/codex/commit-history-section.md:1-6`) and appends `{{RECENT_ROUND_FILES}}` under a recent-files directive (`8-12`). The stop hook builds `COMMIT_HISTORY` from `BASE_COMMIT..HEAD` when valid, otherwise falls back to recent branch commits with an annotation (`hooks/loop-codex-stop-hook.sh:1000-1007`). It builds recent round file references from the current loop timestamp and last up to three rounds (`1009-1015`), renders this template with fallback (`1017-1026`), then injects it into Codex review prompt content (`1036`, `1049`, `1064-1086`).
- inputs_outputs_state: Inputs are git log output, `BASE_COMMIT`, `HEAD`, `CURRENT_ROUND`, loop timestamp, and recent RLCR summary/review result paths. Output is a markdown section included in `round-N-review-prompt.md`. It does not directly mutate loop state except by contributing text to generated review prompts.
- gates_or_invariants: Keeps review context bounded and stable: newest history is weighted, but recurring/systemic patterns remain visible (`commit-history-section.md:12`). Stop-hook truncates normal history to the last 80 commits after base (`hooks/loop-codex-stop-hook.sh:1000`) and uses stable repository-relative `.humanize/rlcr/...` references for round files.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh:1024-1026`. Depends on git commands and `load_and_render_safe`. Tested by `tests/test-commit-history-section.sh`.
- edge_cases_or_failure_modes: First round yields `(first round, no prior history)` and no commits yields `(no commits yet)` (`tests/test-commit-history-section.sh:41-55`). Corrupted, unrelated, or empty base commit paths fall back to recent branch commits with or without annotation depending on case (`122-130`, `166-172`, `254-262`). Missing template fallback is required to still render commits and round files (`196-222`).
- validation_or_tests: `tests/test-commit-history-section.sh:33-61` validates round 0 placeholders, `64-114` validates commits plus round references, `117-143` validates corrupted base fallback, and `180-222` validates missing-template fallback.
- skip_candidate: `no`

### DO_NOT_BLOCK_STOP_WHEN_BACKGROUND_RUNNING-HZ-195 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for the template-rendering subsystem used by RLCR hook block messages and prompt generation.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and test helpers (`tests/robustness/test-template-stress-robustness.sh:14-17`), creates a temp dir (`19`), then checks normal rendering, fallback rendering, large values/templates, many variables, special characters, and placeholder-injection prevention. Key tests include standard substitution (`33-42`), missing-template fallback (`44-52`), 1KB template load/render (`54-72`), 10KB and 100KB variable values (`82-109`), 100KB template file (`111-132`), 50 substitutions (`134-151`), regex metacharacters (`161-170`), ampersands (`172-180`), backslashes (`183-193`), newlines (`195-205`), empty values (`207-216`), placeholder injection prevention (`218-228`), dollar signs (`241-250`), repeated variables (`252-261`), boundary variables (`263-272`), and placeholder-only templates (`274-283`).
- inputs_outputs_state: Inputs are in-memory template strings, generated template files under `TEST_DIR`, and variable assignments passed to `render_template` / `load_and_render` / `load_and_render_safe`. Outputs are pass/fail assertions and final summary (`289`). State is limited to temporary test files.
- gates_or_invariants: Establishes that rendering is single-pass and safe: values containing `{{OTHER_VAR}}` must not be re-expanded (`221-228`). It also asserts metacharacters are literal data, not regex/sed replacements, and that large values/templates complete while preserving substitution.
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`. Relevant implementation: `render_template` uses environment-prefixed variables and an awk scanner that emits replacement values without rescanning (`hooks/lib/template-loader.sh:56-136`); `load_and_render_safe` falls back on missing/empty templates (`188-211`).
- edge_cases_or_failure_modes: Uses Bash brace expansion to generate large strings (`84`, `99`), so shell compatibility matters. Size checks are threshold-based rather than exact-byte assertions for large cases. The “Unicode content” test uses basic ASCII text despite its label (`230-239`), so it mainly checks ordinary value propagation.
- validation_or_tests: This file is itself the stress validation. Related broader tests include `tests/test-template-loader.sh:197-225` for safe fallback and existing-template behavior, and `tests/test-template-loader.sh:488-525` for placeholder-injection prevention.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 item evidence sections present; each full assigned item id is used only as its section heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`