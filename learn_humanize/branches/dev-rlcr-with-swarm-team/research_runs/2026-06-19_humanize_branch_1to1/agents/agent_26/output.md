# agent_26 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: RLCR stop-hook state machine. It is registered as a Claude Code `Stop` hook in `hooks/hooks.json:63`, with command `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` and a 7200s timeout at `hooks/hooks.json:67`. The file intercepts exit attempts, validates RLCR state, blocks unsafe exits, runs Codex review, advances normal/review/finalize phases, and emits Claude hook JSON decisions.
- algorithmic_behavior: The hook reads hook input from stdin (`hooks/loop-codex-stop-hook.sh:28`), derives `PROJECT_ROOT`, locates `.humanize/rlcr`, extracts session id, and chooses the active loop via `find_active_loop` (`hooks/loop-codex-stop-hook.sh:42`, `hooks/loop-codex-stop-hook.sh:59`, `hooks/loop-codex-stop-hook.sh:61`). It resolves either `state.md` or `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:74`) and treats `finalize-state.md` as the finalize phase (`hooks/loop-codex-stop-hook.sh:80`). It parses state frontmatter, validates schema-critical fields, then applies a chain of quick gates before invoking Codex. In implementation phase it creates a review prompt and runs `codex exec` with configured model/effort/timeout (`hooks/loop-codex-stop-hook.sh:917`, `hooks/loop-codex-stop-hook.sh:930`, `hooks/loop-codex-stop-hook.sh:1307`). If Codex’s last non-empty review line is `COMPLETE`, it toggles `review_started: true`, creates `.review-phase-started`, and runs `codex review` (`hooks/loop-codex-stop-hook.sh:1423`, `hooks/loop-codex-stop-hook.sh:1457`, `hooks/loop-codex-stop-hook.sh:1464`, `hooks/loop-codex-stop-hook.sh:1469`). In review phase, `codex review` output is scanned for severity markers via `detect_review_issues` from `hooks/lib/loop-common.sh:468`; no issues advances to finalize (`hooks/loop-codex-stop-hook.sh:1059`). If Codex returns issues, the hook increments the round and writes a next-round prompt (`hooks/loop-codex-stop-hook.sh:1541`, `hooks/loop-codex-stop-hook.sh:1559`).
- inputs_outputs_state: Inputs are hook JSON stdin, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `round-N-summary.md`, plan backup, git repository state, Task/Todo transcript state, prompt templates, and Codex CLI availability. Key state fields are mapped from `STATE_*` values: `PLAN_TRACKED`, `START_BRANCH`, `BASE_BRANCH`, `BASE_COMMIT`, `PLAN_FILE`, `CURRENT_ROUND`, `MAX_ITERATIONS`, `PUSH_EVERY_ROUND`, `FULL_REVIEW_ROUND`, `REVIEW_STARTED`, Codex config, ask-question flag, and agent-teams flag (`hooks/loop-codex-stop-hook.sh:104`). Outputs include hook JSON with `decision: block` and a reason/system message for every blocking condition, debug files under `XDG_CACHE_HOME` or `$HOME/.cache` (`hooks/loop-codex-stop-hook.sh:903`), prompt/result files in the loop directory, state-file rewrites, and terminal state-file renames. Finalize completion renames `finalize-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:768`). Terminal abnormal endings use `end_loop`, which renames to `<reason>-state.md` in `hooks/lib/loop-common.sh:1231`.
- gates_or_invariants: Important invariants include valid Codex model/effort characters (`hooks/loop-codex-stop-hook.sh:123`), required raw `current_round` and `max_iterations` fields (`hooks/loop-codex-stop-hook.sh:136`), numeric `current_round` (`hooks/loop-codex-stop-hook.sh:150`), required `plan_tracked`, `start_branch`, `review_started`, and `base_branch` schema fields (`hooks/loop-codex-stop-hook.sh:166`, `hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:199`), branch consistency against `start_branch` (`hooks/loop-codex-stop-hook.sh:247`), plan backup/existence/diff integrity outside review phase (`hooks/loop-codex-stop-hook.sh:266`, `hooks/loop-codex-stop-hook.sh:319`), all tasks completed before exit (`hooks/loop-codex-stop-hook.sh:349`), fail-closed git status (`hooks/loop-codex-stop-hook.sh:428`), large-file block over 2000 lines for changed code/docs (`hooks/loop-codex-stop-hook.sh:457`), clean git worktree before review (`hooks/loop-codex-stop-hook.sh:541`), optional pushed-commits requirement when `push_every_round` is true (`hooks/loop-codex-stop-hook.sh:602`), required summary file (`hooks/loop-codex-stop-hook.sh:642`), initialized goal tracker on round 0 (`hooks/loop-codex-stop-hook.sh:678`), max-iteration stop outside review/finalize (`hooks/loop-codex-stop-hook.sh:756`), Codex CLI presence (`hooks/loop-codex-stop-hook.sh:882`), non-empty review result (`hooks/loop-codex-stop-hook.sh:1412`), review-phase marker file to prevent manual `review_started` toggles (`hooks/loop-codex-stop-hook.sh:1482`), and strict terminal markers where `COMPLETE` or `STOP` must be the final non-empty line (`hooks/loop-codex-stop-hook.sh:1423`).
- dependencies_and_callers: Called by Claude Code hook config. Depends on `hooks/lib/loop-common.sh` for state parsing, active-loop selection, markers, template helpers, issue detection, `end_loop`, and path utilities (`hooks/loop-codex-stop-hook.sh:47`); on `scripts/portable-timeout.sh` for `run_with_timeout` (`hooks/loop-codex-stop-hook.sh:51`); on `hooks/check-todos-from-transcript.py` for task completion (`hooks/loop-codex-stop-hook.sh:349`); on `jq`, `git`, `codex`, `sed`, `awk`, `grep`, `wc`, `cp`, `mv`; and on many templates under `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude`. The state schema is produced by `scripts/setup-rlcr-loop.sh`, which writes `current_round`, `max_iterations`, Codex config, `full_review_round`, plan fields, branch fields, `base_commit`, `review_started`, `ask_codex_question`, session id, and agent teams at `scripts/setup-rlcr-loop.sh:769`.
- edge_cases_or_failure_modes: No active loop or no active state file exits successfully without blocking (`hooks/loop-codex-stop-hook.sh:64`, `hooks/loop-codex-stop-hook.sh:75`). Missing/old/corrupt state schema can block or terminate as unexpected. Git command timeout/failure blocks before review (`hooks/loop-codex-stop-hook.sh:233`). Plan file deleted, uncommitted, or changed blocks outside review phase. Task checker parse errors block (`hooks/loop-codex-stop-hook.sh:356`). Stale `.git/index.lock` is removed before asking Claude to commit (`hooks/loop-codex-stop-hook.sh:409`). Missing summary file blocks. `full_review_round` missing only warns and defaults to 5 (`hooks/loop-codex-stop-hook.sh:219`). Invalid `full_review_round` below 2 defaults to 5 (`hooks/loop-codex-stop-hook.sh:802`). Codex failure, missing output file, copy failure, empty result, or empty review log is hard-blocking with debug paths (`hooks/loop-codex-stop-hook.sh:1320`, `hooks/loop-codex-stop-hook.sh:1383`, `hooks/loop-codex-stop-hook.sh:1411`, `hooks/loop-codex-stop-hook.sh:1201`). `STOP` triggers circuit-breaker handling and terminal state rename (`hooks/loop-codex-stop-hook.sh:1507`). Open questions can inject an AskUserQuestion notice when enabled (`hooks/loop-codex-stop-hook.sh:1564`). Agent-teams continuation text is appended only during implementation phase (`hooks/loop-codex-stop-hook.sh:1630`).
- validation_or_tests: Directly exercised by many hook tests found by reference, especially `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-agent-teams.sh`, and `tests/robustness/test-hook-system-robustness.sh`, which call `hooks/loop-codex-stop-hook.sh` under constructed loop states. State parsing behavior is covered by the assigned `tests/robustness/test-state-file-robustness.sh`. Terminal state naming is covered by `tests/test-state-exit-naming.sh`, whose header states expected terminal files at `tests/test-state-exit-naming.sh:6`. The aggregate test runner includes related suites at `tests/run-all-tests.sh:41`, `tests/run-all-tests.sh:45`, `tests/run-all-tests.sh:57`, and robustness suites including state-file robustness at `tests/run-all-tests.sh:59`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-056 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:gen-plan` command contract. It validates command metadata, companion relevance-agent metadata, package version consistency, naming/YAML/model/content constraints, and the `scripts/validate-gen-plan-io.sh` validator’s exit-code semantics.
- algorithmic_behavior: The script derives `PROJECT_ROOT`, `COMMANDS_DIR`, and `AGENTS_DIR` (`tests/test-gen-plan.sh:11`), maintains local pass/fail counters (`tests/test-gen-plan.sh:22`), then runs positive tests PT-1 through PT-9 and negative/validator tests. Positive tests assert the command file exists, has frontmatter description, allowed tools, argument hint, and that `agents/draft-relevance-checker.md` exists with expected `name`, `model: haiku`, and `tools` fields (`tests/test-gen-plan.sh:54`, `tests/test-gen-plan.sh:71`, `tests/test-gen-plan.sh:85`, `tests/test-gen-plan.sh:98`, `tests/test-gen-plan.sh:111`, `tests/test-gen-plan.sh:124`, `tests/test-gen-plan.sh:138`, `tests/test-gen-plan.sh:152`). It also checks `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and README versions agree (`tests/test-gen-plan.sh:164`). Negative tests create temporary invalid fixtures to prove validators reject uppercase/space names, missing frontmatter, malformed YAML, invalid models, emoji/CJK content, and invalid IO cases (`tests/test-gen-plan.sh:191`, `tests/test-gen-plan.sh:196`, `tests/test-gen-plan.sh:201`, `tests/test-gen-plan.sh:304`, `tests/test-gen-plan.sh:350`, `tests/test-gen-plan.sh:409`). The final section invokes `scripts/validate-gen-plan-io.sh` with invalid and valid argument combinations and asserts specific exit codes (`tests/test-gen-plan.sh:431`).
- inputs_outputs_state: Inputs are repository files `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and executable `scripts/validate-gen-plan-io.sh`. The script creates temporary fixture directories using `mktemp -d` and removes them with `trap` (`tests/test-gen-plan.sh:192`, `tests/test-gen-plan.sh:435`). Outputs are colored PASS/FAIL lines, final pass/fail counts, and process exit 0 if all assertions pass or 1 otherwise (`tests/test-gen-plan.sh:547`, `tests/test-gen-plan.sh:554`).
- gates_or_invariants: `gen-plan.md` must have YAML frontmatter with `description`, `allowed-tools`, and `argument-hint`. The relevance agent must be named exactly `draft-relevance-checker` and use `haiku` (`tests/test-gen-plan.sh:124`, `tests/test-gen-plan.sh:138`). Names must match `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:196`). Model names must be exact aliases `opus|sonnet|haiku` or full-model prefixes `claude-`, `gpt-`, `o<digit>`, or `gemini-` (`tests/test-gen-plan.sh:352`). The command and agent should avoid CJK/emoji according to the script’s `grep -P` checks (`tests/test-gen-plan.sh:410`, `tests/test-gen-plan.sh:418`). IO validator exit codes are part of the command contract: missing option value/unknown/help is 6, input not found is 1, empty input is 2, missing output directory is 3, existing output or directory output is 4, valid input/output is 0 (`tests/test-gen-plan.sh:438`, `tests/test-gen-plan.sh:474`, `tests/test-gen-plan.sh:483`, `tests/test-gen-plan.sh:493`, `tests/test-gen-plan.sh:503`, `tests/test-gen-plan.sh:513`, `tests/test-gen-plan.sh:523`, `tests/test-gen-plan.sh:532`).
- dependencies_and_callers: Included in the aggregate runner at `tests/run-all-tests.sh:52`. It validates `commands/gen-plan.md`, whose workflow calls `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` in Phase 1 and maps its exit codes at `commands/gen-plan.md:30`, then uses the draft relevance checker agent at `commands/gen-plan.md:50`. It directly executes `scripts/validate-gen-plan-io.sh`, whose documented exit codes are at `scripts/validate-gen-plan-io.sh:4` and whose template copy/draft append side effect is at `scripts/validate-gen-plan-io.sh:148`.
- edge_cases_or_failure_modes: The script uses `set -euo pipefail`, but most expected failures are guarded by `|| EXIT_CODE=$?`. It creates two separate temp directories and installs a second `trap`, so the later trap overwrites the earlier fixture cleanup trap; this is a test hygiene edge case, not an algorithmic behavior of the plugin. YAML syntax validation is a lightweight regex over extracted frontmatter rather than a full YAML parser (`tests/test-gen-plan.sh:304`). Emoji/CJK checks depend on `grep -P`; the command redirects errors and treats unsupported `-P` as no match, which can make the content check permissive on platforms without PCRE. The valid-path IO test calls the validator, which copies the plan template and writes an output file as a real side effect (`tests/test-gen-plan.sh:523`).
- validation_or_tests: This file is itself the test. It asserts `commands/gen-plan.md` frontmatter and workflow surface; validates `agents/draft-relevance-checker.md`; checks version consistency; runs fixture-based negative validations; and exercises the real IO validation script. It exits nonzero when `TESTS_FAILED` is nonzero (`tests/test-gen-plan.sh:554`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-086 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR bash validator’s git-push gate. It defines the user-facing transition when `git push` is attempted during an RLCR loop that was not started with per-round push enabled.
- algorithmic_behavior: The template is static markdown. It states that current commits should stay local and the loop will handle local commits until completion (`prompt-template/block/git-push.md:3`). It provides the enabling command flag: `/humanize:start-rlcr-loop plan.md --push-every-round` (`prompt-template/block/git-push.md:6`). It has no placeholders and no runtime branching.
- inputs_outputs_state: Input is the template lookup call from `hooks/loop-bash-validator.sh`. Output is stderr markdown shown when a blocked Bash command matches `git push` while `push_every_round` is not true. No repo state is modified by the template itself.
- gates_or_invariants: The gate is implemented in `hooks/loop-bash-validator.sh`: after strict RLCR state parsing (`hooks/loop-bash-validator.sh:81`), it reads `STATE_PUSH_EVERY_ROUND` (`hooks/loop-bash-validator.sh:94`); when that value is not true and the lowercased command matches `^[[:space:]]*git[[:space:]]+push`, it renders this template and exits 2 (`hooks/loop-bash-validator.sh:96`). The invariant is that default RLCR loops keep commits local; per-round remote push requires opt-in.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md"` in `hooks/loop-bash-validator.sh:103`. Template loading/rendering is provided by `hooks/lib/template-loader.sh`, which keeps missing placeholders literal and falls back if the template is absent (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:170`). `tests/test-template-loader.sh`, `tests/test-error-scenarios.sh`, and `tests/test-templates-comprehensive.sh` also reference this template for loader/template coverage.
- edge_cases_or_failure_modes: Because the template has no variables, rendering cannot corrupt dynamic content. If missing, the caller fallback says commits should stay local and to use `--push-every-round` (`hooks/loop-bash-validator.sh:99`). The command detection only covers commands beginning with `git push` after whitespace; more indirect push forms would be handled only if other validators catch them.
- validation_or_tests: Referenced by `tests/test-template-loader.sh`, which loads `block/git-push.md`; `tests/test-error-scenarios.sh`, which checks missing-template behavior; and `tests/test-templates-comprehensive.sh`, which renders it through `load_and_render_safe`. The actual push block behavior is tied to `loop-bash-validator.sh` and state parsing.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-116 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude continuation prompt template for normal implementation-phase RLCR rounds after Codex found issues. It defines the instruction payload that re-enters Claude with Codex feedback and read-only plan/goal-tracker context.
- algorithmic_behavior: The template tells Claude the work is unfinished, asks it to review the original implementation plan via `@{{PLAN_FILE}}`, requires Task system tracking for all discovered issues, embeds Codex review content between explicit markers, and instructs Claude to read but not directly modify `@{{GOAL_TRACKER_FILE}}` after round 0 (`prompt-template/claude/next-round-prompt.md:1`, `prompt-template/claude/next-round-prompt.md:5`, `prompt-template/claude/next-round-prompt.md:12`, `prompt-template/claude/next-round-prompt.md:16`, `prompt-template/claude/next-round-prompt.md:22`, `prompt-template/claude/next-round-prompt.md:30`). The stop hook renders it with `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` into `round-<next>-prompt.md` (`hooks/loop-codex-stop-hook.sh:1559`).
- inputs_outputs_state: Inputs are `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` from the active RLCR state and Codex review result. Output is a next-round prompt file in the loop directory, which is then returned as the hook block reason (`hooks/loop-codex-stop-hook.sh:1644`). The template itself does not mutate state; the hook updates `current_round` immediately before rendering (`hooks/loop-codex-stop-hook.sh:1541`).
- gates_or_invariants: It enforces that Claude must not only address the most important issues and must create tasks for all discovered issues (`prompt-template/claude/next-round-prompt.md:12`). It makes the goal tracker read-only after round 0 and requires Goal Tracker Update Requests instead of direct edits (`prompt-template/claude/next-round-prompt.md:30`). The stop hook can add an Open Question notice into this rendered prompt when review content contains an open-question marker and `ask_codex_question` is true (`hooks/loop-codex-stop-hook.sh:1564`), append post-alignment action items after full-alignment checks (`hooks/loop-codex-stop-hook.sh:1600`), append footer instructions (`hooks/loop-codex-stop-hook.sh:1608`), optionally append push instructions (`hooks/loop-codex-stop-hook.sh:1614`), append goal-tracker update request text (`hooks/loop-codex-stop-hook.sh:1623`), and append agent-team continuation instructions when enabled (`hooks/loop-codex-stop-hook.sh:1630`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` when implementation-phase Codex review did not end with `COMPLETE` or `STOP`. Rendering uses `hooks/lib/template-loader.sh`, whose substitution is single-pass to prevent placeholder injection from values such as review content (`hooks/lib/template-loader.sh:54`). It coordinates with other Claude templates appended after it: `claude/open-question-notice.md`, `claude/post-alignment-action-items.md`, `claude/next-round-footer.md`, `claude/push-every-round-note.md`, `claude/goal-tracker-update-request.md`, and `claude/agent-teams-continue.md`.
- edge_cases_or_failure_modes: If this template is missing or empty, the hook fallback is a smaller “Next Round Instructions” prompt with review content and plan/goal tracker references (`hooks/loop-codex-stop-hook.sh:1551`). Because `REVIEW_CONTENT` is embedded in a single-pass template renderer, any `{{...}}` patterns inside Codex output are not recursively substituted. Open-question insertion depends on the rendered markers being present; it uses an awk insertion around the Codex review end marker and following separator (`hooks/loop-codex-stop-hook.sh:1581`).
- validation_or_tests: Template reference coverage exists in `tests/test-template-references.sh` and broader template suites. Behavior is indirectly validated by stop-hook tests that assert next-round prompt generation and round advancement, especially `tests/test-finalize-phase.sh` and `tests/test-plan-file-hooks.sh` references to `loop-codex-stop-hook.sh`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-146 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It probes tolerant parsing (`parse_state_file`, `get_current_round`) and strict parsing (`parse_state_file_strict`) under valid, malformed, partial, missing, binary, CRLF, and newer-field cases.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` and shared test helpers (`tests/robustness/test-state-file-robustness.sh:16`), creates a temp test directory (`tests/robustness/test-state-file-robustness.sh:19`), writes many synthetic state files, calls parser functions, and asserts expected variables or failure modes. Positive tests cover normal full state files, `get_current_round`, extra ignored fields, quoted values, and round zero (`tests/robustness/test-state-file-robustness.sh:33`, `tests/robustness/test-state-file-robustness.sh:62`, `tests/robustness/test-state-file-robustness.sh:72`, `tests/robustness/test-state-file-robustness.sh:100`, `tests/robustness/test-state-file-robustness.sh:124`). Negative/edge tests cover missing YAML separators, non-numeric rounds, missing required fields, empty/partial/malformed files, large and negative numbers, paths with spaces, nonexistent files, binary trailing data, CRLF, and `full_review_round` behavior (`tests/robustness/test-state-file-robustness.sh:149`, `tests/robustness/test-state-file-robustness.sh:173`, `tests/robustness/test-state-file-robustness.sh:201`, `tests/robustness/test-state-file-robustness.sh:230`, `tests/robustness/test-state-file-robustness.sh:242`, `tests/robustness/test-state-file-robustness.sh:259`, `tests/robustness/test-state-file-robustness.sh:277`, `tests/robustness/test-state-file-robustness.sh:297`, `tests/robustness/test-state-file-robustness.sh:314`, `tests/robustness/test-state-file-robustness.sh:358`, `tests/robustness/test-state-file-robustness.sh:367`, `tests/robustness/test-state-file-robustness.sh:386`, `tests/robustness/test-state-file-robustness.sh:399`).
- inputs_outputs_state: Inputs are generated fixture files under `TEST_DIR`, plus parser implementations in `hooks/lib/loop-common.sh`. Outputs are PASS/FAIL counters via shared helpers and the final summary from `print_test_summary` (`tests/robustness/test-state-file-robustness.sh:480`). The test mutates only temporary files and exits with the summary status.
- gates_or_invariants: Strict parser must reject missing separators, missing required fields, non-numeric `current_round`, and non-numeric `max_iterations`; the implementation documents return codes in `hooks/lib/loop-common.sh:377` and enforces separators/required fields/numeric values at `hooks/lib/loop-common.sh:394`, `hooks/lib/loop-common.sh:406`, `hooks/lib/loop-common.sh:424`, and `hooks/lib/loop-common.sh:430`. It also requires `review_started` and `base_branch` in strict mode (`hooks/lib/loop-common.sh:415`, `hooks/lib/loop-common.sh:419`), but this assigned test’s older strict fixtures mostly focus on round/max/frontmatter. Tolerant parsing must default `current_round` to 0, `max_iterations` to 10, `push_every_round` to false, `full_review_round` to 5, `ask_codex_question` to true, and `agent_teams` to false (`hooks/lib/loop-common.sh:363`). Quoted string parsing should strip surrounding quotes for path/branch fields (`hooks/lib/loop-common.sh:315`). `full_review_round` should parse explicit 7, default to 5 when absent, and preserve minimum value 2 in tolerant parsing (`tests/robustness/test-state-file-robustness.sh:399`, `tests/robustness/test-state-file-robustness.sh:425`, `tests/robustness/test-state-file-robustness.sh:450`).
- dependencies_and_callers: Included in aggregate test runner at `tests/run-all-tests.sh:59`. Depends on shared helper functions `pass`, `fail`, `setup_test_dir`, and `print_test_summary` from `tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:58`, and `tests/test-helpers.sh:86`. It directly validates parsing functions consumed by `hooks/loop-codex-stop-hook.sh`, `loop-bash-validator.sh`, read/write/edit validators, monitors, and cancellation scripts.
- edge_cases_or_failure_modes: The test intentionally documents that tolerant parsing is permissive: missing frontmatter returns default round 0 (`tests/robustness/test-state-file-robustness.sh:163`), non-numeric current round “handles gracefully” without a strong value assertion (`tests/robustness/test-state-file-robustness.sh:190`), partial YAML may return either 0 or 5 (`tests/robustness/test-state-file-robustness.sh:251`), CRLF only must not crash (`tests/robustness/test-state-file-robustness.sh:391`), and negative round only must return some value (`tests/robustness/test-state-file-robustness.sh:307`). This means the executable spec protects crash-resistance more than full semantic rejection for tolerant helpers; strict parser is the fail-closed path for hook gates.
- validation_or_tests: This file is the validation. It specifically exercises `parse_state_file`, `parse_state_file_strict`, and `get_current_round`; the underlying parser code is in `hooks/lib/loop-common.sh:352`, `hooks/lib/loop-common.sh:386`, and `hooks/lib/loop-common.sh:296`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 (the five assigned IDs are present once each as the five `###` Item Evidence headings)
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`