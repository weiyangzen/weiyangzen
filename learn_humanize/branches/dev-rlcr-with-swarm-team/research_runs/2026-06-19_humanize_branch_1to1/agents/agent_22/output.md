# agent_22 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is a thin command wrapper around `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` and documents the user-visible loop protocol (`commands/start-rlcr-loop.md:1`, `commands/start-rlcr-loop.md:10`).
- algorithmic_behavior: Defines the staged RLCR lifecycle: implementation work, summary write, Codex review on attempted exit, issue feedback loop, `COMPLETE` transition into Review Phase, `codex review --base <branch>`, `[P0-9]` issue remediation, and Finalize Phase (`commands/start-rlcr-loop.md:16`, `commands/start-rlcr-loop.md:22`, `commands/start-rlcr-loop.md:25`). It also describes Goal Tracker semantics: immutable goal/acceptance criteria plus mutable tasks, deferrals, and plan evolution (`commands/start-rlcr-loop.md:27`, `commands/start-rlcr-loop.md:31`).
- inputs_outputs_state: Inputs are plan path or `--plan-file`, max rounds, Codex model/effort/timeout, plan tracking, push cadence, base branch, full-review interval, `--skip-impl`, `--claude-answer-codex`, and `--agent-teams` from the argument hint (`commands/start-rlcr-loop.md:3`). Output is activation of setup script (`commands/start-rlcr-loop.md:12`) which creates `.humanize/rlcr/<timestamp>/state.md`, goal tracker, prompts, and loop files in `scripts/setup-rlcr-loop.sh:725`, `scripts/setup-rlcr-loop.sh:767`.
- gates_or_invariants: Invariants documented here are: summaries must be written before exit; `goal-tracker.md` must stay current; the agent must not bypass loop state or cancel commands manually; exit only through max iterations, successful complete+review, or user cancel (`commands/start-rlcr-loop.md:46`, `commands/start-rlcr-loop.md:54`). Full alignment checks default every 5 rounds and can be configured, with minimum 2 (`commands/start-rlcr-loop.md:39`).
- dependencies_and_callers: Depends directly on `scripts/setup-rlcr-loop.sh` through the command body (`commands/start-rlcr-loop.md:12`). Runtime behavior coordinates with stop hook transitions in `hooks/loop-codex-stop-hook.sh`, especially strict `COMPLETE` handling and review phase gating (`hooks/loop-codex-stop-hook.sh:1429`, `hooks/loop-codex-stop-hook.sh:1482`). Cancel path is implemented by `scripts/cancel-rlcr-loop.sh`.
- edge_cases_or_failure_modes: Skip-implementation mode makes the plan optional and starts directly in code review (`commands/start-rlcr-loop.md:69`, `commands/start-rlcr-loop.md:77`). Base branch auto-detection falls back remote default, then local `main`, then `master` as described (`commands/start-rlcr-loop.md:67`) and implemented in `scripts/setup-rlcr-loop.sh:648`.
- validation_or_tests: This file is itself workflow documentation rather than an executable test. Its algorithmic claims are covered by setup/stop/cancel tests, including plan robustness and cancel signal specs assigned below.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-052 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancel authorization algorithm. It proves the only permitted state-file mutation during active loop protection is a signal-authorized rename from `state.md` to `cancel-state.md` (`tests/test-cancel-signal-file.sh:5`).
- algorithmic_behavior: The test creates isolated `.humanize/rlcr/<timestamp>/state.md` fixtures, exports `CLAUDE_PROJECT_DIR`, then feeds Bash hook JSON into `hooks/loop-bash-validator.sh` via `run_bash_validator` (`tests/test-cancel-signal-file.sh:42`, `tests/test-cancel-signal-file.sh:63`). Positive cases allow `mv <loop>/state.md <loop>/cancel-state.md` only when `<loop>/.cancel-requested` exists, including normalized `./` paths, quoted paths, and literal `${loop_dir}` helper syntax (`tests/test-cancel-signal-file.sh:80`, `tests/test-cancel-signal-file.sh:100`, `tests/test-cancel-signal-file.sh:365`, `tests/test-cancel-signal-file.sh:450`).
- inputs_outputs_state: Inputs are synthetic Bash commands encoded as hook JSON plus presence or absence of `.cancel-requested`. Expected outputs are validator exit `0` for authorized cancel and exit `2` for blocked mutation/security cases (`tests/test-cancel-signal-file.sh:90`, `tests/test-cancel-signal-file.sh:131`). State transition under test is protection-aware cancellation: active `state.md` may become `cancel-state.md` only under the exact signal and command form. The production cancel script creates the signal then renames active state at `scripts/cancel-rlcr-loop.sh:148` and `scripts/cancel-rlcr-loop.sh:155`.
- gates_or_invariants: The main invariant is exact command shape: signal file in the active loop directory, source exactly `state.md` or `finalize-state.md`, destination exactly `cancel-state.md`, no extra args, no shell chaining, no command substitution, no hidden variables, no symlink source. The implementation enforces these in `is_cancel_authorized` (`hooks/lib/loop-common.sh:652`, `hooks/lib/loop-common.sh:681`, `hooks/lib/loop-common.sh:691`, `hooks/lib/loop-common.sh:710`, `hooks/lib/loop-common.sh:794`, `hooks/lib/loop-common.sh:800`, `hooks/lib/loop-common.sh:815`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `to_lower` and `is_cancel_authorized` (`tests/test-cancel-signal-file.sh:33`). Calls `hooks/loop-bash-validator.sh`, whose RLCR protection path checks active loop state, blocks state mutations, and delegates cancel exceptions to `is_cancel_authorized` (`hooks/loop-bash-validator.sh:77`, `hooks/loop-bash-validator.sh:139`, `hooks/loop-bash-validator.sh:149`).
- edge_cases_or_failure_modes: Covers missing signal, signal in wrong directory, `echo`/`sed`/`rm` state mutations, wrong destination, arbitrary destination, command injection through `&&`, `;`, newlines, `$()`, backticks, `${IFS}`, extra arguments before/after state path, `sudo`/`env`/`command` prefixes, chained commands, pipes, background operator, `sh -c`/`bash -c`, redirection-prefixed commands, quoted/escaped/ANSI-C redirection targets, unrelated `/tmp/state.md`, and no active loop (`tests/test-cancel-signal-file.sh:121`, `tests/test-cancel-signal-file.sh:201`, `tests/test-cancel-signal-file.sh:243`, `tests/test-cancel-signal-file.sh:303`, `tests/test-cancel-signal-file.sh:533`, `tests/test-cancel-signal-file.sh:552`, `tests/test-cancel-signal-file.sh:652`, `tests/test-cancel-signal-file.sh:812`, `tests/test-cancel-signal-file.sh:852`, `tests/test-cancel-signal-file.sh:1212`, `tests/test-cancel-signal-file.sh:1233`).
- validation_or_tests: This is the test. It maintains pass/fail counters and exits with `TESTS_FAILED` (`tests/test-cancel-signal-file.sh:22`, `tests/test-cancel-signal-file.sh:1357`). It also directly validates the helper’s true/false behavior in seven helper tests (`tests/test-cancel-signal-file.sh:1252`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-082 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for a commit hygiene gate that prevents `.humanize/` loop state from entering version control (`prompt-template/block/git-add-humanize.md:1`, `prompt-template/block/git-add-humanize.md:3`).
- algorithmic_behavior: The template explains why a Bash command was blocked, enumerates safe narrow staging patterns, and documents broad or direct staging commands that are unsafe when `.humanize` exists (`prompt-template/block/git-add-humanize.md:8`, `prompt-template/block/git-add-humanize.md:16`). It is rendered by `git_add_humanize_blocked_message` (`hooks/lib/loop-common.sh:1147`) when `git_adds_humanize` returns true.
- inputs_outputs_state: Input is a detected `git add` command that targets `.humanize` or would sweep it into staging. Output is the rendered block text to stderr and validator exit `2` from `hooks/loop-bash-validator.sh:115`. It does not mutate state; it preserves `.humanize/` as local runtime state.
- gates_or_invariants: Invariant: `.humanize/` must not be committed, even if the user uses force or broad staging. The detector handles direct `.humanize` references, `git add -A/--all`, `git add -f .`, force+all flags, and broad scope when `.humanize` is not ignored (`hooks/lib/loop-common.sh:1027`, `hooks/lib/loop-common.sh:1086`, `hooks/lib/loop-common.sh:1094`, `hooks/lib/loop-common.sh:1117`, `hooks/lib/loop-common.sh:1124`). The template also warns that a commit message containing literal `.humanize` can retrigger protection (`prompt-template/block/git-add-humanize.md:34`).
- dependencies_and_callers: Called only through `hooks/loop-bash-validator.sh` after command parsing and active-loop checks (`hooks/loop-bash-validator.sh:109`, `hooks/loop-bash-validator.sh:115`). Template loading flows through `load_and_render_safe` in `hooks/lib/loop-common.sh:1184`, with inline fallback content if the template is unavailable.
- edge_cases_or_failure_modes: The helper lowercases command input, so flags like `-A` become `-a`; it explicitly accounts for that (`hooks/lib/loop-common.sh:1031`). It splits chained commands on shell operators to catch `cd repo && git add .humanize` (`hooks/lib/loop-common.sh:1052`). It avoids overblocking names like `.humanizeconfig` by requiring path boundary matches (`hooks/lib/loop-common.sh:1086`).
- validation_or_tests: Not a test itself. Behavior is likely covered by Bash validator pattern tests and allowlist/validator tests referenced in repo search, while the template’s contract is exercised by `git_add_humanize_blocked_message`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-112 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Prompt contract for the RLCR Finalize Phase after implementation is complete and Codex review has passed (`prompt-template/claude/finalize-phase-prompt.md:1`, `prompt-template/claude/finalize-phase-prompt.md:3`).
- algorithmic_behavior: Directs the agent to run the `code-simplifier:code-simplifier` subagent, simplify/refactor recent changes, preserve functionality, run tests, commit, and write `finalize-summary.md` (`prompt-template/claude/finalize-phase-prompt.md:7`, `prompt-template/claude/finalize-phase-prompt.md:16`, `prompt-template/claude/finalize-phase-prompt.md:41`). It uses template variables for base branch, start branch, original plan, goal tracker, and finalize summary file (`prompt-template/claude/finalize-phase-prompt.md:29`, `prompt-template/claude/finalize-phase-prompt.md:38`, `prompt-template/claude/finalize-phase-prompt.md:45`).
- inputs_outputs_state: Inputs are rendered variables `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`. Output is a block decision reason from the stop hook that keeps the agent in-session while `state.md` has been renamed to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:1065`, `hooks/loop-codex-stop-hook.sh:1071`, `hooks/loop-codex-stop-hook.sh:1132`).
- gates_or_invariants: Non-negotiable invariants: no functional behavior changes, no failing existing tests, no new bugs, and only functionality-equivalent cleanup (`prompt-template/claude/finalize-phase-prompt.md:16`). Completion gate requires all tasks completed, changes committed, and finalize summary written (`prompt-template/claude/finalize-phase-prompt.md:41`). Stop hook then expects `finalize-summary.md` and, once checks pass, renames `finalize-state.md` to `complete-state.md` without another Codex review (`hooks/loop-codex-stop-hook.sh:635`, `hooks/loop-codex-stop-hook.sh:768`, `hooks/loop-codex-stop-hook.sh:771`).
- dependencies_and_callers: Rendered by `enter_finalize_phase` after code review passes with no `[P0-9]` issues (`hooks/loop-codex-stop-hook.sh:1046`, `hooks/loop-codex-stop-hook.sh:1059`, `hooks/loop-codex-stop-hook.sh:1061`). Normal COMPLETE transitions first enter review phase and create `.review-phase-started`; only absence of review issues proceeds to this prompt (`hooks/loop-codex-stop-hook.sh:1429`, `hooks/loop-codex-stop-hook.sh:1456`, `hooks/loop-codex-stop-hook.sh:1469`, `hooks/loop-codex-stop-hook.sh:1504`).
- edge_cases_or_failure_modes: If code review is skipped, a separate skipped prompt is used instead of this file (`hooks/loop-codex-stop-hook.sh:1077`, `prompt-template/claude/finalize-phase-skipped-prompt.md:1`). If `review_started=true` lacks `.review-phase-started`, the stop hook blocks as inconsistent manual state tampering (`hooks/loop-codex-stop-hook.sh:1482`).
- validation_or_tests: Covered by finalize phase tests referenced in `tests/test-finalize-phase.sh`, including state transition to `finalize-state.md`, blocking state edits, summary requirement, and completion to `complete-state.md`. The assigned template itself is a prompt surface, not executable code.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-142 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `scripts/setup-rlcr-loop.sh` plan-file validation. It verifies accepted and rejected plan file shapes before a loop can start (`tests/robustness/test-plan-file-robustness.sh:3`, `tests/robustness/test-plan-file-robustness.sh:46`).
- algorithmic_behavior: Creates an isolated git repo, injects a mock `codex` binary to avoid real Codex calls, then invokes the production setup script with different plan paths (`tests/robustness/test-plan-file-robustness.sh:27`, `tests/robustness/test-plan-file-robustness.sh:61`, `tests/robustness/test-plan-file-robustness.sh:112`). `test_plan_validation` classifies setup output as validation failure when it sees known plan errors, or success when setup reaches Codex availability, gitignore policy, activation output, or created loop state (`tests/robustness/test-plan-file-robustness.sh:64`, `tests/robustness/test-plan-file-robustness.sh:70`, `tests/robustness/test-plan-file-robustness.sh:82`, `tests/robustness/test-plan-file-robustness.sh:88`).
- inputs_outputs_state: Inputs are generated plan files: valid markdown, mostly comments with minimum content, 5KB standard file, rich markdown, empty/comment-only files, 1MB+ file, mixed line endings, binary/null content, long lines, special shell characters, whitespace-only files, nested comments, missing file, unreadable file, symlink, directory, and file disappearance (`tests/robustness/test-plan-file-robustness.sh:178`, `tests/robustness/test-plan-file-robustness.sh:300`, `tests/robustness/test-plan-file-robustness.sh:325`, `tests/robustness/test-plan-file-robustness.sh:355`, `tests/robustness/test-plan-file-robustness.sh:491`, `tests/robustness/test-plan-file-robustness.sh:527`). Outputs are pass/fail records and final test summary exit code (`tests/robustness/test-plan-file-robustness.sh:557`).
- gates_or_invariants: Production setup rejects missing/absolute/space/metacharacter/symlink/escaping paths, missing or unreadable files, plan files outside project, submodule-contained files, tracked plans unless `--track-plan-file`, files under 5 lines, and plans with fewer than 3 content lines (`scripts/setup-rlcr-loop.sh:356`, `scripts/setup-rlcr-loop.sh:370`, `scripts/setup-rlcr-loop.sh:382`, `scripts/setup-rlcr-loop.sh:418`, `scripts/setup-rlcr-loop.sh:430`, `scripts/setup-rlcr-loop.sh:444`, `scripts/setup-rlcr-loop.sh:499`, `scripts/setup-rlcr-loop.sh:519`, `scripts/setup-rlcr-loop.sh:567`). The test mirrors content-line semantics with `count_content_lines`: skip blank lines, `#` comments, single-line and multi-line HTML comments (`tests/robustness/test-plan-file-robustness.sh:129`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, mock `codex`, git, and `tests/test-helpers.sh` (`tests/robustness/test-plan-file-robustness.sh:15`, `tests/robustness/test-plan-file-robustness.sh:108`). The production setup writes loop state if validation passes (`scripts/setup-rlcr-loop.sh:725`, `scripts/setup-rlcr-loop.sh:767`) and reports activation (`scripts/setup-rlcr-loop.sh:1099`).
- edge_cases_or_failure_modes: Large file handling specifically guards prior SIGPIPE-style failures in output pipelines (`tests/robustness/test-plan-file-robustness.sh:325`). Binary/null/long-line cases mostly assert shell tools can still count/read lines rather than fully invoking production validation for every case (`tests/robustness/test-plan-file-robustness.sh:368`, `tests/robustness/test-plan-file-robustness.sh:390`, `tests/robustness/test-plan-file-robustness.sh:514`). Permission checks tolerate root-like environments where unreadable files still appear readable (`tests/robustness/test-plan-file-robustness.sh:477`).
- validation_or_tests: This file is the validation suite. It exits with `print_test_summary "Plan File Robustness Test Summary"` and the helper-provided status (`tests/robustness/test-plan-file-robustness.sh:557`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 item sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`