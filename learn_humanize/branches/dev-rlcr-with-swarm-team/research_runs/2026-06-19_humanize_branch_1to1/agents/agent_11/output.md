# agent_11 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt fragment library for the RLCR loop. These files define the instructions injected into Claude during implementation continuation, agent-team mode, review remediation, finalize/simplification, goal-tracker update requests, push requirements, and open-question handling.
- algorithmic_behavior: The directory is flat and contains 11 Markdown fragments: `agent-teams-continue.md`, `agent-teams-instructions.md`, `finalize-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `goal-tracker-update-request.md`, `next-round-footer.md`, `next-round-prompt.md`, `open-question-notice.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, and `review-phase-prompt.md`. `scripts/setup-rlcr-loop.sh:1002-1008` appends `claude/agent-teams-instructions.md` to round-0 prompts when agent teams are enabled. `hooks/loop-codex-stop-hook.sh:1104-1137` renders finalize prompts, `1183-1185` renders review-phase prompts, `1559-1562` renders next-round prompts, `1577-1596` injects open-question notices, `1602-1605` appends post-alignment action items, `1611-1620` appends footer and push notes, `1624-1628` appends goal-tracker update request guidance, and `1630-1637` appends agent-team continuation instructions.
- inputs_outputs_state: Inputs are template variables rendered by `hooks/lib/template-loader.sh`, especially `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_CONTENT`, `NEXT_SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, and `REVIEW_SKIP_REASON`. Outputs are prompt files such as `round-N-prompt.md` or hook JSON block reasons. State transitions are indirect: `next-round-prompt.md` instructs continued implementation after Codex feedback; `review-phase-prompt.md` keeps Claude in review remediation; `finalize-phase-prompt.md` moves passed work into simplification; `finalize-phase-skipped-prompt.md` allows finalize after skipped review with manual verification; `open-question-notice.md` changes the expected action order by requiring `AskUserQuestion` before other fixes.
- gates_or_invariants: `agent-teams-instructions.md:7-13` makes the Claude session a team leader and forbids direct implementation work in that mode. `agent-teams-instructions.md:16-21` imposes file ownership and conflict-prevention rules. `finalize-phase-prompt.md:16-23` and `finalize-phase-skipped-prompt.md:26-33` require functionality-equivalent cleanup, no test failures, and no new bugs. `next-round-footer.md:4-9` forbids lying, state-file edits, or cancellation as an exit path and requires commit plus summary. `review-phase-prompt.md:11-14` requires all severity-marked findings to be fixed and summarized. `next-round-prompt.md:12-14` requires Task system tracking for all discovered issues rather than cherry-picking one issue.
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh` for iterative RLCR control. Initial agent-team insertion is done by `scripts/setup-rlcr-loop.sh`. Rendering depends on `hooks/lib/template-loader.sh:58-132` single-pass `{{VAR}}` replacement and `load_and_render_safe` fallback behavior at `170-203`. The directory coordinates with sibling `prompt-template/codex` templates: Codex produces review/alignment content, then these Claude templates tell Claude how to react. It also coordinates with `prompt-template/block` templates for hard validator failures and with loop state files under `.humanize/rlcr`.
- edge_cases_or_failure_modes: Missing templates are intended to fall back where `load_and_render_safe` is used; direct `load_template` consumers provide manual fallback strings for optional append-only fragments. Placeholder injection from `REVIEW_CONTENT` is mitigated by the single-pass loader design described in `hooks/lib/template-loader.sh:54-57`. Agent-team continuation is gated out during review phase by `hooks/loop-codex-stop-hook.sh:1630-1631`, preventing review-remediation prompts from incorrectly delegating. If `ASK_CODEX_QUESTION` is enabled, the open-question detector only triggers on lines containing `Open Question` shorter than 40 chars at `1564-1573`, so differently formatted open questions may not inject the notice.
- validation_or_tests: `tests/test-template-references.sh:56-111` scans shell template references and fails if referenced files are missing; its common-template cross-check and safe-rendering checks appear at `149-201`. `tests/test-agent-teams.sh` validates the existence and use of `agent-teams-instructions.md` and `agent-teams-continue.md`, including implementation-phase inclusion and review-phase exclusion. `tests/run-all-tests.sh:32-57` includes `test-template-loader.sh`, `test-template-references.sh`, `test-finalize-phase.sh`, and `test-agent-teams.sh` in the suite order.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-041 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for `/humanize:start-pr-loop`. It validates environment and options, identifies the active PR, resolves fork/upstream context, initializes `.humanize/pr-loop/<timestamp>/` state, fetches existing PR feedback, optionally posts bot trigger comments, verifies Claude reaction, creates the PR-loop goal tracker, and emits the round-0 prompt plus completion requirements.
- algorithmic_behavior: Argument parsing at `scripts/setup-pr-loop.sh:111-174` accepts `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout`, including numeric validation and `MODEL:EFFORT` parsing. Mutual exclusion at `220-240` blocks setup if either RLCR or PR loop is active. Prerequisites at `243-277` require a git repo with HEAD, `gh` installed and authenticated, and `codex` available. PR discovery and fork support at `283-379` derive the branch, current repo, parent repo, PR number, PR URL repo, PR state, and base repo for later API calls. Startup review status is fetched at `437-448`, with a JSON default case if the reviewer-status helper fails. Case 4 or 5 handling at `462-603` reuses an existing trigger comment mentioning all active bots after latest commit, or posts a fresh trigger and verifies Claude eyes reaction when Claude is monitored. State is written at `615-635`; goal tracker at `641-713`; initial prompt and critical requirements at `721-942`; final output at `860-945`.
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR`, git state, GitHub CLI responses, active bot set, helper script outputs, and templates under `prompt-template/pr-loop`. Outputs include `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-pr-resolve.md` path expectation, and `round-0-prompt.md`. State fields include `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, `codex_timeout`, polling settings, `startup_case`, latest commit metadata, `last_trigger_at`, and `trigger_comment_id`.
- gates_or_invariants: At least one bot flag is mandatory at `180-191`. Only one loop type can run at a time at `220-240`. PR must exist, be numeric, and not be merged/closed at `351-374`. YAML safety gates reject branch names containing `:`, `#`, quotes, backticks, or newline at `384-390`; model and effort names are restricted to safe character sets at `393-407`. Case 4/5 trigger posting aborts and removes the loop directory if GitHub comment posting fails at `525-538`. Claude monitoring requires a trigger comment id and an eyes reaction; both failures clean up the loop directory and exit at `568-599`. Initial prompts branch on the fetched comment sentinel `*No comments found.*` at `723-729`, which controls whether the round starts as wait-for-review or fix-comments mode.
- dependencies_and_callers: Called by `commands/start-pr-loop.md:1-14`, whose allowed tool is the setup script. It sources `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/loop-common.sh` at `33-43`. It calls `scripts/fetch-pr-comments.sh` at `430-431`, `scripts/check-pr-reviewer-status.sh` at `437-438`, and `scripts/check-bot-reactions.sh` at `587`. It uses `build_bot_mention_string` and `build_yaml_list` from `hooks/lib/loop-common.sh:874-895`, and active-loop detection from `find_active_loop` / `find_active_pr_loop` at `hooks/lib/loop-common.sh:233-256` and `917-930`. The stop-hook consumer is `hooks/pr-loop-stop-hook.sh`, which later reads this state and round files.
- edge_cases_or_failure_modes: Fork PR lookup is handled by trying the current repo, parsing the PR URL to avoid assuming repo identity, and using `FORK_OWNER:BRANCH` against the parent repo at `334-348`. Existing trigger detection strips fenced code blocks, tilde code blocks, inline code, indented code, and quoted lines before mention matching at `481-498`, avoiding false trigger reuse when mentions are not actionable. Mention matching requires all active bot patterns, so partial prior mentions lead to a new trigger. The script intentionally does not fall back to local time when GitHub timestamp retrieval fails at `558-561` to avoid clock-skew filtering bugs. If `check-pr-reviewer-status.sh` fails, the script defaults to startup case 1 at `437-441`, which may under-trigger re-review but preserves initialization.
- validation_or_tests: `tests/test-pr-loop-system.sh:1384-1447` tests the Case 4/5 `--claude` failure path when `trigger_comment_id` cannot be retrieved and verifies cleanup. `tests/test-pr-loop-system.sh:1453-1535` tests goal-tracker creation through `setup-pr-loop.sh`. `tests/robustness/test-setup-scripts-robustness.sh:475-507` covers help, missing bot flags, unknown options, and invalid `--max`. `tests/test-pr-loop-scripts.sh` and `tests/test-pr-loop.sh` include broader setup-script coverage. I did not execute this setup script because it writes `.humanize/pr-loop` state and requires external GitHub/Codex environment.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-071 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`. It defines expected behavior for locating the template root, loading existing/missing templates, rendering placeholders, fallback rendering, directory validation, literal handling of shell metacharacters, placeholder-injection prevention, multiline values, empty values, Unicode, and variable-name edge cases.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12`, tracks pass/fail counters at `20-34`, then runs 41 numbered checks. Tests 1-14 validate baseline loader behavior: `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` at `41-244`. Tests 15-33 assert literal rendering of shell-sensitive values such as `&`, backslashes, `$`, backticks, pipes, semicolons, glob syntax, parentheses, redirection syntax, quotes, hashes, tildes, combined metacharacters, regex text, JSON-like content, and multiline values at `247-486`. Tests 34-36 prove single-pass substitution by ensuring placeholders embedded inside variable values are not recursively replaced at `488-543`. Tests 37-41 cover empty substitutions, ASCII and non-ASCII template/value content, underscore-prefixed variable names, and numbered variable names at `545-639`. The final summary exits nonzero if any test fails at `641-659`.
- inputs_outputs_state: Inputs are the repository-local `prompt-template` directory and the loader functions. Outputs are colored PASS/FAIL lines and process exit status. The test does not maintain persistent repository state by design; its state is in shell counters `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: Missing template loading must return empty content, while `load_and_render_safe` must use fallback content for missing or empty templates. Unreplaced variables must remain as placeholders, unused variables must be ignored, and variable values containing `{{OTHER_VAR}}` must not be rescanned. Literal metacharacter preservation is a security invariant because prompt content can include review text, shell snippets, JSON, paths, and user-authored code references.
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`. It references real templates including `block/git-push.md` at `59-64` and `block/wrong-round-number.md` at `151-163`. It is part of the aggregate suite in `tests/run-all-tests.sh:32-35`. The loader itself is consumed by the RLCR and PR-loop setup/hooks, including `scripts/setup-pr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: The test is sensitive to shell environment writability because `render_template` uses a Bash here-string into `awk` at `hooks/lib/template-loader.sh:74-129`; in this managed read-only sandbox, Bash reported `cannot create temp file for here document: Operation not permitted`, causing render-related tests to fail even though file inspection succeeded. It also exposed that calling `load_and_render_safe` with no template variables can trip `env_vars[@]: unbound variable` in this sandbox path, which is relevant to empty-argument rendering behavior.
- validation_or_tests: I ran `bash tests/test-template-loader.sh` read-only. Result in this environment: 5 passed, 38 failed. The failures were dominated by the sandbox error `cannot create temp file for here document: Operation not permitted` at `hooks/lib/template-loader.sh:129`, plus one `env_vars[@]: unbound variable` case for an existing template with no variables. Because the repository export is read-only, this run is useful as environment evidence but not a clean product validation run.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-101 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Bash validator when a model tries to write `round-N-summary.md` using shell commands instead of Claude Write/Edit tools. It is a guardrail prompt, not an algorithm implementation itself, but it defines an enforcement contract used by the algorithm.
- algorithmic_behavior: The template states that Bash writes to summary files are blocked, renders the correct destination via `{{CORRECT_PATH}}`, and explains that commands like `cat`, `echo`, `sed`, and `awk` bypass validation hooks. It is loaded through `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:600-609`, which supplies `CORRECT_PATH`.
- inputs_outputs_state: Input is the correct active round summary path. Output is a rendered error message emitted to stderr by `hooks/loop-bash-validator.sh` when a prohibited command targets `round-[0-9]+-summary.md`. It does not mutate state; it prevents state corruption by forcing summary writes through tool-level validators.
- gates_or_invariants: The gate is triggered by `hooks/loop-bash-validator.sh:370-379`. When `command_modifies_file` detects a Bash command modifying a round summary file, the validator computes `ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`, renders this template, and exits with code 2. The invariant is that summary files must be written through Write/Edit so round-number and location validation still apply.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` via `load_and_render_safe`; caller chain is `loop-bash-validator.sh` -> `summary_bash_blocked_message` -> `prompt-template/block/summary-bash-write.md`. `tests/test-template-references.sh:152-159` explicitly includes `block/summary-bash-write.md` in common-template existence checks.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `summary_bash_blocked_message` has a fallback at `hooks/lib/loop-common.sh:604-608`, preserving the block but with less detailed wording. The Bash detector protects only patterns it recognizes as modifying `round-N-summary.md`; command parsing blind spots in `command_modifies_file` would be upstream failure modes. The rendered `CORRECT_PATH` must be accurate for the active loop; otherwise the user may be instructed to write the wrong round summary.
- validation_or_tests: Covered by template reference validation in `tests/test-template-references.sh:152-166` and by Bash validator behavior tests that search for summary blocking paths. The assigned `tests/test-template-loader.sh` also indirectly validates real block-template loading via `block/git-push.md` and `block/wrong-round-number.md`; I inspected this specific file directly at lines `1-8`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-131 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 task template used when initial fetched PR comments exist. It defines the first actionable state for Claude: analyze human and bot comments, fix issues, commit, push, trigger bot re-review, and write a resolution summary to the expected `round-0-pr-resolve.md` path.
- algorithmic_behavior: The task prioritizes human comments first and bot comments newest-first at `prompt-template/pr-loop/round-0-task-has-comments.md:6-8`. It requires reading relevant code, making changes, and adding tests if needed at `10-13`. It then requires commit, push, and a `gh pr comment` command using `{{PR_NUMBER}}` and `{{BOT_MENTION_STRING}}` at `15-21`. It requires a resolution summary at `23-26`, and states system-managed file and re-review rules at `30-35`. The final note at `39-43` tells Claude that the stop hook will poll, run local Codex validation, continue if issues remain, and end when all bots approve.
- inputs_outputs_state: Inputs are `PR_NUMBER`, `BOT_MENTION_STRING`, `RESOLVE_PATH`, `START_BRANCH`, and `ACTIVE_BOTS_DISPLAY`, supplied by `scripts/setup-pr-loop.sh:731-738`. Output is appended into `.humanize/pr-loop/<timestamp>/round-0-prompt.md` at `scripts/setup-pr-loop.sh:847-851` when `COMMENT_COUNT` is nonzero. The expected human/model output is a pushed commit, a PR trigger comment, and a resolution file at `RESOLVE_PATH`.
- gates_or_invariants: This template is selected only when `scripts/setup-pr-loop.sh:723-729` does not find the exact no-comments sentinel in the fetched comment file. It forbids modifying `.humanize/pr-loop` state files at line `32`, requires push before review can happen at line `33`, and requires the correct bot mention format at line `34`. Its summary path is later guarded by PR-loop validators: `hooks/loop-write-validator.sh:92-96` validates correct-round `round-N-pr-resolve.md` writes, and `tests/test-pr-loop-hooks.sh:1431-1494` checks wrong-round blocked vs correct-round allowed behavior.
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh:766-848`, which chooses between `round-0-task-no-comments.md` and this file. It coordinates with sibling PR-loop templates: `round-0-header.md` provides the prompt header, `critical-requirements-has-comments.md` reinforces completion obligations, and `goal-tracker-initial.md` initializes tracking. Downstream behavior is handled by `hooks/pr-loop-stop-hook.sh`, which polls for reviews and updates PR-loop state after the summary is written.
- edge_cases_or_failure_modes: The comment/no-comment decision is binary: any content other than the exact `*No comments found.*` sentinel sets `COMMENT_COUNT=1`, so malformed fetch output or warning text could select the “has comments” path. The embedded `gh pr comment` command assumes the current GitHub CLI context can resolve the PR; setup itself uses resolved repo context for API calls, but the prompt command shown here does not include `--repo`. If `BOT_MENTION_STRING` contains multiple bots, the rendered command triggers all active reviewers at once, which is intended but can spam if prior trigger reuse failed upstream.
- validation_or_tests: Directly referenced by `scripts/setup-pr-loop.sh:847`. Template existence is covered by prompt-template inventory and PR-loop setup tests. `tests/test-pr-loop-system.sh:1453-1535` exercises setup-generated PR-loop artifacts, while `tests/test-pr-loop-hooks.sh:1431-1525` validates resolution-summary round gates that this template directs Claude to satisfy. I inspected the file directly at lines `1-43`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 Item Evidence sections present; each assigned item appears once as an evidence heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`