# agent_20 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for transforming a draft document into a structured implementation plan. It defines the orchestration contract rather than shell logic: validate IO, screen repository relevance, analyze draft quality, resolve ambiguities, generate plan sections, and write/review the output.
- algorithmic_behavior: The command is a six-phase pipeline at `commands/gen-plan.md:19`: IO validation, relevance check, draft analysis, issue resolution, plan generation, and write/complete. It delegates initial path checks to `scripts/validate-gen-plan-io.sh` via `${CLAUDE_PLUGIN_ROOT}` at `commands/gen-plan.md:30-36`, then invokes `humanize:draft-relevance-checker` with `model: "haiku"` at `commands/gen-plan.md:56-65`. Plan synthesis is constrained by a fixed schema: goal, ACs, path boundaries, feasibility hints, dependency sequence, and code style requirements at `commands/gen-plan.md:146-223`.
- inputs_outputs_state: Inputs are `$ARGUMENTS` containing `--input <draft.md> --output <plan.md>`, the draft content, repository context gathered by Read/Glob/Grep/Task, and user answers collected through `AskUserQuestion`. Output is a new plan file initialized by the validator/template and then updated through Edit. The validator copies `prompt-template/plan/gen-plan-template.md` and appends the original draft at `scripts/validate-gen-plan-io.sh:148-157`; the command later requires the original draft section to remain intact at `commands/gen-plan.md:255-260`.
- gates_or_invariants: Exit-code gate from validation is explicit: 0 continues, 1 input missing, 2 empty input, 3 missing output dir, 4 output exists or is directory, 5 no write permission, 6 invalid args, 7 missing template at `commands/gen-plan.md:38-47`. Relevance gate stops on `NOT_RELEVANT` at `commands/gen-plan.md:67-72`. The generated plan must use AC-X or AC-X.Y, include positive and negative tests for each AC, omit time estimates and line numbers, preserve all draft information, and keep implementation code free of plan-progress terms at `commands/gen-plan.md:225-247`.
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, `prompt-template/plan/gen-plan-template.md`, and `agents/draft-relevance-checker.md`. The validator’s parser and side effects are tested in `tests/test-gen-plan.sh:426-541`. README exposes `/humanize:gen-plan --input ... --output ...` as user-facing workflow, while command frontmatter allows Bash validation, Read, Glob, Grep, Task, Write, and AskUserQuestion at `commands/gen-plan.md:4-12`.
- edge_cases_or_failure_modes: Known failure modes are invalid/missing args, empty input, output directory absence, output collision, permission failure, missing template, unrelated drafts, unresolved ambiguity, mixed-language output needing user confirmation, and incomplete generation. The command explicitly distinguishes hard numeric requirements from optimization trends before writing ACs at `commands/gen-plan.md:126-139`.
- validation_or_tests: `tests/test-gen-plan.sh` verifies command structure/frontmatter and validates `validate-gen-plan-io.sh` exit codes for missing flag values, unknown options, missing input, empty input, absent output dir, existing output, directory output, valid paths, and help at `tests/test-gen-plan.sh:438-538`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-050 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI stripping and pass/fail count extraction used by the aggregate test runner. It protects the result parser from GNU/BSD sed differences and colored test output.
- algorithmic_behavior: The script sets counters, defines `pass`/`fail`, then runs eight deterministic parsing cases. Core transform is `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"` at `tests/test-ansi-parsing.sh:45-47`; counts are extracted with `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed...`, then `grep -oE '[0-9]+$'` at `tests/test-ansi-parsing.sh:74-90`.
- inputs_outputs_state: Inputs are synthetic strings containing ANSI color sequences, bold/color combinations, multiline summary text, zero-count text, and plain text. Outputs are PASS/FAIL lines and final colored totals at `tests/test-ansi-parsing.sh:163-178`. State is only local shell counters `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: ANSI stripping must preserve visible text while removing SGR color/bold codes; parsing must work with multiple colors in one line, multiline summaries, zero values, and no ANSI codes. Final exit is 0 only when `TESTS_FAILED` is 0 at `tests/test-ansi-parsing.sh:170-178`.
- dependencies_and_callers: Directly mirrors `tests/run-all-tests.sh:124-128`, where each suite’s output is stripped before extracting `Passed:` and `Failed:` totals. It is included in the aggregate suite list at `tests/run-all-tests.sh:43`.
- edge_cases_or_failure_modes: Covers BSD/macOS portability by using ANSI-C quoted escape rather than sed `\x1b`; covers combined style code `\033[1;32m`, multiple SGR spans, multiline output with final summary selection via `tail -1`, and plain text. It does not cover non-SGR ANSI controls or lowercase labels, so its contract is intentionally scoped to SGR color output.
- validation_or_tests: Self-validating shell test with eight cases at `tests/test-ansi-parsing.sh:41-158`; aggregate runner consumes its summary and would fail if ANSI codes prevented numeric extraction.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-080 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for the Finalize Phase state-file protection invariant. It tells the agent that `finalize-state.md` is loop-managed and points allowed work toward simplification, commit, and `finalize-summary.md`.
- algorithmic_behavior: This is a static prompt block loaded by `finalize_state_file_blocked_message()` in `hooks/lib/loop-common.sh:591-598`. The hook layer emits it when Write/Edit/Bash attempts target `finalize-state.md`; examples include `hooks/loop-write-validator.sh:154-156`, `hooks/loop-edit-validator.sh:117-119`, and `hooks/loop-bash-validator.sh:139-145`.
- inputs_outputs_state: Input is the validator’s detection that a path or shell command targets `finalize-state.md`. Output is the denial text beginning with “Finalize State File Modification Blocked” at `prompt-template/block/finalize-state-file-modification.md:1-3`. State transition is negative: no state file is changed; control remains with the loop system.
- gates_or_invariants: The invariant is that `finalize-state.md` may not be modified by the implementation agent. The only permitted adjacent work is running code-simplifier, committing changes, and writing `finalize-summary.md` at `prompt-template/block/finalize-state-file-modification.md:5-8`.
- dependencies_and_callers: Depends on the template loader through `load_and_render_safe` and `TEMPLATE_DIR`; fallback text exists in `hooks/lib/loop-common.sh:591-598` if the template is absent. The matching predicate is `is_finalize_state_file_path()` at `hooks/lib/loop-common.sh:634-638`.
- edge_cases_or_failure_modes: Because `is_state_file_path()` also matches `finalize-state.md`, validators check finalize first to avoid weaker generic state messaging; this ordering is documented in `hooks/loop-edit-validator.sh:115` and `hooks/loop-write-validator.sh:152`. Bash protections also block source-side `mv`/`cp` operations involving finalize state through source-pattern checks.
- validation_or_tests: Finalize protection is covered by `tests/test-finalize-phase.sh` references found for Write/Edit/Bash blocking; the assigned robustness test also indirectly relies on active-state resolution that prefers `finalize-state.md` in `hooks/lib/loop-common.sh:176-190`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-110 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Continuation prompt fragment that preserves Agent Teams mode across RLCR rounds. It is a coordination contract: the team leader should split remaining work and coordinate, not implement directly.
- algorithmic_behavior: Static template loaded in the stop hook when a next-round prompt is generated and both `AGENT_TEAMS == true` and `REVIEW_STARTED != true` at `hooks/loop-codex-stop-hook.sh:1630-1638`. It appends the mode instruction after normal next-round prompt content.
- inputs_outputs_state: Inputs are parsed state fields `agent_teams` and `review_started` from `state.md`; these are parsed in `hooks/lib/loop-common.sh:326-330` and defaulted with `STATE_AGENT_TEAMS="${STATE_AGENT_TEAMS:-false}"` at `hooks/lib/loop-common.sh:371`. Output is appended prompt text at `prompt-template/claude/agent-teams-continue.md:2-4`.
- gates_or_invariants: Only active during implementation continuation, not review phase. The invariant is role separation: team leader coordinates team members rather than doing implementation work personally.
- dependencies_and_callers: Caller is `hooks/loop-codex-stop-hook.sh`; tests check template existence/content in `tests/test-agent-teams.sh` per search hit around `tests/test-agent-teams.sh:318`. Fallback text duplicates the same behavior if the template cannot load at `hooks/loop-codex-stop-hook.sh:1632-1635`.
- edge_cases_or_failure_modes: If the template is missing, behavior remains via fallback. If `review_started` is true, the continuation is suppressed to avoid mixing implementation-team orchestration into review flow. If `agent_teams` is absent, default false prevents accidental activation.
- validation_or_tests: Validated by template existence/content tests and by stop-hook next-round prompt generation paths. The state parser’s `agent_teams` field support is part of `hooks/lib/loop-common.sh`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-140 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for RLCR/PR hook entrypoints and shared state parsing. It tests JSON handling, state-file protection, plan-file validation, command-injection boundaries, concurrency safety, stop-hook state-machine behavior, and large/nested JSON resilience.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `tests/test-helpers.sh` at `tests/robustness/test-hook-system-robustness.sh:20-24`, creates temp state fixtures, invokes hook scripts through stdin JSON, captures exit codes/output, and records pass/fail. It groups tests into Edit Validator, Plan File Validator, State File Parsing, Command Injection Prevention, Concurrent Access, Stop Hook State Parsing, and JSON Edge Cases.
- inputs_outputs_state: Inputs are hook JSON payloads for Edit, Bash, Write, Read, and stop hooks; synthetic `.humanize/rlcr/<timestamp>/state.md` files; temporary git repos; and a mock `codex` executable for active-loop stop-hook behavior at `tests/robustness/test-hook-system-robustness.sh:592-605`. Outputs are PASS/FAIL counts through `print_test_summary` at `tests/robustness/test-hook-system-robustness.sh:671-672`. State transitions under test include blocking `state.md` edits, preserving state during concurrent reads, and renaming invalid active state to `unexpected-state.md` in missing-field stop-hook paths.
- gates_or_invariants: Edit to `state.md` and traversal to `state.md` must be blocked, with traversal expecting exit 2 at `tests/robustness/test-hook-system-robustness.sh:124-155`. Paths outside `.humanize/rlcr` are delegated to sandbox and allowed at `tests/robustness/test-hook-system-robustness.sh:109-122`. Bash must block active-loop `state.md` and post-round-0 `goal-tracker.md` modifications, but allow unrelated dangerous commands through for sandbox enforcement at `tests/robustness/test-hook-system-robustness.sh:317-382`. Active valid loops must return JSON `decision:block` from the stop hook at `tests/robustness/test-hook-system-robustness.sh:559-613`.
- dependencies_and_callers: Exercises `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. Shared behavior comes from `validate_hook_input()` at `hooks/lib/loop-common.sh:76-113`, `find_active_loop()` at `hooks/lib/loop-common.sh:215-291`, `get_current_round()` at `hooks/lib/loop-common.sh:293-306`, strict parsing at `hooks/lib/loop-common.sh:386-449`, and terminal rename `end_loop()` at `hooks/lib/loop-common.sh:1231-1264`.
- edge_cases_or_failure_modes: Covers malformed JSON, missing `file_path`, traversal via `../`, whitespace in frontmatter, quoted path values, missing closing frontmatter delimiter, embedded newlines, binary-looking content, concurrent hook invocations, missing state directories, corrupted state content, missing critical fields, active-loop review continuation, 100KB JSON payloads, escaped nulls, and nested JSON. A notable contract is graceful handling: many malformed inputs need only exit below 128 rather than crash.
- validation_or_tests: This file is itself a test suite and is included in the aggregate runner at `tests/run-all-tests.sh:72`. It validates both black-box hook outputs and shared parser behavior, including the distinction between validator hard blocks (`exit 2`) and stop-hook loop continuation (`exit 0` plus `decision:block`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 5 assigned item headings are present once: 020, 050, 080, 110, 140
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`