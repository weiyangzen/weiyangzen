# agent_01 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the Humanize Claude Code plugin that implements the RLCR feedback loop. The package metadata identifies version `1.9.0` and describes iterative development with Codex review in `.claude-plugin/plugin.json:1-18`; README defines RLCR as Claude implementation plus independent Codex review in `README.md:7-13`.
- algorithmic_behavior: The root coordinates command entrypoints, hooks, templates, scripts, tests, and CI. `/humanize:start-rlcr-loop` is a command wrapper that can only execute `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:1-14`). Setup validates a single plan, git state, branch/base branch, Codex config, then creates `.humanize/rlcr/<timestamp>/` state (`scripts/setup-rlcr-loop.sh:721-787`), goal tracker (`scripts/setup-rlcr-loop.sh:806-928`), and initial prompt (`scripts/setup-rlcr-loop.sh:930-1052`). Hook registration in `hooks/hooks.json:4-79` wires `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, and `Stop` into the RLCR/PR-loop state machines. The stop hook then drives implementation review, code review, finalize, and terminal state transitions (`hooks/loop-codex-stop-hook.sh:1423-1535`).
- inputs_outputs_state: Inputs are plan markdown, command options, Claude hook JSON, git repository state, task/transcript state, and Codex CLI output. Outputs include `.humanize/rlcr/<timestamp>/state.md`, `finalize-state.md`, terminal `*-state.md` files, round prompts/summaries/review results, goal tracker, and cache debug files. The state frontmatter includes `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, `session_id`, and `agent_teams` (`scripts/setup-rlcr-loop.sh:767-786`). `review_started: true` plus `.review-phase-started` marks review phase (`hooks/loop-codex-stop-hook.sh:1456-1465`); `finalize-state.md` marks finalize phase (`hooks/loop-codex-stop-hook.sh:1065-1149`).
- gates_or_invariants: Root invariants include one active RLCR/PR loop at a time (`scripts/setup-rlcr-loop.sh:256-280`), clean git tree before starting (`scripts/setup-rlcr-loop.sh:625-645`), safe plan paths and no symlink traversal (`scripts/setup-rlcr-loop.sh:356-416`), plan tracking consistency (`scripts/setup-rlcr-loop.sh:459-508`), branch consistency (`hooks/loop-codex-stop-hook.sh:227-257`), plan integrity before review (`hooks/loop-codex-stop-hook.sh:260-340`), incomplete task blocking before Codex (`hooks/loop-codex-stop-hook.sh:342-398`), summary existence (`hooks/loop-codex-stop-hook.sh:631-667`), goal tracker initialization (`hooks/loop-codex-stop-hook.sh:670-745`), and code-review pass with no `[P0-9]` findings before finalize (`hooks/loop-codex-stop-hook.sh:1034-1062`).
- dependencies_and_callers: Main dependencies are `git`, `jq`, `python3`, `codex`, shell utilities, Claude hook runtime, and optional `zsh` for specific monitor tests. `hooks/lib/loop-common.sh` is the shared state/parser/path/message library used by RLCR hooks (`hooks/lib/loop-common.sh:1-14`). `hooks/lib/template-loader.sh` resolves and renders `prompt-template/**` with single-pass placeholder substitution (`hooks/lib/template-loader.sh:24-31`, `hooks/lib/template-loader.sh:50-132`). CI covers template and plan-file gates in `.github/workflows/template-test.yml:1-64` and `.github/workflows/plan-file-test.yml:1-60`.
- edge_cases_or_failure_modes: The root code explicitly handles malformed state, missing schema fields, branch switch, deleted/modified plan files, missing Codex, empty review output, Codex failures, max iteration termination, STOP circuit breaker, finalize cancellation, session-id filtering, and stale/terminal loop detection. `find_active_loop` intentionally checks only the newest loop without a session filter to prevent stale loop revival (`hooks/lib/loop-common.sh:215-291`). The export has no usable `.git` metadata in this sandbox, so repository validation here relied on direct file inspection and the provided source commit rather than local Git history.
- validation_or_tests: Recursive inspection found the core folders `.claude-plugin`, `.github`, `agents`, `commands`, `hooks`, `prompt-template`, `scripts`, and `tests`; `wc` over files reported 42,812 total lines. `tests/run-all-tests.sh:32-76` enumerates template, hook, RLCR, PR-loop, session, agent-teams, and robustness suites. This research did not execute the suites; it inspected the executable specs and CI wiring only.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-031 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Write gate for RLCR and PR-loop control surfaces. It prevents Claude from writing generated prompts/todos, state files, protected plan backups, wrong-round summaries, wrong-directory summaries, post-round-0 goal tracker updates, and PR-loop read-only artifacts (`hooks/loop-write-validator.sh:3-13`).
- algorithmic_behavior: Reads hook JSON from stdin, validates syntax/shape, rejects deeply nested input, and exits early unless `tool_name` is `Write` (`hooks/loop-write-validator.sh:25-45`). It lowercases the target path, extracts `session_id`, applies early blocks for `round-*-todos.md` and `round-*-prompt.md` (`hooks/loop-write-validator.sh:48-71`), applies PR-loop protections and PR resolve round validation (`hooks/loop-write-validator.sh:73-97`), then determines whether the write is an RLCR summary/finalize/control file requiring active-loop validation (`hooks/loop-write-validator.sh:103-120`).
- inputs_outputs_state: Inputs are `tool_name`, `tool_input.file_path`, optional `session_id`, `CLAUDE_PROJECT_DIR`, active `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, and templates loaded via shared helpers. Outputs are exit `0` to allow, exit `1` for invalid hook/malformed state safety failures, and exit `2` with a rendered stderr block for policy violations. It reads but does not mutate loop state.
- gates_or_invariants: Requires `tool_input.file_path` for Write (`hooks/loop-write-validator.sh:43-48`). Finds the active session-aware loop and fails closed on malformed state via `parse_state_file_strict` (`hooks/loop-write-validator.sh:126-147`). Blocks `finalize-state.md` before generic `state.md` because the generic matcher also matches finalize state (`hooks/loop-write-validator.sh:150-162`). Allows `finalize-summary.md` only at the exact active-loop path (`hooks/loop-write-validator.sh:164-175`). Blocks `.humanize/rlcr/**/plan.md` backup writes (`hooks/loop-write-validator.sh:177-187`). Blocks `goal-tracker.md` after round 0 and points the agent to the current summary file (`hooks/loop-write-validator.sh:190-198`). For summaries, validates current round and exact active-loop directory (`hooks/loop-write-validator.sh:230-265`).
- dependencies_and_callers: Called by `hooks/hooks.json:14-22`. Depends on `hooks/lib/loop-common.sh` for JSON validation (`hooks/lib/loop-common.sh:76-150`), session-aware active loop discovery (`hooks/lib/loop-common.sh:233-291`), strict state parsing (`hooks/lib/loop-common.sh:386-449`), round/path helpers (`hooks/lib/loop-common.sh:518-560`), and rendered block messages (`hooks/lib/loop-common.sh:562-620`, `hooks/lib/loop-common.sh:1217-1229`). Template rendering comes from `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If no active RLCR loop exists, RLCR-specific writes are allowed after early prompt/todos checks (`hooks/loop-write-validator.sh:131-133`). The allowlist permits only specific legacy/bootstrap files such as `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop (`hooks/lib/loop-common.sh:539-560`). The code computes `IS_FINALIZE_PHASE` but the finalize-summary allow path only checks exact path, not phase, so an active normal loop can write `finalize-summary.md` if targeted exactly (`hooks/loop-write-validator.sh:135-175`). Legacy `.humanize-loop.local` is intentionally outside the current loop pattern and is allowed by tests (`tests/test-plan-file-hooks.sh:1068-1106`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` verifies Write blocks `.humanize/rlcr/**/plan.md` (`tests/test-plan-file-hooks.sh:226-237`) and allows legacy `.humanize-loop.local` paths (`tests/test-plan-file-hooks.sh:1082-1093`). Related allowlist behavior is covered by `tests/test-allowlist-validators.sh` according to root test orchestration (`tests/run-all-tests.sh:32-76`). This research inspected tests rather than executing them.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-061 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file and state-machine protection. It validates the UserPromptSubmit plan validator, Write/Edit/Bash protection of `plan.md`, stop-hook plan integrity gates, YAML field parsing, placeholder detection, and legacy-path exclusions (`tests/test-plan-file-hooks.sh:3-10`).
- algorithmic_behavior: Creates an isolated temp git repo, exports a private cache directory, injects a mock `codex` executable, and constructs `.humanize/rlcr/2024-01-01_12-00-00` with plan backup plus `state.md` (`tests/test-plan-file-hooks.sh:30-58`, `tests/test-plan-file-hooks.sh:63-114`). Each scenario runs a hook with synthetic JSON or empty stop input, captures stdout/stderr and exit code, then increments pass/fail counters. Final process exit is the failed-test count (`tests/test-plan-file-hooks.sh:1108-1117`).
- inputs_outputs_state: Inputs are generated plan files, git branches, `.gitignore`, loop `state.md`, `goal-tracker.md`, `round-0-summary.md`, and hook JSON for Write/Edit/Bash. Outputs are PASS/FAIL/SKIP lines, counters, and process exit status. The tests exercise state fields `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started`.
- gates_or_invariants: Tests require valid state to pass, missing v1.5.0+ fields to block, and branch changes to block (`tests/test-plan-file-hooks.sh:119-217`). They require Write/Edit/Bash hooks to block plan backup writes/deletes (`tests/test-plan-file-hooks.sh:226-298`). They assert Bash command-injection bypasses are blocked for command substitution, glob, brace expansion, pipes, and backticks (`tests/test-plan-file-hooks.sh:304-367`). They require quote-stripping for YAML `plan_file` and `start_branch`, branch mismatch detection under quotes, and hyphenated plan paths (`tests/test-plan-file-hooks.sh:373-500`).
- dependencies_and_callers: Depends on `git`, `jq`, shell, `mktemp`, and the local hooks under `hooks/`. It mocks `codex` so stop-hook tests do not call the real CLI (`tests/test-plan-file-hooks.sh:38-58`). It is part of the full suite (`tests/run-all-tests.sh:32-76`) and is directly executed by plan-file CI (`.github/workflows/plan-file-test.yml:42-50`).
- edge_cases_or_failure_modes: Covers modified, deleted, and missing backup plan files (`tests/test-plan-file-hooks.sh:509-600`); tracked plan race conditions with uncommitted modifications (`tests/test-plan-file-hooks.sh:602-671`); old-schema JSON block behavior (`tests/test-plan-file-hooks.sh:673-695`); committed tracked-plan drift where git status is clean but backup content differs (`tests/test-plan-file-hooks.sh:697-775`); section-specific goal-tracker placeholder reporting for Ultimate Goal, Acceptance Criteria, Active Tasks, and all combined (`tests/test-plan-file-hooks.sh:781-1061`); and negative tests showing `.humanize-loop.local` is no longer a protected loop directory (`tests/test-plan-file-hooks.sh:1068-1106`).
- validation_or_tests: This file is itself the validation artifact. It is also routed through CI by `.github/workflows/plan-file-test.yml:47-50`. This research inspected the file and did not execute it.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-091 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template that prevents transition to Codex review while Claude still has incomplete Task/Todo work. It is a gate message, not persisted loop state (`prompt-template/block/incomplete-todos.md:1-12`).
- algorithmic_behavior: Renders a markdown block headed “Incomplete Tasks Detected,” injects `{{INCOMPLETE_LIST}}`, and instructs the agent to complete all remaining tasks, mark each as `completed` with TaskUpdate, and only then write summary/stop (`prompt-template/block/incomplete-todos.md:3-12`). This makes task completion a pre-review invariant rather than something Codex discovers later.
- inputs_outputs_state: Input is `INCOMPLETE_LIST` from `hooks/check-todos-from-transcript.py`. Output is a JSON `decision: block` reason emitted by the stop hook when the checker exits with incomplete work (`hooks/loop-codex-stop-hook.sh:342-397`). It does not mutate `state.md`; it blocks the attempted Stop so the active round remains open.
- gates_or_invariants: The stop hook runs the task checker before expensive Codex review (`hooks/loop-codex-stop-hook.sh:342-354`). Checker exit `2` is treated as a parse-error block, while exit `1` renders this template and blocks with system message “incomplete tasks detected” (`hooks/loop-codex-stop-hook.sh:356-397`). The checker treats new Task files under `~/.claude/tasks/<session_id>/` as authoritative and legacy TodoWrite transcript entries as fallback/legacy (`hooks/check-todos-from-transcript.py:104-147`, `hooks/check-todos-from-transcript.py:171-196`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in the stop hook (`hooks/loop-codex-stop-hook.sh:380-386`) and rendered by `hooks/lib/template-loader.sh:167-203`. Depends indirectly on hook input fields `session_id`, `tasks_base_dir`, and `transcript_path` accepted by the checker (`hooks/check-todos-from-transcript.py:150-176`).
- edge_cases_or_failure_modes: If the checker script is missing, the stop hook skips this gate because it only runs when the file exists (`hooks/loop-codex-stop-hook.sh:349-351`). Malformed task files are skipped by the checker (`hooks/check-todos-from-transcript.py:143-145`), so unreadable/malformed task JSON can under-report incomplete tasks. Empty hook input exits successfully in the checker (`hooks/check-todos-from-transcript.py:151-157`), which means the gate relies on Stop hook input containing usable session/transcript pointers.
- validation_or_tests: `tests/run-all-tests.sh:32-76` includes `test-todo-checker.sh` and broader stop-hook suites. Template existence/reference coverage is enforced through `.github/workflows/template-test.yml:26-64`. This research inspected the template and its call sites.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-121 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit template for the RLCR Review Phase. It documents the `codex review` invocation and the transition contract: severity findings continue the loop; no findings move to Finalize Phase (`prompt-template/codex/code-review-phase.md:1-18`).
- algorithmic_behavior: The template records `{{BASE_BRANCH}}`, `{{REVIEW_ROUND}}`, and `{{TIMESTAMP}}`, explains that `codex review` does not consume prompt input, specifies that review output is scanned for `[P0-9]` markers, and lists generated files for audit/debug (`prompt-template/codex/code-review-phase.md:3-18`, `prompt-template/codex/code-review-phase.md:30-36`). The stop hook writes this as `round-N-review-prompt.md` before running `codex review --base <review_base>` (`hooks/loop-codex-stop-hook.sh:951-989`).
- inputs_outputs_state: Inputs are rendered placeholders from stop-hook review state: review round, base branch, timestamp, and extra currently supplied values such as `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE` (`hooks/loop-codex-stop-hook.sh:983-989`). Output is an audit markdown file in the loop directory, plus cache `.cmd` and `.log` files generated by the stop hook (`hooks/loop-codex-stop-hook.sh:965-1018`). State transition is handled outside the template: issues update `current_round` and create the next review prompt, no issues rename `state.md` to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:1034-1062`, `hooks/loop-codex-stop-hook.sh:1151-1196`).
- gates_or_invariants: Review base prefers captured `base_commit` over `base_branch` to avoid empty diffs when the working branch advances (`hooks/loop-codex-stop-hook.sh:956-964`). `detect_review_issues` scans the review log for `[P?]` markers near the beginning of lines and extracts from the first finding onward (`hooks/lib/loop-common.sh:468-511`). A failed review command, missing/empty log, or missing output is blocking and cannot skip to finalize (`hooks/loop-codex-stop-hook.sh:1038-1055`, `hooks/loop-codex-stop-hook.sh:1198-1271`).
- dependencies_and_callers: Called only from `run_codex_code_review` in `hooks/loop-codex-stop-hook.sh:951-1019`. Uses `codex review`, `git` base refs captured during setup (`scripts/setup-rlcr-loop.sh:648-719`), the shared template loader, and cache paths under `XDG_CACHE_HOME` (`hooks/loop-codex-stop-hook.sh:903-913`).
- edge_cases_or_failure_modes: The template is slightly behind the stop-hook fallback: it does not include `BASE_COMMIT`, `REVIEW_BASE`, or `REVIEW_BASE_TYPE`, even though the hook passes them (`hooks/loop-codex-stop-hook.sh:971-989`), so audit files rendered from this template omit the exact captured commit/review-base details. The template also lists a `round-N-codex-review.out` stdout capture (`prompt-template/codex/code-review-phase.md:34-36`), but the current review helper creates `.cmd` and combined `.log` only (`hooks/loop-codex-stop-hook.sh:965-1018`). Functionally, the gate relies on the actual `.log`, not the template text.
- validation_or_tests: Template presence and placeholder/reference integrity are covered by template CI (`.github/workflows/template-test.yml:26-64`) and comprehensive test orchestration (`tests/run-all-tests.sh:32-76`). Code-review behavior is covered by dedicated suites named in the runner, including `test-codex-review-merge.sh` (`tests/run-all-tests.sh:45-47`). This research inspected the template and stop-hook integration.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 section headers present; each assigned header appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`