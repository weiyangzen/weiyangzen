# agent_30 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-030 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse guard for Claude `Read` operations. It protects the RLCR loop state machine from stale or wrong-location reads of round prompt/summary/todos artifacts while allowing non-RLCR reads to pass through.
- algorithmic_behavior: The script reads hook JSON from stdin, validates it via shared JSON guards, ignores non-`Read` tools, requires `tool_input.file_path`, lowercases the path for pattern checks, and extracts `session_id` for session-aware loop filtering (`hooks/loop-read-validator.sh:25-52`). It blocks most `round-*-todos.md` reads unless the file is explicitly allowlisted for the active loop (`58-65`). For `round-*-summary.md` and `round-*-prompt.md`, it resolves the active `.humanize/rlcr` session, parses the active state file, derives `current_round`, extracts the requested round number, and enforces both round correctness and exact directory path (`72-170`).
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name`, `tool_input.file_path`, and optional `session_id`. Output is an exit code plus optional stderr block message. Exit `0` means pass-through or allowed read; exit `1` means malformed input/state; exit `2` means policy block with a rendered prompt-template reason. State read is `.humanize/rlcr/<session>/{state.md|finalize-state.md}`, resolved through `resolve_active_state_file`; `STATE_CURRENT_ROUND` becomes the state variable driving round validation (`91-99`).
- gates_or_invariants: JSON must be valid, UTF-8, contain `tool_name`, and not exceed nesting depth 30 through `validate_hook_input` and `is_deeply_nested` in `hooks/lib/loop-common.sh:76-149`. Read hooks must include `tool_input.file_path` (`hooks/loop-read-validator.sh:43-48`). RLCR prompt/summary reads must come from `.humanize/rlcr/`, match the current round, and equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` (`122-168`). Malformed active state fails closed (`94-98`). Todos files are generally blocked because native Task tools are intended as the task state surface (`55-65`).
- dependencies_and_callers: Registered for Claude `Read` hooks in `hooks/hooks.json:33-39`. It sources `hooks/lib/loop-common.sh` (`hooks/loop-read-validator.sh:17-20`), which provides active-loop discovery with zombie-loop protection and session filtering (`hooks/lib/loop-common.sh:215-291`), strict state parsing (`386-448`), round-file detection (`520-537`), allowlist entries (`542-560`), and template-backed block messages (`563-570`). `tests/test-allowlist-validators.sh:234-284`, `tests/test-finalize-phase.sh:839-850`, and `tests/robustness/test-hook-input-robustness.sh:35-115,180-199` exercise its allowlist, finalize-state, JSON, and nesting behavior.
- edge_cases_or_failure_modes: If no active loop exists, prompt/summary reads are allowed (`87-89`), which avoids stale blocking outside an active RLCR run. `is_in_humanize_loop_dir` is a substring check for `.humanize/rlcr/` (`hooks/lib/loop-common.sh:831-835`), so the later exact `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` check is the stronger path invariant. `resolve_active_state_file` prefers `finalize-state.md` over `state.md` (`180-189`), allowing finalize-phase current-round checks. Allowlist bypass permits only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` at the active loop path (`542-560`).
- validation_or_tests: Covered by allowlist tests for allowed historical files and blocked stale todos/summary files (`tests/test-allowlist-validators.sh:234-284`), finalize-state read validation (`tests/test-finalize-phase.sh:839-850`), hook input robustness including invalid JSON, missing `file_path`, and deep nesting (`tests/robustness/test-hook-input-robustness.sh:35-115,180-199`), and aggregate inclusion in `tests/run-all-tests.sh:44-50,64-75`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-060 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Runtime executable specification for RLCR monitor shutdown, cleanup idempotence, terminal restoration, and signal handling. It validates the monitor behavior that keeps the user-facing RLCR loop safe when `.humanize/rlcr` disappears or the process receives interrupt/termination signals.
- algorithmic_behavior: The test creates `/tmp/test-monitor-runtime-$$`, seeds a fake `.humanize/rlcr/.../state.md`, and then generates focused helper scripts to simulate or inspect monitor functions (`tests/test-monitor-runtime.sh:45-65`). Test 1 verifies `_graceful_stop` calls `_cleanup`, `_cleanup` calls `_restore_terminal`, and the user-facing deletion reason appears (`61-135`). Test 2 proves `cleanup_done` makes cleanup idempotent (`143-177`). Test 3 models the monitor loop directory existence check and expects graceful stop after `rm -rf .humanize/rlcr` (`186-245`). Tests 4-5 inspect `scripts/humanize.sh` for terminal reset and `_graceful_stop -> _cleanup` (`263-324`). Tests 6-9 exercise bash and zsh signal trap behavior (`333-490`).
- inputs_outputs_state: Input is the repository’s `scripts/humanize.sh` and temporary generated shell scripts. Outputs are PASS/FAIL lines, accumulated `TESTS_PASSED` and `TESTS_FAILED`, and process exit `0` only when no failure is recorded (`493-512`). Test state includes `cleanup_done`, `monitor_running`, `tail_pid`, and temporary `.humanize/rlcr` existence. The production implementation defines those monitor locals in `scripts/humanize.sh:642-645`.
- gates_or_invariants: `_cleanup` must run at most once, set `monitor_running=false`, reset traps, kill a live `tail_pid`, restore the terminal, and emit a stopped message; these are anchored in `scripts/humanize.sh:649-672`. `_graceful_stop` must call `_cleanup` before printing the specific stop reason (`677-688`). The monitor loop must stop cleanly when `.humanize/rlcr` no longer exists (`750-755`, also repeated in nested polling at `811-815`). Terminal restore must reset the scroll region with `printf "\033[r"` (`618-623`). Bash traps must attach `_cleanup` to `INT TERM`; zsh must define `TRAPINT` and `TRAPTERM` (`695-706`).
- dependencies_and_callers: Sources or inspects `scripts/humanize.sh` repeatedly (`tests/test-monitor-runtime.sh:69-70,267-284,307-320,391-405,419-422`). It is part of the aggregate suite at `tests/run-all-tests.sh:49-50`. It uses only shell, grep, zsh if present, and temporary files under `/tmp`.
- edge_cases_or_failure_modes: The first synthetic state file lacks full YAML frontmatter, but the test does not rely on real state parsing; it focuses on monitor cleanup paths. The zsh SIGINT test is conditional and skipped if `zsh` is absent (`432-490`). Some checks are source-inspection based rather than full terminal integration because non-interactive shells cannot reliably validate terminal cursor state (`260-287`). There is a small shell bug in the generated Test 4 snippet where `grep -q '_restore_terminal' ... | grep -q '_cleanup'` cannot work as intended because the first `grep -q` emits no output (`282`), but the following direct source check with `grep -A30 "^    _cleanup()"` supplies the actual cleanup-call assertion (`305-311`).
- validation_or_tests: This file is itself a validation suite. It reports a summary and exits nonzero on any failed assertion (`493-512`). Its coverage maps to the implementation lines in `scripts/humanize.sh:618-688,695-706,750-755,811-815`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-090 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Block prompt template for the Round 0 goal-tracker initialization gate. It defines the corrective transition when the stop hook detects missing immutable goal-tracker content.
- algorithmic_behavior: The template tells the agent it is in Round 0, names missing items from `{{MISSING_ITEMS}}`, and requires reading `{{GOAL_TRACKER_FILE}}`, replacing placeholder text with an Ultimate Goal, defining 3-7 testable Acceptance Criteria, populating Active Tasks mapped to acceptance criteria, and writing `goal-tracker.md` (`prompt-template/block/goal-tracker-not-initialized.md:1-15`). It explicitly states the immutable section can only be set in Round 0 and becomes read-only later (`16`), then permits another exit attempt after update (`18`).
- inputs_outputs_state: Inputs are template variables `GOAL_TRACKER_FILE` and `MISSING_ITEMS`; output is rendered block text embedded in the stop-hook JSON reason. The relevant caller builds `MISSING_ITEMS` from placeholder checks and renders this template through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:720-733`), then returns a JSON block decision with system message `Loop: Goal Tracker not initialized in Round 0` (`735-743`).
- gates_or_invariants: Round 0 is the only initialization window for the immutable goal-tracker section. The required content must replace placeholders, not merely preserve file shape. Acceptance Criteria count is constrained to 3-7 and tasks must map to criteria (`prompt-template/block/goal-tracker-not-initialized.md:8-14`). The stop hook blocks exit while missing items remain (`hooks/loop-codex-stop-hook.sh:726-743`).
- dependencies_and_callers: Used by `hooks/loop-codex-stop-hook.sh` when goal-tracker placeholders are detected (`731-733`). Rendering uses `hooks/lib/template-loader.sh` fallback behavior: missing or empty templates degrade to fallback content instead of crashing (`hooks/lib/template-loader.sh:168-203`).
- edge_cases_or_failure_modes: The template is not executable code, but it is algorithmically significant because its instructions become the agent’s next action contract. If the template is missing or renders empty, the caller’s fallback gives a shorter but still blocking message (`hooks/loop-codex-stop-hook.sh:727-731`; `hooks/lib/template-loader.sh:179-199`). If the agent updates goal-tracker after Round 0, other read-only guards should prevent immutable-section modification; this template’s own invariant is advisory text used during Round 0.
- validation_or_tests: Indirectly covered through stop-hook and template-reference suites; `tests/test-template-references.sh` includes hook/template reference coverage for `hooks/loop-codex-stop-hook.sh`, and aggregate execution includes template and goal-tracker robustness suites (`tests/run-all-tests.sh:38-40,61`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-120 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Review-phase prompt template that controls the RLCR transition after Codex review finds priority-marked issues. It defines the repair-only contract for the next Claude round.
- algorithmic_behavior: The template embeds `{{REVIEW_CONTENT}}`, instructs the agent to address all `[P0-9]` severity findings, avoid unrelated work, commit fixes, and write a summary to `{{SUMMARY_FILE}}` (`prompt-template/claude/review-phase-prompt.md:1-15`). It specifies summary content: fixed issues, resolution method, and unresolved issues with explanation (`16-22`). It states COMPLETE has no effect, another Codex review follows, and the loop continues until no `[P0-9]` issues are found (`23-28`).
- inputs_outputs_state: Inputs are `REVIEW_CONTENT` from Codex review extraction and `SUMMARY_FILE` for the next round. Output is written to the next round prompt file by the stop hook (`hooks/loop-codex-stop-hook.sh:1183-1185`) and returned as the block reason in JSON (`1187-1192`). The state transition is implementation/review back to a repair round rather than finalize/complete.
- gates_or_invariants: Review-phase completion is disabled until priority review issues are addressed. Scope is restricted to fixes only, not feature work (`prompt-template/claude/review-phase-prompt.md:11-13`). The loop gate is severity-marker based: `[P0-9]` findings force another repair pass, and absence of such findings allows progression.
- dependencies_and_callers: Called from `continue_review_loop_with_issues` in `hooks/loop-codex-stop-hook.sh:1168-1185`. Review issue extraction is documented in `hooks/lib/loop-common.sh:451-467`, which scans Codex review logs for `[P?]` markers near line starts. `tests/test-agent-teams.sh:586-599` confirms review prompts are generated through this template path and do not include agent-team continuation text, while still containing review issue content.
- edge_cases_or_failure_modes: If the template is absent or empty, the stop hook fallback still includes review content, issue-fixing instructions, commit requirement, and summary path (`hooks/loop-codex-stop-hook.sh:1168-1181`). The template depends on review output preserving `[P0-9]` markers; if review content uses another severity format, this phase may not trigger as designed.
- validation_or_tests: Indirectly exercised by agent-team review-phase tests (`tests/test-agent-teams.sh:576-599`) and template-reference coverage. Aggregate runner includes codex review merge tests and agent-team tests (`tests/run-all-tests.sh:46,56-57`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-150 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the portable timeout abstraction used by setup scripts, PR/RLCR hooks, GitHub CLI polling, Codex execution, and review checks. It verifies command-boundary behavior that prevents hung external commands from freezing the RLCR/PR loop.
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh` and shared `tests/test-helpers.sh` (`tests/robustness/test-timeout-robustness.sh:14-18`). It validates detected implementation values (`31-49`), successful fast commands (`51-60`), timeout exit code `124` for long sleeps when a timeout backend exists (`62-77`), argument preservation (`79-87`), original exit-code preservation (`89-100`), pipeline/subshell use through `sh -c` (`102-110`, `249-257`), edge cases such as 1-second and zero-second timeouts (`120-155`), large output (`157-168`), rapid cycles (`170-186`), nonexistent and empty commands (`188-210`), special characters (`211-219`), signal-handling commands (`221-233`), fallback detection (`235-247`), and exported `TIMEOUT_IMPL` (`259-272`).
- inputs_outputs_state: Inputs are timeout seconds plus command argv passed to `run_with_timeout`. Outputs are command stdout/stderr and exit status. The implementation under test chooses `TIMEOUT_IMPL` from `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` (`scripts/portable-timeout.sh:10-29`) and dispatches in `run_with_timeout` (`33-71`). The test outputs PASS/FAIL via shared helpers and exits with `print_test_summary` result (`tests/robustness/test-timeout-robustness.sh:274-279`; helpers at `tests/test-helpers.sh:30-78`).
- gates_or_invariants: Timeout backends must report one of the known labels, timed-out commands should return `124` when a backend exists, non-timeout command failures must preserve their own nonzero status, stdout with args/special characters should remain intact, and no-timeout environments must degrade by running the command with a warning rather than failing discovery (`scripts/portable-timeout.sh:64-68`).
- dependencies_and_callers: `run_with_timeout` is used broadly by `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, PR status/reaction check scripts, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and robustness tests, as shown by repository references. Aggregate runner includes this suite at `tests/run-all-tests.sh:68`.
- edge_cases_or_failure_modes: If `TIMEOUT_IMPL=none`, timeout-specific long-sleep checks are skipped as passes (`tests/robustness/test-timeout-robustness.sh:65-77,122-137,142-155`), so the environment can still pass while lacking true timeout enforcement. The Python fallback uses `subprocess.run(..., timeout=N)` and exits `124` on `TimeoutExpired`, but it does not explicitly kill descendant process groups beyond Python subprocess semantics (`scripts/portable-timeout.sh:47-61`). Zero-timeout behavior is allowed to either succeed or return `124` because backend behavior varies (`147-152`). Empty command handling is accepted unconditionally after invocation (`201-210`), so this test documents tolerance rather than a strict expected code.
- validation_or_tests: This file is itself the validation suite for timeout behavior and uses shared summary helpers to return failure if any strict assertion fails (`tests/test-helpers.sh:58-78`). It anchors expected behavior to `scripts/portable-timeout.sh:10-76`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, one per assigned research row
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`