# agent_21 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-021 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting a PR review loop. It is not the implementation itself; it is the command surface that constrains tool access to `scripts/setup-pr-loop.sh` and documents the PR-loop algorithm for the operator. Frontmatter sets `allowed-tools` to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)` at `commands/start-pr-loop.md:4`, then delegates execution at `commands/start-pr-loop.md:10-14`.
- algorithmic_behavior: The command initializes a remote-bot review loop that detects the PR for the current branch, fetches review comments, asks the agent to fix issues, requires push plus bot re-trigger, and relies on the Stop Hook to poll every 30 seconds with a 15-minute timeout, then local Codex validation (`commands/start-pr-loop.md:16-24`). It defines comment precedence as human comments before bot comments, with newest bot comments first (`commands/start-pr-loop.md:31-35`).
- inputs_outputs_state: Inputs are `--claude` and/or `--codex`, plus optional `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout SECONDS` (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). The delegated script validates at least one bot flag (`scripts/setup-pr-loop.sh:180-191`), builds `ACTIVE_BOTS_ARRAY` (`scripts/setup-pr-loop.sh:193-203`), resolves bot mentions with `build_bot_mention_string` (`scripts/setup-pr-loop.sh:213-214`; helper at `hooks/lib/loop-common.sh:883-896`), then creates `.humanize/pr-loop/<timestamp>/state.md` with round, max iteration, PR number, configured/active bots, Codex settings, polling settings, startup case, latest commit, and trigger fields (`scripts/setup-pr-loop.sh:615-635`). It also creates a PR-loop goal tracker (`scripts/setup-pr-loop.sh:641-713`) and initial prompt/resolution path (`scripts/setup-pr-loop.sh:717-851`).
- gates_or_invariants: Requires at least one bot flag (`commands/start-pr-loop.md:25-30`; enforced at `scripts/setup-pr-loop.sh:180-191`). Only one RLCR or PR loop may be active (`scripts/setup-pr-loop.sh:220-241`). Requires git repo, at least one commit, authenticated `gh`, installed `codex`, and a valid open PR (`scripts/setup-pr-loop.sh:243-277`, `scripts/setup-pr-loop.sh:351-374`). Branch, model, and effort are YAML-safety checked before writing state (`scripts/setup-pr-loop.sh:380-407`). For re-review cases, setup reuses an existing trigger only if all active bots are mentioned after the latest commit and ignores mentions inside code/quote contexts (`scripts/setup-pr-loop.sh:462-503`).
- dependencies_and_callers: Direct dependency is `scripts/setup-pr-loop.sh`. That script depends on `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, Git, GitHub CLI, Codex CLI, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, and for Claude bot re-trigger confirmation `scripts/check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:33-40`, `scripts/setup-pr-loop.sh:430-438`, `scripts/setup-pr-loop.sh:564-600`). Runtime continuation depends on `hooks/pr-loop-stop-hook.sh`, which finds active PR loops through `find_active_pr_loop` (`hooks/lib/loop-common.sh:914-933`).
- edge_cases_or_failure_modes: If no bot flag is passed, setup aborts before state creation. If a loop already exists, it blocks to prevent cross-loop state corruption. If PR lookup fails, the command instructs PR creation. Fork PR support is handled by resolving current and parent repos and extracting the PR URL repo (`scripts/setup-pr-loop.sh:289-349`). Case 4/5 startup avoids duplicate trigger spam and cleans up the loop dir if posting or Claude eyes verification fails (`scripts/setup-pr-loop.sh:457-600`). The command explicitly forbids bypassing loop termination by editing state or cancel files (`commands/start-pr-loop.md:50-57`).
- validation_or_tests: The assigned file is a command contract; its implementation is exercised by robustness coverage in `tests/robustness/test-setup-scripts-robustness.sh` as found by repository search, including PR-loop argument validation and valid setup progression. The file’s workflow rules also align with PR-loop API/stop-hook robustness tests under `tests/robustness/test-pr-loop-api-robustness.sh`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-051 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the shell-write detection primitive used by the Bash pre-tool hook. It tests `command_modifies_file` from `hooks/lib/loop-common.sh`, which is central to preventing agents from bypassing Write/Edit path guards with shell redirection, `tee`, in-place editors, or file operations.
- algorithmic_behavior: The test lowercases each candidate command with `to_lower` and asserts whether `command_modifies_file "$command_lower" "$pattern"` should return true (`tests/test-bash-validator-patterns.sh:36-62`). It covers redirections (`tests/test-bash-validator-patterns.sh:69-81`), `tee` (`tests/test-bash-validator-patterns.sh:82-94`), in-place editors (`tests/test-bash-validator-patterns.sh:95-108`), `mv/cp/rm` (`tests/test-bash-validator-patterns.sh:109-122`), `dd/truncate/exec/printf` (`tests/test-bash-validator-patterns.sh:123-134`), false positives for read-only commands (`tests/test-bash-validator-patterns.sh:135-153`), positional edge cases and variable non-matches (`tests/test-bash-validator-patterns.sh:154-173`), state file patterns (`tests/test-bash-validator-patterns.sh:174-184`), and round summary filename patterns (`tests/test-bash-validator-patterns.sh:185-194`).
- inputs_outputs_state: Inputs are hardcoded shell command strings and regex file patterns, defaulting to `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:37-55`). Outputs are PASS/FAIL counters and process exit: zero when all assertions pass, one when any assertion fails (`tests/test-bash-validator-patterns.sh:196-214`). It does not mutate repository state.
- gates_or_invariants: The tested invariant is that commands which write or replace protected files must be detected, while common readers such as `cat`, `grep`, `head`, `wc`, `diff`, and `stat` must not be blocked (`tests/test-bash-validator-patterns.sh:142-153`). The production matcher enumerates exact pattern families for redirects, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv/cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` (`hooks/lib/loop-common.sh:1187-1215`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` directly (`tests/test-bash-validator-patterns.sh:11-13`). The production caller is `hooks/loop-bash-validator.sh`, which parses hook JSON, only applies to Bash tool calls, lowercases the command, finds active RLCR/PR loops, and exits early when no loop is active (`hooks/loop-bash-validator.sh:20-70`). The same helper is then used to block RLCR state/finalize-state modifications (`hooks/loop-bash-validator.sh:139-156`), plan backup writes (`hooks/loop-bash-validator.sh:330-341`), goal-tracker writes (`hooks/loop-bash-validator.sh:343-358`), prompt/summary/todos writes (`hooks/loop-bash-validator.sh:360-393`), and PR-loop state/read-only file writes (`hooks/loop-bash-validator.sh:397-443`).
- edge_cases_or_failure_modes: The test documents one known limitation: multiple-source `cp file1 file2 goal-tracker.md` is not detected because the matcher expects two operands (`tests/test-bash-validator-patterns.sh:165-168`). Variable-only destinations such as `echo x > $FILE` are intentionally not treated as a literal target match (`tests/test-bash-validator-patterns.sh:170-172`). The production hook has extra source-side `mv/cp` protection for RLCR state files beyond this helper, including command segment splitting and redirection stripping (`hooks/loop-bash-validator.sh:158-230` and continuing through the same state-source block).
- validation_or_tests: This file is the validation. It reports counted results and exits nonzero on any failed pattern assertion (`tests/test-bash-validator-patterns.sh:196-214`). It covers both generic matcher behavior and specific patterns used by state and summary protections.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-081 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Block-template contract for PR-loop force-push handling. It tells the agent that a non-fast-forward SHA change resets review state and requires a fresh trigger comment.
- algorithmic_behavior: Template variables `{{OLD_COMMIT}}`, `{{NEW_COMMIT}}`, and `{{BOT_MENTION_STRING}}` are rendered into a blocking message (`prompt-template/block/force-push-detected.md:1-17`). The PR stop hook detects whether the stored latest commit is no longer an ancestor of current `HEAD`; if so, it updates tracking and returns a blocking JSON decision using this template (`hooks/pr-loop-stop-hook.sh:365-421`).
- inputs_outputs_state: Inputs are the old stored PR commit SHA, current HEAD SHA, and configured bot mention string. On force push, the stop hook writes `latest_commit_sha` and `latest_commit_at` to the new HEAD, clears `last_trigger_at` and `trigger_comment_id`, and updates in-memory PR variables (`hooks/pr-loop-stop-hook.sh:396-410`). Output is a hook JSON object with decision `block`, a rendered reason, and system message “PR Loop: Force push detected - please re-trigger bots” (`hooks/pr-loop-stop-hook.sh:419-421`).
- gates_or_invariants: The invariant is that pre-force-push trigger comments must not be reused after history rewrite. The hook obtains the new head commit timestamp so later trigger detection only accepts comments after the force push (`hooks/pr-loop-stop-hook.sh:385-394`). The template explicitly requires posting a new trigger comment mentioning the configured bots (`prompt-template/block/force-push-detected.md:7-15`).
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/force-push-detected.md"` (`hooks/pr-loop-stop-hook.sh:412-417`). It depends on template loading from `hooks/lib/template-loader.sh`, bot mention construction from `build_bot_mention_string` (`hooks/lib/loop-common.sh:883-896`), git ancestry checks, and `gh pr view` for commit timestamps.
- edge_cases_or_failure_modes: If GitHub commit timestamp lookup fails, the hook falls back to current UTC time (`hooks/pr-loop-stop-hook.sh:391-394`), which keeps the loop conservative but can require a trigger comment after that fallback timestamp. The hook updates state before blocking, so the next attempt uses the new SHA and empty trigger fields. If old SHA is still an ancestor, this is treated as a normal fast-forward/new-commit case rather than force push (`hooks/pr-loop-stop-hook.sh:373-379`).
- validation_or_tests: Repository search shows force-push protection is part of `hooks/pr-loop-stop-hook.sh`; the assigned template is short and has no direct test file, but its behavior is exercised indirectly by PR-loop stop-hook/API robustness coverage in `tests/robustness/test-pr-loop-api-robustness.sh` and by the stop-hook force-push code path.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-111 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Prompt-template contract for RLCR Agent Teams mode. It changes the initial implementation prompt from single-agent execution to team-leader orchestration with delegated team members.
- algorithmic_behavior: The template instructs the primary agent to split work into independent units, create agent teams using the Task tool with `team_name`, coordinate non-overlapping file ownership, monitor blockers, review team output, and avoid doing implementation work directly (`prompt-template/claude/agent-teams-instructions.md:1-27`). It therefore defines coordination semantics rather than code-level validation.
- inputs_outputs_state: Input is the `--agent-teams` flag to `scripts/setup-rlcr-loop.sh` (`scripts/setup-rlcr-loop.sh:86-88`, `scripts/setup-rlcr-loop.sh:225-227`). The script records `agent_teams: true|false` in RLCR state (`scripts/setup-rlcr-loop.sh:767-785`) and appends this template to `round-0-prompt.md` when enabled (`scripts/setup-rlcr-loop.sh:1002-1008`). Output is an augmented round-zero prompt requiring team-leader behavior.
- gates_or_invariants: `--agent-teams` is gated by `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; without that environment variable, setup aborts with guidance before creating loop artifacts (`scripts/setup-rlcr-loop.sh:282-298`). The template’s invariants include independent parallelizable task boundaries, no same-file assignment to multiple members, team progress tracked via TaskList, and quality review before completion (`prompt-template/claude/agent-teams-instructions.md:14-22`).
- dependencies_and_callers: Called only from `scripts/setup-rlcr-loop.sh` during initial prompt creation. It shares RLCR setup dependencies: active loop exclusion (`scripts/setup-rlcr-loop.sh:260-280`), plan validation, Codex availability, state creation, goal tracker generation, and prompt assembly. The stop hook also has a separate continuation template path for agent teams during implementation phase (`hooks/loop-codex-stop-hook.sh:1630-1632`), indicating this initial template participates in a broader prompt lifecycle.
- edge_cases_or_failure_modes: If the template file is missing, setup falls back to an inline shorter Agent Teams instruction block (`scripts/setup-rlcr-loop.sh:1009-1018`). The template can create coordination risk if tasks are not cleanly partitioned; it explicitly mitigates this by requiring file ownership boundaries and leader review (`prompt-template/claude/agent-teams-instructions.md:16-21`). Because each team member is told to commit independently (`prompt-template/claude/agent-teams-instructions.md:21`), integration conflicts are an expected operational edge case and are delegated back to the leader (`prompt-template/claude/agent-teams-instructions.md:27`).
- validation_or_tests: Validation is primarily setup-time: the environment-variable gate prevents unsupported mode activation (`scripts/setup-rlcr-loop.sh:282-298`). The template injection path is deterministic and has an inline fallback (`scripts/setup-rlcr-loop.sh:1002-1018`). No direct unit test for this exact template was found in the assigned files.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-141 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR plan-file path and content validation in `scripts/setup-rlcr-loop.sh`. It treats path validation as a security boundary for the plan input that drives the RLCR algorithm.
- algorithmic_behavior: The test creates a temp git repo, installs a mock `codex`, creates valid or invalid plan files, then invokes `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"` (`tests/robustness/test-path-validation-robustness.sh:12-39`, `tests/robustness/test-path-validation-robustness.sh:76-113`). Its classifier treats known plan validation errors as rejection and treats later “requires codex” or “must be gitignored” errors as evidence that path/content validation passed (`tests/robustness/test-path-validation-robustness.sh:91-105`).
- inputs_outputs_state: Inputs are plan paths and generated file contents. Positive cases include normal relative paths, root filenames, dash/underscore, nested directories, dots, long filenames, and deep nesting (`tests/robustness/test-path-validation-robustness.sh:119-169`, `tests/robustness/test-path-validation-robustness.sh:387-412`). Negative cases include absolute paths, spaces, shell metacharacters, symlinked file and parent directory, missing file, directory-as-file, empty/short/comment-only content (`tests/robustness/test-path-validation-robustness.sh:179-332`, `tests/robustness/test-path-validation-robustness.sh:342-377`, `tests/robustness/test-path-validation-robustness.sh:425-480`). It outputs PASS/FAIL counts through `print_test_summary` and exits with that status (`tests/robustness/test-path-validation-robustness.sh:482-487`).
- gates_or_invariants: Production validation rejects absolute paths, whitespace in path, shell metacharacters, direct symlink plan files, missing parent dirs, symlink parent path segments, non-files, unreadable files, paths outside the project, submodule-contained plans, and invalid tracking state (`scripts/setup-rlcr-loop.sh:356-508`). It requires at least five lines and at least three meaningful non-comment content lines (`scripts/setup-rlcr-loop.sh:519-572`). It also requires a git repo with at least one commit before plan validation proceeds (`scripts/setup-rlcr-loop.sh:330-344`) and Codex availability after plan/content validation (`scripts/setup-rlcr-loop.sh:579-585`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` for temp directory and summary helpers (`tests/robustness/test-path-validation-robustness.sh:12-16`; helpers at `tests/test-helpers.sh:58-89`). Calls `scripts/setup-rlcr-loop.sh`, which depends on `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:25-31`). The setup script also blocks concurrent RLCR/PR loops via `find_active_loop` and `find_active_pr_loop` before validating the plan (`scripts/setup-rlcr-loop.sh:260-280`).
- edge_cases_or_failure_modes: Unicode/CJK/emoji path characters are explicitly allowed as user content paths, while shell metacharacters remain rejected (`tests/robustness/test-path-validation-robustness.sh:414-415`; production metacharacter gate at `scripts/setup-rlcr-loop.sh:370-377`). Long filename tests are skipped as passed if the filesystem rejects file creation (`tests/robustness/test-path-validation-robustness.sh:387-400`). The comments-only test is stricter than ordinary markdown semantics because production treats lines starting with `#` as comments rather than headings when counting meaningful content (`scripts/setup-rlcr-loop.sh:529-568`).
- validation_or_tests: This file is the validation suite and is tightly mapped to production error messages. It intentionally runs production setup rather than a mock validator, with only Codex mocked to keep tests fast (`tests/robustness/test-path-validation-robustness.sh:18-35`). The final summary is generated by shared helper counters and determines the shell exit code (`tests/robustness/test-path-validation-robustness.sh:486-487`; `tests/test-helpers.sh:58-78`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV_RLCR_WITH_SWARM_TEAM-HZ-021, DEV_RLCR_WITH_SWARM_TEAM-HZ-051, DEV_RLCR_WITH_SWARM_TEAM-HZ-081, DEV_RLCR_WITH_SWARM_TEAM-HZ-111, DEV_RLCR_WITH_SWARM_TEAM-HZ-141
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`