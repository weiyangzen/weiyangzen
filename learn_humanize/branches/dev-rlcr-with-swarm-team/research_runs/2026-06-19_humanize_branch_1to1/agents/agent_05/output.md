# agent_05 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Prompt contract surface for the Humanize RLCR and PR-loop algorithms. The directory is not passive prose: hooks and setup scripts render these templates into blocking decisions, next-round prompts, review instructions, generated plan skeletons, goal trackers, and PR-loop task instructions.
- algorithmic_behavior: The tree has five child roles: `block/` holds denial reasons and recovery actions for validators and stop hooks, including git cleanliness, wrong-round access, protected state, plan mutation, missing triggers, and bot timeout templates; `claude/` holds worker-side phase prompts for next-round work, review remediation, finalize flow, goal-tracker update requests, agent-teams delegation, and push reminders; `codex/` holds reviewer-side prompts for regular review, full alignment audit, goal tracker mutation authority, and `codex review` audit metadata; `pr-loop/` holds PR-loop bootstrap prompts, critical completion requirements, initial goal tracker, and Codex goal-tracker update instructions; `plan/` holds the generated implementation-plan skeleton. Recursive inventory found 49 markdown templates and 1,140 total lines under this directory.
- inputs_outputs_state: Inputs are `{{VAR}}` placeholder values supplied by hook/script callers. The renderer is `hooks/lib/template-loader.sh`: `get_template_dir` resolves `prompt-template` from `hooks/lib` at lines 24-31; `load_template` reads a relative template at lines 33-48; `render_template` does single-pass placeholder replacement at lines 50-132; `load_and_render_safe` substitutes a fallback if the template is missing or renders empty at lines 167-203. Outputs are human-facing markdown strings embedded in hook JSON `reason` payloads, generated prompt files, goal trackers, or plan files.
- gates_or_invariants: Placeholder syntax is uppercase `{{VARIABLE_NAME}}` and missing variables remain literal, per `hooks/lib/template-loader.sh:7-13`. The single-pass renderer intentionally prevents placeholder injection from substituted content, per `hooks/lib/template-loader.sh:54-57`. `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:205-221`. `.claude/CLAUDE.md:8-9` adds a sync invariant between `commands/gen-plan.md` and `prompt-template/plan/gen-plan-template.md`.
- dependencies_and_callers: Hook callers include `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, and `hooks/pr-loop-stop-hook.sh`; setup callers include `scripts/setup-pr-loop.sh`. `scripts/validate-gen-plan-io.sh:133-149` copies the plan template to the requested output file. `commands/gen-plan.md:142-247` embeds the same plan structure and generation rules.
- edge_cases_or_failure_modes: Missing templates can otherwise lead to empty block reasons, which is why critical render paths use `load_and_render_safe` fallback behavior. Bad placeholder syntax, lowercase placeholders, extra braces, or stale plan-template sync can weaken generated prompts without changing shell code. Unreferenced templates may be legitimate dynamic assets, but missing referenced templates are critical.
- validation_or_tests: `tests/test-template-loader.sh:41-208` validates directory resolution, template loading, single/multiple/multiline substitution, unreplaced-variable behavior, and safe fallback. `tests/test-template-references.sh:51-229` scans shell template references, checks referenced files exist, and requires critical validators to use safe rendering. `tests/test-templates-comprehensive.sh:83-194` loads every template and validates placeholder syntax. `tests/run-all-tests.sh:32-76` includes these template suites plus gen-plan and PR-loop suites.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-035 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop gate that translates GitHub reaction state into loop approvals or readiness checks. It detects Codex `+1` reactions on a PR body and Claude `eyes` reactions on trigger comments.
- algorithmic_behavior: The script exposes two commands. `codex-thumbsup` parses a PR number and optional `--after` timestamp at lines 86-117, resolves the base repo for fork PRs by trying current repo then parent repo at lines 119-150, paginates issue reactions with `gh api` and merges pages through `jq -s 'add // []'` at lines 152-162, selects the first `chatgpt-codex-connector[bot]` `+1` at lines 164-168, rejects missing or older-than-filter reactions at lines 170-183, then prints the reaction JSON at lines 185-187. `claude-eyes` parses comment id, optional `--pr`, retry count, and delay at lines 190-230, resolves the base repo similarly at lines 232-262, then loops `seq 1 "$MAX_RETRIES"` with sleep, paginated comment-reaction fetch, and selection of `claude[bot]` `eyes` at lines 264-300.
- inputs_outputs_state: Inputs are CLI args, GitHub CLI context, optional current-parent repo relationship, and reaction JSON from GitHub. Outputs are a single reaction JSON object on stdout when found, diagnostic retry/timeout messages on stderr, and exit codes: `0` found, `1` not found or timed out, `2` argument/API error. It does not write repository state directly; state transitions are performed by callers.
- gates_or_invariants: Uses `set -euo pipefail` at line 18. All `gh` invocations are wrapped by `run_with_timeout` from `scripts/portable-timeout.sh`, sourced at lines 31-33. Pagination is mandatory for correctness beyond the default reaction page size, noted at lines 155-156 and 271-272. The `--after` comparison is lexicographic ISO timestamp comparison at lines 177-183, so correctness assumes comparable ISO 8601 strings.
- dependencies_and_callers: Depends on `gh`, `jq`, and `scripts/portable-timeout.sh`, whose timeout implementation selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at `scripts/portable-timeout.sh:9-71`. `scripts/setup-pr-loop.sh:564-599` blocks setup if Claude eyes cannot be verified. `hooks/pr-loop-stop-hook.sh:662-719` removes Codex from `active_bots` or approves the loop when a valid `+1` exists; `hooks/pr-loop-stop-hook.sh:768-790` blocks when Claude eyes is missing; `hooks/pr-loop-stop-hook.sh:985-1004` treats Codex `+1` during polling as approval.
- edge_cases_or_failure_modes: Missing PR number/comment id, unknown options, or multiple positional identifiers return exit `2`. Parent-repo lookup can fall back to an empty/current repo value if repo discovery fails. API errors in `codex-thumbsup` are fatal exit `2`; API errors in `claude-eyes` are swallowed per attempt and can become exit `1`, which callers treat as not ready. `claude-eyes` sleeps before the first check, so even `--retry 1` waits for the delay. Invalid retry values are not explicitly numeric-validated before `seq`.
- validation_or_tests: Direct reaction tests live in `tests/test-pr-loop-system.sh:199-230`, covering Codex `+1`, `--after` filtering, and Claude eyes with `--retry 1 --delay 0`. The `gh` mock routes PR and comment reaction endpoints to fixtures at `tests/mocks/gh:104-152`, and repo/PR view behavior at `tests/mocks/gh:27-72` and `197-225`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-065 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop script CLI contracts and one core cancellation state transition. Despite the filename, it focuses on setup/cancel/fetch/poll scripts rather than bot-reaction logic.
- algorithmic_behavior: `run_setup_tests` validates `scripts/setup-pr-loop.sh` help, required bot flags, invalid options, `--claude`, `--codex`, combined bot flags, `--max`, invalid `--max`, `--codex-model`, and `--codex-timeout` at lines 18-166. `run_cancel_tests` validates help, no-loop behavior, and active loop cancellation by creating `.humanize/pr-loop/<timestamp>/state.md` then expecting `cancel-state.md` and no `state.md` at lines 172-253. `run_fetch_tests` validates `fetch-pr-comments.sh` help, missing PR, missing output, and invalid PR number at lines 259-326. `run_poll_tests` validates `poll-pr-reviews.sh` help, missing PR, missing `--after`, and missing `--bots` at lines 332-399. `run_script_tests` orchestrates all four groups at lines 405-410.
- inputs_outputs_state: Inputs are `PROJECT_ROOT`, `TEST_DIR`, test helper functions `pass` and `fail`, and the real scripts under `scripts/`. It executes scripts and inspects exit codes/stdout/stderr. The only filesystem state mutation in this file is test fixture state under `TEST_DIR/.humanize/pr-loop/<timestamp>`, where it asserts `cancel-pr-loop.sh` renames `state.md` to `cancel-state.md`.
- gates_or_invariants: Setup requires at least one bot flag; `setup-pr-loop.sh:111-180` parses and validates the options the test covers. Cancellation must report `NO_LOOP` with exit `1` when no active loop exists and must preserve canceled state as `cancel-state.md`; `scripts/cancel-pr-loop.sh:72-132` implements that. Fetch requires numeric PR and output path; `scripts/fetch-pr-comments.sh:27-106` implements that. Poll requires numeric PR, `--after`, and `--bots`; `scripts/poll-pr-reviews.sh:23-102` implements that.
- dependencies_and_callers: Intended to be sourced by a broader harness that defines `PROJECT_ROOT`, `TEST_DIR`, `pass`, and `fail` as indicated by its usage comment at line 11. It is not listed directly in `tests/run-all-tests.sh`, but related broad PR-loop suites are listed at `tests/run-all-tests.sh:52-54`.
- edge_cases_or_failure_modes: Because the tests often allow scripts to fail later and only check absence/presence of a specific parser error, they are parser-contract tests rather than full integration tests. They depend on message substrings such as "at least one bot flag", "unknown option", "must be.*integer", `NO_LOOP`, and `CANCELLED`; wording-only changes can fail tests. The active-loop test assumes newest-loop selection by sortable timestamp and uses `CLAUDE_PROJECT_DIR` to constrain lookup to `TEST_DIR`.
- validation_or_tests: This file is itself validation. It covers positive and negative CLI parsing plus cancellation state mutation, but it does not test `scripts/check-bot-reactions.sh`; reaction coverage is in `tests/test-pr-loop-system.sh:199-230`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-095 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR plan-integrity gate. It explains that the tracked plan changed during an active session and forces cancellation/restart rather than letting the session continue with mutated requirements.
- algorithmic_behavior: The template is short but stateful through placeholders: line 3 names `{{PLAN_FILE}}`, lines 7-10 prescribe cancel-update-restart recovery, and line 12 exposes `{{BACKUP_PATH}}`. It is rendered when `hooks/loop-codex-stop-hook.sh` detects content drift between the active plan and its loop backup.
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH`. Output is markdown used as the JSON `reason` in a stop-hook block decision. The hook also emits `systemMessage: "Loop: Blocked - plan file modified"` at `hooks/loop-codex-stop-hook.sh:335-336`.
- gates_or_invariants: The invariant is requirement-source immutability during active RLCR sessions. `hooks/loop-codex-stop-hook.sh:300-315` separately blocks uncommitted tracked plan changes when `--track-plan-file` is active, and `hooks/loop-codex-stop-hook.sh:318-338` blocks if `diff -q "$FULL_PLAN_PATH" "$BACKUP_PLAN"` finds content changes.
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md"` at `hooks/loop-codex-stop-hook.sh:332-334`; fallback text at lines 320-331 preserves the same essential contract if the template is missing. Depends on the template loader and on setup creating the backup plan path referenced by the hook.
- edge_cases_or_failure_modes: The surrounding hook comment at `hooks/loop-codex-stop-hook.sh:318` says plan changes are now allowed because `plan.md` is a symlink, but the following diff gate still blocks when active plan and backup differ. That mismatch is a documentation/comment risk. If placeholder rendering fails, fallback still blocks. If backup is missing or diff errors for reasons other than content drift, the same block path may fire.
- validation_or_tests: Template existence and safe-render coverage are included by `tests/test-template-references.sh:51-229`; general loading/syntax coverage by `tests/test-templates-comprehensive.sh:83-194`. Plan-file hook robustness is included in the test suite list at `tests/run-all-tests.sh:37,41,66`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-125 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Canonical skeleton for generated implementation plans. It shapes downstream RLCR work by requiring goal description, testable acceptance criteria, path boundaries, feasible references, dependency sequencing, and code-style constraints.
- algorithmic_behavior: The template requires a goal section at lines 1-4, TDD-style acceptance criteria with positive and negative tests at lines 6-23, path boundaries with upper/lower scope and allowed choices at lines 25-44, non-prescriptive feasibility hints at lines 46-55, dependency/milestone sequencing without time estimates at lines 57-67, and implementation notes forbidding plan-specific terminology in code/comments at lines 69-74.
- inputs_outputs_state: Input is the template file selected by `scripts/validate-gen-plan-io.sh`; output is a new plan file. The validator resolves the template from `CLAUDE_PLUGIN_ROOT` or script-relative fallback at `scripts/validate-gen-plan-io.sh:133-140`, errors with exit `7` if missing at lines 142-145, copies it to the output at lines 148-149, then appends the original draft between explicit start/end markers at lines 151-157.
- gates_or_invariants: `scripts/validate-gen-plan-io.sh:79-124` gates generation on existing non-empty input, existing output directory, non-existing output file path, and writable output directory before copying this template. `commands/gen-plan.md:225-247` adds generation invariants: AC-X format, no time estimates, path-only code references, deterministic-design respect, positive/negative tests per AC, code-style constraint, and complete preservation of all draft information. `.claude/CLAUDE.md:8-9` requires this template and the command’s Plan Structure section to stay synchronized.
- dependencies_and_callers: Called by `commands/gen-plan.md:30-47` through `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh`. The command then performs relevance checking, draft analysis, issue clarification, and final plan writing using the structure mirrored from this template at `commands/gen-plan.md:142-247`.
- edge_cases_or_failure_modes: Missing template is treated as plugin configuration error exit `7`. Existing output file blocks generation, so retries require a new path or cleanup. Because the template is manually mirrored in `commands/gen-plan.md`, unsynchronized edits create conflicting instructions. The template contains placeholders like `<...>` for humans/agents to replace, not renderer variables.
- validation_or_tests: `tests/test-gen-plan.sh:47-180` validates gen-plan command structure, allowed tools, argument hint, relevance-checker agent metadata, and version consistency; the IO validator itself enforces path and template existence gates. Template load and placeholder syntax are covered by `tests/test-templates-comprehensive.sh:83-194`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, each with one assigned section heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`