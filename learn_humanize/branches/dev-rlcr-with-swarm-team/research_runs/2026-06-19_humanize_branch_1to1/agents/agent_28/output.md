# agent_28 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit safety gate for an active RLCR loop. It blocks prompt submission when loop state is malformed, when the checkout branch has drifted from the loop start branch, or when the plan-file tracking invariant no longer matches the loop mode. The file declares its scope directly: schema version, branch consistency, and plan-file tracking status checks at `hooks/loop-plan-file-validator.sh:3-8`.
- algorithmic_behavior: Reads hook JSON from stdin (`INPUT=$(cat)`) at `hooks/loop-plan-file-validator.sh:26-30`, extracts `session_id`, resolves the active loop under `.humanize/rlcr`, then exits allow/quietly when no active loop exists at `hooks/loop-plan-file-validator.sh:32-39`. For an active loop it chooses `finalize-state.md` before `state.md` via `resolve_active_state_file` at `hooks/loop-plan-file-validator.sh:41-42`, strict-parses it at `hooks/loop-plan-file-validator.sh:44-48`, maps parsed `STATE_*` values into local `PLAN_TRACKED`, `PLAN_FILE`, and `START_BRANCH` at `hooks/loop-plan-file-validator.sh:50-53`, and performs ordered checks: required schema fields, current branch equality, then tracked/untracked plan-file mode.
- inputs_outputs_state: Inputs are hook stdin JSON, optional `.session_id`, `CLAUDE_PROJECT_DIR` or current working directory, `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md}`, git branch state, and the plan file path from YAML frontmatter. Outputs are either no output with exit `0` for allow/no-loop, JSON `{"decision":"block","reason":...}` with exit `0` for user-correctable blocks, or stderr plus exit `1` for malformed state. It does not mutate files. State consumed includes `plan_tracked`, `plan_file`, and `start_branch`; strict parser additionally requires `current_round`, `max_iterations`, `review_started`, and `base_branch` in `hooks/lib/loop-common.sh:386-448`.
- gates_or_invariants: Required `plan_tracked` and `start_branch` must be present after strict parse, otherwise a rendered schema-outdated block is emitted through `schema_validation_error` at `hooks/loop-plan-file-validator.sh:59-89`. Current git branch must equal `start_branch`; branch mismatch blocks with the started/current branch values at `hooks/loop-plan-file-validator.sh:95-115`. If `plan_tracked=true`, the plan must still be tracked by git and have no porcelain status at `hooks/loop-plan-file-validator.sh:123-192`. If `plan_tracked` is not true, the plan must remain untracked/gitignored; a tracked plan blocks at `hooks/loop-plan-file-validator.sh:193-228`. Git timeouts or unexpected git errors fail closed through JSON blocks at `hooks/loop-plan-file-validator.sh:95-105`, `hooks/loop-plan-file-validator.sh:127-147`, `hooks/loop-plan-file-validator.sh:152-172`, and `hooks/loop-plan-file-validator.sh:197-217`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` at `hooks/loop-plan-file-validator.sh:16-17` for `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, field constants, strict parsing, and template loading. Depends on `scripts/portable-timeout.sh` at `hooks/loop-plan-file-validator.sh:19-24`; `run_with_timeout` chooses `gtimeout`, GNU `timeout`, Python subprocess timeout, or no-timeout fallback in `scripts/portable-timeout.sh:9-71`. Template output uses `load_and_render_safe` for schema messages at `hooks/loop-plan-file-validator.sh:64-69`; that loader is implemented in `hooks/lib/template-loader.sh:170-203`. Tests invoke this hook directly in `tests/test-plan-file-hooks.sh:123-160`, `tests/test-finalize-phase.sh:853-865`, and `tests/robustness/test-hook-system-robustness.sh:199-237`.
- edge_cases_or_failure_modes: No active loop is allowed silently (`hooks/loop-plan-file-validator.sh:36-39`). Malformed state is a hard fail with exit `1` at `hooks/loop-plan-file-validator.sh:44-48`, unlike user-correctable branch/tracking blocks that exit `0` with JSON. State file selection handles finalize phase by preferring `finalize-state.md`; this behavior is tested at `tests/test-finalize-phase.sh:853-865`. YAML-quoted `plan_file` and `start_branch` are handled by shared parsing and tested in `tests/test-plan-file-hooks.sh:134-147` and `tests/test-plan-file-hooks.sh:373-421`. Hyphenated plan paths are tested at `tests/test-plan-file-hooks.sh:469-500`. Git command timeout returns `124` from the portable timeout layer and causes fail-closed blocks for tracking/status checks.
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers valid state pass-through, malformed/missing fields, branch changes, quoted YAML fields, hyphenated plan paths, and downstream plan-integrity behavior. `tests/test-finalize-phase.sh:853-865` verifies `finalize-state.md` parsing. `tests/robustness/test-hook-system-robustness.sh:232-243` verifies empty JSON does not crash. Shared strict parsing robustness appears in `tests/robustness/test-concurrent-state-robustness.sh` and `tests/robustness/test-state-transition-robustness.sh` references found in the repo.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-058 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for `.humanize` escape prevention and shell-glob safety. It verifies that broad or forced `git add` commands cannot accidentally or deliberately add local loop state under `.humanize`, and that directory iteration patterns avoid zsh/bash glob failures in empty or dotfile-only loop directories.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` at `tests/test-humanize-escape.sh:16-19`, then defines `assert_blocks` and `assert_allows` wrappers that lowercase commands through `to_lower` and call `git_adds_humanize` at `tests/test-humanize-escape.sh:44-70`. It then enumerates command cases by semantic group: direct `.humanize` path variants, quoted variants, force/all/broad variants, chained commands, `git -C`/`--git-dir` variants, allowed safe commands, and find-based directory iteration cases.
- inputs_outputs_state: Inputs are literal command strings and temporary directories created under `/tmp`. Outputs are PASS/FAIL lines and final process exit `0` only when all assertions pass (`tests/test-humanize-escape.sh:337-352`). The test mutates only temporary directories such as `/tmp/test-humanize-git-add-$$`, `/tmp/test-humanize-empty-$$`, `/tmp/test-humanize-dotfiles-$$`, and related state-detection directories, then removes them at each test block.
- gates_or_invariants: Direct `.humanize` paths must block for forms such as `./.humanize`, nested `path/to/.humanize`, parent-relative `../project/.humanize`, and trailing/file subpaths at `tests/test-humanize-escape.sh:83-87`. Quoted forms must block at `tests/test-humanize-escape.sh:96-100`. Force plus broad scope or all-scope must block at `tests/test-humanize-escape.sh:109-119`. `git add -A` and `--all` block when `.humanize` exists at `tests/test-humanize-escape.sh:128-144`. Chained segments and git global options must still block at `tests/test-humanize-escape.sh:153-166`. Safe specific file adds, non-add git commands, patch mode, and `.humanize*` lookalikes must not block at `tests/test-humanize-escape.sh:175-192`.
- dependencies_and_callers: Core dependency is `git_adds_humanize` in `hooks/lib/loop-common.sh:1027-1145`. That function splits shell segments on `&&`, `||`, `|`, and `;`, detects `git ... add`, strips quotes for direct path matching, detects force/all/broad-scope flags, consults `.humanize` existence, and uses `git check-ignore` for broad non-force adds. `to_lower` is defined in `hooks/lib/loop-common.sh:513-516`; the lowercasing explains why `git_adds_humanize` treats `-A` as `-a` (`hooks/lib/loop-common.sh:1031-1032`). The production caller is `hooks/loop-bash-validator.sh`, which calls `git_adds_humanize "$COMMAND_LOWER"` and emits `git_add_humanize_blocked_message` with exit `2` at `hooks/loop-bash-validator.sh:109-118`.
- edge_cases_or_failure_modes: The test encodes a key context-dependent behavior: non-force `-A/--all` blocks only when a `.humanize` directory exists, so it explicitly creates one before those assertions (`tests/test-humanize-escape.sh:128-135`). It guards against false positives on `.humanizeconfig`, `.humanize-backup`, and `.humanizerc` at `tests/test-humanize-escape.sh:189-192`. It also verifies find-based iteration handles empty directories, dotfiles-only directories, no `*-state.md`, one `*-state.md`, and timestamped session directory selection without shell glob expansion errors at `tests/test-humanize-escape.sh:201-332`.
- validation_or_tests: This file is itself the validator specification. It returns nonzero if any assertion fails (`tests/test-humanize-escape.sh:344-352`). It specifically validates fixes listed in its header: no zsh/bash “no matches found” errors and blocking `.humanize` path variants at `tests/test-humanize-escape.sh:3-12`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-088 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for a Bash-tool write attempt against `goal-tracker.md`. It is part of the RLCR guardrail surface that forces edits through Write/Edit tooling instead of shell redirection or in-place shell mutation, preserving hook visibility.
- algorithmic_behavior: Contains a short rendered message: title, prohibition on Bash commands modifying `goal-tracker.md`, a correct path placeholder, and rationale that shell commands bypass validation hooks at `prompt-template/block/goal-tracker-bash-write.md:1-8`. The only dynamic field is `{{CORRECT_PATH}}` at `prompt-template/block/goal-tracker-bash-write.md:5`.
- inputs_outputs_state: Input is the template variable `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message "$correct_path"` in `hooks/lib/loop-common.sh:611-620`. Output is a rendered Markdown block written to stderr by callers. It does not modify state; it provides the user-facing reason for a gate decision.
- gates_or_invariants: The invariant is that Round 0 `goal-tracker.md` Bash modifications must use Write/Edit rather than Bash because Bash redirection/`sed`/`awk`/`cat` can bypass file validation hooks. The template text states this at `prompt-template/block/goal-tracker-bash-write.md:3-8`. In production, `hooks/loop-bash-validator.sh` detects command writes to `goal-tracker.md` and, when `CURRENT_ROUND == 0`, emits this template and exits `2` at `hooks/loop-bash-validator.sh:349-352`.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-bash-write.md" ... "CORRECT_PATH=$correct_path"` in `hooks/lib/loop-common.sh:611-620`. `load_and_render_safe` renders placeholders and falls back if the template is missing or empty in `hooks/lib/template-loader.sh:170-203`. `tests/test-template-references.sh:152-159` includes `block/goal-tracker-bash-write.md` in the required common-template inventory.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the fallback in `goal_tracker_bash_blocked_message` still blocks with the correct path (`hooks/lib/loop-common.sh:615-619`). If `CORRECT_PATH` contains template-looking text, rendering is single-pass, so injected `{{...}}` in values is not recursively expanded; this is guaranteed by `hooks/lib/template-loader.sh:50-58` and implementation at `hooks/lib/template-loader.sh:71-131`.
- validation_or_tests: Cross-reference existence is tested by `tests/test-template-references.sh:145-165`. The generic template loader behavior that protects this template is covered by `tests/robustness/test-template-error-robustness.sh`, especially fallback and malformed/template value cases at `tests/robustness/test-template-error-robustness.sh:34-63` and `tests/robustness/test-template-error-robustness.sh:165-186`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-118 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Next-round instruction block appended after a Full Goal Alignment Check. It changes the loop’s next action priorities toward forgotten tasks, unmet acceptance criteria, and unjustified deferrals.
- algorithmic_behavior: Static Markdown with a heading and three priority bullets: Forgotten Items, AC Status, and Deferred Items at `prompt-template/claude/post-alignment-action-items.md:2-7`. It has no placeholders. The Codex stop hook appends it only when `FULL_ALIGNMENT_CHECK == true`: load template at `hooks/loop-codex-stop-hook.sh:1600-1603`, append to `NEXT_PROMPT_FILE` if non-empty at `hooks/loop-codex-stop-hook.sh:1603-1605`.
- inputs_outputs_state: Input is the boolean stop-hook state `FULL_ALIGNMENT_CHECK`. Output is extra text appended to the next round prompt file. It does not write loop state directly, but it influences the next worker/Claude round by reprioritizing action items discovered during a full alignment review.
- gates_or_invariants: The implicit gate is conditional injection only after full alignment checks; regular rounds do not receive this block. The content enforces review-contract priorities: address ignored tasks, prioritize NOT MET acceptance criteria, and un-defer unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:4-7`).
- dependencies_and_callers: Consumed by `hooks/loop-codex-stop-hook.sh:1600-1606`. It is loaded with `load_template`, not the safe fallback wrapper; if missing or empty, the hook simply skips appending because of the `[[ -n "$POST_ALIGNMENT" ]]` guard at `hooks/loop-codex-stop-hook.sh:1602-1605`. It coordinates with the surrounding next-round prompt build: base next prompt is rendered at `hooks/loop-codex-stop-hook.sh:1550-1562`, open-question notices may be inserted at `hooks/loop-codex-stop-hook.sh:1564-1598`, then this post-alignment block is appended before the footer at `hooks/loop-codex-stop-hook.sh:1608-1612`.
- edge_cases_or_failure_modes: Missing template degrades silently to no post-alignment action block because the hook uses `load_template` and only appends non-empty content. Since the template is static, no placeholder-substitution failure is possible. The first blank line at `prompt-template/claude/post-alignment-action-items.md:1` is harmless but means appended output begins with spacing before the heading.
- validation_or_tests: Covered indirectly by stop-hook prompt generation behavior and template inventory/reference tests. Search results show the direct production reference at `hooks/loop-codex-stop-hook.sh:1602`. This item is small but core-relevant because it defines the post-alignment transition contract for subsequent loop work.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-148 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the RLCR prompt-template system under error and edge conditions. It verifies template loading and rendering fail soft where required, preserve content safety, and tolerate filesystem/concurrency edge cases.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and shared test helpers at `tests/robustness/test-template-error-robustness.sh:15-19`, creates an isolated temp directory via `setup_test_dir` at `tests/robustness/test-template-error-robustness.sh:20`, then runs 20 numbered tests grouped by missing templates, malformed placeholders, invalid variable names, variable-value edge cases, filesystem cases, concurrent loading, and discovery cases. It exits with `print_test_summary` status at `tests/robustness/test-template-error-robustness.sh:336-341`.
- inputs_outputs_state: Inputs are generated template strings, generated files under `$TEST_DIR`, fallback strings, and variable assignments. Outputs are PASS/FAIL lines plus summary exit code. State mutations are confined to the temp directory created by `tests/test-helpers.sh:84-89`, with cleanup registered by trap.
- gates_or_invariants: Missing template file or missing template directory must return fallback at `tests/robustness/test-template-error-robustness.sh:34-51`. Empty templates must either return empty or fallback without crashing at `tests/robustness/test-template-error-robustness.sh:53-63`. Malformed placeholder forms must produce non-empty output rather than crash at `tests/robustness/test-template-error-robustness.sh:73-115`. Invalid names with spaces, special characters, or numeric names must be handled without empty/crash outputs at `tests/robustness/test-template-error-robustness.sh:125-155`. Values containing `{{OTHER}}` must not recursively expand at `tests/robustness/test-template-error-robustness.sh:165-174`; multiline values should preserve content at `tests/robustness/test-template-error-robustness.sh:176-186`.
- dependencies_and_callers: Tests `render_template` and `load_and_render_safe` from `hooks/lib/template-loader.sh`. The loader states single-pass substitution and missing-variable preservation in `hooks/lib/template-loader.sh:7-14`, implements scanning substitution in `hooks/lib/template-loader.sh:58-131`, and implements fallback-on-missing/empty in `hooks/lib/template-loader.sh:170-203`. It uses `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh:30-78`.
- edge_cases_or_failure_modes: Filesystem cases include UTF-8 BOM templates at `tests/robustness/test-template-error-robustness.sh:208-218`, whitespace-only templates at `tests/robustness/test-template-error-robustness.sh:220-226`, filenames with spaces at `tests/robustness/test-template-error-robustness.sh:228-243`, permission-denied files when not root at `tests/robustness/test-template-error-robustness.sh:245-260`, symlinked templates at `tests/robustness/test-template-error-robustness.sh:320-334`, and templates in subdirectories at `tests/robustness/test-template-error-robustness.sh:309-318`. Concurrency is tested by launching 10 background loads and requiring zero failures plus file preservation at `tests/robustness/test-template-error-robustness.sh:270-299`.
- validation_or_tests: This file is itself the robustness test. It also complements broader template tests referenced in the repo, including `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/robustness/test-template-stress-robustness.sh`. Its direct target, `load_and_render_safe`, is important to critical block-message paths such as `hooks/loop-plan-file-validator.sh:64-69` and `hooks/lib/loop-common.sh:611-620`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DEV_RLCR_WITH_SWARM_TEAM-HZ-028`, `DEV_RLCR_WITH_SWARM_TEAM-HZ-058`, `DEV_RLCR_WITH_SWARM_TEAM-HZ-088`, `DEV_RLCR_WITH_SWARM_TEAM-HZ-118`, `DEV_RLCR_WITH_SWARM_TEAM-HZ-148`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`