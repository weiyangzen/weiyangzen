# agent_09 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for RLCR and PR-loop hooks. Recursive inspection found two files only: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. Together they define state-file schema constants, active-loop discovery, session isolation, prompt/template rendering, review-result detection, protected-file gates, PR-loop bot mapping, and goal-tracker mutation helpers.
- algorithmic_behavior: `template-loader.sh` resolves `prompt-template` from the hook library path (`hooks/lib/template-loader.sh:26`), loads templates (`:36`), renders `{{VAR}}` placeholders in a single awk pass using `TMPL_VAR_` environment variables (`:58`, `:74`), preserves unknown placeholders (`:119`), and provides fallback rendering (`:170`). `loop-common.sh` source-guards itself (`hooks/lib/loop-common.sh:16`), declares canonical state fields and defaults (`:24`, `:42`), validates hook JSON and UTF-8/null-byte safety (`:76`), extracts `session_id` (`:171`), resolves active or terminal state files (`:180`, `:197`), discovers active loop directories with or without session filtering (`:233`), parses state frontmatter in tolerant and strict modes (`:312`, `:352`, `:386`), detects Codex review issues from `[P?]` markers near line starts (`:468`), and updates PR goal trackers idempotently (`:1287`).
- inputs_outputs_state: Inputs include hook JSON, `.humanize/rlcr/**/state.md`, `finalize-state.md`, terminal `*-state.md`, PR-loop state directories, command strings from Bash hooks, Codex review logs under `$CACHE_DIR`, templates under `$TEMPLATE_DIR`, and goal-tracker markdown. Outputs include extracted variables in global `STATE_*` variables, rendered block/prompt text, stdout path lookups, stderr diagnostics, generated review-result files, renamed terminal state files through `end_loop` (`hooks/lib/loop-common.sh:1238`), and goal-tracker table/stat/log edits (`:1375`, `:1386`, `:1418`).
- gates_or_invariants: Strict parser requires YAML separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`, plus numeric rounds/limits and boolean `review_started` (`hooks/lib/loop-common.sh:394`, `:406`, `:424`, `:436`). `find_active_loop` without a session only checks the newest directory to prevent stale loop revival (`:242`); with a session filter, the newest terminal state for that session blocks fallback to older active loops (`:259`, `:284`). Cancel authorization requires a `.cancel-requested` signal and a tightly parsed `mv state.md|finalize-state.md cancel-state.md` command, rejecting command substitution, pipes, extra args, hidden variables, mixed quotes, and symlink source files (`:670`, `:681`, `:691`, `:794`, `:815`). Git staging protection blocks direct or broad `.humanize` adds (`:1049`). Template rendering is intentionally single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:54`).
- dependencies_and_callers: Sourced by RLCR validators and stop hooks including `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and setup/cancel scripts. Direct caller examples: plan validator uses `extract_session_id`, `find_active_loop`, and `parse_state_file_strict` (`hooks/loop-plan-file-validator.sh:29`, `:34`, `:45`); PR stop hook uses `map_author_to_bot`, `map_bot_to_author`, and `update_pr_goal_tracker` (`hooks/pr-loop-stop-hook.sh:974`, `:1202`, `:1541`); setup scripts source the library for defaults and loop conflict checks (`scripts/setup-rlcr-loop.sh:29`, `scripts/setup-pr-loop.sh:40`).
- edge_cases_or_failure_modes: Empty stored `session_id` intentionally matches any filter for backward compatibility (`hooks/lib/loop-common.sh:275`). Tolerant parsing defaults optional or legacy fields but leaves schema-critical `review_started` empty so callers can block on stale state (`:363`, `:372`). State parsing is grep/sed frontmatter extraction, not a full YAML parser, so branch/model values are separately constrained by setup scripts. `detect_review_issues` treats missing or empty Codex logs as hard error return `2` (`:473`), but a `[P?]` marker beyond the first 10 characters is ignored by design (`:486`). `validate_template_dir` warns rather than aborts when sourced through `loop-common.sh`, allowing inline fallbacks (`:163`).
- validation_or_tests: Covered by `tests/test-template-loader.sh` for template resolution/render/fallback behavior (`tests/test-template-loader.sh:41`, `:81`, `:197`), `tests/test-session-id.sh` for session filtering and stale-loop prevention (`tests/test-session-id.sh:203`, `:634`), state parser robustness tests (`tests/robustness/test-state-file-robustness.sh:149`, `:201`), cancel authorization/security tests, Codex review marker tests (`tests/test-codex-review-merge.sh:57`, `:134`), and PR goal-tracker tests (`tests/test-pr-loop-system.sh:1180`, `:1242`). Tests were not executed in this read-only research pass.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-039 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop polling script that normalizes GitHub issue comments, review comments, and PR reviews from watched bots into one JSON contract for `hooks/pr-loop-stop-hook.sh`.
- algorithmic_behavior: Parses `PR_NUMBER`, `--after`, and `--bots` (`scripts/poll-pr-reviews.sh:23`), validates required args and numeric PR number (`:83`, `:99`), requires `gh` and `jq` (`:108`, `:113`), resolves the base/upstream repo for fork PR support by trying current repo then parent repo (`:127`, `:135`, `:139`, `:147`), maps logical bot names to GitHub authors with a codex special case (`:167`), fetches three API streams with retries (`:210`, `:241`, `:258`, `:279`), normalizes each stream into a shared object shape (`:242`, `:259`, `:280`), combines arrays (`:297`), filters by timestamp and watched author regex (`:300`), then emits final JSON (`:317`).
- inputs_outputs_state: Inputs are CLI args, authenticated `gh` context, current or parent GitHub repository, GitHub API endpoints for `/issues/{pr}/comments`, `/pulls/{pr}/comments`, and `/pulls/{pr}/reviews`, plus `jq`. Output is JSON with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:321`). It creates only temporary files under `mktemp -d` and removes them on exit (`:193`, `:195`); no persistent repo state is mutated.
- gates_or_invariants: Fails closed for malformed invocation, missing dependencies, unresolvable PR base repo, or unparseable owner/name (`scripts/poll-pr-reviews.sh:84`, `:108`, `:147`, `:155`). API fetch failures are intentionally not fatal: `fetch_with_retry` retries three times, warns, returns `[]`, and exits `0` so PR polling can continue or time out at the orchestrator level (`:200`, `:221`, `:231`). Approval-only PR reviews are preserved by replacing empty review body with a state placeholder (`:276`, `:288`).
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` as `$PLUGIN_ROOT/scripts/poll-pr-reviews.sh` (`hooks/pr-loop-stop-hook.sh:812`) inside the per-bot polling loop (`:908`). The stop hook passes `--after` according to trigger/startup-case rules (`:817`, `:825`, `:829`, `:836`) and consumes `.bots_responded[]` and `.comments` from the JSON (`:971`, `:1044`). Downstream grouping maps configured bots back to authors and writes `round-N-pr-comment.md` (`hooks/pr-loop-stop-hook.sh:1186`, `:1202`).
- edge_cases_or_failure_modes: Timestamp comparison is string-based (`scripts/poll-pr-reviews.sh:302`), so it assumes canonical ISO-8601 UTC-like ordering. API warnings may precede JSON on stderr only because fetch failures warn to stderr; tests extract JSON defensively. If `gh api` returns invalid JSON while exiting success, later `jq` processing can still abort under `set -euo pipefail`. The script builds an author regex from comma-separated bots after trimming spaces (`:177`, `:179`); an empty element in a comma list can produce an unintended `[bot]` author pattern. `API_FAILURES` is incremented inside the retry function (`:205`, `:226`) but not used in the final JSON.
- validation_or_tests: `tests/robustness/test-pr-loop-api-robustness.sh` validates help, missing args, mocked valid JSON, slow empty API responses, and API failure returning exit `0` with boolean `has_new_comments:false` (`tests/robustness/test-pr-loop-api-robustness.sh:619`, `:632`, `:646`, `:677`, `:744`). `tests/test-pr-loop-hooks.sh` verifies approval-only reviews are included and `--after` excludes older comments (`tests/test-pr-loop-hooks.sh:1322`, `:1371`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-069 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR `session_id` isolation, PostToolUse session capture, active-loop lookup semantics, cancel behavior, and command-boundary security around the setup signal file.
- algorithmic_behavior: Sources shared helpers and `hooks/lib/loop-common.sh` (`tests/test-session-id.sh:15`, `:20`), creates temp git repos and loop directories, then exercises the session-id lifecycle. It verifies setup creates `state.md` with an empty `session_id` and a `.humanize/.pending-session-id` signal (`:34`, `:62`, `:72`, `:87`); the PostToolUse hook records the hook `session_id` and removes the signal only for setup invocations (`:113`, `:145`, `:149`, `:157`); absence of a signal is a no-op (`:171`, `:187`); `find_active_loop` honors matching, non-matching, empty, and finalize-state session ids (`:203`, `:228`, `:239`, `:289`); and `parse_state_file` exposes `STATE_SESSION_ID` (`:321`, `:338`).
- inputs_outputs_state: Inputs are generated `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md` fixtures, mock hook JSON with `tool_input.command` and `session_id`, real scripts `scripts/setup-rlcr-loop.sh`, `hooks/loop-post-bash-hook.sh`, and `scripts/cancel-rlcr-loop.sh`, plus `git` for temp repo setup. Outputs are pass/fail/skip records through `tests/test-helpers.sh` (`tests/test-helpers.sh:30`, `:35`, `:46`) and transient temp directories. The test mutates only temp fixtures when run.
- gates_or_invariants: Specifies that empty stored `session_id` matches any session (`tests/test-session-id.sh:239`), no-filter lookup remains backward compatible (`:264`), terminal newest state prevents stale older loop revival for both filtered and unfiltered lookup (`:634`, `:677`), different sessions do not block each other (`:687`), and `cancel-state.md` is also terminal for revival prevention (`:748`). It also specifies setup signal line 2 must be an absolute full setup script path (`:786`, `:813`) and the PostToolUse hook must reject substring or concatenated command forms (`:826`, `:883`) while accepting quoted/unquoted and tab-delimited real invocations (`:927`, `:965`, `:1010`).
- dependencies_and_callers: Depends on the implementation in `hooks/lib/loop-common.sh` (`find_active_loop`, `parse_state_file`), `hooks/loop-post-bash-hook.sh` for signal consumption and safe `awk` replacement (`hooks/loop-post-bash-hook.sh:56`, `:94`, `:105`), `scripts/setup-rlcr-loop.sh` for initial state/signal creation (`scripts/setup-rlcr-loop.sh:767`, `:789`), and `scripts/cancel-rlcr-loop.sh` for terminal cancel behavior.
- edge_cases_or_failure_modes: The setup-script assertions can fail early if `scripts/setup-rlcr-loop.sh` cannot pass its environment prerequisites, notably `codex` availability (`scripts/setup-rlcr-loop.sh:579`), although the test invokes setup with `|| true` (`tests/test-session-id.sh:57`, `:809`) and then checks artifacts. PostToolUse tests are skipped if the hook file is absent (`:162`, `:198`). Special-character session ids with `/` and `&` are explicitly tested to protect replacement safety (`:531`, `:553`). The command-boundary checks protect against race/false-positive signal consumption (`:463`, `:826`, `:883`).
- validation_or_tests: This file is itself the validation artifact. It ends by printing a summary via `print_test_summary "Session ID Feature Tests"` (`tests/test-session-id.sh:1055`, `:1059`). It covers both positive behavior and security regressions for session capture and active-loop lookup.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-099 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for stale RLCR state schema detection. It is part of the validation gate that prevents a loop started by an older humanize version from continuing with missing state fields.
- algorithmic_behavior: Defines a short markdown reason headed `State Schema Outdated` (`prompt-template/block/schema-outdated.md:1`), interpolates `{{FIELD_NAME}}` into the missing-field message (`:3`), explains the old-version cause (`:5`), and gives recovery options to cancel, update to `1.1.2+`, and restart (`:7`).
- inputs_outputs_state: Input is one template variable, `FIELD_NAME`, supplied by `hooks/loop-plan-file-validator.sh` (`hooks/loop-plan-file-validator.sh:60`, `:65`). Output is rendered markdown, JSON-escaped by the caller and returned as a hook block response (`:67`, `:71`). The template itself mutates no state.
- gates_or_invariants: The caller reaches this template after locating the active loop with the current hook `session_id` (`hooks/loop-plan-file-validator.sh:29`, `:34`) and strict-parsing the state file (`:45`). It checks schema-critical fields `plan_tracked` and `start_branch` and blocks when either is empty (`:79`, `:85`). The template’s line saying the session will be terminated as unexpected (`prompt-template/block/schema-outdated.md:12`) is stronger than this direct caller’s behavior, which emits a block decision and exits without renaming state.
- dependencies_and_callers: Directly referenced only by `hooks/loop-plan-file-validator.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md"` (`hooks/loop-plan-file-validator.sh:65`). Rendering depends on `hooks/lib/template-loader.sh` fallback-safe behavior (`hooks/lib/template-loader.sh:170`).
- edge_cases_or_failure_modes: If the template is missing, the caller uses an inline fallback (`hooks/loop-plan-file-validator.sh:62`). If `FIELD_NAME` is not supplied, the placeholder remains because rendering preserves unknown variables (`hooks/lib/template-loader.sh:119`). The message assumes the operator can cancel via `/humanize:cancel-rlcr-loop`; if the state is malformed enough that strict parsing fails before field checks, the caller instead emits a generic malformed-state block (`hooks/loop-plan-file-validator.sh:44`).
- validation_or_tests: Template existence and safe rendering are indirectly covered by template reference/comprehensive tests; direct call-site discovery is shown by `rg` at `hooks/loop-plan-file-validator.sh:65`. No dedicated assertion was found for the exact prose.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-129 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR-loop goal tracker schema template. It seeds the markdown document that later PR-loop analysis updates with per-round reviewer issue counts, totals, and log entries.
- algorithmic_behavior: Renders PR metadata placeholders for PR number, branch, start time, monitored bots, and startup case (`prompt-template/pr-loop/goal-tracker-initial.md:3`, `:5`). Defines the ultimate goal as approval by all monitored bot reviewers (`:11`). Seeds an Issue Summary table with Round 0 and zero counts (`:15`, `:17`), Total Statistics with found/resolved/remaining all zero (`:21`), and an Issue Log waiting for initial reviews (`:27`, `:29`).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, and `STARTUP_CASE`. `scripts/setup-pr-loop.sh` renders it with `load_and_render_safe` and writes the result to `$GOAL_TRACKER_FILE` (`scripts/setup-pr-loop.sh:712`, `:713`). The output file becomes persistent PR-loop state under `.humanize/pr-loop/.../goal-tracker.md`.
- gates_or_invariants: Downstream `update_pr_goal_tracker` assumes the `## Issue Summary`, `## Total Statistics`, and `## Issue Log` sections exist, along with totals named `Total Issues Found`, `Total Issues Resolved`, and `Remaining` (`hooks/lib/loop-common.sh:1279`, `:1339`, `:1375`, `:1386`, `:1418`). This template provides exactly those anchor sections and initial counters. Missing placeholders remain visible rather than being silently dropped due the single-pass renderer.
- dependencies_and_callers: Directly used in PR-loop setup (`scripts/setup-pr-loop.sh:712`). Later consumed by `hooks/lib/loop-common.sh:update_pr_goal_tracker`, which inserts table rows, updates totals, appends round logs, and repairs partial updates idempotently (`hooks/lib/loop-common.sh:1303`, `:1339`, `:1377`, `:1419`). PR stop hook can call the helper after Codex analysis (`hooks/pr-loop-stop-hook.sh:1536`, `:1541`).
- edge_cases_or_failure_modes: If this template exists, it overrides the richer fallback block embedded in `scripts/setup-pr-loop.sh` that includes acceptance criteria/current-status/open-issues/log tables (`scripts/setup-pr-loop.sh:680`). That is acceptable for the current helper because tests require only the Issue Summary, Total Statistics, and Issue Log schema anchors. If the rendered tracker is manually partially updated, helper tests expect repair without double-counting (`tests/test-pr-loop-system.sh:1242`).
- validation_or_tests: `tests/test-pr-loop-system.sh` checks the template contains required sections and total fields (`tests/test-pr-loop-system.sh:642`, `:647`, `:651`). The same test suite verifies `update_pr_goal_tracker` inserts rows inside the table and repairs partial summary/log updates (`:1180`, `:1242`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections present above; item IDs are intentionally not repeated here to preserve the exactly-once evidence invariant.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`