# agent_18 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow wrapper for cancelling an active PR loop. It is a command definition rather than the cancellation algorithm itself: frontmatter restricts allowed tools to `scripts/cancel-pr-loop.sh` with optional `--force`, hides the command from slash-command tooling, and tells the assistant to delegate all logic to the script at `commands/cancel-pr-loop.md:1-5`.
- algorithmic_behavior: The command instructs the assistant to run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"` and classify behavior only from the first output line: `NO_LOOP` or `NO_ACTIVE_LOOP` becomes “No active PR loop found,” while `CANCELLED` reports the cancellation message from script output at `commands/cancel-pr-loop.md:9-20`. The underlying script finds the newest `.humanize/pr-loop/` directory, requires `state.md` to exist, writes `.cancel-requested`, renames `state.md` to `cancel-state.md`, and outputs `CANCELLED` plus round and PR metadata at `scripts/cancel-pr-loop.sh:72-130`.
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT` for command resolution, optional script argument `--force`, and the current project root via `CLAUDE_PROJECT_DIR` or `pwd` in the script at `scripts/cancel-pr-loop.sh:24-66` and `scripts/cancel-pr-loop.sh:72-73`. Output contract is first-line status plus human-readable detail. State transition is active PR loop state file `state.md` to terminal `cancel-state.md`, preserving loop artifacts, with `.cancel-requested` as the cancellation signal at `scripts/cancel-pr-loop.sh:88-122`.
- gates_or_invariants: The command emphasizes the invariant that a PR loop is active only if `state.md` exists in the newest PR loop directory at `commands/cancel-pr-loop.md:21`. It explicitly does not affect RLCR loops and points users to `/humanize:cancel-rlcr-loop` for RLCR at `commands/cancel-pr-loop.md:25`. The script exits `1` for no active loop and `3` for unknown options at `scripts/cancel-pr-loop.sh:11-15` and `scripts/cancel-pr-loop.sh:60-64`.
- dependencies_and_callers: Depends on `scripts/cancel-pr-loop.sh`; user-facing references appear in PR loop start/stop flows, including `commands/start-pr-loop.md` and `hooks/pr-loop-stop-hook.sh` references found around cancellation guidance. The script itself depends on shell utilities `ls`, `sort`, `head`, `grep`, `sed`, `tr`, `touch`, and `mv`.
- edge_cases_or_failure_modes: If no PR loop directory exists, script emits `NO_LOOP` and exits `1`; if a directory exists but has no `state.md`, it emits `NO_ACTIVE_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:78-98`. Missing `current_round`, `max_iterations`, or `pr_number` fall back to `?` before output at `scripts/cancel-pr-loop.sh:104-112`. `--force` is parsed but has no behavioral effect at `scripts/cancel-pr-loop.sh:24-40`.
- validation_or_tests: The command’s behavior is covered indirectly by `tests/test-pr-loop-scripts.sh`, which includes `cancel-pr-loop.sh` tests according to search hits at `tests/test-pr-loop-scripts.sh:169-185`. No test was run for this research pass; evidence is based on direct file inspection and referenced production script behavior.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-048 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR Agent Teams mode. It verifies CLI gating, state persistence, state parsing, prompt injection, active-loop discovery, and phase-specific stop-hook prompt generation for `--agent-teams`.
- algorithmic_behavior: The test sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh`, then drives `scripts/setup-rlcr-loop.sh` and `hooks/loop-codex-stop-hook.sh` in temporary git repositories at `tests/test-agent-teams.sh:15-30`. It asserts that `--agent-teams` fails unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, including rejection of `"0"`, `"false"`, `"yes"`, and `"true"` values at `tests/test-agent-teams.sh:54-79`. It then verifies successful setup creates RLCR state with `agent_teams: true`, while default setup writes `agent_teams: false` at `tests/test-agent-teams.sh:103-153`.
- inputs_outputs_state: Inputs include a gitignored plan file, `CLAUDE_PROJECT_DIR`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `--agent-teams`, and mocked Codex commands injected into `PATH`. Outputs are test pass/fail counters plus generated `.humanize/rlcr/.../state.md`, `round-0-prompt.md`, and next-round prompts. Persistent state under test is `agent_teams` in YAML frontmatter. Production setup writes the field at `scripts/setup-rlcr-loop.sh:767-785`; parser constants and extraction live at `hooks/lib/loop-common.sh:24-40` and `hooks/lib/loop-common.sh:312-331`.
- gates_or_invariants: Main gate: Agent Teams mode can only be enabled when `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is exactly `1`; production validation enforces this at `scripts/setup-rlcr-loop.sh:283-298`. State invariant: missing `agent_teams` defaults to `false` in tolerant and strict parsers at `hooks/lib/loop-common.sh:363-372` and `hooks/lib/loop-common.sh:442-447`. Phase invariant: continuation instructions appear during implementation when `AGENT_TEAMS=true` and `REVIEW_STARTED` is not true, but must not appear in review-phase prompts; production gate is at `hooks/loop-codex-stop-hook.sh:1630-1638`, while review-phase prompt generation bypasses Agent Teams continuation via `continue_review_loop_with_issues` at `hooks/loop-codex-stop-hook.sh:1153-1185`.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `prompt-template/claude/agent-teams-instructions.md`, `prompt-template/claude/agent-teams-continue.md`, `prompt-template/claude/review-phase-prompt.md`, and `tests/test-helpers.sh`. The initial prompt injects the instructions template when enabled at `scripts/setup-rlcr-loop.sh:1002-1020`; the continuation template is loaded by the stop hook at `hooks/loop-codex-stop-hook.sh:1631-1637`.
- edge_cases_or_failure_modes: Test setup frequently appends `|| true` around setup calls, so it verifies artifacts rather than trusting exit status in later cases at `tests/test-agent-teams.sh:104`, `tests/test-agent-teams.sh:146`, and `tests/test-agent-teams.sh:234`. It covers missing env var, non-exact env var values, missing `agent_teams` field defaulting false, prompt file absence as failure or skip depending on scenario, and review-phase contamination by Agent Teams continuation. The mocked stop-hook paths also check that implementation prompts get `Agent Teams` text only when enabled, while review prompts include `P1` review findings and no Agent Teams text at `tests/test-agent-teams.sh:506-603`.
- validation_or_tests: This file is itself the validation asset. Test result plumbing comes from `tests/test-helpers.sh`, where failures increment `TESTS_FAILED` and `print_test_summary` returns nonzero if any fail at `tests/test-helpers.sh:30-77`. It is also listed in `tests/run-all-tests.sh` according to search results. I did not execute it in this read-only research pass.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-078 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking PR-loop prompt template used when the Claude GitHub bot does not add an eyes reaction to a trigger comment after retries. It turns a bot-observability timeout into an actionable stop-hook block.
- algorithmic_behavior: Template renders a concise failure message with `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}`, lists possible causes, required checks, and restart instruction at `prompt-template/block/claude-eyes-timeout.md:1-18`. Production caller in `hooks/pr-loop-stop-hook.sh` verifies Claude is configured, a trigger is required, and a trigger comment ID exists; then it invokes `check-bot-reactions.sh claude-eyes` with retry count `3` and delay `5` at `hooks/pr-loop-stop-hook.sh:760-778`. Empty or `null` reaction renders this template and returns a JSON block decision at `hooks/pr-loop-stop-hook.sh:779-790`.
- inputs_outputs_state: Inputs are template variables `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, plus stop-hook context: `PR_CONFIGURED_BOTS_ARRAY`, `REQUIRE_TRIGGER`, `PR_TRIGGER_COMMENT_ID`, and `PR_NUMBER`. Output is JSON with `"decision": "block"`, rendered `reason`, and system message “PR Loop: Claude bot not responding - check bot configuration” at `hooks/pr-loop-stop-hook.sh:785-789`. It does not mutate persistent loop state; it gates forward progress until a new trigger comment restarts review.
- gates_or_invariants: Eyes verification only runs after a trigger is confirmed, preventing stale trigger checks at `hooks/pr-loop-stop-hook.sh:750-759`. It is blocking only when Claude is configured and a trigger is required; if Claude is configured but no trigger is required, the hook skips eyes verification at `hooks/pr-loop-stop-hook.sh:798-800`. Template rendering uses `load_and_render_safe`, so if the template is missing an inline fallback is rendered instead at `hooks/pr-loop-stop-hook.sh:781-786`.
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh`; depends on `scripts/check-bot-reactions.sh` for reaction polling and `hooks/lib/template-loader.sh` for safe single-pass rendering. Template-loader syntax preserves missing placeholders and avoids recursive placeholder expansion at `hooks/lib/template-loader.sh:7-14` and `hooks/lib/template-loader.sh:50-58`; safe fallback behavior is implemented at `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If no trigger comment ID is available despite trigger state, the hook warns rather than blocking at `hooks/pr-loop-stop-hook.sh:794-797`. If reaction command fails or returns empty/null, the timeout path triggers. If the template file is absent or renders empty, fallback content still blocks with retry details via `load_and_render_safe`.
- validation_or_tests: Template rendering is covered generally by template-loader tests and comprehensive template tests; direct search shows `tests/test-templates-comprehensive.sh` includes real template testing for block templates nearby. No dedicated direct test for `claude-eyes-timeout.md` was inspected beyond its production caller. The file is core because it defines a stop-hook gate message and operator recovery path.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-108 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Blocking RLCR validator template for attempts to write or edit a round-scoped file with a round number that does not match the active loop’s `current_round`.
- algorithmic_behavior: The template renders the attempted action, attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, current round, correct path, and explicit instruction not to self-increment the round number at `prompt-template/block/wrong-round-number.md:1-7`. `hooks/loop-write-validator.sh` uses it for summary-file writes when extracted `CLAUDE_ROUND` differs from `CURRENT_ROUND`, emits the rendered message to stderr, and exits `2` at `hooks/loop-write-validator.sh:230-247`. `hooks/loop-edit-validator.sh` performs the same guard for edits at `hooks/loop-edit-validator.sh:155-179`.
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Output is a blocking hook message on stderr and exit code `2`; no persistent state mutation occurs. The correct path is computed from `ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` in both validators at `hooks/loop-edit-validator.sh:165-177` and `hooks/loop-write-validator.sh:233-245`.
- gates_or_invariants: Invariant: the assistant may only write/edit the current round’s summary file unless the target file is allowlisted. The validators extract filenames under `.humanize/rlcr/...`, compare parsed round number to active state round, and apply `is_allowlisted_file` before blocking at `hooks/loop-edit-validator.sh:155-178` and `hooks/loop-write-validator.sh:218-247`. The template’s final line reinforces that the loop controller, not the assistant, advances rounds.
- dependencies_and_callers: Called by `hooks/loop-edit-validator.sh` and `hooks/loop-write-validator.sh`, both using `load_and_render_safe` from `hooks/lib/template-loader.sh`. Related functions such as `extract_round_number`, `is_round_file_type`, and `is_allowlisted_file` are supplied through shared hook libraries.
- edge_cases_or_failure_modes: If the filename cannot be extracted from `.humanize/rlcr`, write validator exits without applying this guard at `hooks/loop-write-validator.sh:218-224`. If no round number is extractable, mismatch logic is skipped. If the template is missing, both validators provide fallback messages that still include attempted round, current round, and correct path at `hooks/loop-edit-validator.sh:167-172` and `hooks/loop-write-validator.sh:235-240`.
- validation_or_tests: Direct template rendering is exercised by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` according to references. Production behavior is validated through write/edit validator tests elsewhere in the suite. This file is core because it enforces RLCR round-number state-machine discipline.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-138 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for `humanize_parse_goal_tracker` in `scripts/humanize.sh`. It validates parsing of the RLCR goal tracker summary tuple used by monitoring/reporting surfaces.
- algorithmic_behavior: The test sources shared helpers and production `scripts/humanize.sh`, then asserts that `humanize_parse_goal_tracker` returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` at `tests/robustness/test-goal-tracker-robustness.sh:15-37`. It defines `parse_result` to select tuple fields by pipe position at `tests/robustness/test-goal-tracker-robustness.sh:39-51`. Positive cases verify list-format ACs, table-format ACs, active task counts excluding completed/deferred rows, completed task row counts, unique completed AC counts, and ultimate goal extraction at `tests/robustness/test-goal-tracker-robustness.sh:60-227`.
- inputs_outputs_state: Inputs are synthetic `goal-tracker.md` variants written under `TEST_DIR`. Output from the production function is a pipe-delimited summary string. No repository state is mutated outside the temporary test directory. Production parser returns defaults for missing files, counts ACs between `### Acceptance Criteria` and `---`, counts table rows in sections, calculates active tasks as active-section data rows minus completed/deferred statuses, counts unique completed ACs, and extracts the first content line under `### Ultimate Goal` at `scripts/humanize.sh:38-118`.
- gates_or_invariants: Parser contract uses section headers as structural gates. Without proper `### Acceptance Criteria` header, AC count should be zero as tested at `tests/robustness/test-goal-tracker-robustness.sh:304-326`. Active task rows are bounded to the `#### Active Tasks` section ending at the next `###`, and completed/deferred status rows are excluded at `scripts/humanize.sh:69-90`. Completed, deferred, and open issue counters subtract Markdown header/separator rows through `_count_table_data_rows` at `scripts/humanize.sh:45-52`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp dirs and pass/fail summary, and on `scripts/humanize.sh`, which in turn may source `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh` at `scripts/humanize.sh:6-16`. The parser is public helper behavior for monitor-style summaries and RLCR progress display.
- edge_cases_or_failure_modes: Negative/robustness coverage includes nonexistent file default `0|0|0|0|0|0|No goal tracker`, empty files, 60 AC entries, special shell/Markdown characters in descriptions, malformed headers, truncated files, embedded binary bytes, open issue counts, deferred task counts, headers-only files, mixed AC formats with bold IDs and sub-numbering, and very long goal summaries at `tests/robustness/test-goal-tracker-robustness.sh:237-524`. The binary-content test expects graceful partial parsing rather than exact count at `tests/robustness/test-goal-tracker-robustness.sh:349-371`. Long summaries are returned in full; truncation is explicitly assigned to a display layer at `tests/robustness/test-goal-tracker-robustness.sh:501-524`.
- validation_or_tests: The file is self-validating: it prints “Goal Tracker Robustness Tests,” accumulates pass/fail through `tests/test-helpers.sh`, calls `print_test_summary`, and exits with that result at `tests/robustness/test-goal-tracker-robustness.sh:526-531`. I did not execute it in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5; each assigned item is represented once as an Item Evidence section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`