# agent_19 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for canceling an active RLCR loop. It is a command-facing control surface over `scripts/cancel-rlcr-loop.sh`, not the cancellation implementation itself.
- algorithmic_behavior: The command restricts allowed tools to the cancel script with and without `--force`, plus `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-4`). The workflow always runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` first (`commands/cancel-rlcr-loop.md:11-15`), branches on the first output line (`commands/cancel-rlcr-loop.md:17-22`), and only prompts for confirmation when the script reports `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:23-35`). The command delegates all state mutation to the script; it explicitly says the script owns cancellation logic and active-loop detection (`commands/cancel-rlcr-loop.md:36-38`).
- inputs_outputs_state: Input is the current project’s active RLCR loop plus optional user confirmation during Finalize Phase. Script outputs consumed by this command are `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_FINALIZE`, and `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:17-22`). In implementation, `scripts/cancel-rlcr-loop.sh` finds the newest active `.humanize/rlcr` loop (`scripts/cancel-rlcr-loop.sh:69-88`), creates `.cancel-requested`, removes pending session-id state, and renames `state.md` or `finalize-state.md` to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:148-155`). Normal cancellation outputs `CANCELLED`; forced Finalize cancellation outputs `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:161-169`).
- gates_or_invariants: The command’s main invariant is confirmation before canceling Finalize Phase unless the user explicitly chooses force (`commands/cancel-rlcr-loop.md:23-34`). The script has a matching gate: Finalize Phase without `--force` exits with `FINALIZE_NEEDS_CONFIRM` and exit code `2` (`scripts/cancel-rlcr-loop.sh:131-142`). Cancellation is global within the current project rather than session-filtered; the implementation documents this as intentional because slash commands lack hook JSON session ids (`scripts/cancel-rlcr-loop.sh:76-87`). Hook-side state-file protection has a special authorized path for the cancel signal: `is_cancel_authorized` requires `.cancel-requested`, rejects substitution/operators/newlines/extra args, and only permits `mv` from the active `state.md` or `finalize-state.md` to `cancel-state.md` (`hooks/lib/loop-common.sh:652-829`).
- dependencies_and_callers: Depends on `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh` and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:3`). The script depends on `hooks/lib/loop-common.sh` for `find_active_loop` (`scripts/cancel-rlcr-loop.sh:72-88`). Hook validators reference cancellation authorization to avoid blocking the script’s state rename (`hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:286-303`).
- edge_cases_or_failure_modes: No active loop maps to `NO_LOOP` or `NO_ACTIVE_LOOP` and user-facing “No active RLCR loop found” (`commands/cancel-rlcr-loop.md:17-18`). Finalize Phase is deliberately not canceled by default; it asks whether to force rename `finalize-state.md` to `cancel-state.md` (`commands/cancel-rlcr-loop.md:23-31`). The implementation preserves the loop directory for summaries, review results, and state information (`commands/cancel-rlcr-loop.md:36-38`). Unknown script flags exit `3` (`scripts/cancel-rlcr-loop.sh:57-61`).
- validation_or_tests: Related tests include cancel-state and no-loop assertions in session/cancel/security suites found by reference search, especially `tests/test-session-id.sh`, `tests/test-cancel-signal-file.sh`, and `tests/robustness/test-cancel-security-robustness.sh`. I did not run tests because this assignment requested research notes only and the workspace is read-only.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-049 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR validator allowlist that temporarily permits specific loop todo and summary files while the broader hook policy blocks wrong-round or wrong-location access.
- algorithmic_behavior: The test creates a temporary git repo and active loop directory with `state.md` frontmatter setting `current_round: 5`, `max_iterations: 42`, `plan_file`, `start_branch`, `base_branch`, and `review_started: false` (`tests/test-allowlist-validators.sh:29-64`). It then validates `is_allowlisted_file` directly and tests Read/Write/Edit/Bash validators using JSON hook input piped to production hook scripts (`tests/test-allowlist-validators.sh:66-393`). The executable contract says exactly four files are allowlisted in the active loop directory: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`tests/test-allowlist-validators.sh:72-102`). The implementation matches that hard-coded list in `hooks/lib/loop-common.sh:539-560`.
- inputs_outputs_state: Inputs are synthetic Claude hook JSON values for `Write`, `Edit`, `Read`, and `Bash`, plus `CLAUDE_PROJECT_DIR="$TEST_DIR"` so hooks resolve `.humanize/rlcr` under the temp project (`tests/test-allowlist-validators.sh:132-139`). Expected outputs are exit code `0` for allowed operations and exit code `2` with message content mentioning `todos` or `round` for blocked operations (`tests/test-allowlist-validators.sh:135-185`, `tests/test-allowlist-validators.sh:191-228`, `tests/test-allowlist-validators.sh:234-284`, `tests/test-allowlist-validators.sh:290-383`). State is created but not advanced; the active-loop lookup sees `state.md` in the newest loop dir.
- gates_or_invariants: Key invariant: allowlisted filenames are only allowed when the full path exactly equals `$ACTIVE_LOOP_DIR/$allowed`; same basename in any other directory is blocked (`tests/test-allowlist-validators.sh:120-126`, `hooks/lib/loop-common.sh:553-559`). Todo allowlist only includes rounds 1 and 2; `round-3-todos.md` and later are blocked (`tests/test-allowlist-validators.sh:104-110`, `tests/test-allowlist-validators.sh:161-172`, `tests/test-allowlist-validators.sh:260-270`, `tests/test-allowlist-validators.sh:329-340`). Summary allowlist only includes rounds 0 and 1; `round-2-summary.md` and `round-3-summary.md` are blocked despite the current round being 5 (`tests/test-allowlist-validators.sh:112-118`, `tests/test-allowlist-validators.sh:174-185`, `tests/test-allowlist-validators.sh:273-284`). Bash validator requires full active-loop path for todo writes, blocking relative names, `/tmp`, old loop directories, and same timestamp under a different root (`tests/test-allowlist-validators.sh:316-383`; implementation at `hooks/loop-bash-validator.sh:381-393`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for direct allowlist testing (`tests/test-allowlist-validators.sh:15-18`). Calls production `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh`. The validators depend on shared helpers such as `find_active_loop`, `is_round_file_type`, `extract_round_number`, `parse_state_file_strict`, and `is_allowlisted_file` in `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: The test covers wrong directory with an allowlisted basename (`tests/test-allowlist-validators.sh:120-126`), old loop directory (`tests/test-allowlist-validators.sh:355-368`), same active-loop basename under a different root (`tests/test-allowlist-validators.sh:370-383`), and generic relative todo filename with no active-loop path (`tests/test-allowlist-validators.sh:342-353`). One notable gap exposed by inspection: `loop-edit-validator.sh` does not call `validate_hook_input`, `is_deeply_nested`, or `require_tool_input_field`; it directly runs `jq` and extracts `.tool_input.file_path` (`hooks/loop-edit-validator.sh:24-35`). This allowlist test uses well-formed Edit JSON only, so it does not exercise malformed Edit input.
- validation_or_tests: This file is itself the test. It exits with the number of failed assertions (`tests/test-allowlist-validators.sh:385-393`). I did not execute it in the read-only branch export because it initializes git/temp state and the assignment asks for research notes only.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-079 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template for a failed Codex review phase where no usable review output was produced. It defines the retry contract presented to Claude/the loop controller when review cannot be accepted.
- algorithmic_behavior: The template renders a concise failure notice with an exit code, expected review result path, debug file paths, and stderr tail (`prompt-template/block/codex-review-failed.md:1-18`). It is loaded by `codex_failure_exit` in `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1251-1260`). The resulting message is returned in a JSON block decision with system message “Loop: Blocked - Codex review failed, retry required” (`hooks/loop-codex-stop-hook.sh:1262-1269`).
- inputs_outputs_state: Template placeholders include `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5-15`). The call site currently passes `FAILURE_REASON`, `ROUND_NUMBER`, `BASE_BRANCH`, `EXIT_CODE`, `STDERR_CONTENT`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, and `CODEX_LOG_FILE` (`hooks/loop-codex-stop-hook.sh:1252-1260`). That means the checked-in template’s `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE` placeholders are not populated by this call site as written, while `CODEX_LOG_FILE` is passed but unused by this template. Output is human-readable markdown embedded as hook JSON `reason`; state is not directly mutated by the template.
- gates_or_invariants: The gate is fail-closed: if Codex exits non-zero, fails to create/copy a review result, or creates an empty result, the stop hook blocks rather than allowing the RLCR loop to advance (`hooks/loop-codex-stop-hook.sh:1346-1418`). The template’s final instruction says the system will attempt another review when the user exits (`prompt-template/block/codex-review-failed.md:18`), matching the hook’s retry-required block transition (`hooks/loop-codex-stop-hook.sh:1262-1269`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` single-pass placeholder rendering (`hooks/lib/template-loader.sh:50-132`) and safe fallback handling (`hooks/lib/template-loader.sh:167-203`). Caller is `hooks/loop-codex-stop-hook.sh`, specifically `codex_failure_exit` and the review result validation path. Review result detection also uses `detect_review_issues` from `hooks/lib/loop-common.sh:468-511`.
- edge_cases_or_failure_modes: The placeholder mismatch is the most important edge case: unresolved placeholders can appear in the rendered block because template variable names do not align with the call-site arguments (`prompt-template/block/codex-review-failed.md:5-15` versus `hooks/loop-codex-stop-hook.sh:1252-1260`). The template also labels `REVIEW_RESULT_FILE` as “not created” (`prompt-template/block/codex-review-failed.md:6`), but the same failure helper is used for non-zero exit, copy failure, missing result, and empty result (`hooks/loop-codex-stop-hook.sh:1346-1418`), so the text can be slightly inaccurate for an empty existing file.
- validation_or_tests: Related behavior is covered indirectly by stop-hook and codex-review tests. `tests/test-codex-review-merge.sh` validates review issue extraction and result-file creation for positive review issue detection, while `hooks/loop-codex-stop-hook.sh` contains the fail-closed checks that call this template. I did not find a dedicated assertion for this exact template’s placeholder completeness in the inspected references.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-109 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Block-message template for enforcing that RLCR round summary files are written inside the active loop directory.
- algorithmic_behavior: The template states that summary files must be in the loop directory and renders the correct path (`prompt-template/block/wrong-summary-location.md:1-5`). It is used by the Write validator when a `round-N-summary.md` path is detected outside `.humanize/rlcr/` (`hooks/loop-write-validator.sh:200-211`). This is part of the summary-file transition contract: Claude may write summaries, but only to the active loop path and round accepted by state.
- inputs_outputs_state: Input placeholder is `CORRECT_PATH` (`prompt-template/block/wrong-summary-location.md:5`). The Write validator computes it as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` after resolving the active loop and parsing `current_round` from state (`hooks/loop-write-validator.sh:126-147`, `hooks/loop-write-validator.sh:204-210`). Output is markdown sent to stderr with exit code `2`; no state mutation occurs.
- gates_or_invariants: Invariant: any summary write outside `.humanize/rlcr/` is blocked and redirected to the active loop summary path (`hooks/loop-write-validator.sh:200-211`). A later gate separately enforces round number and exact active-loop directory path for summary files inside `.humanize/rlcr/` (`hooks/loop-write-validator.sh:230-265`). `finalize-summary.md` is handled before this check and allowed only when it is in the active loop directory (`hooks/loop-write-validator.sh:164-175`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-summary-location.md"` in `hooks/loop-write-validator.sh:206-210`. Depends on shared path/type helpers in `hooks/lib/loop-common.sh`, including `is_round_file_type`, `is_in_humanize_loop_dir`, `find_active_loop`, and `parse_state_file_strict`. The template rendering path is `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: This block only applies to Write, not Edit; Edit exits early for paths not inside `.humanize/rlcr/` (`hooks/loop-edit-validator.sh:82-88`). Bash attempts to modify summary files are blocked by a different template/helper, `summary_bash_blocked_message`, because Bash bypasses normal Write/Edit validation (`hooks/loop-bash-validator.sh:370-379`, `hooks/lib/loop-common.sh:600-609`). If no active loop exists, Write validator exits `0` before this block (`hooks/loop-write-validator.sh:126-133`), so ordinary non-loop summary-looking filenames are not blocked outside an active loop.
- validation_or_tests: `tests/test-allowlist-validators.sh` exercises summary round allowlist behavior and wrong-round blocking but does not directly assert this wrong-location template. `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` reference wrong-summary-location rendering, and the template loader’s single-pass rendering behavior is defined in `hooks/lib/template-loader.sh:50-132`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-139 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test suite for production hook JSON parsing, Bash command pattern detection, monitor helper behavior, and monitor edge-case resilience. It is an executable specification for failure handling around hook input and monitoring surfaces.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared `tests/test-helpers.sh`, creates a temp directory, then pipes JSON into real `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh` (`tests/robustness/test-hook-input-robustness.sh:12-20`, `tests/robustness/test-hook-input-robustness.sh:35-69`). It verifies valid JSON passes, malformed/missing fields are rejected without signal crashes, long commands and special characters pass, Unicode paths pass, unknown tools are ignored, deeply nested JSON is rejected, non-UTF8 input is rejected, and null-byte-stripped input does not crash (`tests/robustness/test-hook-input-robustness.sh:35-235`). It then tests monitor/log/terminal edge cases (`tests/robustness/test-hook-input-robustness.sh:237-342`), `command_modifies_file` patterns for `sed -i` and redirects (`tests/robustness/test-hook-input-robustness.sh:343-370`), production monitor parser functions (`tests/robustness/test-hook-input-robustness.sh:372-433`), and real `_humanize_monitor_codex` behavior for missing sessions, narrow terminals, and ANSI logs (`tests/robustness/test-hook-input-robustness.sh:435-670`).
- inputs_outputs_state: Inputs are JSON hook payloads, synthetic log files, synthetic `.humanize/rlcr` session directories, fake `$HOME`/`XDG_CACHE_HOME`, and temporary runner scripts created under the test directory. Outputs are pass/fail counters through `print_test_summary` and final process exit status (`tests/robustness/test-hook-input-robustness.sh:666-671`; helper at `tests/test-helpers.sh:58-78`). The test intentionally mutates only temp dirs: log files, fake project dirs, fake loop state, and generated runner scripts.
- gates_or_invariants: JSON hook invariant: Read/Write/Bash validators must validate syntax, require relevant fields, reject depth over 30, reject invalid UTF-8, and avoid signal exits (`tests/robustness/test-hook-input-robustness.sh:71-235`; implementation in `hooks/lib/loop-common.sh:76-150`, `hooks/loop-read-validator.sh:25-46`, `hooks/loop-write-validator.sh:25-46`, `hooks/loop-bash-validator.sh:24-45`). Unknown tools should pass through (`tests/robustness/test-hook-input-robustness.sh:163-175`). Monitor invariants: terminal width/log deletion/ANSI/binary/rapid updates should not crash (`tests/robustness/test-hook-input-robustness.sh:245-342`, `tests/robustness/test-hook-input-robustness.sh:444-664`). Command detection invariant: `command_modifies_file` must detect file mutations via `sed -i` and redirects (`tests/robustness/test-hook-input-robustness.sh:351-370`).
- dependencies_and_callers: Depends on production hook validators, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`, and `tests/test-helpers.sh` (`tests/robustness/test-hook-input-robustness.sh:14-18`, `tests/robustness/test-hook-input-robustness.sh:381-428`, `tests/robustness/test-hook-input-robustness.sh:470-471`). It relies on `jq` and `iconv` indirectly through `validate_hook_input` (`hooks/lib/loop-common.sh:86-100`) and shell utilities such as `sed`, `wc`, `grep`, `mktemp`, `tput` shims, and `kill`.
- edge_cases_or_failure_modes: The test explicitly expects invalid JSON, empty JSON, missing `tool_input.file_path`, excessive nesting, and non-UTF8 bytes to be rejected with non-signal nonzero exits (`tests/robustness/test-hook-input-robustness.sh:71-115`, `tests/robustness/test-hook-input-robustness.sh:177-217`). It documents that Bash strips null bytes before hook code can detect them, so the assertion is only “exit < 128” (`tests/robustness/test-hook-input-robustness.sh:219-235`). Inspection shows coverage asymmetry: Edit validator is not exercised here, and its implementation does not use `validate_hook_input` (`hooks/loop-edit-validator.sh:24-35`), so malformed Edit payload behavior is outside this test’s robustness contract. Another subtle issue: early tests use `RESULT=$(...) || EXIT_CODE=$?` and then `EXIT_CODE=${EXIT_CODE:-0}` without clearing `EXIT_CODE` before each case (`tests/robustness/test-hook-input-robustness.sh:39-64`); because the first three cases expect success, this is unlikely to mask current failures, but it is fragile if reused later.
- validation_or_tests: This file is itself the validation suite and finishes with `print_test_summary "Hook Input & Monitor Test Summary"` (`tests/robustness/test-hook-input-robustness.sh:666-671`). I did not run it because this branch export is read-only and the assignment requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 evidence headings present once for the assigned rows: cancel command, allowlist validator test, Codex review failure block, wrong summary location block, hook input robustness test
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`