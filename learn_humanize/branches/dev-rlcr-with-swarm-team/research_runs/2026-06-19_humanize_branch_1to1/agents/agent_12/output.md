# agent_12 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side prompt contract set for the RLCR loop. The directory contains four templates: `regular-review.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `code-review-phase.md`. Together they define when Codex can return completion, when it must block with findings, how it audits goal drift, and how it documents `codex review` invocations.
- algorithmic_behavior: `regular-review.md` requires Codex to read the original plan, current prompt, Claude summary, docs path, and goal tracker before deciding whether all tasks are complete (`prompt-template/codex/regular-review.md:3`, `:11`, `:20`, `:33`, `:49`). `full-alignment-review.md` is the stronger periodic audit; it checks acceptance criteria, forgotten items, deferrals, history, and stagnation, with `STOP` as the circuit-breaker output on stalled progress (`prompt-template/codex/full-alignment-review.md:19`, `:30`, `:36`, `:59`, `:84`). `goal-tracker-update-section.md` delegates post-round tracker edits to Codex, not Claude, after Round 0 (`prompt-template/codex/goal-tracker-update-section.md:1`, `:3`, `:6`, `:10`). `code-review-phase.md` records the automated `codex review --base ...` phase and expected priority marker parsing (`prompt-template/codex/code-review-phase.md:3`, `:14`, `:15`, `:30`).
- inputs_outputs_state: Inputs are template variables rendered by `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, historical round indices, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:793`, `:811`, `:842`, `:860`). Outputs are review prompt files under `.humanize/rlcr/<timestamp>/round-N-review-prompt.md` and review result expectations written to `round-N-review-result.md` (`hooks/loop-codex-stop-hook.sh:787`, `:789`, `:855`, `:872`). The state transition is reviewer-driven: findings continue the implementation loop; `COMPLETE` moves the RLCR loop toward review/finalize; `STOP` signals stagnation.
- gates_or_invariants: Completion is only allowed when every original task and acceptance criterion is fully met, with no deferred or pending work (`prompt-template/codex/regular-review.md:53`, `:54`, `:55`; `prompt-template/codex/full-alignment-review.md:88`, `:91`, `:92`). Full alignment rounds are selected by `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1`, and invalid intervals default to 5 in the Stop hook (`hooks/loop-codex-stop-hook.sh:799`, `:802`, `:807`). Goal tracker immutable sections must not be changed by update handling (`prompt-template/codex/goal-tracker-update-section.md:10`).
- dependencies_and_callers: The directory is loaded through `hooks/lib/template-loader.sh`, whose renderer keeps missing placeholders literal and performs single-pass substitution (`hooks/lib/template-loader.sh:7`, `:12`, `:54`, `:116`). `hooks/loop-codex-stop-hook.sh` renders all four templates with safe fallbacks (`hooks/loop-codex-stop-hook.sh:796`, `:844`, `:860`, `:983`). It coordinates with `scripts/setup-rlcr-loop.sh`, which writes `full_review_round`, `codex_model`, `codex_effort`, `plan_file`, `base_branch`, and `base_commit` into loop state (`scripts/setup-rlcr-loop.sh:767`, `:771`, `:775`, `:776`, `:779`, `:780`).
- edge_cases_or_failure_modes: Missing templates degrade to inline fallback prompts rather than empty output because callers use `load_and_render_safe` (`hooks/lib/template-loader.sh:170`, `:179`, `:192`). Missing variables remain as `{{VAR}}`, so incorrect caller variable wiring can leak unresolved placeholders (`hooks/lib/template-loader.sh:12`, `:120`). `code-review-phase.md` is an audit prompt only; it cannot steer `codex review`, because the template states that review mode uses git diff rather than prompt input (`prompt-template/codex/code-review-phase.md:3`, `:4`).
- validation_or_tests: `tests/test-template-references.sh` scans template loader references and verifies missing templates are failures (`tests/test-template-references.sh:83`, `:94`, `:102`, `:105`). `tests/test-templates-comprehensive.sh` has a real render check for `codex/goal-tracker-update-section.md` per repository search hits, but I did not execute tests because this run is read-only research.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-042 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script for `/humanize:start-rlcr-loop`. It converts user arguments plus repository state into an active `.humanize/rlcr/<timestamp>/` loop with state, plan backup/link, goal tracker, round-0 prompt, and session-id handoff.
- algorithmic_behavior: The script parses loop configuration (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--claude-answer-codex`, `--agent-teams`) at `scripts/setup-rlcr-loop.sh:133`. It rejects concurrent RLCR/PR loops (`:261`, `:273`), validates a plan unless skip-implementation mode bypasses it (`:309`, `:349`), validates git/codex prerequisites (`:334`, `:340`, `:579`), captures the working branch and review base (`:591`, `:652`, `:711`), then creates the loop directory and generated files (`:725`, `:731`, `:767`, `:809`, `:934`).
- inputs_outputs_state: Inputs are command-line args, `CLAUDE_PROJECT_DIR`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, git metadata, the plan file, and Codex CLI availability. Outputs include `.humanize/rlcr/<timestamp>/state.md` with `current_round: 0`, model, effort, timeout, plan path, branch, base commit, review flag, and agent team flag (`scripts/setup-rlcr-loop.sh:767`, `:769`, `:771`, `:780`, `:781`, `:784`); `.humanize/.pending-session-id` for the PostToolUse hook (`:789`, `:798`); `goal-tracker.md` (`:809`, `:837`); and `round-0-prompt.md` (`:938`, `:975`). In normal mode, state starts with `review_started: false`; in skip-implementation mode it starts review mode immediately and writes `.review-phase-started` (`:764`, `:800`).
- gates_or_invariants: Plan path must be relative, space-free, shell-metacharacter-free, non-symlink, readable, inside the project, outside submodules, and consistent with `--track-plan-file` (`scripts/setup-rlcr-loop.sh:356`, `:362`, `:370`, `:382`, `:418`, `:430`, `:444`, `:483`, `:499`). Plan content must have at least five lines and at least three non-comment content lines (`:519`, `:567`). Full alignment interval must be numeric and at least 2 (`:201`, `:210`). Working tree must be clean before loop start (`:630`, `:636`). Branch, Codex model, and effort values are checked for YAML-safe characters before being written to state (`:597`, `:607`, `:616`, `:703`).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md`, which whitelists only this setup script as the command implementation (`commands/start-rlcr-loop.md:4`, `:13`). It sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:26`, `:30`), relying on shared defaults and loop discovery (`hooks/lib/loop-common.sh:42`, `:44`, `:156`, `:161`). The pending session-id file is later consumed by `hooks/loop-post-bash-hook.sh` per repository references.
- edge_cases_or_failure_modes: `--skip-impl` with no plan intentionally bypasses plan validation and writes an internal placeholder plan (`scripts/setup-rlcr-loop.sh:311`, `:734`, `:752`). The script rejects remote-only base branches because `codex review --base` needs a local ref (`:653`, `:659`, `:660`). The plan content heuristic treats markdown heading lines beginning with `#` as comments for content counting, so heading-heavy plans can fail as insufficient content (`:529`, `:559`, `:567`). One potential serialization edge: branch/model/effort/base branch are YAML-safety checked, but `plan_file` is written unquoted to state (`:776`) after path checks that do not appear to reject colon characters.
- validation_or_tests: Related executable tests include `tests/test-plan-file-validation.sh`, robustness tests for setup/path/base-branch behavior, `tests/test-session-id.sh`, and `tests/test-agent-teams.sh` by repository reference search. I did not run them in this read-only branch export; a read-only `git status` attempt also hit sandbox temp-file restrictions from macOS tooling, so cleanliness was not independently verified.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-072 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that prevents prompt-template reference drift. It checks that shell hook references resolve to real templates and that critical validators use safe rendering with fallbacks.
- algorithmic_behavior: Section 1 hard-codes scripts to scan, then extracts paths from `load_template`, `load_and_render`, or `load_and_render_safe` calls that use `"$TEMPLATE_DIR"` and a quoted template path (`tests/test-template-references.sh:56`, `:57`, `:83`, `:94`, `:96`). Existing templates increment pass count; missing templates increment fail count and are collected (`:102`, `:105`, `:106`). Section 2 enumerates all markdown templates and warns if not directly referenced from `hooks/` (`:121`, `:126`, `:134`, `:136`, `:139`). Section 3 asserts common block templates exist (`:152`, `:161`). Section 4 verifies read/write/edit validators do not use unsafe `load_and_render` calls (`:176`, `:185`, `:189`, `:197`).
- inputs_outputs_state: Inputs are the repository root inferred from the test location, `prompt-template/`, and a fixed set of hook/library scripts (`tests/test-template-references.sh:14`, `:15`, `:16`, `:57`). Outputs are colored PASS/FAIL/WARN lines, counters, missing-template list, and process exit code (`:29`, `:34`, `:39`, `:208`, `:221`, `:229`). State is local counter arrays only: `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, `MISSING_TEMPLATES`, and `FOUND_REFERENCES` (`:25`, `:71`, `:72`).
- gates_or_invariants: Missing referenced templates are hard failures (`tests/test-template-references.sh:104`, `:105`, `:213`, `:225`). Missing scanned scripts are warnings rather than failures (`:75`, `:76`). Unreferenced templates are warnings because dynamic usage is allowed (`:130`, `:139`). Critical read/write/edit validators must use `load_and_render_safe`, not raw `load_and_render`, for template rendering (`:172`, `:174`, `:196`).
- dependencies_and_callers: Depends on standard shell utilities `find`, `grep`, `sed`, `basename`, and the project layout. It specifically validates hooks that call into `hooks/lib/template-loader.sh`, whose `load_and_render_safe` provides fallback behavior (`hooks/lib/template-loader.sh:170`, `:179`, `:202`).
- edge_cases_or_failure_modes: The script claims to scan all shell scripts that use template loading, but `SCRIPTS_TO_CHECK` is a fixed list and omits setup scripts such as `scripts/setup-pr-loop.sh`, even though that script loads `pr-loop/round-0-task-no-comments.md` (`tests/test-template-references.sh:56`, `:57`; `scripts/setup-pr-loop.sh:800`). The parser only detects same-line calls with literal `"$TEMPLATE_DIR"` followed by a quoted path, so multiline calls or variable-composed paths can be missed (`tests/test-template-references.sh:93`, `:96`). Because it does not use `set -e`, it accumulates findings rather than stopping at the first failure (`:12`).
- validation_or_tests: This file is itself the validator. Its final gate exits success only when `TESTS_FAILED` is zero (`tests/test-template-references.sh:221`, `:224`), otherwise it exits 1 and instructs fixing missing templates (`:227`, `:228`, `:229`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-102 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Blocking message template for RLCR validators that disallow direct `round-*-todos.md` file access and route task tracking to Claude Code native Task tools.
- algorithmic_behavior: The template is intentionally short and imperative: do not create or access `round-*-todos.md`; use `TaskCreate`, `TaskUpdate`, and `TaskList` instead (`prompt-template/block/todos-file-access.md:1`, `:3`, `:5`). It defines the human-facing reason emitted when read/write/edit/bash validators block todo-file access.
- inputs_outputs_state: The template has no placeholders and no internal state. It is loaded by `todos_blocked_message`, whose `action` parameter is accepted but not interpolated (`hooks/lib/loop-common.sh:562`, `:564`, `:570`). Output is markdown text to stderr from validators, typically paired with a blocking exit.
- gates_or_invariants: The invariant is that Claude should not manage round todo files directly; task state should live in native task tooling. There is a small exception surface in `is_allowlisted_file`, which allows only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` inside the active loop directory (`hooks/lib/loop-common.sh:546`, `:547`, `:548`, `:549`, `:550`). Bash validation specifically blocks modifications to non-allowlisted `round-[0-9]+-todos.md` targets (`hooks/loop-bash-validator.sh:385`, `:389`, `:390`).
- dependencies_and_callers: Loaded via `hooks/lib/loop-common.sh` and called by read/write/edit/bash validators (`hooks/lib/loop-common.sh:570`; repository references show `hooks/loop-read-validator.sh:63`, `hooks/loop-write-validator.sh:63`, `hooks/loop-edit-validator.sh:46`, and `hooks/loop-bash-validator.sh:390`). The template is included in the common template existence list in `tests/test-template-references.sh` (`tests/test-template-references.sh:152`, `:153`, `:161`).
- edge_cases_or_failure_modes: The message says no `round-*-todos.md` access, but the shared allowlist permits early-round todo files in the active loop directory, so the message is stricter than the implementation in those limited cases (`hooks/lib/loop-common.sh:546`, `:547`, `:548`). If the template is missing, the fallback message in `todos_blocked_message` preserves the same core instruction (`hooks/lib/loop-common.sh:566`, `:568`).
- validation_or_tests: `tests/test-template-references.sh` verifies this exact template exists as a common template (`tests/test-template-references.sh:152`, `:153`, `:163`). `tests/test-allowlist-validators.sh` covers allow/block behavior for round todo files by repository search references, but I did not execute tests in this read-only research run.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-132 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 task template for the “no review comments yet” branch. It prevents users/Claude from manually triggering first review comments and delegates initial review polling to the Stop hook.
- algorithmic_behavior: The template tells Claude that monitored bots will automatically review, and that Claude should simply write the initial summary and attempt to exit (`prompt-template/pr-loop/round-0-task-no-comments.md:4`, `:6`, `:8`, `:12`). It then codifies rules: do not comment to trigger review, do not modify `.humanize/pr-loop/` state files, and trust Stop hook polling (`:18`, `:20`, `:21`, `:22`). It describes the post-exit transition: poll for bot reviews, run local Codex validation if reviews indicate approval, continue on issues, end if all bots approve (`:26`, `:27`, `:28`, `:29`, `:30`).
- inputs_outputs_state: Inputs are rendered placeholders `{{ACTIVE_BOTS_DISPLAY}}` and `{{RESOLVE_PATH}}` (`prompt-template/pr-loop/round-0-task-no-comments.md:6`, `:12`). Output is a section of the generated PR-loop round-0 prompt, loaded into `TASK_CONTENT` when setup detects no existing comments (`scripts/setup-pr-loop.sh:800`). It does not mutate state directly; it steers the user to write the resolution summary and let the Stop hook advance PR-loop state.
- gates_or_invariants: First reviews must be automatic, not manually triggered by a comment (`prompt-template/pr-loop/round-0-task-no-comments.md:6`, `:20`). `.humanize/pr-loop/` files are system-managed (`:21`). Review polling cadence and timeout are contractually visible to Claude: every 30 seconds, up to 15 minutes per bot (`:26`, `:27`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe`, with an inline fallback containing the same no-comments instructions (`scripts/setup-pr-loop.sh:780`, `:794`, `:800`). It coordinates with `hooks/pr-loop-stop-hook.sh`, which enforces PR-loop comment behavior and polling according to repository references, including a no-trigger-comment block path.
- edge_cases_or_failure_modes: If `ACTIVE_BOTS_DISPLAY` or `RESOLVE_PATH` is not supplied, the single-pass renderer leaves unresolved placeholders visible rather than guessing (`hooks/lib/template-loader.sh:12`, `:120`). If the template is missing, `setup-pr-loop.sh` still has a fallback task body (`scripts/setup-pr-loop.sh:780`, `:800`). Template-reference coverage is weaker here because `tests/test-template-references.sh` mainly scans `hooks/`, while this template is loaded from `scripts/setup-pr-loop.sh` (`tests/test-template-references.sh:136`; `scripts/setup-pr-loop.sh:800`).
- validation_or_tests: Indirectly covered by template loader tests and PR-loop setup tests in the repository; direct existence is likely reported as an unreferenced-template warning by `tests/test-template-references.sh` because its direct reference is outside `hooks/`. I did not execute tests due the read-only research constraint.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`