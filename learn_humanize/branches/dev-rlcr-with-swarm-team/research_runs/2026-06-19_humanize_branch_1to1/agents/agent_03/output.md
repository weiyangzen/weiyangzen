# agent_03 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command contract layer for the Humanize loop system. The directory recursively contains exactly five command specs: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, `commands/start-pr-loop.md`, `commands/cancel-rlcr-loop.md`, and `commands/cancel-pr-loop.md`.
- algorithmic_behavior: Each command file delegates real state-machine work to scripts through frontmatter `allowed-tools`, then defines the operator/agent protocol for interpreting outputs. `start-pr-loop.md` routes to `scripts/setup-pr-loop.sh` and describes the PR review loop from PR detection through bot polling, local Codex validation, approval, max iterations, or cancellation at lines 10-24 and 37-62. `start-rlcr-loop.md` routes to `scripts/setup-rlcr-loop.sh` and defines the RLCR implementation, review, and finalize phases at lines 16-25 and 60-67. `gen-plan.md` is the most algorithmic command spec: it enforces IO validation, repository relevance checking, draft analysis, user clarification, deterministic plan generation, and final review/write phases at lines 19-27, 30-47, 50-73, 76-109, 111-140, 142-248, and 251-304.
- inputs_outputs_state: Inputs are slash-command arguments such as bot flags, max iteration counts, Codex model/timeouts, plan paths, or draft/output paths. Outputs are either setup/cancel script outputs interpreted by the agent, generated `.humanize/...` loop state via scripts, or a generated plan file. `cancel-pr-loop.md` explicitly maps first-line script tokens `NO_LOOP`, `NO_ACTIVE_LOOP`, and `CANCELLED` to user-facing responses at lines 17-19; `cancel-rlcr-loop.md` also handles `CANCELLED_FINALIZE` and `FINALIZE_NEEDS_CONFIRM` at lines 17-34.
- gates_or_invariants: Commands intentionally keep business logic in scripts. PR and RLCR loops are mutually exclusive by setup-script policy referenced in `start-pr-loop.md` and `start-rlcr-loop.md`; both forbid “cheating” by editing state or canceling directly in-loop (`start-pr-loop.md` lines 50-56, `start-rlcr-loop.md` lines 46-52). `gen-plan.md` requires AC-X acceptance criteria, positive and negative tests, no time estimates, no line-number code references, preservation of all original draft content, and code comments free of plan workflow terminology at lines 225-248.
- dependencies_and_callers: These files are invoked by the Claude Code slash-command/plugin layer. They call `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and `scripts/validate-gen-plan-io.sh`; `gen-plan.md` also requires `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` tools in frontmatter lines 4-11.
- edge_cases_or_failure_modes: `cancel-rlcr-loop.md` has a finalize-phase confirmation branch to prevent premature cancellation during finalization. `gen-plan.md` fails closed for missing input, empty input, missing output dir, existing output file, no write permission, invalid args, or missing template based on exit codes listed at lines 38-47. `start-pr-loop.md` requires at least one bot flag at lines 25-30; `start-rlcr-loop.md` supports `--skip-impl` for review-only workflows at lines 69-86.
- validation_or_tests: Command behavior is indirectly tested by script tests and template tests: `tests/test-pr-loop-scripts.sh` validates `setup-pr-loop.sh` and `cancel-pr-loop.sh` argument/exit behavior; `tests/test-gen-plan.sh` and `scripts/validate-gen-plan-io.sh` cover plan IO validation; `tests/test-templates-comprehensive.sh` validates template rendering referenced by command-driven hooks.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-033 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: PR-loop cancellation transition script. It changes the active PR loop from `state.md` to terminal `cancel-state.md` and creates `.cancel-requested` so hooks/polling can observe cancellation.
- algorithmic_behavior: Parses `--force` and help flags at lines 24-66, resolves project root from `CLAUDE_PROJECT_DIR` or `pwd` at line 72, selects the newest `.humanize/pr-loop/*/` directory by reverse lexical sort at line 76, requires `state.md` to exist at lines 88-98, extracts `current_round`, `max_iterations`, and `pr_number` from YAML frontmatter with `grep`/`sed` at lines 104-112, then touches `.cancel-requested` and moves `state.md` to `cancel-state.md` at lines 118-122.
- inputs_outputs_state: Input is optional `--force`; in this PR script it is parsed but has no additional effect (`FORCE` is set at lines 24-30 and unused afterward). Outputs are first-line machine tokens: `NO_LOOP` on no PR-loop directories, `NO_ACTIVE_LOOP` when newest directory lacks `state.md`, and `CANCELLED` on success at lines 78-81, 95-97, and 128-130. State output is `$LOOP_DIR/.cancel-requested` plus `$LOOP_DIR/cancel-state.md`.
- gates_or_invariants: The script only targets `.humanize/pr-loop`, not `.humanize/rlcr`, as documented in help lines 48-56 and implemented by `LOOP_BASE_DIR="$PROJECT_ROOT/.humanize/pr-loop"` at line 73. It treats only the newest loop directory as cancellable and only if `state.md` exists, aligning with `find_active_pr_loop` semantics in `hooks/lib/loop-common.sh` lines 914-933.
- dependencies_and_callers: Called by slash command `commands/cancel-pr-loop.md` lines 11-19. Its `.cancel-requested` output is consumed by `hooks/pr-loop-stop-hook.sh`, which exits polling if the signal appears at lines 952-956. Bash validators contain a broader cancel authorization mechanism for state moves, with `.cancel-requested` required in `is_cancel_authorized` at `hooks/lib/loop-common.sh` lines 652-829.
- edge_cases_or_failure_modes: Missing loop base or empty loop directory returns exit 1 with `NO_LOOP`. A terminal newest directory containing only `cancel-state.md`, `approve-state.md`, etc. returns `NO_ACTIVE_LOOP` because `state.md` is absent. Malformed state frontmatter does not block cancellation; missing fields are replaced with `?` at lines 109-112. `set -euo pipefail` means failed `touch` or `mv` exits as an unhandled error rather than printing a custom token.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` checks help output, `NO_LOOP`, and active cancellation/rename behavior at lines 181-252. Cancel-signal safety is covered more generally by `tests/test-cancel-signal-file.sh` lines 80-155 and `tests/robustness/test-cancel-security-robustness.sh` lines 39-90 and 99-250, which assert only a signal-authorized `mv state.md cancel-state.md` shape is allowed through validators.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-063 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop hook, polling, validator, comment-fetching, and monitor behavior. It is not production code, but it codifies the core PR-loop algorithm’s expected state transitions and guardrails.
- algorithmic_behavior: Defines `run_hook_tests()` and nested shell tests for validator protections, comment sorting/deduplication, stop-hook trigger logic, E2E stop-hook execution with mocked `gh`/`git`, approval-only PR review handling, fixture-backed fetch/poll scripts, wrong-round protection, and monitor display. Test groups start at lines 16, 212, 263, 470, 758, 1152, 1223, 1423, and 1539.
- inputs_outputs_state: Inputs are generated temp `.humanize/pr-loop/<timestamp>/state.md` files, mock hook JSON, mocked `gh` API responses, fixtures under `tests/fixtures`, and real project scripts/hooks. Outputs are `pass`/`fail` assertions plus generated temp files such as `round-N-pr-resolve.md`, fetched comment markdown, and JSON from `poll-pr-reviews.sh`. The test file deliberately mutates only test directories.
- gates_or_invariants: Encodes key invariants: `active_bots` and `configured_bots` are YAML lists, not loose strings (lines 27-67, 289-325, 699-725); PR loop `state.md` and generated `round-N-pr-comment.md` are read-only while correct-round `round-N-pr-resolve.md` is writable (lines 69-167 and 1432-1535); human comments sort before bot comments and timestamps sort newest first (lines 223-259); comments dedupe by ID (lines 272-287); configured bots are polled even if no longer active so previously approved bots can be re-added when they report issues (lines 327-389).
- dependencies_and_callers: Directly calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, and `tests/setup-monitor-test-env.sh`. It also asserts jq snippets mirrored from the production stop hook and fetch/poll scripts.
- edge_cases_or_failure_modes: Covers missing resolution summaries blocking exit (lines 767-845), trigger comments filtered to the current GitHub user (lines 539-587), newer trigger refresh (lines 589-605), round > 0 missing triggers blocking (lines 607-623), round 0 preferring `last_trigger_at` over `started_at` when present (lines 625-646), fallback to `started_at` otherwise (lines 647-666), trigger-anchored per-bot timeout (lines 668-697), paginated trigger detection (lines 948-1051), and use of trigger timestamp for round 0 in runtime logs (lines 1053-1143).
- validation_or_tests: The file is itself validation. It also verifies approval-only reviews with empty bodies are preserved via `[Review state: ...]` placeholders (lines 1161-1219), fixture-backed `fetch-pr-comments.sh`/`poll-pr-reviews.sh` include and filter approval-only reviews (lines 1232-1419), and monitor output parses YAML lists and empty active bots (lines 1548-1621).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-093 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Blocking prompt template for a PR-loop state-machine gate: stop-hook exit is blocked until the user posts an explicit bot trigger comment when the current startup case or round requires it.
- algorithmic_behavior: Renders a short reason explaining that the loop is waiting for a trigger comment, including `{{STARTUP_CASE}}`, `{{STARTUP_CASE_DESC}}`, `{{CURRENT_ROUND}}`, and `{{BOT_MENTION_STRING}}` placeholders at lines 5-14. It gives a concrete trigger-comment form using the configured bot mentions.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/pr-loop-stop-hook.sh` lines 728-745 after `REQUIRE_TRIGGER=true` and `PR_LAST_TRIGGER_AT` is empty. Output is embedded in hook JSON as the block `reason`, with system message `PR Loop: Missing trigger comment - please @mention bots first`.
- gates_or_invariants: Enforces that a user-authored PR comment mentioning the configured bots is a required transition before polling in round > 0, startup cases 4/5, or when new commits invalidate stale triggers. The stop hook computes those conditions at lines 613-644 and blocks with this template at lines 728-747.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh`; fallback behavior exists if the template is missing. Trigger detection itself depends on GitHub issue comments from the current user and configured bot mention patterns in `hooks/pr-loop-stop-hook.sh` lines 454-526 and 567-607.
- edge_cases_or_failure_modes: If variables are absent, the template loader preserves unresolved placeholders rather than doing recursive substitution. If no trigger is required, this template is skipped. If a trigger timestamp exists but no comment ID exists, the later Claude-eyes check only warns at `hooks/pr-loop-stop-hook.sh` lines 794-797 instead of using this block.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` validates missing-trigger blocking at lines 607-623, runtime trigger detection at lines 846-947, pagination at lines 948-1051, and runtime round-0 trigger priority at lines 1053-1143.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-123 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Shared RLCR Codex-review prompt section that delegates post-Round-0 `goal-tracker.md` mutations to Codex/operator responsibility while preserving immutable goal sections.
- algorithmic_behavior: Tells Codex that if Claude’s summary contains a “Goal Tracker Update Request,” it must evaluate whether the request is justified, update `@{{GOAL_TRACKER_FILE}}` if approved, reject with rationale if not, and never modify the immutable Ultimate Goal and Acceptance Criteria section at lines 1-11. Lines 13-17 enumerate supported update types: task completion, new issues, plan changes, and justified deferrals.
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE`, rendered through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` lines 793-797. Output is inserted into regular and full-alignment Codex review prompts at `hooks/loop-codex-stop-hook.sh` lines 819-840, influencing Codex’s review instructions rather than directly editing state itself.
- gates_or_invariants: Preserves goal drift controls: mutable sections may change, immutable ultimate goal/acceptance criteria may not. It establishes a human/Codex approval gate for Claude-requested tracker changes after Round 0 rather than allowing Claude to directly mutate the tracker.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`; rendering behavior depends on `hooks/lib/template-loader.sh`, whose single-pass substitution prevents placeholder injection and keeps missing variables literal. This template is separate from the PR-loop-specific `prompt-template/pr-loop/codex-goal-tracker-update.md`, which updates PR issue summary tables.
- edge_cases_or_failure_modes: If the template is missing, the RLCR stop hook falls back to a shorter “Goal Tracker Updates” instruction at lines 793-797. If Codex rejects a request, the template requires the review to explain why. Deferrals are explicitly constrained to strong justification.
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this real template with `GOAL_TRACKER_FILE=.humanize/rlcr/20240101/goal-tracker.md` and asserts both the section heading and path substitution at lines 531-539.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 Item Evidence headings present, each corresponding to one assigned non-`none` row.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`