# agent_06 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts/` is the repository’s shell orchestration surface for Humanize loop runtime setup, cancellation, PR review polling, reviewer-status classification, reaction checks, monitor display support, timeout portability, and gen-plan IO validation. It coordinates closely with `hooks/`, `prompt-template/`, `.humanize/rlcr/`, and `.humanize/pr-loop/`.
- algorithmic_behavior: The directory contains PR-loop entrypoints (`setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `check-bot-reactions.sh`, `check-pr-reviewer-status.sh`), RLCR-loop entrypoints (`setup-rlcr-loop.sh`, `cancel-rlcr-loop.sh`), monitor helpers (`humanize.sh`, `lib/monitor-common.sh`), cross-platform timeout support (`portable-timeout.sh`), and a planner IO validator (`validate-gen-plan-io.sh`). `setup-pr-loop.sh` validates bot flags and prerequisites, resolves PR/fork repository context, fetches initial comments, computes startup case through `check-pr-reviewer-status.sh`, writes loop state and goal tracker, then emits the initial prompt (`scripts/setup-pr-loop.sh:180`, `scripts/setup-pr-loop.sh:209`, `scripts/setup-pr-loop.sh:430`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:712`). `setup-rlcr-loop.sh` performs analogous RLCR setup with stricter plan-file/git validations and writes RLCR state/goal tracker/prompt surfaces (`scripts/setup-rlcr-loop.sh:248`, `scripts/setup-rlcr-loop.sh:346`, `scripts/setup-rlcr-loop.sh:626`, `scripts/setup-rlcr-loop.sh:809`).
- inputs_outputs_state: Inputs are CLI args, Git state, GitHub CLI/API output, plan/comment timestamps, configured bots, and environment such as `CLAUDE_PROJECT_DIR`. Outputs include `.humanize/pr-loop/<timestamp>/state.md`, `round-*-pr-comment.md`, `round-*-prompt.md`, `goal-tracker.md`, cancellation state files, formatted JSON, and markdown reports. State transitions are file-based: `state.md` is created by setup, moved to `cancel-state.md` by cancellation, or later moved by hooks to `approve-state.md`, `merged-state.md`, `closed-state.md`, `maxiter-state.md`, etc.
- gates_or_invariants: Enforces one active loop at a time (`setup-pr-loop.sh:222`, `setup-rlcr-loop.sh:259`), required bot flags (`setup-pr-loop.sh:180`), Git repo/commit/clean-tree requirements, `gh`/`codex` availability, YAML-safe branch/model/effort values (`setup-pr-loop.sh:384`, `setup-pr-loop.sh:393`), relative safe plan paths for RLCR (`setup-rlcr-loop.sh:356`, `scripts/setup-rlcr-loop.sh:370`), fork PR base-repo resolution, timeout wrapping for Git/GitHub calls, and read-only generated prompt/comment files through shared hook library messaging (`hooks/lib/loop-common.sh:1017`).
- dependencies_and_callers: Slash commands call setup/cancel scripts (`commands/start-pr-loop.md`, `commands/start-rlcr-loop.md`, `commands/cancel-pr-loop.md`, `commands/cancel-rlcr-loop.md`). `hooks/pr-loop-stop-hook.sh` calls `check-bot-reactions.sh`, `poll-pr-reviews.sh`, and `check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:676`, `hooks/pr-loop-stop-hook.sh:812`, `hooks/pr-loop-stop-hook.sh:1557`). `humanize.sh` sources `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh` (`scripts/humanize.sh:6`, `scripts/humanize.sh:12`). Most GitHub-facing scripts depend on `gh`, `jq`, and `portable-timeout.sh`.
- edge_cases_or_failure_modes: Covered cases include fork PRs whose PR number exists only in parent repo, old triggers after force push, new commits invalidating prior triggers, API failures and partial comment fetches, bot reaction absence, bot timeout, unavailable `gh`/`jq`/`codex`, unpushed commits, uncommitted changes, unsafe YAML characters, missing plan file, symlink/path traversal risks, and macOS Bash 3.2 limitations avoided by not using namerefs/associative arrays in hook-adjacent paths.
- validation_or_tests: `tests/test-pr-loop-system.sh` exercises reviewer-status cases, script setup, reactions, monitor parsing, and PR goal tracker behavior (`tests/test-pr-loop-system.sh:159`, `tests/test-pr-loop-system.sh:500`, `tests/test-pr-loop-system.sh:1327`). `tests/test-pr-loop-scripts.sh` covers setup/cancel/fetch/poll script CLI behavior. `tests/test-pr-loop-stophook.sh` is the focused executable stop-hook specification. I inspected tests only; no test commands were executed.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-036 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Startup-case classifier for PR loop control flow. It determines whether no/some/all configured bot reviewers have commented and whether any reviewer’s latest review is stale relative to the PR’s latest commit.
- algorithmic_behavior: Parses `<pr_number> --bots <bot1,bot2>` (`scripts/check-pr-reviewer-status.sh:49`). Maps logical bot names to GitHub authors, with special `codex -> chatgpt-codex-connector[bot]` mapping (`scripts/check-pr-reviewer-status.sh:94`). Resolves the PR base repo by trying the current repo first, then parent repo for fork support (`scripts/check-pr-reviewer-status.sh:113`, `scripts/check-pr-reviewer-status.sh:126`). Fetches latest PR head/commit date, issue comments, inline review comments, and PR review submissions, then combines all three sources (`scripts/check-pr-reviewer-status.sh:142`, `scripts/check-pr-reviewer-status.sh:156`, `scripts/check-pr-reviewer-status.sh:159`, `scripts/check-pr-reviewer-status.sh:164`, `scripts/check-pr-reviewer-status.sh:168`). Per bot, it finds matching comments/reviews, records commented/missing/stale arrays, and tracks newest review timestamp (`scripts/check-pr-reviewer-status.sh:179`, `scripts/check-pr-reviewer-status.sh:183`, `scripts/check-pr-reviewer-status.sh:194`, `scripts/check-pr-reviewer-status.sh:199`).
- inputs_outputs_state: Inputs are PR number, comma-separated bot list, GitHub repo metadata, PR commit metadata, issue comments, pull review comments, and PR review submissions. Output is JSON with `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` (`scripts/check-pr-reviewer-status.sh:259`). It does not persist state itself; callers write its case fields into PR-loop `state.md`.
- gates_or_invariants: Requires PR number and `--bots`; rejects unknown options and multiple PR numbers (`scripts/check-pr-reviewer-status.sh:75`, `scripts/check-pr-reviewer-status.sh:81`). Fails closed if commit info cannot be fetched (`scripts/check-pr-reviewer-status.sh:143`). Uses per-bot staleness rather than global newest-review staleness, so one fresh bot review does not hide another stale bot review (`scripts/check-pr-reviewer-status.sh:192`).
- dependencies_and_callers: Sources `portable-timeout.sh` and uses `run_with_timeout` for `gh` calls (`scripts/check-pr-reviewer-status.sh:39`). Depends on `gh` and `jq`. Called during PR-loop setup (`scripts/setup-pr-loop.sh:438`) and dynamically by the PR stop hook to update `startup_case` as comments arrive (`hooks/pr-loop-stop-hook.sh:1557`).
- edge_cases_or_failure_modes: Comment fetch failures degrade to empty arrays, which can classify reviewers as missing if API calls fail (`scripts/check-pr-reviewer-status.sh:156`, `scripts/check-pr-reviewer-status.sh:159`, `scripts/check-pr-reviewer-status.sh:164`). Timestamp comparison is string-based ISO-8601 lexical comparison, so it assumes normalized UTC timestamps. Empty bot entries from malformed comma lists are not explicitly filtered. If repo resolution fails, it warns and uses current repo, which may still fail later (`scripts/check-pr-reviewer-status.sh:137`).
- validation_or_tests: `tests/test-pr-loop-system.sh` checks case 1 no comments, case 2 partial comments, case 3 all commented, case 4 stale all-commented reviews, and case 5 partial/stale reviews (`tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:173`, `tests/test-pr-loop-system.sh:493`, `tests/test-pr-loop-system.sh:1327`, `tests/test-pr-loop-system.sh:1347`). `tests/test-pr-loop-stophook.sh` indirectly validates dynamic `startup_case` refresh through the stop hook.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-066 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop stop-hook behavior. It models stop-hook state transitions using temporary `.humanize/pr-loop/...` directories plus mocked `gh`, `git`, and `codex` commands.
- algorithmic_behavior: Defines `run_stophook_tests()` and nested scenario functions, then invokes the selected stop-hook scenarios at the end (`tests/test-pr-loop-stophook.sh:15`, `tests/test-pr-loop-stophook.sh:1768`). Each test creates a synthetic `state.md`, writes required `round-N-pr-resolve.md`, injects mock binaries through `PATH`, sets `CLAUDE_PROJECT_DIR`, runs `hooks/pr-loop-stop-hook.sh`, and asserts either block messages or state-file transitions.
- inputs_outputs_state: Inputs are fixture YAML frontmatter fields such as `current_round`, `configured_bots`, `active_bots`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, `trigger_comment_id`, plus mocked GitHub API responses. Outputs under test are hook stderr/stdout JSON block reasons, `state.md` rewrites, `approve-state.md`, `round-*-pr-check.md`, and active-bot list changes.
- gates_or_invariants: Specifies that old trigger comments before `latest_commit_at` are rejected (`tests/test-pr-loop-stophook.sh:20`), startup case 1 round 0 does not require a trigger (`tests/test-pr-loop-stophook.sh:140`), empty `active_bots` completes the loop (`tests/test-pr-loop-stophook.sh:246`), unpushed commits block exit (`tests/test-pr-loop-stophook.sh:452`), history rewrite blocks until re-triggered (`tests/test-pr-loop-stophook.sh:538`), case 4/5 missing trigger blocks (`tests/test-pr-loop-stophook.sh:642`), timed-out bots are removed (`tests/test-pr-loop-stophook.sh:754`), Codex `+1` removes codex (`tests/test-pr-loop-stophook.sh:896`), missing Claude eyes reaction blocks (`tests/test-pr-loop-stophook.sh:1011`), startup case is dynamically re-evaluated (`tests/test-pr-loop-stophook.sh:1145`), fork PRs use parent repo lookup (`tests/test-pr-loop-stophook.sh:1390`), and mixed approvals must not prematurely complete the loop (`tests/test-pr-loop-stophook.sh:1519`).
- dependencies_and_callers: Intended to be sourced by a larger shell test harness that defines `TEST_DIR`, `PROJECT_ROOT`, `pass`, and `fail` (`tests/test-pr-loop-stophook.sh:12`). It depends on shell `timeout`, `date` with GNU/BSD fallback flags, `grep`, `sed`, and the production `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: The test suite intentionally covers races around trigger timestamps, force-push state invalidation, bot silence/timeouts, reaction-only approval, fork PR lookup, and partial Codex analysis. One defined function `test_stophook_dynamic_startup_case` appears not to be invoked in the final run list; the later `test_stophook_dynamic_startup_case_update` is invoked instead (`tests/test-pr-loop-stophook.sh:328`, `tests/test-pr-loop-stophook.sh:1778`).
- validation_or_tests: This file is itself validation. It contains assertions T-STOPHOOK-1 through T-STOPHOOK-13c and verifies both messages and file-system state. I inspected it directly but did not execute it.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-096 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the write-validation gate that prevents agents from editing generated PR-loop prompt/comment files.
- algorithmic_behavior: The template states that `round-*-pr-comment.md` and `round-*-prompt.md` files in `.humanize/pr-loop/` cannot be written because the PR loop system generates and manages them (`prompt-template/block/pr-loop-prompt-write.md:1`, `prompt-template/block/pr-loop-prompt-write.md:3`). It distinguishes fetched PR comments from round instructions (`prompt-template/block/pr-loop-prompt-write.md:5`).
- inputs_outputs_state: No runtime inputs or placeholders. Output is a markdown block message returned by shared hook logic. It does not mutate state.
- gates_or_invariants: Encodes the invariant that generated `round-*-pr-comment.md` and `round-*-prompt.md` are read-only to the agent. The shared function `pr_loop_prompt_blocked_message()` loads it with fallback text (`hooks/lib/loop-common.sh:1017`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/lib/loop-common.sh`; the template loader uses single-pass `{{VAR}}` substitution and fallback behavior (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:134`). It is referenced by template reference tests (`tests/test-template-references.sh:157`).
- edge_cases_or_failure_modes: If missing, fallback content still blocks writes, but with less exact copy. Because this template has no placeholders, template injection is not relevant.
- validation_or_tests: Covered by template reference/comprehensive tests and indirectly by hook-system tests that validate write blocking for generated loop files.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-126 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: Prompt contract injected into the stop hook’s local Codex review prompt to make Codex update PR-loop `goal-tracker.md` after analyzing bot feedback.
- algorithmic_behavior: Requires Codex to update `{{GOAL_TRACKER_FILE}}`, add one Issue Summary row per reviewer for `{{NEXT_ROUND}}`, adjust total found/resolved/remaining statistics, and append an Issue Log entry for the round (`prompt-template/pr-loop/codex-goal-tracker-update.md:1`, `prompt-template/pr-loop/codex-goal-tracker-update.md:7`, `prompt-template/pr-loop/codex-goal-tracker-update.md:12`, `prompt-template/pr-loop/codex-goal-tracker-update.md:17`). It defines allowed status values and a markdown example (`prompt-template/pr-loop/codex-goal-tracker-update.md:10`, `prompt-template/pr-loop/codex-goal-tracker-update.md:22`). Important rules restrict edits to Issue Summary, Total Statistics, and Issue Log while preserving PR Information and Ultimate Goal (`prompt-template/pr-loop/codex-goal-tracker-update.md:57`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `NEXT_ROUND` placeholders. Output is prompt text embedded into `round-${NEXT_ROUND}-codex-prompt.md`; expected downstream state is modified `goal-tracker.md`. The production stop hook also has an idempotent helper fallback, `update_pr_goal_tracker`, to repair or complete tracker updates if Codex omits them (`hooks/lib/loop-common.sh:1270`).
- gates_or_invariants: Enforces additive, per-reviewer row updates, cumulative statistics consistency, and limited write scope. It requires `Remaining = Total Found - Total Resolved` (`prompt-template/pr-loop/codex-goal-tracker-update.md:15`) and separate rows per reviewer (`prompt-template/pr-loop/codex-goal-tracker-update.md:64`).
- dependencies_and_callers: Loaded in `hooks/pr-loop-stop-hook.sh` before local Codex execution (`hooks/pr-loop-stop-hook.sh:1253`, `hooks/pr-loop-stop-hook.sh:1262`) and injected into the prompt that asks Codex to output per-bot status and final recommendation (`hooks/pr-loop-stop-hook.sh:1264`, `hooks/pr-loop-stop-hook.sh:1302`). Uses the shared template loader and fallback text if unavailable.
- edge_cases_or_failure_modes: The template assumes the goal tracker already has the named sections and table schema. It relies on Codex following instructions; the hook’s helper protects against missing/partial updates through round+reviewer idempotency checks (`hooks/lib/loop-common.sh:1303`). Mixed approval is a known edge case: resolved count must remain zero unless all bots approve, verified by the stop-hook test (`tests/test-pr-loop-stophook.sh:1519`, `tests/test-pr-loop-stophook.sh:1734`).
- validation_or_tests: `tests/test-pr-loop-stophook.sh` validates mixed approval behavior and tracker-related state. `tests/test-pr-loop-system.sh` has stop-hook goal tracker update integration coverage and tracker creation checks. Template reference tests ensure the template is present.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 section headers present; each assigned research item represented once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`