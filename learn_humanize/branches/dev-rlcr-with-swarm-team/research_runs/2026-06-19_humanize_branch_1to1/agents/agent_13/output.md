# agent_13 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation prompt scaffold for `/humanize:gen-plan`. Recursive inspection found exactly one descendant, `prompt-template/plan/gen-plan-template.md`, which defines the structured plan contract that the gen-plan workflow seeds before the agent edits the output.
- algorithmic_behavior: `gen-plan-template.md` encodes the output schema: title, goal description, TDD-style acceptance criteria with positive and negative tests, path boundaries, feasibility hints, dependencies/sequence, and implementation notes (`prompt-template/plan/gen-plan-template.md:1`, `:6`, `:25`, `:46`, `:57`, `:69`). It is copied by `scripts/validate-gen-plan-io.sh` before the original draft is appended (`scripts/validate-gen-plan-io.sh:148-157`), then `commands/gen-plan.md` instructs the agent to replace placeholders while preserving the appended draft (`commands/gen-plan.md:251-260`).
- inputs_outputs_state: Input is the human draft plus later clarifications from the gen-plan command flow. Output is a newly seeded plan file containing this template and an appended `--- Original Design Draft Start ---` / `End` section (`scripts/validate-gen-plan-io.sh:151-157`). State transition is: output path must not exist, validator creates scaffold, agent edits scaffold into final plan, original draft remains intact.
- gates_or_invariants: Acceptance criteria must be deterministic and include positive/negative tests (`prompt-template/plan/gen-plan-template.md:6-23`). Path boundaries must define maximum/minimum acceptable scope and allowed choices (`:25-44`). Deterministic designs may collapse upper/lower bounds and fix allowed choices (`:44`). Implementation code/comments must avoid plan-progress terminology such as `AC-`, `Milestone`, `Step`, and `Phase` (`:69-74`). `commands/gen-plan.md` reinforces no time estimates, AC-X format, clear dependencies, and complete preservation of draft information (`commands/gen-plan.md:225-247`).
- dependencies_and_callers: Directly used by `scripts/validate-gen-plan-io.sh`, resolved from `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`scripts/validate-gen-plan-io.sh:133-140`). The command surface is `commands/gen-plan.md`, which allows the validation script and uses it as Phase 1 (`commands/gen-plan.md:1-11`, `:30-47`). The comprehensive template tests load and syntax-check all Markdown templates under `prompt-template/`, so this plan template is included in recursive loading/syntax validation (`tests/test-templates-comprehensive.sh:90-107`, `:118-194`).
- edge_cases_or_failure_modes: The template explicitly handles deterministic designs with few implementation choices (`prompt-template/plan/gen-plan-template.md:44`). If the template file is missing, gen-plan validation fails with exit code 7 before output generation completes (`scripts/validate-gen-plan-io.sh:142-145`). The directory is not required by `validate_template_dir`, which only checks `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:216-221`), so plan-template presence is guarded by the gen-plan IO script rather than generic template-dir validation.
- validation_or_tests: `tests/test-templates-comprehensive.sh` recursively loads and syntax-validates all `*.md` templates (`:90-107`, `:118-194`). `tests/test-gen-plan.sh` validates the gen-plan command and the IO script behavior, including successful valid-path output generation (`tests/test-gen-plan.sh:426-539`). Tests were inspected, not executed, per read-only research scope.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-043 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime gate and bootstrapper for `/humanize:gen-plan`. Despite the name, it both validates IO and writes the initial output plan by combining the plan template with the source draft.
- algorithmic_behavior: Parses only `--input`, `--output`, `-h`, and `--help`; unknown or missing values call `usage` and exit 6 (`scripts/validate-gen-plan-io.sh:16-56`). Normalizes input/output paths through `realpath -m` with fallback to the original path (`:69-72`). Performs ordered validation checks: input exists, input non-empty, output directory exists, output is not a directory, output does not exist, output directory writable (`:79-124`). On success, it counts input lines, locates `prompt-template/plan/gen-plan-template.md`, copies it to output, appends a draft delimiter block and original draft content, then exits 0 (`:126-160`).
- inputs_outputs_state: Inputs are CLI args, filesystem paths, optional `CLAUDE_PLUGIN_ROOT`, and the source draft file (`scripts/validate-gen-plan-io.sh:26-47`, `:133-140`). Outputs are validation status text, specific exit codes, and a newly created output plan file (`:4-12`, `:148-157`). State transition is all-or-nothing before copy: output must be absent through validation, then becomes a seeded plan scaffold plus original draft.
- gates_or_invariants: Exit code contract is explicit: 1 input missing, 2 input empty, 3 output directory missing, 4 output exists or is directory, 5 no write permission, 6 invalid args/help, 7 missing plan template (`scripts/validate-gen-plan-io.sh:4-12`). Output overwrite is forbidden (`:103-116`). The output directory must already exist and be writable (`:95-124`). Template existence is a plugin-configuration gate (`:142-145`).
- dependencies_and_callers: Invoked by `commands/gen-plan.md` Phase 1 as `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS`, and the command maps each exit code to a stop/continue decision (`commands/gen-plan.md:30-47`). Depends on shell utilities `realpath`, `dirname`, `wc`, `cp`, and `cat`, with `realpath` fallback (`scripts/validate-gen-plan-io.sh:69-72`, `:127`, `:149`, `:155`).
- edge_cases_or_failure_modes: `--help` intentionally exits 6 through `usage`, so help is treated as invalid/stop in command flow (`scripts/validate-gen-plan-io.sh:48-50`; `tests/test-gen-plan.sh:532-539`). If `realpath -m` is unavailable, paths are not canonicalized but validation continues (`scripts/validate-gen-plan-io.sh:69-71`). A copy/append failure after validation is not mapped to the documented exit codes because `set -e` will terminate with the failing command’s status (`:14`, `:148-157`). There is a race window between existence checks and `cp` if another process creates the output path after validation (`:103-116`, `:149`). Permission is tested at directory level with `-w`; later filesystem failures such as quota/full disk remain possible (`:118-124`).
- validation_or_tests: `tests/test-gen-plan.sh` exercises missing option values, flag-as-value, unknown option, missing input, empty input, missing output directory, output exists, output is directory, valid paths, and help exit behavior (`tests/test-gen-plan.sh:438-539`). `commands/gen-plan.md` documents the operational exit-code handling (`commands/gen-plan.md:38-47`). Tests were inspected, not executed.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-073 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template loading/rendering subsystem. It verifies template discovery, placeholder grammar, safe rendering behavior, fallback behavior, and representative real template integration.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, obtains the template directory via `get_template_dir`, and maintains pass/fail/warning counters (`tests/test-templates-comprehensive.sh:13-59`). Section 1 asserts required subdirectories `block`, `codex`, and `claude` exist (`:61-80`). Section 2 loads every `*.md` template under the template root and rejects empty/failed loads (`:82-112`). Section 3 scans every template for malformed placeholders, including extra braces, wrong brackets, unclosed placeholders, spaces inside placeholders, and lowercase placeholder names (`:113-194`). Later sections directly exercise rendering behavior, fallback loading, real templates, and stress rendering (`:261-606`).
- inputs_outputs_state: Inputs are the repository’s `prompt-template/` tree and `hooks/lib/template-loader.sh` functions (`tests/test-templates-comprehensive.sh:15-18`, `:59`). Outputs are colorized PASS/FAIL/WARN lines plus exit code 0 when `TESTS_FAILED == 0`, otherwise 1 (`:19-57`, `:611-625`). State is local to counters and temporary directories; temp malformed-placeholder directories are removed after use (`:204-259`), and fallback tests create/remove an incomplete temp dir (`:481-490`).
- gates_or_invariants: Template placeholders must be uppercase double-brace `{{VAR_NAME}}` with no spaces or lowercase starts (`tests/test-templates-comprehensive.sh:122-175`). Generic template directory validation requires `block`, `codex`, and `claude` (`hooks/lib/template-loader.sh:216-221`). Rendering must preserve empty content and empty lines, support single/multiple lines, special regex characters, long values, repeated variables, adjacent variables, and variables at line boundaries (`tests/test-templates-comprehensive.sh:267-438`). Fallback must render fallback content for missing templates and prefer real templates when present (`:446-463`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, particularly `load_template`, `render_template`, `load_and_render_safe`, and `validate_template_dir` (`hooks/lib/template-loader.sh:33-48`, `:58-132`, `:170-203`, `:208-221`). It is part of the broader test suite; `tests/run-all-tests.sh` includes template-related tests and the repository also has focused `tests/test-template-loader.sh` and `tests/test-template-references.sh` discovered in the tree.
- edge_cases_or_failure_modes: The script uses `set -uo pipefail` but not `set -e`, so failures accumulate and the final exit is controlled by `TESTS_FAILED` (`tests/test-templates-comprehensive.sh:13`, `:619-625`). Some edge checks are intentionally “no crash” rather than exact semantic equality, such as backslashes, quotes, CJK-labeled handling, markdown formatting, and multiline values (`:329-360`, `:373-402`). The CJK section does not inject actual CJK in the shown value; it uses ASCII placeholder content (`:347-357`). Directory validation does not require `plan/`, though recursive loading/syntax checks include all Markdown files under the template root (`:90-107`, `:118-194`).
- validation_or_tests: This file is itself validation. It includes real integration checks for `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:497-540`), and a stress loop of 100 render calls plus all-template dummy render verification (`:547-606`). Not executed during this research pass.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-103 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-gate prompt block rendered when a loop is not allowed to exit because local commits are ahead of the remote. It is a template-level transition contract for “blocked until pushed.”
- algorithmic_behavior: The template renders a Markdown block titled `Unpushed Commits Detected`, substitutes `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, explains that `--push-every-round` requires pushing before exit, shows `git push origin {{CURRENT_BRANCH}}`, and tells the user to retry exit after pushing (`prompt-template/block/unpushed-commits.md:1-12`).
- inputs_outputs_state: Inputs are the computed ahead count and current branch from stop hooks. Output is rendered Markdown placed into a JSON hook response as the block reason. Runtime state transition is external: hook detects ahead commits, returns `decision: block`; after push, the user can attempt exit again and the gate may clear.
- gates_or_invariants: In RLCR, this block is only checked when `PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:598-627`). In PR loop stop handling, unpushed commits are always a stop condition because bots need pushed changes for review (`hooks/pr-loop-stop-hook.sh:313-360`). The invariant is that local commits must be pushed before loop exit is accepted.
- dependencies_and_callers: Loaded with `load_and_render_safe` in RLCR stop hook using fallback content if the template is missing (`hooks/loop-codex-stop-hook.sh:609-616`). Loaded similarly by PR loop stop hook after ahead-count detection with upstream/origin/PR-head fallback logic (`hooks/pr-loop-stop-hook.sh:318-357`). The generic template renderer performs single-pass substitution and leaves missing placeholders intact (`hooks/lib/template-loader.sh:50-132`).
- edge_cases_or_failure_modes: The rendered command assumes remote name `origin` and branch push syntax (`prompt-template/block/unpushed-commits.md:7-10`), while PR-loop detection may be comparing against PR head or missing remote refs (`hooks/pr-loop-stop-hook.sh:323-345`). Detached or unusual branch names can render awkwardly because the template is instructional rather than an executed command. If the template is absent or empty, hooks use fallback messages (`hooks/loop-codex-stop-hook.sh:609-616`; `hooks/pr-loop-stop-hook.sh:351-357`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this real template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then checks for title, count, and branch interpolation (`tests/test-templates-comprehensive.sh:517-528`). Caller logic is covered by stop-hook and PR-loop tests elsewhere, but this assigned file’s direct template rendering is specified there.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-133 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared runtime monitor library for RLCR and PR-loop monitoring. It centralizes terminal colors, file/session discovery, state-file detection, phase detection, and goal-tracker summary parsing.
- algorithmic_behavior: Defines ANSI color helpers (`scripts/lib/monitor-common.sh:8-23`), cross-platform file-size lookup (`:29-35`), timestamp-session discovery using `YYYY-MM-DD_HH-MM-SS` names (`:37-63`), terminal scroll-region setup/restore (`:65-92`), status color mapping (`:124-141`), state-file discovery (`:143-188`), YAML-frontmatter value extraction (`:190-209`), timestamp/string formatting (`:211-252`), PR-loop phase detection (`:254-342`), phase display text (`:344-381`), RLCR-style goal-tracker parsing (`:383-463`), and PR goal-tracker parsing (`:465-498`).
- inputs_outputs_state: Inputs are loop/session directories, `state.md` or `*-state.md` files, `round-*-pr-check.md` files, goal-tracker Markdown, PR goal-tracker Markdown, timestamps, and terminal state. Outputs are echoed scalar values or pipe-delimited summaries such as `path|status`, `phase`, or `total_acs|completed_acs|...|goal_summary`. Persistent side effect is the PR phase cache file `/tmp/humanize-phase-${session_name}-${highest_round}.size`, updated during `get_pr_loop_phase` (`scripts/lib/monitor-common.sh:297-307`).
- gates_or_invariants: `monitor_find_latest_session` only considers one-level child directories matching timestamp format and selects the lexicographically latest valid name (`scripts/lib/monitor-common.sh:49-60`). `monitor_find_state_file` prioritizes `state.md` as `active`; otherwise it returns the first `*-state.md` stop reason or `unknown` (`:162-187`). `get_pr_loop_phase` final states override active analysis (`approve`, `cancel`, `maxiter` files), then recent/growing `round-*-pr-check.md` yields `codex_analyzing`, then `state.md` frontmatter selects `waiting_initial_review` for round 0 startup case 1 or `waiting_reviewer` otherwise (`:272-341`). Goal parsing assumes Markdown section/table conventions and emits zero/default summaries when files are missing (`:390-463`, `:468-498`).
- dependencies_and_callers: `scripts/humanize.sh` sources this file at startup (`scripts/humanize.sh:6-10`). RLCR monitor wraps `monitor_find_latest_session`, `monitor_find_state_file`, `parse_goal_tracker`, and `monitor_get_file_size` (`scripts/humanize.sh:243-246`, `:341-405`, `:739-742`). PR monitor wraps session/state helpers, maps `approve` to `approved` and `maxiter` to `max-iterations`, displays `get_pr_loop_phase` output, and uses `humanize_parse_pr_goal_tracker` for issue stats (`scripts/humanize.sh:1156-1219`, `:1290-1336`, `:1428-1439`). Tests source it directly in `tests/test-pr-loop-system.sh` (`:257-318`, `:377-405`, `:770-843`, `:850-899`).
- edge_cases_or_failure_modes: Missing session dirs yield empty/`unknown` results (`scripts/lib/monitor-common.sh:44-47`, `:157-160`, `:339-341`). `find` is used instead of globs to avoid zsh no-match failures (`:49-60`). File size uses Linux `stat -c%s`, macOS `stat -f%z`, then `0` fallback (`:32-35`). `get_pr_loop_phase` treats files modified within 10 seconds or growing since cache as active analysis (`:292-318`); future mtimes would also look recent because negative age is `< 10`. Multiple `*-state.md` files are not sorted before first selection (`:172-181`). The signal-handling section is documentation only and explicitly says actual setup should be inline in monitor functions (`:98-122`). Frontmatter extraction and grep patterns assume simple keys and known Markdown structure (`:203-208`, `:407-459`, `:475-497`).
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies approved/waiting/cancelled/maxiter phases (`:256-309`, `:459-487`), goal-tracker parsing counts (`:315-370`), PR goal-tracker stats (`:377-410`), active/approve/unknown state-file detection (`:424-452`, `:794-820`), latest session selection and file-size fallback (`:770-843`), and `codex_analyzing` versus `waiting_reviewer` based on file growth/mtime cache (`:850-899`). `scripts/humanize.sh` has its own older `humanize_parse_goal_tracker` implementation before sourcing/wrapping, so monitor parsing behavior has both shared-library and public-helper surfaces (`scripts/humanize.sh:36-60`, `:367-370`). Tests were inspected, not executed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 unique Item Evidence headings; each assigned ID appears exactly once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`