# agent_23 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Pre-review/finalization gate helper for the RLCR stop hook. It decides whether Claude may leave a phase by detecting unfinished work in either legacy transcript todos or the newer file-backed Task system.
- algorithmic_behavior: The script reads hook JSON from stdin and exits with a three-state result: `0` means no unfinished tasks, `1` means unfinished tasks were found, and `2` means the hook input JSON could not be parsed (`hooks/check-todos-from-transcript.py:9-13`, `150-161`, `177-196`). It extracts tool calls from transcript JSONL in three formats: assistant message content, generic `message` content, and direct `tool_use` entries (`23-55`). For legacy `TodoWrite`, it tracks only the most recent todo list seen in the transcript, then treats any todo whose status is not exactly `completed` as incomplete (`58-101`). For the new Task system, it scans `~/.claude/tasks/<session_id>/*.json` or a test override directory, treats missing status as `pending`, and considers only `completed` and `deleted` terminal (`104-147`).
- inputs_outputs_state: Inputs are stdin JSON fields `session_id`, `tasks_base_dir`, and `transcript_path` (`150-175`). File inputs are a transcript JSONL file and per-task JSON files in the session task directory (`70-88`, `117-128`). Outputs are process exit codes plus stdout beginning with `INCOMPLETE_TODOS` when blocking details exist (`181-196`). For Task entries it emits a line including task id, status, and subject/description fallback (`187-190`); for TodoWrite entries it emits status and content (`190-191`). It does not mutate repository state.
- gates_or_invariants: Empty stdin allows progress (`153-156`). Missing transcript path or missing task directory is treated as no incomplete work (`64-65`, `121-122`). Malformed transcript JSONL lines are ignored, but malformed stdin JSON is a hard parse error (`76-80`, `158-161`). Malformed or unreadable task files are skipped (`143-145`). Any status other than `completed` for legacy todos blocks; for Task files, any status outside `completed` or `deleted` blocks (`91-99`, `130-142`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` before expensive Codex review (`hooks/loop-codex-stop-hook.sh:342-354`). Exit `2` is converted into a JSON hook block with system message `Loop: Blocked - task checker parse error` (`356-372`). Exit `1` is converted into a JSON hook block using `prompt-template/block/incomplete-todos.md` or fallback text (`375-396`). This makes the script a gate before git cleanliness and code-review transitions.
- edge_cases_or_failure_modes: The “latest TodoWrite wins” rule can clear older pending todos if the newest TodoWrite list is all complete; this is intentional and covered by tests (`tests/test-todo-checker.sh:197-210`). Ghost tasks are avoided by reading the file-backed Task directory as authoritative when a session id is present (`hooks/check-todos-from-transcript.py:104-120`). Missing status is blocking for both legacy todos and Task files (`tests/test-todo-checker.sh:250-263`, `309-324`). Empty content still blocks if status is pending (`266-279`). A mixed legacy/file-backed run blocks if either source has an incomplete item (`417-435`).
- validation_or_tests: `tests/test-todo-checker.sh` exercises completed and incomplete legacy todos (`107-143`), in-progress handling (`145-155`), invalid JSONL tolerance (`190-194`), alternate transcript shapes (`213-240`), missing status and empty content (`250-279`), Task directory statuses including `pending`, `completed`, `in_progress`, and `deleted` (`309-415`), mixed TodoWrite plus Task inputs (`417-435`), and non-existent task session directories (`445-455`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-053 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the Codex review issue-detection gate used by the RLCR loop. It locks down how `detect_review_issues()` interprets Codex review logs and decides whether the loop continues review remediation or enters finalize.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, constructs isolated `LOOP_DIR` and `CACHE_DIR`, writes synthetic `round-N-codex-review.log` files, then calls `detect_review_issues <round>` (`tests/test-codex-review-merge.sh:37-53`, `72-80`). The algorithm under test scans the entire log file from the first line, detects the first `[P0-9]` marker only if that marker appears in the first 10 characters of a line, extracts from that line through EOF, writes `round-N-review-result.md`, and returns `0` when issues exist, `1` when no issue marker exists, and `2` for missing or empty log files (`1-16`).
- inputs_outputs_state: Inputs are cache files named `$CACHE_DIR/round-<round>-codex-review.log` and required globals `LOOP_DIR` and `CACHE_DIR` (`tests/test-codex-review-merge.sh:48-53`). The implementation reads `$CACHE_DIR/round-${round}-codex-review.log` and writes `$LOOP_DIR/round-${round}-review-result.md` when issues are found (`hooks/lib/loop-common.sh:468-505`). Function stdout is the merged review issue content headed by `## Codex Review Issues` (`hooks/lib/loop-common.sh:504-505`); stderr records audit messages about analysis and extraction (`479-502`).
- gates_or_invariants: Marker position is strict: `[P?]` outside the first 10 characters must not trigger a review failure (`tests/test-codex-review-merge.sh:83-107`). Full-file scanning is mandatory, so markers late in files over 50 lines and early in long files are both detected (`171-227`). The first matching line is the start of extraction, preserving all following issue text and trailing output (`229-255`). Missing and empty log files are hard errors returning `2`, not “no issues” (`133-169`).
- dependencies_and_callers: The test constrains `detect_review_issues()` in `hooks/lib/loop-common.sh:468-510`. The RLCR stop hook calls this after `run_codex_code_review` in `run_and_handle_code_review()` (`hooks/loop-codex-stop-hook.sh:1034-1050`). Exit `2` triggers `block_review_failure`, exit `0` calls `continue_review_loop_with_issues`, and exit `1` enters finalize (`1052-1062`).
- edge_cases_or_failure_modes: The test protects against a regression where only the last N log lines were scanned; `[P1]` at line 5 in a 70-line file must still be detected (`tests/test-codex-review-merge.sh:200-227`). It also checks line-start and common `- [P1]` formats (`257-303`), exactly 50-line files (`332-354`), and result-file creation (`305-330`). A false positive is prevented when text merely mentions `[P1]` later in the line (`83-107`).
- validation_or_tests: The assigned file is itself the validation asset. It has 12 tests covering positive detection, position filtering, no markers, missing/empty logs, long logs, first-match extraction, common Codex markdown formats, result-file persistence, and boundary line counts (`tests/test-codex-review-merge.sh:56-354`). It exits non-zero if any assertion fails (`356-368`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-083 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Small prompt block used by the RLCR stop hook’s git-clean gate to explain the special `.humanize/` untracked-directory case and guide the user toward committing only a `.gitignore` update.
- algorithmic_behavior: The template contains a targeted message for an untracked `.humanize/` directory created by `humanize:start-rlcr-loop`, stating that it should not be committed and giving the exact remediation commands to append `.humanize*` to `.gitignore` and stage `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). It does not perform logic itself; it supplies branch/phase transition text when the hook blocks due to a dirty git tree.
- inputs_outputs_state: Input is implicit: it is loaded as static markdown by the hook when `git status --porcelain` has untracked entries matching `.humanize` (`hooks/loop-codex-stop-hook.sh:545-559`). Output is prompt text merged into `SPECIAL_NOTES` for a block response. No state is mutated by the template itself; the suggested user action would mutate `.gitignore` and the git index only if followed.
- gates_or_invariants: It reinforces the invariant that `.humanize/` is local loop state and must not be committed (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). It pairs with the broader git-add protection template, which says `.humanize/` contains local loop state and broad add commands are blocked when `.humanize` exists (`prompt-template/block/git-add-humanize.md:1-24`).
- dependencies_and_callers: Directly loaded by `hooks/loop-codex-stop-hook.sh` during “Git Clean and Pushed?” checks (`hooks/loop-codex-stop-hook.sh:534-559`). Related low-level guard behavior lives in `hooks/lib/loop-common.sh`, which detects `.humanize` git-add attempts and provides standard blocked-command messaging (`hooks/lib/loop-common.sh:1027-1182`). `rg` found the template path referenced only by the stop hook, making it a narrow UX component for that gate.
- edge_cases_or_failure_modes: The hook uses a broad `grep -q '\.humanize'` over untracked lines, so this note can be attached for `.humanize/` and legacy `.humanize-*` directories (`hooks/loop-codex-stop-hook.sh:549-556`). The template suggests `echo '.humanize*' >> .gitignore`; repeated use could duplicate the ignore line, and the template does not mention checking for an existing entry. It is also not a standalone block for non-humanize untracked files; those use `block/git-not-clean-untracked.md` when present (`561-568`).
- validation_or_tests: The assigned template is covered indirectly through git-clean and `.humanize` protection tests. `tests/test-humanize-escape.sh` validates blocking direct and variant `git add .humanize` commands, broad adds with `.humanize` present, quoted paths, forced adds, and non-overblocking `.humanizeconfig`-style names (`tests/test-humanize-escape.sh:78-192`). The stop hook’s special-note path is visible in `hooks/loop-codex-stop-hook.sh:552-559`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-113 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase fallback contract for RLCR when automated code review is skipped. It defines the manual verification and no-behavior-change constraints before the agent can complete the loop without a full Codex review signal.
- algorithmic_behavior: The template renders `{{REVIEW_SKIP_REASON}}`, warns that implementation could not be fully validated, then instructs manual verification: review changes, run available tests, and check common quality issues (`prompt-template/claude/finalize-phase-skipped-prompt.md:1-14`). It optionally allows a `code-simplifier:code-simplifier` Task-agent pass focused on `{{BASE_BRANCH}}` to `{{START_BRANCH}}` (`15-24`). It then states non-negotiable constraints: preserve functionality, keep existing tests passing, avoid new bugs, and only perform functionality-equivalent cleanup (`26-34`). Before exit, it requires task completion, commit, and a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` with work, modified files, test confirmation, and manual verification notes (`40-50`).
- inputs_outputs_state: Inputs are template variables `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE` (`3`, `19`, `37-44`). Output is rendered markdown used as the hook block reason. The caller renames active `state.md` to `finalize-state.md` before producing the prompt, so the prompt participates in a state transition from implementation/review to finalize (`hooks/loop-codex-stop-hook.sh:1065-1075`).
- gates_or_invariants: The key gate is that skipped review does not equal validated completion: the prompt explicitly requires manual verification before finalizing (`prompt-template/claude/finalize-phase-skipped-prompt.md:5-14`). It preserves the finalize-phase invariant that simplification must be behavior-preserving and tests must not regress (`28-34`). It also requires all tasks to be completed via TaskUpdate before exiting (`42-43`), which aligns with the todo checker gate in `hooks/check-todos-from-transcript.py`.
- dependencies_and_callers: Loaded by `enter_finalize_phase()` in `hooks/loop-codex-stop-hook.sh` when `skip_reason` is non-empty (`hooks/loop-codex-stop-hook.sh:1067-1110`). The normal non-skipped peer is `prompt-template/claude/finalize-phase-prompt.md`, which states Codex review passed and all acceptance criteria were met (`prompt-template/claude/finalize-phase-prompt.md:1-5`). The skipped variant intentionally softens that claim and adds manual verification duties.
- edge_cases_or_failure_modes: The template assumes a skipped-review path is allowed by the caller. In the reviewed caller slice, `run_and_handle_code_review()` treats review command failure as blocking rather than skipping (`hooks/loop-codex-stop-hook.sh:1040-1044`), so this prompt likely handles other configured skip paths, such as missing base-branch review configuration, rather than command failure. If rendered variables are missing, the fallback in the caller has similar language but fewer reference-file details (`1077-1104`).
- validation_or_tests: `tests/test-finalize-phase.sh` references TodoWrite/finalize behavior and review-result file creation around finalize transitions (`tests/test-finalize-phase.sh:718-772`, `801-804` from search results). The template itself is also exercised by the template comprehensive suite through `PLAN_FILE` and `GOAL_TRACKER_FILE` rendering patterns (`tests/test-templates-comprehensive.sh:533-536` from search results). The caller’s rendering path is concrete at `hooks/loop-codex-stop-hook.sh:1104-1110`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-143 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for PR-loop API handling. It tests that PR loop state detection, comment fetching, polling, and stop-hook behavior remain stable under empty results, API failures, rate limits, network errors, malformed state, slow APIs, long/unicode comments, and bot-specific response formats.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared test helpers, then creates isolated git repos and mocked `gh` binaries to drive real scripts end-to-end (`tests/robustness/test-pr-loop-api-robustness.sh:16-21`, `32-39`). `create_mock_gh` implements repo discovery, PR discovery, and behavior-specific API responses such as empty arrays, rate limit errors, network errors, auth failures, Claude approval comments, Codex severity comments, mixed bots, unicode, and long bodies (`34-239`). It creates PR-loop `state.md` fixtures with frontmatter fields such as `current_round`, `max_iterations`, `pr_number`, configured/active bots, base branch, and startup/review flags (`241-262`).
- inputs_outputs_state: Inputs are synthetic `.humanize/pr-loop/<timestamp>/state.md` files, mocked `gh` commands placed on `PATH`, temporary git repos, and calls to real scripts: `scripts/fetch-pr-comments.sh`, `hooks/pr-loop-stop-hook.sh`, and `scripts/poll-pr-reviews.sh` (`285-849`). Outputs checked include active-loop directory strings, markdown comment files, hook exit codes, writable `approve-state.md` paths, and JSON from polling with `comments`, `has_new_comments`, and related fields (`290-345`, `361-425`, `446-550`, `565-609`, `621-843`).
- gates_or_invariants: `find_active_pr_loop` must return the newest PR-loop directory only when `state.md` exists (`hooks/lib/loop-common.sh:914-933`), and tests confirm state is detected even when `pr_number` is missing (`tests/robustness/test-pr-loop-api-robustness.sh:285-345`). `fetch-pr-comments.sh` must create structured markdown for empty arrays and preserve PR/repo metadata (`355-377`), while handling rate limits and network failures without silent false success (`379-425`). Bot comments must survive formatting, including Claude LGTM text, Codex `[P1]` severity markers, and multiple bots (`435-501`). `poll-pr-reviews.sh` must reject missing args, show help, output valid JSON with boolean `has_new_comments`, and on API failure return exit `0` with parseable JSON and boolean false (`619-843`).
- dependencies_and_callers: Tests `find_active_pr_loop()` from `hooks/lib/loop-common.sh:917-933`; `fetch-pr-comments.sh`, whose contract includes PR number/output args, optional `--after`/`--bots`, repo/fork resolution, retrying API calls, normalizing issue comments/review comments/PR reviews, deduplication, sorting, and markdown output (`scripts/fetch-pr-comments.sh:27-120`, `130-220`, `260-343`, `360-450`); `poll-pr-reviews.sh`, whose contract includes required `--after` and `--bots`, bot author mapping (`codex` to `chatgpt-codex-connector[bot]`), retry-to-empty-array behavior, filtering, and JSON output (`scripts/poll-pr-reviews.sh:23-116`, `126-187`, `207-235`, `299-326`); and `hooks/pr-loop-stop-hook.sh`, which uses `find_active_pr_loop`, parses YAML-list state, exits cleanly if no loop exists, and moves or writes `approve-state.md` when all bots are complete or timed out (`hooks/pr-loop-stop-hook.sh:65-77`, `88-115`, `444-447`, `1098-1126`).
- edge_cases_or_failure_modes: The mock `gh` emits non-zero exits for rate limiting and network failures to verify retries and graceful degradation (`tests/robustness/test-pr-loop-api-robustness.sh:111-129`). Unicode and 10k-character comments verify JSON/body handling without truncation or encoding failure (`196-224`, `511-550`). Corrupted `state.md` must not crash the stop hook with an abnormal exit (`578-593`). Slow API calls must complete without hanging and should produce `has_new_comments=false` for empty results (`677-742`). API failures in `poll-pr-reviews.sh` are stricter than earlier tests: exit `0`, JSON starts at the first `{`, JSON parses with `jq -e`, and `has_new_comments` is boolean false, not the string `"false"` (`811-840`).
- validation_or_tests: The assigned file is the validation suite. It contains 19 named tests: PR loop state handling (`285-345`), fetch-pr-comments empty/error/bot/JSON behavior (`355-550`), stop-hook no-loop/corrupted-state/approve-path behavior (`560-609`), and poll-pr-reviews help/argument/JSON/slow/API-failure behavior (`619-843`). It ends with `print_test_summary "PR Loop API Robustness Test Summary"` and exits with the test summary code (`845-850`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 evidence sections present; each assigned row has one corresponding section header.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`