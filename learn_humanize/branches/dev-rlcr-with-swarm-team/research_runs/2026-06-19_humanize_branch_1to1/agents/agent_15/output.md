# agent_15 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor support library. The directory contains one recursive descendant, `scripts/lib/monitor-common.sh`, sourced by `scripts/humanize.sh` at lines 6-10 and by PR-loop system tests. It centralizes cross-shell monitor helpers for RLCR and PR loop status rendering.
- algorithmic_behavior: Provides terminal color escape functions, cross-platform file sizing, timestamped session discovery, terminal scroll-region setup/restore, state-file detection, YAML frontmatter extraction, string truncation, PR-loop phase inference, and goal-tracker parsing. Session discovery uses `find` instead of shell globs to avoid zsh `nomatch` failures, visible in `monitor_find_latest_session` lines 40-63. State detection prefers active `state.md`, then any terminal `*-state.md`, returning `path|status` at lines 154-188. PR phase detection prioritizes terminal states, then detects Codex activity by latest `round-*-pr-check.md` file growth or recent mtime at lines 267-342.
- inputs_outputs_state: Inputs are loop/session directories, markdown state files, goal trackers, status strings, and optional max lengths. Outputs are shell strings such as latest session path, `state_file|status`, phase names, display strings, parsed tracker counts, ANSI codes, and formatted/truncated strings. It mutates almost no repository state; the notable side effect is a `/tmp/humanize-phase-<session>-<round>.size` cache written by `get_pr_loop_phase` lines 297-307 for growth comparison.
- gates_or_invariants: Timestamp session directories must match `YYYY-MM-DD_HH-MM-SS` before selection at lines 54-58. `monitor_find_state_file` returns `|unknown` for missing or invalid session dirs at lines 157-160 and 186-187. Status colors map known statuses and default unknown statuses to yellow/dim at lines 130-140. PR phase terminal files have precedence over active waiting states at lines 272-275. Goal tracker counting confines searches to relevant markdown sections to avoid cross-section contamination, for example lines 407-445.
- dependencies_and_callers: `scripts/humanize.sh` wraps `monitor_find_latest_session` for RLCR monitor use at lines 243-246 and PR monitor use at lines 1156-1159. It wraps `monitor_find_state_file` for RLCR status-bar rendering at lines 341-344 and adapts PR status names at lines 1205-1222. PR monitor display calls `get_pr_loop_phase` and `get_pr_loop_phase_display` at lines 1290-1293 and 1428-1430, and uses `humanize_parse_pr_goal_tracker` at lines 1331-1334. `tests/test-pr-loop-system.sh` directly sources this library for shared monitor tests.
- edge_cases_or_failure_modes: Missing loop directories return empty paths or `unknown` instead of throwing. Empty/dotfile-only directories are handled through `find`, not `*` globs. File-size helpers support Linux `stat -c%s` and macOS `stat -f%z` with `0` fallback at lines 32-35. PR phase activity can be misclassified briefly as analyzing when a latest check file is newly seen or modified within 10 seconds, by design at lines 315-318. `/tmp` cache write failure is ignored with `|| true`, preserving monitor operation.
- validation_or_tests: `tests/test-zsh-monitor-safety.sh` directly exercises the no-glob-error behavior under zsh. `tests/test-pr-loop-system.sh` covers shared monitor session/state/file-size helpers and PR phase/goal parsing. `tests/run-all-tests.sh` includes both `test-zsh-monitor-safety.sh` and PR-loop system tests in the aggregate suite.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-045 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Aggregate executable specification for plugin behavior. It defines the canonical ordered test sweep for template loading, validators, loop state transitions, monitoring, PR-loop orchestration, agent teams, and robustness suites.
- algorithmic_behavior: Initializes counters and failed-suite tracking at lines 28-30, declares an ordered `TEST_SUITES` array at lines 33-76, and separately declares suites that must run under zsh at lines 78-81. For each suite it resolves the path, skips missing files, detects zsh requirements, runs the script while capturing stdout/stderr, strips ANSI escapes, extracts the last `Passed:` and `Failed:` counts, aggregates totals, records failed suites, prints failure tails, and exits nonzero if any suite failed.
- inputs_outputs_state: Input is the repository test tree rooted by `SCRIPT_DIR` computed at line 14 and the availability of `zsh` for interpreter-specific tests. Outputs are console progress, aggregate pass/fail counts, a failed-suite list, and process exit `0` or `1`. State is in-memory only: `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES`.
- gates_or_invariants: Missing test files are skipped rather than failed at lines 86-89. `test-zsh-monitor-safety.sh` must use the zsh interpreter, not bash, enforced at lines 91-117. Suite failure is defined as either nonzero exit status or parsed failed count greater than zero at lines 133-135. ANSI stripping must happen before parsing counts at lines 124-128.
- dependencies_and_callers: Depends on every named suite being executable or interpreter-runnable. It directly includes the assigned `tests/test-zsh-monitor-safety.sh` at line 49 and `tests/robustness/test-cancel-security-robustness.sh` at line 67. `tests/test-ansi-parsing.sh` documents that it tests the portable ANSI stripping used here.
- edge_cases_or_failure_modes: If a suite emits no parseable counts, `passed` and `failed` default to `0`, so a zero-exit suite can appear as passed with zero tests. If `zsh` is absent, zsh-specific suites are skipped and do not affect totals. Because the script uses `set -uo pipefail`, it temporarily disables exit-on-error around suite execution at lines 108-122 to continue aggregation after failures.
- validation_or_tests: This file is itself the validation harness. Its behavior is indirectly validated by `tests/test-ansi-parsing.sh` and by successful execution of the declared suite list. I did not run it because this worker is restricted to research notes and the branch export is read-only.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-075 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: zsh-specific executable specification for monitor runtime safety. It protects the monitor algorithms from zsh `nomatch` glob failures when loop/cache directories are empty or contain only dotfiles.
- algorithmic_behavior: Runs under `#!/usr/bin/env zsh`, creates an isolated `/tmp/test-zsh-humanize-$$` workspace, exports a test-local `XDG_CACHE_HOME`, sources `scripts/humanize.sh`, and then executes eight scenarios. The scenarios cover sourcing without `no matches found`, latest-session discovery on empty and dotfile-only dirs, state-file discovery with no `*-state.md`, empty Codex cache log iteration, valid timestamp session iteration, proof that zsh is active, and a comparison between the old glob pattern and the new `find` pattern.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr` directories, timestamped session names, dotfiles, cache dirs, and sourced production shell functions. Outputs are PASS/FAIL lines and final exit `0` or `1`. State is temporary filesystem state under `/tmp` and `$XDG_CACHE_HOME`; cleanup removes the workspace via trap at lines 56-60.
- gates_or_invariants: The test must run under zsh, checked through `$ZSH_VERSION` at lines 282-286. The shared pattern under test is `find ... -mindepth 1 -maxdepth 1` instead of an unguarded glob, used for session iteration at lines 105-115, 141-150, and 255-265. No test result may contain `no matches found`; that string is treated as a zsh safety failure at lines 72-76, 118-124, 152-157, and 199-205.
- dependencies_and_callers: Depends on `scripts/humanize.sh`, which sources `scripts/lib/monitor-common.sh` at lines 6-10. `tests/run-all-tests.sh` marks this suite as zsh-only at lines 78-81 and invokes it through `zsh "$suite_path"` at lines 109-113.
- edge_cases_or_failure_modes: Empty loop dirs, dotfile-only loop dirs, no terminal state files, empty cache dirs, and strict zsh glob behavior are explicit cases. The final demonstration disables zsh `nomatch` inside a subshell at line 301 only to show the legacy pattern would otherwise be dangerous, then confirms the `find` pattern works at lines 314-320.
- validation_or_tests: The file is the direct validation asset for zsh monitor safety. It mirrors key helper logic, especially `monitor_find_latest_session` and `monitor_find_state_file`, and verifies the safety property in a real zsh process.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-105 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Hook block-message template for wrong loop-file directory access. It is part of the validator contract that redirects agents from stale or misplaced `.humanize/rlcr` paths to the active loop directory.
- algorithmic_behavior: Defines a short markdown block with three placeholders: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` at lines 3-6. Rendering is handled by `hooks/lib/template-loader.sh`, whose `render_template` performs single-pass `{{VAR}}` substitution and leaves missing placeholders intact at lines 50-132.
- inputs_outputs_state: Inputs are validator-provided values for attempted action, attempted path, and computed correct path. Output is a rendered block message to stderr. It has no persistent state and no branching itself.
- gates_or_invariants: The template is used only after validators determine that the file is in `.humanize/rlcr` but not under the active session path. The read validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and blocks with this template at `hooks/loop-read-validator.sh` lines 157-167. The write validator does the same at `hooks/loop-write-validator.sh` lines 254-264. Template rendering is safe single-pass, preventing placeholder injection from substituted values.
- dependencies_and_callers: Depends on `load_and_render_safe` in `hooks/lib/template-loader.sh` lines 170-203. The read and write validators provide inline fallback messages if the template is missing. Search did not show an edit-validator call to this specific template; edit validation uses the wrong-round template for summary round mismatches in the inspected section.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` emits the fallback message with the same variable substitution behavior. Missing variables remain visible as placeholders, which is useful for diagnosing bad caller data but can produce less polished output. The final line suggests `cat {{FILE_PATH}}`, which may be confusing when the gate is for a write action, but callers set `ACTION` to distinguish context.
- validation_or_tests: Template loader behavior is covered by `tests/test-template-loader.sh`, especially real-template integration around lines 147-164, and broader template validation is covered by `tests/test-templates-comprehensive.sh` around lines 493-512. Direct references confirm this template is wired into read/write validator block paths.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-135 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable security specification for cancel authorization. It validates that only the intended cancel transition, moving active state to `cancel-state.md` after a signal file exists, can bypass state-file write protections.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers at lines 17-18, creates a mock active loop with `state.md`, then runs 26 positive and negative authorization cases against `is_cancel_authorized`. Positive cases allow valid signal-file-backed moves from `state.md` or `finalize-state.md` to `cancel-state.md`, with double quotes, single quotes, or no quotes. Negative cases cover missing signal, command substitution, backticks, command chaining, pipes, wrong source/destination, extra args, newline injection, variable expansion, non-`mv` commands, mixed quotes, IFS manipulation, trailing spaces, nonstandard source names, and symlink sources.
- inputs_outputs_state: Inputs are synthetic loop directories, `.cancel-requested`, command strings lowercased by the test, and filesystem symlinks. Outputs are PASS/FAIL counts via `tests/test-helpers.sh` and final exit from `print_test_summary` at lines 411-412. Test state is temporary via `setup_test_dir`, which creates `mktemp -d` and registers cleanup in `tests/test-helpers.sh` lines 84-89.
- gates_or_invariants: The implementation gate under test requires `.cancel-requested` at `hooks/lib/loop-common.sh` lines 674-679; rejects `$(`, backticks, newlines, shell operators, remaining `$`, mixed quote delimiters, trailing multi-space, non-`mv` structure, extra args, invalid source/destination, and symlink source files at lines 681-828. Expected sources are exactly `state.md` or `finalize-state.md`; expected destination is exactly `cancel-state.md`. The symlink check selects the source by exact path comparison, avoiding substring confusion when the loop path contains `finalize` at lines 815-823.
- dependencies_and_callers: Production Bash validation calls `is_cancel_authorized` before blocking state-file modification in `hooks/loop-bash-validator.sh` lines 139-155 and again for source-side `mv/cp` detection at lines 286-323. `scripts/cancel-rlcr-loop.sh` creates `.cancel-requested` and performs `mv "$ACTIVE_STATE_FILE" "$LOOP_DIR/cancel-state.md"` at lines 148-155, which is the allowed command shape. `tests/run-all-tests.sh` includes this robustness suite at line 67.
- edge_cases_or_failure_modes: The suite documents strict choices: mixed quote styles are rejected even if paths are otherwise valid, multiple trailing spaces are rejected, `/./` path normalization is accepted, literal `$LOOP_DIR`/`${LOOP_DIR}` are normalized by the implementation but other variables are rejected, symlink `state.md` and `finalize-state.md` are rejected, and directories whose path includes `finalize` must not bypass state-vs-finalize symlink checks.
- validation_or_tests: This file is the targeted robustness validation for cancel security. Related broader coverage exists in `tests/test-cancel-signal-file.sh`, while the production authorization implementation lives in `hooks/lib/loop-common.sh` lines 648-829.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`