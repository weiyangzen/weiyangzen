# agent_07 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification suite for the Humanize PR/RLCR loop algorithms. The directory contains 58 files and about 27,692 shell/fixture lines covering setup, reviewer polling, prompt/template loading, hook validators, monitor phase display, state transitions, cancellation, session ownership, PR-loop stop-hook behavior, and robustness/security regressions.
- algorithmic_behavior: The suite is coordinated by `tests/run-all-tests.sh`, whose ordered `TEST_SUITES` array runs template, validator, todo, finalize, PR-loop, session, agent-team, and robustness suites (`tests/run-all-tests.sh:32`). It treats `test-zsh-monitor-safety.sh` specially under zsh (`tests/run-all-tests.sh:78`) and aggregates suite pass/fail counts from stripped output (`tests/run-all-tests.sh:124`). `tests/test-pr-loop-system.sh` is the main PR-loop state-machine spec: it creates isolated temp git repos, injects `tests/mocks/gh`, and runs named tests via `run_test` with per-test setup/cleanup (`tests/test-pr-loop-system.sh:50`, `tests/test-pr-loop-system.sh:80`). The directory’s child roles are: `fixtures/` JSON API payloads for issue comments, review comments, PR reviews, reactions; `mocks/gh` a fixture/environment-backed GitHub CLI; `setup-fixture-mock-gh.sh` a narrower fixture-backed mock for fetch/poll scripts; `robustness/` adversarial tests for API failure, hook input, path validation, concurrent state, state transition, timeouts, templates, setup scripts, and cancel security.
- inputs_outputs_state: Inputs include mock GitHub JSON fixtures, environment variables like `MOCK_GH_FIXTURES_DIR`, `MOCK_GH_PR_STATE`, `MOCK_GH_LATEST_COMMIT_AT`, temp `.humanize/rlcr` and `.humanize/pr-loop` state directories, and hook JSON on stdin. Outputs are test pass/fail counts, generated temp loop state files, prompt/comment/resolve files, goal trackers, and hook validator exit codes. The mocked `gh` routes PR metadata, comments, reviews, reactions, repo info, and auth status from fixture files and env vars (`tests/mocks/gh:8`, `tests/mocks/gh:127`).
- gates_or_invariants: The tests encode mutual exclusion between active PR and RLCR loops (`tests/test-pr-loop-system.sh:110`, `tests/test-pr-loop-system.sh:129`), immutable PR loop `state.md` and generated comment/prompt files with resolve files allowed (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`), controlled cancel transitions only through signal-authorized `state.md -> cancel-state.md` renames (`tests/test-cancel-signal-file.sh:5`, `tests/test-cancel-signal-file.sh:80`), session-id scoping for loop ownership (`tests/test-session-id.sh:5`), and stop-hook trigger validation after force-push/new commits (`tests/test-pr-loop-stophook.sh:5`, `tests/test-pr-loop-stophook.sh:20`).
- dependencies_and_callers: Test suites exercise `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, and prompt templates under `prompt-template/`. `setup-fixture-mock-gh.sh` explicitly exists to test `fetch-pr-comments.sh` and `poll-pr-reviews.sh` (`tests/setup-fixture-mock-gh.sh:3`).
- edge_cases_or_failure_modes: Covered failures include missing/invalid fetch args (`tests/test-pr-loop-scripts.sh:279`), empty/rate-limited/network GitHub APIs (`tests/robustness/test-pr-loop-api-robustness.sh:355`, `tests/robustness/test-pr-loop-api-robustness.sh:379`, `tests/robustness/test-pr-loop-api-robustness.sh:408`), old trigger comments after force push (`tests/test-pr-loop-stophook.sh:20`), missing trigger comment ID cleanup for startup cases 4/5 (`tests/test-pr-loop-system.sh:1384`), zsh glob failures in empty/dotfile-only directories (`tests/test-zsh-monitor-safety.sh:5`), and chained/prefixed/redirection command bypasses around protected state files (`tests/test-cancel-signal-file.sh:246` onward).
- validation_or_tests: The directory is itself the validation surface. Main validation entry is `tests/run-all-tests.sh`; focused PR-loop validation is `tests/test-pr-loop-system.sh`, whose runner covers reviewer status, reactions, PR reviews, phases, goal trackers, startup cases, monitor output, setup failure, and stop-hook goal-tracker integration (`tests/test-pr-loop-system.sh:1761`). Fetch-comment behavior is also covered by fixture tests in `tests/test-pr-loop-hooks.sh:1242` and robustness tests in `tests/robustness/test-pr-loop-api-robustness.sh:347`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-037 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop ingestion script that fetches all review-relevant GitHub discussion channels and writes a normalized Markdown comment bundle for downstream prompt generation and bot review analysis.
- algorithmic_behavior: Parses `<pr_number> <output_file>` plus `--after` and `--bots` (`scripts/fetch-pr-comments.sh:27`). Resolves the correct PR base repository by first checking the current repo and then a parent repo for fork PRs (`scripts/fetch-pr-comments.sh:126`, `scripts/fetch-pr-comments.sh:139`). Fetches three GitHub API streams with retries: issue comments, inline PR review comments, and PR reviews (`scripts/fetch-pr-comments.sh:210`, `scripts/fetch-pr-comments.sh:216`, `scripts/fetch-pr-comments.sh:220`). Converts each stream into a shared JSON shape with type/id/author/timestamps/body/path/line/state (`scripts/fetch-pr-comments.sh:260`, `scripts/fetch-pr-comments.sh:280`, `scripts/fetch-pr-comments.sh:300`), deduplicates by id (`scripts/fetch-pr-comments.sh:322`), optionally filters by timestamp (`scripts/fetch-pr-comments.sh:326`), sorts humans before bots and newest first (`scripts/fetch-pr-comments.sh:334`), then writes Markdown sections for human and bot comments (`scripts/fetch-pr-comments.sh:357`, `scripts/fetch-pr-comments.sh:381`).
- inputs_outputs_state: Inputs are CLI args, GitHub CLI auth/repo context, `gh api` JSON, `jq`, current date, and optional active bot names. Output is a Markdown file containing PR number, fetch timestamp, repository, human comments, bot comments grouped by bot when `--bots` is supplied, and warning footer if API endpoints failed. It uses a `mktemp -d` workspace and deletes it via `trap` (`scripts/fetch-pr-comments.sh:168`).
- gates_or_invariants: Requires numeric PR number, output file, `gh`, and `jq` before doing work (`scripts/fetch-pr-comments.sh:92`, `scripts/fetch-pr-comments.sh:112`). PR base repo must resolve to owner/name or the script exits (`scripts/fetch-pr-comments.sh:151`, `scripts/fetch-pr-comments.sh:156`). Retry loop makes API endpoint failures non-fatal for formatting by substituting `[]` and incrementing `API_FAILURES` (`scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:199`). Approval-only PR reviews are preserved by replacing empty/null bodies with a review-state placeholder (`scripts/fetch-pr-comments.sh:300`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` to create `.humanize/pr-loop/<timestamp>/round-0-pr-comment.md` before startup-case selection (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:430`). It feeds the round-0 prompt path where setup chooses no-comment or has-comment task/critical templates (`scripts/setup-pr-loop.sh:800`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:914`, `scripts/setup-pr-loop.sh:940`). It depends on `gh`, `jq`, `sed`, `date`, `mktemp`, and shell utilities.
- edge_cases_or_failure_modes: Handles fork PR lookup by parent repo; handles empty JSON streams; filters null `created_at` before date sorting (`scripts/fetch-pr-comments.sh:337`); uses lexicographic ISO timestamp comparison for `--after`, which is correct for normalized UTC ISO strings but sensitive to non-normalized timestamp formats; maps `codex` to `chatgpt-codex-connector[bot]` while defaulting other bot names to `<bot>[bot]` (`scripts/fetch-pr-comments.sh:386`). Potential dedupe collision exists because issue comments, review comments, and reviews are unified only by numeric `id`; if GitHub resource id spaces overlapped, one record could be dropped.
- validation_or_tests: Basic argument validation is tested in `tests/test-pr-loop-scripts.sh:256`. Fixture coverage confirms issue comments, inline comments, approval-only reviews, and `--after` filtering (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1287`). Robustness tests cover empty arrays, rate limits, network errors, Claude/Codex/multiple bot formatting, Unicode, and long comment bodies (`tests/robustness/test-pr-loop-api-robustness.sh:347`, `tests/robustness/test-pr-loop-api-robustness.sh:427`, `tests/robustness/test-pr-loop-api-robustness.sh:503`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-067 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: Focused executable specification for the PR-loop state machine and its interaction with reviewer status, bot reactions, monitoring, goal tracking, startup cases, setup cleanup, and stop-hook integration.
- algorithmic_behavior: Sets up isolated temp git repositories and fixture-backed `gh` mocks per test (`tests/test-pr-loop-system.sh:50`), runs functions in subshells through `run_test` (`tests/test-pr-loop-system.sh:80`), and dispatches test groups by optional filter in `main` (`tests/test-pr-loop-system.sh:1761`). It verifies PR/RLCR mutual exclusion (`tests/test-pr-loop-system.sh:110`, `tests/test-pr-loop-system.sh:129`), reviewer status cases 1/2/3/4/5 (`tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:173`, `tests/test-pr-loop-system.sh:493`, `tests/test-pr-loop-system.sh:1315`, `tests/test-pr-loop-system.sh:1347`), reaction detection (`tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:223`), PR review submissions as reviewer comments (`tests/test-pr-loop-system.sh:236`), phase derivation from state files (`tests/test-pr-loop-system.sh:256`, `tests/test-pr-loop-system.sh:459`, `tests/test-pr-loop-system.sh:474`), monitor output phases (`tests/test-pr-loop-system.sh:946`), and goal-tracker parsing/update/idempotency/repair (`tests/test-pr-loop-system.sh:315`, `tests/test-pr-loop-system.sh:377`, `tests/test-pr-loop-system.sh:681`, `tests/test-pr-loop-system.sh:722`, `tests/test-pr-loop-system.sh:1242`).
- inputs_outputs_state: Inputs are JSON fixtures under `tests/fixtures`, `MOCK_GH_*` environment variables, generated `.humanize/pr-loop/<session>/state.md`, `approve-state.md`, `cancel-state.md`, `maxiter-state.md`, `goal-tracker.md`, and mock `codex` output. Outputs are pass/fail counters plus created/deleted temp PR-loop directories, generated goal tracker rows, and hook output captures. It models state transitions including active `state.md`, terminal approval, cancel, max-iteration, and Codex-analyzing via recent `round-*-pr-check.md` growth (`tests/test-pr-loop-system.sh:849`).
- gates_or_invariants: Round-0 startup cases 1/2/3 do not require a trigger, while startup cases 4/5 and later rounds do (`tests/test-pr-loop-system.sh:1112`). Setup cases 4/5 with missing `trigger_comment_id` must fail and clean the loop directory (`tests/test-pr-loop-system.sh:1384`). Goal tracker updates must be idempotent for the same round/bot and repair partial summary/log updates (`tests/test-pr-loop-system.sh:722`, `tests/test-pr-loop-system.sh:1242`). Monitor must report the right phase label for approved, waiting, cancelled, and Codex-analyzing states (`tests/test-pr-loop-system.sh:946`, `tests/test-pr-loop-system.sh:984`, `tests/test-pr-loop-system.sh:1025`, `tests/test-pr-loop-system.sh:1064`).
- dependencies_and_callers: Exercises `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, `hooks/lib/loop-common.sh`, and `hooks/pr-loop-stop-hook.sh`. It depends on `git`, `jq`, `timeout`, shell date compatibility with GNU/BSD variants, and mocks under `tests/mocks`.
- edge_cases_or_failure_modes: Explicitly covers no comments, partial comments, all comments, comments followed by newer commits, missing trigger IDs, stale force-push trigger assumptions, approval-only reviews, unpushed commit detection pattern, force-push ancestry logic, monitor file growth false positives/old mtime fallback, and stop-hook goal-tracker update from mocked bot review analysis.
- validation_or_tests: This file is run directly as `tests/test-pr-loop-system.sh [filter]` and by `tests/run-all-tests.sh` (`tests/run-all-tests.sh:53`). Its `main` reports total run/passed/failed and exits nonzero on any failure (`tests/test-pr-loop-system.sh:1889`, `tests/test-pr-loop-system.sh:1899`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-097 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template used by validators to explain why direct mutation of `.humanize/pr-loop/state.md` is forbidden.
- algorithmic_behavior: Defines a standard denial message titled “PR Loop State File Modification Blocked” and states that `.humanize/pr-loop/state.md` is PR-loop managed (`prompt-template/block/pr-loop-state-modification.md:1`, `prompt-template/block/pr-loop-state-modification.md:3`). It lists the state fields protected by the invariant: round number, PR number/branch, active bots, Codex configuration, and polling settings (`prompt-template/block/pr-loop-state-modification.md:5`).
- inputs_outputs_state: Inputs are template-loading calls from hook/common code; no variables are rendered in this specific file. Output is human-facing validator text. It does not mutate state; it documents the state ownership rule that prevents corruption.
- gates_or_invariants: The invariant is direct: modifying this state file would corrupt PR-loop state (`prompt-template/block/pr-loop-state-modification.md:12`). The production wrapper `pr_loop_state_blocked_message` loads this template with a fallback if missing (`hooks/lib/loop-common.sh:1008`, `hooks/lib/loop-common.sh:1014`).
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh` and surfaced by PR-loop write/bash validators. Tests assert `state.md` writes are blocked in `tests/test-pr-loop-hooks.sh:68` and broad cancel/state bypass cases are covered in `tests/test-cancel-signal-file.sh`.
- edge_cases_or_failure_modes: If this template is missing, production falls back to a shorter block message (`hooks/lib/loop-common.sh:1010`). A stale or weak template would not change enforcement, but it would degrade operator guidance during a blocked action.
- validation_or_tests: Template existence/reference integrity is covered by `tests/test-template-references.sh`, and validator behavior that uses this message path is covered by `tests/test-pr-loop-hooks.sh:85` and `tests/test-cancel-signal-file.sh:141`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-127 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: Comment-present PR-loop completion contract. It tells the working agent what gates must be satisfied after addressing existing PR comments before the stop hook can poll for bot reviews.
- algorithmic_behavior: Requires three ordered completion actions: commit and push changes, comment on the PR with `{{BOT_MENTION_STRING}} please review`, and write a resolution summary to `{{RESOLVE_PATH}}` (`prompt-template/pr-loop/critical-requirements-has-comments.md:6`, `prompt-template/pr-loop/critical-requirements-has-comments.md:8`, `prompt-template/pr-loop/critical-requirements-has-comments.md:12`, `prompt-template/pr-loop/critical-requirements-has-comments.md:15`). The summary must include issues addressed, files modified, and tests added (`prompt-template/pr-loop/critical-requirements-has-comments.md:18`). It ends by handing off to the stop hook for review polling (`prompt-template/pr-loop/critical-requirements-has-comments.md:23`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, supplied by `scripts/setup-pr-loop.sh` when comments exist. Output is appended/printed critical instruction text in the initial PR-loop setup output. It drives subsequent external state transitions: pushed branch changes, new PR trigger comment, and a local `round-0-pr-resolve.md` summary.
- gates_or_invariants: Used only when `COMMENT_COUNT` is nonzero; no-comment startup uses a different critical template and forbids triggering review. The production branch selects this template when comments are present (`scripts/setup-pr-loop.sh:915`, `scripts/setup-pr-loop.sh:940`). Its invariant is that existing reviewer comments require real code/doc changes to be committed, pushed, and re-triggered before stop-hook polling is meaningful.
- dependencies_and_callers: Loaded via `load_and_render_safe` in `scripts/setup-pr-loop.sh` with a fallback critical block (`scripts/setup-pr-loop.sh:916`, `scripts/setup-pr-loop.sh:940`). It coordinates with `fetch-pr-comments.sh` because the fetched comment count determines which critical requirements are shown (`scripts/setup-pr-loop.sh:430`, `scripts/setup-pr-loop.sh:890`), and with `hooks/pr-loop-stop-hook.sh`, which later validates trigger/review state.
- edge_cases_or_failure_modes: If variables are missing, the rendered prompt may contain unresolved placeholders, weakening the workflow contract. If comments exist but `COMMENT_COUNT` is computed incorrectly, setup could choose the no-comment contract and skip the required PR trigger. If the agent writes a summary without push/comment, the stop hook may block waiting for a valid trigger or review.
- validation_or_tests: Template loading and reference checks are covered by template suites in `tests/run-all-tests.sh:34` and `tests/run-all-tests.sh:38`. The has-comments flow is exercised indirectly by PR-loop system tests for startup cases with comments and trigger requirements (`tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:1315`, `tests/test-pr-loop-system.sh:1384`) and stop-hook trigger validation (`tests/test-pr-loop-stophook.sh:20`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections above; each assigned item has one section.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`