# agent_29 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-029 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: PostToolUse Bash hook for the RLCR loop. It records the current Claude Code `session_id` into the active RLCR `state.md` immediately after `setup-rlcr-loop.sh` runs, so subsequent RLCR hooks can identify the loop leader session rather than accidentally applying loop restrictions to team-member sessions. The hook is registered for Bash PostToolUse in `hooks/hooks.json:52-59`.
- algorithmic_behavior: Reads hook JSON from stdin (`hooks/loop-post-bash-hook.sh:26-27`), resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` (`:29-33`), then checks `.humanize/.pending-session-id`. If absent, it exits as a normal no-op (`:35-38`). If present, it reads line 1 as the target `state.md` path and line 2 as the expected setup-script command signature (`:40-48`). When a signature exists, it extracts `.tool_input.command` via `jq` and only proceeds if the command starts with the exact setup script path in accepted quoted or unquoted forms (`:56-91`). It extracts `.session_id` via `jq` (`:94-98`) and patches only an empty `session_id:` line using `awk`, then removes the pending signal file (`:105-123`).
- inputs_outputs_state: Inputs are hook JSON stdin, `CLAUDE_PROJECT_DIR`, `.humanize/.pending-session-id`, the target RLCR `state.md`, and `jq` availability. Output is an in-place state transition from `session_id:` to `session_id: <hook session id>` when the current value is empty (`:107-119`). It deletes `.humanize/.pending-session-id` after a successful command/session handling (`:122-123`). `scripts/setup-rlcr-loop.sh` initializes `session_id:` in `state.md` and writes the two-line pending signal with the resolved script path at `scripts/setup-rlcr-loop.sh:767-798`.
- gates_or_invariants: The command must be a real setup invocation, not arbitrary text containing the setup script name. Accepted forms require the exact signature followed by end-of-string or POSIX whitespace, quoted or unquoted (`hooks/loop-post-bash-hook.sh:70-86`). Non-setup Bash events preserve the signal and leave `state.md` unchanged (`:88-91`). Missing or invalid target state file cleans the stale signal (`:50-53`). Missing session id leaves the signal for a later hook attempt (`:100-103`). Existing non-empty `session_id` is not overwritten (`:105-120`).
- dependencies_and_callers: Depends on Claude hook JSON schema, `jq`, `grep`, `sed`, `awk`, `mv`, and the setup signal produced by `scripts/setup-rlcr-loop.sh`. Installed through `hooks/hooks.json:52-59`. Related downstream session filtering is covered by the broader `tests/test-session-id.sh` suite, which sources `hooks/lib/loop-common.sh` and validates hook behavior.
- edge_cases_or_failure_modes: If `jq` is missing and a command signature is present, the hook cannot confirm the command and exits without consuming the signal (`hooks/loop-post-bash-hook.sh:60-68`). If `session_id` is unavailable, the pending signal remains, which is intentional retry behavior (`:100-103`). If the state line contains whitespace after `session_id:` rather than exactly `session_id:`, `CURRENT_SESSION_ID` may be considered empty but the `awk` replacement pattern only matches `^session_id:$` (`:107-118`). A stale signal pointing to a removed state file is removed (`:50-53`).
- validation_or_tests: `tests/test-session-id.sh` directly validates initial `session_id` creation (`tests/test-session-id.sh:54-78`), pending signal creation (`:83-107`), successful hook recording and signal removal (`:139-160`), rejection of non-setup commands while preserving the signal (`:484-505`), acceptance of quoted setup invocation (`:515-524`), special characters in session ids (`:553-570`), full script path signature shape (`:811-820`), false-positive rejection for `echo`/basename-only commands (`:848-875`), rejection of quoted-prefix concatenation (`:905-920`), and acceptance of unquoted or tab-delimited invocations (`:950-1004`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-059 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: End-to-end executable specification for the real monitor behavior in `scripts/humanize.sh`. It does not stub `_humanize_monitor_codex`; it sources the production script and exercises monitor cleanup, deletion detection, terminal restoration, shell compatibility, and PR monitor behavior (`tests/test-monitor-e2e-real.sh:3-13`, `:148-153`).
- algorithmic_behavior: Creates disposable project/session directories under `/tmp/test-monitor-e2e-real-$$`, writes realistic `.humanize/rlcr/<timestamp>/state.md` and `goal-tracker.md`, creates matching fake cache logs under `XDG_CACHE_HOME`, then runs monitor processes in the background (`:48-56`, `:64-108`, `:160-163`). It triggers state-machine transitions externally by deleting `.humanize/rlcr` (`:168-176`), sending SIGINT/SIGTERM to monitor processes (`:492-519`, `:647-665`, `:938-965`), and verifying output and exit behavior. It repeats the same categories for bash and zsh where zsh is available.
- inputs_outputs_state: Inputs are generated RLCR state fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, `plan_file`, `base_branch`, and `review_started` (`:69-82`, `:248-261`, `:387-400`); generated goal trackers (`:84-97`, `:263-276`, `:402-415`); fake `HOME`/`XDG_CACHE_HOME`; shimmed `tput`/`clear`; and real `scripts/humanize.sh`. Outputs are PASS/FAIL counters and final exit status, with success requiring `TESTS_FAILED=0` (`:995-1017`).
- gates_or_invariants: For RLCR monitor deletion, the process must exit within a bounded wait and emit `Monitoring stopped:` plus a deletion reason (`:171-201`). Output must not contain shell glob failures such as `no matches found` or `bad pattern` (`:204-209`, `:362-365`, `:544-549`, `:678-681`). Cleanup must restore terminal state, checked by cleanup text and source-level presence of scroll reset `printf "\033[r"` (`:211-224`). Graceful stop must return `EXIT_CODE:0` in deletion tests (`:226-230`, `:368-371`). SIGINT tests allow cleanup messaging or clean exit-code evidence because process-group signal delivery differs across shells (`:532-541`, `:672-675`, `:973-982`).
- dependencies_and_callers: Depends on `scripts/humanize.sh`, especially `_humanize_monitor_codex` at `scripts/humanize.sh:225`, codex cleanup/graceful-stop helpers at `:647-688`, loop deletion checks at `:749-755` and `:811-815`, and `_humanize_monitor_pr` at `:1125`. It also depends on process utilities (`kill`, `pkill`, `sleep`, `wait`), optional `zsh`, terminal shims, cache path sanitization matching production behavior, and PR monitor command dispatch through `humanize monitor pr`.
- edge_cases_or_failure_modes: The script intentionally covers deletion of the whole `.humanize/rlcr` directory while monitoring (`tests/test-monitor-e2e-real.sh:168-185`), zsh no-match glob hazards (`:240-373`), SIGINT delivery differences in bash/zsh (`:492-519`, `:647-665`), terminal cleanup races, and PR monitor operation both with `--once` and continuous polling (`:686-824`, `:827-990`). It has permissive fallback passes for some signal cases after force termination if the monitor demonstrably started, so those assertions are more liveness-oriented than strict signal-semantics proofs (`:505-519`, `:951-965`).
- validation_or_tests: The file is itself an executable test suite. It covers six major tests: RLCR codex monitor deletion in bash (`:61-231`), RLCR codex monitor deletion in zsh (`:234-373`), RLCR codex monitor SIGINT in bash (`:376-549`), RLCR codex monitor SIGINT in zsh (`:552-683`), PR monitor `--once`/deletion path (`:686-824`), and PR monitor continuous SIGINT path (`:827-990`). I inspected the test; I did not execute it in this read-only research pass.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-089 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Prompt block used as the user-facing gate message when direct `goal-tracker.md` modification is blocked after Round 0. It encodes the RLCR ownership rule: after Round 0, only Codex modifies the Goal Tracker (`prompt-template/block/goal-tracker-modification.md:1-5`).
- algorithmic_behavior: Renders a blocking instruction with `{{CURRENT_ROUND}}` in the title and `{{SUMMARY_FILE}}` as the required request location (`:1`, `:9-10`). It tells the blocked actor to add a `Goal Tracker Update Request` section to their summary with requested changes and justification (`:12-23`). This converts direct mutation attempts into a reviewable request that Codex can later apply if justified.
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `SUMMARY_FILE`. Output is a rendered markdown message from `goal_tracker_blocked_message`, which calls `load_and_render_safe` with this template and a fallback (`hooks/lib/loop-common.sh:1217-1228`). State transition is procedural: direct Write/Edit/Bash changes to `goal-tracker.md` are rejected by validators, and desired changes move into the summary-file request channel.
- gates_or_invariants: The invariant is explicit: “only Codex can modify the Goal Tracker” after Round 0 (`prompt-template/block/goal-tracker-modification.md:3`). It forbids direct `goal-tracker.md` modification via Write, Edit, or Bash (`:5`). The request must include both requested changes and justification (`:16-22`).
- dependencies_and_callers: Called through `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1217-1228`. The blocking message is used by write validation (`hooks/loop-write-validator.sh:196`), edit validation (`hooks/loop-edit-validator.sh:147`), and bash validation when a command targets the active loop goal tracker (`hooks/loop-bash-validator.sh:351-355`). Rendering depends on `hooks/lib/template-loader.sh`, whose `load_and_render_safe` fallback behavior is defined at `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `goal_tracker_blocked_message` falls back to a shorter built-in message (`hooks/lib/loop-common.sh:1222-1228`). The template is instructional rather than a parser, so it does not itself verify that the summary file later contains the requested section or that Codex applies it. The gate assumes the caller has already determined the current round and that the target path is the active `goal-tracker.md`.
- validation_or_tests: `tests/test-template-references.sh` includes `block/goal-tracker-modification.md` in the common-template existence set (`tests/test-template-references.sh:152-159`). The validator call sites above are direct behavioral references. Broader template rendering robustness is covered by `tests/robustness/test-template-stress-robustness.sh`.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-119 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Claude next-round prompt note for the `--push-every-round` mode. It turns the `push_every_round` loop setting into an explicit per-round instruction: push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:2`).
- algorithmic_behavior: The template contains a single prompt-level reminder. `hooks/loop-codex-stop-hook.sh` appends it to `NEXT_PROMPT_FILE` only when `PUSH_EVERY_ROUND` is `true` (`hooks/loop-codex-stop-hook.sh:1614-1621`). If the template is absent, the hook uses the fallback “Also push your changes after committing” (`:1616-1619`).
- inputs_outputs_state: Input is the loop state/config flag `push_every_round`, initialized by setup in `state.md` (`scripts/setup-rlcr-loop.sh:767-775`). Output is appended markdown in the next Claude prompt. It shifts the expected round lifecycle from local commit completion to commit plus remote push completion, but it does not itself execute or validate the push.
- gates_or_invariants: The invariant is conditional: when `--push-every-round` is enabled, each round’s work should be pushed after committing. This is a prompt contract rather than an enforcement hook. The file does not specify branch, remote name, sync policy, or recovery behavior.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:1614-1621`. The setup CLI exposes `--push-every-round` and records `push_every_round: $PUSH_EVERY_ROUND` in state (`scripts/setup-rlcr-loop.sh:767-775`). Related user-facing docs/templates include `prompt-template/block/unpushed-commits.md` and `prompt-template/block/git-push.md`, but this assigned file is specifically the Claude next-round note.
- edge_cases_or_failure_modes: If the remote push fails, this template has no retry or validation logic; enforcement must come from later git/push validators or human review. If the template is missing, the fallback still preserves the prompt-level push instruction (`hooks/loop-codex-stop-hook.sh:1616-1619`). The file begins with a blank line before the note (`prompt-template/claude/push-every-round-note.md:1-2`), which affects prompt spacing only.
- validation_or_tests: Direct reference is visible in `hooks/loop-codex-stop-hook.sh:1616`. Search evidence also shows the same template path referenced by that hook. No dedicated executable test was found for this exact two-line template; its behavior is covered indirectly by template loading and next-prompt construction.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-149 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for the RLCR prompt template renderer. It tests `hooks/lib/template-loader.sh` under normal, large, and special-character substitutions, protecting prompt rendering from corruption, injection, and shell/sed escaping bugs (`tests/robustness/test-template-stress-robustness.sh:3-10`).
- algorithmic_behavior: Sources production `template-loader.sh` and shared test helpers (`:14-18`), creates a temporary test directory (`:19`), then exercises `render_template`, `load_and_render`, and `load_and_render_safe`. It checks ordinary substitution (`:33-42`), fallback rendering for missing templates (`:44-52`), file-template rendering (`:54-72`), large variable values/templates (`:82-132`), many variables (`:134-151`), and edge cases involving special characters (`:161-283`).
- inputs_outputs_state: Inputs are inline template strings, generated template files in `TEST_DIR`, and `VAR=value` assignments. Outputs are rendered strings and PASS/FAIL counters. Final status is delegated to `print_test_summary`, which returns success only when no failures were recorded (`tests/robustness/test-template-stress-robustness.sh:285-290`; helper behavior at `tests/test-helpers.sh:58-77`).
- gates_or_invariants: Key renderer invariants include single-pass substitution, fallback safety, preservation of regex/sed-special characters, correct handling of ampersands, backslashes, dollar signs, empty values, repeated placeholders, placeholders at string boundaries, and placeholder-only templates. The injection-prevention test is especially important: a value containing `{{VAR_B}}` must remain literal rather than being expanded during the same render pass (`tests/robustness/test-template-stress-robustness.sh:218-228`). That matches the production renderer’s documented single-pass design in `hooks/lib/template-loader.sh:50-57` and implementation at `:71-129`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, especially `render_template` (`hooks/lib/template-loader.sh:58-132`), `load_and_render` (`:134-147`), and `load_and_render_safe` (`:167-203`). Depends on `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and summary handling (`tests/test-helpers.sh:30-89`). Uses standard shell utilities `seq`, `printf`, `date`, `wc`, and `grep`.
- edge_cases_or_failure_modes: Large values are tested at 10KB and 100KB (`tests/robustness/test-template-stress-robustness.sh:82-109`), and large file rendering at roughly 100KB (`:111-132`). Special-character coverage includes regex metacharacters (`:161-170`), ampersands (`:172-181`), Windows-style backslashes (`:183-193`), embedded newlines (`:195-205`), empty values (`:207-216`), dollar signs (`:241-250`), and variable placement boundaries (`:252-283`). The “Unicode content” test is actually basic ASCII text despite its label (`:230-239`), so it is a weak true-Unicode coverage point.
- validation_or_tests: The file is itself a robustness test suite and exits through `print_test_summary` (`:289-290`). I inspected it and its production renderer dependency; I did not execute it in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV_RLCR_WITH_SWARM_TEAM-HZ-029, DEV_RLCR_WITH_SWARM_TEAM-HZ-059, DEV_RLCR_WITH_SWARM_TEAM-HZ-089, DEV_RLCR_WITH_SWARM_TEAM-HZ-119, DEV_RLCR_WITH_SWARM_TEAM-HZ-149
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`