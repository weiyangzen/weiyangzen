# agent_02 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Declarative sub-agent prompt directory for gen-plan draft relevance validation. Recursive inspection found one file: `agents/draft-relevance-checker.md`.
- algorithmic_behavior: `agents/draft-relevance-checker.md:1-5` defines the agent name, description, model `haiku`, and tools `Read, Glob, Grep`. The prompt requires the agent to inspect repository docs/structure, analyze a supplied draft for semantic repository relevance, then emit exactly one verdict shape: `RELEVANT: ...` or `NOT_RELEVANT: ...` (`agents/draft-relevance-checker.md:14-29`).
- inputs_outputs_state: Input is draft document content plus repository context gathered through read/glob/grep. Output is a short relevance verdict. It does not mutate repo state; it acts as a classification gate before plan generation.
- gates_or_invariants: The relevance policy is intentionally lenient: informal, multilingual, or rough drafts are acceptable, semantic relevance beats syntactic similarity, and doubt resolves to relevant (`agents/draft-relevance-checker.md:31-36`).
- dependencies_and_callers: `commands/gen-plan.md:57` instructs use of `humanize:draft-relevance-checker`. The agent depends on repository docs such as `README.md` or `CLAUDE.md`, directory structure, and existing paths/functions/features (`agents/draft-relevance-checker.md:16-25`).
- edge_cases_or_failure_modes: Ambiguous drafts are biased toward relevant. Drafts unrelated to the repository, such as the provided cooking example, should be rejected (`agents/draft-relevance-checker.md:38-46`). A false negative would improperly block gen-plan input.
- validation_or_tests: `tests/test-gen-plan.sh` validates this agent exists, has the expected name/model/tools/frontmatter, valid naming/model conventions, and English-only content (`tests/test-gen-plan.sh:111-155`, `tests/test-gen-plan.sh:241-244`, `tests/test-gen-plan.sh:292`, `tests/test-gen-plan.sh:396`, `tests/test-gen-plan.sh:419-421`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-032 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Stop hook for the PR loop state machine. It intercepts exit attempts, requires a resolution summary, polls configured review bots, runs local Codex over bot feedback, and advances or blocks the loop (`hooks/pr-loop-stop-hook.sh:3-19`).
- algorithmic_behavior: The hook finds the active `.humanize/pr-loop/<timestamp>/state.md`, parses YAML-ish frontmatter including `current_round`, `pr_number`, bot arrays, trigger fields, commit fields, and Codex settings (`hooks/pr-loop-stop-hook.sh:49-75`, `hooks/pr-loop-stop-hook.sh:88-175`). It resolves the correct PR base repo for forks before using `gh` (`hooks/pr-loop-stop-hook.sh:197-237`), exits cleanly if the PR is merged/closed (`hooks/pr-loop-stop-hook.sh:239-256`), blocks if the round resolution file is missing (`hooks/pr-loop-stop-hook.sh:258-277`), enforces clean/pushed git state (`hooks/pr-loop-stop-hook.sh:279-361`), detects force pushes and clears stale trigger state (`hooks/pr-loop-stop-hook.sh:364-424`), and enforces max iterations / no-active-bot terminal states (`hooks/pr-loop-stop-hook.sh:426-448`).
- inputs_outputs_state: Inputs are hook stdin, `.humanize/pr-loop/.../state.md`, `round-N-pr-resolve.md`, local git state, GitHub PR state/comments/reactions, bot review polling output, and Codex CLI output. Outputs include JSON block decisions, updated `state.md`, terminal renamed state files such as `approve-state.md`, `merged-state.md`, `closed-state.md`, `maxiter-state.md`, and `usage-limit-state.md`, plus `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md` (`hooks/pr-loop-stop-hook.sh:1182-1244`, `hooks/pr-loop-stop-hook.sh:1568-1590`, `hooks/pr-loop-stop-hook.sh:1606-1644`).
- gates_or_invariants: Required gates include valid PR number and numeric round (`hooks/pr-loop-stop-hook.sh:186-195`), clean non-`.humanize` working tree, no unpushed commits, no stale trigger after new commits or force-push, trigger comments for later rounds and startup cases 4/5, Claude eyes reaction when Claude is configured and trigger-required, Codex CLI availability, nonempty Codex output, and final marker interpretation (`APPROVE`, `WAITING_FOR_BOTS`, `USAGE_LIMIT_HIT`, or issues remaining) (`hooks/pr-loop-stop-hook.sh:609-747`, `hooks/pr-loop-stop-hook.sh:749-800`, `hooks/pr-loop-stop-hook.sh:1305-1423`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for bot mapping, YAML-list helpers, loop discovery, and goal-tracker updates; `scripts/portable-timeout.sh`; GitHub CLI `gh`; `git`; `jq`; Codex CLI; `scripts/poll-pr-reviews.sh`; `scripts/check-bot-reactions.sh`; `scripts/check-pr-reviewer-status.sh`; and block templates loaded with `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:52-63`, `hooks/pr-loop-stop-hook.sh:356-357`, `hooks/pr-loop-stop-hook.sh:415-417`, `hooks/pr-loop-stop-hook.sh:740-742`, `hooks/pr-loop-stop-hook.sh:785-786`, `hooks/pr-loop-stop-hook.sh:1262`).
- edge_cases_or_failure_modes: Fork PR lookup falls back from current repo to parent repo (`hooks/pr-loop-stop-hook.sh:214-237`). Trigger detection paginates issue comments and filters by current user plus commit timestamp to avoid stale trigger reuse (`hooks/pr-loop-stop-hook.sh:459-526`). Per-bot timeout is anchored to trigger time, with startup cases 2/3 using current time to avoid immediate historical timeout (`hooks/pr-loop-stop-hook.sh:817-879`). Codex approval can arrive as a +1 reaction instead of a comment (`hooks/pr-loop-stop-hook.sh:647-719`, `hooks/pr-loop-stop-hook.sh:985-1040`). Timed-out bots are removed from `active_bots`, possibly approving if none remain (`hooks/pr-loop-stop-hook.sh:1065-1180`). Cancel signal aborts polling without further transition (`hooks/pr-loop-stop-hook.sh:952-956`).
- validation_or_tests: Covered by PR loop test surfaces including `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-scripts.sh`, and robustness suites for PR API, concurrent state, state transitions, and hook input. Static inspection only in this worker run; no files were modified or tests executed.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-062 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable regression specification for RLCR setup plan-file validation. It verifies `scripts/setup-rlcr-loop.sh` rejects unsafe or inconsistent plan-file inputs before loop state is created.
- algorithmic_behavior: The test creates temporary git repositories, configures commits, writes plan fixtures, and injects a mock `codex` binary to avoid invoking real Codex (`tests/test-plan-file-validation.sh:36-83`). It then runs `scripts/setup-rlcr-loop.sh` under many path, repo, tracking, content, CLI, branch, and Codex-parameter scenarios, recording pass/fail/skip counters and exiting with `TESTS_FAILED` (`tests/test-plan-file-validation.sh:32-34`, `tests/test-plan-file-validation.sh:742-751`).
- inputs_outputs_state: Inputs are temporary plan paths, git repo states, CLI options like `--plan-file`, `--track-plan-file`, and `--codex-model`, plus branch names and file contents. Outputs are terminal PASS/FAIL/SKIP reports and a process exit code equal to failure count. It creates and removes temporary test repos via `mktemp -d` and `trap` (`tests/test-plan-file-validation.sh:37-38`).
- gates_or_invariants: Test expectations mirror implementation gates in `scripts/setup-rlcr-loop.sh`: only one plan-file source (`scripts/setup-rlcr-loop.sh:300-307`), project must be a git repo with at least one commit (`scripts/setup-rlcr-loop.sh:334-344`), plan path must be relative, space-free, metacharacter-free, non-symlink, existing/readable, inside project, and outside submodules (`scripts/setup-rlcr-loop.sh:356-457`), tracking state must match `--track-plan-file` (`scripts/setup-rlcr-loop.sh:463-508`), content must be at least 5 lines and at least 3 non-comment content lines (`scripts/setup-rlcr-loop.sh:519-572`), and branch/model/effort values must be YAML-safe (`scripts/setup-rlcr-loop.sh:597-623`).
- dependencies_and_callers: Directly depends on `scripts/setup-rlcr-loop.sh`, git, chmod behavior for permission test, shell path handling, and mock `codex`. Runtime continuation of the same plan-file invariant is enforced by `hooks/loop-plan-file-validator.sh`, which strict-parses state, blocks branch changes, and preserves tracked/gitignored plan-file status during an active loop (`hooks/loop-plan-file-validator.sh:44-48`, `hooks/loop-plan-file-validator.sh:91-115`, `hooks/loop-plan-file-validator.sh:117-229`).
- edge_cases_or_failure_modes: Covers absolute paths, nonexistent files, nonexistent parent directories, spaces in path/filename, shell metacharacters, direct symlink, inaccessible directory/path resolution, `../` project escape, non-git repo, git repo without commits, tracked plan without flag, untracked plan with flag, modified tracked plan with flag, YAML-unsafe branch characters, blank-only plans, sparse plans, HTML-comment-only plans, hash-comment-only plans, single-line HTML comments with valid content, duplicated positional/explicit plan-file inputs, invalid Codex model/effort characters, and valid dotted/hyphenated Codex model (`tests/test-plan-file-validation.sh:88-220`, `tests/test-plan-file-validation.sh:244-424`, `tests/test-plan-file-validation.sh:431-740`).
- validation_or_tests: This file is itself the validation artifact. It should be run only in a writable test environment because it creates temp repos and files; this worker performed static research only as requested.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-092 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Human-facing block template for the RLCR Stop hook large-file gate. It explains why exit is blocked when files exceed the configured line limit.
- algorithmic_behavior: Template consumes `{{MAX_LINES}}` and `{{LARGE_FILES}}`, lists oversized files, and instructs the agent to split code or documentation before attempting exit again (`prompt-template/block/large-files.md:1-25`).
- inputs_outputs_state: Inputs are rendered variables from `hooks/loop-codex-stop-hook.sh`, where `MAX_LINES=2000` and `LARGE_FILES` is accumulated during file scanning (`hooks/loop-codex-stop-hook.sh:457-520`). Output is a block reason passed into a JSON decision; the template itself does not write state.
- gates_or_invariants: Code files must be split into modules below the limit while preserving behavior; documentation files must be split into logical sections while preserving references, navigation, and narrative flow (`prompt-template/block/large-files.md:13-23`).
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` in `hooks/loop-codex-stop-hook.sh:518-520`, followed by a block message about files over `MAX_LINES` (`hooks/loop-codex-stop-hook.sh:524`). Rendering depends on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: The template distinguishes code and documentation remediation. It optionally points to a `code-simplifier` plugin if installed, but does not require it (`prompt-template/block/large-files.md:16`). If template loading fails, caller has a fallback block message (`hooks/loop-codex-stop-hook.sh:511-520`).
- validation_or_tests: Template reference and safe-render behavior are covered by template-loader/template-reference tests, including checks around `load_and_render_safe` use (`tests/test-template-references.sh:69-94`, `tests/test-template-loader.sh:197-221`).
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-122 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex prompt template for mandatory full goal-alignment checkpoints during RLCR. It is the circuit-breaker review contract for verifying implementation progress against the original plan and goal tracker.
- algorithmic_behavior: The template requires Codex to read `@{{PLAN_FILE}}` first, review Claude’s summary, audit every immutable acceptance criterion, detect forgotten work, audit deferrals, review implementation reality, update the goal tracker section, inspect recent historical summaries/reviews, and decide whether progress is complete, incomplete, or stagnating (`prompt-template/codex/full-alignment-review.md:1-93`).
- inputs_outputs_state: Inputs are rendered placeholders including current round, plan file, summary content, goal tracker file, docs path, goal tracker update section, completed iteration count, loop timestamp, previous round numbers, and review result file. Output is written to `@{{REVIEW_RESULT_FILE}}` with action items and a required final line of either `STOP` for stagnation or `COMPLETE` only under strict full-completion conditions (`prompt-template/codex/full-alignment-review.md:61-93`).
- gates_or_invariants: Mandatory gates include reading the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), evaluating every acceptance criterion with evidence/blocker/deferral justification (`prompt-template/codex/full-alignment-review.md:19-29`), identifying forgotten or unverified work (`prompt-template/codex/full-alignment-review.md:30-35`), treating deferred items as incomplete (`prompt-template/codex/full-alignment-review.md:86-93`), and writing `STOP` when stagnation signals are present (`prompt-template/codex/full-alignment-review.md:74-85`).
- dependencies_and_callers: Selected by `hooks/loop-codex-stop-hook.sh` on full alignment rounds, where the interval is validated and computed as `(round % FULL_REVIEW_ROUND) == FULL_REVIEW_ROUND - 1` (`hooks/loop-codex-stop-hook.sh:799-807`). It is rendered with `load_and_render_safe "$TEMPLATE_DIR" "codex/full-alignment-review.md"` and the goal tracker update section (`hooks/loop-codex-stop-hook.sh:844-850`). The interval is configured by `scripts/setup-rlcr-loop.sh` via `--full-review-round` and persisted as `full_review_round` (`scripts/setup-rlcr-loop.sh:201-214`, `scripts/setup-rlcr-loop.sh:775`).
- edge_cases_or_failure_modes: Handles repeated issues, no meaningful acceptance-criteria progress, repeated Claude mistakes, circular discussion, no new code, and repeated Codex feedback without resolution (`prompt-template/codex/full-alignment-review.md:76-83`). It also prevents false completion when any acceptance criterion is deferred or only partially met (`prompt-template/codex/full-alignment-review.md:88-93`).
- validation_or_tests: Template loading/reference behavior is covered by template-reference and loader tests. State parsing around `full_review_round` defaults/minimums is covered in robustness tests referenced by `tests/robustness/test-state-file-robustness.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV_RLCR_WITH_SWARM_TEAM-HZ-002, DEV_RLCR_WITH_SWARM_TEAM-HZ-032, DEV_RLCR_WITH_SWARM_TEAM-HZ-062, DEV_RLCR_WITH_SWARM_TEAM-HZ-092, DEV_RLCR_WITH_SWARM_TEAM-HZ-122
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`