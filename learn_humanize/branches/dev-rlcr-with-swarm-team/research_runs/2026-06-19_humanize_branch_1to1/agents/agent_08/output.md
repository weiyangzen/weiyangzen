# agent_08 dev-rlcr-with-swarm-team 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0d5f0943ae9b1f80c5115aa946ebeb289e2cb83d`

## Item Evidence

### DEV_RLCR_WITH_SWARM_TEAM-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: Behavior-defining public documentation for the Humanize plugin’s two main workflow algorithms: RLCR, an iterative Claude implementation plus Codex review loop, and PR loop, a GitHub PR review remediation loop. The README defines user-visible commands, expected prerequisites, major phases, model/timeout/max-iteration knobs, monitor entry points, and the security posture for Codex sandboxing.
- algorithmic_behavior: The RLCR flow is documented as `plan.md -> Claude Implements & Summarizes -> Codex Reviews Summary -> feedback loop -> codex review -> done` in the Mermaid graph at `README.md:99-107`. The phase split is explicit at `README.md:109-112`: implementation phase repeats Claude work and Codex summary review until `COMPLETE`; review phase runs `codex review --base <branch>` and interprets `[P0-9]` severity markers. The PR loop behavior is documented at `README.md:202-210`: detect current branch PR, fetch bot comments, let Claude analyze and fix issues, push changes and trigger re-review, then use a Stop Hook to poll every 30 seconds with 15 minute timeout per bot before local Codex validation.
- inputs_outputs_state: Inputs include a plan file or generated plan file for RLCR (`README.md:115-122`), a draft file for `/humanize:gen-plan` (`README.md:165-181`), an open GitHub PR plus selected bot flags for PR loop (`README.md:184-199`), Codex model/effort strings, timeout values, max iterations, base branch, tracking/push flags, and optional sandbox bypass env var. Outputs include `.humanize/rlcr/<timestamp>/` loop artifacts and monitorable logs (`README.md:123-127`), generated structured plans with `AC-X` acceptance criteria (`README.md:176-181`), and PR loop state driven by pushed fixes plus bot approvals (`README.md:202-210`). State progression is bounded by `--max`, with default 42 iterations for both RLCR and PR loop (`README.md:149`, `README.md:195`).
- gates_or_invariants: Codex CLI is a prerequisite (`README.md:52-55`); PR loop additionally requires GitHub CLI authentication and an associated open PR (`README.md:212-215`). Codex sandbox bypass is documented as dangerous, disabled by default, and only enabled by `HUMANIZE_CODEX_BYPASS_SANDBOX=true` or `1` (`README.md:60-69`). The review phase depends on base-branch detection, prioritized as remote default, `main`, then `master` (`README.md:156-157`). PR loop requires at least one bot flag, `--claude` or `--codex` (`README.md:187-191`).
- dependencies_and_callers: The README points users to plugin commands `/humanize:start-rlcr-loop`, `/humanize:gen-plan`, `/humanize:start-pr-loop`, and cancellation commands (`README.md:115-138`). It also documents sourcing `scripts/humanize.sh` and invoking `humanize monitor [rlcr|pr]`, tying the documentation directly to the assigned monitor script (`README.md:123-127`, `README.md:217-219`). Runtime dependencies include Codex CLI, GitHub CLI for PR loop, Claude Code plugin loading, and Stop Hook behavior.
- edge_cases_or_failure_modes: Sandbox bypass creates unrestricted filesystem and command execution risk (`README.md:81-84`). PR loop can stall or stop on missing GH auth, missing Codex, no associated open PR, bot timeout, max iterations, or unresolved review concerns. RLCR can skip implementation with `--skip-impl`, making the plan optional (`README.md:160-161`). There is a minor command naming ambiguity: Quick Start uses `/humanize:start-rlcr-loop` (`README.md:121`), while the command table omits the `humanize:` namespace for some rows (`README.md:134-138`).
- validation_or_tests: The README itself is documentation, but its described monitor path is tested by PR monitor tests in `tests/test-pr-loop-hooks.sh` and the PR loop runner `tests/test-pr-loop.sh`. Command behavior maps to setup scripts referenced elsewhere: `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, and Stop Hook scripts.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-038 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell utility implementing the user-facing `humanize monitor rlcr` and `humanize monitor pr` algorithms, plus shared parsers for goal tracker, git state, git status, and shell-compatible array splitting. It is core operational code because it interprets loop state directories, log files, terminal resize signals, git status, and PR/RLCR phase metadata into live monitor behavior.
- algorithmic_behavior: Startup sources shared monitor helpers from `scripts/lib/monitor-common.sh` and loop constants from `hooks/lib/loop-common.sh` if present (`scripts/humanize.sh:6-16`). Public helpers include `humanize_split_to_array` for bash/zsh pipe-delimited parsing (`scripts/humanize.sh:24-34`), `humanize_parse_goal_tracker` for AC/task/issue counts (`scripts/humanize.sh:38-118`), `humanize_detect_git_state` for repo state classification (`scripts/humanize.sh:122-168`), and `humanize_parse_git_status` for porcelain and diffstat summaries (`scripts/humanize.sh:172-216`). `_humanize_monitor_codex` monitors `.humanize/rlcr`, finds the latest timestamp session, locates Codex run/review logs in `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, draws a fixed status bar, tails logs incrementally, switches to newer sessions/logs, and handles resize/deletion/truncation (`scripts/humanize.sh:225-1075`). `_humanize_monitor_pr` monitors `.humanize/pr-loop`, supports `--once`, tails latest PR loop activity files, and renders PR round/bot/phase status (`scripts/humanize.sh:1125-1554`). The `humanize` dispatcher exposes only `monitor rlcr` and `monitor pr` subcommands (`scripts/humanize.sh:1078-1118`).
- inputs_outputs_state: Inputs are current working directory, `.humanize/rlcr` or `.humanize/pr-loop`, timestamp-named session dirs, `state.md` or `*-state.md`, `goal-tracker.md`, cached Codex logs, PR round files, git status, terminal dimensions, shell type, and optional `--once`. Outputs are terminal status bars, live log tails, centered waiting/error messages, one-shot PR monitor reports, and nonzero returns for missing monitor roots or missing sessions. RLCR state parsing extracts `current_round`, `max_iterations`, `full_review_round`, `codex_model`, `codex_effort`, `started_at`, `plan_file`, `ask_codex_question`, and `review_started` (`scripts/humanize.sh:346-365`). PR state parsing reads YAML frontmatter fields for round, max iterations, PR number, branch, configured/active bots, Codex config, and start time (`scripts/humanize.sh:1224-1253`).
- gates_or_invariants: The RLCR monitor refuses to run without `.humanize/rlcr` (`scripts/humanize.sh:236-240`) or without a timestamp session (`scripts/humanize.sh:713-718`). It requires log search to stay scoped to the current session to avoid cross-session contamination (`scripts/humanize.sh:261-296`). It enforces a consistency invariant that implementation log rounds must be strictly lower than review log rounds when both exist; otherwise it prints an inconsistent log state error and returns empty (`scripts/humanize.sh:328-335`). Terminal rendering requires enough height for the status bar plus content and waits through resize until valid (`scripts/humanize.sh:572-599`, `scripts/humanize.sh:726-734`). Signal handlers only set a resize flag for `WINCH` to avoid racing escape-sequence output (`scripts/humanize.sh:690-707`). Cleanup is idempotent through `cleanup_done` and restores terminal state (`scripts/humanize.sh:647-673`). PR monitor maps `approve` to `approved` and `maxiter` to `max-iterations` for display (`scripts/humanize.sh:1205-1221`), and `--once` errors on missing sessions or missing state files (`scripts/humanize.sh:1405-1420`).
- dependencies_and_callers: Called by users after sourcing the script, as documented in `README.md:123-127`. It depends on `scripts/lib/monitor-common.sh` for `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size`; those helpers implement timestamp session selection and state-file detection (`scripts/lib/monitor-common.sh:32-63`, `scripts/lib/monitor-common.sh:154-188`). It optionally uses `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` from monitor-common for richer PR status (`scripts/lib/monitor-common.sh:267-381`, `scripts/lib/monitor-common.sh:468-498`). It uses `DEFAULT_CODEX_MODEL` from `hooks/lib/loop-common.sh` as a PR monitor default (`scripts/humanize.sh:1249`). External commands include `git`, `sed`, `grep`, `sort`, `wc`, `find`, `stat`, `tail`, `date`, `tput`, `basename`, `kill`, and shell traps.
- edge_cases_or_failure_modes: Missing goal tracker returns zeroed counters and “No goal tracker” (`scripts/humanize.sh:39-42`). Non-git directories return “not a git repo” status from git parsing (`scripts/humanize.sh:172-177`), while git state detection distinguishes permission errors, rebase, merge, shallow clones, detached HEAD, and normal branch state (`scripts/humanize.sh:122-168`). RLCR monitor handles no log yet, loop dir deletion, session deletion, log deletion, newer session discovery, newer log discovery, and log truncation/rotation (`scripts/humanize.sh:794-907`, `scripts/humanize.sh:961-1064`). Date formatting uses GNU `date -d` and falls back to raw timestamps on platforms without it (`scripts/humanize.sh:424-432`). PR monitor `--once` uses `xargs ls -lt` only when round files exist, avoiding empty xargs in the normal path (`scripts/humanize.sh:1458-1465`). Phase detection uses `/tmp/humanize-phase-<session>-<round>.size` cache in monitor-common, so stale or unwritable `/tmp` can reduce phase fidelity but not block display (`scripts/lib/monitor-common.sh:297-317`).
- validation_or_tests: PR monitor behavior is exercised by `tests/test-pr-loop-hooks.sh`, especially monitor YAML list parsing, configured bot display, and empty active bot display (`tests/test-pr-loop-hooks.sh:1547-1621`). The top-level PR test runner sources these modules via `tests/test-pr-loop.sh:32-34` and runs them at `tests/test-pr-loop.sh:40-47`. Zsh and monitor safety are separately referenced by `tests/test-zsh-monitor-safety.sh` in search results. I did not execute tests because this assignment explicitly requested read-only research notes, and the runner initializes temp/mock test state.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-068 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Executable specification and aggregation runner for the PR loop test suite. It does not implement PR loop behavior directly; it defines the canonical ordering and environment setup for validating PR loop scripts, hooks, and Stop Hook state transitions.
- algorithmic_behavior: The runner enables strict shell mode with `set -euo pipefail` (`tests/test-pr-loop.sh:13`), resolves `SCRIPT_DIR` and `PROJECT_ROOT` (`tests/test-pr-loop.sh:15-16`), sources common helpers and the PR loop library (`tests/test-pr-loop.sh:18-20`), initializes the PR loop test environment (`tests/test-pr-loop.sh:22-27`), sources three test modules (`tests/test-pr-loop.sh:29-34`), runs script, hook, and Stop Hook tests in order (`tests/test-pr-loop.sh:37-47`), then prints the shared summary (`tests/test-pr-loop.sh:49-53`).
- inputs_outputs_state: Inputs are executable test modules under `tests/`, helper functions from `tests/test-helpers.sh`, and mocks from `tests/test-pr-loop-lib.sh`. The test library creates a temporary mock bin dir, prepends it to `PATH`, and installs mock `gh` and `codex` commands (`tests/test-pr-loop-lib.sh:107-121`). Outputs are pass/fail counters and a final nonzero return if `TESTS_FAILED > 0` (`tests/test-pr-loop-lib.sh:127-144`). State flows from environment setup to module sourcing to module-level test execution to summary aggregation.
- gates_or_invariants: Strict mode makes unset variables and failed commands fatal unless individual tests explicitly handle failures. The runner requires the sourced modules to define `run_script_tests`, `run_hook_tests`, and `run_stophook_tests`; missing definitions would fail the run at call time. The shared summary gate returns failure when any test failed (`tests/test-pr-loop-lib.sh:137-143`). Mocked `gh` responses cover auth, repo owner/name, PR number/state/commit timestamp, and API empty arrays (`tests/test-pr-loop-lib.sh:27-87`); mocked `codex` returns successful placeholder output (`tests/test-pr-loop-lib.sh:90-100`).
- dependencies_and_callers: This runner is included in the broader test list via `tests/run-all-tests.sh` search results and coordinates `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh`. The script module covers setup/cancel/fetch/poll validation, the hook module covers state/prompt/comment protection, sorting, dedupe, bot status extraction, trigger detection, approval markers, round correctness, and monitor display, while Stop Hook tests cover force-push rejection, startup case behavior, approval state creation, unpushed commit blocking, missing trigger blocking, bot timeout removal, approvals, fork PR base resolution, and goal tracker mixed approval. The module entry points are visible at `tests/test-pr-loop-scripts.sh:405`, `tests/test-pr-loop-hooks.sh:15`, and `tests/test-pr-loop-stophook.sh:15`.
- edge_cases_or_failure_modes: Because it is an aggregate runner, a setup failure prevents all downstream modules from running. Mock behavior can mask real GitHub/Codex integration failures, so this runner is strongest as local executable specification rather than full end-to-end production proof. It also mutates test temp directories and mock PATH state via `init_pr_loop_test_env`, which is why I did not execute it in this read-only research branch.
- validation_or_tests: The file is itself the validation entry point for PR loop. It validates script argument handling, hook protections, Stop Hook polling and approval transitions, trigger semantics, timeout behavior, fork PR handling, and monitor output indirectly through sourced modules.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-098 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Prompt/block template used as the human-facing denial message when an agent attempts to write to `round-*-prompt.md` files. It defines a write-protection contract: prompt files are instructions to the agent, not mutable work products.
- algorithmic_behavior: The template states that `round-*-prompt.md` files cannot be written (`prompt-template/block/prompt-file-write.md:1-3`), explains that they contain instructions from Codex to Claude (`prompt-template/block/prompt-file-write.md:5`), and redirects the workflow to read the prompt, execute its tasks, and write results to the summary file (`prompt-template/block/prompt-file-write.md:7-10`). It also defines the fallback behavior when prompt contents appear wrong: document the problem in the summary rather than editing the prompt (`prompt-template/block/prompt-file-write.md:12`).
- inputs_outputs_state: Input is a blocked write/edit attempt targeting a round prompt file. Output is a rendered block message. The intended state transition is from invalid prompt mutation attempt back to valid loop execution: prompt remains unchanged, work proceeds from prompt instructions, and output goes to the summary channel/file.
- gates_or_invariants: The invariant is prompt immutability: the agent must not modify its own instructions. The shared message function `prompt_write_blocked_message` loads this template through `load_and_render_safe` in `hooks/lib/loop-common.sh:573-580`. If the template is unavailable, the fallback preserves the same gate: “You cannot write to round-*-prompt.md files.”
- dependencies_and_callers: Referenced by `hooks/lib/loop-common.sh` as a standard blocking message (`hooks/lib/loop-common.sh:573-580`). Template existence is included in cross-reference validation under `COMMON_TEMPLATES` in `tests/test-template-references.sh:152-159`. Search results also show validator scripts use `load_and_render_safe`, meaning missing templates should degrade to fallback text rather than breaking validation.
- edge_cases_or_failure_modes: If a prompt contains erroneous instructions, the template explicitly disallows in-place correction and requires documenting the issue in the summary (`prompt-template/block/prompt-file-write.md:12`). If template loading fails, `load_and_render_safe` fallback still blocks writes. A possible failure mode would be a validator path mismatch that fails to classify a prompt file as protected, but that logic lives outside this template.
- validation_or_tests: `tests/test-template-references.sh` checks that this common template exists (`tests/test-template-references.sh:152-166`) and verifies critical validators use safe template loading rather than unsafe rendering (`tests/test-template-references.sh:172-200`). Hook tests also cover protected PR prompt/comment files and RLCR prompt-style protections through sourced modules.
- skip_candidate: `no`

### DEV_RLCR_WITH_SWARM_TEAM-HZ-128 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 critical requirements template for the “no comments yet” startup path. It defines the initial no-op resolution behavior that lets the Stop Hook begin polling for automatic bot reviews without requiring the agent to comment on the PR.
- algorithmic_behavior: The template instructs the agent to write a resolution summary to `{{RESOLVE_PATH}}` (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-10`), include that Round 0 is awaiting initial bot reviews and has no issues to address (`prompt-template/pr-loop/critical-requirements-no-comments.md:11-14`), then try to exit so the Stop Hook can poll (`prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`). It explicitly prohibits commenting on the PR to trigger review because the bots are expected to review automatically for a new PR (`prompt-template/pr-loop/critical-requirements-no-comments.md:17-18`).
- inputs_outputs_state: Input variables include `RESOLVE_PATH`, rendered by `scripts/setup-pr-loop.sh` with `TEMPLATE_VARS` (`scripts/setup-pr-loop.sh:731-738`). The output is appended to the activation output after the initial prompt. State transition: setup detects no fetched comments, writes `round-0-prompt.md`, emits this critical requirements block, the agent writes `round-0-pr-resolve.md`, and exit triggers Stop Hook polling for first bot reviews.
- gates_or_invariants: The no-comment gate is selected only when `COMMENT_FILE` contains the exact sentinel `*No comments found.*`, setting `COMMENT_COUNT=0` (`scripts/setup-pr-loop.sh:721-729`). In that branch, setup renders `pr-loop/critical-requirements-no-comments.md` with a fallback carrying the same requirements (`scripts/setup-pr-loop.sh:890-915`). The invariant is: do not trigger review manually on a fresh PR; write the resolve summary and let the Stop Hook poll.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe` (`scripts/setup-pr-loop.sh:914`). It depends on the PR loop setup script’s comment-fetching behavior, template loader, Stop Hook polling, and the existence of `round-0-pr-resolve.md` as the expected summary path (`scripts/setup-pr-loop.sh:721`). It pairs with the alternate `critical-requirements-has-comments.md` branch for cases where comments exist (`scripts/setup-pr-loop.sh:915-940`).
- edge_cases_or_failure_modes: The branch depends on the exact “No comments found” sentinel from `fetch-pr-comments.sh`; if that output changes, the setup script may route to the has-comments workflow incorrectly. If the template is missing, the fallback still preserves the required summary and no-comment behavior (`scripts/setup-pr-loop.sh:893-914`). A stale or invalid `RESOLVE_PATH` would cause the Stop Hook to block later because the required summary file would be missing or in the wrong location.
- validation_or_tests: PR loop tests cover no-comment startup and Stop Hook behavior. Relevant test names include `test_stophook_case1_no_trigger_required`, which validates the no-trigger exception for `startup_case=1` and round 0, plus tests for missing resolve files and trigger behavior in `tests/test-pr-loop-stophook.sh` and `tests/test-pr-loop-hooks.sh`. The aggregate runner invokes those modules via `tests/test-pr-loop.sh:32-47`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections present, each with one assigned item heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`