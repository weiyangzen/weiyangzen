# agent_10 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Reusable block-message template catalog for RLCR and PR-loop hook decisions. The directory is flat, contains 32 Markdown fragments, and is loaded by `hooks/lib/template-loader.sh` through `load_template`, `load_and_render`, and `load_and_render_safe` (`hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:170`). These templates are not passive docs: hook validators and stop hooks render them into block reasons and operator instructions.
- algorithmic_behavior: The blocks encode user-facing state-machine feedback for gate failures and required next actions. Major child roles are: state/protected-file mutation blocks (`state-file-modification.md:1`, `finalize-state-file-modification.md:1`, `pr-loop-state-modification.md:1`), path/round invariants (`wrong-round-number.md:1`, `wrong-round-file.md:1`, `wrong-directory-path.md:1`, `wrong-file-location.md:1`, `wrong-summary-location.md:1`), stop-hook gates (`git-not-clean.md:1`, `git-status-failed.md:1`, `large-files.md:1`, `incomplete-todos.md:1`, `work-summary-missing.md:1`), PR-loop review gates (`force-push-detected.md:1`, `no-trigger-comment.md:1`, `claude-eyes-timeout.md:1`), git safety (`git-add-humanize.md:1`, `git-push.md:1`, `unpushed-commits.md:1`), and goal/plan/schema invariants (`goal-tracker-not-initialized.md:1`, `goal-tracker-modification.md:1`, `plan-file-modified.md:1`, `schema-outdated.md:1`).
- inputs_outputs_state: Inputs are uppercase `{{VAR}}` placeholders supplied by callers, for example `{{CURRENT_ROUND}}`, `{{CORRECT_PATH}}`, `{{AHEAD_COUNT}}`, `{{CURRENT_BRANCH}}`, `{{BOT_MENTION_STRING}}`, `{{OLD_COMMIT}}`, and `{{NEW_COMMIT}}`. Outputs are rendered Markdown strings that become stderr validation messages or JSON hook `reason` bodies. The templates themselves do not mutate state, but they are coupled to state transitions such as `state.md` protection, force-push trigger reset, missing summary blocks, unpushed-commit exit blocks, and PR trigger/eyes checks.
- gates_or_invariants: Template syntax is constrained by `template-loader.sh` comments to uppercase letters, numbers, and underscores in `{{VARIABLE_NAME}}` form, with single-pass substitution and unreplaced placeholders preserved (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:50`). `validate_template_dir` requires sibling directories `block`, `codex`, and `claude` (`hooks/lib/template-loader.sh:208`). Several blocks define hard invariants: do not mutate `state.md`, `finalize-state.md`, generated prompt/comment files, plan backups, `.humanize/`, or wrong-round files.
- dependencies_and_callers: Called heavily by `hooks/lib/loop-common.sh` standard message functions (`hooks/lib/loop-common.sh:451`, `hooks/lib/loop-common.sh:460`, `hooks/lib/loop-common.sh:469`, `hooks/lib/loop-common.sh:895`, `hooks/lib/loop-common.sh:1034`, `hooks/lib/loop-common.sh:1104`), RLCR stop hook (`hooks/loop-codex-stop-hook.sh:590`), PR stop hook (`hooks/pr-loop-stop-hook.sh:355`, `hooks/pr-loop-stop-hook.sh:414`, `hooks/pr-loop-stop-hook.sh:739`, `hooks/pr-loop-stop-hook.sh:784`), read/write/edit/bash validators, and comprehensive template tests. Parent/sibling coordination is through `prompt-template/codex`, `prompt-template/claude`, and `hooks/lib/template-loader.sh`; this directory supplies short block reasons while sibling directories supply full prompts.
- edge_cases_or_failure_modes: Missing or empty templates fall back to inline messages via `load_and_render_safe` (`hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:192`). Placeholder injection is intentionally limited by single-pass rendering, so values containing `{{OTHER}}` are not recursively expanded. If a template is syntactically malformed, callers may still render it with unreplaced text unless tests catch it. Some templates show shell commands using raw placeholder values, so safety depends on caller-side validation rather than template escaping.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads every Markdown template recursively (`tests/test-templates-comprehensive.sh:90`), validates placeholder syntax (`tests/test-templates-comprehensive.sh:118`), tests malformed placeholder detection (`tests/test-templates-comprehensive.sh:204`), renders all templates with dummy values (`tests/test-templates-comprehensive.sh:562`), and specifically renders real block templates including `block/unpushed-commits.md` (`tests/test-templates-comprehensive.sh:517`). `tests/test-template-references.sh` additionally checks template references and safe rendering usage.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap/runtime setup script for `/humanize:start-pr-loop`. It transitions a repository from no active PR loop into an initialized `.humanize/pr-loop/<timestamp>/` session with PR metadata, fetched review comments, a goal tracker, and the first Claude-facing prompt.
- algorithmic_behavior: Parses CLI flags `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout` (`scripts/setup-pr-loop.sh:110`). It enforces at least one monitored bot (`scripts/setup-pr-loop.sh:179`), builds `ACTIVE_BOTS_ARRAY` (`scripts/setup-pr-loop.sh:196`), blocks concurrent RLCR/PR loops (`scripts/setup-pr-loop.sh:220`), validates git/gh/codex prerequisites (`scripts/setup-pr-loop.sh:243`), resolves the current branch and PR including fork-parent lookup (`scripts/setup-pr-loop.sh:283`, `scripts/setup-pr-loop.sh:295`, `scripts/setup-pr-loop.sh:334`), rejects merged/closed PRs (`scripts/setup-pr-loop.sh:365`), validates YAML-safe branch/model/effort fields (`scripts/setup-pr-loop.sh:384`), fetches initial comments (`scripts/setup-pr-loop.sh:430`), classifies startup case through `check-pr-reviewer-status.sh` (`scripts/setup-pr-loop.sh:437`), handles Case 4/5 re-review trigger creation/reuse (`scripts/setup-pr-loop.sh:462`), creates `state.md` (`scripts/setup-pr-loop.sh:615`), creates `goal-tracker.md` (`scripts/setup-pr-loop.sh:641`), and writes `round-0-prompt.md` from templates plus fetched comments (`scripts/setup-pr-loop.sh:721`).
- inputs_outputs_state: Inputs include CLI flags, `CLAUDE_PROJECT_DIR`, optional `TEMPLATE_DIR`, current git branch/HEAD, GitHub CLI auth/repo/PR state, bot comment/review data, and template files. Outputs are `.humanize/pr-loop/<timestamp>/state.md`, `round-0-pr-comment.md`, `goal-tracker.md`, `round-0-prompt.md`, and stdout activation/prompt text. State fields include `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, Codex config, poll timings, `startup_case`, latest commit info, `last_trigger_at`, and `trigger_comment_id` (`scripts/setup-pr-loop.sh:615`).
- gates_or_invariants: One loop at a time is required (`scripts/setup-pr-loop.sh:220`). PR number must be numeric (`scripts/setup-pr-loop.sh:359`). Branch names may not contain YAML-unsafe `: # " ' \`` or newlines (`scripts/setup-pr-loop.sh:384`); Codex model/effort are restricted to safe character classes (`scripts/setup-pr-loop.sh:393`). Case 4/5 trigger matching requires all active bot mentions, strips code/quote contexts before matching, and reuses existing valid trigger comments instead of spamming (`scripts/setup-pr-loop.sh:470`, `scripts/setup-pr-loop.sh:481`). Claude mode requires a trigger comment ID and an eyes reaction; failures clean up the loop directory (`scripts/setup-pr-loop.sh:564`, `scripts/setup-pr-loop.sh:580`, `scripts/setup-pr-loop.sh:587`).
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` and `hooks/lib/template-loader.sh` (`scripts/setup-pr-loop.sh:33`, `scripts/setup-pr-loop.sh:37`), then `hooks/lib/loop-common.sh` for active-loop discovery and bot mention/YAML helpers (`scripts/setup-pr-loop.sh:210`). Calls `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:587`). Depends on `git`, `gh`, `codex`, `jq`, and templates under `prompt-template/pr-loop`. The resulting state is consumed by `hooks/pr-loop-stop-hook.sh`, which parses the same YAML-list fields (`hooks/pr-loop-stop-hook.sh:87`) and finds active sessions through `find_active_pr_loop` (`hooks/lib/loop-common.sh:801`).
- edge_cases_or_failure_modes: Missing bot flag, unknown option, missing option value, and nonnumeric max/timeout exit early (`scripts/setup-pr-loop.sh:123`, `scripts/setup-pr-loop.sh:150`, `scripts/setup-pr-loop.sh:162`). Missing git repo, no commit, missing gh auth, missing codex, no PR, invalid PR, merged/closed PR all fail closed. Fork PR support avoids using fork-local PR numbers by resolving base repo. If reviewer-status lookup fails, it defaults to Case 1 (`scripts/setup-pr-loop.sh:437`). If GitHub timestamp lookup after posting a trigger fails, it intentionally avoids local-time fallback to prevent clock-skew filtering bugs (`scripts/setup-pr-loop.sh:558`). Comment existence is binary via the `*No comments found.*` sentinel (`scripts/setup-pr-loop.sh:723`). A SIGPIPE trap exits cleanly if stdout consumer closes (`scripts/setup-pr-loop.sh:857`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help, missing/invalid bot flags, both bot flags, `--max`, `--codex-model`, and `--codex-timeout` parsing (`tests/test-pr-loop-scripts.sh:27`, `tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:66`, `tests/test-pr-loop-scripts.sh:104`, `tests/test-pr-loop-scripts.sh:131`). `tests/robustness/test-setup-scripts-robustness.sh` covers PR-loop help, missing bot, unknown option, nonnumeric `--max`, and non-git rejection (`tests/robustness/test-setup-scripts-robustness.sh:475`). `tests/test-pr-loop-system.sh` checks Case 4/5 Claude trigger-ID failure cleanup and goal-tracker creation integration (`tests/test-pr-loop-system.sh:1384`, `tests/test-pr-loop-system.sh:1453`). Not executed in this research pass.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template subsystem used by hook and setup scripts. It verifies that all prompt templates can be found, loaded, syntax-checked, rendered, and safely fallen back when missing or empty.
- algorithmic_behavior: Initializes counters and resolves `TEMPLATE_DIR` through `get_template_dir` (`tests/test-templates-comprehensive.sh:59`). Section 1 requires the template root and `block`, `codex`, `claude` subdirectories (`tests/test-templates-comprehensive.sh:66`, `tests/test-templates-comprehensive.sh:74`). Section 2 loads every Markdown template with `find ... -print0 | sort -z` and rejects empty load results (`tests/test-templates-comprehensive.sh:90`). Section 3 scans placeholder syntax for extra braces, wrong brackets, unclosed placeholders, spaced placeholders, and lowercase placeholders (`tests/test-templates-comprehensive.sh:118`). Section 3.5 creates malformed sample templates to prove the regex checks detect representative bad patterns (`tests/test-templates-comprehensive.sh:204`). Section 4 exercises rendering edge cases such as empty content, multiline content, blank lines, regex-special characters, backslashes, quotes, markdown, long values, multiline values, repeated variables, and adjacent variables (`tests/test-templates-comprehensive.sh:266`). Section 5 tests `load_and_render_safe` fallback and `validate_template_dir` (`tests/test-templates-comprehensive.sh:443`). Sections 6 and 7 render real templates and stress rapid/all-template rendering (`tests/test-templates-comprehensive.sh:493`, `tests/test-templates-comprehensive.sh:543`).
- inputs_outputs_state: Inputs are the repository template tree and `hooks/lib/template-loader.sh`. Temporary directories/files are created with `mktemp -d` for malformed and incomplete-template tests (`tests/test-templates-comprehensive.sh:205`, `tests/test-templates-comprehensive.sh:484`). Outputs are colored PASS/FAIL/WARN lines, aggregate counters, and process exit code 0 when `TESTS_FAILED` is zero, otherwise 1 (`tests/test-templates-comprehensive.sh:611`).
- gates_or_invariants: Valid templates must load to non-whitespace content, use uppercase `{{VAR}}` placeholder syntax, survive rendering with arbitrary values, and avoid leaving placeholders after dummy full-template rendering when all placeholder names are provided (`tests/test-templates-comprehensive.sh:590`). `validate_template_dir` must reject nonexistent or incomplete directories (`tests/test-templates-comprehensive.sh:474`, `tests/test-templates-comprehensive.sh:482`). Real-template checks assert rendered semantic markers, for example `block/unpushed-commits.md` must include “Unpushed Commits”, the count, and branch name (`tests/test-templates-comprehensive.sh:517`).
- dependencies_and_callers: Sources `hooks/lib/template-loader.sh` (`tests/test-templates-comprehensive.sh:17`) and depends on POSIX-ish shell tools `find`, `sort`, `grep`, `sed`, `tr`, `wc`, `mktemp`, and Bash arrays/process substitution. It is a likely CI/run-all-tests validation asset; it covers the same APIs used by setup scripts, stop hooks, and validators.
- edge_cases_or_failure_modes: The script uses `set -uo pipefail` rather than `set -e`, so it accumulates failures instead of aborting on first failed assertion (`tests/test-templates-comprehensive.sh:13`). Some syntax regexes are heuristic and may warn or false-positive on template documentation examples; the single-brace check is explicitly skipped as too noisy (`tests/test-templates-comprehensive.sh:160`). Rendering tests for backslashes, quotes, CJK-like content, and multiline values mainly assert no crash or expected marker presence, not byte-perfect preservation in all cases (`tests/test-templates-comprehensive.sh:328`, `tests/test-templates-comprehensive.sh:339`, `tests/test-templates-comprehensive.sh:348`, `tests/test-templates-comprehensive.sh:392`).
- validation_or_tests: This file is itself the validation surface. It also overlaps with `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and robustness template tests that exercise `load_and_render_safe`, missing templates, special names, concurrency, and larger stress cases. Not executed in this research pass.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-gate prompt block rendered when local commits must be pushed before a loop may exit. It applies to RLCR when push-every-round is enabled and to PR-loop when bot review depends on remote-visible commits.
- algorithmic_behavior: Renders a title, count/branch-specific explanation, condition text, required `git push origin {{CURRENT_BRANCH}}` action, and retry instruction (`prompt-template/block/unpushed-commits.md:1`). It does not compute ahead counts itself; it is the contract text attached to the hook block.
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH` placeholders (`prompt-template/block/unpushed-commits.md:3`, `prompt-template/block/unpushed-commits.md:9`). Output is Markdown used as a hook block `reason`. The effective state transition is “exit attempt blocked until remote push makes ahead count zero”; after push, the same stop hook can be attempted again (`prompt-template/block/unpushed-commits.md:12`).
- gates_or_invariants: The template encodes the invariant that unpushed commits are incompatible with loop exit when the relevant push policy is active (`prompt-template/block/unpushed-commits.md:5`). It assumes caller-side detection and validation of branch/count; it only displays the command.
- dependencies_and_callers: RLCR stop hook renders it when `PUSH_EVERY_ROUND` is true and `git status -sb` reports ahead commits (`hooks/loop-codex-stop-hook.sh:578`, `hooks/loop-codex-stop-hook.sh:590`). PR stop hook renders it when `AHEAD_COUNT` is positive, including fail-closed paths where PR head cannot be fetched (`hooks/pr-loop-stop-hook.sh:333`, `hooks/pr-loop-stop-hook.sh:349`, `hooks/pr-loop-stop-hook.sh:355`). Template-loader performs substitution. `tests/test-templates-comprehensive.sh` validates this exact real template (`tests/test-templates-comprehensive.sh:517`).
- edge_cases_or_failure_modes: If branch names contain shell-sensitive characters, the displayed command is not escaped by the template; safety depends on setup/state validation and user judgment. In PR-loop, missing upstream or missing remote branch falls back to comparing against PR `headRefOid`; if that cannot be fetched, the caller assumes one unpushed commit for safety (`hooks/pr-loop-stop-hook.sh:323`, `hooks/pr-loop-stop-hook.sh:333`). If the template is missing, callers have inline fallback messages.
- validation_or_tests: Real-template rendering check asserts the title, numeric count, and branch placeholder replacement (`tests/test-templates-comprehensive.sh:517`). Hook behavior is indirectly covered by PR/RLCR stop-hook tests and template-loader tests. Not executed in this research pass.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-130 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for secure cancellation of active loop state. It protects the narrow authorized state transition from `state.md` or `finalize-state.md` to `cancel-state.md` while rejecting command-injection and path-bypass attempts.
- algorithmic_behavior: Sets up a mock loop directory with `state.md` (`tests/robustness/test-cancel-security-robustness.sh:27`), then calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` for positive and negative command strings. Positive cases require `.cancel-requested` and accept `state.md`, `finalize-state.md`, single quotes, and unquoted paths (`tests/robustness/test-cancel-security-robustness.sh:39`, `tests/robustness/test-cancel-security-robustness.sh:53`, `tests/robustness/test-cancel-security-robustness.sh:65`, `tests/robustness/test-cancel-security-robustness.sh:78`). Negative cases cover missing signal, command substitution, backticks, semicolon/AND/OR/pipe chaining, wrong source/destination, extra args, newline injection, variable expansion, non-`mv`, mixed quotes, trailing spaces, nonstandard source name, symlink source files, and paths containing `finalize` (`tests/robustness/test-cancel-security-robustness.sh:99`, `tests/robustness/test-cancel-security-robustness.sh:109`, `tests/robustness/test-cancel-security-robustness.sh:135`, `tests/robustness/test-cancel-security-robustness.sh:174`, `tests/robustness/test-cancel-security-robustness.sh:213`, `tests/robustness/test-cancel-security-robustness.sh:278`, `tests/robustness/test-cancel-security-robustness.sh:317`, `tests/robustness/test-cancel-security-robustness.sh:369`).
- inputs_outputs_state: Inputs are synthetic filesystem state under `TEST_DIR`, a `.cancel-requested` signal file, command strings normalized to lowercase, and `loop-common.sh`. Outputs are PASS/FAIL counters via `test-helpers.sh`, plus process exit from `print_test_summary` (`tests/robustness/test-cancel-security-robustness.sh:411`). The test mutates only temporary files: creates/removes signal files, creates/restores `state.md`, creates symlinks, and removes tricky test directories.
- gates_or_invariants: The underlying invariant from `is_cancel_authorized` is: signal file must exist, command must be exactly an `mv` from normalized loop-local `state.md` or `finalize-state.md` to loop-local `cancel-state.md`, no chaining/substitution/remaining variables/mixed quote styles/extra arguments are allowed, and the source must not be a symlink (`hooks/lib/loop-common.sh:557`, `hooks/lib/loop-common.sh:563`, `hooks/lib/loop-common.sh:568`, `hooks/lib/loop-common.sh:602`, `hooks/lib/loop-common.sh:681`, `hooks/lib/loop-common.sh:687`, `hooks/lib/loop-common.sh:702`). The cancel exception is what lets validators distinguish legitimate cancellation from state-file tampering.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-cancel-security-robustness.sh:17`). It validates behavior used by `hooks/loop-bash-validator.sh`, which checks `is_cancel_authorized` before blocking writes/moves involving `state.md` or `finalize-state.md` (`hooks/loop-bash-validator.sh:139`, `hooks/loop-bash-validator.sh:149`, `hooks/loop-bash-validator.sh:286`, `hooks/loop-bash-validator.sh:309`). Runtime callers include `scripts/cancel-rlcr-loop.sh` and `scripts/cancel-pr-loop.sh`, which create `.cancel-requested` and rename state files.
- edge_cases_or_failure_modes: The tests explicitly guard against historical substring confusion where a loop directory containing `finalize` could cause `state.md` symlink checks to be misclassified (`tests/robustness/test-cancel-security-robustness.sh:369`). Mixed quote styles and multiple trailing spaces are rejected even if otherwise harmless, favoring strict parsing over permissiveness (`tests/robustness/test-cancel-security-robustness.sh:278`, `tests/robustness/test-cancel-security-robustness.sh:304`). Path normalization accepts `/./` but not arbitrary alternate paths (`tests/robustness/test-cancel-security-robustness.sh:265`). The parser is command-string based, so it intentionally rejects many shell features instead of attempting full shell parsing.
- validation_or_tests: This file is the validation suite for cancel authorization. It ends with `print_test_summary` and exits with that status (`tests/robustness/test-cancel-security-robustness.sh:411`). Not executed in this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique assigned item headings above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`