# agent_25 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-025 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash hook validator for RLCR and PR loop protected state. It is registered for the Bash matcher in `hooks/hooks.json:42-48` and enforces that shell commands cannot bypass Write/Edit/read-only loop contracts.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON and nesting, exits early for non-Bash tools, then extracts `.tool_input.command` (`hooks/loop-bash-validator.sh:24-48`). It discovers the active RLCR loop under `.humanize/rlcr` and active PR loop under `.humanize/pr-loop` (`hooks/loop-bash-validator.sh:54-63`). With no active loop it allows the command (`hooks/loop-bash-validator.sh:64-67`). With an RLCR loop it parses `state.md` or `finalize-state.md` strictly, derives `current_round` and `push_every_round`, blocks `git push` unless `push_every_round: true`, blocks `.humanize` git adds, and blocks Bash mutations to state, finalize-state, plan backup, goal tracker, prompts, summaries, and most todo paths (`hooks/loop-bash-validator.sh:74-395`). With a PR loop it blocks PR loop state/read-only round files (`hooks/loop-bash-validator.sh:403-443`).
- inputs_outputs_state: Input is the hook JSON payload plus environment `CLAUDE_PROJECT_DIR` or cwd for project root (`hooks/loop-bash-validator.sh:24-55`). Output is no stdout on allow, rendered denial messages on stderr for blocked commands, and exit code `0` allow, `1` invalid input/malformed state, `2` blocked operation. It reads active loop state via `parse_state_file_strict`; it does not mutate files itself.
- gates_or_invariants: Fail-closed JSON and state validation uses `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, and `parse_state_file_strict` (`hooks/lib/loop-common.sh:60-134`, `hooks/lib/loop-common.sh:250-335`). State file protection checks `finalize-state.md` before `state.md` because the latter pattern also matches the former (`hooks/loop-bash-validator.sh:131-145`). Bash source-move/copy bypasses are handled by splitting chained commands and stripping leading redirections before matching `mv|cp ... state.md` as a source (`hooks/loop-bash-validator.sh:169-284`). Shell wrapper bypasses like `sh -c 'mv state.md ...'` are separately blocked (`hooks/loop-bash-validator.sh:306-328`). Only active loop `round-1-todos.md`/`round-2-todos.md` paths are conditionally permitted; same basename elsewhere is blocked by requiring the active loop dir prefix (`hooks/loop-bash-validator.sh:385-392`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-bash-validator.sh:16-18`), which in turn sources the template loader and resolves `prompt-template` (`hooks/lib/loop-common.sh:140-149`). Uses rendered block templates through `load_and_render_safe`, including `block/goal-tracker-bash-write.md` for round-0 goal tracker Bash edits (`hooks/lib/loop-common.sh:499-507`). Shared helpers define git-add blocking (`hooks/lib/loop-common.sh:936-1032`) and generic command modification detection for redirection, tee, sed -i, awk/perl in-place, mv/cp, rm, dd, truncate, printf redirection, and exec redirection (`hooks/lib/loop-common.sh:1077-1102`). Tests call this validator from allowlist, finalize, PR loop, plan-file, cancel, and robustness suites, e.g. `tests/robustness/test-hook-system-robustness.sh` references it in multiple cases and `tests/test-pr-loop-hooks.sh` invokes it for PR loop protection.
- edge_cases_or_failure_modes: Malformed JSON, missing Bash command, over-deep JSON, malformed state frontmatter, missing required state fields, invalid `review_started`, and nonnumeric rounds all block/fail closed through common helpers (`hooks/lib/loop-common.sh:60-134`, `hooks/lib/loop-common.sh:250-335`). Cancel-state renames are the only authorized state-file exception, gated on `.cancel-requested`, exact `mv` structure, no command substitution, no shell chaining, no mixed quotes, exact source/destination paths, and no symlink source (`hooks/lib/loop-common.sh:557-716`). The command parser is regex based, so exotic shell syntax beyond covered redirections/wrappers may be residual risk; the file explicitly expands common bypass handling for chained operators and redirections (`hooks/loop-bash-validator.sh:169-207`).
- validation_or_tests: Direct coverage appears in `tests/test-allowlist-validators.sh`, `tests/test-finalize-phase.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-cancel-signal-file.sh`, `tests/test-plan-file-hooks.sh`, and robustness hook input/system tests found by repository references. Template reference coverage includes `tests/test-template-references.sh`, which lists `hooks/loop-bash-validator.sh` and `block/goal-tracker-bash-write.md`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-055 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared executable-test harness utilities for shell test suites. It is not the production RLCR algorithm, but it standardizes pass/fail accounting and temporary repository setup for core algorithm specifications.
- algorithmic_behavior: Defines ANSI color constants and global counters (`tests/test-helpers.sh:13-24`), then exposes `pass`, `fail`, and `skip` functions that print result lines and increment counters (`tests/test-helpers.sh:30-52`). `print_test_summary` prints totals and returns success only when `TESTS_FAILED` is zero (`tests/test-helpers.sh:58-78`). `setup_test_dir` creates a temp directory and installs an EXIT cleanup trap (`tests/test-helpers.sh:84-89`). `init_test_git_repo` creates a minimal git repo, configures identity and no GPG signing, commits `file.txt`, then returns to the prior directory (`tests/test-helpers.sh:91-105`).
- inputs_outputs_state: Inputs are function arguments such as test names, expected/got values, temp directory target, and repo target directory. Outputs are human-readable test logs and process return codes from `print_test_summary`; state is held in `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, and `TEST_DIR`. It may create and remove temp directories outside the repo when sourced by tests.
- gates_or_invariants: A test script’s final success is determined solely by `TESTS_FAILED == 0` (`tests/test-helpers.sh:71-77`). `fail` never exits immediately; it accumulates failures for summary reporting (`tests/test-helpers.sh:35-44`). `setup_test_dir` registers one cleanup trap, so later trap overrides in caller scripts could affect cleanup if not composed.
- dependencies_and_callers: No external library dependencies beyond shell, `mktemp`, and git for repo initialization. It is sourced from top-level tests as `source "$SCRIPT_DIR/test-helpers.sh"` and robustness tests as `source "$SCRIPT_DIR/../test-helpers.sh"` per file comments (`tests/test-helpers.sh:5-6`). Repository references show broad use across PR loop, robustness, path validation, state transition, git operations, setup, hook input, and timeout tests.
- edge_cases_or_failure_modes: `echo -e` color rendering can vary by shell, though scripts use bash. `init_test_git_repo` changes directories and relies on `cd -`; with `set -e` in callers, git failures abort the caller rather than register through `fail`. The cleanup trap removes `$TEST_DIR` without shell-quoting in the trap string (`tests/test-helpers.sh:87-88`); `mktemp -d` paths generally avoid special characters, but this is a shell-safety assumption.
- validation_or_tests: This file is itself support code rather than a tested target. Its behavior is indirectly exercised by every test suite that sources it, including the assigned timeout robustness test (`tests/robustness/test-timeout-robustness.sh:16-18`) and the run-all harness list (`tests/run-all-tests.sh:55-72`).
- skip_candidate: `yes: support harness rather than production algorithm, but it is still core executable-spec infrastructure used by the assigned and sibling tests`

### ADD_CAREFUL_MODE_V1_6_5-HZ-085 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Denial-message template for the RLCR gate that blocks Bash-based `goal-tracker.md` mutation in round 0 and redirects the agent to Write/Edit tooling.
- algorithmic_behavior: Provides a rendered Markdown block headed “Bash Write Blocked: Use Write or Edit Tool” and instructs not to use Bash to modify `goal-tracker.md` (`prompt-template/block/goal-tracker-bash-write.md:1-4`). It renders the `{{CORRECT_PATH}}` placeholder into the target goal tracker path (`prompt-template/block/goal-tracker-bash-write.md:5`). It explains the policy reason: Bash commands bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:7-8`).
- inputs_outputs_state: Input is template variable `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message "$GOAL_TRACKER_PATH"` in the Bash validator path (`hooks/loop-bash-validator.sh:349-353`) through `load_and_render_safe` (`hooks/lib/loop-common.sh:499-507`). Output is the stderr block message returned to the blocked Bash tool. It carries no persistent state.
- gates_or_invariants: Applies only when `command_modifies_file "$COMMAND_LOWER" "goal-tracker\.md"` matches and `CURRENT_ROUND == 0` (`hooks/loop-bash-validator.sh:349-357`). For rounds greater than 0, the system switches to a different template/message requiring a Goal Tracker Update Request in the summary (`hooks/loop-bash-validator.sh:353-356`, `hooks/lib/loop-common.sh:1105-1116`). The template renderer is single-pass, so placeholder-like text in variable values is not recursively expanded (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-132`).
- dependencies_and_callers: Called by `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh:499-507`; that function is called by `hooks/loop-bash-validator.sh:349-353`. Template existence is included in template reference tests according to repository references.
- edge_cases_or_failure_modes: If this template is missing or empty, the common helper falls back to an inline message with the same `CORRECT_PATH` variable (`hooks/lib/loop-common.sh:500-507`, `hooks/lib/template-loader.sh:170-203`). If `CORRECT_PATH` is unexpectedly empty, the rendered path guidance becomes weak, but the block decision has already occurred in the validator.
- validation_or_tests: Covered indirectly when Bash goal-tracker edits are blocked. `tests/test-template-references.sh` references `block/goal-tracker-bash-write.md`, and robustness goal-tracker/hook-system tests exercise the gate path by invoking `loop-bash-validator.sh`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-115 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Review-phase transition prompt for RLCR. It defines the contract when Codex review finds `[P0-9]` issues: the agent must fix review findings, commit, write a summary, and continue until review passes.
- algorithmic_behavior: The template states that the loop is in Review Phase and inserts `{{REVIEW_CONTENT}}` under Review Results (`prompt-template/claude/review-phase-prompt.md:1-8`). It instructs the agent to address all severity-marked issues, avoid unrelated changes, commit fixes, and write the summary to `{{SUMMARY_FILE}}` (`prompt-template/claude/review-phase-prompt.md:9-15`). It defines expected summary content and states that COMPLETE has no effect during review phase; progress requires code review findings to be resolved (`prompt-template/claude/review-phase-prompt.md:16-28`).
- inputs_outputs_state: Inputs are `REVIEW_CONTENT` and `SUMMARY_FILE`, rendered by `continue_review_loop_with_issues` (`hooks/loop-codex-stop-hook.sh:1118-1152`). Output is written to `$LOOP_DIR/round-${round}-prompt.md` for the next round and included as the stop-hook block reason/system prompt (`hooks/loop-codex-stop-hook.sh:1131-1160`). State transition side effects occur in the caller: it increments `current_round` in `state.md` before creating this prompt (`hooks/loop-codex-stop-hook.sh:1126-1133`).
- gates_or_invariants: Review phase starts when implementation output’s last non-empty line is exactly `COMPLETE`, `review_started` was false, and base branch exists; the stop hook sets `review_started: true`, touches `.review-phase-started`, then runs Codex review (`hooks/loop-codex-stop-hook.sh:1396-1435`). During review phase, COMPLETE is ignored and Codex review determines exit (`hooks/loop-codex-stop-hook.sh:1397-1401`). A missing `.review-phase-started` marker with `review_started=true` blocks as a state inconsistency (`hooks/loop-codex-stop-hook.sh:1448-1464`). Review issues are detected by scanning the Codex review log for `[P?]` markers in the first 10 characters of a line and extracting from first issue onward (`hooks/lib/loop-common.sh:338-398`).
- dependencies_and_callers: Rendered through `load_and_render_safe` in `continue_review_loop_with_issues` (`hooks/loop-codex-stop-hook.sh:1150-1152`). The review driver runs `codex review --base "$review_base"` under `run_with_timeout` and stores debug/log artifacts (`hooks/loop-codex-stop-hook.sh:918-985`). `run_and_handle_code_review` routes outcomes: command failure blocks, issue detection continues review loop with this template, no issues enters finalize (`hooks/loop-codex-stop-hook.sh:990-1029`).
- edge_cases_or_failure_modes: If Codex review fails or produces no usable log content, the hook blocks rather than finalizing (`hooks/loop-codex-stop-hook.sh:1007-1024`). If the template is missing, the stop hook has an inline fallback prompt with equivalent placeholders (`hooks/loop-codex-stop-hook.sh:1135-1152`). Since `REVIEW_CONTENT` may contain `{{...}}`, single-pass rendering prevents accidental secondary substitution (`hooks/lib/template-loader.sh:54-58`).
- validation_or_tests: Review issue extraction is tested by `tests/test-codex-review-merge.sh` per repository references, covering marker location, empty/missing logs, long logs, extraction boundaries, and result-file creation. Finalize/review blocking paths are referenced in `tests/test-finalize-phase.sh`, including review failure and empty output cases.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-145 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout layer used by setup hooks, stop hooks, PR scripts, and GitHub API polling. It validates that bounded external commands preserve expected exit behavior and do not hang the RLCR/PR control loop.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-18`). It verifies timeout implementation detection, accepted implementation names, quick command success, timeout exit code `124`, argument passing, successful nonzero exit-code preservation, pipeline/subshell execution through `sh -c`, very short timeout elapsed time, zero timeout behavior, large output handling, rapid cycles, missing command failure, empty command handling, special-character argument preservation, signal-handling behavior, fallback-chain detection, and `TIMEOUT_IMPL` export (`tests/robustness/test-timeout-robustness.sh:31-272`). It exits with `print_test_summary` result (`tests/robustness/test-timeout-robustness.sh:274-279`).
- inputs_outputs_state: Inputs are the local environment’s available timeout binaries/interpreters and command arguments passed to `run_with_timeout`. Output is a pass/fail summary through test helpers and process exit status. State is only test counters plus `TIMEOUT_IMPL` sourced from `portable-timeout.sh`.
- gates_or_invariants: `detect_timeout_impl` priority is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:6-27`). `run_with_timeout` must return GNU-compatible `124` for Python timeout fallback and preserve child return codes for normal completion (`scripts/portable-timeout.sh:33-71`). Tests accept `none` as a valid implementation but skip timeout-enforcement assertions when no implementation exists (`tests/robustness/test-timeout-robustness.sh:42-49`, `tests/robustness/test-timeout-robustness.sh:62-77`, `tests/robustness/test-timeout-robustness.sh:120-155`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` and `tests/test-helpers.sh` (`tests/robustness/test-timeout-robustness.sh:16-17`). The timeout wrapper is used broadly: `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and `hooks/loop-plan-file-validator.sh` all call `run_with_timeout` per repository reference search. This test is included in the master test list at `tests/run-all-tests.sh:55-66`.
- edge_cases_or_failure_modes: The Python fallback uses `subprocess.run(sys.argv[1:], timeout=...)` and does not capture child stdout/stderr itself, allowing normal streaming but relying on Python timeout semantics (`scripts/portable-timeout.sh:47-61`). Zero timeout is intentionally platform-variable and accepts either success or `124` (`tests/robustness/test-timeout-robustness.sh:139-152`). Signal-handling test accepts either `0` or `124` because implementations differ when a child traps TERM (`tests/robustness/test-timeout-robustness.sh:221-233`). The `none` fallback runs without timeout and warns, so callers remain vulnerable to hangs on systems without `gtimeout`, GNU `timeout`, or Python (`scripts/portable-timeout.sh:64-69`).
- validation_or_tests: This file is itself the validation suite; it is run by `tests/run-all-tests.sh` as `robustness/test-timeout-robustness.sh` (`tests/run-all-tests.sh:55-66`). I did not execute it because this worker was instructed to produce research notes only and not modify files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item-evidence headings, matching the 5 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`