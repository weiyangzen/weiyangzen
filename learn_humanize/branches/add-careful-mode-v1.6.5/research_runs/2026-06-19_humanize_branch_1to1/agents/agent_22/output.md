# agent_22 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is a command wrapper around `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` and documents the loop contract, allowed entry arguments, stop conditions, and the two-phase implementation/review algorithm.
- algorithmic_behavior: Lines 12-14 delegate all runtime setup to `scripts/setup-rlcr-loop.sh`. Lines 16-25 define the main loop: implement plan, write summary, Codex reviews, continue on findings, enter Review Phase on `COMPLETE`, run `codex review --base <branch>`, fix `[P0-9]` issues, then finalize when no issues remain. Lines 27-45 define the Goal Tracker anti-drift mechanism with immutable acceptance criteria plus mutable task/evolution/deferred state. Lines 69-86 define `--skip-impl`, which bypasses implementation and starts directly in code review mode.
- inputs_outputs_state: Inputs are the plan file or `--plan-file`, loop controls such as `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, and `--ask-codex-question` from line 3. Output is an initialized `.humanize/rlcr/<timestamp>/` loop state generated by the setup script, including prompts, summaries, state frontmatter, plan backup, and goal tracker. State transitions documented here are implementation phase -> review phase -> finalize phase, or implementation phase skipped directly to review phase.
- gates_or_invariants: The command permits only one setup tool invocation via frontmatter `allowed-tools` at lines 3-5. Important behavioral invariants are: summaries must be written before exit, `goal-tracker.md` must stay current, changed files/tests must be reported, loop state/cancel commands must not be used to cheat, and Codex feedback is authoritative for continuation, lines 46-53. Stop gates are max iteration count, Codex `COMPLETE` plus clean review, or explicit cancel, lines 54-58.
- dependencies_and_callers: Direct dependency is `scripts/setup-rlcr-loop.sh`, which parses these command options, validates git/plan prerequisites, writes state frontmatter, copies or synthesizes plan content, and creates initial prompt/goal tracker files. Related stop enforcement occurs in `hooks/loop-codex-stop-hook.sh`, and shared state parsing lives in `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: `--skip-impl` makes plan optional and disables goal tracker initialization, lines 77-81. Base branch is auto-detected if omitted, with priority documented at line 67. Full alignment checks are interval-based and configurable with a minimum documented at line 39. The command doc itself does not validate arguments; validation is delegated to the setup script.
- validation_or_tests: Behavior is indirectly specified by robustness tests for state parsing/discovery in `tests/robustness/test-state-transition-robustness.sh` and setup validation in `scripts/setup-rlcr-loop.sh`. Relevant setup lines include option parsing for `--ask-codex-question` at `scripts/setup-rlcr-loop.sh:211` and state write of `ask_codex_question` at `scripts/setup-rlcr-loop.sh:731`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt template loader’s failure tolerance, fallback rendering, and strict-mode safety. This is core-adjacent because templates define hook block messages and algorithmic prompts, while the loader’s error behavior determines whether hooks block cleanly or crash.
- algorithmic_behavior: Lines 11-15 locate project root, source `hooks/lib/template-loader.sh`, and resolve `prompt-template`. The script runs 12 numbered cases. Tests 1-4 verify missing files/directories and empty content return empty output with exit 0. Test 5 verifies values containing regex-like characters render literally. Test 6 verifies `load_and_render` does not abort under `set -euo pipefail`. Tests 7-8 verify `load_and_render_safe` fallback behavior and variable substitution. Tests 9-10 validate template directory acceptance/rejection. Tests 11-12 verify malformed placeholders remain unchanged.
- inputs_outputs_state: Inputs are template directory paths, template names, fallback strings, and `VAR=value` assignments. Outputs are colored PASS/FAIL lines and process exit 0 when `TESTS_FAILED == 0`, nonzero otherwise, lines 218-225. State is local counters `TESTS_PASSED` and `TESTS_FAILED`, lines 22-35.
- gates_or_invariants: Missing templates must not crash hooks and must return empty for unsafe loader paths, lines 45-78. Safe loader must fall back rather than return empty, lines 137-158. Placeholder replacement must be single-pass/literal with no regex interpretation of values, lines 94-106. Empty `{{}}` and unclosed `{{` are preserved, lines 182-206. Valid template dir requires `block`, `codex`, and `claude` subdirectories as enforced by `hooks/lib/template-loader.sh:216`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` functions: `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Those functions are used by `hooks/lib/loop-common.sh` block-message helpers and by `hooks/loop-codex-stop-hook.sh` for user-facing block prompts.
- edge_cases_or_failure_modes: Explicitly covers missing template files, missing directories, empty templates, strict-mode subshell continuation, special characters in values, missing fallback templates, invalid template directory, empty placeholder names, and unclosed placeholders. It does not exercise nested placeholder injection directly, though `template-loader.sh` documents single-pass behavior at lines 54-57.
- validation_or_tests: This file is itself the validation script. Its final gate is `TESTS_FAILED == 0`, lines 218-225. It validates resilience for templates such as `prompt-template/block/git-not-clean.md` and `prompt-template/claude/open-question-notice.md` indirectly through the shared loader contract.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-082 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Prompt block template for the RLCR stop gate when git is not clean. It defines the user-facing remediation contract that blocks loop exit before expensive Codex review and before a round can be considered complete.
- algorithmic_behavior: The template renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, lines 3-4, then instructs required actions: optionally run `code-simplifier`, review untracked files and ignore build artifacts, stage real changes, and commit with a descriptive project-convention message, lines 5-10. Lines 11-16 enforce commit authorship rules and forbid AI co-author attribution. Line 16 allows retrying exit only after committing.
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`, supplied by `hooks/loop-codex-stop-hook.sh:559-561`. Output is a rendered Markdown block placed in a JSON hook decision with `"decision": "block"` and a system message, `hooks/loop-codex-stop-hook.sh:563-570`. It does not mutate state itself; it participates in a stop-hook gate.
- gates_or_invariants: The invariant is “dirty git state blocks stop.” The caller computes dirty state from cached git status, sets `GIT_ISSUES="uncommitted changes"` if status is nonempty, and exits after returning a block decision, `hooks/loop-codex-stop-hook.sh:523-571`. The template also encodes commit hygiene: no AI authorship and no `Co-Authored-By: Claude` style trailer, lines 11-14.
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` in `hooks/loop-codex-stop-hook.sh:559`. Rendering depends on `hooks/lib/template-loader.sh`, especially fallback-safe behavior. Special notes may come from sibling templates `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md`, `hooks/loop-codex-stop-hook.sh:531-546`.
- edge_cases_or_failure_modes: If this template is missing or renders empty, the stop hook uses an inline fallback, `hooks/loop-codex-stop-hook.sh:553-558`. If untracked `.humanize*` entries exist, special notes distinguish intentional local state from other artifacts. If other untracked files exist, the rendered note asks for ignore-or-commit review. Template values with unusual characters rely on the loader’s literal single-pass renderer tested in `tests/test-error-scenarios.sh`.
- validation_or_tests: Covered by `tests/test-error-scenarios.sh` through loader error and fallback behavior, especially missing-template fallback and special-character substitution. Caller behavior is visible at `hooks/loop-codex-stop-hook.sh:518-571`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-112 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Prompt notice template for the `--ask-codex-question` RLCR mode. It forces a state transition from “fix Codex findings” to “clarify open questions with the user first” when Codex review output contains an Open Question heading.
- algorithmic_behavior: The single-line template tells Claude that Codex found Open Questions and that `AskUserQuestion` must be used before resolving other Codex findings. The stop hook injects it only when `ASK_CODEX_QUESTION == "true"` and the review result file contains a line shorter than 40 characters with substring `Open Question`, `hooks/loop-codex-stop-hook.sh:1530-1546`.
- inputs_outputs_state: Inputs are the persisted `ask_codex_question` state field and the Codex review result content. `scripts/setup-rlcr-loop.sh` parses `--ask-codex-question` at lines 211-213 and writes `ask_codex_question: $ASK_CODEX_QUESTION` into `state.md` at line 731. Output is additional Markdown inserted into the next round prompt between the Codex review end marker and the goal tracker reference, `hooks/loop-codex-stop-hook.sh:1547-1558`.
- gates_or_invariants: The invariant is priority ordering: open questions must be clarified before other findings are handled. Detection is intentionally narrow: a line must contain `Open Question` and be under 40 characters, so long prose mentions do not trigger the notice. If the flag is false, no notice is injected even if Codex mentions open questions.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which loads `claude/open-question-notice.md` via `load_template` at line 1543 and has an inline fallback at lines 1544-1545. The state field is parsed in `hooks/lib/loop-common.sh` as `FIELD_ASK_CODEX_QUESTION`, lines 29 and 235, with default false at lines 244 and 333.
- edge_cases_or_failure_modes: Missing template falls back to identical inline text. Detection may miss headings longer than 39 characters or phrasing that does not include exact substring `Open Question`. It may trigger on any short line containing that substring, not just a Markdown heading. The template itself has no placeholders and no rendering variables.
- validation_or_tests: Setup state persistence is covered by inspection of `scripts/setup-rlcr-loop.sh:211-213` and `:731`. Template loader resilience is covered generically by `tests/test-error-scenarios.sh`. I did not find a dedicated test that asserts open-question injection into the next prompt.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-142 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-machine parsing and loop-directory discovery. It validates the state transitions and schema assumptions used by setup, stop hooks, finalize, and cancel behavior.
- algorithmic_behavior: Lines 14-19 source `hooks/lib/loop-common.sh` and test helpers, then create a temp test directory. Helper functions generate `state.md`, `finalize-state.md`, and `cancel-state.md` fixtures with YAML frontmatter, lines 30-89. Tests 1-3 validate round 0, round 5, and max-round parsing. Tests 4-5 validate finalize-state detection/coexistence. Tests 6-7 validate cancel-state exclusion and newer active state discovery. Tests 8-10 cover negative, over-max, and non-numeric rounds. Tests 11-13 enforce required schema fields. Tests 14-16 cover newest-directory, lexicographic ordering, and nested wrong-location ignore behavior.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/` directories and state frontmatter fields such as `current_round`, `max_iterations`, `base_branch`, and `review_started`. Outputs are PASS/FAIL counters through `tests/test-helpers.sh` and final exit status from `print_test_summary`, lines 423-424. State transitions under test are regular active loop, finalize active loop, cancel inactive loop, and new loop after cancel.
- gates_or_invariants: `parse_state_file_strict` must reject missing YAML separators, missing required `current_round`, `max_iterations`, `review_started`, or `base_branch`, non-numeric round, non-numeric max, and invalid review boolean as defined in `hooks/lib/loop-common.sh:250-336`. `find_active_loop` only checks the lexicographically newest child directory and accepts only top-level `state.md` or `finalize-state.md`, `hooks/lib/loop-common.sh:152-175`. `get_current_round` is tolerant and defaults to 0 when missing, `hooks/lib/loop-common.sh:177-190`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` functions `parse_state_file_strict`, `get_current_round`, and `find_active_loop`, plus `tests/test-helpers.sh` functions `setup_test_dir`, `pass`, `fail`, and `print_test_summary`. It validates assumptions created by `scripts/setup-rlcr-loop.sh`, which writes required fields into `state.md` at lines 716-733.
- edge_cases_or_failure_modes: Negative rounds are accepted by parser regex and documented in the test as potentially erroneous but parsed, lines 217-236. Rounds exceeding max are parsed, with enforcement expected elsewhere, lines 238-257. `find_active_loop` uses lexicographic ordering and does not validate timestamp shape, lines 385-390. Older active directories are ignored if the newest directory lacks active state; nested `state.md` under a subdirectory is ignored, lines 404-417.
- validation_or_tests: This file is itself validation. Its final gate is `print_test_summary "State Transition Robustness Test Summary"; exit $?`, lines 423-424. It provides direct executable coverage for core RLCR state discovery and strict state schema validation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`