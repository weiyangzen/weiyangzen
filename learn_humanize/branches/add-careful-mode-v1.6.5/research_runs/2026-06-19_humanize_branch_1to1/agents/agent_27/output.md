# agent_27 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-027 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Edit` gate for RLCR and PR loop protected files. It is registered as a Claude hook at `hooks/hooks.json:29` and prevents agent edits to scheduler-owned prompts, state, tracker, todo, and PR-loop artifacts before any file mutation occurs.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits immediately unless the tool is `Edit` (`hooks/loop-edit-validator.sh:24`-`31`). It lowercases the path for classification, blocks `round-N-todos.md` unless the target is an active-loop allowlist exception (`hooks/loop-edit-validator.sh:38`-`45`), blocks `round-N-prompt.md` writes (`hooks/loop-edit-validator.sh:48`-`51`), applies PR-loop state/read-only/round validation (`hooks/loop-edit-validator.sh:57`-`76`), then applies RLCR-only checks for active state, immutable files, plan backup, post-round-0 goal tracker, and current-round summary naming (`hooks/loop-edit-validator.sh:83`-`183`).
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR` or current working directory, optional `PROJECT_ROOT`, `LOOP_BASE_DIR`, `LOOP_DIR`, the active `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, and shared helper/template functions from `hooks/lib/loop-common.sh`. Outputs are process exit status and stderr block messages: `0` allows the edit, `2` blocks policy violations, and `1` blocks malformed RLCR state after strict parsing (`hooks/loop-edit-validator.sh:105`-`109`). The script does not mutate files; it reads state to decide whether an edit may proceed.
- gates_or_invariants: Only `Edit` calls are governed (`hooks/loop-edit-validator.sh:27`-`29`). Prompt, state, and finalize-state files are system-owned; finalize-state is checked before generic state because `finalize-state.md` also matches `state.md` (`hooks/loop-edit-validator.sh:115`-`124`). Goal tracker is editable only in round 0 (`hooks/loop-edit-validator.sh:145`-`148`). Summary edits must target `round-${CURRENT_ROUND}-summary.md` unless allowlisted (`hooks/loop-edit-validator.sh:155`-`179`). Strict state parsing requires YAML separators, numeric `current_round` and `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:259`-`335`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16`-`18`) for path classifiers, active-loop lookup, strict state parsing, template-rendered block messages, and PR round validation. Key helper behavior includes newest active loop lookup (`hooks/lib/loop-common.sh:157`-`175`), round file matching (`hooks/lib/loop-common.sh:407`-`423`), allowlist matching (`hooks/lib/loop-common.sh:429`-`447`), RLCR/PR directory detection (`hooks/lib/loop-common.sh:719`-`722`, `790`-`798`), PR read-only file matching (`hooks/lib/loop-common.sh:824`-`841`), and goal-tracker block messaging (`hooks/lib/loop-common.sh:1105`-`1116`).
- edge_cases_or_failure_modes: Non-`Edit` tools are ignored, so analogous protections must exist in read/write/bash validators. Todo files are blocked when no active loop exists, but arbitrary RLCR paths can pass if no active loop is found after the RLCR directory check (`hooks/loop-edit-validator.sh:95`-`97`). A malformed or schema-outdated state file fail-closes all guarded RLCR edits. PR `round-N-pr-resolve.md` validation parses `current_round` only from `---` frontmatter (`hooks/lib/loop-common.sh:867`-`874`), so plain YAML state can default to round `0` and incorrectly reject a valid PR resolve filename.
- validation_or_tests: Directly exercised by `tests/test-pr-loop-hooks.sh:1520`, `tests/test-finalize-phase.sh:372`, `tests/test-plan-file-hooks.sh:247`, `tests/test-plan-file-hooks.sh:1099`, `tests/test-allowlist-validators.sh:195`-`221`, and robustness tests such as `tests/robustness/test-hook-system-robustness.sh:44`-`147`. I inspected rather than executed tests in this read-only research pass.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Real end-to-end executable specification for RLCR and PR monitor lifecycle behavior. It validates production monitor functions from `scripts/humanize.sh`, not stubs, especially graceful deletion handling, shell glob safety, terminal cleanup, cross-shell behavior, and interrupt handling.
- algorithmic_behavior: Creates isolated `/tmp/test-monitor-e2e-real-$$` projects and fake cache trees (`tests/test-monitor-e2e-real.sh:48`-`56`). Tests 1 and 2 build `.humanize/rlcr/<timestamp>` sessions with `state.md`, `goal-tracker.md`, and cache logs, source `scripts/humanize.sh`, run `_humanize_monitor_codex`, delete `.humanize/rlcr`, and assert clean exit, user-facing deletion text, no zsh/bash glob errors, cleanup message, scroll reset source code, and exit code 0 (`tests/test-monitor-e2e-real.sh:64`-`231`, `243`-`373`). Tests 3 and 4 run the same RLCR monitor under SIGINT for bash and zsh (`tests/test-monitor-e2e-real.sh:382`-`550`, `561`-`683`). Tests 5 and 6 cover PR monitor `--once` deletion-adjacent behavior and continuous SIGINT behavior (`tests/test-monitor-e2e-real.sh:692`-`824`, `833`-`990`). The summary exits 0 only when `TESTS_FAILED` remains zero (`tests/test-monitor-e2e-real.sh:992`-`1017`).
- inputs_outputs_state: Inputs are the repository’s `scripts/humanize.sh`, optional `zsh`, shell utilities (`sed`, `grep`, `tail`, `kill`, `pkill`, `sleep`, `rm`), fake `HOME` and `XDG_CACHE_HOME`, synthetic RLCR/PR loop state files, and shimmed `tput`/`clear` functions. Outputs are pass/fail lines, captured monitor output files under `/tmp`, background monitor process exit codes, and final test exit status. Runtime state is modeled with RLCR `.humanize/rlcr/<timestamp>/state.md`, PR `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, and cache logs.
- gates_or_invariants: RLCR monitor must avoid glob errors by using `find`-based discovery (`scripts/humanize.sh:280`-`328`), restore terminal scroll region with `printf "\033[r"` (`scripts/humanize.sh:542`-`545`), call cleanup before graceful deletion messages (`scripts/humanize.sh:583`-`596`), and return 0 when `.humanize/rlcr` disappears (`scripts/humanize.sh:642`-`645`, `682`-`685`, `775`-`778`). PR monitor must support `--once` snapshot exit (`scripts/humanize.sh:1235`-`1313`) and continuous cleanup through EXIT/INT/TERM traps (`scripts/humanize.sh:1194`-`1233`, `1323`-`1375`). Each async wait is bounded to avoid hanging tests.
- dependencies_and_callers: Included by the aggregate test runner at `tests/run-all-tests.sh:51`. It exercises `_humanize_monitor_codex` (`scripts/humanize.sh:219`) and `_humanize_monitor_pr` (`scripts/humanize.sh:951`), which in turn use shared monitor helpers from `scripts/lib/monitor-common.sh` through sourced `scripts/humanize.sh`.
- edge_cases_or_failure_modes: SIGINT tests accept SIGTERM fallback as pass when process-group interrupt delivery is difficult (`tests/test-monitor-e2e-real.sh:514`-`518`, `960`-`964`), so they are partly liveness tests rather than strict SIGINT proofs. The PR `--once` deletion case starts deletion after a short delay but `--once` can finish before observing deletion (`tests/test-monitor-e2e-real.sh:784`-`805`). PR fixtures omit `---` frontmatter while `_pr_parse_state_md` reads frontmatter-delimited content (`scripts/humanize.sh:1055`-`1067`), so PR display values may default even when the fixture has fields. zsh coverage is skipped if zsh is unavailable (`tests/test-monitor-e2e-real.sh:240`-`242`, `558`-`560`). Scroll reset is partly verified by source grep rather than only runtime terminal bytes (`tests/test-monitor-e2e-real.sh:219`-`224`).
- validation_or_tests: This file is itself validation. The intended command is `bash tests/test-monitor-e2e-real.sh`; I did not execute it during this research-only pass.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-087 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Blocking prompt template for Round 0 goal-tracker initialization. It is an algorithmic gate that prevents the loop from advancing while the immutable goal, acceptance criteria, or active task mapping still contains placeholder content.
- algorithmic_behavior: The template tells the agent that Round 0 goal tracker initialization is incomplete (`prompt-template/block/goal-tracker-not-initialized.md:1`-`3`), renders the target file and missing item list (`prompt-template/block/goal-tracker-not-initialized.md:5`-`6`), and prescribes required remediation: read the goal tracker, replace placeholders, define the ultimate goal, create 3-7 specific testable acceptance criteria, populate active tasks, and write `goal-tracker.md` (`prompt-template/block/goal-tracker-not-initialized.md:8`-`14`). It emphasizes that the immutable section can only be set in Round 0 (`prompt-template/block/goal-tracker-not-initialized.md:16`).
- inputs_outputs_state: Inputs are `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, supplied by the stop hook after inspecting the goal, acceptance criteria, and active task sections (`hooks/loop-codex-stop-hook.sh:672`-`709`). Output is a rendered block reason embedded in a JSON hook response with `"decision": "block"` and a system message (`hooks/loop-codex-stop-hook.sh:711`-`719`). The template does not mutate state; it blocks exit/advance until the agent updates `goal-tracker.md` and retries.
- gates_or_invariants: The immutable section must be initialized in Round 0, with acceptance criteria specific and testable, and active tasks mapped to those criteria. After Round 0, direct goal tracker edits are blocked by validators such as `hooks/loop-edit-validator.sh:145`-`148`, so this template is the last safe correction path for missing immutable goal state.
- dependencies_and_callers: Rendered via `load_and_render_safe` from `hooks/loop-codex-stop-hook.sh:707`, using the template loader’s single-pass substitution behavior (`hooks/lib/template-loader.sh:50`-`74`). It coordinates with `prompt-template/block/goal-tracker-modification.md` through `goal_tracker_blocked_message` for later rounds (`hooks/lib/loop-common.sh:1105`-`1116`).
- edge_cases_or_failure_modes: Caller-side placeholder detection looks for generic `[To be ...]` patterns in specific sections (`hooks/loop-codex-stop-hook.sh:672`-`684`), so weak but non-placeholder content can pass. If expected headings drift, especially the active tasks heading used by the hook, missing content may not be detected. Missing template variables remain as placeholders because the renderer keeps undefined `{{VAR}}` text rather than failing (`hooks/lib/template-loader.sh:12`-`13`, `116`-`120`).
- validation_or_tests: Indirectly covered by stop-hook and template-reference tests; repository search shows the template is referenced by `hooks/loop-codex-stop-hook.sh:707` and template reference coverage includes stop-hook/template wiring checks. I inspected the rendered-call path, not runtime output.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-117 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory Codex full-alignment review prompt at configured intervals. It defines the review contract that compares the original plan, goal tracker, Claude’s summary, implementation reality, deferred work, and recent round history before allowing completion or forcing continued work/stop.
- algorithmic_behavior: Requires Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5`-`10`), embeds Claude’s round summary (`prompt-template/codex/full-alignment-review.md:13`-`16`), audits each immutable acceptance criterion with evidence/blockers/deferral justification (`prompt-template/codex/full-alignment-review.md:19`-`29`), detects forgotten items by comparing plan and goal tracker (`prompt-template/codex/full-alignment-review.md:30`-`34`), audits deferred items (`prompt-template/codex/full-alignment-review.md:36`-`40`), requires a completion summary (`prompt-template/codex/full-alignment-review.md:42`-`48`), performs implementation review (`prompt-template/codex/full-alignment-review.md:50`-`55`), injects goal-tracker update handling (`prompt-template/codex/full-alignment-review.md:57`), and performs stagnation detection over historical prompts, summaries, and review results (`prompt-template/codex/full-alignment-review.md:59`-`85`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, previous round indices, and `REVIEW_RESULT_FILE`, supplied by `hooks/loop-codex-stop-hook.sh:820`-`831`. Output is a rendered `round-${CURRENT_ROUND}-review-prompt.md`; Codex must write `round-${CURRENT_ROUND}-review-result.md` or the stop hook may copy stdout into that path (`hooks/loop-codex-stop-hook.sh:1332`-`1348`). The review result’s last non-empty line drives downstream control flow (`hooks/loop-codex-stop-hook.sh:1390`-`1398`).
- gates_or_invariants: Full alignment review is selected when `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1`, with invalid intervals defaulted to 5 (`hooks/loop-codex-stop-hook.sh:775`-`784`). The template permits `COMPLETE` only when all original plan tasks are done, all acceptance criteria are fully met, and there are no deferrals (`prompt-template/codex/full-alignment-review.md:88`-`93`). Stagnation requires `STOP` as the final line (`prompt-template/codex/full-alignment-review.md:84`-`90`). Any unmet or deferred acceptance criterion requires findings and action items rather than completion.
- dependencies_and_callers: Depends on `prompt-template/codex/goal-tracker-update-section.md` being rendered into Part 3 (`hooks/loop-codex-stop-hook.sh:769`-`773`), the original plan file, current goal tracker, docs path hardcoded as `docs` in the stop hook (`hooks/loop-codex-stop-hook.sh:756`-`757`), and historical `.humanize/rlcr/<timestamp>/round-N-*` files named in the template (`prompt-template/codex/full-alignment-review.md:63`-`73`).
- edge_cases_or_failure_modes: Historical indices are clamped to zero for early rounds (`hooks/loop-codex-stop-hook.sh:787`-`793`), which can duplicate previous-round references during early full reviews. `DOCS_PATH` is fixed to `docs`, so repositories with alternate documentation locations rely on reviewer judgment. Stagnation criteria are qualitative, so outputs can vary across reviewers. If Codex exits successfully but does not create a review result and stdout is empty, the stop hook hard-fails (`hooks/loop-codex-stop-hook.sh:1350`-`1385`). Undefined template variables remain literal placeholders under the single-pass renderer.
- validation_or_tests: The stop-hook validates review-result existence, non-empty content, and terminal marker parsing (`hooks/loop-codex-stop-hook.sh:1350`-`1398`). Template loading has explicit usage coverage in `hooks/lib/template-loader.sh:34`; template-reference tests inspect hook/template wiring. I inspected the prompt contract and caller path rather than executing stop-hook tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item evidence headings present, each assigned ID appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`