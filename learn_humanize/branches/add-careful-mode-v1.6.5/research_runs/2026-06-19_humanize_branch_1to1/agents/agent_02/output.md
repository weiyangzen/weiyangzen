# agent_02 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Defines the repository’s Claude subagent specs. This directory currently contains one recursive descendant, `agents/draft-relevance-checker.md`, which is the semantic relevance gate for `/humanize:gen-plan`.
- algorithmic_behavior: `draft-relevance-checker.md` declares frontmatter `name: draft-relevance-checker`, `model: haiku`, and `tools: Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-6`. Its procedure is: explore repo documentation and structure, analyze the provided draft against repo concepts/files/features, then return exactly `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` (`agents/draft-relevance-checker.md:14-29`). The judgment policy is intentionally permissive: ambiguous cases should be treated as relevant (`agents/draft-relevance-checker.md:33-36`).
- inputs_outputs_state: Input is draft document content plus read-only repository context gathered through Read/Glob/Grep. Output is a single verdict string consumed by the gen-plan flow. It writes no repository state and has no persistent local state.
- gates_or_invariants: This is a pre-generation gate after IO validation. `commands/gen-plan.md:50-72` says the command reads the draft, invokes `humanize:draft-relevance-checker`, stops on `NOT_RELEVANT`, and continues to Phase 3 on `RELEVANT`. Required output prefixes are an invariant because downstream command instructions branch on them.
- dependencies_and_callers: Called by `commands/gen-plan.md:56-65` through the Task tool using the haiku model. It coordinates with `scripts/validate-gen-plan-io.sh` indirectly because gen-plan performs IO validation before relevance checking (`commands/gen-plan.md:50-52`). Tests validate the agent file exists, name is correct, model is `haiku`, and tools are specified at `tests/test-gen-plan.sh:111-153`; later tests validate model naming and English-only content at `tests/test-gen-plan.sh:390-422`.
- edge_cases_or_failure_modes: A draft in any language or rough form should still pass if semantically related. False negatives are mitigated by the “if in doubt, relevant” rule. Failure modes include missing/renamed agent file, wrong model/frontmatter, missing tools declaration, or an answer that does not begin with one of the two required verdict prefixes.
- validation_or_tests: `tests/test-gen-plan.sh` is the main executable spec for this directory’s only agent: existence/name/model/tools checks at `tests/test-gen-plan.sh:111-153`, model validity at `tests/test-gen-plan.sh:390-400`, and content character policy at `tests/test-gen-plan.sh:403-422`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Implements the user-facing cancellation transition for an active PR loop under `.humanize/pr-loop/`. It is a runtime workflow script, not just a helper.
- algorithmic_behavior: Parses only `--force` and help (`scripts/cancel-pr-loop.sh:24-66`); `--force` is accepted but currently has no behavioral effect (`scripts/cancel-pr-loop.sh:28-30`, `scripts/cancel-pr-loop.sh:40`). It resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, uses `.humanize/pr-loop` as its loop base, selects the lexicographically newest loop directory, and requires `state.md` inside it (`scripts/cancel-pr-loop.sh:72-98`). It extracts `current_round`, `max_iterations`, and `pr_number` from frontmatter-like lines with `grep`/`sed`, defaulting missing fields to `?` (`scripts/cancel-pr-loop.sh:104-112`). The actual state transition is `touch .cancel-requested` then `mv state.md cancel-state.md` (`scripts/cancel-pr-loop.sh:118-123`), followed by a `CANCELLED` sentinel and summary (`scripts/cancel-pr-loop.sh:128-130`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR`, current working directory, and files under `.humanize/pr-loop/<timestamp>/`. Outputs are stdout sentinels (`NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`), process exit status, a `.cancel-requested` signal file, and renamed loop state. State transition is active `state.md` to terminal `cancel-state.md`.
- gates_or_invariants: Active PR loop means the newest PR loop directory contains `state.md`; if no directory exists it exits with `NO_LOOP` and status 1 (`scripts/cancel-pr-loop.sh:76-82`), and if newest directory lacks `state.md` it exits with `NO_ACTIVE_LOOP` and status 1 (`scripts/cancel-pr-loop.sh:88-98`). The command wrapper documents that “the script handles all cancellation logic” and that active means `state.md` exists in newest `.humanize/pr-loop/` directory (`commands/cancel-pr-loop.md:17-21`).
- dependencies_and_callers: Exposed through `/humanize:cancel-pr-loop` by `commands/cancel-pr-loop.md:1-25`. The PR stop hook observes `.cancel-requested` during polling and exits cleanly when present (`hooks/pr-loop-stop-hook.sh:951-955`). Setup blocks starting a second PR loop and tells users to cancel first (`scripts/setup-pr-loop.sh:235-240`). The broader hook safety layer has a protected exception for authorized moves to `cancel-state.md` when a cancel signal exists (`hooks/loop-bash-validator.sh:133-143`), with `is_cancel_authorized` enforcing signal presence, no command substitution/chaining, exactly two args, expected source/destination, and no symlink source (`hooks/lib/loop-common.sh:540-710`).
- edge_cases_or_failure_modes: Directory selection is lexicographic reverse sort of subdirectories, so a newer non-active directory can cause `NO_ACTIVE_LOOP` even if an older active state exists; the system appears designed around one active loop. Missing metadata produces `?` in the cancellation summary. `touch` or `mv` failures are not wrapped, so `set -euo pipefail` will exit with the underlying command failure rather than the documented generic status 3. Existing `cancel-state.md` could be overwritten by `mv` behavior. The script affects only PR loops and explicitly not RLCR loops (`commands/cancel-pr-loop.md:25`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` exercises help output, no-loop behavior, and active-loop cancellation. It asserts no loop returns status 1 plus `NO_LOOP` (`tests/test-pr-loop-scripts.sh:192-207`) and active cancellation returns `CANCELLED` while `cancel-state.md` exists and `state.md` no longer does (`tests/test-pr-loop-scripts.sh:211-252`). Additional cancel-signal robustness tests cover the validator exception path, but the assigned script’s direct tests are in `test-pr-loop-scripts.sh`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-062 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared executable test fixture library for the PR loop test suite. It supplies mock external commands, initializes isolated test state, and defines the PR-loop-specific summary gate.
- algorithmic_behavior: Uses `TEST_PR_LOOP_LIB_LOADED` as an idempotent source guard (`tests/test-pr-loop-lib.sh:10-13`). It establishes `SCRIPT_DIR` and `PROJECT_ROOT` defaults and sources `test-helpers.sh` only if `setup_test_dir` is not already defined (`tests/test-pr-loop-lib.sh:15-21`). `create_mock_gh` writes an executable mock `gh` that handles selected `auth status`, `repo view`, `pr view`, and `api` cases, returning stable JSON/timestamps/state or empty arrays (`tests/test-pr-loop-lib.sh:27-88`). `create_mock_codex` writes an executable `codex` that emits fixed output and succeeds (`tests/test-pr-loop-lib.sh:90-100`). `init_pr_loop_test_env` creates a temp directory, prepends mock binaries to `PATH`, creates both mocks, and exports `MOCK_BIN_DIR` (`tests/test-pr-loop-lib.sh:107-121`). `print_test_summary` reports counters and returns failure when `TESTS_FAILED > 0` (`tests/test-pr-loop-lib.sh:127-144`).
- inputs_outputs_state: Inputs are global helper functions/counters, a mock directory path for mock creation, and the caller’s shell environment. Outputs are executable mock files, mutated `PATH`, exported `MOCK_BIN_DIR`, initialized `TEST_DIR` via `setup_test_dir`, stdout summaries, and shell return status. It does not modify production code paths.
- gates_or_invariants: The idempotency guard prevents duplicate function definitions and repeated setup. The mock `gh` deliberately fails unhandled command shapes with status 1 (`tests/test-pr-loop-lib.sh:84-85`), which keeps tests honest about expected API calls. Summary status is a test gate: any failed assertion makes the aggregate runner return nonzero (`tests/test-pr-loop-lib.sh:137-143`).
- dependencies_and_callers: `tests/test-pr-loop.sh` sources `test-helpers.sh` then this library (`tests/test-pr-loop.sh:18-20`), calls `init_pr_loop_test_env` before loading modules (`tests/test-pr-loop.sh:22-34`), runs script/hook/stop-hook suites, and calls `print_test_summary` at the end (`tests/test-pr-loop.sh:40-53`). The library depends on counters and helper functions from `tests/test-helpers.sh`, including `pass`, `fail`, and `setup_test_dir` (`tests/test-helpers.sh:22-44`, `tests/test-helpers.sh:84-89`).
- edge_cases_or_failure_modes: `create_mock_codex` assumes its target directory already exists; this is true through `init_pr_loop_test_env` but not guaranteed for direct calls. The mock `gh api` returns `testuser` for `user` and `[]` for other API calls, so tests requiring richer API behavior must provide their own mocks. Because this library is sourced after `test-helpers.sh`, its zero-argument `print_test_summary` overrides the helper version that accepts an optional title.
- validation_or_tests: This file is itself test infrastructure. Its behavior is exercised transitively by `tests/test-pr-loop.sh`, which relies on its setup and summary path before running all PR loop test modules (`tests/test-pr-loop.sh:26-53`).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-092 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop hook when the original plan file changes during an active session. It defines the human-facing remediation contract for a plan-integrity gate.
- algorithmic_behavior: The template renders a titled block saying the plan file `{{PLAN_FILE}}` changed, states that modifying plan files during an active session is forbidden, provides the cancel/update/restart sequence, and reports `{{BACKUP_PATH}}` (`prompt-template/block/plan-file-modified.md:1-12`).
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH` template variables. Output is rendered markdown used as the `reason` in a hook block decision. The template itself does not read or write state; state is supplied by the hook and template loader.
- gates_or_invariants: The invariant is plan immutability across the session. In `hooks/loop-codex-stop-hook.sh`, after checking tracked uncommitted status, the hook compares the live plan against the backup; any diff renders this template and emits a JSON block decision (`hooks/loop-codex-stop-hook.sh:316-335`). The remediation path requires canceling the active RLCR loop before changing the plan.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md"` with `PLAN_FILE` and `BACKUP_PATH` at `hooks/loop-codex-stop-hook.sh:330-332`. Rendering semantics come from `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}`, missing variables are preserved, and substitution is single-pass (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-58`). Missing or empty templates fall back to hook-provided text through `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: The template is not used for the earlier uncommitted tracked-plan check at `hooks/loop-codex-stop-hook.sh:300-312`; it is used for backup-content mismatch, including committed changes. If variables are omitted, placeholders remain visible. If the template is missing or renders empty, the hook fallback preserves equivalent blocking instructions (`hooks/loop-codex-stop-hook.sh:318-329`).
- validation_or_tests: Validation is indirect through the stop-hook plan-integrity path and template-loader behavior. The inspected references show no dedicated assertion for this exact file content, but its caller has a fallback and the renderer has a documented safe-substitution contract.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-122 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: Critical completion-contract template for PR loop startup when fetched PR comments exist. It tells the worker what must happen before handing control to the Stop Hook.
- algorithmic_behavior: The rendered block requires three completion actions: commit and push changes, comment on the PR with `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review"`, and write a resolution summary to `{{RESOLVE_PATH}}` including issues, files modified, and tests added (`prompt-template/pr-loop/critical-requirements-has-comments.md:1-24`).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`. Output is a rendered critical-requirements block printed during `start-pr-loop` activation. It does not mutate state directly, but it establishes required external side effects: git commit/push, PR trigger comment, and resolve summary file.
- gates_or_invariants: This template is selected only when comment content exists. `scripts/setup-pr-loop.sh` determines comment presence by checking whether the fetched comments file equals the `*No comments found.*` sentinel; otherwise `COMMENT_COUNT=1` (`scripts/setup-pr-loop.sh:723-729`). The has-comments branch uses this critical template after outputting the round-0 prompt (`scripts/setup-pr-loop.sh:890-942`). The invariant is that review-feedback work must be pushed and retrigger review before the Stop Hook polls for bot responses.
- dependencies_and_callers: Variables are assembled in `TEMPLATE_VARS` at `scripts/setup-pr-loop.sh:731-738`, including `RESOLVE_PATH` set to `round-0-pr-resolve.md` at `scripts/setup-pr-loop.sh:721`. The template is loaded via `load_and_render_safe` at `scripts/setup-pr-loop.sh:940`, with an inline fallback carrying the same required actions at `scripts/setup-pr-loop.sh:916-939`. Template substitution behavior is provided by `hooks/lib/template-loader.sh:50-58` and fallback behavior by `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: The comment-count logic is boolean, not an actual count; any file not matching the no-comments sentinel triggers the has-comments path. If template variables are missing, unresolved placeholders remain because missing variables are preserved by the renderer. The trigger body in this critical template uses `please review`, while the round task fallback uses `please review the latest changes` (`scripts/setup-pr-loop.sh:817-823`); both are review-triggering forms, but they are not identical strings. If the template is missing, fallback content is used.
- validation_or_tests: Indirectly validated through setup-pr-loop startup behavior and template-loader coverage. The inspected references show the live render path and fallback path in `scripts/setup-pr-loop.sh:890-942`; no dedicated direct test assertion for this specific template text was found in the inspected search results.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5; each assigned item_id is present exactly once as an Item Evidence heading`
- missing_items: `none`
- duplicate_items: `none in Item Evidence headings`
- final_worker_status: `complete`