# agent_16 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness specification directory for the Humanize loop algorithms. It contains 17 bash suites, 8,654 total lines, and is wired into the aggregate runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/run-all-tests.sh:55), which schedules every `robustness/*.sh` suite in order.
- algorithmic_behavior: The directory tests failure-resistant behavior around RLCR setup, PR-loop setup, hook validation, state-machine parsing, monitor rendering, timeout execution, git status parsing, template rendering, cancel authorization, and API/polling behavior. Most suites source production code directly rather than reimplementing behavior: examples include `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/portable-timeout.sh`, and direct invocations of `scripts/setup-rlcr-loop.sh`.
- inputs_outputs_state: Inputs are synthetic temp repos, `.humanize/rlcr/*` and `.humanize/pr-loop/*` state files, mocked `codex`/`gh` binaries, JSON hook payloads, plan files, log files, terminal dimensions, and git worktrees. Outputs are `pass`/`fail`/`skip` counters from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/test-helpers.sh:30), validator exit codes, hook JSON block/allow decisions, generated loop state files, and monitor text.
- gates_or_invariants: Directory-level invariants are fail-closed validation, no malformed state crash, no unauthorized `.humanize` state/summary mutation, no unsafe plan-file path, no cancel-command injection, no timeout hang, no template recursive placeholder injection, and correct active-loop selection. Test helpers return nonzero if any suite failure is recorded via `print_test_summary` at [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/test-helpers.sh:58).
- dependencies_and_callers: Parent caller is `tests/run-all-tests.sh`, with robustness suites listed at lines 56-72. Child roles: `test-path-validation-robustness.sh` and `test-plan-file-robustness.sh` exercise `setup-rlcr-loop.sh`; `test-setup-scripts-robustness.sh` covers RLCR/PR argument and concurrency gates; `test-state-file-robustness.sh`, `test-state-transition-robustness.sh`, `test-session-robustness.sh`, and `test-concurrent-state-robustness.sh` cover loop-state parsing/selection; `test-hook-input-robustness.sh` and `test-hook-system-robustness.sh` cover validators and monitor edge cases; `test-template-error-robustness.sh` and `test-template-stress-robustness.sh` cover template loader behavior; `test-cancel-security-robustness.sh` covers cancel authorization; `test-pr-loop-api-robustness.sh` covers mocked GitHub API/polling; `test-git-operations-robustness.sh`, `test-goal-tracker-robustness.sh`, `test-timeout-robustness.sh`, and `test-base-branch-detection.sh` cover shared status/timeout/branch helpers.
- edge_cases_or_failure_modes: Covered edge classes include absolute paths, shell metacharacters, symlink chains, empty/comment-only/short plans, missing/corrupt YAML frontmatter, binary/null/Unicode content, CRLF, stale or cancelled loops, concurrent reads during writes, missing API auth/network/rate-limit errors, deeply nested JSON, malformed hook payloads, unsupported terminal widths, detached/rebase/merge git states, and command-injection attempts in cancel commands.
- validation_or_tests: This item is itself the validation surface. Representative direct child test ranges: path validation cases at [tests/robustness/test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/robustness/test-path-validation-robustness.sh:120), hook JSON/monitor cases at `tests/robustness/test-hook-input-robustness.sh:35`, cancel security cases at `tests/robustness/test-cancel-security-robustness.sh:39`, setup-script cases at `tests/robustness/test-setup-scripts-robustness.sh:96`, and PR API cases at `tests/robustness/test-pr-loop-api-robustness.sh:285`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Test fixture generator for PR-loop monitor behavior. It creates controlled `.humanize/pr-loop/<timestamp>/state.md` files so monitor tests can validate YAML-list bot parsing without going through production validators.
- algorithmic_behavior: Requires `<test_dir>` and optional `<test_name>`, then dispatches on `yaml_list`, `configured`, or `empty` at [tests/setup-monitor-test-env.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/setup-monitor-test-env.sh:20). Each branch writes YAML frontmatter with `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- inputs_outputs_state: Input 1 is target test directory; input 2 selects fixture shape. Output is the created directory plus a state file, and the script echoes `TEST_DIR` at line 91. State transition variants are: both bots active (`yaml_list`), only `codex` active while both are configured (`configured`), and empty `active_bots` to mean all approved (`empty`).
- gates_or_invariants: Missing `TEST_DIR` exits with usage at lines 15-18. Unknown fixture exits with available names at lines 84-88. Fixture paths are deterministic timestamps under `.humanize/pr-loop`, preserving monitor active-loop discovery assumptions.
- dependencies_and_callers: Called by PR-loop monitor tests at [tests/test-pr-loop-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/test-pr-loop-hooks.sh:1553), `:1580`, and `:1602`. Those tests then run `source scripts/humanize.sh && humanize monitor pr --once` and assert display of `active_bots`, `configured_bots`, or `none`.
- edge_cases_or_failure_modes: Exercises YAML-list parsing, partial approval, and empty-list rendering. It does not validate filesystem permissions or malformed YAML; it is a positive fixture helper, not a negative robustness suite.
- validation_or_tests: Indirectly validates `scripts/humanize.sh` PR monitor parsing, especially configured/active bot rendering referenced by production parsing at `scripts/humanize.sh:1062-1079` and display at `scripts/humanize.sh:1152-1156`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR Codex-review failure gate. It turns a failed or missing Codex review result into a user-facing retry/block message.
- algorithmic_behavior: Static Markdown template with placeholders for exit/debug state: `{{CODEX_EXIT_CODE}}`, `{{REVIEW_RESULT_FILE}}`, `{{CODEX_CMD_FILE}}`, `{{CODEX_STDOUT_FILE}}`, `{{CODEX_STDERR_FILE}}`, and `{{STDERR_CONTENT}}` at [prompt-template/block/codex-review-failed.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/prompt-template/block/codex-review-failed.md:5). It instructs that another review will be attempted when the user exits.
- inputs_outputs_state: Inputs are render variables supplied by the Codex stop hook. Output is Markdown used as the `"reason"` in a hook block decision, so the state transition is “Codex review failed/no output” -> “block exit and retry later.”
- gates_or_invariants: The template assumes the review result file was not created, explicitly marks it “not created” on line 6, and requires debug artifact paths plus stderr tail. It is rendered through `load_and_render_safe`, whose single-pass placeholder replacement prevents injected `{{OTHER}}` content from being re-expanded in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/hooks/lib/template-loader.sh:54).
- dependencies_and_callers: Referenced by `hooks/loop-codex-stop-hook.sh` at [line 1219](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/hooks/loop-codex-stop-hook.sh:1219), inside a function that emits `{"decision":"block"}` at lines 1229-1237. Related Codex execution/debug files are created at lines 1252-1279, and missing-review handling occurs at lines 1350-1369.
- edge_cases_or_failure_modes: Covers nonzero Codex exit, no review file, missing stdout/stderr, permission/disk issues around review-file creation, and prompt misunderstanding. One notable mismatch: caller passes `EXIT_CODE`, `CODEX_CMD_FILE`, and `CODEX_LOG_FILE` at lines 1223-1227, while this template expects `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE`; unless another call path supplies those names, some placeholders may remain unreplaced.
- validation_or_tests: Template existence and safe render usage are covered broadly by template-reference tests and robustness template suites; the hook behavior is exercised by `tests/robustness/test-hook-system-robustness.sh`, which includes stop-hook corrupted/missing/normal-state behavior beginning at line 461.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Prompt/block template for enforcing the RLCR summary-file location invariant.
- algorithmic_behavior: Minimal Markdown block saying summary files must be in the loop directory and rendering `{{CORRECT_PATH}}` at [prompt-template/block/wrong-summary-location.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/prompt-template/block/wrong-summary-location.md:3). It is used when a Write tool target looks like a summary file but is outside the active `.humanize/rlcr` loop directory.
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` by [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/hooks/loop-write-validator.sh:203). Output is stderr text plus validator exit code `2`, causing Claude Code to block the write attempt.
- gates_or_invariants: Summary writes outside `.humanize/rlcr` are rejected before generic path extraction. Finalize summaries inside the active loop directory are explicitly allowed first at `hooks/loop-write-validator.sh:168-172`, preventing the wrong-location rule from blocking valid finalize writes.
- dependencies_and_callers: Rendered via `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-summary-location.md"` at `hooks/loop-write-validator.sh:207`. It depends on `IS_SUMMARY_FILE`, `IN_HUMANIZE_LOOP_DIR`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` set earlier by hook/common logic.
- edge_cases_or_failure_modes: Handles misplaced summary files. Wrong round inside the loop is handled separately by `block/wrong-round-number.md` at `hooks/loop-write-validator.sh:228-244`, and wrong active-loop subdirectory is handled by `block/wrong-directory-path.md` at lines 252-262.
- validation_or_tests: Covered by hook-system and hook-input robustness suites that pipe JSON into validators and check block/allow behavior, especially write/edit validator cases in `tests/robustness/test-hook-system-robustness.sh:40-421`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-136 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for `scripts/setup-rlcr-loop.sh` plan-path validation, including path syntax, symlink safety, length/depth behavior, and minimal content validation.
- algorithmic_behavior: Sets strict bash mode, sources shared helpers, creates a temp git repo, installs a mock `codex`, creates valid plan fixtures, and invokes the production setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"` at [tests/robustness/test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/tests/robustness/test-path-validation-robustness.sh:86). `test_path_validation` interprets known setup errors as rejection and “requires codex” or “must be gitignored” as evidence that path validation passed.
- inputs_outputs_state: Inputs are repo-relative plan paths and generated plan files. Outputs are pass/fail counters and the final process exit from `print_test_summary` at line 484. The script mutates only its temp repo: `.humanize/rlcr` cleanup at line 82, plan fixtures, symlinks, and git state.
- gates_or_invariants: Valid path allowlist accepts relative simple paths, nested directories, dash/underscore, and dots at lines 120-167. Rejection gates cover absolute paths, whitespace, shell metacharacters `; & | $ \` < > ( ) { } [ ] ! # ~ * ? \`, symlink files, parent-directory symlinks, symlink chains, empty/comment-only/short/missing plans, and directories-as-files at lines 177-478.
- dependencies_and_callers: Directly targets production validation in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-careful-mode-v1.6.5/scripts/setup-rlcr-loop.sh:327), which rejects absolute paths, spaces, shell metacharacters, symlink file/parents, non-files, unreadable files, outside-project resolution, tracked-plan policy, and insufficient content at lines 327-543. It uses helper counters from `tests/test-helpers.sh`.
- edge_cases_or_failure_modes: Notable edge coverage includes parent symlink traversal, symlink chain `A->B->C`, 250-character filename, 10-level nested path, and a comment noting Unicode/CJK/emoji paths are allowed. The helper’s “accepted” classification can be indirect because a later gitignore or codex-availability error is treated as validation success after earlier path gates pass.
- validation_or_tests: 31 named tests: positive valid paths at lines 120-167; invalid syntax at lines 177-330; symlink security at lines 340-375; long/deep paths at lines 385-410; content/file existence gates at lines 423-478.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ADD_CAREFUL_MODE_V1_6_5-HZ-016`, `ADD_CAREFUL_MODE_V1_6_5-HZ-046`, `ADD_CAREFUL_MODE_V1_6_5-HZ-076`, `ADD_CAREFUL_MODE_V1_6_5-HZ-106`, `ADD_CAREFUL_MODE_V1_6_5-HZ-136`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`