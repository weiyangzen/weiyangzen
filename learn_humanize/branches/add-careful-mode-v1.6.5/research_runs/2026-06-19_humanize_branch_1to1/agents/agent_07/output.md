# agent_07 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`
- note: read-only research only; no files modified and no test suites executed.

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification corpus for the Humanize loop algorithms. The directory contains 56 files: shell suites, robustness suites, PR-loop GitHub fixtures, a mock `gh` executable, shared test helpers, and the aggregate test runner. It is not application runtime code, but it is core because it defines expected behavior for state-machine transitions, validators, prompt/template integrity, monitor behavior, setup validation, PR-loop polling, and error handling.
- algorithmic_behavior: `tests/run-all-tests.sh` declares the ordered suite list at `tests/run-all-tests.sh:32-73`, marks `test-zsh-monitor-safety.sh` as zsh-only at `tests/run-all-tests.sh:75-78`, runs each suite with captured output at `tests/run-all-tests.sh:104-119`, extracts `Passed:`/`Failed:` counters after stripping ANSI at `tests/run-all-tests.sh:121-128`, and fails the whole run if any suite has nonzero exit or failed count at `tests/run-all-tests.sh:130-158`. The top-level suites cover template loading/references, todo checking, plan validation, state exit naming, hook behavior, finalize phase, review merge detection, cancel signaling, monitor runtime/e2e, PR-loop scripts/hooks/system behavior, and PR-loop stop hooks. The `tests/robustness` subtree exercises malformed state files, session selection, goal tracker parsing, path validation, git operation parsing, hook input, template stress/error cases, concurrent state behavior, setup scripts, timeout handling, base branch detection, state transitions, and PR-loop API failures.
- inputs_outputs_state: Inputs are synthetic temp repos, generated `.humanize/rlcr` and `.humanize/pr-loop` state directories, markdown plans/summaries, JSON hook payloads, fixture JSON, and mocked GitHub CLI responses. `tests/test-helpers.sh` centralizes pass/fail/skip counters and summary output at `tests/test-helpers.sh:30-77`, plus temp repo helpers at `tests/test-helpers.sh:84-105`. `tests/mocks/gh` routes `gh pr`, `gh api`, `gh repo`, and `gh auth` commands to fixtures/env-controlled outputs at `tests/mocks/gh:27-246`. Fixture files under `tests/fixtures` provide issue comments, review comments, reactions, and PR reviews for PR-loop fetch/poll tests.
- gates_or_invariants: The directory asserts several core invariants: only active state files should make a loop active, terminal `*-state.md` files should not revive loops, plan paths must stay inside the repo and reject unsafe characters/symlinks, state files are protected from direct edits, cancel requires a signal file and exact rename shape, templates must exist and render safely, monitor cleanup must restore terminal state, and PR-loop bots/reviews must be polled and classified deterministically. Examples: finalize tests enumerate positive/negative finalize-phase gates at `tests/test-finalize-phase.sh:6-18`; cancel tests specify that only `mv state.md cancel-state.md` with a signal is allowed and other state writes are blocked at `tests/test-cancel-signal-file.sh:6-8`; state robustness tests define corrupted/missing/non-numeric state-file cases at `tests/robustness/test-state-file-robustness.sh:5-9`; state-transition robustness defines valid/invalid progression cases at `tests/robustness/test-state-transition-robustness.sh:5-9`.
- dependencies_and_callers: The tests call production scripts and hooks rather than isolated replicas: `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `hooks/lib/loop-common.sh`, validators, stop hooks, and templates. They depend on shell tools such as `bash`, `git`, `jq`, `sed`, `grep`, `find`, `stat`, and optionally `zsh`. PR-loop tests inject `tests/mocks/gh` into `PATH` and use `tests/fixtures` for deterministic GitHub data.
- edge_cases_or_failure_modes: The suite is intentionally heavy on boundary cases: malformed YAML frontmatter, missing required fields, non-numeric rounds, negative or over-max rounds, legacy `.humanize-loop.local` separation, unsafe plan paths, symlinks, command injection attempts, command substitution, chained shell operators, redirection-prefix bypasses, API failures/rate limits/network errors, monitor directory deletion, log truncation/deletion, zsh no-match behavior, and concurrent state writes. A notable coverage gap for this branch: searching the tests found no direct test for `ask_codex_question` / `--ask-codex-question` / `AskUserQuestion`, even though that field is parsed/rendered by runtime code and consumed by the Codex stop hook.
- validation_or_tests: This item is the validation surface. Static inspection only; I did not run the tests because the assignment requested research notes only and the branch export is read-only. The aggregate runner’s pass/fail logic is at `tests/run-all-tests.sh:121-158`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing runtime shell utility for Humanize monitor workflows. It exposes `humanize monitor rlcr` and `humanize monitor pr`, parses loop state/goal/git data, tails loop logs, shows a fixed terminal status bar, and switches across sessions/logs as loop state changes. It is core algorithm support code for observing RLCR and PR-loop state, not for mutating authoritative loop state.
- algorithmic_behavior: The script sources shared monitor helpers from `scripts/lib/monitor-common.sh` at `scripts/humanize.sh:6-10`. It provides reusable parsers: `humanize_split_to_array` handles bash/zsh pipe-delimited output at `scripts/humanize.sh:18-28`; `humanize_parse_goal_tracker` counts ACs, active/completed/deferred tasks, open issues, and goal summary from markdown sections at `scripts/humanize.sh:32-112`; `humanize_detect_git_state` classifies normal/detached/rebase/merge/shallow/permission states at `scripts/humanize.sh:116-162`; `humanize_parse_git_status` converts porcelain/diffstat into modified/added/deleted/untracked/insertions/deletions at `scripts/humanize.sh:166-210`.
- inputs_outputs_state: RLCR monitor input is `.humanize/rlcr`, project-specific cached Codex logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, each session’s `state.md` or terminal `*-state.md`, `goal-tracker.md`, and live git status. It outputs terminal status/tail text and returns nonzero on missing loop/session. `_find_latest_codex_log` derives the project cache path from `pwd`, searches timestamp-named session dirs, reads `round-*-codex-run.log` and `round-*-codex-review.log`, and returns the latest valid log at `scripts/humanize.sh:246-342`. State parsing reads `current_round`, `max_iterations`, model/effort, `started_at`, `plan_file`, `ask_codex_question`, and `review_started` at `scripts/humanize.sh:349-366`.
- gates_or_invariants: The RLCR monitor refuses to start if `.humanize/rlcr` is absent at `scripts/humanize.sh:230-235` or if no session directories exist at `scripts/humanize.sh:610-619`. Session names must match `YYYY-MM-DD_HH-MM-SS` before logs are considered at `scripts/humanize.sh:286-289`. A defensive log-order invariant rejects inconsistent sessions where a run log round is greater than or equal to the earliest review log round at `scripts/humanize.sh:330-337`. The status bar maps active loops into `active(impl)` or `active(review)` based on `review_started` at `scripts/humanize.sh:470-479`. It displays the branch feature flag `ask_codex_question` as On/Off at `scripts/humanize.sh:486-493`; the flag is produced by setup code and consumed by the stop hook, while this script is the monitor read/display path.
- dependencies_and_callers: Depends on `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` from `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:237-239`, `scripts/humanize.sh:344-347`, `scripts/humanize.sh:630-633`). Uses `git`, `find`, `stat`, `tail`, `tput`, `sed`, `grep`, `sort`, `wc`, `tr`, and shell traps. The public dispatcher is `humanize()` at `scripts/humanize.sh:903-944`, routing `monitor rlcr` to `_humanize_monitor_codex` and `monitor pr` to `_humanize_monitor_pr`.
- edge_cases_or_failure_modes: The monitor handles missing loop dirs, no logs yet, deleted current session, deleted log, newer session discovery, same-session newer log discovery, log truncation/rotation, terminal cleanup on signals, bash/zsh array behavior, and status rendering when terminal width forces truncation. Cleanup/trap behavior for RLCR is at `scripts/humanize.sh:550-607`; the main RLCR polling/tailing loop is at `scripts/humanize.sh:640-891`. PR monitor supports `--once` for scripting/testing at `scripts/humanize.sh:1235-1312`, maps `approve-state.md` to `approved` and `maxiter-state.md` to `max-iterations` at `scripts/humanize.sh:1031-1048`, and tails the newest PR activity file among `round-*-pr-check.md`, `round-*-pr-feedback.md`, and `round-*-pr-comment.md` at `scripts/humanize.sh:987-1028`.
- validation_or_tests: Covered by goal tracker robustness tests, git operation robustness tests, monitor runtime/e2e tests, PR-loop system monitor helper tests, and zsh monitor safety tests. Relevant inspected references include `tests/robustness/test-goal-tracker-robustness.sh:5-10`, `tests/robustness/test-git-operations-robustness.sh:5-10`, `tests/test-monitor-runtime.sh:6-9`, `tests/test-monitor-e2e-real.sh:9-12`, and `tests/test-pr-loop-system.sh:767-826`. Static search found no tests directly asserting `ask_codex_question` display or stop-hook injection behavior.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Focused executable specification for RLCR state-file exit naming and active-loop discovery. It proves that `state.md` is the only normal active-loop marker, terminal renamed state files are preserved but not treated as active, and `end_loop` uses canonical reason-specific filenames.
- algorithmic_behavior: The script creates a temp git repo and loop directory, sources `hooks/lib/loop-common.sh`, and calls `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. Tests 1-7 verify active-loop detection: `complete-state.md` alone is ignored at `tests/test-state-exit-naming.sh:38-74`, `state.md` is active at `tests/test-state-exit-naming.sh:76-95`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are ignored at `tests/test-state-exit-naming.sh:97-159`, and a newer session with `state.md` takes precedence at `tests/test-state-exit-naming.sh:161-180`.
- inputs_outputs_state: Inputs are generated state markdown files under `.humanize/rlcr/<timestamp>`, a legacy `.humanize-loop.local` directory, and an initialized git repo for realistic project context. Outputs are PASS/FAIL/SKIP counters and process exit equal to failed count at `tests/test-state-exit-naming.sh:293-302`. State transition assertions are file-system based: a source `state.md` should become `<reason>-state.md` and the active marker should disappear.
- gates_or_invariants: `end_loop` must reject invalid reasons with an error at `tests/test-state-exit-naming.sh:186-204`, create exactly `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` for valid reasons at `tests/test-state-exit-naming.sh:206-230`, and fail gracefully when the source state file is missing at `tests/test-state-exit-naming.sh:232-243`. The underlying production function validates reasons and renames at `hooks/lib/loop-common.sh:1125-1151`. `find_active_loop` only checks the newest directory and only treats `state.md` or `finalize-state.md` as active at `hooks/lib/loop-common.sh:152-175`.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`, especially `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. It sets `CLAUDE_PROJECT_DIR` before sourcing the library at `tests/test-state-exit-naming.sh:64-67`. It also uses `git init`, local user config, and temp directories.
- edge_cases_or_failure_modes: Covers invalid reason, missing state file, terminal states that should not revive loops, newest-directory precedence, new `.humanize/rlcr` path recognition, negative legacy `.humanize-loop.local` matching, and separation from legacy directories at `tests/test-state-exit-naming.sh:249-290`. One subtle test-harness behavior: after the invalid-reason check it enables `set -e` at `tests/test-state-exit-naming.sh:196-199`, so later unexpected command failures may abort the script instead of incrementing `TESTS_FAILED`.
- validation_or_tests: This file is itself the validation spec and is included in the aggregate runner at `tests/run-all-tests.sh:33-40`. It overlaps with broader finalize/session robustness coverage, but uniquely checks canonical exit filenames for all five non-finalize RLCR terminal reasons.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Guard/block prompt template used when a validator prevents direct modification of RLCR `state.md`. It defines the user-facing contract that loop state is system-managed and direct edits would corrupt the state machine.
- algorithmic_behavior: The template is static markdown with no variables. It emits a heading, a prohibition on modifying `state.md`, a short list of state contents, and a corruption rationale at `prompt-template/block/state-file-modification.md:1-10`. The production caller is `state_file_blocked_message`, which loads this template with a fallback at `hooks/lib/loop-common.sh:469-476`.
- inputs_outputs_state: Input is only the validator choosing this message path; there are no template placeholders. Output is a rendered block message for Claude/tool users. It does not mutate state; it supports the invariant that `state.md` transitions must happen through loop scripts/hooks rather than through arbitrary Write/Edit/Bash actions.
- gates_or_invariants: Establishes the state-file write gate: direct modification of `state.md` is blocked because the file stores loop-control data. Related path predicates classify state targets at `hooks/lib/loop-common.sh:515-519`, while finalization and PR-loop state have separate block templates. If the template is missing, `load_and_render_safe` falls back to the inline message at `hooks/lib/loop-common.sh:471-475`.
- dependencies_and_callers: Used by validator flows through `hooks/lib/loop-common.sh`. The template reference scanner explicitly includes `block/state-file-modification.md` in its expected template set at `tests/test-template-references.sh:155`, and comprehensive template tests exercise loading/rendering of real templates.
- edge_cases_or_failure_modes: Because the template is static, placeholder substitution cannot corrupt it. The main risk is message drift: it lists current round, max iterations, and Codex configuration at `prompt-template/block/state-file-modification.md:5-8`, while current state files also include fields such as `base_branch`, `review_started`, `full_review_round`, and `ask_codex_question`. That does not weaken the gate, but it is less complete as operator explanation.
- validation_or_tests: Template existence is validated by the template reference suite, and render safety is covered by template loader/comprehensive template tests. Static inspection only; no test execution.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-127 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 task template for the “no review comments yet” startup path. It instructs the agent to wait for automatic monitored-bot reviews, write an initial summary, avoid posting a trigger comment, avoid modifying `.humanize/pr-loop` state files, and rely on the Stop Hook for polling and validation.
- algorithmic_behavior: The template uses `{{ACTIVE_BOTS_DISPLAY}}` to name monitored bots and `{{RESOLVE_PATH}}` as the summary target. The primary task content is at `prompt-template/pr-loop/round-0-task-no-comments.md:4-15`; rules are at `prompt-template/pr-loop/round-0-task-no-comments.md:18-23`; the expected Stop Hook sequence is described at `prompt-template/pr-loop/round-0-task-no-comments.md:26-30`. The setup caller selects this template only when `COMMENT_COUNT` is zero, using `load_and_render_safe` at `scripts/setup-pr-loop.sh:766-800`.
- inputs_outputs_state: Inputs are comment count, active bot display text, and the resolve/summary path. Output is appended prompt text in the PR-loop session’s `round-0-prompt.md`. It does not write state itself; it shapes the agent’s next action so that the PR-loop stop hook can poll review arrivals and transition loop state.
- gates_or_invariants: The critical invariant is “no manual trigger comment” for the fresh PR path: first reviews are automatic, so the agent should write the summary and exit. The template also states that `.humanize/pr-loop` state files are managed by the system at `prompt-template/pr-loop/round-0-task-no-comments.md:20-22`. Stop-hook gates are explicit: poll every 30 seconds up to 15 minutes per bot, validate approvals with local Codex, continue if issues are found, and end only if all bots approve at `prompt-template/pr-loop/round-0-task-no-comments.md:26-30`.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` with an inline fallback that mirrors the template at `scripts/setup-pr-loop.sh:769-800`. It coordinates with `scripts/fetch-pr-comments.sh` for comment count, `.humanize/pr-loop/<session>/state.md` for active bot state, and `hooks/pr-loop-stop-hook.sh` for polling, approval detection, feedback generation, max-iteration/timeout handling, and terminal state renames. The alternate branch for existing comments uses a different template (`round-0-task-has-comments.md`) starting at `scripts/setup-pr-loop.sh:801`.
- edge_cases_or_failure_modes: Applies only when no comments are present at setup time; if comments exist, the wrong template would suppress issue-handling instructions. It assumes monitored bots will review automatically; if they do not, the Stop Hook timeout path must handle the wait. Missing template variables would leave unresolved placeholders unless caught by template tests. The instruction not to modify state files aligns with validator protections, but the template itself is advisory rather than an enforcement mechanism.
- validation_or_tests: Covered indirectly by PR-loop setup/script/hook/system tests that exercise no-comment startup behavior, including the case-1 no-trigger path in PR-loop stop-hook/system suites. Template existence/rendering is covered by template reference and comprehensive template suites. Static inspection only; no test execution.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections above; exact item identifiers are intentionally not repeated here to preserve the once-only output invariant
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`