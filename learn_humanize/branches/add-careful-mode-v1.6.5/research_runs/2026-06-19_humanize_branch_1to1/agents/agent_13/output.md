# agent_13 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation contract directory. It contains a single recursive child, `prompt-template/plan/gen-plan-template.md`, which defines the canonical structure for generated implementation plans: goal, acceptance criteria, path boundaries, feasibility hints, dependency sequence, and implementation notes.
- algorithmic_behavior: The template shapes generated plans into deterministic work units. It requires TDD-style positive and negative tests for every acceptance criterion at `prompt-template/plan/gen-plan-template.md:6-23`, path boundaries at `prompt-template/plan/gen-plan-template.md:25-44`, dependency sequencing without time estimates at `prompt-template/plan/gen-plan-template.md:57-67`, and code-style constraints that forbid leaking plan workflow labels into implementation code at `prompt-template/plan/gen-plan-template.md:69-74`.
- inputs_outputs_state: Input is a user draft plus this template. `scripts/validate-gen-plan-io.sh` resolves the template path from `CLAUDE_PLUGIN_ROOT` or script-relative fallback, then copies it to the requested output and appends the original draft at `scripts/validate-gen-plan-io.sh:133-157`. Output is a new plan file pre-seeded with the template and original draft; the command later edits placeholders into a completed plan.
- gates_or_invariants: The IO validator gates plan generation on input file existence, non-empty input, existing output directory, non-existing output file, output directory writability, and template presence with exit codes 1-7 at `scripts/validate-gen-plan-io.sh:4-12` and `scripts/validate-gen-plan-io.sh:79-145`. The command-level plan schema also requires AC-X formatting, positive/negative tests, no time estimates, no line-number references, and preservation of all original draft information at `commands/gen-plan.md:225-247`.
- dependencies_and_callers: The directory is consumed by `/gen-plan` instructions in `commands/gen-plan.md`, especially the Phase 5 plan structure mirrored at `commands/gen-plan.md:142-223`. The executable dependency is `scripts/validate-gen-plan-io.sh`, invoked by the command at `commands/gen-plan.md:30-47`. It coordinates with the broader `prompt-template` tree as one of several template namespaces (`block`, `claude`, `codex`, `plan`, `pr-loop`).
- edge_cases_or_failure_modes: Missing template is a plugin configuration failure and exits 7 at `scripts/validate-gen-plan-io.sh:142-145`. Existing output path or directory path-as-output both exit 4 at `scripts/validate-gen-plan-io.sh:103-116`. Deterministic designs may collapse upper/lower path bounds and must be represented as fixed choices, per `prompt-template/plan/gen-plan-template.md:44`.
- validation_or_tests: `scripts/validate-gen-plan-io.sh` itself is the executable validation surface for this directory. `commands/gen-plan.md:251-270` adds a post-generation review requirement: read the complete plan and fix inconsistencies using Edit, preserving the original draft.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-043 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor shutdown behavior when `.humanize/` disappears during monitoring.
- algorithmic_behavior: The script provides three modes: `setup` creates a synthetic active RLCR session under `.humanize/rlcr/2026-01-16_99-99-99` with `state.md` and `goal-tracker.md`; `delete` removes `.humanize`; `cleanup` removes leftovers. These actions are defined at `tests/manual-monitor-test.sh:23-72`.
- inputs_outputs_state: Input is the command argument `{setup|delete|cleanup}`. `setup` writes loop state fields including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` at `tests/manual-monitor-test.sh:26-32`, plus minimal goal tracker content at `tests/manual-monitor-test.sh:33-38`. Output is terminal guidance and filesystem mutation under `.humanize/`. The target monitor state transition is active monitor -> graceful stop after `.humanize` removal.
- gates_or_invariants: The expected invariant is that `humanize monitor rlcr` exits cleanly with the specific message `Monitoring stopped: .humanize/rlcr directory no longer exists`, restores terminal scroll/cursor state, and emits no shell glob errors. The script states these expected observations at `tests/manual-monitor-test.sh:51-56`.
- dependencies_and_callers: Depends on `scripts/humanize.sh` monitor entrypoint, invoked manually as `source scripts/humanize.sh && humanize monitor rlcr` at `tests/manual-monitor-test.sh:10-12`. The implementation path is `_humanize_monitor_codex`: it restores terminal state in `_restore_terminal` at `scripts/humanize.sh:542-548`, runs `_cleanup` at `scripts/humanize.sh:557-581`, and `_graceful_stop` prints the specific stop reason at `scripts/humanize.sh:583-595`.
- edge_cases_or_failure_modes: It is intentionally destructive to `.humanize` via `rm -rf .humanize` at `tests/manual-monitor-test.sh:49` and `tests/manual-monitor-test.sh:60`; it should only be run in a disposable or prepared test repo. It is manual rather than assertion-based, so it depends on human observation of the second terminal. The monitor’s repeated directory-existence checks cover no-log and incremental-loop cases at `scripts/humanize.sh:640-645`, `scripts/humanize.sh:682-685`, and, by search evidence, later at `scripts/humanize.sh:777`.
- validation_or_tests: Related automated coverage exists in `tests/test-monitor-runtime.sh` and `tests/test-monitor-e2e-real.sh` for graceful-stop messaging and glob-error absence. Shared monitor helpers avoid zsh glob failures by using `find` instead of unmatched globs in `scripts/lib/monitor-common.sh:49-60`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-073 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR/PR-loop hook library. It provides the validators’ core state parsing, active-loop discovery, path classification, command blocking primitives, template-backed block messages, cancel authorization, git-add protection, loop termination, and PR goal-tracker update helper.
- algorithmic_behavior: JSON hook input is validated for null bytes, UTF-8, JSON syntax, and required `tool_name` at `hooks/lib/loop-common.sh:60-97`; required tool fields and JSON nesting limits are enforced at `hooks/lib/loop-common.sh:102-134`. Active RLCR session selection only considers the newest timestamp directory and requires `state.md` or `finalize-state.md`, preventing older zombie loops from reactivating at `hooks/lib/loop-common.sh:152-175`. State frontmatter is parsed in tolerant mode at `hooks/lib/loop-common.sh:211-248` and strict fail-closed mode at `hooks/lib/loop-common.sh:259-335`.
- inputs_outputs_state: Inputs are hook JSON, shell command strings, file paths, loop directories, state files, review logs, and optional bot-result JSON. Outputs are shell return codes, rendered block messages on stderr, extracted review issue content, global `STATE_*` variables, and file mutations only in explicit helpers such as `end_loop` and `update_pr_goal_tracker`.
- gates_or_invariants: Strict state parsing requires YAML separators, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric round/iteration fields, and boolean `review_started` at `hooks/lib/loop-common.sh:267-328`. Round file classification is centralized in `is_round_file_type` and `extract_round_number` at `hooks/lib/loop-common.sh:405-424`. The allowlist permits only a small historical set of todo/summary files at `hooks/lib/loop-common.sh:426-447`. `command_modifies_file` detects redirection, `tee`, in-place edits, `mv/cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns at `hooks/lib/loop-common.sh:1074-1102`.
- dependencies_and_callers: Sourced by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and stop hooks. Read validation uses `find_active_loop`, strict state parsing, round extraction, and block rendering to reject wrong round/location reads at `hooks/loop-read-validator.sh:55-168`. Write/Edit validators use path predicates and message helpers to block prompt, state, plan backup, and post-round-0 goal tracker edits at `hooks/loop-write-validator.sh:55-260` and `hooks/loop-edit-validator.sh:38-183`. Bash validation uses `command_modifies_file`, `is_cancel_authorized`, and git-add protection at `hooks/loop-bash-validator.sh:115-393`.
- edge_cases_or_failure_modes: Invalid JSON, null bytes, non-UTF-8, missing fields, or excessive nesting fail before validator-specific logic. Malformed state files cause validators to block for safety, as seen in `hooks/loop-read-validator.sh:94-98`, `hooks/loop-write-validator.sh:140-144`, and `hooks/loop-bash-validator.sh:81-85`. `is_cancel_authorized` rejects command substitution, shell operators, leftover `$`, mixed quotes, extra args, wrong source/destination, and symlinked state source at `hooks/lib/loop-common.sh:539-716`. `git_adds_humanize` blocks direct `.humanize`, forced broad adds, and `git add -A` when `.humanize` exists at `hooks/lib/loop-common.sh:914-1032`.
- validation_or_tests: Covered by targeted tests: template references in `tests/test-template-references.sh:149-180`, allowlist validators in `tests/test-allowlist-validators.sh`, Bash command patterns in `tests/test-bash-validator-patterns.sh`, hook input robustness in `tests/robustness/test-hook-input-robustness.sh`, state robustness in `tests/robustness/test-state-file-robustness.sh`, cancel security in `tests/robustness/test-cancel-security-robustness.sh`, and PR-loop tests. Review issue extraction scans Codex logs for `[P?]` markers and returns a hard error on missing/empty logs at `hooks/lib/loop-common.sh:338-398`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-103 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template for the read validator when an agent tries to read an RLCR round prompt or summary from outside the active loop directory.
- algorithmic_behavior: The template renders the attempted `{{FILE_PATH}}`, the canonical `{{ACTIVE_LOOP_DIR}}/`, and current-round prompt/summary paths using `{{CURRENT_ROUND}}` at `prompt-template/block/wrong-file-location.md:1-9`. It communicates a gate failure without directly changing loop state.
- inputs_outputs_state: Inputs are template variables supplied by `loop-read-validator.sh`: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` at `hooks/loop-read-validator.sh:122-130`. Output is a stderr block message and validator exit code 2 at `hooks/loop-read-validator.sh:127-131`.
- gates_or_invariants: The invariant is that current round files must be read from the active `.humanize/rlcr/<session>/` directory. `loop-read-validator.sh` first restricts this behavior to `round-N-summary.md` and `round-N-prompt.md` reads at `hooks/loop-read-validator.sh:69-71`, then gates non-loop-directory paths at `hooks/loop-read-validator.sh:118-132`.
- dependencies_and_callers: Loaded through `load_and_render_safe`, whose single-pass renderer avoids recursive placeholder expansion and preserves missing placeholders literally at `hooks/lib/template-loader.sh:50-132` and fallback behavior at `hooks/lib/template-loader.sh:167-203`. The direct caller is `hooks/loop-read-validator.sh:127`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to an inline wrong-location message from `hooks/loop-read-validator.sh:124-126`. The final line says `cat {{FILE_PATH}}` at `prompt-template/block/wrong-file-location.md:9`; this is guidance for reading the originally requested file only after the validator has explained that loop files live elsewhere, so it could be confusing if the attempted file is a stale round file outside the loop.
- validation_or_tests: Template presence and critical safe-render usage are checked indirectly by `tests/test-template-references.sh:130-180`; the read-validator behavior is also exercised by hook-system and template robustness tests in the suite.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-133 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `humanize_parse_goal_tracker`, the parser that feeds monitor status metrics from RLCR `goal-tracker.md`.
- algorithmic_behavior: The test sources `tests/test-helpers.sh` and production `scripts/humanize.sh`, creates a temp test directory, then writes synthetic goal trackers and asserts the pipe-delimited parser output. The parser contract is documented as `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` at `tests/robustness/test-goal-tracker-robustness.sh:31-37`; helper field extraction is implemented at `tests/robustness/test-goal-tracker-robustness.sh:38-51`.
- inputs_outputs_state: Inputs are generated markdown files in `$TEST_DIR`. Output is pass/fail counts through `pass`, `fail`, and `print_test_summary`; exit status is the test summary status at `tests/robustness/test-goal-tracker-robustness.sh:526-531`. The production parser returns defaults for missing tracker files at `scripts/humanize.sh:32-37` and emits the seven-field summary at `scripts/humanize.sh:111`.
- gates_or_invariants: It validates AC counting in list and table formats at `tests/robustness/test-goal-tracker-robustness.sh:60-116`, active-task counting excluding completed/deferred rows at `tests/robustness/test-goal-tracker-robustness.sh:118-153`, completed task and unique completed AC counts at `tests/robustness/test-goal-tracker-robustness.sh:155-202`, and goal summary extraction at `tests/robustness/test-goal-tracker-robustness.sh:204-227`. The parser implementation counts ACs between `### Acceptance Criteria` and `---` at `scripts/humanize.sh:48-54`, active tasks from `#### Active Tasks` until the next heading at `scripts/humanize.sh:56-84`, completed/deferred/open issue table rows at `scripts/humanize.sh:86-102`, and truncates the goal summary to 60 chars at `scripts/humanize.sh:104-109`.
- dependencies_and_callers: Depends on `setup_test_dir` and result helpers from `tests/test-helpers.sh:30-89`. It is included in the full suite via `tests/run-all-tests.sh:58`. Runtime caller is the RLCR monitor status bar, which invokes `_parse_goal_tracker` and splits the seven fields at `scripts/humanize.sh:369-413`.
- edge_cases_or_failure_modes: Negative/edge tests cover nonexistent file defaults at `tests/robustness/test-goal-tracker-robustness.sh:237-244`, empty files at `tests/robustness/test-goal-tracker-robustness.sh:246-256`, 60 ACs at `tests/robustness/test-goal-tracker-robustness.sh:258-279`, special shell/markdown characters at `tests/robustness/test-goal-tracker-robustness.sh:281-302`, malformed files without proper headers at `tests/robustness/test-goal-tracker-robustness.sh:304-326`, truncated files at `tests/robustness/test-goal-tracker-robustness.sh:328-347`, binary bytes mixed into markdown at `tests/robustness/test-goal-tracker-robustness.sh:349-371`, only headers at `tests/robustness/test-goal-tracker-robustness.sh:441-475`, mixed bold/sub-numbered AC formats at `tests/robustness/test-goal-tracker-robustness.sh:477-499`, and long goal truncation at `tests/robustness/test-goal-tracker-robustness.sh:501-524`.
- validation_or_tests: This file is itself the validation asset. It asserts 18 scenarios and exits nonzero if any fail. I did not run it because the task requested research notes only and the sandbox is read-only, but the test writes only to a temp directory through `mktemp -d` when executed normally.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 required item evidence sections present above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`