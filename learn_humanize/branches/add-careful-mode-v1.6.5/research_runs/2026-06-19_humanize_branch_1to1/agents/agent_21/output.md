# agent_21 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-021 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the PR review loop. The file is a thin command wrapper that exposes `/humanize:start-pr-loop` and delegates all operational behavior to `scripts/setup-pr-loop.sh` via the allowed Bash tool declaration at `commands/start-pr-loop.md:3-14`.
- algorithmic_behavior: Defines the user-facing PR loop algorithm: detect current-branch PR, fetch bot review comments, let the agent fix issues, require push and bot re-trigger comment, then rely on the Stop Hook to poll every 30 seconds for up to 15 minutes and invoke local Codex validation (`commands/start-pr-loop.md:16-24`, `commands/start-pr-loop.md:37-45`). It also codifies comment priority: human comments first, then newest bot comments (`commands/start-pr-loop.md:31-35`).
- inputs_outputs_state: Inputs are command arguments `$ARGUMENTS`, especially required bot flags `--claude` and/or `--codex`, plus optional `--max`, `--codex-model`, and `--codex-timeout` surfaced in the argument hint (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). Output is the setup script’s generated loop prompt/state, not produced by this markdown directly. Downstream state is created under `.humanize/pr-loop/<timestamp>/state.md` by `scripts/setup-pr-loop.sh`, including `current_round`, `max_iterations`, PR number, active/configured bot lists, Codex settings, polling settings, trigger timestamps, and trigger comment id (`scripts/setup-pr-loop.sh:615-635`).
- gates_or_invariants: At least one bot flag is required (`commands/start-pr-loop.md:25-30`). The workflow invariant is that fixes must be committed, pushed, and followed by the exact bot mentions emitted by setup (`commands/start-pr-loop.md:39-48`). The command explicitly forbids manually editing state files or canceling by bypassing process controls (`commands/start-pr-loop.md:50-56`).
- dependencies_and_callers: Depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh` (`commands/start-pr-loop.md:4`, `commands/start-pr-loop.md:12-14`). The setup script in turn depends on `portable-timeout.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`, `gh`, `codex`, `git`, and GitHub PR APIs (`scripts/setup-pr-loop.sh:33-43`, `scripts/setup-pr-loop.sh:210-214`, `scripts/setup-pr-loop.sh:243-277`, `scripts/setup-pr-loop.sh:430-438`, `scripts/setup-pr-loop.sh:585-588`). Runtime continuation is handled by `hooks/pr-loop-stop-hook.sh`, registered in `hooks/hooks.json`, and discovered by `find_active_pr_loop` (`hooks/pr-loop-stop-hook.sh:49-67`).
- edge_cases_or_failure_modes: The delegated setup script rejects missing bot flags, unknown options, non-numeric `--max` or `--codex-timeout`, non-git repos, missing commits, unauthenticated/missing `gh`, missing `codex`, no associated PR, merged/closed PRs, YAML-unsafe branch/model/effort values, duplicate active loops, and failed trigger-comment/eyes-reaction verification (`scripts/setup-pr-loop.sh:110-190`, `scripts/setup-pr-loop.sh:220-277`, `scripts/setup-pr-loop.sh:351-390`, `scripts/setup-pr-loop.sh:393-407`, `scripts/setup-pr-loop.sh:525-601`).
- validation_or_tests: Covered indirectly by PR loop script/system tests: `tests/test-pr-loop-scripts.sh` validates setup/cancel command behavior, `tests/test-pr-loop-system.sh` includes setup integration and goal tracker creation, and robustness tests cover help, missing bot flag, unknown option, numeric validation, non-git rejection, active-loop exclusion, and valid setup reaching prerequisite gates (`tests/robustness/test-setup-scripts-robustness.sh:474-778`).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-051 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for `detect_review_issues()` in `hooks/lib/loop-common.sh`. It pins how Codex review logs are scanned and converted into next-round issue feedback.
- algorithmic_behavior: The test header states the algorithm: scan the entire log from line 1, find the first line where `[P<digit>]` appears in the first 10 characters, extract from that line to EOF, and return no-issue status if no matching line exists (`tests/test-codex-review-merge.sh:5-16`). The implementation under test uses `awk` against `substr($0, 1, 10) ~ /\[P[0-9]\]/`, then `sed` from the found line to EOF and prefixes stdout with `## Codex Review Issues` (`hooks/lib/loop-common.sh:338-398`).
- inputs_outputs_state: Inputs are synthetic log files named `$CACHE_DIR/round-N-codex-review.log`, plus required globals `LOOP_DIR` and `CACHE_DIR` configured by `setup_test_env` (`tests/test-codex-review-merge.sh:47-54`). Outputs are function stdout, exit code, stderr diagnostics, and a persisted `$LOOP_DIR/round-N-review-result.md` when issues are found (`hooks/lib/loop-common.sh:356-392`, `tests/test-codex-review-merge.sh:306-330`).
- gates_or_invariants: Exit code contract is central: `0` means issues found, `1` means no issue marker, `2` means missing or empty log and must be treated as a hard error (`hooks/lib/loop-common.sh:338-343`). Marker detection is intentionally constrained to first 10 characters of each line; markers later in a sentence are ignored (`tests/test-codex-review-merge.sh:83-107`). The scan must cover the full file, not just recent tail output (`tests/test-codex-review-merge.sh:171-227`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`tests/test-codex-review-merge.sh:41-42`). Production caller is `run_and_handle_code_review()` in `hooks/loop-codex-stop-hook.sh`, which treats `2` as blocking review failure, `0` as continue loop with issues, and `1` as proceed to finalize (`hooks/loop-codex-stop-hook.sh:1001-1029`).
- edge_cases_or_failure_modes: Tests cover marker outside first 10 chars, no marker, missing log, empty log, long logs with late and early markers, multiple markers where extraction starts at first marker, marker at column 0, common dash-prefix format, result-file creation, and exactly 50-line logs (`tests/test-codex-review-merge.sh:83-354`). It also verifies `[P0]` through `[P3]`-style digit markers rather than prose-only issue language (`tests/test-codex-review-merge.sh:235-303`).
- validation_or_tests: The file is itself the validation harness. It creates an isolated temp cache, increments pass/fail counters, and exits non-zero if any assertion fails (`tests/test-codex-review-merge.sh:23-44`, `tests/test-codex-review-merge.sh:356-368`). It is a focused regression suite for the “full log scan plus first marker extraction” behavior.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-081 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Blocking prompt fragment used when the RLCR/Codex stop hook detects untracked files that are not `.humanize*` local loop directories. It influences the git-clean gate by telling the agent how to classify untracked files before exit can proceed.
- algorithmic_behavior: The template does not compute state itself. It contributes deterministic guidance to the stop-hook’s git-clean transition: untracked files may be build artifacts, test outputs, runtime files, dependencies, editor files, logs, cache, or temp files, and should often be added to `.gitignore` rather than committed (`prompt-template/block/git-not-clean-untracked.md:2-10`).
- inputs_outputs_state: Input is implicit: `GIT_STATUS_CACHED` in `hooks/loop-codex-stop-hook.sh` contains one or more `??` entries not matching `.humanize`. Output is appended to `SPECIAL_NOTES` and then rendered into the blocking response when `GIT_ISSUES` is non-empty (`hooks/loop-codex-stop-hook.sh:523-547`, `hooks/loop-codex-stop-hook.sh:550-555`).
- gates_or_invariants: The gate blocks exit on any uncommitted changes. This fragment refines the invariant for untracked non-loop files: review them and either commit intentionally or ignore generated artifacts. It deliberately separates `.humanize*` local-loop notes from other untracked files (`hooks/loop-codex-stop-hook.sh:527-547`).
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:539-546`). If loading fails, the fallback is “Review untracked files - add to .gitignore or commit them” (`hooks/loop-codex-stop-hook.sh:542-545`). Template resolution depends on `hooks/lib/template-loader.sh`, sourced by `hooks/lib/loop-common.sh` and the stop hook (`hooks/lib/loop-common.sh:140-149`).
- edge_cases_or_failure_modes: If all untracked files are `.humanize*`, this template is not used and a separate local-note template is loaded (`hooks/loop-codex-stop-hook.sh:530-537`). If both `.humanize*` and other untracked files exist, both notes can be concatenated into `SPECIAL_NOTES` (`hooks/loop-codex-stop-hook.sh:520-547`). Missing template is non-fatal due to inline fallback.
- validation_or_tests: Template behavior is validated indirectly through stop-hook/template tests. Reference search shows comprehensive template tests and stop-hook git-clean paths load block templates; the concrete caller has a fallback, so validation should assert both successful template rendering and fallback behavior (`hooks/loop-codex-stop-hook.sh:542-545`, `tests/test-templates-comprehensive.sh:535` for related template coverage patterns).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-111 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude next-round prompt template for RLCR/Codex review loops. It defines the transition from a Codex review result back to agent implementation work.
- algorithmic_behavior: The template instructs the agent that work is not finished, requires reading the original implementation plan, creates Todos for all discovered issues, embeds Codex review content between explicit markers, and requires reading the goal tracker as read-only after Round 0 (`prompt-template/claude/next-round-prompt.md:1-31`). This is the main prompt body generated when Codex found issues.
- inputs_outputs_state: Inputs are template variables `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{GOAL_TRACKER_FILE}}` (`prompt-template/claude/next-round-prompt.md:5-8`, `prompt-template/claude/next-round-prompt.md:16-24`). Output is `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md`, produced by `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:1512-1528`).
- gates_or_invariants: Invariants are prompt-level but algorithmically important: the agent must not cherry-pick only important issues, must create Todos for every discovered issue, must align with the original plan, and cannot directly modify `goal-tracker.md` after Round 0 (`prompt-template/claude/next-round-prompt.md:5-13`, `prompt-template/claude/next-round-prompt.md:22-31`). The review markers are also structural anchors for later injection of open-question notices (`hooks/loop-codex-stop-hook.sh:1530-1563`).
- dependencies_and_callers: Called by `continue_review_loop_with_issues` flow in `hooks/loop-codex-stop-hook.sh` when `detect_review_issues()` returns issue content (`hooks/loop-codex-stop-hook.sh:1017-1025`, `hooks/loop-codex-stop-hook.sh:1512-1528`). The stop hook appends post-alignment instructions, a footer, optional push-every-round note, and goal-tracker update request template after rendering this base prompt (`hooks/loop-codex-stop-hook.sh:1566-1594`).
- edge_cases_or_failure_modes: If this template fails to load, fallback prompt content is used (`hooks/loop-codex-stop-hook.sh:1516-1525`). If `ASK_CODEX_QUESTION` is true and the review result contains a short line with “Open Question”, the stop hook inserts an AskUserQuestion notice after the review-result end marker and separator (`hooks/loop-codex-stop-hook.sh:1530-1563`). If the marker layout changes, that injection can fail or land in the wrong location.
- validation_or_tests: Covered indirectly by template rendering tests and stop-hook next-round tests. The critical contract is that rendered output preserves the Codex review markers and variable substitutions, because the stop hook uses those markers for open-question injection and continuation prompt assembly (`prompt-template/claude/next-round-prompt.md:16-20`, `hooks/loop-codex-stop-hook.sh:1547-1562`).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-141 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR state-file parsing helpers in `hooks/lib/loop-common.sh`, especially tolerant parsing, strict validation, and `get_current_round`.
- algorithmic_behavior: Exercises `parse_state_file`, `parse_state_file_strict`, and `get_current_round` across valid, malformed, missing, partial, and edge-value YAML frontmatter (`tests/robustness/test-state-file-robustness.sh:3-17`). Tolerant parsing extracts known fields and applies defaults for non-schema-critical values; strict parsing requires frontmatter separators plus required fields and validates numeric/boolean fields (`hooks/lib/loop-common.sh:180-248`, `hooks/lib/loop-common.sh:250-336`).
- inputs_outputs_state: Inputs are temp `state*.md` files under `TEST_DIR` with YAML frontmatter variants (`tests/robustness/test-state-file-robustness.sh:19-35`). Outputs are shell global variables like `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, `STATE_FULL_REVIEW_ROUND`, and function exit codes (`hooks/lib/loop-common.sh:192-209`, `tests/robustness/test-state-file-robustness.sh:52-59`, `tests/robustness/test-state-file-robustness.sh:415-448`).
- gates_or_invariants: Tolerant parser returns failure only for non-existent files and otherwise defaults `current_round=0`, `max_iterations=10`, `push_every_round=false`, `full_review_round=5`, and `ask_codex_question=false` (`hooks/lib/loop-common.sh:214-247`). Strict parser rejects missing files, fewer than two `---` separators, missing `current_round`, `max_iterations`, `review_started`, or `base_branch`, non-numeric round/max values, and invalid `review_started` boolean (`hooks/lib/loop-common.sh:262-335`). `get_current_round` defaults to `0` when extraction fails (`hooks/lib/loop-common.sh:180-190`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-state-file-robustness.sh:14-19`). State parsing is shared by RLCR validators/stop hooks and active-loop logic; `loop-common.sh` documents its consumers as loop read/write/bash validators (`hooks/lib/loop-common.sh:3-9`) and includes related active-loop helpers (`hooks/lib/loop-common.sh:152-175`).
- edge_cases_or_failure_modes: Tests cover extra fields, quoted strings, zero values, missing frontmatter, non-numeric current round, missing required fields, empty file, partial YAML, malformed YAML, very large numbers, negative numbers, spaces in quoted values, trailing whitespace, non-existent file, binary content after YAML, CRLF line endings, explicit `full_review_round`, default `full_review_round`, and minimum valid `full_review_round=2` (`tests/robustness/test-state-file-robustness.sh:33-474`).
- validation_or_tests: The file is the validation harness. It uses shared `setup_test_dir`, `pass`, `fail`, and `print_test_summary` helpers (`tests/test-helpers.sh:30-89`) and exits with the summary status (`tests/robustness/test-state-file-robustness.sh:476-481`). It is a robustness suite rather than production logic, but it encodes the accepted parser behavior for corrupted and partially written state files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`