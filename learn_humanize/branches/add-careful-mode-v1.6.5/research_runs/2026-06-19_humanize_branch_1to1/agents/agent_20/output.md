# agent_20 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for `/humanize:gen-plan`; it turns a rough draft into a structured implementation plan with acceptance criteria, path boundaries, feasibility hints, dependencies, and implementation notes. The command is hidden from the slash-command tool surface but remains a command definition via frontmatter at `commands/gen-plan.md:1-13`.
- algorithmic_behavior: Defines a six-phase pipeline: IO validation, repository relevance check, draft analysis, user issue resolution, plan generation, and write/review/report completion at `commands/gen-plan.md:19-26`. Phase 1 delegates path validation and initial output-file seeding to `scripts/validate-gen-plan-io.sh` at `commands/gen-plan.md:30-47`; Phase 2 invokes `humanize:draft-relevance-checker` and stops on `NOT_RELEVANT` at `commands/gen-plan.md:50-72`; Phase 3 expands into codebase exploration for clarity, consistency, completeness, and functionality at `commands/gen-plan.md:76-108`; Phase 4 preserves draft content and asks the user to resolve unclear requirements and numeric thresholds at `commands/gen-plan.md:111-139`; Phase 5 generates the final plan schema and rules at `commands/gen-plan.md:142-248`; Phase 6 edits the seeded output, reviews it, optionally unifies language, and reports results at `commands/gen-plan.md:251-304`.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, specifically `--input <draft.md> --output <plan.md>` from `commands/gen-plan.md:3` and `commands/gen-plan.md:35`. The validator resolves absolute paths, requires non-empty input, requires a non-existing output file in an existing writable directory, then copies `prompt-template/plan/gen-plan-template.md` and appends the original draft at `scripts/validate-gen-plan-io.sh:69-160`. Output state is a new plan file containing generated structured plan content plus the original draft retained at the bottom; the command requires using Edit, not Write, after validation-created seed content exists at `commands/gen-plan.md:253-260`.
- gates_or_invariants: Tool access is constrained to validation Bash, Read, Glob, Grep, Task, Write, and AskUserQuestion at `commands/gen-plan.md:4-11`. Exit codes 1-7 from the validator map to explicit stop reasons at `commands/gen-plan.md:38-47`. Relevance is lenient but hard-gated on `NOT_RELEVANT` at `commands/gen-plan.md:67-72`. The plan must use AC-X or AC-X.Y criteria, no time estimates, no line ranges, TDD positive/negative tests, affirmative path boundaries, deterministic-design narrowing, and full draft preservation at `commands/gen-plan.md:225-248`.
- dependencies_and_callers: Depends directly on `scripts/validate-gen-plan-io.sh`, `prompt-template/plan/gen-plan-template.md`, and the `agents/draft-relevance-checker.md` agent. The agent’s contract is to explore README/CLAUDE/main files and return `RELEVANT:` or `NOT_RELEVANT:` at `agents/draft-relevance-checker.md:14-29`. README documents `/humanize:gen-plan --input ... --output ...` as the user-facing flow. `tests/test-gen-plan.sh` validates command structure, required frontmatter, YAML-like syntax, English-only content, and validator exit behavior at `tests/test-gen-plan.sh:55-103`, `tests/test-gen-plan.sh:280-340`, and `tests/test-gen-plan.sh:426-539`.
- edge_cases_or_failure_modes: Invalid/missing args exit 6; missing input exits 1; empty input exits 2; missing output dir exits 3; existing output or directory output exits 4; unwritable dir exits 5; missing template exits 7 per `scripts/validate-gen-plan-io.sh:4-12` and `scripts/validate-gen-plan-io.sh:79-146`. Mixed-language final plans trigger an AskUserQuestion unification branch rather than automatic translation at `commands/gen-plan.md:272-285`. Numeric metrics must be classified as hard requirement versus optimization direction before acceptance criteria are finalized at `commands/gen-plan.md:126-138`.
- validation_or_tests: `tests/test-gen-plan.sh` checks file existence, description, allowed-tools, argument hint, required frontmatter, YAML-like syntax, no Emoji/CJK content, and validator behavior for invalid args and IO paths. It explicitly asserts valid paths exit 0 and help exits 6 at `tests/test-gen-plan.sh:523-539`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancel state transition. It verifies that loop state files are normally immutable and that cancellation is only authorized by a loop-local `.cancel-requested` signal plus an exact `mv state.md cancel-state.md` style transition.
- algorithmic_behavior: Creates isolated fake RLCR loop directories under a temporary `.humanize/rlcr/<timestamp>` tree and writes a representative `state.md` at `tests/test-cancel-signal-file.sh:42-59`. It simulates the Claude Bash hook by JSON-wrapping a Bash command with `jq` and piping it to `hooks/loop-bash-validator.sh` at `tests/test-cancel-signal-file.sh:61-74`. Positive tests assert allowed cancellation with a signal file for direct absolute paths, `/./` path normalization, quoted paths, and literal `${loop_dir}` syntax at `tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-486`. Negative tests assert exit 2 for unauthorized state mutation or bypass attempts at `tests/test-cancel-signal-file.sh:121-1248`, then direct helper tests validate `is_cancel_authorized` true/false behavior at `tests/test-cancel-signal-file.sh:1259-1343`.
- inputs_outputs_state: Inputs are synthetic Bash command strings and a fake loop state directory selected through `CLAUDE_PROJECT_DIR` at `tests/test-cancel-signal-file.sh:58`. Outputs are pass/fail counters and script exit status equal to `TESTS_FAILED` at `tests/test-cancel-signal-file.sh:1349-1357`. The protected state transition is from active `state.md` to terminal `cancel-state.md`; the signal input is exactly `.cancel-requested` in the active loop directory, not a parent directory at `tests/test-cancel-signal-file.sh:201-217`.
- gates_or_invariants: The central invariant is: without a loop-local `.cancel-requested`, any command modifying or moving `state.md` is blocked; with the signal, only a direct `mv` from active-loop `state.md` or `finalize-state.md` to active-loop `cancel-state.md` is allowed. The implementation gate lives in `is_cancel_authorized`, which requires the signal file, rejects `$()`/backticks/newlines/shell operators/remaining `$`, requires command start `mv`, parses exactly two args, validates exact normalized source/destination, and rejects symlink sources at `hooks/lib/loop-common.sh:539-716`. The Bash validator calls this exception before otherwise blocking `state.md`/`finalize-state.md` modification at `hooks/loop-bash-validator.sh:139-155` and source moves/copies at `hooks/loop-bash-validator.sh:158-328`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` directly at `tests/test-cancel-signal-file.sh:32-33`; exercises `hooks/loop-bash-validator.sh` as the PreToolUse Bash gate at `tests/test-cancel-signal-file.sh:72`. Related production cancel scripts create `.cancel-requested` and rename state files, but this test focuses on hook authorization rather than the cancel command implementation.
- edge_cases_or_failure_modes: Covers no signal, wrong signal directory, writes via echo/sed/rm, wrong destination, arbitrary destination, command substitution, backticks, newlines, `mv --`, `cp -f`, extra args, hidden `${IFS}`, sudo/env/command prefixes, leading whitespace, command chaining via `;`, `&&`, `||`, `|`, `|&`, background `&`, shell wrappers, many redirection-prefix forms including quoted/ANSI-C/escaped targets, unrelated `/tmp/state.md`, and no active loop. The “no active loop” case intentionally allows commands because no active `state.md` exists at `tests/test-cancel-signal-file.sh:1233-1249`.
- validation_or_tests: The file is itself the validation suite. It includes 5 positive validator cases, 53 negative validator cases, and 7 direct helper tests. The result contract is nonzero exit only when one or more assertions fail at `tests/test-cancel-signal-file.sh:1349-1357`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Prompt block template for the RLCR stop-hook git-clean gate. It gives a special remediation when the working tree is dirty because `.humanize*` local loop artifacts are untracked.
- algorithmic_behavior: The template emits a short “Special Case” note saying `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` with `echo '.humanize*' >> .gitignore` followed by `git add .gitignore` at `prompt-template/block/git-not-clean-humanize-local.md:2-8`.
- inputs_outputs_state: Input is the stop hook’s cached git status when untracked files include `.humanize`; output is appended advisory text in the hook’s block reason. The hook detects untracked `.humanize*` entries from `GIT_STATUS_CACHED`, loads this template, and concatenates it into `SPECIAL_NOTES` at `hooks/loop-codex-stop-hook.sh:523-537`.
- gates_or_invariants: It does not itself enforce a gate; it parameterizes the git-clean gate. The invariant it supports is that RLCR loop artifacts remain local and uncommitted while the user fixes git hygiene by ignoring `.humanize*`, not by committing loop state.
- dependencies_and_callers: Called only by `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` at `hooks/loop-codex-stop-hook.sh:532`. If loading fails, the hook falls back to a generic note that `.humanize*` directories are intentionally untracked at `hooks/loop-codex-stop-hook.sh:533-535`.
- edge_cases_or_failure_modes: If `.humanize*` is mixed with other untracked files, this template is only one note; the hook also loads `git-not-clean-untracked.md` for other untracked artifacts at `hooks/loop-codex-stop-hook.sh:539-545`. If `.humanize*` is tracked or staged rather than untracked, this specific note may not be sufficient; the broader git-clean gate still blocks on dirty status.
- validation_or_tests: No direct template-specific test was found by reference search; validation is indirect through stop-hook behavior and template-loader fallback behavior.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer template appended to next-round RLCR prompts. It encodes the required worker exit contract for the next loop round: do not fake exit, commit changes, and write the next summary.
- algorithmic_behavior: The footer inserts a separator, forbids exiting by lying, editing loop state files, or running `cancel-rlcr-loop`, then instructs the worker to optionally invoke code-simplifier, commit with a descriptive message, and write work summary to `@{{NEXT_SUMMARY_FILE}}` at `prompt-template/claude/next-round-footer.md:2-9`.
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, computed as `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` by the stop hook at `hooks/loop-codex-stop-hook.sh:1514`. Output is rendered prompt text appended to `NEXT_PROMPT_FILE` through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:1574-1578`.
- gates_or_invariants: The template supports RLCR round progression by making the summary file the required handoff state for the next stop-hook review. It reinforces state-file immutability and cancellation discipline; it does not itself enforce those rules, but pairs with Bash/Edit/Write validators that block direct state edits and unauthorized cancellation.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` after any post-alignment instructions and before optional push-every-round notes at `hooks/loop-codex-stop-hook.sh:1566-1586`. If missing, the fallback is a shorter “Commit your changes and write summary to {{NEXT_SUMMARY_FILE}}” block at `hooks/loop-codex-stop-hook.sh:1575-1577`.
- edge_cases_or_failure_modes: If `NEXT_SUMMARY_FILE` is wrong or not rendered, the worker may write summary to the wrong file and fail the next stop-hook progression. If code-simplifier is not installed, the footer makes that optional with “If installed” wording at `prompt-template/claude/next-round-footer.md:7`.
- validation_or_tests: No direct template-specific test was found by reference search; coverage is indirect through generated next-round prompt construction in the stop hook.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-140 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness contract for RLCR and PR loop setup scripts. It verifies argument parsing, plan validation, git/environment gates, YAML-safety checks, mutual exclusion, symlink defense, timeout behavior, full-review-round configuration, and skip-implementation mode.
- algorithmic_behavior: Builds isolated temp repos with helper functions, then runs `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh` with `CLAUDE_PROJECT_DIR` pointed at each temp repo at `tests/robustness/test-setup-scripts-robustness.sh:31-87`. The suite is grouped into RLCR argument parsing tests 1-8, plan validation tests 9-13, YAML safety tests 14-15, git edge cases 16-18, PR loop tests 19-23, mutual exclusion tests 24-25, symlink tests 26-27, positive progression tests 28-30, timeout tests 31-35, full-review-round tests 36-40, and skip-impl tests 41-45.
- inputs_outputs_state: Inputs are setup-script args such as positional plan, `--plan-file`, `--max`, `--codex-timeout`, `--codex-model`, `--full-review-round`, `--skip-impl`, and PR bot flags `--claude`/`--codex`. Outputs are pass/fail counters from `tests/test-helpers.sh` and final summary exit status at `tests/robustness/test-setup-scripts-robustness.sh:1073-1074`. In success/progression cases, setup scripts create `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, prompts, and for skip-impl `.review-phase-started`.
- gates_or_invariants: RLCR setup must reject duplicate plan specification, missing plan unless `--skip-impl`, non-git repos, repos without commits, absolute plan paths, spaces, shell metacharacters, symlink plan files or parent dirs, tracked plan files unless `--track-plan-file`, too-short plans, comment-only plans, missing Codex, YAML-unsafe branches, invalid Codex model/effort, active RLCR loops, active PR loops, invalid full-review-round values below 2, and nonnumeric numeric args. These are implemented in `scripts/setup-rlcr-loop.sh:123-231`, `scripts/setup-rlcr-loop.sh:247-297`, `scripts/setup-rlcr-loop.sh:304-556`, and `scripts/setup-rlcr-loop.sh:568-594`. PR setup must require at least one bot, reject invalid args, require git with a commit, require gh installed/authenticated, require Codex, and serialize YAML-safe state at `scripts/setup-pr-loop.sh:110-189`, `scripts/setup-pr-loop.sh:243-277`, and `scripts/setup-pr-loop.sh:381-407`.
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `scripts/portable-timeout.sh` at `tests/robustness/test-setup-scripts-robustness.sh:15-19`. Exercises `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, git, mocked `codex`, mocked `gh`, mocked `timeout`/`gtimeout`, and production loop-common active-loop detection. The timeout behavior is tied to `run_with_timeout`, which prefers `gtimeout`, GNU `timeout`, Python, then no timeout at `scripts/portable-timeout.sh:9-76`.
- edge_cases_or_failure_modes: Notable edge cases include `--max=-5` versus `--max -5`, empty numeric values, plan paths with semicolons, branch names containing `:`, Codex model injection via semicolon, plan files under symlinked parent dirs, timeout command returning 124 during git checks, zero timeout currently accepted despite “positive integer” wording, very large timeout accepted, `--full-review-round 1` rejected, and `--skip-impl` allowing no plan while still creating review-state artifacts. The test intentionally treats later dependency failures, such as missing Codex or gh auth failure, as proof that earlier parsing passed at `tests/robustness/test-setup-scripts-robustness.sh:668-739`.
- validation_or_tests: The file is itself the validation suite with 45 scenario tests. It asserts exact or substring-matched failure causes and verifies skip-impl filesystem state: `.review-phase-started`, `review_started: true`, no placeholder text in skip-impl goal tracker, and `SKIP-IMPL MODE` output at `tests/robustness/test-setup-scripts-robustness.sh:975-1067`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections present, matching the 5 assigned rows in order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`