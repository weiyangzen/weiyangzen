# agent_06 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`
- note: read-only research only; no files modified. The branch export did not expose usable git metadata in this sandbox, so evidence is from direct filesystem inspection.

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime shell orchestration layer for Humanize loops. `scripts/` owns setup/cancel entrypoints, GitHub PR comment/review collection, bot reaction/status probes, portable timeout wrapping, plan IO generation, and terminal monitors. It coordinates with command wrappers under `commands/`, hook validators/stop hooks under `hooks/`, and prompt contracts under `prompt-template/`.
- algorithmic_behavior: The directory contains 12 shell scripts totaling 206920 bytes. PR-loop path: `setup-pr-loop.sh` parses bot/model/timeout options, enforces one active loop, resolves fork-aware PR context, fetches comments, computes startup case, creates `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, and `round-0-prompt.md` (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:220`, `scripts/setup-pr-loop.sh:283`, `scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:721`). It delegates fetching to `fetch-pr-comments.sh`, startup classification to `check-pr-reviewer-status.sh`, and Claude eyes verification to `check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:430`, `scripts/setup-pr-loop.sh:437`, `scripts/setup-pr-loop.sh:587`). PR runtime then depends on `hooks/pr-loop-stop-hook.sh`, which calls `poll-pr-reviews.sh`, `check-bot-reactions.sh`, and `check-pr-reviewer-status.sh` to advance active bots and state files (`hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:998`, `hooks/pr-loop-stop-hook.sh:1547`). RLCR path: `setup-rlcr-loop.sh` validates plan paths/content, captures base branch/commit, initializes `.humanize/rlcr/<timestamp>/state.md`, supports `--skip-impl`, and emits the first round prompt (`scripts/setup-rlcr-loop.sh:270`, `scripts/setup-rlcr-loop.sh:327`, `scripts/setup-rlcr-loop.sh:490`, `scripts/setup-rlcr-loop.sh:620`, `scripts/setup-rlcr-loop.sh:716`, `scripts/setup-rlcr-loop.sh:872`). `humanize.sh` provides `humanize monitor rlcr|pr`, using `scripts/lib/monitor-common.sh` for status, phase, and goal-tracker parsing (`scripts/humanize.sh:6`, `scripts/humanize.sh:903`, `scripts/humanize.sh:951`; `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:267`, `scripts/lib/monitor-common.sh:390`).
- inputs_outputs_state: Inputs include CLI flags, git state, `gh` API data, PR numbers, bot lists, timestamps, plan files, and session directories. Outputs are markdown prompt/comment/summary/goal-tracker files, YAML-frontmatter state files, JSON polling/status objects, terminal monitor output, and state-file renames such as `cancel-state.md`, `approve-state.md`, `maxiter-state.md`, `finalize-state.md`, or `complete-state.md` via hooks/cancel scripts (`scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`; `scripts/cancel-rlcr-loop.sh:117`, `scripts/cancel-rlcr-loop.sh:138`; `hooks/pr-loop-stop-hook.sh:433`, `hooks/pr-loop-stop-hook.sh:445`; `hooks/loop-codex-stop-hook.sh:746`). `validate-gen-plan-io.sh` turns an input draft into a generated output plan by copying `prompt-template/plan/gen-plan-template.md` and appending the draft (`scripts/validate-gen-plan-io.sh:133`, `scripts/validate-gen-plan-io.sh:148`).
- gates_or_invariants: One loop type active at a time (`scripts/setup-pr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:247`). PR state stores `configured_bots` and mutable `active_bots` as YAML lists (`scripts/setup-pr-loop.sh:608`, `scripts/setup-pr-loop.sh:621`). Branch/model/effort fields are YAML-safety checked (`scripts/setup-pr-loop.sh:384`, `scripts/setup-rlcr-loop.sh:568`, `scripts/setup-rlcr-loop.sh:578`). Fork PR support requires resolving the base/upstream repo before PR operations (`scripts/setup-pr-loop.sh:290`, `scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`). RLCR plan files must be relative, readable, non-symlink, inside the project, not in a submodule, contentful, and either tracked-clean with `--track-plan-file` or untracked/gitignored otherwise (`scripts/setup-rlcr-loop.sh:327`, `scripts/setup-rlcr-loop.sh:353`, `scripts/setup-rlcr-loop.sh:401`, `scripts/setup-rlcr-loop.sh:415`, `scripts/setup-rlcr-loop.sh:454`, `scripts/setup-rlcr-loop.sh:490`). `portable-timeout.sh` normalizes timeout behavior across GNU/macOS/Python/no-timeout environments (`scripts/portable-timeout.sh:9`, `scripts/portable-timeout.sh:33`).
- dependencies_and_callers: `commands/start-pr-loop.md` and `commands/start-rlcr-loop.md` are user-facing wrappers for setup scripts; cancel command files wrap cancel scripts (`commands/start-pr-loop.md:4`, `commands/start-rlcr-loop.md:4`, `commands/cancel-pr-loop.md:3`, `commands/cancel-rlcr-loop.md:3`). Hooks source `hooks/lib/loop-common.sh`, which supplies active-loop discovery, YAML helpers, PR goal-tracker updates, and protection messages (`hooks/lib/loop-common.sh:157`, `hooks/lib/loop-common.sh:761`, `hooks/lib/loop-common.sh:773`, `hooks/lib/loop-common.sh:1174`). Scripts rely on `gh`, `jq`, `git`, `codex`, `stat`, `sed`, `awk`, `tail`, and optional `gtimeout`/`timeout`/Python.
- edge_cases_or_failure_modes: Network/API failures are retried and may degrade to empty arrays for fetch/poll (`scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:199`, `scripts/poll-pr-reviews.sh:207`, `scripts/poll-pr-reviews.sh:231`). Missing `gh`, `jq`, `codex`, no PR, closed/merged PR, missing trigger comment ID, Claude eyes timeout, PR in fork, stale trigger after force push, and unpushed commits are explicit gates across scripts/hooks (`scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:351`, `scripts/setup-pr-loop.sh:367`, `scripts/setup-pr-loop.sh:568`, `scripts/setup-pr-loop.sh:587`; `hooks/pr-loop-stop-hook.sh:721`, `hooks/pr-loop-stop-hook.sh:776`). Monitor code handles missing/deleted/truncated logs and session switches (`scripts/humanize.sh:661`, `scripts/humanize.sh:797`, `scripts/humanize.sh:817`, `scripts/humanize.sh:1323`).
- validation_or_tests: Covered by `tests/test-pr-loop.sh` modules for setup/cancel/fetch/poll argument gates, validators, stop-hook state transitions, fixtures, approval-only reviews, wrong-round protection, and monitor parsing (`tests/test-pr-loop.sh:18`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop-scripts.sh:405`, `tests/test-pr-loop-hooks.sh:26`, `tests/test-pr-loop-stophook.sh:20`). Additional coverage includes plan validation, base-branch detection, PR API robustness, timeout robustness, and monitor E2E tests referenced under `tests/robustness/` and `tests/test-gen-plan.sh`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-036 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: PR-loop data ingestion script. It materializes GitHub PR feedback into a deterministic markdown comment file consumed by `setup-pr-loop.sh` and later prompts.
- algorithmic_behavior: Parses `<pr_number> <output_file> [--after timestamp] [--bots bot1,bot2]`, validates numeric PR and required output path (`scripts/fetch-pr-comments.sh:27`, `scripts/fetch-pr-comments.sh:92`). Resolves current vs parent repo for fork PRs (`scripts/fetch-pr-comments.sh:130`, `scripts/fetch-pr-comments.sh:139`, `scripts/fetch-pr-comments.sh:143`). Fetches issue comments, inline review comments, and PR reviews through paginated GitHub API calls with 3 retries each (`scripts/fetch-pr-comments.sh:176`, `scripts/fetch-pr-comments.sh:184`, `scripts/fetch-pr-comments.sh:212`, `scripts/fetch-pr-comments.sh:216`, `scripts/fetch-pr-comments.sh:220`). Normalizes all sources into unified objects, substitutes `[Review state: STATE]` for empty approval-only review bodies, deduplicates by ID, filters by `created_at > --after`, and sorts humans before bots then newest first (`scripts/fetch-pr-comments.sh:260`, `scripts/fetch-pr-comments.sh:300`, `scripts/fetch-pr-comments.sh:323`, `scripts/fetch-pr-comments.sh:327`, `scripts/fetch-pr-comments.sh:337`).
- inputs_outputs_state: Inputs are GitHub CLI auth/context, PR number, optional timestamp, optional active bot list. Output is a markdown file beginning with PR metadata and ending with `*End of comments*` (`scripts/fetch-pr-comments.sh:245`, `scripts/fetch-pr-comments.sh:443`). It writes a sentinel `*No comments found.*` when the normalized comment set is empty; `setup-pr-loop.sh` uses that sentinel to select the no-comments startup task (`scripts/fetch-pr-comments.sh:348`, `scripts/setup-pr-loop.sh:723`).
- gates_or_invariants: Requires `gh` and `jq` (`scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:117`). Bot detection treats GitHub user type `Bot` or login suffix `[bot]` as bot (`scripts/fetch-pr-comments.sh:226`). Optional bot grouping maps `codex` to `chatgpt-codex-connector[bot]` and other names to `<bot>[bot]` (`scripts/fetch-pr-comments.sh:381`, `scripts/fetch-pr-comments.sh:386`). Sorting excludes entries with null `created_at` before `fromdateiso8601` to avoid jq parse errors (`scripts/fetch-pr-comments.sh:336`).
- dependencies_and_callers: Called directly by `scripts/setup-pr-loop.sh` when creating `round-0-pr-comment.md` (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:431`). Fixture tests call it with mocked `gh` (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1251`). Robustness tests exercise empty JSON, API failure, bot comments, unicode, long bodies, and pagination paths under `tests/robustness/test-pr-loop-api-robustness.sh`.
- edge_cases_or_failure_modes: If a fetch endpoint fails after retries, script writes `[]`, increments `API_FAILURES`, continues, and appends a warning to the markdown (`scripts/fetch-pr-comments.sh:199`, `scripts/fetch-pr-comments.sh:445`). This avoids aborting a PR loop on partial GitHub outage but risks incomplete feedback. Timestamp filtering is lexicographic on ISO strings (`scripts/fetch-pr-comments.sh:327`); it assumes comparable ISO 8601 UTC values. Deduplication by raw ID may merge objects from different API domains if IDs collide, though GitHub IDs are generally globally unique enough for this use (`scripts/fetch-pr-comments.sh:322`).
- validation_or_tests: Direct argument validation is in `tests/test-pr-loop-scripts.sh:259`, including help, missing PR, missing output, and invalid PR (`tests/test-pr-loop-scripts.sh:268`, `tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`). Fixture validation asserts all comment types and approval-only review placeholders are present and that `--after` excludes older comments (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1287`).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-066 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Executable top-level PR-loop specification runner. It composes the PR-loop test suite rather than implementing runtime behavior itself.
- algorithmic_behavior: Enables strict shell mode, derives `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared helpers and PR-loop library, initializes a test environment, sources three test modules, runs each module, and prints a summary (`tests/test-pr-loop.sh:13`, `tests/test-pr-loop.sh:15`, `tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:40`, `tests/test-pr-loop.sh:53`).
- inputs_outputs_state: Input is the repository checkout plus test fixtures/mocks. State is a temporary PR-loop test environment created by `init_pr_loop_test_env` (`tests/test-pr-loop.sh:26`). Output is test pass/fail reporting through helper functions and `print_test_summary` (`tests/test-pr-loop.sh:53`).
- gates_or_invariants: The runner assumes `test-helpers.sh` and `test-pr-loop-lib.sh` define the test harness before modules load (`tests/test-pr-loop.sh:18`). It establishes module order: script argument validation first, hook functionality second, stop-hook transitions last (`tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`, `tests/test-pr-loop.sh:44`, `tests/test-pr-loop.sh:47`).
- dependencies_and_callers: Sources `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:32`). Those modules execute production scripts and hooks including `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, and `scripts/humanize.sh`.
- edge_cases_or_failure_modes: Because it uses `set -euo pipefail`, any unsafely written module or missing sourced file can abort the entire suite before summary. The runner itself has no skip/selection logic; all modules run unconditionally (`tests/test-pr-loop.sh:40`). It depends on helper cleanup isolation to prevent temporary `.humanize` state from bleeding across tests.
- validation_or_tests: The runner validates the PR-loop subsystem indirectly. Script tests cover CLI parsing and missing required options (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`). Hook tests cover protected files, sorting, deduplication, YAML list parsing, bot re-add logic, fixture-backed fetch/poll behavior, wrong-round blocking, and monitor parsing (`tests/test-pr-loop-hooks.sh:26`, `tests/test-pr-loop-hooks.sh:222`, `tests/test-pr-loop-hooks.sh:271`, `tests/test-pr-loop-hooks.sh:1221`, `tests/test-pr-loop-hooks.sh:1422`, `tests/test-pr-loop-hooks.sh:1537`). Stop-hook tests cover force-push trigger invalidation, startup-case exceptions, approval state creation, unpushed commit blocking, dynamic startup-case updates, fork PR resolution, and mixed approval behavior (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:246`, `tests/test-pr-loop-stophook.sh:452`, `tests/test-pr-loop-stophook.sh:1390`, `tests/test-pr-loop-stophook.sh:1519`).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-096 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for outdated RLCR state schemas. It defines the user-facing contract when a loop state file lacks required fields and the system refuses to continue.
- algorithmic_behavior: Static markdown with one placeholder, `{{FIELD_NAME}}`, naming the missing state field (`prompt-template/block/schema-outdated.md:3`). The message tells the user the session was started with an older Humanize version and gives remediation options: cancel session, update plugin to version 1.1.2+, restart (`prompt-template/block/schema-outdated.md:5`, `prompt-template/block/schema-outdated.md:7`). It declares the session will terminate as `unexpected` to preserve state information (`prompt-template/block/schema-outdated.md:12`).
- inputs_outputs_state: Input is a template variable `FIELD_NAME`; output is rendered markdown for hook/block responses. State transition described by the template is terminal: preserve information by ending the session as unexpected, matching the RLCR stop-hook’s schema-failure behavior (`hooks/loop-codex-stop-hook.sh:132`, `hooks/loop-codex-stop-hook.sh:137`).
- gates_or_invariants: The invariant is schema compatibility: required frontmatter fields must exist before the loop may proceed. `hooks/loop-codex-stop-hook.sh` explicitly checks critical fields before relying on defaults (`hooks/loop-codex-stop-hook.sh:92`, `hooks/loop-codex-stop-hook.sh:132`). It also blocks older schemas missing `plan_tracked`, `start_branch`, `review_started`, or `base_branch` (`hooks/loop-codex-stop-hook.sh:164`, `hooks/loop-codex-stop-hook.sh:183`, `hooks/loop-codex-stop-hook.sh:197`).
- dependencies_and_callers: Loaded through the template system in `hooks/lib/template-loader.sh`, which uses single-pass placeholder replacement and keeps missing placeholders intact (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`). Direct literal references to this file were not found in the inspected hook snippets; the active schema checks currently inline similar messages in `hooks/loop-codex-stop-hook.sh`, making this template a shared/fallback block contract rather than the only implementation surface.
- edge_cases_or_failure_modes: If `FIELD_NAME` is not supplied, the placeholder remains due to single-pass renderer behavior (`hooks/lib/template-loader.sh:120`). The hard-coded “version 1.1.2+” guidance may be stale for newer schema fields such as `review_started`/`base_branch` introduced later, while the hook’s inline messages mention pre-1.5.0 cases (`hooks/loop-codex-stop-hook.sh:183`, `hooks/loop-codex-stop-hook.sh:197`).
- validation_or_tests: Schema validation behavior is covered by state-transition and robustness tests that exercise missing/malformed state fields via `loop-common`/stop-hook paths, including strict parsing requirements for `review_started` and `base_branch` in `hooks/lib/loop-common.sh:250` and `hooks/lib/loop-common.sh:303`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-126 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task prompt for the “comments already exist” startup path. It tells the active Claude session how to handle fetched PR feedback and what artifacts/actions are required before the stop hook can continue.
- algorithmic_behavior: The template instructs the worker to analyze comments, prioritize human feedback over bots, fix reviewer issues, add tests when needed, commit/push changes, post a bot-mention PR comment, and write a resolution summary to `@{{RESOLVE_PATH}}` (`prompt-template/pr-loop/round-0-task-has-comments.md:6`, `prompt-template/pr-loop/round-0-task-has-comments.md:10`, `prompt-template/pr-loop/round-0-task-has-comments.md:15`, `prompt-template/pr-loop/round-0-task-has-comments.md:23`). It embeds the required trigger command using `{{PR_NUMBER}}` and `{{BOT_MENTION_STRING}}` (`prompt-template/pr-loop/round-0-task-has-comments.md:20`). It also tells the user that the stop hook will poll every 30 seconds for up to 15 minutes per bot, validate approvals with local Codex, loop on remaining issues, and end when all bots approve (`prompt-template/pr-loop/round-0-task-has-comments.md:39`).
- inputs_outputs_state: Inputs are rendered variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, supplied by `setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:731`). Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after fetched comments when the comment sentinel is absent (`scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:801`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:851`). User-created output required by the prompt is `round-0-pr-resolve.md`; pushed commits and PR trigger comments are external outputs required for bot review.
- gates_or_invariants: The prompt forbids modifying `.humanize/pr-loop/` state files, requires pushing changes, requires the correct bot mention format, and requires thorough handling of valid reviewer concerns (`prompt-template/pr-loop/round-0-task-has-comments.md:30`). These textual rules align with hook validators blocking state/comment/prompt writes while allowing the correct-round `round-N-pr-resolve.md` write (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`, `tests/test-pr-loop-hooks.sh:1431`).
- dependencies_and_callers: Loaded by `setup-pr-loop.sh` through `load_and_render_safe` when `COMMENT_COUNT != 0` (`scripts/setup-pr-loop.sh:766`, `scripts/setup-pr-loop.sh:801`, `scripts/setup-pr-loop.sh:847`). The stop hook later enforces the implied workflow by requiring trigger comments for re-review cases, polling bot reviews, running local Codex validation, and updating `active_bots` (`hooks/pr-loop-stop-hook.sh:721`, `hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:1243`, `hooks/pr-loop-stop-hook.sh:1416`).
- edge_cases_or_failure_modes: If variables are missing, placeholders remain in the rendered prompt due to template-loader behavior (`hooks/lib/template-loader.sh:120`). The sample `gh pr comment` omits `--repo`, while setup/stop-hook internals use base-repo resolution for fork support; a user running the shown command from a fork checkout may need repo context despite the prompt not showing it (`scripts/setup-pr-loop.sh:526`, `scripts/fetch-pr-comments.sh:126`). The prompt assumes comments are actionable; human comments explicitly take precedence, so conflicting bot vs human feedback requires human-first judgment (`prompt-template/pr-loop/round-0-task-has-comments.md:6`).
- validation_or_tests: Template use is indirectly covered by setup and template-reference tests. `tests/test-pr-loop-scripts.sh` checks setup argument flows, while PR-loop hook tests verify protected state files, allowed resolve files, wrong-round blocking, trigger detection, and fixture-backed fetch/poll behavior that this prompt depends on (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:135`, `tests/test-pr-loop-hooks.sh:390`, `tests/test-pr-loop-hooks.sh:1221`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 section headers; IDs intentionally appear only in their item headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`