# agent_26 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Stop-hook controller for the RLCR loop. It intercepts exit attempts, validates loop invariants, runs Codex review, advances rounds, enters review/finalize phases, or allows loop completion. It is registered as a `Stop` hook in `hooks/hooks.json:52-59` with a 7200s timeout.
- algorithmic_behavior: The hook reads JSON hook input from stdin (`hooks/loop-codex-stop-hook.sh:29`), finds the newest active `.humanize/rlcr/<timestamp>` loop via `find_active_loop` (`:43-60`), selects normal `state.md` or finalize `finalize-state.md` (`:72-82`), parses state frontmatter (`:88-117`), then runs fail-closed prechecks before invoking Codex. Normal implementation rounds build a Codex prompt (`:763-849`), execute `codex exec` with configured model/effort/timeout (`:893-899`, `:1273-1275`), require a non-empty review result (`:1332-1385`), and interpret the final non-empty line as `COMPLETE` or `STOP` only when exactly matched (`:1390-1397`, `:1473-1500`). Non-complete feedback increments `current_round`, writes the next prompt, optionally injects Open Question handling, and returns a blocking JSON response (`:1507-1608`).
- inputs_outputs_state: Inputs are stdin hook JSON, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, round summary files, plan backup, current git branch/status, task transcript/session data, Codex CLI output, and prompt templates. Outputs are Claude hook JSON decisions (`decision: block` for continued work), generated review prompt/result files, cache audit files under `$XDG_CACHE_HOME/humanize/<sanitized-project>/<loop>/`, state file mutations, and terminal state files. State transitions include: no active loop -> exit 0 (`:61-64`); corrupt/outdated state -> `unexpected-state.md` via `end_loop` or block message (`:121-175`, `:183-208`); implementation `COMPLETE` -> set `review_started: true`, touch `.review-phase-started`, run code review (`:1415-1435`); code-review no issues -> rename to `finalize-state.md` and block with finalize prompt (`:1001-1029`, `:1034-1115`); finalize checks pass -> rename to `complete-state.md` and allow exit (`:744-749`); `STOP` -> `stop-state.md` (`:1473-1500`); max iterations -> `maxiter-state.md` (`:727-735`, `:1404-1408`).
- gates_or_invariants: Required state fields are enforced beyond tolerant parsing: `current_round`, `max_iterations`, `plan_tracked`, `start_branch`, `review_started`, and `base_branch` must exist and be valid (`:132-208`). Codex model/effort are regex-restricted for YAML safety (`:119-130`). Branch must remain the start branch (`:228-255`). Before review, the plan backup must exist and match the original plan unless already in review phase (`:264-338`). Incomplete Claude tasks block before Codex (`:347-396`). Git status failures fail closed (`:405-427`), changed code/doc files over 2000 lines block (`:429-510`), dirty git state blocks (`:519-572`), and unpushed commits block when `push_every_round=true` (`:574-604`). Summary file existence is mandatory (`:611-643`). Round 0 goal tracker placeholders block initialization (`:645-721`). Review phase cannot be manually toggled because `.review-phase-started` must exist (`:1448-1464`). Codex review failure or empty output is a hard block, not a skip (`:1007-1024`, `:1165-1238`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `find_active_loop`, state parsers, marker constants, `detect_review_issues`, and `end_loop` (`hooks/loop-codex-stop-hook.sh:46-52`; helper definitions in `hooks/lib/loop-common.sh:31-45`, `:157-175`, `:211-248`, `:338-398`, `:1118-1151`). Uses `scripts/portable-timeout.sh` for bounded git/Codex execution (`hooks/loop-codex-stop-hook.sh:50-55`). Calls `hooks/check-todos-from-transcript.py` to merge legacy TodoWrite and new task-directory checks (`hooks/loop-codex-stop-hook.sh:347-395`; Python behavior at `hooks/check-todos-from-transcript.py:148-193`). Renders templates through `load_and_render_safe`, including assigned `prompt-template/codex/code-review-phase.md` at `hooks/loop-codex-stop-hook.sh:950-956`. The stop hook is one member of a hook suite with pre-tool validators in `hooks/hooks.json:14-50`.
- edge_cases_or_failure_modes: Missing active state allows exit, which is intentional (`:61-82`). Missing `full_review_round` only warns and defaults to 5 (`:217-222`), while invalid values are clamped to 5 (`:775-784`). `BASE_COMMIT` is preferred over `BASE_BRANCH` for `codex review --base`, avoiding self-comparison when the branch advances (`:923-930`, `:979-980`). If Codex writes to stdout instead of the review file, stdout is copied to the expected file (`:1332-1348`). Marker detection only checks the final non-empty line, preventing false positives such as “CANNOT COMPLETE” (`:1390-1395`). `STOP` outside a full-alignment round is still honored but logged as unexpected (`:1473-1499`). `ASK_CODEX_QUESTION=true` uses a fragile heuristic: line contains “Open Question” and is under 40 chars (`:1530-1543`). The script itself is 1609 lines, below its own 2000-line large-file threshold but close enough to be a maintenance risk.
- validation_or_tests: `tests/test-finalize-phase.sh` exercises finalize summary blocking, git-clean blocking, finalize completion without Codex invocation, empty Codex review output blocking, incomplete todos blocking, and normal round continuation (`tests/test-finalize-phase.sh:429-500`, `:630-697`, `:699-805`). `tests/test-codex-review-merge.sh` directly validates `detect_review_issues`: full-log scan, first-10-character severity matching, missing/empty log hard errors, extraction from first issue, and result-file creation (`tests/test-codex-review-merge.sh:1-16`, `:57-120`, `:133-169`, `:171-355`). Robustness and plan-file tests also invoke this hook repeatedly, visible in repository references.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for shell-escape and local-state protection behavior, focused on preventing `.humanize` state from being staged and ensuring loop/session discovery patterns are safe in empty or dotfile-only directories.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` (`tests/test-humanize-escape.sh:16-18`), defines `assert_blocks`/`assert_allows` wrappers around `git_adds_humanize` after lowercasing commands (`:44-70`), then enumerates allowed and blocked command forms. It verifies direct `.humanize` path variants (`:83-87`), quoted variants (`:96-100`), forced broad adds (`:109-119`), all-add forms when `.humanize` exists (`:128-144`), chained commands (`:153-155`), `git -C`/`--git-dir` forms (`:164-166`), and allowed non-dangerous commands (`:175-192`). The second half models the find-based directory iteration patterns used by the plugin and asserts zero-match cases do not throw shell glob errors (`:201-294`) plus timestamped session detection (`:303-332`).
- inputs_outputs_state: Inputs are hard-coded shell command strings and temporary directories under `/tmp`. The test mutates only ephemeral test directories such as `/tmp/test-humanize-git-add-$$` and `/tmp/test-humanize-empty-$$` (`:130-144`, `:204-215`). Outputs are colored PASS/FAIL lines, aggregate counters, and process exit code 0 only when all assertions pass (`:334-352`). It does not alter RLCR state.
- gates_or_invariants: Core invariant is that any `git add` capable of staging `.humanize` loop state must return true from `git_adds_humanize` and therefore be blockable by `loop-bash-validator.sh`. Direct references should be blocked independent of whether `.humanize` exists; `-A`/`--all` require a real `.humanize` directory for the tested non-force path (`:128-140`). Forced broad-scope adds are blocked because they bypass ignore rules (`:113-119`). Allowed invariant: specific source paths, `.gitignore`, patch mode, and similarly named files like `.humanizeconfig` must not be overblocked (`:175-192`). Directory discovery invariant: use `find` loops rather than unmatched globs for empty, dotfile-only, and no-state cases (`:201-294`).
- dependencies_and_callers: Depends on `git_adds_humanize` and `to_lower` from `hooks/lib/loop-common.sh`. The helper’s detector splits chained commands, recognizes `git ... add` with git options, strips quotes, blocks direct `.humanize`, force/all/broad-scope combinations, and consults `git check-ignore` for broad non-force adds (`hooks/lib/loop-common.sh:914-1032`). The validator caller is `hooks/loop-bash-validator.sh`, which invokes `git_adds_humanize` for every Bash command while any RLCR or PR loop is active and exits 2 with the block message if true (`hooks/loop-bash-validator.sh:109-118`). The test is included in the aggregate runner at `tests/run-all-tests.sh:47-49`.
- edge_cases_or_failure_modes: The test lowercases input before invoking the detector, matching production behavior but also requiring the detector to treat `-A` as `-a` (`hooks/lib/loop-common.sh:918-919`). It exercises quotes but not escaped spaces, shell arrays, command substitution, or multiline shell syntax. `git_adds_humanize` splits on shell operators with simple `sed`, so complex quoted operators may be approximated. For broad `git add .`, behavior depends on the current working directory and whether `.humanize` exists and is ignored (`hooks/lib/loop-common.sh:1011-1027`); the test only creates a temporary `.humanize` for the `-A` group. Session detection in this file is a local simulation and not a direct call to `find_active_loop`.
- validation_or_tests: This file is itself the validation artifact. It covers seven command/path groups plus zsh-safe iteration and session detection, then returns nonzero if any assertion fails (`tests/test-humanize-escape.sh:72-352`). Related tests broaden the surface: `tests/test-bash-validator-patterns.sh` validates file-modification pattern detection, and robustness/session tests exercise the real loop discovery helper.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the goal-tracker ownership invariant: after round 0, Claude must not directly modify `goal-tracker.md`; change requests must go through the round summary for Codex review.
- algorithmic_behavior: The template renders a round-specific block title (`prompt-template/block/goal-tracker-modification.md:1`), states that only Codex can modify the tracker after round 0 (`:3-5`), and gives the required summary section format (`:7-23`). It is not executable itself; its algorithmic role is contract text used by validators to redirect a forbidden direct edit into an auditable “Goal Tracker Update Request” flow.
- inputs_outputs_state: Inputs are `CURRENT_ROUND` and `SUMMARY_FILE`, passed by `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:1104-1116`). Output is rendered Markdown sent to stderr by Write/Edit/Bash validators when they block. It does not mutate files, but it shapes the next allowed state transition: Claude writes the requested tracker change into the current round summary, then Codex may apply justified updates during review via the goal-tracker update section loaded by the stop hook (`hooks/loop-codex-stop-hook.sh:769-773`).
- gates_or_invariants: Direct Write/Edit to goal-tracker after round 0 is forbidden (`hooks/loop-write-validator.sh:188-196`, `hooks/loop-edit-validator.sh:141-149`). Bash modifications to goal-tracker are always blocked; round 0 Bash gets a Write/Edit instruction, while later rounds render this assigned template (`hooks/loop-bash-validator.sh:343-357`). The invariant preserves Codex as the single post-round-0 tracker writer and keeps tracker evolution justified against the Ultimate Goal.
- dependencies_and_callers: Rendered by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1104-1116`. Called by `loop-write-validator.sh` for Write paths (`:192-195`), `loop-edit-validator.sh` for Edit paths (`:145-148`), and `loop-bash-validator.sh` for Bash commands (`:349-356`). Template existence is covered by `tests/test-template-references.sh:152-164`.
- edge_cases_or_failure_modes: The template relies on validators to identify the correct current round and summary path; malformed state causes validators to fail closed before rendering (`hooks/loop-write-validator.sh:140-145`, `hooks/loop-edit-validator.sh:105-110`, `hooks/loop-bash-validator.sh:81-85`). The text says “only Codex can modify” but cannot enforce by itself; enforcement is distributed across Write/Edit/Bash validators. It does not mention finalize phase separately, so any post-round-0 goal-tracker block uses the same request path.
- validation_or_tests: Cross-reference validation checks that `block/goal-tracker-modification.md` exists (`tests/test-template-references.sh:152-164`). Hook-system robustness includes Bash goal-tracker blocking after round 0 (`tests/robustness/test-hook-system-robustness.sh` references this behavior around its goal-tracker modification test). Write/Edit/Bash validators provide the live enforcement surface.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-116 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit prompt template documenting the Codex code-review phase. It records review configuration and expected review-output semantics for generated `round-N-review-prompt.md` audit files.
- algorithmic_behavior: The template states the phase is audit-only because `codex review` does not accept prompt input (`prompt-template/codex/code-review-phase.md:3-4`). It describes the four-step review algorithm: run `codex review --base`, scan for severity markers, send fixes back to Claude if issues exist, otherwise transition to Finalize Phase (`:12-18`). It documents expected issue formatting with `[P0]`/`[P1]` style markers (`:19-28`) and generated files (`:30-36`).
- inputs_outputs_state: Inputs are `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP` as used by the current template (`prompt-template/codex/code-review-phase.md:1-10`). The hook passes additional values too: `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE` (`hooks/loop-codex-stop-hook.sh:950-956`), but this template does not currently render them. Output is `round-<review_round>-review-prompt.md` in the loop directory (`hooks/loop-codex-stop-hook.sh:935-956`). Actual review execution writes a command audit file and combined log in cache (`:932-970`) and then `round-N-review-result.md` only if issues are extracted by `detect_review_issues` (`hooks/lib/loop-common.sh:355-398`).
- gates_or_invariants: The template’s severity-marker contract aligns with the actual detector, but the implementation is stricter than prose: `detect_review_issues` only treats `[P0-9]` markers in the first 10 characters of a line as actionable (`hooks/lib/loop-common.sh:347-378`). Review success is absence of those markers in a non-empty log, not a textual `COMPLETE` marker (`hooks/loop-codex-stop-hook.sh:1013-1029`). Review failure or missing/empty output blocks and requires retry (`:1007-1024`, `:1165-1238`).
- dependencies_and_callers: Called exclusively by `run_codex_code_review` in `hooks/loop-codex-stop-hook.sh:918-986`. The actual command uses `review_base="${BASE_COMMIT:-$BASE_BRANCH}"` (`:923-930`) and runs `codex review --base "$review_base"` from `PROJECT_ROOT` (`:975-980`). `detect_review_issues` in `hooks/lib/loop-common.sh:338-398` consumes the combined log. Related tests are `tests/test-codex-review-merge.sh` and review/finalize sections of `tests/test-finalize-phase.sh`.
- edge_cases_or_failure_modes: The template appears partly stale relative to implementation. It documents `codex review --base {{BASE_BRANCH}}` (`prompt-template/codex/code-review-phase.md:14`) while the hook may use a captured base commit (`hooks/loop-codex-stop-hook.sh:923-930`). It lists `round-{{REVIEW_ROUND}}-codex-review.out` (`prompt-template/codex/code-review-phase.md:35`), but the implementation captures both stdout and stderr into `round-N-codex-review.log` and does not create a `.out` file for `codex review` (`hooks/loop-codex-stop-hook.sh:932-980`). Because this is audit text, the stale fields do not change execution, but they can mislead debugging evidence.
- validation_or_tests: The template is indirectly covered when `run_codex_code_review` writes the audit prompt during stop-hook tests. `tests/test-codex-review-merge.sh` validates the marker-scan algorithm documented by the template (`tests/test-codex-review-merge.sh:1-16`, `:57-355`). `tests/test-finalize-phase.sh` validates that empty review output blocks and preserves review state (`tests/test-finalize-phase.sh:630-697`), and that successful finalize/review transitions do not invoke unnecessary Codex calls in finalize (`:429-500`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`