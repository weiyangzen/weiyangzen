# agent_29 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Read` gate for RLCR loop files. It prevents Claude from reading stale, misplaced, or wrong-session RLCR round files while allowing normal reads and generally allowing PR-loop files. It is registered as the `Read` PreToolUse hook in `hooks/hooks.json:33-39`.
- algorithmic_behavior: The validator reads hook JSON from stdin, validates it with shared helpers, ignores non-`Read` tools, then enforces path/round/session rules for `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md`. Flow: parse input at `hooks/loop-read-validator.sh:25-49`; block non-allowlisted todos reads at `:55-62`; allow non-round summary/prompt reads at `:69-71`; find active RLCR loop and parse `state.md` or `finalize-state.md` at `:80-99`; extract requested round from filename at `:105-116`; then validate location, round number, and exact active-loop directory at `:122-168`.
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path` (`hooks/loop-read-validator.sh:25-49`). Output is either silent `exit 0` allow, `exit 1` for malformed hook/state input, or `exit 2` with a rendered block message on stderr for policy violations. It reads loop state from `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/rlcr/<newest>/state.md` or `finalize-state.md` (`:80-92`) and stores the parsed current round in `CURRENT_ROUND` (`:95-99`). It does not mutate loop state.
- gates_or_invariants: Requires valid JSON and a `tool_input.file_path` for `Read` (`hooks/loop-read-validator.sh:27-46`), and rejects JSON nested deeper than 30 levels (`:32-35`). Todos files are blocked unless explicitly allowlisted in the active loop (`:55-62`). Summary/prompt files must be in `.humanize/rlcr/` (`:122-131`), must match `CURRENT_ROUND` unless allowlisted (`:138-150`), and must exactly equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` (`:157-168`). Malformed active state fails closed (`:94-98`). The allowlist is intentionally narrow: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in `hooks/lib/loop-common.sh:426-447`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, `to_lower`, `is_round_file_type`, `find_active_loop`, `parse_state_file_strict`, `extract_round_number`, `is_allowlisted_file`, and rendering helpers (`hooks/loop-read-validator.sh:17-19`; helper definitions at `hooks/lib/loop-common.sh:60-134`, `:157-175`, `:250-336`, `:400-457`). The template loader is sourced transitively and resolves `prompt-template` via `hooks/lib/template-loader.sh:24-31`; block messages use `block/wrong-file-location.md`, `block/wrong-round-file.md`, and `block/wrong-directory-path.md` at `hooks/loop-read-validator.sh:127`, `:144`, and `:163`.
- edge_cases_or_failure_modes: If no active loop exists, summary/prompt reads are allowed (`hooks/loop-read-validator.sh:84-86`), and todos reads are still blocked unless no active loop plus non-allowlisted condition triggers the todos block (`:55-62`). If filename does not contain a parseable round number, the validator allows (`:105-108`), so enforcement is focused on recognized round file names. `finalize-state.md` takes precedence over `state.md` (`:88-92`). Path classification is regex-based through `is_in_humanize_loop_dir`, which checks for `.humanize/rlcr/` substring (`hooks/lib/loop-common.sh:718-722`); exact directory equality is then enforced for recognized round files (`hooks/loop-read-validator.sh:157-168`). Case-insensitive file type detection comes from lowercasing the full path (`:48-49`) and matching `round-[0-9]+-TYPE.md` (`hooks/lib/loop-common.sh:405-412`).
- validation_or_tests: Direct coverage appears in `tests/test-allowlist-validators.sh`, including read allowlist cases for `round-1-todos.md` and historical `round-0-summary.md`, plus blocking of non-allowlisted `round-3-todos.md` and `round-3-summary.md` (`tests/test-allowlist-validators.sh:234-283`). Finalize-state parsing is tested in `tests/test-finalize-phase.sh:839-850`. Hook input robustness for this validator is exercised in `tests/robustness/test-hook-input-robustness.sh` references found at lines covering valid JSON, invalid JSON, missing fields, deep nesting, and null-byte cases.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file immutability and hook safety. It defines expected behavior across the UserPromptSubmit plan validator, Write/Edit/Bash PreToolUse validators, and the Stop hook’s plan-integrity/goal-tracker gates.
- algorithmic_behavior: The test builds isolated temp git repos, stubs `codex`, creates `.humanize/rlcr/<timestamp>` sessions, writes state frontmatter, then invokes the real hook scripts with synthetic hook JSON. Setup and mock Codex are defined at `tests/test-plan-file-hooks.sh:30-58`; `setup_test_loop` initializes a git repo, creates `plans/test-plan.md`, copies it to loop backup `plan.md`, and writes v1.5.0+ state fields at `:63-114`. Tests then assert exit codes and output substrings rather than internal implementation details.
- inputs_outputs_state: Inputs are temporary filesystem state, git state, hook JSON, and generated `state.md` frontmatter. Outputs are PASS/FAIL counters and shell exit status equal to `TESTS_FAILED` (`tests/test-plan-file-hooks.sh:1108-1117`). The test intentionally mutates temp loop state and temp git repositories to model branch changes, plan edits/deletions, committed plan changes, schema downgrades, and placeholder goal tracker content, but it does not modify the repository under test.
- gates_or_invariants: It establishes these invariants: valid plan-loop state passes the UserPromptSubmit hook (`tests/test-plan-file-hooks.sh:119-147`); malformed/outdated state is blocked (`:149-189`); branch switching is blocked (`:194-217`); loop backup `plan.md` cannot be written/edited/modified via Write, Edit, Bash, direct `.humanize/rlcr/plan.md`, or shell expansion bypasses (`:226-367`); quoted YAML fields and hyphenated paths are parsed correctly (`:373-499`); Stop hook blocks modified, deleted, missing-backup, uncommitted tracked, and committed tracked plan-file changes (`:509-775`); goal tracker placeholder reporting is section-specific (`:781-1062`); legacy `.humanize-loop.local` paths are not treated as active RLCR paths (`:1064-1106`).
- dependencies_and_callers: Calls `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` throughout the file. It relies on git commands in temp repos and on a PATH-prepended mock `codex` binary (`tests/test-plan-file-hooks.sh:38-55`) so Stop hook paths can execute without invoking the real Codex CLI. The tested hooks depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `scripts/portable-timeout.sh`; specific connections include plan validator schema/branch/tracking checks in `hooks/loop-plan-file-validator.sh:44-229`, Bash plan-backup blocking in `hooks/loop-bash-validator.sh:336-341`, and Stop hook plan integrity in `hooks/loop-codex-stop-hook.sh:264-338`.
- edge_cases_or_failure_modes: Covers command-injection/bypass classes around plan backup paths: command substitution `$()` (`tests/test-plan-file-hooks.sh:304-315`), glob expansion (`:317-328`), brace expansion (`:330-341`), piped `tee` (`:343-354`), and backticks (`:356-367`). It covers YAML quote stripping for `start_branch` and `plan_file` (`:373-467`), paths with hyphens (`:469-499`), old state schemas missing `plan_tracked`, `review_started`, or `base_branch` (`:149-189`, `:673-695`), tracked-plan race conditions using both `git status` and content diff (`:602-775`), and section-specific placeholder false positives in goal tracker parsing (`:781-1062`). It also documents a negative boundary: `.humanize-loop.local` is legacy and allowed (`:1064-1106`).
- validation_or_tests: This file is itself the validation asset. It complements narrower plan path validation in `tests/test-plan-file-validation.sh`, allowlist/read-validator behavior in `tests/test-allowlist-validators.sh`, Bash pattern matching in `tests/test-bash-validator-patterns.sh`, and robustness coverage under `tests/robustness/`. I inspected it directly; I did not execute it because this run was requested as read-only branch research notes.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-089 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the large-file gate. It defines the user-facing contract when changed code or documentation files exceed the RLCR line-count threshold.
- algorithmic_behavior: The template renders a blocking message headed “Large Files Detected,” listing files passed through `{{LARGE_FILES}}` and the configured `{{MAX_LINES}}` threshold (`prompt-template/block/large-files.md:1-4`). It gives separate required actions for code files and documentation files (`:11-24`) and instructs the agent to split, preserve functionality or narrative coherence, update references/navigation, commit, and retry exit (`:13-25`).
- inputs_outputs_state: Inputs are template variables `MAX_LINES` and `LARGE_FILES`. The Stop hook computes `MAX_LINES=2000` (`hooks/loop-codex-stop-hook.sh:430-436`), scans changed git-status entries for code/doc extensions (`:437-486`), builds bullet entries with filename, line count, and type (`:482-485`), then renders this template into a JSON block reason (`:488-508`). Output is not a file write from the template itself; it becomes the Stop hook’s `reason` string in JSON.
- gates_or_invariants: Enforces a maintainability invariant before Codex review: changed code or docs above the line limit block loop exit. Code files must be split into modules under the threshold with “strictly unchanged” functionality (`prompt-template/block/large-files.md:13-17`). Documentation must be split into logical sections under the threshold with cross-references and table-of-contents/navigation updates (`:19-24`). The surrounding hook applies the check only when git status is available in a repo and only to changed tracked/new files with recognized extensions (`hooks/loop-codex-stop-hook.sh:437-486`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` at `hooks/loop-codex-stop-hook.sh:496-498`. Rendering is handled by `hooks/lib/template-loader.sh`, whose placeholder system is single-pass and preserves unresolved placeholders (`hooks/lib/template-loader.sh:7-13`, `:50-132`, `:167-203`). The template is part of the broader Stop hook quick-gate sequence before summary/review transitions.
- edge_cases_or_failure_modes: If the template is missing or empty, the Stop hook fallback is shorter and less prescriptive (`hooks/loop-codex-stop-hook.sh:488-495`). The large-file scan skips deleted files, unknown extensions, and nonnumeric `wc -l` results (`:454-480`). Because it scans `git status --porcelain`, unchanged large files are not blocked by this gate (`:440-486`). The instruction to use `code-simplifier` is conditional on plugin availability (`prompt-template/block/large-files.md:16`), so absence of that plugin is not itself a blocking condition.
- validation_or_tests: Template existence/reference coverage appears in template reference tests (`tests/test-template-references.sh` references `block/large-files.md` indirectly through hook template validation). The actual algorithmic caller is the Stop hook large-file gate at `hooks/loop-codex-stop-hook.sh:430-508`. I did not find a dedicated assigned test section in `tests/test-plan-file-hooks.sh` for large-file detection; this template is nevertheless core to the Stop hook’s pre-review validation behavior.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-119 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex review prompt template for non-full-alignment RLCR implementation rounds. It defines the normal review contract that decides whether the loop advances, requests another round, or eventually enters the review/finalize phases.
- algorithmic_behavior: The template instructs Codex to first read the original plan (`@{{PLAN_FILE}}`) before reviewing Claude’s claimed work (`prompt-template/codex/regular-review.md:3-12`). It embeds Claude’s summary via `{{SUMMARY_CONTENT}}` (`:13-18`), requires a deep implementation review for discrepancies and incomplete/deferred work (`:20-31`), mandates a goal-alignment check against `@{{GOAL_TRACKER_FILE}}` (`:33-45`), includes a rendered goal-tracker update section (`:47`), and defines output semantics: write comments to `@{{REVIEW_RESULT_FILE}}` when work is incomplete and only put `COMPLETE` on the last line if all original plan tasks and ACs are fully done with no deferrals or pending work (`:49-57`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, passed by `hooks/loop-codex-stop-hook.sh:836-848`. Output is a rendered `round-N-review-prompt.md` in the active loop directory (`hooks/loop-codex-stop-hook.sh:763-765`, `:836-848`). Codex then consumes that prompt via `codex exec`; its result is expected at `round-N-review-result.md` or copied from stdout (`:1250-1388`).
- gates_or_invariants: The key invariant is strict completion: `COMPLETE` is allowed only when every original plan task is done, all acceptance criteria are met, and there are no deferred or pending tasks (`prompt-template/codex/regular-review.md:51-57`). Deferred tasks are explicitly incomplete, and Codex must not accept Claude’s deferral as final (`:24-31`, `:53-56`). Goal tracker alignment is mandatory, including AC progress, forgotten items, deferral justification, and plan evolution validity (`:33-45`). The Stop hook interprets the final non-empty result line exactly: only a line equal to `COMPLETE` transitions implementation into review phase; `STOP` triggers circuit breaker/end-loop handling; otherwise feedback creates the next round (`hooks/loop-codex-stop-hook.sh:1390-1436`, `:1473-1608`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` for regular review rounds when `FULL_ALIGNMENT_CHECK=false` (`hooks/loop-codex-stop-hook.sh:775-849`). Full-alignment rounds use `prompt-template/codex/full-alignment-review.md` instead (`:818-831`). The included `GOAL_TRACKER_UPDATE_SECTION` is itself rendered from `prompt-template/codex/goal-tracker-update-section.md` (`:769-773`). Template rendering is single-pass via `hooks/lib/template-loader.sh:50-132`, which matters because embedded summary/review content containing `{{...}}` cannot inject extra substitutions.
- edge_cases_or_failure_modes: If this template is missing, the Stop hook uses a minimal regular-review fallback (`hooks/loop-codex-stop-hook.sh:807-816`), which preserves loop function but loses the stricter detailed review contract. If Codex exits nonzero, fails to write a result, or writes an empty result, the Stop hook blocks with retry diagnostics (`:1313-1385`). A `COMPLETE` line in review phase is ignored; review phase completion is determined by absence of `[P0-9]` issues from `codex review`, not by this prompt’s marker (`:1396-1401`, `:1442-1470`). Full alignment cadence bypasses this template on rounds where `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` (`:775-785`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` uses a mock `codex` that prints `COMPLETE` to exercise Stop hook paths without real Codex latency (`tests/test-plan-file-hooks.sh:38-58`) and validates that Stop hook parsing/gates occur before or around Codex execution, including quoted YAML handling and plan-integrity blocks (`:424-467`, `:509-775`). Template reference and rendering behavior are covered by `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/test-templates-comprehensive.sh` according to repository file references. The direct caller lines are `hooks/loop-codex-stop-hook.sh:760-849`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item sections present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`