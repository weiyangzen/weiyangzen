# agent_18 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`
- research_scope: read-only inspection of assigned files plus directly referenced scripts/hooks/templates
- validation_execution: not run; research notes only

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling an active PR loop. It is the user-facing command contract that delegates all algorithmic cancellation behavior to `scripts/cancel-pr-loop.sh`; the command itself does not implement cancellation logic.
- algorithmic_behavior: The command allows only two Bash invocations, `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh` and the same script with `--force`, via frontmatter `allowed-tools` at `commands/cancel-pr-loop.md:1-4`. The workflow instructs the agent to run the script, inspect the first output line, and map `NO_LOOP` or `NO_ACTIVE_LOOP` to “No active PR loop found,” while `CANCELLED` should surface the script cancellation message at `commands/cancel-pr-loop.md:11-20`.
- inputs_outputs_state: Input is the current plugin/project context, especially `CLAUDE_PLUGIN_ROOT` for the script path and, through the script, `CLAUDE_PROJECT_DIR` or current working directory as project root. Output is a textual status protocol whose first line is one of `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`. The script state transition is concrete: locate newest `.humanize/pr-loop/*/`, require `state.md`, create `.cancel-requested`, then move `state.md` to `cancel-state.md`; this is implemented in `scripts/cancel-pr-loop.sh:72-132`.
- gates_or_invariants: The key invariant is that an active PR loop exists only when `state.md` exists in the newest `.humanize/pr-loop/` directory, stated at `commands/cancel-pr-loop.md:21`. The command must preserve loop artifacts for reference at `commands/cancel-pr-loop.md:23`. It must not affect RLCR loops, explicitly documented at `commands/cancel-pr-loop.md:25`; the script reinforces that it only targets `.humanize/pr-loop/` at `scripts/cancel-pr-loop.sh:54-56`.
- dependencies_and_callers: Depends on `scripts/cancel-pr-loop.sh`, whose exit/status protocol is documented in `scripts/cancel-pr-loop.sh:11-15` and whose help text mirrors the command contract at `scripts/cancel-pr-loop.sh:34-57`. `commands/start-pr-loop.md:62` references the user running `/humanize:cancel-pr-loop`, and PR loop blocking messages in `hooks/pr-loop-stop-hook.sh:1168-1174`, `hooks/pr-loop-stop-hook.sh:1311-1315`, and `hooks/pr-loop-stop-hook.sh:1389-1397` direct users to this cancellation command.
- edge_cases_or_failure_modes: No loop base directory or no newest loop directory returns `NO_LOOP` and exit `1` in `scripts/cancel-pr-loop.sh:76-82`. A loop directory without `state.md` returns `NO_ACTIVE_LOOP` and exit `1` in `scripts/cancel-pr-loop.sh:88-98`. Missing fields such as `current_round`, `max_iterations`, or `pr_number` are tolerated by substituting `?` at `scripts/cancel-pr-loop.sh:104-112`. Unknown command options exit `3` at `scripts/cancel-pr-loop.sh:60-64`. `--force` is parsed but currently has no extra behavior at `scripts/cancel-pr-loop.sh:24-31` and `scripts/cancel-pr-loop.sh:39-40`.
- validation_or_tests: Covered by broader PR-loop script tests in `tests/test-pr-loop-scripts.sh` references at `tests/test-pr-loop-scripts.sh:169-185`, and cancellation/security behavior has additional related coverage in `tests/test-cancel-signal-file.sh` and robustness suites. The assigned markdown file itself is a command definition, not an executable test.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-048 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the aggregate test runner’s ANSI-stripping and pass/fail count parsing algorithm. It guards portability of test summary parsing across GNU/Linux and BSD/macOS sed behavior.
- algorithmic_behavior: The file defines a small bash test harness with counters at `tests/test-ansi-parsing.sh:18-31`, then exercises the exact stripping expression used by the aggregate runner: `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"` at `tests/test-ansi-parsing.sh:45-48`. It validates basic color removal, multiple colors, passed count extraction, failed count extraction, zero failed count extraction, multi-line suite output, plain text without ANSI codes, and bold+color combined sequences at `tests/test-ansi-parsing.sh:41-158`.
- inputs_outputs_state: Inputs are synthetic strings containing ANSI SGR sequences such as `\033[0;32m`, `\033[0;31m`, and `\033[1;32m`. Outputs are stripped text and parsed numeric pass/fail counts. Internal state is limited to `TESTS_PASSED` and `TESTS_FAILED`, incremented by `pass` and `fail`, and summarized at `tests/test-ansi-parsing.sh:160-178`.
- gates_or_invariants: The invariant under test is that SGR color/bold escape codes matching `ESC[` plus digits/semicolons plus `m` are removed without relying on sed hex escapes that may differ on BSD sed. Count extraction must use the stripped output with `grep -oE 'Passed:[[:space:]]*[0-9]+'` and equivalent `Failed` pattern at `tests/test-ansi-parsing.sh:73-90`. Multi-line parsing must select the last numeric summary with `tail -1` at `tests/test-ansi-parsing.sh:123-125`, matching the aggregate runner’s behavior.
- dependencies_and_callers: The direct caller/integration point is `tests/run-all-tests.sh`, which includes `test-ansi-parsing.sh` in its suite list at `tests/run-all-tests.sh:32-44`. The production parsing algorithm appears at `tests/run-all-tests.sh:121-128`, where each suite output is stripped and pass/fail counts are accumulated. The test depends on bash ANSI-C quoting, sed, grep, and shell arithmetic.
- edge_cases_or_failure_modes: It covers empty/no ANSI input at `tests/test-ansi-parsing.sh:133-144`, zero counts at `tests/test-ansi-parsing.sh:97-109`, multiple ANSI sequences on one line at `tests/test-ansi-parsing.sh:55-67`, and bold/color combined parameters at `tests/test-ansi-parsing.sh:146-158`. It does not cover non-SGR ANSI control sequences such as cursor movement or erase commands; the algorithm intentionally targets the `...m` color/style class only.
- validation_or_tests: This file is self-validating: if `TESTS_FAILED` remains zero it prints “All tests passed!” and exits `0`; otherwise it exits `1` at `tests/test-ansi-parsing.sh:170-178`. It is part of the all-tests suite at `tests/run-all-tests.sh:43`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Block prompt template for the PR-loop force-push gate. It defines the operator-facing transition when a PR’s tracked commit SHA changes in a non-fast-forward manner.
- algorithmic_behavior: The template renders a block reason stating the commit SHA changed from `{{OLD_COMMIT}}` to `{{NEW_COMMIT}}`, declares that force pushes reset review state, and requires a new trigger comment mentioning `{{BOT_MENTION_STRING}}` before the loop can continue at `prompt-template/block/force-push-detected.md:1-17`.
- inputs_outputs_state: Template inputs are `OLD_COMMIT`, `NEW_COMMIT`, `BOT_MENTION_STRING`, and also `PR_NUMBER` supplied by the caller, though `PR_NUMBER` is not used in this template. Output is markdown embedded as the `reason` field in a Claude hook JSON block response. The hook state transition occurs before rendering: `hooks/pr-loop-stop-hook.sh` preserves the old SHA, gets the new HEAD commit timestamp, updates `latest_commit_sha` and `latest_commit_at`, clears `last_trigger_at` and `trigger_comment_id`, then renders this template at `hooks/pr-loop-stop-hook.sh:381-419`.
- gates_or_invariants: The gate blocks continuation after a history rewrite until a trigger comment newer than the force push exists. The hook clears stale trigger metadata at `hooks/pr-loop-stop-hook.sh:395-403`, which prevents older trigger comments or eyes reactions from satisfying the new review cycle. The rendered JSON sets `"decision": "block"` and system message `"PR Loop: Force push detected - please re-trigger bots"` at `hooks/pr-loop-stop-hook.sh:418-419`.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/force-push-detected.md"` at `hooks/pr-loop-stop-hook.sh:414-416`. Rendering is performed by the template loader, which uses single-pass `{{VAR}}` substitution and preserves unresolved placeholders by design at `hooks/lib/template-loader.sh:50-131`. If the template is missing or renders empty, `load_and_render_safe` uses an inline fallback at `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If the hook cannot retrieve the new head commit timestamp, it falls back to the current UTC timestamp at `hooks/pr-loop-stop-hook.sh:390-393`; this may be less precise but still prevents stale triggers. If template variables are missing, placeholders remain due to template-loader behavior at `hooks/lib/template-loader.sh:12-13` and `hooks/lib/template-loader.sh:119-122`. The template itself has no executable validation and relies on caller-side force-push detection and state mutation.
- validation_or_tests: Template reference and comprehensive template suites likely validate existence/rendering broadly; direct reference found through `hooks/pr-loop-stop-hook.sh:414`. Functional force-push behavior would be exercised through PR-loop stop-hook tests rather than this markdown file directly.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template used when RLCR code review was skipped. It defines a degraded-validation transition contract: the implementation may finalize, but must do manual verification and preserve functionality because automated review did not run.
- algorithmic_behavior: The template warns with `{{REVIEW_SKIP_REASON}}`, tells the agent the implementation “could not be fully validated,” provides manual verification steps, optionally suggests the `code-simplifier:code-simplifier` subagent, constrains allowed changes to functionality-equivalent cleanup, references the original plan and goal tracker, and requires a finalize summary at `{{FINALIZE_SUMMARY_FILE}}`; see `prompt-template/claude/finalize-phase-skipped-prompt.md:1-50`.
- inputs_outputs_state: Inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a markdown prompt embedded as the `reason` in a blocking hook response. State transition is performed by `enter_finalize_phase`: it renames `state.md` to `finalize-state.md`, computes `finalize-summary.md`, renders this skipped-review template if `skip_reason` is non-empty, and returns JSON with `"decision": "block"` at `hooks/loop-codex-stop-hook.sh:1032-1115`.
- gates_or_invariants: The template’s non-negotiable constraints are no functionality changes, no failing existing tests, no new bugs, and only functionality-equivalent changes at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`. Before exiting, it requires all tasks completed, a commit, and a summary file at `prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`. The hook uses the skipped template only when `skip_reason` is non-empty at `hooks/loop-codex-stop-hook.sh:1044-1077`; otherwise it uses the normal finalize prompt at `hooks/loop-codex-stop-hook.sh:1078-1105`.
- dependencies_and_callers: Called via `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-skipped-prompt.md"` in `hooks/loop-codex-stop-hook.sh:1071-1077`. The transition is reached from code-review handling when review is skipped, e.g. missing `BASE_BRANCH` sets `REVIEW_SKIPPED=true` and a skip reason at `hooks/loop-codex-stop-hook.sh:1415-1419`. Rendering depends on `hooks/lib/template-loader.sh`, whose safe loader falls back to an inline skipped-review prompt if this file is missing or empty at `hooks/loop-codex-stop-hook.sh:1044-1069` and `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If review is skipped because required context such as `base_branch` is unavailable, the system still transitions to finalize but surfaces that validation was incomplete. If any placeholder input is missing, the unresolved token remains because rendering is single-pass and non-destructive. The template asks for a commit and summary even though review was skipped, so the downstream finalization gate depends on agent compliance and hook enforcement elsewhere.
- validation_or_tests: Review/finalize phase behavior is covered by `tests/test-finalize-phase.sh` references in the repository, while template existence/rendering is covered by template suites. The assigned file is the prompt contract, not the executable hook.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-138 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for PR-loop API handling, bot comment ingestion, PR-loop state discovery, stop-hook resilience, and poll JSON output under GitHub API failures and mocked responses.
- algorithmic_behavior: The script sets strict bash mode, sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates an isolated temporary test directory, and defines a behavior-driven mock `gh` command at `tests/robustness/test-pr-loop-api-robustness.sh:14-38`. The mock handles `gh repo view`, `gh pr view`, and behavior-specific `gh api` outputs for empty arrays, rate limits, network errors, auth failure, Claude approval, Codex issues, mixed bots, Unicode, and long comments at `tests/robustness/test-pr-loop-api-robustness.sh:43-239`.
- inputs_outputs_state: Inputs are temporary git repositories, generated `.humanize/pr-loop/.../state.md` files, mocked `gh` command outputs, JSON comments, and hook stdin `{}`. Outputs are pass/fail counters through `tests/test-helpers.sh`, generated markdown comment files, hook exit codes, and JSON from `scripts/poll-pr-reviews.sh`. Test state includes PR loop frontmatter created by `create_pr_loop_state` at `tests/robustness/test-pr-loop-api-robustness.sh:241-262` and git repos initialized by `init_basic_git_repo` at `tests/robustness/test-pr-loop-api-robustness.sh:264-276`.
- gates_or_invariants: The PR-loop active-state invariant is that `find_active_pr_loop` detects the newest PR-loop directory only when `state.md` exists; this is asserted at `tests/robustness/test-pr-loop-api-robustness.sh:285-345` and implemented in `hooks/lib/loop-common.sh:801-820`. Fetching PR comments must create a valid markdown file for empty comments, tolerate rate-limit/network failures gracefully, preserve bot authors and severity markers, process Unicode, and handle long bodies at `tests/robustness/test-pr-loop-api-robustness.sh:355-550`. The stop hook must exit normally with no active PR loop and avoid crash exits for corrupted state at `tests/robustness/test-pr-loop-api-robustness.sh:560-593`. Polling must reject missing args, output valid JSON with `has_new_comments`, tolerate slow API, and on API failure exit `0` with parseable JSON and boolean `has_new_comments:false` at `tests/robustness/test-pr-loop-api-robustness.sh:619-843`.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh` for `find_active_pr_loop`, `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary`, `scripts/fetch-pr-comments.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/poll-pr-reviews.sh`, `jq`, `git`, and shell utilities. `scripts/fetch-pr-comments.sh` implements repo resolution, retrying API fetches, comment normalization, timestamp filtering, deduplication, sort order, and markdown formatting at `scripts/fetch-pr-comments.sh:122-452`. `scripts/poll-pr-reviews.sh` implements argument validation, repo resolution, bot author mapping, retry-to-empty-array behavior, filtering, and final JSON output at `scripts/poll-pr-reviews.sh:83-328`. The suite is included in the aggregate runner at `tests/run-all-tests.sh:72`.
- edge_cases_or_failure_modes: Covered edge cases include missing `pr_number` while still detecting active loop at `tests/robustness/test-pr-loop-api-robustness.sh:324-345`, API rate limit and network failure handling at `tests/robustness/test-pr-loop-api-robustness.sh:379-425`, multiple bot formats at `tests/robustness/test-pr-loop-api-robustness.sh:435-501`, Unicode and long comment bodies at `tests/robustness/test-pr-loop-api-robustness.sh:511-550`, corrupted state file handling at `tests/robustness/test-pr-loop-api-robustness.sh:576-593`, slow API response at `tests/robustness/test-pr-loop-api-robustness.sh:677-742`, and hard API failure JSON recovery at `tests/robustness/test-pr-loop-api-robustness.sh:744-843`. One notable design behavior is that `fetch-pr-comments.sh` logs API failures but exits `0` after writing warnings when failures are recoverable, as implemented at `scripts/fetch-pr-comments.sh:176-205` and `scripts/fetch-pr-comments.sh:445-452`.
- validation_or_tests: This file is itself validation. It terminates through `print_test_summary "PR Loop API Robustness Test Summary"` and exits with that summary status at `tests/robustness/test-pr-loop-api-robustness.sh:845-850`. The aggregate runner includes it after other robustness tests at `tests/run-all-tests.sh:55-73`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections present; item ids are used only as the five section headers
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`