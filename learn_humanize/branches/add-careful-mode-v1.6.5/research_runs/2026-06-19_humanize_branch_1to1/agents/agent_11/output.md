# agent_11 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt fragments that drive RLCR loop phase transitions after Codex review output: regular next-round work, review-phase fixes, finalize-phase cleanup, open-question blocking, push obligations, and goal-tracker update requests.
- algorithmic_behavior: The directory contains nine templates recursively: `next-round-prompt.md`, `next-round-footer.md`, `push-every-round-note.md`, `goal-tracker-update-request.md`, `open-question-notice.md`, `post-alignment-action-items.md`, `review-phase-prompt.md`, `finalize-phase-prompt.md`, and `finalize-phase-skipped-prompt.md`. They are loaded with `load_template` / `load_and_render_safe`, whose single-pass `{{VAR}}` substitution avoids recursively expanding placeholders embedded in values (`hooks/lib/template-loader.sh:50-132`).
- inputs_outputs_state: Inputs are rendered variables such as `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `NEXT_SUMMARY_FILE`, `SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, and `REVIEW_SKIP_REASON`. Outputs are generated prompt files under `.humanize/rlcr/<timestamp>/round-N-prompt.md` or stop-hook JSON `reason` payloads (`hooks/loop-codex-stop-hook.sh:1041-1115`, `hooks/loop-codex-stop-hook.sh:1131-1162`, `hooks/loop-codex-stop-hook.sh:1512-1608`).
- gates_or_invariants: `next-round-footer.md:4-9` enforces no state-file cheating/cancel escape, commit after work, and writing `NEXT_SUMMARY_FILE`. `review-phase-prompt.md:11-14` limits review work to `[P0-9]` fixes and requires a summary. `finalize-phase-prompt.md:16-24` and `finalize-phase-skipped-prompt.md:26-33` require functionality-equivalent cleanup only. `open-question-notice.md:1` forces `AskUserQuestion` before other fixes when Codex reports open questions and the setup option enables that behavior.
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`: finalize templates at `1044-1104`, review-phase template at `1131-1152`, next-round prompt at `1516-1528`, open-question notice at `1530-1564`, post-alignment append at `1566-1572`, footer at `1574-1578`, push note at `1580-1587`, and goal tracker update request at `1589-1594`. Template directory validity requires `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude` (`hooks/lib/template-loader.sh:205-221`).
- edge_cases_or_failure_modes: Missing templates degrade to inline fallbacks via `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`), so prompt generation is not a hard failure unless later file writes fail. Open-question injection depends on a short line containing `Open Question` and an expected marker/footer position in the generated next-round prompt (`hooks/loop-codex-stop-hook.sh:1530-1563`). If the marker shape changes, the notice may not be inserted where intended.
- validation_or_tests: `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/test-templates-comprehensive.sh` are included in the aggregate runner before robustness suites (`tests/run-all-tests.sh:32-73`). This assigned directory was inspected recursively with no artificial depth limit; all files are direct prompt templates, not generated artifacts.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-041 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Entry-point setup script for `/humanize:start-rlcr-loop`; it initializes an RLCR session, validates the plan and repository, creates the loop state machine files, seeds the goal tracker, and emits the first Claude prompt.
- algorithmic_behavior: Parses command options including plan source, max iterations, Codex model/effort/timeout, push-every-round, base branch, full-review interval, skip-impl, and ask-Codex-question (`scripts/setup-rlcr-loop.sh:35-231`). It blocks concurrent RLCR/PR loops using `find_active_loop` and `find_active_pr_loop` (`scripts/setup-rlcr-loop.sh:237-268`), validates exactly one plan input (`270-298`), validates git repository state (`300-314`), validates plan path safety (`320-429`), plan tracking policy (`430-481`), plan content sufficiency (`483-548`), Codex availability (`550-556`), branch/model YAML safety (`562-594`), and base branch selection (`596-668`).
- inputs_outputs_state: Primary input is a relative markdown plan path unless `--skip-impl` omits it. Environment input `CLAUDE_PROJECT_DIR` overrides the project root (`scripts/setup-rlcr-loop.sh:237`). Outputs are `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`674-740`, `741-978`). State frontmatter includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, `ask_codex_question`, and `started_at` (`716-734`).
- gates_or_invariants: Only one active loop type can exist (`249-268`). Plan paths must be relative, no spaces, no shell metacharacters, no symlink file or symlink parent traversal, inside project, and outside submodules (`327-429`). Without `--track-plan-file`, a plan must be gitignored/untracked; with it, the plan must be tracked and clean (`454-478`). Branch names, base branches, model names, and effort strings are sanitized before writing YAML-ish state (`568-594`, `652-658`). `--full-review-round` must be an integer at least 2 (`191-205`).
- dependencies_and_callers: Slash command wrapper allows only this script with arguments (`commands/start-rlcr-loop.md:1-14`). It sources `scripts/portable-timeout.sh` (`scripts/setup-rlcr-loop.sh:27-29`) and `hooks/lib/loop-common.sh` for active-loop detection (`239-241`). Downstream stop hooks consume the state fields and generated files, including missing-summary checks and Codex review transitions (`hooks/loop-codex-stop-hook.sh:611-643`, `1396-1471`).
- edge_cases_or_failure_modes: `--skip-impl` with no plan creates a placeholder plan inside the loop dir, marks `review_started: true`, and touches `.review-phase-started` (`scripts/setup-rlcr-loop.sh:279-289`, `682-701`, `713-739`). Plan content validation treats markdown headings beginning with `#` as comments for the content-line count, so a heading-only plan can fail even if it has many markdown headings (`500-543`). Git command timeouts fail closed (`304-314`, `434-450`). A remote-only base branch is rejected because `codex review --base` requires a local ref (`601-618`, `620-649`). `git status` could not be run in this read-only sandbox because the platform tried to create a temp cache under `/tmp`.
- validation_or_tests: Plan/setup behavior is covered by `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, `tests/robustness/test-path-validation-robustness.sh`, `tests/robustness/test-base-branch-detection.sh`, and `tests/robustness/test-setup-scripts-robustness.sh` as referenced by `tests/run-all-tests.sh:37`, `63`, `59`, `66`, and `67`. I did not execute setup because it writes `.humanize/rlcr` state.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for the stop-hook “all tasks complete before Codex review” gate implemented by `hooks/check-todos-from-transcript.py` and invoked by `hooks/loop-codex-stop-hook.sh`.
- algorithmic_behavior: Creates temporary JSONL transcripts and pipes hook JSON into the Python checker (`tests/test-todo-checker.sh:37-43`). Test group 1 validates hook-input behavior: invalid JSON exits 2, empty input exits 0, missing `transcript_path` exits 0, and nonexistent transcript exits 0 (`52-98`). Groups 2-4 validate legacy `TodoWrite`: all completed exits 0, pending/in-progress/missing status exits 1, incomplete task names appear in output, invalid JSONL lines are ignored, latest `TodoWrite` wins, direct `tool_use` and `type: message` formats work, empty content remains incomplete, and unicode content is accepted (`107-295`).
- inputs_outputs_state: Inputs are stdin hook JSON with `transcript_path` and generated transcript files. Expected outputs are checker exit codes `0`, `1`, or `2` and stdout containing incomplete task details. The production checker extracts tool calls from assistant/message/direct entries (`hooks/check-todos-from-transcript.py:23-60`), tracks latest legacy `TodoWrite` list (`63-106`), reads authoritative task files from `~/.claude/tasks/<session_id>/` when a session id is present (`109-145`), and emits `INCOMPLETE_TODOS` plus item lines before exit 1 (`174-193`).
- gates_or_invariants: The stop hook runs this checker before expensive Codex review (`hooks/loop-codex-stop-hook.sh:340-352`). Parse errors block with a task-checker error (`354-370`). Incomplete tasks block immediately and render `prompt-template/block/incomplete-todos.md` or fallback (`373-395`). Only status `completed` clears legacy todos (`hooks/check-todos-from-transcript.py:96-104`); for task files, `completed` and `deleted` are terminal (`128-130`).
- dependencies_and_callers: Direct dependency is `python3 hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:12-15`). Runtime caller is `hooks/loop-codex-stop-hook.sh:347-395`; hook registration runs that stop hook on Claude Stop events (`hooks/hooks.json:52-64`). Aggregate suite includes this test as an early core suite (`tests/run-all-tests.sh:32-37`).
- edge_cases_or_failure_modes: Tests 17-25 expect `TaskCreate` / `TaskUpdate` state to be reconstructed from transcript JSONL (`tests/test-todo-checker.sh:303-438`), but current production code does not parse `TaskCreate` or `TaskUpdate` transcript calls; it reads task JSON files from `~/.claude/tasks/<session_id>/` instead (`hooks/check-todos-from-transcript.py:109-166`). Because these tests do not pass `session_id` or create task files, several “new Task system” assertions appear stale against the current implementation. This is an important test/implementation contract drift.
- validation_or_tests: The file itself ends with a pass/fail counter and exits 0 only when no test failed (`tests/test-todo-checker.sh:440-458`). I inspected it as an executable spec; I did not run it because the assigned task is read-only research and the newer-task assertions appear inconsistent with the current checker.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-101 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the stop-hook gate that prevents an RLCR or finalize-phase exit attempt when the required work summary file has not been written.
- algorithmic_behavior: Provides a concise rendered reason: title, statement that the user attempted to exit without a summary, target `{{SUMMARY_FILE}}`, required summary contents, and permission to retry after writing it (`prompt-template/block/work-summary-missing.md:1-16`). It has one variable placeholder: `SUMMARY_FILE`.
- inputs_outputs_state: Input is the expected summary path selected by `hooks/loop-codex-stop-hook.sh`: `finalize-summary.md` in finalize phase or `round-${CURRENT_ROUND}-summary.md` otherwise (`hooks/loop-codex-stop-hook.sh:611-616`). Output is a JSON stop-hook block decision with rendered `reason` and phase-specific `systemMessage` (`618-643`).
- gates_or_invariants: The invariant is that a summary file must exist before the loop can advance to task checking/Codex review/final completion. The stop hook checks file existence only at this gate (`hooks/loop-codex-stop-hook.sh:618`); content quality is later reviewed by Codex or by finalize completion checks.
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "block/work-summary-missing.md"` in the stop hook (`hooks/loop-codex-stop-hook.sh:622-626`). The safe loader falls back if the template is absent, but this file is present and therefore defines the canonical user-facing contract (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If `SUMMARY_FILE` is wrong due to corrupt loop state, this template will faithfully point to the wrong target; the correctness of the path depends on state parsing and phase detection before this block. If the file exists but is empty, this specific template/gate does not block; later review-result behavior determines whether the loop advances.
- validation_or_tests: Template reference/comprehensive suites are in the aggregate runner (`tests/run-all-tests.sh:38-40`). Stop-hook summary behavior is also exercised by finalize/error scenario suites in the runner (`tests/run-all-tests.sh:42`, `45`).
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-131 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for concurrent and malformed access to RLCR/PR loop state files, especially `hooks/lib/loop-common.sh` functions used by setup scripts and hooks.
- algorithmic_behavior: Sources shared test helpers and `loop-common.sh`, creates synthetic loop directories/state files, then exercises parsing, active-loop detection, concurrent reads, atomic write visibility, strict validation, PR loop detection, stale/cancelled loop behavior, and edge numeric/permission cases (`tests/robustness/test-concurrent-state-robustness.sh:15-30`, `52-559`).
- inputs_outputs_state: Inputs are temporary `state.md`, `finalize-state.md`, and `cancel-state.md` files created under `TEST_DIR`. Outputs are pass/fail counters through `tests/test-helpers.sh` (`tests/test-helpers.sh:30-78`). The key production state functions are `find_active_loop`, which considers only the newest timestamp dir and accepts `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:152-175`), `get_current_round`, which extracts `current_round` from frontmatter with default 0 (`177-190`), and `parse_state_file_strict`, which rejects missing frontmatter, required fields, nonnumeric values, and invalid `review_started` (`250-335`).
- gates_or_invariants: Newest-directory-only detection prevents older “zombie” loops from reviving (`hooks/lib/loop-common.sh:152-155`; tested at `tests/robustness/test-concurrent-state-robustness.sh:186-202`, `453-467`). `cancel-state.md` is not active (`469-485`). `finalize-state.md` is active (`204-220`). Atomic state updates should be temp-file-then-`mv`; the test requires 20/20 valid reads during simulated atomic writes (`280-323`). Strict parser must reject malformed YAML-like frontmatter and missing frontmatter (`333-372`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` (`tests/robustness/test-concurrent-state-robustness.sh:15-19`) and `hooks/lib/loop-common.sh` (`71-73`). Included in the aggregate test suite at `tests/run-all-tests.sh:68`. Production callers include setup mutual exclusion (`scripts/setup-rlcr-loop.sh:239-250`) and hook phase/state logic through the shared library.
- edge_cases_or_failure_modes: Leading whitespace before `current_round` may default to 0 or parse depending on implementation tolerance (`tests/robustness/test-concurrent-state-robustness.sh:80-98`). CRLF frontmatter may not parse because the sed separator expects exact `---`, but the expected behavior is graceful default/failure rather than crash (`100-121`). Inline comments are tolerated by stripping in the test assertion (`141-160`). Permission-denied behavior is skipped as root and otherwise expects empty/0 rather than failure (`530-553`). Strict parser’s “malformed YAML array” rejection relies on current grep/sed validation and required-field checks; it is not a full YAML parser.
- validation_or_tests: This file is itself the validation harness and exits via `print_test_summary` (`tests/robustness/test-concurrent-state-robustness.sh:555-560`). I inspected it directly; I did not execute it because the task is research-only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`