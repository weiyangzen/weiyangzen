# agent_08 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing behavior contract for the plugin’s two main loop algorithms: RLCR and PR loop. It defines the expected workflow phases, command surfaces, default options, required tooling, and monitoring entry points. It is documentation, but it is behavior-defining because it establishes the public state machine users expect from `start-rlcr-loop`, `gen-plan`, `start-pr-loop`, cancel commands, and monitors.
- algorithmic_behavior: The README describes RLCR as an iterative implementation and review cycle: plan input enters Claude implementation, Claude produces summaries, Codex reviews, feedback returns to Claude, Codex eventually marks completion, then `codex review --base <branch>` performs code review before completion (`README.md:60-72`). For plan generation, it documents validation of input/output paths, repository relevance checking, draft quality analysis, user issue resolution, and generation of structured `AC-X` acceptance criteria (`README.md:126-143`). For PR loop, it documents branch-to-PR detection, bot comment fetching, Claude issue fixing, push plus bot re-trigger, stop-hook polling, local Codex validation, and repeated iteration until bot approval or max iterations (`README.md:163-172`).
- inputs_outputs_state: RLCR input is a plan file via positional path or `--plan-file`; outputs are `.humanize/rlcr/<timestamp>/` state/progress artifacts and eventual code/review state (`README.md:80-89`, `README.md:103-124`). PR loop input is `--claude` and/or `--codex`, plus max/model/timeout options (`README.md:145-160`); output is PR-loop state under `.humanize/pr-loop/<timestamp>/`, remote PR comments, local Codex validation files, and monitor display (`README.md:163-181`). Documented defaults are `max=42`, RLCR Codex model/effort `gpt-5.2-codex:high`, RLCR timeout `5400`, PR-loop Codex model/effort `gpt-5.2-codex:medium`, and PR-loop timeout `900` (`README.md:110-159`).
- gates_or_invariants: RLCR has two phases, with implementation phase preceding code-review phase (`README.md:70-72`). PR loop requires at least one bot flag (`README.md:148-152`), GitHub CLI authentication, Codex CLI, and an open PR for the current branch (`README.md:173-176`). The code review phase is documented as severity-marker based via `[P0-9]` (`README.md:70-72`). `--skip-impl` explicitly gates bypassing implementation into review phase and makes the plan file optional (`README.md:121-122`).
- dependencies_and_callers: The README maps user commands to implementation scripts and hooks in the repo: `/humanize:start-rlcr-loop`, `/humanize:cancel-rlcr-loop`, `/humanize:gen-plan`, `/humanize:start-pr-loop`, `/humanize:cancel-pr-loop` (`README.md:91-99`). It points monitoring users to `scripts/humanize.sh` loaded from the plugin cache and invoked as `humanize monitor [rlcr|pr]` (`README.md:84-88`, `README.md:178-181`). It establishes `codex` as the review dependency (`README.md:52-55`) and `gh` as a PR-loop dependency (`README.md:173-175`).
- edge_cases_or_failure_modes: Documented failure surfaces include max-iteration auto-stop, optional cancel commands, PR prerequisites not met, and branch/base detection for code review (`README.md:89`, `README.md:117-123`, `README.md:155-160`, `README.md:173-176`). The quick-start numbering skips from step 1 to step 3 (`README.md:76-83`), which is documentation-only and not algorithmic.
- validation_or_tests: README behavior aligns with concrete scripts and tests observed in this branch: PR-loop setup calls `fetch-pr-comments.sh` and `check-pr-reviewer-status.sh` to seed comments and startup case (`scripts/setup-pr-loop.sh:430-445`); PR-loop stop hook implements polling and bot-response state transitions (`hooks/pr-loop-stop-hook.sh:801-1044`); monitor code consumes `.humanize/rlcr` and `.humanize/pr-loop` session directories (`scripts/humanize.sh:224-240`, `scripts/humanize.sh:951-985`). No tests directly assert README text, but its documented defaults match implementation defaults in `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh` for model/effort.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-038 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime polling adapter that turns GitHub PR issue comments, inline review comments, and PR review submissions into a normalized JSON event stream for the PR-loop stop hook. It is part of the PR-loop state machine’s “wait for bot review” transition.
- algorithmic_behavior: The script parses `<pr_number> --after <timestamp> --bots <bot1,bot2>` (`scripts/poll-pr-reviews.sh:19-81`), validates required fields and numeric PR number (`scripts/poll-pr-reviews.sh:83-102`), checks for `gh` and `jq` (`scripts/poll-pr-reviews.sh:104-116`), resolves the correct base repo for current-repo or fork PRs (`scripts/poll-pr-reviews.sh:122-150`), maps logical bot names to GitHub author logins, with `codex` mapped to `chatgpt-codex-connector[bot]` (`scripts/poll-pr-reviews.sh:164-187`), fetches three comment channels, normalizes their fields, filters by timestamp and author regex, and emits one JSON object (`scripts/poll-pr-reviews.sh:193-327`).
- inputs_outputs_state: Inputs are CLI args, current GitHub repository context, PR number, ISO timestamp cutoff, and comma-separated logical bot list (`scripts/poll-pr-reviews.sh:7-10`, `scripts/poll-pr-reviews.sh:23-81`). It writes only temporary files under a `mktemp -d` directory and registers cleanup via trap (`scripts/poll-pr-reviews.sh:193-199`). Output is JSON with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:316-326`). Comments are normalized with `type`, `id`, `author`, `author_type`, `created_at`, `body`, plus `path`/`line` for review comments and `state` for PR reviews (`scripts/poll-pr-reviews.sh:240-294`).
- gates_or_invariants: Required gates are valid PR number, nonempty `--after`, nonempty `--bots`, installed `gh`, installed `jq`, and successful PR base-repo resolution (`scripts/poll-pr-reviews.sh:83-150`). It intentionally returns successful empty arrays for per-endpoint API failures after retries, allowing the outer poll loop to continue rather than terminating on transient GitHub API failure (`scripts/poll-pr-reviews.sh:207-235`). It includes empty-body PR reviews by substituting `[Review state: STATE]`, so approval-only reviews are not dropped (`scripts/poll-pr-reviews.sh:276-294`). Filtering uses lexicographic ISO timestamp comparison `.created_at >= $after`; this relies on normalized comparable timestamp strings (`scripts/poll-pr-reviews.sh:299-305`).
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` as `POLL_SCRIPT="$PLUGIN_ROOT/scripts/poll-pr-reviews.sh"` during the per-bot polling loop (`hooks/pr-loop-stop-hook.sh:801-812`, `hooks/pr-loop-stop-hook.sh:960-965`). The stop hook passes only still-waiting bots, then maps `bots_responded` authors back to logical bot names and updates dynamic response state (`hooks/pr-loop-stop-hook.sh:967-982`). Setup-time sibling behavior uses `scripts/fetch-pr-comments.sh`, while state classification uses `scripts/check-pr-reviewer-status.sh` (`scripts/setup-pr-loop.sh:430-445`). The bot mapping is duplicated in `scripts/check-pr-reviewer-status.sh` (`scripts/check-pr-reviewer-status.sh:91-100`), so mapping drift would affect loop consistency.
- edge_cases_or_failure_modes: Fork PRs are handled by checking the current repo, then parent repo (`scripts/poll-pr-reviews.sh:132-150`). Missing or failing API endpoints increment diagnostic warnings but return `[]` so final output can remain valid JSON with `has_new_comments:false` (`scripts/poll-pr-reviews.sh:221-235`, `scripts/poll-pr-reviews.sh:317-326`). If all API calls fail, warnings may precede JSON on combined stdout/stderr in caller tests, so consumers should parse from JSON start when stderr is merged. Bot regex construction trims spaces and escapes brackets but does not validate bot names beyond nonempty list, so unusual bot strings could widen or alter regex matching (`scripts/poll-pr-reviews.sh:175-187`). Timestamp comparison is string-based and assumes ISO 8601 ordering; non-normalized timestamps could misfilter. Because `gh api --paginate` output shape can vary, each jq transform guards `if type == "array" then ... else []` (`scripts/poll-pr-reviews.sh:242-254`, `scripts/poll-pr-reviews.sh:259-273`, `scripts/poll-pr-reviews.sh:280-293`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` checks help output and required-argument errors (`tests/test-pr-loop-scripts.sh:329-390`). `tests/test-pr-loop-hooks.sh` validates approval-only review capture, placeholder substitution, bot-pattern matching, fixture-backed JSON output, and `--after` filtering (`tests/test-pr-loop-hooks.sh:1160-1219`, `tests/test-pr-loop-hooks.sh:1322-1419`). `tests/robustness/test-pr-loop-api-robustness.sh` checks help, missing args, mocked JSON structure, slow API behavior, and API failure returning valid JSON with boolean `has_new_comments:false` (`tests/robustness/test-pr-loop-api-robustness.sh:619-843`). I did not execute tests because the assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-068 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the template-loading algorithm used by validators and hooks to render prompt/error blocks safely. It verifies path resolution, loading, fallback behavior, single-pass placeholder replacement, shell metacharacter literalness, placeholder-injection prevention, and variable edge cases.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` from project root (`tests/test-template-loader.sh:10-13`), then runs a linear suite with counters and final exit status (`tests/test-template-loader.sh:20-39`, `tests/test-template-loader.sh:641-659`). It checks `get_template_dir` resolves from `hooks/lib` to `prompt-template` (`tests/test-template-loader.sh:41-52`), `load_template` reads existing files and returns empty for missing files (`tests/test-template-loader.sh:54-78`), `render_template` substitutes `{{VAR}}` placeholders while leaving missing placeholders intact (`tests/test-template-loader.sh:80-194`), and `load_and_render`/`load_and_render_safe` integrate load plus rendering and fallback (`tests/test-template-loader.sh:146-222`).
- inputs_outputs_state: Inputs are literal template strings, real template files under `prompt-template`, and `VAR=value` arguments. Output is shell stdout/stderr PASS/FAIL lines plus process exit 0 only when `TESTS_FAILED` is zero (`tests/test-template-loader.sh:23-34`, `tests/test-template-loader.sh:641-659`). The test mutates only shell variables and counters; it does not create persistent fixtures. It uses real production template files such as `block/git-push.md` and `block/wrong-round-number.md` (`tests/test-template-loader.sh:58-64`, `tests/test-template-loader.sh:150-164`).
- gates_or_invariants: The central invariant is single-pass rendering: inserted values containing `{{OTHER_VAR}}` must not be rescanned or substituted, preventing prompt corruption and placeholder injection (`tests/test-template-loader.sh:488-543`). Values containing shell metacharacters must render literally, including ampersand, backslash, dollar signs, backticks, pipes, semicolons, globs, parentheses, redirections, quotes, hashes, tildes, combined shell syntax, Windows paths, regex-like strings, JSON-like strings, and multiline content (`tests/test-template-loader.sh:246-487`). Missing variables remain as placeholders (`tests/test-template-loader.sh:181-194`), unused variables are ignored (`tests/test-template-loader.sh:166-179`), empty substitution works (`tests/test-template-loader.sh:555-565`), and variable names may include underscores and numbers (`tests/test-template-loader.sh:617-639`).
- dependencies_and_callers: It directly depends on `hooks/lib/template-loader.sh`, whose `render_template` builds `TMPL_VAR_` environment variables and performs a character-scanning awk replacement without rescanning inserted values (`hooks/lib/template-loader.sh:58-132`). The loader is sourced through `hooks/lib/loop-common.sh` and used across validators and stop hooks via `load_and_render_safe` (`hooks/lib/loop-common.sh:142`, `hooks/lib/loop-common.sh:457-506`). It validates templates that are referenced by runtime validators, including `block/summary-bash-write.md` indirectly through common message functions and template reference tests.
- edge_cases_or_failure_modes: The test’s shell-metacharacter cases specifically guard against common awk/sed replacement hazards such as `&` expanding to matched text, backslashes being consumed, dollar signs/backticks being executed by the shell, and placeholder values causing second-pass substitution (`tests/test-template-loader.sh:257-543`). It includes Unicode-labeled tests but mostly ASCII content; the actual non-ASCII cases are encoded using Bash `$'...'` byte escapes for accented text (`tests/test-template-loader.sh:579-615`). Because the script uses Bash features and `set -euo pipefail`, a hard error in the loader or missing project layout aborts the test rather than recording a normal `fail`.
- validation_or_tests: This file is itself the validator. Related broader coverage exists in `tests/test-template-references.sh`, which ensures referenced templates exist and critical validators use `load_and_render_safe` (`tests/test-template-references.sh:51-110`, `tests/test-template-references.sh:170-230`), plus robustness/stress tests for template errors and special cases found by repository search. I did not execute this script because the requested deliverable is research notes only.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-098 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Runtime blocking-message template for the RLCR hook validation gate that forbids modifying round summary files through Bash. It is a small prompt block, but it defines a concrete validation contract: summaries must be written through editor/write tools so validation hooks can enforce round-number and path rules.
- algorithmic_behavior: The template emits a “Bash Write Blocked” message, instructs the agent not to use Bash commands to modify summary files, renders the correct path via `{{CORRECT_PATH}}`, and explains that shell commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/summary-bash-write.md:1-8`). It has one variable placeholder, `CORRECT_PATH`, and no conditional behavior.
- inputs_outputs_state: Input is the `CORRECT_PATH` variable supplied by `summary_bash_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:487-496`). Output is markdown text returned by `load_and_render_safe`; if the file is missing or renders empty, the fallback message in `loop-common.sh` uses the same variable (`hooks/lib/loop-common.sh:491-495`). It does not persist state; it shapes hook response text.
- gates_or_invariants: The invariant is that Bash writes to summary files are invalid even if they target the right file, because they bypass tool-level validators (`prompt-template/block/summary-bash-write.md:3-8`). This complements round and location gates in write/edit validators: validators distinguish write/edit tool access from shell mutation and route Bash-based summary mutations through this message. The template requires `CORRECT_PATH` to be accurate; otherwise the block could direct the agent to the wrong summary target.
- dependencies_and_callers: Directly referenced by `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:487-496`). Template-loader behavior is supplied by `hooks/lib/template-loader.sh`, especially `load_and_render_safe` fallback behavior (`hooks/lib/template-loader.sh:170-203`). `tests/test-template-references.sh` explicitly includes `block/summary-bash-write.md` in common template existence checks (`tests/test-template-references.sh:149-167`). Repository-wide search shows this block is part of the broader hook validation messaging family alongside `goal-tracker-bash-write.md`, `state-file-modification.md`, and prompt-file write blocks.
- edge_cases_or_failure_modes: If `summary-bash-write.md` is missing, `load_and_render_safe` suppresses the missing-template warning and emits the fallback message (`hooks/lib/template-loader.sh:170-187`, `hooks/lib/loop-common.sh:491-495`). If `CORRECT_PATH` contains shell metacharacters or placeholder-looking text, the production template renderer’s single-pass awk implementation is designed to render it literally without executing shell syntax or expanding injected placeholders (`hooks/lib/template-loader.sh:54-132`), and `tests/test-template-loader.sh` covers those hazards (`tests/test-template-loader.sh:246-543`). The template itself is a skip candidate only for complex algorithm work, not for core validation behavior, because its text is the user-visible gate output.
- validation_or_tests: Existence is checked by `tests/test-template-references.sh` common-template validation (`tests/test-template-references.sh:152-167`). Rendering safety and fallback behavior are covered by `tests/test-template-loader.sh` through generic rendering, special-character, injection-prevention, and safe-fallback cases (`tests/test-template-loader.sh:80-222`, `tests/test-template-loader.sh:246-543`). I did not run tests because this is a read-only research pass.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-128 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for RLCR and PR loop terminal monitors. It centralizes cross-platform file/session detection, terminal setup/restoration helpers, status color mapping, PR-loop phase detection, YAML-frontmatter extraction, display formatting, and goal-tracker parsing used by `scripts/humanize.sh`.
- algorithmic_behavior: The library exposes color helpers (`scripts/lib/monitor-common.sh:12-23`), cross-platform file size retrieval via GNU/BSD `stat` fallback (`scripts/lib/monitor-common.sh:29-35`), timestamp-named session selection using lexicographic max over `YYYY-MM-DD_HH-MM-SS` directories (`scripts/lib/monitor-common.sh:37-63`), terminal scroll-region setup/restoration (`scripts/lib/monitor-common.sh:69-92`), loop status color classification (`scripts/lib/monitor-common.sh:124-141`), state-file detection with priority for active `state.md` over terminal `*-state.md` files (`scripts/lib/monitor-common.sh:143-188`), frontmatter value extraction (`scripts/lib/monitor-common.sh:190-209`), timestamp/string formatting helpers (`scripts/lib/monitor-common.sh:211-252`), PR phase detection (`scripts/lib/monitor-common.sh:254-381`), generic goal-tracker parsing (`scripts/lib/monitor-common.sh:383-463`), and PR goal-tracker stats parsing (`scripts/lib/monitor-common.sh:465-498`).
- inputs_outputs_state: Inputs are filesystem paths to loop directories, session directories, state files, goal trackers, and timestamps. Outputs are simple strings: file sizes, latest session path, `state_path|status`, YAML values, display timestamps, truncated strings, PR phase names, phase display messages, and pipe-delimited tracker stats (`scripts/lib/monitor-common.sh:147-154`, `scripts/lib/monitor-common.sh:258-267`, `scripts/lib/monitor-common.sh:387-390`, `scripts/lib/monitor-common.sh:465-468`). The only persistent side effect found is `get_pr_loop_phase` caching observed `round-N-pr-check.md` sizes under `/tmp/humanize-phase-${session_name}-${highest_round}.size` to detect file growth (`scripts/lib/monitor-common.sh:292-318`).
- gates_or_invariants: `monitor_find_latest_session` accepts only directory basenames matching the timestamp pattern and compares names lexicographically, which is valid for the chosen sortable timestamp format (`scripts/lib/monitor-common.sh:49-60`). `monitor_find_state_file` treats `state.md` as active and only then scans terminal state files, returning `unknown` for invalid or missing dirs (`scripts/lib/monitor-common.sh:157-188`). `get_pr_loop_phase` checks terminal states first, then detects active Codex analysis from latest `round-*-pr-check.md` file growth or mtime younger than 10 seconds, then falls back to `state.md` frontmatter: round 0 plus startup case 1 is `waiting_initial_review`, otherwise `waiting_reviewer` (`scripts/lib/monitor-common.sh:272-342`). Goal parsing assumes markdown section headings and tables with stable headings such as `### Acceptance Criteria`, `#### Active Tasks`, `### Completed and Verified`, `### Explicitly Deferred`, and `### Open Issues` (`scripts/lib/monitor-common.sh:397-462`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` when present (`scripts/humanize.sh:6-10`). RLCR monitor delegates latest-session and state-file lookup to this library (`scripts/humanize.sh:237-247`, `scripts/humanize.sh:344-347`). PR monitor delegates latest-session, state-file lookup, phase detection, phase display, and PR goal tracker parsing (`scripts/humanize.sh:982-1048`, `scripts/humanize.sh:1117-1168`, `scripts/humanize.sh:1255-1261`). `tests/test-pr-loop-system.sh` sources this file directly for phase detection, goal parsing, state detection, latest-session detection, file-size behavior, and file-growth phase detection (`tests/test-pr-loop-system.sh:256-417`, `tests/test-pr-loop-system.sh:770-899`).
- edge_cases_or_failure_modes: Missing loop/session dirs return empty string or `unknown` rather than failing (`scripts/lib/monitor-common.sh:44-47`, `scripts/lib/monitor-common.sh:157-160`). `monitor_find_state_file` uses the first `*-state.md` returned by `find`, so multiple terminal state files would be nondeterministic (`scripts/lib/monitor-common.sh:168-181`). `monitor_get_yaml_value` uses simple `grep`/`sed`, so keys with regex metacharacters or duplicate keys are not robustly parsed (`scripts/lib/monitor-common.sh:203-208`). `get_pr_loop_phase` depends on `/tmp` cache names based only on session basename and round; identical timestamp basenames in different projects could collide (`scripts/lib/monitor-common.sh:297-306`). Its recent-mtime check may report `codex_analyzing` for a recently touched but idle file (`scripts/lib/monitor-common.sh:308-318`). Goal tracker parsing is regex/table based and may miscount malformed markdown, nested sections, or data rows that do not match expected table columns (`scripts/lib/monitor-common.sh:397-462`).
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies approved/cancelled/maxiter/waiting phases, generic goal tracker counts, PR goal tracker stats, active and terminal state-file detection, latest timestamp session selection, file-size fallback behavior, and active-Codex detection via recent/growing `pr-check` files (`tests/test-pr-loop-system.sh:256-417`, `tests/test-pr-loop-system.sh:423-487`, `tests/test-pr-loop-system.sh:770-899`). `scripts/humanize.sh` also contains parallel/legacy parsing code for RLCR goal trackers, which makes monitor-common both a shared dependency and a partial duplicate of monitor parsing logic (`scripts/humanize.sh:30-112`). I did not run tests during this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections present; item identifiers are used exactly once each in the Item Evidence headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`