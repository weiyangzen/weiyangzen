# agent_12 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side review prompt templates for the RLCR loop. This directory defines the review contract used when Claude’s implementation output is handed to Codex for ordinary review, full alignment review, goal-tracker updates, and Codex review-phase audit logging.
- algorithmic_behavior: Contains four recursive children:
  - `prompt-template/codex/regular-review.md`: regular per-round review prompt. It requires Codex to read `@{{PLAN_FILE}}`, compare implementation claims against `@{{PROMPT_FILE}}`, inspect `@{{DOCS_PATH}}`, reject deferrals, perform goal alignment, and only end with `COMPLETE` when all planned work and ACs are fully complete (`regular-review.md:1-57`).
  - `prompt-template/codex/full-alignment-review.md`: interval-based mandatory audit. It requires reading the original plan, auditing each AC, detecting forgotten/deferred items, reviewing recent round history, checking stagnation, and outputting `STOP` if progress has stalled (`full-alignment-review.md:1-93`).
  - `prompt-template/codex/goal-tracker-update-section.md`: embedded section that transfers responsibility for post-round goal-tracker edits to Codex, while prohibiting IMMUTABLE SECTION changes (`goal-tracker-update-section.md:1-17`).
  - `prompt-template/codex/code-review-phase.md`: audit artifact for `codex review --base {{BASE_BRANCH}}`; documents severity marker scanning and generated command/output files (`code-review-phase.md:1-36`).
- inputs_outputs_state: Inputs are template variables rendered by `hooks/lib/template-loader.sh`, whose syntax is single-pass `{{VARIABLE_NAME}}` replacement and keeps missing placeholders intact (`hooks/lib/template-loader.sh:7-13`, `58-131`). Outputs are review prompt files under `.humanize/rlcr/<timestamp>/round-N-review-prompt.md` and audit prompts for Codex review phase. `hooks/loop-codex-stop-hook.sh` injects the goal-tracker section at `772-773`, selects full alignment on configured rounds at `775-785`, writes full-alignment prompts at `820-831`, regular prompts at `836-848`, and code-review audit prompts at `950-956`.
- gates_or_invariants: The templates enforce “no deferred work is complete” as a review invariant. `regular-review.md:53-57` and `full-alignment-review.md:88-93` define the only completion condition as all original tasks/ACs fully met with no deferrals or pending work. Full alignment also adds a stagnation circuit breaker: write `STOP` as last line when repeated unresolved issues or lack of code progress is detected (`full-alignment-review.md:74-85`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`; fallback strings exist but the directory provides the authoritative richer prompts (`loop-codex-stop-hook.sh:769-848`, `937-956`). The loader itself resolves templates from plugin root `prompt-template` via `get_template_dir` (`template-loader.sh:24-31`) and falls back safely if a template is missing (`template-loader.sh:170-203`).
- edge_cases_or_failure_modes: Missing templates degrade to fallback prompts instead of crashing, which preserves loop progress but weakens the review contract. Single-pass rendering prevents injected placeholder expansion in user/summary content (`template-loader.sh:54-57`). If `FULL_REVIEW_ROUND` is invalid or below 2, the hook defaults to 5 before selecting full alignment rounds (`loop-codex-stop-hook.sh:777-785`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` references the real `codex/goal-tracker-update-section.md` template and validates rendering coverage (`rg` hit at `tests/test-templates-comprehensive.sh:531-539`). Runtime hook references confirm all four templates are actively used.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Preflight and materialization script for the `/gen-plan` command. It validates draft input and output target constraints, locates the plan template, copies it to the target, and appends the original draft content.
- algorithmic_behavior: Parses `--input` and `--output` only, rejects missing values, flag-as-value, unknown options, and help via `usage` exit 6 (`scripts/validate-gen-plan-io.sh:16-67`). Converts paths with `realpath -m` when available (`70-72`). Performs ordered validation: input exists (`79-85`), input non-empty (`87-93`), output directory exists (`95-101`), output path is not a directory (`103-109`), output does not already exist (`111-116`), and output directory is writable (`118-124`). On success it prints validation metadata, locates `prompt-template/plan/gen-plan-template.md` via `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`133-140`), exits 7 if missing (`142-146`), then writes the template plus draft envelope to the new output file (`148-157`).
- inputs_outputs_state: Inputs are CLI args, filesystem state, optional `CLAUDE_PLUGIN_ROOT`, and `prompt-template/plan/gen-plan-template.md`. Output is a newly created plan file containing the static template followed by `--- Original Design Draft Start ---`, draft content, and `--- Original Design Draft End ---` (`151-157`). It mutates only the specified output file after all gates pass.
- gates_or_invariants: Output path must be a new file under an existing writable directory; existing files and directories both exit 4 (`103-116`). Input must be present and non-empty before template resolution or file creation (`79-93`). Template presence is treated as plugin configuration, not user input, with exit 7 (`142-146`).
- dependencies_and_callers: `/gen-plan` command allows this script explicitly in frontmatter (`commands/gen-plan.md:4-5`) and invokes it as Phase 1 (`commands/gen-plan.md:30-46`). The command maps each exit code to user-facing stop behavior before relevance checking or plan generation (`commands/gen-plan.md:38-46`).
- edge_cases_or_failure_modes: Uses `realpath -m` with fallback to raw path, so behavior may differ on systems without GNU realpath (`70-71`). `set -e` means unexpected command failures outside guarded checks abort. The script’s success path copies and appends using shell redirection; if the filesystem becomes unwritable after the `-w` check, the copy/append step can still fail. Existing output directories and files share exit 4 but print distinct validation errors (`103-116`).
- validation_or_tests: `tests/test-gen-plan.sh:426-542` exercises this script’s executable presence, invalid arg exits, missing input, empty input, missing output directory, existing output file, output-as-directory, valid path exit 0, and help exit 6.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh safety specification for humanize monitor file discovery. It guards against zsh `nomatch` failures in empty or dotfile-only loop/cache directories and verifies the monitor can be sourced under zsh.
- algorithmic_behavior: Requires zsh by shebang and verifies `$ZSH_VERSION` later (`tests/test-zsh-monitor-safety.sh:1`, `276-286`). Creates an isolated `/tmp/test-zsh-humanize-$$` workspace and isolated `XDG_CACHE_HOME` (`48-60`). Test 1 sources `scripts/humanize.sh` and fails if output contains `no matches found` (`68-77`). Tests 2-4 reproduce monitor session/state lookup logic using `find` process substitution, covering empty `.humanize/rlcr`, dotfile-only loop dir, and session without `*-state.md` (`80-205`). Test 5 covers empty cache log lookup for `round-*-codex-run.log` (`207-235`). Test 6 ensures timestamped session directory iteration finds the lexicographically latest timestamp (`240-273`). Test 8 demonstrates the old glob pattern risk and verifies the new `find` pattern works (`289-321`).
- inputs_outputs_state: Inputs are the checked-out `scripts/humanize.sh`, zsh runtime behavior, temporary `.humanize/rlcr` session directories, dotfiles, state files, and cache directories. Outputs are PASS/FAIL counters and exit 0 only when `TESTS_FAILED` is 0 (`323-341`). It writes only to `/tmp` and `XDG_CACHE_HOME` under that temp base during test execution.
- gates_or_invariants: The invariant under test is “monitor discovery must not rely on unmatched globs.” All tested discovery loops use `find ... 2>/dev/null`, skip empty lines and non-directories/files, and accept empty results as valid. This maps directly to production `monitor_find_latest_session`, which states it uses `find` to avoid zsh `no matches found` errors (`scripts/lib/monitor-common.sh:49-60`), and `monitor_find_state_file`, which uses `find` for `*-state.md` discovery (`monitor-common.sh:168-181`).
- dependencies_and_callers: Sources `scripts/humanize.sh` (`tests/test-zsh-monitor-safety.sh:72`, `92`), whose monitor wrapper delegates latest-session discovery to `monitor_find_latest_session` (`scripts/humanize.sh:237-240`), log discovery to `find` over cache dirs (`scripts/humanize.sh:280-339`), and state discovery to `monitor_find_state_file` (`scripts/humanize.sh:344-347`).
- edge_cases_or_failure_modes: Covers missing loop directory indirectly through helper checks, empty directories, dotfiles that should not count as sessions, no matching state files, empty cache dirs, multiple valid sessions, and zsh shell mismatch. The test’s local helper copies production logic rather than invoking private nested functions directly, so it is a behavioral spec for the pattern and production helpers rather than full black-box coverage of the nested monitor function bodies.
- validation_or_tests: This file is itself the validation asset. It complements broader monitor tests referenced by `rg`, including `tests/test-monitor-e2e-real.sh` zsh/cache cases and `tests/test-pr-loop-system.sh` shared monitor helper tests.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Blocking prompt template for read/write guard violations when a loop actor targets a valid round file name in the wrong directory.
- algorithmic_behavior: Renders a short error block with action, attempted file path, corrected path, and a suggested `cat {{FILE_PATH}}` command (`prompt-template/block/wrong-directory-path.md:1-6`). It is used after validators compute `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and compare it against the requested `FILE_PATH`.
- inputs_outputs_state: Inputs are `ACTION`, `FILE_PATH`, and `CORRECT_PATH`. Output is a rendered Markdown error message to stderr; no state transition happens in the template itself. The read validator calls it with `ACTION=read` and exits 2 on mismatch (`hooks/loop-read-validator.sh:157-167`). The write validator calls it with `ACTION=write to` and exits 2 on mismatch (`hooks/loop-write-validator.sh:252-262`).
- gates_or_invariants: Enforces active-loop directory confinement: even if the filename is round-correct, the path must equal the current active loop directory path exactly. The validators compute the canonical expected active-loop path and block all mismatches (`loop-read-validator.sh:153-168`, `loop-write-validator.sh:248-263`).
- dependencies_and_callers: Depends on `load_and_render_safe` fallback handling (`hooks/lib/template-loader.sh:170-203`). Direct callers are `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`; both provide fallback text if this template is unavailable.
- edge_cases_or_failure_modes: The suggested command currently says `cat {{FILE_PATH}}` even in wrong-directory cases; that may intentionally help inspect the attempted path, but it does not suggest `cat {{CORRECT_PATH}}`. Since rendering is single-pass, any braces inside a supplied path remain literal and cannot trigger secondary substitutions (`template-loader.sh:54-57`).
- validation_or_tests: Coverage is indirect through validator tests and template rendering tests. `rg` shows both read/write validators use this exact template and comprehensive template tests include real block templates.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-132 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git-state helpers in `scripts/humanize.sh`, especially monitor/status behavior that summarizes repository dirtiness and abnormal git states.
- algorithmic_behavior: Sources `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and production `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:15-20`). Defines `parse_result` for the pipe-delimited `humanize_parse_git_status` output (`35-47`) and `init_test_repo` to create local git repos with initial commits (`49-62`). Tests normal status parsing: clean repo, branch name, untracked, modified, staged added, insertion counts (`71-158`). Tests edge states: non-git directories, detached HEAD, simultaneous file states, deleted files, empty repo, feature branch, rename-as-modified, many untracked files, filenames with spaces, binary files, staged plus unstaged changes on the same file, and deletion counts (`168-367`). Tests `humanize_detect_git_state` for normal, detached, rebase, merge, shallow clone, non-git, and permission-error scenarios (`377-500`).
- inputs_outputs_state: Inputs are temporary git repositories, git command behavior, and production functions. `humanize_parse_git_status` returns `modified|added|deleted|untracked|insertions|deletions`, with a seventh message field for non-git repos (`scripts/humanize.sh:164-210`). `humanize_detect_git_state` returns one of `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo` based on `.git` metadata (`scripts/humanize.sh:114-162`). The test outputs pass/fail via helper functions and exits with `print_test_summary` status (`tests/robustness/test-git-operations-robustness.sh:502-507`).
- gates_or_invariants: Git status counts must be stable across common working-tree states. Renames count as modified by design (`scripts/humanize.sh:187`; test expectation at `tests/robustness/test-git-operations-robustness.sh:265-277`). Non-git dirs must not crash and should return a detectable non-git marker (`scripts/humanize.sh:167-170`; test at `168-177`). Git-state detection prioritizes rebase, then merge, then shallow, then detached, then normal (`scripts/humanize.sh:136-161`), and tests simulate those sentinel files/directories directly.
- dependencies_and_callers: Depends on local git CLI availability and writable temp test dirs. Production callers include monitor/status code paths in `scripts/humanize.sh`; `_parse_git_status` aliases `humanize_parse_git_status` inside monitor setup (`scripts/humanize.sh:372` from `rg`). Related robustness coverage also exists in `tests/robustness/test-hook-input-robustness.sh`, which checks these functions are callable.
- edge_cases_or_failure_modes: Covers detached HEAD, empty repos without commits, rebase/merge/shallow sentinel state, permission-denied `.git`, filenames with spaces, binary files, many untracked files, and mixed staged/unstaged state. A notable nuance: `git diff --shortstat HEAD` may fail in no-commit repos, so production falls back to `git diff --shortstat` (`scripts/humanize.sh:197-198`). Permission-error behavior is platform-dependent, and the test accepts either `permission_error` or `not_a_repo` (`tests/robustness/test-git-operations-robustness.sh:478-496`).
- validation_or_tests: This file is itself the validation asset for the production git helpers. It directly exercises `humanize_parse_git_status` and `humanize_detect_git_state` from the sourced production script.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ADD_CAREFUL_MODE_V1_6_5-HZ-012, ADD_CAREFUL_MODE_V1_6_5-HZ-042, ADD_CAREFUL_MODE_V1_6_5-HZ-072, ADD_CAREFUL_MODE_V1_6_5-HZ-102, ADD_CAREFUL_MODE_V1_6_5-HZ-132
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`