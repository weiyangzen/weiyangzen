# agent_17 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-017 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Defines the read-only relevance-classification subagent used by `gen-plan` before deeper draft analysis. Its frontmatter declares `name: draft-relevance-checker`, purpose, `model: haiku`, and read/search-only tools `Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-5`.
- algorithmic_behavior: The agent receives draft document content, quickly samples repository context, compares draft semantics to repository concepts/components/files/features, and returns exactly one verdict shape: `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` (`agents/draft-relevance-checker.md:12-29`). The decision policy is intentionally lenient: informal or multilingual drafts are allowed, semantic relevance matters more than syntax, and doubt should resolve to relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft content plus repository context read through README/CLAUDE/docs, directory structure, and file/function/feature references (`agents/draft-relevance-checker.md:14-25`). Output is a single textual verdict. It does not mutate repository or loop state because its declared tools are read/search-only.
- gates_or_invariants: This is a gate in `commands/gen-plan.md`: after IO validation, the command invokes `humanize:draft-relevance-checker`; `NOT_RELEVANT` stops the command, while `RELEVANT` continues to Phase 3 (`commands/gen-plan.md:50-72`). Output vocabulary is part of the contract; downstream flow depends on the `RELEVANT`/`NOT_RELEVANT` prefix.
- dependencies_and_callers: Called by the `gen-plan` command via Task tool with haiku model (`commands/gen-plan.md:56-64`). Structural validation is covered by `tests/test-gen-plan.sh`, which checks the file exists, frontmatter name equals `draft-relevance-checker`, model equals `haiku`, tools are declared, and model naming is valid (`tests/test-gen-plan.sh:111-153`, `tests/test-gen-plan.sh:391-399`).
- edge_cases_or_failure_modes: Main risk is false-negative relevance rejection. The prompt mitigates this by checking repository semantics, allowing rough/informal/multilingual drafts, and leaning toward relevant when uncertain. Another edge is output drift: if the agent fails to use the exact verdict prefixes, `gen-plan` can no longer reliably branch.
- validation_or_tests: `tests/test-gen-plan.sh` is the direct structural spec for this agent file. The behavioral spec is embedded in `commands/gen-plan.md` Phase 2 and in the agent prompt itself.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-047 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR hook allowlist behavior around historical/current round todo and summary files. It verifies the central allowlist helper plus Read, Write, Edit, and Bash hook validators.
- algorithmic_behavior: The script creates a temporary git repo and active loop directory with `state.md` containing `current_round: 5` and other state fields (`tests/test-allowlist-validators.sh:29-64`). It sources `hooks/lib/loop-common.sh` (`tests/test-allowlist-validators.sh:15-17`), then asserts allowed and blocked paths through direct helper calls and validator hook JSON.
- inputs_outputs_state: Inputs are generated hook payloads such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}` and Bash command payloads through stdin to the hook scripts (`tests/test-allowlist-validators.sh:135-383`). State is isolated under `TEST_DIR`, including `.humanize/rlcr/2024-01-01_12-00-00/state.md`. Output is PASS/FAIL counters and process exit equal to `TESTS_FAILED` (`tests/test-allowlist-validators.sh:385-393`).
- gates_or_invariants: Production helper `is_allowlisted_file` only accepts exact files in the exact active loop directory: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`hooks/lib/loop-common.sh:426-447`). Write/Edit/Read validators block non-allowlisted todo files with exit 2 (`hooks/loop-write-validator.sh:55-62`, `hooks/loop-edit-validator.sh:38-45`, `hooks/loop-read-validator.sh:55-62`). Summary wrong-round validation is bypassed only for allowlisted files (`hooks/loop-write-validator.sh:228-238`, `hooks/loop-edit-validator.sh:155-172`, `hooks/loop-read-validator.sh:138-142`). Bash validator is stricter for todos: the command must modify `round-1-todos.md` or `round-2-todos.md` under the active loop path, preventing basename-only or different-root bypasses (`hooks/loop-bash-validator.sh:385-390`).
- dependencies_and_callers: Depends on `git`, `jq` indirectly through hook validators, `hooks/lib/loop-common.sh`, and the four hook scripts under `hooks/loop-*-validator.sh`. The test sets `CLAUDE_PROJECT_DIR="$TEST_DIR"` so active-loop discovery resolves inside the isolated repo (`tests/test-allowlist-validators.sh:132-134`).
- edge_cases_or_failure_modes: Covered edge cases include allowlisted names in wrong directories (`tests/test-allowlist-validators.sh:120-126`), unallowlisted future todos/summaries (`tests/test-allowlist-validators.sh:104-119`, `tests/test-allowlist-validators.sh:161-185`, `tests/test-allowlist-validators.sh:217-228`, `tests/test-allowlist-validators.sh:260-284`), generic relative `round-1-todos.md` without full path (`tests/test-allowlist-validators.sh:342-353`), old loop directories (`tests/test-allowlist-validators.sh:355-368`), and same active-loop basename under a different root (`tests/test-allowlist-validators.sh:370-383`).
- validation_or_tests: This file is itself the validation harness. It checks helper return codes, hook exit codes, and blocker text containing expected terms like `todos` or `round`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-077 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for attempts to modify `finalize-state.md`, the loop-managed state marker for Finalize Phase.
- algorithmic_behavior: The template tells the agent that `finalize-state.md` cannot be modified because the loop system manages it during Finalize Phase (`prompt-template/block/finalize-state-file-modification.md:1-3`). It redirects work to the only allowed Finalize Phase responsibilities: run the code-simplifier agent, commit changes, and write `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:5-8`).
- inputs_outputs_state: Input is an attempted Write/Edit/Bash modification targeting `finalize-state.md`. Output is the rendered block text returned by `finalize_state_file_blocked_message`, which loads this template with a fallback (`hooks/lib/loop-common.sh:478-485`). The state transition is negative: the attempted state-file mutation is blocked; ownership remains with the loop system.
- gates_or_invariants: `finalize-state.md` is protected before generic `state.md` matching because generic state matching can also match `finalize-state.md` (`hooks/loop-write-validator.sh:148-159`, `hooks/loop-edit-validator.sh:112-124`). Bash modifications to `finalize-state.md` are blocked with exit 2 unless the command is an authorized cancel operation (`hooks/loop-bash-validator.sh:139-145`). In contrast, writing `finalize-summary.md` in the active loop directory is explicitly allowed during Finalize Phase (`hooks/loop-write-validator.sh:162-172`).
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh` by Write, Edit, and Bash validators. It coordinates with Finalize Phase state detection in validators, which parse `finalize-state.md` instead of `state.md` when present (`hooks/loop-read-validator.sh:88-95`, `hooks/loop-bash-validator.sh:74-82`).
- edge_cases_or_failure_modes: The key security edge is substring/pattern overlap between `state.md` and `finalize-state.md`; production validators intentionally check finalize-state first. Another edge is legitimate cancellation: Bash protection delegates to `is_cancel_authorized` before blocking, so cancellation can still rename state through the controlled flow.
- validation_or_tests: Direct behavior is covered by finalize-phase validator tests elsewhere in the suite; the assigned template is the user-facing blocker body loaded by the shared helper and validators.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-107 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize Phase prompt template used after Codex review has passed. It defines the terminal workflow before loop completion: simplification-only refactor, verification, commit, and finalize summary.
- algorithmic_behavior: The prompt announces that implementation and acceptance criteria are complete, then instructs the agent to invoke `code-simplifier:code-simplifier` via Task tool (`prompt-template/claude/finalize-phase-prompt.md:1-14`). It constrains work to functionality-equivalent cleanup: no functional changes, no test regressions, no new bugs, and cleanup only (`prompt-template/claude/finalize-phase-prompt.md:16-24`). Focus areas include recently modified code, branch diff from `{{BASE_BRANCH}}` to `{{START_BRANCH}}`, duplicate consolidation, simpler control flow, and dead-code removal (`prompt-template/claude/finalize-phase-prompt.md:25-34`).
- inputs_outputs_state: Inputs are rendered placeholders `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:28-45`). Output is a block prompt emitted by the stop hook. State transition happens in `enter_finalize_phase`: `state.md` is renamed to `finalize-state.md`, then this template is loaded for the normal non-skipped path (`hooks/loop-codex-stop-hook.sh:1032-1042`, `hooks/loop-codex-stop-hook.sh:1079-1104`).
- gates_or_invariants: Before exiting, the agent must mark tasks completed via TaskUpdate, commit changes, and write the finalize summary (`prompt-template/claude/finalize-phase-prompt.md:41-51`). The stop hook only reaches Finalize Phase after implementation COMPLETE and review success; max-iteration termination bypasses finalize (`hooks/loop-codex-stop-hook.sh:1396-1435`, `hooks/loop-codex-stop-hook.sh:1405-1408`). Review phase state has an anti-tamper marker check: if `review_started=true` lacks `.review-phase-started`, the hook blocks as invalid state (`hooks/loop-codex-stop-hook.sh:1448-1463`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` in the normal review-passed path. It coordinates with `prompt-template/claude/finalize-phase-skipped-prompt.md` for skipped-review cases, `finalize-state.md` validators for state protection, and stop-hook checks requiring `finalize-summary.md` before completion.
- edge_cases_or_failure_modes: If `BASE_BRANCH` is missing, review is skipped and a different skipped-review prompt is used rather than this template (`hooks/loop-codex-stop-hook.sh:1415-1419`). If the agent modifies behavior during cleanup, violates tests, omits TaskUpdate completion, fails to commit, or omits the summary, the Finalize Phase contract is unmet. In review phase, a literal COMPLETE marker from the agent is ignored; review output controls the transition (`hooks/loop-codex-stop-hook.sh:1397-1401`).
- validation_or_tests: Finalize behavior is exercised by the broader finalize-phase test suite. This template’s direct contract is also enforced by hook flow: state rename, rendered prompt emission, protected finalize-state, allowed finalize-summary, and later stop-hook completion checks.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-137 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for production plan-file validation in `scripts/setup-rlcr-loop.sh`, focused on malformed, huge, unusual, missing, and disappearing plan files.
- algorithmic_behavior: The test creates an isolated temp repo, configures a private cache, and installs a mock `codex` binary to avoid calling the real tool (`tests/robustness/test-plan-file-robustness.sh:18-40`). Its `test_plan_validation` wrapper invokes `scripts/setup-rlcr-loop.sh` with a candidate plan path, captures output to a file, maps known validation errors to rejection, and treats downstream environment blockers such as missing codex/gitignore requirements as validation-passed once plan validation itself has cleared (`tests/robustness/test-plan-file-robustness.sh:48-105`).
- inputs_outputs_state: Inputs are generated plan files under `TEST_DIR`, passed as relative plan paths to `setup-rlcr-loop.sh`. Outputs are PASS/FAIL records through `tests/test-helpers.sh` and final exit from `print_test_summary` (`tests/robustness/test-plan-file-robustness.sh:108-121`, `tests/robustness/test-plan-file-robustness.sh:551-556`). State is isolated: a temp git repo is initialized and `.humanize/rlcr` may be created for accepted plans; trap cleanup removes the temp tree.
- gates_or_invariants: Production path gates reject shell metacharacters in plan paths (`scripts/setup-rlcr-loop.sh:341-348`), direct file symlinks (`scripts/setup-rlcr-loop.sh:353-356`), symlink parent traversal (`scripts/setup-rlcr-loop.sh:366-385`), missing files (`scripts/setup-rlcr-loop.sh:389-392`), and unreadable files (`scripts/setup-rlcr-loop.sh:395-398`). Content gates require at least 5 physical lines and at least 3 content lines, excluding blank lines, `#` comment lines, single-line HTML comments, and multi-line HTML comment bodies (`scripts/setup-rlcr-loop.sh:490-543`). The test helper mirrors this content-line algorithm (`tests/robustness/test-plan-file-robustness.sh:127-167`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `tests/test-helpers.sh`, `git`, shell utilities such as `wc`, `grep`, `mktemp`, and the injected mock `codex`. The wrapper deliberately separates plan validation from later setup concerns by accepting known post-validation blockers (`tests/robustness/test-plan-file-robustness.sh:70-95`).
- edge_cases_or_failure_modes: Production-invoked cases include valid structured plans, minimum-content plans, standard 5KB plans, rich markdown, empty files, comments-only files, large 1MB+ files, mixed CRLF/LF endings, and a missing file after deletion (`tests/robustness/test-plan-file-robustness.sh:176-364`, `tests/robustness/test-plan-file-robustness.sh:525-549`). The script also includes local/helper assertions for binary bytes, very long lines, shell-special characters in content, whitespace-only files, complex comments, nonexistent/unreadable/symlink/directory detection, and null bytes (`tests/robustness/test-plan-file-robustness.sh:366-523`). Important nuance: not every edge case calls production validation; several validate local helper or platform behavior only.
- validation_or_tests: This file is the validation harness. It verifies both acceptance and rejection behavior and includes timing for large-file validation (`tests/robustness/test-plan-file-robustness.sh:341-345`), ensuring prior SIGPIPE-sensitive output handling remains robust.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 Item Evidence headings present; one heading per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`