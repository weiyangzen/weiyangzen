# agent_09 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer for RLCR and PR-loop hooks. The directory contains `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`; together they implement hook input validation, state parsing, active-loop discovery, prompt/template rendering, path/round gates, cancel authorization, `.humanize` git-add protection, review-log issue extraction, and PR goal-tracker updates.
- algorithmic_behavior: `loop-common.sh` defines state schema constants and exit reasons at `hooks/lib/loop-common.sh:15-45`, validates hook JSON including null-byte, UTF-8, jq syntax, and `tool_name` at `:60-97`, requires tool input fields at `:102-115`, and detects deeply nested JSON at `:120-134`. It sources `template-loader.sh` and initializes `TEMPLATE_DIR` at `:140-149`. It selects only the newest active loop directory, requiring `state.md` or `finalize-state.md`, at `:157-175`. It has tolerant state parsing at `:211-248` and strict state validation at `:259-336`, including required `current_round`, `max_iterations`, `review_started`, and `base_branch`. It extracts Codex review issues by scanning for `[P?]` markers in the first 10 characters of a line and writes `round-N-review-result.md` at `:355-398`. It implements round-file helpers, allowlists, and blocking-message renderers at `:401-535`. It implements strict cancel authorization with signal-file, shell-injection, argument, exact-path, and symlink checks at `:557-716`. It maps PR-loop bot names, validates PR-loop paths and `round-N-pr-resolve.md` round numbers, and blocks PR-loop managed files at `:733-912`. It detects unsafe `git add` commands that would add `.humanize` at `:936-1032`, detects shell modification patterns at `:1077-1102`, renames state files through `end_loop` at `:1125-1151`, and performs idempotent PR goal-tracker table/log mutation at `:1174-1321`.
- inputs_outputs_state: Primary inputs are hook JSON on stdin, hook tool fields, repo paths, `.humanize/rlcr/*/state.md`, `.humanize/rlcr/*/finalize-state.md`, `.humanize/pr-loop/*/state.md`, review log files under `$CACHE_DIR`, template markdown files under `prompt-template`, shell command strings, and optional bot-results JSON. Outputs are exported shell variables such as `VALIDATED_TOOL_NAME`, `VALIDATED_TOOL_INPUT`, `STATE_*`, rendered block messages on stdout/stderr, validator return codes, generated review-result files, renamed state files, and mutated PR goal-tracker markdown. `template-loader.sh` outputs loaded/rendered template content, with `load_and_render_safe` falling back to caller-provided text when a template is missing or empty.
- gates_or_invariants: Hook input must be valid JSON, not null-containing, not invalid UTF-8 when `iconv` is available, and no deeper than the configured depth. Strict state files must have YAML frontmatter separators, numeric `current_round` and `max_iterations`, boolean `review_started`, and `base_branch`. Active-loop lookup intentionally ignores older directories even if they contain state, preventing stale loop revival. Cancel is authorized only when `.cancel-requested` exists and the command is a two-argument `mv` from current `state.md` or `finalize-state.md` to `cancel-state.md`, with no shell operators, substitutions, leftover variables, mixed quoting, extra args, or symlinked source. `.humanize` git-add protection blocks direct `.humanize` references, force-add broad scopes, and broad add/all commands when `.humanize` exists and is not ignored. Template rendering is single-pass, so replacement values containing `{{OTHER}}` are not recursively expanded.
- dependencies_and_callers: `loop-common.sh` is sourced by hook validators such as `hooks/loop-read-validator.sh:17-19` and `hooks/loop-write-validator.sh:17-19`. Read validation uses `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, `find_active_loop`, `is_allowlisted_file`, `todos_blocked_message`, and `parse_state_file_strict` at `hooks/loop-read-validator.sh:27-62` and `:94-99`. Write validation uses the same input/state gates and blocks todos/prompt/PR-loop managed files at `hooks/loop-write-validator.sh:27-67` and `:74-145`. Template functions are validated by `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/robustness/test-template-error-robustness.sh`, and `tests/robustness/test-template-stress-robustness.sh`. Review extraction is exercised by `tests/test-codex-review-merge.sh`. State parsing is exercised by `tests/robustness/test-state-file-robustness.sh`, `tests/robustness/test-state-transition-robustness.sh`, and `tests/robustness/test-concurrent-state-robustness.sh`. Cancel authorization is exercised by `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`.
- edge_cases_or_failure_modes: If `iconv` is absent, UTF-8 validation is skipped and jq becomes the effective JSON validity gate. If `jq` is absent, valid input is treated as invalid because jq calls fail. Strict parsing accepts negative numeric values syntactically for `current_round` and `max_iterations`; range semantics are left to callers. `detect_review_issues` deliberately ignores `[P?]` markers appearing after character 10, so indented or prose-late markers do not trigger continuation. `find_active_loop` only considers lexicographically newest timestamp directory. `is_cancel_authorized` lowercases the active-loop path for comparison, which is aligned with lowercased command input but can be sensitive on case-sensitive filesystems if callers do not normalize consistently. `git_adds_humanize` depends on current working directory for `.humanize` existence and `git check-ignore`. `render_template` comments document uppercase variable syntax, but the implementation accepts any variable name passed through the `TMPL_VAR_` environment convention and keeps unknown placeholders unchanged.
- validation_or_tests: `tests/test-template-references.sh` explicitly includes `hooks/lib/loop-common.sh` in its scan list at `tests/test-template-references.sh:57-64`, checks common template existence at `:152-167`, and requires critical validators to use `load_and_render_safe` at `:172-200`. Direct function tests appear across `tests/test-template-loader.sh`, `tests/test-codex-review-merge.sh`, `tests/test-state-exit-naming.sh`, `tests/test-humanize-escape.sh`, and multiple `tests/robustness/*` scripts. I inspected these references but did not execute tests in this read-only research run.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-039 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Cross-platform timeout abstraction used by setup scripts, hooks, monitor/API scripts, and robustness tests so git, gh, Codex, and shell operations do not hang indefinitely on macOS or Linux.
- algorithmic_behavior: `detect_timeout_impl` chooses `gtimeout`, then GNU `timeout`, then `python3`, then `python`, then `none` at `scripts/portable-timeout.sh:10-27`. The selected implementation is captured once in global `TIMEOUT_IMPL` at `:29`. `run_with_timeout` accepts `timeout_seconds command [args...]`, stores the remaining args as a Bash array, and dispatches by implementation at `:33-70`. GNU paths call `gtimeout "$timeout_secs" "${cmd[@]}"` or `timeout "$timeout_secs" "${cmd[@]}"` at `:38-46`. Python fallback uses `subprocess.run(sys.argv[1:], timeout=timeout_secs)` and maps `TimeoutExpired` to exit code `124` at `:47-62`. The `none` path emits a warning and runs the command without timeout at `:64-68`. `TIMEOUT_IMPL` is exported at `:73-76`.
- inputs_outputs_state: Inputs are the current `PATH`, available timeout/Python binaries, the timeout seconds value, and command argv. Outputs are the wrapped command’s stdout/stderr and exit code, with timeout intended to return `124` for GNU/Python implementations. Script state is the exported `TIMEOUT_IMPL`; callers source the file and use the function directly rather than relying on exported functions.
- gates_or_invariants: BSD/non-GNU `timeout` is rejected by requiring `timeout --version` to succeed at `scripts/portable-timeout.sh:13-19`. Command arguments are preserved as an array, avoiding lossy string re-parsing. The fallback contract is explicit: if no timeout backend exists, the command still runs and a warning is emitted.
- dependencies_and_callers: `tests/robustness/test-base-branch-detection.sh` sources this file at `tests/robustness/test-base-branch-detection.sh:19` and uses `run_with_timeout` in its branch detection helper at `:44`, `:51`, and `:57`. `scripts/setup-rlcr-loop.sh` sources it at `scripts/setup-rlcr-loop.sh:29` and uses it for git setup/base-branch detection at `:562`, `:604`, `:608`, `:624`, `:627`, `:632`, `:636`, and `:663`. Other direct callers include `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-plan-file-validator.sh`, `scripts/setup-pr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, and `tests/robustness/test-timeout-robustness.sh`.
- edge_cases_or_failure_modes: `timeout_secs` is interpolated into Python code without validation, so nonnumeric values can become Python syntax/runtime errors. The `none` backend can still hang forever. Python `subprocess.run(..., timeout=...)` kills the direct child process but does not necessarily clean a whole descendant process group. Backend detection happens at source time; later `PATH` changes do not update `TIMEOUT_IMPL` unless `detect_timeout_impl` is called again manually. Empty command input is passed through and fails according to the backend. Exit-code semantics for `timeout 0` can vary by backend and are tested separately in timeout robustness coverage.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` covers implementation detection, normal command execution, timeout exit behavior, argv preservation, command exit propagation, pipelines through `sh -c`, repeated calls, nonexistent commands, empty commands, special characters, trap behavior, subshells, and exported `TIMEOUT_IMPL` according to the references found at `tests/robustness/test-timeout-robustness.sh:16-266`. Base-branch tests also validate timeout use around missing origin remotes and local ref checks.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-069 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that guards template-reference integrity. It prevents validators from rendering empty or missing block messages when they block a hook operation.
- algorithmic_behavior: The script initializes project/template roots at `tests/test-template-references.sh:14-17`, color/counter state at `:18-27`, and pass/fail/warn helpers at `:29-49`. Section 1 scans a fixed list of template-using hook scripts at `:54-64`. It skips comment lines at `:87-91`, matches direct `load_template`, `load_and_render`, or `load_and_render_safe` calls using literal `"$TEMPLATE_DIR"` at `:93-96`, extracts the quoted template path via `sed`, increments reference count, and fails missing files at `:98-107`. Section 2 enumerates all `prompt-template/**/*.md` files with `find -print0` at `:121-126` and warns when a template path is not directly referenced under `hooks/` at `:130-141`. Section 3 hard-codes common block templates expected by `loop-common.sh` at `:149-167`. Section 4 requires critical validators to avoid unsafe `load_and_render` calls and use `load_and_render_safe` instead at `:172-200`. Final summary prints counts and exits 0 only when `TESTS_FAILED` is zero at `:206-230`.
- inputs_outputs_state: Inputs are the repository filesystem, `prompt-template` contents, and shell script source text under `hooks/` plus `hooks/lib/loop-common.sh`. Outputs are colored pass/fail/warn messages, summary counts, missing-template listings, and process exit code. Mutable state is only in shell arrays/counters: `MISSING_TEMPLATES`, `UNREFERENCED`, `FOUND_REFERENCES`, `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS`.
- gates_or_invariants: Every direct template reference in the scanned scripts must resolve to an existing file. Common templates used by shared blocking functions must exist. Critical read/write/edit validators must use the safe rendering path. Unreferenced templates are warnings, not failures, because dynamic use is allowed. Missing scanned scripts are warnings, not hard failures.
- dependencies_and_callers: This test is coupled to `hooks/lib/template-loader.sh` function names and to `hooks/lib/loop-common.sh` block-message calls. The scanned script list is fixed at `tests/test-template-references.sh:57-64`, so adding a new template-consuming hook outside that list requires updating this test or it will not be checked by Section 1. It verifies templates used by validators such as `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`.
- edge_cases_or_failure_modes: The regex only detects single-line calls with literal `"$TEMPLATE_DIR"` followed by a quoted static template path. It misses dynamic paths, alternate template directory variables, unquoted paths, multiline calls, or scripts outside the fixed scan list. Section 2 searches only under `hooks/`, so templates used only by `scripts/` may be warned as unreferenced. The script uses `set -uo pipefail` without `set -e`, which is intentional for accumulating failures, but unexpected command failures may need explicit handling to avoid misleading counts.
- validation_or_tests: The script is itself the validation artifact. It also indirectly validates the common template `prompt-template/block/todos-file-access.md` via the `COMMON_TEMPLATES` list at `tests/test-template-references.sh:152-164`, and validates safe fallback usage in critical validators at `:176-200`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-099 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Static block-message template used when a hook blocks access to legacy `round-*-todos.md` files and directs the agent to use native task tooling instead.
- algorithmic_behavior: The template renders a heading at `prompt-template/block/todos-file-access.md:1`, the direct prohibition at `:3`, the required alternate tools at `:5`, and rationale about UI/state integration at `:7-8`. It contains no placeholders, so rendering is a pure file-load operation.
- inputs_outputs_state: Input is the template file content loaded through `load_and_render_safe`. Output is the user-facing block message written to stderr by validators. There is no internal state transition inside the template, but as part of hook execution it contributes to validator exit code `2` for blocked operations.
- gates_or_invariants: The behavioral contract is “do not create or access `round-*-todos.md`; use TaskCreate, TaskUpdate, TaskList.” In implementation, this message is reached via `todos_blocked_message` in `hooks/lib/loop-common.sh:451-457`. Read and write validators call it when a todos file is not allowlisted at `hooks/loop-read-validator.sh:55-62` and `hooks/loop-write-validator.sh:55-62`. `hooks/loop-edit-validator.sh` and `hooks/loop-bash-validator.sh` also invoke the same block path for todo-file edits or shell modifications according to references at `hooks/loop-edit-validator.sh:38-43` and `hooks/loop-bash-validator.sh:385-390`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` and `load_and_render_safe` fallback behavior. The direct caller is `todos_blocked_message`; downstream callers are read/write/edit/bash validators. `tests/test-template-references.sh` includes this exact path in `COMMON_TEMPLATES` at `tests/test-template-references.sh:152-164`.
- edge_cases_or_failure_modes: The template wording says not to access any `round-*-todos.md`, while the validator layer has a narrow allowlist for active-loop bootstrap files such as `round-1-todos.md` and `round-2-todos.md` in `hooks/lib/loop-common.sh:429-447`. That means the message is accurate for blocked cases but does not describe the internal allowlist exception. The `action` argument passed to `todos_blocked_message` is currently unused, so Read/Write/Edit/Bash-specific wording is not reflected.
- validation_or_tests: Template presence is checked by `tests/test-template-references.sh:152-164`. Allowlist/blocking behavior around todos files is covered by `tests/test-allowlist-validators.sh`, including active-loop allowlist cases, wrong-directory blocks, non-allowlisted round blocks, and Bash path-restricted checks as referenced at `tests/test-allowlist-validators.sh:72-379`.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-129 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness test for RLCR setup base-branch auto-detection, especially missing-origin and local-main/local-master fallback behavior.
- algorithmic_behavior: The script sources shared test helpers and `portable-timeout.sh` at `tests/robustness/test-base-branch-detection.sh:16-20`, initializes a temporary test directory at `:24`, and defines a local `detect_base_branch` helper at `:37-64`. The helper tries remote default branch from `git remote show origin | grep "HEAD branch:" | sed ...` with `|| true` to survive missing origin at `:41-48`, then local `main` at `:50-54`, local `master` at `:56-60`, and returns failure at `:62-63`. Test 1 creates a no-origin repo with `main` and expects `main` at `:73-93`. Test 2 creates a no-origin repo with `master` and no `main`, expecting `master`, at `:99-122`. Test 3 creates only `develop` and expects detection failure at `:129-149`. Test 4 creates a bare origin and local `main`, pushes it, and accepts detected `main` or another non-failed branch result at `:155-191`. The summary is printed at `:197-198`.
- inputs_outputs_state: Inputs are local git binaries, timeout backend, temporary filesystem, branch refs, and optional origin remote metadata. Outputs are pass/fail messages through `test-helpers.sh`, final summary, and exit status from the test helper framework. State changes are confined to test repos under `$TEST_DIR`, including `git init`, local branches, commits, a bare remote, and remote tracking setup.
- gates_or_invariants: Detection priority under test is remote default, then local `main`, then local `master`, then failure. Missing `origin` must not terminate the script under `set -euo pipefail`; the remote command is guarded with `|| true` at `tests/robustness/test-base-branch-detection.sh:42-44`. All git probes are wrapped with `run_with_timeout`, so hanging git/remote calls are bounded.
- dependencies_and_callers: The test states it replicates `setup-rlcr-loop.sh` base-branch detection. The corresponding production logic is at `scripts/setup-rlcr-loop.sh:596-650`: user-specified `BASE_BRANCH` is validated first at `:601-618`, remote default is considered only if the branch exists locally at `:620-630`, local `main` at `:631-634`, local `master` at `:635-638`, and no detected branch exits with a diagnostic at `:639-649`. Production also captures `BASE_COMMIT` at `scripts/setup-rlcr-loop.sh:660-668`.
- edge_cases_or_failure_modes: The test helper does not include a user-specified `--base-branch` path despite the file header listing it. It also does not fully mirror production’s local-existence check for remote default branches; production verifies `refs/heads/$REMOTE_DEFAULT` at `scripts/setup-rlcr-loop.sh:625-629`, while the test helper returns any nonempty non-unknown remote default at `tests/robustness/test-base-branch-detection.sh:45-48`. Test 4 is loose: any non-`FAILED` result can pass at `:181-190`, so it is not a strict remote-priority assertion. Missing coverage includes remote default `(unknown)`, remote default existing only on origin, explicit base branch existing only remotely, YAML-unsafe branch names, `BASE_COMMIT` capture failure, and timeout failure behavior.
- validation_or_tests: This assigned file is the validation artifact. It validates the intended fallback behavior with isolated git repositories and relies on `scripts/portable-timeout.sh` for bounded git calls. I inspected it and the corresponding production setup logic but did not execute it in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item sections present above; each assigned item is represented by one section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`