# agent_24 add-careful-mode-v1.6.5 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `a12de5d9f36bb10cd62955881f4e76d67d3f50ce`

## Item Evidence

### ADD_CAREFUL_MODE_V1_6_5-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude Code plugin hook dispatch table for the RLCR and PR loop gate system. It binds lifecycle events and tool matchers to validator/stop-hook executables rather than implementing validation itself.
- algorithmic_behavior: On `UserPromptSubmit`, it invokes `hooks/loop-plan-file-validator.sh` to validate plan-loop prompt/file preconditions (`hooks/hooks.json:4-12`). On `PreToolUse`, it routes `Write`, `Edit`, `Read`, and `Bash` tool calls to the corresponding loop validators (`hooks/hooks.json:14-50`). On `Stop`, it invokes both `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh`, each with a 7200 second timeout (`hooks/hooks.json:52-65`).
- inputs_outputs_state: Input is Claude’s hook event stream and hook payloads passed to the configured commands through the Claude hook runtime. Output is whatever each validator/stop hook returns, typically JSON decisions such as block/allow plus human-facing reasons. The JSON file itself has no mutable state; it connects hook events to stateful scripts that read `.humanize` loop state, git state, summaries, todos, plan backups, and PR loop state.
- gates_or_invariants: Invariant is event-to-validator coverage: prompt submission, tool pre-use, and stop events must all be intercepted for loop safety. `${CLAUDE_PLUGIN_ROOT}` must resolve to the plugin root, and all referenced scripts must exist and be executable. Stop hooks are deliberately long-running-capable because Codex/PR review phases may run during stop handling.
- dependencies_and_callers: Consumed by Claude Code’s plugin hook loader. It depends on `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, `loop-bash-validator.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh`. Repository tests call those hook scripts directly, for example `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, and robustness hook tests.
- edge_cases_or_failure_modes: If `${CLAUDE_PLUGIN_ROOT}` is unset or wrong, all command paths fail. If both RLCR and PR loop state exist, both stop hooks are dispatched for the same stop event and must self-select/no-op correctly. If a stop hook exceeds 7200 seconds, the hook runtime can time it out. A malformed matcher or missing script would remove an entire gate class.
- validation_or_tests: Direct tests exercise the referenced scripts rather than this JSON file as a standalone unit. `tests/test-template-references.sh` includes the stop hook and tool validators in its script list; `tests/test-plan-file-hooks.sh` explicitly targets the plan validator, write/edit/bash validators, and stop hook.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:gen-plan` command surface. It verifies command metadata, its draft relevance agent contract, version consistency, frontmatter conventions, model-name rules, English-only content, and the IO validator’s exit-code gate behavior.
- algorithmic_behavior: Initializes project paths for `commands`, `agents`, and repository root (`tests/test-gen-plan.sh:11-14`), tracks pass/fail counters (`tests/test-gen-plan.sh:22-40`), then runs positive checks for `commands/gen-plan.md` existence, description, allowed tools, argument hint, and `agents/draft-relevance-checker.md` metadata (`tests/test-gen-plan.sh:55-157`). It verifies version agreement across `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md` (`tests/test-gen-plan.sh:160-180`). Negative fixtures are created in a temporary directory to ensure invalid names, missing frontmatter, malformed YAML, and invalid model aliases are rejected by helper predicates (`tests/test-gen-plan.sh:191-399`). It then drives `scripts/validate-gen-plan-io.sh` through expected exit codes for missing option values, unknown options, missing input, empty input, bad output target, valid paths, and help (`tests/test-gen-plan.sh:426-542`).
- inputs_outputs_state: Inputs are repository files under `commands/`, `agents/`, `.claude-plugin/`, `README.md`, and the executable `scripts/validate-gen-plan-io.sh`. The test creates temporary fixture files/directories with `mktemp -d` and cleans them via `trap` (`tests/test-gen-plan.sh:191-193`). Output is colored PASS/FAIL text and process exit `0` only when `TESTS_FAILED` is zero (`tests/test-gen-plan.sh:547-562`). State mutation is limited to temp fixture directories and validator-created temp output files.
- gates_or_invariants: Command and agent names must match lowercase kebab-case via `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:196-199`). Command frontmatter must start with `---` and include `description:` (`tests/test-gen-plan.sh:201-206`). Agent frontmatter must include `name:` and `description:` (`tests/test-gen-plan.sh:208-214`). Model values must be exact `opus|sonnet|haiku` or full IDs prefixed by `claude-`, `gpt-`, `o[0-9]`, or `gemini-` (`tests/test-gen-plan.sh:350-358`). Emoji/CJK characters are rejected with `grep -P` when available (`tests/test-gen-plan.sh:404-423`). The IO validator contract is exact: invalid arguments exit `6`, missing input `1`, empty input `2`, missing output dir `3`, existing output or output directory `4`, valid path `0`, help `6`.
- dependencies_and_callers: Listed in the aggregate test runner at `tests/run-all-tests.sh:52`. It validates `commands/gen-plan.md`, whose frontmatter allows only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh:*)`, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` (`commands/gen-plan.md:1-12`). The command’s workflow requires IO validation first and maps validator exit codes to stop/continue behavior (`commands/gen-plan.md:30-47`), then invokes the `humanize:draft-relevance-checker` agent (`commands/gen-plan.md:50-72`). The agent itself declares `name: draft-relevance-checker`, `model: haiku`, and `tools: Read, Glob, Grep` (`agents/draft-relevance-checker.md:1-6`). The IO script defines the authoritative exit-code table (`scripts/validate-gen-plan-io.sh:4-12`), argument parser (`scripts/validate-gen-plan-io.sh:29-67`), path checks (`scripts/validate-gen-plan-io.sh:79-124`), and template/draft output creation (`scripts/validate-gen-plan-io.sh:133-160`).
- edge_cases_or_failure_modes: YAML validation is a lightweight shell heuristic, not a full YAML parser, so it can miss semantically invalid but syntactically plausible frontmatter. `grep -P` may be unavailable; the test suppresses errors with `2>/dev/null`, so the Emoji/CJK check can become environment-dependent. The valid-path validator test invokes a script that copies the plan template and appends draft content, so it depends on writable temp directories and the presence of `prompt-template/plan/gen-plan-template.md`.
- validation_or_tests: This file is itself the validation asset. It does not prove generated plan semantic quality; it proves the command scaffold, relevance-agent metadata, content hygiene, version sync, and IO validator exit behavior.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Fail-closed block reason template for the RLCR stop hook when `git status --porcelain` cannot complete successfully. It is part of the repository-state verification gate before a loop round can exit.
- algorithmic_behavior: Renders a Markdown block headed “Git Status Failed” and interpolates `{{GIT_STATUS_EXIT}}` into the failure message (`prompt-template/block/git-status-failed.md:1-4`). The body explains possible causes: lock contention, invalid repository state, or large repository slowness (`prompt-template/block/git-status-failed.md:5-8`), then instructs manual `git status` checking (`prompt-template/block/git-status-failed.md:10`).
- inputs_outputs_state: Input is the `GIT_STATUS_EXIT` template variable supplied by `hooks/loop-codex-stop-hook.sh`. Output is Markdown text used as the hook block reason. It has no state mutation. The stop hook caches git status output, captures the status exit code without `|| echo ""`, and if nonzero loads this template with `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:398-425`).
- gates_or_invariants: The invariant is fail-closed git verification: if git status fails or times out, the stop hook blocks instead of treating the repository as clean. This protects downstream gates that depend on cached git status, including large-file detection, dirty-worktree checks, and push-every-round checks.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` with `GIT_STATUS_EXIT` (`hooks/loop-codex-stop-hook.sh:421-422`). Rendering behavior comes from `hooks/lib/template-loader.sh`, where `render_template` performs single-pass replacement and preserves unknown placeholders (`hooks/lib/template-loader.sh:50-132`), while `load_and_render_safe` falls back on missing/empty templates (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook fallback still blocks (`hooks/loop-codex-stop-hook.sh:416-424`). If `GIT_STATUS_EXIT` is not passed, the placeholder remains visible because missing variables are intentionally preserved by the renderer (`hooks/lib/template-loader.sh:119-122`). If git is unavailable or the directory is not a git repo, this specific template is not used because the stop hook first checks `command -v git` and `git rev-parse --git-dir` (`hooks/loop-codex-stop-hook.sh:408`).
- validation_or_tests: Template reference safety is covered by template-reference tests that scan hook template usage. The behavior also sits inside stop-hook tests that exercise hook block paths. The direct caller includes an inline fallback with the same variable, so the gate remains testable even if the template file is absent.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-114 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt contract fragment for the `--push-every-round` mode. It tells the implementation agent that commits must be pushed after each round when that mode is active.
- algorithmic_behavior: Contains a single note: when `--push-every-round` is enabled, push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:1-2`). In later RLCR rounds, the stop hook appends this template to the next-round prompt only when `PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:1580-1587`). Round 0 setup uses an inline equivalent note under the same flag (`scripts/setup-rlcr-loop.sh:970-975`).
- inputs_outputs_state: Input is the loop state/config flag represented as `PUSH_EVERY_ROUND`. The template has no variables. Output is appended prompt text in the generated round prompt. It does not mutate loop state; it shapes the next agent action so the hard unpushed-commit gate is less surprising.
- gates_or_invariants: The corresponding hard gate is in the stop hook: if `PUSH_EVERY_ROUND` is true, it checks whether the local branch is ahead of remote with `git status -sb`, extracts the ahead count and branch, and blocks exit when unpushed commits are detected (`hooks/loop-codex-stop-hook.sh:574-603`). The invariant is prompt/gate alignment: the prompt says push each round, and the stop hook enforces that before allowing exit.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` (`hooks/loop-codex-stop-hook.sh:1582`). The fallback if missing is “Also push your changes after committing” (`hooks/loop-codex-stop-hook.sh:1583-1585`). The setup script separately documents the CLI flag as requiring git push after each round and emits the round-0 note inline.
- edge_cases_or_failure_modes: If the template is missing, the fallback keeps the prompt contract but loses exact wording. Because the template is only appended in the “review found issues, continue loop” path, round 0 relies on the setup script’s inline message rather than this file. The prompt note itself is advisory; enforcement depends on the stop hook’s git-ahead detection and remote tracking visibility.
- validation_or_tests: Push-every-round behavior is exercised indirectly through stop-hook and bash-validator tests around unpushed commits and push blocking. This template is also discoverable by template-reference tests through its stop-hook caller.
- skip_candidate: `no`

### ADD_CAREFUL_MODE_V1_6_5-HZ-144 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for the template rendering subsystem used by RLCR/PR validators and stop hooks. It stress-tests `render_template`, `load_and_render`, and `load_and_render_safe` under large values, large templates, many variables, and shell/sed/regex-sensitive characters.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and shared test helpers, then creates a temp test directory (`tests/robustness/test-template-stress-robustness.sh:14-19`). Positive tests cover standard substitution, fallback rendering for missing templates, and loading a roughly 1KB template file (`tests/robustness/test-template-stress-robustness.sh:33-72`). Stress tests cover 10KB values, 100KB values, a generated 100KB template file, and 50 substitutions in one template (`tests/robustness/test-template-stress-robustness.sh:82-151`). Edge tests cover regex metacharacters, ampersands, backslashes, multiline values, empty values, placeholder-in-value injection prevention, dollar signs, repeated variables, boundary placeholders, and templates containing only a placeholder (`tests/robustness/test-template-stress-robustness.sh:161-283`). It exits through the shared test summary (`tests/robustness/test-template-stress-robustness.sh:285-290`).
- inputs_outputs_state: Inputs are generated in-memory templates, generated fixture files under `$TEST_DIR`, and variable assignments passed as `VAR=value` arguments. Output is PASS/FAIL reporting and final test exit status. State is limited to temp files such as `standard.md` and `large.md`; it does not modify repository files.
- gates_or_invariants: Enforces that the renderer can safely handle large data without truncating substitutions, can load/render files over 100KB, preserves values containing regex and replacement-special characters, supports empty and multiline values, replaces repeated placeholders consistently, and prevents placeholder injection by keeping `{{VAR_B}}` embedded in a substituted value from being re-expanded (`tests/robustness/test-template-stress-robustness.sh:218-228`). These map to `template-loader.sh` invariants: single-pass replacement (`hooks/lib/template-loader.sh:54-57`), unknown placeholders remain as-is (`hooks/lib/template-loader.sh:119-122`), and fallback rendering is used when templates are missing or render empty (`hooks/lib/template-loader.sh:167-203`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, whose `render_template` uses an awk scanner over `{{...}}` placeholders and environment-prefixed variables (`hooks/lib/template-loader.sh:58-132`), plus `load_and_render` and `load_and_render_safe` (`hooks/lib/template-loader.sh:134-203`). The template loader is called broadly by RLCR/PR hook scripts for block messages, next-round prompts, review prompts, and goal-tracker update sections.
- edge_cases_or_failure_modes: The test measures elapsed time but does not set performance thresholds, so it detects functional failure more than latency regression. “Unicode content” is labeled as Unicode but uses ASCII content only (`tests/robustness/test-template-stress-robustness.sh:230-239`). It does not test circular references because the loader intentionally does not support inter-variable references. Very large shell variables and multiline environment values may expose platform limits outside normal repository use.
- validation_or_tests: This file is itself the robustness test. It complements `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and `tests/robustness/test-template-error-robustness.sh`, which cover broader normal, comprehensive, and error-path template behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`