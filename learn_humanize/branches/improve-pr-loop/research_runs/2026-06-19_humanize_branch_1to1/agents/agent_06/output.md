# agent_06 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts/` is the runtime command layer for the Humanize loop system. It contains PR-loop setup, cancellation, PR comment fetching, reviewer polling/status/reaction helpers, shared monitor utilities, RLCR setup/cancel parity scripts, timeout portability, and gen-plan IO validation. The core descendants are all shell scripts plus `scripts/lib/monitor-common.sh`; there are no deeper algorithmic subdirectories beyond `scripts/lib`.
- algorithmic_behavior: The PR-loop path begins in `scripts/setup-pr-loop.sh`, which parses bot/model/timeout options, enforces mutual exclusion with RLCR loops, validates `git`, `gh`, and `codex`, resolves the PR and base repository including fork cases, classifies startup state, writes `.humanize/pr-loop/<timestamp>/state.md`, fetches initial comments, creates `goal-tracker.md`, and renders `round-0-prompt.md` from templates (`scripts/setup-pr-loop.sh:110`, `scripts/setup-pr-loop.sh:220`, `scripts/setup-pr-loop.sh:312`, `scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:712`, `scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:800`). The stop-hook caller then uses `scripts/poll-pr-reviews.sh` for polling and `scripts/check-bot-reactions.sh` for reaction gates (`hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:675`, `hooks/pr-loop-stop-hook.sh:776`). `scripts/check-pr-reviewer-status.sh` computes cases 1-5 from bot comment freshness relative to latest PR commit (`scripts/check-pr-reviewer-status.sh:22`, `scripts/check-pr-reviewer-status.sh:179`, `scripts/check-pr-reviewer-status.sh:220`). `scripts/humanize.sh` exposes `humanize monitor rlcr|pr` and maps session files into terminal status (`scripts/humanize.sh:883`, `scripts/humanize.sh:931`). `scripts/setup-rlcr-loop.sh` is the sibling RLCR state-machine bootstrapper, sharing the active-loop exclusion model and state-file conventions (`scripts/setup-rlcr-loop.sh:240`, `scripts/setup-rlcr-loop.sh:709`). `scripts/validate-gen-plan-io.sh` is a separate gen-plan validator/copy helper, not PR-loop-specific, but still command-surface validation code (`scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:148`).
- inputs_outputs_state: Inputs include CLI flags and positional arguments, environment variables such as `CLAUDE_PROJECT_DIR`, repo/PR metadata from `git` and `gh`, GitHub API JSON, template files under `prompt-template/`, and active loop state under `.humanize/`. Outputs include generated state files, prompt/comment/resolve/check/feedback files, JSON hook decisions, Markdown comment bundles, goal trackers, and terminal monitor output. PR setup writes these state fields: `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, `codex_timeout`, `poll_interval`, `poll_timeout`, `started_at`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id` (`scripts/setup-pr-loop.sh:615`). Cancellation scripts transition active loops by touching `.cancel-requested` and renaming `state.md` to `cancel-state.md` (`scripts/cancel-pr-loop.sh:118`, `scripts/cancel-rlcr-loop.sh:134`). Monitors infer active/final states through `state.md` and `*-state.md` files (`scripts/lib/monitor-common.sh:154`).
- gates_or_invariants: Major invariants are: only one RLCR or PR loop active at a time (`scripts/setup-pr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:240`); PR branch/model/effort fields must be YAML-safe (`scripts/setup-pr-loop.sh:384`, `scripts/setup-pr-loop.sh:393`); PR numbers must be numeric in fetch/poll scripts (`scripts/fetch-pr-comments.sh:103`, `scripts/poll-pr-reviews.sh:99`); `gh`/`jq` prerequisites are mandatory for PR API helpers (`scripts/fetch-pr-comments.sh:112`, `scripts/poll-pr-reviews.sh:108`); fork PRs must resolve to the base repository before API operations (`scripts/setup-pr-loop.sh:290`, `scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`); Claude-trigger re-review requires an `eyes` reaction when applicable (`scripts/setup-pr-loop.sh:564`, `hooks/pr-loop-stop-hook.sh:767`); Codex approval can arrive as a `+1` reaction and removes `codex` from `active_bots` (`hooks/pr-loop-stop-hook.sh:661`, `hooks/pr-loop-stop-hook.sh:984`); PR-loop monitor phase is derived from state files and latest `round-*-pr-check.md` growth (`scripts/lib/monitor-common.sh:267`).
- dependencies_and_callers: `commands/start-pr-loop.md` invokes `scripts/setup-pr-loop.sh`; `commands/cancel-pr-loop.md` invokes `scripts/cancel-pr-loop.sh` (found by reference search; command files point to those scripts). `hooks/pr-loop-stop-hook.sh` is the primary runtime caller for `poll-pr-reviews.sh`, `check-bot-reactions.sh`, and `check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:1547`). `scripts/setup-pr-loop.sh` calls `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:587`). `scripts/setup-pr-loop.sh` and RLCR scripts source `scripts/portable-timeout.sh` and hook libraries such as `hooks/lib/template-loader.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:35`, `scripts/setup-pr-loop.sh:39`, `scripts/setup-pr-loop.sh:211`). `scripts/humanize.sh` sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6`).
- edge_cases_or_failure_modes: PR setup aborts when no bot is selected, an active sibling loop exists, the repo has no commit, `gh` is unauthenticated, `codex` is absent, no PR is found, PR is closed/merged, branch/model/effort are YAML-unsafe, trigger comments cannot be posted/found for stale-review cases, or Claude `eyes` is missing (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:225`, `scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:351`, `scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:384`, `scripts/setup-pr-loop.sh:528`, `scripts/setup-pr-loop.sh:568`, `scripts/setup-pr-loop.sh:587`). Poll/fetch helpers degrade API endpoint failures to empty arrays plus warnings so downstream jq formatting continues (`scripts/fetch-pr-comments.sh:199`, `scripts/fetch-pr-comments.sh:445`, `scripts/poll-pr-reviews.sh:225`). RLCR setup has stricter plan-file path/tracking/content gates, including no absolute paths, spaces, metacharacters, symlinks, submodules, tracked-plan mismatch, and insufficient content (`scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:327`, `scripts/setup-rlcr-loop.sh:336`, `scripts/setup-rlcr-loop.sh:347`, `scripts/setup-rlcr-loop.sh:408`, `scripts/setup-rlcr-loop.sh:447`, `scripts/setup-rlcr-loop.sh:483`). `git status --short` was not relied on during research because this read-only export lacks normal `.git` access and emitted sandbox/Xcode cache errors.
- validation_or_tests: PR-loop aggregate tests source `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:32`). Script tests validate setup/cancel/fetch/poll argument errors and cancellation behavior (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`). Hook tests validate YAML list storage, protected PR-loop files, sorting, dedupe, bot status extraction, trigger detection, approval markers, fixture-backed comment fetching/polling, and monitor parsing (`tests/test-pr-loop-hooks.sh:27`, `tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:222`, `tests/test-pr-loop-hooks.sh:271`, `tests/test-pr-loop-hooks.sh:327`, `tests/test-pr-loop-hooks.sh:390`, `tests/test-pr-loop-hooks.sh:415`, `tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1322`, `tests/test-pr-loop-hooks.sh:1548`). Stop-hook tests cover force-push/trigger validation, approve-state creation, unpushed commits, Codex `+1`, Claude `eyes` timeout, dynamic startup-case update, and goal-tracker mixed approval (`tests/test-pr-loop-stophook.sh:21`, `tests/test-pr-loop-stophook.sh:247`, `tests/test-pr-loop-stophook.sh:453`, `tests/test-pr-loop-stophook.sh:897`, `tests/test-pr-loop-stophook.sh:1011`, `tests/test-pr-loop-stophook.sh:1146`, `tests/test-pr-loop-stophook.sh:1520`). I inspected these tests but did not execute them, consistent with the research-only/no-modification assignment.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-036 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: This script is the PR-loop comment ingestion and normalization stage. It converts GitHub issue comments, inline review comments, and PR review submissions into one Markdown artifact consumed by PR-loop round prompts.
- algorithmic_behavior: It parses `<pr_number> <output_file>` plus `--after <timestamp>` and `--bots <bot1,bot2>` (`scripts/fetch-pr-comments.sh:27`). It validates required arguments and numeric PR number (`scripts/fetch-pr-comments.sh:92`). It requires `gh` and `jq` (`scripts/fetch-pr-comments.sh:112`). It resolves the correct PR base repository by checking the current repo first and parent repo for forks (`scripts/fetch-pr-comments.sh:126`, `scripts/fetch-pr-comments.sh:139`, `scripts/fetch-pr-comments.sh:143`). It fetches three endpoints with retry: issue comments, PR review comments, and PR reviews (`scripts/fetch-pr-comments.sh:184`, `scripts/fetch-pr-comments.sh:212`, `scripts/fetch-pr-comments.sh:216`, `scripts/fetch-pr-comments.sh:220`). It normalizes each source into common fields (`type`, `id`, `author`, `author_type`, `created_at`, `body`, optional path/line/state), inserts a placeholder body for empty approval-only reviews, deduplicates by `id`, optionally filters by `created_at > --after`, sorts human comments before bot comments and then newest first, then emits Markdown sections (`scripts/fetch-pr-comments.sh:260`, `scripts/fetch-pr-comments.sh:281`, `scripts/fetch-pr-comments.sh:303`, `scripts/fetch-pr-comments.sh:322`, `scripts/fetch-pr-comments.sh:327`, `scripts/fetch-pr-comments.sh:337`, `scripts/fetch-pr-comments.sh:357`).
- inputs_outputs_state: Inputs are GitHub CLI authentication context, PR number, destination Markdown path, optional timestamp filter, optional active bot list, and GitHub API payloads. Output is a Markdown file beginning with `# PR Comments for #<number>`, fetched timestamp, repository name, human and bot sections, and end marker (`scripts/fetch-pr-comments.sh:245`, `scripts/fetch-pr-comments.sh:358`, `scripts/fetch-pr-comments.sh:395`, `scripts/fetch-pr-comments.sh:425`, `scripts/fetch-pr-comments.sh:440`). It does not mutate loop state directly; `scripts/setup-pr-loop.sh` writes the output to `round-0-pr-comment.md` and appends it into `round-0-prompt.md` (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:763`).
- gates_or_invariants: Required gates are argument validation, numeric PR number, `gh`/`jq` availability, and successful PR-base-repo resolution (`scripts/fetch-pr-comments.sh:93`, `scripts/fetch-pr-comments.sh:103`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:151`). Formatting invariants are one unified schema across all comment sources, dedupe by `id`, human comments before bot comments, bot comments grouped by requested bot list when `--bots` is supplied, and explicit no-comments sentinel `*No comments found.*` (`scripts/fetch-pr-comments.sh:322`, `scripts/fetch-pr-comments.sh:334`, `scripts/fetch-pr-comments.sh:348`, `scripts/fetch-pr-comments.sh:381`). Bot-name mapping is hardcoded for `codex -> chatgpt-codex-connector[bot]`; other bot names map to `<bot>[bot]` (`scripts/fetch-pr-comments.sh:386`).
- dependencies_and_callers: Direct dependencies are `gh`, `jq`, `mktemp`, `sed`, `date`, and shell cleanup via `trap`. Primary caller is `scripts/setup-pr-loop.sh`, which uses this script to fetch initial round comments and pass the same active bot list for grouped output (`scripts/setup-pr-loop.sh:430`). Fixture-backed tests call it directly (`tests/test-pr-loop-hooks.sh:1251`, `tests/test-pr-loop-hooks.sh:1296`), and script-validation tests assert help/missing argument/invalid PR behavior (`tests/test-pr-loop-scripts.sh:259`).
- edge_cases_or_failure_modes: If a GitHub API endpoint fails after three retries, the script writes `[]` for that source, increments `API_FAILURES`, continues formatting, exits `0`, and appends a warning to the output (`scripts/fetch-pr-comments.sh:195`, `scripts/fetch-pr-comments.sh:199`, `scripts/fetch-pr-comments.sh:445`). jq processing failures for individual sources are tolerated with `|| true`, which can silently omit malformed endpoint data (`scripts/fetch-pr-comments.sh:278`, `scripts/fetch-pr-comments.sh:298`, `scripts/fetch-pr-comments.sh:320`). The timestamp filter is a lexicographic ISO comparison, so it assumes consistent ISO 8601 UTC-ish values (`scripts/fetch-pr-comments.sh:327`). `unique_by(.id)` can conflate issue comments, review comments, and PR reviews if GitHub IDs overlap across resource types, because the dedupe key is not namespaced by type (`scripts/fetch-pr-comments.sh:322`). `is_bot()` and `format_timestamp()` are defined but not used in the current script body (`scripts/fetch-pr-comments.sh:227`, `scripts/fetch-pr-comments.sh:239`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help output, missing PR, missing output file, and invalid PR number (`tests/test-pr-loop-scripts.sh:268`, `tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`). Fixture tests verify all comment types are emitted, approval-only reviews get `[Review state: APPROVED]`, and `--after` filters out earlier comments (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1277`, `tests/test-pr-loop-hooks.sh:1287`). The robustness suite also references fetch API failure and parsing scenarios, but I only inspected direct references rather than executing them.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-066 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: This file is the aggregate executable specification for PR-loop behavior. It is not the implementation itself; it initializes the test environment and dispatches to the script, hook, and stop-hook test modules.
- algorithmic_behavior: It sets strict shell mode, derives `SCRIPT_DIR` and `PROJECT_ROOT`, sources `test-helpers.sh` and `test-pr-loop-lib.sh`, calls `init_pr_loop_test_env`, sources the three PR-loop test modules, runs them in order, and then prints the summary (`tests/test-pr-loop.sh:13`, `tests/test-pr-loop.sh:15`, `tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`, `tests/test-pr-loop.sh:53`). The delegated suite order is significant: script-level parser/state tests first, hook validator/comment tests second, stop-hook state-transition tests third (`tests/test-pr-loop.sh:40`, `tests/test-pr-loop.sh:43`, `tests/test-pr-loop.sh:46`).
- inputs_outputs_state: Inputs are the repository checkout, sourced helper files, shell environment, and temporary test fixtures created by `init_pr_loop_test_env`. The test library creates a temp test directory, prepends a mock-bin path, and writes mock `gh` and `codex` commands to isolate GitHub/Codex dependencies (`tests/test-pr-loop-lib.sh:108`, `tests/test-pr-loop-lib.sh:112`, `tests/test-pr-loop-lib.sh:117`). Output is TAP-like/log-style pass/fail text from helper functions and an exit status from `print_test_summary` (`tests/test-pr-loop-lib.sh:128`). State created during execution is temporary test state such as `.humanize/pr-loop/<timestamp>/state.md`, mock GitHub fixture responses, and mock command binaries.
- gates_or_invariants: Because the runner uses `set -euo pipefail`, missing sourced files or unhandled failures in module setup abort the run (`tests/test-pr-loop.sh:13`). It requires the delegated functions `run_script_tests`, `run_hook_tests`, and `run_stophook_tests` to be defined by the modules it sources (`tests/test-pr-loop.sh:32`). The summary is the final gate: any nonzero `TESTS_FAILED` returns failure (`tests/test-pr-loop-lib.sh:137`). Mock isolation is an invariant of this runner; the modules assume `PROJECT_ROOT`, `TEST_DIR`, and mock PATH are initialized before tests run (`tests/test-pr-loop-lib.sh:108`).
- dependencies_and_callers: It depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:32`). The script tests exercise `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh` (`tests/test-pr-loop-scripts.sh:5`). The hook tests exercise validators, comment sorting/gating, fixture-backed fetch/poll, and PR monitor behavior (`tests/test-pr-loop-hooks.sh:5`). The stop-hook tests exercise `hooks/pr-loop-stop-hook.sh` with controlled `gh`/`git` mocks (`tests/test-pr-loop-stophook.sh:5`).
- edge_cases_or_failure_modes: This file has no cleanup trap itself; cleanup behavior is delegated to `test-helpers.sh` and library setup. If a module leaves `CLAUDE_PROJECT_DIR` or `PATH` altered, later tests can be affected; the modules mostly unset `CLAUDE_PROJECT_DIR` in individual tests, but the runner does not enforce isolation. Since the runner sources modules rather than spawning them, function/variable name collisions across modules are possible. It also assumes the mock `gh`/`codex` implementations are sufficient for all called script branches; more complex fixture tests replace the `gh` mock locally (`tests/test-pr-loop-hooks.sh:1232`).
- validation_or_tests: The runner is itself the validation entrypoint. The delegated script tests cover CLI parsing and cancel-state rename (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`). Hook tests cover managed-file write protection, YAML list parsing, sorting, dedupe, bot re-add logic, trigger detection, approval markers, fixture-backed fetch/poll, and monitor display (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:288`, `tests/test-pr-loop-hooks.sh:353`, `tests/test-pr-loop-hooks.sh:390`, `tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1548`). Stop-hook tests cover old trigger rejection after force-push-like commit movement, empty `active_bots` approval, unpushed commit blocking, force-push detection, Codex `+1`, Claude `eyes` timeout, dynamic startup-case updates, and mixed bot approval (`tests/test-pr-loop-stophook.sh:21`, `tests/test-pr-loop-stophook.sh:247`, `tests/test-pr-loop-stophook.sh:453`, `tests/test-pr-loop-stophook.sh:539`, `tests/test-pr-loop-stophook.sh:897`, `tests/test-pr-loop-stophook.sh:1011`, `tests/test-pr-loop-stophook.sh:1146`, `tests/test-pr-loop-stophook.sh:1520`).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-096 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: This is a blocking message template for RLCR state-schema validation. It defines the user-facing contract when an active state file is missing a required field after a plugin upgrade.
- algorithmic_behavior: The template accepts `{{FIELD_NAME}}` and renders a concise block reason: the state file is missing a required field, the session likely started with an older Humanize version, the user can cancel, update to version `1.1.2+`, and restart, and the session will be terminated as `unexpected` to preserve state (`prompt-template/block/schema-outdated.md:1`, `prompt-template/block/schema-outdated.md:3`, `prompt-template/block/schema-outdated.md:5`, `prompt-template/block/schema-outdated.md:7`, `prompt-template/block/schema-outdated.md:12`). It is data/template logic, not executable code.
- inputs_outputs_state: Input is one variable, `FIELD_NAME`. Output is Markdown text embedded as the `reason` value in a hook JSON block response. It does not mutate state directly. The state transition implied by the text is fail-closed termination/blocking rather than continuing with a malformed or old state schema.
- gates_or_invariants: Its caller requires specific state fields to exist, currently `plan_tracked` and `start_branch`, mapped through field constants in `loop-common.sh` (`hooks/loop-plan-file-validator.sh:79`). Missing values trigger `schema_validation_error`, which renders this template and returns `{"decision":"block"}` (`hooks/loop-plan-file-validator.sh:59`, `hooks/loop-plan-file-validator.sh:65`, `hooks/loop-plan-file-validator.sh:71`). The invariant is that old state files are not silently interpreted because branch consistency and plan tracking gates depend on these fields (`hooks/loop-plan-file-validator.sh:5`).
- dependencies_and_callers: Called only by `hooks/loop-plan-file-validator.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md" ... "FIELD_NAME=$field_name"` (`hooks/loop-plan-file-validator.sh:65`). Rendering depends on `hooks/lib/template-loader.sh`, whose placeholder syntax is `{{VARIABLE_NAME}}` and single-pass replacement (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:54`). If the template is missing or empty, the hook uses a hardcoded fallback message (`hooks/loop-plan-file-validator.sh:62`, `hooks/lib/template-loader.sh:170`).
- edge_cases_or_failure_modes: The template text says `/humanize:cancel-rlcr-loop`, so it is RLCR-specific and would be misleading if reused for PR-loop state. It references version `1.1.2+`, which may become stale if schema requirements change. Missing `FIELD_NAME` would leave the placeholder unreplaced because the renderer preserves unknown placeholders by design (`hooks/lib/template-loader.sh:120`). This file is a skip candidate for PR-loop-specific runtime behavior, but not for the broader branch core algorithm because it enforces loop-state schema gating.
- validation_or_tests: Template reference tests scan `load_and_render_safe` calls and verify referenced templates exist (`tests/test-template-references.sh:83`, `tests/test-template-references.sh:172`). Template loader tests verify safe fallback and existing-template behavior (`tests/test-template-loader.sh:197`, `tests/test-template-loader.sh:211`). I found the direct caller by reference search and inspected the caller path.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-126 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: This is the Round 0 PR-loop task template used when the initial PR comment fetch finds no comments. It defines the no-review-yet branch of the PR-loop workflow.
- algorithmic_behavior: The template tells the agent that no review comments exist yet, monitored bots will review automatically, no manual trigger comment is needed, and the agent should write the initial summary to `@{{RESOLVE_PATH}}` before trying to exit (`prompt-template/pr-loop/round-0-task-no-comments.md:4`, `prompt-template/pr-loop/round-0-task-no-comments.md:6`, `prompt-template/pr-loop/round-0-task-no-comments.md:8`, `prompt-template/pr-loop/round-0-task-no-comments.md:12`). It also states the key invariants for this branch: do not comment to trigger review, do not modify `.humanize/pr-loop/` managed files, and rely on the Stop Hook for polling and Codex validation (`prompt-template/pr-loop/round-0-task-no-comments.md:18`, `prompt-template/pr-loop/round-0-task-no-comments.md:20`). The final note describes the algorithmic transition after exit: Stop Hook polls every 30 seconds for up to 15 minutes per bot, Codex validates reviews, issues produce feedback/continuation, and all approvals end the loop (`prompt-template/pr-loop/round-0-task-no-comments.md:26`).
- inputs_outputs_state: Inputs are rendered variables `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH` from `scripts/setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:731`). Output is appended into `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the comment bundle (`scripts/setup-pr-loop.sh:760`, `scripts/setup-pr-loop.sh:763`, `scripts/setup-pr-loop.sh:800`, `scripts/setup-pr-loop.sh:850`). The state context is `current_round: 0`, `startup_case: 1`, and an initial `round-0-pr-resolve.md` path that the agent is allowed to write.
- gates_or_invariants: The selection gate is the no-comment sentinel from `fetch-pr-comments.sh`: if `round-0-pr-comment.md` contains exactly `*No comments found.*`, setup chooses this template and sets `COMMENT_COUNT=0` (`scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:800`). This template pairs with Stop Hook trigger logic where Round 0 startup cases 1, 2, and 3 do not require a trigger, while stale-review cases 4/5 or later rounds do (`hooks/pr-loop-stop-hook.sh:612`, `hooks/pr-loop-stop-hook.sh:627`). It intentionally forbids @bot comments in the initial no-comment case to avoid duplicate/spam triggering.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-task-no-comments.md" "$FALLBACK_TASK" ...` (`scripts/setup-pr-loop.sh:800`). Rendering depends on `hooks/lib/template-loader.sh` single-pass substitution (`hooks/lib/template-loader.sh:54`). It is coordinated with sibling templates `round-0-header.md`, `critical-requirements-no-comments.md`, and the has-comments alternative (`scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:914`). Stop-hook behavior described by the template is implemented in `hooks/pr-loop-stop-hook.sh` polling and approval paths (`hooks/pr-loop-stop-hook.sh:801`, `hooks/pr-loop-stop-hook.sh:907`, `hooks/pr-loop-stop-hook.sh:1017`, `hooks/pr-loop-stop-hook.sh:1582`).
- edge_cases_or_failure_modes: If `fetch-pr-comments.sh` emits anything other than the exact sentinel, even only warnings or metadata with zero relevant bot comments, setup treats comments as present and uses the has-comments path (`scripts/setup-pr-loop.sh:723`). The template assumes automatic bot review for fresh PRs; repositories without such automatic bot configuration may later time out in the stop hook rather than during setup. It tells the agent not to comment, so if startup case was incorrectly classified as no-comments despite stale reviews/new commits, the loop may wait until Stop Hook detects/requires a trigger. The variable `ACTIVE_BOTS_DISPLAY` is display-only and does not itself enforce which bots are active.
- validation_or_tests: Script setup tests only indirectly verify setup argument parsing, not this template’s text. Stop-hook tests explicitly cover the Round 0 startup_case 1 no-trigger exception (`tests/test-pr-loop-stophook.sh:140`). Hook tests verify trigger priority/fallback and timeout anchoring around Round 0 behavior (`tests/test-pr-loop-hooks.sh:625`, `tests/test-pr-loop-hooks.sh:647`, `tests/test-pr-loop-hooks.sh:668`). Template reference tests verify that `load_and_render_safe` references point to real templates (`tests/test-template-references.sh:83`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 section headers above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`