# agent_28 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit gate for active RLCR sessions. It validates loop state schema, prevents branch switching, and enforces the original plan-file tracking contract before a user prompt is accepted. Registered in [hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/hooks.json:4) as the UserPromptSubmit command.
- algorithmic_behavior: Reads hook stdin but does not inspect prompt content; discovers the newest active loop via `find_active_loop "$PROJECT_ROOT/.humanize/rlcr"` in [loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-plan-file-validator.sh:27). If no active loop exists, it exits cleanly. It prefers `finalize-state.md` over `state.md` during Finalize Phase, then calls `parse_state_file_strict` and maps parsed `STATE_*` values into local `PLAN_TRACKED`, `PLAN_FILE`, and `START_BRANCH` fields.
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md}`, and git state for `HEAD`, tracking, and porcelain status. Outputs are either no output with exit 0 for allow, stderr plus exit 1 for malformed state parsing, or JSON `{"decision":"block","reason":...}` with exit 0 for user-facing hook blocks. It does not mutate state.
- gates_or_invariants: Requires strict frontmatter fields inherited from [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:255), including `current_round`, `max_iterations`, `review_started`, and `base_branch`; then explicitly checks `plan_tracked` and `start_branch` at [loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-plan-file-validator.sh:80). It blocks if current git branch differs from `start_branch`, if tracked plans become untracked or dirty, or if untracked/gitignored plans become tracked.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for constants, active-loop discovery, strict state parsing, and template loading; depends on `scripts/portable-timeout.sh` for `run_with_timeout`; depends on `jq` for JSON escaping and block output. The setup path writes the expected state fields in [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/setup-rlcr-loop.sh:709), and initial plan tracking rules are established earlier in [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/setup-rlcr-loop.sh:447).
- edge_cases_or_failure_modes: Fail-closed on malformed state; fail-closed on git branch lookup failure or timeout; distinguishes `git ls-files` exit 0 tracked, 1 untracked, 124 timeout, and other unexpected errors. Finalize Phase is supported by switching to `finalize-state.md` when present. A notable behavior is mixed block semantics: strict parse failure exits 1 with stderr rather than JSON, while most runtime violations return JSON and exit 0.
- validation_or_tests: Covered directly in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-plan-file-hooks.sh:119) for valid state, quoted plan file parsing, malformed/outdated state, branch switching, quoted `start_branch`, and hyphenated plan paths. Finalize-state parsing is checked in [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-finalize-phase.sh:853). Robustness coverage checks empty JSON and pass-through behavior in [test-hook-system-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/robustness/test-hook-system-robustness.sh:232).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR monitor runtime cleanup behavior, especially graceful shutdown when `.humanize/rlcr` disappears and terminal state restoration after stop or signal handling.
- algorithmic_behavior: Creates an isolated `/tmp/test-monitor-runtime-$$` workspace, synthesizes small scripts that source or mirror monitor functions, then asserts expected output markers. It does not drive a full RLCR session; it tests the monitor lifecycle primitives and source-code invariants around `_graceful_stop`, `_cleanup`, `_restore_terminal`, traps, and directory-deletion polling.
- inputs_outputs_state: Inputs are `scripts/humanize.sh` and generated temporary shell scripts under the test temp directory. Outputs are PASS/FAIL counters and process exit 0 only if all required checks pass. It creates and removes temporary `.humanize/rlcr` directories and test scripts, but only in `/tmp`.
- gates_or_invariants: `_cleanup` must be idempotent through `cleanup_done`; `_graceful_stop` must call `_cleanup`; `_restore_terminal` must reset the scroll region with `printf "\033[r"`; bash must install `trap '_cleanup' INT TERM`; zsh must define `TRAPINT` and `TRAPTERM`; cleanup must reset traps with `trap - INT TERM`. These correspond to implementation lines in [humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/humanize.sh:523) through [humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/humanize.sh:587).
- dependencies_and_callers: Depends on Bash, optionally zsh, standard shell utilities, and `scripts/humanize.sh`. The real monitor loop checks for deleted `.humanize/rlcr` in multiple polling loops and routes to `_graceful_stop` at [humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/humanize.sh:620), [humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/humanize.sh:662), and [humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/humanize.sh:755).
- edge_cases_or_failure_modes: The zsh SIGINT runtime case is skipped if zsh is unavailable. Some checks are textual rather than behavioral, so they can miss semantic regressions if names remain but behavior changes. One generated script contains a pipeline `grep -q ... | grep -q ...` that cannot actually prove `_cleanup` calls `_restore_terminal`, but the outer test later performs the intended direct source check with `grep -A30`.
- validation_or_tests: Test cases cover graceful-stop message content, restore call, cleanup idempotency, directory-deletion detection, terminal scroll reset source marker, `_cleanup -> _restore_terminal`, `_graceful_stop -> _cleanup`, bash SIGINT trap behavior, trap setup, trap reset, and optional zsh SIGINT handling. The file itself is the validation artifact; I did not execute it in this read-only research run.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template that prevents transition from Claude implementation work to Codex review while the latest TodoWrite state still has incomplete todos.
- algorithmic_behavior: Renders a concise blocking instruction with `{{INCOMPLETE_LIST}}`, telling the agent to complete remaining todos, mark them completed via TodoWrite, and only then write the summary and stop. Its last line explicitly blocks Codex review until all todos are finished in [incomplete-todos.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/prompt-template/block/incomplete-todos.md:12).
- inputs_outputs_state: Input variable is `INCOMPLETE_LIST`. The rendered output becomes the Stop-hook JSON `reason`, paired with system message `Loop: Blocked - incomplete todos detected, please finish all tasks first` in [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-codex-stop-hook.sh:381). It does not write files or mutate loop state.
- gates_or_invariants: This is a pre-review invariant: every todo in the latest TodoWrite snapshot must have `status == "completed"` before expensive Codex review starts. The detector reads the transcript path from hook input and returns exit 1 with incomplete entries when any todo is not completed in [check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/check-todos-from-transcript.py:118).
- dependencies_and_callers: Called only by `hooks/loop-codex-stop-hook.sh` during the Quick Check before git status and review prompt generation. Rendering uses `load_and_render_safe` from [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/template-loader.sh:170), so a fallback message is used if the template is missing.
- edge_cases_or_failure_modes: Empty stdin, missing transcript path, missing transcript file, or no TodoWrite calls allow progress because the checker exits 0. JSON parse errors exit 2 and produce a different parse-error block, not this template. Todo statuses other than exactly `completed` are treated as incomplete.
- validation_or_tests: No dedicated test file for this exact markdown template was found in the inspected references, but the call path is explicit in the stop hook and the checker’s exit-code contract is documented in [check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/check-todos-from-transcript.py:7).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-118 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular Codex review prompt template for non-full-alignment RLCR rounds. It defines the reviewer contract that determines whether the loop advances, blocks with review feedback, or may complete when Codex outputs `COMPLETE` as the final line.
- algorithmic_behavior: Forces Codex to read the original plan first, compare Claude’s claimed work against the plan and current prompt, inspect goal-tracker alignment, and write findings to `{{REVIEW_RESULT_FILE}}` unless everything is fully done. It treats deferred or pending work as incomplete and mandates a Goal Alignment Summary format.
- inputs_outputs_state: Inputs include `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, passed in [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-codex-stop-hook.sh:834). Output is the rendered `round-N-review-prompt.md`, then Codex’s subsequent review output/result controls loop behavior.
- gates_or_invariants: Completion is deliberately strict: only the literal final-line `COMPLETE` is allowed when all original plan tasks and acceptance criteria are done with no deferrals or pending work, as stated in [regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/prompt-template/codex/regular-review.md:53). Findings, mismatched claims, deferred tasks, unfinished tasks, forgotten items, or unjustified deferrals should be emitted as review comments instead.
- dependencies_and_callers: Rendered by the RLCR Stop hook when `FULL_ALIGNMENT_CHECK` is false; the alternate branch renders `codex/full-alignment-review.md`. Template variables are substituted single-pass by `render_template`, which intentionally avoids re-expanding placeholders inside values in [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/template-loader.sh:50).
- edge_cases_or_failure_modes: If Codex follows Claude’s deferrals, the template explicitly says that is wrong; deferred work must be forced back into scope. If placeholders are missing, they remain literal due to template-loader behavior, potentially weakening the review prompt. If the template file is missing, the fallback regular-review prompt is much shorter and loses the detailed deferral and goal-alignment contract.
- validation_or_tests: The rendering path is covered structurally by the stop hook’s review-prompt creation logic in [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-codex-stop-hook.sh:793). Downstream review phase behavior is implemented around `review_started`, `.review-phase-started`, and repeated code review in [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-codex-stop-hook.sh:1440).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 Item Evidence headings present; each assigned item is represented once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`