# agent_02 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`
- inspection_note: read-only research only; no files modified. A `git status --short` check could not complete because this branch export has no usable `.git` metadata in the sandboxed read-only environment.

## Item Evidence

### IMPROVE_PR_LOOP-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Recursive inventory found one file: `agents/draft-relevance-checker.md`. It defines a Claude/Task subagent prompt, with frontmatter name/model/tools at `agents/draft-relevance-checker.md:1-6`, and a verdict contract at `agents/draft-relevance-checker.md:27-29`. Its role is semantic draft relevance gating for `gen-plan`, not direct PR-loop execution.
- algorithmic_behavior: The agent is instructed to inspect repository docs/structure, compare a supplied draft against repo concepts/files/features, and return exactly either `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` (`agents/draft-relevance-checker.md:14-29`). `commands/gen-plan.md:50-72` uses that verdict as a branch: `NOT_RELEVANT` stops plan generation, while `RELEVANT` advances to deeper draft analysis.
- inputs_outputs_state: Inputs are draft content supplied through the Task tool plus repository context accessed via `Read`, `Glob`, and `Grep`. Output is a single textual verdict prefix plus explanation. It does not mutate repo state itself; it changes command flow state by allowing or blocking `gen-plan` Phase 3.
- gates_or_invariants: The gate is intentionally lenient: informal drafts and any reasonable connection should pass, and uncertainty should lean relevant (`agents/draft-relevance-checker.md:31-36`). The only machine-facing invariant is the verdict prefix.
- dependencies_and_callers: Called by `commands/gen-plan.md:56-64` through `humanize:draft-relevance-checker`. Validated structurally by `tests/test-gen-plan.sh:111-153` for file existence, name, model, and tools. No PR-loop setup, stop-hook, cancel, polling, or review-routing caller was found.
- edge_cases_or_failure_modes: Model judgment is nondeterministic and semantic rather than rule-based; malformed output prefixes create ambiguity for the caller; the lenient policy can admit false positives. The tool list allows repository reads/searches but no writes, so behavioral risk is mostly incorrect gating, not direct mutation.
- validation_or_tests: `tests/test-gen-plan.sh:111-153` checks that the agent file exists, has `name: draft-relevance-checker`, uses `model: haiku`, and declares tools. The tests do not validate semantic relevance decisions.
- skip_candidate: `yes: important to gen-plan relevance gating, but not part of the improve-pr-loop runtime workflow, cancellation path, PR review polling, or PR-loop prompt transition logic.`

### IMPROVE_PR_LOOP-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for active PR loops. It is the command-backed state transition from active `state.md` to cancelled `cancel-state.md`, scoped specifically to `.humanize/pr-loop/` (`scripts/cancel-pr-loop.sh:3-6`, `scripts/cancel-pr-loop.sh:54-56`).
- algorithmic_behavior: Parses `--force` and help (`scripts/cancel-pr-loop.sh:24-66`), resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, locates the lexicographically newest loop directory under `.humanize/pr-loop`, requires `state.md`, extracts round/max/pr metadata, touches `.cancel-requested`, then renames `state.md` to `cancel-state.md` (`scripts/cancel-pr-loop.sh:72-122`). It emits a sentinel first line: `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED` (`scripts/cancel-pr-loop.sh:78-97`, `scripts/cancel-pr-loop.sh:128-130`).
- inputs_outputs_state: Inputs are shell args, `CLAUDE_PROJECT_DIR`, `.humanize/pr-loop/*/state.md`, and optional frontmatter keys `current_round`, `max_iterations`, `pr_number` (`scripts/cancel-pr-loop.sh:104-112`). Outputs are stdout status lines and exit codes. State mutation is creation of `.cancel-requested` and rename of `state.md` to `cancel-state.md`.
- gates_or_invariants: Only a loop directory with active `state.md` is cancellable (`scripts/cancel-pr-loop.sh:88-98`). PR-loop cancellation is intentionally separate from RLCR cancellation, per help text and command docs. The slash command contract says the script owns all cancellation logic and a PR loop is active if `state.md` exists in the newest PR-loop directory (`commands/cancel-pr-loop.md:17-25`).
- dependencies_and_callers: Called by `/humanize:cancel-pr-loop` via `commands/cancel-pr-loop.md:1-15`. The stop hook observes `.cancel-requested` while polling and exits the poll loop when present (`hooks/pr-loop-stop-hook.sh:951-955`). It depends on standard shell tools: `ls`, `sort`, `head`, `grep`, `sed`, `tr`, `touch`, and `mv`.
- edge_cases_or_failure_modes: `--force` is parsed but currently has no additional effect (`scripts/cancel-pr-loop.sh:28-30`, `scripts/cancel-pr-loop.sh:40`). If the newest PR-loop directory has no `state.md`, older active loops are ignored. Missing metadata prints `?` rather than failing (`scripts/cancel-pr-loop.sh:109-112`). There is no explicit lock, so concurrent state changes between check, `touch`, and `mv` can fail under `set -e`. Documented exit code `3` covers unknown options, but runtime `touch`/`mv` failures propagate their native shell command status rather than being normalized.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:181-252` covers help output, no-loop sentinel/exit behavior, and active-loop cancellation including the `state.md` to `cancel-state.md` rename. Broader cancellation command safety is supported by hook-side authorization logic in `hooks/lib/loop-common.sh:533-710`, especially the `.cancel-requested` signal invariant.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-062 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared executable test harness for PR-loop tests. It centralizes environment setup, mocks external services, and summarizes test results for the PR-loop test runner (`tests/test-pr-loop-lib.sh:1-8`).
- algorithmic_behavior: A load guard prevents repeated initialization (`tests/test-pr-loop-lib.sh:10-13`). It sources `test-helpers.sh` only if `setup_test_dir` is unavailable (`tests/test-pr-loop-lib.sh:18-21`). `create_mock_gh` installs a deterministic `gh` fixture handling auth, repo metadata, PR metadata/state/commits, and generic API calls (`tests/test-pr-loop-lib.sh:27-88`). `create_mock_codex` installs a successful `codex` shim (`tests/test-pr-loop-lib.sh:90-101`). `init_pr_loop_test_env` creates a temp mock bin dir, prepends it to `PATH`, and exports `MOCK_BIN_DIR` (`tests/test-pr-loop-lib.sh:107-121`).
- inputs_outputs_state: Inputs are sourced shell context, optional pre-set `SCRIPT_DIR`/`PROJECT_ROOT`, and helper functions from `tests/test-helpers.sh`. Outputs are shell functions and exported environment. State changes during tests include creating temp directories, writing executable mock binaries, and mutating process `PATH`.
- gates_or_invariants: Mock `gh` returns success only for recognized command shapes and exits `1` for unhandled commands (`tests/test-pr-loop-lib.sh:36-86`). `print_test_summary` returns failure if `TESTS_FAILED` is greater than zero (`tests/test-pr-loop-lib.sh:127-144`). The one-time load guard makes function definitions stable across multiple sources.
- dependencies_and_callers: Main caller is `tests/test-pr-loop.sh`, which sources `tests/test-helpers.sh` and this library, calls `init_pr_loop_test_env`, then sources/runs script, hook, and stop-hook test modules (`tests/test-pr-loop.sh:18-47`). It depends on helper counters/functions from `tests/test-helpers.sh:22-44` and `setup_test_dir` from `tests/test-helpers.sh:84-89`.
- edge_cases_or_failure_modes: The mock `gh` is argument-order sensitive, so real code using equivalent but differently ordered flags may fail tests. The codex mock always succeeds, which is useful for routing tests but cannot catch Codex failure handling. This file defines its own `print_test_summary`, shadowing the helper version and omitting skipped-count reporting (`tests/test-pr-loop-lib.sh:127-144`; compare `tests/test-helpers.sh:58-78`).
- validation_or_tests: It is itself test infrastructure. Its behavior is exercised indirectly by `tests/test-pr-loop.sh:26-47` and all sourced PR-loop test modules that depend on the mock `gh`/`codex` commands.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-092 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Blocking prompt template for plan-file integrity violations during an active RLCR session. The template explains that the plan file changed, forbids plan edits during the active session, gives cancel/update/restart steps, and exposes the backup path (`prompt-template/block/plan-file-modified.md:1-12`).
- algorithmic_behavior: `hooks/loop-codex-stop-hook.sh` skips plan integrity checks once review has started (`hooks/loop-codex-stop-hook.sh:263-265`), otherwise compares the live plan with the backup at `$LOOP_DIR/plan.md`. On content mismatch, it renders this template through `load_and_render_safe` with `PLAN_FILE` and `BACKUP_PATH`, then emits a JSON block decision (`hooks/loop-codex-stop-hook.sh:315-334`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}` and `{{BACKUP_PATH}}`. Output is block-reason Markdown embedded in the stop-hook JSON response. It does not directly mutate files; it halts the loop’s exit/progression path until the user cancels and restarts with a consistent plan.
- gates_or_invariants: The invariant is immutable plan content during the implementation phase. The template’s required recovery path is cancel current session, update plan, start a new session (`prompt-template/block/plan-file-modified.md:7-10`). Template rendering is single-pass, and missing variables remain as placeholders (`hooks/lib/template-loader.sh:50-58`). Missing/empty templates fall back to caller-provided text (`hooks/lib/template-loader.sh:170-186`).
- dependencies_and_callers: Depends on the shared template loader and `loop-codex-stop-hook`. It is referenced only in the RLCR/Codex stop-hook path found at `hooks/loop-codex-stop-hook.sh:329-331`; related plan-file validation also appears in `hooks/loop-plan-file-validator.sh`, but this exact template is rendered by the stop hook.
- edge_cases_or_failure_modes: If `PLAN_FILE` or `BACKUP_PATH` is not supplied, unresolved placeholders remain in user-facing output. Deleted plan files and missing backups are handled by separate block messages before this template path (`hooks/loop-codex-stop-hook.sh:270-294`). The wording says “session” while the commands are RLCR-specific, which is clear operationally but not PR-loop-specific.
- validation_or_tests: Runtime validation is the stop-hook diff gate at `hooks/loop-codex-stop-hook.sh:315-334`. Template loading/fallback behavior is covered by template-loader tests referenced around `tests/test-template-loader.sh:197-221` and template reference checks around `tests/test-template-references.sh:69-94`.
- skip_candidate: `yes: this is an RLCR plan-integrity block, not a PR-loop workflow component, though it is still an algorithmic loop gate in the broader plugin.`

### IMPROVE_PR_LOOP-HZ-122 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 critical instruction block for the “no review comments yet” startup case. It tells the agent/user to write the initial resolution summary, try to exit, and not comment on the PR to trigger review (`prompt-template/pr-loop/critical-requirements-no-comments.md:2-20`).
- algorithmic_behavior: `scripts/setup-pr-loop.sh` sets `RESOLVE_PATH` to `round-0-pr-resolve.md` (`scripts/setup-pr-loop.sh:721`), detects no comments by the exact sentinel `*No comments found.*` in the fetched comment file (`scripts/setup-pr-loop.sh:723-729`), builds the Round 0 prompt using no-comments task content (`scripts/setup-pr-loop.sh:766-800`), then renders this critical block when `COMMENT_COUNT` is zero (`scripts/setup-pr-loop.sh:890-915`). It is printed after the generated initial prompt (`scripts/setup-pr-loop.sh:887-942`).
- inputs_outputs_state: Input placeholder is `{{RESOLVE_PATH}}`, plus the caller’s no-comments classification. Output is user-facing critical prompt text. The intended state transition is from PR-loop setup into “Round 0 awaiting initial bot reviews”: the user writes the resolve summary, exits, and the Stop Hook polls for bot reviews.
- gates_or_invariants: The key invariant is “do not comment on the PR to trigger review” because first reviews are expected to happen automatically (`prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`). The required completion evidence is a summary at `RESOLVE_PATH` noting Round 0 awaiting initial bot reviews and no issues to address (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-14`).
- dependencies_and_callers: Depends on `scripts/setup-pr-loop.sh`, the fetched comment file format, and `hooks/lib/template-loader.sh` safe rendering. It coordinates with the PR stop hook, which later polls bot responses and can observe cancellation via `.cancel-requested` (`hooks/pr-loop-stop-hook.sh:951-961`).
- edge_cases_or_failure_modes: The no-comments branch depends on an exact sentinel line; any fetch error or changed empty-output wording can route to the has-comments path. If `RESOLVE_PATH` is missing from template variables, the placeholder remains unresolved due to single-pass loader behavior. If the PR platform or bots do not auto-review new PRs, the prompt’s “do not comment” instruction can lead to waiting until polling timeout.
- validation_or_tests: The rendering path is directly present in `scripts/setup-pr-loop.sh:890-915`. General template reference and safe-render coverage exists via template tests, but no isolated behavioral test was found that asserts this exact no-comments critical text.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 unique assigned item sections above, one per row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`