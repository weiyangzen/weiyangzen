# agent_24 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude plugin hook registry for RLCR and PR-loop enforcement. It wires `UserPromptSubmit` to plan/state validation, `PreToolUse` to path and command validators, and `Stop` to both loop stop hooks: `hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`.
- algorithmic_behavior: The file is the dispatch table that makes the loop state machine enforceable at runtime. Prompt submission runs `loop-plan-file-validator.sh`; tool use routes `Write`, `Edit`, `Read`, and `Bash` through dedicated validators; stop attempts invoke `loop-codex-stop-hook.sh` and `pr-loop-stop-hook.sh` with 7200-second hook timeouts: `hooks/hooks.json:9`, `hooks/hooks.json:20`, `hooks/hooks.json:29`, `hooks/hooks.json:38`, `hooks/hooks.json:47`, `hooks/hooks.json:57`, `hooks/hooks.json:62`.
- inputs_outputs_state: Inputs are Claude hook events on stdin plus plugin/project env such as `CLAUDE_PLUGIN_ROOT` and `CLAUDE_PROJECT_DIR`. Outputs are validator exit status or JSON hook decisions. State lives under `.humanize/rlcr/<timestamp>/` and `.humanize/pr-loop/<timestamp>/`, with RLCR stop-hook state described in `hooks/loop-codex-stop-hook.sh:8` and PR-loop state described in `hooks/pr-loop-stop-hook.sh:14`.
- gates_or_invariants: Prompt-submit gate fails closed on malformed state, branch changes, and plan tracking drift: `hooks/loop-plan-file-validator.sh:44`, `hooks/loop-plan-file-validator.sh:95`, `hooks/loop-plan-file-validator.sh:123`. Tool gates block state-file edits, prompt/todo bypasses, wrong-round summaries, goal-tracker modification after round 0, `.humanize` force-adds, and PR-loop protected files: `hooks/loop-write-validator.sh:55`, `hooks/loop-write-validator.sh:76`, `hooks/loop-write-validator.sh:140`, `hooks/loop-bash-validator.sh:96`, `hooks/loop-bash-validator.sh:115`, `hooks/loop-bash-validator.sh:139`, `hooks/loop-bash-validator.sh:336`, `hooks/loop-bash-validator.sh:403`.
- dependencies_and_callers: Depends on every script referenced in the hook JSON plus shared `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `scripts/portable-timeout.sh`. Tests reference this hook surface through validator scripts in `tests/test-plan-file-hooks.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, and robustness suites.
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is absent or wrong, hook commands cannot resolve. If no active loop exists, individual hooks generally exit allow: `hooks/loop-codex-stop-hook.sh:59`, `hooks/pr-loop-stop-hook.sh:64`. Stop hooks run together, so each must self-filter by active loop type. Exact tool matchers mean unlisted tools are not protected by this registry. Long stop-hook timeout allows review/polling but can hold a Stop event for up to 7200 seconds.
- validation_or_tests: Coverage is broad but indirect through shell tests. Plan-file hook behavior is directly exercised in `tests/test-plan-file-hooks.sh`; PR stop hook in `tests/test-pr-loop-stophook.sh` and `tests/test-pr-loop-hooks.sh`; hook input/state robustness in `tests/robustness/test-hook-system-robustness.sh` and `tests/robustness/test-hook-input-robustness.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:gen-plan` command contract. It validates command metadata, relevance-agent metadata, version consistency, and the IO validator’s exit-code protocol: `tests/test-gen-plan.sh:3`, `tests/test-gen-plan.sh:47`, `tests/test-gen-plan.sh:182`, `tests/test-gen-plan.sh:426`.
- algorithmic_behavior: Positive tests check `commands/gen-plan.md` exists and has frontmatter fields, `agents/draft-relevance-checker.md` exists with `name: draft-relevance-checker`, `model: haiku`, and tools, then verifies version agreement across plugin metadata and README: `tests/test-gen-plan.sh:59`, `tests/test-gen-plan.sh:72`, `tests/test-gen-plan.sh:86`, `tests/test-gen-plan.sh:111`, `tests/test-gen-plan.sh:124`, `tests/test-gen-plan.sh:138`, `tests/test-gen-plan.sh:168`.
- inputs_outputs_state: Inputs are repository files under `commands/`, `agents/`, `.claude-plugin/`, `README.md`, and `scripts/validate-gen-plan-io.sh`. The test creates temporary invalid fixtures under `mktemp -d`, tracks `TESTS_PASSED` and `TESTS_FAILED`, prints colored PASS/FAIL lines, and exits `0` only when failures are zero: `tests/test-gen-plan.sh:22`, `tests/test-gen-plan.sh:192`, `tests/test-gen-plan.sh:554`.
- gates_or_invariants: Name format must match `^[a-z][a-z0-9-]*$`; command and agent frontmatter must contain required fields; model aliases must be exact for `opus|sonnet|haiku` or use accepted provider prefixes; command/agent content should not contain Emoji/CJK; validator script must use stable exit codes for bad args and path failures: `tests/test-gen-plan.sh:196`, `tests/test-gen-plan.sh:201`, `tests/test-gen-plan.sh:209`, `tests/test-gen-plan.sh:350`, `tests/test-gen-plan.sh:409`, `tests/test-gen-plan.sh:438`.
- dependencies_and_callers: Mirrors the gen-plan command workflow in `commands/gen-plan.md`, where IO validation runs first and exit codes `0..7` determine whether the command continues or stops: `commands/gen-plan.md:30`, `commands/gen-plan.md:38`. The validator implements those codes and writes the plan template plus original draft on success: `scripts/validate-gen-plan-io.sh:4`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:95`, `scripts/validate-gen-plan-io.sh:103`, `scripts/validate-gen-plan-io.sh:148`. The relevance agent contract is in `agents/draft-relevance-checker.md:1`.
- edge_cases_or_failure_modes: The YAML syntax checker is deliberately shallow and line-pattern based, not a full YAML parser: `tests/test-gen-plan.sh:305`. `grep -P` unsupported on a platform can make the Emoji/CJK check pass by falling into the `else` branch: `tests/test-gen-plan.sh:410`. The second `trap` for `SCRIPT_TEST_DIR` overwrites the earlier fixture cleanup trap, so the first temp directory can be left behind if the script exits after line `436`. Exit codes `5` and `7` from the validator are documented but not exercised here.
- validation_or_tests: This file is itself the validator. It is included by `tests/run-all-tests.sh` as `test-gen-plan.sh`, and its final gate is `TESTS_FAILED == 0`: `tests/test-gen-plan.sh:554`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Fail-closed prompt block for RLCR stop-hook git status failures. It tells Claude/user that repository state cannot be verified after `git status` fails or times out: `prompt-template/block/git-status-failed.md:1`, `prompt-template/block/git-status-failed.md:3`.
- algorithmic_behavior: `loop-codex-stop-hook.sh` caches `git status --porcelain` once, captures the exit code, and blocks the stop attempt if the command fails. On failure it renders this template with `GIT_STATUS_EXIT` and returns a JSON hook block decision: `hooks/loop-codex-stop-hook.sh:396`, `hooks/loop-codex-stop-hook.sh:406`, `hooks/loop-codex-stop-hook.sh:410`, `hooks/loop-codex-stop-hook.sh:412`, `hooks/loop-codex-stop-hook.sh:419`, `hooks/loop-codex-stop-hook.sh:421`.
- inputs_outputs_state: Input variable is `{{GIT_STATUS_EXIT}}`. Output is Markdown reason text embedded in a hook JSON object. It does not mutate state itself, but it prevents the RLCR state machine from proceeding when repository cleanliness cannot be checked.
- gates_or_invariants: The invariant is “cannot verify repository state, so do not proceed.” The stop-hook comment explicitly calls this fail-closed behavior to prevent bypassing large-file and git-clean checks: `hooks/loop-codex-stop-hook.sh:399`, `hooks/loop-codex-stop-hook.sh:401`.
- dependencies_and_callers: Rendered through `load_and_render_safe`, which uses single-pass placeholder substitution and falls back if the template is missing or renders empty: `hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:167`, `hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:190`. PR loop has a separate inline git-status-failed message rather than this template: `hooks/pr-loop-stop-hook.sh:282`, `hooks/pr-loop-stop-hook.sh:286`.
- edge_cases_or_failure_modes: Missing `GIT_STATUS_EXIT` would leave the placeholder unresolved because missing template variables are preserved: `hooks/lib/template-loader.sh:120`. Missing template falls back to a shorter built-in message: `hooks/loop-codex-stop-hook.sh:414`. Slow or locked git operations are expected cases called out in the template: `prompt-template/block/git-status-failed.md:5`.
- validation_or_tests: Covered by stop-hook tests that exercise git status failure paths and by template-reference tests. Direct search shows the template is referenced from `hooks/loop-codex-stop-hook.sh` and `tests/test-template-references.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-114 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing review-phase repair prompt for RLCR. It defines the contract after Codex review finds issues: fix all `[P0-9]` findings, avoid unrelated work, commit fixes, and write the round summary: `prompt-template/claude/review-phase-prompt.md:1`, `prompt-template/claude/review-phase-prompt.md:11`, `prompt-template/claude/review-phase-prompt.md:13`, `prompt-template/claude/review-phase-prompt.md:14`.
- algorithmic_behavior: The RLCR stop hook enters review phase when implementation output ends with exact `COMPLETE`, sets `review_started: true`, creates `.review-phase-started`, and runs `codex review`. If review issues are detected, it renders this template into the next round prompt using `REVIEW_CONTENT` and `SUMMARY_FILE`: `hooks/loop-codex-stop-hook.sh:1394`, `hooks/loop-codex-stop-hook.sh:1421`, `hooks/loop-codex-stop-hook.sh:1428`, `hooks/loop-codex-stop-hook.sh:1433`, `hooks/loop-codex-stop-hook.sh:1148`.
- inputs_outputs_state: Inputs are `{{REVIEW_CONTENT}}` and `{{SUMMARY_FILE}}`: `prompt-template/claude/review-phase-prompt.md:7`, `prompt-template/claude/review-phase-prompt.md:14`. Output is `round-N-prompt.md` plus a JSON block reason/system message from the stop hook. State transitions include current round advancement and review-loop continuation until review passes.
- gates_or_invariants: `COMPLETE` has no effect during review phase; only absence of `[P0-9]` review issues exits toward finalize: `prompt-template/claude/review-phase-prompt.md:25`, `prompt-template/claude/review-phase-prompt.md:28`, `hooks/loop-codex-stop-hook.sh:1396`, `hooks/loop-codex-stop-hook.sh:1443`. Codex review command failure or empty output is blocking, not success: `hooks/loop-codex-stop-hook.sh:1006`, `hooks/loop-codex-stop-hook.sh:1017`.
- dependencies_and_callers: Called by `loop-codex-stop-hook.sh` via `load_and_render_safe`; review itself is run with `codex review --base`, preferring the captured base commit over branch when available: `hooks/loop-codex-stop-hook.sh:921`, `hooks/loop-codex-stop-hook.sh:977`, `hooks/loop-codex-stop-hook.sh:999`.
- edge_cases_or_failure_modes: If `review_started=true` is manually toggled without the marker file, the hook blocks as inconsistent state: `hooks/loop-codex-stop-hook.sh:1446`, `hooks/loop-codex-stop-hook.sh:1449`. If no base branch is configured, review phase is skipped with a recorded reason: `hooks/loop-codex-stop-hook.sh:1413`. If the template is missing, the stop hook uses an embedded fallback prompt: `hooks/loop-codex-stop-hook.sh:1133`.
- validation_or_tests: Review/finalize behavior is exercised in `tests/test-finalize-phase.sh`, including review failure and empty output paths. Template presence is also checked by `tests/test-template-references.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-144 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the shared timeout abstraction used by git, gh, Codex, setup scripts, stop hooks, and validators. It verifies cross-platform timeout behavior before loop gates rely on it: `tests/robustness/test-timeout-robustness.sh:3`, `tests/robustness/test-timeout-robustness.sh:16`.
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh`, confirms a valid `TIMEOUT_IMPL`, and exercises `run_with_timeout` over successful commands, timed-out commands, commands with args, pipeline/subshell commands, non-existent commands, signal handling, rapid cycles, and exported timeout metadata: `tests/robustness/test-timeout-robustness.sh:31`, `tests/robustness/test-timeout-robustness.sh:51`, `tests/robustness/test-timeout-robustness.sh:62`, `tests/robustness/test-timeout-robustness.sh:79`, `tests/robustness/test-timeout-robustness.sh:102`, `tests/robustness/test-timeout-robustness.sh:170`, `tests/robustness/test-timeout-robustness.sh:221`, `tests/robustness/test-timeout-robustness.sh:259`.
- inputs_outputs_state: Inputs are local shell commands and sourced helpers. Outputs are PASS/FAIL counters from `tests/test-helpers.sh`, with final process exit from `print_test_summary`: `tests/test-helpers.sh:30`, `tests/test-helpers.sh:58`, `tests/robustness/test-timeout-robustness.sh:278`.
- gates_or_invariants: Valid timeout implementations are exactly `gtimeout`, `timeout`, `python3`, `python`, or `none`: `tests/robustness/test-timeout-robustness.sh:42`. When a timeout implementation exists, timed-out commands should return `124`: `tests/robustness/test-timeout-robustness.sh:65`, `tests/robustness/test-timeout-robustness.sh:70`. Successful commands should preserve output and exit code: `tests/robustness/test-timeout-robustness.sh:54`, `tests/robustness/test-timeout-robustness.sh:93`.
- dependencies_and_callers: The implementation chooses `gtimeout`, GNU `timeout`, Python, or no timeout in priority order: `scripts/portable-timeout.sh:6`, `scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:29`. `run_with_timeout` dispatches to the chosen backend and Python maps `TimeoutExpired` to `124`: `scripts/portable-timeout.sh:33`, `scripts/portable-timeout.sh:38`, `scripts/portable-timeout.sh:47`, `scripts/portable-timeout.sh:56`. The helper is used broadly by stop hooks, plan validator, PR setup, RLCR setup, and review scripts.
- edge_cases_or_failure_modes: If no timeout backend exists, timeout-specific cases are skipped or commands run without enforcement: `tests/robustness/test-timeout-robustness.sh:75`, `scripts/portable-timeout.sh:64`. Zero timeout accepts either immediate success or timeout: `tests/robustness/test-timeout-robustness.sh:139`. Signal handling accepts `0` or `124`: `tests/robustness/test-timeout-robustness.sh:221`. Empty command is marked handled regardless of exit code: `tests/robustness/test-timeout-robustness.sh:201`.
- validation_or_tests: This file is itself the robustness test and is paired with `scripts/portable-timeout.sh`. I inspected it only; I did not execute tests because this run requested research notes only and no modification.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique assigned item sections were included above, one per requested row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`