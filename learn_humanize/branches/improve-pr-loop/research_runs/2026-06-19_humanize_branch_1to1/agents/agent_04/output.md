# agent_04 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: The `hooks` directory is the Claude Code hook enforcement and stop-hook orchestration layer for both RLCR and PR review loops. It registers validators and stop hooks through `hooks/hooks.json`: `UserPromptSubmit` runs `loop-plan-file-validator.sh`, `PreToolUse` routes `Write`, `Edit`, `Read`, and `Bash` through their matching validators, and `Stop` runs both `loop-codex-stop-hook.sh` and `pr-loop-stop-hook.sh` with long timeouts (`hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`).
- algorithmic_behavior: The directory splits behavior into three families. First, input/tool validators protect managed loop files before Claude can read/write/edit/bash them. `loop-write-validator.sh` blocks todos, prompt files, PR-loop `state.md`, PR-loop generated review artifacts, wrong PR resolve rounds, RLCR state/finalize state, plan backups, and goal tracker edits after round 0 (`hooks/loop-write-validator.sh:55`, `hooks/loop-write-validator.sh:76`, `hooks/loop-write-validator.sh:89`, `hooks/loop-write-validator.sh:140`, `hooks/loop-write-validator.sh:152`, `hooks/loop-write-validator.sh:179`, `hooks/loop-write-validator.sh:192`). `loop-edit-validator.sh` mirrors edit protection for the same surfaces (`hooks/loop-edit-validator.sh:38`, `hooks/loop-edit-validator.sh:59`, `hooks/loop-edit-validator.sh:117`, `hooks/loop-edit-validator.sh:145`). `loop-read-validator.sh` blocks stale/wrong RLCR round reads and todos while allowing PR-loop reads for comments and feedback (`hooks/loop-read-validator.sh:55`, `hooks/loop-read-validator.sh:69`, `hooks/loop-read-validator.sh:122`, `hooks/loop-read-validator.sh:138`). `loop-bash-validator.sh` closes bypass paths via command-pattern detection for redirects, `tee`, `sed -i`, `mv`, `cp`, `rm`, `git add`, shell wrappers, and relative paths (`hooks/loop-bash-validator.sh:115`, `hooks/loop-bash-validator.sh:139`, `hooks/loop-bash-validator.sh:169`, `hooks/loop-bash-validator.sh:306`, `hooks/loop-bash-validator.sh:336`, `hooks/loop-bash-validator.sh:349`, `hooks/loop-bash-validator.sh:403`).
- algorithmic_behavior: Second, shared libraries supply the reusable algorithmic primitives. `loop-common.sh` validates hook JSON, rejects null bytes/invalid UTF-8/deep JSON, finds the newest active RLCR/PR loop, parses state frontmatter, maps PR bot names, validates PR resolve round numbers, detects broad `.humanize` git-add operations, authorizes cancel-state moves, detects Codex review issue markers, and updates PR goal trackers (`hooks/lib/loop-common.sh:59`, `hooks/lib/loop-common.sh:119`, `hooks/lib/loop-common.sh:156`, `hooks/lib/loop-common.sh:209`, `hooks/lib/loop-common.sh:349`, `hooks/lib/loop-common.sh:551`, `hooks/lib/loop-common.sh:727`, `hooks/lib/loop-common.sh:798`, `hooks/lib/loop-common.sh:843`, `hooks/lib/loop-common.sh:930`, `hooks/lib/loop-common.sh:1168`). `template-loader.sh` locates `prompt-template`, performs single-pass `{{VAR}}` rendering to prevent placeholder injection, and falls back safely when templates are missing (`hooks/lib/template-loader.sh:26`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:170`).
- algorithmic_behavior: Third, stop hooks run the loop state machines. `loop-codex-stop-hook.sh` is the RLCR loop engine: it validates state schema, branch/plan invariants, todo completion, git cleanliness, summary presence, goal tracker initialization, max iteration limits, Codex review prompts, review-phase transitions, finalize phase, `COMPLETE`/`STOP` markers, and code-review severity detection (`hooks/loop-codex-stop-hook.sh:59`, `hooks/loop-codex-stop-hook.sh:88`, `hooks/loop-codex-stop-hook.sh:163`, `hooks/loop-codex-stop-hook.sh:227`, `hooks/loop-codex-stop-hook.sh:263`, `hooks/loop-codex-stop-hook.sh:345`, `hooks/loop-codex-stop-hook.sh:406`, `hooks/loop-codex-stop-hook.sh:616`, `hooks/loop-codex-stop-hook.sh:742`, `hooks/loop-codex-stop-hook.sh:816`, `hooks/loop-codex-stop-hook.sh:1272`, `hooks/loop-codex-stop-hook.sh:1395`, `hooks/loop-codex-stop-hook.sh:1446`, `hooks/loop-codex-stop-hook.sh:1472`). `pr-loop-stop-hook.sh` is the PR loop engine: it parses PR state, resolves fork base repos, checks PR open/closed/merged state, requires a resolution file, enforces clean and pushed git state, detects force pushes and stale triggers, requires `@bot` triggers by startup case, validates Claude `eyes`, polls bot reviews, treats Codex `+1` as approval, runs local Codex validation, updates `active_bots`, updates the goal tracker, and blocks with next-round feedback when issues remain (`hooks/pr-loop-stop-hook.sh:87`, `hooks/pr-loop-stop-hook.sh:204`, `hooks/pr-loop-stop-hook.sh:243`, `hooks/pr-loop-stop-hook.sh:261`, `hooks/pr-loop-stop-hook.sh:282`, `hooks/pr-loop-stop-hook.sh:369`, `hooks/pr-loop-stop-hook.sh:465`, `hooks/pr-loop-stop-hook.sh:619`, `hooks/pr-loop-stop-hook.sh:661`, `hooks/pr-loop-stop-hook.sh:727`, `hooks/pr-loop-stop-hook.sh:767`, `hooks/pr-loop-stop-hook.sh:907`, `hooks/pr-loop-stop-hook.sh:1186`, `hooks/pr-loop-stop-hook.sh:1263`, `hooks/pr-loop-stop-hook.sh:1368`, `hooks/pr-loop-stop-hook.sh:1437`, `hooks/pr-loop-stop-hook.sh:1558`, `hooks/pr-loop-stop-hook.sh:1597`).
- inputs_outputs_state: Inputs are Claude hook JSON on stdin, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/<timestamp>` state, `.humanize/pr-loop/<timestamp>` state, transcript JSONL paths, git status/branch/commit data, GitHub CLI API responses, local Codex CLI output, and prompt templates. Outputs are either hook allow exits, hook block JSON with `decision`, `reason`, and sometimes `systemMessage`, stderr diagnostics, generated loop artifacts, renamed state files such as `approve-state.md`, `complete-state.md`, `maxiter-state.md`, `closed-state.md`, `merged-state.md`, `usage-limit-state.md`, prompt/check/feedback files, and modified loop state frontmatter. State transitions include `state.md` to terminal state files in both loops (`hooks/lib/loop-common.sh:1119`, `hooks/pr-loop-stop-hook.sh:245`, `hooks/pr-loop-stop-hook.sh:251`, `hooks/pr-loop-stop-hook.sh:443`, `hooks/pr-loop-stop-hook.sh:690`, `hooks/pr-loop-stop-hook.sh:1405`, `hooks/loop-codex-stop-hook.sh:1032`, `hooks/loop-codex-stop-hook.sh:1395`, `hooks/loop-codex-stop-hook.sh:1505`).
- gates_or_invariants: Key invariants are fail-closed parsing, correct active loop directory only, managed state files not user-modifiable, PR `round-N-pr-resolve.md` round must match `current_round`, RLCR summaries must live in active loop directory, `.humanize` must not be git-added, branch must not change during RLCR, PR loop must be clean and pushed before remote bot polling, force-push/new-commit triggers invalidate stale `last_trigger_at`, Claude `eyes` is blocking when required, and review/finalize phases are gated by markers and expected files (`hooks/lib/loop-common.sh:79`, `hooks/lib/loop-common.sh:263`, `hooks/lib/loop-common.sh:868`, `hooks/lib/loop-common.sh:930`, `hooks/loop-plan-file-validator.sh:95`, `hooks/pr-loop-stop-hook.sh:298`, `hooks/pr-loop-stop-hook.sh:349`, `hooks/pr-loop-stop-hook.sh:395`, `hooks/pr-loop-stop-hook.sh:547`, `hooks/pr-loop-stop-hook.sh:778`, `hooks/loop-codex-stop-hook.sh:1446`).
- dependencies_and_callers: Registered by `hooks/hooks.json`; sourced libraries are `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `scripts/portable-timeout.sh`. Runtime external dependencies are `bash`, `jq`, `sed`, `awk`, `grep`, `git`, `gh`, `codex`, optional `iconv`, and timeout implementations. PR hook dependencies include `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:58`, `hooks/pr-loop-stop-hook.sh:675`, `hooks/pr-loop-stop-hook.sh:776`, `hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:1547`). RLCR hook depends on `hooks/check-todos-from-transcript.py` and Codex CLI (`hooks/loop-codex-stop-hook.sh:345`, `hooks/loop-codex-stop-hook.sh:856`, `hooks/loop-codex-stop-hook.sh:1272`, `hooks/loop-codex-stop-hook.sh:977`).
- edge_cases_or_failure_modes: The hooks explicitly cover malformed JSON, missing tool fields, deeply nested JSON, missing/corrupt state frontmatter, missing templates, missing active loop, stale round files, command-injection-like cancel attempts, symlinked state cancel sources, missing/timeout git operations, missing Codex, empty Codex output, fork PRs where current repo is not the PR base, paginated GitHub APIs, PR closed/merged, force pushes, new commits after previous triggers, bots timing out, rate limits, missing Claude `eyes`, and mixed bot approval where some bots approve while others still report issues (`hooks/lib/loop-common.sh:62`, `hooks/lib/loop-common.sh:72`, `hooks/lib/loop-common.sh:127`, `hooks/lib/loop-common.sh:696`, `hooks/pr-loop-stop-hook.sh:232`, `hooks/pr-loop-stop-hook.sh:431`, `hooks/pr-loop-stop-hook.sh:778`, `hooks/pr-loop-stop-hook.sh:922`, `hooks/pr-loop-stop-hook.sh:1405`, `hooks/loop-codex-stop-hook.sh:1312`, `hooks/loop-codex-stop-hook.sh:1349`, `hooks/loop-codex-stop-hook.sh:1377`).
- validation_or_tests: Hook behavior is covered by several test files. The assigned PR stop-hook suite exercises force-push/stale trigger, startup-case exceptions, approval-state creation, unpushed commits, missing triggers, bot timeouts, Codex `+1`, Claude `eyes`, fork PR resolution, and mixed approval (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:246`, `tests/test-pr-loop-stophook.sh:452`, `tests/test-pr-loop-stophook.sh:538`, `tests/test-pr-loop-stophook.sh:642`, `tests/test-pr-loop-stophook.sh:754`, `tests/test-pr-loop-stophook.sh:896`, `tests/test-pr-loop-stophook.sh:1011`, `tests/test-pr-loop-stophook.sh:1390`, `tests/test-pr-loop-stophook.sh:1519`). Additional validation is referenced in `tests/test-pr-loop-hooks.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, `tests/test-todo-checker.sh`, and robustness suites. I did not execute tests because this assignment is a read-only research pass.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-034 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime GitHub reaction probe used by PR loop setup and stop-hook logic to convert bot reactions into loop decisions. It detects Codex `+1` approval on the PR body and Claude `eyes` acknowledgement on trigger comments (`scripts/check-bot-reactions.sh:3`, `scripts/check-bot-reactions.sh:5`, `scripts/check-bot-reactions.sh:10`).
- algorithmic_behavior: The script dispatches on `codex-thumbsup` and `claude-eyes` (`scripts/check-bot-reactions.sh:86`, `scripts/check-bot-reactions.sh:190`). For `codex-thumbsup`, it parses `<pr_number>` plus optional `--after`, resolves the PR base repo by first trying the current repo and then the parent repo for fork support, fetches issue reactions with pagination, filters for `chatgpt-codex-connector[bot]` and `+1`, rejects reactions older than `--after`, and prints the reaction JSON (`scripts/check-bot-reactions.sh:92`, `scripts/check-bot-reactions.sh:119`, `scripts/check-bot-reactions.sh:124`, `scripts/check-bot-reactions.sh:138`, `scripts/check-bot-reactions.sh:157`, `scripts/check-bot-reactions.sh:166`, `scripts/check-bot-reactions.sh:177`, `scripts/check-bot-reactions.sh:186`). For `claude-eyes`, it parses `<comment_id>`, optional `--pr`, `--retry`, and `--delay`, resolves the PR base repo when a PR is supplied, then loops with sleep/retry and paginated comment-reaction fetches until it finds `claude[bot]` with `eyes` (`scripts/check-bot-reactions.sh:197`, `scripts/check-bot-reactions.sh:232`, `scripts/check-bot-reactions.sh:265`, `scripts/check-bot-reactions.sh:273`, `scripts/check-bot-reactions.sh:282`, `scripts/check-bot-reactions.sh:286`).
- inputs_outputs_state: Inputs are CLI args, current GitHub repository context, optional PR number for fork resolution, GitHub reactions API responses, `jq`, `gh`, and `scripts/portable-timeout.sh` (`scripts/check-bot-reactions.sh:31`). Outputs are JSON reaction objects on stdout for success, stderr diagnostics for usage/API/timeout cases, and exit codes: `0` reaction found, `1` not found or retries exhausted, `2` argument/API failure (`scripts/check-bot-reactions.sh:13`, `scripts/check-bot-reactions.sh:57`, `scripts/check-bot-reactions.sh:160`, `scripts/check-bot-reactions.sh:299`). It has no persistent state.
- gates_or_invariants: The script requires a PR number for `codex-thumbsup` and a comment ID for `claude-eyes`; unknown options and duplicate positional arguments are hard errors (`scripts/check-bot-reactions.sh:98`, `scripts/check-bot-reactions.sh:106`, `scripts/check-bot-reactions.sh:114`, `scripts/check-bot-reactions.sh:211`, `scripts/check-bot-reactions.sh:219`, `scripts/check-bot-reactions.sh:227`). It uses base repository reactions for fork PR correctness and `--paginate | jq -s 'add // []'` to avoid first-page-only decisions (`scripts/check-bot-reactions.sh:152`, `scripts/check-bot-reactions.sh:156`, `scripts/check-bot-reactions.sh:269`, `scripts/check-bot-reactions.sh:272`). Timestamp filtering is lexicographic ISO-8601 comparison, so correctness depends on normalized UTC timestamps (`scripts/check-bot-reactions.sh:177`).
- dependencies_and_callers: Direct callers include `scripts/setup-pr-loop.sh` for mandatory Claude `eyes` verification during case 4/5 setup (`scripts/setup-pr-loop.sh:587`) and `hooks/pr-loop-stop-hook.sh` for Codex `+1` before/during polling and Claude `eyes` after trigger validation (`hooks/pr-loop-stop-hook.sh:675`, `hooks/pr-loop-stop-hook.sh:776`, `hooks/pr-loop-stop-hook.sh:998`). Tests call it directly in `tests/test-pr-loop-system.sh` (`tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:207`, `tests/test-pr-loop-system.sh:223`). Runtime dependencies are `bash`, `gh`, `jq`, and `run_with_timeout`.
- edge_cases_or_failure_modes: If repo resolution fails, it falls back to current repo, which can still make API fetch fail for forks (`scripts/check-bot-reactions.sh:147`, `scripts/check-bot-reactions.sh:259`). Codex mode treats reaction-fetch failure as exit `2`, while Claude mode swallows per-attempt API errors and retries until exhaustion (`scripts/check-bot-reactions.sh:157`, `scripts/check-bot-reactions.sh:273`, `scripts/check-bot-reactions.sh:276`). Claude mode sleeps before the first check, so `--retry 3 --delay 5` waits before each attempt (`scripts/check-bot-reactions.sh:265`, `scripts/check-bot-reactions.sh:267`). It selects the first matching reaction from array order rather than newest explicitly (`scripts/check-bot-reactions.sh:166`), so API ordering matters if multiple matching reactions exist.
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies Codex `+1` detection, `--after` rejection, and Claude `eyes` detection with fast retry settings (`tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:207`, `tests/test-pr-loop-system.sh:223`). The assigned stop-hook suite indirectly validates its integration through Codex approval and Claude eyes timeout paths (`tests/test-pr-loop-stophook.sh:896`, `tests/test-pr-loop-stophook.sh:1011`).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-064 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/pr-loop-stop-hook.sh`. It defines `run_stophook_tests`, creates isolated `.humanize/pr-loop/<timestamp>` fixtures, injects mock `gh`, `git`, and sometimes `codex`, invokes the stop hook, and asserts behavior through output text and state-file side effects (`tests/test-pr-loop-stophook.sh:1`, `tests/test-pr-loop-stophook.sh:15`, `tests/test-pr-loop-stophook.sh:1768`).
- algorithmic_behavior: The suite models PR-loop state transitions and guard conditions as scenarios. It verifies old trigger comments are rejected when `latest_commit_at` is newer than the trigger (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:25`, `tests/test-pr-loop-stophook.sh:75`, `tests/test-pr-loop-stophook.sh:129`). It verifies round 0 startup case 1 does not require a trigger (`tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:145`, `tests/test-pr-loop-stophook.sh:231`). It verifies empty `active_bots` moves the loop to approval (`tests/test-pr-loop-stophook.sh:246`, `tests/test-pr-loop-stophook.sh:312`). It checks unpushed commits block exit, force-push history rewrites block and request retrigger, and startup case 4 missing trigger blocks (`tests/test-pr-loop-stophook.sh:452`, `tests/test-pr-loop-stophook.sh:528`, `tests/test-pr-loop-stophook.sh:538`, `tests/test-pr-loop-stophook.sh:632`, `tests/test-pr-loop-stophook.sh:642`, `tests/test-pr-loop-stophook.sh:744`).
- algorithmic_behavior: It also specifies bot lifecycle handling: timeout auto-removes bots and can create `approve-state.md` with empty `active_bots` (`tests/test-pr-loop-stophook.sh:754`, `tests/test-pr-loop-stophook.sh:850`, `tests/test-pr-loop-stophook.sh:874`); Codex `+1` removes Codex and can approve when it is the only bot (`tests/test-pr-loop-stophook.sh:896`, `tests/test-pr-loop-stophook.sh:999`); Claude missing `eyes` is blocking (`tests/test-pr-loop-stophook.sh:1011`, `tests/test-pr-loop-stophook.sh:1131`, `tests/test-pr-loop-stophook.sh:1135`); dynamic startup-case re-evaluation preserves/updates `startup_case` after new bot comments (`tests/test-pr-loop-stophook.sh:1145`, `tests/test-pr-loop-stophook.sh:1357`, `tests/test-pr-loop-stophook.sh:1364`); fork PR base repo resolution avoids current-fork lookup failure (`tests/test-pr-loop-stophook.sh:1390`, `tests/test-pr-loop-stophook.sh:1424`, `tests/test-pr-loop-stophook.sh:1501`); mixed approval keeps the loop active when one bot approves and another reports issues (`tests/test-pr-loop-stophook.sh:1519`, `tests/test-pr-loop-stophook.sh:1678`, `tests/test-pr-loop-stophook.sh:1740`, `tests/test-pr-loop-stophook.sh:1746`).
- inputs_outputs_state: Inputs are generated YAML frontmatter state files with fields such as `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id`; per-test mock command behavior; `CLAUDE_PROJECT_DIR`; and `PATH` overrides (`tests/test-pr-loop-stophook.sh:27`, `tests/test-pr-loop-stophook.sh:122`, `tests/test-pr-loop-stophook.sh:523`, `tests/test-pr-loop-stophook.sh:1704`). Outputs under test are hook block messages, renamed/mutated state files, generated `round-N-pr-check.md`, and active bot YAML changes (`tests/test-pr-loop-stophook.sh:313`, `tests/test-pr-loop-stophook.sh:438`, `tests/test-pr-loop-stophook.sh:867`, `tests/test-pr-loop-stophook.sh:1716`, `tests/test-pr-loop-stophook.sh:1750`).
- gates_or_invariants: The tests encode core PR-loop invariants: trigger comments must be from after the latest commit or force push, startup case 1 round 0 is exempt from trigger, PR loops require clean and pushed code, force pushes require new bot mentions, missing trigger blocks for cases 4/5 or later rounds, Claude `eyes` must be observed when Claude is configured and trigger is required, timeouts remove nonresponsive bots, Codex `+1` is approval only after the correct timestamp, and partial approval must not inflate resolved counts or complete the loop prematurely (`tests/test-pr-loop-stophook.sh:129`, `tests/test-pr-loop-stophook.sh:231`, `tests/test-pr-loop-stophook.sh:528`, `tests/test-pr-loop-stophook.sh:632`, `tests/test-pr-loop-stophook.sh:744`, `tests/test-pr-loop-stophook.sh:1135`, `tests/test-pr-loop-stophook.sh:1734`).
- dependencies_and_callers: Intended to be sourced by the broader test harness because it only defines `run_stophook_tests`; it assumes `PROJECT_ROOT`, `TEST_DIR`, and helper functions `pass`/`fail` are already defined (`tests/test-pr-loop-stophook.sh:12`, `tests/test-pr-loop-stophook.sh:15`). It depends on `timeout`, `date` with GNU/BSD fallbacks, mockable `gh`, `git`, and `codex`, and the real `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: Several assertions are text-grep tolerant rather than exact JSON validation, so message wording changes can still pass if broad keywords remain (`tests/test-pr-loop-stophook.sh:131`, `tests/test-pr-loop-stophook.sh:317`, `tests/test-pr-loop-stophook.sh:855`, `tests/test-pr-loop-stophook.sh:1136`). One earlier dynamic startup-case test is defined but not invoked in the final runner, while the later `test_stophook_dynamic_startup_case_update` is invoked (`tests/test-pr-loop-stophook.sh:327`, `tests/test-pr-loop-stophook.sh:1778`). Some tests use wall-clock timeouts around hooks that poll, making behavior sensitive to local shell `timeout` availability and timing (`tests/test-pr-loop-stophook.sh:434`, `tests/test-pr-loop-stophook.sh:852`, `tests/test-pr-loop-stophook.sh:1339`, `tests/test-pr-loop-stophook.sh:1709`).
- validation_or_tests: This file is itself validation. Its final runner calls 12 stop-hook scenarios (`tests/test-pr-loop-stophook.sh:1768`, `tests/test-pr-loop-stophook.sh:1780`). I did not execute it in this read-only research pass.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-094 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Block-message template for PR-loop state-file mutation attempts. It explains that `.humanize/pr-loop/state.md` is system-managed and must not be edited (`prompt-template/block/pr-loop-state-modification.md:1`, `prompt-template/block/pr-loop-state-modification.md:3`).
- algorithmic_behavior: This template is loaded by `pr_loop_state_blocked_message()` in `hooks/lib/loop-common.sh` and emitted by Write/Edit/Bash validators whenever a tool attempts to modify PR-loop `state.md` (`hooks/lib/loop-common.sh:889`, `hooks/loop-write-validator.sh:78`, `hooks/loop-edit-validator.sh:61`, `hooks/loop-bash-validator.sh:407`, `hooks/loop-bash-validator.sh:412`). It is not a parser or state machine itself, but it is part of the enforcement contract: protected state mutations are converted into a stable user-facing block reason.
- inputs_outputs_state: Inputs are template loading by `load_and_render_safe`; it has no variables. Output is static Markdown with the blocked operation reason and a short list of state contents: round number, PR number/branch, active bots, Codex config, and polling settings (`prompt-template/block/pr-loop-state-modification.md:5`). It does not mutate state.
- gates_or_invariants: Encodes the invariant that PR-loop `state.md` is authoritative scheduler/hook state, not an editable user artifact. The reason states that manual edits would corrupt PR-loop state (`prompt-template/block/pr-loop-state-modification.md:12`).
- dependencies_and_callers: Depends on the template loader path resolution from `hooks/lib/template-loader.sh` and is reached through `hooks/lib/loop-common.sh`. Primary callers are `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh`.
- edge_cases_or_failure_modes: If the template is missing, `pr_loop_state_blocked_message()` falls back to a shorter inline message, so enforcement continues but the richer field explanation is lost (`hooks/lib/loop-common.sh:891`). Because the template contains no variables, render-time placeholder injection is not relevant.
- validation_or_tests: Template reference coverage is checked by `tests/test-template-references.sh`, which includes hook scripts that load block templates. PR-loop hook tests also rely on state-file protection indirectly through validator behavior in `tests/test-pr-loop-hooks.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-124 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: Prompt header template for the initial PR review loop instruction file. It gives the worker the round-0 context: PR number, branch, active bots, and fetched review comments (`prompt-template/pr-loop/round-0-header.md:1`, `prompt-template/pr-loop/round-0-header.md:3`, `prompt-template/pr-loop/round-0-header.md:7`, `prompt-template/pr-loop/round-0-header.md:12`).
- algorithmic_behavior: `scripts/setup-pr-loop.sh` renders this template into `.humanize/pr-loop/<timestamp>/round-0-prompt.md`, then appends the fetched `round-0-pr-comment.md` content and a task section selected by whether comments exist (`scripts/setup-pr-loop.sh:731`, `scripts/setup-pr-loop.sh:740`, `scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:761`, `scripts/setup-pr-loop.sh:763`, `scripts/setup-pr-loop.sh:766`). The header is the top of the initial prompt that starts the PR-loop workflow; it does not decide startup case itself, but presents the state generated just before it.
- inputs_outputs_state: Inputs are `{{PR_NUMBER}}`, `{{START_BRANCH}}`, and `{{ACTIVE_BOTS_DISPLAY}}` supplied by `setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:731`). Output is Markdown prompt text. It does not mutate state, but it references state established by setup: `state.md` has `current_round: 0`, `configured_bots`, `active_bots`, `startup_case`, latest commit fields, and trigger fields (`scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:629`, `scripts/setup-pr-loop.sh:630`, `scripts/setup-pr-loop.sh:632`).
- gates_or_invariants: The first line instructs “Read and execute below with ultrathink” (`prompt-template/pr-loop/round-0-header.md:1`). The invariant is that Round 0 begins from fetched remote PR comments, not from an ungrounded prompt; the comments section begins immediately after the header (`prompt-template/pr-loop/round-0-header.md:12`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe`, which falls back to an inline equivalent if this file is unavailable (`scripts/setup-pr-loop.sh:740`, `scripts/setup-pr-loop.sh:758`). It depends on `hooks/lib/template-loader.sh` syntax: single-pass `{{VAR}}` substitution (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:50`).
- edge_cases_or_failure_modes: If rendering fails or the template is missing, setup uses `FALLBACK_HEADER`, which is effectively the same content with the same variables (`scripts/setup-pr-loop.sh:740`). If `ACTIVE_BOTS_DISPLAY` is empty, the prompt can render with an empty active-bots field; setup normally derives it from configured active bots before rendering (`scripts/setup-pr-loop.sh:643`).
- validation_or_tests: Template usage is covered by template-reference tests and by PR-loop setup/system tests that exercise prompt generation paths. The prompt content is directly tied to setup lines that write `round-0-prompt.md` (`scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:761`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 5 assigned item headings are present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`