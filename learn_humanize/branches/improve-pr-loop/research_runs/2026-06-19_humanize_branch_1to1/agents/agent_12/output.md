# agent_12 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex review prompt surface for the RLCR loop. The directory contains four templates used to drive review-phase decisions, goal-tracker mutation authority, full-alignment audits, stagnation circuit breaking, and audit documentation for `codex review`.
- algorithmic_behavior: The directory has exactly four files: `code-review-phase.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`. `hooks/loop-codex-stop-hook.sh` renders `goal-tracker-update-section.md` first and injects it into both regular and full-alignment prompts at `hooks/loop-codex-stop-hook.sh:767-771`, `:818-829`, and `:834-846`. The hook selects full-alignment review when `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` after validating `FULL_REVIEW_ROUND` and defaulting invalid values to `5` at `hooks/loop-codex-stop-hook.sh:773-783`. Regular reviews instruct Codex to compare the plan, prompt, summary, docs, and goal tracker, then write findings or a terminal `COMPLETE` marker only when all work is done with no pending or deferred tasks at `prompt-template/codex/regular-review.md:20-57`. Full-alignment reviews add mandatory AC-by-AC audit, forgotten item detection, deferred item audit, progress/stagnation review over historical round files, and allow `STOP` as the last-line circuit-break marker at `prompt-template/codex/full-alignment-review.md:19-93`. `code-review-phase.md` documents the separate `codex review --base` invocation, severity marker scanning, and generated audit/debug artifacts at `prompt-template/codex/code-review-phase.md:12-36`; the hook renders that audit file before running code review at `hooks/loop-codex-stop-hook.sh:916-954`.
- inputs_outputs_state: Inputs are rendered variables from hook state: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, previous round indices, `REVIEW_RESULT_FILE`, `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP`. Output is the review prompt file under `.humanize/rlcr/<timestamp>/round-N-review-prompt.md`, plus Codex review result files and cache command/log files described in `code-review-phase.md:30-36` and created by `loop-codex-stop-hook.sh:930-968` and `:1250-1273`. State transitions are textual but operationally significant: `COMPLETE` from summary review enters review/finalize flow, while full-alignment `STOP` triggers loop termination through hook logic visible around `hooks/loop-codex-stop-hook.sh:1388-1497`.
- gates_or_invariants: The templates enforce plan-first review: both regular and full-alignment require reading `@{{PLAN_FILE}}` before review (`regular-review.md:3-11`, `full-alignment-review.md:5-10`). The goal tracker immutable section must not be modified by Codex update handling (`goal-tracker-update-section.md:5-11`). Deferred work is treated as incomplete in both review modes (`regular-review.md:24-31`, `:53-56`; `full-alignment-review.md:88-93`). Full-alignment reviews must consider stagnation signs and emit `STOP` only as the final line when stagnation is found (`full-alignment-review.md:59-90`). Template rendering uses single-pass substitution and keeps missing placeholders rather than recursively expanding injected content, preventing placeholder injection from summaries or review content (`hooks/lib/template-loader.sh:50-58`, `:71-129`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, which loads all four templates. Rendering depends on `hooks/lib/template-loader.sh` via `load_and_render_safe`; fallback text is used if templates are missing or empty (`template-loader.sh:167-203`). The resulting prompts are consumed by `codex exec` in implementation-review mode (`loop-codex-stop-hook.sh:1248-1273`) and the code-review audit template accompanies `codex review --base` (`loop-codex-stop-hook.sh:916-977`). `tests/test-templates-comprehensive.sh` includes direct coverage for at least `goal-tracker-update-section.md` rendering, found at `tests/test-templates-comprehensive.sh:531-539`.
- edge_cases_or_failure_modes: Missing or empty templates do not hard-fail; the hook falls back to embedded fallback prompts through `load_and_render_safe` (`template-loader.sh:179-199`), which keeps the loop operable but with weaker prompt detail. Invalid `FULL_REVIEW_ROUND` values below `2` are corrected to `5` (`loop-codex-stop-hook.sh:776-779`). The full-alignment template references previous history using clamped previous round indices computed by the hook, preventing negative round paths (`loop-codex-stop-hook.sh:785-791`). A subtle template drift exists: `code-review-phase.md` says it scans `[P0-9]` severity markers, while hook comments and tests elsewhere indicate first-characters marker detection is important; the template itself is audit documentation, not the scanner. If Codex follows regular-review’s terminal `COMPLETE` instruction too eagerly, hook-level review/finalize gates still run separately.
- validation_or_tests: Template rendering is covered by the shared template-loader tests and comprehensive template tests. Operational behavior is also exercised indirectly by finalize/review-phase tests such as `tests/test-finalize-phase.sh`, which assert `COMPLETE` review transitions, review-result creation, and Codex review failure blocking. The directory itself is a core prompt/control surface rather than executable code.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Front-gate and generator for `/gen-plan` input/output paths. It validates a draft file and target plan path, then creates the initial plan file by copying the plan template and appending the original draft.
- algorithmic_behavior: The script parses only `--input`, `--output`, and help flags (`scripts/validate-gen-plan-io.sh:29-56`). It requires both arguments (`:58-67`), canonicalizes paths with `realpath -m` when available (`:69-72`), prints validation context (`:74-77`), then runs ordered checks: input exists (`:79-85`), input non-empty (`:87-93`), output directory exists (`:95-101`), output path is not a directory or existing file (`:103-116`), and output directory is writable (`:118-124`). On success it reports line count (`:126-131`), resolves the plan template using `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`:133-140`), fails if the template is missing (`:142-146`), copies the template to the requested output, appends a delimiter plus the draft body, and exits success (`:148-161`).
- inputs_outputs_state: Inputs are command-line arguments and optional `CLAUDE_PLUGIN_ROOT`. The input file must be an existing non-empty draft. The output is a new plan file at `--output`, created by `cp "$TEMPLATE_FILE" "$OUTPUT_FILE"` followed by appended original draft content (`:148-157`). It emits machine-readable-ish status strings like `VALIDATION_ERROR: INPUT_NOT_FOUND`, `VALIDATION_ERROR: OUTPUT_EXISTS`, and `VALIDATION_SUCCESS` (`:81`, `:112`, `:128`). It does not maintain persistent state beyond the generated output file.
- gates_or_invariants: Exit codes are explicitly defined at the top (`:4-12`) and mirrored by `/gen-plan` command instructions at `commands/gen-plan.md:38-47`. Output must be a new file path, not an existing file and not a directory (`validate-gen-plan-io.sh:103-116`). It deliberately refuses to create missing output directories, instead telling the user to create them (`:95-100`). Template existence is treated as plugin configuration failure with exit `7` (`:142-146`). `set -e` means unexpected command failures outside guarded checks terminate the script.
- dependencies_and_callers: Called by `commands/gen-plan.md` during Phase 1 IO validation (`commands/gen-plan.md:30-36`) and whitelisted in that command’s allowed tools (`commands/gen-plan.md:4-5`). Depends on `prompt-template/plan/gen-plan-template.md`, `realpath`, `dirname`, `wc`, `tr`, `cp`, and `cat`. Its exit-code contract is tested by `tests/test-gen-plan.sh:426-541`.
- edge_cases_or_failure_modes: Help exits with code `6`, same as invalid arguments, by design (`validate-gen-plan-io.sh:16-24`, `:48-50`). If `realpath -m` is unavailable, the script falls back to the original path string (`:69-71`). Values beginning with `--` are rejected as missing option values (`:33-36`, `:41-44`), so file paths that literally start with `--` cannot be passed directly. Permission validation uses `[[ -w "$OUTPUT_DIR" ]]`, which may not fully predict write success on ACL or network filesystems. Since the script copies and appends after validation, a race can occur if the output path is created by another process between check and copy. In this read-only branch export, I did not execute the valid write path.
- validation_or_tests: `tests/test-gen-plan.sh:438-539` covers missing option values, unknown options, missing input, empty input, missing output directory, existing output, directory-as-output, valid success path, and help behavior. `/gen-plan` maps each exit code to a user-facing stop condition at `commands/gen-plan.md:38-47`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh safety specification for monitor directory/file discovery. It protects the monitor algorithms against zsh `nomatch` glob failures in empty, dotfile-only, and no-log/no-state conditions.
- algorithmic_behavior: The test must run under zsh (`tests/test-zsh-monitor-safety.sh:1-9`) with strict mode (`:12-13`). It creates an isolated `/tmp/test-zsh-humanize-$$` project and isolated `XDG_CACHE_HOME` (`:48-55`), then cleans it on exit (`:56-60`). Test 1 sources `scripts/humanize.sh` and fails if stderr/stdout contains `no matches found` (`:62-77`). Tests 2 and 3 model latest-session discovery using `find` over `.humanize/rlcr`, checking empty and dotfile-only directories do not trigger glob errors and return empty (`:80-157`). Test 4 models state-file discovery using a `find "$session_dir" -name '*-state.md'` loop and expects `|unknown` when no state file exists (`:160-205`). Test 5 models Codex log lookup with an empty cache directory and expects zero matches without error (`:207-237`). Test 6 verifies timestamp-named session directories are counted and lexicographically latest timestamp wins (`:239-273`). Test 7 verifies zsh is actually active (`:275-286`). Test 8 demonstrates the old glob pattern would be unsafe and the new `find` pattern is safe (`:288-321`). The final summary exits nonzero if any failure was recorded (`:323-341`).
- inputs_outputs_state: Inputs are the checked-out project root, zsh runtime, temporary `.humanize/rlcr` fixtures, and temporary cache directories. Outputs are PASS/FAIL lines and exit `0` when all tests pass. State is local to the temp directory and `XDG_CACHE_HOME`, removed by trap. It does not modify repository files.
- gates_or_invariants: Invariant: monitor discovery must use `find` rather than unguarded globs when directories may be empty or contain only dotfiles. Valid session directories must match `YYYY-MM-DD_HH-MM-SS` (`:109`, `:145`, `:259`). `state.md` has priority for active loops, while no `*-state.md` yields `|unknown` in the modeled logic (`:172-197`). Codex log scan must tolerate missing matching files and empty caches (`:221-235`). The script explicitly verifies the shell with `$ZSH_VERSION` (`:282-286`).
- dependencies_and_callers: It sources `scripts/humanize.sh` (`:72`, `:92`), whose monitor code delegates latest-session and state-file lookup to `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:237-239`, `:344-347`). The production helper uses `find` in `monitor_find_latest_session` (`scripts/lib/monitor-common.sh:40-63`) and `monitor_find_state_file` (`:154-188`). `_find_latest_codex_log` in `scripts/humanize.sh` also uses `find` for session and log scanning (`scripts/humanize.sh:280-339`), matching this test’s intent.
- edge_cases_or_failure_modes: The test reimplements helper logic locally in several subshells instead of invoking every production function directly, so it is strongest as a regression spec for shell patterns and weaker as full integration coverage. It depends on zsh being installed and executing the script; running it under bash would fail the shell-verification gate. It uses `/tmp` and process-id naming; concurrent runs are unlikely to collide but not impossible if pid reuse and stale directories align. Test 8 sets `nomatch` off inside the demonstration subshell (`:300-321`), so it documents old behavior without intentionally failing the whole suite.
- validation_or_tests: The file is itself validation. It aligns with broader monitor tests found through references such as `tests/test-monitor-e2e-real.sh` and `tests/test-pr-loop-system.sh`, but its unique role is zsh runtime safety around empty-match conditions.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Blocking prompt template for file access path enforcement. It tells the agent that a read or write targeted the right filename in the wrong loop directory and gives the canonical path.
- algorithmic_behavior: The template has three rendered fields: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:1-6`). It is loaded by both read and write validators when a round file path does not equal the current active loop directory plus expected filename. `hooks/loop-read-validator.sh` computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and exits with code `2` after rendering this template on mismatch (`hooks/loop-read-validator.sh:153-168`). `hooks/loop-write-validator.sh` performs the same path check and block for writes (`hooks/loop-write-validator.sh:248-263`).
- inputs_outputs_state: Inputs are validator-derived `ACTION`, attempted `FILE_PATH`, and computed `CORRECT_PATH`. Output is a short stderr block rendered through `load_and_render_safe` and hook exit code `2`, which blocks the tool action. No persistent state is changed. The message also includes a command hint, `cat {{FILE_PATH}}`, at line `6`; given the surrounding text, this appears intended as a user-facing recovery hint, though it references the attempted path rather than `{{CORRECT_PATH}}`.
- gates_or_invariants: Invariant: round prompt/summary/review file access must occur inside the active loop directory, not any similarly named stale or wrong directory. The validators apply this after round-number checks; read validator lines `138-150` block wrong round first, then directory path at `153-168`. Write validator similarly checks round number before directory path at `235-263`. Rendering is single-pass and fallback-backed by `hooks/lib/template-loader.sh:167-203`.
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`. Depends on the hook context variables `ACTIVE_LOOP_DIR`, `CLAUDE_FILENAME`, `FILE_PATH`, and `ACTION`, plus `hooks/lib/template-loader.sh`. Sibling block templates handle wrong round number/file cases, so this file is specifically the directory-location gate, not the round-index gate.
- edge_cases_or_failure_modes: The template’s final hint says `cat {{FILE_PATH}}`; if the attempted path is wrong, a more useful hint may be the correct path. This may be a wording bug or an intentional “if you need this exact file” hint, but it can mislead during recovery. The comparison in callers is exact string equality (`[[ "$FILE_PATH" != "$CORRECT_PATH" ]]`), so semantically equivalent paths with symlinks, `..`, or non-normalized forms may block. The template is intentionally terse and does not explain active loop discovery.
- validation_or_tests: Covered indirectly through hook tests for plan/loop validators and template-loader tests. Direct references are the read-validator block at `hooks/loop-read-validator.sh:157-167` and write-validator block at `hooks/loop-write-validator.sh:252-262`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-132 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for `humanize_parse_goal_tracker`, the parser that converts `goal-tracker.md` into monitor/status counts for acceptance criteria, tasks, deferred work, open issues, and goal summary.
- algorithmic_behavior: The script uses bash strict mode (`tests/robustness/test-goal-tracker-robustness.sh:13`), resolves project paths (`:15-16`), sources shared test helpers and production `scripts/humanize.sh` (`:18-22`), creates a temp test directory (`:24`), and defines `parse_result` to read fields from the pipe-delimited parser output (`:38-51`). It then creates synthetic goal-tracker files and calls `humanize_parse_goal_tracker` for 18 cases. Positive cases verify list AC count (`:60-87`), table AC count (`:89-116`), active task count excluding completed/deferred statuses (`:118-153`), completed task rows and unique completed AC count (`:155-202`), and ultimate goal extraction (`:204-227`). Negative/edge cases verify nonexistent file defaults (`:237-244`), empty file zero counts (`:246-256`), 60 ACs without overflow (`:258-279`), special characters in descriptions (`:281-302`), malformed sections return zero ACs (`:304-326`), truncated files still count parseable ACs (`:328-347`), binary content does not fully break parsing (`:349-371`), open issue count (`:373-409`), deferred task count (`:411-439`), headers-only zero counts (`:441-475`), mixed bold/subnumbered AC formats (`:477-499`), and long goal summary truncation (`:501-524`). It exits via `print_test_summary` (`:526-531`).
- inputs_outputs_state: Inputs are temporary markdown files generated by the test and the production parser from `scripts/humanize.sh`. The parser output contract is `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary`, documented in test comments (`test-goal-tracker-robustness.sh:35-36`) and implemented at `scripts/humanize.sh:30-32`, `:111`. Output is test PASS/FAIL reporting plus exit code from `print_test_summary`. State is temp-only via `tests/test-helpers.sh:84-89`.
- gates_or_invariants: Parser must return `0|0|0|0|0|0|No goal tracker` for missing files (`scripts/humanize.sh:32-37`). AC counting is limited to the `### Acceptance Criteria` through `---` range and recognizes list or table AC forms with integer AC ids (`scripts/humanize.sh:48-54`). Active tasks are data rows in `#### Active Tasks` minus rows whose status column is completed or deferred, clamped at zero (`scripts/humanize.sh:56-84`). Completed, deferred, and open issue counts are markdown table data-row counts with header/separator subtraction (`scripts/humanize.sh:39-46`, `:86-102`). Unique completed AC count comes from the completed section (`scripts/humanize.sh:90-94`). Goal summary is the first nonempty non-placeholder line after `### Ultimate Goal`, truncated to 60 characters (`scripts/humanize.sh:104-109`).
- dependencies_and_callers: Direct dependency is `scripts/humanize.sh`, which sources `scripts/lib/monitor-common.sh` if present (`scripts/humanize.sh:6-10`) and defines `humanize_parse_goal_tracker` at `:32-112`. The parser is used by monitor rendering through `_parse_goal_tracker() { humanize_parse_goal_tracker "$@"; }` at `scripts/humanize.sh:367-369`, so these counts feed runtime monitor status. Shared helpers provide `pass`, `fail`, temp directory setup, and final summary (`tests/test-helpers.sh:30-89`).
- edge_cases_or_failure_modes: The tests intentionally document malformed markdown behavior: without proper `###` headers, AC count should be zero (`test-goal-tracker-robustness.sh:304-326`). Subnumbered ACs like `AC-3.1` are not expected to count as normal ACs, while bold `**AC-2**` should count (`:477-499`). Binary content mixed into markdown is tolerated only to the extent that at least one parseable AC remains (`:349-371`). Goal extraction truncates, so monitor summaries lose detail after 60 characters. Because parser internals use sed/grep table heuristics, unusual markdown tables, escaped pipes, or alternative heading depths may be miscounted; the test codifies the accepted subset rather than a full markdown parser.
- validation_or_tests: This file is the validation for the goal tracker parser. It covers standard, large, malformed, truncated, binary, special-character, and section-count scenarios. I did not execute it because the branch export is read-only and the user requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5; the five assigned item ids appear as the five item section headings above, each once.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`