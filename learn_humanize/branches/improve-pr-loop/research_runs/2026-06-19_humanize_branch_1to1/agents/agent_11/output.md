# agent_11 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-side prompt fragments for the RLCR loop’s phase machine. The directory has no nested subdirectories and contains eight markdown templates: `finalize-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `goal-tracker-update-request.md`, `next-round-footer.md`, `next-round-prompt.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, and `review-phase-prompt.md`.
- algorithmic_behavior: The templates encode transitions after Codex feedback rather than executable code. `next-round-prompt.md` reintroduces the original plan, injects `{{REVIEW_CONTENT}}`, requires TodoWrite coverage for all discovered issues, and makes `goal-tracker.md` read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1`, `:12`, `:16`, `:22`). `review-phase-prompt.md` switches semantics to code-review repair: address all `[P0-9]` findings, fixes only, then commit and summarize (`prompt-template/claude/review-phase-prompt.md:1`, `:11`). `finalize-phase-prompt.md` starts the cleanup-only phase after review pass and forbids behavior changes (`prompt-template/claude/finalize-phase-prompt.md:1`, `:3`, `:16`). `finalize-phase-skipped-prompt.md` handles a degraded path when review was skipped and asks for manual verification (`prompt-template/claude/finalize-phase-skipped-prompt.md:1`, `:3`, `:7`). `post-alignment-action-items.md` biases work after full alignment checks toward forgotten items, unmet ACs, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2`). `next-round-footer.md` and `push-every-round-note.md` add commit, summary, and optional push requirements (`prompt-template/claude/next-round-footer.md:4`, `:8`; `prompt-template/claude/push-every-round-note.md:2`). `goal-tracker-update-request.md` defines the only allowed post-round-0 mutation path for goal tracker changes through summary requests (`prompt-template/claude/goal-tracker-update-request.md:2`).
- inputs_outputs_state: Inputs are placeholder variables such as `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, `{{REVIEW_CONTENT}}`, `{{SUMMARY_FILE}}`, `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{FINALIZE_SUMMARY_FILE}}`, and `{{REVIEW_SKIP_REASON}}`. Outputs are rendered prompts consumed by Claude during implementation, review repair, and finalize phases. State transitions implied by the directory are implementation feedback loop -> review phase when Codex says complete -> finalize phase when code review has no `[P0-9]` findings, with a separate review-skipped finalize variant.
- gates_or_invariants: Key prompt-level invariants are: create todos for all discovered work, do not silently narrow scope, do not directly mutate goal tracker after round 0, do not exit by editing loop state or cancelling the loop, commit before summary, complete todos before finalize exit, and preserve functionality during simplification (`next-round-prompt.md:12`, `:30`; `next-round-footer.md:4`; `finalize-phase-prompt.md:20`, `:43`).
- dependencies_and_callers: The templates are rendered by loop orchestration code through the shared template loader. Nearby caller evidence shows block templates are loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`, and the same `TEMPLATE_DIR` mechanism is initialized from `hooks/lib/loop-common.sh:139`. These Claude templates coordinate with the initial prompt emitted by `scripts/setup-rlcr-loop.sh`, which creates round-0 prompts directly (`scripts/setup-rlcr-loop.sh:858`) and with stop-hook phase prompts for later rounds.
- edge_cases_or_failure_modes: The directory encodes special paths for skipped review and full alignment follow-up. Risk areas are placeholder rendering omissions, stale `REVIEW_CONTENT`, or prompt mismatch with hook state causing the worker to write the wrong summary file. The finalize-skipped template explicitly weakens validation from Codex-backed to manual verification (`finalize-phase-skipped-prompt.md:9`).
- validation_or_tests: No direct tests target this directory by name in the inspected set. Indirect validation is through stop-hook behavior and phase tests elsewhere; the templates themselves are static contracts.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-041 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script behind `/humanize:start-rlcr-loop`; creates the RLCR loop state directory, validates prerequisites, selects model/review/base-branch settings, seeds state and goal-tracker files, and prints the first prompt.
- algorithmic_behavior: The script parses CLI options (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--plan-file`, `--track-plan-file`, `--base-branch`, `--full-review-round`, `--skip-impl`) at `scripts/setup-rlcr-loop.sh:120`. It enforces mutual exclusion against active RLCR and PR loops via `find_active_loop` and `find_active_pr_loop` (`:240`). It validates git repository existence and at least one commit (`:297`, `:303`), validates plan paths and content unless skip-impl without a plan is active (`:313`, `:480`), validates Codex availability (`:543`), captures branch/base commit state (`:555`, `:653`), creates `.humanize/rlcr/<timestamp>` (`:667`), writes `state.md` (`:709`), creates a goal tracker (`:737`), and emits either normal or skip-impl round-0 prompt (`:864`, `:902`).
- inputs_outputs_state: Inputs are arguments, current git repository state, `CLAUDE_PROJECT_DIR`, local branches/remotes, the plan file, and Codex CLI presence. Outputs include `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, optional `.review-phase-started`, `round-0-prompt.md`, and activation text. State fields include `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, and `started_at` (`:709`).
- gates_or_invariants: Hard gates include one active loop only, relative plan paths only, no spaces or shell metacharacters in plan paths, no symlink file or symlink parent traversal, plan must resolve inside project, plan cannot live in a submodule, tracked-plan mode requires a clean tracked file, default mode requires the plan not be tracked, plan needs at least five lines and at least three content lines, model/effort/branch/base names must be YAML-safe, and base branch must exist locally (`:263`, `:320`, `:327`, `:334`, `:346`, `:359`, `:403`, `:408`, `:447`, `:483`, `:531`, `:561`, `:571`, `:594`).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md` through `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`. It sources `scripts/portable-timeout.sh` for bounded git commands (`scripts/setup-rlcr-loop.sh:27`) and `hooks/lib/loop-common.sh` for active-loop discovery (`:232`). It depends on `git`, `codex`, `wc`, `grep`, `sed`, `awk`, and shell filesystem operations.
- edge_cases_or_failure_modes: Handles skip-impl with no plan by generating an internal placeholder and entering review mode immediately (`:272`, `:676`, `:728`). Fails closed on git timeout while checking status/tracking (`:427`). Detects remote default branches that exist remotely but not locally and errors because `codex review --base` needs a local ref (`:600`). Uses a captured `BASE_COMMIT` to avoid empty diffs when starting on the base branch itself (`:653`). The script writes files, so in this read-only branch export I inspected only and did not run it.
- validation_or_tests: Related executable specs include `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, `tests/robustness/test-path-validation-robustness.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, and `tests/robustness/test-base-branch-detection.sh`, all found by reference scan. These cover path safety, setup argument handling, and base branch detection.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook helper that blocks exit when the latest TodoWrite state still contains incomplete todos.
- algorithmic_behavior: The test creates a temp directory, feeds hook input JSON to the checker, and asserts exit-code semantics. It verifies invalid stdin JSON exits `2` (`tests/test-todo-checker.sh:51`), empty input/missing transcript/missing file exits `0` (`:63`, `:75`, `:87`), completed latest todos exit `0` (`:106`), pending or in-progress todos exit `1` (`:121`, `:144`), incomplete output includes task detail (`:136`), invalid JSONL transcript lines are ignored (`:179`), and the latest TodoWrite wins across multiple calls (`:196`).
- inputs_outputs_state: Inputs are synthetic transcript JSONL files in Claude assistant format, direct `tool_use` format, and alternative `message` format. The checker input is JSON containing `transcript_path`. Outputs are process exit codes plus stdout markers/details for incomplete todos. The specified state transition is not persistent state; it selects the most recent TodoWrite payload as the current todo state.
- gates_or_invariants: Only status exactly equal to `completed` is accepted. Missing status, `pending`, and `in_progress` are incomplete (`tests/test-todo-checker.sh:144`, `:249`). No transcript or no todos is permissive and exits `0`; malformed hook input is a parse error and exits `2`. Unicode content and empty todo content are accepted syntactically but status still controls completion (`:265`, `:280`).
- dependencies_and_callers: Depends on `python3` and `hooks/check-todos-from-transcript.py`. The stop hook invokes that checker via `TODO_CHECKER="$SCRIPT_DIR/check-todos-from-transcript.py"` in `hooks/loop-codex-stop-hook.sh:345`. Production behavior in the checker reads JSONL, recognizes assistant/message/direct formats, stores `latest_todos`, and prints `INCOMPLETE_TODOS` before exiting `1` (`hooks/check-todos-from-transcript.py:20`, `:51`, `:65`, `:78`, `:126`).
- edge_cases_or_failure_modes: The spec intentionally allows absence of transcript data to avoid blocking when transcript metadata is unavailable. Invalid JSONL lines inside a transcript are ignored, which favors robustness over strict transcript validation. Empty todo content can still block if status is incomplete. The “latest TodoWrite wins” rule can hide older pending todos if a later TodoWrite omits them.
- validation_or_tests: The file is itself the validation script and exits `0` only if `TESTS_FAILED` remains zero (`tests/test-todo-checker.sh:295`). I inspected it but did not execute it.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-101 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Stop-hook block template used when Claude attempts to exit an active loop without writing the required summary file.
- algorithmic_behavior: The template tells the agent the exit attempt is blocked because no work summary exists and requires writing to `{{SUMMARY_FILE}}` (`prompt-template/block/work-summary-missing.md:3`, `:5`). It defines the minimum summary payload: implemented work, created/modified files, tests added/passed, and remaining items (`:10`).
- inputs_outputs_state: Input is `SUMMARY_FILE` supplied by the stop hook. Output is rendered markdown included in the hook JSON `reason` field. The state effect is a blocked stop decision, not a file mutation by the template itself.
- gates_or_invariants: Exit is blocked until the expected summary file exists. The hook chooses `finalize-summary.md` in finalize phase and `round-N-summary.md` otherwise (`hooks/loop-codex-stop-hook.sh:609`). If the file is absent, it renders this template and returns JSON decision `block` (`hooks/loop-codex-stop-hook.sh:616`, `:632`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/work-summary-missing.md"` with an inline fallback (`hooks/loop-codex-stop-hook.sh:620`). Template loading is provided through `hooks/lib/loop-common.sh`, which sources `template-loader.sh` and establishes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:139`).
- edge_cases_or_failure_modes: If template loading fails, the hook has a fallback reason with less detail. If a summary file exists but is empty or low quality, this particular gate does not catch that; subsequent Codex review must judge content quality.
- validation_or_tests: No direct test was found for this exact template. It is covered indirectly by stop-hook behavior around missing summary files.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-131 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for git status/state helpers in `scripts/humanize.sh`, especially monitor/status behavior used by Humanize loop tooling.
- algorithmic_behavior: The test sources production `scripts/humanize.sh` and validates `humanize_parse_git_status` and `humanize_detect_git_state` against real temporary git repositories (`tests/robustness/test-git-operations-robustness.sh:15`, `:19`, `:32`). It initializes repos with commits (`:49`), parses pipe-delimited counts with `parse_result` (`:35`), and covers clean state, branch name, untracked/modified/staged/deleted files, insertions/deletions, non-git directories, detached HEAD, feature branches, renames, many untracked files, filenames with spaces, binary files, and simultaneous staged plus unstaged changes (`:71`, `:98`, `:113`, `:128`, `:145`, `:168`, `:180`, `:197`, `:218`, `:248`, `:265`, `:284`, `:301`, `:316`, `:331`, `:349`).
- inputs_outputs_state: Inputs are controlled git worktrees and simulated git metadata files. Outputs are pass/fail assertions via `test-helpers.sh`. The production parser returns `modified|added|deleted|untracked|insertions|deletions`; the state detector returns `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo`.
- gates_or_invariants: `humanize_parse_git_status` must not crash outside git and must count porcelain status categories consistently. Renames count as modified. `humanize_detect_git_state` must prioritize rebase, merge, shallow, detached, then normal after confirming repo accessibility (`scripts/humanize.sh:116`, `:136`, `:142`, `:148`, `:154`, `:166`, `:179`).
- dependencies_and_callers: Depends on `git`, `scripts/humanize.sh`, `scripts/portable-timeout.sh`, and `tests/test-helpers.sh`. The production functions are also wrapped by the RLCR monitor status bar (`scripts/humanize.sh:367`). The robustness test simulates rebase by creating `.git/rebase-merge`, merge by creating `MERGE_HEAD`, shallow clone by creating `.git/shallow`, and permission issues by chmodding `.git` (`tests/robustness/test-git-operations-robustness.sh:403`, `:434`, `:450`, `:478`).
- edge_cases_or_failure_modes: Permission behavior is platform-sensitive; the test accepts either `permission_error` or `not_a_repo` for the simulated unreadable `.git` case (`tests/robustness/test-git-operations-robustness.sh:487`). Diffstat parsing may return zero for binary-only changes, so the binary assertion checks untracked count only (`:316`). Empty repositories are expected to produce a non-empty result rather than crash (`:233`).
- validation_or_tests: The file is the validation harness and ends with `print_test_summary "Git Operations Robustness Test Summary"` then exits with the helper’s status (`tests/robustness/test-git-operations-robustness.sh:502`). I inspected it but did not execute it.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence headings present; item IDs intentionally appear only in headings to avoid duplicate cursor evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`