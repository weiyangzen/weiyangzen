# agent_16 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness test suite for the loop algorithms. The directory is flat, with 17 shell specs covering RLCR setup validation, PR-loop API handling, hook input/state protection, state parsing/transitions, session discovery, template rendering, git/status parsing, timeout fallback, and cancel authorization. It is core because these scripts encode executable contracts for the branch’s state machine, hook gates, prompt/template safety, and PR/RLCR loop coordination.
- algorithmic_behavior: Each child script builds isolated temp fixtures, usually via `tests/test-helpers.sh`, then invokes production shell functions or scripts. Representative contracts: `test-base-branch-detection.sh` mirrors setup base-branch fallback logic from remote default to local `main`/`master` and failure when none exists (`tests/robustness/test-base-branch-detection.sh:5`, `:35`, `:67`, `:96`, `:125`, `:152`); `test-cancel-security-robustness.sh` exercises `is_cancel_authorized` against signal-file, exact `mv`, injection, quote, path, and symlink cases (`:5`, `:39`, `:99`, `:317`, `:332`, `:369`); `test-concurrent-state-robustness.sh`, `test-session-robustness.sh`, `test-state-file-robustness.sh`, and `test-state-transition-robustness.sh` cover active loop discovery, strict/tolerant parsing, finalize/cancel states, malformed YAML, and round fields (`test-concurrent-state-robustness.sh:170`, `:333`; `test-session-robustness.sh:33`, `:259`; `test-state-file-robustness.sh:33`, `:149`, `:399`; `test-state-transition-robustness.sh:98`, `:145`, `:179`, `:289`).
- inputs_outputs_state: Inputs are generated Git repositories, `.humanize/rlcr/*` and `.humanize/pr-loop/*` state trees, hook JSON payloads, mock `gh`/`codex` binaries, plan files, goal trackers, templates, and shell commands. Outputs are pass/fail assertions through `print_test_summary`, hook exit codes, generated loop state files, formatted PR comment markdown/JSON, and detected parser values. State transitions validated include `state.md` active, `finalize-state.md` active/finalize, `cancel-state.md` inactive, `approve-state.md` PR approval, review-started flags, active/configured bot shrink/re-add behavior, and setup-generated `.review-phase-started` in skip-impl mode.
- gates_or_invariants: Main invariants include fail-closed JSON validation and max-depth checks from `hooks/lib/loop-common.sh:59` and `:116`; newest-directory-only active loop discovery from `hooks/lib/loop-common.sh:151`; strict state requirements for frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch` from `hooks/lib/loop-common.sh:246`; summary/state/plan/goal-tracker write gates from `hooks/loop-write-validator.sh:55`, `:140`, `:152`, `:179`, `:192`, `:202`, `:228`, `:254`; cancel command invariants from `hooks/lib/loop-common.sh:533`; timeout fallback order from `scripts/portable-timeout.sh:6`; and one-pass template substitution from `hooks/lib/template-loader.sh:50`.
- dependencies_and_callers: The suite depends on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `hooks/loop-*-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. It coordinates with parent `tests/` via `tests/test-helpers.sh` and sibling non-robustness tests such as `tests/test-pr-loop-hooks.sh`, which calls `tests/setup-monitor-test-env.sh` for monitor fixtures.
- edge_cases_or_failure_modes: Covered failures include malformed/missing hook JSON, deep JSON, null/invalid UTF-8, binary-looking payloads, command injection, stale or incomplete state, CRLF, missing frontmatter, negative/non-numeric rounds, old zombie sessions, symlink session or plan paths, shell metacharacter plan paths, API rate limits/network errors, missing `gh`/`jq`, slow PR polling, large plan/template values, missing templates, permission-denied templates/state files, and commands ignoring signals.
- validation_or_tests: This item itself is validation. I inspected recursively with `find tests/robustness -type f`; directory contains only shell files and no subdirectories below `tests/robustness`. I did not run the scripts because this branch export is read-only and the tests create temp repos/files and invoke Git/Xcode-backed tooling that failed under the sandbox; evidence is source inspection.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Fixture generator for PR monitor tests. It creates canonical `.humanize/pr-loop/<timestamp>/state.md` frontmatter variants used to verify monitor parsing of `configured_bots` and `active_bots`.
- algorithmic_behavior: The script parses `TEST_DIR` and `TEST_NAME` (`tests/setup-monitor-test-env.sh:12`), requires a test dir (`:15`), switches on `yaml_list`, `configured`, and `empty` (`:20`, `:43`, `:64`), writes a timestamped PR loop state file with YAML frontmatter and bot lists, rejects unknown fixture names with available options (`:84`), then echoes the test directory (`:91`). The three scenarios model: both bots active (`yaml_list`, `:31`-`:37`), only `codex` still active after partial approval (`configured`, `:53`-`:58`), and an empty `active_bots:` field meaning all approved (`empty`, `:74`-`:78`).
- inputs_outputs_state: Inputs are `<test_dir>` and `<test_name>`. Output is a populated state tree under `$TEST_DIR/.humanize/pr-loop/<fixed timestamp>/state.md`; stdout is `$TEST_DIR`. State fields include `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- gates_or_invariants: Requires exactly a writable test dir argument and one of the known scenario labels. Uses stable timestamps and fixed PR numbers to make monitor expectations deterministic. Keeps `configured_bots` immutable in all scenarios while changing `active_bots`, matching `pr-loop-stop-hook.sh`’s invariant that configured bots are the full polling set and active bots shrink as approvals arrive (`hooks/pr-loop-stop-hook.sh:110`-`:161`).
- dependencies_and_callers: Called by monitor tests in `tests/test-pr-loop-hooks.sh`: YAML-list parsing fixture at `tests/test-pr-loop-hooks.sh:1553`, configured-vs-active fixture at `:1580`, and empty-active fixture at `:1602`. Parsed by monitor code in `scripts/humanize.sh`, which extracts `configured_bots` and `active_bots` from PR-loop state (`scripts/humanize.sh:1042`-`:1059`) and displays them (`:1132`, `:1256`-`:1257`).
- edge_cases_or_failure_modes: Unknown fixture names fail closed with exit 1. `empty` deliberately emits `active_bots:` with no YAML list entries, exercising parser behavior for empty lists. The script does not create a Git repo or validate monitor output itself; it is strictly fixture setup.
- validation_or_tests: Its behavior is validated indirectly by `tests/test-pr-loop-hooks.sh` monitor cases (`:1548`-`:1620`) and by PR-loop parser expectations in `hooks/pr-loop-stop-hook.sh:120`-`:159`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Prompt/block template for a hard RLCR review failure gate. It tells Claude that Codex review produced no usable output and that the loop will retry review on the next exit attempt.
- algorithmic_behavior: Template renders a failure title, the Codex exit code, missing review-result path, debug file paths, and the last stderr lines (`prompt-template/block/codex-review-failed.md:1`-`:18`). It is used in `block_review_failure` in `hooks/loop-codex-stop-hook.sh:1163`, where review failure is explicitly hard-blocking (`:1163`-`:1165`), stderr is tailed from cache (`:1173`-`:1177`), template variables are passed to `load_and_render_safe` (`:1216`-`:1225`), and the hook returns JSON with `"decision": "block"` (`:1227`-`:1234`).
- inputs_outputs_state: Inputs are template variables including `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT`. In the current caller, the passed variable names partially differ: caller passes `EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_LOG_FILE`, and `STDERR_CONTENT` (`hooks/loop-codex-stop-hook.sh:1217`-`:1225`). Output is rendered markdown embedded in the hook JSON reason.
- gates_or_invariants: Enforces that Codex review failure cannot be skipped; the system must keep the loop active and require another exit/review attempt. The debug section is the operator handoff surface for diagnosing missing `round-N-review-result.md` and Codex stderr.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` single-pass rendering (`hooks/lib/template-loader.sh:50`) and fallback behavior (`:170`). Direct caller is `hooks/loop-codex-stop-hook.sh:block_review_failure`. Covered by broad template reference/comprehensive tests (`tests/test-template-references.sh` references block templates) and finalization/Codex-review tests that assert failure blocking behavior.
- edge_cases_or_failure_modes: If template variables are not supplied, placeholders remain because rendering preserves missing variables (`hooks/lib/template-loader.sh:13`, `:119`-`:122`). Notably, the template expects `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE`, while the caller supplies `EXIT_CODE` and `CODEX_LOG_FILE`; this can leave unresolved debug placeholders in the rendered block. If the template is missing/empty, `load_and_render_safe` falls back to the inline message in the hook (`hooks/loop-codex-stop-hook.sh:1179`-`:1214`).
- validation_or_tests: Template rendering behavior is exercised by `tests/robustness/test-template-error-robustness.sh` missing/fallback/malformed cases and `tests/robustness/test-template-stress-robustness.sh` one-pass substitution stress cases. The specific failure gate is source-validated in `hooks/loop-codex-stop-hook.sh:1163`-`:1235`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Prompt/block template for the summary-location gate. It tells the agent that `round-N-summary.md` must be written inside the active RLCR loop directory, not elsewhere.
- algorithmic_behavior: The template is a minimal diagnostic with title, rule, and rendered correct path (`prompt-template/block/wrong-summary-location.md:1`-`:5`). `hooks/loop-write-validator.sh` invokes it when a path matches `round-[0-9]+-summary.md` but is not inside `.humanize/rlcr/` (`hooks/loop-write-validator.sh:202`-`:209`), then exits 2 to block the write. The same validator later enforces the active loop subdirectory path (`:252`-`:260`).
- inputs_outputs_state: Input variable is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-write-validator.sh:203`). Output is markdown on stderr, with exit status 2 from the validator. It does not mutate state.
- gates_or_invariants: Summary files must be in the active loop directory and match the current round unless allowlisted. This supports the RLCR stop hook’s requirement that the expected summary file exists before Codex review can proceed, preventing summaries from being written in arbitrary locations.
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh`. The template depends on the shared template loader through `loop-common.sh` (`hooks/lib/loop-common.sh:139`-`:145`). Related read/edit validators enforce similar round/file access behavior.
- edge_cases_or_failure_modes: If no active loop is found, the write validator exits 0 earlier (`hooks/loop-write-validator.sh:123`-`:130`), so this block appears only during active RLCR state. If the template is missing, the inline fallback at `hooks/loop-write-validator.sh:204`-`:206` is rendered instead.
- validation_or_tests: Template substitution is covered in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` around wrong-summary-location variables; write-path enforcement is covered by hook robustness/input tests and allowlist validator tests for summary paths.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-136 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Focused executable specification for RLCR plan-file validation in `scripts/setup-rlcr-loop.sh`. It verifies that plan files are sufficiently detailed, readable, stable, and robust to unusual content before loop startup proceeds.
- algorithmic_behavior: The script creates an isolated temp dir and cache (`tests/robustness/test-plan-file-robustness.sh:18`-`:24`), mocks `codex` to avoid real CLI execution (`:27`-`:40`), defines `test_plan_validation` to run production `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"` (`:48`-`:62`), classifies known plan-validation errors as rejection (`:64`-`:68`), and treats later setup milestones such as Codex availability, gitignore gating, `start-rlcr-loop activated`, or created `.humanize/rlcr/*/state.md` as validation success (`:70`-`:95`). It also defines a local `count_content_lines` mirror for blank/comment filtering (`:127`-`:167`).
- inputs_outputs_state: Inputs are generated plan files in the temp Git repo, plus a mocked `codex` on `PATH`. Outputs are pass/fail assertions and, on accepted paths, possible `.humanize/rlcr/<timestamp>/state.md` created by the setup script. Production state under test includes plan backup creation and loop startup state after validation, but this test mostly stops at validation classification.
- gates_or_invariants: Production gates mapped by the test are: relative non-symlink plan paths inside the project (`scripts/setup-rlcr-loop.sh:320`-`:406`), git tracking policy requiring either gitignored untracked plans or clean tracked plans with `--track-plan-file` (`:427`-`:472`), minimum five physical lines (`:483`-`:491`), and at least three content lines after excluding blank lines, `#` comments, and HTML comments (`:493`-`:536`). The test’s positive cases assert acceptance for normal/rich/standard/large/mixed-line-ending plans (`tests/robustness/test-plan-file-robustness.sh:176`, `:204`, `:232`, `:251`, `:323`, `:353`). Negative cases assert rejection for empty, comments-only, and disappeared/missing plan files (`:298`, `:307`, `:525`).
- dependencies_and_callers: Depends directly on `scripts/setup-rlcr-loop.sh`, `tests/test-helpers.sh`, Git, `wc`, `grep`, and a mocked `codex`. The production script in turn depends on `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout and active-loop checks (`scripts/setup-rlcr-loop.sh:29`, `:232`-`:243`).
- edge_cases_or_failure_modes: Covers empty file, comments-only, large file over 1 MB with 20,000 task lines, mixed CRLF/LF, binary bytes, long lines, shell-special text content, whitespace-only files, nested/complex HTML comments, missing file, unreadable file, symlink file, directory-as-file, null bytes, and disappearance before validation (`tests/robustness/test-plan-file-robustness.sh:298`-`:549`). Some edge cases only validate local shell readability/counting rather than invoking production validation, e.g. binary content, long lines, special chars, whitespace-only, nested comments, unreadable detection, symlink detection, directory detection, and null-byte line count (`:366`-`:523`).
- validation_or_tests: This file is itself the validation artifact. I did not execute it in the read-only branch export because it initializes Git repos and writes many temp files; source inspection confirms it uses production setup for the main positive/negative validation paths and local checks for additional file-content edge cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: five evidence sections above, one per assigned row
- missing_items: none
- duplicate_items: none in `## Item Evidence`
- final_worker_status: `complete`