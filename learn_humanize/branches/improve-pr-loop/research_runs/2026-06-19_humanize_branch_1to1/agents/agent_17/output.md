# agent_17 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-017 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Repository relevance gate for the `gen-plan` workflow. It is a Claude subagent definition, not executable shell code, and decides whether a user draft is semantically connected to the current repository before plan generation proceeds.
- algorithmic_behavior: The frontmatter declares `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-5`. The prompt tells the agent to quickly inspect repository docs/structure/technologies, compare the draft against repository concepts/files/features, and emit exactly one verdict form: `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` at `agents/draft-relevance-checker.md:14-29`. The policy is intentionally permissive: semantic relevance over syntactic similarity, informal or multilingual drafts allowed, and uncertainty should lean relevant at `agents/draft-relevance-checker.md:31-36`.
- inputs_outputs_state: Input is draft document content plus repository context discovered via read/search tools. Output is a single textual verdict consumed by the command flow. It does not persist state or mutate files. In `commands/gen-plan.md:50-72`, Phase 2 reads the draft, invokes `humanize:draft-relevance-checker`, stops on `NOT_RELEVANT`, and proceeds on `RELEVANT`.
- gates_or_invariants: The hard gate is only for clearly unrelated drafts. It must not overfit to exact filenames; the invariant is lenient semantic matching. If the draft mentions repository technologies, use, modification, learning, paths, functions, or features, it should pass.
- dependencies_and_callers: Called by `commands/gen-plan.md:56-65` through the Task tool with haiku model. Structural validation is in `tests/test-gen-plan.sh:111-155`, which checks file existence, agent name, model, and tools field. Additional validation references require valid naming/frontmatter/model/content language in `tests/test-gen-plan.sh:241-421`.
- edge_cases_or_failure_modes: False positives are preferred over false negatives by design. Ambiguous, rough, or non-English drafts should not fail solely due to presentation. A malformed verdict string would be a downstream parsing risk because the command workflow expects the `RELEVANT:` / `NOT_RELEVANT:` prefix.
- validation_or_tests: `tests/test-gen-plan.sh` validates the agent artifact exists and has correct metadata; it does not simulate semantic decisions. Functional behavior is prompt-contract based.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-047 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop allowlist behavior across shared file classification and Read/Write/Edit/Bash validators. It defines which historical todo/summary files are allowed despite current-round protections.
- algorithmic_behavior: The test creates a temp git repo and active loop under `.humanize/rlcr/2024-01-01_12-00-00`, writes a `state.md` with `current_round: 5`, and exports active loop context at `tests/test-allowlist-validators.sh:29-70`. It then verifies `is_allowlisted_file` accepts only active-loop exact paths for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`, while rejecting later rounds and wrong directories at `tests/test-allowlist-validators.sh:72-126`. The implementation allowlist is in `hooks/lib/loop-common.sh:423-441`.
- inputs_outputs_state: Inputs are synthetic hook JSON payloads for `Read`, `Write`, `Edit`, and `Bash`, plus `CLAUDE_PROJECT_DIR` and the temporary loop state. Outputs are PASS/FAIL lines and process exit equal to `TESTS_FAILED` at `tests/test-allowlist-validators.sh:385-393`. State is isolated to a temp repo and removed by trap at `tests/test-allowlist-validators.sh:30-31`.
- gates_or_invariants: Exact active-loop path matching is the key invariant. Same basename outside the active loop is blocked. For tool validators, expected allowed cases exit `0`; blocked cases exit `2` and include relevant message text such as `todos` or `round`. Bash allowlist is stricter: it requires the active loop absolute path for `round-1-todos.md` or `round-2-todos.md`, not a relative basename.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-allowlist-validators.sh:15-17`. Exercises `hooks/loop-write-validator.sh` at `tests/test-allowlist-validators.sh:135-185`, `hooks/loop-edit-validator.sh` at `tests/test-allowlist-validators.sh:191-227`, `hooks/loop-read-validator.sh` at `tests/test-allowlist-validators.sh:234-283`, and `hooks/loop-bash-validator.sh` at `tests/test-allowlist-validators.sh:290-382`. The validators use `find_active_loop`, strict state parsing, path type helpers, and allowlist checks, for example `hooks/loop-read-validator.sh:55-63` and `hooks/loop-write-validator.sh:55-63`.
- edge_cases_or_failure_modes: Covers wrong directory, non-allowlisted future todo/summary files, generic relative path, old loop directory, and same active-loop basename under a different root at `tests/test-allowlist-validators.sh:316-382`. Because current round is `5`, the test proves allowlisting is an explicit exception to normal current-round enforcement, not just current-round acceptance.
- validation_or_tests: This file is itself the validator regression suite. Its core sections are function allowlist tests `1-7`, Write tests `8-11`, Edit tests `12-14`, Read tests `15-18`, and Bash path-restriction tests `19-25`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-077 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Static blocking-message template for attempts to modify `finalize-state.md`, which is system-managed during the RLCR Finalize Phase.
- algorithmic_behavior: The template declares the block title and states that `finalize-state.md` cannot be modified because the loop system owns it at `prompt-template/block/finalize-state-file-modification.md:1-3`. It redirects the agent toward valid final-phase work: run the code-simplifier agent, commit changes, and write `finalize-summary.md` at `prompt-template/block/finalize-state-file-modification.md:5-8`.
- inputs_outputs_state: It has no dynamic placeholders. Input is the validator choosing this template; output is markdown text emitted on stderr or as a hook block reason. It does not transition state itself. The state transition into Finalize Phase is performed by `hooks/loop-codex-stop-hook.sh:1036-1039`, which renames `state.md` to `finalize-state.md` and sets the expected summary path.
- gates_or_invariants: The invariant is that `finalize-state.md` is not agent-editable. The shared message loader is `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:472-479`. Validators check `finalize-state.md` before generic `state.md` because the latter pattern also matches the suffix; see `hooks/loop-write-validator.sh:148-154`, `hooks/loop-edit-validator.sh:112-119`, and `hooks/loop-bash-validator.sh:127-145`.
- dependencies_and_callers: Loaded via `load_and_render_safe` from the shared template directory in `hooks/lib/loop-common.sh:478`. Called by Write/Edit validators for direct file paths and by Bash validator for redirection, destination modification, and `mv`/`cp` source-protection paths at `hooks/loop-bash-validator.sh:286-327`.
- edge_cases_or_failure_modes: Bash protection explicitly covers moving or copying from `finalize-state.md`, including shell-wrapper bypass attempts, at `hooks/loop-bash-validator.sh:286-327`. There is an authorized cancel exception through `is_cancel_authorized` when a cancel signal exists; that exception is implemented in `hooks/lib/loop-common.sh:533-710`, not described in this user-facing block.
- validation_or_tests: Related finalize-phase tests exercise the blocked behavior in `tests/test-finalize-phase.sh`, and robustness tests around cancel/security cover symlink and source-path cases. This assigned template is the canonical text used when those gates fire.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-107 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize Phase prompt template shown after Codex review has passed and the implementation is considered complete. It defines the final cleanup/refactor contract before loop completion.
- algorithmic_behavior: The prompt announces that Codex review passed and instructs Claude to use the `code-simplifier:code-simplifier` agent through the Task tool at `prompt-template/claude/finalize-phase-prompt.md:1-14`. It sets non-negotiable refactor constraints: no functionality changes, no test failures, no new bugs, and functionality-equivalent cleanup only at `prompt-template/claude/finalize-phase-prompt.md:16-24`. It scopes simplification to recently added/modified code and especially the diff from `{{BASE_BRANCH}}` to `{{START_BRANCH}}` at `prompt-template/claude/finalize-phase-prompt.md:25-34`.
- inputs_outputs_state: Inputs are render variables `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`. Output is a blocking prompt/reason returned to Claude. State transition is orchestrated by `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh:1030-1114`: it moves `state.md` to `finalize-state.md`, sets `finalize-summary.md`, renders this template with the variables, and returns a JSON decision block.
- gates_or_invariants: Entry gate is no review issues found, shown at `hooks/loop-codex-stop-hook.sh:1024-1027`. Exit requirements are completing todos, committing changes, and writing a finalize summary at `prompt-template/claude/finalize-phase-prompt.md:41-51`. The prompt requires tests still pass and summary includes simplifications, modified files, test confirmation, and refactor notes.
- dependencies_and_callers: Depends on the Task tool and the `code-simplifier:code-simplifier` agent. References original plan and goal tracker through rendered file variables at `prompt-template/claude/finalize-phase-prompt.md:36-40`. Rendered by `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:1097-1102`. If review was skipped, a sibling skipped template is used instead at `hooks/loop-codex-stop-hook.sh:1042-1075`, so this assigned template represents the clean pass path.
- edge_cases_or_failure_modes: A missing or wrong render variable would produce an unusable path in the final instructions. The prompt permits cleanup but forbids behavior change, so overly aggressive simplification is a contract violation even if code still compiles. It also assumes a usable Task subagent exists.
- validation_or_tests: Finalize-phase tests cover active loop detection through `finalize-state.md`, allowing `finalize-summary.md`, and blocking `finalize-state.md` edits. The template’s behavior is primarily validated through stop-hook rendering and hook-gate tests rather than direct golden-output comparison.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-137 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: End-to-end robustness specification for PR-loop API-facing behavior under GitHub CLI failures, bot comment shapes, JSON edge cases, state detection, stop-hook resilience, and polling output contracts.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates an isolated temp directory, and builds mock `gh` executables with scenario-specific behavior at `tests/robustness/test-pr-loop-api-robustness.sh:16-38`. `create_mock_gh` handles `gh repo view`, `gh pr view`, and API cases including empty arrays, rate limits, network errors, Claude approvals, Codex severity comments, mixed bots, Unicode comments, and long bodies at `tests/robustness/test-pr-loop-api-robustness.sh:32-238`.
- inputs_outputs_state: Inputs are mocked GitHub CLI responses, temp git repos, PR-loop `state.md` files, hook stdin `{}`, and script args for `fetch-pr-comments.sh` and `poll-pr-reviews.sh`. Outputs are PASS/FAIL counters via `print_test_summary` and generated files such as `comments.md`. PR-loop state fixtures are written under `.humanize/pr-loop/2026-01-19_00-00-00/state.md` with configured/active bots at `tests/robustness/test-pr-loop-api-robustness.sh:241-261`.
- gates_or_invariants: `find_active_pr_loop` must detect an active loop even with missing `pr_number` at `tests/robustness/test-pr-loop-api-robustness.sh:285-345`; implementation only requires newest dir with `state.md` in `hooks/lib/loop-common.sh:795-814`. `fetch-pr-comments.sh` must create structured output for empty comments, preserve bot/severity content, process Unicode and long bodies, and warn or fail gracefully on API errors at `tests/robustness/test-pr-loop-api-robustness.sh:355-550`. `poll-pr-reviews.sh` has a stricter failure invariant: on API failure it must exit `0`, emit valid JSON, and set `has_new_comments` to boolean false at `tests/robustness/test-pr-loop-api-robustness.sh:811-843`.
- dependencies_and_callers: Exercises `scripts/fetch-pr-comments.sh`, whose real behavior resolves current or parent repo, retries three API endpoints, combines issue comments, inline review comments, and PR reviews, deduplicates, filters, sorts, and writes markdown at `scripts/fetch-pr-comments.sh:126-220` and `scripts/fetch-pr-comments.sh:260-452`. Exercises `scripts/poll-pr-reviews.sh`, which validates required args, resolves repo, maps bot names to authors, retries API calls, filters comments by timestamp and bot regex, and returns JSON at `scripts/poll-pr-reviews.sh:83-187` and `scripts/poll-pr-reviews.sh:207-328`. Also invokes `hooks/pr-loop-stop-hook.sh`, which calls the poll script during PR-loop review polling at `hooks/pr-loop-stop-hook.sh:800-814`.
- edge_cases_or_failure_modes: Covers rate limiting, connection refusal, empty arrays, mixed bot authors, Codex `[P1]` style severity markers, Unicode escape output, 10KB comment bodies, slow API responses, corrupted state files, missing active loop, and missing required poll args. The mock includes an `auth_failure` behavior at `tests/robustness/test-pr-loop-api-robustness.sh:131-143`, but this assigned test does not run that scenario.
- validation_or_tests: This file is the robustness validator. Test groups are PR-loop state handling `1-3`, fetch-pr-comments `4-6`, bot response parsing `7-9`, JSON edges `10-11`, stop-hook behavior `12-14`, and poll-pr-reviews `15-19`, ending with `print_test_summary "PR Loop API Robustness Test Summary"` at `tests/robustness/test-pr-loop-api-robustness.sh:845-850`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, matching the 5 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`