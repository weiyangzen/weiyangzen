# agent_14 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: Prompt contract layer for the PR review loop. These templates create the initial Round 0 agent instructions, completion requirements, PR goal tracker seed, and later Codex goal-tracker update instructions.
- algorithmic_behavior: The directory contains seven templates: `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, `critical-requirements-no-comments.md`, `critical-requirements-has-comments.md`, `goal-tracker-initial.md`, and `codex-goal-tracker-update.md`. `scripts/setup-pr-loop.sh` renders the initial goal tracker with `pr-loop/goal-tracker-initial.md` at lines 712-713, writes the Round 0 prompt header at lines 757-764, selects no-comments vs has-comments task body at lines 766-848, and prints matching critical completion requirements at lines 890-942. `hooks/pr-loop-stop-hook.sh` renders `codex-goal-tracker-update.md` into the local Codex bot-review prompt at lines 1252-1261 and appends it to the required analysis structure at line 1301.
- inputs_outputs_state: Inputs are template variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `STARTED_AT`, `STARTUP_CASE`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are `.humanize/pr-loop/<timestamp>/goal-tracker.md`, `round-0-prompt.md`, printed setup instructions, and Codex review prompts. State transitions are instruction-level: no-comments Round 0 tells the agent to write `round-0-pr-resolve.md` and exit for polling, while has-comments Round 0 requires fixing, committing, pushing, commenting on the PR, and writing the resolve summary.
- gates_or_invariants: The no-comments path explicitly forbids triggering the first review because bots review automatically; the has-comments path requires commit, push, and bot mention. Both Round 0 task templates forbid modifying `.humanize/pr-loop/` state files. `codex-goal-tracker-update.md` requires preserving goal tracker structure, appending per-reviewer rows, updating totals, and not changing PR information/header sections.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` through `load_and_render_safe`. Main callers are `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh`. File protection is coordinated with `hooks/loop-write-validator.sh` lines 74-93, `hooks/loop-edit-validator.sh` lines 57-76, and PR helpers in `hooks/lib/loop-common.sh` lines 783-887.
- edge_cases_or_failure_modes: If templates are missing, setup and stop hook fall back to inline prompt text (`setup-pr-loop.sh` lines 740-755, 769-799, 803-847, 893-940; `pr-loop-stop-hook.sh` lines 1258-1261). Comment detection is sentinel-based: only `*No comments found.*` sets `COMMENT_COUNT=0`; any other fetched content selects the has-comments workflow (`setup-pr-loop.sh` lines 723-729). Template text is advisory, so enforcement relies on stop hooks and validators rather than template content alone.
- validation_or_tests: `tests/test-pr-loop-system.sh` references `goal-tracker-initial.md` directly and verifies setup creates a goal tracker with expected structure. `tests/run-all-tests.sh` includes `test-pr-loop.sh` and `test-pr-loop-system.sh` at lines 53-54, plus PR-loop robustness at line 72. Template existence and rendering are broadly covered by `test-template-references.sh`, `test-templates-comprehensive.sh`, and `test-template-loader.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level executable specification and regression gate for the Humanize plugin, including RLCR hooks, PR-loop behavior, template rendering, validators, and robustness suites.
- algorithmic_behavior: Defines ordered `TEST_SUITES` at lines 32-73, runs each existing script, captures output and exit code, strips ANSI codes, extracts `Passed:` and `Failed:` counts, aggregates totals, and exits nonzero if any suite exits nonzero or reports failures. It special-cases zsh execution for `test-zsh-monitor-safety.sh` through `ZSH_TESTS` at lines 75-78 and branch logic at lines 88-118.
- inputs_outputs_state: Input is the repository test tree under `tests/`; no CLI arguments are required. Output is a terminal summary with per-suite pass/fail/skips, total passed/failed counts, failed suite names, and process exit status. State is held in shell arrays/counters: `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` at lines 28-30.
- gates_or_invariants: Missing test files are skipped rather than failing lines 83-86. zsh-required tests are skipped if `zsh` is unavailable lines 106-114. Failure gate is strict on both shell exit and parsed failed count lines 130-135. It intentionally uses `set +e` around suite execution so one failing suite does not abort the aggregate run lines 104-119.
- dependencies_and_callers: Depends on all listed test scripts being executable by path. Includes core template and hook tests first (`test-template-loader.sh`, validators, plan hooks), PR-loop tests at lines 53-54, and robustness tests at lines 56-72. Likely called manually or by CI as the repository-wide validation entrypoint.
- edge_cases_or_failure_modes: Pass/fail extraction assumes test output includes `Passed:` and `Failed:`; suites without that format contribute zero even if they pass. ANSI stripping is limited to standard CSI color codes line 123. Missing suite files reduce coverage silently with a yellow skip. zsh absence also skips coverage.
- validation_or_tests: The file is itself the aggregate validation driver. I did not execute it because the task is read-only research and the full suite may create temp repos/files; inspection confirms it includes the assigned robustness suite at line 69.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-074 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared template resolution and rendering library for hook/user-facing block messages and PR/RLCR prompts.
- algorithmic_behavior: `get_template_dir` maps a caller script directory to repository `prompt-template` by walking two levels up lines 26-31. `load_template` returns file contents or warns and returns empty for missing templates lines 36-47. `render_template` performs single-pass `{{VAR}}` replacement using `awk` and environment variables prefixed with `TMPL_VAR_` lines 58-132. `load_and_render` composes loading plus rendering lines 136-147. `append_template` concatenates a loaded template to base content lines 151-161. `load_and_render_safe` uses rendered fallback text if loading or rendering yields empty content lines 170-203. `validate_template_dir` requires the template root plus `block`, `codex`, and `claude` subdirectories lines 208-222.
- inputs_outputs_state: Inputs are a template root, relative template name, and `VAR=value` render bindings. Outputs are rendered text on stdout and warnings/errors on stderr. It mutates no repository state. `render_template` stores bindings only in subprocess environment entries prefixed with `TMPL_VAR_`.
- gates_or_invariants: Rendering is deliberately single-pass, so placeholders contained inside variable values are not recursively expanded lines 54-57 and 117-118. Missing variables remain as original placeholders lines 119-122. Missing templates degrade to empty or fallback depending on caller. `validate_template_dir` enforces minimum template tree shape.
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh` lines 139-148, which initializes `TEMPLATE_DIR` and warns on validation failure. Used across read/write/edit/bash/stop hooks, plan-file validator, setup scripts, and tests. PR-loop setup calls `load_and_render_safe` for all assigned PR-loop templates.
- edge_cases_or_failure_modes: Variable names are not validated against the documented uppercase-only syntax; any text between braces can be looked up if the caller passes a matching environment binding. Newlines in variable values are passed through environment variables and printed by awk. `env "${env_vars[@]}"` can fail if extremely large values exceed environment limits. `load_and_render_safe` treats an intentionally empty template as missing/empty and can return fallback.
- validation_or_tests: `tests/test-template-loader.sh` covers path resolution, existing/missing load, single/multiple/multiline substitutions, unreplaced placeholders, safe fallback, and directory validation lines 42-244. It also covers shell metacharacters, Windows paths, JSON-like content, multiline values, placeholder injection prevention, empty variables, Unicode, underscore and numeric variable names lines 257-639. `tests/robustness/test-template-error-robustness.sh` covers missing directories/templates, empty files, unclosed/nested placeholders, invalid variable names, template syntax in values, BOM, whitespace-only files, and special filenames lines 34-235.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: User-facing block template for RLCR read attempts against stale or non-current round prompt/summary files.
- algorithmic_behavior: The template tells the agent it tried to read `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md` while the current round is `{{CURRENT_ROUND}}`, lists the active current prompt and summary paths, and gives an explicit `cat {{FILE_PATH}}` escape if the historical file is intentionally needed lines 1-9.
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`. Output is a rendered markdown block written to stderr by the read validator. It changes no state directly.
- gates_or_invariants: The invariant is that ordinary reads of `round-N-prompt.md` and `round-N-summary.md` should target the current active loop round unless the file is allowlisted. The validator blocks wrong-round reads with exit 2 after rendering this template.
- dependencies_and_callers: Called from `hooks/loop-read-validator.sh` lines 138-150 after validating hook JSON, locating the active RLCR loop, parsing current round, extracting requested round, and confirming the path is not allowlisted. Uses `load_and_render_safe`, so missing template falls back to inline wrong-round text.
- edge_cases_or_failure_modes: No active loop means no block (`loop-read-validator.sh` lines 80-85). Malformed state fails closed before rendering lines 94-98. Files without extractable round numbers pass through lines 105-108. Allowlisted historical files bypass the block line 138. The template's final `cat` instruction is informational; actual hook enforcement still blocks the current Read tool call.
- validation_or_tests: `tests/test-template-loader.sh` validates similar wrong-round template rendering through `block/wrong-round-number.md` at lines 151-163. `tests/test-templates-comprehensive.sh` also checks wrong-round rendered content. `tests/test-allowlist-validators.sh` verifies read validator allows allowlisted historical summaries and blocks non-allowlisted wrong-round summaries, including cases around lines 247-283.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-134 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for hook validators and stop hooks under malformed input, protected file access, state parsing anomalies, concurrent invocations, and large/hostile JSON.
- algorithmic_behavior: Sets strict shell mode, sources `loop-common.sh`, `portable-timeout.sh`, and test helpers lines 18-24, then creates an isolated test directory. It runs grouped tests for edit validation, plan-file validation, state parsing, command injection prevention, concurrent access, stop-hook state parsing, and JSON edge cases. Each test invokes actual hook scripts with synthetic JSON and asserts exit code/output via `pass`/`fail`.
- inputs_outputs_state: Inputs are generated JSON hook payloads, temporary `.humanize/rlcr` state trees, temporary git repos for plan/stop-hook tests, and a mock `codex` binary for active-loop stop-hook behavior. Outputs are test pass/fail lines plus final `print_test_summary` and exit status at lines 671-672. Temporary state is created under `$TEST_DIR`.
- gates_or_invariants: Edit validator must allow normal edits, block `state.md`, handle malformed/missing fields without crashing, allow paths outside `.humanize` for sandbox delegation, and block traversal to protected state files lines 40-155. Plan validator must allow valid plan edits, pass non-plan edits, and tolerate empty JSON lines 165-243. Bash validator must block `state.md` and post-round-zero `goal-tracker.md` writes but pass unrelated commands to the sandbox lines 317-382. Active RLCR stop hook with complete state must block exit with a block decision lines 559-613; missing/corrupt/incomplete state must end or pass gracefully lines 461-557.
- dependencies_and_callers: Depends on `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, git, jq, and test helpers. Included in aggregate runner at `tests/run-all-tests.sh` line 69.
- edge_cases_or_failure_modes: Explicitly covers malformed JSON, missing `file_path`, path traversal to state, state files with whitespace/special values/missing closing delimiter, newline strings, binary-looking content, concurrent read-validator invocations, no active state, corrupted state, missing required state fields, large 100 KB JSON, escaped nulls, and deeply nested JSON. Some assertions accept multiple graceful outcomes when behavior is intentionally tolerant, such as malformed state parsing returning either default or parsed round lines 301-307.
- validation_or_tests: This file is a validation suite. It self-validates the hook system through 21 named tests and returns the aggregate result. I did not run it because this assignment requested research only and the filesystem profile is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above; item IDs were checked against the assignment list without adding duplicate ID mentions here.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`