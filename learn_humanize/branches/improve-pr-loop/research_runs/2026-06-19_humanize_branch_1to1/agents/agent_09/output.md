# agent_09 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`
- inspection_scope: read-only branch export; no files modified; tests were inspected, not executed.

## Item Evidence

### IMPROVE_PR_LOOP-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for RLCR and PR-loop hooks. `hooks/lib/loop-common.sh` provides hook input validation, loop state parsing, file/path gates, cancel authorization, PR-loop bot/state helpers, review-result extraction, and goal-tracker updates. `hooks/lib/template-loader.sh` provides prompt template lookup/render/fallback plumbing used by validators and stop hooks.
- algorithmic_behavior: `loop-common.sh` validates JSON hook input with null-byte, UTF-8, `jq`, and required `tool_name` checks at `hooks/lib/loop-common.sh:59-96`, plus required tool input fields at `hooks/lib/loop-common.sh:101-114` and deep nesting detection at `hooks/lib/loop-common.sh:119-133`. It finds only the newest active RLCR loop directory and ignores older "zombie" loops at `hooks/lib/loop-common.sh:156-174`; parses state in tolerant and strict modes at `hooks/lib/loop-common.sh:209-330`; scans Codex review logs for `[P?]` issue markers in the first ten chars of a line and writes extracted audit output at `hooks/lib/loop-common.sh:349-392`; blocks or classifies round files, state files, finalize files, `.humanize` git-add operations, and Bash file modifications at `hooks/lib/loop-common.sh:394-527` and `hooks/lib/loop-common.sh:930-1096`. PR-loop-specific helpers map bot names, locate active PR-loop state, classify read-only PR-loop files, validate `round-N-pr-resolve.md` against the current round, and update PR goal trackers at `hooks/lib/loop-common.sh:722-887` and `hooks/lib/loop-common.sh:1151-1315`.
- inputs_outputs_state: Inputs include Claude hook JSON from validators, `.humanize/rlcr/*/state.md` or `finalize-state.md`, `.humanize/pr-loop/*/state.md`, command strings lowercased by callers, Codex review logs under `$CACHE_DIR`, template files under `prompt-template`, and goal tracker markdown. Outputs are shell return codes, stderr block messages, rendered prompt blocks, exported `STATE_*`/`PR_*` shell variables, renamed loop state files via `end_loop` at `hooks/lib/loop-common.sh:1119-1145`, review-result files via `detect_review_issues`, and rewritten PR goal tracker markdown via a temp-file replace at `hooks/lib/loop-common.sh:1253-1313`.
- gates_or_invariants: Strict state parsing rejects missing frontmatter, missing `current_round`, `max_iterations`, `review_started`, `base_branch`, nonnumeric numeric fields, and nonboolean `review_started` at `hooks/lib/loop-common.sh:263-327`. `is_cancel_authorized` requires `.cancel-requested`, rejects command substitution, backticks, newlines, shell operators, remaining `$`, mixed quotes, extra args, wrong source/destination, and symlinked source files at `hooks/lib/loop-common.sh:533-709`. Template rendering is intentionally single-pass so injected `{{VAR}}` text in a value is not rescanned, per `hooks/lib/template-loader.sh:50-132`. `git_adds_humanize` blocks direct `.humanize` staging, force broad adds, and broad adds when `.humanize` is unignored at `hooks/lib/loop-common.sh:930-1026`. `validate_pr_resolve_round` blocks stale PR resolution files by comparing the target round to active state at `hooks/lib/loop-common.sh:837-887`.
- dependencies_and_callers: Depends on `jq`, `sed`, `grep`, `awk`, `tr`, optional `iconv`, and sometimes `git`. `loop-common.sh` sources `template-loader.sh` and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:139-149`. Main callers include `hooks/loop-read-validator.sh:17-60`, `hooks/loop-write-validator.sh:17-67`, `hooks/loop-edit-validator.sh:16-50`, `hooks/loop-bash-validator.sh:16-118`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh:51-58`. `template-loader.sh` is also sourced by setup scripts such as `scripts/setup-pr-loop.sh:33-42`.
- edge_cases_or_failure_modes: Bash cannot reliably retain embedded null bytes even though `validate_hook_input` tries to reject them; the robustness test documents that Bash strips null bytes in `tests/robustness/test-hook-input-robustness.sh:219-235`. `find_active_loop` intentionally ignores older active-looking directories, which prevents stale resurrection but means a newer malformed/partial directory hides older valid state. Template docs say variable names are uppercase-only at `hooks/lib/template-loader.sh:7-13`, but the renderer scans any text between braces and only replaces names provided through env variables. `git_adds_humanize` relies on current working directory for `.humanize` and `git check-ignore`, so callers must run in the project context. `command_modifies_file` is regex-based and broad by design; complex shell syntax can be overblocked or underdetected outside covered patterns.
- validation_or_tests: Direct coverage includes `tests/test-template-loader.sh` for template path/loading/rendering/fallback, `tests/test-template-references.sh` for missing referenced templates, `tests/robustness/test-hook-input-robustness.sh` for JSON/deep nesting/UTF-8 behavior, `tests/robustness/test-cancel-security-robustness.sh` for cancel authorization, `tests/robustness/test-state-transition-robustness.sh` and `tests/robustness/test-state-file-robustness.sh` for strict state parsing, and `tests/test-pr-loop-system.sh:513-538`, `tests/test-pr-loop-system.sh:681-760`, `tests/test-pr-loop-system.sh:1180-1276` for PR goal tracker update/idempotency/repair behavior.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-039 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Runtime compatibility shim that gives setup scripts, stop hooks, PR polling, and reviewer-status scripts a single `run_with_timeout` API across macOS and Linux.
- algorithmic_behavior: `detect_timeout_impl` chooses timeout support in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` at `scripts/portable-timeout.sh:10-27`. `TIMEOUT_IMPL` is fixed when the file is sourced at `scripts/portable-timeout.sh:29`. `run_with_timeout <seconds> <command> [args...]` dispatches to the chosen implementation at `scripts/portable-timeout.sh:33-71`; GNU paths execute the command array directly, Python fallback runs `subprocess.run(sys.argv[1:], timeout=timeout_secs)` and maps `TimeoutExpired` to exit code `124`, and `none` warns then runs unbounded.
- inputs_outputs_state: Inputs are timeout seconds plus a command argv vector. Outputs are the wrapped command’s stdout/stderr and exit status; timeout exit is intended to match GNU `124` for Python fallback. State is the exported `TIMEOUT_IMPL` variable at `scripts/portable-timeout.sh:73-76`; the function itself is not exported and is expected to be used by sourcing scripts.
- gates_or_invariants: Detection refuses non-GNU `timeout` by checking `timeout --version` at `scripts/portable-timeout.sh:13-19`, which avoids assuming BSD/macOS behavior. The wrapper preserves argument boundaries through Bash arrays for native timeout paths at `scripts/portable-timeout.sh:36-45` and through `sys.argv[1:]` for Python at `scripts/portable-timeout.sh:49-61`.
- dependencies_and_callers: Callers source this file directly, including `scripts/setup-pr-loop.sh:33-35`, `scripts/check-pr-reviewer-status.sh:38-40`, `scripts/check-bot-reactions.sh`, `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh:55-58`, and `hooks/loop-plan-file-validator.sh`. It wraps `git`, `gh`, and `codex` operations in the PR/RLCR loop paths.
- edge_cases_or_failure_modes: If no timeout implementation exists, core operations run without a time bound and only emit a warning at `scripts/portable-timeout.sh:64-68`. Python fallback times out the immediate subprocess, not necessarily an entire process group; shell commands that spawn detached children may survive. A zero-second timeout is intentionally implementation-dependent per the robustness spec. If a command is empty or nonexistent, behavior is whatever the selected implementation returns.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` verifies detection and allowed implementation names at lines `31-49`, quick command success at `51-60`, timeout exit `124` at `62-77`, argument/exit-code preservation at `79-100`, pipeline/subshell behavior at `102-110` and `249-257`, large output and rapid cycles at `157-186`, missing/empty command handling at `188-209`, special chars at `211-219`, and `TIMEOUT_IMPL` availability at `259-272`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-069 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that prevents hook validators from referencing missing prompt templates, which would otherwise produce empty or degraded block messages during safety gates.
- algorithmic_behavior: The script defines project/template roots at `tests/test-template-references.sh:14-17`, scans a fixed set of hook scripts including `hooks/lib/loop-common.sh` at `tests/test-template-references.sh:56-64`, and extracts template paths from `load_template`, `load_and_render`, and `load_and_render_safe` calls that include `"$TEMPLATE_DIR"` on the same line at `tests/test-template-references.sh:83-110`. It then enumerates all `prompt-template/**/*.md` files and reports whether they are directly referenced at `tests/test-template-references.sh:121-142`. It separately validates a curated common-template list at `tests/test-template-references.sh:149-167` and checks critical validators use `load_and_render_safe` instead of unsafe `load_and_render` at `tests/test-template-references.sh:172-201`.
- inputs_outputs_state: Inputs are shell hook source files under `hooks/` and markdown templates under `prompt-template/`. Outputs are colored pass/fail/warn lines, counters `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, `MISSING_TEMPLATES`, and a process exit code: `0` only when `TESTS_FAILED` is zero at `tests/test-template-references.sh:221-230`.
- gates_or_invariants: Missing referenced templates are fatal, because `fail` increments `TESTS_FAILED` at `tests/test-template-references.sh:34-37` and the summary exits nonzero. Unreferenced templates are warnings only at `tests/test-template-references.sh:134-140`. Critical read/write/edit validators must render through `load_and_render_safe` so a missing template still has a fallback message at `tests/test-template-references.sh:176-200`.
- dependencies_and_callers: Depends on `find`, `grep`, `sed`, Bash arrays, and the repository layout. It validates references from `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/lib/loop-common.sh`. It is itself part of the test suite and appears in `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The extractor only catches direct same-line calls with literal quoted template paths after `"$TEMPLATE_DIR"`; dynamically built paths or multi-line invocations can be missed. The curated `COMMON_TEMPLATES` list at `tests/test-template-references.sh:152-159` can drift behind new common message functions, although Section 1 still catches direct references. The script uses `set -uo pipefail` rather than `set -e`, allowing all failures to be aggregated before final exit.
- validation_or_tests: This file is a validation harness. It directly validates `prompt-template/block/todos-file-access.md` in the common list at `tests/test-template-references.sh:152-163` and validates safe rendering usage for critical validators at `tests/test-template-references.sh:172-201`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-099 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Static block-message contract for the RLCR file-access gate that forbids `round-*-todos.md` files and routes agents to native `TodoWrite` state tracking.
- algorithmic_behavior: The template has no variables. Its semantic transition is direct: on attempted read/write/edit/Bash access to `round-*-todos.md`, tell the agent not to create or access those files and to use native TodoWrite instead at `prompt-template/block/todos-file-access.md:1-8`.
- inputs_outputs_state: Input is implicit selection by `todos_blocked_message`; output is markdown text emitted to stderr by validators or embedded as a block reason. It does not read or mutate state itself, but it protects loop state by preventing file-based todo surfaces from becoming the active coordination mechanism.
- gates_or_invariants: The invariant is "Do NOT create or access `round-*-todos.md` files" at `prompt-template/block/todos-file-access.md:3`; the required replacement channel is native TodoWrite at `prompt-template/block/todos-file-access.md:5-8`. `loop-common.sh` supplies a fallback with the same policy if the template is missing at `hooks/lib/loop-common.sh:443-452`.
- dependencies_and_callers: Loaded by `todos_blocked_message` in `hooks/lib/loop-common.sh:445-452`. Read/write/edit validators call that helper for direct todo-file paths at `hooks/loop-read-validator.sh:55-62`, `hooks/loop-write-validator.sh:55-62`, and `hooks/loop-edit-validator.sh:38-45`. Bash validator calls it for command-level todo modifications at `hooks/loop-bash-validator.sh:385-391`.
- edge_cases_or_failure_modes: Some legacy allowlisted todo files are exempted by `is_allowlisted_file` for active-loop paths at `hooks/lib/loop-common.sh:420-441`, so this block is not universal for every historical `round-N-todos.md` path. Because the template has no placeholders, render failures are mainly missing-file or template directory issues; `load_and_render_safe` degrades to fallback text.
- validation_or_tests: `tests/test-template-references.sh` asserts this file exists in the common template set at `tests/test-template-references.sh:152-163`. TodoWrite transcript behavior is separately validated by `tests/test-todo-checker.sh`, which treats native TodoWrite calls as the authoritative task state.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-129 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Security robustness spec for the cancel authorization gate in `is_cancel_authorized`, especially protection against command injection, path bypasses, quote tricks, variable expansion, and symlink attacks when renaming active loop state to `cancel-state.md`.
- algorithmic_behavior: The test creates a mock loop directory with `state.md` at `tests/robustness/test-cancel-security-robustness.sh:27-31`, sources `loop-common.sh` and test helpers at `tests/robustness/test-cancel-security-robustness.sh:15-18`, then calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` directly for positive and negative command strings. Positive cases require `.cancel-requested` and accept exact `mv state.md cancel-state.md`, `finalize-state.md`, single quotes, and unquoted paths at `tests/robustness/test-cancel-security-robustness.sh:39-89`.
- inputs_outputs_state: Inputs are temporary filesystem state, the `.cancel-requested` signal file, optional `state.md`/`finalize-state.md` symlinks, and lowercased shell command strings. Outputs are test pass/fail counters via `test-helpers.sh` and final exit through `print_test_summary` at `tests/robustness/test-cancel-security-robustness.sh:407-412`. The suite mutates only its temp test directories: creates/removes signal files, symlinks, and mock state files.
- gates_or_invariants: Negative cases assert rejection without a signal file, command substitution, backticks, `;`, `&&`, `||`, pipes, wrong source/destination, extra args, newline injection, remaining variables like `${HOME}`/`${IFS}`, non-`mv` commands, mixed quotes, and multiple trailing spaces at `tests/robustness/test-cancel-security-robustness.sh:99-315`. Filesystem hardening checks reject nonstandard source filenames and symlinked `state.md`/`finalize-state.md` at `tests/robustness/test-cancel-security-robustness.sh:317-367`. Regression cases ensure loop directories containing the substring `finalize` do not bypass exact source-file symlink checks at `tests/robustness/test-cancel-security-robustness.sh:369-405`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` implementation at `hooks/lib/loop-common.sh:533-709`, `tests/test-helpers.sh`, POSIX-ish filesystem operations, and symlink support. It validates behavior used by `hooks/loop-bash-validator.sh`, where authorized cancel `mv` commands are allowed while other state-file writes are blocked at `hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:286-303`, and `hooks/loop-bash-validator.sh:309-327`.
- edge_cases_or_failure_modes: The test validates authorized/not-authorized as boolean behavior but generally does not assert the exact nonzero return code. Commands are lowercased before validation, matching hook behavior, so case-sensitive filesystem edge cases outside the tested temp paths are not explored. The symlink message in Test 23 includes `$?` after `! is_cancel_authorized`, which reports the negated `if` status rather than the helper’s raw code, but the pass/fail condition still verifies rejection.
- validation_or_tests: This file is the executable validation. It covers 26 direct cases and exits with the helper summary at `tests/robustness/test-cancel-security-robustness.sh:411-412`. Broader cancel-flow coverage also exists in `tests/test-cancel-signal-file.sh`, while Bash validator integration coverage exercises the same helper through hook JSON.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections present above, one per assigned row.
- missing_items: none.
- duplicate_items: none.
- final_worker_status: `complete`