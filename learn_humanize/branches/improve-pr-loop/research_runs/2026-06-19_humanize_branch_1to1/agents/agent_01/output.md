# agent_01 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`
- notes: repository export is read-only and has no usable `.git` metadata in this sandbox; I treated the scheduler-provided source commit/tree as authoritative. No files were modified.

## Item Evidence

### IMPROVE_PR_LOOP-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: The repository root is a Claude/Humanize plugin implementation for two loop state machines: an RLCR local review loop and a PR review loop. For this branch’s core algorithm subset, the root coordinates slash commands, setup/cancel scripts, hook registration, validators, prompt/block templates, helper libraries, GitHub polling scripts, and a broad shell test suite. The PR loop’s main lifecycle is: start command -> setup script creates `.humanize/pr-loop/<timestamp>/` state and prompts -> Claude/user fixes and pushes -> Stop hook polls bot reviews -> local Codex validates bot feedback -> state either advances to next round, blocks with feedback, or exits to terminal state.
- algorithmic_behavior: The root’s executable surfaces are organized by role. `commands/` exposes slash-command entry points, with `commands/start-pr-loop.md` allowing only `${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh` and documenting the Stop hook cycle. `scripts/setup-pr-loop.sh` parses bot flags and loop parameters, enforces mutual exclusion with RLCR/PR loops, validates git/GitHub/Codex prerequisites, classifies startup review cases, creates state, goal tracker, and initial round prompts, and injects critical completion requirements. `hooks/hooks.json` registers `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, and `loop-bash-validator.sh` as PreToolUse validators, plus both RLCR and PR Stop hooks with 7200-second timeouts. `hooks/pr-loop-stop-hook.sh` is the PR loop gatekeeper. `scripts/poll-pr-reviews.sh`, `scripts/fetch-pr-comments.sh`, `scripts/check-bot-reactions.sh`, and `scripts/check-pr-reviewer-status.sh` normalize GitHub comments, reviews, reactions, and reviewer-case detection. `prompt-template/` supplies prompt contracts and block messages loaded by `hooks/lib/template-loader.sh`. `tests/` and `tests/robustness/` provide executable shell specifications for state transitions, validators, PR API behavior, fixtures, monitor behavior, and error paths.
- inputs_outputs_state: Primary inputs are slash-command arguments, Claude hook JSON, Git status, GitHub CLI PR/review/comment/reaction responses, local `.humanize/rlcr` and `.humanize/pr-loop` state directories, prompt templates, and configured bots. Primary outputs are JSON hook decisions (`decision: block` with `reason`/`systemMessage`), state files renamed to terminal files such as `approve-state.md`, `closed-state.md`, `merged-state.md`, `usage-limit-state.md`, generated round artifacts (`round-N-pr-comment.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, `round-N-codex-prompt.md`), and user-facing prompts. State is held in YAML frontmatter fields such as `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, `startup_case`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id`.
- gates_or_invariants: Root-level invariants include one active loop at a time, protected system-managed files under `.humanize/*`, current-round-only resolution summaries, clean and pushed git changes before exit, no stale trigger reuse after new commits or force pushes, per-bot timeout tracking, configured-bot polling even when bots were previously approved, and template-backed block messages. Hook registration in `hooks/hooks.json` makes these gates mandatory on tool use and Stop events. Setup and validators also maintain the split between user-writable files such as `round-N-pr-resolve.md` and system-managed state/comment/prompt/check artifacts.
- dependencies_and_callers: `commands/start-pr-loop.md` calls `scripts/setup-pr-loop.sh`, and `commands/cancel-pr-loop.md` calls `scripts/cancel-pr-loop.sh`. Setup and Stop hooks source `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `scripts/portable-timeout.sh`. The PR Stop hook calls `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, and `scripts/check-pr-reviewer-status.sh`, and invokes `codex exec`. Validators and Stop hooks load block templates from `prompt-template/block/**` and PR-loop templates from `prompt-template/pr-loop/**`. Tests source helper scripts and use mocked `gh`, `git`, and fixture JSON under `tests/fixtures/`.
- edge_cases_or_failure_modes: The root tree explicitly handles missing git repo/commit, missing `gh`, missing Codex, existing active loop conflicts, fork PR base repository resolution, missing resolution summary, dirty or unpushed work, closed/merged PRs, force-pushed history, stale trigger comments, missing Claude eyes reactions, polling API failure/retry, approval-only PR reviews with empty bodies, bot usage limits, timed-out bots, wrong-round summary writes, monitor display of empty bot lists, and template fallback when template loading fails. A structural edge case in this export is that git commands could not be used because the read-only branch export has no `.git` directory.
- validation_or_tests: The root includes `tests/run-all-tests.sh`, focused PR loop tests (`tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop.sh`), validator tests, template reference/comprehensive tests, and robustness tests for hook input, path validation, PR loop API behavior, state transitions, timeouts, and setup scripts. GitHub Actions under `.github/workflows/` cover shell syntax, templates, plan-file tests, and version checks. I did not execute the full suite because the task is research-only and the sandbox is read-only, but inspected the assigned executable specification directly.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: This is the PR loop Stop hook and the central PR-loop state-machine gate. It intercepts Claude’s exit attempt, validates that the latest work is ready for remote bot review, polls GitHub for bot responses, asks local Codex to classify bot feedback, mutates PR loop state, and either permits loop completion or blocks with next-round feedback.
- algorithmic_behavior: The hook reads hook stdin but primarily operates from `CLAUDE_PROJECT_DIR`/`pwd`, finds the newest active `.humanize/pr-loop` directory, and exits inertly if no active PR loop/state exists. It parses YAML frontmatter state into scalar variables and arrays in `parse_pr_loop_state()` at lines 87-174. It resolves the correct PR base repository for forks before PR operations at lines 196-237. It exits the loop for `MERGED` or `CLOSED` PR states by renaming `state.md` at lines 238-255. It requires a current-round resolution summary at lines 257-276. It enforces clean worktree and pushed commits at lines 278-360. It detects force pushes by checking whether the stored commit SHA is still an ancestor of current HEAD, clears trigger state, and blocks for a fresh trigger at lines 363-423. It caps rounds with `max_iterations` and exits when `active_bots` is already empty at lines 425-447. It detects the most recent trigger comment by the current GitHub user, with pagination and optional `latest_commit_at` filtering, at lines 450-525, then refreshes stale commit timestamps and trigger fields at lines 533-606. It computes whether a trigger is required from round/startup-case/new-commit state at lines 608-643. It handles Codex thumbsup reactions as approval at lines 645-718 and again during polling at lines 984-1039. It blocks when a required trigger is missing at lines 720-746. It verifies Claude eyes reaction on the trigger comment when Claude is configured and trigger-required at lines 748-799. It polls configured bots, not only active bots, from lines 801-1062, deduplicates comments by ID, and anchors timeouts to the trigger timestamp. It auto-removes timed-out bots and blocks if no usable reviews arrive at lines 1064-1178. It writes grouped bot comments to `round-N-pr-comment.md` at lines 1181-1230. It builds and runs a local Codex validation prompt, requiring exact terminal markers, at lines 1232-1357. It interprets `APPROVE`, `WAITING_FOR_BOTS`, `USAGE_LIMIT_HIT`, and issue cases at lines 1359-1413. It extracts per-bot statuses, re-adds previously approved bots if new issues appear, updates the goal tracker, rewrites state for the next round, and emits `round-N-pr-feedback.md` as a blocking Stop-hook reason at lines 1415-1636.
- inputs_outputs_state: Inputs include `.humanize/pr-loop/<timestamp>/state.md`, `round-current-pr-resolve.md`, GitHub PR state/commits/comments/reviews/reactions, git status/branch/head/upstream data, configured prompt templates, `codex` CLI, `jq`, helper scripts, and optional `.cancel-requested`. Outputs include terminal state-file renames (`merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, `usage-limit-state.md`), state rewrites with incremented `current_round` and updated bot/commit/trigger fields, generated comment/check/feedback/prompt files, goal tracker updates, stderr diagnostics, and JSON hook block decisions. State transitions are mostly represented by moving `state.md` out of the active name or rewriting YAML frontmatter atomically via temp file plus `mv`.
- gates_or_invariants: Key gates are: valid `pr_number` and numeric `current_round` at lines 185-194; PR must not be closed/merged unless terminalizing at lines 243-255; resolution summary must exist before exit at lines 261-276; non-`.humanize` git status must be clean at lines 282-310; local commits must be pushed at lines 312-360; force-pushed or newly pushed commits clear trigger fields and require a fresh bot mention at lines 363-423 and 533-564; later rounds always require a trigger at lines 619-627; trigger comments must be from the current GitHub user and after latest commit when applicable at lines 494-511; Claude eyes reaction is mandatory for trigger-required Claude runs at lines 748-799; all configured bots must respond or time out before Codex evaluation at lines 880-948; comments are deduped by ID at lines 1042-1058; Codex output must be non-empty and end with one of the recognized markers at lines 1332-1405; bot state is recomputed across all configured bots to allow re-adds at lines 1431-1494.
- dependencies_and_callers: Registered as a Stop hook in `hooks/hooks.json` lines 52-64. It sources `hooks/lib/loop-common.sh` for active-loop discovery, bot-author mapping, YAML-list rendering, mention-string building, template loading, and goal-tracker updating. It sources `scripts/portable-timeout.sh` for bounded git/GitHub/Codex operations. It calls `scripts/poll-pr-reviews.sh` at line 811/961, `scripts/check-bot-reactions.sh` at lines 675, 778, and 998, and `scripts/check-pr-reviewer-status.sh` at line 1547. It loads prompt/block templates via `load_and_render_safe`, including `block/unpushed-commits.md`, `block/force-push-detected.md`, `block/no-trigger-comment.md`, `block/claude-eyes-timeout.md`, and `pr-loop/codex-goal-tracker-update.md`.
- edge_cases_or_failure_modes: Notable cases include missing active loop or state causing no-op exit; invalid state fields causing non-blocking exit; failure to resolve base repo falling back to current repo; closed/merged PRs terminalizing loop; missing resolve file blocking before expensive polling; git status failure blocking; no upstream branch fallback to origin branch or PR head SHA; unavailable PR head fail-closed as unpushed; unreachable stored commit treated as force push; local clock skew mitigated by refreshing PR commit timestamp; stale triggers cleared after new commits; missing current GitHub user only warns but may make trigger detection fail; startup cases 1-3 can avoid initial trigger, while 4-5 and later rounds require one; historical comment processing uses epoch timestamp but anchors timeout to now to avoid instant timeout; API polling failures retry instead of aborting; cancel signal exits poll loop; empty review bodies are preserved upstream as state placeholders; Codex absence/failure/empty output blocks; usage limits terminalize without blocking; timed-out bots can be removed and can cause approval if no active bots remain.
- validation_or_tests: Directly covered by `tests/test-pr-loop-hooks.sh`: state YAML format and protected files at lines 26-210; comment dedup/YAML parsing/status extraction/re-add/trigger/marker detection at lines 271-467; trigger user filtering, trigger refresh, missing trigger, round-0 timestamp priority, timeout anchoring, configured bot state, and round-file naming at lines 538-754; E2E missing resolve, trigger detection, pagination, and trigger-priority runtime cases at lines 766-1148; approval-only and fixture-backed fetch/poll tests at lines 1160-1419; wrong-round resolution validation and monitor active/configured bot display at lines 1431-1621. Additional related coverage exists in `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-system.sh`, and robustness PR-loop/state/timeout/API tests.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: This is an executable specification for PR-loop hook behavior. It validates the PR-loop Stop hook’s state-machine assumptions indirectly and directly through unit-style shell snippets, mocked runtime executions, validator checks, fixture-backed GitHub polling, and monitor behavior.
- algorithmic_behavior: The file defines `run_hook_tests()` and executes grouped test functions in sequence. The first group validates protected PR-loop files and allowed resolution summaries: YAML list storage for `active_bots`, blocking writes to `state.md` and `round-*-pr-comment.md`, allowing `round-current-pr-resolve.md`, and blocking Bash redirection into state files at lines 26-210. Sorting and gate-keeper groups validate timestamp ordering, human-before-bot ordering, comment deduplication, configured-bot YAML parsing, Codex status extraction, previously approved bot re-addition, trigger detection, and final marker parsing at lines 222-467. Integration tests validate trigger filtering by current user, refreshing newer triggers, missing trigger blocking, round-0 trigger priority/fallback, trigger-anchored timeouts, separate configured-bot tracking, and consistent next-round artifact naming at lines 538-754. End-to-end tests execute `hooks/pr-loop-stop-hook.sh` with mocked `gh` and `git` binaries to prove missing resolve blocking, trigger detection/persistence, paginated trigger detection, and round-0 last-trigger priority at lines 766-1148. Approval-only and fixture-backed tests verify that empty-body PR reviews become state placeholders and that fetch/poll scripts include and filter issue comments, review comments, and PR reviews correctly at lines 1160-1419. Final groups validate wrong-round `pr-resolve` write/edit blocking and monitor display for YAML `active_bots`/`configured_bots` at lines 1431-1621.
- inputs_outputs_state: Inputs are temporary test directories, synthetic `.humanize/pr-loop/<timestamp>/state.md` files, synthetic hook JSON, mocked `gh`/`git` scripts, test fixture JSON, generated mock PATH directories, helper functions such as `pass`, `fail`, and `init_test_git_repo` from the broader test harness, and project scripts/hooks. Outputs are test pass/fail calls and temporary generated state/output files. The tests assert hook exit codes, grep-able block text, JSON validity, generated comment files, state-field behavior, and monitor text.
- gates_or_invariants: The test file codifies invariants that PR-loop state uses YAML lists; system state/comment/prompt files are protected; only current-round resolution summaries are writable; trigger comments must belong to the current user; newer trigger timestamps replace older ones; later rounds require triggers; round 0 prefers `last_trigger_at` over `started_at`; per-bot timeouts are trigger-anchored; configured bots remain distinct from active bots; all round artifacts use `NEXT_ROUND`; approval-only reviews are not dropped; `--after` filters exclude older comments; and monitor output handles nonempty and empty bot lists.
- dependencies_and_callers: The file is intended to be sourced by the test harness (`source test-pr-loop-hooks.sh && run_hook_tests` in its header). It invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, and `tests/setup-monitor-test-env.sh`. It depends on `jq`, shell helpers from the test harness, and fixture JSON under `tests/fixtures/`.
- edge_cases_or_failure_modes: The test file itself contains a portability caveat: `test_bot_readd_logic()` uses `declare -A` at lines 360 and 363, while production code avoids associative arrays for macOS Bash 3.2. If the test suite is run under macOS’s default Bash 3.2, this specific unit may fail even though production uses dynamic-variable maps. Runtime mocks are intentionally partial and can terminate the Stop hook before full polling/Codex stages, so some E2E checks verify logs/early behavior rather than full end-to-end approval. Tests that create mock binaries and mutate PATH rely on cleanup by the outer harness. Fixture-backed tests require `jq` and executable helper scripts.
- validation_or_tests: This file is itself validation. It covers positive, negative, security, integration, E2E, fixture-backed, wrong-round, and monitor cases. It complements more focused tests such as `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-system.sh`, and robustness tests. I inspected the file directly but did not run it, per the read-only research requirement.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: This is a block-message template used by validators when an agent attempts to modify the loop’s backup `plan.md`. It is algorithmically relevant as a policy surface: it explains the invariant that backup plan files are read-only and preserve plan integrity.
- algorithmic_behavior: The template is static Markdown. Lines 1-7 state that `plan.md` in the loop directory is a backup of the original plan file, cannot be modified, preserves plan integrity, and should be read rather than edited. It does not parse inputs itself; the validator loads it via `load_and_render_safe` and emits it as the reason for blocking a prohibited Write/Edit/Bash operation.
- inputs_outputs_state: Inputs are implicit template variables from the loader context, though this particular template contains no placeholders. Output is Markdown block text returned to Claude/user as the hook block reason. It does not mutate state.
- gates_or_invariants: The invariant is that loop backup `plan.md` is immutable. The cross-reference pass found this template loaded by `hooks/loop-edit-validator.sh` and `hooks/loop-bash-validator.sh`; `hooks/loop-write-validator.sh` also references the same block template path. The gate prevents direct or shell-mediated modification of the backup plan, preserving the original plan against accidental or adversarial edits during loop execution.
- dependencies_and_callers: Called through `hooks/lib/template-loader.sh`’s safe rendering path from write/edit/bash validators. Related validation coverage exists in plan-file hook/validation/robustness tests and template-reference tests.
- edge_cases_or_failure_modes: Because the template has no placeholders, rendering failure risk is low. If the template is missing or template directory resolution fails, callers use inline fallback messages. A possible classification nuance is that this template is more of a validation contract than executable algorithm code, but it is part of the core state-machine guard surface because validators use it to enforce protected loop artifacts.
- validation_or_tests: Template references are checked by `tests/test-template-references.sh`/comprehensive template tests, and the write/edit/bash validator tests exercise protected-file behavior. The file itself has seven lines and no executable logic.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-121 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: This is the PR-loop critical completion contract inserted when setup starts from existing PR comments. It tells the working agent what must happen before Stop-hook polling can progress: commit and push changes, trigger remote bot re-review by PR comment, and write the current-round resolution summary.
- algorithmic_behavior: The template is static Markdown with placeholders. Lines 6-23 define the required completion sequence. It requires committing and pushing changes at lines 8-10, posting a PR comment using `{{PR_NUMBER}}` and `{{BOT_MENTION_STRING}}` at lines 12-13, writing the resolution summary to `{{RESOLVE_PATH}}` at lines 15-17, and including issues addressed, files modified, and tests added at lines 18-21. Line 23 ties the contract to the Stop hook: after the summary exists, the Stop hook polls for bot reviews.
- inputs_outputs_state: Inputs are template variables from `scripts/setup-pr-loop.sh`: PR number, bot mention string, and resolution path. Output is user-facing prompt text appended to the setup/initial task for PR loops with existing comments. It does not mutate state directly, but it establishes the preconditions that `hooks/pr-loop-stop-hook.sh` later enforces: pushed clean git state, a fresh trigger comment when needed, and an existing `round-N-pr-resolve.md`.
- gates_or_invariants: The template defines the human/agent-side completion gate. It aligns with Stop-hook gates for missing resolve file, uncommitted changes, unpushed commits, and missing trigger comments. It also reinforces that bot review polling should only occur after the agent has provided enough resolution evidence for the hook/Codex path to evaluate the next round.
- dependencies_and_callers: `scripts/setup-pr-loop.sh` loads this template via `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-has-comments.md"` in the existing-comments branch. It is sibling to `critical-requirements-no-comments.md`, `round-0-task-has-comments.md`, `round-0-task-no-comments.md`, `round-0-header.md`, and goal tracker templates. It is indirectly consumed by agents via the slash-command setup output.
- edge_cases_or_failure_modes: If `BOT_MENTION_STRING` is wrong or empty, the trigger command in the prompt would not wake the intended bots; setup builds it from selected bot flags via shared helper functions. If `RESOLVE_PATH` is stale or round-mismatched, validators/Stop hook block wrong/missing summary behavior. If the template is absent, setup uses a fallback critical message. As with the previous template, this is a prompt contract rather than executable code, but it is core to the loop’s transition protocol because the Stop hook assumes these actions have been taken.
- validation_or_tests: Template presence/reference is covered by template tests. The behavior specified here is validated by `tests/test-pr-loop-hooks.sh`: missing resolution summary blocks at lines 766-843, wrong-round resolution writes/edits block at lines 1431-1535, trigger detection/priority tests at lines 538-754 and 845-1148, and git cleanliness/push behavior is covered in related PR-loop and robustness tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections present above; each assigned item header appears once.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`