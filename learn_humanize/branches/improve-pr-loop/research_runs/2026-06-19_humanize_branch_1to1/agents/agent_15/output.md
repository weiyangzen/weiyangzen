# agent_15 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell-library directory for humanize monitor runtime helpers. Recursive inspection found one child, `scripts/lib/monitor-common.sh`, which is sourced by `scripts/humanize.sh` at `scripts/humanize.sh:6-10` and supplies reusable PR-loop/RLCR monitor primitives.
- algorithmic_behavior: `monitor-common.sh` provides cross-cutting monitor algorithms rather than a standalone executable. It defines ANSI/color helpers at `scripts/lib/monitor-common.sh:14-23`, cross-platform file sizing via Linux/macOS `stat` fallbacks at `scripts/lib/monitor-common.sh:32-35`, latest-session discovery by timestamped directory name at `scripts/lib/monitor-common.sh:40-63`, terminal scroll-region setup/restore at `scripts/lib/monitor-common.sh:71-92`, loop status coloring at `scripts/lib/monitor-common.sh:130-141`, state-file detection at `scripts/lib/monitor-common.sh:154-188`, YAML frontmatter extraction at `scripts/lib/monitor-common.sh:197-209`, timestamp/string formatting at `scripts/lib/monitor-common.sh:218-252`, PR-loop phase detection at `scripts/lib/monitor-common.sh:267-342`, phase display text at `scripts/lib/monitor-common.sh:346-381`, goal-tracker parsing at `scripts/lib/monitor-common.sh:390-463`, and PR goal-tracker issue parsing at `scripts/lib/monitor-common.sh:468-498`.
- inputs_outputs_state: Inputs are filesystem paths, session directories, state markdown files, goal trackers, status strings, and active bot strings. Outputs are shell stdout values such as `path|status`, phase names, formatted descriptions, numeric tracker summaries, and ANSI escape sequences. The only persistent state mutation observed is `get_pr_loop_phase`, which caches the last seen `round-N-pr-check.md` size in `/tmp/humanize-phase-<session>-<round>.size` at `scripts/lib/monitor-common.sh:297-307` to infer whether Codex output is still growing.
- gates_or_invariants: Latest-session selection only accepts basename pattern `YYYY-MM-DD_HH-MM-SS` (`scripts/lib/monitor-common.sh:54-60`). State detection prioritizes live `state.md` over terminal `*-state.md` files (`scripts/lib/monitor-common.sh:162-181`). PR phase detection treats `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` as terminal before checking file growth (`scripts/lib/monitor-common.sh:272-276`). Initial-review phase is specifically `current_round=0` and `startup_case=1`; otherwise an active `state.md` becomes `waiting_reviewer` (`scripts/lib/monitor-common.sh:321-338`).
- dependencies_and_callers: `scripts/humanize.sh` loads this library if present (`scripts/humanize.sh:6-10`) and calls helpers such as `monitor_find_latest_session`, `monitor_find_state_file`, `monitor_get_file_size`, `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` in monitor flows. Tests source it directly in `tests/test-pr-loop-system.sh`, for example phase detection at `tests/test-pr-loop-system.sh:256-309` and shared utility tests referenced around `tests/test-pr-loop-system.sh:1846-1848`.
- edge_cases_or_failure_modes: Missing loop/session directories return empty string or `unknown` instead of failing (`scripts/lib/monitor-common.sh:44-47`, `scripts/lib/monitor-common.sh:157-160`). Unknown statuses default yellow (`scripts/lib/monitor-common.sh:139`). YAML parsing is simple `grep`/`sed` over frontmatter and can only handle direct `key: value` lines (`scripts/lib/monitor-common.sh:203-208`). `get_pr_loop_phase` depends on sortable timestamp directory names and mtime/size heuristics; stale `/tmp` cache or clock skew can affect `codex_analyzing` detection (`scripts/lib/monitor-common.sh:301-316`). `parse_goal_tracker` uses section/table regexes and can undercount malformed markdown, but clamps negative active task counts to zero (`scripts/lib/monitor-common.sh:419-435`).
- validation_or_tests: `tests/test-pr-loop-system.sh` exercises phase states, state file lookup, latest-session lookup, file sizing, and tracker parsing. Repository search also shows robustness coverage for goal tracker parsing in `tests/robustness/test-goal-tracker-robustness.sh`, though that file primarily targets `scripts/humanize.sh`’s parser variant.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Executable test fixture generator for the PR-loop GitHub interaction layer. It creates a temporary `gh` CLI shim so fetch/poll tests can exercise comment/review algorithms without hitting the live GitHub API.
- algorithmic_behavior: The script validates two required arguments, `mock_bin_dir` and `fixtures_dir`, then writes an executable `gh` shell script into the mock bin directory (`tests/setup-fixture-mock-gh.sh:16-27`, `tests/setup-fixture-mock-gh.sh:99-101`). The generated mock dispatches on top-level `gh` command: `auth status` succeeds, `repo view` returns fixed owner/name/parent JSON, `pr view` returns fixed PR number or state, and `api` returns fixture files for issue comments, review comments, and PR reviews (`tests/setup-fixture-mock-gh.sh:33-92`).
- inputs_outputs_state: Inputs are the destination mock binary directory and a fixture directory containing `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`. Output is the path to the mock bin directory on stdout (`tests/setup-fixture-mock-gh.sh:101`) plus a generated executable at `<mock_bin_dir>/gh`. Runtime state is local to the caller’s temporary test directory; no repository state is mutated.
- gates_or_invariants: Missing arguments are rejected with usage and exit 1 (`tests/setup-fixture-mock-gh.sh:19-22`). The mock uses endpoint substring matching: `/issues/*/comments` maps to issue comments, `/pulls/*/comments` maps to inline review comments, and `/pulls/*/reviews` maps to PR review summaries (`tests/setup-fixture-mock-gh.sh:71-87`). Unhandled commands fail closed with stderr and exit 1 (`tests/setup-fixture-mock-gh.sh:95-96`).
- dependencies_and_callers: Called by `tests/test-pr-loop-hooks.sh` via `setup_fixture_mock_gh`, which passes `tests/fixtures` and prepends the resulting mock bin directory to `PATH` (`tests/test-pr-loop-hooks.sh:1231-1239`). The fixture-backed tests then run `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` against this mock (`tests/test-pr-loop-hooks.sh:1242-1320`, `tests/test-pr-loop-hooks.sh:1322-1419`).
- edge_cases_or_failure_modes: The generated `repo view --json parent` returns `{"parent": null}` (`tests/setup-fixture-mock-gh.sh:44-45`), so fork-parent fallback paths in fetch/poll scripts are not deeply exercised by this mock. The mock returns empty array for unknown `gh api` endpoints (`tests/setup-fixture-mock-gh.sh:89-91`), which can mask endpoint misspecification unless the caller asserts specific returned content. It assumes fixture filenames use underscore names, matching the repo’s duplicate hyphen/underscore fixture set.
- validation_or_tests: Fixture-backed assertions verify `fetch-pr-comments.sh` includes human issue comments, inline review comments, and approval-only reviews rendered as `[Review state: APPROVED]` (`tests/test-pr-loop-hooks.sh:1242-1283`), respects `--after` filtering (`tests/test-pr-loop-hooks.sh:1287-1320`), and that `poll-pr-reviews.sh` emits valid JSON with placeholder bodies and bot responders (`tests/test-pr-loop-hooks.sh:1322-1367`). Later tests ensure timestamp filtering excludes earlier change-request text (`tests/test-pr-loop-hooks.sh:1371-1411`). The production scripts under test process PR-review empty bodies into placeholders at `scripts/fetch-pr-comments.sh:300-320` and `scripts/poll-pr-reviews.sh:276-294`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-075 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking prompt template for a PR-loop transition gate where Claude is configured but does not acknowledge the trigger comment with an eyes reaction before timeout.
- algorithmic_behavior: The template renders a failure explanation with `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}`, then instructs the user to verify bot installation, permissions, PR state, and to post a new trigger comment after configuration (`prompt-template/block/claude-eyes-timeout.md:1-18`). It is not executable logic itself; it defines the human-facing contract for the stop-hook’s blocking decision.
- inputs_outputs_state: Inputs are template variables `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, supplied by the PR-loop stop hook as `3` and `15` respectively (`hooks/pr-loop-stop-hook.sh:784-785`). Output is rendered markdown embedded in a JSON decision reason. No persistent state is changed by the template.
- gates_or_invariants: The gate fires only after trigger validation, when Claude is configured, trigger comments are required, and a confirmed trigger comment ID exists (`hooks/pr-loop-stop-hook.sh:748-771`). The stop hook checks `scripts/check-bot-reactions.sh claude-eyes <trigger_id> --pr <PR_NUMBER> --retry 3 --delay 5`; empty or `null` response blocks (`hooks/pr-loop-stop-hook.sh:774-789`). The emitted JSON decision is `block`, not a retry/continue signal (`hooks/pr-loop-stop-hook.sh:787-789`).
- dependencies_and_callers: Rendered through `load_and_render_safe` from `hooks/lib/template-loader.sh`, whose single-pass renderer replaces `{{VAR}}` placeholders without recursively expanding replacement values (`hooks/lib/template-loader.sh:50-132`) and falls back if a template is missing or empty (`hooks/lib/template-loader.sh:170-203`). Direct caller is `hooks/pr-loop-stop-hook.sh:780-789`.
- edge_cases_or_failure_modes: If the template is missing, the stop hook has an inline fallback message (`hooks/pr-loop-stop-hook.sh:780-784`). If trigger detection says a trigger exists but no comment ID is available, the hook warns and skips eyes verification rather than rendering this block (`hooks/pr-loop-stop-hook.sh:793-796`). If Claude is configured but the startup case does not require a trigger, the hook also skips eyes verification (`hooks/pr-loop-stop-hook.sh:797-799`).
- validation_or_tests: Template reference tests check critical validators use safe rendering (`tests/test-template-references.sh:172-199` in search results). The concrete stop-hook path is covered indirectly by PR-loop hook tests and by template directory validation, which requires the `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-105 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR round-file invariants. It tells the agent that it attempted to edit/write a `round-N-<type>.md` file whose round does not match the active loop round.
- algorithmic_behavior: The template renders the attempted action, attempted Claude round, file type, current round, correct path, and a hard instruction not to increment round numbers manually (`prompt-template/block/wrong-round-number.md:1-7`). It is a small but central guard message used by edit/write validators to keep summary-file state transitions aligned with `state.md`.
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Outputs are markdown printed to stderr by validators, followed by exit 2 to block the attempted tool action (`hooks/loop-edit-validator.sh:165-178`, `hooks/loop-write-validator.sh:231-244`). No file state is modified by the template.
- gates_or_invariants: Validators first identify summary files via `is_round_file_type` and extract round numbers via `extract_round_number` (`hooks/lib/loop-common.sh:399-418`). The block triggers when the extracted round is present, differs from `CURRENT_ROUND`, and the path is not allowlisted (`hooks/loop-edit-validator.sh:155-179`, `hooks/loop-write-validator.sh:228-245`). Allowlisted historical files include several early todo/summary paths (`hooks/lib/loop-common.sh:420-440`).
- dependencies_and_callers: Direct callers are `hooks/loop-edit-validator.sh:167-178` and `hooks/loop-write-validator.sh:233-244`; `hooks/loop-write-validator.sh` also validates directory correctness immediately afterward (`hooks/loop-write-validator.sh:248-260`). Rendering uses `load_and_render_safe`, whose fallback behavior prevents missing template files from disabling the guard (`hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: Only filenames matching `round-[0-9]+-(summary|prompt|todos).md` produce a round number (`hooks/lib/loop-common.sh:401-418`), so unrelated markdown names bypass this specific message and may be caught by other path validators. Case is normalized before extraction (`hooks/lib/loop-common.sh:411-417`). Existing allowlisted files can bypass wrong-round blocking even if the number differs (`hooks/lib/loop-common.sh:427-440`).
- validation_or_tests: `tests/test-template-loader.sh` renders this exact template and asserts the title, attempted `round-3-summary.md`, and current round `5` appear (`tests/test-template-loader.sh:147-164`). `tests/test-templates-comprehensive.sh` repeats a real-template rendering check with the correct path (`tests/test-templates-comprehensive.sh:497-510`). Wrong-round validation tests start in `tests/test-pr-loop-hooks.sh:1421-1435`, and repository search shows hook references to this template in both edit and write validators.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-135 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for production plan-path validation in `scripts/setup-rlcr-loop.sh`. It verifies the RLCR setup gate accepts safe plan file paths and rejects path traversal/injection/symlink/content failures before a loop is created.
- algorithmic_behavior: The test creates an isolated temporary git repository, installs a mock `codex` executable to avoid real CLI calls, commits an initial README, then repeatedly runs `scripts/setup-rlcr-loop.sh` with different plan paths (`tests/robustness/test-path-validation-robustness.sh:16-46`, `tests/robustness/test-path-validation-robustness.sh:74-111`). It classifies validation failure by matching production error text such as plan path errors, symlink errors, too-simple plans, insufficient content, and missing directories (`tests/robustness/test-path-validation-robustness.sh:89-103`).
- inputs_outputs_state: Inputs are generated plan files under `$TEST_DIR`, path strings passed to `setup-rlcr-loop.sh`, and production script output. Outputs are pass/fail records through `test-helpers.sh` and a final process exit from `print_test_summary` (`tests/robustness/test-path-validation-robustness.sh:480-485`). It mutates only its temporary test repo and removes active RLCR loop state before each case to avoid false “already active” failures (`tests/robustness/test-path-validation-robustness.sh:81-86`).
- gates_or_invariants: Positive cases require acceptance of normal relative paths, root filenames, dash/underscore, nested directories, and dots in filenames (`tests/robustness/test-path-validation-robustness.sh:120-167`). Negative cases require rejection of absolute paths, whitespace, shell metacharacters, command-substitution characters, glob characters, backslash, tilde, brackets, hash, and exclamation (`tests/robustness/test-path-validation-robustness.sh:177-330`). Symlink cases require rejecting both plan-file symlinks and symlinked parent directories (`tests/robustness/test-path-validation-robustness.sh:332-375`). Long safe filenames and 10-level nested paths should be accepted (`tests/robustness/test-path-validation-robustness.sh:377-410`). Content gates reject empty files, comment-only files, fewer than five lines, nonexistent files, and directories (`tests/robustness/test-path-validation-robustness.sh:415-478`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp-dir and pass/fail functions, git for temporary repo setup, and the production target `scripts/setup-rlcr-loop.sh` (`tests/robustness/test-path-validation-robustness.sh:12-15`, `tests/robustness/test-path-validation-robustness.sh:37-46`, `tests/robustness/test-path-validation-robustness.sh:86`). It is included in the aggregate runner list as `robustness/test-path-validation-robustness.sh` (`tests/run-all-tests.sh:59` from search results).
- edge_cases_or_failure_modes: The mock `codex` exits 0, so a valid path can proceed beyond validation; the classifier treats “requires codex” and “must be gitignored” as validation-passed signals for compatibility with environments where later gates differ (`tests/robustness/test-path-validation-robustness.sh:95-103`). The production script’s content parser treats lines starting with `#` as comments rather than markdown headings, so a markdown plan must include at least three non-comment content lines (`scripts/setup-rlcr-loop.sh:493-536`). Unicode/CJK/emoji paths are explicitly allowed and not tested as rejection cases (`tests/robustness/test-path-validation-robustness.sh:412-414`).
- validation_or_tests: This file is itself validation. Its expected behavior maps directly to production gates in `scripts/setup-rlcr-loop.sh`: relative path only (`scripts/setup-rlcr-loop.sh:320-324`), no spaces (`scripts/setup-rlcr-loop.sh:326-332`), no shell metacharacters (`scripts/setup-rlcr-loop.sh:334-341`), no file or parent symlinks (`scripts/setup-rlcr-loop.sh:346-380`), must exist and be readable (`scripts/setup-rlcr-loop.sh:382-392`), must resolve under project root (`scripts/setup-rlcr-loop.sh:394-406`), cannot live inside a submodule (`scripts/setup-rlcr-loop.sh:408-421`), tracking/gitignore policy (`scripts/setup-rlcr-loop.sh:423-472`), minimum line count (`scripts/setup-rlcr-loop.sh:483-491`), and minimum meaningful content (`scripts/setup-rlcr-loop.sh:493-536`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 Item Evidence headings; each assigned item appears once in Item Evidence.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`