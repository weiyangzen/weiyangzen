# agent_29 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Read` gate for RLCR loop files. It prevents the agent from reading stale, misplaced, or wrong-round `round-N-summary.md` / `round-N-prompt.md` files and blocks most `round-N-todos.md` reads so task tracking stays in the native todo surface. It explicitly leaves PR-loop files generally readable per the header comments at `hooks/loop-read-validator.sh:3-12`.
- algorithmic_behavior: The hook reads JSON from stdin, validates hook structure through `validate_hook_input`, rejects overly nested JSON via `is_deeply_nested`, and exits early unless `.tool_name` is `Read` at `hooks/loop-read-validator.sh:25-41`. For `Read`, it requires `tool_input.file_path`, lowercases the path for type checks, blocks todos files unless they match a small active-loop allowlist, then only applies round-file validation to prompt/summary paths at `hooks/loop-read-validator.sh:43-71`. For prompt/summary files it resolves the active RLCR loop, switches to `finalize-state.md` when present, strictly parses current round, extracts the requested round from the filename, determines file type, and enforces location, round number, and exact directory path at `hooks/loop-read-validator.sh:73-168`.
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.file_path`. Outputs are exit status plus optional stderr block text rendered through templates. Exit `0` allows, exit `1` indicates malformed input/state safety failure, and exit `2` blocks the read with a user-facing reason. It does not mutate loop state. State read is from `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/rlcr/<active>/state.md` or `finalize-state.md`, with override variables `PROJECT_ROOT`, `LOOP_BASE_DIR`, and `LOOP_DIR` supported at `hooks/loop-read-validator.sh:80-82`.
- gates_or_invariants: JSON must be syntactically valid, include `tool_name`, avoid null/invalid UTF-8 per `hooks/lib/loop-common.sh:59-96`, and avoid depth above 30 at `hooks/loop-read-validator.sh:32-35`. `Read` must include `tool_input.file_path` at `hooks/loop-read-validator.sh:43-46`. Todos files matching `round-[0-9]+-todos.md` are blocked unless `is_allowlisted_file` permits exact paths like `round-1-todos.md` or `round-2-todos.md` in the active loop (`hooks/lib/loop-common.sh:401-405`, `hooks/lib/loop-common.sh:423-441`). Prompt/summary reads must be inside `.humanize/rlcr/`, must target the current round unless allowlisted, and must equal `$ACTIVE_LOOP_DIR/$filename`, not an old session directory or copied file (`hooks/loop-read-validator.sh:122-168`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-read-validator.sh:17-19`. Key dependencies include `find_active_loop`, which uses the lexicographically newest child with `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151-174`); `parse_state_file_strict`, which requires frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch` and validates numeric/boolean values (`hooks/lib/loop-common.sh:246-330`); `is_round_file_type` / `extract_round_number` (`hooks/lib/loop-common.sh:399-418`); `is_in_humanize_loop_dir` (`hooks/lib/loop-common.sh:712-716`); and safe template rendering from `hooks/lib/template-loader.sh:167-203`. The hook is registered in `hooks/hooks.json` per search results and is included in template reference checks at `tests/test-template-references.sh:56-64`.
- edge_cases_or_failure_modes: If no active RLCR loop exists, prompt/summary reads are allowed after type detection because there is no current loop to enforce (`hooks/loop-read-validator.sh:84-86`). Malformed active state fails closed with “Malformed state file” at `hooks/loop-read-validator.sh:94-98`. A path containing `.humanize/rlcr/` is treated as in-loop by simple substring matching, so the later exact `$ACTIVE_LOOP_DIR/$filename` comparison is the main old-session guard. Filenames without an extractable round number exit `0`, so only `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md` participate in round validation (`hooks/loop-read-validator.sh:105-108`). The hard allowlist means specific old todo/summary files can bypass current-round blocks when they exactly match the active loop path.
- validation_or_tests: Finalize-phase behavior is covered by `tests/test-finalize-phase.sh:839-850`, which moves `state.md` to `finalize-state.md` and expects current-round summary reads to pass. Robust hook input handling is covered by `tests/robustness/test-hook-system-robustness.sh:637-665` for null/deep JSON cases and `tests/robustness/test-hook-system-robustness.sh:421-445` for concurrent read-hook invocations preserving state. Template existence and use of safe rendering for this validator are checked at `tests/test-template-references.sh:56-64` and `tests/test-template-references.sh:172-200`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file protection across UserPromptSubmit, Write, Edit, Bash, and Stop hooks. Its algorithmic purpose is to prove the original plan and loop backup remain immutable during an active loop, that state schema/branch gates fail closed, and that known command-pattern bypasses cannot modify `.humanize/rlcr/**/plan.md`.
- algorithmic_behavior: The script creates a temporary git repository, installs a mock `codex` binary, creates a gitignored `plans/test-plan.md`, backs it up to `.humanize/rlcr/<timestamp>/plan.md`, and writes loop `state.md` with v1.5.0-style fields at `tests/test-plan-file-hooks.sh:30-114`. It then drives hooks by piping JSON into hook scripts and checking exit codes plus output text. Early tests validate `loop-plan-file-validator.sh` with valid state, quoted YAML fields, missing schema fields, and branch changes (`tests/test-plan-file-hooks.sh:119-217`). Middle tests validate direct Write/Edit/Bash plan-backup blocking and shell bypass prevention (`tests/test-plan-file-hooks.sh:226-367`). Later tests validate stop-hook plan integrity, tracked/untracked plan behavior, committed-plan drift, section-specific goal-tracker placeholder reporting, and legacy-path non-blocking behavior (`tests/test-plan-file-hooks.sh:509-1106`).
- inputs_outputs_state: Inputs are generated hook JSON payloads and synthetic RLCR state/plan/goal-tracker files under `TEST_DIR`. Outputs are TAP-like colored PASS/FAIL/SKIP lines and the process exit code equal to `TESTS_FAILED` at `tests/test-plan-file-hooks.sh:1108-1117`. The script mutates only its temp repo: git branches, commits, `.gitignore`, `.humanize/rlcr`, `plans/`, and per-test subrepos. Hook-visible state includes `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, `PATH` with mocked `codex`, and YAML fields `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `review_started`.
- gates_or_invariants: Required state schema must include `review_started` and `base_branch`; missing fields are expected to block as malformed or schema-outdated (`tests/test-plan-file-hooks.sh:149-188`, `tests/test-plan-file-hooks.sh:673-695`). Branch switching during an active loop must block (`tests/test-plan-file-hooks.sh:194-217`). `.humanize/rlcr/**/plan.md` is read-only to Write, Edit, and Bash (`tests/test-plan-file-hooks.sh:226-298`). Bash modification detection must catch redirection, `rm`, direct `.humanize/rlcr/plan.md`, command substitution, glob expansion, brace expansion, pipes to `tee`, and backtick substitution (`tests/test-plan-file-hooks.sh:260-367`). Stop-hook plan integrity requires both backup existence and original/backup content equality, including tracked files with uncommitted changes and tracked files with committed drift (`tests/test-plan-file-hooks.sh:509-775`). Legacy `.humanize-loop.local` is explicitly not a protected loop path (`tests/test-plan-file-hooks.sh:1064-1106`).
- dependencies_and_callers: Calls `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The tested plan validator uses strict state parsing and branch checks at `hooks/loop-plan-file-validator.sh:44-115`, plus tracked-plan status checks starting at `hooks/loop-plan-file-validator.sh:117-180`. The write/edit hooks block plan backups at `hooks/loop-write-validator.sh:175-185` and `hooks/loop-edit-validator.sh:127-138`. The bash hook delegates plan backup detection to `command_modifies_file` at `hooks/loop-bash-validator.sh:330-341`, whose command patterns cover redirection, tee, in-place edits, copy/move, remove, `dd`, `truncate`, `printf`, and `exec` at `hooks/lib/loop-common.sh:1068-1096`. Stop-hook plan integrity is enforced at `hooks/loop-codex-stop-hook.sh:256-337`.
- edge_cases_or_failure_modes: The test suite captures regressions around YAML quote stripping for `plan_file` and `start_branch` (`tests/test-plan-file-hooks.sh:134-147`, `tests/test-plan-file-hooks.sh:373-422`, `tests/test-plan-file-hooks.sh:424-499`). It covers race-condition-like tracked-plan modification after loop setup (`tests/test-plan-file-hooks.sh:602-671`) and committed tracked-plan drift where `git status` is clean but backup diff catches mutation (`tests/test-plan-file-hooks.sh:697-775`). It ensures stop-hook placeholder diagnostics are section-specific, avoiding broad false positives across Ultimate Goal, Acceptance Criteria, and Active Tasks (`tests/test-plan-file-hooks.sh:777-1062`). A notable limitation is that it creates many temp files and git repos, so it is more integration/spec coverage than isolated unit coverage.
- validation_or_tests: This file is itself the validation artifact. It defines 17 numbered top-level tests plus decimal subtests through 14.4 and exits nonzero on any failure. It also mocks `codex` to avoid slow external review execution at `tests/test-plan-file-hooks.sh:38-58`. The specific hook behaviors it asserts correspond to implementation points in the sibling hooks and stop hook described above.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-089 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: User-facing block template for the stop-hook large-file quality gate. It defines the remediation contract shown when the RLCR stop hook detects changed code or documentation files exceeding the configured line-count threshold.
- algorithmic_behavior: The template has two placeholders: `{{MAX_LINES}}` for the line limit and `{{LARGE_FILES}}` for the generated bullet list of oversized files (`prompt-template/block/large-files.md:1-4`). It tells the agent why the gate matters, then splits required remediation into code-file and documentation-file paths. Code files must be split into smaller modular files with unchanged behavior, optionally reviewed via `code-simplifier`, and keep clear boundaries (`prompt-template/block/large-files.md:11-18`). Documentation files must be split into logical sections, preserve cross-references, maintain narrative flow, and update navigation/TOC structures (`prompt-template/block/large-files.md:19-23`). It ends by requiring commit and exit retry (`prompt-template/block/large-files.md:25`).
- inputs_outputs_state: Input variables are supplied by `load_and_render_safe` in the stop hook. `MAX_LINES` is hard-coded to `2000` at `hooks/loop-codex-stop-hook.sh:433`; `LARGE_FILES` is built from git status entries with bullets like path, line count, and file type at `hooks/loop-codex-stop-hook.sh:480-482`. Output is embedded as the JSON `reason` in a block response with system message “large files detected” at `hooks/loop-codex-stop-hook.sh:494-505`. The template itself is stateless.
- gates_or_invariants: The underlying gate only scans files reported by cached `git status --porcelain`, skips deleted files, classifies known code extensions and docs extensions, counts lines with `wc -l`, and blocks when `line_count > MAX_LINES` (`hooks/loop-codex-stop-hook.sh:435-486`). Unknown extensions are ignored (`hooks/loop-codex-stop-hook.sh:461-471`). The template reinforces the invariant that splitting must preserve functionality for code and preserve references/flow for docs.
- dependencies_and_callers: Called only by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` at `hooks/loop-codex-stop-hook.sh:494-496`. Rendering semantics come from `hooks/lib/template-loader.sh:50-132` and safe fallback behavior from `hooks/lib/template-loader.sh:167-203`. Search found direct references only in the template and stop hook.
- edge_cases_or_failure_modes: If git status fails, the stop hook blocks earlier with `block/git-status-failed.md`, so this template is not reached (`hooks/loop-codex-stop-hook.sh:396-425`). Only dirty/new tracked-status files are scanned; a clean oversized file already in the repo is not reported by this gate. Files exactly equal to the threshold pass because the comparison is strictly greater than `MAX_LINES` (`hooks/loop-codex-stop-hook.sh:480`). The template says “commit the changes,” but the same stop hook later enforces clean git state before review, so this wording aligns with the broader stop flow.
- validation_or_tests: Template reference coverage is provided by `tests/test-template-references.sh`, which scans hook scripts for template references and checks existence (`tests/test-template-references.sh:52-111`). No dedicated large-file behavior test was identified in the assigned script set; the implementation path is directly visible in `hooks/loop-codex-stop-hook.sh:427-508`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-119 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Skeleton contract for generated implementation plans. It defines the sections and algorithmic acceptance structure that `gen-plan` seeds before an agent fills in the final plan from a draft.
- algorithmic_behavior: The template requires a title, clear goal description, TDD-oriented acceptance criteria with positive and negative tests, path boundaries, feasibility hints, dependencies/sequence, and implementation notes (`prompt-template/plan/gen-plan-template.md:1-74`). Acceptance criteria are structured as `AC-X` and optional `AC-X.Y` subcriteria, each with deterministic pass and fail/rejection cases (`prompt-template/plan/gen-plan-template.md:6-23`). Path boundaries define upper and lower acceptable scope and allowed/prohibited choices, with special handling when a draft specifies a deterministic design and choices collapse to a fixed approach (`prompt-template/plan/gen-plan-template.md:25-44`). Feasibility hints are explicitly non-prescriptive (`prompt-template/plan/gen-plan-template.md:46-55`). Dependencies are expressed as relative sequencing, not estimates (`prompt-template/plan/gen-plan-template.md:57-67`). Implementation notes prohibit leaking plan-progress terminology such as “AC-”, “Milestone”, “Step”, or “Phase” into code/comments (`prompt-template/plan/gen-plan-template.md:69-74`).
- inputs_outputs_state: The file is copied verbatim to the requested output plan path by `scripts/validate-gen-plan-io.sh`, then the original draft is appended below an “Original Design Draft” separator (`scripts/validate-gen-plan-io.sh:133-159`). Inputs to that script are `--input <draft>` and `--output <plan>`, with validation for input existence/non-empty, output directory existence, output nonexistence, write permission, and template availability (`scripts/validate-gen-plan-io.sh:29-146`). Output is a new combined plan draft that the gen-plan command then edits in place.
- gates_or_invariants: The template enforces deterministic verification by requiring positive and negative tests for each criterion (`prompt-template/plan/gen-plan-template.md:8-23`). It separates requirements from suggestions by labeling feasibility hints as reference-only (`prompt-template/plan/gen-plan-template.md:46-49`). It constrains planning language by requiring milestones/phases/steps only in the plan and forbidding that terminology in resulting code (`prompt-template/plan/gen-plan-template.md:69-74`). The command-level contract mirrors and expands these rules: no time estimates, AC-X format, dependencies rather than timing, affirmative path boundaries, deterministic-design respect, and preservation of all draft content (`commands/gen-plan.md:225-248`).
- dependencies_and_callers: Used by `scripts/validate-gen-plan-io.sh` via `${CLAUDE_PLUGIN_ROOT}/prompt-template/plan/gen-plan-template.md` or a script-relative fallback (`scripts/validate-gen-plan-io.sh:133-140`). The `/gen-plan` command invokes that validator in Phase 1 and defines the overall six-phase workflow at `commands/gen-plan.md:19-47`. The command repeats the same plan structure inline at `commands/gen-plan.md:142-223` and instructs the agent to edit the seeded template while keeping the original draft intact at `commands/gen-plan.md:251-270`.
- edge_cases_or_failure_modes: If the template is missing, IO validation exits `7` and the command reports a plugin configuration error (`scripts/validate-gen-plan-io.sh:142-146`, `commands/gen-plan.md:38-47`). Existing output paths, directory outputs, missing directories, empty inputs, and unwritable output directories all fail before the template is copied (`scripts/validate-gen-plan-io.sh:79-124`). Because the command duplicates the template structure inline, drift between `commands/gen-plan.md` and this file is possible; search showed both contain matching section names and rules, but there is no assigned-file evidence of an automated drift check specific to this duplication.
- validation_or_tests: IO/template presence validation is implemented in `scripts/validate-gen-plan-io.sh:79-159`. The gen-plan command’s Phase 6 requires a full readback/review of the generated plan and consistency fixes before completion (`commands/gen-plan.md:262-270`). Broader plan-file validation tests exist under `tests/test-plan-file-validation.sh` and robustness suites, but the assigned direct template behavior is mainly covered by the IO validator path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`