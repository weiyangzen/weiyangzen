# agent_07 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable-spec suite for the Humanize RLCR and PR-loop algorithms. The directory is not runtime code, but it is core algorithm evidence because it encodes lifecycle, monitor, state-file, hook, template, PR review, cancellation, finalization, git, path, and robustness contracts. Recursive inventory covered top-level suites, `tests/robustness/`, `tests/fixtures/`, and `tests/mocks/`; `wc` reported `25848` total test/fixture lines.
- algorithmic_behavior: `tests/run-all-tests.sh:33` defines the ordered suite list, including state-exit, finalize, monitor, PR-loop, and robustness suites; `tests/run-all-tests.sh:75` special-cases zsh-only validation; `tests/run-all-tests.sh:104` captures suite output and exit codes; `tests/run-all-tests.sh:121` strips ANSI and aggregates `Passed:`/`Failed:` counts as the release gate. `tests/test-pr-loop.sh:18` wires shared helpers and modules, then runs script tests, hook tests, and stop-hook tests at `tests/test-pr-loop.sh:40`.
- inputs_outputs_state: Inputs are temporary git repos, mocked hook JSON, `.humanize/rlcr` and `.humanize/pr-loop` session trees, mock `gh` responses from `tests/fixtures/`, and fake CLI tools. Outputs are pass/fail summaries, generated temporary loop files, mock round artifacts, and process exit status. `tests/test-helpers.sh:30` and `tests/test-helpers.sh:84` define common pass/fail counters and temp directory setup; `tests/test-pr-loop-lib.sh:27` creates mock `gh`; `tests/test-pr-loop-lib.sh:90` creates mock `codex`.
- gates_or_invariants: The suite treats terminal state naming, protected loop files, round numbering, plan integrity, goal-tracker mutation rules, cancel authorization, force-push detection, reviewer approval state, and template availability as hard gates. `tests/test-template-references.sh:153` explicitly checks common block templates, including the state-file block. `tests/test-finalize-phase.sh:530` validates `COMPLETE` entering Finalize Phase via state rename; `tests/test-cancel-signal-file.sh:80` validates the only allowed cancel-state rename path.
- dependencies_and_callers: Test modules call sibling runtime scripts under `scripts/`, hooks under `hooks/`, shared shell libraries, templates under `prompt-template/`, and mock GitHub API fixtures. PR-loop system coverage starts at `tests/test-pr-loop-system.sh:1775`, with cases for mutual exclusion, reviewer status, phase detection, monitor output, goal tracker repair, startup case detection, setup failure cleanup, and stop-hook updates.
- edge_cases_or_failure_modes: Robustness subtree covers malformed or missing state frontmatter, concurrent state reads, non-numeric rounds, missing tools, zsh glob safety, terminal cleanup, API failures, timeout fallback, path bypasses, cancel command injection, symlinks, permission changes, and large/malformed templates. Some suites require external shell capabilities such as `git`, `jq`, `zsh`, `timeout`, `stat`, and writable temp directories; in this read-only export I inspected statically rather than executing them.
- validation_or_tests: The directory itself is the validation layer. High-signal suites include `tests/test-state-exit-naming.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-pr-loop-system.sh`, `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-cancel-signal-file.sh`, `tests/robustness/test-state-transition-robustness.sh`, `tests/robustness/test-git-operations-robustness.sh`, and `tests/robustness/test-pr-loop-api-robustness.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Shell entrypoint for user-facing Humanize monitors. It dispatches `humanize monitor rlcr` and `humanize monitor pr`, parses loop metadata, renders fixed terminal status bars, tails live logs/files, and bridges to shared monitor logic. It sources `scripts/lib/monitor-common.sh` when present at `scripts/humanize.sh:6`.
- algorithmic_behavior: Public helpers include pipe-array splitting at `scripts/humanize.sh:18`, RLCR goal-tracker parsing at `scripts/humanize.sh:32`, git-state detection at `scripts/humanize.sh:116`, and porcelain/diffstat parsing at `scripts/humanize.sh:166`. `_humanize_monitor_codex` starts at `scripts/humanize.sh:219`; it finds the newest RLCR session, finds project-cache Codex run/review logs, draws state/progress/git context, tails logs incrementally, and switches on newer sessions/logs. The top-level dispatcher starts at `scripts/humanize.sh:884`. `_humanize_monitor_pr` starts at `scripts/humanize.sh:931`; it finds PR-loop session activity files, parses PR state, computes phase display via shared helpers, supports `--once`, and tails live PR files.
- inputs_outputs_state: Inputs are `.humanize/rlcr`, `.humanize/pr-loop`, `state.md` or terminal `*-state.md`, `goal-tracker.md`, cached Codex logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, git status, terminal size, and PR round files. Outputs are terminal UI text, tail output, status summaries, and command exit codes. The script does not authoritatively mutate loop state; it reads state and file sizes, while PR phase detection in the shared library may update a transient `/tmp` size cache.
- gates_or_invariants: RLCR monitor requires `.humanize/rlcr` to exist or returns an error at `scripts/humanize.sh:230`. Log search uses timestamp-shaped session directories and `find` instead of globs for zsh safety at `scripts/humanize.sh:280`. It enforces a defensive run/review ordering invariant: max `codex-run` round must be lower than min `codex-review` round, otherwise it prints an inconsistency error at `scripts/humanize.sh:330`. PR monitor requires `.humanize/pr-loop` at `scripts/humanize.sh:955` and maps `approve` to `approved`, `maxiter` to `max-iterations` at `scripts/humanize.sh:1014`.
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for session/state/phase helpers, `git`, `stat`, `find`, `sed`, `grep`, `tail`, `tput`, and shell trap behavior. Called interactively via `humanize monitor rlcr` or `humanize monitor pr`; setup help references `humanize monitor pr` in `scripts/setup-pr-loop.sh:105`. Tests source it directly in monitor, git, goal-tracker, hook-input, and PR-loop suites.
- edge_cases_or_failure_modes: Missing loop directory returns nonzero; missing session returns guidance; no current log renders a waiting/no-log state; deleted loop/session/log triggers graceful stop or session switch; truncated/rotated log resets search at `scripts/humanize.sh:777`; zsh traps use `TRAPINT`/`TRAPTERM`; bash uses `trap`. Git parsing returns zeros outside a repo. Goal tracker parsing is markdown-shape dependent and may under/over-count if headings/tables drift. `tput`/terminal assumptions are central to interactive mode.
- validation_or_tests: Covered by `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, `tests/robustness/test-goal-tracker-robustness.sh:81`, `tests/robustness/test-git-operations-robustness.sh:75`, and PR monitor checks in `tests/test-pr-loop-system.sh:935`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Focused executable specification for RLCR state-file lifecycle naming and active-loop detection. It proves that active state is represented by `state.md`, while exit reasons preserve state by renaming to `<reason>-state.md`.
- algorithmic_behavior: The test creates a temp git repo and loop tree at `tests/test-state-exit-naming.sh:31`, sources `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:66`, then exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. Tests 1-7 validate active-loop discovery and newest-session precedence; tests 8-10 validate `end_loop`; tests 11-13 validate new path matching and legacy path exclusion.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/state.md` files and terminal state files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md`. Outputs are pass/fail counters and an exit code equal to failed count at `tests/test-state-exit-naming.sh:302`. The key state transition is calling `end_loop "$dir" "$dir/state.md" "$reason"` and expecting `$dir/${reason}-state.md` at `tests/test-state-exit-naming.sh:206`.
- gates_or_invariants: Terminal state files must not be active when `state.md` is absent: complete at `tests/test-state-exit-naming.sh:51`, cancel at `tests/test-state-exit-naming.sh:97`, unexpected at `tests/test-state-exit-naming.sh:114`, maxiter at `tests/test-state-exit-naming.sh:129`, stop at `tests/test-state-exit-naming.sh:145`. `state.md` must be active at `tests/test-state-exit-naming.sh:76`. Newer active timestamp wins at `tests/test-state-exit-naming.sh:161`. Invalid `end_loop` reason must fail at `tests/test-state-exit-naming.sh:186`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, especially `find_active_loop` at `hooks/lib/loop-common.sh:156`, `is_in_humanize_loop_dir` at `hooks/lib/loop-common.sh:713`, and `end_loop` at `hooks/lib/loop-common.sh:1119`. It is part of the aggregate runner at `tests/run-all-tests.sh:39`.
- edge_cases_or_failure_modes: Uses `set -uo pipefail` but not global `set -e`, allowing negative tests to inspect exit codes. It does not cover `finalize-state.md` active behavior; that is covered elsewhere. It intentionally rejects `.humanize-loop.local` legacy paths at `tests/test-state-exit-naming.sh:258` and ensures searching the new base path does not accidentally discover legacy loops at `tests/test-state-exit-naming.sh:267`.
- validation_or_tests: This file is itself the validation. It maps directly to hook behavior: `end_loop` validates reason and renames state at `hooks/lib/loop-common.sh:1124`; missing state returns failure at `hooks/lib/loop-common.sh:1141`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template for the safety gate that forbids agent-side modification of RLCR `state.md`. It is a small but core contract surface because validators render it when blocking writes or bash modifications to loop-managed state.
- algorithmic_behavior: The template declares the block title at `prompt-template/block/state-file-modification.md:1`, states that `state.md` is loop-managed at `prompt-template/block/state-file-modification.md:3`, and explains the protected state contents at `prompt-template/block/state-file-modification.md:5`. It has no variables and performs no computation itself; its algorithmic role is policy text emitted by validators.
- inputs_outputs_state: Input is a validator decision that a tool action targets `state.md`. Output is markdown guidance returned to the agent/user. It does not read or write state. It preserves the invariant that current round, max iterations, and Codex configuration are owned by the loop system.
- gates_or_invariants: The block supports the invariant that direct state edits corrupt loop state. It is loaded by `state_file_blocked_message` in `hooks/lib/loop-common.sh:463`. Write validator blocks `state.md` at `hooks/loop-write-validator.sh:148`; bash validator blocks command-based state modification at `hooks/loop-bash-validator.sh:128`. Separate templates handle `finalize-state.md` and PR-loop state, so this file’s scope is specifically RLCR `state.md`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` through `load_and_render_safe` called from `hooks/lib/loop-common.sh:469`. Referenced by template integrity checks in `tests/test-template-references.sh:153`. Consumed by read/write/edit/bash validator flows through shared loop-common messaging functions.
- edge_cases_or_failure_modes: Since it has no placeholders, render-time substitution cannot fail from missing variables. The main failure mode is missing file or wrong path; `load_and_render_safe` has an inline fallback at `hooks/lib/loop-common.sh:465`, and `tests/test-template-references.sh:161` verifies common templates exist.
- validation_or_tests: Covered by template reference, loader, comprehensive template, and hook validator suites, especially `tests/test-template-references.sh:153`, `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and state-file blocking tests in PR/RLCR hook suites.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-127 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for RLCR and PR-loop status displays. It centralizes color constants, file/session helpers, state-file detection, YAML extraction, display formatting, PR phase detection, and goal-tracker parsing for `scripts/humanize.sh`.
- algorithmic_behavior: Cross-platform file sizing is at `scripts/lib/monitor-common.sh:32`; latest timestamp session discovery is at `scripts/lib/monitor-common.sh:40`; terminal setup/restore helpers start at `scripts/lib/monitor-common.sh:71`; state-file detection starts at `scripts/lib/monitor-common.sh:154`; YAML scalar extraction starts at `scripts/lib/monitor-common.sh:197`; truncation starts at `scripts/lib/monitor-common.sh:233`. PR phase detection starts at `scripts/lib/monitor-common.sh:267`; it prioritizes final state files, detects a growing/latest `round-*-pr-check.md`, then falls back to `state.md` round/startup-case. General goal tracker parsing starts at `scripts/lib/monitor-common.sh:390`; PR issue statistics parsing starts at `scripts/lib/monitor-common.sh:468`.
- inputs_outputs_state: Inputs are session directories, state files, PR check files, goal trackers, terminal dimensions, `stat`, `date`, and `/tmp`. Outputs are pipe-delimited parser records, phase strings such as `approved`, `cancelled`, `maxiter`, `codex_analyzing`, `waiting_initial_review`, `waiting_reviewer`, and human-readable phase labels from `scripts/lib/monitor-common.sh:346`. It may write a transient `/tmp/humanize-phase-<session>-<round>.size` cache at `scripts/lib/monitor-common.sh:297` to compare file growth.
- gates_or_invariants: `monitor_find_latest_session` only accepts timestamp-shaped names at `scripts/lib/monitor-common.sh:54`. `monitor_find_state_file` prioritizes `state.md` as active at `scripts/lib/monitor-common.sh:162`, then returns the first `*-state.md` stop reason at `scripts/lib/monitor-common.sh:168`. PR phase final states are checked before activity detection at `scripts/lib/monitor-common.sh:272`. `get_pr_loop_phase` treats a growing or recently modified PR-check file as Codex analysis at `scripts/lib/monitor-common.sh:315`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh:9`; RLCR monitor wraps `monitor_find_state_file` at `scripts/humanize.sh:344`; PR monitor uses phase helpers at `scripts/humanize.sh:1100` and issue parsing at `scripts/humanize.sh:1141`. Tests source it directly in `tests/test-pr-loop-system.sh:258` and later monitor/state cases.
- edge_cases_or_failure_modes: Missing session directory returns empty/unknown. Multiple terminal `*-state.md` files are not sorted, so result depends on `find` order. If `/tmp` cache writes fail, PR-check files with nonzero size can repeatedly look “growing” until the mtime fallback ages out or the previous size is unavailable. `monitor_truncate_string` assumes `max_len` is large enough for an ellipsis. YAML parsing is line-oriented and handles simple frontmatter keys, not full YAML.
- validation_or_tests: Covered by shared monitor tests in `tests/test-pr-loop-system.sh:794`, phase detection tests around `tests/test-pr-loop-system.sh:258`, monitor output tests near `tests/test-pr-loop-system.sh:1857`, zsh/monitor safety tests, and real monitor tests in `tests/test-monitor-e2e-real.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`