# agent_20 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Command workflow definition for converting a user draft into a deterministic implementation plan. It defines the slash-command contract, allowed tools, phase order, plan schema, validation gates, and completion reporting.
- algorithmic_behavior: The command runs in six phases: IO validation, relevance check, draft analysis, issue resolution, plan generation, and write/complete. Phase 1 delegates to [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/validate-gen-plan-io.sh:29), then branches on exit codes 0-7 as documented in [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/commands/gen-plan.md:38). Phase 2 invokes `humanize:draft-relevance-checker` with the haiku model and requires `RELEVANT:` or `NOT_RELEVANT:` output, stopping on unrelated drafts. Phase 3 uses Explore agents to inspect repo context for clarity, consistency, completeness, and functionality risks. Phase 4 requires user clarification for unresolved issues and explicitly classifies numeric metrics as hard requirements or optimization directions. Phase 5 emits a fixed plan structure with AC-style criteria, positive and negative tests, path boundaries, feasibility hints, dependencies, and implementation notes. Phase 6 updates the template-created output with Edit, rereads the full plan, optionally unifies language, and reports path, summary, AC count, and language status.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, expected as `--input <draft> --output <plan>`, the source draft content, repository context, and user clarification answers. Output is a new plan file that starts from [prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/prompt-template/plan/gen-plan-template.md:1) and preserves the original draft appended by the validation script. State transition is draft plus template to structured plan plus original draft; no loop state file is directly mutated by this command.
- gates_or_invariants: Output path must be new, writable, and in an existing directory; input must exist and be non-empty. Relevance must not be completely unrelated. All draft information must be preserved as a superset, not replaced. Acceptance criteria must use `AC-X` or `AC-X.Y`, each with positive and negative tests. Generated plans must avoid time estimates and line-number references. Implementation notes must warn that plan-specific progress terminology should not appear in implementation code or comments. Mixed-language unification requires user consent.
- dependencies_and_callers: Depends on [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/validate-gen-plan-io.sh:1), [prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/prompt-template/plan/gen-plan-template.md:1), and [agents/draft-relevance-checker.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/agents/draft-relevance-checker.md:1). Structural and script contracts are exercised by [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-gen-plan.sh:47) and its validation-script section at [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-gen-plan.sh:425).
- edge_cases_or_failure_modes: Explicit stops include missing input, empty input, missing output directory, existing output path, non-writable output directory, invalid arguments, and missing plan template. Semantic stops include `NOT_RELEVANT`, unresolved missing information, contradictions found during review, and user-declined language unification leaving mixed languages as-is.
- validation_or_tests: [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-gen-plan.sh:54) verifies command existence, frontmatter, allowed tools, argument hint, relevance agent name/model/tools, version consistency, naming/frontmatter/YAML constraints, English-only content, and `validate-gen-plan-io.sh` exit codes for invalid arguments and IO failures.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancel transition. It proves that state-file mutation is normally blocked and that a cancel operation is authorized only by a `.cancel-requested` file in the active loop directory plus an exact `mv state.md cancel-state.md` or finalize equivalent shape.
- algorithmic_behavior: The test creates an active RLCR loop under a temp `.humanize/rlcr/<timestamp>` with `state.md`, exports `CLAUDE_PROJECT_DIR`, builds Bash-hook JSON through `jq`, and runs [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-bash-validator.sh:24). Positive cases allow exact move commands with absolute, normalized `./`, single-quoted, and literal `${loop_dir}` path forms. Negative cases require exit 2 for missing signal, wrong signal location, writes, sed edits, rm, wrong destination, arbitrary destination, extra args, hidden variables, command substitutions, shell operators, wrappers, prefixes, and many redirection-prefix variants.
- inputs_outputs_state: Inputs are synthetic loop directories, state files, optional `.cancel-requested` signal files, and command strings. Outputs are validator exit codes and test pass/fail counters. The test itself does not perform the move; it validates pre-tool authorization behavior. The modeled state transition is active `state.md` or `finalize-state.md` to preserved `cancel-state.md`.
- gates_or_invariants: The signal must be in the active loop directory, not a parent or unrelated directory. The command must start with `mv`, have exactly two arguments, source must be active loop `state.md` or `finalize-state.md`, destination must be active loop `cancel-state.md`, no extra `$` after loop-dir normalization, no command substitution/backticks/newlines/chaining operators, no mixed quote styles, no multiple trailing spaces, and the source must not be a symlink as enforced in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:551).
- dependencies_and_callers: Sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:33) and exercises `is_cancel_authorized`, `to_lower`, `find_active_loop`, `parse_state_file_strict`, and `command_modifies_file`. The corresponding user command is [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/commands/cancel-rlcr-loop.md:9), and the concrete state transition is implemented by [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/scripts/cancel-rlcr-loop.sh:134).
- edge_cases_or_failure_modes: Covers bypass classes: chained `&&`, `;`, `||`, pipes, background `&`, `|&`, `sh -c` and `bash -c`, `sudo`, `env`, `command --`, redirection-before-command forms including quoted and ANSI-C quoted targets, command substitution, backticks, hidden `${IFS}`, unrelated `/tmp/state.md`, and no-active-loop behavior where commands are allowed because there is no active state file.
- validation_or_tests: This file is itself the validation suite. It ends by printing pass/fail counts and exits with `TESTS_FAILED`, making any missed guard a nonzero test result at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-cancel-signal-file.sh:1349).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Prompt/block template for a git-clean gate special case. It explains that `.humanize/` is local RLCR loop state and must be ignored rather than committed.
- algorithmic_behavior: When the stop hook detects uncommitted changes and untracked `.humanize*` paths, it loads this template and appends it to special notes before blocking expensive Codex review. The insertion point is [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-codex-stop-hook.sh:528), where untracked files matching `.humanize` trigger `load_template`.
- inputs_outputs_state: Input is the cached git status from the stop hook. Output is human-facing remediation text telling the agent to add `.humanize*` to `.gitignore` and stage `.gitignore`. It does not mutate repository state itself; it directs the next allowed user/agent action.
- gates_or_invariants: Invariant is that `.humanize/` created by `humanize:start-rlcr-loop` is local loop state and should not be committed. This complements the stronger Bash gate in `git_adds_humanize`, which blocks direct or broad git-add patterns that would stage `.humanize` files in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:930).
- dependencies_and_callers: Loaded through [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/template-loader.sh:36). Caller is the git cleanliness check in `loop-codex-stop-hook.sh`; sibling templates include `block/git-not-clean-untracked.md` and `block/git-add-humanize.md`.
- edge_cases_or_failure_modes: If the template is missing, the hook falls back to a shorter inline note. The template suggests appending `.humanize*` to `.gitignore`; if an agent instead tries to force-add `.humanize`, `git_adds_humanize` blocks it. It is a prompt-only transition aid, not an enforcement mechanism by itself.
- validation_or_tests: Behavior is indirectly covered by tests around humanize escape/git-add protection, but this specific 8-line template has no standalone test. Its rendering path is covered by the shared template loader fallback behavior in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/template-loader.sh:167).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer template defining the required next-round exit contract for Claude after Codex review finds issues.
- algorithmic_behavior: After review issues are found, `loop-codex-stop-hook.sh` increments `current_round`, writes the next round prompt, appends optional post-alignment notes, then appends this footer through `load_and_render_safe` with `NEXT_SUMMARY_FILE` substitution at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-codex-stop-hook.sh:1536). The footer forbids dishonest exits, state-file edits, and `cancel-rlcr-loop`, then requires commit and summary creation.
- inputs_outputs_state: Input variable is `NEXT_SUMMARY_FILE`, rendered into `@{{NEXT_SUMMARY_FILE}}`. Output is appended prompt text in `round-N-prompt.md`. State transition is behavioral: a blocked stop-hook response hands Claude the next-round work instructions and defines what artifact must be written before the next exit attempt.
- gates_or_invariants: The footer preserves loop integrity by forbidding lying, manual loop-state editing, or running cancellation as an escape path. It also sets the completion gate for the round: commit changes and write the summary file. The optional code-simplifier instruction is conditional on plugin availability, not a hard shell gate.
- dependencies_and_callers: Depends on [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/template-loader.sh:50) for single-pass placeholder rendering. Caller is the RLCR stop hook’s next-round prompt builder. It coordinates with `claude/next-round-prompt.md`, `claude/post-alignment-action-items.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the hook uses a fallback footer that still requires commit and summary but lacks the stronger anti-cheating language. Placeholder injection is limited by single-pass rendering, so `NEXT_SUMMARY_FILE` content is not recursively interpreted.
- validation_or_tests: No direct test for this 9-line template, but its load/render path is exercised by the template loader and the stop hook flow. The summary-file write requirement is reinforced by Bash guards that block shell writes to `round-*-summary.md` and instruct use of Write/Edit in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/loop-bash-validator.sh:371).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-140 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It verifies tolerant and strict parsing behavior for YAML frontmatter used by hooks and validators.
- algorithmic_behavior: The test sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:16) and [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-helpers.sh:30), creates temporary state files, calls `parse_state_file`, `parse_state_file_strict`, and `get_current_round`, then records pass/fail outcomes. Positive cases verify all required fields, current round extraction, ignored extra fields, quoted values, zero round, special path values, large numbers, `full_review_round`, and default `full_review_round`.
- inputs_outputs_state: Inputs are synthetic `state.md` variants with YAML frontmatter. Outputs are shell globals such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, and `STATE_FULL_REVIEW_ROUND`, plus exit status from strict/tolerant parsers. No persistent repo state is changed; all files are under a temp directory.
- gates_or_invariants: Tolerant parser defaults missing `current_round` to `0`, `max_iterations` to `10`, `push_every_round` to `false`, and `full_review_round` to `5` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:237). Strict parser requires frontmatter separators plus `current_round`, `max_iterations`, `review_started`, and `base_branch`, validates numeric round/iteration values, and validates `review_started` boolean at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/hooks/lib/loop-common.sh:263).
- dependencies_and_callers: This test targets shared parser APIs used by `loop-bash-validator.sh`, `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-codex-stop-hook.sh`. It relies on helper summary behavior from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/test-helpers.sh:58).
- edge_cases_or_failure_modes: Covers missing frontmatter, nonnumeric current round, missing required fields, empty files, partial YAML, malformed YAML, negative round, whitespace, nonexistent file, binary bytes after YAML, CRLF, and optional `full_review_round` absence/minimum. Some tolerant-mode tests intentionally accept broad outcomes as long as parsing does not crash, for example nonnumeric and CRLF handling.
- validation_or_tests: This file is the validation suite and exits with the helper summary status at [tests/robustness/test-state-file-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-pr-loop/tests/robustness/test-state-file-robustness.sh:480). It complements broader state transition and concurrent-state robustness tests, but the assigned file focuses specifically on parser resilience.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_PR_LOOP-HZ-020, IMPROVE_PR_LOOP-HZ-050, IMPROVE_PR_LOOP-HZ-080, IMPROVE_PR_LOOP-HZ-110, IMPROVE_PR_LOOP-HZ-140
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`