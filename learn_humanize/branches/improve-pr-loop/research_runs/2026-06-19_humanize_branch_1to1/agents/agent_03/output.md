# agent_03 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: `commands/` is the user-facing slash-command contract layer. It does not implement the loop algorithms directly; it routes allowed tool execution to scripts, defines argument surfaces, and documents the state-machine expectations for PR loop, RLCR loop, plan generation, and cancellation.
- algorithmic_behavior: `commands/start-pr-loop.md:1-4` allows only `scripts/setup-pr-loop.sh:*` and defines the PR-loop entrypoint; `commands/start-pr-loop.md:16-24` documents the PR loop sequence: detect PR, fetch bot comments, fix, push and trigger review, Stop Hook polling, then local Codex validation. `commands/start-rlcr-loop.md:16-26` defines the RLCR implementation-to-review flow, and `commands/start-rlcr-loop.md:60-67` defines the two active phases. `commands/cancel-pr-loop.md:17-23` delegates PR cancellation to the script and treats `state.md` in the newest `.humanize/pr-loop/` directory as active. `commands/cancel-rlcr-loop.md:17-34` adds Finalize Phase confirmation handling. `commands/gen-plan.md:19-26` defines a separate draft-to-plan algorithm with IO validation, relevance check, analysis, user issue resolution, plan generation, and write/review completion.
- inputs_outputs_state: Inputs are slash command arguments via `$ARGUMENTS`, `${CLAUDE_PLUGIN_ROOT}` for script resolution, and user confirmation for RLCR finalize cancellation. Outputs are script invocations plus user-facing instructions, not direct file writes from the command specs. State transitions are delegated: PR start creates `.humanize/pr-loop/...`, RLCR start creates `.humanize/rlcr/...`, cancellation renames active state files, and gen-plan writes a structured plan after validation.
- gates_or_invariants: Front matter `allowed-tools` is the first gate: PR start can only run setup, PR cancel can only run cancel, RLCR cancel can run normal or `--force` plus `AskUserQuestion`. PR start requires at least one bot flag per `commands/start-pr-loop.md:25-30`; the PR workflow requires summaries, pushes, correct bot tags, and no state-file cheating per `commands/start-pr-loop.md:50-56`. RLCR requires goal tracker maintenance and prohibits state-file/cancel bypass per `commands/start-rlcr-loop.md:46-52`. Gen-plan hard-gates IO validation exit codes at `commands/gen-plan.md:30-47`.
- dependencies_and_callers: The directory coordinates with `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and `scripts/validate-gen-plan-io.sh`. Runtime enforcement is completed by hooks such as `hooks/pr-loop-stop-hook.sh`, `hooks/loop-codex-stop-hook.sh`, and validators under `hooks/`.
- edge_cases_or_failure_modes: Command docs rely on scripts to emit sentinel first lines such as `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `FINALIZE_NEEDS_CONFIRM`, and `CANCELLED_FINALIZE`; if script output changes, the command response contract breaks. `gen-plan` is a skip-adjacent child for this branch’s PR-loop focus, but it is still part of `commands/` and implements a core planning workflow.
- validation_or_tests: `tests/test-pr-loop.sh:32-41` sources `tests/test-pr-loop-scripts.sh` and runs command-backed script tests. `tests/run-all-tests.sh:32-54` includes both `test-pr-loop.sh` and `test-pr-loop-system.sh`, so command-routed PR behavior has both low-level script and broader system coverage.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-033 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Cancels the active RLCR loop by creating an authorization/signal file and moving the active loop state to `cancel-state.md`, preserving the session directory for audit/history.
- algorithmic_behavior: Argument parsing accepts `--force`, `-h`, and `--help`, rejects unknown options with exit `3` at `scripts/cancel-rlcr-loop.sh:24-63`. It resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, selects the lexicographically newest child under `.humanize/rlcr`, and exits `NO_LOOP` when none exists at `scripts/cancel-rlcr-loop.sh:69-78`. It treats `state.md` as a normal active loop and `finalize-state.md` as Finalize Phase at `scripts/cancel-rlcr-loop.sh:85-99`. It extracts `current_round` and `max_iterations` for reporting at `scripts/cancel-rlcr-loop.sh:105-111`. Finalize Phase without `--force` emits `FINALIZE_NEEDS_CONFIRM` and exits `2` before mutating state at `scripts/cancel-rlcr-loop.sh:117-127`.
- inputs_outputs_state: Inputs are CLI args, environment `CLAUDE_PROJECT_DIR`, newest `.humanize/rlcr/<timestamp>/` directory, and either `state.md` or `finalize-state.md`. Outputs are sentinel lines and status details on stdout. Successful cancellation writes `.cancel-requested` and moves the active state file to `cancel-state.md` at `scripts/cancel-rlcr-loop.sh:134-138`. Normal loop success emits `CANCELLED`; finalize success emits `CANCELLED_FINALIZE` at `scripts/cancel-rlcr-loop.sh:144-152`.
- gates_or_invariants: The Finalize Phase confirmation gate is the main semantic invariant: no state mutation unless `--force` is present. Active loop detection is file-name based: only `state.md` or `finalize-state.md` count. Missing round metadata degrades to `?`, preserving cancellation ability. `set -euo pipefail` makes `touch` or `mv` failures hard failures, though the documented “Other error” exit `3` is not explicitly trapped for filesystem failures.
- dependencies_and_callers: `commands/cancel-rlcr-loop.md:17-34` is the slash-command caller and maps `FINALIZE_NEEDS_CONFIRM` to an `AskUserQuestion` confirmation, then reruns with `--force` if approved. The `.cancel-requested` signal coordinates with hook authorization tested in `tests/test-cancel-signal-file.sh`, where a state rename is allowed only when the signal is in the active loop directory.
- edge_cases_or_failure_modes: The script only inspects the newest loop directory. If the newest directory is stale and lacks active state while an older directory is active, it returns `NO_ACTIVE_LOOP`. If both `state.md` and `finalize-state.md` exist, normal `state.md` wins. Lexicographic `sort -r` assumes timestamp-style directory names. Newline or malformed YAML around `current_round`/`max_iterations` is not parsed structurally; simple grep/sed extraction is used.
- validation_or_tests: `tests/test-cancel-signal-file.sh:80-94` validates that `mv state.md cancel-state.md` is allowed with `.cancel-requested`; `tests/test-cancel-signal-file.sh:121-155` validates missing-signal and non-rename mutations are blocked; `tests/test-cancel-signal-file.sh:243-317` covers command-injection attempts around the authorized cancel shape. The command-level Finalize Phase user interaction is specified in `commands/cancel-rlcr-loop.md:21-34`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-063 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable shell specification for PR-loop script argument parsing and selected state behavior across setup, cancellation, comment fetch, and polling scripts.
- algorithmic_behavior: `run_setup_tests` targets `scripts/setup-pr-loop.sh` and asserts help output, required bot flags, unknown option rejection, recognition of `--claude`, `--codex`, both bot flags, `--max`, invalid `--max`, `--codex-model`, and `--codex-timeout` at `tests/test-pr-loop-scripts.sh:18-166`. `run_cancel_tests` targets `scripts/cancel-pr-loop.sh` and asserts help, `NO_LOOP` on no active loop, and active PR cancellation state rename at `tests/test-pr-loop-scripts.sh:172-253`. `run_fetch_tests` validates fetch script help, required PR number, required output file, and numeric PR validation at `tests/test-pr-loop-scripts.sh:259-326`. `run_poll_tests` validates poll script help, required PR number, required `--after`, and required `--bots` at `tests/test-pr-loop-scripts.sh:332-399`.
- inputs_outputs_state: Inputs are `PROJECT_ROOT`, `TEST_DIR`, shell `pass`/`fail` helpers from the parent test harness, mock loop directories, and direct script arguments. Outputs are pass/fail records via helper calls. The main state transition under test is PR cancellation: the test creates `.humanize/pr-loop/2026-01-18_12-00-00/state.md` and expects `cancel-state.md` to exist while `state.md` is gone at `tests/test-pr-loop-scripts.sh:217-241`.
- gates_or_invariants: Setup tests verify that at least one bot flag is mandatory and invalid numeric options are rejected. Fetch and poll tests verify scripts reject missing required positional/flag inputs before GitHub work. The flag-recognition tests intentionally tolerate later environmental failure, checking that valid flags do not trigger the “at least one bot flag” error at `tests/test-pr-loop-scripts.sh:66-101`.
- dependencies_and_callers: `tests/test-pr-loop.sh:18-20` sources helpers and PR-loop test library, `tests/test-pr-loop.sh:32-34` sources this file alongside hook and stop-hook test modules, and `tests/test-pr-loop.sh:40-47` calls `run_script_tests`, `run_hook_tests`, and `run_stophook_tests`. `run_script_tests` is the exported entrypoint at `tests/test-pr-loop-scripts.sh:405-409`.
- edge_cases_or_failure_modes: This file focuses on argument and sentinel behavior, not full GitHub API behavior. Some positive setup parsing tests may still fail later because no usable git/gh/codex environment exists; the asserted invariant is that parsing advanced beyond the targeted validation. The tests use `CLAUDE_PROJECT_DIR="$TEST_DIR"` to isolate cancel behavior from the real repository at `tests/test-pr-loop-scripts.sh:193-201` and `tests/test-pr-loop-scripts.sh:213-234`.
- validation_or_tests: The file is itself validation. It is included in the main PR-loop test runner and transitively in `tests/run-all-tests.sh:32-54`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-093 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: User-facing block template for attempts to write generated PR-loop prompt/comment files.
- algorithmic_behavior: The template states that `round-*-pr-comment.md` and `round-*-prompt.md` under `.humanize/pr-loop/` are generated system files and read-only at `prompt-template/block/pr-loop-prompt-write.md:1-9`. It defines the remediation contract rather than executable behavior.
- inputs_outputs_state: It has no placeholders and no dynamic inputs. Output is static Markdown used as an error/block reason. It does not mutate state; it prevents mutation by explaining the invariant.
- gates_or_invariants: The invariant is that fetched PR comments and system prompts are owned by the PR loop system, not the worker/user. `hooks/lib/loop-common.sh:898-905` loads this template through `pr_loop_prompt_blocked_message`. Write validator blocks PR-loop read-only files with this message at `hooks/loop-write-validator.sh:83-86`; edit validator does the same at `hooks/loop-edit-validator.sh:66-68`; Bash validator blocks matching PR-loop generated files at `hooks/loop-bash-validator.sh:423-440`.
- dependencies_and_callers: Depends on the template loader fallback path through `load_and_render_safe` in `hooks/lib/loop-common.sh:899-905`. The read-only file classifier includes `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` at `hooks/lib/loop-common.sh:825-835`.
- edge_cases_or_failure_modes: The template text names only `round-*-pr-comment.md` and `round-*-prompt.md`, while the validator also uses it for `round-*-codex-prompt.md`, `round-*-pr-check.md`, and `round-*-pr-feedback.md`. That is behaviorally safe but can be less precise for users blocked on those other generated files.
- validation_or_tests: Template reference coverage is implied by `tests/test-template-references.sh` in the all-tests list, and PR-loop file-protection behavior is exercised through hook/system tests. The direct consumer references are visible in `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-123 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR-loop goal tracker template rendered into `.humanize/pr-loop/<timestamp>/goal-tracker.md` during setup.
- algorithmic_behavior: The template records PR metadata placeholders `{{PR_NUMBER}}`, `{{START_BRANCH}}`, `{{STARTED_AT}}`, `{{ACTIVE_BOTS_DISPLAY}}`, and `{{STARTUP_CASE}}` at `prompt-template/pr-loop/goal-tracker-initial.md:3-10`. It defines the ultimate goal as getting all monitored bot reviewers to approve the PR at `prompt-template/pr-loop/goal-tracker-initial.md:11-14`. It initializes Issue Summary, Total Statistics, and Issue Log sections with zero issues and Round 0 awaiting initial reviews at `prompt-template/pr-loop/goal-tracker-initial.md:15-33`.
- inputs_outputs_state: Inputs are setup-time template variables supplied by `scripts/setup-pr-loop.sh:658-666`. Output is the initial `goal-tracker.md`, written after `load_and_render_safe` at `scripts/setup-pr-loop.sh:641-713`. Initial state is Round 0, total issues found/resolved/remaining all `0`, and an issue log entry indicating the loop is awaiting initial reviews.
- gates_or_invariants: The schema is consumed by monitor/stat parsers. `scripts/lib/monitor-common.sh:475-487` extracts `Total Issues Found`, `Total Issues Resolved`, and `Remaining`; `scripts/lib/monitor-common.sh:489-495` derives the last reviewer from the Issue Summary table. The template must preserve these labels and table shape for monitoring to remain stable.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` during PR loop setup. The setup script builds `BOT_AC_ROWS` at `scripts/setup-pr-loop.sh:646-653`, but this template does not currently include a `{{BOT_AC_ROWS}}` placeholder; the fallback tracker in the setup script includes an Acceptance Criteria table, so the actual template and fallback differ in structure.
- edge_cases_or_failure_modes: Placeholder values are rendered single-pass by the template loader, so missing variables remain visible as `{{...}}`. If section headers or statistic labels change, monitor parsing and schema tests can break. The current template is issue-statistics oriented rather than acceptance-criteria oriented, which matches PR loop monitoring but differs from the setup fallback’s richer AC table.
- validation_or_tests: `tests/test-pr-loop-system.sh:642-655` checks the template contains `Issue Summary`, `Total Statistics`, `Issue Log`, and the three required statistic labels. `tests/test-pr-loop-system.sh:1453-1528` verifies setup creates `goal-tracker.md`, includes the expected Issue Summary structure, and renders the mocked PR number.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 section headings present, one per assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`