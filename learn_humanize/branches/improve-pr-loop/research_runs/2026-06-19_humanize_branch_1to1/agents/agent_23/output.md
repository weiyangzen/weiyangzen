# agent_23 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that enforces the RLCR “all Claude todos must be complete before exit/finalize” gate. It is called by `hooks/loop-codex-stop-hook.sh` before expensive review/finalize logic, at `hooks/loop-codex-stop-hook.sh:340-394`.
- algorithmic_behavior: Reads hook input JSON from stdin, extracts `transcript_path`, scans the transcript JSONL file, and remembers the most recent `TodoWrite` tool call. It supports three transcript shapes: Claude assistant messages with `message.content[].type=tool_use` at `hooks/check-todos-from-transcript.py:51-64`, alternative `type=message` entries at `hooks/check-todos-from-transcript.py:65-76`, and direct `type=tool_use` entries at `hooks/check-todos-from-transcript.py:78-85`. After the latest todo list is found, every todo whose `status` is not exactly `completed` is treated as incomplete at `hooks/check-todos-from-transcript.py:118-125`.
- inputs_outputs_state: Input is stdin JSON containing optional `transcript_path`; the transcript path is expanded with `Path(...).expanduser()` at `hooks/check-todos-from-transcript.py:104-109`. Output is process exit status plus optional stdout/stderr. Exit `0` means no blocking todo evidence, including empty input, missing `transcript_path`, nonexistent transcript, no todos, or all completed. Exit `1` emits `INCOMPLETE_TODOS` plus one line per incomplete todo at `hooks/check-todos-from-transcript.py:126-131`. Exit `2` emits `PARSE_ERROR` on invalid hook JSON at `hooks/check-todos-from-transcript.py:99-102`. The script itself does not mutate repo state.
- gates_or_invariants: The invariant is strict completion: only `status == "completed"` passes; missing, blank, `pending`, or `in_progress` statuses block. Invalid JSONL transcript lines are ignored rather than failing the gate at `hooks/check-todos-from-transcript.py:36-39`, while invalid hook input JSON fails closed with exit `2`. The latest non-empty TodoWrite list wins, so stale earlier incomplete todos do not block if a later TodoWrite marks all tasks complete.
- dependencies_and_callers: Depends only on Python standard library modules `json`, `sys`, and `pathlib`. Main caller is `hooks/loop-codex-stop-hook.sh:345-393`, which maps exit `2` to a JSON block decision for parse errors and exit `1` to the `block/incomplete-todos.md` prompt. Integration coverage appears in `tests/test-finalize-phase.sh:715-740`, where incomplete transcript todos block finalize and prevent Codex invocation.
- edge_cases_or_failure_modes: Nonexistent transcript paths return no todos and allow exit (`hooks/check-todos-from-transcript.py:25-26`), which is intentionally permissive but means missing transcript evidence is not fail-closed. Empty TodoWrite arrays are ignored because `latest_todos` updates only when `todos` is truthy at `hooks/check-todos-from-transcript.py:62-63`, so an explicit latest empty todo list does not clear earlier todos if present before it. If a todo lacks `content`, the printed incomplete line uses an empty string at `hooks/check-todos-from-transcript.py:121-124`.
- validation_or_tests: Dedicated coverage in `tests/test-todo-checker.sh` validates invalid hook JSON exit `2`, empty/missing transcript behavior, all-completed exit `0`, incomplete/pending/in-progress exit `1`, invalid JSONL-line tolerance, latest TodoWrite selection, alternate transcript formats, missing status, empty content, and unicode handling (`tests/test-todo-checker.sh:51-293`). Finalize-phase integration specifically verifies incomplete todos block final completion before Codex can run (`tests/test-finalize-phase.sh:699-740`).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR finalize-phase state machine. It validates the transition from normal implementation/review state into `finalize-state.md`, the finalize completion transition into `complete-state.md`, and the guardrails that prevent premature or unsafe completion.
- algorithmic_behavior: The test creates an isolated git repo and loop directory, writes a representative `state.md` frontmatter document, mocks `codex exec` and `codex review`, then exercises shared helpers and hooks as black-box commands. The fixture state includes `current_round`, `max_iterations`, Codex settings, `push_every_round`, `plan_file`, `start_branch`, `base_branch`, and `review_started` at `tests/test-finalize-phase.sh:190-205`. Mock Codex commands are injected through a temp `bin/codex` at `tests/test-finalize-phase.sh:53-121`, including successful output, call tracking, review failure, and empty-review-output modes.
- inputs_outputs_state: Inputs are generated hook JSON payloads, temp transcript files, mock Codex stdout/stderr behavior, and synthetic `.humanize/rlcr/<timestamp>` state files. Outputs are test assertions over hook exit codes, JSON block decisions, created or renamed state files, Codex marker files, review cache logs, and next-round/review result files. Expected state transitions include `state.md -> finalize-state.md` on successful completion/review (`tests/test-finalize-phase.sh:524-539`), `finalize-state.md -> complete-state.md` after summary/git/todo gates pass (`tests/test-finalize-phase.sh:476-496`), and `state.md -> maxiter-state.md` when completion occurs at max iterations (`tests/test-finalize-phase.sh:542-560`).
- gates_or_invariants: The finalize phase is active if the newest loop has `finalize-state.md`, while `complete-state.md` must not be considered active (`tests/test-finalize-phase.sh:283-320`). `finalize-state.md` is protected from Write, Edit, Bash redirection, `mv`, and `cp` source operations (`tests/test-finalize-phase.sh:345-415`). Finalize completion still requires `finalize-summary.md`, a clean git worktree, and completed TodoWrite tasks (`tests/test-finalize-phase.sh:429-471`, `tests/test-finalize-phase.sh:699-740`). Finalize deliberately skips Codex invocation (`tests/test-finalize-phase.sh:446-503`). Review failures and empty review outputs are hard blockers that must preserve `state.md`, avoid `finalize-state.md`/`complete-state.md`, and set/preserve `review_started: true` (`tests/test-finalize-phase.sh:562-696`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-finalize-phase.sh:48-49`, then invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The implementation under test detects finalize phase in `hooks/loop-codex-stop-hook.sh:67-82`, performs todo gating at `hooks/loop-codex-stop-hook.sh:340-394`, selects `finalize-summary.md` in finalize phase at `hooks/loop-codex-stop-hook.sh:609-641`, completes finalize without Codex at `hooks/loop-codex-stop-hook.sh:737-748`, and enters finalize through `enter_finalize_phase` at `hooks/loop-codex-stop-hook.sh:1030-1113`.
- edge_cases_or_failure_modes: Covers max-iteration edge behavior where `COMPLETE` should skip finalize and produce `maxiter-state.md`. Covers review command nonzero exit and zero-byte stdout/result as blocking failures, including cache-log verification at `tests/test-finalize-phase.sh:659-676`. Covers validator parsing when only `finalize-state.md` exists, ensuring Bash, Read, and plan-file validators do not assume only `state.md` (`tests/test-finalize-phase.sh:817-865`). Also checks normal RLCR rounds remain unaffected and still increment `current_round` when review returns feedback (`tests/test-finalize-phase.sh:743-814`).
- validation_or_tests: This file is itself the validation suite. It defines positive cases T-POS-1 through T-POS-5 and negative cases T-NEG-1 through T-NEG-9 in comments at `tests/test-finalize-phase.sh:5-19`, then exits with the failure count at `tests/test-finalize-phase.sh:867-876`. I did not execute it in this read-only research pass.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Prompt block used by the Bash validator to enforce the RLCR default push policy: local commits should not be pushed unless the loop was started with `--push-every-round`.
- algorithmic_behavior: The template is static markdown. It communicates that “current commits should stay local” and gives the exact startup flag form `/humanize:start-rlcr-loop plan.md --push-every-round` at `prompt-template/block/git-push.md:1-9`. The algorithmic decision is not in this file; the file is the operator-facing reason emitted when the decision has already been made by the validator.
- inputs_outputs_state: No template variables are used. Input is only template loading via `load_and_render_safe`; output is the markdown block written to stderr by the validator. It performs no state mutation.
- gates_or_invariants: The corresponding gate lives in `hooks/loop-bash-validator.sh:88-105`: when an active RLCR loop exists, state is parsed, `push_every_round` is read, and `git push` is blocked with exit `2` unless `STATE_PUSH_EVERY_ROUND == "true"`. This template preserves that invariant by explaining that pushes are opt-in per loop start.
- dependencies_and_callers: Direct caller is `hooks/loop-bash-validator.sh:96-104`, which loads `block/git-push.md` with an inline fallback. The state value comes from `parse_state_file_strict` after selecting `state.md` or `finalize-state.md` at `hooks/loop-bash-validator.sh:74-86`. Template loader behavior is provided by `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: Since there are no variables, rendering is low risk. If the file is missing or empty, `load_and_render_safe` falls back to the inline message in `hooks/loop-bash-validator.sh:99-103`. The command detector only checks commands matching `^[[:space:]]*git[[:space:]]+push` at `hooks/loop-bash-validator.sh:98`, so wrapped push commands depend on broader Bash-validator command parsing rather than this template.
- validation_or_tests: Template loading is checked by `tests/test-template-loader.sh` references for `block/git-push.md`; comprehensive template tests verify the real template is preferred over fallback (`tests/test-templates-comprehensive.sh:456-462` per repository search). Push-policy caller references are visible in `hooks/loop-bash-validator.sh:88-105`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt footer fragment appended to next-round Claude instructions when `push_every_round` is enabled, preserving the loop’s remote-sync obligation across rounds.
- algorithmic_behavior: Static two-line note: when `--push-every-round` is enabled, the agent must push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:1-2`). It does not decide whether pushing is required; it encodes the required instruction after state says pushing is required.
- inputs_outputs_state: No variables. Input is template loading; output is appended markdown in a generated next-round prompt. It mutates no state by itself, but its caller appends it to `round-${NEXT_ROUND}-prompt.md`.
- gates_or_invariants: Complements the stop-hook unpushed-commit gate. When `PUSH_EVERY_ROUND == "true"`, `hooks/loop-codex-stop-hook.sh:576-602` blocks exit if local branch status shows commits ahead of remote. The note is appended only under the same state flag at `hooks/loop-codex-stop-hook.sh:1542-1549`, keeping prompt instructions aligned with the exit gate.
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh:1542-1549`, inside next-round prompt generation after review feedback. Setup script also has related inline text for initial loop prompt generation per search hits in `scripts/setup-rlcr-loop.sh:962-966`. It depends on `load_template` from `hooks/lib/template-loader.sh:36-48`.
- edge_cases_or_failure_modes: If missing, caller falls back to `Also push your changes after committing.` at `hooks/loop-codex-stop-hook.sh:1544-1547`. Because it has no variables, it is unaffected by substitution escaping. The note alone does not guarantee remote sync; enforcement depends on the stop-hook gate and git status detecting ahead commits.
- validation_or_tests: Indirectly covered by template reference/loading suites and by stop-hook push enforcement around `push_every_round`. No dedicated test for this exact two-line file was found in the assigned context, but caller logic is explicit and fallback-safe.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-143 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for the prompt-template rendering subsystem used throughout RLCR hooks and validators. It validates that algorithmic prompt/block generation remains stable under large content and special-character inputs.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and shared test helpers at `tests/robustness/test-template-stress-robustness.sh:14-18`, creates a temp directory, then exercises `render_template`, `load_and_render`, and `load_and_render_safe`. The template loader implements single-pass `{{VAR}}` substitution with `awk` and environment-prefixed variables at `hooks/lib/template-loader.sh:58-132`, keeping missing placeholders unchanged and preventing substituted values from being rescanned.
- inputs_outputs_state: Inputs are inline template strings, generated template files in `$TEST_DIR`, and `VAR=value` argument arrays. Outputs are rendered strings and pass/fail counters. The test creates standard and large template files (`tests/robustness/test-template-stress-robustness.sh:57-66`, `tests/robustness/test-template-stress-robustness.sh:111-124`) but uses a temp directory with cleanup from `tests/test-helpers.sh:84-89`.
- gates_or_invariants: Core invariants under stress: standard substitution works (`tests/robustness/test-template-stress-robustness.sh:33-42`); missing templates use fallback (`tests/robustness/test-template-stress-robustness.sh:44-52`); 10KB and 100KB variable values render without truncation (`tests/robustness/test-template-stress-robustness.sh:82-109`); a roughly 100KB template renders with substitution (`tests/robustness/test-template-stress-robustness.sh:111-132`); 50 substitutions work in one call (`tests/robustness/test-template-stress-robustness.sh:134-151`); regex, ampersand, backslash, newline, empty, dollar-sign, repeated, boundary, and placeholder-only values render as expected (`tests/robustness/test-template-stress-robustness.sh:161-283`). The injection-prevention invariant is explicit: a variable value containing `{{VAR_B}}` must remain literal, not trigger a second substitution (`tests/robustness/test-template-stress-robustness.sh:218-228`).
- dependencies_and_callers: Depends on Bash, `awk`, `grep`, `wc`, `date`, `seq`, and the repository’s `hooks/lib/template-loader.sh`. The loader is widely used by RLCR/PR hooks and validators for block decisions and generated prompts, including stop-hook block templates, finalize/review prompts, next-round prompts, and git-push blocks. Shared summary behavior comes from `tests/test-helpers.sh:58-78`.
- edge_cases_or_failure_modes: The test intentionally avoids circular-reference behavior because the renderer does not support references between variables (`tests/robustness/test-template-stress-robustness.sh:5-9`). It measures elapsed time but does not enforce a timeout/performance threshold beyond successful completion. The “Unicode” case uses basic ASCII text despite the label (`tests/robustness/test-template-stress-robustness.sh:230-239`), so true non-ASCII stress is not covered here. Very large environment values can still hit shell/environment-size limits outside the tested 100KB range.
- validation_or_tests: This file is itself the validation suite, ending with `print_test_summary` and `exit $?` at `tests/robustness/test-template-stress-robustness.sh:285-290`. Broader template behavior is also covered by `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and `tests/robustness/test-template-error-robustness.sh` per repository references. I did not execute this script in the read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique Item Evidence headings, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`