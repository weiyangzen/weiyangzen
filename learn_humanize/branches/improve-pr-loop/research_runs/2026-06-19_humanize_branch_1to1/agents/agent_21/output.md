# agent_21 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-021 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the PR review loop; it is a thin command wrapper around `scripts/setup-pr-loop.sh` via `allowed-tools` and the command body at `commands/start-pr-loop.md:1-14`.
- algorithmic_behavior: Defines the high-level loop: detect current branch PR, fetch selected bot comments, ask the agent to fix issues, require push plus bot trigger comment, then let the Stop Hook poll and use local Codex validation before either continuing or ending; see `commands/start-pr-loop.md:16-24` and `commands/start-pr-loop.md:37-46`.
- inputs_outputs_state: Inputs are `$ARGUMENTS` with required bot flags `--claude` or `--codex`, optional `--max`, `--codex-model`, and `--codex-timeout` from `commands/start-pr-loop.md:3` and `commands/start-pr-loop.md:25-30`. The delegated setup script creates `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, and `round-0-prompt.md`; the script writes state fields such as `current_round`, `max_iterations`, `pr_number`, `active_bots`, Codex settings, poll settings, startup case, and trigger metadata at `scripts/setup-pr-loop.sh:615-634`.
- gates_or_invariants: At least one bot flag is required (`commands/start-pr-loop.md:25-30`; enforced in `scripts/setup-pr-loop.sh:179-190`). The workflow requires summaries, pushed fixes, correct bot mentions, and no direct state-file tampering or cancel-command cheating (`commands/start-pr-loop.md:50-57`).
- dependencies_and_callers: Depends on `${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh` (`commands/start-pr-loop.md:4` and `commands/start-pr-loop.md:12-14`). The setup path further depends on `gh`, `codex`, current Git branch, PR lookup, `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, template loading, and shared helpers from `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:208-277`, `scripts/setup-pr-loop.sh:430-438`).
- edge_cases_or_failure_modes: Existing RLCR or PR loops block startup (`scripts/setup-pr-loop.sh:220-241`); non-Git repos, missing commits, missing or unauthenticated `gh`, missing `codex`, absent/merged/closed PRs, YAML-unsafe branch/model/effort values, and failed trigger/Claude-eyes verification also block or clean up loop state (`scripts/setup-pr-loop.sh:243-407`, `scripts/setup-pr-loop.sh:525-599`).
- validation_or_tests: Covered indirectly by setup and PR-loop suites, especially argument/help/error paths in `tests/robustness/test-setup-scripts-robustness.sh` and system integration references in `tests/test-pr-loop-system.sh`; command is not itself executable beyond delegating to the setup script.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-051 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for `detect_review_issues`, the Codex-review log analysis gate used by the Stop Hook to decide whether the loop continues with review issues or can proceed toward finalization; test purpose and algorithm are stated at `tests/test-codex-review-merge.sh:3-16`.
- algorithmic_behavior: Creates isolated `LOOP_DIR` and `CACHE_DIR`, writes synthetic `round-N-codex-review.log` files, calls `detect_review_issues N`, and asserts exit code plus extracted content. The implementation scans the entire log, finds the first line whose first 10 characters contain `[P<digit>]`, extracts from that line through EOF, writes `round-N-review-result.md`, and returns success for issues; see `hooks/lib/loop-common.sh:332-387`.
- inputs_outputs_state: Inputs are `CACHE_DIR/round-${round}-codex-review.log`, global `LOOP_DIR`, and round number (`tests/test-codex-review-merge.sh:48-54`, `hooks/lib/loop-common.sh:349-352`). Outputs are stdout beginning with a Codex-review issues header, stderr diagnostics, return codes 0/1/2, and the audit result file `LOOP_DIR/round-${round}-review-result.md` (`hooks/lib/loop-common.sh:381-386`).
- gates_or_invariants: Issue markers count only when `[P0]` through `[P9]` appears within `substr($0, 1, 10)` (`hooks/lib/loop-common.sh:364-372`). Missing or empty logs are hard errors with return 2, no marker returns 1, and marker detection returns 0 (`hooks/lib/loop-common.sh:354-391`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` directly (`tests/test-codex-review-merge.sh:41-42`). Runtime caller is `hooks/loop-codex-stop-hook.sh`, which runs Codex review and invokes `detect_review_issues "$round"` to branch on review outcome (`hooks/loop-codex-stop-hook.sh:986-1015`).
- edge_cases_or_failure_modes: Covers markers outside first 10 characters ignored (`tests/test-codex-review-merge.sh:83-107`), absent markers (`tests/test-codex-review-merge.sh:109-131`), missing and empty logs (`tests/test-codex-review-merge.sh:133-169`), long logs with early or late issues (`tests/test-codex-review-merge.sh:171-227`), multiple issue lines with extraction from the first marker (`tests/test-codex-review-merge.sh:229-255`), marker at line start and dash-prefixed marker (`tests/test-codex-review-merge.sh:257-303`), result-file creation (`tests/test-codex-review-merge.sh:305-330`), and exactly 50-line logs (`tests/test-codex-review-merge.sh:332-354`).
- validation_or_tests: Self-contained test script maintains local pass/fail counters and exits nonzero if any assertion fails (`tests/test-codex-review-merge.sh:23-35`, `tests/test-codex-review-merge.sh:356-368`). It is included in the aggregate runner at `tests/run-all-tests.sh:46`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-081 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block inserted into the Stop Hook’s Git-cleanliness gate when untracked non-`.humanize` files are present, steering the agent toward ignore hygiene instead of blindly committing generated artifacts.
- algorithmic_behavior: Provides a concise note that untracked files may be build artifacts, test outputs, or runtime-generated files and usually belong in `.gitignore`, with examples for build outputs, dependencies, IDE/editor files, logs, cache, and temporary files at `prompt-template/block/git-not-clean-untracked.md:2-10`.
- inputs_outputs_state: No template variables are used in this block. It is loaded as static text and concatenated into `SPECIAL_NOTES`; the rendered parent block becomes the JSON `reason` for a Stop Hook block decision (`hooks/loop-codex-stop-hook.sh:518-565`).
- gates_or_invariants: The gate is triggered when cached Git status is nonempty and `OTHER_UNTRACKED` contains untracked files other than `.humanize` paths (`hooks/loop-codex-stop-hook.sh:521-545`). The invariant is that the loop cannot proceed to expensive review/finalization while the worktree has uncommitted changes (`hooks/loop-codex-stop-hook.sh:548-565`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh:540`; embedded through `prompt-template/block/git-not-clean.md`, whose required actions include reviewing untracked files, staging real changes, and committing with project conventions (`prompt-template/block/git-not-clean.md:1-16`).
- edge_cases_or_failure_modes: If the template fails to load, the Stop Hook falls back to “Review untracked files - add to .gitignore or commit them” (`hooks/loop-codex-stop-hook.sh:540-543`). It does not classify specific untracked paths itself; classification is limited to `.humanize` versus other untracked status lines in the hook (`hooks/loop-codex-stop-hook.sh:526-544`).
- validation_or_tests: Template reference is discoverable via `rg`; broader template loader/reference coverage exists in `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/test-templates-comprehensive.sh`, while the concrete call site is `hooks/loop-codex-stop-hook.sh:540`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-111 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Next-round instruction template for the RLCR/PR-style loop when Codex review finds issues and the Stop Hook needs to hand actionable feedback back to Claude.
- algorithmic_behavior: Forces continuation by stating work is not finished, requires reading the original plan via `@{{PLAN_FILE}}`, requires Todos for all discovered issues rather than only the highest-priority ones, embeds Codex review content inside explicit start/end markers, and makes the goal tracker read-only after Round 0 with update requests routed through the summary (`prompt-template/claude/next-round-prompt.md:1-31`).
- inputs_outputs_state: Inputs are `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`; the Stop Hook renders them into `round-${NEXT_ROUND}-prompt.md` after incrementing `current_round` in the state file (`hooks/loop-codex-stop-hook.sh:1505-1527`). It also creates `round-${NEXT_ROUND}-summary.md` and appends footer, optional push note, and goal-tracker update-request text (`hooks/loop-codex-stop-hook.sh:1510-1555`).
- gates_or_invariants: The template enforces plan alignment, exhaustive issue tracking, and read-only goal-tracker discipline after Round 0 (`prompt-template/claude/next-round-prompt.md:5-13`, `prompt-template/claude/next-round-prompt.md:22-31`). The state transition invariant is that the prompt is only generated after review issues are found and `current_round` is advanced to `NEXT_ROUND` (`hooks/loop-codex-stop-hook.sh:1501-1508`).
- dependencies_and_callers: Rendered through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:1523-1526`; depends on template-loader behavior, review-content production from `detect_review_issues`, and downstream footer templates `claude/next-round-footer.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md`.
- edge_cases_or_failure_modes: If the template cannot be loaded, the hook falls back to a shorter inline prompt containing Codex review and plan/goal references (`hooks/loop-codex-stop-hook.sh:1514-1523`). The template itself does not validate placeholder presence; missing or empty rendered values would degrade instructions but not stop generation unless the loader fails.
- validation_or_tests: Referenced directly by `hooks/loop-codex-stop-hook.sh:1523`; template existence and render safety are covered by the repository’s template tests (`tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-141 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for loop state discovery and state-frontmatter parsing, especially valid round progression, finalize/cancel handling, invalid schema cases, and active loop directory selection.
- algorithmic_behavior: Builds synthetic `.humanize/rlcr/<timestamp>` directories and state files, then exercises `parse_state_file_strict`, `get_current_round`, and `find_active_loop`; helper state writers are defined at `tests/robustness/test-state-transition-robustness.sh:30-89`.
- inputs_outputs_state: Inputs are generated `state.md`, `finalize-state.md`, and `cancel-state.md` files. Outputs are pass/fail assertions via `tests/test-helpers.sh`, plus process exit from `print_test_summary` at `tests/robustness/test-state-transition-robustness.sh:420-424`. The implementation reads frontmatter fields such as `current_round`, `max_iterations`, `base_branch`, and `review_started` (`hooks/lib/loop-common.sh:255-330`).
- gates_or_invariants: Strict parsing requires file existence, at least two YAML separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`; `current_round` and `max_iterations` must be numeric with optional leading minus; `review_started` must be boolean (`hooks/lib/loop-common.sh:258-323`). Active loop discovery checks only the lexicographically newest immediate child directory and accepts `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151-174`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and shared test helpers at `tests/robustness/test-state-transition-robustness.sh:14-19`. The same parsing/discovery functions are used by validators and hooks including `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: Validates round 0, arbitrary positive rounds, and max round (`tests/robustness/test-state-transition-robustness.sh:98-138`); finalize state detection and coexistence with regular state (`tests/robustness/test-state-transition-robustness.sh:148-172`); cancel state not active and newer regular state after cancel (`tests/robustness/test-state-transition-robustness.sh:182-207`); negative and over-max rounds parsed but semantic enforcement left elsewhere (`tests/robustness/test-state-transition-robustness.sh:217-257`); nonnumeric and missing required fields rejected (`tests/robustness/test-state-transition-robustness.sh:259-361`); newest/lexicographic directory choice and nested misplaced state ignored (`tests/robustness/test-state-transition-robustness.sh:371-417`).
- validation_or_tests: The file is itself executable validation and is included by the aggregate runner at `tests/run-all-tests.sh:71`. It specifically distinguishes parser validation from semantic loop enforcement, making negative and over-max rounds known tolerated parse outputs rather than parse failures.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_PR_LOOP-HZ-021, IMPROVE_PR_LOOP-HZ-051, IMPROVE_PR_LOOP-HZ-081, IMPROVE_PR_LOOP-HZ-111, IMPROVE_PR_LOOP-HZ-141
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`