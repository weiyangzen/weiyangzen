# agent_30 improve-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `61f03daecb8ff9c20e535a636b90aa92a3d7c9b2`

## Item Evidence

### IMPROVE_PR_LOOP-HZ-030 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse guard for the `Write` tool. It protects RLCR and PR-loop state-machine files from writes that would corrupt loop state, bypass generated artifacts, or write the wrong round’s resolution or summary. The hook is registered for `Write` in `hooks/hooks.json:16-20` and shares its classifier/parser logic with `hooks/lib/loop-common.sh`.
- algorithmic_behavior: Reads hook JSON from stdin, validates it, ignores non-`Write` tools, then applies ordered path gates. The first gates reject invalid hook input and missing `tool_input.file_path` (`hooks/loop-write-validator.sh:25-49`). It blocks RLCR `round-*-todos.md` unless the target is in a narrow active-loop allowlist (`hooks/loop-write-validator.sh:55-63`, allowlist in `hooks/lib/loop-common.sh:420-441`), blocks prompt files (`hooks/loop-write-validator.sh:65-68`), and applies PR-loop protection before RLCR summary logic (`hooks/loop-write-validator.sh:74-94`). For PR loops it blocks `state.md`, generated PR-loop read-only files, and allows only current-round `round-N-pr-resolve.md` writes after `validate_pr_resolve_round` passes. For RLCR it finds the active loop, detects finalize phase, strictly parses state, blocks state/finalize-state writes, permits active `finalize-summary.md`, blocks `plan.md` backup writes, blocks `goal-tracker.md` after round 0, blocks summaries outside `.humanize/rlcr`, validates summary round number, and finally validates that loop-local summary paths are in the active loop directory (`hooks/loop-write-validator.sh:100-263`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.file_path`. It uses `CLAUDE_PROJECT_DIR` or `pwd` as project root (`hooks/loop-write-validator.sh:56`, `hooks/loop-write-validator.sh:124`). Outputs are process exit status and stderr block messages rendered from `prompt-template/block/*` with inline fallbacks. Exit `0` means allowed or irrelevant; exit `1` means malformed/unsafe input or malformed loop state; exit `2` means intentional tool-use block. It reads but does not mutate loop state. State inputs include `.humanize/rlcr/<timestamp>/state.md`, optional `.humanize/rlcr/<timestamp>/finalize-state.md`, and `.humanize/pr-loop/<timestamp>/state.md`.
- gates_or_invariants: JSON must be valid, UTF-8, not null-byte-bearing, and not deeper than 30 levels through `validate_hook_input` and `is_deeply_nested` (`hooks/lib/loop-common.sh:59-133`). Write hooks require `tool_input.file_path` (`hooks/loop-write-validator.sh:43-46`). RLCR state parsing is fail-closed via `parse_state_file_strict`, requiring frontmatter plus `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/loop-common.sh:246-329`). `finalize-state.md` is checked before generic `state.md` because the generic classifier also matches it (`hooks/loop-write-validator.sh:150-159`). After round 0, `goal-tracker.md` can no longer be written directly (`hooks/loop-write-validator.sh:192-195`). Summary writes must be in the active loop directory and match `current_round` unless explicitly allowlisted (`hooks/loop-write-validator.sh:202-263`). PR-loop generated files `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` are read-only; `pr-resolve` is writable only for the current PR round (`hooks/lib/loop-common.sh:816-887`).
- dependencies_and_callers: Depends on `jq`, `sed`, `grep`, `basename`, and `hooks/lib/loop-common.sh` (`hooks/loop-write-validator.sh:17-19`). `loop-common.sh` depends on `hooks/lib/template-loader.sh`, which performs single-pass placeholder rendering and fallback loading (`hooks/lib/template-loader.sh:50-203`). Related sibling validators are `hooks/loop-edit-validator.sh` and `hooks/loop-bash-validator.sh`, which enforce similar boundaries for other tool surfaces. PR-loop state helpers include `find_active_pr_loop`, `is_pr_loop_readonly_file`, and `validate_pr_resolve_round` (`hooks/lib/loop-common.sh:783-887`).
- edge_cases_or_failure_modes: If no active RLCR loop exists, RLCR-specific summary/state checks return allowed (`hooks/loop-write-validator.sh:128-130`), but PR-loop directory checks still run earlier for paths under `.humanize/pr-loop`. If the newest loop state is malformed, the hook blocks for safety (`hooks/loop-write-validator.sh:140-144`). Historical allowlisted files are allowed even if they do not match current round, specifically `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop dir (`hooks/lib/loop-common.sh:427-432`). `is_state_file_path` matches any `state.md` suffix, so order matters for `finalize-state.md`. Path classification is string/regex based and does not canonicalize symlinks here, so correctness depends on upstream tool paths being absolute or stable. Template absence is nonfatal because all block messages have fallbacks.
- validation_or_tests: Covered by focused tests: PR-loop state write blocked, PR comment write blocked, PR resolve allowed (`tests/test-pr-loop-hooks.sh:68-166`); wrong-round and correct-round PR resolve behavior (`tests/test-pr-loop-hooks.sh:1431-1497`); RLCR allowlist for todos/summaries and blocking non-allowlisted round files (`tests/test-allowlist-validators.sh:129-185`); plan backup write blocking (`tests/test-plan-file-hooks.sh:220-237`); finalize summary allowed and finalize state blocked (`tests/test-finalize-phase.sh:338-366`). Robustness tests also exercise valid JSON, binary-looking content, and large content through the production write hook (`tests/robustness/test-hook-input-robustness.sh`, `tests/robustness/test-hook-system-robustness.sh` references found by search).
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-060 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for setup-time RLCR plan-file and option validation. It verifies the guardrails in `scripts/setup-rlcr-loop.sh` that establish a safe, immutable plan input before the RLCR state machine starts.
- algorithmic_behavior: Creates disposable git repositories under `mktemp -d`, mocks `codex` in a temporary `bin` directory, runs `scripts/setup-rlcr-loop.sh` with specific arguments, captures exit status and stderr/stdout, and asserts error text or non-error text. The helper repo has an initial commit, `plans/test-plan.md`, and `plans/` in `.gitignore` (`tests/test-plan-file-validation.sh:36-83`). The script is organized as sequential scenario tests and exits with the number of failed tests (`tests/test-plan-file-validation.sh:742-751`).
- inputs_outputs_state: Inputs are generated plan files, git repository layouts, CLI flags, branch names, and mocked `codex`. Outputs are PASS/FAIL/SKIP lines and a final exit code equal to `TESTS_FAILED`. It mutates only temporary directories and unsets `CLAUDE_PROJECT_DIR` so production setup uses the temp repo as project root (`tests/test-plan-file-validation.sh:19-21`). It intentionally invokes production setup instead of reimplementing validation.
- gates_or_invariants: The tested setup invariants include: reject absolute paths (`tests/test-plan-file-validation.sh:88-101`, implemented at `scripts/setup-rlcr-loop.sh:320-324`); reject missing files and missing parent directories (`tests/test-plan-file-validation.sh:103-125`, implementation `scripts/setup-rlcr-loop.sh:352-386`); reject paths or filenames containing whitespace (`tests/test-plan-file-validation.sh:127-166`, implementation `scripts/setup-rlcr-loop.sh:326-332`); reject shell metacharacters in plan paths (`tests/test-plan-file-validation.sh:168-186`, implementation `scripts/setup-rlcr-loop.sh:334-341`); reject symlink plan files and symlink parent path traversal (`tests/test-plan-file-validation.sh:188-199`, implementation `scripts/setup-rlcr-loop.sh:346-380`); handle inaccessible or unresolvable parent dirs gracefully (`tests/test-plan-file-validation.sh:201-242`, implementation `scripts/setup-rlcr-loop.sh:394-406`); reject `../` escaping outside the project (`tests/test-plan-file-validation.sh:244-271`); reject non-git repos and repos without commits (`tests/test-plan-file-validation.sh:273-322`, implementation `scripts/setup-rlcr-loop.sh:293-307`); enforce tracked-plan mode semantics (`tests/test-plan-file-validation.sh:328-425`, implementation `scripts/setup-rlcr-loop.sh:423-472`); reject YAML-unsafe branch/model/effort values (`tests/test-plan-file-validation.sh:431-510`, `tests/test-plan-file-validation.sh:700-740`, implementation `scripts/setup-rlcr-loop.sh:561-587`); and reject plans with insufficient real content after removing blank lines, `#` comments, and HTML comments (`tests/test-plan-file-validation.sh:517-660`, implementation `scripts/setup-rlcr-loop.sh:476-541`).
- dependencies_and_callers: Depends on `git`, `mktemp`, `chmod`, shell heredocs, and the production `scripts/setup-rlcr-loop.sh`. Production setup depends on `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, `codex` availability, and git commands with `GIT_TIMEOUT=30` (`scripts/setup-rlcr-loop.sh:24-30`, `scripts/setup-rlcr-loop.sh:543-548`). The test supplies a mock `codex` to keep validation fast and local (`tests/test-plan-file-validation.sh:73-83`).
- edge_cases_or_failure_modes: Some branch-name tests accept either production rejection or git’s own branch rejection because git versions may disallow certain names before setup sees them (`tests/test-plan-file-validation.sh:459-510`). The permission-denial path-resolution test can skip if chmod restrictions are unavailable (`tests/test-plan-file-validation.sh:225-240`). The “valid plan accepted” tests only assert that content validation did not fail; setup may still fail later for unrelated environment reasons (`tests/test-plan-file-validation.sh:629-630`, `tests/test-plan-file-validation.sh:655-656`, `tests/test-plan-file-validation.sh:735-736`). The test treats Markdown headings beginning with `#` as comments for the content-count gate, so valid fixture content uses plain text headings (`tests/test-plan-file-validation.sh:609-624`).
- validation_or_tests: This file is itself the validation artifact. It maps directly to `setup-rlcr-loop.sh` validation blocks for CLI parsing (`scripts/setup-rlcr-loop.sh:120-224`, `scripts/setup-rlcr-loop.sh:263-291`), git repo checks (`scripts/setup-rlcr-loop.sh:293-307`), plan path checks (`scripts/setup-rlcr-loop.sh:313-421`), plan tracking checks (`scripts/setup-rlcr-loop.sh:423-472`), content checks (`scripts/setup-rlcr-loop.sh:476-541`), branch/model safety (`scripts/setup-rlcr-loop.sh:561-587`), and loop state creation (`scripts/setup-rlcr-loop.sh:667-724`) once validation passes.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-090 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Blocking prompt template for a PR-loop transition gate: when the PR loop requires a user trigger comment but none has been detected, this template explains why the loop cannot proceed and tells the user to post a bot mention.
- algorithmic_behavior: Defines a rendered Markdown block with placeholders for `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING` (`prompt-template/block/no-trigger-comment.md:1-17`). It is loaded in `hooks/pr-loop-stop-hook.sh` when `REQUIRE_TRIGGER` is true and `PR_LAST_TRIGGER_AT` is empty (`hooks/pr-loop-stop-hook.sh:727-745`). The rendered message becomes the `reason` field of a JSON hook response with `"decision": "block"`, while `systemMessage` is a short PR-loop missing-trigger message (`hooks/pr-loop-stop-hook.sh:739-744`).
- inputs_outputs_state: Inputs are computed by the stop hook from PR-loop state and GitHub comment detection: startup case, current round, configured bot mention string, and a case description (`hooks/pr-loop-stop-hook.sh:727-741`). Output is user-facing Markdown embedded in JSON to block the stop/exit flow. It does not mutate files or state. Its effect is to hold the PR loop before review polling until a valid trigger timestamp exists.
- gates_or_invariants: The gate is explicit-trigger presence. Startup cases 4 and 5 get special descriptions for new commits after prior reviews; other cases describe a subsequent-round trigger requirement (`hooks/pr-loop-stop-hook.sh:727-734`). The hook validates trigger presence before Claude eyes reaction checks, avoiding wasted reaction polling on stale or absent trigger IDs (`hooks/pr-loop-stop-hook.sh:720-725`, `hooks/pr-loop-stop-hook.sh:748-759`). The template rendering path is safe-fallback and single-pass through `load_and_render_safe` and `render_template`, so missing templates use fallback text and placeholder-like content in values is not re-expanded (`hooks/lib/template-loader.sh:50-203`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh:739`. Trigger detection depends on GitHub CLI/API flow in the same stop hook: it builds mentions via `build_bot_mention_string` (`hooks/lib/loop-common.sh:764-777`), detects trigger comments, and updates `PR_LAST_TRIGGER_AT`/comment ID before reaching this gate. A related force-push template uses the same mention concept when a new trigger is needed after history rewrite (`prompt-template/block/force-push-detected.md`, found by search).
- edge_cases_or_failure_modes: If no template file is present, fallback text still blocks with a concrete `gh pr comment` command (`hooks/pr-loop-stop-hook.sh:736-739`). If `PR_STARTUP_CASE` is empty, the caller defaults it to case 1 in the rendered variables (`hooks/pr-loop-stop-hook.sh:730-741`). If trigger detection fails to set `PR_LAST_TRIGGER_AT`, this template blocks even if a user believes they commented, preventing review polling from using an unsafe timestamp window. A defensive later block also exists for round greater than zero with no trigger if control reaches polling setup unexpectedly (`hooks/pr-loop-stop-hook.sh:847-866`).
- validation_or_tests: PR-loop tests cover missing-trigger blocking and trigger detection. Search hits show `tests/test-pr-loop-hooks.sh:618` for missing trigger blocks, `tests/test-pr-loop-hooks.sh:845-1043` for trigger detection including pagination, and `tests/test-pr-loop-stophook.sh:642-677` for missing-trigger cases. Template reference tests include `tests/test-template-references.sh`, and template rendering behavior is covered by `tests/test-template-loader.sh`.
- skip_candidate: `no`

### IMPROVE_PR_LOOP-HZ-120 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: Prompt fragment injected into Codex’s PR-review validation prompt to make Codex update `.humanize/pr-loop/<timestamp>/goal-tracker.md` after analyzing bot reviews. It is part of the PR-loop accounting contract for issue totals and per-round review status.
- algorithmic_behavior: Tells Codex to update `{{GOAL_TRACKER_FILE}}` after analysis, with three required mutations: add one Issue Summary row for `{{NEXT_ROUND}}`, update Total Statistics, and add a Round `{{NEXT_ROUND}}` Issue Log entry (`prompt-template/pr-loop/codex-goal-tracker-update.md:1-21`). It gives a concrete Markdown example with separate reviewer rows and statuses `Issues Found`, `All Resolved`, or `Approved` (`prompt-template/pr-loop/codex-goal-tracker-update.md:22-55`). It constrains Codex to preserve file structure, update only the named sections, leave PR Information and Ultimate Goal headers alone, append rather than replace, and keep one Issue Summary row per reviewer (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `NEXT_ROUND`, supplied by `hooks/pr-loop-stop-hook.sh:1252-1261`. Output is not a standalone file mutation by the template itself; it becomes instructions embedded in `round-N-codex-prompt.md` (`hooks/pr-loop-stop-hook.sh:1263-1302`). Codex then runs against the project root and writes its analysis to `round-N-pr-check.md` while potentially updating the goal tracker (`hooks/pr-loop-stop-hook.sh:1322-1333`). The stop hook also has a fallback updater that calls `update_pr_goal_tracker` if Codex does not update or only partially updates (`hooks/pr-loop-stop-hook.sh:1371-1375`, `hooks/pr-loop-stop-hook.sh:1526-1532`).
- gates_or_invariants: Goal tracker rows are keyed by round and reviewer; each reviewer gets a separate row (`prompt-template/pr-loop/codex-goal-tracker-update.md:63-64`). The stop hook’s fallback updater enforces idempotency by checking the specific round plus reviewer combination in both Issue Summary and Issue Log before appending (`hooks/lib/loop-common.sh:1178-1208`). Issue resolution accounting is conservative: in the non-final approval path, issues found this round are not counted as resolved because full approval has not happened (`hooks/pr-loop-stop-hook.sh:1518-1524`). In the all-approved path, the hook records a zero-new-issues result for reviewer `All` before moving state to `approve-state.md` (`hooks/pr-loop-stop-hook.sh:1367-1378`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh:1261`, which loads this template with `load_and_render_safe`. It depends on `hooks/lib/template-loader.sh` for safe single-pass rendering and fallback behavior. It coordinates with `update_pr_goal_tracker` in `hooks/lib/loop-common.sh:1151-1225+`, Codex prompt construction (`hooks/pr-loop-stop-hook.sh:1242-1302`), Codex result classification (`hooks/pr-loop-stop-hook.sh:1359-1413`), active bot updates (`hooks/pr-loop-stop-hook.sh:1415-1496`), and PR-loop setup’s initial goal tracker creation (`scripts/setup-pr-loop.sh:641-715`).
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook injects a shorter fallback instruction (`hooks/pr-loop-stop-hook.sh:1258-1261`). Codex may fail, time out, or produce empty output; those cases block before tracker/result transitions (`hooks/pr-loop-stop-hook.sh:1304-1357`). Codex may also update the tracker imperfectly, so `update_pr_goal_tracker` performs partial-update completion and duplicate avoidance (`hooks/lib/loop-common.sh:1184-1218`). The template’s example uses reviewer names such as `claude` and `codex`; the fallback updater intentionally uses lowercase `codex` to avoid duplicate rows with configured bot names (`hooks/pr-loop-stop-hook.sh:1526-1531`).
- validation_or_tests: Goal-tracker parsing and update behavior is tested in PR-loop system and robustness tests. Search found `tests/test-pr-loop-system.sh:1541-1728`, which creates an initial tracker, mocks Codex output ending in `ISSUES_REMAINING`, runs the stop hook, and verifies a Round 1 row was added. `tests/test-pr-loop-system.sh:1805` includes PR goal tracker parsing, and `tests/test-pr-loop-system.sh:1886` runs the stop-hook tracker update test. Robustness coverage exists in `tests/robustness/test-goal-tracker-robustness.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`