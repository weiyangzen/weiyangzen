# agent_15 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt contract directory for the RLCR state machine. It defines what the implementation agent must do when a stop hook blocks exit, when Codex requests another round, when review/finalize/methodology phases begin, when drift recovery is required, and when Agent Teams mode is active.
- algorithmic_behavior: The directory is flat, with 14 markdown fragments and no recursive child subdirectories. Runtime code loads these templates through `hooks/lib/template-loader.sh`, primarily from `hooks/loop-codex-stop-hook.sh`. Initial Agent Teams prompts are appended by `scripts/setup-rlcr-loop.sh:1375-1384`; later loop transitions load specific Claude templates in the stop hook at `hooks/loop-codex-stop-hook.sh:1382`, `1410`, `1575`, `2080`, `2090`, `2153`, `2178`, `2187`, `2193`, `2201`, and `2210-2216`. Methodology analysis uses `hooks/lib/methodology-analysis.sh:82-87`.
- inputs_outputs_state: Inputs are rendered variables such as `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, `SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, `STALL_COUNT`, and `LAST_MAINLINE_VERDICT`. Outputs are prompt files like `.humanize/rlcr/<loop>/round-N-prompt.md` or JSON hook block `reason` content. State transitions encoded by the templates include implementation continuation, review-fix loop, drift recovery, finalize phase, skipped-review finalize phase, methodology analysis phase, and Agent Teams continuation.
- gates_or_invariants: The templates enforce re-anchoring on the plan and goal tracker before code (`next-round-prompt.md:12-27`, `review-phase-prompt.md:5-12`), a single mainline objective (`next-round-prompt.md:20-25`, `drift-replan-prompt.md:25-31`), task lane tags `[mainline]`, `[blocking]`, `[queued]` (`next-round-prompt.md:29-45`), no queued work taking over the round (`next-round-prompt.md:67-75`), finalize-only functionality-equivalent refactoring (`finalize-phase-prompt.md:16-24`), and Agent Teams delegation with strict file ownership (`agent-teams-core.md:1-31`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` is the dominant caller. It builds review/fix prompts and appends footer, push, open-question, post-alignment, and goal-tracker request fragments (`loop-codex-stop-hook.sh:2140-2205`). `scripts/setup-rlcr-loop.sh` uses `agent-teams-instructions.md` and `agent-teams-core.md` for round 0 Agent Teams setup (`setup-rlcr-loop.sh:1375-1384`). `hooks/lib/methodology-analysis.sh` renders `methodology-analysis-prompt.md` before terminal-state conversion (`methodology-analysis.sh:74-99`). Template loading depends on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: Missing templates usually fall back through `load_and_render_safe`, so a missing file degrades prompt richness rather than crashing in many stop-hook paths. Agent Teams setup in `setup-rlcr-loop.sh` checks template existence and has an inline fallback (`setup-rlcr-loop.sh:1384-1394`). Open-question notice insertion depends on a simple short-line heuristic containing `Open Question` (`loop-codex-stop-hook.sh:2140-2157`). Drift recovery only uses `drift-replan-prompt.md` after two consecutive stalled/regressed mainline verdicts, while three triggers the circuit breaker (`loop-codex-stop-hook.sh:1861-1872`, `1971-1974`).
- validation_or_tests: `tests/test-agent-teams.sh` checks the Agent Teams header/core/continuation templates are present. `tests/test-finalize-phase.sh:923-944` validates drift recovery prompt creation and confirms the generated prompt contains `Drift Recovery Mode`. Template reference coverage is also checked by `tests/test-template-references.sh`, which scans shell template references and fails on missing template files.
- skip_candidate: `no`

### DEV-HZ-045 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Primary RLCR stop-hook state machine. It intercepts Claude stop attempts, validates loop state and working tree conditions, runs Codex review, advances phases, blocks unsafe exits, and generates the next instruction prompt.
- algorithmic_behavior: The hook reads JSON hook input from stdin (`loop-codex-stop-hook.sh:28`), resolves the active loop by session (`61-68`), handles background-task short-circuits (`71-83`), determines active phase from `state.md`, `finalize-state.md`, or `methodology-analysis-state.md` (`91-101`), parses YAML frontmatter state (`107-170`), validates state schema/model/effort (`172-199`), then executes a sequence of quick checks before review. It validates branch consistency (`286-316`), plan integrity outside review phase (`319-399`), incomplete tasks through `check-todos-from-transcript.py` (`401-457`), git status and large changed files (`492-604`), methodology analysis completion (`606-671`), git cleanliness/push requirements (`673-789`), summary/contract existence (`791-862`), BitLesson delta (`864-883`), goal tracker initialization (`885-961`), max iteration handling (`963-980`), finalize completion (`982-998`), Codex review prompt generation (`1007-1132`), Codex CLI setup (`1140-1221`), Codex summary review (`1685-1833`), marker parsing (`1835-1855`), COMPLETE/STOP/drift handling (`1879-2008`), and next-round prompt generation (`2014-2244`).
- inputs_outputs_state: Inputs include hook JSON, transcript path/session id, `.humanize/rlcr/<loop>/state.md`, plan backup, goal tracker, round summary/contract files, Codex CLI output, git state, and template files. Outputs include JSON hook decisions with `decision: block`, generated review prompts/results, next round prompts, summary scaffolds, state mutations via `upsert_state_fields`, marker files such as `.review-phase-started`, final state renames such as `finalize-state.md`, `complete-state.md`, `stop-state.md`, or methodology state files, and debug logs under cache (`1162-1172`).
- gates_or_invariants: Fail-closed behavior is explicit for missing critical state fields (`186-199`), invalid model/effort (`172-184`), branch changes (`306-316`), plan deletion/modification (`333-397`), task checker parse/incomplete tasks (`415-456`), git-status failures (`501-515`), large changed code/docs files over 2000 lines (`524-603`), tracked `.humanize` state (`684-697`), dirty git state (`699-756`), unpushed commits when `push_every_round` is enabled (`762-787`), missing summary (`804-829`), missing round contract under anti-drift state (`831-862`), invalid BitLesson delta (`868-883`), missing mainline verdict (`1847-1853`), invalid review phase marker (`1946-1962`), and Codex review failure/no output (`1613-1683`, `1732-1830`).
- dependencies_and_callers: Sourced libraries are `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/methodology-analysis.sh` (`42-54`). It calls `check-todos-from-transcript.py` (`408-413`), `scripts/bitlesson-validate-delta.sh` (`868-875`), `codex exec` (`1719-1720`), and `codex review` (`1289-1291`). Template dependencies span `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude`.
- edge_cases_or_failure_modes: The hook cleans stale `.git/index.lock` after timed-out git status (`459-483`, `501-505`). Review phase cannot be entered by merely editing state because `.review-phase-started` is required (`1946-1962`). Codex COMPLETE is ignored during review phase; only `codex review` with no `[P0-9]` issues promotes to finalize (`1888-1893`, `1940-1969`). Missing `Mainline Progress Verdict` blocks implementation rounds unless STOP was returned (`1847-1853`). Methodology phase requires both non-empty `methodology-analysis-done.md` and non-empty report before terminal rename (`methodology-analysis.sh:114-169`, invoked at `617-670`).
- validation_or_tests: Covered broadly by `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-stop-hook-legacy-compat.sh`, `tests/test-stop-hook-bg-allow.sh`, `tests/test-disable-nested-codex-hooks.sh`, `tests/test-commit-history-section.sh`, and BitLesson/template tests. The drift recovery path is directly asserted in `tests/test-finalize-phase.sh:923-944`.
- skip_candidate: `no`

### DEV-HZ-075 `file` `tests/test-app-auth.sh`
- cursor: `[_]`
- core_role: Executable specification for the dashboard server auth/migration contract in `viz/server/app.py`. It is core-adjacent rather than RLCR loop control, but it protects Humanize monitor behavior that exposes RLCR sessions and cancellation endpoints.
- algorithmic_behavior: The script performs static grep/AST checks plus an optional Flask-backed behavioral probe. It checks CLI flags (`test-app-auth.sh:35-42`), remote bind token requirement (`44-50`), configurable bind host for `app.run` (`52-56`), auth middleware wiring (`58-75`), remote-mode WebSocket rejection (`77-82`), removal of legacy project persistence helpers (`84-111`), project mutation endpoints returning 410 (`113-121`), session-scoped cancel helper and missing-session-id 400 route (`123-168`), CSRF and cancellation fixes (`170-212`), and fail-closed `_request_authorized` behavior (`215-305`).
- inputs_outputs_state: Input is the source file `viz/server/app.py` (`21-23`) and optionally `VIZ_TEST_VENV` for runtime Flask import (`267-304`). Outputs are PASS/FAIL lines and process exit code 1 if any assertion failed (`307-316`). No repository state is modified by the test.
- gates_or_invariants: Remote binds must require `--auth-token`; `Authorization: Bearer` and `?token=` must both be accepted; non-localhost WebSocket transport must be disabled; `viz-projects.json` must not be used from non-comment code; project switch/add/remove must be gone with 410; cancel must be session-scoped and pass `--project`; CSRF must use mutating-method and request-relative origin checks; `_request_authorized` must deny non-loopback access when `AUTH_TOKEN` is empty (`221-255`).
- dependencies_and_callers: Depends on `grep`, `python3` AST parsing, and optionally a Flask-capable Python at `${VIZ_TEST_VENV:-/tmp/viz-routes-test-venv}`. It targets implementation anchors in `viz/server/app.py`, including `_request_authorized` (`app.py:72-87`), `_enforce_auth_and_csrf` (`397-415`), removed project mutators (`495-499`), cancel routes (`873-925`), WebSocket gate (`1553-1566`), and `main()` auth/trust-proxy startup gates (`1651-1730`).
- edge_cases_or_failure_modes: It intentionally skips the runtime probe as PASS if Flask is missing, relying on the AST/static check (`test-app-auth.sh:267-305`). Because many checks are grep-based, semantically equivalent refactors could fail if expected string anchors move or wording changes. Conversely, static checks may pass code that only looks right; the runtime probe mitigates that for `_request_authorized` when Flask is available.
- validation_or_tests: This is itself the validation artifact. It exits with failed count if any assertion fails (`312-314`) and prints a final success message only when all required checks pass (`316`).
- skip_candidate: `yes: it is an executable specification for dashboard auth and migration behavior, not the RLCR scheduler core state machine itself; still relevant because dashboard cancel/auth endpoints can affect active RLCR sessions`

### DEV-HZ-105 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop setup input validation around plan files, branch/model serialization safety, and clean git prerequisites.
- algorithmic_behavior: The script creates temporary git repos (`test-plan-file-validation.sh:36-71`), installs a mock `codex` command to avoid real Codex calls (`73-83`), and repeatedly invokes `scripts/setup-rlcr-loop.sh` against controlled path/git/content cases. It asserts rejection of absolute paths (`88-101`), nonexistent files/dirs (`103-125`), spaces (`127-166`), shell metacharacters (`168-186`), symlinks (`188-199`), permission/path resolution errors (`201-242`), project escapes (`244-271`), non-git repos (`273-296`), repos without commits (`298-322`), tracked/untracked plan mismatches (`328-425`), YAML-unsafe branch names (`431-511`), empty/comment-only/sparse plans (`517-607`), valid content (`609-660`), CLI option shape (`662-694`), and Codex model/effort validation (`696-740`).
- inputs_outputs_state: Inputs are temporary repos under `mktemp`, plan files written inside those repos, CLI args to `setup-rlcr-loop.sh`, and mock `codex` on PATH. Outputs are PASS/FAIL/SKIP counters and exit code equal to failed test count (`742-751`). It mutates only its temp directories, cleaned by trap (`37-38`).
- gates_or_invariants: Encodes setup invariants implemented in `scripts/setup-rlcr-loop.sh`: plan path must be relative (`460-464`), no spaces (`466-472`), no shell metacharacters (`474-481`), no symlink file or parent symlink (`486-520`), file must exist/read and resolve within project (`522-546`), not be inside submodule (`548-561`), tracked plan requires `--track-plan-file` and clean state (`587-601`), untracked/gitignored plan is required otherwise (`602-611`), minimum line/content count is enforced (`623-676`), branch/model/effort values must be YAML-safe (`697-720`), and working tree must be clean before loop start (`724-746`).
- dependencies_and_callers: Depends on `git`, `mktemp`, shell utilities, and `scripts/setup-rlcr-loop.sh`. The implementation anchors are in `scripts/setup-rlcr-loop.sh:404-720` for argument/path/content/model validation and `728-820` for working-tree/base-branch checks. The test unsets `CLAUDE_PROJECT_DIR` so setup resolves the temp repo as project root (`19-21`).
- edge_cases_or_failure_modes: Some branch-name tests accept Git rejecting an unsafe branch before Humanize sees it (`459-493`). Permission tests can skip if `chmod 000` is unsupported (`225-241`). Some “accept” cases only assert that a specific validation error is absent, because setup may later fail on unrelated environment conditions (`625-633`, `731-740`).
- validation_or_tests: This file is the validation artifact for plan setup hardening. It returns nonzero if any assertion fails (`751`).
- skip_candidate: `no`

### DEV-HZ-135 `file` `prompt-template/block/bitlesson-delta-invalid.md`
- cursor: `[_]`
- core_role: Block-message template for one BitLesson summary-validation gate: a present `## BitLesson Delta` section with an invalid or ambiguous `Action`.
- algorithmic_behavior: The template is short and static (`bitlesson-delta-invalid.md:1-7`). `scripts/bitlesson-validate-delta.sh` extracts the `## BitLesson Delta` block while ignoring fenced code and HTML comments (`bitlesson-validate-delta.sh:92-179`), parses candidate `Action:` lines (`202-204`), and if there is not exactly one action or the action is not `none`, `add`, or `update`, renders this template through `load_and_render_safe` (`206-217`).
- inputs_outputs_state: Inputs are the round summary file, BitLesson file path/relpath, allow-empty setting, template directory, and current round (`bitlesson-validate-delta.sh:24-67`). Output on invalid action is hook-compatible JSON with `decision: block`, rendered reason, and system message `Loop: BitLesson Delta must include action none/add/update...` (`78-89`, `216-217`). It does not modify state.
- gates_or_invariants: Exactly one valid action is required. Allowed states are `none`, `add`, and `update` (`bitlesson-delta-invalid.md:3-7`). This is upstream of more specific gates for `none` consistency, empty BitLesson knowledge base, concrete Lesson IDs, Notes, file existence, ID format, and ID presence in `.humanize/bitlesson.md` (`bitlesson-validate-delta.sh:242-385`).
- dependencies_and_callers: Called by `scripts/bitlesson-validate-delta.sh`, which is invoked from the stop hook when `bitlesson_required: true` and not in finalize phase (`loop-codex-stop-hook.sh:864-883`). Rendering depends on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: A `## BitLesson Delta` inside a code fence or HTML comment is ignored and becomes a missing-section block rather than an invalid-action block (`bitlesson-validate-delta.sh:95-179`). Duplicate `Action:` lines also fail this invalid-action gate because the count must be exactly one (`202-206`). Lower/upper case actions are normalized to lowercase (`202`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` covers missing/hidden BitLesson sections, notes validation, and valid add/update flows (`116-152`). Direct invalid-action coverage was not seen in that file, but template existence is covered by template reference/comprehensive tests.
- skip_candidate: `no`

### DEV-HZ-165 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Bash-tool guard message for summary-file writes. It preserves RLCR summary validation by steering agents to Write/Edit tooling rather than shell redirection or shell text editing.
- algorithmic_behavior: The template tells the agent that Bash writes are blocked and renders the current correct summary path via `{{CORRECT_PATH}}` (`summary-bash-write.md:1-8`). `hooks/lib/loop-common.sh:897-905` exposes `summary_bash_blocked_message`, and `hooks/loop-bash-validator.sh:529-537` calls it whenever a Bash command modifies `round-[0-9]+-summary.md`.
- inputs_outputs_state: Input is the detected Bash command from the hook, active loop state/current round from common hook context, and the computed correct path `ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`loop-bash-validator.sh:534-536`). Output is the rendered block message to stderr and hook exit code 2 (`536-537`), which blocks the Bash command. It does not mutate loop state.
- gates_or_invariants: Summary files must be written through Write/Edit rather than Bash, because Write/Edit validators enforce correct round number/location and avoid bypassing hook checks. The validator blocks any Bash command classified as modifying `round-N-summary.md`; read-only commands should not match because modification detection is separated in `command_modifies_file` and pattern tests (`tests/test-bash-validator-patterns.sh:192-194`).
- dependencies_and_callers: Depends on `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, and the template renderer. `tests/test-template-references.sh:149-167` explicitly lists `block/summary-bash-write.md` as a common template that must exist.
- edge_cases_or_failure_modes: Detection depends on command-pattern parsing; unusual shell constructs that modify a summary file must be recognized by `command_modifies_file` to trigger this template. The message always points at the current round summary, so attempts to Bash-write a historical summary are still redirected to the active summary path by this gate (`loop-bash-validator.sh:534-536`).
- validation_or_tests: Template existence is checked by `tests/test-template-references.sh:152-167`. Real rendering with `CORRECT_PATH` is covered in template loader/comprehensive tests using similar block templates (`tests/test-template-loader.sh:147-164`, `tests/test-templates-comprehensive.sh:498-512`). Bash modification pattern coverage exists in `tests/test-bash-validator-patterns.sh:192-194`.
- skip_candidate: `no`

### DEV-HZ-195 `file` `prompt-template/explore/report-template.md`
- cursor: `[_]`
- core_role: Canonical audit/report template for `/humanize:explore-idea` runs. It defines how parallel idea-worker outcomes are synthesized, ranked, adopted, and cleaned up.
- algorithmic_behavior: The template emits run metadata (`report-template.md:1-9`), a summary (`12-15`), Tier 1 product-direction ranking (`18-29`), Tier 2 implementation-readiness ranking (`32-42`), worker result summaries (`46-49`), adoption paths (`52-85`), discard/cleanup commands (`87-93`), full worker details (`97-99`), and cleanup reference for `.humanize/explore/<RUN_ID>/` artifacts (`103-126`).
- inputs_outputs_state: Inputs are placeholder substitutions documented by `commands/explore-idea.md:301-321`: run/base metadata, report/final idea paths, ranking rows/rationales, worker result entries, winner worktree/branch/commit, cleanup commands, all worker details, and branch/worktree removal commands. Output is `.humanize/explore/<run-id>/explore-report.md`, paired with `.humanize/explore/<run-id>/final-idea.md`.
- gates_or_invariants: The report must keep two distinct ranking tiers: product direction quality independent of prototype success, and implementation readiness based on worker outcomes (`report-template.md:18-42`; command spec at `commands/explore-idea.md:279-300`). The default adoption path is “Generate Plan From Final Idea” before “Prototype Fast Path” (`report-template.md:52-76`), preserving clean productization as the preferred route. The template explicitly says not to create a legacy alias in the command spec (`commands/explore-idea.md:267-272`).
- dependencies_and_callers: `scripts/validate-explore-idea-io.sh` requires this template to exist (`validate-explore-idea-io.sh:248-260`) and emits `REPORT_TEMPLATE`, `REPORT_PATH`, and `FINAL_IDEA_PATH` (`440-477`). `commands/explore-idea.md` instructs the coordinator to read `REPORT_TEMPLATE` and synthesize it (`273-321`). Tests reference it directly.
- edge_cases_or_failure_modes: If all workers fail, command spec says still write a failure report with failure summary/cleanup and no ranking sections (`commands/explore-idea.md:368-375`), so the template is a normal-success shape but the command allows a failure-specific rendering. Placeholder substitution appears manual/string-based in the command contract rather than through `{{VAR}}` template-loader syntax, so all `<...>` placeholders must be replaced by the explore coordinator.
- validation_or_tests: `tests/test-explore-manifest.sh:183-188` checks Tier 1/Tier 2 sections. `tests/test-explore-command-structure.sh:212-241` checks all report placeholders, `293-299` checks gen-plan points at `<FINAL_IDEA_PATH>`, and `319-326` checks the clean final-idea path appears before prototype fast path. `scripts/validate-explore-idea-io.sh:253-260` hard-fails if missing.
- skip_candidate: `no`

### DEV-HZ-225 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Stress/edge-case executable specification for the prompt-template rendering subsystem used by RLCR hooks and setup scripts.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` and shared test helpers (`test-template-stress-robustness.sh:14-18`), creates a temp directory (`19`), then verifies normal substitution (`33-42`), safe fallback rendering for missing templates (`44-52`), loading/rendering a standard template file (`54-72`), large variable values from 10KB to 100KB (`82-109`), a large 100KB template file (`111-132`), 50 substitutions (`134-151`), special character values (`161-193`), multiline and empty values (`195-216`), placeholder injection prevention (`218-228`), basic character content (`230-239`), dollar signs (`241-250`), repeated variables (`252-261`), boundary variables (`263-272`), and a template containing only one placeholder (`274-283`).
- inputs_outputs_state: Inputs are generated strings/templates plus the actual template loader functions. Outputs are PASS/FAIL counters via `tests/test-helpers.sh`, summary from `print_test_summary`, and exit code equal to the helper summary result (`289-290`). Temp files live under `mktemp` and are cleaned by helper trap.
- gates_or_invariants: The renderer must be single-pass and must not expand placeholders introduced by variable values. This is explicitly implemented in `hooks/lib/template-loader.sh:48-56` and tested at `test-template-stress-robustness.sh:218-228`. Missing templates must use fallback via `load_and_render_safe` (`44-52`; implementation at `template-loader.sh:185-211`). Large values/templates should render without truncation (`82-132`). Special characters such as regex metacharacters, ampersands, backslashes, newlines, empty values, and `$` must survive substitution (`161-250`).
- dependencies_and_callers: Depends on Bash, `awk`, `env`, `wc`, `grep`, `date`, `seq`, `mktemp`, `hooks/lib/template-loader.sh`, and `tests/test-helpers.sh`. It validates behavior used throughout stop hooks, validators, and setup scripts whenever `load_template`, `render_template`, or `load_and_render_safe` are called.
- edge_cases_or_failure_modes: Very large argument/environment values are passed through environment variables by `render_template` (`template-loader.sh:60-79`), so OS environment-size limits are the practical ceiling beyond the tested 100KB case. The test’s “Unicode content” case uses `Hello World` rather than actual non-ASCII, so it does not strongly validate Unicode despite the label (`230-239`). Timing variables are collected but no hard performance budget is enforced (`86-107`, `122-129`).
- validation_or_tests: This file is the validation artifact. Broader renderer coverage also exists in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`, including additional shell-special-character and injection regressions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8 item evidence headings present; all assigned headings are unique`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`