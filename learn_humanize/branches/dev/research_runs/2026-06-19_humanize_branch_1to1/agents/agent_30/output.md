# agent_30 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-030 `file` `agents/plan-compliance-checker.md`
- cursor: `[_]`
- core_role: Prompt/policy agent for the `/start-rlcr-loop` preflight. It validates that a proposed implementation plan is relevant to the current repository and does not require branch switching before the RLCR loop begins. The agent is declared as `plan-compliance-checker`, model `sonnet`, with read-only tools `Read`, `Glob`, and `Grep` at `agents/plan-compliance-checker.md:1-6`.
- algorithmic_behavior: The prompt defines two checks. Check A explores repository context through docs and structure, then compares the plan to repo concepts, paths, functions, or features at `agents/plan-compliance-checker.md:16-29`. Check B scans the full plan for branch-switching instructions such as `git checkout -b`, `git switch`, `git branch`, `gh pr checkout`, worktree creation, or natural-language instructions to switch/create/work on a branch at `agents/plan-compliance-checker.md:31-39`.
- inputs_outputs_state: Input is the plan file content plus repository context gathered by the agent. Output must be exactly one line with one of three verdict forms: `PASS: ...`, `FAIL_RELEVANCE: ...`, or `FAIL_BRANCH_SWITCH: ...` at `agents/plan-compliance-checker.md:48-55`. The file itself has no persistent state transition; the caller turns the verdict into a preflight gate. `commands/start-rlcr-loop.md:41-50` invokes this agent, and `commands/start-rlcr-loop.md:52-56` parses the output fail-closed: `PASS` continues, either `FAIL_*` stops the command, and malformed output stops the command.
- gates_or_invariants: The relevance gate is intentionally lenient: reject only clearly unrelated plans and lean `PASS` when uncertain at `agents/plan-compliance-checker.md:29` and `agents/plan-compliance-checker.md:58-60`. The branch gate protects the RLCR invariant that the working branch remains constant across rounds at `agents/plan-compliance-checker.md:46`. Safe/non-blocking branch references are explicitly excluded: `git checkout -- <file>`, negated instructions, descriptive branch mentions, and `--base-branch` review configuration at `agents/plan-compliance-checker.md:40-45`.
- dependencies_and_callers: Direct caller is the command prompt `commands/start-rlcr-loop.md`, which extracts a safe relative plan path, reads it, then calls `humanize:plan-compliance-checker` at `commands/start-rlcr-loop.md:21-41`. The command’s pre-check is skipped for `--skip-impl`, `-h`, or `--help`, or when no safe/readable plan path can be determined at `commands/start-rlcr-loop.md:17-39`.
- edge_cases_or_failure_modes: Near-empty or non-substantive plans are considered under relevance at `agents/plan-compliance-checker.md:27`. False positives on branch switching are mitigated by the safe-pattern list and “lean toward PASS” rule at `agents/plan-compliance-checker.md:40-45` and `agents/plan-compliance-checker.md:60`. Output formatting is a critical failure surface because the caller stops if no recognized verdict appears at `commands/start-rlcr-loop.md:52-56`.
- validation_or_tests: No direct shell test was assigned for this prompt, but caller behavior is specified in `commands/start-rlcr-loop.md:41-56`. The validation contract is structural: the agent prompt must emit one recognizable verdict, and the command must stop on `FAIL_RELEVANCE`, `FAIL_BRANCH_SWITCH`, or malformed output.
- skip_candidate: `no`

### DEV-HZ-060 `file` `scripts/rlcr-stop-gate.sh`
- cursor: `[_]`
- core_role: Runtime wrapper that exposes RLCR stop-hook logic to non-hook environments, especially skill workflows. It reuses `hooks/loop-codex-stop-hook.sh` instead of duplicating phase-transition/review logic, as documented at `scripts/rlcr-stop-gate.sh:3-14`.
- algorithmic_behavior: The script resolves its installed runtime root from its own path at `scripts/rlcr-stop-gate.sh:19-27`, initializes session/transcript/model/permission values from environment at `scripts/rlcr-stop-gate.sh:29-33`, parses CLI options at `scripts/rlcr-stop-gate.sh:48-79`, builds a Claude Stop-hook-shaped JSON payload with `jq` at `scripts/rlcr-stop-gate.sh:108-123`, pipes that payload to `hooks/loop-codex-stop-hook.sh` with `CLAUDE_PROJECT_DIR` pinned to the target project at `scripts/rlcr-stop-gate.sh:125-128`, then maps hook output to wrapper exit codes at `scripts/rlcr-stop-gate.sh:130-178`.
- inputs_outputs_state: Inputs are `--session-id`, `--transcript-path`, `--project-root`, `--json`, plus `CLAUDE_SESSION_ID`, `CLAUDE_TRANSCRIPT_PATH`, `CODEX_MODEL`, and `CODEX_PERMISSION_MODE`. Output is either human-readable `ALLOW:`/`BLOCK:` text or raw JSON when `--json` is passed. Exit states are explicit: `0` allow, `10` blocked, `20` wrapper/runtime error at `scripts/rlcr-stop-gate.sh:8-14`. The wrapper itself does not mutate RLCR state, but the delegated hook may transition loop files such as `state.md`, `finalize-state.md`, review result files, prompts, and completion markers. The underlying hook starts by resolving the active loop at `hooks/loop-codex-stop-hook.sh:42-69`.
- gates_or_invariants: Missing project context is benign and allows exit at `scripts/rlcr-stop-gate.sh:81-87`. Missing/non-executable hook and missing `jq` are infrastructure errors at `scripts/rlcr-stop-gate.sh:89-97`. Hook nonzero exit and non-JSON output are wrapper errors at `scripts/rlcr-stop-gate.sh:130-145`. A hook JSON decision of `block` becomes exit `10` at `scripts/rlcr-stop-gate.sh:152-159`. Empty hook output or JSON with no `decision` means allow at `scripts/rlcr-stop-gate.sh:136-140` and `scripts/rlcr-stop-gate.sh:162-173`. Any unexpected decision is exit `20` at `scripts/rlcr-stop-gate.sh:176-178`.
- dependencies_and_callers: Depends on `hooks/lib/project-root.sh` for deterministic root resolution at `scripts/rlcr-stop-gate.sh:22-27`; that resolver prioritizes linked worktree, `CLAUDE_PROJECT_DIR`, then git toplevel, and intentionally avoids `pwd` fallback at `hooks/lib/project-root.sh:5-22` and `hooks/lib/project-root.sh:51-76`. Depends on `jq` and executable `hooks/loop-codex-stop-hook.sh`. The Kimi-oriented RLCR skill calls this wrapper explicitly each round at `skills/humanize-rlcr/SKILL-kimi.md:9-17` and `skills/humanize-rlcr/SKILL-kimi.md:50-63`. The native Codex RLCR skill instead relies on Stop hooks automatically but documents the same gate set at `skills/humanize-rlcr/SKILL.md:46-68`.
- edge_cases_or_failure_modes: The JSON construction intentionally preserves `transcript_path` when `session_id` is empty by using independent `if/then/else` null conversion rather than a collapsing `select(length > 0)` pattern; the comment explains the regression at `scripts/rlcr-stop-gate.sh:99-107`. If the hook returns JSON with only `systemMessage` and no decision, the wrapper surfaces it as `ALLOW:` instead of blocking at `scripts/rlcr-stop-gate.sh:162-173`. If no root is resolvable, enforcement is skipped rather than failing the caller at `scripts/rlcr-stop-gate.sh:81-87`.
- validation_or_tests: `tests/test-stop-gate.sh` exercises project-root/default cwd blocking, `--project-root` override, tracked `.humanize` state blocking, ignoring `.humanize-backup`, no-active-loop allow, and the empty-session/transcript forwarding regression at `tests/test-stop-gate.sh:64-88`, `tests/test-stop-gate.sh:91-116`, `tests/test-stop-gate.sh:118-178`, `tests/test-stop-gate.sh:180-204`, and `tests/test-stop-gate.sh:206-287`. `tests/run-all-tests.sh:59-73` includes both `test-stop-gate.sh` and related stop-hook suites in the aggregate runner.
- skip_candidate: `no`

### DEV-HZ-090 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh` error handling and fallback behavior. It verifies prompt-template loading/rendering fails safely under missing files, invalid directories, strict shell mode, malformed placeholders, and special characters.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` and computes `TEMPLATE_DIR` through `get_template_dir` at `tests/test-error-scenarios.sh:11-15`. It runs twelve cases: missing template, missing template directory, missing template through `load_and_render`, empty content rendering, special regex characters in values, strict `set -euo pipefail` continuation, `load_and_render_safe` fallback, fallback variable substitution, valid/invalid template directory validation, empty placeholder preservation, and unclosed placeholder preservation at `tests/test-error-scenarios.sh:42-207`.
- inputs_outputs_state: Inputs are synthetic template names, fallback strings, placeholder assignments, and an isolated subshell script. Outputs are PASS/FAIL counters and exit `0` only when `TESTS_FAILED == 0`, otherwise exit `1` at `tests/test-error-scenarios.sh:208-226`. The test mutates only shell variables and transient command outputs; no repository state is changed.
- gates_or_invariants: Missing template files and directories must return empty output and exit `0` for non-safe loader calls at `tests/test-error-scenarios.sh:42-78`, matching `load_template`’s “warn to stderr, no stdout” behavior at `hooks/lib/template-loader.sh:33-46`. `load_and_render_safe` must use fallback content and render fallback variables at `tests/test-error-scenarios.sh:133-158`, matching `load_and_render_safe` and `_emit_fallback` at `hooks/lib/template-loader.sh:174-211`. Rendering must be single-pass and literal-safe, preserving invalid placeholders and handling values with regex-like characters at `tests/test-error-scenarios.sh:93-106` and `tests/test-error-scenarios.sh:182-206`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`. The exercised implementation defines `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` at `hooks/lib/template-loader.sh:24-238`. Many hook paths rely on `load_and_render_safe`; `tests/test-template-references.sh` also audits critical validators for safe rendering usage, and `tests/run-all-tests.sh:59-73` includes this test suite.
- edge_cases_or_failure_modes: The important failure mode is shell strict-mode abort from command substitution when a template is missing; Test 6 verifies the script continues under `set -euo pipefail` at `tests/test-error-scenarios.sh:108-131`. Empty content must render empty rather than emit junk at `tests/test-error-scenarios.sh:80-91`. Empty placeholder `{{}}` and unclosed `{{UNCLOSED` must remain literal at `tests/test-error-scenarios.sh:182-206`, aligning with the renderer’s explicit close-brace scan at `hooks/lib/template-loader.sh:87-127`.
- validation_or_tests: This file is itself validation. Its assertions cover the template loader’s runtime error posture. Broader related coverage exists in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness tests, but this assigned file specifically targets graceful error scenarios and is included by the aggregate runner at `tests/run-all-tests.sh:59-73`.
- skip_candidate: `no`

### DEV-HZ-120 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook helper that blocks loop exit when blocking/mainline tasks or legacy todos remain incomplete.
- algorithmic_behavior: The test drives the Python checker through stdin JSON and synthetic transcript/task files. It validates input handling, legacy `TodoWrite` parsing, transcript format variations, edge cases, and the file-based Task system at `tests/test-todo-checker.sh:46-538`. It creates a temporary test directory and cleans it on exit at `tests/test-todo-checker.sh:37-39`.
- inputs_outputs_state: Inputs include stdin hook JSON with `transcript_path`, `session_id`, and test-only `tasks_base_dir`; JSONL transcript fixtures; and task JSON files under mock `tasks_base_dir/<session_id>/`. Expected outputs are checker exit codes: `0` allow, `1` incomplete work, `2` parse error, matching implementation docs at `hooks/check-todos-from-transcript.py:9-15`. The test records shell PASS/FAIL counters and exits `0` only when all assertions pass at `tests/test-todo-checker.sh:538-556`.
- gates_or_invariants: Invalid stdin JSON must exit `2`, while empty input, missing `transcript_path`, and nonexistent transcript files allow exit `0` at `tests/test-todo-checker.sh:52-98`. Legacy `TodoWrite` uses only the latest todo list, treats any status other than `completed` as incomplete, ignores `[queued]` only when it is a lane prefix, and does not let inline `[queued]` bypass blockers at `tests/test-todo-checker.sh:107-188` and `tests/test-todo-checker.sh:227-241`. File-based tasks default missing status to pending, treat `completed` and `deleted` as non-blocking, ignore queued-prefix tasks, and include task IDs in output at `tests/test-todo-checker.sh:339-536`.
- dependencies_and_callers: Depends on `hooks/check-todos-from-transcript.py`, where lane classification is prefix-only via `LANE_PREFIX_PATTERN` at `hooks/check-todos-from-transcript.py:24-35`. Legacy transcript parsing extracts tool calls from assistant/message/direct tool-use shapes at `hooks/check-todos-from-transcript.py:38-70` and uses the latest `TodoWrite` at `hooks/check-todos-from-transcript.py:73-120`. File-based tasks are read from `~/.claude/tasks/<session_id>/` or test override at `hooks/check-todos-from-transcript.py:123-170`. The stop hook invokes this checker at `hooks/loop-codex-stop-hook.sh:408-444` and renders an incomplete-todos block if it returns incomplete work.
- edge_cases_or_failure_modes: Invalid JSONL transcript lines are ignored rather than fatal at `tests/test-todo-checker.sh:210-225`, matching `json.JSONDecodeError: continue` in `hooks/check-todos-from-transcript.py:91-94`. Missing todo status and empty todo content still block at `tests/test-todo-checker.sh:280-309`. Nonexistent session task directories allow exit at `tests/test-todo-checker.sh:526-536`. A mixed legacy/file task scenario must detect the incomplete file-based task even if transcript todos are completed at `tests/test-todo-checker.sh:498-516`.
- validation_or_tests: This file is itself the primary validation suite for the task/todo checker and is included by `tests/run-all-tests.sh:59-64`. It confirms output details such as incomplete task names at `tests/test-todo-checker.sh:137-143`, lane markers at `tests/test-todo-checker.sh:175-188` and `tests/test-todo-checker.sh:407-439`, and `Task #` markers at `tests/test-todo-checker.sh:518-524`.
- skip_candidate: `no`

### DEV-HZ-150 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by RLCR hook validators when an agent attempts an invalid goal-tracker modification after Round 0 or targets the wrong tracker file. It enforces the mutable/immutable split of `goal-tracker.md`.
- algorithmic_behavior: The template renders a “Goal Tracker Update Blocked” message with `{{CURRENT_ROUND}}` and `{{CORRECT_PATH}}` placeholders at `prompt-template/block/goal-tracker-modification.md:1-14`. It tells the agent that after Round 0 only the `MUTABLE SECTION` of the active goal tracker may be changed, and only by Write/Edit to the correct active tracker path.
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `CORRECT_PATH`. Output is a rendered markdown block returned by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1517-1535`. The template does not mutate state; it is emitted as stderr/blocking reason by validators that then exit with a blocking code.
- gates_or_invariants: Invariant 1: keep the `IMMUTABLE SECTION` unchanged at `prompt-template/block/goal-tracker-modification.md:8-11`. Invariant 2: do not modify `goal-tracker.md` through Bash at `prompt-template/block/goal-tracker-modification.md:11`. Invariant 3: do not write to an old loop session’s tracker at `prompt-template/block/goal-tracker-modification.md:12`. If a safe automated correction is not possible, the agent should include an optional `Goal Tracker Update Request` in the summary at `prompt-template/block/goal-tracker-modification.md:14`.
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-modification.md"` at `hooks/lib/loop-common.sh:1517-1535`. `hooks/loop-write-validator.sh` uses it when a Write targets the wrong tracker path or changes immutable content after Round 0 at `hooks/loop-write-validator.sh:253-272`. `hooks/loop-edit-validator.sh` uses it for wrong path, failed edit preview, or immutable-section changes at `hooks/loop-edit-validator.sh:204-236`. `hooks/loop-bash-validator.sh` uses it for Bash commands that modify `goal-tracker.md` after Round 0 at `hooks/loop-bash-validator.sh:503-516`.
- edge_cases_or_failure_modes: The template has a fallback embedded in `goal_tracker_blocked_message`, so missing template content still produces a blocking reason at `hooks/lib/loop-common.sh:1522-1535`. Wrong-session tracker writes are blocked by path comparison before content mutation checks in write/edit validators at `hooks/loop-write-validator.sh:253-261` and `hooks/loop-edit-validator.sh:204-212`. Bash modifications are always blocked; Round 0 uses a different Bash-specific message, while Round > 0 uses this template at `hooks/loop-bash-validator.sh:509-515`.
- validation_or_tests: Direct template existence/reference coverage is handled by `tests/test-template-references.sh`, which scans `load_and_render_safe` references. Behavioral validation is distributed across validator tests, including write/edit/bash validator suites. The concrete call sites above prove this template is in the active blocking path.
- skip_candidate: `no`

### DEV-HZ-180 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template used when code review was skipped. It transitions the loop into finalize instructions while warning that implementation could not be fully validated.
- algorithmic_behavior: The template renders a warning using `{{REVIEW_SKIP_REASON}}`, instructs manual verification, optionally suggests a code-simplifier task, states non-negotiable refactor constraints, references the original plan and goal tracker, and lists required before-exit actions at `prompt-template/claude/finalize-phase-skipped-prompt.md:1-51`.
- inputs_outputs_state: Inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a markdown finalize prompt written by the stop hook when it has renamed state into finalize phase. In `hooks/loop-codex-stop-hook.sh`, `finalize_prompt` is built from this template when `skip_reason` is nonempty at `hooks/loop-codex-stop-hook.sh:1352-1388`.
- gates_or_invariants: The template hardens finalization by requiring no functionality changes, no existing test failures, no new bugs, and only functionality-equivalent cleanup at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`. Before exit, it requires all `[mainline]` and `[blocking]` tasks complete, `[queued]` tasks documented as non-blocking, a descriptive commit, and a finalize summary at `prompt-template/claude/finalize-phase-skipped-prompt.md:40-51`.
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-skipped-prompt.md"` in `hooks/loop-codex-stop-hook.sh:1382-1388`. The same hook branch defines a fallback prompt at `hooks/loop-codex-stop-hook.sh:1355-1380`. It depends on template-loader behavior for fallback safety and placeholder substitution.
- edge_cases_or_failure_modes: The central edge case is skipped review: the prompt explicitly says manual verification is required because code review did not fully validate the implementation at `prompt-template/claude/finalize-phase-skipped-prompt.md:3-13`. Another risk is optional simplification causing behavior changes; the constraints at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34` make that non-negotiable. If the template is missing or empty, the hook fallback still produces a finalize prompt with the same essential constraints at `hooks/loop-codex-stop-hook.sh:1355-1388`.
- validation_or_tests: Template reference coverage is handled by the template reference/comprehensive tests. Runtime behavior is covered through finalize-phase and stop-hook tests in the suite; `tests/run-all-tests.sh:73-76` includes `test-error-scenarios.sh`, `test-allowlist-validators.sh`, `test-finalize-phase.sh`, and `test-codex-review-merge.sh`.
- skip_candidate: `no`

### DEV-HZ-210 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: Top-level Humanize skill instruction defining the RLCR workflow, command entry points, plan structure, goal-tracker rules, storage layout, monitoring, prerequisites, and exit-code references. It is a user-facing/non-invocable skill bundle manifest and workflow guide, not executable code.
- algorithmic_behavior: The skill describes Humanize as an iterative loop where AI implements, another AI reviews, issues feed back into more rounds, and work continues until acceptance criteria are met at `skills/humanize/SKILL.md:22-29`. It defines RLCR Phase 1 as implementation plus summary plus Codex completeness review, transitioning to review phase when Codex outputs `COMPLETE` at `skills/humanize/SKILL.md:32-42`. Phase 2 runs `codex review --base <branch>`, fixes `[P0-9]` issues, and completes into Finalize Phase when no issues remain at `skills/humanize/SKILL.md:43-49`.
- inputs_outputs_state: Inputs are user commands and plan files passed to `setup-rlcr-loop.sh`, options such as `--max`, `--codex-model`, `--codex-timeout`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--track-plan-file`, and `--push-every-round` at `skills/humanize/SKILL.md:61-89`. Outputs/state are files under `.humanize/rlcr/<timestamp>/`, including `state.md`, `goal-tracker.md`, round summaries/review results, `finalize-state.md`, `finalize-summary.md`, methodology-analysis files, and `complete-state.md` at `skills/humanize/SKILL.md:181-204`. One-shot skill results live under `.humanize/skill/<timestamp>/` at the same section.
- gates_or_invariants: Important rules require writing summaries, maintaining the goal tracker, including implementation/files/tests details, not editing state files or running cancel commands to cheat, and relying on the native Stop hook after summaries are written at `skills/humanize/SKILL.md:167-175`. The Goal Tracker system separates immutable Ultimate Goal/Acceptance Criteria from mutable Active Tasks/Completed/Deferred/Evolution Log at `skills/humanize/SKILL.md:153-165`. Plan guidance requires acceptance criteria, path boundaries, dependencies/milestones, and implementation notes that code should not contain plan terminology at `skills/humanize/SKILL.md:112-151`.
- dependencies_and_callers: The skill references runtime-root-hydrated scripts: `setup-rlcr-loop.sh` at `skills/humanize/SKILL.md:61-69`, `cancel-rlcr-loop.sh` at `skills/humanize/SKILL.md:90-96`, `validate-gen-plan-io.sh` at `skills/humanize/SKILL.md:98-104`, `ask-codex.sh` at `skills/humanize/SKILL.md:106-110`, and `humanize monitor rlcr` through `scripts/humanize.sh` at `skills/humanize/SKILL.md:206-213`. It assumes Codex CLI as a prerequisite at `skills/humanize/SKILL.md:176-179`.
- edge_cases_or_failure_modes: Review-only mode is supported by `--skip-impl` at `skills/humanize/SKILL.md:67-69` and `skills/humanize/SKILL.md:80`. Plan-file tracking and push enforcement are opt-in gates at `skills/humanize/SKILL.md:81-83`. Methodology analysis is disabled by default through `--privacy`/`--no-privacy` option semantics at `skills/humanize/SKILL.md:87-88`. The skill warns against cheating by state edits or cancel commands, which would bypass the intended stop-hook state machine at `skills/humanize/SKILL.md:169-174`.
- validation_or_tests: This file is documentation/policy, but its commands and referenced scripts are covered across the test suite. Installation tests check that `skills/humanize/SKILL.md` is synced into Codex/Kimi skill roots, for example `tests/test-codex-hook-install.sh` references the installed skill path. The aggregate runner includes relevant setup, hook, template, monitor, and config tests in `tests/run-all-tests.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV-HZ-030, DEV-HZ-060, DEV-HZ-090, DEV-HZ-120, DEV-HZ-150, DEV-HZ-180, DEV-HZ-210
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`