# agent_14 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-014 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: User-facing block-message template directory for RLCR gates. It is not executable by itself, but it is part of the core hook/validator algorithm because Bash, Read, Write, Edit, stop-hook, plan-file, BitLesson, git-cleanliness, drift, and finalize gates render these files to explain why a state transition is blocked.
- algorithmic_behavior: The directory is loaded through `hooks/lib/template-loader.sh`: `get_template_dir` resolves `prompt-template`, `load_template` reads a named markdown file, `render_template` performs single-pass `{{VAR}}` substitution, and `load_and_render_safe` falls back when a template is missing or empty (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:56`, `hooks/lib/template-loader.sh:188`). `hooks/lib/loop-common.sh` initializes `TEMPLATE_DIR` and validates required subdirectories (`hooks/lib/loop-common.sh:240`, `hooks/lib/loop-common.sh:243`, `hooks/lib/template-loader.sh:224`). Recursive inspection found only direct files under `prompt-template/block`, no nested descendants.
- inputs_outputs_state: Inputs are a template name such as `block/git-push.md`, optional fallback text, and optional variable assignments. Outputs are markdown block reasons written to stderr by PreToolUse validators or embedded in JSON block decisions by stop-hook scripts. State is not mutated by templates themselves; they encode gate messages for state owned elsewhere, such as active loop files under `.humanize/rlcr`, state frontmatter, task lists, BitLesson KB, git status, and finalize/methodology marker files.
- gates_or_invariants: The directory covers these invariant classes: BitLesson delta contract (`bitlesson-delta-*.md`), stop-hook and review failures (`codex-review-failed.md`, `mainline-*`), git hygiene (`git-add-humanize.md`, `git-not-clean*.md`, `git-push.md`, `git-tracked-humanize.md`, `unpushed-commits.md`), loop artifact immutability (`state-file-modification.md`, `plan-backup-protected.md`, `prompt-file-write.md`, `summary-bash-write.md`, `round-contract-*`), correct active loop path/round (`wrong-*`), task completion (`incomplete-todos.md`), file size policy (`large-files.md`), goal tracker initialization/mutation (`goal-tracker-*`), finalize/methodology phase restrictions, and direct hook execution blocking.
- dependencies_and_callers: Major callers include `hooks/loop-bash-validator.sh` for git push, plan backup, round contract Bash writes, and direct stop hook execution (`hooks/loop-bash-validator.sh:199`, `hooks/loop-bash-validator.sh:235`, `hooks/loop-bash-validator.sh:498`, `hooks/loop-bash-validator.sh:552`); `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` for plan, wrong-location, wrong-round, state, and goal-tracker blocks (`hooks/loop-write-validator.sh:240`, `hooks/loop-edit-validator.sh:190`); `hooks/loop-codex-stop-hook.sh` for plan integrity, incomplete tasks, git cleanliness, summary/contract presence, BitLesson validation, goal tracker initialization, drift, and Codex review failures (`hooks/loop-codex-stop-hook.sh:391`, `hooks/loop-codex-stop-hook.sh:849`, `hooks/loop-codex-stop-hook.sh:1664`); and `scripts/bitlesson-validate-delta.sh` for BitLesson-specific blocks (`scripts/bitlesson-validate-delta.sh:196`, `scripts/bitlesson-validate-delta.sh:250`, `scripts/bitlesson-validate-delta.sh:263`).
- edge_cases_or_failure_modes: Missing or empty templates degrade to fallback text via `load_and_render_safe` (`hooks/lib/template-loader.sh:194`, `hooks/lib/template-loader.sh:197`, `hooks/lib/template-loader.sh:205`). Placeholder values are single-pass to avoid recursive substitution/injection (`hooks/lib/template-loader.sh:52`, `hooks/lib/template-loader.sh:69`). If the whole template directory is invalid, `loop-common.sh` only warns and continues with inline fallbacks (`hooks/lib/loop-common.sh:245`). Individual templates use placeholders such as `{{PLAN_FILE}}`, `{{CURRENT_ROUND}}`, `{{CORRECT_PATH}}`, `{{GIT_ISSUES}}`, and `{{ACTION}}`; missing variables remain visible as placeholders rather than failing closed.
- validation_or_tests: `tests/test-template-references.sh` checks referenced templates and critical `load_and_render_safe` usage; `tests/test-template-loader.sh` and `tests/test-error-scenarios.sh` cover loader/fallback behavior; `tests/robustness/test-template-error-robustness.sh` stress-tests malformed, missing, permission, symlink, BOM, and concurrent template cases. `tests/test-plan-file-hooks.sh` exercises many block templates indirectly through plan, Bash, Write, Edit, and stop-hook gates. I inspected, but did not execute, these tests.
- skip_candidate: `no`

### DEV-HZ-044 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash command validator for active RLCR loops. It blocks shell commands that would bypass Write/Edit validators, mutate protected RLCR state, run hook scripts manually, push when disallowed, or write during methodology analysis.
- algorithmic_behavior: The script reads hook JSON from stdin, validates JSON and depth with `validate_hook_input` and `is_deeply_nested`, exits early unless `tool_name` is `Bash`, and requires `tool_input.command` (`hooks/loop-bash-validator.sh:22`, `hooks/loop-bash-validator.sh:25`, `hooks/loop-bash-validator.sh:30`, `hooks/loop-bash-validator.sh:36`, `hooks/loop-bash-validator.sh:41`). It resolves the project root, extracts `session_id`, finds the active loop, and then applies phase-specific and RLCR-specific pattern gates (`hooks/loop-bash-validator.sh:52`, `hooks/loop-bash-validator.sh:55`, `hooks/loop-bash-validator.sh:59`). Exit `0` means allow, exit `1` means invalid/safety failure, and exit `2` means block with a user-facing reason.
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name`, `tool_input.command`, and optional `session_id`. Outputs are stderr block messages or silent allow. It reads `.humanize/rlcr/<session>/state.md`, `finalize-state.md`, or `methodology-analysis-state.md` through shared helpers; it does not intentionally mutate state. The meaningful state transition it enforces is "attempted Bash command" -> allowed or blocked before execution.
- gates_or_invariants: Methodology analysis mode blocks write-like git commands, file manipulation commands, interpreters, shell script entry points, build tools, sourcing, direct script execution, and redirection, while allowing a tightly constrained leading `cancel-rlcr-loop.sh` invocation (`hooks/loop-bash-validator.sh:81`, `hooks/loop-bash-validator.sh:96`, `hooks/loop-bash-validator.sh:102`, `hooks/loop-bash-validator.sh:107`, `hooks/loop-bash-validator.sh:114`, `hooks/loop-bash-validator.sh:121`, `hooks/loop-bash-validator.sh:128`, `hooks/loop-bash-validator.sh:135`, `hooks/loop-bash-validator.sh:142`, `hooks/loop-bash-validator.sh:149`, `hooks/loop-bash-validator.sh:156`, `hooks/loop-bash-validator.sh:164`). During active loops it blocks manual execution of `loop-codex-stop-hook.sh` and `rlcr-stop-gate.sh`, including common wrappers (`env`, `command`, assignments, `timeout`, `nice`, `nohup`, `strace`, `ltrace`) (`hooks/loop-bash-validator.sh:184`, `hooks/loop-bash-validator.sh:197`, `hooks/loop-bash-validator.sh:199`). It blocks `git push` unless `push_every_round` is true (`hooks/loop-bash-validator.sh:228`), `.humanize` staging via `git_adds_humanize` (`hooks/loop-bash-validator.sh:247`), state/finalize/methodology state writes or source moves (`hooks/loop-bash-validator.sh:271`, `hooks/loop-bash-validator.sh:280`, `hooks/loop-bash-validator.sh:290`, `hooks/loop-bash-validator.sh:333`, `hooks/loop-bash-validator.sh:461`), plan backup writes (`hooks/loop-bash-validator.sh:496`), goal-tracker writes (`hooks/loop-bash-validator.sh:509`), prompt/summary/round-contract Bash writes (`hooks/loop-bash-validator.sh:524`, `hooks/loop-bash-validator.sh:534`, `hooks/loop-bash-validator.sh:546`), and most `round-*-todos.md` access outside a narrow allowlist (`hooks/loop-bash-validator.sh:561`).
- dependencies_and_callers: Sourced dependencies are `hooks/lib/loop-common.sh` (`hooks/loop-bash-validator.sh:15`), which provides project resolution, state parsing, active-loop discovery, message rendering, path helpers, command classifiers, and cancel authorization. It indirectly depends on `jq`, `sed`, `grep`, `awk`, and the template directory. Hook registration appears in `hooks/hooks.json` and `config/codex-hooks.json`; validation coverage includes `tests/test-bash-validator-patterns.sh`, `tests/test-allowlist-validators.sh`, `tests/test-plan-file-hooks.sh`, `tests/robustness/test-hook-input-robustness.sh`, and methodology/finalize tests.
- edge_cases_or_failure_modes: The script explicitly handles session-aware loop filtering so unrelated sessions are not restricted (`hooks/loop-bash-validator.sh:55`, `hooks/loop-bash-validator.sh:77`). It treats malformed state as a hard safety block (`hooks/loop-bash-validator.sh:214`). It splits shell segments for state-source `mv/cp` detection while protecting redirection operators such as `&>`, `>&`, and `&>>` from accidental splitting (`hooks/loop-bash-validator.sh:316`, `hooks/loop-bash-validator.sh:321`). It strips leading redirections with many quoting styles before matching `mv`/`cp` source patterns (`hooks/loop-bash-validator.sh:350`). Cancel signal authorization can bypass state-file blocks only through `is_cancel_authorized` checks (`hooks/loop-bash-validator.sh:273`, `hooks/loop-bash-validator.sh:292`). Pattern-based command parsing is intentionally conservative and may over-block complex write-capable Bash in protected phases.
- validation_or_tests: `tests/test-plan-file-hooks.sh` verifies plan backup write/rm and bypass attempts through this validator (`tests/test-plan-file-hooks.sh:286`, `tests/test-plan-file-hooks.sh:299`, `tests/test-plan-file-hooks.sh:312`, `tests/test-plan-file-hooks.sh:330`, `tests/test-plan-file-hooks.sh:343`, `tests/test-plan-file-hooks.sh:356`, `tests/test-plan-file-hooks.sh:369`, `tests/test-plan-file-hooks.sh:382`). `tests/test-allowlist-validators.sh` verifies direct hook execution wrapper blocking (`tests/test-allowlist-validators.sh:488`, `tests/test-allowlist-validators.sh:507`, `tests/test-allowlist-validators.sh:517`, `tests/test-allowlist-validators.sh:542`, `tests/test-allowlist-validators.sh:572`). I inspected, but did not execute, these tests.
- skip_candidate: `no`

### DEV-HZ-074 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI escape stripping and numeric test-summary parsing. It protects the broader test runner’s algorithm for reading colored `Passed:` and `Failed:` counts on GNU/Linux and BSD/macOS sed.
- algorithmic_behavior: The test defines colored output constants, counters, `pass` and `fail` helpers, then applies a portable ANSI-stripping expression using `esc=$'\033'` and `sed "s/${esc}\\[[0-9;]*m//g"` (`tests/test-ansi-parsing.sh:14`, `tests/test-ansi-parsing.sh:21`, `tests/test-ansi-parsing.sh:26`, `tests/test-ansi-parsing.sh:45`, `tests/test-ansi-parsing.sh:47`). It extracts counts using `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed` equivalents followed by a trailing numeric grep (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`).
- inputs_outputs_state: Inputs are synthetic shell strings containing ANSI SGR sequences, plain text, multi-line summaries, and bold/color combinations. Outputs are colored PASS/FAIL lines and a final process exit status equal to success when `TESTS_FAILED` is zero (`tests/test-ansi-parsing.sh:163`, `tests/test-ansi-parsing.sh:170`). State is limited to `TESTS_PASSED` and `TESTS_FAILED` counters.
- gates_or_invariants: The invariant is that SGR color and bold sequences must be removed without changing the semantic count text, and parsing must produce exact integer counts. Tests cover basic color (`tests/test-ansi-parsing.sh:44`), multiple colors (`tests/test-ansi-parsing.sh:59`), passed count extraction (`tests/test-ansi-parsing.sh:73`), failed count extraction (`tests/test-ansi-parsing.sh:87`), zero count extraction (`tests/test-ansi-parsing.sh:101`), complex multi-line output with `tail -1` count selection (`tests/test-ansi-parsing.sh:115`, `tests/test-ansi-parsing.sh:124`), no ANSI codes (`tests/test-ansi-parsing.sh:136`), and bold+color combined (`tests/test-ansi-parsing.sh:150`).
- dependencies_and_callers: Depends on Bash ANSI-C quoting, `sed`, `grep`, and shell arithmetic. It is likely run by `tests/run-all-tests.sh`, whose output parsing is referenced in the file comments (`tests/test-ansi-parsing.sh:3`, `tests/test-ansi-parsing.sh:5`). It does not depend on RLCR state files or templates directly.
- edge_cases_or_failure_modes: The stripper only matches SGR `m` sequences with digits and semicolons, not all ANSI control sequences. That is acceptable for the test-runner count parsing it specifies. It defaults grep extraction to `0` on failure (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`), so malformed summary lines can silently produce zero in the parser under test. Multi-line parsing intentionally selects the last extracted count (`tests/test-ansi-parsing.sh:124`, `tests/test-ansi-parsing.sh:125`).
- validation_or_tests: This file is itself the validation. It exits `0` only when all eight parser cases pass (`tests/test-ansi-parsing.sh:170`). I inspected but did not execute it.
- skip_candidate: `no`

### DEV-HZ-104 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Large executable spec for RLCR plan-file integrity and related hook protections. It verifies that plan sources and loop backups cannot drift during an active loop, that schema/branch state is validated, and that Write/Edit/Bash/Stop hooks enforce the same plan contract.
- algorithmic_behavior: The script creates an isolated git repo under `mktemp`, installs a mock `codex`, builds `.humanize/rlcr/<timestamp>` loop directories, creates plan files, plan backups, state frontmatter, round contracts, summaries, and goal trackers, then invokes hook scripts and asserts exit codes/output substrings (`tests/test-plan-file-hooks.sh:31`, `tests/test-plan-file-hooks.sh:40`, `tests/test-plan-file-hooks.sh:78`, `tests/test-plan-file-hooks.sh:97`, `tests/test-plan-file-hooks.sh:119`, `tests/test-plan-file-hooks.sh:124`, `tests/test-plan-file-hooks.sh:139`). It uses direct hook invocations for `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, and `loop-codex-stop-hook.sh`.
- inputs_outputs_state: Inputs are synthetic hook JSON (`tool_name`, `tool_input.file_path`, `tool_input.command`) and filesystem/git states. Outputs are PASS/FAIL counters and final exit status `TESTS_FAILED` (`tests/test-plan-file-hooks.sh:1172`, `tests/test-plan-file-hooks.sh:1181`). The test mutates only its temp repos: switching branches, deleting/modifying plans, committing plan changes, creating malformed states, and altering goal-tracker placeholder content.
- gates_or_invariants: It asserts valid state passes and quoted YAML fields are parsed (`tests/test-plan-file-hooks.sh:145`, `tests/test-plan-file-hooks.sh:160`); malformed or missing schema fields block (`tests/test-plan-file-hooks.sh:175`, `tests/test-plan-file-hooks.sh:196`); branch changes block (`tests/test-plan-file-hooks.sh:220`); Write/Edit/Bash cannot modify `plan.md` backup (`tests/test-plan-file-hooks.sh:252`, `tests/test-plan-file-hooks.sh:269`, `tests/test-plan-file-hooks.sh:286`); Bash blocks direct `.humanize/rlcr/plan.md`, command substitution, glob, brace, pipe, and backtick bypass attempts (`tests/test-plan-file-hooks.sh:312`, `tests/test-plan-file-hooks.sh:330`, `tests/test-plan-file-hooks.sh:343`, `tests/test-plan-file-hooks.sh:356`, `tests/test-plan-file-hooks.sh:369`, `tests/test-plan-file-hooks.sh:382`); stop hook parses quoted fields and requires round contracts when anti-drift state is present (`tests/test-plan-file-hooks.sh:450`, `tests/test-plan-file-hooks.sh:495`); stop hook blocks modified, deleted, missing-backup, uncommitted tracked, and committed tracked plan changes (`tests/test-plan-file-hooks.sh:567`, `tests/test-plan-file-hooks.sh:607`, `tests/test-plan-file-hooks.sh:641`, `tests/test-plan-file-hooks.sh:660`, `tests/test-plan-file-hooks.sh:756`); goal-tracker placeholder reporting is section-specific (`tests/test-plan-file-hooks.sh:841`, `tests/test-plan-file-hooks.sh:916`, `tests/test-plan-file-hooks.sh:988`, `tests/test-plan-file-hooks.sh:1058`); legacy `.humanize-loop.local` paths are allowed (`tests/test-plan-file-hooks.sh:1132`, `tests/test-plan-file-hooks.sh:1146`, `tests/test-plan-file-hooks.sh:1159`).
- dependencies_and_callers: Depends on `git`, `jq`, Bash, the project hooks, and `hooks/lib/loop-common.sh` behaviors. It exercises `loop-plan-file-validator.sh` schema/branch/tracking gates (`hooks/loop-plan-file-validator.sh:45`, `hooks/loop-plan-file-validator.sh:80`, `hooks/loop-plan-file-validator.sh:96`, `hooks/loop-plan-file-validator.sh:124`), Write/Edit plan backup blocks (`hooks/loop-write-validator.sh:240`, `hooks/loop-edit-validator.sh:190`), Bash plan backup and command modification checks (`hooks/loop-bash-validator.sh:496`), and stop-hook plan diff/backup checks (`hooks/loop-codex-stop-hook.sh:332`, `hooks/loop-codex-stop-hook.sh:345`, `hooks/loop-codex-stop-hook.sh:358`, `hooks/loop-codex-stop-hook.sh:377`).
- edge_cases_or_failure_modes: The test explicitly targets bypass/failure modes: YAML quoting around `plan_file` and `start_branch`, branch mismatch, missing v1.5.0 fields, direct `.humanize/rlcr/plan.md` with no timestamp component, shell expansion in protected paths, piped `tee`, tracked plan race via uncommitted changes, committed plan drift with clean git status, missing backup, deleted original plan, path names with hyphens, and ensuring obsolete legacy directories are not overblocked.
- validation_or_tests: This file is the validation suite for the plan-file hook contract. It does not just unit-test one file; it is an integration test across validators and stop hook. I inspected but did not execute it.
- skip_candidate: `no`

### DEV-HZ-134 `file` `prompt-template/block/bitlesson-delta-inconsistent.md`
- cursor: `[_]`
- core_role: Block template for BitLesson Delta declaration mismatches. It is part of the stop-hook validation contract requiring summaries to accurately declare whether reusable lessons were added, updated, or not changed.
- algorithmic_behavior: The template tells the worker that the `## BitLesson Delta` declaration does not match expected BitLesson state (`prompt-template/block/bitlesson-delta-inconsistent.md:1`, `prompt-template/block/bitlesson-delta-inconsistent.md:3`). It specifies two core mappings: `Action: none` must use `Lesson ID(s): NONE` or omit IDs, while `Action: add|update` must provide concrete IDs that exist in `.humanize/bitlesson.md` (`prompt-template/block/bitlesson-delta-inconsistent.md:5`, `prompt-template/block/bitlesson-delta-inconsistent.md:6`, `prompt-template/block/bitlesson-delta-inconsistent.md:7`).
- inputs_outputs_state: Inputs are rendered indirectly by `scripts/bitlesson-validate-delta.sh` via `load_and_render_safe`; some call sites pass variables such as `ACTION`, `BITLESSON_FILE`, `MISSING_IDS`, or `INVALID_IDS`, but this specific template has no placeholders, so those values are ignored if the template exists. Output is the markdown reason embedded in a JSON block response from `block_exit` (`scripts/bitlesson-validate-delta.sh:78`, `scripts/bitlesson-validate-delta.sh:81`).
- gates_or_invariants: It backs several invariant failures: non-empty/non-NONE Lesson IDs for `Action: none` (`scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:250`); missing or `NONE` IDs for `add`/`update` (`scripts/bitlesson-validate-delta.sh:269`, `scripts/bitlesson-validate-delta.sh:276`); missing BitLesson file for non-none action (`scripts/bitlesson-validate-delta.sh:299`, `scripts/bitlesson-validate-delta.sh:306`); zero concrete IDs after parsing (`scripts/bitlesson-validate-delta.sh:342`, `scripts/bitlesson-validate-delta.sh:349`); invalid ID format (`scripts/bitlesson-validate-delta.sh:322`, `scripts/bitlesson-validate-delta.sh:364`); and IDs not found in the KB (`scripts/bitlesson-validate-delta.sh:369`, `scripts/bitlesson-validate-delta.sh:379`).
- dependencies_and_callers: Primary caller is `scripts/bitlesson-validate-delta.sh`; that script is invoked by `hooks/loop-codex-stop-hook.sh` when BitLesson checks are required in non-finalize rounds (`hooks/loop-codex-stop-hook.sh:868`, `hooks/loop-codex-stop-hook.sh:869`). It depends on the template loader and on `prompt-template/block/bitlesson-delta-missing.md`, `bitlesson-delta-invalid.md`, `bitlesson-delta-empty-kb.md`, and `bitlesson-delta-missing-notes.md` for adjacent failure classes.
- edge_cases_or_failure_modes: Because this template is generic and has no placeholders, more specific fallback messages in `bitlesson-validate-delta.sh` can be more detailed than the rendered template for invalid/missing IDs. The validator mitigates false positives by ignoring `## BitLesson Delta` inside fenced code blocks or HTML comments (`scripts/bitlesson-validate-delta.sh:95`, `scripts/bitlesson-validate-delta.sh:153`) and by accepting both `Lesson ID(s):` and `Lesson IDs:` field spellings (`scripts/bitlesson-validate-delta.sh:220`, `scripts/bitlesson-validate-delta.sh:222`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` is the direct validator test suite; repository searches show it checks fenced-code and HTML-comment false positives plus normal text validation. The template is also indirectly covered through `hooks/loop-codex-stop-hook.sh` BitLesson validation (`hooks/loop-codex-stop-hook.sh:879`). I inspected but did not execute tests.
- skip_candidate: `no`

### DEV-HZ-164 `file` `prompt-template/block/stop-hook-direct-execution.md`
- cursor: `[_]`
- core_role: Block template for preventing manual Bash execution of RLCR stop-hook/gate scripts during an active loop. This preserves the intended hook lifecycle: the hooks system, not the worker, owns stop-hook invocation.
- algorithmic_behavior: The template states that direct hook execution through Bash is not allowed, that hooks are automatically triggered at the right time, and that the worker should complete work and end the response (`prompt-template/block/stop-hook-direct-execution.md:1`, `prompt-template/block/stop-hook-direct-execution.md:3`, `prompt-template/block/stop-hook-direct-execution.md:5`, `prompt-template/block/stop-hook-direct-execution.md:7`). It has no placeholders.
- inputs_outputs_state: Input is an attempted Bash command matched by `loop-bash-validator.sh` against blocked hook script patterns. Output is markdown written to stderr through `stop_hook_direct_execution_blocked_message` (`hooks/lib/loop-common.sh:1475`, `hooks/lib/loop-common.sh:1484`), and the validator exits `2` (`hooks/loop-bash-validator.sh:199`, `hooks/loop-bash-validator.sh:201`). It does not mutate loop state; it blocks a forbidden transition from "active loop" to "manual stop hook execution".
- gates_or_invariants: It backs the invariant that `loop-codex-stop-hook.sh` and `rlcr-stop-gate.sh` are managed entry points, not worker-run commands (`hooks/loop-bash-validator.sh:179`, `hooks/loop-bash-validator.sh:184`). The matcher covers bare path execution, shell wrappers (`bash`, `sh`, `zsh`, `source`, `.`), environment/assignment wrappers, `command`, and utility wrappers such as `timeout`, `nice`, `nohup`, `strace`, and `ltrace` (`hooks/loop-bash-validator.sh:185`, `hooks/loop-bash-validator.sh:197`, `hooks/loop-bash-validator.sh:198`).
- dependencies_and_callers: Primary caller is `hooks/loop-bash-validator.sh`; message helper lives in `hooks/lib/loop-common.sh`. Related executable wrappers are `hooks/loop-codex-stop-hook.sh` and `scripts/rlcr-stop-gate.sh`. Test coverage in `tests/test-allowlist-validators.sh` enumerates wrapper commands that must block (`tests/test-allowlist-validators.sh:488`, `tests/test-allowlist-validators.sh:507`, `tests/test-allowlist-validators.sh:517`, `tests/test-allowlist-validators.sh:537`, `tests/test-allowlist-validators.sh:542`, `tests/test-allowlist-validators.sh:567`, `tests/test-allowlist-validators.sh:572`).
- edge_cases_or_failure_modes: The block is only evaluated after an active loop is found; without an active loop, `loop-bash-validator.sh` allows commands before this gate (`hooks/loop-bash-validator.sh:173`). The regex is wrapper-aware but still pattern-based, so unusual shell constructs may fall through or be caught by other methodology/command-modification gates. Template fallback text in `loop-common.sh` mirrors this file if the template is missing (`hooks/lib/loop-common.sh:1476`).
- validation_or_tests: `tests/test-allowlist-validators.sh` directly asserts output contains "direct execution" and exit code `2` for many wrapper forms. I inspected but did not execute it.
- skip_candidate: `no`

### DEV-HZ-194 `file` `prompt-template/explore/final-idea-template.md`
- cursor: `[_]`
- core_role: Explore-mode synthesis template for the final selected product/implementation idea. It is a prompt/report contract that converts exploration outputs into a plan-ready artifact for `/humanize:gen-plan`.
- algorithmic_behavior: The template defines required sections: run context, final recommendation, rationale, approach summary, objective evidence, explore outcomes, constraints, known risks, cross-direction learnings, and suggested productization flow (`prompt-template/explore/final-idea-template.md:3`, `prompt-template/explore/final-idea-template.md:10`, `prompt-template/explore/final-idea-template.md:14`, `prompt-template/explore/final-idea-template.md:18`, `prompt-template/explore/final-idea-template.md:22`, `prompt-template/explore/final-idea-template.md:26`, `prompt-template/explore/final-idea-template.md:30`, `prompt-template/explore/final-idea-template.md:34`, `prompt-template/explore/final-idea-template.md:38`, `prompt-template/explore/final-idea-template.md:42`). It uses angle-bracket placeholders rather than the hook loader’s `{{VAR}}` syntax, so it appears to be filled by explore command generation logic rather than `render_template`.
- inputs_outputs_state: Inputs are explore-run artifacts: run ID, directions JSON, explore report path, final idea output path, final recommendation, rationale, approach summary, objective evidence, explore outcomes, constraints, risks, and learnings (`prompt-template/explore/final-idea-template.md:5`, `prompt-template/explore/final-idea-template.md:6`, `prompt-template/explore/final-idea-template.md:7`, `prompt-template/explore/final-idea-template.md:8`). Output is a markdown final idea document containing a productization command snippet (`prompt-template/explore/final-idea-template.md:44`, `prompt-template/explore/final-idea-template.md:45`, `prompt-template/explore/final-idea-template.md:46`). It does not mutate RLCR state directly; it creates a handoff surface from exploration to planning.
- gates_or_invariants: The invariant is that an explore run must produce a final idea with enough evidence and constraints to seed plan generation. The template explicitly embeds the default next transition from final idea to plan to RLCR loop (`/humanize:gen-plan --input <FINAL_IDEA_PATH> --output <plan-path>` then `/humanize:start-rlcr-loop <plan-path>`) (`prompt-template/explore/final-idea-template.md:44`, `prompt-template/explore/final-idea-template.md:45`, `prompt-template/explore/final-idea-template.md:46`).
- dependencies_and_callers: `tests/test-explore-command-structure.sh` checks that this template exists and includes "Final Recommendation", "Explore Outcomes", and "Suggested Productization Flow" (`tests/test-explore-command-structure.sh:243`, `tests/test-explore-command-structure.sh:249`). `tests/test-explore-manifest.sh` checks file existence (`tests/test-explore-manifest.sh:37`). `tests/test-validate-explore-idea-io.sh` treats missing `final-idea-template.md` as exit `9` from explore IO validation (`tests/test-validate-explore-idea-io.sh:270`, `tests/test-validate-explore-idea-io.sh:278`). It coordinates with sibling explore templates `worker-prompt.md` and `report-template.md`, and with `scripts/validate-explore-idea-io.sh`.
- edge_cases_or_failure_modes: Missing final idea template is a hard explore IO validation failure, not a fallback-render case (`tests/test-validate-explore-idea-io.sh:270`). Placeholder drift is tested by structure tests that enumerate required placeholders after line 260 in `tests/test-explore-command-structure.sh`; because this template uses literal angle placeholders, a generator must replace them explicitly or final artifacts may leak placeholders. The command block assumes the generated final idea path is suitable as `gen-plan` input.
- validation_or_tests: Covered by `tests/test-explore-command-structure.sh`, `tests/test-explore-manifest.sh`, and `tests/test-validate-explore-idea-io.sh`. I inspected but did not execute them.
- skip_candidate: `no`

### DEV-HZ-224 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test suite for the template loader’s failure behavior. It verifies hook block templates can be missing, malformed, unreadable, unusual, or concurrently loaded without crashing validators.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` and test helpers, creates a temp test directory, then exercises `load_and_render_safe`, `render_template`, and template file loading under error and edge conditions (`tests/robustness/test-template-error-robustness.sh:15`, `tests/robustness/test-template-error-robustness.sh:17`, `tests/robustness/test-template-error-robustness.sh:20`). It uses `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh` and exits with the summary status (`tests/robustness/test-template-error-robustness.sh:340`).
- inputs_outputs_state: Inputs are synthetic template strings/files: missing paths, empty files, malformed placeholders, nested placeholders, invalid variable names, values containing template syntax or newlines, BOM-prefixed files, whitespace-only files, filenames with spaces, permission-denied files, concurrent reads, subdirectory templates, and symlinked templates. Outputs are PASS/FAIL records and final exit status. State is confined to the temp directory, test counters, and background job PIDs.
- gates_or_invariants: Missing template and missing template directory must return fallback (`tests/robustness/test-template-error-robustness.sh:34`, `tests/robustness/test-template-error-robustness.sh:43`). Empty templates must either return empty or fallback gracefully (`tests/robustness/test-template-error-robustness.sh:53`). Malformed/unclosed/nested/empty placeholders and invalid variable names must not crash rendering (`tests/robustness/test-template-error-robustness.sh:73`, `tests/robustness/test-template-error-robustness.sh:84`, `tests/robustness/test-template-error-robustness.sh:95`, `tests/robustness/test-template-error-robustness.sh:106`, `tests/robustness/test-template-error-robustness.sh:125`, `tests/robustness/test-template-error-robustness.sh:135`, `tests/robustness/test-template-error-robustness.sh:146`). Values containing `{{OTHER}}` must not be recursively expanded (`tests/robustness/test-template-error-robustness.sh:165`, `tests/robustness/test-template-error-robustness.sh:170`). Newlines must be preserved enough to contain the expected text (`tests/robustness/test-template-error-robustness.sh:176`). Concurrent safe loads must all succeed and preserve the template file (`tests/robustness/test-template-error-robustness.sh:270`, `tests/robustness/test-template-error-robustness.sh:294`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`, especially `render_template` single-pass awk behavior (`hooks/lib/template-loader.sh:56`, `hooks/lib/template-loader.sh:73`) and `load_and_render_safe` fallback behavior (`hooks/lib/template-loader.sh:188`, `hooks/lib/template-loader.sh:197`, `hooks/lib/template-loader.sh:202`). Depends on filesystem operations (`touch`, `chmod`, `ln -sf`), `head`, `tr`, background jobs, and the repo’s test helpers.
- edge_cases_or_failure_modes: It accepts some variability: empty template can be empty or fallback; permission handling may vary by root/non-root user (`tests/robustness/test-template-error-robustness.sh:245`, `tests/robustness/test-template-error-robustness.sh:248`); special-character filename and symlink tests may pass with alternate behavior if filesystem support differs (`tests/robustness/test-template-error-robustness.sh:228`, `tests/robustness/test-template-error-robustness.sh:320`). It intentionally tolerates malformed placeholder output as long as rendering does not crash or return empty unexpectedly.
- validation_or_tests: This file is itself the validation. It specifically backs the reliability of `prompt-template/block` consumers because validators use `load_and_render_safe` to avoid turning a missing template into a hook failure. I inspected but did not execute it.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 unique item sections present: DEV-HZ-014, DEV-HZ-044, DEV-HZ-074, DEV-HZ-104, DEV-HZ-134, DEV-HZ-164, DEV-HZ-194, DEV-HZ-224
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`