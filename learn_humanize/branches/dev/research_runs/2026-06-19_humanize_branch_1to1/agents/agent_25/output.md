# agent_25 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-025 `directory` `skills/humanize-refine-plan`
- cursor: `[_]`
- core_role: Flow-skill directory for converting an annotated `gen-plan` document into a comment-free refined plan plus QA ledger. Recursive inspection found one child, `skills/humanize-refine-plan/SKILL.md`; no nested scripts or assets exist under the directory. The skill is declared as `type: flow` and `user-invocable: false` at `skills/humanize-refine-plan/SKILL.md:1-6`.
- algorithmic_behavior: The flow is a strict pipeline: parse arguments and paths, load merged config, validate IO through `scripts/validate-refine-plan-io.sh`, extract valid `CMT:` / `ENDCMT` blocks with a stateful scanner, classify comments as `question`, `change_request`, or `research_request`, process them in order, regenerate the plan, validate the resulting plan, populate the QA template, optionally translate variants, then write artifacts atomically; see `skills/humanize-refine-plan/SKILL.md:18-48` and `skills/humanize-refine-plan/SKILL.md:88-107`.
- inputs_outputs_state: Inputs are `--input`, optional `--output`, optional `--qa-dir`, optional `--alt-language`, and either `--discussion` or `--direct`; argument rules and defaults are specified at `skills/humanize-refine-plan/SKILL.md:50-65`. Outputs are the refined plan, QA ledger, and optional translated variants, with convergence metadata recorded in QA at `skills/humanize-refine-plan/SKILL.md:96-107`. State preserved across the transform is the existing `gen-plan` schema, required sections, optional appendices, task routing tags, identifiers, and unresolved decision/convergence status.
- gates_or_invariants: Required sections must remain intact, including Goal Description, Acceptance Criteria, Path Boundaries, Dependencies, Task Breakdown, Claude-Codex Deliberation, Pending User Decisions, and Implementation Notes at `skills/humanize-refine-plan/SKILL.md:71-83`. Final plan must remove all resolved comment blocks, keep task tags restricted to `coding` or `analyze`, generate QA from `prompt-template/plan/refine-plan-qa-template.md`, and write atomically at `skills/humanize-refine-plan/SKILL.md:69-86`. `--discussion` and `--direct` are mutually exclusive, and `--alt-language` must not be passed to the validator at `skills/humanize-refine-plan/SKILL.md:62-65`.
- dependencies_and_callers: The sibling command `commands/refine-plan.md` is the operational implementation contract and enforces planning-only writes at `commands/refine-plan.md:19-28`, validator invocation at `commands/refine-plan.md:157-180`, scanner behavior at `commands/refine-plan.md:184-220`, and QA population at `commands/refine-plan.md:457-485`. The directory coordinates with `scripts/validate-refine-plan-io.sh`, `prompt-template/plan/refine-plan-qa-template.md`, `scripts/install-skill.sh`, and install docs; wiring is checked in `tests/test-refine-plan.sh:831-847`.
- edge_cases_or_failure_modes: IO validation blocks missing, empty, or schema-invalid input; malformed, nested, unterminated, empty, fenced, or HTML-commented comment markers; unwritable output/input directories; and invalid QA directory state. The validator’s concrete exit-code behavior is in `scripts/validate-refine-plan-io.sh:573-692`, and regression coverage for those failures is in `tests/test-refine-plan.sh:1055-1355`. Alternate language values are normalized to a fixed table and English is a no-op at `skills/humanize-refine-plan/SKILL.md:108-130`.
- validation_or_tests: `tests/test-refine-plan.sh` validates QA template sections, the `user-invocable: false` skill frontmatter, installer inclusion, installation docs, validator argument handling, exit codes, parser edge cases, mode reporting, and auto-created QA directories; key wiring assertions are at `tests/test-refine-plan.sh:820-847`, and validator regression coverage is at `tests/test-refine-plan.sh:1055-1355`.
- skip_candidate: `no`

### DEV-HZ-055 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Stop-hook validation gate for the RLCR round summary’s `## BitLesson Delta` section. It enforces that a round either explicitly records no lesson or references concrete lesson IDs already present in the BitLesson knowledge file.
- algorithmic_behavior: The script parses required CLI arguments at `scripts/bitlesson-validate-delta.sh:17-67`, sources the template loader at `scripts/bitlesson-validate-delta.sh:74-76`, and emits hook-compatible JSON block decisions through `block_exit` at `scripts/bitlesson-validate-delta.sh:78-90`. The main extractor is an `awk` state machine that detects the `## BitLesson Delta` section while ignoring fenced code and HTML comments at `scripts/bitlesson-validate-delta.sh:92-179`.
- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` at `scripts/bitlesson-validate-delta.sh:4-14`. On success it prints nothing and exits zero. On contract failure it prints JSON with `decision: block`, `reason`, and `systemMessage`, then exits zero so the hook can surface the block. It reads summary and BitLesson files but does not write state.
- gates_or_invariants: The summary must contain one visible `Action:` in the delta block, and the action must be `none`, `add`, or `update`; see `scripts/bitlesson-validate-delta.sh:200-218`. `Action: none` allows `Lesson ID(s): NONE` or blank IDs, but blocks when the BitLesson file has no concrete lessons and `allow-empty-none` is not true at `scripts/bitlesson-validate-delta.sh:242-267`. `add` and `update` require non-`NONE` IDs, non-placeholder `Notes:`, an existing BitLesson file, IDs matching `BL-YYYYMMDD-short-name`, and matching `Lesson ID:` entries in the file at `scripts/bitlesson-validate-delta.sh:269-384`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` only for non-finalize rounds when BitLesson validation is required; the hook invokes it with summary, KB path, relpath, allow-empty flag, template dir, and current round at `hooks/loop-codex-stop-hook.sh:864-883`. It depends on `jq`, `awk`, `sed`, `tr`, `hooks/lib/template-loader.sh`, and templates under `prompt-template/block/bitlesson-delta-*.md`.
- edge_cases_or_failure_modes: Missing summary file or missing required arguments exits nonzero before hook-style blocking at `scripts/bitlesson-validate-delta.sh:62-72`. Delta headings inside fenced code or HTML comments are ignored by design. Multiple or absent action lines block. Placeholder notes such as bracketed or angle-bracket text block for add/update at `scripts/bitlesson-validate-delta.sh:281-297`. Comma-separated IDs are trimmed individually, and invalid IDs are reported separately from missing valid IDs at `scripts/bitlesson-validate-delta.sh:315-383`.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` creates fixture summary and BitLesson files, then asserts block JSON or empty output. It covers whitespace-only notes, bracket and angle placeholders, delta sections inside fences/comments, and normal valid add/update cases at `tests/test-bitlesson-validate-delta.sh:64-152`.
- skip_candidate: `no`

### DEV-HZ-085 `file` `tests/test-commit-history-section.sh`
- cursor: `[_]`
- core_role: Executable specification for the Integral commit-history context injected into Codex review prompts. It mirrors the stop-hook logic that summarizes commits since `BASE_COMMIT` and references recent round summary/review files.
- algorithmic_behavior: The test initializes a temporary git repo, derives `BASE_COMMIT`, computes `COMMIT_HISTORY`, builds `RECENT_ROUND_FILES`, renders `prompt-template/codex/commit-history-section.md` through `load_and_render_safe`, and checks the rendered output. The hook implementation it mirrors is at `hooks/loop-codex-stop-hook.sh:1042-1071`.
- inputs_outputs_state: Inputs are a temporary repo, `CURRENT_ROUND`, `BASE_COMMIT`, `LOOP_TIMESTAMP`, the commit-history template, and fallback text. Outputs are test pass/fail records via `test-helpers.sh` and the final summary at `tests/test-commit-history-section.sh:268-271`. Test state evolves by creating commits, switching to an orphan branch to produce an unrelated commit, and rendering different round contexts.
- gates_or_invariants: Round zero must display `(no commits yet)` and `(first round, no prior history)` at `tests/test-commit-history-section.sh:33-61`. Round three must include all three commits and round file references for rounds zero through two at `tests/test-commit-history-section.sh:63-114`. The hook must validate that `BASE_COMMIT` is an ancestor before using a range log; otherwise it falls back to recent commits with an explanatory annotation at `tests/test-commit-history-section.sh:116-147` and `hooks/loop-codex-stop-hook.sh:1042-1052`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, git, `hooks/lib/template-loader.sh`, and `prompt-template/codex/commit-history-section.md`. It is a standalone executable spec; in the inspected `tests/run-all-tests.sh` slice, this specific file is not listed, while the hook code remains the runtime caller of the same algorithm.
- edge_cases_or_failure_modes: Covers nonexistent `BASE_COMMIT`, valid but unrelated base commit, empty base commit, no commits since base, missing template fallback, and the round-one lower boundary. It also checks that the fallback section includes both commits and recent round files, preventing degraded context if the template file is absent at `tests/test-commit-history-section.sh:180-222`.
- validation_or_tests: The file is itself the validation artifact. It asserts six cases: round zero placeholders, round three commits and files, corrupted base fallback, unrelated base fallback, missing template fallback, round-one boundary, and empty-base fallback at `tests/test-commit-history-section.sh:33-266`.
- skip_candidate: `no`

### DEV-HZ-115 `file` `tests/test-style-compliance.sh`
- cursor: `[_]`
- core_role: Executable style-invariant gate for plan-authored implementation surfaces. It prevents workflow/planning literals from leaking into scoped runtime code and comments.
- algorithmic_behavior: The script computes `PLUGIN_ROOT`, collects every `.sh` and `.py` file under `viz/`, adds the plan-authored `scripts/cancel-rlcr-session.sh` if present, then scans each scoped file for the literal substrings `AC-`, `Milestone`, `Step `, and `Phase `; see `tests/test-style-compliance.sh:30-63` and `tests/test-style-compliance.sh:74-93`.
- inputs_outputs_state: Inputs are repository files under the scoped paths. Outputs are colored PASS/FAIL lines and a nonzero exit if any forbidden literal appears. It maintains only counters and arrays in process memory, with no persistent state writes.
- gates_or_invariants: The scoped file set must not be empty, or the test fails at `tests/test-style-compliance.sh:65-68`. Each forbidden pattern is reported once with all offending files and matching lines at `tests/test-style-compliance.sh:76-91`. The broader `scripts/`, `commands/`, `hooks/`, and `tests/` trees are intentionally excluded because they may legitimately contain workflow terminology or fixtures, as documented at `tests/test-style-compliance.sh:12-27`.
- dependencies_and_callers: Depends on `find`, `grep`, `sed`, bash arrays, and the presence of the `viz/` directory or the scoped extra script. It is included in the main test runner at `tests/run-all-tests.sh:120`.
- edge_cases_or_failure_modes: If `viz/` is absent and the extra plan-authored script is absent, the test treats that as a failure rather than a vacuous pass. The test file itself contains forbidden literals in comments and pattern definitions, but it excludes `tests/` from scan scope, preventing self-failure by design.
- validation_or_tests: This file is the validation gate. Its final summary and failure exit behavior are at `tests/test-style-compliance.sh:95-104`.
- skip_candidate: `no`

### DEV-HZ-145 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the git-clean exit gate. It turns dirty working-tree state into user-facing required actions and commit hygiene rules.
- algorithmic_behavior: The template renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then instructs the actor to review untracked files, stage only specific real changes, and commit with a project-conventional message at `prompt-template/block/git-not-clean.md:1-10`. It is loaded through `load_and_render_safe`, whose single-pass substitution behavior is implemented in `hooks/lib/template-loader.sh:56-135` and fallback handling at `hooks/lib/template-loader.sh:185-211`.
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES` from the stop hook. Output is the rendered `reason` string embedded in hook JSON block responses. The template itself is stateless; the hook controls the state transition by blocking loop exit until git cleanliness is restored.
- gates_or_invariants: The template forbids broad staging commands, forbids staging `.humanize/` or legacy `.humanize-*` loop artifacts, requires project-conventional commit messages, and forbids AI authorship/co-author attribution at `prompt-template/block/git-not-clean.md:11-17`.
- dependencies_and_callers: Called in methodology-analysis completion when tracked changes remain at `hooks/loop-codex-stop-hook.sh:629-655`, and in the normal git-clean gate at `hooks/loop-codex-stop-hook.sh:699-755`. It coordinates with sibling templates `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md` for `SPECIAL_NOTES`, loaded at `hooks/loop-codex-stop-hook.sh:712-728`.
- edge_cases_or_failure_modes: Untracked `.humanize/` and `.humanize-*` paths are excluded from the dirty block decision but may produce local-state notes; see `hooks/loop-codex-stop-hook.sh:699-728`. Other untracked paths add artifact-review notes. Before blocking, the hook runs stale index-lock cleanup at `hooks/loop-codex-stop-hook.sh:633-635` and `hooks/loop-codex-stop-hook.sh:732-745`.
- validation_or_tests: No dedicated test for this exact template was found in the inspected assigned files, but it is exercised through the stop-hook git-clean branches and template loader contract. The runtime JSON block construction is visible at `hooks/loop-codex-stop-hook.sh:646-654` and `hooks/loop-codex-stop-hook.sh:747-755`.
- skip_candidate: `no`

### DEV-HZ-175 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Continuation prompt template for Agent Teams mode after a Codex review found issues. It preserves the team-leader workflow while forcing a fresh team and review-first planning for the next implementation round.
- algorithmic_behavior: The template tells the Team Leader to continue within RLCR, treat previous teammates as gone, analyze the Codex feedback before spawning new members, avoid redoing already-correct work, provide cold-start context to every new member, prioritize critical issues, and verify current file state; see `prompt-template/claude/agent-teams-continue.md:1-12`.
- inputs_outputs_state: It has no template variables. Output is appended to the next Claude prompt along with `agent-teams-core.md` when Agent Teams remains active. The state transition is conversational: previous round review feedback becomes the new round’s assignment context, but prior team membership is explicitly invalidated.
- gates_or_invariants: The stop hook appends the continuation template only when `AGENT_TEAMS` is true and review has not already started, at `hooks/loop-codex-stop-hook.sh:2207-2217`. If either continuation or core template is missing, the hook uses a shorter fallback that still requires delegation at `hooks/loop-codex-stop-hook.sh:2218-2225`.
- dependencies_and_callers: Runtime caller is `hooks/loop-codex-stop-hook.sh`, which loads `prompt-template/claude/agent-teams-continue.md` and `prompt-template/claude/agent-teams-core.md` together at `hooks/loop-codex-stop-hook.sh:2207-2227`. The core template reinforces cold-start team member context at `prompt-template/claude/agent-teams-core.md:17`.
- edge_cases_or_failure_modes: The template addresses stale-team references, over-retrying already completed work, missing context for newly spawned members, multi-round work overflow, and partial changes from prior rounds. Missing template content degrades to generic delegation guidance, losing the specific continuation safeguards.
- validation_or_tests: `tests/test-agent-teams.sh` verifies the continuation template exists, is at least 200 bytes, and contains a `Continuation Context` section at `tests/test-agent-teams.sh:372-385`.
- skip_candidate: `no`

### DEV-HZ-205 `file` `skills/ask-gemini/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction surface for delegating an up-to-date research question to Gemini CLI and incorporating the returned, source-backed answer into the main response.
- algorithmic_behavior: The skill requires invoking only `${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh`, preserves optional `--gemini-model` and `--gemini-timeout` as separate shell arguments, and passes the remaining free-form question as one quoted final argument; see `skills/ask-gemini/SKILL.md:15-31`. It explicitly forbids unquoted `$ARGUMENTS` because the shell would re-parse user text at `skills/ask-gemini/SKILL.md:33-39`.
- inputs_outputs_state: Inputs are a free-form question/task and optional model/timeout flags. Outputs are Gemini stdout for the answer and stderr for status; the skill says responses are saved under `.humanize/skill/<timestamp>/output.md` at `skills/ask-gemini/SKILL.md:41-61`. The backing script creates `.humanize/skill/<unique-id>/{input,output,metadata}.md` and cache logs at `scripts/ask-gemini.sh:16-19` and `scripts/ask-gemini.sh:184-199`.
- gates_or_invariants: Gemini must be instructed to perform Google Search for current information at `skills/ask-gemini/SKILL.md:10-13` and `scripts/ask-gemini.sh:236-241`. The script blocks missing `gemini`, empty questions, invalid model characters, and unknown flags at `scripts/ask-gemini.sh:91-168`. It runs with `--sandbox` by default unless `HUMANIZE_GEMINI_YOLO` enables `--yolo` at `scripts/ask-gemini.sh:224-231`.
- dependencies_and_callers: Depends on the Gemini CLI, `scripts/ask-gemini.sh`, `scripts/portable-timeout.sh`, `hooks/lib/project-root.sh`, writable project `.humanize/skill` storage, and writable cache or fallback cache. Monitoring support references ask-gemini skill invocations through `scripts/lib/monitor-skill.sh` as surfaced by the repository search.
- edge_cases_or_failure_modes: Exit code `1` covers validation and process/empty-response failures, `124` indicates timeout, and other codes are propagated from Gemini; the skill’s exit table is at `skills/ask-gemini/SKILL.md:48-55`. The script writes metadata for timeout, process error, empty response, and success at `scripts/ask-gemini.sh:294-380`, then prints the response at `scripts/ask-gemini.sh:382-388`.
- validation_or_tests: No dedicated ask-gemini test was found in the inspected `tests/run-all-tests.sh` excerpt; the main behavioral contract is encoded in `skills/ask-gemini/SKILL.md` and the backing implementation in `scripts/ask-gemini.sh`. Related monitor plumbing recognizes ask-gemini invocations through the `.humanize/skill` storage convention.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item evidence sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`