# agent_27 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-027 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Robustness regression suite for the Humanize RLCR core shell algorithms: setup, state parsing, active-session discovery, hook validators, git/goal parsing, template rendering, path validation, cancellation authorization, and timeout portability. It is executable behavioral evidence around production scripts and libraries rather than production runtime code.
- algorithmic_behavior: The directory recursively contains 16 shell tests, 8,155 total lines. Each file creates isolated temp fixtures through `tests/test-helpers.sh`, sources production libraries or scripts, drives edge inputs, then records pass/fail summaries. Important child roles include:
  - `test-base-branch-detection.sh`: reconstructs setup base-branch selection logic with `origin/HEAD`, local `main`, local `master`, and failure when no default branch exists; see `detect_base_branch()` at `tests/robustness/test-base-branch-detection.sh:37` and cases at lines 73, 100, 129, 156.
  - `test-cancel-security-robustness.sh`: tests `is_cancel_authorized` from `hooks/lib/loop-common.sh` against valid cancel moves, missing `.cancel-requested`, shell injection, wrong source/dest paths, mixed quoting, symlink source files, and paths containing `finalize`; see lines 39-90 for allowed forms and 99-392 for rejection cases.
  - `test-concurrent-state-robustness.sh`: validates state file parsing and `find_active_loop` under whitespace, CRLF, missing fields, concurrent reads, simulated writes, malformed YAML, old/cancelled sessions, huge/zero rounds, and permissions; see parser cases at lines 59-156, active-loop cases at 170-236, concurrency at 250-318, strict rejects at 332-367.
  - `test-git-operations-robustness.sh`: drives production `humanize_parse_git_status` and git-state detection across clean, modified, staged, untracked, deleted, renamed, empty repo, detached HEAD, rebase, merge, shallow, non-git, and permission-error scenarios; see setup/helpers at lines 36-60 and cases at 71-480.
  - `test-goal-tracker-robustness.sh`: drives production `humanize_parse_goal_tracker` over AC list/table formats, active/completed/deferred tasks, issue counts, missing/empty/malformed/truncated/binary files, mixed AC syntax, and long goal summaries; see helpers at lines 39-57 and cases at 70-562.
  - `test-hook-input-robustness.sh`: pipes JSON into production hook validators and monitor routines, covering valid Read/Write/Bash JSON, invalid/missing fields, long commands, Unicode, deep JSON rejection, binary/null handling, terminal/log rendering, command modification detection, and parser availability; see cases at lines 35-231 and monitor/parser checks at 245-619.
  - `test-hook-system-robustness.sh`: covers hook validators not covered by input-only tests: edit/write/bash/stop decisions, blocked `state.md` mutation, goal-tracker immutability after round 0, mutable goal-tracker allowances, concurrent hook invocations, corrupted/missing state handling, large JSON, escaped nulls, and deep JSON; see cases at lines 39-150, 316-561, 611-838.
  - `test-path-validation-robustness.sh`: validates production `setup-rlcr-loop.sh` plan path constraints using mock `codex`; accepts simple relative paths and rejects absolute paths, spaces, shell metacharacters, symlink files/parents/chains, empty/comment-only/short/missing/directory plans; see helper at lines 78-108 and cases at 122-474.
  - `test-plan-file-robustness.sh`: tests production plan-content validation for valid/rich/large/mixed-line-ending plans and rejects empty/comment-only/nonexistent/unreadable/symlink/directory/null/disappearing plans; see helpers at lines 27-130 and cases at 178-529.
  - `test-session-robustness.sh`: tests `find_active_loop` session selection across newest sessions, finalize sessions, many sessions, zombie-loop protection, empty/nonexistent bases, unexpected names, symlinks, spaces, deep paths, rapid creation, finished/cancelled sessions, and Unicode names; see cases at lines 33-306.
  - `test-setup-scripts-robustness.sh`: broad `setup-rlcr-loop.sh` CLI/setup regression suite covering usage, argument validation, plan validation, git repo gates, dirty working tree, active-loop block, symlink rejection, valid startup, timeout/model/full-review args, `--skip-impl` scaffolding, and dependency checks; see helpers at lines 31-84 and cases at 102-1162.
  - `test-state-file-robustness.sh`: tests `parse_state_file`, strict mode, defaults, `get_current_round`, `full_review_round`, drift-tracking defaults, special values, malformed/empty/binary/CRLF state files; see cases at lines 33-504.
  - `test-state-transition-robustness.sh`: verifies state lifecycle invariants: valid rounds, finalize detection/precedence, cancel ignored, new active loop after cancel, invalid/missing state fields, newest lexicographic selection, and wrong-location state ignored; see helpers at lines 30-74 and cases at 98-406.
  - `test-template-error-robustness.sh`: error-path coverage for `hooks/lib/template-loader.sh`: missing template fallback, malformed placeholders, invalid variable names, injected template syntax in values, newlines, BOM, whitespace templates, special filenames, permissions, concurrent loads, subdirectories, symlinks; see cases at lines 34-322.
  - `test-template-stress-robustness.sh`: stress/performance-ish template rendering for 1KB/100KB templates and values, 50 substitutions, regex/sed metacharacters, ampersands, backslashes, newlines, empty values, placeholder-injection prevention, repeated/boundary placeholders; see cases at lines 33-276.
  - `test-timeout-robustness.sh`: validates `scripts/portable-timeout.sh` detection/export and `run_with_timeout` behavior for quick commands, timeout exit 124, args, exit-code preservation, pipelines, short/zero timeout, large output, retries, nonexistent/empty command, special args, signal handling, and subshells; see cases at lines 31-261.
- inputs_outputs_state: Inputs are generated fixture repos, `.humanize/rlcr/<session>` directories, plan markdown files, state YAML frontmatter, hook JSON payloads, template strings/files, goal trackers, and shell commands. Outputs are shell exit codes plus pass/fail counters. State transitions under test include `state.md` active loop, `finalize-state.md`, `cancel-state.md`, `complete-state.md`, `.cancel-requested`, review/summary scaffolds, and git states such as normal/detached/rebase/merge/shallow.
- gates_or_invariants: Enforces production invariants around safe plan path syntax, no symlink traversal, dirty/non-git repo rejection, no concurrent RLCR start, active-session selection by newest valid directory, finalize precedence, cancel authorization requiring a signal and exact move shape, strict state parser rejection of malformed critical state, hook block/pass-through decisions, template literal rendering without shell expansion or placeholder reinjection, and portable timeout semantics.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and hook executables under `hooks/`. These tests are likely called by project-level test runners or manually during release/regression validation; no single directory-local runner was found in the assigned subtree.
- edge_cases_or_failure_modes: Heavy coverage for malformed JSON/YAML/Markdown, binary/null bytes, Unicode, CRLF, huge files/values, concurrency, disappearing files, permission-denied files, symlink chains, command injection metacharacters, stale/zombie sessions, missing dependencies, missing git commits, active-loop collisions, timeout implementation absence, and platform differences around `timeout`/`gtimeout`.
- validation_or_tests: The directory is itself validation. I inspected all 16 files recursively via `find`, `wc -l`, and targeted line indexes; I did not execute the suite because the assignment requests research notes only and this environment is read-only.
- skip_candidate: `no`

### DEV-HZ-057 `file` `scripts/cancel-rlcr-session.sh`
- cursor: `[_]`
- core_role: Session-scoped RLCR cancellation command for the Humanize dashboard. It turns a named active session into a cancelled session without touching other sessions in the same project.
- algorithmic_behavior: Parses `--session-id`, optional `--project`, optional `--force`, and a legacy positional session id at `scripts/cancel-rlcr-session.sh:28-49`. It validates the session id against path traversal and shell/path hazards at lines 57-76, resolves `PROJECT_ROOT` from `--project`, `CLAUDE_PROJECT_DIR`, or `pwd` at lines 78-84, then targets `$PROJECT_ROOT/.humanize/rlcr/$SESSION_ID` at line 86. It selects exactly one active state file in priority order: `state.md` normal loop, `methodology-analysis-state.md`, then `finalize-state.md` at lines 99-108. If finalizing and not forced, it emits `FINALIZE_NEEDS_CONFIRM` and exits 2 at lines 114-118. Success creates `.cancel-requested` and renames the active state file to `cancel-state.md` at lines 120-121.
- inputs_outputs_state: Inputs are CLI args, current/project directory, optional `CLAUDE_PROJECT_DIR`, and session files under `.humanize/rlcr/<sid>`. Outputs include stdout status tokens `NO_SESSION`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, or `CANCELLED <sid>`; stderr human messages; exit codes documented at lines 16-20. State transition is active state file to `cancel-state.md` plus `.cancel-requested`.
- gates_or_invariants: The session id must not contain `/` or backslash, must not be `.`/`..`, must not start with dot or parent token, and must match `[A-Za-z0-9._-]+` at lines 65-76. Project root must resolve via `cd`/`pwd` or exit 3 at lines 81-84. The target session directory must exist, and one active state file must exist, or cancellation fails at lines 88-112. Finalize cancellation requires explicit `--force`.
- dependencies_and_callers: Mirrors `scripts/cancel-rlcr-loop.sh` behavior per header lines 5-10. Expected caller is dashboard/session tooling that knows a session id. It does not source shared libraries, so the security checks are local to this script.
- edge_cases_or_failure_modes: Legacy positional mode supports one non-flag sid but rejects extra positionals at lines 39-45. `--session-id` and `--project` assume a following argument; with `set -u`, a missing value can terminate as an unbound parameter rather than a clean exit. Existing `cancel-state.md` would be overwritten by `mv` behavior depending on platform. If both normal and methodology/finalize state files exist, normal wins due priority order. Dot-containing ids are allowed except leading dot and parent-token patterns.
- validation_or_tests: Related coverage appears in `tests/robustness/test-cancel-security-robustness.sh`, which focuses on shared cancel authorization and symlink/path bypasses, not this exact script. This script’s session-id validation and finalize-force exit behavior would benefit from direct tests.
- skip_candidate: `no`

### DEV-HZ-087 `file` `tests/test-config-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for config merge precedence in `scripts/lib/config-loader.sh`.
- algorithmic_behavior: Sources `tests/test-helpers.sh` and `scripts/lib/config-loader.sh` at `tests/test-config-merge.sh:16-33`, then creates isolated project/user config fixtures and calls `load_merged_config "$PROJECT_ROOT" "$PROJECT_DIR"` with controlled `XDG_CONFIG_HOME` and optional `HUMANIZE_CONFIG`. It reads merged fields using `get_config_value`.
- inputs_outputs_state: Inputs are default config under repository config, user config at `$XDG_CONFIG_HOME/humanize/config.json`, project config at `.humanize/config.json`, and custom config path via `HUMANIZE_CONFIG`. Outputs are pass/fail test-helper events and final summary via `print_test_summary` at line 200. State is temporary fixture directories and JSON files created per test.
- gates_or_invariants: Validates that defaults supply `bitlesson_model=haiku`, `agent_teams=false`, and nonempty `gen_plan_mode` at lines 43-64; project config overrides defaults at lines 70-89; project wins over user config at lines 95-109; user keys are preserved when project lacks the key at lines 115-132; nulls in higher layers do not override lower values at lines 138-150; `HUMANIZE_CONFIG` replaces the default project config path at lines 156-173; all layers merge additively at lines 179-198.
- dependencies_and_callers: Direct dependency on `scripts/lib/config-loader.sh` and `tests/test-helpers.sh`. It is likely called by a shell test runner or manual validation to prevent configuration precedence regressions.
- edge_cases_or_failure_modes: Covers null stripping and additive merge but does not cover malformed JSON, missing `jq`, nested object merge semantics, arrays, boolean/string type preservation beyond `agent_teams`, unreadable config files, or conflicts between `HUMANIZE_CONFIG` and user config beyond the custom project path case.
- validation_or_tests: This file is the validation artifact. I inspected all 200 lines; it was not executed in the read-only research run.
- skip_candidate: `no`

### DEV-HZ-117 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, covering template path discovery, loading, rendering, safe fallback behavior, directory validation, literal metacharacter handling, placeholder-injection prevention, and edge-case variable names/Unicode.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12`, defines local `pass`/`fail` counters at lines 20-34, then performs 41 numbered checks. It tests `get_template_dir` at lines 41-52; `load_template` existing/missing file behavior at 54-78; `render_template` single/multiple/multiline/special values at 80-145; `load_and_render` real template integration at 146-164; unused and unreplaced variables at 166-194; `load_and_render_safe` fallback and normal loading at 196-221; `validate_template_dir` at 224-244; shell metacharacter literal rendering at 247-486; placeholder injection prevention at 488-543; and additional edge cases at 545-639. It exits 0 only when `TESTS_FAILED` is zero at lines 641-659.
- inputs_outputs_state: Inputs are template strings, template files under `prompt-template`, fallback strings, and `NAME=value` render arguments. Outputs are rendered strings and process exit status. No persistent repo state is modified; only shell variables/counters are used.
- gates_or_invariants: Required invariants include: missing templates return empty or fallback, existing templates override fallback, unused variables are ignored, unreplaced placeholders remain unchanged, shell metacharacters such as `&`, backslashes, `$`, backticks, pipes, semicolons, globs, quotes, redirection, JSON, regex, and multiline values render literally, and values containing `{{OTHER_VAR}}` are not recursively expanded. Variable names with underscore prefix and digits are accepted at lines 617-639.
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh` and the real `prompt-template` tree, including `block/git-push.md` and `block/wrong-round-number.md`. It is sibling coverage for robustness template tests under `tests/robustness/test-template-error-robustness.sh` and `tests/robustness/test-template-stress-robustness.sh`.
- edge_cases_or_failure_modes: The suite explicitly targets historical sed/gsub-style hazards, especially ampersand replacement and placeholder reinjection. It includes non-ASCII checks at lines 579-615. It does not cover invalid argument forms without `=`, duplicate variables with conflicting values, template read permission errors, or path traversal in template names; those are partly covered by robustness tests.
- validation_or_tests: This file is the validation artifact. I inspected all 659 lines; it was not executed in this read-only research run.
- skip_candidate: `no`

### DEV-HZ-147 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Prompt block used when a git status operation fails or times out, acting as an algorithmic gate message that blocks automated continuation until repository state can be verified.
- algorithmic_behavior: The template renders a single variable, `{{GIT_STATUS_EXIT}}`, into the failure sentence at `prompt-template/block/git-status-failed.md:3`. It lists possible causes: git lock/contention, invalid repository state, or slow large repository at lines 5-8. It instructs the actor to manually check git status and retry at line 10.
- inputs_outputs_state: Input is the git status exit code. Output is Markdown diagnostic text for a hook/script/agent prompt. It does not mutate state, but it communicates that repo-state verification failed and should block or pause workflow.
- gates_or_invariants: The invariant is conservative: if git status cannot be verified, do not assume a clean repository. This supports dirty-worktree and lock-contention safety gates elsewhere.
- dependencies_and_callers: Expected to be loaded through `hooks/lib/template-loader.sh` by hook or script code that detects git status failure. It is in the same template family exercised by `tests/test-template-loader.sh`, though this exact file is not directly referenced there.
- edge_cases_or_failure_modes: Only one placeholder is present; if caller omits `GIT_STATUS_EXIT`, the raw placeholder remains. The message is generic and does not include command stderr, path, or timeout duration, so caller-side context must carry those details if needed.
- validation_or_tests: Template-loader tests validate generic placeholder rendering and missing-placeholder behavior; no direct test for this exact template was identified in the assigned files.
- skip_candidate: `no`

### DEV-HZ-177 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Claude prompt fragment that switches an RLCR cycle into Agent Teams mode and assigns the model the Team Leader role.
- algorithmic_behavior: Declares “Agent Teams Mode” at `prompt-template/claude/agent-teams-instructions.md:1`, tells the model it is operating as Team Leader within RLCR at line 3, and sets first-round behavior: read the plan thoroughly before creating the team at line 5. It enumerates required context surfaces: plan file, goal tracker, and work summary at lines 6-8.
- inputs_outputs_state: Inputs are the surrounding rendered prompt and the plan/goal/summary files referenced by the RLCR runtime. Output is instruction text that shapes downstream agent decomposition, task assignment, and summary-writing behavior. It does not directly mutate state.
- gates_or_invariants: The prompt enforces read-before-delegation and requires the goal tracker to be read before splitting tasks. It also requires a work summary after teammates finish, creating a completion-evidence handoff gate.
- dependencies_and_callers: Expected to be loaded by prompt assembly code when config enables `agent_teams`; `tests/test-config-merge.sh` verifies the `agent_teams` default/override behavior, and template-loader tests verify template loading/rendering generally.
- edge_cases_or_failure_modes: The fragment assumes first round only and does not define later-round behavior, failure handling, teammate count, path boundaries, or exact summary schema. Those must be supplied by surrounding prompt templates or runtime state.
- validation_or_tests: No direct test for this exact template was found in assigned files; its activation is indirectly related to config merge coverage for `agent_teams`.
- skip_candidate: `no`

### DEV-HZ-207 `file` `skills/humanize-refine-plan/SKILL.md`
- cursor: `[_]`
- core_role: Skill/flow specification for refining an annotated `gen-plan` implementation plan into a comment-free refined plan plus a QA ledger while preserving schema and convergence semantics.
- algorithmic_behavior: Defines metadata at `skills/humanize-refine-plan/SKILL.md:1-6` and a workflow graph at lines 18-48. The flow parses arguments, loads merged config, validates IO through `scripts/validate-refine-plan-io.sh`, extracts `CMT:`/`ENDCMT` blocks with a stateful scanner, reports parse errors with location/context, classifies comments as question/change_request/research_request, optionally asks user in discussion mode, processes comments in order, repairs or blocks invalid plan output, generates QA from `prompt-template/plan/refine-plan-qa-template.md`, optionally creates translated variants, writes atomically, and reports paths/counts/mode/convergence.
- inputs_outputs_state: Required input is `--input <annotated-plan.md>` containing valid `gen-plan` schema and at least one comment block at lines 50-54. Optional inputs are `--output`, `--qa-dir`, `--alt-language`, `--discussion`, and `--direct` at lines 55-60. Outputs are a refined plan, QA ledger, and optional language variants at lines 96-107. State is filesystem output written atomically to the selected plan/QA paths.
- gates_or_invariants: Argument invariants: `--discussion` and `--direct` are mutually exclusive, validator must not receive `--alt-language`, and omitted output means in-place refinement plus separate QA at lines 62-65. Workflow invariants: preserve `gen-plan` schema, remove all resolved `CMT:` blocks, keep required sections intact, preserve optional sections, restrict task routing tags to `coding` or `analyze`, generate QA from shipped template, and atomic-write all outputs at lines 67-87. Validation exit codes are specified at lines 131-143.
- dependencies_and_callers: Hydrated with `{{HUMANIZE_RUNTIME_ROOT}}` at lines 12-16. Depends on `scripts/validate-refine-plan-io.sh`, merged humanize config behavior, `prompt-template/plan/refine-plan-qa-template.md`, and the runtime flow/skill invoker (`/flow:humanize-refine-plan` or `/skill:humanize-refine-plan`) at lines 144-160.
- edge_cases_or_failure_modes: Validation can fail for missing/empty input, no comment blocks, missing required sections, unwritable output/QA dirs, or invalid args per lines 131-143. Parse errors must include line/column/heading/context. Ambiguous comments require either user discussion or safe direct assumptions. Alternate language handling accepts names or ISO codes, treats English as no-op, preserves identifiers, and skips duplicate-language variants at lines 108-129.
- validation_or_tests: This is an instruction artifact, not executable code. Related validation is delegated to `scripts/validate-refine-plan-io.sh`; no assigned test file directly exercises this skill.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present for DEV-HZ-027, DEV-HZ-057, DEV-HZ-087, DEV-HZ-117, DEV-HZ-147, DEV-HZ-177, DEV-HZ-207
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`