# agent_05 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: Repository documentation and plan-contract directory. It contains user-facing installation/usage docs, RLCR/BitLesson workflow contracts, a frozen streaming protocol contract, and the generated viz monitor implementation plan that drives core monitor/web behavior. Recursive contents inspected: `docs/bitlesson.md`, `docs/usage.md`, `docs/streaming-protocol.md`, `docs/install-for-claude.md`, `docs/install-for-codex.md`, `docs/install-for-kimi.md`, `docs/drafts/viz-monitor-web.md`, `docs/plans/viz-monitor-web.md`, `docs/images/monitor.png`, and `docs/images/rlcr-workflow.svg`.
- algorithmic_behavior: `docs/bitlesson.md:7-23` defines provider routing for BitLesson selection from merged config, including Codex/OpenAI routing for `gpt-*` and `oN-*`, Claude routing for `claude-*|haiku|sonnet|opus`, fallback when provider binaries are absent, and `provider_mode: "codex-only"` forcing Codex/OpenAI routing. `docs/bitlesson.md:27-35` defines loop-time behavior: initialize `.humanize/bitlesson.md`, run `scripts/bitlesson-select.sh` for each task/sub-task, apply selected lesson IDs or `NONE`, and validate `## BitLesson Delta` in every round summary. `docs/streaming-protocol.md:7-13` defines the live-log source and SSE channel shape for RLCR cache logs. `docs/streaming-protocol.md:15-31` fixes truncation/rotation resync, snapshot/append behavior, remote SSE auth, reconnect replay, latency budget, and backpressure. `docs/plans/viz-monitor-web.md:13-100` defines AC gates for CLI migration, data-source reuse, multi-loop viewing, live-log latency, history browsing, auth, cancel, project isolation, test matrix, and code-style compliance. `docs/plans/viz-monitor-web.md:224-246` maps these gates to ordered tasks T0-T14 with dependencies.
- inputs_outputs_state: Inputs are drafts, plans, `.humanize/rlcr/<session>/` metadata, cache logs under `~/.cache/humanize/<sanitized-project>/<session>/`, merged Humanize config, plugin install targets, and monitor CLI flags. Outputs are plan docs, user install state, monitor web behavior, BitLesson summary blocks, SSE frames, and executable test expectations. The streaming contract outputs `snapshot`, `append`, `resync`, and `eof` JSON payloads over SSE with strictly increasing event ids and canonical filenames rather than absolute paths (`docs/streaming-protocol.md:12-13`). The viz plan outputs a task DAG where `T2` architecture review gates streaming implementation and `T13` validates the full AC-9 matrix (`docs/plans/viz-monitor-web.md:234-245`).
- gates_or_invariants: BitLesson requires `Action: none|add|update` with consistent lesson ID semantics (`docs/bitlesson.md:38-51`). Streaming requires path scoping to exact cache-log basename, standard base64 payloads, byte offsets, resync on shrink/inode change/disappearance, max 64 KiB raw snapshot chunks, remote SSE over HTTPS with token query param, 256 retained events, median append-to-render `<= 2.0s`, and no silent data loss (`docs/streaming-protocol.md:12-31`). The viz plan forbids new dashboard-only hooks and dashboard writes to `.humanize/rlcr/` (`docs/plans/viz-monitor-web.md:22-29`), requires localhost default plus auth for non-localhost binding (`docs/plans/viz-monitor-web.md:55-63`), and forbids workflow-marker terms in implementation code (`docs/plans/viz-monitor-web.md:96-100`). Install docs define runtime bundle invariants for Codex and Kimi skills (`docs/install-for-codex.md:25-34`, `docs/install-for-kimi.md:26-34`, `docs/install-for-kimi.md:56-67`).
- dependencies_and_callers: `docs/usage.md:58-69` is the command reference tying `/gen-idea`, `/explore-idea`, `/gen-plan`, `/refine-plan`, `/start-rlcr-loop`, and `/ask-codex` together. `docs/usage.md:118-147` documents `start-rlcr-loop` runtime options including quiz, Codex model, push behavior, full-review rounds, and agent teams. `docs/plans/viz-monitor-web.md:177-191` references implementation paths: `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, `scripts/lib/monitor-skill.sh`, `scripts/cancel-rlcr-loop.sh`, `viz/server/parser.py`, `viz/server/watcher.py`, `viz/server/app.py`, `viz/scripts/viz-start.sh`, `viz/static/js/*`, removed `commands/viz.md`/`skills/humanize-viz`, `tests/test-viz.sh`, `README.md`, and `docs/usage.md`. `docs/images/rlcr-workflow.svg:3-17` visually models Plan -> Implement -> Review Summary -> Code Review -> Done plus feedback loops. `docs/images/monitor.png` is a 900x173 RGBA PNG screenshot asset, support evidence rather than algorithm logic.
- edge_cases_or_failure_modes: Streaming covers late joiners, rotated/truncated/recreated/missing logs, retained-history misses, invalid `Last-Event-Id`, client overflow, public-network exposure, and full-file polling regressions (`docs/streaming-protocol.md:15-31`). Viz plan covers startup race with metadata but no cache logs, two concurrent active loops, remote unauthenticated reads, project switch mutation bugs, project-global cancel hazards in multi-loop UI, and per-project tmux/port collisions (`docs/plans/viz-monitor-web.md:83-94`, `docs/plans/viz-monitor-web.md:296-304`). BitLesson covers empty or inconsistent deltas and invalid lesson IDs (`docs/bitlesson.md:47-51`). Install docs cover missing Codex/Kimi scripts, missing hooks, unsupported old Codex hook feature naming, and writable output/cache directories (`docs/install-for-codex.md:102-117`, `docs/install-for-kimi.md:211-269`).
- validation_or_tests: `docs/plans/viz-monitor-web.md:83-94` mandates tests for concurrent sessions, missing-cache startup, rotation/truncation resync, auth on all endpoints, project isolation, monitor backward compatibility, and cache-path parity. `docs/streaming-protocol.md:27-31` states latency and backpressure assertions, including CI failure for median latency above 2s. `docs/bitlesson.md:47-51` defines strict validation for BitLesson deltas. `docs/install-for-codex.md:36-81` and `docs/install-for-kimi.md:97-108` provide install verification surfaces. The assigned `tests/test-viz.sh` and related `tests/test-streaming.sh` are downstream executable checks for these docs contracts.
- skip_candidate: `no`

### DEV-HZ-035 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Command workflow definition for turning a draft into a structured implementation plan and optionally auto-starting RLCR after convergence. This is core planning/RLCR algorithm policy, not executable shell.
- algorithmic_behavior: The workflow is strictly sequential (`commands/gen-plan.md:32-45`): parse mode, load merged config, validate IO, run relevance check, run Codex first-pass analysis, synthesize Claude candidate plan, run iterative Claude/second-Codex convergence, resolve issues/disagreements, generate final plan, then write/report/optional auto-start. Phase 0 parses `--auto-start-rlcr-if-converged`, `--discussion`, `--direct`, and `--coach`, rejects simultaneous `--discussion` and `--direct`, and initializes coach-mode state (`commands/gen-plan.md:51-70`). Phase 0.5 delegates config loading to `scripts/lib/config-loader.sh` and defines merge precedence plus legacy `chinese_plan` fallback into `alternative_plan_language` (`commands/gen-plan.md:134-200`). Phase 5 runs a second Codex reasonability review and explicitly checks deferred-AC semantics, AC/task bidirectional coverage, DEC/FUT linkage, and convergence status (`commands/gen-plan.md:331-381`). Phase 7 defines final plan schema and generation rules (`commands/gen-plan.md:460-641`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, an input draft, an output path, merged config from default/user/project layers, Codex first-pass findings, Claude candidate plan state, second Codex review rounds, user decisions, and optional coach quiz answers. Outputs are the plan file, optional translated variant, convergence metadata, pending decisions, and optional RLCR startup. State variables include `AUTO_START_RLCR_IF_CONVERGED`, `GEN_PLAN_MODE`, `PLAN_CONVERGENCE_STATUS`, `HUMAN_REVIEW_REQUIRED`, `ALT_PLAN_LANGUAGE`, `ALT_PLAN_LANG_CODE`, `COACH_CHECK_STATUS`, `COACH_CHECK_COUNT`, `COACH_CHECK_SUMMARY`, and `COACH_MAINLINE_LEDGER` (`commands/gen-plan.md:53-69`, `commands/gen-plan.md:161-200`, `commands/gen-plan.md:378-381`).
- gates_or_invariants: Hard no-coding constraint limits pre-auto-start writes to the plan output and optional translated variant (`commands/gen-plan.md:20-29`). Tool phases must not be parallelized across phases (`commands/gen-plan.md:32-35`). IO validation exit codes 0-7 are mapped to stop/continue behavior (`commands/gen-plan.md:204-222`). Relevance check stops for `NOT_RELEVANT` drafts (`commands/gen-plan.md:226-252`). Direct mode skips convergence, forces `partially_converged`, and cannot satisfy auto-start (`commands/gen-plan.md:331-334`). Deferred work cannot remain as current-scope AC; it must become `FUT-*` plus resolved `DEC-*` linkage or force partial convergence and human review (`commands/gen-plan.md:345-349`, `commands/gen-plan.md:366-381`, `commands/gen-plan.md:613-621`). Every AC needs positive and negative tests, every AC must map to at least one task, and every task must target current-scope ACs only (`commands/gen-plan.md:611-625`). Auto-start requires converged discussion mode, no pending decisions, resolved deferred-work DEC/FUT linkage, and coach success if enabled (`commands/gen-plan.md:709-731`).
- dependencies_and_callers: Calls `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`, and `scripts/lib/config-loader.sh` (`commands/gen-plan.md:5-7`, `commands/gen-plan.md:136-146`, `commands/gen-plan.md:204-210`, `commands/gen-plan.md:262-265`, `commands/gen-plan.md:727-731`). Uses Task subagents for relevance and Explore investigations (`commands/gen-plan.md:232-240`, `commands/gen-plan.md:319-327`). The output plan is intended as input to `/humanize:start-rlcr-loop` (`commands/gen-plan.md:719-731`).
- edge_cases_or_failure_modes: Missing Codex or Codex runtime failure triggers AskUserQuestion with retry or Claude-only continuation options (`commands/gen-plan.md:280-285`). Malformed optional config is warned and ignored, but malformed default config or missing `jq` stops execution (`commands/gen-plan.md:140-147`, `commands/gen-plan.md:191-198`). Unsupported alternative language disables translation with a warning (`commands/gen-plan.md:187-190`). Coach mismatch is not treated as generic wrong answer; memory mismatch, self-check rejection, and education gaps can revise the plan, block expansion, or create pending decisions (`commands/gen-plan.md:89-130`). Max convergence rounds with unresolved opinions carry decisions forward (`commands/gen-plan.md:369-381`). Auto-start failure must be reported with manual fallback (`commands/gen-plan.md:727-737`).
- validation_or_tests: Validation is defined by the plan self-review in Phase 8: read the complete plan and verify completeness, draft alignment, explicit disagreement handling, no deferred AC leakage, AC/task coverage, DEC/FUT linkage, future-work isolation, and no lingering handoff template section (`commands/gen-plan.md:654-668`). It also validates optional translation filename/content rules (`commands/gen-plan.md:684-707`) and report metrics such as AC count, convergence rounds, coach status, unresolved decisions, language unification, and auto-start status (`commands/gen-plan.md:739-749`).
- skip_candidate: `no`

### DEV-HZ-065 `file` `scripts/validate-gen-idea-io.sh`
- cursor: `[_]`
- core_role: Runtime validation script for `/humanize:gen-idea` IO. It is a deterministic gate that classifies input mode, validates `--n`, derives output paths and companion JSON path, prevents partial/colliding writes, resolves template path, and emits machine-readable key/value output.
- algorithmic_behavior: Parses `<idea-text-or-path> [--n <int>] [--output <path>]` and rejects unknown or duplicate positional arguments (`scripts/validate-gen-idea-io.sh:17-67`). Validates required idea input, integer `N`, and range 2-10 (`scripts/validate-gen-idea-io.sh:69-84`). Uses a narrow path heuristic: only whitespace-free inputs ending in `.md` are treated as missing-path intent when not found; slashes alone are allowed in inline ideas (`scripts/validate-gen-idea-io.sh:90-102`). Existing readable non-empty `.md` files become `INPUT_MODE=file`; otherwise inline mode emits the idea body in a stdout sentinel block and derives a slug from the first 40 chars (`scripts/validate-gen-idea-io.sh:104-141`). Resolves default output under `$PROJECT_ROOT/.humanize/ideas/<slug>-<timestamp>.md` when `--output` is absent and auto-creates that default directory only (`scripts/validate-gen-idea-io.sh:143-164`). Enforces `.md` suffix, derives `DIRECTIONS_JSON_FILE="${OUTPUT_FILE%.md}.directions.json"`, checks draft and companion collisions, checks write permission, validates template existence, then prints success metadata (`scripts/validate-gen-idea-io.sh:152-218`).
- inputs_outputs_state: Inputs are CLI args, current git root or current working directory, `CLAUDE_PLUGIN_ROOT` when set, file readability/existence state, output directory permissions, and template file existence. Outputs are exit codes 0-8, human-readable `VALIDATION_ERROR` or `WARNING` lines, and success keys: `INPUT_MODE`, optional `IDEA_BODY_FILE`, `OUTPUT_FILE`, `DIRECTIONS_JSON_FILE`, `SLUG`, `TEMPLATE_FILE`, `N`, and optional `=== IDEA_BODY_BEGIN/END ===` body (`scripts/validate-gen-idea-io.sh:203-217`). State variables include `IDEA_INPUT`, `N`, `OUTPUT_FILE`, `INPUT_MODE`, `IDEA_BODY_FILE`, `SLUG`, `PROJECT_ROOT`, `DEFAULT_OUTPUT`, `OUTPUT_DIR`, `DIRECTIONS_JSON_FILE`, and `TEMPLATE_FILE`.
- gates_or_invariants: Exit codes are explicit: 1 missing/empty input, 2 path-like invalid file, 3 output parent missing, 4 output exists, 5 no write permission, 6 invalid args, 7 template missing, 8 companion exists (`scripts/validate-gen-idea-io.sh:4-13`). `--n` must be numeric and 2-10 despite the message saying “non-negative integer” for the regex failure (`scripts/validate-gen-idea-io.sh:75-84`). Output must end in `.md` so companion derivation is unambiguous (`scripts/validate-gen-idea-io.sh:152-160`). Existing draft or companion file blocks before any command write can occur (`scripts/validate-gen-idea-io.sh:172-181`). Inline mode avoids tempfiles and uses stdout sentinel blocks as the authoritative body (`scripts/validate-gen-idea-io.sh:129-136`, `scripts/validate-gen-idea-io.sh:213-217`).
- dependencies_and_callers: Called by `commands/gen-idea.md` Phase 1, which parses its stdout and maps exit codes to user messages (`commands/gen-idea.md:48-66`). The companion path is later used by `commands/gen-idea.md` to write/validate directions JSON and by `commands/explore-idea.md` as input for prototype exploration. Uses `git rev-parse`, `realpath`, `date`, `sed`, `tr`, `basename`, `dirname`, and shell filesystem tests. Template dependency is `prompt-template/idea/gen-idea-template.md` under `CLAUDE_PLUGIN_ROOT` or relative to the script (`scripts/validate-gen-idea-io.sh:190-201`).
- edge_cases_or_failure_modes: A real missing path containing whitespace and not ending in `.md` is intentionally treated as inline text (`scripts/validate-gen-idea-io.sh:90-96`). Short inline ideas under 10 chars emit a warning but proceed (`scripts/validate-gen-idea-io.sh:132-135`). Slash-containing ideas like `undo/redo` and `CI/CD` are inline unless they end in `.md`, preventing false `INPUT_NOT_FOUND` (`scripts/validate-gen-idea-io.sh:90-102`). Default output directory creation is best-effort and failures are ignored until the later directory/write checks (`scripts/validate-gen-idea-io.sh:162-169`). On systems without `realpath -m`, the script falls back to the original output string (`scripts/validate-gen-idea-io.sh:158`).
- validation_or_tests: Assigned `tests/test-gen-idea-dual-write.sh` validates success emission of `DIRECTIONS_JSON_FILE`, dual-write command references, no-write behavior when output/companion exists, and schema validation fixture. Related `tests/test-validate-gen-idea-io.sh:44-160` validates `.md` suffix enforcement, companion path derivation, exit 8 for companion collisions, exit 4 for draft collision, exit 1 for missing idea, and slash-containing inline ideas.
- skip_candidate: `no`

### DEV-HZ-095 `file` `tests/test-gen-idea-dual-write.sh`
- cursor: `[_]`
- core_role: Executable specification for the gen-idea dual-write contract linking the IO validator, `commands/gen-idea.md`, and directions JSON schema validation. It is a structural shell test, not a live agent test.
- algorithmic_behavior: Creates an isolated test directory, mock git repository, fake plugin root with `prompt-template/idea/gen-idea-template.md`, exports `CLAUDE_PLUGIN_ROOT`, and wraps `scripts/validate-gen-idea-io.sh` in `run_validate` (`tests/test-gen-idea-dual-write.sh:28-40`). Positive tests assert that validation succeeds and emits `DIRECTIONS_JSON_FILE`, that `commands/gen-idea.md` contains the companion path instruction, that it hints to `explore-idea`, that it includes `validate-directions-json.sh` in allowed tools, and that the valid fixture passes schema validation when `jq` exists (`tests/test-gen-idea-dual-write.sh:42-90`). Negative tests assert validation fails for existing draft and existing companion JSON, and that `commands/gen-idea.md` has error handling alongside dual-write instructions (`tests/test-gen-idea-dual-write.sh:92-125`).
- inputs_outputs_state: Inputs are the validator script, gen-idea command markdown, schema validator script, `tests/fixtures/directions/valid.directions.json`, temporary output dirs/files, and availability of `jq`. Outputs are pass/fail/skip records through `test-helpers.sh` and final test summary (`tests/test-gen-idea-dual-write.sh:15-18`, `tests/test-gen-idea-dual-write.sh:127-128`). State is held in `TEST_DIR`, `MOCK_REPO`, `PLUGIN_ROOT`, `EXIT_CODE`, `OUTPUT`, and derived `DJSON`.
- gates_or_invariants: The core invariant is “no partial write possible”: validation must reject pre-existing output or companion before the command writes either file (`tests/test-gen-idea-dual-write.sh:5-8`, `tests/test-gen-idea-dual-write.sh:96-118`). `commands/gen-idea.md` must reference `DIRECTIONS_JSON_FILE`, `explore-idea`, and `validate-directions-json.sh` so the command and validator share the same dual-write contract (`tests/test-gen-idea-dual-write.sh:57-76`). Schema validation is optional-skipped if `jq` is unavailable rather than failing the whole suite (`tests/test-gen-idea-dual-write.sh:78-90`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/validate-gen-idea-io.sh`, `commands/gen-idea.md`, `scripts/validate-directions-json.sh`, and `tests/fixtures/directions/valid.directions.json` (`tests/test-gen-idea-dual-write.sh:15-22`). It validates the contract described in `commands/gen-idea.md:192-260`, where the draft and companion JSON are written and the companion is validated.
- edge_cases_or_failure_modes: Covers output draft collision and companion JSON collision, both of which prevent either artifact from being written (`tests/test-gen-idea-dual-write.sh:96-118`). Skips the schema fixture check if `jq` is missing (`tests/test-gen-idea-dual-write.sh:78-90`). It does not execute Claude/Task phases, so it cannot verify actual direction generation quality; it only checks deterministic shell/file-content surfaces (`tests/test-gen-idea-dual-write.sh:10`).
- validation_or_tests: This file is itself validation. It complements `tests/test-validate-gen-idea-io.sh`, which expands coverage to `.md` suffix, subdir companion derivation, missing idea, and slash-containing inline ideas. It also indirectly validates that `commands/gen-idea.md` cleanup rules exist: if companion schema validation fails, delete both just-written files (`commands/gen-idea.md:244-249`, `commands/gen-idea.md:266-273`).
- skip_candidate: `no`

### DEV-HZ-125 `file` `tests/test-viz.sh`
- cursor: `[_]`
- core_role: Executable specification for the Humanize viz dashboard and `humanize monitor web` migration. It validates shell helpers, Python server modules, parser/analyzer/exporter algorithms, migration away from `/humanize:viz`, foreground monitor dispatch, static assets, and selected regression gates.
- algorithmic_behavior: Performs a Python availability precheck, creates a temp test dir, and then runs grouped tests (`tests/test-viz.sh:15-34`). Group 1 validates shell syntax for `viz-start.sh`, `viz-stop.sh`, and `viz-status.sh` (`tests/test-viz.sh:36-48`). Group 2 compiles `parser.py`, `analyzer.py`, `exporter.py`, `app.py`, and `watcher.py` (`tests/test-viz.sh:50-62`). Group 3 builds a mock `.humanize/rlcr/<session>` with `state.md`, `goal-tracker.md`, round summaries, and review result, then asserts parser behavior: valid session detection, parsed fields, canonical round indices, missing round summary handling, verdict parsing, goal tracker extraction, completed AC status, task counts, and session listing (`tests/test-viz.sh:64-216`). It also tests malformed session rejection and duration calculation across on-disk round summaries when `state.current_round` lags (`tests/test-viz.sh:218-281`). Group 4 validates analytics over empty and mock sessions (`tests/test-viz.sh:283-332`). Group 5 validates Markdown export of bilingual/dict-valued session data (`tests/test-viz.sh:334-388`). Groups 6-8 validate opt-in daemon helper presence, `monitor web` migration, port probe host handling, static assets, English-only UI file, and Flask requirement (`tests/test-viz.sh:390-518`).
- inputs_outputs_state: Inputs are `viz/scripts/*`, `viz/server/{parser,analyzer,exporter,app,watcher}.py`, `scripts/humanize.sh`, `commands/start-rlcr-loop.md`, `README.md`, `viz/static/*`, `viz/server/requirements.txt`, and temporary RLCR session fixtures. Outputs are pass/fail records via `test-helpers.sh`. State fixtures include session frontmatter (`current_round`, `max_iterations`, `plan_file`, branches, Codex model), goal-tracker sections, summaries with BitLesson Delta blocks, review results with mainline verdict, and file mtimes for duration regression (`tests/test-viz.sh:70-146`, `tests/test-viz.sh:237-265`).
- gates_or_invariants: Parser must accept only valid sessions with `state.md`, include rounds `0..current_round` even when a summary is missing, parse `Mainline Progress Verdict: ADVANCED` into `advanced`, and count tasks/ACs consistently (`tests/test-viz.sh:147-216`). Duration must span every on-disk round summary, not only `range(current_round+1)` (`tests/test-viz.sh:237-281`). Analyzer must exclude non-reviewed rounds from verdict distribution (`tests/test-viz.sh:317-329`). Exporter must return a string and handle `{en,zh}` dicts without `TypeError` (`tests/test-viz.sh:340-386`). Migration invariants require removed `commands/viz.md`, removed `skills/humanize-viz`, `_humanize_monitor_web` in `scripts/humanize.sh`, dispatch including `web`, no `/humanize:viz` reference in `commands/start-rlcr-loop.md`, and README docs for `humanize monitor web` (`tests/test-viz.sh:418-462`). Foreground port probing must branch on requested `--host` and use `$probe_host` instead of literal localhost (`tests/test-viz.sh:464-479`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and references `PLUGIN_ROOT`, `VIZ_DIR`, and `SERVER_DIR` (`tests/test-viz.sh:17-22`). It tests production functions/classes through Python imports and static grep. It corresponds directly to the generated plan’s AC-9 test matrix and references `tests/test-viz.sh` as the extended matrix path (`docs/plans/viz-monitor-web.md:190`, `docs/plans/viz-monitor-web.md:245`). `scripts/humanize.sh:_humanize_monitor_web` is the caller-facing entry under `humanize monitor web` (`scripts/humanize.sh:1203-1347`, `scripts/humanize.sh:1355-1384`).
- edge_cases_or_failure_modes: Handles missing `python3` by skipping (`tests/test-viz.sh:28-32`). Checks malformed session directories without `state.md` are invalid (`tests/test-viz.sh:218-235`). Covers lagging `current_round` with extra summary files on disk (`tests/test-viz.sh:237-281`). Checks no regression to auto-started dashboard markers from RLCR setup/cancel after migration to explicit CLI entry (`tests/test-viz.sh:390-406`). Checks foreground port selection for specific non-loopback host conflict risk (`tests/test-viz.sh:464-479`). It does not perform browser/SSE live latency testing; that is specified in docs and likely covered by separate streaming tests.
- validation_or_tests: This file is itself validation and ends with `print_test_summary "Humanize Viz Tests"` (`tests/test-viz.sh:514-518`). Static assertions connect to docs contracts: `docs/streaming-protocol.md` for SSE/live-log behavior, `docs/plans/viz-monitor-web.md` for monitor web migration, project isolation, and test matrix, and `scripts/humanize.sh:1290-1318` for host-aware port probing.
- skip_candidate: `no`

### DEV-HZ-155 `file` `prompt-template/block/mainline-verdict-missing.md`
- cursor: `[_]`
- core_role: Prompt/block template for a stop-hook gate when Codex implementation review output omits the required mainline progress verdict. It defines the human/agent-facing block reason that prevents unsafe RLCR state transition.
- algorithmic_behavior: The template states that review output is missing `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED`, explains that Humanize cannot safely update drift state or choose the next-round prompt without it, and instructs retrying exit so Codex reruns implementation review (`prompt-template/block/mainline-verdict-missing.md:1-9`). It includes placeholders for the review result and review prompt files (`prompt-template/block/mainline-verdict-missing.md:11-13`).
- inputs_outputs_state: Inputs are `{{REVIEW_RESULT_FILE}}` and `{{REVIEW_PROMPT_FILE}}`. Output is rendered markdown used as the `reason` in a JSON block decision. It participates in drift-state gating because the missing verdict blocks choosing `ADVANCED`, `STALLED`, or `REGRESSED` state.
- gates_or_invariants: The invariant is that an implementation review must contain exactly the required mainline verdict line before Humanize can update drift state or choose next prompt (`prompt-template/block/mainline-verdict-missing.md:3-7`). Without the verdict, stop hook blocks rather than guessing.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh:block_missing_mainline_verdict`, which loads and renders this template with a fallback copy and emits JSON `{ "decision": "block", ... }` (`hooks/loop-codex-stop-hook.sh:1483-1515`). The hook passes `REVIEW_RESULT_FILE` and `REVIEW_PROMPT_FILE` to `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1503-1505`).
- edge_cases_or_failure_modes: If the template is missing or cannot render, the stop hook has an inline fallback with the same text and placeholders (`hooks/loop-codex-stop-hook.sh:1489-1505`). If Codex review returns partial findings without the mainline verdict, this gate prevents drift-state corruption and forces a rerun rather than continuing with ambiguous progress.
- validation_or_tests: Direct tests were not assigned for this template, but it is referenced by grep in the stop hook and its behavior is covered by stop-hook gating tests around missing review outputs. The concrete validation surface is the hook’s JSON block decision (`hooks/loop-codex-stop-hook.sh:1507-1515`).
- skip_candidate: `no`

### DEV-HZ-185 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: One-line prompt template injected into the next Claude prompt when Codex review contains open questions. It enforces human clarification before resolving other Codex findings.
- algorithmic_behavior: The template says Codex found Open Questions and Claude must use `AskUserQuestion` to clarify those questions with the user first, before resolving any other Codex findings (`prompt-template/claude/open-question-notice.md:1`). It is a control-flow notice, not a content template with variables.
- inputs_outputs_state: Input is detection of an “Open Question” marker in Codex review content. Output is inserted text in `NEXT_PROMPT_FILE` before the goal tracker reference, changing the next-round task order so user clarification precedes implementation/review fixes.
- gates_or_invariants: Clarification precedence is mandatory: open questions block resolving other Codex findings until AskUserQuestion has been used. The hook’s detector looks for a line containing “Open Question” with length under 40 chars when `ASK_CODEX_QUESTION=true` (`hooks/loop-codex-stop-hook.sh:2140-2149`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` when open-question detection succeeds (`hooks/loop-codex-stop-hook.sh:2151-2156`). If the template load is empty, the hook uses an inline fallback string with the same instruction (`hooks/loop-codex-stop-hook.sh:2153-2156`). The hook inserts it between the Codex review end marker and `## Goal Tracker Reference` (`hooks/loop-codex-stop-hook.sh:2157-2165`).
- edge_cases_or_failure_modes: Detection is intentionally narrow: only short lines containing “Open Question” trigger injection, so verbose headings or differently worded question sections may not trigger. Missing template does not break behavior because of the inline fallback. If `ASK_CODEX_QUESTION` is false, open-question notice injection is skipped even if review text contains questions.
- validation_or_tests: Direct tests were not assigned for this exact template. Functional validation belongs to stop-hook tests that inspect next prompt injection and AskUserQuestion behavior. The caller lines provide the concrete gating behavior (`hooks/loop-codex-stop-hook.sh:2140-2165`).
- skip_candidate: `no`

### DEV-HZ-215 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for production goal-tracker parsing in `scripts/humanize.sh`. It validates monitor/RLCR summary state extraction across normal, malformed, legacy, and edge-case goal tracker files.
- algorithmic_behavior: Sources `tests/test-helpers.sh` and production `scripts/humanize.sh`, then calls `humanize_parse_goal_tracker` and `humanize_parse_goal_tracker_issue_counts` directly (`tests/robustness/test-goal-tracker-robustness.sh:15-24`). It defines helpers to split pipe-delimited parser output fields (`tests/robustness/test-goal-tracker-robustness.sh:38-61`). Positive tests cover AC counting in list and table formats, active task counting excluding completed/deferred statuses, completed task rows, unique completed ACs, and ultimate goal extraction (`tests/robustness/test-goal-tracker-robustness.sh:63-237`). Negative/edge tests cover nonexistent file defaults, empty files, 60 ACs, special characters, malformed headers, truncated files, binary content, open issues, deferred tasks, blocking/queued issue split, legacy open issue fallback, headers-only file, mixed AC formats, and long goal summaries (`tests/robustness/test-goal-tracker-robustness.sh:239-589`).
- inputs_outputs_state: Inputs are generated temporary `goal-tracker.md` variants and the production functions in `scripts/humanize.sh`. `humanize_parse_goal_tracker` returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`tests/robustness/test-goal-tracker-robustness.sh:35-37`, `scripts/humanize.sh:69-153`). `humanize_parse_goal_tracker_issue_counts` returns `blocking_issues|queued_issues|open_issues` (`tests/robustness/test-goal-tracker-robustness.sh:53-61`, `scripts/humanize.sh:36-67`). Test outputs are pass/fail records and a final summary.
- gates_or_invariants: Nonexistent tracker must return `0|0|0|0|0|0|No goal tracker` (`tests/robustness/test-goal-tracker-robustness.sh:247-254`, `scripts/humanize.sh:71-76`). Active tasks equal active table data rows minus completed/deferred rows, floored at zero (`scripts/humanize.sh:95-123`). Total and completed ACs are unique `AC-?[0-9]+(\.[0-9]+)?` matches in their sections (`scripts/humanize.sh:87-93`, `scripts/humanize.sh:129-135`). New issue schema separates blocking and queued issues; legacy `### Open Issues` maps to blocking for safety (`scripts/humanize.sh:56-64`, `tests/robustness/test-goal-tracker-robustness.sh:451-498`). Goal summary is the first non-empty content line under `### Ultimate Goal`, with fallback `No goal defined` (`scripts/humanize.sh:146-151`).
- dependencies_and_callers: Depends on `scripts/humanize.sh`, specifically `humanize_parse_goal_tracker_issue_counts` at `scripts/humanize.sh:38-67` and `humanize_parse_goal_tracker` at `scripts/humanize.sh:71-153`. These functions are used by terminal monitor paths and parser parity logic; `viz/server/parser.py` references using the same methodology as `scripts/humanize.sh:humanize_parse_goal_tracker` for goal tracker parsing. The tests are part of robustness coverage for monitor/RLCR state display.
- edge_cases_or_failure_modes: Covers malformed files without proper `###` headers returning zero ACs (`tests/robustness/test-goal-tracker-robustness.sh:314-336`), truncated markdown still yielding parseable AC count (`tests/robustness/test-goal-tracker-robustness.sh:338-357`), binary bytes mixed into a markdown file without crashing (`tests/robustness/test-goal-tracker-robustness.sh:359-381`), large 60-item AC lists without overflow (`tests/robustness/test-goal-tracker-robustness.sh:268-289`), special shell/markdown characters in descriptions (`tests/robustness/test-goal-tracker-robustness.sh:291-312`), headers-only files (`tests/robustness/test-goal-tracker-robustness.sh:500-534`), and long goal summary handling without display-layer truncation (`tests/robustness/test-goal-tracker-robustness.sh:560-583`).
- validation_or_tests: This file is itself validation and exits with the test summary status (`tests/robustness/test-goal-tracker-robustness.sh:585-590`). It provides deterministic shell coverage for parser behavior that feeds monitor displays and RLCR state summaries.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8/8 item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`