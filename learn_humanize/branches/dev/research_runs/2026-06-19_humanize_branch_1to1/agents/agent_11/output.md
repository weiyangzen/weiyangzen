# agent_11 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable-spec directory for Humanize’s RLCR loop, hook validators, setup state machine, monitoring, planning/explore workflows, config/model routing, cancellation, visualization, and robustness behavior. The tree has top-level shell suites, `tests/robustness/`, `tests/fixtures/`, `tests/fixtures/directions/`, and `tests/mocks/`; `rg --files tests` found all descendants with no deeper subtrees beyond those directories.
- algorithmic_behavior: `tests/run-all-tests.sh` is the suite coordinator: it defines bounded parallelism with `HUMANIZE_TEST_JOBS` and CPU cap at lines 19-37, enumerates top-level and robustness suites at lines 59-138, marks zsh-only and serial suites at lines 140-149, injects a mock `codex` when missing at lines 155-166, installs a portable `timeout` shim at lines 168-188, runs suites and records exit/time files at lines 225-241, parses pass/fail counts after stripping ANSI at lines 264-283, throttles background jobs at lines 317-340, and prints sorted results/fail details at lines 387-415.
- inputs_outputs_state: Inputs are executable test scripts, mock binaries, fixture JSON, temp git repos, hook JSON payloads, synthetic `.humanize/rlcr/<timestamp>/state.md` trees, and controlled env vars such as `CLAUDE_PROJECT_DIR`, `PATH`, `XDG_CACHE_HOME`, `HUMANIZE_TEST_JOBS`, mock Codex/GitHub variables, and provider/config overrides. Outputs are process exit codes, pass/fail counters, temporary repo/session artifacts, generated RLCR state files, prompts, summaries, contracts, and captured command output. Shared helper `tests/test-helpers.sh` owns `pass`, `fail`, `skip`, summary printing, temp dir cleanup, and basic git repo creation at lines 30-105.
- gates_or_invariants: The suite checks strict single-loop activation, clean git setup gates, plan file validation, state file schema, allowed hook access, forbidden state/plan modification, round-contract presence, mainline verdict presence, full-review cadence, review/finalize transitions, monitor terminal restoration, path canonicalization, session isolation, auth/token rules for viz, and provider/model routing. `tests/run-all-tests.sh` treats any nonzero suite exit or parsed failed count as suite failure at lines 272-283 and preserves full detail for failed suites at lines 398-409.
- dependencies_and_callers: Tests call production scripts/hooks directly rather than mocking most logic: `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-{read,write,edit,bash,plan-file}-validator.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/ask-codex.sh`, validation scripts, viz server modules, and config/model-router libraries. `tests/mocks/gh` emulates `gh pr view/comment`, `gh api`, repo metadata, reactions/comments/reviews, and auth status using fixtures and env knobs at lines 27-246.
- edge_cases_or_failure_modes: Robustness suites cover malformed JSON, binary-looking payloads, deep nesting, missing/corrupt state, partial writes, concurrent reads/writes, symlinks, shell metacharacters, absolute paths, submodules, remote-only base branches, missing dependencies, timeout exit 124, zsh glob hazards, deleted session directories, SIGINT/SIGTERM cleanup, restricted `PATH`, missing template files, huge templates, and stale/legacy `.humanize-*` paths. Fixtures under `tests/fixtures/` provide GitHub API and directions-schema data for deterministic non-network tests.
- validation_or_tests: This directory is itself validation. Key child roles include setup validation (`test-plan-file-validation.sh`, `robustness/test-setup-scripts-robustness.sh`, `robustness/test-path-validation-robustness.sh`), RLCR state/hook validation (`test-state-exit-naming.sh`, `test-stop-gate.sh`, `test-finalize-phase.sh`, `test-plan-file-hooks.sh`, `test-allowlist-validators.sh`, robustness state/hook suites), monitor validation (`test-monitor-e2e-real.sh`, deletion/sigint split wrappers, runtime and zsh safety tests), planning/explore validation (`test-gen-plan.sh`, `test-refine-plan.sh`, `test-validate-*.sh`, `test-worker-result-contract.sh`, `test-explore-*`), and viz/auth/streaming tests.
- skip_candidate: `no`

### DEV-HZ-041 `file` `docs/usage.md`
- cursor: `[_]`
- core_role: User-facing behavioral contract for Humanize commands and the RLCR workflow/state machine.
- algorithmic_behavior: Defines the two-phase RLCR loop: implementation phase where Claude works and Codex reviews summaries, then review phase via `codex review --base <branch>` with `[P0-9]` findings; loop ends only when acceptance criteria are met or no issues remain at lines 5-12. It defines the pre-flight plan-understanding quiz as advisory friction before loop start at lines 16-35, skip semantics for `--skip-quiz` and `--yolo` at lines 37-41, and command flow from `gen-plan`/`refine-plan` into `start-rlcr-loop` at lines 43-56.
- inputs_outputs_state: Inputs include plan paths, draft/idea paths, directions JSON, annotated plan files, CLI flags, config files, env vars, and project path for monitors. Outputs include `.humanize/ideas/<slug>-<timestamp>.md`, companion `.directions.json`, `.humanize/explore/<RUN_ID>/` manifests/reports/results, generated plans, QA ledgers, `.humanize/skill/<timestamp>/` ask-codex artifacts, RLCR state under `.humanize/rlcr/<timestamp>/`, and monitor cache logs under `~/.cache/humanize/<sanitized-project>/<session>/` at lines 80-116, 267-305, and 379-385.
- gates_or_invariants: `start-rlcr-loop` options define max iterations, model/effort, review timeout, plan tracking cleanliness, push-per-round, base branch detection priority, full alignment interval, skip-impl, Codex-question handling, agent teams, and quiz controls at lines 118-148. Config precedence is plugin defaults, user config, project config, then CLI flags at lines 307-314. Codex model precedence is CLI, feature defaults, config defaults, hardcoded fallback at lines 350-354. Remote viz access must require token for non-loopback hosts and rejects WebSocket transport in remote mode at lines 403-413.
- dependencies_and_callers: Aligns with `commands/*.md`, `scripts/setup-rlcr-loop.sh`, `scripts/humanize.sh`, `scripts/ask-codex.sh`, validation scripts, viz scripts/server, and config loader. It is also mirrored by tests such as `test-gen-plan.sh`, `test-refine-plan.sh`, monitor tests, config tests, and viz auth/live-route tests.
- edge_cases_or_failure_modes: Documents full automation risk via `--yolo`, optional plan under `--skip-impl`, remote monitor security requirements, SSH tunnel preferred remote pattern, direct LAN token requirement, and dangerous `HUMANIZE_CODEX_BYPASS_SANDBOX` mode with explicit unsafe environments at lines 37-41, 137-145, 415-454, and 462-495.
- validation_or_tests: Covered by command-structure, setup, config, monitor, viz auth, explore/gen/refine, and ask-codex tests. The doc is behavior-defining rather than executable, so tests indirectly enforce consistency through script/template assertions.
- skip_candidate: `no`

### DEV-HZ-071 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Placeholder helper intended to set up monitor test state directories, but currently no valid scenario exists.
- algorithmic_behavior: Parses `TEST_DIR` and optional `TEST_NAME`, requires the first arg, and rejects every `TEST_NAME` via default `case` branch at lines 12-26. It only echoes `TEST_DIR` after the unreachable success path at line 28.
- inputs_outputs_state: Input arg 1 is target test directory; arg 2 defaults to `default`. If `TEST_DIR` is missing, stderr usage and exit 1 occur at lines 15-18. For all test names, stderr says unknown test and exits 1 at lines 20-25. No `.humanize/rlcr` state is created.
- gates_or_invariants: Fail-closed placeholder: no unsupported monitor fixture can silently succeed. `set -euo pipefail` at line 10 makes unset/failed operations fatal.
- dependencies_and_callers: No internal source dependencies. It is related to monitor e2e/runtime tests but those create their own fixtures directly rather than using this script.
- edge_cases_or_failure_modes: Missing first argument and every requested scenario are intentional failures. The advertised usage says it creates directory/state files, but implementation currently has no available case.
- validation_or_tests: Not a meaningful positive executable spec in current state; useful only as a negative/placeholder contract.
- skip_candidate: `yes: currently contains no executable positive setup case and appears retained as a stub`

### DEV-HZ-101 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: True end-to-end executable spec for `scripts/humanize.sh` terminal RLCR monitor cleanup and log/session behavior.
- algorithmic_behavior: Runs the real `_humanize_monitor_codex` function, not a stub, in bash and zsh. It seeds `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, fake cache logs, shimmed terminal commands, then deletes `.humanize/rlcr` or sends SIGINT and asserts graceful termination. Test functions are `monitor_test_bash_deletion` at line 56, `monitor_test_zsh_deletion` at line 233, `monitor_test_bash_sigint` at line 377, and `monitor_test_zsh_sigint` at line 555.
- inputs_outputs_state: Inputs are temp project directories under `/tmp/test-monitor-e2e-real-$$`, synthetic state frontmatter with `current_round`, `max_iterations`, model/effort, `plan_file`, branches, and `review_started` at lines 65-78, 246-259, 387-400, and 568-581; goal tracker files at lines 80-93, 261-274, 402-415, and 583-596; fake `HOME`/`XDG_CACHE_HOME` and cache logs at lines 95-104, 276-284, 417-425, and 598-606. Outputs are captured monitor logs, pass/fail counters, and process exit evidence.
- gates_or_invariants: Deletion tests require monitor exit within bounded loops, message containing “Monitoring stopped:” and “directory no longer exists”, no zsh/bash glob errors, terminal cleanup message, scroll reset implementation in source, and exit code 0 at lines 167-227 and 335-370. SIGINT tests require process exit or controlled termination, cleanup indication or clean exit code, and no glob errors at lines 492-549 and 649-684.
- dependencies_and_callers: Sources `scripts/humanize.sh` inside generated runner scripts at lines 144-149, 312-319, 464-471, and 632-637. It depends on monitor cache path sanitization matching production sed rules at lines 99-103, 280-284, 421-425, and 602-606. Split wrapper tests `test-monitor-e2e-deletion.sh` and `test-monitor-e2e-sigint.sh` source this file to run subsets.
- edge_cases_or_failure_modes: Covers complete RLCR directory deletion, session directory disappearance, zsh glob strictness, bash/zsh signal handling differences, terminal scroll region reset, missing/late log polling, and process cleanup. Bash SIGINT has a fallback pass path when SIGINT delivery is unreliable but SIGTERM cleanup succeeds at lines 505-518.
- validation_or_tests: The file runs all four tests when executed directly and exits 0 only if `TESTS_FAILED` is zero; summary and verified claims are at lines 691-724. Production monitor cleanup uses `_graceful_stop` to call `_cleanup`, restore terminal, and emit deletion reason in `scripts/humanize.sh` lines 736-777 and checks deleted `.humanize/rlcr` in the main loop at lines 838-844.
- skip_candidate: `no`

### DEV-HZ-131 `file` `hooks/lib/project-root.sh`
- cursor: `[_]`
- core_role: Deterministic project-root and path-canonicalization library for hooks/scripts that must locate the active RLCR state tree and authorize path access consistently.
- algorithmic_behavior: `resolve_project_root` chooses a linked git worktree toplevel when it differs from `CLAUDE_PROJECT_DIR` and is represented by a `.git` file, otherwise `CLAUDE_PROJECT_DIR`, otherwise `git rev-parse --show-toplevel`, otherwise failure at lines 51-75. `canonicalize_path_prefix` resolves only the parent directory and preserves the basename for authorization comparisons at lines 95-119. `canonicalize_path` resolves full path when possible, otherwise parent plus basename, with Python fallback at lines 136-167.
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR`, current git context, and arbitrary filesystem paths. Outputs are canonical absolute root/path strings on stdout or return 1 when no root can be determined. It has no persistent state except `_HUMANIZE_PROJECT_ROOT_SOURCED` include guard at lines 33-36.
- gates_or_invariants: `pwd` is intentionally not a fallback to avoid drift from `cd` during sessions and missed `.humanize/rlcr` lookups at lines 20-22. Symlinked prefixes are normalized for setup-time/hook-time consistency at lines 24-30. Security invariant: `canonicalize_path` dereferences leaf symlinks and must not authorize user-supplied expected filenames; callers should use `canonicalize_path_prefix` for that at lines 121-132.
- dependencies_and_callers: Called by setup, cancel, stop gate, ask-codex/gemini, all core validators, and loop-common. `rg` found `resolve_project_root` use in `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/ask-codex.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-{bash,read,write,edit,plan-file}-validator.sh`, and `scripts/rlcr-stop-gate.sh`. Read/write validators use `canonicalize_path_prefix` for expected path comparisons.
- edge_cases_or_failure_modes: Handles missing git root, missing env root, symlinked macOS prefixes such as `/var` vs `/private/var`, non-existent target leaf paths, inaccessible parents, lack of `realpath`, lack of `python3`, and linked-worktree workers inheriting coordinator `CLAUDE_PROJECT_DIR`.
- validation_or_tests: Exercised by root/session/validator tests, especially `test-stop-gate.sh`, `test-plan-file-hooks.sh`, `test-allowlist-validators.sh`, session tests, cancel security tests, and robustness hook/path suites. The file is core algorithm, not just utility, because wrong root selection changes which RLCR loop state is active.
- skip_candidate: `no`

### DEV-HZ-161 `file` `prompt-template/block/round-contract-missing.md`
- cursor: `[_]`
- core_role: Stop-hook blocking prompt for the round-contract gate; prevents round exit without a focused mainline contract.
- algorithmic_behavior: Instructs the agent to write the current round contract to `{{ROUND_CONTRACT_FILE}}` at lines 3-4. Required fields are a single mainline objective, target ACs, blocking issues, queued/out-of-scope issues, and success criteria at lines 6-11. It explicitly says not to continue without the contract because the loop uses it to prevent goal drift at line 13.
- inputs_outputs_state: Input is the rendered `ROUND_CONTRACT_FILE` placeholder. Output is a block reason shown to the agent; expected side effect is creation/update of `round-N-contract.md` in the active loop directory.
- gates_or_invariants: One mainline objective per round, explicit blocking vs queued separation, concrete success criteria, and no exit continuation until the contract exists.
- dependencies_and_callers: Loaded through the template-loader/stop-hook path when contract evidence is missing. `scripts/setup-rlcr-loop.sh` creates `round-0-contract.md` and embeds contract setup instructions in normal and skip-impl prompts at lines 1200-1228 and 1309-1319; review follow-up prompts require refreshing `ROUND_CONTRACT_FILE` in `hooks/loop-codex-stop-hook.sh` lines 1556-1582.
- edge_cases_or_failure_modes: If the template is missing, template-loader fallbacks are used elsewhere; if the contract is missing or placed in the wrong location, the loop blocks because drift cannot be bounded. The template does not itself validate file contents; validation is hook-driven.
- validation_or_tests: Covered by `test-template-references.sh`, `test-templates-comprehensive.sh`, `test-task-tag-routing.sh`, setup robustness skip-impl contract tests at `tests/robustness/test-setup-scripts-robustness.sh` lines 959-983, and hook-system tests around contract/write access.
- skip_candidate: `no`

### DEV-HZ-191 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory periodic Codex full-alignment review template; defines drift/stagnation gates and COMPLETE/STOP verdict contract for implementation review rounds.
- algorithmic_behavior: Requires reading the original plan first and scopes completion gates to current acceptance criteria/current-scope tasks while excluding future/out-of-scope items at lines 5-13. It requires a goal-tracker audit with AC status, forgotten item detection, deferral audit, and completion summary at lines 24-53. It requires a mainline drift audit and exact `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` line at lines 55-69. It requires historical review/summary inspection and stagnation detection at lines 80-105.
- inputs_outputs_state: Inputs are rendered placeholders for current round, plan file, summary content, commit-history section, goal tracker, docs path, completed iterations, loop timestamp, previous round paths, and review result file. Output is Codex review content written to `{{REVIEW_RESULT_FILE}}`, ending with either action items plus possibly `STOP`, or `COMPLETE` only under strict current-scope completion.
- gates_or_invariants: Future/out-of-scope `FUT-*` items must not block completion, but deferred current-scope items are incomplete at lines 11-13 and 115-117. Missing mainline verdict blocks the round at line 69. `STOP` is required as final line when stagnation is detected at lines 97-105 and 114. `COMPLETE` is permitted only when all current-scope plan tasks/ACs are fully met with no current-scope deferrals at lines 109-117.
- dependencies_and_callers: Used by RLCR stop-hook review prompt generation and drift logic. `hooks/lib/loop-common.sh` extracts and normalizes the mainline verdict at lines 602-662; `hooks/loop-codex-stop-hook.sh` blocks missing verdicts at lines 1483-1516 and triggers drift stop via `stop_for_mainline_drift` at lines 1445-1481.
- edge_cases_or_failure_modes: Ambiguous or omitted verdict lines normalize to unknown and block via missing-verdict path. Repeated issues, no meaningful AC progress, circular feedback, no code changes, or repeated Codex feedback trigger stagnation handling. Current-scope deferral prevents COMPLETE even if the rest passes.
- validation_or_tests: `test-gen-plan.sh` checks full-alignment template behavior around future work and current-scope completion gates. State robustness tests cover drift fields, and stop-hook tests cover missing verdict/drift-stop behavior. Template existence/references are covered by template reference/comprehensive tests.
- skip_candidate: `no`

### DEV-HZ-221 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Large robustness executable spec for `scripts/setup-rlcr-loop.sh`, covering CLI parsing, plan validation, git/root safety, YAML-safe state serialization, skip-impl transitions, and dependency checks.
- algorithmic_behavior: Creates temp repos and plans via `create_minimal_plan` and `init_basic_git_repo` at lines 31-63, prepares restricted runtime bins at lines 65-80, and invokes setup with isolated `CLAUDE_PROJECT_DIR` through `run_rlcr_setup` at lines 82-93. It then runs grouped tests for arguments, plan validation, YAML safety, git repo prerequisites, mutual exclusion, symlink protection, success path, timeouts, full-review interval, skip-impl, and dependencies.
- inputs_outputs_state: Inputs include setup flags (`--help`, `--max`, `--codex-timeout`, `--codex-model`, `--plan-file`, `--full-review-round`, `--skip-impl`), temp plan files, mock `codex`/`jq`/`timeout`, branch names, `.gitignore`, and synthetic active `.humanize/rlcr` state. Outputs are setup exit codes/messages plus generated `.humanize/rlcr/<timestamp>/state.md`, `.review-phase-started`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, and `round-0-prompt.md` in skip-impl cases.
- gates_or_invariants: Enforces missing/invalid args rejection at lines 102-220; rejects comment-only, too-short, space-containing, metacharacter, and absolute plan paths at lines 230-347; rejects YAML-unsafe branch/model strings at lines 357-408; rejects non-git dirs, no-commit repos, dirty `.humanizeconfig`, tracked plan without flag, and active existing loops at lines 418-530; rejects symlink plan files and symlink parent dirs at lines 540-593. Full-review round must be numeric and at least 2 at lines 770-859.
- dependencies_and_callers: Directly exercises `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, shared helpers, git, jq/codex availability, and setup output consumed later by stop hooks/monitor. The implementation gates it asserts are visible in `scripts/setup-rlcr-loop.sh`: dependency check lines 345-368, active loop check lines 374-384, plan validation lines 460-614, content validation lines 623-676, YAML validation lines 697-720, clean worktree lines 728-746, state creation lines 885-912, skip-impl marker line 927, and prompt/contract creation lines 1200-1290.
- edge_cases_or_failure_modes: Covers empty values, negative/non-numeric numbers, absent arguments, unknown flags, branch creation differences across git versions, restricted PATH, missing `codex`, missing `jq`, multiple missing deps in one error, git command timeout exit 124, zero timeout accepted as current behavior, huge timeout accepted, optional plan in `--skip-impl`, and preserving plan anchors in skip-impl with plan at lines 985-1049.
- validation_or_tests: Self-validates with `print_test_summary` and exits with the aggregate result at lines 1187-1192. Skip-impl generated artifacts are checked for `build_finish_round=0`, `review_started: true`, no placeholder goal tracker text, BitLesson summary defaults, contract mainline objective, prompt contract reference, original-plan prompt/contract anchors at lines 885-1049.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV-HZ-011, DEV-HZ-041, DEV-HZ-071, DEV-HZ-101, DEV-HZ-131, DEV-HZ-161, DEV-HZ-191, DEV-HZ-221
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`