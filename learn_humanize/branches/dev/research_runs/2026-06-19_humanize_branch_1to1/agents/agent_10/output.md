# agent_10 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-010 `directory` `templates`
- cursor: `[_]`
- core_role: Project-level seed templates. Recursive inspection found one descendant, `templates/bitlesson.md`, which is the canonical initial content copied into each project’s `.humanize/bitlesson.md`.
- algorithmic_behavior: The template defines the BitLesson knowledge-base schema: each entry must follow the exact field order under `## Entry Template (Strict)`, including `Lesson ID`, `Scope`, `Problem Description`, `Root Cause`, `Solution`, `Constraints`, `Validation Evidence`, and `Source Rounds` (`templates/bitlesson.md:5-19`). The `## Entries` section is the append point for concrete lessons (`templates/bitlesson.md:21-23`).
- inputs_outputs_state: Input is the plugin template file plus a project root and relative BitLesson path. `scripts/bitlesson-init.sh` copies the template to `<project>/.humanize/bitlesson.md` only if missing (`scripts/bitlesson-init.sh:83-86`) and prints the resolved path (`scripts/bitlesson-init.sh:88`). State transition is “no project KB” -> “placeholder KB with no concrete lessons”.
- gates_or_invariants: The template’s placeholder lesson ID is intentionally not counted as a concrete lesson by the delta validator, which excludes `<BL-YYYYMMDD-short-name>` and placeholder-like IDs (`scripts/bitlesson-validate-delta.sh:227-239`). `bitlesson-init.sh` rejects absolute or `..` relpaths (`scripts/bitlesson-init.sh:70-72`) and refuses to overwrite non-regular existing paths (`scripts/bitlesson-init.sh:78-80`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh` initializes `.humanize/bitlesson.md` from `templates/bitlesson.md` at RLCR setup; `docs/bitlesson.md` documents that initialization path (`docs/bitlesson.md:27-34`). The selector later reads the initialized file (`scripts/bitlesson-select.sh:82-97`), and the stop hook validates summaries against it through `scripts/bitlesson-validate-delta.sh` (`hooks/loop-codex-stop-hook.sh:865-880`).
- edge_cases_or_failure_modes: A freshly initialized template has no `## Lesson:` entry, so `bitlesson-select.sh` short-circuits to `LESSON_IDS: NONE` instead of invoking a model (`scripts/bitlesson-select.sh:93-97`). If strict mode disallows empty `none`, the stop gate blocks when concrete lesson count is zero (`scripts/bitlesson-validate-delta.sh:254-267`).
- validation_or_tests: Covered by `tests/test-bitlesson-select-routing.sh` for placeholder short-circuit behavior and `tests/test-bitlesson-validate-delta.sh` for concrete lesson ID/delta consistency. `tests/run-all-tests.sh` includes both suites (`tests/run-all-tests.sh:105-108`) plus template coverage (`tests/run-all-tests.sh:61-68`).
- skip_candidate: `no`

### DEV-HZ-040 `file` `docs/bitlesson.md`
- cursor: `[_]`
- core_role: Behavior-defining documentation for the BitLesson memory workflow inside RLCR rounds.
- algorithmic_behavior: Defines provider routing inputs and the round workflow. The selector reads `bitlesson_model` from the merged config hierarchy (`docs/bitlesson.md:5-13`), routes `gpt-*`/`o[N]-*` to Codex and `claude-*`/`haiku`/`sonnet`/`opus` to Claude (`docs/bitlesson.md:14-18`), and falls back to the default Codex model when the configured provider binary is missing (`docs/bitlesson.md:19`). The workflow initializes `.humanize/bitlesson.md`, runs `scripts/bitlesson-select.sh` per task, records selected IDs or `NONE`, and requires a `## BitLesson Delta` in every round summary (`docs/bitlesson.md:25-35`).
- inputs_outputs_state: Inputs are merged config, provider binaries, the project KB `.humanize/bitlesson.md`, task/sub-task descriptions, paths, and round summary content. Outputs are selected lesson IDs plus rationale from `bitlesson-select.sh` and a validated `## BitLesson Delta` block in the summary. State transitions are: missing KB -> initialized KB; task -> selected lessons/NONE; round summary -> accepted or blocked delta; optional KB update -> concrete lesson entry.
- gates_or_invariants: The summary contract requires exactly `Action: none|add|update`, `Lesson ID(s)`, and `Notes` (`docs/bitlesson.md:36-45`). `Action: none` must use `NONE` or empty IDs, while `add`/`update` must reference concrete `BL-YYYYMMDD-short-name` IDs present in `.humanize/bitlesson.md` (`docs/bitlesson.md:47-50`). Strict mode can block repeated empty `none` via `--require-bitlesson-entry-for-none` (`docs/bitlesson.md:51`).
- dependencies_and_callers: Implemented by `scripts/bitlesson-init.sh`, `scripts/bitlesson-select.sh`, `scripts/lib/model-router.sh`, `scripts/bitlesson-validate-delta.sh`, and `hooks/loop-codex-stop-hook.sh`. The stop hook parses BitLesson frontmatter (`hooks/loop-codex-stop-hook.sh:115-167`) and invokes the delta validator for non-finalize rounds when required (`hooks/loop-codex-stop-hook.sh:865-880`).
- edge_cases_or_failure_modes: Codex-only installs force BitLesson selection onto Codex/OpenAI even when legacy defaults route to Claude (`docs/bitlesson.md:21-23`; implemented at `scripts/bitlesson-select.sh:127-130`). Empty KBs may either allow `none` or block depending on `bitlesson_allow_empty_none`/env override. Delta detection ignores headings inside fences and HTML comments (`scripts/bitlesson-validate-delta.sh:92-179`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` exercises provider routing, dependency fallback, codex-only override, missing/placeholder KB behavior, and stable output. `tests/test-bitlesson-validate-delta.sh` exercises delta shape, fenced/comment false positives, IDs, notes, and strict empty-KB blocking. Both are included by `tests/run-all-tests.sh` (`tests/run-all-tests.sh:105-108`).
- skip_candidate: `no`

### DEV-HZ-070 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-wide executable specification and orchestration harness for core behavior tests.
- algorithmic_behavior: Builds a fixed `TEST_SUITES` array of shell suites covering validators, stop gates, monitor behavior, config/model routing, BitLesson, visualization, and robustness (`tests/run-all-tests.sh:59-138`). It runs most suites in parallel with bounded concurrency (`tests/run-all-tests.sh:19-37`, `tests/run-all-tests.sh:317-340`), keeps signal-heavy runtime tests serial (`tests/run-all-tests.sh:145-149`, `tests/run-all-tests.sh:366-375`), captures each suite’s output/exit/time in a temp directory (`tests/run-all-tests.sh:151-153`, `tests/run-all-tests.sh:225-240`), aggregates `Passed:`/`Failed:` counters from suite output (`tests/run-all-tests.sh:264-270`), and exits nonzero if any suite fails (`tests/run-all-tests.sh:398-415`).
- inputs_outputs_state: Inputs are `HUMANIZE_TEST_JOBS`, available shell/runtime binaries, and each suite under `tests/`. Outputs are sorted per-suite pass/fail lines, skipped-suite notices, detailed failing logs, aggregate totals, and process exit status 0/1. Internal state is held in `OUTPUT_DIR`, `ACTIVE_PIDS`, `SERIAL_SUITES`, `FAILED_SUITES`, and `SORT_FILE`.
- gates_or_invariants: `HUMANIZE_TEST_JOBS` must be an integer >= 1 (`tests/run-all-tests.sh:33-37`). Default concurrency caps at 8 and bottoms at 1 (`tests/run-all-tests.sh:21-31`). zsh-only suites are skipped if `zsh` is unavailable (`tests/run-all-tests.sh:140-143`, `tests/run-all-tests.sh:310-314`). Missing suite files are skipped, not fatal (`tests/run-all-tests.sh:299-302`). A suite fails if its exit code is nonzero or parsed `Failed:` count is >0 (`tests/run-all-tests.sh:272-278`).
- dependencies_and_callers: Depends on shell, `mktemp`, `sed`, `grep`, `sort`, `cut`, optional `zsh`, and `python3` for portable milliseconds and timeout shim (`tests/run-all-tests.sh:168-187`, `tests/run-all-tests.sh:219-223`). Provides mock `codex` when absent so setup scripts that only check `command -v codex` can run (`tests/run-all-tests.sh:155-166`).
- edge_cases_or_failure_modes: Bash 3.2 compatibility avoids associative arrays and uses PID files (`tests/run-all-tests.sh:286-288`). If `wait -n` is unavailable, it waits the oldest PID (`tests/run-all-tests.sh:39-44`, `tests/run-all-tests.sh:335-339`). ANSI stripping depends on `esc` being set before collection (`tests/run-all-tests.sh:264-267`, `tests/run-all-tests.sh:351`). Suite output without parseable counters contributes zero pass/fail counts but can still fail by exit code.
- validation_or_tests: This is itself the aggregate validation entry point. It includes the assigned monitor deletion test (`tests/run-all-tests.sh:81`), assigned session robustness test (`tests/run-all-tests.sh:123`), BitLesson tests (`tests/run-all-tests.sh:105-108`), and commit-history section test (`tests/run-all-tests.sh:76` indirectly via listed suite `test-codex-review-merge.sh` plus standalone `test-commit-history-section.sh` exists but is not in this runner list).
- skip_candidate: `no`

### DEV-HZ-100 `file` `tests/test-monitor-e2e-deletion.sh`
- cursor: `[_]`
- core_role: Focused executable spec for monitor shutdown when `.humanize/rlcr` is deleted during active monitoring.
- algorithmic_behavior: Sources `tests/test-monitor-e2e-real.sh` (`tests/test-monitor-e2e-deletion.sh:4-5`), then runs only `monitor_test_bash_deletion` and `monitor_test_zsh_deletion` (`tests/test-monitor-e2e-deletion.sh:12-13`). It prints aggregate pass/fail counters and exits 0 only when `TESTS_FAILED` is zero (`tests/test-monitor-e2e-deletion.sh:15-21`).
- inputs_outputs_state: Inputs are the real monitor implementation in `scripts/humanize.sh`, temp projects created by the sourced helper, shell availability, and fake HOME/cache directories. Outputs are pass/fail lines, captured monitor output assertions, and exit code 0/1. State is stored in helper globals `TESTS_PASSED` and `TESTS_FAILED` from the sourced file (`tests/test-monitor-e2e-real.sh:25-37`).
- gates_or_invariants: Bash deletion test creates a realistic RLCR session state and goal tracker (`tests/test-monitor-e2e-real.sh:60-93`), launches `_humanize_monitor_codex`, deletes `.humanize/rlcr`, and requires the monitor to exit within a bounded loop of 20 half-second waits (`tests/test-monitor-e2e-real.sh:156-181`). It requires “Monitoring stopped:”, “directory no longer exists”, no glob errors, cleanup messaging, scroll-region reset in source, and `EXIT_CODE:0` (`tests/test-monitor-e2e-real.sh:187-227`). The zsh variant skips only if `zsh` is unavailable and otherwise applies similar assertions (`tests/test-monitor-e2e-real.sh:238-370`).
- dependencies_and_callers: Depends on `tests/test-monitor-e2e-real.sh`, real `_humanize_monitor_codex` from `scripts/humanize.sh`, `pkill`, shell process control, temp filesystem, and terminal command shims. It is included in the full test runner (`tests/run-all-tests.sh:81`) and separated from SIGINT/runtime monitor suites for parallel stability.
- edge_cases_or_failure_modes: Protects against historical shell glob failures such as zsh “no matches found” and bash “bad pattern” (`tests/test-monitor-e2e-real.sh:8-12`, `tests/test-monitor-e2e-real.sh:200-205`, `tests/test-monitor-e2e-real.sh:360-364`). It force-kills the monitor if it misses the deletion exit deadline, then records a failure (`tests/test-monitor-e2e-real.sh:174-181`, `tests/test-monitor-e2e-real.sh:342-349`).
- validation_or_tests: This file is the narrow validation wrapper. Broader helper coverage remains in `tests/test-monitor-e2e-real.sh`, which also contains SIGINT and other monitor tests beyond this split wrapper.
- skip_candidate: `no`

### DEV-HZ-130 `file` `hooks/lib/methodology-analysis.sh`
- cursor: `[_]`
- core_role: Stop-hook library implementing the Methodology Analysis Phase state transition before an RLCR loop fully exits.
- algorithmic_behavior: `enter_methodology_analysis_phase` guards privacy/re-entry/completion, renames the active state file to `methodology-analysis-state.md`, records the original terminal exit reason, creates an empty done marker, renders `claude/methodology-analysis-prompt.md`, and returns a JSON block decision (`hooks/lib/methodology-analysis.sh:38-100`). `complete_methodology_analysis` requires nonempty `methodology-analysis-done.md`, nonempty `methodology-analysis-report.md`, a present `.methodology-exit-reason`, and a valid reason among `complete|stop|maxiter` before allowing completion (`hooks/lib/methodology-analysis.sh:114-170`). `block_methodology_analysis_incomplete` emits a JSON block with completion instructions (`hooks/lib/methodology-analysis.sh:179-202`).
- inputs_outputs_state: Inputs are `PRIVACY_MODE`, `STATE_FILE`, `LOOP_DIR`, `CURRENT_ROUND`, `MAX_ITERATIONS`, `TEMPLATE_DIR`, exit reason, report/done marker files, and `.methodology-exit-reason`. Outputs are block JSON, stderr diagnostics, marker files, and phase state files. State transitions are active `state.md`/`finalize-state.md` -> `methodology-analysis-state.md`; completion validated -> terminal `<exit_reason>-state.md` by the stop hook (`hooks/loop-codex-stop-hook.sh:617-665`).
- gates_or_invariants: Privacy mode skips entry (`hooks/lib/methodology-analysis.sh:42-46`). Re-entry is blocked if `methodology-analysis-state.md` already exists (`hooks/lib/methodology-analysis.sh:48-52`). Prior nonempty done marker skips phase (`hooks/lib/methodology-analysis.sh:54-62`). Completion fails closed on missing/empty done marker, missing/empty report, missing exit-reason marker, or invalid exit reason (`hooks/lib/methodology-analysis.sh:118-163`).
- dependencies_and_callers: Sourced by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:54`). The stop hook enters this phase on max-iteration exit and finalize completion (`hooks/loop-codex-stop-hook.sh:972-979`, `hooks/loop-codex-stop-hook.sh:988-997`), and also on other stop/circuit-breaker paths. It uses `load_and_render_safe` from `hooks/lib/template-loader.sh` and `jq` to construct block JSON.
- edge_cases_or_failure_modes: The library deliberately does not perform the final terminal rename; the stop hook does it only after a git-clean check, preventing tracked edits made during analysis from slipping through (`hooks/loop-codex-stop-hook.sh:617-665`). Read/write/edit validators restrict the originating session during methodology analysis to sanitized methodology artifacts: reads allow only report/done/state inside the loop dir and block project files (`hooks/loop-read-validator.sh:86-167`); writes/edits allow only report and done (`hooks/loop-write-validator.sh:88-142`, `hooks/loop-edit-validator.sh:71-123`). Spawned agents are a known limitation and rely on the prompt’s sanitization rules (`hooks/loop-read-validator.sh:79-83`).
- validation_or_tests: `tests/test-rlcr-sources.sh` checks methodology-analysis and finalize phases classify as active. App route tests reference cancellation/renaming around `methodology-analysis-state.md`. There is no single dedicated unit test for this file, but behavior is integrated through stop-hook, read/write/edit validator, and lifecycle tests.
- skip_candidate: `no`

### DEV-HZ-160 `file` `prompt-template/block/round-contract-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the Bash validator gate that forbids modifying RLCR round contract files through shell commands.
- algorithmic_behavior: Renders a short denial message: “Do not use Bash commands to modify round contract files” and instructs the agent to use Write/Edit at `{{CORRECT_PATH}}` (`prompt-template/block/round-contract-bash-write.md:1-7`). The actual algorithm is in `hooks/loop-bash-validator.sh`, which detects Bash commands modifying `round-[0-9]+-contract.md`, computes the current active-loop contract path, renders this template, and exits with code 2 (`hooks/loop-bash-validator.sh:540-555`).
- inputs_outputs_state: Input is `CORRECT_PATH`, derived as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-contract.md` (`hooks/loop-bash-validator.sh:546-553`). Output is stderr block text plus validator exit code 2. State does not mutate; it prevents invalid mutation path and preserves round contract integrity.
- gates_or_invariants: Contract writes must go through Write/Edit tooling so round scoping and file validators can enforce current-round location (`hooks/loop-bash-validator.sh:541-544`). The template loader performs single-pass `{{VAR}}` substitution, keeping missing placeholders unchanged and preventing nested placeholder injection (`hooks/lib/template-loader.sh:48-135`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/round-contract-bash-write.md"` in `hooks/loop-bash-validator.sh` (`hooks/loop-bash-validator.sh:548-553`). Related location checks for contract files exist in write/edit/read validators, but this specific template is for Bash command denial.
- edge_cases_or_failure_modes: If the template is missing or empty, the validator uses its inline fallback with the same semantic instruction (`hooks/loop-bash-validator.sh:548-553`). If `CURRENT_ROUND` or `ACTIVE_LOOP_DIR` are wrong upstream, the rendered corrective path could be wrong; the gate itself relies on active-loop discovery and parsed state.
- validation_or_tests: Covered by template loading/comprehensive template tests (`tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`) and Bash validator pattern tests included by `tests/run-all-tests.sh` (`tests/run-all-tests.sh:61-68`). The direct caller line is in `hooks/loop-bash-validator.sh:546-555`.
- skip_candidate: `no`

### DEV-HZ-190 `file` `prompt-template/codex/commit-history-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section injecting the Integral-context signal: accumulated branch commits and recent RLCR round files.
- algorithmic_behavior: The template wraps `{{COMMIT_HISTORY}}` in a code block and lists `{{RECENT_ROUND_FILES}}`, then instructs the reviewer to use the history to detect recurring issues, stalled progress, and drift, weighting recent rounds while watching systemic trends (`prompt-template/codex/commit-history-section.md:1-12`). The stop hook computes commit history from `BASE_COMMIT..HEAD` when `BASE_COMMIT` is an ancestor, otherwise falls back to the last 30 commits with an annotation (`hooks/loop-codex-stop-hook.sh:1042-1052`). It builds up to three prior summary/review file references (`hooks/loop-codex-stop-hook.sh:1054-1060`), renders this section (`hooks/loop-codex-stop-hook.sh:1062-1071`), and injects it into regular or full-alignment Codex review prompts (`hooks/loop-codex-stop-hook.sh:1100-1131`).
- inputs_outputs_state: Inputs are git history, `BASE_COMMIT`, `CURRENT_ROUND`, `LOOP_TIMESTAMP`, and previous round files. Output is prompt text embedded into `regular-review.md` or `full-alignment-review.md` at `{{COMMIT_HISTORY_SECTION}}` (`prompt-template/codex/regular-review.md:23`, `prompt-template/codex/full-alignment-review.md:22`). State transition is not repository state but review-state enrichment: Codex receives cross-round context before deciding COMPLETE vs issues.
- gates_or_invariants: `BASE_COMMIT` must be an ancestor before using range history (`hooks/loop-codex-stop-hook.sh:1042-1045`). Empty history becomes `(no commits yet)` (`hooks/loop-codex-stop-hook.sh:1052`). Round references never go below round 0 and include only recent prior rounds, not the current round (`hooks/loop-codex-stop-hook.sh:1054-1060`). Missing template falls back to an inline equivalent (`hooks/loop-codex-stop-hook.sh:1062-1071`).
- dependencies_and_callers: Depends on `git merge-base`, `git log`, `tail`, and `load_and_render_safe`. Used in both review modes, and covered by `tests/test-commit-history-section.sh`, which mirrors stop-hook logic (`tests/test-commit-history-section.sh:1-10`).
- edge_cases_or_failure_modes: Corrupted, nonexistent, unrelated, or empty `BASE_COMMIT` must not crash under `set -e`; fallback annotates the history as partial (`tests/test-commit-history-section.sh:117-180`, `tests/test-commit-history-section.sh:247-266`). Round 0 uses first-round/no-history placeholders (`tests/test-commit-history-section.sh:33-61`). Missing template fallback must still include commits, round files, and the instruction to read them (`tests/test-commit-history-section.sh:180-222`).
- validation_or_tests: `tests/test-commit-history-section.sh` validates round 0, round 3, corrupted base commit, unrelated base commit, missing template fallback, round 1 boundary, and empty base commit (`tests/test-commit-history-section.sh:33-271`). This test is not listed in `tests/run-all-tests.sh` by name, so it may need direct execution by maintainers unless invoked elsewhere.
- skip_candidate: `no`

### DEV-HZ-220 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for RLCR session discovery, especially `find_active_loop` zombie-loop protection and active/terminal state classification.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-session-robustness.sh:14-18`), creates synthetic `.humanize/rlcr`-style directory trees, calls `find_active_loop`, and asserts returned active loop path or empty string across 18 scenarios (`tests/robustness/test-session-robustness.sh:33-317`). The implementation checks only the newest directory when no session filter is provided, returning it only when `resolve_active_state_file` finds `methodology-analysis-state.md`, `finalize-state.md`, or `state.md` (`hooks/lib/loop-common.sh:264-280`, `hooks/lib/loop-common.sh:332-356`).
- inputs_outputs_state: Inputs are a loop base directory, optional state files, directory names/order, and filesystem shape. Output is the selected active loop path or empty string. Test state is entirely temp-dir filesystem state; each case removes/recreates `$TEST_DIR/rlcr`.
- gates_or_invariants: Newest active session wins (`tests/robustness/test-session-robustness.sh:33-47`). Finalize phase counts active (`tests/robustness/test-session-robustness.sh:48-61`). Only the newest directory is checked without session filter, so older stale `state.md` must not be revived when the newest dir lacks active state or is terminal (`tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:287-302`). Terminal-only `complete-state.md` and `cancel-state.md` are ignored (`tests/robustness/test-session-robustness.sh:259-285`).
- dependencies_and_callers: Tests `find_active_loop`, `resolve_active_state_file`, and indirectly directory sorting via `ls -1d ... | sort -r` (`hooks/lib/loop-common.sh:342-356`). `find_active_loop` is used by hooks/validators to identify the active RLCR loop and has a session-filter path with stricter isolation plus optional bg-marker fallback for stop hook use (`hooks/lib/loop-common.sh:359-425`).
- edge_cases_or_failure_modes: Covers empty/nonexistent base dirs, base dirs with only files, no state files, non-standard names, symlinked dirs, spaces, deep nested state that should be ignored by one-level scanning, rapid creation, terminal states, mixed terminal/newer vs stale/older active, and Unicode-like naming simplification (`tests/robustness/test-session-robustness.sh:128-317`). Performance expectation is <1000ms for 15 sessions (`tests/robustness/test-session-robustness.sh:63-87`).
- validation_or_tests: This file is included in the main test runner under robustness tests (`tests/run-all-tests.sh:121-124`). It ends with `print_test_summary "Session Robustness Test Summary"` and exits with that result (`tests/robustness/test-session-robustness.sh:318-323`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DEV-HZ-010`, `DEV-HZ-040`, `DEV-HZ-070`, `DEV-HZ-100`, `DEV-HZ-130`, `DEV-HZ-160`, `DEV-HZ-190`, `DEV-HZ-220`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`