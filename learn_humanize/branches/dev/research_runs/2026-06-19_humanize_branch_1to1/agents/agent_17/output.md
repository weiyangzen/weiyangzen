# agent_17 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-017 `directory` `prompt-template/explore`
- cursor: `[_]`
- core_role: Template contract directory for `/humanize:explore-idea`, containing the worker dispatch prompt, the run audit report skeleton, and the final plan-ready synthesis skeleton. Recursive inspection found exactly three files: `prompt-template/explore/worker-prompt.md`, `prompt-template/explore/report-template.md`, and `prompt-template/explore/final-idea-template.md`.
- algorithmic_behavior: `worker-prompt.md` defines the per-direction worker state machine: validate worktree/root, assert `HEAD == <BASE_COMMIT>`, create `explore/<RUN_ID>/<DIR_SLUG>`, iterate implementation/test/Codex review up to `<MAX_WORKER_ITERATIONS>`, commit local changes, and emit JSON between sentinel markers as the last action (`worker-prompt.md:67`, `worker-prompt.md:71`, `worker-prompt.md:83`, `worker-prompt.md:94`, `worker-prompt.md:104`, `worker-prompt.md:119`, `worker-prompt.md:130`). `report-template.md` defines the coordinator’s two-tier scoring output: product-direction ranking independent of prototype success and implementation-readiness ranking based on worker outcomes (`report-template.md:18`, `report-template.md:32`). `final-idea-template.md` defines the plan-ready synthesis passed to `/humanize:gen-plan`, including final recommendation, evidence, outcomes, constraints, risks, learnings, and productization commands (`final-idea-template.md:10`, `final-idea-template.md:42`).
- inputs_outputs_state: Inputs are placeholders rendered from validated explore IO and directions JSON: run id, direction id, dir slug, base branch/commit, max iterations, Codex timeout, Codex review model spec, original idea, and direction fields (`worker-prompt.md:8`, `worker-prompt.md:28`, `commands/explore-idea.md:139`). Worker output is a JSON object with schema/version/status/review/test/git fields bounded by `=== EXPLORE_RESULT_JSON_BEGIN ===` and `=== EXPLORE_RESULT_JSON_END ===` (`worker-prompt.md:132`). Coordinator outputs are `.humanize/explore/<RUN_ID>/manifest.json`, `dispatch-prompts/`, `worker-results.jsonl`, `explore-report.md`, and `final-idea.md` (`report-template.md:105`, `commands/explore-idea.md:161`, `commands/explore-idea.md:267`).
- gates_or_invariants: Hard constraints forbid modifying outside the assigned worktree, nested skills/slash commands, subagents, git push, sibling worktree access, and direct Codex CLI usage outside `ask-codex.sh` (`worker-prompt.md:16`). The base-anchor invariant is explicit: do not `git checkout <BASE_BRANCH>`; assert current HEAD matches `<BASE_COMMIT>` before creating the branch (`worker-prompt.md:71`, `commands/explore-idea.md:31`). Codex review must use rendered `gpt-5.5:xhigh` and metadata must confirm model `gpt-5.5` and effort `xhigh`; mismatches make review unavailable/failed (`worker-prompt.md:14`, `worker-prompt.md:25`, `worker-prompt.md:112`). Coordinator must require user confirmation before worker dispatch and write manifest before dispatch (`commands/explore-idea.md:25`, `commands/explore-idea.md:127`).
- dependencies_and_callers: `scripts/validate-explore-idea-io.sh` locates and hard-requires all three templates, emitting their paths as `WORKER_PROMPT_TEMPLATE`, `REPORT_TEMPLATE`, and `FINAL_IDEA_TEMPLATE` (`scripts/validate-explore-idea-io.sh:249`, `scripts/validate-explore-idea-io.sh:253`, `scripts/validate-explore-idea-io.sh:405`, `scripts/validate-explore-idea-io.sh:474`). `commands/explore-idea.md` reads and substitutes these templates during prompt/report/final-idea synthesis (`commands/explore-idea.md:141`, `commands/explore-idea.md:267`, `commands/explore-idea.md:337`). README and docs point users from `final-idea.md` into `gen-plan` and RLCR.
- edge_cases_or_failure_modes: Worker result parsing fails closed to coordinator-generated `no_summary` rows if sentinels are absent or JSON is invalid (`commands/explore-idea.md:232`). Worker startup failure records a structured failure row, not a dropped direction (`commands/explore-idea.md:219`). If all workers fail, `.failed` is written and failure artifacts are still generated (`commands/explore-idea.md:252`, `commands/explore-idea.md:368`). Template injection risk is handled by treating direction data as untrusted and inserting it inside data sections (`worker-prompt.md:28`, `commands/explore-idea.md:142`).
- validation_or_tests: `tests/test-explore-manifest.sh` asserts template existence, manifest fields, run directory shape, report tiers, final-idea sections, and worker-results fields (`tests/test-explore-manifest.sh:25`, `tests/test-explore-manifest.sh:48`, `tests/test-explore-manifest.sh:183`). `tests/test-worker-result-contract.sh` asserts worker sentinel order, required placeholders, JSON fields, hard-constraint placement, no-push wording, `CLAUDE_PROJECT_DIR` scoping, Codex model placeholder, and branch format (`tests/test-worker-result-contract.sh:40`, `tests/test-worker-result-contract.sh:67`, `tests/test-worker-result-contract.sh:97`, `tests/test-worker-result-contract.sh:143`). `tests/test-explore-command-structure.sh` verifies command/template alignment, placeholder coverage, adoption flow, and validation exit-code coverage (`tests/test-explore-command-structure.sh:212`, `tests/test-explore-command-structure.sh:260`, `tests/test-explore-command-structure.sh:293`, `tests/test-explore-command-structure.sh:350`).
- skip_candidate: `no`

### DEV-HZ-047 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: `UserPromptSubmit` hook guard for active RLCR loops. It blocks unsafe user prompts when loop state, branch, or plan-file tracking invariants have drifted before the next assistant action proceeds.
- algorithmic_behavior: The hook reads JSON stdin, extracts `session_id`, resolves the project root, finds the active loop filtered by session, resolves the active state file, strictly parses it, then validates schema-critical fields, branch consistency, and plan-file tracking/cleanliness (`hooks/loop-plan-file-validator.sh:27`, `hooks/loop-plan-file-validator.sh:31`, `hooks/loop-plan-file-validator.sh:35`, `hooks/loop-plan-file-validator.sh:43`, `hooks/loop-plan-file-validator.sh:46`). If no active loop is found, it allows silently (`hooks/loop-plan-file-validator.sh:37`).
- inputs_outputs_state: Input is hook JSON from `UserPromptSubmit`; only `session_id` is consumed directly by this file (`hooks/loop-plan-file-validator.sh:28`, `hooks/lib/loop-common.sh:253`). State input is `.humanize/rlcr/<session>/{methodology-analysis-state.md,finalize-state.md,state.md}` via `resolve_active_state_file` (`hooks/lib/loop-common.sh:268`). Outputs are either no output with exit `0` for allow, or JSON `{"decision":"block","reason":...}` with exit `0`; malformed state exits `1` after an stderr safety message (`hooks/loop-plan-file-validator.sh:47`, `hooks/loop-plan-file-validator.sh:72`, `hooks/loop-plan-file-validator.sh:100`, `hooks/loop-plan-file-validator.sh:176`).
- gates_or_invariants: Strict parser requires YAML frontmatter plus `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:533`, `hooks/lib/loop-common.sh:553`, `hooks/lib/loop-common.sh:562`). This hook additionally requires `plan_tracked` and `start_branch` (`hooks/loop-plan-file-validator.sh:80`). Branch must remain equal to `start_branch`; branch switching is blocked (`hooks/loop-plan-file-validator.sh:96`, `hooks/loop-plan-file-validator.sh:108`). If `plan_tracked=true`, the plan must still be tracked and clean (`hooks/loop-plan-file-validator.sh:124`, `hooks/loop-plan-file-validator.sh:175`, `hooks/loop-plan-file-validator.sh:185`). If `plan_tracked=false`, the plan must remain untracked/gitignored and must not become tracked (`hooks/loop-plan-file-validator.sh:194`, `hooks/loop-plan-file-validator.sh:221`).
- dependencies_and_callers: Registered in `hooks/hooks.json` as a `UserPromptSubmit` command (`hooks/hooks.json:4`, `hooks/hooks.json:9`). Depends on `hooks/lib/loop-common.sh` for project resolution, session extraction, active loop discovery, state parsing, field constants, and template loading (`hooks/loop-plan-file-validator.sh:16`). Depends on `scripts/portable-timeout.sh` for bounded git checks (`hooks/loop-plan-file-validator.sh:22`). Uses template `block/schema-outdated.md` via `load_and_render_safe` for schema errors (`hooks/loop-plan-file-validator.sh:66`).
- edge_cases_or_failure_modes: Git command timeout/failure blocks rather than allowing unknown state (`hooks/loop-plan-file-validator.sh:97`, `hooks/loop-plan-file-validator.sh:130`, `hooks/loop-plan-file-validator.sh:155`, `hooks/loop-plan-file-validator.sh:200`). `git ls-files --error-unmatch` exit `1` is treated as ordinary “not tracked,” while `124` or other unexpected codes block (`hooks/loop-plan-file-validator.sh:127`). Quoted YAML branch and plan fields are stripped by shared parsing (`hooks/lib/loop-common.sh:450`, `hooks/lib/loop-common.sh:453`). Session filtering prevents stale loops for other sessions from being revived (`hooks/lib/loop-common.sh:315`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` verifies allow on valid state, quoted `plan_file`, malformed/outdated schema blocking, missing `start_branch`, branch-change blocking, quoted `start_branch`, and hyphenated plan paths (`tests/test-plan-file-hooks.sh:145`, `tests/test-plan-file-hooks.sh:160`, `tests/test-plan-file-hooks.sh:175`, `tests/test-plan-file-hooks.sh:196`, `tests/test-plan-file-hooks.sh:220`, `tests/test-plan-file-hooks.sh:399`, `tests/test-plan-file-hooks.sh:527`). `tests/robustness/test-hook-system-robustness.sh` verifies valid plan edits pass through, non-plan files pass through, and empty JSON does not crash (`tests/robustness/test-hook-system-robustness.sh:164`, `tests/robustness/test-hook-system-robustness.sh:216`, `tests/robustness/test-hook-system-robustness.sh:231`). `tests/test-finalize-phase.sh` invokes it against finalize-phase state (`tests/test-finalize-phase.sh:1096`).
- skip_candidate: `no`

### DEV-HZ-077 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/ask-codex.sh`, the one-shot Codex consultation path used directly by `/humanize:ask-codex` and indirectly by explore workers for review. It defines CLI validation, artifact creation, metadata semantics, timeout/error behavior, concurrency uniqueness, cache fallback, and nested-hook disabling behavior.
- algorithmic_behavior: The test builds a mock git project and mock `codex` binary, runs `ask-codex.sh` under controlled environment variables, and asserts observable contract surfaces rather than calling real Codex (`tests/test-ask-codex.sh:31`, `tests/test-ask-codex.sh:41`, `tests/test-ask-codex.sh:104`, `tests/test-ask-codex.sh:135`). It exercises validation failures, successful response persistence, Codex error propagation, empty response handling, timeout handling, concurrent invocation directory uniqueness, model/effort parsing, cache-file creation, skill guidance text, and auto-probe behavior for disabling nested hooks.
- inputs_outputs_state: Inputs are CLI args (`--codex-model`, `--codex-timeout`, `--`, question text), env vars controlling mock Codex, `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and `XDG_CONFIG_HOME` (`tests/test-ask-codex.sh:107`, `tests/test-ask-codex.sh:139`). Expected project-local outputs are `.humanize/skill/<unique-id>/input.md`, `output.md`, `metadata.md`, and `.codex-disable-hooks-features`; expected cache outputs are `codex-run.cmd` and `codex-run.out` (`tests/test-ask-codex.sh:242`, `tests/test-ask-codex.sh:255`, `tests/test-ask-codex.sh:262`, `tests/test-ask-codex.sh:521`, `tests/test-ask-codex.sh:621`). The implementation writes those artifacts in `scripts/ask-codex.sh` (`scripts/ask-codex.sh:202`, `scripts/ask-codex.sh:224`, `scripts/ask-codex.sh:291`, `scripts/ask-codex.sh:420`, `scripts/ask-codex.sh:424`).
- gates_or_invariants: Empty question, unknown option, missing option value, nonnumeric timeout, invalid model chars, and invalid effort chars all exit `1` (`tests/test-ask-codex.sh:152`, `tests/test-ask-codex.sh:170`, `tests/test-ask-codex.sh:179`, `tests/test-ask-codex.sh:188`, `tests/test-ask-codex.sh:197`, `tests/test-ask-codex.sh:206`, `tests/test-ask-codex.sh:215`). `--codex-model MODEL:EFFORT` must split into model and effort, while `MODEL` alone uses default effort (`tests/test-ask-codex.sh:446`, `tests/test-ask-codex.sh:458`). Codex nonzero exit propagates, timeout exit `124` stays `124`, and empty stdout exits `1` with `status: empty_response` metadata (`tests/test-ask-codex.sh:336`, `tests/test-ask-codex.sh:356`, `tests/test-ask-codex.sh:375`).
- dependencies_and_callers: Directly targets `scripts/ask-codex.sh` and `skills/ask-codex/SKILL.md` (`tests/test-ask-codex.sh:19`). The script under test sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh`, inherits `DEFAULT_CODEX_MODEL` / `DEFAULT_CODEX_EFFORT`, resolves project root, and invokes `codex exec` with hook-disable args and `-C "$PROJECT_ROOT"` (`scripts/ask-codex.sh:29`, `scripts/ask-codex.sh:33`, `scripts/ask-codex.sh:192`, `scripts/ask-codex.sh:244`, `scripts/ask-codex.sh:285`, `scripts/ask-codex.sh:326`). `prompt-template/explore/worker-prompt.md` requires workers to call this script, not raw `codex` (`prompt-template/explore/worker-prompt.md:23`, `prompt-template/explore/worker-prompt.md:104`).
- edge_cases_or_failure_modes: The test covers older Codex CLIs that support only some hook feature names and CLIs that do not support `--disable` at all (`tests/test-ask-codex.sh:280`, `tests/test-ask-codex.sh:304`, `tests/test-ask-codex.sh:637`). It checks large noisy help output does not break feature probing (`tests/test-ask-codex.sh:599`). It exercises two rapid background calls to detect unique project/cache directories (`tests/test-ask-codex.sh:402`). It simulates unwritable home cache and expects fallback resolution to project-local cache (`tests/test-ask-codex.sh:492`). The skill guidance tests guard against unsafe bare `$ARGUMENTS` shell expansion (`tests/test-ask-codex.sh:553`).
- validation_or_tests: This file is the validation suite. It is included in `tests/run-all-tests.sh` as `test-ask-codex.sh` (`tests/run-all-tests.sh:104`). Its pass/fail accounting is delegated to `tests/test-helpers.sh` via `pass`, `fail`, and `print_test_summary`.
- skip_candidate: `no`

### DEV-HZ-107 `file` `tests/test-rlcr-sources.sh`
- cursor: `[_]`
- core_role: Executable parity and behavior spec for `viz/server/rlcr_sources.py`, the dashboard’s Python source of truth for RLCR session enumeration and live log discovery.
- algorithmic_behavior: The test compares Python path sanitization to the shell `sed` pipeline used in `scripts/humanize.sh`, seeds fake RLCR session directories, partitions them into active/historical/unknown, seeds fake cache log files, validates strict log filename matching and deterministic ordering, and checks cache directory construction under `XDG_CACHE_HOME` (`tests/test-rlcr-sources.sh:39`, `tests/test-rlcr-sources.sh:68`, `tests/test-rlcr-sources.sh:101`, `tests/test-rlcr-sources.sh:213`, `tests/test-rlcr-sources.sh:265`).
- inputs_outputs_state: Inputs are synthetic project paths, temp `.humanize/rlcr` session directories, state-marker files, cache directories, and `XDG_CACHE_HOME` (`tests/test-rlcr-sources.sh:68`, `tests/test-rlcr-sources.sh:104`, `tests/test-rlcr-sources.sh:214`, `tests/test-rlcr-sources.sh:266`). Outputs are function return values printed by inline Python: sanitized strings, session id lists by classification, log tuples in `round|tool|role` format, and constructed cache paths (`tests/test-rlcr-sources.sh:78`, `tests/test-rlcr-sources.sh:121`, `tests/test-rlcr-sources.sh:224`, `tests/test-rlcr-sources.sh:265`).
- gates_or_invariants: `sanitize_project_path()` must exactly match `sed 's/[^a-zA-Z0-9._-]/-/g' | sed 's/--*/-/g'` for paths with spaces, slashes, tildes, unicode, repeated special chars, and empty string (`tests/test-rlcr-sources.sh:39`, `tests/test-rlcr-sources.sh:68`, `tests/test-rlcr-sources.sh:88`; implementation at `viz/server/rlcr_sources.py:58`). Active session markers are exactly `state.md`, `methodology-analysis-state.md`, and `finalize-state.md`; other `*-state.md` files are historical (`tests/test-rlcr-sources.sh:158`, `viz/server/rlcr_sources.py:31`, `viz/server/rlcr_sources.py:99`). `live_log_paths()` must only accept `round-N-{codex|gemini}-{run|review}.log` and sort by `(round, tool, role)` (`tests/test-rlcr-sources.sh:213`, `viz/server/rlcr_sources.py:50`, `viz/server/rlcr_sources.py:190`).
- dependencies_and_callers: Imports `sanitize_project_path`, `enumerate_sessions`, `partition_sessions`, `live_log_paths`, and `cache_dir_for_session` from `viz/server/rlcr_sources.py` (`tests/test-rlcr-sources.sh:49`, `tests/test-rlcr-sources.sh:124`, `tests/test-rlcr-sources.sh:227`, `tests/test-rlcr-sources.sh:269`). The implementation intentionally mirrors monitor/parser semantics for active/finalize/methodology states (`viz/server/rlcr_sources.py:38`, `viz/server/rlcr_sources.py:40`).
- edge_cases_or_failure_modes: Missing RLCR directory returns `[]`, missing cache directory returns `[]`, and unreadable/missing directories are treated as empty discovery results to support dashboard polling during startup races (`tests/test-rlcr-sources.sh:196`, `tests/test-rlcr-sources.sh:248`, `viz/server/rlcr_sources.py:12`, `viz/server/rlcr_sources.py:147`, `viz/server/rlcr_sources.py:199`). Non-session files under `.humanize/rlcr` are skipped (`tests/test-rlcr-sources.sh:118`, `viz/server/rlcr_sources.py:156`). Invalid log names such as `random-file.txt` and nonnumeric rounds are ignored (`tests/test-rlcr-sources.sh:221`).
- validation_or_tests: This file is the validation suite and exits `1` if any `_fail` was recorded (`tests/test-rlcr-sources.sh:282`). It is included in `tests/run-all-tests.sh` as `test-rlcr-sources.sh` (`tests/run-all-tests.sh:119`).
- skip_candidate: `no`

### DEV-HZ-137 `file` `prompt-template/block/bitlesson-delta-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt fragment for the BitLesson Delta completion gate. It tells the user/agent that a round summary cannot pass because it lacks the required `## BitLesson Delta` section.
- algorithmic_behavior: The template supplies the rendered block reason when `scripts/bitlesson-validate-delta.sh` cannot detect a real BitLesson Delta heading outside fences/comments (`scripts/bitlesson-validate-delta.sh:181`, `scripts/bitlesson-validate-delta.sh:196`). The fragment states the minimal valid shape: heading, `Action: none|add|update`, `Lesson ID(s): <IDs or NONE>`, and `Notes: <what changed and why>` (`prompt-template/block/bitlesson-delta-missing.md:3`, `prompt-template/block/bitlesson-delta-missing.md:7`).
- inputs_outputs_state: Input is absence of a valid `## BitLesson Delta` block in the summary file. Output is a JSON block decision from `block_exit`, with this template as `reason` and a system message identifying the round (`scripts/bitlesson-validate-delta.sh:78`, `scripts/bitlesson-validate-delta.sh:197`).
- gates_or_invariants: Every RLCR round summary subject to BitLesson validation must include the `## BitLesson Delta` section. Detection ignores headings inside Markdown code fences and HTML comments through the validator’s fence/comment state machine (`scripts/bitlesson-validate-delta.sh:92`, `scripts/bitlesson-validate-delta.sh:104`, `scripts/bitlesson-validate-delta.sh:133`, `scripts/bitlesson-validate-delta.sh:153`). The minimal section format constrains the downstream parser’s accepted fields and action enum (`prompt-template/block/bitlesson-delta-missing.md:7`, `scripts/bitlesson-validate-delta.sh:202`, `scripts/bitlesson-validate-delta.sh:206`).
- dependencies_and_callers: Rendered by `scripts/bitlesson-validate-delta.sh` via `hooks/lib/template-loader.sh` and `load_and_render_safe`; fallback inline content mirrors this template if the file is unavailable (`scripts/bitlesson-validate-delta.sh:74`, `scripts/bitlesson-validate-delta.sh:181`, `scripts/bitlesson-validate-delta.sh:196`).
- edge_cases_or_failure_modes: If the template is missing, the validator uses its inline fallback, so the gate remains enforceable (`scripts/bitlesson-validate-delta.sh:181`). If a summary includes an invalid action, inconsistent IDs, missing notes, invalid ID formats, missing lesson file, or empty knowledge base with `Action: none`, later templates/gates handle those separately (`scripts/bitlesson-validate-delta.sh:206`, `scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:254`, `scripts/bitlesson-validate-delta.sh:285`, `scripts/bitlesson-validate-delta.sh:299`, `scripts/bitlesson-validate-delta.sh:322`).
- validation_or_tests: Covered through `scripts/bitlesson-validate-delta.sh` behavior and template-loader/rendering tests. Cross-reference shows the exact template call at `scripts/bitlesson-validate-delta.sh:196`; comprehensive template tests cover real block templates generally.
- skip_candidate: `no`

### DEV-HZ-167 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Blocking prompt fragment for the RLCR stop hook’s push-every-round invariant. It prevents loop exit while local commits are ahead of the remote when `push_every_round` is enabled.
- algorithmic_behavior: When the stop hook sees `PUSH_EVERY_ROUND=true`, it runs `git status -sb`, extracts an `ahead N` count, resolves the current branch, renders this template with `AHEAD_COUNT` and `CURRENT_BRANCH`, and emits a JSON block decision (`hooks/loop-codex-stop-hook.sh:762`, `hooks/loop-codex-stop-hook.sh:764`, `hooks/loop-codex-stop-hook.sh:767`, `hooks/loop-codex-stop-hook.sh:774`, `hooks/loop-codex-stop-hook.sh:778`). The template provides the required action: `git push origin {{CURRENT_BRANCH}}` (`prompt-template/block/unpushed-commits.md:7`).
- inputs_outputs_state: Inputs are ahead count from git status and current branch name; output is a block reason telling the agent/user that stop cannot proceed until commits are pushed (`prompt-template/block/unpushed-commits.md:3`, `hooks/loop-codex-stop-hook.sh:775`). It does not mutate repo state itself; it constrains the stop transition.
- gates_or_invariants: Invariant: with `--push-every-round`, stopping is not allowed when local branch has unpushed commits (`prompt-template/block/unpushed-commits.md:5`). The hook only evaluates this after the worktree cleanliness check path and only inside the non-final branch where push-every-round applies (`hooks/loop-codex-stop-hook.sh:758`, `hooks/loop-codex-stop-hook.sh:762`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` with inline fallback (`hooks/loop-codex-stop-hook.sh:769`, `hooks/loop-codex-stop-hook.sh:774`). Template rendering depends on the shared template loader initialized through loop-common infrastructure.
- edge_cases_or_failure_modes: If `git status -sb` times out/fails, the pipeline’s `|| true` makes `GIT_AHEAD` empty and this specific gate will not block on ahead count; other git/worktree gates may still block earlier. If current branch cannot be resolved, the hook uses `unknown` in the rendered reason (`hooks/loop-codex-stop-hook.sh:767`). If template loading fails, fallback text still blocks (`hooks/loop-codex-stop-hook.sh:769`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders the real template with `AHEAD_COUNT` and `CURRENT_BRANCH` and asserts expected output (`tests/test-templates-comprehensive.sh:517`). Stop-hook behavior around tracked plan integrity and schema blocking is tested in `tests/test-plan-file-hooks.sh`, while this fragment’s direct rendering contract is covered by the template test.
- skip_candidate: `no`

### DEV-HZ-197 `file` `prompt-template/idea/gen-idea-template.md`
- cursor: `[_]`
- core_role: Output template for `/humanize:gen-idea` idea synthesis. It structures a generated idea draft into one primary direction, alternatives, and synthesis notes, and pairs with a companion `.directions.json` used by explore-idea.
- algorithmic_behavior: The template defines placeholders for title, original idea, primary direction name/rationale/approach/evidence/risks, alternatives, and synthesis notes (`prompt-template/idea/gen-idea-template.md:1`, `prompt-template/idea/gen-idea-template.md:3`, `prompt-template/idea/gen-idea-template.md:7`, `prompt-template/idea/gen-idea-template.md:25`, `prompt-template/idea/gen-idea-template.md:29`). The IO validator only succeeds if this template exists, then emits `TEMPLATE_FILE` for the command to consume (`scripts/validate-gen-idea-io.sh:190`, `scripts/validate-gen-idea-io.sh:197`, `scripts/validate-gen-idea-io.sh:211`).
- inputs_outputs_state: Inputs are the original inline/file idea, requested direction count `--n`, and output path. The validator derives `OUTPUT_FILE`, `DIRECTIONS_JSON_FILE`, `SLUG`, and template path, and for inline input emits the idea body between sentinel markers (`scripts/validate-gen-idea-io.sh:69`, `scripts/validate-gen-idea-io.sh:75`, `scripts/validate-gen-idea-io.sh:143`, `scripts/validate-gen-idea-io.sh:203`, `scripts/validate-gen-idea-io.sh:214`). The rendered Markdown output is expected to be accompanied by `<output>.directions.json`, later accepted by explore validation (`scripts/validate-gen-idea-io.sh:160`, `scripts/validate-explore-idea-io.sh:217`).
- gates_or_invariants: Input must exist or be valid inline text; file inputs must be readable `.md`; `--n` must be numeric and between 2 and 10; output must end in `.md`; parent directory must exist unless default output path is used; output and companion JSON must not already exist; output directory must be writable; template file must exist (`scripts/validate-gen-idea-io.sh:104`, `scripts/validate-gen-idea-io.sh:115`, `scripts/validate-gen-idea-io.sh:75`, `scripts/validate-gen-idea-io.sh:80`, `scripts/validate-gen-idea-io.sh:152`, `scripts/validate-gen-idea-io.sh:166`, `scripts/validate-gen-idea-io.sh:172`, `scripts/validate-gen-idea-io.sh:178`, `scripts/validate-gen-idea-io.sh:184`, `scripts/validate-gen-idea-io.sh:197`).
- dependencies_and_callers: `scripts/validate-gen-idea-io.sh` locates it through `CLAUDE_PLUGIN_ROOT` or repo-relative fallback (`scripts/validate-gen-idea-io.sh:190`). `scripts/validate-explore-idea-io.sh` expects the generated draft to have a companion `.directions.json` and advises regenerating with `/humanize:gen-idea` if missing (`scripts/validate-explore-idea-io.sh:223`, `scripts/validate-explore-idea-io.sh:228`). Tests create/touch this template when validating gen-idea dual-write and validation behavior.
- edge_cases_or_failure_modes: A nonexistent whitespace-free `.md` input is treated as path-not-found, but path-like text with whitespace may be treated as inline by design (`scripts/validate-gen-idea-io.sh:90`, `scripts/validate-gen-idea-io.sh:124`). Default output creates `.humanize/ideas` if needed; user-supplied missing output dirs fail (`scripts/validate-gen-idea-io.sh:145`, `scripts/validate-gen-idea-io.sh:162`, `scripts/validate-gen-idea-io.sh:166`). Existing companion JSON blocks even if the Markdown output path does not exist, preventing split-brain draft/directions pairs (`scripts/validate-gen-idea-io.sh:178`).
- validation_or_tests: `tests/test-gen-idea-dual-write.sh` and `tests/test-validate-gen-idea-io.sh` reference/touch `prompt-template/idea/gen-idea-template.md` for validation setup. `scripts/validate-gen-idea-io.sh` is the direct executable gate for template existence and emitted `TEMPLATE_FILE`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique assigned item sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`