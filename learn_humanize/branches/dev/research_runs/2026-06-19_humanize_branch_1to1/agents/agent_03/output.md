# agent_03 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`
- inspection_note: read-only filesystem inspection only. The branch export did not expose a usable `.git` checkout for local Git verification, so source commit is taken from scheduler metadata.

## Item Evidence

### DEV-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command workflow layer for the Humanize pipeline. The directory contains six command definitions: `gen-idea.md`, `explore-idea.md`, `gen-plan.md`, `refine-plan.md`, `start-rlcr-loop.md`, and `cancel-rlcr-loop.md`.
- algorithmic_behavior: `gen-idea.md` converts loose input into a draft plus companion `.directions.json`, using read-only directed exploration and a schema validation cleanup gate (`commands/gen-idea.md:19`, `commands/gen-idea.md:48`, `commands/gen-idea.md:196`, `commands/gen-idea.md:244`). `explore-idea.md` consumes that directions file, creates a bounded parallel prototype run, records worker JSONL, and synthesizes report/final idea artifacts (`commands/explore-idea.md:41`, `commands/explore-idea.md:125`, `commands/explore-idea.md:201`, `commands/explore-idea.md:265`). `gen-plan.md` turns drafts into RLCR-ready plans through validation, Codex-first analysis, Claude/Codex convergence, decision consolidation, and optional auto-start (`commands/gen-plan.md:32`, `commands/gen-plan.md:256`, `commands/gen-plan.md:331`, `commands/gen-plan.md:709`). `refine-plan.md` processes annotated plans with comment extraction/classification, targeted research, QA ledger generation, and atomic final writes (`commands/refine-plan.md:30`, `commands/refine-plan.md:184`, `commands/refine-plan.md:276`, `commands/refine-plan.md:508`). `start-rlcr-loop.md` wraps implementation loop setup with plan compliance checks, an understanding quiz, goal tracker expectations, BitLesson workflow, and skip-implementation mode (`commands/start-rlcr-loop.md:13`, `commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:109`, `commands/start-rlcr-loop.md:139`). `cancel-rlcr-loop.md` delegates cancellation to the script and adds finalize-phase confirmation (`commands/cancel-rlcr-loop.md:11`, `commands/cancel-rlcr-loop.md:24`, `commands/cancel-rlcr-loop.md:37`).
- inputs_outputs_state: Inputs are command `$ARGUMENTS`, draft/plan paths, generated directions JSON, project config, Git state, hook state, and user confirmations. Outputs include `.humanize/ideas/*.md`, `.directions.json`, `.humanize/explore/<run>/manifest.json`, `worker-results.jsonl`, `explore-report.md`, `final-idea.md`, generated plan files, refined plan/QA files, RLCR loop directories, and loop state files such as `state.md`, `finalize-state.md`, `complete-state.md`, `cancel-state.md`.
- gates_or_invariants: Directory-wide invariants are phase ordering, no implementation during planning commands, validator-first IO handling, no partial outputs on validation failure, no remote push for explore workers, explicit confirmation before prototype dispatch, AC/task bidirectional coverage in plans, preserved plan schema during refinement, and cancellation through script-managed state.
- dependencies_and_callers: Commands call sibling runtime scripts under `scripts/`, prompt templates under `prompt-template/`, hook behavior under `hooks/`, and tests under `tests/`. The top-level flow is `gen-idea` -> optional `explore-idea` -> `gen-plan` -> `start-rlcr-loop`, with `refine-plan` revising annotated plan artifacts and `cancel-rlcr-loop` ending active RLCR state.
- edge_cases_or_failure_modes: Missing companion directions JSON, invalid schema, dirty or non-Git checkout, template absence, dispatch failure, all workers failed, malformed comment blocks, unsupported language variants, unresolved decisions, Codex unavailable, plan irrelevance, max iterations, finalize cancellation, and stale or protected loop state all have explicit stop paths.
- validation_or_tests: Covered by structure and IO tests including `tests/test-explore-command-structure.sh`, `tests/test-explore-manifest.sh`, `tests/test-validate-explore-idea-io.sh`, `tests/test-validate-gen-idea-io.sh`, `tests/test-gen-plan.sh`, `tests/test-refine-plan.sh`, `tests/test-finalize-phase.sh`, and hook robustness suites.
- skip_candidate: `no`

### DEV-HZ-033 `file` `commands/explore-idea.md`
- cursor: `[_]`
- core_role: Coordinator command for bounded parallel prototype exploration after idea generation. It is the bridge from static `.directions.json` research directions to isolated worker prototypes and canonical explore artifacts.
- algorithmic_behavior: The command enforces hard constraints before any worker launch: explicit user confirmation, no remote push, manifest before dispatch, canonical artifact names, no nested skills/slash commands, exact branch naming, targeted tests only, `CLAUDE_PROJECT_DIR="$PWD"` for worker Codex calls, exact `CODEX_REVIEW_MODEL_SPEC`, and no dropped worker results (`commands/explore-idea.md:23`). Phase 1 delegates validation to `validate-explore-idea-io.sh` and parses key-value stdout, including colon-containing values by splitting only on the first `": "` (`commands/explore-idea.md:52`, `commands/explore-idea.md:67`). Phase 2 displays a dispatch plan and stops without manifest/worktrees unless the user confirms (`commands/explore-idea.md:84`, `commands/explore-idea.md:121`). Phase 3 creates `<RUN_DIR>/dispatch-prompts`, builds escaped per-direction prompts, hashes them, and writes `manifest.json` before worker dispatch (`commands/explore-idea.md:125`, `commands/explore-idea.md:137`, `commands/explore-idea.md:161`). Phase 4 dispatches workers in batches limited by `EFFECTIVE_CONCURRENCY` using worktree isolation (`commands/explore-idea.md:201`, `commands/explore-idea.md:210`). Phase 5 extracts sentinel-wrapped worker JSON, appends one result per selected direction to `worker-results.jsonl`, generates fallback failure rows, and marks all-failed runs with `.failed` plus manifest failure reason (`commands/explore-idea.md:228`, `commands/explore-idea.md:252`). Phase 6 ranks product directions separately from implementation readiness and writes `explore-report.md` and `final-idea.md` (`commands/explore-idea.md:265`, `commands/explore-idea.md:279`, `commands/explore-idea.md:335`).
- inputs_outputs_state: Inputs are a draft `.md` with companion `.directions.json` or a direct `.directions.json`, selected direction IDs, concurrency/timeout caps, validated `BASE_BRANCH` and `BASE_COMMIT`, prompt/report/final templates, and user confirmation. Outputs are `<RUN_DIR>/manifest.json`, per-direction dispatch prompts, `worker-results.jsonl`, `.failed` if needed, `explore-report.md`, and `final-idea.md`. Manifest state starts with worker `status: pending`, then final statuses are copied from result rows (`commands/explore-idea.md:184`, `commands/explore-idea.md:259`).
- gates_or_invariants: Validation exit codes 1-9 map to stop messages before writes or dispatch (`commands/explore-idea.md:59`). Manifest write failure writes `.failed` and aborts (`commands/explore-idea.md:197`). Worker output must use `EXPLORE_RESULT_JSON_BEGIN/END`; absent or invalid JSON becomes a `no_summary` row, not a silent omission (`commands/explore-idea.md:234`, `commands/explore-idea.md:243`). All worker failure still produces failure artifacts, not success ranking (`commands/explore-idea.md:368`). The validation script enforces numeric caps, schema validation, Git checkout presence, resolved HEAD, and no dirty tracked files (`scripts/validate-explore-idea-io.sh:186`, `scripts/validate-explore-idea-io.sh:271`, `scripts/validate-explore-idea-io.sh:378`, `scripts/validate-explore-idea-io.sh:396`).
- dependencies_and_callers: Depends on `scripts/validate-explore-idea-io.sh`, `scripts/validate-directions-json.sh`, `prompt-template/explore/worker-prompt.md`, `report-template.md`, and `final-idea-template.md`. The worker template adds the base-commit assertion, `ask-codex.sh` metadata check for `gpt-5.5:xhigh`, targeted tests, commit behavior, and result schema (`prompt-template/explore/worker-prompt.md:16`, `prompt-template/explore/worker-prompt.md:71`, `prompt-template/explore/worker-prompt.md:104`, `prompt-template/explore/worker-prompt.md:132`).
- edge_cases_or_failure_modes: Missing input, unreadable file, missing companion, unsupported extension, invalid schema, cap violations, bad selectors, duplicate resolved selectors, non-Git/unborn/dirty checkout, run directory collision, missing templates, user cancellation, worker start failure, invalid worker JSON, all workers failed, and per-worker collection exception are all explicit failure paths.
- validation_or_tests: `tests/test-explore-command-structure.sh` checks allowed tools, six phases, constraints, worktree dispatch, result sentinels, and two-tier report structure (`tests/test-explore-command-structure.sh:46`, `tests/test-explore-command-structure.sh:99`, `tests/test-explore-command-structure.sh:120`, `tests/test-explore-command-structure.sh:158`, `tests/test-explore-command-structure.sh:184`). `tests/test-explore-manifest.sh` verifies manifest fields, run directory structure, `.failed`, and JSONL schema documentation (`tests/test-explore-manifest.sh:45`, `tests/test-explore-manifest.sh:100`, `tests/test-explore-manifest.sh:146`). `tests/test-validate-explore-idea-io.sh` covers validator exits and success key output (`tests/test-validate-explore-idea-io.sh:60`, `tests/test-validate-explore-idea-io.sh:285`).
- skip_candidate: `no`

### DEV-HZ-063 `file` `scripts/validate-directions-json.sh`
- cursor: `[_]`
- core_role: Runtime schema gate for the `directions.json` companion contract produced by `gen-idea` and consumed by `explore-idea`.
- algorithmic_behavior: Accepts exactly one file path, requires `jq`, and evaluates a single `jq -e` predicate against schema version 1 (`scripts/validate-directions-json.sh:22`, `scripts/validate-directions-json.sh:33`, `scripts/validate-directions-json.sh:40`). The schema requires top-level `schema_version == 1`, string `title`, `original_idea`, `synthesis_notes`, `metadata`, and `directions` (`scripts/validate-directions-json.sh:51`). Direction array size is 1 through 10, exactly one direction is primary, every `direction_id` is unique and matches `dir-[0-9]{2}-[a-z0-9-]+`, every `dir_slug` is unique and lower-safe, and `direction_id` must equal `dir-<padded source_index>-<dir_slug>` (`scripts/validate-directions-json.sh:61`, `scripts/validate-directions-json.sh:66`, `scripts/validate-directions-json.sh:70`, `scripts/validate-directions-json.sh:75`, `scripts/validate-directions-json.sh:80`). `display_order` must be sequential from 0 through K, metadata counts must match, timestamp must match `YYYYMMDD-HHmmss`, and confidence must be `high`, `medium`, or `low` (`scripts/validate-directions-json.sh:85`, `scripts/validate-directions-json.sh:89`, `scripts/validate-directions-json.sh:100`).
- inputs_outputs_state: Input is `<path/to/file.directions.json>`. Output is `VALIDATION_SUCCESS` with exit 0, or stderr error plus exit 1, 2, or 3. It is stateless and does not modify files.
- gates_or_invariants: Exit 1 covers missing argument/help/file missing, exit 2 covers missing `jq`, and exit 3 covers invalid JSON or schema mismatch (`scripts/validate-directions-json.sh:7`, `scripts/validate-directions-json.sh:28`, `scripts/validate-directions-json.sh:33`, `scripts/validate-directions-json.sh:119`). Required invariants include one primary, unique identifiers, safe branch/path slugs, integer source/display indexes, count consistency, and string-only evidence/risk arrays.
- dependencies_and_callers: Called by `commands/gen-idea.md` after writing draft and companion JSON, with cleanup on failure (`commands/gen-idea.md:244`). Called indirectly by `scripts/validate-explore-idea-io.sh` before explore dispatch (`scripts/validate-explore-idea-io.sh:248`, `scripts/validate-explore-idea-io.sh:271`). Depends only on Bash and `jq`.
- edge_cases_or_failure_modes: Invalid JSON is folded into schema failure because `jq -e` stderr is suppressed. Help exits through `usage` with code 1. Whitespace-only unsafe IDs, duplicate IDs/slugs/source indexes, display-order gaps, count mismatches, bad confidence values, missing metadata fields, and non-string array members all fail.
- validation_or_tests: `tests/test-directions-json-schema.sh` mutates a valid fixture across negative cases for missing schema, excessive directions, primary count, duplicate/empty/spaced IDs, slug format, field types, metadata mismatches, display-order gaps, missing booleans, mismatched derived IDs, and out-of-range source indexes (`tests/test-directions-json-schema.sh:46`, `tests/test-directions-json-schema.sh:59`, `tests/test-directions-json-schema.sh:66`, `tests/test-directions-json-schema.sh:78`, `tests/test-directions-json-schema.sh:92`, `tests/test-directions-json-schema.sh:274`, `tests/test-directions-json-schema.sh:288`). `tests/fixtures/directions/valid.directions.json` shows the accepted schema shape (`tests/fixtures/directions/valid.directions.json:1`).
- skip_candidate: `no`

### DEV-HZ-093 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR Finalize Phase and surrounding stop-hook state transitions.
- algorithmic_behavior: Builds an isolated test repo, sources `hooks/lib/loop-common.sh`, creates mock `codex` commands, and writes synthetic loop state with fields such as `current_round`, `max_iterations`, `codex_model`, `review_started`, `mainline_stall_count`, `last_mainline_verdict`, and `drift_status` (`tests/test-finalize-phase.sh:48`, `tests/test-finalize-phase.sh:53`, `tests/test-finalize-phase.sh:114`, `tests/test-finalize-phase.sh:208`). It verifies helper recognition of `finalize-state.md` and `finalize-summary.md`, active-loop detection for `state.md` and `finalize-state.md`, and non-active handling for `complete-state.md` (`tests/test-finalize-phase.sh:279`, `tests/test-finalize-phase.sh:301`, `tests/test-finalize-phase.sh:331`, `tests/test-finalize-phase.sh:346`, `tests/test-finalize-phase.sh:370`).
- inputs_outputs_state: Inputs are hook JSON payloads, mock Codex outputs, synthetic transcripts, and temp loop files. Outputs are pass/fail counters plus temporary state transitions. The tested production transitions include `state.md` to `finalize-state.md` after complete review, `finalize-state.md` to `complete-state.md` after finalize checks, `state.md` to `maxiter-state.md` at max iterations, preservation of `state.md` with `review_started: true` on review failures, and `stop-state.md` on drift circuit breaker (`tests/test-finalize-phase.sh:591`, `tests/test-finalize-phase.sh:609`, `tests/test-finalize-phase.sh:633`, `tests/test-finalize-phase.sh:694`, `tests/test-finalize-phase.sh:998`).
- gates_or_invariants: Finalize phase requires a `finalize-summary.md`, a clean Git state, complete todos/tasks, and no Codex invocation during finalize completion (`tests/test-finalize-phase.sh:494`, `tests/test-finalize-phase.sh:519`, `tests/test-finalize-phase.sh:541`, `tests/test-finalize-phase.sh:563`, `tests/test-finalize-phase.sh:775`). Write/Edit/Bash validators must block modification or copying/moving from `finalize-state.md`, and must block round contracts during Finalize Phase (`tests/test-finalize-phase.sh:398`, `tests/test-finalize-phase.sh:410`, `tests/test-finalize-phase.sh:422`, `tests/test-finalize-phase.sh:446`, `tests/test-finalize-phase.sh:458`, `tests/test-finalize-phase.sh:470`).
- dependencies_and_callers: Exercises `hooks/loop-codex-stop-hook.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/check-todos-from-transcript.py`, and `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: Codex review non-zero exit blocks rather than entering finalize, and preserves `review_started: true` (`tests/test-finalize-phase.sh:633`, `tests/test-finalize-phase.sh:678`). Empty Codex review stdout also blocks, writes an empty log, and preserves state (`tests/test-finalize-phase.sh:694`, `tests/test-finalize-phase.sh:730`, `tests/test-finalize-phase.sh:759`). Non-complete review feedback keeps `state.md`, increments the round, writes `round-3-review-result.md`, and includes feedback in block output (`tests/test-finalize-phase.sh:820`, `tests/test-finalize-phase.sh:866`, `tests/test-finalize-phase.sh:874`, `tests/test-finalize-phase.sh:882`). Missing `Mainline Progress Verdict` blocks without next prompt or state mutation (`tests/test-finalize-phase.sh:950`). Two stalled rounds enter drift recovery prompt/state, while third stalled/regressed round creates `stop-state.md` (`tests/test-finalize-phase.sh:895`, `tests/test-finalize-phase.sh:998`).
- validation_or_tests: The file is itself the executable spec and exits with `TESTS_FAILED` (`tests/test-finalize-phase.sh:1106`). It also validates that Bash, Read, and plan-file validators can parse `finalize-state.md`, while Read blocks finalize-phase round contracts (`tests/test-finalize-phase.sh:1047`, `tests/test-finalize-phase.sh:1080`).
- skip_candidate: `no`

### DEV-HZ-123 `file` `tests/test-validate-gen-idea-io.sh`
- cursor: `[_]`
- core_role: Executable regression suite for `scripts/validate-gen-idea-io.sh`, focused on output suffix rules, companion `.directions.json` derivation, collision detection, and inline/path classification.
- algorithmic_behavior: Sets up a temporary Git repo and plugin root with `prompt-template/idea/gen-idea-template.md`, then calls the validator inside the repo (`tests/test-validate-gen-idea-io.sh:26`, `tests/test-validate-gen-idea-io.sh:28`, `tests/test-validate-gen-idea-io.sh:32`, `tests/test-validate-gen-idea-io.sh:38`). Positive tests assert a valid `.md` output emits `DIRECTIONS_JSON_FILE` ending in `.directions.json`, including nested output directories (`tests/test-validate-gen-idea-io.sh:49`, `tests/test-validate-gen-idea-io.sh:66`).
- inputs_outputs_state: Inputs are inline idea text and `--output` paths. Outputs are validator stdout, exit codes, and pass/fail counts. The test only writes under its temporary directory.
- gates_or_invariants: No `.md` suffix and `.txt` suffix must fail with exit 6, existing companion JSON must fail with exit 8, existing draft output must fail with exit 4, and missing idea must fail with exit 1 (`tests/test-validate-gen-idea-io.sh:86`, `tests/test-validate-gen-idea-io.sh:95`, `tests/test-validate-gen-idea-io.sh:104`, `tests/test-validate-gen-idea-io.sh:116`, `tests/test-validate-gen-idea-io.sh:128`).
- dependencies_and_callers: Depends on `scripts/validate-gen-idea-io.sh` and `tests/test-helpers.sh`. The underlying validator parses `--n`, `--output`, distinguishes `.md` path inputs from inline text, constructs default `.humanize/ideas/<slug>-<timestamp>.md`, derives `${OUTPUT_FILE%.md}.directions.json`, and emits inline body sentinels (`scripts/validate-gen-idea-io.sh:32`, `scripts/validate-gen-idea-io.sh:90`, `scripts/validate-gen-idea-io.sh:143`, `scripts/validate-gen-idea-io.sh:152`, `scripts/validate-gen-idea-io.sh:160`, `scripts/validate-gen-idea-io.sh:203`).
- edge_cases_or_failure_modes: Regression coverage ensures slash-containing ideas such as `undo/redo` and `CI/CD` are treated as inline text instead of missing file paths (`tests/test-validate-gen-idea-io.sh:137`, `tests/test-validate-gen-idea-io.sh:150`). The validator limitation noted in code is that a real nonexistent path containing whitespace and not ending `.md` is treated as inline text (`scripts/validate-gen-idea-io.sh:90`).
- validation_or_tests: This file is the assigned executable test and ends through `print_test_summary` (`tests/test-validate-gen-idea-io.sh:161`).
- skip_candidate: `no`

### DEV-HZ-153 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the large-file gate. It does not implement detection itself, but defines the user-facing contract when the hook finds files over the configured line limit.
- algorithmic_behavior: Renders `{{MAX_LINES}}` and `{{LARGE_FILES}}`, explains why oversized files matter, then prescribes different remediation for code and documentation (`prompt-template/block/large-files.md:1`, `prompt-template/block/large-files.md:3`, `prompt-template/block/large-files.md:11`). Code files must be split into smaller modules below the max line count while keeping behavior strictly unchanged; docs must be split into logical sections with cross-references and navigation updates (`prompt-template/block/large-files.md:13`, `prompt-template/block/large-files.md:19`).
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`. Output is a rendered block reason. State remains unchanged except the stop hook blocks exit until the condition is fixed.
- gates_or_invariants: Invariant is per-file line count below `{{MAX_LINES}}`; remediation requires split, behavior preservation, optional `code-simplifier`, commit, and retry (`prompt-template/block/large-files.md:14`, `prompt-template/block/large-files.md:16`, `prompt-template/block/large-files.md:25`).
- dependencies_and_callers: Called via safe template rendering from the stop hook when large files are detected (`hooks/loop-codex-stop-hook.sh:590`). Depends on `hooks/lib/template-loader.sh` behavior indirectly through `load_and_render_safe`.
- edge_cases_or_failure_modes: If the template is missing, the hook has a fallback block path; this file itself has no parsing or enforcement logic. A possible UX edge is that generated advice always says to commit after splitting, so it assumes the active loop requires committed changes before exit.
- validation_or_tests: Indirectly covered by stop-hook tests that exercise blocking templates and by template-loader/comprehensive template tests; direct algorithmic behavior is prompt contract text rather than executable logic.
- skip_candidate: `no`

### DEV-HZ-183 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer appended to next-round RLCR prompts. It encodes final instructions for the next blocked iteration after Codex review feedback.
- algorithmic_behavior: Adds an anti-cheating note, asks the agent to optionally run `code-simplifier`, commit changes, and write the next work summary to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:2`, `prompt-template/claude/next-round-footer.md:4`, `prompt-template/claude/next-round-footer.md:6`).
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, supplied by the stop hook. Output is appended text in the generated next-round prompt. It does not mutate state directly, but points the worker toward the summary file required for the next stop attempt.
- gates_or_invariants: The footer reinforces no lying, no state-file edits, no cancellation to escape the loop, and summary-writing before next exit. Those are behavioral gates enforced elsewhere by validators and the stop hook.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` computes `NEXT_SUMMARY_FILE`, renders this template, and appends it to `NEXT_PROMPT_FILE` (`hooks/loop-codex-stop-hook.sh:2023`, `hooks/loop-codex-stop-hook.sh:2186`). If rendering fails, the hook has a fallback footer.
- edge_cases_or_failure_modes: Template-only surface; if `NEXT_SUMMARY_FILE` is wrong or stale, the footer will direct summary writing to the wrong file. The hook owns that variable, not the template.
- validation_or_tests: Indirectly covered by stop-hook prompt generation and template reference tests. No standalone executable test for this nine-line footer was found in assigned paths.
- skip_candidate: `no`

### DEV-HZ-213 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for concurrent and edge-case access to RLCR loop state helpers.
- algorithmic_behavior: Creates synthetic `state.md` files, sources `hooks/lib/loop-common.sh`, and tests parsing, active-loop detection, concurrent reads, atomic write behavior, strict validation, stale loop avoidance, terminal-state exclusion, and permission handling (`tests/robustness/test-concurrent-state-robustness.sh:1`, `tests/robustness/test-concurrent-state-robustness.sh:30`, `tests/robustness/test-concurrent-state-robustness.sh:71`).
- inputs_outputs_state: Inputs are temporary directories, generated state files, and background writer processes. Outputs are pass/fail summary and temporary files only. It does not alter repository state.
- gates_or_invariants: `get_current_round` must parse normal trailing whitespace and comments, degrade safely on leading whitespace, CRLF, empty values, and permission failures, and handle Unicode, zero, and very large round values (`tests/robustness/test-concurrent-state-robustness.sh:59`, `tests/robustness/test-concurrent-state-robustness.sh:80`, `tests/robustness/test-concurrent-state-robustness.sh:100`, `tests/robustness/test-concurrent-state-robustness.sh:123`, `tests/robustness/test-concurrent-state-robustness.sh:373`, `tests/robustness/test-concurrent-state-robustness.sh:439`). `find_active_loop` should only consider the newest timestamped loop directory and return active only for `state.md` or `finalize-state.md`, not old stale states or terminal `cancel-state.md` (`tests/robustness/test-concurrent-state-robustness.sh:170`, `tests/robustness/test-concurrent-state-robustness.sh:186`, `tests/robustness/test-concurrent-state-robustness.sh:203`, `tests/robustness/test-concurrent-state-robustness.sh:397`, `tests/robustness/test-concurrent-state-robustness.sh:413`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` and `hooks/lib/loop-common.sh`. The production helpers are used by hook validators and the stop hook, including `find_active_loop`, `get_current_round`, `parse_state_file`, and `parse_state_file_strict` (`hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:430`, `hooks/lib/loop-common.sh:494`, `hooks/lib/loop-common.sh:533`).
- edge_cases_or_failure_modes: Ten concurrent reads must all recover the actual round value, not the default (`tests/robustness/test-concurrent-state-robustness.sh:250`). Reads during simulated atomic writes via temp file plus `mv` must all return numeric values (`tests/robustness/test-concurrent-state-robustness.sh:279`). `parse_state_file_strict` must reject malformed YAML and missing frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:332`, `tests/robustness/test-concurrent-state-robustness.sh:353`). Permission handling is skipped when running as root (`tests/robustness/test-concurrent-state-robustness.sh:474`).
- validation_or_tests: This file is the assigned executable robustness test and exits through `print_test_summary` (`tests/robustness/test-concurrent-state-robustness.sh:503`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item evidence sections present for the 8 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`