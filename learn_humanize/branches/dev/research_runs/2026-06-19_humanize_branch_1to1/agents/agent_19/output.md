# agent_19 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-019 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-output template directory for the planning/refinement algorithm. Recursive inspection found exactly two files: `prompt-template/plan/gen-plan-template.md` and `prompt-template/plan/refine-plan-qa-template.md`.
- algorithmic_behavior: `gen-plan-template.md` defines the canonical generated-plan schema: goal, TDD-style acceptance criteria, path boundaries, allowed choices, feasibility hints, dependency sequence, task table, future work, deliberation/convergence, pending decisions, and implementation notes. Key gates include AC completion scope at `prompt-template/plan/gen-plan-template.md:6`, exact routing tags at `prompt-template/plan/gen-plan-template.md:70`, future-work separation at `prompt-template/plan/gen-plan-template.md:83`, and convergence/pending-decision fields at `prompt-template/plan/gen-plan-template.md:92`. `refine-plan-qa-template.md` defines the QA artifact schema for refine-plan comment handling, including ledger, answers, research findings, applied changes, remaining decisions, and metadata at `prompt-template/plan/refine-plan-qa-template.md:1`.
- inputs_outputs_state: Inputs are draft plans, comments, AC/task/DEC/FUT identifiers, routing decisions, and merged config such as `alternative_plan_language`. Outputs are the main plan file, optional translated variant, and the refine-plan QA document. The directory does not mutate state directly; it constrains downstream state by requiring AC/task cross-references, convergence status, pending decisions, and routing tags.
- gates_or_invariants: Every `AC-*` must be covered by at least one task, every task must target at least one current AC, and task tags must be exactly one of `coding` or `analyze` per `prompt-template/plan/gen-plan-template.md:72`. Future/deferred work must not appear under acceptance criteria per `prompt-template/plan/gen-plan-template.md:83`. Translated variants preserve identifiers and paths per `prompt-template/plan/gen-plan-template.md:122`.
- dependencies_and_callers: `scripts/validate-gen-plan-io.sh` locates `prompt-template/plan/gen-plan-template.md` through `CLAUDE_PLUGIN_ROOT` or script-relative fallback at `scripts/validate-gen-plan-io.sh:169`. `tests/test-gen-plan.sh` verifies the embedded `commands/gen-plan.md` Plan Structure block exactly matches `gen-plan-template.md` at `tests/test-gen-plan.sh:948`. `commands/refine-plan.md` requires reading and fully populating `refine-plan-qa-template.md` at `commands/refine-plan.md:457`.
- edge_cases_or_failure_modes: Deterministic drafts may collapse upper/lower bounds and narrow allowed choices, explicitly called out at `prompt-template/plan/gen-plan-template.md:45`. Unsupported, empty, absent, or English `alternative_plan_language` suppresses translated variant generation at `prompt-template/plan/gen-plan-template.md:132`. Stale divergence between command-embedded plan structure and the file is caught by `tests/test-gen-plan.sh:950`.
- validation_or_tests: `tests/test-gen-plan.sh` covers gen-plan template embedding and validation-script template lookup. `tests/test-refine-plan.sh` registers `REFINE_PLAN_QA_TEMPLATE` at `tests/test-refine-plan.sh:25` and validates refine-plan command structure, validator behavior, and QA template coverage according to its header at `tests/test-refine-plan.sh:3`.
- skip_candidate: `no`

### DEV-HZ-049 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: Claude `PreToolUse` Read validator for RLCR loop artifacts. It prevents reads of stale, wrong-location, wrong-round, or phase-inappropriate loop files.
- algorithmic_behavior: The hook reads JSON from stdin, validates JSON structure and nesting, exits early for non-`Read` tools, requires `tool_input.file_path`, lowercases the path, and extracts `session_id` at `hooks/loop-read-validator.sh:23`. It blocks non-allowlisted `round-*-todos.md` reads at `hooks/loop-read-validator.sh:56`. It enforces methodology-analysis read isolation before general round-file checks at `hooks/loop-read-validator.sh:76`. It then identifies goal tracker and round summary/prompt/contract reads, resolves the active loop and current round, blocks finalize-phase contract reads, validates goal-tracker location, validates round number, and canonicalizes the expected directory path at `hooks/loop-read-validator.sh:175`.
- inputs_outputs_state: Inputs are hook JSON with `tool_name`, `tool_input.file_path`, and optional `session_id`. Outputs are exit `0` for allow, exit `1` for malformed hook/state input, and exit `2` with a block message on stderr for policy violations. State inputs are `.humanize/rlcr/<loop>/state.md`, `finalize-state.md`, or `methodology-analysis-state.md`, resolved through `resolve_active_state_file`.
- gates_or_invariants: Non-`Read` tools are outside this validator at `hooks/loop-read-validator.sh:37`. Methodology-analysis phase allows only `methodology-analysis-report.md`, `methodology-analysis-done.md`, and `methodology-analysis-state.md` inside the loop and blocks project files at `hooks/loop-read-validator.sh:130`. Finalize phase blocks historical contract reads at `hooks/loop-read-validator.sh:218`. Current-round files must match `STATE_CURRENT_ROUND` unless allowlisted at `hooks/loop-read-validator.sh:286`. The final path check uses `canonicalize_path_prefix` to avoid symlink leaf bypasses at `hooks/loop-read-validator.sh:307`.
- dependencies_and_callers: It sources `hooks/lib/loop-common.sh` at `hooks/loop-read-validator.sh:17`. Important dependencies include `validate_hook_input` at `hooks/lib/loop-common.sh:91`, `is_deeply_nested` at `hooks/lib/loop-common.sh:151`, `find_active_loop` at `hooks/lib/loop-common.sh:332`, `parse_state_file_strict` at `hooks/lib/loop-common.sh:533`, `is_round_file_type` at `hooks/lib/loop-common.sh:797`, `is_allowlisted_file` at `hooks/lib/loop-common.sh:819`, and `is_in_humanize_loop_dir` at `hooks/lib/loop-common.sh:1258`. Hook installation references it in `hooks/hooks.json:38`.
- edge_cases_or_failure_modes: Null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, missing `file_path`, and deeply nested JSON fail before policy checks via shared validation. Methodology-analysis canonicalization fails closed on unresolved traversal segments or symlinks at `hooks/loop-read-validator.sh:99`. With no active loop, most round-file checks allow the read at `hooks/loop-read-validator.sh:200`, but todos reads block unless the active loop and allowlist both match at `hooks/loop-read-validator.sh:60`. Malformed state files block for safety at `hooks/loop-read-validator.sh:211`.
- validation_or_tests: `tests/test-allowlist-validators.sh` directly exercises read allowlist behavior for todos and summaries, including allowed and blocked cases at `tests/test-allowlist-validators.sh:294`. `tests/test-finalize-phase.sh` covers finalize-state detection and finalize-phase contract blocking, including read-validator calls near `tests/test-finalize-phase.sh:1070`. `tests/robustness/test-hook-input-robustness.sh` covers malformed hook input paths and nesting. `tests/test-template-references.sh` includes this hook in template-reference validation at `tests/test-template-references.sh:57`.
- skip_candidate: `no`

### DEV-HZ-079 `file` `tests/test-bitlesson-select-routing.sh`
- cursor: `[_]`
- core_role: Executable specification for the BitLesson selector’s provider-routing algorithm, dependency fallback behavior, prompt transport, and stable output contract.
- algorithmic_behavior: The test creates temporary BitLesson fixtures and mock `codex`/`claude` binaries, then invokes `scripts/bitlesson-select.sh` with `--task`, `--paths`, and `--bitlesson-file`. It verifies `gpt-*` routes to Codex at `tests/test-bitlesson-select-routing.sh:137`, Codex receives the prompt through stdin with trailing `-` at `tests/test-bitlesson-select-routing.sh:165`, `haiku`/`sonnet`/case-insensitive `OPUS` route to Claude at `tests/test-bitlesson-select-routing.sh:200`, unknown models fail at `tests/test-bitlesson-select-routing.sh:287`, missing Codex fails for Codex models at `tests/test-bitlesson-select-routing.sh:313`, missing Claude falls back to Codex at `tests/test-bitlesson-select-routing.sh:349`, `codex-only` forces Codex routing at `tests/test-bitlesson-select-routing.sh:390`, placeholder BitLesson files short-circuit to `NONE` at `tests/test-bitlesson-select-routing.sh:416`, and Codex helper invocation disables hooks and avoids `--full-auto` at `tests/test-bitlesson-select-routing.sh:440`.
- inputs_outputs_state: Inputs are synthetic `.humanize/config.json`, BitLesson files with or without `## Lesson:` entries, mock PATH contents, task strings, and related paths. Expected outputs are exact selector lines containing `LESSON_IDS:` and `RATIONALE:` or nonzero errors with informative stderr. No persistent repo state is changed; each case uses isolated temp directories via `setup_test_dir`.
- gates_or_invariants: Codex branch must pass the selector prompt on stdin and use a trailing `-`. Provider routing must honor model family and `provider_mode`. Placeholder BitLesson files must not invoke external models. Codex helper mode must be read-only, skip git repo checks where supported, disable available hook feature names, use ephemeral mode where supported, and omit `--full-auto`.
- dependencies_and_callers: The test targets `scripts/bitlesson-select.sh` at `tests/test-bitlesson-select-routing.sh:8` and sources `tests/test-helpers.sh` at `tests/test-bitlesson-select-routing.sh:7`. The implementation under test loads config/model routing and loop defaults, validates args and BitLesson file content, detects provider, applies fallback, builds a stable selector prompt, runs Codex or Claude, and normalizes model output at `scripts/bitlesson-select.sh:9`, `scripts/bitlesson-select.sh:64`, `scripts/bitlesson-select.sh:113`, `scripts/bitlesson-select.sh:125`, `scripts/bitlesson-select.sh:147`, `scripts/bitlesson-select.sh:187`, and `scripts/bitlesson-select.sh:252`.
- edge_cases_or_failure_modes: Missing `codex` under a Codex-selected model is a hard failure; missing `claude` under a Claude-selected model falls back to Codex. Unknown model names must fail instead of guessing. The test protects against a prior `echo "$help" | grep -q` pipefail/SIGPIPE hazard by asserting those probes are absent at `tests/test-bitlesson-select-routing.sh:499`.
- validation_or_tests: This file is itself the validation harness and is included in `tests/run-all-tests.sh:106`. It uses mocks that assert nonempty stdin and stable output format, so failures expose routing, prompt transport, binary dependency, or CLI argument regressions.
- skip_candidate: `no`

### DEV-HZ-109 `file` `tests/test-skill-monitor.sh`
- cursor: `[_]`
- core_role: Executable specification for `_humanize_monitor_skill --once`, the read-only monitor over `.humanize/skill` invocation records for ask-codex/ask-gemini style skill calls.
- algorithmic_behavior: The test builds a temporary git repo, sources `scripts/humanize.sh`, creates timestamp-named `.humanize/skill/<id>` directories, and checks monitor output. It covers missing skill directory at `tests/test-skill-monitor.sh:106`, empty directory at `tests/test-skill-monitor.sh:120`, a single completed invocation at `tests/test-skill-monitor.sh:135`, multiple mixed statuses at `tests/test-skill-monitor.sh:194`, running invocation detection via absent `metadata.md` at `tests/test-skill-monitor.sh:245`, recent invocation listing at `tests/test-skill-monitor.sh:269`, first-line question extraction at `tests/test-skill-monitor.sh:295`, empty-response behavior at `tests/test-skill-monitor.sh:348`, and ignoring non-timestamp directories at `tests/test-skill-monitor.sh:371`.
- inputs_outputs_state: Inputs are local `.humanize/skill` directory trees containing `input.md`, optional `metadata.md`, and optional `output.md`. Outputs are process exit code plus a textual `--once` summary containing counts, focused invocation metadata, watched output, and recent invocation rows. State is derived from directory names, metadata status fields, output/cache file presence, and question text under `## Question`.
- gates_or_invariants: Valid invocation directories must start with a timestamp pattern. A missing monitor directory or no matching invocations is an error. Invocations without `metadata.md` are running. The focused invocation is the newest with watchable content, falling back to newest if no content exists. Multi-line questions display only the first nonempty line after `## Question`.
- dependencies_and_callers: The test depends on `scripts/humanize.sh` sourcing monitor code at `tests/test-skill-monitor.sh:52`. The implementation lives in `scripts/lib/monitor-skill.sh`, where `_humanize_monitor_skill` parses `--once` and optional `--tool-filter` at `scripts/lib/monitor-skill.sh:18`, lists timestamp directories at `scripts/lib/monitor-skill.sh:77`, counts statuses at `scripts/lib/monitor-skill.sh:119`, extracts questions at `scripts/lib/monitor-skill.sh:145`, finds cache/output files at `scripts/lib/monitor-skill.sh:165`, and prints `--once` output at `scripts/lib/monitor-skill.sh:339`.
- edge_cases_or_failure_modes: Missing `.humanize/skill` returns nonzero with guidance. Empty directories return nonzero. `empty_response` counts separately from errors and prints no-output messaging. Non-matching directories are ignored even if they contain `input.md`. Running invocations can have no output and still be counted. Interactive terminal behavior is explicitly out of scope for this test, per `tests/test-skill-monitor.sh:5`.
- validation_or_tests: The test maintains counters and exits `1` if any case fails at `tests/test-skill-monitor.sh:395`. It validates the monitor’s public text contract rather than internal helper functions alone, so it acts as a regression test for user-facing monitoring output.
- skip_candidate: `no`

### DEV-HZ-139 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop hook when the Codex review phase fails to produce a review result.
- algorithmic_behavior: The template renders a markdown failure notice with review exit code, missing review-result file, debug file paths, last stderr excerpt, and an instruction that the system will retry review on the next exit attempt at `prompt-template/block/codex-review-failed.md:1`.
- inputs_outputs_state: Intended inputs are `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT`. Output is a rendered markdown reason string used inside a JSON block response from the stop hook. It does not mutate state directly; it communicates a blocked transition out of the loop.
- gates_or_invariants: Review output is mandatory. The user is told the review result file was not created and to inspect debug artifacts before retrying. This supports the invariant that RLCR exit cannot skip code review when review generation fails.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` loads this template through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:1663`, then emits a JSON decision of `"block"` with system message `"Loop: Blocked - Codex review failed, retry required"` at `hooks/loop-codex-stop-hook.sh:1674`. Template rendering is single-pass and leaves missing variables unchanged per `hooks/lib/template-loader.sh:7` and `hooks/lib/template-loader.sh:115`.
- edge_cases_or_failure_modes: There is a concrete placeholder mismatch risk. The call site passes `EXIT_CODE`, `CODEX_LOG_FILE`, and no `CODEX_STDOUT_FILE` or `CODEX_STDERR_FILE` at `hooks/loop-codex-stop-hook.sh:1665`, while the template expects `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE` at `prompt-template/block/codex-review-failed.md:5`. Because missing variables remain literal, rendered block messages may expose unresolved placeholders.
- validation_or_tests: `tests/test-template-references.sh` scans hook scripts for template references and existence at `tests/test-template-references.sh:56`; it appears to validate presence rather than placeholder-name compatibility. No direct placeholder-contract test was found in the inspected references.
- skip_candidate: `no`

### DEV-HZ-169 `file` `prompt-template/block/wrong-contract-location.md`
- cursor: `[_]`
- core_role: Block template for write-time enforcement that round contract files are written only to the active RLCR loop directory.
- algorithmic_behavior: The template emits a short markdown error titled “Wrong Round Contract Location,” states that round contract files must be in the active loop directory, and renders the correct path through `{{CORRECT_PATH}}` at `prompt-template/block/wrong-contract-location.md:1`.
- inputs_outputs_state: Input is `CORRECT_PATH`, calculated as the active loop’s current `round-${CURRENT_ROUND}-contract.md`. Output is a rendered stderr block message from the write validator, followed by exit `2`. It does not hold state, but it reports the correct state-derived target.
- gates_or_invariants: Summary and contract files outside `.humanize/rlcr` are blocked by `hooks/loop-write-validator.sh:283`; contract files use this template specifically at `hooks/loop-write-validator.sh:284`. The invariant is that the active-loop directory owns current round contracts, preventing scattered or stale contract files.
- dependencies_and_callers: The caller is `hooks/loop-write-validator.sh`, which computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-contract.md"` and invokes `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-contract-location.md"` at `hooks/loop-write-validator.sh:285`. Rendering depends on the shared template loader and fallback behavior.
- edge_cases_or_failure_modes: The template is intentionally minimal and only needs `CORRECT_PATH`, which the caller supplies. If the template is missing, the fallback includes a more imperative line, “Write the round contract to the correct path,” at `hooks/loop-write-validator.sh:286`. If active loop or current round detection is wrong upstream, this message will faithfully point at the wrong computed path.
- validation_or_tests: `tests/test-template-references.sh` includes `loop-write-validator.sh` in template reference scanning at `tests/test-template-references.sh:57` and checks that referenced templates exist. Finalize-phase tests also cover contract blocking behavior for write/edit validators around `tests/test-finalize-phase.sh:417`.
- skip_candidate: `no`

### DEV-HZ-199 `file` `prompt-template/plan/refine-plan-qa-template.md`
- cursor: `[_]`
- core_role: Canonical QA document template for `/humanize:refine-plan`, recording how comments were extracted, classified, answered, researched, applied, deferred, or left as decisions.
- algorithmic_behavior: The template structures the QA artifact into required sections: summary at `prompt-template/plan/refine-plan-qa-template.md:3`, comment ledger at `prompt-template/plan/refine-plan-qa-template.md:7`, answers at `prompt-template/plan/refine-plan-qa-template.md:19`, research findings at `prompt-template/plan/refine-plan-qa-template.md:38`, plan changes at `prompt-template/plan/refine-plan-qa-template.md:60`, remaining decisions at `prompt-template/plan/refine-plan-qa-template.md:86`, and refinement metadata at `prompt-template/plan/refine-plan-qa-template.md:108`.
- inputs_outputs_state: Inputs are raw `CMT-N` comments, classifications such as question/change_request/research_request, original text excerpts, plan locations, research scope, changed plan sections, cross-reference updates, unresolved decisions, input/output/QA file paths, counts, convergence status, and refinement date. Output is a populated QA markdown file paired with the refined plan.
- gates_or_invariants: The refine-plan command requires validation before QA generation: required sections, no remaining comment markers, valid `AC-*` references, valid task dependencies, exactly one routing tag per task, and agreement between pending decisions and convergence status at `commands/refine-plan.md:442`. Phase 6 then requires reading and completely populating this template; the QA document is not optional at `commands/refine-plan.md:457`.
- dependencies_and_callers: `commands/refine-plan.md` is the primary caller and enumerates all seven required template sections at `commands/refine-plan.md:461`. `tests/test-refine-plan.sh` defines `REFINE_PLAN_QA_TEMPLATE="$PROMPT_TEMPLATE_DIR/refine-plan-qa-template.md"` at `tests/test-refine-plan.sh:26`, tying the file into refine-plan command validation.
- edge_cases_or_failure_modes: If comments remain unresolved, they must be represented under remaining decisions with `Status: PENDING`; if a research request is deferred, the QA must explain why in impact-on-plan fields. The template itself cannot enforce completeness, so failures appear downstream as unpopulated placeholders, missing ledger rows, stale cross-references, or mismatch between convergence and pending-decision state.
- validation_or_tests: `tests/test-refine-plan.sh` covers refine-plan command structure, validator behavior, QA template coverage, language variant and atomic write requirements, and installation wiring according to `tests/test-refine-plan.sh:3`. The command-level gate at `commands/refine-plan.md:473` requires the ledger to contain exactly one row per raw extracted comment in document order.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7/7 item evidence sections present in assignment order`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`