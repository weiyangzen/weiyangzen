# agent_16 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-016 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex review prompt template pack for the RLCR review state machine. The directory has five files and no deeper descendants: `regular-review.md`, `full-alignment-review.md`, `code-review-phase.md`, `goal-tracker-update-section.md`, and `commit-history-section.md`.
- algorithmic_behavior: `regular-review.md` drives per-round implementation review: read original plan first, treat current-scope ACs/tasks as completion gates, ignore `Future Work`/`Out of Scope` as blockers, classify findings into Mainline/Blocking/Queued lanes, include `Mainline Progress Verdict`, and only end with `COMPLETE` when all current-scope work is fully done (`prompt-template/codex/regular-review.md:3`, `:10`, `:53`, `:60`, `:76`). `full-alignment-review.md` adds periodic audit behavior: AC-by-AC table, forgotten-item detection, deferral audit, drift audit, stagnation/circuit-breaker check, and `STOP` vs `COMPLETE` terminal output (`prompt-template/codex/full-alignment-review.md:24`, `:35`, `:41`, `:55`, `:80`, `:105`, `:114`). `code-review-phase.md` is an audit template for `codex review --base`, explicitly noting that prompt input must not be passed when `--base` is used (`prompt-template/codex/code-review-phase.md:3`, `:14`, `:30`). `goal-tracker-update-section.md` delegates mutable tracker correction responsibility to the reviewer while forbidding immutable-section edits (`prompt-template/codex/goal-tracker-update-section.md:1`, `:6`, `:10`). `commit-history-section.md` injects accumulated commits and recent round files as required review context (`prompt-template/codex/commit-history-section.md:1`, `:8`).
- inputs_outputs_state: Inputs are template variables such as `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `COMMIT_HISTORY_SECTION`, `GOAL_TRACKER_UPDATE_SECTION`, previous-round indices, and `REVIEW_RESULT_FILE`. Outputs are rendered `round-N-review-prompt.md` audit/review prompts and audit-only `round-N-review-prompt.md` for the review phase; the prompts in turn require Codex to write `round-N-review-result.md`.
- gates_or_invariants: Mandatory `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` appears in both review prompts and omission is explicitly blocked by the stop hook (`prompt-template/codex/regular-review.md:60`, `prompt-template/codex/full-alignment-review.md:64`, `hooks/loop-codex-stop-hook.sh:1489`). `COMPLETE` is allowed only as the last line when all current-scope ACs/tasks are met with no current-scope deferrals (`prompt-template/codex/regular-review.md:76`, `prompt-template/codex/full-alignment-review.md:115`). Full alignment can terminally emit `STOP` on stagnation (`prompt-template/codex/full-alignment-review.md:105`). Tracker updates must never modify immutable Ultimate Goal / ACs (`prompt-template/codex/goal-tracker-update-section.md:10`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` renders the shared goal-tracker section (`hooks/loop-codex-stop-hook.sh:1016`), commit-history section (`:1042`), chooses full alignment every configured interval (`:1022`), renders either `full-alignment-review.md` or `regular-review.md` (`:1100`, `:1117`), and renders `code-review-phase.md` before running `codex review` (`:1249`, `:1264`). It runs `codex review --base` via `run_with_timeout` with no prompt stdin (`hooks/loop-codex-stop-hook.sh:1289`).
- edge_cases_or_failure_modes: Invalid `full_review_round` is reset to 5 before selecting full alignment (`hooks/loop-codex-stop-hook.sh:1024`). Previous-round references are clamped at zero (`:1037`). If `BASE_COMMIT` is unavailable or not ancestor, commit history falls back to recent branch commits and annotates the limitation (`:1042`). Missing mandatory verdict triggers a block response rather than continuing (`:1489`). If a template is missing, the stop hook has inline fallbacks (`:1017`, `:1062`, `:1074`, `:1087`, `:1250`).
- validation_or_tests: Template references are covered by `tests/test-template-references.sh` per search result. `tests/test-commit-history-section.sh` exercises commit-history rendering, including recent round file references. `tests/test-finalize-phase.sh` includes cases where missing `Mainline Progress Verdict` blocks exit and where `COMPLETE` triggers review behavior. `tests/test-gen-plan.sh` checks that the Codex review templates use semantic deferred-AC detection rather than blocking on keyword hits.
- skip_candidate: `no`

### DEV-HZ-046 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse edit gate for RLCR loop artifacts. It is a direct validator in the RLCR state machine that decides whether Claude’s `Edit` tool can modify a requested path.
- algorithmic_behavior: Reads hook JSON from stdin, ignores all tools except `Edit`, extracts `.tool_input.file_path`, lowercases it, and extracts `session_id` (`hooks/loop-edit-validator.sh:23`). It blocks non-allowlisted `round-*-todos.md` edits and all `round-*-prompt.md` edits before deeper loop checks (`:40`, `:50`). It enforces methodology-analysis write isolation before the general `.humanize/rlcr` early exit, allowing only `methodology-analysis-report.md` and `methodology-analysis-done.md` in the active loop (`:56`, `:71`, `:111`). For `.humanize/rlcr` paths it finds the session-matched active loop, parses current state strictly, blocks state/finalize/methodology state edits, blocks contract edits during finalize, protects `plan.md` backups, validates goal-tracker edits, and checks summary/contract round numbers (`:130`, `:141`, `:155`, `:166`, `:181`, `:190`, `:204`, `:247`).
- inputs_outputs_state: Input is the hook payload JSON. Required fields for guarded goal-tracker updates after round 0 are `.tool_input.old_string`, `.tool_input.new_string`, and optional `.tool_input.replace_all` (`hooks/loop-edit-validator.sh:215`). Outputs are exit codes: `0` allow, `2` policy block with rendered message to stderr, `1` malformed/unsafe input errors such as bad state file or missing required edit fields (`:26`, `:45`, `:51`, `:155`, `:217`). It does not mutate state; it previews potential goal-tracker content in memory.
- gates_or_invariants: Session-aware loop filtering must not fall back to unrelated active sessions (`hooks/loop-edit-validator.sh:65`). Methodology analysis only permits methodology artifacts and fails closed on unresolved traversal/symlink paths (`:80`, `:88`, `:92`). State files are hook-managed and blocked (`:162`). Goal tracker edits after round 0 must target exactly the active loop’s tracker and preserve the immutable section (`:204`, `:210`, `:234`). Wrong-round summary/contract edits are redirected to `round-${CURRENT_ROUND}-...` unless allowlisted (`:258`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16`), relying on `to_lower`, `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, `is_goal_tracker_path`, `canonicalize_path`, `preview_edit_result`, and `goal_tracker_mutable_update_allowed`. `loop-common.sh` defines round-file matching and extraction (`hooks/lib/loop-common.sh:797`, `:807`), allowlist entries (`:819`), todos/prompt/state messages (`:846`, `:856`, `:865`), immutable-goal-tracker comparison (`:956`), edit preview via Perl (`:973`), and `.humanize/rlcr` detection (`:1258`).
- edge_cases_or_failure_modes: Non-`Edit` tool payloads are ignored (`hooks/loop-edit-validator.sh:26`). If project root or active loop cannot be resolved, the hook exits open except where an earlier global prompt/todos rule applies (`:62`, `:143`). Malformed active state blocks with exit 1 (`:155`). Methodology mode rejects unresolved traversal segments and unresolved symlink leaf paths (`:81`, `:92`). Missing `old_string`/`new_string` for round > 0 goal-tracker edits exits 1 (`:216`). If edit preview fails, goal-tracker modification is blocked (`:229`). `plan.md` backups inside `.humanize/rlcr` are blocked separately (`:190`).
- validation_or_tests: `tests/test-allowlist-validators.sh` covers allowlisted/non-allowlisted todos access for Edit/Read/Bash; search results show Edit allows active `round-2-todos.md` and blocks `round-4-todos.md`. Goal-tracker immutable-section behavior is indirectly tied to `goal_tracker_mutable_update_allowed` in `loop-common.sh`. Methodology/finalize/round-contract behavior is covered by finalize and hook validator suites in the repo.
- skip_candidate: `no`

### DEV-HZ-076 `file` `tests/test-app-routes-live.sh`
- cursor: `[_]`
- core_role: Live executable specification for `viz/server/app.py` route behavior using Flask `test_client`, not text-pattern tests. It validates dashboard/session parsing and API behavior for RLCR loop state visibility.
- algorithmic_behavior: Bootstraps or reuses a Python venv with Flask, flask-sock, watchdog, and pyyaml; skips cleanly if Python/venv/deps are unavailable (`tests/test-app-routes-live.sh:47`, `:52`, `:59`, `:66`). Creates all fixtures under `mktemp` and sets cache/project globals in a reloaded Flask module (`:71`, `:89`, `:137`). Groups route assertions across health, sessions, projects, auth, SSE, CSRF, cancel lifecycle, parser formats, report handling, and phase classification (`:160`, `:248`, `:308`, `:342`, `:471`, `:623`, `:735`, `:1071`, `:1107`, `:1145`).
- inputs_outputs_state: Inputs are temporary `.humanize/rlcr/<session>` trees with state files such as `state.md`, `complete-state.md`, `methodology-analysis-state.md`, `finalize-state.md`, `cancel-state.md`, plus cache logs under `XDG_CACHE_HOME` (`tests/test-app-routes-live.sh:90`, `:108`, `:162`, `:344`). Outputs are PASS/FAIL counts; script exits 1 if any Python-side assertion fails (`:1207`). It mutates only temporary fixtures, including renaming active state files to terminal state for SSE/cancel tests (`:391`, `:441`, `:650`, `:666`).
- gates_or_invariants: `/api/health` is open in all modes (`tests/test-app-routes-live.sh:175`, `:254`). Remote-bound protected endpoints require bearer token, while valid token passes (`:260`, `:266`, `:272`, `:286`, `:292`). Deprecated project mutation routes return 410 (`:198`, `:204`, `:210`). SSE validates basenames and emits snapshot/eof/resync sequences (`:323`, `:334`, `:394`, `:449`). CSRF blocks cross-origin mutating requests but leaves GET open (`:482`, `:507`, `:517`). Cancel must support active/analyzing/finalizing but reject terminal cancelled state (`:641`, `:656`, `:672`). Session ids must reject traversal/non-session dirs with 404 (`:1049`). COMPLETE verdict parsing requires terminal marker line, not prose occurrence (`:1071`, `:1081`).
- dependencies_and_callers: Imports `viz/server/app.py` by placing `viz/server` on `sys.path` (`tests/test-app-routes-live.sh:84`). Uses `parser.parse_review_result` for verdict parser coverage (`:1078`). Depends on `scripts/cancel-rlcr-session.sh` through the cancel route, verified by state-file renames (`:648`, `:656`, `:680`). Exercises project-local cache behavior and `rlcr_sources` cache resolution via seeded `.cache`/`XDG_CACHE_HOME` (`:970`, `:976`).
- edge_cases_or_failure_modes: Missing dependencies cause SKIP exit 0 rather than false failure (`tests/test-app-routes-live.sh:47`, `:55`, `:59`, `:66`). SSE active sessions can hang, so terminal state is forced for deterministic eof (`:383`). Truncation recovery uses a writer thread and terminal transition to guarantee stream closure (`:426`, `:451`). Wildcard bind `0.0.0.0` compares Origin to request Host, not literal bind (`:528`). IPv6 bracket stripping is checked (`:564`). Malformed Origin ports must return 403, not 500 (`:599`). Stray `CLAUDE_PROJECT_DIR` must not redirect cancellation away from dashboard project (`:680`). Parser tests cover legacy/new/decimal/dashless/multi-id/table AC formats (`:735`, `:833`, `:872`, `:914`).
- validation_or_tests: The file is itself the validation harness. It ends with accumulated pass/fail summary and exits nonzero on any failure (`tests/test-app-routes-live.sh:1207`). It also includes source-level regression checks for methodology report prompt using latest rounds (`:1006`, `:1012`).
- skip_candidate: `no`

### DEV-HZ-106 `file` `tests/test-refine-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:refine-plan` planning-only workflow, its IO validator, QA template, installation wiring, comment parsing rules, language variant rules, and atomic write contract.
- algorithmic_behavior: Defines assertion helpers and fixture generators, then runs structural checks against `commands/refine-plan.md`, `prompt-template/plan/refine-plan-qa-template.md`, `scripts/validate-refine-plan-io.sh`, installation docs/scripts, plus real validator invocations against generated fixture files (`tests/test-refine-plan.sh:16`, `:25`, `:63`, `:156`, `:167`). It also includes a reference scanner/classifier/language normalizer to encode expected behavior independently of the command text (`:435`, `:540`, `:571`, `:593`, `:619`).
- inputs_outputs_state: Inputs are repository files plus temporary annotated plan fixtures under `mktemp` (`tests/test-refine-plan.sh:635`). Outputs are PASS/FAIL counters and exit 0/1 at summary (`:1356`). The validator subprocess output/exit code is captured by `run_validator_capture` (`:156`). Temporary fixtures include valid plans, no-comment plans, comments inside fences/HTML, empty/nested/unterminated comments, missing sections, read-only dirs, and mixed valid/ignored comments (`:167`, `:206`, `:245`, `:285`, `:301`, `:342`, `:383`).
- gates_or_invariants: Command metadata must expose exact argument hint, allow validator Bash, allow `AskUserQuestion`, and be hidden from slash-command tool (`tests/test-refine-plan.sh:680`). The command must be planning-only and sequential across phases (`:688`, `commands/refine-plan.md:19`, `:30`). Validator exit codes 0-7 must be documented and honored (`tests/test-refine-plan.sh:715`, `commands/refine-plan.md:165`). Comment extraction must be stateful, preserve inline surrounding text, ignore fenced/HTML markers, reject nested/stray/unclosed markers, and count only non-empty blocks (`tests/test-refine-plan.sh:727`, `scripts/validate-refine-plan-io.sh:16`, `:589`). Classification precedence is `research_request` > `change_request` > `question` (`tests/test-refine-plan.sh:966`, `commands/refine-plan.md:296`). Output rules require QA generated before atomic writes, same-dir temp files, cleanup, and main in-place plan updated last (`tests/test-refine-plan.sh:801`, `commands/refine-plan.md:508`).
- dependencies_and_callers: Checks `commands/refine-plan.md`, `prompt-template/plan/refine-plan-qa-template.md`, `scripts/validate-refine-plan-io.sh`, `skills/humanize-refine-plan/SKILL.md`, `scripts/install-skill.sh`, install docs, plugin/marketplace JSON, and README version (`tests/test-refine-plan.sh:25`, `:831`, `:868`). The command itself depends on `config-loader.sh` semantics for `alternative_plan_language` and `gen_plan_mode` (`commands/refine-plan.md:86`). Validator uses AWK scanners for comment and visible-section detection (`scripts/validate-refine-plan-io.sh:16`, `:318`).
- edge_cases_or_failure_modes: Invalid/missing argument values, unexpected `--alt-language`, mutually exclusive `--discussion`/`--direct`, and `--help` all exit 7 (`tests/test-refine-plan.sh:1063`). Missing input exits 1; empty input exits 2; no valid comments, HTML-only/fence-only comments, empty comments, unterminated and nested comments exit 3 (`:1113`, `:1130`, `:1139`, `:1148`, `:1157`, `:1166`, `:1187`). Missing required sections or sections only inside fence/HTML exit 4 (`:1202`, `:1211`, `:1220`). Missing/non-writable output or in-place input dir exits 5; non-directory QA path exits 6 (`:1231`, `:1241`, `:1262`, `:1287`). Valid in-place/new-file modes must report mode and create QA dir (`:1297`, `:1334`).
- validation_or_tests: The file validates both specification text and executable validator behavior. Related implementation anchors: `commands/refine-plan.md` declares phases and scanner/classification/atomic write contracts (`commands/refine-plan.md:46`, `:184`, `:276`, `:332`, `:395`, `:457`, `:508`); `scripts/validate-refine-plan-io.sh` documents exact exit codes (`scripts/validate-refine-plan-io.sh:4`) and implements scanners/arg parsing/path checks (`:16`, `:480`, `:573`); QA template contains required sections and ledger columns (`prompt-template/plan/refine-plan-qa-template.md:1`, `:7`, `:11`, `:108`).
- skip_candidate: `no`

### DEV-HZ-136 `file` `prompt-template/block/bitlesson-delta-missing-notes.md`
- cursor: `[_]`
- core_role: Block-message template for BitLesson delta validation when an `add` or `update` action omits meaningful `Notes:`.
- algorithmic_behavior: Renders a concise failure reason: `Action: {{ACTION}}` requires a `Notes:` field explaining what changed and why, and notes must not be empty or placeholder-like (`prompt-template/block/bitlesson-delta-missing-notes.md:1`, `:3`, `:5`).
- inputs_outputs_state: Input is template variable `ACTION`. Output is a rendered block reason used in a hook/validator block response. No persistent state changes are made by the template itself.
- gates_or_invariants: For BitLesson actions other than `none`, `Lesson ID(s)` and non-placeholder `Notes:` are required. The specific invariant for this template is that `Notes:` must not be empty and must not match placeholder bracket/angle text such as `[what changed and why]`.
- dependencies_and_callers: `scripts/bitlesson-validate-delta.sh` extracts `Notes:` from `BITLESSON_DELTA_BLOCK`, trims whitespace, treats bracketed/angled placeholders as invalid, loads this template with `ACTION=$BITLESSON_ACTION`, and blocks exit when invalid (`scripts/bitlesson-validate-delta.sh:281`, `:283`, `:285`, `:294`, `:296`). The same fallback wording is embedded in the script (`:286`).
- edge_cases_or_failure_modes: Missing `Notes:` line yields empty notes and blocks. Placeholder-only notes matching `^(\[.*\]|<.*>)$` block, including `[what changed and why]` (`scripts/bitlesson-validate-delta.sh:283`). Notes inside fenced blocks/HTML comments are covered by BitLesson delta tests per search result (`tests/test-bitlesson-validate-delta.sh:124`, `:138`), preventing quoted template examples from satisfying the real delta requirement.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` covers notes extraction/placeholder behavior. `tests/test-template-references.sh` verifies block template references. The template is a core gate message, not the parser itself.
- skip_candidate: `no`

### DEV-HZ-166 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template enforcing the RLCR rule that agents should not create/access `round-*-todos.md` files directly and should use native task tools instead.
- algorithmic_behavior: Renders a policy block with title, direct prohibition, required replacement tools `TaskCreate`, `TaskUpdate`, `TaskList`, and rationale that native task tools provide UI-visible state tracking (`prompt-template/block/todos-file-access.md:1`, `:3`, `:5`, `:7`).
- inputs_outputs_state: No dynamic placeholders are required in the current template. Output is a rendered stderr/block reason used by validators; no state mutation.
- gates_or_invariants: Direct access to `round-*-todos.md` is blocked unless the path is in a narrow active-loop allowlist. The invariant is that task state must flow through native task tooling rather than direct todos-file edits/reads.
- dependencies_and_callers: `hooks/lib/loop-common.sh` defines `todos_blocked_message`, with this template as the primary rendering target and an inline fallback containing the same tool guidance (`hooks/lib/loop-common.sh:844`, `:852`). `hooks/loop-edit-validator.sh` invokes it for non-allowlisted Edit operations targeting `round-*-todos.md` (`hooks/loop-edit-validator.sh:40`). Related Bash validator logic blocks `round-*-todos.md` writes unless the active-loop allowlist matches (`hooks/loop-bash-validator.sh:561`, `:567` from search result).
- edge_cases_or_failure_modes: Active-loop allowlist permits only specific historical files (`round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, `round-1-summary.md`) when canonical path matches the active loop (`hooks/lib/loop-common.sh:828`). Same filename in wrong dir, old loop, generic relative path, or later round remains blocked.
- validation_or_tests: `tests/test-allowlist-validators.sh` covers allowed and blocked Read/Write/Edit/Bash access for `round-*-todos.md`; search results show explicit tests for allowing active `round-1-todos.md`/`round-2-todos.md` and blocking `round-3`/`round-4`, wrong dirs, old loops, and generic paths. `tests/test-template-references.sh` includes `block/todos-file-access.md`.
- skip_candidate: `no`

### DEV-HZ-196 `file` `prompt-template/explore/worker-prompt.md`
- cursor: `[_]`
- core_role: Worker prompt template for `/humanize:explore-idea`; it defines the per-direction prototype worker state machine, hard isolation constraints, Codex review contract, commit behavior, and required JSON result schema.
- algorithmic_behavior: Worker must implement a scoped prototype for one idea direction, review it with Codex through `ask-codex.sh`, commit locally, and emit sentinel-wrapped JSON (`prompt-template/explore/worker-prompt.md:1`, `:3`, `:128`). It treats direction data as untrusted input (`:28`). Setup verifies worktree root, confirms HEAD equals `<BASE_COMMIT>`, creates `explore/<RUN_ID>/<DIR_SLUG>`, exports `CLAUDE_PROJECT_DIR="$PWD"`, and verifies `ask-codex.sh` resolves to this worktree (`:69`, `:71`, `:78`, `:83`, `:86`). Each iteration explores, implements, runs targeted tests, calls Codex review with timeout/model spec, checks metadata, and applies feedback until LGTM or max iterations (`:92`, `:96`, `:104`, `:112`, `:115`).
- inputs_outputs_state: Inputs are placeholder-substituted run metadata, direction text, base branch/commit, max iterations, Codex timeout, and Codex review model spec (`prompt-template/explore/worker-prompt.md:6`, `:32`). Outputs are local branch/commit when changes exist (`:117`) and JSON between `=== EXPLORE_RESULT_JSON_BEGIN/END ===` with schema fields for status, Codex metadata, tests, branch/worktree/commit, dirty state, summary, arrays, `bitlesson_action`, and `error` (`:132`, `:135`, `:156`). Worker state transitions are `success`, `partial`, or `failed`; Codex verdict transitions are `lgtm`, `partial`, `failed`, or `unavailable` (`:162`, `:167`).
- gates_or_invariants: Hard constraints forbid leaving the worktree, nested skills/slash commands, nested agents/tasks, git push, sibling worktree access, direct `codex` CLI, unscoped Codex calls, silent model/effort downgrades, and output after result sentinel (`prompt-template/explore/worker-prompt.md:16`, `:18`, `:23`, `:25`, `:26`). Codex review metadata must show model `gpt-5.5` and effort `xhigh`; otherwise the review is unavailable/failed (`:25`, `:112`). If HEAD differs from `<BASE_COMMIT>`, worker emits failure and stops (`:78`). Tests must be targeted, not full-suite, and new minimal tests are required if none exist for touched area (`:98`). Final action must be printing sentinel JSON (`:130`).
- dependencies_and_callers: `/humanize:explore-idea` reads this template and substitutes placeholders including `<CODEX_TIMEOUT_MIN>` and `<CODEX_REVIEW_MODEL_SPEC>` from validation stdout, expected `gpt-5.5:xhigh` (`commands/explore-idea.md:140`, `:153`). Coordinator records worker failure/no-summary rows with matching fields and `bitlesson_action: "none"` (`commands/explore-idea.md:221`, `:234`). `tests/test-worker-result-contract.sh` verifies required placeholders, JSON fields, `CLAUDE_PROJECT_DIR` scoping, exact `--codex-model "<CODEX_REVIEW_MODEL_SPEC>"`, and branch naming (`tests/test-worker-result-contract.sh:70`, `:98`, `:177`, `:184`, `:193`).
- edge_cases_or_failure_modes: Base commit mismatch short-circuits before implementation (`prompt-template/explore/worker-prompt.md:78`). `ask-codex.sh` metadata missing/unreadable means review cannot be trusted (`:112`). Wrong model/effort is classified failed or unavailable rather than accepted (`:113`). No changes yields `commit_status: "none"` (`:126`). Unrecoverable errors still require sentinel result with `task_status: "failed"` (`:182`). Sentinel must be last to avoid parser ambiguity (`:26`, `:130`).
- validation_or_tests: `tests/test-worker-result-contract.sh` is the direct contract test. `scripts/validate-explore-idea-io.sh` emits `CODEX_REVIEW_MODEL: gpt-5.5`, `CODEX_REVIEW_EFFORT: xhigh`, and combined spec (`scripts/validate-explore-idea-io.sh:449`). `commands/explore-idea.md` enforces prompt generation and result parsing against this schema.
- skip_candidate: `no`

### DEV-HZ-226 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for `scripts/portable-timeout.sh`, covering timeout implementation detection, exit-code semantics, argument passing, signal behavior, and fallback compatibility.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh`, then executes 18 tests over positive and edge-case timeout behavior (`tests/robustness/test-timeout-robustness.sh:14`, `:16`, `:24`, `:112`). It validates `TIMEOUT_IMPL` exists and is one of `gtimeout|timeout|python3|python|none` (`:31`, `:39`). It calls `run_with_timeout` around quick commands, timed sleeps, argument-heavy commands, shell pipelines, large-output commands, repeated cycles, nonexistent commands, empty command strings, special chars, signal-trapping commands, subshells, and exported env visibility (`:51`, `:62`, `:79`, `:89`, `:102`, `:157`, `:170`, `:188`, `:201`, `:211`, `:221`, `:249`, `:259`).
- inputs_outputs_state: Inputs are local command availability (`gtimeout`, GNU `timeout`, `python3`, `python`) and shell commands under test. Outputs are PASS/FAIL via `test-helpers.sh`; final exit is `print_test_summary` result (`tests/robustness/test-timeout-robustness.sh:278`). No repository state is modified.
- gates_or_invariants: Timed-out commands should return 124 when a real timeout implementation exists (`tests/robustness/test-timeout-robustness.sh:62`). Successful commands preserve output and original exit code (`:51`, `:89`). Very short timeout must return 124 and finish quickly (`:120`). Zero timeout may portably exit 0 or 124 only (`:139`). Special characters must pass as literal args, not be shell-expanded (`:211`). `detect_timeout_impl` must return a valid implementation (`:235`). `TIMEOUT_IMPL` must be non-empty and exported/available to subshells where supported (`:259`).
- dependencies_and_callers: `scripts/portable-timeout.sh` chooses `gtimeout` first, then GNU `timeout`, then `python3`, then `python`, then `none` (`scripts/portable-timeout.sh:6`, `:10`). `run_with_timeout` dispatches to the selected implementation, Python uses `subprocess.run(..., timeout=...)`, maps `TimeoutExpired` to 124, and no-timeout fallback warns and runs directly (`scripts/portable-timeout.sh:38`, `:43`, `:52`, `:61`, `:69`). `TIMEOUT_IMPL` is exported (`:78`). This wrapper is also used by Codex review execution in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:1289`).
- edge_cases_or_failure_modes: If `TIMEOUT_IMPL=none`, timeout-specific tests are skipped/passed as skip to support minimal systems (`tests/robustness/test-timeout-robustness.sh:65`, `:122`, `:142`). GNU `timeout` detection rejects non-GNU shims unless `--version` output looks like GNU/coreutils/timeout version (`scripts/portable-timeout.sh:15`). Empty command is accepted as “handled” regardless of exit (`tests/robustness/test-timeout-robustness.sh:201`). Signal-trapping command may exit 0 or 124 depending on backend (`:221`). Large-output test pipes wrapper output into `wc -l` to ensure output is not truncated/deadlocked (`:157`).
- validation_or_tests: The file is the validation harness. It directly validates the timeout library that protects longer-running Codex/review subprocesses from hanging the RLCR loop.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DEV-HZ-016`, `DEV-HZ-046`, `DEV-HZ-076`, `DEV-HZ-106`, `DEV-HZ-136`, `DEV-HZ-166`, `DEV-HZ-196`, `DEV-HZ-226`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`