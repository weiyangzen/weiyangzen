# agent_02 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Agent prompt specifications for preflight validation, plan comprehension, BitLesson selection, and draft relevance. Recursive inspection found exactly four files: `agents/bitlesson-selector.md`, `agents/draft-relevance-checker.md`, `agents/plan-compliance-checker.md`, and `agents/plan-understanding-quiz.md`.
- algorithmic_behavior: `bitlesson-selector` selects only directly relevant lesson IDs or `NONE` with a stable two-line output contract (`agents/bitlesson-selector.md:26-39`). `draft-relevance-checker` performs lenient repository relevance classification for draft-to-plan generation (`agents/draft-relevance-checker.md:12-36`). `plan-compliance-checker` combines repository relevance with branch-switch detection before RLCR startup (`agents/plan-compliance-checker.md:16-55`). `plan-understanding-quiz` generates exactly two 4-option implementation-comprehension questions plus a summary (`agents/plan-understanding-quiz.md:29-75`).
- inputs_outputs_state: Inputs are natural-language task, draft, plan, repository context, and BitLesson content. Outputs are strict text verdicts or structured fields: `LESSON_IDS/RATIONALE`, `RELEVANT/NOT_RELEVANT`, `PASS/FAIL_RELEVANCE/FAIL_BRANCH_SWITCH`, and 13 quiz fields. These agents do not directly mutate repo state; callers consume their outputs as gates or advisory checks.
- gates_or_invariants: Stable output formats are explicit (`agents/bitlesson-selector.md:32-39`, `agents/plan-compliance-checker.md:48-60`, `agents/plan-understanding-quiz.md:57-84`). Branch-switch plans fail RLCR preflight because the loop requires a constant branch (`agents/plan-compliance-checker.md:31-47`). Quiz answers must be `A/B/C/D` and all fields must be present (`agents/plan-understanding-quiz.md:77-84`).
- dependencies_and_callers: `commands/gen-plan.md:232-246` invokes `humanize:draft-relevance-checker`. `commands/start-rlcr-loop.md:41-56` invokes `humanize:plan-compliance-checker`; `commands/start-rlcr-loop.md:75-86` invokes `humanize:plan-understanding-quiz`; `commands/start-rlcr-loop.md:173-180` requires BitLesson selection per round. `agents/bitlesson-selector.md:19-24` documents runtime dispatch via `scripts/bitlesson-select.sh`.
- edge_cases_or_failure_modes: Relevance checks intentionally lean permissive to reduce false negatives (`agents/draft-relevance-checker.md:33-36`, `agents/plan-compliance-checker.md:58-60`). Branch-switch detection has safe-pattern exclusions like file restore and negated instructions (`agents/plan-compliance-checker.md:40-45`). Quiz generation degrades by deriving questions from sparse hints if plan detail is weak (`agents/plan-understanding-quiz.md:84`).
- validation_or_tests: `tests/test-gen-plan.sh:111-116` asserts `draft-relevance-checker.md` exists; `tests/test-gen-plan.sh:477-506` validates name, model, and tools; `tests/test-gen-plan.sh:642-648` validates required frontmatter. Additional command-level callers enforce parse behavior in their own workflow text.
- skip_candidate: `no`

### DEV-HZ-032 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling the active RLCR loop through the supported cancel script, including finalize-phase confirmation behavior.
- algorithmic_behavior: The command runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` and branches on the first output token (`commands/cancel-rlcr-loop.md:11-22`). For `FINALIZE_NEEDS_CONFIRM`, it asks the user whether to force cancel or let finalization finish (`commands/cancel-rlcr-loop.md:24-35`). It delegates all actual cancellation mechanics to the script (`commands/cancel-rlcr-loop.md:37-39`).
- inputs_outputs_state: Input is the current project’s newest loop state under `.humanize/rlcr` and an optional user confirmation. Outputs are user-facing messages derived from script tokens: `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:17-22`). State transition is performed by the script: active state file becomes `cancel-state.md`.
- gates_or_invariants: Frontmatter disables model invocation and restricts tools to the cancel script, forced cancel script, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-5`). Finalize cancellation is not automatic; the command must ask the user before invoking `--force` (`commands/cancel-rlcr-loop.md:24-33`).
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-loop.sh`, whose active state order is `state.md`, `methodology-analysis-state.md`, then `finalize-state.md` (`scripts/cancel-rlcr-loop.sh:103-121`) and whose finalize gate exits with `FINALIZE_NEEDS_CONFIRM` unless forced (`scripts/cancel-rlcr-loop.sh:139-150`). The setup docs list this command as a stop path (`commands/start-rlcr-loop.md:182-186`).
- edge_cases_or_failure_modes: No active loop reports tokenized no-loop states (`commands/cancel-rlcr-loop.md:17-18`). Finalize phase requires confirmation because normal completion would end the loop without returning to review (`commands/cancel-rlcr-loop.md:26-30`). The newest loop directory is preserved for reference, not deleted (`commands/cancel-rlcr-loop.md:37-39`).
- validation_or_tests: `tests/robustness/test-cancel-security-robustness.sh` exercises the lower-level authorization used by hook guards. `tests/test-cancel-signal-file.sh` and `tests/test-cancel-session.sh` are related coverage for cancel signal/session behavior. The script itself documents exit codes and behavior (`scripts/cancel-rlcr-loop.sh:11-16`, `scripts/cancel-rlcr-loop.sh:172-184`).
- skip_candidate: `no`

### DEV-HZ-062 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Runtime statusline utility with one core RLCR state resolver field; the rest formats generic CLI session, cost, context, branch, and fast-mode status.
- algorithmic_behavior: Reads JSON from stdin (`scripts/statusline.sh:19-24`), extracts model/cwd/session/cost/context fields, computes RLCR status from `<cwd>/.humanize/rlcr`, colors it, then greedily wraps fields to 75 percent of terminal width (`scripts/statusline.sh:257-263`, `scripts/statusline.sh:376-403`). `_resolve_rlcr_display` maps state files to display states: methodology analysis to `Analyzing`, finalize to `Finalizing`, normal state to `Active`, terminal `*-state.md` to capitalized reason, otherwise `Off` (`scripts/statusline.sh:36-63`).
- inputs_outputs_state: Inputs are Claude statusline JSON, filesystem state under `.humanize/rlcr`, transcript/session index files, user settings, git branch, and terminal width. Output is colored statusline text. It does not mutate RLCR state.
- gates_or_invariants: If no `.humanize` dir exists, RLCR is `Off` (`scripts/statusline.sh:257-262`). Session-aware loop files take precedence: if any state file has `session_id`, session-unaware entries are ignored for session-filtered display (`scripts/statusline.sh:76-119`, `scripts/statusline.sh:121-154`). Without a session id and no session-aware files, only the newest loop directory is considered for zombie-loop protection (`scripts/statusline.sh:82-92`).
- dependencies_and_callers: Uses `jq`, `awk`, `grep`, `sed`, `stty`/`tput`, and `git` (`scripts/statusline.sh:21-24`, `scripts/statusline.sh:196-209`, `scripts/statusline.sh:237-240`, `scripts/statusline.sh:379-382`). Comments say RLCR lookup mirrors `find_active_loop()` from `hooks/lib/loop-common.sh` (`scripts/statusline.sh:65-67`).
- edge_cases_or_failure_modes: Missing RLCR dir returns `Off` (`scripts/statusline.sh:71-74`). Empty newest dir returns `Off` (`scripts/statusline.sh:86-89`). Missing transcript falls back to constructed transcript path, then `sessions-index.json`, then raw session id (`scripts/statusline.sh:184-218`). Terminal width lookup falls back to `80` (`scripts/statusline.sh:379-382`).
- validation_or_tests: No dedicated statusline test was assigned. Related behavior is session-aware loop state logic; the script is marked in comments as reference/template and only the RLCR field is repository-specific (`scripts/statusline.sh:7-14`).
- skip_candidate: `no`

### DEV-HZ-092 `file` `tests/test-explore-manifest.sh`
- cursor: `[_]`
- core_role: Executable static specification for `/humanize:explore-idea` manifest, run directory layout, worker result contract, and canonical report/final idea artifacts.
- algorithmic_behavior: Sources test helpers, locates command/templates/validator (`tests/test-explore-manifest.sh:11-18`), checks required files exist (`tests/test-explore-manifest.sh:28-42`), then greps command/template text for documented manifest fields, worker fields, directory names, JSONL fields, write-order constraint, and final synthesis sections.
- inputs_outputs_state: Inputs are repository files: `commands/explore-idea.md`, `prompt-template/explore/worker-prompt.md`, `prompt-template/explore/report-template.md`, `prompt-template/explore/final-idea-template.md`, and `scripts/validate-explore-idea-io.sh`. Output is pass/fail test summary through `test-helpers.sh`. It does not create explore runs.
- gates_or_invariants: Manifest must include coordinator fields such as `run_id`, `created_at`, `base_commit`, `concurrency`, `runtime_spike_status`, and `workers` (`tests/test-explore-manifest.sh:48-76`). Per-worker manifest entries must include `direction_id`, `prompt_hash`, branch, and status (`tests/test-explore-manifest.sh:82-97`). Manifest write-before-dispatch is asserted statically (`tests/test-explore-manifest.sh:176-181`).
- dependencies_and_callers: Validates command behavior documented in `commands/explore-idea.md`, including the manifest schema (`commands/explore-idea.md:161-197`), worker result collection (`commands/explore-idea.md:232-261`), and artifact synthesis (`commands/explore-idea.md:265-279`). Included in `tests/run-all-tests.sh:98-102`.
- edge_cases_or_failure_modes: Static grep-based checks can miss malformed JSON examples or runtime divergence, but they catch contract drift in field names and artifact names. It checks `.failed` sentinel documentation for error recovery (`tests/test-explore-manifest.sh:138-143`) and requires canonical `explore-report.md`/`final-idea.md` names (`tests/test-explore-manifest.sh:124-136`).
- validation_or_tests: The test itself is validation. It also requires final idea template sections for plan-ready synthesis: Final Recommendation, Rationale, Approach Summary, Objective Evidence, Explore Outcomes, Constraints, Known Risks, and Cross-Direction Learnings (`tests/test-explore-manifest.sh:190-212`).
- skip_candidate: `no`

### DEV-HZ-122 `file` `tests/test-validate-explore-idea-io.sh`
- cursor: `[_]`
- core_role: Executable test suite for `scripts/validate-explore-idea-io.sh`, covering pre-dispatch input validation, selector resolution, cap enforcement, git anchoring, template availability, and structured success output.
- algorithmic_behavior: Builds mock git repos and plugin template roots (`tests/test-validate-explore-idea-io.sh:33-58`), then exercises negative exit-code cases and positive structured output. It verifies `.directions.json` and `.md` companion handling, schema validation, option caps, direction selector resolution, dirty checkout blocking, non-git/unborn checkout failures, run ID shape, canonical artifact paths, and static dirty-check/base-anchor contract text.
- inputs_outputs_state: Inputs are fixture `tests/fixtures/directions/valid.directions.json`, temporary repos, copied validator/template files, and command arguments. Outputs are pass/fail assertions and captured validator stdout/stderr. Test state is confined to the test temp directory.
- gates_or_invariants: Exit code mapping is part of the contract: missing input, not found, missing companion, unsupported extension, schema failure, invalid args, git state failure, run-dir collision, and template missing (`scripts/validate-explore-idea-io.sh:19-30`). The test confirms many of these directly (`tests/test-validate-explore-idea-io.sh:67-283`). Success must emit all required keys and `VALIDATION_SUCCESS` (`tests/test-validate-explore-idea-io.sh:293-335`).
- dependencies_and_callers: Directly targets `scripts/validate-explore-idea-io.sh`. The command consumes that validator in Phase 1 and parses key-value lines split on the first literal separator (`commands/explore-idea.md:52-77`). Suite inclusion appears in `tests/run-all-tests.sh:98-102`.
- edge_cases_or_failure_modes: Covers duplicate raw selectors and mixed selector forms resolving to the same direction (`tests/test-validate-explore-idea-io.sh:147-163`), many dirty tracked files to avoid pipeline/SIGPIPE regressions (`tests/test-validate-explore-idea-io.sh:190-219`), non-git and unborn repos (`tests/test-validate-explore-idea-io.sh:221-254`), missing worker/report/final templates (`tests/test-validate-explore-idea-io.sh:256-283`), and draft basename versus metadata-derived slug rules (`tests/test-validate-explore-idea-io.sh:372-395`).
- validation_or_tests: This is direct validation coverage for the validator. It also has static contract assertions that worker setup documents detached HEAD and does not run `git checkout <BASE_BRANCH>` (`tests/test-validate-explore-idea-io.sh:409-419`) and that dirty-check captures `git diff` output rather than piping to `grep -q` (`tests/test-validate-explore-idea-io.sh:422-429`).
- skip_candidate: `no`

### DEV-HZ-152 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template that prevents transition to Codex review or loop exit while task tracking still has incomplete tasks.
- algorithmic_behavior: Renders an incomplete task list into a blocking message and instructs the agent to complete every remaining task, mark each via `TaskUpdate` as `completed`, and only then write summary/stop (`prompt-template/block/incomplete-todos.md:1-10`).
- inputs_outputs_state: Input placeholder is `{{INCOMPLETE_LIST}}` (`prompt-template/block/incomplete-todos.md:5`). Output is a block reason rendered into hook JSON by `hooks/loop-codex-stop-hook.sh`.
- gates_or_invariants: The invariant is strict: do not proceed to Codex review until all tasks are finished (`prompt-template/block/incomplete-todos.md:12`). The required action enumerates complete tasks, update status, then stop (`prompt-template/block/incomplete-todos.md:7-10`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh:434-455` loads this template when todo checker exits with incomplete tasks, substitutes `INCOMPLETE_LIST`, and returns JSON with `"decision": "block"`.
- edge_cases_or_failure_modes: If the template cannot be loaded, the stop hook has a fallback message with the same placeholder (`hooks/loop-codex-stop-hook.sh:439-445`). The template assumes the incomplete list is already computed by the hook-side checker; it does not validate task status itself.
- validation_or_tests: Covered indirectly by stop-hook/todo-checker tests, especially template-loading and incomplete-task stop behavior. The assigned file is a small policy template, not executable logic.
- skip_candidate: `no`

### DEV-HZ-182 `file` `prompt-template/claude/methodology-analysis-prompt.md`
- cursor: `[_]`
- core_role: RLCR exit-phase prompt defining the methodology-analysis subphase before final loop exit, including sanitization, optional issue filing, and completion marker requirements.
- algorithmic_behavior: Instructs Claude to spawn an Opus analysis agent over `round-*-summary.md` and `round-*-review-result.md`, write a sanitized `methodology-analysis-report.md`, then derive all user-facing content only from that report (`prompt-template/claude/methodology-analysis-prompt.md:12-46`). If improvements exist, it asks the user whether to file a sanitized GitHub issue and writes a completion marker (`prompt-template/claude/methodology-analysis-prompt.md:47-73`).
- inputs_outputs_state: Inputs are placeholders `{{LOOP_DIR}}`, `{{EXIT_REASON}}`, `{{EXIT_REASON_DESCRIPTION}}`, `{{CURRENT_ROUND}}`, and `{{MAX_ITERATIONS}}` (`prompt-template/claude/methodology-analysis-prompt.md:5-7`, `prompt-template/claude/methodology-analysis-prompt.md:16-20`). Outputs are `methodology-analysis-report.md`, optional GitHub issue content, and mandatory `methodology-analysis-done.md` (`prompt-template/claude/methodology-analysis-prompt.md:20`, `prompt-template/claude/methodology-analysis-prompt.md:62-69`, `prompt-template/claude/methodology-analysis-prompt.md:71-73`).
- gates_or_invariants: Strong privacy invariant: report must not contain paths, identifiers, domain terms, code, raw errors, URLs, or project-specific identifying information (`prompt-template/claude/methodology-analysis-prompt.md:22-31`). User-facing content must come solely from the sanitized report (`prompt-template/claude/methodology-analysis-prompt.md:43-46`). Completion marker must have meaningful content before exit (`prompt-template/claude/methodology-analysis-prompt.md:71-73`).
- dependencies_and_callers: Rendered by `hooks/lib/methodology-analysis.sh:74-87`, which renames active state to `methodology-analysis-state.md`, records `.methodology-exit-reason`, touches the done file, and returns block JSON (`hooks/lib/methodology-analysis.sh:64-99`). Completion validation checks the done marker and report (`hooks/lib/methodology-analysis.sh:114-129`).
- edge_cases_or_failure_modes: Privacy mode skips the phase (`hooks/lib/methodology-analysis.sh:42-46`). Re-entry is skipped if `methodology-analysis-state.md` already exists or non-empty done marker exists (`hooks/lib/methodology-analysis.sh:48-62`). Missing or empty completion marker prevents completion (`hooks/lib/methodology-analysis.sh:114-129`).
- validation_or_tests: Covered indirectly by methodology/finalize stop-hook tests and template loader coverage. The prompt itself is a transition contract rather than executable code.
- skip_candidate: `no`

### DEV-HZ-212 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Security robustness test for cancel authorization, focused on preventing state-file modification bypasses when the bash hook allows the cancel transition.
- algorithmic_behavior: Creates a mock loop with `state.md`, sources `hooks/lib/loop-common.sh`, and calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` across valid cancel forms and malicious command strings (`tests/robustness/test-cancel-security-robustness.sh:15-30`, `tests/robustness/test-cancel-security-robustness.sh:39-90`). It tests authorization through `.cancel-requested`, then injection, wrong path, extra argument, quote, whitespace, operator, variable, and symlink cases.
- inputs_outputs_state: Inputs are constructed shell command strings and mock loop filesystem state, especially `.cancel-requested`, `state.md`, and `finalize-state.md`. Outputs are test pass/fail messages. State changes include creating/removing test signal files and symlinks inside the temp loop directories.
- gates_or_invariants: Valid cancel requires the signal file plus exact `mv` from `state.md`, `finalize-state.md`, or `methodology-analysis-state.md` to `cancel-state.md`, with no shell chaining, substitution, hidden variables, mixed quotes, extra args, or symlink source. These invariants are implemented in `hooks/lib/loop-common.sh:1038-1056`, `hooks/lib/loop-common.sh:1062-1085`, `hooks/lib/loop-common.sh:1097-1119`, `hooks/lib/loop-common.sh:1149-1194`, and `hooks/lib/loop-common.sh:1196-1228`.
- dependencies_and_callers: Directly depends on `is_cancel_authorized` from `hooks/lib/loop-common.sh`. That predicate is called by `hooks/loop-bash-validator.sh` to allow only authorized state-file moves during cancellation (`hooks/loop-bash-validator.sh:271-296`, `hooks/loop-bash-validator.sh:428-455`, `hooks/loop-bash-validator.sh:458-487`). The cancel script creates `.cancel-requested` before moving the state file (`scripts/cancel-rlcr-loop.sh:156-166`).
- edge_cases_or_failure_modes: Negative coverage includes missing signal (`tests/robustness/test-cancel-security-robustness.sh:99-107`), command substitution/backticks (`tests/robustness/test-cancel-security-robustness.sh:109-133`), semicolon/AND/OR/pipe/newline chaining (`tests/robustness/test-cancel-security-robustness.sh:135-172`, `tests/robustness/test-cancel-security-robustness.sh:213-224`, `tests/robustness/test-cancel-security-robustness.sh:252-263`), wrong source/destination and extra args (`tests/robustness/test-cancel-security-robustness.sh:174-211`), variable/IFS manipulation (`tests/robustness/test-cancel-security-robustness.sh:226-237`, `tests/robustness/test-cancel-security-robustness.sh:291-302`), quote/trailing-space rejection (`tests/robustness/test-cancel-security-robustness.sh:278-315`), and symlink/path-regression cases (`tests/robustness/test-cancel-security-robustness.sh:317-405`).
- validation_or_tests: This file is the validation artifact. It specifically guards regressions where path strings containing `finalize` could misclassify symlinked `state.md` (`tests/robustness/test-cancel-security-robustness.sh:369-388`) and similarly checks `finalize-state.md` symlink rejection under tricky paths (`tests/robustness/test-cancel-security-robustness.sh:390-405`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV-HZ-002, DEV-HZ-032, DEV-HZ-062, DEV-HZ-092, DEV-HZ-122, DEV-HZ-152, DEV-HZ-182, DEV-HZ-212
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`