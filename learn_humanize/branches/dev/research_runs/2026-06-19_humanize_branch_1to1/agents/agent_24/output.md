# agent_24 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-024 `directory` `skills/humanize-gen-plan`
- cursor: `[_]`
- core_role: Flow skill definition for turning a rough draft into a structured implementation plan. Recursive inspection found a single child file, `skills/humanize-gen-plan/SKILL.md`, so the directory's algorithmic surface is entirely the skill instruction and its embedded flowchart.
- algorithmic_behavior: The skill defines a sequential planning algorithm: validate input/output paths, stop on validation failure, read the draft, reject unrelated drafts, analyze clarity/consistency/completeness/functionality, resolve issues with the user, confirm quantitative metrics, generate plan content, write the plan, review/fix inconsistencies, enforce language consistency, then report success. This flow is encoded in the mermaid graph at `skills/humanize-gen-plan/SKILL.md:19-47`. It delegates the first gate to `{{HUMANIZE_RUNTIME_ROOT}}/scripts/validate-gen-plan-io.sh --input <draft> --output <plan>` at `skills/humanize-gen-plan/SKILL.md:21`.
- inputs_outputs_state: Inputs are `--input <path/to/draft.md>` and `--output <path/to/plan.md>`, with optional `--coach`, documented at `skills/humanize-gen-plan/SKILL.md:49-57`. Output is a plan file with sections for goal description, AC-formatted acceptance criteria, path boundaries, dependencies/sequence, and implementation notes at `skills/humanize-gen-plan/SKILL.md:74-113`. State transitions are pass/fail gates rather than persistent runtime state: failed validation, irrelevant draft, unresolved issues, inconsistent generated plan, and multilingual output all branch before success.
- gates_or_invariants: Validation exit codes are part of the contract: input not found, empty input, missing output directory, existing output file, permission failure, invalid arguments, and missing template are enumerated at `skills/humanize-gen-plan/SKILL.md:115-127`. Coach mode adds mandatory stage quizzes before expanding each planning layer, separates decision questions from coaching quizzes, grades the user's answer, and requires overall acceptance before writing final plan at `skills/humanize-gen-plan/SKILL.md:58-72`.
- dependencies_and_callers: Sibling/parent coordination is through the installed runtime root placeholder and the gen-plan command. `commands/gen-plan.md` allows `validate-gen-plan-io.sh`, `ask-codex.sh`, `setup-rlcr-loop.sh`, `Read`, `Write`, `Task`, and `AskUserQuestion` at `commands/gen-plan.md:4-13`, and its phase list mirrors/extends this skill's flow at `commands/gen-plan.md:32-45`. The validation helper's accepted flags and exit codes are implemented in `scripts/validate-gen-plan-io.sh:16-28` and parsed at `scripts/validate-gen-plan-io.sh:37-80`.
- edge_cases_or_failure_modes: Skip/stop cases include invalid arguments, missing/empty input, nonexistent output directory, pre-existing output path, no write permission, missing plan template, unrelated draft, unresolved issue analysis, unconfirmed quantitative metrics, generated inconsistencies, and mixed-language output. `--discussion` and `--direct` are mutually exclusive in the helper at `scripts/validate-gen-plan-io.sh:82-86`; direct mode with auto-start only emits a note and does not mark convergence at `scripts/validate-gen-plan-io.sh:99-102`.
- validation_or_tests: `tests/test-gen-plan.sh:779-946` validates the delegated IO helper: missing flag values return 6, missing input returns 1, empty input returns 2, missing output directory returns 3, existing output or directory returns 4, valid paths return 0, `--coach` is accepted, deprecated learning mode is rejected, `--discussion`/`--direct` are recognized, and combined discussion/direct flags are rejected.
- skip_candidate: `no`

### DEV-HZ-054 `file` `scripts/bitlesson-select.sh`
- cursor: `[_]`
- core_role: Runtime selector script that chooses which BitLesson knowledge-base lessons should apply to a sub-task and emits a stable two-line machine-readable result.
- algorithmic_behavior: The script parses `--task`, `--paths`, and `--bitlesson-file`, validates them, reads the BitLesson file, short-circuits to `LESSON_IDS: NONE` when no concrete `## Lesson:` entries exist, resolves project/config context, routes to Codex or Claude based on configured model/provider mode, sends a constrained selector prompt, then normalizes the model output back into exactly `LESSON_IDS:` and `RATIONALE:` lines. Argument parsing and required-field gates are at `scripts/bitlesson-select.sh:23-85`; placeholder short-circuit is at `scripts/bitlesson-select.sh:87-97`; prompt construction is at `scripts/bitlesson-select.sh:147-179`; execution and output enforcement are at `scripts/bitlesson-select.sh:187-277`.
- inputs_outputs_state: Inputs are a task string, comma-separated related paths, and a BitLesson Markdown file. Runtime state is read from merged config: `bitlesson_model`, fallback `codex_model`, and `provider_mode` at `scripts/bitlesson-select.sh:113-120`. Output is exactly two stdout lines, `LESSON_IDS: <comma-separated lesson IDs or NONE>` and `RATIONALE: <one concise sentence>`, described in usage at `scripts/bitlesson-select.sh:23-31` and enforced at `scripts/bitlesson-select.sh:252-277`.
- gates_or_invariants: The script fails closed on missing task, paths, bitlesson file path, nonexistent file, or whitespace-only file at `scripts/bitlesson-select.sh:64-91`. It requires stable model output; missing either expected line returns an error and prints raw output at `scripts/bitlesson-select.sh:268-273`. The prompt forbids tool use and repository inspection, limiting the selector to prompt-provided task, paths, and BitLesson content at `scripts/bitlesson-select.sh:165-177`. Codex runs in read-only mode with low reasoning and project root scoping at `scripts/bitlesson-select.sh:215-221`.
- dependencies_and_callers: It sources config loading, model routing, project-root resolution, portable timeout, and loop-common at `scripts/bitlesson-select.sh:9-21`. Provider detection and dependency checks come from `scripts/lib/model-router.sh:10-60`. Config merge behavior comes from `scripts/lib/config-loader.sh:63-159`. Runtime callers include BitLesson selector command/agent paths and RLCR prompt generation; `agents/bitlesson-selector.md:22` documents that this script is the execution layer for model-routed selection, and `hooks/loop-codex-stop-hook.sh:1583-1594` injects bitlesson-selector requirements into follow-up prompts when BitLesson is required.
- edge_cases_or_failure_modes: Unknown arguments exit 1. Empty or missing BitLesson files exit 1. A BitLesson file with no concrete `## Lesson:` entries exits 0 with `NONE` without invoking a model. `provider_mode=codex-only` forces Claude-configured models onto the Codex fallback model at `scripts/bitlesson-select.sh:127-130`. If the configured provider binary is missing, the script falls back to Codex, then validates Codex availability at `scripts/bitlesson-select.sh:136-141`. Timeouts return 124 at `scripts/bitlesson-select.sh:237-240`; nonzero provider exits are propagated at `scripts/bitlesson-select.sh:242-246`. Codex feature probing avoids `echo|grep -q` pipefail/SIGPIPE hazards by capturing help output before grep at `scripts/bitlesson-select.sh:192-207`.
- validation_or_tests: `tests/test-bitlesson-select-routing.sh:137-508` covers Codex routing for `gpt-*`, stdin prompt delivery via trailing `-`, Claude routing for haiku/sonnet/opus, unknown-model failure, missing Codex failure, missing Claude fallback to Codex, `provider_mode=codex-only`, placeholder short-circuit to `NONE`, hook-disable/ephemeral/read-only Codex args, and the absence of pipefail-prone help probes.
- skip_candidate: `no`

### DEV-HZ-084 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the code-review issue extraction algorithm used by `detect_review_issues()` in `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The test states the algorithm explicitly: scan only the last 50 lines of a Codex review log, find the first line whose first 10 characters contain `[P<digit>]`, extract from that line through EOF, output extracted content, and return 1 when no issue marker is found. This is documented at `tests/test-codex-review-merge.sh:3-16`. The implementation under test has the same contract at `hooks/lib/loop-common.sh:720-739` and the concrete logic at `hooks/lib/loop-common.sh:740-784`.
- inputs_outputs_state: Inputs are test-created review logs named `round-<n>-codex-review.log` under an isolated `CACHE_DIR`, plus `LOOP_DIR` for audit output. The test sets these globals in `setup_test_env()` at `tests/test-codex-review-merge.sh:47-54`, sources `hooks/lib/loop-common.sh` at `tests/test-codex-review-merge.sh:41-42`, and invokes `detect_review_issues <round>`. Outputs are return codes, stdout extraction content, and when issues are found the result file `round-<n>-review-result.md`, verified at `tests/test-codex-review-merge.sh:306-330`.
- gates_or_invariants: The review detector must distinguish three outcomes: return 0 for issues found, 1 for no issues, and 2 for missing/empty logs. The implementation documents these at `hooks/lib/loop-common.sh:720-724`. The invariant that issue markers must appear in the first 10 characters is tested at `tests/test-codex-review-merge.sh:56-107`; the invariant that only the last 50 lines are scanned is tested at `tests/test-codex-review-merge.sh:171-227`.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`. The test uses temporary cache isolation via `XDG_CACHE_HOME` at `tests/test-codex-review-merge.sh:37-39` and does not require network or real Codex execution. The underlying implementation uses `tail`, `awk`, `sed`, `wc`, and the global `CACHE_DIR`/`LOOP_DIR` to scan and persist review results at `hooks/lib/loop-common.sh:741-779`.
- edge_cases_or_failure_modes: Covered edge cases include marker outside the first 10 characters, no marker, missing log, empty log, marker late in a long log, marker early but outside the last-50-line window, multiple markers with extraction beginning at the first, marker at position 0, dash-prefixed marker, result-file creation, and exactly 50-line logs. These scenarios are laid out across `tests/test-codex-review-merge.sh:56-354`.
- validation_or_tests: The file is itself the validation asset. It accumulates pass/fail counts and exits 1 if any test fails at `tests/test-codex-review-merge.sh:356-368`.
- skip_candidate: `no`

### DEV-HZ-114 `file` `tests/test-streaming.sh`
- cursor: `[_]`
- core_role: Executable specification for dashboard log streaming, replay, parser cache-log discovery, registry sharing, reconnect semantics, and idle stream eviction.
- algorithmic_behavior: The test covers `viz/server/log_streamer.py` and parser extensions for the frozen streaming protocol: snapshot existing bytes in 64 KiB chunks, append newly written bytes, resync on truncation/rotation/recreation/missing/overflow, sticky EOF, replay from `Last-Event-Id`, deterministic cache-log discovery, shared registry semantics, and idle retention cleanup. The contract scope is listed at `tests/test-streaming.sh:3-18`. The implementation declares event shapes, 64 KiB chunking, and 256-event retention at `viz/server/log_streamer.py:1-18` and constants at `viz/server/log_streamer.py:29-50`.
- inputs_outputs_state: Inputs are temporary cache files, synthetic round log basenames, and Python snippets importing `LogStream`, `LogStreamRegistry`, and `cache_logs_for_session`. Outputs are printed assertions over event counts, event types, offsets, decoded base64 payloads, replay-window booleans, parser metadata, and registry sizes. State inside `LogStream` includes `_offset`, `_stat`, `_eof_emitted`, `_retained`, `_missing_emitted`, and `_resync_pending` initialized at `viz/server/log_streamer.py:102-128`.
- gates_or_invariants: Missing startup snapshot emits no events, while first poll emits `resync missing` at `tests/test-streaming.sh:56-80`. Existing-file snapshots must start at offset 0 and carry `eof=False` at `tests/test-streaming.sh:82-112`. Appends must use the prior byte length as offset at `tests/test-streaming.sh:114-136`. Truncation and rotation must emit resync followed by fresh snapshot at `tests/test-streaming.sh:138-189`. EOF must be one-shot and make later polls no-ops at `tests/test-streaming.sh:219-243`. Replay in-window returns only newer events, replay overflow emits `resync overflow`, and chunking splits a 130 KiB file into offsets `0,65536,131072` at `tests/test-streaming.sh:245-340`.
- dependencies_and_callers: Imports from `viz/server/log_streamer.py` through a temporary Python path at `tests/test-streaming.sh:47-54`. Parser integration uses `cache_logs_for_session()` from `viz/server/parser.py`, which delegates to `rlcr_sources.cache_dir_for_session()` and `rlcr_sources.live_log_paths()` at `viz/server/parser.py:29-48`. The registry tests exercise `LogStreamRegistry.get_or_create`, `acquire`, `release`, and `streams_in_cache_dir`, implemented at `viz/server/log_streamer.py:343-494`.
- edge_cases_or_failure_modes: Explicitly tests missing files, missing-then-reappears, inode rotation, in-place truncation, EOF idempotency, replay after retention overflow, large snapshot chunking, deterministic ordering of run/review logs, reconnect using the same registered stream, reconnect from an out-of-window/foreign stream, and idle refcount-zero streams that would otherwise leak if no later release occurred. Idle eviction by both `acquire()` and `streams_in_cache_dir()` is tested at `tests/test-streaming.sh:474-520`.
- validation_or_tests: The file is a self-contained shell/Python test. It skips only when `python3` is unavailable at `tests/test-streaming.sh:30-33`, tracks pass/fail counts at `tests/test-streaming.sh:35-42`, and exits 1 on failures at `tests/test-streaming.sh:522-530`.
- skip_candidate: `no`

### DEV-HZ-144 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block used by the RLCR stop hook to explain untracked-file handling when the loop is blocked by a dirty git tree.
- algorithmic_behavior: The template adds a fixed note that untracked files may be build artifacts, test outputs, runtime-generated files, dependencies, IDE files, logs, caches, or temp files, and advises adding appropriate patterns to `.gitignore`. The content is in `prompt-template/block/git-not-clean-untracked.md:2-10`.
- inputs_outputs_state: It has no dynamic placeholders and no internal state. Input is selection by the template loader; output is static Markdown appended to the dirty-git block. The stop hook loads it when there are untracked files outside the `.humanize[-/]` local-state pattern at `hooks/loop-codex-stop-hook.sh:721-728`.
- gates_or_invariants: The invariant is diagnostic, not computational: untracked files should not be blindly committed. The hook first removes `.humanize/` and `.humanize-*` local paths from the blocking status set, then applies this generic note only for other untracked files at `hooks/loop-codex-stop-hook.sh:700-728`.
- dependencies_and_callers: Called through `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` in `hooks/loop-codex-stop-hook.sh:724`, with fallback text if the template cannot load at `hooks/loop-codex-stop-hook.sh:725-727`.
- edge_cases_or_failure_modes: Because the block has no placeholders, rendering failures are limited to missing template/file-loader issues, handled by fallback text. A subtle classification edge case is tested around `.humanizeconfig` versus `.humanize/` and `.humanize-old/`: `.humanizeconfig` remains a normal untracked file while dash/slash variants receive the special local note, and this generic note must still appear for normal untracked files at `tests/test-stop-hook-legacy-compat.sh:165-182`.
- validation_or_tests: `tests/test-stop-hook-legacy-compat.sh:173-176` asserts blocked output includes `Note on Untracked Files` alongside the special Humanize-local note when applicable.
- skip_candidate: `no`

### DEV-HZ-174 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Prompt block used by write validation to enforce that round summary files are written inside the active loop directory.
- algorithmic_behavior: The template emits a hard instruction titled `Wrong Summary Location`, says summary files must be in the loop directory, and renders the expected path through `{{CORRECT_PATH}}` at `prompt-template/block/wrong-summary-location.md:1-5`.
- inputs_outputs_state: Input is the `CORRECT_PATH` variable supplied by validators. Output is Markdown sent to stderr/blocking feedback. In the write validator, when a summary or contract file is outside the active `.humanize/rlcr` loop directory, summary writes compute `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` and render this template at `hooks/loop-write-validator.sh:283-297`.
- gates_or_invariants: The validation gate exits with status 2 after rendering the block at `hooks/loop-write-validator.sh:299`, so wrong summary placement is a hard block. The invariant is path-level: a round summary must be named for the current round and located under the active loop directory.
- dependencies_and_callers: The direct caller is `hooks/loop-write-validator.sh`, via `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-summary-location.md" ... "CORRECT_PATH=$CORRECT_PATH"` at `hooks/loop-write-validator.sh:292-297`. Related sibling templates enforce wrong contract location and wrong round number, as seen near `hooks/loop-write-validator.sh:284-290`.
- edge_cases_or_failure_modes: If the template is missing or cannot render, the caller supplies a fallback block containing the same `{{CORRECT_PATH}}` placeholder at `hooks/loop-write-validator.sh:293-296`. Canonicalization and additional path checks continue below this branch for other file types, but this specific block handles the outside-loop summary/contract case first.
- validation_or_tests: Template loader integration tests exercise real template rendering with `CORRECT_PATH` substitution for sibling path blocks at `tests/test-templates-comprehensive.sh:493-510`. The specific summary-location block is referenced by `rg` in the write validator path and participates in the same `load_and_render_safe` rendering contract.
- skip_candidate: `no`

### DEV-HZ-204 `file` `skills/ask-codex/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction defining the safe invocation contract for one-shot Codex consultation via `scripts/ask-codex.sh`.
- algorithmic_behavior: The skill tells the agent to send a question/task to Codex and return stdout, while keeping status on stderr. It has a key shell-safety rule: free-form user text must be quoted, and when flags are present, flags remain separate shell arguments while the remaining task is one quoted final argument. The safe simple invocation is at `skills/ask-codex/SKILL.md:16-20`; the flagged invocation example is at `skills/ask-codex/SKILL.md:22-28`; the forbidden bare `$ARGUMENTS` form is at `skills/ask-codex/SKILL.md:30-36`.
- inputs_outputs_state: Inputs are optional `--codex-model MODEL:EFFORT`, optional `--codex-timeout SECONDS`, and free-form question/task text from the skill `argument-hint` at `skills/ask-codex/SKILL.md:4`. Output semantics are stdout containing Codex response and stderr containing status info at `skills/ask-codex/SKILL.md:38-42`. The backing script writes project-local `.humanize/skill/<unique-id>/{input,output,metadata}.md` and cache logs; this is implemented in `scripts/ask-codex.sh:15-18`, directory creation at `scripts/ask-codex.sh:202-218`, and final output/metadata at `scripts/ask-codex.sh:420-444`.
- gates_or_invariants: The skill documents exit-code handling: 0 success, 1 validation error, 124 timeout, and other Codex process errors at `skills/ask-codex/SKILL.md:44-51`. The backing script enforces Codex availability, non-empty question, safe model characters, safe effort characters, project-root resolution, timeout handling, nonzero exit handling, and empty-response handling at `scripts/ask-codex.sh:153-196` and `scripts/ask-codex.sh:338-414`.
- dependencies_and_callers: The skill's allowed tool is `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh:*)` at `skills/ask-codex/SKILL.md:5`. The script itself sources `portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout and default model/effort config at `scripts/ask-codex.sh:26-33`. Gen-plan uses `ask-codex.sh` for first-pass planning analysis at `commands/gen-plan.md:256-285`; worker prompts require `CLAUDE_PROJECT_DIR` scoping for Codex calls at `tests/test-worker-result-contract.sh:177-182`.
- edge_cases_or_failure_modes: The major edge case this skill prevents is shell reparsing of user text containing spaces or metacharacters like `(`, `)`, `;`, `#`, `*`, or `[`, documented at `skills/ask-codex/SKILL.md:12-36`. Runtime edge cases include missing Codex, empty question, invalid model/effort characters, unresolved project root, home cache not writable fallback at `scripts/ask-codex.sh:214-218`, timeout metadata at `scripts/ask-codex.sh:338-360`, Codex nonzero exit at `scripts/ask-codex.sh:363-388`, and empty stdout at `scripts/ask-codex.sh:390-414`.
- validation_or_tests: `tests/test-ask-codex.sh:553-569` asserts the skill warns against bare `$ARGUMENTS`, documents the quoted simple invocation, and requires a single quoted final argument for free-form text. Additional runtime tests in the same file exercise output/cache/metadata behavior and Codex hook-disable probing. `tests/test-unified-codex-config.sh:678-770` validates config-backed model/effort defaults and `--codex-model` overrides for the script.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`