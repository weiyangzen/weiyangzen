# agent_23 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`
- note: read-only inspection only; no files modified and no test suites executed.

## Item Evidence

### DEV-HZ-023 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: Primary Humanize skill package for the RLCR algorithm surface. Recursive inspection found one child file, `skills/humanize/SKILL.md`; no nested subdirectories. The skill declares `user-invocable: false` and `disable-model-invocation: true` at `skills/humanize/SKILL.md:1-6`, making it a documentation/control contract rather than a direct slash-flow entrypoint.
- algorithmic_behavior: Defines the Humanize loop as two phases: implementation summary review followed by code review, with automatic Codex native `Stop` hook gating on supported Codex installs (`skills/humanize/SKILL.md:32-49`). It documents setup, cancel, plan generation, and ask-codex command entrypoints (`skills/humanize/SKILL.md:61-110`) and sets the expected plan schema with acceptance criteria, path boundaries, dependencies, and implementation notes (`skills/humanize/SKILL.md:112-151`).
- inputs_outputs_state: Inputs are a plan file or `--skip-impl`, Codex model/effort options, base branch, max rounds, quiz/agent/team flags, and optional plan tracking (`skills/humanize/SKILL.md:73-88`). Outputs/state live under `.humanize/`, especially `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, round summaries/review results, finalize files, and complete state (`skills/humanize/SKILL.md:181-204`). The runtime root placeholder is hydrated by the installer (`skills/humanize/SKILL.md:12-20`).
- gates_or_invariants: Requires summaries before exit, goal-tracker maintenance, no state-file cheating, native Stop hook use, and trusting external review (`skills/humanize/SKILL.md:167-174`). It establishes the Goal Tracker immutable/mutable split, acceptance-criteria mapping, explicit deferrals, and periodic full alignment checks (`skills/humanize/SKILL.md:153-165`).
- dependencies_and_callers: Installed by `scripts/install-skill.sh`, which syncs `humanize`, `humanize-gen-plan`, `humanize-refine-plan`, and `humanize-rlcr` (`scripts/install-skill.sh:39-44`), copies runtime bundles into `<skills-dir>/humanize` (`scripts/install-skill.sh:183-193`), and hydrates `{{HUMANIZE_RUNTIME_ROOT}}` (`scripts/install-skill.sh:195-219`). It coordinates with sibling RLCR entrypoint skills: Codex-native `skills/humanize-rlcr/SKILL.md:24-49` and Kimi gate-based `skills/humanize-rlcr/SKILL-kimi.md:9-17`.
- edge_cases_or_failure_modes: The skill warns that Codex native Stop hook behavior depends on Codex CLI hook support (`skills/humanize/SKILL.md:48`). It documents review-only mode, max iterations, full-review intervals, tracked-plan enforcement, push-every-round, and yolo/quiz shortcuts (`skills/humanize/SKILL.md:73-88`). Because it is a single-file directory, skip risk is low; the directory is core because installation and runtime behavior depend on its hydrated command paths and workflow contract.
- validation_or_tests: `tests/test-codex-hook-install.sh` asserts Codex install syncs this skill bundle and keeps the RLCR entrypoint skill (`tests/test-codex-hook-install.sh:129-139`). It also checks provider-specific skill content and hook setup. No direct execution of `skills/humanize/SKILL.md` occurs; validation is through installer and workflow tests.
- skip_candidate: `no`

### DEV-HZ-053 `file` `scripts/bitlesson-init.sh`
- cursor: `[_]`
- core_role: Deterministic BitLesson state bootstrapper. It creates the project-local BitLesson knowledge file used by RLCR summary/lesson discipline, defaulting to `.humanize/bitlesson.md` (`scripts/bitlesson-init.sh:5-15`, `scripts/bitlesson-init.sh:18-20`).
- algorithmic_behavior: Parses `--project-root`, `--template`, and optional `--bitlesson-relpath` (`scripts/bitlesson-init.sh:22-46`). It validates required args and existence of the project root/template (`scripts/bitlesson-init.sh:48-68`), rejects absolute or parent-traversing relative paths (`scripts/bitlesson-init.sh:70-73`), canonicalizes the project root (`scripts/bitlesson-init.sh:75`), then creates parent directories and copies the template only when the target file is missing (`scripts/bitlesson-init.sh:78-86`).
- inputs_outputs_state: Inputs are an existing project directory, an existing template file, and a safe relative destination. Output is the resolved absolute BitLesson file path on stdout (`scripts/bitlesson-init.sh:88`). State transition is idempotent: missing regular file becomes copied from template; existing regular file is preserved; existing non-file blocks.
- gates_or_invariants: Never overwrites an existing regular file (`scripts/bitlesson-init.sh:83-86`). The destination must remain inside the project root via relative-path validation (`scripts/bitlesson-init.sh:70-73`). Existing non-regular path is a hard error (`scripts/bitlesson-init.sh:78-81`).
- dependencies_and_callers: Called by `scripts/setup-rlcr-loop.sh` to initialize `.humanize/bitlesson.md` from `templates/bitlesson.md` before state creation (`scripts/setup-rlcr-loop.sh:861-871`). The created path is then recorded in RLCR state as `bitlesson_file` and paired with `bitlesson_required`/`bitlesson_allow_empty_none` (`scripts/setup-rlcr-loop.sh:880-907`). The Stop hook later reads these fields and validates BitLesson deltas when required (`hooks/loop-codex-stop-hook.sh:144-168`, `hooks/loop-codex-stop-hook.sh:868-883`).
- edge_cases_or_failure_modes: Unknown args and missing option values fail through parser behavior under `set -euo pipefail`; missing required args emit usage and exit. Unsafe relpaths, missing template/root, and target directories occupying the file path are explicit failures. There is no atomic copy guard; a failed `cp` under `set -e` aborts before printing a path.
- validation_or_tests: No direct dedicated test for `bitlesson-init.sh` was found in the inspected test references. It is covered indirectly by RLCR setup behavior and legacy Stop-hook tests around BitLesson enforcement, especially the rule that legacy state without `bitlesson_required` remains disabled even when `.humanize/bitlesson.md` exists (`tests/test-stop-hook-legacy-compat.sh:185-197`).
- skip_candidate: `no`

### DEV-HZ-083 `file` `tests/test-codex-hook-install.sh`
- cursor: `[_]`
- core_role: Executable specification for Codex-native Humanize installation, hook merge behavior, feature compatibility gates, provider-specific skill surfaces, and BitLesson selector shim behavior.
- algorithmic_behavior: Builds a temp fake Codex environment, fake `codex` binary, Codex home, hooks file, user config dir, and command-bin dir (`tests/test-codex-hook-install.sh:29-40`). The fake Codex supports `--help`, `features list`, `features enable hooks`, and `exec` (`tests/test-codex-hook-install.sh:40-79`). It seeds an existing hooks file with SessionStart, a stale Humanize Stop hook, and an unrelated Stop hook (`tests/test-codex-hook-install.sh:82-119`), then runs `scripts/install-skill.sh --target codex` (`tests/test-codex-hook-install.sh:121-127`).
- inputs_outputs_state: Inputs are fake PATH/CODEX_HOME/XDG_CONFIG_HOME, install args, and seeded hooks JSON. Expected outputs include synced `skills/humanize/SKILL.md`, synced `humanize-rlcr`, written `hooks.json`, `.hooks-enabled`, Humanize user config, and executable `bitlesson-selector` shim (`tests/test-codex-hook-install.sh:129-163`). It verifies user config state: `bitlesson_model` is `gpt-5.5` and `provider_mode` is `codex-only` (`tests/test-codex-hook-install.sh:165-177`).
- gates_or_invariants: Hook merge must add the managed Stop command, preserve unrelated Stop hooks, remove stale Humanize hook commands, preserve SessionStart hooks, and leave exactly one managed Humanize Stop hook (`tests/test-codex-hook-install.sh:179-237`). A second install must remain idempotent (`tests/test-codex-hook-install.sh:262-294`) while feature enable runs on each install/update (`tests/test-codex-hook-install.sh:296-300`).
- dependencies_and_callers: Tests `scripts/install-skill.sh`, which syncs skills/runtime, seeds Codex user config, installs native hooks, and creates the shim (`scripts/install-skill.sh:260-438`). Native hook support is enforced by `scripts/install-codex-hooks.sh`, which rejects legacy config keys, missing native hooks, and missing `--disable` support (`scripts/install-codex-hooks.sh:84-117`). Hook JSON merge uses `config/codex-hooks.json`, whose Stop command points at `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` (`config/codex-hooks.json:1-16`).
- edge_cases_or_failure_modes: Rejects legacy `codex_hooks` config and explains the rename (`tests/test-codex-hook-install.sh:302-332`), rejects builds exposing only legacy `codex_hooks` (`tests/test-codex-hook-install.sh:334-377`), rejects builds without native hooks (`tests/test-codex-hook-install.sh:379-425`), and rejects hooks-capable Codex builds without `--disable` because recursive Stop-hook prevention depends on it (`tests/test-codex-hook-install.sh:427-482`). It also verifies Kimi installs get the gate-based RLCR skill and do not mention native Stop hooks (`tests/test-codex-hook-install.sh:484-530`).
- validation_or_tests: The file is itself the validation asset. Additional installer invariants include `--target both` writing `provider_mode: codex-only` (`tests/test-codex-hook-install.sh:532-559`) and rejecting shared/equivalent Codex/Kimi skills dirs, including normalized nonexistent paths (`tests/test-codex-hook-install.sh:561-625`).
- skip_candidate: `no`

### DEV-HZ-113 `file` `tests/test-stop-hook-legacy-compat.sh`
- cursor: `[_]`
- core_role: Executable regression spec for legacy compatibility in `hooks/loop-codex-stop-hook.sh`, focused on git dirty filtering for legacy Humanize local dirs and BitLesson enforcement defaults.
- algorithmic_behavior: Creates temp git repos with `.gitignore`, a plan file, `.humanize/rlcr/<timestamp>/state.md`, `round-0-summary.md`, and `goal-tracker.md` (`tests/test-stop-hook-legacy-compat.sh:40-118`). The fixture intentionally omits `bitlesson_required` from state and omits BitLesson Delta from the summary so regressions would block before fake Codex (`tests/test-stop-hook-legacy-compat.sh:87-93`). A fake `codex` writes a marker when invoked (`tests/test-stop-hook-legacy-compat.sh:25-38`, `tests/test-stop-hook-legacy-compat.sh:120-136`).
- inputs_outputs_state: Inputs are constructed repo state, optional `.humanize/bitlesson.md`, untracked paths, and hook JSON `{}`. Outputs are hook exit code, marker file presence, and hook messages. State transition is observational only: it confirms whether the Stop hook reaches Codex review or blocks.
- gates_or_invariants: Untracked `.humanize-old` must be ignored for dirty checks and allow Codex invocation (`tests/test-stop-hook-legacy-compat.sh:145-160`). Untracked `.humanizeconfig` must still be treated as a real untracked file and block before Codex, while still showing the Humanize local-dir special note and generic untracked note (`tests/test-stop-hook-legacy-compat.sh:162-183`). Legacy loops without `bitlesson_required` must keep BitLesson enforcement disabled even if `.humanize/bitlesson.md` exists (`tests/test-stop-hook-legacy-compat.sh:185-197`).
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh` and `tests/test-helpers.sh` (`tests/test-stop-hook-legacy-compat.sh:14-20`). The hook supports this behavior by defaulting `BITLESSON_REQUIRED` to `false` unless the raw field exists (`hooks/loop-codex-stop-hook.sh:144-147`) and by excluding only `.humanize/` plus dash-separated `.humanize-*` from dirty blocking (`hooks/loop-codex-stop-hook.sh:699-705`).
- edge_cases_or_failure_modes: The test explicitly notes `.humanize/` may be covered by a global gitignore, so `.humanize-old/` is used to ensure a visible untracked Humanize-local path (`tests/test-stop-hook-legacy-compat.sh:166-170`). This guards against an over-broad ignore rule that would accidentally exempt `.humanizeconfig`.
- validation_or_tests: The file is the validation asset. It also validates the assigned `git-not-clean-humanize-local.md` template indirectly by requiring the "Special Case - .humanize directory detected" text when a dirty block includes Humanize-local paths (`tests/test-stop-hook-legacy-compat.sh:173-177`).
- skip_candidate: `no`

### DEV-HZ-143 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Stop-hook prompt block explaining the special handling for local `.humanize` runtime directories during git-clean enforcement.
- algorithmic_behavior: Emits a short remediation block titled "Special Case - .humanize directory detected", states `.humanize/` is created by `humanize:start-rlcr-loop` and should not be committed, and suggests adding `.humanize*` to `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:1-8`).
- inputs_outputs_state: No template variables. Input is only selection by the Stop hook; output is static markdown appended to the Stop hook block reason. It does not mutate state itself.
- gates_or_invariants: The actual gate is in `hooks/loop-codex-stop-hook.sh`: if cached git status still has blocking changes after excluding `.humanize/` and `.humanize-*`, the hook loads this template when untracked Humanize-local paths are present (`hooks/loop-codex-stop-hook.sh:699-718`). The invariant is advisory separation: Humanize runtime dirs should be ignored locally, but unrelated untracked files still block.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` in the Stop hook (`hooks/loop-codex-stop-hook.sh:712-718`). It depends on the template loader sourced through `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:240-248`).
- edge_cases_or_failure_modes: The template recommends a broad `.humanize*` ignore, but runtime dirty filtering is narrower: it excludes `.humanize/` and dash-separated legacy variants, not `.humanizeconfig` (`hooks/loop-codex-stop-hook.sh:700-705`). `tests/test-stop-hook-legacy-compat.sh` protects that edge by ensuring `.humanizeconfig` still blocks (`tests/test-stop-hook-legacy-compat.sh:162-183`).
- validation_or_tests: Covered indirectly by `tests/test-stop-hook-legacy-compat.sh`, which expects the special-case template text in the dirty-block output (`tests/test-stop-hook-legacy-compat.sh:173-177`). Template existence/reference coverage is also searched by template reference tests, but this specific block’s behavioral assertion is in the legacy compatibility test.
- skip_candidate: `no`

### DEV-HZ-173 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Validator block template for enforcing current-round file naming in RLCR summary/contract writes and edits.
- algorithmic_behavior: Renders a "Wrong Round Number" message, interpolating `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, and `{{CORRECT_PATH}}`, then instructs the agent not to increment the round number manually (`prompt-template/block/wrong-round-number.md:1-7`).
- inputs_outputs_state: Inputs are template variables supplied by write/edit validators. Output is a markdown block sent to stderr with the correct file path. It does not mutate loop state.
- gates_or_invariants: `hooks/loop-write-validator.sh` extracts the requested round from summary/contract filenames, compares it to `CURRENT_ROUND`, renders this template, and exits `2` on mismatch unless the file is allowlisted (`hooks/loop-write-validator.sh:315-336`). `hooks/loop-edit-validator.sh` applies the same gate for edits (`hooks/loop-edit-validator.sh:247-272`).
- dependencies_and_callers: Depends on `load_and_render_safe` from the template loader path initialized by `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:240-248`). It coordinates with validator helpers such as `extract_round_number`, file-type detection, active-loop resolution, and allowlist checks in the validator scripts.
- edge_cases_or_failure_modes: Handles both `summary` and `contract` file types and both write/edit actions through variables. If template loading fails, validators have inline fallback text (`hooks/loop-write-validator.sh:324-329`, `hooks/loop-edit-validator.sh:260-265`). Allowlisted files bypass the block.
- validation_or_tests: `tests/test-template-loader.sh` renders the real template and asserts title, wrong-round filename, and current-round text are substituted (`tests/test-template-loader.sh:151-164`). `tests/test-templates-comprehensive.sh` repeats real-template rendering and checks the correct path appears (`tests/test-templates-comprehensive.sh:498-510`).
- skip_candidate: `no`

### DEV-HZ-203 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Sourceable terminal monitor implementation for Humanize one-shot skill invocations under `.humanize/skill`, including ask-codex and ask-gemini. It provides `_humanize_monitor_skill` for `humanize monitor skill|codex|gemini`.
- algorithmic_behavior: Parses `--once` and `--tool-filter` (`scripts/lib/monitor-skill.sh:31-41`), requires `.humanize/skill` (`scripts/lib/monitor-skill.sh:43-48`), classifies invocation tool from `metadata.md` or `input.md` (`scripts/lib/monitor-skill.sh:50-64`), filters by tool with legacy unknown-as-codex behavior (`scripts/lib/monitor-skill.sh:66-75`), lists timestamp-shaped invocation dirs newest-first (`scripts/lib/monitor-skill.sh:77-95`), chooses the newest invocation with watchable content (`scripts/lib/monitor-skill.sh:97-117`), counts statuses (`scripts/lib/monitor-skill.sh:119-143`), extracts first-line question text (`scripts/lib/monitor-skill.sh:145-152`), resolves global cache (`scripts/lib/monitor-skill.sh:154-163`), and chooses the best monitored file from global/local cache or project-local output/input (`scripts/lib/monitor-skill.sh:165-217`).
- inputs_outputs_state: Inputs are invocation directories named like `YYYY-MM-DD_HH-MM-SS...`, `metadata.md`, `input.md`, optional `output.md`, optional cache files, terminal dimensions, and optional filters. `--once` outputs aggregate counts, focused invocation metadata, watched output, and recent invocation rows (`scripts/lib/monitor-skill.sh:339-425`). Interactive mode draws a fixed status bar and tails the selected file (`scripts/lib/monitor-skill.sh:428-527`). It does not modify invocation state; it only starts/stops a background `tail`.
- gates_or_invariants: Invalid/missing `.humanize/skill` returns error (`scripts/lib/monitor-skill.sh:43-48`); no valid invocations returns error in once mode (`scripts/lib/monitor-skill.sh:339-347`). Directory names must match the timestamp prefix regex (`scripts/lib/monitor-skill.sh:84-86`, `scripts/lib/monitor-skill.sh:126-128`). Running invocations are inferred by absent `metadata.md` and prefer live logs over outputs (`scripts/lib/monitor-skill.sh:173-207`); completed invocations prefer `output.md` then cache outputs/logs (`scripts/lib/monitor-skill.sh:208-215`).
- dependencies_and_callers: The script is sourced by `scripts/humanize.sh` if present (`scripts/humanize.sh:1408-1411`). Command routing maps `humanize monitor skill` to all tools, `monitor codex` to `--tool-filter codex`, and `monitor gemini` to `--tool-filter gemini` (`scripts/humanize.sh:1355-1371`). It depends on `monitor-common.sh` functions, `humanize_split_to_array`, `git rev-parse`, `find`, `sort`, `head`, `tail`, `tput`, and zsh compatibility options (`scripts/lib/monitor-skill.sh:8-21`).
- edge_cases_or_failure_modes: Tool metadata can be missing, producing `unknown`; unknown invocations pass the codex filter for legacy compatibility (`scripts/lib/monitor-skill.sh:72-73`). Empty output displays "No output available" unless still running (`scripts/lib/monitor-skill.sh:394-400`). Interactive cleanup guards tail termination, resets scroll region, restores cursor/screen, and handles bash/zsh traps (`scripts/lib/monitor-skill.sh:438-470`). Non-terminal `tput` failures are not broadly guarded in draw/setup paths.
- validation_or_tests: `tests/test-skill-monitor.sh` covers once-mode only, including missing directory, empty invocation dir, single invocation summaries, mixed status counts, running invocation detection, recent list, first-line question extraction, empty-response output, and ignoring non-timestamp dirs (`tests/test-skill-monitor.sh:107-389`). Interactive monitor behavior is explicitly not tested (`tests/test-skill-monitor.sh:3-7`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item sections present, one per assigned row: DEV-HZ-023, DEV-HZ-053, DEV-HZ-083, DEV-HZ-113, DEV-HZ-143, DEV-HZ-173, DEV-HZ-203
- missing_items: none
- duplicate_items: none in Item Evidence section
- final_worker_status: `complete`