# agent_06 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-006 `directory` `hooks`
- cursor: `[_]`
- core_role: The `hooks` directory is the RLCR control plane. It registers Claude Code hooks, validates tool access to loop-managed files, enforces plan and state invariants, gates natural stops through Codex review, manages review/finalize/methodology-analysis phases, and coordinates background task parking. The manifest wiring is in `hooks/hooks.json:14` for PreToolUse validators, `hooks/hooks.json:52` for PostToolUse Bash, `hooks/hooks.json:63` for Stop, and `hooks/hooks.json:4` for UserPromptSubmit plan validation.
- algorithmic_behavior: The directory implements a session-aware loop state machine. `loop-codex-stop-hook.sh` resolves the active loop, short-circuits for pending background tasks, validates state schema, verifies branch and plan integrity, blocks incomplete tasks, enforces git cleanliness, requires summaries and round contracts, runs Codex summary review, transitions to code review, enters finalize, and optionally enters methodology analysis before terminal state. Key stop-hook stages are active loop lookup at `hooks/loop-codex-stop-hook.sh:64`, background handling at `hooks/loop-codex-stop-hook.sh:83`, schema checks at `hooks/loop-codex-stop-hook.sh:225`, branch check at `hooks/loop-codex-stop-hook.sh:289`, task gate at `hooks/loop-codex-stop-hook.sh:408`, git clean gate at `hooks/loop-codex-stop-hook.sh:680`, summary gate at `hooks/loop-codex-stop-hook.sh:804`, Codex prompt construction at `hooks/loop-codex-stop-hook.sh:1010`, implementation review execution at `hooks/loop-codex-stop-hook.sh:1719`, completion handling at `hooks/loop-codex-stop-hook.sh:1889`, and next-round prompt generation at `hooks/loop-codex-stop-hook.sh:2021`. PreToolUse validators enforce file-scoped behavior: Read blocks stale round artifacts and methodology-phase project reads (`hooks/loop-read-validator.sh:56`, `hooks/loop-read-validator.sh:86`, `hooks/loop-read-validator.sh:286`); Write blocks todos, prompts, state files, plan backups, and immutable goal-tracker edits (`hooks/loop-write-validator.sh:57`, `hooks/loop-write-validator.sh:202`, `hooks/loop-write-validator.sh:240`, `hooks/loop-write-validator.sh:253`); Edit mirrors the same restrictions for literal edits (`hooks/loop-edit-validator.sh:40`, `hooks/loop-edit-validator.sh:166`, `hooks/loop-edit-validator.sh:204`); Bash blocks shell bypasses against state, prompt, summary, contract, goal-tracker, plan-backup, and hook scripts (`hooks/loop-bash-validator.sh:81`, `hooks/loop-bash-validator.sh:199`, `hooks/loop-bash-validator.sh:271`, `hooks/loop-bash-validator.sh:496`, `hooks/loop-bash-validator.sh:561`).
- inputs_outputs_state: Inputs are hook JSON on stdin, `session_id`, `transcript_path`, active project root, `.humanize/rlcr/<timestamp>` loop directories, `state.md` or phase-specific state files, round summaries/contracts, goal tracker, git state, Codex CLI output, merged Humanize config, and templates. Outputs are either allow-by-exit, block JSON, stderr block messages, loop files, review prompts/results, cache logs under `XDG_CACHE_HOME`, phase marker files, and renamed terminal state files. State transitions include `state.md` to review mode via `review_started=true` and `.review-phase-started` at `hooks/loop-codex-stop-hook.sh:1918`, `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:1349`, `finalize-state.md` to terminal complete at `hooks/loop-codex-stop-hook.sh:995`, `state.md` to `methodology-analysis-state.md` at `hooks/lib/methodology-analysis.sh:64`, and terminal rename after methodology completion at `hooks/loop-codex-stop-hook.sh:657`.
- gates_or_invariants: The shared library validates JSON, rejects deep nesting, extracts session ids, resolves only the newest active loop per session, and applies strict state parsing (`hooks/lib/loop-common.sh:91`, `hooks/lib/loop-common.sh:151`, `hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:533`). State files are hook-owned and cannot be modified by tool calls (`hooks/lib/loop-common.sh:864`, `hooks/lib/loop-common.sh:873`, `hooks/lib/loop-common.sh:1017`). Goal tracker immutable sections must remain unchanged after round 0 (`hooks/lib/loop-common.sh:954`). Plan backups in loop dirs are read-only (`hooks/loop-write-validator.sh:240`, `hooks/loop-edit-validator.sh:190`). Git branch must not change during a loop (`hooks/loop-plan-file-validator.sh:96`, `hooks/loop-codex-stop-hook.sh:306`). Git state must be clean before review/exit, excluding untracked local Humanize runtime artifacts (`hooks/loop-codex-stop-hook.sh:699`). Codex review failures are blocking, not skipped (`hooks/loop-codex-stop-hook.sh:1318`, `hooks/loop-codex-stop-hook.sh:1610`). Direct execution of hook scripts is blocked during active loops (`hooks/loop-bash-validator.sh:184`).
- dependencies_and_callers: `hooks/hooks.json` is consumed by Claude Code plugin hook installation. Validators source `hooks/lib/loop-common.sh`, which sources `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, config loader, and `hooks/lib/loop-bg-tasks.sh`. The stop hook additionally sources `scripts/portable-timeout.sh` and `hooks/lib/methodology-analysis.sh`. `hooks/check-todos-from-transcript.py` is called by the stop hook at `hooks/loop-codex-stop-hook.sh:408`. `hooks/loop-post-bash-hook.sh` depends on `.humanize/.pending-session-id` from setup and writes the `session_id` into state (`hooks/loop-post-bash-hook.sh:9`, `hooks/loop-post-bash-hook.sh:180`). Prompt-template blocks are loaded through `load_and_render_safe` for user-facing block reasons.
- edge_cases_or_failure_modes: The directory explicitly handles malformed JSON, invalid UTF-8, deeply nested JSON, symlink/path traversal hazards, stale or foreign background-task markers, stale git `index.lock`, outdated state schema, missing Codex CLI, missing/empty Codex output, full-alignment STOP, mainline drift, missing methodology analysis artifacts, and tracked `.humanize` state. Symlink-sensitive comparisons deliberately canonicalize parents but not leaf names to avoid approving symlink escapes (`hooks/lib/project-root.sh:78`, `hooks/loop-write-validator.sh:345`, `hooks/loop-read-validator.sh:307`). Background tasks fail open on uncertain liveness, but stale marker cleanup requires a readable transcript with no pending ids (`hooks/lib/loop-bg-tasks.sh:120`, `hooks/lib/loop-bg-tasks.sh:428`). `loop-edit-validator.sh` does not call the shared `validate_hook_input` helper before its first `jq` parse, so invalid Edit hook JSON is handled by shell/jq failure rather than the same normalized error path used by Read/Write/Bash.
- validation_or_tests: `tests/robustness/test-hook-input-robustness.sh` directly pipes JSON into production read/write/bash validators and tests malformed input, nesting, non-UTF8, command parsing, and monitor edge cases. `tests/test-worker-result-contract.sh` validates worker prompt contracts that the hook ecosystem relies on. Hook behavior is also indirectly exercised by setup/start/cancel workflows outside this assigned set.
- skip_candidate: `no`

### DEV-HZ-036 `file` `commands/refine-plan.md`
- cursor: `[_]`
- core_role: This command defines the planning-only refinement workflow for annotated generated plans. It is algorithmic because it specifies a deterministic phase order, comment scanner, classification model, schema-preserving refinement rules, QA ledger generation, and atomic write transaction for plan/RLCR inputs.
- algorithmic_behavior: The command is a strict phase machine. It forbids implementation work and limits writes to refined plan outputs, QA docs, and optional translated variants (`commands/refine-plan.md:19`). Phase 0 parses `--input`, optional `--output`, optional `--qa-dir`, optional `--alt-language`, and mutually exclusive `--discussion` or `--direct` (`commands/refine-plan.md:46`). Phase 0.5 resolves config using `config-loader.sh` semantics and normalizes language variants (`commands/refine-plan.md:86`, `commands/refine-plan.md:126`). Phase 1 delegates IO/schema/comment validation to `validate-refine-plan-io.sh` with explicit exit-code handling (`commands/refine-plan.md:157`). Phase 2 requires a stateful scanner that ignores fenced code and HTML comments while extracting `CMT:`, `<cmt>`, and `<comment>` blocks (`commands/refine-plan.md:184`). Phase 3 classifies comments into question, change request, or research request (`commands/refine-plan.md:276`). Phase 4 processes each comment in document order, optionally asks the user in discussion mode, and updates pending decisions/convergence status (`commands/refine-plan.md:332`). Phase 5 produces the refined plan while preserving required gen-plan sections (`commands/refine-plan.md:395`). Phase 6 writes a QA ledger with one row per raw comment (`commands/refine-plan.md:457`). Phase 7 performs temp-file based atomic finalization (`commands/refine-plan.md:508`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, an annotated gen-plan-format plan, merged config values `alternative_plan_language` and `gen_plan_mode`, comment blocks, user answers in discussion mode, and targeted repository reads for research requests. Outputs are `REFINED_PLAN_TEXT`, a QA document under `QA_DIR`, and optional language variants named by inserting `_<code>` before the extension (`commands/refine-plan.md:521`). Internal state includes `ORIGINAL_PLAN_TEXT`, extracted comment records, `PLAN_WITH_COMMENTS_REMOVED`, classifications, dispositions, pending decisions, main language, variant language, and convergence status (`converged` or `partially_converged`).
- gates_or_invariants: The refined plan must reuse the existing gen-plan schema and must not invent top-level sections (`commands/refine-plan.md:28`). The scanner must preserve surrounding non-comment text, assign IDs only to non-empty blocks, and stop on nested, stray, or unterminated comments (`commands/refine-plan.md:223`, `commands/refine-plan.md:244`). Required sections include Goal Description, Acceptance Criteria, Path Boundaries, Feasibility Hints, Dependencies, Task Breakdown, Claude-Codex Deliberation, Pending User Decisions, and Implementation Notes (`commands/refine-plan.md:401`). Before QA generation it must verify no comment markers remain, AC/task references are valid, task tags are `coding` or `analyze`, and pending decisions match convergence state (`commands/refine-plan.md:442`). Atomic writes must prepare all content before replacing destinations and must replace auxiliary outputs before an in-place main plan (`commands/refine-plan.md:546`).
- dependencies_and_callers: It depends on `${CLAUDE_PLUGIN_ROOT}/scripts/validate-refine-plan-io.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/lib/config-loader.sh`, `${CLAUDE_PLUGIN_ROOT}/prompt-template/plan/refine-plan-qa-template.md`, the gen-plan schema/template, and Claude tools Read/Glob/Grep/Write/Edit/AskUserQuestion as declared in frontmatter (`commands/refine-plan.md:4`). It is a slash-command workflow likely called after a plan has been annotated with comments.
- edge_cases_or_failure_modes: Invalid args, missing input, empty input, no comment blocks, missing gen-plan sections, unwritable output or QA dirs, unsupported CLI language, unsupported config language, mixed-language ambiguity, unresolved user decisions, parse errors with exact line/column context, temp write failure, translation failure, and partial finalization risk are all explicitly handled. Direct mode makes minimal safe assumptions and records them rather than blocking for every ambiguity (`commands/refine-plan.md:603`).
- validation_or_tests: The command delegates preflight to `validate-refine-plan-io.sh` and includes its exit-code contract at `commands/refine-plan.md:165`. Related tests are likely under refine-plan tests outside this assigned set; this assigned file itself is a workflow specification, not an executable validator.
- skip_candidate: `no`

### DEV-HZ-066 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: This script is the concrete preflight validator for the gen-plan command. It normalizes and validates draft input/output arguments before plan generation and guards against overwriting outputs or running with a missing plan template.
- algorithmic_behavior: It parses CLI flags in a `while`/`case` loop (`scripts/validate-gen-plan-io.sh:37`), accepts `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, `--direct`, and `--coach`, rejects unknown flags, and treats help as usage exit code 6 (`scripts/validate-gen-plan-io.sh:16`). It enforces mutual exclusion between discussion and direct modes (`scripts/validate-gen-plan-io.sh:82`), requires both input and output (`scripts/validate-gen-plan-io.sh:88`), normalizes paths with `realpath -m` when available (`scripts/validate-gen-plan-io.sh:104`), then runs ordered checks for input existence, non-empty input, output directory existence, output path not being a directory, output path not already existing, and output directory writability (`scripts/validate-gen-plan-io.sh:114`). It prints validation metadata and checks for the gen-plan template through `CLAUDE_PLUGIN_ROOT` or a script-relative fallback (`scripts/validate-gen-plan-io.sh:169`).
- inputs_outputs_state: Inputs are command-line flags, the filesystem, optional `CLAUDE_PLUGIN_ROOT`, and the expected template at `prompt-template/plan/gen-plan-template.md`. Outputs are stdout/stderr validation messages, a `TEMPLATE_FILE:` line on success, and exit codes 0 through 7. It has no persistent state and performs no writes.
- gates_or_invariants: The output must be a new file path, not an existing file or directory (`scripts/validate-gen-plan-io.sh:138`). Output dir must exist and be writable (`scripts/validate-gen-plan-io.sh:130`, `scripts/validate-gen-plan-io.sh:153`). Input must exist and be non-empty (`scripts/validate-gen-plan-io.sh:114`, `scripts/validate-gen-plan-io.sh:122`). `--discussion` and `--direct` cannot both be set (`scripts/validate-gen-plan-io.sh:82`). Direct mode plus auto-start is allowed but produces a note that auto-start will be skipped (`scripts/validate-gen-plan-io.sh:99`).
- dependencies_and_callers: Called by `commands/gen-plan.md` via allowed-tools and by `tests/test-gen-plan.sh` script tests. It depends on shell utilities `realpath`, `dirname`, `wc`, and filesystem permissions. Template lookup depends on plugin layout.
- edge_cases_or_failure_modes: Exit 6 covers missing flag values, unknown flags, missing required args, help, and mutual-exclusive mode flags. Exit 7 covers missing template and is a plugin configuration error. The script prints `VALIDATION_SUCCESS` before checking template existence (`scripts/validate-gen-plan-io.sh:161`, `scripts/validate-gen-plan-io.sh:178`), so a missing template can produce success-looking output followed by exit 7. On systems without GNU `realpath -m`, it falls back to the original path string, which is acceptable but less normalized.
- validation_or_tests: `tests/test-gen-plan.sh` validates this script’s exit codes for missing flag values, unknown flags, nonexistent input, empty input, missing output dir, existing output, output directory target, valid paths, auto-start flag, coach flag, deprecated `--learning-mode`, discussion/direct recognition, mutual exclusion, and help handling (`tests/test-gen-plan.sh:778`).
- skip_candidate: `no`

### DEV-HZ-096 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: This is an executable specification for gen-plan command structure, plan-template invariants, convergence semantics, coach-mode gates, validator IO behavior, and related review-template contracts.
- algorithmic_behavior: It sets project paths, accumulates pass/fail counts, and runs a long sequence of grep/awk-based assertions (`tests/test-gen-plan.sh:11`, `tests/test-gen-plan.sh:25`). Positive tests verify `commands/gen-plan.md` frontmatter, allowed tools, argument hints, the draft relevance agent, Claude/Codex deliberation workflow, auto-start gating, coach-mode quiz requirements, direct-mode partial convergence, phase ordering, plan template sections, task tag requirements, deferred/future-work semantics, DEC/FUT linkage, and review template treatment of future items (`tests/test-gen-plan.sh:54`, `tests/test-gen-plan.sh:119`, `tests/test-gen-plan.sh:235`, `tests/test-gen-plan.sh:287`, `tests/test-gen-plan.sh:372`, `tests/test-gen-plan.sh:430`). Negative tests synthesize invalid command/agent fixtures to validate naming, frontmatter, YAML syntax, and model-name checks (`tests/test-gen-plan.sh:535`). Script tests execute `validate-gen-plan-io.sh` under many valid and invalid inputs (`tests/test-gen-plan.sh:778`). The final test requires the markdown Plan Structure block embedded in gen-plan.md to exactly match `prompt-template/plan/gen-plan-template.md` (`tests/test-gen-plan.sh:948`).
- inputs_outputs_state: Inputs are repository files under `commands`, `agents`, `.claude-plugin`, `README.md`, prompt templates, `scripts/validate-gen-plan-io.sh`, and temporary fixtures. Outputs are colored PASS/FAIL lines, counters, and process exit 0 only when all tests pass (`tests/test-gen-plan.sh:958`). Runtime state is local shell counters plus temporary directories.
- gates_or_invariants: Gen-plan must expose `--auto-start-rlcr-if-converged` and `--coach`, must not expose deprecated `--learning-mode`, must require ultrathink, must include Pending User Decisions, Codex first-pass, iterative convergence, max three rounds, and coding/analyze task tags (`tests/test-gen-plan.sh:133`, `tests/test-gen-plan.sh:145`, `tests/test-gen-plan.sh:247`, `tests/test-gen-plan.sh:253`, `tests/test-gen-plan.sh:259`, `tests/test-gen-plan.sh:271`, `tests/test-gen-plan.sh:323`). Direct mode must produce `PLAN_CONVERGENCE_STATUS=partially_converged` and auto-start requires discussion mode (`tests/test-gen-plan.sh:235`, `tests/test-gen-plan.sh:241`). Future work must be represented as FUT items rather than current completion gates, and deferred status must not be used as a convergence escape hatch (`tests/test-gen-plan.sh:372`, `tests/test-gen-plan.sh:406`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `prompt-template/plan/gen-plan-template.md`, `prompt-template/codex/regular-review.md`, `prompt-template/codex/full-alignment-review.md`, plugin JSON, marketplace JSON, README, and `scripts/validate-gen-plan-io.sh`. It is intended for CI or local validation of planning/RLCR command contracts.
- edge_cases_or_failure_modes: The test is intentionally structural and string-based, so it can miss semantic regressions if required strings are present in the wrong operational context. The first fixture cleanup trap is overwritten by the later script-test cleanup trap (`tests/test-gen-plan.sh:545`, `tests/test-gen-plan.sh:789`), which can leave the earlier temp fixture directory behind. The Emoji/CJK checks use `grep -P`; if PCRE is unavailable, the condition behaves like no match and can incorrectly pass the English-only check (`tests/test-gen-plan.sh:763`). The exact plan-template equality check is strict and will fail on harmless whitespace changes.
- validation_or_tests: This file is itself the validation surface. It was inspected, not executed, to keep this branch export research-only and avoid temp-output side effects.
- skip_candidate: `no`

### DEV-HZ-126 `file` `tests/test-worker-result-contract.sh`
- cursor: `[_]`
- core_role: This is an executable contract test for the explore-idea worker prompt result protocol. It ensures spawned workers emit machine-parseable result JSON between sentinels and are constrained from unsafe repository operations.
- algorithmic_behavior: The script locates `prompt-template/explore/worker-prompt.md`, verifies existence, checks result sentinel markers and their order, validates required placeholder variables, validates required JSON field names, then checks hard constraints such as untrusted direction data placement, no untrusted direction-name interpolation in bash snippets, no nested skills/slash commands, no remote push, `CLAUDE_PROJECT_DIR` scoping, explicit Codex review model placeholder, and branch naming format (`tests/test-worker-result-contract.sh:19`, `tests/test-worker-result-contract.sh:40`, `tests/test-worker-result-contract.sh:67`, `tests/test-worker-result-contract.sh:97`, `tests/test-worker-result-contract.sh:136`).
- inputs_outputs_state: Input is the worker prompt template plus `tests/test-helpers.sh`. Output is PASS/FAIL test logging and summary exit status. State is limited to pass/fail counters from the helper.
- gates_or_invariants: Worker output must include `=== EXPLORE_RESULT_JSON_BEGIN ===` and `=== EXPLORE_RESULT_JSON_END ===` in order (`tests/test-worker-result-contract.sh:40`, `tests/test-worker-result-contract.sh:54`). Required result JSON fields include schema version, run/direction identifiers, task status, Codex review metadata and verdict, test results, worktree/branch/commit details, dirty state, commit status, summary, lesson fields, and error (`tests/test-worker-result-contract.sh:97`). Hard constraints must precede untrusted direction data and explicitly forbid remote push (`tests/test-worker-result-contract.sh:143`, `tests/test-worker-result-contract.sh:169`).
- dependencies_and_callers: Depends on `prompt-template/explore/worker-prompt.md` and `tests/test-helpers.sh`. It supports explore-idea style worker orchestration and downstream result parsers that rely on the sentinel-delimited JSON contract.
- edge_cases_or_failure_modes: The test checks presence of strings rather than parsing the JSON example or rendering the template with real values. Sentinel order uses only the first occurrence of each marker (`tests/test-worker-result-contract.sh:55`). Bash snippet safety checks only whether `<DIRECTION_NAME>` appears inside fenced bash blocks, not every possible untrusted placeholder (`tests/test-worker-result-contract.sh:154`).
- validation_or_tests: This file is itself the validation suite for the worker result contract. It was inspected, not executed.
- skip_candidate: `no`

### DEV-HZ-156 `file` `prompt-template/block/methodology-analysis-state-file-modification.md`
- cursor: `[_]`
- core_role: This is a block-message template used when a tool or bash command attempts to modify `methodology-analysis-state.md`, which is owned by the loop system during the Methodology Analysis Phase.
- algorithmic_behavior: The template tells the agent the state file cannot be modified, explains that methodology analysis runs before loop exit, and redirects allowed work toward spawning an Opus analysis agent, reviewing the sanitized report, optionally helping file an issue, and writing `methodology-analysis-done.md` (`prompt-template/block/methodology-analysis-state-file-modification.md:1`). It is loaded by `methodology_analysis_state_file_blocked_message` in `hooks/lib/loop-common.sh:1017` and emitted by Write/Edit/Bash validators when they detect methodology-analysis state modification attempts.
- inputs_outputs_state: Inputs are none at template level. Output is static markdown block text. The protected state is `methodology-analysis-state.md`; the allowed completion marker is `methodology-analysis-done.md`.
- gates_or_invariants: The invariant is that methodology-analysis state is hook-managed, not agent-editable. Completion must be signaled through a separate done marker, not by editing the active state file.
- dependencies_and_callers: Called by shared hook rendering through `load_and_render_safe`, primarily from `hooks/loop-write-validator.sh:202`, `hooks/loop-edit-validator.sh:166`, and `hooks/loop-bash-validator.sh:271` via the shared message function.
- edge_cases_or_failure_modes: This template has no variables, so rendering cannot fail due to missing substitutions. If the template is missing, callers fall back to an inline message in `hooks/lib/loop-common.sh:1019`. It is a guard message, not enforcement by itself.
- validation_or_tests: The methodology-state block is indirectly covered by hook robustness and loop validator tests that check state-file protection behavior. Direct inspection confirms the message aligns with the methodology-analysis library’s required completion marker.
- skip_candidate: `no`

### DEV-HZ-186 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: This prompt block is appended after a Full Goal Alignment Check to bias the next implementation round toward Codex-identified alignment gaps, unmet acceptance criteria, unjustified deferrals, and queued-issue discipline.
- algorithmic_behavior: It is loaded only when the stop hook has just run a full alignment check (`hooks/loop-codex-stop-hook.sh:2176`). The block instructs the next Claude round to address forgotten items, prioritize unmet ACs, un-defer unjustified deferrals, and keep non-blocking follow-up work queued (`prompt-template/claude/post-alignment-action-items.md:2`).
- inputs_outputs_state: Inputs are none in the template itself. Output is static markdown appended to the generated next-round prompt. It affects next-round behavior but does not write state directly.
- gates_or_invariants: It preserves the mainline/queued split by telling the agent not to pull non-blocking follow-up work into the mainline unless it clearly blocks progress. It also reinforces that AC failures and forgotten work from alignment review are higher priority than optional queued work.
- dependencies_and_callers: The only direct caller in inspected code is `hooks/loop-codex-stop-hook.sh:2176`, after prompt generation and before footer/task-routing additions. It depends on the stop hook’s full-alignment scheduling logic at `hooks/loop-codex-stop-hook.sh:1022`.
- edge_cases_or_failure_modes: The template has no variables and no enforcement mechanism by itself. If it is absent, the stop hook simply skips appending it (`hooks/loop-codex-stop-hook.sh:2178`). Because it is appended text, its priority depends on the surrounding prompt context and model compliance.
- validation_or_tests: `tests/test-gen-plan.sh` validates full-alignment review template behavior and related FUT/deferral semantics, while this specific block is indirectly covered by stop-hook prompt assembly.
- skip_candidate: `no`

### DEV-HZ-216 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: This is an executable robustness specification for hook input parsing, command modification detection, monitor helper integration, and monitor terminal/log edge cases. It validates the defensive surface around the hook directory’s core state machine.
- algorithmic_behavior: It sets up a test dir, sources `hooks/lib/loop-common.sh`, then pipes JSON into production hook validators. Tests 1 through 10c cover valid Read/Write/Bash inputs, invalid JSON, missing tool names, missing required file paths, extremely long commands, special shell characters, Unicode paths, unknown tools, deeply nested JSON, non-UTF8 bytes, and null-byte handling (`tests/robustness/test-hook-input-robustness.sh:35`, `tests/robustness/test-hook-input-robustness.sh:71`, `tests/robustness/test-hook-input-robustness.sh:117`, `tests/robustness/test-hook-input-robustness.sh:177`, `tests/robustness/test-hook-input-robustness.sh:201`). Tests 11 through 18 cover terminal widths, log growth/deletion, ANSI stripping, binary logs, and rapid updates (`tests/robustness/test-hook-input-robustness.sh:245`). Tests 19 and 20 assert `command_modifies_file` detects `sed -i` and redirects (`tests/robustness/test-hook-input-robustness.sh:351`). Tests 21 through 26 source `scripts/humanize.sh` and invoke production monitor functions under fake projects, cache dirs, terminal shims, and auto-deleted session dirs (`tests/robustness/test-hook-input-robustness.sh:381`, `tests/robustness/test-hook-input-robustness.sh:444`, `tests/robustness/test-hook-input-robustness.sh:488`, `tests/robustness/test-hook-input-robustness.sh:580`).
- inputs_outputs_state: Inputs are the repository root, production hook scripts, `loop-common.sh`, `scripts/humanize.sh`, temp project directories, fake HOME/cache dirs, synthetic `.humanize/rlcr` state files, synthetic goal trackers, and generated logs. Outputs are PASS/FAIL lines and a test summary. Runtime state includes temp dirs and background monitor/delete processes.
- gates_or_invariants: Hook validators should reject malformed JSON and deep nesting without signal crashes, reject non-UTF8 with validation failure, accept valid UTF-8 Unicode paths, ignore unknown tools, and handle very long command strings. Monitor helpers must return pipe-delimited parser output and not crash under missing sessions, narrow terminals, or ANSI-coded logs. Command mutation detection must identify common file-write patterns.
- dependencies_and_callers: Depends on `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, and `scripts/humanize.sh`. It exercises production functions rather than mocks for the core hook parsing path.
- edge_cases_or_failure_modes: The null-byte case documents a shell limitation: Bash strips null bytes before the hook can inspect them, so the assertion only requires no signal crash (`tests/robustness/test-hook-input-robustness.sh:219`). It does not test `loop-edit-validator.sh` malformed JSON behavior. Monitor tests use background deletion and time waits, so they may be timing-sensitive on very slow systems (`tests/robustness/test-hook-input-robustness.sh:531`). Sourcing `scripts/humanize.sh` mutates the shell environment for later tests, which is intended but broad.
- validation_or_tests: This file is itself the robustness test suite. It was inspected, not executed, because the branch export is research-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8/8 item evidence sections present; each assigned section heading is unique`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`