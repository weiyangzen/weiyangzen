# agent_28 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-028 `file` `agents/bitlesson-selector.md`
- cursor: `[_]`
- core_role: Defines the deterministic agent prompt/policy for choosing which BitLesson knowledge-base entries apply to one sub-task. It is part of the RLCR execution discipline because later implementation prompts require `bitlesson-selector` to run per task/sub-task.
- algorithmic_behavior: The prompt narrows selection to direct relevance only, prefers precision over recall, and requires `NONE` when no lesson applies. Its stable output contract is exactly two lines: `LESSON_IDS:` and `RATIONALE:` with no extra sections (`agents/bitlesson-selector.md:26`, `agents/bitlesson-selector.md:32`).
- inputs_outputs_state: Inputs are current sub-task description, related file paths, and project BitLesson content, normally from `.humanize/bitlesson.md` (`agents/bitlesson-selector.md:12`). Output is a parseable lesson-id list or `NONE` plus one concise rationale (`agents/bitlesson-selector.md:34`). The prompt itself does not mutate state; it influences later work by selecting lessons consumed by Claude and reviewable by Codex (`agents/bitlesson-selector.md:21`).
- gates_or_invariants: Invariant is deterministic, minimal output: no weak matches, no extra sections, exact line labels. Downstream parser enforcement appears in `scripts/bitlesson-select.sh`, which extracts `LESSON_IDS` and `RATIONALE` and fails if either is missing (`scripts/bitlesson-select.sh:252`, `scripts/bitlesson-select.sh:268`).
- dependencies_and_callers: Runtime execution is explicitly delegated to `scripts/bitlesson-select.sh`, routing to Codex for `gpt-*` models and Claude for `haiku`, `sonnet`, or `opus` based on `bitlesson_model` (`agents/bitlesson-selector.md:19`). Related callers/prompts require use before tasks: `prompt-template/claude/next-round-prompt.md` and `hooks/loop-codex-stop-hook.sh:2053`.
- edge_cases_or_failure_modes: Empty/no-applicable lesson selection must return `NONE`; over-selection is a prompt-level failure because the policy says precision over recall. If the model emits code fences, prose, or omits either required line, `bitlesson-select.sh` rejects the output as unexpected format (`scripts/bitlesson-select.sh:268`). If the BitLesson file has no recorded `## Lesson:` entries, the runtime bypasses model selection and emits `LESSON_IDS: NONE` with rationale (`scripts/bitlesson-select.sh:93`).
- validation_or_tests: Covered indirectly by `tests/test-bitlesson-select-routing.sh`, which validates provider routing and parseable `LESSON_IDS` output; the assigned prompt is also mirrored in the runtime prompt construction at `scripts/bitlesson-select.sh:147`.
- skip_candidate: `no`

### DEV-HZ-058 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Provides the sourced `humanize` shell function and monitor command surface. Its algorithmic role is state/log discovery, RLCR/skill monitor rendering, web dashboard launch routing, and lightweight parsing of RLCR state, goal tracker, and git status. The core stop/review algorithm lives mostly in hooks, but this file is the runtime user-facing monitor/router.
- algorithmic_behavior: On load, it sources monitor/common helpers and loop constants from `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh` (`scripts/humanize.sh:6`, `scripts/humanize.sh:12`). It defines parsers for pipe-delimited output, goal tracker counts, git repository state, and porcelain git status (`scripts/humanize.sh:24`, `scripts/humanize.sh:71`, `scripts/humanize.sh:158`, `scripts/humanize.sh:208`). `_humanize_monitor_codex` selects the latest `.humanize/rlcr` session, finds the newest run/review log under the project cache, parses state frontmatter, renders a fixed status bar, tails incremental log bytes, and switches sessions/logs as newer artifacts appear (`scripts/humanize.sh:261`, `scripts/humanize.sh:291`, `scripts/humanize.sh:383`, `scripts/humanize.sh:415`, `scripts/humanize.sh:838`).
- inputs_outputs_state: Inputs include `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `.review-phase-started`, git status, cached Codex log files, `.humanize/skill` records via sourced `monitor-skill.sh`, and web flags like `--project`, `--host`, `--port`, `--auth-token`, `--daemon` (`scripts/humanize.sh:266`, `scripts/humanize.sh:421`, `scripts/humanize.sh:542`, `scripts/humanize.sh:1203`). Outputs are terminal monitor UI, error messages, and child process launches. State mutation is limited to web monitor dependency setup: it may create/update `.humanize/viz-venv` and `.requirements_installed` before launching the dashboard (`scripts/humanize.sh:1244`, `scripts/humanize.sh:1265`).
- gates_or_invariants: RLCR monitor refuses to run outside a project with `.humanize/rlcr` and exits when no sessions exist (`scripts/humanize.sh:272`, `scripts/humanize.sh:803`). Log ordering invariant: if any review log round is not greater than all run log rounds, `_find_latest_codex_log` returns an error because run/review chronology is inconsistent (`scripts/humanize.sh:364`). Terminal invariants include minimum height, scroll-region restoration, and signal cleanup (`scripts/humanize.sh:663`, `scripts/humanize.sh:738`, `scripts/humanize.sh:784`). Web monitor requires `.humanize/` to exist, resolves a project directory, finds or creates a venv, chooses an open port in 18000-18099, and warns when binding non-localhost without an auth token (`scripts/humanize.sh:1231`, `scripts/humanize.sh:1235`, `scripts/humanize.sh:1290`, `scripts/humanize.sh:1320`).
- dependencies_and_callers: Depends on `monitor_find_latest_session`, `monitor_get_file_size`, and `monitor_find_state_file` from `scripts/lib/monitor-common.sh` (`scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`), and `_humanize_monitor_skill` from `scripts/lib/monitor-skill.sh` (`scripts/humanize.sh:1408`, `scripts/lib/monitor-skill.sh:18`). It also expects `viz/server/app.py`, `viz/server/requirements.txt`, and `viz/scripts/viz-start.sh` for web dashboard mode (`scripts/humanize.sh:1241`, `scripts/humanize.sh:1251`). The public dispatcher maps `humanize monitor rlcr|skill|codex|gemini|web` to the relevant monitor function (`scripts/humanize.sh:1351`).
- edge_cases_or_failure_modes: Handles missing loop dir, missing session, missing log, terminal resize, too-small terminal, deleted session/log, log truncation/rotation, and switching to newer sessions (`scripts/humanize.sh:842`, `scripts/humanize.sh:884`, `scripts/humanize.sh:1081`, `scripts/humanize.sh:1099`, `scripts/humanize.sh:1134`). Cross-platform weak spots are mitigated but present: `date -d` falls back to raw timestamp on platforms without GNU date (`scripts/humanize.sh:481`), and `grep -oP` for build round is non-portable but failure is suppressed (`scripts/humanize.sh:544`). Web mode can fail on venv creation, pip install, missing dashboard launcher, or exhausted port range (`scripts/humanize.sh:1252`, `scripts/humanize.sh:1267`, `scripts/humanize.sh:1279`, `scripts/humanize.sh:1314`).
- validation_or_tests: This file is a runtime shell utility rather than a narrow unit target. Its behavior is supported by monitor helper design and surrounding tests for hooks/state, but direct validation would require live `.humanize/rlcr` or `.humanize/skill` fixtures plus terminal/web launch checks. No tests were executed in this read-only research pass.
- skip_candidate: `no`

### DEV-HZ-088 `file` `tests/test-directions-json-schema.sh`
- cursor: `[_]`
- core_role: Executable specification for the `directions.json` schema version 1 contract used by idea/direction generation. It verifies that the validator accepts the canonical fixture and rejects malformed direction sets.
- algorithmic_behavior: The test sources shared helpers, points at `scripts/validate-directions-json.sh`, requires `jq`, creates mutated fixtures from `tests/fixtures/directions/valid.directions.json`, and runs the validator on each fixture (`tests/test-directions-json-schema.sh:10`, `tests/test-directions-json-schema.sh:14`, `tests/test-directions-json-schema.sh:22`, `tests/test-directions-json-schema.sh:30`, `tests/test-directions-json-schema.sh:39`). It has one positive case and 34 negative cases that deliberately break schema fields, uniqueness, counts, types, ID derivation, ordering, and metadata consistency (`tests/test-directions-json-schema.sh:46`, `tests/test-directions-json-schema.sh:59`, `tests/test-directions-json-schema.sh:295`).
- inputs_outputs_state: Inputs are the valid directions fixture and jq mutation expressions. Output is pass/fail summary through shared `pass`, `fail`, and `print_test_summary` helpers (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:58`). Temporary mutated fixtures are written under `TEST_DIR` created by `mktemp -d` and removed on exit (`tests/test-helpers.sh:86`).
- gates_or_invariants: The tested validator enforces schema version `1`, required top-level string keys, `directions` length 1 through 10, exactly one boolean `is_primary`, unique safe `direction_id`, unique lowercase `dir_slug`, integer bounded unique `source_index`, derived ID format `dir-<source_index>-<dir_slug>`, sequential `display_order`, metadata count consistency, timestamp format, confidence enum, and required per-direction string/array fields (`scripts/validate-directions-json.sh:40`, `scripts/validate-directions-json.sh:51`, `scripts/validate-directions-json.sh:61`, `scripts/validate-directions-json.sh:66`, `scripts/validate-directions-json.sh:70`, `scripts/validate-directions-json.sh:85`, `scripts/validate-directions-json.sh:89`, `scripts/validate-directions-json.sh:100`, `scripts/validate-directions-json.sh:103`).
- dependencies_and_callers: Depends on `jq`, `scripts/validate-directions-json.sh`, shared `tests/test-helpers.sh`, and the valid fixture. It is likely invoked from the repository aggregate test runner; it also documents AC-3 coverage in its header (`tests/test-directions-json-schema.sh:3`).
- edge_cases_or_failure_modes: If `jq` is unavailable, the test exits 0 with a skip message, so CI without jq will not enforce this schema (`tests/test-directions-json-schema.sh:22`). Because `set -euo pipefail` is enabled, a broken jq mutation or missing fixture aborts early (`tests/test-directions-json-schema.sh:8`). Negative tests only assert non-zero exit, not the specific validator exit code or error text, so they catch rejection but not diagnostic quality.
- validation_or_tests: This file itself is the validation surface. The underlying validator returns `VALIDATION_SUCCESS` on exit 0 and `VALIDATION_FAILED` with exit 3 for schema failures (`scripts/validate-directions-json.sh:115`, `scripts/validate-directions-json.sh:119`).
- skip_candidate: `no`

### DEV-HZ-118 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that validates prompt-template references used by hook validators. Its purpose is to prevent hook blocks from rendering empty/missing messages when a validator blocks a user action.
- algorithmic_behavior: The script scans a fixed list of hook shell scripts for `load_template`, `load_and_render`, or `load_and_render_safe` calls with a quoted `$TEMPLATE_DIR` path, then verifies the referenced markdown template exists (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:66`, `tests/test-template-references.sh:87`, `tests/test-template-references.sh:94`, `tests/test-template-references.sh:100`). It then enumerates all markdown templates, warns if a template is not directly referenced, checks a hard-coded set of common block templates, and ensures critical read/write/edit validators use `load_and_render_safe` rather than unsafe rendering (`tests/test-template-references.sh:119`, `tests/test-template-references.sh:152`, `tests/test-template-references.sh:172`).
- inputs_outputs_state: Inputs are `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, and the entire `prompt-template` markdown tree (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:126`). Outputs are colored pass/fail/warn lines and an exit code: 0 only when `TESTS_FAILED` remains 0 (`tests/test-template-references.sh:221`).
- gates_or_invariants: Missing referenced templates fail the test (`tests/test-template-references.sh:102`). Missing common block templates fail (`tests/test-template-references.sh:161`). Any non-safe `load_and_render` call in critical validators fails (`tests/test-template-references.sh:176`, `tests/test-template-references.sh:196`). Unreferenced templates are warnings only and do not fail (`tests/test-template-references.sh:139`).
- dependencies_and_callers: Uses `find`, `grep`, `sed`, Bash arrays, and direct hook/template paths. It validates the template-loader contract implemented in `hooks/lib/template-loader.sh`, especially safe rendering with fallback.
- edge_cases_or_failure_modes: It uses `set -uo pipefail` but not `set -e`, intentionally accumulating failures instead of aborting (`tests/test-template-references.sh:12`). The reference extractor only recognizes quoted literal template paths after `"$TEMPLATE_DIR"`; dynamically constructed paths may be missed and then only show as unreferenced warnings. Missing scripts are warnings, not hard failures, so a deleted critical script could avoid some reference checks (`tests/test-template-references.sh:74`).
- validation_or_tests: The final gate prints missing templates explicitly and exits 1 if any fail count exists (`tests/test-template-references.sh:213`, `tests/test-template-references.sh:225`). No external fixture is needed beyond the repository tree.
- skip_candidate: `no`

### DEV-HZ-148 `file` `prompt-template/block/git-tracked-humanize.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that `.humanize/` runtime loop state must not be tracked or staged in git. It supports stop-hook and command-validator safety by giving the user the precise remediation command.
- algorithmic_behavior: The template tells the user tracked/staged `.humanize/` files were detected, classifies them as local loop state, instructs `git rm --cached -r .humanize`, and forbids force-adding or committing RLCR trackers, summaries, contracts, or markers (`prompt-template/block/git-tracked-humanize.md:1`, `prompt-template/block/git-tracked-humanize.md:7`, `prompt-template/block/git-tracked-humanize.md:16`).
- inputs_outputs_state: Inputs are none at template level; it is rendered as static block text. Output is the blocking reason returned by `git_tracked_humanize_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:1447`). The intended state transition is from tracked/staged `.humanize` entries to untracked local state after `git rm --cached -r .humanize`.
- gates_or_invariants: The caller’s predicate `git_has_tracked_humanize_state` uses `git ls-files -- .humanize` and returns success when local Humanize state is tracked or staged (`hooks/lib/loop-common.sh:1423`, `hooks/lib/loop-common.sh:1440`). The invariant intentionally allows untracked `.humanize` but blocks tracked/staged `.humanize` (`hooks/lib/loop-common.sh:1423`).
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/git-tracked-humanize.md"` with a fallback copy in `hooks/lib/loop-common.sh:1470`. Related command blocking for `git add .humanize` is nearby but uses a different template (`hooks/lib/loop-common.sh:1383`).
- edge_cases_or_failure_modes: The detection scope is explicitly `.humanize/`, not similarly named paths such as `.humanize-backup` or `.humanizeconfig` (`hooks/lib/loop-common.sh:1427`). If the template file is missing, the caller has a fallback string, but `tests/test-template-references.sh` should detect the missing file as a template-reference failure. The remediation command can fail when no tracked entries exist, but this message is only intended after tracked/indexed state was detected.
- validation_or_tests: `tests/test-template-references.sh` checks existence of directly referenced block templates. Behavior around `.humanize` path escaping is referenced in comments as covered by `tests/test-humanize-escape.sh` (`hooks/lib/loop-common.sh:1429`).
- skip_candidate: `no`

### DEV-HZ-178 `file` `prompt-template/claude/drift-replan-prompt.md`
- cursor: `[_]`
- core_role: Claude prompt template for the anti-drift recovery transition after Codex judges recent implementation rounds as not advancing the mainline. It converts a normal next-round prompt into a recovery round with a rewritten contract and narrowed objective.
- algorithmic_behavior: It declares drift recovery mode, injects `STALL_COUNT` and `LAST_MAINLINE_VERDICT`, tells the worker not to continue normal issue-clearing, requires re-reading the original plan, goal tracker, recent summaries/reviews, and rewriting the round contract before code (`prompt-template/claude/drift-replan-prompt.md:3`, `prompt-template/claude/drift-replan-prompt.md:7`, `prompt-template/claude/drift-replan-prompt.md:10`, `prompt-template/claude/drift-replan-prompt.md:17`). The recovery contract must identify exactly one recovered mainline objective, 1-2 target ACs, drift root cause, blocking issues, queued out-of-scope issues, and success criteria for an `ADVANCED` verdict (`prompt-template/claude/drift-replan-prompt.md:25`).
- inputs_outputs_state: Inputs are template variables `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, `STALL_COUNT`, and `LAST_MAINLINE_VERDICT`. Output is the next round prompt file rendered by the stop hook when `DRIFT_REPLAN_REQUIRED` is true (`hooks/loop-codex-stop-hook.sh:2079`). Expected state transition is from drift/stall into a recovery round that rewrites `round-<N>-contract.md` before implementation.
- gates_or_invariants: Invariants include exactly one recovered mainline objective, only blocking work that directly unblocks that objective, queued work explicitly out of scope, and no implementation until the recovery contract exists (`prompt-template/claude/drift-replan-prompt.md:25`, `prompt-template/claude/drift-replan-prompt.md:33`, `prompt-template/claude/drift-replan-prompt.md:42`). Goal tracker immutable section must remain unchanged, while mutable drift/stagnation cause and blocking/queued classification must stay aligned with the contract (`prompt-template/claude/drift-replan-prompt.md:55`).
- dependencies_and_callers: The stop hook renders this template instead of the normal next-round prompt when drift replan is required, passing all named variables into `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:2079`, `hooks/loop-codex-stop-hook.sh:2081`). Setup initializes anti-drift state fields `mainline_stall_count`, `last_mainline_verdict`, and `drift_status` in `state.md` (`scripts/setup-rlcr-loop.sh:907`). `scripts/humanize.sh` displays these fields in the RLCR monitor (`scripts/humanize.sh:401`, `scripts/humanize.sh:597`).
- edge_cases_or_failure_modes: If a credible recovered objective cannot be produced, the prompt requires saying so in the summary with concrete blockers rather than pretending progress (`prompt-template/claude/drift-replan-prompt.md:63`). If Task tooling is unavailable in the agent environment, the required TaskCreate/TaskUpdate/TaskList lane tagging cannot be followed literally (`prompt-template/claude/drift-replan-prompt.md:35`). If BitLesson is required and the drift template does not mention `bitlesson-selector`, the stop hook appends a BitLesson selection section after rendering (`hooks/loop-codex-stop-hook.sh:2101`).
- validation_or_tests: Existence and direct references are covered by `tests/test-template-references.sh`. Runtime correctness is enforced by the stop hook’s contract-presence and anti-drift state handling; this file is the instruction surface for that transition.
- skip_candidate: `no`

### DEV-HZ-208 `file` `skills/humanize-rlcr/SKILL-kimi.md`
- cursor: `[_]`
- core_role: Skill-mode instruction file for running Humanize RLCR without native hooks while preserving hook-equivalent enforcement. It defines the canonical operator flow for Kimi/skill use: setup, work round, commit/summary, stop-gate wrapper, and gate-result handling.
- algorithmic_behavior: The skill explicitly forbids reimplementing review logic manually and requires calling `scripts/rlcr-stop-gate.sh`, which executes `hooks/loop-codex-stop-hook.sh` so skill-mode and hook-mode behavior stay aligned (`skills/humanize-rlcr/SKILL-kimi.md:9`, `skills/humanize-rlcr/SKILL-kimi.md:12`, `skills/humanize-rlcr/SKILL-kimi.md:16`). Setup runs `scripts/setup-rlcr-loop.sh` with arguments and stops on non-zero exit (`skills/humanize-rlcr/SKILL-kimi.md:30`). Each round reads the generated prompt, implements, commits, writes the required summary, runs the stop gate, and handles gate exit codes (`skills/humanize-rlcr/SKILL-kimi.md:40`).
- inputs_outputs_state: Inputs are user arguments, generated prompt files under `.humanize/rlcr/<timestamp>/round-<N>-prompt.md` or finalize prompts, environment variables `CLAUDE_SESSION_ID` and `CLAUDE_TRANSCRIPT_PATH`, and runtime root placeholder `{{HUMANIZE_RUNTIME_ROOT}}` (`skills/humanize-rlcr/SKILL-kimi.md:18`, `skills/humanize-rlcr/SKILL-kimi.md:44`, `skills/humanize-rlcr/SKILL-kimi.md:53`). Outputs are commits, `round-<N>-summary.md` or `finalize-summary.md`, and the gate result. Gate exit 0 means allowed to exit, 10 means blocked and must continue, and 20 means infrastructure error that must be reported (`skills/humanize-rlcr/SKILL-kimi.md:60`).
- gates_or_invariants: Enforced gates include state/schema validation, branch consistency, plan integrity, incomplete Task/Todo blocking, git-clean requirement, push-every-round unpushed commit blocking, summary presence, max iteration handling, full-alignment rounds, strict `COMPLETE`/`STOP` markers, review-phase transition guard, code-review markers, Codex review failure/empty-output blocking, and open-question handling (`skills/humanize-rlcr/SKILL-kimi.md:65`). Critical invariants forbid manual `state.md`/`finalize-state.md` edits, skipping a blocked hook result, ad-hoc Codex review in place of hook-managed phase transitions, and using anything except generated loop files as source of truth (`skills/humanize-rlcr/SKILL-kimi.md:84`).
- dependencies_and_callers: Depends on installed/hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, and `hooks/loop-codex-stop-hook.sh`. Options pass through to setup, including plan file, max iterations, Codex model/timeout, base branch, full-review interval, skip implementation, push every round, Claude answering, agent teams, yolo, and quiz skipping (`skills/humanize-rlcr/SKILL-kimi.md:91`). Cancel flow delegates to `scripts/cancel-rlcr-loop.sh` (`skills/humanize-rlcr/SKILL-kimi.md:124`).
- edge_cases_or_failure_modes: If the runtime root placeholder is not hydrated during install, every command path is invalid. If setup exits non-zero, the skill requires stopping rather than continuing (`skills/humanize-rlcr/SKILL-kimi.md:38`). Gate exit 20 is an infrastructure failure and must not be faked as completion (`skills/humanize-rlcr/SKILL-kimi.md:63`). If implementation changes are not committed or summary files are missing, the hook-equivalent gate is expected to block through the normal stop-hook logic.
- validation_or_tests: The skill’s validation strategy is not self-contained; it delegates all behavioral enforcement to `rlcr-stop-gate.sh` and the stop hook. The options table records the expected review-phase Codex model as `gpt-5.5:high` (`skills/humanize-rlcr/SKILL-kimi.md:112`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 assigned item sections above; each assigned scheduler row is represented once as an item header
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`