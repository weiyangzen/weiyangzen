# agent_09 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-009 `directory` `skills`
- cursor: `[_]`
- core_role: The `skills` directory is a recursively inspected workflow-contract surface for Humanize. The inventory contains seven files only: `skills/ask-codex/SKILL.md`, `skills/ask-gemini/SKILL.md`, `skills/humanize/SKILL.md`, `skills/humanize-gen-plan/SKILL.md`, `skills/humanize-refine-plan/SKILL.md`, `skills/humanize-rlcr/SKILL.md`, and `skills/humanize-rlcr/SKILL-kimi.md`.
- algorithmic_behavior: These files do not implement shell logic directly; they define the user-facing flow and command contracts that route into `scripts/` and `hooks/`. `humanize` documents RLCR’s two-phase loop: implementation summary review, then `codex review --base` code review, with severity markers driving remediation or finalize transition (`skills/humanize/SKILL.md:32`, `skills/humanize/SKILL.md:41`, `skills/humanize/SKILL.md:43`, `skills/humanize/SKILL.md:47`). `humanize-rlcr` defines the Codex native Stop-hook sequence: run setup, read generated prompt, implement, commit, write summary, exit so hooks run (`skills/humanize-rlcr/SKILL.md:24`, `skills/humanize-rlcr/SKILL.md:40`, `skills/humanize-rlcr/SKILL.md:47`). `SKILL-kimi.md` defines hook-equivalent skill mode by explicitly calling `scripts/rlcr-stop-gate.sh`, which delegates to `hooks/loop-codex-stop-hook.sh` (`skills/humanize-rlcr/SKILL-kimi.md:9`, `skills/humanize-rlcr/SKILL-kimi.md:13`, `skills/humanize-rlcr/SKILL-kimi.md:16`).
- inputs_outputs_state: Inputs are command arguments, plan paths, model flags, and optional workflow modes. Outputs are loop artifacts under `.humanize/rlcr/`, skill results under `.humanize/skill/`, generated plans, QA ledgers, and external reviewer output. `ask-codex` and `ask-gemini` wrap shell scripts and save outputs to `.humanize/skill/<timestamp>/output.md` (`skills/ask-codex/SKILL.md:19`, `skills/ask-codex/SKILL.md:55`, `skills/ask-gemini/SKILL.md:22`, `skills/ask-gemini/SKILL.md:59`). `humanize-refine-plan` outputs a refined plan, QA ledger, and optional variants (`skills/humanize-refine-plan/SKILL.md:41`, `skills/humanize-refine-plan/SKILL.md:45`, `skills/humanize-refine-plan/SKILL.md:98`).
- gates_or_invariants: The directory encodes major invariants: never manually edit state files, never bypass blocked hook results, and never run ad-hoc review commands in place of hook-managed transitions (`skills/humanize-rlcr/SKILL.md:69`, `skills/humanize-rlcr/SKILL.md:71`, `skills/humanize-rlcr/SKILL.md:73`). Plan generation requires relevance checks, issue resolution, metric confirmation, structured AC output, and post-write review (`skills/humanize-gen-plan/SKILL.md:21`, `skills/humanize-gen-plan/SKILL.md:26`, `skills/humanize-gen-plan/SKILL.md:33`, `skills/humanize-gen-plan/SKILL.md:36`, `skills/humanize-gen-plan/SKILL.md:38`). Refine-plan must preserve required schema sections, remove `CMT:` blocks, restrict routing tags, and write atomically (`skills/humanize-refine-plan/SKILL.md:71`, `skills/humanize-refine-plan/SKILL.md:72`, `skills/humanize-refine-plan/SKILL.md:84`, `skills/humanize-refine-plan/SKILL.md:86`).
- dependencies_and_callers: The skills coordinate with `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/validate-refine-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/ask-gemini.sh`, and Stop hooks. The `allowed-tools` metadata on one-shot skills constrains shell entrypoints (`skills/ask-codex/SKILL.md:5`, `skills/ask-gemini/SKILL.md:5`). The flow skills use `{{HUMANIZE_RUNTIME_ROOT}}` hydration as the installed runtime root (`skills/humanize/SKILL.md:14`, `skills/humanize-rlcr/SKILL.md:16`).
- edge_cases_or_failure_modes: Key failure modes are unsafe unquoted shell argument expansion in one-shot skills (`skills/ask-codex/SKILL.md:14`, `skills/ask-codex/SKILL.md:30`, `skills/ask-gemini/SKILL.md:17`, `skills/ask-gemini/SKILL.md:33`), nonzero setup or gate exits (`skills/humanize-rlcr/SKILL.md:34`, `skills/humanize-rlcr/SKILL-kimi.md:60`), ambiguous refine-plan comments requiring user input or direct-mode assumptions (`skills/humanize-refine-plan/SKILL.md:31`, `skills/humanize-refine-plan/SKILL.md:59`), and missing/invalid plan IO (`skills/humanize-gen-plan/SKILL.md:115`, `skills/humanize-refine-plan/SKILL.md:131`).
- validation_or_tests: These skill contracts are backed by tests elsewhere: routing/installation tests, RLCR stop-gate tests, gen-plan/refine-plan tests, and template-reference tests. I did not execute tests because the branch export is read-only and the assignment requested research notes only.
- skip_candidate: `no`

### DEV-HZ-039 `file` `config/default_config.json`
- cursor: `[_]`
- core_role: This is the default configuration layer for model routing and workflow defaults. It sets `codex_model` to `gpt-5.5`, `codex_effort` to `high`, `bitlesson_model` to `haiku`, `agent_teams` to `false`, `alternative_plan_language` to empty, and `gen_plan_mode` to `discussion` (`config/default_config.json:2`, `config/default_config.json:7`).
- algorithmic_behavior: `scripts/lib/config-loader.sh` treats this file as the required default layer, then overlays optional user and project configs with null stripping (`scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:109`, `scripts/lib/config-loader.sh:113`, `scripts/lib/config-loader.sh:131`). `hooks/lib/loop-common.sh` consumes the merged config to derive `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, and `DEFAULT_AGENT_TEAMS` (`hooks/lib/loop-common.sh:204`, `hooks/lib/loop-common.sh:222`, `hooks/lib/loop-common.sh:230`, `hooks/lib/loop-common.sh:235`).
- inputs_outputs_state: Input is JSON object content from this file plus optional overlays at `$XDG_CONFIG_HOME/humanize/config.json`, `$HOME/.config/humanize/config.json`, `.humanize/config.json`, or `$HUMANIZE_CONFIG` (`scripts/lib/config-loader.sh:78`, `scripts/lib/config-loader.sh:87`). Output is merged JSON returned by `load_merged_config`, then scalar values returned by `get_config_value` (`scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:139`). Setup persists resolved model/effort/team defaults into loop state (`scripts/setup-rlcr-loop.sh:889`, `scripts/setup-rlcr-loop.sh:902`).
- gates_or_invariants: The default config must exist and be a JSON object; missing or malformed required default config is fatal (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`, `scripts/lib/config-loader.sh:54`). `loop-common.sh` rejects invalid configured Codex model characters, unsupported Codex prefixes, and invalid effort values before falling back (`hooks/lib/loop-common.sh:212`, `hooks/lib/loop-common.sh:216`, `hooks/lib/loop-common.sh:224`).
- dependencies_and_callers: `bitlesson-select.sh` uses `bitlesson_model` to detect provider and may fall back to Codex defaults (`scripts/bitlesson-select.sh:113`, `scripts/bitlesson-select.sh:125`, `scripts/bitlesson-select.sh:136`). The gen-plan template documents that `alternative_plan_language` comes through the same merged config hierarchy and controls translated variant output (`prompt-template/plan/gen-plan-template.md:124`, `prompt-template/plan/gen-plan-template.md:132`).
- edge_cases_or_failure_modes: Malformed optional user/project configs are ignored with warnings, but malformed required default config fails closed (`scripts/lib/config-loader.sh:53`, `scripts/lib/config-loader.sh:57`). Higher-layer null values do not erase lower-layer defaults because `strip_nulls` removes nulls before merging (`scripts/lib/config-loader.sh:119`, `scripts/lib/config-loader.sh:131`).
- validation_or_tests: `tests/test-config-merge.sh` verifies default-only values, project override precedence, user/project priority, additive merge, and nonempty `gen_plan_mode` (`tests/test-config-merge.sh:5`, `tests/test-config-merge.sh:43`, `tests/test-config-merge.sh:45`, `tests/test-config-merge.sh:52`, `tests/test-config-merge.sh:59`, `tests/test-config-merge.sh:102`). `tests/test-config-error-handling.sh` covers missing/malformed default behavior by description in search hits. Not executed in this run.
- skip_candidate: `no`

### DEV-HZ-069 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: This is a manual executable specification for monitor shutdown behavior, not an automated unit test. It verifies that deleting `.humanize/` while `humanize monitor rlcr` is running yields a clean user-facing stop and terminal restoration (`tests/manual-monitor-test.sh:5`, `tests/manual-monitor-test.sh:13`).
- algorithmic_behavior: It has three command modes. `setup` creates a synthetic loop directory and writes minimal `state.md` and `goal-tracker.md` fixtures (`tests/manual-monitor-test.sh:23`, `tests/manual-monitor-test.sh:26`, `tests/manual-monitor-test.sh:32`, `tests/manual-monitor-test.sh:38`). `delete` removes `.humanize` to trigger monitor graceful exit checks (`tests/manual-monitor-test.sh:47`, `tests/manual-monitor-test.sh:49`). `cleanup` removes leftovers (`tests/manual-monitor-test.sh:58`, `tests/manual-monitor-test.sh:60`).
- inputs_outputs_state: Input is the first CLI argument: `setup`, `delete`, or `cleanup` (`tests/manual-monitor-test.sh:23`). Outputs are local runtime state under `.humanize/rlcr/2026-01-16_99-99-99/`, console instructions, and deletion of `.humanize` (`tests/manual-monitor-test.sh:40`, `tests/manual-monitor-test.sh:51`). State transitions are synthetic loop creation, runtime root deletion, and cleanup.
- gates_or_invariants: Expected monitor behavior is explicitly stated: clean message `Monitoring stopped: .humanize/rlcr directory no longer exists`, terminal scroll/cursor restoration, and no shell glob errors (`tests/manual-monitor-test.sh:52`, `tests/manual-monitor-test.sh:54`). Usage errors exit nonzero (`tests/manual-monitor-test.sh:63`, `tests/manual-monitor-test.sh:70`).
- dependencies_and_callers: The manual procedure depends on sourcing `scripts/humanize.sh` and running `humanize monitor rlcr` in a second terminal (`tests/manual-monitor-test.sh:11`, `tests/manual-monitor-test.sh:44`). It coordinates with monitor code in `scripts/humanize.sh`, which reads state fields such as current round, model, plan file, and agent-teams display.
- edge_cases_or_failure_modes: The test intentionally covers the destructive disappearance of `.humanize`, which can otherwise cause monitor loops to throw missing-path or shell expansion errors. Because it creates and deletes runtime state, it should not be run in this read-only branch export.
- validation_or_tests: This file is itself the manual validation script. I inspected it only and did not execute it.
- skip_candidate: `no`

### DEV-HZ-099 `file` `tests/test-model-router.sh`
- cursor: `[_]`
- core_role: This is the executable specification for `scripts/lib/model-router.sh`, which classifies model names into providers, checks provider CLI dependencies, and maps reasoning effort values (`tests/test-model-router.sh:2`, `tests/test-model-router.sh:8`).
- algorithmic_behavior: The tests assert that `gpt-*` and `oN*` models route to `codex`, while Claude family names and aliases route to `claude` (`tests/test-model-router.sh:37`, `tests/test-model-router.sh:54`, `tests/test-model-router.sh:71`, `tests/test-model-router.sh:122`, `tests/test-model-router.sh:173`). Unknown and empty model names must fail with errors (`tests/test-model-router.sh:207`, `tests/test-model-router.sh:224`). The implementation matches this in `detect_provider` (`scripts/lib/model-router.sh:10`, `scripts/lib/model-router.sh:18`, `scripts/lib/model-router.sh:23`, `scripts/lib/model-router.sh:28`).
- inputs_outputs_state: Inputs are model strings, provider names, effort strings, and PATH contents. Outputs are provider strings, dependency success/failure, mapped effort strings, pass/fail test accounting, and temporary mock binaries via `create_mock_binary` (`tests/test-model-router.sh:17`, `tests/test-model-router.sh:239`, `tests/test-model-router.sh:273`). It mutates only test temp state when run.
- gates_or_invariants: Required binaries must exist for the selected provider (`tests/test-model-router.sh:243`, `tests/test-model-router.sh:258`, `tests/test-model-router.sh:277`, `tests/test-model-router.sh:292`; implementation at `scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:49`, `scripts/lib/model-router.sh:53`). Effort is restricted to `xhigh`, `high`, `medium`, or `low`; `xhigh` maps to `high` for Claude with an info log, but passes through for Codex (`tests/test-model-router.sh:311`, `tests/test-model-router.sh:346`; implementation at `scripts/lib/model-router.sh:75`, `scripts/lib/model-router.sh:84`).
- dependencies_and_callers: The test sources `tests/test-helpers.sh` and `scripts/lib/model-router.sh` (`tests/test-model-router.sh:7`, `tests/test-model-router.sh:8`). Runtime callers include `scripts/bitlesson-select.sh`, which calls `detect_provider` and `check_provider_dependency` after reading `bitlesson_model` (`scripts/bitlesson-select.sh:125`, `scripts/bitlesson-select.sh:136`).
- edge_cases_or_failure_modes: It covers unknown model names, empty model names, absent `codex`, absent `claude`, and unknown effort values (`tests/test-model-router.sh:198`, `tests/test-model-router.sh:215`, `tests/test-model-router.sh:249`, `tests/test-model-router.sh:283`, `tests/test-model-router.sh:388`, `tests/test-model-router.sh:405`). Minor test-label duplication exists: two sections are named “Test 10” (`tests/test-model-router.sh:216`, `tests/test-model-router.sh:233`), but behavior remains distinct.
- validation_or_tests: This file is the validation suite and ends with `print_test_summary` (`tests/test-model-router.sh:426`). I did not execute it.
- skip_candidate: `no`

### DEV-HZ-129 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: This is the shared guard/state/template library for RLCR hooks and scripts. It is sourced by read/write/edit/bash validators, the Codex Stop hook, setup/cancel scripts, and `scripts/humanize.sh` (`hooks/lib/loop-common.sh:5`, `hooks/lib/loop-common.sh:13`; callers at `hooks/loop-codex-stop-hook.sh:44`, `hooks/loop-bash-validator.sh:16`, `scripts/setup-rlcr-loop.sh:33`).
- algorithmic_behavior: It validates hook JSON, loads merged config-backed defaults, resolves active loop directories with session isolation, parses loop state, normalizes progress/drift enums, detects Codex review findings, renders block messages, enforces cancel authorization, blocks unsafe git-add patterns, previews edits, and ends loops by renaming state files (`hooks/lib/loop-common.sh:91`, `hooks/lib/loop-common.sh:198`, `hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:494`, `hooks/lib/loop-common.sh:740`, `hooks/lib/loop-common.sh:1056`, `hooks/lib/loop-common.sh:1285`, `hooks/lib/loop-common.sh:1545`).
- inputs_outputs_state: Inputs include raw hook JSON, `.humanize/rlcr/*/*state.md`, command strings, file paths, template variables, merged config JSON, Codex review logs, and cancel signal files. Outputs include exported `VALIDATED_TOOL_NAME`, `VALIDATED_TOOL_INPUT`, `STATE_*` variables, block-message text, review result files, updated YAML fields, and terminal state-file names such as `complete-state.md`, `cancel-state.md`, or `unexpected-state.md` (`hooks/lib/loop-common.sh:118`, `hooks/lib/loop-common.sh:501`, `hooks/lib/loop-common.sh:664`, `hooks/lib/loop-common.sh:778`, `hooks/lib/loop-common.sh:1560`).
- gates_or_invariants: Hook input rejects null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, and excessive depth (`hooks/lib/loop-common.sh:94`, `hooks/lib/loop-common.sh:104`, `hooks/lib/loop-common.sh:112`, `hooks/lib/loop-common.sh:119`, `hooks/lib/loop-common.sh:151`). Strict state parsing requires frontmatter separators, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric rounds, and boolean review state (`hooks/lib/loop-common.sh:541`, `hooks/lib/loop-common.sh:553`, `hooks/lib/loop-common.sh:571`, `hooks/lib/loop-common.sh:583`). Prompt writes, state writes, goal-tracker immutable edits, unsafe cancel commands, and `.humanize` git tracking are blocked through helpers (`hooks/lib/loop-common.sh:855`, `hooks/lib/loop-common.sh:864`, `hooks/lib/loop-common.sh:954`, `hooks/lib/loop-common.sh:1067`, `hooks/lib/loop-common.sh:1423`).
- dependencies_and_callers: It sources `project-root.sh`, `scripts/lib/config-loader.sh`, `template-loader.sh`, and `loop-bg-tasks.sh` (`hooks/lib/loop-common.sh:178`, `hooks/lib/loop-common.sh:183`, `hooks/lib/loop-common.sh:240`, `hooks/lib/loop-common.sh:1583`). The Stop hook uses `detect_review_issues` after running Codex review (`hooks/loop-codex-stop-hook.sh:1328`), and validators use prompt/state/message helpers to block tool actions (`hooks/loop-write-validator.sh:67`, `hooks/loop-edit-validator.sh:50`, `hooks/loop-bash-validator.sh:524`).
- edge_cases_or_failure_modes: Session-filtered active-loop lookup prevents stale loop revival when a session’s newest directory is terminal, and marker fallback is opt-in only for Stop-hook background adoption (`hooks/lib/loop-common.sh:315`, `hooks/lib/loop-common.sh:393`, `hooks/lib/loop-common.sh:415`). Cancel authorization rejects command substitution, newlines, shell operators, extra args, mixed quotes, path escapes, and symlink sources (`hooks/lib/loop-common.sh:1067`, `hooks/lib/loop-common.sh:1077`, `hooks/lib/loop-common.sh:1150`, `hooks/lib/loop-common.sh:1190`, `hooks/lib/loop-common.sh:1218`, `hooks/lib/loop-common.sh:1250`). Review issue detection intentionally scans only the last 50 log lines and only markers in the first 10 characters, reducing false positives but making early findings outside that window ignored (`hooks/lib/loop-common.sh:728`, `hooks/lib/loop-common.sh:755`, `hooks/lib/loop-common.sh:761`).
- validation_or_tests: Referenced tests include `tests/test-codex-review-merge.sh` for review marker extraction, `tests/test-session-id.sh` and `tests/test-stop-hook-bg-allow.sh` for active-loop/session behavior, `tests/robustness/test-cancel-security-robustness.sh` for cancel authorization, `tests/test-bash-validator-patterns.sh` for command write detection, `tests/test-unified-codex-config.sh` for config-backed defaults, and `tests/test-template-references.sh` for template references. I did not run them.
- skip_candidate: `no`

### DEV-HZ-159 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: This is the block-message template returned when a tool or Bash command attempts to modify `round-*-prompt.md` files (`prompt-template/block/prompt-file-write.md:1`, `prompt-template/block/prompt-file-write.md:3`).
- algorithmic_behavior: The template states the invariant that prompt files are instructions from Codex to Claude and must be read/executed, not modified (`prompt-template/block/prompt-file-write.md:5`, `prompt-template/block/prompt-file-write.md:7`). It redirects work output to the summary file (`prompt-template/block/prompt-file-write.md:10`) and tells the agent to document prompt errors in the summary rather than patching the prompt (`prompt-template/block/prompt-file-write.md:12`).
- inputs_outputs_state: It has no template variables. Its output is rendered text from `prompt_write_blocked_message`, with an inline fallback if the template is missing (`hooks/lib/loop-common.sh:855`, `hooks/lib/loop-common.sh:861`).
- gates_or_invariants: Write and Edit validators block prompt files directly (`hooks/loop-write-validator.sh:67`, `hooks/loop-edit-validator.sh:50`). Bash validator blocks commands that modify `round-N-prompt.md` via redirection, `tee`, `sed -i`, `mv`, `cp`, `rm`, and similar command patterns through `command_modifies_file` (`hooks/loop-bash-validator.sh:524`, `hooks/lib/loop-common.sh:1494`).
- dependencies_and_callers: The template is loaded through `load_and_render_safe`, whose missing-template fallback behavior prevents hook failures from becoming unhandled crashes (`hooks/lib/template-loader.sh:188`, `hooks/lib/template-loader.sh:197`). `tests/test-template-references.sh` explicitly lists this template as a common template expected by `loop-common.sh` (`tests/test-template-references.sh:149`, `tests/test-template-references.sh:154`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, `prompt_write_blocked_message` falls back to an inline shorter message (`hooks/lib/loop-common.sh:857`, `hooks/lib/template-loader.sh:197`, `hooks/lib/template-loader.sh:205`). Because the template has no variables, placeholder injection is not relevant here, but it still benefits from the same safe loader.
- validation_or_tests: Covered by template-reference tests and hook validator tests that attempt prompt-file writes/edits/bash modifications. I did not execute tests.
- skip_candidate: `no`

### DEV-HZ-189 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: This is the audit template generated for Codex review phase invocations. It documents, rather than feeds, the `codex review --base` command because Codex CLI compatibility forbids prompt stdin when `--base` is used (`prompt-template/codex/code-review-phase.md:3`, `prompt-template/codex/code-review-phase.md:4`).
- algorithmic_behavior: The template records base branch, review round, timestamp, expected behavior, marker scanning, remediation transition, and finalize transition (`prompt-template/codex/code-review-phase.md:6`, `prompt-template/codex/code-review-phase.md:14`, `prompt-template/codex/code-review-phase.md:17`). It defines expected issue format with severity markers such as `[P0]` and `[P1]` (`prompt-template/codex/code-review-phase.md:21`, `prompt-template/codex/code-review-phase.md:27`).
- inputs_outputs_state: Inputs are `{{BASE_BRANCH}}`, `{{REVIEW_ROUND}}`, and `{{TIMESTAMP}}` (`prompt-template/codex/code-review-phase.md:8`, `prompt-template/codex/code-review-phase.md:10`). Output is `round-N-review-prompt.md`, while related artifacts include review result, command capture, stdout capture, and stderr/log capture (`prompt-template/codex/code-review-phase.md:30`, `prompt-template/codex/code-review-phase.md:36`).
- gates_or_invariants: The associated Stop-hook path writes this audit prompt, records the command, runs `codex review --base "$review_base"` without prompt input, then blocks on command failure or no output, continues on review findings, and enters finalize only when no review findings are detected (`hooks/loop-codex-stop-hook.sh:1247`, `hooks/loop-codex-stop-hook.sh:1264`, `hooks/loop-codex-stop-hook.sh:1290`, `hooks/loop-codex-stop-hook.sh:1320`, `hooks/loop-codex-stop-hook.sh:1328`, `hooks/loop-codex-stop-hook.sh:1337`).
- dependencies_and_callers: `run_codex_code_review` loads this template through `load_and_render_safe` and writes it to the loop directory (`hooks/loop-codex-stop-hook.sh:1231`, `hooks/loop-codex-stop-hook.sh:1264`, `hooks/loop-codex-stop-hook.sh:1270`). The issue-detection dependency is `detect_review_issues` in `loop-common.sh` (`hooks/lib/loop-common.sh:740`).
- edge_cases_or_failure_modes: If the template is missing, the Stop hook uses an inline fallback that includes provider, base branch, base commit, resolved review base, round, and timestamp (`hooks/loop-codex-stop-hook.sh:1250`, `hooks/loop-codex-stop-hook.sh:1263`). Review failure or empty output is a hard block with retry instructions, not a pass-through (`hooks/loop-codex-stop-hook.sh:1610`, `hooks/loop-codex-stop-hook.sh:1647`).
- validation_or_tests: `tests/test-disable-nested-codex-hooks.sh` checks that review phase avoids prompt input with `--base`; `tests/test-finalize-phase.sh` covers review failure/empty output blocking; `tests/test-codex-review-merge.sh` covers marker extraction. I did not execute tests.
- skip_candidate: `no`

### DEV-HZ-219 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: This robustness suite specifies plan-file validation behavior in `scripts/setup-rlcr-loop.sh`, focusing on file existence, readability, size, comments/content counting, line endings, symlinks, and disappearance (`tests/robustness/test-plan-file-robustness.sh:3`, `tests/robustness/test-plan-file-robustness.sh:10`).
- algorithmic_behavior: It creates an isolated temp directory, mock Codex binary, and mock git repository; then `test_plan_validation` runs the production setup script with `CLAUDE_PROJECT_DIR` pointing at the temp repo and classifies output as accepted or rejected (`tests/robustness/test-plan-file-robustness.sh:19`, `tests/robustness/test-plan-file-robustness.sh:27`, `tests/robustness/test-plan-file-robustness.sh:48`, `tests/robustness/test-plan-file-robustness.sh:61`, `tests/robustness/test-plan-file-robustness.sh:113`). Positive production cases include valid plans, minimum three content lines, standard size, rich markdown, large files, and mixed line endings (`tests/robustness/test-plan-file-robustness.sh:178`, `tests/robustness/test-plan-file-robustness.sh:226`, `tests/robustness/test-plan-file-robustness.sh:234`, `tests/robustness/test-plan-file-robustness.sh:253`, `tests/robustness/test-plan-file-robustness.sh:325`, `tests/robustness/test-plan-file-robustness.sh:355`).
- inputs_outputs_state: Inputs are generated plan files under the temp repo. Outputs are pass/fail assertions, optional `.humanize/rlcr` loop creation, and captured setup output temp files (`tests/robustness/test-plan-file-robustness.sh:53`, `tests/robustness/test-plan-file-robustness.sh:82`, `tests/robustness/test-plan-file-robustness.sh:89`). The script mutates temp filesystem and git state when run.
- gates_or_invariants: The production validator rejects absolute paths, spaces, shell metacharacters, symlink leafs, symlink parent dirs, missing files, unreadable files, paths outside the project, submodule paths, tracked non-plan mode files, fewer than five total lines, and fewer than three content lines (`scripts/setup-rlcr-loop.sh:461`, `scripts/setup-rlcr-loop.sh:468`, `scripts/setup-rlcr-loop.sh:476`, `scripts/setup-rlcr-loop.sh:487`, `scripts/setup-rlcr-loop.sh:513`, `scripts/setup-rlcr-loop.sh:523`, `scripts/setup-rlcr-loop.sh:529`, `scripts/setup-rlcr-loop.sh:543`, `scripts/setup-rlcr-loop.sh:556`, `scripts/setup-rlcr-loop.sh:605`, `scripts/setup-rlcr-loop.sh:625`, `scripts/setup-rlcr-loop.sh:671`).
- dependencies_and_callers: The suite depends on `tests/test-helpers.sh`, `scripts/setup-rlcr-loop.sh`, git, Bash, and a mock `codex` in PATH (`tests/robustness/test-plan-file-robustness.sh:26`, `tests/robustness/test-plan-file-robustness.sh:108`). Its local `count_content_lines` mirrors production content counting for some direct edge checks (`tests/robustness/test-plan-file-robustness.sh:129`, `scripts/setup-rlcr-loop.sh:638`).
- edge_cases_or_failure_modes: Negative/edge sections cover empty files, comments-only files, binary/null bytes, very long lines, special shell characters, whitespace-only content, nested/complex HTML comments, missing files, unreadable files, symlink plans, directories instead of files, and deleted files (`tests/robustness/test-plan-file-robustness.sh:300`, `tests/robustness/test-plan-file-robustness.sh:309`, `tests/robustness/test-plan-file-robustness.sh:368`, `tests/robustness/test-plan-file-robustness.sh:390`, `tests/robustness/test-plan-file-robustness.sh:409`, `tests/robustness/test-plan-file-robustness.sh:432`, `tests/robustness/test-plan-file-robustness.sh:444`, `tests/robustness/test-plan-file-robustness.sh:468`, `tests/robustness/test-plan-file-robustness.sh:477`, `tests/robustness/test-plan-file-robustness.sh:491`, `tests/robustness/test-plan-file-robustness.sh:503`, `tests/robustness/test-plan-file-robustness.sh:514`, `tests/robustness/test-plan-file-robustness.sh:527`). Some later cases assert local readability/counting rather than invoking production validation, so they are robustness probes rather than full end-to-end setup checks.
- validation_or_tests: This file is the validation suite and ends with `print_test_summary` plus process exit by test status (`tests/robustness/test-plan-file-robustness.sh:557`). I inspected it but did not execute it.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8/8 item evidence headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`