# agent_12 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-012 `file` `README.md`
- cursor: `[_]`
- core_role: Top-level behavior documentation for Humanize/RLCR. It defines the user-facing loop model: Claude implements, Codex reviews, issues feed back into subsequent implementation rounds until acceptance criteria are met. Key workflow references are at `README.md:9`, `README.md:13`, `README.md:20`, and `README.md:26`.
- algorithmic_behavior: Describes RLCR as a two-phase iterative state machine: implementation phase plus code review phase. It also documents optional upstream idea/planning stages and downstream monitoring. The quick start establishes command order: generate idea, explore directions, generate plan, refine annotated plan, start RLCR loop, ask Gemini, monitor progress at `README.md:42` through `README.md:91`.
- inputs_outputs_state: Inputs are a plan/draft path and optional idea artifacts. Outputs are `.humanize/ideas/<slug>-<timestamp>.md`, `.directions.json`, `.humanize/explore/<run-id>/explore-report.md`, `.humanize/explore/<run-id>/final-idea.md`, generated `docs/plan.md`, and active RLCR loop state under `.humanize/rlcr` as implied by `/humanize:start-rlcr-loop docs/plan.md` at `README.md:67` through `README.md:70`.
- gates_or_invariants: The documented invariant is “Begin with the End in Mind”: before loop start, Humanize verifies the human understands the plan and remains architect, referenced at `README.md:18`. Another invariant is independent review: “One Build + One Review” at `README.md:16`, which separates implementation from Codex review.
- dependencies_and_callers: The README points users to command implementations under plugin commands and scripts indirectly: `/humanize:gen-idea`, `/humanize:explore-idea`, `/humanize:gen-plan`, `/humanize:refine-plan`, `/humanize:start-rlcr-loop`, `/humanize:ask-gemini`, and `humanize monitor ...` at `README.md:44` through `README.md:85`. It depends on Codex CLI for review at `README.md:40` and optionally Gemini CLI at `README.md:72`.
- edge_cases_or_failure_modes: This file is documentation, not executable. Potential behavioral ambiguity is that it presents the loop at a high level and delegates operational details such as state fields, hook gates, and monitor semantics to `docs/usage.md`, hook scripts, and tests. It contains a likely typo in monitor setup text: `.bashec` at `README.md:79`.
- validation_or_tests: No direct test targets README text. It is behavior-defining documentation rather than a validator. Related executable specifications for documented commands and loop behavior appear in assigned test files and broader test scripts.
- skip_candidate: `no`

### DEV-HZ-042 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook validator that prevents RLCR progression while Claude still has incomplete blocking/mainline work. It supports legacy transcript `TodoWrite` and Claude task files, as stated in the header at `hooks/check-todos-from-transcript.py:5` through `hooks/check-todos-from-transcript.py:12`.
- algorithmic_behavior: The script reads hook JSON from stdin, checks task-directory state first when `session_id` exists, then checks transcript TodoWrite state when `transcript_path` exists. It emits no output and exits 0 when clear, prints `INCOMPLETE_TODOS` plus item details and exits 1 when blocking incomplete work exists, and exits 2 on invalid hook-input JSON. Main dispatch is at `hooks/check-todos-from-transcript.py:173` through `hooks/check-todos-from-transcript.py:221`.
- inputs_outputs_state: Inputs are JSON fields `session_id`, optional `tasks_base_dir`, and optional `transcript_path` at `hooks/check-todos-from-transcript.py:188` through `hooks/check-todos-from-transcript.py:198`. Transcript input is JSONL with assistant/message/direct `tool_use` entries parsed by `extract_tool_calls_from_entry` at `hooks/check-todos-from-transcript.py:38` through `hooks/check-todos-from-transcript.py:70`. Task input is `~/.claude/tasks/<session_id>/*.json` or a test override at `hooks/check-todos-from-transcript.py:136` through `hooks/check-todos-from-transcript.py:144`. Output lines include status, lane marker, source, and task id where applicable at `hooks/check-todos-from-transcript.py:204` through `hooks/check-todos-from-transcript.py:220`.
- gates_or_invariants: Lane classification only recognizes a leading tag matching `[mainline]`, `[blocking]`, or `[queued]`; untagged work defaults to blocking for safety at `hooks/check-todos-from-transcript.py:24` through `hooks/check-todos-from-transcript.py:35`. Queued tasks are non-blocking and skipped at `hooks/check-todos-from-transcript.py:110` through `hooks/check-todos-from-transcript.py:112` and `hooks/check-todos-from-transcript.py:156` through `hooks/check-todos-from-transcript.py:158`. Legacy TodoWrite uses only the latest TodoWrite list, replacing prior snapshots at `hooks/check-todos-from-transcript.py:82` through `hooks/check-todos-from-transcript.py:103`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` before expensive Codex review. The stop hook pipes its full hook input into this script at `hooks/loop-codex-stop-hook.sh:408` through `hooks/loop-codex-stop-hook.sh:413`, blocks on exit 2 parse errors at `hooks/loop-codex-stop-hook.sh:415` through `hooks/loop-codex-stop-hook.sh:431`, and blocks incomplete tasks using the `block/incomplete-todos.md` template at `hooks/loop-codex-stop-hook.sh:434` through `hooks/loop-codex-stop-hook.sh:455`.
- edge_cases_or_failure_modes: Missing transcript path or missing tasks directory means no incomplete work is found, so exit 0 at `hooks/check-todos-from-transcript.py:79` through `hooks/check-todos-from-transcript.py:80` and `hooks/check-todos-from-transcript.py:140` through `hooks/check-todos-from-transcript.py:141`. Malformed transcript JSONL lines are ignored at `hooks/check-todos-from-transcript.py:91` through `hooks/check-todos-from-transcript.py:94`. Malformed or unreadable task JSON files are skipped at `hooks/check-todos-from-transcript.py:166` through `hooks/check-todos-from-transcript.py:168`. A task with no status defaults to pending at `hooks/check-todos-from-transcript.py:149`, so it blocks unless queued.
- validation_or_tests: Covered by `tests/test-todo-checker.sh`. Relevant cases include completed vs pending TodoWrite at `tests/test-todo-checker.sh:107` through `tests/test-todo-checker.sh:135`, queued TodoWrite not blocking at `tests/test-todo-checker.sh:160` through `tests/test-todo-checker.sh:172`, inline queued tags still blocking at `tests/test-todo-checker.sh:175` through `tests/test-todo-checker.sh:188`, file task pending/completed/in-progress at `tests/test-todo-checker.sh:340` through `tests/test-todo-checker.sh:388`, queued file tasks at `tests/test-todo-checker.sh:390` through `tests/test-todo-checker.sh:405`, explicit blocking tasks at `tests/test-todo-checker.sh:407` through `tests/test-todo-checker.sh:422`, and inline queued text still blocking at `tests/test-todo-checker.sh:424` through `tests/test-todo-checker.sh:439`.
- skip_candidate: `no`

### DEV-HZ-072 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for Agent Teams mode in the RLCR state machine. It validates CLI gating, config-driven enablement, persisted state, state parsing, initial prompt injection, continuation prompt injection, drift recovery behavior, and review-phase exclusion.
- algorithmic_behavior: The test runs synthetic git repos and invokes `scripts/setup-rlcr-loop.sh` with and without `--agent-teams`. It verifies that Agent Teams mode requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, records `agent_teams: true` or `false` in state, and changes prompt generation only when appropriate. Scope is summarized at `tests/test-agent-teams.sh:3` through `tests/test-agent-teams.sh:13`.
- inputs_outputs_state: Inputs include a test plan file, `.humanize/config.json`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `CLAUDE_PROJECT_DIR`, synthetic `state.md`, mock Codex executables, and hook JSON. Outputs include `.humanize/rlcr/**/state.md`, `round-0-prompt.md`, `round-4-prompt.md`, and `round-6-prompt.md`. State fields asserted include `agent_teams`, `current_round`, `review_started`, `mainline_stall_count`, `last_mainline_verdict`, and `drift_status`.
- gates_or_invariants: The setup gate rejects Agent Teams unless the env var equals exactly `1`; bad values `0`, `false`, `yes`, and `true` are expected to fail at `tests/test-agent-teams.sh:71` through `tests/test-agent-teams.sh:79`. This corresponds to implementation in `scripts/setup-rlcr-loop.sh:390` through `scripts/setup-rlcr-loop.sh:401`. State persistence is checked at `tests/test-agent-teams.sh:113` through `tests/test-agent-teams.sh:152`; setup writes `agent_teams: $AGENT_TEAMS` at `scripts/setup-rlcr-loop.sh:885` through `scripts/setup-rlcr-loop.sh:903`.
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` at `tests/test-agent-teams.sh:17` through `tests/test-agent-teams.sh:22`. Calls `scripts/setup-rlcr-loop.sh` via `SETUP_SCRIPT` at `tests/test-agent-teams.sh:29`. Exercises `parse_state_file` and `find_active_loop` from loop-common at `tests/test-agent-teams.sh:189` through `tests/test-agent-teams.sh:241` and `tests/test-agent-teams.sh:391` through `tests/test-agent-teams.sh:414`. Later tests invoke `hooks/loop-codex-stop-hook.sh` at `tests/test-agent-teams.sh:427` through `tests/test-agent-teams.sh:429`.
- edge_cases_or_failure_modes: Missing env var must fail at `tests/test-agent-teams.sh:54` through `tests/test-agent-teams.sh:69`. Missing `agent_teams` state defaults false at `tests/test-agent-teams.sh:218` through `tests/test-agent-teams.sh:241`, matching loop-common defaulting at `hooks/lib/loop-common.sh:511` through `hooks/lib/loop-common.sh:513` and strict-parser optional defaults at `hooks/lib/loop-common.sh:589` through `hooks/lib/loop-common.sh:593`. Template absence is tolerated as skip/fallback for initial templates at `tests/test-agent-teams.sh:331` through `tests/test-agent-teams.sh:388`, and stop-hook implementation has an explicit fallback block at `hooks/loop-codex-stop-hook.sh:2217` through `hooks/loop-codex-stop-hook.sh:2225`.
- validation_or_tests: This file is itself validation. It asserts initial prompt includes team-leader/team coordination text when enabled at `tests/test-agent-teams.sh:245` through `tests/test-agent-teams.sh:291`, and excludes it when disabled at `tests/test-agent-teams.sh:294` through `tests/test-agent-teams.sh:328`. It asserts implementation continuation prompts include Agent Teams guidance at `tests/test-agent-teams.sh:592` through `tests/test-agent-teams.sh:629`, drift recovery keeps Agent Teams continuation at `tests/test-agent-teams.sh:631` through `tests/test-agent-teams.sh:669`, disabled mode excludes continuation at `tests/test-agent-teams.sh:671` through `tests/test-agent-teams.sh:701`, and review phase excludes Agent Teams continuation while preserving review issues at `tests/test-agent-teams.sh:703` through `tests/test-agent-teams.sh:734`. The prompt injection under test is implemented in setup at `scripts/setup-rlcr-loop.sh:1375` through `scripts/setup-rlcr-loop.sh:1395` and stop hook at `hooks/loop-codex-stop-hook.sh:2207` through `hooks/loop-codex-stop-hook.sh:2227`.
- skip_candidate: `no`

### DEV-HZ-102 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: Thin executable specification for terminal monitor SIGINT behavior. It partitions the larger real monitor e2e suite to run only bash and zsh signal-interruption cases.
- algorithmic_behavior: The file resolves its test directory, sources `tests/test-monitor-e2e-real.sh`, prints a heading, runs `monitor_test_bash_sigint` and `monitor_test_zsh_sigint`, then exits 0 only when `TESTS_FAILED` is zero. Its complete control flow is at `tests/test-monitor-e2e-sigint.sh:1` through `tests/test-monitor-e2e-sigint.sh:21`.
- inputs_outputs_state: Inputs are shell environment, availability of zsh for the zsh case, and helper state from the sourced e2e library. Outputs are stdout summary lines and process exit status. The sourced tests create synthetic `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, fake HOME/XDG cache logs, and temporary runner scripts, as seen in `tests/test-monitor-e2e-real.sh:382` through `tests/test-monitor-e2e-real.sh:478` for bash and `tests/test-monitor-e2e-real.sh:563` through `tests/test-monitor-e2e-real.sh:645` for zsh.
- gates_or_invariants: The wrapper’s invariant is that both selected SIGINT tests must pass, because the final status is `[[ $TESTS_FAILED -eq 0 ]] && exit 0 || exit 1` at `tests/test-monitor-e2e-sigint.sh:19` through `tests/test-monitor-e2e-sigint.sh:21`. The underlying bash test accepts either clean SIGINT exit or forced SIGTERM after monitor startup because bash group signal delivery is complex, at `tests/test-monitor-e2e-real.sh:505` through `tests/test-monitor-e2e-real.sh:519`. The zsh test is stricter: failure if still running after SIGINT timeout at `tests/test-monitor-e2e-real.sh:649` through `tests/test-monitor-e2e-real.sh:666`.
- dependencies_and_callers: Depends on `tests/test-monitor-e2e-real.sh` via `source` at `tests/test-monitor-e2e-sigint.sh:4` through `tests/test-monitor-e2e-sigint.sh:5`. The underlying test sources and executes the real `_humanize_monitor_codex` from `scripts/humanize.sh` in generated runner scripts at `tests/test-monitor-e2e-real.sh:464` through `tests/test-monitor-e2e-real.sh:468` and `tests/test-monitor-e2e-real.sh:632` through `tests/test-monitor-e2e-real.sh:635`.
- edge_cases_or_failure_modes: If zsh is unavailable, the zsh function prints a skip and does not fail at `tests/test-monitor-e2e-real.sh:560` through `tests/test-monitor-e2e-real.sh:562`. If monitor startup exits early, bash fails with captured output at `tests/test-monitor-e2e-real.sh:520` through `tests/test-monitor-e2e-real.sh:527`; zsh fails at `tests/test-monitor-e2e-real.sh:668` through `tests/test-monitor-e2e-real.sh:670`. Both tests assert no zsh/bash glob errors such as “no matches found” or “bad pattern” at `tests/test-monitor-e2e-real.sh:544` through `tests/test-monitor-e2e-real.sh:549` and `tests/test-monitor-e2e-real.sh:680` through `tests/test-monitor-e2e-real.sh:683`.
- validation_or_tests: This wrapper validates `scripts/humanize.sh` cleanup/trap behavior. The monitor cleanup function resets traps, kills tail process, restores terminal, and prints “Stopped monitoring.” at `scripts/humanize.sh:736` through `scripts/humanize.sh:762`. Signal handlers are shell-specific: zsh uses `TRAPINT`/`TRAPTERM`; bash uses `trap '_cleanup' INT TERM` at `scripts/humanize.sh:784` through `scripts/humanize.sh:795`.
- skip_candidate: `no`

### DEV-HZ-132 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared prompt/template rendering library for RLCR hooks and scripts. It defines template directory resolution, file loading, single-pass variable substitution, safe fallback rendering, template appending, and template directory validation.
- algorithmic_behavior: Templates use `{{VARIABLE_NAME}}` placeholders and single-pass substitution. The design explicitly keeps missing variables unchanged and prevents values containing `{{...}}` from being rescanned, documented at `hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:14` and `hooks/lib/template-loader.sh:48` through `hooks/lib/template-loader.sh:55`.
- inputs_outputs_state: `get_template_dir` takes a script/lib path and returns `<plugin_root>/prompt-template` at `hooks/lib/template-loader.sh:26` through `hooks/lib/template-loader.sh:31`. `load_template` takes template dir/name and outputs file contents or warning at `hooks/lib/template-loader.sh:36` through `hooks/lib/template-loader.sh:46`. `render_template` takes content plus `VAR=value` arguments and outputs rendered content at `hooks/lib/template-loader.sh:56` through `hooks/lib/template-loader.sh:136`. `load_and_render_safe` outputs rendered template or rendered fallback at `hooks/lib/template-loader.sh:188` through `hooks/lib/template-loader.sh:211`.
- gates_or_invariants: Substitution variables are stored as environment variables with `TMPL_VAR_` prefix to avoid collisions at `hooks/lib/template-loader.sh:60` through `hooks/lib/template-loader.sh:67`. The awk renderer scans each line left to right, replaces known placeholders, preserves unknown placeholders, and advances beyond replacement without rescanning at `hooks/lib/template-loader.sh:83` through `hooks/lib/template-loader.sh:128`. `validate_template_dir` requires `block`, `codex`, `claude`, and `plan` subdirectories at `hooks/lib/template-loader.sh:216` through `hooks/lib/template-loader.sh:237`.
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh` at `hooks/lib/loop-common.sh:240`, which initializes `TEMPLATE_DIR` and warns if validation fails at `hooks/lib/loop-common.sh:242` through `hooks/lib/loop-common.sh:248`. Callers include read/write/edit/bash validators, stop hook, methodology analysis, plan validator, and BitLesson scripts. Reference scan found extensive use of `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`, including incomplete task blocks, Codex review prompts, finalize prompts, drift prompts, and next-round prompts.
- edge_cases_or_failure_modes: Missing template in `load_template` only warns and returns empty at `hooks/lib/template-loader.sh:41` through `hooks/lib/template-loader.sh:45`. `load_and_render_safe` falls back if load result or rendered result is empty at `hooks/lib/template-loader.sh:194` through `hooks/lib/template-loader.sh:210`. `render_template` returns an error if awk fails at `hooks/lib/template-loader.sh:130` through `hooks/lib/template-loader.sh:133`. A notable shell-safety risk is that `env ${env_vars[@]+"${env_vars[@]}"} awk ...` relies on arguments carrying arbitrary values safely through array expansion; tests cover many special characters.
- validation_or_tests: Covered by `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-template-references.sh`, and robustness/stress tests. Direct cases include missing template fallback, valid/incomplete template dirs, special shell characters, multiline values, unreplaced variables, and single-pass non-recursive behavior. `tests/test-template-references.sh` also enforces that critical validators prefer `load_and_render_safe`.
- skip_candidate: `no`

### DEV-HZ-162 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for sessions started with an older or incomplete RLCR state schema. It is used when required state fields are missing so hooks can fail closed without corrupting loop state.
- algorithmic_behavior: Renders a concise operator-facing block reason with `{{FIELD_NAME}}`, explains that the state file lacks a required field, and instructs cancellation/update/restart. Template body is at `prompt-template/block/schema-outdated.md:1` through `prompt-template/block/schema-outdated.md:12`.
- inputs_outputs_state: Input is the `FIELD_NAME` template variable at `prompt-template/block/schema-outdated.md:3`. Output is Markdown text used as the hook block reason. It does not mutate state itself.
- gates_or_invariants: The invariant is schema compatibility: missing required state fields must terminate/block the active session as unexpected to preserve state information, stated at `prompt-template/block/schema-outdated.md:5` through `prompt-template/block/schema-outdated.md:12`.
- dependencies_and_callers: Called by `hooks/loop-plan-file-validator.sh` through `load_and_render_safe` inside `schema_validation_error` at `hooks/loop-plan-file-validator.sh:60` through `hooks/loop-plan-file-validator.sh:67`. That validator checks required fields `plan_tracked` and `start_branch` after strict parsing at `hooks/loop-plan-file-validator.sh:45` through `hooks/loop-plan-file-validator.sh:90`. The same template can be used anywhere `TEMPLATE_DIR/block/schema-outdated.md` is loaded.
- edge_cases_or_failure_modes: If this template is missing, the caller uses an inline fallback at `hooks/loop-plan-file-validator.sh:61` through `hooks/loop-plan-file-validator.sh:66`. If `FIELD_NAME` is not provided, template-loader preserves `{{FIELD_NAME}}` unchanged because unknown placeholders are not removed. The version text says `1.1.2+` at `prompt-template/block/schema-outdated.md:9`; if schema requirements evolve further, this message could become stale.
- validation_or_tests: Indirectly validated by template reference/loading tests and by hook/state robustness tests. Strict state parser behavior that triggers schema-failure paths is exercised in `tests/robustness/test-state-file-robustness.sh` via missing required fields at `tests/robustness/test-state-file-robustness.sh:201` through `tests/robustness/test-state-file-robustness.sh:228`.
- skip_candidate: `no`

### DEV-HZ-192 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section assigning Codex responsibility for mutable `goal-tracker.md` updates when Claude requests changes or reviewer detects tracker drift.
- algorithmic_behavior: Instructs Codex to evaluate tracker alignment, update only the mutable section when correction is needed, explain rejected requested changes, and categorize common updates into active/completed/deferred/plan-evolution/blocking/queued sections. The complete template is at `prompt-template/codex/goal-tracker-update-section.md:1` through `prompt-template/codex/goal-tracker-update-section.md:18`.
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE` at `prompt-template/codex/goal-tracker-update-section.md:6`. Output is inserted into Codex review prompts. State impact is indirect but important: Codex may modify the mutable section of the referenced `goal-tracker.md`, including task movement, Plan Evolution Log, Blocking Side Issues, Queued Side Issues, Completed and Verified, and Explicitly Deferred sections.
- gates_or_invariants: The key invariant is “NEVER modify the IMMUTABLE SECTION” at `prompt-template/codex/goal-tracker-update-section.md:10`. Deferrals require strong justification at `prompt-template/codex/goal-tracker-update-section.md:18`. The mutable section must stay aligned with the Ultimate Goal and current acceptance-criteria progress at `prompt-template/codex/goal-tracker-update-section.md:5`.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` into `GOAL_TRACKER_UPDATE_SECTION` at `hooks/loop-codex-stop-hook.sh:1016` through `hooks/loop-codex-stop-hook.sh:1020`. That rendered section is then passed into both full-alignment and regular Codex review prompt templates at `hooks/loop-codex-stop-hook.sh:1100` through `hooks/loop-codex-stop-hook.sh:1129`. It depends on `hooks/lib/template-loader.sh` for substitution and fallback behavior.
- edge_cases_or_failure_modes: If the template is absent or empty, stop hook falls back to a minimal “Goal Tracker Updates” section at `hooks/loop-codex-stop-hook.sh:1016` through `hooks/loop-codex-stop-hook.sh:1020`. If Codex follows this template too broadly, it may update mutable tracking while review should only report issues; the invariant limiting changes to the mutable section mitigates that. If `GOAL_TRACKER_FILE` is missing, the prompt may preserve an unresolved placeholder.
- validation_or_tests: Rendered directly by `tests/test-templates-comprehensive.sh` around its real-template case, found at `tests/test-templates-comprehensive.sh:531` through `tests/test-templates-comprehensive.sh:539` in reference scan. Stop-hook prompt composition around this section is validated indirectly by stop-hook tests that inspect generated review prompts.
- skip_candidate: `no`

### DEV-HZ-222 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for RLCR state file parsing. It defines expected tolerant vs strict behavior for valid, malformed, partial, missing, large, special-character, and evolving state schema fields.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, creates temporary `state.md` variants, calls `parse_state_file`, `parse_state_file_strict`, and `get_current_round`, and checks pass/fail outcomes. Scope is declared at `tests/robustness/test-state-file-robustness.sh:3` through `tests/robustness/test-state-file-robustness.sh:10`.
- inputs_outputs_state: Inputs are synthetic state files containing YAML frontmatter-like fields. Outputs are shell variables set by loop-common, especially `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, `STATE_FULL_REVIEW_ROUND`, `STATE_MAINLINE_STALL_COUNT`, `STATE_LAST_MAINLINE_VERDICT`, and `STATE_DRIFT_STATUS`. It also uses exit codes from strict parser and summary output from test helpers.
- gates_or_invariants: Valid files with required fields must parse and preserve expected values at `tests/robustness/test-state-file-robustness.sh:33` through `tests/robustness/test-state-file-robustness.sh:60`. Extra fields are ignored at `tests/robustness/test-state-file-robustness.sh:72` through `tests/robustness/test-state-file-robustness.sh:98`. Strict parser rejects missing frontmatter, non-numeric `current_round`, and missing required fields at `tests/robustness/test-state-file-robustness.sh:149` through `tests/robustness/test-state-file-robustness.sh:215`. Tolerant parser uses defaults for missing fields at `tests/robustness/test-state-file-robustness.sh:217` through `tests/robustness/test-state-file-robustness.sh:228`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/robustness/test-state-file-robustness.sh:14` through `tests/robustness/test-state-file-robustness.sh:17`. The tested parser implementation extracts frontmatter and fields in `hooks/lib/loop-common.sh:430` through `hooks/lib/loop-common.sh:469`; tolerant defaults are at `hooks/lib/loop-common.sh:494` through `hooks/lib/loop-common.sh:521`; strict validation and defaults are at `hooks/lib/loop-common.sh:533` through `hooks/lib/loop-common.sh:600`.
- edge_cases_or_failure_modes: Empty files return current round default 0 at `tests/robustness/test-state-file-robustness.sh:230` through `tests/robustness/test-state-file-robustness.sh:240`. Partial YAML is allowed to return 0 or 5 as long as it does not crash at `tests/robustness/test-state-file-robustness.sh:242` through `tests/robustness/test-state-file-robustness.sh:257`. Malformed YAML returns default 0 at `tests/robustness/test-state-file-robustness.sh:259` through `tests/robustness/test-state-file-robustness.sh:275`. Very large and negative round values are accepted/handled gracefully at `tests/robustness/test-state-file-robustness.sh:277` through `tests/robustness/test-state-file-robustness.sh:312`. Binary content after YAML and CRLF endings must not crash at `tests/robustness/test-state-file-robustness.sh:367` through `tests/robustness/test-state-file-robustness.sh:397`.
- validation_or_tests: This file is validation. It additionally locks in newer schema defaults: `full_review_round` parses when present and defaults to 5 when absent at `tests/robustness/test-state-file-robustness.sh:399` through `tests/robustness/test-state-file-robustness.sh:474`; drift-tracking fields parse and default safely at `tests/robustness/test-state-file-robustness.sh:476` through `tests/robustness/test-state-file-robustness.sh:523`. It finishes through `print_test_summary` at `tests/robustness/test-state-file-robustness.sh:525` through `tests/robustness/test-state-file-robustness.sh:530`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8 unique item evidence sections above; item IDs intentionally not repeated here to preserve exact-once item-heading accounting`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`