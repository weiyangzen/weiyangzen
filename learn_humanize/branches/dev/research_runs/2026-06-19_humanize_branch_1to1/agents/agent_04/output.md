# agent_04 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-004 `directory` `config`
- cursor: `[_]`
- core_role: Repository-local defaults and Codex hook template directory. It contains exactly `config/default_config.json` and `config/codex-hooks.json`; no deeper descendants were present.
- algorithmic_behavior: `default_config.json` seeds merged Humanize config with Codex review defaults (`codex_model: gpt-5.5`, `codex_effort: high`), BitLesson routing (`bitlesson_model: haiku`), RLCR team mode default (`agent_teams: false`), alternative plan generation language, and plan mode. `codex-hooks.json` declares a native Codex `Stop` hook that runs `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` with timeout `7200` and a status message.
- inputs_outputs_state: Inputs are static JSON plus optional user/project config overlays. `scripts/lib/config-loader.sh` requires the default config at `<plugin_root>/config/default_config.json`, then overlays user and project JSON with null stripping and precedence default < user < project at lines `77-132`. Outputs are merged config values consumed by shell runtime. The hook template input is hydrated by `scripts/install-codex-hooks.sh`, replacing `{{HUMANIZE_RUNTIME_ROOT}}` before writing Codex `hooks.json` at lines `139-205`.
- gates_or_invariants: The default config is required and malformed required config is fatal (`scripts/lib/config-loader.sh:40-60`, `108-111`). Codex hook installation refuses missing hook template (`scripts/install-codex-hooks.sh:72`), rejects legacy Codex hook feature keys, and requires native `hooks` plus `--disable` support (`84-117`). Runtime config validates Codex model/effort shape and falls back on invalid values (`hooks/lib/loop-common.sh:211-230`).
- dependencies_and_callers: Used by `hooks/lib/loop-common.sh` for RLCR defaults (`DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, `DEFAULT_AGENT_TEAMS`) at `204-235`; `scripts/install-codex-hooks.sh` for native Codex hook installation at `14` and `128-205`; docs and tests verify the schema and defaults.
- edge_cases_or_failure_modes: Missing/malformed required default config aborts config loading. Missing optional user/project configs become `{}`. Invalid Codex model characters, unsupported model prefixes, or invalid effort values emit warnings and are ignored. Hook template paths with quotes/backslashes are JSON-escaped and shell-quoted during install (`scripts/install-codex-hooks.sh:139-153`).
- validation_or_tests: `tests/test-unified-codex-config.sh:69-80` verifies `codex_model`, `codex_effort`, and absence of stale reviewer keys. `tests/test-unified-codex-config.sh:102-122` verifies default-only and project override behavior. `tests/test-config-merge.sh:43-64` verifies `bitlesson_model`, `agent_teams`, and `gen_plan_mode` defaults.
- skip_candidate: `no`

### DEV-HZ-034 `file` `commands/gen-idea.md`
- cursor: `[_]`
- core_role: Slash-command workflow specification for turning loose idea input into a repo-grounded draft and companion `.directions.json`, feeding both `/humanize:gen-plan` and `/humanize:explore-idea`.
- algorithmic_behavior: Defines a strict sequential pipeline: parse input, validate IO, generate exactly `N` orthogonal directions, dispatch read-only `Explore` subagents in one parallel Task message, synthesize a primary direction plus alternatives, write draft, write companion JSON, validate JSON, and report next commands (`commands/gen-idea.md:25-34`, `76-149`, `192-262`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, optional `--n`, optional `--output`, inline idea text or a `.md` file. Phase 1 consumes structured stdout from `scripts/validate-gen-idea-io.sh`, including `INPUT_MODE`, `OUTPUT_FILE`, `DIRECTIONS_JSON_FILE`, `SLUG`, `TEMPLATE_FILE`, `N`, and optionally `IDEA_BODY_FILE` (`50-72`). Outputs are `OUTPUT_FILE` and `DIRECTIONS_JSON_FILE`, with schema version `1`, metadata, primary flag, direction IDs, evidence arrays, risks, confidence, and raw subagent responses (`196-243`).
- gates_or_invariants: Draft-only: no implementation, source edits, or commits; only the draft and companion JSON may be written, with cleanup allowed only for those just-written files on validation failure (`19-23`). Direction generation requires orthogonality and retries once if too few directions are produced; fewer than two directions is fatal (`90-107`). Exploration drops degraded proposals and requires at least two surviving proposals (`138-145`). Companion JSON must pass `validate-directions-json.sh`; failure deletes both outputs (`244-250`).
- dependencies_and_callers: Calls `scripts/validate-gen-idea-io.sh` and `scripts/validate-directions-json.sh` via allowed tools (`4-7`, `50-53`, `244-247`). Reads the idea template from `TEMPLATE_FILE` (`167`). Produces companion JSON that `commands/explore-idea.md` and `scripts/validate-explore-idea-io.sh` consume.
- edge_cases_or_failure_modes: Input validation exit codes cover missing input, unreadable/missing `.md`, missing output directory, output collision, permission failure, invalid output suffix, missing template, and companion JSON collision (`55-65`). Short inline ideas are warnings, not errors (`66`). If Phase 2 or Phase 3 degrades below two viable directions, it stops without partial output (`266-273`). Fabricated repo references are forbidden; no concrete precedent must use the explicit sentinel (`123-136`, `271`).
- validation_or_tests: `tests/test-gen-idea-dual-write.sh` checks the dual-write contract, `DIRECTIONS_JSON_FILE` references, `explore-idea` hint, and schema validator tool references. `scripts/validate-gen-idea-io.sh:69-217` enforces input/path/output/template preconditions; `scripts/validate-directions-json.sh:40-120` validates the companion schema.
- skip_candidate: `no`

### DEV-HZ-064 `file` `scripts/validate-explore-idea-io.sh`
- cursor: `[_]`
- core_role: Pre-dispatch validator and run metadata generator for `/humanize:explore-idea`; it prevents worker side effects until inputs, selection, templates, git anchor, and run path are valid.
- algorithmic_behavior: Parses a `.directions.json` input directly or resolves a draft `.md` to its companion file (`207-235`), validates numeric caps (`186-201`), validates JSON schema through `validate-directions-json.sh` (`266-275`), selects direction IDs by default display order or explicit selectors (`287-352`), computes effective concurrency (`363-364`), verifies git base state (`378-403`), derives `RUN_ID` and paths (`414-441`), checks collision (`443-447`), then emits structured `KEY: value` output plus `VALIDATION_SUCCESS` (`457-477`).
- inputs_outputs_state: Inputs are `<input-path>` plus `--directions`, `--concurrency`, `--max-worker-iterations`, `--worker-timeout-min`, and `--codex-timeout-min` (`5-18`, `143-170`). Outputs include absolute `DIRECTIONS_JSON_FILE`, optional `DRAFT_PATH`, `RUN_ID`, `RUN_DIR`, `REPORT_PATH`, `FINAL_IDEA_PATH`, `BASE_BRANCH`, `BASE_COMMIT`, selected direction IDs, concurrency and timeout values, fixed Codex review model spec, and template paths (`31-52`, `457-477`).
- gates_or_invariants: Caps are hard: max selected directions `10`, concurrency `10`, worker iterations `3`, worker timeout `60`, Codex timeout `20` (`60-69`, `198-201`, `358-360`). Requires `jq` (`266-269`), valid schema (`271-275`), worker/report/final templates (`248-260`, `405-408`), a real git checkout with resolvable `HEAD` (`378-388`), and no uncommitted tracked changes (`396-403`). Worker base contract forbids checking out the coordinator branch in worktrees; workers assert `HEAD == BASE_COMMIT` (`367-377`).
- dependencies_and_callers: Called by `commands/explore-idea.md:54-77`, which parses its stdout, displays confirmation, writes manifests, and dispatches isolated workers. Depends on `scripts/validate-directions-json.sh`, `prompt-template/explore/worker-prompt.md`, `report-template.md`, and `final-idea-template.md` (`248-251`). Produces `CODEX_REVIEW_MODEL_SPEC` expected as `gpt-5.5:xhigh` for worker review calls (`449-451`).
- edge_cases_or_failure_modes: Exit codes are explicit: missing input `1`, missing/unreadable file `2`, missing companion `3`, unsupported extension `4`, schema failure `5`, argument/cap/selector errors `6`, git checkout failures `7`, run directory collision `8`, missing template `9` (`19-30`). Detects duplicate raw selectors and duplicate resolved selectors, including mixed source-index plus ID duplicates (`301-349`). Run nonce can be supplied through `HUMANIZE_EXPLORE_RUN_NONCE`; otherwise it uses `/dev/urandom`, then PID/random/time fallback (`120-140`).
- validation_or_tests: `tests/test-validate-explore-idea-io.sh:67-260` covers error exits including missing input, bad files, invalid schema, caps, duplicate selectors, dirty checkout, non-git checkout, unborn git repo, and missing templates. `tests/test-validate-explore-idea-io.sh:293-405` checks success output, concurrency capping, run ID derivation, and report/final path construction.
- skip_candidate: `no`

### DEV-HZ-094 `file` `tests/test-frontend-migration.sh`
- cursor: `[_]`
- core_role: Executable frontend regression spec for the viz dashboard SPA migration, auth propagation, streaming lifecycle, targeted refresh behavior, and pipeline drag listener hardening.
- algorithmic_behavior: Pattern-checks `viz/static/js/app.js`, `viz/static/js/actions.js`, `viz/static/js/pipeline.js`, and `viz/static/css/layout.css` without launching a browser (`tests/test-frontend-migration.sh:11-18`). It accumulates pass/fail counters and exits nonzero if any invariant fails (`24-29`, `309-318`).
- inputs_outputs_state: Inputs are static frontend files. Output is terminal PASS/FAIL accounting with process exit `1` when `FAIL_COUNT > 0` (`309-316`). It does not mutate application state.
- gates_or_invariants: Ensures project switcher/add chrome and `/api/projects/{switch,add,remove}` calls are gone (`30-68`); token propagation exists through `_resolveAuthToken`, `Authorization: Bearer`, `window.authedFetch`, and `_withToken` for SSE query tokens (`70-102`); live log SSE handles `snapshot`, `append`, `resync`, `eof` (`104-132`); non-home routes tear down live panes and remote mode skips WebSocket (`152-170`); home/session detail refresh is targeted and debounced (`172-192`, `260-307`); cancel button statuses include active/analyzing/finalizing (`198-204`); UTF-8 log decoding uses `TextDecoder` and `Uint8Array` (`206-217`); pipeline window drag listeners are singleton guarded (`219-258`).
- dependencies_and_callers: Bound to production functions in `viz/static/js/app.js`, including `_resolveAuthToken` (`22-56`), `_withToken` (`60-64`), `_scheduleHomeRefresh` (`127-133`), `_scheduleSessionPartialRefresh` (`137-150`), `_mountLiveLogPane` (`687-792`), `_teardownAllLivePanes` (`794-799`), and `_ensureSessionLogPane` (`935-980`). Also depends on `pipeline.js` singleton guard (`viz/static/js/pipeline.js:25-31`).
- edge_cases_or_failure_modes: Pattern-based checks can miss semantic regressions or fail on equivalent refactors with different names. It specifically guards historical failures: remote WebSocket rejection, frantic refresh/flicker, EOF page rebuilds, CJK/emoji mojibake, route-change race after async fetch, EventSource restart on targeted refresh, and duplicate drag handlers.
- validation_or_tests: The file is itself the validation artifact. It is likely run by the broader test harness `tests/run-all-tests.sh`; inspected only in this pass.
- skip_candidate: `no`

### DEV-HZ-124 `file` `tests/test-viz-isolation.sh`
- cursor: `[_]`
- core_role: Executable spec for per-project viz daemon isolation, tmux session naming, persistent URL probing, host-aware port selection, readiness fail-closed behavior, and IPv6 URL correctness.
- algorithmic_behavior: Sources `viz/scripts/viz-session-name.sh`, calls `viz_tmux_session_name` with controlled project paths, then pattern-checks `viz-start.sh`, `viz-stop.sh`, and `viz-status.sh` for helper usage, exact-match tmux behavior, `viz.url` lifecycle, bind-aware probing, readiness cleanup, and IPv6 bracketing (`tests/test-viz-isolation.sh:33-267`).
- inputs_outputs_state: Inputs are shell scripts under `viz/scripts/`. Outputs are PASS/FAIL lines and nonzero exit if failures exist (`268-277`). The only temporary state is a `mktemp` probe script replaying URL construction (`226-254`), cleaned by trap.
- gates_or_invariants: Different project paths must yield distinct stable `humanize-viz-<hash>` names, while empty input falls back to legacy `humanize-viz` defensively (`48-79`). Start/stop/status must source the helper, avoid hard-coded `TMUX_SESSION="humanize-viz"`, and derive from `$PROJECT_DIR` (`81-106`). Start persists `viz.url`, status probes `$probe_url/api/health`, stop removes both `viz.port` and `viz.url`, and status keeps localhost fallback for older deployments (`108-134`). `find_port` must probe configured host for specific non-loopback binds (`136-173`). Readiness must fail closed and remove stale files (`175-208`). Specific IPv6 hosts must be bracketed (`210-267`).
- dependencies_and_callers: Production helper `viz/scripts/viz-session-name.sh:14-40` hashes absolute project paths and returns the tmux session name. `viz-start.sh` uses the helper at `21-24` and `61-64`, writes `URL_FILE` at `56-64` and `192`, probes configured hosts in `find_port` at `140-158`, and performs readiness cleanup at `223-238`. `viz-status.sh:31-58` reads `viz.url` and exact-kills stale sessions. `viz-stop.sh:31-40` exact-kills and removes state.
- edge_cases_or_failure_modes: Guards against one project killing another’s daemon, stale port/url files, localhost-only health probes for remote binds, false-ready startup after Flask crashes, invalid unbracketed IPv6 URLs, and prefix collisions between legacy/global and hashed tmux names.
- validation_or_tests: The file is itself the runnable validation spec. It is pattern-heavy plus a small behavioral replay for URL formation; it does not start real tmux/Flask processes.
- skip_candidate: `no`

### DEV-HZ-154 `file` `prompt-template/block/mainline-drift-stop.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR mainline drift circuit breaker; it defines the user-facing terminal state when repeated rounds fail to advance the mainline.
- algorithmic_behavior: Renders a concise stop reason with placeholders for consecutive stall count, last mainline verdict, and plan anchor; declares drift status `replan_required`; instructs that the loop should not continue automatically and prescribes re-reading the original plan, diagnosing stalls/regressions, and restarting with a narrower recovered objective (`prompt-template/block/mainline-drift-stop.md:1-14`).
- inputs_outputs_state: Inputs are `{{STALL_COUNT}}`, `{{LAST_VERDICT}}`, and `{{PLAN_FILE}}`. Output is Markdown embedded as the `reason` in a stop-hook JSON block. The producing code updates state fields first, then renders this template and calls `end_loop` (`hooks/loop-codex-stop-hook.sh:1445-1480`).
- gates_or_invariants: The invariant is circuit-breaker stop, not retry: once triggered, `drift_status` becomes `replan_required` and the hook returns a blocking decision (`hooks/loop-codex-stop-hook.sh:1451-1479`). The message explicitly prevents automatic continuation (`mainline-drift-stop.md:9`).
- dependencies_and_callers: Called by `stop_for_mainline_drift()` through `load_and_render_safe "$TEMPLATE_DIR" "block/mainline-drift-stop.md"` (`hooks/loop-codex-stop-hook.sh:1465-1468`). Related state constants live in `hooks/lib/loop-common.sh`, including `DRIFT_STATUS_REPLAN_REQUIRED`.
- edge_cases_or_failure_modes: If template loading fails, hook code has an inline fallback with equivalent semantics (`hooks/loop-codex-stop-hook.sh:1456-1463`). If placeholders are missing or stale, the user-facing reason loses concrete context, but the state transition has already been applied before rendering.
- validation_or_tests: `tests/test-finalize-phase.sh` references drift recovery state updates and preservation; search hits show assertions around `round=4`, stall count, verdict, and `replan_required`. `tests/robustness/test-state-file-robustness.sh` parses drift-tracking fields. This exact template is primarily indirectly validated through stop-hook behavior.
- skip_candidate: `no`

### DEV-HZ-184 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Normal next-round prompt template for RLCR continuation after review; it re-anchors the next implementation round around the original plan, goal tracker, round contract, review findings, task lane rules, and BitLesson routing.
- algorithmic_behavior: Requires the worker to re-read the original plan, goal tracker, and recent summaries/reviews before code; write a new round contract; maintain exactly one mainline objective; classify tasks as `[mainline]`, `[blocking]`, or `[queued]`; run `bitlesson-selector` for each task/subtask; and use Codex review content as input without letting queued work replace the round objective (`prompt-template/claude/next-round-prompt.md:1-75`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, `{{ROUND_CONTRACT_FILE}}`, `{{BITLESSON_FILE}}`, and `{{REVIEW_CONTENT}}`. Output is the next round prompt file written by `hooks/loop-codex-stop-hook.sh` when drift replan is not required (`hooks/loop-codex-stop-hook.sh:2089-2099`). It drives subsequent agent behavior rather than mutating state itself.
- gates_or_invariants: Hard gate: do not implement until the round contract exists (`next-round-prompt.md:12-28`). Task lane invariants require mainline work as primary success condition, blocking work only when it blocks the mainline, and queued work documented but out of scope (`29-45`). Goal tracker mutable section must stay updated, immutable section must not change after Round 0 (`54-65`). Mainline guardrails require stable objective and classification of review findings (`67-75`).
- dependencies_and_callers: Loaded by the stop hook at `hooks/loop-codex-stop-hook.sh:2090-2098`. If drift is required, the hook chooses `claude/drift-replan-prompt.md` instead (`2079-2088`). If Agent Teams mode is enabled, the hook injects an extra delegation warning into the generated prompt (`2116-2125`).
- edge_cases_or_failure_modes: The template assumes referenced files exist and are readable; separate hook gates catch missing round contracts before exit (`hooks/loop-codex-stop-hook.sh:838-850`). If review content contains many issues, the prompt explicitly prevents queued/non-blocking issues from taking over the round (`next-round-prompt.md:69-75`).
- validation_or_tests: Indirectly covered by hook/finalize-phase and agent-team tests that assert round contract and mainline objective behavior. `rg` references show related tests in `tests/test-finalize-phase.sh` and `tests/test-agent-teams.sh`; no standalone test executes this template in isolation.
- skip_candidate: `no`

### DEV-HZ-214 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test suite for production git status/state helpers in `scripts/humanize.sh`, covering clean/dirty status parsing and special repository states used by monitor and RLCR surfaces.
- algorithmic_behavior: Sources `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`, creates temporary git repositories, then calls `humanize_parse_git_status` and `humanize_detect_git_state` across 25 scenarios (`tests/robustness/test-git-operations-robustness.sh:13-21`, `49-62`, `71-506`).
- inputs_outputs_state: Inputs are temporary repos with controlled file mutations, staged changes, branch/detached states, simulated rebase/merge/shallow markers, and permission scenarios. Outputs are test pass/fail records via helper functions and final summary exit status (`502-507`). It mutates only test temp directories.
- gates_or_invariants: `humanize_parse_git_status` must return pipe-delimited `modified|added|deleted|untracked|insertions|deletions`, with non-git returning a recognizable marker (`32-47`, `168-177`). It must count untracked, modified, staged added, deleted, renamed-as-modified, many files, spaces in filenames, binary files, staged plus unstaged changes, insertions, and deletions (`71-367`). `humanize_detect_git_state` must distinguish normal, detached, rebase, merge, shallow, non-git, and permission/not-repo cases (`377-500`).
- dependencies_and_callers: Tests production implementation in `scripts/humanize.sh:158-252`. `humanize_detect_git_state` inspects `git rev-parse --git-dir`, readability, `rebase-merge`, `rebase-apply`, `MERGE_HEAD`, `shallow`, and symbolic HEAD (`158-204`). `humanize_parse_git_status` parses porcelain status and `git diff --shortstat HEAD` (`208-252`). The monitor aliases `_parse_git_status` to this function in `scripts/humanize.sh:411`.
- edge_cases_or_failure_modes: Simulates detached HEAD, empty repo, feature branches, rename status, many untracked files, names with spaces, binary files, same-file staged/unstaged status, manually created rebase/merge/shallow markers, non-git directory, and unreadable `.git`. The permission case allows either `permission_error` or `not_a_repo` on systems where git reports differently (`478-496`).
- validation_or_tests: This file is the validation artifact. It directly exercises the production functions rather than using only grep patterns.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV-HZ-004, DEV-HZ-034, DEV-HZ-064, DEV-HZ-094, DEV-HZ-124, DEV-HZ-154, DEV-HZ-184, DEV-HZ-214
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`