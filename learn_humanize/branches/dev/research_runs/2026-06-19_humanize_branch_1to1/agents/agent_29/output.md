# agent_29 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-029 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Defines a lightweight subagent policy for the `gen-plan` workflow’s relevance gate. It decides whether a user draft belongs to the current repository before a plan file is generated.
- algorithmic_behavior: The prompt instructs the agent to first inspect repository context, specifically README/CLAUDE/docs, directory structure, technologies, languages, and purpose at `agents/draft-relevance-checker.md:14-20`; then compare draft semantics against repository concepts, components, paths, functions, and features at `agents/draft-relevance-checker.md:21-25`; then emit one of exactly two verdict prefixes, `RELEVANT:` or `NOT_RELEVANT:`, at `agents/draft-relevance-checker.md:27-29`. The policy is intentionally permissive: uncertain cases lean relevant at `agents/draft-relevance-checker.md:33-36`.
- inputs_outputs_state: Input is draft document content supplied by the caller, plus read-only repository observations through `Read`, `Glob`, and `Grep` declared in frontmatter at `agents/draft-relevance-checker.md:1-5`. Output is a single verdict line with a short explanation. It does not mutate repository or persistent loop state.
- gates_or_invariants: The invariant is semantic relevance, not exact string matching, and the decision threshold is lenient. The only protocol-visible outputs are the two uppercase prefixes. A non-relevant verdict stops `gen-plan`; relevant proceeds to template copy and draft append, as specified by `commands/gen-plan.md:226-249`.
- dependencies_and_callers: Called by `commands/gen-plan.md` Phase 2, which explicitly invokes `humanize:draft-relevance-checker` with model `haiku` and asks it to explore the repo and return `RELEVANT:` or `NOT_RELEVANT:` at `commands/gen-plan.md:232-240`. Structural tests validate file existence, `name`, `model`, tool declaration, naming convention, frontmatter completeness, valid model name, and English-only content in `tests/test-gen-plan.sh:111-115`, `tests/test-gen-plan.sh:476-509`, `tests/test-gen-plan.sh:594-598`, `tests/test-gen-plan.sh:642-648`, and `tests/test-gen-plan.sh:744-774`.
- edge_cases_or_failure_modes: Because the policy says to mark doubtful drafts relevant, false positives are preferred over false negatives. Informal drafts, any language, and rough ideas are accepted inputs at `agents/draft-relevance-checker.md:33-34`. Failure modes are malformed/missing frontmatter, unsupported model, missing tools, or output not starting with the required verdict prefix.
- validation_or_tests: Covered by `tests/test-gen-plan.sh` metadata and content checks; runtime behavior is indirectly specified by the command prompt in `commands/gen-plan.md`.
- skip_candidate: `no`

### DEV-HZ-059 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Provides the repository’s portable timeout abstraction used by RLCR setup, hooks, reviewer calls, question helpers, and tests across macOS/Linux.
- algorithmic_behavior: `detect_timeout_impl` selects the first available implementation in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` at `scripts/portable-timeout.sh:9-32`. GNU `timeout` is accepted only if `timeout --version` output looks like GNU/coreutils/timeout-version output, preventing accidental use of incompatible shims at `scripts/portable-timeout.sh:15-21`. `TIMEOUT_IMPL` is computed once at source time at `scripts/portable-timeout.sh:34`. `run_with_timeout` dispatches by implementation: native timeout wrappers receive seconds plus the command array, Python uses `subprocess.run(..., timeout=seconds)`, and `none` emits a warning then runs the command unbounded at `scripts/portable-timeout.sh:38-75`.
- inputs_outputs_state: Inputs are timeout seconds and argv vector. Output is the wrapped command’s stdout/stderr and exit code. On Python timeout it exits `124` to match GNU timeout at `scripts/portable-timeout.sh:61-62`; on other Python exceptions it prints an error to stderr and exits `1` at `scripts/portable-timeout.sh:63-65`. It exports `TIMEOUT_IMPL` for sourcing scripts at `scripts/portable-timeout.sh:78-81`.
- gates_or_invariants: Preserves command argument boundaries via Bash arrays at `scripts/portable-timeout.sh:41`, `scripts/portable-timeout.sh:45`, and `scripts/portable-timeout.sh:49`. Maintains GNU-style timeout exit code `124` for Python fallback. Does not require `export -f`, because sourcing scripts use the function directly.
- dependencies_and_callers: Sourced by RLCR setup at `scripts/setup-rlcr-loop.sh:25-27`, the Codex stop hook at `hooks/loop-codex-stop-hook.sh:51`, plan validator, ask helpers, selector scripts, and multiple tests. Key runtime callers wrap git probes, Codex review, and Codex exec, for example `hooks/loop-codex-stop-hook.sh:1290` and `hooks/loop-codex-stop-hook.sh:1719`.
- edge_cases_or_failure_modes: If no timeout backend exists, commands run without enforcement and only a warning is emitted at `scripts/portable-timeout.sh:69-73`. Python fallback does not explicitly terminate descendant process groups beyond `subprocess.run` behavior. The timeout value is interpolated directly into the Python command text at `scripts/portable-timeout.sh:59`, so callers are expected to pass numeric seconds. Empty command, nonexistent command, zero timeout, special characters, large output, signal-handling commands, and rapid cycles are treated as expected edge cases by robustness tests.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` validates implementation detection, valid implementation vocabulary, quick success, timeout exit `124`, argument preservation, exit-code preservation, pipelines through `sh -c`, short and zero timeouts, large output, rapid cycles, nonexistent/empty commands, special characters, signal behavior, fallback detection, subshell execution, and exported `TIMEOUT_IMPL` at `tests/robustness/test-timeout-robustness.sh:31-269`.
- skip_candidate: `no`

### DEV-HZ-089 `file` `tests/test-disable-nested-codex-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification proving nested Codex invocations inside the RLCR stop hook disable native hooks so the hook does not recursively trigger itself, and proving `codex review --base` is invoked without prompt stdin.
- algorithmic_behavior: The test builds isolated temp git repos and loop state at `tests/test-disable-nested-codex-hooks.sh:34-40` and `tests/test-disable-nested-codex-hooks.sh:42-64`. It installs a mock `codex` binary that responds to `--help`, validates supported `--disable` features, records argv, simulates `codex exec` returning “Review: keep iterating,” and simulates `codex review` rejecting the incompatible `--base` plus prompt-input combination at `tests/test-disable-nested-codex-hooks.sh:66-135`. `setup_loop_dir` creates `.humanize/rlcr/<timestamp>/state.md`, goal tracker, summary, and optional `.review-phase-started` marker at `tests/test-disable-nested-codex-hooks.sh:138-190`. `run_loop_hook` pipes empty hook JSON into `hooks/loop-codex-stop-hook.sh` under mocked PATH and asserts the hook exits zero at `tests/test-disable-nested-codex-hooks.sh:193-212`.
- inputs_outputs_state: Inputs are temp repo state, mock supported feature list, and review_started true/false. Outputs are captured Codex argv files and pass/fail counters. State transitions tested include implementation phase invoking nested `codex exec`, review phase invoking nested `codex review`, and legacy feature probing with only `codex_hooks`.
- gates_or_invariants: The hook must pass `--disable hooks --disable plugin_hooks --disable codex_hooks` to `codex exec` when all are supported at `tests/test-disable-nested-codex-hooks.sh:214-223`, pass the same disables to `codex review` at `tests/test-disable-nested-codex-hooks.sh:225-234`, avoid the old brittle `codex --help | grep` pipeline at `tests/test-disable-nested-codex-hooks.sh:236-242`, invoke review with `--base` and no trailing `-`/stdin at `tests/test-disable-nested-codex-hooks.sh:244-249`, document the `--base` prompt incompatibility in the audit prompt at `tests/test-disable-nested-codex-hooks.sh:251-259`, and disable only actually supported legacy features at `tests/test-disable-nested-codex-hooks.sh:262-273`.
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`. The corresponding implementation probes `codex --help`, tests each feature name, caches supported names, and builds `CODEX_DISABLE_HOOKS_ARGS` at `hooks/loop-codex-stop-hook.sh:1176-1202`. These args are used for review at `hooks/loop-codex-stop-hook.sh:1283-1290` and exec at `hooks/loop-codex-stop-hook.sh:1709-1719`. The audit prompt compatibility note is generated at `hooks/loop-codex-stop-hook.sh:1249-1270`.
- edge_cases_or_failure_modes: The mock intentionally fails if `codex review --base` receives prompt stdin or `-`, reproducing Codex 0.130.0 behavior at `tests/test-disable-nested-codex-hooks.sh:119-127`. It also fails unsupported feature names at `tests/test-disable-nested-codex-hooks.sh:76-82`, ensuring feature probing is compatibility-safe for older CLIs. A missing `.review-phase-started` marker would be a hook state inconsistency in the actual hook, guarded at `hooks/loop-codex-stop-hook.sh:1946-1961`.
- validation_or_tests: The file is itself a test and is included in the suite list via `tests/run-all-tests.sh` per repository search. It exits `1` if any pass/fail counter records failure at `tests/test-disable-nested-codex-hooks.sh:275-284`.
- skip_candidate: `no`

### DEV-HZ-119 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Comprehensive executable specification for prompt-template loading, syntax validation, rendering behavior, fallback behavior, and integration rendering against real templates.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` and computes the template root with `get_template_dir` at `tests/test-templates-comprehensive.sh:15-18` and `tests/test-templates-comprehensive.sh:59`. It validates directory structure for `block`, `codex`, and `claude` at `tests/test-templates-comprehensive.sh:64-80`, loads every `*.md` template and verifies non-whitespace content at `tests/test-templates-comprehensive.sh:85-111`, scans all templates for malformed placeholder forms at `tests/test-templates-comprehensive.sh:116-194`, unit-tests malformed placeholder regexes at `tests/test-templates-comprehensive.sh:197-259`, exercises render edge cases at `tests/test-templates-comprehensive.sh:261-439`, validates fallback and directory checks at `tests/test-templates-comprehensive.sh:440-490`, renders selected real templates at `tests/test-templates-comprehensive.sh:492-540`, then stress-loads/renders all templates with dummy values at `tests/test-templates-comprehensive.sh:542-606`.
- inputs_outputs_state: Inputs are the repository’s `prompt-template/**/*.md` files and synthetic template strings/values. Outputs are pass/fail/warn counters and process exit status. It creates temporary malformed-placeholder fixtures and temp dirs, then removes them at `tests/test-templates-comprehensive.sh:204-259` and `tests/test-templates-comprehensive.sh:481-490`; it does not modify repository files.
- gates_or_invariants: Template placeholders must be uppercase `{{VAR_NAME}}` style; extra braces, wrong bracket types, spaced placeholders, and lowercase placeholders are failures at `tests/test-templates-comprehensive.sh:122-175`. `render_template` must preserve empty lines, support multiple/adjacent/repeated variables, tolerate special regex characters, quotes, markdown, long values, and multiline values without crashing at `tests/test-templates-comprehensive.sh:266-439`. Missing templates must use fallback, existing templates must override fallback, valid template dirs must pass, invalid/incomplete dirs must fail at `tests/test-templates-comprehensive.sh:445-490`. Final gate exits `0` only when `TESTS_FAILED` is zero at `tests/test-templates-comprehensive.sh:611-625`.
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`, whose contract says templates use `{{VARIABLE_NAME}}`, missing variables remain unchanged, and substitution is single-pass at `hooks/lib/template-loader.sh:7-21`. `render_template` implements the single-pass awk scanner at `hooks/lib/template-loader.sh:56-136`; `load_and_render_safe` implements fallback at `hooks/lib/template-loader.sh:185-211`; `validate_template_dir` requires `block`, `codex`, `claude`, and `plan` at `hooks/lib/template-loader.sh:213-238`. The tests exercise real templates including `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at `tests/test-templates-comprehensive.sh:497-540`.
- edge_cases_or_failure_modes: The directory-structure section only checks `block`, `codex`, and `claude`, while `validate_template_dir` requires `plan` too; this means a missing `plan` directory would be caught later in Section 5 rather than Section 1. Some difficult value cases only assert “no crash” rather than exact byte preservation, notably backslashes, quotes, CJK-like values, markdown, and multiline values at `tests/test-templates-comprehensive.sh:328-402`. Unreplaced placeholders during full-template stress are warnings, not failures, at `tests/test-templates-comprehensive.sh:590-597`.
- validation_or_tests: This file is itself a CI-style validation suite for templates and the loader. It also indirectly validates the assigned templates `prompt-template/block/goal-tracker-bash-write.md` and `prompt-template/claude/finalize-phase-prompt.md` because it loads and syntax-checks every markdown template at `tests/test-templates-comprehensive.sh:90-107` and `tests/test-templates-comprehensive.sh:118-194`.
- skip_candidate: `no`

### DEV-HZ-149 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Blocking-message template used by hook validators when a Bash command attempts to modify `goal-tracker.md`, enforcing tool-mediated edits so validation hooks can inspect the change.
- algorithmic_behavior: The template emits a “Bash Write Blocked” message, tells the user not to use Bash commands to modify `goal-tracker.md`, points to the correct path via `{{CORRECT_PATH}}`, and explains that shell commands like `cat`, `echo`, `sed`, and `awk` bypass validation hooks at `prompt-template/block/goal-tracker-bash-write.md:1-8`.
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by the caller. Output is rendered markdown shown on stderr by validators. It does not alter state itself; it is a user-facing guard response for a blocked transition.
- gates_or_invariants: The invariant is that goal tracker mutations must flow through Write/Edit-style tools, not Bash, because validators can then enforce round/state constraints and immutable/mutable-section rules. The single placeholder must use valid uppercase syntax, satisfying the loader/test contract.
- dependencies_and_callers: Called by `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh:908-917`, which wraps it with `load_and_render_safe`. Template existence is checked in `tests/test-template-references.sh:152-159`. Bash validation relies on this shared message path when blocking shell writes in active RLCR loops.
- edge_cases_or_failure_modes: If the template is missing or empty, the fallback in `hooks/lib/loop-common.sh:912-916` still blocks with a simpler message. If `CORRECT_PATH` is wrong, user guidance points to the wrong destination, but the gate still blocks unsafe Bash writes. Because it is a static message, it depends on caller-side detection to decide when to emit.
- validation_or_tests: Loaded and syntax-checked by the comprehensive template test’s all-template passes at `tests/test-templates-comprehensive.sh:90-107` and `tests/test-templates-comprehensive.sh:118-194`; specifically cross-referenced as a common template in `tests/test-template-references.sh:152-159`.
- skip_candidate: `no`

### DEV-HZ-179 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template handed back to the implementation agent after Codex review passes, defining the final simplification/refactor transition before loop completion.
- algorithmic_behavior: The prompt declares Codex review passed and implementation complete at `prompt-template/claude/finalize-phase-prompt.md:1-5`. It instructs the agent to invoke `code-simplifier:code-simplifier` via the Task tool at `prompt-template/claude/finalize-phase-prompt.md:7-14`. It sets hard constraints: preserve functionality, keep tests passing, avoid new bugs, and perform only functionality-equivalent cleanup at `prompt-template/claude/finalize-phase-prompt.md:16-24`. It scopes focus to recently changed code, especially changes from `{{BASE_BRANCH}}` to `{{START_BRANCH}}`, and simplification/readability/dedup/control-flow/dead-code cleanup at `prompt-template/claude/finalize-phase-prompt.md:25-35`. It references the original plan and goal tracker at `prompt-template/claude/finalize-phase-prompt.md:36-40`, then requires completion of mainline/blocking tasks, permits queued non-blocking follow-ups, requires a commit, and requires a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` at `prompt-template/claude/finalize-phase-prompt.md:41-52`.
- inputs_outputs_state: Inputs are template variables `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a rendered blocking reason from the stop hook, not a file by itself. It participates in state transition from `state.md` to `finalize-state.md`.
- gates_or_invariants: Finalize must be cleanup-only and functionality-equivalent. Existing tests must not fail. All `[mainline]` and `[blocking]` tasks must be completed before exit; `[queued]` can remain only as documented follow-up. A commit and `finalize-summary.md` are required before final completion.
- dependencies_and_callers: Rendered by `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh:1343-1424` after successful code review. That function renames `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:1349-1350`, computes the summary path, renders this template via `load_and_render_safe` with all variables at `hooks/loop-codex-stop-hook.sh:1390-1415`, and returns a hook block response carrying the prompt. Finalize completion later skips Codex review and moves to methodology analysis or `complete-state.md` once checks pass at `hooks/loop-codex-stop-hook.sh:982-998`.
- edge_cases_or_failure_modes: There is a separate skipped-review template path when `skip_reason` is non-empty at `hooks/loop-codex-stop-hook.sh:1355-1388`; this assigned template is used only for normal review-passed finalization. If the template is missing, the fallback at `hooks/loop-codex-stop-hook.sh:1390-1408` preserves core constraints but with less detail. Validators prevent direct edits to `finalize-state.md` and allow only the active loop’s `finalize-summary.md`, as shown in `hooks/loop-write-validator.sh:183-234` and `hooks/loop-edit-validator.sh:147-183`.
- validation_or_tests: Loaded/syntax-checked by `tests/test-templates-comprehensive.sh` all-template sections. Runtime transition behavior is guarded by `hooks/loop-codex-stop-hook.sh`; setup output documents the overall path: implementation COMPLETE enters Review Phase, code review checks `[P0-9]`, and no issues enters Finalize Phase at `scripts/setup-rlcr-loop.sh:1498-1503`.
- skip_candidate: `no`

### DEV-HZ-209 `file` `skills/humanize-rlcr/SKILL.md`
- cursor: `[_]`
- core_role: Codex flow skill entrypoint for starting and operating the RLCR loop using native Stop hooks. It is a top-level algorithmic contract for loop setup, per-round work, hook-managed transitions, and cancellation.
- algorithmic_behavior: The skill frontmatter identifies `humanize-rlcr` as a non-user-invocable flow with model invocation disabled at `skills/humanize-rlcr/SKILL.md:1-7`. It says Codex installs require native hooks support and install Humanize Stop hooks automatically at `skills/humanize-rlcr/SKILL.md:11-13`. The installer hydrates `{{HUMANIZE_RUNTIME_ROOT}}`, and all commands assume that root at `skills/humanize-rlcr/SKILL.md:14-23`. Required sequence: run `setup-rlcr-loop.sh` with arguments, stop on nonzero setup, then for each round read the generated prompt, implement, commit, write round or finalize summary, exit normally, and let the native Stop hook decide whether to block and continue at `skills/humanize-rlcr/SKILL.md:24-49`.
- inputs_outputs_state: Inputs are flow arguments passed to `setup-rlcr-loop.sh`, especially plan path/options at `skills/humanize-rlcr/SKILL.md:76-95`. Generated state lives under `.humanize/rlcr/<timestamp>/`, with prompts, summaries, `state.md`, and `finalize-state.md`. Outputs include commits, summary files, hook block/continue instructions, and eventual completion/cancel state. The setup command path is `{{HUMANIZE_RUNTIME_ROOT}}/scripts/setup-rlcr-loop.sh` at `skills/humanize-rlcr/SKILL.md:28-32`; cancel path is `{{HUMANIZE_RUNTIME_ROOT}}/scripts/cancel-rlcr-loop.sh` at `skills/humanize-rlcr/SKILL.md:109-113`.
- gates_or_invariants: Native Stop-hook enforcement includes strict state/schema validation, branch consistency, plan integrity, incomplete Task/Todo blocking, git-clean requirement, optional push-every-round blocking, summary presence, max-iteration handling, full-alignment rounds, strict COMPLETE/STOP marker handling, review-phase marker guard, code-review gating on `[P0-9]`, hard blocking on review failure/empty output, and open-question handling at `skills/humanize-rlcr/SKILL.md:50-68`. Critical rules forbid manual state edits, skipping blocked hook results, ad-hoc Codex commands replacing hook-managed transitions, and ignoring generated loop files as source of truth at `skills/humanize-rlcr/SKILL.md:69-75`.
- dependencies_and_callers: Installed by `scripts/install-skill.sh`, which syncs `humanize-rlcr` skills and can install a Kimi-specific variant according to search hits in `scripts/install-skill.sh`. Runtime depends on `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/cancel-rlcr-loop.sh`, generated `.humanize/rlcr` files, and native Codex Stop hooks. Codex review defaults are documented as `gpt-5.5:high` at `skills/humanize-rlcr/SKILL.md:86-98`.
- edge_cases_or_failure_modes: Setup nonzero is terminal for the skill invocation at `skills/humanize-rlcr/SKILL.md:34`. The skill explicitly treats hook block responses as authoritative, so bypassing them or manually editing state is a violation. `--skip-impl` starts directly in review path; `--push-every-round` adds unpushed-commit blocking; `--claude-answer-codex`, `--agent-teams`, `--yolo`, and `--skip-quiz` alter review/question/team behavior at `skills/humanize-rlcr/SKILL.md:90-95`.
- validation_or_tests: Installation tests check Codex/Kimi skill presence and variant behavior in `tests/test-codex-hook-install.sh` per repository search. Runtime behavior is validated across hook/setup/template tests, including nested Codex hook disabling and finalize transitions described above.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 evidence headings present; each assigned evidence heading is unique
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`