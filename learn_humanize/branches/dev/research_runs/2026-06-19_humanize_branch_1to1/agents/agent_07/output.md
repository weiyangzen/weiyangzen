# agent_07 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-007 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Repository-local prompt and block-message template corpus for the Humanize workflows. It supplies the static contracts consumed by hook validators, RLCR round generation, Codex reviews, explore-idea workers, gen-idea/gen-plan, and refine-plan QA. The directory has 67 markdown templates under `block/`, `claude/`, `codex/`, `explore/`, `idea/`, and `plan/`.
- algorithmic_behavior: The directory is not passive documentation; it defines behavioral transitions and fail-closed messages used by shell code. `hooks/lib/template-loader.sh` resolves `prompt-template` from the plugin root, loads templates by relative name, and renders `{{VAR}}` placeholders in a single pass, preserving unresolved placeholders rather than recursively expanding injected content (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:48`, `hooks/lib/template-loader.sh:52`, `hooks/lib/template-loader.sh:56`). `validate_template_dir` requires at least `block`, `codex`, `claude`, and `plan` subdirectories (`hooks/lib/template-loader.sh:213`, `hooks/lib/template-loader.sh:224`). `load_and_render_safe` supplies fallback text when templates are missing or render empty (`hooks/lib/template-loader.sh:185`, `hooks/lib/template-loader.sh:197`, `hooks/lib/template-loader.sh:202`).
- inputs_outputs_state: Inputs are template names plus variable assignments from hook/setup scripts. Outputs are rendered prompts, block reasons, review prompts, worker instructions, and plan/QA scaffold text. State is mostly external: rendered content is written into `.humanize/rlcr/<timestamp>/round-N-prompt.md`, review prompt/result files, or returned as hook stderr/JSON block reasons. Missing templates do not normally mutate state; safe loaders emit fallback content.
- gates_or_invariants: Template syntax uses uppercase placeholder names (`hooks/lib/template-loader.sh:7`). Rendering is single-pass to prevent prompt-content placeholder injection (`hooks/lib/template-loader.sh:52`). Tests assert all templates load and render with dummy values (`tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:563`, `tests/test-templates-comprehensive.sh:584`) and that hook references point to existing templates (`tests/test-template-references.sh:83`, `tests/test-template-references.sh:102`). Critical validators should use `load_and_render_safe` instead of unsafe rendering (`tests/test-template-references.sh:172`, `tests/test-template-references.sh:196`).
- dependencies_and_callers: Main callers include `hooks/loop-codex-stop-hook.sh` for Codex review prompts, next-round prompts, finalize prompts, and push notes (`hooks/loop-codex-stop-hook.sh:1102`, `hooks/loop-codex-stop-hook.sh:1118`, `hooks/loop-codex-stop-hook.sh:1575`, `hooks/loop-codex-stop-hook.sh:2090`, `hooks/loop-codex-stop-hook.sh:2193`); validators for protected plan backup messages (`hooks/loop-write-validator.sh:243`, `hooks/loop-edit-validator.sh:194`, `hooks/loop-bash-validator.sh:498`); `scripts/validate-gen-plan-io.sh` for `plan/gen-plan-template.md`; `scripts/validate-explore-idea-io.sh` for explore worker/report/final-idea templates; and `commands/refine-plan.md` for `plan/refine-plan-qa-template.md`.
- edge_cases_or_failure_modes: Missing or empty templates could otherwise produce blank block messages; fallback rendering mitigates that. Invalid placeholder syntax is test-covered. Some templates are intentionally not directly referenced by hooks because they are used by commands or dynamically, so an “unreferenced” warning is informational rather than fatal (`tests/test-template-references.sh:130`, `tests/test-template-references.sh:139`). Template values can include regex characters, quotes, markdown, long content, and multiline content; rendering tests focus on “no crash” and preservation rather than semantic validation (`tests/test-templates-comprehensive.sh:319`, `tests/test-templates-comprehensive.sh:339`, `tests/test-templates-comprehensive.sh:383`, `tests/test-templates-comprehensive.sh:392`).
- validation_or_tests: `tests/test-template-references.sh` verifies hook-referenced templates exist and critical scripts use safe rendering. `tests/test-templates-comprehensive.sh` loads every template, validates syntax, tests rendering edge cases, verifies fallback behavior, and ensures all templates can render (`tests/test-templates-comprehensive.sh:5`, `tests/test-templates-comprehensive.sh:445`, `tests/test-templates-comprehensive.sh:456`, `tests/test-templates-comprehensive.sh:466`). Additional focused tests cover explore templates (`tests/test-worker-result-contract.sh`, `tests/test-explore-manifest.sh`, `tests/test-explore-command-structure.sh`), gen-plan template parity (`tests/test-gen-plan.sh:948`), and agent-team templates (`tests/test-agent-teams.sh:331`).
- skip_candidate: `no`

### DEV-HZ-037 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting an RLCR loop. It front-loads plan compliance, user understanding checks, and then delegates actual state initialization to `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The command first extracts the plan path from `$ARGUMENTS`, preferring `--plan-file` and otherwise the first safe positional argument (`commands/start-rlcr-loop.md:21`). It applies an advisory path-safety gate before reading: relative path only, no parent traversal, safe characters only (`commands/start-rlcr-loop.md:28`). It invokes `humanize:plan-compliance-checker` through the Task tool and fails closed unless output contains `PASS`; `FAIL_RELEVANCE` and `FAIL_BRANCH_SWITCH` stop the command (`commands/start-rlcr-loop.md:41`, `commands/start-rlcr-loop.md:52`). It then optionally runs a plan-understanding quiz, skipping for `--skip-impl`, `--yolo`, `--skip-quiz`, help, or unavailable plan content (`commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:64`). Wrong quiz answers do not automatically block; the user is asked whether to proceed or stop (`commands/start-rlcr-loop.md:96`, `commands/start-rlcr-loop.md:100`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, plan file content, Task-agent outputs, and optional user answers via `AskUserQuestion`. The command’s direct output is setup script invocation and explanatory text. Persistent state is created by `setup-rlcr-loop.sh`: `.humanize/rlcr/<timestamp>/state.md`, protected plan backup, goal tracker, summary scaffold, round contract, and round prompt (`scripts/setup-rlcr-loop.sh:826`, `scripts/setup-rlcr-loop.sh:834`, `scripts/setup-rlcr-loop.sh:885`, `scripts/setup-rlcr-loop.sh:934`, `scripts/setup-rlcr-loop.sh:1200`).
- gates_or_invariants: The command must not proceed on malformed compliance-check output (`commands/start-rlcr-loop.md:52`). It defines one round as “agent believes the entire plan is finished,” not one task/stage (`commands/start-rlcr-loop.md:130`). It establishes Goal Tracker invariants: immutable ultimate goal/ACs and mutable active/completed/deferred tracking (`commands/start-rlcr-loop.md:139`). It prohibits cheating by editing state files or running cancel commands (`commands/start-rlcr-loop.md:160`). It defines BitLesson per-round summary requirements (`commands/start-rlcr-loop.md:168`).
- dependencies_and_callers: Allowed tools are `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)`, `Read`, `Task`, and `AskUserQuestion` (`commands/start-rlcr-loop.md:4`). The actual setup script enforces required dependencies (`codex`, `jq`, `git`), single active loop, plan path safety, clean worktree, branch/base selection, model/effort validation, and prompt/state writing (`scripts/setup-rlcr-loop.sh:340`, `scripts/setup-rlcr-loop.sh:374`, `scripts/setup-rlcr-loop.sh:450`, `scripts/setup-rlcr-loop.sh:723`, `scripts/setup-rlcr-loop.sh:748`).
- edge_cases_or_failure_modes: Pre-check is skipped for help/skip-impl or unsafe path and leaves final validation to setup. Quiz-agent malformed output warns and continues (`commands/start-rlcr-loop.md:86`). In setup, `--skip-impl` can run without a plan, creating an internal placeholder (`scripts/setup-rlcr-loop.sh:413`, `scripts/setup-rlcr-loop.sh:835`). Plan files are rejected if absolute, contain spaces/shell metacharacters, are symlinks, escape the project, live inside submodules, are tracked without `--track-plan-file`, or are untracked when tracking is requested (`scripts/setup-rlcr-loop.sh:460`, `scripts/setup-rlcr-loop.sh:466`, `scripts/setup-rlcr-loop.sh:474`, `scripts/setup-rlcr-loop.sh:486`, `scripts/setup-rlcr-loop.sh:534`, `scripts/setup-rlcr-loop.sh:548`, `scripts/setup-rlcr-loop.sh:587`, `scripts/setup-rlcr-loop.sh:603`).
- validation_or_tests: Related tests include command/source checks in `tests/test-rlcr-sources.sh`, plan-file validation tests, base-branch detection robustness tests, setup-script robustness tests, task-tag routing tests, and viz tests confirming this command no longer references the old viz command (`tests/test-viz.sh:452`). I did not execute tests because the branch export is read-only and not a full git checkout; inspection only was requested.
- skip_candidate: `no`

### DEV-HZ-067 `file` `scripts/validate-refine-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime IO validator for `/humanize:refine-plan`. It validates annotated plan inputs, output target writability, QA directory setup, required gen-plan sections, and comment-block syntax before the command performs any plan mutation.
- algorithmic_behavior: The script defines two AWK scanners. `scan_cmt_blocks` recognizes `CMT:/ENDCMT`, `<cmt></cmt>`, and `<comment></comment>` blocks, ignores markers in fenced code and HTML comments, counts only non-empty comment blocks, and fails on nested starts, stray ends, or missing end markers (`scripts/validate-refine-plan-io.sh:16`, `scripts/validate-refine-plan-io.sh:62`, `scripts/validate-refine-plan-io.sh:139`, `scripts/validate-refine-plan-io.sh:202`, `scripts/validate-refine-plan-io.sh:227`, `scripts/validate-refine-plan-io.sh:294`, `scripts/validate-refine-plan-io.sh:303`). `scan_sections` strips fenced code, HTML comments, and CMT blocks before emitting visible markdown headings, so required sections hidden inside ignored regions do not satisfy schema checks (`scripts/validate-refine-plan-io.sh:318`, `scripts/validate-refine-plan-io.sh:339`, `scripts/validate-refine-plan-io.sh:378`, `scripts/validate-refine-plan-io.sh:472`).
- inputs_outputs_state: Inputs are CLI flags `--input`, optional `--output`, optional `--qa-dir`, and mutually exclusive `--discussion`/`--direct` (`scripts/validate-refine-plan-io.sh:480`, `scripts/validate-refine-plan-io.sh:493`, `scripts/validate-refine-plan-io.sh:499`). Output defaults to input for in-place mode (`scripts/validate-refine-plan-io.sh:556`). It prints absolute input/output paths, QA directory, validation errors, or `VALIDATION_SUCCESS` plus mode and counts (`scripts/validate-refine-plan-io.sh:567`, `scripts/validate-refine-plan-io.sh:680`). The only intended filesystem side effect is auto-creating missing `QA_DIR` (`scripts/validate-refine-plan-io.sh:662`).
- gates_or_invariants: Exit codes are explicit: 1 missing input, 2 empty input, 3 no valid/malformed comment blocks, 4 missing required gen-plan sections, 5 output/input directory not writable, 6 QA directory not writable, 7 invalid arguments (`scripts/validate-refine-plan-io.sh:4`). It requires nine gen-plan sections, including `## Goal Description`, `## Acceptance Criteria`, `## Task Breakdown`, and `## Implementation Notes` (`scripts/validate-refine-plan-io.sh:608`). `--discussion` and `--direct` are mutually exclusive (`scripts/validate-refine-plan-io.sh:544`). It treats in-place mode specially by requiring the input directory to be writable for atomic replacement (`scripts/validate-refine-plan-io.sh:653`).
- dependencies_and_callers: Called by `commands/refine-plan.md` Phase 1, which passes only validator-supported arguments and explicitly excludes `--alt-language` (`commands/refine-plan.md:72`, `commands/refine-plan.md:157`, `commands/refine-plan.md:161`). The command maps validator exits to user-facing stop reasons and treats QA directory auto-creation as expected setup (`commands/refine-plan.md:165`, `commands/refine-plan.md:176`). It depends on `bash`, `awk`, `realpath -m`, `grep`, `wc`, and standard POSIX-ish filesystem predicates.
- edge_cases_or_failure_modes: Handles nested comments, stray end markers, missing end markers, empty comment blocks, comments inside HTML/fences, required sections only inside ignored regions, missing/non-writable output directory, non-writable input directory for in-place mode, and invalid/non-directory QA path. One portability consideration: `realpath -m` is GNU-style; the script falls back to the original path if `realpath` fails (`scripts/validate-refine-plan-io.sh:561`).
- validation_or_tests: `tests/test-refine-plan.sh` has focused validator checks: required sections in fences or HTML comments exit 4 (`tests/test-refine-plan.sh:1211`, `tests/test-refine-plan.sh:1220`); missing/non-writable output dirs exit 5 and report specific errors (`tests/test-refine-plan.sh:1231`, `tests/test-refine-plan.sh:1238`); non-writable input dir in in-place mode exits 5 (`tests/test-refine-plan.sh:1256`); non-directory QA path exits 6 (`tests/test-refine-plan.sh:1287`); valid in-place and new-file modes exit 0 and report mode/target (`tests/test-refine-plan.sh:1297`, `tests/test-refine-plan.sh:1334`); mixed real/ignored comments count only valid non-empty CMT blocks (`tests/test-refine-plan.sh:1318`).
- skip_candidate: `no`

### DEV-HZ-097 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared executable-test harness library. It standardizes pass/fail/skip counters, colored output, summary exit behavior, temporary test directories, and mock git repository initialization for shell tests.
- algorithmic_behavior: `pass`, `fail`, and `skip` print colored status lines and increment global counters (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:46`). `print_test_summary` reports counts and returns success only when `TESTS_FAILED` is zero (`tests/test-helpers.sh:58`, `tests/test-helpers.sh:71`). `setup_test_dir` creates a temporary directory and registers cleanup on shell exit (`tests/test-helpers.sh:84`). `init_test_git_repo` creates a minimal git repo with user config, disabled GPG signing, an initial file, and initial commit (`tests/test-helpers.sh:91`).
- inputs_outputs_state: Inputs are optional summary title, target directory path for git init, and test names/messages. Outputs are terminal lines and shell return status from `print_test_summary`. State is global shell variables `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, plus `TEST_DIR` created by `setup_test_dir`.
- gates_or_invariants: Tests using this helper should increment counters rather than exiting immediately on a failed assertion, then call `print_test_summary` to determine final exit. Temporary directories are deleted by the `EXIT` trap, so tests assume one active `TEST_DIR` per script. `init_test_git_repo` changes directories and returns to the previous directory, making it safe for callers that rely on current working directory restoration (`tests/test-helpers.sh:96`, `tests/test-helpers.sh:104`).
- dependencies_and_callers: Used by many scripts under `tests/` and `tests/robustness/`; the assigned `tests/robustness/test-hook-system-robustness.sh` sources it at line 23 and relies on `setup_test_dir`, `pass`, `fail`, and `print_test_summary`.
- edge_cases_or_failure_modes: `setup_test_dir` installs a single `trap`; if a test installs another EXIT trap without chaining, cleanup may be overwritten. `init_test_git_repo` assumes `git` exists and can create commits. The helper intentionally does not set `set -euo pipefail`; calling scripts own strict-mode policy.
- validation_or_tests: This file is itself a test support dependency rather than a directly tested subject. Its behavior is indirectly validated by all tests that source it and complete summary reporting, including the hook-system robustness test ending through `print_test_summary` (`tests/robustness/test-hook-system-robustness.sh:848`).
- skip_candidate: `no`

### DEV-HZ-127 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for monitor runtime safety under zsh, especially avoiding zsh `nomatch` glob failures in empty or dotfile-only `.humanize/rlcr` directories.
- algorithmic_behavior: The test must run under zsh (`tests/test-zsh-monitor-safety.sh:1`, `tests/test-zsh-monitor-safety.sh:5`). It creates an isolated `/tmp/test-zsh-humanize-$$` workspace, isolated `XDG_CACHE_HOME`, and cleanup trap (`tests/test-zsh-monitor-safety.sh:48`, `tests/test-zsh-monitor-safety.sh:52`, `tests/test-zsh-monitor-safety.sh:56`). It sources `scripts/humanize.sh` and fails if output includes “no matches found” (`tests/test-zsh-monitor-safety.sh:71`). It then recreates monitor search patterns using `find` loops for latest sessions, state files, and Codex logs (`tests/test-zsh-monitor-safety.sh:99`, `tests/test-zsh-monitor-safety.sh:172`, `tests/test-zsh-monitor-safety.sh:221`).
- inputs_outputs_state: Inputs are the repo root, generated `.humanize/rlcr` directories/files, and generated cache directories. Outputs are PASS/FAIL counters and process exit 0 only if no failures (`tests/test-zsh-monitor-safety.sh:323`, `tests/test-zsh-monitor-safety.sh:333`). State is confined to the temp workspace and deleted on exit.
- gates_or_invariants: The monitor must not use unguarded globs over possibly empty directories. Session discovery accepts only timestamp directories matching `YYYY-MM-DD_HH-MM-SS` and picks the lexical latest (`tests/test-zsh-monitor-safety.sh:109`, `tests/test-zsh-monitor-safety.sh:267`). State discovery prioritizes `state.md`, then any `*-state.md`, otherwise returns unknown (`tests/test-zsh-monitor-safety.sh:177`, `tests/test-zsh-monitor-safety.sh:181`, `tests/test-zsh-monitor-safety.sh:192`). The final test demonstrates the old glob pattern risk and the new `find` pattern safety (`tests/test-zsh-monitor-safety.sh:289`, `tests/test-zsh-monitor-safety.sh:307`, `tests/test-zsh-monitor-safety.sh:314`).
- dependencies_and_callers: Targets `scripts/humanize.sh` and its shared `scripts/lib/monitor-common.sh`. The implementation under test delegates `_find_latest_session` to `monitor_find_latest_session`, which explicitly uses `find` to avoid zsh “no matches found” (`scripts/humanize.sh:279`, `scripts/lib/monitor-common.sh:37`, `scripts/lib/monitor-common.sh:49`). `_find_state_file` delegates to `monitor_find_state_file` (`scripts/humanize.sh:377`, `scripts/lib/monitor-common.sh:153`). Codex log lookup in `humanize.sh` searches cache files via `find` and validates implementation/review log ordering (`scripts/humanize.sh:338`, `scripts/humanize.sh:362`, `scripts/humanize.sh:364`).
- edge_cases_or_failure_modes: Empty loop dirs, dotfile-only dirs, sessions without state files, empty cache dirs, multiple timestamped sessions, and strict zsh behavior are all covered. The test does not execute the full interactive monitor UI; it verifies the file-discovery primitives and source-time safety.
- validation_or_tests: This file is the validation artifact. It reports eight focused tests and exits nonzero if any fail (`tests/test-zsh-monitor-safety.sh:38`, `tests/test-zsh-monitor-safety.sh:327`, `tests/test-zsh-monitor-safety.sh:333`).
- skip_candidate: `no`

### DEV-HZ-157 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for protecting the copied `plan.md` inside an active `.humanize/rlcr/<session>/` loop directory. It preserves the original plan backup as a read-only session anchor.
- algorithmic_behavior: The template tells the agent that loop-local `plan.md` is a backup and cannot be modified, because it ensures plan integrity, and should be read only (`prompt-template/block/plan-backup-protected.md:1`, `prompt-template/block/plan-backup-protected.md:3`, `prompt-template/block/plan-backup-protected.md:7`). It has no placeholders, so rendering is static.
- inputs_outputs_state: Input is any hook-detected attempt to write/edit/modify `.humanize/rlcr/.../plan.md`. Output is this rendered error text to stderr and hook exit 2. No state is changed; the attempted write is blocked.
- gates_or_invariants: Plan backup is read-only in all rounds. `loop-write-validator.sh` blocks Write to loop-local `plan.md` (`hooks/loop-write-validator.sh:237`, `hooks/loop-write-validator.sh:240`). `loop-edit-validator.sh` blocks Edit to the same file (`hooks/loop-edit-validator.sh:187`, `hooks/loop-edit-validator.sh:191`). `loop-bash-validator.sh` blocks shell commands that modify `.humanize/rlcr/.../plan.md` (`hooks/loop-bash-validator.sh:490`, `hooks/loop-bash-validator.sh:496`). All callers use `load_and_render_safe` with inline fallback text.
- dependencies_and_callers: Called by `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`. The protected file is created by `setup-rlcr-loop.sh` when copying the source plan into the loop directory (`scripts/setup-rlcr-loop.sh:834`, `scripts/setup-rlcr-loop.sh:855`).
- edge_cases_or_failure_modes: If this template is missing, callers fall back to “Writing/Editing plan.md backup is not allowed during RLCR loop.” The template has no variable interpolation risks. Detection is path-based and scoped to `.humanize/rlcr`, so normal project plan files are handled by other plan-file validators rather than this block.
- validation_or_tests: Template reference tests ensure `block/plan-backup-protected.md` exists when referenced. Hook robustness tests cover related state/plan protections broadly, while plan-file and hook-input robustness tests cover path normalization and protected loop-file behavior.
- skip_candidate: `no`

### DEV-HZ-187 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Conditional prompt fragment appended to round prompts when RLCR state has `push_every_round: true`, instructing the agent to push commits after each round.
- algorithmic_behavior: The file is a two-line static note: when `--push-every-round` is enabled, push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:1`, `prompt-template/claude/push-every-round-note.md:2`). It has no placeholders or state mutation itself.
- inputs_outputs_state: Input is the `PUSH_EVERY_ROUND` boolean in loop state or setup options. Output is prompt text appended to `round-N-prompt.md`. State transition originates in setup parsing `--push-every-round` (`scripts/setup-rlcr-loop.sh:233`) and writing `push_every_round: $PUSH_EVERY_ROUND` to `state.md` (`scripts/setup-rlcr-loop.sh:892`).
- gates_or_invariants: The note is appended only when `PUSH_EVERY_ROUND` is true in next-round generation (`hooks/loop-codex-stop-hook.sh:2191`, `hooks/loop-codex-stop-hook.sh:2193`). Setup also writes an equivalent inline note into round-0 prompts when push-every-round is enabled (`scripts/setup-rlcr-loop.sh:1438`, `scripts/setup-rlcr-loop.sh:1442`). Stop hooks separately enforce unpushed-commit blocking via `block/unpushed-commits.md`; this note is the instruction layer, not the enforcement layer.
- dependencies_and_callers: Used by `hooks/loop-codex-stop-hook.sh` during next-round prompt construction. Setup uses inline text for round 0 rather than loading this template, creating a small duplication but preserving the same behavior.
- edge_cases_or_failure_modes: If the template is missing, `loop-codex-stop-hook.sh` falls back to “Also push your changes after committing.” (`hooks/loop-codex-stop-hook.sh:2193`, `hooks/loop-codex-stop-hook.sh:2194`). Since it is static, no render failure or placeholder mismatch is possible. Mis-set `push_every_round` state would either omit needed push instructions or add unnecessary push instructions.
- validation_or_tests: Template reference tests detect that the file exists for the hook reference. Push enforcement is covered indirectly through stop-hook and git-state tests; the specific fragment is simple enough that existence and caller-path validation are the relevant checks.
- skip_candidate: `no`

### DEV-HZ-217 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Large executable robustness specification for hook validators and stop-hook state handling. It verifies JSON handling, command/path protections, goal-tracker immutability, active-loop stop blocking, malformed-state behavior, and concurrent invocation safety.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and shared test helpers, then creates an isolated temp directory (`tests/robustness/test-hook-system-robustness.sh:19`, `tests/robustness/test-hook-system-robustness.sh:21`, `tests/robustness/test-hook-system-robustness.sh:25`). It executes hook scripts by piping JSON into them with `CLAUDE_PROJECT_DIR` pointing at test workspaces. Test groups cover Edit validator behavior, plan-file validator behavior, state parsing, command injection prevention, concurrent hook invocations, stop-hook state parsing, and JSON edge cases.
- inputs_outputs_state: Inputs are generated hook JSON payloads for `Read`, `Write`, `Edit`, and `Bash`; generated `.humanize/rlcr/<timestamp>/state.md`; generated goal trackers; temporary git repos; and a mock `codex` binary for active-loop stop-hook tests (`tests/robustness/test-hook-system-robustness.sh:39`, `tests/robustness/test-hook-system-robustness.sh:55`, `tests/robustness/test-hook-system-robustness.sh:166`, `tests/robustness/test-hook-system-robustness.sh:769`). Outputs are pass/fail counters and final exit status from `print_test_summary` (`tests/robustness/test-hook-system-robustness.sh:848`). Some tests intentionally check state-file preservation or rename to `unexpected-state.md` for invalid loops (`tests/robustness/test-hook-system-robustness.sh:722`).
- gates_or_invariants: State files must not be modified through Edit, Bash, or traversal paths (`tests/robustness/test-hook-system-robustness.sh:52`, `tests/robustness/test-hook-system-robustness.sh:123`, `tests/robustness/test-hook-system-robustness.sh:316`). Paths outside `.humanize` are intentionally allowed through and delegated to the sandbox (`tests/robustness/test-hook-system-robustness.sh:108`, `tests/robustness/test-hook-system-robustness.sh:559`). Plan-file edits are allowed only for the configured plan and non-plan files pass through (`tests/robustness/test-hook-system-robustness.sh:164`, `tests/robustness/test-hook-system-robustness.sh:216`). After round 0, goal-tracker Write/Edit may modify the mutable section but must preserve the immutable section; Bash modifications to goal-tracker are blocked (`tests/robustness/test-hook-system-robustness.sh:402`, `tests/robustness/test-hook-system-robustness.sh:418`, `tests/robustness/test-hook-system-robustness.sh:467`, `tests/robustness/test-hook-system-robustness.sh:499`, `tests/robustness/test-hook-system-robustness.sh:531`). Current active loops with valid state must block exit via decision block (`tests/robustness/test-hook-system-robustness.sh:736`, `tests/robustness/test-hook-system-robustness.sh:784`).
- dependencies_and_callers: Depends on `jq`, `bash`, `git`, `mktemp`, `run_with_timeout`, and the hook scripts under `hooks/`: `loop-edit-validator.sh`, `loop-plan-file-validator.sh`, `loop-bash-validator.sh`, `loop-write-validator.sh`, `loop-read-validator.sh`, and `loop-codex-stop-hook.sh`. It also exercises `get_current_round` from `loop-common.sh` (`tests/robustness/test-hook-system-robustness.sh:263`, `tests/robustness/test-hook-system-robustness.sh:282`, `tests/robustness/test-hook-system-robustness.sh:300`).
- edge_cases_or_failure_modes: Covers malformed JSON, missing JSON fields, traversal to protected files, empty JSON, frontmatter with whitespace/special values, malformed state without closing delimiter, binary-looking content, newlines in strings, large payloads, escaped nulls, deeply nested JSON, corrupted state, missing critical state fields, and ten concurrent read-validator invocations (`tests/robustness/test-hook-system-robustness.sh:79`, `tests/robustness/test-hook-system-robustness.sh:93`, `tests/robustness/test-hook-system-robustness.sh:231`, `tests/robustness/test-hook-system-robustness.sh:289`, `tests/robustness/test-hook-system-robustness.sh:574`, `tests/robustness/test-hook-system-robustness.sh:589`, `tests/robustness/test-hook-system-robustness.sh:611`, `tests/robustness/test-hook-system-robustness.sh:670`, `tests/robustness/test-hook-system-robustness.sh:693`, `tests/robustness/test-hook-system-robustness.sh:800`, `tests/robustness/test-hook-system-robustness.sh:814`, `tests/robustness/test-hook-system-robustness.sh:828`).
- validation_or_tests: This file is itself the validation suite. It ends through `print_test_summary "Hook System Robustness Test Summary"` and exits with that status (`tests/robustness/test-hook-system-robustness.sh:848`). I inspected but did not run it because the task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`