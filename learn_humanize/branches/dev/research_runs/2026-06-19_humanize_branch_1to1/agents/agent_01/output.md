# agent_01 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude plugin. It coordinates the RLCR implementation/review loop, idea generation/exploration/planning flows, hook-based runtime enforcement, prompt/template rendering, configuration, tests, skills, docs, and the monitor dashboard.
- algorithmic_behavior: Recursive inspection shows the root is organized around command entrypoints (`commands/`), runtime scripts (`scripts/`), hook validators and stop hooks (`hooks/`), prompt blocks (`prompt-template/`), executable specifications (`tests/`), plugin skills (`skills/`), docs, and `viz/`. `commands/start-rlcr-loop.md:13` runs plan compliance, `commands/start-rlcr-loop.md:60` runs the understanding quiz, then `commands/start-rlcr-loop.md:109` invokes `scripts/setup-rlcr-loop.sh`. `hooks/hooks.json:14` routes PreToolUse validators, `hooks/hooks.json:52` routes PostToolUse session capture, and `hooks/hooks.json:63` routes Stop to the Codex stop hook. `commands/explore-idea.md:41` defines a separate six-phase bounded worktree exploration algorithm.
- inputs_outputs_state: Inputs include plan files, idea drafts, directions JSON, `.humanize/config.json`, hook JSON payloads, git branch state, Codex/JQ/Git availability, and prompt templates. Outputs include `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-N-prompt.md`, summaries, review results, BitLesson files, explore manifests, `worker-results.jsonl`, `explore-report.md`, `final-idea.md`, and monitor-readable session data. RLCR state moves from setup to implementation review, code review, finalize, cancel, max-iteration, or stop-gate paths; explore state moves from validation to confirmed manifest, worker results, and synthesis.
- gates_or_invariants: Repository-level gates include safe plan path validation, clean git tree, single active loop, local base branch selection, model/effort validation, hook-enforced file access, no direct state tampering, explicit explore dispatch confirmation, manifest-before-worker dispatch, no remote push in explore, and structured worker result capture. `config/default_config.json:2` seeds Codex defaults and `tests/run-all-tests.sh:59` lists the broad validation surface.
- dependencies_and_callers: Parent path is only the read-only branch export. Sibling coordination is internal: commands call scripts; scripts source `hooks/lib/loop-common.sh` and `scripts/portable-timeout.sh`; hooks render `prompt-template/`; tests assert command/script/template contracts; `viz/server` parses runtime files produced by RLCR sessions; `README.md:42` documents the user flow from idea to explore to plan to loop to monitor.
- edge_cases_or_failure_modes: The export has no usable `.git` metadata, so production git behavior was inspected through script/test contents rather than repository commands. Runtime edge handling includes missing dependencies, origin-less repos, remote-only base branches, symlink/traversal/metacharacter plan paths, dirty working trees, malformed config, stale state keys, missing worker sentinels, all explore workers failing, and old/incorrect goal-tracker access.
- validation_or_tests: `tests/run-all-tests.sh:59` enumerates shell, hook, config, explore, monitor, and robustness suites, with mocks for missing Codex/timeout binaries at `tests/run-all-tests.sh:155`. Assigned tests are part of that harness. No files were modified and no full test run was executed for this research-only pass.
- skip_candidate: `no`

### DEV-HZ-031 `file` `agents/plan-understanding-quiz.md`
- cursor: `[_]`
- core_role: Agent prompt contract for the advisory plan-understanding preflight before an RLCR loop starts.
- algorithmic_behavior: The agent receives plan content, reads it thoroughly, explores repository context, then emits two implementation-focused multiple-choice questions and a short technical summary. Key sections: task definition at `agents/plan-understanding-quiz.md:14`, repo exploration requirements at `agents/plan-understanding-quiz.md:24`, two-question contract at `agents/plan-understanding-quiz.md:31`, question quality rules at `agents/plan-understanding-quiz.md:36`, summary generation at `agents/plan-understanding-quiz.md:53`, and strict output schema at `agents/plan-understanding-quiz.md:59`.
- inputs_outputs_state: Input is the plan file content plus repository context discovered through Read/Glob/Grep. Output is exactly 13 structured fields: two questions, eight options, two answers, and `PLAN_SUMMARY`. It does not write persistent state itself; it gates the command-level decision path in `commands/start-rlcr-loop.md:60`.
- gates_or_invariants: Must generate exactly two questions, each with four A-D options and one correct answer. Answers must be a single A-D letter, correct-answer positions should vary, output language follows the plan language, and questions must test implementation understanding rather than generic plan awareness.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md:75` through the Task tool using the `humanize:plan-understanding-quiz` agent. It depends on the start command to parse fields, ask the user via `AskUserQuestion`, and decide whether to proceed.
- edge_cases_or_failure_modes: If the quiz output is malformed, the start command warns and continues without the quiz (`commands/start-rlcr-loop.md:86`). If answers are wrong, the command shows the summary and asks whether to proceed or stop (`commands/start-rlcr-loop.md:100`). Very short or low-detail plans fall back to available implementation hints (`agents/plan-understanding-quiz.md:84`).
- validation_or_tests: No dedicated test file was found for this agent prompt in the assigned set. Its behavior is indirectly specified by `commands/start-rlcr-loop.md:60` and the exact output schema inside the prompt.
- skip_candidate: `no`

### DEV-HZ-061 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Main runtime initializer for `/humanize:start-rlcr-loop`; it validates prerequisites, freezes loop configuration, creates RLCR session state, initializes goal tracking, and emits the initial prompt/contract scaffolds.
- algorithmic_behavior: Defaults and sourced libraries are established at `scripts/setup-rlcr-loop.sh:17` and `scripts/setup-rlcr-loop.sh:25`. CLI parsing spans `scripts/setup-rlcr-loop.sh:189`, including `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--skip-impl`, quiz/yolo flags, Agent Teams, BitLesson strictness, and privacy. Prerequisites check project root, dependencies, active-loop mutual exclusion, and Agent Teams env at `scripts/setup-rlcr-loop.sh:331`, `scripts/setup-rlcr-loop.sh:345`, `scripts/setup-rlcr-loop.sh:374`, and `scripts/setup-rlcr-loop.sh:390`. Plan validation covers path safety, symlinks, project containment, submodules, tracking policy, and content at `scripts/setup-rlcr-loop.sh:450` and `scripts/setup-rlcr-loop.sh:620`. Branch/model/git gates run at `scripts/setup-rlcr-loop.sh:691`, `scripts/setup-rlcr-loop.sh:707`, and `scripts/setup-rlcr-loop.sh:728`. Base branch resolution is implemented at `scripts/setup-rlcr-loop.sh:753`, state emission at `scripts/setup-rlcr-loop.sh:885`, goal tracker creation at `scripts/setup-rlcr-loop.sh:934`, and prompt generation at `scripts/setup-rlcr-loop.sh:1200`.
- inputs_outputs_state: Inputs are the plan path or skip-impl mode, CLI options, config-backed `DEFAULT_CODEX_MODEL` and `DEFAULT_CODEX_EFFORT`, the current git branch, base branch refs, and templates. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `.pending-session-id`, optional `.review-phase-started`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, `round-0-prompt.md`, and initialized `.humanize/bitlesson.md`. State starts at `current_round: 0`; `review_started` is false in normal mode and true in skip-impl mode; `bitlesson_required` is disabled for review-only skip-impl.
- gates_or_invariants: Requires `codex`, `jq`, and `git`; exactly one active loop; git repo with at least one commit; safe relative plan paths; no spaces or shell metacharacters; no symlink traversal; plan must be either clean tracked with `--track-plan-file` or untracked/gitignored otherwise; minimum plan line/content counts; clean working tree excluding `.humanize` runtime paths; YAML-safe branch names; Codex model chars and effort among `xhigh|high|medium|low`; base branch must be local; base commit is captured to avoid diff drift.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md:113` and referenced by `commands/gen-plan.md` auto-start guidance. It sources `scripts/portable-timeout.sh:38` for portable timeouts and `hooks/lib/loop-common.sh:24` for state fields, config defaults, active-loop helpers, and templates. It calls `scripts/bitlesson-init.sh` at `scripts/setup-rlcr-loop.sh:868`. `hooks/loop-post-bash-hook.sh` consumes `.pending-session-id`; `hooks/loop-codex-stop-hook.sh` consumes `state.md`, summaries, goal tracker, and review markers.
- edge_cases_or_failure_modes: Handles `--skip-impl` with no plan by writing an internal placeholder; handles `--skip-impl` with a plan anchor by extracting plan goal/ACs; blocks remote-only user base branches with a fetch hint; fails closed on git timeouts; blocks malformed branch/model values before YAML state write; warns/ignores tracked-plan flag in skip-impl without plan; suppresses SIGPIPE only after critical setup is complete.
- validation_or_tests: Covered by `tests/test-unified-codex-config.sh:382`, `tests/robustness/test-base-branch-detection.sh:35`, `tests/test-plan-file-validation.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, `tests/test-session-id.sh`, and hook/state transition suites listed in `tests/run-all-tests.sh:59`.
- skip_candidate: `no`

### DEV-HZ-091 `file` `tests/test-explore-command-structure.sh`
- cursor: `[_]`
- core_role: Executable structural specification for the `/humanize:explore-idea` command and its required artifacts.
- algorithmic_behavior: The test sets canonical paths at `tests/test-explore-command-structure.sh:20`, asserts command/validator existence at `tests/test-explore-command-structure.sh:30`, verifies allowed tools at `tests/test-explore-command-structure.sh:46`, checks all six workflow phases at `tests/test-explore-command-structure.sh:99`, verifies hard constraints at `tests/test-explore-command-structure.sh:120`, validates Agent worktree dispatch at `tests/test-explore-command-structure.sh:159`, checks sentinel/JSONL result collection at `tests/test-explore-command-structure.sh:183`, asserts report and final-idea template placeholders at `tests/test-explore-command-structure.sh:202`, verifies productization/adoption paths at `tests/test-explore-command-structure.sh:293`, and checks validator exit codes at `tests/test-explore-command-structure.sh:347`.
- inputs_outputs_state: Inputs are `commands/explore-idea.md`, `scripts/validate-explore-idea-io.sh`, `prompt-template/explore/report-template.md`, `prompt-template/explore/final-idea-template.md`, and `docs/usage.md`. Outputs are pass/fail counters through `tests/test-helpers.sh`; it does not mutate repository state.
- gates_or_invariants: Enforces no remote push, explicit confirmation before dispatch, manifest before workers, no nested skills, single Agent-tool batch language, `isolation: "worktree"`, worker branch format, sentinel result parsing, mandatory JSONL persistence, two-tier report ranking, final idea plan-readiness, clean productization path before prototype fast path, first-colon key parsing, and validator exits 1-9.
- dependencies_and_callers: Sourced helper is `tests/test-helpers.sh:30`. The test mirrors the command algorithm in `commands/explore-idea.md:23` and the validator contract in `scripts/validate-explore-idea-io.sh`.
- edge_cases_or_failure_modes: Grep-based assertions are intentionally structural and can fail on wording changes even if semantics remain. It checks alternate text patterns for several constraints, but missing placeholders, renamed artifacts, missing sentinel markers, or changed validator exit codes hard-fail.
- validation_or_tests: Included in the global harness at `tests/run-all-tests.sh:98`. This file is itself validation evidence for the explore command’s algorithmic contract.
- skip_candidate: `no`

### DEV-HZ-121 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: Executable specification for unified `codex_model` / `codex_effort` configuration across config defaults, config merge, runtime setup, stop hook, stale key handling, and `ask-codex`.
- algorithmic_behavior: Helper assertions are defined at `tests/test-unified-codex-config.sh:21`. It validates default config keys at `tests/test-unified-codex-config.sh:62`, config merge hierarchy at `tests/test-unified-codex-config.sh:89`, `loop-common.sh` config-backed defaults at `tests/test-unified-codex-config.sh:131`, stop-hook state/default fallback at `tests/test-unified-codex-config.sh:257`, setup script state field names at `tests/test-unified-codex-config.sh:382`, stale reviewer-key ignoring at `tests/test-unified-codex-config.sh:396`, invalid stop-hook effort rejection at `tests/test-unified-codex-config.sh:477`, real setup execution with override at `tests/test-unified-codex-config.sh:552`, validation regexes at `tests/test-unified-codex-config.sh:641`, and `ask-codex` config defaults at `tests/test-unified-codex-config.sh:681`.
- inputs_outputs_state: Inputs are `config/default_config.json`, `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/ask-codex.sh`, temp project configs, temp state files, and mock Codex binaries. Outputs are pass/fail results, temporary configs/repos/state files, and captured stderr/output for behavior checks.
- gates_or_invariants: `default_config.json` must contain `codex_model: gpt-5.5` and `codex_effort: high`, while stale `loop_reviewer_*` keys must be absent/ignored. Caller-provided defaults override config. Invalid characters or unsupported non-Codex model prefixes warn and fall back. Valid efforts are only `xhigh`, `high`, `medium`, `low`. Stop hook must reject invalid effort before invoking Codex. Setup state must emit `codex_model` and `codex_effort`, not stale reviewer fields.
- dependencies_and_callers: Depends on `jq` for JSON checks, `scripts/portable-timeout.sh:38`, `tests/test-helpers.sh:86`, `hooks/lib/loop-common.sh:208`, and temp git repo helpers. It tests behavior used by setup, stop-hook, and `ask-codex`.
- edge_cases_or_failure_modes: Skips specific sections if `jq`, Codex, or target files are unavailable. Covers stale config/state keys, bare state without Codex fields, project overrides, caller presets, malformed config values, shell-safe but unsupported models, invalid state effort, and setup execution against a local bare remote to avoid network use.
- validation_or_tests: Included in `tests/run-all-tests.sh:89`. This file is the primary regression test for the config-key migration and Codex default propagation.
- skip_candidate: `no`

### DEV-HZ-151 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Stop-hook block template used when Round 0 attempts to exit before initializing required goal-tracker content.
- algorithmic_behavior: The template names the failure, inserts the target tracker path through `{{GOAL_TRACKER_FILE}}`, lists missing items through `{{MISSING_ITEMS}}`, requires reading the tracker, replacing placeholders with an ultimate goal, 3-7 testable acceptance criteria, and Active Tasks mapped to ACs, then tells the agent to update `goal-tracker.md` and retry. Immutable-section timing is emphasized at `prompt-template/block/goal-tracker-not-initialized.md:16`.
- inputs_outputs_state: Inputs are rendered variables from the stop hook. Output is a blocking prompt/reason shown to the agent. The intended state transition is from placeholder tracker to initialized tracker before another exit attempt.
- gates_or_invariants: Only Round 0 can initialize the immutable section. Required content includes Ultimate Goal, Acceptance Criteria, and Active Tasks. The stop hook detects placeholders and missing sections around `hooks/loop-codex-stop-hook.sh:895`, builds `MISSING_ITEMS` around `hooks/loop-codex-stop-hook.sh:928`, and renders this template at `hooks/loop-codex-stop-hook.sh:947`.
- dependencies_and_callers: Loaded through `load_and_render_safe` in the stop hook with an inline fallback if the template is missing. It depends on setup-created `goal-tracker.md` placeholders from `scripts/setup-rlcr-loop.sh:1067` and normal-mode prompt instructions at `scripts/setup-rlcr-loop.sh:1298`.
- edge_cases_or_failure_modes: Skip-impl goal trackers are generated with concrete content, so this block should not trigger for normal review-only mode. If template loading fails, the stop hook has a fallback block with the same core variables.
- validation_or_tests: Referenced by template coverage in `tests/test-templates-comprehensive.sh` and goal-tracker/hook robustness suites found by recursive inspection. The exact assigned template is also directly referenced by the stop hook.
- skip_candidate: `no`

### DEV-HZ-181 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt snippet that defines an optional fallback channel for Claude to request goal-tracker mutable-section reconciliation when direct safe editing is not possible.
- algorithmic_behavior: The template instructs Claude to include a `## Goal Tracker Update Request` section in the round summary, with requested changes and justification. Examples cover marking tasks complete with evidence, adding blocking or queued issues, recording plan evolution, and deferring tasks with AC impact at `prompt-template/claude/goal-tracker-update-request.md:6`.
- inputs_outputs_state: Input is Claude’s inability or uncertainty around safely updating `goal-tracker.md` directly. Output is a structured section in the summary. State transition is indirect: Codex/stop-hook review can reconcile tracker changes if justified.
- gates_or_invariants: It is optional, scoped to the mutable section, and requires justification explaining how changes serve the Ultimate Goal. It does not authorize direct immutable-section edits after Round 0.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh:2201` for prompt generation, with fallback text at `hooks/loop-codex-stop-hook.sh:2203`. Related Codex responsibility is defined in `prompt-template/codex/goal-tracker-update-section.md:1`, and next-round prompt language references this fallback in `prompt-template/claude/next-round-prompt.md:63`.
- edge_cases_or_failure_modes: A request alone does not mutate state; it can become stale if not applied by review logic. If the template is missing, the stop hook inserts a minimal fallback sentence.
- validation_or_tests: Covered indirectly by template comprehensive checks and stop-hook prompt generation tests; recursive references show it is part of both Claude prompt flow and Codex reconciliation instructions.
- skip_candidate: `no`

### DEV-HZ-211 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness test for the base-branch auto-detection algorithm used by setup before code review.
- algorithmic_behavior: The test defines a local `detect_base_branch` helper at `tests/robustness/test-base-branch-detection.sh:37` that tries remote default branch, local `main`, local `master`, then failure. It creates isolated git repos for no-origin/main at `tests/robustness/test-base-branch-detection.sh:70`, no-origin/master at `tests/robustness/test-base-branch-detection.sh:99`, no main/master failure at `tests/robustness/test-base-branch-detection.sh:128`, and simulated origin at `tests/robustness/test-base-branch-detection.sh:155`.
- inputs_outputs_state: Inputs are temp git repositories, optional bare remotes, branch refs, and `run_with_timeout`. Outputs are pass/fail results. State changes are isolated to temp repos created under `TEST_DIR`.
- gates_or_invariants: Remote default must be non-empty and not `(unknown)`. If no usable remote default is found, local `main` wins over local `master`. If neither exists, detection returns failure. `run_with_timeout` guards all git calls.
- dependencies_and_callers: Sources `tests/test-helpers.sh:18` and `scripts/portable-timeout.sh:19`. It mirrors production setup logic at `scripts/setup-rlcr-loop.sh:772`, though production also validates user-specified `--base-branch`, verifies the remote default exists locally, and emits fetch guidance for remote-only branches.
- edge_cases_or_failure_modes: Covers missing origin without tripping `pipefail`, fallback to `main`, fallback to `master`, total failure when only another branch exists, and origin presence with acceptable fallback to local `main` if `git remote show` is not informative. Test header mentions user-specified priority, but this file’s helper does not exercise explicit `--base-branch`; that behavior is covered in production setup logic and broader setup robustness tests.
- validation_or_tests: Included in `tests/run-all-tests.sh:132`. This file is itself the executable regression coverage for the auto-detection fallback ladder.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8/8 unique Item Evidence headings present, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`