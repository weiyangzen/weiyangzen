# agent_22 dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `eec73c4dfcc4f9791933e3cbaa616d4f261ed9e2`

## Item Evidence

### DEV-HZ-022 `directory` `skills/ask-gemini`
- cursor: `[_]`
- core_role: Defines the `/humanize:ask-gemini` skill contract and routes all execution to `scripts/ask-gemini.sh`. Recursive inspection found only `skills/ask-gemini/SKILL.md`; there are no hidden child roles beyond that file.
- algorithmic_behavior: The skill frontmatter names the skill, describes it as an independent Gemini deep-research consultation, and restricts tool access to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh:*)` at `skills/ask-gemini/SKILL.md:1-6`. The body defines the invocation algorithm: pass free-form user text as one quoted argument when no flags are present, or reconstruct flags as separate shell arguments with the remaining question as one quoted final argument at `skills/ask-gemini/SKILL.md:17-31`.
- inputs_outputs_state: Inputs are optional `--gemini-model`, optional `--gemini-timeout`, and a free-form question/task. Output contract is Gemini stdout for the caller to read, stderr status/debug, and persisted response state under `.humanize/skill/<timestamp>/output.md` as documented at `skills/ask-gemini/SKILL.md:41-60`.
- gates_or_invariants: The main invariant is shell safety: never pass unquoted `$ARGUMENTS`, because shell reparsing can break or reinterpret metacharacters; this is explicitly forbidden at `skills/ask-gemini/SKILL.md:33-39`. Exit-code interpretation is part of the gate contract: `0` success, `1` validation, `124` timeout, other process error at `skills/ask-gemini/SKILL.md:48-55`.
- dependencies_and_callers: Coordinates directly with DEV-HZ-052 `scripts/ask-gemini.sh`. The skill depends on `CLAUDE_PLUGIN_ROOT` being set so the script path resolves. Monitor/docs references show the invocation is later surfaced through skill monitoring and user docs, but this directory itself only owns the skill entrypoint.
- edge_cases_or_failure_modes: Handles shell metacharacters in questions by requiring quote preservation. Timeout, missing Gemini CLI, invalid flags, and process failures are delegated to the script and exposed through the exit-code table.
- validation_or_tests: No tests live inside `skills/ask-gemini`; validation is by the script behavior and by monitor visibility of `.humanize/skill` records.
- skip_candidate: `no`

### DEV-HZ-052 `file` `scripts/ask-gemini.sh`
- cursor: `[_]`
- core_role: Runtime implementation for the ask-gemini workflow: parse user flags, resolve the project, persist invocation metadata, run Gemini CLI with a web-research prompt, and return the answer.
- algorithmic_behavior: The script sources `portable-timeout.sh` and `hooks/lib/project-root.sh` at `scripts/ask-gemini.sh:27-33`, sets defaults `gemini-3.1-pro-preview` and `3600` seconds at `scripts/ask-gemini.sh:39-43`, parses flags and question parts at `scripts/ask-gemini.sh:91-139`, then validates prerequisites before any execution at `scripts/ask-gemini.sh:145-168`. It resolves the project root at `scripts/ask-gemini.sh:174-178`, creates project-local and cache storage at `scripts/ask-gemini.sh:184-199`, writes `input.md` at `scripts/ask-gemini.sh:205-218`, builds Gemini args and the mandatory Google Search prompt at `scripts/ask-gemini.sh:224-241`, runs through `run_with_timeout` at `scripts/ask-gemini.sh:279-288`, then handles result classes at `scripts/ask-gemini.sh:294-362`.
- inputs_outputs_state: Inputs are CLI args `--gemini-model MODEL`, `--gemini-timeout SECONDS`, `--`, and the remaining free-form task. Env inputs include `HUMANIZE_GEMINI_YOLO`, `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, `HOME`, and PATH lookup for `gemini`. Outputs are stdout answer at `scripts/ask-gemini.sh:384-388`, stderr status at `scripts/ask-gemini.sh:267-288`, `.humanize/skill/<unique-id>/{input,output,metadata}.md`, and cache files `gemini-run.{cmd,out,log}` at `scripts/ask-gemini.sh:247-261`.
- gates_or_invariants: Requires `gemini` in PATH, non-empty question, positive integer timeout, and model names limited to `[a-zA-Z0-9._-]+` at `scripts/ask-gemini.sh:145-168`. The approval mode is controlled by `HUMANIZE_GEMINI_YOLO`; otherwise Gemini runs with `--sandbox` at `scripts/ask-gemini.sh:226-231`. Empty Gemini stdout is treated as a failure even with exit code `0` at `scripts/ask-gemini.sh:340-362`.
- dependencies_and_callers: Called by `skills/ask-gemini/SKILL.md`. Depends on Gemini CLI, jq-free shell utilities, `/dev/urandom`, `od`, `sed`, `tail`, `date`, `cp`, and the sourced project-root/timeout helpers. Its output metadata is consumed by skill monitoring through `.humanize/skill`.
- edge_cases_or_failure_modes: If home cache creation fails, it falls back to `$SKILL_DIR/cache` and warns at `scripts/ask-gemini.sh:195-199`. Timeout exits `124` and writes timeout metadata at `scripts/ask-gemini.sh:294-313`. Non-zero Gemini exits preserve stderr tail and metadata at `scripts/ask-gemini.sh:316-337`. Empty output exits `1` with `empty_response` metadata at `scripts/ask-gemini.sh:340-361`.
- validation_or_tests: No direct assigned test file for this script. Behavior is observable through skill monitor records and the skill’s documented exit contract. I did not execute it because the branch export is read-only research and the script would write `.humanize/skill` state and call external Gemini.
- skip_candidate: `no`

### DEV-HZ-082 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the cancel signal-file escape hatch in RLCR Bash validation. It proves that state-file mutation is blocked except for a narrowly authorized cancellation move.
- algorithmic_behavior: The suite creates a fake active loop with `.humanize/rlcr/<timestamp>/state.md` and exports `CLAUDE_PROJECT_DIR` at `tests/test-cancel-signal-file.sh:42-59`. It drives `hooks/loop-bash-validator.sh` by constructing Bash hook JSON with jq at `tests/test-cancel-signal-file.sh:61-74`, and also directly calls `is_cancel_authorized` from `hooks/lib/loop-common.sh` after sourcing it at `tests/test-cancel-signal-file.sh:32-33`.
- inputs_outputs_state: Inputs are synthesized Bash command strings and optional `$LOOP_DIR/.cancel-requested`. Outputs are PASS/FAIL counters and process exit equal to `TESTS_FAILED` at `tests/test-cancel-signal-file.sh:1421-1433`. Test state is reset under a temp directory for each case, so each command is evaluated against a fresh active loop.
- gates_or_invariants: The positive path allows only `mv <active-loop>/state.md <active-loop>/cancel-state.md` when `.cancel-requested` exists in the active loop directory. Positive coverage includes exact path, `/./` normalization, single quotes, and literal `${loop_dir}` normalization at `tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-380`, and `tests/test-cancel-signal-file.sh:450-486`. The underlying helper enforces the real algorithm: signal file required, no command substitution/newlines/chaining, no remaining `$`, command must start with `mv`, exactly two args, source must be loop `state.md`/`finalize-state.md`/`methodology-analysis-state.md`, destination must be loop `cancel-state.md`, and source cannot be a symlink at `hooks/lib/loop-common.sh:1038-1255`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `hooks/loop-bash-validator.sh`, `jq`, `mktemp`, `sha`-independent shell checks, and project-root resolution via `CLAUDE_PROJECT_DIR`. It tests the Bash validator integration where `loop-bash-validator.sh` calls `is_cancel_authorized` before blocking state-file mutation at `hooks/loop-bash-validator.sh:271-296` and source-path bypasses at `hooks/loop-bash-validator.sh:310-456`.
- edge_cases_or_failure_modes: Negative matrix covers missing signal, signal in wrong directory, `echo > state.md`, `sed -i`, `rm`, wrong destination, arbitrary destination, command injection through `&&`, `;`, `$()`, backticks, newline, hidden `${IFS}`, options/prefixes (`sudo`, `env`, `command --`), chains/pipes/background operators, shell wrappers, redirection prefixes, quoted/escaped/ANSI-C redirection targets, unrelated `/tmp/state.md`, and extra args at `tests/test-cancel-signal-file.sh:118-1249`. It also proves no active loop means the validator allows the command at `tests/test-cancel-signal-file.sh:1230-1249`.
- validation_or_tests: The file is itself the validation asset. Helper-level regression tests prove symlinked path prefixes are accepted after canonicalization while destination/source symlink aliases are rejected at `tests/test-cancel-signal-file.sh:1345-1419`. I did not execute it in this read-only research pass.
- skip_candidate: `no`

### DEV-HZ-112 `file` `tests/test-stop-hook-bg-allow.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR stop-hook background-task short-circuit. It verifies the hook exits naturally while background work is still running, instead of spending Codex review/gate cycles prematurely.
- algorithmic_behavior: The header defines the intended algorithm: pending background Agent/Bash work should make the stop hook exit `0` with a `systemMessage`, leave loop state unchanged, and resume normal review on a later stop after completion at `tests/test-stop-hook-bg-allow.sh:3-10`. Fixture setup creates a minimal git repo, plan, loop state, summary, and goal tracker sufficient for normal Codex flow at `tests/test-stop-hook-bg-allow.sh:97-178`. Transcript builders emit async Agent launch records, background Bash launch records, legacy XML completion records, and SDK `system/task_notification` completion records at `tests/test-stop-hook-bg-allow.sh:186-253`.
- inputs_outputs_state: Inputs are hook JSON containing `transcript_path` and optionally `session_id`, JSONL transcript events, active loop state files, fake HOME for tilde paths, and fake `lsof`. Outputs are hook JSON/system messages, wrapper `ALLOW:` lines, Codex marker files, `bg-pending.marker`, and preserved or changed loop state. The runner captures `RUN_EXIT_CODE`, `RUN_OUTPUT`, and Codex marker state at `tests/test-stop-hook-bg-allow.sh:264-289`.
- gates_or_invariants: The implementation under test is `loop-codex-stop-hook.sh`, which session-filters active loops with marker fallback at `hooks/loop-codex-stop-hook.sh:61-68`, then delegates before normal phase parsing to `handle_bg_task_short_circuit` at `hooks/loop-codex-stop-hook.sh:71-83`. That helper runs four ordered guards: ambiguous no-session marker exit, cross-session parked-loop notice, pending-bg short-circuit and marker write, and same-session stale-marker cleanup at `hooks/lib/loop-bg-tasks.sh:299-435`. `find_active_loop` only uses marker fallback when explicitly opted in, and exact session matches take precedence at `hooks/lib/loop-common.sh:325-424`.
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/loop-bg-tasks.sh`, `tests/test-helpers.sh`, jq, git, fake Codex, fake `lsof`, and temp transcript files. The wrapper forwards `--project-root`, `--transcript-path`, and optional `--session-id`, maps hook JSON with no `decision` to `ALLOW`, and exits `0` for allowed stops at `scripts/rlcr-stop-gate.sh:99-173`.
- edge_cases_or_failure_modes: AC coverage includes no bg reaching Codex, pending subagent/shell short-circuit, completed launches reaching Codex, multi-task counts, missing/empty/no transcript fail-closed reaching Codex, no active loop silent exit, finalize-phase short-circuit, wrapper propagation, tilde expansion, cross-session parked-loop preservation, exact-session precedence, stale marker cleanup, SDK and legacy completion recognition, missing/malformed transcript preserving marker, validator isolation from marker fallback, no-session wrapper ambiguity, pre-loop timestamp filtering with UTC/JST/PST conversion, wrapper no-session with/without prior marker, and `lsof` liveness pruning at `tests/test-stop-hook-bg-allow.sh:354-1462`.
- validation_or_tests: The file is the validation asset. Key assertions distinguish short-circuit from normal flow: `assert_systemmessage_only` requires exit `0`, no Codex marker, JSON `systemMessage`, and unchanged state at `tests/test-stop-hook-bg-allow.sh:291-329`; `assert_reached_codex` requires exit `0` and Codex marker at `tests/test-stop-hook-bg-allow.sh:331-339`. I did not execute it in this read-only research pass.
- skip_candidate: `no`

### DEV-HZ-142 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Block-message template for the `.humanize` git-add protection gate. It is user-facing enforcement text for keeping local loop/runtime state out of version control.
- algorithmic_behavior: The template explains why the command was blocked, lists safe alternatives, lists blocked broad/add-all patterns, and gives the sanctioned `.gitignore` update flow at `prompt-template/block/git-add-humanize.md:1-34`.
- inputs_outputs_state: It has no dynamic placeholders; output is rendered text to stderr. It does not mutate state. The state it protects is `.humanize/`, which contains local loop state and should remain uncommitted.
- gates_or_invariants: The gate invariant is that commands which directly or broadly stage `.humanize` must be rejected. The template is rendered by `git_add_humanize_blocked_message`, which falls back to equivalent inline text and calls `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` at `hooks/lib/loop-common.sh:1383-1420`.
- dependencies_and_callers: `hooks/loop-bash-validator.sh` calls `git_adds_humanize "$COMMAND_LOWER"` and emits this block on exit `2` at `hooks/loop-bash-validator.sh:241-250`. The detector handles `git -C`, chained segments, direct `.humanize` references, force/all flags, broad `.` or `*`, and checks whether `.humanize` exists and is gitignored at `hooks/lib/loop-common.sh:1263-1381`.
- edge_cases_or_failure_modes: The template warns that the commit message for adding `.humanize*` to `.gitignore` must not contain the literal `.humanize` string at `prompt-template/block/git-add-humanize.md:26-34`; this is a practical interaction edge with the same protection language/patterns. Underlying detection lowercases input, so `-A` becomes `-a` and both cases are handled at `hooks/lib/loop-common.sh:1267-1283`.
- validation_or_tests: Validated indirectly by Bash validator behavior and any tests that exercise git-add protection. This assigned file is a static template, so direct validation is render-site coverage rather than unit execution.
- skip_candidate: `no`

### DEV-HZ-172 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template for the read-side round isolation gate. It prevents agents from reading stale/future round prompt/summary files as if they were active.
- algorithmic_behavior: The template reports the requested `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, the active `{{CURRENT_ROUND}}`, the current prompt and summary paths, and a raw `cat {{FILE_PATH}}` escape instruction if the specific file is truly needed at `prompt-template/block/wrong-round-file.md:1-9`.
- inputs_outputs_state: Inputs are renderer variables `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`. Output is stderr block text and hook exit `2` from the read validator. It does not mutate loop state.
- gates_or_invariants: The invariant is current-round isolation: if a read targets a round file whose extracted round number differs from the active state round and is not allowlisted, the hook blocks. The render site is `hooks/loop-read-validator.sh:286-298`.
- dependencies_and_callers: Depends on `extract_round_number`, `is_round_file_type`, and `is_allowlisted_file` from `hooks/lib/loop-common.sh`. Round extraction supports summary/prompt/todos/contract names at `hooks/lib/loop-common.sh:795-814`; allowlisted historical files are explicitly listed at `hooks/lib/loop-common.sh:816-842`. Write/edit wrong-round cases use a sibling `wrong-round-number.md`, not this template, as seen in `hooks/loop-write-validator.sh:318-335` and `hooks/loop-edit-validator.sh:247-271`.
- edge_cases_or_failure_modes: If the file is outside the active loop directory, a different wrong-location template fires first at `hooks/loop-read-validator.sh:270-279`. If the filename does not yield a round number, the read validator exits `0` earlier at `hooks/loop-read-validator.sh:251-254`.
- validation_or_tests: Validated indirectly through read-validator tests or hook integration. This assigned file is static render text but belongs to a concrete algorithmic gate.
- skip_candidate: `no`

### DEV-HZ-202 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared shell utility library for humanize terminal monitor functions. It centralizes monitor display helpers, session/state discovery, frontmatter parsing, and goal-tracker summary parsing.
- algorithmic_behavior: Provides ANSI color functions at `scripts/lib/monitor-common.sh:14-23`, cross-platform file-size lookup at `scripts/lib/monitor-common.sh:32-35`, latest timestamped session discovery at `scripts/lib/monitor-common.sh:40-63`, terminal scroll-region setup/restore at `scripts/lib/monitor-common.sh:71-92`, status-to-color mapping at `scripts/lib/monitor-common.sh:130-141`, active/terminal state file detection at `scripts/lib/monitor-common.sh:154-192`, YAML frontmatter extraction at `scripts/lib/monitor-common.sh:201-213`, timestamp/truncation helpers at `scripts/lib/monitor-common.sh:222-256`, and goal-tracker parsers at `scripts/lib/monitor-common.sh:265-383`.
- inputs_outputs_state: Inputs are filesystem paths to loop dirs, session dirs, state files, metadata files, and `goal-tracker.md`. Outputs are plain strings such as `path|status`, counts separated by `|`, formatted timestamps, truncated strings, and ANSI escape codes. Persistent state is not written; terminal state can be temporarily altered by scroll-region helpers.
- gates_or_invariants: `monitor_find_latest_session` only accepts directory names matching `YYYY-MM-DD_HH-MM-SS` and returns the lexically newest at `scripts/lib/monitor-common.sh:49-62`. `monitor_find_state_file` prioritizes `methodology-analysis-state.md`, then `state.md`, then any `*-state.md` terminal file at `scripts/lib/monitor-common.sh:162-190`. Goal parsing subtracts markdown table header/separator rows, supports old `Open Issues` and newer Blocking/Queued side issue sections, excludes completed/deferred active tasks, and clamps negative active counts to zero at `scripts/lib/monitor-common.sh:272-294` and `scripts/lib/monitor-common.sh:307-383`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at `scripts/humanize.sh:6-9`. `scripts/lib/monitor-skill.sh` consumes `monitor_get_yaml_value` and `monitor_format_timestamp` for ask-codex/ask-gemini invocation displays, as referenced in `scripts/lib/monitor-skill.sh`. Docs note this shell monitor side is intentionally single-session and should not leak into the web monitor model.
- edge_cases_or_failure_modes: Missing loop dirs or files return empty/default values rather than hard failures (`""`, `|unknown`, `0|0|0`, or `No goal tracker`). YAML parsing is simple sed/grep over the first frontmatter block and strips quotes, so complex YAML structures are not supported. Terminal helpers assume `clear`/`tput` are usable. The session finder is single-session by design and only looks one level below the loop dir.
- validation_or_tests: Robustness tests for goal tracker parsing exist around `scripts/humanize.sh` parsing, and monitor skill tests exercise metadata display, but this assigned library was not executed in the read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DEV-HZ-022, DEV-HZ-052, DEV-HZ-082, DEV-HZ-112, DEV-HZ-142, DEV-HZ-172, DEV-HZ-202
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`