# agent_08 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-008 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime command layer for the Humanize RLCR/PR loop state machines. The directory contains loop setup, cancellation, monitoring, PR-review polling, Codex invocation, config/model routing, BitLesson enforcement, timeout portability, and installer helpers.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` creates RLCR sessions, validates plan/gitrepo/base branch state, writes `.humanize/rlcr/<timestamp>/state.md`, copies or creates `plan.md`, initializes BitLesson, creates `goal-tracker.md`, and seeds round prompts/summary templates (`scripts/setup-rlcr-loop.sh:280`, `scripts/setup-rlcr-loop.sh:321`, `scripts/setup-rlcr-loop.sh:411`, `scripts/setup-rlcr-loop.sh:681`, `scripts/setup-rlcr-loop.sh:831`). `scripts/setup-pr-loop.sh` creates PR-loop sessions, detects the PR, resolves fork/base repo context, classifies startup reviewer state, posts/reuses bot trigger comments for cases 4/5, verifies Claude eyes reactions, writes PR loop state, goal tracker, comments, and resolve prompt (`scripts/setup-pr-loop.sh:217`, `scripts/setup-pr-loop.sh:280`, `scripts/setup-pr-loop.sh:437`, `scripts/setup-pr-loop.sh:457`, `scripts/setup-pr-loop.sh:605`). `scripts/humanize.sh` exposes `humanize monitor rlcr|pr|skill`, renders RLCR/PR status bars, follows round logs, switches sessions/logs, parses tracker and git state, and handles terminal cleanup (`scripts/humanize.sh:38`, `scripts/humanize.sh:124`, `scripts/humanize.sh:227`, `scripts/humanize.sh:1106`, `scripts/humanize.sh:1158`). PR helpers fetch comments/reviews/reactions and convert GitHub state into JSON inputs for the PR stop hook (`scripts/check-pr-reviewer-status.sh:153`, `scripts/poll-pr-reviews.sh:240`, `scripts/check-bot-reactions.sh:152`).
- inputs_outputs_state: Inputs include CLI flags, `CLAUDE_PROJECT_DIR`, git branch/status/base refs, `gh` API data, Codex/Claude binaries, plan path, PR number/state, bot lists, BitLesson file, and prompt templates. Outputs are `.humanize/rlcr/*` and `.humanize/pr-loop/*` state directories, prompt/summary/comment/review files, `goal-tracker.md`, `.humanize/.pending-session-id`, `.cancel-requested`, cache logs under XDG/home cache, JSON reviewer-status/reaction payloads, and monitor/statusline text.
- gates_or_invariants: Enforces one active loop at a time (`scripts/setup-pr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:324`); dependency availability for `codex`, `jq`, `git`, and `gh` where needed (`scripts/setup-rlcr-loop.sh:295`, `scripts/setup-pr-loop.sh:255`); YAML-safe branch/model/effort values (`scripts/setup-pr-loop.sh:384`, `scripts/setup-rlcr-loop.sh:654`); clean git tree before RLCR start, ignoring runtime `.humanize/` (`scripts/setup-rlcr-loop.sh:681`); plan path safety including relative-only, no spaces/metacharacters, no symlinks, in-project containment, submodule rejection, tracking mode rules, and minimum content (`scripts/setup-rlcr-loop.sh:421`, `scripts/setup-rlcr-loop.sh:447`, `scripts/setup-rlcr-loop.sh:495`, `scripts/setup-rlcr-loop.sh:528`, `scripts/setup-rlcr-loop.sh:584`). PR loop gates include numeric PR, open/not merged state, fork-aware base repo lookup, trigger-comment recovery, and mandatory Claude eyes verification for trigger-required Claude runs (`scripts/setup-pr-loop.sh:359`, `scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:470`, `scripts/setup-pr-loop.sh:564`).
- dependencies_and_callers: Called by slash commands/hooks and user shell entrypoints. Depends on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `scripts/lib/monitor-common.sh`, `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/lib/monitor-skill.sh`, prompt templates, `git`, `gh`, `jq`, `codex`, and optionally `claude`. `scripts/rlcr-stop-gate.sh` wraps `hooks/loop-codex-stop-hook.sh` as a Stop hook decision runner. Cancellation scripts coordinate with hook authorization by creating `.cancel-requested` and renaming active state files (`scripts/cancel-rlcr-loop.sh:148`, `scripts/cancel-pr-loop.sh:118`).
- edge_cases_or_failure_modes: Handles fork PRs by resolving parent/base repo for PR lookup and reactions (`scripts/setup-pr-loop.sh:290`, `scripts/check-pr-reviewer-status.sh:109`, `scripts/check-bot-reactions.sh:119`); avoids duplicate trigger spam by finding existing post-commit mentions outside code/quote contexts (`scripts/setup-pr-loop.sh:472`); refuses trigger-required setup when trigger comment ID cannot be found for Claude eyes verification (`scripts/setup-pr-loop.sh:568`); supports skip-impl RLCR mode with review phase marker and placeholder plan (`scripts/setup-rlcr-loop.sh:374`, `scripts/setup-rlcr-loop.sh:878`); cancellation refuses finalize-phase RLCR unless `--force` (`scripts/cancel-rlcr-loop.sh:131`); monitor handles missing/deleted/truncated logs and session switches (`scripts/humanize.sh:822`, `scripts/humanize.sh:996`, `scripts/humanize.sh:1014`, `scripts/humanize.sh:1049`); portable timeout falls back from `gtimeout` to GNU `timeout`, Python, or no-timeout warning (`scripts/portable-timeout.sh:9`).
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh` for mutual exclusion, startup cases, PR helper outputs, monitor phases, force-push ancestry, trigger requirements, and goal-tracker updates; by `tests/robustness/test-hook-system-robustness.sh` for validator and stop-hook robustness; and by `tests/setup-monitor-test-env.sh` for monitor fixture state. No mutating tests were executed in this research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-038 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Hook registration manifest that wires Claude tool lifecycle events into RLCR/PR validators and stop gates.
- algorithmic_behavior: Registers `UserPromptSubmit` to `hooks/loop-plan-file-validator.sh` (`hooks/hooks.json:4`), `PreToolUse` validators for `Write`, `Edit`, `Read`, and `Bash` (`hooks/hooks.json:14`), a `PostToolUse` Bash hook for session-id capture (`hooks/hooks.json:52`), and two Stop hooks, `loop-codex-stop-hook.sh` and `pr-loop-stop-hook.sh`, each with a 7200 second timeout (`hooks/hooks.json:63`).
- inputs_outputs_state: Input is Claude’s hook event stream. Output is routing to command hooks whose JSON decisions can allow/block tool use or session stop. It does not mutate state itself, but it enables validators that protect `.humanize` state files, prompts, summaries, plan files, PR resolve files, and loop lifecycle transitions.
- gates_or_invariants: Ensures all relevant user prompt, tool-use, post-bash, and stop transitions pass through the Humanize hook layer. Stop hooks are both installed, allowing each hook to no-op when its loop type is inactive and block only when the corresponding active loop requires continuation.
- dependencies_and_callers: Consumed by Claude plugin/hook loader. Commands are rooted through `${CLAUDE_PLUGIN_ROOT}` and depend on the hook scripts under `hooks/`.
- edge_cases_or_failure_modes: Manifest-level failure modes are missing/incorrect `${CLAUDE_PLUGIN_ROOT}`, unavailable hook scripts, invalid JSON, or hook timeout. Because both Stop hooks run for every stop event, each hook must handle “no active state” safely.
- validation_or_tests: Robustness suite verifies missing state is allowed for both RLCR and PR stop hooks (`tests/robustness/test-hook-system-robustness.sh:461`, `tests/robustness/test-hook-system-robustness.sh:477`) and that active RLCR state yields a block decision (`tests/robustness/test-hook-system-robustness.sh:559`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-068 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Fixture generator for PR monitor behavior; creates state layouts consumed by `humanize monitor pr` and monitor-common phase/parsing tests.
- algorithmic_behavior: Accepts `<test_dir> <test_name>` and writes `.humanize/pr-loop/<timestamp>/state.md` for one of three scenarios: `yaml_list` with both configured and active bots (`tests/setup-monitor-test-env.sh:20`), `configured` with `active_bots` reduced to `codex` to model partial approval (`tests/setup-monitor-test-env.sh:43`), and `empty` with empty `active_bots` to model all approved (`tests/setup-monitor-test-env.sh:64`). Prints the test directory path on success (`tests/setup-monitor-test-env.sh:91`).
- inputs_outputs_state: Inputs are filesystem target path and fixture name. Outputs are deterministic PR loop state directories containing YAML frontmatter fields such as `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- gates_or_invariants: Requires `TEST_DIR`; unknown fixture names fail with available options (`tests/setup-monitor-test-env.sh:15`, `tests/setup-monitor-test-env.sh:84`). Uses YAML list formatting for bot arrays, exercising parser compatibility with multi-line state.
- dependencies_and_callers: Intended for monitor tests or manual test setup. Depends only on shell and filesystem operations.
- edge_cases_or_failure_modes: Empty `active_bots:` without list items tests parser behavior for “all approved”; unknown fixture names exit nonzero; does not validate whether generated timestamps are latest relative to other sessions.
- validation_or_tests: Supports monitor tests that source `scripts/lib/monitor-common.sh` and `scripts/humanize.sh`, especially parser and phase display assertions in `tests/test-pr-loop-system.sh:946` through `tests/test-pr-loop-system.sh:1106`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-098 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: Executable specification for the PR loop algorithm, covering setup, GitHub bot-state classification, reaction gates, monitor phase detection, goal-tracker updates, and stop-hook integration.
- algorithmic_behavior: Test harness installs mocked `gh` fixtures, creates temporary git repos, runs each test in an isolated temp directory, and provides filtered test groups through `main` (`tests/test-pr-loop-system.sh:50`, `tests/test-pr-loop-system.sh:80`, `tests/test-pr-loop-system.sh:1761`). It verifies RLCR/PR mutual exclusion (`tests/test-pr-loop-system.sh:110`, `tests/test-pr-loop-system.sh:129`), reviewer startup cases 1/2/3/4/5 (`tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:173`, `tests/test-pr-loop-system.sh:493`, `tests/test-pr-loop-system.sh:1315`, `tests/test-pr-loop-system.sh:1347`), Codex `+1` and Claude `eyes` reaction detection (`tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:223`), PR review submissions as bot comments (`tests/test-pr-loop-system.sh:236`), monitor phases and one-shot display (`tests/test-pr-loop-system.sh:256`, `tests/test-pr-loop-system.sh:849`, `tests/test-pr-loop-system.sh:946`), goal-tracker parsing/update/idempotency/repair (`tests/test-pr-loop-system.sh:315`, `tests/test-pr-loop-system.sh:377`, `tests/test-pr-loop-system.sh:681`, `tests/test-pr-loop-system.sh:722`, `tests/test-pr-loop-system.sh:1242`), force-push ancestry logic (`tests/test-pr-loop-system.sh:578`), and stop-hook goal tracker update via mocked `gh`, `git`, and `codex` (`tests/test-pr-loop-system.sh:1541`).
- inputs_outputs_state: Inputs are fixture JSON under `tests/fixtures`, mock binaries under `tests/mocks`, environment variables such as `MOCK_GH_PR_NUMBER`, `MOCK_GH_LATEST_COMMIT_AT`, and temp `.humanize/pr-loop` state. Outputs are pass/fail counters and mutated temporary state/goal-tracker files used for assertions.
- gates_or_invariants: PR loop cannot start while RLCR is active and vice versa; startup cases 1/2/3 at round 0 do not require trigger, cases 4/5 and later rounds do (`tests/test-pr-loop-system.sh:1112`); Codex approval is timestamp-filtered (`tests/test-pr-loop-system.sh:207`); Claude eyes verification must fail setup if trigger ID cannot be retrieved (`tests/test-pr-loop-system.sh:1384`); goal tracker updates must be idempotent for the same round/bot and must insert rows inside the Issue Summary table (`tests/test-pr-loop-system.sh:722`, `tests/test-pr-loop-system.sh:1180`).
- dependencies_and_callers: Calls `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/lib/loop-common.sh`, `scripts/lib/monitor-common.sh`, and `scripts/humanize.sh`. Depends on shell, git, jq, timeout, mock `gh`, and mock `codex`.
- edge_cases_or_failure_modes: Covers no comments, partial comments, all comments, stale comments after new commits, missing trigger comment ID cleanup, approval-only PR reviews, cancelled/max-iteration states, recently growing PR-check files, unpushed-commit detection patterns, and partial goal-tracker updates where either summary row or issue log entry is missing.
- validation_or_tests: This file is itself the validation harness. It ends by reporting total run/pass/fail counts and exits nonzero if any test failed (`tests/test-pr-loop-system.sh:1889`). Not executed during this read-only research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-128 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Prompt-level enforcement contract for RLCR goal-tracker ownership after round 0.
- algorithmic_behavior: States that after round 0 only Codex may modify `goal-tracker.md` (`prompt-template/block/goal-tracker-modification.md:3`) and that Claude cannot directly modify it through `Write`, `Edit`, or `Bash` (`prompt-template/block/goal-tracker-modification.md:5`). It redirects Claude to include a `Goal Tracker Update Request` section in the current summary file (`prompt-template/block/goal-tracker-modification.md:9`) with requested changes and justification (`prompt-template/block/goal-tracker-modification.md:12`).
- inputs_outputs_state: Inputs are template variables `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`. Output is a blocking instruction rendered into hook responses or prompts when a goal-tracker mutation is disallowed.
- gates_or_invariants: Enforces a single writer model for goal-tracker evolution after initialization: Claude proposes changes in the summary; Codex reviews and applies justified updates.
- dependencies_and_callers: Used by hook validators/template loader when `goal-tracker.md` modification is blocked after round 0. Coordinates with Bash/Edit/Write validators and the RLCR stop hook’s summary review/update flow.
- edge_cases_or_failure_modes: If this template is missing, hooks must rely on fallback text; if summary path substitution is wrong, Claude may place the update request in the wrong file. It is not a code gate by itself, but expresses the human/model contract used by blocking validators.
- validation_or_tests: Robustness suite explicitly expects Bash modification of `goal-tracker.md` after round 0 to be blocked (`tests/robustness/test-hook-system-robustness.sh:353`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-158 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Prompt contract for RLCR review findings that include Codex-discovered open questions.
- algorithmic_behavior: Single-line instruction tells Claude that when Codex finds open questions, Claude must use `AskUserQuestion` first before resolving other Codex findings (`prompt-template/claude/open-question-notice.md:1`).
- inputs_outputs_state: Input is the RLCR stop-hook condition that detects open-question findings in Codex output. Output is injected instruction text in the next Claude prompt or block message.
- gates_or_invariants: Establishes a clarification-before-fixing invariant: unresolved product/implementation questions must be escalated to the user before ordinary issue resolution proceeds.
- dependencies_and_callers: Used by the RLCR Codex stop hook when `ask_codex_question` state is enabled and Codex output indicates open questions. Depends on prompt/template loading.
- edge_cases_or_failure_modes: Because the template is only one line, malformed or over-broad open-question detection in the caller could over-trigger user-question flow; if omitted, Claude may resolve ambiguous findings without user clarification.
- validation_or_tests: Indirectly covered by RLCR stop-hook behavior and prompt-template rendering expectations; no dedicated test in assigned files directly asserts this template text.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-188 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for hook validators and stop hooks under malformed JSON, path traversal, protected-state mutation attempts, concurrent hook calls, corrupted state, and large/nested payloads.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and test helpers, creates a temp project, then runs 21 numbered robustness tests (`tests/robustness/test-hook-system-robustness.sh:20`). It verifies valid/malformed Edit JSON handling and state-file edit blocking (`tests/robustness/test-hook-system-robustness.sh:40`, `tests/robustness/test-hook-system-robustness.sh:53`, `tests/robustness/test-hook-system-robustness.sh:80`), path traversal behavior inside and outside `.humanize` (`tests/robustness/test-hook-system-robustness.sh:109`, `tests/robustness/test-hook-system-robustness.sh:124`), plan-file validator behavior (`tests/robustness/test-hook-system-robustness.sh:165`), state parsing tolerance (`tests/robustness/test-hook-system-robustness.sh:253`), Bash validator protection of `state.md` and post-round-0 `goal-tracker.md` (`tests/robustness/test-hook-system-robustness.sh:317`, `tests/robustness/test-hook-system-robustness.sh:353`), concurrent read validator calls (`tests/robustness/test-hook-system-robustness.sh:421`), missing/corrupt/incomplete/active stop-hook state behavior (`tests/robustness/test-hook-system-robustness.sh:461`, `tests/robustness/test-hook-system-robustness.sh:493`, `tests/robustness/test-hook-system-robustness.sh:516`, `tests/robustness/test-hook-system-robustness.sh:559`), and large/null/nested JSON handling (`tests/robustness/test-hook-system-robustness.sh:623`, `tests/robustness/test-hook-system-robustness.sh:637`, `tests/robustness/test-hook-system-robustness.sh:651`).
- inputs_outputs_state: Inputs are synthetic hook JSON events, temporary `.humanize/rlcr` state files, temp git repos, and mock `codex`. Outputs are pass/fail records from test helpers plus temporary state mutations such as `unexpected-state.md` when required fields are missing.
- gates_or_invariants: Protected RLCR state files must not be changed by Edit/Bash; goal tracker after round 0 is protected; non-Humanize paths are delegated to Claude sandbox rather than over-blocked; missing loop state must allow stop; corrupted state must not crash; incomplete state should end or allow cleanly rather than trap the session; valid active loop state must block session stop with a block decision.
- dependencies_and_callers: Exercises `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, git, and mock `codex`.
- edge_cases_or_failure_modes: Malformed JSON, missing `file_path`, traversal to protected state through normalized paths, special characters in state values, missing YAML delimiter, command strings containing dangerous shell operations outside Humanize-owned state, concurrent hook calls, missing `.humanize`, corrupted frontmatter, large payloads, escaped nulls, and deep nesting.
- validation_or_tests: This file is itself the robustness test runner and exits with `print_test_summary` status (`tests/robustness/test-hook-system-robustness.sh:671`). Not executed during this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present; headers are unique and correspond to `DO_NOT_WISH_CODING-HZ-008`, `DO_NOT_WISH_CODING-HZ-038`, `DO_NOT_WISH_CODING-HZ-068`, `DO_NOT_WISH_CODING-HZ-098`, `DO_NOT_WISH_CODING-HZ-128`, `DO_NOT_WISH_CODING-HZ-158`, `DO_NOT_WISH_CODING-HZ-188`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`