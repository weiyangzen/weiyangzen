# agent_01 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`
- note: Research only. No files were modified. I inspected the assigned paths and followed direct runtime/test/template references needed to understand behavior. I did not run mutating tests in this read-only branch export.

## Item Evidence

### DO_NOT_WISH_CODING-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude/Codex plugin. It coordinates plan generation, RLCR implementation loops, PR review loops, guard hooks, prompt templates, skills, config, and tests. The root has 15 top-level file groups in the export: `commands`, `scripts`, `hooks`, `prompt-template`, `skills`, `agents`, `config`, `templates`, `tests`, `docs`, `.github`, `.claude`, `.claude-plugin`, `README.md`, and `.gitignore`.
- algorithmic_behavior: The root implements a workflow state machine rather than one library. `commands/*.md` define slash-command algorithms; `scripts/*.sh` execute setup, state creation, Codex calls, PR polling, cancellation, BitLesson validation, and monitoring; `hooks/hooks.json` wires validators and stop hooks into Claude Code events; `hooks/lib/loop-common.sh` supplies shared parsing, state, template, and guard helpers; `prompt-template/**` provides renderable block/review/phase contracts; `skills/**/SKILL.md` exposes workflow instructions to non-slash-command runtimes; `tests/**` specifies script, hook, monitor, and robustness behavior.
- inputs_outputs_state: Main inputs are draft docs, plan files, CLI flags, `.humanize/config.json`, Git branch/PR state, GitHub bot reviews, Codex/Claude outputs, and hook JSON. Main outputs are structured plan files, translated plan variants, `.humanize/rlcr/<timestamp>/` loop state, `.humanize/pr-loop/<timestamp>/` PR-loop state, `.humanize/skill/<timestamp>/` one-shot Codex outputs, generated prompts/summaries/review results, and local commits/pushes required by prompts. Runtime state is deliberately local under `.humanize/`, documented in `skills/humanize/SKILL.md:217-240`.
- gates_or_invariants: Root-level invariants include one active loop at a time (`scripts/setup-rlcr-loop.sh:320-345`, `scripts/setup-pr-loop.sh:217-241`), no direct state-file modification (`hooks/loop-write-validator.sh:150-162`, `hooks/loop-bash-validator.sh:139-168`), plan immutability during RLCR (`hooks/loop-codex-stop-hook.sh:291-371`), no branch switching (`hooks/loop-codex-stop-hook.sh:258-288`), clean committed work before stop review (`hooks/loop-codex-stop-hook.sh:578-675`), BitLesson summary delta validation (`hooks/loop-codex-stop-hook.sh:716-735`), and `.humanize` staging protection (`hooks/lib/loop-common.sh:1095-1230`).
- dependencies_and_callers: `README.md:9-27` defines the RLCR concept. `config/default_config.json:1-8` sets model/config defaults. `scripts/lib/config-loader.sh:63-137` merges default, user, and project config layers. `hooks/hooks.json:1-80` registers UserPromptSubmit, PreToolUse, PostToolUse, and Stop hooks. `commands/start-rlcr-loop.md:110-129` calls `scripts/setup-rlcr-loop.sh`; `commands/start-pr-loop.md:10-24` calls `scripts/setup-pr-loop.sh`; `commands/gen-plan.md:582-602` can auto-start RLCR.
- edge_cases_or_failure_modes: The system fail-closes on malformed hook JSON, deeply nested hook payloads, missing dependencies, stale or corrupted state, branch drift, dirty worktrees, missing summaries, Codex failures, PR lookup failures, symlink/path traversal in plan files, YAML-unsafe branch/model fields, and direct hook execution attempts. It uses timeouts for git/Codex/GitHub operations and template fallbacks when prompt templates cannot be loaded.
- validation_or_tests: The test tree contains 74 files, including script split runners, hook tests, monitor tests, config tests, model routing tests, template tests, and robustness tests. Relevant examples include `tests/test-pr-loop-1-scripts.sh`, `tests/test-stop-gate.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-bash-validator-patterns.sh`, and `tests/robustness/test-setup-scripts-robustness.sh`. I did not execute them because this branch export is read-only and many tests create temp repos, `.humanize` state, or mock binaries.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-031 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for generating a structured implementation plan from a draft and optionally transitioning into RLCR. It is a planning algorithm, not an implementation script.
- algorithmic_behavior: The command enforces “no coding during plan generation” and permits only plan/config/optional translated-plan writes before any auto-start (`commands/gen-plan.md:21-30`). It defines a strictly sequential phase pipeline (`commands/gen-plan.md:34-47`): parse mode flags, load config, validate IO, check repository relevance, run first Codex analysis, synthesize a Claude candidate plan, iterate Claude/Codex convergence, resolve user decisions, generate final plan structure, write/review/translate output, and optionally start RLCR.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, draft file, output path, `.humanize/config.json`, plan template path from validation, Codex analysis output, Claude candidate plans, convergence matrix, and user answers. Outputs are the main plan file, optional translated variant, optional default `.humanize/config.json`, and possibly an RLCR setup invocation. State variables include `AUTO_START_RLCR_IF_CONVERGED`, `GEN_PLAN_MODE`, `ALT_PLAN_LANGUAGE`, `ALT_PLAN_LANG_CODE`, `PLAN_CONVERGENCE_STATUS`, and `HUMAN_REVIEW_REQUIRED`.
- gates_or_invariants: Mode flags `--discussion` and `--direct` are mutually exclusive (`commands/gen-plan.md:51-60`). IO validation consumes specific exit codes from `scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:120-138`). Relevance must return `RELEVANT` before file creation (`commands/gen-plan.md:142-168`). Direct mode skips convergence and cannot satisfy auto-start conditions (`commands/gen-plan.md:243-246`). Pending user decisions are consolidated and can block auto-start (`commands/gen-plan.md:302-317`, `commands/gen-plan.md:582-589`). The final plan must include AC-format acceptance criteria, positive/negative tests, path boundaries, task routing tags, deliberation traceability, and pending decisions (`commands/gen-plan.md:362-518`).
- dependencies_and_callers: Uses `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, `prompt-template/plan/gen-plan-template.md`, Task agents `humanize:draft-relevance-checker` and Explore, and AskUserQuestion. It is exposed by the Humanize skill as “Generate Plan” (`skills/humanize/SKILL.md:61-69`, `skills/humanize/SKILL.md:134-140`).
- edge_cases_or_failure_modes: Malformed `.humanize/config.json` falls back to defaults without blocking (`commands/gen-plan.md:107-115`). Unsupported `alternative_plan_language` disables translation with a warning (`commands/gen-plan.md:101-106`). Missing Codex allows user choice to retry or continue Claude-only (`commands/gen-plan.md:196-201`). Mixed-language output triggers a user language-unification decision (`commands/gen-plan.md:543-555`). Auto-start failure reports the manual command (`commands/gen-plan.md:598-608`).
- validation_or_tests: Built-in validation is primarily command-script gating plus self-review: complete file reread, consistency check, draft preservation, no contradictions, and optional translation guard (`commands/gen-plan.md:521-580`). Related repository tests include `tests/test-gen-plan.sh`, `tests/test-config-error-handling.sh`, and `tests/test-template-references.sh`, though I did not execute them.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-061 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for the RLCR loop. It validates environment and plan inputs, creates `.humanize/rlcr/<timestamp>/` state, copies or synthesizes the plan backup, initializes BitLesson, writes the goal tracker, creates the round-0 prompt/summary, and prints the initial instructions.
- algorithmic_behavior: The script parses loop options and behavioral flags (`scripts/setup-rlcr-loop.sh:151-279`), checks required tools (`scripts/setup-rlcr-loop.sh:290-318`), enforces no active RLCR/PR loop (`scripts/setup-rlcr-loop.sh:320-345`), validates Agent Teams enablement (`scripts/setup-rlcr-loop.sh:347-363`), normalizes positional versus `--plan-file` input (`scripts/setup-rlcr-loop.sh:365-393`), hardens plan paths and content (`scripts/setup-rlcr-loop.sh:411-642`), records branch/base branch context (`scripts/setup-rlcr-loop.sh:644-777`), and writes loop artifacts (`scripts/setup-rlcr-loop.sh:779-1312`).
- inputs_outputs_state: Inputs are a relative plan path or `--skip-impl`, `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--track-plan-file`, `--claude-answer-codex`, `--agent-teams`, BitLesson flags, git state, config-derived Codex defaults, and environment variables such as `CLAUDE_PROJECT_DIR` and `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`. Outputs include `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, `round-0-summary.md`, `round-0-prompt.md`, `.humanize/.pending-session-id`, `.humanize/bitlesson.md`, and `.review-phase-started` for skip-impl mode. `state.md` fields are written at `scripts/setup-rlcr-loop.sh:842-865`.
- gates_or_invariants: Requires `codex`, `jq`, and `git` (`scripts/setup-rlcr-loop.sh:295-318`). Requires a git repo with at least one commit (`scripts/setup-rlcr-loop.sh:395-409`). Rejects absolute plan paths, spaces, shell metacharacters, symlink files/parents, missing/unreadable files, project escapes, and submodule-contained plans (`scripts/setup-rlcr-loop.sh:421-523`). Enforces tracked-plan cleanliness when `--track-plan-file` is used, and otherwise requires the plan to be untracked/gitignored (`scripts/setup-rlcr-loop.sh:524-573`). Requires at least five lines and at least three content lines (`scripts/setup-rlcr-loop.sh:577-642`). Requires clean git status except untracked `.humanize/` (`scripts/setup-rlcr-loop.sh:680-703`). Validates YAML-safe branch/model fields and allowed effort values (`scripts/setup-rlcr-loop.sh:654-678`, `scripts/setup-rlcr-loop.sh:761-767`).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md:114-116`, `commands/gen-plan.md:598-602`, and documented in `skills/humanize/SKILL.md:72-85`. It sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:25-33`), calls `scripts/bitlesson-init.sh` (`scripts/setup-rlcr-loop.sh:822-828`), and conditionally appends `prompt-template/claude/agent-teams-instructions.md` plus `prompt-template/claude/agent-teams-core.md` (`scripts/setup-rlcr-loop.sh:1148-1157`).
- edge_cases_or_failure_modes: `--skip-impl` can run without a real plan and writes a placeholder plan plus immediate review-phase marker (`scripts/setup-rlcr-loop.sh:374-384`, `scripts/setup-rlcr-loop.sh:791-813`, `scripts/setup-rlcr-loop.sh:878-880`). Remote-only base branches are rejected because `codex review --base` needs a local ref (`scripts/setup-rlcr-loop.sh:710-727`). Base branch auto-detection falls back remote default, local `main`, then local `master`, and errors if none exist (`scripts/setup-rlcr-loop.sh:729-758`). Pipe output is trapped late to avoid broken-pipe failures after setup work is done (`scripts/setup-rlcr-loop.sh:1218-1220`).
- validation_or_tests: Related validation is broad: `tests/robustness/test-setup-scripts-robustness.sh` covers setup edge cases; `tests/test-stop-gate.sh` covers downstream stop-gate behavior; `tests/test-plan-file-hooks.sh` covers plan-file protection; `tests/test-agent-teams.sh` covers Agent Teams prompt behavior. I did not execute tests in the read-only export.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-091 `file` `tests/test-pr-loop-1-scripts.sh`
- cursor: `[_]`
- core_role: Executable test runner for the first parallel shard of PR-loop tests. It validates script argument parsing and basic state side effects for PR-loop support scripts.
- algorithmic_behavior: The runner sets strict shell mode (`tests/test-pr-loop-1-scripts.sh:9`), resolves `SCRIPT_DIR` and `PROJECT_ROOT` (`tests/test-pr-loop-1-scripts.sh:11-12`), sources shared helpers and PR-loop test library (`tests/test-pr-loop-1-scripts.sh:14-15`), initializes a mock PR-loop test environment (`tests/test-pr-loop-1-scripts.sh:17`), sources the script test definitions (`tests/test-pr-loop-1-scripts.sh:19`), runs `run_script_tests` (`tests/test-pr-loop-1-scripts.sh:21`), prints a summary, and exits with the summary status (`tests/test-pr-loop-1-scripts.sh:23-24`).
- inputs_outputs_state: Inputs are the repository test files and shell environment. `init_pr_loop_test_env` creates a temp test directory, mock `gh`, and mock `codex` in PATH (`tests/test-pr-loop-lib.sh:107-121`). Output is pass/fail text plus a process exit code. Test-local state can include temp `.humanize/pr-loop/<timestamp>/state.md` and `cancel-state.md` for cancel tests (`tests/test-pr-loop-scripts.sh:211-247`), but not repository state.
- gates_or_invariants: The runner delegates exact assertions to `tests/test-pr-loop-scripts.sh`. That file checks `setup-pr-loop.sh` help, missing bot flag, invalid bot flag, recognized `--claude`/`--codex`, combined bots, `--max`, invalid `--max`, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:18-166`); `cancel-pr-loop.sh` help/no-loop/active-loop behavior (`tests/test-pr-loop-scripts.sh:172-253`); `fetch-pr-comments.sh` missing/invalid arguments (`tests/test-pr-loop-scripts.sh:259-326`); and `poll-pr-reviews.sh` missing PR, missing `--after`, and missing `--bots` (`tests/test-pr-loop-scripts.sh:332-399`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for counters, pass/fail, temp dirs, and optional git repo setup (`tests/test-helpers.sh:18-105`); `tests/test-pr-loop-lib.sh` for mocks and setup (`tests/test-pr-loop-lib.sh:27-121`); and the target scripts `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`. It is a split runner parallel to other PR-loop test shards referenced by its header (`tests/test-pr-loop-1-scripts.sh:3-6`).
- edge_cases_or_failure_modes: This shard focuses on argument validation, not full PR integration or stop-hook polling. Some positive flag-recognition tests intentionally allow later failure due to missing git/PR context as long as the specific argument is parsed (`tests/test-pr-loop-scripts.sh:66-90`, `tests/test-pr-loop-scripts.sh:104-153`). Mock `gh` returns simplified JSON and empty arrays, so it cannot prove GitHub API semantics beyond parser behavior (`tests/test-pr-loop-lib.sh:27-88`).
- validation_or_tests: This file itself is the validation entry point. It was inspected, not executed, because it creates temp files/mocks and can invoke git-backed scripts in a read-only managed workspace.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-121 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Rendered block-message template for the `.humanize` version-control protection gate. It explains why staging `.humanize/` is blocked and gives safe alternatives.
- algorithmic_behavior: The template declares `.humanize/` local loop state that must not be committed (`prompt-template/block/git-add-humanize.md:1-6`), suggests targeted staging commands (`prompt-template/block/git-add-humanize.md:8-14`), lists blocked broad/direct staging patterns (`prompt-template/block/git-add-humanize.md:16-25`), and documents a safe path for adding `.humanize*` to `.gitignore` via only `.gitignore` staging (`prompt-template/block/git-add-humanize.md:26-34`).
- inputs_outputs_state: Input is a render call from `git_add_humanize_blocked_message`; there are no template variables in this file. Output is the human-facing stderr message shown by hook validators. It does not mutate state, but it preserves the invariant that `.humanize` runtime state stays local.
- gates_or_invariants: The underlying detector blocks direct `.humanize` references, force-add broad scopes, `git add -A/--all` when `.humanize` exists, and broad scope staging when `.humanize` is not ignored (`hooks/lib/loop-common.sh:1095-1190`). The message renderer loads this template with an inline fallback (`hooks/lib/loop-common.sh:1193-1230`). `hooks/loop-bash-validator.sh` invokes the gate for Bash commands (`hooks/loop-bash-validator.sh:121-130`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` through `load_and_render_safe`, and on `hooks/lib/loop-common.sh` for the detection and renderer functions. It is indirectly used by the PreToolUse Bash hook from `hooks/hooks.json:42-49`.
- edge_cases_or_failure_modes: The detector intentionally avoids over-blocking names like `.humanizeconfig` or `.humanize-backup` by matching `.humanize` as a path segment (`hooks/lib/loop-common.sh:1132-1138`). Force add with broad scope is blocked because it bypasses `.gitignore` (`hooks/lib/loop-common.sh:1163-1168`). Non-force broad add is allowed if `.humanize` is already ignored (`hooks/lib/loop-common.sh:1181-1187`).
- validation_or_tests: Related tests are in hook/bash validator suites such as `tests/test-bash-validator-patterns.sh`, `tests/test-allowlist-validators.sh`, and `tests/test-plan-file-hooks.sh`. I inspected the implementation path but did not execute tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-151 `file` `prompt-template/claude/agent-teams-core.md`
- cursor: `[_]`
- core_role: Core Agent Teams prompt contract for RLCR. It converts the main Claude actor into a team leader whose algorithmic role is coordination, delegation, conflict prevention, progress monitoring, and quality review rather than direct implementation.
- algorithmic_behavior: The template sets a hard role rule that the leader must not write code or edit files (`prompt-template/claude/agent-teams-core.md:1-12`). It defines task splitting, cold-start context requirements, strict file ownership boundaries, coordination, output review, commit sequencing, plan approval for high-risk tasks, and BitLesson discipline (`prompt-template/claude/agent-teams-core.md:14-24`). It reiterates Task tool usage, monitoring/reassignment, merge/conflict responsibility, and no direct implementation (`prompt-template/claude/agent-teams-core.md:25-31`).
- inputs_outputs_state: Input is the generated RLCR prompt context when `--agent-teams` is enabled. Output is appended instruction text in `round-0-prompt.md` and later continuation prompts. It does not write state directly, but it changes execution ownership: team members perform coding while the leader coordinates.
- gates_or_invariants: Setup requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` before enabling Agent Teams (`scripts/setup-rlcr-loop.sh:347-363`). The setup script appends this core template when present (`scripts/setup-rlcr-loop.sh:1148-1157`) or uses a fallback (`scripts/setup-rlcr-loop.sh:1158-1167`). The stop hook reinjects delegation warnings and continuation/core instructions in later prompts (`hooks/loop-codex-stop-hook.sh:1702-1724`, `hooks/loop-codex-stop-hook.sh:1793-1813`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `prompt-template/claude/agent-teams-instructions.md`, and `prompt-template/claude/agent-teams-continue.md`. The mode is exposed as `--agent-teams` in setup help (`scripts/setup-rlcr-loop.sh:89-91`) and skill docs (`skills/humanize/SKILL.md:98-100`).
- edge_cases_or_failure_modes: The template explicitly calls out zero-context worker cold starts (`prompt-template/claude/agent-teams-core.md:17`) and silent overwrite risk when two teammates edit the same file (`prompt-template/claude/agent-teams-core.md:18`). If the file is missing, setup and stop hook fall back to inline Agent Teams text, reducing detail but preserving the no-direct-coding invariant.
- validation_or_tests: Related test coverage is indicated by `tests/test-agent-teams.sh` and template reference tests. I did not execute them in read-only mode.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-181 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: Top-level Humanize skill instruction. It documents the plugin’s RLCR, PR-loop, plan-generation, ask-codex, cancellation, monitoring, state layout, prerequisites, and required stop-gate behavior for skill-mode execution.
- algorithmic_behavior: The skill defines the philosophy of iterative AI implementation plus independent review (`skills/humanize/SKILL.md:21-28`). It specifies RLCR as two phases: implementation summary review by Codex, then `codex review --base <branch>` with severity markers before finalize (`skills/humanize/SKILL.md:31-48`). It defines PR loop behavior from PR detection through bot review polling and local Codex validation (`skills/humanize/SKILL.md:49-60`). It defines plan generation outputs (`skills/humanize/SKILL.md:61-69`) and command entrypoints (`skills/humanize/SKILL.md:70-146`).
- inputs_outputs_state: Inputs are hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, plan files, CLI options, PR bot flags, Codex model/timeout options, and user commands. Outputs are loop state under `.humanize/rlcr`, PR-loop state under `.humanize/pr-loop`, skill outputs under `.humanize/skill`, generated plan files, summaries, review results, finalize state, and monitor displays. The state directory layout is documented at `skills/humanize/SKILL.md:217-240`.
- gates_or_invariants: Skill-mode RLCR must run `scripts/rlcr-stop-gate.sh` so the same hook-equivalent transitions and blocking apply outside Claude Code hooks (`skills/humanize/SKILL.md:47`, `skills/humanize/SKILL.md:82-85`). Important rules require summaries, goal tracker maintenance, thorough evidence, no state-file/cancel cheating, and stop-gate usage (`skills/humanize/SKILL.md:203-210`). The goal tracker has immutable and mutable sections to prevent goal drift (`skills/humanize/SKILL.md:189-201`).
- dependencies_and_callers: References `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh`. It depends on runtime-root hydration by the installer (`skills/humanize/SKILL.md:11-20`) and external prerequisites `codex` and `gh` (`skills/humanize/SKILL.md:212-216`).
- edge_cases_or_failure_modes: Supports review-only mode with `--skip-impl` (`skills/humanize/SKILL.md:78-80`, `skills/humanize/SKILL.md:94-100`). `--track-plan-file`, `--push-every-round`, `--claude-answer-codex`, `--agent-teams`, `--yolo`, and `--skip-quiz` alter setup and review behavior (`skills/humanize/SKILL.md:87-100`). Exit codes for `ask-codex.sh` and `validate-gen-plan-io.sh` are documented for handling validation and timeout failures (`skills/humanize/SKILL.md:253-268`).
- validation_or_tests: The skill points to operational validation through `rlcr-stop-gate.sh`, monitor commands, and documented exit codes. Repository tests exercise the referenced scripts/hooks, including stop gate, monitor, setup scripts, config, and PR loop tests. I inspected rather than executed due read-only constraints.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 item evidence sections present, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`