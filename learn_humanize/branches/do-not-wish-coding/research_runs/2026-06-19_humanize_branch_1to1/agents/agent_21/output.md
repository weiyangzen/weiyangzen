# agent_21 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-021 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: Skill-level workflow contract for Humanize. The directory contains one recursive child, `skills/humanize/SKILL.md`, and defines the user-facing algorithm for RLCR, PR review loops, and gen-plan. It is not an executable state machine by itself, but it is the instruction surface that routes users into the executable scripts and hooks.
- algorithmic_behavior: `SKILL.md` describes three core workflows. RLCR has an implementation phase where AI writes summaries and Codex checks for `COMPLETE`, then a code-review phase driven by `codex review --base <branch>` and severity markers `[P0-9]`, ending in finalize when no issues remain (`skills/humanize/SKILL.md:31-47`). PR Loop detects a current PR, fetches bot comments, asks AI to fix, pushes, triggers bot rereview, polls every 30 seconds with 15 minute timeout per bot, and repeats until approval or max iterations (`skills/humanize/SKILL.md:49-59`). Gen-plan transforms a draft into a structured implementation plan with ACs, path boundaries, dependencies, and sequencing (`skills/humanize/SKILL.md:61-68`).
- inputs_outputs_state: Inputs are plan files, current branch/PR state, GitHub bot reviews, command flags such as `--max`, `--codex-model`, `--base-branch`, `--skip-impl`, `--track-plan-file`, `--push-every-round`, `--claude`, `--codex`, and gen-plan `--input/--output` (`skills/humanize/SKILL.md:72-146`). Outputs are `.humanize/rlcr/<timestamp>/state.md`, goal tracker, round summaries, review results, finalize/complete state files, and `.humanize/pr-loop/<timestamp>/state.md` plus `resolution-N.md` (`skills/humanize/SKILL.md:217-240`). State transitions are specified conceptually: implementation to review when Codex outputs `COMPLETE`, review to finalize when `codex review` finds no issues, and PR loop rounds continue until bots approve or iteration caps are hit.
- gates_or_invariants: The skill explicitly requires running `scripts/rlcr-stop-gate.sh` in skill mode to enforce hook-equivalent transitions and blocking (`skills/humanize/SKILL.md:47`, `skills/humanize/SKILL.md:82-85`). It also forbids cheating by editing state files or running cancel commands to escape, requires summaries each round, requires goal tracker maintenance, and says the stop gate should control phase changes (`skills/humanize/SKILL.md:203-210`). Gen-plan’s plan-file structure requires ACs, positive/negative tests, path boundaries, dependencies, and implementation notes that prevent plan terminology from leaking into code (`skills/humanize/SKILL.md:148-187`).
- dependencies_and_callers: The skill routes to `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`, and `scripts/ask-codex.sh` (`skills/humanize/SKILL.md:72-146`). It depends on `codex` and `gh` (`skills/humanize/SKILL.md:212-215`). It coordinates with sibling `scripts/`, `hooks/`, `prompt-template/`, `commands/`, and runtime `.humanize/` folders; the directory structure section names the state files the hook library and cancel scripts later operate on (`skills/humanize/SKILL.md:217-240`).
- edge_cases_or_failure_modes: The skill is hydrated with `{{HUMANIZE_RUNTIME_ROOT}}`; if not replaced by installation, command examples are inert (`skills/humanize/SKILL.md:11-19`). It declares `user-invocable: false` (`skills/humanize/SKILL.md:1-5`), so direct invocation depends on command wrappers. It gives algorithmic rules but not enforcement; enforcement lives in scripts/hooks. It also documents default models such as `gpt-5.4`, which may drift from installed runtime defaults if scripts/config evolve.
- validation_or_tests: The directory itself has no test file, but it points to executable surfaces covered by tests: PR loop scripts, RLCR state/hook tests, gen-plan tests, template reference tests, and hook robustness tests found under `tests/`. Its workflow claims are concretely backed by the assigned `hooks/lib/loop-common.sh`, `scripts/cancel-pr-loop.sh`, and `tests/test-gen-plan.sh`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-051 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for the PR loop state machine. It converts the newest active `.humanize/pr-loop/<timestamp>/state.md` into `cancel-state.md` and creates `.cancel-requested`, giving both persistent terminal state and a live signal for polling stop hooks.
- algorithmic_behavior: Parses only `--force` and help; `--force` is accepted but currently has no extra behavior (`scripts/cancel-pr-loop.sh:24-66`). It resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, sets `LOOP_BASE_DIR` to `.humanize/pr-loop`, selects the lexicographically newest child directory, and requires a `state.md` in that directory (`scripts/cancel-pr-loop.sh:72-98`). It extracts `current_round`, `max_iterations`, and `pr_number` from simple `key:` lines, defaults missing fields to `?`, touches `.cancel-requested`, moves `state.md` to `cancel-state.md`, then prints `CANCELLED` plus a summary (`scripts/cancel-pr-loop.sh:104-132`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR`, current working directory, newest `.humanize/pr-loop/*/state.md`, and optional frontmatter fields `current_round`, `max_iterations`, `pr_number` (`scripts/cancel-pr-loop.sh:72-112`). Outputs are sentinel stdout values `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`; filesystem outputs are `.cancel-requested` and `cancel-state.md` (`scripts/cancel-pr-loop.sh:78-97`, `scripts/cancel-pr-loop.sh:118-130`). State transition is active PR loop `state.md` to terminal `cancel-state.md`.
- gates_or_invariants: It affects only `.humanize/pr-loop/`, not RLCR (`scripts/cancel-pr-loop.sh:48-56`). It refuses to operate when no loop directory exists or when the newest loop directory lacks `state.md` (`scripts/cancel-pr-loop.sh:78-98`). `set -euo pipefail` makes unexpected command failures abort (`scripts/cancel-pr-loop.sh:18`). The command wrapper allows only this script and `--force`, and says the script owns all cancellation logic (`commands/cancel-pr-loop.md:1-21`).
- dependencies_and_callers: Called from `commands/cancel-pr-loop.md` via allowed Bash tools (`commands/cancel-pr-loop.md:1-15`) and documented in the skill (`skills/humanize/SKILL.md:128-132`). The PR stop hook checks `.cancel-requested` during polling and exits the poll loop when present (`hooks/pr-loop-stop-hook.sh:952-956`). `tests/test-pr-loop-scripts.sh` includes help, no-loop, and active-loop cancellation tests (`tests/test-pr-loop-scripts.sh:169-250`).
- edge_cases_or_failure_modes: Selection is by `ls | sort -r | head -1`, so it assumes timestamp-like directory names and does not search older directories if the newest is terminal or malformed (`scripts/cancel-pr-loop.sh:76`, `scripts/cancel-pr-loop.sh:91-98`). It has no race protection if another process moves/removes `state.md` between check and `mv`; `set -e` would abort without the documented sentinel. It does not validate YAML frontmatter, only greps simple top-level fields. `--force` may mislead users because it is a no-op (`scripts/cancel-pr-loop.sh:28-30`, `scripts/cancel-pr-loop.sh:40`). Paths with embedded newlines would be brittle because discovery uses text sorting.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` verifies `--help`, missing loop returning exit 1 with `NO_LOOP`, and active cancellation creating `cancel-state.md` while removing `state.md` (`tests/test-pr-loop-scripts.sh:181-244`). PR loop system/monitor tests also model `cancel-state.md` as a terminal state.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-081 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the gen-plan command surface. It validates command metadata, agent metadata, convergence workflow requirements, plan template consistency, validator exit codes, and content constraints.
- algorithmic_behavior: Defines `pass`/`fail` counters without exiting immediately, then runs positive and negative checks under `set -euo pipefail` (`tests/test-gen-plan.sh:9-40`). Positive tests require `commands/gen-plan.md`, frontmatter description, allowed tools, argument hint, `agents/draft-relevance-checker.md`, ask-codex availability, auto-start flag, direct-mode non-convergence, ultrathink instruction, pending decisions, Codex first-pass before Claude plan, convergence loop, max-three-round limit, task tags, and pending-decision consolidation (`tests/test-gen-plan.sh:54-248`). Agent tests require `name: draft-relevance-checker`, `model: haiku`, and `tools:` (`tests/test-gen-plan.sh:249-288`). Version consistency checks `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md` (`tests/test-gen-plan.sh:290-311`). Negative tests create invalid fixtures for names, missing frontmatter, malformed YAML, and invalid model aliases (`tests/test-gen-plan.sh:313-532`). It then exercises `scripts/validate-gen-plan-io.sh` exit-code behavior and checks that the markdown block embedded in `commands/gen-plan.md` exactly equals `prompt-template/plan/gen-plan-template.md` (`tests/test-gen-plan.sh:556-716`).
- inputs_outputs_state: Inputs are repository files under `commands/`, `agents/`, `prompt-template/plan/`, `scripts/`, `.claude-plugin/`, and `README.md` (`tests/test-gen-plan.sh:11-15`, `tests/test-gen-plan.sh:295-297`, `tests/test-gen-plan.sh:562`). It creates temporary fixture directories with `mktemp -d` for negative tests and validator tests (`tests/test-gen-plan.sh:322-324`, `tests/test-gen-plan.sh:564-567`). Outputs are PASS/FAIL lines, a summary count, and process exit 0 only if `TESTS_FAILED` is zero (`tests/test-gen-plan.sh:718-736`).
- gates_or_invariants: Enforces gen-plan as planning-only and convergence-aware by requiring direct mode to set `PLAN_CONVERGENCE_STATUS=partially_converged`, auto-start to require `GEN_PLAN_MODE=discussion`, and pending user decisions to be consolidated from `QUESTIONS_FOR_USER` plus `needs_user_decision` sources (`tests/test-gen-plan.sh:137-147`, `tests/test-gen-plan.sh:237-247`). Enforces exact short model aliases rather than prefix aliases for `opus`, `sonnet`, and `haiku` (`tests/test-gen-plan.sh:481-520`). Enforces English-only content by rejecting CJK/emoji when `grep -P` supports the regex (`tests/test-gen-plan.sh:534-554`). Enforces IO validator exit code contract: invalid args 6, missing input 1, empty input 2, missing output dir 3, existing output or directory 4, valid paths 0, help 6 (`tests/test-gen-plan.sh:569-703`).
- dependencies_and_callers: Validates `commands/gen-plan.md`, which allows `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, Read/Glob/Grep/Task/Write/AskUserQuestion (`commands/gen-plan.md:1-14`). It validates `agents/draft-relevance-checker.md`, a haiku agent with Read/Glob/Grep tools and relevance verdict outputs (`agents/draft-relevance-checker.md:1-37`). It is included by `tests/run-all-tests.sh` as `test-gen-plan.sh` according to repository search output.
- edge_cases_or_failure_modes: The script installs an `EXIT` trap for `TEST_FIXTURES_DIR`, then later installs another `EXIT` trap for `SCRIPT_TEST_DIR`, which overrides the first cleanup trap in Bash (`tests/test-gen-plan.sh:323-324`, `tests/test-gen-plan.sh:566-567`). Its YAML checks are regex heuristics, not a real YAML parser (`tests/test-gen-plan.sh:435-450`). If `grep -P` is unavailable, the emoji/CJK test can silently pass because stderr is suppressed and a nonzero grep is treated as “not found” (`tests/test-gen-plan.sh:540-552`). Some option-recognition checks intentionally ignore validator failure and only assert that errors are not “unknown option” (`tests/test-gen-plan.sh:672-694`).
- validation_or_tests: This file is itself the validation. It cross-checks the current command against `scripts/validate-gen-plan-io.sh`, whose actual parser and exit codes are defined at `scripts/validate-gen-plan-io.sh:16-179`, including `--discussion/--direct` mutual exclusion (`scripts/validate-gen-plan-io.sh:76-80`) and template existence gate (`scripts/validate-gen-plan-io.sh:162-178`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-111 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared hook/runtime library for the RLCR and PR loop state machines. It centralizes state field constants, JSON hook input validation, config-backed Codex defaults, active loop discovery, state parsing, review issue extraction, file/path gate messages, cancel authorization, PR bot naming, `.humanize` git-add protection, terminal state transitions, and PR goal tracker updates.
- algorithmic_behavior: Uses a source guard to avoid double-sourcing readonly constants (`hooks/lib/loop-common.sh:16-18`). Defines state fields and exit reason constants (`hooks/lib/loop-common.sh:24-64`). `validate_hook_input` rejects null bytes, invalid UTF-8 when `iconv` exists, invalid JSON, and missing `tool_name`, then sets `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` globals (`hooks/lib/loop-common.sh:70-116`). It sources config-loader while preserving caller shell options, loads merged config, and sets `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, and `DEFAULT_CODEX_EFFORT` with precedence from caller pre-set values, config, then hardcoded fallback (`hooks/lib/loop-common.sh:159-194`). `find_active_loop` implements zombie-loop protection: without session filter it only checks the newest directory; with a session filter it finds the newest directory for that session and returns it only if active (`hooks/lib/loop-common.sh:251-327`). State parsers extract YAML-like frontmatter into `STATE_*` globals; strict mode rejects missing separators, missing required fields, nonnumeric rounds/max iterations, and invalid `review_started` (`hooks/lib/loop-common.sh:344-486`). `detect_review_issues` scans only the last 50 lines of Codex review logs for `[P0-9]` in the first 10 characters, writes extracted issues to `round-N-review-result.md`, and returns 0/1/2 for issues/no issues/log missing (`hooks/lib/loop-common.sh:488-557`). Path/message helpers render block templates via `load_and_render_safe` (`hooks/lib/loop-common.sh:608-666`). `is_cancel_authorized` validates a narrow `mv state.md|finalize-state.md cancel-state.md` command only when `.cancel-requested` exists, rejecting command substitution, shell operators, extra args, mixed quote styles, remaining `$`, wrong paths, and symlink sources (`hooks/lib/loop-common.sh:698-875`). PR helpers map bot names, locate active PR loops, validate `round-N-pr-resolve.md`, and block PR loop state/read-only file writes through callers (`hooks/lib/loop-common.sh:883-1071`). `git_adds_humanize` detects unsafe `git add` forms targeting `.humanize` or broad force/all adds (`hooks/lib/loop-common.sh:1073-1191`). `command_modifies_file` detects shell commands that modify protected paths via redirection, tee, sed/awk/perl in-place edits, mv/cp/rm/dd/truncate/printf/exec fd (`hooks/lib/loop-common.sh:1247-1275`). `end_loop` renames active state to `<reason>-state.md` after reason validation (`hooks/lib/loop-common.sh:1291-1324`). `update_pr_goal_tracker` idempotently appends or repairs per-round reviewer rows/log entries and updates total statistics (`hooks/lib/loop-common.sh:1330-1494`).
- inputs_outputs_state: Inputs include hook JSON, state files in `.humanize/rlcr/<timestamp>/`, PR loop state under `.humanize/pr-loop/<timestamp>/`, config from `.humanize/config.json` through `scripts/lib/config-loader.sh`, templates under `prompt-template/`, Codex review logs in `$CACHE_DIR`, and optional bot result JSON (`hooks/lib/loop-common.sh:70-177`, `hooks/lib/loop-common.sh:509-513`, `hooks/lib/loop-common.sh:1347-1350`). Outputs are global variables (`VALIDATED_*`, `STATE_*`, default model globals), rendered blocking messages, extracted review result files, state renames to terminal files, and updated PR goal tracker markdown (`hooks/lib/loop-common.sh:105-114`, `hooks/lib/loop-common.sh:396-486`, `hooks/lib/loop-common.sh:546-552`, `hooks/lib/loop-common.sh:1313-1319`, `hooks/lib/loop-common.sh:1432-1492`). State transitions include active `state.md`/`finalize-state.md` discovery, terminal transitions to `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`, and PR goal tracker issue count accumulation.
- gates_or_invariants: Fail-closed gates include strict state parsing before validators allow file operations, template-backed block messages, protected state/finalize files, protected goal tracker after round 0, authorized cancel-only state renames, `.humanize` git-add protection, and PR loop read-only file protections. Callers show these gates in action: write/edit validators parse strict state and block state writes (`hooks/loop-write-validator.sh:142-162`, `hooks/loop-edit-validator.sh:102-125`), read validator blocks wrong round/location reads (`hooks/loop-read-validator.sh:118-150`), Bash validator blocks unsafe git adds, unauthorized state edits, summary Bash writes, and PR state/read-only writes (`hooks/loop-bash-validator.sh:121-130`, `hooks/loop-bash-validator.sh:151-168`, `hooks/loop-bash-validator.sh:387-390`, `hooks/loop-bash-validator.sh:415-455`).
- dependencies_and_callers: Sourced by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/ask-codex.sh`, `scripts/bitlesson-select.sh`, and `scripts/humanize.sh` according to repository references. It depends on `jq`, optionally `iconv`, `git`, `sed`, `awk`, `grep`, `tail`, `wc`, `date`, and sibling `hooks/lib/template-loader.sh` plus `scripts/lib/config-loader.sh` (`hooks/lib/loop-common.sh:99-106`, `hooks/lib/loop-common.sh:159-194`).
- edge_cases_or_failure_modes: State parsing is YAML-like grep/sed parsing, not full YAML, so quoted/nested/multiline values are limited (`hooks/lib/loop-common.sh:344-367`). `parse_state_file_strict` documentation lists return codes through 5 but the implementation also returns 6 for invalid `review_started` (`hooks/lib/loop-common.sh:414-423`, `hooks/lib/loop-common.sh:473-476`). Numeric validation allows negative rounds/max iterations (`hooks/lib/loop-common.sh:461-470`). Active loop discovery relies on lexicographic directory ordering and intentionally refuses to revive older active loops when the newest relevant loop is terminal (`hooks/lib/loop-common.sh:251-327`). `detect_review_issues` can miss issues outside the final 50 lines or with `[P?]` after character 10 by design (`hooks/lib/loop-common.sh:497-505`, `hooks/lib/loop-common.sh:524-535`). Shell-command detection is regex-based and necessarily conservative/brittle for complex quoting or generated shell. `git_adds_humanize` checks `.humanize` and `git check-ignore` in the current process directory, which may be imperfect for `git -C <dir> add ...` cases despite recognizing the command form (`hooks/lib/loop-common.sh:1080-1125`, `hooks/lib/loop-common.sh:1170-1185`).
- validation_or_tests: Extensively covered. `tests/test-codex-review-merge.sh` targets `detect_review_issues`; `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh` target `is_cancel_authorized`; `tests/test-state-exit-naming.sh`, `tests/test-session-id.sh`, and robustness state/session tests target `find_active_loop` and `end_loop`; `tests/test-humanize-escape.sh` targets `git_adds_humanize`; `tests/test-bash-validator-patterns.sh` targets `command_modifies_file`; `tests/test-pr-loop-system.sh` targets `update_pr_goal_tracker`; `tests/test-template-references.sh` validates common template references including `block/summary-bash-write.md` (`tests/test-template-references.sh:149-167`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-141 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when an active RLCR loop detects Bash-based writes to round summary files. It is a gate payload, not a state machine, but it enforces the state machine’s “summaries must be written through Write/Edit validation” invariant.
- algorithmic_behavior: The template emits a short markdown block titled “Bash Write Blocked: Use Write or Edit Tool”, tells the agent not to use Bash to modify summary files, gives the correct path through `{{CORRECT_PATH}}`, and explains that `cat`, `echo`, `sed`, `awk`, etc. bypass validation hooks (`prompt-template/block/summary-bash-write.md:1-8`).
- inputs_outputs_state: Input is the `CORRECT_PATH` template variable. Output is a rendered markdown error/block message to stderr through caller functions. It has no direct filesystem state transition.
- gates_or_invariants: Enforces that summary files are modified only with Write/Edit tools so round number and location validators can run. This prevents Bash redirects or in-place commands from bypassing summary validation.
- dependencies_and_callers: `summary_bash_blocked_message` loads this template with `CORRECT_PATH=$correct_path` and has an inline fallback if loading fails (`hooks/lib/loop-common.sh:646-655`). `hooks/loop-bash-validator.sh` calls that function when `command_modifies_file` matches `round-[0-9]+-summary.md`, using the current active loop and round to construct the correct path (`hooks/loop-bash-validator.sh:382-390`). `tests/test-template-references.sh` includes `block/summary-bash-write.md` in the required common template list (`tests/test-template-references.sh:149-167`).
- edge_cases_or_failure_modes: If the template is missing or fails to render, `loop-common.sh` falls back to a shorter inline message (`hooks/lib/loop-common.sh:650-655`). The template only covers Bash writes; Write/Edit behavior is enforced by separate validators. The correctness of `{{CORRECT_PATH}}` depends on successful active loop discovery and strict state parsing before the Bash validator reaches this block.
- validation_or_tests: Template existence is checked by `tests/test-template-references.sh` (`tests/test-template-references.sh:149-167`). Functional use is indirectly tested by Bash validator pattern tests and hook tests that exercise summary-file write blocking.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-171 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: Prompt header template for the initial PR review loop round. It seeds `round-0-prompt.md` with PR identity and the section where fetched comments are appended, anchoring the PR loop’s first agent task.
- algorithmic_behavior: Renders “Read and execute below with ultrathink”, a `PR Review Loop (Round 0)` heading, a concise description that the agent is monitoring remote review bot feedback, PR number, branch, active bots, and a “Review Comments” section lead-in (`prompt-template/pr-loop/round-0-header.md:1-15`).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `START_BRANCH`, and `ACTIVE_BOTS_DISPLAY` template variables (`prompt-template/pr-loop/round-0-header.md:7-10`). In setup, additional template variables such as `RESOLVE_PATH` and `BOT_MENTION_STRING` are prepared for adjacent task templates (`scripts/setup-pr-loop.sh:731-738`). Output is the first content written to `.humanize/pr-loop/<timestamp>/round-0-prompt.md`; setup then appends fetched comments and either the no-comments or has-comments task body (`scripts/setup-pr-loop.sh:721-764`, `scripts/setup-pr-loop.sh:766-851`).
- gates_or_invariants: It sets the round identity to Round 0 and instructs ultrathink execution. By separating the header from task templates, setup can append the fetched comments before choosing the task branch based on the `*No comments found.*` sentinel (`scripts/setup-pr-loop.sh:723-729`, `scripts/setup-pr-loop.sh:766-847`).
- dependencies_and_callers: Loaded by `scripts/setup-pr-loop.sh` via `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-header.md"` with a matching fallback header (`scripts/setup-pr-loop.sh:740-758`). It coordinates with `prompt-template/pr-loop/round-0-task-no-comments.md`, `prompt-template/pr-loop/round-0-task-has-comments.md`, fetched comment files, and the generated `round-0-pr-resolve.md` path (`scripts/setup-pr-loop.sh:721-851`).
- edge_cases_or_failure_modes: If the template is missing or invalid, setup falls back to an inline header with the same placeholders (`scripts/setup-pr-loop.sh:740-758`). If template variables are not rendered, the prompt would expose raw placeholders. The template does not itself enforce PR state validity or comment freshness; those are setup/fetch responsibilities. Large or malformed fetched comment content is appended after the header without this template sanitizing it (`scripts/setup-pr-loop.sh:760-764`).
- validation_or_tests: Covered indirectly by PR loop setup and system tests. Repository references show setup uses this exact template, and PR loop tests validate prompt/session creation paths. Unlike `block/summary-bash-write.md`, it is not listed in the common-template cross-reference array in `tests/test-template-references.sh`, so coverage is through PR setup behavior rather than that common-template list.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DO_NOT_WISH_CODING-HZ-021, DO_NOT_WISH_CODING-HZ-051, DO_NOT_WISH_CODING-HZ-081, DO_NOT_WISH_CODING-HZ-111, DO_NOT_WISH_CODING-HZ-141, DO_NOT_WISH_CODING-HZ-171
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`