# agent_13 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-013 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared hook/runtime library for the RLCR and PR-loop state machines. The directory contains exactly two recursive descendants in this export: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`.
- algorithmic_behavior: `loop-common.sh` defines common constants, JSON hook input validation, state-file discovery/parsing, loop directory selection, path classification, mutation detection, cancellation authorization, bot-name mapping, `.humanize` git-add protection, terminal state transition helpers, and PR goal-tracker updates. Key state helpers include `validate_hook_input` for JSON/null-byte/UTF-8/tool-name validation at `hooks/lib/loop-common.sh:79`, `find_active_loop` for session-aware RLCR active-loop selection at `hooks/lib/loop-common.sh:269`, tolerant and strict state parsers at `hooks/lib/loop-common.sh:389` and `hooks/lib/loop-common.sh:423`, `find_active_pr_loop` at `hooks/lib/loop-common.sh:963`, `validate_pr_resolve_round` at `hooks/lib/loop-common.sh:1008`, `git_adds_humanize` at `hooks/lib/loop-common.sh:1095`, `command_modifies_file` at `hooks/lib/loop-common.sh:1250`, and `end_loop` at `hooks/lib/loop-common.sh:1298`. `template-loader.sh` supplies template directory resolution, loading, single-pass rendering, fallback rendering, and template directory validation at `hooks/lib/template-loader.sh:26`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:170`, and `hooks/lib/template-loader.sh:208`.
- inputs_outputs_state: Inputs include Claude hook JSON from validators/stop hooks, `.humanize/rlcr/<timestamp>/*state.md`, `.humanize/pr-loop/<timestamp>/state.md`, template files under `prompt-template`, shell command strings, file paths, bot lists, Git state, and optional config from `scripts/lib/config-loader.sh`. Outputs are shell return codes, stdout paths or rendered strings, stderr block messages, global `STATE_*` variables, updated goal tracker content, and renamed terminal state files such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`. Config-backed defaults are loaded by sourcing `scripts/lib/config-loader.sh` at `hooks/lib/loop-common.sh:167`; defaults are merged from plugin, user, and project configs by `scripts/lib/config-loader.sh:63`.
- gates_or_invariants: Hook JSON must be valid, non-null-byte, and contain `tool_name`; deeply nested JSON is detected by `is_deeply_nested` at `hooks/lib/loop-common.sh:139`. Strict state parsing requires YAML frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, plus numeric/boolean validation at `hooks/lib/loop-common.sh:431` through `hooks/lib/loop-common.sh:485`. `find_active_loop` intentionally checks only the newest unfiltered directory to prevent zombie loop revival, while session-filtered lookup stops at the newest matching session even if terminal at `hooks/lib/loop-common.sh:251` through `hooks/lib/loop-common.sh:327`. Cancellation is tightly constrained to an authorized signal file plus an exact `mv state.md|finalize-state.md cancel-state.md` shape, rejecting command substitution, shell chaining, mixed quotes, extra args, remaining `$`, and symlinked sources at `hooks/lib/loop-common.sh:716` through `hooks/lib/loop-common.sh:875`. Template rendering is single-pass so values containing `{{OTHER}}` are not recursively substituted, documented and implemented at `hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:131`.
- dependencies_and_callers: The directory is sourced by hook scripts registered in `hooks/hooks.json`: read/write/edit/bash validators, `loop-post-bash-hook.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh`. `hooks/loop-bash-validator.sh` uses shared input validation, session-aware active-loop discovery, strict state parsing, `git_adds_humanize`, cancel authorization, and `command_modifies_file` at `hooks/loop-bash-validator.sh:24` through `hooks/loop-bash-validator.sh:129`. `hooks/loop-write-validator.sh` uses state parsing and the plan-backup template at `hooks/loop-write-validator.sh:135` through `hooks/loop-write-validator.sh:186`. `hooks/loop-codex-stop-hook.sh` uses `find_active_loop` and templates at `hooks/loop-codex-stop-hook.sh:45` through `hooks/loop-codex-stop-hook.sh:62`. `hooks/pr-loop-stop-hook.sh` uses `find_active_pr_loop`, bot mapping, YAML list building, and goal-tracker updates at `hooks/pr-loop-stop-hook.sh:52` through `hooks/pr-loop-stop-hook.sh:184`.
- edge_cases_or_failure_modes: JSON with null bytes or invalid UTF-8 is rejected; missing `jq` affects config/template-dependent behavior. Active-loop discovery avoids older stale loops but can ignore older active directories by design. `command_modifies_file` is regex-based and intentionally has known limitations such as not detecting `cp file1 file2 goal-tracker.md` with multiple sources, called out by `tests/test-bash-validator-patterns.sh:165`. `git_adds_humanize` depends on current working directory and `git check-ignore`; if `.humanize` does not exist, broad non-force adds are not blocked. `template-loader.sh` uses `env "${env_vars[@]}" awk`; malformed variable assignments are not strongly validated here, but template robustness tests cover unusual values elsewhere.
- validation_or_tests: Direct specs include `tests/test-bash-validator-patterns.sh`, which exercises `command_modifies_file`, and `tests/robustness/test-pr-loop-api-robustness.sh`, which exercises `find_active_pr_loop` and PR-loop API behavior. Related coverage appears in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/robustness/test-template-error-robustness.sh`, `tests/robustness/test-hook-input-robustness.sh`, `tests/test-session-id.sh`, and PR-loop hook/stophook tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-043 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: PostToolUse Bash hook that records the Claude session ID into a just-created RLCR state file, binding an active loop to the initiating leader session before team members or later sessions can be affected by the same hook set.
- algorithmic_behavior: The hook reads raw hook JSON from stdin at `hooks/loop-post-bash-hook.sh:26`, locates `.humanize/.pending-session-id` under `CLAUDE_PROJECT_DIR` or cwd at `hooks/loop-post-bash-hook.sh:29` through `hooks/loop-post-bash-hook.sh:33`, and treats that signal as a two-line one-shot protocol: target state path and full setup-script command signature. If the signal is absent it exits 0. If the state file is missing, it deletes the stale signal and exits 0. If a signature exists, it extracts `.tool_input.command` with `jq` and requires the command to start with the exact setup script path, quoted or unquoted, followed by end-of-string or POSIX whitespace at `hooks/loop-post-bash-hook.sh:56` through `hooks/loop-post-bash-hook.sh:91`. It then extracts `.session_id`, patches only an empty `session_id:` line with `awk`, and removes the signal at `hooks/loop-post-bash-hook.sh:94` through `hooks/loop-post-bash-hook.sh:123`.
- inputs_outputs_state: Inputs are hook JSON fields `tool_input.command` and `session_id`, plus `.humanize/.pending-session-id`. The signal is produced by `scripts/setup-rlcr-loop.sh`, which writes `session_id:` empty in `state.md` at `scripts/setup-rlcr-loop.sh:842` through `scripts/setup-rlcr-loop.sh:864`, then writes the pending signal at `scripts/setup-rlcr-loop.sh:867` through `scripts/setup-rlcr-loop.sh:876`. Outputs are an in-place state transition from `session_id:` to `session_id: <id>`, a temporary state file `<state>.tmp.$$`, and deletion of the pending signal. It otherwise exits silently with code 0.
- gates_or_invariants: The hook is intentionally one-shot and only consumes the signal for a real setup invocation. Boundary-aware matching rejects commands that merely mention the path, use only the basename, or concatenate characters after the quoted/unquoted path. It patches only if the current state has an empty `session_id`; an existing session ID is not overwritten at `hooks/loop-post-bash-hook.sh:105` through `hooks/loop-post-bash-hook.sh:120`. If `jq` is unavailable or command/session extraction fails, it leaves the signal for a later attempt instead of deleting it, except for a bad state-file pointer.
- dependencies_and_callers: Registered as a `PostToolUse` Bash hook in `hooks/hooks.json:52` through `hooks/hooks.json:60`. It coordinates with `scripts/setup-rlcr-loop.sh`’s pending-signal creation and with `find_active_loop` session filtering in `hooks/lib/loop-common.sh:269`, which later lets validators and stop hooks apply only to the matching session.
- edge_cases_or_failure_modes: Empty or missing state path deletes the signal as stale. Missing command leaves the signal intact. Missing session ID leaves the signal intact. Session IDs with `/`, `&`, or `.` are safe because replacement uses `awk -v`, not `sed` replacement syntax. Quoted path plus suffix, `echo <setup-path>`, and basename-only forms are rejected by the start-of-command boundary check.
- validation_or_tests: `tests/test-session-id.sh` is the detailed executable spec for this hook. It covers initial empty `session_id`, signal creation, recording/removal, no-op without signal, rejecting non-setup commands, accepting quoted/unquoted setup invocations, special characters, false positives, and tab-delimited invocations. The hook also sits in the global hook registration at `hooks/hooks.json:58`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-073 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the command mutation detector used by the Bash validator to prevent shell-command bypasses of RLCR/PR-loop file protections.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:11` through `tests/test-bash-validator-patterns.sh:13`, lowercases candidate commands through `to_lower`, then asserts whether `command_modifies_file` returns true or false for a supplied file regex at `tests/test-bash-validator-patterns.sh:36` through `tests/test-bash-validator-patterns.sh:61`. It groups cases by redirection, `tee`, in-place editors, file operations, destructive/low-level writers, false positives, edge cases, state-file patterns, and summary-file patterns.
- inputs_outputs_state: Inputs are static shell command strings and file regexes such as `goal-tracker\.md`, `state\.md`, and `round-[0-9]+-summary\.md`. Outputs are PASS/FAIL lines and process exit code 0 when `TESTS_FAILED` is zero, otherwise 1 at `tests/test-bash-validator-patterns.sh:197` through `tests/test-bash-validator-patterns.sh:214`. It mutates only local test counters.
- gates_or_invariants: The contract requires detection of `>`, `>>`, `tee`, `tee -a`, `sed -i`, `awk -i inplace`, `perl -i`/`-pie`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec N>` patterns at `tests/test-bash-validator-patterns.sh:69` through `tests/test-bash-validator-patterns.sh:134`. It also requires non-mutating reads such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `stat`, and `diff` not to be caught at `tests/test-bash-validator-patterns.sh:136` through `tests/test-bash-validator-patterns.sh:153`. The lowercase conversion makes case-insensitive detection part of the expected behavior.
- dependencies_and_callers: This is a direct spec for `command_modifies_file` in `hooks/lib/loop-common.sh:1250`. The function is used by `hooks/loop-bash-validator.sh` to block RLCR state/finalize-state writes, plan backup writes, goal-tracker writes after round 0, prompt/summary/todos mutations, and PR-loop state/read-only file mutations. Examples of caller use are at `hooks/loop-bash-validator.sh:151`, `hooks/loop-bash-validator.sh:161`, and `hooks/loop-bash-validator.sh:346` through `hooks/loop-bash-validator.sh:450`.
- edge_cases_or_failure_modes: The test explicitly documents a known limitation: multi-source `cp file1.md file2.md goal-tracker.md` is not detected because the matcher expects `cp src dest` shape at `tests/test-bash-validator-patterns.sh:165` through `tests/test-bash-validator-patterns.sh:168`. It also verifies variables are not treated as literal target paths, e.g. `echo x > $FILE` is ignored at `tests/test-bash-validator-patterns.sh:170` through `tests/test-bash-validator-patterns.sh:172`. It validates bare redirection forms like `> goal-tracker.md` and no-space redirection like `>goal-tracker.md`.
- validation_or_tests: This file is itself validation. Related broader hook tests include `tests/robustness/test-hook-input-robustness.sh`, `tests/test-finalize-phase.sh`, and `tests/test-pr-loop-hooks.sh`, which invoke the full Bash validator around these shared matching primitives.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-103 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: Executable specification for the non-hook `scripts/rlcr-stop-gate.sh` wrapper, especially project-root resolution and stop-gate exit-code mapping.
- algorithmic_behavior: The test builds minimal git-backed RLCR loop fixtures via `setup_active_loop_fixture` at `tests/test-stop-gate.sh:18` through `tests/test-stop-gate.sh:59`. Each fixture has a `plan.md`, copied `.humanize/rlcr/<timestamp>/plan.md`, and active `state.md` with `current_round: 0`, `max_iterations: 42`, `review_started: false`, branch fields, and empty `session_id`. The fixture intentionally omits the required round summary, so the underlying stop hook should block. Test 1 runs the gate from inside the project and expects exit 10 plus `BLOCK:` output at `tests/test-stop-gate.sh:64` through `tests/test-stop-gate.sh:89`. Test 2 runs outside the project with `--project-root` and expects the same block behavior at `tests/test-stop-gate.sh:91` through `tests/test-stop-gate.sh:116`. Test 3 runs in an empty project and expects exit 0 plus `ALLOW:` at `tests/test-stop-gate.sh:118` through `tests/test-stop-gate.sh:142`.
- inputs_outputs_state: Inputs are temporary project directories, git branch state, the `scripts/rlcr-stop-gate.sh` wrapper, and generated `.humanize/rlcr` state files. Outputs are captured stdout/stderr files and test counters. The target wrapper itself builds a Stop hook JSON envelope with `hook_event_name`, `stop_hook_active`, `cwd`, and optional `session_id`/`transcript_path` at `scripts/rlcr-stop-gate.sh:84` through `scripts/rlcr-stop-gate.sh:97`, then maps empty hook output to `ALLOW` exit 0 and JSON `{decision:"block"}` to `BLOCK` exit 10 at `scripts/rlcr-stop-gate.sh:99` through `scripts/rlcr-stop-gate.sh:134`.
- gates_or_invariants: The wrapper must default project root to caller cwd, not the plugin install directory, as established by `PROJECT_ROOT="${CLAUDE_PROJECT_DIR:-$(pwd)}"` in `scripts/rlcr-stop-gate.sh:21`. `--project-root` must override that root at `scripts/rlcr-stop-gate.sh:53` through `scripts/rlcr-stop-gate.sh:56`. Runtime errors map to exit 20, including missing executable hook, missing `jq`, hook non-zero exit, non-JSON hook output, and unexpected decisions at `scripts/rlcr-stop-gate.sh:74` through `scripts/rlcr-stop-gate.sh:82` and `scripts/rlcr-stop-gate.sh:104` through `scripts/rlcr-stop-gate.sh:138`.
- dependencies_and_callers: The test depends on `tests/test-helpers.sh` for temp directory and git fixture helpers at `tests/test-helpers.sh:86` through `tests/test-helpers.sh:104`. The wrapper calls `hooks/loop-codex-stop-hook.sh`, which discovers active loops with `find_active_loop` at `hooks/loop-codex-stop-hook.sh:58` through `hooks/loop-codex-stop-hook.sh:65` and then enforces summary/plan/git/review gates.
- edge_cases_or_failure_modes: A missing summary should not be treated as wrapper failure; it is a legitimate stop-gate block. An empty project should not fabricate a loop from plugin paths. `--project-root` must work even when cwd is outside the target repo. Because the fixture calls git, the test assumes a writable temp directory and working local git environment.
- validation_or_tests: This file is the validation for `scripts/rlcr-stop-gate.sh`. Related stop-hook behavior is covered in `tests/test-finalize-phase.sh`, `tests/robustness/test-hook-system-robustness.sh`, and direct `loop-codex-stop-hook.sh` tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-133 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that the loop-local `plan.md` backup under `.humanize/rlcr/<timestamp>/` cannot be edited by the agent.
- algorithmic_behavior: The template is static markdown with no placeholders. It tells the agent the loop `plan.md` is a backup of the original plan and cannot be modified, preserving plan integrity; if needed, it should be read rather than changed at `prompt-template/block/plan-backup-protected.md:1` through `prompt-template/block/plan-backup-protected.md:7`.
- inputs_outputs_state: Input is the validator’s decision that a Write/Edit/Bash target is the protected plan backup. Output is the rendered markdown reason emitted to stderr by validators. It does not itself mutate state.
- gates_or_invariants: The invariant is enforced by Write/Edit/Bash validators, not by the template alone. `hooks/loop-write-validator.sh` blocks writes to `plan.md` backup when the file path is inside `.humanize/rlcr/` and renders this template at `hooks/loop-write-validator.sh:177` through `hooks/loop-write-validator.sh:186`. `hooks/loop-bash-validator.sh` similarly blocks shell modifications to `.humanize/rlcr/.../plan.md` via `command_modifies_file` and renders the same template at `hooks/loop-bash-validator.sh:346` through `hooks/loop-bash-validator.sh:352`. `hooks/loop-edit-validator.sh` also references the same template.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/lib/template-loader.sh:170`, so missing template content falls back to inline messages. It is part of the broader template directory validated by `validate_template_dir` at `hooks/lib/template-loader.sh:208`.
- edge_cases_or_failure_modes: Because the template contains no variables, render-time placeholder injection is irrelevant here. The main edge is path classification: validators must correctly decide whether `plan.md` is the protected backup versus an ordinary project plan file. Bash detection is regex-based, so it inherits the command-pattern limitations of `command_modifies_file`.
- validation_or_tests: Template existence and references are checked by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls. Plan backup protection is exercised by hook/plan-file validation tests such as `tests/test-plan-file-hooks.sh`, `tests/test-plan-file-validation.sh`, and Bash validator coverage.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-163 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex prompt template for periodic mandatory full-goal alignment checks in the RLCR stop-hook state machine.
- algorithmic_behavior: The template instructs Codex to read the original implementation plan first, audit the goal tracker, compare plan scope against active/completed/deferred items, audit deferrals, summarize remaining work, review implementation reality, update the goal tracker section, and perform a stagnation/circuit-breaker check using historical round artifacts. It defines the output contract: write findings/action items to `{{REVIEW_RESULT_FILE}}`, write `STOP` as the last line if stagnating, and write `COMPLETE` only when all original plan tasks and acceptance criteria are fully met with no deferrals at `prompt-template/codex/full-alignment-review.md:84` through `prompt-template/codex/full-alignment-review.md:93`.
- inputs_outputs_state: Inputs are placeholders rendered by the stop hook: `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE`. The hook renders these at `hooks/loop-codex-stop-hook.sh:909` through `hooks/loop-codex-stop-hook.sh:922` when `FULL_ALIGNMENT_CHECK=true`. Output is a generated `round-N-review-prompt.md` consumed by Codex, then a review result that controls RLCR transitions: continue, finalize/complete, stop, or block.
- gates_or_invariants: The full-alignment cadence is computed by `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` at `hooks/loop-codex-stop-hook.sh:873` through `hooks/loop-codex-stop-hook.sh:876`. Previous-round indices are clamped to zero to avoid negative references at `hooks/loop-codex-stop-hook.sh:878` through `hooks/loop-codex-stop-hook.sh:884`. The template itself makes deferred acceptance criteria incomplete and reserves `COMPLETE` for fully met, non-deferred scope. It also introduces the stagnation circuit breaker: repeated issues, no meaningful AC progress, circular discussion, no code changes, or repeated unaddressed Codex feedback should end with `STOP` at `prompt-template/codex/full-alignment-review.md:74` through `prompt-template/codex/full-alignment-review.md:85`.
- dependencies_and_callers: Rendered via `load_and_render_safe` from `hooks/lib/template-loader.sh`. It depends on the goal-tracker update section template, plan backup state, round summaries/review results, and Codex CLI execution in `hooks/loop-codex-stop-hook.sh`. Template-loader single-pass rendering prevents review or summary content containing `{{...}}` from being accidentally expanded.
- edge_cases_or_failure_modes: Missing template falls back to a shorter inline full-alignment prompt at `hooks/loop-codex-stop-hook.sh:887` through `hooks/loop-codex-stop-hook.sh:896`, which preserves review output but loses detailed audit/stagnation instructions. If `FULL_REVIEW_ROUND` is invalid, the hook warns and defaults to 5 at `hooks/loop-codex-stop-hook.sh:870` through `hooks/loop-codex-stop-hook.sh:871`. Early full-review rounds use clamped historical references, so previous and previous-previous may both point to round 0.
- validation_or_tests: Template references are covered by `tests/test-template-references.sh` and broader template rendering tests. Stop-hook behavior that chooses between regular and full alignment prompts is covered by `tests/test-finalize-phase.sh`, `tests/test-agent-teams.sh`, and robustness tests around stop-hook state parsing.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-193 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for the PR-loop API and stop-hook path, covering mocked GitHub CLI behavior, comment fetching, polling JSON contracts, PR-loop state handling, and graceful API failure behavior.
- algorithmic_behavior: The file sources `hooks/lib/loop-common.sh` and shared test helpers at `tests/robustness/test-pr-loop-api-robustness.sh:16` through `tests/robustness/test-pr-loop-api-robustness.sh:20`. It creates a behavior-driven mock `gh` command at `tests/robustness/test-pr-loop-api-robustness.sh:34` through `tests/robustness/test-pr-loop-api-robustness.sh:239`, supporting repo/pr lookups and API modes such as empty arrays, rate limits, network errors, auth failure, Claude approval, Codex severity issues, mixed bots, Unicode comments, and long comments. It creates PR-loop state fixtures at `tests/robustness/test-pr-loop-api-robustness.sh:241` through `tests/robustness/test-pr-loop-api-robustness.sh:262`, initializes minimal git repos at `tests/robustness/test-pr-loop-api-robustness.sh:264` through `tests/robustness/test-pr-loop-api-robustness.sh:276`, runs fetch-related tests 1-11 in `run_fetch_tests`, and poll/stop-hook tests 12-19 in `run_poll_tests`.
- inputs_outputs_state: Inputs are temporary repos, generated `.humanize/pr-loop/<timestamp>/state.md`, mocked `gh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, and `hooks/pr-loop-stop-hook.sh`. Outputs are formatted markdown comment files, poll JSON, stop-hook exit codes, created `approve-state.md` path checks, PASS/FAIL counters, and final test exit code. The test uses real scripts end-to-end where practical: `fetch-pr-comments.sh` is invoked in tests 4-11, `pr-loop-stop-hook.sh` in tests 12-13, and `poll-pr-reviews.sh` in tests 15-19.
- gates_or_invariants: `find_active_pr_loop` must detect active PR loops with `state.md`, including missing `pr_number`, at `tests/robustness/test-pr-loop-api-robustness.sh:292` through `tests/robustness/test-pr-loop-api-robustness.sh:352`. `fetch-pr-comments.sh` must produce valid markdown for empty comments, tolerate rate limits and network errors gracefully, include Claude and Codex bot comments, preserve severity markers, include multiple bots, process Unicode, and handle long bodies at `tests/robustness/test-pr-loop-api-robustness.sh:362` through `tests/robustness/test-pr-loop-api-robustness.sh:557`. `poll-pr-reviews.sh` must show help, reject missing args, output JSON with boolean `has_new_comments`, tolerate slow API, and on API failure still exit 0 with valid JSON and `has_new_comments == false` as a boolean at `tests/robustness/test-pr-loop-api-robustness.sh:630` through `tests/robustness/test-pr-loop-api-robustness.sh:854`.
- dependencies_and_callers: This test covers `find_active_pr_loop` from `hooks/lib/loop-common.sh:963`, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, and portions of `pr-loop-stop-hook.sh`. The fetch script resolves current or parent repo, retries three API endpoints, normalizes issue comments/review comments/PR reviews, deduplicates by ID, sorts humans before bots and newest first, and appends API failure warnings at `scripts/fetch-pr-comments.sh:126` through `scripts/fetch-pr-comments.sh:220` and `scripts/fetch-pr-comments.sh:322` through `scripts/fetch-pr-comments.sh:450`. The poll script resolves repo, maps bot names to GitHub authors, fetches issue/review/review-summary data, filters by timestamp and bot author regex, and emits JSON at `scripts/poll-pr-reviews.sh:122` through `scripts/poll-pr-reviews.sh:187` and `scripts/poll-pr-reviews.sh:207` through `scripts/poll-pr-reviews.sh:327`. The PR stop hook parses YAML list state, checks PR terminal state, requires resolution summary, enforces clean/pushed git state, detects triggers, polls, handles timeouts, runs Codex validation, and renames terminal states at `hooks/pr-loop-stop-hook.sh:88` through `hooks/pr-loop-stop-hook.sh:175`, `hooks/pr-loop-stop-hook.sh:244` through `hooks/pr-loop-stop-hook.sh:277`, `hooks/pr-loop-stop-hook.sh:283` through `hooks/pr-loop-stop-hook.sh:361`, and `hooks/pr-loop-stop-hook.sh:802` through `hooks/pr-loop-stop-hook.sh:1646`.
- edge_cases_or_failure_modes: The test explicitly distinguishes acceptable behavior for fetch API errors: a warning file or non-zero exit can be acceptable for rate limits/network issues depending on output presence, but poll API failure has a strict contract of exit 0 plus parseable JSON with boolean false. It checks malformed PR-loop state does not crash with signal-level exit codes (`exit < 128`) at `tests/robustness/test-pr-loop-api-robustness.sh:587` through `tests/robustness/test-pr-loop-api-robustness.sh:604`. It verifies approval-state path writability but does not force a complete approval transition. Long comments are accepted as either fully processed or possibly truncated if still handled successfully.
- validation_or_tests: This file is the validation. It can also be sourced by split tests: `tests/robustness/test-pr-loop-api-fetch.sh` and `tests/robustness/test-pr-loop-api-poll.sh` reference it as the shared test library. I did not execute it in this read-only branch export because it creates temporary repos/files and depends on writable temp/git behavior; this pass is research-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item-evidence headings present; item IDs intentionally not repeated here to preserve exact-once ID occurrence.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`