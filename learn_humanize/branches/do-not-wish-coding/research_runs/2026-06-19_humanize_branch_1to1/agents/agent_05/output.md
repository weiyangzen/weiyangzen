# agent_05 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: Documentation surface for Humanize workflow algorithms. The directory is mixed: `docs/bitlesson.md` is behavior-defining for the BitLesson state/summary contract, `docs/usage.md` describes RLCR/PR loop command semantics and monitor behavior, install docs define runtime deployment expectations, and `docs/images/*` are illustrative monitor/workflow assets.
- algorithmic_behavior: Recursively inspected children: `docs/bitlesson.md`, `docs/usage.md`, `docs/install-for-claude.md`, `docs/install-for-codex.md`, `docs/install-for-kimi.md`, `docs/images/monitor.png`, and `docs/images/rlcr-workflow.svg`. `docs/usage.md:7-12` defines the high-level two-phase RLCR loop: implementation summaries are reviewed until COMPLETE, then `codex review --base <branch>` checks code quality. `docs/usage.md:26-35` documents the advisory Plan Understanding Quiz transition before loop start. `docs/usage.md:56-86`, `docs/usage.md:88-112`, and `docs/usage.md:114-141` define command inputs and loop behavior for `start-rlcr-loop`, `gen-plan`, and `start-pr-loop`. `docs/images/rlcr-workflow.svg:4-17` encodes the visual state graph from plan to implementation, review summary, code review, done, and feedback loops. `docs/images/monitor.png` is a screenshot of the monitor status bar fields, not executable logic.
- inputs_outputs_state: Inputs documented here include plan files, command flags, config keys, PR bot flags, base branch, Codex model/effort, and monitor targets. Outputs include `.humanize/rlcr/<timestamp>/` progress data (`docs/usage.md:231`), skill output cache paths (`docs/usage.md:164-165`), and installed skill/runtime directories in Codex/Kimi docs (`docs/install-for-codex.md:25-52`, `docs/install-for-kimi.md:25-28`). State transitions described include plan quiz pass/miss/proceed/stop (`docs/usage.md:28-35`), implementation to review phase (`docs/usage.md:132-140`), and cancellation endpoints (`docs/usage.md:233-237`).
- gates_or_invariants: `docs/usage.md:33-35` states the quiz is advisory, not a hard gate. `docs/usage.md:68-71` documents base branch resolution priority. `docs/usage.md:240-264` documents sandbox-bypass semantics and security invariant: unset values use sandbox protection. The directory coordinates with `scripts/setup-rlcr-loop.sh`, `scripts/humanize.sh`, `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, hook templates, and command files.
- dependencies_and_callers: Documentation points users to shell commands and installed plugin/skill paths. `docs/install-for-claude.md:57-71` and `docs/usage.md:216-229` coordinate with `scripts/humanize.sh` monitor functions. Codex/Kimi install docs coordinate with `scripts/install-skills-codex.sh`, `scripts/install-skills-kimi.sh`, and `scripts/install-skill.sh` (`docs/install-for-codex.md:13-23`, `docs/install-for-kimi.md:21-33`).
- edge_cases_or_failure_modes: Misconfiguration and missing runtimes are documented as troubleshooting cases: missing installed scripts (`docs/install-for-codex.md:70-76`, `docs/install-for-kimi.md:204-210`), unavailable Codex binary (`docs/install-for-kimi.md:194-202`), and unwritable output paths (`docs/install-for-kimi.md:230-236`). Sandbox bypass is explicitly risky (`docs/usage.md:240-264`).
- validation_or_tests: Directory behavior is validated indirectly by tests covering templates, monitor runtime/e2e behavior, config merge, BitLesson routing, and install/runtime assumptions. `docs/bitlesson.md` has direct executable support from `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, and stop-hook checks.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-035 `file` `docs/bitlesson.md`
- cursor: `[_]`
- core_role: Behavior-defining documentation for the BitLesson knowledge-capture workflow used inside RLCR rounds.
- algorithmic_behavior: Defines provider routing and selection workflow. Config precedence for `bitlesson_model` is documented as default config, user config, project config, then CLI flags (`docs/bitlesson.md:7-13`). Provider routing maps `gpt-*` and `o[N]-*` to Codex, and Claude/Haiku/Sonnet/Opus names to Claude (`docs/bitlesson.md:14-18`). Workflow requires project knowledge at `.humanize/bitlesson.md`, initialization from `templates/bitlesson.md`, task/sub-task selection through `scripts/bitlesson-select.sh`, applying selected IDs or `NONE`, and validating `## BitLesson Delta` in summaries (`docs/bitlesson.md:21-31`).
- inputs_outputs_state: Inputs are merged config, task/sub-task descriptions, related paths, `.humanize/bitlesson.md`, and round summary content. Outputs are selector lines `LESSON_IDS`/`RATIONALE` from `scripts/bitlesson-select.sh:31-34`, updated or unchanged BitLesson entries, and a summary `## BitLesson Delta` block. State transition is per-round: missing knowledge base initializes, selector chooses IDs, implementation applies lessons, stop gate validates declared delta.
- gates_or_invariants: Summary contract requires `Action: none|add|update`, `Lesson ID(s)`, and notes (`docs/bitlesson.md:32-41`). Strict validation requires `Action: none` to use `NONE` or empty IDs, while `add`/`update` must reference concrete `BL-YYYYMMDD-short-name` IDs present in `.humanize/bitlesson.md` (`docs/bitlesson.md:43-47`). `--require-bitlesson-entry-for-none` can block repeated empty knowledge-base `none` reports (`docs/bitlesson.md:47`).
- dependencies_and_callers: Implemented by `scripts/bitlesson-select.sh`, which loads config (`scripts/bitlesson-select.sh:9-17`), detects provider (`scripts/bitlesson-select.sh:89`), falls back to Codex if provider binary is missing (`scripts/bitlesson-select.sh:95-100`), and enforces two-line output (`scripts/bitlesson-select.sh:197-222`). Stop-hook validation is delegated at `hooks/loop-codex-stop-hook.sh:720-734` to `scripts/bitlesson-validate-delta.sh`. Setup injects the required selection instructions into round-0 prompts at `scripts/setup-rlcr-loop.sh:1127-1146`.
- edge_cases_or_failure_modes: Missing `--task`, `--paths`, or `--bitlesson-file` fails early (`scripts/bitlesson-select.sh:67-83`). Missing/empty BitLesson file fails (`scripts/bitlesson-select.sh:102-111`). Selector timeout exits 124 (`scripts/bitlesson-select.sh:183-186`). Malformed selector output fails with raw output shown (`scripts/bitlesson-select.sh:213-218`). Delta validator blocks missing sections, invalid actions, inconsistent IDs, missing files, bad ID format, and IDs absent from the knowledge base (`scripts/bitlesson-validate-delta.sh:92-108`, `scripts/bitlesson-validate-delta.sh:122-134`, `scripts/bitlesson-validate-delta.sh:158-183`, `scripts/bitlesson-validate-delta.sh:185-282`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` covers provider routing. `scripts/bitlesson-validate-delta.sh` is the executable gate for the documented summary contract. Related prompt and command docs reference the same workflow in `commands/start-rlcr-loop.md:160-172`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-065 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor graceful shutdown when `.humanize/` is deleted during monitoring.
- algorithmic_behavior: Provides a three-command state-machine harness: `setup` creates `.humanize/rlcr/2026-01-16_99-99-99/` with `state.md` and `goal-tracker.md`; `delete` removes `.humanize`; `cleanup` removes leftovers (`tests/manual-monitor-test.sh:23-72`). The intended concurrent monitor command is `source scripts/humanize.sh && humanize monitor rlcr` (`tests/manual-monitor-test.sh:10-14`, `tests/manual-monitor-test.sh:43-45`).
- inputs_outputs_state: Input is the first CLI argument: `setup`, `delete`, `cleanup`, or invalid. `setup` writes synthetic RLCR state keys `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` (`tests/manual-monitor-test.sh:26-32`) plus a minimal goal tracker (`tests/manual-monitor-test.sh:33-38`). `delete` transitions the active monitor from a valid `.humanize/rlcr` tree to missing loop root by `rm -rf .humanize` (`tests/manual-monitor-test.sh:47-56`). Output is user-readable guidance and expected monitor behavior.
- gates_or_invariants: Monitor should emit clean stop text containing `Monitoring stopped: .humanize/rlcr directory no longer exists`, restore terminal scroll region/cursor, and avoid shell glob errors (`tests/manual-monitor-test.sh:51-54`). Production monitor enforces the deletion gate by checking `[[ ! -d "$loop_dir" ]]` in the main loop and no-log/log loops, then calling `_graceful_stop` (`scripts/humanize.sh:777-783`, `scripts/humanize.sh:839-842`, `scripts/humanize.sh:950-953`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` RLCR monitor. `_graceful_stop` calls `_cleanup`, which stops the monitor, resets traps, kills background tail process if present, restores terminal, and prints stop text (`scripts/humanize.sh:675-716`). Terminal restoration resets scroll region and moves cursor to bottom (`scripts/humanize.sh:645-651`). Shared monitor utilities also define equivalent terminal setup/restore helpers (`scripts/lib/monitor-common.sh:69-92`).
- edge_cases_or_failure_modes: Invalid command exits 1 with usage (`tests/manual-monitor-test.sh:63-71`). The manual test can delete real `.humanize` data if run in a live project, so it is intentionally manual and destructive within the current directory. The expected behavior covers deleted root, terminal state restoration, and zsh/bash no-match safety.
- validation_or_tests: Automated counterparts exist: `tests/test-monitor-runtime.sh` checks `_cleanup`, `_graceful_stop`, trap reset, and terminal restoration; `tests/test-monitor-e2e-real.sh` checks graceful stop message and scroll-region reset. This manual script is still useful for real-terminal behavior not fully captured by unit tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-095 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared PR-loop test harness library that defines mock GitHub/Codex commands, initializes isolated test PATH state, and standardizes PR-loop test summaries.
- algorithmic_behavior: Uses an idempotent load guard `TEST_PR_LOOP_LIB_LOADED` (`tests/test-pr-loop-lib.sh:10-13`). Resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources `tests/test-helpers.sh` if needed (`tests/test-pr-loop-lib.sh:14-21`), then defines mock creation and environment setup functions. `create_mock_gh` emits a mock `gh` executable that handles `auth status`, `repo view --json owner/name`, `pr view` variants for commits/number/state, and `api` requests returning user or empty arrays (`tests/test-pr-loop-lib.sh:27-88`). `create_mock_codex` emits a successful mock Codex command (`tests/test-pr-loop-lib.sh:90-101`). `init_pr_loop_test_env` creates `TEST_DIR`, prepends mock bin dir to PATH, and exports it (`tests/test-pr-loop-lib.sh:107-121`).
- inputs_outputs_state: Inputs are a mock binary directory for creation functions and ambient test counters from `test-helpers.sh`. Outputs are executable mock files, `PATH` mutation, `MOCK_BIN_DIR`, and a final return code through `print_test_summary`. State transition: not loaded -> library loaded; no test dir -> temp test dir; real external CLIs -> deterministic mocks.
- gates_or_invariants: The load guard prevents duplicate function definitions and repeated setup. Mock `gh` exits 1 on unhandled commands (`tests/test-pr-loop-lib.sh:84-85`), keeping tests fail-closed when production code calls an unexpected GitHub CLI shape. `print_test_summary` returns 1 if any failures occurred and 0 otherwise (`tests/test-pr-loop-lib.sh:127-144`), mirroring shared helper semantics in `tests/test-helpers.sh:58-78`.
- dependencies_and_callers: Sourced by PR-loop split runners: `tests/test-pr-loop-1-scripts.sh:14-23`, `tests/test-pr-loop-2-hooks.sh:14-23`, `tests/test-pr-loop-3-stophook.sh:15-30`, and the combined `tests/test-pr-loop.sh`. It depends on `tests/test-helpers.sh` for `setup_test_dir`, counters, and pass/fail functions (`tests/test-helpers.sh:22-89`).
- edge_cases_or_failure_modes: Mock coverage is intentionally narrow; any new production `gh` invocation not modeled will fail with `Mock gh: unhandled command`, which is a useful regression signal but can require test harness updates. The mocked `gh api user` returns plain `testuser`, while other API calls return `[]`; tests relying on richer API payloads need their own mocks.
- validation_or_tests: This file is not itself a top-level test; it is executable specification infrastructure for the PR-loop scripts, hooks, and stop-hook suites. Its success criteria are consumed through the split runners’ final `print_test_summary`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-125 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR bash validator when `git push` is attempted while per-round pushing is disabled.
- algorithmic_behavior: The template communicates the transition denial: current commits stay local, the loop handles local commits until completion, and pushing requires starting the loop with `--push-every-round` (`prompt-template/block/git-push.md:1-9`). The production gate lives in `hooks/loop-bash-validator.sh`: after strict active state parsing, it reads `STATE_PUSH_EVERY_ROUND`; if not `true` and the command lowercases to `git push`, it renders this template and exits 2 (`hooks/loop-bash-validator.sh:89-117`).
- inputs_outputs_state: Inputs are the attempted Bash command, active loop directory/state file, and `push_every_round` state. Output is a rendered block message on stderr and exit status 2 from the validator. State does not mutate; it prevents an external remote-push transition.
- gates_or_invariants: Invariant: default RLCR behavior keeps commits local. Only explicit `--push-every-round` changes that gate. The validator fails closed on malformed state before evaluating push (`hooks/loop-bash-validator.sh:91-97`). The template loader provides fallback text if this file is missing (`hooks/loop-bash-validator.sh:111-115`).
- dependencies_and_callers: Consumed through `load_and_render_safe` from `hooks/lib/template-loader.sh`, whose safe loader returns fallback content for missing/empty templates and uses single-pass rendering to avoid placeholder injection (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:167-180`). Template behavior is covered by template-loader tests such as `tests/test-template-loader.sh` and comprehensive template tests.
- edge_cases_or_failure_modes: Regex only blocks commands beginning with whitespace plus `git push`; aliases or wrapper commands would not match this specific gate. If state says `push_every_round: true`, the push gate is bypassed and other validators still apply. Missing template falls back to inline block text.
- validation_or_tests: `tests/test-template-loader.sh` references `block/git-push.md`; `tests/test-templates-comprehensive.sh` exercises safe rendering. The behavior is also indirectly validated by bash-validator tests around RLCR command blocking.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-155 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt fragment defining how Claude should request Goal Tracker state changes instead of editing protected or reviewed tracker state arbitrarily.
- algorithmic_behavior: Provides a conditional summary section titled `## Goal Tracker Update Request`, with `Requested Changes` and `Justification` subsections (`prompt-template/claude/goal-tracker-update-request.md:1-16`). The stop hook appends this template to the next-round prompt after footer/push instructions (`hooks/loop-codex-stop-hook.sh:1786-1791`), so each continued implementation round carries a structured channel for tracker updates.
- inputs_outputs_state: Input is whether Claude determines Goal Tracker changes are needed. Output is a structured request embedded in the round summary, not a direct state mutation. The intended state transition is proposal -> Codex review -> justified Goal Tracker update, as line 16 states Codex reviews the request and updates if justified.
- gates_or_invariants: Invariant: Goal Tracker changes need justification tied to the Ultimate Goal (`prompt-template/claude/goal-tracker-update-request.md:12-16`). Setup creates a Goal Tracker with immutable and mutable sections, and round-0 prompt warns the immutable section can only be modified in round 0 (`scripts/setup-rlcr-loop.sh:1094-1105`). Command docs require maintaining the tracker before exit (`commands/start-rlcr-loop.md:147-156`).
- dependencies_and_callers: Loaded by `load_template` in `hooks/loop-codex-stop-hook.sh:1787`; missing template falls back to a one-line instruction (`hooks/loop-codex-stop-hook.sh:1788-1790`). It coordinates with `goal-tracker.md`, stop-hook state transitions, and any Codex/Claude review logic that consumes round summaries.
- edge_cases_or_failure_modes: Because this is a prompt contract, enforcement depends on downstream stop-hook/review behavior. If the template is absent, the fallback is weaker and loses examples for completed tasks, open issues, plan evolution, and deferrals. If Claude updates tracker files directly rather than requesting changes, separate validators/protected-file hooks must catch that.
- validation_or_tests: Indirectly covered by template loading tests and stop-hook prompt-generation tests. The template’s role is validated when next-round prompts include this section after Codex feedback paths.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-185 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for git status parsing and git repository state detection used by the monitor/status algorithms.
- algorithmic_behavior: Sources production `scripts/humanize.sh` and tests `humanize_parse_git_status` plus `humanize_detect_git_state` (`tests/robustness/test-git-operations-robustness.sh:15-20`, `tests/robustness/test-git-operations-robustness.sh:28-34`). `parse_result` splits the pipe-delimited parser output into fields (`tests/robustness/test-git-operations-robustness.sh:35-47`). `init_test_repo` creates local git repos with user config and an initial commit (`tests/robustness/test-git-operations-robustness.sh:49-62`).
- inputs_outputs_state: Inputs are synthetic git repositories and file operations: clean repo, untracked/modified/staged/deleted files, detached HEAD, empty repo, feature branches, renames, binary files, simultaneous staged/unstaged changes, rebase/merge/shallow markers, and permission-like `.git` states. Outputs are pass/fail counters through `tests/test-helpers.sh`, ending with `print_test_summary` and script exit (`tests/robustness/test-git-operations-robustness.sh:502-507`).
- gates_or_invariants: `humanize_parse_git_status` must return `modified|added|deleted|untracked|insertions|deletions` and return a non-git marker for non-repos (`scripts/humanize.sh:172-218`). It counts porcelain status classes, treats renames as modified, and parses diff shortstat for insertions/deletions (`scripts/humanize.sh:181-217`). `humanize_detect_git_state` must classify `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo` based on git-dir accessibility and marker files (`scripts/humanize.sh:122-170`).
- dependencies_and_callers: Depends on `git`, `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:15-21`). Production caller is the RLCR monitor status bar, which invokes `humanize_parse_git_status` through `_parse_git_status` and renders clean/changed counts (`scripts/humanize.sh:371-428`, `scripts/humanize.sh:565-577`).
- edge_cases_or_failure_modes: Tests intentionally cover non-git directories (`tests/robustness/test-git-operations-robustness.sh:168-178`), detached HEAD (`tests/robustness/test-git-operations-robustness.sh:180-195`), empty repos without commits (`tests/robustness/test-git-operations-robustness.sh:233-246`), files with spaces (`tests/robustness/test-git-operations-robustness.sh:301-314`), binary files (`tests/robustness/test-git-operations-robustness.sh:316-329`), simultaneous staged/unstaged changes (`tests/robustness/test-git-operations-robustness.sh:331-347`), simulated rebase/merge/shallow state (`tests/robustness/test-git-operations-robustness.sh:403-464`), and permission ambiguity where `permission_error` or `not_a_repo` is acceptable (`tests/robustness/test-git-operations-robustness.sh:478-500`).
- validation_or_tests: This file is itself the validation suite for git monitor/status behavior. It defines 25 numbered cases and exits nonzero through `print_test_summary` when any expectation fails.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present; each assigned row represented once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`