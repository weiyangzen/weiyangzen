# agent_27 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-027 `file` `agents/plan-compliance-checker.md`
- cursor: `[_]`
- core_role: Specialized Claude agent prompt that gates `start-rlcr-loop` plans before the RLCR implementation loop starts. It defines an early rejection contract for unrelated plans and branch-switching plans, while defaulting leniently to pass for uncertain cases.
- algorithmic_behavior: Performs two ordered checks: repository relevance and branch-switch detection. Relevance requires a quick repository scan of README/CLAUDE/docs and directory structure, then comparison against the plan’s concepts, paths, functions, technologies, and substantive content at `agents/plan-compliance-checker.md:16-30`. Branch-switch detection scans the entire plan for branch-changing or worktree-creation instructions at `agents/plan-compliance-checker.md:31-46`, then excludes safe patterns such as file restore checkout, negated branch-switch instructions, descriptive branch references, and `--base-branch` review parameters.
- inputs_outputs_state: Input is plan-file content supplied by the invoking command plus read-only repository context via `Read`, `Glob`, and `Grep` tools declared at `agents/plan-compliance-checker.md:1-5`. Output is exactly one standalone verdict: `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH`, with a short summary/reason/quote at `agents/plan-compliance-checker.md:48-55`. It does not persist state; its output controls whether setup continues.
- gates_or_invariants: Must output exactly one verdict and never zero or multiple verdicts at `agents/plan-compliance-checker.md:58`. Relevance false positives are avoided by “Be lenient” and “If in doubt ... PASS” rules at `agents/plan-compliance-checker.md:29` and `agents/plan-compliance-checker.md:59-60`. Branch switching is a hard incompatibility because RLCR requires a stable working branch across loop rounds at `agents/plan-compliance-checker.md:46`.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md`, which extracts a safe relative plan path, reads it, invokes `humanize:plan-compliance-checker` with model `sonnet`, and fail-closes on malformed results at `commands/start-rlcr-loop.md:14-58`. The command explicitly skips this pre-check for `--skip-impl`, `-h`, or `--help` at `commands/start-rlcr-loop.md:18-21`.
- edge_cases_or_failure_modes: A plan with only vague but possibly related content should pass, not fail relevance. Branch words in descriptive or negated contexts should pass. Unsafe absolute paths, parent traversal, unsafe path characters, unreadable files, or absent plan paths are skipped by the caller and delegated to the setup script rather than handled by this agent at `commands/start-rlcr-loop.md:29-40`. Because the command parser tests “output contains `PASS`” before failure tokens at `commands/start-rlcr-loop.md:53-57`, the agent’s exact single-verdict invariant is important to avoid ambiguous mixed output.
- validation_or_tests: No dedicated shell test was found for this agent prompt. The observable contract is encoded in `commands/start-rlcr-loop.md:42-57` and the examples at `agents/plan-compliance-checker.md:64-76`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-057 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop polling helper that queries GitHub for bot-authored issue comments, review comments, and PR reviews after a timestamp, normalizes them into one JSON response, and lets the PR loop detect reviewer bot responses.
- algorithmic_behavior: Parses `<pr_number> --after <timestamp> --bots <bot1,bot2>` at `scripts/poll-pr-reviews.sh:19-81`, validates required arguments and numeric PR number at `scripts/poll-pr-reviews.sh:83-102`, checks `gh` and `jq` availability at `scripts/poll-pr-reviews.sh:104-116`, resolves the PR’s base repository with fork support at `scripts/poll-pr-reviews.sh:122-158`, maps logical bot names to GitHub authors at `scripts/poll-pr-reviews.sh:164-187`, fetches three API surfaces with retries at `scripts/poll-pr-reviews.sh:207-235`, normalizes them into comment objects at `scripts/poll-pr-reviews.sh:240-294`, filters by timestamp and bot-author regex at `scripts/poll-pr-reviews.sh:299-305`, then emits summary JSON at `scripts/poll-pr-reviews.sh:311-327`.
- inputs_outputs_state: Inputs are CLI args, current GitHub repo context from `gh repo view`, PR lookup from `gh pr view`, and GitHub API endpoints for issue comments, pull review comments, and PR reviews. Output is JSON with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` at `scripts/poll-pr-reviews.sh:317-326`. It uses temporary files under `mktemp -d` and cleans them via `trap` at `scripts/poll-pr-reviews.sh:193-199`; no repo files are modified.
- gates_or_invariants: Fails fast for invalid CLI shape, missing tools, missing repo context, missing PR, or unparseable repo owner/name. API fetch failures are intentionally non-fatal: `fetch_with_retry` logs warnings, increments diagnostics, returns `[]`, and exits success so polling can continue during partial outages at `scripts/poll-pr-reviews.sh:204-235`. Approval-only PR reviews are preserved by replacing empty review bodies with `[Review state: <state>]` at `scripts/poll-pr-reviews.sh:276-289`. The timestamp filter is lexical ISO comparison, so callers must pass comparable ISO 8601 UTC-like timestamps.
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` as `POLL_SCRIPT` at `hooks/pr-loop-stop-hook.sh:812`. The stop hook computes the `--after` timestamp from trigger time, startup time, or epoch for historical startup cases at `hooks/pr-loop-stop-hook.sh:817-847`; polls only waiting bots at `hooks/pr-loop-stop-hook.sh:961-966`; maps returned authors back to logical bot names at `hooks/pr-loop-stop-hook.sh:968-983`; deduplicates comments by id at `hooks/pr-loop-stop-hook.sh:1043-1059`; and writes round review files from accumulated comments at `hooks/pr-loop-stop-hook.sh:1182-1229`.
- edge_cases_or_failure_modes: Fork PRs are handled by falling back from current repo to parent repo at `scripts/poll-pr-reviews.sh:132-145`. Unknown bot names map generically to `<bot>[bot]`, while `codex` maps specially to `chatgpt-codex-connector[bot]` at `scripts/poll-pr-reviews.sh:167-172`. Empty `--bots` is rejected, but a comma list with blank entries could add an empty bot pattern because only spaces are trimmed at `scripts/poll-pr-reviews.sh:177-187`. Warnings on stderr can precede JSON during API failures, which tests account for by extracting JSON from the first `{`.
- validation_or_tests: Argument/help checks are covered by `tests/test-pr-loop-scripts.sh:329-399`. Fixture-backed tests assert approval-only reviews and `--after` filtering at `tests/test-pr-loop-hooks.sh:1322-1411`. Robustness tests assert valid JSON, slow API handling, and API failure returning exit 0 with boolean `has_new_comments:false` at `tests/robustness/test-pr-loop-api-robustness.sh:630-855`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-087 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: Thin executable test shard for monitor SIGINT behavior. It is not itself an implementation algorithm, but it is an executable specification for the monitor cleanup contract under interrupt signals.
- algorithmic_behavior: Sources `tests/test-monitor-e2e-real.sh` at `tests/test-monitor-e2e-sigint.sh:4-5`, prints a SIGINT test banner at `tests/test-monitor-e2e-sigint.sh:7-10`, runs exactly three delegated test functions at `tests/test-monitor-e2e-sigint.sh:12-14`, then prints aggregate pass/fail counts and exits 0 only when `TESTS_FAILED` is zero at `tests/test-monitor-e2e-sigint.sh:16-22`.
- inputs_outputs_state: Inputs are the shared helper functions and global counters from `tests/test-monitor-e2e-real.sh`. It mutates only shell counters indirectly via delegated tests. Output is human-readable test status; final process exit status is the pass/fail gate.
- gates_or_invariants: `set -euo pipefail` at `tests/test-monitor-e2e-sigint.sh:3` makes unexpected shell errors fail the shard. The final invariant is `[[ $TESTS_FAILED -eq 0 ]] && exit 0 || exit 1` at `tests/test-monitor-e2e-sigint.sh:22`. The shard intentionally runs only the SIGINT subset: bash RLCR monitor, zsh RLCR monitor, and PR monitor.
- dependencies_and_callers: Depends entirely on `tests/test-monitor-e2e-real.sh`. The delegated bash SIGINT test starts the real `_humanize_monitor_codex`, sends SIGINT to the process group, waits with timeout, verifies cleanup/clean exit and no glob errors at `tests/test-monitor-e2e-real.sh:377-550`. The zsh SIGINT test checks zsh availability, runs the real monitor under zsh, sends SIGINT, and requires exit plus no glob errors at `tests/test-monitor-e2e-real.sh:555-685`. The PR SIGINT shard begins at `tests/test-monitor-e2e-real.sh:834` and exercises `_humanize_monitor_pr` without `--once`.
- edge_cases_or_failure_modes: zsh absence causes the zsh delegated test to print a skip rather than fail at `tests/test-monitor-e2e-real.sh:560-562`. Bash SIGINT delivery can be flaky, so the helper allows a SIGTERM cleanup path to count as pass if the monitor ran and had to be force-terminated after SIGINT delivery issues at `tests/test-monitor-e2e-real.sh:505-518`. The tests assert absence of `no matches found` or `bad pattern` to guard zsh/bash glob failures.
- validation_or_tests: This file is itself the validation entrypoint. It validates signal cleanup, terminal restoration indications, graceful stop messaging or clean exit codes, bounded waits, and no shell glob errors through the delegated real monitor tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-117 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when the PR loop has confirmed a Claude trigger comment but Claude fails to add the expected `eyes` reaction after retries. It turns a bot-liveness gate failure into a user-facing block reason.
- algorithmic_behavior: Renders a markdown block with retry count and total wait placeholders at `prompt-template/block/claude-eyes-timeout.md:1-4`, lists likely root causes at `prompt-template/block/claude-eyes-timeout.md:5-8`, gives required remediation actions at `prompt-template/block/claude-eyes-timeout.md:10-13`, and instructs the user to configure the GitHub App and post a fresh trigger comment at `prompt-template/block/claude-eyes-timeout.md:15-18`.
- inputs_outputs_state: Inputs are `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, substituted by the template loader’s single-pass `{{VAR}}` renderer. Output is markdown assigned to the PR stop hook’s JSON `reason`. The template itself is stateless.
- gates_or_invariants: The template is used only after trigger validation and only when Claude is configured, trigger is required, and a confirmed trigger comment id exists. The caller checks `check-bot-reactions.sh claude-eyes ... --retry 3 --delay 5` and blocks if the result is empty or `null` at `hooks/pr-loop-stop-hook.sh:768-790`. Missing or empty template files are fail-soft because `load_and_render_safe` falls back to an inline message at `hooks/pr-loop-stop-hook.sh:781-786` and `hooks/lib/template-loader.sh:167-203`.
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh:785`, with `TEMPLATE_DIR="$PLUGIN_ROOT/prompt-template"` defined earlier at `hooks/pr-loop-stop-hook.sh:58`. Rendering uses `hooks/lib/template-loader.sh`, whose placeholder syntax and single-pass behavior are defined at `hooks/lib/template-loader.sh:7-14` and `hooks/lib/template-loader.sh:50-132`.
- edge_cases_or_failure_modes: If the Claude app is not installed, lacks permission, is down, or cannot react to the PR state, the loop blocks before polling for reviews. If a trigger exists but no trigger comment id is available, the caller warns and skips eyes verification rather than using this template at `hooks/pr-loop-stop-hook.sh:794-797`. If the template is missing, the fallback preserves the block decision.
- validation_or_tests: Template reference and safe-rendering usage are indirectly guarded by `tests/test-template-references.sh`, which checks critical validators use `load_and_render_safe` at `tests/test-template-references.sh:172-201`, and broader template loader tests exercise fallback rendering. The PR hook behavior around this block is in `hooks/pr-loop-stop-hook.sh:749-800`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-147 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR read validation when an agent tries to read a prompt or summary from a non-current round. It supports the loop invariant that agents operate on the active round files unless an allowlist applies.
- algorithmic_behavior: Renders a concise markdown diagnostic naming the attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, the active `{{CURRENT_ROUND}}`, the correct current prompt and summary paths, and a direct `cat {{FILE_PATH}}` escape if the old file is truly needed at `prompt-template/block/wrong-round-file.md:1-9`.
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, supplied by the read validator. Output is markdown written to stderr before the validator exits with code 2. No persistent state is modified.
- gates_or_invariants: The gate fires when the round embedded in the requested filename differs from current round and the file is not allowlisted at `hooks/loop-read-validator.sh:138-150`. Before that, the validator separately verifies the file is inside the active humanize loop directory at `hooks/loop-read-validator.sh:122-132`; after this gate, it verifies the exact directory path at `hooks/loop-read-validator.sh:153-168`.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh:144`, via `load_and_render_safe`. Rendering semantics and fallback handling come from `hooks/lib/template-loader.sh:167-203`. The file-type discriminator only handles prompt and summary files at `hooks/loop-read-validator.sh:110-116`.
- edge_cases_or_failure_modes: If the read target is allowlisted, wrong-round blocking is bypassed. If the template is missing or renders empty, the fallback at `hooks/loop-read-validator.sh:139-143` still blocks with exit 2. If `FILE_TYPE` is empty because the filename is neither prompt nor summary, the template may render an incomplete attempted filename, but the surrounding validators still enforce location/path rules.
- validation_or_tests: Template reference safety is covered indirectly by `tests/test-template-references.sh:172-201`, which includes `loop-read-validator.sh` in the critical scripts list. Template loader behavior is covered by `tests/test-template-loader.sh` and robustness tests for `load_and_render_safe`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-177 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Runtime monitor implementation for `humanize monitor skill`, displaying ask-codex skill invocation status from `.humanize/skill` with both one-shot summary mode and interactive live tail mode.
- algorithmic_behavior: `_humanize_monitor_skill` initializes bash/zsh-compatible options, parses `--once`, validates `.humanize/skill`, then defines local helpers for listing timestamp-named invocation directories, selecting the newest watchable invocation, counting statuses, extracting the question, resolving global cache paths, choosing the best monitored file, and drawing a fixed status bar. See entry and setup at `scripts/lib/monitor-skill.sh:15-40`, directory ordering at `scripts/lib/monitor-skill.sh:42-80`, status counting at `scripts/lib/monitor-skill.sh:82-105`, question/cache/file selection at `scripts/lib/monitor-skill.sh:107-167`, status bar rendering at `scripts/lib/monitor-skill.sh:169-266`, `--once` output at `scripts/lib/monitor-skill.sh:268-345`, and interactive tail loop at `scripts/lib/monitor-skill.sh:348-447`.
- inputs_outputs_state: Inputs are `.humanize/skill/<timestamp...>/input.md`, optional `metadata.md`, optional `output.md`, local cache files under `<invocation>/cache`, and global cache files under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/skill-<unique_id>`. Output is either a textual report in `--once` mode or terminal UI plus a live `tail -f` in interactive mode. State is local shell state only: focused directory/file, tail pid, cleanup flags, and terminal scroll/cursor state.
- gates_or_invariants: Invocation directories must start with `YYYY-MM-DD_HH-MM-SS` to count at `scripts/lib/monitor-skill.sh:42-52` and `scripts/lib/monitor-skill.sh:86-104`. Running status is inferred from absence of `metadata.md`; terminal statuses are parsed from YAML `status` with recognized values `success`, `error`, `timeout`, and `empty_response`. Running invocations prefer cache logs; completed invocations prefer `output.md` and then cache outputs/logs at `scripts/lib/monitor-skill.sh:137-165`. Interactive cleanup is idempotent, kills/disowns the tail process, resets scroll region, restores cursor/screen, and prints `Monitor stopped.` at `scripts/lib/monitor-skill.sh:358-381`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at `scripts/humanize.sh:1589-1592` and dispatched by `humanize monitor skill` at `scripts/humanize.sh:1110-1123`. Depends on `monitor_get_yaml_value` and `monitor_format_timestamp` from `scripts/lib/monitor-common.sh:195-228`, plus `humanize_split_to_array` from `scripts/humanize.sh:22-34`. Cache path calculation depends on `git rev-parse --show-toplevel` with `pwd` fallback at `scripts/lib/monitor-skill.sh:118-124`.
- edge_cases_or_failure_modes: Missing `.humanize/skill` returns an error and suggests running `/humanize:ask-codex` at `scripts/lib/monitor-skill.sh:35-40`; empty invocation directory returns “No skill invocations found” in `--once` mode at `scripts/lib/monitor-skill.sh:268-274`. Non-timestamp directories are ignored. A running invocation without cache falls back to `input.md`; completed invocation without output can show cache files or “No output available.” Question extraction only captures the first non-empty line under `## Question` at `scripts/lib/monitor-skill.sh:107-114`. Interactive mode depends on usable `tput`; tiny terminal widths could make truncation math fragile because max lengths are computed from `tput cols`.
- validation_or_tests: Focused unit-style coverage is in `tests/test-skill-monitor.sh`, which verifies missing directory, empty directory, single and mixed invocations, running state, recent list, question extraction, empty response, and ignored non-skill dirs at `tests/test-skill-monitor.sh:107-389`. The `test-monitor-e2e-sigint.sh` shard covers RLCR/PR monitors rather than this skill monitor, so signal behavior for this specific file is not directly covered there.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6 item sections; each assigned item_id is present once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`