# agent_24 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-024 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness specification suite for the branch’s shell-based loop algorithms. The directory is flat, with 19 direct shell test files and no subdirectories. It validates setup routing, state/session discovery, hook validators, template rendering, PR-loop API handling, timeout portability, git/goal-tracker parsing, and security/path gates.
- algorithmic_behavior: The suite creates isolated temp repos, mock `codex` binaries, mock `gh` APIs, synthetic `.humanize/rlcr` and `.humanize/pr-loop` state trees, JSON hook payloads, plan files, templates, logs, and git histories, then invokes production functions/scripts. Representative production surfaces include `hooks/lib/loop-common.sh` (`find_active_loop`, `get_current_round`, `parse_state_file`, `parse_state_file_strict`, `find_active_pr_loop`), `scripts/humanize.sh` (`humanize_parse_goal_tracker`, `humanize_parse_git_status`, `humanize_detect_git_state`), `hooks/lib/template-loader.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/portable-timeout.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`.
- inputs_outputs_state: Inputs are generated files and command payloads, not external live services. Outputs are test-helper pass/fail records and script exit codes. State transitions under test include `state.md` active loops, `finalize-state.md` finalize loops, `cancel-state.md` non-active cancelled loops, `complete-state.md` finished loops, PR-loop `state.md`, plan validation pass/reject, and hook JSON allow/block decisions. The suite repeatedly uses temporary workspaces and cleans them with traps rather than writing repository state.
- gates_or_invariants: Key invariants are: protected loop state files cannot be modified by hooks; only newest session directories are considered active to avoid stale/zombie loops; malformed state and JSON must reject or degrade without signal crashes; plan paths must remain safe relative paths and reject symlink traversal; templates must preserve literal values and use fallback on missing/malformed files; PR API scripts must return structured output under empty, rate-limited, slow, or failed APIs; timeout wrappers must preserve exit codes and return timeout semantics.
- dependencies_and_callers: All files depend on `tests/test-helpers.sh` for assertions. Child roles: `test-base-branch-detection.sh:35` mirrors setup base-branch detection priorities; `test-cancel-security-robustness.sh:17` targets loop-common cancel authorization; `test-concurrent-state-robustness.sh:30` builds state files for concurrent parse/discovery tests; `test-git-operations-robustness.sh:19` sources production git parsers; `test-goal-tracker-robustness.sh:22` sources goal-tracker parsing; `test-hook-input-robustness.sh:5` pipes JSON into real hook validators; `test-hook-system-robustness.sh:22` covers edit/plan/bash/write/stop hooks; `test-path-validation-robustness.sh:76` invokes setup path validation; `test-plan-file-robustness.sh:48` invokes setup plan validation; `test-pr-loop-api-robustness.sh:34` builds a comprehensive mock `gh`; split wrappers `test-pr-loop-api-fetch.sh:15` and `test-pr-loop-api-poll.sh:14` source that shared file and run group functions; `test-session-robustness.sh:16` tests session discovery; `test-setup-scripts-robustness.sh:5` covers RLCR/PR setup scripts; `test-state-file-robustness.sh:16` covers state parsing; `test-state-transition-robustness.sh:16` covers state machine/discovery transitions; `test-template-error-robustness.sh:17` and `test-template-stress-robustness.sh:3` cover template failures/stress; `test-timeout-robustness.sh` covers portable timeout behavior.
- edge_cases_or_failure_modes: Covered edge cases include missing origin remotes, no `main`/`master`, absolute/danger-character paths, symlink files and parent symlinks, disappearing/unreadable/binary/large/CRLF plan files, malformed YAML frontmatter, non-numeric/negative/huge rounds, concurrent atomic state writes, unicode paths/content, malformed/deep/non-UTF8/null-byte JSON, state modification command injection, ANSI/binary/rapid log updates, detached/rebase/merge/shallow git states, missing/rate-limited/network-failed GitHub APIs, long PR comments, missing templates, unclosed placeholders, huge template values, regex/sed metacharacters, and placeholder injection.
- validation_or_tests: This item is itself the validation suite. It coordinates with sibling top-level tests such as `tests/test-model-router.sh`, config tests, PR-loop system tests, and the production scripts/hooks under `scripts/` and `hooks/`. The directory gives broad negative-path coverage for core algorithms rather than a single runtime entry point.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-054 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: PR-loop startup/re-review classifier. It inspects all PR issue comments, review comments, and review submissions across the whole PR to decide whether reviewers are missing, fresh, or stale relative to the latest commit.
- algorithmic_behavior: It parses `<pr_number> --bots <comma-list>` (`scripts/check-pr-reviewer-status.sh:49`), maps logical bot names to GitHub authors with a codex special case (`scripts/check-pr-reviewer-status.sh:94`), resolves the PR base repo by trying current repo then parent fork repo (`scripts/check-pr-reviewer-status.sh:113`), fetches latest PR commit metadata (`scripts/check-pr-reviewer-status.sh:142`), fetches issue comments, inline review comments, and PR review submissions (`scripts/check-pr-reviewer-status.sh:153`), combines them with `jq -s 'add // []'` (`scripts/check-pr-reviewer-status.sh:168`), then checks each configured bot for any comments and per-bot staleness (`scripts/check-pr-reviewer-status.sh:179`).
- inputs_outputs_state: Inputs are the PR number, comma-separated bot list, current GitHub repository context, `gh` API responses, and `jq`. Output is JSON containing `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` (`scripts/check-pr-reviewer-status.sh:259`). It has no persistent state; downstream PR-loop scripts persist the derived case into loop state.
- gates_or_invariants: Required arguments are hard gates (`scripts/check-pr-reviewer-status.sh:75`, `scripts/check-pr-reviewer-status.sh:81`). Commit info fetch failure is fatal (`scripts/check-pr-reviewer-status.sh:143`). Comment fetch failures degrade to empty arrays (`scripts/check-pr-reviewer-status.sh:156`, `scripts/check-pr-reviewer-status.sh:159`, `scripts/check-pr-reviewer-status.sh:164`). Case logic is: no bot comments => case 1; some bots missing and none stale => case 2; all bots commented and none stale => case 3; all bots commented and at least one stale => case 4; some bots missing and at least one commented bot stale => case 5 (`scripts/check-pr-reviewer-status.sh:220`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` (`scripts/check-pr-reviewer-status.sh:39`), `gh`, `jq`, and ISO-like commit/comment timestamps for lexicographic comparison. `scripts/setup-pr-loop.sh:438` calls it during startup case determination and falls back to case 1 on failure. `hooks/pr-loop-stop-hook.sh:1557` re-evaluates it dynamically while polling a PR loop. Tests referencing it include `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-stophook.sh`, and robustness PR API tests.
- edge_cases_or_failure_modes: Fork PR support is explicitly handled by probing parent repo (`scripts/check-pr-reviewer-status.sh:126`). Unknown bot names map to `<bot>[bot]`; `codex` maps to `chatgpt-codex-connector[bot]`. Staleness is per-bot, not based on global newest review, preventing one fresh reviewer from masking another stale reviewer (`scripts/check-pr-reviewer-status.sh:192`). If current repo cannot be resolved, it warns and proceeds with an empty/unknown repo, likely failing commit fetch. Bot list elements are comma-split without trimming, so whitespace in `--bots` can alter author matching.
- validation_or_tests: `tests/test-pr-loop-system.sh` exercises no-comments, partial/all comments, codex mapping, and stale-commit cases. `tests/test-pr-loop-stophook.sh` mocks hook-time reviewer status updates. `tests/robustness/test-pr-loop-api-robustness.sh` validates API failure and mocked `gh` behavior around PR-loop scripts that share the same API assumptions.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-084 `file` `tests/test-model-router.sh`
- cursor: `[_]`
- core_role: Executable specification for provider routing, provider binary dependency checks, and effort normalization implemented by `scripts/lib/model-router.sh`.
- algorithmic_behavior: The test sources `tests/test-helpers.sh` and `scripts/lib/model-router.sh` (`tests/test-model-router.sh:7`) and defines `create_mock_binary` to place executable mock provider commands in a temp `PATH` (`tests/test-model-router.sh:17`). It asserts Codex routing for `gpt-*` and `o*` models (`tests/test-model-router.sh:37`, `tests/test-model-router.sh:54`, `tests/test-model-router.sh:71`, `tests/test-model-router.sh:88`, `tests/test-model-router.sh:105`), Claude routing for haiku/sonnet/opus and `claude-*` names (`tests/test-model-router.sh:122`, `tests/test-model-router.sh:139`, `tests/test-model-router.sh:156`, `tests/test-model-router.sh:173`, `tests/test-model-router.sh:190`), rejection of unknown/empty models (`tests/test-model-router.sh:207`, `tests/test-model-router.sh:224`), dependency success/failure for `codex` and `claude` binaries (`tests/test-model-router.sh:239`, `tests/test-model-router.sh:256`, `tests/test-model-router.sh:273`, `tests/test-model-router.sh:290`), and effort mapping/rejection (`tests/test-model-router.sh:311` through `tests/test-model-router.sh:414`).
- inputs_outputs_state: Inputs are literal model strings, effort strings, target provider strings, and controlled `PATH` values. Outputs are pass/fail assertions and the final `print_test_summary` call (`tests/test-model-router.sh:426`). Temporary mock binaries are created under test dirs; no repository state is modified.
- gates_or_invariants: Invariants mirror `scripts/lib/model-router.sh`: non-empty model required; `gpt-*` and `o[0-9]*` route to `codex`; `claude-*`, `haiku`, `sonnet`, and `opus` route to `claude`; unknown models are errors; provider dependency requires the matching binary in `PATH`; valid efforts are only `xhigh`, `high`, `medium`, `low`; `xhigh` maps to `high` for Claude with an info log, but passes through for Codex.
- dependencies_and_callers: Direct dependency is `scripts/lib/model-router.sh`, whose public functions are `detect_provider`, `check_provider_dependency`, and `map_effort`. Production caller `scripts/bitlesson-select.sh:11` sources the router, then uses `detect_provider` at `scripts/bitlesson-select.sh:89` and dependency checks at `scripts/bitlesson-select.sh:95`. The test is included by `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: It verifies empty model and unknown effort non-zero exits, missing binaries, case-insensitive Claude model detection via an uppercase `OPUS` example, and safe `PATH` isolation. It does not directly test unknown provider input to `check_provider_dependency`/`map_effort`, though the library has error paths for that.
- validation_or_tests: This file is the validation. It is also a contract for future provider routing changes because it pins exact accepted model-name patterns and effort vocabulary.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-114 `file` `prompt-template/block/bitlesson-delta-inconsistent.md`
- cursor: `[_]`
- core_role: Stop-gate remediation block for invalid or inconsistent `## BitLesson Delta` declarations in round summaries.
- algorithmic_behavior: The template is static Markdown. It tells the agent that the `## BitLesson Delta` declaration does not match expected BitLesson state (`prompt-template/block/bitlesson-delta-inconsistent.md:3`) and defines the transition rules: `Action: none` requires `Lesson ID(s): NONE` or omitted IDs, while `Action: add|update` requires concrete existing lesson IDs in `.humanize/bitlesson.md` (`prompt-template/block/bitlesson-delta-inconsistent.md:5`).
- inputs_outputs_state: Inputs are implicit validation failures from the BitLesson delta validator. The template itself has no variables. Output is a human-readable block reason rendered into a hook response. The state transition it enforces is “cannot exit current loop round until the summary’s BitLesson Delta action and IDs are consistent with the BitLesson file.”
- gates_or_invariants: It defines the invariant coupling summary action to lesson IDs. `none` cannot carry concrete lesson IDs; `add`/`update` cannot use `NONE` or missing IDs; referenced IDs must already be present in `.humanize/bitlesson.md`.
- dependencies_and_callers: `scripts/bitlesson-validate-delta.sh` loads it with `load_and_render_safe` for inconsistent `none`, missing IDs for `add|update`, no concrete IDs, invalid ID format, and invalid/nonexistent IDs (`scripts/bitlesson-validate-delta.sh:166`, `scripts/bitlesson-validate-delta.sh:192`, `scripts/bitlesson-validate-delta.sh:204`, `scripts/bitlesson-validate-delta.sh:247`, `scripts/bitlesson-validate-delta.sh:262`, `scripts/bitlesson-validate-delta.sh:277`). `hooks/loop-codex-stop-hook.sh` invokes that validator after summary existence checks (`hooks/loop-codex-stop-hook.sh:717`).
- edge_cases_or_failure_modes: Because the file has no placeholders, it cannot display specific invalid IDs unless the caller uses a different fallback/message. Its correctness depends on the validator detecting the specific mismatch and on template-loader fallback behavior if the file is missing.
- validation_or_tests: Covered indirectly by BitLesson delta tests and hook-stop tests; template-loader robustness tests cover missing/malformed template rendering behavior used by this gate.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-144 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Stop-gate remediation block for the RLCR loop when an agent attempts to exit without writing the required round or finalize work summary.
- algorithmic_behavior: The template explains the missing summary condition (`prompt-template/block/work-summary-missing.md:3`), renders the required target path via `{{SUMMARY_FILE}}` (`prompt-template/block/work-summary-missing.md:5`), lists required summary contents (`prompt-template/block/work-summary-missing.md:10`), and tells the agent it may attempt exit again after writing the summary (`prompt-template/block/work-summary-missing.md:16`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable supplied by the stop hook. Output is a rendered block reason. The enforced state transition is from “exit attempted with no summary file” back to active loop work; only after the summary file exists can the stop hook continue to later gates such as BitLesson Delta validation.
- gates_or_invariants: A summary file must exist for each non-finalize round as `round-${CURRENT_ROUND}-summary.md` or as `finalize-summary.md` in finalize phase. The summary should include implemented work, files changed, tests, and remaining items. This template is the first user-facing remediation before content-specific summary gates run.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` selects `SUMMARY_FILE` based on phase (`hooks/loop-codex-stop-hook.sh:684`), checks existence (`hooks/loop-codex-stop-hook.sh:689`), and renders this template with `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:696`). It depends on `hooks/lib/template-loader.sh` for placeholder substitution.
- edge_cases_or_failure_modes: If template loading fails, the stop hook has a fallback message containing the same `{{SUMMARY_FILE}}` placeholder. If the summary path includes shell-special characters, correctness depends on template rendering preserving literal values rather than treating them as commands; that behavior is stressed in template robustness tests.
- validation_or_tests: Stop-hook tests cover missing-state and active-loop blocking behavior, and template robustness tests cover fallback rendering, special characters, newlines, large values, and malformed templates.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-174 `file` `scripts/lib/config-loader.sh`
- cursor: `[_]`
- core_role: Shared JSON configuration loader and accessor for plugin defaults, user config, and project config. It centralizes config precedence for BitLesson/model defaults and loop-common runtime defaults.
- algorithmic_behavior: The file has a source guard (`scripts/lib/config-loader.sh:2`), requires `jq` (`scripts/lib/config-loader.sh:17`), validates each config layer as a JSON object (`scripts/lib/config-loader.sh:24`), and exposes `load_merged_config <plugin_root> <project_root>` (`scripts/lib/config-loader.sh:63`) plus `get_config_value <merged_json> <key>` (`scripts/lib/config-loader.sh:139`). Merge order is empty object, required default config, optional user config, optional project config (`scripts/lib/config-loader.sh:113`). A recursive `strip_nulls` filter removes nulls from objects/arrays before jq object multiplication, so higher-layer nulls do not erase lower-layer values (`scripts/lib/config-loader.sh:119`).
- inputs_outputs_state: Inputs are `plugin_root`, `project_root`, `$plugin_root/config/default_config.json`, user config at `$XDG_CONFIG_HOME/humanize/config.json` or `$HOME/.config/humanize/config.json`, and project config at `$HUMANIZE_CONFIG` or `$project_root/.humanize/config.json` (`scripts/lib/config-loader.sh:77` through `scripts/lib/config-loader.sh:88`). Output is merged JSON on stdout, warnings/errors on stderr, and scalar config values from `get_config_value`. Temporary layer files are created in `mktemp -d` and removed by trap (`scripts/lib/config-loader.sh:100`); no persistent state is written.
- gates_or_invariants: Missing or malformed required default config is fatal (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`). Missing optional user/project configs become `{}`. Malformed optional configs emit a warning and become `{}` (`scripts/lib/config-loader.sh:57`). `jq` is mandatory. `get_config_value` only reads top-level keys, returns strings unchanged, converts non-null non-strings with `tostring`, and returns empty for missing/null keys (`scripts/lib/config-loader.sh:148`).
- dependencies_and_callers: `scripts/bitlesson-select.sh:10` sources it, then loads merged config and reads `bitlesson_model` (`scripts/bitlesson-select.sh:15`). `hooks/lib/loop-common.sh:167` sources it and reads default BitLesson/Codex settings (`hooks/lib/loop-common.sh:177` through `hooks/lib/loop-common.sh:188`). Tests include `tests/test-config-merge.sh`, `tests/test-config-error-handling.sh`, and `tests/test-unified-codex-config.sh`.
- edge_cases_or_failure_modes: The helper intentionally uses `exit 1` inside the subshell path for required-layer failures because `set -e` does not reliably propagate nested function failures (`scripts/lib/config-loader.sh:43`). If `HOME` is unset and `XDG_CONFIG_HOME` is unset, the optional user path resolves under `/.config/humanize/config.json` and will normally be ignored as missing. Non-object JSON arrays/scalars are rejected as malformed layers. Deep nulls inside arrays are removed, which can compact arrays rather than preserve null positions.
- validation_or_tests: `tests/test-config-merge.sh` validates precedence, null stripping, and `HUMANIZE_CONFIG`. `tests/test-config-error-handling.sh` validates missing default failures and malformed optional fallback warnings. `tests/test-unified-codex-config.sh` validates `codex_model`/`codex_effort` defaults and project overrides flowing through loop-common and setup/PR loop state.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6/6 item evidence headings present; the headings above are the only item-id occurrences`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`