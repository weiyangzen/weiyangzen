# agent_12 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-012 `file` `README.md`
- cursor: `[_]`
- core_role: Top-level behavior-defining documentation for Humanize’s RLCR workflow. It defines the product’s algorithmic contract at a high level: Claude implements, Codex independently reviews, and feedback loops continue until acceptance criteria are met.
- algorithmic_behavior: The README states RLCR has two phases, “Implementation” and “Code Review” (`README.md:26`). Implementation uses Claude work plus Codex review of summaries; Code Review uses Codex for code-quality review. The loop is iterative, with issues feeding back into implementation until resolved (`README.md:15-18`, `README.md:26`).
- inputs_outputs_state: Inputs are a draft/plan file and user commands such as `/humanize:gen-plan --input draft.md --output docs/plan.md` and `/humanize:start-rlcr-loop docs/plan.md` (`README.md:43-51`). Outputs include an active monitored RLCR loop surfaced through `humanize monitor rlcr` (`README.md:53-57`) and loop artifacts documented elsewhere under `.humanize/rlcr`.
- gates_or_invariants: The main invariant is independent implementation/review separation: “Claude implements, Codex independently reviews” (`README.md:16`). Another gate is “Begin with the End in Mind,” requiring the human to understand the plan before loop start (`README.md:18`).
- dependencies_and_callers: Depends on the Codex CLI for review (`README.md:39`). Points users to `docs/usage.md`, install guides, and configuration documentation (`README.md:65-72`). Operationally, the README describes commands implemented under `commands/` and scripts/hooks under `scripts/` and `hooks/`.
- edge_cases_or_failure_modes: README itself does not enumerate failure behavior, but its install prerequisite creates an implicit failure mode: Codex CLI missing means review-dependent workflows cannot run (`README.md:39`). The “human must remain the architect” rule implies plan misunderstanding is a pre-loop risk (`README.md:18`).
- validation_or_tests: Not directly tested as an executable artifact. It is behavior-defining prose that aligns with executable specs such as `tests/test-ask-codex.sh`, `tests/test-state-exit-naming.sh`, and finalization/review tests in `tests/test-finalize-phase.sh`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-042 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit hook that enforces RLCR loop state integrity before allowing further user prompting. It protects the plan-file tracking invariant and branch consistency during active RLCR loops.
- algorithmic_behavior: Reads JSON hook input from stdin (`hooks/loop-plan-file-validator.sh:26-30`), extracts `session_id`, finds the active loop under `.humanize/rlcr`, resolves `state.md` or `finalize-state.md`, and strict-parses state (`hooks/loop-plan-file-validator.sh:32-48`). If no active loop exists, it allows exit immediately (`hooks/loop-plan-file-validator.sh:36-39`). It then validates required schema fields, current branch, and plan-file tracking state.
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR`, active loop state frontmatter, current Git branch, and Git index/status for `plan_file`. Outputs are either exit 0 with no JSON for allow, or JSON like `{"decision":"block","reason":...}` for blocked conditions (`hooks/loop-plan-file-validator.sh:71-76`, `hooks/loop-plan-file-validator.sh:99-104`, `hooks/loop-plan-file-validator.sh:175-180`). It does not mutate state.
- gates_or_invariants: Requires schema fields `plan_tracked` and `start_branch` to be present (`hooks/loop-plan-file-validator.sh:79-89`). Blocks if current branch differs from `start_branch` (`hooks/loop-plan-file-validator.sh:95-115`). If `plan_tracked=true`, plan file must remain tracked and clean (`hooks/loop-plan-file-validator.sh:123-192`). If not tracked, the plan file must remain untracked/gitignored (`hooks/loop-plan-file-validator.sh:193-228`).
- dependencies_and_callers: Registered as a UserPromptSubmit hook in `hooks/hooks.json:4-10`. Sources `hooks/lib/loop-common.sh` for `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, field constants, and templates (`hooks/loop-plan-file-validator.sh:13-21`). Uses `scripts/portable-timeout.sh` and `git` operations with a 30-second timeout (`hooks/loop-plan-file-validator.sh:19-24`, `hooks/loop-plan-file-validator.sh:96`, `hooks/loop-plan-file-validator.sh:127`, `hooks/loop-plan-file-validator.sh:152`).
- edge_cases_or_failure_modes: Fails closed on malformed state by exiting 1 with stderr (`hooks/loop-plan-file-validator.sh:44-48`). Blocks on Git timeout or unexpected Git errors (`hooks/loop-plan-file-validator.sh:98-105`, `hooks/loop-plan-file-validator.sh:129-146`, `hooks/loop-plan-file-validator.sh:154-171`, `hooks/loop-plan-file-validator.sh:199-216`). Blocks if a tracked plan becomes untracked, dirty, or an untracked plan becomes tracked (`hooks/loop-plan-file-validator.sh:174-191`, `hooks/loop-plan-file-validator.sh:220-227`).
- validation_or_tests: Referenced by `tests/test-plan-file-hooks.sh` and robustness tests. `tests/test-finalize-phase.sh` explicitly verifies the validator parses `finalize-state.md` without error (`tests/test-finalize-phase.sh:853-864`). `tests/robustness/test-hook-system-robustness.sh` also invokes it in malformed/hook-system scenarios.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-072 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable specification for the one-shot `ask-codex` algorithm. It verifies CLI parsing, Codex invocation behavior, storage side effects, error state classification, concurrency-safe directory naming, cache output, and safe skill guidance.
- algorithmic_behavior: Builds a mock Git project and mock `codex` binary controlled by `MOCK_CODEX_EXIT_CODE`, `MOCK_CODEX_STDOUT`, and `MOCK_CODEX_STDERR` (`tests/test-ask-codex.sh:29-58`). `run_ask_codex` executes `scripts/ask-codex.sh` with `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and the mock `PATH` (`tests/test-ask-codex.sh:67-75`). The target script parses options, validates model/effort, creates `.humanize/skill/<unique-id>`, runs `codex exec`, writes cache artifacts, and classifies result metadata (`scripts/ask-codex.sh:86-147`, `scripts/ask-codex.sh:202-237`, `scripts/ask-codex.sh:243-297`, `scripts/ask-codex.sh:308-410`).
- inputs_outputs_state: Inputs are CLI args, optional model/effort and timeout flags, environment variables, mock Codex stdout/stderr/exit code, Git project root, and `skills/ask-codex/SKILL.md`. Outputs are stdout containing Codex response, stderr status lines, `.humanize/skill/<id>/input.md`, `output.md`, `metadata.md`, and cache files `codex-run.cmd`, `codex-run.out`, `codex-run.log` (`tests/test-ask-codex.sh:174-199`, `tests/test-ask-codex.sh:382-405`; implementation at `scripts/ask-codex.sh:261-275`, `scripts/ask-codex.sh:387-403`).
- gates_or_invariants: Rejects empty question, unknown options, missing option values, non-numeric timeout, and unsafe model/effort characters (`tests/test-ask-codex.sh:84-154`; implementation at `scripts/ask-codex.sh:105-135`, `scripts/ask-codex.sh:162-186`). Requires Codex binary availability (`scripts/ask-codex.sh:153-160`). Preserves default effort when model lacks explicit effort (`tests/test-ask-codex.sh:341-350`). Treats `--` as the end of options so leading-dash question text is safe (`tests/test-ask-codex.sh:352-361`).
- dependencies_and_callers: Tests `scripts/ask-codex.sh` and `skills/ask-codex/SKILL.md` (`tests/test-ask-codex.sh:17-18`). Target script depends on `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout and config-backed Codex defaults (`scripts/ask-codex.sh:26-33`, `hooks/lib/loop-common.sh:183-190`). The command is referenced by `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, and task-tag routing tests for `analyze` tasks.
- edge_cases_or_failure_modes: Codex non-zero exit propagates exactly and records `status: error` (`tests/test-ask-codex.sh:220-238`; implementation `scripts/ask-codex.sh:332-355`). Empty stdout exits 1 and records `status: empty_response` (`tests/test-ask-codex.sh:240-257`; implementation `scripts/ask-codex.sh:358-380`). Timeout exit 124 emits timeout guidance and records `status: timeout` (`tests/test-ask-codex.sh:259-276`; implementation `scripts/ask-codex.sh:308-330`). Concurrent calls must produce distinct project-local and cache directories (`tests/test-ask-codex.sh:286-320`).
- validation_or_tests: This file is the validation harness. It also validates safe skill documentation: warns against bare `$ARGUMENTS`, documents quoted simple invocation, and requires free-form text as one quoted final argument (`tests/test-ask-codex.sh:415-433`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-102 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR state-file lifecycle naming and active-loop detection. It ensures terminal state files are not treated as active, `state.md` is active, and `end_loop` maps valid exit reasons to deterministic terminal filenames.
- algorithmic_behavior: Creates temporary Git/loop directories, sources `hooks/lib/loop-common.sh`, then checks `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` behavior (`tests/test-state-exit-naming.sh:31-67`). It verifies `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are terminal/inactive while `state.md` is active (`tests/test-state-exit-naming.sh:38-159`).
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr/<timestamp>/...-state.md` files with YAML frontmatter and test paths. Outputs are pass/fail counters and exit code equal to failed-test count (`tests/test-state-exit-naming.sh:293-302`). State transition output from `end_loop` is a renamed `${reason}-state.md` terminal file (`tests/test-state-exit-naming.sh:206-230`).
- gates_or_invariants: Active-loop detection should return only a loop with active `state.md` in this spec (`tests/test-state-exit-naming.sh:76-95`), and a newer directory with active state takes precedence (`tests/test-state-exit-naming.sh:161-180`). `end_loop` rejects invalid reasons and missing source state (`tests/test-state-exit-naming.sh:186-203`, `tests/test-state-exit-naming.sh:232-243`). Valid reasons are exactly `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`tests/test-state-exit-naming.sh:209-230`; constants at `hooks/lib/loop-common.sh:54-64`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` (`tests/test-state-exit-naming.sh:66-67`). The helper implementation resolves active states via `resolve_active_state_file` and `find_active_loop` (`hooks/lib/loop-common.sh:212-226`, `hooks/lib/loop-common.sh:251-327`). `is_in_humanize_loop_dir` recognizes only `.humanize/rlcr/` paths (`hooks/lib/loop-common.sh:877-881`).
- edge_cases_or_failure_modes: Explicitly guards against legacy `.humanize-loop.local` paths being recognized (`tests/test-state-exit-naming.sh:258-291`). Rejects invalid end reasons and missing state files (`tests/test-state-exit-naming.sh:186-243`). Note: current shared helper treats `finalize-state.md` as an active state (`hooks/lib/loop-common.sh:219-226`), so this test is a narrower state-exit naming spec, not the full current active-state matrix.
- validation_or_tests: This file is itself the validation. Related coverage in `tests/test-finalize-phase.sh` verifies `finalize-state.md` active handling and terminal `complete-state.md` behavior, including finalize completion renaming (`tests/test-finalize-phase.sh:293-319`, `tests/test-finalize-phase.sh:488-492`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-132 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Blocking prompt template for PR-loop trigger gating. It defines the user-facing reason when the PR loop cannot proceed because no required @bot trigger comment has been detected.
- algorithmic_behavior: Renders a block message explaining that the PR loop is waiting for a trigger comment (`prompt-template/block/no-trigger-comment.md:1-4`), includes startup-case and current-round context (`prompt-template/block/no-trigger-comment.md:5-8`), and gives the required action plus example mention (`prompt-template/block/no-trigger-comment.md:9-17`).
- inputs_outputs_state: Inputs are template variables `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING` (`prompt-template/block/no-trigger-comment.md:6-14`). Output is Markdown used as the JSON `reason` body in a stop-hook block decision.
- gates_or_invariants: Enforces explicit trigger before review polling for startup cases/rounds that require it. The required action is a PR comment mentioning the configured bot string (`prompt-template/block/no-trigger-comment.md:9-15`).
- dependencies_and_callers: Rendered by `hooks/pr-loop-stop-hook.sh` when `REQUIRE_TRIGGER=true` and `PR_LAST_TRIGGER_AT` is empty (`hooks/pr-loop-stop-hook.sh:721-747`). The stop hook passes the template variables via `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:737-742`) and returns JSON `{"decision":"block", ...}` (`hooks/pr-loop-stop-hook.sh:744-746`).
- edge_cases_or_failure_modes: If the template is missing or fails, `pr-loop-stop-hook.sh` has an inline fallback message (`hooks/pr-loop-stop-hook.sh:737-740`). The broader stop hook also defensively blocks later if a later round lacks a trigger before polling (`hooks/pr-loop-stop-hook.sh:849-867`).
- validation_or_tests: Exercised indirectly by PR-loop stop-hook/API tests. `tests/robustness/test-pr-loop-api-poll.sh` sources `test-pr-loop-api-robustness.sh`, whose `run_poll_tests` covers stop-hook no-loop/corrupt-state behavior and poll-trigger-adjacent behavior (`tests/robustness/test-pr-loop-api-poll.sh:13-17`, `tests/robustness/test-pr-loop-api-robustness.sh:560-620`). Direct template reference is visible in `hooks/pr-loop-stop-hook.sh:740`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-162 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit prompt/template for RLCR Code Review Phase invocation. It documents the transition contract from implementation completion into automated `codex review` and subsequent branch of either remediation or finalization.
- algorithmic_behavior: Records review round, base branch, timestamp, and the exact phase algorithm: run `codex review --base {{BASE_BRANCH}}`, scan output for `[P0-9]` severity markers, return a fix prompt when issues exist, and transition to Finalize Phase when no issues exist (`prompt-template/codex/code-review-phase.md:6-18`).
- inputs_outputs_state: Inputs are `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP` (`prompt-template/codex/code-review-phase.md:1`, `prompt-template/codex/code-review-phase.md:8-10`). Outputs are audit files and captures: `round-N-review-prompt.md`, `round-N-review-result.md`, `round-N-codex-review.cmd`, `.out`, and `.log` (`prompt-template/codex/code-review-phase.md:30-36`).
- gates_or_invariants: `codex review` does not accept prompt input; it reviews based on Git diff (`prompt-template/codex/code-review-phase.md:3-4`). Severity markers `[P0-9]` are the gate signal for issue detection (`prompt-template/codex/code-review-phase.md:14-17`, `prompt-template/codex/code-review-phase.md:21-28`). No markers means finalize transition.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` before running review (`hooks/loop-codex-stop-hook.sh:1040`). The hook then records command invocation and executes `codex review --base "$review_base"` with configured args and timeout (`hooks/loop-codex-stop-hook.sh:1059-1066`). Marker detection is implemented by `detect_review_issues` in `hooks/lib/loop-common.sh`, scanning the last 50 log lines for `[P?]` markers (`hooks/lib/loop-common.sh:488-557`).
- edge_cases_or_failure_modes: Missing/empty review logs are hard errors from `detect_review_issues` (`hooks/lib/loop-common.sh:514-518`). The hook also blocks exit when `codex review` fails or produces no output, per references in `hooks/loop-codex-stop-hook.sh:1308-1368`. False positives are reduced by scanning only the tail and requiring `[P?]` in the first 10 characters (`hooks/lib/loop-common.sh:497-505`, `hooks/lib/loop-common.sh:524-535`).
- validation_or_tests: Covered by `tests/test-finalize-phase.sh` and `tests/test-codex-review-merge.sh`, including review-result creation, clean review finalization, review failure blocking, and empty output blocking (`tests/test-finalize-phase.sh:566-653`, `tests/test-finalize-phase.sh:801-804`; `tests/test-codex-review-merge.sh:318-329`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-192 `file` `tests/robustness/test-pr-loop-api-poll.sh`
- cursor: `[_]`
- core_role: Thin executable harness for the second half of PR Loop API robustness coverage. It delegates to `run_poll_tests`, focusing on PR stop-hook polling integration and `scripts/poll-pr-reviews.sh`.
- algorithmic_behavior: Sources `tests/robustness/test-pr-loop-api-robustness.sh`, invokes `run_poll_tests`, prints a summary, and exits with the summary status (`tests/robustness/test-pr-loop-api-poll.sh:13-17`). The delegated test group covers tests 12-19: stop hook no-active-loop handling, corrupted state handling, approve-state path creation, poll script help/arg validation, JSON output shape, slow API behavior, and API failure behavior (`tests/robustness/test-pr-loop-api-robustness.sh:560-855`).
- inputs_outputs_state: Inputs are the shared robustness test library, temporary Git repos, mock `gh` binaries, PR number, timestamp, and bot list. Outputs are test pass/fail counters and process exit status from `print_test_summary`. The production poll script outputs JSON with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:317-327`).
- gates_or_invariants: `poll-pr-reviews.sh` requires PR number, `--after`, `--bots`, numeric PR number, `gh`, and `jq` (`scripts/poll-pr-reviews.sh:83-116`). Bot authors are mapped consistently, including `codex -> chatgpt-codex-connector[bot]` (`scripts/poll-pr-reviews.sh:164-173`). Polling must return valid JSON and boolean `has_new_comments`, even when API calls fail (`tests/robustness/test-pr-loop-api-robustness.sh:669-685`, `tests/robustness/test-pr-loop-api-robustness.sh:822-850`).
- dependencies_and_callers: Depends directly on `tests/robustness/test-pr-loop-api-robustness.sh` (`tests/robustness/test-pr-loop-api-poll.sh:13-16`). Production flow is `hooks/pr-loop-stop-hook.sh` calling `scripts/poll-pr-reviews.sh` (`hooks/pr-loop-stop-hook.sh:802-867`, `hooks/pr-loop-stop-hook.sh:961-966`). The poll script calls GitHub APIs for issue comments, review comments, and PR reviews, then filters by timestamp and bot author (`scripts/poll-pr-reviews.sh:240-305`).
- edge_cases_or_failure_modes: The harness verifies no active PR loop exits 0 and corrupted state does not crash with signal-level exit (`tests/robustness/test-pr-loop-api-robustness.sh:571-604`). It verifies slow API calls complete gracefully (`tests/robustness/test-pr-loop-api-robustness.sh:688-752`). API fetch failures retry and then degrade to `[]` without terminating the script (`scripts/poll-pr-reviews.sh:200-235`), and the test requires exit 0 plus valid JSON with boolean false (`tests/robustness/test-pr-loop-api-robustness.sh:755-850`).
- validation_or_tests: This file is the split runner for poll tests and is listed in the aggregate runner (`tests/run-all-tests.sh:119`). Its direct value is orchestration, not unique assertions; the assertions live in the sourced shared file.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7 unique Item Evidence headings present; each assigned item_id is represented once in the evidence section`
- missing_items: `none`
- duplicate_items: `none in Item Evidence headings`
- final_worker_status: `complete`