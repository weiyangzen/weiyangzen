# agent_15 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt fragment directory for the RLCR loop. These templates define Claude’s implementation, continuation, review-repair, finalize, goal-tracker, and agent-teams instructions. They are not runtime code, but they are core to the loop’s behavioral contract because `hooks/loop-codex-stop-hook.sh` and `scripts/setup-rlcr-loop.sh` assemble them into round prompts and phase prompts.
- algorithmic_behavior: The directory is flat, with 12 markdown templates. `agent-teams-instructions.md` sets initial Agent Teams mode and tells the leader to read the plan and goal tracker before splitting work (`prompt-template/claude/agent-teams-instructions.md:1`). `agent-teams-core.md` enforces a coordinator-only role, strict file ownership boundaries, Task tool delegation, review of teammate output, and BitLesson selection before subtasks (`prompt-template/claude/agent-teams-core.md:3`, `prompt-template/claude/agent-teams-core.md:16`, `prompt-template/claude/agent-teams-core.md:18`, `prompt-template/claude/agent-teams-core.md:23`). `agent-teams-continue.md` resets team assumptions between rounds, requiring a brand-new team and review-first allocation from Codex feedback (`prompt-template/claude/agent-teams-continue.md:7`, `prompt-template/claude/agent-teams-continue.md:8`). `review-phase-prompt.md` translates Codex findings into Claude repair work and says COMPLETE has no effect in review phase (`prompt-template/claude/review-phase-prompt.md:11`, `prompt-template/claude/review-phase-prompt.md:26`). `finalize-phase-prompt.md` and `finalize-phase-skipped-prompt.md` define the post-review cleanup state, with stricter behavior when review passed and manual-verification behavior when review was skipped (`prompt-template/claude/finalize-phase-prompt.md:3`, `prompt-template/claude/finalize-phase-skipped-prompt.md:3`). `next-round-prompt.md` injects original plan, Codex review, goal tracker reference, and BitLesson requirements for continued implementation (`prompt-template/claude/next-round-prompt.md:5`, `prompt-template/claude/next-round-prompt.md:15`, `prompt-template/claude/next-round-prompt.md:27`). `next-round-footer.md` prevents false exits and state tampering, then requires commit and summary write (`prompt-template/claude/next-round-footer.md:4`, `prompt-template/claude/next-round-footer.md:8`). Small inserts add open-question blocking, post-alignment emphasis, push-every-round enforcement, and goal-tracker update request syntax (`prompt-template/claude/open-question-notice.md:1`, `prompt-template/claude/post-alignment-action-items.md:4`, `prompt-template/claude/push-every-round-note.md:2`, `prompt-template/claude/goal-tracker-update-request.md:2`).
- inputs_outputs_state: Inputs are template placeholders such as `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, `{{REVIEW_CONTENT}}`, `{{SUMMARY_FILE}}`, `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{FINALIZE_SUMMARY_FILE}}`, and `{{REVIEW_SKIP_REASON}}`. Outputs are markdown instructions written into RLCR prompt files or hook block messages. State is indirect: these templates drive whether Claude repairs findings, asks user questions, writes summaries, commits, enters finalize cleanup, or pushes each round. They do not mutate state files themselves.
- gates_or_invariants: Invariants include: Claude must not directly implement when in agent-teams leader mode (`prompt-template/claude/agent-teams-core.md:3`); file ownership must avoid overlapping teammate edits (`prompt-template/claude/agent-teams-core.md:18`); all Codex `[P0-9]` findings must be addressed in review phase (`prompt-template/claude/review-phase-prompt.md:12`); goal tracker is read-only after round 0 and update requests must go through summaries (`prompt-template/claude/next-round-prompt.md:35`); open questions must be asked before other findings (`prompt-template/claude/open-question-notice.md:1`); finalize simplification must preserve functionality and tests (`prompt-template/claude/finalize-phase-prompt.md:20`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh` injects agent-teams initial templates when `--agent-teams` is enabled, using `claude/agent-teams-instructions.md` and `claude/agent-teams-core.md`. `hooks/loop-codex-stop-hook.sh` loads finalize, review, next-round, open-question, post-alignment, push, goal update, and agent-teams continuation fragments at its prompt construction call sites. Rendering depends on `hooks/lib/template-loader.sh`, whose `load_template`, `load_and_render_safe`, and single-pass `render_template` functions support these files.
- edge_cases_or_failure_modes: Agent Teams mode assumes each teammate is cold-started and old teammates are gone on continuation (`prompt-template/claude/agent-teams-continue.md:7`); if Codex found open questions, the open-question notice creates a priority inversion over other findings (`prompt-template/claude/open-question-notice.md:1`); when review was skipped, finalization becomes weaker and depends on manual checks (`prompt-template/claude/finalize-phase-skipped-prompt.md:9`); if `--push-every-round` is enabled, generated instructions require remote push after each round (`prompt-template/claude/push-every-round-note.md:2`).
- validation_or_tests: Agent Teams behavior is covered by `tests/test-agent-teams.sh`, including template existence and continuation injection. Template rendering behavior for all prompt templates is broadly covered by `tests/test-template-loader.sh`, which verifies loading, fallback, and safe placeholder substitution. The hook integration sites are in `hooks/loop-codex-stop-hook.sh`, while initial setup injection is in `scripts/setup-rlcr-loop.sh`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-045 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse gate for Claude `Write` operations in RLCR and PR-loop state directories. It protects generated loop control surfaces from direct writes and enforces the current round’s allowed summary path.
- algorithmic_behavior: The script reads hook JSON from stdin (`hooks/loop-write-validator.sh:25`), validates JSON and depth through shared helpers (`hooks/loop-write-validator.sh:28`, `hooks/loop-write-validator.sh:33`), exits early unless `tool_name` is `Write` (`hooks/loop-write-validator.sh:39`), then requires `tool_input.file_path` (`hooks/loop-write-validator.sh:43`). It blocks RLCR todos unless the path is allowlisted for the active loop (`hooks/loop-write-validator.sh:58`), blocks round prompt files (`hooks/loop-write-validator.sh:68`), protects PR-loop state and read-only generated files (`hooks/loop-write-validator.sh:77`, `hooks/loop-write-validator.sh:87`), validates PR resolve summary round number (`hooks/loop-write-validator.sh:93`), identifies RLCR summary/finalize-summary/loop-dir paths (`hooks/loop-write-validator.sh:103`), locates the active session-aware loop (`hooks/loop-write-validator.sh:129`), strictly parses the active state file (`hooks/loop-write-validator.sh:143`), blocks state and finalize-state writes (`hooks/loop-write-validator.sh:154`, `hooks/loop-write-validator.sh:159`), allows only the active loop’s `finalize-summary.md` in finalize phase (`hooks/loop-write-validator.sh:170`), blocks plan backup writes (`hooks/loop-write-validator.sh:181`), blocks goal-tracker writes after round 0 (`hooks/loop-write-validator.sh:194`), blocks summaries outside `.humanize/rlcr` (`hooks/loop-write-validator.sh:204`), checks wrong round numbers (`hooks/loop-write-validator.sh:230`), and finally enforces exact active-loop directory path (`hooks/loop-write-validator.sh:254`).
- inputs_outputs_state: Input is a Claude hook JSON payload containing `tool_name`, `tool_input.file_path`, and optionally `session_id`. Output is process exit status plus stderr block text rendered from templates. Exit `0` allows the write, exit `1` indicates malformed hook input or malformed loop state, and exit `2` blocks a policy violation. It reads `.humanize/rlcr/<session>/state.md` or `finalize-state.md`, and `.humanize/pr-loop/<session>/state.md`, but does not mutate them.
- gates_or_invariants: Only the active current round summary is writable by `Write`; `state.md`, `finalize-state.md`, prompt files, plan backup, PR-loop state, and PR-loop generated files are system-owned. Goal tracker becomes write-protected after round 0. Summary writes must be inside the active loop directory and match the current round. Malformed state fails closed (`hooks/loop-write-validator.sh:143`). Active-loop discovery uses session filtering from hook input (`hooks/loop-write-validator.sh:51`).
- dependencies_and_callers: Registered in `hooks/hooks.json` as a PreToolUse matcher for `Write` (`hooks/hooks.json:14`, `hooks/hooks.json:20`). It sources `hooks/lib/loop-common.sh` (`hooks/loop-write-validator.sh:18`), which provides `validate_hook_input`, `is_deeply_nested`, `find_active_loop`, `resolve_active_state_file`, state parsing, path classifiers, and block message renderers. It depends on `jq` for JSON parsing and `hooks/lib/template-loader.sh` via `loop-common.sh` for rendering template messages.
- edge_cases_or_failure_modes: With no active loop, most RLCR checks are bypassed and normal writes are allowed (`hooks/loop-write-validator.sh:131`). It compares final directory correctness by exact string path (`hooks/loop-write-validator.sh:256`), so semantically equivalent but non-identical paths can be blocked. The PR-loop `pr-resolve` path is the only PR-loop round file intentionally writable, and only if its round matches active PR loop state (`hooks/loop-write-validator.sh:93`). The finalize summary allowance is narrow: `FILE_PATH` must equal `$ACTIVE_LOOP_DIR/finalize-summary.md` exactly (`hooks/loop-write-validator.sh:172`).
- validation_or_tests: Direct and indirect coverage exists in the hook robustness/finalize tests. `tests/test-finalize-phase.sh` exercises write blocking for `finalize-state.md`, and `tests/robustness/test-hook-system-robustness.sh` covers state write blocking, malformed state behavior, and path traversal defenses. `tests/test-template-loader.sh` validates the template fallback/render path used for block messages.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-075 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the safe RLCR cancellation escape hatch. It validates that the Bash hook allows only the intended `state.md` to `cancel-state.md` rename when a `.cancel-requested` signal exists in the active loop directory.
- algorithmic_behavior: The test creates an isolated temporary `.humanize/rlcr/<timestamp>/state.md` with round metadata (`tests/test-cancel-signal-file.sh:42`, `tests/test-cancel-signal-file.sh:47`) and exports `CLAUDE_PROJECT_DIR` to point validators at that temp project (`tests/test-cancel-signal-file.sh:58`). `run_bash_validator` builds JSON with `tool_name: Bash` and `tool_input.command`, sends it to `hooks/loop-bash-validator.sh`, and returns the hook exit code (`tests/test-cancel-signal-file.sh:63`, `tests/test-cancel-signal-file.sh:72`). Positive tests allow exact active-loop rename with a signal (`tests/test-cancel-signal-file.sh:80`, `tests/test-cancel-signal-file.sh:82`, `tests/test-cancel-signal-file.sh:83`) and `/./` path normalization (`tests/test-cancel-signal-file.sh:100`, `tests/test-cancel-signal-file.sh:104`). Additional positive tests cover quoted paths and literal `${loop_dir}` command syntax (`tests/test-cancel-signal-file.sh:365`, `tests/test-cancel-signal-file.sh:450`, `tests/test-cancel-signal-file.sh:470`).
- inputs_outputs_state: Inputs are synthetic Bash commands and signal-file presence/absence. Output is a pass/fail counter and final process exit equal to failed test count (`tests/test-cancel-signal-file.sh:1349`, `tests/test-cancel-signal-file.sh:1357`). The test creates loop state fixtures and signal files but the validator only authorizes or blocks; it does not perform the actual rename. The intended state transition being guarded is active `state.md` or `finalize-state.md` becoming terminal `cancel-state.md`, as implemented by `is_cancel_authorized` in `hooks/lib/loop-common.sh`.
- gates_or_invariants: The signal file must be inside the active loop directory (`tests/test-cancel-signal-file.sh:201`, `tests/test-cancel-signal-file.sh:204`). Without the signal, even the exact rename is blocked (`tests/test-cancel-signal-file.sh:121`). With the signal, other state modifications remain blocked: redirect write, `sed -i`, wrong destination, `rm`, arbitrary destination, and `cp` (`tests/test-cancel-signal-file.sh:141`, `tests/test-cancel-signal-file.sh:161`, `tests/test-cancel-signal-file.sh:181`, `tests/test-cancel-signal-file.sh:223`, `tests/test-cancel-signal-file.sh:283`, `tests/test-cancel-signal-file.sh:406`). The helper must enforce exactly two arguments, correct source and destination, no leftover shell variables, no shell operators, no command substitution, no wrapper bypass, and no unrelated `state.md` path.
- dependencies_and_callers: It sources `hooks/lib/loop-common.sh` (`tests/test-cancel-signal-file.sh:33`) and invokes `hooks/loop-bash-validator.sh` (`tests/test-cancel-signal-file.sh:72`). The core implementation under test is `is_cancel_authorized`, documented and implemented in `hooks/lib/loop-common.sh:698` through `hooks/lib/loop-common.sh:875`, and the Bash validator’s state-file protection path in `hooks/loop-bash-validator.sh:151` through `hooks/loop-bash-validator.sh:340`.
- edge_cases_or_failure_modes: The test suite explicitly covers injection via `&&`, `;`, `$()`, backticks, newline, `${IFS}`, prefix commands (`sudo`, `env`, `command --`), chained shell operators, pipes, background operators, `sh -c`/`bash -c`, leading redirections, append redirections, quoted redirection targets, escaped-space redirection targets, and ANSI-C quoting (`tests/test-cancel-signal-file.sh:243`, `tests/test-cancel-signal-file.sh:263`, `tests/test-cancel-signal-file.sh:303`, `tests/test-cancel-signal-file.sh:323`, `tests/test-cancel-signal-file.sh:345`, `tests/test-cancel-signal-file.sh:533`, `tests/test-cancel-signal-file.sh:552`, `tests/test-cancel-signal-file.sh:592`, `tests/test-cancel-signal-file.sh:632`, `tests/test-cancel-signal-file.sh:672`, `tests/test-cancel-signal-file.sh:812`, `tests/test-cancel-signal-file.sh:852`, `tests/test-cancel-signal-file.sh:972`, `tests/test-cancel-signal-file.sh:1032`, `tests/test-cancel-signal-file.sh:1112`, `tests/test-cancel-signal-file.sh:1152`). It also asserts that when there is no active loop, commands are allowed (`tests/test-cancel-signal-file.sh:1233`), preserving non-loop Bash behavior.
- validation_or_tests: This file is itself the validation artifact. It contains 53 validator-level tests plus direct helper checks for `is_cancel_authorized` (`tests/test-cancel-signal-file.sh:1251`). It should be run in an environment with `jq`, Bash, and the repository hook scripts available.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-105 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR/PR-loop template loading and rendering library. It validates that prompt/block templates can be found, rendered, safely fall back, and resist placeholder injection from variable values.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` (`tests/test-template-loader.sh:12`) and exercises `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. It verifies template-root discovery from `hooks/lib` to `prompt-template` (`tests/test-template-loader.sh:45`), loads an existing real block template (`tests/test-template-loader.sh:59`), checks missing template behavior (`tests/test-template-loader.sh:72`), tests single and multiple variable replacement (`tests/test-template-loader.sh:85`, `tests/test-template-loader.sh:100`), multiline replacement (`tests/test-template-loader.sh:115`), and integration rendering with `block/wrong-round-number.md` (`tests/test-template-loader.sh:151`). It then stress-tests literal rendering of shell metacharacters and structured content (`tests/test-template-loader.sh:247`) and verifies single-pass placeholder injection prevention (`tests/test-template-loader.sh:488`).
- inputs_outputs_state: Inputs are template strings, `VAR=value` assignments, and real template filenames. Outputs are rendered strings compared to expected literals, pass/fail counters, and final exit `0` only when `TESTS_FAILED` is zero (`tests/test-template-loader.sh:651`). There is no persistent repository state; all behavior is in-memory except reading template files.
- gates_or_invariants: Missing variables must remain as placeholders (`tests/test-template-loader.sh:182`). Missing templates through `load_and_render_safe` must render the fallback (`tests/test-template-loader.sh:197`). Existing templates must not use fallback content (`tests/test-template-loader.sh:212`). Template directories must contain required subdirectories (`tests/test-template-loader.sh:225`), matching `validate_template_dir` requirements in `hooks/lib/template-loader.sh:216`. Variable values containing `{{OTHER_VAR}}` must not be rescanned or expanded (`tests/test-template-loader.sh:499`, `tests/test-template-loader.sh:512`, `tests/test-template-loader.sh:525`).
- dependencies_and_callers: It directly tests `hooks/lib/template-loader.sh`, whose renderer builds `TMPL_VAR_` environment variables and performs one-pass `awk` substitution (`hooks/lib/template-loader.sh:62`, `hooks/lib/template-loader.sh:74`, `hooks/lib/template-loader.sh:116`). Callers of this library include hook block-message renderers in `hooks/lib/loop-common.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: The file covers ampersands, backslashes, dollars, backticks, pipes, semicolons, globs, parentheses, redirection syntax, quotes, comments, tilde, combined metacharacters, Windows paths, regex-like strings, JSON-like values, multiline metacharacter values, empty substitutions, non-ASCII template/value content, underscore-prefixed variables, and numeric variable names (`tests/test-template-loader.sh:257`, `tests/test-template-loader.sh:269`, `tests/test-template-loader.sh:281`, `tests/test-template-loader.sh:293`, `tests/test-template-loader.sh:305`, `tests/test-template-loader.sh:317`, `tests/test-template-loader.sh:329`, `tests/test-template-loader.sh:341`, `tests/test-template-loader.sh:353`, `tests/test-template-loader.sh:365`, `tests/test-template-loader.sh:389`, `tests/test-template-loader.sh:413`, `tests/test-template-loader.sh:437`, `tests/test-template-loader.sh:449`, `tests/test-template-loader.sh:461`, `tests/test-template-loader.sh:473`, `tests/test-template-loader.sh:555`, `tests/test-template-loader.sh:579`, `tests/test-template-loader.sh:604`, `tests/test-template-loader.sh:617`, `tests/test-template-loader.sh:629`).
- validation_or_tests: This file is itself the primary validation. Its coverage is important for every assigned template file because the same loader renders Claude prompts, Codex review prompts, and block messages.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-135 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Block-message template for attempts to write PR-loop generated prompt/comment files. It provides the human-facing explanation when validators reject direct writes to PR-loop read-only files.
- algorithmic_behavior: The template emits a “PR Loop File Write Blocked” message (`prompt-template/block/pr-loop-prompt-write.md:1`), says `round-*-pr-comment.md` and `round-*-prompt.md` in `.humanize/pr-loop/` cannot be written (`prompt-template/block/pr-loop-prompt-write.md:3`), and explains those files are generated by the PR loop system (`prompt-template/block/pr-loop-prompt-write.md:5`). It distinguishes fetched PR comments from current-round instructions (`prompt-template/block/pr-loop-prompt-write.md:6`, `prompt-template/block/pr-loop-prompt-write.md:7`).
- inputs_outputs_state: There are no placeholders in this template. Input is only the validator’s decision to render it. Output is static markdown on stderr through `pr_loop_prompt_blocked_message`. It does not mutate state.
- gates_or_invariants: The invariant expressed is that generated PR-loop comment and prompt files are read-only and system-managed. The implementation gate is broader than the wording: shared helper `is_pr_loop_readonly_file` also treats `round-*-codex-prompt.md`, `round-*-pr-check.md`, and `round-*-pr-feedback.md` as read-only (`hooks/lib/loop-common.sh:990`, `hooks/lib/loop-common.sh:995`). The Bash validator applies the same read-only class to command-based writes (`hooks/loop-bash-validator.sh:435`).
- dependencies_and_callers: Loaded by `pr_loop_prompt_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:1063`, `hooks/lib/loop-common.sh:1070`). It is called from `hooks/loop-write-validator.sh` for PR-loop read-only file writes (`hooks/loop-write-validator.sh:87`) and from `hooks/loop-bash-validator.sh` for Bash command attempts against PR-loop generated files (`hooks/loop-bash-validator.sh:443`).
- edge_cases_or_failure_modes: The template text names only two generated file classes, while the code blocks five classes. That is a documentation precision issue: users attempting to write `round-*-codex-prompt.md`, `round-*-pr-check.md`, or `round-*-pr-feedback.md` may receive a message that does not list their exact file type, although it still communicates system-managed read-only status.
- validation_or_tests: Template existence and safe loading are covered by template reference and comprehensive template tests. PR-loop validator behavior is covered by PR-loop script and stop-hook tests, plus Bash/Edit/Write validator robustness tests around PR-loop state and generated surfaces.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-165 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex review prompt for normal RLCR review rounds. It defines the acceptance gate that decides whether Claude continues repair work or the loop can move toward completion/finalize.
- algorithmic_behavior: The prompt is parameterized by `{{CURRENT_ROUND}}`, `{{PLAN_FILE}}`, `{{PROMPT_FILE}}`, `{{SUMMARY_CONTENT}}`, `{{DOCS_PATH}}`, `{{GOAL_TRACKER_FILE}}`, `{{GOAL_TRACKER_UPDATE_SECTION}}`, and `{{REVIEW_RESULT_FILE}}`. It requires Codex to read the original plan before reviewing (`prompt-template/codex/regular-review.md:5`, `prompt-template/codex/regular-review.md:8`), compare Claude’s claims against the plan and current prompt (`prompt-template/codex/regular-review.md:11`), inspect Claude’s summary (`prompt-template/codex/regular-review.md:14`), and conduct a skeptical implementation review focused on plan-vs-actual gaps (`prompt-template/codex/regular-review.md:22`). It treats deferred and pending tasks as incomplete and asks Codex to draft a concrete implementation plan for Claude when work remains (`prompt-template/codex/regular-review.md:24`, `prompt-template/codex/regular-review.md:28`, `prompt-template/codex/regular-review.md:30`). It mandates a goal-alignment check over acceptance criteria, forgotten items, deferrals, and plan evolution (`prompt-template/codex/regular-review.md:33`, `prompt-template/codex/regular-review.md:35`).
- inputs_outputs_state: Input is rendered review context from the active RLCR loop: plan backup, round prompt, Claude summary, docs path, goal tracker, and output path. Output is either review comments written to `{{REVIEW_RESULT_FILE}}` or the sentinel `COMPLETE` as the last line (`prompt-template/codex/regular-review.md:52`, `prompt-template/codex/regular-review.md:53`). The template does not mutate state directly, but the `COMPLETE` sentinel is consumed by the stop-hook’s state machine to end the repair loop or enter finalize flow.
- gates_or_invariants: Codex must not output `COMPLETE` unless all original plan tasks are complete, all acceptance criteria are met, and there are no deferrals or pending work (`prompt-template/codex/regular-review.md:53`, `prompt-template/codex/regular-review.md:56`). Deferrals are explicitly incomplete (`prompt-template/codex/regular-review.md:54`). Pending work is explicitly incomplete (`prompt-template/codex/regular-review.md:55`). The review must include a goal alignment summary in the requested format (`prompt-template/codex/regular-review.md:42`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` loads this template for regular review rounds. Rendering depends on `hooks/lib/template-loader.sh`. Its output is paired with Claude-side `prompt-template/claude/review-phase-prompt.md`, which turns Codex findings into the next repair prompt.
- edge_cases_or_failure_modes: If Claude says work is deferred, Codex is instructed not to accept that deferral and must force completion now (`prompt-template/codex/regular-review.md:24`). If Claude admits work is pending, Codex must include the pending tasks in review rather than treating honesty as acceptance (`prompt-template/codex/regular-review.md:28`). If the actual implementation mismatches Claude’s summary, review comments must be written rather than `COMPLETE` (`prompt-template/codex/regular-review.md:52`). A malformed or weak review prompt render would directly weaken RLCR completion gating, so the single-pass template loader tests are relevant here.
- validation_or_tests: Template loading/rendering is covered by `tests/test-template-loader.sh`. Stop-hook behavior around review/finalize phases is covered by `tests/test-finalize-phase.sh`, including COMPLETE handling and review failure cases. Agent-teams tests also assert that review-phase prompts do not accidentally include implementation-phase agent-teams continuation instructions.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-195 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for starting RLCR and PR loops. It validates setup-time guards before the RLCR state machine is allowed to create active loop state.
- algorithmic_behavior: The test sources shared helpers and portable timeout support (`tests/robustness/test-setup-scripts-robustness.sh:17`, `tests/robustness/test-setup-scripts-robustness.sh:18`), creates isolated temp repositories, and defines helpers for minimal plans, git initialization, RLCR setup invocation, and PR setup invocation (`tests/robustness/test-setup-scripts-robustness.sh:31`, `tests/robustness/test-setup-scripts-robustness.sh:51`, `tests/robustness/test-setup-scripts-robustness.sh:67`, `tests/robustness/test-setup-scripts-robustness.sh:80`). It then runs grouped tests over argument parsing, plan validation, YAML safety, git prerequisites, PR-loop setup, loop mutual exclusion, symlink protection, success-path validation, timeout behavior, full-review-round validation, skip-implementation mode, and dependency reporting.
- inputs_outputs_state: Inputs are command-line arguments to `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, temp plan files, mock `codex`, `jq`, `gh`, `timeout`, and git repositories. Outputs are pass/fail/skip counters from `tests/test-helpers.sh` and final exit from `print_test_summary` (`tests/robustness/test-setup-scripts-robustness.sh:1215`). For success and skip-impl cases, setup creates `.humanize/rlcr/<timestamp>/state.md`; skip-impl additionally creates `.review-phase-started` and a simplified goal tracker (`tests/robustness/test-setup-scripts-robustness.sh:1011`, `tests/robustness/test-setup-scripts-robustness.sh:1012`, `tests/robustness/test-setup-scripts-robustness.sh:1025`, `tests/robustness/test-setup-scripts-robustness.sh:1038`).
- gates_or_invariants: RLCR setup must reject missing plan file unless `--skip-impl` is used (`tests/robustness/test-setup-scripts-robustness.sh:105`, `tests/robustness/test-setup-scripts-robustness.sh:975`). Numeric options must reject non-numeric or missing values (`tests/robustness/test-setup-scripts-robustness.sh:118`, `tests/robustness/test-setup-scripts-robustness.sh:166`, `tests/robustness/test-setup-scripts-robustness.sh:931`, `tests/robustness/test-setup-scripts-robustness.sh:949`). Plans must be meaningful, relative, shell-safe, non-symlinked, inside the project, and either gitignored or explicitly tracked cleanly (`tests/robustness/test-setup-scripts-robustness.sh:224`, `tests/robustness/test-setup-scripts-robustness.sh:254`, `tests/robustness/test-setup-scripts-robustness.sh:280`, `tests/robustness/test-setup-scripts-robustness.sh:303`, `tests/robustness/test-setup-scripts-robustness.sh:323`, `tests/robustness/test-setup-scripts-robustness.sh:444`, `tests/robustness/test-setup-scripts-robustness.sh:605`, `tests/robustness/test-setup-scripts-robustness.sh:632`). Setup must run inside a git repo with at least one commit (`tests/robustness/test-setup-scripts-robustness.sh:412`, `tests/robustness/test-setup-scripts-robustness.sh:425`). Active RLCR and PR loops are mutually exclusive (`tests/robustness/test-setup-scripts-robustness.sh:535`, `tests/robustness/test-setup-scripts-robustness.sh:565`). Dependency failures must report missing tools together (`tests/robustness/test-setup-scripts-robustness.sh:1085`, `tests/robustness/test-setup-scripts-robustness.sh:1113`, `tests/robustness/test-setup-scripts-robustness.sh:1142`).
- dependencies_and_callers: This test targets `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`. It relies on `tests/test-helpers.sh` for counters and temp cleanup, `scripts/portable-timeout.sh` for timeout abstraction, and git for repository setup. It uses mock binaries to isolate validation paths and avoid depending on real Codex/GitHub behavior.
- edge_cases_or_failure_modes: The test documents current behavior that `--codex-timeout 0` is accepted because the regex is non-negative numeric despite the error text saying positive integer (`tests/robustness/test-setup-scripts-robustness.sh:749`). Very large timeout values are accepted (`tests/robustness/test-setup-scripts-robustness.sh:781`). YAML-unsafe branch names may be skipped if the installed git refuses to create such a branch (`tests/robustness/test-setup-scripts-robustness.sh:374`). Missing dependency tests may skip if system `codex` or `jq` is still discoverable despite restricted PATH (`tests/robustness/test-setup-scripts-robustness.sh:1106`, `tests/robustness/test-setup-scripts-robustness.sh:1135`). The timeout simulation uses a mock `timeout`/`gtimeout` returning `124` for `git rev-parse`, and the test accepts either a timeout-specific message or any non-zero failure (`tests/robustness/test-setup-scripts-robustness.sh:817`, `tests/robustness/test-setup-scripts-robustness.sh:846`).
- validation_or_tests: This file is itself the assigned validation artifact. Its assertions map directly to setup-script code: RLCR argument parsing in `scripts/setup-rlcr-loop.sh:151`, dependency reporting in `scripts/setup-rlcr-loop.sh:300`, mutual exclusion in `scripts/setup-rlcr-loop.sh:324`, plan validation in `scripts/setup-rlcr-loop.sh:421`, content validation in `scripts/setup-rlcr-loop.sh:584`, YAML/model safety in `scripts/setup-rlcr-loop.sh:654`, and skip-impl state creation in `scripts/setup-rlcr-loop.sh:878`. PR-loop argument and prerequisite checks map to `scripts/setup-pr-loop.sh:111`, bot validation at `scripts/setup-pr-loop.sh:180`, mutual exclusion at `scripts/setup-pr-loop.sh:220`, and git prerequisite checks at `scripts/setup-pr-loop.sh:243`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`