# agent_11 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the repository’s executable specification layer for the Humanize plugin’s core control loops: RLCR, PR loop, hook enforcement, template rendering, config/model routing, Codex review handoff, monitor behavior, and API polling/fetching. Recursive inspection found 74 files across `tests/`, `tests/robustness/`, `tests/fixtures/`, and `tests/mocks/`.
- algorithmic_behavior: The central runner [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:59) declares the suite list and runs it in parallel with `HUMANIZE_TEST_JOBS` throttling, per-suite temp output, zsh routing for zsh-only tests, and pass/fail aggregation. It strips ANSI color before extracting `Passed:` and `Failed:` counts, which is directly specified by [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-ansi-parsing.sh:38). The shared helper [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-helpers.sh:30) provides `pass`, `fail`, `skip`, summary printing, temp-directory cleanup, and mock git repo creation.
- inputs_outputs_state: Inputs are shell scripts, hook JSON payloads, generated `.humanize/rlcr` and `.humanize/pr-loop` state trees, mock `gh`/`codex` binaries, fixture JSON, and temporary git repositories. Outputs are process exit codes, per-test `PASS`/`FAIL` counters, generated loop files, markdown summaries, and runner aggregate status. Many suites create isolated temp directories with `mktemp -d`; the main runner stores suite output in a temp output directory and removes it on exit at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:127).
- gates_or_invariants: Directory-level invariants include isolated test state, no shared mutable repo state during parallel execution, portable shell behavior across GNU/BSD tools, correct hook fail-open/fail-closed semantics, strict `.humanize` protection, valid YAML frontmatter for state files, and stable pass/fail summary format. The runner treats nonzero suite exit or any parsed failed count as suite failure at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:247).
- dependencies_and_callers: Tests depend on `bash`, sometimes `zsh`, `git`, `jq`, `sed`, mocked `gh`, and repository scripts under `hooks/`, `scripts/`, `commands/`, `prompt-template/`, and `config/`. `tests/mocks/gh` is a fixture-backed GitHub CLI mock; `tests/setup-fixture-mock-gh.sh` emits a mock `gh` for PR fetch/poll tests. The robustness subtree delegates to production code, for example `test-pr-loop-api-fetch.sh` sources `test-pr-loop-api-robustness.sh`.
- edge_cases_or_failure_modes: Covered edge classes include malformed hook JSON, stale/concurrent state files, corrupt YAML frontmatter, wrong round files, protected prompt/state/todos edits, path traversal/symlink/absolute path issues, missing templates, bad config JSON, missing tools, Git detached/dirty states, API rate limits/network errors, Unicode/long PR comments, monitor deletion/SIGINT, zsh glob safety, and timeout fallback behavior. Fixture naming uses both hyphen and underscore variants such as `issue-comments.json` and `issue_comments.json`, matching different mock paths.
- validation_or_tests: The directory is itself the validation suite. Representative children: RLCR validators (`test-allowlist-validators.sh`, `test-bash-validator-patterns.sh`, `test-finalize-phase.sh`), PR loop (`test-pr-loop*.sh`, robustness PR API split tests), monitor (`test-monitor-*`, `test-skill-monitor.sh`), templates (`test-template-*`, `test-templates-comprehensive.sh`), config/model routing (`test-config-*`, `test-model-router.sh`, `test-unified-codex-config.sh`), and one-shot skill execution (`test-ask-codex.sh`). I did not execute the suite; this pass was read-only research.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-041 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: `hooks/loop-edit-validator.sh` is a Claude `PreToolUse` gate for the `Edit` tool. It prevents manual edits to generated RLCR and PR-loop control files, preserving the loop state machine’s authority boundaries.
- algorithmic_behavior: The script reads hook JSON from stdin, extracts `.tool_name`, and exits immediately unless the tool is `Edit` at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/hooks/loop-edit-validator.sh:24). For `Edit`, it lowercases `file_path`, extracts `session_id`, blocks generic `round-*-todos.md` unless active-loop allowlisted, blocks `round-*-prompt.md`, then applies PR-loop protections before RLCR protections. Inside RLCR, it locates the session-aware active loop, resolves `finalize-state.md` before `state.md`, strictly parses the active state, blocks state/finalize-state/plan backup edits, blocks `goal-tracker.md` after round 0, and validates summary-file round numbers.
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name`, `tool_input.file_path`, and optionally `session_id`. Output is exit `0` to allow, exit `2` to block with a rendered user-facing message, or exit `1` for malformed state safety failure. It reads `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, plus `.humanize/pr-loop/<timestamp>/state.md` when relevant. It does not mutate state.
- gates_or_invariants: Key invariants are: only `Edit` is governed; prompt files are read-only; state files are system-managed; PR-loop generated files (`pr-comment`, `prompt`, `codex-prompt`, `pr-check`, `pr-feedback`) are read-only; `round-N-pr-resolve.md` may be edited only for the current PR round; `goal-tracker.md` is editable only during round 0; wrong-round summaries are blocked unless allowlisted. The state parser is fail-closed: malformed active RLCR state blocks the operation at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/hooks/loop-edit-validator.sh:105).
- dependencies_and_callers: It sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/hooks/lib/loop-common.sh:1), relying on `to_lower`, `extract_session_id`, `is_round_file_type`, `find_active_loop`, `is_allowlisted_file`, `parse_state_file_strict`, PR-loop path classifiers, and template-rendered block messages. It is registered in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/hooks/hooks.json:29). Related sibling validators are `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh`.
- edge_cases_or_failure_modes: Missing active loop allows RLCR-path edits after generic prompt/todos checks; malformed state returns exit `1`; `finalize-state.md` is checked before generic `state.md` because the generic matcher would otherwise catch it; allowlisted files are exact-path matches only for the active loop. Invalid JSON is indirectly handled by `jq` under `set -euo pipefail`, so malformed hook input can terminate nonzero.
- validation_or_tests: Covered by [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-allowlist-validators.sh:191), [tests/robustness/test-hook-system-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/robustness/test-hook-system-robustness.sh:6), and template reference checks in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-template-references.sh:61).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-071 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: This is an executable portability spec for parsing colored test output in the central test runner. It protects the runner’s ability to aggregate pass/fail counts from ANSI-colored suite summaries.
- algorithmic_behavior: The test constructs strings with ANSI SGR escape sequences, strips them using `sed "s/${esc}\\[[0-9;]*m//g"`, then extracts numeric counts with `grep -oE`. It covers basic color removal, multiple colors per line, `Passed:` count extraction, `Failed:` count extraction, zero counts, multi-line suite-summary parsing, plain text without ANSI, and combined bold/color sequences.
- inputs_outputs_state: Inputs are hard-coded shell strings containing ANSI escapes and plain text. Outputs are local `PASS`/`FAIL` lines, `TESTS_PASSED`/`TESTS_FAILED` counters, and exit `0` when all assertions pass or exit `1` otherwise. It has no persistent state and no external fixtures.
- gates_or_invariants: The invariant is that the runner’s sed expression must work on both GNU/Linux and BSD/macOS sed using `$'\033'` ANSI-C quoting. Count extraction must return the last numeric value on `Passed:` or `Failed:` lines and must not be confused by color codes.
- dependencies_and_callers: It depends on `bash`, `sed`, `grep`, and shell ANSI-C quoting. Its behavior corresponds directly to [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:247), where the central runner strips ANSI before parsing suite totals. The runner includes it in `TEST_SUITES` at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:71).
- edge_cases_or_failure_modes: It catches non-portable sed escaping, multiple escape sequences in one line, bold-plus-color SGR lists such as `1;32m`, zero failed counts, no-color output, and multi-line summaries where the relevant count appears among other colored text.
- validation_or_tests: The file is self-validating. It prints a summary at [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-ansi-parsing.sh:160) and exits according to `TESTS_FAILED`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-101 `file` `tests/test-skill-monitor.sh`
- cursor: `[_]`
- core_role: This is the executable spec for `_humanize_monitor_skill --once`, the monitor for one-shot `ask-codex` skill invocations stored under `.humanize/skill`.
- algorithmic_behavior: The test creates temporary git repos, sources [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/scripts/humanize.sh:1), synthesizes invocation directories, and calls `_humanize_monitor_skill --once`. It verifies missing directory errors, empty directory errors, aggregate counts, focused invocation selection, metadata display, output rendering, recent invocation listing, first-line question extraction, running detection via missing `metadata.md`, `empty_response` handling, and filtering of non-timestamp directories.
- inputs_outputs_state: Inputs are mock `.humanize/skill/<timestamp-id>/input.md`, `metadata.md`, and `output.md` files. `metadata.md` contains YAML fields such as `model`, `effort`, `duration`, `status`, and `started_at`. Outputs are captured monitor text and exit codes. The monitor state machine classifies invocations into `success`, `error`, `timeout`, `empty_response`, and `running`.
- gates_or_invariants: Valid invocation directories must start with `YYYY-MM-DD_HH-MM-SS`; non-matching directories are ignored. Missing `.humanize/skill` and empty skill directories must return nonzero. A missing `metadata.md` means `running`. `--once` should focus the newest invocation with watchable content, falling back to the newest directory if needed. Multi-line questions are reduced to the first non-empty question line.
- dependencies_and_callers: The production implementation is [scripts/lib/monitor-skill.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/scripts/lib/monitor-skill.sh:15), sourced by `scripts/humanize.sh`. It uses monitor helpers such as `monitor_get_yaml_value` and `humanize_split_to_array`. The test depends on `git`, temporary directories, and local shell sourcing. The runner includes it at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:99).
- edge_cases_or_failure_modes: It covers no skill directory, no invocations, mixed terminal statuses, a running invocation with no metadata, empty responses with no output, junk directories inside `.humanize/skill`, and multi-line question bodies that should not leak extra context into the one-line summary.
- validation_or_tests: The test has its own counters and exits nonzero if any assertion fails at [tests/test-skill-monitor.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-skill-monitor.sh:395). It validates production output strings from the monitor’s `--once` branch at [scripts/lib/monitor-skill.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/scripts/lib/monitor-skill.sh:268).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-131 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: This block template defines a stop/exit gate message for the large-file invariant. It instructs the agent that loop exit is blocked while files exceed the configured line limit.
- algorithmic_behavior: The template renders `{{MAX_LINES}}` and `{{LARGE_FILES}}`, explains why oversized files are a maintainability problem, and prescribes different remediation paths for code files and documentation files. For code, it requires splitting into smaller modules with unchanged behavior; for docs, it requires logical sectioning plus updated navigation/cross-references.
- inputs_outputs_state: Inputs are template variables `MAX_LINES` and `LARGE_FILES`. Output is markdown shown to the agent as a blocking reason. It does not maintain state itself, but it participates in a state transition by preventing “stop” or “complete” progression until the large-file condition is cleared.
- gates_or_invariants: The invariant is each file must be below `MAX_LINES`; functionality must remain strictly unchanged after code splitting; documentation splits must preserve cross-references and narrative flow; the agent must commit the split and attempt exit again.
- dependencies_and_callers: It is consumed through the shared template loader (`load_template` / `load_and_render_safe`) used by hooks. It belongs to `prompt-template/block`, alongside other block messages for protected state, prompt, todos, and goal-tracker operations.
- edge_cases_or_failure_modes: If `{{LARGE_FILES}}` is empty or malformed, the message would lack actionable file names. If `MAX_LINES` is too low, this gate can force mechanical fragmentation. The template recommends optional `code-simplifier` use only if installed, so missing plugin support should not block remediation.
- validation_or_tests: Template existence/rendering is covered by template suites such as [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-template-references.sh:1) and [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-templates-comprehensive.sh:1). This file itself is declarative, not executable.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-161 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: This Claude prompt template drives the RLCR review-fix phase after Codex review finds issues. It defines the transition contract from Codex findings back to Claude remediation.
- algorithmic_behavior: The template injects `{{REVIEW_CONTENT}}` as the review result body and `{{SUMMARY_FILE}}` as the required summary output path. It instructs Claude to read `.humanize/bitlesson.md`, run `bitlesson-selector` for each fix task, address all `[P0-9]` severity findings, avoid unrelated changes, commit fixes, and write a summary. It also states that `COMPLETE` has no effect in review phase.
- inputs_outputs_state: Inputs are Codex review content and the next round summary-file path. Outputs are Claude’s fixes, a commit, and a written summary describing fixed/unresolved findings. In the broader loop, this template creates the next `round-N-prompt.md` during review phase; the expected state transition is “review findings present” to “Claude fix summary ready for another Codex review.”
- gates_or_invariants: All `[P0-9]` issues must be addressed; scope is fixes only; new features and unrelated changes are disallowed; summary writing is mandatory; Codex performs another review after the summary. The template explicitly prevents bypassing review by sending `COMPLETE`.
- dependencies_and_callers: It is rendered by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/hooks/loop-codex-stop-hook.sh:1292), which writes the next review-fix prompt and appends task-tag routing. It depends on the template loader and on the Codex review detector that identifies `[P0-9]` findings.
- edge_cases_or_failure_modes: If review content contains false positives, the prompt still requires addressing or explaining unresolved issues. If `SUMMARY_FILE` is wrong, Claude may write evidence to the wrong round. If bitlesson tooling is unavailable, the instruction creates an operational prerequisite that may need graceful handling elsewhere.
- validation_or_tests: Review-phase behavior is covered by [tests/test-codex-review-merge.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/test-codex-review-merge.sh:1), template reference validation, and comprehensive template rendering tests. The stop hook’s render site is the primary caller-level validation surface.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-191 `file` `tests/robustness/test-pr-loop-api-fetch.sh`
- cursor: `[_]`
- core_role: This is a parallel split wrapper for the PR-loop API robustness suite. It runs the fetch/state half of the PR loop API tests, covering active PR-loop state detection and `fetch-pr-comments.sh` behavior under API and JSON edge cases.
- algorithmic_behavior: The file sets strict shell mode, resolves its directory, sources [tests/robustness/test-pr-loop-api-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/robustness/test-pr-loop-api-robustness.sh:1), invokes `run_fetch_tests`, prints a named summary, and exits with the summary status. It contains no test cases directly; it delegates to the shared library so fetch and poll halves can run independently in parallel.
- inputs_outputs_state: Inputs are the shared robustness library, shared test helper state, temporary repos, generated PR-loop state files, and mocked `gh` binaries. Outputs are the pass/fail summary titled `PR Loop API Fetch Tests` and the shell exit code from `print_test_summary`.
- gates_or_invariants: The wrapper invariant is split-suite fidelity: it must run only tests 1-11 from the shared robustness suite, not the poll/stop-hook group. The delegated fetch tests assert active PR loop detection, YAML-list state acceptance, missing `pr_number` tolerance, empty comments output, API rate-limit/network handling, Claude/Codex bot formatting, multiple bot responses, Unicode, and long comment bodies.
- dependencies_and_callers: It depends on `test-pr-loop-api-robustness.sh`, which sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates mock `gh`, and calls [scripts/fetch-pr-comments.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/scripts/fetch-pr-comments.sh:1). The central runner includes this split at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/run-all-tests.sh:118).
- edge_cases_or_failure_modes: Because it sources the shared robustness file, any top-level initialization in that file runs before `run_fetch_tests`; this is intentional but means the wrapper is not independent of shared globals like `TEST_DIR`. Failures can come from missing `jq`, broken mock `gh`, incorrect repo detection, API retry semantics, JSON parsing errors, or formatted markdown not containing expected bot/severity markers.
- validation_or_tests: Delegated test cases are defined in `run_fetch_tests` at [tests/robustness/test-pr-loop-api-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/tests/robustness/test-pr-loop-api-robustness.sh:283). The production fetch script retries failed API endpoints, writes markdown output, deduplicates by ID, filters by timestamp, sorts human comments before bot comments, and warns on partial API failure at [scripts/fetch-pr-comments.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/do-not-wish-coding/scripts/fetch-pr-comments.sh:183).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `DO_NOT_WISH_CODING-HZ-011`, `DO_NOT_WISH_CODING-HZ-041`, `DO_NOT_WISH_CODING-HZ-071`, `DO_NOT_WISH_CODING-HZ-101`, `DO_NOT_WISH_CODING-HZ-131`, `DO_NOT_WISH_CODING-HZ-161`, `DO_NOT_WISH_CODING-HZ-191`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`