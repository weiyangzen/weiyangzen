# agent_26 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-026 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Repository relevance policy agent for the `gen-plan` command. It gates whether a user draft should become a generated plan by deciding if the draft is semantically connected to the current repository.
- algorithmic_behavior: The prompt instructs the subagent to quickly inspect repository context, then classify the provided draft as either relevant or not relevant. The exploration targets README/CLAUDE docs, directory structure, technologies, languages, purpose, existing paths, functions, and features (`agents/draft-relevance-checker.md:14-25`). The decision rule is intentionally lenient: semantic connection is enough, rough or multilingual drafts are acceptable, and doubt should resolve to relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft document content plus read-only repository exploration through `Read`, `Glob`, and `Grep` declared in frontmatter (`agents/draft-relevance-checker.md:1-6`). Output must be exactly one verdict prefix with a short explanation: `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:27-30`). It has no persistent state; it is a prompt contract.
- gates_or_invariants: The invariant is a two-state classifier with strict output prefixes. The relevance threshold is deliberately permissive, so the gate blocks only drafts that are clearly unrelated. The `commands/gen-plan.md` flow stops on `NOT_RELEVANT` and proceeds to create the output plan on `RELEVANT` (`commands/gen-plan.md:142-164`).
- dependencies_and_callers: Called by `commands/gen-plan.md` via the Task tool as `humanize:draft-relevance-checker` using the `haiku` model (`commands/gen-plan.md:148-156`). Structural expectations are covered in `tests/test-gen-plan.sh`, including file existence, frontmatter name, model, tools, valid naming, and English-only content (`tests/test-gen-plan.sh:111-116`, `tests/test-gen-plan.sh:254-287`, `tests/test-gen-plan.sh:372-375`, `tests/test-gen-plan.sh:421-427`, `tests/test-gen-plan.sh:523-552`).
- edge_cases_or_failure_modes: Because the gate is lenient, borderline drafts may pass and produce a plan even with weak repository relevance. Because repository exploration is “quick,” a relevant draft that only matches obscure files could be missed if the agent does not inspect enough context. The prompt supports informal and multilingual drafts, but the output examples and tests expect English metadata/content.
- validation_or_tests: Existing validation is static/frontmatter-oriented in `tests/test-gen-plan.sh`; it does not prove classifier quality. I did not execute tests because this run requested read-only research notes only.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-056 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Main shell entrypoint for `humanize monitor ...`, with the core runtime monitor implementations for RLCR, PR loop, and skill-monitor dispatch. It converts loop/session files, cached logs, git state, terminal dimensions, and signal events into a live terminal status UI.
- algorithmic_behavior: The file sources shared monitor utilities and loop constants at startup (`scripts/humanize.sh:6-16`). Public helpers split pipe-delimited records portably across bash/zsh (`scripts/humanize.sh:22-34`), summarize RLCR goal trackers (`scripts/humanize.sh:36-120`), detect special git states (`scripts/humanize.sh:122-170`), and parse git status/diff stats (`scripts/humanize.sh:172-218`). `_humanize_monitor_codex` is the largest state machine: it finds the latest `.humanize/rlcr` session, maps that session to cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, selects the latest Codex run/review log, rejects inconsistent run/review round ordering, parses state metadata, draws the fixed status bar, tails log increments, responds to resize, switches sessions/logs, handles deleted/truncated files, and restores the terminal on stop (`scripts/humanize.sh:227-1103`). `_humanize_monitor_pr` provides the PR-loop monitor, with `--once` status mode and continuous tail mode for `round-*-pr-check.md`, `round-*-pr-feedback.md`, and `round-*-pr-comment.md` (`scripts/humanize.sh:1158-1587`).
- inputs_outputs_state: Inputs are `.humanize/rlcr` or `.humanize/pr-loop` session directories, `state.md` or stop-state files, `goal-tracker.md`, cache log files, current git worktree status, terminal `tput` dimensions, shell type, `HOME`/`XDG_CACHE_HOME`, and CLI args. Outputs are terminal escape sequences, status bars, tailed log content, user-facing stop/wait messages, and nonzero returns for missing loop/session preconditions. Internal state includes `current_session_dir`, `current_file`, `last_size`, `file_size`, `monitor_running`, `cleanup_done`, `tail_pid`/`TAIL_PID`, `resize_needed`, and cached no-log status (`scripts/humanize.sh:670-775`, `scripts/humanize.sh:1392-1395`).
- gates_or_invariants: RLCR monitoring requires `.humanize/rlcr` to exist and at least one timestamp-named session before it can run (`scripts/humanize.sh:238-246`, `scripts/humanize.sh:737-746`). Log lookup requires an explicit session to avoid cross-session leakage (`scripts/humanize.sh:263-267`). Codex run logs and review logs are guarded so implementation-phase round numbers must be lower than review-phase round numbers when both exist (`scripts/humanize.sh:330-337`). Terminal height must be at least status-bar height plus content space; too-small terminals enter a wait-for-resize loop (`scripts/humanize.sh:600-627`, `scripts/humanize.sh:754-762`). Cleanup is idempotent and resets traps plus scroll region before printing stop messages (`scripts/humanize.sh:677-715`). PR monitor maps `approve-state.md` and `maxiter-state.md` to display-friendly statuses (`scripts/humanize.sh:1238-1255`).
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for session/state/file-size/PR phase helpers (`scripts/humanize.sh:6-10`, `scripts/humanize.sh:245-247`, `scripts/humanize.sh:343-345`, `scripts/humanize.sh:1189-1191`, `scripts/humanize.sh:1238-1244`) and on `hooks/lib/loop-common.sh` for defaults such as `DEFAULT_CODEX_MODEL` (`scripts/humanize.sh:12-16`, `scripts/humanize.sh:1282`). It dispatches through `humanize monitor rlcr|pr|skill` (`scripts/humanize.sh:1105-1151`) and sources `scripts/lib/monitor-skill.sh` if present (`scripts/humanize.sh:1589-1592`). User-facing callers are documented in `README.md`, `docs/usage.md`, and install docs; tests call functions directly and through `humanize monitor`.
- edge_cases_or_failure_modes: Missing `.humanize/rlcr` or `.humanize/pr-loop` returns an error rather than waiting (`scripts/humanize.sh:238-243`, `scripts/humanize.sh:1182-1187`). Missing RLCR log enters a polling/no-log display and can switch sessions when a newer session appears (`scripts/humanize.sh:822-935`). Deleted loop directories stop gracefully, while deleted session/log files either switch to the latest session or wait for new sessions (`scripts/humanize.sh:779-783`, `scripts/humanize.sh:839-843`, `scripts/humanize.sh:1014-1046`). Truncated logs reset to log discovery (`scripts/humanize.sh:996-1006`). The state parsers are grep/sed based and tolerate missing fields by emitting `N/A` or defaults; malformed numeric fields can still leak into arithmetic contexts such as review round math. Date conversion uses GNU `date -d` with fallback to raw timestamp, so macOS displays may keep UTC text (`scripts/humanize.sh:430-438`).
- validation_or_tests: Direct executable coverage exists in `tests/test-monitor-e2e-real.sh`, plus monitor references in `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-skill-monitor.sh`, and robustness monitor tests. I inspected but did not execute tests because this run requested research notes only.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-086 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Real end-to-end executable specification for the monitor state machines in `scripts/humanize.sh`. It verifies runtime behavior around deletion, signal handling, zsh/bash compatibility, terminal restoration, and glob-safety.
- algorithmic_behavior: The harness creates isolated `/tmp/test-monitor-e2e-real-$$` projects, synthetic `.humanize` session state, goal trackers, fake cache logs, and shimmed `tput`/`clear` functions, then sources the real `scripts/humanize.sh` and runs actual monitor functions in child scripts (`tests/test-monitor-e2e-real.sh:43-51`, `tests/test-monitor-e2e-real.sh:60-158`). It tracks pass/fail counters with `pass` and `fail` helpers and exits based on aggregate failures (`tests/test-monitor-e2e-real.sh:25-37`, `tests/test-monitor-e2e-real.sh:1002-1038`).
- inputs_outputs_state: Inputs are the repository’s real monitor scripts, optional `zsh`, `/tmp` writable space, fake `HOME`/`XDG_CACHE_HOME`, synthetic state files, synthetic goal trackers, and process signals. Outputs are colored PASS/FAIL lines, diagnostic failure text, and final exit status. Runtime state is mostly temp directories, monitor PIDs, output capture files, wait counters, and fake cache directories.
- gates_or_invariants: The tests assert monitors exit after `.humanize/rlcr` deletion, show a graceful stop message, mention directory deletion, avoid zsh/bash glob errors, restore terminal/scroll-region behavior, and return `EXIT_CODE:0` for graceful RLCR deletion (`tests/test-monitor-e2e-real.sh:164-227`). Zsh deletion repeats the same invariants when zsh is available (`tests/test-monitor-e2e-real.sh:238-370`). SIGINT tests require either cleanup messages or clean exit indicators and no glob errors (`tests/test-monitor-e2e-real.sh:492-549`, `tests/test-monitor-e2e-real.sh:649-683`). PR monitor tests cover `--once` and continuous monitor interruption (`tests/test-monitor-e2e-real.sh:691-829`, `tests/test-monitor-e2e-real.sh:834-997`).
- dependencies_and_callers: Depends on `scripts/humanize.sh`, which in turn sources `scripts/lib/monitor-common.sh`; child runner scripts set `PROJECT_ROOT` and source the real file (`tests/test-monitor-e2e-real.sh:144-148`, `tests/test-monitor-e2e-real.sh:312-316`, `tests/test-monitor-e2e-real.sh:464-468`, `tests/test-monitor-e2e-real.sh:780-797`, `tests/test-monitor-e2e-real.sh:923-928`). Wrapper tests `tests/test-monitor-e2e-deletion.sh` and `tests/test-monitor-e2e-sigint.sh` source this file.
- edge_cases_or_failure_modes: Zsh tests skip when `zsh` is unavailable (`tests/test-monitor-e2e-real.sh:238-240`, `tests/test-monitor-e2e-real.sh:560-562`). Some signal delivery cases are treated as pass after forced termination because SIGINT delivery to background shells/process groups is unreliable in the harness (`tests/test-monitor-e2e-real.sh:505-515`, `tests/test-monitor-e2e-real.sh:957-968`). The PR deletion case uses `humanize monitor pr --once`, so it primarily validates clean one-shot behavior rather than long-running directory-deletion detection (`tests/test-monitor-e2e-real.sh:788-799`). The tests intentionally create and remove files under `/tmp`, so they are not pure read-only checks.
- validation_or_tests: This file is itself validation. It is not a unit stub; comments explicitly say it runs the real `_humanize_monitor_codex` (`tests/test-monitor-e2e-real.sh:3-12`). I did not execute it during this research-only run.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-116 `file` `prompt-template/block/bitlesson-delta-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the BitLesson stop-gate when a round summary lacks the required `## BitLesson Delta` section.
- algorithmic_behavior: The template emits a concise failure reason and a minimal required markdown shape: `## BitLesson Delta`, `Action: none|add|update`, `Lesson ID(s): <IDs or NONE>`, and `Notes: <what changed and why>` (`prompt-template/block/bitlesson-delta-missing.md:1-12`). It is static and has no variable placeholders.
- inputs_outputs_state: Input is just template loading; no runtime variables are required. Output is rendered markdown used as the block reason. The surrounding validator wraps it into JSON with `decision: block`, `reason`, and `systemMessage` (`scripts/bitlesson-validate-delta.sh:78-90`).
- gates_or_invariants: `scripts/bitlesson-validate-delta.sh` checks the summary with a case-insensitive heading regex for `## BitLesson Delta`; if absent, it loads this template and blocks the stop hook (`scripts/bitlesson-validate-delta.sh:92-108`). The invariant enforced by this template is presence of the section and the three minimal fields; downstream script logic separately validates action count, ID consistency, ID format, and knowledge-base existence (`scripts/bitlesson-validate-delta.sh:111-285`).
- dependencies_and_callers: Rendered via `load_and_render_safe` from `hooks/lib/template-loader.sh` inside `scripts/bitlesson-validate-delta.sh` (`scripts/bitlesson-validate-delta.sh:74-77`, `scripts/bitlesson-validate-delta.sh:107`). The BitLesson requirement is also seeded into RLCR setup/prompt flows in `scripts/setup-rlcr-loop.sh` and referenced by `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: If the template file is missing, the validator has an inline fallback with equivalent text (`scripts/bitlesson-validate-delta.sh:93-107`). Because the missing-section check only tests for the heading, summaries with the heading but malformed fields route to other templates. The template gives no dynamic round/file context, so the `systemMessage` carries the round-specific detail.
- validation_or_tests: Template loading is indirectly covered by template-loader/template-reference tests and by BitLesson validator paths. I did not execute validation scripts in this read-only research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-146 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR read validation when an agent tries to read a round prompt/summary from outside the active loop directory.
- algorithmic_behavior: The template tells the actor the attempted `FILE_PATH` is outside the active loop directory, lists the current round prompt and summary paths under `ACTIVE_LOOP_DIR`, and provides a shell read suggestion (`prompt-template/block/wrong-file-location.md:1-9`). It relies on variable substitution for `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`.
- inputs_outputs_state: Inputs are supplied by `hooks/loop-read-validator.sh`: attempted path, active loop directory, and current round parsed from active state (`hooks/loop-read-validator.sh:83-100`, `hooks/loop-read-validator.sh:122-130`). Output is rendered markdown written to stderr before the hook exits with code 2 (`hooks/loop-read-validator.sh:127-131`). The template itself has no state; validator state comes from active loop/session resolution.
- gates_or_invariants: The read validator first ignores non-Read tools, blocks unauthorized todos reads, and only applies this template to `round-*-summary.md` or `round-*-prompt.md` files (`hooks/loop-read-validator.sh:37-74`). It resolves the active RLCR directory, strictly parses state, extracts current round, determines file type, and if the requested round file is not inside `.humanize/rlcr`, blocks with this template (`hooks/loop-read-validator.sh:85-131`). Later gates handle wrong round number and wrong directory path within `.humanize/rlcr` (`hooks/loop-read-validator.sh:138-168`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, path classification, active loop lookup, state parsing, and template loading (`hooks/loop-read-validator.sh:17-19`). Uses `load_and_render_safe` with an inline fallback if this template cannot be loaded (`hooks/loop-read-validator.sh:124-130`). Related sibling templates handle wrong round and wrong directory cases.
- edge_cases_or_failure_modes: The rendered message lists the correct active prompt/summary paths but its final line says to run `cat {{FILE_PATH}}`; for a wrong-location path, that can be interpreted as suggesting the blocked path rather than the current active path. The hook only reaches this template for paths with prompt/summary filename patterns; other wrong files bypass it. If active state is malformed, the hook fails closed before rendering this template (`hooks/loop-read-validator.sh:91-100`).
- validation_or_tests: Template existence and rendering are covered generically by template reference/loader tests; read-validator behavior is covered by hook/allowlist and robustness tests. I inspected references but did not execute tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-176 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor library for RLCR, PR-loop, and skill monitors. It centralizes session discovery, state-file discovery, file sizing, terminal setup/restore, simple frontmatter parsing, PR phase detection, and goal-tracker statistics.
- algorithmic_behavior: Provides ANSI color helpers (`scripts/lib/monitor-common.sh:8-23`), cross-platform file size via GNU/BSD `stat` (`scripts/lib/monitor-common.sh:29-35`), latest timestamp-named session selection without shell globs (`scripts/lib/monitor-common.sh:37-63`), terminal scroll-region setup/restore (`scripts/lib/monitor-common.sh:69-92`), status-to-color mapping (`scripts/lib/monitor-common.sh:128-141`), active/stop state-file detection (`scripts/lib/monitor-common.sh:147-188`), one-key YAML/frontmatter extraction (`scripts/lib/monitor-common.sh:194-209`), timestamp/string formatting (`scripts/lib/monitor-common.sh:215-252`), PR phase detection (`scripts/lib/monitor-common.sh:258-342`), PR phase display text (`scripts/lib/monitor-common.sh:344-381`), RLCR goal tracker parsing (`scripts/lib/monitor-common.sh:387-463`), and PR issue tracker parsing (`scripts/lib/monitor-common.sh:465-498`).
- inputs_outputs_state: Inputs are loop/session directories, markdown state files, goal trackers, PR review/check files, terminal dimensions, and local file metadata. Outputs are pipe-delimited summary strings, status names, display strings, ANSI escape sequences, and file/session paths. It writes one small phase cache file under `/tmp/humanize-phase-<session>-<round>.size` to compare PR check file growth (`scripts/lib/monitor-common.sh:297-307`).
- gates_or_invariants: Session discovery only accepts directory names matching `YYYY-MM-DD_HH-MM-SS`, then picks the lexicographically latest timestamp (`scripts/lib/monitor-common.sh:49-60`). State discovery prioritizes `state.md` as active before any `*-state.md` stop file (`scripts/lib/monitor-common.sh:162-187`). PR phase detection prioritizes final state files, then detects Codex analyzing if the latest `round-*-pr-check.md` is growing or was modified in the last 10 seconds, otherwise uses `current_round` and `startup_case` to distinguish initial review waiting from reviewer polling (`scripts/lib/monitor-common.sh:272-341`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` (`scripts/humanize.sh:6-10`) and used directly by monitor/system tests (`tests/test-pr-loop-system.sh:257-317`, `tests/test-pr-loop-system.sh:770-817`). `scripts/lib/monitor-skill.sh` also depends on common helpers for YAML/timestamp utilities. External commands used include `find`, `stat`, `sed`, `grep`, `sort`, `wc`, `tr`, `tail`, `date`, `tput`, and `cat`.
- edge_cases_or_failure_modes: `monitor_find_state_file` uses the first `find` result for `*-state.md`, so multiple stop-state files have nondeterministic selection (`scripts/lib/monitor-common.sh:168-181`). YAML parsing is line-oriented and does not handle nested or quoted edge cases beyond simple stripping. PR phase detection can briefly classify a recently modified but finished `pr-check` file as `codex_analyzing` because age under 10 seconds is enough (`scripts/lib/monitor-common.sh:308-317`). Stale `/tmp` cache files can affect growth comparisons. Terminal helpers assume usable `tput` and do not guard all failures.
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh` for shared monitor helpers, by `tests/test-monitor-e2e-real.sh` through real monitor execution, and by skill-monitor tests through sourcing. I did not run tests in this research-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 section headings present, matching the 6 assigned rows in order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`