# agent_04 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-004 `directory` `config`
- cursor: `[_]`
- core_role: `config` is the plugin default configuration layer. Recursively it contains only `config/default_config.json`, so the directory’s algorithmic responsibility is to supply required baseline values for model routing, loop defaults, BitLesson selection, agent-team mode, and plan-generation mode.
- algorithmic_behavior: The directory is consumed by `scripts/lib/config-loader.sh`, which resolves `default_config_path="$plugin_root/config/default_config.json"` and treats it as the required layer in the four-layer merge (`{}` + default + user + project) at `scripts/lib/config-loader.sh:77` and `scripts/lib/config-loader.sh:109-132`.
- inputs_outputs_state: Input is the repository/plugin root passed to `load_merged_config`; output is merged JSON emitted on stdout. State is not mutated by the directory itself, but the merged values become runtime defaults in `hooks/lib/loop-common.sh:177-189`.
- gates_or_invariants: `default_config.json` is mandatory: missing or malformed required default config exits non-zero in `_config_loader_prepare_layer` (`scripts/lib/config-loader.sh:40-56`). User/project overlays are optional and malformed optional layers degrade to `{}` with warning (`scripts/lib/config-loader.sh:57-59`).
- dependencies_and_callers: Used by `load_merged_config` callers, including `hooks/lib/loop-common.sh:167-189` and `scripts/bitlesson-select.sh:15-17`; tested by `tests/test-config-merge.sh:36-64`, `tests/test-config-error-handling.sh:37-80`, and `tests/test-unified-codex-config.sh:51-72`.
- edge_cases_or_failure_modes: Directory absence or missing default config is fatal; malformed default JSON is fatal; malformed user/project JSON is ignored with warning. Higher-layer `null` values are stripped before merge so they do not erase lower defaults (`scripts/lib/config-loader.sh:119-131`; `tests/test-config-merge.sh:135-150`).
- validation_or_tests: Config merge tests prove default-only values, overlay precedence, additive merge, null stripping, and `HUMANIZE_CONFIG` project-path override (`tests/test-config-merge.sh:43-193`). Error tests prove missing default is fatal while missing optional configs are not (`tests/test-config-error-handling.sh:48-53`, `tests/test-config-error-handling.sh:139-172`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-034 `file` `config/default_config.json`
- cursor: `[_]`
- core_role: Required default JSON object for the runtime config algorithm. It establishes `codex_model`, `codex_effort`, `bitlesson_model`, `agent_teams`, deprecated `chinese_plan`, and `gen_plan_mode` defaults (`config/default_config.json:2-7`).
- algorithmic_behavior: Loaded as the required layer by `load_merged_config`, then merged beneath user and project config with jq object multiplication after recursive null stripping (`scripts/lib/config-loader.sh:113-132`). `get_config_value` extracts scalar values as strings for shell consumers (`scripts/lib/config-loader.sh:139-158`).
- inputs_outputs_state: Inputs are the JSON keys in this file plus optional user config at `$XDG_CONFIG_HOME/humanize/config.json` or `$HOME/.config/humanize/config.json`, and project config at `.humanize/config.json` or `$HUMANIZE_CONFIG` (`scripts/lib/config-loader.sh:77-88`). Outputs feed `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, and `DEFAULT_CODEX_EFFORT` (`hooks/lib/loop-common.sh:179-189`).
- gates_or_invariants: Must be a JSON object. Default model keys must be `codex_model`/`codex_effort`, not stale `loop_reviewer_*` keys; this is enforced by `tests/test-unified-codex-config.sh:61-72`. Setup validates codex effort against `xhigh|high|medium|low` when used in RLCR state (`scripts/setup-rlcr-loop.sh:666-675`).
- dependencies_and_callers: `codex_model`/`codex_effort` route Codex-backed review/setup/ask flows via `hooks/lib/loop-common.sh:186-189`, `scripts/setup-rlcr-loop.sh:43-44`, `scripts/ask-codex.sh:41-42`, and PR loop setup/stop through config-backed model with PR-specific default effort (`hooks/pr-loop-stop-hook.sh:28-31`, `hooks/pr-loop-stop-hook.sh:170-171`). `bitlesson_model` routes selector provider in `scripts/bitlesson-select.sh:15-17`.
- edge_cases_or_failure_modes: Missing file blocks all merged-config loading. Optional overlays may override defaults, but `null` values do not erase them. Unknown `bitlesson_model` fails selector routing; missing Claude for a Claude model can fall back to Codex, while missing Codex for a GPT model fails (`tests/test-bitlesson-select-routing.sh:176-236`, `tests/test-bitlesson-select-routing.sh:238-273`).
- validation_or_tests: Direct assertions cover `codex_model: gpt-5.4`, `codex_effort: high`, absent reviewer keys (`tests/test-unified-codex-config.sh:61-72`), `bitlesson_model: haiku`, `agent_teams: false`, and non-empty `gen_plan_mode` (`tests/test-config-merge.sh:45-64`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-064 `file` `templates/bitlesson.md`
- cursor: `[_]`
- core_role: Seed template for the project-local BitLesson knowledge base. It defines the strict entry schema and placeholder area used when initializing `.humanize/bitlesson.md` (`templates/bitlesson.md:1-23`).
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` sets `PLUGIN_BITLESSON_TEMPLATE="$SCRIPT_DIR/../templates/bitlesson.md"` and invokes `scripts/bitlesson-init.sh` to copy it into `.humanize/bitlesson.md` only if absent (`scripts/setup-rlcr-loop.sh:819-828`; `scripts/bitlesson-init.sh:83-88`).
- inputs_outputs_state: Inputs are `--project-root`, `--template`, and optional `--bitlesson-relpath`; output is the resolved BitLesson file path on stdout (`scripts/bitlesson-init.sh:5-15`, `scripts/bitlesson-init.sh:88`). State transition is missing knowledge base to initialized `.humanize/bitlesson.md`; existing files are not overwritten (`scripts/bitlesson-init.sh:10-14`, `scripts/bitlesson-init.sh:83-86`).
- gates_or_invariants: Template path must exist, target root must be an existing directory, BitLesson relpath must be relative and cannot contain `..`, and an existing target must be a regular file (`scripts/bitlesson-init.sh:60-80`). The entry schema requires exact field order: Lesson heading, Lesson ID, Scope, Problem Description, Root Cause, Solution, Constraints, Validation Evidence, Source Rounds (`templates/bitlesson.md:5-19`).
- dependencies_and_callers: Runtime selection reads the initialized file in `scripts/bitlesson-select.sh:102-144`. Stop-hook validation requires a `## BitLesson Delta` section when `bitlesson_required` is true and cross-checks declared lesson IDs against `.humanize/bitlesson.md` through `scripts/bitlesson-validate-delta.sh`, invoked at `hooks/loop-codex-stop-hook.sh:720-734`.
- edge_cases_or_failure_modes: Whitespace-only or missing BitLesson file blocks selector execution (`scripts/bitlesson-select.sh:79-109`). Empty knowledge base can still allow `Action: none` unless strict `--require-bitlesson-entry-for-none` disables that (`scripts/setup-rlcr-loop.sh:99-102`, `scripts/bitlesson-validate-delta.sh:170-182`).
- validation_or_tests: Routing tests create mock BitLesson files and verify model/provider selection uses `bitlesson_model` (`tests/test-bitlesson-select-routing.sh:18-27`, `tests/test-bitlesson-select-routing.sh:61-279`). Delta validator references enforce missing/invalid/inconsistent BitLesson Delta behavior through prompt blocks and file ID checks (`scripts/bitlesson-validate-delta.sh:92-133`, `scripts/bitlesson-validate-delta.sh:136-182`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-094 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable behavioral specification for PR-loop validators, PR stop-hook gates, comment/review aggregation, trigger freshness, round-scoped resolution files, and monitor display. It is not production code, but it defines core algorithm contracts for the PR loop.
- algorithmic_behavior: `run_hook_tests()` wraps grouped tests (`tests/test-pr-loop-hooks.sh:15-23`). It creates temporary `.humanize/pr-loop/<timestamp>/state.md` fixtures, mocks `gh`/`git`, sends hook JSON to validators, and runs production scripts such as `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/humanize.sh`.
- inputs_outputs_state: Inputs include hook JSON for `Write`, `Edit`, and `Bash` tool calls (`tests/test-pr-loop-hooks.sh:86-90`, `tests/test-pr-loop-hooks.sh:193-199`, `tests/test-pr-loop-hooks.sh:1515-1520`), PR-loop YAML state files with `configured_bots`, `active_bots`, `current_round`, trigger fields, and mocked GitHub API responses. Outputs are pass/fail counters plus hook block decisions, stderr logs, generated comment markdown, and poll JSON.
- gates_or_invariants: Protects PR-loop `state.md` and read-only `round-N-pr-comment.md` from writes (`tests/test-pr-loop-hooks.sh:68-130`), allows only current-round `round-N-pr-resolve.md` writes/edits (`tests/test-pr-loop-hooks.sh:135-163`, `tests/test-pr-loop-hooks.sh:1431-1535`), requires resolve summaries before stop (`tests/test-pr-loop-hooks.sh:766-840`), requires current-user trigger detection and refresh rules (`tests/test-pr-loop-hooks.sh:538-696`), and recognizes final `APPROVE` / `WAITING_FOR_BOTS` markers (`tests/test-pr-loop-hooks.sh:415-457`).
- dependencies_and_callers: Exercises `hooks/loop-write-validator.sh:77-96`, `hooks/loop-edit-validator.sh:60-79`, PR helper functions `find_active_pr_loop`, `is_pr_loop_readonly_file`, and `validate_pr_resolve_round` in `hooks/lib/loop-common.sh:963-1051`, and stop-hook logic in `hooks/pr-loop-stop-hook.sh:258-880`.
- edge_cases_or_failure_modes: Covers bot YAML list parsing and backward-compatible configured/active bot separation (`tests/test-pr-loop-hooks.sh:288-324`, `tests/test-pr-loop-hooks.sh:698-723`), previously approved bot re-add when new issues appear (`tests/test-pr-loop-hooks.sh:353-388`), pagination for trigger detection (`tests/test-pr-loop-hooks.sh:947-1049`), round 0 preferring `last_trigger_at` over `started_at` (`tests/test-pr-loop-hooks.sh:1052-1141`), approval-only PR reviews with empty bodies (`tests/test-pr-loop-hooks.sh:1160-1219`), and empty active bot monitor display (`tests/test-pr-loop-hooks.sh:1596-1621`).
- validation_or_tests: The file is itself the validation harness. It also verifies fixture-backed fetch/poll outputs include issue comments, inline review comments, approval placeholders, valid JSON, bot response lists, and `--after` filtering (`tests/test-pr-loop-hooks.sh:1231-1419`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-124 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Block-response template for the RLCR stop hook when tracked/staged/untracked non-local work makes git dirty. It defines the transition from attempted exit to “blocked until commit” (`prompt-template/block/git-not-clean.md:1-16`).
- algorithmic_behavior: Rendered by `hooks/loop-codex-stop-hook.sh` after cached git status filtering. The hook excludes untracked `.humanize/` state from dirty determination, adds special notes for `.humanize*` or other untracked files, then renders this template with `GIT_ISSUES` and `SPECIAL_NOTES` (`hooks/loop-codex-stop-hook.sh:584-632`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES` placeholders (`prompt-template/block/git-not-clean.md:3-4`) plus cached `git status --porcelain`. Output is a JSON hook decision `{ "decision": "block", "reason": ..., "systemMessage": ... }` at `hooks/loop-codex-stop-hook.sh:634-641`. No repository state is changed except stale `index.lock` cleanup before blocking (`hooks/loop-codex-stop-hook.sh:619-623`).
- gates_or_invariants: Requires real changes to be staged and committed before exit; instructs adding build artifacts to `.gitignore`; forbids AI authorship and `Co-Authored-By` AI attribution (`prompt-template/block/git-not-clean.md:5-16`). It optionally invokes `code-simplifier` before committing if installed (`prompt-template/block/git-not-clean.md:6`).
- dependencies_and_callers: Loaded through `load_and_render_safe`, whose single-pass rendering prevents placeholder injection and falls back if the template is missing (`hooks/lib/template-loader.sh:50-131`, `hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: Missing template falls back to inline git-not-clean text (`hooks/loop-codex-stop-hook.sh:624-630`). `.humanize/` untracked files are filtered out of the block condition but can still contribute special notes if present (`hooks/loop-codex-stop-hook.sh:590-615`). Non-humanize untracked files trigger artifact review guidance.
- validation_or_tests: Indirectly validated by stop-hook dirty-git flows and template-loader tests; behavior is anchored in the stop hook’s block decision path at `hooks/loop-codex-stop-hook.sh:619-643`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-154 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template used when code review was skipped. It is an algorithmic transition contract: implementation cannot claim full validation, so the next phase requires manual verification, conservative cleanup, commit, and finalize summary.
- algorithmic_behavior: Rendered only when `skip_reason` is non-empty in `hooks/loop-codex-stop-hook.sh`; placeholders include `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:1146-1181`; template placeholders at `prompt-template/claude/finalize-phase-skipped-prompt.md:3`, `:19`, `:37-44`).
- inputs_outputs_state: Input is the skipped-review reason and loop metadata. Output is the next finalize prompt written by the stop hook as part of the finalize transition. The prompt instructs manual verification, test execution, TaskUpdate completion, commit, and writing the finalize summary to the rendered path (`prompt-template/claude/finalize-phase-skipped-prompt.md:7-14`, `:40-50`).
- gates_or_invariants: Non-negotiable constraints: no functionality changes, no existing test failures, no new bugs, and only functionality-equivalent cleanup (`prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`). Final exit requires all tasks completed, committed changes, and summary evidence (`prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`).
- dependencies_and_callers: Uses the same `load_and_render_safe` template loader with fallback text (`hooks/loop-codex-stop-hook.sh:1148-1181`; `hooks/lib/template-loader.sh:167-203`). It references the optional `code-simplifier:code-simplifier` Task agent (`prompt-template/claude/finalize-phase-skipped-prompt.md:15-24`).
- edge_cases_or_failure_modes: If this template is missing or renders empty, the stop hook falls back to inline skipped-finalize text with the same core constraints (`hooks/loop-codex-stop-hook.sh:1148-1175`). Since review was skipped, validation evidence may be manual or partial; the prompt explicitly requires reporting tests “if possible” (`prompt-template/claude/finalize-phase-skipped-prompt.md:49`).
- validation_or_tests: Finalize-phase behavior is covered by stop-hook/finalize tests outside this assigned item; the render branch is directly visible in `hooks/loop-codex-stop-hook.sh:1146-1181`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-184 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR/PR-loop state-file parsing and active-loop detection under malformed files, concurrent reads, atomic writes, stale loop directories, terminal states, and permission errors.
- algorithmic_behavior: Creates temporary state directories with `create_state_file()` (`tests/robustness/test-concurrent-state-robustness.sh:30-50`), sources `hooks/lib/loop-common.sh`, then exercises `get_current_round`, `find_active_loop`, `parse_state_file_strict`, and `find_active_pr_loop` across 22 tests (`tests/robustness/test-concurrent-state-robustness.sh:59-558`).
- inputs_outputs_state: Inputs are synthetic `state.md`, `finalize-state.md`, and `cancel-state.md` files with YAML frontmatter. Outputs are pass/fail counts from `tests/test-helpers.sh:30-78`. State transitions are simulated via atomic temp-file writes followed by `mv` during concurrent reads (`tests/robustness/test-concurrent-state-robustness.sh:279-322`).
- gates_or_invariants: Tolerant `get_current_round` defaults to `0` on unreadable or nonmatching values (`hooks/lib/loop-common.sh:329-342`), while `parse_state_file_strict` requires frontmatter, required fields, numeric round/max iterations, boolean `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:414-485`). `find_active_loop` must check only the newest directory without a session filter to prevent zombie-loop revival (`hooks/lib/loop-common.sh:251-292`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` state helpers, especially `resolve_active_state_file` (`hooks/lib/loop-common.sh:212-226`), `find_active_loop` (`hooks/lib/loop-common.sh:269-327`), `get_current_round` (`hooks/lib/loop-common.sh:332-342`), `parse_state_file_strict` (`hooks/lib/loop-common.sh:423-485`), and `find_active_pr_loop` (`hooks/lib/loop-common.sh:963-979`).
- edge_cases_or_failure_modes: Explicitly covers trailing/leading whitespace, CRLF degradation, empty current_round, inline comments, nonexistent/empty loop bases, finalize-state detection, simultaneous reads, read-during-atomic-write consistency, malformed/missing frontmatter strict failures, Unicode content, YAML bot lists, old stale loop ignored when newer dir lacks state, `cancel-state.md` not active, very large and zero rounds, and permission-denied reads (`tests/robustness/test-concurrent-state-robustness.sh:59-552`).
- validation_or_tests: The file is executable validation and exits with `print_test_summary` status (`tests/robustness/test-concurrent-state-robustness.sh:554-559`). It encodes the expected split between tolerant read helpers and strict safety parsing.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DO_NOT_WISH_CODING-HZ-004, DO_NOT_WISH_CODING-HZ-034, DO_NOT_WISH_CODING-HZ-064, DO_NOT_WISH_CODING-HZ-094, DO_NOT_WISH_CODING-HZ-124, DO_NOT_WISH_CODING-HZ-154, DO_NOT_WISH_CODING-HZ-184
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`