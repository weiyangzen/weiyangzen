# agent_09 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-009 `directory` `skills`
- cursor: `[_]`
- core_role: Recursive inspection found four skill manifests only: `skills/humanize/SKILL.md`, `skills/humanize-rlcr/SKILL.md`, `skills/humanize-gen-plan/SKILL.md`, and `skills/ask-codex/SKILL.md`. The directory is a declarative workflow surface that maps user-invoked skills/flows onto the repository’s RLCR, PR-loop, plan-generation, and one-shot Codex consultation algorithms.
- algorithmic_behavior: `humanize` describes the umbrella algorithm: RLCR implementation then review phases at `skills/humanize/SKILL.md:31`, PR-loop remote-review handling at `skills/humanize/SKILL.md:49`, and structured plan generation at `skills/humanize/SKILL.md:61`. `humanize-rlcr` makes skill mode hook-equivalent by requiring `scripts/rlcr-stop-gate.sh`, which executes the real stop-hook path, at `skills/humanize-rlcr/SKILL.md:10` and `skills/humanize-rlcr/SKILL.md:17`. `humanize-gen-plan` defines the plan synthesis state machine in its flowchart at `skills/humanize-gen-plan/SKILL.md:18`. `ask-codex` is a bounded expert-call wrapper around `scripts/ask-codex.sh` with shell-quoting constraints at `skills/ask-codex/SKILL.md:14`.
- inputs_outputs_state: Inputs are plan paths, flags, Codex model/effort/timeouts, bot selections, and free-form questions. Outputs are `.humanize/rlcr/<timestamp>/state.md`, prompts, summaries, review results, finalize/complete state, `.humanize/pr-loop/<timestamp>/state.md`, PR resolution files, and `.humanize/skill/<timestamp>/output.md`, documented at `skills/humanize/SKILL.md:217` and `skills/ask-codex/SKILL.md:55`.
- gates_or_invariants: RLCR skill mode must run setup, implement, commit, write the summary, and then run the stop gate; gate exit `0` permits exit, `10` continues the loop, and `20` is infrastructure failure at `skills/humanize-rlcr/SKILL.md:41` and `skills/humanize-rlcr/SKILL.md:61`. Critical invariants include never manually editing state files, never skipping blocked gates, and never replacing the gate with ad-hoc Codex calls at `skills/humanize-rlcr/SKILL.md:85`.
- dependencies_and_callers: The skills coordinate with `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and the hook stack. Runtime paths are hydrated through `{{HUMANIZE_RUNTIME_ROOT}}` in `skills/humanize/SKILL.md:11`, `skills/humanize-rlcr/SKILL.md:19`, and `skills/humanize-gen-plan/SKILL.md:12`.
- edge_cases_or_failure_modes: `ask-codex` explicitly rejects unsafe unquoted `$ARGUMENTS` expansion because user text may contain shell metacharacters at `skills/ask-codex/SKILL.md:22` and `skills/ask-codex/SKILL.md:30`. `humanize-gen-plan` must stop on invalid input/output paths, irrelevant drafts, unresolved inconsistencies, output file collisions, and missing templates, with exit codes listed at `skills/humanize-gen-plan/SKILL.md:95`. `humanize-rlcr` treats malformed runtime/hook state as infrastructure failure rather than completion at `skills/humanize-rlcr/SKILL.md:63`.
- validation_or_tests: The directory itself is not executable, but its contracts are exercised by tests assigned in this run and adjacent suites: agent teams via `tests/test-agent-teams.sh`, PR-loop orchestration via `tests/test-pr-loop.sh`, path/security robustness via `tests/robustness/test-path-validation-robustness.sh`, and ask/plan/config suites listed under `tests/`. I did not run tests because this task is research-only on a read-only branch export.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-039 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: This is the `PreToolUse` Bash gate for both RLCR and PR loops, registered for Bash commands in `hooks/hooks.json:42`. It prevents shell-command bypasses of Write/Edit/Stop protections during active loop state machines.
- algorithmic_behavior: It reads hook JSON from stdin, validates JSON and nesting, ignores non-Bash tools, extracts `.tool_input.command`, locates active RLCR and PR-loop directories, and exits allow-fast when neither loop is active at `hooks/loop-bash-validator.sh:24`, `hooks/loop-bash-validator.sh:38`, and `hooks/loop-bash-validator.sh:59`. During active loops it blocks direct execution of stop hooks/gates at `hooks/loop-bash-validator.sh:78`, push commands when `push_every_round` is false at `hooks/loop-bash-validator.sh:108`, `.humanize` git-add attempts at `hooks/loop-bash-validator.sh:127`, RLCR state/plan/prompt/summary/todos writes, and PR-loop state/read-only-file writes.
- inputs_outputs_state: Input is Claude hook JSON with `tool_name`, `tool_input.command`, and optional session fields. Output is either silent allow with exit `0`, validation failure exit `1`, or block exit `2` with a rendered block template/message on stderr. It does not mutate state; it enforces invariants before Bash execution.
- gates_or_invariants: It fail-closes malformed hook input via `validate_hook_input` and `is_deeply_nested` at `hooks/loop-bash-validator.sh:27` and `hooks/loop-bash-validator.sh:32`. RLCR state file parsing uses strict validation and blocks malformed state at `hooks/loop-bash-validator.sh:94`. It protects `finalize-state.md` before generic `state.md` because the latter pattern also matches finalize state at `hooks/loop-bash-validator.sh:151`.
- dependencies_and_callers: It sources `hooks/lib/loop-common.sh` at `hooks/loop-bash-validator.sh:17`. Key helpers are `validate_hook_input` at `hooks/lib/loop-common.sh:79`, `parse_state_file_strict` at `hooks/lib/loop-common.sh:423`, `git_adds_humanize` at `hooks/lib/loop-common.sh:1095`, and `command_modifies_file` at `hooks/lib/loop-common.sh:1250`. User-facing messages are rendered through helpers that use template loading, including `stop_hook_direct_execution_blocked_message` at `hooks/lib/loop-common.sh:1235`.
- edge_cases_or_failure_modes: The most defensive logic handles `mv`/`cp` of state files from source positions, not only destination writes. It splits chained commands on shell operators, protects redirection placeholders, strips leading redirections, and then checks cleaned segments at `hooks/loop-bash-validator.sh:181` through `hooks/loop-bash-validator.sh:316`. It also detects wrapper payloads like `sh -c` or `bash -c` at `hooks/loop-bash-validator.sh:318`. The todos allowlist is path-restricted to active-loop `round-1-todos.md` or `round-2-todos.md`, preventing same-basename bypasses at `hooks/loop-bash-validator.sh:397`.
- validation_or_tests: Directly covered by PR-loop hook tests for Bash protection at `tests/test-pr-loop-hooks.sh:174`, plan-file hook tests referenced in repository search, allowlist validator tests, finalize-phase tests, and hook input robustness tests. Relevant PR-loop protection expectations are state blocking and read-only prompt/comment blocking at `tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, and `tests/test-pr-loop-hooks.sh:193`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-069 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR `--agent-teams` mode: setup-time feature gating, state serialization/parsing, prompt injection, and stop-hook continuation behavior.
- algorithmic_behavior: The test sources shared helpers and `loop-common.sh` at `tests/test-agent-teams.sh:17`, targets `scripts/setup-rlcr-loop.sh` at `tests/test-agent-teams.sh:29`, and validates that `--agent-teams` requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, records `agent_teams: true`, defaults to false without the flag, and propagates the field through `parse_state_file`.
- inputs_outputs_state: Inputs are disposable git repos, temporary plan files, `CLAUDE_PROJECT_DIR`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, mock Codex executables, synthetic RLCR `state.md`, `goal-tracker.md`, and summary files. Outputs are pass/fail assertions, generated `.humanize/rlcr/.../state.md`, `round-0-prompt.md`, and next-round prompt files such as `round-4-prompt.md`.
- gates_or_invariants: Bad env values `0`, `false`, `yes`, and `true` must be rejected at `tests/test-agent-teams.sh:71`; only exact `1` enables setup, mirroring production validation at `scripts/setup-rlcr-loop.sh:351`. Agent Teams instructions must appear in the initial prompt when enabled at `tests/test-agent-teams.sh:238`, and must not appear without the flag at `tests/test-agent-teams.sh:287`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and templates `prompt-template/claude/agent-teams-instructions.md`, `agent-teams-core.md`, and `agent-teams-continue.md`. Production injection call sites are `scripts/setup-rlcr-loop.sh:1148` and `hooks/loop-codex-stop-hook.sh:1793`.
- edge_cases_or_failure_modes: It explicitly tests missing template/content cases with skip/fail behavior at `tests/test-agent-teams.sh:301`, `tests/test-agent-teams.sh:317`, and `tests/test-agent-teams.sh:339`. Stop-hook tests verify implementation-phase continuation includes Agent Teams text only when `agent_teams=true` and `review_started=false`, while review-phase prompts exclude it even if `agent_teams=true` at `tests/test-agent-teams.sh:536`, `tests/test-agent-teams.sh:573`, and `tests/test-agent-teams.sh:603`.
- validation_or_tests: This file is itself the validation suite. It drives the actual stop hook with mocked `codex exec` feedback and mocked `codex review` issues at `tests/test-agent-teams.sh:496` and `tests/test-agent-teams.sh:514`, then checks generated prompt content and final test summary at `tests/test-agent-teams.sh:636`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-099 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Thin top-level executable test harness for the PR-loop core algorithm. It does not implement PR-loop behavior itself; it initializes the test environment, sources the PR-loop test modules, and runs them in a fixed order.
- algorithmic_behavior: It sets strict shell mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources `tests/test-helpers.sh` and `tests/test-pr-loop-lib.sh`, calls `init_pr_loop_test_env`, then sources `test-pr-loop-scripts.sh`, `test-pr-loop-hooks.sh`, and `test-pr-loop-stophook.sh` at `tests/test-pr-loop.sh:13` through `tests/test-pr-loop.sh:34`. It runs script tests, hook tests, and stop-hook tests at `tests/test-pr-loop.sh:40`.
- inputs_outputs_state: Inputs are the local test modules and mocks created by `init_pr_loop_test_env`, including mocked `gh` and `codex` commands from `tests/test-pr-loop-lib.sh:27` and `tests/test-pr-loop-lib.sh:90`. Output is the aggregate pass/fail summary from `print_test_summary` at `tests/test-pr-loop.sh:53`.
- gates_or_invariants: The harness gate is order and completeness: setup/cancel/fetch/poll tests run before validator and stop-hook integration tests. The strict shell flags at `tests/test-pr-loop.sh:13` make missing modules or failed commands fatal unless a sourced test intentionally handles exits.
- dependencies_and_callers: Depends on `test-pr-loop-lib.sh` for mock setup and summary reporting; that library wires mock binaries into `PATH` at `tests/test-pr-loop-lib.sh:107`. The sourced script module covers `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh` at `tests/test-pr-loop-scripts.sh:5`. The hook module covers validator/comment/fixture/monitor behavior at `tests/test-pr-loop-hooks.sh:3`. The stop-hook module covers force-push detection, triggers, bot timeout, and state management at `tests/test-pr-loop-stophook.sh:3`.
- edge_cases_or_failure_modes: The runner itself has little branching; failures are delegated. Covered edge classes include missing or invalid bot flags, PR-loop state write protection, Bash protection without an RLCR loop, timestamp sorting, trigger filtering, missing trigger blocking, force-push handling, timeout auto-removal, fork base resolution, and mixed bot approval states, as shown by function names in `tests/test-pr-loop-stophook.sh:20` and `tests/test-pr-loop-hooks.sh:539`.
- validation_or_tests: This file is the aggregate validation entry point for PR-loop behavior. I did not execute it because the assignment asks for research notes only and the branch export is read-only; the file’s validation surface is documented by its sourced module calls.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-129 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Block-template contract for the RLCR Round 0 Goal Tracker initialization gate. It tells the agent why the loop cannot exit and exactly what must be initialized before retrying.
- algorithmic_behavior: The template renders the target `{{GOAL_TRACKER_FILE}}`, a generated `{{MISSING_ITEMS}}` list, and required actions to read the file, replace placeholders, define the Ultimate Goal, define 3-7 testable Acceptance Criteria, and populate Active Tasks at `prompt-template/block/goal-tracker-not-initialized.md:5`.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh` after it detects placeholder content in the Goal Tracker. Output is a block reason embedded in the hook’s JSON decision; it does not write the Goal Tracker directly.
- gates_or_invariants: The invariant is that Round 0 is the only time the immutable Goal Tracker section may be set. The template states that after Round 0 the immutable section becomes read-only at `prompt-template/block/goal-tracker-not-initialized.md:16`.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:799`. The hook computes missing Ultimate Goal, Acceptance Criteria, and Active Tasks placeholders at `hooks/loop-codex-stop-hook.sh:760`, `hooks/loop-codex-stop-hook.sh:765`, and `hooks/loop-codex-stop-hook.sh:771`.
- edge_cases_or_failure_modes: If only some sections still contain placeholders, the hook’s generated missing-items list is section-specific at `hooks/loop-codex-stop-hook.sh:779`. If the template is missing, the hook has a minimal fallback block reason at `hooks/loop-codex-stop-hook.sh:795`.
- validation_or_tests: Validation is through the stop-hook Goal Tracker gate: when `MISSING_ITEMS` is non-empty, the hook emits `"decision": "block"` and system message `"Loop: Goal Tracker not initialized in Round 0"` at `hooks/loop-codex-stop-hook.sh:803`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-159 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt fragment appended after a Full Goal Alignment Check to steer the next implementation round toward alignment findings.
- algorithmic_behavior: The fragment tells the agent to focus on forgotten items, Acceptance Criteria marked not met, and unjustified deferrals at `prompt-template/claude/post-alignment-action-items.md:4`.
- inputs_outputs_state: Input is the stop-hook boolean `FULL_ALIGNMENT_CHECK`. Output is appended text in the next-round prompt file when that boolean is true. It does not mutate loop state directly.
- gates_or_invariants: The invariant is behavioral: after an alignment review, the next round must prioritize unresolved goal-alignment findings before ordinary work. The hook appends this fragment before the standard footer and Goal Tracker update request at `hooks/loop-codex-stop-hook.sh:1762`.
- dependencies_and_callers: Loaded with `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"` at `hooks/loop-codex-stop-hook.sh:1764`; if non-empty, appended to `NEXT_PROMPT_FILE` at `hooks/loop-codex-stop-hook.sh:1766`.
- edge_cases_or_failure_modes: Unlike many block templates, this call site has no fallback text; if the template is absent or empty, the post-alignment section is silently omitted because the append is guarded only by `[[ -n "$POST_ALIGNMENT" ]]` at `hooks/loop-codex-stop-hook.sh:1765`.
- validation_or_tests: The validation surface is prompt-generation behavior in the stop hook. No assigned test directly targets this small fragment, but its call site is within the next-round prompt construction path that also appends footers, push notes, Goal Tracker update requests, and Agent Teams continuation at `hooks/loop-codex-stop-hook.sh:1770`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-189 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for plan-file path and content validation in `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The test creates a disposable git repo, installs a mock `codex`, writes valid or invalid plan files, invokes the production setup script through `test_path_validation`, and classifies setup output as accepted or rejected based on production validation error patterns at `tests/robustness/test-path-validation-robustness.sh:76`.
- inputs_outputs_state: Inputs are plan path strings and file contents. Outputs are pass/fail assertions and the final test summary at `tests/robustness/test-path-validation-robustness.sh:486`. The helper clears any existing test RLCR loop before each run with `rm -rf "$TEST_DIR/.humanize/rlcr"` at `tests/robustness/test-path-validation-robustness.sh:83`, so each validation case starts from a clean loop state.
- gates_or_invariants: Valid paths include relative paths, root filenames, dash/underscore, nested directories, dotted filenames, long filenames, and deep nesting at `tests/robustness/test-path-validation-robustness.sh:122`. Invalid paths include absolute paths, spaces, semicolon, pipe, dollar sign, backticks, angle brackets, ampersand, glob characters, question mark, backslash, tilde, parentheses, braces, square brackets, hash, and exclamation mark at `tests/robustness/test-path-validation-robustness.sh:179`. These mirror production gates rejecting absolute paths, whitespace, and shell metacharacters at `scripts/setup-rlcr-loop.sh:421`, `scripts/setup-rlcr-loop.sh:427`, and `scripts/setup-rlcr-loop.sh:435`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, git, a mock `codex`, and `scripts/setup-rlcr-loop.sh`. The production implementation it verifies rejects plan-file symlinks and parent-directory symlinks at `scripts/setup-rlcr-loop.sh:447` and `scripts/setup-rlcr-loop.sh:460`, enforces project containment at `scripts/setup-rlcr-loop.sh:495`, and validates content at `scripts/setup-rlcr-loop.sh:584`.
- edge_cases_or_failure_modes: Symlink file, parent symlink, and symlink chain rejection are covered at `tests/robustness/test-path-validation-robustness.sh:342`, `tests/robustness/test-path-validation-robustness.sh:352`, and `tests/robustness/test-path-validation-robustness.sh:367`. Empty files, comments-only files, insufficient lines, nonexistent files, and directories masquerading as plan files are rejected at `tests/robustness/test-path-validation-robustness.sh:425`. The suite notes Unicode/CJK/emoji paths are allowed at `tests/robustness/test-path-validation-robustness.sh:414`, so path validation is not ASCII-only.
- validation_or_tests: This file is itself the validation suite. It cross-checks production output for errors such as plan path failures, symbolic links, missing directories, too-simple plans, and insufficient content at `tests/robustness/test-path-validation-robustness.sh:91`. I did not execute it because the assignment is read-only research and the export does not include a usable `.git` checkout.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present, matching assigned_item_count
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`