# agent_18 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-018 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR review-loop prompt contract directory. It defines the Round 0 user-facing instructions, goal tracker seed, critical completion requirements, and Codex goal-tracker update instructions used by `/humanize:start-pr-loop` and the PR-loop Stop Hook.
- algorithmic_behavior: `scripts/setup-pr-loop.sh` renders `goal-tracker-initial.md` into `goal-tracker.md` (`scripts/setup-pr-loop.sh:712-715`), renders `round-0-header.md` into `round-0-prompt.md` (`scripts/setup-pr-loop.sh:757-764`), then branches on the fetched-comments sentinel (`scripts/setup-pr-loop.sh:723-729`) to append either `round-0-task-no-comments.md` (`scripts/setup-pr-loop.sh:767-800`) or `round-0-task-has-comments.md` (`scripts/setup-pr-loop.sh:801-847`). It also prints either `critical-requirements-no-comments.md` or `critical-requirements-has-comments.md` as the final command contract (`scripts/setup-pr-loop.sh:890-942`). `hooks/pr-loop-stop-hook.sh` renders `codex-goal-tracker-update.md` into the Codex validation prompt (`hooks/pr-loop-stop-hook.sh:1253-1303`).
- inputs_outputs_state: Inputs are template variables including `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND` (`prompt-template/pr-loop/goal-tracker-initial.md:5-9`, `prompt-template/pr-loop/round-0-header.md:7-15`, `prompt-template/pr-loop/codex-goal-tracker-update.md:3-18`). Outputs are prompt text, a seeded goal tracker table, and Stop Hook instructions for future state updates. The state transition is Round 0 waiting-for-review when no comments exist, or Round 0 fix-and-trigger-review when comments exist.
- gates_or_invariants: No-comments path forbids manually triggering first review and requires only a resolution summary plus exit attempt (`prompt-template/pr-loop/round-0-task-no-comments.md:6-23`, `prompt-template/pr-loop/critical-requirements-no-comments.md:6-20`). Has-comments path requires analyzing comments, fixing issues, committing, pushing, triggering bot review by `gh pr comment`, and writing the resolution summary (`prompt-template/pr-loop/round-0-task-has-comments.md:6-27`, `prompt-template/pr-loop/critical-requirements-has-comments.md:6-24`). All task prompts forbid modifying `.humanize/pr-loop/` state files (`prompt-template/pr-loop/round-0-task-no-comments.md:18-23`, `prompt-template/pr-loop/round-0-task-has-comments.md:30-35`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` single-pass rendering and safe fallback behavior (`hooks/lib/template-loader.sh:167-203`). Primary callers are `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh`; sibling block templates enforce write protections for PR-loop state/prompt files (`hooks/lib/loop-common.sh:1058-1070` from search evidence).
- edge_cases_or_failure_modes: Missing or empty templates fall back to embedded defaults in `setup-pr-loop.sh` and `pr-loop-stop-hook.sh` (`scripts/setup-pr-loop.sh:740-758`, `scripts/setup-pr-loop.sh:769-847`, `hooks/pr-loop-stop-hook.sh:1259-1263`). Comment detection is binary: only exact `*No comments found.*` means zero comments; any other fetched content sets `COMMENT_COUNT=1` (`scripts/setup-pr-loop.sh:723-729`).
- validation_or_tests: Covered by PR-loop script/hook/system tests discovered under `tests/test-pr-loop-*.sh` and `tests/test-pr-loop-system.sh`; template loading robustness is separately specified by `tests/robustness/test-template-error-robustness.sh`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-048 `file` `scripts/bitlesson-init.sh`
- cursor: `[_]`
- core_role: BitLesson knowledge-base bootstrap script for RLCR setup. It creates a project-local `.humanize/bitlesson.md` from the plugin template exactly once.
- algorithmic_behavior: Parses `--project-root`, `--template`, and optional `--bitlesson-relpath` (`scripts/bitlesson-init.sh:22-46`); validates required arguments and file-system preconditions (`scripts/bitlesson-init.sh:48-68`); rejects absolute or parent-traversal relative paths (`scripts/bitlesson-init.sh:70-73`); resolves the project root physically with `pwd -P` (`scripts/bitlesson-init.sh:75-76`); creates parent directories and copies the template only when the BitLesson file is missing (`scripts/bitlesson-init.sh:83-86`); prints the resolved file path on success (`scripts/bitlesson-init.sh:88`).
- inputs_outputs_state: Inputs are an existing project root, an existing template file, and a relative BitLesson path defaulting to `.humanize/bitlesson.md` (`scripts/bitlesson-init.sh:18-20`). Output is the absolute BitLesson path on stdout. State transition is absent file to template-created file; existing regular file is preserved without overwrite.
- gates_or_invariants: Fails if project root is absent or not a directory, template is absent or not a file, BitLesson path is absolute, BitLesson path contains `..`, or target path exists but is not a regular file (`scripts/bitlesson-init.sh:60-80`). The no-overwrite invariant is explicit in usage and implemented by `if [[ ! -f "$BITLESSON_FILE" ]]` (`scripts/bitlesson-init.sh:10-14`, `scripts/bitlesson-init.sh:83-86`).
- dependencies_and_callers: Called by RLCR setup immediately before state-file creation (`scripts/setup-rlcr-loop.sh:822-828`). Downstream BitLesson behavior reads the same default path in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:127-140`) and validates deltas with `bitlesson-validate-delta.sh` (`hooks/loop-codex-stop-hook.sh:720-727`). Depends on shell builtins plus `mkdir`, `cp`, `dirname`, and `pwd`.
- edge_cases_or_failure_modes: A missing value after an option can trip `shift 2` under `set -e` before a custom usage error. Empty `--bitlesson-relpath` resolves to the project root path and is rejected as non-regular because the root directory exists. Symlink-to-file targets satisfy `-f` and are preserved. The path guard blocks traversal but otherwise permits quoted spaces or special characters because all expansions are quoted.
- validation_or_tests: No direct `bitlesson-init.sh` unit test appeared in the inspected references. Indirect coverage comes through RLCR setup and BitLesson selector/validator tests; the template seeded by this script is `templates/bitlesson.md`, whose strict entry schema is defined at `templates/bitlesson.md:5-19`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-078 `file` `tests/test-config-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for four-layer configuration merge behavior in `scripts/lib/config-loader.sh`.
- algorithmic_behavior: Sources `config-loader.sh` (`tests/test-config-merge.sh:27-33`), creates isolated temp projects/user config roots, calls `load_merged_config`, and asserts selected values through `get_config_value`. The tested implementation loads default, user, and project layers (`scripts/lib/config-loader.sh:77-88`), normalizes missing optional layers to `{}` (`scripts/lib/config-loader.sh:35-50`), strips nulls recursively (`scripts/lib/config-loader.sh:119-126`), and merges with project overriding user overriding defaults (`scripts/lib/config-loader.sh:128-132`).
- inputs_outputs_state: Inputs are `config/default_config.json`, optional `$XDG_CONFIG_HOME/humanize/config.json`, optional `$PROJECT_DIR/.humanize/config.json`, and optional `HUMANIZE_CONFIG`. Outputs are pass/fail messages and final summary via `print_test_summary` (`tests/test-config-merge.sh:200`; helper at `tests/test-helpers.sh:58-78`). State is isolated per case by `setup_test_dir` and temporary project directories.
- gates_or_invariants: Asserts default-only values (`tests/test-config-merge.sh:39-64`), project overrides defaults (`tests/test-config-merge.sh:70-89`), project wins over user for same key (`tests/test-config-merge.sh:95-109`), user-only keys survive project additive merge (`tests/test-config-merge.sh:115-132`), project `null` does not override a lower value (`tests/test-config-merge.sh:138-150`), `HUMANIZE_CONFIG` replaces the default project config path (`tests/test-config-merge.sh:156-173`), and all layers can contribute distinct keys (`tests/test-config-merge.sh:179-198`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/lib/config-loader.sh`, `jq` as required by `_config_loader_require_jq` (`scripts/lib/config-loader.sh:17-22`), and `config/default_config.json` defaults (`config/default_config.json:1-8`). Runtime callers of the merge API include `scripts/bitlesson-select.sh` (`scripts/bitlesson-select.sh:13-17`) and `hooks/lib/loop-common.sh` config-backed defaults from search evidence.
- edge_cases_or_failure_modes: This file intentionally focuses on successful merge semantics, especially null stripping and custom project config path. Malformed/missing required config handling is in separate tests discovered as `tests/test-config-error-handling.sh`, not this assigned file.
- validation_or_tests: The file is itself the validation harness. It exits through `print_test_summary`, returning nonzero if any `fail` increments the shared failure counter.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-108 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for the Stop Hook task/todo completion gate implemented by `hooks/check-todos-from-transcript.py`.
- algorithmic_behavior: Invokes the Python checker with synthetic hook input JSON, transcript JSONL files, and mock task directories. The implementation accepts empty/no-transcript/no-missing-file cases, parses legacy `TodoWrite` calls from multiple transcript shapes, keeps the latest TodoWrite list, reads file-based Claude tasks by `session_id`, and reports any item not completed/deleted as incomplete (`hooks/check-todos-from-transcript.py:58-101`, `hooks/check-todos-from-transcript.py:104-147`, `hooks/check-todos-from-transcript.py:150-196`).
- inputs_outputs_state: Inputs are stdin hook JSON fields `transcript_path`, `session_id`, and optional `tasks_base_dir`; generated transcript files under `$TEST_DIR`; and task JSON files under `$tasks_base_dir/$session_id`. Outputs are exit code `0` for clear/no tasks, `1` with `INCOMPLETE_TODOS` details for incomplete items, and `2` for invalid hook JSON (`hooks/check-todos-from-transcript.py:9-12`, `hooks/check-todos-from-transcript.py:158-161`, `hooks/check-todos-from-transcript.py:193-196`).
- gates_or_invariants: Invalid hook JSON returns parse error (`tests/test-todo-checker.sh:52-62`); empty input, missing transcript path, and nonexistent transcript allow progress (`tests/test-todo-checker.sh:64-98`). Legacy `pending`, `in_progress`, missing status, and empty-content todos block (`tests/test-todo-checker.sh:122-158`, `tests/test-todo-checker.sh:250-279`); all completed todos pass (`tests/test-todo-checker.sh:107-120`). File tasks default missing status to pending and treat `completed` and `deleted` as non-blocking (`tests/test-todo-checker.sh:309-415`).
- dependencies_and_callers: Depends directly on Python 3 and `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:12-15`). The checker is used by `hooks/loop-codex-stop-hook.sh` from search evidence (`hooks/loop-codex-stop-hook.sh:380`).
- edge_cases_or_failure_modes: Invalid JSONL transcript lines are ignored while valid lines still count (`tests/test-todo-checker.sh:180-195`); multiple TodoWrite calls use the latest list (`tests/test-todo-checker.sh:197-211`); direct `tool_use` and alternative `message` formats are accepted (`tests/test-todo-checker.sh:213-240`); Unicode content passes (`tests/test-todo-checker.sh:281-294`); mixed legacy and file-task systems aggregate incompletes (`tests/test-todo-checker.sh:417-443`); missing task session directory passes (`tests/test-todo-checker.sh:445-455`).
- validation_or_tests: The file has 25 numbered cases and exits nonzero if any fail (`tests/test-todo-checker.sh:457-475`). It verifies both exit status and selected output text, including incomplete todo content and task ID markers (`tests/test-todo-checker.sh:137-143`, `tests/test-todo-checker.sh:437-443`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-138 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Blocking prompt template for stale RLCR state schema detection.
- algorithmic_behavior: Renders the missing field name into a user-facing block reason (`prompt-template/block/schema-outdated.md:1-12`). `hooks/loop-plan-file-validator.sh` uses it when required v1.1.2+ state fields are missing (`hooks/loop-plan-file-validator.sh:59-77`), after strict state parsing succeeds and before branch/plan-file checks.
- inputs_outputs_state: Input is `FIELD_NAME`, supplied as `FIELD_NAME=$field_name` (`hooks/loop-plan-file-validator.sh:64-65`). Output is JSON `decision: block` with the rendered reason after newline escaping (`hooks/loop-plan-file-validator.sh:67-76`). State transition is an active prompt submission being blocked because the session was started with an older state shape.
- gates_or_invariants: The validator requires `plan_tracked` and `start_branch` (`hooks/loop-plan-file-validator.sh:79-88`), using constants from `hooks/lib/loop-common.sh:24-29`. The template instructs cancel/update/restart and states the session will terminate as unexpected to preserve state information (`prompt-template/block/schema-outdated.md:5-12`).
- dependencies_and_callers: Depends on `load_and_render_safe` from `template-loader.sh` and strict state parsing from `loop-common.sh` (`hooks/loop-plan-file-validator.sh:16-18`, `hooks/loop-plan-file-validator.sh:44-48`). The strict parser separately enforces required fields such as `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:423-459`).
- edge_cases_or_failure_modes: If this template is missing, the validator falls back to an embedded RLCR schema-outdated message (`hooks/loop-plan-file-validator.sh:60-65`). If the state file is malformed rather than merely old, the hook fails closed with a different error before this template is used (`hooks/loop-plan-file-validator.sh:44-48`).
- validation_or_tests: Related robustness tests exercise strict state parsing failures in `tests/robustness/test-concurrent-state-robustness.sh` and `tests/robustness/test-state-transition-robustness.sh` from search evidence; this exact template path is rendered by the validator rather than directly unit-tested in the inspected assignment.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-168 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: Critical completion contract for PR-loop Round 0 when fetched PR comments already exist.
- algorithmic_behavior: Supplies the final, high-salience instructions printed after the generated prompt: commit and push changes, comment on the PR to trigger re-review, and write the resolution summary (`prompt-template/pr-loop/critical-requirements-has-comments.md:6-24`). `scripts/setup-pr-loop.sh` selects this template only when `COMMENT_COUNT` is nonzero (`scripts/setup-pr-loop.sh:801-847`, `scripts/setup-pr-loop.sh:915-940`).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH` (`prompt-template/pr-loop/critical-requirements-has-comments.md:12-16`). Output is rendered text to stdout after the initial prompt (`scripts/setup-pr-loop.sh:890-942`). State expectation is that the user creates remote-visible changes and a resolution file before the Stop Hook polls bot reviews.
- gates_or_invariants: Enforces remote review visibility: changes must be committed and pushed (`prompt-template/pr-loop/critical-requirements-has-comments.md:8-10`), review must be retriggered with `gh pr comment` (`prompt-template/pr-loop/critical-requirements-has-comments.md:12-13`), and the summary must include issues addressed, files modified, and tests added if any (`prompt-template/pr-loop/critical-requirements-has-comments.md:15-21`).
- dependencies_and_callers: Depends on `setup-pr-loop.sh` comment detection and template rendering. Its sibling `critical-requirements-no-comments.md` handles the zero-comment startup case and explicitly forbids manual PR comments (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-20`).
- edge_cases_or_failure_modes: If rendered variables are wrong or empty, the generated `gh pr comment` command or summary path becomes wrong; `load_and_render_safe` fallback preserves the same contract if the template file cannot load (`scripts/setup-pr-loop.sh:916-940`). The command body is quoted as one string, so bot mention content must be safe for that context.
- validation_or_tests: Covered indirectly by setup PR-loop tests discovered under `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-system.sh`, and robustness setup-script tests; template fallback behavior is covered by template-loader robustness specs.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-198 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the prompt template loader/rendering subsystem used by RLCR, PR-loop, and block templates.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and shared test helpers (`tests/robustness/test-template-error-robustness.sh:15-20`), then probes `load_and_render_safe` and `render_template` under missing files, malformed placeholders, unusual variable names/values, file-system edge cases, concurrency, and discovery paths.
- inputs_outputs_state: Inputs are temporary template files, direct template strings, fallback messages, and `VAR=value` substitution arguments. Outputs are pass/fail summary through `print_test_summary` (`tests/robustness/test-template-error-robustness.sh:337-341`). State includes a temp directory, chmod changes for permission testing, background jobs for concurrent loading, and symlink creation where supported.
- gates_or_invariants: Missing template or missing directory must return fallback (`tests/robustness/test-template-error-robustness.sh:34-51`); malformed placeholders must not crash (`tests/robustness/test-template-error-robustness.sh:73-115`); template syntax in replacement values must not recursively expand (`tests/robustness/test-template-error-robustness.sh:165-174`), matching the single-pass invariant in `template-loader.sh` (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, especially `load_template`, `render_template`, and `load_and_render_safe` (`hooks/lib/template-loader.sh:36-48`, `hooks/lib/template-loader.sh:58-132`, `hooks/lib/template-loader.sh:170-203`). This library is used by PR-loop setup, PR-loop stop hook, RLCR validators, and BitLesson validation from search evidence.
- edge_cases_or_failure_modes: Covers empty templates with loose acceptance of empty or fallback (`tests/robustness/test-template-error-robustness.sh:53-63`), nested braces (`tests/robustness/test-template-error-robustness.sh:84-93`), spaces/special chars/numeric-only variable names (`tests/robustness/test-template-error-robustness.sh:125-155`), newline values and very long names (`tests/robustness/test-template-error-robustness.sh:176-198`), UTF-8 BOM (`tests/robustness/test-template-error-robustness.sh:208-218`), whitespace-only templates (`tests/robustness/test-template-error-robustness.sh:220-226`), filenames with spaces (`tests/robustness/test-template-error-robustness.sh:228-243`), permission denial with root skip (`tests/robustness/test-template-error-robustness.sh:245-260`), ten concurrent renders (`tests/robustness/test-template-error-robustness.sh:270-299`), subdirectory templates and symlinks (`tests/robustness/test-template-error-robustness.sh:309-334`).
- validation_or_tests: The file is itself a robustness gate and exits with the helper summary status (`tests/robustness/test-template-error-robustness.sh:337-341`). It intentionally accepts some system-dependent outcomes, such as permission behavior and symlink support, while still requiring non-crashing behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique `## Item Evidence` sections, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`