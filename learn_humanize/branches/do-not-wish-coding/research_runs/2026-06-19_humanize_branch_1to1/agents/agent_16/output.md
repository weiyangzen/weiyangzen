# agent_16 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-016 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-facing RLCR review template set. It defines how Codex reviews Claude’s implementation summaries, how periodic full-alignment audits behave, how goal-tracker updates are delegated to Codex, and how the separate `codex review --base` phase is documented for audit.
- algorithmic_behavior: Recursively inspected all child files: `code-review-phase.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`. `regular-review.md` requires Codex to read the original plan, compare Claude’s summary to actual implementation, reject deferrals as incomplete, update/review the goal tracker, and emit `COMPLETE` only when all tasks and ACs are done with no pending or deferred work (`prompt-template/codex/regular-review.md:1`, `prompt-template/codex/regular-review.md:20`, `prompt-template/codex/regular-review.md:33`, `prompt-template/codex/regular-review.md:49`). `full-alignment-review.md` adds mandatory AC table auditing, forgotten/deferred item checks, historical round review, and a stagnation circuit breaker that outputs `STOP` instead of `COMPLETE` (`prompt-template/codex/full-alignment-review.md:19`, `prompt-template/codex/full-alignment-review.md:59`, `prompt-template/codex/full-alignment-review.md:84`). `goal-tracker-update-section.md` makes Codex responsible for justified goal-tracker mutations while forbidding immutable-section changes (`prompt-template/codex/goal-tracker-update-section.md:1`, `prompt-template/codex/goal-tracker-update-section.md:6`, `prompt-template/codex/goal-tracker-update-section.md:10`). `code-review-phase.md` documents the post-`COMPLETE` `codex review --base` audit and expected `[P0-9]` marker format (`prompt-template/codex/code-review-phase.md:1`, `prompt-template/codex/code-review-phase.md:14`, `prompt-template/codex/code-review-phase.md:21`).
- inputs_outputs_state: Inputs are rendered variables from `hooks/loop-codex-stop-hook.sh`: plan path, prompt path, summary content, goal tracker path, docs path, loop timestamp, previous rounds, and review-result path (`hooks/loop-codex-stop-hook.sh:860`, `hooks/loop-codex-stop-hook.sh:909`, `hooks/loop-codex-stop-hook.sh:925`). Outputs are `round-N-review-prompt.md`, `round-N-review-result.md`, optional goal-tracker edits by Codex, and final markers `COMPLETE` or `STOP`. `COMPLETE` in implementation review starts review phase, not final completion; the hook sets `review_started: true`, writes `.review-phase-started`, and runs `codex review` (`hooks/loop-codex-stop-hook.sh:1539`, `hooks/loop-codex-stop-hook.sh:1566`, `hooks/loop-codex-stop-hook.sh:1572`, `hooks/loop-codex-stop-hook.sh:1579`). A clean `codex review` then enters finalize (`hooks/loop-codex-stop-hook.sh:1113`).
- gates_or_invariants: Full alignment is scheduled every `full_review_round` with invalid values defaulted to 5 (`hooks/loop-codex-stop-hook.sh:866`, `hooks/loop-codex-stop-hook.sh:869`). The templates define strict completion: no deferred items, no pending tasks, all ACs met (`prompt-template/codex/regular-review.md:53`, `prompt-template/codex/full-alignment-review.md:91`). The review phase ignores `COMPLETE` from prompt review and uses `codex review` marker absence as the gate (`hooks/loop-codex-stop-hook.sh:1540`, `hooks/loop-codex-stop-hook.sh:1592`).
- dependencies_and_callers: Called via `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` for regular/full prompts and goal-tracker update injection (`hooks/loop-codex-stop-hook.sh:863`, `hooks/loop-codex-stop-hook.sh:911`, `hooks/loop-codex-stop-hook.sh:926`). `code-review-phase.md` is rendered in `run_codex_code_review` to create an audit prompt file before invoking `codex review --base` (`hooks/loop-codex-stop-hook.sh:1009`, `hooks/loop-codex-stop-hook.sh:1040`, `hooks/loop-codex-stop-hook.sh:1066`). Template fallback behavior depends on `hooks/lib/template-loader.sh` through `load_and_render_safe`.
- edge_cases_or_failure_modes: Full-alignment history references clamp prior round numbers to zero so low interval values do not produce negative paths (`hooks/loop-codex-stop-hook.sh:881`). `STOP` is honored even outside full-alignment rounds, but logged as unexpected (`hooks/loop-codex-stop-hook.sh:1617`, `hooks/loop-codex-stop-hook.sh:1633`). `code-review-phase.md` is explicitly audit documentation because `codex review` accepts base diff context, not prompt input (`prompt-template/codex/code-review-phase.md:3`).
- validation_or_tests: `tests/test-codex-review-merge.sh` validates the `[P0-9]` log-detection behavior consumed by `code-review-phase.md`. `tests/test-template-references.sh` validates literal template references for selected RLCR hooks and validators; it does not scan every script dynamically, so unreferenced or dynamically referenced templates are warning-level rather than exhaustive failures (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:130`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-046 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop hook for the PR review loop state machine. It intercepts exit attempts, validates local readiness, polls remote PR-review bots, runs local Codex to classify bot feedback, mutates PR-loop state, and blocks Claude with the next-round remediation prompt when issues remain.
- algorithmic_behavior: The hook reads stdin hook input, locates the active PR loop under `.humanize/pr-loop`, sources shared loop functions and timeout helpers, then parses `state.md` frontmatter into round, max iteration, PR number, bot lists, Codex config, polling config, trigger timestamps, and commit tracking fields (`hooks/pr-loop-stop-hook.sh:43`, `hooks/pr-loop-stop-hook.sh:49`, `hooks/pr-loop-stop-hook.sh:52`, `hooks/pr-loop-stop-hook.sh:88`). YAML-list parsing supports multiline lists plus older comma-separated inline format and keeps `configured_bots` immutable while `active_bots` shrinks/expands (`hooks/pr-loop-stop-hook.sh:111`, `hooks/pr-loop-stop-hook.sh:121`, `hooks/pr-loop-stop-hook.sh:162`). It resolves fork/base repo context for PR operations (`hooks/pr-loop-stop-hook.sh:197`), exits terminally on merged/closed PRs (`hooks/pr-loop-stop-hook.sh:244`), requires the current round resolution file (`hooks/pr-loop-stop-hook.sh:262`), enforces clean and pushed Git state excluding `.humanize` (`hooks/pr-loop-stop-hook.sh:283`, `hooks/pr-loop-stop-hook.sh:296`, `hooks/pr-loop-stop-hook.sh:350`), detects force-push history rewrites and clears stale trigger fields (`hooks/pr-loop-stop-hook.sh:370`, `hooks/pr-loop-stop-hook.sh:397`), then polls all configured bots for new comments after the correct trigger/start timestamp (`hooks/pr-loop-stop-hook.sh:806`, `hooks/pr-loop-stop-hook.sh:817`, `hooks/pr-loop-stop-hook.sh:881`).
- inputs_outputs_state: Inputs include `.humanize/pr-loop/<timestamp>/state.md`, `round-N-pr-resolve.md`, Git status/history, GitHub PR state/comments/reactions via `gh`, bot polling scripts, Codex CLI, and templates. Outputs include JSON hook decisions, `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, goal-tracker updates, and state-file transitions. Terminal transitions rename `state.md` to `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, or `usage-limit-state.md` (`hooks/pr-loop-stop-hook.sh:247`, `hooks/pr-loop-stop-hook.sh:252`, `hooks/pr-loop-stop-hook.sh:432`, `hooks/pr-loop-stop-hook.sh:1377`, `hooks/pr-loop-stop-hook.sh:1414`). Continuing transitions rewrite `current_round`, `active_bots`, latest commit fields, and trigger fields (`hooks/pr-loop-stop-hook.sh:1506`, `hooks/pr-loop-stop-hook.sh:1568`).
- gates_or_invariants: Required gates are active loop + valid PR number/current round, PR still open, resolution summary present, no non-humanize dirty files, no unpushed commits, no force-push without fresh trigger, max iterations not exceeded, trigger freshness based on startup case/round, Claude eyes reaction when Claude is configured and trigger-required, all configured bots responded or timed out, Codex CLI available, Codex output non-empty, and final Codex marker classification (`hooks/pr-loop-stop-hook.sh:186`, `hooks/pr-loop-stop-hook.sh:264`, `hooks/pr-loop-stop-hook.sh:299`, `hooks/pr-loop-stop-hook.sh:350`, `hooks/pr-loop-stop-hook.sh:620`, `hooks/pr-loop-stop-hook.sh:728`, `hooks/pr-loop-stop-hook.sh:768`, `hooks/pr-loop-stop-hook.sh:1305`, `hooks/pr-loop-stop-hook.sh:1357`). `configured_bots` remains the polling universe so approved bots can be re-added if new issues appear (`hooks/pr-loop-stop-hook.sh:806`, `hooks/pr-loop-stop-hook.sh:1441`).
- dependencies_and_callers: Created state is seeded by `scripts/setup-pr-loop.sh`, which writes `configured_bots`, `active_bots`, Codex config, polling config, startup case, latest commit, and trigger fields into `state.md` (`scripts/setup-pr-loop.sh:608`, `scripts/setup-pr-loop.sh:615`). Shared helpers come from `hooks/lib/loop-common.sh`: bot author mapping, YAML list building, mention-string building, active PR-loop detection, and goal-tracker updates (`hooks/lib/loop-common.sh:891`, `hooks/lib/loop-common.sh:919`, `hooks/lib/loop-common.sh:931`, `hooks/lib/loop-common.sh:963`, `hooks/lib/loop-common.sh:1347`). External scripts include `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, and `scripts/check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:676`, `hooks/pr-loop-stop-hook.sh:777`, `hooks/pr-loop-stop-hook.sh:812`, `hooks/pr-loop-stop-hook.sh:1557`).
- edge_cases_or_failure_modes: Missing active loop or missing state file exits 0 to let other hooks handle (`hooks/pr-loop-stop-hook.sh:66`, `hooks/pr-loop-stop-hook.sh:69`, `hooks/pr-loop-stop-hook.sh:75`). Invalid `current_round` warns and exits without blocking (`hooks/pr-loop-stop-hook.sh:192`). Fork PRs fall back through current repo then parent repo, with warning fallback if unresolved (`hooks/pr-loop-stop-hook.sh:221`, `hooks/pr-loop-stop-hook.sh:233`). New commits after stored timestamp clear old triggers even without force push (`hooks/pr-loop-stop-hook.sh:548`). Startup cases 1/2/3 can proceed without a new trigger on round 0, while cases 4/5 and later rounds require one (`hooks/pr-loop-stop-hook.sh:613`). Timed-out bots are removed from `active_bots` and can cause approval if no active bots remain (`hooks/pr-loop-stop-hook.sh:921`, `hooks/pr-loop-stop-hook.sh:1098`). Codex `+1` reactions are treated as approval both before and during polling (`hooks/pr-loop-stop-hook.sh:662`, `hooks/pr-loop-stop-hook.sh:985`). `USAGE_LIMIT_HIT` terminates the loop as an external-service limitation (`hooks/pr-loop-stop-hook.sh:1414`).
- validation_or_tests: Related executable coverage exists in PR-loop stop-hook/system tests and hook validators; direct references include `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-system.sh` from repository search. The assigned `tests/test-template-references.sh` also validates that selected hook/validator template references resolve. I did not execute tests in this read-only research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-076 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR code-review merge gate: it defines how `detect_review_issues()` interprets `codex review` logs and decides whether to continue review remediation or allow finalize.
- algorithmic_behavior: The file states the algorithm explicitly: scan only the last 50 log lines, find the first `[P?]` marker where `?` is a digit and the marker appears within the first 10 characters, extract from that line to the end, return 0 when issues are found, and return 1 when no issues are found (`tests/test-codex-review-merge.sh:5`, `tests/test-codex-review-merge.sh:11`). It sets an isolated `XDG_CACHE_HOME`, creates `LOOP_DIR` and `CACHE_DIR`, sources `hooks/lib/loop-common.sh`, and writes per-round log fixtures (`tests/test-codex-review-merge.sh:37`, `tests/test-codex-review-merge.sh:41`, `tests/test-codex-review-merge.sh:48`).
- inputs_outputs_state: Inputs are log files at `$CACHE_DIR/round-N-codex-review.log`; outputs are the function exit code, stdout content, and `round-N-review-result.md` in `$LOOP_DIR` when issues are found (`tests/test-codex-review-merge.sh:72`, `tests/test-codex-review-merge.sh:321`). The implementation under test uses the same paths and writes the extracted content to the result file (`hooks/lib/loop-common.sh:509`, `hooks/lib/loop-common.sh:511`, `hooks/lib/loop-common.sh:547`).
- gates_or_invariants: A `[P0-9]` marker outside the first 10 characters is ignored (`tests/test-codex-review-merge.sh:84`). Markers outside the last-50-line scan window are ignored (`tests/test-codex-review-merge.sh:200`). Missing or empty logs are hard errors with exit code 2, not “no issues” (`tests/test-codex-review-merge.sh:134`, `tests/test-codex-review-merge.sh:153`). Exactly 50 lines still include the first line in the scan window (`tests/test-codex-review-merge.sh:333`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `detect_review_issues` (`tests/test-codex-review-merge.sh:41`). The caller in `hooks/loop-codex-stop-hook.sh` treats exit 2 as a blocking review failure, exit 0 as remediation-needed, and exit 1 as review passed (`hooks/loop-codex-stop-hook.sh:1100`, `hooks/loop-codex-stop-hook.sh:1106`, `hooks/loop-codex-stop-hook.sh:1109`, `hooks/loop-codex-stop-hook.sh:1113`).
- edge_cases_or_failure_modes: Tests cover marker at line start, marker after dash prefix, multiple markers where extraction begins at the first marker, late marker in a long log, early marker outside the tail window, missing log, empty log, and result-file creation (`tests/test-codex-review-merge.sh:229`, `tests/test-codex-review-merge.sh:258`, `tests/test-codex-review-merge.sh:280`, `tests/test-codex-review-merge.sh:305`). This narrows false positives from debug output while making empty/missing review output fail closed.
- validation_or_tests: The script self-validates with pass/fail counters and exits 1 if any test fails (`tests/test-codex-review-merge.sh:356`, `tests/test-codex-review-merge.sh:366`). I inspected it directly; not executed during this research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-106 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable template-integrity validator for the RLCR hook/template layer. It prevents missing templates from producing empty or unhelpful block messages when validators stop an action.
- algorithmic_behavior: The test defines `PROJECT_ROOT` and `TEMPLATE_DIR`, then scans a fixed list of critical shell scripts for literal calls to `load_template`, `load_and_render`, or `load_and_render_safe` with `$TEMPLATE_DIR` as the first argument (`tests/test-template-references.sh:14`, `tests/test-template-references.sh:56`, `tests/test-template-references.sh:66`). For each non-comment matching line, it extracts the second quoted argument as a template path and requires the corresponding markdown file to exist (`tests/test-template-references.sh:87`, `tests/test-template-references.sh:93`, `tests/test-template-references.sh:96`, `tests/test-template-references.sh:102`). It also enumerates all markdown templates, reports unreferenced templates as warnings, validates a common-template allowlist, and checks critical validators use `load_and_render_safe` rather than unsafe rendering (`tests/test-template-references.sh:121`, `tests/test-template-references.sh:149`, `tests/test-template-references.sh:172`).
- inputs_outputs_state: Inputs are selected hook files and `prompt-template/**/*.md`. Outputs are pass/fail/warn counters, a missing-template list, and process exit status. It exits 0 only when no failures are recorded; warnings do not fail the run (`tests/test-template-references.sh:208`, `tests/test-template-references.sh:213`, `tests/test-template-references.sh:221`).
- gates_or_invariants: Literal template references in the selected scripts must resolve to files. Common block templates used by loop-common message helpers must exist (`tests/test-template-references.sh:152`). `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh` must not use raw `load_and_render` calls for critical block messages (`tests/test-template-references.sh:176`, `tests/test-template-references.sh:185`).
- dependencies_and_callers: This test depends on the template-loading conventions used by `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/lib/loop-common.sh` (`tests/test-template-references.sh:56`). It indirectly validates block templates including `block/state-file-modification.md`, `block/goal-tracker-modification.md`, and related guard messages (`tests/test-template-references.sh:152`).
- edge_cases_or_failure_modes: The scan is intentionally simple and literal: comments are skipped, but dynamic template paths and scripts outside `SCRIPTS_TO_CHECK` are not exhaustively validated (`tests/test-template-references.sh:74`, `tests/test-template-references.sh:87`). Unreferenced templates only warn because some templates may be used dynamically (`tests/test-template-references.sh:130`, `tests/test-template-references.sh:139`).
- validation_or_tests: The script is itself the validation gate and exits nonzero on missing templates or unsafe render calls (`tests/test-template-references.sh:225`). I inspected it directly; not executed during this research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-136 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Block-message template enforcing the PR-loop state-file immutability invariant. It tells agents not to modify `.humanize/pr-loop/state.md` because that state is owned by the PR-loop system.
- algorithmic_behavior: The template has no substitution variables. It emits a short markdown block explaining that `state.md` in `.humanize/pr-loop/` is managed and that modifying it would corrupt PR-loop state (`prompt-template/block/pr-loop-state-modification.md:1`, `prompt-template/block/pr-loop-state-modification.md:3`, `prompt-template/block/pr-loop-state-modification.md:12`). It enumerates state contents such as round number, PR number/branch, active bots, Codex configuration, and polling settings (`prompt-template/block/pr-loop-state-modification.md:5`).
- inputs_outputs_state: Input is a validator decision that a tool action targets PR-loop `state.md`; output is rendered markdown used as the blocking reason. It does not mutate state itself. State protection preserves fields consumed by `hooks/pr-loop-stop-hook.sh`, including current round, bot arrays, trigger metadata, and polling configuration.
- gates_or_invariants: The invariant is that PR-loop `state.md` is a system-managed transition ledger, not an agent-editable artifact. This is used to prevent manual corruption of state transitions such as round advancement, active-bot reduction, trigger timestamps, and terminal-state renames.
- dependencies_and_callers: `hooks/lib/loop-common.sh` wraps this template in `pr_loop_state_blocked_message()` with a fallback (`hooks/lib/loop-common.sh:1054`, `hooks/lib/loop-common.sh:1060`). It is called by write/edit/bash validators when `.humanize/pr-loop/.../state.md` is targeted (`hooks/loop-write-validator.sh:77`, `hooks/loop-edit-validator.sh:60`, `hooks/loop-bash-validator.sh:418`). The helper `is_in_pr_loop_dir()` provides path detection (`hooks/lib/loop-common.sh:948`).
- edge_cases_or_failure_modes: The template itself cannot detect path traversal or shell redirection; that responsibility sits in validators. If the template is missing, `load_and_render_safe` falls back to a simpler message from `loop-common.sh` (`hooks/lib/loop-common.sh:1056`).
- validation_or_tests: `tests/test-template-references.sh` includes `block/state-file-modification.md` but not this PR-specific template in its `COMMON_TEMPLATES` list; direct repository tests for PR-loop hooks reference write/bash attempts to `.humanize/pr-loop/.../state.md` from `tests/test-pr-loop-hooks.sh` search results. The assigned direct template was inspected.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-166 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Plan-generation schema for the RLCR workflow. It defines the structured contract that later setup, goal tracking, task routing, and review gates expect from a generated implementation plan.
- algorithmic_behavior: The template requires a goal description, ACs with positive and negative tests, path boundaries, feasibility hints, dependencies/sequence, a task breakdown, Claude-Codex deliberation, pending decisions, implementation notes, and output-file conventions (`prompt-template/plan/gen-plan-template.md:1`, `prompt-template/plan/gen-plan-template.md:6`, `prompt-template/plan/gen-plan-template.md:25`, `prompt-template/plan/gen-plan-template.md:57`). The task breakdown is algorithmically important: each task must carry exactly one routing tag, `coding` for Claude or `analyze` for Codex via `/humanize:ask-codex`, plus dependencies (`prompt-template/plan/gen-plan-template.md:69`, `prompt-template/plan/gen-plan-template.md:71`, `prompt-template/plan/gen-plan-template.md:75`).
- inputs_outputs_state: Input is the upstream draft/user goal plus `.humanize/config.json` language configuration as handled by the gen-plan command. Output is the main plan file, conventionally `plan.md`, and optionally a translated variant where the language code is inserted before the extension (`prompt-template/plan/gen-plan-template.md:106`, `prompt-template/plan/gen-plan-template.md:112`). Identifiers, AC IDs, task IDs, file paths, API names, and flags remain unchanged in translated variants (`prompt-template/plan/gen-plan-template.md:118`).
- gates_or_invariants: ACs must be deterministically verifiable with positive and negative tests (`prompt-template/plan/gen-plan-template.md:6`). Path boundaries define upper/lower acceptable implementation scope and allowed choices (`prompt-template/plan/gen-plan-template.md:25`). Deterministic designs should narrow choices rather than invent flexibility (`prompt-template/plan/gen-plan-template.md:44`). Generated implementation code must not include plan workflow markers like `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:99`). Unsupported or disabled `alternative_plan_language` produces no translation variant (`prompt-template/plan/gen-plan-template.md:120`).
- dependencies_and_callers: `commands/gen-plan.md` embeds a matching plan-structure block and defines config handling for `alternative_plan_language`; repository search shows tests verify the command/template structure match and the task-tag column exists (`tests/test-gen-plan.sh:123`, `tests/test-gen-plan.sh:708`). `commands/start-rlcr-loop.md` consumes the task routing semantics, mapping `coding -> Claude` and `analyze -> Codex` (`commands/start-rlcr-loop.md:141`). `scripts/validate-gen-plan-io.sh` references this template as the plan structure source.
- edge_cases_or_failure_modes: If a draft is highly deterministic, upper and lower bounds may converge; the template explicitly allows that (`prompt-template/plan/gen-plan-template.md:44`). Output filenames without extensions become `output_<code>` for translated variants, while dotted paths insert before the extension (`prompt-template/plan/gen-plan-template.md:114`). The template is a contract, not executable logic; enforcement depends on the gen-plan command and tests.
- validation_or_tests: `tests/test-gen-plan.sh` validates that the plan template includes the `coding`/`analyze` task-tag column and that the command’s embedded plan-structure block matches this template (`tests/test-gen-plan.sh:226`, `tests/test-gen-plan.sh:231`, `tests/test-gen-plan.sh:708`). I inspected the template directly.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-196 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for loop `state.md` parsing. It defines tolerant versus strict parser behavior for valid, malformed, partial, and forward-compatible state files.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` and test helpers, creates temporary state files, and tests `parse_state_file`, `parse_state_file_strict`, and `get_current_round` under positive and negative cases (`tests/robustness/test-state-file-robustness.sh:14`, `tests/robustness/test-state-file-robustness.sh:16`, `tests/robustness/test-state-file-robustness.sh:19`). Positive tests cover all required/common fields, `get_current_round`, extra unrecognized fields, quoted values, and round 0 (`tests/robustness/test-state-file-robustness.sh:33`, `tests/robustness/test-state-file-robustness.sh:62`, `tests/robustness/test-state-file-robustness.sh:72`, `tests/robustness/test-state-file-robustness.sh:100`, `tests/robustness/test-state-file-robustness.sh:124`). Negative and edge tests cover missing frontmatter, nonnumeric rounds, missing required fields, empty files, partial YAML, malformed YAML, very large and negative numbers, special-character values, missing files, binary suffixes, CRLF line endings, and `full_review_round` parsing/default/minimum (`tests/robustness/test-state-file-robustness.sh:149`, `tests/robustness/test-state-file-robustness.sh:173`, `tests/robustness/test-state-file-robustness.sh:201`, `tests/robustness/test-state-file-robustness.sh:230`, `tests/robustness/test-state-file-robustness.sh:399`).
- inputs_outputs_state: Inputs are generated `state.md` files in `$TEST_DIR`. Outputs are global `STATE_*` variables from `parse_state_file`, scalar output from `get_current_round`, pass/fail counters, and final process status. The implementation parses frontmatter with `sed`, extracts fields with `grep`/`sed`, and applies tolerant defaults: `STATE_CURRENT_ROUND=0`, `STATE_MAX_ITERATIONS=10`, `STATE_PUSH_EVERY_ROUND=false`, `STATE_FULL_REVIEW_ROUND=5`, `STATE_ASK_CODEX_QUESTION=true`, and `STATE_AGENT_TEAMS=false` (`hooks/lib/loop-common.sh:389`, `hooks/lib/loop-common.sh:396`, `hooks/lib/loop-common.sh:400`).
- gates_or_invariants: Strict parsing must reject missing files, fewer than two `---` separators, missing required fields, nonnumeric current/max iteration fields, and invalid `review_started` booleans (`hooks/lib/loop-common.sh:423`, `hooks/lib/loop-common.sh:431`, `hooks/lib/loop-common.sh:443`, `hooks/lib/loop-common.sh:461`, `hooks/lib/loop-common.sh:473`). Tolerant parsing must not crash on malformed or incomplete files and should default safely where appropriate (`tests/robustness/test-state-file-robustness.sh:163`, `tests/robustness/test-state-file-robustness.sh:217`, `tests/robustness/test-state-file-robustness.sh:230`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` parser functions (`hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:348`, `hooks/lib/loop-common.sh:389`, `hooks/lib/loop-common.sh:423`) and `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and summary printing. The parsers are consumed by loop hooks and validators such as `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`.
- edge_cases_or_failure_modes: The tests intentionally accept broad tolerant behavior for some cases, such as nonnumeric current round and CRLF files, as long as the parser does not crash (`tests/robustness/test-state-file-robustness.sh:190`, `tests/robustness/test-state-file-robustness.sh:386`). Strict parser validation has grown beyond the comments in this test: implementation also requires `review_started` and `base_branch` (`hooks/lib/loop-common.sh:452`, `hooks/lib/loop-common.sh:456`), while this suite generally asserts nonzero failure rather than exact error code for malformed strict cases.
- validation_or_tests: The file is itself the validation suite and ends with `print_test_summary` plus `exit $?` (`tests/robustness/test-state-file-robustness.sh:476`). I inspected it directly; not executed during this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7 unique Item Evidence headings`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`