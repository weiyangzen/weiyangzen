# agent_06 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-006 `directory` `hooks`
- cursor: `[_]`
- core_role: Hook-layer algorithm directory for Humanize loop enforcement. It wires Claude Code hook phases to validators and Stop hooks in `hooks/hooks.json:4-78`, then coordinates RLCR and PR-loop state machines through shared shell libraries, block templates, and Codex/GitHub subprocesses.
- algorithmic_behavior: `hooks/hooks.json:4-78` registers `UserPromptSubmit` plan validation, `PreToolUse` Read/Write/Edit/Bash validators, `PostToolUse` session-id patching, and two Stop hooks: RLCR (`loop-codex-stop-hook.sh`) and PR loop (`pr-loop-stop-hook.sh`). `hooks/lib/loop-common.sh` supplies the core predicates: JSON validation and nesting guard (`79-153`), active loop selection with session filtering and zombie-loop protection (`269-327`), strict/tolerant YAML frontmatter parsing (`389-485`), review issue detection (`509-557`), path/file type classifiers (`566-689`, `949-1000`), cancel authorization checks (`716-875`), `.humanize` git-add detection (`1095-1191`), shell modification detection (`1250-1275`), loop terminal state rename (`1298-1324`), and PR goal-tracker updates (`1347-1494`). `hooks/lib/template-loader.sh` loads templates and renders `{{VAR}}` placeholders in a single pass to prevent placeholder injection (`50-132`), with fallback rendering (`170-203`).
- inputs_outputs_state: Hook inputs are JSON from Claude Code on stdin, including `tool_name`, `tool_input`, `session_id`, and transcript/task fields. Outputs are either exit status only or JSON hook decisions with `decision: block`, `reason`, and optional `systemMessage`. Persistent state lives under `.humanize/rlcr/<timestamp>/` or `.humanize/pr-loop/<timestamp>/`; active states are `state.md` or `finalize-state.md`, terminal states are renamed to `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, `approve-state.md`, etc. RLCR Stop writes round prompts, summaries, review prompts/results, cache logs, finalize summaries, and review phase marker `.review-phase-started`. PR Stop writes round PR comments, Codex prompts/checks, feedback, and updates `active_bots`.
- gates_or_invariants: Validators fail closed on malformed hook JSON, deep JSON, malformed state, branch drift, protected state files, prompt/todo misuse, stale round file access, and Bash bypass attempts. Read validator blocks wrong round/location prompt-summary reads and todo reads except allowlisted early files (`loop-read-validator.sh:58-65`, `122-167`). Write/Edit validators block prompt writes, state/finalize-state writes, plan backup writes, goal tracker edits after round 0, wrong summary rounds, and PR read-only files (`loop-write-validator.sh:58-70`, `154-197`, `230-265`; `loop-edit-validator.sh:41-53`, `117-148`, `155-179`). Bash validator blocks direct hook execution, git push unless `push_every_round`, `.humanize` git-adds, state/finalize-state write/move/copy bypasses, plan backup, goal tracker, prompt, summary, todos, and PR state/read-only file mutations (`loop-bash-validator.sh:78-129`, `151-405`, `415-454`). Stop hooks enforce clean git, committed/pushed work where required, summary existence, task completion, line-count gate, plan integrity, review phase consistency, and Codex result markers.
- dependencies_and_callers: Called by Claude Code hook system through `hooks/hooks.json`. Depends on `jq`, `git`, `gh`, `codex`, Python 3, `scripts/portable-timeout.sh`, `scripts/lib/config-loader.sh`, PR helper scripts (`poll-pr-reviews.sh`, `check-bot-reactions.sh`, `check-pr-reviewer-status.sh`), BitLesson validator, and prompt templates under `prompt-template/`. `loop-post-bash-hook.sh` depends on `.humanize/.pending-session-id` emitted by setup and patches `session_id` exactly once (`32-123`). `check-todos-from-transcript.py` is called by RLCR Stop before Codex review (`loop-codex-stop-hook.sh:380-428`) and combines Claude task directory state with legacy TodoWrite transcript parsing (`check-todos-from-transcript.py:58-101`, `104-147`, `150-196`).
- edge_cases_or_failure_modes: Hook input null bytes/invalid UTF-8/deep nesting are rejected (`loop-common.sh:82-103`, `139-153`). Active-loop discovery intentionally ignores older loop dirs without a session match to avoid reviving stale loops (`269-327`). Cancel via Bash is only allowed with `.cancel-requested`, exact two-arg `mv`, no shell substitutions/operators, normalized paths, and non-symlink state source (`716-875`). RLCR Stop blocks Codex missing, nonzero, no output, missing review result, or empty review result (`loop-codex-stop-hook.sh:947-967`, `1456-1528`). Review phase cannot be faked by editing `review_started`; it requires `.review-phase-started` (`1592-1608`). PR Stop handles fork PR base repo resolution (`pr-loop-stop-hook.sh:197-237`), merged/closed PRs (`244-256`), unpushed commits with upstream/PR-head fallbacks (`313-361`), force push invalidation (`365-424`), missing trigger comments (`721-747`), Claude eyes timeout (`768-800`), per-bot timeout removal (`1065-1180`), Codex local validation failure/empty output (`1305-1367`), and usage-limit termination (`1414-1423`). One notable asymmetry: `loop-edit-validator.sh` parses JSON directly with `jq` but does not call `validate_hook_input`/`is_deeply_nested` like Read/Write/Bash.
- validation_or_tests: Covered by the global runner (`tests/run-all-tests.sh`) and named suites including template loader, bash validator patterns, todo checker, plan file validation/hooks, stop gate, finalization, PR loop hooks/stophook/system, session ID, Agent Teams, and robustness suites (`tests/run-all-tests.sh:59-120`). Direct target tests include bash/path/state/cancel/security/template/session robustness and PR-loop API fetch/poll. `tests/test-helpers.sh:30-78` defines pass/fail/summary counters consumed by the runner.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-036 `file` `docs/usage.md`
- cursor: `[_]`
- core_role: Public behavior contract for Humanize workflows and state machines. It defines user-visible algorithm phases, command inputs, configuration precedence, PR-loop polling behavior, cancellation, and sandbox/security knobs.
- algorithmic_behavior: The doc defines RLCR as two phases: Claude implementation reviewed by Codex summaries until `COMPLETE`, then `codex review --base <branch>` scanning `[P0-9]` severity markers (`docs/usage.md:5-12`). It defines a preflight Plan Understanding Quiz that analyzes the plan and asks two technical multiple-choice questions; the quiz is advisory and can be skipped with `--skip-quiz` or `--yolo` (`14-41`). It specifies `gen-plan` flow from input/output validation through repository relevance analysis, discussion/direct plan generation, and optional RLCR auto-start (`88-112`). It defines PR loop behavior as PR detection, bot comment fetch, Claude fixes, push/retrigger, Stop-hook polling every 30s with 15-minute timeout per bot, local Codex validation, and repeat until approval/max iterations (`114-145`).
- inputs_outputs_state: Inputs include CLI slash commands and options: `/start-rlcr-loop`, `/cancel-rlcr-loop`, `/gen-plan`, `/start-pr-loop`, `/cancel-pr-loop`, and `/ask-codex` (`43-52`). RLCR options include plan path, max iterations, Codex model/timeout, plan tracking, push-per-round, base branch, full review interval, skip implementation, open-question handling, Agent Teams, yolo, and quiz skip (`56-86`). PR options include bot flags, max iterations, Codex model/timeout (`114-130`). `ask-codex` saves outputs to `.humanize/skill/<timestamp>/` (`147-165`). Monitoring reads `.humanize/rlcr/<timestamp>/` (`216-231`). Config inputs come from defaults, user config, project config, then CLI flags (`167-213`).
- gates_or_invariants: The document makes several behavior guarantees: loop continues until acceptance criteria/no review issues (`12`), PR loop requires `gh`, Codex CLI, and an open PR (`142-145`), `--track-plan-file` requires clean tracked plan status (`68`), PR loop requires at least one bot flag (`117-121`), and sandbox bypass is explicitly dangerous and opt-in through `HUMANIZE_CODEX_BYPASS_SANDBOX=true|1` (`238-273`). Config precedence is a contract: CLI flags override feature defaults, merged config, and hardcoded fallback (`205-209`).
- dependencies_and_callers: It references install docs, command scripts, monitor shell helper `scripts/humanize.sh`, Codex CLI, GitHub CLI, Claude Code Agent Teams environment variable, and `.humanize/config.json`. It aligns with hook behavior in `loop-codex-stop-hook.sh` for `COMPLETE`, `[P0-9]`, `--base`, `--push-every-round`, full review rounds, open questions, Agent Teams, and sandbox flag.
- edge_cases_or_failure_modes: The doc explicitly models “wishful coding” as a workflow failure mitigated by the quiz (`16-35`); yolo mode skips that safeguard (`39-41`, `82-84`). It warns sandbox bypass grants unrestricted filesystem and command access (`240-264`). Legacy config keys are ignored rather than migrated (`211-213`). PR loop can terminate on max iterations or all approvals (`132-140`).
- validation_or_tests: Usage semantics are exercised indirectly by setup/stop-hook tests listed in `tests/run-all-tests.sh:59-120`, especially `test-gen-plan.sh`, `test-ask-codex.sh`, `test-pr-loop-1-scripts.sh`, `test-pr-loop-2-hooks.sh`, `test-pr-loop-3-stophook.sh`, `test-pr-loop-system.sh`, config tests, and model-router tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-066 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level executable specification and orchestrator for the repository’s algorithmic test suite. It defines the canonical list of behavior suites and the pass/fail aggregation contract.
- algorithmic_behavior: Determines parallelism from `HUMANIZE_TEST_JOBS` or CPU count capped to 1-8 (`tests/run-all-tests.sh:19-37`), detects `wait -n` support for Bash portability (`39-44`), declares the canonical suite list (`59-120`), marks zsh-only suites (`122-125`), creates an isolated output dir with cleanup (`127-129`), injects a mock `codex` binary when absent so setup checks can pass (`131-142`), launches each suite in parallel with per-suite stdout/exit/time files (`163-220`), waits and aggregates results (`222-267`), sorts display by elapsed time (`276-277`), then exits 1 if any suite failed or reported failed assertions (`287-304`).
- inputs_outputs_state: Inputs are executable suite files under `tests/`, optional `HUMANIZE_TEST_JOBS`, availability of `codex` and `zsh`, and suite output format containing `Passed:`/`Failed:` lines. Outputs are terminal summary lines, per-suite detail logs in a temp output directory, and process exit code 0/1. State is in associative arrays `PIDS`, `SKIPPED`, `FAILED_SUITES`, and temp files named by suite path.
- gates_or_invariants: `HUMANIZE_TEST_JOBS` must be integer >=1 (`33-37`). Missing suite files are skipped, not failed (`175-178`). zsh suites are skipped when zsh is unavailable (`180-184`). A suite fails if its exit code is nonzero or parsed failed count is >0 (`247-260`). Full suite log is preserved for failing suites (`259-260`). Test output is sorted by duration to surface performance/slow-suite ordering (`276-277`).
- dependencies_and_callers: Depends on Bash, `mktemp`, `getconf`, `date +%s%3N`, `sort`, `cut`, `sed`, suite scripts, and the `tests/test-helpers.sh` output contract (`Passed:` and `Failed:` at `tests/test-helpers.sh:58-78`). It enumerates core validators, PR loop, RLCR state, config, model routing, BitLesson, monitor, and robustness suites.
- edge_cases_or_failure_modes: Invalid job count fails immediately. Older Bash without `wait -n` falls back to waiting the oldest active PID (`202-218`). Missing `codex` is masked by a no-op mock, but suites requiring specific Codex behavior create their own mocks (`131-134`). Missing zsh causes skip, so zsh-specific regressions may be untested in minimal environments. The `date +%s%3N` timing expression is GNU-style and may be portability-sensitive on some macOS shells.
- validation_or_tests: This file is itself the validation orchestrator. It includes assigned robustness test `robustness/test-goal-tracker-robustness.sh` at `104`, PR script suite family at `86-89`, and PR API suites at `118-119`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-096 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable contract for PR-loop script argument parsing and cancellation state transitions across `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh`.
- algorithmic_behavior: Defines four test groups. `run_setup_tests` verifies setup help output, missing/invalid bot flag errors, recognition of `--claude`, `--codex`, both bots, `--max`, invalid max, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:18-166`). `run_cancel_tests` verifies help, no-loop result, and active-loop cancellation that renames `state.md` to `cancel-state.md` (`172-253`). `run_fetch_tests` verifies help, required PR number, required output path, and numeric PR validation (`259-326`). `run_poll_tests` verifies help, required PR number, required `--after`, and required `--bots` (`332-399`). `run_script_tests` composes all groups (`405-409`).
- inputs_outputs_state: Inputs are script paths under `scripts/`, `PROJECT_ROOT`, `TEST_DIR`, exported `CLAUDE_PROJECT_DIR` for cancellation tests, and mock `.humanize/pr-loop/<timestamp>/state.md` created in the temp repo (`217-228`). Outputs are pass/fail calls from `tests/test-helpers.sh`. State transition tested: active PR loop `state.md` becomes `cancel-state.md` and output includes `CANCELLED` (`230-244`).
- gates_or_invariants: `setup-pr-loop.sh` must reject missing bot flags and unknown options, parse positive integer `--max`, parse model/effort and timeout options, and not misclassify valid bot flags as missing (`38-165`). Production setup implements those guards at `scripts/setup-pr-loop.sh:111-190`. `cancel-pr-loop.sh` must return exit 1 and `NO_LOOP` without a loop and exit 0 with `CANCELLED` plus file rename for active loops (`192-244`; production at `scripts/cancel-pr-loop.sh:72-132`). `fetch-pr-comments.sh` and `poll-pr-reviews.sh` must fail early on missing/invalid required arguments before GitHub API work (`scripts/fetch-pr-comments.sh:92-106`; `scripts/poll-pr-reviews.sh:83-101`).
- dependencies_and_callers: Intended to be sourced by a higher-level PR-loop test harness; it assumes `pass`, `fail`, `PROJECT_ROOT`, and `TEST_DIR` exist. It calls production scripts directly. It does not mock `gh` here because these cases are mostly argument-validation paths intended to fail before network/API access or proceed only far enough not to produce the specific parse error.
- edge_cases_or_failure_modes: Valid `--claude`, `--codex`, and both-bot tests may still fail later due to missing git/PR context, but the assertion only checks the absence of “at least one bot flag” (`66-101`). Cancellation uses `CLAUDE_PROJECT_DIR` to isolate state lookup in the temp dir (`193-202`, `213-234`). Exit-code variables are normalized with `${exit_code:-0}` to handle successful commands.
- validation_or_tests: This file is included indirectly through PR-loop suite grouping in `tests/run-all-tests.sh` via `test-pr-loop-1-scripts.sh` (`86`) and related PR suites. It validates script surface behavior, not full GitHub polling.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-126 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Block template for the fail-closed git status gate in RLCR Stop hook. It defines the user-facing remediation when repository state cannot be verified.
- algorithmic_behavior: Template renders a `# Git Status Failed` block and substitutes `{{GIT_STATUS_EXIT}}` into the failed or timed-out git status message (`prompt-template/block/git-status-failed.md:1-10`). It is loaded by `loop-codex-stop-hook.sh` when cached `git status --porcelain` exits nonzero (`loop-codex-stop-hook.sh:467-487`).
- inputs_outputs_state: Input variable is `GIT_STATUS_EXIT`. Output is markdown used as the `reason` field of a JSON hook block decision (`loop-codex-stop-hook.sh:482-485`). No persistent state changes happen in the template itself; the caller may remove stale `.git/index.lock` before rendering (`473-477`).
- gates_or_invariants: The template supports a hard gate: if git status fails or times out, the loop cannot proceed to Codex review because clean/dirty state cannot be trusted. It instructs manual `git status` checking and names lock contention, invalid repository state, and large repository slowness as possible causes (`5-9`).
- dependencies_and_callers: Depends on `load_and_render_safe` single-pass substitution (`hooks/lib/template-loader.sh:170-203`). Caller is RLCR Stop hook’s git clean gate.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to the inline message in `loop-codex-stop-hook.sh:477-481`. If `GIT_STATUS_EXIT` is unset, the placeholder remains due to template renderer missing-variable behavior (`template-loader.sh:119-122`).
- validation_or_tests: Template existence/reference is covered by `tests/run-all-tests.sh` suites `test-template-loader.sh`, `test-template-references.sh`, `test-templates-comprehensive.sh`, and template robustness/error tests (`61`, `65`, `68`, `108`, `116`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-156 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Next-round prompt footer template that encodes exit/summary/commit instructions for RLCR continuation rounds.
- algorithmic_behavior: Adds separator, a warning not to fake exit by lying, editing loop state files, or executing cancel, then instructs optional code-simplifier usage, commit, and writing the work summary to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:1-9`). RLCR Stop appends it when building `round-N-prompt.md` after Codex found issues (`loop-codex-stop-hook.sh:1770-1775`).
- inputs_outputs_state: Input variable is `NEXT_SUMMARY_FILE`. Output is markdown appended to the generated next-round prompt. It indirectly influences later Stop-hook validation because the next exit must produce that summary file (`loop-codex-stop-hook.sh:678-714`).
- gates_or_invariants: Establishes the behavioral invariant that Claude must not bypass loop control by state-file editing or cancel execution, matching Bash/Write/Edit gates that block state mutation and direct hook execution (`loop-bash-validator.sh:78-81`, `151-168`; `loop-write-validator.sh:154-161`). It also requires commit before summary/exit, matching RLCR git-clean gate (`loop-codex-stop-hook.sh:578-643`).
- dependencies_and_callers: Rendered via `load_and_render_safe` and appended with task-tag routing notes and optional push/goal-tracker instructions (`loop-codex-stop-hook.sh:1770-1792`). Depends on next-round prompt generation after review content has been written.
- edge_cases_or_failure_modes: If missing, caller fallback is a shorter commit-and-summary instruction (`loop-codex-stop-hook.sh:1770-1774`). If `NEXT_SUMMARY_FILE` is not substituted, the prompt would still contain a placeholder, but renderer intentionally preserves missing placeholders rather than failing (`template-loader.sh:119-122`).
- validation_or_tests: Covered by template loader/reference/comprehensive tests in the global runner. Its instructions are enforced by hook gates for summary file presence and protected state/cancel behavior.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-186 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for `humanize_parse_goal_tracker` in `scripts/humanize.sh`, defining the monitor/goal-tracker parser’s output schema and tolerance to malformed or unusual markdown.
- algorithmic_behavior: Sources test helpers and production `scripts/humanize.sh` (`tests/robustness/test-goal-tracker-robustness.sh:18-24`), then calls `humanize_parse_goal_tracker` and splits its pipe-delimited result with `parse_result` into `total_acs`, `completed_acs`, `active_tasks`, `completed_tasks`, `deferred_tasks`, `open_issues`, and `goal_summary` (`35-50`). The production parser returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` and counts AC identifiers, active table rows minus completed/deferred, completed/deferred/open issue rows, and first non-placeholder ultimate-goal line (`scripts/humanize.sh:36-119`).
- inputs_outputs_state: Inputs are generated temp `goal-tracker*.md` fixtures in `TEST_DIR`, covering list/table AC formats, task tables, completed/deferred/open issue sections, missing files, empty files, large files, special characters, malformed headers, truncated content, binary bytes, mixed AC formatting, and long goal text. Outputs are pass/fail assertions and final exit from `print_test_summary` (`526-531`). It creates no persistent repo state.
- gates_or_invariants: Missing file must return exact default `0|0|0|0|0|0|No goal tracker` (`237-244`). Proper `### Acceptance Criteria` section is required for malformed files to count ACs (`304-326`). Truncated and binary-mixed files must still return parseable counts rather than crashing (`328-371`). Active task counts must exclude completed/deferred statuses (`118-153`). Completed ACs are unique even with multiple completed rows per AC (`155-202`). Parser must handle at least 60 AC items (`258-279`) and special shell/markdown characters in descriptions (`281-302`). Long goal summaries are returned whole; truncation is a display-layer concern (`501-524`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/humanize.sh`, `mktemp`, `sed`, `grep`, `cut`, `wc`, and shell utilities. Production parser also sources monitor/common and hook common libraries (`scripts/humanize.sh:6-16`), so this robustness test can expose parse regressions caused by shared-library startup.
- edge_cases_or_failure_modes: The test expects graceful behavior for empty, malformed, truncated, and binary-containing files. It notes mixed format behavior with bold ACs and sub-numbered ACs (`477-499`); production regex at `scripts/humanize.sh:58-59` currently supports decimal AC identifiers, so this test’s assertion is intentionally loose (`>=2`) rather than exact. Binary content is tolerated by requiring at least one AC, not exact total (`349-371`).
- validation_or_tests: Included in the top-level runner at `tests/run-all-tests.sh:104`. It contains 18 explicit tests and exits with the shared helper summary code (`tests/robustness/test-goal-tracker-robustness.sh:60-531`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 headings matched the 7 assigned rows; item IDs are intentionally present only in the item headings to keep the one-to-one cursor evidence unambiguous.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`