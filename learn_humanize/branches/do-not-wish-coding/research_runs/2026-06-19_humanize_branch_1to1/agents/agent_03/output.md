# agent_03 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`
- notes: Read-only research only. No files modified. No test suites executed.

## Item Evidence

### DO_NOT_WISH_CODING-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command workflow layer for the repository’s planning, RLCR implementation-review loop, PR review loop, and loop cancellation. The directory contains five command specs: `gen-plan.md`, `start-rlcr-loop.md`, `start-pr-loop.md`, `cancel-rlcr-loop.md`, and `cancel-pr-loop.md`; byte inventory totals 48,319 bytes.
- algorithmic_behavior: `commands/gen-plan.md` defines a strict sequential planning algorithm: no coding during plan generation (`commands/gen-plan.md:21`), mode/config setup, IO validation, relevance check, Codex first-pass critique, Claude candidate plan, iterative Claude/Codex convergence, user-decision consolidation, final plan structure, write/review/translation, and optional auto-start (`commands/gen-plan.md:34`, `commands/gen-plan.md:243`, `commands/gen-plan.md:289`, `commands/gen-plan.md:521`). `commands/start-rlcr-loop.md` defines pre-flight plan compliance, quiz, RLCR setup, Goal Tracker, BitLesson, two-phase implementation/review flow, and skip-implementation mode (`commands/start-rlcr-loop.md:14`, `commands/start-rlcr-loop.md:61`, `commands/start-rlcr-loop.md:110`, `commands/start-rlcr-loop.md:180`). `commands/start-pr-loop.md` defines PR bot-review orchestration with bot selection, comment priority, resolution summary, push/trigger, Stop Hook polling, and Codex validation (`commands/start-pr-loop.md:16`, `commands/start-pr-loop.md:25`, `commands/start-pr-loop.md:31`). The cancel commands are thin workflow wrappers around cancellation scripts (`commands/cancel-rlcr-loop.md:17`, `commands/cancel-pr-loop.md:17`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, plan/draft paths, bot flags, model/timeout flags, project config, and user choices via `AskUserQuestion`. Outputs are generated plan files, optional translated plan variants, setup-script invocations, PR/RLCR loop state directories, review prompts/summaries, and cancellation status messages. State is mostly delegated to `.humanize/rlcr/` and `.humanize/pr-loop/` via setup/cancel scripts rather than directly implemented in command markdown.
- gates_or_invariants: Frontmatter `allowed-tools` gates each command to the intended scripts/tools. `gen-plan` enforces “no coding” and sequential phases (`commands/gen-plan.md:21`, `commands/gen-plan.md:36`). It also requires explicit plan convergence and no pending user decisions before optional auto-start (`commands/gen-plan.md:584`). `start-rlcr-loop` fail-closes malformed compliance output and branch-switch/relevance failures (`commands/start-rlcr-loop.md:53`). PR/RLCR cancellation wrappers declare that scripts own all cancellation logic (`commands/cancel-rlcr-loop.md:36`, `commands/cancel-pr-loop.md:21`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/cancel-pr-loop.sh`, plus `Read`, `Write`, `Glob`, `Grep`, `Task`, and `AskUserQuestion` as declared in command frontmatter. The command layer coordinates with hook templates under `prompt-template/`, loop state helpers under `hooks/lib/loop-common.sh`, and stop hooks for loop progression.
- edge_cases_or_failure_modes: `gen-plan` handles direct/discussion conflict, unsupported alternative plan language, malformed config JSON, Codex unavailability, unresolved decisions, malformed quiz output, mixed-language plan content, and failed auto-start (`commands/gen-plan.md:58`, `commands/gen-plan.md:106`, `commands/gen-plan.md:114`, `commands/gen-plan.md:198`, `commands/gen-plan.md:629`). `start-rlcr-loop` skips pre-checks for `--skip-impl`/help and quiz for `--yolo`/`--skip-quiz` (`commands/start-rlcr-loop.md:18`, `commands/start-rlcr-loop.md:65`). Cancel during RLCR Finalize Phase requires explicit confirmation in the wrapper flow (`commands/cancel-rlcr-loop.md:21`).
- validation_or_tests: Command behavior is covered indirectly by `tests/test-gen-plan.sh`, `tests/test-plan-file-validation.sh`, `tests/test-finalize-phase.sh`, `tests/test-pr-loop-3-stophook.sh`, and related robustness suites. `tests/test-gen-plan.sh` verifies command file metadata, English-only content, gen-plan template consistency, and `validate-gen-plan-io.sh` behavior (`tests/test-gen-plan.sh:557`, `tests/test-gen-plan.sh:708`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-033 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop, including plan validation, user-understanding checks, setup delegation, and the operator-facing contract for loop phases.
- algorithmic_behavior: Phase zero extracts the plan path from `$ARGUMENTS`, skipping pre-checks for `--skip-impl` and help (`commands/start-rlcr-loop.md:18`). It performs path-safety screening before reading a plan (`commands/start-rlcr-loop.md:29`) and invokes a plan-compliance agent that must return `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`commands/start-rlcr-loop.md:42`). The parse is fail-closed: relevance or branch-switch failures stop the command, and malformed output stops as unexpected (`commands/start-rlcr-loop.md:53`). A second advisory quiz agent can generate two multiple-choice questions; wrong answers trigger a proceed-or-stop user decision (`commands/start-rlcr-loop.md:76`, `commands/start-rlcr-loop.md:101`). If gates pass or are skipped, it invokes `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:112`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, optional plan path, flags such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--agent-teams`, `--yolo`, and `--skip-quiz` (`commands/start-rlcr-loop.md:3`). Output is the setup-script call plus an active RLCR session. The delegated setup script creates `.humanize/rlcr/<timestamp>/state.md` with round, max-iteration, Codex, branch, plan, review, session, agent-team, and BitLesson fields (`scripts/setup-rlcr-loop.sh:842`) and a pending session-id signal (`scripts/setup-rlcr-loop.sh:867`).
- gates_or_invariants: Plan file pre-check refuses obviously irrelevant plans and branch-switching instructions before setup (`commands/start-rlcr-loop.md:55`). Quiz is advisory, not an automatic hard block unless the user chooses to stop (`commands/start-rlcr-loop.md:63`, `commands/start-rlcr-loop.md:105`). Runtime setup gates include dependency checks for `jq`/`git`, mutual exclusion with active RLCR or PR loops, Git repository/commit checks, relative non-symlink plan path, clean working tree, YAML-safe branch/model fields, local base branch resolution, and sufficient plan content (`scripts/setup-rlcr-loop.sh:300`, `scripts/setup-rlcr-loop.sh:324`, `scripts/setup-rlcr-loop.sh:399`, `scripts/setup-rlcr-loop.sh:421`, `scripts/setup-rlcr-loop.sh:584`, `scripts/setup-rlcr-loop.sh:654`, `scripts/setup-rlcr-loop.sh:685`).
- dependencies_and_callers: Declares `scripts/setup-rlcr-loop.sh`, `Read`, `Task`, and `AskUserQuestion` in frontmatter (`commands/start-rlcr-loop.md:4`). It coordinates with `/humanize:ask-codex` for `analyze` tasks, Codex review during Review Phase, `.humanize/bitlesson.md`, and hook-managed state/summary files (`commands/start-rlcr-loop.md:120`, `commands/start-rlcr-loop.md:160`).
- edge_cases_or_failure_modes: Unsafe plan paths skip the pre-check and let setup enforce validation (`commands/start-rlcr-loop.md:36`). Missing plan path also delegates error handling unless `--skip-impl` is active (`commands/start-rlcr-loop.md:27`, `scripts/setup-rlcr-loop.sh:374`). Malformed quiz output warns and continues (`commands/start-rlcr-loop.md:87`). In `--skip-impl`, setup creates a placeholder plan and starts directly in review mode (`scripts/setup-rlcr-loop.sh:792`, `scripts/setup-rlcr-loop.sh:878`).
- validation_or_tests: Covered by `tests/test-plan-file-validation.sh` for setup path/content/model gates, `tests/robustness/test-setup-scripts-robustness.sh` for setup edge cases, `tests/test-task-tag-routing.sh` for task routing, `tests/test-finalize-phase.sh` for review/finalize transitions, and stop-hook tests for iterative behavior.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-063 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Side-effect-free IO validator for `/humanize:gen-plan`; it turns CLI argument/path problems into deterministic exit codes consumed by `commands/gen-plan.md`.
- algorithmic_behavior: Parses `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, `--direct`, and help (`scripts/validate-gen-plan-io.sh:35`). It rejects missing option values, unknown flags, and mutually exclusive `--discussion`/`--direct` (`scripts/validate-gen-plan-io.sh:76`). It resolves absolute-ish paths with `realpath -m` fallback, prints the input/output/dir, validates input file existence and non-empty content, validates output directory existence, rejects output directories or existing output files, checks write permission, then locates the plan template using `CLAUDE_PLUGIN_ROOT` or a script-relative fallback (`scripts/validate-gen-plan-io.sh:98`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:132`, `scripts/validate-gen-plan-io.sh:147`, `scripts/validate-gen-plan-io.sh:162`).
- inputs_outputs_state: Inputs are command-line arguments and filesystem state. Outputs are stdout diagnostics, `VALIDATION_SUCCESS`, `TEMPLATE_FILE: <path>`, and exit codes. It does not create the output plan and explicitly notes side-effect-free behavior in the calling command (`commands/gen-plan.md:138`).
- gates_or_invariants: Exit code contract is documented at the top: success `0`, input missing `1`, input empty `2`, output dir missing `3`, output exists or is directory `4`, no write permission `5`, invalid args `6`, template missing `7` (`scripts/validate-gen-plan-io.sh:4`). Required `--input`/`--output` are hard gates (`scripts/validate-gen-plan-io.sh:82`). Direct mode with auto-start is accepted but only emits a note because auto-start convergence is later handled by `gen-plan` (`scripts/validate-gen-plan-io.sh:93`).
- dependencies_and_callers: Called from `commands/gen-plan.md` Phase 1 (`commands/gen-plan.md:120`). Depends on POSIX shell tools: `realpath`, `dirname`, `wc`, `tr`, file tests, and the plugin template `prompt-template/plan/gen-plan-template.md`.
- edge_cases_or_failure_modes: `-h`/`--help` exits via `usage` with code `6`, which tests intentionally expect. `realpath -m` failure falls back to the raw path (`scripts/validate-gen-plan-io.sh:99`). Template lookup happens after IO validation, so misconfigured plugin template produces exit `7` only after valid input/output paths. It treats an output path that is a directory as output-exists class `4` (`scripts/validate-gen-plan-io.sh:132`).
- validation_or_tests: `tests/test-gen-plan.sh` directly exercises missing values, flag-as-value, unknown flag, missing input, empty input, output directory missing, output existing, output path directory, valid paths, auto-start flag acceptance, discussion/direct recognition, mutual exclusion, and help exit behavior (`tests/test-gen-plan.sh:557` through `tests/test-gen-plan.sh:705`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-093 `file` `tests/test-pr-loop-3-stophook.sh`
- cursor: `[_]`
- core_role: Executable test-runner shard for the PR loop Stop Hook integration suite; this is split 3/3 and focuses only on the slow stop-hook module.
- algorithmic_behavior: Sets strict shell mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources common test helpers and PR-loop library, skips in GitHub Actions, initializes the PR loop test environment, sources `tests/test-pr-loop-stophook.sh`, runs `run_stophook_tests`, prints a summary, and exits with the aggregated result (`tests/test-pr-loop-3-stophook.sh:10`, `tests/test-pr-loop-3-stophook.sh:15`, `tests/test-pr-loop-3-stophook.sh:18`, `tests/test-pr-loop-3-stophook.sh:24`, `tests/test-pr-loop-3-stophook.sh:26`, `tests/test-pr-loop-3-stophook.sh:28`).
- inputs_outputs_state: Inputs are local test scripts, mocked Git/GitHub fixtures created by the sourced test file, and environment such as `GITHUB_ACTIONS`. Outputs are pass/fail/skip lines via helper functions and an exit code from `print_test_summary`. Test state is created under the helper-managed temporary environment rather than under production `.humanize/`.
- gates_or_invariants: In CI (`GITHUB_ACTIONS=true`), the shard marks the whole module skipped and exits cleanly because the module is slow and timeout-driven (`tests/test-pr-loop-3-stophook.sh:18`). It requires `test-helpers.sh` and `test-pr-loop-lib.sh` to load successfully before initialization (`tests/test-pr-loop-3-stophook.sh:15`).
- dependencies_and_callers: Sourced implementation is `tests/test-pr-loop-stophook.sh`, whose `run_stophook_tests` covers force-push trigger rejection, startup-case exceptions, approve-state creation, unpushed commit blocking, force-push detection, missing trigger blocking, bot timeout auto-remove, Codex thumbs-up approval, Claude eyes timeout, dynamic startup-case update, fork PR base repo resolution, and mixed bot approval/issue handling (`tests/test-pr-loop-stophook.sh:1768`).
- edge_cases_or_failure_modes: The runner itself is a thin wrapper, so most failure modes are dependency/setup failures or CI skip. The underlying tests mock stale trigger comments after force push (`tests/test-pr-loop-stophook.sh:20`), bot timeout/approval cases, fork PR lookup, and mixed approval where one bot approves and another still has issues.
- validation_or_tests: This file is itself validation infrastructure and is included by aggregate runners such as `tests/run-all-tests.sh` and `tests/test-pr-loop.sh` per comments in the broader suite. It does not directly implement PR-loop logic; it executes the stop-hook specification module.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-123 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Block-message template fragment used when the RLCR stop hook detects non-humanize untracked files in a dirty working tree.
- algorithmic_behavior: The template is static guidance: untracked files may be artifacts, test outputs, runtime files, dependencies, IDE files, logs, caches, or temp files, and should usually be added to `.gitignore` rather than committed (`prompt-template/block/git-not-clean-untracked.md:2`). It has no placeholders and no script logic.
- inputs_outputs_state: Input is the stop hook’s dirty-git classification. Output is appended human-readable note text inside a larger block reason. It does not mutate state.
- gates_or_invariants: It supports the invariant that loop exits should not proceed with an unclean working tree. The actual gate is in `hooks/loop-codex-stop-hook.sh`: `.humanize/` untracked state is filtered out, other untracked files trigger loading this template, and a fallback is used if the template is missing (`hooks/loop-codex-stop-hook.sh:590`, `hooks/loop-codex-stop-hook.sh:608`, `hooks/loop-codex-stop-hook.sh:611`).
- dependencies_and_callers: Called through the template loader from `hooks/loop-codex-stop-hook.sh`. Related setup-time clean-tree enforcement lives in `scripts/setup-rlcr-loop.sh`, which blocks non-clean status after filtering untracked `.humanize/` entries (`scripts/setup-rlcr-loop.sh:685`).
- edge_cases_or_failure_modes: Since the template is static, failure mode is missing-template fallback rather than malformed interpolation. It is advisory text inside a stronger dirty-tree block; it does not distinguish safe untracked source files from artifacts beyond broad categories.
- validation_or_tests: Dirty git and untracked file behavior is exercised by `tests/robustness/test-git-operations-robustness.sh`, and template loading/reference behavior is covered by template-loader/template-reference tests in the suite.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-153 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing prompt used after Codex code review passes and the RLCR loop transitions into Finalize Phase.
- algorithmic_behavior: Instructs Claude that implementation is complete, asks it to use `code-simplifier:code-simplifier` via `Task`, and limits work to simplification/refactoring without behavior change (`prompt-template/claude/finalize-phase-prompt.md:1`, `prompt-template/claude/finalize-phase-prompt.md:9`, `prompt-template/claude/finalize-phase-prompt.md:16`). It directs focus to recent changes, especially the diff from `{{BASE_BRANCH}}` to `{{START_BRANCH}}`, and references the original plan and goal tracker (`prompt-template/claude/finalize-phase-prompt.md:27`, `prompt-template/claude/finalize-phase-prompt.md:38`).
- inputs_outputs_state: Inputs are template variables `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`. Output is prompt text returned in a stop-hook block decision. Runtime state transition happens in `enter_finalize_phase`: `state.md` is renamed to `finalize-state.md`, `finalize-summary.md` is created if absent, this template is rendered, and a block JSON is returned (`hooks/loop-codex-stop-hook.sh:1119`, `hooks/loop-codex-stop-hook.sh:1125`, `hooks/loop-codex-stop-hook.sh:1128`, `hooks/loop-codex-stop-hook.sh:1203`).
- gates_or_invariants: Non-negotiable constraints are no functionality changes, no failing existing tests, no new bugs, and only functionality-equivalent changes (`prompt-template/claude/finalize-phase-prompt.md:16`). Before exiting, Claude must complete tasks, commit changes, and write the finalize summary (`prompt-template/claude/finalize-phase-prompt.md:41`). On a later stop attempt, if checks pass in Finalize Phase, the stop hook skips Codex review and renames `finalize-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:831`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` after `run_and_handle_code_review` detects no review issues (`hooks/loop-codex-stop-hook.sh:1113`). It depends on the optional `code-simplifier:code-simplifier` agent and on loop-managed plan/goal tracker/finalize summary files.
- edge_cases_or_failure_modes: If code review is skipped, the hook uses `prompt-template/claude/finalize-phase-skipped-prompt.md` instead (`hooks/loop-codex-stop-hook.sh:1148`). Finalize-state files are protected by write/edit/bash validators; finalize summary is specifically allowed during Finalize Phase. If finalize summary or todos are incomplete, separate stop-hook gates block completion.
- validation_or_tests: `tests/test-finalize-phase.sh` covers Finalize Phase entry on COMPLETE, `state.md` to `finalize-state.md`, finalize summary handling, protection of `finalize-state.md`, completion to `complete-state.md`, max-iteration bypass, code-review failure/empty-output blocking, and incomplete-todo blocking.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-183 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable security specification for cancel authorization, specifically the `is_cancel_authorized` predicate in `hooks/lib/loop-common.sh`.
- algorithmic_behavior: Creates a mock loop directory with `state.md`, then directly calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` across positive and negative command strings (`tests/robustness/test-cancel-security-robustness.sh:27`, `tests/robustness/test-cancel-security-robustness.sh:39`). Positive cases require `.cancel-requested` and accept valid `mv state.md cancel-state.md`, valid `mv finalize-state.md cancel-state.md`, single quotes, and unquoted paths (`tests/robustness/test-cancel-security-robustness.sh:39`, `tests/robustness/test-cancel-security-robustness.sh:51`, `tests/robustness/test-cancel-security-robustness.sh:65`, `tests/robustness/test-cancel-security-robustness.sh:78`).
- inputs_outputs_state: Inputs are constructed shell command strings, mock state files, `.cancel-requested`, symlinks, and loop paths. Outputs are helper `pass`/`fail` records and final test summary (`tests/robustness/test-cancel-security-robustness.sh:411`). State is temporary test filesystem state only; it does not call the production cancel scripts.
- gates_or_invariants: The tested predicate requires the cancel signal file, rejects command substitution/backticks/newlines/shell operators/remaining variables, requires an `mv` command with exactly two args, enforces exact source `state.md` or `finalize-state.md`, exact destination `cancel-state.md`, rejects mixed quote styles, rejects multiple trailing spaces, and rejects symlinked source files (`hooks/lib/loop-common.sh:722`, `hooks/lib/loop-common.sh:727`, `hooks/lib/loop-common.sh:737`, `hooks/lib/loop-common.sh:756`, `hooks/lib/loop-common.sh:761`, `hooks/lib/loop-common.sh:799`, `hooks/lib/loop-common.sh:846`, `hooks/lib/loop-common.sh:861`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-cancel-security-robustness.sh:17`). The production cancel scripts create `.cancel-requested` and rename active state files (`scripts/cancel-rlcr-loop.sh:148`, `scripts/cancel-rlcr-loop.sh:155`, `scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`); hook/bash validators rely on `is_cancel_authorized` to distinguish legitimate cancel operations from state-file tampering.
- edge_cases_or_failure_modes: Negative cases include missing signal, `$(...)`, backticks, `;`, `&&`, pipes, wrong source/destination, extra args, newline injection, `${HOME}`/`${IFS}`, non-`mv`, `||`, mixed quotes, multiple trailing spaces, non-standard source filename, source symlinks, and regression cases where the loop directory path itself contains `finalize` (`tests/robustness/test-cancel-security-robustness.sh:99`, `tests/robustness/test-cancel-security-robustness.sh:109`, `tests/robustness/test-cancel-security-robustness.sh:135`, `tests/robustness/test-cancel-security-robustness.sh:226`, `tests/robustness/test-cancel-security-robustness.sh:278`, `tests/robustness/test-cancel-security-robustness.sh:332`, `tests/robustness/test-cancel-security-robustness.sh:369`). Path normalization with `/./` is explicitly accepted (`tests/robustness/test-cancel-security-robustness.sh:265`).
- validation_or_tests: This file is the validation artifact. Its coverage maps tightly to `is_cancel_authorized` return classes and documents a previous substring-match symlink bypass concern fixed by exact path comparison (`hooks/lib/loop-common.sh:861`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 unique assigned item headings present, each counted once and in assigned order
- missing_items: none
- duplicate_items: none by heading count
- final_worker_status: `complete`