# agent_20 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-020 `directory` `skills/ask-codex`
- cursor: `[_]`
- core_role: Defines the Claude-facing `ask-codex` skill wrapper for one-shot Codex consultation. The directory contains one child file, `skills/ask-codex/SKILL.md`, and coordinates with `scripts/ask-codex.sh` as its only allowed execution surface.
- algorithmic_behavior: The skill instructs callers to pass free-form task text as a quoted single argument when no flags are present, and to reconstruct flagged invocations with flags separated from the final quoted prompt. This is a routing and shell-safety contract: `SKILL.md:14-22` warns that user text may contain shell metacharacters, while `SKILL.md:30-36` explicitly forbids bare `$ARGUMENTS` expansion because the shell would re-parse the prompt before `ask-codex.sh` receives it. Output interpretation is also specified: stdout is Codex’s answer, stderr is status, and non-zero exits must be reported (`SKILL.md:38-51`).
- inputs_outputs_state: Inputs are optional `--codex-model MODEL:EFFORT`, optional `--codex-timeout SECONDS`, and one question/task string (`SKILL.md:4`, `SKILL.md:16-28`). Output is the stdout returned by `scripts/ask-codex.sh`; persistent state is `.humanize/skill/<timestamp>/output.md` (`SKILL.md:55-56`). The backing script stores richer state in `.humanize/skill/<unique-id>` and cache files, as shown in `scripts/ask-codex.sh:15-17`, writes input metadata at `scripts/ask-codex.sh:224-237`, runs `codex exec` at `scripts/ask-codex.sh:295-297`, and emits saved output at `scripts/ask-codex.sh:403-410`.
- gates_or_invariants: The key invariant is no unquoted free-form shell expansion. The skill’s `allowed-tools` restricts execution to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh:*)` (`SKILL.md:5`). The backing script validates Codex availability, non-empty question, safe model characters, and safe effort characters (`scripts/ask-codex.sh:153-185`). Timeout exit code `124` maps to a retry recommendation (`SKILL.md:46-51`; `scripts/ask-codex.sh:308-315`).
- dependencies_and_callers: Documentation references the skill in `docs/usage.md` and `docs/install-for-claude.md`; task routing sends analyze tasks through `/humanize:ask-codex` in `commands/start-rlcr-loop.md` and `prompt-template/plan/gen-plan-template.md`. Runtime depends on `scripts/ask-codex.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh` for config-backed defaults (`scripts/ask-codex.sh:26-43`). The monitor path observes `.humanize/skill` via `scripts/lib/monitor-skill.sh:12-20`.
- edge_cases_or_failure_modes: Unsafe prompt text containing `(`, `)`, `;`, `#`, `*`, or `[` is the primary edge case addressed by the quoted invocation requirement (`SKILL.md:14-22`). Script-level failures include missing Codex, empty question, invalid flags/model/effort, timeout, empty response, and Codex process errors (`SKILL.md:46-51`; `scripts/ask-codex.sh:153-185`, `scripts/ask-codex.sh:308-315`). Cache fallback handles home cache write failures by using project-local cache (`scripts/ask-codex.sh:210-218`).
- validation_or_tests: `tests/test-ask-codex.sh` is the direct executable spec for this skill/script pair. It uses a mock `codex` binary (`tests/test-ask-codex.sh:39-53`), runs the script inside a mock project (`tests/test-ask-codex.sh:67-75`), verifies cache artifacts (`tests/test-ask-codex.sh:388-405`), and checks the skill guidance for both unsafe bare `$ARGUMENTS` warning and safe quoted invocation (`tests/test-ask-codex.sh:415-425`). `tests/run-all-tests.sh:93-94` includes this test in the suite.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-050 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Implements the BitLesson Delta validation gate for non-finalize RLCR stop-hook rounds. It enforces that each round summary declares whether the reusable lesson knowledge base was unchanged, added to, or updated, and checks consistency against the BitLesson file.
- algorithmic_behavior: The script parses required CLI arguments (`scripts/bitlesson-validate-delta.sh:24-60`), validates presence of the summary file (`scripts/bitlesson-validate-delta.sh:62-72`), loads the template system (`scripts/bitlesson-validate-delta.sh:74-76`), and emits JSON block decisions through `block_exit` (`scripts/bitlesson-validate-delta.sh:78-90`). It requires a `## BitLesson Delta` section (`scripts/bitlesson-validate-delta.sh:92-109`), extracts the section with awk (`scripts/bitlesson-validate-delta.sh:111-116`), extracts exactly one valid `Action` of `none`, `add`, or `update` (`scripts/bitlesson-validate-delta.sh:118-134`), and then validates lesson ID semantics for the chosen action.
- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` (`scripts/bitlesson-validate-delta.sh:4-14`). Output is either no stdout and exit `0` for success, or a JSON object with `"decision": "block"`, a rendered reason, and `systemMessage` (`scripts/bitlesson-validate-delta.sh:78-90`). It reads but does not mutate the BitLesson file. It computes `CONCRETE_BITLESSON_COUNT` by counting non-placeholder `Lesson ID:` entries (`scripts/bitlesson-validate-delta.sh:143-156`).
- gates_or_invariants: Required section gate: missing `## BitLesson Delta` blocks (`scripts/bitlesson-validate-delta.sh:92-109`). Action gate: exactly one valid action blocks otherwise (`scripts/bitlesson-validate-delta.sh:118-134`). `Action: none` requires no concrete IDs or `NONE` and may be forbidden when the KB has no concrete entries unless `--allow-empty-none true` (`scripts/bitlesson-validate-delta.sh:158-183`). `add` or `update` requires non-`NONE` concrete IDs, an existing BitLesson file, valid ID format `BL-YYYYMMDD-short-name`, and matching entries in the file (`scripts/bitlesson-validate-delta.sh:185-282`).
- dependencies_and_callers: The RLCR stop hook invokes this script only when not in finalize phase and BitLesson is required (`hooks/loop-codex-stop-hook.sh:716-735`). It depends on `hooks/lib/template-loader.sh`, specifically `load_and_render_safe`, which provides template fallback behavior and single-pass placeholder rendering (`hooks/lib/template-loader.sh:167-203`). It uses block templates such as `block/bitlesson-delta-missing.md`, `block/bitlesson-delta-invalid.md`, `block/bitlesson-delta-inconsistent.md`, and `block/bitlesson-delta-empty-kb.md` (`scripts/bitlesson-validate-delta.sh:107`, `scripts/bitlesson-validate-delta.sh:132`, `scripts/bitlesson-validate-delta.sh:166-181`).
- edge_cases_or_failure_modes: Missing required arguments or missing summary file exit `1` (`scripts/bitlesson-validate-delta.sh:62-72`). Missing templates fall back to inline markdown via `load_and_render_safe`. Multiple `Action` lines, absent action, invalid action, non-`NONE` IDs with `none`, missing IDs with `add` or `update`, invalid comma-delimited IDs, missing BitLesson file, and IDs declared in the summary but absent from the KB all block with JSON (`scripts/bitlesson-validate-delta.sh:122-134`, `scripts/bitlesson-validate-delta.sh:158-183`, `scripts/bitlesson-validate-delta.sh:185-282`).
- validation_or_tests: No direct assigned test file targets BitLesson Delta, but the caller path is covered through stop-hook behavior. The repository search shows the stop hook treats a non-empty validator output as the final hook response (`hooks/loop-codex-stop-hook.sh:721-734`). Template loader behavior is validated indirectly by comprehensive template and stop-hook tests listed in `tests/run-all-tests.sh:65-73`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-080 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state machine. It verifies transitions among normal loop, review, finalize, completion, and max-iteration terminal states, plus validator protections around `finalize-state.md`.
- algorithmic_behavior: The test creates an isolated Git repo and RLCR loop directory, mocks `codex exec` and `codex review`, sources `hooks/lib/loop-common.sh`, and drives real hook scripts with JSON hook input (`tests/test-finalize-phase.sh:40-178`). It first validates helper functions for finalize path recognition (`tests/test-finalize-phase.sh:238-280`), then checks active-loop detection for `finalize-state.md` and non-detection for `complete-state.md` (`tests/test-finalize-phase.sh:282-335`). It executes write/edit/bash validators against finalize-state protection (`tests/test-finalize-phase.sh:337-416`) and runs `hooks/loop-codex-stop-hook.sh` across finalize and normal phases (`tests/test-finalize-phase.sh:417-865`).
- inputs_outputs_state: Inputs are generated loop state files, goal tracker, round summaries, finalize summary, transcript JSONL, and mocked Codex outputs. State transitions under test include `state.md` renamed to `finalize-state.md` on clean `COMPLETE` review (`tests/test-finalize-phase.sh:524-540`), `finalize-state.md` renamed to `complete-state.md` when finalize checks pass (`tests/test-finalize-phase.sh:476-496`), and `state.md` renamed to `maxiter-state.md` when max iterations are reached before finalize (`tests/test-finalize-phase.sh:542-560`). Outputs are PASS/FAIL counters and exit code equal to failed test count (`tests/test-finalize-phase.sh:867-876`).
- gates_or_invariants: Finalize phase must require `finalize-summary.md` (`tests/test-finalize-phase.sh:429-444`), clean Git state (`tests/test-finalize-phase.sh:454-471`), and completed todos in transcript (`tests/test-finalize-phase.sh:698-741`). It must not invoke Codex during finalize checks or finalize completion (`tests/test-finalize-phase.sh:446-504`, `tests/test-finalize-phase.sh:735-741`). `finalize-state.md` is system-managed and blocked for Write, Edit, Bash redirection, and Bash `mv`/`cp` source operations (`tests/test-finalize-phase.sh:345-416`). Normal RLCR rounds must remain unaffected: non-`COMPLETE` review blocks with feedback, keeps `state.md`, increments the round, and writes review result (`tests/test-finalize-phase.sh:743-814`).
- dependencies_and_callers: Tests production functions from `hooks/lib/loop-common.sh`, including `resolve_active_state_file`, `find_active_loop`, `is_finalize_state_file_path`, `is_finalize_summary_path`, and `finalize_state_file_blocked_message` (`hooks/lib/loop-common.sh:212-226`, `hooks/lib/loop-common.sh:637-689`). Exercises `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The stop hook detects finalize phase at `hooks/loop-codex-stop-hook.sh:68-82`, skips BitLesson and goal-tracker checks in finalize where appropriate (`hooks/loop-codex-stop-hook.sh:716-746`), completes finalize by renaming state to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:830-842`), and enters finalize after review success (`hooks/loop-codex-stop-hook.sh:1088-1126`).
- edge_cases_or_failure_modes: Codex review failure after `COMPLETE` must block and preserve `state.md` with `review_started: true` (`tests/test-finalize-phase.sh:562-620`). Empty Codex review stdout must block, preserve state, and create an empty review log (`tests/test-finalize-phase.sh:621-696`). Max iterations skip finalize (`tests/test-finalize-phase.sh:542-560`). Validators must parse `finalize-state.md` correctly even when `state.md` is absent (`tests/test-finalize-phase.sh:816-865`).
- validation_or_tests: This file is itself a validation asset and is included in the full suite at `tests/run-all-tests.sh:73`. I inspected it directly but did not execute it in this read-only branch export.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-110 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for shell-compatibility safety in the humanize monitor path, especially preventing zsh `nomatch` glob failures when directories are empty or contain only dotfiles.
- algorithmic_behavior: The test must run under zsh (`tests/test-zsh-monitor-safety.sh:1-13`) and creates `/tmp/test-zsh-humanize-$$` as an isolated workspace (`tests/test-zsh-monitor-safety.sh:48-60`). It sources `scripts/humanize.sh` under zsh and checks that no `no matches found` error appears (`tests/test-zsh-monitor-safety.sh:62-77`). It then models monitor discovery routines using `find` loops rather than shell globs for session discovery, state-file discovery, cache-log discovery, and session iteration (`tests/test-zsh-monitor-safety.sh:79-273`).
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr` directory layouts: empty loop dir, dotfile-only dir, session dir with no `*-state.md`, empty cache dir, and multiple valid timestamp session dirs. Outputs are PASS/FAIL counters and final exit code `0` or `1` (`tests/test-zsh-monitor-safety.sh:323-341`). It temporarily mutates only the isolated test base and removes it on exit (`tests/test-zsh-monitor-safety.sh:56-60`).
- gates_or_invariants: Monitor code must avoid raw unmatched glob expansion in zsh. The invariant is that discovery paths use `find ... 2>/dev/null` plus empty-line and type checks, as shown in the tested patterns for latest session (`tests/test-zsh-monitor-safety.sh:99-115`), dotfile-only directories (`tests/test-zsh-monitor-safety.sh:136-157`), state files (`tests/test-zsh-monitor-safety.sh:172-205`), cache logs (`tests/test-zsh-monitor-safety.sh:217-235`), and full session iteration (`tests/test-zsh-monitor-safety.sh:250-273`). Test 8 contrasts the old glob pattern with the new `find` pattern (`tests/test-zsh-monitor-safety.sh:288-321`).
- dependencies_and_callers: The test sources `scripts/humanize.sh`, whose monitor command exposes `humanize monitor rlcr`, `pr`, and `skill` (`scripts/humanize.sh:1120-1148`). Production monitor functions call shared helpers such as `monitor_find_latest_session`, `_find_latest_codex_log`, and `monitor_find_state_file`; direct references in `scripts/humanize.sh:246-345` show the monitored discovery surfaces. The skill monitor applies zsh-specific local options `ksharrays no_monitor` and uses `find` for skill invocation directories (`scripts/lib/monitor-skill.sh:15-18`, `scripts/lib/monitor-skill.sh:42-53`, `scripts/lib/monitor-skill.sh:82-104`).
- edge_cases_or_failure_modes: Empty directories, hidden files only, missing state files, empty cache directories, and zsh strict glob behavior are explicit edge cases (`tests/test-zsh-monitor-safety.sh:80-235`). The test also asserts the latest timestamp session is selected lexically when multiple valid session directories exist (`tests/test-zsh-monitor-safety.sh:246-273`). It verifies zsh is actually the executing shell via `$ZSH_VERSION` (`tests/test-zsh-monitor-safety.sh:275-286`).
- validation_or_tests: This is included in the normal suite at `tests/run-all-tests.sh:77` and appears again in the monitor-focused group at `tests/run-all-tests.sh:124` in the searched output. I inspected it directly but did not execute it in this read-only branch export.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-140 `file` `prompt-template/block/stop-hook-direct-execution.md`
- cursor: `[_]`
- core_role: Prompt/block template for the active-loop Bash validator’s direct-hook-execution guard. It tells the user that manually running hook scripts through Bash is blocked because hooks are managed by the hook system.
- algorithmic_behavior: The template is static markdown with a title and instruction text (`prompt-template/block/stop-hook-direct-execution.md:1-7`). It becomes the rendered blocking message through `stop_hook_direct_execution_blocked_message`, which uses `load_and_render_safe` and this template path (`hooks/lib/loop-common.sh:1233-1245`). The loader preserves fallback behavior if the template is missing or empty (`hooks/lib/template-loader.sh:167-203`).
- inputs_outputs_state: It has no runtime inputs and no persistent state. Its output is human-readable block text used as stderr or hook block reason by validator logic. The relevant fallback text mirrors the file content in `hooks/lib/loop-common.sh:1236-1242`.
- gates_or_invariants: The invariant is that users should not manually invoke managed hook scripts during an active loop; they should complete their response and let the hooks system run automatically (`prompt-template/block/stop-hook-direct-execution.md:3-7`). This prevents bypassing the intended Stop hook lifecycle or double-running stateful hook scripts.
- dependencies_and_callers: Directly loaded by `hooks/lib/loop-common.sh:1233-1245`. The broader enforcement is in Bash validator logic; the same validator file handles loop-file protection and command blocking when an active loop exists (`hooks/loop-bash-validator.sh:132-170`). Search found no other direct template callers.
- edge_cases_or_failure_modes: If the template is absent, `load_and_render_safe` supplies the inline fallback and still blocks (`hooks/lib/template-loader.sh:179-186`). Because this template has no variables, placeholder injection is not a concern here; template rendering is single-pass anyway (`hooks/lib/template-loader.sh:50-58`).
- validation_or_tests: It is indirectly covered by stop-gate, template reference, and comprehensive template tests in the suite (`tests/run-all-tests.sh:65-73`). I inspected the concrete template and loader/caller path.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-170 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR loop goal-tracker template. It seeds the PR review loop’s progress ledger with immutable run metadata, the approval goal, issue statistics, and an initial Round 0 issue log.
- algorithmic_behavior: The template renders variables for PR number, branch, start time, monitored bots, and startup case (`prompt-template/pr-loop/goal-tracker-initial.md:3-13`). It initializes issue summary and total statistics to zero (`prompt-template/pr-loop/goal-tracker-initial.md:15-25`) and records Round 0 as “Awaiting initial reviews” (`prompt-template/pr-loop/goal-tracker-initial.md:27-33`). `scripts/setup-pr-loop.sh` renders it with `load_and_render_safe` and writes the result to `goal-tracker.md` (`scripts/setup-pr-loop.sh:712-715`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, and `STARTUP_CASE`. Output is the initial PR loop `goal-tracker.md` file. The fallback goal tracker in `setup-pr-loop.sh` is richer and includes acceptance-criteria rows and status tables (`scripts/setup-pr-loop.sh:680-710`), but the assigned template is the actual preferred file loaded at setup (`scripts/setup-pr-loop.sh:712-713`).
- gates_or_invariants: It establishes the PR loop objective: “Get all monitored bot reviewers ... to approve this PR” (`prompt-template/pr-loop/goal-tracker-initial.md:11-13`). It also initializes issue counts to zero, so later PR loop rounds can increment found/resolved/remaining counts from a known baseline (`prompt-template/pr-loop/goal-tracker-initial.md:21-25`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` during PR loop setup after active bot metadata and fallback tracker content have been assembled (`scripts/setup-pr-loop.sh:680-715`). Rendering depends on `hooks/lib/template-loader.sh` single-pass variable replacement (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-203`). `tests/test-pr-loop-system.sh` references this template path directly in its system coverage (`tests/test-pr-loop-system.sh:644` from search output).
- edge_cases_or_failure_modes: Missing variables remain as placeholders because the template renderer leaves unresolved `{{VAR}}` intact (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:119-122`). Missing or empty template content falls back to the inline tracker in `setup-pr-loop.sh` (`hooks/lib/template-loader.sh:179-199`; `scripts/setup-pr-loop.sh:680-713`). The assigned template lacks the richer acceptance-criteria table from the fallback, which could make it a compatibility-sensitive surface if tests expect fallback-only sections.
- validation_or_tests: PR loop tests are included in the full test suite (`tests/run-all-tests.sh:86-89`), and `tests/test-pr-loop-system.sh` references the template path. I inspected the assigned template and setup caller but did not execute tests.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-200 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout abstraction used by Codex, PR, Git, setup, and BitLesson scripts.
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-18`). It verifies timeout implementation detection, normal command execution, timeout exit behavior, argument preservation, command exit-code preservation, pipeline/subshell execution through `sh -c`, large output handling, rapid repeated calls, nonexistent command handling, zero-timeout handling, signal handling, fallback-chain detection, and export visibility (`tests/robustness/test-timeout-robustness.sh:31-272`).
- inputs_outputs_state: Inputs are local commands such as `echo`, `sleep`, `printf`, `sh -c`, `seq`, and `nonexistent_command_12345`. Outputs are PASS/FAIL records via `test-helpers.sh` and a final summary (`tests/robustness/test-timeout-robustness.sh:274-279`). It reads `TIMEOUT_IMPL` from `portable-timeout.sh`, which is computed by `detect_timeout_impl` (`scripts/portable-timeout.sh:9-30`), and calls `run_with_timeout` (`scripts/portable-timeout.sh:31-71`).
- gates_or_invariants: Valid implementations are `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` (`tests/robustness/test-timeout-robustness.sh:39-49`; `scripts/portable-timeout.sh:9-27`). Timed-out commands should return `124` when an implementation exists (`tests/robustness/test-timeout-robustness.sh:62-77`, `tests/robustness/test-timeout-robustness.sh:120-137`). Successful commands must preserve stdout and exit status (`tests/robustness/test-timeout-robustness.sh:51-60`, `tests/robustness/test-timeout-robustness.sh:89-110`). The Python fallback must also return `124` on `subprocess.TimeoutExpired` (`scripts/portable-timeout.sh:47-61`).
- dependencies_and_callers: `scripts/portable-timeout.sh` is sourced by `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/bitlesson-select.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/pr-loop-stop-hook.sh` per repository search. The core implementation priority is Homebrew `gtimeout`, GNU `timeout`, Python, then no timeout (`scripts/portable-timeout.sh:6-27`).
- edge_cases_or_failure_modes: Edge coverage includes zero timeout accepting either immediate success or timeout (`tests/robustness/test-timeout-robustness.sh:139-155`), high-volume stdout (`tests/robustness/test-timeout-robustness.sh:157-168`), rapid cycles (`tests/robustness/test-timeout-robustness.sh:170-186`), missing command (`tests/robustness/test-timeout-robustness.sh:188-199`), empty command string (`tests/robustness/test-timeout-robustness.sh:201-209`), shell-special characters in arguments (`tests/robustness/test-timeout-robustness.sh:211-219`), and commands trapping `TERM` (`tests/robustness/test-timeout-robustness.sh:221-233`). If no timeout implementation exists, production runs without timeout and warns (`scripts/portable-timeout.sh:64-69`), and tests mark timeout-specific assertions as skipped/pass-style skips (`tests/robustness/test-timeout-robustness.sh:65-77`).
- validation_or_tests: This file is included in the robustness suite at `tests/run-all-tests.sh:111`. I inspected it directly but did not execute it in this read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 Item Evidence headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`