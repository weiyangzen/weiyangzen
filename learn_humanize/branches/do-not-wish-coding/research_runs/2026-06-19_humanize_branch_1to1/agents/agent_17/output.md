# agent_17 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-017 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation template directory. It currently contains one recursive child, `prompt-template/plan/gen-plan-template.md`, sized 4980 bytes, so the directory’s algorithmic surface is the canonical structure for generated implementation plans.
- algorithmic_behavior: `gen-plan-template.md` defines required plan sections: goal, acceptance criteria with positive/negative tests, path boundaries, feasibility hints, dependency sequence, task breakdown, Claude-Codex deliberation, pending user decisions, implementation notes, and output naming rules (`prompt-template/plan/gen-plan-template.md:1`, `:6`, `:25`, `:57`, `:69`, `:80`, `:91`, `:99`, `:106`). It constrains task routing to exactly one tag, `coding` or `analyze`, with `analyze` routed through `/humanize:ask-codex` (`prompt-template/plan/gen-plan-template.md:71`).
- inputs_outputs_state: Input is design/draft context consumed by `/humanize:gen-plan`; output is the main plan file such as `plan.md`, plus an optional translated variant when `alternative_plan_language` resolves to a supported language (`prompt-template/plan/gen-plan-template.md:108`, `:112`). The template also preserves language-neutral identifiers in translation (`prompt-template/plan/gen-plan-template.md:118`).
- gates_or_invariants: Acceptance criteria must include deterministic positive and negative tests (`prompt-template/plan/gen-plan-template.md:8`). Path boundaries must define upper/lower acceptable scope and allowed choices (`prompt-template/plan/gen-plan-template.md:25`). Implementation code must not carry plan markers such as `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:101`). Unsupported, empty, absent, or English alternative language disables translated output (`prompt-template/plan/gen-plan-template.md:120`).
- dependencies_and_callers: `commands/gen-plan.md` copies this template into the output plan after IO validation and relevance checks (`commands/gen-plan.md:164`). `scripts/validate-gen-plan-io.sh` locates this file through `CLAUDE_PLUGIN_ROOT` or script-relative fallback and exits 7 if missing (`scripts/validate-gen-plan-io.sh:162`). `commands/gen-plan.md` embeds the same plan structure and repeats translation filename rules (`commands/gen-plan.md:420`, `:478`, `:559`).
- edge_cases_or_failure_modes: Missing template is a plugin configuration failure (`scripts/validate-gen-plan-io.sh:171`). Alternative-language output without an extension appends `_<code>` to the filename (`prompt-template/plan/gen-plan-template.md:116`). Highly deterministic drafts can collapse upper/lower path bounds into the same constraint (`prompt-template/plan/gen-plan-template.md:44`).
- validation_or_tests: `tests/test-gen-plan.sh` checks the gen-plan command references `scripts/ask-codex.sh`, requires Codex first-pass analysis before Claude candidate planning, requires deliberation and pending-decision sections, and confirms the embedded Plan Structure block exactly matches `gen-plan-template.md` (`tests/test-gen-plan.sh:125`, `:161`, `:179`, `:189`, `:195`, `:708`). `tests/test-templates-comprehensive.sh` also loads and renders every markdown template recursively (`tests/test-templates-comprehensive.sh:90`, `:561`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-047 `file` `scripts/ask-codex.sh`
- cursor: `[_]`
- core_role: Runtime workflow script for one-shot Codex consultation. It bridges humanize commands/skills and `codex exec`, records invocation artifacts, applies model/effort/timeout policy, and returns Codex stdout to the caller.
- algorithmic_behavior: Sources `portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout portability and config-backed `DEFAULT_CODEX_MODEL` / `DEFAULT_CODEX_EFFORT` (`scripts/ask-codex.sh:26`, `:29`, `:32`). Parses `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--`, help, unknown options, and positional question text (`scripts/ask-codex.sh:86`). Validates Codex CLI availability, non-empty question, safe model chars, and safe effort chars (`scripts/ask-codex.sh:153`, `:162`, `:172`, `:180`). Runs `printf '%s' "$QUESTION" | run_with_timeout ... codex exec ... -` (`scripts/ask-codex.sh:296`).
- inputs_outputs_state: Inputs are CLI args, env config from `.humanize/config.json` via loop-common, `HUMANIZE_CODEX_BYPASS_SANDBOX`, `XDG_CACHE_HOME`, `HOME`, and current git/project root. Outputs are clean Codex response on stdout, status/debug on stderr, project-local `.humanize/skill/<unique-id>/input.md`, `output.md`, `metadata.md`, and cache files `codex-run.cmd/out/log` (`scripts/ask-codex.sh:205`, `:224`, `:261`, `:388`, `:391`, `:409`).
- gates_or_invariants: `codex` must exist (`scripts/ask-codex.sh:154`). Timeout must be numeric, although the regex accepts zero despite the error text saying positive (`scripts/ask-codex.sh:125`). Model permits alphanumeric, dot, underscore, hyphen; effort permits alphanumeric, underscore, hyphen (`scripts/ask-codex.sh:173`, `:181`). Default execution uses `--full-auto`; bypass env switches to dangerous sandbox bypass (`scripts/ask-codex.sh:249`). Empty stdout after exit 0 is treated as failure (`scripts/ask-codex.sh:358`).
- dependencies_and_callers: `commands/gen-plan.md` invokes it for Codex first-pass planning and convergence review (`commands/gen-plan.md:178`, `:198`). The ask-codex skill restricts allowed tools to this script and warns against unquoted `$ARGUMENTS` (`skills/ask-codex/SKILL.md:5`, `:14`, `:30`). The plan template’s `analyze` tag explicitly routes through `/humanize:ask-codex` (`prompt-template/plan/gen-plan-template.md:71`). `scripts/lib/monitor-skill.sh` monitors `.humanize/skill` artifacts and status metadata (`scripts/lib/monitor-skill.sh:12`, `:82`, `:127`).
- edge_cases_or_failure_modes: Missing Codex exits 1 with install guidance (`scripts/ask-codex.sh:153`). Timeout exit 124 writes timeout metadata and suggests doubling timeout (`scripts/ask-codex.sh:308`). Non-zero Codex exit propagates the exit code and tails stderr (`scripts/ask-codex.sh:332`). Home cache creation failure falls back to project-local cache (`scripts/ask-codex.sh:214`). Unique IDs include timestamp, PID, and random bytes to avoid concurrent collisions (`scripts/ask-codex.sh:202`).
- validation_or_tests: `tests/test-ask-codex.sh` uses a mock Codex binary to verify validation errors, successful stdout, artifact creation, timeout/error/empty metadata states, concurrent unique directories, argument parsing, cache files, and safe skill guidance (`tests/test-ask-codex.sh:39`, `:84`, `:164`, `:220`, `:259`, `:286`, `:330`, `:382`, `:415`). `tests/test-unified-codex-config.sh` verifies config-backed defaults feed ask-codex behavior.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-077 `file` `tests/test-config-error-handling.sh`
- cursor: `[_]`
- core_role: Executable specification for configuration loader failure and fallback behavior. It constrains the config merge algorithm used by loop-common and Codex-dependent runtime defaults.
- algorithmic_behavior: Sources `tests/test-helpers.sh` and `scripts/lib/config-loader.sh`, then exercises `load_merged_config` and `get_config_value` across required default config, optional project config, and optional user config layers (`tests/test-config-error-handling.sh:16`, `:20`, `:32`). It asserts missing required default config fails, malformed optional layers warn and fall back, and missing optional layers are non-fatal.
- inputs_outputs_state: Inputs are temp plugin/project directories, `XDG_CONFIG_HOME`, project `.humanize/config.json`, user `humanize/config.json`, and the repo’s `config/default_config.json`. Outputs are pass/fail counters via helper functions and merged JSON captured into shell variables (`tests/test-config-error-handling.sh:40`, `:64`, `:74`, `:139`).
- gates_or_invariants: `scripts/lib/config-loader.sh` requires `jq` and treats default config as required (`scripts/lib/config-loader.sh:17`, `:109`). Each config layer must be a JSON object; malformed required default exits, malformed optional user/project config emits warning and becomes `{}` (`scripts/lib/config-loader.sh:52`, `:53`, `:57`). Merge order is empty, default, user, project with null stripping (`scripts/lib/config-loader.sh:113`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` sources config-loader, loads merged config best-effort, and derives `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, and `DEFAULT_CODEX_EFFORT` (`hooks/lib/loop-common.sh:167`, `:177`, `:180`, `:186`). `scripts/ask-codex.sh`, setup scripts, and PR/RLCR hooks indirectly depend on this default resolution.
- edge_cases_or_failure_modes: Missing `default_config.json` must be fatal (`tests/test-config-error-handling.sh:40`). Malformed project and user configs must surface warning text matching malformed/ignoring/warning but still produce default `bitlesson_model=haiku` (`tests/test-config-error-handling.sh:59`, `:87`). Empty `{}` project config, missing project config, and missing user config directory must all preserve defaults (`tests/test-config-error-handling.sh:116`, `:134`, `:156`).
- validation_or_tests: This file is itself the validation. It ends with `print_test_summary "Config Error Handling Tests"` so any failed assertion returns nonzero through the helper summary (`tests/test-config-error-handling.sh:174`; `tests/test-helpers.sh:58`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-107 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Broad executable specification for the template subsystem. It validates recursive template loading, placeholder grammar, rendering safety, fallback behavior, integration with real templates, and render stress behavior.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, resolves `TEMPLATE_DIR`, verifies required subdirectories `block`, `codex`, and `claude`, recursively finds all markdown templates, loads each with `load_template`, and validates placeholder syntax via grep patterns (`tests/test-templates-comprehensive.sh:15`, `:17`, `:59`, `:74`, `:90`, `:118`). It then exercises `render_template`, `load_and_render_safe`, and `validate_template_dir`.
- inputs_outputs_state: Inputs are every `prompt-template/**/*.md` file plus inline synthetic templates. Outputs are colored pass/fail/warn counts and process exit 0 or 1 (`tests/test-templates-comprehensive.sh:26`, `:611`). It creates temporary malformed-template directories only for test cases and removes them (`tests/test-templates-comprehensive.sh:205`, `:259`).
- gates_or_invariants: Placeholder syntax must be uppercase double braces `{{VAR_NAME}}`; extra braces, wrong bracket types, spaces, and lowercase placeholders fail (`tests/test-templates-comprehensive.sh:122`, `:128`, `:134`, `:140`, `:165`, `:171`). `validate_template_dir` must reject missing or incomplete template roots (`tests/test-templates-comprehensive.sh:466`, `:474`, `:482`). All templates must load and render with dummy values (`tests/test-templates-comprehensive.sh:561`).
- dependencies_and_callers: It constrains `hooks/lib/template-loader.sh`, whose `render_template` performs single-pass awk substitution with `TMPL_VAR_` environment variables and leaves missing variables untouched (`hooks/lib/template-loader.sh:50`, `:62`, `:71`, `:116`, `:120`). `loop-common.sh` uses `load_and_render_safe` for block messages including prompt-file writes (`hooks/lib/loop-common.sh:619`).
- edge_cases_or_failure_modes: Tests empty content, multiline content, empty-line preservation, regex-like values, backslashes, quotes, CJK-like content, markdown formatting, long values, multiline values, variables at boundaries, repeated variables, adjacent variables, missing-template fallback, and rapid 100-call stress (`tests/test-templates-comprehensive.sh:266`, `:285`, `:300`, `:319`, `:329`, `:339`, `:348`, `:373`, `:383`, `:393`, `:404`, `:423`, `:432`, `:446`, `:548`).
- validation_or_tests: The file is self-validating and also complements `tests/test-template-loader.sh`, which explicitly tests placeholder injection prevention where a substituted value contains `{{OTHER_VAR}}` and must not be rescanned (`tests/test-template-loader.sh:500`, `:525`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-137 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Block template enforcing a core RLCR invariant: generated round prompt files are instruction inputs and cannot be modified by the agent that reads them.
- algorithmic_behavior: Emits a concise denial message for writes to `round-*-prompt.md`, explains that prompt files contain instructions from Codex to Claude, and redirects the workflow to read prompt, execute tasks, and write results to the summary file (`prompt-template/block/prompt-file-write.md:1`, `:3`, `:5`, `:7`).
- inputs_outputs_state: Input is the write/edit/bash validator’s decision that a target path matches a round prompt file. Output is rendered block text on stderr or hook response; there are no placeholders in this template, so rendering is static.
- gates_or_invariants: Prompt files are immutable from the worker/Claude side. If a prompt contains errors, the permitted state transition is documenting that in the summary file rather than changing the prompt (`prompt-template/block/prompt-file-write.md:12`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` exposes `prompt_write_blocked_message`, loading this template with fallback (`hooks/lib/loop-common.sh:619`). `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` call that message when prompt writes are detected (`hooks/loop-write-validator.sh:69`; `hooks/loop-edit-validator.sh:52`). `hooks/loop-bash-validator.sh` blocks bash commands modifying `round-[0-9]+-prompt.md` (`hooks/loop-bash-validator.sh:377`).
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline prompt-write warning (`hooks/lib/loop-common.sh:621`). PR loop has a separate stricter template for `round-*-pr-comment.md` or prompt writes under `.humanize/pr-loop/`, so this file is RLCR/general round-prompt specific (`prompt-template/block/pr-loop-prompt-write.md:3`).
- validation_or_tests: `tests/test-template-references.sh` includes `block/prompt-file-write.md` in template-reference coverage (`tests/test-template-references.sh:154`). The comprehensive template test loads and syntax-validates all markdown templates, including no-placeholder templates (`tests/test-templates-comprehensive.sh:90`, `:186`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-167 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop Codex prompt contract that instructs Codex to update `goal-tracker.md` after bot-review analysis. It defines the expected tracker mutation surface for issue summaries, totals, and per-round log entries.
- algorithmic_behavior: Requires a row in the Issue Summary table using `{{NEXT_ROUND}}`, increments Total Issues Found/Resolved, recalculates Remaining, and adds a `### Round {{NEXT_ROUND}}` Issue Log entry (`prompt-template/pr-loop/codex-goal-tracker-update.md:3`, `:7`, `:12`, `:17`). It constrains status values to "Issues Found", "All Resolved", or "Approved" (`prompt-template/pr-loop/codex-goal-tracker-update.md:10`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `NEXT_ROUND` template variables. Output is instruction text injected into the Codex review prompt, not a direct file write by the template loader (`prompt-template/pr-loop/codex-goal-tracker-update.md:3`, `:9`, `:18`).
- gates_or_invariants: Important rules preserve file structure, markdown table formatting, and header sections; updates are limited to Issue Summary, Total Statistics, and Issue Log, with separate summary rows per reviewer (`prompt-template/pr-loop/codex-goal-tracker-update.md:57`).
- dependencies_and_callers: `hooks/pr-loop-stop-hook.sh` builds `GOAL_TRACKER_TEMPLATE_VARS`, renders this file through `load_and_render_safe`, appends it to `round-${NEXT_ROUND}-codex-prompt.md`, then runs `codex exec` (`hooks/pr-loop-stop-hook.sh:1253`, `:1262`, `:1302`, `:1323`, `:1342`). `scripts/setup-pr-loop.sh` creates the initial PR goal tracker that this instruction later updates (`scripts/setup-pr-loop.sh:641`, `:712`).
- edge_cases_or_failure_modes: If the template is unavailable, the stop hook falls back to a minimal instruction to update the tracker (`hooks/pr-loop-stop-hook.sh:1259`). Codex may fail, produce empty output, or not update correctly; the hook blocks on Codex failure/empty output (`hooks/pr-loop-stop-hook.sh:1345`, `:1357`) and later calls `update_pr_goal_tracker` as an idempotent consistency helper (`hooks/pr-loop-stop-hook.sh:1381`, `:1536`).
- validation_or_tests: `update_pr_goal_tracker` checks round+reviewer idempotency, updates totals only when adding a new summary row, inserts a table row, and appends a log entry (`hooks/lib/loop-common.sh:1330`, `:1363`, `:1435`, `:1446`, `:1478`). PR-loop hook tests verify consistent `NEXT_ROUND` naming across generated round files (`tests/test-pr-loop-hooks.sh:726`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-197 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for RLCR state transition/discovery semantics. It constrains how loop state files are parsed, how active/finalize/cancel states are detected, and which malformed states are rejected.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers, creates synthetic `.humanize/rlcr/<timestamp>/` directories, writes `state.md`, `finalize-state.md`, or `cancel-state.md`, then calls `parse_state_file_strict`, `get_current_round`, and `find_active_loop` (`tests/robustness/test-state-transition-robustness.sh:14`, `:16`, `:30`, `:54`, `:74`, `:103`, `:120`, `:153`).
- inputs_outputs_state: Inputs are temporary YAML-frontmatter state files with fields such as `current_round`, `max_iterations`, `plan_file`, `base_branch`, and `review_started`. Outputs are pass/fail assertions and summary exit status (`tests/robustness/test-state-transition-robustness.sh:35`, `:423`).
- gates_or_invariants: Strict parser requires a file, at least two `---` separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`; numeric fields must match `^-?[0-9]+$`; `review_started` must be boolean (`hooks/lib/loop-common.sh:423`, `:431`, `:443`, `:461`, `:467`, `:473`). `find_active_loop` without session filter checks only the lexicographically newest directory, preserving zombie-loop protection (`hooks/lib/loop-common.sh:251`, `:278`).
- dependencies_and_callers: The tested functions live in `hooks/lib/loop-common.sh`, used by read/write/edit/bash validators, setup/cancel scripts, and stop hooks (`hooks/lib/loop-common.sh:3`). `resolve_active_state_file` prefers `finalize-state.md` before `state.md` (`hooks/lib/loop-common.sh:212`), and `get_current_round` extracts from frontmatter with default 0 (`hooks/lib/loop-common.sh:329`).
- edge_cases_or_failure_modes: Negative rounds parse as valid numeric values, and over-max rounds parse with enforcement elsewhere (`tests/robustness/test-state-transition-robustness.sh:217`, `:238`). Non-numeric round, missing current round, missing max iterations, and missing base branch are rejected (`tests/robustness/test-state-transition-robustness.sh:261`, `:292`, `:315`, `:339`). Cancel state is not active; a newer regular state after cancel is active; nested state files in the wrong location are ignored (`tests/robustness/test-state-transition-robustness.sh:182`, `:194`, `:404`).
- validation_or_tests: This file is the validation harness. It includes valid progression tests for round 0, round 5, and max round; finalize detection and precedence; cancel handling; invalid schema checks; and loop directory discovery ordering (`tests/robustness/test-state-transition-robustness.sh:98`, `:114`, `:127`, `:148`, `:160`, `:371`, `:385`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: DO_NOT_WISH_CODING-HZ-017, DO_NOT_WISH_CODING-HZ-047, DO_NOT_WISH_CODING-HZ-077, DO_NOT_WISH_CODING-HZ-107, DO_NOT_WISH_CODING-HZ-137, DO_NOT_WISH_CODING-HZ-167, DO_NOT_WISH_CODING-HZ-197
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`