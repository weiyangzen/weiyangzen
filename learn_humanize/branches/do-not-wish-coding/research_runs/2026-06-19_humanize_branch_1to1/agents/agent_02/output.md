# agent_02 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Prompt-spec directory for specialized sub-agents used by planning, validation, and lesson-selection workflows. It is not executable shell code, but it defines algorithmic contracts for downstream agent calls: what context to inspect, what decisions to make, and the exact output schemas expected by scripts or command workflows.
- algorithmic_behavior: Contains four agent specs. `agents/bitlesson-selector.md:10-18` selects relevant BitLesson entries from a sub-task, related paths, and `bitlesson.md`; its decision rules require directly relevant lessons only and return `NONE` when no lesson applies (`agents/bitlesson-selector.md:26-39`). `agents/draft-relevance-checker.md:14-29` explores repo context, checks semantic relevance of a draft, and returns either `RELEVANT` or `NOT_RELEVANT`. `agents/plan-compliance-checker.md:14-55` runs two checks before RLCR: repository relevance and branch-switch detection, returning exactly one of `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH`. `agents/plan-understanding-quiz.md:14-75` reads a plan, explores referenced repo context, and emits exactly two multiple-choice technical comprehension questions plus a short plan summary.
- inputs_outputs_state: Inputs are prompt-provided task/plan/draft content plus repository files discoverable through allowed `Read`, `Glob`, and `Grep` tools (`agents/*:4-6`). Outputs are stable text protocols: BitLesson selector emits `LESSON_IDS` and `RATIONALE`; draft checker emits one relevance verdict; plan checker emits one pass/fail verdict; quiz emits 13 required fields (`agents/plan-understanding-quiz.md:57-83`). These specs do not mutate repository state directly.
- gates_or_invariants: Output-shape invariants are strict. The BitLesson selector says “No extra sections” (`agents/bitlesson-selector.md:32-40`). The plan checker must output exactly one verdict and avoid false positives on safe branch references such as `--base-branch` (`agents/plan-compliance-checker.md:40-60`). The quiz must produce exactly two questions, exactly four options each, one answer letter each, and all 13 fields (`agents/plan-understanding-quiz.md:31-34`, `agents/plan-understanding-quiz.md:77-82`). The draft checker is intentionally lenient and defaults toward relevance when unsure (`agents/draft-relevance-checker.md:31-36`).
- dependencies_and_callers: `agents/bitlesson-selector.md:19-23` documents runtime execution through `scripts/bitlesson-select.sh`, routed to Codex CLI for `gpt-*` models or Claude CLI for Claude model names based on `bitlesson_model`. `agents/plan-compliance-checker.md:3` ties the checker to `start-rlcr-loop`; `agents/plan-understanding-quiz.md:3` ties quiz generation to user-readiness validation for `start-rlcr-loop`; `agents/draft-relevance-checker.md:3` ties draft relevance to `gen-plan`.
- edge_cases_or_failure_modes: Likely failure modes are malformed agent output, over-broad BitLesson selection, false branch-switch rejection, and underspecified plans. The specs mitigate these by demanding stable output formats, lenient relevance, explicit safe-pattern exclusions for branch detection, and generating quiz questions from whatever implementation hints exist even for short plans (`agents/plan-understanding-quiz.md:84`).
- validation_or_tests: Directory-level tests were not directly assigned, but related coverage exists in `tests/test-agent-teams.sh`, `tests/test-bitlesson-select-routing.sh`, and `tests/test-gen-plan.sh` per repository file listing. The strongest directly inspected linkage is the BitLesson runtime note in `agents/bitlesson-selector.md:21-24`, which explains cross-agent review expectations.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-032 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the PR review loop. It constrains user-facing command arguments, allowed tool execution, and the behavioral contract handed to the setup script and stop hook.
- algorithmic_behavior: The command is a thin dispatcher: its only allowed tool is `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)` (`commands/start-pr-loop.md:4`), and the command body invokes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh" $ARGUMENTS` (`commands/start-pr-loop.md:10-14`). The workflow describes a PR-associated loop: detect branch PR, fetch bot comments, let the agent fix issues, push changes, tag bots, and rely on the stop hook to poll and validate (`commands/start-pr-loop.md:16-24`, `commands/start-pr-loop.md:37-46`).
- inputs_outputs_state: Inputs are command flags: at least one bot flag `--claude` or `--codex`, optional `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout` (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). Runtime outputs are produced by `scripts/setup-pr-loop.sh`: `.humanize/pr-loop/<timestamp>/state.md`, round prompt/comment/resolve files, and goal tracker. The command itself outputs no state beyond running that script.
- gates_or_invariants: Requires at least one bot flag (`commands/start-pr-loop.md:25-30`). Enforces “write summaries”, “push changes”, “tag bots”, and “no cheating” against state edits or cancel-command bypasses (`commands/start-pr-loop.md:50-56`). Stop conditions are max iterations, all monitored bots approving, or explicit cancel (`commands/start-pr-loop.md:58-62`).
- dependencies_and_callers: Delegates to `scripts/setup-pr-loop.sh`. The setup script then checks mutual exclusion with active RLCR/PR loops (`scripts/setup-pr-loop.sh:220-241`), requires git repo, gh auth, and Codex CLI (`scripts/setup-pr-loop.sh:243-277`), discovers PR context including fork support (`scripts/setup-pr-loop.sh:283-379`), writes YAML state (`scripts/setup-pr-loop.sh:615-635`), renders prompt/goal templates (`scripts/setup-pr-loop.sh:641-851`), and the runtime stop behavior is in `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: Missing bot flags are rejected by setup (`scripts/setup-pr-loop.sh:180-191`). Existing active RLCR or PR loops block start (`scripts/setup-pr-loop.sh:225-241`). No git repo, no commits, missing gh auth, missing Codex, no PR, merged/closed PR, YAML-unsafe branch/model/effort values, and fork PR lookup all have explicit setup paths (`scripts/setup-pr-loop.sh:243-407`). Startup cases 4/5 may post/reuse trigger comments and require Claude “eyes” verification when Claude is monitored (`scripts/setup-pr-loop.sh:462-601`).
- validation_or_tests: The assigned split runner `tests/test-pr-loop-2-hooks.sh` sources the PR-loop hook suite and runs `run_hook_tests` (`tests/test-pr-loop-2-hooks.sh:14-23`). The broader inspected suite validates YAML bot lists, state-file protection, trigger handling, per-bot status parsing, stop-hook behavior, and monitor parsing in `tests/test-pr-loop-hooks.sh`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-062 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Runtime status-line formatter for Claude Code sessions with one repository-specific field: RLCR loop status. Most fields are generic session telemetry; the script’s algorithmic relevance is session-aware RLCR state discovery and display.
- algorithmic_behavior: Reads a JSON object from stdin (`scripts/statusline.sh:19`) and extracts fields through `jq` via `get_value` (`scripts/statusline.sh:21-24`). It formats duration from milliseconds (`scripts/statusline.sh:26-34`), resolves RLCR display state from `.humanize/rlcr/<session>` state files (`scripts/statusline.sh:36-61`), discovers the current RLCR session with optional session-id filtering (`scripts/statusline.sh:63-151`), computes colors (`scripts/statusline.sh:153-163`, `scripts/statusline.sh:261-267`), reads session title from transcript or `sessions-index.json` (`scripts/statusline.sh:171-214`), builds a context usage bar (`scripts/statusline.sh:271-320`), and greedily wraps fields to 75% terminal width (`scripts/statusline.sh:375-399`).
- inputs_outputs_state: Inputs are stdin JSON fields `.model.display_name`, `.cwd`, `.session_id`, `.transcript_path`, `.cost.*`, and `.context_window.used_percentage` (`scripts/statusline.sh:165-170`, `scripts/statusline.sh:238-241`, `scripts/statusline.sh:322-326`). It reads `$HOME/.claude/settings.json` for `fastMode` (`scripts/statusline.sh:216-228`), `$HOME/.claude/projects/...` for session naming (`scripts/statusline.sh:185-207`), git branch from `cwd` (`scripts/statusline.sh:233-237`), and `.humanize/rlcr` under `cwd` (`scripts/statusline.sh:253-258`). Output is one or more ANSI-colored status-line rows printed to stdout (`scripts/statusline.sh:386-399`). It does not write files.
- gates_or_invariants: RLCR display precedence is `finalize-state.md` → `state.md` → first terminal `*-state.md` → `Off` (`scripts/statusline.sh:40-59`). If no session id is provided and no session-aware files exist, only the newest directory is considered to avoid stale loop revival (`scripts/statusline.sh:80-90`). If session-aware files exist, directories without `session_id` are ignored in that mode (`scripts/statusline.sh:92-114`). With a session id, it scans newest-to-oldest and returns only a matching stored session id, skipping session-unaware entries when any session-aware state exists (`scripts/statusline.sh:117-150`).
- dependencies_and_callers: Depends on `jq`, `git`, `grep`, `awk`, `sed`, `stty`, `tput`, and shell access to `.humanize/rlcr`. Its RLCR discovery mirrors `find_active_loop` in `hooks/lib/loop-common.sh` per comment (`scripts/statusline.sh:63-64`), and the shared implementation has similar zombie-loop and session-id semantics (`hooks/lib/loop-common.sh:251-327`).
- edge_cases_or_failure_modes: Missing or invalid `cwd` yields branch `?` and RLCR `Off` (`scripts/statusline.sh:233-258`). Missing transcript falls back to constructed path, then sessions index, then full session id (`scripts/statusline.sh:185-214`). Missing terminal width falls back from `/dev/tty` to `tput` to `80` (`scripts/statusline.sh:375-378`). Terminal state reason is derived from the first `*-state.md`, so multiple terminal files in one session directory would depend on shell `ls` order (`scripts/statusline.sh:45-56`). `format_duration` assumes numeric milliseconds; malformed stdin values could produce shell arithmetic errors.
- validation_or_tests: `rg` found no direct statusline-specific test references beyond this file. Related RLCR session-discovery semantics are tested elsewhere in session/loop robustness suites, but no inspected assigned test directly executes `scripts/statusline.sh`.
- skip_candidate: `yes: mostly reference/status display; only the RLCR status field is core-loop related, as the file disclaimer states at scripts/statusline.sh:7-14`

### DO_NOT_WISH_CODING-HZ-092 `file` `tests/test-pr-loop-2-hooks.sh`
- cursor: `[_]`
- core_role: Executable test runner for the PR-loop hook subset. It is a split test entrypoint, not the implementation itself, but it selects and runs the core hook behavior suite.
- algorithmic_behavior: Uses strict shell mode (`tests/test-pr-loop-2-hooks.sh:9`), resolves `SCRIPT_DIR` and `PROJECT_ROOT` (`tests/test-pr-loop-2-hooks.sh:11-12`), sources common helpers and the PR-loop test library (`tests/test-pr-loop-2-hooks.sh:14-15`), initializes a mock PR-loop test environment (`tests/test-pr-loop-2-hooks.sh:17`), sources `tests/test-pr-loop-hooks.sh` (`tests/test-pr-loop-2-hooks.sh:19`), runs `run_hook_tests` (`tests/test-pr-loop-2-hooks.sh:21`), prints summary, and exits with that status (`tests/test-pr-loop-2-hooks.sh:23-24`).
- inputs_outputs_state: Inputs are the checked-out test files plus local shell environment. `init_pr_loop_test_env` creates a temp test directory, mock `gh`, mock `codex`, and prepends them to `PATH` (`tests/test-pr-loop-lib.sh:107-121`). Output is test progress and pass/fail summary. Test state is isolated under helper-created temp directories and mock repos.
- gates_or_invariants: The runner enforces `set -euo pipefail`, so source/init failures abort. It depends on `print_test_summary` returning nonzero when any helper-recorded failures occurred (`tests/test-pr-loop-lib.sh:127-144`). The delegated suite validates PR-loop invariants: state/comment files are protected while resolve files are allowed (`tests/test-pr-loop-hooks.sh:68-163`), bash bypasses are blocked (`tests/test-pr-loop-hooks.sh:174-210`), comments sort human-first/newest-first (`tests/test-pr-loop-hooks.sh:222-259`), configured bots parse as YAML lists (`tests/test-pr-loop-hooks.sh:288-324`), per-bot status markers are parsed (`tests/test-pr-loop-hooks.sh:326-466`), and trigger/timeout/round naming rules hold (`tests/test-pr-loop-hooks.sh:588-754`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and `tests/test-pr-loop-hooks.sh`. The suite calls production validators such as `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/pr-loop-stop-hook.sh`, and scripts such as `scripts/fetch-pr-comments.sh`.
- edge_cases_or_failure_modes: Because this is a split runner, it can pass only if the sourced suite is complete and the helper counters are initialized. It does not itself choose individual tests; any missing function name or sourcing error fails the whole split. It also depends on tools used by the delegated suite, especially `jq`, shell, and mocked `gh`/`codex` behavior.
- validation_or_tests: This file is itself validation. The strongest behavioral coverage comes from the sourced suite: stop hook blocks missing resolve summaries (`tests/test-pr-loop-hooks.sh:766-843`), detects trigger comments and updates state (`tests/test-pr-loop-hooks.sh:845-936`), prioritizes `last_trigger_at` for round 0 (`tests/test-pr-loop-hooks.sh:1052-1136`), handles approval-only reviews (`tests/test-pr-loop-hooks.sh:1162-1283`), validates `fetch-pr-comments.sh --after` (`tests/test-pr-loop-hooks.sh:1287-1318`), blocks wrong-round `pr-resolve` writes/edits (`tests/test-pr-loop-hooks.sh:1431-1526`), and checks monitor bot-list parsing (`tests/test-pr-loop-hooks.sh:1547-1622`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-122 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Blocking prompt fragment for a specific git-cleanliness gate: `.humanize/` directory detected as local generated state that must not be committed.
- algorithmic_behavior: The template emits a “Special Case” message that tells the user `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). It includes concrete remediation commands to append `.humanize*` and stage `.gitignore`.
- inputs_outputs_state: Inputs are indirect: a hook or setup script loads this template when it detects `.humanize` in git cleanliness output. The template itself has no variables and no state. Output is user-facing markdown text.
- gates_or_invariants: Reinforces the invariant that generated loop state under `.humanize/` stays out of version control. This aligns with bash validator protections against `git add` targeting `.humanize` (`hooks/loop-bash-validator.sh:121-130`) and PR/RLCR state-file write protections in validators.
- dependencies_and_callers: Template loading is mediated by `hooks/lib/template-loader.sh`; related block templates are loaded through `load_and_render_safe`. The exact assigned template name did not appear in the targeted `rg` output for directly inspected files, so it may be selected by a broader git-cleanliness path not read in full here.
- edge_cases_or_failure_modes: The recommended command uses append-only `.gitignore` mutation, which can duplicate entries if repeated. It stages `.gitignore` but does not remove already tracked `.humanize` files; a user may still need `git rm --cached` if files were already tracked. The pattern `.humanize*` also ignores `.humanize`-prefixed paths beyond the exact directory.
- validation_or_tests: No direct assigned test references this exact template. Related validation exists in hook tests for blocking direct state modifications and `.humanize` git-add attempts, e.g. `hooks/loop-bash-validator.sh:121-130`.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-152 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Prompt fragment that switches RLCR startup behavior into Agent Teams mode and defines the team-leader responsibilities for the first round.
- algorithmic_behavior: The template tells the active Claude session it is the Team Leader inside an RLCR cycle (`prompt-template/claude/agent-teams-instructions.md:1-3`). It instructs the leader to read the implementation plan before creating a team and identifies required coordination artifacts: the plan file, `goal-tracker.md`, and a designated work summary file (`prompt-template/claude/agent-teams-instructions.md:5-8`).
- inputs_outputs_state: Inputs are the surrounding rendered RLCR prompt and plan content. Output is markdown instruction text embedded into the larger prompt. It does not mutate files directly, but it changes expected agent behavior: delegate work only after understanding the plan, consult `goal-tracker.md`, and write a work summary after teammates finish.
- gates_or_invariants: Enforces an ordering invariant for Agent Teams mode: initial round plan comprehension precedes team creation (`prompt-template/claude/agent-teams-instructions.md:5`). It also establishes required awareness of tracking and summary surfaces, reducing risk that delegated tasks drift from acceptance criteria (`prompt-template/claude/agent-teams-instructions.md:6-8`).
- dependencies_and_callers: The RLCR setup script has an `--agent-teams` flag in its argument handling and persists `agent_teams` in state (`scripts/setup-rlcr-loop.sh` references found via shared `FIELD_AGENT_TEAMS` in `hooks/lib/loop-common.sh:39-40`, parsing in `hooks/lib/loop-common.sh:365-408`). The template is part of the Claude prompt-template family and likely rendered by RLCR startup when Agent Teams mode is enabled.
- edge_cases_or_failure_modes: The fragment is intentionally high-level and assumes the larger prompt provides the actual plan and summary-file path. If embedded without those surrounding variables, it still names expected artifacts but not exact paths except `goal-tracker.md`. It does not specify how to handle teammate failure, conflicts, or incomplete summaries; those behaviors must come from the surrounding RLCR loop.
- validation_or_tests: Repository listing includes `tests/test-agent-teams.sh`, but I did not inspect it as an assigned file. Shared state parsing recognizes `agent_teams` with default `false` (`hooks/lib/loop-common.sh:365-408`), which supports this template’s mode switch.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-182 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness test specification for RLCR base-branch auto-detection. It validates the branch-selection fallback order used before Codex review comparison.
- algorithmic_behavior: The test defines a local `detect_base_branch` helper (`tests/robustness/test-base-branch-detection.sh:37-64`) that attempts remote default branch via `git remote show origin | grep "HEAD branch:"`, rejects empty or `(unknown)`, then falls back to local `main`, then local `master`, and returns failure if none exists (`tests/robustness/test-base-branch-detection.sh:41-63`). It creates disposable git repos to exercise missing-origin/local-main, missing-origin/local-master, no-main/no-master failure, and a simulated origin repo (`tests/robustness/test-base-branch-detection.sh:67-191`).
- inputs_outputs_state: Inputs are temporary git repositories created under `TEST_DIR`, `git`, shared `test-helpers.sh`, and `scripts/portable-timeout.sh` (`tests/robustness/test-base-branch-detection.sh:16-24`). Outputs are pass/fail records through helper functions and a final summary (`tests/robustness/test-base-branch-detection.sh:193-198`). It mutates only temp repos created for the test.
- gates_or_invariants: The intended priority is documented as user-specified `--base-branch`, remote default, local main, local master, then error (`tests/robustness/test-base-branch-detection.sh:5-10`). The helper itself tests only auto-detection priorities, not the user-specified branch path. It guards missing origin with `|| true` so `pipefail` does not abort when `git remote show origin` fails (`tests/robustness/test-base-branch-detection.sh:41-44`).
- dependencies_and_callers: Tests logic from `scripts/setup-rlcr-loop.sh`. Current implementation validates user-provided base branch locally first and distinguishes remote-only branches (`scripts/setup-rlcr-loop.sh:710-727`). For auto-detection, current implementation reads remote default, but only accepts it if the branch exists locally (`scripts/setup-rlcr-loop.sh:729-739`), then falls back to local main/master and errors with fetch advice when remote default exists only remotely (`scripts/setup-rlcr-loop.sh:740-757`). It also rejects YAML-unsafe branch names and captures base commit SHA (`scripts/setup-rlcr-loop.sh:761-777`).
- edge_cases_or_failure_modes: There is a coverage drift: the test comment says it replicates `setup-rlcr-loop.sh` lines 549-567 (`tests/robustness/test-base-branch-detection.sh:35`), but current implementation is around `scripts/setup-rlcr-loop.sh:705-777` and includes local-existence validation for remote defaults. The test’s helper accepts remote default without checking local existence (`tests/robustness/test-base-branch-detection.sh:41-48`), so it may miss a current failure mode where `origin/HEAD` names a branch absent from local refs. It also does not test explicit `--base-branch`, YAML-unsafe names, timeout exits, or base commit capture.
- validation_or_tests: Four concrete scenarios are tested: local `main` fallback when origin is missing (`tests/robustness/test-base-branch-detection.sh:70-93`), local `master` fallback (`tests/robustness/test-base-branch-detection.sh:99-122`), failure with only `develop` (`tests/robustness/test-base-branch-detection.sh:128-149`), and origin remote simulation accepting `main` or fallback result (`tests/robustness/test-base-branch-detection.sh:155-191`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique assigned item sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`