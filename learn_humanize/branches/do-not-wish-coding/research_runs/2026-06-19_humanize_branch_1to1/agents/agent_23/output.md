# agent_23 do-not-wish-coding 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `ac6cd9c180bcb9b84f6083fba1e458b4aab9ae14`

## Item Evidence

### DO_NOT_WISH_CODING-HZ-023 `directory` `skills/humanize-rlcr`
- cursor: `[_]`
- core_role: Skill-mode entrypoint for the RLCR loop. The directory recursively contains one file, `skills/humanize-rlcr/SKILL.md`, which defines a flow that starts RLCR and delegates stop/transition enforcement to the same hook path used outside skill mode.
- algorithmic_behavior: The skill forbids reimplementing review logic and requires calling `scripts/rlcr-stop-gate.sh`, which wraps `hooks/loop-codex-stop-hook.sh` so skill-mode and hook-mode behavior stay aligned (`skills/humanize-rlcr/SKILL.md:10`, `skills/humanize-rlcr/SKILL.md:14`, `skills/humanize-rlcr/SKILL.md:17`). The required loop is setup, read prompt, implement, commit, write summary, run gate, then either exit or continue (`skills/humanize-rlcr/SKILL.md:31`, `skills/humanize-rlcr/SKILL.md:43`, `skills/humanize-rlcr/SKILL.md:51`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` to `setup-rlcr-loop.sh`, generated prompt files under `.humanize/rlcr/<timestamp>/`, optional Claude session/transcript env vars, and loop options such as `--max`, `--base-branch`, `--push-every-round`, and `--skip-impl` (`skills/humanize-rlcr/SKILL.md:35`, `skills/humanize-rlcr/SKILL.md:45`, `skills/humanize-rlcr/SKILL.md:54`, `skills/humanize-rlcr/SKILL.md:94`). Outputs are commits, summary files, and gate exit status; state remains in generated loop files, not manually edited state files (`skills/humanize-rlcr/SKILL.md:47`, `skills/humanize-rlcr/SKILL.md:49`, `skills/humanize-rlcr/SKILL.md:87`).
- gates_or_invariants: Gate exit `0` means allowed exit, `10` means blocked and must continue, `20` means infrastructure error (`skills/humanize-rlcr/SKILL.md:61`). Invariants include branch checks, plan integrity, incomplete Task/Todo blocking, clean git requirement, unpushed commit blocking under `--push-every-round`, summary checks, strict `COMPLETE`/`STOP` markers, review phase guard, and Codex review failure blocking (`skills/humanize-rlcr/SKILL.md:68`).
- dependencies_and_callers: Depends on installed runtime root hydration via `{{HUMANIZE_RUNTIME_ROOT}}`, `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/cancel-rlcr-loop.sh`, and downstream hook state under `.humanize/rlcr/` (`skills/humanize-rlcr/SKILL.md:21`, `skills/humanize-rlcr/SKILL.md:36`, `skills/humanize-rlcr/SKILL.md:54`, `skills/humanize-rlcr/SKILL.md:131`). Install docs reference syncing this skill for Codex/Kimi, so it is packaged as a runtime flow, not a standalone algorithm implementation.
- edge_cases_or_failure_modes: Setup nonzero exit must stop; gate `20` must be reported without fake completion; blocked gate `10` cannot be bypassed; ad-hoc `codex exec` or `codex review` cannot replace the gate for phase transitions (`skills/humanize-rlcr/SKILL.md:39`, `skills/humanize-rlcr/SKILL.md:63`, `skills/humanize-rlcr/SKILL.md:88`, `skills/humanize-rlcr/SKILL.md:89`).
- validation_or_tests: No direct test file is inside the skill directory. Its executable validation path is indirect: correctness is delegated to `rlcr-stop-gate.sh` and hook-mode tests around `hooks/loop-codex-stop-hook.sh`; this directory’s own algorithmic responsibility is routing and invariant documentation.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-053 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop monitor that detects GitHub reactions used as review-loop state transitions: Codex approval via `+1` on PR issue body and Claude receipt confirmation via `eyes` on trigger comments (`scripts/check-bot-reactions.sh:5`, `scripts/check-bot-reactions.sh:10`).
- algorithmic_behavior: Dispatches on `codex-thumbsup` or `claude-eyes` (`scripts/check-bot-reactions.sh:86`). For Codex, it resolves the base repo, fetches paginated issue reactions, filters for `chatgpt-codex-connector[bot]` with `+1`, optionally enforces `--after`, emits the reaction JSON, and exits `0` (`scripts/check-bot-reactions.sh:123`, `scripts/check-bot-reactions.sh:157`, `scripts/check-bot-reactions.sh:166`, `scripts/check-bot-reactions.sh:177`). For Claude, it retries after a delay, fetches paginated comment reactions, filters for `claude[bot]` with `eyes`, emits JSON, or times out with exit `1` (`scripts/check-bot-reactions.sh:264`, `scripts/check-bot-reactions.sh:273`, `scripts/check-bot-reactions.sh:282`, `scripts/check-bot-reactions.sh:298`).
- inputs_outputs_state: Inputs are command name, PR number or comment ID, optional `--after`, `--retry`, `--delay`, and `--pr`; external state is GitHub repo/PR/comment reactions accessed through `gh api`. Outputs are reaction JSON on stdout, human diagnostics on stderr, and exit codes `0` found, `1` not found, `2` error (`scripts/check-bot-reactions.sh:13`, `scripts/check-bot-reactions.sh:44`, `scripts/check-bot-reactions.sh:57`).
- gates_or_invariants: Requires PR number for `codex-thumbsup` and comment ID for `claude-eyes`; unknown options or duplicate IDs are hard errors (`scripts/check-bot-reactions.sh:99`, `scripts/check-bot-reactions.sh:114`, `scripts/check-bot-reactions.sh:212`, `scripts/check-bot-reactions.sh:227`). Uses the PR base repository for fork support and paginates reactions to avoid missing entries beyond the first page (`scripts/check-bot-reactions.sh:119`, `scripts/check-bot-reactions.sh:152`, `scripts/check-bot-reactions.sh:232`, `scripts/check-bot-reactions.sh:269`).
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` and depends on `gh`, `jq`, `sleep`, and GitHub API permissions (`scripts/check-bot-reactions.sh:31`). Called by `hooks/pr-loop-stop-hook.sh` for Codex approval removal and polling (`hooks/pr-loop-stop-hook.sh:676`, `hooks/pr-loop-stop-hook.sh:999`), Claude eyes blocking (`hooks/pr-loop-stop-hook.sh:777`), and by `scripts/setup-pr-loop.sh` during initial Claude setup verification (`scripts/setup-pr-loop.sh:587`).
- edge_cases_or_failure_modes: If current repo cannot resolve or PR is only in parent repo, it falls back through current -> parent -> current (`scripts/check-bot-reactions.sh:127`, `scripts/check-bot-reactions.sh:136`, `scripts/check-bot-reactions.sh:147`). API failure in Codex mode exits `2`; API failure in Claude retry mode continues until attempts are exhausted (`scripts/check-bot-reactions.sh:159`, `scripts/check-bot-reactions.sh:275`). Timestamp comparison is lexicographic ISO string comparison; malformed timestamps could misgate.
- validation_or_tests: `tests/test-pr-loop-system.sh` has reaction checks for Codex `+1`, the `--after` filter, and Claude `eyes` (`tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:207`, `tests/test-pr-loop-system.sh:223`). I did not run tests because this assignment is read-only research notes only.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-083 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for `.humanize` escape protections and zsh-safe directory iteration in the Humanize loop tooling (`tests/test-humanize-escape.sh:3`, `tests/test-humanize-escape.sh:9`).
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, lowercases candidate commands through `to_lower`, and asserts whether `git_adds_humanize` blocks or allows each command (`tests/test-humanize-escape.sh:16`, `tests/test-humanize-escape.sh:49`, `tests/test-humanize-escape.sh:51`, `tests/test-humanize-escape.sh:63`). Test groups cover direct `./.humanize`, quoted paths, force/all flags, chained commands, `git -C`, allowed non-add/different-file cases, find-based empty/dotfile/state/session detection (`tests/test-humanize-escape.sh:80`, `tests/test-humanize-escape.sh:93`, `tests/test-humanize-escape.sh:106`, `tests/test-humanize-escape.sh:150`, `tests/test-humanize-escape.sh:161`, `tests/test-humanize-escape.sh:172`, `tests/test-humanize-escape.sh:198`, `tests/test-humanize-escape.sh:300`).
- inputs_outputs_state: Inputs are hardcoded command strings plus temporary directories under `/tmp` for `.humanize`, empty, dotfile-only, state-file, and session-directory scenarios (`tests/test-humanize-escape.sh:130`, `tests/test-humanize-escape.sh:205`, `tests/test-humanize-escape.sh:226`, `tests/test-humanize-escape.sh:249`, `tests/test-humanize-escape.sh:304`). Outputs are PASS/FAIL lines and final exit `0` if all assertions pass or `1` if any fail (`tests/test-humanize-escape.sh:29`, `tests/test-humanize-escape.sh:34`, `tests/test-humanize-escape.sh:344`).
- gates_or_invariants: Blocks direct `.humanize` path variants, forced broad adds, all-add flags when `.humanize` exists, chained add commands, and git-global-option variants (`tests/test-humanize-escape.sh:83`, `tests/test-humanize-escape.sh:109`, `tests/test-humanize-escape.sh:135`, `tests/test-humanize-escape.sh:153`, `tests/test-humanize-escape.sh:164`). Allows specific source files, git status/diff/log, patch mode, and similarly named files such as `.humanizeconfig` (`tests/test-humanize-escape.sh:175`, `tests/test-humanize-escape.sh:180`, `tests/test-humanize-escape.sh:185`, `tests/test-humanize-escape.sh:190`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` functions `to_lower` and `git_adds_humanize` (`hooks/lib/loop-common.sh:560`, `hooks/lib/loop-common.sh:1095`). The actual runtime enforcement path is `hooks/loop-bash-validator.sh`, which calls `git_adds_humanize` and exits `2` with a block message if true (`hooks/loop-bash-validator.sh:127`).
- edge_cases_or_failure_modes: The spec intentionally checks shell edge cases that previously caused no-match errors: empty directories, dotfiles-only directories, no `*-state.md` files, valid state files, and timestamp-named session directories while ignoring dotfile signals (`tests/test-humanize-escape.sh:204`, `tests/test-humanize-escape.sh:225`, `tests/test-humanize-escape.sh:248`, `tests/test-humanize-escape.sh:271`, `tests/test-humanize-escape.sh:303`). Cleanup uses `rm -rf` on generated `/tmp/test-humanize-*` paths.
- validation_or_tests: This file is itself the validation artifact. It directly exercises the helper contract that `git_adds_humanize` implements: segment shell operators, detect `git add` after git options, strip quotes, block exact `.humanize` path components, and conditionally block broad scope based on `.humanize` existence/gitignore (`hooks/lib/loop-common.sh:1098`, `hooks/lib/loop-common.sh:1119`, `hooks/lib/loop-common.sh:1130`, `hooks/lib/loop-common.sh:1136`, `hooks/lib/loop-common.sh:1170`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-113 `file` `prompt-template/block/bitlesson-delta-empty-kb.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the BitLesson learning gate when a round declares `Action: none` but the knowledge base has no concrete reusable lessons (`prompt-template/block/bitlesson-delta-empty-kb.md:1`, `prompt-template/block/bitlesson-delta-empty-kb.md:3`).
- algorithmic_behavior: Provides the user-facing remediation contract: `Action: none` is disallowed in an empty KB, and resolving previous-round issues must add or update at least one reusable lesson and report `Action: add` or `Action: update` (`prompt-template/block/bitlesson-delta-empty-kb.md:5`).
- inputs_outputs_state: Inputs are rendered through the template loader; this concrete file currently contains no placeholders, while the fallback in the validator accepts `CURRENT_ROUND` and `BITLESSON_FILE` (`scripts/bitlesson-validate-delta.sh:179`). Output is a Markdown block reason passed to `block_exit` (`scripts/bitlesson-validate-delta.sh:182`).
- gates_or_invariants: The gate fires only when `BITLESSON_ACTION == none`, lesson IDs are consistent with none, concrete lesson count is zero, and `BITLESSON_ALLOW_EMPTY_NONE` is not true (`scripts/bitlesson-validate-delta.sh:158`, `scripts/bitlesson-validate-delta.sh:170`).
- dependencies_and_callers: Called by `scripts/bitlesson-validate-delta.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/bitlesson-delta-empty-kb.md"` (`scripts/bitlesson-validate-delta.sh:179`). It coordinates with sibling BitLesson inconsistency templates for non-empty IDs under `Action: none` and missing IDs under add/update (`scripts/bitlesson-validate-delta.sh:166`, `scripts/bitlesson-validate-delta.sh:192`).
- edge_cases_or_failure_modes: If the template fails to load, the validator uses an inline fallback that includes current round and file path (`scripts/bitlesson-validate-delta.sh:171`). Because the checked-in template omits those placeholders, the displayed block is simpler than the fallback and may provide less precise context.
- validation_or_tests: No direct test reference was found for this exact template. Its behavior is validated indirectly by the BitLesson delta validator path and template-loading mechanism; I did not run validation in this read-only research pass.
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-143 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Shared stop-hook block template enforcing push-before-exit when the loop requires remote visibility of commits (`prompt-template/block/unpushed-commits.md:1`, `prompt-template/block/unpushed-commits.md:3`).
- algorithmic_behavior: Renders `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, explains that `--push-every-round` is enabled, gives the exact required `git push origin {{CURRENT_BRANCH}}`, and tells the agent it may retry exit afterward (`prompt-template/block/unpushed-commits.md:5`, `prompt-template/block/unpushed-commits.md:8`).
- inputs_outputs_state: Inputs are the ahead count and current branch from git status/rev parsing. Output is Markdown used as the `reason` field in hook JSON block responses (`hooks/loop-codex-stop-hook.sh:661`, `hooks/pr-loop-stop-hook.sh:356`).
- gates_or_invariants: RLCR stop hook renders it only when `PUSH_EVERY_ROUND` is true and `git status -sb` reports ahead commits (`hooks/loop-codex-stop-hook.sh:649`, `hooks/loop-codex-stop-hook.sh:651`). PR loop stop hook independently computes local commits ahead of PR head and blocks when `AHEAD_COUNT > 0` (`hooks/pr-loop-stop-hook.sh:340`, `hooks/pr-loop-stop-hook.sh:350`).
- dependencies_and_callers: Depends on the shared template renderer `load_and_render_safe`. Called by both `hooks/loop-codex-stop-hook.sh` for RLCR and `hooks/pr-loop-stop-hook.sh` for PR review loops, so the same message coordinates two loop types.
- edge_cases_or_failure_modes: If remote/PR head comparison cannot count commits, PR loop treats it as one unpushed commit (`hooks/pr-loop-stop-hook.sh:341`). If template loading fails, both callers have fallback text; the RLCR fallback is less specific but still blocks (`hooks/loop-codex-stop-hook.sh:656`, `hooks/pr-loop-stop-hook.sh:351`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this real template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, asserting title, count, and branch appear (`tests/test-templates-comprehensive.sh:517`).
- skip_candidate: `no`

### DO_NOT_WISH_CODING-HZ-173 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template for startup case where the PR has no comments yet and bots are expected to review automatically (`prompt-template/pr-loop/round-0-task-no-comments.md:4`, `prompt-template/pr-loop/round-0-task-no-comments.md:6`).
- algorithmic_behavior: Instructs the worker not to trigger first reviews manually, to write an initial summary to `@{{RESOLVE_PATH}}`, and to exit so the stop hook can poll for bot reviews (`prompt-template/pr-loop/round-0-task-no-comments.md:8`, `prompt-template/pr-loop/round-0-task-no-comments.md:12`, `prompt-template/pr-loop/round-0-task-no-comments.md:20`).
- inputs_outputs_state: Inputs are `{{ACTIVE_BOTS_DISPLAY}}` and `{{RESOLVE_PATH}}` from setup. Output is appended into `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the round-0 header and fetched comments (`scripts/setup-pr-loop.sh:731`, `scripts/setup-pr-loop.sh:761`, `scripts/setup-pr-loop.sh:764`, `scripts/setup-pr-loop.sh:800`).
- gates_or_invariants: The template preserves the invariant that first reviews are automatic, `.humanize/pr-loop/` state files are system-managed, and stop hook owns polling/Codex validation (`prompt-template/pr-loop/round-0-task-no-comments.md:20`, `prompt-template/pr-loop/round-0-task-no-comments.md:21`, `prompt-template/pr-loop/round-0-task-no-comments.md:22`).
- dependencies_and_callers: Selected by `scripts/setup-pr-loop.sh` when `fetch-pr-comments.sh` output contains the no-comments sentinel, causing `COMMENT_COUNT=0` (`scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:725`, `scripts/setup-pr-loop.sh:767`). It coordinates with `hooks/pr-loop-stop-hook.sh`, which later polls every 30 seconds up to configured bot timeouts and checks reactions/reviews (`prompt-template/pr-loop/round-0-task-no-comments.md:26`, `hooks/pr-loop-stop-hook.sh:807`).
- edge_cases_or_failure_modes: If comments exist, setup uses the alternate normal task template rather than this file (`scripts/setup-pr-loop.sh:801`). If this template fails to load, setup has an inline fallback with equivalent instructions (`scripts/setup-pr-loop.sh:769`).
- validation_or_tests: No direct renderer test for this exact file was found, but setup logic has the same fallback text and a later test fixture references “Case 1: No comments yet” setup (`tests/test-pr-loop-system.sh:1457`). I did not execute tests due to the read-only/no-modification research constraint.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 unique item evidence headings present; all use cursor `[_]`
- missing_items: none
- duplicate_items: none in item evidence headings
- final_worker_status: `complete`