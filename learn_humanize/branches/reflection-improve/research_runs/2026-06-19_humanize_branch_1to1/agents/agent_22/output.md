# agent_22 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-022 `directory` `skills/humanize-gen-plan`
- cursor: `[_]`
- core_role: Flow skill package that defines the draft-to-plan generation algorithm. Recursive inspection found one descendant, `skills/humanize-gen-plan/SKILL.md`; no scripts, assets, or nested references live inside the directory itself.
- algorithmic_behavior: The flow validates `--input` and `--output`, reads a draft, checks repository relevance, iteratively analyzes clarity/consistency/completeness/functionality, asks the user to resolve issues when needed, confirms whether quantitative metrics are hard requirements or trend signals, generates a structured `plan.md`, reviews/fixes inconsistencies, and checks for mixed languages before reporting success (`skills/humanize-gen-plan/SKILL.md:18`, `skills/humanize-gen-plan/SKILL.md:20`, `skills/humanize-gen-plan/SKILL.md:25`, `skills/humanize-gen-plan/SKILL.md:28`, `skills/humanize-gen-plan/SKILL.md:30`, `skills/humanize-gen-plan/SKILL.md:32`, `skills/humanize-gen-plan/SKILL.md:35`, `skills/humanize-gen-plan/SKILL.md:37`, `skills/humanize-gen-plan/SKILL.md:41`).
- inputs_outputs_state: Inputs are required `--input <path/to/draft.md>` and `--output <path/to/plan.md>` (`skills/humanize-gen-plan/SKILL.md:50`). Output is a plan containing goal description, AC-format acceptance criteria with positive/negative tests, path boundaries, dependencies/milestones, and implementation notes (`skills/humanize-gen-plan/SKILL.md:54`, `skills/humanize-gen-plan/SKILL.md:58`). The flow is state-light: its state is the draft, user clarifications, and newly written plan file; it does not define persistent RLCR loop state.
- gates_or_invariants: The first gate is IO validation through `{{HUMANIZE_RUNTIME_ROOT}}/scripts/validate-gen-plan-io.sh --input <draft> --output <plan>` (`skills/humanize-gen-plan/SKILL.md:20`). Relevance to repository is a hard stop before analysis (`skills/humanize-gen-plan/SKILL.md:25`). The output path must be new, not overwritten, per validation exit codes and validator behavior (`skills/humanize-gen-plan/SKILL.md:95`; `scripts/validate-gen-plan-io.sh:132`). The plan must be internally consistent and language-unified before success (`skills/humanize-gen-plan/SKILL.md:37`, `skills/humanize-gen-plan/SKILL.md:41`).
- dependencies_and_callers: Depends on runtime-root hydration (`skills/humanize-gen-plan/SKILL.md:12`) and the IO validator. The validator enforces missing/empty input, output directory existence, output nonexistence, write permission, and existence of `prompt-template/plan/gen-plan-template.md` (`scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:116`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:140`, `scripts/validate-gen-plan-io.sh:147`, `scripts/validate-gen-plan-io.sh:171`). Installation/docs expose the skill alongside `humanize` and `humanize-rlcr` (`scripts/install-skill.sh`, `docs/install-for-codex.md`, `docs/install-for-kimi.md` from search results).
- edge_cases_or_failure_modes: Invalid arguments, missing input, empty input, missing output directory, existing output file or directory, unwritable output directory, and missing plan template map to exit codes 1-7 (`skills/humanize-gen-plan/SKILL.md:95`; `scripts/validate-gen-plan-io.sh:4`). Mixed `--discussion` and `--direct` modes fail with exit 6; direct mode plus auto-start is only noted and skipped (`scripts/validate-gen-plan-io.sh:76`, `scripts/validate-gen-plan-io.sh:93`).
- validation_or_tests: No test file exists inside this directory. Related validation is implemented by `scripts/validate-gen-plan-io.sh`, which is the concrete gate named by the skill (`skills/humanize-gen-plan/SKILL.md:20`; `scripts/validate-gen-plan-io.sh:103`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-052 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation command for active RLCR loops. It is part of the loop state machine because it converts active state files into `cancel-state.md` and creates a cancel signal consumed by validators.
- algorithmic_behavior: Parses only `--force` and help; finds the project root from `CLAUDE_PROJECT_DIR` or `pwd`; sources `hooks/lib/loop-common.sh`; calls `find_active_loop "$LOOP_BASE_DIR"` without session filtering; classifies the active file as normal, methodology-analysis, or finalize; extracts `current_round` and `max_iterations`; blocks finalize cancellation unless `--force`; then touches `.cancel-requested`, removes pending/session and methodology marker files, and renames the active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:24`, `scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:74`, `scripts/cancel-rlcr-loop.sh:87`, `scripts/cancel-rlcr-loop.sh:99`, `scripts/cancel-rlcr-loop.sh:123`, `scripts/cancel-rlcr-loop.sh:135`, `scripts/cancel-rlcr-loop.sh:153`, `scripts/cancel-rlcr-loop.sh:156`, `scripts/cancel-rlcr-loop.sh:159`, `scripts/cancel-rlcr-loop.sh:162`).
- inputs_outputs_state: Input is optional `--force`; environment input is `CLAUDE_PROJECT_DIR`. Outputs are machine-readable leading statuses such as `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, and `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:89`, `scripts/cancel-rlcr-loop.sh:114`, `scripts/cancel-rlcr-loop.sh:136`, `scripts/cancel-rlcr-loop.sh:168`). State transition is `state.md` or `methodology-analysis-state.md` or `finalize-state.md` to `cancel-state.md`.
- gates_or_invariants: Exit codes are 0 success, 1 no active loop, 2 finalize confirmation required, and 3 other/argument error (`scripts/cancel-rlcr-loop.sh:11`). Product invariant: cancellation is global, not session-scoped, because slash command invocation lacks the hook `session_id` (`scripts/cancel-rlcr-loop.sh:76`). It intentionally uses newest active loop lookup via `find_active_loop`, which itself only checks the newest directory without session filtering to avoid reviving zombie loops (`hooks/lib/loop-common.sh:284`).
- dependencies_and_callers: Depends on `find_active_loop` and `resolve_active_state_file` from `hooks/lib/loop-common.sh` (`scripts/cancel-rlcr-loop.sh:72`; `hooks/lib/loop-common.sh:213`, `hooks/lib/loop-common.sh:275`). The `humanize` and `humanize-rlcr` skills reference this runtime command, and docs/commands point users to `/humanize:cancel-rlcr-loop` (search results). The Bash validator permits the matching `mv active-state cancel-state.md` only when `.cancel-requested` exists and `is_cancel_authorized` accepts the exact operation (`hooks/loop-bash-validator.sh:402`, `hooks/loop-bash-validator.sh:423`; `hooks/lib/loop-common.sh:871`).
- edge_cases_or_failure_modes: Unknown options exit 3 (`scripts/cancel-rlcr-loop.sh:57`). Existing loop directory without an active state file exits 1 (`scripts/cancel-rlcr-loop.sh:113`). Finalize phase requires `--force`, because completion is otherwise imminent (`scripts/cancel-rlcr-loop.sh:135`). Missing round fields degrade to `?` for display, not failure (`scripts/cancel-rlcr-loop.sh:127`). If `mv` fails under `set -euo pipefail`, the script exits nonzero before success output.
- validation_or_tests: `tests/test-session-id.sh` verifies cancellation ignores session id and renames state to `cancel-state.md` (`tests/test-session-id.sh:368`, `tests/test-session-id.sh:372`, `tests/test-session-id.sh:379`). The same test verifies zombie-loop protection: newest completed loop means no older stale active loop is cancelled (`tests/test-session-id.sh:420`). `tests/test-cancel-signal-file.sh` verifies `.cancel-requested` is required and only authorizes an exact active-state to `cancel-state.md` move, rejecting wrong destinations and injection attempts (`tests/test-cancel-signal-file.sh:80`, `tests/test-cancel-signal-file.sh:121`, `tests/test-cancel-signal-file.sh:181`, `tests/test-cancel-signal-file.sh:243`, `tests/test-cancel-signal-file.sh:263`, `tests/test-cancel-signal-file.sh:303`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-082 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared executable test harness utilities for the repository’s Bash tests. It is not an RLCR state machine implementation itself, but it underpins executable specifications for core behavior.
- algorithmic_behavior: Defines color constants, global counters, result functions `pass`, `fail`, `skip`, a `print_test_summary` exit-status aggregator, `setup_test_dir` with cleanup trap, and `init_test_git_repo` for isolated repository fixtures (`tests/test-helpers.sh:13`, `tests/test-helpers.sh:22`, `tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:46`, `tests/test-helpers.sh:58`, `tests/test-helpers.sh:86`, `tests/test-helpers.sh:93`).
- inputs_outputs_state: Inputs are message strings to result functions, optional expected/got values for `fail`, an optional title for `print_test_summary`, and a directory path for `init_test_git_repo`. Outputs are colored PASS/FAIL/SKIP lines and a summary return code. State is process-global counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED`, plus `TEST_DIR` created by `setup_test_dir` (`tests/test-helpers.sh:22`, `tests/test-helpers.sh:58`, `tests/test-helpers.sh:87`).
- gates_or_invariants: The invariant is that any nonzero failed count makes `print_test_summary` return 1; zero failures returns 0 (`tests/test-helpers.sh:71`). `setup_test_dir` installs a trap that removes the temporary directory at shell exit (`tests/test-helpers.sh:87`). `init_test_git_repo` always creates a first commit with local test identity and GPG signing disabled (`tests/test-helpers.sh:97`, `tests/test-helpers.sh:100`, `tests/test-helpers.sh:103`).
- dependencies_and_callers: Depends on standard shell commands plus `git`. It is sourced by robustness tests such as `tests/robustness/test-template-stress-robustness.sh` (`tests/robustness/test-template-stress-robustness.sh:17`) and other tests from search results. It coordinates with tests that source implementation libraries directly, for example `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: The cleanup trap is a single `EXIT` trap and can be overwritten by later traps in a calling script. `init_test_git_repo` changes directories and returns via `cd -`; if `git init` or commit fails, callers using `set -e` will abort. Counters are global and not namespace-protected, so sourced tests must avoid reusing those variable names accidentally.
- validation_or_tests: This file is itself support code rather than a test target. Its behavior is exercised transitively whenever tests source it; `tests/robustness/test-template-stress-robustness.sh` uses `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/robustness/test-template-stress-robustness.sh:19`, `tests/robustness/test-template-stress-robustness.sh:39`, `tests/robustness/test-template-stress-robustness.sh:41`, `tests/robustness/test-template-stress-robustness.sh:289`).
- skip_candidate: `yes: test harness support, not a core algorithm implementation, though it is relevant as executable-spec infrastructure`

### REFLECTION_IMPROVE-HZ-112 `file` `hooks/lib/methodology-analysis.sh`
- cursor: `[_]`
- core_role: RLCR stop-hook library implementing the Methodology Analysis Phase inserted between loop exit decision and terminal state preservation.
- algorithmic_behavior: `enter_methodology_analysis_phase` skips when privacy mode is true, skips re-entry if `methodology-analysis-state.md` exists, skips if a previous nonempty done marker exists, renames the active state file to `methodology-analysis-state.md`, records the original exit reason in `.methodology-exit-reason`, creates an empty `methodology-analysis-done.md`, renders a prompt, and emits hook block JSON (`hooks/lib/methodology-analysis.sh:38`, `hooks/lib/methodology-analysis.sh:42`, `hooks/lib/methodology-analysis.sh:48`, `hooks/lib/methodology-analysis.sh:55`, `hooks/lib/methodology-analysis.sh:64`, `hooks/lib/methodology-analysis.sh:69`, `hooks/lib/methodology-analysis.sh:72`, `hooks/lib/methodology-analysis.sh:82`, `hooks/lib/methodology-analysis.sh:90`). `complete_methodology_analysis` requires nonempty done and report artifacts, validates the saved exit reason, then renames `methodology-analysis-state.md` to `<exit_reason>-state.md` and removes the marker (`hooks/lib/methodology-analysis.sh:114`, `hooks/lib/methodology-analysis.sh:118`, `hooks/lib/methodology-analysis.sh:129`, `hooks/lib/methodology-analysis.sh:142`, `hooks/lib/methodology-analysis.sh:152`, `hooks/lib/methodology-analysis.sh:162`, `hooks/lib/methodology-analysis.sh:168`). `block_methodology_analysis_incomplete` emits block JSON instructing completion (`hooks/lib/methodology-analysis.sh:180`).
- inputs_outputs_state: Inputs are `exit_reason` and `exit_reason_description`, plus globals `PRIVACY_MODE`, `STATE_FILE`, `LOOP_DIR`, `CURRENT_ROUND`, `MAX_ITERATIONS`, and `TEMPLATE_DIR` (`hooks/lib/methodology-analysis.sh:22`, `hooks/lib/methodology-analysis.sh:26`). Outputs are JSON decisions with `"decision": "block"` during entry/incompletion, stderr diagnostics, and state files. State transitions are active `state.md`/`finalize-state.md` to `methodology-analysis-state.md`, then to `complete-state.md`, `stop-state.md`, or `maxiter-state.md`.
- gates_or_invariants: Privacy mode prevents the analysis phase (`hooks/lib/methodology-analysis.sh:42`). Completion fails closed unless both marker and report exist with content (`hooks/lib/methodology-analysis.sh:119`, `hooks/lib/methodology-analysis.sh:125`, `hooks/lib/methodology-analysis.sh:131`, `hooks/lib/methodology-analysis.sh:137`). Missing or invalid `.methodology-exit-reason` blocks completion (`hooks/lib/methodology-analysis.sh:143`, `hooks/lib/methodology-analysis.sh:153`). Valid exit reasons are only `complete`, `stop`, and `maxiter` (`hooks/lib/methodology-analysis.sh:153`).
- dependencies_and_callers: Sourced by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:53`). The stop hook detects the phase by active state filename (`hooks/loop-codex-stop-hook.sh:83`) and short-circuits normal checks while methodology analysis is active (`hooks/loop-codex-stop-hook.sh:596`). It enters the phase before max-iteration exit, finalize completion, COMPLETE-at-maxiter, and STOP/circuit-breaker exits (`hooks/loop-codex-stop-hook.sh:856`, `hooks/loop-codex-stop-hook.sh:872`, `hooks/loop-codex-stop-hook.sh:1587`, `hooks/loop-codex-stop-hook.sh:1684`). It depends on `load_and_render_safe` from `hooks/lib/template-loader.sh` for prompt rendering (`hooks/lib/methodology-analysis.sh:82`).
- edge_cases_or_failure_modes: If the empty placeholder done file remains empty, completion blocks. If the analysis report is missing or empty, completion blocks with warning. If `.methodology-exit-reason` is removed or corrupted, completion blocks. If privacy mode is true, this feature is skipped and callers proceed to the old terminal path. If `mv "$STATE_FILE"` fails, the caller will not enter the phase.
- validation_or_tests: Related validators constrain the phase: Bash writes/builds/scripts are blocked during methodology analysis except safe reads and cancel (`hooks/loop-bash-validator.sh:68`, `hooks/loop-bash-validator.sh:87`, `hooks/loop-bash-validator.sh:95`, `hooks/loop-bash-validator.sh:101`, `hooks/loop-bash-validator.sh:115`, `hooks/loop-bash-validator.sh:150`); read/write/edit validators only allow methodology report/done artifacts per search results. The prompt template `prompt-template/claude/methodology-analysis-prompt.md` requires the report and done marker (search results). Direct tests were not found for this library in the assigned set, but multiple validators and stop-hook branches reference its state files.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-142 `file` `prompt-template/block/stop-hook-direct-execution.md`
- cursor: `[_]`
- core_role: Block-message template for the Bash validator gate that prevents manual execution of hook scripts during active loops.
- algorithmic_behavior: Provides the user-facing reason when a Bash command attempts to run hook scripts directly. It states direct execution of hook scripts is not allowed during an active loop and instructs the agent to finish work and end the response so hooks fire automatically (`prompt-template/block/stop-hook-direct-execution.md:1`, `prompt-template/block/stop-hook-direct-execution.md:3`, `prompt-template/block/stop-hook-direct-execution.md:5`, `prompt-template/block/stop-hook-direct-execution.md:7`).
- inputs_outputs_state: No template variables are used. Output is static Markdown content rendered through `stop_hook_direct_execution_blocked_message`, then sent to stderr by the Bash validator (`hooks/lib/loop-common.sh:1263`, `hooks/lib/loop-common.sh:1272`; `hooks/loop-bash-validator.sh:174`). It does not mutate state.
- gates_or_invariants: Enforcement pattern blocks `loop-codex-stop-hook.sh`, `pr-loop-stop-hook.sh`, and `rlcr-stop-gate.sh` when invoked directly or through shell wrappers while an RLCR or PR loop is active (`hooks/loop-bash-validator.sh:161`, `hooks/loop-bash-validator.sh:172`). The invariant is that stop/finalization gates run only through the hook system, not through user Bash commands (`hooks/loop-bash-validator.sh:167`).
- dependencies_and_callers: Loaded by `load_and_render_safe "$TEMPLATE_DIR" "block/stop-hook-direct-execution.md"` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:1272`). Called from `hooks/loop-bash-validator.sh` when its direct-execution regex matches (`hooks/loop-bash-validator.sh:172`, `hooks/loop-bash-validator.sh:174`).
- edge_cases_or_failure_modes: If this template is missing or empty, the renderer falls back to an inline message in `stop_hook_direct_execution_blocked_message` (`hooks/lib/loop-common.sh:1264`). The block depends on the command regex; unusual invocation patterns not matching the regex could escape this specific message, though later state-file protections may still block unsafe effects.
- validation_or_tests: `tests/test-bash-validator-patterns.sh` exercises command modification detection broadly and documents pattern limitations (`tests/test-bash-validator-patterns.sh:3`, `tests/test-bash-validator-patterns.sh:69`, `tests/test-bash-validator-patterns.sh:155`). `tests/test-template-references.sh` verifies critical validators use safe template rendering for block messages (`tests/test-template-references.sh:172`). No specific assigned test directly asserts this exact template text.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-172 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt block defining Round 0 completion behavior before automated bot review comments exist.
- algorithmic_behavior: Instructs the worker/agent that completion requires writing a resolution summary to `{{RESOLVE_PATH}}`, noting Round 0 is awaiting initial bot reviews and that there are no issues yet, then trying to exit so the Stop Hook polls for bot reviews. It explicitly forbids commenting on the PR to trigger review (`prompt-template/pr-loop/critical-requirements-no-comments.md:6`, `prompt-template/pr-loop/critical-requirements-no-comments.md:8`, `prompt-template/pr-loop/critical-requirements-no-comments.md:11`, `prompt-template/pr-loop/critical-requirements-no-comments.md:15`, `prompt-template/pr-loop/critical-requirements-no-comments.md:17`).
- inputs_outputs_state: Input is `RESOLVE_PATH` injected by the template renderer (`prompt-template/pr-loop/critical-requirements-no-comments.md:9`). Output is a prompt segment appended to the PR-loop task content. The intended state output is a resolution summary file at the rendered path, after which Stop Hook polling advances the PR-loop control state.
- gates_or_invariants: The gate is “write resolution summary, then exit”; it keeps Round 0 review acquisition in the Stop Hook rather than in manual PR comments (`prompt-template/pr-loop/critical-requirements-no-comments.md:15`, `prompt-template/pr-loop/critical-requirements-no-comments.md:20`). It enforces no manual PR trigger comment when there are no existing comments (`prompt-template/pr-loop/critical-requirements-no-comments.md:17`).
- dependencies_and_callers: Used by `scripts/setup-pr-loop.sh` when no comments exist; the setup script has an inline fallback and renders this template with `load_and_render_safe` (`scripts/setup-pr-loop.sh:900`, `scripts/setup-pr-loop.sh:914`). Depends on `hooks/lib/template-loader.sh` single-pass variable replacement via setup script sourcing (`scripts/setup-pr-loop.sh:39`; `hooks/lib/template-loader.sh:170`).
- edge_cases_or_failure_modes: Missing template falls back to nearly identical inline critical content in `scripts/setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:900`). If `RESOLVE_PATH` is missing, `render_template` leaves `{{RESOLVE_PATH}}` unchanged because missing placeholders are preserved (`hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:120`), which would make the prompt ambiguous.
- validation_or_tests: Template existence and safe rendering are covered by template reference tests and broader template loader tests. `tests/test-template-loader.sh` verifies single and multiple substitutions (`tests/test-template-loader.sh:81`, `tests/test-template-loader.sh:96`), and `tests/test-template-references.sh` checks critical validators use `load_and_render_safe` (`tests/test-template-references.sh:172`). No direct assigned test asserts this PR-loop block’s exact text.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-202 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the prompt template rendering subsystem used by RLCR/PR hooks and block templates.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh` and `tests/test-helpers.sh`, creates a temp directory, then runs 18 checks covering normal substitution, fallback rendering, standard and large templates, large values, many variables, special characters, newline values, empty values, placeholder-in-value injection prevention, repeated variables, boundary placeholders, and placeholder-only templates (`tests/robustness/test-template-stress-robustness.sh:14`, `tests/robustness/test-template-stress-robustness.sh:16`, `tests/robustness/test-template-stress-robustness.sh:17`, `tests/robustness/test-template-stress-robustness.sh:19`, `tests/robustness/test-template-stress-robustness.sh:33`, `tests/robustness/test-template-stress-robustness.sh:46`, `tests/robustness/test-template-stress-robustness.sh:82`, `tests/robustness/test-template-stress-robustness.sh:111`, `tests/robustness/test-template-stress-robustness.sh:134`, `tests/robustness/test-template-stress-robustness.sh:161`, `tests/robustness/test-template-stress-robustness.sh:218`, `tests/robustness/test-template-stress-robustness.sh:252`, `tests/robustness/test-template-stress-robustness.sh:263`, `tests/robustness/test-template-stress-robustness.sh:274`).
- inputs_outputs_state: Inputs are generated template strings/files and `VAR=value` assignments. Outputs are PASS/FAIL lines and final process exit status from `print_test_summary` (`tests/robustness/test-template-stress-robustness.sh:289`). State is temporary files under `TEST_DIR`, plus helper counters from `tests/test-helpers.sh`.
- gates_or_invariants: Core invariants are that `render_template` performs single-pass substitution, missing templates use fallback through `load_and_render_safe`, large values/templates do not truncate, regex/sed/shell metacharacters render literally, empty values are valid, and placeholders embedded in replacement values are not recursively expanded (`tests/robustness/test-template-stress-robustness.sh:47`, `tests/robustness/test-template-stress-robustness.sh:90`, `tests/robustness/test-template-stress-robustness.sh:128`, `tests/robustness/test-template-stress-robustness.sh:164`, `tests/robustness/test-template-stress-robustness.sh:176`, `tests/robustness/test-template-stress-robustness.sh:187`, `tests/robustness/test-template-stress-robustness.sh:211`, `tests/robustness/test-template-stress-robustness.sh:222`). These map directly to `template-loader.sh` comments and implementation using environment variables plus awk single-pass scanning (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:74`, `hooks/lib/template-loader.sh:116`).
- dependencies_and_callers: Depends on Bash, `date`, `wc`, `grep`, `seq`, `mktemp`, `awk` via the template loader, and helper functions. It validates `hooks/lib/template-loader.sh`, which is used by RLCR/PR validators and prompt renderers across the repo (`hooks/lib/template-loader.sh:15`, `hooks/lib/template-loader.sh:170`).
- edge_cases_or_failure_modes: The timing measurements use `date +%s%N`, which can be platform-sensitive on some non-GNU systems. Very large `VAR=value` inputs are passed through environment variables in `render_template`, so OS environment-size limits are an implicit ceiling beyond this test’s 100KB case (`hooks/lib/template-loader.sh:64`). The test labels one case “Unicode” but only verifies basic ASCII content (`tests/robustness/test-template-stress-robustness.sh:230`).
- validation_or_tests: This file is a validation suite. It complements `tests/test-template-loader.sh`, which covers standard substitutions and shell metacharacters (`tests/test-template-loader.sh:81`, `tests/test-template-loader.sh:257`, `tests/test-template-loader.sh:500`), and error robustness tests found in search results. It exits with the summary status, making it suitable for CI gating (`tests/robustness/test-template-stress-robustness.sh:289`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 Item Evidence sections, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`