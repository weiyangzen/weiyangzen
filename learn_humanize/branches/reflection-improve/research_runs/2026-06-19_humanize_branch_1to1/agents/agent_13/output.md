# agent_13 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-013 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared RLCR/PR-loop hook library directory. It contains `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/methodology-analysis.sh`; these files provide the common state machine helpers, prompt-template rendering, validator block messages, session-aware loop discovery, cancellation authorization, and methodology-analysis phase transitions used by the hook layer.
- algorithmic_behavior: `loop-common.sh` defines state fields and exit reasons at lines 24-65, validates hook JSON at lines 80-117, rejects deep JSON at lines 140-154, resolves active state priority as `methodology-analysis-state.md` then `finalize-state.md` then `state.md` at lines 217-229, finds the newest active loop with optional `session_id` filtering at lines 275-333, parses frontmatter in tolerant and strict modes at lines 397-495, detects Codex review issue markers in the last 50 log lines at lines 519-567, authorizes secure cancel moves at lines 741-903, detects protected Bash write patterns at lines 1278-1303, and renames terminal state through `end_loop` at lines 1326-1352. `template-loader.sh` resolves `prompt-template`, loads templates, does single-pass `{{VAR}}` substitution, provides fallback rendering, and validates template subdirs at lines 26-222. `methodology-analysis.sh` enters, completes, and blocks the methodology-analysis phase at lines 38-203.
- inputs_outputs_state: Inputs include hook JSON, loop directories under `.humanize/rlcr`, state YAML frontmatter, command strings, template files, Codex logs, and config JSON via `scripts/lib/config-loader.sh`. Outputs include shell return codes, rendered block messages, JSON hook responses from callers, mutated loop state file names through `mv`, review-result files, methodology marker files, and goal-tracker updates. State variables exported by parsing include `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PUSH_EVERY_ROUND`, `STATE_REVIEW_STARTED`, `STATE_SESSION_ID`, `STATE_AGENT_TEAMS`, and `STATE_PRIVACY_MODE`.
- gates_or_invariants: Source guards prevent double-sourcing in all three files. JSON validation rejects null bytes, invalid UTF-8 when `iconv` exists, malformed JSON, missing `tool_name`, and excessive nesting. Strict state parsing requires two `---` separators, required fields `current_round`, `max_iterations`, `review_started`, and `base_branch`, numeric round/max values, and boolean `review_started`. Active loop discovery intentionally checks only the newest directory without a session filter to avoid reviving stale loops, and with a session filter stops at the newest matching terminal or active loop. Methodology analysis cannot re-enter if `methodology-analysis-state.md` exists and skips if `methodology-analysis-done.md` already has content. Cancellation requires `.cancel-requested`, an exact two-argument `mv` from `state.md`, `finalize-state.md`, or `methodology-analysis-state.md` to `cancel-state.md`, no command chaining, no hidden variables, no command substitution, no mixed quote styles, and no symlink source.
- dependencies_and_callers: `loop-common.sh` sources `scripts/lib/config-loader.sh` at line 168 and `template-loader.sh` at line 195, then is sourced by read/write/edit/bash/plan validators, stop hooks, setup/cancel scripts, ask-codex, and PR-loop scripts. `methodology-analysis.sh` is sourced by `hooks/loop-codex-stop-hook.sh` and calls `load_and_render_safe`. `template-loader.sh` is used broadly by validators and scripts for block/prompt rendering. External dependencies include `bash`, `jq`, `sed`, `awk`, `grep`, `tail`, `wc`, `tr`, optional `iconv`, and `git` for some helper checks.
- edge_cases_or_failure_modes: `command_modifies_file` is pattern-based and explicitly misses some multi-source `cp` cases, documented in `tests/test-bash-validator-patterns.sh` lines 165-168. Strict parser returns distinct failure classes for missing file, missing YAML separators, missing fields, non-numeric fields, and invalid booleans. `find_active_loop` uses lexicographic newest sorting, so malformed timestamp names can affect newest selection. Template rendering is single-pass to avoid placeholder injection, but missing variables remain as `{{VAR}}`. Methodology completion fails closed on missing/empty done marker, missing/empty report, missing exit-reason marker, or invalid exit reason. Config loading is best-effort in `loop-common.sh`, but malformed required default config can still surface through `config-loader.sh`.
- validation_or_tests: Covered by many tests, including assigned `tests/test-bash-validator-patterns.sh`, `tests/test-stop-gate.sh`, and `tests/robustness/test-plan-file-robustness.sh`; additional relevant coverage includes `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-session-id.sh`, `tests/test-cancel-signal-file.sh`, `tests/robustness/test-state-file-robustness.sh`, `tests/robustness/test-state-transition-robustness.sh`, and `tests/robustness/test-session-robustness.sh`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-043 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: PostToolUse Bash hook that records the current Claude Code `session_id` into the newly created RLCR `state.md` immediately after `setup-rlcr-loop.sh` runs, binding subsequent hooks to the leader session.
- algorithmic_behavior: Reads hook JSON from stdin at line 27, resolves project root from `CLAUDE_PROJECT_DIR` or `pwd` at line 30, looks for `.humanize/.pending-session-id` at line 33, reads state path and setup command signature from that signal at lines 43-48, verifies the signal points to an existing state file at lines 50-54, extracts `.tool_input.command` with `jq` at lines 61-64, and only consumes the signal when the command starts with the full setup script path in quoted or unquoted form with an end/whitespace boundary at lines 70-91. It then extracts `.session_id` at lines 94-103, patches only an empty `session_id:` line with `awk` at lines 105-120, removes the one-shot signal at lines 122-123, and exits 0.
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/.pending-session-id` line 1 state path, line 2 setup script signature, and the state file’s `session_id:` field. Outputs are an updated `state.md` containing `session_id: <id>` and removal of the pending signal. If no signal, no matching setup command, no command field, no `session_id`, or already-populated state field, it exits without state mutation except cleanup of invalid signal paths.
- gates_or_invariants: Signal is one-shot only after successful recording. Command matching must start with the full setup path, preventing `echo /path/setup-rlcr-loop.sh`, basename-only mentions, and quoted-prefix concatenation. Current `session_id` must be empty before replacement. `awk -v new_id` avoids `sed` replacement hazards for slash/ampersand session IDs.
- dependencies_and_callers: The signal is created by `scripts/setup-rlcr-loop.sh` at lines 874-883 after it writes `session_id:` into `state.md` at lines 848-872. The recorded session drives `find_active_loop` session filtering in `hooks/lib/loop-common.sh` lines 275-333 and is consumed by validators such as `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, and `loop-plan-file-validator.sh`. Requires `jq` for command/session extraction; without `jq`, command extraction remains empty and signature-protected flows do not consume the signal.
- edge_cases_or_failure_modes: Invalid signal with missing/nonexistent state path is cleaned up. Empty `HOOK_COMMAND` preserves the signal for a later attempt. Empty `SESSION_ID` preserves the signal. A signal with no command signature skips command verification, relying only on state path and session ID. If the state line is `session_id: ` with spaces, the hook treats it as empty via `sed`; if the line is missing entirely, no patch occurs but the signal is still removed after the empty check path.
- validation_or_tests: `tests/test-session-id.sh` verifies state field creation, pending signal creation, successful quoted setup recording, no-op without signal, rejection of non-setup commands, preservation after false positives, special session IDs with `/` and `&`, rejection of echo/basename/quoted-prefix concatenation, and acceptance of unquoted and tab-delimited setup invocations around lines 110-165, 464-570, and 826-1050.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-073 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/loop-common.sh::command_modifies_file`, the regex helper used by Bash validators to block shell-based writes to protected RLCR files.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` at line 13, defines pass/fail counters at lines 20-34, lowercases each command with `to_lower`, and asserts whether `command_modifies_file` matches a target file pattern at lines 36-62. It exercises redirection, `tee`, in-place editors, file operations, `dd`, `truncate`, `exec` redirection, read-only false positives, edge cases, and alternate patterns for `state.md` and `round-N-summary.md`.
- inputs_outputs_state: Inputs are literal Bash command strings and optional regex target patterns such as `goal-tracker\.md`, `state\.md`, and `round-[0-9]+-summary\.md`. Outputs are PASS/FAIL lines and process exit 0 when `TESTS_FAILED=0`, else exit 1 at lines 206-214. It does not create RLCR state; it directly validates the detection primitive.
- gates_or_invariants: Commands are normalized to lowercase before matching, so case-insensitive protection is expected. Positive cases include `>`, `>>`, `tee`, `tee -a`, `sed -i`, `awk -i inplace`, `perl -i`/`-pie`, `mv`, `cp`, `rm`, `rm -f`, `rm -rf`, `dd of=`, `truncate`, `exec 3>`, and `printf ... >`. Negative cases include `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, `diff`, variable redirection to `$FILE`, and non-target pipelines.
- dependencies_and_callers: Depends on `loop-common.sh`, whose `command_modifies_file` pattern list is at lines 1278-1303. That helper is used in `hooks/loop-bash-validator.sh` to block state/finalize/methodology state writes, plan backup writes, goal tracker writes, prompt writes, summary writes, todo writes, PR state writes, and PR read-only file writes.
- edge_cases_or_failure_modes: The test documents a known limitation: `cp file1.md file2.md goal-tracker.md` is not detected because the pattern expects a two-argument `cp src dest` shape at lines 165-168. It also only expects literal target filename matching; variable target paths such as `$FILE` are intentionally not detected. Pattern matching is regex-based, not shell parsing, so coverage is broad but not complete.
- validation_or_tests: This file is itself the validation. It confirms `command_modifies_file` covers common write primitives and avoids basic read-only false positives. It complements broader `loop-bash-validator.sh` behavior at lines 245-527, where the helper is embedded in live hook gates.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-103 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/rlcr-stop-gate.sh`, the non-hook wrapper that reuses the RLCR stop-hook state machine from command-line or skill workflows.
- algorithmic_behavior: Builds a minimal active RLCR fixture in `setup_active_loop_fixture` at lines 18-59: initializes a git repo, creates `.humanize/rlcr/2026-03-01_00-00-00`, writes a plan and backup, and writes `state.md` with fields required by strict parsing including `current_round`, `max_iterations`, `push_every_round`, `plan_file`, `plan_tracked`, `base_branch`, `review_started`, `session_id`, and `agent_teams`. It then runs `scripts/rlcr-stop-gate.sh` from the target project cwd at lines 69-75, from outside with `--project-root` at lines 96-102, and in an empty project at lines 122-128.
- inputs_outputs_state: Inputs are temporary project directories, minimal loop state, and wrapper args. Expected outputs are exit code 10 with output containing `BLOCK:` when an active loop exists but required summary is missing, and exit code 0 with `ALLOW:` when no active loop exists. The test uses `tests/test-helpers.sh` pass/fail counters and exits with `print_test_summary` at lines 144-145.
- gates_or_invariants: Verifies the wrapper chooses caller cwd as default project root rather than the plugin install dir, honors `--project-root`, maps hook block JSON to human-readable `BLOCK:` output and exit 10, and maps no hook output to `ALLOW:` and exit 0. The fixture intentionally omits the current summary so `loop-codex-stop-hook.sh` reaches the “summary missing” block path after state parsing.
- dependencies_and_callers: Direct target is `scripts/rlcr-stop-gate.sh`, which builds Stop hook JSON with `cwd`, `session_id`, and `transcript_path` at lines 84-97, runs `hooks/loop-codex-stop-hook.sh` with `CLAUDE_PROJECT_DIR="$PROJECT_ROOT"` at line 102, returns wrapper errors as exit 20, block decisions as exit 10, and allow as exit 0. The delegated stop hook uses `hooks/lib/loop-common.sh` and `hooks/lib/methodology-analysis.sh`.
- edge_cases_or_failure_modes: Missing `jq` or missing/non-executable hook script is wrapper exit 20 in `rlcr-stop-gate.sh` lines 74-82. Non-JSON hook output is also exit 20 at lines 116-120. Any unexpected hook decision is exit 20 at lines 136-138. The test does not cover `--json`, wrapper option parsing errors, methodology-analysis phase, or malformed state, but it does prove project-root routing and core block/allow mapping.
- validation_or_tests: This file is the validation. The production block reason comes from `hooks/loop-codex-stop-hook.sh` summary-file gate around lines 707-720, while methodology-analysis completion precedes git checks at lines 585-605 and can short-circuit other stop behavior when active.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-133 `file` `prompt-template/block/methodology-analysis-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for attempts to modify `methodology-analysis-state.md`, a terminal-phase state file managed by the loop system during the methodology-analysis phase.
- algorithmic_behavior: Provides the user-facing denial text: the state file is managed by the loop system, and work should focus on spawning the analysis agent, reviewing the sanitized report, optionally filing an issue, and writing `methodology-analysis-done.md`. The template has no variables, so rendering is direct content loading.
- inputs_outputs_state: Input is a `load_and_render_safe` request for `block/methodology-analysis-state-file-modification.md`. Output is Markdown text emitted to stderr by validators. It does not modify state; it preserves the invariant that only system code renames or manages methodology-analysis state.
- gates_or_invariants: Used when paths match `methodology-analysis-state.md` before generic `state.md` because `state\.md` also matches the suffix. This ordering is explicit in write/edit/bash validators and in comments around `loop-write-validator.sh` lines 190-198, `loop-edit-validator.sh` lines 153-160, and `loop-bash-validator.sh` lines 245-252 and 402-409.
- dependencies_and_callers: Loaded by `methodology_analysis_state_file_blocked_message` in `hooks/lib/loop-common.sh` lines 702-709. Called by `loop-write-validator.sh` lines 195-197, `loop-edit-validator.sh` lines 158-160, and `loop-bash-validator.sh` lines 245-251, 402-409, and 436-442. Backed by `template-loader.sh::load_and_render_safe`.
- edge_cases_or_failure_modes: If the template is missing or empty, `loop-common.sh` has an inline fallback at lines 704-706. If a Bash command is an authorized cancel move, `is_cancel_authorized` can allow moving `methodology-analysis-state.md` to `cancel-state.md` instead of emitting this block. During methodology analysis, broader read/write/edit/bash restrictions may block before this specific template for non-state project-file access.
- validation_or_tests: Indirectly covered by validator tests for methodology-analysis restrictions and by template reference/comprehensive tests. The state path helper is in `hooks/lib/loop-common.sh` lines 697-700, and the methodology phase itself is entered/completed in `hooks/lib/methodology-analysis.sh` lines 38-170.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-163 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Conditional prompt footer snippet that tells Claude to push commits after each round when the RLCR loop is started with `--push-every-round`.
- algorithmic_behavior: Static two-line template with one instruction sentence. It is appended to generated next-round prompts only when `PUSH_EVERY_ROUND=="true"` in `hooks/loop-codex-stop-hook.sh` lines 1821-1828. The same state flag is written by setup at `scripts/setup-rlcr-loop.sh` lines 197-199 and 855.
- inputs_outputs_state: Input is the state field `push_every_round: true|false`, parsed as `STATE_PUSH_EVERY_ROUND` in `hooks/lib/loop-common.sh` line 365 and defaulted to false in tolerant/strict parse paths at lines 413 and 490. Output is additional prompt text appended to `NEXT_PROMPT_FILE`; if missing, the stop hook falls back to “Also push your changes after committing.”
- gates_or_invariants: The note aligns with two enforcement gates: `hooks/loop-bash-validator.sh` blocks `git push` when `push_every_round` is not true at lines 195-211, and `hooks/loop-codex-stop-hook.sh` blocks exit on local commits ahead of remote when `push_every_round` is true at lines 675-704. Thus prompt guidance and stop/command gates share one state flag.
- dependencies_and_callers: Loaded with `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` by `loop-codex-stop-hook.sh`. Related setup output also adds a push instruction on initial activation when the flag is true, per `scripts/setup-rlcr-loop.sh` references around lines 1211 and 1295.
- edge_cases_or_failure_modes: Missing template degrades to the inline fallback in the stop hook. If state omits `push_every_round`, parsers default false, so the note is not appended and `git push` remains blocked by Bash validator. This file is not itself a gate; it is a prompt contract synchronized with gates elsewhere.
- validation_or_tests: Indirectly validated through state parsing and push gate tests. References show `push_every_round` is parsed and displayed in `scripts/humanize.sh`, written in setup, checked in Bash validator, and checked in stop hook.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-193 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for production plan-file validation in `scripts/setup-rlcr-loop.sh`, especially content counting, large files, mixed encodings/line endings, and disappearing or invalid plan files.
- algorithmic_behavior: Creates an isolated temp repo, sets `XDG_CACHE_HOME`, installs a mock `codex` executable at lines 18-40, and defines `test_plan_validation` at lines 46-106 to run `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"` while capturing output to a temp file. It treats production validation errors such as “Plan is too simple”, “insufficient content”, “not found”, “not readable”, and “symbolic link” as rejection, and treats missing codex, gitignore errors, “start-rlcr-loop activated”, or created loop state as validation pass. It defines a local `count_content_lines` helper at lines 129-169 mirroring production comment/blank-line exclusion rules.
- inputs_outputs_state: Inputs are generated plan files in a temporary git repo: valid markdown, mostly-comment content, 5KB standard file, rich markdown, empty file, comments-only file, >1MB file, mixed CRLF/LF, binary/null-containing files, long-line files, special-character content, whitespace-only, complex HTML comments, missing files, unreadable files, symlinks, directories, and deleted files. Outputs are PASS/FAIL counters via `tests/test-helpers.sh` and exit status from `print_test_summary` at lines 557-558. Some tests call production setup, which may create `.humanize/rlcr` state in the temp repo.
- gates_or_invariants: Production validation requires a relative no-space path, no shell metacharacters, no file or parent-directory symlinks, existing/readable file, path within project, not in a git submodule, correct plan tracking/gitignore mode, at least five physical lines, and at least three content lines excluding blanks, `#` comment lines, single-line HTML comments, and multi-line HTML comments. These are implemented in `scripts/setup-rlcr-loop.sh` lines 427-643. The robustness test explicitly expects exactly three content lines to pass at lines 226-231, empty/comment-only plans to reject at lines 300-323, and large >1MB files to pass without SIGPIPE failure at lines 325-353.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `tests/test-helpers.sh`, `git`, `wc`, `chmod`, `ln`, `mktemp`, and shell builtins. Production setup also sources `hooks/lib/loop-common.sh` and uses `scripts/portable-timeout.sh`. The test’s mock `codex` prevents real Codex execution, keeping focus on validation.
- edge_cases_or_failure_modes: `test_plan_validation` is heuristic: it classifies some later setup failures as “validation passed” when the plan itself got past validation, such as “requires codex” or “must be gitignored.” Binary and null-byte tests mostly verify shell/file utilities tolerate the content rather than asserting production accepts those exact plans. The unreadable-file check may pass as “readable” when running as root. The disappearing-file case deletes the file before production validation, not literally during a read race.
- validation_or_tests: This file is the validation. It complements `tests/test-plan-file-validation.sh` and `tests/robustness/test-setup-scripts-robustness.sh`, while the production implementation under test is `scripts/setup-rlcr-loop.sh` lines 417-648.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `REFLECTION_IMPROVE-HZ-013`, `REFLECTION_IMPROVE-HZ-043`, `REFLECTION_IMPROVE-HZ-073`, `REFLECTION_IMPROVE-HZ-103`, `REFLECTION_IMPROVE-HZ-133`, `REFLECTION_IMPROVE-HZ-163`, `REFLECTION_IMPROVE-HZ-193`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`