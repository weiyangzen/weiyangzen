# agent_03 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command workflow definitions for the repository’s humanize plan, RLCR, PR-loop, and cancellation algorithms. Recursive inspection found exactly five command files: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, `commands/start-pr-loop.md`, `commands/cancel-rlcr-loop.md`, and `commands/cancel-pr-loop.md`.
- algorithmic_behavior: `commands/gen-plan.md` defines a strict no-coding planning pipeline: parse mode flags, load `.humanize/config.json`, validate IO, run draft relevance check, invoke Codex first-pass analysis, synthesize and converge a Claude/Codex plan, resolve pending decisions, write final plan, optionally auto-start RLCR only when converged in discussion mode and no pending decisions remain (`commands/gen-plan.md:21`, `commands/gen-plan.md:34`, `commands/gen-plan.md:120`, `commands/gen-plan.md:172`, `commands/gen-plan.md:243`, `commands/gen-plan.md:582`). `commands/start-rlcr-loop.md` defines preflight plan compliance, plan quiz, and then delegates setup to `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:14`, `commands/start-rlcr-loop.md:61`, `commands/start-rlcr-loop.md:110`). `commands/start-pr-loop.md` delegates PR loop setup and describes bot review polling and Codex validation (`commands/start-pr-loop.md:10`, `commands/start-pr-loop.md:16`). Cancel commands delegate to their scripts and interpret first-line machine statuses (`commands/cancel-pr-loop.md:11`, `commands/cancel-rlcr-loop.md:11`).
- inputs_outputs_state: Inputs are slash-command `$ARGUMENTS`, plan/draft paths, bot flags, Codex model/timeout options, plan tracking/base branch options, and cancellation confirmation choices. Outputs include generated plan files, optional translated plan variants, `.humanize/rlcr/<timestamp>/state.md`, prompts and summaries, `.humanize/pr-loop/<timestamp>/state.md`, and terminal state files such as `cancel-state.md`, `approve-state.md`, `finalize-state.md`, and `complete-state.md`. `scripts/setup-rlcr-loop.sh` persists key RLCR fields such as `current_round`, `max_iterations`, `codex_model`, `push_every_round`, `full_review_round`, `plan_file`, `base_branch`, `review_started`, `agent_teams`, `privacy_mode`, and BitLesson fields (`scripts/setup-rlcr-loop.sh:848`).
- gates_or_invariants: The directory enforces command-level allowed-tool boundaries in YAML frontmatter: gen-plan can call validation, ask-codex, setup-rlcr, read/glob/grep/task/write/AskUserQuestion (`commands/gen-plan.md:4`); start-rlcr can call setup plus Read/Task/AskUserQuestion (`commands/start-rlcr-loop.md:4`); PR loop setup is restricted to `setup-pr-loop.sh` (`commands/start-pr-loop.md:4`). Gen-plan is explicitly sequential and non-parallel across phases (`commands/gen-plan.md:36`). Start-RLCR fails closed on plan compliance agent output: only `PASS` continues; relevance or branch-switch failures stop (`commands/start-rlcr-loop.md:53`). Cancel-RLCR adds a finalize confirmation gate before force-cancel (`commands/cancel-rlcr-loop.md:22`).
- dependencies_and_callers: The command files are entrypoints that depend on `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/cancel-pr-loop.sh`, Task agents `humanize:draft-relevance-checker`, `humanize:plan-compliance-checker`, and `humanize:plan-understanding-quiz`, plus templates under `prompt-template/plan`, `prompt-template/claude`, and `prompt-template/pr-loop`.
- edge_cases_or_failure_modes: Gen-plan rejects simultaneous `--discussion` and `--direct`, unsupported language config, malformed config JSON, non-relevant drafts, Codex unavailability, unresolved decisions, direct mode auto-start attempts, and auto-start failures (`commands/gen-plan.md:58`, `commands/gen-plan.md:81`, `commands/gen-plan.md:198`, `commands/gen-plan.md:245`, `commands/gen-plan.md:604`). Start-RLCR skips precheck for help/skip-impl, skips unsafe absolute/traversal plan paths in markdown precheck so setup can validate, blocks malformed plan-compliance output, and allows user continuation after quiz failure (`commands/start-rlcr-loop.md:18`, `commands/start-rlcr-loop.md:29`, `commands/start-rlcr-loop.md:53`, `commands/start-rlcr-loop.md:101`). PR loop requires at least one bot flag and requires push/tag behavior for remote review (`commands/start-pr-loop.md:25`, `commands/start-pr-loop.md:50`).
- validation_or_tests: `tests/test-gen-plan.sh` validates gen-plan command structure, Codex-first and convergence sections, direct-mode non-convergence, auto-start discussion-mode condition, task tag requirements, template matching, and `validate-gen-plan-io.sh` exit behavior (`tests/test-gen-plan.sh:119`, `tests/test-gen-plan.sh:557`, `tests/test-gen-plan.sh:708`). `tests/test-plan-file-validation.sh` covers setup-RLCR plan path/tracking validation. `tests/test-agent-teams.sh` covers `--agent-teams` state and template insertion. `tests/test-pr-loop-3-stophook.sh` runs the PR stop-hook integration split.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-033 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Command workflow contract for starting RLCR, including preflight plan compliance, advisory plan-understanding quiz, setup invocation, and high-level loop/review phase semantics.
- algorithmic_behavior: The file first extracts a plan path from either `--plan-file <path>` or the first positional argument, ignoring values of known flags (`commands/start-rlcr-loop.md:22`). It applies a basic path safety gate for the markdown-level precheck: relative path only, no parent traversal, and a restricted character set (`commands/start-rlcr-loop.md:29`). If readable, it invokes `humanize:plan-compliance-checker` and requires an exact PASS/FAIL class (`commands/start-rlcr-loop.md:42`). It then optionally invokes `humanize:plan-understanding-quiz`, parses two multiple-choice questions plus answer keys and summary, asks the user, and either proceeds or stops based on the user’s choice after wrong answers (`commands/start-rlcr-loop.md:61`, `commands/start-rlcr-loop.md:87`, `commands/start-rlcr-loop.md:101`). Finally it runs `scripts/setup-rlcr-loop.sh $ARGUMENTS` (`commands/start-rlcr-loop.md:110`).
- inputs_outputs_state: Inputs are command arguments including plan path, `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--claude-answer-codex`, `--agent-teams`, `--yolo`, `--skip-quiz`, and `--privacy` (`commands/start-rlcr-loop.md:3`). Output is an initialized RLCR loop under `.humanize/rlcr/<timestamp>/`, with setup-script state and prompt files. The delegated setup script writes `state.md` with current round, model/effort, plan/base branch, review flags, agent teams, privacy, and BitLesson settings (`scripts/setup-rlcr-loop.sh:848`), plus `.pending-session-id` for hook session capture (`scripts/setup-rlcr-loop.sh:874`).
- gates_or_invariants: Precheck is skipped only for `--skip-impl` and help (`commands/start-rlcr-loop.md:18`). Compliance checker is fail-closed on `FAIL_RELEVANCE`, `FAIL_BRANCH_SWITCH`, or malformed output (`commands/start-rlcr-loop.md:53`). Quiz is advisory but can stop if the user chooses to review the plan first (`commands/start-rlcr-loop.md:101`). Setup enforces no simultaneous active RLCR/PR loops (`scripts/setup-rlcr-loop.sh:340`), validates `--agent-teams` requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`scripts/setup-rlcr-loop.sh:357`), rejects both positional and `--plan-file` (`scripts/setup-rlcr-loop.sh:371`), and enforces plan file containment/tracking rules (`scripts/setup-rlcr-loop.sh:500`, `scripts/setup-rlcr-loop.sh:554`).
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh`; uses Task agents `humanize:plan-compliance-checker` and `humanize:plan-understanding-quiz`; depends indirectly on `hooks/loop-codex-stop-hook.sh`, `hooks/lib/loop-common.sh`, prompt templates, BitLesson scripts, and Codex CLI during stop-hook review. The command is referenced by gen-plan auto-start (`commands/gen-plan.md:590`).
- edge_cases_or_failure_modes: Unsafe plan paths skip markdown-level precheck and are left to setup validation (`commands/start-rlcr-loop.md:31`). Missing/unreadable plan content skips precheck and quiz (`commands/start-rlcr-loop.md:40`, `commands/start-rlcr-loop.md:70`). Malformed quiz output warns and proceeds (`commands/start-rlcr-loop.md:87`). `--skip-impl` bypasses implementation setup and starts review path (`commands/start-rlcr-loop.md:189`); setup creates `.review-phase-started` in that mode (`scripts/setup-rlcr-loop.sh:885`). Branch-switching instructions are explicitly incompatible with RLCR (`commands/start-rlcr-loop.md:56`).
- validation_or_tests: `tests/test-plan-file-validation.sh` exercises plan path, symlink, submodule, tracking, clean-state, `--plan-file`, and model validation cases. `tests/test-agent-teams.sh` validates agent-teams env gating, state field persistence, prompt inclusion, and no review-phase continuation injection. Stop-hook behavior is covered by RLCR stop-hook tests and shared parser tests in `hooks/lib/loop-common.sh`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-063 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Side-effect-free IO and argument validator for `commands/gen-plan.md`; it is the first hard gate before draft relevance and planning synthesis.
- algorithmic_behavior: Parses `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, `--direct`, and help (`scripts/validate-gen-plan-io.sh:35`). It rejects missing values, unknown options, and mutually exclusive `--discussion`/`--direct` (`scripts/validate-gen-plan-io.sh:76`). It normalizes input/output with `realpath -m` when available, derives the output directory, checks input existence and non-empty content, checks output directory existence, rejects output directory or pre-existing output file, verifies write permission, then locates `prompt-template/plan/gen-plan-template.md` through `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`scripts/validate-gen-plan-io.sh:98`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:132`, `scripts/validate-gen-plan-io.sh:147`, `scripts/validate-gen-plan-io.sh:162`).
- inputs_outputs_state: Inputs are command-line args and environment `CLAUDE_PLUGIN_ROOT`. Outputs are stdout diagnostic lines including `VALIDATION_SUCCESS` and `TEMPLATE_FILE: <path>` for command parsing (`scripts/validate-gen-plan-io.sh:157`, `scripts/validate-gen-plan-io.sh:177`). It does not create the output file and exits with machine-readable codes 0-7 documented in the header (`scripts/validate-gen-plan-io.sh:4`).
- gates_or_invariants: Exit code contract: 1 input missing, 2 input empty, 3 output directory missing, 4 output exists/is directory, 5 no write permission, 6 invalid args/help, 7 missing template (`scripts/validate-gen-plan-io.sh:4`). It accepts `--auto-start-rlcr-if-converged` as a flag but only warns when combined with `--direct`; it does not block because gen-plan later decides auto-start eligibility (`scripts/validate-gen-plan-io.sh:93`).
- dependencies_and_callers: Called by `commands/gen-plan.md` Phase 1 (`commands/gen-plan.md:120`). Depends on shell builtins, `realpath`, `dirname`, `wc`, and the plan template at `prompt-template/plan/gen-plan-template.md`.
- edge_cases_or_failure_modes: `realpath` failure falls back to original path, which may preserve relative paths for later checks (`scripts/validate-gen-plan-io.sh:98`). Help intentionally exits 6 via `usage`, matching invalid-argument handling (`scripts/validate-gen-plan-io.sh:16`). `/dev/null` as input is considered an empty file by `-s`, so flag-recognition tests can receive validation errors without being unknown-option failures. Output directory write check is environment-dependent under restricted or read-only filesystems (`scripts/validate-gen-plan-io.sh:147`).
- validation_or_tests: `tests/test-gen-plan.sh` includes script tests for missing values, unknown option, input not found, empty input, missing output directory, existing output, directory output, valid paths, auto-start flag, discussion/direct recognition, mutual exclusion, and help exit code (`tests/test-gen-plan.sh:557`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-093 `file` `tests/test-pr-loop-3-stophook.sh`
- cursor: `[_]`
- core_role: Third parallel test runner for PR loop stop-hook integration behavior. It is an executable specification wrapper around the slow stop-hook polling module.
- algorithmic_behavior: Sets strict shell mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources `tests/test-helpers.sh` and `tests/test-pr-loop-lib.sh`, skips entirely in GitHub Actions, initializes the PR loop test environment, sources `tests/test-pr-loop-stophook.sh`, runs `run_stophook_tests`, prints a test summary, and exits with the accumulated test status (`tests/test-pr-loop-3-stophook.sh:10`, `tests/test-pr-loop-3-stophook.sh:12`, `tests/test-pr-loop-3-stophook.sh:18`, `tests/test-pr-loop-3-stophook.sh:24`, `tests/test-pr-loop-3-stophook.sh:26`, `tests/test-pr-loop-3-stophook.sh:28`).
- inputs_outputs_state: Inputs are environment variables, especially `GITHUB_ACTIONS`, plus helper-provided `TEST_DIR` and mocked PATH state. It creates temporary `.humanize/pr-loop/<timestamp>/state.md` fixtures through the sourced test module, then invokes `hooks/pr-loop-stop-hook.sh`. Outputs are pass/fail counters and summary lines from helper functions; state outputs in fixtures include updated `state.md`, `approve-state.md`, and `round-*-pr-check.md`.
- gates_or_invariants: The wrapper preserves split-test isolation: only stop-hook tests are run, not the full PR loop suite (`tests/test-pr-loop-3-stophook.sh:3`). It avoids slow or network-sensitive polling behavior in GitHub Actions by marking the module skipped (`tests/test-pr-loop-3-stophook.sh:18`). It requires helper sourcing before environment initialization and test loading (`tests/test-pr-loop-3-stophook.sh:15`).
- dependencies_and_callers: Depends directly on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and `tests/test-pr-loop-stophook.sh`. The sourced module mocks `gh`, `git`, and `codex`, sets `CLAUDE_PROJECT_DIR`, and calls `hooks/pr-loop-stop-hook.sh`. The hook itself uses `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, and templates.
- edge_cases_or_failure_modes: If helpers or the sourced stop-hook module are absent, sourcing fails under `set -euo pipefail`. In CI it intentionally skips, so CI does not prove stop-hook polling behavior. Because tests use shell mocks and timeouts, failures can reflect mock drift with hook CLI usage. The slow module covers timeout-sensitive behavior, including bot response delays and Claude eyes retry timing.
- validation_or_tests: The sourced `run_stophook_tests` includes force-push old-trigger rejection, startup case 1 no-trigger exception, empty `active_bots` approval, unpushed commit blocking, force-push history rewrite detection, missing trigger blocking, bot timeout auto-removal, Codex +1 approval, Claude eyes timeout, dynamic `startup_case` persistence, fork PR base repo resolution, and mixed approval behavior (`tests/test-pr-loop-stophook.sh:1768`). Concrete invariants include unpushed commits from `git status -sb` blocking (`tests/test-pr-loop-stophook.sh:493`), force-push simulated by `git merge-base` exit 1 (`tests/test-pr-loop-stophook.sh:596`), timeout producing empty `active_bots` in `approve-state.md` (`tests/test-pr-loop-stophook.sh:874`), and mixed approval leaving Codex active while removing Claude (`tests/test-pr-loop-stophook.sh:1746`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-123 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Block-message template fragment used by RLCR git-clean gating to explain that `.humanize/` is local loop state and should not be committed.
- algorithmic_behavior: Contains a short special-case note: `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` via `echo '.humanize*' >> .gitignore` followed by `git add .gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2`).
- inputs_outputs_state: It has no direct runtime inputs. It is loaded as template text and appended into `SPECIAL_NOTES` when the stop hook sees untracked `.humanize*` paths while constructing a dirty-git block message (`hooks/loop-codex-stop-hook.sh:628`). Output is user-facing guidance inside the JSON block reason emitted by the hook.
- gates_or_invariants: The hook excludes untracked `.humanize/` from the dirty determination itself because plugin state is intentionally untracked (`hooks/loop-codex-stop-hook.sh:618`). However, if other uncommitted changes cause a git-clean block and `.humanize*` is present among untracked files, this template is included as a corrective invariant: ignore `.humanize*`, do not commit it (`hooks/loop-codex-stop-hook.sh:625`).
- dependencies_and_callers: Called by `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:630`). Rendered with the broader `block/git-not-clean.md` template through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:659`).
- edge_cases_or_failure_modes: The template suggests appending `.humanize*` and staging `.gitignore`, but does not mention checking for an existing ignore entry, so repeated manual use may duplicate lines. It also stages only `.gitignore`; the broader hook still requires all non-`.humanize/` changes to be committed before exit. If the template is missing, the hook falls back to a generic note (`hooks/loop-codex-stop-hook.sh:631`).
- validation_or_tests: Related tests assert `.humanize*` ignore behavior in plan-file/finalize contexts (`tests/test-plan-file-hooks.sh:792`, `tests/test-finalize-phase.sh:170`). The actual caller path is covered through RLCR stop-hook dirty-git behavior.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-153 `file` `prompt-template/claude/agent-teams-core.md`
- cursor: `[_]`
- core_role: Shared agent-teams instruction block for RLCR prompts. It changes the implementation algorithm from single-agent execution to team-leader coordination and delegation.
- algorithmic_behavior: The template assigns the primary agent the role of team leader and prohibits direct coding or file edits (`prompt-template/claude/agent-teams-core.md:1`). It instructs the leader to split work into independent units, spawn team members with the Task tool and `team_name`, prevent overlapping file changes, monitor progress, wait for teammates, review outputs, and coordinate commits (`prompt-template/claude/agent-teams-core.md:5`, `prompt-template/claude/agent-teams-core.md:16`). It highlights cold-start context requirements for team members and strict file ownership to avoid silent overwrites (`prompt-template/claude/agent-teams-core.md:17`, `prompt-template/claude/agent-teams-core.md:18`).
- inputs_outputs_state: Input is `agent_teams: true` in RLCR state, set by `setup-rlcr-loop.sh --agent-teams` (`scripts/setup-rlcr-loop.sh:245`, `scripts/setup-rlcr-loop.sh:865`). Output is additional prompt text in `round-0-prompt.md` during setup (`scripts/setup-rlcr-loop.sh:1155`) and in subsequent implementation prompts when still outside review phase (`hooks/loop-codex-stop-hook.sh:1837`).
- gates_or_invariants: Setup refuses `--agent-teams` unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`scripts/setup-rlcr-loop.sh:357`). The template’s invariant is no direct implementation by the team leader: any urge to implement must be redirected to a team member (`prompt-template/claude/agent-teams-core.md:12`). It requires strict file boundaries and sequencing for same-file edits because parallel agent writes can silently overwrite (`prompt-template/claude/agent-teams-core.md:18`). Continuation injection is skipped in review phase, preserving code-review semantics (`hooks/loop-codex-stop-hook.sh:1837`).
- dependencies_and_callers: Loaded by `scripts/setup-rlcr-loop.sh` along with `agent-teams-instructions.md` for the initial prompt (`scripts/setup-rlcr-loop.sh:1157`) and by `hooks/loop-codex-stop-hook.sh` with `agent-teams-continue.md` for continuation prompts (`hooks/loop-codex-stop-hook.sh:1840`). Tested by `tests/test-agent-teams.sh`.
- edge_cases_or_failure_modes: If template files are missing during setup, setup writes a shorter fallback block (`scripts/setup-rlcr-loop.sh:1164`), reducing detail but preserving the core delegation requirement. In review phase, team continuation instructions are intentionally omitted, so users should not expect team mode to apply to Codex review prompts. The template asks each teammate to commit their own changes (`prompt-template/claude/agent-teams-core.md:21`), which can conflict with repositories that prefer single integrator commits unless the leader coordinates sequencing.
- validation_or_tests: `tests/test-agent-teams.sh` checks env var gating, `agent_teams: true/false` in `state.md`, initial prompt inclusion, absence without the flag, template existence/content, active-loop parsing with agent_teams, continuation injection during implementation, and no continuation injection in review phase (`tests/test-agent-teams.sh:29`, `tests/test-agent-teams.sh:114`, `tests/test-agent-teams.sh:212`, `tests/test-agent-teams.sh:314`, `tests/test-agent-teams.sh:556`, `tests/test-agent-teams.sh:614`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-183 `file` `skills/humanize-rlcr/SKILL.md`
- cursor: `[_]`
- core_role: Skill-mode RLCR flow definition that preserves hook-equivalent behavior in environments without native hooks by routing all phase transitions through `scripts/rlcr-stop-gate.sh`.
- algorithmic_behavior: The skill requires setup via `setup-rlcr-loop.sh`, then each round reads the generated loop prompt, implements changes, commits, writes the required summary, and runs `rlcr-stop-gate.sh` with optional session/transcript arguments (`skills/humanize-rlcr/SKILL.md:29`, `skills/humanize-rlcr/SKILL.md:41`, `skills/humanize-rlcr/SKILL.md:51`). Gate exit handling is explicit: `0` means allowed/done, `10` means blocked and continue according to instructions, `20` means infrastructure error and must not be faked as completion (`skills/humanize-rlcr/SKILL.md:61`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, optional `CLAUDE_SESSION_ID`, and optional `CLAUDE_TRANSCRIPT_PATH` (`skills/humanize-rlcr/SKILL.md:19`, `skills/humanize-rlcr/SKILL.md:53`). Outputs and state are generated under `.humanize/rlcr/<timestamp>/`: prompts, summaries, review results, `state.md`, `finalize-state.md`, and terminal files. The wrapper builds Stop-hook JSON with `hook_event_name`, `stop_hook_active`, `cwd`, `session_id`, and `transcript_path`, then invokes `hooks/loop-codex-stop-hook.sh` (`scripts/rlcr-stop-gate.sh:84`, `scripts/rlcr-stop-gate.sh:102`).
- gates_or_invariants: The skill explicitly forbids reimplementing review logic manually and requires calling the wrapper (`skills/humanize-rlcr/SKILL.md:10`). It forbids manual edits to `state.md` or `finalize-state.md`, skipping blocked gate results, and ad-hoc `codex exec`/`codex review` in place of the gate (`skills/humanize-rlcr/SKILL.md:85`). Through the wrapper, it inherits state/schema validation, branch consistency, plan-file integrity, incomplete Task/Todo blocking, git-clean and push-every-round checks, summary presence, max-iteration handling, full-alignment rounds, COMPLETE/STOP marker handling, review-phase marker guard, `[P0-9]` code-review gating, and open-question handling (`skills/humanize-rlcr/SKILL.md:66`).
- dependencies_and_callers: Depends on hydrated runtime root, `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/loop-codex-stop-hook.sh`, `jq`, generated prompt/summary files, and Codex. `rlcr-stop-gate.sh` maps hook block JSON to exit 10 and hook/runtime errors to 20 (`scripts/rlcr-stop-gate.sh:8`, `scripts/rlcr-stop-gate.sh:126`).
- edge_cases_or_failure_modes: Setup non-zero stops the skill (`skills/humanize-rlcr/SKILL.md:39`). Missing or non-executable hook script and missing `jq` produce wrapper exit 20 (`scripts/rlcr-stop-gate.sh:74`, `scripts/rlcr-stop-gate.sh:79`). Non-JSON hook output or unexpected hook decision also exits 20 (`scripts/rlcr-stop-gate.sh:116`, `scripts/rlcr-stop-gate.sh:136`). A blocked hook result must continue the loop rather than being treated as success (`skills/humanize-rlcr/SKILL.md:63`). The skill’s options table says `--skip-quiz` is implicit in skill mode, but the underlying setup script treats `--skip-quiz` as a no-op because quiz logic lives in command markdown (`scripts/setup-rlcr-loop.sh:253`).
- validation_or_tests: `scripts/rlcr-stop-gate.sh` itself documents and enforces exit-code mapping (`scripts/rlcr-stop-gate.sh:8`). `hooks/loop-codex-stop-hook.sh` validates active state detection, schema fields, model/effort safety, finalize completion, review phase transitions, and review marker consistency (`hooks/loop-codex-stop-hook.sh:77`, `hooks/loop-codex-stop-hook.sh:172`, `hooks/loop-codex-stop-hook.sh:869`, `hooks/loop-codex-stop-hook.sh:1604`, `hooks/loop-codex-stop-hook.sh:1632`). Shared parsing is in `hooks/lib/loop-common.sh`, including active-state precedence and strict field validation (`hooks/lib/loop-common.sh:217`, `hooks/lib/loop-common.sh:424`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `REFLECTION_IMPROVE-HZ-003`, `REFLECTION_IMPROVE-HZ-033`, `REFLECTION_IMPROVE-HZ-063`, `REFLECTION_IMPROVE-HZ-093`, `REFLECTION_IMPROVE-HZ-123`, `REFLECTION_IMPROVE-HZ-153`, `REFLECTION_IMPROVE-HZ-183`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`