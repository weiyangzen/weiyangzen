# agent_02 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Prompt-spec directory for specialized validation and routing agents used by plan generation, RLCR startup, and BitLesson discipline. It contains four agent definitions: `agents/draft-relevance-checker.md`, `agents/plan-compliance-checker.md`, `agents/bitlesson-selector.md`, and `agents/plan-understanding-quiz.md`.
- algorithmic_behavior: `draft-relevance-checker` is a lenient semantic relevance gate for `gen-plan`; it explores docs/structure and returns exactly `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:14`, `agents/draft-relevance-checker.md:27`). `plan-compliance-checker` performs two RLCR preflight checks: repository relevance and branch-switch detection, then emits one of `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`agents/plan-compliance-checker.md:16`, `agents/plan-compliance-checker.md:31`, `agents/plan-compliance-checker.md:50`). `bitlesson-selector` maps a task plus project `bitlesson.md` to precise lesson IDs or `NONE` with deterministic two-line output (`agents/bitlesson-selector.md:12`, `agents/bitlesson-selector.md:26`, `agents/bitlesson-selector.md:32`). `plan-understanding-quiz` generates exactly two technical comprehension questions plus a plan summary to detect whether the user understands a plan before RLCR starts (`agents/plan-understanding-quiz.md:16`, `agents/plan-understanding-quiz.md:31`, `agents/plan-understanding-quiz.md:57`).
- inputs_outputs_state: Inputs are prompt-provided document content, repo context discovered through `Read`, `Glob`, and `Grep`, plus task/file paths for BitLesson selection. Outputs are strict text contracts: relevance verdicts (`agents/draft-relevance-checker.md:27`), compliance verdicts (`agents/plan-compliance-checker.md:48`), `LESSON_IDS`/`RATIONALE` (`agents/bitlesson-selector.md:34`), and 13 quiz fields (`agents/plan-understanding-quiz.md:59`). These files do not persist state themselves; callers consume their verdicts and decide whether to continue, block, or ask the user.
- gates_or_invariants: Relevance checks are intentionally lenient and pass when plausibly connected (`agents/draft-relevance-checker.md:33`, `agents/plan-compliance-checker.md:29`). Branch-switch detection must reject explicit branch/worktree switching but avoid false positives for file restore, negated instructions, descriptive branch references, and `--base-branch` review configuration (`agents/plan-compliance-checker.md:33`, `agents/plan-compliance-checker.md:40`). BitLesson selection prefers precision and returns `NONE` if nothing directly applies (`agents/bitlesson-selector.md:28`). Quiz output must include all fields and one A-D answer per question (`agents/plan-understanding-quiz.md:79`).
- dependencies_and_callers: `commands/gen-plan.md` invokes `humanize:draft-relevance-checker` during Phase 2 (`commands/gen-plan.md:142`, `commands/gen-plan.md:149`). `commands/start-rlcr-loop.md` invokes `humanize:plan-compliance-checker` before setup and `humanize:plan-understanding-quiz` for advisory readiness (`commands/start-rlcr-loop.md:42`, `commands/start-rlcr-loop.md:76`). `bitlesson-selector` is required by RLCR workflow text and agent-team prompts (`commands/start-rlcr-loop.md:160`, `prompt-template/claude/agent-teams-core.md:23` found by reference scan). Runtime routing for `bitlesson-selector` is described as `scripts/bitlesson-select.sh`, choosing Codex for `gpt-*` or Claude for Claude-family models (`agents/bitlesson-selector.md:21`).
- edge_cases_or_failure_modes: Malformed compliance output is fail-closed by `commands/start-rlcr-loop.md` (`commands/start-rlcr-loop.md:53`, `commands/start-rlcr-loop.md:57`). Quiz output malformed or missing fields is advisory-only and continues with a warning (`commands/start-rlcr-loop.md:87`). Draft relevance can false-pass by design when uncertain (`agents/draft-relevance-checker.md:36`), while branch-switch checks also lean pass when uncertain to avoid false positives (`agents/plan-compliance-checker.md:60`).
- validation_or_tests: `tests/test-gen-plan.sh` validates `draft-relevance-checker.md` existence, name, model `haiku`, tools field, frontmatter, valid naming, valid model, and English-only content (`tests/test-gen-plan.sh:111`, `tests/test-gen-plan.sh:255`, `tests/test-gen-plan.sh:269`, `tests/test-gen-plan.sh:282`, `tests/test-gen-plan.sh:421`, `tests/test-gen-plan.sh:523`, `tests/test-gen-plan.sh:548`). `tests/test-bitlesson-select-routing.sh` validates routing/output format for BitLesson selection by reference. No direct exhaustive tests were found for all `plan-compliance-checker` or `plan-understanding-quiz` prompt semantics beyond caller parsing rules in `commands/start-rlcr-loop.md`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-032 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command definition that starts the PR review loop by delegating to `scripts/setup-pr-loop.sh`. It is a workflow contract rather than an implementation script.
- algorithmic_behavior: The command permits only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)` and runs `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh" $ARGUMENTS` (`commands/start-pr-loop.md:4`, `commands/start-pr-loop.md:12`). It defines the high-level loop: detect PR, fetch specified bot reviews, let the user fix issues, push, trigger bot review, stop hook polls every 30 seconds up to 15 minutes, and local Codex validates bot feedback (`commands/start-pr-loop.md:16`, `commands/start-pr-loop.md:22`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, especially required bot flags `--claude` and/or `--codex`, optional max iterations and Codex model/timeout (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25`). Output is produced by the setup script: activation message, initial PR-loop prompt, loop directory, and required resolution-summary path. State is created under `.humanize/pr-loop/<timestamp>/` by `scripts/setup-pr-loop.sh`, including `state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-prompt.md`, and expected `round-0-pr-resolve.md` (`scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:721`).
- gates_or_invariants: At least one bot flag is required (`commands/start-pr-loop.md:25`, `scripts/setup-pr-loop.sh:180`). Human comments take priority over bot comments (`commands/start-pr-loop.md:31`). The user must write summaries, push fixes, use the correct `@bot` mention, avoid state-file edits/cancel bypasses, and trust the stop hook (`commands/start-pr-loop.md:50`). The loop ends only by max iteration count, all monitored bots approving, or `/humanize:cancel-pr-loop` (`commands/start-pr-loop.md:58`).
- dependencies_and_callers: Direct dependency is `scripts/setup-pr-loop.sh` (`commands/start-pr-loop.md:13`). The setup script depends on `gh`, authenticated GitHub CLI, `codex`, Git repo state, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, optional `scripts/check-bot-reactions.sh`, template rendering via `hooks/lib/template-loader.sh`, and shared loop helpers in `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:32`, `scripts/setup-pr-loop.sh:35`).
- edge_cases_or_failure_modes: The delegated setup script blocks if an RLCR or PR loop is already active (`scripts/setup-pr-loop.sh:222`, `scripts/setup-pr-loop.sh:234`), if not in a usable Git repo (`scripts/setup-pr-loop.sh:243`), if no PR exists for the branch (`scripts/setup-pr-loop.sh:344` inferred from nearby PR detection continuation), if PR is merged/closed (`scripts/setup-pr-loop.sh:365` by read context), if branch/model/effort contain YAML-unsafe characters (`scripts/setup-pr-loop.sh:390` by read context), or if Claude trigger eyes reaction cannot be verified for `--claude` after posting/reusing a trigger (`scripts/setup-pr-loop.sh:564`, `scripts/setup-pr-loop.sh:587`). Fork PRs are explicitly handled by resolving current vs parent/base repo before PR operations (`scripts/setup-pr-loop.sh:290`, `scripts/setup-pr-loop.sh:334`).
- validation_or_tests: Covered by PR loop script/system suites and the assigned hook runner. `tests/test-pr-loop-scripts.sh` targets `setup-pr-loop.sh`; `tests/test-pr-loop-system.sh` has integration cases including missing `trigger_comment_id` and goal tracker creation by setup. `tests/test-unified-codex-config.sh` verifies PR-loop Codex defaults/overrides for `setup-pr-loop.sh` by reference scan. `tests/test-pr-loop-2-hooks.sh` validates the hook side of this command’s lifecycle.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-062 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Claude Code status-line script that includes one Humanize-specific algorithmic field: RLCR status display. The file itself states only the final `RLCR` field is related to this repository (`scripts/statusline.sh:7`, `scripts/statusline.sh:12`).
- algorithmic_behavior: Reads JSON from stdin, extracts model/cwd/session/cost/context values with `jq`, computes a compact terminal status line, and resolves `.humanize/rlcr` loop state into display labels (`scripts/statusline.sh:19`, `scripts/statusline.sh:22`, `scripts/statusline.sh:257`). `_resolve_rlcr_display` maps state files to `Analyzing`, `Finalizing`, `Active`, terminal reason derived from `*-state.md`, or `Off` (`scripts/statusline.sh:36`, `scripts/statusline.sh:40`). `get_rlcr_status` mirrors `find_active_loop()` logic: if no session-aware state exists, use newest directory as zombie-loop protection; if session-aware files exist, prefer session-aware directories or match a specific session id (`scripts/statusline.sh:65`, `scripts/statusline.sh:76`, `scripts/statusline.sh:82`, `scripts/statusline.sh:121`).
- inputs_outputs_state: Input is Claude Code status JSON with `.model.display_name`, `.cwd`, `.session_id`, `.transcript_path`, `.cost.*`, and `.context_window.used_percentage` (`scripts/statusline.sh:170`). Output is one or more formatted terminal lines packed to 75% of terminal width (`scripts/statusline.sh:379`, `scripts/statusline.sh:384`, `scripts/statusline.sh:398`). It reads but does not mutate `.humanize/rlcr`, transcript JSONL, sessions index, Git branch, and `$HOME/.claude/settings.json` (`scripts/statusline.sh:189`, `scripts/statusline.sh:204`, `scripts/statusline.sh:222`, `scripts/statusline.sh:238`).
- gates_or_invariants: Missing `.humanize` or loop dir returns `Off` (`scripts/statusline.sh:71`, `scripts/statusline.sh:258`). Session-aware loops ignore session-unaware entries once any state has `session_id` (`scripts/statusline.sh:76`, `scripts/statusline.sh:142`). State precedence is methodology-analysis, finalize, active state, then other terminal state (`scripts/statusline.sh:40`, `scripts/statusline.sh:100`, `scripts/statusline.sh:129`). Color mapping treats Active/Finalizing as green, Complete cyan, Cancel/Stop/Pause yellow, Maxiter/Failed/Timeout red, Off dim (`scripts/statusline.sh:157`).
- dependencies_and_callers: Requires `jq`, `git`, `stty`/`tput`, `sed`, `awk`, `grep`, and standard shell tools. It coordinates with RLCR state naming conventions in `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md,methodology-analysis-state.md,*-state.md}` and with `session_id` written by hook/setup machinery.
- edge_cases_or_failure_modes: If `jq` is missing or input fields are absent, most values degrade to empty/`?` because `get_value` suppresses errors (`scripts/statusline.sh:22`, `scripts/statusline.sh:241`). It uses `ls | sort -r` on timestamp directories, so nonconforming directory names can affect newest selection (`scripts/statusline.sh:86`, `scripts/statusline.sh:116`). It strips spaces from parsed `session_id`, which is fine for UUID-like ids but would alter ids containing spaces (`scripts/statusline.sh:111`, `scripts/statusline.sh:140`). Terminal width detection falls back to `80` when `/dev/tty` is unavailable (`scripts/statusline.sh:380`).
- validation_or_tests: No dedicated statusline test was found in the assigned set. Its own disclaimer classifies it as a reference/template and indicates future Humanize updates generally should not involve it (`scripts/statusline.sh:11`, `scripts/statusline.sh:13`). The underlying session-aware loop logic is conceptually tied to `hooks/lib/loop-common.sh` and session-id tests, but this script is not directly exercised by the PR-loop hook runner.
- skip_candidate: `yes: mostly generic status-line UI/reference script; only lines 36-167 and 257-263 participate in Humanize RLCR status display, not core PR/RLCR state transition logic`

### REFLECTION_IMPROVE-HZ-092 `file` `tests/test-pr-loop-2-hooks.sh`
- cursor: `[_]`
- core_role: Thin executable test runner for the PR-loop hook subset. It initializes a shared PR-loop test environment, sources the hook test implementation, runs `run_hook_tests`, prints a summary, and exits with the aggregated result (`tests/test-pr-loop-2-hooks.sh:14`, `tests/test-pr-loop-2-hooks.sh:17`, `tests/test-pr-loop-2-hooks.sh:19`, `tests/test-pr-loop-2-hooks.sh:21`).
- algorithmic_behavior: Delegates behavior to `tests/test-pr-loop-hooks.sh`, whose `run_hook_tests` covers validator protections, comment sorting, gatekeeper logic, stop-hook integration, mocked E2E stop-hook execution, approval-only review handling, fixture-backed fetch/poll behavior, wrong-round validation, and monitor display (`tests/test-pr-loop-hooks.sh:15`, `tests/test-pr-loop-hooks.sh:27`, `tests/test-pr-loop-hooks.sh:223`, `tests/test-pr-loop-hooks.sh:272`, `tests/test-pr-loop-hooks.sh:539`, `tests/test-pr-loop-hooks.sh:767`, `tests/test-pr-loop-hooks.sh:1161`, `tests/test-pr-loop-hooks.sh:1243`, `tests/test-pr-loop-hooks.sh:1432`, `tests/test-pr-loop-hooks.sh:1548`).
- inputs_outputs_state: Inputs are repo test helpers, mock temp directories, fixture JSON files, mocked `gh`/`git` binaries, hook JSON payloads, and PR-loop state files created under test dirs (`tests/test-pr-loop-2-hooks.sh:14`, `tests/test-pr-loop-hooks.sh:479`, `tests/test-pr-loop-hooks.sh:1232`). Output is pass/fail accounting via `print_test_summary "PR Loop Hook Tests"` (`tests/test-pr-loop-2-hooks.sh:23`).
- gates_or_invariants: The suite asserts PR-loop state and comment files are write-protected but current-round `round-N-pr-resolve.md` writes are allowed (`tests/test-pr-loop-hooks.sh:69`, `tests/test-pr-loop-hooks.sh:103`, `tests/test-pr-loop-hooks.sh:136`). It asserts `active_bots`/`configured_bots` YAML-list parsing and display (`tests/test-pr-loop-hooks.sh:27`, `tests/test-pr-loop-hooks.sh:289`, `tests/test-pr-loop-hooks.sh:699`, `tests/test-pr-loop-hooks.sh:1548`). It enforces human-before-bot and newest-first sorting (`tests/test-pr-loop-hooks.sh:223`, `tests/test-pr-loop-hooks.sh:240`), deduplication by comment id (`tests/test-pr-loop-hooks.sh:272`), `APPROVE`/`WAITING_FOR_BOTS` marker recognition (`tests/test-pr-loop-hooks.sh:416`, `tests/test-pr-loop-hooks.sh:438`), trigger detection filtered to current user and paginated comments (`tests/test-pr-loop-hooks.sh:539`, `tests/test-pr-loop-hooks.sh:948`), trigger-anchored timeout (`tests/test-pr-loop-hooks.sh:668`), and wrong-round resolve blocking (`tests/test-pr-loop-hooks.sh:1432`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-hooks.sh`, hooks `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `pr-loop-stop-hook.sh`, scripts `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `humanize.sh`, and fixture/mock scripts (`tests/test-pr-loop-2-hooks.sh:14`, `tests/test-pr-loop-hooks.sh:1232`). It is one split of a larger PR-loop suite; comments state `test-pr-loop.sh` is the combined runner (`tests/test-pr-loop-2-hooks.sh:5`).
- edge_cases_or_failure_modes: The runner itself has no filtering or recovery; `set -euo pipefail` means sourcing/init failures abort (`tests/test-pr-loop-2-hooks.sh:9`). The underlying hook tests deliberately cover missing resolve summaries, missing trigger comments for later rounds, pagination, approval-only empty-body reviews, fork/base repo fixture behavior, wrong-round edits, and monitor empty active bot display (`tests/test-pr-loop-hooks.sh:607`, `tests/test-pr-loop-hooks.sh:767`, `tests/test-pr-loop-hooks.sh:948`, `tests/test-pr-loop-hooks.sh:1161`, `tests/test-pr-loop-hooks.sh:1199`, `tests/test-pr-loop-hooks.sh:1597`).
- validation_or_tests: This file is itself validation. I did not execute it because the task requested research notes only and the sandbox is read-only/restricted. Direct inspection shows it is an executable specification wrapper, not the implementation of hook behavior.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-122 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template shown when a Bash validator detects a `git add` command that would stage `.humanize/` local loop state. It supports the invariant that runtime loop state must not be committed.
- algorithmic_behavior: The template explains that `.humanize/` contains local loop state, is already intended for `.gitignore`, and the command was blocked because it would add `.humanize` files (`prompt-template/block/git-add-humanize.md:1`, `prompt-template/block/git-add-humanize.md:3`, `prompt-template/block/git-add-humanize.md:6`). It directs the user toward specific-path staging, directory-specific staging, or patch mode, and lists blocked broad/forced examples (`prompt-template/block/git-add-humanize.md:8`, `prompt-template/block/git-add-humanize.md:16`).
- inputs_outputs_state: The template has no dynamic placeholders. It is loaded by `git_add_humanize_blocked_message` as `block/git-add-humanize.md`, with an inline fallback if template loading fails (`hooks/lib/loop-common.sh:1221`, `hooks/lib/loop-common.sh:1258`). The output is stderr content when `hooks/loop-bash-validator.sh` exits with code `2` after detection (`hooks/loop-bash-validator.sh:221`).
- gates_or_invariants: Blocked commands include direct `.humanize`, `git add -A`, `git add --all`, `git add .`, and `git add -f .` examples (`prompt-template/block/git-add-humanize.md:20`). If adding `.humanize*` to `.gitignore` is needed, the template requires staging only `.gitignore` and using a commit message that does not contain literal `.humanize` to avoid re-triggering protection (`prompt-template/block/git-add-humanize.md:26`, `prompt-template/block/git-add-humanize.md:34`).
- dependencies_and_callers: Detection is implemented by `git_adds_humanize` in `hooks/lib/loop-common.sh`, which handles direct references, quoted/path variants, forced broad scope, `-A/--all` when `.humanize` exists, and broad `.`/`*` when `.humanize` is not ignored (`hooks/lib/loop-common.sh:1101`, `hooks/lib/loop-common.sh:1160`, `hooks/lib/loop-common.sh:1191`, `hooks/lib/loop-common.sh:1203`, `hooks/lib/loop-common.sh:1209`). `hooks/loop-bash-validator.sh` calls this check for any active loop after parsing Bash hook input (`hooks/loop-bash-validator.sh:215`, `hooks/loop-bash-validator.sh:221`).
- edge_cases_or_failure_modes: The detector receives lowercased commands, so it explicitly accounts for `-A` becoming `-a` (`hooks/lib/loop-common.sh:1105`). It splits shell chains on `&&`, `||`, `|`, and `;` (`hooks/lib/loop-common.sh:1126`). It avoids over-blocking `.humanizeconfig` and similar names by requiring `.humanize` to be a path component (`hooks/lib/loop-common.sh:1160`). Broad `git add .` is only blocked if `.humanize` exists and is not ignored; force broad adds bypass `.gitignore` and are blocked regardless (`hooks/lib/loop-common.sh:1191`, `hooks/lib/loop-common.sh:1198`, `hooks/lib/loop-common.sh:1211`).
- validation_or_tests: `tests/test-humanize-escape.sh` directly tests `git_adds_humanize` path variants, quoted paths, force-add cases, `-A/--all`, chained commands, and false positives such as `.humanizeconfig` (`tests/test-humanize-escape.sh:41`, `tests/test-humanize-escape.sh:83`, `tests/test-humanize-escape.sh:96`, `tests/test-humanize-escape.sh:109`, `tests/test-humanize-escape.sh:125`, `tests/test-humanize-escape.sh:153`, `tests/test-humanize-escape.sh:190`). This assigned template is also covered indirectly whenever `loop-bash-validator.sh` blocks and renders the message.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-152 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Continuation prompt block for Claude Agent Teams mode inside the RLCR loop after Codex feedback. It tells the team leader how to start a fresh team for the next iteration without relying on vanished previous agents.
- algorithmic_behavior: The block sets context that this is a continuation from a previous round where Codex reviewed work and provided feedback (`prompt-template/claude/agent-teams-continue.md:3`). It instructs the team leader to review feedback before spawning agents, avoid redoing correct work, provide cold-start context to new team members, prioritize critical items if one round is insufficient, and verify current file state before assigning ownership (`prompt-template/claude/agent-teams-continue.md:7`, `prompt-template/claude/agent-teams-continue.md:8`, `prompt-template/claude/agent-teams-continue.md:9`, `prompt-template/claude/agent-teams-continue.md:10`, `prompt-template/claude/agent-teams-continue.md:11`, `prompt-template/claude/agent-teams-continue.md:12`).
- inputs_outputs_state: Inputs are previous Codex review feedback, goal tracker/prior summaries, current file state, and team-leader decisions. Output is not structured data but constrained behavior for the next RLCR round: a newly spawned team with explicit acceptance criteria and current-state context. It does not write state itself; it guides the RLCR prompt assembly consumed by Claude.
- gates_or_invariants: Previous team members no longer exist and must not be messaged or referenced (`prompt-template/claude/agent-teams-continue.md:7`). New team members have no conversation history, so the leader must include prior accomplishments, current state, review findings, and acceptance criteria, while not repeating `CLAUDE.md` (`prompt-template/claude/agent-teams-continue.md:10`). Work should target review issues and gaps only, not restart already accepted work (`prompt-template/claude/agent-teams-continue.md:9`).
- dependencies_and_callers: It belongs to the Claude prompt-template family used by RLCR setup/stop-hook prompt construction. Related templates include `prompt-template/claude/agent-teams-core.md`, `agent-teams-instructions.md`, `next-round-prompt.md`, and review/finalize prompts found in the same directory. The command surface exposes `--agent-teams` in `commands/start-rlcr-loop.md` arguments (`commands/start-rlcr-loop.md:3`), and the RLCR flow’s review/next-round prompts coordinate with Codex feedback (`commands/start-rlcr-loop.md:123`, `commands/start-rlcr-loop.md:124`).
- edge_cases_or_failure_modes: The main failure mode is stale or missing context: old team members cannot be contacted, and new ones lack previous conversation history (`prompt-template/claude/agent-teams-continue.md:7`, `prompt-template/claude/agent-teams-continue.md:10`). Another risk is over-scoping a round; the template mitigates this by prioritizing high-priority review issues when remaining work exceeds a single team’s capacity (`prompt-template/claude/agent-teams-continue.md:11`).
- validation_or_tests: `tests/test-agent-teams.sh` exists by repository file listing and likely covers agent-team templates, but I did not need to inspect it deeply for this assigned prompt block. Template-reference coverage is also likely part of `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh` by file listing. No direct parser contract exists in this block; it is behavioral prompt guidance.
- skip_candidate: `yes: important RLCR prompt guidance, but not an executable algorithm or state-machine implementation; it is a continuation policy block consumed by surrounding RLCR prompt assembly`

### REFLECTION_IMPROVE-HZ-182 `file` `skills/humanize-gen-plan/SKILL.md`
- cursor: `[_]`
- core_role: Flow-skill instruction for generating a structured implementation plan from a draft. It mirrors and condenses the `gen-plan` command algorithm: validate IO, check relevance, analyze/fix draft issues, handle metrics, generate plan structure, write output, review consistency, unify language, and report success.
- algorithmic_behavior: The Mermaid flow defines a strict state transition sequence from `BEGIN` to `VALIDATE`, relevance check, analysis loop, metric confirmation, plan generation, write, review/fix loop, language unification, and success/failure termination (`skills/humanize-gen-plan/SKILL.md:18`, `skills/humanize-gen-plan/SKILL.md:20`, `skills/humanize-gen-plan/SKILL.md:25`, `skills/humanize-gen-plan/SKILL.md:28`, `skills/humanize-gen-plan/SKILL.md:32`, `skills/humanize-gen-plan/SKILL.md:35`, `skills/humanize-gen-plan/SKILL.md:36`, `skills/humanize-gen-plan/SKILL.md:37`, `skills/humanize-gen-plan/SKILL.md:41`, `skills/humanize-gen-plan/SKILL.md:44`). It requires generated plans to contain goal description, AC-X acceptance criteria with positive and negative tests, path boundaries, allowed/prohibited choices, dependencies/milestones, and implementation notes (`skills/humanize-gen-plan/SKILL.md:54`, `skills/humanize-gen-plan/SKILL.md:64`, `skills/humanize-gen-plan/SKILL.md:72`, `skills/humanize-gen-plan/SKILL.md:84`, `skills/humanize-gen-plan/SKILL.md:91`).
- inputs_outputs_state: Required inputs are `--input <draft.md>` and `--output <plan.md>` (`skills/humanize-gen-plan/SKILL.md:48`). The skill references an installer-hydrated runtime root and calls `scripts/validate-gen-plan-io.sh` with the draft/output paths (`skills/humanize-gen-plan/SKILL.md:12`, `skills/humanize-gen-plan/SKILL.md:20`). Output is the new plan file and success report including plan path, AC count, and language status (`skills/humanize-gen-plan/SKILL.md:36`, `skills/humanize-gen-plan/SKILL.md:44`).
- gates_or_invariants: Validation failure stops before reading/generation (`skills/humanize-gen-plan/SKILL.md:21`, `skills/humanize-gen-plan/SKILL.md:22`). Irrelevant drafts stop with an error (`skills/humanize-gen-plan/SKILL.md:25`, `skills/humanize-gen-plan/SKILL.md:26`). Issues trigger AskUserQuestion resolution and loop back to analysis (`skills/humanize-gen-plan/SKILL.md:29`, `skills/humanize-gen-plan/SKILL.md:30`). Quantitative metrics require confirmation as hard requirement or trend (`skills/humanize-gen-plan/SKILL.md:32`, `skills/humanize-gen-plan/SKILL.md:33`). Inconsistencies loop until fixed (`skills/humanize-gen-plan/SKILL.md:37`, `skills/humanize-gen-plan/SKILL.md:38`, `skills/humanize-gen-plan/SKILL.md:39`). The output file must be new; validation exit code `4` covers already existing output (`skills/humanize-gen-plan/SKILL.md:103`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, whose concrete exit codes match the skill table: missing input `1`, empty input `2`, missing output dir `3`, existing output `4`, no write permission `5`, invalid args `6`, missing template `7` (`scripts/validate-gen-plan-io.sh:4`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:116`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:132`, `scripts/validate-gen-plan-io.sh:147`, `scripts/validate-gen-plan-io.sh:171`). The command implementation in `commands/gen-plan.md` expands this with config loading, Codex first-pass analysis, convergence loop, optional alternate-language output, and optional RLCR auto-start (`commands/gen-plan.md:34`, `commands/gen-plan.md:64`, `commands/gen-plan.md:172`).
- edge_cases_or_failure_modes: Missing runtime-root hydration would leave `{{HUMANIZE_RUNTIME_ROOT}}` unresolved in the skill text, but command execution normally uses `CLAUDE_PLUGIN_ROOT` instead (`skills/humanize-gen-plan/SKILL.md:12`, `commands/gen-plan.md:125`). `validate-gen-plan-io.sh` uses `realpath -m` when available and falls back to raw paths, so path canonicalization can vary by platform (`scripts/validate-gen-plan-io.sh:98`). The validator is side-effect-free and does not create the output file (`commands/gen-plan.md:138`), but `commands/gen-plan.md` may create default `.humanize/config.json` before validation/config phases if missing (`commands/gen-plan.md:68`, `commands/gen-plan.md:70`).
- validation_or_tests: `tests/test-gen-plan.sh` validates command frontmatter, `ask-codex.sh` permission/reference, auto-start option, draft relevance agent metadata, naming, YAML/frontmatter, model validity, and English-only content (`tests/test-gen-plan.sh:125`, `tests/test-gen-plan.sh:131`, `tests/test-gen-plan.sh:255`, `tests/test-gen-plan.sh:269`, `tests/test-gen-plan.sh:421`, `tests/test-gen-plan.sh:548`). The IO validator itself encodes explicit exit codes and messages (`scripts/validate-gen-plan-io.sh:103`, `scripts/validate-gen-plan-io.sh:155`, `scripts/validate-gen-plan-io.sh:177`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: REFLECTION_IMPROVE-HZ-002, REFLECTION_IMPROVE-HZ-032, REFLECTION_IMPROVE-HZ-062, REFLECTION_IMPROVE-HZ-092, REFLECTION_IMPROVE-HZ-122, REFLECTION_IMPROVE-HZ-152, REFLECTION_IMPROVE-HZ-182
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`