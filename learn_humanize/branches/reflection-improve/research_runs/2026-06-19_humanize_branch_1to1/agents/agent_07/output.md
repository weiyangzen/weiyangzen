# agent_07 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-007 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Template contract surface for the RLCR and PR-loop state machines. It defines prompts, validator block messages, review gates, next-round instructions, plan skeletons, and goal-tracker update contracts consumed by hooks through `hooks/lib/template-loader.sh`.
- algorithmic_behavior: The directory is organized into `block/`, `claude/`, `codex/`, `plan/`, and `pr-loop/`. `block/` contains fail-closed user-facing gate reasons such as incomplete tasks, force push, no trigger, protected state files, git dirty state, git status failure, and missing summaries. `claude/` templates drive implementation/finalize/next-round behavior, including task creation, read-only goal tracker rules, summary locations, and update-request routing. `codex/` templates define review and full-alignment acceptance logic; `prompt-template/codex/regular-review.md:51-57` says Codex should output findings unless all plan tasks are complete and the last line `COMPLETE` is the only completion condition. `plan/gen-plan-template.md:69-78` defines task routing with exactly one `coding` or `analyze` tag and dependency columns. `pr-loop/` templates define the PR-review loop lifecycle: initial goal tracker schema at `prompt-template/pr-loop/goal-tracker-initial.md:15-25`, no-comments round-0 waiting behavior at `prompt-template/pr-loop/round-0-task-no-comments.md:6-14`, has-comments fix/push/re-trigger behavior at `prompt-template/pr-loop/round-0-task-has-comments.md:6-23`, and Codex goal-tracker update mechanics at `prompt-template/pr-loop/codex-goal-tracker-update.md:7-20`.
- inputs_outputs_state: Inputs are `{{VAR}}` placeholders rendered by `render_template`; `hooks/lib/template-loader.sh:7-13` defines placeholder syntax and single-pass semantics, while `hooks/lib/template-loader.sh:58-131` performs substitution from `VAR=value` inputs. Outputs are rendered markdown prompts or block reasons written to hook JSON responses, next-round prompt files, review prompts, or goal tracker instructions. The templates do not mutate state directly; they encode allowed transitions such as block, continue, finalize, complete, trigger required, and update request.
- gates_or_invariants: Template loading is fail-soft with fallbacks where needed; `load_and_render_safe` falls back on missing/empty templates at `hooks/lib/template-loader.sh:167-203`. Syntax invariant is uppercase placeholders with no spaces, checked in `tests/test-templates-comprehensive.sh:122-193`. Directory structure invariant requires `block`, `codex`, and `claude` subdirectories in `hooks/lib/template-loader.sh:205-221`; tests also assert these at `tests/test-templates-comprehensive.sh:64-80`. PR-loop invariants include no state-file modification (`prompt-template/pr-loop/round-0-task-has-comments.md:30-35`), push before review when comments require fixes (`prompt-template/pr-loop/round-0-task-has-comments.md:15-20`), read-only goal tracker after round 0 (`prompt-template/claude/next-round-prompt.md:27-36`), and no completion with deferrals or pending work (`prompt-template/codex/regular-review.md:53-57`).
- dependencies_and_callers: `hooks/lib/template-loader.sh` is the central dependency. `hooks/loop-codex-stop-hook.sh` consumes many block and claude templates, including `block/git-status-failed.md` at `hooks/loop-codex-stop-hook.sh:480-493` and `claude/goal-tracker-update-request.md` at `hooks/loop-codex-stop-hook.sh:1830-1835`. `hooks/pr-loop-stop-hook.sh` sets `TEMPLATE_DIR` at `hooks/pr-loop-stop-hook.sh:57-58` and consumes PR-loop/block templates for unpushed commits, force push, no trigger, Claude eyes timeout, and goal tracker updates. `scripts/validate-gen-plan-io.sh` references the plan template. `scripts/install-skill.sh` treats `prompt-template` as a runtime component to copy/install.
- edge_cases_or_failure_modes: Missing templates can produce empty or misleading block messages if unsafe loading is used; `tests/test-template-references.sh:172-200` specifically checks critical validators use safe rendering. Placeholder injection is mitigated by single-pass rendering in `hooks/lib/template-loader.sh:54-57`. Unreferenced templates are warnings, not failures, because some templates can be used dynamically (`tests/test-template-references.sh:130-140`). Large and special-character substitutions are covered by comprehensive rendering tests, including multiline and shell-like values in `tests/test-templates-comprehensive.sh:320-397`.
- validation_or_tests: `tests/test-template-references.sh:83-110` scans hook `load_template`, `load_and_render`, and `load_and_render_safe` calls and verifies referenced files exist. `tests/test-templates-comprehensive.sh:90-107` loads every template recursively, and `tests/test-templates-comprehensive.sh:118-194` validates placeholder syntax. `tests/test-template-loader.sh` and robustness template tests exercise rendering behavior with special characters, nested placeholder values, empty values, and stress cases.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-037 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook preflight gate that prevents RLCR/Codex review from running while Claude still has incomplete tasks. It bridges legacy transcript `TodoWrite` state and the newer file-backed Claude task system.
- algorithmic_behavior: `extract_tool_calls_from_entry` recognizes assistant-message, message, and direct `tool_use` transcript shapes (`hooks/check-todos-from-transcript.py:23-55`). `find_incomplete_todos_from_transcript` reads JSONL, ignores malformed lines, tracks only the most recent `TodoWrite` list, and returns todos whose status is not `completed` (`hooks/check-todos-from-transcript.py:58-101`). `find_incomplete_tasks_from_directory` reads `~/.claude/tasks/<session_id>/*.json` or a test override and treats any status other than `completed` or `deleted` as incomplete (`hooks/check-todos-from-transcript.py:104-147`). `main` merges task-directory findings and transcript findings, prints `INCOMPLETE_TODOS` plus details, and exits 1 when work remains (`hooks/check-todos-from-transcript.py:150-196`).
- inputs_outputs_state: Input is hook JSON on stdin containing optional `session_id`, `tasks_base_dir`, and `transcript_path` (`hooks/check-todos-from-transcript.py:15`, `hooks/check-todos-from-transcript.py:165-175`). Output is exit code 0 for no incomplete tasks or absent data, exit code 1 with stdout marker/list for incomplete tasks, and exit code 2 with stderr `PARSE_ERROR` for invalid hook JSON (`hooks/check-todos-from-transcript.py:9-13`, `hooks/check-todos-from-transcript.py:158-161`, `hooks/check-todos-from-transcript.py:193-196`). It reads state but does not mutate transcript or task files.
- gates_or_invariants: Only `completed` closes legacy todos (`hooks/check-todos-from-transcript.py:91-99`). Only `completed` and `deleted` close file-backed tasks (`hooks/check-todos-from-transcript.py:130-132`). Missing transcript path, nonexistent transcript file, missing session directory, empty stdin, and malformed JSONL lines fail open as no incomplete tasks, while invalid hook JSON fails as parse error (`hooks/check-todos-from-transcript.py:64-65`, `hooks/check-todos-from-transcript.py:76-80`, `hooks/check-todos-from-transcript.py:121-122`, `hooks/check-todos-from-transcript.py:153-161`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh:380-436` before git status and Codex review. On exit 2, the stop hook blocks with a parse-error reason (`hooks/loop-codex-stop-hook.sh:394-410`). On exit 1, it renders `prompt-template/block/incomplete-todos.md` and blocks immediately (`hooks/loop-codex-stop-hook.sh:413-435`). Depends only on Python stdlib: `json`, `sys`, `pathlib`, and typing imports.
- edge_cases_or_failure_modes: Malformed task JSON and unreadable task files are skipped rather than blocking (`hooks/check-todos-from-transcript.py:143-145`), so corruption can hide a task. Empty `content` is still reported as an incomplete todo with a blank label (`hooks/check-todos-from-transcript.py:183-191`). Combining task-directory and transcript inputs can report both systems at once; there is no deduplication across sources. A missing task directory is treated as no tasks, which is correct for absent state but fail-open if the directory path is wrong.
- validation_or_tests: `tests/test-todo-checker.sh:52-98` validates input handling and parse/missing-file exits. `tests/test-todo-checker.sh:107-158` covers completed, pending, and in-progress legacy todos. `tests/test-todo-checker.sh:197-240` confirms latest `TodoWrite`, direct `tool_use`, and `message` formats. `tests/test-todo-checker.sh:309-455` covers file-backed tasks, default pending status, deleted-task ignore behavior, mixed legacy/task inputs, task ID output, and nonexistent sessions.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-067 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Test fixture generator for a mock `gh` CLI used by PR-loop API and hook tests. It isolates core PR-loop behavior from live GitHub by returning deterministic fixture JSON.
- algorithmic_behavior: The script requires `<mock_bin_dir> <fixtures_dir>`, creates the bin directory, writes an executable `gh` shell script, and prints the mock bin path (`tests/setup-fixture-mock-gh.sh:16-24`, `tests/setup-fixture-mock-gh.sh:27-101`). The generated mock handles `gh auth status`, `gh repo view`, `gh pr view`, and `gh api` endpoint patterns. It returns owner/name and parent repo stubs (`tests/setup-fixture-mock-gh.sh:40-52`), PR number/state stubs (`tests/setup-fixture-mock-gh.sh:54-62`), current user (`tests/setup-fixture-mock-gh.sh:64-69`), issue comments, review comments, and PR reviews from fixture files (`tests/setup-fixture-mock-gh.sh:71-87`), and `[]` for unknown API endpoints (`tests/setup-fixture-mock-gh.sh:89-91`).
- inputs_outputs_state: Inputs are a target mock binary directory and a fixtures directory containing `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`. Output is `$MOCK_BIN_DIR/gh`, executable, plus stdout of `$MOCK_BIN_DIR`. It mutates only the provided test directory, not repository state.
- gates_or_invariants: Missing arguments cause usage text and exit 1 (`tests/setup-fixture-mock-gh.sh:19-22`). Unhandled non-API commands print an error and exit 1 (`tests/setup-fixture-mock-gh.sh:95-96`). The fixture directory path is captured into the generated script at creation time (`tests/setup-fixture-mock-gh.sh:31`), making later PATH-based tests deterministic.
- dependencies_and_callers: Consumed by `tests/test-pr-loop-hooks.sh:1237` to create a fixture-backed `gh`. It supports scripts named in its header: fetch/poll PR comment and review scripts (`tests/setup-fixture-mock-gh.sh:3-5`). Depends on fixture files under `tests/fixtures/` and shell tools `mkdir`, `cat`, `chmod`.
- edge_cases_or_failure_modes: Pattern matching is coarse (`"$2" == *"/issues/"*"/comments"*` etc.), so it validates endpoint classes rather than exact URLs. It does not simulate pagination, jq transformations, auth failure, rate limits, missing fixture files, or fork PR lookup beyond returning `parent: null`. Missing fixture JSON would fail when the generated mock tries to `cat` it.
- validation_or_tests: Its direct validation is through callers such as `tests/test-pr-loop-hooks.sh`, which inject it into PATH for PR-loop hook behavior. The script itself is an executable test fixture rather than a production hook.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-097 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/pr-loop-stop-hook.sh`, especially PR-loop transition gates around triggers, force pushes, bot responses, active bot shrinkage, fork lookup, timeout behavior, and goal-tracker correctness.
- algorithmic_behavior: Defines `run_stophook_tests` and nested test functions, then executes the stop-hook test sequence at `tests/test-pr-loop-stophook.sh:1768-1780`. Each test builds an isolated `.humanize/pr-loop/<timestamp>/state.md`, creates round resolve files, writes mock `gh`/`git`/sometimes `codex` commands into a temporary PATH, sets `CLAUDE_PROJECT_DIR`, invokes `hooks/pr-loop-stop-hook.sh`, and asserts block or transition behavior.
- inputs_outputs_state: Inputs are synthetic PR-loop state frontmatter fields: `current_round`, `pr_number`, `configured_bots`, `active_bots`, `poll_timeout`, `last_trigger_at`, `trigger_comment_id`, `startup_case`, `latest_commit_sha`, and `latest_commit_at`. Outputs under test are hook stdout/stderr block text, mutation or replacement of `state.md`, creation of `approve-state.md`, creation of check files such as `round-1-pr-check.md`, and updates to `active_bots`. The test does not depend on live GitHub or git history because it controls `gh` and `git`.
- gates_or_invariants: The suite asserts old trigger comments before `latest_commit_at` are rejected after a force push (`tests/test-pr-loop-stophook.sh:25-47`, `tests/test-pr-loop-stophook.sh:76-83`, `tests/test-pr-loop-stophook.sh:129-135`). Case 1 round 0 does not require a trigger (`tests/test-pr-loop-stophook.sh:140-239`). Empty `active_bots` completes by creating `approve-state.md` (`tests/test-pr-loop-stophook.sh:246-320`). Unpushed commits block (`tests/test-pr-loop-stophook.sh:493-532`). Non-ancestor commit history detects force push (`tests/test-pr-loop-stophook.sh:538-637`). Case 4 missing trigger blocks (`tests/test-pr-loop-stophook.sh:642-748`). Bot timeout must remove bots and can complete the loop (`tests/test-pr-loop-stophook.sh:754-890`). Codex `+1` reactions approve codex (`tests/test-pr-loop-stophook.sh:896-1005`). Missing Claude eyes reaction blocks (`tests/test-pr-loop-stophook.sh:1011-1139`). Dynamic `startup_case` re-evaluation must persist a changed case (`tests/test-pr-loop-stophook.sh:1145-1385`). Fork PRs must resolve through parent repo (`tests/test-pr-loop-stophook.sh:1390-1514`). Mixed approval must not inflate resolved counts or complete early; codex stays active while claude is removed (`tests/test-pr-loop-stophook.sh:1519-1763`).
- dependencies_and_callers: This file is sourced by `tests/test-pr-loop-3-stophook.sh` and `tests/test-pr-loop.sh` according to repository search results. It depends on `tests/test-helpers.sh` for `pass`/`fail` and `$TEST_DIR`/`$PROJECT_ROOT`, on `timeout`, `date` GNU/BSD fallbacks, and on the real `hooks/pr-loop-stop-hook.sh`. The implementation under test parses state at `hooks/pr-loop-stop-hook.sh:88-175`, resolves fork/base repo around `hooks/pr-loop-stop-hook.sh:202-224`, checks unpushed commits at `hooks/pr-loop-stop-hook.sh:313-360`, detects force pushes at `hooks/pr-loop-stop-hook.sh:365-421`, validates trigger comments at `hooks/pr-loop-stop-hook.sh:721-747`, verifies Claude eyes at `hooks/pr-loop-stop-hook.sh:768-790`, and handles polling/timeouts around `hooks/pr-loop-stop-hook.sh:802-980`.
- edge_cases_or_failure_modes: Many assertions accept output text alternatives rather than exact JSON decisions, making them robust but less strict about exact reason payloads. Several tests use real time with small timeouts and `timeout`, so slow machines can affect polling behavior. Mock command coverage is custom per test and can miss real `gh` option combinations. The suite intentionally validates compatibility with both GNU and BSD `date` syntax in dynamic timestamp tests (`tests/test-pr-loop-stophook.sh:1528-1534`).
- validation_or_tests: The file is itself the validation artifact. It covers the stop-hook’s critical state transitions from open `state.md` to blocked exit, updated `state.md`, or terminal `approve-state.md`, and it validates that approval is per-bot rather than all-or-nothing.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-127 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Fail-closed block template used when a stop hook cannot verify repository cleanliness because `git status` failed or timed out.
- algorithmic_behavior: Renders a concise blocking reason with the failing exit code placeholder `{{GIT_STATUS_EXIT}}`, then lists likely causes and asks the agent/user to manually check git status (`prompt-template/block/git-status-failed.md:1-10`). It does not decide the gate; it is the message surface for a gate in hook code.
- inputs_outputs_state: Input is `GIT_STATUS_EXIT`. Output is markdown included in a hook JSON `reason`. No state mutation.
- gates_or_invariants: The underlying invariant is fail-closed repository verification: if git status cannot run, the loop must not proceed. `hooks/loop-codex-stop-hook.sh:474-493` captures `git status --porcelain`, cleans stale `index.lock` on failure, renders this template through `load_and_render_safe`, and blocks with system message `Loop: Blocked - git status failed`.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh:489-490`. It depends on `hooks/lib/template-loader.sh` safe rendering and is covered by reference validation.
- edge_cases_or_failure_modes: The template covers lock contention, invalid repo state, and slow large repos (`prompt-template/block/git-status-failed.md:5-8`). If the template is missing, the caller has an inline fallback (`hooks/loop-codex-stop-hook.sh:484-488`). If `GIT_STATUS_EXIT` is unset by caller error, the placeholder would remain or render blank depending on passed variables.
- validation_or_tests: Template existence is checked by `tests/test-template-references.sh:83-110`; all templates are load-tested and syntax-checked by `tests/test-templates-comprehensive.sh:90-194`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-157 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt fragment that routes post-round goal-tracker mutations through a request/approval workflow instead of allowing Claude to directly modify the tracker after round 0.
- algorithmic_behavior: Tells Claude to include a `## Goal Tracker Update Request` section when updates are needed, with `Requested Changes` and `Justification` subsections (`prompt-template/claude/goal-tracker-update-request.md:2-14`). It gives examples for marking tasks complete, adding open issues, recording plan evolution, and deferring tasks with AC impact (`prompt-template/claude/goal-tracker-update-request.md:6-10`). It states Codex will review and apply justified changes (`prompt-template/claude/goal-tracker-update-request.md:16`).
- inputs_outputs_state: No placeholders. Input is inclusion context in a next-round prompt. Output is static markdown appended to the next prompt. It does not update `goal-tracker.md`; it creates a structured request surface in Claude’s summary.
- gates_or_invariants: Invariant is goal tracker write ownership: Claude cannot directly modify goal tracker after round 0. This fragment complements `prompt-template/claude/next-round-prompt.md:27-36`, which states goal tracker is read-only after round 0, and `prompt-template/block/goal-tracker-modification.md:1-25`, which blocks direct edits and tells Claude to request changes instead.
- dependencies_and_callers: Appended by `hooks/loop-codex-stop-hook.sh:1830-1835` when generating the next prompt; if missing, the hook uses an inline fallback. Related Codex responsibility is defined in `prompt-template/codex/goal-tracker-update-section.md:1-17`, where Codex evaluates and applies justified requests.
- edge_cases_or_failure_modes: Because this file is static and appended as guidance, the gate depends on separate write/edit validators to enforce it. Ambiguous or unjustified requests can be rejected by Codex; the template requires justification but cannot ensure quality. It includes deferral as an allowed request shape, but Codex review templates still treat unjustified deferrals and incomplete work as blockers.
- validation_or_tests: Existence and loading are covered by template reference/comprehensive tests. Its integration path is explicitly present in `hooks/loop-codex-stop-hook.sh:1830-1835`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-187 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR/PR-loop state parsing and active-loop detection under concurrent reads, atomic writes, malformed state, stale directories, and edge-case frontmatter.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and creates temporary state directories/files, then runs 22 tests with `pass`/`fail` accounting (`tests/robustness/test-concurrent-state-robustness.sh:15-23`, `tests/robustness/test-concurrent-state-robustness.sh:558-559`). It uses `create_state_file` to generate canonical state frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:30-49`). It checks `get_current_round`, `find_active_loop`, `find_active_pr_loop`, and `parse_state_file_strict` behavior.
- inputs_outputs_state: Inputs are synthetic state files with frontmatter variants: trailing whitespace, leading whitespace, CRLF, empty values, comments, malformed YAML, no frontmatter, Unicode, large/zero rounds, permissions, terminal state files, and timestamped loop directories. Outputs are test pass/fail messages and exit status from `print_test_summary`. It mutates only the temporary test directory.
- gates_or_invariants: `get_current_round` should parse normal numeric values and degrade to 0/empty for unsupported forms rather than crashing (`tests/robustness/test-concurrent-state-robustness.sh:59-160`). Active-loop detection intentionally checks only the newest timestamped directory to prevent stale older state revival (`tests/robustness/test-concurrent-state-robustness.sh:186-201`, `tests/robustness/test-concurrent-state-robustness.sh:452-466`), matching `hooks/lib/loop-common.sh:257-299`. `find_active_loop` recognizes `finalize-state.md` as active but not `cancel-state.md` (`tests/robustness/test-concurrent-state-robustness.sh:203-219`, `tests/robustness/test-concurrent-state-robustness.sh:468-484`). Concurrent reads must all retrieve the real nonzero round value (`tests/robustness/test-concurrent-state-robustness.sh:250-277`). Atomic temp-write plus `mv` should produce only numeric reads during active writes (`tests/robustness/test-concurrent-state-robustness.sh:279-322`). Strict parser rejects malformed YAML and missing frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:332-371`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, standard shell utilities, and `awk` for CRLF generation. It is included in the aggregate test runner at `tests/run-all-tests.sh:114`. The functions under test are defined in `hooks/lib/loop-common.sh`: `find_active_loop` at `hooks/lib/loop-common.sh:275-333`, `get_current_round` at `hooks/lib/loop-common.sh:339-349`, and related state-file resolution at `hooks/lib/loop-common.sh:213-255`.
- edge_cases_or_failure_modes: Some expectations are intentionally tolerant, accepting either strict parse or graceful default for leading whitespace/CRLF (`tests/robustness/test-concurrent-state-robustness.sh:91-98`, `tests/robustness/test-concurrent-state-robustness.sh:113-120`). Permission test is skipped when running as root (`tests/robustness/test-concurrent-state-robustness.sh:529-552`). The concurrent write test depends on atomic `mv`; non-atomic writers are not accepted as safe by this spec.
- validation_or_tests: This file is the validation artifact for concurrent state robustness. It ends with `print_test_summary` and exits with the accumulated test result (`tests/robustness/test-concurrent-state-robustness.sh:558-559`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique assigned item sections above, each with cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`