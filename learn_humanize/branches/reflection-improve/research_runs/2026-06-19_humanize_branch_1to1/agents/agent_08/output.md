# agent_08 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-008 `directory` `scripts`
- cursor: `[_]`
- core_role: The [scripts](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts) directory is the command/runtime layer for the Humanize loop algorithms. It initializes RLCR and PR loop sessions, validates prerequisites, fetches and polls PR review signals, exposes monitor commands, routes model/provider choices, manages BitLesson selection and delta enforcement, and supplies cancellation/wrapper utilities used by hooks and slash-command workflows.
- algorithmic_behavior: Recursive inspection found 24 shell files under `scripts`, including 4 shared libraries/wrappers and the main loop command scripts. `setup-rlcr-loop.sh` creates `.humanize/rlcr/<timestamp>` sessions after dependency, mutual exclusion, plan, git, base-branch, and content gates; it writes `state.md`, `goal-tracker.md`, initial prompts, BitLesson state, and summary templates. Key sections include dependency checks at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/setup-rlcr-loop.sh:288), mutual exclusion at line 327, plan path validation at line 417, content validation at line 583, clean-worktree gating at line 687, base-branch capture at line 711, state creation at line 837, goal tracker creation at line 891, and prompt/task routing at line 1049. `setup-pr-loop.sh` creates `.humanize/pr-loop/<timestamp>` sessions, builds `active_bots`/`configured_bots`, fetches initial comments, detects startup cases, handles re-review triggers, verifies Claude eyes reactions, writes PR state and tracker files, and emits the round-0 prompt. Core PR setup appears at [scripts/setup-pr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/setup-pr-loop.sh:177), mutual exclusion at line 217, startup case detection at line 433, case 4/5 trigger logic at line 457, state creation at line 604, goal tracker creation at line 638, and prompt emission at line 717.
- inputs_outputs_state: Primary inputs are CLI/slash-command arguments, `CLAUDE_PROJECT_DIR`, repo git state, plan files, PR numbers, bot flags, GitHub API output from `gh`, Codex model/effort settings, and JSON config files. Outputs are loop state directories, frontmatter `state.md` files, prompt files, `goal-tracker.md`, review/comment snapshots, cancellation markers, monitor output, skill invocation logs, and JSON/markdown status data. `humanize.sh` is the public shell entry: it sources monitor and hook libraries at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/humanize.sh:6), parses RLCR goal trackers at line 36, detects git states at line 124, parses porcelain git status at line 172, dispatches `humanize monitor rlcr|pr|skill` at line 1105, and implements PR monitor parsing/display at line 1157. `monitor-common.sh` centralizes session lookup, state-file priority, PR phase classification, and tracker parsing; its outline includes state-file detection at [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/lib/monitor-common.sh:153), PR phase detection at line 259, and tracker parsing at line 388.
- gates_or_invariants: Only one RLCR or PR loop may be active at a time, enforced by both setup scripts. RLCR requires `codex`, `jq`, and `git`; PR setup requires a git repo with a commit, authenticated `gh`, and `codex`. RLCR plan files must be relative, readable, inside the project, not symlink-traversed, not in submodules, and either tracked-clean when `--track-plan-file` is used or gitignored otherwise. RLCR start requires a clean worktree excluding ignored `.humanize` runtime artifacts. PR setup requires at least one bot flag and YAML-safe branch/model/effort values, and for case 4/5 it must obtain or post a trigger comment; Claude monitoring additionally requires an eyes reaction before the loop is accepted. Shared timeout behavior goes through `portable-timeout.sh`, whose `run_with_timeout` wrapper starts at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/portable-timeout.sh:33).
- dependencies_and_callers: The scripts depend on `hooks/lib/loop-common.sh`, `jq`, `git`, `gh`, `codex`, optional `claude`, `timeout`/`gtimeout` or Python fallback, and prompt/templates under `prompt-template` and `templates`. PR scripts coordinate with `check-pr-reviewer-status.sh`, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, and `check-bot-reactions.sh`. `ask-codex.sh` stores skill calls under `.humanize/skill` and cache paths, while `monitor-skill.sh` reads those metadata files. `config-loader.sh` merges default/user/project JSON config and `model-router.sh` maps model names to Codex or Claude providers. Cancel scripts write `.cancel-requested` and `cancel-state.md` signals read by hook logic.
- edge_cases_or_failure_modes: The directory explicitly handles missing dependencies, non-git or empty repos, git timeouts, unclean working trees, remote-only base branches, YAML-unsafe values, too-short or comment-only plans, missing PR trigger comments, bot startup cases with commits after review, fork PR repository resolution, paginated GitHub comments/reactions, bot timeouts, Codex absence/failure/empty output, non-GNU timeout availability, shallow/detached/rebase/merge git states, and monitor sessions with terminal cleanup. PR state is designed so `configured_bots` never shrinks while `active_bots` shrinks and can be re-expanded when previously approved bots report new issues.
- validation_or_tests: `tests/test-pr-loop-system.sh` exercises PR script behavior end-to-end, including setup, status checks, monitor states, tracker updates, stop-hook integration, force-push and trigger handling. `tests/robustness/test-git-operations-robustness.sh` tests `humanize_detect_git_state` and `humanize_parse_git_status`. `tests/setup-monitor-test-env.sh` creates monitor fixtures for the YAML-list bot state used by `humanize monitor pr`. No commands were run as validation in this worker pass; evidence is from read-only inspection.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-038 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/hooks.json) is the declarative dispatch table that attaches Claude hook phases to Humanize RLCR/PR loop validators and stop hooks. It is not itself a validator implementation, but it is core wiring for when each validator/state-machine transition is invoked.
- algorithmic_behavior: The file registers `UserPromptSubmit` to `loop-plan-file-validator.sh`, `PreToolUse` validators for `Write`, `Edit`, `Read`, and `Bash`, a `PostToolUse` Bash hook, and two `Stop` hooks. Lines [4-13](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/hooks.json:4) route user prompts through plan validation. Lines [14-51](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/hooks.json:14) gate tool use before writes, edits, reads, and bash commands execute. Lines [52-62](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/hooks.json:52) add post-Bash session bookkeeping. Lines [63-76](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/hooks.json:63) run RLCR and PR stop hooks with 7200-second timeouts.
- inputs_outputs_state: Input is Claude’s hook event stream and the `CLAUDE_PLUGIN_ROOT` environment variable used to resolve script paths. Output is hook process execution; the called scripts then emit hook JSON decisions, update loop state, or block tool/stop actions. The JSON file itself has no dynamic state, but its mapping determines whether state files, goal trackers, prompts, summaries, PR reviews, and cancellation/finalization transitions are protected.
- gates_or_invariants: Each protected tool class has a single validator command. The stop phase always includes both RLCR and PR stop hooks, allowing each hook to no-op unless its loop type is active. Long stop timeout is an invariant for Codex review/polling paths that can legitimately take many minutes.
- dependencies_and_callers: Depends on executable hook scripts under `hooks/`, especially `loop-bash-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, `loop-post-bash-hook.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh`. The validators in turn share `hooks/lib/loop-common.sh`. Related inspected behavior: the Bash validator blocks direct state/goal/prompt/summary/todo mutation and unsafe direct stop-hook execution, while the PR stop hook polls PR reviews and updates `active_bots`/goal tracker state.
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is unset or a hook script is missing/non-executable, the configured command cannot enforce the intended gate. Because this file dispatches by matcher names, a mismatch between Claude tool names and these matcher strings would bypass the intended validator. Since both stop hooks are registered together, each stop hook must correctly detect whether its own loop is active to avoid false blocking.
- validation_or_tests: The PR loop tests indirectly depend on this routing by testing stop-hook and validator-facing behavior, but this JSON file is mostly covered by inspection and hook integration rather than direct unit assertions.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-068 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: [tests/setup-monitor-test-env.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/setup-monitor-test-env.sh) is an executable fixture generator for PR monitor behavior. It constructs `.humanize/pr-loop/<timestamp>/state.md` test sessions that encode YAML-list `configured_bots` and `active_bots` states.
- algorithmic_behavior: The script takes `<test_dir> <test_name>` as arguments and exits early if `test_dir` is missing at lines [12-18](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/setup-monitor-test-env.sh:12). It supports three cases: `yaml_list` creates round 1 with both `claude` and `codex` configured and active at lines [20-42](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/setup-monitor-test-env.sh:20); `configured` creates round 2 with both configured but only `codex` active at lines [43-63](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/setup-monitor-test-env.sh:43); `empty` creates round 3 with configured bots and an empty `active_bots:` key at lines [64-83](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/setup-monitor-test-env.sh:64). Unknown cases fail at lines [84-88](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/setup-monitor-test-env.sh:84).
- inputs_outputs_state: Inputs are a filesystem root for the fixture and a symbolic test case name. Output is a test directory path on stdout plus a PR loop session containing `state.md`. The generated frontmatter includes `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- gates_or_invariants: The fixture state follows the YAML-list format expected by PR monitor parsing. It preserves the invariant that `configured_bots` is the complete bot set and `active_bots` is the mutable subset that can be empty after approval. `set -euo pipefail` makes fixture generation fail closed on unexpected errors.
- dependencies_and_callers: Depends only on POSIX-ish shell utilities and mkdir/heredoc behavior. It is intended for monitor tests that exercise `scripts/humanize.sh` and `scripts/lib/monitor-common.sh` parsing of PR loop state files.
- edge_cases_or_failure_modes: Missing `TEST_DIR` exits with usage. Unknown `TEST_NAME` exits with a list of valid cases. The fixture does not create comment/check/feedback files, so tests using it must target state parsing/display rather than live tailing behavior.
- validation_or_tests: It is itself a test helper rather than a validator. It concretely specifies monitor expectations for active/configured bot parsing, partial approvals, and all-approved empty active list display.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-098 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: [tests/test-pr-loop-system.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh) is the broad executable specification for the PR loop state machine, PR GitHub integration helpers, monitor phase logic, goal tracker updates, and stop-hook transitions.
- algorithmic_behavior: The harness defines logging, setup, cleanup, and per-test isolation at lines [35-104](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:35). It tests mutual exclusion between RLCR and PR loops at lines [107-146](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:107), PR reviewer startup cases and PR review submissions at lines [149-193](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:149), [233-250](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:233), and [490-507](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:490), reaction detection at lines [196-229](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:196), phase detection at lines [253-309](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:253), goal tracker parsing and updating at lines [312-417](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:312), [510-538](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:510), [678-764](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:678), and [1177-1309](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:1177). It also covers setup case 4/5 trigger logic at lines [1312-1447](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:1312), goal tracker creation at lines [1450-1539](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:1450), stop-hook tracker updates at lines [1541-1755](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:1541), and runner selection at lines [1758-1904](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:1758).
- inputs_outputs_state: Inputs are temporary repositories, mocked `gh`, mocked `codex`, shell environment such as `CLAUDE_PROJECT_DIR`, PR numbers, bot choices, fake state files, fake review/comment payloads, and generated goal trackers. Outputs are assertions over state files, hook JSON decisions, monitor text, tracker statistics, active bot lists, generated comments/check files, and script exit codes. The test intentionally creates `.humanize/pr-loop` and `.humanize/rlcr` directories to model active/approved/cancelled/max-iteration loop state.
- gates_or_invariants: The suite asserts that PR and RLCR loops are mutually exclusive; startup cases 1/2/3 do not require a trigger at round 0, while case 4/5 or later rounds do require trigger handling; setup case 4/5 must fail and clean up if `trigger_comment_id` cannot be retrieved for Claude eyes verification; `configured_bots` remains the full set while `active_bots` changes; monitor phases map to approved, waiting, cancelled, max-iteration, and Codex-analyzing states; `update_pr_goal_tracker` must be idempotent, repair partial updates, and insert rows inside the Issue Summary table.
- dependencies_and_callers: The test sources or invokes `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, `hooks/lib/loop-common.sh`, and `hooks/pr-loop-stop-hook.sh`. It uses a mock `gh` CLI and mock `codex` output to drive deterministic PR loop transitions.
- edge_cases_or_failure_modes: Covered failure modes include missing trigger comment IDs, bot comments after new commits, partial and full reviewer response states, PR review submissions as distinct from comments, reactions filtered by timestamp, force-push ancestry changes, unpushed commits, empty and malformed tracker rows, partial tracker writes, Codex analysis in-progress detection by file growth, and stop-hook behavior when reviews report unresolved issues.
- validation_or_tests: This file is the validation surface. The main runner at line [1761](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/test-pr-loop-system.sh:1761) supports selective execution groups such as `all`, monitor, setup, and stop-hook related tests. I inspected the test specification read-only; I did not execute it.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-128 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: [prompt-template/block/goal-tracker-bash-write.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/block/goal-tracker-bash-write.md) is a block-message template used by validation hooks to enforce that `goal-tracker.md` changes go through Write/Edit tooling rather than Bash.
- algorithmic_behavior: The template tells the agent not to use Bash commands to modify `goal-tracker.md` at lines [1-3](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/block/goal-tracker-bash-write.md:1), points to the expected path placeholder `{{CORRECT_PATH}}` at line [5](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/block/goal-tracker-bash-write.md:5), and explains that `cat`, `echo`, `sed`, `awk`, and similar Bash commands bypass validation hooks at lines [7-8](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/block/goal-tracker-bash-write.md:7). The corresponding renderer is `goal_tracker_bash_blocked_message` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/lib/loop-common.sh:667) per direct reference search.
- inputs_outputs_state: Inputs are template variables supplied by the hook renderer, especially `CORRECT_PATH`. Output is the hook block reason shown to the agent when Bash attempts to mutate the tracker. It does not update state itself; it shapes the recovery action by directing the agent back through Write/Edit validators.
- gates_or_invariants: The invariant is that goal tracker mutation must remain observable by Write/Edit validators, which can enforce round-specific tracker rules. This matters because Bash redirection and stream editors can bypass per-file mutation policies unless explicitly blocked.
- dependencies_and_callers: Depends on `load_and_render_safe` and hook logic in `hooks/lib/loop-common.sh`, and is consumed by Bash validation paths such as `hooks/loop-bash-validator.sh` when the command targets `goal-tracker.md`.
- edge_cases_or_failure_modes: If `CORRECT_PATH` is absent, the message would still block but provide a less useful recovery path. If a non-Bash tool writes the tracker incorrectly, this template does not apply; that path is handled by Write/Edit validators instead. If a Bash command mutates a differently named tracker copy, detection depends on the caller’s path matching.
- validation_or_tests: PR goal tracker update tests in `tests/test-pr-loop-system.sh` validate the tracker semantics that this template protects, while Bash-specific block behavior is exercised through hook validator coverage rather than this template alone.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-158 `file` `prompt-template/claude/methodology-analysis-prompt.md`
- cursor: `[_]`
- core_role: [prompt-template/claude/methodology-analysis-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md) defines the post-RLCR methodology-analysis transition contract. It is core algorithmic prompt material because it governs what happens after an RLCR loop reaches its exit point before fully completing.
- algorithmic_behavior: The prompt declares the exit context and round counts at lines [1-8](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:1). It instructs Claude to spawn an Opus agent that reads `round-*-summary.md` and `round-*-review-result.md` and writes `methodology-analysis-report.md` at lines [12-21](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:12). It imposes strict sanitization rules at lines [22-30](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:22), focus areas at lines [32-40](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:32), and a report-only source rule for user-facing content at lines [43-45](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:43). It then branches on whether improvements were found and optionally asks the user to file a sanitized GitHub issue at lines [47-69](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:47). The done marker is mandatory at lines [71-73](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/prompt-template/claude/methodology-analysis-prompt.md:71).
- inputs_outputs_state: Inputs are `{{EXIT_REASON}}`, `{{EXIT_REASON_DESCRIPTION}}`, `{{CURRENT_ROUND}}`, `{{MAX_ITERATIONS}}`, `{{LOOP_DIR}}`, plus the loop’s round summaries and review-result files. Outputs are `methodology-analysis-report.md`, optional user-facing sanitized suggestions, optional GitHub issue creation via `gh issue create`, and the required `methodology-analysis-done.md` marker.
- gates_or_invariants: The report must not expose project-identifying details such as paths, symbols, branch names, hashes, URLs, raw errors, or code snippets. User-facing content must derive only from the sanitized report, not raw development records. The phase cannot be considered complete until `methodology-analysis-done.md` contains meaningful content.
- dependencies_and_callers: The prompt is used by methodology-analysis hook support under `hooks/lib/methodology-analysis.sh` and state detection in `hooks/lib/loop-common.sh`, where `methodology-analysis-state.md` is treated as a managed state file. It depends on the Claude Agent tool, optional `AskUserQuestion`, and optional `gh` for issue creation.
- edge_cases_or_failure_modes: If the Opus agent fails to write a report, subsequent instructions cannot safely proceed. If no improvements are found, the prompt requires a short completion path rather than issue creation. If improvements are found but the user declines, it still must write the done marker. If `gh` is unavailable after user confirmation, the prompt falls back to providing issue title/body for manual creation.
- validation_or_tests: This is a prompt contract rather than a shell test. Related validation is in hook-level methodology state protections and stop/finalization behavior, not in a direct test file assigned here.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-188 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/robustness/test-git-operations-robustness.sh) is an executable robustness specification for git-state parsing used by Humanize monitors and loop status reporting.
- algorithmic_behavior: The test sources production `scripts/humanize.sh` and targets `humanize_parse_git_status` and `humanize_detect_git_state`. Helper parsing and temp repo setup are defined at lines [36-70](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/robustness/test-git-operations-robustness.sh:36). Tests 1-6 cover clean repos, branch name detection, untracked files, modified files, staged additions, and insertion counts at lines [71-158](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/robustness/test-git-operations-robustness.sh:71). Tests 7-12 cover non-git directories, detached HEAD, mixed states, deleted files, empty repos, and feature branches at lines [168-260](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/robustness/test-git-operations-robustness.sh:168). Tests 13-18 cover renames, many untracked files, filenames with spaces, binary files, staged and unstaged changes to the same file, and deletion counts at lines [265-367](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/robustness/test-git-operations-robustness.sh:265). Tests 19-25 cover normal, detached, rebase, merge, shallow, non-git, and simulated permission-error repository states at lines [370-500](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/tests/robustness/test-git-operations-robustness.sh:370).
- inputs_outputs_state: Inputs are throwaway git repositories with controlled worktree/index states and simulated `.git` metadata files such as `rebase-merge`, `MERGE_HEAD`, and `shallow`. Outputs are assertions over pipe-delimited summaries from `humanize_parse_git_status` and state names from `humanize_detect_git_state`. The production functions return status counts for modified/added/deleted/untracked/insertions/deletions and state names such as `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo`.
- gates_or_invariants: The tested invariants are that porcelain git status is parsed correctly across staged, unstaged, renamed, deleted, binary, and spaced-path files; that non-git directories are gracefully reported; and that special repository states are detected before normal-state fallback. Production implementation references are [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/humanize.sh:124) for git state detection and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/humanize.sh:172) for status parsing.
- dependencies_and_callers: Depends on `git`, shell temp directories, and `scripts/humanize.sh`; through `humanize.sh`, it also sources `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh` when present. Callers are developers or CI invoking robustness tests to protect monitor/loop status behavior.
- edge_cases_or_failure_modes: The tests intentionally model detached HEAD, no commits, mixed staged/unstaged changes, file deletion, rename counting as modification, many untracked files, filenames containing spaces, binary diffs, simultaneous staged and unstaged edits, shallow clone markers, simulated rebase/merge metadata, non-git directories, and inaccessible `.git` directories. These are high-value edge cases because loop startup and monitor status can otherwise mislead users or fail noisily.
- validation_or_tests: This file is the validation. I inspected the full 507-line test file read-only; I did not run it in this branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item evidence sections, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`