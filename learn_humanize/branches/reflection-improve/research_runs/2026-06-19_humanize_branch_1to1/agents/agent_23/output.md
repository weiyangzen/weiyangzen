# agent_23 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`
- validation_scope: read-only inspection only; this branch export has no usable `.git` metadata, and write-style validation was not run.

## Item Evidence

### REFLECTION_IMPROVE-HZ-023 `directory` `skills/humanize-rlcr`
- cursor: `[_]`
- core_role: Skill-mode entrypoint for RLCR. It defines how an agent should run the Ralph Loop with Codex Review when native hooks are unavailable, while preserving hook-equivalent state-machine enforcement.
- algorithmic_behavior: The directory contains one file, `skills/humanize-rlcr/SKILL.md`. The skill explicitly forbids reimplementing review logic and routes all phase transitions through `scripts/rlcr-stop-gate.sh` (`skills/humanize-rlcr/SKILL.md:10-17`). It defines a round loop: read generated prompt, implement changes, commit, write the expected summary file, then call the stop gate wrapper (`skills/humanize-rlcr/SKILL.md:41-59`). Gate exits are interpreted as allow, RLCR block, or infrastructure error (`skills/humanize-rlcr/SKILL.md:61-65`).
- inputs_outputs_state: Inputs are setup arguments passed to `setup-rlcr-loop.sh`, including plan path, max iterations, Codex model/timeouts, base branch, review cadence, push policy, and skip/finalize flags (`skills/humanize-rlcr/SKILL.md:92-113`). The installer hydrates `{{HUMANIZE_RUNTIME_ROOT}}` into an absolute runtime root (`skills/humanize-rlcr/SKILL.md:19-27`), via `scripts/install-skill.sh:144`. Outputs are generated loop prompts, summaries, commits, and terminal gate decisions. State lives in generated `.humanize/rlcr/<timestamp>/...` files, especially `state.md`, `finalize-state.md`, prompt files, summary files, and review outputs (`skills/humanize-rlcr/SKILL.md:45-50`, `skills/humanize-rlcr/SKILL.md:87-90`).
- gates_or_invariants: The skill lists enforced invariants: state/schema validation, branch consistency, plan integrity, incomplete Task/Todo blocking, git-clean exit, optional push-every-round blocking, summary presence, max-iteration handling, full-review cadence, strict completion markers, review-phase marker, code-review issue markers, Codex failure blocking, and open-question handling (`skills/humanize-rlcr/SKILL.md:66-84`). Critical rules forbid manual state edits, skipping blocked gate results, ad-hoc Codex review substitution, or treating non-generated files as source of truth (`skills/humanize-rlcr/SKILL.md:85-90`).
- dependencies_and_callers: Coordinates with `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/cancel-rlcr-loop.sh`, and the hook implementation `hooks/loop-codex-stop-hook.sh` (`skills/humanize-rlcr/SKILL.md:33-37`, `skills/humanize-rlcr/SKILL.md:53-58`, `skills/humanize-rlcr/SKILL.md:128-132`). It is installed alongside other skills by `scripts/install-skill.sh:36` and documented in `docs/install-for-codex.md` and `docs/install-for-kimi.md`.
- edge_cases_or_failure_modes: Setup failure is terminal for the skill flow (`skills/humanize-rlcr/SKILL.md:39`). Gate exit `10` means the agent must continue exactly from returned instructions, while exit `20` is infrastructure failure and must not be faked as completion (`skills/humanize-rlcr/SKILL.md:61-65`). Skill hydration failure would leave `{{HUMANIZE_RUNTIME_ROOT}}` unresolved and break all command examples.
- validation_or_tests: Direct wrapper behavior is covered by `tests/test-stop-gate.sh` references to `scripts/rlcr-stop-gate.sh`. Bash validator blocks direct execution of `rlcr-stop-gate.sh` and related hook scripts in active loops (`hooks/loop-bash-validator.sh:172`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-053 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime verifier for PR loop bot reaction gates. It converts GitHub reaction state into deterministic shell exit status and JSON payloads for Codex approval and Claude receipt acknowledgement.
- algorithmic_behavior: Exposes two commands: `codex-thumbsup <pr_number> [--after <timestamp>]` and `claude-eyes <comment_id> [--retry N] [--delay S]` (`scripts/check-bot-reactions.sh:9-16`, `scripts/check-bot-reactions.sh:39-71`). `codex-thumbsup` resolves the PR base repo, fetches issue reactions with pagination, filters for user `chatgpt-codex-connector[bot]` and content `+1`, optionally rejects old reactions using `--after`, then prints the reaction JSON (`scripts/check-bot-reactions.sh:123-187`). `claude-eyes` resolves repo context, retries, fetches comment reactions with pagination, filters for user `claude[bot]` and content `eyes`, and exits only after success or retry exhaustion (`scripts/check-bot-reactions.sh:190-300`).
- inputs_outputs_state: Inputs are CLI arguments, current GitHub repo context from `gh repo view`, optional PR number for fork-safe lookup, GitHub API responses, and timeout configuration from `portable-timeout.sh` (`scripts/check-bot-reactions.sh:24-33`). Outputs are JSON reaction objects on stdout for success, diagnostic text on stderr for errors/retries, and exit codes: `0` found, `1` not found or timeout, `2` argument/API errors (`scripts/check-bot-reactions.sh:13-16`, `scripts/check-bot-reactions.sh:57-60`). It does not write state itself; callers update PR loop state.
- gates_or_invariants: Uses the base repository for reaction API calls so fork PRs resolve correctly (`scripts/check-bot-reactions.sh:119-121`, `scripts/check-bot-reactions.sh:232-234`). Pagination is mandatory and merged with `jq -s 'add // []'` to avoid missing reactions after the first page (`scripts/check-bot-reactions.sh:152-159`, `scripts/check-bot-reactions.sh:269-275`). Codex timestamps are compared lexicographically as ISO-8601 UTC strings (`scripts/check-bot-reactions.sh:177-183`).
- dependencies_and_callers: Depends on `gh`, `jq`, `sleep`, and `scripts/portable-timeout.sh`. `portable-timeout.sh` selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:10-29`) and normalizes timeout exits to `124` for Python (`scripts/portable-timeout.sh:47-61`). Called by `scripts/setup-pr-loop.sh:587` to confirm Claude eyes after trigger posting, and by `hooks/pr-loop-stop-hook.sh:676`, `hooks/pr-loop-stop-hook.sh:777`, and `hooks/pr-loop-stop-hook.sh:999` for Codex approval and Claude response gates.
- edge_cases_or_failure_modes: Missing or duplicate positional arguments exit `2` (`scripts/check-bot-reactions.sh:114-117`, `scripts/check-bot-reactions.sh:216-229`). API failure fetching PR reactions exits `2` for Codex (`scripts/check-bot-reactions.sh:157-162`), but Claude comment reaction API failure is retried by continuing to the next attempt (`scripts/check-bot-reactions.sh:273-278`). If repo detection fails, `PR_BASE_REPO` can fall back to empty/current state (`scripts/check-bot-reactions.sh:147-150`, `scripts/check-bot-reactions.sh:259-262`), which can turn later API calls into hard errors or silent retry misses. Claude always sleeps before each check, including attempt 1 (`scripts/check-bot-reactions.sh:264-268`).
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies Codex `+1` detection, `--after` filtering, and Claude eyes detection with `--retry 1 --delay 0` (`tests/test-pr-loop-system.sh:199-229`). PR stop hook consumes successful Codex results by removing `codex` from `active_bots` and may rename state to `approve-state.md` when no bots remain (`hooks/pr-loop-stop-hook.sh:678-716`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-083 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for `.humanize` escape protection and zsh-safe filesystem scanning. It validates that local loop state cannot be accidentally or forcibly staged, and that monitor loops avoid glob failures in empty/dotfile-only directories.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` (`tests/test-humanize-escape.sh:16-19`), defines pass/fail counters (`tests/test-humanize-escape.sh:25-38`), then uses `assert_blocks` and `assert_allows` wrappers around `git_adds_humanize` after lowercasing commands with `to_lower` (`tests/test-humanize-escape.sh:44-70`). It tests direct `.humanize` path variants, quoted paths, force/all flags, chained commands, `git -C`/`--git-dir`, allowed non-loop paths, patch mode, and similarly named non-loop files (`tests/test-humanize-escape.sh:83-192`). It then tests find-based iteration for empty directories, dotfiles-only directories, missing and present `*-state.md` files, and timestamped session directory selection (`tests/test-humanize-escape.sh:204-332`).
- inputs_outputs_state: Inputs are synthetic shell command strings and temporary directories under `/tmp`. Outputs are console PASS/FAIL lines and final process exit `0` when all assertions pass or `1` when any fail (`tests/test-humanize-escape.sh:337-352`). It temporarily creates `.humanize` under `/tmp/test-humanize-git-add-$$` to exercise `git add -A` behavior when local state exists (`tests/test-humanize-escape.sh:128-144`).
- gates_or_invariants: The tested invariant is that `git_adds_humanize` must block direct `.humanize` references, force broad adds, all adds when `.humanize` exists, and broad non-force adds only when `.humanize` is not ignored (`hooks/lib/loop-common.sh:1101-1219`). It must not overblock `.humanizeconfig`, `.humanize-backup`, `.humanizerc`, ordinary source paths, or patch-mode adds (`tests/test-humanize-escape.sh:175-192`). Filesystem iteration must use `find` rather than unmatched globs to avoid zsh “no matches found” behavior, matching monitor helper intent (`scripts/lib/monitor-common.sh:49-60`, `scripts/lib/monitor-common.sh:172-185`).
- dependencies_and_callers: Depends on `to_lower` (`hooks/lib/loop-common.sh:569-572`) and `git_adds_humanize` (`hooks/lib/loop-common.sh:1123-1219`). The production enforcement caller is `hooks/loop-bash-validator.sh`, which blocks commands when `git_adds_humanize "$COMMAND_LOWER"` returns true (`hooks/loop-bash-validator.sh:215-224`). The monitor-related patterns correspond to `scripts/lib/monitor-common.sh` session and state scanning.
- edge_cases_or_failure_modes: `git_adds_humanize` lowercases all input, so uppercase flags like `-A` become `-a` and are intentionally matched as all flags (`hooks/lib/loop-common.sh:1105-1107`, `hooks/lib/loop-common.sh:1176-1183`). It splits on shell operators including pipe, which can simplify shell syntax but lets it catch chained add attempts (`hooks/lib/loop-common.sh:1126-1134`). Non-force broad-scope blocking depends on the current working directory having `.humanize` and `git check-ignore` behavior (`hooks/lib/loop-common.sh:1198-1215`).
- validation_or_tests: This file is itself the focused test. Related coverage appears in `tests/test-bash-validator-patterns.sh` and `tests/test-zsh-monitor-safety.sh`, while production monitor functions implement the find loops being modeled (`scripts/lib/monitor-common.sh:40-63`, `scripts/lib/monitor-common.sh:154-190`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-113 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared prompt-template loading and rendering engine for RLCR hooks, PR loop hooks, validators, and block messages.
- algorithmic_behavior: Resolves template root from a hooks/lib script directory (`hooks/lib/template-loader.sh:24-31`), reads template files by relative name with warning-on-missing behavior (`hooks/lib/template-loader.sh:33-48`), and renders `{{VAR}}` placeholders in a single pass using environment variables prefixed with `TMPL_VAR_` plus an AWK scanner (`hooks/lib/template-loader.sh:50-132`). Provides `load_and_render`, `append_template`, and `load_and_render_safe` wrappers (`hooks/lib/template-loader.sh:134-203`). `validate_template_dir` verifies required `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`).
- inputs_outputs_state: Inputs are template directory path, template relative path, fallback strings, and `VAR=value` assignments. Outputs are rendered prompt/block text on stdout or fallback text when safe rendering cannot load a template. It has no persistent state, but it builds temporary process environment variables for each render (`hooks/lib/template-loader.sh:62-80`).
- gates_or_invariants: Single-pass substitution is the main invariant: values containing `{{OTHER_VAR}}` must be emitted literally and not rescanned (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:115-125`). Missing variables remain as original placeholders (`hooks/lib/template-loader.sh:119-122`). Unclosed `{{` is treated literally (`hooks/lib/template-loader.sh:102-108`). Safe rendering falls back on missing or empty template/render result (`hooks/lib/template-loader.sh:170-203`).
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh:195`, which initializes and validates `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:197-203`). Used broadly by `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, read/write/edit/bash validators, plan-file validator, methodology analysis, bitlesson validation, and setup scripts. The reference scan shows critical callers such as `hooks/loop-codex-stop-hook.sh:423`, `hooks/loop-codex-stop-hook.sh:948`, `hooks/pr-loop-stop-hook.sh:785`, and `scripts/setup-pr-loop.sh:712`.
- edge_cases_or_failure_modes: `load_template` returns empty string for missing files and logs a warning (`hooks/lib/template-loader.sh:41-47`); callers using `load_and_render` directly receive empty output, while `load_and_render_safe` gets fallback behavior. Placeholder names are documented as uppercase/number/underscore only (`hooks/lib/template-loader.sh:7-13`), but the AWK scanner replaces any exact text between braces if a matching `TMPL_VAR_...` exists (`hooks/lib/template-loader.sh:111-118`). Newlines in values are passed through environment variables and can expand into multi-line output.
- validation_or_tests: `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness tests exercise special characters, missing templates, fallback behavior, non-recursive substitution, empty values, long values, and template directory validation. `tests/test-template-references.sh` checks critical templates and safe loader usage.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-143 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for enforcing that RLCR summary files are written through proper file tools rather than shell redirection or text-processing commands that can bypass validators.
- algorithmic_behavior: The template emits a concise blocking reason: “Bash Write Blocked: Use Write or Edit Tool,” tells the agent not to use Bash commands to modify summary files, inserts the correct path with `{{CORRECT_PATH}}`, and explains that shell commands bypass validation hooks (`prompt-template/block/summary-bash-write.md:1-8`).
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by `summary_bash_blocked_message "$correct_summary_path"` in `hooks/lib/loop-common.sh:656-665`. Output is rendered Markdown shown to the agent through validators. No state is changed by the template itself.
- gates_or_invariants: Enforces the invariant that summary write paths must pass through Write/Edit tooling so round-number and path validation hooks can inspect them. The template is intentionally a hard block message, not a warning (`prompt-template/block/summary-bash-write.md:3-8`).
- dependencies_and_callers: Rendered through `load_and_render_safe` in `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:656-665`). The shared template loader performs the `{{CORRECT_PATH}}` substitution (`hooks/lib/template-loader.sh:58-132`). Bash command validators call these shared block-message functions after detecting disallowed command patterns.
- edge_cases_or_failure_modes: If the template is missing or empty, `summary_bash_blocked_message` has an inline fallback with the same `CORRECT_PATH` variable (`hooks/lib/loop-common.sh:660-665`). If `CORRECT_PATH` itself contains braces, the loader’s single-pass rendering prevents second-stage placeholder expansion (`hooks/lib/template-loader.sh:54-57`).
- validation_or_tests: Template reference coverage includes `block/summary-bash-write.md` in `tests/test-template-references.sh:156`. The broader template test suites validate safe substitution and fallback mechanics used by this template.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-173 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial state document template for the PR review loop’s goal tracker. It establishes the tracking schema for monitored bot approval, issue accounting, and startup metadata.
- algorithmic_behavior: Defines PR metadata fields for number, branch, start time, monitored bots, and startup case (`prompt-template/pr-loop/goal-tracker-initial.md:3-9`). Sets the ultimate goal as approval by all monitored bots (`prompt-template/pr-loop/goal-tracker-initial.md:11-14`). Initializes issue summary, total statistics, and issue log to round zero with no issues found/resolved (`prompt-template/pr-loop/goal-tracker-initial.md:15-33`).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, and `STARTUP_CASE`. `scripts/setup-pr-loop.sh` renders it via `load_and_render_safe` into `GOAL_TRACKER_FILE` (`scripts/setup-pr-loop.sh:712-715`). Output is `.humanize/pr-loop/<timestamp>/goal-tracker.md`, which later PR loop rounds and Codex instructions use as the source for issue/approval progress.
- gates_or_invariants: The initial schema requires stable sections: PR Information, Ultimate Goal, Issue Summary, Total Statistics, and Issue Log. Counts initialize to zero and remaining issues initialize to zero (`prompt-template/pr-loop/goal-tracker-initial.md:21-25`). Startup metadata is duplicated in both the header and round-zero log (`prompt-template/pr-loop/goal-tracker-initial.md:5-9`, `prompt-template/pr-loop/goal-tracker-initial.md:29-33`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` through setup-pr-loop’s `load_and_render_safe` call (`scripts/setup-pr-loop.sh:712`). Goal tracker write/modification controls are related to shared block-message functions in `hooks/lib/loop-common.sh`, including round-zero Bash write blocking and later modification blocking (`hooks/lib/loop-common.sh:667-675`, `hooks/lib/loop-common.sh:1305-1317`).
- edge_cases_or_failure_modes: If the template is missing or fails to render, setup uses `FALLBACK_GOAL_TRACKER` defined just before the render call (`scripts/setup-pr-loop.sh:680-712`). Placeholder values are single-pass rendered, so bot display names or startup text containing template syntax are not recursively expanded. The minimal template here omits richer fallback sections such as acceptance criteria, so consumers must tolerate either schema variant if fallback is used.
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies the required goal tracker sections and statistics fields directly against this template (`tests/test-pr-loop-system.sh:642-655`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-203 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the portable timeout abstraction used by GitHub API calls, Git calls, Codex execution/review, setup scripts, and PR/RLCR hooks.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-17`). It validates timeout implementation detection, implementation enum validity, successful fast command execution, timeout exit code `124`, argument preservation, command exit-code preservation, pipeline handling through `sh -c`, short timeout elapsed time, zero timeout tolerance, large output, rapid cycles, missing command behavior, empty command behavior, special characters, signal handling, fallback chain detection, subshell handling, and exported `TIMEOUT_IMPL` visibility (`tests/robustness/test-timeout-robustness.sh:31-272`).
- inputs_outputs_state: Inputs are the host’s available timeout tools (`gtimeout`, GNU `timeout`, `python3`, `python`, or none), command invocations, and shell environment. Outputs are pass/fail assertions and `print_test_summary` exit status (`tests/robustness/test-timeout-robustness.sh:274-279`). It does not modify repository state.
- gates_or_invariants: `detect_timeout_impl` must return only `gtimeout`, `timeout`, `python3`, `python`, or `none` (`tests/robustness/test-timeout-robustness.sh:42-49`, `tests/robustness/test-timeout-robustness.sh:235-247`). Timed-out commands should exit `124` when a timeout implementation exists (`tests/robustness/test-timeout-robustness.sh:62-77`). Fast commands must preserve stdout and exit status (`tests/robustness/test-timeout-robustness.sh:51-60`, `tests/robustness/test-timeout-robustness.sh:89-100`).
- dependencies_and_callers: Tests `scripts/portable-timeout.sh`, whose implementation prioritizes `gtimeout`, GNU `timeout`, Python, then no-timeout fallback (`scripts/portable-timeout.sh:6-29`), runs commands through arrays to preserve arguments (`scripts/portable-timeout.sh:33-46`), uses Python `subprocess.run(..., timeout=...)` for fallback (`scripts/portable-timeout.sh:47-61`), and exports `TIMEOUT_IMPL` (`scripts/portable-timeout.sh:73-76`). Production callers include `scripts/check-bot-reactions.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and `hooks/loop-plan-file-validator.sh`.
- edge_cases_or_failure_modes: When no timeout implementation exists, the wrapper warns and runs without timeout (`scripts/portable-timeout.sh:64-68`), so timeout tests skip strict timeout expectations in that case (`tests/robustness/test-timeout-robustness.sh:65-77`, `tests/robustness/test-timeout-robustness.sh:120-137`). Zero timeout behavior is accepted as either immediate success or timeout (`tests/robustness/test-timeout-robustness.sh:139-155`). Commands that trap termination may exit `0` or `124`, both accepted (`tests/robustness/test-timeout-robustness.sh:221-233`). Python fallback does not capture output itself, so subprocess inherits stdout/stderr, matching shell usage.
- validation_or_tests: This file is the direct robustness test. Its target abstraction is used by the assigned reaction checker at every `gh` call (`scripts/check-bot-reactions.sh:124`, `scripts/check-bot-reactions.sh:157`, `scripts/check-bot-reactions.sh:273`) and by RLCR/PR stop hooks for bounded GitHub, Git, and Codex operations.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`