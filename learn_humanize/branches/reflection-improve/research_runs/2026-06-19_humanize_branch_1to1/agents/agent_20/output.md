# agent_20 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-020 `directory` `skills/ask-codex`
- cursor: `[_]`
- core_role: Defines the `/humanize:ask-codex` skill contract for one-shot Codex consultation; directory contains one file, [SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/skills/ask-codex/SKILL.md:1), so its algorithmic scope is command routing and safe argument handling rather than implementation.
- algorithmic_behavior: The skill instructs callers to invoke `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh` with the entire free-form task as one quoted argument when no flags are present, and to keep flags separate while passing the remaining question as one quoted final argument when flags exist (`SKILL.md:14-28`). It explicitly forbids bare `$ARGUMENTS` expansion because shell re-parsing can break on spaces or metacharacters (`SKILL.md:30-36`).
- inputs_outputs_state: Inputs are optional `--codex-model MODEL:EFFORT`, optional `--codex-timeout SECONDS`, and a free-form question (`SKILL.md:4`, `SKILL.md:22-28`). Output contract says Codex response goes to stdout, status to stderr, and responses are saved under `.humanize/skill/<timestamp>/output.md` (`SKILL.md:38-56`).
- gates_or_invariants: Invariant is shell-quoting safety: user text must not be interpolated unquoted into shell arguments (`SKILL.md:14-36`). Exit-code interpretation is part of the skill contract: `0` success, `1` validation, `124` timeout, other Codex process errors (`SKILL.md:44-51`).
- dependencies_and_callers: Delegates all runtime behavior to [scripts/ask-codex.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/ask-codex.sh:1), which validates Codex availability, question non-emptiness, model/effort syntax, creates `.humanize/skill` and cache artifacts, runs `codex exec`, and persists metadata (`scripts/ask-codex.sh:153-186`, `scripts/ask-codex.sh:202-218`, `scripts/ask-codex.sh:295-410`). The skill is referenced by routing prompts and tests, including `tests/test-ask-codex.sh:415-430`, which asserts the unsafe-form warning and quoted invocation guidance exist.
- edge_cases_or_failure_modes: Misquoting can cause shell expansion or command parse failure before `ask-codex.sh` starts (`SKILL.md:30-36`). Runtime failures include missing `codex`, empty question, invalid flags/model/effort, timeout, non-zero Codex exit, and empty stdout (`scripts/ask-codex.sh:153-186`, `scripts/ask-codex.sh:308-380`).
- validation_or_tests: `tests/test-ask-codex.sh` uses a mock `codex` binary, validates argument errors and storage/cache output, and specifically checks the skill guidance for safe quoting (`tests/test-ask-codex.sh:39-74`, `tests/test-ask-codex.sh:382-430`). I did not execute tests because this assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-050 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Runtime stop-hook validator for the `## BitLesson Delta` summary contract; it enforces whether a loop round may exit based on declared lesson actions and concrete BitLesson knowledge-base state.
- algorithmic_behavior: Parses required CLI arguments, requires the summary file, loads template rendering helpers, and emits JSON block decisions through `block_exit` while exiting `0` so the calling hook can return a policy block (`scripts/bitlesson-validate-delta.sh:24-90`). It requires a `## BitLesson Delta` section (`:92-109`), extracts that section until the next `##` heading (`:111-116`), parses exactly one `Action: none|add|update` (`:118-134`), parses `Lesson ID(s)` or `Lesson IDs` (`:136-142`), counts existing concrete `Lesson ID:` entries in the BitLesson file while ignoring placeholders (`:143-156`), then validates action-specific consistency (`:158-283`).
- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` (`:4-13`, `:17-67`). Output is either no stdout and exit `0` for pass (`:285`), or a JSON object with `decision: block`, rendered `reason`, and `systemMessage` (`:78-90`). It reads but does not mutate summary or BitLesson files.
- gates_or_invariants: Missing delta section blocks (`:92-109`). Action must appear exactly once and be one of `none`, `add`, `update` (`:118-134`). `Action: none` requires blank/NONE IDs and, unless `allow-empty-none=true`, at least one concrete existing KB lesson (`:158-183`). `add`/`update` require concrete IDs, an existing BitLesson file, ID regex `^BL-[0-9]{8}-[A-Za-z0-9._-]+$`, and matching `Lesson ID:` entries in the file (`:185-282`).
- dependencies_and_callers: Sources [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/lib/template-loader.sh:170), using `load_and_render_safe` fallbacks when templates are absent (`template-loader.sh:170-203`). Called by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/loop-codex-stop-hook.sh:749) only for non-finalize rounds when `BITLESSON_REQUIRED=true` (`loop-codex-stop-hook.sh:745-764`).
- edge_cases_or_failure_modes: Multiple action lines or invalid action blocks (`:122-134`); empty/placeholder-only KB plus `Action: none` blocks on non-empty-required rounds (`:170-183`); comma-separated ID lists are trimmed and individually checked (`:213-238`); invalid IDs are reported before missing IDs (`:252-282`); missing required args or summary file exit `1` as script failures (`:62-72`), distinct from policy blocks.
- validation_or_tests: The stop hook integrates this validator and treats any stdout JSON as a block (`loop-codex-stop-hook.sh:749-763`). Related setup templates require users to include `## BitLesson Delta` in round summaries (`scripts/setup-rlcr-loop.sh:1040`, `scripts/setup-rlcr-loop.sh:1147`). I inspected behavior but did not execute tests.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-080 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state machine, validator protections, stop-hook completion path, and review/finalize boundary.
- algorithmic_behavior: Creates an isolated git repo and loop directory with `state.md`, `plan.md`, and `goal-tracker.md` (`tests/test-finalize-phase.sh:144-226`), then mocks `codex exec` and `codex review` success/failure/empty-output paths (`:51-142`). It verifies helper predicates for `finalize-state.md` and `finalize-summary.md` (`:238-279`), active-loop detection for finalize vs complete states (`:290-320`), normal `state.md` behavior (`:326-335`), write/edit/bash validators for finalize files (`:341-415`), stop-hook finalize completion checks (`:421-504`), COMPLETE-to-finalize transition (`:510-540`), max-iteration terminal path (`:542-560`), review failure and empty-review blocking (`:562-696`), incomplete todos blocking (`:698-741`), normal non-COMPLETE review loop behavior (`:743-815`), and parser compatibility for finalize state (`:817-865`).
- inputs_outputs_state: Inputs are fabricated hook JSON payloads for Write/Edit/Bash/Stop/UserPromptSubmit validators, mock transcript JSONL files, mock `codex` outputs, and loop state frontmatter (`:346-384`, `:434-436`, `:715-722`, `:775-778`, `:855-858`). Expected state transitions include `state.md -> finalize-state.md` on COMPLETE before max iterations (`:524-540`), `finalize-state.md -> complete-state.md` after finalize checks pass (`:476-493`), `state.md -> maxiter-state.md` at max iterations (`:542-560`), normal feedback loop increment to round 4 (`:791-797`), and preservation of `state.md` with `review_started: true` when review fails or is empty (`:609-619`, `:686-696`).
- gates_or_invariants: Finalize state is active; complete state is terminal and inactive (`:293-320`). `finalize-state.md` is protected from Write/Edit/Bash modification and from `mv`/`cp` source exfiltration (`:357-415`). Finalize completion requires `finalize-summary.md`, clean git state, completed todos, and skips Codex review (`:429-504`, `:721-741`). COMPLETE cannot enter finalize if max iterations are reached (`:542-560`) or if review fails/returns empty (`:562-696`). Normal rounds must still run review and keep `state.md` (`:775-815`).
- dependencies_and_callers: Sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/lib/loop-common.sh:213), exercising `find_active_loop`, `resolve_active_state_file`, `parse_state_file`, `is_finalize_state_file_path`, `is_finalize_summary_path`, and `finalize_state_file_blocked_message` (`loop-common.sh:213-333`, `loop-common.sh:397-496`, `loop-common.sh:647-715`). It invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-plan-file-validator.sh`.
- edge_cases_or_failure_modes: Codex review non-zero exit must block without creating finalize/complete states and must mark `review_started: true` (`tests/test-finalize-phase.sh:566-619`). Empty review stdout must block, leave an empty combined review log, and preserve review state (`:625-696`). Incomplete TodoWrite transcript blocks finalize before Codex invocation (`:715-741`). Parser tests require validators to accept `finalize-state.md` as the sole active state (`:820-865`).
- validation_or_tests: This file is itself the validation surface. Its assertions map directly to implementation paths in `loop-codex-stop-hook.sh`, including finalize detection (`loop-codex-stop-hook.sh:71-85`), BitLesson skip in finalize (`:745-764`), finalize completion rename (`:863-878`), `enter_finalize_phase` rename (`:1156-1168`), and review-started mutation/marker (`:1598-1619`). Not executed in this read-only research run.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-110 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable safety specification for monitor/session discovery under zsh, especially avoiding `nomatch` glob failures in empty or dotfile-only `.humanize` directories.
- algorithmic_behavior: Runs under zsh with `set -euo pipefail`, creates an isolated `/tmp/test-zsh-humanize-$$` workspace, sources `scripts/humanize.sh`, then simulates monitor helper loops using `find` instead of unmatched globs (`tests/test-zsh-monitor-safety.sh:1-77`, `:86-124`, `:136-157`, `:169-205`). It checks cache-log discovery with no files (`:214-235`), full session iteration and latest selection (`:246-273`), verifies zsh is actually running (`:276-286`), and demonstrates why old glob iteration would fail while the new `find` pattern is safe (`:289-321`).
- inputs_outputs_state: Inputs are generated `.humanize/rlcr` directories, dotfiles, timestamped session directories, and cache directories (`:68-70`, `:86-88`, `:133-150`, `:166-191`, `:214-227`, `:246-265`). Outputs are pass/fail counters and exit `0` only when no failures occur (`:323-341`). It mutates only its temporary workspace and deletes it via trap (`:48-60`).
- gates_or_invariants: No sourced or simulated monitor path may emit zsh `no matches found` (`:72-77`, `:118-124`, `:152-157`, `:199-205`). Session discovery must ignore non-directories/dotfiles and only accept timestamp-shaped session directory names (`:105-115`, `:141-150`, `:255-265`). Latest session is selected lexicographically by timestamp-style name (`:267-273`).
- dependencies_and_callers: Depends on [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/scripts/humanize.sh:1) and mirrors monitor-common discovery behavior. Related implementation references include `scripts/humanize.sh` monitor paths that parse `goal-tracker.md` and state details (`scripts/humanize.sh:364-409`, `scripts/humanize.sh:495`), plus `scripts/lib/monitor-common.sh` parsing helpers.
- edge_cases_or_failure_modes: Empty loop directory, dotfile-only loop directory, session directory with no `*-state.md`, empty cache with no `round-*-codex-run.log`, and zsh `nomatch` behavior are explicitly covered (`tests/test-zsh-monitor-safety.sh:86-124`, `:127-157`, `:160-205`, `:208-235`, `:289-321`). The test itself writes to `/tmp`, so in the current managed read-only environment it may be unsuitable to execute without a writable temp allowance.
- validation_or_tests: This file is a standalone zsh test and must be run by zsh (`:1-13`, `:282-286`). I inspected but did not run it.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-140 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-template contract for outdated RLCR state schemas; it tells the user which required field is missing and forces termination/restart rather than continuing with ambiguous state.
- algorithmic_behavior: Renders `{{FIELD_NAME}}` into a concise block message, explains the session was started with an older humanize version, and offers cancel/update/restart options (`prompt-template/block/schema-outdated.md:1-12`). No code executes here; the algorithmic effect comes from hook callers that load it into a JSON block.
- inputs_outputs_state: Input variable is `FIELD_NAME` (`:3`). Output is markdown reason text consumed by hook JSON block responses. It does not read or mutate state files.
- gates_or_invariants: Invariant is fail-closed schema handling: missing required state fields terminate the session as unexpected to preserve state information (`:3-12`). The version guidance is specifically `1.1.2+` (`:9`).
- dependencies_and_callers: Loaded by [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/loop-plan-file-validator.sh:60), which calls `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md"` and emits `decision: block` when `plan_tracked` or `start_branch` is missing (`loop-plan-file-validator.sh:60-89`). Rendering uses `load_and_render_safe` from `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: If the template is absent or renders empty, caller fallback text is used (`template-loader.sh:179-203`). The template’s stated version threshold is older than later stop-hook schema checks for `review_started`/`base_branch` (`hooks/loop-codex-stop-hook.sh:221-240`), so it is scoped to plan-file validation’s v1.1.2 fields, not every schema gate.
- validation_or_tests: Covered indirectly by plan-file validator tests and template reference/comprehensive tests; `hooks/loop-plan-file-validator.sh:79-89` is the direct gate surface. Not executed here.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-170 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop Codex prompt fragment defining how Codex must update `goal-tracker.md` after analyzing bot reviews; it is a review-loop state reconciliation contract.
- algorithmic_behavior: Instructs Codex to update a rendered `{{GOAL_TRACKER_FILE}}`, add one Issue Summary row per reviewer/round, update aggregate totals, and add an Issue Log entry for `{{NEXT_ROUND}}` (`prompt-template/pr-loop/codex-goal-tracker-update.md:1-21`). Provides a concrete markdown example (`:22-55`) and rules preserving file structure and limiting edits to specific sections (`:57-64`).
- inputs_outputs_state: Inputs are template variables `GOAL_TRACKER_FILE` and `NEXT_ROUND` (`:3`, `:9`, `:18`). Output is prompt text embedded into a Codex review prompt; intended state mutation is the PR-loop `goal-tracker.md` Issue Summary, Total Statistics, and Issue Log sections (`:7-20`, `:57-64`).
- gates_or_invariants: Each reviewer gets a separate Issue Summary row (`:64`); the header sections `PR Information` and `Ultimate Goal` must not be modified (`:61-62`); additions should append to existing tables rather than replace them (`:63`); `Remaining = Total Found - Total Resolved` (`:12-15`).
- dependencies_and_callers: Loaded by [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/pr-loop-stop-hook.sh:1253), which sets `GOAL_TRACKER_FILE="$LOOP_DIR/goal-tracker.md"` and `NEXT_ROUND`, falls back to a shorter goal-tracker instruction if missing, and embeds the rendered section into `round-${NEXT_ROUND}-codex-prompt.md` (`pr-loop-stop-hook.sh:1238-1264`).
- edge_cases_or_failure_modes: Template-level risk is arithmetic drift in totals if Codex overwrites instead of incrementing; rules attempt to prevent structural replacement and header churn (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`). If missing, fallback has less detailed section-level constraints (`pr-loop-stop-hook.sh:1259-1262`), reducing enforcement specificity.
- validation_or_tests: Template reference and comprehensive template tests include goal-tracker templates generally; direct caller evidence is `pr-loop-stop-hook.sh:1253-1264`. Not executed here.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-200 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR state parsing, active-loop selection, terminal-state exclusion, and schema validation around state transitions.
- algorithmic_behavior: Sources `loop-common.sh` and test helpers, creates full active states, finalize states, and cancel states (`tests/robustness/test-state-transition-robustness.sh:14-89`). It verifies valid state parsing for rounds 0, 5, and max round (`:95-138`), finalize detection and coexistence with `state.md` (`:140-172`), cancel-state inactivity and newer active loop after cancel (`:174-207`), invalid numeric edge cases (`:209-282`), required schema fields (`:284-361`), newest-directory selection (`:363-383`), lexicographic ordering (`:385-402`), and ignoring nested wrong-location state files (`:404-417`).
- inputs_outputs_state: Inputs are generated `.humanize/rlcr/<timestamp>` directories and state markdown frontmatter with fields such as `current_round`, `max_iterations`, `base_branch`, and `review_started` (`:30-71`, `:217-350`). Outputs are pass/fail results via `print_test_summary` and process exit status (`:419-424`). Temporary files are created through `setup_test_dir`.
- gates_or_invariants: `parse_state_file_strict` must accept valid full states and reject missing `current_round`, `max_iterations`, or `base_branch` (`:103-112`, `:292-361`). `find_active_loop` must treat `finalize-state.md` as active, `cancel-state.md` as inactive, select the newest valid top-level loop, and ignore nested state files (`:148-207`, `:371-417`). Non-numeric `current_round` is rejected by strict parsing (`:259-282`), while negative and over-max numeric values are parseable with enforcement elsewhere (`:230-257`).
- dependencies_and_callers: Exercises [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/reflection-improve/hooks/lib/loop-common.sh:275), especially `find_active_loop` (`loop-common.sh:275-333`), `get_current_round` (`:339-349`), and `parse_state_file_strict` (`:424-496`). Depends on `tests/test-helpers.sh` for temp setup and summary output (`tests/robustness/test-state-transition-robustness.sh:16-19`).
- edge_cases_or_failure_modes: Negative rounds are accepted by the numeric regex (`:230-236`), over-max rounds parse but are enforced elsewhere (`:238-257`), non-numeric rounds fail strict parse (`:259-282`), missing schema fields fail (`:292-361`), terminal cancel states prevent stale active-loop revival (`:182-207`), and nested state files are ignored (`:404-417`).
- validation_or_tests: This is itself the validation surface for state transition robustness. It aligns with `loop-common.sh` comments that active-state resolution prefers methodology/finalize/state files and active-loop discovery checks only the newest directory without a session filter (`loop-common.sh:213-333`). Not executed in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item sections present, each with one required heading and cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`