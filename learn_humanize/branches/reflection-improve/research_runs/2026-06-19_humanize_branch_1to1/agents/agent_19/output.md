# agent_19 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-019 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer for runtime configuration, model/provider routing, and Humanize monitor state rendering. The directory contains four files: `config-loader.sh`, `model-router.sh`, `monitor-common.sh`, and `monitor-skill.sh`.
- algorithmic_behavior: `config-loader.sh` implements a layered JSON merge algorithm. It requires `jq`, normalizes each config layer to a JSON object, treats `config/default_config.json` as required, treats user/project config as optional, strips nulls recursively, then merges default, user, and project layers in precedence order (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:24`, `scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:113`). `model-router.sh` maps model names to providers: `gpt-*` and `o[0-9]*` route to Codex, Claude-family strings route to Claude, and unknown names fail (`scripts/lib/model-router.sh:10`, `scripts/lib/model-router.sh:18`, `scripts/lib/model-router.sh:23`, `scripts/lib/model-router.sh:28`). `model-router.sh` also validates provider binaries and maps unsupported `xhigh` effort to Claude `high` (`scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:49`, `scripts/lib/model-router.sh:62`, `scripts/lib/model-router.sh:84`). `monitor-common.sh` provides monitor state detection, PR-loop phase inference, YAML frontmatter extraction, goal tracker parsing, path truncation, and terminal helpers (`scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:201`, `scripts/lib/monitor-common.sh:271`, `scripts/lib/monitor-common.sh:394`). `monitor-skill.sh` implements the skill invocation monitor, selecting the newest/best `.humanize/skill` invocation, counting statuses, resolving cache/output files, and rendering either `--once` output or an interactive terminal view (`scripts/lib/monitor-skill.sh:15`, `scripts/lib/monitor-skill.sh:44`, `scripts/lib/monitor-skill.sh:63`, `scripts/lib/monitor-skill.sh:84`, `scripts/lib/monitor-skill.sh:130`, `scripts/lib/monitor-skill.sh:268`, `scripts/lib/monitor-skill.sh:348`).
- inputs_outputs_state: `config-loader.sh` inputs are plugin root, project root, optional `XDG_CONFIG_HOME`, and optional `HUMANIZE_CONFIG`; output is merged JSON on stdout plus warnings/errors on stderr (`scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:78`, `scripts/lib/config-loader.sh:84`, `scripts/lib/config-loader.sh:135`). `get_config_value` takes merged JSON and a flat key, outputting a stringified scalar or empty value (`scripts/lib/config-loader.sh:139`, `scripts/lib/config-loader.sh:148`). `model-router.sh` inputs are model names, provider names, and effort values; outputs are provider strings, normalized effort strings, or nonzero errors (`scripts/lib/model-router.sh:10`, `scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:62`). `monitor-common.sh` reads session directories, state files, `goal-tracker.md`, `round-*-pr-check.md`, and temp size caches under `/tmp/humanize-phase-*` (`scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:301`, `scripts/lib/monitor-common.sh:310`, `scripts/lib/monitor-common.sh:394`). `monitor-skill.sh` reads `.humanize/skill`, per-invocation `metadata.md`, `input.md`, `output.md`, local `cache/`, and global `~/.cache/humanize/...` (`scripts/lib/monitor-skill.sh:20`, `scripts/lib/monitor-skill.sh:92`, `scripts/lib/monitor-skill.sh:107`, `scripts/lib/monitor-skill.sh:118`, `scripts/lib/monitor-skill.sh:130`).
- gates_or_invariants: Config layers must be JSON objects; malformed required defaults are fatal, optional malformed layers are ignored with warnings (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`, `scripts/lib/config-loader.sh:53`, `scripts/lib/config-loader.sh:57`). Model routing accepts only known Codex/Claude model patterns and known efforts (`scripts/lib/model-router.sh:18`, `scripts/lib/model-router.sh:75`). Provider dependency checks require the selected binary in `PATH` (`scripts/lib/model-router.sh:49`). Monitor state detection prioritizes active files before terminal state files (`scripts/lib/monitor-common.sh:162`, `scripts/lib/monitor-common.sh:172`). Skill monitor assumes timestamp-named invocation directories and treats absent `metadata.md` as running (`scripts/lib/monitor-skill.sh:42`, `scripts/lib/monitor-skill.sh:90`, `scripts/lib/monitor-skill.sh:135`).
- dependencies_and_callers: `scripts/bitlesson-select.sh` sources both `config-loader.sh` and `model-router.sh` (`scripts/bitlesson-select.sh:10`, `scripts/bitlesson-select.sh:11`). `hooks/lib/loop-common.sh` sources `config-loader.sh` and uses merged config for `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, and `DEFAULT_CODEX_EFFORT` (`hooks/lib/loop-common.sh:168`, `hooks/lib/loop-common.sh:178`, `hooks/lib/loop-common.sh:181`, `hooks/lib/loop-common.sh:187`). `scripts/humanize.sh` sources `monitor-common.sh` and `monitor-skill.sh` and calls monitor helpers for RLCR/PR-loop/skill displays (`scripts/humanize.sh:8`, `scripts/humanize.sh:345`, `scripts/humanize.sh:1122`, `scripts/humanize.sh:1323`, `scripts/humanize.sh:1590`). Tests call these libraries directly: `tests/test-config-merge.sh`, `tests/test-config-error-handling.sh`, `tests/test-model-router.sh`, `tests/test-skill-monitor.sh`, and `tests/test-pr-loop-system.sh`.
- edge_cases_or_failure_modes: Missing `jq` blocks config loading (`scripts/lib/config-loader.sh:17`). Missing required default config exits from the merge subshell (`scripts/lib/config-loader.sh:41`, `scripts/lib/config-loader.sh:46`). Null values are stripped instead of overriding lower layers (`scripts/lib/config-loader.sh:119`). Unknown or empty model names fail provider detection (`scripts/lib/model-router.sh:13`, `scripts/lib/model-router.sh:28`). Missing selected CLI binary fails dependency check unless caller implements fallback (`scripts/lib/model-router.sh:53`). Monitor phase detection can classify a recently modified or growing `round-*-pr-check.md` as Codex analyzing and stores size state in `/tmp` (`scripts/lib/monitor-common.sh:301`, `scripts/lib/monitor-common.sh:319`). Skill monitor truncates long paths/questions for terminal width and may fall back to `input.md` or no output if cache/output files are absent (`scripts/lib/monitor-skill.sh:152`, `scripts/lib/monitor-skill.sh:210`, `scripts/lib/monitor-skill.sh:223`).
- validation_or_tests: Config merge behavior is covered by `tests/test-config-merge.sh`, including defaults, project override, user/project precedence, additive merge, null stripping, and `HUMANIZE_CONFIG` (`tests/test-config-merge.sh:35`, `tests/test-config-merge.sh:67`, `tests/test-config-merge.sh:92`, `tests/test-config-merge.sh:112`, `tests/test-config-merge.sh:135`, `tests/test-config-merge.sh:153`). Config error handling is covered by `tests/test-config-error-handling.sh`, including missing default fatality and malformed optional layer warnings (`tests/test-config-error-handling.sh:36`, `tests/test-config-error-handling.sh:56`, `tests/test-config-error-handling.sh:84`). Model routing is covered by `tests/test-model-router.sh`, including Codex/Claude patterns, unknown/empty model failures, binary checks, and effort mapping (`tests/test-model-router.sh:29`, `tests/test-model-router.sh:207`, `tests/test-model-router.sh:243`, `tests/test-model-router.sh:311`, `tests/test-model-router.sh:397`). Monitor behavior has coverage in `tests/test-skill-monitor.sh` and `tests/test-pr-loop-system.sh` call sites found during inspection. I did not execute tests because this branch export is read-only and the test scripts create temp files, mock binaries, and git repos.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-049 `file` `scripts/bitlesson-select.sh`
- cursor: `[_]`
- core_role: Runtime selector that chooses which BitLesson entries apply to a sub-task, routing the selection prompt through Codex or Claude based on `bitlesson_model`.
- algorithmic_behavior: The script loads merged config, defaults `bitlesson_model` to `haiku`, detects provider with `detect_provider`, checks the required CLI, optionally falls back from missing Claude to Codex, validates the BitLesson file, builds a constrained selector prompt, executes the selected model with a 120-second timeout, and normalizes output to exactly `LESSON_IDS:` and `RATIONALE:` lines (`scripts/bitlesson-select.sh:9`, `scripts/bitlesson-select.sh:15`, `scripts/bitlesson-select.sh:16`, `scripts/bitlesson-select.sh:89`, `scripts/bitlesson-select.sh:95`, `scripts/bitlesson-select.sh:102`, `scripts/bitlesson-select.sh:128`, `scripts/bitlesson-select.sh:165`, `scripts/bitlesson-select.sh:168`, `scripts/bitlesson-select.sh:179`, `scripts/bitlesson-select.sh:197`, `scripts/bitlesson-select.sh:221`).
- inputs_outputs_state: Required CLI inputs are `--task`, `--paths`, and `--bitlesson-file` (`scripts/bitlesson-select.sh:26`, `scripts/bitlesson-select.sh:41`, `scripts/bitlesson-select.sh:67`, `scripts/bitlesson-select.sh:73`, `scripts/bitlesson-select.sh:79`). Config inputs come from `config/default_config.json`, user config, `.humanize/config.json`, or `HUMANIZE_CONFIG` through `load_merged_config` (`scripts/bitlesson-select.sh:15`). The BitLesson file content is embedded in the prompt between explicit sentinels (`scripts/bitlesson-select.sh:141`, `scripts/bitlesson-select.sh:142`, `scripts/bitlesson-select.sh:144`). Output is exactly two stdout lines if successful; errors and raw invalid model output go to stderr (`scripts/bitlesson-select.sh:213`, `scripts/bitlesson-select.sh:216`, `scripts/bitlesson-select.sh:221`).
- gates_or_invariants: `--task` and `--paths` must be non-empty, `--bitlesson-file` must be supplied, exist, and contain non-whitespace content (`scripts/bitlesson-select.sh:67`, `scripts/bitlesson-select.sh:73`, `scripts/bitlesson-select.sh:79`, `scripts/bitlesson-select.sh:102`, `scripts/bitlesson-select.sh:107`). Provider must be known and dependency must be available after fallback (`scripts/bitlesson-select.sh:89`, `scripts/bitlesson-select.sh:95`, `scripts/bitlesson-select.sh:99`). Codex runs with `model_reasoning_effort=high` and either `--full-auto` or the sandbox bypass flag from `HUMANIZE_CODEX_BYPASS_SANDBOX` (`scripts/bitlesson-select.sh:169`, `scripts/bitlesson-select.sh:171`, `scripts/bitlesson-select.sh:173`, `scripts/bitlesson-select.sh:176`). Selector output must contain both stable keys or the script fails and prints raw output (`scripts/bitlesson-select.sh:197`, `scripts/bitlesson-select.sh:205`, `scripts/bitlesson-select.sh:213`).
- dependencies_and_callers: Depends on `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh` (`scripts/bitlesson-select.sh:10`, `scripts/bitlesson-select.sh:11`, `scripts/bitlesson-select.sh:20`, `scripts/bitlesson-select.sh:23`). `loop-common.sh` also loads `DEFAULT_BITLESSON_MODEL`, described as controlling the CLI BitLesson selector (`hooks/lib/loop-common.sh:180`). BitLesson lifecycle references include `scripts/setup-rlcr-loop.sh` creating/enforcing `.humanize/bitlesson.md` state (`scripts/setup-rlcr-loop.sh:828`, `scripts/setup-rlcr-loop.sh:867`) and `hooks/loop-codex-stop-hook.sh` validating BitLesson delta behavior (`hooks/loop-codex-stop-hook.sh:749`).
- edge_cases_or_failure_modes: Unknown `bitlesson_model` causes `detect_provider` failure before execution (`scripts/lib/model-router.sh:28`). Missing Claude falls back to the configured default Codex model, but missing Codex after fallback remains fatal (`scripts/bitlesson-select.sh:95`, `scripts/bitlesson-select.sh:97`, `scripts/bitlesson-select.sh:99`). The script resolves `CODEX_PROJECT_ROOT` from the BitLesson file’s containing git repo, otherwise from the file directory (`scripts/bitlesson-select.sh:117`, `scripts/bitlesson-select.sh:118`, `scripts/bitlesson-select.sh:121`). Timeout returns exit 124 with a dedicated message (`scripts/bitlesson-select.sh:183`). Successful model execution with malformed output is still rejected by the stable-format gate (`scripts/bitlesson-select.sh:213`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` covers Codex routing for `gpt-*`, Claude routing for `haiku`, `sonnet`, and case-insensitive `OPUS`, unknown model failure, missing Codex failure, and missing Claude fallback to Codex (`tests/test-bitlesson-select-routing.sh:61`, `tests/test-bitlesson-select-routing.sh:89`, `tests/test-bitlesson-select-routing.sh:118`, `tests/test-bitlesson-select-routing.sh:147`, `tests/test-bitlesson-select-routing.sh:176`, `tests/test-bitlesson-select-routing.sh:202`, `tests/test-bitlesson-select-routing.sh:238`). It uses mock BitLesson files and mock CLI binaries that emit stable selector output (`tests/test-bitlesson-select-routing.sh:18`, `tests/test-bitlesson-select-routing.sh:29`, `tests/test-bitlesson-select-routing.sh:44`). I inspected but did not execute this test in the read-only branch export.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-079 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for template loader failure tolerance and safe rendering semantics.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh`, resolves the prompt-template directory, then runs twelve cases against `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir` (`tests/test-error-scenarios.sh:11`, `tests/test-error-scenarios.sh:13`, `tests/test-error-scenarios.sh:15`, `tests/test-error-scenarios.sh:42`, `tests/test-error-scenarios.sh:67`, `tests/test-error-scenarios.sh:80`, `tests/test-error-scenarios.sh:133`, `tests/test-error-scenarios.sh:160`). It manually counts pass/fail and exits nonzero on any failure (`tests/test-error-scenarios.sh:22`, `tests/test-error-scenarios.sh:25`, `tests/test-error-scenarios.sh:30`, `tests/test-error-scenarios.sh:218`).
- inputs_outputs_state: Inputs are the repository’s real `hooks/lib/template-loader.sh`, the real prompt-template directory, synthetic template strings, missing template paths, fallback strings, and an isolated strict-mode subshell (`tests/test-error-scenarios.sh:13`, `tests/test-error-scenarios.sh:46`, `tests/test-error-scenarios.sh:59`, `tests/test-error-scenarios.sh:98`, `tests/test-error-scenarios.sh:114`). Output is colored PASS/FAIL text plus final counts (`tests/test-error-scenarios.sh:17`, `tests/test-error-scenarios.sh:25`, `tests/test-error-scenarios.sh:211`).
- gates_or_invariants: Missing template files and missing template directories must return empty output without a failing exit for non-safe loaders (`tests/test-error-scenarios.sh:45`, `tests/test-error-scenarios.sh:58`, `tests/test-error-scenarios.sh:71`). Empty template content must render to empty output (`tests/test-error-scenarios.sh:84`). Special regex characters in values must be literal, not regex-expanded (`tests/test-error-scenarios.sh:97`). Strict shell mode must not abort when `load_and_render` returns empty for a missing template (`tests/test-error-scenarios.sh:112`). Safe rendering must use fallback text and substitute fallback variables (`tests/test-error-scenarios.sh:137`, `tests/test-error-scenarios.sh:150`). Empty and unclosed placeholders must remain unchanged (`tests/test-error-scenarios.sh:183`, `tests/test-error-scenarios.sh:196`).
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`. The production implementation documents single-pass placeholder substitution and missing-variable preservation (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:71`). `hooks/lib/loop-common.sh` sources that loader and warns rather than failing if the template directory is invalid (`hooks/lib/loop-common.sh:195`, `hooks/lib/loop-common.sh:198`, `hooks/lib/loop-common.sh:200`).
- edge_cases_or_failure_modes: This test intentionally asserts graceful degradation for missing files/directories, empty content, strict shell mode, placeholder syntax errors, and regex-like values. The underlying loader prints warnings for missing templates but returns empty (`hooks/lib/template-loader.sh:41`, `hooks/lib/template-loader.sh:44`). Single-pass AWK substitution prevents placeholder injection from values containing `{{...}}` (`hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`).
- validation_or_tests: The file is itself validation. It checks twelve cases and exits 0 only if `TESTS_FAILED` is zero (`tests/test-error-scenarios.sh:218`, `tests/test-error-scenarios.sh:221`, `tests/test-error-scenarios.sh:225`). I inspected the executable spec but did not run it in the read-only branch export.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-109 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: Executable specification for the unified `codex_model` / `codex_effort` configuration path across default config, merged config, loop setup, state parsing, stop hooks, PR loop, and ask-codex.
- algorithmic_behavior: The script verifies that `config/default_config.json` exposes `codex_model` and `codex_effort` and no stale reviewer keys, then checks that `scripts/lib/config-loader.sh` exposes those keys through default and project override layers (`tests/test-unified-codex-config.sh:50`, `tests/test-unified-codex-config.sh:61`, `tests/test-unified-codex-config.sh:68`, `tests/test-unified-codex-config.sh:77`, `tests/test-unified-codex-config.sh:83`, `tests/test-unified-codex-config.sh:94`, `tests/test-unified-codex-config.sh:108`). It then validates `hooks/lib/loop-common.sh` defaults, state-file fallback chains, stale-key ignoring, stop-hook effort validation, setup script state output, input regexes, PR loop inheritance/overrides, and ask-codex runtime behavior (`tests/test-unified-codex-config.sh:119`, `tests/test-unified-codex-config.sh:176`, `tests/test-unified-codex-config.sh:301`, `tests/test-unified-codex-config.sh:315`, `tests/test-unified-codex-config.sh:397`, `tests/test-unified-codex-config.sh:471`, `tests/test-unified-codex-config.sh:544`, `tests/test-unified-codex-config.sh:584`, `tests/test-unified-codex-config.sh:654`, `tests/test-unified-codex-config.sh:687`, `tests/test-unified-codex-config.sh:807`).
- inputs_outputs_state: Inputs include real project files, synthetic `.humanize/config.json` files, synthetic `state.md` frontmatter files, temporary git repos, local bare remotes, and mock `codex`/`gh` binaries (`tests/test-unified-codex-config.sh:56`, `tests/test-unified-codex-config.sh:106`, `tests/test-unified-codex-config.sh:187`, `tests/test-unified-codex-config.sh:263`, `tests/test-unified-codex-config.sh:328`, `tests/test-unified-codex-config.sh:435`, `tests/test-unified-codex-config.sh:482`, `tests/test-unified-codex-config.sh:724`, `tests/test-unified-codex-config.sh:822`). Outputs are shared helper PASS/FAIL/SKIP results, stderr assertions, generated loop state files, and final summary via `print_test_summary` (`tests/test-unified-codex-config.sh:18`, `tests/test-unified-codex-config.sh:22`, `tests/test-unified-codex-config.sh:878`).
- gates_or_invariants: Defaults must be `gpt-5.4` and `high` (`tests/test-unified-codex-config.sh:61`, `tests/test-unified-codex-config.sh:64`; `config/default_config.json:2`, `config/default_config.json:3`). Deprecated `loop_reviewer_*` keys must be absent or ignored (`tests/test-unified-codex-config.sh:67`, `tests/test-unified-codex-config.sh:142`, `tests/test-unified-codex-config.sh:343`). State values must override defaults; missing state fields must fall back to config-backed defaults (`tests/test-unified-codex-config.sh:208`, `tests/test-unified-codex-config.sh:243`, `tests/test-unified-codex-config.sh:282`). Invalid effort such as `superhigh` must stop before invoking Codex (`tests/test-unified-codex-config.sh:447`, `tests/test-unified-codex-config.sh:454`, `tests/test-unified-codex-config.sh:461`). Model names reject spaces and shell metacharacters; efforts accept only `xhigh`, `high`, `medium`, `low` (`tests/test-unified-codex-config.sh:550`, `tests/test-unified-codex-config.sh:558`, `tests/test-unified-codex-config.sh:565`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for assertions and temp git repos (`tests/test-unified-codex-config.sh:18`; `tests/test-helpers.sh:30`, `tests/test-helpers.sh:86`, `tests/test-helpers.sh:93`). Production dependencies include `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `hooks/pr-loop-stop-hook.sh`, and `scripts/ask-codex.sh` (`tests/test-unified-codex-config.sh:83`, `tests/test-unified-codex-config.sh:125`, `tests/test-unified-codex-config.sh:402`, `tests/test-unified-codex-config.sh:590`, `tests/test-unified-codex-config.sh:660`). The production default assignment is in `loop-common.sh` with pre-set value > config > hardcoded fallback precedence (`hooks/lib/loop-common.sh:184`, `hooks/lib/loop-common.sh:187`, `hooks/lib/loop-common.sh:188`, `hooks/lib/loop-common.sh:190`).
- edge_cases_or_failure_modes: The test covers missing optional fields, stale config/state keys, invalid state edits, no real Codex invocation on invalid effort, setup help text drift, PR-loop hardcoded model regressions, and ask-codex overriding config defaults at runtime. Some sections skip when external dependencies are absent, notably `jq`, `codex`, or files under test (`tests/test-unified-codex-config.sh:58`, `tests/test-unified-codex-config.sh:404`, `tests/test-unified-codex-config.sh:477`, `tests/test-unified-codex-config.sh:813`). The stop hook’s production gate checks model regex and effort enum before proceeding (`hooks/loop-codex-stop-hook.sh:158`, `hooks/loop-codex-stop-hook.sh:160`, `hooks/loop-codex-stop-hook.sh:165`).
- validation_or_tests: The file is a broad executable validation suite for config unification. It asserts both static source patterns and runtime behavior with mocks, ending with `print_test_summary` (`tests/test-unified-codex-config.sh:31`, `tests/test-unified-codex-config.sh:38`, `tests/test-unified-codex-config.sh:878`). I inspected it but did not run it because the branch export is read-only and the suite creates temp repos, config files, mock binaries, and generated state.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-139 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Prompt/block template defining a hard behavioral gate: agents must not write to generated round prompt files.
- algorithmic_behavior: The template states that `round-*-prompt.md` files contain instructions from Codex to Claude, so the receiving agent cannot modify its own instructions and must instead read the prompt, execute tasks, and write results to the summary file (`prompt-template/block/prompt-file-write.md:1`, `prompt-template/block/prompt-file-write.md:3`, `prompt-template/block/prompt-file-write.md:5`, `prompt-template/block/prompt-file-write.md:7`, `prompt-template/block/prompt-file-write.md:8`, `prompt-template/block/prompt-file-write.md:10`). It also routes prompt errors to summary documentation rather than prompt edits (`prompt-template/block/prompt-file-write.md:12`).
- inputs_outputs_state: Input is an attempted prompt-file write/edit situation. Output is a human-readable block message generated through the template loader path, with no variables in this specific template (`prompt-template/block/prompt-file-write.md:1`). The expected state transition is from blocked write attempt to summary-file reporting.
- gates_or_invariants: `round-*-prompt.md` is treated as immutable instruction input. The invariant is separation of instruction files from agent output files: read prompt, execute task, write summary (`prompt-template/block/prompt-file-write.md:3`, `prompt-template/block/prompt-file-write.md:7`).
- dependencies_and_callers: The template directory is loaded by `hooks/lib/template-loader.sh` and made available through `hooks/lib/loop-common.sh` (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:36`, `hooks/lib/loop-common.sh:195`, `hooks/lib/loop-common.sh:198`). The exact caller was not isolated in the assigned-file pass, but it fits the block-template family used by loop validators.
- edge_cases_or_failure_modes: If the prompt itself contains errors, the template explicitly prevents self-modification and instructs the agent to document the issue in the summary (`prompt-template/block/prompt-file-write.md:12`). If the template were missing, loader behavior would degrade to empty or fallback depending on caller, as specified by `hooks/lib/template-loader.sh` and tested by `tests/test-error-scenarios.sh`.
- validation_or_tests: Template loader error behavior and fallback semantics are covered by `tests/test-error-scenarios.sh`, including missing block templates and strict-mode resilience (`tests/test-error-scenarios.sh:45`, `tests/test-error-scenarios.sh:112`, `tests/test-error-scenarios.sh:137`). I did not find a direct assertion for this exact file in the inspected call sites; it remains a core prompt gate because it defines an algorithmic write boundary.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-169 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Plan-generation contract template that defines the required structure for goal description, acceptance criteria, path boundaries, dependencies, task routing, deliberation, pending decisions, implementation notes, and translated output variants.
- algorithmic_behavior: The template requires acceptance criteria with positive and negative deterministic tests (`prompt-template/plan/gen-plan-template.md:6`, `prompt-template/plan/gen-plan-template.md:8`, `prompt-template/plan/gen-plan-template.md:10`, `prompt-template/plan/gen-plan-template.md:14`). It defines upper/lower implementation bounds and allowed choices, including special handling for deterministic designs where choices are fixed (`prompt-template/plan/gen-plan-template.md:25`, `prompt-template/plan/gen-plan-template.md:29`, `prompt-template/plan/gen-plan-template.md:34`, `prompt-template/plan/gen-plan-template.md:39`, `prompt-template/plan/gen-plan-template.md:44`). It separates non-prescriptive hints from requirements (`prompt-template/plan/gen-plan-template.md:46`, `prompt-template/plan/gen-plan-template.md:48`). It requires milestone/phase sequencing and a task table where each task has exactly one routing tag, either `coding` or `analyze` (`prompt-template/plan/gen-plan-template.md:57`, `prompt-template/plan/gen-plan-template.md:69`, `prompt-template/plan/gen-plan-template.md:71`, `prompt-template/plan/gen-plan-template.md:75`). It also requires deliberation/convergence and pending user decision tracking (`prompt-template/plan/gen-plan-template.md:80`, `prompt-template/plan/gen-plan-template.md:88`, `prompt-template/plan/gen-plan-template.md:91`).
- inputs_outputs_state: Input is a draft goal/request and relevant planning context. Output is a generated `plan.md`-style document following this structure (`prompt-template/plan/gen-plan-template.md:1`, `prompt-template/plan/gen-plan-template.md:106`). Optional config input `alternative_plan_language` controls whether a translated variant is generated; when enabled, identifiers and technical names remain unchanged while the content is translated (`prompt-template/plan/gen-plan-template.md:110`, `prompt-template/plan/gen-plan-template.md:112`, `prompt-template/plan/gen-plan-template.md:118`, `prompt-template/plan/gen-plan-template.md:120`).
- gates_or_invariants: Acceptance criteria must be testable with pass/fail cases. Tasks must have exactly one routing tag. Generated implementation code/comments must not carry plan workflow markers such as `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:8`, `prompt-template/plan/gen-plan-template.md:71`, `prompt-template/plan/gen-plan-template.md:101`). If `alternative_plan_language` is empty, English, absent, or unsupported, no translated variant should be written (`prompt-template/plan/gen-plan-template.md:120`).
- dependencies_and_callers: The template is under the repository prompt-template tree loaded by `hooks/lib/template-loader.sh` (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:36`). Config defaults include `gen_plan_mode` and are merged through `config-loader.sh` (`config/default_config.json:7`; `scripts/lib/config-loader.sh:63`). `tests/test-config-merge.sh` asserts `gen_plan_mode` exists and participates in the merge (`tests/test-config-merge.sh:59`, `tests/test-config-merge.sh:183`, `tests/test-config-merge.sh:188`).
- edge_cases_or_failure_modes: Deterministic designs are allowed to collapse upper/lower bounds into a fixed implementation path rather than pretending optionality exists (`prompt-template/plan/gen-plan-template.md:44`). Unsupported translation language intentionally produces no variant, while missing `.humanize/config.json` is expected to lead to a default config with empty `alternative_plan_language` (`prompt-template/plan/gen-plan-template.md:120`). A malformed or missing template would be governed by shared template loader fallback behavior.
- validation_or_tests: No direct executable test for this exact template was found in the assigned pass. Related validation exists for template loading/rendering safety in `tests/test-error-scenarios.sh` and config merge behavior in `tests/test-config-merge.sh`. This is not a skip candidate because the template defines planning transitions and routing contracts used by the algorithmic workflow.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-199 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state file parsing and current-round extraction under valid, malformed, partial, and edge-case state files.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh`, creates a temp test directory, then generates many synthetic `state.md` variants to exercise tolerant `parse_state_file`, strict `parse_state_file_strict`, and `get_current_round` (`tests/robustness/test-state-file-robustness.sh:14`, `tests/robustness/test-state-file-robustness.sh:16`, `tests/robustness/test-state-file-robustness.sh:19`, `tests/robustness/test-state-file-robustness.sh:33`, `tests/robustness/test-state-file-robustness.sh:62`, `tests/robustness/test-state-file-robustness.sh:149`, `tests/robustness/test-state-file-robustness.sh:201`). It distinguishes strict rejection from tolerant fallback behavior and exits through the shared test summary (`tests/robustness/test-state-file-robustness.sh:476`, `tests/robustness/test-state-file-robustness.sh:480`).
- inputs_outputs_state: Inputs are synthetic YAML-frontmatter state files containing fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `plan_file`, `start_branch`, `review_started`, and `full_review_round` (`tests/robustness/test-state-file-robustness.sh:35`, `tests/robustness/test-state-file-robustness.sh:75`, `tests/robustness/test-state-file-robustness.sh:103`, `tests/robustness/test-state-file-robustness.sh:402`). Outputs are global `STATE_*` variables set by production parser functions plus PASS/FAIL summary. Production tolerant parser defaults `STATE_CURRENT_ROUND=0`, `STATE_MAX_ITERATIONS=10`, `STATE_FULL_REVIEW_ROUND=5`, `STATE_ASK_CODEX_QUESTION=true`, `STATE_AGENT_TEAMS=false`, and `STATE_PRIVACY_MODE=true` (`hooks/lib/loop-common.sh:397`, `hooks/lib/loop-common.sh:404`, `hooks/lib/loop-common.sh:411`, `hooks/lib/loop-common.sh:418`).
- gates_or_invariants: Valid state files must parse expected round and iteration fields (`tests/robustness/test-state-file-robustness.sh:33`, `tests/robustness/test-state-file-robustness.sh:52`). Extra fields must be ignored (`tests/robustness/test-state-file-robustness.sh:72`). Quoted strings and paths with spaces must preserve usable values (`tests/robustness/test-state-file-robustness.sh:100`, `tests/robustness/test-state-file-robustness.sh:314`). Strict mode must reject missing frontmatter, non-numeric `current_round`, and missing required fields (`tests/robustness/test-state-file-robustness.sh:149`, `tests/robustness/test-state-file-robustness.sh:173`, `tests/robustness/test-state-file-robustness.sh:201`). Tolerant mode must default or avoid crashing on missing/malformed/empty/partial files (`tests/robustness/test-state-file-robustness.sh:163`, `tests/robustness/test-state-file-robustness.sh:217`, `tests/robustness/test-state-file-robustness.sh:230`, `tests/robustness/test-state-file-robustness.sh:242`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-state-file-robustness.sh:16`, `tests/robustness/test-state-file-robustness.sh:17`). Production parser implementation is in `loop-common.sh`: `get_current_round` extracts raw frontmatter and defaults to 0 (`hooks/lib/loop-common.sh:339`, `hooks/lib/loop-common.sh:343`, `hooks/lib/loop-common.sh:348`); `_parse_state_fields` maps recognized fields to globals (`hooks/lib/loop-common.sh:355`, `hooks/lib/loop-common.sh:363`, `hooks/lib/loop-common.sh:366`, `hooks/lib/loop-common.sh:370`); `parse_state_file_strict` validates separators, required fields, numerics, and boolean `review_started` (`hooks/lib/loop-common.sh:433`, `hooks/lib/loop-common.sh:441`, `hooks/lib/loop-common.sh:453`, `hooks/lib/loop-common.sh:471`, `hooks/lib/loop-common.sh:483`).
- edge_cases_or_failure_modes: Covered cases include absent YAML separators, single separator, malformed YAML, very large numbers, negative numbers, trailing whitespace, nonexistent file, binary payload after YAML, CRLF endings, optional `full_review_round` defaulting, and minimum full-review interval (`tests/robustness/test-state-file-robustness.sh:149`, `tests/robustness/test-state-file-robustness.sh:242`, `tests/robustness/test-state-file-robustness.sh:259`, `tests/robustness/test-state-file-robustness.sh:277`, `tests/robustness/test-state-file-robustness.sh:297`, `tests/robustness/test-state-file-robustness.sh:338`, `tests/robustness/test-state-file-robustness.sh:358`, `tests/robustness/test-state-file-robustness.sh:367`, `tests/robustness/test-state-file-robustness.sh:386`, `tests/robustness/test-state-file-robustness.sh:399`, `tests/robustness/test-state-file-robustness.sh:425`, `tests/robustness/test-state-file-robustness.sh:450`). One notable behavior: strict parser currently requires `review_started` and `base_branch` in production (`hooks/lib/loop-common.sh:462`, `hooks/lib/loop-common.sh:466`), while several test cases target tolerant mode or older/minimal state shapes.
- validation_or_tests: This file is itself the validation suite and ends with `print_test_summary "State File Robustness Test Summary"` (`tests/robustness/test-state-file-robustness.sh:480`). I inspected it but did not run it because the branch export is read-only and the suite writes many synthetic state files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 Item Evidence headers present, one per assigned row
- missing_items: none
- duplicate_items: none in Item Evidence headers
- final_worker_status: `complete`