# agent_04 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-004 `directory` `config`
- cursor: `[_]`
- core_role: Runtime configuration root for Humanize defaults. Recursive inspection found one child file: `config/default_config.json`; no nested subdirectories.
- algorithmic_behavior: The directory is the plugin-default layer in the merged configuration stack. `scripts/lib/config-loader.sh` resolves `default_config_path="$plugin_root/config/default_config.json"` and treats it as required before merging user and project layers (`scripts/lib/config-loader.sh:77-111`). The directory therefore provides baseline behavior for Codex model selection, BitLesson provider routing, Agent Teams defaulting, and plan generation mode.
- inputs_outputs_state: Input is a JSON object file under this directory. Output is merged JSON from `load_merged_config`, with precedence `{}` < plugin defaults < user config < project config (`scripts/lib/config-loader.sh:113-135`). State is not written back into `config`; defaults are read-only and copied into runtime state files or command behavior elsewhere.
- gates_or_invariants: The default config file is mandatory; missing or malformed required default config causes a fatal non-zero path (`scripts/lib/config-loader.sh:40-56`). Layers must be JSON objects, and null values are stripped before merge (`scripts/lib/config-loader.sh:119-131`).
- dependencies_and_callers: Called by `hooks/lib/loop-common.sh` to set `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, and `DEFAULT_CODEX_EFFORT` (`hooks/lib/loop-common.sh:178-190`); by `scripts/bitlesson-select.sh` for selector model routing (`scripts/bitlesson-select.sh:13-17`); by setup scripts and config tests through `load_merged_config`.
- edge_cases_or_failure_modes: If `jq` is missing, the config layer fails before merge (`scripts/lib/config-loader.sh:17-21`). If user/project config is malformed, it is ignored with a warning, but malformed or missing plugin default config is fatal. A `HUMANIZE_CONFIG` env var changes the project layer path, not this default directory (`scripts/lib/config-loader.sh:84-88`).
- validation_or_tests: `tests/test-config-merge.sh` covers default-only, user, project, null-stripping, and `HUMANIZE_CONFIG` behavior. `tests/test-config-error-handling.sh` covers missing/malformed default and optional-layer behavior. `tests/test-unified-codex-config.sh` asserts default `codex_model`, `codex_effort`, and absence of stale reviewer keys.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-034 `file` `config/default_config.json`
- cursor: `[_]`
- core_role: Required plugin-default JSON object that seeds runtime defaults for Codex, BitLesson, Agent Teams, Chinese/alternative plan compatibility, and plan generation mode.
- algorithmic_behavior: The file contains the default keys: `codex_model: gpt-5.4`, `codex_effort: high`, `bitlesson_model: haiku`, `agent_teams: false`, `chinese_plan: false`, and `gen_plan_mode: discussion` (`config/default_config.json:1-8`). These values are merged and then read by feature-specific scripts; absent project/user values inherit these defaults.
- inputs_outputs_state: Input is static JSON. Output values flow into shell variables such as `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, and `DEFAULT_BITLESSON_MODEL` (`hooks/lib/loop-common.sh:181-190`), BitLesson selector model (`scripts/bitlesson-select.sh:15-17`), generated loop state files (`scripts/setup-rlcr-loop.sh:848-870`, `scripts/setup-pr-loop.sh:623-624`), and generated plan command behavior (`commands/gen-plan.md:107-112`).
- gates_or_invariants: Must exist and parse as a JSON object. The config loader rejects missing required defaults and required malformed defaults (`scripts/lib/config-loader.sh:40-56`). Values are not schema-validated here beyond JSON object shape; downstream callers impose semantic defaults or validations.
- dependencies_and_callers: `scripts/lib/config-loader.sh` is the direct loader; `hooks/lib/loop-common.sh`, `scripts/bitlesson-select.sh`, setup scripts, `commands/gen-plan.md`, and docs consume or document these keys. The PR loop intentionally does not pre-set `codex_model` so loop-common config defaults apply (`scripts/setup-pr-loop.sh:18`).
- edge_cases_or_failure_modes: Deprecated `chinese_plan` is retained for backward compatibility in plan generation; `commands/gen-plan.md` says `alternative_plan_language` takes precedence when present and emits warnings for deprecated or unsupported language settings (`commands/gen-plan.md:81-112`). Stale keys like `loop_reviewer_model` are ignored by tests and should not appear in defaults.
- validation_or_tests: `tests/test-unified-codex-config.sh` asserts `codex_model` is `gpt-5.4`, `codex_effort` is `high`, and legacy reviewer keys are absent. `tests/test-config-merge.sh` asserts `bitlesson_model`, `agent_teams`, and `gen_plan_mode` defaulting and override precedence. `tests/test-config-error-handling.sh` asserts fatal behavior when this file is missing.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-064 `file` `templates/bitlesson.md`
- cursor: `[_]`
- core_role: Initial project BitLesson knowledge-base template. It defines the strict entry schema used by RLCR memory capture and later selector/validator gates.
- algorithmic_behavior: The template establishes an empty knowledge base with a required field order: `Lesson ID`, `Scope`, `Problem Description`, `Root Cause`, `Solution`, `Constraints`, `Validation Evidence`, and `Source Rounds` (`templates/bitlesson.md:5-19`), followed by an `## Entries` insertion point (`templates/bitlesson.md:21-23`). `scripts/setup-rlcr-loop.sh` initializes `.humanize/bitlesson.md` from this template before creating loop state (`scripts/setup-rlcr-loop.sh:825-834`).
- inputs_outputs_state: Input is the template file plus project root and relative target path. `scripts/bitlesson-init.sh` creates `<project>/.humanize/bitlesson.md` if missing and prints the resolved path (`scripts/bitlesson-init.sh:8-14`, `83-88`). The generated project file becomes persistent local loop memory and is referenced in prompts, selector calls, and summary validation.
- gates_or_invariants: `bitlesson-init.sh` rejects missing project/template arguments, nonexistent project root, missing template, absolute or `..` relpaths, and non-regular existing BitLesson paths (`scripts/bitlesson-init.sh:48-80`). The validator treats placeholder IDs such as `<BL-YYYYMMDD-short-name>` as non-concrete entries (`scripts/bitlesson-validate-delta.sh:143-155`).
- dependencies_and_callers: Used by `scripts/setup-rlcr-loop.sh` for initialization and prompt generation. Runtime task selection uses `scripts/bitlesson-select.sh`, which requires a nonempty BitLesson file, embeds its content in a selector prompt, and returns stable `LESSON_IDS` and `RATIONALE` lines (`scripts/bitlesson-select.sh:102-158`, `197-222`). Summary enforcement uses `scripts/bitlesson-validate-delta.sh`.
- edge_cases_or_failure_modes: Empty or whitespace-only BitLesson files fail selection (`scripts/bitlesson-select.sh:107-110`). `Action: none` can be blocked if the KB has no concrete entries and strict mode is enabled (`scripts/bitlesson-validate-delta.sh:158-183`). `Action: add|update` requires concrete IDs in `BL-YYYYMMDD-short-name` format that exist in the KB (`scripts/bitlesson-validate-delta.sh:185-283`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` covers selector routing for Codex/Claude model names, unknown model failure, and fallback behavior. `scripts/bitlesson-validate-delta.sh` is invoked by `hooks/loop-codex-stop-hook.sh` for all non-finalize RLCR summaries when BitLesson is required (`hooks/loop-codex-stop-hook.sh:745-764`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-094 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop hook behavior, including validators, stop-hook polling, bot state transitions, trigger detection, approval-only review handling, fixture-backed GitHub API processing, wrong-round resolution protection, and monitor display.
- algorithmic_behavior: The file defines `run_hook_tests()` (`tests/test-pr-loop-hooks.sh:15`) and runs themed test clusters. Validator tests assert PR loop state and generated comment/check files are read-only while `round-N-pr-resolve.md` is writable for the current round (`tests/test-pr-loop-hooks.sh:26-210`). Sorting/gate tests specify newest-first timestamp ordering, human-before-bot ordering, deduplication by comment ID, YAML list parsing, per-bot status extraction, approved-bot re-add when new issues appear, and terminal `APPROVE`/`WAITING_FOR_BOTS` markers (`tests/test-pr-loop-hooks.sh:222-466`).
- inputs_outputs_state: Inputs are synthetic `.humanize/pr-loop/<timestamp>/state.md` files, mock `gh`/`git` binaries, mocked PR comments/reviews, fixture JSON files, and hook JSON payloads for `Write`, `Edit`, and `Bash`. Outputs are pass/fail assertions, hook exit codes, block messages, generated PR comment files, stop-hook stderr, and monitor output. State transitions under test include `configured_bots` as immutable polling scope, `active_bots` as shrinking/re-expanding issue scope, `last_trigger_at` refresh, timeout-driven active-bot removal, and loop completion through `approve-state.md`.
- gates_or_invariants: Write/Edit/Bash validators must exit `2` for protected PR loop state/read-only files and wrong-round resolve files (`tests/test-pr-loop-hooks.sh:68-163`, `1431-1535`). Stop hook must block missing resolution summaries (`tests/test-pr-loop-hooks.sh:766-840`), require/refresh user-authored trigger comments, prefer `last_trigger_at` even in round 0, and anchor per-bot timeout to trigger time (`tests/test-pr-loop-hooks.sh:538-754`, `845-1148`). Fetch/poll must include empty-body approval reviews with placeholders and respect `--after` filters (`tests/test-pr-loop-hooks.sh:1160-1419`).
- dependencies_and_callers: Directly calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, and `tests/setup-monitor-test-env.sh`. It depends on harness globals/functions such as `TEST_DIR`, `SCRIPT_DIR`, `PROJECT_ROOT`, `pass`, `fail`, and `init_test_git_repo`.
- edge_cases_or_failure_modes: Covers PR loop protections when no RLCR loop exists (`tests/test-pr-loop-hooks.sh:174-210`), trigger comments from other users ignored (`538-586`), paginated trigger comments on later pages (`947-1048`), round 0 trigger priority (`1052-1148`), empty `active_bots` monitor display (`1596-1621`), approval-only reviews with `null` or empty bodies (`1161-1196`), and wrong-round writes/edits (`1431-1535`). It also intentionally mocks `gh --jq --paginate` behavior because pagination shape is a known failure mode (`879-915`, `979-1022`).
- validation_or_tests: This file is itself validation. It is included in the repo test surface and `tests/run-all-tests.sh` references `test-pr-loop-hooks.sh`. Direct implementation surfaces include PR state parsing (`hooks/pr-loop-stop-hook.sh:88-175`), polling and timeout logic (`hooks/pr-loop-stop-hook.sh:802-1125`), validator PR-loop protection (`hooks/loop-write-validator.sh:73-97`, `hooks/loop-edit-validator.sh:56-80`), and fetch/poll scripts.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-124 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Stop-hook block template appended when the RLCR repository has non-`.humanize` untracked files. It shapes the remediation guidance inside the broader “git not clean” gate.
- algorithmic_behavior: The template warns that untracked files may be build artifacts, dependencies, IDE files, logs, caches, or temp files, and says to review them and add suitable `.gitignore` patterns (`prompt-template/block/git-not-clean-untracked.md:2-10`). `hooks/loop-codex-stop-hook.sh` loads it when `OTHER_UNTRACKED` is non-empty (`hooks/loop-codex-stop-hook.sh:637-644`) and injects it into the rendered git-not-clean block (`hooks/loop-codex-stop-hook.sh:653-661`).
- inputs_outputs_state: Input is cached `git status --porcelain` output. `.humanize/` untracked entries are excluded from dirty blocking because plugin state is intentionally local (`hooks/loop-codex-stop-hook.sh:618-621`), while other `??` entries add this note to `SPECIAL_NOTES`. Output is a JSON stop-hook block decision with rendered reason and system message (`hooks/loop-codex-stop-hook.sh:663-671`).
- gates_or_invariants: The template does not independently block; the invariant is that any non-ignored, non-`.humanize` dirty state blocks RLCR exit until changes are committed, ignored, or removed. If the template cannot be loaded, the hook falls back to a concise hardcoded untracked-file instruction (`hooks/loop-codex-stop-hook.sh:640-643`).
- dependencies_and_callers: Called through `load_template`/`load_and_render_safe` from `hooks/loop-codex-stop-hook.sh`. It coordinates with `prompt-template/block/git-not-clean.md` as the parent block and with `prompt-template/block/git-not-clean-humanize-local.md` for `.humanize*` local-state notes.
- edge_cases_or_failure_modes: `.humanize/` untracked files do not dirty the gate, but `.humanize*` untracked entries can still receive a separate explanatory note if they appear in raw status (`hooks/loop-codex-stop-hook.sh:625-635`). Template absence is non-fatal because of fallback text. The hook also cleans stale `.git/index.lock` before presenting the block (`hooks/loop-codex-stop-hook.sh:648-651`).
- validation_or_tests: Dirty-git blocking is exercised by finalize/stop-hook tests and git robustness tests, including untracked file counting scenarios in `tests/robustness/test-git-operations-robustness.sh`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-154 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Agent Teams prompt header for the initial RLCR round. It changes the implementation prompt from single-agent execution to team-leader orchestration.
- algorithmic_behavior: The file declares Agent Teams mode, identifies the current actor as Team Leader, and instructs the leader to read the implementation plan before creating the team (`prompt-template/claude/agent-teams-instructions.md:1-5`). It names the plan file, `goal-tracker.md`, and work summary as required RLCR coordination surfaces (`prompt-template/claude/agent-teams-instructions.md:5-8`).
- inputs_outputs_state: Input is the `AGENT_TEAMS=true` setup option/config state. Output is appended text in `round-0-prompt.md` when both this header and `agent-teams-core.md` are present (`scripts/setup-rlcr-loop.sh:1155-1164`). The same setup writes `agent_teams: $AGENT_TEAMS` to loop state (`scripts/setup-rlcr-loop.sh:865`).
- gates_or_invariants: The prompt imposes a workflow invariant rather than a shell gate: the Team Leader must coordinate tasks, consult `goal-tracker.md`, and produce the designated summary. The setup script has a fallback Agent Teams block if the template files are missing, preserving the mode’s core constraint (`scripts/setup-rlcr-loop.sh:1165-1175`).
- dependencies_and_callers: Appended by `scripts/setup-rlcr-loop.sh` together with `prompt-template/claude/agent-teams-core.md`. Later rounds use related continuation templates such as `prompt-template/claude/agent-teams-continue.md`; stop-hook prompt generation references Agent Teams continuation behavior.
- edge_cases_or_failure_modes: If this header or the core template is absent, setup does not fail; it injects a simplified fallback. If `agent_teams` is false, no initial Agent Teams instructions are appended. Tests also assert review phase does not incorrectly include continuation Agent Teams text.
- validation_or_tests: `tests/test-agent-teams.sh` checks the template exists with content, that `agent_teams` is recorded and parsed, and that implementation/review phase prompts include or exclude Agent Teams continuation as expected.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-184 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: Installed skill instruction that defines the Humanize algorithmic workflow contract for RLCR, PR loop automation, plan generation, ask-codex consultation, monitoring, and state layout.
- algorithmic_behavior: The skill describes the two-phase RLCR loop: implementation summaries reviewed by Codex until `COMPLETE`, then `codex review --base <branch>` until no issues remain and finalize begins (`skills/humanize/SKILL.md:31-48`). It also defines the PR loop as PR detection, bot comment fetching, issue fixing, bot retriggering, polling, local Codex validation, and repeat-until-approval/max-iterations (`skills/humanize/SKILL.md:49-60`). Command sections bind these workflows to concrete scripts (`skills/humanize/SKILL.md:72-147`).
- inputs_outputs_state: Inputs include a plan file, PR bot selection flags, Codex model/timeouts, base branch, full-review cadence, Agent Teams option, and one-shot questions. Outputs are `.humanize/rlcr`, `.humanize/pr-loop`, and `.humanize/skill` artifacts, including state files, prompts, summaries, review results, finalize/methodology files, and PR resolution files (`skills/humanize/SKILL.md:218-245`).
- gates_or_invariants: The skill explicitly requires running `scripts/rlcr-stop-gate.sh` in skill mode to enforce hook-equivalent transitions and blocking (`skills/humanize/SKILL.md:47`, `83-84`, `210`). Important rules require summaries, goal-tracker maintenance, thorough evidence, no state-file cheating, and stop-gate use each round (`skills/humanize/SKILL.md:204-211`).
- dependencies_and_callers: References runtime root hydration (`skills/humanize/SKILL.md:11-20`), `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh`. Requires `codex` and `gh` (`skills/humanize/SKILL.md:213-217`).
- edge_cases_or_failure_modes: Review-only mode uses `--skip-impl`; plan immutability can be enforced with `--track-plan-file`; methodology analysis can be disabled with `--privacy`; `--yolo` skips quiz and answers Codex questions automatically (`skills/humanize/SKILL.md:87-101`). The skill is instruction-only, so correctness depends on the scripts and hooks honoring these contracts.
- validation_or_tests: The contracts are covered across the shell test suite: RLCR stop gate tests, PR loop hook/system tests, config tests, monitor tests, plan-file validation tests, and Agent Teams tests. The assigned `tests/test-pr-loop-hooks.sh` specifically validates the PR-loop portions described in this skill.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item sections above, one per assigned row; item IDs are intentionally not repeated here to preserve single-occurrence evidence headings.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`