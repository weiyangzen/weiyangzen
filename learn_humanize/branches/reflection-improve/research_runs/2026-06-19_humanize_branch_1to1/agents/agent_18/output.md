# agent_18 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`
- research_mode: read-only source inspection; no files modified; tests not executed

## Item Evidence

### REFLECTION_IMPROVE-HZ-018 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR-loop prompt-template bundle for initializing and advancing the remote-review loop. It defines the round-0 agent prompt, initial goal tracker, critical completion contract, and Codex goal-tracker update instructions consumed by PR-loop setup and stop-hook validation.
- algorithmic_behavior: The directory contains seven templates. `round-0-header.md:1-15` creates the shared PR context and fetched-comment section. `round-0-task-no-comments.md:4-30` defines the fresh-PR branch: write an initial summary, do not trigger review manually, and let the Stop Hook poll. `round-0-task-has-comments.md:4-43` defines the review-comments branch: analyze comments, prioritize human comments, fix issues, commit, push, comment with bot mentions, and write a resolution summary. `critical-requirements-no-comments.md:6-20` and `critical-requirements-has-comments.md:6-23` repeat the terminal completion contract. `goal-tracker-initial.md:1-33` seeds PR metadata, monitored bots, issue summary, totals, and round-0 log. `codex-goal-tracker-update.md:7-20` instructs Codex to append per-reviewer rows, update totals, and add issue-log entries for the next round.
- inputs_outputs_state: Inputs are template variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `STARTED_AT`, `STARTUP_CASE`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are rendered prompt material under `.humanize/pr-loop/<timestamp>/`, especially `round-0-prompt.md`, `goal-tracker.md`, terminal critical instructions, and Codex prompt sections. State transition is from initialized PR-loop state to either “awaiting initial bot reviews” or “human/agent must resolve existing comments before polling.”
- gates_or_invariants: `scripts/setup-pr-loop.sh:723-729` chooses the no-comment branch only when the fetched comments file contains the exact `*No comments found.*` sentinel; otherwise it treats comments as present. Existing-comment flow requires commit, push, PR comment, and resolution summary. Fresh-PR flow forbids triggering the first review manually. Both task templates mark `.humanize/pr-loop/` state files as system-managed and not agent-editable.
- dependencies_and_callers: `scripts/setup-pr-loop.sh:712-713` renders `goal-tracker-initial.md`; `scripts/setup-pr-loop.sh:758-764` renders the header and appends fetched comments; `scripts/setup-pr-loop.sh:767-848` selects and appends the task template; `scripts/setup-pr-loop.sh:890-941` renders the critical requirements. `hooks/pr-loop-stop-hook.sh:1253-1263` loads `codex-goal-tracker-update.md` into Codex’s per-bot review validation prompt. All rendering goes through the template-loader safe-render path with script-local fallbacks.
- edge_cases_or_failure_modes: Comment detection is binary rather than a real count: any non-sentinel content becomes `COMMENT_COUNT=1`. Template load failure falls back to embedded text in `setup-pr-loop.sh`, so behavior can continue with stale fallback content if a template is broken. The no-comment branch depends on the fetch script preserving the sentinel exactly. Multi-bot mentions rely on `BOT_MENTION_STRING` being precomputed correctly by the shared helper.
- validation_or_tests: PR-loop suites are registered in `tests/run-all-tests.sh:86-89`, and the setup script itself exercises these templates during PR-loop initialization. The directory is core, not documentation-only, because it defines the agent contract and state-transition prompts for the PR-loop algorithm.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-048 `file` `scripts/bitlesson-init.sh`
- cursor: `[_]`
- core_role: Runtime bootstrap script for BitLesson state. It initializes a project-local `.humanize/bitlesson.md` from a plugin template without overwriting existing project knowledge.
- algorithmic_behavior: The script parses `--project-root`, `--template`, optional `--bitlesson-relpath`, and help flags at `scripts/bitlesson-init.sh:22-46`. It requires project root and template path, verifies the root is an existing directory and template is an existing file, rejects absolute or parent-traversal relpaths, resolves the project root with `pwd -P`, rejects existing non-regular targets, creates parent directories, copies the template only when the target file is missing, and prints the resolved target path at `scripts/bitlesson-init.sh:48-88`.
- inputs_outputs_state: Inputs are CLI flags, a project directory, a template file, and an optional relative state path. Output is stdout containing the resolved BitLesson file path. State transition is idempotent: missing target becomes a copied template; existing regular file is preserved unchanged; existing directory or special file at the target is a hard error.
- gates_or_invariants: `set -euo pipefail` makes argument and filesystem failures fatal. `scripts/bitlesson-init.sh:70-73` blocks absolute paths and `..` segments. `scripts/bitlesson-init.sh:78-80` blocks non-file targets. `scripts/bitlesson-init.sh:83-86` enforces no-overwrite creation.
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh:828-834` calls this script to initialize `.humanize/bitlesson.md` before writing RLCR loop state. `scripts/setup-rlcr-loop.sh:867-869` records BitLesson enforcement fields in `state.md`. The default template is `templates/bitlesson.md:1-23`, which defines the strict lesson-entry schema.
- edge_cases_or_failure_modes: A symlink to a regular file passes the `-f` check, so the script rejects directories and special files but does not independently reject regular-file symlinks. The relpath guard handles absolute paths and literal `..` path components, but not all possible filesystem aliasing through existing parent symlinks. The script creates parent directories before copying and will fail naturally if the project export is read-only.
- validation_or_tests: No dedicated direct test for `bitlesson-init.sh` was found in the assigned test files. It is exercised indirectly by RLCR setup flows after validation reaches `scripts/setup-rlcr-loop.sh:831-834`; the robustness suite validates many setup paths but does not assert BitLesson file creation directly.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-078 `file` `tests/test-config-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for four-layer JSON config merge behavior implemented by `scripts/lib/config-loader.sh`.
- algorithmic_behavior: The test sources `scripts/lib/config-loader.sh` at `tests/test-config-merge.sh:20-33`, then creates isolated temp project/user config directories and calls `load_merged_config` plus `get_config_value`. It verifies default-only values at `tests/test-config-merge.sh:39-64`, project override at `70-89`, project-over-user priority at `95-109`, additive user preservation at `115-132`, null stripping at `138-150`, `HUMANIZE_CONFIG` path override at `156-173`, and additive contribution from all layers at `179-198`.
- inputs_outputs_state: Inputs are generated JSON config files under temp project and XDG config roots, plus the plugin default config. Outputs are PASS/FAIL messages through `test-helpers.sh` and a final `print_test_summary` at `tests/test-config-merge.sh:200`. State is limited to temp directories from `setup_test_dir`; the repository is not modified by the test.
- gates_or_invariants: The required invariant is default config, then user config, then project config, with higher layers winning only for non-null keys. `scripts/lib/config-loader.sh:77-88` resolves default/user/project paths, including `HUMANIZE_CONFIG`; `scripts/lib/config-loader.sh:109-131` prepares the layers and merges them with recursive `strip_nulls`; `scripts/lib/config-loader.sh:139-158` normalizes retrieved config values for shell assertions.
- dependencies_and_callers: Depends on `jq` through `_config_loader_require_jq` at `scripts/lib/config-loader.sh:17-21`, `tests/test-helpers.sh`, and `config/default_config.json` values such as `bitlesson_model`. The merge loader is consumed by shared loop configuration in `hooks/lib/loop-common.sh:178-189` and BitLesson selection in `scripts/bitlesson-select.sh:15-16`. The suite is registered in `tests/run-all-tests.sh:83`.
- edge_cases_or_failure_modes: This test covers null-stripping and layer priority but does not cover malformed JSON, missing default config, non-object JSON, nested object merging beyond the configured keys, or array null stripping; those appear to be handled by separate config error tests. It assumes `jq` is installed and available.
- validation_or_tests: The file is itself the validation artifact. It is a core executable spec because config values drive model defaults, BitLesson model choice, agent-team behavior, and loop setup defaults.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-108 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for the Stop Hook todo/task completion gate implemented by `hooks/check-todos-from-transcript.py`.
- algorithmic_behavior: The test validates input handling at `tests/test-todo-checker.sh:52-99`, legacy `TodoWrite` detection at `107-158`, transcript format variations at `167-240`, edge cases at `250-294`, and the newer file-backed Task system at `296-455`. It expects invalid hook JSON to exit `2`, incomplete work to exit `1`, and no tasks/all-complete states to exit `0`.
- inputs_outputs_state: Inputs are hook JSON on stdin, generated transcript JSONL files, generated task JSON files under a mock `tasks_base_dir`, and optional `session_id`. Outputs are process exit code and stdout content, including incomplete task details. Test state is confined to `mktemp -d` and cleaned by the trap at `tests/test-todo-checker.sh:37-39`.
- gates_or_invariants: Legacy transcript mode uses the latest `TodoWrite` state, treats any status other than `completed` as incomplete, ignores invalid JSONL lines, and handles assistant/message/direct tool-use formats. File-backed Task mode treats task files as authoritative, defaults missing task status to `pending`, ignores `deleted`, blocks `in_progress` and `pending`, includes task IDs in output, and combines file-backed tasks with legacy todos.
- dependencies_and_callers: The checker implementation extracts tool calls at `hooks/check-todos-from-transcript.py:23-55`, parses latest legacy todos at `58-101`, reads task files at `104-147`, and formats blocking output with `INCOMPLETE_TODOS` at `181-196`. `hooks/loop-codex-stop-hook.sh:387` references this checker as a Stop Hook gate. The suite is registered in `tests/run-all-tests.sh:63`.
- edge_cases_or_failure_modes: Empty input, missing `transcript_path`, non-existent transcript, empty transcript, and non-existent task session all allow progress. Malformed task files are skipped by the checker at `hooks/check-todos-from-transcript.py:143-145`, which avoids hard failure but can hide corrupted task state. Mixed legacy and file-backed modes aggregate incomplete items from both sources.
- validation_or_tests: The file is the validation artifact for todo/task completion semantics. It is core because it determines whether an agent may stop or must continue resolving outstanding tracked work.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-138 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Guard-message template for blocking direct modifications to PR-loop `state.md`.
- algorithmic_behavior: The template states that `.humanize/pr-loop/` `state.md` is system-managed and lists protected state contents: current round, PR number and branch, active bots, Codex configuration, and polling settings at `prompt-template/block/pr-loop-state-modification.md:1-12`. It is rendered as the standard denial message when validators detect state-file writes.
- inputs_outputs_state: Input is an attempted Write/Edit/Bash operation targeting PR-loop state. Output is a rendered markdown error message to stderr. The intended state transition is no transition: the attempted mutation is blocked before the state file changes.
- gates_or_invariants: `hooks/lib/loop-common.sh:1082-1089` loads this template through `pr_loop_state_blocked_message`. Write validation blocks state writes with exit `2` at `hooks/loop-write-validator.sh:79-83`. Edit validation blocks state edits with exit `2` at `hooks/loop-edit-validator.sh:62-66`. Bash validation blocks both full PR-loop state paths and bare `state.md` relative-path attempts at `hooks/loop-bash-validator.sh:537-548`.
- dependencies_and_callers: Depends on the shared template loader and `TEMPLATE_DIR` resolution in loop-common. It coordinates with PR-loop path classifiers such as `is_in_pr_loop_dir`, `is_state_file_path`, and Bash command modification detection.
- edge_cases_or_failure_modes: The Bash validator includes a broad bare `state.md` check while an active PR loop exists, which intentionally catches relative-path bypasses but can also block benign commands mentioning `state.md` if they are classified as modifying commands. If the template cannot be loaded, loop-common falls back to a shorter embedded message.
- validation_or_tests: PR-loop hook suites are registered in `tests/run-all-tests.sh:86-89` and include state-file protection scenarios. The block is core because it preserves PR-loop state authority and prevents agent-side corruption of round, bot, and polling metadata.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-168 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex regular-review prompt template for RLCR review rounds. It defines the acceptance gate that decides whether implementation work is complete or must continue.
- algorithmic_behavior: The prompt forces Codex to read the original plan first at `prompt-template/codex/regular-review.md:5-11`, perform a critical implementation review at `20-31`, run a mandatory goal-alignment check against the goal tracker at `33-45`, include the goal-tracker update section at `47`, and write review output to the configured result file at `49-57`. It explicitly treats deferred and pending work as incomplete at `24-31` and `53-56`.
- inputs_outputs_state: Inputs are template variables including `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`. Output is a review prompt written for Codex. State transitions occur through Codex’s review result and goal-tracker updates: only a final-line `COMPLETE` is allowed to stop the implementing agent, and only when all original plan tasks and acceptance criteria are fully satisfied.
- gates_or_invariants: The main invariant is no deferral-based completion. Any mismatch between claimed work and actual implementation, any pending task, any unjustified deferral, or any ignored acceptance criterion must produce review comments rather than terminal completion. The goal-alignment summary format is mandatory at `prompt-template/codex/regular-review.md:42-45`.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh:961-975` renders this template for regular review prompts when full-alignment mode is not selected; `hooks/loop-codex-stop-hook.sh:946-959` routes to a separate full-alignment template for that mode. The embedded fallback at `hooks/loop-codex-stop-hook.sh:935-944` is much weaker than this template, so successful template loading materially affects review strictness.
- edge_cases_or_failure_modes: The template relies on Codex actually reading linked files and honoring the “COMPLETE last line” rule. If `GOAL_TRACKER_UPDATE_SECTION` is missing or weak, the review still demands a goal-alignment check but may not update tracking consistently. If full-alignment mode is enabled, this file is bypassed.
- validation_or_tests: Template and Codex-review suites are registered in `tests/run-all-tests.sh:65` and `tests/run-all-tests.sh:74`. This file is core because it defines the review-loop completion gate rather than merely formatting prose.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-198 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Large executable robustness specification for setup-time safety gates in `setup-rlcr-loop.sh` and `setup-pr-loop.sh`.
- algorithmic_behavior: The file declares its coverage at `tests/robustness/test-setup-scripts-robustness.sh:3-10`, defines temp repo helpers and isolated runners at `31-87`, then tests setup behavior across argument parsing, plan validation, git prerequisites, YAML safety, loop mutual exclusion, symlink protection, positive dependency progression, timeout behavior, full-review-round bounds, skip-implementation mode, and dependency reporting.
- inputs_outputs_state: Inputs are generated git repositories, generated plan files, `.gitignore` state, fake active `.humanize/rlcr` and `.humanize/pr-loop` directories, and mocked `codex`, `gh`, `jq`, `timeout`, and `gtimeout` commands on PATH. Outputs are PASS/FAIL/skip messages and the final `print_test_summary` at `tests/robustness/test-setup-scripts-robustness.sh:1211-1216`. State transitions under test include creating RLCR state, `.review-phase-started`, goal tracker files, placeholder skip-impl plans, and refusing setup when active loops already exist.
- gates_or_invariants: RLCR argument gates cover help, missing plan, invalid `--max`, invalid timeout, missing model argument, unknown option, and duplicate plan specification at `96-214`. Plan gates reject comment-only, too-short, spaced, metacharacter, and absolute paths at `224-341`. YAML gates reject unsafe branch/model values at `351-402`. Git gates reject non-repos, repos without commits, and tracked plans without `--track-plan-file` at `412-460`. PR-loop gates reject missing bot flags, unknown options, invalid `--max`, and non-git roots at `473-525`. Mutual exclusion blocks overlapping RLCR/PR loops at `535-595`. Symlink protection covers both direct plan symlinks and symlinked parent directories at `605-658`.
- dependencies_and_callers: Exercises `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, plus `scripts/portable-timeout.sh` loaded at `tests/robustness/test-setup-scripts-robustness.sh:18`. Related implementation points include `scripts/setup-rlcr-loop.sh:221-237` for `--full-review-round` and `--skip-impl`, `scripts/setup-rlcr-loop.sh:441-483` for metacharacter and symlink path rejection, `scripts/setup-rlcr-loop.sh:557-576` for tracked-plan policy, `scripts/setup-rlcr-loop.sh:661-769` for YAML-unsafe branch checks, and `scripts/setup-pr-loop.sh:180-190` plus `243-277` for bot and prerequisite gates. Registered in `tests/run-all-tests.sh:113`.
- edge_cases_or_failure_modes: Several scenarios are environment-sensitive and can pass as skipped or alternative pass paths when branch names with colons cannot be created, symlinks are unavailable, or real `codex`/`jq` binaries are installed. The suite intentionally accepts timeout `0` as current behavior at `749-765` despite the user-facing “positive integer” wording. Many tests reuse `EXIT_CODE` with `EXIT_CODE=${EXIT_CODE:-0}` rather than unsetting before every command, so a successful command after an earlier failure can inherit stale exit state in some assertions.
- validation_or_tests: The file is itself the validation artifact. Later groups validate positive progression to dependency/auth checks at `668-739`, timeout simulations at `749-866`, full-review-round bounds at `876-965`, skip-impl state/marker/goal-tracker behavior at `975-1075`, and dependency reporting for missing/present `codex` and `jq` at `1085-1209`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item headings in Item Evidence; each assigned row represented once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`