# agent_05 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: User-facing specification layer for Humanize/RLCR workflows, installation targets, monitoring behavior, configuration precedence, model routing, PR loop lifecycle, and BitLesson memory contracts. The directory is small but algorithmically relevant because it describes command semantics that scripts and hooks implement.
- algorithmic_behavior: `docs/usage.md` defines the RLCR two-phase loop: implementation summary review until `COMPLETE`, then `codex review --base <branch>` with `[P0-9]` issue markers, continuing until acceptance or no issues remain (`docs/usage.md:7`, `docs/usage.md:9`, `docs/usage.md:10`, `docs/usage.md:12`). It also defines Plan Understanding Quiz behavior as advisory, not blocking (`docs/usage.md:28`, `docs/usage.md:33`, `docs/usage.md:35`), command options for `start-rlcr-loop`, including base branch priority `user input > remote default > main > master` (`docs/usage.md:70`, `docs/usage.md:71`), PR loop state machine steps (`docs/usage.md:132` through `docs/usage.md:140`), and config precedence (`docs/usage.md:169` through `docs/usage.md:173`). `docs/bitlesson.md` defines BitLesson selection and delta validation, covered separately in HZ-035. Install docs define runtime dependency surfaces copied into skill/plugin locations: Codex copies `scripts/`, `hooks/`, and `prompt-template/` into `${CODEX_HOME:-~/.codex}/skills/humanize` (`docs/install-for-codex.md:41` through `docs/install-for-codex.md:52`); Kimi copies the same runtime bundle plus `SKILL.md` hydration (`docs/install-for-kimi.md:43` through `docs/install-for-kimi.md:63`); Claude plugin install exposes `/humanize:start-rlcr-loop`, `/humanize:gen-plan`, and `/humanize:ask-codex` (`docs/install-for-claude.md:47` through `docs/install-for-claude.md:55`). `docs/images/rlcr-workflow.svg` is a behavior diagram of Plan -> Implement -> Review Summary -> Code Review -> Done with feedback loops on incomplete summary/code review issues (`docs/images/rlcr-workflow.svg:4` through `docs/images/rlcr-workflow.svg:17`, `docs/images/rlcr-workflow.svg:71` through `docs/images/rlcr-workflow.svg:75`). `docs/images/monitor.png` is a static monitor screenshot asset, not executable logic.
- inputs_outputs_state: Inputs described by docs include plan files, drafts, CLI options, `.humanize/config.json`, user config, plugin defaults, PR association, GitHub bot reviews, and Codex/Claude/Kimi runtime availability. Outputs/state include `.humanize/rlcr/<timestamp>/` progress data (`docs/usage.md:231`), `.humanize/skill/<timestamp>/` ask-codex artifacts (`docs/usage.md:164`, `docs/usage.md:165`), installed skill directories (`docs/install-for-codex.md:36` through `docs/install-for-codex.md:52`; `docs/install-for-kimi.md:25` through `docs/install-for-kimi.md:28`), and monitor commands for RLCR/PR loops (`docs/usage.md:224` through `docs/usage.md:228`).
- gates_or_invariants: Config hierarchy is invariant across docs and implementation: defaults, user config, project config, CLI flags (`docs/usage.md:169` through `docs/usage.md:173`). RLCR max/review/full-review/base-branch options constrain loop transitions (`docs/usage.md:61` through `docs/usage.md:85`). Security gate documentation for `HUMANIZE_CODEX_BYPASS_SANDBOX` makes unsafe Codex bypass explicit and default-off (`docs/usage.md:240` through `docs/usage.md:249`, `docs/usage.md:255` through `docs/usage.md:264`). Installation docs require `codex`, `jq`, and `git` for Claude use (`docs/install-for-claude.md:3` through `docs/install-for-claude.md:7`).
- dependencies_and_callers: Docs coordinate with root README, runtime scripts, and hooks. `docs/usage.md` references commands implemented under `commands/` and `scripts/`; monitor docs map to `scripts/humanize.sh`; base-branch docs map to `scripts/setup-rlcr-loop.sh:711` through `scripts/setup-rlcr-loop.sh:783`; BitLesson docs map to `scripts/bitlesson-init.sh`, `scripts/bitlesson-select.sh`, and `scripts/bitlesson-validate-delta.sh`. Install docs coordinate with `scripts/install-skill.sh`, `scripts/install-skills-codex.sh`, and `scripts/install-skills-kimi.sh` by naming copied runtime folders.
- edge_cases_or_failure_modes: Docs identify missing prerequisites, unsupported sandbox environments, missing scripts after install (`docs/install-for-codex.md:70` through `docs/install-for-codex.md:76`; `docs/install-for-kimi.md:194` through `docs/install-for-kimi.md:210`), unwritable output/cache directories (`docs/install-for-kimi.md:230` through `docs/install-for-kimi.md:236`), missing active loop monitor directory (`docs/usage.md:224` through `docs/usage.md:231`), and dangerous bypass mode risks (`docs/usage.md:251` through `docs/usage.md:264`).
- validation_or_tests: Directory contents are indirectly validated by template/reference and behavior tests. Monitor documentation is supported by `tests/manual-monitor-test.sh`, `tests/test-monitor-runtime.sh`, and E2E monitor tests. Base-branch docs are supported by `tests/robustness/test-base-branch-detection.sh`. BitLesson docs are supported by selector routing and delta validation tests. Static inspection only; I did not run tests because this branch export is read-only and tests create temporary/project state.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-035 `file` `docs/bitlesson.md`
- cursor: `[_]`
- core_role: Behavior-defining documentation for the BitLesson knowledge capture subsystem used during RLCR implementation rounds.
- algorithmic_behavior: Defines `.humanize/bitlesson.md` as the per-project knowledge base (`docs/bitlesson.md:23`). At loop start, the file is initialized from `templates/bitlesson.md` when missing (`docs/bitlesson.md:25` through `docs/bitlesson.md:28`), each task/sub-task runs through `scripts/bitlesson-select.sh` (`docs/bitlesson.md:28`), selected IDs or `NONE` are applied during work (`docs/bitlesson.md:29`), and the stop gate validates a `## BitLesson Delta` section in each round summary (`docs/bitlesson.md:30`). The implementation matches this: setup initializes via `scripts/bitlesson-init.sh` (`scripts/setup-rlcr-loop.sh:828` through `scripts/setup-rlcr-loop.sh:834`), writes BitLesson state fields to `state.md` (`scripts/setup-rlcr-loop.sh:867` through `scripts/setup-rlcr-loop.sh:869`), adds a required selector section to the round prompt (`scripts/setup-rlcr-loop.sh:1139` through `scripts/setup-rlcr-loop.sh:1152`), and stop hook runs the validator (`hooks/loop-codex-stop-hook.sh:746` through `hooks/loop-codex-stop-hook.sh:757`).
- inputs_outputs_state: Inputs are merged config key `bitlesson_model` from defaults, user config, project config, and CLI where applicable (`docs/bitlesson.md:7` through `docs/bitlesson.md:12`), task description, related paths, and `.humanize/bitlesson.md`. Outputs are selector lines `LESSON_IDS` and `RATIONALE` from `scripts/bitlesson-select.sh` (`scripts/bitlesson-select.sh:31` through `scripts/bitlesson-select.sh:34`, `scripts/bitlesson-select.sh:221`, `scripts/bitlesson-select.sh:222`) and a summary `## BitLesson Delta` block (`docs/bitlesson.md:34` through `docs/bitlesson.md:41`). State is persisted in `.humanize/bitlesson.md` and RLCR `state.md` flags for required validation and empty-none behavior.
- gates_or_invariants: Provider routing is documented as `gpt-*` and `o[N]-*` to Codex, `claude-*`, `haiku`, `sonnet`, `opus` to Claude (`docs/bitlesson.md:14` through `docs/bitlesson.md:18`), with fallback to the default Codex model if the configured provider binary is missing (`docs/bitlesson.md:19`). The selector enforces non-empty `--task`, `--paths`, and `--bitlesson-file` (`scripts/bitlesson-select.sh:67` through `scripts/bitlesson-select.sh:83`), non-empty BitLesson file content (`scripts/bitlesson-select.sh:102` through `scripts/bitlesson-select.sh:110`), 120-second timeout (`scripts/bitlesson-select.sh:165`, `scripts/bitlesson-select.sh:183` through `scripts/bitlesson-select.sh:185`), and exact two-line output format (`scripts/bitlesson-select.sh:193` through `scripts/bitlesson-select.sh:222`). Delta validation requires exactly one action in `none|add|update` (`scripts/bitlesson-validate-delta.sh:118` through `scripts/bitlesson-validate-delta.sh:133`), `Action: none` with `NONE` or empty IDs (`docs/bitlesson.md:45`; `scripts/bitlesson-validate-delta.sh:158` through `scripts/bitlesson-validate-delta.sh:168`), and concrete existing `BL-YYYYMMDD-short-name` IDs for add/update (`docs/bitlesson.md:46`; `scripts/bitlesson-validate-delta.sh:185` through `scripts/bitlesson-validate-delta.sh:282`).
- dependencies_and_callers: Depends on `config/default_config.json` for `bitlesson_model`, `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `scripts/portable-timeout.sh`, Codex or Claude CLIs, `templates/bitlesson.md`, prompt templates under `prompt-template/block/bitlesson-delta-*`, and `hooks/lib/template-loader.sh`. Called from `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and task instructions in `prompt-template/claude/agent-teams-core.md` and next-round prompts.
- edge_cases_or_failure_modes: Missing or empty knowledge base blocks selector execution; missing provider binary falls back to Codex if possible (`scripts/bitlesson-select.sh:95` through `scripts/bitlesson-select.sh:100`); selector timeout exits 124; unexpected model output fails format parsing; missing summary delta returns a hook block decision (`scripts/bitlesson-validate-delta.sh:92` through `scripts/bitlesson-validate-delta.sh:108`); empty knowledge base can block `Action: none` when `--require-bitlesson-entry-for-none` makes empty-none false (`docs/bitlesson.md:47`; `scripts/bitlesson-validate-delta.sh:170` through `scripts/bitlesson-validate-delta.sh:182`); add/update fails when IDs are malformed or absent from `.humanize/bitlesson.md`.
- validation_or_tests: Selector routing is covered by `tests/test-bitlesson-select-routing.sh`; config precedence by `tests/test-config-merge.sh` and `tests/test-config-error-handling.sh`; stop gate delta paths by tests referencing BitLesson templates and `scripts/bitlesson-validate-delta.sh`. Static inspection only in this worker run.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-065 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor graceful-stop behavior when `.humanize/` is deleted during active `humanize monitor rlcr`.
- algorithmic_behavior: Defines three commands: `setup` creates a minimal RLCR session under `.humanize/rlcr/2026-01-16_99-99-99` with `state.md` and `goal-tracker.md` (`tests/manual-monitor-test.sh:23` through `tests/manual-monitor-test.sh:46`); `delete` removes `.humanize` to trigger monitor shutdown (`tests/manual-monitor-test.sh:47` through `tests/manual-monitor-test.sh:57`); `cleanup` removes leftover `.humanize` (`tests/manual-monitor-test.sh:58` through `tests/manual-monitor-test.sh:62`). The expected observed transition is that a running monitor exits cleanly with “Monitoring stopped: .humanize/rlcr directory no longer exists” and restored terminal state (`tests/manual-monitor-test.sh:51` through `tests/manual-monitor-test.sh:54`).
- inputs_outputs_state: Input is one positional command: `setup`, `delete`, or `cleanup` (`tests/manual-monitor-test.sh:23`, `tests/manual-monitor-test.sh:64`). State created during setup includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` in `state.md` (`tests/manual-monitor-test.sh:27` through `tests/manual-monitor-test.sh:32`) plus minimal Goal Tracker immutable sections (`tests/manual-monitor-test.sh:33` through `tests/manual-monitor-test.sh:38`). Output is human-readable setup/delete/cleanup guidance; no machine-readable pass/fail is emitted.
- gates_or_invariants: Runs with `set -euo pipefail` (`tests/manual-monitor-test.sh:17`) and changes to project root before acting (`tests/manual-monitor-test.sh:19` through `tests/manual-monitor-test.sh:21`). The monitor implementation checks `.humanize/rlcr` existence before start (`scripts/humanize.sh:238` through `scripts/humanize.sh:243`) and repeatedly during active monitoring, no-log wait, and incremental log loops (`scripts/humanize.sh:777` through `scripts/humanize.sh:783`, `scripts/humanize.sh:839` through `scripts/humanize.sh:843`, `scripts/humanize.sh:950` through `scripts/humanize.sh:953`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` and specifically `_humanize_monitor_codex`; monitor setup/restoration uses terminal control sequences and `tput` (`scripts/humanize.sh:590` through `scripts/humanize.sh:651`). `_graceful_stop` calls `_cleanup`, which kills tail processes, resets the terminal, and prints the stop reason (`scripts/humanize.sh:675` through `scripts/humanize.sh:715`).
- edge_cases_or_failure_modes: If the monitor is not running in another terminal, `delete` simply removes `.humanize` and only prints what to check. The manual spec also guards against zsh/bash glob errors by requiring no “no matches found” errors (`tests/manual-monitor-test.sh:52` through `tests/manual-monitor-test.sh:54`). Cleanup/delete are destructive to local `.humanize`, so this test is intentionally manual and not safe to run in the read-only branch export.
- validation_or_tests: Automated analogs exist in `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-monitor-e2e-sigint.sh`, `tests/test-zsh-monitor-safety.sh`, and robustness hook-input monitor tests. This worker did not execute it because it modifies `.humanize`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-095 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared PR-loop test harness that normalizes setup, mock external dependencies, and summary behavior across PR-loop script/hook/system tests.
- algorithmic_behavior: Uses an idempotent guard `TEST_PR_LOOP_LIB_LOADED` to avoid double definitions (`tests/test-pr-loop-lib.sh:10` through `tests/test-pr-loop-lib.sh:13`). Resolves `SCRIPT_DIR` and `PROJECT_ROOT` defaults and sources `tests/test-helpers.sh` if `setup_test_dir` is not already defined (`tests/test-pr-loop-lib.sh:14` through `tests/test-pr-loop-lib.sh:21`). `create_mock_gh` writes a mock `gh` executable supporting `auth status`, `repo view --json owner|name`, `pr view` cases for commits/number/state, and `api user`/empty arrays (`tests/test-pr-loop-lib.sh:27` through `tests/test-pr-loop-lib.sh:88`). `create_mock_codex` writes a mock Codex CLI that prints “Mock codex output” and exits 0 (`tests/test-pr-loop-lib.sh:90` through `tests/test-pr-loop-lib.sh:101`). `init_pr_loop_test_env` creates a temp test dir, prepends mock bin to `PATH`, installs mocks, and exports `MOCK_BIN_DIR` (`tests/test-pr-loop-lib.sh:107` through `tests/test-pr-loop-lib.sh:121`). `print_test_summary` reports pass/fail counts and returns nonzero when failures exist (`tests/test-pr-loop-lib.sh:127` through `tests/test-pr-loop-lib.sh:144`).
- inputs_outputs_state: Inputs are target mock directory arguments for `create_mock_gh` and `create_mock_codex`, inherited test helper counters, and caller-provided environment. Outputs are executable files under `$TEST_DIR/mock_bin`, modified `PATH`, exported `MOCK_BIN_DIR`, and terminal summary text. State uses shared `TESTS_PASSED`/`TESTS_FAILED` counters from `tests/test-helpers.sh`.
- gates_or_invariants: Mock `gh` intentionally returns deterministic PR metadata: owner `testowner`, repo `testrepo`, PR number `123`, PR state `OPEN`, commit timestamp `2026-01-18T12:00:00Z`, and empty API arrays unless otherwise handled (`tests/test-pr-loop-lib.sh:36` through `tests/test-pr-loop-lib.sh:80`). Unknown `gh` invocations fail with exit 1 (`tests/test-pr-loop-lib.sh:84`, `tests/test-pr-loop-lib.sh:85`), making unsupported API calls visible in tests. The library assumes `tests/test-helpers.sh` semantics for `setup_test_dir`, `pass`, and `fail` (`tests/test-helpers.sh:30` through `tests/test-helpers.sh:89`).
- dependencies_and_callers: Direct callers include `tests/test-pr-loop.sh`, `tests/test-pr-loop-1-scripts.sh`, `tests/test-pr-loop-2-hooks.sh`, and `tests/test-pr-loop-3-stophook.sh`, each sourcing the library and calling `init_pr_loop_test_env`. It supports scripts using GitHub CLI and Codex in PR loop setup/stop hooks without making network calls.
- edge_cases_or_failure_modes: Because the mock `gh` uses positional and substring matching, changed command shapes can fall into “unhandled command” and fail fast. `create_mock_codex` does not simulate review issue formats, timeouts, or stderr, so tests using this library validate integration plumbing more than Codex content semantics. Re-sourcing is guarded, but function definitions are global after first load.
- validation_or_tests: The library is itself a support asset for PR-loop tests rather than an independent test case. Its behavior is indirectly validated whenever PR-loop tests pass. Static inspection only here.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-125 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block prompt template for the “dirty Git working tree” gate before an RLCR loop can exit or proceed to expensive review.
- algorithmic_behavior: The template renders a blocker explaining that the agent is trying to stop with `{{GIT_ISSUES}}`, inserts `{{SPECIAL_NOTES}}`, then requires review of untracked files, staging real changes, and committing with a descriptive message (`prompt-template/block/git-not-clean.md:1` through `prompt-template/block/git-not-clean.md:10`). It also adds an optional simplification step if a `code-simplifier` plugin is installed (`prompt-template/block/git-not-clean.md:6`) and commit authorship rules forbidding AI attribution (`prompt-template/block/git-not-clean.md:11` through `prompt-template/block/git-not-clean.md:14`).
- inputs_outputs_state: Inputs are template variables `GIT_ISSUES` and `SPECIAL_NOTES`. The stop hook sets `GIT_ISSUES="uncommitted changes"` when cached Git status, excluding untracked `.humanize/`, is non-empty (`hooks/loop-codex-stop-hook.sh:613` through `hooks/loop-codex-stop-hook.sh:623`). It appends special notes for `.humanize*` untracked state and other untracked files (`hooks/loop-codex-stop-hook.sh:625` through `hooks/loop-codex-stop-hook.sh:645`). Output is a JSON hook decision with `"decision": "block"`, rendered reason, and system message (`hooks/loop-codex-stop-hook.sh:659` through `hooks/loop-codex-stop-hook.sh:669`).
- gates_or_invariants: `.humanize/` untracked runtime state is excluded from dirty determination but can still contribute explanatory notes (`hooks/loop-codex-stop-hook.sh:618` through `hooks/loop-codex-stop-hook.sh:621`, `hooks/loop-codex-stop-hook.sh:628` through `hooks/loop-codex-stop-hook.sh:635`). Template rendering uses `load_and_render_safe`, so missing template falls back to a minimal Git Not Clean prompt (`hooks/loop-codex-stop-hook.sh:653` through `hooks/loop-codex-stop-hook.sh:661`). Rendering is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:71` through `hooks/lib/template-loader.sh:131`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh`; loaded via `hooks/lib/template-loader.sh`. Related sibling templates are `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md`, inserted into `SPECIAL_NOTES`.
- edge_cases_or_failure_modes: If Git is not available or the project is not a repo, this specific gate is skipped because it is inside `GIT_IS_REPO == true` (`hooks/loop-codex-stop-hook.sh:614`). If untracked `.humanize` is the only dirty state, the gate should not block due to filtering. If the template is missing or empty, fallback text is used. The template itself instructs `git add -A` only “if all files should be tracked”, preserving review of artifacts before staging.
- validation_or_tests: Template presence and safe rendering are covered by `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`; dirty Git/finalize scenarios are exercised indirectly in stop-hook/finalize tests. Static inspection only here.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-155 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase transition prompt emitted after Codex review passes, defining the allowed final refactor/simplification behavior and required completion outputs.
- algorithmic_behavior: Declares implementation complete and all acceptance criteria met (`prompt-template/claude/finalize-phase-prompt.md:3`), then directs Claude into Finalize Phase for simplification/refactoring (`prompt-template/claude/finalize-phase-prompt.md:5`). It requires use of the `code-simplifier:code-simplifier` agent through Task tool (`prompt-template/claude/finalize-phase-prompt.md:7` through `prompt-template/claude/finalize-phase-prompt.md:14`). It constrains work to functionality-equivalent cleanup: no functionality changes, no test failures, no new bugs, simplification only (`prompt-template/claude/finalize-phase-prompt.md:16` through `prompt-template/claude/finalize-phase-prompt.md:24`). Focus is recent code and diffs from `{{BASE_BRANCH}}` to `{{START_BRANCH}}` (`prompt-template/claude/finalize-phase-prompt.md:25` through `prompt-template/claude/finalize-phase-prompt.md:35`). Before exit, todos must be completed, changes committed, and summary written to `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:41` through `prompt-template/claude/finalize-phase-prompt.md:50`).
- inputs_outputs_state: Inputs are placeholders `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Stop hook renders it when code review passes and review was not skipped (`hooks/loop-codex-stop-hook.sh:1220` through `hooks/loop-codex-stop-hook.sh:1245`). Output is a hook JSON block/reason prompt that tells the agent what to do next (`hooks/loop-codex-stop-hook.sh:1248` through `hooks/loop-codex-stop-hook.sh:1250` and surrounding finalize handling). State transition is from review-complete into finalize phase, with `finalize_summary_file` becoming a required artifact.
- gates_or_invariants: The prompt explicitly forbids behavior changes and test regressions (`prompt-template/claude/finalize-phase-prompt.md:18` through `prompt-template/claude/finalize-phase-prompt.md:24`). It preserves source-of-truth references to plan and goal tracker (`prompt-template/claude/finalize-phase-prompt.md:36` through `prompt-template/claude/finalize-phase-prompt.md:39`). Rendering uses `load_and_render_safe`; if missing, fallback still includes the same branch-range and final summary requirements (`hooks/loop-codex-stop-hook.sh:1220` through `hooks/loop-codex-stop-hook.sh:1245`).
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, `hooks/lib/template-loader.sh`, Codex review phase state, and the availability of a code-simplifier agent if installed. Related skipped-review variant is `prompt-template/claude/finalize-phase-skipped-prompt.md`, rendered when review is skipped (`hooks/loop-codex-stop-hook.sh:1188` through `hooks/loop-codex-stop-hook.sh:1218`).
- edge_cases_or_failure_modes: If code review is skipped, this template is not used; the skipped variant weakens simplification to optional and adds manual verification warnings. If the code-simplifier agent is unavailable, the prompt still names it as required, so agent runtime capability affects feasibility. If placeholders are missing from render inputs, single-pass renderer keeps unresolved placeholders rather than failing (`hooks/lib/template-loader.sh:116` through `hooks/lib/template-loader.sh:122`).
- validation_or_tests: `tests/test-finalize-phase.sh` checks finalize behavior and that the prompt mentions code-simplifier; template load/render coverage comes from `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`. Static inspection only here.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-185 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for base branch auto-detection used by `start-rlcr-loop`.
- algorithmic_behavior: Defines a local `detect_base_branch(project_root, git_timeout)` helper mirroring setup logic at the time of test writing: try `git remote show origin | grep "HEAD branch:"`, reject empty/`(unknown)`, then fallback to local `main`, then local `master`, else return 1 (`tests/robustness/test-base-branch-detection.sh:35` through `tests/robustness/test-base-branch-detection.sh:64`). It creates isolated git repos to test: no origin with local main returns `main` (`tests/robustness/test-base-branch-detection.sh:70` through `tests/robustness/test-base-branch-detection.sh:93`), no origin with only master returns `master` (`tests/robustness/test-base-branch-detection.sh:99` through `tests/robustness/test-base-branch-detection.sh:122`), no origin and only develop fails (`tests/robustness/test-base-branch-detection.sh:128` through `tests/robustness/test-base-branch-detection.sh:149`), and a simulated origin/default case returns `main` or an acceptable detected branch (`tests/robustness/test-base-branch-detection.sh:155` through `tests/robustness/test-base-branch-detection.sh:191`).
- inputs_outputs_state: Inputs are temporary git repositories, optional origin remote, local branch names, and timeout seconds. Outputs are detected branch names on stdout or failure status. Test state is isolated in `TEST_DIR` from `setup_test_dir` (`tests/robustness/test-base-branch-detection.sh:24`) and summarized by `print_test_summary` (`tests/robustness/test-base-branch-detection.sh:197`, `tests/robustness/test-base-branch-detection.sh:198`).
- gates_or_invariants: Uses `set -euo pipefail` (`tests/robustness/test-base-branch-detection.sh:14`) and `run_with_timeout` from `scripts/portable-timeout.sh` for git calls (`tests/robustness/test-base-branch-detection.sh:19`, `tests/robustness/test-base-branch-detection.sh:36`). The helper guards the remote-default pipeline with `|| true` so missing origin does not abort under pipefail (`tests/robustness/test-base-branch-detection.sh:41` through `tests/robustness/test-base-branch-detection.sh:44`). Current production setup extends the tested concept: explicit `--base-branch` validates a local branch and diagnoses remote-only branches (`scripts/setup-rlcr-loop.sh:716` through `scripts/setup-rlcr-loop.sh:733`); auto-detected remote defaults are only accepted if they also exist locally (`scripts/setup-rlcr-loop.sh:735` through `scripts/setup-rlcr-loop.sh:745`); then local `main`, local `master`, or hard failure follows (`scripts/setup-rlcr-loop.sh:746` through `scripts/setup-rlcr-loop.sh:764`); YAML-unsafe branch names are rejected (`scripts/setup-rlcr-loop.sh:767` through `scripts/setup-rlcr-loop.sh:773`); and `base_commit` is captured to avoid moving-ref/self-diff problems (`scripts/setup-rlcr-loop.sh:775` through `scripts/setup-rlcr-loop.sh:783`).
- dependencies_and_callers: Depends on git, `tests/test-helpers.sh`, and `scripts/portable-timeout.sh`. It validates behavior documented in `docs/usage.md:70` and `docs/usage.md:71` and implemented in `scripts/setup-rlcr-loop.sh`.
- edge_cases_or_failure_modes: Covers missing origin, missing main/master, remote/default fallback ambiguity, and git command timeouts. It does not directly cover explicit `--base-branch`, remote default existing only remotely, YAML-unsafe names, or `base_commit` capture, which are implemented in the production script but outside this helper’s scenarios. The comment says it replicates setup lines `549-567` (`tests/robustness/test-base-branch-detection.sh:35`), but current production line numbers have moved and logic has become stricter around local remote-default verification.
- validation_or_tests: This is itself a robustness test and is listed in `tests/run-all-tests.sh`. Static inspection only here; running it would create temporary git repositories and commits, which I avoided under the read-only research directive.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7/7 section headers present; each assigned item is represented once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`