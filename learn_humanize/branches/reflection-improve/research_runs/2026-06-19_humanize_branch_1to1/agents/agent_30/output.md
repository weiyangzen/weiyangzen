# agent_30 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-030 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for canceling an active RLCR loop while preserving loop artifacts; it is a user-facing router around `scripts/cancel-rlcr-loop.sh`, not the cancellation implementation itself.
- algorithmic_behavior: The command instructs the agent to run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` and branch only on the first output line: `NO_LOOP`/`NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:11-23`). If finalization is active, it asks the user whether to force cancellation; only a positive answer runs the script with `--force` (`commands/cancel-rlcr-loop.md:24-35`).
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT`, script output status, and possibly explicit user confirmation. Outputs are user-facing cancellation/no-loop messages. State mutation is delegated to the script, which creates `.cancel-requested`, removes pending setup markers, and renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:152-163`).
- gates_or_invariants: The command’s key invariant is that the script owns all cancellation logic, with active loop detection based on `state.md`, `methodology-analysis-state.md`, or `finalize-state.md` in the newest RLCR loop directory (`commands/cancel-rlcr-loop.md:37`). Finalize-phase cancellation is guarded by confirmation unless forced; the script emits `FINALIZE_NEEDS_CONFIRM` and exits before mutation when not forced (`scripts/cancel-rlcr-loop.sh:135-145`).
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-loop.sh`; the command frontmatter allows only that script, the `--force` variant, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-5`). The script depends on `hooks/lib/loop-common.sh` for `find_active_loop` (`scripts/cancel-rlcr-loop.sh:72-87`).
- edge_cases_or_failure_modes: No loop returns `NO_LOOP`; a newest loop directory without an active state returns `NO_ACTIVE_LOOP` (`scripts/cancel-rlcr-loop.sh:89-117`). Methodology Analysis has a distinct cancellation status (`scripts/cancel-rlcr-loop.sh:107-109`, `scripts/cancel-rlcr-loop.sh:172-175`). Finalize without force intentionally returns status code 2 and requires command-level confirmation (`scripts/cancel-rlcr-loop.sh:135-145`).
- validation_or_tests: Cancellation semantics are indirectly covered by state/finalize/cancel robustness tests referenced around finalize-state detection and cancel security, including finalize state handling in `tests/test-finalize-phase.sh:357-414` and active-loop state precedence in `tests/robustness/test-concurrent-state-robustness.sh:203-218`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-060 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for the PR review loop. It converts a current branch/PR plus selected bot reviewers into a `.humanize/pr-loop/<timestamp>/` state machine with initial comments, state, goal tracker, and round-0 prompt.
- algorithmic_behavior: Parses `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout` (`scripts/setup-pr-loop.sh:111-174`), requires at least one bot (`scripts/setup-pr-loop.sh:180-191`), resolves the active PR including fork-parent lookup (`scripts/setup-pr-loop.sh:289-378`), fetches initial comments (`scripts/setup-pr-loop.sh:421-431`), classifies startup case through `check-pr-reviewer-status.sh` (`scripts/setup-pr-loop.sh:437-455`), optionally posts or reuses an all-bot trigger comment for cases 4/5 (`scripts/setup-pr-loop.sh:462-562`), then writes loop state, goal tracker, and round-0 prompt (`scripts/setup-pr-loop.sh:604-851`).
- inputs_outputs_state: Inputs are bot flags, max iteration and Codex settings, `CLAUDE_PROJECT_DIR`/cwd, Git branch/HEAD, GitHub CLI PR metadata, bot comment/reaction APIs, and templates. Outputs are `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, and `round-0-prompt.md` (`scripts/setup-pr-loop.sh:413-431`, `scripts/setup-pr-loop.sh:615-635`, `scripts/setup-pr-loop.sh:641-713`, `scripts/setup-pr-loop.sh:721-851`), plus a setup banner and initial prompt on stdout (`scripts/setup-pr-loop.sh:860-942`).
- gates_or_invariants: Enforces mutual exclusion with active RLCR or PR loops (`scripts/setup-pr-loop.sh:217-241`), requires git repo plus at least one commit (`scripts/setup-pr-loop.sh:243-253`), requires `gh` authenticated and `codex` installed (`scripts/setup-pr-loop.sh:255-277`), rejects merged/closed PRs (`scripts/setup-pr-loop.sh:365-374`), and validates YAML-safe branch/model/effort values before state serialization (`scripts/setup-pr-loop.sh:380-407`). For Claude case 4/5 triggers, it refuses to continue if the trigger comment ID cannot be recovered or if the eyes reaction is missing (`scripts/setup-pr-loop.sh:564-599`).
- dependencies_and_callers: Called by `commands/start-pr-loop.md`, whose allowed tool points at `scripts/setup-pr-loop.sh:*` (`commands/start-pr-loop.md:1-14`). Sources `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:33-43`). Calls `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:430-438`, `scripts/setup-pr-loop.sh:587`), and uses `git`, `gh`, `jq`, and `codex`.
- edge_cases_or_failure_modes: Handles fork PRs by checking parent repo with fork-qualified branch names (`scripts/setup-pr-loop.sh:334-348`). Avoids duplicate trigger spam by scanning comments after the latest commit and stripping code/quote contexts before matching mentions (`scripts/setup-pr-loop.sh:467-503`). On failure to post a trigger or validate Claude reaction, it removes the newly created loop dir to avoid orphan state (`scripts/setup-pr-loop.sh:525-537`, `scripts/setup-pr-loop.sh:580-598`). If no PR is found or PR number is nonnumeric, setup exits before state creation (`scripts/setup-pr-loop.sh:351-363`).
- validation_or_tests: Argument parsing is covered by `tests/test-pr-loop-scripts.sh:27-165`. Case 4/5 missing trigger ID cleanup is covered by `tests/test-pr-loop-system.sh:1384-1447`. Goal tracker creation is covered by `tests/test-pr-loop-system.sh:1453-1535`. Config-backed model behavior and `--codex-model` override are covered by `tests/test-unified-codex-config.sh:590-650` and `tests/test-unified-codex-config.sh:687-790`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-090 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file admission control. It documents and tests the path, repository, tracking, content, branch, CLI, and Codex-parameter gates that must pass before an RLCR loop can start.
- algorithmic_behavior: Creates temporary git repositories, unsets `CLAUDE_PROJECT_DIR` so the setup script uses the temp repo (`tests/test-plan-file-validation.sh:16-21`), installs a mock `codex` binary (`tests/test-plan-file-validation.sh:73-83`), then repeatedly invokes `scripts/setup-rlcr-loop.sh` with crafted plan paths and options, checking exit code plus diagnostic text.
- inputs_outputs_state: Inputs are generated temp plan files, git repo states, branch names, chmod permissions, mocked PATH, and setup script options. Outputs are `PASS`/`FAIL`/`SKIP` counters and process exit equal to failed count (`tests/test-plan-file-validation.sh:742-751`). State is confined to `mktemp -d`, removed by trap (`tests/test-plan-file-validation.sh:36-39`).
- gates_or_invariants: Specifies rejection of absolute paths, missing files/dirs, spaces, shell metacharacters, symlinks, unresolved/inaccessible paths, project escapes, non-git repos, repos without commits, tracked-plan mismatch, modified tracked plans, YAML-unsafe branch names, insufficient plan content, conflicting `--plan-file` and positional args, and unsafe Codex model/effort values (`tests/test-plan-file-validation.sh:88-186`, `tests/test-plan-file-validation.sh:188-323`, `tests/test-plan-file-validation.sh:328-425`, `tests/test-plan-file-validation.sh:431-660`, `tests/test-plan-file-validation.sh:666-739`).
- dependencies_and_callers: Depends directly on `scripts/setup-rlcr-loop.sh`; the tested implementation includes corresponding guards for plan merge, git validation, path safety, tracking, content counting, branch safety, model validation, and effort validation (`scripts/setup-rlcr-loop.sh:371-684`). Also depends on `git`, shell filesystem permissions, and a POSIX-ish environment with Bash.
- edge_cases_or_failure_modes: Some branch names may be rejected by git before the setup script runs; the test treats that as acceptable (`tests/test-plan-file-validation.sh:459-474`, `tests/test-plan-file-validation.sh:476-511`). Permission-denial test is skipped if chmod cannot make the directory inaccessible (`tests/test-plan-file-validation.sh:225-241`). Valid-content acceptance tests only assert absence of the “insufficient content” error, allowing later setup gates to fail for unrelated reasons (`tests/test-plan-file-validation.sh:625-657`). The shell-metacharacter loop records a `fail` then still emits a `pass` after the loop, so the final exit count remains the real failure signal (`tests/test-plan-file-validation.sh:179-186`).
- validation_or_tests: This file is itself the validation artifact. It maps to implementation guards in `scripts/setup-rlcr-loop.sh:427-579` for path/tracking and `scripts/setup-rlcr-loop.sh:590-643` for content, with branch/model/effort serialization guards at `scripts/setup-rlcr-loop.sh:654-684`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-120 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Static prompt block used when a hook blocks modification of `finalize-state.md`, preserving the Finalize Phase state transition boundary.
- algorithmic_behavior: Renders a concise block telling the agent that `finalize-state.md` is system-managed and that Finalize Phase work should focus on running code simplification, committing changes, and writing `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1-8`).
- inputs_outputs_state: No template variables. Input is only the caller choosing this block. Output is the rendered block text. It does not mutate state; it communicates a blocked transition to the agent.
- gates_or_invariants: Enforces the invariant that `finalize-state.md` cannot be manually edited or written. `hooks/lib/loop-common.sh` wraps it in `finalize_state_file_blocked_message` (`hooks/lib/loop-common.sh:647-654`), and validators check finalize-state before generic `state.md` because `finalize-state.md` also ends in `state.md` (`hooks/loop-write-validator.sh:191-207`, `hooks/loop-edit-validator.sh:153-170`).
- dependencies_and_callers: Loaded via `load_and_render_safe` from `hooks/lib/loop-common.sh:647-654`. Called by write/edit validators on finalize-state paths (`hooks/loop-write-validator.sh:200-202`, `hooks/loop-edit-validator.sh:163-165`), and also referenced in the broader bash-validator state-file protection surface.
- edge_cases_or_failure_modes: Because the block is static, it cannot include the active loop path or current round; correctness depends on the caller selecting the right template. Specific-before-generic path checks are required to avoid showing the wrong state-file message for `finalize-state.md`.
- validation_or_tests: Finalize state protection is covered by `tests/test-finalize-phase.sh`, including write, edit, bash redirection, `mv` from, and `cp` from `finalize-state.md` blocking (`tests/test-finalize-phase.sh:357-414`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-150 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Prompt block for RLCR round-number validation failures, especially attempts to edit or write a summary file for a round other than the active `current_round`.
- algorithmic_behavior: Renders `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`, tells the agent the attempted round is wrong, gives the correct path, and explicitly says not to increment the round number manually (`prompt-template/block/wrong-round-number.md:1-7`).
- inputs_outputs_state: Inputs are validator-provided template variables. Output is a blocked-operation message. No state mutation occurs; it protects the state machine’s round cursor by redirecting work to the active round file.
- gates_or_invariants: Used when a summary filename’s embedded round differs from parsed `current_round` and the file is not allowlisted. Write validation constructs `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` and renders this template before exiting 2 (`hooks/loop-write-validator.sh:276-292`). Edit validation mirrors the same gate for summary edits (`hooks/loop-edit-validator.sh:201-224`).
- dependencies_and_callers: Depends on template loading through `load_and_render_safe`; callers depend on active state parsing from `resolve_active_state_file` and `parse_state_file_strict` before comparing round numbers (`hooks/loop-write-validator.sh:176-188`, `hooks/loop-edit-validator.sh:143-151`).
- edge_cases_or_failure_modes: Only summary round files are covered by this validator path; PR-loop `round-*-pr-resolve.md` mismatch uses a separate hardcoded message in `hooks/lib/loop-common.sh:1030-1077`. Allowlisted files bypass wrong-round blocking. Missing or malformed state fails closed before this template can be rendered (`hooks/loop-write-validator.sh:183-187`, `hooks/loop-edit-validator.sh:146-150`).
- validation_or_tests: Template rendering is tested with real variables in `tests/test-template-loader.sh:151-164` and `tests/test-templates-comprehensive.sh:498-510`, verifying title, attempted round, current round, and correct path substitution.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-180 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Sourced monitor library for `humanize monitor skill`; it observes `.humanize/skill` ask-codex invocations and displays aggregate status plus the best live/completed output.
- algorithmic_behavior: `_humanize_monitor_skill` parses `--once`, requires `.humanize/skill`, lists timestamp-like invocation directories newest-first (`scripts/lib/monitor-skill.sh:27-53`), chooses the newest invocation with watchable content or falls back to newest (`scripts/lib/monitor-skill.sh:60-80`), counts statuses from `metadata.md` (`scripts/lib/monitor-skill.sh:82-105`), extracts the first question line from `input.md` (`scripts/lib/monitor-skill.sh:107-114`), resolves global/local cache output files (`scripts/lib/monitor-skill.sh:116-167`), and either prints a one-shot report or runs an interactive status-bar plus `tail -f` loop (`scripts/lib/monitor-skill.sh:268-346`, `scripts/lib/monitor-skill.sh:348-447`).
- inputs_outputs_state: Inputs are cwd, `.humanize/skill/<timestamp...>/input.md`, optional `metadata.md`, optional `output.md`, cache logs under `~/.cache/humanize/...`, and optional `--once`. Outputs are terminal summaries/status bars and tailed output. Runtime state is local shell variables plus a background `tail` PID; it writes no repo files.
- gates_or_invariants: Missing `.humanize/skill` returns an error with guidance to run ask-codex first (`scripts/lib/monitor-skill.sh:35-40`). `--once` returns nonzero when no invocations exist (`scripts/lib/monitor-skill.sh:268-274`). Only timestamp-prefixed directories count as invocations (`scripts/lib/monitor-skill.sh:42-53`, `scripts/lib/monitor-skill.sh:86-91`). No `metadata.md` means running (`scripts/lib/monitor-skill.sh:92-102`, `scripts/lib/monitor-skill.sh:134-135`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` and dispatched from the monitor command path (`scripts/humanize.sh:1122`, `scripts/humanize.sh:1589`). Depends on `monitor-common.sh` helpers such as `monitor_get_yaml_value` and `monitor_format_timestamp` (`scripts/lib/monitor-skill.sh:8-10`, `scripts/lib/monitor-skill.sh:181-194`, `scripts/lib/monitor-skill.sh:216-221`), plus `humanize_split_to_array`, `git`, `find`, `sort`, `sed`, `grep`, `tput`, and `tail`.
- edge_cases_or_failure_modes: Running invocations prefer cache logs over output (`scripts/lib/monitor-skill.sh:152-158`); completed invocations prefer `output.md` then cache (`scripts/lib/monitor-skill.sh:159-165`). Missing cache displays “not found.” Empty completed outputs show “No output available” in once mode (`scripts/lib/monitor-skill.sh:316-322`). Interactive mode depends on terminal capabilities and includes cleanup to kill/disown tail and restore the screen on exit/signals (`scripts/lib/monitor-skill.sh:358-390`).
- validation_or_tests: `tests/test-skill-monitor.sh` covers missing directories, empty invocation sets, single and multiple invocations, running status, recent list ordering, first-line question extraction, empty responses, and ignoring non-timestamp dirs (`tests/test-skill-monitor.sh:110-180`, `tests/test-skill-monitor.sh:198-389`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 item evidence sections present, matching the 6 assigned rows in order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`