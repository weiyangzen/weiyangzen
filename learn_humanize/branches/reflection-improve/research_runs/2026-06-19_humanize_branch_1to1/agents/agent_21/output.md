# agent_21 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-021 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: User-facing skill manifest for the Humanize plugin. Recursive inspection shows the directory contains only `skills/humanize/SKILL.md`, so this file is the full directory payload. It defines the public algorithm contract for RLCR loops, PR loops, gen-plan, ask-codex, cancellation, monitoring, state layout, and expected exit codes.
- algorithmic_behavior: The skill describes three workflows. RLCR is a two-phase iterative state machine: implementation summaries are reviewed by Codex until `COMPLETE`, then `codex review --base <branch>` checks severity-marked code issues before finalization, with `scripts/rlcr-stop-gate.sh` required in skill mode (`skills/humanize/SKILL.md:31`, `skills/humanize/SKILL.md:47`). PR Loop detects the current PR, fetches selected bot reviews, asks the agent to fix issues, pushes changes, comments for re-review, and relies on the stop hook to poll for fresh reviews (`skills/humanize/SKILL.md:49`, `skills/humanize/SKILL.md:57`). Generate Plan turns drafts into structured implementation plans with AC-style acceptance criteria, path boundaries, sequencing, and task tags (`skills/humanize/SKILL.md:61`, `skills/humanize/SKILL.md:149`).
- inputs_outputs_state: Inputs are plan files or `--skip-impl` for RLCR (`skills/humanize/SKILL.md:74`), bot flags for PR loops (`skills/humanize/SKILL.md:113`), draft/output paths for gen-plan (`skills/humanize/SKILL.md:137`), and direct questions for ask-codex (`skills/humanize/SKILL.md:145`). Outputs are `.humanize/rlcr/<timestamp>/` state, goal tracker, round summaries/reviews, finalize/methodology/complete state files; `.humanize/pr-loop/<timestamp>/state.md` and `resolution-N.md`; and `.humanize/skill/<timestamp>/input.md`, `output.md`, `metadata.md` (`skills/humanize/SKILL.md:220`). The goal tracker is split into immutable goal/acceptance criteria and mutable active/completed/deferred/evolution sections (`skills/humanize/SKILL.md:190`).
- gates_or_invariants: The manifest explicitly forbids cheating by editing state files or running cancel commands, requires summaries and goal-tracker maintenance, and instructs users to run the stop gate each round (`skills/humanize/SKILL.md:204`). Plan generation must avoid plan terminology in implementation code (`skills/humanize/SKILL.md:186`). RLCR options include plan-file immutability, push-every-round, Plan Understanding Quiz controls, agent teams, and privacy mode (`skills/humanize/SKILL.md:87`). PR loop completion requires commit/push and re-review trigger behavior as described by command templates elsewhere.
- dependencies_and_callers: The skill routes to runtime scripts under `{{HUMANIZE_RUNTIME_ROOT}}/scripts/`, including `setup-rlcr-loop.sh`, `rlcr-stop-gate.sh`, `cancel-rlcr-loop.sh`, `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `validate-gen-plan-io.sh`, `ask-codex.sh`, and `humanize.sh` (`skills/humanize/SKILL.md:76`, `skills/humanize/SKILL.md:252`). Runtime prerequisites are `codex` and `gh` (`skills/humanize/SKILL.md:213`).
- edge_cases_or_failure_modes: The manifest exposes cancellation paths for both RLCR and PR loops, review-only RLCR via `--skip-impl`, max-iteration limits, Codex timeout configuration, full review cadence, and validation exit codes for ask-codex and gen-plan IO (`skills/humanize/SKILL.md:257`). It warns that PR cancellation does not affect RLCR (`skills/humanize/SKILL.md:129`).
- validation_or_tests: Directory structure was verified recursively with `find`, yielding only `skills/humanize` and `skills/humanize/SKILL.md`. Related tests cover script-level contracts, especially `tests/test-gen-plan.sh`, `tests/test-pr-loop-scripts.sh`, and hook validator suites, but this skill directory itself is documentation/dispatch metadata rather than executable code.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-051 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for active PR loops. It is part of PR loop state transition behavior because it converts the newest active `.humanize/pr-loop/<timestamp>/state.md` into `cancel-state.md` and creates `.cancel-requested`.
- algorithmic_behavior: Parses `--force` and help flags, rejects unknown options with exit code `3`, resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, then targets `$PROJECT_ROOT/.humanize/pr-loop` (`scripts/cancel-pr-loop.sh:24`, `scripts/cancel-pr-loop.sh:72`). It chooses the lexicographically newest loop directory with `ls -1d ... | sort -r | head -1` (`scripts/cancel-pr-loop.sh:76`). If `state.md` exists, it extracts `current_round`, `max_iterations`, and `pr_number`, writes `.cancel-requested`, renames `state.md` to `cancel-state.md`, and prints `CANCELLED` plus a summary (`scripts/cancel-pr-loop.sh:104`, `scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`).
- inputs_outputs_state: Inputs are optional CLI args, the project root environment, and the newest PR loop `state.md`. Outputs are shell status text and state mutation inside `.humanize/pr-loop/<timestamp>/`: `.cancel-requested` is created and `state.md` becomes `cancel-state.md`. Exit codes are `0` on success, `1` for no loop or inactive newest loop, `3` for usage/other errors (`scripts/cancel-pr-loop.sh:11`).
- gates_or_invariants: The script only affects `.humanize/pr-loop/`, not RLCR (`scripts/cancel-pr-loop.sh:55`). It requires `state.md` in the newest loop; a loop directory without `state.md` is treated as inactive and exits `NO_ACTIVE_LOOP` (`scripts/cancel-pr-loop.sh:91`). `--force` is accepted but has no extra effect (`scripts/cancel-pr-loop.sh:28`, `scripts/cancel-pr-loop.sh:40`).
- dependencies_and_callers: Called by `commands/cancel-pr-loop.md`, whose allowed tools are only this script and the `--force` variant (`commands/cancel-pr-loop.md:3`). The command wrapper expects first-line tokens `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED` (`commands/cancel-pr-loop.md:17`). The skill manifest advertises it as the PR Loop cancel command (`skills/humanize/SKILL.md:129`). It does not source `loop-common.sh`; it reimplements newest-directory selection locally.
- edge_cases_or_failure_modes: If `.humanize/pr-loop` is missing or empty, prints `NO_LOOP` and exits `1` (`scripts/cancel-pr-loop.sh:78`). If newest directory is terminal or malformed without `state.md`, it will not search older loops and exits `NO_ACTIVE_LOOP`, preserving zombie-loop protection by implication (`scripts/cancel-pr-loop.sh:91`). Missing fields default to `?` in the human summary (`scripts/cancel-pr-loop.sh:109`). Because it uses `mv` under `set -e`, filesystem errors during rename would abort as an uncaught other error rather than producing a custom token.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help output, no-loop exit behavior, and active-loop cancellation including the rename from `state.md` to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:181`, `tests/test-pr-loop-scripts.sh:192`, `tests/test-pr-loop-scripts.sh:211`). Static reference search confirms setup/status docs point to this command.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-081 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `gen-plan` command, its companion relevance agent, plan template, version surfaces, and IO validator. It defines expected planning workflow structure and negative validation behavior.
- algorithmic_behavior: The test initializes repo-relative command and agent paths, tracks pass/fail counts, then runs positive and negative checks (`tests/test-gen-plan.sh:11`, `tests/test-gen-plan.sh:22`). Positive tests assert `commands/gen-plan.md` exists, has frontmatter fields, declares allowed tools and argument hint, references `scripts/ask-codex.sh`, exposes `--auto-start-rlcr-if-converged`, requires discussion mode for auto-start, uses “ultrathink”, includes pending-user-decision and Codex/Claude convergence phases, orders Phase 3 before Phase 4, and pins template sections for deliberation, task breakdown, and routing tags (`tests/test-gen-plan.sh:57`, `tests/test-gen-plan.sh:121`, `tests/test-gen-plan.sh:179`, `tests/test-gen-plan.sh:189`, `tests/test-gen-plan.sh:219`). Agent checks pin `agents/draft-relevance-checker.md` name, model `haiku`, and tools (`tests/test-gen-plan.sh:249`). Version consistency is checked across `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md` (`tests/test-gen-plan.sh:291`).
- inputs_outputs_state: Inputs are repository files under `commands/`, `agents/`, `prompt-template/plan/`, `.claude-plugin/`, `README.md`, and `scripts/validate-gen-plan-io.sh`. The script creates temporary fixtures with `mktemp -d` and cleans them with a trap (`tests/test-gen-plan.sh:322`). Outputs are console pass/fail lines and process exit `0` only if no failures (`tests/test-gen-plan.sh:728`).
- gates_or_invariants: Name validation requires lowercase/kebab names via `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:326`). Required command frontmatter must include `description`, while agent frontmatter must include `name` and `description` (`tests/test-gen-plan.sh:332`). YAML syntax is approximated by checking frontmatter lines for key/list/string forms (`tests/test-gen-plan.sh:435`). Model names must be exact aliases `opus|sonnet|haiku` or full IDs starting with `claude-`, `gpt-`, `oN`, or `gemini-`; prefix-like aliases such as `opus-v2` are rejected (`tests/test-gen-plan.sh:481`). Content must avoid Emoji/CJK in command and relevance agent files (`tests/test-gen-plan.sh:534`).
- dependencies_and_callers: Depends on `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `prompt-template/plan/gen-plan-template.md`, and `scripts/validate-gen-plan-io.sh`. It validates command behavior against the actual command spec: `commands/gen-plan.md` runs the validator in Phase 1 and treats exit codes `0` through `7` as control-flow gates (`commands/gen-plan.md:120`, `commands/gen-plan.md:128`). The validator itself parses `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, and `--direct`, rejects mutually exclusive modes, checks input/output filesystem state, and emits `TEMPLATE_FILE:` on success (`scripts/validate-gen-plan-io.sh:35`, `scripts/validate-gen-plan-io.sh:76`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:177`).
- edge_cases_or_failure_modes: Negative tests construct invalid fixtures for uppercase/spaced names, missing frontmatter, malformed YAML, invalid/empty/partial model aliases, unsupported characters, missing validator args, unknown flags, nonexistent input, empty input, nonexistent output dir, pre-existing output path, output path as directory, and mutually exclusive `--discussion --direct` (`tests/test-gen-plan.sh:348`, `tests/test-gen-plan.sh:379`, `tests/test-gen-plan.sh:430`, `tests/test-gen-plan.sh:476`, `tests/test-gen-plan.sh:569`, `tests/test-gen-plan.sh:688`). One notable implementation detail: the script sets two `trap` commands for separate temp dirs, so the second trap replaces the first (`tests/test-gen-plan.sh:324`, `tests/test-gen-plan.sh:567`), which is a cleanup edge case in the test script itself.
- validation_or_tests: This file is itself a test suite and ends with a fail-count gate (`tests/test-gen-plan.sh:718`). It additionally compares the embedded markdown plan block in `commands/gen-plan.md` against `prompt-template/plan/gen-plan-template.md` byte-for-byte (`tests/test-gen-plan.sh:708`), making template drift a hard failure.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-111 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared hook/runtime library for the RLCR and PR loop state machines. It provides JSON hook input validation, active loop discovery, state parsing, review issue detection, access-control block messages, cancellation authorization, `.humanize` git-add protection, terminal state transitions, PR bot name mapping, PR resolve round validation, and PR goal tracker repair/update logic.
- algorithmic_behavior: The library defines canonical state field names and exit reason constants (`hooks/lib/loop-common.sh:24`, `hooks/lib/loop-common.sh:55`). `validate_hook_input` rejects null bytes, invalid UTF-8 where `iconv` exists, invalid JSON, and missing `tool_name`, then extracts `tool_input` (`hooks/lib/loop-common.sh:80`). `find_active_loop` implements zombie-loop protection: without a session filter, only the newest loop dir is considered; with a session filter, it scans newest-to-oldest until it finds the newest directory for that session, returning it only if it has an active state file (`hooks/lib/loop-common.sh:275`). `parse_state_file` is tolerant and defaults optional fields, while `parse_state_file_strict` requires frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, plus numeric/boolean checks (`hooks/lib/loop-common.sh:397`, `hooks/lib/loop-common.sh:433`). `detect_review_issues` scans only the last 50 lines of Codex review logs for `[P0-9]` markers in the first ten characters, writes extracted issues to `round-N-review-result.md`, and returns distinct codes for issues, no issues, and missing/empty logs (`hooks/lib/loop-common.sh:519`). `is_cancel_authorized` allows a state-file rename only when `.cancel-requested` exists and the command is a tightly parsed `mv` from active state file to `cancel-state.md`, rejecting command substitutions, operators, mixed quotes, extra args, hidden variables, and symlinked source state files (`hooks/lib/loop-common.sh:741`). `git_adds_humanize` detects direct, force, all, and broad-scope `git add` variants that would stage `.humanize` (`hooks/lib/loop-common.sh:1123`). `update_pr_goal_tracker` idempotently adds/repairs issue summary rows, total statistics, and issue log entries per round/reviewer (`hooks/lib/loop-common.sh:1375`).
- inputs_outputs_state: Inputs include hook JSON, `.humanize/rlcr` and `.humanize/pr-loop` directory trees, YAML frontmatter state files, Codex review logs in `CACHE_DIR`, Bash command strings, PR bot JSON summaries, and template files. Outputs include global parsed variables such as `STATE_CURRENT_ROUND`, rendered block messages, extracted review-result files, terminal state renames via `end_loop`, and mutated PR goal trackers (`hooks/lib/loop-common.sh:351`, `hooks/lib/loop-common.sh:557`, `hooks/lib/loop-common.sh:1319`, `hooks/lib/loop-common.sh:1519`). It sources config defaults from `scripts/lib/config-loader.sh`, then sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:160`, `hooks/lib/loop-common.sh:195`).
- gates_or_invariants: Active-state precedence is `methodology-analysis-state.md`, then `finalize-state.md`, then `state.md` (`hooks/lib/loop-common.sh:217`). Strict parser blocks missing schema fields before hooks act on stale/corrupt state (`hooks/lib/loop-common.sh:453`). State files and generated prompt/todos files are guarded by template-backed block messages (`hooks/lib/loop-common.sh:618`, `hooks/lib/loop-common.sh:638`, `hooks/lib/loop-common.sh:647`). PR resolve files must match the current PR loop round (`hooks/lib/loop-common.sh:1036`). `.humanize` should never be staged with broad or forced add commands when unsafe (`hooks/lib/loop-common.sh:1101`). `end_loop` only accepts `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` reasons (`hooks/lib/loop-common.sh:1331`).
- dependencies_and_callers: Sourced by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, `loop-codex-stop-hook.sh`, `pr-loop-stop-hook.sh`, `setup-rlcr-loop.sh`, `setup-pr-loop.sh`, `cancel-rlcr-loop.sh`, `ask-codex.sh`, `bitlesson-select.sh`, `humanize.sh`, and multiple tests. Examples: write/edit/read validators use `find_active_loop` and `parse_state_file_strict`; bash validator uses `git_adds_humanize` and `is_cancel_authorized`; PR stop hook calls `update_pr_goal_tracker`; Codex stop hook calls `detect_review_issues`.
- edge_cases_or_failure_modes: The active-loop search intentionally ignores older active loops when the newest relevant loop is terminal to prevent stale revival (`hooks/lib/loop-common.sh:326`). `parse_state_file_strict` accepts negative numeric values because the regex allows `-?`, leaving semantic range checks to callers (`hooks/lib/loop-common.sh:471`). `detect_review_issues` may ignore valid markers outside the last 50 lines or after character ten by design to avoid false positives and huge-log issues (`hooks/lib/loop-common.sh:507`). `is_cancel_authorized` lowercases and normalizes command text, so it is geared for validator-provided lowercase input; mixed quote styles and multiple trailing spaces are explicit failure modes (`hooks/lib/loop-common.sh:724`). `git_adds_humanize` only blocks broad non-force adds when `.humanize` exists and is not gitignored, but force/broad combinations are blocked without that directory check (`hooks/lib/loop-common.sh:1191`).
- validation_or_tests: Direct test coverage includes session and zombie-loop tests in `tests/test-session-id.sh`, review extraction tests in `tests/test-codex-review-merge.sh`, cancellation authorization/security tests in `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, state parser robustness in `tests/robustness/test-state-file-robustness.sh`, `.humanize` escape tests in `tests/test-humanize-escape.sh`, PR goal tracker tests in `tests/test-pr-loop-system.sh`, and template-loader robustness tests. Static inspection only; no tests were executed in this read-only research run.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-141 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template for the invariant that agents cannot modify `state.md`. It is a small policy surface used by validators to explain why direct state-file edits are blocked.
- algorithmic_behavior: The template emits the heading “State File Modification Blocked” and instructs the agent that `state.md` is loop-managed; if work is done, the agent should stop and let another review round auto-trigger (`prompt-template/block/state-file-modification.md:1`). `state_file_blocked_message` in `loop-common.sh` loads this template with an inline fallback if unavailable (`hooks/lib/loop-common.sh:638`).
- inputs_outputs_state: Input is the template-loader call from hook validators. Output is a rendered block message to stderr/validator response. It does not mutate state itself; it protects state by shaping validator feedback.
- gates_or_invariants: The invariant is strict: agent writes/edits to `state.md` corrupt loop state and are blocked. The prescribed transition is indirect: finish the response and let hooks advance the loop (`prompt-template/block/state-file-modification.md:3`).
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/state-file-modification.md"` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:644`). Called by write/edit/bash validators when paths or commands target `state.md` during active RLCR state (`hooks/loop-write-validator.sh:206`, `hooks/loop-edit-validator.sh:169`, `hooks/loop-bash-validator.sh:269` from reference search).
- edge_cases_or_failure_modes: If the template is missing or empty, `loop-common.sh` supplies a shorter fallback, so validator behavior degrades gracefully (`hooks/lib/loop-common.sh:640`). This template applies to RLCR `state.md`; PR loop has a separate `pr-loop-state-modification.md` template.
- validation_or_tests: `tests/test-template-references.sh` lists this template among required block references, and template-loader tests verify `load_and_render_safe` fallback behavior. State-file robustness and hook validator suites exercise the broader blocked-state invariant.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-171 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR Loop prompt template used when a PR already has bot comments/issues. It defines the mandatory work-completion contract that lets the stop hook know how to poll for the next bot review cycle.
- algorithmic_behavior: The template tells the agent that completion requires committing and pushing changes, commenting on the PR with `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review"`, and writing a resolution summary to `{{RESOLVE_PATH}}` with issues addressed, files modified, and tests added (`prompt-template/pr-loop/critical-requirements-has-comments.md:6`, `prompt-template/pr-loop/critical-requirements-has-comments.md:12`, `prompt-template/pr-loop/critical-requirements-has-comments.md:15`). It closes by stating the stop hook will poll for bot reviews (`prompt-template/pr-loop/critical-requirements-has-comments.md:23`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, provided by setup code. Output is rendered prompt text embedded in the task instructions for an agent handling existing PR comments. The expected downstream state is a pushed branch, a PR comment that triggers re-review, and a resolution file in the loop directory.
- gates_or_invariants: The completion gate requires all three surfaces: git commit/push, PR trigger comment, and local resolution summary. Omitting any one risks the PR stop hook having nothing to poll or validate. This differs from the no-comments round-zero path, which explicitly says not to comment because bots should review automatically.
- dependencies_and_callers: `scripts/setup-pr-loop.sh` selects this template in the branch where comments already exist; the fallback has matching content, then `load_and_render_safe` renders `pr-loop/critical-requirements-has-comments.md` with `TEMPLATE_VARS` (`scripts/setup-pr-loop.sh:916`, `scripts/setup-pr-loop.sh:940`). The template is also discoverable through the template reference tests.
- edge_cases_or_failure_modes: Incorrect or empty template variables would produce an invalid `gh pr comment` command or bad summary path. If the template is missing, setup uses an inline fallback with the same variable placeholders (`scripts/setup-pr-loop.sh:916`). The template assumes `gh` auth and remote push ability; failures there are handled by PR loop scripts/hooks, not by the template.
- validation_or_tests: Covered indirectly by PR loop system/setup tests and template reference tests. Static reference search shows this is the “has comments” critical requirement path only; `critical-requirements-no-comments.md` handles initial review waiting.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-201 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for template system error handling. It verifies that `hooks/lib/template-loader.sh` remains resilient under missing templates, malformed placeholders, unusual variable names/values, filesystem anomalies, concurrency, subdirectories, and symlinks.
- algorithmic_behavior: The script sources `template-loader.sh` and shared test helpers, creates a temp test dir, then runs 20 named tests (`tests/robustness/test-template-error-robustness.sh:15`, `tests/robustness/test-template-error-robustness.sh:20`). It checks `load_and_render_safe` fallback behavior for missing files and dirs, acceptable handling of empty templates, non-crashing behavior for unclosed/nested/empty placeholders, handling of invalid variable names, single-pass substitution when values contain `{{OTHER}}`, preservation of multiline values, long variable names, BOM/whitespace templates, special filenames, permission-denied paths, concurrent loads, nested directories, and symlinked templates (`tests/robustness/test-template-error-robustness.sh:34`, `tests/robustness/test-template-error-robustness.sh:73`, `tests/robustness/test-template-error-robustness.sh:125`, `tests/robustness/test-template-error-robustness.sh:165`, `tests/robustness/test-template-error-robustness.sh:208`, `tests/robustness/test-template-error-robustness.sh:270`, `tests/robustness/test-template-error-robustness.sh:309`).
- inputs_outputs_state: Inputs are generated temporary template files plus `render_template` and `load_and_render_safe` from `hooks/lib/template-loader.sh`. Outputs are pass/fail records through `test-helpers.sh` and the final `print_test_summary` exit code (`tests/robustness/test-template-error-robustness.sh:340`). Temporary state is confined to the test directory created by helper setup.
- gates_or_invariants: The main invariants are: missing templates must return fallback content; rendering malformed placeholders must not crash; variable values containing template syntax must not recursively expand; concurrent reads must not corrupt template files; filesystem oddities must either render correctly or degrade gracefully (`tests/robustness/test-template-error-robustness.sh:36`, `tests/robustness/test-template-error-robustness.sh:76`, `tests/robustness/test-template-error-robustness.sh:168`, `tests/robustness/test-template-error-robustness.sh:274`). These map directly to `template-loader.sh`, which documents uppercase placeholder syntax, single-pass substitution, and fallback-safe loading (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:170`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for `render_template` and `load_and_render_safe`, plus `tests/test-helpers.sh` for setup and reporting (`tests/robustness/test-template-error-robustness.sh:17`). This matters to the core loop because `loop-common.sh` and stop hooks use `load_and_render_safe` for blocking messages and prompt assembly throughout the RLCR/PR workflows (`hooks/lib/loop-common.sh:626`, `hooks/lib/loop-common.sh:644`, `hooks/lib/loop-common.sh:1258`).
- edge_cases_or_failure_modes: Several tests intentionally accept multiple safe outcomes rather than one exact output: empty templates may return empty or fallback; special-character filenames may render or use fallback; permission behavior may vary by platform; symlink support may be skipped (`tests/robustness/test-template-error-robustness.sh:57`, `tests/robustness/test-template-error-robustness.sh:231`, `tests/robustness/test-template-error-robustness.sh:245`, `tests/robustness/test-template-error-robustness.sh:320`). The permission test skips when running as root (`tests/robustness/test-template-error-robustness.sh:248`).
- validation_or_tests: This file is itself a validation script. It statically aligns with `template-loader.sh` behavior: single-pass awk rendering avoids placeholder injection, missing/empty template content falls back, and template dir validation is intentionally separate (`hooks/lib/template-loader.sh:71`, `hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:192`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`