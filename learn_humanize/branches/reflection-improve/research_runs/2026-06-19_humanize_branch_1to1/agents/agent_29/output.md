# agent_29 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-029 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling an active PR review loop. It is a thin operator-facing contract around `scripts/cancel-pr-loop.sh`, not the implementation itself.
- algorithmic_behavior: The command instructs the assistant to run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"` and classify the first output line: `NO_LOOP` / `NO_ACTIVE_LOOP` maps to “No active PR loop found”, while `CANCELLED` maps to reporting the cancellation message from the script. See `commands/cancel-pr-loop.md:11` and `commands/cancel-pr-loop.md:17`.
- inputs_outputs_state: Input is the current project’s PR loop state under `.humanize/pr-loop/`, resolved by the script through `CLAUDE_PROJECT_DIR` or `pwd`. Output is a user response based on the script’s first line. State mutation is delegated to `scripts/cancel-pr-loop.sh`, which creates `.cancel-requested` and renames `state.md` to `cancel-state.md` in the newest PR loop directory (`scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`).
- gates_or_invariants: The command explicitly preserves the loop directory and does not delete comments, summaries, or state evidence (`commands/cancel-pr-loop.md:21`, `commands/cancel-pr-loop.md:23`). It is scoped only to PR loops and must not affect RLCR loops (`commands/cancel-pr-loop.md:25`).
- dependencies_and_callers: Allowed tools restrict execution to `scripts/cancel-pr-loop.sh` with or without `--force` (`commands/cancel-pr-loop.md:3`). Related setup docs point users to `/humanize:cancel-pr-loop` for PR loop cancellation, while RLCR has a separate cancel path. The backing script searches `.humanize/pr-loop`, not `.humanize/rlcr` (`scripts/cancel-pr-loop.sh:72`).
- edge_cases_or_failure_modes: If no PR loop directory exists, the script emits `NO_LOOP` and exits `1` (`scripts/cancel-pr-loop.sh:78`). If a newest loop directory exists but lacks active `state.md`, it emits `NO_ACTIVE_LOOP` and exits `1` (`scripts/cancel-pr-loop.sh:91`). The `--force` flag is accepted but currently has no additional behavior (`scripts/cancel-pr-loop.sh:24`, `scripts/cancel-pr-loop.sh:40`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` checks help output, no-loop behavior, and active cancellation. The active-loop test asserts `CANCELLED`, verifies `cancel-state.md` exists, and verifies `state.md` was removed (`tests/test-pr-loop-scripts.sh:181`, `tests/test-pr-loop-scripts.sh:192`, `tests/test-pr-loop-scripts.sh:211`, `tests/test-pr-loop-scripts.sh:236`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-059 `file` `scripts/rlcr-stop-gate.sh`
- cursor: `[_]`
- core_role: Non-hook CLI wrapper that runs the RLCR stop-hook algorithm from skill or other non-native-hook environments. It preserves the same block/allow semantics as `hooks/loop-codex-stop-hook.sh`.
- algorithmic_behavior: The script builds a synthetic Claude Code Stop hook JSON object containing `hook_event_name: "Stop"`, `stop_hook_active: false`, `cwd`, and optional `session_id` / `transcript_path`, then pipes it to `hooks/loop-codex-stop-hook.sh` with `CLAUDE_PROJECT_DIR` set to the selected project root (`scripts/rlcr-stop-gate.sh:84`, `scripts/rlcr-stop-gate.sh:87`, `scripts/rlcr-stop-gate.sh:102`). Empty hook output means allow; JSON with `.decision == "block"` means stop is denied and the loop should continue (`scripts/rlcr-stop-gate.sh:110`, `scripts/rlcr-stop-gate.sh:126`).
- inputs_outputs_state: Inputs are CLI flags `--session-id`, `--transcript-path`, `--project-root`, and `--json`, with environment fallbacks from `CLAUDE_SESSION_ID`, `CLAUDE_TRANSCRIPT_PATH`, and `CLAUDE_PROJECT_DIR` (`scripts/rlcr-stop-gate.sh:21`, `scripts/rlcr-stop-gate.sh:24`, `scripts/rlcr-stop-gate.sh:41`). Outputs are normalized operator messages and exit codes: `0` allow, `10` block, `20` wrapper/runtime error (`scripts/rlcr-stop-gate.sh:8`). The wrapper itself does not directly mutate loop state, but the underlying stop hook may transition RLCR state files as part of its review/finalize/methodology flow.
- gates_or_invariants: It requires an executable hook script and `jq`, otherwise exits `20` (`scripts/rlcr-stop-gate.sh:74`, `scripts/rlcr-stop-gate.sh:79`). It fail-closes on hook nonzero exit, non-JSON hook output, or unexpected decision values (`scripts/rlcr-stop-gate.sh:104`, `scripts/rlcr-stop-gate.sh:116`, `scripts/rlcr-stop-gate.sh:136`). It preserves hook-mode behavior rather than reimplementing review logic.
- dependencies_and_callers: Direct dependency is `hooks/loop-codex-stop-hook.sh` (`scripts/rlcr-stop-gate.sh:22`). The `humanize-rlcr` skill requires this wrapper every round and maps exits `0`, `10`, and `20` to done, continue, and infrastructure error respectively (`skills/humanize-rlcr/SKILL.md:10`, `skills/humanize-rlcr/SKILL.md:51`, `skills/humanize-rlcr/SKILL.md:61`). The bash validator explicitly blocks manual shell execution of `rlcr-stop-gate.sh` during active loops to prevent bypassing hook control (`hooks/loop-bash-validator.sh:172`).
- edge_cases_or_failure_modes: Unknown options or missing option values exit `20` (`scripts/rlcr-stop-gate.sh:43`, `scripts/rlcr-stop-gate.sh:66`). If `--json` is provided during a block, raw hook JSON is printed; otherwise it prints `BLOCK:` plus `systemMessage` and reason (`scripts/rlcr-stop-gate.sh:126`). If no active loop exists, the hook emits no output and the wrapper prints `ALLOW: stop gate passed.` (`scripts/rlcr-stop-gate.sh:111`).
- validation_or_tests: `tests/test-stop-gate.sh` verifies default project root uses caller `cwd`, `--project-root` overrides target repo selection, active loops block with exit `10`, and no active loop allows with exit `0` (`tests/test-stop-gate.sh:64`, `tests/test-stop-gate.sh:91`, `tests/test-stop-gate.sh:118`). The fixture intentionally creates an active loop that blocks on missing summary (`tests/test-stop-gate.sh:18`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-089 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file and loop-artifact protection across UserPromptSubmit, Write, Edit, Bash, and Stop hooks. It codifies safety behavior rather than implementing runtime behavior.
- algorithmic_behavior: The test creates isolated temporary git repositories, mocks `codex`, creates `.humanize/rlcr/<timestamp>` loop state, plan backups, summaries, and goal trackers, then invokes hook scripts with JSON hook inputs. The setup helper initializes git, writes `plans/test-plan.md`, gitignores `plans/`, copies the backup to loop `plan.md`, and writes v1.5.0-style state including `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` (`tests/test-plan-file-hooks.sh:30`, `tests/test-plan-file-hooks.sh:40`, `tests/test-plan-file-hooks.sh:63`, `tests/test-plan-file-hooks.sh:87`, `tests/test-plan-file-hooks.sh:99`, `tests/test-plan-file-hooks.sh:103`).
- inputs_outputs_state: Inputs are synthetic hook JSON objects and generated repo/loop files. Outputs are PASS/FAIL counters and process exit equal to failure count (`tests/test-plan-file-hooks.sh:26`, `tests/test-plan-file-hooks.sh:1117`). State transitions are simulated by overwriting `state.md`, switching branches, modifying/deleting plan files, creating tracked-plan repos, adding summaries, and populating goal trackers.
- gates_or_invariants: The spec asserts malformed/outdated state blocks, YAML-quoted values parse correctly, branch switching blocks, `plan.md` backups in `.humanize/rlcr` are immutable, Bash cannot bypass Write/Edit hooks, plan-file integrity is checked both for gitignored and tracked files, and legacy `.humanize-loop.local` is no longer protected (`tests/test-plan-file-hooks.sh:149`, `tests/test-plan-file-hooks.sh:194`, `tests/test-plan-file-hooks.sh:226`, `tests/test-plan-file-hooks.sh:260`, `tests/test-plan-file-hooks.sh:509`, `tests/test-plan-file-hooks.sh:602`, `tests/test-plan-file-hooks.sh:697`, `tests/test-plan-file-hooks.sh:1064`).
- dependencies_and_callers: It directly exercises `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` (`tests/test-plan-file-hooks.sh:6`, `tests/test-plan-file-hooks.sh:125`, `tests/test-plan-file-hooks.sh:230`, `tests/test-plan-file-hooks.sh:247`, `tests/test-plan-file-hooks.sh:264`, `tests/test-plan-file-hooks.sh:539`). Those hooks depend on shared parsing and template behavior from `hooks/lib/loop-common.sh` and stop-hook logic.
- edge_cases_or_failure_modes: The file targets several known bypass classes: direct `.humanize/rlcr/plan.md` without a timestamp subdir, command substitution, glob expansion, brace expansion, piped `tee`, backtick substitution, quoted YAML fields, hyphenated plan paths, tracked file uncommitted changes, tracked file committed changes with clean git status, and section-specific placeholder detection (`tests/test-plan-file-hooks.sh:286`, `tests/test-plan-file-hooks.sh:304`, `tests/test-plan-file-hooks.sh:317`, `tests/test-plan-file-hooks.sh:330`, `tests/test-plan-file-hooks.sh:343`, `tests/test-plan-file-hooks.sh:356`, `tests/test-plan-file-hooks.sh:373`, `tests/test-plan-file-hooks.sh:469`, `tests/test-plan-file-hooks.sh:602`, `tests/test-plan-file-hooks.sh:697`, `tests/test-plan-file-hooks.sh:777`).
- validation_or_tests: This file is itself validation. It covers allow-path behavior for valid state (`tests/test-plan-file-hooks.sh:119`), block-path behavior for schema/branch/plan integrity failures, hook input path protection, stop-hook JSON block responses, and final summary counts (`tests/test-plan-file-hooks.sh:1108`). It does not verify implementation by modifying the repository under test; it uses temporary directories via `mktemp -d` and cleans them with a trap (`tests/test-plan-file-hooks.sh:31`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-119 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template for a failed Codex review invocation. It defines the user-facing contract when review output is missing and the RLCR stop hook needs to force a retry.
- algorithmic_behavior: The template renders a concise failure report with exit code, missing review-result file, debug command/stdout/stderr paths, and the last stderr lines, then instructs the user that another review will be attempted on exit (`prompt-template/block/codex-review-failed.md:1`, `prompt-template/block/codex-review-failed.md:5`, `prompt-template/block/codex-review-failed.md:8`, `prompt-template/block/codex-review-failed.md:13`, `prompt-template/block/codex-review-failed.md:18`).
- inputs_outputs_state: Inputs are template variables including `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT`. Output is rendered Markdown embedded in hook JSON `.reason`; no state mutation happens in this template.
- gates_or_invariants: It supports the invariant that Codex review failure is blocking, not ignorable. The stop hook wraps rendered content into JSON with `decision: block` and `systemMessage: "Loop: Blocked - Codex review failed, retry required"` (`hooks/loop-codex-stop-hook.sh:1409`).
- dependencies_and_callers: The stop hook calls `load_and_render_safe "$TEMPLATE_DIR" "block/codex-review-failed.md"` in its Codex review failure path (`hooks/loop-codex-stop-hook.sh:1399`). Rendering uses `hooks/lib/template-loader.sh`, whose missing variable behavior intentionally keeps unresolved placeholders as-is (`hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:116`).
- edge_cases_or_failure_modes: There is a placeholder-name mismatch risk at the observed call site: the hook passes `EXIT_CODE`, `CODEX_LOG_FILE`, and not visibly `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, or `CODEX_STDERR_FILE` in that `load_and_render_safe` call (`hooks/loop-codex-stop-hook.sh:1399`). Because the renderer preserves missing placeholders, the template could show literal placeholders unless another surrounding path supplies them. This is a diagnostic-quality edge case, not a loop-state mutation issue.
- validation_or_tests: Template reference coverage includes `tests/test-template-references.sh`, which checks hook/template relationships. Runtime failure behavior is indirectly exercised by stop-hook tests and review-phase tests; the specific failure renderer is called from `hooks/loop-codex-stop-hook.sh:1360` through `hooks/loop-codex-stop-hook.sh:1417`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-149 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template for read attempts against a stale or future RLCR round file. It is part of the read-routing guard that keeps agents on the current round’s prompt/summary files.
- algorithmic_behavior: The template reports that the attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md` does not match `{{CURRENT_ROUND}}`, lists the current prompt and summary paths, and provides an explicit `cat {{FILE_PATH}}` escape when the user truly needs that historical file (`prompt-template/block/wrong-round-file.md:3`, `prompt-template/block/wrong-round-file.md:5`, `prompt-template/block/wrong-round-file.md:9`).
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`. Output is rendered Markdown to stderr from the read validator; no files are changed.
- gates_or_invariants: The read validator invokes this template when the requested round number differs from active `CURRENT_ROUND` and the file is not allowlisted (`hooks/loop-read-validator.sh:211`). This enforces active-round source-of-truth and prevents accidental reasoning from stale prompt/summary content.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`, which validates file location first, then validates round number, then validates directory path (`hooks/loop-read-validator.sh:195`, `hooks/loop-read-validator.sh:211`, `hooks/loop-read-validator.sh:232`). Rendering uses `load_and_render_safe`, with a fallback message if the template is missing (`hooks/loop-read-validator.sh:212`).
- edge_cases_or_failure_modes: Allowlisted files bypass the wrong-round block, so historical or special loop files can still be read when explicitly allowed (`hooks/loop-read-validator.sh:211`). The template’s final instruction permits manual `cat` of the requested file, which is useful for intentional inspection but weakens the guard if interpreted as permission to use stale files as current-round source.
- validation_or_tests: Read-validator behavior is covered by the broader hook/template tests, including `tests/test-template-references.sh`. The assigned template’s concrete call site is `hooks/loop-read-validator.sh:217`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-179 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitoring utility library for RLCR and PR loop terminal monitors. It centralizes file/session/state discovery, phase detection, lightweight YAML parsing, formatting, and goal-tracker summarization.
- algorithmic_behavior: The library defines ANSI color helpers, portable file-size lookup, latest timestamped session discovery, terminal scroll-region helpers, state-file detection, YAML frontmatter value extraction, string/timestamp formatting, PR-loop phase detection, and goal-tracker parsers (`scripts/lib/monitor-common.sh:12`, `scripts/lib/monitor-common.sh:29`, `scripts/lib/monitor-common.sh:37`, `scripts/lib/monitor-common.sh:69`, `scripts/lib/monitor-common.sh:147`, `scripts/lib/monitor-common.sh:198`, `scripts/lib/monitor-common.sh:219`, `scripts/lib/monitor-common.sh:262`, `scripts/lib/monitor-common.sh:391`, `scripts/lib/monitor-common.sh:469`).
- inputs_outputs_state: Inputs are filesystem paths to loop directories, session directories, state files, and goal trackers. Outputs are shell strings such as latest session path, `state_file|status`, phase labels, formatted timestamps, truncated strings, and pipe-delimited tracker metrics. State mutation is minimal: `get_pr_loop_phase` writes a `/tmp/humanize-phase-<session>-<round>.size` cache file to detect whether Codex output is growing (`scripts/lib/monitor-common.sh:301`, `scripts/lib/monitor-common.sh:310`).
- gates_or_invariants: `monitor_find_latest_session` only accepts directory names matching `YYYY-MM-DD_HH-MM-SS` and chooses lexicographically latest timestamp (`scripts/lib/monitor-common.sh:54`). `monitor_find_state_file` prioritizes active files (`methodology-analysis-state.md`, then `state.md`) over terminal `*-state.md` files (`scripts/lib/monitor-common.sh:162`). PR phase detection treats `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` as terminal before detecting in-progress Codex analysis (`scripts/lib/monitor-common.sh:276`).
- dependencies_and_callers: `scripts/humanize.sh` sources the library at startup (`scripts/humanize.sh:6`) and uses it in RLCR monitor helpers for latest session and state file lookup (`scripts/humanize.sh:245`, `scripts/humanize.sh:343`), and in PR monitor helpers for latest session, state mapping, phase display, and issue stats (`scripts/humanize.sh:1189`, `scripts/humanize.sh:1238`, `scripts/humanize.sh:1323`, `scripts/humanize.sh:1364`). `scripts/lib/monitor-skill.sh` uses `monitor_get_yaml_value` for skill invocation metadata.
- edge_cases_or_failure_modes: State-file discovery returns `|unknown` for missing or invalid sessions (`scripts/lib/monitor-common.sh:157`). YAML parsing is intentionally simple grep/sed and strips double quotes, so complex YAML values, duplicate keys, multiline values, or colons inside values are not robustly parsed (`scripts/lib/monitor-common.sh:207`). PR phase detection can report `codex_analyzing` on a newly observed or recently modified `pr-check` file even if growth is not sustained, because it treats file age under 10 seconds as active (`scripts/lib/monitor-common.sh:312`). Goal tracker parsing is table/heading-format dependent and returns zero/default values when the file is absent (`scripts/lib/monitor-common.sh:396`).
- validation_or_tests: `tests/test-pr-loop-system.sh` directly tests latest-session selection, state-file detection, and portable file-size behavior (`tests/test-pr-loop-system.sh:770`, `tests/test-pr-loop-system.sh:794`, `tests/test-pr-loop-system.sh:826`). Additional tests exercise PR phase and tracker parsing via `get_pr_loop_phase`, `parse_goal_tracker`, and `humanize_parse_pr_goal_tracker` (`tests/test-pr-loop-system.sh:266`, `tests/test-pr-loop-system.sh:360`, `tests/test-pr-loop-system.sh:405`). Robustness tests cover tracker parsing edge cases under `tests/robustness/test-goal-tracker-robustness.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 unique item evidence sections above, matching the 6 assigned rows in order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`