# agent_15 reflection-improve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `13a47fb2260667a272b448e8d3c1a521f2382590`

## Item Evidence

### REFLECTION_IMPROVE-HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt layer for the RLCR state machine. These templates define what Claude must do after Codex review feedback, during review remediation, during finalize, during methodology analysis, and when agent-teams mode is enabled.
- algorithmic_behavior: The directory is loaded by `hooks/loop-codex-stop-hook.sh` through `load_template` / `load_and_render_safe`. `next-round-prompt.md` forms the main implementation retry prompt, injecting the original plan, Codex review content, goal tracker, and BitLesson requirements (`prompt-template/claude/next-round-prompt.md:1-36`; caller at `hooks/loop-codex-stop-hook.sh:1740-1744`). `next-round-footer.md` adds the no-manual-exit invariant plus commit and summary obligations (`prompt-template/claude/next-round-footer.md:4-9`; caller at `hooks/loop-codex-stop-hook.sh:1814-1819`). `review-phase-prompt.md` constrains remediation to `[P0-9]` findings and states that `COMPLETE` is ignored in review phase (`prompt-template/claude/review-phase-prompt.md:11-28`; caller at `hooks/loop-codex-stop-hook.sh:1329-1332`). Finalize templates hand off to the code-simplifier agent after review success or skipped review (`prompt-template/claude/finalize-phase-prompt.md:1-50`, `prompt-template/claude/finalize-phase-skipped-prompt.md:1-50`; callers at `hooks/loop-codex-stop-hook.sh:1212-1245`). Methodology analysis asks for sanitized loop-process analysis and writes completion markers (`prompt-template/claude/methodology-analysis-prompt.md:1-73`; caller at `hooks/lib/methodology-analysis.sh:82`).
- inputs_outputs_state: Inputs are rendered variables such as `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `NEXT_SUMMARY_FILE`, `REVIEW_SKIP_REASON`, `BASE_BRANCH`, and `START_BRANCH`. Outputs are prompt files like `round-N-prompt.md` or stop-hook block reasons. State transitions encoded by these prompts include implementation retry, review remediation, finalize, methodology analysis, push-every-round note injection, post-alignment action injection, and agent-team continuation. Agent-team initial setup uses `agent-teams-instructions.md` plus `agent-teams-core.md` from `scripts/setup-rlcr-loop.sh` (`scripts/setup-rlcr-loop.sh:1155-1158`); continuation uses `agent-teams-continue.md` plus `agent-teams-core.md` only when `AGENT_TEAMS=true` and not in review phase (`hooks/loop-codex-stop-hook.sh:1837-1857`).
- gates_or_invariants: Templates establish non-code gates: Claude must not edit loop state or use `cancel-rlcr-loop` deceptively (`next-round-footer.md:4`); review phase fixes must focus only on review findings (`review-phase-prompt.md:11-28`); goal tracker is read-only after round 0 and updates must be requested in summary (`next-round-prompt.md:27-36`, `goal-tracker-update-request.md:2-16`); open questions require `AskUserQuestion` before other work (`open-question-notice.md:1`, injected by `hooks/loop-codex-stop-hook.sh:1770-1803`); agent-team leader must delegate and avoid direct implementation (`agent-teams-core.md:1-31`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for single-pass rendering and fallback behavior. Primary callers are `hooks/loop-codex-stop-hook.sh` for RLCR follow-up/finalize/review prompts, `hooks/lib/methodology-analysis.sh` for the methodology phase, and `scripts/setup-rlcr-loop.sh` for initial agent-team prompt construction. Sibling template directories provide block messages and Codex review templates; this directory specifically covers Claude instructions.
- edge_cases_or_failure_modes: Missing templates degrade through fallback prompts in `load_and_render_safe`, preserving loop continuity. Agent-team prompts are intentionally skipped during review phase to avoid delegating review-remediation control into team continuation text (`hooks/loop-codex-stop-hook.sh:1837-1839`). The methodology prompt accepts a known hook limitation: spawned agents with different `session_id` are controlled by prompt sanitization rather than hooks (`prompt-template/claude/methodology-analysis-prompt.md:43-69`, reinforced by `hooks/loop-write-validator.sh:108-112`).
- validation_or_tests: `tests/test-agent-teams.sh` validates the agent-team template files and verifies prompt injection behavior; `tests/test-template-references.sh` validates template references; `tests/test-templates-comprehensive.sh` and `tests/test-template-loader.sh` validate template loading/rendering behavior.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-045 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Write validator for RLCR and PR-loop state-machine file protections. It controls which files Claude may create or overwrite inside `.humanize/rlcr` and `.humanize/pr-loop`.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON, rejects excessive nesting, exits immediately for non-`Write` tools, and requires `tool_input.file_path` (`hooks/loop-write-validator.sh:25-49`). It blocks writes to round todo files unless allowlisted for the active loop (`:58-66`) and blocks prompt-file writes (`:68-71`). For PR loops, it blocks `state.md`, generated read-only files, and validates `round-N-pr-resolve.md` round correctness (`:77-97`). During methodology analysis, it only allows `methodology-analysis-report.md` and `methodology-analysis-done.md`, blocking all other writes in the session-matched loop (`:106-138`). For RLCR files, it resolves the active loop, parses state strictly, blocks state/finalize/methodology state files, permits only the active loop’s `finalize-summary.md`, blocks plan backup writes, blocks `goal-tracker.md` after round 0, and validates summary file location/round/directory (`:167-313`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name`, `tool_input.file_path`, and optional `session_id`. Output is exit `0` for allowed writes, exit `1` for malformed input/state safety failures, or exit `2` plus a rendered block message for policy violations. It reads active loop state from `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md,methodology-analysis-state.md}` via `resolve_active_state_file` and `parse_state_file_strict`.
- gates_or_invariants: Fails closed on invalid JSON, missing file path, deep JSON, and malformed state (`:27-45`, `:183-187`). Protects system-managed state files (`:195-208`), generated prompts/todos (`:58-71`), PR loop generated state/read-only artifacts (`:80-90`), plan backup integrity (`:227-233`), and goal-tracker immutability after round 0 (`:240-244`). Ensures summaries are written only to the current active loop and current round (`:250-310`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`:17-20`), which supplies JSON validation, session-aware loop discovery, strict state parsing, template loading, file-type predicates, and PR-loop helpers. Uses `prompt-template/block/plan-backup-protected.md` at `:230` and other block templates via `load_and_render_safe`.
- edge_cases_or_failure_modes: If no active RLCR loop is found, RLCR-specific writes are allowed (`:172-174`). Methodology analysis deliberately does not fall back to unfiltered loop lookup to avoid restricting unrelated sessions (`:108-112`). For a target file that does not yet exist, it tries resolving the parent directory before comparing paths (`:115-124`). A filename of `plan.md` outside `.humanize/rlcr` is not blocked by the plan-backup gate (`:227-233`).
- validation_or_tests: Covered by allowlist/path/state validation suites such as `tests/test-allowlist-validators.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-plan-file-validation.sh`, `tests/robustness/test-state-file-robustness.sh`, and template fallback tests. The plan-backup branch is paired with `prompt-template/block/plan-backup-protected.md`.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-075 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR cancel-signal exception in Bash validation. It verifies that the state-file protection allows only the authorized cancel transition and blocks state-file mutation bypasses.
- algorithmic_behavior: Builds a temporary `.humanize/rlcr/<timestamp>/state.md` with required state fields and sets `CLAUDE_PROJECT_DIR` for validator discovery (`tests/test-cancel-signal-file.sh:42-59`). `run_bash_validator` wraps commands as hook JSON and invokes `hooks/loop-bash-validator.sh` (`:61-74`). Positive tests allow `mv state.md cancel-state.md` only when `.cancel-requested` exists in the active loop (`:80-115`) and allow quoted/literal `${loop_dir}` forms (`:365-486`). Negative tests block missing signal, wrong destination, `rm`, `sed -i`, redirection, copy, chained commands, command substitution, shell wrappers, prefixes like `sudo`/`env`/`command --`, hidden variables like `${IFS}`, unrelated state files, extra args, and redirection-prefix bypasses (`:121-357`, `:386-1206`, `:1212-1227`). It also checks the helper directly (`:1251-1343`).
- inputs_outputs_state: Inputs are shell command strings. Outputs are validator exit codes: `0` when authorized, `2` when blocked, and helper true/false returns for `is_cancel_authorized`. State transition under test is active `state.md` / `finalize-state.md` / `methodology-analysis-state.md` to terminal `cancel-state.md`, gated by `.cancel-requested`.
- gates_or_invariants: The test encodes these invariants: signal file must be in the active loop directory (`:201-217`); signal does not authorize other state edits (`:141-175`, `:223-237`); command must be a plain two-argument `mv` to `cancel-state.md`; shell operators and variable substitutions must be rejected; no active loop means no cancel protection is applied (`:1230-1249`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`:32-33`) and exercises `hooks/loop-bash-validator.sh`. The relevant implementation is `is_cancel_authorized` in `hooks/lib/loop-common.sh:723-903` and Bash validator state-modification checks around `hooks/loop-bash-validator.sh:239-266` plus later source/destination checks.
- edge_cases_or_failure_modes: Covers path normalization with `/./` (`tests/test-cancel-signal-file.sh:100-115`), single-quoted paths (`:365-380`), literal `${loop_dir}` normalization (`:450-486`), command substitution (`:303-337`), newline injection (`:345-357`), redirection prefix variants (`:849-1206`), and wrong-directory `state.md` names (`:1212-1227`). It does not execute the actual cancel command; it validates hook authorization behavior.
- validation_or_tests: This file is itself validation. It exits with the number of failed assertions (`:1349-1357`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-105 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the template loading/rendering layer that powers RLCR/PR-loop prompts and block messages.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, computes `TEMPLATE_DIR`, and tests `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` (`tests/test-template-loader.sh:10-13`, `:41-244`). It then stress-tests rendering safety for shell metacharacters, multiline values, placeholder-injection prevention, empty variables, Unicode, underscore-prefixed variables, and numeric variable names (`:247-640`).
- inputs_outputs_state: Inputs are template strings, template file paths under `prompt-template`, and `VAR=value` substitutions. Outputs are rendered strings and aggregate pass/fail counters. No persistent repository state is mutated outside test temp/runtime behavior.
- gates_or_invariants: Rendering must be single-pass: injected `{{VAR}}` text inside a value must not be recursively substituted (`:488-543`). Missing variables remain as placeholders (`:181-194`). Missing templates return empty for `load_template` but use fallback content for `load_and_render_safe` (`:67-78`, `:196-222`). Special characters such as `&`, `\`, `$`, backticks, pipes, semicolons, glob tokens, quotes, redirection syntax, JSON, and multiline content must render literally, not as shell syntax (`:247-486`).
- dependencies_and_callers: Directly tests `hooks/lib/template-loader.sh`, where `render_template` builds `TMPL_VAR_` environment variables and uses awk for non-recursive placeholder replacement (`hooks/lib/template-loader.sh:50-132`). This loader is used by `hooks/lib/loop-common.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and setup scripts.
- edge_cases_or_failure_modes: Covers path-like values, Windows paths, regex-like strings, JSON object text, non-ASCII values, and values that contain later or earlier placeholder names (`tests/test-template-loader.sh:269-486`, `:499-543`, `:567-615`). It does not cover every filesystem failure mode; those are expanded in robustness tests such as `tests/robustness/test-template-error-robustness.sh`.
- validation_or_tests: This file is a standalone validation script; it prints totals and exits `0` only when `TESTS_FAILED` is zero (`:641-659`).
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-135 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for the plan-backup invariant: the copied `plan.md` inside an RLCR loop directory is read-only and must not be modified by Claude.
- algorithmic_behavior: Provides the rendered reason when validators block a write/edit/bash attempt against `.humanize/rlcr/.../plan.md`. Its content states that the loop `plan.md` is a backup of the original plan and should be read instead of modified (`prompt-template/block/plan-backup-protected.md:1-7`).
- inputs_outputs_state: Has no variables. Input is just the validator selecting this template; output is a human-facing block reason. State protected is the loop-local plan backup, which anchors plan integrity across rounds.
- gates_or_invariants: Write validator blocks direct Write to loop `plan.md` and renders this template (`hooks/loop-write-validator.sh:227-233`). Edit validator uses the same template for edits (`hooks/loop-edit-validator.sh:181`). Bash validator uses it for shell modification attempts (`hooks/loop-bash-validator.sh:472`). The invariant is that plan changes must not occur through the loop backup.
- dependencies_and_callers: Depends on `load_and_render_safe` fallback mechanics. Called by Write/Edit/Bash validators when `basename == plan.md` and path is under `.humanize/rlcr`.
- edge_cases_or_failure_modes: The template is intentionally tiny and variable-free, so rendering risk is low. It is not a policy implementation by itself; enforcement depends on validator path detection. A `plan.md` outside `.humanize/rlcr` is not covered by this specific block.
- validation_or_tests: Indirectly covered by plan-file and template-reference tests, plus validator tests that exercise protected loop artifacts.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-165 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit template for the RLCR code-review phase. It documents the `codex review` invocation, expected issue-marker contract, and generated review artifacts.
- algorithmic_behavior: The stop hook renders this file as `round-N-review-prompt.md` before running `codex review` (`hooks/loop-codex-stop-hook.sh:1060-1085`). The template explicitly says `codex review` does not accept prompt input and reviews based on git diff (`prompt-template/codex/code-review-phase.md:3-5`). It records base branch, round, timestamp, expected `[P0-9]` issue markers, and generated command/log/result files (`:6-36`).
- inputs_outputs_state: Inputs are `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP`; the caller also passes `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE`, though this template currently does not render those extra variables (`hooks/loop-codex-stop-hook.sh:1077-1083`). Output is an audit markdown file in the loop directory. The actual review output goes to cache/log files and `round-N-review-result.md`.
- gates_or_invariants: The phase transition contract is: run `codex review --base <review_base>`, scan output for `[P0-9]`, continue remediation if markers exist, otherwise transition to finalize (`prompt-template/codex/code-review-phase.md:12-18`). Implementation confirms this: the command file records `codex review --base`, then `run_and_handle_code_review` treats review failure as blocking and uses issue detection to choose finalize vs. review loop (`hooks/loop-codex-stop-hook.sh:1096-1104`, `:1114-1124`, `hooks/lib/loop-common.sh:498-567`).
- dependencies_and_callers: Depends on template loader and the Codex stop hook review flow. Related implementation is `detect_review_issues`, which scans the last 50 log lines for `[P0-9]` near line starts and writes `round-N-review-result.md` (`hooks/lib/loop-common.sh:507-567`).
- edge_cases_or_failure_modes: Because it is an audit file, stale/missing text does not directly change review behavior as long as `codex review` command execution still works. A notable mismatch is that the caller passes base-commit/review-base variables not represented in the template; the fallback includes them, but the committed template does not. This is an audit completeness concern, not a runtime gate failure.
- validation_or_tests: Covered by finalize/review-phase tests and template-reference tests. The behavioral contract is exercised through stop-hook tests that verify review phase and finalize transitions.
- skip_candidate: `no`

### REFLECTION_IMPROVE-HZ-195 `file` `tests/robustness/test-pr-loop-api-poll.sh`
- cursor: `[_]`
- core_role: Split test entry point for the PR-loop polling/stop-hook robustness suite. It delegates to shared poll tests rather than containing the test bodies itself.
- algorithmic_behavior: Sources `tests/robustness/test-pr-loop-api-robustness.sh`, invokes `run_poll_tests`, prints a named summary, and exits with that result (`tests/robustness/test-pr-loop-api-poll.sh:11-18`). The delegated group covers Tests 12-19: stop hook behavior with no active loop, corrupted PR-loop state, approve-state path writability, `poll-pr-reviews.sh --help`, missing argument rejection, valid JSON output with mocked `gh`, slow API handling, and API-failure fallback (`tests/robustness/test-pr-loop-api-robustness.sh:560-855`).
- inputs_outputs_state: Inputs are temp repos, mocked `gh`, PR-loop state fixtures, and command-line args to `scripts/poll-pr-reviews.sh`. Outputs are pass/fail counters and process exit status. The PR-loop state under test lives in `.humanize/pr-loop/<timestamp>/state.md`; helper state creation includes `current_round`, `pr_number`, `configured_bots`, `active_bots`, `startup_case`, and `review_started` (`tests/robustness/test-pr-loop-api-robustness.sh:241-261`).
- gates_or_invariants: The wrapper enforces the poll subset as a standalone parallel shard. The delegated tests assert `pr-loop-stop-hook.sh` exits cleanly without an active loop (`:571-585`), handles corrupted state without crashing above signal-style exit codes (`:587-604`), and that `poll-pr-reviews.sh` must produce parseable JSON with boolean `has_new_comments`; on API failure, it must still exit `0` and return `has_new_comments: false` as a boolean (`:822-850`).
- dependencies_and_callers: Depends on `test-pr-loop-api-robustness.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/poll-pr-reviews.sh`, `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `jq`, `git`, and mocked `gh`. `poll-pr-reviews.sh` maps bot names to authors, resolves current or parent repo, retries API calls, filters issue comments/review comments/PR reviews by timestamp and bot pattern, and emits `{comments,bots_responded,has_new_comments,comment_count}` (`scripts/poll-pr-reviews.sh:23-117`, `:126-187`, `:200-326`).
- edge_cases_or_failure_modes: This assigned file is mostly a harness wrapper, so the direct algorithmic content is minimal. The delegated tests cover slow API responses (`tests/robustness/test-pr-loop-api-robustness.sh:688-753`), complete API failure with warnings before JSON (`:755-854`), corrupted state (`:587-604`), and missing args (`:643-655`). It does not run fetch-comment tests; those are the sibling split handled by `run_fetch_tests`.
- validation_or_tests: This file is a validation entry point. It should be considered core only as a shard launcher for `run_poll_tests`; the detailed executable specification resides in the sourced robustness file.
- skip_candidate: `yes: wrapper-only split runner; core behavior is in the sourced robustness test group and PR-loop scripts, but this file is still part of the executable test surface`

## Worker Self-Test
- assigned_items_seen: 7 unique item sections present above; no item section merged
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`