# agent_06 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts/` directory is the executable command and orchestration layer for Humanize RLCR, PR-loop, gen-plan, ask-codex, monitor, install, timeout, and GitHub-review helper workflows. I inspected it recursively with no depth cap: 17 shell files including `scripts/lib/monitor-common.sh` and `scripts/lib/monitor-skill.sh`.
- algorithmic_behavior: `setup-rlcr-loop.sh` initializes RLCR sessions, validates dependencies, validates plan paths/content/git state, determines base branch, writes `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-0-prompt.md`, and pending session-id signal state. The branch-specific dependency gate is explicit: it accumulates missing `codex`, `jq`, and `git`, then exits before loop setup with a combined actionable error (`scripts/setup-rlcr-loop.sh:257-286`). It also enforces one active loop (`scripts/setup-rlcr-loop.sh:292-313`), `--agent-teams` env gating (`scripts/setup-rlcr-loop.sh:319-330`), plan path safety (`scripts/setup-rlcr-loop.sh:389-475`), clean git working tree (`scripts/setup-rlcr-loop.sh:655-670`), base branch resolution (`scripts/setup-rlcr-loop.sh:677-744`), and state/prompt creation (`scripts/setup-rlcr-loop.sh:792-823`, `scripts/setup-rlcr-loop.sh:999-1080`). `setup-pr-loop.sh` initializes PR review loops: validates bot flags, prevents overlapping RLCR/PR loops, resolves fork/base PR context, fetches comments, determines startup case, optionally posts trigger comments, verifies Claude eyes reaction, then writes PR loop state, goal tracker, and round prompt (`scripts/setup-pr-loop.sh:186-238`, `scripts/setup-pr-loop.sh:283-379`, `scripts/setup-pr-loop.sh:430-602`, `scripts/setup-pr-loop.sh:615-635`, `scripts/setup-pr-loop.sh:721-851`). `ask-codex.sh` creates `.humanize/skill/<id>` and cache records, builds `codex exec`, applies timeout, persists stdout/stderr/debug metadata, and classifies timeout/error/empty/success (`scripts/ask-codex.sh:220-410`). PR helpers normalize GitHub API data: `fetch-pr-comments.sh` fetches issue comments, inline review comments, and PR reviews with retries, deduplicates, sorts human comments before bot comments, and writes markdown (`scripts/fetch-pr-comments.sh:183-220`, `scripts/fetch-pr-comments.sh:260-343`, `scripts/fetch-pr-comments.sh:345-450`); `poll-pr-reviews.sh` returns JSON for watched bot comments after a timestamp (`scripts/poll-pr-reviews.sh:299-326`); `check-pr-reviewer-status.sh` classifies cases 1-5 based on which bots commented and stale per-bot review timestamps (`scripts/check-pr-reviewer-status.sh:220-245`); `check-bot-reactions.sh` checks Codex thumbs-up and Claude eyes reactions with fork-aware repo resolution (`scripts/check-bot-reactions.sh:119-187`, `scripts/check-bot-reactions.sh:232-300`). `rlcr-stop-gate.sh` wraps the stop hook for non-hook skill mode, maps hook block decisions to exit 10 and allow to exit 0 (`scripts/rlcr-stop-gate.sh:84-134`). `cancel-rlcr-loop.sh` and `cancel-pr-loop.sh` implement user-requested loop cancellation by creating `.cancel-requested` and moving active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:148-169`, `scripts/cancel-pr-loop.sh:118-130`). `humanize.sh` exposes `humanize monitor rlcr|pr|skill`, parses goal/git state, and live-tails loop/cache files (`scripts/humanize.sh:24-216`, `scripts/humanize.sh:1103-1149`). `validate-gen-plan-io.sh` validates draft/output IO and combines the plan template with source draft (`scripts/validate-gen-plan-io.sh:79-159`). `install-skill.sh` syncs skill/runtime bundles for Kimi/Codex and hydrates runtime-root placeholders (`scripts/install-skill.sh:66-167`); `install-skills-codex.sh` and `install-skills-kimi.sh` are thin target wrappers (`scripts/install-skills-codex.sh:8-9`, `scripts/install-skills-kimi.sh:8-9`). `portable-timeout.sh` abstracts `gtimeout`, GNU `timeout`, Python timeout, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`).
- inputs_outputs_state: Inputs include CLI args, environment variables (`CLAUDE_PROJECT_DIR`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `HUMANIZE_CODEX_BYPASS_SANDBOX`, `XDG_CACHE_HOME`, `CODEX_HOME`), git repository state, plan files, GitHub PR/reaction APIs, and hook state files. Outputs include `.humanize/rlcr/<session>/state.md`, `finalize-state.md`, `cancel-state.md`, `goal-tracker.md`, prompts/summaries, `.humanize/pr-loop/<session>/state.md`, PR comment/prompt/check/feedback markdown, `.humanize/skill/<id>/input.md|output.md|metadata.md`, cache logs under `~/.cache/humanize`, rendered gen-plan files, and installer target skill directories. State transitions include RLCR inactive to active setup, implementation to review via stop hook, review to finalize through hook logic, active to cancelled via cancellation scripts, PR active to approve/cancel/maxiter via PR hook, and skill invocation running to success/error/timeout/empty metadata.
- gates_or_invariants: Dependency check before RLCR setup now requires `codex`, `jq`, and `git` (`scripts/setup-rlcr-loop.sh:263-286`). RLCR/PR loops are mutually exclusive (`scripts/setup-rlcr-loop.sh:292-313`, `scripts/setup-pr-loop.sh:208-238`). RLCR plan paths must be relative, no spaces/metacharacters, no symlink traversal, inside project, readable, not submodule-contained, and tracked/gitignored according to mode (`scripts/setup-rlcr-loop.sh:389-541`). RLCR requires clean git status before start (`scripts/setup-rlcr-loop.sh:655-670`). Branch/model/effort names are YAML-safety validated (`scripts/setup-rlcr-loop.sh:622-648`, `scripts/setup-pr-loop.sh:384-407`). PR loop requires bot flags, `gh`, authenticated `gh`, `codex`, valid PR, non-merged/non-closed PR, and numeric PR number (`scripts/setup-pr-loop.sh:186-277`, `scripts/setup-pr-loop.sh:351-374`). Force-push and stale-review cases are handled by sibling hooks using these scripts/templates. Timeout wrapper preserves exit 124 semantics for callers (`scripts/portable-timeout.sh:47-61`).
- dependencies_and_callers: Command files in `commands/` call `setup-rlcr-loop.sh`, `setup-pr-loop.sh`, cancellation scripts, and `validate-gen-plan-io.sh`. Skills in `skills/humanize*` call `setup-rlcr-loop.sh`, `rlcr-stop-gate.sh`, cancellation, PR setup, gen-plan, and ask-codex. Hooks call `portable-timeout.sh`, PR helper scripts, `check-bot-reactions.sh`, `poll-pr-reviews.sh`, and `check-pr-reviewer-status.sh` (for example `hooks/pr-loop-stop-hook.sh:676`, `hooks/pr-loop-stop-hook.sh:812`, `hooks/pr-loop-stop-hook.sh:1557`). Shared library dependencies are `hooks/lib/loop-common.sh` for state constants/defaults and loop helpers, `hooks/lib/template-loader.sh` for templates, `jq`, `git`, `gh`, `codex`, `find`, `sed`, `awk`, `stat`, and shell builtins.
- edge_cases_or_failure_modes: Missing dependency blocks setup before partial state creation. Git commands are wrapped with timeout in setup paths. Fork PRs are resolved to base repositories before API/reaction calls. API fetch failures in comment/poll helpers degrade to empty arrays plus warnings rather than crashing mid-loop. PR trigger logic avoids duplicate mentions and strips quoted/code contexts before detecting existing trigger comments (`scripts/setup-pr-loop.sh:467-503`). Claude eyes verification failure removes the just-created loop dir (`scripts/setup-pr-loop.sh:564-599`). Monitors handle empty directories, deleted sessions/logs, log truncation/rotation, small terminals, Bash/zsh differences, and cache fallback. `git status --short` could not be run here because this read-only export is not a git checkout and the sandbox blocked Xcode temp cache creation; no files were modified.
- validation_or_tests: Relevant tests include assigned `tests/test-humanize-escape.sh`, assigned `tests/robustness/test-cancel-security-robustness.sh`, plus `tests/test-plan-file-validation.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-ask-codex.sh`, `tests/test-stop-gate.sh`, `tests/test-agent-teams.sh`, `tests/test-gen-plan.sh`, `tests/test-monitor-runtime.sh`, `tests/test-skill-monitor.sh`, and robustness suites referencing these scripts. I did not run the full test suite because the branch export is read-only and several tests create temp repos/files.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-036 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse write-path validator for RLCR and PR-loop state machines. It prevents Claude from writing generated prompts/todos/state, wrong round files, protected goal trackers, plan backups, and PR-loop read-only artifacts.
- algorithmic_behavior: The script reads hook JSON from stdin, validates JSON structure through `validate_hook_input`, rejects nested JSON over depth 30, ignores non-`Write` tools, requires `.tool_input.file_path`, lowercases the path for matching, and extracts `session_id` for session-aware active-loop lookup (`hooks/loop-write-validator.sh:25-53`). It blocks round todos unless the path is allowlisted for the active loop (`hooks/loop-write-validator.sh:58-65`), blocks all round prompt writes (`hooks/loop-write-validator.sh:68-71`), and applies PR-loop protections: state writes blocked, generated PR comment/prompt/check/feedback files blocked, and `round-N-pr-resolve.md` round number validated (`hooks/loop-write-validator.sh:77-96`). For RLCR, it allows unrelated writes, locates the active loop, resolves `finalize-state.md` before `state.md`, parses state strictly, then uses `current_round` to gate protected writes (`hooks/loop-write-validator.sh:103-147`). It blocks `finalize-state.md` and `state.md` writes (`hooks/loop-write-validator.sh:154-162`), allows `finalize-summary.md` only at the exact active-loop path (`hooks/loop-write-validator.sh:170-174`), blocks `.humanize/rlcr/*/plan.md` backup writes (`hooks/loop-write-validator.sh:181-187`), blocks `goal-tracker.md` after round 0 (`hooks/loop-write-validator.sh:194-198`), blocks summaries outside `.humanize/rlcr`, validates summary round number, and finally enforces exact active-loop directory path (`hooks/loop-write-validator.sh:204-267`).
- inputs_outputs_state: Input is a Claude hook JSON payload with `tool_name`, `tool_input.file_path`, optional `session_id`, and environment `CLAUDE_PROJECT_DIR`. Output is exit 0 for allow, exit 1 for malformed/unsafe hook input or malformed loop state, and exit 2 with a rendered stderr block reason for policy blocks. It does not mutate state; it reads active loop state and template files.
- gates_or_invariants: Fail-closed on invalid JSON, missing `file_path`, deep JSON, malformed state frontmatter, missing required `current_round`/`max_iterations`/`review_started`/`base_branch`, and nonnumeric state values through `parse_state_file_strict` (`hooks/lib/loop-common.sh:392-464`). Loop-generated prompt/state files are system-owned. Goal tracker immutable section is only writable in round 0. Summary files must match current round and active session directory. PR loop state and generated review files are read-only; only the current `round-N-pr-resolve.md` is writable.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, active-loop lookup, state parsing, path classifiers, allowlist, and block-message renderers (`hooks/loop-write-validator.sh:18-19`). `loop-common.sh` depends on `jq`, `iconv` when available, and `hooks/lib/template-loader.sh` (`hooks/lib/loop-common.sh:91-188`). Block messages come from `prompt-template/block/*.md` via `load_and_render_safe`.
- edge_cases_or_failure_modes: No active loop means protected-looking RLCR paths generally allow except immediate prompt/todos checks. `finalize-state.md` is checked before generic `state.md` because its name also matches the generic state pattern (`hooks/loop-write-validator.sh:152-160`). Allowlisted early todos/summaries bypass the generic block (`hooks/lib/loop-common.sh:563-584`). Session filtering avoids a stale loop from another session being treated as active (`hooks/lib/loop-common.sh:230-306`). Lowercase matching improves case-insensitive detection but exact final path comparison uses the original `FILE_PATH`, so wrong capitalization or directory mismatch blocks.
- validation_or_tests: Covered by validator suites such as `tests/test-allowlist-validators.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-session-id.sh`, `tests/test-pr-loop-hooks.sh`, and robustness hook-input tests. Assigned `tests/test-humanize-escape.sh` also validates shared path-protection helpers used by validators.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-066 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for path-escape and shell/glob safety behavior used by Humanize validators and monitors, especially `.humanize` git-add protection and zsh-safe directory iteration.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, defines `assert_blocks` and `assert_allows`, lowercases command strings with `to_lower`, and invokes production `git_adds_humanize` (`tests/test-humanize-escape.sh:16-69`). It verifies blocking for direct `.humanize` path variants including `./.humanize`, trailing slash, nested file, path prefixes, parent-relative path, quoted paths, `-f`/`--force`, broad-scope force adds, combined `-fA`/`-Af`, `-A`/`--all` when `.humanize` exists, chained shell segments, and `git -C`/`--git-dir` invocations (`tests/test-humanize-escape.sh:77-167`). It verifies allowed commands for specific files, `git status/diff/log`, patch mode, and similarly named non-directory files like `.humanizeconfig` (`tests/test-humanize-escape.sh:169-192`). It then tests find-based iteration patterns for empty dirs, dotfiles-only dirs, no `*-state.md`, terminal state detection, and latest timestamped session directory detection (`tests/test-humanize-escape.sh:195-332`).
- inputs_outputs_state: Inputs are synthetic command strings and temporary directories under `/tmp`. Output is colored PASS/FAIL lines, counters `TESTS_PASSED`/`TESTS_FAILED`, and process exit 0 only when no tests fail (`tests/test-humanize-escape.sh:337-352`). Temporary `.humanize` dirs are created only under `/tmp` and removed during the test.
- gates_or_invariants: `.humanize/` loop state must not be staged by direct path, quoted path, path variant, broad force add, or all-add commands when present. Specific non-loop paths must not be overblocked. Find-based scanning must not use unsafe shell globs that produce zsh “no matches found” failures. Latest session detection must only consider timestamp-shaped directories and ignore dotfiles.
- dependencies_and_callers: Depends on production `git_adds_humanize`, `to_lower`, and related shell parsing in `hooks/lib/loop-common.sh:1051-1169`. Uses `find`, `mkdir`, `touch`, `rm`, and `/tmp`. It is part of the general shell test suite and complements Bash validator tests for `.humanize` protections.
- edge_cases_or_failure_modes: Explicitly covers quoted `.humanize`, path-containing `.humanize` segments, force flags that bypass gitignore, combined flags after lowercasing, chained shell operators, `git -C`, empty directory iteration, dotfiles-only directories, absence of state files, and lexicographic timestamp ordering. It avoids relying on glob expansion, which is the failure mode it guards against.
- validation_or_tests: This file is itself the validation artifact. It validates the production helper whose logic blocks `.humanize` staging in Bash validators and protects local loop state from accidental commits.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-096 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: PR-loop block template for the force-push/non-fast-forward transition. It tells the agent that review state was reset and a fresh bot trigger comment is required.
- algorithmic_behavior: The template renders `OLD_COMMIT`, `NEW_COMMIT`, and `BOT_MENTION_STRING`, states that commit history was rewritten, lists required actions, and provides the trigger comment body (`prompt-template/block/force-push-detected.md:1-17`). The actual transition is in `hooks/pr-loop-stop-hook.sh`: on force-push detection it preserves the old SHA, obtains the new HEAD commit timestamp, rewrites `latest_commit_sha`, `latest_commit_at`, clears `last_trigger_at` and `trigger_comment_id`, then renders this template and returns a JSON block decision (`hooks/pr-loop-stop-hook.sh:380-421`).
- inputs_outputs_state: Inputs are old/new commit SHAs and the active bot mention string. Output is a rendered markdown reason embedded in hook JSON with system message “PR Loop: Force push detected - please re-trigger bots” (`hooks/pr-loop-stop-hook.sh:419-420`). State side effects occur in the caller, not the template: PR loop state is updated to the new commit and trigger fields are cleared (`hooks/pr-loop-stop-hook.sh:396-410`).
- gates_or_invariants: A rewritten PR history invalidates previous review/trigger state. After force push, the agent must post a new trigger comment mentioning all active bots before the PR loop can continue. Stale `trigger_comment_id` is cleared so old Claude eyes checks cannot satisfy the new cycle.
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` through `load_and_render_safe` with an inline fallback if the template cannot load (`hooks/pr-loop-stop-hook.sh:412-417`). Depends on template-loader substitution semantics and PR-loop state fields written by `scripts/setup-pr-loop.sh`.
- edge_cases_or_failure_modes: If the new commit timestamp cannot be fetched from GitHub, the caller falls back to current UTC time (`hooks/pr-loop-stop-hook.sh:391-394`). If the template is unavailable, the fallback still blocks and asks for a new @bot trigger. The template passes `PR_NUMBER` from the caller but does not currently use it.
- validation_or_tests: PR-loop stop-hook tests reference force-push handling; template integrity is also covered by template reference/comprehensive tests. The concrete caller block at `hooks/pr-loop-stop-hook.sh:380-421` is the executable behavior tied to this prompt.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-126 `file` `prompt-template/claude/agent-teams-core.md`
- cursor: `[_]`
- core_role: Static prompt contract for RLCR Agent Teams mode. It changes Claude’s role from implementer to coordinator/team leader when `--agent-teams` is enabled.
- algorithmic_behavior: The template instructs the leader to never write code or edit files directly, split work into independent units, spawn team members with the Task tool and `team_name`, coordinate ownership, monitor progress, wait for teammates, and review outputs (`prompt-template/claude/agent-teams-core.md:1-13`). Its guidelines define task splitting, cold-start context requirements, strict file conflict prevention, coordination, quality review, per-member commits, and optional plan approval for high-risk work (`prompt-template/claude/agent-teams-core.md:14-23`). The Important section reiterates Task-tool usage, reassignment, merge/conflict resolution, no direct coding, and idle teammate semantics (`prompt-template/claude/agent-teams-core.md:24-30`).
- inputs_outputs_state: The template has no dynamic placeholders. It is appended to `round-0-prompt.md` by `scripts/setup-rlcr-loop.sh` when `AGENT_TEAMS=true` and both header/core templates exist (`scripts/setup-rlcr-loop.sh:1027-1036`). It is also appended to later implementation-round prompts by `hooks/loop-codex-stop-hook.sh` when `AGENT_TEAMS=true` and `REVIEW_STARTED` is not true (`hooks/loop-codex-stop-hook.sh:1645-1655`). State input is `agent_teams: true` in RLCR state (`scripts/setup-rlcr-loop.sh:807-810`), plus the setup-time environment gate `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`scripts/setup-rlcr-loop.sh:319-330`).
- gates_or_invariants: The invariant is strict delegation: the leader must not directly implement. File ownership boundaries are required because same-file parallel edits can silently overwrite. Dependent same-file tasks should be sequenced using `blockedBy`. Team member output must be reviewed before completion. The continuation caller intentionally excludes this guidance from review phase prompts (`hooks/loop-codex-stop-hook.sh:1645-1647`).
- dependencies_and_callers: Primary callers are `scripts/setup-rlcr-loop.sh` and `hooks/loop-codex-stop-hook.sh`. Tests in `tests/test-agent-teams.sh` validate env gating, state field persistence/defaulting, parse-state support, initial prompt inclusion, template existence/content, implementation-phase continuation inclusion, and review-phase exclusion (`tests/test-agent-teams.sh:32-152`, `tests/test-agent-teams.sh:212-260`).
- edge_cases_or_failure_modes: Cold-start team members do not inherit the leader conversation, so task prompts must include relevant goals and paths. Parallel same-file edits risk silent data loss rather than merge conflicts. If templates are missing, setup and continuation callers have fallback team-leader instructions, but the full guidelines are reduced (`scripts/setup-rlcr-loop.sh:1037-1046`, `hooks/loop-codex-stop-hook.sh:1656-1664`).
- validation_or_tests: `tests/test-agent-teams.sh` is the main executable spec; it checks template file size/content and the prompt/state behavior around `--agent-teams`. Template reference tests also cover prompt-template availability.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-156 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for secure RLCR cancel authorization, especially guarding the `state.md` or `finalize-state.md` to `cancel-state.md` transition against spoofed Bash commands and path bypasses.
- algorithmic_behavior: The test sources production `hooks/lib/loop-common.sh` and test helpers, creates a mock active loop with `state.md`, and directly exercises `is_cancel_authorized` using lowercased command strings (`tests/robustness/test-cancel-security-robustness.sh:15-31`). Positive cases require `.cancel-requested` and accept exact `mv state.md cancel-state.md`, `mv finalize-state.md cancel-state.md`, single-quoted paths, and unquoted paths (`tests/robustness/test-cancel-security-robustness.sh:39-89`). Negative cases reject missing signal file, command substitution, backticks, semicolon/AND/OR/pipe chaining, wrong destination, wrong source, extra args, newline injection, variable expansion, non-`mv`, mixed quotes, IFS manipulation, multiple trailing spaces, nonstandard source names, symlinked `state.md`, symlinked `finalize-state.md`, and regression paths where the loop directory name contains `finalize` (`tests/robustness/test-cancel-security-robustness.sh:99-405`). It prints a summary and exits through `print_test_summary` (`tests/robustness/test-cancel-security-robustness.sh:407-412`).
- inputs_outputs_state: Inputs are temporary loop files, `.cancel-requested` signal presence, symlink fixtures, and candidate shell command strings. Output is PASS/FAIL accounting from test helpers and exit status. The test mutates only its temp test directory: creates/removes state files, symlinks, and signal files.
- gates_or_invariants: Cancel authorization is not just “a command mentions cancel-state”; it requires an explicit signal file, must be a single `mv` with exactly two args, source must be exactly active `state.md` or `finalize-state.md`, destination must be exactly `cancel-state.md`, no shell operators or variable expansion remain after loop-dir normalization, no mixed quote styles, no suspicious trailing spaces, and source cannot be a symlink. Production helper documents these return classes at `hooks/lib/loop-common.sh:676-694` and implements them at `hooks/lib/loop-common.sh:700-852`.
- dependencies_and_callers: Depends on `is_cancel_authorized` in `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `ln`, `touch`, and temp directory helpers. The production cancellation script creates `.cancel-requested` before moving state (`scripts/cancel-rlcr-loop.sh:148-155`); Bash validators can use `is_cancel_authorized` to distinguish that legitimate scripted transition from agent-initiated state tampering.
- edge_cases_or_failure_modes: Explicitly covers shell injection forms, quote parsing ambiguity, path normalization with `/./`, unexpanded shell variables, symlink swaps, wrong source/destination, extra arguments, newline command injection, and a previous class of bug where a loop directory containing the word `finalize` could misclassify `state.md` as `finalize-state.md` during symlink checks (`tests/robustness/test-cancel-security-robustness.sh:369-405`).
- validation_or_tests: This file is itself the validation artifact for cancel-security behavior. It maps closely to production helper logic in `hooks/lib/loop-common.sh:676-852` and cancellation state movement in `scripts/cancel-rlcr-loop.sh:148-155`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-006`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-036`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-066`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-096`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-126`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-156`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`