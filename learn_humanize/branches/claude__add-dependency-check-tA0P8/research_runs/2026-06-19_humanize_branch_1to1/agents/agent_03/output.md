# agent_03 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command interface layer for the humanize workflows. The directory has no subdirectories and contains five command specs: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, `commands/start-pr-loop.md`, and `commands/cancel-pr-loop.md`. These files do not implement shell algorithms directly; they define command metadata, allowed tools, required argument shape, operator-facing phase rules, and delegation into scripts under `scripts/`.
- algorithmic_behavior: `start-rlcr-loop.md` delegates to `scripts/setup-rlcr-loop.sh` and defines the RLCR state machine at the user-command level: implementation phase, Codex summary review, review phase via `codex review --base <branch>`, then finalize phase when review issues are clear (`commands/start-rlcr-loop.md:16`, `commands/start-rlcr-loop.md:22`, `commands/start-rlcr-loop.md:25`, `commands/start-rlcr-loop.md:60`). `cancel-rlcr-loop.md` delegates cancellation and has a finalize-phase confirmation branch keyed by `FINALIZE_NEEDS_CONFIRM` before force-cancel (`commands/cancel-rlcr-loop.md:17`, `commands/cancel-rlcr-loop.md:21`, `commands/cancel-rlcr-loop.md:23`). `start-pr-loop.md` delegates to `scripts/setup-pr-loop.sh` and defines the PR bot review loop: detect PR, fetch bot comments, fix, push, tag bots, poll every 30s for 15 minutes, and use local Codex validation (`commands/start-pr-loop.md:16`, `commands/start-pr-loop.md:22`, `commands/start-pr-loop.md:23`). `cancel-pr-loop.md` delegates PR cancellation and treats `state.md` in newest `.humanize/pr-loop/` as active (`commands/cancel-pr-loop.md:17`, `commands/cancel-pr-loop.md:21`). `gen-plan.md` is a plan-generation workflow: IO validation, relevance check, draft analysis, issue clarification, structured plan generation, and output review (`commands/gen-plan.md:19`, `commands/gen-plan.md:30`, `commands/gen-plan.md:50`, `commands/gen-plan.md:76`, `commands/gen-plan.md:111`, `commands/gen-plan.md:142`, `commands/gen-plan.md:251`).
- inputs_outputs_state: Inputs are slash-command arguments (`$ARGUMENTS`) and optional flags such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, and PR bot flags (`commands/start-rlcr-loop.md:3`, `commands/start-pr-loop.md:3`). Outputs are initialized loop directories and state created by setup scripts; for RLCR, backing script state includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, `ask_codex_question`, `session_id`, `agent_teams`, and `started_at` (`scripts/setup-rlcr-loop.sh:792`). For PR loops, backing state includes `current_round`, `max_iterations`, `pr_number`, `start_branch`, bot lists, Codex settings, poll settings, startup metadata, and trigger metadata (`scripts/setup-pr-loop.sh:615`). `gen-plan` validates and then writes a template-plus-draft output file via `scripts/validate-gen-plan-io.sh` (`scripts/validate-gen-plan-io.sh:148`, `scripts/validate-gen-plan-io.sh:151`).
- gates_or_invariants: Command specs enforce tool boundaries through frontmatter `allowed-tools`, e.g. setup/cancel scripts only for loop commands and validation/read/write/task/user-question tools for `gen-plan` (`commands/start-rlcr-loop.md:4`, `commands/cancel-rlcr-loop.md:3`, `commands/gen-plan.md:4`). RLCR command invariants include always writing summaries, maintaining `goal-tracker.md`, no manual state-file or cancel-command cheating, and relying on Codex/stop-hook transitions (`commands/start-rlcr-loop.md:46`). Review success requires no `[P0-9]` markers before finalize (`commands/start-rlcr-loop.md:24`, `commands/start-rlcr-loop.md:57`). PR command invariants include required bot flags, human comments before bot comments, pushing changes, tagging configured bots, and preserving cancellation through script-managed state (`commands/start-pr-loop.md:25`, `commands/start-pr-loop.md:31`, `commands/start-pr-loop.md:50`). `gen-plan` gates include hard IO validation exit codes 1-7 (`commands/gen-plan.md:38`), repository relevance (`commands/gen-plan.md:50`), explicit confirmation of quantitative metrics (`commands/gen-plan.md:126`), preserving all draft content (`commands/gen-plan.md:247`), and using `Edit` rather than `Write` after the validator seeds the output (`commands/gen-plan.md:257`).
- dependencies_and_callers: Parent caller is the Claude slash-command system. Script dependencies are `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, and `scripts/validate-gen-plan-io.sh` (`commands/start-rlcr-loop.md:13`, `commands/cancel-rlcr-loop.md:14`, `commands/start-pr-loop.md:13`, `commands/cancel-pr-loop.md:14`, `commands/gen-plan.md:35`). RLCR command behavior coordinates with hooks under `hooks/`, especially `loop-codex-stop-hook.sh` and validators, because the command initializes state that stop hooks later consume. `gen-plan` coordinates with `prompt-template/plan/gen-plan-template.md`, `Task` agents, and final `Edit` operations.
- edge_cases_or_failure_modes: `cancel-rlcr-loop` has a special finalize-phase hazard: normal cancellation returns `FINALIZE_NEEDS_CONFIRM`; only explicit user confirmation runs `--force`, which renames active state to `cancel-state.md` (`commands/cancel-rlcr-loop.md:21`, `scripts/cancel-rlcr-loop.sh:131`, `scripts/cancel-rlcr-loop.sh:155`). `start-rlcr-loop --skip-impl` makes plan optional and starts directly in review path (`commands/start-rlcr-loop.md:69`). `gen-plan` stops on unrelated drafts, unresolved significant issues, invalid/missing IO, existing output, no output write permission, or missing template (`commands/gen-plan.md:38`, `commands/gen-plan.md:67`, `commands/gen-plan.md:296`). `start-pr-loop` can stop by max iteration, all monitored bots approving, or explicit cancel (`commands/start-pr-loop.md:58`); its backing setup checks Claude bot “eyes” response when Claude monitoring is enabled (`scripts/setup-pr-loop.sh:564`, `scripts/setup-pr-loop.sh:587`).
- validation_or_tests: Command behavior is covered indirectly by workflow tests rather than tests inside `commands/`: RLCR finalize behavior by `tests/test-finalize-phase.sh`, plan hooks by `tests/test-plan-file-hooks.sh`, gen-plan by `tests/test-gen-plan.sh`, and PR loop by `tests/test-pr-loop*.sh` from the repository inventory. Backing scripts provide executable validations, e.g. `validate-gen-plan-io.sh` maps deterministic exit codes and writes the seeded output (`scripts/validate-gen-plan-io.sh:4`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:142`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-033 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit hook validator for active RLCR loops. It enforces loop state schema, branch consistency, and plan-file tracking invariants before accepting user prompt submissions during normal or finalize RLCR states (`hooks/loop-plan-file-validator.sh:3`, `hooks/loop-plan-file-validator.sh:5`).
- algorithmic_behavior: The validator reads hook JSON from stdin, extracts `session_id`, resolves the session-scoped active loop under `.humanize/rlcr`, and exits allow if no active loop exists (`hooks/loop-plan-file-validator.sh:26`, `hooks/loop-plan-file-validator.sh:30`, `hooks/loop-plan-file-validator.sh:33`, `hooks/loop-plan-file-validator.sh:37`). It resolves `finalize-state.md` before `state.md`, parses with `parse_state_file_strict`, maps state fields, then checks required schema fields `plan_tracked` and `start_branch` (`hooks/loop-plan-file-validator.sh:41`, `hooks/loop-plan-file-validator.sh:45`, `hooks/loop-plan-file-validator.sh:50`, `hooks/loop-plan-file-validator.sh:79`). It verifies the current git branch equals the state’s `start_branch` (`hooks/loop-plan-file-validator.sh:95`, `hooks/loop-plan-file-validator.sh:107`). Finally, it branches on `plan_tracked`: tracked plans must still be tracked and clean; untracked plans must remain untracked/gitignored (`hooks/loop-plan-file-validator.sh:121`, `hooks/loop-plan-file-validator.sh:123`, `hooks/loop-plan-file-validator.sh:174`, `hooks/loop-plan-file-validator.sh:184`, `hooks/loop-plan-file-validator.sh:193`, `hooks/loop-plan-file-validator.sh:220`).
- inputs_outputs_state: Input is a UserPromptSubmit hook JSON payload from stdin; only `session_id` is used for loop filtering (`hooks/loop-plan-file-validator.sh:27`, `hooks/loop-plan-file-validator.sh:30`). State inputs are `.humanize/rlcr/<timestamp>/state.md` or `.humanize/rlcr/<timestamp>/finalize-state.md`, parsed into `STATE_PLAN_TRACKED`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH` (`hooks/loop-plan-file-validator.sh:42`, `hooks/loop-plan-file-validator.sh:51`). Git inputs are `git rev-parse --abbrev-ref HEAD`, `git ls-files --error-unmatch <plan>`, and `git status --porcelain <plan>` with 30-second timeout (`hooks/loop-plan-file-validator.sh:23`, `hooks/loop-plan-file-validator.sh:96`, `hooks/loop-plan-file-validator.sh:127`, `hooks/loop-plan-file-validator.sh:152`). Outputs are either no output with exit 0 for allow, or Claude hook JSON `{"decision":"block","reason":...}` with exit 0 for policy blocks; malformed strict state exits 1 as a hard validator error (`hooks/loop-plan-file-validator.sh:37`, `hooks/loop-plan-file-validator.sh:46`, `hooks/loop-plan-file-validator.sh:71`, `hooks/loop-plan-file-validator.sh:99`).
- gates_or_invariants: Fail-closed on malformed state, git timeouts, and unexpected git errors (`hooks/loop-plan-file-validator.sh:45`, `hooks/loop-plan-file-validator.sh:129`, `hooks/loop-plan-file-validator.sh:138`, `hooks/loop-plan-file-validator.sh:154`, `hooks/loop-plan-file-validator.sh:163`, `hooks/loop-plan-file-validator.sh:199`, `hooks/loop-plan-file-validator.sh:208`). Schema requires `plan_tracked` and `start_branch` in addition to strict parser requirements such as `current_round`, `max_iterations`, `review_started`, and `base_branch` inherited from `parse_state_file_strict` (`hooks/loop-plan-file-validator.sh:79`, `hooks/lib/loop-common.sh:401`, `hooks/lib/loop-common.sh:421`, `hooks/lib/loop-common.sh:430`, `hooks/lib/loop-common.sh:434`). Branch switching is forbidden during an active loop (`hooks/loop-plan-file-validator.sh:107`). Tracked plan mode forbids removal from git tracking and uncommitted plan modifications (`hooks/loop-plan-file-validator.sh:174`, `hooks/loop-plan-file-validator.sh:184`). Untracked plan mode forbids the plan file becoming tracked (`hooks/loop-plan-file-validator.sh:220`).
- dependencies_and_callers: Called by Claude Code hook configuration for UserPromptSubmit. Depends on `hooks/lib/loop-common.sh` for `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, field constants, and templated messages (`hooks/loop-plan-file-validator.sh:17`). Depends on `scripts/portable-timeout.sh` for bounded git operations (`hooks/loop-plan-file-validator.sh:20`). Uses `jq` to escape/render block reasons (`hooks/loop-plan-file-validator.sh:69`). The active-loop semantics come from `find_active_loop`: newest active loop only, or newest loop for the matching session if a session filter exists (`hooks/lib/loop-common.sh:230`, `hooks/lib/loop-common.sh:248`).
- edge_cases_or_failure_modes: No active loop allows prompt submission (`hooks/loop-plan-file-validator.sh:36`). A finalize phase still validates through `finalize-state.md` because active state resolution prefers it (`hooks/loop-plan-file-validator.sh:41`, `hooks/lib/loop-common.sh:195`). Missing schema fields produce a rendered schema-outdated block, not a silent default (`hooks/loop-plan-file-validator.sh:59`, `hooks/loop-plan-file-validator.sh:85`). Git branch lookup failure or timeout blocks because branch consistency cannot be proven (`hooks/loop-plan-file-validator.sh:98`). `git ls-files` exit 1 is a normal “not tracked” result; exit 124 is timeout; any other nonzero exit is unexpected and blocked (`hooks/loop-plan-file-validator.sh:126`, `hooks/loop-plan-file-validator.sh:129`, `hooks/loop-plan-file-validator.sh:138`). A tracked plan with any porcelain status, including staged or unstaged changes, blocks (`hooks/loop-plan-file-validator.sh:184`).
- validation_or_tests: `tests/test-finalize-phase.sh` explicitly verifies this validator parses `finalize-state.md` successfully (`tests/test-finalize-phase.sh:853`, `tests/test-finalize-phase.sh:857`, `tests/test-finalize-phase.sh:861`). `tests/test-plan-file-hooks.sh` covers missing schema, branch mismatch, quoted `start_branch`, quoted `plan_file`, tracked plan modifications, plan deletion, and outdated schema paths (`tests/test-plan-file-hooks.sh:150`, `tests/test-plan-file-hooks.sh:194`, `tests/test-plan-file-hooks.sh:373`, `tests/test-plan-file-hooks.sh:399`, `tests/test-plan-file-hooks.sh:425`, `tests/test-plan-file-hooks.sh:509`, `tests/test-plan-file-hooks.sh:549`, `tests/test-plan-file-hooks.sh:673`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-063 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR finalize phase and its integration with validators, stop hook, state naming, code-review gating, summary/todo/git-clean gates, and terminal-state detection. It is a test harness, but it defines expected core algorithm behavior for finalize transitions (`tests/test-finalize-phase.sh:3`, `tests/test-finalize-phase.sh:5`, `tests/test-finalize-phase.sh:12`).
- algorithmic_behavior: The test creates an isolated git repo, mock `codex` executables, a loop directory, state frontmatter, plan backup, and goal tracker (`tests/test-finalize-phase.sh:40`, `tests/test-finalize-phase.sh:53`, `tests/test-finalize-phase.sh:77`, `tests/test-finalize-phase.sh:100`, `tests/test-finalize-phase.sh:123`, `tests/test-finalize-phase.sh:144`, `tests/test-finalize-phase.sh:180`). It verifies helper path classifiers for `finalize-state.md` and `finalize-summary.md` (`tests/test-finalize-phase.sh:238`, `tests/test-finalize-phase.sh:260`). It asserts active-loop resolution treats `finalize-state.md` as active but `complete-state.md` as terminal/inactive (`tests/test-finalize-phase.sh:290`, `tests/test-finalize-phase.sh:293`, `tests/test-finalize-phase.sh:305`, `tests/test-finalize-phase.sh:314`). It exercises write/edit/bash validators to allow `finalize-summary.md` while blocking direct or shell-mediated modification/copy/move of `finalize-state.md` (`tests/test-finalize-phase.sh:345`, `tests/test-finalize-phase.sh:357`, `tests/test-finalize-phase.sh:369`, `tests/test-finalize-phase.sh:381`, `tests/test-finalize-phase.sh:393`, `tests/test-finalize-phase.sh:405`). It then exercises stop-hook finalize completion and implementation-to-review-to-finalize transitions (`tests/test-finalize-phase.sh:418`, `tests/test-finalize-phase.sh:524`, `tests/test-finalize-phase.sh:563`, `tests/test-finalize-phase.sh:622`, `tests/test-finalize-phase.sh:699`, `tests/test-finalize-phase.sh:775`).
- inputs_outputs_state: Inputs are temporary state files with frontmatter fields `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `review_started`, and `started_at` (`tests/test-finalize-phase.sh:190`). Stop-hook inputs are minimal JSON such as `{"stop_hook_active": false, "transcript": []}` or `{"transcript_path": ...}` (`tests/test-finalize-phase.sh:434`, `tests/test-finalize-phase.sh:722`, `tests/test-finalize-phase.sh:776`). Expected outputs include block JSON for missing finalize summary, dirty git, incomplete todos, review failures, empty review output, and normal feedback; allowed exit with state rename to `complete-state.md` after finalize summary and clean git pass (`tests/test-finalize-phase.sh:440`, `tests/test-finalize-phase.sh:467`, `tests/test-finalize-phase.sh:487`, `tests/test-finalize-phase.sh:590`, `tests/test-finalize-phase.sh:648`, `tests/test-finalize-phase.sh:729`, `tests/test-finalize-phase.sh:785`). State transitions asserted: `state.md -> finalize-state.md` when implementation COMPLETE and code review is clean; `finalize-state.md -> complete-state.md` when finalize checks pass; `state.md -> maxiter-state.md` when COMPLETE occurs at max iteration; `state.md` remains with `review_started: true` on review failure or empty review output (`tests/test-finalize-phase.sh:531`, `tests/test-finalize-phase.sh:489`, `tests/test-finalize-phase.sh:555`, `tests/test-finalize-phase.sh:609`, `tests/test-finalize-phase.sh:686`).
- gates_or_invariants: Finalize phase never invokes `codex`; it only enforces summary, git-clean, and todo completion before terminal rename (`tests/test-finalize-phase.sh:446`, `tests/test-finalize-phase.sh:498`, `hooks/loop-codex-stop-hook.sh:778`, `hooks/loop-codex-stop-hook.sh:783`). Finalize still requires `finalize-summary.md` (`tests/test-finalize-phase.sh:429`), clean git (`tests/test-finalize-phase.sh:454`), and completed todos from transcript (`tests/test-finalize-phase.sh:715`, `tests/test-finalize-phase.sh:721`). Review phase must block on `codex review` nonzero exit or empty stdout and must not skip into finalize (`tests/test-finalize-phase.sh:566`, `tests/test-finalize-phase.sh:588`, `tests/test-finalize-phase.sh:625`, `tests/test-finalize-phase.sh:647`). Normal non-COMPLETE implementation rounds remain unaffected: hook blocks with Codex feedback, leaves `state.md`, increments `current_round`, and writes `round-N-review-result.md` (`tests/test-finalize-phase.sh:781`, `tests/test-finalize-phase.sh:791`, `tests/test-finalize-phase.sh:799`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for helper functions (`tests/test-finalize-phase.sh:48`). Calls production validators `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and stop hook `hooks/loop-codex-stop-hook.sh` (`tests/test-finalize-phase.sh:348`, `tests/test-finalize-phase.sh:372`, `tests/test-finalize-phase.sh:384`, `tests/test-finalize-phase.sh:436`, `tests/test-finalize-phase.sh:843`, `tests/test-finalize-phase.sh:857`). Uses a mock `codex` on PATH to simulate implementation review output and code-review output (`tests/test-finalize-phase.sh:53`, `tests/test-finalize-phase.sh:77`, `tests/test-finalize-phase.sh:102`, `tests/test-finalize-phase.sh:125`).
- edge_cases_or_failure_modes: Missing summary blocks before Codex could be called (`tests/test-finalize-phase.sh:429`, `tests/test-finalize-phase.sh:446`). Dirty git blocks even in finalize phase (`tests/test-finalize-phase.sh:454`). Incomplete todos block and still do not invoke Codex (`tests/test-finalize-phase.sh:721`, `tests/test-finalize-phase.sh:735`). Max iteration takes precedence over finalize entry when current round equals max (`tests/test-finalize-phase.sh:542`, `tests/test-finalize-phase.sh:555`). Review failure and empty review output preserve `state.md` and set/keep `review_started: true` for retry rather than terminalizing incorrectly (`tests/test-finalize-phase.sh:609`, `tests/test-finalize-phase.sh:686`). Validators must parse `finalize-state.md` without treating it as terminal or malformed (`tests/test-finalize-phase.sh:826`, `tests/test-finalize-phase.sh:839`, `tests/test-finalize-phase.sh:853`).
- validation_or_tests: This file is itself the validation artifact and exits with the number of failed tests (`tests/test-finalize-phase.sh:867`, `tests/test-finalize-phase.sh:876`). It includes positive cases T-POS-1 through T-POS-5 and negative cases T-NEG-1 through T-NEG-9 in comments and executable assertions (`tests/test-finalize-phase.sh:5`, `tests/test-finalize-phase.sh:12`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-093 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Prompt/block template for PR-loop setup failure when the Claude GitHub bot does not acknowledge a trigger comment with an “eyes” reaction after bounded retries. It is part of algorithmic operator feedback for a remote-bot readiness gate, not a standalone algorithm (`prompt-template/block/claude-eyes-timeout.md:1`, `prompt-template/block/claude-eyes-timeout.md:3`).
- algorithmic_behavior: The template renders retry count and total wait seconds, then gives likely causes and required remediation actions (`prompt-template/block/claude-eyes-timeout.md:3`, `prompt-template/block/claude-eyes-timeout.md:5`, `prompt-template/block/claude-eyes-timeout.md:10`). In the backing PR setup flow, Claude monitoring requires verifying the eyes reaction with `check-bot-reactions.sh claude-eyes` using 3 retries and 5-second delay; failure is an error that prevents setup from silently proceeding (`scripts/setup-pr-loop.sh:564`, `scripts/setup-pr-loop.sh:587`).
- inputs_outputs_state: Inputs are template variables `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}` (`prompt-template/block/claude-eyes-timeout.md:3`). Output is Markdown shown to the user/operator explaining the failed gate and expected corrective action. It does not mutate state itself; the associated setup script determines whether PR-loop state creation can continue.
- gates_or_invariants: The invariant is that a Claude-monitored PR loop must not assume Claude bot participation unless the bot reacts to the trigger. The rendered text directs verification of app installation, permissions, and PR state (`prompt-template/block/claude-eyes-timeout.md:10`). It also directs posting a new trigger comment after configuration is corrected (`prompt-template/block/claude-eyes-timeout.md:18`).
- dependencies_and_callers: Intended caller is the template loader through PR-loop setup or related GitHub-bot readiness logic. The concrete dependency path found is `scripts/setup-pr-loop.sh`, which performs the eyes reaction check; template rendering is supported by `hooks/lib/template-loader.sh` and `load_and_render_safe` patterns used across the repo.
- edge_cases_or_failure_modes: Covers missing GitHub App installation, Claude service downtime, and missing repository permissions (`prompt-template/block/claude-eyes-timeout.md:5`). Another failure mode appears in setup if `gh` cannot determine the trigger comment ID, which blocks verification of the eyes reaction (`scripts/setup-pr-loop.sh:573`).
- validation_or_tests: Template reference coverage exists in broad template tests from the repository inventory, especially `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`. PR setup behavior is covered by PR-loop test suites. This specific template has no executable logic beyond placeholder rendering.
- skip_candidate: `yes: template-only evidence block; core-adjacent because it defines the failure contract for the PR-loop Claude-eyes readiness gate, but it does not implement state transitions itself`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-123 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Prompt/block template for wrong-round file access. It communicates a state-machine invariant: the actor must use files for the current loop round and must not manually increment round numbers (`prompt-template/block/wrong-round-number.md:1`, `prompt-template/block/wrong-round-number.md:7`).
- algorithmic_behavior: The template renders the attempted action, attempted Claude round, file type, current round, and correct path (`prompt-template/block/wrong-round-number.md:3`, `prompt-template/block/wrong-round-number.md:5`). The related common-library logic validates round-scoped file paths and emits wrong-round messaging when a user tries to read/write/edit the wrong `round-N-*` file; the PR-loop branch has explicit wrong-round handling for `round-<current>-pr-resolve.md` (`hooks/lib/loop-common.sh:981`, `hooks/lib/loop-common.sh:1004`, `hooks/lib/loop-common.sh:1011`, `hooks/lib/loop-common.sh:1021`).
- inputs_outputs_state: Inputs are template variables `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-round-number.md:3`, `prompt-template/block/wrong-round-number.md:5`). Output is Markdown used as a block/error message. It depends on state-derived `current_round`, but does not mutate state.
- gates_or_invariants: The invariant is round monotonicity controlled by the loop, not by the assistant. The last line is the key gate: “Do NOT increment the round number yourself” (`prompt-template/block/wrong-round-number.md:7`). This prevents accidental bypass of `current_round` state transitions in validators and stop hooks.
- dependencies_and_callers: Template is loaded by shared validator/message code via `load_and_render_safe` patterns. The state source is active loop state resolved by common functions and parsed for `current_round`; PR-loop-specific code extracts `current_round` from `state.md` before validating resolve-file access (`hooks/lib/loop-common.sh:1004`, `hooks/lib/loop-common.sh:1011`). Related validators are `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh`.
- edge_cases_or_failure_modes: Handles user attempts to operate on future or stale round files, including cases where the correct path is not obvious. Because it is only a template, correctness depends on callers supplying accurate `CURRENT_ROUND` and `CORRECT_PATH`. A stale or corrupted state file could produce misleading values; strict/tolerant state parsing elsewhere mitigates that risk.
- validation_or_tests: Covered indirectly by hook and state/round validator tests from the repository inventory, including `tests/test-state-exit-naming.sh`, `tests/test-plan-file-hooks.sh`, and template-reference tests. No direct executable assertions are present in this template.
- skip_candidate: `yes: template-only block; core-adjacent because it encodes a round-state invariant, but it is not executable validator logic`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-153 `file` `skills/humanize-rlcr/SKILL.md`
- cursor: `[_]`
- core_role: Skill-mode RLCR flow definition. Its purpose is to make environments without native hooks follow the same RLCR enforcement and phase transitions by delegating to `scripts/rlcr-stop-gate.sh`, which wraps `hooks/loop-codex-stop-hook.sh` (`skills/humanize-rlcr/SKILL.md:3`, `skills/humanize-rlcr/SKILL.md:9`, `skills/humanize-rlcr/SKILL.md:12`, `skills/humanize-rlcr/SKILL.md:16`).
- algorithmic_behavior: The required sequence is setup, work round, summary write, then gate execution (`skills/humanize-rlcr/SKILL.md:28`, `skills/humanize-rlcr/SKILL.md:30`, `skills/humanize-rlcr/SKILL.md:40`, `skills/humanize-rlcr/SKILL.md:50`). For each round, it tells the agent to read `.humanize/rlcr/<timestamp>/round-<N>-prompt.md` or finalize prompts, implement changes, commit, write `round-<N>-summary.md` or `finalize-summary.md`, and run the stop gate (`skills/humanize-rlcr/SKILL.md:44`, `skills/humanize-rlcr/SKILL.md:45`, `skills/humanize-rlcr/SKILL.md:46`, `skills/humanize-rlcr/SKILL.md:47`, `skills/humanize-rlcr/SKILL.md:52`). Gate results are mapped to continue/finish/error semantics: exit 0 allowed, exit 10 blocked by RLCR logic, exit 20 infrastructure failure (`skills/humanize-rlcr/SKILL.md:60`). The wrapper constructs Stop-hook JSON, invokes `loop-codex-stop-hook.sh`, maps JSON block decisions to exit 10, empty output to allow, and hook/runtime failures to exit 20 (`scripts/rlcr-stop-gate.sh:8`, `scripts/rlcr-stop-gate.sh:84`, `scripts/rlcr-stop-gate.sh:102`, `scripts/rlcr-stop-gate.sh:110`, `scripts/rlcr-stop-gate.sh:126`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` passed through `setup-rlcr-loop.sh`, optional `CLAUDE_SESSION_ID`, optional `CLAUDE_TRANSCRIPT_PATH`, loop prompt files, state files, and generated summaries (`skills/humanize-rlcr/SKILL.md:35`, `skills/humanize-rlcr/SKILL.md:53`, `skills/humanize-rlcr/SKILL.md:54`, `skills/humanize-rlcr/SKILL.md:55`). Outputs are commits, summary files, and gate decisions. State is managed by setup and hook logic, not manually by the skill; the skill explicitly treats generated loop files as source of truth (`skills/humanize-rlcr/SKILL.md:89`).
- gates_or_invariants: The skill enforces state/schema validation, branch consistency, plan-file integrity, incomplete Task/Todo blocking, git-clean exit requirement, push-every-round blocking, summary presence, max-iteration handling, full alignment rounds, strict `COMPLETE`/`STOP` markers, review-phase marker guard, code-review `[P0-9]` gating, hard blocking on Codex review failure or empty output, and open-question handling (`skills/humanize-rlcr/SKILL.md:65`, `skills/humanize-rlcr/SKILL.md:69`, `skills/humanize-rlcr/SKILL.md:82`). Critical rules prohibit manual edits to `state.md` or `finalize-state.md`, manual completion bypass, ad-hoc `codex exec`/`codex review` replacing the gate, or ignoring generated round/review files (`skills/humanize-rlcr/SKILL.md:84`).
- dependencies_and_callers: Called as `/flow:humanize-rlcr` or loaded as `/skill:humanize-rlcr` (`skills/humanize-rlcr/SKILL.md:112`). Depends on hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, and `scripts/cancel-rlcr-loop.sh` (`skills/humanize-rlcr/SKILL.md:18`, `skills/humanize-rlcr/SKILL.md:32`, `skills/humanize-rlcr/SKILL.md:125`). `rlcr-stop-gate.sh` depends on `jq` and executable `hooks/loop-codex-stop-hook.sh` (`scripts/rlcr-stop-gate.sh:74`, `scripts/rlcr-stop-gate.sh:79`). Options passed through include plan path, `--track-plan-file`, `--max`, model/effort, timeout, base branch, full-review interval, skip-impl, push-every-round, Claude answer mode, and agent teams (`skills/humanize-rlcr/SKILL.md:91`).
- edge_cases_or_failure_modes: Setup nonzero stops the flow (`skills/humanize-rlcr/SKILL.md:38`). Gate exit 20 must be reported as infrastructure error and must not be faked as completion (`skills/humanize-rlcr/SKILL.md:63`). Gate exit 10 requires following returned instructions exactly and continuing, not bypassing (`skills/humanize-rlcr/SKILL.md:62`). Finalize phase uses `finalize-summary.md` rather than round summaries (`skills/humanize-rlcr/SKILL.md:49`). `--skip-impl` starts directly in review path, and review phase uses `gpt-5.2:high` per the skill doc (`skills/humanize-rlcr/SKILL.md:105`, `skills/humanize-rlcr/SKILL.md:110`). A non-executable hook or missing `jq` in the wrapper returns infrastructure error 20 (`scripts/rlcr-stop-gate.sh:74`, `scripts/rlcr-stop-gate.sh:79`).
- validation_or_tests: Behavior is validated through the same hook and finalize tests because this skill intentionally reuses stop-hook logic. `tests/test-finalize-phase.sh` verifies finalize summary, no-Codex finalize completion, state renames, incomplete todos, review failures, and validator parsing of `finalize-state.md`. Wrapper behavior is described in `scripts/rlcr-stop-gate.sh` exit-code contract (`scripts/rlcr-stop-gate.sh:8`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-003`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-033`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-063`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-093`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-123`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-153`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`