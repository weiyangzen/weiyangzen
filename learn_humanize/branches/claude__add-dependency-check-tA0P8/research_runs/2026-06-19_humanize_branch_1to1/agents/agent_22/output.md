# agent_22 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-022 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Agent prompt/policy for the `/gen-plan` relevance gate. It defines the `draft-relevance-checker` agent frontmatter, including `model: haiku` and tools `Read, Glob, Grep` at [agents/draft-relevance-checker.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/agents/draft-relevance-checker.md:1). The caller is the gen-plan command’s Phase 2 relevance check, which invokes `humanize:draft-relevance-checker` after IO validation at [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/commands/gen-plan.md:50).
- algorithmic_behavior: The agent first explores repository documentation and structure, then compares draft content against repo concepts, technologies, paths, functions, features, modification intent, usage, or learning intent. It returns exactly one verdict prefix: `RELEVANT:` or `NOT_RELEVANT:` at [agents/draft-relevance-checker.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/agents/draft-relevance-checker.md:27).
- inputs_outputs_state: Input is draft document content plus read-only repository context gathered through the allowed tools. Output is a short verdict line with explanation. The downstream state transition is external: gen-plan stops on `NOT_RELEVANT` and continues to Phase 3 on `RELEVANT`, as specified at [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/commands/gen-plan.md:67).
- gates_or_invariants: The relevance decision is deliberately permissive: informal drafts, any language, and rough ideas are acceptable; semantic relevance matters more than syntactic similarity; doubt resolves to relevant at [agents/draft-relevance-checker.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/agents/draft-relevance-checker.md:31). This makes it a low-friction safety gate, not a strict classifier.
- dependencies_and_callers: Called by `commands/gen-plan.md`; validated by `tests/test-gen-plan.sh`, which checks that the file exists, has the expected name/model/tools, naming convention, required frontmatter, valid model, and English-only content.
- edge_cases_or_failure_modes: False negatives are the main risk, mitigated by “lean toward relevant.” If repository docs are sparse, the agent relies on directory structure and file names. If output does not use the required prefix, the caller may not be able to branch cleanly.
- validation_or_tests: `tests/test-gen-plan.sh` contains direct validations for this agent file: existence, `name: draft-relevance-checker`, `model: haiku`, tool specification, naming convention, frontmatter completeness, valid model, and content character constraints.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-052 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level executable test orchestrator for the Humanize plugin. It defines the authoritative suite list, executes each suite in isolated output files, aggregates pass/fail counts, and exits nonzero on any failed suite at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/run-all-tests.sh:31).
- algorithmic_behavior: The script builds `TEST_SUITES`, creates a temp output directory, optionally injects a mock `codex` binary if none is installed, launches every existing suite concurrently, waits for all child PIDs, strips ANSI escape sequences, extracts `Passed:` and `Failed:` counters, sorts suite result lines by elapsed time, prints failure details, and exits `0` only when no suite failed.
- inputs_outputs_state: Inputs are executable test files under `tests/`, availability of `codex` and `zsh`, and suite stdout/stderr. Outputs are console summary lines, per-suite temporary `.out`, `.exit`, `.time`, and `.detail` files under a `mktemp` dir, then process exit code `0` or `1`. The temp dir is removed by the `EXIT` trap at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/run-all-tests.sh:91).
- gates_or_invariants: Missing suite files are skipped rather than hard-failing; zsh-only tests are skipped if zsh is unavailable; failed process exit code or extracted failed-count greater than zero marks a suite failed. The summary counters depend on each suite printing `Passed:` and `Failed:` in the shared helper format.
- dependencies_and_callers: Depends on all listed suite paths, `mktemp`, `date +%s%3N`, `sed`, `grep`, `sort`, `cut`, and optional `zsh`. The suite includes this worker’s assigned `tests/test-session-id.sh` at line 59 and `tests/robustness/test-template-stress-robustness.sh` at line 72.
- edge_cases_or_failure_modes: `set -u` is enabled without `set -e`, so aggregation can continue after child failures. If a suite crashes before writing an exit file, the orchestrator defaults that suite to exit code `1`. ANSI parsing is regex-based; `tests/test-ansi-parsing.sh` explicitly covers the portable stripping approach. Millisecond timing with `date +%s%3N` may be platform-sensitive if a system `date` lacks `%N`.
- validation_or_tests: This file itself is not a unit under another local runner except by direct execution. It is the aggregator used to validate the full suite surface and includes robustness, hook, PR loop, session ID, template, monitor, and setup tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-082 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR session isolation. It verifies that `session_id` is created, captured by the PostToolUse Bash hook, parsed from state files, used to filter active loops, and intentionally ignored by global cancel. The tested scope is summarized in the header at [tests/test-session-id.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-session-id.sh:3).
- algorithmic_behavior: The script repeatedly creates isolated temp repos, synthetic `.humanize/rlcr/<timestamp>/state.md` or terminal state files, invokes setup/hook/cancel paths, then asserts expected state. It sources shared helpers and `hooks/lib/loop-common.sh` at [tests/test-session-id.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-session-id.sh:15). Core scenarios include setup adding an empty `session_id` field and `.pending-session-id`, hook writing the JSON session only for a matching setup command, `find_active_loop` matching or rejecting sessions, terminal-state zombie-loop protection, and boundary-aware command matching.
- inputs_outputs_state: Inputs include mock hook JSON with `tool_input.command` and `session_id`, generated state files, pending signal file contents, and shell environment `CLAUDE_PROJECT_DIR`. Outputs are updated `state.md` lines, removed or preserved `.humanize/.pending-session-id`, renamed `cancel-state.md`, and pass/fail counters printed by `print_test_summary`.
- gates_or_invariants: Setup must create `session_id:` empty in `state.md`; line 2 of `.pending-session-id` must be an absolute setup script path ending in `/setup-rlcr-loop.sh`; the hook must consume the signal only when the Bash command starts with the exact command signature, quoted or unquoted, with end or whitespace boundary. The hook must reject arbitrary `echo`, basename-only, substring, and quoted-prefix concatenation commands at [tests/test-session-id.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-session-id.sh:826).
- dependencies_and_callers: Directly exercises `scripts/setup-rlcr-loop.sh`, `hooks/loop-post-bash-hook.sh`, `hooks/lib/loop-common.sh`, and `scripts/cancel-rlcr-loop.sh`. Implementation references: setup writes `session_id:` and the signal file at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/scripts/setup-rlcr-loop.sh:792); the post hook validates command signature, extracts JSON via `jq`, safely patches with `awk`, then removes the signal at [hooks/loop-post-bash-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-post-bash-hook.sh:56); active-loop filtering is implemented in `find_active_loop` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:230); cancel intentionally calls unfiltered lookup at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/scripts/cancel-rlcr-loop.sh:76).
- edge_cases_or_failure_modes: Covers missing hook file via skips, no signal file no-op, empty stored session ID as backward-compatible match, special characters `/`, `&`, `.`, YAML-safe punctuation, tab-delimited invocation, newer non-matching loops, terminal `complete-state.md` and `cancel-state.md` preventing stale revival, and global cancel refusing to revive older active loops when newest directory is terminal.
- validation_or_tests: This is itself a test suite and is included in `tests/run-all-tests.sh`. It uses shared `pass`, `fail`, `skip`, and `print_test_summary` helpers from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-helpers.sh:30), so its exit status derives from accumulated failures.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-112 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: Block-message template for preventing manual or agent writes to PR loop state. It tells the actor that `.humanize/pr-loop/state.md` is managed by the PR loop system and that modifying it corrupts loop state at [prompt-template/block/pr-loop-state-modification.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/prompt-template/block/pr-loop-state-modification.md:1).
- algorithmic_behavior: This file is passive template content. Its algorithmic role is as a standardized validator response emitted when a write validator detects protected PR loop state modification. The loader path is `pr_loop_state_blocked_message`, which calls `load_and_render_safe` with this template and a fallback at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:1032).
- inputs_outputs_state: Input is the decision by a validator that the protected state file would be modified. Output is a markdown block reason shown to the user/agent. No file state is changed by this template itself.
- gates_or_invariants: The invariant enforced by message content is that current round, PR number/branch, active bot config, Codex config, and polling settings are system-managed and not user-editable. This protects PR-loop control-state integrity.
- dependencies_and_callers: Depends on the generic template loader, especially `load_and_render_safe`. Likely surfaced by PR-loop write/bash validators through shared loop-common blocked-message functions. The template has no variables, so rendering is content passthrough.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline shorter block. Because the file has no placeholders, variable substitution failure is not a concern.
- validation_or_tests: Covered indirectly by template existence/comprehensiveness tests and by validator tests that exercise protected `.humanize` state writes. `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh` are included in the aggregate runner.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-142 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop prompt template instructing Codex how to update `goal-tracker.md` after per-bot review analysis. It defines the required edits to Issue Summary, Total Statistics, and Issue Log at [prompt-template/pr-loop/codex-goal-tracker-update.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/prompt-template/pr-loop/codex-goal-tracker-update.md:1).
- algorithmic_behavior: The template renders `{{GOAL_TRACKER_FILE}}` and `{{NEXT_ROUND}}`, then directs Codex to append one Issue Summary row per reviewer, increment total found/resolved counts, recompute remaining, and add a `### Round {{NEXT_ROUND}}` Issue Log entry. It also constrains modification scope: keep structure intact, preserve headers, add to existing tables, and do not replace sections.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/pr-loop-stop-hook.sh`: goal tracker path and next round number at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/pr-loop-stop-hook.sh:1253). Output is prompt text inserted into `CODEX_PROMPT_FILE`; expected downstream output is a modified PR goal tracker.
- gates_or_invariants: Each reviewer gets a separate table row; status is constrained to “Issues Found”, “All Resolved”, or “Approved”; only the three named sections may be edited; Issue Summary rows must be appended rather than replacing the table. These rules preserve the goal tracker as a structured progress ledger.
- dependencies_and_callers: Rendered by `load_and_render_safe` in `hooks/pr-loop-stop-hook.sh` at line 1262. There is also a defensive implementation fallback: `update_pr_goal_tracker` in `hooks/lib/loop-common.sh` updates the same three sections idempotently if Codex does not, with per-round/per-reviewer duplicate checks at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:1294).
- edge_cases_or_failure_modes: Human/LLM prompt compliance is not guaranteed, hence the hook also calls `update_pr_goal_tracker` on approve and non-approve paths. The template’s example status values do not perfectly mirror helper status values such as `Resolved` and `In Progress`, so prompt-driven and helper-driven updates may have minor vocabulary divergence.
- validation_or_tests: PR-loop system tests validate goal tracker structure and update behavior, including row insertion inside Issue Summary and idempotent update scenarios. Relevant references appear in `tests/test-pr-loop-system.sh` around the goal tracker update and Issue Summary table checks.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-172 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the template rendering engine. It stress-tests `render_template`, `load_and_render`, and `load_and_render_safe` under large inputs and substitution edge cases at [tests/robustness/test-template-stress-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/robustness/test-template-stress-robustness.sh:3).
- algorithmic_behavior: The script creates a temp dir, sources `hooks/lib/template-loader.sh`, then runs 18 sequential tests. It verifies standard substitution, fallback rendering, 1KB/100KB templates, 10KB/100KB variable values, 50 substitutions, regex/sed-special characters, ampersands, backslashes, embedded newlines, empty values, placeholder injection prevention, dollar signs, repeated variables, boundary placeholders, and a template containing only a placeholder.
- inputs_outputs_state: Inputs are inline template strings, generated template files under `TEST_DIR`, and `VAR=value` arguments. Outputs are rendered strings checked by Bash comparisons or grep, pass/fail counters, and final process status from `print_test_summary`.
- gates_or_invariants: The core invariant is single-pass substitution: a value containing `{{VAR_B}}` must remain literal and must not trigger a second replacement, as asserted at [tests/robustness/test-template-stress-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/robustness/test-template-stress-robustness.sh:218). Special characters must be output literally, not interpreted as regex, sed replacement, shell expansion, or path escapes.
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh`, whose implementation uses environment-prefixed variables and a single-pass `awk` scanner at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/template-loader.sh:58). It also depends on shared test helpers and common shell utilities `seq`, `wc`, `date`, and `grep`.
- edge_cases_or_failure_modes: Very large values are passed through environment variables, so extreme sizes beyond these tests could hit OS environment limits. Timing is measured but not gated by a maximum threshold. The Unicode test label is conservative in implementation: it uses basic ASCII content rather than non-ASCII text, so it mainly confirms normal character path handling.
- validation_or_tests: This file is itself a robustness suite and is included by `tests/run-all-tests.sh` at line 72. Its final summary exits with the helper status at [tests/robustness/test-template-stress-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/robustness/test-template-stress-robustness.sh:289).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-022`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-052`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-082`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-112`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-142`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-172`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`