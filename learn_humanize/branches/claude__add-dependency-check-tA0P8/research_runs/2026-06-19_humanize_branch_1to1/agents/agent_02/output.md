# agent_02 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: `agents/` contains one recursively inspected child, `agents/draft-relevance-checker.md`. It defines the specialized relevance-check subagent used by gen-plan before generating an implementation plan.
- algorithmic_behavior: The agent is instructed to explore the repository quickly, inspect docs and directory structure, compare draft semantics against repo concepts/components, and return exactly one verdict shape: `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` (`agents/draft-relevance-checker.md:14-30`). Its decision policy is intentionally permissive: informal drafts and drafts in any language are accepted if reasonably connected, and uncertainty leans relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft content supplied by the caller plus read-only repository context gathered through `Read`, `Glob`, and `Grep` (`agents/draft-relevance-checker.md:4-5`, `agents/draft-relevance-checker.md:14-25`). Output is a textual relevance verdict. No filesystem state transition is defined in the agent file itself.
- gates_or_invariants: The main invariant is the verdict contract. The surrounding gen-plan flow treats `NOT_RELEVANT` as a hard stop and `RELEVANT` as permission to proceed (`commands/gen-plan.md:67-72`; `skills/humanize-gen-plan/SKILL.md:23-27`). The checker must be lenient and semantic, not syntactic (`agents/draft-relevance-checker.md:33-36`).
- dependencies_and_callers: `commands/gen-plan.md` Phase 2 explicitly invokes `humanize:draft-relevance-checker` via Task with model `haiku` (`commands/gen-plan.md:56-65`). `skills/humanize-gen-plan/SKILL.md` mirrors the same relevance checkpoint in its workflow diagram (`skills/humanize-gen-plan/SKILL.md:23-27`). Tests validate the file exists, frontmatter name is `draft-relevance-checker`, model is `haiku`, tools are declared, naming is valid, frontmatter is complete, model is allowed, and content is English-only (`tests/test-gen-plan.sh:111-155`, `tests/test-gen-plan.sh:241-245`, `tests/test-gen-plan.sh:289-296`, `tests/test-gen-plan.sh:391-421`).
- edge_cases_or_failure_modes: Skip candidate if viewed as pure prompt copy, but it is algorithmically relevant because it gates gen-plan execution. Its lenient rule means false positives are favored over false negatives. It explicitly accepts informal, multilingual, or rough drafts (`agents/draft-relevance-checker.md:33-35`). A malformed or missing verdict could break the caller’s Phase 2 branch, though the agent file does not define parser recovery.
- validation_or_tests: `tests/test-gen-plan.sh` is the direct executable spec for this directory’s child agent metadata and content constraints. I inspected, but did not execute, tests in this read-only branch export.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-032 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Claude `Edit` actions. It enforces RLCR and PR-loop file ownership, preventing edits to generated prompts, state files, most todos files, wrong-round summaries, and protected loop backups.
- algorithmic_behavior: The hook reads JSON from stdin, extracts `.tool_name`, exits unless it is `Edit`, extracts `.tool_input.file_path`, lowercases it, and derives a session id for session-aware active-loop lookup (`hooks/loop-edit-validator.sh:24-35`). It blocks most `round-*-todos.md` edits unless the file is allowlisted for the active loop (`hooks/loop-edit-validator.sh:41-48`), blocks prompt-file edits unconditionally (`hooks/loop-edit-validator.sh:51-54`), applies PR-loop protections (`hooks/loop-edit-validator.sh:60-80`), then if the path is inside `.humanize/rlcr/`, finds the active loop and parses strict state to get `CURRENT_ROUND` (`hooks/loop-edit-validator.sh:86-110`). It then blocks `finalize-state.md`, `state.md`, RLCR `plan.md` backups, post-round-zero goal tracker edits, and non-current summary edits (`hooks/loop-edit-validator.sh:117-180`).
- inputs_outputs_state: Input is Claude hook JSON on stdin. Important fields are `tool_name`, `tool_input.file_path`, and optional `session_id`. Output is either no stdout/stderr with exit `0` for allow, an explanatory markdown block on stderr with exit `2` for policy block, or exit `1` for malformed active state (`hooks/loop-edit-validator.sh:106-108`). It does not mutate files.
- gates_or_invariants: Only `Edit` is handled (`hooks/loop-edit-validator.sh:27-29`). Todos are blocked except exact allowlisted historical files in the active loop (`hooks/loop-edit-validator.sh:41-48`; allowlist in `hooks/lib/loop-common.sh:566-584`). Prompt files are read-only (`hooks/loop-edit-validator.sh:51-54`). Active RLCR state must parse strictly or the operation fails closed (`hooks/loop-edit-validator.sh:105-109`). Finalize-state is checked before generic state because `state.md` matching also catches `finalize-state.md` (`hooks/loop-edit-validator.sh:113-124`). Goal tracker edits are blocked after round zero (`hooks/loop-edit-validator.sh:145-149`). Summary edits are allowed only for the current round unless allowlisted (`hooks/loop-edit-validator.sh:155-180`).
- dependencies_and_callers: Loaded from `hooks/hooks.json` as the PreToolUse command for matcher `Edit` (`hooks/hooks.json:24-30`). It sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16-18`), which in turn sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:171-180`). It depends on `jq`, `sed`, basename/path helpers, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, PR-loop helpers, and rendered block templates.
- edge_cases_or_failure_modes: Unlike `loop-read-validator.sh`, this script does not call `validate_hook_input`; malformed JSON can cause `jq` under `set -euo pipefail` to abort early rather than producing a rendered policy message (`hooks/loop-edit-validator.sh:24-31`). No active loop means the hook allows RLCR paths after initial classification (`hooks/loop-edit-validator.sh:98-100`). Wrong-round summary detection depends on extracting a filename from `.humanize/rlcr/...`; if extraction fails, the summary check silently skips (`hooks/loop-edit-validator.sh:155-181`). The script uses `load_and_render_safe` for its direct template render, preserving fallback behavior if templates are absent (`hooks/loop-edit-validator.sh:135`, `hooks/loop-edit-validator.sh:172-177`).
- validation_or_tests: `tests/test-allowlist-validators.sh` covers edit allowlist behavior for `round-2-todos.md`, `round-1-summary.md`, and blocking non-allowlisted todos (`tests/test-allowlist-validators.sh:188-228`). `tests/test-template-references.sh` includes this script in template-reference checks and verifies critical validators use `load_and_render_safe` (`tests/test-template-references.sh:56-64`, `tests/test-template-references.sh:172-200`). Additional direct callers appear in finalize/plan/pr-loop hook tests by grep, but I did not execute tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-062 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable negative/edge-case specification for `hooks/lib/template-loader.sh`. It verifies graceful behavior under missing templates, invalid template directories, empty content, strict shell mode, fallback rendering, directory validation, and malformed placeholders.
- algorithmic_behavior: The script sources `template-loader.sh`, derives `TEMPLATE_DIR` with `get_template_dir`, then runs 12 numbered assertions while tracking pass/fail counters (`tests/test-error-scenarios.sh:11-23`, `tests/test-error-scenarios.sh:25-35`). It expects missing template loads to return an empty string with exit `0` (`tests/test-error-scenarios.sh:42-65`), `load_and_render` missing-template behavior to return empty (`tests/test-error-scenarios.sh:67-78`), empty render content to stay empty (`tests/test-error-scenarios.sh:80-91`), special regex-like values to render literally (`tests/test-error-scenarios.sh:93-106`), and strict `set -euo pipefail` callers to continue after missing templates (`tests/test-error-scenarios.sh:108-131`). It also validates fallback use and fallback variable substitution (`tests/test-error-scenarios.sh:133-158`), valid/invalid template directory return codes (`tests/test-error-scenarios.sh:160-180`), and placeholder grammar edge cases (`tests/test-error-scenarios.sh:182-206`).
- inputs_outputs_state: Inputs are the real project template directory plus synthetic strings and invalid paths. Outputs are colored PASS/FAIL lines and a process exit equal to success if `TESTS_FAILED` is zero, otherwise `1` (`tests/test-error-scenarios.sh:211-226`). It creates no persistent repository state.
- gates_or_invariants: Missing templates and missing directories are non-fatal for `load_template`/`load_and_render` and should yield empty output with exit `0` (`tests/test-error-scenarios.sh:45-78`). `load_and_render_safe` must convert missing/empty template conditions into fallback output (`tests/test-error-scenarios.sh:137-158`). `validate_template_dir` must accept a valid `prompt-template` tree and reject nonexistent paths (`tests/test-error-scenarios.sh:164-180`). Empty placeholder `{{}}` and unclosed `{{` must stay literal (`tests/test-error-scenarios.sh:186-206`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh` (`tests/test-error-scenarios.sh:13`). It is related to broader template validation in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness template tests. `tests/run-all-tests.sh` lists `test-template-loader.sh`, but this specific error-scenario script is a standalone executable spec in the repository.
- edge_cases_or_failure_modes: The test intentionally uses `set -uo pipefail` at top level, not `set -e`, so it can inspect nonzero results without aborting (`tests/test-error-scenarios.sh:9`). The strict-mode case is run in a subshell with `set -euo pipefail` and captured with `|| true`, ensuring it can detect crashes via missing `SCRIPT_COMPLETED` (`tests/test-error-scenarios.sh:114-131`). It redirects expected warnings to `/dev/null` for missing-template checks.
- validation_or_tests: This file itself is validation evidence for the template loader. I inspected it but did not run it.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-092 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared prompt/template rendering library used by RLCR hooks, PR-loop setup/hooks, and validators. It provides the fallback-safe rendering layer that keeps hook policy messages available even when template files are absent.
- algorithmic_behavior: `get_template_dir` maps a `hooks/lib` script directory up two levels to `<plugin_root>/prompt-template` (`hooks/lib/template-loader.sh:24-31`). `load_template` cats an existing template or emits a warning plus empty output for missing files (`hooks/lib/template-loader.sh:33-48`). `render_template` accepts content plus `VAR=value` pairs, exports them with `TMPL_VAR_` prefixes, and uses a single awk pass to replace `{{VAR}}` placeholders while leaving unknown placeholders intact (`hooks/lib/template-loader.sh:50-132`). `load_and_render` composes loading and rendering only when content is nonempty (`hooks/lib/template-loader.sh:134-147`). `append_template` appends another loaded template (`hooks/lib/template-loader.sh:149-161`). `load_and_render_safe` loads a template quietly and uses a caller-provided fallback if the template is missing, empty, or renders empty (`hooks/lib/template-loader.sh:167-203`). `validate_template_dir` requires the base directory plus `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`).
- inputs_outputs_state: Inputs are a template directory, template relative path, template content string, fallback string, and variable assignments. Outputs are rendered markdown on stdout and warnings/errors on stderr for non-safe loading or directory validation. It does not mutate files.
- gates_or_invariants: Rendering is single-pass by design to prevent placeholder injection from variable values (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:71-129`). Unknown placeholders are preserved (`hooks/lib/template-loader.sh:119-122`). Missing template files are graceful, not fatal (`hooks/lib/template-loader.sh:41-47`). `load_and_render_safe` must never return empty when fallback is available unless fallback itself renders empty (`hooks/lib/template-loader.sh:176-202`). Directory validation requires the three prompt subtrees (`hooks/lib/template-loader.sh:211-219`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` sources it and initializes/validates `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:171-180`). Direct call sites span `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, stop hooks, and `scripts/setup-pr-loop.sh`, as shown by repository references. The critical hook usage is tested by `tests/test-template-references.sh` (`tests/test-template-references.sh:56-64`, `tests/test-template-references.sh:83-107`, `tests/test-template-references.sh:172-200`).
- edge_cases_or_failure_modes: Values with shell metacharacters, regex characters, ampersands, backslashes, dollars, backticks, JSON-like text, multiline values, empty values, Unicode, placeholder text inside values, missing variables, empty placeholders, and unclosed placeholders are all intended to be literal or preserved based on tests (`tests/test-template-loader.sh:247-543`, `tests/test-template-loader.sh:555-639`; `tests/test-error-scenarios.sh:93-106`, `tests/test-error-scenarios.sh:182-206`). A subtle invariant is that variable assignment names are not validated in the shell wrapper; invalid names would become unusual environment names passed to `env`, while the documented syntax says uppercase letters, numbers, and underscores (`hooks/lib/template-loader.sh:7-13`).
- validation_or_tests: Direct tests include `tests/test-template-loader.sh` for normal rendering, metacharacters, injection prevention, and edge cases; `tests/test-error-scenarios.sh` for missing/error behavior; `tests/test-templates-comprehensive.sh` and robustness template tests for broader coverage. I inspected but did not execute them.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-122 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Markdown block template for the RLCR read-validator gate that rejects reading prompt/summary files from a non-current round unless allowlisted.
- algorithmic_behavior: The template renders a short wrong-round diagnostic using `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{FILE_PATH}}` (`prompt-template/block/wrong-round-file.md:1-9`). It names the current round’s prompt and summary paths and gives a direct `cat {{FILE_PATH}}` escape instruction if the caller truly needs the blocked file (`prompt-template/block/wrong-round-file.md:5-9`).
- inputs_outputs_state: Inputs are variables supplied by `load_and_render_safe` in `hooks/loop-read-validator.sh` (`hooks/loop-read-validator.sh:144-149`). Output is markdown written to stderr before the read validator exits with block status `2` (`hooks/loop-read-validator.sh:138-150`). It has no state.
- gates_or_invariants: The corresponding gate is triggered only after the read hook confirms the target is a prompt or summary round file, an active loop exists, strict state parsing succeeds, the filename round can be extracted, and the requested round differs from `CURRENT_ROUND` while not allowlisted (`hooks/loop-read-validator.sh:72-107`, `hooks/loop-read-validator.sh:138-150`). The allowlist permits specific historical files such as `round-0-summary.md` and `round-1-summary.md` in the active loop (`hooks/lib/loop-common.sh:566-584`).
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-round-file.md"` (`hooks/loop-read-validator.sh:144`). It depends on `hooks/lib/template-loader.sh` for variable substitution and on `hooks/lib/loop-common.sh` for active-loop, current-round, and allowlist semantics. It is discovered by `tests/test-template-references.sh`, which scans hook scripts and verifies referenced templates exist (`tests/test-template-references.sh:56-107`).
- edge_cases_or_failure_modes: This template is not used by `hooks/loop-edit-validator.sh`; edit/write wrong-round summary gates use `block/wrong-round-number.md` instead (`hooks/loop-edit-validator.sh:172-177`). If the template is missing or empty, `loop-read-validator.sh` has an inline fallback message (`hooks/loop-read-validator.sh:139-149`). The template’s final line suggests `cat {{FILE_PATH}}`, which may still be blocked elsewhere depending on the hook command path and policy context.
- validation_or_tests: Indirectly validated by template-reference tests and read-validator allowlist/wrong-round tests. `tests/test-allowlist-validators.sh` exercises read-validator allowance/blocking for historical/current and non-allowlisted round files (`tests/test-allowlist-validators.sh:230-284`). I inspected but did not execute tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-152 `file` `skills/humanize-gen-plan/SKILL.md`
- cursor: `[_]`
- core_role: Skill-level workflow contract for generating a structured implementation plan from a draft. It is the high-level algorithm that coordinates IO validation, relevance gating, analysis, user clarification, quantitative metric confirmation, plan generation, review, language unification, and final reporting.
- algorithmic_behavior: The mermaid flow defines the state machine: begin, run `scripts/validate-gen-plan-io.sh`, stop on validation failure, read draft, check relevance, stop if irrelevant, analyze for clarity/consistency/completeness/functionality, loop through user issue resolution until no issues remain, confirm quantitative metrics when present, generate a structured plan, write it using Edit, review and fix inconsistencies until clean, optionally unify language, then report success (`skills/humanize-gen-plan/SKILL.md:17-45`). The plan structure is fixed around goal, AC-format acceptance criteria with positive/negative tests, path boundaries, dependencies/milestones, and implementation notes (`skills/humanize-gen-plan/SKILL.md:53-92`).
- inputs_outputs_state: Required inputs are `--input <path/to/draft.md>` and `--output <path/to/plan.md>` (`skills/humanize-gen-plan/SKILL.md:47-52`). Output is a generated plan file at the requested path plus a success report containing plan path, AC count, and language-unification status (`skills/humanize-gen-plan/SKILL.md:40-44`). The runtime root is hydrated by installer as `{{HUMANIZE_RUNTIME_ROOT}}`, used to locate `validate-gen-plan-io.sh` (`skills/humanize-gen-plan/SKILL.md:11-19`).
- gates_or_invariants: Validation exit codes are explicitly mapped from `0` through `7` (`skills/humanize-gen-plan/SKILL.md:94-106`). Relevance failure stops the workflow (`skills/humanize-gen-plan/SKILL.md:23-27`). Analysis issues force AskUserQuestion-style resolution before generation (`skills/humanize-gen-plan/SKILL.md:27-35`). Quantitative metrics require user confirmation as hard requirement vs trend (`skills/humanize-gen-plan/SKILL.md:31-34`). Generated code should not contain plan terminology; that constraint is part of the required plan output (`skills/humanize-gen-plan/SKILL.md:90-92`).
- dependencies_and_callers: Mirrors `commands/gen-plan.md`, whose command frontmatter allows Bash validation, Read/Glob/Grep/Task/Write/AskUserQuestion (`commands/gen-plan.md:1-12`) and gives detailed phase rules. The validation script implements the exit-code contract and copies `prompt-template/plan/gen-plan-template.md` plus original draft into the output before analysis (`scripts/validate-gen-plan-io.sh:79-160`). Relevance depends on `agents/draft-relevance-checker.md` through the command’s Phase 2 (`commands/gen-plan.md:56-65`).
- edge_cases_or_failure_modes: Validation can stop for missing input, empty input, missing output dir, existing output path, no write permission, invalid args, or missing plan template (`skills/humanize-gen-plan/SKILL.md:94-106`; implemented in `scripts/validate-gen-plan-io.sh:79-145`). Multiple languages trigger a user choice rather than automatic rewrite (`skills/humanize-gen-plan/SKILL.md:40-43`). Quantitative metrics are treated carefully because hard requirements affect acceptance criteria (`skills/humanize-gen-plan/SKILL.md:31-34`). The skill says “Write plan to output file using Edit tool” in the flow (`skills/humanize-gen-plan/SKILL.md:35`), relying on the validation script having already created the output file from the template.
- validation_or_tests: `tests/test-gen-plan.sh` validates gen-plan command/agent structure and metadata, while `scripts/validate-gen-plan-io.sh` embodies the IO validation gate. I inspected, but did not execute, tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-002, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-032, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-062, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-092, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-122, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-152
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`