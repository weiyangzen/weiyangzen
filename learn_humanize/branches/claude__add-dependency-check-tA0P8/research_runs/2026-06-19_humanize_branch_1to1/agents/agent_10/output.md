# agent_10 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-010 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer for RLCR and PR-loop hooks. The directory recursively contains two files only: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. Together they provide hook input validation, loop-state lookup/parsing, transition helpers, prompt-template rendering, state/file protection predicates, cancel authorization, git-add protection, and PR-loop goal-tracker updates.
- algorithmic_behavior: `loop-common.sh` starts with a source guard and shared constants for state fields, default Codex runtime, review markers, and terminal state reasons at `hooks/lib/loop-common.sh:16`, `hooks/lib/loop-common.sh:24`, `hooks/lib/loop-common.sh:42`, `hooks/lib/loop-common.sh:62`, and `hooks/lib/loop-common.sh:66`. Hook JSON is validated by `validate_hook_input`, which rejects null bytes, invalid UTF-8 when `iconv` is present, invalid JSON, and missing `tool_name`; it exposes `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` at `hooks/lib/loop-common.sh:91`. `is_deeply_nested` computes JSON path depth with `jq` and blocks depth above a caller-provided threshold, defaulting to 30 at `hooks/lib/loop-common.sh:151`. State resolution prefers `finalize-state.md` over `state.md` at `hooks/lib/loop-common.sh:195`; terminal state fallback for session filtering is handled by `resolve_any_state_file` at `hooks/lib/loop-common.sh:212`. `find_active_loop` implements zombie-loop protection: without a session filter it only checks the newest loop directory; with a session filter it finds that session’s newest loop, then returns it only if active, so terminal states such as `cancel-state.md` prevent older stale loops from reviving at `hooks/lib/loop-common.sh:248`. Tolerant state parsing uses defaults for optional fields at `hooks/lib/loop-common.sh:367`; strict parsing rejects missing frontmatter, missing required fields, non-numeric rounds, and invalid `review_started` at `hooks/lib/loop-common.sh:401`. `detect_review_issues` scans only the last 50 log lines for `[P?]` markers, writes an extracted audit result, and returns 0 for issues, 1 for no issues, 2 for missing/empty logs at `hooks/lib/loop-common.sh:487`. File/round helpers classify `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md` paths at `hooks/lib/loop-common.sh:544` and extract round numbers at `hooks/lib/loop-common.sh:554`. Blocking-message helpers delegate to templates with inline fallbacks, for example todos, prompt writes, state writes, finalize-state writes, summary Bash writes, and goal-tracker Bash writes at `hooks/lib/loop-common.sh:588`, `hooks/lib/loop-common.sh:598`, `hooks/lib/loop-common.sh:607`, `hooks/lib/loop-common.sh:616`, `hooks/lib/loop-common.sh:626`, and `hooks/lib/loop-common.sh:637`. Cancel authorization is a constrained recognizer for the one permitted `mv state.md|finalize-state.md cancel-state.md` transition after `.cancel-requested` exists; it rejects substitutions, shell operators, newlines, mixed quotes, extra args, wrong source/dest, remaining `$`, and symlink sources at `hooks/lib/loop-common.sh:694`. PR-loop helpers map bot names, find active PR loops, identify read-only PR-loop artifacts, and validate `round-N-pr-resolve.md` round numbers at `hooks/lib/loop-common.sh:870`, `hooks/lib/loop-common.sh:941`, `hooks/lib/loop-common.sh:970`, and `hooks/lib/loop-common.sh:986`. `git_adds_humanize` blocks direct or broad `git add` operations that would stage `.humanize` state, including forced broad adds, at `hooks/lib/loop-common.sh:1073`. `command_modifies_file` is the generic Bash-command write detector for redirects, `tee`, in-place editors, `mv/cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns at `hooks/lib/loop-common.sh:1214`. `end_loop` validates exit reason and renames the active state file to `<reason>-state.md` at `hooks/lib/loop-common.sh:1262`. `update_pr_goal_tracker` updates PR goal-tracker summary/log sections idempotently by round and reviewer, recalculates totals, and appends issue-log entries at `hooks/lib/loop-common.sh:1311`.
- inputs_outputs_state: Main inputs are hook JSON, loop directories under `.humanize/rlcr` and `.humanize/pr-loop`, YAML frontmatter fields, command strings, prompt templates, git status/ignore state, and review logs. Outputs are exported shell variables, stdout path/status values, stderr block messages, return codes, and state mutations in callers using helpers such as `end_loop` or `update_pr_goal_tracker`. `template-loader.sh` computes `prompt-template` from a script directory at `hooks/lib/template-loader.sh:26`, reads templates by relative name at `hooks/lib/template-loader.sh:36`, renders `{{VAR}}` placeholders in a single pass with `awk` and `TMPL_VAR_` environment variables at `hooks/lib/template-loader.sh:58`, and offers safe fallback rendering at `hooks/lib/template-loader.sh:170`.
- gates_or_invariants: JSON gates require syntactically valid JSON, `tool_name`, required `tool_input` fields in callers, UTF-8 when checkable, and shallow-enough structure. Loop selection invariant: only newest relevant active loop is considered; terminal states block revival. Strict state invariant: frontmatter must include `current_round`, `max_iterations`, `review_started`, and `base_branch`, with numeric rounds and boolean `review_started`. Cancel invariant: only an exact two-argument `mv` from active `state.md` or `finalize-state.md` to active `cancel-state.md` is authorized, and only with `.cancel-requested`. Template invariant: rendering is single-pass; missing placeholders remain literal, preventing injected `{{VAR}}` values from being recursively expanded.
- dependencies_and_callers: `loop-common.sh` sources `template-loader.sh` at `hooks/lib/loop-common.sh:171`. Production callers include `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and monitor scripts; the file header enumerates these at `hooks/lib/loop-common.sh:5`. The robustness test directly sources this directory at `tests/robustness/test-hook-input-robustness.sh:16`. `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh` all call `validate_hook_input` and `is_deeply_nested` at `hooks/loop-read-validator.sh:28`, `hooks/loop-read-validator.sh:33`, `hooks/loop-write-validator.sh:28`, `hooks/loop-write-validator.sh:33`, `hooks/loop-bash-validator.sh:27`, and `hooks/loop-bash-validator.sh:32`.
- edge_cases_or_failure_modes: `validate_hook_input` cannot reliably see null bytes already stripped by Bash command substitution, which the robustness tests acknowledge at `tests/robustness/test-hook-input-robustness.sh:219`. `find_active_loop` uses lexicographic newest directory selection via `ls | sort -r`, so naming must preserve timestamp ordering; robustness notes elsewhere call out that it is not timestamp validation. `render_template` passes all substitutions through environment variables, so very large content may be constrained by environment-size limits. `git_adds_humanize` depends on current working directory for `.humanize` existence and `git check-ignore`, so callers must run in the project root for broad-add detection to match intended scope. `update_pr_goal_tracker` writes temp files beside the tracker and can leave `*.update.$$` artifacts if interrupted mid-update.
- validation_or_tests: Covered by `tests/robustness/test-hook-input-robustness.sh` for JSON parsing, deep nesting, non-UTF8, command modification detection, and monitor helper integration; by `tests/robustness/test-cancel-security-robustness.sh` for cancel authorization bypasses and symlink checks; by `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and `tests/robustness/test-template-error-robustness.sh` for template loading/rendering behavior; and by session/state robustness tests referenced by `rg`. I did not execute tests because the assignment requested research notes only and the environment/export is read-only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-040 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: User-facing cancellation transition script for active RLCR loops. It creates the cancellation signal consumed by Bash validators and renames the active state file into the terminal `cancel-state.md` state.
- algorithmic_behavior: Parses `--force` and help flags, rejecting unknown options with exit 3 at `scripts/cancel-rlcr-loop.sh:24`. Resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, sets `LOOP_BASE_DIR` to `.humanize/rlcr`, and sources `hooks/lib/loop-common.sh` for `find_active_loop` at `scripts/cancel-rlcr-loop.sh:69`. It deliberately calls `find_active_loop` without session filtering, with an inline product decision that slash-command cancellation is global because it lacks hook JSON `session_id` at `scripts/cancel-rlcr-loop.sh:76`. If no active loop is found, it emits `NO_LOOP` and exits 1 at `scripts/cancel-rlcr-loop.sh:89`. It distinguishes normal loop state (`state.md`) from finalize phase (`finalize-state.md`) at `scripts/cancel-rlcr-loop.sh:99`. It extracts `current_round` and `max_iterations` with `grep/sed`, defaulting to `?` when absent at `scripts/cancel-rlcr-loop.sh:119`. During finalize phase, it refuses to proceed unless `--force` is set, emitting `FINALIZE_NEEDS_CONFIRM` plus loop and round metadata and exiting 2 at `scripts/cancel-rlcr-loop.sh:131`. Actual cancellation touches `.cancel-requested`, removes a pending session-id signal, and moves the active state file to `cancel-state.md` at `scripts/cancel-rlcr-loop.sh:148`.
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR`, current working directory, `.humanize/rlcr/<timestamp>/{state.md,finalize-state.md}`, and frontmatter fields `current_round` and `max_iterations`. Outputs are machine-readable leading status tokens: `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, or `CANCELLED_FINALIZE`; explanatory text; exit codes 0, 1, 2, or 3. State transitions are `state.md -> cancel-state.md` for normal loops and `finalize-state.md -> cancel-state.md` for forced finalize cancellation, plus creation of `.cancel-requested` and deletion of `.humanize/.pending-session-id`.
- gates_or_invariants: It only cancels the newest active loop selected by `find_active_loop`; terminal loops are not cancelled. Finalize cancellation is gated by `--force`, matching the command wrapper that asks the user before running the force path. The signal file must be created before the move so `loop-bash-validator.sh` can recognize the move as an authorized state transition. It preserves state by moving rather than deleting.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh` at `scripts/cancel-rlcr-loop.sh:74`. The slash command wrapper `commands/cancel-rlcr-loop.md` allows exactly the normal and `--force` Bash invocations and instructs the UI to branch on `CANCELLED`, `CANCELLED_FINALIZE`, and `FINALIZE_NEEDS_CONFIRM` at `commands/cancel-rlcr-loop.md:3` and `commands/cancel-rlcr-loop.md:14`. The Bash validator’s cancel exception depends on the `.cancel-requested` signal through `is_cancel_authorized`, called at `hooks/loop-bash-validator.sh:141`, `hooks/loop-bash-validator.sh:151`, `hooks/loop-bash-validator.sh:289`, `hooks/loop-bash-validator.sh:298`, `hooks/loop-bash-validator.sh:313`, and `hooks/loop-bash-validator.sh:322`.
- edge_cases_or_failure_modes: If the newest active loop directory exists but has neither `state.md` nor `finalize-state.md`, the script emits `NO_ACTIVE_LOOP` and exits 1 at `scripts/cancel-rlcr-loop.sh:103`. If state frontmatter lacks round fields, user output uses `?` while still cancelling. Because the script is global by design, concurrent active sessions in the same project are not disambiguated. `mv` is unguarded beyond `set -euo pipefail`; filesystem failure after `.cancel-requested` is touched but before state rename could leave an authorization signal without terminal transition. It removes only `.humanize/.pending-session-id`, not other transient files.
- validation_or_tests: `tests/test-session-id.sh` exercises cancellation behavior despite session IDs and expects `cancel-state.md` creation. `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh` verify the signal-gated move and bypass rejection via `is_cancel_authorized`, including finalize-state sources, quote styles, shell operators, wrong paths, extra args, variable expansion, and symlink source rejection. I did not execute tests in this read-only research run.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-070 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for monitor cleanup and signal-runtime behavior in `scripts/humanize.sh`, especially the `_humanize_monitor_codex` nested functions `_restore_terminal`, `_cleanup`, and `_graceful_stop`.
- algorithmic_behavior: Builds a temporary test project under `/tmp/test-monitor-runtime-$$`, installs an EXIT trap to remove it, and tracks pass/fail counters at `tests/test-monitor-runtime.sh:45`. Test 1 creates a fake RLCR state directory and a small runner that sources `scripts/humanize.sh`, stubs `_restore_terminal`, defines `_cleanup`, mirrors `_graceful_stop`, and asserts output contains terminal restore, cleanup, graceful-stop message, and user-friendly deletion reason at `tests/test-monitor-runtime.sh:61`. Test 2 verifies cleanup idempotency by calling `_graceful_stop` and `_cleanup` multiple times and expecting `FINAL_COUNT: 1` at `tests/test-monitor-runtime.sh:143`. Test 3 simulates loop directory deletion detection with `check_loop_dir`, expecting continuation before deletion and graceful stop after `rm -rf .humanize/rlcr` at `tests/test-monitor-runtime.sh:186`. Test 4 source-inspects `scripts/humanize.sh` for `_restore_terminal`, scroll-region reset `printf "\033[r"`, and `_cleanup -> _restore_terminal` at `tests/test-monitor-runtime.sh:263`. Test 5 source-inspects `_graceful_stop -> _cleanup` at `tests/test-monitor-runtime.sh:320`. Test 6 creates a Bash trap and self-sends SIGINT, expecting cleanup to run at `tests/test-monitor-runtime.sh:333`. Test 7 checks Bash and zsh signal handlers exist in source at `tests/test-monitor-runtime.sh:390`. Test 8 checks cleanup resets traps at `tests/test-monitor-runtime.sh:418`. Test 9 conditionally runs a zsh SIGINT simulation using `TRAPINT` if zsh is available at `tests/test-monitor-runtime.sh:432`.
- inputs_outputs_state: Inputs are `scripts/humanize.sh`, shell availability, fake loop directories, shell traps, and synthetic SIGINT. Outputs are PASS/FAIL lines, a summary, and exit 0 only when `TESTS_FAILED` is zero at `tests/test-monitor-runtime.sh:502`. The test mutates only its temp directory and generated helper scripts when run. The implementation under test restores terminal state with scroll-region reset at `scripts/humanize.sh:644`, marks `cleanup_done`, stops `monitor_running`, resets traps, kills `tail_pid`, calls `_restore_terminal`, and prints `Stopped monitoring.` at `scripts/humanize.sh:675`; `_graceful_stop` calls `_cleanup` and then prints a deletion/cancel message at `scripts/humanize.sh:703`.
- gates_or_invariants: Cleanup must be idempotent, must restore terminal state, must reset traps to prevent re-triggering, and graceful stop must call cleanup before reporting the reason. Monitor directory deletion must produce a clean, non-crashing exit path. Bash uses `trap '_cleanup' INT TERM`; zsh uses `TRAPINT` and `TRAPTERM`, as implemented at `scripts/humanize.sh:721`.
- dependencies_and_callers: Depends on `scripts/humanize.sh` and the local shell runtime. It is narrower than the full monitor e2e suite but overlaps with `tests/test-monitor-e2e-real.sh`, which invokes the real `_humanize_monitor_codex`. The production monitor also depends on `scripts/lib/monitor-common.sh` and `hooks/lib/loop-common.sh`, sourced by `scripts/humanize.sh:6` and `scripts/humanize.sh:12`.
- edge_cases_or_failure_modes: Several checks are source-pattern checks rather than full behavioral assertions, so source formatting changes can break tests without changing behavior. Test 4 contains a pipeline `grep -q '_restore_terminal' ... | grep -q '_cleanup'` at `tests/test-monitor-runtime.sh:282`; because the first `grep -q` emits no stdout, that nested check is ineffective, but the later `grep -A30 "^    _cleanup()"` gate at `tests/test-monitor-runtime.sh:307` performs the actual call-chain validation. Signal tests rely on timing sleeps and may be flaky on slow or heavily loaded environments. zsh coverage is skipped if zsh is unavailable.
- validation_or_tests: This file is itself the validation asset. It verifies runtime cleanup semantics that are further exercised by `tests/robustness/test-hook-input-robustness.sh` monitor integration tests at `tests/robustness/test-hook-input-robustness.sh:444`, `tests/robustness/test-hook-input-robustness.sh:488`, and `tests/robustness/test-hook-input-robustness.sh:580`. I did not execute it because running it would create and delete temp files and the assignment requested research notes only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-100 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the “git not clean” gate. It defines the message shown when Claude tries to stop an RLCR loop while the repository has uncommitted changes.
- algorithmic_behavior: Template placeholders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` are rendered by `load_and_render_safe` at stop-hook time. The text tells the agent it is trying to stop with the rendered git issue count/status, includes optional notes, then requires review of untracked files, staging real changes, and committing with project conventions at `prompt-template/block/git-not-clean.md:3`. It also injects a special step to run `code-simplifier` first if installed at `prompt-template/block/git-not-clean.md:6`. Important rules forbid AI authorship and `Co-Authored-By: Claude` style attribution at `prompt-template/block/git-not-clean.md:11`.
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`, produced in `hooks/loop-codex-stop-hook.sh` from cached git status and optional untracked-file notes at `hooks/loop-codex-stop-hook.sh:557`. Output is markdown block text embedded as the JSON `reason` returned by the stop hook. The stop hook wraps it in a JSON object with `"decision": "block"` and a system message at `hooks/loop-codex-stop-hook.sh:602`.
- gates_or_invariants: The gate fires when `GIT_IS_REPO` is true and cached porcelain status is non-empty; `GIT_ISSUES` becomes `uncommitted changes` at `hooks/loop-codex-stop-hook.sh:560`. Before rendering, the stop hook runs `cleanup_stale_index_lock` so the follow-up git add/commit path is less likely to fail on a stale lock at `hooks/loop-codex-stop-hook.sh:589`. The template’s invariant is that stop cannot proceed until work is committed; it does not itself implement the check, but it defines the required remediation contract.
- dependencies_and_callers: Called only via `hooks/loop-codex-stop-hook.sh` in the assigned search scope: the stop hook loads `block/git-not-clean.md` with fallback text at `hooks/loop-codex-stop-hook.sh:598`. It is rendered through `hooks/lib/template-loader.sh` single-pass rendering. Related note templates `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md` may populate `SPECIAL_NOTES` at `hooks/loop-codex-stop-hook.sh:569` and `hooks/loop-codex-stop-hook.sh:579`.
- edge_cases_or_failure_modes: If `SPECIAL_NOTES` is empty, line 4 renders as blank space but remains harmless. If note templates are missing, the stop hook falls back to inline notes. The required action says `git add -A` can be used “if all files should be tracked,” while `git_adds_humanize` elsewhere blocks `.humanize` staging; agents must reconcile that with `.humanize` protections. Template correctness depends on no recursive placeholder expansion, which `render_template` intentionally avoids.
- validation_or_tests: Template references and safe-rendering patterns are covered by `tests/test-template-references.sh`; comprehensive template rendering is covered by `tests/test-templates-comprehensive.sh`; fallback behavior is covered by template error/stress tests. The stop-hook branch using this template is indirectly covered by stop-hook tests that simulate dirty git state. I did not execute tests in this research run.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-130 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing next-round prompt fragment that establishes the protocol for requesting goal-tracker changes after direct edits are blocked.
- algorithmic_behavior: Provides a markdown section Claude should include in its round summary only if goal-tracker updates are needed. The embedded snippet starts with `## Goal Tracker Update Request`, then asks for requested changes and justification at `prompt-template/claude/goal-tracker-update-request.md:2`. Example bullets include marking tasks complete with evidence, adding open issues, recording plan evolution, and deferring tasks with acceptance-criteria impact at `prompt-template/claude/goal-tracker-update-request.md:6`. The final sentence says Codex will review and update the tracker if justified at `prompt-template/claude/goal-tracker-update-request.md:16`.
- inputs_outputs_state: It has no dynamic placeholders. Input is simply the template file loaded by `load_template`; output is appended verbatim to the next-round prompt. It does not mutate state directly. Its state-transition role is contractual: because direct `goal-tracker.md` writes are blocked after round 0, it routes proposed mutations through the summary file to Codex.
- gates_or_invariants: The write gate is enforced in `hooks/loop-write-validator.sh`: after current round is parsed, writing `goal-tracker.md` when `CURRENT_ROUND > 0` emits `goal_tracker_blocked_message` and exits 2 at `hooks/loop-write-validator.sh:194`. Bash bypasses are similarly blocked and instructed to use a summary request at `hooks/loop-bash-validator.sh:349`. The template defines the allowed request format that satisfies those gates.
- dependencies_and_callers: Appended by `hooks/loop-codex-stop-hook.sh` when preparing the next Claude prompt at `hooks/loop-codex-stop-hook.sh:1638`; if missing, the hook falls back to a one-line instruction at `hooks/loop-codex-stop-hook.sh:1640`. The Codex review prompt includes a corresponding “Goal Tracker Update Requests” responsibility section loaded from `prompt-template/codex/goal-tracker-update-section.md`, referenced at `hooks/loop-codex-stop-hook.sh:808`, so Claude requests have a downstream reviewer contract.
- edge_cases_or_failure_modes: Because the template is appended unconditionally to next-round prompts, agents may include unnecessary update requests unless they follow “If Goal Tracker needs updates.” It is guidance-only; malformed or unjustified requests rely on Codex review to reject or ignore them. Since it has no placeholders, render failures are mainly missing-file fallback issues.
- validation_or_tests: Template reference search shows direct load at `hooks/loop-codex-stop-hook.sh:1639`. Comprehensive template tests check goal-tracker update sections elsewhere; write/Bash validators exercise the enforced indirect-update path. I did not execute tests in this read-only research run.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-160 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for hook input parsing, command-pattern detection, monitor parsing helpers, and monitor edge cases. It is directly relevant to dependency-check behavior because it asserts the validators fail closed on malformed/deep/non-UTF8 inputs and do not crash on monitor/log edge cases.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers, then creates an isolated test directory at `tests/robustness/test-hook-input-robustness.sh:14`. Tests 1-3 pipe well-formed JSON into production read/write/bash validators and expect exit 0 for non-loop paths at `tests/robustness/test-hook-input-robustness.sh:35`. Tests 4-6 expect invalid JSON, missing `tool_name`, and missing `tool_input.file_path` to be rejected with non-signal exits at `tests/robustness/test-hook-input-robustness.sh:71`. Test 7 sends a 10KB Bash command and expects it to pass without size-related failure at `tests/robustness/test-hook-input-robustness.sh:117`. Tests 8-10 verify special characters, Unicode path escapes, and unknown tool names are handled safely at `tests/robustness/test-hook-input-robustness.sh:135`. Test 10a constructs 50 nested object levels and expects rejection from the shared `is_deeply_nested` gate at `tests/robustness/test-hook-input-robustness.sh:177`. Test 10b sends raw invalid UTF-8 bytes and requires exit 1 at `tests/robustness/test-hook-input-robustness.sh:201`. Test 10c documents Bash null-byte stripping and only asserts no signal crash at `tests/robustness/test-hook-input-robustness.sh:219`. Monitor edge tests cover terminal width, log growth/deletion, ANSI stripping, binary logs, narrow/wide formatting, and rapid updates at `tests/robustness/test-hook-input-robustness.sh:245`. Command-pattern tests assert `command_modifies_file` detects `sed -i` and redirects at `tests/robustness/test-hook-input-robustness.sh:351`. Monitor helper integration sources `scripts/humanize.sh`, creates a minimal goal tracker, and verifies `humanize_parse_goal_tracker`, `humanize_parse_git_status`, and `humanize_detect_git_state` return parseable state at `tests/robustness/test-hook-input-robustness.sh:381`. Monitor loop integration runs real `_humanize_monitor_codex` in controlled fake projects with missing session, narrow terminal, and ANSI-coded log cases at `tests/robustness/test-hook-input-robustness.sh:444`, `tests/robustness/test-hook-input-robustness.sh:488`, and `tests/robustness/test-hook-input-robustness.sh:580`.
- inputs_outputs_state: Inputs are synthetic JSON payloads, production hook validator scripts, temporary logs, fake `.humanize/rlcr` sessions, fake `HOME`/`XDG_CACHE_HOME`, shimmed `tput`/`clear`, and production monitor functions. Outputs are PASS/FAIL test-helper records and final `print_test_summary` exit status at `tests/robustness/test-hook-input-robustness.sh:670`. Running it creates temp files/directories and background monitor/delete processes; it is not purely read-only.
- gates_or_invariants: Hook validators must reject malformed JSON, missing required fields, overly deep structures, and non-UTF8 without crashing. They must pass valid and unknown-tool JSON when not applicable. Monitor helpers must return stable pipe-delimited numeric fields. `_humanize_monitor_codex` must exit gracefully when no session exists and must not signal-crash under narrow terminal or ANSI log input. Command modification detection must flag common shell-write forms used to bypass Write/Edit hooks.
- dependencies_and_callers: Depends on `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`, and `tests/test-helpers.sh`. Production implementation anchors include `validate_hook_input` at `hooks/lib/loop-common.sh:91`, `is_deeply_nested` at `hooks/lib/loop-common.sh:151`, `require_tool_input_field` at `hooks/lib/loop-common.sh:133`, `command_modifies_file` at `hooks/lib/loop-common.sh:1214`, `humanize_parse_goal_tracker` at `scripts/humanize.sh:38`, `humanize_detect_git_state` at `scripts/humanize.sh:122`, `humanize_parse_git_status` at `scripts/humanize.sh:172`, and `_humanize_monitor_codex` at `scripts/humanize.sh:225`.
- edge_cases_or_failure_modes: The test expects non-UTF8 rejection only when `iconv` is available, because production validation skips that check if absent; however test 10b still requires exit 1, so minimal environments without `iconv` may behave differently. It writes raw binary and ANSI data through shell variables and files, which can vary by shell locale. Monitor loop tests rely on timed deletion after one second and wait loops, so slow systems may need timing tolerance. The script resets `trap` midway for monitor temp dirs at `tests/robustness/test-hook-input-robustness.sh:448`, replacing the earlier test-helper cleanup trap, so cleanup behavior depends on test-helper implementation and later trap ownership.
- validation_or_tests: This file is itself a high-level validation suite for robustness. It complements narrower suites: `tests/test-monitor-runtime.sh` for monitor cleanup/signal behavior, `tests/robustness/test-goal-tracker-robustness.sh` for goal-tracker parser edge cases, `tests/robustness/test-git-operations-robustness.sh` for git parser states, and `tests/robustness/test-cancel-security-robustness.sh` for cancel authorization. I did not run it because the task is research-only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 item sections present above, one for each assigned checklist row.
- missing_items: none.
- duplicate_items: none in `## Item Evidence`; each assigned item appears as exactly one evidence section.
- final_worker_status: `complete`