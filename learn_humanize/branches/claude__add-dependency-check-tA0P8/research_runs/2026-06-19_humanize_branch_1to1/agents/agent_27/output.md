# agent_27 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-027 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR iterative implementation/review loop. The file is not the implementation engine itself; it is the command surface that restricts execution to `scripts/setup-rlcr-loop.sh` through `allowed-tools` and passes user arguments through `$ARGUMENTS` (`commands/start-rlcr-loop.md:1-14`).
- algorithmic_behavior: Defines the high-level RLCR state machine: user implements a plan, writes a summary, Codex reviews the summary, feedback loops until Codex emits completion, then the loop enters review phase where `codex review --base <branch>` checks for severity markers, and only a clean review proceeds to finalize/end (`commands/start-rlcr-loop.md:16-25`, `commands/start-rlcr-loop.md:60-67`). It also defines the goal tracker contract with immutable goal/acceptance criteria and mutable task/progress sections (`commands/start-rlcr-loop.md:27-45`).
- inputs_outputs_state: Inputs are the plan path or `--plan-file`, iteration/model/timeout options, plan tracking, push-per-round, base branch, full-review interval, skip-implementation mode, Codex-question handling, and agent-teams mode (`commands/start-rlcr-loop.md:3`). The delegated setup script parses these options and rejects malformed values such as missing `--codex-model`, nonnumeric `--codex-timeout`, invalid `--full-review-round`, unknown options, and multiple plan files (`scripts/setup-rlcr-loop.sh:150-240`). Outputs are a timestamped `.humanize/rlcr/<timestamp>/` state directory, `state.md`, prompt files, goal tracker, optional skip-implementation marker, and a pending session-id signal (`scripts/setup-rlcr-loop.sh:750-828`).
- gates_or_invariants: The command declares a narrow allowed tool boundary for the setup script only (`commands/start-rlcr-loop.md:4`). The workflow rules require summaries, goal-tracker maintenance, implementation/test detail, no direct state-file manipulation, and no cancellation bypass (`commands/start-rlcr-loop.md:46-53`). Setup-level gates prevent simultaneous RLCR/PR loops (`scripts/setup-rlcr-loop.sh:300-313`), require an explicit experimental env var for `--agent-teams` (`scripts/setup-rlcr-loop.sh:319-330`), require a Git repo with at least one commit (`scripts/setup-rlcr-loop.sh:367-377`), and only allow missing plan files when `--skip-impl` is active (`scripts/setup-rlcr-loop.sh:342-360`).
- dependencies_and_callers: Direct dependency is `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:10-14`). The active loop is then enforced by hooks including `loop-codex-stop-hook.sh`, plan-file validators, bash/read/write/edit validators, and session-id post hook discovered via repository references. Related tests cover setup option validation, plan-file validation, base-branch behavior, skip-implementation mode, session-id binding, and agent-teams mode.
- edge_cases_or_failure_modes: `--skip-impl` makes the plan optional and sets `review_started` immediately, writes `.review-phase-started`, and creates a simplified goal tracker (`scripts/setup-rlcr-loop.sh:759-828`, `scripts/setup-rlcr-loop.sh:836-857`). `--track-plan-file` is ignored with warning if skip-implementation has no real plan (`scripts/setup-rlcr-loop.sh:342-352`). Full-review interval has a minimum of 2 (`scripts/setup-rlcr-loop.sh:203-217`). Agent-teams mode can fail before loop creation if the required env var is absent.
- validation_or_tests: The command itself is a markdown command definition, but its delegated algorithm is covered by shell tests found by reference search: `tests/test-plan-file-validation.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, `tests/robustness/test-base-branch-detection.sh`, `tests/test-session-id.sh`, and `tests/test-agent-teams.sh`. I did not execute tests in this research-only read pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-057 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the portable ANSI-stripping and pass/fail count parsing algorithm used by the aggregate test runner. It guards the test harness from miscounting colored suite summaries on GNU/Linux and BSD/macOS sed implementations (`tests/test-ansi-parsing.sh:3-6`).
- algorithmic_behavior: The script builds synthetic colored and plain output strings, strips SGR ANSI escape sequences using `esc=$'\033'` and `sed "s/${esc}\\[[0-9;]*m//g"`, then extracts numeric counts with `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed` followed by a trailing number (`tests/test-ansi-parsing.sh:38-47`, `tests/test-ansi-parsing.sh:74-90`). It accumulates test results through `pass()` and `fail()` counters and exits nonzero only when accumulated failures are present (`tests/test-ansi-parsing.sh:18-31`, `tests/test-ansi-parsing.sh:163-178`).
- inputs_outputs_state: Inputs are inline test strings containing ANSI color codes, bold/color combinations, zero counts, multiple colored tokens, multi-line suite output, and plain text (`tests/test-ansi-parsing.sh:45-153`). Outputs are colored PASS/FAIL lines, a final `Passed`/`Failed` summary, and process exit status 0 or 1 (`tests/test-ansi-parsing.sh:21-31`, `tests/test-ansi-parsing.sh:167-178`). Internal state is limited to `TESTS_PASSED`, `TESTS_FAILED`, `input`, `output_stripped`, `passed`, and `failed`.
- gates_or_invariants: The invariant under test is that stripping ANSI SGR sequences preserves parseable plain text. Covered gates include basic color removal (`tests/test-ansi-parsing.sh:42-53`), multiple colors on one line (`tests/test-ansi-parsing.sh:55-67`), passed-count extraction (`tests/test-ansi-parsing.sh:69-81`), failed-count extraction (`tests/test-ansi-parsing.sh:83-95`), zero failed counts (`tests/test-ansi-parsing.sh:97-109`), multi-line suite output using the last count (`tests/test-ansi-parsing.sh:111-130`), no-ANSI input (`tests/test-ansi-parsing.sh:132-144`), and bold-plus-color SGR codes (`tests/test-ansi-parsing.sh:146-158`).
- dependencies_and_callers: `tests/run-all-tests.sh` includes this suite in `TEST_SUITES` (`tests/run-all-tests.sh:31-44`). The runner uses the same `esc=$'\033'` plus sed stripping and count extraction before totaling suite results (`tests/run-all-tests.sh:173-193`) and then fails if any suite exit code or parsed failed count indicates failure (`tests/run-all-tests.sh:195-204`).
- edge_cases_or_failure_modes: The sed expression handles CSI SGR color sequences ending in `m` with digits and semicolons, but it does not attempt to strip all possible terminal escape sequences. The parser depends on exact `Passed:` and `Failed:` labels; suites using alternate labels may produce zero parsed counts even if their exit status still drives failure. The multi-line case explicitly uses `tail -1` to avoid earlier count-like text taking precedence (`tests/test-ansi-parsing.sh:123-125`).
- validation_or_tests: This file is itself the validation artifact and is part of the aggregate runner. It validates the parsing algorithm used by `tests/run-all-tests.sh:190-193`. I did not execute it during this research pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-087 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification for template-reference integrity across hook scripts. It prevents validators from rendering empty or missing block messages when they need to explain blocked actions (`tests/test-template-references.sh:3-9`).
- algorithmic_behavior: The script establishes `PROJECT_ROOT` and `TEMPLATE_DIR`, scans a fixed list of hook/common shell scripts for direct `load_template`, `load_and_render`, and `load_and_render_safe` calls using `$TEMPLATE_DIR`, extracts literal template paths with `sed`, and hard-fails any reference whose template file is absent (`tests/test-template-references.sh:14-17`, `tests/test-template-references.sh:56-110`). It then enumerates every markdown template and warns if it is not directly referenced in hooks, checks a hard-coded common-template set, and verifies critical validators use safe rendering (`tests/test-template-references.sh:116-200`).
- inputs_outputs_state: Inputs are the listed hook scripts (`hooks/loop-codex-stop-hook.sh`, `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, and `hooks/lib/loop-common.sh`) plus all `prompt-template/**/*.md` files (`tests/test-template-references.sh:57-64`, `tests/test-template-references.sh:121-127`). Outputs are PASS/FAIL/WARN lines, total counts, a missing-template list, and exit status 0 only when `TESTS_FAILED` is zero (`tests/test-template-references.sh:25-42`, `tests/test-template-references.sh:203-230`). Internal state includes `MISSING_TEMPLATES`, `FOUND_REFERENCES`, `TEMPLATE_FILES`, `UNREFERENCED`, and counters.
- gates_or_invariants: Section 1 enforces that direct template references from checked scripts exist (`tests/test-template-references.sh:51-115`). Section 2 treats unreferenced templates as warnings, not failures, because dynamic usage may be valid (`tests/test-template-references.sh:117-142`). Section 3 requires common block templates including `block/todos-file-access.md` to exist (`tests/test-template-references.sh:145-167`). Section 4 fails critical read/write/edit validators if they use unsafe `load_and_render` calls instead of `load_and_render_safe` (`tests/test-template-references.sh:170-201`).
- dependencies_and_callers: Included in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:31-38`). Depends on the template loader contract in `hooks/lib/template-loader.sh`, where `load_template` returns empty for missing files and `load_and_render_safe` falls back when content is missing (`hooks/lib/template-loader.sh:34-48`, `hooks/lib/template-loader.sh:167-185`). It also validates templates used by `hooks/lib/loop-common.sh`, including `todos_blocked_message` (`hooks/lib/loop-common.sh:586-595`).
- edge_cases_or_failure_modes: It only detects literal quoted template names in direct calls using `"$TEMPLATE_DIR"`; dynamic paths, alternate variables, generated names, or scripts outside `SCRIPTS_TO_CHECK` can be missed (`tests/test-template-references.sh:66-97`). Comment skipping only ignores lines whose first non-space character is `#` (`tests/test-template-references.sh:87-91`). Unreferenced templates are warnings, so dead templates do not fail the test.
- validation_or_tests: This file validates template integrity and safe-rendering usage directly. Related broader coverage exists in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-error-scenarios.sh`, and robustness template tests found by reference search. I did not execute tests during this research pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-117 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Static block-message template used when loop validators block direct creation, access, or modification of `round-*-todos.md` files. Its algorithmic purpose is to redirect task-state tracking from ad hoc todo files to native task tools (`prompt-template/block/todos-file-access.md:1-8`).
- algorithmic_behavior: The rendered message tells the agent not to create or access `round-*-todos.md` files and to use `TaskCreate`, `TaskUpdate`, and `TaskList` instead (`prompt-template/block/todos-file-access.md:3-8`). It is loaded through `todos_blocked_message`, which uses `load_and_render_safe` with a fallback so the blocker can still emit useful text if the template is missing (`hooks/lib/loop-common.sh:586-595`).
- inputs_outputs_state: The template takes no variables. Input is simply the validator’s decision that a todos file access/modification should be blocked. Output is the markdown block message written to stderr by the relevant validator. It does not mutate repository state, loop state, or task state.
- gates_or_invariants: The policy invariant is that task tracking should remain in native task tools rather than `round-*-todos.md`. The shared allowlist currently permits only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop directory (`hooks/lib/loop-common.sh:563-584`). Bash modification attempts to todos files are blocked unless the command targets active-loop `round-1` or `round-2` todos by full active-loop path (`hooks/loop-bash-validator.sh:381-392`).
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh` for blocked reads, `hooks/loop-write-validator.sh` for blocked writes, `hooks/loop-edit-validator.sh` for blocked edits, and `hooks/loop-bash-validator.sh` for blocked shell modifications, as found by direct references. The common message loader is in `hooks/lib/loop-common.sh:586-595`. `tests/test-template-references.sh` hard-requires this template in its common-template set (`tests/test-template-references.sh:152-159`), and `tests/test-allowlist-validators.sh` exercises allowed and blocked todos-file cases.
- edge_cases_or_failure_modes: The template wording is stricter than the validator allowlist because it says not to create or access any `round-*-todos.md`, while code allows specific active-loop todos files for early rounds. If this template is deleted or empty, `load_and_render_safe` preserves behavior with the fallback message (`hooks/lib/loop-common.sh:590-595`). Because there are no placeholders, rendering cannot fail from missing variables.
- validation_or_tests: Validated by template existence checks in `tests/test-template-references.sh:152-167` and by allowlist behavior tests in `tests/test-allowlist-validators.sh` references. I did not execute tests during this research pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-147 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template used when fetched PR comments already exist. It defines the worker-facing review-response workflow for comment triage, code fixes, push/re-review triggering, and resolution summary creation (`prompt-template/pr-loop/round-0-task-has-comments.md:4-27`).
- algorithmic_behavior: The template instructs the agent to prioritize human comments before bot comments, read relevant code, make needed changes, add tests when appropriate, commit and push, then post a bot-triggering PR comment with `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review the latest changes"` (`prompt-template/pr-loop/round-0-task-has-comments.md:6-21`). It requires writing a resolution summary to `@{{RESOLVE_PATH}}` listing addressed issues, modified files, and tests (`prompt-template/pr-loop/round-0-task-has-comments.md:23-27`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, supplied by `scripts/setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:731-738`). Output is rendered task content appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the PR comment dump (`scripts/setup-pr-loop.sh:757-765`, `scripts/setup-pr-loop.sh:847-851`). The expected state transition after agent work is: fixes committed and pushed, trigger comment posted, resolution summary present, then stop hook polls and either returns more feedback or completes the loop (`prompt-template/pr-loop/round-0-task-has-comments.md:39-43`).
- gates_or_invariants: Important rules forbid modifying `.humanize/pr-loop/` state files, require pushes so remote bots can review the new commit, require the exact bot mention trigger format, and require addressing all valid reviewer concerns (`prompt-template/pr-loop/round-0-task-has-comments.md:30-35`). PR-loop bash validators separately protect `state.md` and generated prompt/comment/check/feedback files from mutation (`hooks/loop-bash-validator.sh:397-442`), with shared block-message helpers in `hooks/lib/loop-common.sh:1032-1048`.
- dependencies_and_callers: `scripts/setup-pr-loop.sh` selects this template when the fetched comments file does not contain the exact no-comments sentinel `*No comments found.*` (`scripts/setup-pr-loop.sh:723-729`, `scripts/setup-pr-loop.sh:766-848`). Rendering uses `load_and_render_safe`, so the script has an embedded fallback copy of the same task if the template cannot be loaded (`scripts/setup-pr-loop.sh:803-847`). The loop then depends on `hooks/pr-loop-stop-hook.sh` to poll reviews and validate approval after exit, matching the template’s stop-hook note.
- edge_cases_or_failure_modes: If comment fetching fails or the no-comments sentinel is absent for any reason, setup treats the PR as having comments and selects this path (`scripts/setup-pr-loop.sh:723-729`). If the template is missing, fallback content preserves the workflow (`scripts/setup-pr-loop.sh:803-847`). The `gh pr comment` instruction depends on a valid PR number, bot mention string, authentication, and remote push; failures there prevent the intended re-review transition.
- validation_or_tests: Template existence and rendering safety are covered by `tests/test-template-references.sh` direct reference scanning and safe-render checks. PR-loop behavior is covered by `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-1-scripts.sh`, `tests/test-pr-loop-2-hooks.sh`, `tests/test-pr-loop-3-stophook.sh`, and PR-loop robustness tests found by reference search. I did not execute tests during this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`