# agent_16 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-016 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for Humanize monitors. The directory contains `monitor-common.sh`, which provides cross-monitor primitives, and `monitor-skill.sh`, which implements `humanize monitor skill`. It is sourced by `scripts/humanize.sh` at `scripts/humanize.sh:6` and `scripts/humanize.sh:1587`.
- algorithmic_behavior: `monitor-common.sh` supplies color helpers (`scripts/lib/monitor-common.sh:14`), cross-platform file sizing (`:32`), timestamp-session selection (`:40`), terminal scroll-region setup/restore (`:71`, `:86`), loop state-file detection (`:154`), YAML frontmatter extraction (`:197`), truncation/time formatting (`:218`, `:233`), PR-loop phase detection (`:267`), phase display mapping (`:346`), goal-tracker parsing (`:390`), and PR issue summary parsing (`:468`). `monitor-skill.sh` implements `_humanize_monitor_skill` (`scripts/lib/monitor-skill.sh:15`) with newest invocation selection, status aggregation, cache/output file selection, one-shot output, and live `tail -f` mode.
- inputs_outputs_state: Inputs are filesystem state under `.humanize/pr-loop`, `.humanize/skill`, invocation `metadata.md`, `input.md`, `output.md`, global/local cache files, PR `state.md`, terminal dimensions, and optional `--once`. Outputs are terminal status bars, one-shot textual summaries, tailed log content, and PR phase/status strings. `get_pr_loop_phase` also writes size cache files under `/tmp/humanize-phase-*` to detect growing `round-*-pr-check.md` files (`scripts/lib/monitor-common.sh:297`).
- gates_or_invariants: Session directories must match `YYYY-MM-DD_HH-MM-SS` in common monitor discovery (`scripts/lib/monitor-common.sh:55`) and skill directories may include timestamp suffixes (`scripts/lib/monitor-skill.sh:50`). State-file priority is active `state.md` before terminal `*-state.md` (`scripts/lib/monitor-common.sh:162`). PR phase final states override activity detection (`:272`). Skill monitor treats absence of `metadata.md` as running (`scripts/lib/monitor-skill.sh:134`) and prefers log files for running invocations, final `output.md` for completed ones (`:152`, `:158`).
- dependencies_and_callers: `scripts/humanize.sh` dispatches `humanize monitor pr` and `humanize monitor skill` (`scripts/humanize.sh:1116`). PR monitor reuses common helpers for latest session, state-file mapping, and phase display (`scripts/humanize.sh:1187`, `:1239`, `:1321`). Skill monitor depends on `monitor_get_yaml_value`, `monitor_format_timestamp`, and `humanize_split_to_array` (`scripts/lib/monitor-skill.sh:8`).
- edge_cases_or_failure_modes: Missing monitor directories return non-zero with user-facing errors (`scripts/lib/monitor-skill.sh:35`; PR equivalent at `scripts/humanize.sh:1180`). Malformed or absent YAML fields default to `N/A`, `none`, or `unknown`. `get_pr_loop_phase` can report `codex_analyzing` when file size grows or mtime is under 10 seconds (`scripts/lib/monitor-common.sh:315`), so very recent static files may temporarily appear active. Terminal-dependent calls (`tput`, alternate screen, cursor control) assume an interactive-capable terminal in live mode.
- validation_or_tests: Assigned `tests/test-pr-loop-hooks.sh` validates PR monitor parsing of YAML `active_bots`, separate `configured_bots`, and empty active-bot display at `tests/test-pr-loop-hooks.sh:1547`, `:1574`, and `:1596`. Broader references include `tests/test-skill-monitor.sh`, `tests/test-monitor-e2e-real.sh`, and `tests/test-pr-loop-system.sh`, which exercise these shared monitor helpers.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-046 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Runtime portability shim that normalizes bounded command execution across macOS/Linux for setup scripts, hooks, Codex calls, GitHub CLI calls, and tests.
- algorithmic_behavior: `detect_timeout_impl` selects `gtimeout`, then GNU `timeout`, then `python3`, then `python`, then `none` (`scripts/portable-timeout.sh:10`). `TIMEOUT_IMPL` is fixed when the file is sourced (`:29`). `run_with_timeout <seconds> <command> [args...]` stores the command as an argv array (`:33`) and dispatches to the selected backend (`:38`). Python fallback uses `subprocess.run(..., timeout=seconds)` and maps `TimeoutExpired` to exit `124`, matching GNU timeout (`:49`).
- inputs_outputs_state: Inputs are the timeout seconds and command argv. Outputs are the child command’s stdout/stderr and exit code, or `124` on Python timeout. It exports `TIMEOUT_IMPL` for callers to inspect (`scripts/portable-timeout.sh:73`). In `none` mode, it emits a warning to stderr and runs unbounded (`:64`).
- gates_or_invariants: GNU `timeout` is accepted only if `timeout --version` succeeds, avoiding non-GNU/BSD ambiguity (`scripts/portable-timeout.sh:13`). Command arguments are preserved through Bash arrays (`:36`). The script is designed to be sourced, not executed as a standalone CLI.
- dependencies_and_callers: Used by PR/RLCR setup scripts, hook validators, stop hooks, bot-check scripts, and `ask-codex.sh`; reference search found callers such as `scripts/setup-pr-loop.sh:35`, `hooks/pr-loop-stop-hook.sh:59`, `hooks/loop-codex-stop-hook.sh:51`, `scripts/ask-codex.sh:29`, and `hooks/loop-plan-file-validator.sh:21`.
- edge_cases_or_failure_modes: If no backend exists, commands can hang because timeout enforcement is explicitly skipped. Python fallback timeout interpolation assumes numeric `timeout_secs`; nonnumeric or shell-special values can break the inline Python command. Python/GNU timeout process-tree semantics differ, so grandchildren may survive in some cases. `TIMEOUT_IMPL` is detected once at source time, so later PATH changes do not change backend unless re-sourced.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` exercises normal execution, timeout exit behavior, argument preservation, command failures, special characters, backend detection, and repeated use; other robustness tests source this shim before exercising Git/GitHub operations.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-076 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop hook safety, trigger/comment processing, PR review polling, monitor display, and round-file invariants. It defines `run_hook_tests` and runs a suite of nested tests (`tests/test-pr-loop-hooks.sh:15`).
- algorithmic_behavior: The file first verifies validators: YAML-list `active_bots`, blocked writes to PR-loop `state.md` and generated comment files, allowed current-round `pr-resolve`, and Bash redirect protection (`tests/test-pr-loop-hooks.sh:26`, `:68`, `:102`, `:135`, `:174`). It then specifies sorting and gate logic: newest-first timestamps, human-before-bot sort, comment dedupe, configured-bot parsing, issue/approval extraction, re-adding previously approved bots with new issues, trigger detection, `APPROVE`, and `WAITING_FOR_BOTS` markers (`:222`, `:271`, `:288`, `:326`, `:353`, `:390`, `:415`, `:437`).
- inputs_outputs_state: Inputs are synthetic `.humanize/pr-loop/<timestamp>/state.md` files, hook JSON payloads for `Write`, `Edit`, and `Bash`, mocked `gh`/`git` executables, fixture-backed GitHub responses, `CLAUDE_PROJECT_DIR`, and PATH overrides. Outputs are pass/fail test reports through helper functions, hook stderr/stdout assertions, and generated temporary files in `TEST_DIR`.
- gates_or_invariants: PR loop generated files are read-only except the current round’s `round-N-pr-resolve.md`. Round numbers must match `current_round` (`tests/test-pr-loop-hooks.sh:1431`, `:1465`, `:1499`). Subsequent rounds require a trigger (`:606`). Round 0 prefers `last_trigger_at` over `started_at` when present (`:624`) and falls back only when no trigger exists (`:646`). Per-bot timeout is anchored to the trigger timestamp, not poll start (`:667`).
- dependencies_and_callers: Depends on `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, `tests/setup-monitor-test-env.sh`, `jq`, and test helper globals like `TEST_DIR`, `SCRIPT_DIR`, `PROJECT_ROOT`, `pass`, and `fail`.
- edge_cases_or_failure_modes: Covers current-user filtering for trigger comments (`tests/test-pr-loop-hooks.sh:538`), newer trigger refresh (`:588`), missing resolve summaries (`:766`), paginated comment APIs (`:947`), approval-only reviews with null/empty bodies (`:1160`), `--after` filtering (`:1287`, `:1371`), and empty active-bot monitor display (`:1596`). It also guards against inconsistent round filenames by requiring comment/check/feedback files to use `NEXT_ROUND` together (`:726`).
- validation_or_tests: This file is itself the validation artifact. It includes E2E mocked stop-hook executions at `tests/test-pr-loop-hooks.sh:766`, `:845`, `:947`, and `:1052`, fixture-backed fetch/poll validation at `:1242` through `:1419`, wrong-round validator checks at `:1431` through `:1535`, and monitor checks at `:1547` through `:1621`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-106 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR stop/finalize attempts when Claude’s task list still contains incomplete work.
- algorithmic_behavior: The template renders an “Incomplete Tasks Detected” block, inserts `{{INCOMPLETE_LIST}}`, and directs the agent to complete all tasks, mark each via `TaskUpdate` with status `completed`, and only then summarize/stop (`prompt-template/block/incomplete-todos.md:1`). It explicitly forbids proceeding to Codex review until tasks are finished (`:12`).
- inputs_outputs_state: Input is the rendered `INCOMPLETE_LIST` supplied by the stop hook after transcript/task inspection. Output is a human/agent-facing block reason returned by the hook to prevent loop exit. It does not mutate state directly; it is the rendered contract used when state validation fails.
- gates_or_invariants: The invariant is “no stop/review transition with incomplete tasks.” The required completion cursor is the Task system, specifically `TaskUpdate` to `completed` (`prompt-template/block/incomplete-todos.md:9`). This gate applies before review progression, preventing incomplete implementation tasks from being hidden by a summary.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` checks for incomplete tasks before Codex review and renders this template through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:361`, `:400`). The template system preserves fallback behavior through `hooks/lib/template-loader.sh:170`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to inline text. The template assumes the upstream checker accurately formats `{{INCOMPLETE_LIST}}`; malformed or noisy lists would be displayed directly. It is prompt-only, so enforcement depends on the hook returning the block decision.
- validation_or_tests: `tests/test-finalize-phase.sh` includes finalize incomplete-task blocking and verifies Codex is not invoked during that check around `tests/test-finalize-phase.sh:709`. `tests/test-todo-checker.sh` validates the checker that feeds incomplete-task evidence.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-136 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude continuation prompt for RLCR review phase when Codex code review found `[P0-9]` issues.
- algorithmic_behavior: The template inserts Codex review findings via `{{REVIEW_CONTENT}}`, instructs Claude to address all severity-marked findings, restricts work to fixes only, requires a commit, and requires a summary written to `{{SUMMARY_FILE}}` (`prompt-template/claude/review-phase-prompt.md:5`, `:11`, `:14`). It states that `COMPLETE` has no effect in review phase and that Codex will re-review until no `[P0-9]` issues remain (`:23`).
- inputs_outputs_state: Inputs are review content and the target summary file path. Output is the next-round prompt written by the loop machinery. State transition is not encoded in the template itself, but the text defines the review-phase contract after implementation completion and before finalization.
- gates_or_invariants: The key invariant is that review phase cannot be skipped by emitting `COMPLETE`; only resolving Codex findings and passing another review allows progression. The scope gate forbids unrelated feature work (`prompt-template/claude/review-phase-prompt.md:12`), and the evidence gate requires a commit plus summary (`:13`, `:14`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` renders this template in the review continuation path (`hooks/loop-codex-stop-hook.sh:1185`, `:1198`) and ignores `COMPLETE` during review phase (`:1444`). Review-phase entry and marker validation occur later in the same hook (`:1469`, `:1492`).
- edge_cases_or_failure_modes: Findings are detected by `[P0-9]` marker convention; issues without those markers may not trigger the same strict loop behavior. Oversized review content is inserted directly. Missing templates fall back via `load_and_render_safe`. The prompt requires a commit even if the underlying environment has no change or commit ability, so hook/test context must align with that expectation.
- validation_or_tests: `tests/test-agent-teams.sh` verifies review-phase prompts do not include agent-team continuation text and do contain review findings around `tests/test-agent-teams.sh:613`. `tests/test-finalize-phase.sh` has negative review-phase checks for Codex review failure and empty output around `tests/test-finalize-phase.sh:563` and `:622`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-166 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for PR-loop GitHub API integration, exercising actual scripts with mocked `gh` behavior under empty responses, API errors, rate limits, malformed state, bot responses, and JSON edge cases.
- algorithmic_behavior: The script runs with `set -euo pipefail`, sources loop common helpers and test helpers, and initializes a test directory (`tests/robustness/test-pr-loop-api-robustness.sh:14`). `create_mock_gh` builds behavior-specific mock GitHub CLIs for repo/pr lookup plus API responses (`:32`). `run_fetch_tests` covers active PR-loop discovery, YAML list state, missing `pr_number`, `fetch-pr-comments.sh` behavior, bot comment parsing, Unicode, and long bodies (`:282`). `run_poll_tests` covers stop-hook no-loop/corrupt-state behavior, approve-state path, poll usage validation, JSON output, slow API, and API failure fallback (`:560`).
- inputs_outputs_state: Inputs are temporary Git repos, generated `.humanize/pr-loop/.../state.md`, mocked `gh` scripts, `PATH`, `CLAUDE_PROJECT_DIR`, and script arguments to `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, and `pr-loop-stop-hook.sh`. Outputs are test pass/fail records, generated comment markdown, JSON from poll scripts, and temporary approve-state files.
- gates_or_invariants: `find_active_pr_loop` must detect active state even with missing optional `pr_number` (`tests/robustness/test-pr-loop-api-robustness.sh:331`). `fetch-pr-comments.sh` must create valid output for empty comments and handle rate/network failures gracefully (`:362`, `:386`, `:415`). `poll-pr-reviews.sh` must reject missing args, emit valid JSON with `has_new_comments`, and on API failure exit `0` with boolean `has_new_comments:false` (`:643`, `:657`, `:755`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `hooks/pr-loop-stop-hook.sh`, Git, `jq`, and mocked `gh`. The test is executable directly via its source guard (`tests/robustness/test-pr-loop-api-robustness.sh:861`).
- edge_cases_or_failure_modes: Models rate-limit stderr with exit 1 (`tests/robustness/test-pr-loop-api-robustness.sh:111`), network exit 6 (`:121`), auth failure (`:131`), approval comments (`:145`), severity-marker comments (`:162`), mixed bots (`:179`), Unicode bodies (`:196`), 10KB bodies (`:210`), corrupted PR-loop state (`:587`), slow API latency (`:688`), and warning-prefixed JSON where the test extracts JSON from the first `{` line (`:830`).
- validation_or_tests: This file is itself the validation artifact. When executed directly, it runs both fetch and poll groups and prints a summary (`tests/robustness/test-pr-loop-api-robustness.sh:861`). Static inspection only; no repository files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6/6 Item Evidence sections present once; item IDs intentionally not repeated here to preserve exact-once cursor evidence.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`