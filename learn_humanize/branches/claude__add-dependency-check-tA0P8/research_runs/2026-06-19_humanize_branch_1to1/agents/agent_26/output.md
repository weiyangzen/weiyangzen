# agent_26 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-026 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Defines the hidden Claude slash-command entrypoint for starting the PR review loop. The command is intentionally thin: its only allowed tool is `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)`, and its executable body runs `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh" $ARGUMENTS` (`commands/start-pr-loop.md:1-14`).
- algorithmic_behavior: Establishes the user-facing workflow contract for PR-loop orchestration: detect the current branch PR, fetch review comments for selected bots, let the agent fix issues, push changes, comment with bot mentions, write a resolution summary, then rely on the Stop Hook to poll and local Codex to validate remote feedback (`commands/start-pr-loop.md:16-24`, `commands/start-pr-loop.md:37-47`). The real algorithmic expansion happens in `scripts/setup-pr-loop.sh`, which parses flags, validates prerequisites, creates loop state, fetches comments, renders the round-0 prompt, and outputs critical completion requirements (`scripts/setup-pr-loop.sh:111-190`, `scripts/setup-pr-loop.sh:220-277`, `scripts/setup-pr-loop.sh:615-945`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, especially required `--claude` and/or `--codex`, plus optional `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout SECONDS` (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). The setup script converts these into active bot arrays, Codex configuration, `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-prompt.md`, and `round-0-pr-resolve.md` (`scripts/setup-pr-loop.sh:193-214`, `scripts/setup-pr-loop.sh:413-431`, `scripts/setup-pr-loop.sh:615-713`, `scripts/setup-pr-loop.sh:721-764`). The command’s visible output is the generated initial prompt and activation message (`scripts/setup-pr-loop.sh:860-888`).
- gates_or_invariants: At least one bot flag is required (`commands/start-pr-loop.md:25-30`; enforced in `scripts/setup-pr-loop.sh:180-190`). The documented loop requires writing summaries, pushing fixes, tagging the correct bots, not editing state/cancel controls to cheat, and trusting Stop Hook polling (`commands/start-pr-loop.md:50-57`). Setup enforces mutual exclusion with active RLCR/PR loops (`scripts/setup-pr-loop.sh:220-240`), git repo/commit presence (`scripts/setup-pr-loop.sh:243-253`), `gh` and `codex` availability/auth (`scripts/setup-pr-loop.sh:255-277`), valid PR state (`scripts/setup-pr-loop.sh:351-374`), and YAML-safe branch/model/effort values (`scripts/setup-pr-loop.sh:384-407`).
- dependencies_and_callers: Depends directly on `scripts/setup-pr-loop.sh`; that script depends on `portable-timeout.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and optionally `check-bot-reactions.sh` for Claude trigger verification (`scripts/setup-pr-loop.sh:33-40`, `scripts/setup-pr-loop.sh:430-438`, `scripts/setup-pr-loop.sh:564-600`). README documents the same command surface (`README.md:194-210`). Stop Hook dependency is named in the command workflow but lives outside this assigned file (`commands/start-pr-loop.md:22-24`, `commands/start-pr-loop.md:43-45`).
- edge_cases_or_failure_modes: Missing bot flags, bad option values, unknown options, missing PR, merged/closed PR, unauthenticated GitHub CLI, missing Codex, unsafe YAML characters, an existing active loop, or failure to retrieve/verify a required trigger comment abort setup (`scripts/setup-pr-loop.sh:111-190`, `scripts/setup-pr-loop.sh:220-277`, `scripts/setup-pr-loop.sh:351-407`, `scripts/setup-pr-loop.sh:564-600`). The command file itself has no fallback or validation beyond delegating to the setup script.
- validation_or_tests: PR-loop behavior is covered by broader PR-loop tests referenced in the repo search, including `tests/test-pr-loop-system.sh` and `tests/test-pr-loop-scripts.sh`. This assigned file’s direct validation surface is mostly structural: hidden command metadata and allowed-tools restriction (`commands/start-pr-loop.md:1-5`), plus setup script argument/prerequisite tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-056 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop file allowlisting across `Read`, `Write`, `Edit`, and `Bash` validators. It directly tests `is_allowlisted_file()` in `hooks/lib/loop-common.sh` and the hook scripts that enforce round-file access (`tests/test-allowlist-validators.sh:1-17`).
- algorithmic_behavior: The test constructs a temporary git repo and active RLCR loop directory with `state.md` containing `current_round: 5`, `max_iterations: 42`, `plan_file`, `start_branch`, `base_branch`, and `review_started: false` (`tests/test-allowlist-validators.sh:29-64`). It then asserts an exact allowlist: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` are allowed only inside the active loop dir, while later todos/summaries and same filenames in other directories are blocked (`tests/test-allowlist-validators.sh:72-127`). Runtime code confirms the allowlist is hard-coded and exact-path based (`hooks/lib/loop-common.sh:563-584`).
- inputs_outputs_state: Inputs are synthesized Claude hook JSON objects such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}` or `{"tool_name":"Bash","tool_input":{"command":"..."}}` piped into validators (`tests/test-allowlist-validators.sh:135-184`, `tests/test-allowlist-validators.sh:191-227`, `tests/test-allowlist-validators.sh:234-283`, `tests/test-allowlist-validators.sh:290-383`). Outputs are validator exit codes and stderr/stdout text: allowlisted cases must return exit `0`; blocked cases must return exit `2` and include a relevant “todos” or “round” error (`tests/test-allowlist-validators.sh:142-184`, `tests/test-allowlist-validators.sh:224-227`, `tests/test-allowlist-validators.sh:267-283`, `tests/test-allowlist-validators.sh:323-383`). Test state transitions are limited to temp repo/loop setup; production validators do not mutate the tested files.
- gates_or_invariants: Core invariant is that allowlisting is narrow by both basename and active loop directory. `is_allowlisted_file` requires `file_path == active_loop_dir/$allowed` for one of four filenames (`hooks/lib/loop-common.sh:570-583`). `Write`, `Edit`, and `Read` validators first find the active loop with optional `session_id`, then only bypass normal blocking if `is_allowlisted_file` passes (`hooks/loop-write-validator.sh:58-66`, `hooks/loop-edit-validator.sh:41-49`, `hooks/loop-read-validator.sh:58-66`). Bash validator’s todos exception is more restrictive: it requires command text to contain the lowercased full active loop path and `round-[12]-todos.md`, blocking bare names, old loop dirs, and same-basename different roots (`hooks/loop-bash-validator.sh:381-393`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`tests/test-allowlist-validators.sh:17`) and executes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` (`tests/test-allowlist-validators.sh:139`, `tests/test-allowlist-validators.sh:195`, `tests/test-allowlist-validators.sh:238`, `tests/test-allowlist-validators.sh:294`). Those validators depend on shared JSON validation, active loop discovery, state parsing, round-number extraction, and template-backed blocked messages in `loop-common.sh` (`hooks/lib/loop-common.sh:91-128`, `hooks/lib/loop-common.sh:230-306`, `hooks/lib/loop-common.sh:392-464`, `hooks/lib/loop-common.sh:542-584`).
- edge_cases_or_failure_modes: Explicit edge cases include wrong active-loop directory (`tests/test-allowlist-validators.sh:120-127`), non-allowlisted rounds (`tests/test-allowlist-validators.sh:104-118`), Bash commands using only a bare filename (`tests/test-allowlist-validators.sh:342-353`), old loop dirs (`tests/test-allowlist-validators.sh:355-368`), and same timestamp basename under another root (`tests/test-allowlist-validators.sh:370-383`). A notable runtime nuance: `loop-edit-validator.sh` does not use the stricter `validate_hook_input`/deep nesting guard that `Read`, `Write`, and `Bash` use; it directly extracts fields with `jq` (`hooks/loop-edit-validator.sh:24-32`) even though this particular test does not cover malformed JSON.
- validation_or_tests: This file is itself the validation artifact. It reports pass/fail counters and exits with `$TESTS_FAILED`, making any failed assertion a nonzero test result (`tests/test-allowlist-validators.sh:385-393`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-086 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, especially prompt-template path resolution, template loading, single-pass placeholder substitution, fallback rendering, directory validation, shell metacharacter safety, and placeholder-injection prevention (`tests/test-template-loader.sh:1-13`).
- algorithmic_behavior: The test calls `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` in sequence. It verifies plugin-root-relative template discovery (`tests/test-template-loader.sh:41-52`; implementation `hooks/lib/template-loader.sh:24-31`), existing/missing template load behavior (`tests/test-template-loader.sh:54-78`; implementation `hooks/lib/template-loader.sh:33-48`), direct substitution for one or many variables and multiline templates (`tests/test-template-loader.sh:80-129`), real template integration using `block/wrong-round-number.md` (`tests/test-template-loader.sh:146-164`), and safe fallback behavior (`tests/test-template-loader.sh:196-222`).
- inputs_outputs_state: Inputs are in-memory template strings plus `VAR=value` assignments and real files under `prompt-template`. Outputs are rendered strings compared exactly to expected values. There is no persistent state mutation. The underlying loader builds `TMPL_VAR_*` environment variables for each assignment and runs a single `awk` pass over the template (`hooks/lib/template-loader.sh:58-131`).
- gates_or_invariants: The essential invariant is single-pass rendering: values containing `{{OTHER_VAR}}` must be emitted literally and not recursively substituted (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-129`; tested at `tests/test-template-loader.sh:488-543`). Missing variables remain as placeholders (`tests/test-template-loader.sh:181-194`). Unused variables are ignored (`tests/test-template-loader.sh:166-179`). Missing templates return empty from `load_template` but `load_and_render_safe` uses fallback text (`hooks/lib/template-loader.sh:36-48`, `hooks/lib/template-loader.sh:170-203`). Valid template directories must contain `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`; tested at `tests/test-template-loader.sh:224-244`).
- dependencies_and_callers: The test sources `hooks/lib/template-loader.sh` directly (`tests/test-template-loader.sh:10-13`). `loop-common.sh` also sources the loader and initializes `TEMPLATE_DIR` for all validators (`hooks/lib/loop-common.sh:171-180`). Runtime users include message helpers such as `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:624-633`) and PR setup prompt rendering (`scripts/setup-pr-loop.sh:712`, `scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:800`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:914`, `scripts/setup-pr-loop.sh:940`).
- edge_cases_or_failure_modes: The test covers shell metacharacters that previously could be interpreted or mishandled by replacement logic: ampersands, backslashes, dollar signs, backticks, pipes, semicolons, globs, parentheses, redirection, quotes, hashes, tildes, combined metacharacters, Windows paths, regex-like content, JSON, and multiline values (`tests/test-template-loader.sh:247-486`). It also covers empty variable values, non-ASCII template/value content, underscore-prefixed variable names, and numbered variable names (`tests/test-template-loader.sh:545-639`). A potential edge not asserted: variable names are documented as uppercase/numbers/underscores (`hooks/lib/template-loader.sh:7-13`), but the implementation accepts any text between `{{` and `}}` if a matching `TMPL_VAR_...` environment variable exists (`hooks/lib/template-loader.sh:111-122`).
- validation_or_tests: The file prints colored PASS/FAIL counters and exits `0` only when `TESTS_FAILED == 0`; otherwise exits `1` (`tests/test-template-loader.sh:641-659`). It is both unit and regression coverage for prompt rendering safety.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-116 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template used when Bash attempts to modify RLCR summary files. It converts a validator block into actionable guidance: do not use Bash for summaries; use Write or Edit at `{{CORRECT_PATH}}`; Bash tools bypass validation hooks (`prompt-template/block/summary-bash-write.md:1-8`).
- algorithmic_behavior: This template is loaded through `summary_bash_blocked_message`, which supplies `CORRECT_PATH` and falls back to an inline Bash-block message if the template is unavailable (`hooks/lib/loop-common.sh:624-633`). `loop-bash-validator.sh` invokes that helper whenever `command_modifies_file "$COMMAND_LOWER" "round-[0-9]+-summary\.md"` matches, then exits `2` (`hooks/loop-bash-validator.sh:370-379`).
- inputs_outputs_state: Input variable is `CORRECT_PATH`, normally the active loop’s current round summary path: `"$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` (`hooks/loop-bash-validator.sh:375-377`). Output is rendered Markdown sent to stderr as the blocking reason. No state changes occur; the transition is validator allow/block only.
- gates_or_invariants: Invariant is that summary mutation must go through Write/Edit validation, not shell redirection, `cat`, `echo`, `sed`, `awk`, or similar Bash write paths (`prompt-template/block/summary-bash-write.md:3-8`). The Bash validator blocks all summary-file modifications by command pattern regardless of current round or allowlist, unlike Write/Edit validators where historical summary files have an allowlist exception (`hooks/loop-bash-validator.sh:370-379`; compare `hooks/loop-write-validator.sh:230-247` and `hooks/loop-edit-validator.sh:155-180`).
- dependencies_and_callers: Depends on `load_and_render_safe` from `template-loader.sh` via `loop-common.sh` (`hooks/lib/template-loader.sh:170-203`, `hooks/lib/loop-common.sh:624-633`). Called by `hooks/loop-bash-validator.sh` only in the observed references. `tests/test-template-references.sh` includes this template in the common-template existence check (`tests/test-template-references.sh:149-167`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` emits the inline fallback (`hooks/lib/template-loader.sh:176-203`, `hooks/lib/loop-common.sh:628-632`). If `CORRECT_PATH` contains characters that look like template placeholders, the single-pass renderer prevents secondary substitution (`hooks/lib/template-loader.sh:50-57`, tested in `tests/test-template-loader.sh:488-543`). Command detection edge cases belong to `command_modifies_file`, not this template; this template only formats the denial.
- validation_or_tests: Template existence is checked by `tests/test-template-references.sh` (`tests/test-template-references.sh:149-167`). Rendering safety for `load_and_render_safe` and special characters is covered by `tests/test-template-loader.sh` (`tests/test-template-loader.sh:196-222`, `tests/test-template-loader.sh:247-543`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-146 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: Prompt-template header for the initial PR review loop prompt. It establishes Round 0 context, identifies the PR number, branch, active bots, and introduces fetched review comments (`prompt-template/pr-loop/round-0-header.md:1-15`).
- algorithmic_behavior: `scripts/setup-pr-loop.sh` builds `TEMPLATE_VARS` with `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, and `BOT_MENTION_STRING`, defines an inline fallback header, renders this template via `load_and_render_safe`, writes it to `round-0-prompt.md`, then appends fetched PR comments from `round-0-pr-comment.md` (`scripts/setup-pr-loop.sh:721-764`). The header is the first prompt segment before task instructions selected by whether comments exist (`scripts/setup-pr-loop.sh:766-851`).
- inputs_outputs_state: Inputs are template variables `{{PR_NUMBER}}`, `{{START_BRANCH}}`, and `{{ACTIVE_BOTS_DISPLAY}}` used directly in this file (`prompt-template/pr-loop/round-0-header.md:7-10`). The setup script also passes extra variables used by subsequent task templates (`scripts/setup-pr-loop.sh:731-738`). Output is Markdown prepended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md`; this participates in PR-loop state by instructing the active agent but does not itself update `state.md`.
- gates_or_invariants: The header hard-codes “Read and execute below with ultrathink” and “PR Review Loop (Round 0)” (`prompt-template/pr-loop/round-0-header.md:1-5`), anchoring the loop phase. It must be rendered before comment content, so review comments remain under the “Review Comments” heading (`scripts/setup-pr-loop.sh:757-764`). If the template is missing, fallback content preserves the same fields and section structure (`scripts/setup-pr-loop.sh:740-758`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` at render site `pr-loop/round-0-header.md` (`scripts/setup-pr-loop.sh:757-758`). Depends on `hooks/lib/template-loader.sh` single-pass rendering through `load_and_render_safe` (`hooks/lib/template-loader.sh:170-203`). It coordinates with sibling PR-loop templates `goal-tracker-initial.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, and critical-requirements templates in the same setup flow (`scripts/setup-pr-loop.sh:712`, `scripts/setup-pr-loop.sh:800`, `scripts/setup-pr-loop.sh:847`, `scripts/setup-pr-loop.sh:914`, `scripts/setup-pr-loop.sh:940`).
- edge_cases_or_failure_modes: Empty or unusual branch/bot display values would render literally under the corresponding fields; setup performs YAML-safety validation for branch/model/effort before creating state, but the template itself has no validation (`scripts/setup-pr-loop.sh:384-407`). Placeholder injection from values is mitigated by the loader’s single-pass renderer (`hooks/lib/template-loader.sh:50-57`, `tests/test-template-loader.sh:488-543`). Missing template falls back to inline content (`scripts/setup-pr-loop.sh:740-758`).
- validation_or_tests: Covered indirectly by template-loader fallback/rendering tests and PR-loop system tests. The direct render mechanism is validated by `tests/test-template-loader.sh` for `load_and_render_safe` behavior (`tests/test-template-loader.sh:196-222`) and by setup script tests referenced in repo search for `setup-pr-loop.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-026`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-056`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-086`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-116`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-146`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`