# agent_19 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-019 `directory` `skills/humanize-gen-plan`
- cursor: `[_]`
- core_role: Defines the `humanize-gen-plan` flow skill. Its algorithmic responsibility is to transform a draft document into a structured implementation plan with goal, acceptance criteria, path boundaries, feasibility hints, dependencies, and milestones. The directory contains one file, `skills/humanize-gen-plan/SKILL.md`; no nested child files were present under the assigned directory.
- algorithmic_behavior: The flow is explicit in the Mermaid graph at `skills/humanize-gen-plan/SKILL.md:17`: begin with IO validation via `{{HUMANIZE_RUNTIME_ROOT}}/scripts/validate-gen-plan-io.sh --input <draft> --output <plan>` at lines `19-21`; read the draft at `23`; reject irrelevant drafts at `24-26`; analyze clarity, consistency, completeness, and functionality at `27`; loop through user clarification if issues remain at `28-30`; confirm quantitative metrics when present at `31-33`; generate the plan at `34-35`; review and fix inconsistencies at `36-39`; ask for language unification when multiple languages appear at `40-42`; report success with plan path, AC count, and language status at `43-44`.
- inputs_outputs_state: Inputs are `--input <path/to/draft.md>` and `--output <path/to/plan.md>` per `SKILL.md:47-52`. Output is a `plan.md`-style document with `Goal Description`, `Acceptance Criteria`, `Path Boundaries`, `Dependencies and Sequence`, and `Implementation Notes` per `SKILL.md:53-92`. The paired validator actually materializes the output by copying `prompt-template/plan/gen-plan-template.md`, appending the original draft between `--- Original Design Draft Start/End ---`, and exiting 0 at `scripts/validate-gen-plan-io.sh:133-160`. State is mostly file-system state: draft path must exist and be non-empty; output path must be new; output directory must be writable.
- gates_or_invariants: Exit-code contract is defined in `SKILL.md:94-105`: 0 success, 1 missing input, 2 empty input, 3 missing output directory, 4 output already exists, 5 no write permission, 6 invalid args, 7 missing plan template. The actual validator enforces argument presence at `scripts/validate-gen-plan-io.sh:29-67`, input existence and non-empty checks at `79-93`, output directory/existence checks at `95-116`, write permission at `118-124`, and template existence at `142-146`. The command wrapper mirrors these phases and exits on each validation failure in `commands/gen-plan.md:30-47`.
- dependencies_and_callers: The skill depends on the hydrated `{{HUMANIZE_RUNTIME_ROOT}}` path at `SKILL.md:11-15`, the validator script at `scripts/validate-gen-plan-io.sh`, and the plan template. It is exposed as `/flow:humanize-gen-plan` or `/skill:humanize-gen-plan` at `SKILL.md:107-122`. `commands/gen-plan.md:4-12` grants the command access to the validator, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion`. Installation docs and scripts reference this skill as part of the shipped skill set.
- edge_cases_or_failure_modes: Drafts unrelated to the repository are rejected by the relevance gate at `SKILL.md:24-26`. Analysis issues cause a user-clarification loop at `28-30`. Quantitative metrics require explicit user confirmation at `31-33`. Multiple languages trigger user language unification at `40-42`. The validator fails if the output already exists or is a directory at `scripts/validate-gen-plan-io.sh:103-116`, so the flow is intentionally create-only and avoids overwriting existing plans.
- validation_or_tests: `tests/test-gen-plan.sh:426-542` tests `validate-gen-plan-io.sh` exit codes for missing values, unknown options, missing/empty input, missing output directory, existing output, output directory path, valid paths, and help. The command flow and validator are also referenced in `commands/gen-plan.md:30-47`. I did not execute the tests because this branch export is read-only and the sandbox blocks write-heavy test setup.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-049 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime setup script for the RLCR loop. It creates the `.humanize/rlcr/<timestamp>/` session, validates prerequisites and plan safety, records loop state, prepares a goal tracker, and emits the initial Claude prompt. In this branch it also implements the dependency preflight for required external commands.
- algorithmic_behavior: Argument parsing handles positional plan file, `--plan-file`, `--track-plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--claude-answer-codex`, and `--agent-teams` at `scripts/setup-rlcr-loop.sh:135-247`. The dependency check initializes `MISSING_DEPS` and appends missing `codex`, `jq`, and `git` with install hints at `263-275`, then emits one consolidated error and exits 1 at `277-286`. It blocks concurrent RLCR/PR loops at `292-313`, validates Agent Teams environment at `319-330`, merges explicit and positional plan paths at `333-340`, and handles `--skip-impl` without a plan by creating an internal placeholder at `342-360`.
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR` for project root fallback at `253`, `DEFAULT_CODEX_MODEL/DEFAULT_CODEX_EFFORT` from `hooks/lib/loop-common.sh`, and a relative plan path unless `--skip-impl` supplies a placeholder. Outputs include `$LOOP_DIR/plan.md` as symlink to the real plan or placeholder at `758-780`, `$LOOP_DIR/state.md` with fields `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, `ask_codex_question`, `session_id`, `agent_teams`, and `started_at` at `792-812`; `.humanize/.pending-session-id` for PostToolUse session capture at `814-823`; `.review-phase-started` for skip-impl at `825-828`; `goal-tracker.md` at `830-953`; and `round-0-prompt.md` at `955-1090`.
- gates_or_invariants: The script requires a git repo and at least one commit at `367-377`. Plan path must be relative at `389-393`, contain no spaces at `395-401`, contain no shell metacharacters at `403-410`, not be a symlink at `415-419`, have an existing non-symlink parent path at `421-449`, exist and be readable at `451-461`, resolve under project root at `463-475`, and not be inside a submodule at `477-490`. Tracking mode is strict: `--track-plan-file` requires the plan to be tracked and clean at `516-530`; otherwise tracked plans are rejected unless made gitignored or run with `--track-plan-file` at `531-540`. Content must have at least 5 lines and at least 3 meaningful non-comment content lines at `552-605`. Branch, model, and effort values are YAML-safety checked at `616-648`. Working tree must be clean at `650-670`. Base branch detection is user input first, then remote default if local, then local `main`, then local `master` at `672-726`, and base commit is captured to avoid self-diff drift at `736-744`.
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` at `25-27` and `hooks/lib/loop-common.sh` at `29-33`. Uses `find_active_loop`, `find_active_pr_loop`, and default Codex model constants from the library. Uses `git`, `codex`, and `jq` as hard dependencies after the branch change. Agent Teams prompt injection reads `prompt-template/claude/agent-teams-instructions.md` and `agent-teams-core.md` when enabled at `1027-1048`. The PostToolUse hook consumes `.humanize/.pending-session-id`; `hooks/loop-post-bash-hook.sh:57-80` documents matching the full setup script path to avoid false positives.
- edge_cases_or_failure_modes: Missing dependencies now fail before loop activation with all missing tools listed, avoiding later mid-loop failures. `--skip-impl` creates a placeholder plan and sets `review_started: true`, so no normal plan content validation is performed when no plan is supplied. Git command timeouts fail closed for repo checks, plan status, tracking detection, working-tree checks, and base branch detection. Branch names containing YAML-unsafe characters are rejected. `--full-review-round` must be numeric and at least 2 at `203-217`. The help text says positive integer for some numeric options, but regexes allow zero for `--max` and `--codex-timeout`; robustness tests document zero timeout as current behavior.
- validation_or_tests: `tests/robustness/test-setup-scripts-robustness.sh` covers general argument, plan, timeout, skip-impl, and dependency behavior. Dependency-specific coverage is at `1085-1209`: missing `codex` at `1085-1111`, missing `jq` at `1113-1140`, multiple missing deps listed together at `1142-1182`, and all deps present proceeding past dependency check at `1184-1209`. Earlier success-path tests confirm valid RLCR setup reaches dependency checking rather than argument failure at `668-692`. Additional plan validation tests exist in `tests/test-plan-file-validation.sh` and robustness tests under `tests/robustness/test-plan-file-robustness.sh` and `test-path-validation-robustness.sh`. I did not execute tests because the branch is read-only and the sandbox blocks write-heavy git/test operations.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-079 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop stop-hook behavior. It defines `run_stophook_tests` and a set of integration-style shell tests that create isolated `.humanize/pr-loop/<timestamp>/state.md` sessions, mock `gh`, `git`, and sometimes `codex`, run `hooks/pr-loop-stop-hook.sh`, and assert state-file or block-decision transitions.
- algorithmic_behavior: The test suite enumerates stop-hook requirements at `tests/test-pr-loop-stophook.sh:3-12`: force-push detection, trigger validation, bot timeout handling, state-file management, and dynamic `startup_case` updates. Individual tests cover old trigger rejection after a later commit timestamp at `20-138`; startup case 1 allowing round-0 operation without a trigger at `140-244`; empty `active_bots` producing approval at `246-325`; dynamic `startup_case` update when bot comments arrive at `327-450`; unpushed commits blocking exit at `452-536`; force-push history rewrite detection at `538-640`; missing trigger blocking startup case 4 at `642-752`; timeout-based bot removal and approval at `754-894`; Codex `+1` reaction approval at `896-1009`; Claude eyes timeout blocking at `1011-1143`; dynamic timestamp-based startup case re-evaluation at `1145-1388`; fork PR parent-repo lookup at `1390-1517`; and mixed bot approval/issue handling at `1519-1766`.
- inputs_outputs_state: Each test writes a synthetic PR loop `state.md` with YAML frontmatter fields such as `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, `codex_timeout`, `poll_interval`, `poll_timeout`, `started_at`, `last_trigger_at`, `trigger_comment_id`, `startup_case`, `latest_commit_sha`, and `latest_commit_at`. Each test also writes the required `round-N-pr-resolve.md` file before invoking the hook. Expected outputs are `approve-state.md` terminal transitions, mutated `state.md` fields such as `startup_case` or `active_bots`, `round-N-pr-check.md`, JSON block decisions containing trigger/timeout/unpushed/force-push messages, or continued state when issues remain.
- gates_or_invariants: The tests assert PR loop stop is blocked unless resolution summaries exist, git is clean and pushed, force-push history remains compatible or receives a fresh trigger, startup cases that require triggers have a current user @bot comment after the latest commit, Claude configured for required triggers responds with an eyes reaction, and all active bots either approve, time out, or are removed before approval. The corresponding hook parses state at `hooks/pr-loop-stop-hook.sh:88-177`, resolves fork/base repo before PR checks at `197-237`, handles closed/merged PR state at `240-256`, blocks missing resolve files at `258-277`, checks git clean and unpushed commits at `279-362`, detects force-push rewrites and clears stale trigger fields at `364-424`, approves empty `active_bots` at `438-448`, filters trigger comments after `latest_commit_at` at `466-526`, and computes trigger requirement from round and startup case at `609-644`.
- dependencies_and_callers: Calls the production hook `hooks/pr-loop-stop-hook.sh`. The hook depends on `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, `jq`, `git`, `gh`, and `codex` for local validation. The tests create mock binaries under `$test_subdir/bin` and prepend them to `PATH`, while `CLAUDE_PROJECT_DIR` scopes the hook to the isolated test project.
- edge_cases_or_failure_modes: The suite exercises stale trigger reuse after force push (`latest_commit_at` after old trigger), startup case 1 no-trigger exception, unpushed local commits, history rewrite where stored SHA is not ancestor of current HEAD, missing required trigger in case 4, per-bot timeout with active bot removal, `codex` approval via issue `+1` reaction, Claude bot not adding eyes reaction, fork PRs where comments live on the parent repository, and mixed approvals where one bot is removed but another remains active. The mixed-approval test specifically guards against inflating resolved issue count when some bots still report issues: it asserts no `approve-state.md`, Codex remains in `active_bots`, and Claude is removed at `1740-1762`.
- validation_or_tests: This file is itself the validation. The test runner invokes all defined tests at `1768-1780`. The hook sections that satisfy the assertions include Codex `+1` pre-poll approval at `hooks/pr-loop-stop-hook.sh:646-719`, missing trigger block at `721-747`, Claude eyes block at `749-800`, polling and timeout bookkeeping at `802-1179`, comment file creation and Codex prompt construction at `1182-1303`, Codex execution and failure gates at `1305-1367`, approval/waiting/usage-limit handling at `1369-1423`, active bot recomputation at `1425-1510`, goal-tracker issue count handling at `1512-1542`, dynamic startup-case re-evaluation at `1554-1566`, state rewrite at `1568-1590`, and feedback block generation at `1599-1646`. I did not execute the tests due read-only sandbox constraints.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-109 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Stop/block message template for the invariant that the loop-local `plan.md` is a backup/reference copy and must not be modified by agents or shell commands during an RLCR session.
- algorithmic_behavior: The template’s complete content is short: title at `prompt-template/block/plan-backup-protected.md:1`, explanation that loop-directory `plan.md` is a backup and cannot be modified at `3`, integrity rationale at `5`, and instruction to read rather than modify at `7`. It does not perform logic itself; it provides the user-facing reason for validator blocks.
- inputs_outputs_state: Input is only template loading through `load_and_render_safe`; there are no placeholder variables in this template. Output is a Markdown reason string emitted to stderr by validators when a write/edit/bash command targets `.humanize/rlcr/.../plan.md`. State transition is a stop-hook/tool-validator block: attempted modification is rejected with exit 2 and no file mutation should occur.
- gates_or_invariants: Enforces plan backup immutability. `hooks/loop-write-validator.sh:177-187` blocks writes to plan backups inside `.humanize/rlcr/`. `hooks/loop-bash-validator.sh:330-340` blocks shell commands that modify `.humanize/rlcr/.../plan.md`. `hooks/loop-edit-validator.sh:127-138` blocks edit attempts to loop backup `plan.md`. The invariant protects the original implementation plan from drift after setup symlinks/copies it into the session.
- dependencies_and_callers: Loaded via `load_and_render_safe` from `hooks/lib/template-loader.sh`, with callers in write, bash, and edit validators. `scripts/setup-rlcr-loop.sh` creates the loop backup/reference at `758-780`, making this template part of the guard surface around that generated state.
- edge_cases_or_failure_modes: Applies only when the target is named `plan.md` under `.humanize/rlcr/`; ordinary source plan file edits outside the loop backup are governed by other plan-file tracking and loop rules. Shell command detection depends on `command_modifies_file` pattern matching in `loop-bash-validator.sh`; complex indirect modifications could rely on broader validator coverage.
- validation_or_tests: Template existence/rendering is included in `tests/test-templates-comprehensive.sh:531-539` for another Codex template and likely broader template-loader coverage; direct call-site coverage is through plan-file hook tests such as `tests/test-plan-file-hooks.sh` and validator tests. The exact enforcement call sites are the strongest validation references: `loop-write-validator.sh:181-186`, `loop-bash-validator.sh:336-340`, and `loop-edit-validator.sh:131-137`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-139 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt block that assigns Codex the responsibility to evaluate and apply Claude’s “Goal Tracker Update Request” sections after round 0, while preserving the goal tracker’s immutable goal and acceptance criteria.
- algorithmic_behavior: The template tells Codex that Claude cannot directly modify `goal-tracker.md` after Round 0 at `prompt-template/codex/goal-tracker-update-section.md:1-3`. It requires Codex to evaluate whether requested changes are justified and serve the Ultimate Goal at `5`; if approved, update `@{{GOAL_TRACKER_FILE}}` at `6`; move tasks among Active/Completed/Deferred at `7`; add Plan Evolution Log entries with round and justification at `8`; add Open Issues at `9`; never modify the immutable section at `10`; and explain rejected requests at `11`. Common request classes are listed at `13-17`.
- inputs_outputs_state: Input is the rendered `GOAL_TRACKER_FILE` template variable. Output is prompt text embedded into Codex’s review prompt. State transitions are indirect: Codex may update mutable sections of `goal-tracker.md` by moving tasks, logging plan evolution, adding issues, or deferring work; it must not mutate Ultimate Goal or Acceptance Criteria after initialization.
- gates_or_invariants: The core invariant is split ownership: after Round 0, Claude reports desired goal-tracker changes in summaries, while Codex review decides whether to apply them. This aligns with setup prompt initialization rules in `scripts/setup-rlcr-loop.sh:1003-1013`, which allow immutable-section setup only in Round 0, and the goal tracker rules at `1055-1070`. Validators block direct goal-tracker modification after Round 0: `hooks/loop-write-validator.sh:190-198`, `hooks/loop-edit-validator.sh:141-148`, and `hooks/loop-bash-validator.sh:343-350` begin that guard path.
- dependencies_and_callers: Loaded in `hooks/loop-codex-stop-hook.sh:808-812` into `GOAL_TRACKER_UPDATE_SECTION`, then used in regular and full alignment Codex review prompt construction. It depends on `load_and_render_safe` and the template variable `GOAL_TRACKER_FILE`. It is related to `prompt-template/pr-loop/codex-goal-tracker-update.md` for PR-loop-specific goal-tracker handling, but this assigned template is for RLCR Codex review.
- edge_cases_or_failure_modes: Codex may reject Claude’s requested goal-tracker update if it does not serve the Ultimate Goal. Deferrals require strong justification at `17`. The invariant can fail if Codex modifies the immutable section, if Claude bypasses validators, or if a summary omits needed update requests; the template reduces that risk by making evaluation explicit in every Codex review prompt.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` renders `codex/goal-tracker-update-section.md` and validates the real template loads. Hook call site is `hooks/loop-codex-stop-hook.sh:808-812`. Goal-tracker robustness and hook tests elsewhere cover mutation blocking and state parsing. I did not run tests in the read-only export.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-169 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It verifies tolerant and strict parsing behavior in `hooks/lib/loop-common.sh`, especially around YAML frontmatter, required fields, numeric values, partial/corrupted files, and newer fields such as `full_review_round`.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-state-file-robustness.sh:14-17`, sets up a temp test directory at `19`, then writes many synthetic `state.md` variants and calls `parse_state_file`, `parse_state_file_strict`, and `get_current_round`. Positive tests cover full valid state at `33-60`, direct current-round extraction at `62-70`, extra fields at `72-98`, quoted strings at `100-122`, and zero round at `124-139`. Negative/edge tests cover missing frontmatter strict rejection and tolerant default at `149-171`, non-numeric current round strict rejection and tolerant handling at `173-199`, missing required fields strict rejection and tolerant defaults at `201-228`, empty file at `230-240`, partial YAML at `242-257`, malformed YAML at `259-275`, very large numbers at `277-295`, negative numbers at `297-312`, special characters and spaces in values at `314-336`, trailing whitespace at `338-356`, non-existent file at `358-365`, binary trailing content at `367-384`, CRLF line endings at `386-397`, and `full_review_round` parsing/default/minimum at `399-474`.
- inputs_outputs_state: Inputs are temp state files with YAML frontmatter. Outputs are pass/fail assertions and parser-set global variables such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, and `STATE_FULL_REVIEW_ROUND`. Tolerant parsing should return usable defaults for missing optional values; strict parsing should reject schema-critical corruption. Final output is `print_test_summary "State File Robustness Test Summary"` and process exit status at `476-481`.
- gates_or_invariants: The underlying parser in `hooks/lib/loop-common.sh` extracts frontmatter with `sed` and returns `current_round` default `0` in `get_current_round` at `311-321`. `_parse_state_fields` sets all known state variables at `327-346`. Tolerant `parse_state_file` requires only file existence, applies defaults for `current_round=0`, `max_iterations=10`, `push_every_round=false`, `full_review_round=5`, `ask_codex_question=true`, and `agent_teams=false`, and leaves `review_started` unset for schema validation at `367-389`. Strict `parse_state_file_strict` requires at least two `---` separators at `409-415`, requires `current_round`, `max_iterations`, `review_started`, and `base_branch` at `421-437`, requires numeric `current_round` and `max_iterations` at `439-449`, and requires boolean `review_started` at `451-455`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` parsing functions and `tests/test-helpers.sh` pass/fail helpers. These parser functions are called by RLCR validators and stop hooks, including `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and scripts such as `cancel-rlcr-loop.sh`.
- edge_cases_or_failure_modes: The suite intentionally distinguishes strict rejection from tolerant non-crashing behavior. Missing frontmatter, missing required fields, non-numeric values, and malformed YAML should not crash. Binary content after frontmatter should not corrupt `current_round`. CRLF handling is only asserted as graceful/non-crashing, not a specific parse result. Negative round numbers are accepted by strict numeric regex in the library because `^-?[0-9]+$` is allowed; this is a known parser behavior rather than a semantic round validity check.
- validation_or_tests: This file is the validation. It directly exercises production parser functions. It also complements `tests/robustness/test-state-transition-robustness.sh` and `test-concurrent-state-robustness.sh`, which use the same parser functions for transition and concurrent-write scenarios. I did not execute it because it writes temp files and the current branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6/6 assigned item sections present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`