# agent_12 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-012 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: RLCR Claude-facing prompt surface. This directory defines the prompt contracts that move Claude through implementation continuation, review remediation, finalize, agent-teams coordination, goal-tracker update requests, open-question handling, push policy, and post-alignment follow-up.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` builds round 0 prompts and conditionally injects agent-teams templates from this directory (`scripts/setup-rlcr-loop.sh:999`, `scripts/setup-rlcr-loop.sh:1027`). `hooks/loop-codex-stop-hook.sh` later renders finalize prompts (`hooks/loop-codex-stop-hook.sh:1119`, `hooks/loop-codex-stop-hook.sh:1147`), review-phase prompts (`hooks/loop-codex-stop-hook.sh:1198`), next-round implementation prompts (`hooks/loop-codex-stop-hook.sh:1574`), open-question notices (`hooks/loop-codex-stop-hook.sh:1590`), post-alignment instructions (`hooks/loop-codex-stop-hook.sh:1615`), footer/push/goal update additions (`hooks/loop-codex-stop-hook.sh:1623`, `hooks/loop-codex-stop-hook.sh:1629`, `hooks/loop-codex-stop-hook.sh:1638`), and agent-team continuation instructions (`hooks/loop-codex-stop-hook.sh:1645`).
- inputs_outputs_state: Inputs are template variables such as `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `SUMMARY_FILE`, `NEXT_SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, and `REVIEW_SKIP_REASON`. Outputs are markdown prompt bodies written into loop files such as `round-N-prompt.md` or returned in hook JSON block reasons. State transitions are indirect: these prompts instruct the active Claude session what phase it is in and what evidence or summary file must be produced.
- gates_or_invariants: `agent-teams-core.md` makes the team leader a coordinator only and forbids direct implementation (`prompt-template/claude/agent-teams-core.md:1`, `prompt-template/claude/agent-teams-core.md:3`, `prompt-template/claude/agent-teams-core.md:18`). `next-round-footer.md` forbids lying, editing loop state, or calling cancellation to escape (`prompt-template/claude/next-round-footer.md:4`). `review-phase-prompt.md` requires addressing all `[P0-9]` findings and limits work to fixes (`prompt-template/claude/review-phase-prompt.md:11`, `prompt-template/claude/review-phase-prompt.md:12`). Finalize templates require functionality-equivalent changes only and test preservation (`prompt-template/claude/finalize-phase-prompt.md:20`, `prompt-template/claude/finalize-phase-prompt.md:21`; `prompt-template/claude/finalize-phase-skipped-prompt.md:30`, `prompt-template/claude/finalize-phase-skipped-prompt.md:31`).
- dependencies_and_callers: Direct callers are `scripts/setup-rlcr-loop.sh` for initial prompt generation and `hooks/loop-codex-stop-hook.sh` for phase-transition prompts. Child roles: `agent-teams-instructions.md` initial agent-team header (`prompt-template/claude/agent-teams-instructions.md:1`), `agent-teams-core.md` shared team-leader rules, `agent-teams-continue.md` new-round team reset guidance (`prompt-template/claude/agent-teams-continue.md:7`), `finalize-phase-prompt.md` passed-review finalize prompt, `finalize-phase-skipped-prompt.md` skipped-review finalize prompt, `goal-tracker-update-request.md` summary schema (`prompt-template/claude/goal-tracker-update-request.md:2`), `next-round-footer.md` exit evidence requirements, `next-round-prompt.md` implementation continuation, `open-question-notice.md` AskUserQuestion gate, `post-alignment-action-items.md` full-alignment follow-up, `push-every-round-note.md` push reminder, and `review-phase-prompt.md` review remediation.
- edge_cases_or_failure_modes: Callers have fallbacks when templates are missing (`hooks/loop-codex-stop-hook.sh:1127`, `hooks/loop-codex-stop-hook.sh:1183`, `hooks/loop-codex-stop-hook.sh:1656`). Review content is inserted into prompts as rendered content, so phase correctness relies on delimiter text and template-loader behavior rather than semantic validation inside these markdown files. Agent-team prompts explicitly warn that old teammates no longer exist and new members are cold-started (`prompt-template/claude/agent-teams-continue.md:7`, `prompt-template/claude/agent-teams-continue.md:10`).
- validation_or_tests: `tests/test-agent-teams.sh` is referenced as validating agent-teams setup and continuation injection, including template existence and review-phase exclusion paths; it is included in the full suite (`tests/run-all-tests.sh:60`). Broader template syntax/reference coverage is also in the full suite (`tests/run-all-tests.sh:33`, `tests/run-all-tests.sh:37`, `tests/run-all-tests.sh:40`). Static research only; tests were not executed in this read-only evidence pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-042 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: PR-loop startup case classifier. It determines whether reviewer bots have commented and whether their reviews are stale relative to the latest PR commit, producing JSON consumed by PR loop setup and stop-hook routing.
- algorithmic_behavior: Requires `pr_number` and `--bots` (`scripts/check-pr-reviewer-status.sh:49`, `scripts/check-pr-reviewer-status.sh:75`, `scripts/check-pr-reviewer-status.sh:81`). Maps logical bot names to GitHub authors, with `codex` mapped to `chatgpt-codex-connector[bot]` and others to `<bot>[bot]` (`scripts/check-pr-reviewer-status.sh:94`). Resolves the PR base repo by checking current repo first and parent repo for fork support (`scripts/check-pr-reviewer-status.sh:113`, `scripts/check-pr-reviewer-status.sh:126`). Fetches latest commit info, issue comments, review comments, and PR review submissions (`scripts/check-pr-reviewer-status.sh:143`, `scripts/check-pr-reviewer-status.sh:156`, `scripts/check-pr-reviewer-status.sh:159`, `scripts/check-pr-reviewer-status.sh:164`), combines them (`scripts/check-pr-reviewer-status.sh:168`), then classifies each bot as commented/missing/stale (`scripts/check-pr-reviewer-status.sh:179`, `scripts/check-pr-reviewer-status.sh:186`, `scripts/check-pr-reviewer-status.sh:194`).
- inputs_outputs_state: Inputs are GitHub CLI context, PR number, comma-separated bot list, `gh`, `jq`, and timeout wrapper. Output is JSON with `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` (`scripts/check-pr-reviewer-status.sh:259`). It does not mutate files; it feeds state fields in `setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:437`, `scripts/setup-pr-loop.sh:443`).
- gates_or_invariants: Commit-info fetch is a hard failure (`scripts/check-pr-reviewer-status.sh:143`, `scripts/check-pr-reviewer-status.sh:146`), while comment endpoint failures degrade to empty arrays (`scripts/check-pr-reviewer-status.sh:156`, `scripts/check-pr-reviewer-status.sh:159`, `scripts/check-pr-reviewer-status.sh:164`). Case logic: no comments is case 1, partial fresh comments case 2, all fresh comments case 3, all with at least one stale bot case 4, and partial with at least one stale bot case 5 (`scripts/check-pr-reviewer-status.sh:220`, `scripts/check-pr-reviewer-status.sh:223`, `scripts/check-pr-reviewer-status.sh:237`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` (`scripts/check-pr-reviewer-status.sh:39`) and external `gh`/`jq`. `scripts/setup-pr-loop.sh` uses it to initialize startup case and latest commit state (`scripts/setup-pr-loop.sh:437`, `scripts/setup-pr-loop.sh:444`). `hooks/pr-loop-stop-hook.sh` uses startup case to require or skip trigger comments (`hooks/pr-loop-stop-hook.sh:620`) and to choose comment polling timestamps (`hooks/pr-loop-stop-hook.sh:817`). The stop hook also re-evaluates startup case dynamically per search evidence at `hooks/pr-loop-stop-hook.sh:1555`.
- edge_cases_or_failure_modes: Fork fallback can still leave `PR_BASE_REPO` empty if both repo probes fail, causing the later PR view to fail closed. Staleness compares ISO timestamp strings lexically (`scripts/check-pr-reviewer-status.sh:194`), which assumes normalized sortable timestamps. Empty or malformed bot names are not sanitized beyond author mapping. The file header describes case 5 as future distinction after all reviewers commented (`scripts/check-pr-reviewer-status.sh:27`), but the implementation uses case 5 for partial stale review state (`scripts/check-pr-reviewer-status.sh:227`).
- validation_or_tests: `tests/test-pr-loop-system.sh` covers no comments case 1 (`tests/test-pr-loop-system.sh:152`), partial case 2 (`tests/test-pr-loop-system.sh:173`), PR review submissions as reviewer evidence (`tests/test-pr-loop-system.sh:236`), all commented case 3 (`tests/test-pr-loop-system.sh:493`), case 4 all-commented stale emission (`tests/test-pr-loop-system.sh:1315`), and case 5 partial stale emission (`tests/test-pr-loop-system.sh:1347`). Static research only; tests were not executed.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-072 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file, git-state, branch-name, content, CLI, and Codex parameter validation. It verifies the setup algorithm fails closed on unsafe plan inputs before loop state is created.
- algorithmic_behavior: Builds temp git repos (`tests/test-plan-file-validation.sh:37`, `tests/test-plan-file-validation.sh:40`), unsets `CLAUDE_PROJECT_DIR` so the production script uses the temp repo (`tests/test-plan-file-validation.sh:19`), and installs a mock `codex` to satisfy dependency checking without invoking the real tool (`tests/test-plan-file-validation.sh:73`). It invokes `scripts/setup-rlcr-loop.sh` repeatedly and asserts exit codes plus error-message fragments.
- inputs_outputs_state: No external arguments. Inputs are generated plan files, git repos, branch names, and CLI flags. Outputs are colored PASS/FAIL/SKIP lines and process exit equal to `TESTS_FAILED` (`tests/test-plan-file-validation.sh:742`, `tests/test-plan-file-validation.sh:751`). State is isolated in `TEST_DIR` and cleaned by trap (`tests/test-plan-file-validation.sh:38`).
- gates_or_invariants: Covers rejection of absolute paths, missing files/directories, spaces, shell metacharacters, symlinks, unresolved directories, path escapes, non-git repos, repos without commits, tracked/untracked plan policy, modified tracked plan files, YAML-unsafe branch names, blank/sparse/comment-only content, conflicting `--plan-file` usage, and unsafe Codex model/effort values (`tests/test-plan-file-validation.sh:88`, `tests/test-plan-file-validation.sh:115`, `tests/test-plan-file-validation.sh:127`, `tests/test-plan-file-validation.sh:168`, `tests/test-plan-file-validation.sh:188`, `tests/test-plan-file-validation.sh:244`, `tests/test-plan-file-validation.sh:273`, `tests/test-plan-file-validation.sh:328`, `tests/test-plan-file-validation.sh:431`, `tests/test-plan-file-validation.sh:517`, `tests/test-plan-file-validation.sh:683`, `tests/test-plan-file-validation.sh:700`).
- dependencies_and_callers: Code under test is `scripts/setup-rlcr-loop.sh`, especially dependency checks for `codex`, `jq`, and `git` (`scripts/setup-rlcr-loop.sh:263`), path validation (`scripts/setup-rlcr-loop.sh:389`), tracking validation (`scripts/setup-rlcr-loop.sh:496`), content validation (`scripts/setup-rlcr-loop.sh:552`), branch/model validation (`scripts/setup-rlcr-loop.sh:616`, `scripts/setup-rlcr-loop.sh:632`), and clean tree/base branch checks. Included in aggregate test runner (`tests/run-all-tests.sh:36`).
- edge_cases_or_failure_modes: The metacharacter loop reports a final pass even after a failure path breaks (`tests/test-plan-file-validation.sh:179`, `tests/test-plan-file-validation.sh:181`, `tests/test-plan-file-validation.sh:186`), so aggregate failure count still records the issue but the log can include a misleading pass. Header claims submodule rejection coverage (`tests/test-plan-file-validation.sh:9`), but no dedicated submodule test was found in the inspected body. Some branch-name tests accept rejection by Git itself as equivalent to script rejection (`tests/test-plan-file-validation.sh:459`, `tests/test-plan-file-validation.sh:471`).
- validation_or_tests: This file is itself validation. It statically aligns with `setup-rlcr-loop.sh` gates: dependency preflight (`scripts/setup-rlcr-loop.sh:263`), relative/no-space/no-metachar path rules (`scripts/setup-rlcr-loop.sh:389`, `scripts/setup-rlcr-loop.sh:395`, `scripts/setup-rlcr-loop.sh:403`), symlink traversal rejection (`scripts/setup-rlcr-loop.sh:415`, `scripts/setup-rlcr-loop.sh:428`), and meaningful content rules (`scripts/setup-rlcr-loop.sh:562`). Not executed in this read-only research pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-102 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Hook block template for a fail-closed git status gate. It is used when the RLCR stop hook cannot verify repository cleanliness.
- algorithmic_behavior: Renders a markdown block headed “Git Status Failed,” includes `{{GIT_STATUS_EXIT}}`, lists likely causes, and instructs manual status checking (`prompt-template/block/git-status-failed.md:1`, `prompt-template/block/git-status-failed.md:3`, `prompt-template/block/git-status-failed.md:5`).
- inputs_outputs_state: Input is `GIT_STATUS_EXIT`. Output becomes the `reason` field in a hook JSON block decision. No file state is changed by the template itself.
- gates_or_invariants: The invariant is fail closed: if `git status --porcelain` fails or times out, the loop cannot verify repo state and must block instead of allowing exit. Caller code explicitly says this prevents bypassing checks (`hooks/loop-codex-stop-hook.sh:438`) and blocks when `GIT_STATUS_EXIT` is nonzero (`hooks/loop-codex-stop-hook.sh:449`, `hooks/loop-codex-stop-hook.sh:460`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh`, which caches git status, runs stale lock cleanup, renders this template with `load_and_render_safe`, then emits `{"decision":"block"}` (`hooks/loop-codex-stop-hook.sh:443`, `hooks/loop-codex-stop-hook.sh:452`, `hooks/loop-codex-stop-hook.sh:458`, `hooks/loop-codex-stop-hook.sh:460`). There is also an inline fallback in `hooks/pr-loop-stop-hook.sh` per search evidence at `hooks/pr-loop-stop-hook.sh:288`.
- edge_cases_or_failure_modes: The template deliberately conflates timeout and general failure because the only variable is an exit code. If the template is missing or malformed, the caller has a fallback string (`hooks/loop-codex-stop-hook.sh:453`). It only runs when the hook has identified a git repository; non-repo handling is outside this template.
- validation_or_tests: Broad template tests are in the aggregate suite (`tests/run-all-tests.sh:33`, `tests/run-all-tests.sh:37`, `tests/run-all-tests.sh:40`). No dedicated direct test for this exact block was found during static reference inspection.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-132 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Main implementation-continuation prompt template after Codex finds work remaining in an RLCR round.
- algorithmic_behavior: Tells Claude the work is unfinished, requires rereading the original plan via `@{{PLAN_FILE}}`, embeds Codex review content between delimiters, instructs Task system tracking for all discovered issues, and points to the goal tracker as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1`, `prompt-template/claude/next-round-prompt.md:5`, `prompt-template/claude/next-round-prompt.md:12`, `prompt-template/claude/next-round-prompt.md:16`, `prompt-template/claude/next-round-prompt.md:22`, `prompt-template/claude/next-round-prompt.md:30`).
- inputs_outputs_state: Inputs are `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`. The stop hook writes the rendered content to `round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:1574`). This does not mutate loop state by itself; it creates the next agent-facing instruction file.
- gates_or_invariants: Requires all discovered issues to be task-tracked and attempted, not only the most important ones (`prompt-template/claude/next-round-prompt.md:12`, `prompt-template/claude/next-round-prompt.md:13`). Goal tracker direct modification is prohibited after round 0 (`prompt-template/claude/next-round-prompt.md:30`), pushing updates through the separate update-request template appended by the hook (`hooks/loop-codex-stop-hook.sh:1638`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` with `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1574`). The hook may inject `open-question-notice.md` if it detects a short “Open Question” heading in the review (`hooks/loop-codex-stop-hook.sh:1579`), append post-alignment action items (`hooks/loop-codex-stop-hook.sh:1615`), footer (`hooks/loop-codex-stop-hook.sh:1623`), push note (`hooks/loop-codex-stop-hook.sh:1629`), goal update request (`hooks/loop-codex-stop-hook.sh:1638`), and agent-teams continuation (`hooks/loop-codex-stop-hook.sh:1645`).
- edge_cases_or_failure_modes: If this template is unavailable, the caller falls back to a shorter built-in prompt immediately before rendering (`hooks/loop-codex-stop-hook.sh:1568`). The review content is inserted directly inside delimiters, so misleading or adversarial review text is controlled by prompt convention rather than parser validation in this file.
- validation_or_tests: Template reference/syntax suites include generic coverage (`tests/run-all-tests.sh:37`, `tests/run-all-tests.sh:40`). Agent-team continuation tests exercise the hook path that appends to next-round prompts, per references in `tests/test-agent-teams.sh` found during static search. Not executed in this read-only pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-162 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Robustness matrix for production path and content validation in `scripts/setup-rlcr-loop.sh`. It complements the narrower plan-file validation test with many shell metacharacter and symlink traversal cases.
- algorithmic_behavior: Sources shared test helpers (`tests/robustness/test-path-validation-robustness.sh:14`), creates a temp repo (`tests/robustness/test-path-validation-robustness.sh:16`, `tests/robustness/test-path-validation-robustness.sh:38`), installs mock `codex` (`tests/robustness/test-path-validation-robustness.sh:22`), creates valid plan content with at least five lines (`tests/robustness/test-path-validation-robustness.sh:49`), and defines `test_path_validation` to invoke the production setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"` (`tests/robustness/test-path-validation-robustness.sh:78`, `tests/robustness/test-path-validation-robustness.sh:88`).
- inputs_outputs_state: No external arguments. Inputs are generated path strings and file fixtures. Output is PASS/FAIL summary via `print_test_summary`, then exit status from that summary (`tests/robustness/test-path-validation-robustness.sh:486`, `tests/robustness/test-path-validation-robustness.sh:487`). State is a temp git repo with ignored test artifacts (`tests/robustness/test-path-validation-robustness.sh:45`).
- gates_or_invariants: Positive cases accept normal relative paths, root filenames, dash/underscore, nested directories, and dots (`tests/robustness/test-path-validation-robustness.sh:122`, `tests/robustness/test-path-validation-robustness.sh:131`, `tests/robustness/test-path-validation-robustness.sh:141`, `tests/robustness/test-path-validation-robustness.sh:151`, `tests/robustness/test-path-validation-robustness.sh:161`). Negative cases reject absolute paths, spaces, semicolon, pipe, dollar, backticks, angle brackets, ampersand, glob characters, backslash, tilde, parentheses, braces, brackets, hash, and exclamation (`tests/robustness/test-path-validation-robustness.sh:179`, `tests/robustness/test-path-validation-robustness.sh:188`, `tests/robustness/test-path-validation-robustness.sh:199`, `tests/robustness/test-path-validation-robustness.sh:217`, `tests/robustness/test-path-validation-robustness.sh:271`, `tests/robustness/test-path-validation-robustness.sh:316`). Symlink file, parent-directory symlink, and symlink chain rejection are explicit (`tests/robustness/test-path-validation-robustness.sh:342`, `tests/robustness/test-path-validation-robustness.sh:352`, `tests/robustness/test-path-validation-robustness.sh:367`).
- dependencies_and_callers: Code under test is `scripts/setup-rlcr-loop.sh`, especially path validation (`scripts/setup-rlcr-loop.sh:389`, `scripts/setup-rlcr-loop.sh:395`, `scripts/setup-rlcr-loop.sh:403`), symlink checks (`scripts/setup-rlcr-loop.sh:415`, `scripts/setup-rlcr-loop.sh:428`), and content validation (`scripts/setup-rlcr-loop.sh:562`). It uses `tests/test-helpers.sh` for counters, temp dir setup, and summary reporting (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:58`, `tests/test-helpers.sh:86`). Included in aggregate test runner (`tests/run-all-tests.sh:69`).
- edge_cases_or_failure_modes: The validation classifier treats downstream “requires codex” and “must be gitignored” messages as path-validation success because they mean earlier path/content gates passed (`tests/robustness/test-path-validation-robustness.sh:97`, `tests/robustness/test-path-validation-robustness.sh:102`). Long filename handling is conditional on filesystem support (`tests/robustness/test-path-validation-robustness.sh:387`, `tests/robustness/test-path-validation-robustness.sh:392`). Unicode/CJK/emoji paths are noted as allowed but not directly fixture-tested in this file (`tests/robustness/test-path-validation-robustness.sh:414`).
- validation_or_tests: This file is itself robustness validation. It covers setup-script acceptance and rejection behavior through production calls and exits through shared summary logic. Static research only; not executed due the research-only/read-only instruction.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 unique item evidence sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`