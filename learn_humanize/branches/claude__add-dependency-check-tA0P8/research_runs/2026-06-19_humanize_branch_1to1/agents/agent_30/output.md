# agent_30 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-030 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash validator for active RLCR and PR loops. It prevents Bash from bypassing Write/Edit/Read validators or mutating local loop control files. It is registered for Bash tools in `hooks/hooks.json:42-48`.
- algorithmic_behavior: Reads hook JSON from stdin, validates structure/depth, exits early for non-Bash tools, extracts `.tool_input.command`, lowercases it, locates active RLCR and PR loops, and allows all Bash commands when neither loop is active (`hooks/loop-bash-validator.sh:24-70`). For RLCR it resolves and strictly parses `state.md` or `finalize-state.md`, then applies gates for push policy, state-file mutation, plan backup writes, goal tracker writes, prompt/summary/todos writes (`hooks/loop-bash-validator.sh:77-395`). For PR loops it blocks `state.md` and generated/read-only round files (`hooks/loop-bash-validator.sh:403-442`).
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR`, active loop files under `.humanize/rlcr` and `.humanize/pr-loop`, and templates via `loop-common.sh`. Outputs are exit `0` allow, exit `1` malformed/missing hook input or malformed state, and exit `2` blocked operation with rendered guidance. It does not itself mutate loop state, but it permits the specific cancel transition command that renames `state.md` or `finalize-state.md` to `cancel-state.md` when authorized by `.cancel-requested`.
- gates_or_invariants: Fails closed on malformed RLCR state via `parse_state_file_strict` (`hooks/loop-bash-validator.sh:81-85`). Blocks `git push` when `push_every_round` is not true (`hooks/loop-bash-validator.sh:96-105`). Blocks `git add` commands that target `.humanize` through `git_adds_humanize` (`hooks/loop-bash-validator.sh:115-118`). State-file protection checks destination writes first, then explicitly checks `mv`/`cp` sources to prevent moving or copying `state.md` away (`hooks/loop-bash-validator.sh:139-156`, `158-304`). It splits chained commands on shell operators and strips leading redirections before source detection (`hooks/loop-bash-validator.sh:174-284`). Shell wrappers using `sh -c` or `bash -c` are separately inspected (`hooks/loop-bash-validator.sh:306-328`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for JSON validation, active loop discovery, state parsing, template loading, `command_modifies_file`, `git_adds_humanize`, and `is_cancel_authorized` (`hooks/loop-bash-validator.sh:16-18`). Key helper semantics are in `hooks/lib/loop-common.sh:91-140`, `195-306`, `401-464`, `694-852`, `1073-1169`, and `1214-1239`. Tests and callers include `tests/test-cancel-signal-file.sh`, `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-allowlist-validators.sh`, PR-loop hook tests, and robustness suites.
- edge_cases_or_failure_modes: Regex-based command parsing is broad but not a full shell parser. The command is lowercased, which is consistent with current patterns but can be surprising on case-sensitive paths. The `state\.md` pattern intentionally catches names such as `cancel-state.md`, so cancellation depends on the later `is_cancel_authorized` exact-command gate. The todos path gate requires the active loop path and currently matches only `round-[12]-todos.md` (`hooks/loop-bash-validator.sh:385-390`), which may be narrow if later rounds need todo file writes. Redirection stripping handles many forms, including `&>`, `&>>`, fd duplication, quoted targets, ANSI-C quoting, and escaped spaces, but future shell syntax variants may need tests.
- validation_or_tests: `tests/test-cancel-signal-file.sh` directly exercises the cancel exception and bypass defenses. Broader suites reference this validator from `tests/run-all-tests.sh:35-84`. I performed syntax checks only: `bash -n hooks/loop-bash-validator.sh scripts/lib/monitor-skill.sh tests/test-cancel-signal-file.sh` passed.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-060 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR cancellation safety gate. It defines the allowed cancellation command shape and many denied bypass forms for `loop-bash-validator.sh` and `is_cancel_authorized`.
- algorithmic_behavior: Creates isolated temporary RLCR loop directories with valid `state.md`, exports `CLAUDE_PROJECT_DIR`, builds Bash hook JSON using `jq`, invokes `hooks/loop-bash-validator.sh`, captures exit code/output, and records pass/fail counts (`tests/test-cancel-signal-file.sh:28-74`). It then runs positive and negative cases against the validator and directly against `is_cancel_authorized`.
- inputs_outputs_state: Inputs are temporary loop state, optional `.cancel-requested`, constructed command strings, and the sourced `hooks/lib/loop-common.sh` helper (`tests/test-cancel-signal-file.sh:32-33`). Outputs are terminal PASS/FAIL lines and final process exit equal to failure count (`tests/test-cancel-signal-file.sh:1349-1357`). The test does not perform the destructive commands; it validates whether the hook would allow or block them.
- gates_or_invariants: Positive gates allow only `mv <loop>/state.md <loop>/cancel-state.md` with signal present, including normalized `./` paths, quoted paths, and literal `${loop_dir}` syntax (`tests/test-cancel-signal-file.sh:80-115`, `365-486`). Negative gates require block without signal, signal in wrong directory, non-mv edits, `rm`, wrong destination, arbitrary destination, unrelated `/tmp/state.md`, extra arguments, hidden variables, command substitution, backticks, newlines, shell operators, shell wrappers, prefixes such as `sudo/env/command`, and many redirection-prefixed forms (`tests/test-cancel-signal-file.sh:121-357`, `493-1249`).
- dependencies_and_callers: Depends on `jq`, Bash, `hooks/loop-bash-validator.sh`, and `hooks/lib/loop-common.sh`. It is part of the full suite in `tests/run-all-tests.sh:47` and is adjacent to `tests/robustness/test-cancel-security-robustness.sh` for deeper cancellation security coverage.
- edge_cases_or_failure_modes: The test deliberately checks authorization through both full validator flow and helper-only paths where JSON/newline encoding would be awkward (`tests/test-cancel-signal-file.sh:342-353`). It uses `/tmp` for arbitrary destinations and temp roots, so full execution is environment-sensitive, but validation itself only inspects commands. It covers `state.md` cancellation extensively but leaves `finalize-state.md` cancellation mainly to helper implementation and other finalize tests.
- validation_or_tests: This file is itself the validation surface. Its key expected exit semantics are allow `0` for the exact authorized cancel command and block `2` for unauthorized state-file mutation. Syntax check with `bash -n tests/test-cancel-signal-file.sh` passed.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-090 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh compatibility and runtime safety spec for humanize monitor path discovery patterns. It guards against zsh `nomatch` failures in empty or dotfile-only loop/cache directories.
- algorithmic_behavior: Runs under zsh, creates an isolated `/tmp/test-zsh-humanize-$$` project, sets `XDG_CACHE_HOME`, sources `scripts/humanize.sh`, and runs targeted checks that mirror monitor discovery functions using `find` instead of shell globs (`tests/test-zsh-monitor-safety.sh:1-60`, `68-124`). It finishes with a pass/fail summary and exits nonzero on failures (`tests/test-zsh-monitor-safety.sh:323-341`).
- inputs_outputs_state: Inputs are zsh runtime, temp `.humanize/rlcr` directories, temp cache dirs, and `scripts/humanize.sh`. Outputs are PASS/FAIL lines. State is temporary filesystem state only: empty loop dirs, dotfiles, a session with no state files, empty cache, and valid timestamped session dirs.
- gates_or_invariants: Verifies sourcing `humanize.sh` does not produce `no matches found` (`tests/test-zsh-monitor-safety.sh:65-77`). Verifies latest-session discovery handles empty and dotfile-only dirs (`tests/test-zsh-monitor-safety.sh:80-157`). Verifies state-file discovery handles no `*-state.md` files (`tests/test-zsh-monitor-safety.sh:160-205`). Verifies empty cache log scanning returns zero safely (`tests/test-zsh-monitor-safety.sh:208-235`). Verifies timestamp session ordering and confirms the test is actually running in zsh (`tests/test-zsh-monitor-safety.sh:240-286`). It demonstrates the old glob pattern risk and the safer `find` pattern (`tests/test-zsh-monitor-safety.sh:288-321`).
- dependencies_and_callers: Depends on `/usr/bin/env zsh`, `scripts/humanize.sh`, and monitor utility behavior implemented in `scripts/lib/monitor-common.sh`, especially `monitor_find_latest_session` and `monitor_find_state_file` using `find` (`scripts/lib/monitor-common.sh:40-63`, `154-188`). `tests/run-all-tests.sh` marks this as a zsh-required suite (`tests/run-all-tests.sh:86-89`).
- edge_cases_or_failure_modes: The test uses local mirror functions for some monitor internals rather than invoking every real interactive monitor path, so it is strongest as a regression check for discovery idioms, not a full terminal UI test. It writes under `/tmp` and assumes zsh is installed. It specifically covers empty/dotfile-only dirs, absent state files, empty cache, valid timestamp sorting, and old glob failure behavior.
- validation_or_tests: This file is a zsh-only test surface. I confirmed zsh exists at `/bin/zsh` and ran `zsh -n tests/test-zsh-monitor-safety.sh` successfully.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-120 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Small block template for the directory-path validation contract in RLCR read/write validators. It tells the agent that the requested file path is wrong and provides the exact active-loop path to use.
- algorithmic_behavior: Renders a Markdown block with `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`, plus a short fallback instruction to read the originally requested file with `cat` (`prompt-template/block/wrong-directory-path.md:1-6`).
- inputs_outputs_state: Inputs are template variables passed by callers. Outputs are rendered text to stderr through `load_and_render_safe`. It has no persistent state and no direct filesystem behavior.
- gates_or_invariants: The invariant belongs to callers: file operations for loop-owned round files must target `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`, not another same-named path. Read validator renders this template when `FILE_PATH != CORRECT_PATH` (`hooks/loop-read-validator.sh:153-167`). Write validator does the same for write paths (`hooks/loop-write-validator.sh:250-264`).
- dependencies_and_callers: Loaded by template-loader infrastructure through `load_and_render_safe` from `loop-common.sh`/template support. Direct call sites are `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`; `rg` found no Bash validator call site for this template.
- edge_cases_or_failure_modes: The template itself performs no escaping or validation, so correctness depends on caller-provided values. The final line says to use `cat {{FILE_PATH}}`, which is natural for read failures but potentially confusing when the action is `write to`; callers still pass action-specific wording.
- validation_or_tests: Covered indirectly by template reference/comprehensive tests and hook path-validation tests. `tests/test-template-references.sh` includes validator/template reference checks, and `tests/test-plan-file-hooks.sh` exercises hook rendering paths.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-150 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Runtime monitor implementation for `humanize monitor skill`, focused on ask-codex skill invocations stored under `.humanize/skill` with optional cache output under `~/.cache/humanize`.
- algorithmic_behavior: Defines `_humanize_monitor_skill`, enables zsh-local `ksharrays` and `no_monitor`, parses `--once`, requires `.humanize/skill`, lists timestamped invocation dirs newest-first, computes status counts, extracts question/metadata, resolves cache directories, selects the best output/log file, and either prints a one-shot report or launches an interactive fixed-header tailing monitor (`scripts/lib/monitor-skill.sh:15-40`, `42-167`, `169-346`, `348-447`).
- inputs_outputs_state: Inputs are `.humanize/skill/<timestamp>/input.md`, `metadata.md`, `output.md`, local `cache/`, global cache under `${XDG_CACHE_HOME:-$HOME/.cache}`, terminal dimensions via `tput`, and optional `--once`. Outputs are formatted monitor text in once mode, or alternate-screen status bar plus live `tail -f` in interactive mode. Runtime state is local variables: focused invocation, watched file, `TAIL_PID`, `monitor_running`, and `cleanup_done`.
- gates_or_invariants: Only timestamp-like directories are counted (`scripts/lib/monitor-skill.sh:42-53`, `84-104`). Running invocations are inferred by absence of `metadata.md` and prefer cache logs over output files (`scripts/lib/monitor-skill.sh:130-167`). Completed invocations prefer non-empty `output.md`, then cache outputs/logs (`scripts/lib/monitor-skill.sh:159-165`). Once mode fails when no invocation exists and prints recent invocations capped at ten (`scripts/lib/monitor-skill.sh:268-345`). Interactive mode cleans up tails and terminal state on exit/signals, using zsh-specific `TRAPINT`/`TRAPTERM` when needed (`scripts/lib/monitor-skill.sh:348-390`, `392-447`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at startup (`scripts/humanize.sh:1587-1590`) and invoked by `humanize monitor skill` (`scripts/humanize.sh:1108-1121`). Depends on `monitor-common.sh` for `monitor_get_yaml_value` and `monitor_format_timestamp`, and on `humanize_split_to_array` from `humanize.sh` (`scripts/humanize.sh:22-34`). Tested by `tests/test-skill-monitor.sh`; zsh runtime assumptions are also related to `tests/test-zsh-monitor-safety.sh`.
- edge_cases_or_failure_modes: Returns `1` if `.humanize/skill` is missing (`scripts/lib/monitor-skill.sh:35-40`). If no watchable content exists, it falls back to newest invocation and may show `none` or waiting messages (`scripts/lib/monitor-skill.sh:74-79`, `316-322`, `433-436`). Narrow terminals could make truncation widths small because it subtracts fixed label widths from `tput cols` (`scripts/lib/monitor-skill.sh:213-235`). Interactive behavior depends on terminal support for `tput smcup`, `csr`, and `tail -f`; cleanup tries to disown/kill/wait to avoid zsh job messages (`scripts/lib/monitor-skill.sh:360-381`).
- validation_or_tests: `tests/test-skill-monitor.sh` exercises once-mode scenarios, missing/empty dirs, cache selection, statuses, and non-matching dirs. Syntax check with `bash -n scripts/lib/monitor-skill.sh` passed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 section headers present in scheduler order; item IDs are not repeated here to preserve the exact-once evidence constraint.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`