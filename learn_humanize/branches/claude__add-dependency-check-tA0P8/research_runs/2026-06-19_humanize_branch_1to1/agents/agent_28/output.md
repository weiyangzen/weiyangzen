# agent_28 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-028 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook task completion gate. It prevents RLCR exit when Claude still has incomplete legacy `TodoWrite` todos or file-backed Claude task-system tasks, using transcript JSONL plus `~/.claude/tasks/<session_id>/` as state inputs.
- algorithmic_behavior: `extract_tool_calls_from_entry` supports assistant entries, message entries, and direct `tool_use` entries (`hooks/check-todos-from-transcript.py:23`). `find_incomplete_todos_from_transcript` streams JSONL, ignores blank/malformed lines, keeps the most recent non-empty `TodoWrite.todos`, and flags every todo whose status is not `completed` (`hooks/check-todos-from-transcript.py:58`). `find_incomplete_tasks_from_directory` reads `*.json` task files, defaults missing status to `pending`, and treats anything except `completed` or `deleted` as incomplete (`hooks/check-todos-from-transcript.py:104`). `main` combines task-directory results first, then transcript results, prints `INCOMPLETE_TODOS`, and exits `1` when any incomplete item remains (`hooks/check-todos-from-transcript.py:150`).
- inputs_outputs_state: Input is hook JSON on stdin with optional `session_id`, `tasks_base_dir`, and `transcript_path` (`hooks/check-todos-from-transcript.py:153`). Outputs are exit `0` for no known incomplete work, exit `1` with a stdout task list for incomplete work, and exit `2` with `PARSE_ERROR` on invalid hook JSON (`hooks/check-todos-from-transcript.py:158`). It reads external task/transcript state but does not mutate files.
- gates_or_invariants: Missing stdin, missing transcript, or missing task dir is permissive exit `0`; invalid hook JSON is a hard parse gate. Legacy todos require exact `status == "completed"`; task files additionally allow `deleted`. In `hooks/loop-codex-stop-hook.sh:364`, exit `1` blocks immediately before expensive Codex review, while exit `2` blocks with a parse-error reason.
- dependencies_and_callers: Uses Python stdlib only: `json`, `sys`, `pathlib`. Called by `hooks/loop-codex-stop-hook.sh:364`, which renders incomplete work through `block/incomplete-todos.md` on failure (`hooks/loop-codex-stop-hook.sh:390`). Covered by `tests/test-todo-checker.sh`.
- edge_cases_or_failure_modes: Malformed transcript lines and malformed task JSON files are skipped, which avoids hook crashes but can hide bad state (`hooks/check-todos-from-transcript.py:76`, `hooks/check-todos-from-transcript.py:143`). Empty `TodoWrite.todos` does not clear a previous non-empty todo list because assignment only occurs when `todos` is truthy (`hooks/check-todos-from-transcript.py:85`). Task file order from `glob("*.json")` is filesystem-dependent. Legacy and task-system items are appended without deduplication.
- validation_or_tests: `tests/test-todo-checker.sh` validates parse handling, empty/missing transcript behavior, incomplete and in-progress todos, latest-`TodoWrite` semantics, direct and alternate transcript formats, missing status, empty content, task directory statuses, deleted-task ignore behavior, mixed legacy/task results, and task-id output (`tests/test-todo-checker.sh:52`, `tests/test-todo-checker.sh:197`, `tests/test-todo-checker.sh:309`, `tests/test-todo-checker.sh:417`). Tests were inspected, not executed, per read-only research scope.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-058 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:ask-codex` one-shot Codex consultation path. It verifies argument parsing, safe model/effort handling, Codex process result classification, output persistence, cache logging, and concurrent directory uniqueness.
- algorithmic_behavior: The test builds a mock git project and prepends a mock `codex` binary controlled by `MOCK_CODEX_EXIT_CODE`, `MOCK_CODEX_STDOUT`, and `MOCK_CODEX_STDERR` (`tests/test-ask-codex.sh:28`, `tests/test-ask-codex.sh:38`). `run_ask_codex` runs `scripts/ask-codex.sh` inside that project with isolated `XDG_CACHE_HOME` (`tests/test-ask-codex.sh:66`). It then exercises validation failures, success persistence, error/timeout/empty response handling, concurrent calls, argument parsing, and cache artifacts.
- inputs_outputs_state: Inputs are command-line options, question text, mock Codex environment variables, `PATH`, `CLAUDE_PROJECT_DIR`, and `XDG_CACHE_HOME`. Expected outputs include stdout Codex response, stderr cache/status lines, `.humanize/skill/<unique-id>/input.md`, `output.md`, `metadata.md`, and cache files `codex-run.cmd`/`codex-run.out` (`tests/test-ask-codex.sh:173`, `tests/test-ask-codex.sh:381`). State transitions are encoded as metadata `status: success`, `error`, `empty_response`, and `timeout`.
- gates_or_invariants: Empty question, unknown flags, missing option arguments, non-numeric timeout, and unsafe model/effort characters must exit `1` (`tests/test-ask-codex.sh:83`). Successful Codex output must be emitted to stdout and saved locally. Non-zero Codex exit codes propagate exactly; timeout exit `124` is classified specially; empty stdout is failure even with Codex exit `0` (`tests/test-ask-codex.sh:219`). Two concurrent calls must create distinct project and cache directories (`tests/test-ask-codex.sh:285`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` for temp dir, pass/fail, and mock git setup (`tests/test-ask-codex.sh:15`). Tests `scripts/ask-codex.sh`, which sources `portable-timeout.sh` and `hooks/lib/loop-common.sh`, validates `codex`, creates unique storage, runs `codex exec`, and writes metadata (`scripts/ask-codex.sh:153`, `scripts/ask-codex.sh:202`, `scripts/ask-codex.sh:296`). Included by `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The implementation accepts timeout `0` because it only checks `^[0-9]+$`, despite the error text saying “positive integer” (`scripts/ask-codex.sh:125`). The test depends on `git init` and writable temp/cache locations, so it is not suitable for the current read-only branch export. Latest-directory checks use sorted paths, which is adequate for the timestamp/pid/random naming scheme but assumes no unrelated concurrent writers in the mock project.
- validation_or_tests: This file is itself the validation surface. It asserts default effort `xhigh` when no effort is supplied, `--` separator handling, timeout recording, cache debug path extraction, and `codex-run.cmd` prompt recording (`tests/test-ask-codex.sh:329`, `tests/test-ask-codex.sh:381`). Tests were inspected, not executed.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-088 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Comprehensive executable spec for prompt-template loading and rendering. It validates the template tree that stop hooks, PR-loop setup, Codex prompts, and block messages depend on for algorithmic gate instructions.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh`, resolves `prompt-template`, validates required subdirectories, recursively loads every `*.md`, scans placeholder syntax, tests malformed-placeholder detection, exercises rendering edge cases, verifies safe fallback behavior, renders selected real templates, and stress-renders all templates with dummy values (`tests/test-templates-comprehensive.sh:17`, `tests/test-templates-comprehensive.sh:59`, `tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:118`, `tests/test-templates-comprehensive.sh:562`).
- inputs_outputs_state: Inputs are the `prompt-template/` tree and the loader functions `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Outputs are pass/fail/warn counts and process exit `0` only when `TESTS_FAILED == 0` (`tests/test-templates-comprehensive.sh:619`). It creates temporary directories for malformed/fallback checks but no persistent repo state.
- gates_or_invariants: Templates must load as non-whitespace content, placeholder syntax must be uppercase `{{VAR_NAME}}` without extra braces/spaces/lowercase, missing templates must use rendered fallback content, existing templates must override fallback content, and all templates must be renderable without unreplaced placeholders when dummy values are supplied (`tests/test-templates-comprehensive.sh:122`, `tests/test-templates-comprehensive.sh:445`, `tests/test-templates-comprehensive.sh:575`). `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:216`).
- dependencies_and_callers: Depends on shell utilities `find`, `grep -E`, `sort -z`, `mktemp`, and the awk-based renderer in `hooks/lib/template-loader.sh:58`. It directly validates assigned template `prompt-template/block/unpushed-commits.md` with real variables (`tests/test-templates-comprehensive.sh:517`). Included by `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The script uses `set -uo pipefail`, not `set -e`, so it accumulates failures instead of aborting early (`tests/test-templates-comprehensive.sh:13`). “CJK” and “emoji-like” coverage is mostly ASCII-safe placeholder text, not strong Unicode validation (`tests/test-templates-comprehensive.sh:348`, `tests/test-templates-comprehensive.sh:362`). Multiline/backslash/quote tests mostly assert “no crash” rather than exact byte preservation. The syntax regexes can produce false positives/negatives for unusual markdown examples, but they cover the project’s intended placeholder dialect.
- validation_or_tests: Section 6 proves real rendering for `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:497`). Section 7 recursively loads and renders all 56 markdown templates found under `prompt-template/` in this branch export (`tests/test-templates-comprehensive.sh:562`). Tests were inspected, not executed.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-118 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-hook block message template for the “unpushed commits” gate. It converts branch-ahead state into a concrete instruction to push before the loop may exit.
- algorithmic_behavior: The template renders a heading, count, branch name, explanation tied to `--push-every-round`, and required command `git push origin {{CURRENT_BRANCH}}` (`prompt-template/block/unpushed-commits.md:1`). It is rendered through `load_and_render_safe` when hooks compute an ahead count.
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH` placeholders (`prompt-template/block/unpushed-commits.md:3`). Output is markdown used as the JSON `reason` in hook block decisions; the template itself does not mutate state. The state transition is stop attempt -> blocked until local commits are pushed.
- gates_or_invariants: In `hooks/loop-codex-stop-hook.sh`, this gate only runs when `PUSH_EVERY_ROUND == "true"` and `git status -sb` reports `ahead N`; it blocks with system message “Loop: Blocked” (`hooks/loop-codex-stop-hook.sh:617`). In `hooks/pr-loop-stop-hook.sh`, PR-loop ahead detection also blocks when `AHEAD_COUNT > 0` (`hooks/pr-loop-stop-hook.sh:350`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh:629` and `hooks/pr-loop-stop-hook.sh:356`, both through `hooks/lib/template-loader.sh`. Validated directly by `tests/test-templates-comprehensive.sh:517`.
- edge_cases_or_failure_modes: The rendered command hard-codes `origin`, which can be wrong if the branch tracks another remote. Branch names are inserted unquoted into a shell snippet; unusual branch names may need quoting. Detached HEAD or failed branch detection can render `unknown` from caller fallback. Loop detection via `git status -sb | grep 'ahead [0-9]*'` is sensitive to status format/localization in the RLCR hook.
- validation_or_tests: Comprehensive template tests render it with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then assert the title, count, and branch appear (`tests/test-templates-comprehensive.sh:518`). The hook call sites provide fallback messages if the template is missing or empty.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-148 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 task contract for a fresh PR with no fetched review comments. It tells the agent to write an initial summary and exit so the stop hook can poll automatic bot reviews, rather than commenting to trigger review.
- algorithmic_behavior: The template uses `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH` to instruct the agent that monitored bots review automatically, the summary should be written to `@{{RESOLVE_PATH}}`, there are no issues yet, state files must not be modified, and the stop hook owns polling and Codex validation (`prompt-template/pr-loop/round-0-task-no-comments.md:4`, `prompt-template/pr-loop/round-0-task-no-comments.md:18`, `prompt-template/pr-loop/round-0-task-no-comments.md:26`).
- inputs_outputs_state: Inputs are template variables prepared in `scripts/setup-pr-loop.sh`, especially `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH` (`scripts/setup-pr-loop.sh:731`). Output is appended into `.humanize/pr-loop/round-0-prompt.md` after the round header and fetched comment file (`scripts/setup-pr-loop.sh:757`, `scripts/setup-pr-loop.sh:763`). The intended state transition is no-comments Round 0 -> agent writes summary -> exit attempt -> stop hook polls reviews -> either feedback loop continues or approvals end the loop.
- gates_or_invariants: The key invariant is “Do not comment to trigger review”; first reviews are automatic (`prompt-template/pr-loop/round-0-task-no-comments.md:20`). The prompt also forbids direct edits to `.humanize/pr-loop/` state files and delegates polling/validation to the stop hook (`prompt-template/pr-loop/round-0-task-no-comments.md:21`).
- dependencies_and_callers: Selected by `scripts/setup-pr-loop.sh` only when the fetched comment file contains the exact sentinel `*No comments found.*` (`scripts/setup-pr-loop.sh:723`). Rendered via `load_and_render_safe` with an inline fallback that mirrors the template (`scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:800`). It depends on the PR comments fetcher preserving that sentinel and on the stop hook’s later polling behavior.
- edge_cases_or_failure_modes: If the “no comments” sentinel changes or is absent, setup chooses the normal “has comments” task path instead. If bots fail to auto-review, the template’s note says the stop hook polls every 30 seconds up to 15 minutes per bot, so timeout/escalation is external to this prompt. `PR_NUMBER`, `START_BRANCH`, and `BOT_MENTION_STRING` are supplied in the variable array but unused here, which is harmless under single-pass rendering.
- validation_or_tests: It is covered generically by `tests/test-templates-comprehensive.sh`, which loads every markdown template, validates placeholder syntax, and stress-renders all templates with dummy values (`tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:118`, `tests/test-templates-comprehensive.sh:562`). There is no dedicated semantic assertion for this specific PR-loop no-comments behavior in the inspected test file.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present; each assigned item_id appears once as a section heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`