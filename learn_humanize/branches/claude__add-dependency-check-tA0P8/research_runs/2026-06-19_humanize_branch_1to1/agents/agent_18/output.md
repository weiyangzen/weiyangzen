# agent_18 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-018 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: Defines the user-facing Humanize skill contract. The directory recursively contains one file, `skills/humanize/SKILL.md`, so the directory’s algorithmic responsibility is the workflow specification for RLCR, PR loop, plan generation, monitoring, state layout, and required operator commands.
- algorithmic_behavior: `SKILL.md` describes two main loops: RLCR implementation/review with a transition from implementation review to code-review phase when Codex outputs `COMPLETE`, and PR loop automation that detects the branch PR, fetches bot comments, asks the agent to fix, triggers bot re-review, polls, validates with local Codex, and repeats until bot approval or max iterations (`skills/humanize/SKILL.md:30`, `skills/humanize/SKILL.md:48`). It also defines goal-tracker discipline: immutable ultimate goal and acceptance criteria, mutable active/completed/deferred state, periodic full-alignment checks (`skills/humanize/SKILL.md:186`).
- inputs_outputs_state: Inputs are plan files, PR bot selections, CLI flags, GitHub PR comments/reactions, and Codex review output. Outputs are command invocations, `.humanize/rlcr/<timestamp>/...` state files, `.humanize/pr-loop/<timestamp>/state.md`, `resolution-N.md`, summaries, review results, and monitor-readable state (`skills/humanize/SKILL.md:214`). The runtime root placeholder is hydrated by installation and used in all command examples (`skills/humanize/SKILL.md:10`).
- gates_or_invariants: Only one active loop should run; RLCR must use `rlcr-stop-gate.sh` for hook-equivalent transitions (`skills/humanize/SKILL.md:46`, `skills/humanize/SKILL.md:206`). Summaries and goal tracker updates are mandatory; users are told not to edit state files or bypass cancellation (`skills/humanize/SKILL.md:200`). PR loop requires GitHub CLI and Codex (`skills/humanize/SKILL.md:209`).
- dependencies_and_callers: Coordinates with `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh` (`skills/humanize/SKILL.md:71`, `skills/humanize/SKILL.md:107`, `skills/humanize/SKILL.md:131`, `skills/humanize/SKILL.md:139`, `skills/humanize/SKILL.md:240`). Sibling prompt templates and hooks implement the actual gates.
- edge_cases_or_failure_modes: Max iterations, cancellation, unresolved review issues, stagnation, deferred acceptance criteria, missing summaries, state-file tampering, missing `codex` or `gh`, and PR loops waiting for bot review are all explicit failure or continuation states.
- validation_or_tests: The assigned tests below exercise this skill surface: PR setup parser tests, robustness tests for setup scripts, dependency checks, mutual exclusion, YAML safety, timeouts, and prompt template behavior.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-048 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for the PR review loop. It creates the `.humanize/pr-loop/<timestamp>/` session state, fetches initial PR comments, determines startup case, optionally posts or reuses trigger comments, verifies Claude bot reaction when needed, then emits the initial work prompt.
- algorithmic_behavior: Parses `--claude`, `--codex`, `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout` (`scripts/setup-pr-loop.sh:111`). It rejects missing bot selection, unknown options, nonnumeric max/timeout, and YAML-unsafe branch/model/effort strings (`scripts/setup-pr-loop.sh:180`, `scripts/setup-pr-loop.sh:384`). It blocks if an RLCR or PR loop is already active (`scripts/setup-pr-loop.sh:220`). It detects the active branch, resolves PR repo context for fork PRs, tries `gh pr view` auto-detection first, falls back to parent repo with `owner:branch`, rejects missing/invalid/closed/merged PRs, and sets `PR_BASE_REPO` for API calls (`scripts/setup-pr-loop.sh:283`, `scripts/setup-pr-loop.sh:295`, `scripts/setup-pr-loop.sh:334`, `scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:378`).
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR`, git branch/HEAD, `gh` API responses, reviewer status JSON, fetched comment markdown, templates under `prompt-template/pr-loop`, and bot reaction checks. Outputs are `$LOOP_DIR/state.md`, `$LOOP_DIR/goal-tracker.md`, `$LOOP_DIR/round-0-pr-comment.md`, `$LOOP_DIR/round-0-pr-resolve.md`, `$LOOP_DIR/round-0-prompt.md`, and terminal prompt text (`scripts/setup-pr-loop.sh:413`, `scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:721`, `scripts/setup-pr-loop.sh:860`).
- gates_or_invariants: Requires a git repo with at least one commit, installed/authenticated `gh`, and installed `codex` (`scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`). It uses `run_with_timeout` for git and many `gh` calls (`scripts/setup-pr-loop.sh:33`). For startup cases 4 or 5, it requires a trigger comment after latest commit; it searches existing comments using all active bot mention regexes outside code/quotes, posts if absent, and removes the loop dir on trigger-post failure (`scripts/setup-pr-loop.sh:462`, `scripts/setup-pr-loop.sh:470`, `scripts/setup-pr-loop.sh:479`, `scripts/setup-pr-loop.sh:528`). Claude mode makes eyes reaction verification blocking (`scripts/setup-pr-loop.sh:564`).
- dependencies_and_callers: Called by `commands/start-pr-loop.md`; sources `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:34`, `scripts/setup-pr-loop.sh:38`). Calls `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`, `git`, `gh`, `jq`, and `codex` (`scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:587`). Uses `build_bot_mention_string`, `build_yaml_list`, `find_active_loop`, and `find_active_pr_loop` from `loop-common.sh`; `build_bot_mention_string` joins selected bots as `@claude @codex` (`hooks/lib/loop-common.sh:907`) and active PR loop detection checks newest state dir (`hooks/lib/loop-common.sh:938`).
- edge_cases_or_failure_modes: Fork PR lookup, branch renamed relative to PR head, invalid PR number from CLI, closed/merged PR, YAML injection characters, missing comment ID after trigger posting, local clock skew avoided by not falling back to local trigger time, duplicate trigger prevention, partial bot mention requiring a new trigger, startup status failure defaulting to case 1, SIGPIPE during final output trapped to clean exit (`scripts/setup-pr-loop.sh:312`, `scripts/setup-pr-loop.sh:337`, `scripts/setup-pr-loop.sh:439`, `scripts/setup-pr-loop.sh:558`, `scripts/setup-pr-loop.sh:857`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help, missing/invalid bot flag, both bot flags, `--max`, `--codex-model`, and `--codex-timeout` parsing (`tests/test-pr-loop-scripts.sh:18`). `tests/robustness/test-setup-scripts-robustness.sh` adds PR setup help, missing bot flag, unknown option, invalid max, non-git rejection, valid arg flow to `gh auth`, and PR timeout validation (`tests/robustness/test-setup-scripts-robustness.sh:473`, `tests/robustness/test-setup-scripts-robustness.sh:714`, `tests/robustness/test-setup-scripts-robustness.sh:767`). Notable dependency-check finding: this PR setup script checks `codex` directly, but uses `jq` without an explicit `command -v jq` preflight; the branch’s explicit missing-`jq` tests in the assigned robustness file target RLCR setup, not this PR setup (`scripts/setup-pr-loop.sh:272`, `scripts/setup-pr-loop.sh:444`, `tests/robustness/test-setup-scripts-robustness.sh:1113`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-078 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Lightweight executable specification for PR loop script argument parsing and basic loop-state behavior. It defines `run_script_tests`, which runs setup, cancel, fetch, and poll script checks.
- algorithmic_behavior: `run_setup_tests` invokes `setup-pr-loop.sh` under invalid and minimally valid arguments, asserting parser behavior before real GitHub/Git prerequisites dominate (`tests/test-pr-loop-scripts.sh:18`). `run_cancel_tests` creates a mock `.humanize/pr-loop/<timestamp>/state.md`, runs cancellation, and expects `cancel-state.md` replacement (`tests/test-pr-loop-scripts.sh:172`). `run_fetch_tests` and `run_poll_tests` specify required argument validation for helper scripts (`tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`).
- inputs_outputs_state: Inputs are `PROJECT_ROOT`, `TEST_DIR`, mocked `.humanize/pr-loop` dirs, and actual script paths. Outputs are pass/fail calls from `test-helpers.sh`; no durable production state is intended. The cancel test mutates only the test loop directory by renaming `state.md` to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:222`, `tests/test-pr-loop-scripts.sh:236`).
- gates_or_invariants: Missing bot flag must fail; unknown setup option must fail; `--claude`, `--codex`, and both together must not be rejected as missing bot flags; numeric options must parse, and invalid `--max` must fail (`tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:52`, `tests/test-pr-loop-scripts.sh:66`, `tests/test-pr-loop-scripts.sh:92`, `tests/test-pr-loop-scripts.sh:117`). Fetch requires numeric PR and output path; poll requires PR, `--after`, and `--bots` (`tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`, `tests/test-pr-loop-scripts.sh:352`).
- dependencies_and_callers: Depends on `PROJECT_ROOT`, `TEST_DIR`, `pass`, and `fail` helpers supplied by the test harness. Calls `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`.
- edge_cases_or_failure_modes: The setup tests intentionally allow later failures such as missing git repo or unavailable dependencies, as long as the tested parser gate was passed. This makes the tests shallow for runtime behavior but useful as stable parser contracts (`tests/test-pr-loop-scripts.sh:68`).
- validation_or_tests: This file is itself validation. It is likely sourced by a broader runner, because its header says `source test-pr-loop-scripts.sh && run_script_tests` (`tests/test-pr-loop-scripts.sh:11`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-108 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when the PR loop requires an explicit trigger comment before polling or continuing.
- algorithmic_behavior: Renders a stop reason telling the agent that a trigger comment is missing, includes startup case and current round variables, and instructs the user/agent to post a PR comment mentioning the configured bot mention string (`prompt-template/block/no-trigger-comment.md:1`).
- inputs_outputs_state: Inputs are `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING`. Output is markdown embedded into hook JSON as a block reason. It does not update state itself.
- gates_or_invariants: The template encodes the invariant that a round requiring trigger cannot proceed without a PR comment mentioning all active bots (`prompt-template/block/no-trigger-comment.md:9`). The PR loop stop hook enforces this when `REQUIRE_TRIGGER=true` and `PR_LAST_TRIGGER_AT` is empty, then emits a `decision: block` JSON object (`hooks/pr-loop-stop-hook.sh:728`, `hooks/pr-loop-stop-hook.sh:740`, `hooks/pr-loop-stop-hook.sh:744`).
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/pr-loop-stop-hook.sh`; fallback text exists if template load fails (`hooks/pr-loop-stop-hook.sh:737`). It relies on template-loader single-pass replacement semantics.
- edge_cases_or_failure_modes: The rendered message can contain unresolved placeholders if caller omits variables. The underlying hook distinguishes startup cases 4 and 5 from later rounds with descriptive text (`hooks/pr-loop-stop-hook.sh:731`).
- validation_or_tests: Template reference tests cover safe template loading generally; PR stop-hook tests and system tests exercise trigger-related blocks indirectly. No direct assigned test file targets this exact markdown file, but `rg` shows the stop hook as its concrete caller.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-138 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory RLCR full-alignment Codex review prompt. It defines the review contract for periodic comprehensive goal checks, including acceptance criteria audit, drift detection, deferral audit, implementation review, stagnation circuit breaker, and completion criteria.
- algorithmic_behavior: Requires Codex to read the original plan first, inspect Claude’s summary, read the goal tracker, verify every immutable acceptance criterion, find forgotten tasks, audit deferred items, summarize remaining work, review implementation against docs, inspect historical rounds for stagnation, and write `STOP` or `COMPLETE` only under strict conditions (`prompt-template/codex/full-alignment-review.md:5`, `prompt-template/codex/full-alignment-review.md:19`, `prompt-template/codex/full-alignment-review.md:50`, `prompt-template/codex/full-alignment-review.md:59`, `prompt-template/codex/full-alignment-review.md:86`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, previous-round variables, and `REVIEW_RESULT_FILE`. Output is the Codex review result file; last-line markers drive loop transitions.
- gates_or_invariants: Completion is allowed only when all original plan tasks are done, all acceptance criteria are fully met, and there are no deferrals (`prompt-template/codex/full-alignment-review.md:91`). Deferrals count as incomplete. Stagnation requires `STOP` as the last line (`prompt-template/codex/full-alignment-review.md:84`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` renders this template when `FULL_ALIGNMENT_CHECK=true` and writes the prompt to `$REVIEW_PROMPT_FILE` (`hooks/loop-codex-stop-hook.sh:857`). It uses `load_and_render_safe`, with a shorter fallback if the template is unavailable (`hooks/loop-codex-stop-hook.sh:835`).
- edge_cases_or_failure_modes: If historical files are missing, the reviewer may have less evidence for stagnation; if variables are missing, placeholders remain. The template explicitly guards against false completion from summaries alone by requiring plan and goal tracker comparison (`prompt-template/codex/full-alignment-review.md:30`).
- validation_or_tests: Template-loader tests validate safe rendering/fallback behavior. The hook’s branch between full and regular review is visible at `hooks/loop-codex-stop-hook.sh:857`; robustness tests cover the `--full-review-round` parameter that controls periodic full reviews (`tests/robustness/test-setup-scripts-robustness.sh:876`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-168 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for setup scripts, covering parser edge cases, plan validation, git repository preconditions, YAML safety, mutual exclusion, symlink protection, timeout behavior, skip-implementation mode, and dependency checks.
- algorithmic_behavior: Builds isolated test repos with `init_basic_git_repo`, writes minimal plans, wraps `setup-rlcr-loop.sh` and `setup-pr-loop.sh` with `CLAUDE_PROJECT_DIR` isolation, then runs numbered scenario tests (`tests/robustness/test-setup-scripts-robustness.sh:31`, `tests/robustness/test-setup-scripts-robustness.sh:51`, `tests/robustness/test-setup-scripts-robustness.sh:67`, `tests/robustness/test-setup-scripts-robustness.sh:80`). It asserts expected stderr substrings and exit codes, using mock binaries to push execution into desired gates.
- inputs_outputs_state: Inputs are temporary repos under `TEST_DIR`, generated plans, mocked `codex`, `gh`, `jq`, `timeout/gtimeout`, symlinks, git branches, and PATH overrides. Outputs are pass/fail/skip test results and transient `.humanize` loop directories in test repos. It ends with `print_test_summary` and exits with the aggregate status (`tests/robustness/test-setup-scripts-robustness.sh:1215`).
- gates_or_invariants: RLCR setup must reject missing/invalid plan arguments, invalid numeric options, comment-only or too-short plans, spaces/metacharacters/absolute plan paths, YAML-unsafe branch/model strings, non-git repos, repos without commits, tracked plan files without explicit opt-in, active loop conflicts, and plan symlinks (`tests/robustness/test-setup-scripts-robustness.sh:105`, `tests/robustness/test-setup-scripts-robustness.sh:224`, `tests/robustness/test-setup-scripts-robustness.sh:351`, `tests/robustness/test-setup-scripts-robustness.sh:412`, `tests/robustness/test-setup-scripts-robustness.sh:535`, `tests/robustness/test-setup-scripts-robustness.sh:605`). PR setup must reject missing bot flag, unknown option, invalid max, and non-git dirs; valid PR args should proceed to `gh auth` rather than parser failure (`tests/robustness/test-setup-scripts-robustness.sh:473`, `tests/robustness/test-setup-scripts-robustness.sh:714`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, `git`, shell utilities, and the setup scripts under test (`tests/robustness/test-setup-scripts-robustness.sh:15`). Dependency-specific tests create restricted PATHs and mocks for `codex` and `jq` (`tests/robustness/test-setup-scripts-robustness.sh:1085`).
- edge_cases_or_failure_modes: Some assertions are environment-sensitive and explicitly skip or accept alternate outcomes when tools are installed globally or symlink creation varies (`tests/robustness/test-setup-scripts-robustness.sh:375`, `tests/robustness/test-setup-scripts-robustness.sh:620`, `tests/robustness/test-setup-scripts-robustness.sh:1106`, `tests/robustness/test-setup-scripts-robustness.sh:1135`). Timeout zero is documented as accepted despite wording saying positive integer, because regex permits nonnegative digits (`tests/robustness/test-setup-scripts-robustness.sh:749`). Very large timeout is accepted (`tests/robustness/test-setup-scripts-robustness.sh:781`).
- validation_or_tests: This file is validation. It specifically contains the branch-relevant dependency-check contract: missing `codex`, missing `jq`, multiple missing dependencies listed together, and all dependencies present passing the dependency check (`tests/robustness/test-setup-scripts-robustness.sh:1085`, `tests/robustness/test-setup-scripts-robustness.sh:1113`, `tests/robustness/test-setup-scripts-robustness.sh:1142`, `tests/robustness/test-setup-scripts-robustness.sh:1184`). These dependency-check cases apply to RLCR setup in this file, while PR-loop setup coverage remains mostly parser/prerequisite oriented.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-018, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-048, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-078, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-108, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-138, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-168
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`