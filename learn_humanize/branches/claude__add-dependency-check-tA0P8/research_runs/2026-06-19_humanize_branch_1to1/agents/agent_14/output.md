# agent_14 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-014 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation template directory. It contains one recursive descendant, `prompt-template/plan/gen-plan-template.md`, which defines the required structure for generated implementation plans.
- algorithmic_behavior: The template requires a title, `Goal Description`, TDD-style `Acceptance Criteria`, affirmative `Path Boundaries`, feasibility hints, dependency sequencing, and implementation notes. The AC section explicitly requires positive and negative tests for deterministic verification at `prompt-template/plan/gen-plan-template.md:6`. Path bounds define upper/lower acceptable scope and allowed/prohibited choices at `prompt-template/plan/gen-plan-template.md:25`. It also mandates that implementation code/comments avoid plan-specific progress terms such as `AC-`, `Milestone`, `Step`, and `Phase` at `prompt-template/plan/gen-plan-template.md:69`.
- inputs_outputs_state: Input is a draft document passed through the gen-plan command flow. `scripts/validate-gen-plan-io.sh` locates this template at `prompt-template/plan/gen-plan-template.md`, copies it to the requested output plan, then appends the original draft under an “Original Design Draft” section at `scripts/validate-gen-plan-io.sh:133` and `scripts/validate-gen-plan-io.sh:148`. Output is a seeded plan file that the command then edits into a complete plan.
- gates_or_invariants: Template existence is an IO validation gate: missing template exits `7` at `scripts/validate-gen-plan-io.sh:142`. The command contract mirrors this exact structure and generation rules in `commands/gen-plan.md:142`, including AC format, dependency sequencing, no time estimates, and draft completeness at `commands/gen-plan.md:225`. Generated plans must preserve all original draft information and add clarifications incrementally at `commands/gen-plan.md:247`.
- dependencies_and_callers: Called by `scripts/validate-gen-plan-io.sh` either via `CLAUDE_PLUGIN_ROOT` or script-relative fallback at `scripts/validate-gen-plan-io.sh:133`. The `/gen-plan` command invokes that validation script first and interprets exit codes at `commands/gen-plan.md:30`. Related humanize skill docs and tests reference the same structure, but this directory’s only concrete algorithmic child is `gen-plan-template.md`.
- edge_cases_or_failure_modes: Missing output directory exits `3`; existing output path or output directory exits `4`; missing template exits `7`; invalid arguments exit `6` in `scripts/validate-gen-plan-io.sh:29`. The template’s deterministic-design note handles drafts with no design freedom by allowing upper/lower bounds to converge at `prompt-template/plan/gen-plan-template.md:44`.
- validation_or_tests: `tests/test-gen-plan.sh` contains validation coverage for `validate-gen-plan-io.sh` argument and path exit codes, with checks listed around `tests/test-gen-plan.sh:426`. `tests/test-template-references.sh` includes `prompt-template/plan/gen-plan-template.md` as a referenced template surface.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-044 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell UI/monitor entrypoint for Humanize loops. It does not implement the scheduler or dependency checker directly; its core algorithmic responsibility is route dispatch and live state/log monitoring for RLCR, PR loop, and skill monitors.
- algorithmic_behavior: It sources shared monitor utilities and loop constants at `scripts/humanize.sh:6` and `scripts/humanize.sh:12`, exposes parsing helpers, then dispatches `humanize monitor rlcr|pr|skill` at `scripts/humanize.sh:1103`. `humanize_parse_goal_tracker` extracts AC counts, completed ACs, active/completed/deferred tasks, open issues, and a goal summary from markdown sections at `scripts/humanize.sh:36`. `humanize_detect_git_state` classifies not-a-repo, permission, rebase, merge, shallow, detached, or normal states at `scripts/humanize.sh:120`. `humanize_parse_git_status` summarizes porcelain status and diffstat at `scripts/humanize.sh:170`.
- inputs_outputs_state: Inputs are local `.humanize/rlcr` and `.humanize/pr-loop` session directories, cached Codex logs, `state.md`, `goal-tracker.md`, git state, terminal size, and optional `--once` for PR monitor. Outputs are terminal status bars, rolling log tails, and one-shot PR status summaries. Runtime state is held in local variables like `current_session_dir`, `current_file`, file sizes, resize flags, and background `tail` PIDs; no persistent state writes are performed by this monitor.
- gates_or_invariants: RLCR monitor requires `.humanize/rlcr` to exist or returns `1` at `scripts/humanize.sh:236`. It requires a timestamped session or returns `1` at `scripts/humanize.sh:740`. It refuses inconsistent Codex log sequencing where max run round is not strictly less than min review round at `scripts/humanize.sh:328`. Terminal size gates render a too-small warning until resized at `scripts/humanize.sh:598`. PR monitor requires `.humanize/pr-loop` at `scripts/humanize.sh:1180` and maps monitor statuses from shared state-file detection at `scripts/humanize.sh:1236`.
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size`, loaded at `scripts/humanize.sh:8`; those helpers validate timestamped session names and state files at `scripts/lib/monitor-common.sh:40` and `scripts/lib/monitor-common.sh:154`. PR phase display uses `get_pr_loop_phase` and `get_pr_loop_phase_display` if present at `scripts/humanize.sh:1321`; those functions detect final states, Codex-analysis file growth, initial-review waiting, and reviewer waiting at `scripts/lib/monitor-common.sh:267`. PR issue stats use `humanize_parse_pr_goal_tracker` at `scripts/humanize.sh:1362`.
- edge_cases_or_failure_modes: Handles missing loop dirs, missing sessions, no log files, session deletion, log deletion, newer sessions, log rotation/truncation, terminal resize, and cleanup on `INT`/`TERM`/`WINCH`. The RLCR log finder searches cache under `XDG_CACHE_HOME` or `$HOME/.cache`, sanitized by current project path and session name at `scripts/humanize.sh:259`. Cross-platform file size and mtimes use GNU/BSD `stat` fallback via shared helpers. One issue to note: `date -d` in RLCR start-time formatting at `scripts/humanize.sh:435` is GNU-specific and falls back to the raw timestamp on macOS.
- validation_or_tests: PR monitor one-shot display is exercised by hook tests that source `scripts/humanize.sh` and run `humanize monitor pr --once` at `tests/test-pr-loop-hooks.sh:1555`, checking YAML list parsing, configured bot display, and empty active bot display at `tests/test-pr-loop-hooks.sh:1547`. Shared monitor helpers are tested in `tests/test-pr-loop-system.sh` according to references for latest-session, state-file, and file-size behavior. The assigned test wrapper `tests/test-pr-loop-2-hooks.sh` invokes these monitor tests through `run_hook_tests`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-074 `file` `tests/test-pr-loop-2-hooks.sh`
- cursor: `[_]`
- core_role: Executable split runner for PR-loop hook and monitor behavior. This file is a thin orchestration wrapper, but it is core as an executable specification entrypoint for validator, stop-hook, fetch-fixture, and monitor assertions.
- algorithmic_behavior: The wrapper sets strict shell mode at `tests/test-pr-loop-2-hooks.sh:9`, derives `SCRIPT_DIR` and `PROJECT_ROOT` at `tests/test-pr-loop-2-hooks.sh:11`, sources shared helpers and `test-pr-loop-lib.sh` at `tests/test-pr-loop-2-hooks.sh:14`, initializes the PR-loop test environment at `tests/test-pr-loop-2-hooks.sh:17`, sources `tests/test-pr-loop-hooks.sh` at `tests/test-pr-loop-2-hooks.sh:19`, runs `run_hook_tests` at `tests/test-pr-loop-2-hooks.sh:21`, then prints a named test summary and exits with that status at `tests/test-pr-loop-2-hooks.sh:23`.
- inputs_outputs_state: Inputs are the repository test harness, mock temp directories, mock `gh` and `codex` executables created by the sourced suite, and hook JSON fixtures generated inline. Outputs are pass/fail records via `pass`/`fail`, a final “PR Loop Hook Tests” summary, and process exit status.
- gates_or_invariants: `set -euo pipefail` makes missing sourced files, unbound variables, and failing commands fatal unless tests explicitly manage `set +e`. The invoked suite enforces invariants such as PR-loop state and comment files being read-only to Claude write tools at `tests/test-pr-loop-hooks.sh:68`, `round-N-pr-resolve.md` being writable at `tests/test-pr-loop-hooks.sh:135`, Bash protection not depending on an RLCR loop at `tests/test-pr-loop-hooks.sh:174`, YAML list parsing for `configured_bots` at `tests/test-pr-loop-hooks.sh:288`, marker detection for `APPROVE` and `WAITING_FOR_BOTS` at `tests/test-pr-loop-hooks.sh:415`, and consistent next-round file naming at `tests/test-pr-loop-hooks.sh:726`.
- dependencies_and_callers: Depends directly on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and `tests/test-pr-loop-hooks.sh`. The sourced suite calls validators such as `hooks/loop-write-validator.sh` and `hooks/loop-bash-validator.sh`, fetch scripts such as `scripts/fetch-pr-comments.sh`, and monitor entrypoints from `scripts/humanize.sh`. It also uses fixture mock setup at `tests/test-pr-loop-hooks.sh:1231`.
- edge_cases_or_failure_modes: The suite covers blocked write attempts to `.humanize/pr-loop/*/state.md` and `round-*-pr-comment.md`, allowed resolution summaries, wrong-round protections, comments deduplication, timestamp sorting, approval-only review fixtures, `--after` filtering, bot re-add logic when a previously approved bot reports new issues, trigger timestamp timeout anchoring, and monitor display with empty active bot lists.
- validation_or_tests: This file is itself the runner. Key validations include fixture-backed fetch coverage for issue comments, inline review comments, and approval-only reviews at `tests/test-pr-loop-hooks.sh:1242`; `--after` filtering at `tests/test-pr-loop-hooks.sh:1287`; monitor YAML/configured/empty bot cases at `tests/test-pr-loop-hooks.sh:1547`; and the full suite registration inside `run_hook_tests`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-104 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template that defines the post-round-0 transition contract for goal tracker changes: Claude cannot directly edit `goal-tracker.md`; changes must be requested for Codex review.
- algorithmic_behavior: The template renders a block titled with `{{CURRENT_ROUND}}` at `prompt-template/block/goal-tracker-modification.md:1`, states that after Round 0 only Codex can modify the Goal Tracker at `prompt-template/block/goal-tracker-modification.md:3`, blocks direct Write/Edit/Bash modifications at `prompt-template/block/goal-tracker-modification.md:5`, and prescribes a “Goal Tracker Update Request” section in `{{SUMMARY_FILE}}` at `prompt-template/block/goal-tracker-modification.md:9`.
- inputs_outputs_state: Inputs are `CURRENT_ROUND` and `SUMMARY_FILE`, passed through the template renderer. Output is a human-facing block emitted to stderr by validators when a prohibited goal-tracker modification is attempted. It does not mutate state; it routes desired state changes into the round summary for Codex to adjudicate.
- gates_or_invariants: The invariant is split-custody after Round 0: Claude may request, but Codex decides and applies goal tracker updates. `hooks/loop-write-validator.sh` enforces this for Write when `CURRENT_ROUND > 0` at `hooks/loop-write-validator.sh:194`. `hooks/loop-edit-validator.sh` enforces the same for Edit according to references around `hooks/loop-edit-validator.sh:145`. `hooks/loop-bash-validator.sh` blocks Bash modifications to `goal-tracker.md`, using this template after Round 0 at `hooks/loop-bash-validator.sh:349`.
- dependencies_and_callers: Rendered through `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1241`, which uses `load_and_render_safe` to load `block/goal-tracker-modification.md` with a fallback at `hooks/lib/loop-common.sh:1250`. Template variable substitution is single-pass and keeps missing placeholders unchanged by design in `hooks/lib/template-loader.sh:50`.
- edge_cases_or_failure_modes: If the template is missing or empty, `goal_tracker_blocked_message` falls back to a shorter inline message at `hooks/lib/loop-common.sh:1246`. Malformed state blocks Write validation before the template can render at `hooks/loop-write-validator.sh:142`. Bash round 0 uses a different block instructing proper Write/Edit usage rather than this post-round-0 request path at `hooks/loop-bash-validator.sh:350`.
- validation_or_tests: Template reference coverage includes this path in `tests/test-template-references.sh` at `tests/test-template-references.sh:158`. The behavioral gate is covered by hook tests that attempt blocked state/loop protected writes and Bash modifications, including PR/RLCR validator patterns in the sourced hook suite.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-134 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt fragment injected into the next Claude round after a Full Goal Alignment Check. It shifts the next implementation round’s priority toward forgotten tasks, unmet acceptance criteria, and unjustified deferrals.
- algorithmic_behavior: The template is a short checklist-like instruction block at `prompt-template/claude/post-alignment-action-items.md:2`. It tells Claude to address forgotten items, prioritize any ACs marked not met, and un-defer deferrals flagged as unjustified at `prompt-template/claude/post-alignment-action-items.md:4`.
- inputs_outputs_state: Input is the boolean stop-hook state `FULL_ALIGNMENT_CHECK`. Output is appended prompt text in the generated `round-${NEXT_ROUND}-prompt.md`; no persistent state is changed directly by this template. Its state effect is indirect: it steers the next round’s task selection and goal-tracker update requests.
- gates_or_invariants: The gate is conditional injection only when `FULL_ALIGNMENT_CHECK == true` at `hooks/loop-codex-stop-hook.sh:1615`. If the template loads and is non-empty, it is appended to the next prompt at `hooks/loop-codex-stop-hook.sh:1617`. This preserves the invariant that full-alignment findings feed into the immediate next round rather than being optional background context.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"` in `hooks/loop-codex-stop-hook.sh:1617`. It sits after the rendered next-round prompt and optional open-question notice, and before footer, push note, goal tracker request template, and agent-team continuation blocks at `hooks/loop-codex-stop-hook.sh:1565`.
- edge_cases_or_failure_modes: If the template is missing or empty, no fallback text is appended; the stop hook simply skips it because it checks `[[ -n "$POST_ALIGNMENT" ]]` at `hooks/loop-codex-stop-hook.sh:1618`. It depends on upstream alignment review correctly setting `FULL_ALIGNMENT_CHECK`; a regular review round will not receive this block.
- validation_or_tests: Template existence is covered by reference/template tests through `load_template` scanning. The surrounding alignment behavior is indirectly covered by stop-hook/finalize tests that exercise review phase and next prompt generation, but this exact fragment has no standalone executable assertion visible in the assigned files.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-164 `file` `tests/robustness/test-pr-loop-api-fetch.sh`
- cursor: `[_]`
- core_role: Executable split runner for the first PR-loop API robustness group: state detection, `fetch-pr-comments.sh`, bot comment parsing, and JSON edge cases.
- algorithmic_behavior: The wrapper sets strict mode at `tests/robustness/test-pr-loop-api-fetch.sh:12`, sources `tests/robustness/test-pr-loop-api-robustness.sh` at `tests/robustness/test-pr-loop-api-fetch.sh:15`, invokes `run_fetch_tests` at `tests/robustness/test-pr-loop-api-fetch.sh:17`, then prints “PR Loop API Fetch Tests” summary and exits with that status at `tests/robustness/test-pr-loop-api-fetch.sh:18`.
- inputs_outputs_state: Inputs come from the shared robustness file: mocked `gh` behavior, generated temp git repos, generated `.humanize/pr-loop/.../state.md`, and calls to real repo scripts. Outputs are pass/fail summary lines, temp output markdown files from `fetch-pr-comments.sh`, and final process status.
- gates_or_invariants: The invoked group requires active PR loop discovery to find the latest session with `state.md` using `find_active_pr_loop` at `tests/robustness/test-pr-loop-api-robustness.sh:292`; the implementation returns only the newest session containing `state.md` at `hooks/lib/loop-common.sh:941`. It verifies YAML list `active_bots` remains accepted at `tests/robustness/test-pr-loop-api-robustness.sh:304`, and that a PR loop without `pr_number` is still detectable at `tests/robustness/test-pr-loop-api-robustness.sh:331`.
- dependencies_and_callers: Depends on `tests/robustness/test-pr-loop-api-robustness.sh`, which sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-pr-loop-api-robustness.sh:18`. It exercises `scripts/fetch-pr-comments.sh` end-to-end with a generated mock `gh` that handles `repo view`, `pr view`, and `api` calls at `tests/robustness/test-pr-loop-api-robustness.sh:32`.
- edge_cases_or_failure_modes: Covers empty API arrays producing a valid output file with PR/repo metadata at `tests/robustness/test-pr-loop-api-robustness.sh:362`; rate-limit API failures producing warnings or non-zero exit at `tests/robustness/test-pr-loop-api-robustness.sh:386`; network errors producing non-zero/error output at `tests/robustness/test-pr-loop-api-robustness.sh:415`; Claude approval comments, Codex severity markers, and mixed bot outputs at `tests/robustness/test-pr-loop-api-robustness.sh:442`; Unicode and very long comments at `tests/robustness/test-pr-loop-api-robustness.sh:518`.
- validation_or_tests: This file is the validation entrypoint for tests 1-11, as stated in its header at `tests/robustness/test-pr-loop-api-fetch.sh:5` and in the shared function declaration at `tests/robustness/test-pr-loop-api-robustness.sh:282`. It intentionally excludes later poll/stop-hook tests, which are in the sibling split runner.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-014`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-044`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-074`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-104`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-134`, `CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-164`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`