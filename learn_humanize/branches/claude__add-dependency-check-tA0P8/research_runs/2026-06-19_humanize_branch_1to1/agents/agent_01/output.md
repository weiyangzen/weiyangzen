# agent_01 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize plugin runtime. The root coordinates slash-command setup scripts, Claude Code hooks, RLCR/PR loop state machines, prompt templates, skills, docs, and executable tests. The top-level flow is documented as plan -> Claude summary -> Codex summary review -> `codex review` -> completion in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/README.md:101) and [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/commands/start-rlcr-loop.md:16).
- algorithmic_behavior: `commands/` exposes command wrappers that call `scripts/`; `scripts/setup-rlcr-loop.sh` initializes `.humanize/rlcr/<timestamp>/state.md`, plan backup/symlink, goal tracker, prompts, and review-phase markers; `hooks/hooks.json` wires PreToolUse/PostToolUse/Stop hooks, including the RLCR stop hook at [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/hooks.json:63); `hooks/` enforces file/state invariants and drives stop-time review; `hooks/lib/` provides shared parsing, state, path, and PR helper functions; `prompt-template/` supplies rendered block/review/next-round/finalize prompts; `skills/` provides installable skill contracts; `tests/` and `tests/robustness/` encode executable behavior.
- inputs_outputs_state: Inputs are slash-command arguments, Claude hook JSON on stdin, git state, plan files, PR/GitHub data for PR loop scripts, Codex CLI output, environment variables such as `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and Codex model/timeout options. Outputs are `.humanize/rlcr/**` loop state/prompt/summary/review files, `.humanize/pr-loop/**` PR loop state, `.humanize/skill/**` ask-codex artifacts, cache logs under XDG cache, hook JSON decisions, and process exit codes.
- gates_or_invariants: Root-level RLCR setup now performs an upfront dependency check for `codex`, `jq`, and `git` before loop creation in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/scripts/setup-rlcr-loop.sh:263). It enforces mutual exclusion with PR/RLCR loops, git repository/clean-worktree requirements, safe plan paths/content, safe YAML fields, local base branch detection, captured base commit, and clean state initialization. Hook validators then enforce protected loop files, current-round access, task completion, git cleanliness, and review-phase gates.
- dependencies_and_callers: `commands/start-rlcr-loop.md` allows only `scripts/setup-rlcr-loop.sh` at [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/commands/start-rlcr-loop.md:4). `hooks/hooks.json` calls validators and stop hooks. `scripts/ask-codex.sh` and setup scripts source `hooks/lib/loop-common.sh`. Tests are orchestrated by [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/run-all-tests.sh:31), including `test-codex-review-merge.sh` at line 46 and robustness coverage at lines 66-84.
- edge_cases_or_failure_modes: The export is not a live git checkout, so git-based repository verification could not be used during this research. Runtime edge cases covered by the tree include missing dependencies, missing/old state schema, branch drift, plan mutation/deletion, symlink/path traversal, missing base branch, dirty git state, large files, stale git `index.lock`, malformed hook JSON, stale session directories, missing/empty Codex logs, and direct attempts to edit state/prompt/plan backup files.
- validation_or_tests: The test suite includes hook tests, state parsing, template references, PR loop behavior, ask-codex, Codex review merge detection, and robustness suites. `tests/run-all-tests.sh` also creates a mock `codex` binary when unavailable to let dependency checks pass in tests ([tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/run-all-tests.sh:95)). Tests were inspected but not executed, consistent with the read-only research-only instruction.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-031 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop hook implementation for the RLCR state machine. It intercepts Claude stop attempts, validates loop state and work readiness, runs Codex review paths, updates RLCR phase state, and either allows exit or returns a blocking JSON decision.
- algorithmic_behavior: The hook reads stdin hook JSON, derives `PROJECT_ROOT`, loads `loop-common.sh` and `portable-timeout.sh`, extracts `session_id`, and finds the active RLCR loop ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-codex-stop-hook.sh:28), [line 61](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-codex-stop-hook.sh:61)). It validates state schema and Codex config, branch consistency, plan integrity, incomplete tasks, git status, large files, uncommitted/unpushed changes, summary existence, goal tracker initialization, and max iterations. In implementation phase it builds a Codex prompt, runs `codex exec`, reads/copies the review result, and checks the last non-empty line for `COMPLETE` or `STOP`. `COMPLETE` transitions to review phase, sets `review_started: true`, creates `.review-phase-started`, and runs `codex review`; `[P0-9]` findings continue the review loop, no findings enter finalize; finalize then requires `finalize-summary.md` and renames finalize state to complete.
- inputs_outputs_state: Inputs include `state.md` or `finalize-state.md`, hook JSON, `round-N-summary.md`, `goal-tracker.md`, git branch/status, `check-todos-from-transcript.py` output, prompt templates, Codex CLI exit/stdout/stderr, and environment toggles like `HUMANIZE_CODEX_BYPASS_SANDBOX`. Outputs include `round-N-review-prompt.md`, `round-N-review-result.md`, cache command/log/out files, `.review-phase-started`, edited `current_round`/`review_started`, renamed terminal state files, next-round prompts, and hook JSON with `decision: block` plus reason/system message.
- gates_or_invariants: Critical fields must be present, not silently defaulted (`current_round`, `max_iterations`, `review_started`, `base_branch`) at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-codex-stop-hook.sh:149). Current branch must equal start branch; plan file must exist and match backup before review phase; task checker must find no incomplete tasks; git status must succeed fail-closed; changed code/docs files over 2000 lines block; git must be clean; summaries are mandatory; review-phase manual toggles are blocked by requiring `.review-phase-started` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-codex-stop-hook.sh:1497)).
- dependencies_and_callers: Called by the Claude Code Stop hook configured in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/hooks.json:63). Depends on `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, `hooks/check-todos-from-transcript.py`, `codex`, `git`, `jq`, `python3`, and templates under `prompt-template/codex`, `prompt-template/claude`, and `prompt-template/block`. Its state files are created by `scripts/setup-rlcr-loop.sh`.
- edge_cases_or_failure_modes: No active loop or no active state allows exit. Invalid model/effort ends unexpected. Git failure blocks and cleans stale `index.lock` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-codex-stop-hook.sh:424)). Missing Codex blocks. Nonzero `codex exec`, missing/empty result, failed `codex review`, and empty review log all block retry. `STOP` ends as stop. Max iteration can end as maxiter. `BASE_COMMIT` is preferred for review base to avoid branch ref drift ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-codex-stop-hook.sh:971)).
- validation_or_tests: `tests/test-codex-review-merge.sh` directly covers the shared `detect_review_issues` review-phase parser used by this hook. Related suites include `test-stop-gate.sh`, `test-finalize-phase.sh`, `test-agent-teams.sh`, and robustness hook/state tests listed in `tests/run-all-tests.sh`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-061 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the Codex review log merge/detection algorithm implemented as `detect_review_issues()` in `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The test creates isolated temp `LOOP_DIR` and `CACHE_DIR`, sources `loop-common.sh`, writes synthetic `round-N-codex-review.log` files, invokes `detect_review_issues N`, and asserts exit codes, extracted content, and result-file creation. The tested algorithm is explicitly documented in the file: scan only the last 50 lines, find first `[P?]` marker with digit in the first 10 characters, extract from that line to end, otherwise return no issues ([tests/test-codex-review-merge.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-codex-review-merge.sh:11)).
- inputs_outputs_state: Inputs are generated log files under `$CACHE_DIR` and exported globals `LOOP_DIR`/`CACHE_DIR`; output is terminal PASS/FAIL reporting, process exit 0/1, and generated `round-N-review-result.md` when issues are found. It mutates only temp directories created by `mktemp`.
- gates_or_invariants: Positive detection requires `[P0-9]` in the first ten characters of a line. Missing or empty logs are hard errors with return code 2. No issue marker returns 1. Issue detection returns 0 and must preserve all content from the first issue line through EOF.
- dependencies_and_callers: Sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:487). Included in the aggregate runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/run-all-tests.sh:46).
- edge_cases_or_failure_modes: Covers markers too far into a line, no markers, missing log, empty log, long logs with markers inside and outside the 50-line scan window, multiple markers, marker at column 0, dash-prefixed markers, exactly 50-line logs, and result-file creation ([tests/test-codex-review-merge.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-codex-review-merge.sh:56), [line 332](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-codex-review-merge.sh:332)).
- validation_or_tests: The file is itself validation. It exits 1 if any assertion fails ([tests/test-codex-review-merge.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/test-codex-review-merge.sh:366)).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-091 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared algorithm library for RLCR and PR loop hooks/scripts. It centralizes state field names/defaults, hook input validation, session-aware loop discovery, state parsing, Codex review issue detection, path classification, block-message rendering, cancel authorization, git-add protection, command modification detection, terminal state renaming, and PR goal tracker updates.
- algorithmic_behavior: Validates hook JSON for null bytes, UTF-8, jq syntax, and `tool_name` ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:91)). Finds active loops with zombie protection and optional `session_id` matching ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:248)). Parses state frontmatter in tolerant and strict modes, with strict mode rejecting missing required fields and nonnumeric rounds. `detect_review_issues()` scans only the last 50 review-log lines for `[P0-9]` in the first ten characters, writes extracted findings to `round-N-review-result.md`, and returns 0/1/2 for issues/no issues/hard error. Validator helpers classify prompt/summary/todos/state/goal-tracker paths and generate template-backed block messages. Security helpers authorize only signal-file-backed cancel `mv` commands, detect `.humanize`-targeting `git add`, and detect Bash writes via redirects, `tee`, in-place edits, moves, copies, removes, and truncation.
- inputs_outputs_state: Inputs include hook JSON, filesystem loop directories, YAML-like markdown frontmatter, Codex review logs, command strings, active loop paths, PR bot result JSON, and templates. Outputs are shell return codes, global `STATE_*`/`VALIDATED_*` variables, rendered block text, extracted review-result files, terminal state renames through `end_loop()`, and PR goal tracker mutations.
- gates_or_invariants: Strict state parsing requires frontmatter separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`, plus boolean `review_started` ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:401)). Cancel is allowed only when `.cancel-requested` exists and the command is a narrowly parsed `mv state.md|finalize-state.md cancel-state.md` without substitution/operators/symlink source ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:694)). `.humanize` must not be staged directly or through broad/forced git-add commands ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:1073)).
- dependencies_and_callers: Sourced by RLCR validators, PR validators/stop hooks, setup/cancel scripts, ask-codex, and tests. It sources `template-loader.sh` and expects `jq`, standard POSIX tools, and sometimes `git`.
- edge_cases_or_failure_modes: Empty stored session IDs preserve backward compatibility. Without a session filter only the newest loop is considered, preventing stale-loop revival. `detect_review_issues()` intentionally ignores early-log markers and markers after column ten, trading false-positive reduction for possible false negatives. Bash command detection is pattern-based and broad but can be shell-syntax fragile. `DEFAULT_CODEX_MODEL` changes depending on whether the library path appears under `.claude/plugins/cache/` ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/lib/loop-common.sh:51)).
- validation_or_tests: Directly covered by `tests/test-codex-review-merge.sh`, state robustness tests, session robustness tests, cancel-security tests, bash-validator pattern tests, hook-input robustness tests, and PR loop tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-121 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template used by loop read validation when Claude attempts to read a round prompt/summary file from outside the active `.humanize/rlcr/<timestamp>/` loop directory.
- algorithmic_behavior: Renders a short markdown block with placeholders for attempted `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`, then points to the active prompt and summary file names ([prompt-template/block/wrong-file-location.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/prompt-template/block/wrong-file-location.md:1)).
- inputs_outputs_state: Inputs are template variables from `hooks/loop-read-validator.sh`; output is stderr markdown consumed by Claude Code as a PreToolUse block reason. It does not mutate state.
- gates_or_invariants: Supports the invariant that RLCR round files must be accessed from the active loop directory, not copied paths, stale directories, or wrong working directories. The read validator renders it when `IN_HUMANIZE_LOOP_DIR=false` for prompt/summary round files ([hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/hooks/loop-read-validator.sh:122)).
- dependencies_and_callers: Loaded via `load_and_render_safe` from `loop-read-validator.sh` with fallback text. Depends on `template-loader.sh` indirectly through `loop-common.sh`.
- edge_cases_or_failure_modes: If placeholders are missing, the rendered instructions may be incomplete. The final line says `If you need this file, use: cat {{FILE_PATH}}`; that is potentially confusing because the trigger is a blocked Read access, though Bash `cat` may still be governed by separate Bash validation rather than the Read validator.
- validation_or_tests: Template availability/reference coverage is likely through `test-template-references.sh` and comprehensive template tests in the root test suite; behavior is exercised indirectly by read-validator/path-validation tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-151 `file` `skills/ask-codex/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction contract for one-shot Codex consultation. It is not the RLCR loop itself, but it defines an algorithmic workflow that routes user questions to `scripts/ask-codex.sh` and tells the agent how to interpret outputs/errors.
- algorithmic_behavior: The skill metadata names `ask-codex`, describes it as sending a question/task to `codex exec`, declares arguments, and restricts allowed tool use to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh:*)` ([skills/ask-codex/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/skills/ask-codex/SKILL.md:1)). Runtime instruction is a single execution of the script with `$ARGUMENTS`, then read stdout carefully and report nonzero errors.
- inputs_outputs_state: Inputs are skill arguments `--codex-model`, `--codex-timeout`, and free-form question/task. Outputs are Codex response on stdout, status/debug on stderr, and saved artifacts under `.humanize/skill/<timestamp>/output.md` per the skill notes. The backing script validates Codex availability, nonempty question, safe model/effort characters, creates local/cache storage, runs `codex exec`, writes metadata, and emits stdout ([scripts/ask-codex.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/scripts/ask-codex.sh:153), [line 391](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/scripts/ask-codex.sh:391)).
- gates_or_invariants: Exit code meanings are part of the contract: 0 success, 1 validation error, 124 timeout, other Codex process error ([skills/ask-codex/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/skills/ask-codex/SKILL.md:26)). The backing script uses the same model/effort safety regex pattern as RLCR and supports `HUMANIZE_CODEX_BYPASS_SANDBOX` for the automation flag.
- dependencies_and_callers: Called by `/humanize:ask-codex` skill invocation and implemented by `scripts/ask-codex.sh`, which sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh`. Requires Codex CLI and a project root; uses git to prefer repository top-level when available.
- edge_cases_or_failure_modes: Missing `codex`, empty question, invalid flags/model/effort, timeout, nonzero Codex exit, empty stdout, and unwritable home cache are all handled by the script. A timeout exits 124 and recommends increasing `--codex-timeout` ([scripts/ask-codex.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/scripts/ask-codex.sh:308)).
- validation_or_tests: `tests/test-ask-codex.sh` is listed in the aggregate runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/tests/run-all-tests.sh:61). README documents the same command and persistence behavior at [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/claude__add-dependency-check-tA0P8/README.md:212).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 unique item sections, matching the 6 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`