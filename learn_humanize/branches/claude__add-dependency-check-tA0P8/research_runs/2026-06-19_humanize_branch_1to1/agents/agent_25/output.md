# agent_25 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-025 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for transforming a user draft into an implementation plan. It is not executable shell logic itself, but it orchestrates validation, repository relevance analysis, user clarification, deterministic plan structure, and final write/review behavior.
- algorithmic_behavior: Defines six ordered phases: IO validation, relevance check, draft analysis, issue resolution, plan generation, and write/complete (`commands/gen-plan.md:19-27`). It delegates first-pass path validation to `scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:32-36`), rejects unrelated drafts through a `humanize:draft-relevance-checker` Task result of `NOT_RELEVANT` (`commands/gen-plan.md:56-72`), uses Explore agents for completeness/functionality investigation (`commands/gen-plan.md:78-108`), and requires user clarification for significant issues and quantitative thresholds (`commands/gen-plan.md:111-139`).
- inputs_outputs_state: Inputs are CLI arguments `--input <draft>` and `--output <plan>` (`commands/gen-plan.md:3`) plus the draft contents. Output is a new plan file that begins from `prompt-template/plan/gen-plan-template.md` and preserves the original draft appended by the validation script (`scripts/validate-gen-plan-io.sh:133-160`). The command state transition is: nonexistent output path -> template-plus-draft output -> edited final plan (`commands/gen-plan.md:251-260`).
- gates_or_invariants: Hard IO gates map validation exit codes 1-7 to user-facing stops (`commands/gen-plan.md:38-47`). Plan invariants include AC identifiers using `AC-X` or `AC-X.Y` (`commands/gen-plan.md:235`), no time estimates (`commands/gen-plan.md:227-232`), code path references without line numbers inside generated plan text (`commands/gen-plan.md:229`), required positive and negative tests for every acceptance criterion (`commands/gen-plan.md:239`), affirmative upper/lower path boundaries (`commands/gen-plan.md:241`), and full preservation of all draft content (`commands/gen-plan.md:247`).
- dependencies_and_callers: Depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:5`, `commands/gen-plan.md:35`), the Read/Glob/Grep/Task/Write/AskUserQuestion tool set (`commands/gen-plan.md:4-11`), the relevance-checker subagent (`commands/gen-plan.md:57-65`), Explore subagents (`commands/gen-plan.md:101-108`), and the plan template copied by the validation script (`scripts/validate-gen-plan-io.sh:135-149`).
- edge_cases_or_failure_modes: Rejects missing input, empty input, missing output directory, existing output, directory-as-output, no write permission, invalid args, and missing template (`scripts/validate-gen-plan-io.sh:79-145`). Relevance false negatives stop generation. Mixed-language output triggers an AskUserQuestion decision rather than automatic translation (`commands/gen-plan.md:272-285`). A draft with fixed deterministic design narrows allowed choices and may collapse path-boundary variance (`commands/gen-plan.md:192-193`, `commands/gen-plan.md:243`).
- validation_or_tests: Validation is primarily encoded in `scripts/validate-gen-plan-io.sh`, which prints `VALIDATION_SUCCESS`, copies the template, appends original draft delimiters, and exits 0 (`scripts/validate-gen-plan-io.sh:126-160`). I did not run tests because this worker was instructed to produce read-only research notes only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-055 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR `--agent-teams` algorithm: CLI enablement, environment gating, state persistence, state parsing, initial prompt injection, next-round prompt injection, and review-phase exclusion.
- algorithmic_behavior: The test first asserts `--agent-teams` fails unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`tests/test-agent-teams.sh:54-69`), rejects values other than `1` (`tests/test-agent-teams.sh:71-79`), succeeds when enabled (`tests/test-agent-teams.sh:103-110`), records `agent_teams: true` in `state.md` (`tests/test-agent-teams.sh:117-121`), defaults to false without the flag (`tests/test-agent-teams.sh:145-153`), and verifies `parse_state_file` reads or defaults `STATE_AGENT_TEAMS` (`tests/test-agent-teams.sh:159-209`). It then checks initial prompt keywords and template existence (`tests/test-agent-teams.sh:233-355`) and exercises the stop hook to ensure implementation-phase continuation prompts include agent-team guidance while review-phase prompts do not (`tests/test-agent-teams.sh:394-630`).
- inputs_outputs_state: Inputs are temporary git repositories, a temporary `temp/plan.md`, environment variables `CLAUDE_PROJECT_DIR` and `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, mocked `codex` binaries for stop-hook sections, and synthetic stop-hook JSON (`tests/test-agent-teams.sh:546-548`). Outputs are pass/fail counters from `tests/test-helpers.sh` (`tests/test-helpers.sh:30-77`), generated `.humanize/rlcr/*/state.md`, `round-0-prompt.md`, and next-round prompts such as `round-4-prompt.md` or `round-6-prompt.md`.
- gates_or_invariants: `--agent-teams` must be opt-in and must require exactly env value `1`; `0`, `false`, `yes`, and `true` are rejected (`tests/test-agent-teams.sh:71-79`). State must persist as `agent_teams: true|false`; missing state field must parse as false (`hooks/lib/loop-common.sh:345-386`). Initial and continuation prompts require team-leader language only when enabled (`tests/test-agent-teams.sh:238-255`, `tests/test-agent-teams.sh:287-295`, `tests/test-agent-teams.sh:552-597`). Review phase explicitly excludes agent-team continuation even if state says true (`tests/test-agent-teams.sh:600-630`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` (`tests/test-agent-teams.sh:17-22`). Calls `scripts/setup-rlcr-loop.sh` (`tests/test-agent-teams.sh:29`) and `hooks/loop-codex-stop-hook.sh` (`tests/test-agent-teams.sh:394-395`). Implementation hooks are visible in setup: option parse at `scripts/setup-rlcr-loop.sh:227-230`, env gate at `scripts/setup-rlcr-loop.sh:319-329`, state write at `scripts/setup-rlcr-loop.sh:792-810`, initial prompt injection at `scripts/setup-rlcr-loop.sh:1027-1048`, and stop-hook continuation injection at `hooks/loop-codex-stop-hook.sh:1645-1666`.
- edge_cases_or_failure_modes: Branch-level dependency checking in setup now requires `codex`, `jq`, and `git` before loop creation (`scripts/setup-rlcr-loop.sh:258-286`). Several early setup calls in this test do not mock `codex`, so environments without a real `codex` binary will fail before state/prompt assertions. The test also intentionally uses `|| true` for some setup invocations (`tests/test-agent-teams.sh:104`, `tests/test-agent-teams.sh:146`, `tests/test-agent-teams.sh:234`, `tests/test-agent-teams.sh:283`), so the actual failure is detected later by missing files rather than immediately by exit code. Template checks are skipped if files are absent rather than hard-failing for missing template files (`tests/test-agent-teams.sh:297-355`).
- validation_or_tests: This file is itself the validation suite. It verifies real generated files and real parser variables rather than only unit-mocking. Stop-hook tests use a mock `codex` whose `exec` or `review` output drives next-round transitions (`tests/test-agent-teams.sh:496-530`). I did not execute it because research instructions forbade modifying files; the test creates temp repos and generated loop files.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-085 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/rlcr-stop-gate.sh`, especially project-root resolution and active-loop blocking semantics outside the native Claude Stop hook environment.
- algorithmic_behavior: Builds a minimal active RLCR loop fixture with `.humanize/rlcr/<timestamp>/state.md`, a copied plan, and no current summary (`tests/test-stop-gate.sh:18-59`). Then it asserts the wrapper defaults project root to caller cwd and blocks that active loop with exit 10 (`tests/test-stop-gate.sh:64-82`), prints a `BLOCK:` reason (`tests/test-stop-gate.sh:84-89`), honors `--project-root` from outside the target repository (`tests/test-stop-gate.sh:91-116`), and exits 0 with `ALLOW:` when no active loop exists (`tests/test-stop-gate.sh:118-142`).
- inputs_outputs_state: Inputs are temp git repositories, generated `plan.md`, copied loop `plan.md`, and a crafted `state.md` with `current_round: 0`, `review_started: false`, `agent_teams: false`, branch metadata, and Codex settings (`tests/test-stop-gate.sh:22-58`). Outputs are wrapper exit codes and `out.txt` containing `BLOCK:` or `ALLOW:`. It does not advance loop state directly; it tests that missing summary causes stop-hook block behavior through the wrapper.
- gates_or_invariants: Exit-code contract is 0 for allowed, 10 for blocked, 20 for wrapper/runtime error in `scripts/rlcr-stop-gate.sh:8-11`. The wrapper default `PROJECT_ROOT` is `${CLAUDE_PROJECT_DIR:-$(pwd)}` (`scripts/rlcr-stop-gate.sh:21`), and `--project-root` overrides it (`scripts/rlcr-stop-gate.sh:53-56`). It must require `jq` (`scripts/rlcr-stop-gate.sh:79-82`), pass Claude-like Stop JSON to the underlying hook (`scripts/rlcr-stop-gate.sh:84-97`), map block JSON to printed `BLOCK:` and exit 10 (`scripts/rlcr-stop-gate.sh:122-133`), and print `ALLOW:` when hook output is empty (`scripts/rlcr-stop-gate.sh:110-113`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` (`tests/test-stop-gate.sh:8-9`), uses `scripts/rlcr-stop-gate.sh` (`tests/test-stop-gate.sh:11`), which wraps `hooks/loop-codex-stop-hook.sh` (`scripts/rlcr-stop-gate.sh:19-22`). The expected block reason comes from the stop hook’s summary-file gate, which blocks exit when the current round summary is missing (`hooks/loop-codex-stop-hook.sh:650-668`).
- edge_cases_or_failure_modes: Missing `jq`, missing/non-executable stop hook, non-JSON hook output, or unexpected hook decisions become wrapper exit 20 (`scripts/rlcr-stop-gate.sh:74-82`, `scripts/rlcr-stop-gate.sh:104-120`, `scripts/rlcr-stop-gate.sh:136-138`). If the wrapper resolved root to plugin install dir instead of cwd, Test 1 would allow or inspect the wrong loop; this test specifically protects against that regression (`tests/test-stop-gate.sh:64-82`).
- validation_or_tests: This test validates project-root behavior through black-box wrapper calls and checks both exit codes and output markers. It uses a single `setup_test_dir` to avoid temp cleanup trap overwrite (`tests/test-stop-gate.sh:61`). I did not execute it under the read-only research constraint.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-115 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR invariant that workers/Claude must not modify `state.md`; loop state transitions are owned by setup/hooks, not by implementation agents.
- algorithmic_behavior: The template emits a short block message headed “State File Modification Blocked” and instructs the agent that `state.md` is managed by the loop system and should not be changed (`prompt-template/block/state-file-modification.md:1-3`). It tells the worker to stop if work is done so another review round is triggered (`prompt-template/block/state-file-modification.md:3`).
- inputs_outputs_state: Input is a validator request to render a block reason. Output is the rendered template text via `state_file_blocked_message()` (`hooks/lib/loop-common.sh:606-613`). It prevents direct state mutation; legitimate transitions such as round increments, review phase start, and loop ending are performed by hook code with controlled writes or renames (`hooks/loop-codex-stop-hook.sh:1471-1479`, `hooks/loop-codex-stop-hook.sh:1556-1559`, `hooks/lib/loop-common.sh:1255-1288`).
- gates_or_invariants: Bash validator blocks destination writes/redirections/moves/copies to `state.md` and source moves/copies from `state.md`, with only authorized cancel exceptions (`hooks/loop-bash-validator.sh:128-180`). Edit validator blocks direct edits to state/finalize-state files (`hooks/loop-edit-validator.sh:112-125`). Write validator sends `.humanize/rlcr` state paths into further validation instead of allowing raw writes (`hooks/loop-write-validator.sh:112-140`). PR loop has a separate state block path for `.humanize/pr-loop` state files (`hooks/loop-bash-validator.sh:404-415`).
- dependencies_and_callers: Loaded by `state_file_blocked_message()` from `TEMPLATE_DIR/block/state-file-modification.md` with a fallback if missing (`hooks/lib/loop-common.sh:606-613`). Referenced by template coverage tests (`tests/test-template-references.sh:155`). Consumed by Bash/Edit/Write validators through common helper functions.
- edge_cases_or_failure_modes: The Bash validator has explicit handling for bypass attempts such as chained commands, shell wrappers, redirections, quoted paths, and `mv state.md /tmp/foo` source moves (`hooks/loop-bash-validator.sh:158-180`). A missing template is not fatal because `loop-common.sh` has an inline fallback (`hooks/lib/loop-common.sh:607-612`). The message is generic to RLCR; PR loop state uses `prompt-template/block/pr-loop-state-modification.md` instead.
- validation_or_tests: Validation coverage is distributed through validator tests and template-reference tests; this assigned file is the renderable policy text, while the enforcement logic lives in hook validators. I inspected enforcement references but did not run tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-145 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR-loop goal tracker schema. It defines the persistent progress surface for bot-review approval loops: PR metadata, ultimate goal, issue summary table, total issue counters, and initial issue log.
- algorithmic_behavior: Renders PR number, branch, start time, monitored bots, and startup case placeholders (`prompt-template/pr-loop/goal-tracker-initial.md:3-10`), sets the goal to getting all monitored bot reviewers to approve (`prompt-template/pr-loop/goal-tracker-initial.md:11-14`), initializes the Issue Summary with Round 0 and zero issue counts (`prompt-template/pr-loop/goal-tracker-initial.md:15-20`), initializes total statistics to zero (`prompt-template/pr-loop/goal-tracker-initial.md:21-25`), and starts the Issue Log as awaiting initial reviews (`prompt-template/pr-loop/goal-tracker-initial.md:27-33`).
- inputs_outputs_state: Inputs are setup-time variables `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, and `STARTED_AT`, assembled in `scripts/setup-pr-loop.sh:658-666`. Output is `.humanize/pr-loop/<timestamp>/goal-tracker.md`, written by rendering this template or fallback content (`scripts/setup-pr-loop.sh:641-713`). Later state transitions append/update issue summary rows, totals, and log entries in `update_pr_goal_tracker()` (`hooks/lib/loop-common.sh:1294-1425`).
- gates_or_invariants: The template must contain `## Issue Summary`, `## Total Statistics`, `## Issue Log`, and the three counter fields; `tests/test-pr-loop-system.sh:642-655` asserts those schema markers. Monitoring depends on exact counter labels `Total Issues Found`, `Total Issues Resolved`, and `Remaining` (`scripts/lib/monitor-common.sh:475-497`). The stop hook’s Codex prompt also instructs per-bot status output and uses goal tracker update instructions as part of PR review validation (`hooks/pr-loop-stop-hook.sh:1253-1295`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` via `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/goal-tracker-initial.md"` (`scripts/setup-pr-loop.sh:712`). Parsed by monitor code (`scripts/lib/monitor-common.sh:465-497`) and tested by PR loop system tests (`tests/test-pr-loop-system.sh:638-655`, `tests/test-pr-loop-system.sh:1453-1528`). Coordinated with `prompt-template/pr-loop/codex-goal-tracker-update.md`, loaded by the PR stop hook (`hooks/pr-loop-stop-hook.sh:1253-1263`).
- edge_cases_or_failure_modes: The setup script computes `BOT_AC_ROWS` but this template does not include that placeholder, so the rendered schema is issue-centric rather than acceptance-criteria-centric (`scripts/setup-pr-loop.sh:646-665`, `prompt-template/pr-loop/goal-tracker-initial.md:15-33`). If template markers drift, monitor parsing falls back to zeros or `none`, and schema tests fail. Startup case is dynamic and can later be recalculated in the PR stop hook (`hooks/pr-loop-stop-hook.sh:1554-1584`), but the initial goal tracker preserves the startup case at creation time.
- validation_or_tests: `test_goal_tracker_schema` validates the static template schema (`tests/test-pr-loop-system.sh:642-655`), and `test_goal_tracker_creation_integration` verifies setup creates a tracker containing the expected structure and PR number (`tests/test-pr-loop-system.sh:1453-1528`). I did not execute tests because this run requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections present, one per assigned row, in the same order as the assignment table
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`