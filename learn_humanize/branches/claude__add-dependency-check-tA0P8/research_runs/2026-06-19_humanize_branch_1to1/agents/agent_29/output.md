# agent_29 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-029 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude Code plugin hook registry for Humanize loop enforcement. It is not an algorithm implementation itself, but it is the dispatch table that routes Claude hook events into the RLCR and PR-loop validators/state-machine guards.
- algorithmic_behavior: Registers `UserPromptSubmit` to `hooks/loop-plan-file-validator.sh` so loop prompts are checked before acceptance (`hooks/hooks.json:4-13`). Registers `PreToolUse` validators for `Write`, `Edit`, `Read`, and `Bash`, mapping each tool to a specific loop protection script (`hooks/hooks.json:14-51`). Registers `PostToolUse` for `Bash` to `loop-post-bash-hook.sh`, which records setup/session state after Bash commands (`hooks/hooks.json:52-62`). Registers two `Stop` hooks, `loop-codex-stop-hook.sh` for RLCR and `pr-loop-stop-hook.sh` for PR loops, each with a 7200 second timeout (`hooks/hooks.json:63-78`).
- inputs_outputs_state: Input is Claude’s hook event stream plus the JSON hook payload passed to the configured command scripts. Output is delegated to the hook scripts’ exit codes/stdout/stderr or JSON hook decisions. This file has no local state mutation, but it gates all state mutations by deciding which validator or Stop hook receives each event.
- gates_or_invariants: All file access and shell writes during active loops are mediated by tool-specific validators. Stop events always invoke both loop Stop hooks, so each Stop hook must independently detect whether its loop type is active and fail open when unrelated. The registry assumes `${CLAUDE_PLUGIN_ROOT}` is available and points to the installed plugin root (`hooks/hooks.json:9`, `hooks/hooks.json:20`, `hooks/hooks.json:29`, `hooks/hooks.json:38`, `hooks/hooks.json:47`, `hooks/hooks.json:58`, `hooks/hooks.json:68`, `hooks/hooks.json:73`).
- dependencies_and_callers: Consumed by Claude Code’s plugin hook system. It depends on `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-post-bash-hook.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. The Bash validator in turn sources `hooks/lib/loop-common.sh` and uses `command_modifies_file` for protected-path detection.
- edge_cases_or_failure_modes: If `${CLAUDE_PLUGIN_ROOT}` is unset or wrong, every hook command resolves incorrectly. Because Stop has no matcher, both RLCR and PR Stop hooks run for every Stop event; incorrect active-loop detection in either script could block unrelated sessions. The 7200 second Stop timeout permits long Codex/PR review polling but can hold Stop processing for a long time if a hook hangs.
- validation_or_tests: Related executable coverage appears across the hook tests. The assigned `tests/test-bash-validator-patterns.sh` covers the shared Bash modification detector used by the Bash validator. The assigned `tests/test-todo-checker.sh` covers the task gate called by `loop-codex-stop-hook.sh`. Other discovered tests cover session-id, plan-file, Stop gate, finalize phase, and PR Stop hook behavior.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-059 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash write-detection subroutine used by loop validators to prevent bypassing `Write`/`Edit` protections through shell commands.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` (`tests/test-bash-validator-patterns.sh:11-13`), lowercases command input with `to_lower`, and calls `command_modifies_file "$command_lower" "$pattern"` through `assert_modifies` and `assert_not_modifies` (`tests/test-bash-validator-patterns.sh:36-62`). It tests positive detection for redirection (`>`/`>>`), `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd of=`, `truncate`, `exec fd>`, and `printf >` (`tests/test-bash-validator-patterns.sh:69-134`). It tests negative detection for read-only commands like `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `file`, `stat`, and `diff` (`tests/test-bash-validator-patterns.sh:136-153`). It also covers bare redirection, no-space redirection, variable targets, state file patterns, and round summary patterns (`tests/test-bash-validator-patterns.sh:155-194`).
- inputs_outputs_state: Inputs are literal shell command strings and regex target patterns, defaulting to `goal-tracker\.md`. Outputs are pass/fail lines and aggregate counters, with process exit `0` when all expectations pass and `1` when any fail (`tests/test-bash-validator-patterns.sh:197-214`). It creates no persistent repository state.
- gates_or_invariants: The key invariant is that commands capable of mutating protected files must match the detector, while commands that only read or mention a protected path must not. Case-insensitivity is enforced by lowercasing before detection (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:80`). The implementation patterns being specified live in `hooks/lib/loop-common.sh:1214-1239`, including `>`, `>>`, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv|cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`. Runtime caller is `hooks/loop-bash-validator.sh`, which uses `command_modifies_file` to block RLCR `finalize-state.md` and `state.md` writes (`hooks/loop-bash-validator.sh:139-156`), plan backup writes (`hooks/loop-bash-validator.sh:336-340`), `goal-tracker.md` writes (`hooks/loop-bash-validator.sh:349-358`), prompt/summary/todos writes (`hooks/loop-bash-validator.sh:365-393`), and PR loop state/read-only file writes (`hooks/loop-bash-validator.sh:403-442`).
- edge_cases_or_failure_modes: The test explicitly documents a known limitation: `cp file1.md file2.md goal-tracker.md` is not detected because the implementation expects a simple `cp src dest` shape (`tests/test-bash-validator-patterns.sh:165-168`). Variable destinations such as `echo x > $FILE` are intentionally not detected because the detector matches literal target patterns (`tests/test-bash-validator-patterns.sh:170-172`). The shared helper’s simple regex model may also miss more complex shell constructs, quoted whitespace paths, process substitutions, or command substitutions unless separately handled by the full Bash validator.
- validation_or_tests: This file is itself the validation. I inspected it directly but did not execute it, per the research-only/no-modification instruction. Expected command is `bash tests/test-bash-validator-patterns.sh`; success requires `TESTS_FAILED=0`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-089 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the Stop-hook preflight that blocks RLCR exit while Claude has incomplete legacy TodoWrite items or new file-based Task items.
- algorithmic_behavior: Sets `TODO_CHECKER="$PROJECT_ROOT/hooks/check-todos-from-transcript.py"` (`tests/test-todo-checker.sh:12-15`) and drives it with hook-like JSON on stdin. Test Group 1 specifies input handling: invalid JSON exits `2`, empty input exits `0`, missing `transcript_path` exits `0`, and nonexistent transcript exits `0` (`tests/test-todo-checker.sh:46-99`). Test Group 2 specifies legacy TodoWrite behavior: all `completed` exits `0`; `pending`, `in_progress`, missing status, or empty-content pending todos exit `1`; output includes incomplete todo text (`tests/test-todo-checker.sh:100-159`, `tests/test-todo-checker.sh:250-279`). Test Group 3 covers transcript format variation: empty transcript exits `0`, invalid JSONL lines are ignored, multiple TodoWrite calls use the latest TodoWrite, direct `tool_use` entries work, and `type: message` format works (`tests/test-todo-checker.sh:160-241`). Test Group 5 specifies the new Task system from `tasks_base_dir/session_id`: missing `status` defaults pending, `completed` exits `0`, `in_progress` exits `1`, multiple tasks fail if any incomplete, all-completed succeeds, `deleted` is ignored, mixed Task and TodoWrite data is combined, Task output includes `Task #`, and nonexistent session directories exit `0` (`tests/test-todo-checker.sh:296-455`).
- inputs_outputs_state: Inputs are JSON stdin fields: `transcript_path`, `session_id`, and test-only `tasks_base_dir`. The checker reads JSONL transcript entries and JSON task files in a session directory. Outputs are exit codes and stdout; incomplete items produce an `INCOMPLETE_TODOS` marker plus formatted list entries (`hooks/check-todos-from-transcript.py:181-196`). The test creates only temporary files under `mktemp -d` and removes them with a trap (`tests/test-todo-checker.sh:37-39`).
- gates_or_invariants: Incomplete work must block Stop before expensive Codex review. In the implementation, invalid hook JSON exits `2` (`hooks/check-todos-from-transcript.py:150-161`), Task files are read from `~/.claude/tasks/<session_id>/` unless overridden (`hooks/check-todos-from-transcript.py:104-121`), Task statuses other than `completed` or `deleted` are incomplete (`hooks/check-todos-from-transcript.py:130-142`), and legacy TodoWrite considers only the latest nonempty TodoWrite list (`hooks/check-todos-from-transcript.py:67-88`). The RLCR Stop hook invokes this checker before Codex review and blocks on exit `1` or `2` (`hooks/loop-codex-stop-hook.sh:357-412`).
- dependencies_and_callers: Direct implementation dependency is Python 3 and the stdlib `json`, `sys`, and `pathlib` modules. Runtime caller is `hooks/loop-codex-stop-hook.sh`, which sets `TODO_CHECKER`, pipes the raw hook input into the Python script, and renders `prompt-template/block/incomplete-todos.md` on exit `1` (`hooks/loop-codex-stop-hook.sh:364-401`).
- edge_cases_or_failure_modes: Missing transcript/session data fails open by design to avoid blocking when no task source exists. Invalid JSONL transcript lines are ignored, which avoids noisy transcript corruption but can hide malformed incomplete TodoWrite entries. Malformed or unreadable Task JSON files are skipped (`hooks/check-todos-from-transcript.py:143-145`), so a broken task file could avoid blocking. Combining Task directory state with legacy TodoWrite means either source can block exit (`hooks/check-todos-from-transcript.py:163-175`).
- validation_or_tests: This file is the validation suite. I inspected it directly but did not execute it, per the research-only/no-modification instruction. Expected command is `bash tests/test-todo-checker.sh`; success requires all 25 tests to pass and process exit `0`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-119 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR summary-file gate. It defines the remediation prompt when Claude attempts to exit without writing the required round or finalize work summary.
- algorithmic_behavior: The template tells the agent it attempted to exit without a work summary and requires writing to `{{SUMMARY_FILE}}` (`prompt-template/block/work-summary-missing.md:1-8`). It specifies required summary content: implemented work, files created/modified, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:10-14`). The Stop hook selects `finalize-summary.md` in finalize phase or `round-${CURRENT_ROUND}-summary.md` otherwise (`hooks/loop-codex-stop-hook.sh:646-655`), checks existence, renders this template with `SUMMARY_FILE`, and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:657-681`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` template variable provided by `load_and_render_safe`. Output is Markdown text embedded as the Stop hook’s `reason` field. It does not mutate state itself; it controls the human/agent instruction returned by a state-machine gate.
- gates_or_invariants: The invariant is file existence, not content validation: if the expected summary file is missing, the Stop hook blocks exit; once the file exists, this specific gate passes and later gates/review can run. The template enforces the contract textually by requiring a summary before another exit attempt (`prompt-template/block/work-summary-missing.md:16`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:661-665`). Template loading depends on `hooks/lib/template-loader.sh`. If this template cannot be loaded, the Stop hook has an inline fallback beginning `# Work Summary Missing` (`hooks/loop-codex-stop-hook.sh:661-663`).
- edge_cases_or_failure_modes: Because the hook only checks for file existence, an empty or low-quality summary may pass this specific gate unless another validator catches it. If `{{SUMMARY_FILE}}` substitution fails, the block message can point at a literal placeholder rather than the real path. If the loop state has the wrong current round or phase, the Stop hook may require the wrong summary filename.
- validation_or_tests: Indirectly covered by Stop-hook/finalize tests in the suite. The exact template is referenced only through `loop-codex-stop-hook.sh`; I did not execute tests in this research pass.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-149 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared runtime monitor library for RLCR, PR-loop, and skill monitoring. It does not advance checklist/loop state directly, but it implements status detection, phase classification, progress parsing, and portable terminal/file helpers used by monitor commands.
- algorithmic_behavior: Provides ANSI color helpers (`scripts/lib/monitor-common.sh:12-23`), portable file size lookup via GNU/BSD `stat` fallback (`scripts/lib/monitor-common.sh:29-35`), latest timestamped session detection for `YYYY-MM-DD_HH-MM-SS` directories (`scripts/lib/monitor-common.sh:37-63`), terminal scroll-region setup/restore (`scripts/lib/monitor-common.sh:69-92`), active/terminal state file detection (`scripts/lib/monitor-common.sh:147-188`), simple YAML frontmatter extraction (`scripts/lib/monitor-common.sh:190-209`), timestamp formatting and truncation helpers (`scripts/lib/monitor-common.sh:215-252`), PR-loop phase detection (`scripts/lib/monitor-common.sh:254-342`), PR phase display text (`scripts/lib/monitor-common.sh:344-381`), RLCR-style goal tracker summary parsing (`scripts/lib/monitor-common.sh:383-463`), and PR goal tracker issue parsing (`scripts/lib/monitor-common.sh:465-498`).
- inputs_outputs_state: Inputs are filesystem paths: loop/session directories, state files, metadata files, PR check files, and goal trackers. Outputs are stdout strings such as `path|status`, `total_acs|completed_acs|...`, or phase names like `approved`, `codex_analyzing`, and `waiting_reviewer`. It writes a transient cache file under `/tmp/humanize-phase-${session_name}-${highest_round}.size` to detect whether `round-*-pr-check.md` is growing (`scripts/lib/monitor-common.sh:297-307`).
- gates_or_invariants: `monitor_find_latest_session` only considers one-level child directories whose basename matches the timestamp regex (`scripts/lib/monitor-common.sh:49-60`). `monitor_find_state_file` prioritizes `state.md` as `active`, then any `*-state.md` terminal file with the stop reason derived from the filename (`scripts/lib/monitor-common.sh:162-187`). `get_pr_loop_phase` checks terminal states first (`approve-state.md`, `cancel-state.md`, `maxiter-state.md`), then detects active Codex analysis by file growth or mtime younger than 10 seconds, then falls back to `state.md` frontmatter to distinguish initial review from reviewer polling (`scripts/lib/monitor-common.sh:272-341`). `parse_goal_tracker` returns a fixed pipe-delimited contract and clamps negative active task counts to zero (`scripts/lib/monitor-common.sh:390-463`).
- dependencies_and_callers: `scripts/humanize.sh` sources this library at startup (`scripts/humanize.sh:6-10`). PR monitor code uses `monitor_find_latest_session` (`scripts/humanize.sh:1187-1190`), `monitor_find_state_file` with PR-specific status remapping (`scripts/humanize.sh:1236-1253`), `get_pr_loop_phase` and display text (`scripts/humanize.sh:1321-1324`, `scripts/humanize.sh:1459-1461`), and `humanize_parse_pr_goal_tracker` for issue stats (`scripts/humanize.sh:1360-1368`). `scripts/lib/monitor-skill.sh` uses `monitor_get_yaml_value` and `monitor_format_timestamp` for skill invocation metadata. Tests in `tests/test-pr-loop-system.sh` cover phase detection, goal tracker parsing, PR goal tracker parsing, state file detection, latest session detection, file size, and Codex-analyzing detection (`tests/test-pr-loop-system.sh:256-416`, `tests/test-pr-loop-system.sh:770-899`).
- edge_cases_or_failure_modes: State-file terminal selection uses the first `find` result for `*-state.md`, so multiple terminal state files can produce nondeterministic status. YAML parsing is simple `grep`/`sed` over frontmatter and does not handle nested structures, multiline values, escaped quotes, or duplicate keys robustly (`scripts/lib/monitor-common.sh:203-208`). Phase detection relies on `/tmp` cache names based on session basename and round, so cache collisions are possible if different loop roots reuse the same session basename. File-growth detection can be affected by clock skew, mtime granularity, or platforms lacking the same `touch -d` behavior used by tests. Goal tracker parsers are format-sensitive to expected headings and Markdown table shapes.
- validation_or_tests: Covered primarily by `tests/test-pr-loop-system.sh`, including approved/waiting phase cases, goal tracker counts, PR issue statistics, active/terminal/no state detection, latest session selection, portable file size behavior, and `codex_analyzing` vs `waiting_reviewer` transition. I inspected references but did not execute tests in this research-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`