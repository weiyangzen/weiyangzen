# agent_13 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-013 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side prompt contracts for the RLCR review loop. The directory has four templates: `full-alignment-review.md`, `regular-review.md`, `goal-tracker-update-section.md`, and `code-review-phase.md`. These define review gates, completion tokens, goal-tracker authority, and audit output for Codex review phases.
- algorithmic_behavior: `regular-review.md` instructs Codex to read the original plan first, compare Claude’s claimed work to implementation reality, reject deferrals, audit goal alignment, and only end with `COMPLETE` when all planned tasks and acceptance criteria are fully done (`prompt-template/codex/regular-review.md:3`, `:20`, `:33`, `:49`). `full-alignment-review.md` adds interval-based full audits, explicit acceptance-criteria tables, forgotten/deferred item detection, historical round review, and a stagnation circuit breaker that ends with `STOP` (`prompt-template/codex/full-alignment-review.md:19`, `:30`, `:36`, `:59`, `:84`). `goal-tracker-update-section.md` gives Codex responsibility for approved goal-tracker edits while preserving the immutable section (`prompt-template/codex/goal-tracker-update-section.md:1`, `:6`, `:10`). `code-review-phase.md` documents the non-prompt `codex review` invocation, severity marker scan, and transition to finalization when no findings are present (`prompt-template/codex/code-review-phase.md:12`, `:14`, `:17`).
- inputs_outputs_state: Inputs are rendered placeholders including `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `CURRENT_ROUND`, `REVIEW_RESULT_FILE`, and full-alignment history paths (`prompt-template/codex/full-alignment-review.md:61`, `:63`; `prompt-template/codex/regular-review.md:5`, `:11`). Output is a review result file or audit prompt file. The state transition is text-driven: review comments keep the loop going, `STOP` stops due to stagnation, and `COMPLETE` moves the loop toward review/finalization.
- gates_or_invariants: Deferrals are treated as incomplete in both regular and full alignment reviews (`prompt-template/codex/regular-review.md:24`, `:53`; `prompt-template/codex/full-alignment-review.md:91`). Codex must read the plan first (`prompt-template/codex/regular-review.md:5`; `prompt-template/codex/full-alignment-review.md:7`). The immutable goal-tracker section must not be changed (`prompt-template/codex/goal-tracker-update-section.md:10`). Full alignment requires historical stagnation analysis and uses `STOP` as the circuit-breaker token (`prompt-template/codex/full-alignment-review.md:76`, `:84`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` renders `goal-tracker-update-section.md` into both review prompt types (`hooks/loop-codex-stop-hook.sh:808`, `:811`), chooses full vs regular based on `FULL_REVIEW_ROUND` (`hooks/loop-codex-stop-hook.sh:814`, `:821`), renders full alignment (`hooks/loop-codex-stop-hook.sh:857`, `:859`) or regular review (`hooks/loop-codex-stop-hook.sh:872`, `:875`), and writes the `code-review-phase.md` audit prompt before `codex review` (`hooks/loop-codex-stop-hook.sh:985`, `:998`).
- edge_cases_or_failure_modes: Template rendering has fallbacks in the hook, so a missing template degrades to a simpler review prompt rather than crashing immediately (`hooks/loop-codex-stop-hook.sh:835`, `:846`). Full-review interval values are validated and defaulted to 5 if invalid (`hooks/loop-codex-stop-hook.sh:816`, `:817`). Previous-round references are clamped to avoid negative file names (`hooks/loop-codex-stop-hook.sh:829`, `:831`). Completion remains vulnerable to review-output convention drift because downstream behavior depends on exact final tokens.
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders `codex/goal-tracker-update-section.md` with a sample goal tracker path (`tests/test-templates-comprehensive.sh:531`). `tests/test-template-references.sh` checks template references and common template existence patterns (`tests/test-template-references.sh:130`, `:149`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-043 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop ingestion script that fetches GitHub PR comments from all review surfaces and normalizes them into a markdown comment file consumed by PR-loop prompts.
- algorithmic_behavior: Parses `<pr_number> <output_file>` plus `--after` and `--bots` (`scripts/fetch-pr-comments.sh:27`, `:37`). Validates required arguments and numeric PR number (`scripts/fetch-pr-comments.sh:92`, `:103`). Resolves the correct base repository by checking the current repo first and then the parent repo for fork PRs (`scripts/fetch-pr-comments.sh:130`, `:139`, `:143`). Fetches issue comments, inline PR review comments, and PR reviews via `gh api --paginate`, using three retries per endpoint (`scripts/fetch-pr-comments.sh:184`, `:212`, `:216`, `:220`). Converts each response into a unified JSON shape, deduplicates by `id`, optionally filters by timestamp, sorts human comments before bot comments and newest first, then writes markdown sections (`scripts/fetch-pr-comments.sh:260`, `:280`, `:300`, `:322`, `:326`, `:334`, `:345`).
- inputs_outputs_state: Inputs are PR number, output file path, optional ISO timestamp, optional comma-separated bot list, current GitHub repository context, and GitHub API JSON. Outputs are a markdown file with fetch timestamp, repository, human comments, bot comments, warning footer if applicable, and `*No comments found.*` sentinel for zero comments (`scripts/fetch-pr-comments.sh:245`, `:348`, `:443`, `:445`). State is transient: a temp directory is created and removed by trap (`scripts/fetch-pr-comments.sh:168`, `:170`); no persistent state is written besides the requested output file.
- gates_or_invariants: Requires `gh` and `jq` before fetching (`scripts/fetch-pr-comments.sh:112`, `:117`). Fails when the PR cannot be found in current or parent repo (`scripts/fetch-pr-comments.sh:151`). Keeps API failures non-fatal but records `API_FAILURES`, writes empty arrays for failed endpoints, and appends an incomplete-data warning (`scripts/fetch-pr-comments.sh:199`, `:200`, `:201`, `:446`). Bot grouping maps `codex` to `chatgpt-codex-connector[bot]` and other bot names to `<name>[bot]` (`scripts/fetch-pr-comments.sh:386`, `:389`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` to build the initial `round-0-pr-comment.md`, grouped by active bots (`scripts/setup-pr-loop.sh:425`, `:430`). `setup-pr-loop.sh` later reads the `*No comments found.*` sentinel to decide whether to use no-comment or has-comment task templates (`scripts/setup-pr-loop.sh:723`, `:725`). The script depends on GitHub CLI authentication, `jq`, valid GitHub API schemas, and a writable output path.
- edge_cases_or_failure_modes: API failures do not fail the script, so a caller may proceed with incomplete review evidence (`scripts/fetch-pr-comments.sh:445`). Timestamp filtering compares raw `.created_at` strings to `--after`; this works for normalized UTC strings but does not normalize mixed offsets (`scripts/fetch-pr-comments.sh:327`). `unique_by(.id)` deduplicates across all comment types, so any cross-surface ID collision would drop one entry (`scripts/fetch-pr-comments.sh:322`). Entries with null `created_at` are filtered before sort, which can drop malformed API entries silently (`scripts/fetch-pr-comments.sh:337`). Output path parent validation is left to shell redirection under `set -e`.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help, missing PR, missing output file, and invalid PR number (`tests/test-pr-loop-scripts.sh:268`, `:279`, `:293`, `:307`). Fixture tests in `tests/test-pr-loop-hooks.sh` verify all comment types including approval-only reviews and `--after` filtering (`tests/test-pr-loop-hooks.sh:1242`, `:1287`). API robustness tests exercise empty arrays, bot parsing, severity markers, Unicode, and long bodies (`tests/robustness/test-pr-loop-api-robustness.sh:359`, `:442`, `:466`, `:518`, `:537`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-073 `file` `tests/test-pr-loop-1-scripts.sh`
- cursor: `[_]`
- core_role: Executable test shard for PR-loop script argument validation. It is a thin runner that isolates the “script tests” subset from the combined PR-loop suite.
- algorithmic_behavior: Computes `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared helpers and PR-loop library, initializes a test environment with mocked CLI tools, sources `tests/test-pr-loop-scripts.sh`, runs `run_script_tests`, prints a summary, and exits with the summary status (`tests/test-pr-loop-1-scripts.sh:11`, `:14`, `:17`, `:19`, `:21`, `:23`).
- inputs_outputs_state: Inputs are the repository checkout and sourced test modules. The runner writes only test output and temp test state created by `init_pr_loop_test_env`. That helper creates `TEST_DIR`, prepends a mock bin directory to `PATH`, and installs mock `gh` and `codex` (`tests/test-pr-loop-lib.sh:107`, `:112`, `:117`). Output is pass/fail lines plus a “PR Loop Script Tests” summary, with the process exit code indicating success or failure.
- gates_or_invariants: Uses `set -euo pipefail` (`tests/test-pr-loop-1-scripts.sh:9`). The shard’s gate is `TESTS_FAILED == 0` via `print_test_summary` (`tests/test-helpers.sh:58`, `:71`). The tests it runs enforce argument/usage behavior for `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh` (`tests/test-pr-loop-scripts.sh:5`, `:405`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and `tests/test-pr-loop-scripts.sh` (`tests/test-pr-loop-1-scripts.sh:14`, `:15`, `:19`). `run_script_tests` fans out to setup, cancel, fetch, and poll test groups (`tests/test-pr-loop-scripts.sh:405`, `:409`).
- edge_cases_or_failure_modes: This file is not self-contained; a failure in any sourced helper prevents the shard from running. Mock behavior is sufficient for argument-path tests but not a complete GitHub simulation. Several underlying tests intentionally accept later failures as long as the option parser did not reject the tested flag, so this shard is primarily parser/contract coverage rather than end-to-end workflow coverage.
- validation_or_tests: The assigned file is itself the validation entry point. Specific downstream checks include setup bot flags and `--max` validation (`tests/test-pr-loop-scripts.sh:38`, `:104`, `:117`), cancel state rename behavior (`tests/test-pr-loop-scripts.sh:211`, `:236`), fetch argument validation (`tests/test-pr-loop-scripts.sh:279`, `:293`, `:307`), and poll required-argument validation (`tests/test-pr-loop-scripts.sh:352`, `:366`, `:380`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-103 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Validator block message used when Bash attempts to modify `goal-tracker.md` in a context where proper Write/Edit tool validation is required.
- algorithmic_behavior: Emits a short block notice: do not use Bash commands to modify `goal-tracker.md`, use the provided `CORRECT_PATH`, and avoid `cat`, `echo`, `sed`, `awk`, etc. because they bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:1`, `:3`, `:5`, `:7`).
- inputs_outputs_state: Input is the rendered `{{CORRECT_PATH}}` placeholder. Output is the block text returned to the agent on stderr by the Bash validator. It does not directly mutate state.
- gates_or_invariants: Enforces the invariant that goal-tracker writes must pass through validated file-editing paths instead of raw shell writes. `hooks/loop-bash-validator.sh` detects commands modifying `goal-tracker.md`; in round 0 it emits this template and exits with code 2, while later rounds use a stricter “request through summary” block (`hooks/loop-bash-validator.sh:343`, `:349`, `:350`, `:352`, `:357`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` wraps this template in `goal_tracker_bash_blocked_message`, with a fallback if the template cannot be loaded (`hooks/lib/loop-common.sh:635`, `:637`, `:643`). The Bash validator calls that helper when `command_modifies_file` matches `goal-tracker.md` (`hooks/loop-bash-validator.sh:349`).
- edge_cases_or_failure_modes: Enforcement quality depends on `command_modifies_file` correctly detecting shell write forms. The repository has targeted pattern tests for redirections, `tee`, in-place editors, `mv`, `cp`, `rm`, `dd`, `truncate`, and fd redirection, but obscure shell constructs may still be parser-sensitive (`tests/test-bash-validator-patterns.sh:75`, `:89`, `:102`, `:116`, `:130`).
- validation_or_tests: `tests/test-template-references.sh` includes this block in the common template existence list (`tests/test-template-references.sh:152`, `:157`). `tests/robustness/test-hook-system-robustness.sh` checks that Bash modification after round 0 is blocked with exit 2, covering the same validator path family (`tests/robustness/test-hook-system-robustness.sh:353`, `:364`).
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-133 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Claude-facing escalation notice inserted into the next-round prompt when Codex review output contains open questions.
- algorithmic_behavior: The template instructs Claude to use `AskUserQuestion` to clarify Codex’s open questions with the user before resolving other findings (`prompt-template/claude/open-question-notice.md:1`). The insertion logic scans the Codex review result for a short line containing `Open Question` when `ASK_CODEX_QUESTION=true`, loads this template, and inserts it into the generated next-round prompt after the Codex review block (`hooks/loop-codex-stop-hook.sh:1579`, `:1581`, `:1583`, `:1592`, `:1596`, `:1605`).
- inputs_outputs_state: Input is Codex review text in `REVIEW_RESULT_FILE` and the `ASK_CODEX_QUESTION` flag. Output is additional prompt text in `NEXT_PROMPT_FILE`. It does not change loop state files directly, but it changes the next agent action contract by forcing user clarification first.
- gates_or_invariants: The gate is clarification-before-implementation for ambiguous Codex findings. The setup help describes the default as asking the user, with `--claude-answer-codex` disabling that behavior (`scripts/setup-rlcr-loop.sh:82`, `:87`, `:223`, `:224`).
- dependencies_and_callers: Called only through `hooks/loop-codex-stop-hook.sh` via `load_template "$TEMPLATE_DIR" "claude/open-question-notice.md"` with an inline fallback matching the template text (`hooks/loop-codex-stop-hook.sh:1592`, `:1594`).
- edge_cases_or_failure_modes: Detection is heuristic: the line must contain `Open Question` and be shorter than 40 characters (`hooks/loop-codex-stop-hook.sh:1580`, `:1584`). Longer headings, lowercase variants, or differently phrased question sections may not trigger the notice. If the expected insertion marker structure is absent, the `awk` insertion may not place the notice even after detection.
- validation_or_tests: Referenced by search in the stop hook and covered indirectly by prompt-generation tests around next-round content. No dedicated single-template test was found for this file in the assigned reads.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-163 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR plan-file validation, mostly exercising production validation in `scripts/setup-rlcr-loop.sh` plus local file-shape helpers for edge cases.
- algorithmic_behavior: Creates an isolated temp repo, sets `XDG_CACHE_HOME`, installs a mock `codex`, initializes git, and writes a `.gitignore` that keeps test artifacts ignored (`tests/robustness/test-plan-file-robustness.sh:18`, `:22`, `:27`, `:39`, `:112`, `:119`). `test_plan_validation` runs the production setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"`, captures output, maps known plan-validation errors to rejection, and treats loop creation, `codex` prerequisite legacy output, or gitignore tracking errors as validation success signals (`tests/robustness/test-plan-file-robustness.sh:46`, `:61`, `:65`, `:70`, `:76`, `:82`, `:88`). The local `count_content_lines` mirrors production comment/blank-line filtering (`tests/robustness/test-plan-file-robustness.sh:129`, `:135`, `:149`, `:154`, `:160`).
- inputs_outputs_state: Inputs are generated plan files and the production `setup-rlcr-loop.sh`. Outputs are pass/fail lines and final summary (`tests/robustness/test-plan-file-robustness.sh:557`). State is limited to the temp repo and its `.humanize/rlcr` loop directory, which is cleared before each production validation run (`tests/robustness/test-plan-file-robustness.sh:56`).
- gates_or_invariants: The production gate rejects absolute paths, spaces, shell metacharacters, symlink plan files, symlink parent directories, missing/unreadable files, paths outside the project, submodule paths, invalid tracking status, fewer than 5 total lines, and fewer than 3 meaningful content lines (`scripts/setup-rlcr-loop.sh:389`, `:395`, `:403`, `:415`, `:428`, `:451`, `:457`, `:463`, `:477`, `:516`, `:552`, `:600`). This branch also adds an upfront dependency check for `codex`, `jq`, and `git` with one aggregated error before loop creation (`scripts/setup-rlcr-loop.sh:258`, `:263`, `:277`).
- dependencies_and_callers: Depends on Bash, git, real `jq`, production `setup-rlcr-loop.sh`, and `tests/test-helpers.sh` (`tests/robustness/test-plan-file-robustness.sh:13`, `:108`). It mocks only `codex`, so the branch-level dependency check still requires `jq` and `git` to exist in the environment (`scripts/setup-rlcr-loop.sh:269`, `:273`).
- edge_cases_or_failure_modes: Positive production cases cover a valid plan, exactly 3 content lines, standard-size files, rich markdown, 1MB-plus files, and mixed line endings (`tests/robustness/test-plan-file-robustness.sh:178`, `:206`, `:234`, `:253`, `:325`, `:355`). Negative or edge cases include empty files, comments-only files, binary/null content, long lines, shell-special text, whitespace-only plans, complex comments, nonexistent files, unreadable permissions, symlinks, directories, and disappearing plan files (`tests/robustness/test-plan-file-robustness.sh:300`, `:309`, `:368`, `:390`, `:409`, `:432`, `:444`, `:468`, `:477`, `:491`, `:503`, `:514`, `:527`). Some later tests are local checks rather than production setup calls, so they verify file handling assumptions more than full setup behavior.
- validation_or_tests: This file is the validation suite. Its strongest production-backed assertions are tests 1-8 and 19, because they call `test_plan_validation`; tests 9-18 mostly validate local counting/readability/detection assumptions. Related setup robustness tests specifically cover the new missing-dependency behavior (`tests/robustness/test-setup-scripts-robustness.sh:1103`, `:1132`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 unique item IDs: CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-013, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-043, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-073, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-103, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-133, CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-163
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`