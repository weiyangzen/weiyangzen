# agent_17 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-017 `directory` `skills/ask-codex`
- cursor: `[_]`
- core_role: Skill definition for a one-shot Codex consultation command. The directory recursively contains only `skills/ask-codex/SKILL.md`, which exposes the command contract and delegates all runtime behavior to `scripts/ask-codex.sh`; see `skills/ask-codex/SKILL.md:1-5` and `skills/ask-codex/SKILL.md:14-18`.
- algorithmic_behavior: The skill tells the caller to run `"${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh" $ARGUMENTS`, then consume stdout as Codex's answer and stderr as status/debug information. The delegated script parses `--codex-model MODEL:EFFORT`, `--codex-timeout SECONDS`, validates the Codex CLI and non-empty question, validates model/effort character sets, detects the git project root, persists input/output/metadata, builds `codex exec` args, runs through `run_with_timeout`, and maps timeout/nonzero/empty-output cases to explicit failures; see `skills/ask-codex/SKILL.md:20-33`, `scripts/ask-codex.sh:89-147`, `scripts/ask-codex.sh:153-186`, `scripts/ask-codex.sh:192-218`, `scripts/ask-codex.sh:243-255`, `scripts/ask-codex.sh:295-329`, and `scripts/ask-codex.sh:332-410`.
- inputs_outputs_state: Inputs are skill arguments, optional `--codex-model`, optional `--codex-timeout`, `HUMANIZE_CODEX_BYPASS_SANDBOX`, and the current git/project directory. Outputs are Codex stdout to the caller, status on stderr, project-local `.humanize/skill/<unique-id>/{input.md,output.md,metadata.md}`, and cache files under `~/.cache/humanize/<sanitized-path>/skill-<unique-id>/codex-run.{cmd,out,log}` with fallback to `.humanize/skill/<unique-id>/cache`; see `skills/ask-codex/SKILL.md:22-24`, `skills/ask-codex/SKILL.md:37-38`, `scripts/ask-codex.sh:15-17`, `scripts/ask-codex.sh:202-218`, `scripts/ask-codex.sh:224-237`, `scripts/ask-codex.sh:261-275`, and `scripts/ask-codex.sh:387-410`.
- gates_or_invariants: Requires `codex` in `PATH`, a non-empty question, positive-integer timeout, model limited to `[a-zA-Z0-9._-]+`, effort limited to `[a-zA-Z0-9_-]+`, and non-empty Codex stdout on success. It defaults to `--full-auto`; only `HUMANIZE_CODEX_BYPASS_SANDBOX=true|1` changes the automation flag to the dangerous bypass mode; see `scripts/ask-codex.sh:120-135`, `scripts/ask-codex.sh:153-186`, `scripts/ask-codex.sh:249-255`, and `scripts/ask-codex.sh:358-380`.
- dependencies_and_callers: The skill depends on the shell script via `allowed-tools` and the usage block (`skills/ask-codex/SKILL.md:5`, `skills/ask-codex/SKILL.md:14-18`). The script sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` for timeout handling and default Codex model/effort (`scripts/ask-codex.sh:26-33`). Tests include `tests/test-ask-codex.sh`, and the mock helper in `tests/test-pr-loop-lib.sh` provides a trivial `codex` mock for PR-loop tests (`tests/test-pr-loop-lib.sh:90-100`).
- edge_cases_or_failure_modes: Missing script assumptions are surfaced as script failures, not handled in the skill file itself. Script failure modes include missing `codex`, missing question, invalid flags, invalid model/effort, unwritable home cache fallback, timeout exit `124`, nonzero Codex exit with stderr tail, and empty stdout reclassified to exit `1`; see `skills/ask-codex/SKILL.md:26-33`, `scripts/ask-codex.sh:153-186`, `scripts/ask-codex.sh:214-218`, `scripts/ask-codex.sh:308-355`, and `scripts/ask-codex.sh:358-380`.
- validation_or_tests: The skill metadata documents expected exit handling (`skills/ask-codex/SKILL.md:26-33`). Runtime behavior is covered by `tests/test-ask-codex.sh` and indirectly by PR-loop mocks. The directory itself has no executable test; it is a declarative skill wrapper around `scripts/ask-codex.sh`.
- skip_candidate: `yes: the assigned directory is not itself a core algorithm implementation; it is a thin skill descriptor. It remains relevant because it is the public routing surface for the one-shot Codex consultation workflow.`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-047 `file` `scripts/rlcr-stop-gate.sh`
- cursor: `[_]`
- core_role: Runtime wrapper that lets non-hook skill workflows reuse the same RLCR stop-hook enforcement as native Claude Code hooks. Its comment explicitly says it wraps `hooks/loop-codex-stop-hook.sh` so skills use identical enforcement and phase transitions; see `scripts/rlcr-stop-gate.sh:3-11`.
- algorithmic_behavior: Parses `--session-id`, `--transcript-path`, `--project-root`, `--json`, and help; resolves `HUMANIZE_ROOT`, default `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, and the hook path; validates the hook is executable and `jq` is available; builds a Stop hook JSON payload with `hook_event_name`, `stop_hook_active:false`, `cwd`, and optional session/transcript fields; pipes that payload into `loop-codex-stop-hook.sh` with `CLAUDE_PROJECT_DIR` set; then maps hook output to allow/block/runtime error; see `scripts/rlcr-stop-gate.sh:19-27`, `scripts/rlcr-stop-gate.sh:41-82`, `scripts/rlcr-stop-gate.sh:84-103`, and `scripts/rlcr-stop-gate.sh:104-138`.
- inputs_outputs_state: Inputs are CLI flags and `CLAUDE_SESSION_ID`, `CLAUDE_TRANSCRIPT_PATH`, `CLAUDE_PROJECT_DIR`. Output is `ALLOW: stop gate passed.` plus exit `0` when the hook emits no JSON, `BLOCK:` plus reason or raw JSON plus exit `10` when hook decision is `block`, and stderr plus exit `20` for wrapper/runtime errors. It does not own state files directly; state transitions occur in the underlying hook under `.humanize/rlcr`; see `scripts/rlcr-stop-gate.sh:24-27`, `scripts/rlcr-stop-gate.sh:110-134`, and `scripts/rlcr-stop-gate.sh:136-138`.
- gates_or_invariants: Fail-closed for missing/non-executable hook, missing `jq`, missing option values, unknown options, hook nonzero exit, hook non-JSON output, and unexpected hook decisions. It treats empty hook output as an allow state, matching the hook contract where no active loop allows exit; see `scripts/rlcr-stop-gate.sh:41-82`, `scripts/rlcr-stop-gate.sh:104-120`, and `scripts/rlcr-stop-gate.sh:122-138`.
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, `jq`, and project RLCR state. The `humanize-rlcr` skill requires this wrapper every round and maps exits `0`, `10`, and `20` to done, continue, and infrastructure error; see `skills/humanize-rlcr/SKILL.md:9-17` and `skills/humanize-rlcr/SKILL.md:50-63`. The broader `humanize` skill also instructs skill mode to run it for hook-equivalent transitions (`skills/humanize/SKILL.md:41-47`, `skills/humanize/SKILL.md:81-84`). The wrapped hook locates active loops by session id, validates schema, branch state, summaries, git cleanliness, large files, review phase, and other gates; initial hook routing is at `hooks/loop-codex-stop-hook.sh:42-66`.
- edge_cases_or_failure_modes: `--project-root` allows checking a target repo from outside that repo; default root must be caller cwd, not plugin install dir. Session/transcript fields are omitted when empty via `jq select(length > 0)`. Hook nonzero exits are normalized to wrapper exit `20`, so skill workflows do not accidentally treat raw hook errors as loop decisions; see `scripts/rlcr-stop-gate.sh:87-107`.
- validation_or_tests: `tests/test-stop-gate.sh` builds minimal active loop fixtures and verifies default cwd root blocks with exit `10`, `--project-root` blocks the target repo from another cwd, and no active loop exits `0` with `ALLOW:`; see `tests/test-stop-gate.sh:18-59`, `tests/test-stop-gate.sh:64-89`, `tests/test-stop-gate.sh:91-116`, and `tests/test-stop-gate.sh:118-144`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-077 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared test fixture library for PR-loop tests. It is not the PR-loop runtime algorithm, but it defines common mocks and environment setup used by the executable PR-loop specifications; see `tests/test-pr-loop-lib.sh:3-8`.
- algorithmic_behavior: Uses a source guard (`TEST_PR_LOOP_LIB_LOADED`) to avoid duplicate initialization, derives `SCRIPT_DIR` and `PROJECT_ROOT`, sources `test-helpers.sh` when needed, defines `create_mock_gh`, `create_mock_codex`, `init_pr_loop_test_env`, and a PR-loop-specific `print_test_summary`; see `tests/test-pr-loop-lib.sh:10-21`, `tests/test-pr-loop-lib.sh:27-88`, `tests/test-pr-loop-lib.sh:90-121`, and `tests/test-pr-loop-lib.sh:127-144`.
- inputs_outputs_state: Inputs are the target mock directory and global test variables from `test-helpers.sh`. Outputs are executable mock binaries in `$TEST_DIR/mock_bin`, `PATH` prepended with that mock directory, and exported `MOCK_BIN_DIR`. The mock `gh` emits deterministic auth, repo owner/name, PR number/state/commits, user, and empty API arrays; the mock `codex` emits `Mock codex output`; see `tests/test-pr-loop-lib.sh:28-88`, `tests/test-pr-loop-lib.sh:91-100`, and `tests/test-pr-loop-lib.sh:107-121`.
- gates_or_invariants: Requires `setup_test_dir` to be available or sourceable from `tests/test-helpers.sh`. Mock `gh` intentionally exits `1` on unhandled commands, which makes tests fail when runtime scripts call unexpected GitHub CLI shapes; see `tests/test-pr-loop-lib.sh:18-21` and `tests/test-pr-loop-lib.sh:82-85`.
- dependencies_and_callers: Directly sourced by the combined runner `tests/test-pr-loop.sh:18-34` and split runners `tests/test-pr-loop-1-scripts.sh:14-23`, `tests/test-pr-loop-2-hooks.sh:14-23`, and `tests/test-pr-loop-3-stophook.sh:15-24`. Depends on `tests/test-helpers.sh` counters and temp-dir setup (`tests/test-helpers.sh:22-32`, `tests/test-helpers.sh:58-89`).
- edge_cases_or_failure_modes: The source guard prevents repeated definitions in multi-module runners. The mock `gh` covers only the command patterns the tests expect, so newly added runtime calls must either match those patterns or update the fixture. The PR-loop-specific `print_test_summary` ignores an optional title argument unlike the generic helper, so split runners passing a title still receive the fixed `"PR Loop Tests"` heading; see `tests/test-pr-loop-lib.sh:127-144`.
- validation_or_tests: This file is support code rather than a test body. Its effectiveness is validated by PR-loop runners that initialize it before sourcing and running script, hook, and stop-hook tests; see `tests/test-pr-loop.sh:26-47` and the split runner references above.
- skip_candidate: `yes: it is a test harness dependency, not executable core algorithm behavior. It should stay in coverage because PR-loop specs rely on its mocks and shared state setup.`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-107 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR stop hook’s large-file gate. It defines the remediation contract when staged/tracked/new files exceed the hook’s line-count limit; see `prompt-template/block/large-files.md:1-5`.
- algorithmic_behavior: The stop hook sets `MAX_LINES=2000`, iterates cached git status entries, resolves rename destinations, skips deleted paths, classifies file extensions as code or documentation, counts lines, accumulates files over the threshold, renders this template with `MAX_LINES` and `LARGE_FILES`, and emits a JSON block decision; see `hooks/loop-codex-stop-hook.sh:466-499`, `hooks/loop-codex-stop-hook.sh:500-523`, and `hooks/loop-codex-stop-hook.sh:525-545`.
- inputs_outputs_state: Inputs are template variables `{{MAX_LINES}}` and `{{LARGE_FILES}}`. Output becomes the hook JSON `reason` shown to the agent/user, with system message `Loop: Blocked - large files detected (>... lines), please split into smaller modules`; see `prompt-template/block/large-files.md:3-4` and `hooks/loop-codex-stop-hook.sh:533-544`.
- gates_or_invariants: For code files, requires splitting into modules each below the limit while preserving functionality strictly unchanged, optionally using the `code-simplifier` plugin, and maintaining clear boundaries. For documentation files, requires splitting into logical sections, preserving cross-references and narrative flow, and updating TOCs/navigation; see `prompt-template/block/large-files.md:11-25`.
- dependencies_and_callers: Rendered only through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` in `hooks/loop-codex-stop-hook.sh:533-535`. Template reference validation scans shell template references and fails missing templates, which covers this path class; see `tests/test-template-references.sh:56-107`.
- edge_cases_or_failure_modes: Unknown file extensions are ignored by the large-file gate (`hooks/loop-codex-stop-hook.sh:500-510`). Renames are checked against the new filename (`hooks/loop-codex-stop-hook.sh:486-489`). If the template is missing or invalid, the hook has an inline fallback at `hooks/loop-codex-stop-hook.sh:525-532`.
- validation_or_tests: Validated structurally by template reference tests and behaviorally by stop-hook tests that exercise blocking templates. The hook’s fallback protects against missing template files, but the repository’s template-reference test is the primary existence guard.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-137 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit template for the RLCR code review phase. It documents the `codex review` invocation and the phase transition rules rather than supplying prompt input to Codex; see `prompt-template/codex/code-review-phase.md:1-5`.
- algorithmic_behavior: The stop hook enters review handling after implementation completion, computes review base as `BASE_COMMIT` when present or `BASE_BRANCH` otherwise, writes this template to `round-<N>-review-prompt.md` for audit, records the command file, runs `codex review --base <review_base>`, then scans the combined log for `[P0-9]` markers; see `hooks/loop-codex-stop-hook.sh:966-1004`, `hooks/loop-codex-stop-hook.sh:1008-1033`, and `hooks/loop-codex-stop-hook.sh:1038-1077`.
- inputs_outputs_state: Inputs are `{{BASE_BRANCH}}`, `{{REVIEW_ROUND}}`, and `{{TIMESTAMP}}` as used by the template; the call site also passes `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE`, but this template version does not render them. Outputs listed by the template are `round-{{REVIEW_ROUND}}-review-prompt.md`, `round-{{REVIEW_ROUND}}-review-result.md`, `round-{{REVIEW_ROUND}}-codex-review.cmd`, `round-{{REVIEW_ROUND}}-codex-review.out`, and `round-{{REVIEW_ROUND}}-codex-review.log`; see `prompt-template/codex/code-review-phase.md:6-18` and `prompt-template/codex/code-review-phase.md:30-36`.
- gates_or_invariants: The phase gate is severity-marker driven: issues in `[P0-9]` format cause a fix prompt and continued review; no issues transition to Finalize Phase. Implementation scans only the last 50 log lines and only markers in the first 10 characters of a line to avoid false positives and large-log costs; see `prompt-template/codex/code-review-phase.md:12-18`, `hooks/lib/loop-common.sh:475-486`, `hooks/lib/loop-common.sh:502-535`, and `hooks/loop-codex-stop-hook.sh:1061-1077`.
- dependencies_and_callers: Rendered by `run_codex_code_review` in `hooks/loop-codex-stop-hook.sh:985-1004`. Depends on `codex review`, `run_with_timeout`, `detect_review_issues`, and downstream transition helpers `continue_review_loop_with_issues`, `enter_finalize_phase`, and `block_review_failure`; see `hooks/loop-codex-stop-hook.sh:1023-1033`, `hooks/loop-codex-stop-hook.sh:1049-1077`, and `hooks/loop-codex-stop-hook.sh:1166-1210`.
- edge_cases_or_failure_modes: The template states `codex review` does not accept prompt input, preventing an incorrect prompt-piping implementation. If `codex review` fails or produces no usable log output, the hook blocks rather than skipping review; see `hooks/loop-codex-stop-hook.sh:1055-1069` and `hooks/loop-codex-stop-hook.sh:1213-1229`. Template drift risk: call-site variables include base commit/review base, but this template only displays base branch.
- validation_or_tests: `tests/test-codex-review-merge.sh` exercises `[P0-9]` detection, last-50-line scanning, first-10-character filtering, missing/empty log hard errors, extraction to result file, and multi-issue extraction. Template existence is checked by `tests/test-template-references.sh:56-107`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-167 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for concurrent RLCR session discovery, focused on `find_active_loop` in `hooks/lib/loop-common.sh`; see `tests/robustness/test-session-robustness.sh:3-10`, `tests/robustness/test-session-robustness.sh:14-20`, and `hooks/lib/loop-common.sh:230-248`.
- algorithmic_behavior: The test creates temporary `.humanize/rlcr`-like directory layouts, invokes `find_active_loop`, compares returned paths or empty output, records pass/fail counts through `test-helpers.sh`, prints a summary, and exits nonzero on failure; see `tests/robustness/test-session-robustness.sh:19-24`, repeated assertions at `tests/robustness/test-session-robustness.sh:33-118`, negative/edge assertions at `tests/robustness/test-session-robustness.sh:128-317`, and summary at `tests/robustness/test-session-robustness.sh:318-323`.
- inputs_outputs_state: Inputs are synthetic directories under `$TEST_DIR/rlcr` with combinations of `state.md`, `finalize-state.md`, `complete-state.md`, `cancel-state.md`, symlinks, spaces, non-standard names, and nested paths. Outputs are PASS/FAIL lines and a final process exit from `print_test_summary`; see `tests/robustness/test-session-robustness.sh:35-45`, `tests/robustness/test-session-robustness.sh:51-60`, and `tests/robustness/test-session-robustness.sh:259-317`.
- gates_or_invariants: Specifies that no-filter lookup checks only the lexicographically newest immediate child directory; returns it only if it has `state.md` or `finalize-state.md`; ignores older stale active sessions when the newest dir is inactive, complete, or canceled; handles many sessions in under 1000 ms; ignores deeply nested state files; see `tests/robustness/test-session-robustness.sh:63-87`, `tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:224-238`, and `tests/robustness/test-session-robustness.sh:287-302`. The implementation matches this no-filter invariant at `hooks/lib/loop-common.sh:257-271`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-session-robustness.sh:14-17`). `find_active_loop` is used by stop/read/write/edit/bash/plan validators and setup/cancel scripts, including `hooks/loop-codex-stop-hook.sh:58-66`, so these tests protect hook routing and stale-loop safety.
- edge_cases_or_failure_modes: Covers empty/nonexistent base dirs, base dirs with files but no subdirs, dirs with no active state, unexpected naming, symlinked dirs, spaces in names, deep nesting, rapid creation, terminal-only sessions, mixed terminal/newest and active/older sessions, and graceful handling of a unicode-labeled test case that actually creates `session-test`; see `tests/robustness/test-session-robustness.sh:128-317`.
- validation_or_tests: This file is itself the validation artifact. Related session-id behavior with filters is separately tested in `tests/test-session-id.sh`; this assigned file primarily verifies no-filter/zombie-loop behavior and broad filesystem edge cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 section headings above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`