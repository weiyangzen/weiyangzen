# agent_24 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-024 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow contract for cancelling an active RLCR loop. It is not the implementation itself; it defines the assistant-visible decision tree and delegates all state mutation to `scripts/cancel-rlcr-loop.sh` (`commands/cancel-rlcr-loop.md:11-15`, `commands/cancel-rlcr-loop.md:36`).
- algorithmic_behavior: The command runs the cancel script, branches only on the first output token, reports no-loop/cancelled results, and requests explicit user confirmation before forcing cancellation during Finalize Phase (`commands/cancel-rlcr-loop.md:17-34`). The key state model is that active means `state.md` or `finalize-state.md` exists in the newest RLCR loop directory (`commands/cancel-rlcr-loop.md:36`).
- inputs_outputs_state: Input is the script status line: `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:17-22`). Output is user-facing narration plus optional `AskUserQuestion`. State transition is implemented by the script: normal `state.md` or `finalize-state.md` becomes `cancel-state.md` after a `.cancel-requested` signal is created (`scripts/cancel-rlcr-loop.sh:148-155`).
- gates_or_invariants: The allowed tools restrict execution to the cancel script, force variant, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:3`). Finalize cancellation is gated by user confirmation unless `--force` is used (`commands/cancel-rlcr-loop.md:23-34`; `scripts/cancel-rlcr-loop.sh:131-142`). The script intentionally cancels globally within the project, not by `session_id`, because slash-command invocation lacks hook JSON context (`scripts/cancel-rlcr-loop.sh:76-87`).
- dependencies_and_callers: Depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh` (`commands/cancel-rlcr-loop.md:13-15`). That script sources `hooks/lib/loop-common.sh` and calls `find_active_loop` (`scripts/cancel-rlcr-loop.sh:72-87`). `find_active_loop` checks the newest RLCR directory and considers `state.md` or `finalize-state.md` active, preserving zombie-loop protection by not reviving older directories (`hooks/lib/loop-common.sh:230-306`).
- edge_cases_or_failure_modes: No active base dir or newest active loop produces no-loop output (`scripts/cancel-rlcr-loop.sh:89-93`, `scripts/cancel-rlcr-loop.sh:103-113`). Finalize without force exits with confirmation-required status and preserves the loop (`scripts/cancel-rlcr-loop.sh:131-142`). Missing `current_round` or `max_iterations` only degrades display to `?`, not cancellation (`scripts/cancel-rlcr-loop.sh:119-125`). Unknown script options exit as error (`scripts/cancel-rlcr-loop.sh:57-61`).
- validation_or_tests: Related tests cover finalize handling and cancellation transitions, including finalize detection and `cancel-state.md` behavior in `tests/test-finalize-phase.sh` and cancel-signal/security tests referenced by search hits. The assigned command file itself is a workflow definition; its executable behavior is validated through the script and loop-common tests rather than through markdown parsing.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-054 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Test fixture generator for PR-loop monitor behavior. It creates deterministic `.humanize/pr-loop/<timestamp>/state.md` files that exercise monitor parsing of `configured_bots` and `active_bots` YAML-list state (`tests/setup-monitor-test-env.sh:3-8`).
- algorithmic_behavior: Accepts `<test_dir> <test_name>`, dispatches by `test_name`, creates one timestamped PR-loop session, writes frontmatter-only state, and echoes the test directory (`tests/setup-monitor-test-env.sh:12-20`, `tests/setup-monitor-test-env.sh:91`). The three fixture modes model: both bots active, partial approval where only `codex` remains active, and empty `active_bots` meaning all approved (`tests/setup-monitor-test-env.sh:21-83`).
- inputs_outputs_state: Inputs are `TEST_DIR` and optional `TEST_NAME` defaulting to `default`; missing test dir exits with usage (`tests/setup-monitor-test-env.sh:12-18`). Output is a filesystem fixture under `$TEST_DIR/.humanize/pr-loop/.../state.md` plus stdout of `$TEST_DIR` (`tests/setup-monitor-test-env.sh:24-41`, `tests/setup-monitor-test-env.sh:46-62`, `tests/setup-monitor-test-env.sh:67-82`, `tests/setup-monitor-test-env.sh:91`).
- gates_or_invariants: `set -euo pipefail` makes fixture creation fail fast (`tests/setup-monitor-test-env.sh:10`). Unknown fixture names are rejected with a finite allowed set: `yaml_list`, `configured`, `empty` (`tests/setup-monitor-test-env.sh:84-88`). The fixture schema uses YAML list indentation expected by monitor parsing.
- dependencies_and_callers: Mirrors state emitted by `scripts/setup-pr-loop.sh`, where `configured_bots` is initialized from `active_bots` and written as YAML lists (`scripts/setup-pr-loop.sh:608-624`). Consumed by monitor code in `scripts/humanize.sh`, which parses `configured_bots` and `active_bots` by scanning YAML-list entries under those keys (`scripts/humanize.sh:1255-1284`) and displays `all approved` when `active_bots` is empty or `none` (`scripts/humanize.sh:1352-1358`).
- edge_cases_or_failure_modes: The `empty` fixture writes `active_bots:` with no list entries, which should parse to `none` via monitor defaults (`tests/setup-monitor-test-env.sh:77-80`; `scripts/humanize.sh:1278-1279`). It does not create `goal-tracker.md` or output files, so tests using it must tolerate missing tracker content. It does not initialize a git repo; it is strictly a monitor-state fixture.
- validation_or_tests: This script is itself executable test setup for monitor tests. Broader monitor and PR-loop tests reference the same state layout, including `humanize monitor pr --once` scenarios in `tests/test-pr-loop-system.sh` and e2e monitor tests found by repository search.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-084 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR terminal state naming and active-loop detection. It verifies that only active state filenames keep a loop live, and that `end_loop` emits reason-specific terminal state files (`tests/test-state-exit-naming.sh:3-10`).
- algorithmic_behavior: Creates an isolated temp git repo and loop directories, sources `hooks/lib/loop-common.sh`, then asserts behavior of `find_active_loop`, `end_loop`, and loop path classification (`tests/test-state-exit-naming.sh:31-68`). It increments local pass/fail counters and exits with the failed-test count (`tests/test-state-exit-naming.sh:23-29`, `tests/test-state-exit-naming.sh:293-302`).
- inputs_outputs_state: Inputs are generated fixture state files under `$TEST_DIR/.humanize/rlcr/<timestamp>/`. Outputs are PASS/FAIL/SKIP logs and process exit status. State transitions under test include `state.md` active, terminal `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` inactive, and `end_loop` moving `state.md` to `${reason}-state.md` (`tests/test-state-exit-naming.sh:38-180`, `tests/test-state-exit-naming.sh:186-243`).
- gates_or_invariants: The critical invariant is that `find_active_loop` must not treat terminal state files as active; only `state.md` is asserted active in this test (`tests/test-state-exit-naming.sh:38-160`). Newer active directories must take precedence (`tests/test-state-exit-naming.sh:161-180`). `end_loop` must reject invalid reasons and missing state files (`tests/test-state-exit-naming.sh:186-204`, `tests/test-state-exit-naming.sh:232-243`). New `.humanize/rlcr` path is accepted, while legacy `.humanize-loop.local` must not be matched or searched (`tests/test-state-exit-naming.sh:245-291`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` (`tests/test-state-exit-naming.sh:66-68`). Relevant implementation: `find_active_loop` checks the newest directory and returns it only if an active state exists (`hooks/lib/loop-common.sh:248-271`), while `end_loop` permits only `complete|cancel|maxiter|stop|unexpected` and renames the active state to `${reason}-state.md` (`hooks/lib/loop-common.sh:1255-1288`).
- edge_cases_or_failure_modes: Because the test is `set -uo pipefail` rather than global `set -e`, it explicitly toggles `set +e`/`set -e` around expected failures (`tests/test-state-exit-naming.sh:13`, `tests/test-state-exit-naming.sh:196-203`, `tests/test-state-exit-naming.sh:235-242`). It assumes lexicographic timestamp ordering for "newer" directories. It verifies legacy directories cannot revive loops even if they contain `state.md` (`tests/test-state-exit-naming.sh:267-291`).
- validation_or_tests: This is a direct validation script. It covers active detection, terminal-state exclusion, valid and invalid `end_loop` reasons, missing state file handling, and path migration away from `.humanize-loop.local`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-114 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR state schema migration failures. It explains that a state file is missing a required field and directs the user to cancel/update/restart (`prompt-template/block/schema-outdated.md:1-12`).
- algorithmic_behavior: Template variable `{{FIELD_NAME}}` is rendered into the missing-field message (`prompt-template/block/schema-outdated.md:3`). The transition instruction is explicit: terminate the session as `unexpected` to preserve state information (`prompt-template/block/schema-outdated.md:12`).
- inputs_outputs_state: Input is `FIELD_NAME` supplied by callers. Output is markdown used as a hook block reason. In plan-file validation, the rendered message is JSON-escaped and returned as a `"decision": "block"` payload (`hooks/loop-plan-file-validator.sh:59-76`).
- gates_or_invariants: The invariant is strict schema compatibility: missing required fields are not silently defaulted for protected operations. `loop-plan-file-validator.sh` checks `plan_tracked` and `start_branch`, renders this template for missing values, and blocks (`hooks/loop-plan-file-validator.sh:79-86`). Strict parser validation also rejects missing critical fields such as `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:392-438`).
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/loop-plan-file-validator.sh` with inline fallback text (`hooks/loop-plan-file-validator.sh:59-65`). Search also shows schema-outdated behavior referenced by `hooks/loop-codex-stop-hook.sh` for missing RLCR fields and by robustness tests around missing required fields.
- edge_cases_or_failure_modes: If the template cannot be loaded, callers have inline fallback text (`hooks/loop-plan-file-validator.sh:62-65`). If a session started with an older plugin version lacks fields, the user-facing action is cancellation plus plugin update/restart, while the loop is preserved through terminal unexpected state rather than corrupting state in place (`prompt-template/block/schema-outdated.md:5-12`).
- validation_or_tests: Related tests include `tests/test-plan-file-hooks.sh` missing-schema checks and robustness tests for missing required fields found in `tests/robustness/test-state-file-robustness.sh` and `tests/robustness/test-hook-system-robustness.sh`. The template is also tied to `unexpected-state.md` coverage in `tests/test-state-exit-naming.sh`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-144 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 prompt block for the no-existing-comments path. It defines the completion contract before the stop hook begins polling review bots (`prompt-template/pr-loop/critical-requirements-no-comments.md:2-20`).
- algorithmic_behavior: Instructs the worker to write a resolution summary to `{{RESOLVE_PATH}}`, state that Round 0 is awaiting initial bot reviews, note no issues yet, then attempt exit so the Stop Hook polls bots (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-20`). It explicitly forbids commenting on the PR to trigger review (`prompt-template/pr-loop/critical-requirements-no-comments.md:17-18`).
- inputs_outputs_state: Input is `RESOLVE_PATH` rendered by setup. `scripts/setup-pr-loop.sh` computes `RESOLVE_PATH="$LOOP_DIR/round-0-pr-resolve.md"` and passes it as a template variable (`scripts/setup-pr-loop.sh:721-738`). Output is rendered prompt text appended during setup when comment count is zero (`scripts/setup-pr-loop.sh:890-914`).
- gates_or_invariants: Used only when fetched PR comments contain the no-comments sentinel, producing `COMMENT_COUNT=0` (`scripts/setup-pr-loop.sh:723-729`, `scripts/setup-pr-loop.sh:892-914`). The invariant is that initial bot review should be triggered by normal PR-loop/stop-hook mechanics, not by manual PR comments (`prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe` with a matching inline fallback (`scripts/setup-pr-loop.sh:890-914`). It coordinates with the PR Stop Hook, which later reads PR-loop state and updates `active_bots` based on bot approvals/issues; setup writes `configured_bots` and `active_bots` to the state file before this prompt is emitted (`scripts/setup-pr-loop.sh:608-624`).
- edge_cases_or_failure_modes: If template loading fails, setup uses its fallback copy with the same `RESOLVE_PATH` placeholder semantics (`scripts/setup-pr-loop.sh:893-914`). If the worker does not create `round-0-pr-resolve.md`, the stop-hook workflow lacks the expected resolution summary. If comments do exist, setup uses the sibling `critical-requirements-has-comments.md` path instead (`scripts/setup-pr-loop.sh:915-918`).
- validation_or_tests: PR-loop system and stop-hook tests exercise Round 0 resolution files and stop-hook behavior; repository search shows many tests creating `round-0-pr-resolve.md` under `.humanize/pr-loop`. This template itself is prompt contract material rather than executable code.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, one per assigned row, all with cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`