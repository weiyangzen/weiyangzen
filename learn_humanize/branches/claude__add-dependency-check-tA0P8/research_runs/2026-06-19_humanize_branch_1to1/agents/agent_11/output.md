# agent_11 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-011 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Guard/block message catalog for RLCR and PR-loop state machines. These Markdown templates are rendered into hook JSON `reason` fields or stderr block messages when protected workflow transitions are denied.
- algorithmic_behavior: The directory contains 32 direct Markdown templates, no nested subdirectories. Templates encode user-facing contract text for blocked state mutation, wrong round/path access, plan integrity, git hygiene, PR trigger/reaction gates, schema errors, and loop completion prerequisites. Template rendering is single-pass through `hooks/lib/template-loader.sh:50-132`, and missing templates fall back through `load_and_render_safe` at `hooks/lib/template-loader.sh:167-203`.
- inputs_outputs_state: Inputs are placeholder bindings like `{{CURRENT_ROUND}}`, `{{CORRECT_PATH}}`, `{{PLAN_FILE}}`, `{{FIELD_NAME}}`, `{{BOT_MENTION_STRING}}`, `{{RETRY_COUNT}}`, and git/review diagnostics. Outputs are Markdown block reasons consumed by hook decisions. State is not mutated by the templates themselves, but they describe allowed next transitions such as write summary, switch branch back, restart loop, post trigger comment, or cancel/restart.
- gates_or_invariants: The directory enforces invariant messaging for managed files: `state.md`, `finalize-state.md`, `.humanize/pr-loop/state.md`, prompt files, todos files, plan backups, goal tracker after Round 0, and broad `.humanize` git-add attempts. Key gate examples include `schema-outdated.md:1-12`, `plan-backup-protected.md:1-7`, `git-not-clean.md:1-16`, `no-trigger-comment.md:1-17`, and `claude-eyes-timeout.md:1-18`.
- dependencies_and_callers: `hooks/lib/template-loader.sh:205-219` validates the presence of `block`, `codex`, and `claude` template directories. `hooks/lib/loop-common.sh:586-643` wraps block templates for todos, prompt, state, finalize-state, summary bash, and goal-tracker bash blocks. `hooks/loop-bash-validator.sh:96-105` consumes `block/git-push.md`; `hooks/loop-codex-stop-hook.sh:347`, `400`, `458`, `533`, `598`, `629`, `663`, and `746` consume plan/task/git/summary/goal templates; `hooks/pr-loop-stop-hook.sh:740-746` consumes `no-trigger-comment.md`, and `hooks/pr-loop-stop-hook.sh:785-792` consumes `claude-eyes-timeout.md`.
- edge_cases_or_failure_modes: Because rendering is single-pass, placeholder-like content inside substituted values is preserved rather than recursively expanded, preventing prompt/template injection. Empty or missing templates degrade to caller fallbacks. Some templates are partial notes rather than full messages, such as `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md`, intended to be inserted into larger block reasons.
- validation_or_tests: Template behavior is indirectly covered by hook suites that grep block decisions/reasons: plan backup blocks in `tests/test-plan-file-hooks.sh:226-284`, schema blocks in `tests/test-plan-file-hooks.sh:673-695`, section-specific goal tracker messages in `tests/test-plan-file-hooks.sh:781-1062`, and robustness block behavior in `tests/robustness/test-hook-system-robustness.sh:53-155` and `317-367`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-041 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop reaction detector for two approval/handshake transitions: Codex PR-body `+1` approval and Claude trigger-comment `eyes` acknowledgment.
- algorithmic_behavior: Parses subcommands at `scripts/check-bot-reactions.sh:79-87`. `codex-thumbsup` accepts `<pr_number>` plus optional `--after`, resolves the base repository for fork PRs, fetches issue reactions with pagination, merges pages with `jq -s 'add // []'`, selects the first `chatgpt-codex-connector[bot]` `+1`, filters by timestamp, emits reaction JSON, and exits `0` or `1` (`scripts/check-bot-reactions.sh:87-187`). `claude-eyes` accepts `<comment_id>`, optional `--pr`, `--retry`, and `--delay`, resolves the PR base repo when possible, sleeps before each attempt, fetches comment reactions with pagination, selects `claude[bot]` `eyes`, and exits `0` on JSON output or `1` after timeout (`scripts/check-bot-reactions.sh:190-300`).
- inputs_outputs_state: Inputs are CLI args, current GitHub repo context from `gh repo view`, optional parent repo metadata, PR/comment IDs, timestamps, retry counts, and GitHub API reaction payloads. Outputs are reaction JSON objects containing `user`, `content`, and `created_at`; stderr diagnostics for usage/API/timeout; and exit codes `0` found, `1` not found, `2` errors (`scripts/check-bot-reactions.sh:13-16`, `57-60`). It does not write repository state.
- gates_or_invariants: API calls are bounded by `GH_TIMEOUT=30` via `scripts/portable-timeout.sh` (`scripts/check-bot-reactions.sh:24-33`). Fork safety requires using the PR base repository, not the fork (`scripts/check-bot-reactions.sh:119-155`, `232-273`). Codex approvals must be newer than `--after` when supplied (`scripts/check-bot-reactions.sh:177-183`). Claude eyes has explicit retry/delay semantics and ignores transient API failures until retries are exhausted (`scripts/check-bot-reactions.sh:264-299`).
- dependencies_and_callers: Depends on `gh`, `jq`, `sleep`, `seq`, and `run_with_timeout` from `scripts/portable-timeout.sh`. Called by `hooks/pr-loop-stop-hook.sh:676` to remove Codex from `active_bots` or approve the PR loop, by `hooks/pr-loop-stop-hook.sh:777` to verify Claude received a trigger comment, by `hooks/pr-loop-stop-hook.sh:999` during polling, and by `scripts/setup-pr-loop.sh:587` for initial Claude eyes verification.
- edge_cases_or_failure_modes: If current repo and parent repo resolution both fail, `PR_BASE_REPO` falls back to possibly-empty `CURRENT_REPO`, so the subsequent API call becomes the hard error path. Timestamp comparison is lexical string comparison, which is safe only for normalized ISO-8601 UTC timestamps. `claude-eyes` always sleeps before the first check, so `--retry 1 --delay 0` is the only immediate check path. Invalid or missing args exit `2`.
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh:199-228` for Codex `+1`, `--after`, and Claude eyes; additional stop-hook integration appears in `tests/test-pr-loop-stophook.sh:897-1000` and `1046` onward. Fixtures include `tests/fixtures/reactions.json` and `tests/fixtures/comment-reactions.json`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-071 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file and loop-file hook invariants, especially plan integrity, active branch immutability, state schema migration gates, and protected `.humanize/rlcr` files.
- algorithmic_behavior: Builds an isolated git repo and loop directory with `setup_test_loop` (`tests/test-plan-file-hooks.sh:63-114`), mocks `codex` to avoid external calls (`40-58`), then drives `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, and `loop-codex-stop-hook.sh` through hook JSON/stdin. It counts pass/fail/skip and exits with failed count (`1108-1117`).
- inputs_outputs_state: Inputs are synthetic hook JSON payloads, generated `.humanize/rlcr/<timestamp>/state.md`, `plan.md` backups, project plan files, git branch/status state, round summary files, and goal-tracker fixtures. Outputs are PASS/FAIL lines and process exit status. It mutates only temporary test repos under `mktemp -d`.
- gates_or_invariants: Valid state passes with no output (`119-147`). Missing `plan_tracked`, `review_started`, `base_branch`, or `start_branch` blocks as malformed/schema-outdated (`149-189`, `673-695`). Branch switching blocks (`194-217`). Write/Edit/Bash attempts against loop `plan.md` backups block (`226-284`). Bash bypass attempts using direct `.humanize/rlcr/plan.md`, command substitution, glob, brace expansion, pipes, and backticks must block (`286-367`). YAML quote stripping must preserve `plan_file` and `start_branch` semantics (`373-499`). Stop hook blocks project plan modification, deletion, missing backup, uncommitted tracked modifications, and committed tracked content drift from backup (`509-775`). Goal tracker placeholder detection is section-specific (`781-1062`). Legacy `.humanize-loop.local` paths are explicitly allowed (`1068-1106`).
- dependencies_and_callers: Depends on git, mocked `codex`, the hook scripts under `hooks/`, `jq` indirectly through hook JSON paths, and local template blocks for error text. It is likely invoked by the repository’s shell test runner/CI, though no direct runner file was required for this item.
- edge_cases_or_failure_modes: Tests both tracked and gitignored plan modes, default branch differences (`main` vs `master`), quoted YAML values, hyphenated plan paths, race-condition-style tracked plan changes, content-diff detection when git status is clean, and old legacy folder names that should no longer be treated as active loop state.
- validation_or_tests: This file is itself validation. It also cross-validates behavior implemented in `hooks/loop-plan-file-validator.sh:44-89` for strict state/schema parsing, `hooks/loop-bash-validator.sh:330-340` for plan backup protection, and `hooks/loop-codex-stop-hook.sh:285-347` for plan backup/content integrity.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-101 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Specific block template for the RLCR gate that prevents `git push` while `push_every_round` is false.
- algorithmic_behavior: Static Markdown message states that commits stay local and the loop handles local commits until completion, then points to the opt-in startup flag: `/humanize:start-rlcr-loop plan.md --push-every-round` (`prompt-template/block/git-push.md:1-9`).
- inputs_outputs_state: Has no placeholders and no internal state. Input is only template selection by the caller. Output is a rendered Markdown block sent to stderr by the bash validator.
- gates_or_invariants: Enforces the invariant that pushing is disallowed during ordinary RLCR rounds unless the loop was started with `--push-every-round`. The actual predicate is in `hooks/loop-bash-validator.sh:88-105`: if active loop state has `STATE_PUSH_EVERY_ROUND` not equal to `true` and command matches `git push`, render this template and exit `2`.
- dependencies_and_callers: Loaded with `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md"` at `hooks/loop-bash-validator.sh:103`. The template depends on template directory discovery and fallback behavior in `hooks/lib/template-loader.sh:24-31` and `167-203`.
- edge_cases_or_failure_modes: Only direct commands matching leading `git push` are gated by the current caller regex; wrapped pushes such as `sh -c "git push"` are not described by this template and would depend on other sandbox/tool policy. If the file is missing, caller fallback still blocks with a shorter message.
- validation_or_tests: No assigned test directly targets this exact template text. The related gate is reachable through bash-validator tests; `tests/robustness/test-hook-system-robustness.sh:369-382` documents the broader design that unrelated dangerous commands are delegated to sandbox policy, while `hooks/loop-bash-validator.sh:96-105` is the concrete validation point for this template.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-131 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer appended to each generated next-round Claude prompt after Codex review finds continuing work.
- algorithmic_behavior: Adds a final instruction block telling Claude not to escape by lying, editing loop state, or running `cancel-rlcr-loop`, then requires optional code simplifier use, a descriptive commit, and writing the work summary to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:1-9`).
- inputs_outputs_state: Input placeholder is `NEXT_SUMMARY_FILE`, supplied by `hooks/loop-codex-stop-hook.sh:1561-1563`. Output is appended Markdown inside `round-<NEXT_ROUND>-prompt.md`, not a hook JSON block. It does not mutate state directly, but it guides the next worker-visible state transition: implement, commit, summarize, then stop.
- gates_or_invariants: Reinforces that loop state files and cancel command are not acceptable completion shortcuts. It also preserves the invariant that each round must produce the expected summary file before stop hook review; missing summaries are blocked by `hooks/loop-codex-stop-hook.sh:650-668` using `block/work-summary-missing.md`.
- dependencies_and_callers: Appended at `hooks/loop-codex-stop-hook.sh:1623-1627` through `load_and_render_safe`. It coordinates with `prompt-template/claude/next-round-prompt.md`, optional open-question and post-alignment inserts (`hooks/loop-codex-stop-hook.sh:1574-1621`), optional push note (`1629-1636`), and goal tracker update request instructions (`1638` onward).
- edge_cases_or_failure_modes: If missing, the fallback only says to commit changes and write summary, losing the explicit anti-bypass and code-simplifier instructions. The `@{{NEXT_SUMMARY_FILE}}` form is prompt-oriented; correctness depends on the caller passing the active loop path.
- validation_or_tests: Indirectly validated by stop-hook behavior that requires the generated summary path and protects state/cancel bypasses. Related protections are exercised in `tests/robustness/test-hook-system-robustness.sh:317-367`, `421-451`, and `559-613`.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-161 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for hook input validation, state parsing, protected-file blocking, concurrency safety, and large/malformed JSON handling across the RLCR and PR hook system.
- algorithmic_behavior: Sources shared helpers from `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `tests/test-helpers.sh` (`tests/robustness/test-hook-system-robustness.sh:20-26`), creates isolated fixtures, invokes validators with JSON stdin, and verifies exit codes plus block-decision text. It ends via `print_test_summary` and exits with aggregate status (`667-672`).
- inputs_outputs_state: Inputs are generated hook JSON, generated `.humanize/rlcr` and `.humanize/pr-loop` state files, temporary git repos, mocked `codex`, large JSON strings, escaped nulls, and deeply nested JSON. Outputs are PASS/FAIL summaries. State changes are limited to temp dirs, including checking whether stop hook renames invalid state to `unexpected-state.md` (`536-550`).
- gates_or_invariants: Edit validator accepts normal JSON but blocks `state.md`, malformed JSON must fail gracefully, missing `file_path` must not crash, outside-.humanize path traversal is delegated to sandbox, and traversal to `.humanize/.../state.md` is blocked (`40-155`). Plan validator allows valid plan/non-plan edits and handles empty JSON (`165-243`). Shared state parsing tolerates whitespace, quoted paths, and malformed delimiters without crashing (`253-307`). Bash validator blocks `state.md` and post-round-0 `goal-tracker.md`, but passes unrelated dangerous commands to sandbox (`317-382`). Concurrent read validators must not corrupt state (`421-451`). Stop hooks allow no-state exits, tolerate corrupt/incomplete state, and block active loop exits with a JSON block (`461-613`). JSON stress inputs under timeout must not crash (`623-665`).
- dependencies_and_callers: Depends on hook scripts `loop-edit-validator.sh`, `loop-plan-file-validator.sh`, `loop-bash-validator.sh`, `loop-write-validator.sh`, `loop-read-validator.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh`; helpers include `get_current_round`, `validate_hook_input`, `is_deeply_nested`, and `run_with_timeout`. It cross-checks implementation paths such as `hooks/loop-bash-validator.sh:26-48` for JSON validation and `31-34` for nesting checks.
- edge_cases_or_failure_modes: Covers malformed JSON exit codes below crash threshold, command-injection-like content, binary-looking write content, 100KB JSON payloads with a five-second timeout, escaped nulls, 10-level nested JSON, missing `.humanize` directories, corrupted state text, missing critical fields, and active-loop stop behavior with mocked Codex output.
- validation_or_tests: This file is validation. It complements `tests/test-plan-file-hooks.sh` by focusing less on plan backup specifics and more on robustness/security boundaries and non-crash guarantees for hook input/state parsing.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6 Item Evidence headings; one heading per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`