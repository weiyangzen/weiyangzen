# agent_20 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-020 `directory` `skills/humanize-rlcr`
- cursor: `[_]`
- core_role: Skill-mode entrypoint for RLCR, ensuring agents without native hooks still route through the same stop-hook enforcement as hook-mode RLCR. The directory recursively contains one core file, `skills/humanize-rlcr/SKILL.md`.
- algorithmic_behavior: `SKILL.md` defines a required sequence: run `scripts/setup-rlcr-loop.sh` with user arguments, then for each round read the generated prompt, implement, commit, write the required summary, and run `scripts/rlcr-stop-gate.sh` (`skills/humanize-rlcr/SKILL.md:28-58`). The key algorithmic choice is delegation: it explicitly says not to reimplement review logic and always call the stop-gate wrapper (`skills/humanize-rlcr/SKILL.md:9-16`). The wrapper itself invokes `hooks/loop-codex-stop-hook.sh`, maps hook block decisions to exit `10`, allowed gate to `0`, and runtime errors to `20` (`scripts/rlcr-stop-gate.sh:1-14`, `scripts/rlcr-stop-gate.sh:99-138`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` passed to setup, optional `CLAUDE_SESSION_ID` and `CLAUDE_TRANSCRIPT_PATH` forwarded into the gate command, and generated loop files under `.humanize/rlcr/<timestamp>/` (`skills/humanize-rlcr/SKILL.md:44-56`). Outputs include committed implementation changes, `round-<N>-summary.md` or `finalize-summary.md`, and gate instructions/exit code. State is not edited by the skill; it is owned by setup and stop-hook state files such as `state.md` and `finalize-state.md`. Setup enforces dependency checks for `codex`, `jq`, and `git` before loop creation (`scripts/setup-rlcr-loop.sh:257-286`).
- gates_or_invariants: Enforces state/schema validation, branch consistency, plan-file integrity, incomplete task blocking, git-clean exit, push-every-round blocking, summary presence, max-iteration handling, full-alignment rounds, strict `COMPLETE`/`STOP` markers, review-phase guard, code-review issue markers, Codex failure handling, and open-question handling (`skills/humanize-rlcr/SKILL.md:65-83`). Critical invariants forbid manual edits to state files, manual completion after a blocked gate, ad-hoc Codex review in place of the gate, and using anything other than generated loop files as source of truth (`skills/humanize-rlcr/SKILL.md:84-90`).
- dependencies_and_callers: Coordinates with `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/loop-codex-stop-hook.sh`, and `scripts/cancel-rlcr-loop.sh`. Setup shares active-loop discovery with `hooks/lib/loop-common.sh` and blocks PR-loop coexistence (`scripts/setup-rlcr-loop.sh:292-313`). Cancel resolves the newest active RLCR loop globally through `find_active_loop` (`scripts/cancel-rlcr-loop.sh:72-88`).
- edge_cases_or_failure_modes: Setup failure must stop the flow (`skills/humanize-rlcr/SKILL.md:38`). Gate exit `10` means blocked but recoverable by following instructions; exit `20` means wrapper/hook/runtime error and must not be treated as completion (`skills/humanize-rlcr/SKILL.md:60-64`). Skill hydration depends on `{{HUMANIZE_RUNTIME_ROOT}}`; if not replaced by the installer, commands are unusable (`skills/humanize-rlcr/SKILL.md:18-27`). `--skip-impl` can create a placeholder plan path and disables plan tracking if no plan exists (`scripts/setup-rlcr-loop.sh:342-360`).
- validation_or_tests: Relevant coverage includes state transition robustness, session-id tests, finalization tests, stop-hook tests, and the wrapper’s dependency on `loop-codex-stop-hook.sh`. I did not execute tests because this branch export is read-only and the suites create temp repos/files and mutate fixtures.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-050 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Gen-plan command preflight and output seeding script. Despite the name, it both validates IO and creates the initial output plan by copying the plan template and appending the original draft.
- algorithmic_behavior: Parses `--input` and `--output`, rejects missing values, unknown flags, and help through `usage()` with exit `6` (`scripts/validate-gen-plan-io.sh:16-67`). It normalizes input/output with `realpath -m` fallback, derives output dir, prints diagnostic paths, then validates input exists, input is nonempty, output dir exists, output path is not a directory, output path does not already exist, and output dir is writable (`scripts/validate-gen-plan-io.sh:69-124`). After success it finds `prompt-template/plan/gen-plan-template.md` via `CLAUDE_PLUGIN_ROOT` or script-relative fallback, exits `7` if missing, copies it to output, then appends draft delimiters and draft content (`scripts/validate-gen-plan-io.sh:133-160`).
- inputs_outputs_state: Inputs are `--input <draft>` and `--output <plan>`, plus optional `CLAUDE_PLUGIN_ROOT`. Outputs are stdout status messages, exit code, and on success a new output file containing the plan template plus `--- Original Design Draft Start/End ---` wrapper and original draft (`scripts/validate-gen-plan-io.sh:148-159`). No persistent state file is updated.
- gates_or_invariants: Exit-code contract is explicit: `1` missing input, `2` empty input, `3` missing output dir, `4` output exists or is directory, `5` no write permission, `6` invalid args, `7` missing template (`scripts/validate-gen-plan-io.sh:4-12`). The invariant is that gen-plan must never overwrite an existing output and must preserve the human draft by appending it after the template.
- dependencies_and_callers: Called by `commands/gen-plan.md` Phase 1 (`commands/gen-plan.md:30-47`) and by the `humanize-gen-plan` skill flow (`skills/humanize-gen-plan/SKILL.md:17-45`, `skills/humanize-gen-plan/SKILL.md:94-105`). Depends on shell utilities `realpath`, `dirname`, `wc`, `tr`, `cp`, and `cat`. The output format depends on `prompt-template/plan/gen-plan-template.md`, whose required plan skeleton includes goal, ACs, path boundaries, feasibility hints, dependencies, and implementation notes (`prompt-template/plan/gen-plan-template.md:1-74`).
- edge_cases_or_failure_modes: The script is not a pure validator; running it in a writable repo creates the output file. `realpath -m` is GNU-specific but has fallback to raw path (`scripts/validate-gen-plan-io.sh:69-71`). There is a race between nonexistence check and `cp`. Template lookup differs between plugin and repository contexts, so incorrect `CLAUDE_PLUGIN_ROOT` can cause exit `7`. Write permission is checked only on the output directory, not by a dry file creation.
- validation_or_tests: `tests/test-gen-plan.sh` includes direct script tests for missing argument values, flag-as-value, unknown option, missing input, empty input, missing output dir, output exists, output is directory, valid paths, and help behavior (`tests/test-gen-plan.sh:426-541`). I inspected but did not run them because the workspace is read-only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-080 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: High-level executable specification for PR-loop behavior across setup, state detection, monitor display, reviewer-status classification, reaction gates, goal tracker updates, and stop-hook integration.
- algorithmic_behavior: The runner creates an isolated temp git repo per test, prepends `tests/mocks` to `PATH`, points fixtures through `MOCK_GH_FIXTURES_DIR`, runs each test in a subshell, and counts pass/fail totals (`tests/test-pr-loop-system.sh:50-104`, `tests/test-pr-loop-system.sh:1761-1904`). It validates mutual exclusion between RLCR and PR loops (`tests/test-pr-loop-system.sh:110-146`), PR reviewer startup cases 1/2/3 and case 4/5 stale-review detection (`tests/test-pr-loop-system.sh:152-193`, `tests/test-pr-loop-system.sh:493-506`, `tests/test-pr-loop-system.sh:1315-1378`), Codex `+1` and Claude `eyes` reaction checks (`tests/test-pr-loop-system.sh:199-229`), PR review submissions as valid bot feedback (`tests/test-pr-loop-system.sh:236-249`), phase detection and monitor output (`tests/test-pr-loop-system.sh:256-309`, `tests/test-pr-loop-system.sh:947-1106`), goal tracker parsing/updating/idempotency/partial repair (`tests/test-pr-loop-system.sh:315-417`, `tests/test-pr-loop-system.sh:513-538`, `tests/test-pr-loop-system.sh:681-764`, `tests/test-pr-loop-system.sh:1180-1309`), trigger requirements by startup case (`tests/test-pr-loop-system.sh:1112-1174`), setup failure cleanup when Claude eyes cannot be verified (`tests/test-pr-loop-system.sh:1384-1447`), and stop-hook goal-tracker integration (`tests/test-pr-loop-system.sh:1542-1755`).
- inputs_outputs_state: Inputs are mock GitHub JSON fixtures, environment variables like `MOCK_GH_PR_NUMBER`, `MOCK_GH_PR_STATE`, `MOCK_GH_LATEST_COMMIT_AT`, `MOCK_GH_HEAD_SHA`, and generated `.humanize/pr-loop/<timestamp>/` files. Outputs are pass/fail console lines and mutations inside the temp repo, especially `state.md`, `approve-state.md`, `cancel-state.md`, `maxiter-state.md`, `goal-tracker.md`, `round-*-pr-check.md`, and `round-*-pr-resolve.md`.
- gates_or_invariants: PR setup must block if any RLCR loop is active, and RLCR setup must block if a PR loop is active. Startup cases 1/2/3 at round 0 do not require a trigger, while case 4/5 and later rounds do (`tests/test-pr-loop-system.sh:1112-1174`). Case 4/5 with Claude must fail if `trigger_comment_id` cannot be retrieved, because eyes verification cannot be performed (`tests/test-pr-loop-system.sh:1384-1447`). Goal tracker updates must be idempotent by round+reviewer, must insert rows inside the Issue Summary table, and must repair partial summary/log updates (`tests/test-pr-loop-system.sh:722-764`, `tests/test-pr-loop-system.sh:1180-1309`).
- dependencies_and_callers: Calls `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/humanize.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/lib/loop-common.sh`, and `scripts/lib/monitor-common.sh`. The underlying reviewer classifier uses issue comments, inline review comments, and PR review submissions from GitHub, maps `codex` to `chatgpt-codex-connector[bot]`, and classifies stale per-bot review state into cases 4/5 (`scripts/check-pr-reviewer-status.sh:153-245`). Reaction checks use GitHub reactions endpoints and fork-aware base repo resolution (`scripts/check-bot-reactions.sh:119-187`).
- edge_cases_or_failure_modes: Several tests intentionally mutate shared fixture files and then restore them, so aborted tests can leave fixture drift. `get_pr_loop_phase` uses `/tmp/humanize-phase-<session>-<round>.size` cache for file-growth detection, so stale cache can affect monitor phase checks (`scripts/lib/monitor-common.sh:292-318`). The test uses GNU/BSD-compatible date fallbacks in one integration case (`tests/test-pr-loop-system.sh:1547-1551`). The runner assumes `jq`, `git`, `timeout`, and shell behavior compatible with these scripts.
- validation_or_tests: This file is itself validation. It is dispatched by `main()` with optional filters such as `mutual_exclusion`, `reviewer_status`, `phase`, `goal_tracker_schema`, `case_4_5`, `setup_failure`, and `goal_tracker_integration` (`tests/test-pr-loop-system.sh:1773-1887`). Not executed here due read-only branch export and write-heavy test setup.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-110 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Blocking message template for RLCR plan-file integrity violations.
- algorithmic_behavior: Renders a concise instruction block saying the plan file changed since session start, plan edits are forbidden during an active session, and recovery requires canceling the current session, updating the plan, and starting a new session (`prompt-template/block/plan-file-modified.md:1-12`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}` and `{{BACKUP_PATH}}`; output is Markdown used as the hook block reason. It does not mutate state itself.
- gates_or_invariants: Enforces the invariant that the implementation plan is immutable while an RLCR session is active unless the session is canceled and restarted. In the stop hook, it is used when `diff -q "$FULL_PLAN_PATH" "$BACKUP_PLAN"` detects plan drift, then returns a JSON block decision with system message `Loop: Blocked - plan file modified` (`hooks/loop-codex-stop-hook.sh:333-352`). A separate tracked-plan check blocks uncommitted plan changes with inline text before this template path (`hooks/loop-codex-stop-hook.sh:320-330`).
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md"` from `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:347-349`). Template existence is part of `tests/test-template-references.sh`, which scans hook references and fails missing templates (`tests/test-template-references.sh:51-115`).
- edge_cases_or_failure_modes: If template loading fails, the stop hook has an inline fallback with equivalent intent (`hooks/loop-codex-stop-hook.sh:335-346`). The nearby hook comment says “Plan changes are now allowed” while the following diff still blocks changed plans (`hooks/loop-codex-stop-hook.sh:333-334`), so the comment is potentially stale or misleading.
- validation_or_tests: Covered indirectly by template reference validation and by plan-file hook tests elsewhere in the suite. Not executed here.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-140 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex regular review prompt defining the review contract that decides whether an RLCR round can stop or must continue.
- algorithmic_behavior: Forces Codex to read the original implementation plan first, compare plan and round prompt against Claude’s summary, perform critical implementation review, reject deferrals as incomplete, produce implementation plans for pending/deferred work, perform mandatory goal alignment against the goal tracker, and write review comments to the review result file unless everything is complete (`prompt-template/codex/regular-review.md:1-57`).
- inputs_outputs_state: Inputs are placeholders `{{CURRENT_ROUND}}`, `{{PLAN_FILE}}`, `{{PROMPT_FILE}}`, `{{SUMMARY_CONTENT}}`, `{{DOCS_PATH}}`, `{{GOAL_TRACKER_FILE}}`, `{{GOAL_TRACKER_UPDATE_SECTION}}`, and `{{REVIEW_RESULT_FILE}}`. Output is the rendered prompt consumed by Codex; Codex then writes `{{REVIEW_RESULT_FILE}}`. State transition is marker-driven: only `COMPLETE` as the last line allows the loop to stop (`prompt-template/codex/regular-review.md:49-57`).
- gates_or_invariants: No deferrals or pending work are allowed for completion; all original plan tasks and all ACs must be met before the last-line `COMPLETE` marker (`prompt-template/codex/regular-review.md:52-57`). The goal alignment summary is mandatory and must report AC coverage, forgotten items, and unjustified deferrals (`prompt-template/codex/regular-review.md:33-45`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` in the non-full-alignment branch via `load_and_render_safe`, with derived variables passed into `prompt-template/codex/regular-review.md` and written to `$REVIEW_PROMPT_FILE` (`hooks/loop-codex-stop-hook.sh:872-887`). Full-alignment rounds use a sibling template instead (`hooks/loop-codex-stop-hook.sh:857-870`).
- edge_cases_or_failure_modes: This is prompt-level control, so correctness depends on Codex following the final-line marker rule. Missing/empty summary content weakens review context but still renders. The template treats admitted unfinished work and planned deferrals as review-blocking even if Claude claims success (`prompt-template/codex/regular-review.md:24-31`).
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`; stop-hook behavior using Codex results is covered by stop-hook and RLCR tests. Not executed here.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-170 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Robustness spec for RLCR state parsing and active-loop discovery, focused on valid/invalid state transitions, terminal state handling, and directory selection behavior.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers, creates synthetic `.humanize/rlcr/<timestamp>/` directories, then asserts parser/discovery behavior across 16 scenarios (`tests/robustness/test-state-transition-robustness.sh:12-24`, `tests/robustness/test-state-transition-robustness.sh:30-89`). It validates round 0, round 5, and max-round parsing (`tests/robustness/test-state-transition-robustness.sh:98-138`); finalize-state detection and coexistence with `state.md` (`tests/robustness/test-state-transition-robustness.sh:148-172`); cancel-state as inactive plus newer state after cancel (`tests/robustness/test-state-transition-robustness.sh:182-207`); malformed numeric/schema cases (`tests/robustness/test-state-transition-robustness.sh:217-361`); and newest-directory / one-level discovery semantics (`tests/robustness/test-state-transition-robustness.sh:371-417`).
- inputs_outputs_state: Inputs are generated `state.md`, `finalize-state.md`, and `cancel-state.md` files in a temp test dir. Outputs are pass/fail records via helper functions and the final summary (`tests/robustness/test-state-transition-robustness.sh:419-424`). It does not touch real repo loop state.
- gates_or_invariants: `parse_state_file_strict` must reject missing YAML separators, missing required fields, nonnumeric round/max values, and invalid `review_started`; it requires `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:392-460`). `find_active_loop` must prefer `finalize-state.md` over `state.md`, ignore terminal-only cancel directories, and only inspect one directory level (`hooks/lib/loop-common.sh:191-228`, `hooks/lib/loop-common.sh:248-306`).
- dependencies_and_callers: Tests `parse_state_file_strict`, `get_current_round`, and `find_active_loop` from `hooks/lib/loop-common.sh`. These helpers are used by validators and stop hooks such as `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: Negative `current_round` is accepted because the strict regex allows `^-?[0-9]+$`, and the test documents it as parsed but suspicious (`tests/robustness/test-state-transition-robustness.sh:217-236`, `hooks/lib/loop-common.sh:439-443`). Rounds greater than `max_iterations` are parsed, with enforcement expected elsewhere (`tests/robustness/test-state-transition-robustness.sh:238-257`). Directory choice is lexicographic rather than timestamp-validating (`tests/robustness/test-state-transition-robustness.sh:385-402`). Nested state files are intentionally ignored (`tests/robustness/test-state-transition-robustness.sh:404-417`).
- validation_or_tests: This file is itself validation and ends with `print_test_summary` plus exit status (`tests/robustness/test-state-transition-robustness.sh:419-424`). Not executed here because the workspace is read-only and the test suite writes temp state files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6/6 item sections present; item IDs appear only in their section headings above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`