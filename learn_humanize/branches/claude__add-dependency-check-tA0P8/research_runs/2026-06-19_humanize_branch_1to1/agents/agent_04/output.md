# agent_04 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`
- notes: research-only pass; no files modified. I did not run tests because this branch export is read-only and the relevant shell tests create fixtures/output files. Git diff/log inspection was also blocked by sandboxed macOS tooling attempting to create `/tmp/xcrun_db-*`; evidence below is from direct file inspection.

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: The `hooks` tree is the runtime enforcement layer for Humanize’s RLCR and PR-loop state machines. `hooks/hooks.json:4-79` wires Claude Code hook events to validators: `UserPromptSubmit` -> plan-file validator, `PreToolUse` -> read/write/edit/bash validators, `PostToolUse Bash` -> session-id recorder, and `Stop` -> RLCR plus PR-loop stop hooks.
- algorithmic_behavior: Recursive contents inspected: 12 files, 6,708 total lines. Shared libraries provide JSON validation, template rendering, active-loop lookup, state parsing, path classification, cancellation authorization, review issue detection, PR bot mapping, and loop termination (`hooks/lib/loop-common.sh:91-1288`, `hooks/lib/template-loader.sh:26-222`). Validators fail closed on malformed hook JSON/deep nesting, protect state/prompt/plan/goal-tracker/todo surfaces, and route block messages through prompt templates. Stop hooks advance or block state transitions based on summaries, Codex review output, git cleanliness, task completion, PR bot responses, and terminal state files.
- inputs_outputs_state: Inputs are Claude hook JSON, `session_id`, `tool_name`, `tool_input`, `.humanize/rlcr/*` and `.humanize/pr-loop/*` state files, git status, GitHub PR state/comments/reactions, task files under `~/.claude/tasks/<session_id>/`, transcript JSONL, prompt templates, and Codex outputs. Outputs include hook JSON block decisions, generated next-round prompts, review/result files, PR feedback files, modified loop state files, terminal state renames such as `complete-state.md`, `cancel-state.md`, `approve-state.md`, and stderr diagnostics.
- gates_or_invariants: Major gates include valid hook JSON and max nesting (`loop-common.sh:91-165`), session-aware active-loop lookup that prevents stale loop revival (`loop-common.sh:248-306`), strict state frontmatter fields (`loop-common.sh:401-464`), branch consistency and plan integrity (`loop-codex-stop-hook.sh:241-355`, `loop-plan-file-validator.sh:91-229`), incomplete task blocking before expensive Codex review (`loop-codex-stop-hook.sh:357-413`), git clean and optional push-before-exit checks (`loop-codex-stop-hook.sh:433-643`), required summary files (`loop-codex-stop-hook.sh:646-682`), review-phase marker verification (`loop-codex-stop-hook.sh:1497-1513`), and PR trigger/force-push/bot-response gates (`pr-loop-stop-hook.sh:365-447`, `620-800`, `802-1180`).
- dependencies_and_callers: Called by Claude Code via `hooks.json`. Depends on `jq`, `python3`, `git`, `gh`, `codex`, `scripts/portable-timeout.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, and `prompt-template/**`. Setup/cancel scripts also share `loop-common.sh`; e.g. `scripts/setup-rlcr-loop.sh:792-823` creates `state.md` and the post-bash signal file consumed by `loop-post-bash-hook.sh`.
- edge_cases_or_failure_modes: Handles invalid UTF-8/null bytes, malformed JSON, missing fields, stale terminal sessions, missing templates with fallbacks, git timeout/index-lock cleanup, shell redirection and wrapper bypasses, direct and broad `git add .humanize`, false-positive session-id claiming, Codex missing/failing/empty output, review logs without issue markers, PR force pushes, stale trigger comments, bot timeouts, missing Claude “eyes” reactions, fork PR repo lookup, and macOS Bash 3.2 limitations by avoiding associative arrays/namerefs in PR-loop code.
- validation_or_tests: Relevant tests include `tests/test-session-id.sh` for session-id capture and active-loop filtering, `tests/test-todo-checker.sh` for task/todo completion gates, `tests/test-stop-gate.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-pr-loop-2-hooks.sh`, `tests/test-pr-loop-stophook.sh`, and robustness suites under `tests/robustness/`. Direct inspection only; not run in this read-only export.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-034 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: PostToolUse Bash hook that records the owning Claude `session_id` into an RLCR `state.md` immediately after setup, so later validators and stop hooks can target the leader session instead of team-member sessions.
- algorithmic_behavior: Reads hook JSON from stdin (`loop-post-bash-hook.sh:26-27`), resolves project root from `CLAUDE_PROJECT_DIR` or `pwd` (`:29-33`), exits normally unless `.humanize/.pending-session-id` exists (`:35-38`), reads state path and setup command signature from the signal (`:40-48`), verifies the state file exists or removes stale signal (`:50-54`), validates that the Bash command starts with the exact setup script path either quoted or unquoted and followed by whitespace/end (`:56-92`), extracts `.session_id` with `jq` (`:94-102`), patches only an empty `session_id:` line via `awk` (`:105-119`), then removes the one-shot signal (`:122-125`).
- inputs_outputs_state: Inputs are hook JSON fields `.tool_input.command` and `.session_id`, `CLAUDE_PROJECT_DIR`, and `.humanize/.pending-session-id` containing line 1 = `state.md` path and line 2 = resolved setup script path. Output/state mutation is a single replacement from `session_id:` to `session_id: <value>` in the target state file plus removal of the pending signal.
- gates_or_invariants: Does not consume the signal for non-setup Bash events, basename-only mentions, echoed paths, quoted path concatenation, missing `jq` command parsing, missing `session_id`, or already-populated `session_id`. It only updates empty `session_id` fields and uses `awk -v` so `/`, `&`, and other replacement-sensitive characters in the session id are preserved (`:105-118`).
- dependencies_and_callers: Producer is `scripts/setup-rlcr-loop.sh:792-823`, which initializes `session_id:` and writes `.pending-session-id`. Consumer side is shared `find_active_loop` session filtering in `hooks/lib/loop-common.sh:248-306`, used by read/write/edit/bash validators and `loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: If the signal points at a missing/empty state path, it is removed (`:50-54`). If `jq` is absent or command parsing fails, the signal remains for a later attempt (`:64-68`). If session id is missing, signal remains (`:100-102`). If the command is not the exact setup invocation, signal remains (`:88-91`). If state has `session_id: existing`, it is not overwritten (`:107-120`).
- validation_or_tests: `tests/test-session-id.sh:31-91` verifies setup creates `session_id:` and the pending signal; `:110-164` verifies the hook records the id and removes the signal; `:470-528` rejects non-setup commands and accepts the real setup invocation; `:531-574` covers special characters; `:786-823` verifies signal format; `:825-1053` covers false positives, quoted-prefix bypass, unquoted calls, and tab-delimited invocations.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-064 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable structural specification for the `gen-plan` command surface and its IO validator. It protects the plan-generation workflow that turns a draft into a structured plan with acceptance criteria, path boundaries, feasibility notes, and dependency/sequence sections.
- algorithmic_behavior: Initializes pass/fail counters and helpers (`tests/test-gen-plan.sh:22-40`), verifies `commands/gen-plan.md` exists and has frontmatter fields (`:55-104`), verifies `agents/draft-relevance-checker.md` exists with expected name/model/tools (`:107-157`), checks version consistency across `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md` (`:160-180`), creates invalid temp fixtures for negative validation (`:191-342`), validates model name patterns (`:344-401`), checks command/agent text for Emoji/CJK characters (`:403-423`), then exercises `scripts/validate-gen-plan-io.sh` argument and file-state exits (`:426-542`).
- inputs_outputs_state: Inputs are repository files under `commands/`, `agents/`, `.claude-plugin/`, `README.md`, and `scripts/validate-gen-plan-io.sh`. The test creates temporary fixture directories with `mktemp -d` and removes them with `trap`. Output is colored PASS/FAIL lines and exit `0` only when `TESTS_FAILED` is zero (`:544-562`).
- gates_or_invariants: Enforces command metadata presence, lowercase hyphenated names (`validate_name`, `:195-199`), minimal frontmatter (`:201-214`), a simple frontmatter syntax check (`:304-319`), allowed model aliases/prefixes (`:350-358`), English-only content via `grep -P` when supported (`:409-421`), and exact validator exit codes: missing arg/unknown/help -> `6`, missing input -> `1`, empty input -> `2`, missing output dir -> `3`, existing output path or directory -> `4`, valid paths -> `0` (`:438-538`).
- dependencies_and_callers: Reads `commands/gen-plan.md`, which defines the workflow and calls `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` (`commands/gen-plan.md:30-47`), invokes the relevance checker agent contract (`commands/gen-plan.md:50-72`; `agents/draft-relevance-checker.md:1-36`), and expects generated plans to contain `## Dependencies and Sequence` (`commands/gen-plan.md:205-215`; `prompt-template/plan/gen-plan-template.md:57-67`). The script under test copies the plan template and appends the original draft (`scripts/validate-gen-plan-io.sh:133-160`).
- edge_cases_or_failure_modes: The YAML checker is intentionally shallow and regex-based, not a full parser. `grep -P` absence can make the non-ASCII check silently pass because errors are redirected. Version extraction depends on exact README text format. The script test’s “valid paths exits 0” creates the output file as a side effect, so repeated use of the same temp path within one run would fail, but it uses a fresh temp directory.
- validation_or_tests: This file is itself the test. Direct inspection only; not run. It is listed in `tests/run-all-tests.sh:53`, so it is part of the broader suite.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-094 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR review-phase failure when `codex review` cannot complete or cannot produce usable output.
- algorithmic_behavior: Provides a concise failure prompt with exit code, missing review result path, debug file locations, and the last stderr lines (`prompt-template/block/codex-review-failed.md:1-18`). It is loaded in `block_review_failure()` after code-review command failure or empty/missing review output (`hooks/loop-codex-stop-hook.sh:1213-1286`).
- inputs_outputs_state: Inputs are template variables for review result file, debug command/stdout/stderr files, exit code, and `STDERR_CONTENT`. Output is rendered markdown embedded as the hook block `reason`, causing Claude to retry exit rather than proceeding to finalize.
- gates_or_invariants: This template supports a hard gate: review failure is not treated as success. `run_and_handle_code_review()` calls `block_review_failure` when `codex review` exits nonzero or review issue detection cannot read output (`hooks/loop-codex-stop-hook.sh:1049-1077`).
- dependencies_and_callers: Caller is only `hooks/loop-codex-stop-hook.sh:1267-1275` via `load_and_render_safe`. Rendering depends on `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: The template variable names appear partially stale relative to the caller: the template uses `{{CODEX_EXIT_CODE}}`, `{{CODEX_STDOUT_FILE}}`, and `{{CODEX_STDERR_FILE}}` (`codex-review-failed.md:5-11`), while `block_review_failure()` passes `EXIT_CODE`, `CODEX_CMD_FILE`, `CODEX_LOG_FILE`, and not stdout/stderr-specific variables (`loop-codex-stop-hook.sh:1267-1275`). Because the renderer preserves unknown placeholders (`template-loader.sh:119-122`), some placeholders may remain unresolved in the user-facing block.
- validation_or_tests: Covered indirectly by stop-hook tests that exercise review failure paths; direct reference found through `rg` only in `loop-codex-stop-hook.sh`. Direct inspection only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-124 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Minimal block template that enforces RLCR summary writes into the active loop directory.
- algorithmic_behavior: Emits a title, a mandatory-location statement, and the correct path placeholder (`prompt-template/block/wrong-summary-location.md:1-5`). It is used by the Write validator when a `round-N-summary.md` path is written outside `.humanize/rlcr/<active-loop>/` (`hooks/loop-write-validator.sh:204-211`).
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`loop-write-validator.sh:204-210`). Output is markdown on stderr and exit `2` from the hook, which blocks the Write tool.
- gates_or_invariants: Summary files must be written for the active round and exact active directory. Related gates in `loop-write-validator.sh` also reject prompt writes (`:68-71`), state writes (`:154-161`), plan backup writes (`:181-187`), goal tracker writes after round 0 (`:194-198`), wrong summary round numbers (`:230-247`), and wrong directory paths (`:254-265`).
- dependencies_and_callers: Depends on `load_and_render_safe` from `hooks/lib/template-loader.sh`, round parsing from `hooks/lib/loop-common.sh:544-560`, and active loop/session resolution from `loop-common.sh:248-306`.
- edge_cases_or_failure_modes: This template is only used for Write. The Edit validator exits early for paths outside `.humanize/rlcr` (`hooks/loop-edit-validator.sh:86-88`), so editing an already-existing external summary-shaped file is not blocked by this specific location template. Bash summary modifications are blocked separately through `summary_bash_blocked_message` (`hooks/loop-bash-validator.sh:375-378`).
- validation_or_tests: Covered by hook/path validation tests such as `tests/test-plan-file-hooks.sh`, `tests/test-allowlist-validators.sh`, and robustness path-validation suites. Direct inspection only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-154 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: User-facing skill contract for Humanize workflows: RLCR implementation/review loop, PR loop automation, structured plan generation, and one-shot Codex consultation.
- algorithmic_behavior: Defines RLCR’s two phases: implementation summary reviewed by Codex, then code review by `codex review --base <branch>` before finalize (`skills/humanize/SKILL.md:30-47`). Defines PR loop polling/re-review flow (`:48-58`). Defines gen-plan output expectations including acceptance criteria, path boundaries, feasibility hints, and dependencies/sequence (`:60-68`, `:145-184`). Provides command invocations for setup, stop gate, cancel, PR setup/cancel, gen-plan validation, and ask-codex (`:71-143`).
- inputs_outputs_state: Inputs are plan files, PR state, draft docs, user options, and runtime root placeholder `{{HUMANIZE_RUNTIME_ROOT}}` (`:10-18`). Outputs/state live under `.humanize/rlcr/<timestamp>/`, `.humanize/pr-loop/<timestamp>/`, and `.humanize/skill/<timestamp>/`, with files such as `state.md`, `goal-tracker.md`, `round-N-summary.md`, `round-N-review-result.md`, `finalize-state.md`, `finalize-summary.md`, and `complete-state.md` (`:214-238`).
- gates_or_invariants: In skill mode, it explicitly requires running `scripts/rlcr-stop-gate.sh` for hook-equivalent transitions (`:46`, `:81-84`, `:200-207`). It requires work summaries, goal-tracker maintenance, thorough changed-file/test reporting, no direct state cheating, and trust in external review (`:200-207`). The goal tracker contract freezes ultimate goal/acceptance criteria while allowing active tasks/completed/deferred/evolution fields to change (`:186-198`).
- dependencies_and_callers: Depends on the installed runtime root scripts: `setup-rlcr-loop.sh`, `rlcr-stop-gate.sh`, `cancel-rlcr-loop.sh`, `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `validate-gen-plan-io.sh`, and `ask-codex.sh`; external prerequisites are `codex` and `gh` (`:209-212`). It is packaged by `scripts/install-skill.sh`, which hydrates the runtime root into skill copies.
- edge_cases_or_failure_modes: This file is instructional, not an enforcing script; enforcement is delegated to hooks/scripts. It says to maintain `goal-tracker.md` (`:202-203`), while validators block direct goal-tracker modifications after round 0 and require summary-based update requests, so users must follow the hook-mediated path. Default model text can be mode-sensitive: the skill lists RLCR default `gpt-5.2:xhigh` (`:86-90`), while `loop-common.sh:51-60` selects `gpt-5.3-codex` in Claude plugin cache mode and `gpt-5.2` otherwise.
- validation_or_tests: Exercised indirectly by command, stop-gate, skill monitor, and gen-plan tests. Directly linked behavior includes `tests/test-gen-plan.sh` validating the gen-plan command/agent/validator surfaces and `tests/test-stop-gate.sh` validating the skill-mode stop gate wrapper.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6/6 Item Evidence headings present; item IDs intentionally not repeated here to preserve the exactly-once heading invariant
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`