# agent_21 claude/add-dependency-check-tA0P8 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `df26142e5fbed5e2ac3e48f001786cfa77296dda`

## Item Evidence

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-021 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Robustness specification suite for the Humanize loop algorithms. The directory is flat and contains 19 shell scripts, 8,852 total lines, covering setup gating, state parsing, hook validators, template rendering, timeout wrapping, PR-loop API handling, and monitor/parser behavior. It functions as executable boundary documentation for failure modes that normal path tests do not cover.
- algorithmic_behavior: Each script creates isolated temporary fixtures through `tests/test-helpers.sh` and drives production scripts or production library functions. The suite covers base branch selection in `test-base-branch-detection.sh` via a local copy of setup auto-detection logic, selecting remote default branch first, then local `main`, then local `master`, else failure at lines 37-61 and cases at lines 73, 100, 129, 156. It covers cancel authorization in `test-cancel-security-robustness.sh` by calling `is_cancel_authorized` from `hooks/lib/loop-common.sh`, accepting only signal-backed `mv state.md|finalize-state.md cancel-state.md` forms and rejecting shell injection, wrong paths, symlinks, and quote/spacing bypasses at lines 40-406. It covers state reading, active-loop selection, finalization/cancel exclusion, PR-loop state shape, concurrent reads, and strict parser rejection in `test-concurrent-state-robustness.sh` lines 30-558, `test-state-file-robustness.sh` lines 34-480, and `test-state-transition-robustness.sh` lines 30-423. It covers production monitor parsers in `scripts/humanize.sh` through `test-git-operations-robustness.sh` and `test-goal-tracker-robustness.sh`, including dirty git states, malformed goal trackers, binary data, and large counts. It covers hook validator JSON and file-protection behavior through `test-hook-input-robustness.sh` and `test-hook-system-robustness.sh`, driving `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-bash-validator.sh`, `loop-edit-validator.sh`, `loop-plan-file-validator.sh`, and stop hooks. It covers setup path and plan-file validation via `test-path-validation-robustness.sh`, `test-plan-file-robustness.sh`, and `test-setup-scripts-robustness.sh`. It covers template behavior through `test-template-error-robustness.sh` and `test-template-stress-robustness.sh`. It covers PR-loop fetch/poll and API degradation through `test-pr-loop-api-robustness.sh`, with wrapper entrypoints `test-pr-loop-api-fetch.sh` and `test-pr-loop-api-poll.sh`. It covers timeout portability through `test-timeout-robustness.sh`.
- inputs_outputs_state: Inputs are shell arguments, mock git repositories, mock `gh`, mock `codex`, temporary `.humanize/rlcr` and `.humanize/pr-loop` state trees, synthetic JSON hook payloads, plan files, template files, and GitHub-like fixture payloads. Outputs are process exit codes, stdout/stderr messages, generated state files such as `state.md`, `finalize-state.md`, `cancel-state.md`, `approve-state.md`, PR comment output files, and test pass/fail counters from `print_test_summary` in `tests/test-helpers.sh` lines 58-78. State transitions under test include active loop to finalize/cancel/approve state, PR loop active bot updates, trigger timestamp refresh, skip-implementation state creation, dependency-check halt before loop creation, and parser tolerant-vs-strict state extraction.
- gates_or_invariants: The suite enforces that setup scripts reject invalid numeric options, unsafe YAML values, unsafe plan paths, symlinked plans, non-git directories, repos without commits, tracked plan files without explicit allowance, active-loop conflicts, and missing dependencies. Branch-specific dependency-check coverage appears in `tests/robustness/test-setup-scripts-robustness.sh` lines 1077-1209: missing `codex`, missing `jq`, multiple missing dependencies, and all dependencies present. Production RLCR dependency aggregation is in `scripts/setup-rlcr-loop.sh` lines 257-286, checking `codex`, `jq`, and `git` before mutual exclusion and plan-file work. PR-loop prerequisites remain separate checks for `gh`, `gh auth`, and `codex` in `scripts/setup-pr-loop.sh` lines 255-277. Other invariants include single active loop, newest-session selection, `finalize-state.md` precedence over `state.md`, `cancel-state.md` not active, no recursive template expansion, hook protection for generated state/prompt/comment files, and timeout command exit-code preservation.
- dependencies_and_callers: The robustness tests depend on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `hooks/pr-loop-stop-hook.sh`, and hook validators. Several tests create mock binaries in temporary `bin` directories and prepend them to `PATH` to isolate behavior. `tests/run-all-tests.sh` includes robustness suites in the broader parallel test inventory, and robustness wrappers split PR API fetch and poll groups so expensive PR-loop cases can run independently.
- edge_cases_or_failure_modes: Covered failures include command substitution and shell metacharacter injection, newline and IFS manipulation, symlink bypasses, stale or zombie loop directories, simultaneous reads during atomic state rewrites, CRLF and Unicode state data, binary content, missing or malformed YAML frontmatter, permission errors, non-git and detached/rebase/merge/shallow git states, malformed hook JSON, non-UTF8 payloads, large JSON/log/template bodies, plan disappearance, null bytes, path traversal inside `.humanize`, slow or failing `gh` APIs, missing `codex`/`jq`/`git`, and absent timeout implementations.
- validation_or_tests: The directory is itself validation. I inspected it recursively with `find`/`rg`, confirmed it contains only `tests/robustness` as a directory and the 19 shell files listed above, and inspected section/test boundaries plus the dependency-check implementation and tests. I did not execute the full suite because the task asked for research notes only and the workspace is read-only.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-051 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor shutdown behavior when the `.humanize` runtime tree disappears while `humanize monitor rlcr` is running.
- algorithmic_behavior: The script dispatches on `setup`, `delete`, or `cleanup` at lines 23-72. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99`, writes a minimal `state.md` with loop fields, and writes a minimal `goal-tracker.md` with immutable goal/acceptance content at lines 24-45. `delete` removes `.humanize` and instructs the operator to verify that the monitor exits cleanly and restores terminal state at lines 47-56. `cleanup` removes leftover `.humanize` at lines 58-61. Unknown commands print usage and exit 1 at lines 63-70.
- inputs_outputs_state: Input is one positional command. Outputs are filesystem state under `.humanize/rlcr`, console instructions, and deletion of `.humanize`. It simulates transition from an active RLCR monitor session to missing runtime directory.
- gates_or_invariants: The expected monitor gate is user-facing: terminal 2 should show the clean stop message `Monitoring stopped: .humanize/rlcr directory no longer exists`, reset scroll/cursor state, and avoid shell glob errors. The script itself uses `set -euo pipefail` at line 17.
- dependencies_and_callers: Depends on `scripts/humanize.sh` and the `humanize monitor rlcr` command, referenced in usage lines 10-13 and setup instructions lines 43-45. It does not source shared helpers; it uses `mkdir`, `echo`, and `rm`.
- edge_cases_or_failure_modes: It targets deletion of the whole `.humanize` tree during live monitoring, stale terminal scroll regions, cursor placement, and zsh/bash glob failures. It is destructive to `.humanize` in the current project by design, so it must run only in a test workspace.
- validation_or_tests: Manual validation only. No automated assertions; expected observations are described at lines 51-56.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-081 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Top-level PR-loop test runner that composes script, hook, and stop-hook modules into one executable PR-loop specification.
- algorithmic_behavior: It computes `SCRIPT_DIR` and `PROJECT_ROOT` at lines 15-16, sources `tests/test-helpers.sh` and `tests/test-pr-loop-lib.sh` at lines 18-20, initializes a PR-loop test environment with `init_pr_loop_test_env` at line 26, sources the three test modules at lines 32-34, then runs `run_script_tests`, `run_hook_tests`, and `run_stophook_tests` at lines 40-47 before calling `print_test_summary` at line 53.
- inputs_outputs_state: Input is no arguments. Outputs are aggregated pass/fail counters from all sourced modules and any temporary fixture state created by `test-pr-loop-lib.sh`. State includes mocked `gh`/`codex`, PR loop `.humanize/pr-loop` directories, round files, hook JSON payloads, and fixture-backed PR comments/reviews.
- gates_or_invariants: `set -euo pipefail` at line 13 means setup/source errors abort the runner. The runner expects the module functions to exist after sourcing; missing helper/module files fail before any summary. The sequence gates script argument validation before hook validators and stop-hook integration.
- dependencies_and_callers: Depends directly on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh`. Those modules drive `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, hook validators, PR-loop stop hook, and fixtures under `tests/fixtures`.
- edge_cases_or_failure_modes: The runner itself has little branching; failure modes are missing helper files, failed environment setup, module function name drift, or a sourced module aborting under `set -e`. The underlying modules cover bot flags, PR comments, trigger detection, stop-hook gating, force-push detection, bot timeout, and PR-loop file protections.
- validation_or_tests: This is an executable validation aggregator, documented as the main runner at lines 3-10. It provides a single entrypoint for broad PR-loop behavior.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-111 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Static hook/block template defining the user-facing denial message when an actor attempts to write generated PR-loop prompt/comment files.
- algorithmic_behavior: The template states that writes to `round-*-pr-comment.md` or `round-*-prompt.md` under `.humanize/pr-loop/` are blocked at line 3. It explains that `round-*-pr-comment.md` is generated from GitHub PR comments and `round-*-prompt.md` contains current-round instructions at lines 5-8, then declares both read-only and system-managed at line 9.
- inputs_outputs_state: Input is a hook decision that selects this block. Output is markdown text returned to the user/agent. It does not mutate state; it reinforces the invariant that generated PR-loop prompt/comment artifacts are read-only.
- gates_or_invariants: Protects `.humanize/pr-loop` generated files from direct writes. This aligns with `hooks/lib/loop-common.sh` PR-loop read-only classification and validators tested in `tests/test-pr-loop-hooks.sh` and `tests/robustness/test-hook-system-robustness.sh`.
- dependencies_and_callers: Loaded through the template system in `hooks/lib/template-loader.sh`, where `load_and_render_safe` provides fallback handling at lines 167-203. Caller is expected to be a hook validator path that blocks `Write`, `Edit`, or Bash-based modifications to PR-loop generated files.
- edge_cases_or_failure_modes: The template is intentionally variable-free, so rendering cannot corrupt it through substitution. Misclassification risk would be if a valid writable file such as `round-*-pr-resolve.md` were routed to this block; that is covered separately by PR resolve round validation tests.
- validation_or_tests: Template availability and reference behavior are covered broadly by template tests; PR-loop write protections are covered in PR-loop hook tests and robustness hook-system tests.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-141 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Plan-generation contract template that structures implementation goals, acceptance criteria, quality bounds, allowed choices, dependency sequencing, and code-style restrictions for downstream loop execution.
- algorithmic_behavior: The template requires a goal description at lines 3-4 and TDD-style acceptance criteria with positive and negative tests at lines 6-23. It defines path boundaries as upper bound, lower bound, and allowed choices at lines 25-44, including a note that deterministic designs may collapse upper/lower choices. It separates non-prescriptive feasibility hints from requirements at lines 46-55. It asks for milestone/dependency sequencing at lines 57-67. It ends with implementation notes prohibiting plan-specific workflow markers such as `AC-`, `Milestone`, `Step`, or `Phase` in implementation code/comments at lines 69-74.
- inputs_outputs_state: Input is a draft goal or request consumed by a plan generator. Output is a structured markdown plan. The state effect is indirect: generated plans become inputs to setup validation and RLCR loop execution, including plan-file validation in `scripts/setup-rlcr-loop.sh`.
- gates_or_invariants: Enforces deterministic verification through positive and negative tests, bounded scope through explicit upper/lower limits, and separation of plan vocabulary from production code. It also distinguishes requirements from suggestions so the execution loop can avoid treating hints as mandatory algorithmic transitions.
- dependencies_and_callers: Likely loaded by plan-generation paths and validated by template/reference tests. It relates to `scripts/validate-gen-plan-io.sh`, `tests/test-gen-plan.sh`, `tests/test-template-loader.sh`, and plan-file robustness tests because generated plans must satisfy minimum content, path, and validation requirements before loop setup.
- edge_cases_or_failure_modes: Poorly filled placeholders could create vague acceptance criteria, missing negative tests, or implementation code polluted with plan labels. Deterministic designs are explicitly handled by the note at lines 44-44 so generators should not invent unnecessary choices.
- validation_or_tests: The template is static, but downstream plan validation is exercised by `tests/robustness/test-plan-file-robustness.sh` and setup validation tests. Template-loading behavior is covered by `hooks/lib/template-loader.sh` and robustness template suites.
- skip_candidate: `no`

### CLAUDE__ADD_DEPENDENCY_CHECK_TA0P8-HZ-171 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Focused robustness specification for template-loader failure behavior under malformed files, missing templates, invalid placeholder names, unusual values, filesystem edge cases, and concurrent reads.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` and `tests/test-helpers.sh` at lines 17-18, creates a temp directory at line 20, then executes 20 numbered checks. Missing template and missing directory must return the fallback via `load_and_render_safe` at lines 34-51. Empty templates may return empty or fallback at lines 53-63. Malformed placeholders such as unclosed braces, nested placeholders, only opening braces, and empty names must not crash and should return non-empty output at lines 73-115. Invalid variable names with spaces, special characters, and numeric names must not crash at lines 125-155. Values containing template syntax must not recursively expand, with an exact expectation of `Result: {{OTHER}}` at lines 165-174; newline values must be preserved at lines 176-186; long variable names must be handled at lines 188-198. Filesystem cases cover BOM, whitespace-only templates, filenames with spaces, permission denied fallback, subdirectories, and symlinks at lines 208-260 and 309-334. Concurrent loading starts 10 background jobs against the same template and requires zero failures plus file preservation at lines 270-299.
- inputs_outputs_state: Inputs are temporary template files, template strings, fallback strings, and `VAR=value` assignments. Outputs are rendered strings from `render_template` and `load_and_render_safe`, pass/fail counters, and temporary files under `$TEST_DIR`. The only concurrent state is shared read access to `$TEST_DIR/concurrent.md`.
- gates_or_invariants: Template rendering must be single-pass, tolerate malformed syntax, preserve missing placeholders or fallback safely, avoid recursive placeholder injection, preserve file content under concurrent reads, and handle inaccessible or odd files without crashing. These map directly to `hooks/lib/template-loader.sh`: variable syntax and single-pass contract at lines 7-14, awk-based replacement at lines 58-132, and fallback behavior at lines 167-203.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` functions `render_template`, `load_template`, and `load_and_render_safe`, plus `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh`. It coordinates with `tests/robustness/test-template-stress-robustness.sh`, which covers large values/files, many substitutions, regex/sed-special characters, Unicode, and repeated placeholders.
- edge_cases_or_failure_modes: Missing files, missing directories, empty files, unclosed/nested placeholders, illegal or unusual variable names, values containing `{{...}}`, multiline values, 500-character variable names, UTF-8 BOM, whitespace-only content, filenames with spaces, unreadable templates, concurrent access races, nested subdirectories, and symlinked template files. Permission and symlink cases are environment-dependent and pass with alternate acceptable outcomes when the platform does not support the exact condition.
- validation_or_tests: The file ends with `print_test_summary "Template Error Robustness Test Summary"` and exits with that status at lines 336-341. I inspected the file directly with line numbers; I did not execute it in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6/6 item sections present; each assigned heading appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`