# agent_18 add-shell-syntax-check-cicd 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `6e0eebdb803522cd4be735589be4d1d76e8e536e`

## Item Evidence

### ADD_SHELL_SYNTAX_CHECK_CICD-HZ-018 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script for `/humanize:start-rlcr-loop`. It initializes a new RLCR session under `.humanize-loop.local/<timestamp>/`, validates the user-provided implementation plan, persists loop configuration into `state.md`, creates the round-0 `goal-tracker.md`, creates the initial round-0 prompt, and prints the prompt plus completion instructions back to the current Claude session. It does not run Codex itself; the Stop hook consumes this setup state later. The slash-command wrapper invokes it directly from [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/commands/start-rlcr-loop.md:12).
- algorithmic_behavior: The script uses strict shell mode with `set -euo pipefail` at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/scripts/setup-rlcr-loop.sh:11). It initializes defaults for Codex model, effort, timeout, and max iterations at lines 17-21, then walks CLI arguments in a `while`/`case` parser at lines 93-157. `--max` and `--codex-timeout` require numeric values, `--codex-model` accepts `MODEL:EFFORT` or just `MODEL`, and `--push-every-round` flips a boolean stored in state. After validation, relative plan paths are resolved against `${CLAUDE_PROJECT_DIR:-$(pwd)}` at lines 173-177. It then creates `.humanize-loop.local/<YYYY-MM-DD_HH-MM-SS>/` at lines 207-214, writes frontmatter state at lines 223-234, creates a goal tracker at lines 240-328, and creates `round-0-prompt.md` at lines 334-387. The generated prompt embeds the plan content, instructs Claude to initialize the goal tracker, maintain task/AC alignment, commit changes, and write `round-0-summary.md`. If `--push-every-round` is enabled, it appends a push requirement at lines 389-395 and prints matching completion instructions at lines 430-440.
- inputs_outputs_state: Inputs are one required plan file path plus optional `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round` flags as documented at lines 7-9 and 45-52. It also reads `CLAUDE_PROJECT_DIR` when resolving project root and relative paths at lines 173-177 and 207. It reads the plan file multiple times: line count validation at lines 185-193, full content capture at line 241, heuristic goal extraction with `grep`/`sed` at lines 263-275, acceptance criteria extraction at lines 285-291, and full embedding into the prompt at line 358. Outputs are a new loop directory, `state.md`, `goal-tracker.md`, `round-0-prompt.md`, and an expected but initially absent `round-0-summary.md` path that Claude must write. The persisted state frontmatter stores `current_round: 0`, max iterations, Codex model/effort/timeout, push mode, absolute plan path, and UTC start time at lines 223-234. Later, [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-codex-stop-hook.sh:326) parses these same frontmatter keys and advances `current_round` with a temp-file rewrite when Codex finds more work at lines 864-867.
- gates_or_invariants: The setup gate requires exactly one plan file, rejects missing plan arguments at lines 163-171, rejects multiple positional plan files at lines 147-153, rejects unknown options at lines 141-145, rejects nonexistent plan files at lines 179-183, rejects plans with fewer than 5 lines at lines 185-193, and requires `codex` on `PATH` at lines 195-201. Numeric gates for `--max` and `--codex-timeout` use `^[0-9]+$` at lines 103-106 and 130-133, although the error text says positive integer while `0` technically passes. Important loop invariants are embedded into generated files: the immutable goal/AC section is only for round 0 at lines 257-281 and reinforced in the prompt at lines 339-349; Claude is told not to edit loop state or cancel the loop at line 380; and completion requires a commit plus a summary written to the exact summary path at lines 382-386 and 430-451. Active loop selection later only considers the newest timestamped loop directory with `state.md`, per [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/lib/loop-common.sh:11), which makes timestamp directory naming part of the state invariant.
- dependencies_and_callers: Direct caller is `/humanize:start-rlcr-loop`, whose command file allows only this Bash script and forwards `$ARGUMENTS` at [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/commands/start-rlcr-loop.md:3) and lines 12-13. Runtime consumers are the hook suite registered in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/hooks.json:4), especially the Stop hook at lines 42-49. The Stop hook finds `.humanize-loop.local`, reads `state.md`, requires the current round summary, runs Codex with the configured model/effort/timeout, and either deletes `state.md` on `COMPLETE`/`STOP` or writes the next prompt and blocks exit. Relevant consumer lines include state parse at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-codex-stop-hook.sh:326), Codex args at lines 724-731, Codex execution at line 750, completion detection at lines 813-828, stop detection at lines 830-857, and next-round prompt generation at lines 864-975. Shared path/state helpers live in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/lib/loop-common.sh:15). Operational dependencies inside the setup script are standard POSIX-ish tools plus Bash features: `date`, `mkdir`, `wc`, `tr`, `grep`, `sed`, `head`, `tail`, `cat`, and `command -v codex`.
- edge_cases_or_failure_modes: `--max 0` and `--codex-timeout 0` pass the regex despite positive-integer messaging, which could make max-iteration or timeout behavior surprising. `--codex-model :effort` or `model:` also pass the colon parser and can persist an empty model or effort because the script does not validate either side at lines 115-122. YAML/frontmatter values are written unescaped at lines 223-234, so paths or option values containing unusual characters could make state parsing fragile; the Stop hook later uses simple `grep`/`sed` extraction. Goal and acceptance extraction are heuristic and only recognize level-2 headings with limited names at lines 263-286; if the plan uses other heading levels or wording, the tracker falls back to placeholders. Timestamp-based loop dirs use second precision at lines 210-212, so two setup invocations in the same second would target the same directory. The script sets `PLAN_CONTENT=$(cat "$PLAN_FILE")` at line 241 but never uses that variable; this is harmless but redundant. The hard `codex` prerequisite at lines 195-201 means setup fails even though Codex is not invoked until the Stop hook. All state output is local under `.humanize-loop.local`, so if that directory is not ignored by the host repo, generated operational files could be accidentally visible to git.
- validation_or_tests: I ran `bash -n scripts/setup-rlcr-loop.sh`; it exited 0 with no syntax errors. The file is executable and has the expected branch-export size, shown by `ls -l` as `-rwxr-xr-x ... 14448 ... scripts/setup-rlcr-loop.sh`. I also inspected the command wrapper, Stop hook, shared loop helpers, and hook registration to confirm the setup outputs are consumed by the runtime loop rather than being standalone dead state.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 Item Evidence section present for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`