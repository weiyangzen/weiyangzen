# agent_12 add-shell-syntax-check-cicd 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `6e0eebdb803522cd4be735589be4d1d76e8e536e`

## Item Evidence

### ADD_SHELL_SYNTAX_CHECK_CICD-HZ-012 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook controller for the RLCR loop. It is registered as the Claude Code `Stop` lifecycle hook in `hooks/hooks.json:42-49` with command `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` and a 7200-second hook timeout. Its job is to intercept attempted session termination while an active `.humanize-loop.local/<timestamp>/state.md` exists, enforce pre-review completion gates, invoke Codex as an independent reviewer, and either allow exit, stop the loop, or block exit with next-round instructions. It is core loop state-machine code rather than a passive utility.
- algorithmic_behavior: The hook reads the full stop-hook JSON payload from stdin into `HOOK_INPUT` (`hooks/loop-codex-stop-hook.sh:29`), resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, loads shared helpers from `hooks/lib/loop-common.sh`, and finds the newest active loop via `find_active_loop` (`hooks/loop-codex-stop-hook.sh:43-50`; helper at `hooks/lib/loop-common.sh:15-33`). If no active loop or no state file exists, it exits 0 and allows Claude to stop (`hooks/loop-codex-stop-hook.sh:52-57`, `322-324`).

  The main control path is a gated review pipeline. First, it runs a native TodoWrite transcript check through `hooks/check-todos-from-transcript.py` and blocks immediately if any latest todo status is not `completed` (`hooks/loop-codex-stop-hook.sh:65-99`; checker behavior at `hooks/check-todos-from-transcript.py:17-84`, `109-122`). Second, it scans changed git files and blocks if any code or documentation file exceeds 2000 lines (`hooks/loop-codex-stop-hook.sh:108-197`). Third, it blocks dirty git worktrees and gives special guidance for `.humanize-loop.local` or other untracked artifacts (`hooks/loop-codex-stop-hook.sh:205-277`). Fourth, when `push_every_round: true` is set in state, it blocks local commits that are ahead of remote (`hooks/loop-codex-stop-hook.sh:280-315`). These are cheap refusal gates before expensive Codex review.

  After the quick gates, it parses YAML-like frontmatter from `state.md` for `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, and `plan_file` (`hooks/loop-codex-stop-hook.sh:326-343`). It validates `current_round` as numeric and removes the state file to stop the loop if corrupted (`hooks/loop-codex-stop-hook.sh:344-349`); invalid `max_iterations` falls back to 42 (`hooks/loop-codex-stop-hook.sh:351-354`). It requires `round-N-summary.md` to exist before review (`hooks/loop-codex-stop-hook.sh:360-392`). On round 0, it also requires the generated `goal-tracker.md` placeholders to be replaced before Codex review (`hooks/loop-codex-stop-hook.sh:398-465`). If `CURRENT_ROUND + 1` exceeds `MAX_ITERATIONS`, it removes `state.md` and allows exit even without completion (`hooks/loop-codex-stop-hook.sh:471-477`).

  The Codex prompt is generated as either a regular review or a full alignment review. Every fifth round where `CURRENT_ROUND % 5 == 4`, `FULL_ALIGNMENT_CHECK=true` and the prompt forces a comprehensive goal alignment audit, acceptance-criteria audit, forgotten-item scan, deferral audit, stagnation check, and strict `COMPLETE`/`STOP` terminal-word rules (`hooks/loop-codex-stop-hook.sh:516-620`). Other rounds generate a regular review prompt requiring comparison against the original plan, implementation review, goal alignment check, and goal-tracker update handling (`hooks/loop-codex-stop-hook.sh:621-681`). Both prompt variants embed Claude’s summary and instruct Codex to update `goal-tracker.md` after round 0 only through Codex-approved update requests (`hooks/loop-codex-stop-hook.sh:497-514`, `670-681`).

  Codex execution writes debug material outside the project tree under `$HOME/.cache/humanize/<sanitized-project-path>/<loop timestamp>/` to avoid context pollution (`hooks/loop-codex-stop-hook.sh:688-703`). It sources `scripts/portable-timeout.sh` when present, otherwise defines a fallback timeout wrapper (`hooks/loop-codex-stop-hook.sh:704-722`; portable implementation at `scripts/portable-timeout.sh:9-71`). The command uses `codex exec -m "$CODEX_MODEL"`, optional `-c model_reasoning_effort=...`, `--full-auto`, and `-C "$PROJECT_ROOT"` (`hooks/loop-codex-stop-hook.sh:724-731`). The prompt content is passed as a command argument to `codex exec`, stdout and stderr are redirected to cache logs, and the exit code is captured without aborting the hook (`hooks/loop-codex-stop-hook.sh:733-755`).

  Codex is expected to create `round-N-review-result.md`; if not, the hook copies nonempty Codex stdout into that file as a fallback (`hooks/loop-codex-stop-hook.sh:757-765`). Missing review output blocks exit with debug file paths and the last 50 stderr lines (`hooks/loop-codex-stop-hook.sh:771-808`). If a review result exists, the last nonempty trimmed line controls the state transition: exact `COMPLETE` removes `state.md` and allows exit (`hooks/loop-codex-stop-hook.sh:813-827`); exact `STOP` removes `state.md` and allows exit while printing circuit-breaker diagnostics (`hooks/loop-codex-stop-hook.sh:830-858`); any other result advances `current_round` in `state.md`, creates `round-(N+1)-prompt.md` from the review feedback, and returns a JSON block decision to continue the loop (`hooks/loop-codex-stop-hook.sh:864-975`).
- inputs_outputs_state: Inputs are the Claude Stop hook JSON on stdin, the active loop directory under `.humanize-loop.local`, `state.md` frontmatter, `round-N-summary.md`, `round-N-prompt.md`, optional `goal-tracker.md`, optional transcript path from hook input, git status, and environment variables including `CLAUDE_PROJECT_DIR` and optional `CODEX_TIMEOUT` fallback (`hooks/loop-codex-stop-hook.sh:29`, `43-48`, `65-70`, `326-343`). The state file format is seeded by `scripts/setup-rlcr-loop.sh:223-234`, with fields consumed by this hook: `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, and `plan_file`.

  Outputs include Claude hook JSON decisions via `jq`: `"decision": "block"` plus a human-readable `reason` and `systemMessage` for incomplete todos, large files, dirty git state, unpushed commits, missing summary, uninitialized goal tracker, Codex failure, or review feedback (`hooks/loop-codex-stop-hook.sh:90-98`, `187-195`, `268-276`, `305-313`, `383-391`, `455-463`, `799-807`, `966-973`). It also writes `round-N-review-prompt.md`, `round-N-review-result.md` if stdout fallback is needed, cache debug files, updated `state.md`, and the next round prompt (`hooks/loop-codex-stop-hook.sh:491-525`, `700-744`, `759-764`, `864-876`). Exit 0 is used for both allow and block hook decisions because Claude Code consumes the JSON decision body; validator-style sibling hooks use exit 2 for PreToolUse blocks.

  State transitions are: no active loop -> allow stop; gate failure -> block without changing `current_round`; max iteration reached -> delete `state.md` and allow stop; Codex `COMPLETE` -> delete `state.md` and allow stop; Codex `STOP` -> delete `state.md` and allow stop; Codex findings -> replace `current_round: N` with `current_round: N+1`, create `round-(N+1)-prompt.md`, and block stop. The loop directory itself remains as historical evidence after `state.md` deletion.
- gates_or_invariants: Active-loop detection only honors the newest timestamp-named directory with `state.md`, preventing older zombie loops from being revived (`hooks/lib/loop-common.sh:11-33`). The hook deliberately does not check `stop_hook_active`, because a continued loop iteration is expected to trigger another stop review (`hooks/loop-codex-stop-hook.sh:31-37`).

  Required invariants before Codex review are: all latest native todos are completed; no changed code/docs file exceeds `MAX_LINES=2000`; git working tree has no staged, unstaged, or untracked changes that need classification; if `push_every_round` is true, branch status must not be ahead; `current_round` must be numeric; a matching `round-N-summary.md` must exist; and round 0 must initialize the goal tracker by replacing placeholders (`hooks/loop-codex-stop-hook.sh:60-197`, `200-315`, `344-465`). The last-line parser is intentionally strict: only the last nonempty line exactly equal to `COMPLETE` or `STOP`, after whitespace trimming, has terminal meaning, avoiding false positives such as “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:813-818`). Deferred tasks are treated as incomplete in both regular and alignment review prompts (`hooks/loop-codex-stop-hook.sh:613-618`, `674-680`).

  State-file ownership is protected by sibling validators. Write/Edit/Bash validators block direct `state.md` edits; prompt files are read-only; summary files must match the active round; `goal-tracker.md` is writable by Claude only during round 0, then updates must be requested in the summary for Codex to approve (`hooks/loop-write-validator.sh:92-104`, `139-170`; `hooks/loop-edit-validator.sh:72-84`, `91-113`; `hooks/loop-bash-validator.sh:74-123`). Read validation blocks stale prompt/summary access through normal Read tool calls, although it explicitly suggests `cat` as an escape when historical files are needed (`hooks/loop-read-validator.sh:98-149`).
- dependencies_and_callers: Direct caller is Claude Code’s Stop hook registry in `hooks/hooks.json:42-49`. The hook depends on `jq`, `python3` for todo transcript checking, `git` for repository gates, `codex` for review execution, standard Unix tools (`sed`, `grep`, `tail`, `wc`, `find`, `cp`, `mv`, `date`, `mkdir`), and optional timeout implementations through `scripts/portable-timeout.sh` (`hooks/loop-codex-stop-hook.sh:65-70`, `110-157`, `205-315`, `704-751`). It sources `hooks/lib/loop-common.sh` for active-loop lookup and lowercase conversion (`hooks/loop-codex-stop-hook.sh:46-50`, `132-135`). It coordinates with `scripts/setup-rlcr-loop.sh`, which creates the loop directory, seeds `state.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:207-234`, `240-328`, `334-387`). It also coordinates with `scripts/humanize.sh`, whose monitor searches the same cache path convention for `round-*-codex-run.log` files (`scripts/humanize.sh:43-55`, `75-95`). The README documents this lifecycle and file layout at `README.md:241-250`.
- edge_cases_or_failure_modes: If `hooks/check-todos-from-transcript.py` is missing, unreadable, receives invalid JSON, lacks a transcript path, or sees no TodoWrite entries, the todo gate allows progress rather than blocking (`hooks/loop-codex-stop-hook.sh:65-100`; checker at `hooks/check-todos-from-transcript.py:87-107`). The dirty-git gate depends on `git status --porcelain`; in restricted environments git itself can fail for external cache/temp reasons, so operational behavior depends on normal developer environment permissions. The large-file gate only checks changed files reported by porcelain status and only selected extensions; unchanged oversized files are not blocked (`hooks/loop-codex-stop-hook.sh:110-157`). Rename parsing handles `old -> new`, deleted files are skipped, and extensions are lowercased (`hooks/loop-codex-stop-hook.sh:119-150`).

  State corruption is handled asymmetrically: invalid `current_round` deletes the state file and stops the loop; invalid `max_iterations` silently becomes 42 (`hooks/loop-codex-stop-hook.sh:344-354`). If the timeout script is absent and no timeout binary exists, Codex may run without a real timeout through the fallback wrapper (`hooks/loop-codex-stop-hook.sh:704-721`). If Codex exits nonzero but still writes or prints review output, the hook may proceed based on that output; nonzero exit is surfaced only when no review result can be produced (`hooks/loop-codex-stop-hook.sh:749-808`). The state increment uses a broad `sed "s/^current_round: .*/..."` replacement, so unusual spacing or duplicate `current_round` keys could produce ambiguous edits (`hooks/loop-codex-stop-hook.sh:864-867`). `STOP` is honored even on non-alignment rounds, but logged as unexpected (`hooks/loop-codex-stop-hook.sh:846-857`).
- validation_or_tests: I ran a read-only shell syntax check for the assigned file: `bash -n hooks/loop-codex-stop-hook.sh`, which exited successfully with no output. The branch includes `.github/workflows/shell-syntax-check.yml`, which discovers all `*.sh` files and checks each with both `bash -n` and `zsh -n` on push and pull request (`.github/workflows/shell-syntax-check.yml:1-8`, `19-60`). This CI workflow covers the assigned stop hook because it matches `*.sh`. No behavioral unit test for the RLCR Stop state machine was found in the inspected paths; validation appears to be shell syntax CI plus runtime hook behavior. A read-only `git status` attempt in this sandbox was not used as evidence because the environment could not create an external xcrun cache file under `/tmp`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence section present; assigned count is 1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`