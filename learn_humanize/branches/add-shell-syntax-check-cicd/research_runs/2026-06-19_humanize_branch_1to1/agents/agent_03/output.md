# agent_03 add-shell-syntax-check-cicd 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `6e0eebdb803522cd4be735589be4d1d76e8e536e`

## Item Evidence

### ADD_SHELL_SYNTAX_CHECK_CICD-HZ-003 `directory` `hooks`
- cursor: `[_]`
- core_role: `hooks` is the installed Claude Code plugin hook surface for the Humanize RLCR loop. It dispatches PreToolUse validators for `Write`, `Edit`, `Read`, and `Bash`, plus a `Stop` hook that runs Codex review before the agent can exit. The directory is core algorithm code, not support-only, because it enforces loop state invariants, controls iteration state transitions, gates tool access, and coordinates Codex review. Dispatcher wiring is in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/hooks.json:4): PreToolUse hooks at lines 4-40 and Stop hook at lines 42-49.
- algorithmic_behavior: The directory has eight recursively inspected files: `hooks.json`, five shell hook scripts, `hooks/lib/loop-common.sh`, and `check-todos-from-transcript.py`. [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/lib/loop-common.sh:15) provides shared primitives: newest-loop selection at lines 15-33, state frontmatter round extraction at lines 37-47, round filename matching/extraction at lines 54-73, protected path checks at lines 160-176, shell-command write-detection regexes at lines 181-201, and standard block messages at lines 77-158 and 203-235. [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-read-validator.sh:22) parses hook JSON, blocks `round-*-todos.md`, then validates reads of current prompt/summary files against the active loop directory and current round at lines 36-39, 67-75, 98-111, 118-130, and 137-148. [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-write-validator.sh:23) blocks todos and prompt writes, protects `state.md`, blocks `goal-tracker.md` after round 0, enforces summary location/current round, and requires exact active-loop path at lines 37-45, 78-86, 92-105, 111-120, 139-154, and 161-171. [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-edit-validator.sh:22) applies the same policy to edits, with loop-scoped validation at lines 50-66 and protected-file/current-round checks at lines 72-113. [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-bash-validator.sh:22) blocks shell bypasses: unauthorized `git push` when `push_every_round` is false at lines 53-66, shell writes to `state.md` at lines 74-77, `goal-tracker.md` at lines 85-94, prompt files at lines 101-104, summaries at lines 111-114, and todos at lines 121-123. [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/check-todos-from-transcript.py:17) scans the Claude transcript JSONL for the latest `TodoWrite` call across three transcript shapes at lines 48-83 and emits `INCOMPLETE_TODOS` with exit code 1 when any latest todo is not `completed` at lines 109-122. [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/hooks/loop-codex-stop-hook.sh:29) is the main loop coordinator: it checks incomplete todos before expensive review at lines 65-99, blocks large changed files over 2000 lines at lines 108-197, requires a clean git tree and optionally pushed commits at lines 206-315, parses `state.md` at lines 327-342, requires current summary at lines 360-392, verifies round-0 goal tracker initialization at lines 398-465, stops on max iterations at lines 471-477, builds regular or full-alignment Codex review prompts at lines 491-681, runs `codex exec` under timeout at lines 690-751, accepts stdout fallback at lines 757-764, gates on exact final `COMPLETE` or `STOP` at lines 813-858, and otherwise advances `current_round` plus writes the next prompt at lines 864-974.
- inputs_outputs_state: Inputs are Claude hook JSON on stdin (`tool_name`, `tool_input.file_path`, `tool_input.command`, `transcript_path`), `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_ROOT`, `.humanize-loop.local/<timestamp>/state.md`, current round prompt/summary files, `goal-tracker.md`, `plan_file`, git status, and Codex CLI availability. PreToolUse validators output allow/deny through process exit: `0` allows; `2` blocks with stderr guidance. The Stop hook outputs Claude hook JSON with `"decision": "block"` when work must continue, or exits `0` to allow stopping. Runtime state lives under `.humanize-loop.local/<timestamp>/`: `state.md`, `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, and `goal-tracker.md`. Stop-hook debug outputs go to `$HOME/.cache/humanize/<sanitized-project>/<loop-timestamp>/` at lines 690-702. State transitions are: no active loop allows exit; incomplete todos/dirty git/missing summary/invalid goal tracker block; `COMPLETE` removes `state.md`; `STOP` removes `state.md`; review findings increment `current_round` and create `round-(N+1)-prompt.md`.
- gates_or_invariants: The newest timestamped loop is the only active loop, preventing older `state.md` files from reviving stale sessions (`loop-common.sh:15-33`). Todos files are never read/written directly; native `TodoWrite` is required (`loop-common.sh:77-90`, validators above). Prompt files are read-only to Claude. Summaries must be for the current round and exact active directory. `state.md` is managed by the loop system only. `goal-tracker.md` is directly writable only in round 0; after that, updates must be requested in summaries and reviewed by Codex. Bash write detection protects against common redirection, `tee`, in-place edit, copy/move, and `dd of=` bypasses (`loop-common.sh:181-201`). Stop review completion requires the last non-empty review line to be exactly `COMPLETE`; stagnation stop requires exactly `STOP` (`loop-codex-stop-hook.sh:813-858`). Full alignment review runs every fifth round when `current_round % 5 == 4` (`loop-codex-stop-hook.sh:516-520`). CI adds a branch-level invariant that every `*.sh` parses under both Bash and Zsh: [`.github/workflows/shell-syntax-check.yml`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/.github/workflows/shell-syntax-check.yml:19) finds shell scripts at lines 19-27, runs `bash -n` at lines 29-44, and runs `zsh -n` at lines 46-61.
- dependencies_and_callers: Installed hook entrypoints are called through `hooks/hooks.json` by Claude Code. The user-facing `/humanize:start-rlcr-loop` command delegates to [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/commands/start-rlcr-loop.md:4), which allows only `scripts/setup-rlcr-loop.sh` and invokes it at lines 12-14. [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/scripts/setup-rlcr-loop.sh:223) creates the loop state file at lines 223-234 and goal tracker at lines 240 onward; those are the state surfaces consumed by the hooks. Stop hook optionally sources [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/add-shell-syntax-check-cicd/scripts/portable-timeout.sh:10), whose timeout backend selection is at lines 10-27 and `run_with_timeout` implementation is at lines 33-71. External tools used by hooks include `jq`, `sed`, `grep`, `tr`, `wc`, `git`, `python3`, `codex`, and optionally GNU `timeout`/`gtimeout`. Plugin metadata in `.claude-plugin/plugin.json` declares the package as the `humanize` plugin, making this hook directory part of the distributed plugin surface.
- edge_cases_or_failure_modes: With no active loop directory, validators and Stop hook allow the action, so hooks are inert outside RLCR sessions. `find_active_loop` intentionally ignores older loop directories even if they still have state, which prevents zombie loops but makes only the newest timestamp recoverable. Shell validators rely on `jq` under `set -euo pipefail`; malformed hook JSON or missing `jq` can fail the validator rather than produce a graceful allow/block. Bash command write detection is regex-based and can miss indirect writes through variables, generated scripts, quoted edge cases, embedded interpreters, or multi-command constructs; it can also false-positive on suspicious but non-mutating text. Stop hook’s git checks depend on a real `.git` checkout and porcelain parsing; this read-only export does not include `.git`, so local commit/diff validation was unavailable. Large-file checking only considers files surfaced by `git status --porcelain` and selected code/doc extensions. The CI shell syntax workflow checks `*.sh` only; it does not validate Python AST, JSON, semantic shell behavior, or shellcheck-style warnings. `STOP` is honored even during non-alignment rounds, with an “unexpected circuit breaker” path (`loop-codex-stop-hook.sh:830-858`). Invalid `current_round` causes Stop hook to delete `state.md` and stop the loop (`loop-codex-stop-hook.sh:344-349`).
- validation_or_tests: Read-only validation performed in this export: `bash -n` passed for `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/lib/loop-common.sh`; `zsh -n` passed for the same six shell files; `jq empty hooks/hooks.json` passed; Python AST parsing passed for `hooks/check-todos-from-transcript.py` using `python3 -c` after avoiding here-doc temp-file creation blocked by the sandbox. `shellcheck` was not available in PATH. Repository-local CI validation for this branch is `.github/workflows/shell-syntax-check.yml`, which will syntax-check all `*.sh` files under Bash and Zsh on every push and pull request.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence heading present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`