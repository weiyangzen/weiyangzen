# agent_10 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: This directory is the hook-block message contract for RLCR loop guardrails. It contains 27 Markdown templates, 11039 bytes total, used to explain why read, write, bash, stop, state-schema, git, todo, plan, and goal-tracker transitions are blocked. The files are not executable by themselves; they become algorithmically active when hook scripts render them into stderr messages or JSON `decision: "block"` responses.
- algorithmic_behavior: The directory is consumed through `hooks/lib/template-loader.sh`. `load_template` reads `prompt-template/<name>` and returns empty output on missing files; `render_template` performs single-pass `{{VAR}}` substitution; `load_and_render_safe` falls back to inline messages if the template is missing or renders empty (`hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:170`). The single-pass renderer intentionally does not recursively expand placeholders embedded in values, and unresolved placeholders remain as-is (`hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`). The directory coordinates with `prompt-template/codex` and `prompt-template/claude` because `validate_template_dir` requires all three subdirectories (`hooks/lib/template-loader.sh:216`).
- inputs_outputs_state: Inputs are template path names plus variable assignments such as `CURRENT_ROUND`, `CORRECT_PATH`, `FIELD_NAME`, `GIT_ISSUES`, `SPECIAL_NOTES`, `INCOMPLETE_LIST`, `MAX_LINES`, `LARGE_FILES`, `AHEAD_COUNT`, `CURRENT_BRANCH`, `GOAL_TRACKER_FILE`, and `SUMMARY_FILE`. Outputs are rendered Markdown block reasons, either printed to stderr by tool validators with exit code 2, or embedded as JSON block reasons by the stop hook and plan-file validator. State transitions are indirect: the templates do not mutate state, but they halt transitions that would read the wrong round, write protected loop files, exit before required work is complete, push when not allowed, commit `.humanize`, or continue with outdated schema.
- gates_or_invariants: The directory defines the user-facing side of these invariants: round-scoped files must match the active loop directory and current round (`wrong-file-location.md`, `wrong-round-file.md`, `wrong-round-number.md`, `wrong-directory-path.md`); prompt/state/finalize-state/todos files are protected (`prompt-file-write.md`, `state-file-modification.md`, `finalize-state-file-modification.md`, `todos-file-access.md`); summaries must be written to the current loop path (`wrong-summary-location.md`, `summary-bash-write.md`, `work-summary-missing.md`); plan backups and original plans are protected while the loop is active (`plan-backup-protected.md`, `plan-file-modified.md`); goal tracker writes are constrained by round phase (`goal-tracker-bash-write.md`, `goal-tracker-modification.md`, `goal-tracker-not-initialized.md`); git and repository-exit gates must verify clean, pushed, and reviewable state (`git-push.md`, `git-add-humanize.md`, `git-not-clean*.md`, `git-status-failed.md`, `unpushed-commits.md`, `large-files.md`); Codex review failure is surfaced with debug file pointers (`codex-review-failed.md`).
- dependencies_and_callers: Shared message wrappers in `hooks/lib/loop-common.sh` load common block templates for todos, prompt writes, state writes, finalize-state writes, summary bash writes, goal-tracker bash writes, `.humanize` git-add blocking, and post-round-0 goal-tracker blocking (`hooks/lib/loop-common.sh:347`, `hooks/lib/loop-common.sh:358`, `hooks/lib/loop-common.sh:367`, `hooks/lib/loop-common.sh:376`, `hooks/lib/loop-common.sh:385`, `hooks/lib/loop-common.sh:396`, `hooks/lib/loop-common.sh:742`, `hooks/lib/loop-common.sh:802`). Read validation loads wrong-location, wrong-round, and wrong-directory templates (`hooks/loop-read-validator.sh:119`, `hooks/loop-read-validator.sh:135`, `hooks/loop-read-validator.sh:156`). Write and edit validators load plan, summary-location, wrong-round, wrong-directory, and goal-tracker blocks (`hooks/loop-write-validator.sh:151`, `hooks/loop-write-validator.sh:164`, `hooks/loop-write-validator.sh:174`, `hooks/loop-write-validator.sh:200`, `hooks/loop-write-validator.sh:226`; `hooks/loop-edit-validator.sh:103`, `hooks/loop-edit-validator.sh:117`, `hooks/loop-edit-validator.sh:127`). Bash validation loads git-push and plan-backup blocks and uses shared state/goal-tracker blockers (`hooks/loop-bash-validator.sh:82`, `hooks/loop-bash-validator.sh:314`). The stop hook loads plan-modified, incomplete-todos, git-status-failed, large-files, git-not-clean, unpushed-commits, work-summary-missing, and goal-tracker-not-initialized templates (`hooks/loop-codex-stop-hook.sh:247`, `hooks/loop-codex-stop-hook.sh:297`, `hooks/loop-codex-stop-hook.sh:335`, `hooks/loop-codex-stop-hook.sh:410`, `hooks/loop-codex-stop-hook.sh:473`, `hooks/loop-codex-stop-hook.sh:504`, `hooks/loop-codex-stop-hook.sh:539`, `hooks/loop-codex-stop-hook.sh:619`).
- edge_cases_or_failure_modes: Missing templates degrade to inline fallback messages via `load_and_render_safe`, so a missing Markdown file should not fail open (`hooks/lib/template-loader.sh:179`). If `validate_template_dir` cannot find expected subdirectories, `loop-common.sh` only warns and keeps using fallbacks (`hooks/lib/loop-common.sh:142`). Placeholder values containing new placeholders are not re-expanded, reducing prompt/template injection risk (`hooks/lib/template-loader.sh:54`). Templates loaded with raw `load_template`, such as the git-not-clean special notes, can be empty and then fallback notes are assembled manually (`hooks/loop-codex-stop-hook.sh:445`, `hooks/loop-codex-stop-hook.sh:456`). Some block templates are not directly referenced in `loop-common.sh`; tests treat direct-reference absence as informational rather than automatic failure (`tests/test-template-references.sh:130`).
- validation_or_tests: `tests/test-template-loader.sh` verifies loading `block/git-push.md`, rendering `block/wrong-round-number.md`, unresolved placeholder preservation, and safe fallback behavior (`tests/test-template-loader.sh:59`, `tests/test-template-loader.sh:151`, `tests/test-template-loader.sh:185`, `tests/test-template-loader.sh:201`, `tests/test-template-loader.sh:216`). `tests/test-templates-comprehensive.sh` verifies `load_and_render_safe` uses real templates when present, rejects invalid template dirs, and renders real `wrong-round-number` and `unpushed-commits` templates with expected substitutions (`tests/test-templates-comprehensive.sh:456`, `tests/test-templates-comprehensive.sh:466`, `tests/test-templates-comprehensive.sh:498`, `tests/test-templates-comprehensive.sh:517`). `tests/test-template-references.sh` checks direct hook references and validates key common template files exist (`tests/test-template-references.sh:134`, `tests/test-template-references.sh:152`).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-040 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: This file is a shared executable test harness for robustness tests. It centralizes pass/fail/skip counters, colored result output, summary exit status, temporary directory cleanup, and mock git repository setup. It is not production RLCR logic, but it is part of the executable specification because robustness tests source it to validate core hook behavior consistently.
- algorithmic_behavior: The harness initializes mutable global counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` (`tests/test-helpers.sh:22`). `pass`, `fail`, and `skip` emit colored status lines and increment the relevant counter (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:46`). `print_test_summary` emits totals, includes skipped count only when nonzero, and returns success only when `TESTS_FAILED` is zero (`tests/test-helpers.sh:58`, `tests/test-helpers.sh:66`, `tests/test-helpers.sh:71`). `setup_test_dir` creates an isolated temp directory and installs an EXIT trap to remove it (`tests/test-helpers.sh:86`). `init_test_git_repo` creates a minimal git repository with local test identity, disables GPG signing, writes `file.txt`, stages it, and creates an initial commit (`tests/test-helpers.sh:93`).
- inputs_outputs_state: Inputs are shell function arguments: test names and optional expected/got strings for `fail`, optional skip reason for `skip`, optional summary title for `print_test_summary`, and target directory for `init_test_git_repo`. Outputs are terminal lines plus function return status from `print_test_summary`. State changes include incrementing shell-global counters, setting global `TEST_DIR`, registering a cleanup trap, changing into and out of the temporary git repo, creating files in temp directories, and creating git metadata.
- gates_or_invariants: The summary gate is strict on failures but tolerant of skips: any failed assertion makes `print_test_summary` return 1; zero failures returns 0 even with skipped tests (`tests/test-helpers.sh:71`). `setup_test_dir` assumes a single active `TEST_DIR` per test process because each call overwrites `TEST_DIR` and replaces the EXIT trap (`tests/test-helpers.sh:87`). `init_test_git_repo` assumes `git` is available and that the caller can write to the given directory; it also assumes the shell can `cd` into that directory and back (`tests/test-helpers.sh:95`, `tests/test-helpers.sh:104`).
- dependencies_and_callers: Robustness tests source this helper using `source "$SCRIPT_DIR/../test-helpers.sh"` from `tests/robustness`. Consumers include goal-tracker, cancel-security, git-operations, plan-file, state-file, template-stress, hook-input, timeout, session, and path-validation robustness tests. The helper functions are used for setup and summary in those tests, for example `setup_test_dir` appears in most robustness scripts and `print_test_summary` closes each suite. `init_test_git_repo` is available for git-specific fixtures; direct use was not found in the sampled robustness grep output, so it may be reserved for current or future git fixture tests.
- edge_cases_or_failure_modes: `echo -e` color output can vary in shells, but the file is Bash-only via shebang (`tests/test-helpers.sh:1`). The cleanup trap uses an unquoted `$TEST_DIR` in the trap string (`tests/test-helpers.sh:88`); `mktemp -d` normally returns safe paths, but paths with spaces would be fragile. `init_test_git_repo` has no local error handling and would let a failed git command affect the calling test depending on that test’s shell options. It changes process working directory temporarily, then uses `cd -` to return; if the original directory is unavailable, subsequent test behavior can drift.
- validation_or_tests: The file itself is not a test target in the inspected evidence, but it validates other tests by defining their assertion semantics and final exit status. `rg` shows robustness suites source it and rely on `setup_test_dir` plus `print_test_summary`, including `tests/robustness/test-goal-tracker-robustness.sh`, `tests/robustness/test-git-operations-robustness.sh`, `tests/robustness/test-hook-input-robustness.sh`, `tests/robustness/test-path-validation-robustness.sh`, and others. The behavior is therefore transitively exercised whenever those robustness suites run.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-070 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: This template is the state-schema compatibility failure block. It explains that an active RLCR loop state file lacks a required field and tells the user the loop will terminate unexpectedly to preserve state information. It is a core transition gate because it prevents newer hook logic from operating on an older state schema.
- algorithmic_behavior: The file contains one placeholder, `{{FIELD_NAME}}`, used to identify the missing state field (`prompt-template/block/schema-outdated.md:3`). Its caller is `schema_validation_error` in `hooks/loop-plan-file-validator.sh`, which renders the template with `FIELD_NAME=$field_name`, JSON-escapes the rendered Markdown using `jq -Rs`, and emits a hook response with `"decision": "block"` (`hooks/loop-plan-file-validator.sh:60`, `hooks/loop-plan-file-validator.sh:65`, `hooks/loop-plan-file-validator.sh:69`, `hooks/loop-plan-file-validator.sh:71`). The validator then exits 0 after emitting the block decision, which is normal hook-block signaling rather than shell failure (`hooks/loop-plan-file-validator.sh:85`).
- inputs_outputs_state: Input state comes from the active loop state file selected under `.humanize/rlcr`, using `state.md` or `finalize-state.md` if present (`hooks/loop-plan-file-validator.sh:29`, `hooks/loop-plan-file-validator.sh:39`). `parse_state_file_strict` extracts frontmatter fields, including `STATE_PLAN_TRACKED` and `STATE_START_BRANCH` (`hooks/lib/loop-common.sh:260`). The plan-file validator maps those into `PLAN_TRACKED` and `START_BRANCH`, then validates required compatibility fields `plan_tracked` and `start_branch` (`hooks/loop-plan-file-validator.sh:50`, `hooks/loop-plan-file-validator.sh:79`). Output is a JSON block response whose reason is the rendered `schema-outdated.md` content. The template itself does not rename or end the loop, but the message states the loop will be terminated as unexpected; that termination behavior would be orchestrated by surrounding loop control after the block.
- gates_or_invariants: Required schema fields for this compatibility check are `plan_tracked` and `start_branch`, declared as constants in `loop-common.sh` (`hooks/lib/loop-common.sh:16`). Empty values trigger the schema-outdated block (`hooks/loop-plan-file-validator.sh:80`, `hooks/loop-plan-file-validator.sh:85`). This gate runs after strict parsing has already required YAML frontmatter, numeric `current_round`, and numeric `max_iterations` (`hooks/lib/loop-common.sh:248`, `hooks/lib/loop-common.sh:270`, `hooks/lib/loop-common.sh:280`, `hooks/lib/loop-common.sh:286`). The schema-outdated template is therefore specifically for older-but-parseable state files missing v1.1.2 compatibility fields, not for malformed frontmatter or nonnumeric counters.
- dependencies_and_callers: Direct dependencies are `loop-plan-file-validator.sh`, `loop-common.sh`, `template-loader.sh`, and `jq`. `loop-plan-file-validator.sh` sources `loop-common.sh`, which sources `template-loader.sh` and initializes `TEMPLATE_DIR` (`hooks/loop-plan-file-validator.sh:17`, `hooks/lib/loop-common.sh:135`, `hooks/lib/loop-common.sh:140`). Rendering uses `load_and_render_safe`, so missing `schema-outdated.md` falls back to the inline compatibility message in the caller (`hooks/loop-plan-file-validator.sh:62`, `hooks/loop-plan-file-validator.sh:65`).
- edge_cases_or_failure_modes: If the state file is malformed, `parse_state_file_strict` blocks with a generic stderr error and exit 1 before this template is used (`hooks/loop-plan-file-validator.sh:44`). If `plan_tracked` or `start_branch` exists but is semantically invalid rather than empty, this particular gate does not catch it; later branch and plan tracking checks handle consistency. If `jq` is unavailable, JSON escaping would fail under `set -euo pipefail`, so the hook would error rather than produce the schema-outdated block. Because rendering preserves unresolved placeholders, a missing `FIELD_NAME` assignment would leave `{{FIELD_NAME}}` visible rather than silently erasing it.
- validation_or_tests: The file is covered indirectly by template-loader behavior and reference coverage. `tests/test-template-loader.sh` verifies `load_and_render_safe` fallback and existing-template selection using other block templates (`tests/test-template-loader.sh:201`, `tests/test-template-loader.sh:216`). `tests/test-template-references.sh` scans hook references and reports whether templates are directly referenced (`tests/test-template-references.sh:134`). No direct test case for `schema-outdated.md` rendering was found in the inspected targeted test slices, making this a focused test gap for the plan-file schema compatibility path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`