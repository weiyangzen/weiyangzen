# agent_17 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-017 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. The file is a thin command contract: it exposes `/humanize:start-rlcr-loop`, restricts execution to `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`, and documents the loop’s expected Codex-review behavior. See `commands/start-rlcr-loop.md:1-14`.
- algorithmic_behavior: The command delegates all initialization to `scripts/setup-rlcr-loop.sh` by passing `$ARGUMENTS` directly to the setup script (`commands/start-rlcr-loop.md:10-14`). The user-facing workflow is: implement the supplied plan, write a round summary, attempt exit, let Codex review, continue on feedback, and terminate only when Codex returns `COMPLETE`, max iterations are reached, or the user cancels (`commands/start-rlcr-loop.md:16-23`, `commands/start-rlcr-loop.md:51-55`). The Goal Tracker contract is part of the algorithm: round 0 establishes immutable goal and acceptance criteria, later rounds update mutable task/deferred/evolution state, and full alignment checks occur at rounds 4, 9, 14, and so on (`commands/start-rlcr-loop.md:24-42`).
- inputs_outputs_state: Inputs are a relative markdown plan path plus optional flags shown in the command frontmatter and setup script: `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--plan-file`, and `--track-plan-file` (`commands/start-rlcr-loop.md:3`, `scripts/setup-rlcr-loop.sh:43-102`, `scripts/setup-rlcr-loop.sh:104-180`). Setup outputs a timestamped `.humanize/rlcr/<timestamp>/` loop directory, copies the plan to `plan.md`, creates `state.md` with `current_round: 0`, Codex config, push policy, plan path, tracking mode, start branch, and timestamp, writes `goal-tracker.md`, and emits `round-0-prompt.md` plus the required `round-0-summary.md` path (`scripts/setup-rlcr-loop.sh:490-521`, `scripts/setup-rlcr-loop.sh:527-618`, `scripts/setup-rlcr-loop.sh:624-684`). Runtime state transitions are performed by the Stop hook registered in `hooks/hooks.json:52-59`: normal `state.md` becomes `finalize-state.md` after Codex `COMPLETE`, then `complete-state.md` after finalize checks pass (`hooks/loop-codex-stop-hook.sh:966-1026`, `hooks/loop-codex-stop-hook.sh:648-660`); otherwise state advances by replacing `current_round` with `NEXT_ROUND` and generating the next prompt/summary filenames (`hooks/loop-codex-stop-hook.sh:1058-1120`).
- gates_or_invariants: The command definition itself enforces a narrow tool boundary through `allowed-tools` (`commands/start-rlcr-loop.md:4`). Setup validates that the project is a git repo with a commit, plan path is relative, has no spaces or shell metacharacters, is not a symlink, has existing parent directory, exists, is readable, stays inside the project, and is not in a submodule (`scripts/setup-rlcr-loop.sh:211-328`). It enforces plan tracking policy: `--track-plan-file` requires a tracked clean file, while the default requires the plan to be untracked/gitignored (`scripts/setup-rlcr-loop.sh:330-379`). It rejects plans under 5 lines or with fewer than 3 meaningful content lines (`scripts/setup-rlcr-loop.sh:381-438`), requires `codex`, records the start branch, and validates branch/model/effort strings for YAML-safe state serialization (`scripts/setup-rlcr-loop.sh:440-484`). The Stop hook adds runtime gates: state schema, branch consistency, plan backup and content integrity, incomplete todos, git status failure, large files over 2000 lines, clean working tree, optional pushed commits, summary file existence, round-0 Goal Tracker initialization, max iterations, Codex executable availability, Codex result-file creation, and non-empty review output (`hooks/loop-codex-stop-hook.sh:129-253`, `hooks/loop-codex-stop-hook.sh:255-340`, `hooks/loop-codex-stop-hook.sh:343-557`, `hooks/loop-codex-stop-hook.sh:559-646`, `hooks/loop-codex-stop-hook.sh:754-955`).
- dependencies_and_callers: Called by Claude slash-command machinery using the frontmatter command definition. It depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:12-14`). The loop lifecycle then depends on hook registration in `hooks/hooks.json`, especially PreToolUse validators and the Stop hook (`hooks/hooks.json:14-59`). Setup and hooks share `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `git`, `jq`, `codex`, and the prompt templates under `prompt-template/`. The README mirrors the command contract and options (`README.md:78-100`).
- edge_cases_or_failure_modes: Setup fails closed on git timeouts, invalid plan paths, symlink traversal, submodule plans, wrong tracking mode, insufficient plan content, missing Codex CLI, YAML-unsafe branch/model/effort values, and absolute paths (`scripts/setup-rlcr-loop.sh:211-484`). Stop-hook edge handling includes no active loop allowing exit, malformed state allowing exit in the Stop hook after reporting an error, state schema too old blocking, plan file deletion/modification blocking, todo parse errors blocking, git status failure blocking, and max-iteration termination even when Codex says `COMPLETE` at or beyond the max (`hooks/loop-codex-stop-hook.sh:61-91`, `hooks/loop-codex-stop-hook.sh:129-253`, `hooks/loop-codex-stop-hook.sh:263-309`, `hooks/loop-codex-stop-hook.sh:319-340`, `hooks/loop-codex-stop-hook.sh:966-974`). Codex review failures are retriable blocks with debug files in XDG cache, covering nonzero exit, missing review file, copy failure, and empty review output (`hooks/loop-codex-stop-hook.sh:782-955`).
- validation_or_tests: The command file is declarative, but its delegated implementation and hooks are covered through the repository’s shell test harness. `tests/run-all-tests.sh` includes suites for plan-file validation, hook behavior, state exit naming, finalize phase, path validation robustness, git operation robustness, timeout robustness, and template behavior (`tests/run-all-tests.sh:32-63`). Direct runtime validation lives in setup and Stop-hook gates rather than in `commands/start-rlcr-loop.md` itself.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-047 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`. It verifies the core template directory resolution, file loading, single-pass rendering, fallback rendering, directory validation, shell metacharacter safety, placeholder-injection resistance, Unicode handling, and variable-name edge cases used by RLCR prompts and validators. The test sources the implementation directly from `hooks/lib/template-loader.sh` (`tests/test-template-loader.sh:10-13`).
- algorithmic_behavior: The script initializes pass/fail counters, defines `pass` and `fail` helpers, then executes numbered assertions from Test 1 through Test 41 (`tests/test-template-loader.sh:20-39`, `tests/test-template-loader.sh:41-659`). Each test computes a result from a template-loader function and compares it to exact expected output or expected grep matches. The final gate exits successfully only if `TESTS_FAILED` remains zero (`tests/test-template-loader.sh:641-659`).
- inputs_outputs_state: Inputs are the repository layout, `PROJECT_ROOT`, the real `prompt-template` tree, and inline template/value pairs supplied to functions such as `render_template`, `load_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` (`tests/test-template-loader.sh:10-13`, `tests/test-template-loader.sh:45-64`, `tests/test-template-loader.sh:85-156`). Outputs are colored PASS/FAIL lines, a numeric summary, and process exit code 0 or 1 (`tests/test-template-loader.sh:23-34`, `tests/test-template-loader.sh:641-659`). State is limited to `TESTS_PASSED` and `TESTS_FAILED` counters, which are incremented by helper functions (`tests/test-template-loader.sh:20-34`).
- gates_or_invariants: The script runs with `set -euo pipefail` (`tests/test-template-loader.sh:8`). It treats missing real templates, unexpected substitutions, shell metacharacter interpretation, recursive placeholder expansion, invalid template directories, and changed wording in required templates as failures. Basic loader invariants are covered by tests for `get_template_dir`, existing/missing `load_template`, single/multiple/multiline substitutions, unused variables, unreplaced variables, safe fallback behavior, and template directory validation (`tests/test-template-loader.sh:41-243`). Rendering safety invariants require literal preservation of `&`, backslashes, `$`, backticks, pipes, semicolons, globs, parentheses, redirections, quotes, comments, tildes, combined shell content, Windows paths, regex-like text, JSON-like content, and multiline values (`tests/test-template-loader.sh:247-486`). Placeholder injection is explicitly blocked by expecting `{{VAR}}` text inside values to remain unexpanded after insertion (`tests/test-template-loader.sh:488-543`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, whose relevant functions are `get_template_dir` (`hooks/lib/template-loader.sh:24-31`), `load_template` (`hooks/lib/template-loader.sh:33-48`), `render_template` (`hooks/lib/template-loader.sh:50-132`), `load_and_render` (`hooks/lib/template-loader.sh:134-147`), `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`), and `validate_template_dir` (`hooks/lib/template-loader.sh:205-222`). The implementation uses `env` plus `awk` for single-pass placeholder scanning, avoiding shell evaluation and avoiding rescanning substituted values (`hooks/lib/template-loader.sh:62-129`). The test is included as the first suite in the aggregate runner (`tests/run-all-tests.sh:32-35`).
- edge_cases_or_failure_modes: The test deliberately exercises missing templates returning empty output, fallback rendering when templates are missing, unreplaced placeholders remaining intact, empty variable values, underscore-prefixed variable names, numeric variable names, and actual non-ASCII bytes in template and value positions (`tests/test-template-loader.sh:68-78`, `tests/test-template-loader.sh:181-221`, `tests/test-template-loader.sh:555-639`). It also guards against a common `awk`/`gsub` ampersand regression by testing multiple ampersands literally (`tests/test-template-loader.sh:425-435`). A practical failure mode is coupling to exact template text: the integration checks require `block/git-push.md` to contain `Git Push Blocked` and `block/wrong-round-number.md` rendering to contain specific phrases (`tests/test-template-loader.sh:59-64`, `tests/test-template-loader.sh:151-164`).
- validation_or_tests: This file is itself the validation gate. It covers 41 numbered tests and exits nonzero if any fail (`tests/test-template-loader.sh:641-659`). Related broader coverage exists in comprehensive, error-scenario, and robustness template suites referenced by the all-tests runner (`tests/run-all-tests.sh:38-42`, `tests/run-all-tests.sh:59-60`), but this assigned file is the focused executable spec for the template-loader core.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-077 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template used when a Read operation targets a round prompt or summary file from the wrong location instead of the active RLCR loop directory. It is part of the validator prompt contract, not executable code, but it defines the user-facing transition for a blocked file-location gate (`prompt-template/block/wrong-file-location.md:1-9`).
- algorithmic_behavior: The template renders a heading, explains that `{{FILE_PATH}}` is outside the active loop location, lists the current round prompt and summary paths derived from `{{ACTIVE_LOOP_DIR}}` and `{{CURRENT_ROUND}}`, and gives a final file-access instruction (`prompt-template/block/wrong-file-location.md:1-9`). In the algorithmic caller, `hooks/loop-read-validator.sh` computes `FILE_PATH`, detects Read tool calls, filters to round prompt/summary files, locates the active loop, parses current round state, and triggers this template when `IN_HUMANIZE_LOOP_DIR` is false (`hooks/loop-read-validator.sh:22-47`, `hooks/loop-read-validator.sh:66-83`, `hooks/loop-read-validator.sh:91-128`).
- inputs_outputs_state: Template inputs are `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`, passed through `load_and_render_safe` by the Read validator (`hooks/loop-read-validator.sh:119-127`). The rendered output is written to stderr and the hook exits with code 2, which blocks the attempted Read (`hooks/loop-read-validator.sh:124-128`). The template itself performs no state mutation. The caller reads loop state from `state.md` or `finalize-state.md` using strict parsing and uses `STATE_CURRENT_ROUND` as the render source (`hooks/loop-read-validator.sh:85-97`).
- gates_or_invariants: The gate applies only to `Read` tool input with a required `tool_input.file_path`; other tools exit cleanly from this hook (`hooks/loop-read-validator.sh:34-43`). It first blocks todos-file reads outside the allowlist, then only continues for paths matching round summary or prompt naming (`hooks/loop-read-validator.sh:48-68`). If no active loop exists, the hook allows the read (`hooks/loop-read-validator.sh:77-83`). Malformed active state fails closed with an error (`hooks/loop-read-validator.sh:91-95`). Once the wrong-location block fires, it occurs before wrong-round and wrong-directory checks, so this template owns the earliest location failure for round prompt/summary reads (`hooks/loop-read-validator.sh:115-129`).
- dependencies_and_callers: Primary caller is `hooks/loop-read-validator.sh:124`. It depends on `hooks/lib/loop-common.sh` for active-loop discovery, round-file classification, allowlist logic, and strict state parsing; relevant helpers include `find_active_loop`, `is_round_file_type`, `extract_round_number`, and `is_allowlisted_file` (`hooks/lib/loop-common.sh:147-170`, `hooks/lib/loop-common.sh:231-345`). Rendering depends on `hooks/lib/template-loader.sh`, especially `load_and_render_safe`, which falls back if the template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`). The hook is registered for Read PreToolUse events in `hooks/hooks.json:33-40`.
- edge_cases_or_failure_modes: Because `loop-read-validator.sh` computes `CORRECT_PATH` before rendering but does not pass it to this template, the rendered message lists both current-round files rather than an exact corrected target for the requested file type (`hooks/loop-read-validator.sh:119-127`, `prompt-template/block/wrong-file-location.md:5-8`). The final line says `If you need this file, use: cat {{FILE_PATH}}`, which can be confusing because the same file path was just blocked for Read; this may intentionally redirect access through Bash or may be a UX inconsistency in the block text (`prompt-template/block/wrong-file-location.md:9`). The template also assumes `ACTIVE_LOOP_DIR` and `CURRENT_ROUND` are valid; the caller protects that through active-loop discovery and strict state parsing before render (`hooks/loop-read-validator.sh:77-97`).
- validation_or_tests: This specific template is referenced by the Read validator and should be covered by template-reference validation. The assigned loader test validates the underlying rendering and fallback mechanics generally, including missing-template fallback, real-template rendering, metacharacter literal behavior, and placeholder preservation (`tests/test-template-loader.sh:197-243`, `tests/test-template-loader.sh:247-543`). Direct behavior validation for the wrong-location branch would need a Read-hook scenario that supplies a round prompt/summary path outside `.humanize/rlcr/<active>/` and asserts exit 2 plus rendered text.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item sections present above; each assigned item id is used once as an Item Evidence heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`