# agent_21 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-021 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Code `Stop` hook for the RLCR loop. It is registered in `hooks/hooks.json:52-59` with command `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` and a 7200 second hook timeout. Its job is to intercept an attempted stop, run fast local gates, optionally run Codex review, and either block Claude with next instructions or transition the loop state.
- algorithmic_behavior: Reads hook JSON from stdin at `hooks/loop-codex-stop-hook.sh:29`, intentionally ignores `stop_hook_active` for iterative loop continuation (`:31-37`), finds the active loop under `.humanize/rlcr` (`:43-63`), detects normal vs Finalize Phase using `state.md` or `finalize-state.md` (`:72-82`), parses strict YAML frontmatter (`:88-104`), then runs gates before Codex: schema, branch, plan integrity, todos, git state, large files, clean worktree, push policy, summary existence, goal tracker initialization, and max iteration checks (`:105-646`). Normal review builds a Codex prompt (`:674-752`), executes `codex exec` with configured model/effort/timeout (`:758-845`), validates the review artifact (`:855-958`), then interprets the last non-empty trimmed line as `COMPLETE`, `STOP`, or actionable feedback (`:960-1128`).
- inputs_outputs_state: Inputs include `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr/<timestamp>/state.md`, optional `finalize-state.md`, loop backup `plan.md`, project plan file, `goal-tracker.md`, current round summary, hook transcript path for TodoWrite checks, git state, templates, and Codex CLI availability. Outputs are hook JSON blocks with `decision: block` plus `reason` and `systemMessage`, generated review prompt/result files, next-round prompt files, Codex debug files in `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<timestamp>/`, and state-file renames. State transitions are: no active loop -> allow exit; normal `state.md` + review issues -> increment `current_round` and block (`:1062-1127`); normal + `COMPLETE` -> `finalize-state.md` and block with Finalize prompt (`:967-1025`); finalize + checks pass -> `complete-state.md` and allow exit (`:654-659`); max iteration -> `maxiter-state.md` (`:642-645`, `:970-973`); `STOP` -> `stop-state.md` (`:1028-1055`); selected invalid config -> `unexpected-state.md` (`:107-123`).
- gates_or_invariants: Requires `plan_tracked` and `start_branch` fields for v1.1.2+ schema (`:134-146`); branch must remain the start branch and git branch lookup must not fail (`:152-179`); plan backup must exist, original plan must exist, tracked plan must have no uncommitted changes, and content must match backup (`:185-253`); incomplete TodoWrite items block before Codex (`:261-309`); git status failure blocks rather than bypassing checks (`:319-340`); changed code/docs files over 2000 lines block (`:349-423`); dirty worktree blocks, with special `.humanize` and untracked-file notes (`:432-486`); optional `push_every_round` blocks unpushed commits (`:492-518`); summary file is mandatory, using `finalize-summary.md` in Finalize Phase and `round-N-summary.md` otherwise (`:525-557`); round 0 goal tracker placeholders block (`:563-633`); Codex completion markers must be the exact last non-empty line to avoid false positives such as “CANNOT COMPLETE” (`:960-965`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for markers, `find_active_loop`, strict state parsing, `to_lower`, and `end_loop`; those constants/functions are defined at `hooks/lib/loop-common.sh:26-40`, `:152-170`, `:240-301`, and `:823-849`. Uses `hooks/lib/template-loader.sh` single-pass template substitution and fallback behavior (`hooks/lib/template-loader.sh:50-132`, `:170-203`). Uses `scripts/portable-timeout.sh` for GNU/macOS/Python timeout compatibility (`scripts/portable-timeout.sh:31-71`). Uses `hooks/check-todos-from-transcript.py`, whose exit code contract is 0 all done/no todos, 1 incomplete, 2 parse error (`hooks/check-todos-from-transcript.py:7-10`, `:90-129`). External commands include `jq`, `git`, `diff`, `awk`, `grep`, `sed`, `wc`, `tail`, `cp`, `mv`, `python3`, and `codex`.
- edge_cases_or_failure_modes: No active loop or missing state allows exit (`hooks/loop-codex-stop-hook.sh:61-63`, `:79-82`). Malformed strict state currently prints an error and allows exit for safety (`:88-92`), while invalid model/effort/current round ends as unexpected (`:107-123`). Git branch/status timeouts fail closed with a block (`:155-166`, `:328-340`). Missing Codex, nonzero Codex, missing/empty review result, or failed stdout copy all block with debug paths (`:758-778`, `:883-955`). If Codex writes output to stdout instead of the requested result file, the hook copies stdout into `round-N-review-result.md` (`:902-918`). If `COMPLETE` arrives when `CURRENT_ROUND >= MAX_ITERATIONS`, Finalize Phase is skipped and the loop is marked maxiter (`:967-973`). Finalize Phase deliberately skips Codex review and only completes after local gates pass (`:649-659`).
- validation_or_tests: `tests/test-finalize-phase.sh` directly exercises this hook for missing finalize summary, clean/dirty git, finalize completion without Codex invocation, `COMPLETE` entering Finalize Phase, max-iteration skip, incomplete todos, and non-complete feedback rounds (`tests/test-finalize-phase.sh:359-445`, `:465-500`, `:526-535`, `:580-610`). `tests/test-plan-file-hooks.sh` also invokes the hook for plan-file protection scenarios, and `tests/test-template-references.sh:58` includes the hook in template-reference coverage. The Finalize prompt linkage is asserted by checking that the block prompt mentions simplification when `COMPLETE` triggers `finalize-state.md` (`tests/test-finalize-phase.sh:471-475`).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-051 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh safety specification for the Humanize monitor path, focused on preventing zsh `nomatch`/“no matches found” failures when RLCR session, state, or Codex log directories are empty or contain only nonmatching entries. It supports the robust-edge branch behavior around shell-safe directory scanning.
- algorithmic_behavior: Runs under zsh via `#!/usr/bin/env zsh` and `set -euo pipefail` (`tests/test-zsh-monitor-safety.sh:1-13`), creates an isolated `/tmp/test-zsh-humanize-$$` workspace and `XDG_CACHE_HOME` (`:48-60`), sources `scripts/humanize.sh` and asserts sourcing does not emit “no matches found” (`:62-77`), then recreates the monitor helper algorithms using `find` loops instead of globs for empty session directories, dotfile-only directories, missing `*-state.md`, empty Codex log cache, and multiple timestamped sessions (`:80-273`). It verifies the shell is actually zsh (`:276-286`) and demonstrates the old glob pattern would be unsafe while the new `find` pattern is safe (`:288-321`).
- inputs_outputs_state: Inputs are the repository’s `scripts/humanize.sh`, the active zsh runtime (`ZSH_VERSION`), temporary `.humanize/rlcr` trees, temporary cache directories under `XDG_CACHE_HOME`, timestamp-formatted session directory names, and synthetic files such as `.cancel-requested`, `.hidden-file`, and `other.md`. Outputs are colored PASS/FAIL lines, `TESTS_PASSED`/`TESTS_FAILED` counters, cleanup via trap, and final exit 0 when all safety checks pass or exit 1 when any check fails (`:24-36`, `:56-60`, `:323-341`).
- gates_or_invariants: The invariant under test is that monitor discovery must use `find ... 2>/dev/null` with guarded reads, not direct unmatched globs. Valid sessions must match `YYYY-MM-DD_HH-MM-SS` (`:109-115`, `:145-150`, `:258-265`). Empty and dotfile-only loop directories must produce empty results without shell errors (`:86-124`, `:133-157`). Missing `*-state.md` must return `|unknown` (`:166-205`). Empty cache directories must produce zero found logs without error (`:214-235`). Timestamp ordering must select the lexicographically latest session (`:246-273`).
- dependencies_and_callers: The test is coupled to `scripts/humanize.sh`, whose monitor implementation defines `_find_latest_session` using `find` at `scripts/humanize.sh:231-253`, `_find_latest_codex_log` using cache-directory `find` loops at `:255-314`, and `_find_state_file` using `find "$session_dir" -name '*-state.md'` at `:316-355`. It also relies on the same cache path sanitization used by the stop hook, documented in `scripts/humanize.sh:264-268` and implemented in `hooks/loop-codex-stop-hook.sh:786-792`. `tests/run-all-tests.sh` includes this script in the test set (`tests/run-all-tests.sh:48`, `:67`).
- edge_cases_or_failure_modes: The historical failure mode is zsh `nomatch` on patterns such as `.humanize/rlcr/*` or `*-state.md` when no path matches. The test covers empty directories, directories containing only dotfiles, sessions with unrelated files but no state, cache directories with no `round-*-codex-run.log`, and multiple sessions with no reliance on filesystem mtime. It does not fully run the interactive monitor UI; instead it sources `humanize.sh` and mirrors the nested helper logic because those helpers are local to `_humanize_monitor_codex`.
- validation_or_tests: This file is itself the validation artifact. It also aligns with broader monitor references found in `tests/test-monitor-e2e-real.sh`, which checks for absence of “no matches found”/bad pattern errors in end-to-end monitor runs, but this assigned file is the zsh-specific executable spec. Its pass condition is zero failures and the final “All zsh runtime safety tests passed!” path (`tests/test-zsh-monitor-safety.sh:333-336`).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-081 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing prompt template for the RLCR Finalize Phase. It defines the human/agent contract after Codex has already accepted implementation completion but before the loop is allowed to finish.
- algorithmic_behavior: The stop hook loads this template after Codex review’s last non-empty line is exactly `COMPLETE`, then renames `state.md` to `finalize-state.md` and blocks exit with the rendered prompt (`hooks/loop-codex-stop-hook.sh:967-1025`). The template tells Claude that Codex review has passed (`prompt-template/claude/finalize-phase-prompt.md:3-5`), requires use of the `code-simplifier:code-simplifier` agent via the Task tool (`:7-14`), constrains the work to functionality-equivalent simplification (`:16-24`), focuses on RLCR-session changes and duplicate/dead/complex code (`:25-34`), provides references to the original plan and goal tracker (`:35-39`), and requires todos completed, changes committed, and a finalize summary written (`:40-50`).
- inputs_outputs_state: Inputs are template variables `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`, passed by the stop hook at `hooks/loop-codex-stop-hook.sh:1010-1013`. Output is rendered Markdown inserted into the hook’s JSON `reason` field (`:1017-1024`). Its required downstream artifact is `finalize-summary.md`; the hook later expects that file in Finalize Phase (`:525-557`) and, when all checks pass, renames `finalize-state.md` to `complete-state.md` (`:654-659`).
- gates_or_invariants: The template’s non-negotiable constraints are no functionality changes, no existing test failures, no new bugs, and only functionality-equivalent cleanup (`prompt-template/claude/finalize-phase-prompt.md:16-24`). Its exit contract requires all todos completed, a descriptive commit, and a summary including simplifications, modified files, passing-test confirmation, and refactoring notes (`:40-50`). The executable enforcement is split: the prompt instructs behavior, while the stop hook enforces summary presence, TodoWrite completion, git cleanliness, and no Codex rerun during finalize (`hooks/loop-codex-stop-hook.sh:261-309`, `:432-557`, `:649-659`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1010-1013`), rendered by `hooks/lib/template-loader.sh` single-pass substitution (`hooks/lib/template-loader.sh:50-132`, `:170-203`), and coordinated with validators that recognize/protect `finalize-state.md` and allow `finalize-summary.md` in Finalize Phase. Related helper predicates are tested in `tests/test-finalize-phase.sh:179-220`, and active-loop detection for `finalize-state.md` is tested at `:231-239`.
- edge_cases_or_failure_modes: If this template is missing or empty, the hook falls back to an inline Finalize prompt with the same basic transition and summary path but less detail (`hooks/loop-codex-stop-hook.sh:990-1008`). The template itself cannot enforce behavior; enforcement depends on the hook and validators. A user can still perform risky refactors during Finalize Phase, so the prompt explicitly requires tests and no functionality changes. If `COMPLETE` occurs at max iterations, this prompt is intentionally bypassed and the state becomes `maxiter-state.md` (`hooks/loop-codex-stop-hook.sh:967-973`).
- validation_or_tests: `tests/test-finalize-phase.sh` verifies Finalize Phase entry blocks with a simplification prompt and creates `finalize-state.md` (`tests/test-finalize-phase.sh:465-480`), Finalize Phase completion renames to `complete-state.md` without invoking Codex (`:417-445`), missing `finalize-summary.md` blocks (`:370-385`), dirty git blocks (`:395-412`), incomplete todos block (`:526-535`), and max iteration skips Finalize Phase (`:483-500`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence headings present, each exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`