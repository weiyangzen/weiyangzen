# agent_24 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-024 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Claude `Read` calls in the RLCR loop. It prevents agents from reading stale or misplaced RLCR prompt/summary files and blocks most direct access to generated `round-*-todos.md` files, preserving the intended state machine path through current-round prompts, current/finalize state files, and native todo handling. It is wired as the `Read` matcher in `hooks/hooks.json:28-39`.
- algorithmic_behavior: The hook reads the entire hook JSON from stdin (`hooks/loop-read-validator.sh:22`), validates hook input via shared `validate_hook_input` (`hooks/loop-read-validator.sh:24-27`), rejects overly nested JSON above depth 30 (`hooks/loop-read-validator.sh:29-32`), and exits pass-through for non-`Read` tools (`hooks/loop-read-validator.sh:34-38`). For `Read`, it requires `tool_input.file_path` (`hooks/loop-read-validator.sh:40-45`) and lowercases the path for pattern checks (`hooks/loop-read-validator.sh:46`). `round-*-todos.md` reads are blocked unless the path is allowlisted for the active loop (`hooks/loop-read-validator.sh:48-60`). Non-summary/non-prompt paths pass (`hooks/loop-read-validator.sh:62-68`). For summary/prompt files, it finds the active loop, selects `finalize-state.md` over `state.md` when present, strictly parses state, extracts the current round, extracts the requested round from the basename, identifies file type, and validates location, round number, and exact directory path (`hooks/loop-read-validator.sh:70-167`).
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path`. Shared validation sets `VALIDATED_TOOL_NAME` after rejecting null bytes, invalid UTF-8 when `iconv` is present, invalid JSON, and missing `tool_name` (`hooks/lib/loop-common.sh:47-92`). Output is process status plus optional stderr block message: exit `0` allows the read, exit `1` rejects malformed hook/state inputs, and exit `2` blocks policy violations with rendered guidance. The validator reads state from `$PROJECT_ROOT/.humanize/rlcr/<active-loop>/state.md` or `finalize-state.md` (`hooks/loop-read-validator.sh:77-95`) and does not write state. Active loop discovery chooses the newest loop directory containing `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:147-170`). Current round comes from strict YAML frontmatter fields, requiring numeric `current_round` and `max_iterations` (`hooks/lib/loop-common.sh:232-295`).
- gates_or_invariants: Invariant 1: only the current round’s `round-N-prompt.md` or `round-N-summary.md` may be read from the exact active loop directory unless allowlisted (`hooks/loop-read-validator.sh:119-165`). Invariant 2: prompt/summary files outside `.humanize/rlcr/` are blocked with a wrong-location template (`hooks/loop-read-validator.sh:119-128`; path test helper at `hooks/lib/loop-common.sh:616-620`). Invariant 3: stale round reads are blocked unless `is_allowlisted_file` permits them (`hooks/loop-read-validator.sh:135-148`). The allowlist is fixed to `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` within the active loop (`hooks/lib/loop-common.sh:324-345`). Invariant 4: all read-block messages use `load_and_render_safe`, so missing templates degrade to inline fallback messages rather than empty hook responses (`hooks/loop-read-validator.sh:121-164`; safe loader behavior in `hooks/lib/template-loader.sh:167-203`).
- dependencies_and_callers: Runtime caller is Claude Code’s hook system via `hooks/hooks.json:28-39`. The script sources `hooks/lib/loop-common.sh` (`hooks/loop-read-validator.sh:14-17`), which in turn establishes `TEMPLATE_DIR` through `get_template_dir` and validates template directory presence (`hooks/lib/loop-common.sh:140-145`; `hooks/lib/template-loader.sh:24-31`). It depends on `jq`, `sed`, `grep`, `tr`, `find`-like shell utilities, optional `iconv`, and the shared template files `prompt-template/block/wrong-file-location.md`, `wrong-round-file.md`, `wrong-directory-path.md`, and `todos-file-access.md`. Sibling validators use the same common functions for write/edit/bash policy, which means malformed state parsing or allowlist semantics are shared across tool gates.
- edge_cases_or_failure_modes: If hook JSON is invalid, missing `tool_name`, missing `tool_input.file_path`, contains invalid UTF-8, or exceeds nesting depth, the hook fails closed (`hooks/loop-read-validator.sh:24-43`; `hooks/lib/loop-common.sh:55-129`). If no active loop exists, prompt/summary reads are allowed (`hooks/loop-read-validator.sh:81-83`), which is intentional pass-through outside an active RLCR session. If an active loop exists but state is malformed, it blocks for safety (`hooks/loop-read-validator.sh:91-95`). Finalize phase changes the state source to `finalize-state.md` (`hooks/loop-read-validator.sh:85-89`). Path validation is string-exact for the final directory check (`hooks/loop-read-validator.sh:154-165`), so symlinked, relative, case-varied, or otherwise normalized-equivalent paths may be rejected. The `.humanize/rlcr/` detector is substring based (`hooks/lib/loop-common.sh:617-620`), so it is a policy heuristic rather than canonical path resolution.
- validation_or_tests: Direct allowlist coverage is in `tests/test-allowlist-validators.sh:236-275`, verifying allowed reads of `round-1-todos.md` and historical `round-0-summary.md`, and blocks for non-allowlisted `round-3-todos.md` and `round-3-summary.md`. Robust hook parsing tests cover valid `Read`, malformed JSON, empty JSON, missing `file_path`, Unicode paths, unknown tool pass-through, deeply nested JSON rejection, non-UTF8 rejection, and null-byte handling in `tests/robustness/test-hook-input-robustness.sh:34-232`. Template reference safety checks include `loop-read-validator.sh` as a critical script and assert safe rendering usage in `tests/test-template-references.sh:176-200`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-054 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template intended to explain a Codex review execution failure to Claude/operator when no review output is produced. Its placeholders define the diagnostic surface for a failed review run: exit code, expected review result path, debug command/stdout/stderr files, and stderr tail (`prompt-template/block/codex-review-failed.md:1-18`).
- algorithmic_behavior: The template itself is declarative, not executable. If rendered, it would produce a markdown block headed “Codex Review Failed”, show `{{CODEX_EXIT_CODE}}`, state that `{{REVIEW_RESULT_FILE}}` was not created, list debug artifacts, embed `{{STDERR_CONTENT}}`, and instruct the system to attempt another review on exit (`prompt-template/block/codex-review-failed.md:3-18`). This corresponds conceptually to the stop-hook review failure transition: block the exit, surface diagnostics, and retry the review rather than advancing the RLCR round.
- inputs_outputs_state: Inputs are template variables `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5-15`). Output would be markdown used as a block reason. The template does not mutate state. The repository’s template renderer performs single-pass `{{VAR}}` substitution and preserves missing placeholders (`hooks/lib/template-loader.sh:50-132`), while `load_and_render_safe` would fall back if the template were missing or rendered empty (`hooks/lib/template-loader.sh:167-203`).
- gates_or_invariants: The intended gate is “Codex review failed to produce output, so do not let the loop complete or continue silently.” The equivalent active runtime gate is implemented inline in `hooks/loop-codex-stop-hook.sh`: it records command/stdout/stderr paths (`hooks/loop-codex-stop-hook.sh:794-849`), builds a JSON `decision: block` reason in `codex_failure_exit` (`hooks/loop-codex-stop-hook.sh:855-881`), blocks on non-zero Codex exit (`hooks/loop-codex-stop-hook.sh:883-900`), blocks when the review result file is not created (`hooks/loop-codex-stop-hook.sh:920-946`), and blocks when it is empty (`hooks/loop-codex-stop-hook.sh:948-955`).
- dependencies_and_callers: I found no direct runtime caller for `prompt-template/block/codex-review-failed.md`. Repository search finds the file and related variables, but no `load_template` or `load_and_render_safe` reference to `block/codex-review-failed.md`; the stop hook uses inline failure text instead (`hooks/loop-codex-stop-hook.sh:861-872`, `hooks/loop-codex-stop-hook.sh:890-945`). It is still included in broad template sweeps because tests iterate every `prompt-template/**/*.md` file (`tests/test-templates-comprehensive.sh:562-606`). Template-reference validation treats unreferenced templates as warnings, not failures (`tests/test-template-references.sh:119-142`).
- edge_cases_or_failure_modes: As an unwired template, the main failure mode is drift: text here can diverge from the actual Codex failure behavior in `codex_failure_exit`. It says stderr last 50 lines (`prompt-template/block/codex-review-failed.md:13`), while the active stop hook captures last 30 lines for non-zero exit and missing-result diagnostics (`hooks/loop-codex-stop-hook.sh:887`, `hooks/loop-codex-stop-hook.sh:924-929`). It also only models “review result file not created”, while the active hook has separate paths for non-zero exit, stdout fallback copy failure, missing file, and empty file (`hooks/loop-codex-stop-hook.sh:883-955`).
- validation_or_tests: Covered only by generic template infrastructure tests, not behavior-specific stop-hook tests found during this pass. `tests/test-templates-comprehensive.sh:562-606` loads and renders all templates with dummy placeholder values. `tests/test-template-references.sh:119-142` reports unreferenced templates as warnings. The active runtime failure behavior is indirectly protected by the stop hook code path but this exact template is not asserted as the rendered block.
- skip_candidate: `yes: appears to be a stale or reserved template; no runtime caller was found, and the active Codex review failure gate is implemented inline in hooks/loop-codex-stop-hook.sh instead`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-084 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude next-round instruction template used when Codex review finds issues and the RLCR loop must continue. It is the handoff contract from Codex review output back to Claude implementation work: re-read the original plan, create todos for all discovered issues, process every Codex finding, and treat the goal tracker as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1-31`).
- algorithmic_behavior: The stop hook uses this template after Codex review content is read and does not end in the completion or stop markers. It first parses the last non-empty review line for exact marker matching (`hooks/loop-codex-stop-hook.sh:958-967`), enters finalize phase on completion (`hooks/loop-codex-stop-hook.sh:966-1026`), terminates on stop/circuit-breaker (`hooks/loop-codex-stop-hook.sh:1028-1055`), and otherwise advances to “Review Found Issues - Continue Loop” (`hooks/loop-codex-stop-hook.sh:1058-1060`). In that transition it increments `current_round` in state (`hooks/loop-codex-stop-hook.sh:1062-1065`), defines the next prompt/summary paths (`hooks/loop-codex-stop-hook.sh:1067-1069`), renders `claude/next-round-prompt.md` with `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` into `round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:1071-1083`), appends post-alignment action items after every 5th-round alignment check (`hooks/loop-codex-stop-hook.sh:1085-1091`), appends footer/push/goal-tracker-update guidance (`hooks/loop-codex-stop-hook.sh:1093-1113`), then blocks Claude’s exit with the generated prompt as the hook reason (`hooks/loop-codex-stop-hook.sh:1115-1125`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{GOAL_TRACKER_FILE}}` (`prompt-template/claude/next-round-prompt.md:5-18`, `prompt-template/claude/next-round-prompt.md:22-31`). Output is markdown written to the next round prompt file. State transition is explicit: `state.md` current round changes from current to next before the prompt is emitted (`hooks/loop-codex-stop-hook.sh:1062-1065`). The prompt itself instructs Claude to create todos for every task in importance order and forbids only addressing the most important issue (`prompt-template/claude/next-round-prompt.md:12-14`). It embeds the Codex review between start/end HTML comments (`prompt-template/claude/next-round-prompt.md:16-19`) so downstream readers can separate review content from template instructions.
- gates_or_invariants: Invariant 1: next-round work must remain aligned with the original plan via `@{{PLAN_FILE}}` before proceeding (`prompt-template/claude/next-round-prompt.md:3-8`). Invariant 2: all discovered issues must be tracked and attempted, not cherry-picked (`prompt-template/claude/next-round-prompt.md:12-14`). Invariant 3: the goal tracker is read-only after round 0; updates must be requested in the summary rather than directly modifying `goal-tracker.md` (`prompt-template/claude/next-round-prompt.md:22-31`). Runtime invariant: this template is only used after Codex produced a non-empty review result whose final marker was neither completion nor stop (`hooks/loop-codex-stop-hook.sh:958-1060`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:1080-1083` through `load_and_render_safe`. It depends on the shared template loader’s single-pass substitution, which prevents review content containing `{{...}}` from triggering nested placeholder replacement (`hooks/lib/template-loader.sh:54-58`, `hooks/lib/template-loader.sh:71-132`). It coordinates with sibling Claude templates appended afterward: `claude/post-alignment-action-items.md`, `claude/next-round-footer.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md` (`hooks/loop-codex-stop-hook.sh:1085-1113`). It is downstream of Codex review templates `codex/full-alignment-review.md` and `codex/regular-review.md`, which instruct Codex to write review output to `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:721-751`).
- edge_cases_or_failure_modes: If this template is missing or empty, `load_and_render_safe` uses a shorter fallback prompt that includes review content and references plan/goal tracker (`hooks/loop-codex-stop-hook.sh:1071-1083`; fallback behavior at `hooks/lib/template-loader.sh:167-203`). If `REVIEW_CONTENT` is large or contains placeholder-like text, single-pass rendering avoids accidental recursive substitution (`hooks/lib/template-loader.sh:54-58`). If Codex’s final line is a phrase containing completion text rather than an exact marker, it will not trigger finalize due to strict last-line matching (`hooks/loop-codex-stop-hook.sh:960-965`), so this next-round path remains the safe default. If state file update via `sed` or `mv` fails, `set -euo pipefail` in the hook should stop before emitting a misleading next prompt.
- validation_or_tests: Template existence and reference coverage is checked by `tests/test-template-references.sh:93-110`, where `claude/next-round-prompt.md` is extracted from `load_and_render_safe` calls and verified to exist. Safe-rendering usage for critical validators is separately checked, and the same test suite treats template references as a regression surface (`tests/test-template-references.sh:172-200`). Comprehensive template tests load/render every markdown template and assert placeholders can be replaced with dummy values (`tests/test-templates-comprehensive.sh:562-606`). Fallback behavior for `load_and_render_safe` is tested in `tests/test-templates-comprehensive.sh:440-463`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-024, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-054, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-084
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`