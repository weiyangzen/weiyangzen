# agent_23 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-023 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit guard for an active RLCR loop. Its algorithmic job is to fail closed before accepting a user prompt when the loop state is malformed, the git branch no longer matches the loop start branch, or the plan file’s tracked/untracked invariant no longer matches how the loop was started. It is wired as the `UserPromptSubmit` command hook in `hooks/hooks.json:4-10`.
- algorithmic_behavior: The script runs under `set -euo pipefail` at `hooks/loop-plan-file-validator.sh:11`, computes `SCRIPT_DIR` and `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` at `hooks/loop-plan-file-validator.sh:13-14`, sources shared loop helpers at `hooks/loop-plan-file-validator.sh:17`, and sources portable timeout support at `hooks/loop-plan-file-validator.sh:20-21`. It consumes all hook stdin into `INPUT` at `hooks/loop-plan-file-validator.sh:26-27`, but does not inspect the prompt content; the validation is loop-state and git-state based. It locates the newest active loop via `find_active_loop "$PROJECT_ROOT/.humanize/rlcr"` at `hooks/loop-plan-file-validator.sh:29-31`; if none exists, it exits permissively at `hooks/loop-plan-file-validator.sh:33-36`. If `finalize-state.md` exists, it validates that file instead of `state.md` at `hooks/loop-plan-file-validator.sh:38-42`. It then calls `parse_state_file_strict` at `hooks/loop-plan-file-validator.sh:44-48`, maps parsed state variables into local names at `hooks/loop-plan-file-validator.sh:50-53`, validates required schema additions `plan_tracked` and `start_branch` at `hooks/loop-plan-file-validator.sh:59-89`, checks current git branch against `start_branch` at `hooks/loop-plan-file-validator.sh:91-115`, and enforces plan-file tracking policy at `hooks/loop-plan-file-validator.sh:117-229`.
- inputs_outputs_state: Inputs are stdin from the Claude hook framework, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/*/state.md` or `.humanize/rlcr/*/finalize-state.md`, current git branch, and `git ls-files` / `git status --porcelain` for the plan path. Outputs are either no output with exit `0` for allow, process exit `1` only for malformed state parse failure at `hooks/loop-plan-file-validator.sh:45-48`, or a JSON object with `"decision": "block"` and `"reason"` followed by exit `0` for recoverable guard blocks. State is read-only from this hook: it does not mutate loop state, plan files, or git state.
- gates_or_invariants: No active loop means allow. Malformed active state fails closed. Required loop-state schema fields are `plan_tracked` and `start_branch`; missing values are rendered through `block/schema-outdated.md` via `load_and_render_safe` at `hooks/loop-plan-file-validator.sh:60-66`. Branch switching is prohibited during an active loop; a mismatch emits a block JSON at `hooks/loop-plan-file-validator.sh:107-115`. If `plan_tracked=true`, the plan path must still be tracked and clean: `git ls-files --error-unmatch` is checked at `hooks/loop-plan-file-validator.sh:123-148`, `git status --porcelain` at `hooks/loop-plan-file-validator.sh:150-172`, untracked state blocks at `hooks/loop-plan-file-validator.sh:174-182`, and any porcelain output blocks at `hooks/loop-plan-file-validator.sh:184-192`. If `plan_tracked` is not `true`, the plan path must remain untracked/gitignored; any tracked result blocks at `hooks/loop-plan-file-validator.sh:193-228`. Git timeouts use `GIT_TIMEOUT=30` at `hooks/loop-plan-file-validator.sh:23-24` and are treated as blocking errors at `hooks/loop-plan-file-validator.sh:129-137`, `154-162`, and `199-207`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, especially `find_active_loop`, field constants, `parse_state_file_strict`, and template loading; `loop-common.sh` sources `template-loader.sh` and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:135-145`. `parse_state_file_strict` requires two YAML frontmatter delimiters, extracts fields, strips quotes for `plan_file` and `start_branch`, and validates numeric round fields at `hooks/lib/loop-common.sh:240-295`. Depends on `scripts/portable-timeout.sh` for `run_with_timeout`. Called by Claude hook configuration in `hooks/hooks.json:4-10`. Behavior aligns with setup metadata written by `scripts/setup-rlcr-loop.sh`, which emits `plan_tracked` and `start_branch` into state at `scripts/setup-rlcr-loop.sh:517-518`.
- edge_cases_or_failure_modes: No active loop is a deliberate allow path. A malformed or missing state file in the newest active loop blocks/fails closed. If git cannot determine the current branch or times out, branch consistency cannot be proven and the hook blocks at `hooks/loop-plan-file-validator.sh:95-105`. Detached HEAD would likely surface as branch name `HEAD`, causing a branch mismatch if `start_branch` is a normal branch. `LS_FILES_EXIT` and `STATUS_EXIT` are not reset before each git command; because each branch of the script uses each variable once, this is not a practical issue in the current flow, but it is a shell-state fragility if future edits add repeated checks. `FULL_PLAN_PATH` is computed at `hooks/loop-plan-file-validator.sh:121` but unused. For `plan_tracked=false`, the script only checks that git does not track the path; it does not independently verify that `.gitignore` matches the path. It also does not validate that `PLAN_FILE` is non-empty beyond strict parser extracting it; downstream git commands with an empty path would be risky, though the setup path normally writes it.
- validation_or_tests: `tests/test-plan-file-hooks.sh` directly exercises this hook. It verifies pass-through with valid state at `tests/test-plan-file-hooks.sh:91-104`, quoted `plan_file` parsing at `tests/test-plan-file-hooks.sh:106-119`, missing `plan_tracked` and `start_branch` blocks at `tests/test-plan-file-hooks.sh:121-159`, branch-change blocking at `tests/test-plan-file-hooks.sh:164-185`, quoted `start_branch` handling and mismatch detection at `tests/test-plan-file-hooks.sh:341-386`, and hyphenated plan path handling at `tests/test-plan-file-hooks.sh:431-460`. Additional references in the same file cover tracked-plan cases around `tests/test-plan-file-hooks.sh:595-690`. I did not run tests because this branch export is read-only and the test scripts create temporary repositories/files.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-053 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared prompt-template loader and single-pass renderer for RLCR hooks. It is core support code for hook decisions and next-round prompt generation because validators and stop hooks use it to load block/prompt templates while preserving safe fallback behavior when templates are missing.
- algorithmic_behavior: `get_template_dir` maps a caller-supplied hooks/lib path to the plugin root’s `prompt-template` directory by going two levels up at `hooks/lib/template-loader.sh:24-31`. `load_template` concatenates a base template directory and template name, prints file contents if present, and otherwise warns to stderr while returning an empty string at `hooks/lib/template-loader.sh:33-48`. `render_template` accepts content plus `VAR=value` arguments, exports them as `TMPL_VAR_*` environment entries at `hooks/lib/template-loader.sh:58-69`, then runs one `awk` pass over each line at `hooks/lib/template-loader.sh:71-129`. The awk scanner finds `{{...}}` delimiters, looks up the exact placeholder name in the prefixed environment map, substitutes known variables, preserves unknown placeholders, and does not rescan substituted values. `load_and_render` composes loading and rendering at `hooks/lib/template-loader.sh:134-147`. `append_template` emits base content followed by loaded template content at `hooks/lib/template-loader.sh:149-161`. `load_and_render_safe` suppresses missing-template warnings, substitutes into a fallback message if the template is missing or if rendering yields empty output, and returns a usable prompt fragment at `hooks/lib/template-loader.sh:167-203`. `validate_template_dir` requires the template root plus `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:205-222`.
- inputs_outputs_state: Inputs are a filesystem template root, a repository-relative template name, template content, and zero or more variable assignments. Outputs are rendered text on stdout and warnings/errors on stderr. There is no persistent state mutation. The renderer’s transient state is only the prefixed process environment passed to awk, avoiding collisions with normal environment names by using `TMPL_VAR_`.
- gates_or_invariants: The main invariant is single-pass substitution: comments explicitly state that `{{VAR}}` inside a variable value is not expanded at `hooks/lib/template-loader.sh:50-57`, and the awk implementation sends replacement values directly to `result` at `hooks/lib/template-loader.sh:115-125`. Missing variables are preserved as their original placeholder at `hooks/lib/template-loader.sh:119-122`. Unclosed `{{` is treated literally by outputting the braces and continuing at `hooks/lib/template-loader.sh:102-109`. Missing templates are non-fatal for normal load paths; safe load paths replace them with caller-provided fallback content at `hooks/lib/template-loader.sh:176-186`. `validate_template_dir` is stricter and returns nonzero if required prompt subdirectories are absent at `hooks/lib/template-loader.sh:211-219`, but `loop-common.sh` treats that failure as a warning and continues with inline fallbacks at `hooks/lib/loop-common.sh:139-145`.
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh` at `hooks/lib/loop-common.sh:135-137`, which initializes `TEMPLATE_DIR` using `get_template_dir` at `hooks/lib/loop-common.sh:139-140`. This makes it available to all RLCR validators and stop hooks that source common logic. Direct uses include schema block rendering in `hooks/loop-plan-file-validator.sh:60-66`, git/plan warning template loads in `hooks/loop-codex-stop-hook.sh:446-456`, next-round prompt and footer rendering in `hooks/loop-codex-stop-hook.sh:1071-1097`, push note loading at `hooks/loop-codex-stop-hook.sh:1099-1105`, and goal tracker request loading at `hooks/loop-codex-stop-hook.sh:1108-1113`. `tests/test-template-references.sh` scans hook references to ensure template paths exist.
- edge_cases_or_failure_modes: Values containing `=` are handled because the parser splits only on the first equals sign: `var_name="${var_assignment%%=*}"`, `var_value="${var_assignment#*=}"` at `hooks/lib/template-loader.sh:65-68`. Variable names are not restricted by code despite the header saying uppercase/numbers/underscores only at `hooks/lib/template-loader.sh:9-13`; any exact text between braces can be looked up if a matching assignment was provided. Empty placeholder `{{}}` is preserved unless an empty variable name is deliberately supplied. Multiline variable values enter the environment and can be emitted by awk, but because awk prints each processed input line, embedded newlines in replacement values may create multi-line output from one source line. If a template file exists but is intentionally empty, `load_and_render_safe` treats it like missing content and uses fallback. Because `load_template` blindly joins paths, template names with `../` could traverse outside the template directory if called with untrusted names; current callers use static template paths discovered in source.
- validation_or_tests: `tests/test-template-loader.sh` covers `get_template_dir`, existing and missing template loads, single and multiple variable substitution, multiline templates, special characters, integration with real templates, ignored unused variables, and preservation of unknown variables at `tests/test-template-loader.sh:41-220`. It has explicit placeholder-injection prevention tests where values containing `{{VAR_B}}` or `{{VAR_A}}` remain unexpanded at `tests/test-template-loader.sh:488-543`, plus empty values, non-ASCII content/value handling, underscore-prefixed variables, and numeric variable names at `tests/test-template-loader.sh:545-639`. `tests/test-error-scenarios.sh` covers missing files/directories returning empty, strict-mode survival, safe fallback substitution, template-dir validation, empty placeholder preservation, and unclosed placeholder preservation at `tests/test-error-scenarios.sh:42-226`. I did not run tests because this branch export is read-only.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-083 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Claude-facing footer template appended to generated next-round RLCR prompts after Codex or review feedback finds issues. It defines the transition contract for continuing the loop: do not escape the loop, optionally simplify code, commit changes, and write the round summary to the generated summary file.
- algorithmic_behavior: The template is short but stateful through its single placeholder. It starts with a separator at `prompt-template/claude/next-round-footer.md:2`, warns that the worker must not exit the RLCR loop by “lying,” editing the loop state file, or running `cancel-rlcr-loop` at `prompt-template/claude/next-round-footer.md:4`, then lists required completion actions at `prompt-template/claude/next-round-footer.md:6-9`. The only template variable is `{{NEXT_SUMMARY_FILE}}` at `prompt-template/claude/next-round-footer.md:9`. In the stop hook, `NEXT_SUMMARY_FILE` is derived as `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` at `hooks/loop-codex-stop-hook.sh:1067-1070`, then rendered into this footer with `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:1093-1097`.
- inputs_outputs_state: Input is the generated `NEXT_SUMMARY_FILE` path supplied by the stop hook. Output is markdown appended to `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md`. It does not mutate state itself; the mutation happens in the caller immediately before prompt generation, where `current_round` is advanced in `state.md` via sed and atomic temp-file move at `hooks/loop-codex-stop-hook.sh:1062-1065`. The footer’s instruction to write the summary creates an expected artifact for later stop-hook validation of the next round.
- gates_or_invariants: The footer reinforces loop integrity by explicitly forbidding manual loop termination or state-file editing at `prompt-template/claude/next-round-footer.md:4`. It requires a descriptive commit at `prompt-template/claude/next-round-footer.md:8` and a summary in the specific generated summary path at `prompt-template/claude/next-round-footer.md:9`. If the template is missing or renders empty, the caller falls back to `## Before Exiting` plus “Commit your changes and write summary to {{NEXT_SUMMARY_FILE}}” at `hooks/loop-codex-stop-hook.sh:1093-1097`; that fallback preserves commit and summary requirements but loses the stronger anti-escape and code-simplifier instructions. If `push_every_round` is true, a separate push-note template is appended after this footer at `hooks/loop-codex-stop-hook.sh:1099-1105`.
- dependencies_and_callers: Loaded only by `hooks/loop-codex-stop-hook.sh` in the next-round prompt assembly path. It depends on `hooks/lib/template-loader.sh` through `load_and_render_safe`, so unresolved placeholders are preserved and missing templates degrade to fallback. It coordinates with `prompt-template/claude/next-round-prompt.md`, optional `prompt-template/claude/post-alignment-action-items.md`, optional `prompt-template/claude/push-every-round-note.md`, and `prompt-template/claude/goal-tracker-update-request.md`, all appended in the same next-round prompt assembly sequence at `hooks/loop-codex-stop-hook.sh:1071-1113`.
- edge_cases_or_failure_modes: The footer assumes the receiving agent can commit; in read-only or restricted environments this instruction can conflict with runtime permissions, but the template itself has no environment detection. It mentions `code-simplifier` only conditionally at `prompt-template/claude/next-round-footer.md:7`, so absence of that plugin should not block the loop. If `NEXT_SUMMARY_FILE` contains literal `{{...}}`, the renderer’s single-pass behavior prevents nested placeholder expansion, preserving the path as supplied. If the stop hook cannot render the template, fallback content is less strict and omits the warning against editing loop state. The path is emitted with an `@` prefix in `@{{NEXT_SUMMARY_FILE}}`, making the generated prompt depend on Claude-style file reference syntax.
- validation_or_tests: The template is indirectly covered by template reference scanning and stop-hook prompt-generation tests. `rg` shows the sole direct load at `hooks/loop-codex-stop-hook.sh:1096`. `tests/test-template-references.sh` is designed to extract `load_template`, `load_and_render`, and `load_and_render_safe` references from hook code and verify referenced templates exist, as described at `tests/test-template-references.sh:67-94`. `tests/test-finalize-phase.sh` includes related prompt checks for code-simplifier wording in finalize flow at `tests/test-finalize-phase.sh:473`, but this specific next-round footer has no dedicated assertion located in the inspected references. I did not run tests because this branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence headings present, matching the 3 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`