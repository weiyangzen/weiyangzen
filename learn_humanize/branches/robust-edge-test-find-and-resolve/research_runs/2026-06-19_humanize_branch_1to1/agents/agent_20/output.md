# agent_20 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-020 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash gate for the RLCR loop. It prevents shell commands from bypassing the safer Write/Edit/Read validators or mutating loop-owned control files. It is registered for the Bash tool in `hooks/hooks.json:42-48`.
- algorithmic_behavior: The hook reads JSON from stdin, validates hook shape through `validate_hook_input`, rejects malformed/deep JSON, ignores non-Bash tools, and requires `tool_input.command` for Bash commands at `hooks/loop-bash-validator.sh:22-45`. It lowercases the command for case-insensitive pattern matching at `hooks/loop-bash-validator.sh:45-46`, locates the newest active loop under `.humanize/rlcr` at `hooks/loop-bash-validator.sh:52-58`, and chooses `finalize-state.md` over `state.md` when finalization is active at `hooks/loop-bash-validator.sh:61-65`. It then parses state strictly and fails closed on malformed state at `hooks/loop-bash-validator.sh:67-72`.
- inputs_outputs_state: Input is Claude hook JSON on stdin with `tool_name` and `tool_input.command`. Environment input includes `CLAUDE_PROJECT_DIR`, defaulting to `pwd`, used to find `.humanize/rlcr` at `hooks/loop-bash-validator.sh:52-54`. State input is the active loop `state.md` or `finalize-state.md`; `current_round` and `push_every_round` drive branch behavior at `hooks/loop-bash-validator.sh:72-83`. Output is process exit status plus stderr guidance: `0` allows the command, `1` blocks malformed/unsafe hook or state input, and `2` blocks a valid Bash command that violates an RLCR invariant.
- gates_or_invariants: It blocks `git push` unless `push_every_round: true` at `hooks/loop-bash-validator.sh:82-91`. It blocks `git add` attempts that would stage `.humanize` loop state at `hooks/loop-bash-validator.sh:94-103`, delegated to `git_adds_humanize` in `hooks/lib/loop-common.sh:622-740`. It blocks writes, removals, redirections, `tee`, in-place editors, `dd`, `truncate`, `exec >`, and `mv` or `cp` destinations for protected files through `command_modifies_file`, defined in `hooks/lib/loop-common.sh:772-800`. It blocks `finalize-state.md` first because `state.md` is a substring at `hooks/loop-bash-validator.sh:117-134`. It additionally blocks `mv` and `cp` when protected state files are the source, including shell segments and wrappers, at `hooks/loop-bash-validator.sh:147-306`. It protects `.humanize/rlcr/**/plan.md` at `hooks/loop-bash-validator.sh:308-319`, `goal-tracker.md` with round-specific guidance at `hooks/loop-bash-validator.sh:321-336`, prompts at `hooks/loop-bash-validator.sh:338-346`, summaries at `hooks/loop-bash-validator.sh:348-357`, and todos files except full-path `round-1-todos.md` or `round-2-todos.md` in the active loop directory at `hooks/loop-bash-validator.sh:359-371`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` loaded at `hooks/loop-bash-validator.sh:14-16`. Key shared dependencies are hook JSON validation at `hooks/lib/loop-common.sh:55-92`, nesting validation at `hooks/lib/loop-common.sh:112-129`, active loop discovery at `hooks/lib/loop-common.sh:147-170`, strict state parsing at `hooks/lib/loop-common.sh:231-296`, cancel authorization at `hooks/lib/loop-common.sh:437-614`, git-add protection at `hooks/lib/loop-common.sh:622-740`, and block-message/template helpers at `hooks/lib/loop-common.sh:347-405` and `hooks/lib/loop-common.sh:742-814`. Template lookup is covered by `tests/test-template-references.sh:56-64`, which scans this hook and the shared library for referenced templates.
- edge_cases_or_failure_modes: With no active loop, all Bash commands pass at `hooks/loop-bash-validator.sh:56-58`. Malformed active state blocks all Bash operations protectively at `hooks/loop-bash-validator.sh:67-70`. Authorized cancellation is a narrow exception: moving `state.md` or `finalize-state.md` to `cancel-state.md` is allowed only when `.cancel-requested` exists and the command has no shell injection, extra args, wrong paths, symlink source, mixed quote styles, shell operators, command substitution, or hidden variables, as enforced in `hooks/lib/loop-common.sh:455-614`. The source-protection segment splitter handles chained commands, pipes, background operators, and redirection operators with placeholder substitutions at `hooks/loop-bash-validator.sh:152-168`; it strips leading redirections before matching source-file operations at `hooks/loop-bash-validator.sh:173-262`. Todo allowlisting is intentionally path-restricted; same basename in another root or old loop directory is blocked by requiring the lowercased active loop prefix at `hooks/loop-bash-validator.sh:363-368`.
- validation_or_tests: `tests/test-bash-validator-patterns.sh:36-61` directly tests `command_modifies_file`, with coverage for redirection, `tee`, in-place editors, `mv/cp/rm`, `dd`, `truncate`, and `exec >` at `tests/test-bash-validator-patterns.sh:69-133`. Finalize-state bash protections are tested in `tests/test-finalize-phase.sh:322-356`. Cancel exceptions are tested in `tests/test-cancel-signal-file.sh:74-124` and later negative cases in that file. Todo path allowlist behavior for Bash is tested in `tests/test-allowlist-validators.sh:288-380`. The full suite runner includes `test-bash-validator-patterns.sh`, `test-allowlist-validators.sh`, `test-finalize-phase.sh`, `test-cancel-signal-file.sh`, and hook robustness tests at `tests/run-all-tests.sh:32-63`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-050 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook preflight that blocks RLCR exit when Claude has unfinished native TodoWrite items. The stop hook invokes this checker before expensive review at `hooks/loop-codex-stop-hook.sh:255-309`.
- algorithmic_behavior: The test script creates a temp workspace, runs the checker as `python3 "$TODO_CHECKER"` with hook JSON on stdin, records exit codes, and increments pass/fail counters through helper functions at `tests/test-todo-checker.sh:20-38`. It specifies the checker’s algorithm through scenarios: invalid or empty hook input must exit `2`; valid hook input missing `transcript_path` or pointing to a nonexistent transcript exits `0`; the latest TodoWrite list is authoritative; every todo status other than exactly `completed` is incomplete; malformed transcript lines are ignored; multiple transcript formats are supported.
- inputs_outputs_state: Inputs are synthetic hook JSON objects and temporary transcript JSONL files under `mktemp -d` at `tests/test-todo-checker.sh:36-38`. Outputs are test pass/fail lines and final process status: `0` only when all checks pass, `1` if any check fails at `tests/test-todo-checker.sh:295-313`. The checker under test returns `0` for no todos/all completed, `1` for incomplete todos, and `2` for hook-input parse errors, documented in `hooks/check-todos-from-transcript.py:7-13` and exercised by `tests/test-todo-checker.sh:51-97`.
- gates_or_invariants: The key invariant is that a session cannot stop while the latest TodoWrite state includes pending, in-progress, missing-status, or otherwise non-completed items. The test enforces all-completed success at `tests/test-todo-checker.sh:106-119`, pending failure at `tests/test-todo-checker.sh:121-134`, in-progress failure at `tests/test-todo-checker.sh:144-157`, missing-status failure at `tests/test-todo-checker.sh:249-263`, and empty-content pending failure at `tests/test-todo-checker.sh:265-278`. It also verifies that incomplete todo content appears in output at `tests/test-todo-checker.sh:136-142`, matching checker output construction in `hooks/check-todos-from-transcript.py:113-126`.
- dependencies_and_callers: Direct dependency is `hooks/check-todos-from-transcript.py`, assigned through `TODO_CHECKER="$PROJECT_ROOT/hooks/check-todos-from-transcript.py"` at `tests/test-todo-checker.sh:11-14`. The checker parses JSONL in `find_latest_todos` at `hooks/check-todos-from-transcript.py:20-87` and main hook JSON at `hooks/check-todos-from-transcript.py:90-129`. Runtime caller is `hooks/loop-codex-stop-hook.sh`, which treats checker exit `2` as a parse-error block and exit `1` as an incomplete-todos block at `hooks/loop-codex-stop-hook.sh:263-308`. The suite runner includes this test as a first-class test suite at `tests/run-all-tests.sh:32-37`.
- edge_cases_or_failure_modes: Invalid JSON on stdin and empty stdin are parse errors and must exit `2` at `tests/test-todo-checker.sh:51-73`. Missing transcript path and nonexistent transcript file fail open with exit `0` at `tests/test-todo-checker.sh:75-97`, consistent with `hooks/check-todos-from-transcript.py:99-111`. Empty transcript files exit `0` at `tests/test-todo-checker.sh:166-177`. Invalid JSONL lines inside a transcript are ignored rather than fatal at `tests/test-todo-checker.sh:179-194`, matching `json.JSONDecodeError: continue` at `hooks/check-todos-from-transcript.py:36-40`. Multiple TodoWrite calls use the latest nonempty todo list at `tests/test-todo-checker.sh:196-210`, matching repeated assignment to `latest_todos` in `hooks/check-todos-from-transcript.py:62-85`. Supported transcript shapes include Claude assistant message blocks, direct `tool_use` entries, and alternative `type: message` entries at `tests/test-todo-checker.sh:212-240`.
- validation_or_tests: This file is itself the validation artifact and contains 16 numbered tests across input handling, todo detection, transcript format variations, and edge cases at `tests/test-todo-checker.sh:45-294`. It is included in the aggregate test runner at `tests/run-all-tests.sh:32-37`. The covered production behavior is visible in `hooks/check-todos-from-transcript.py:51-85` for transcript formats and `hooks/check-todos-from-transcript.py:113-126` for incomplete status detection.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-080 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Small block-message template used by write-path validation when an agent attempts to write a round summary outside the active RLCR loop directory. It is part of the prompt/template contract that turns a validation failure into actionable remediation.
- algorithmic_behavior: The template contains a title, a mandatory-location rule, and a `{{CORRECT_PATH}}` interpolation slot at `prompt-template/block/wrong-summary-location.md:1-5`. The write validator renders it when `IS_SUMMARY_FILE` is true and the target is not inside `.humanize/rlcr`, building `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` at `hooks/loop-write-validator.sh:170-181`. The template does not implement branching itself; it is the output surface for that branch in the validator state machine.
- inputs_outputs_state: Input is the template variable `CORRECT_PATH`, supplied by `load_and_render_safe` from `hooks/loop-write-validator.sh:175-180`. Output is a stderr block message with the current loop’s canonical summary path, followed by validator exit `2` at `hooks/loop-write-validator.sh:179-181`. State dependencies are the active loop directory and current round, previously derived by the write validator before this branch.
- gates_or_invariants: The invariant represented is that summary files must be placed inside the active loop directory, not an arbitrary project path or external path. The exact user-facing rule is expressed at `prompt-template/block/wrong-summary-location.md:3-5`. It complements the later wrong-directory check for files already under `.humanize/rlcr` but in the wrong loop directory at `hooks/loop-write-validator.sh:220-234`.
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh:174-181`. The template is referenced in repository-wide template validation via `tests/test-template-references.sh:56-64` and reference extraction at `tests/test-template-references.sh:83-110`, which prevents missing-template failures from producing empty block messages. Related templates using the same `CORRECT_PATH` contract include `prompt-template/block/wrong-directory-path.md`, `prompt-template/block/wrong-round-number.md`, and `prompt-template/block/summary-bash-write.md`, found by reference search, but this file specifically covers the outside-loop summary location case.
- edge_cases_or_failure_modes: If this template were missing or malformed, `load_and_render_safe` would fall back to the inline message in `hooks/loop-write-validator.sh:176-180`, so the validator still blocks. If `CORRECT_PATH` is not supplied, the template would render with an unresolved placeholder or fallback behavior depending on the template loader; repository tests are intended to catch missing references, not semantic variable omissions. This template is not used by `hooks/loop-bash-validator.sh`; Bash summary writes use `summary_bash_blocked_message` and `prompt-template/block/summary-bash-write.md` through `hooks/loop-bash-validator.sh:353-356` and `hooks/lib/loop-common.sh:385-394`.
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh:56-64` and `tests/test-template-references.sh:83-110`. Rendering behavior for this path is indirectly covered by write-validator tests and template-loader/comprehensive template suites listed in `tests/run-all-tests.sh:32-45`. The core branch invoking it is visible at `hooks/loop-write-validator.sh:174-181`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present; item IDs intentionally appear only in their section headers`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`