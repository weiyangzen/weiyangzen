# agent_12 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex review prompt bundle for the RLCR loop. It defines the review contracts that decide whether Claude may stop, must continue, must enter Finalize Phase, or must stop for stagnation. Recursive inspection found exactly three child templates: `regular-review.md`, `full-alignment-review.md`, and `goal-tracker-update-section.md`.
- algorithmic_behavior: `regular-review.md` requires Codex to read the original plan first, compare Claude’s claimed summary against the plan and prompt, reject deferrals/pending work, audit goal alignment, and only end with `COMPLETE` when all original tasks and ACs are fully done (`prompt-template/codex/regular-review.md:5`, `:20`, `:33`, `:49`, `:53`). `full-alignment-review.md` is the every-fifth-round checkpoint; it requires AC-by-AC status, forgotten item detection, deferred item audit, implementation review, historical last-5-round stagnation review, and `STOP` as the final line on stagnation (`prompt-template/codex/full-alignment-review.md:3`, `:19`, `:30`, `:36`, `:59`, `:74`, `:84`, `:88`). `goal-tracker-update-section.md` injects a shared obligation for Codex to evaluate Claude’s “Goal Tracker Update Request,” apply approved changes itself, and never edit the immutable goal/AC section (`prompt-template/codex/goal-tracker-update-section.md:1`, `:3`, `:5`, `:6`, `:10`).
- inputs_outputs_state: Inputs are loop state, current round, max iterations, plan file, prompt file, summary content, goal tracker, docs path, loop timestamp, and history paths. `hooks/loop-codex-stop-hook.sh` reads `SUMMARY_CONTENT`, builds `round-N-review-prompt.md`, selects full-alignment when `CURRENT_ROUND % 5 == 4`, and renders these templates with `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:674`, `:678`, `:683`, `:686`, `:723`, `:739`). Outputs are `round-N-review-prompt.md`, `round-N-review-result.md`, cache debug files under `XDG_CACHE_HOME`/`$HOME/.cache`, and loop state transitions (`hooks/loop-codex-stop-hook.sh:782`, `:794`, `:827`, `:844`, `:958`). State changes are strict: final line `COMPLETE` renames `state.md` to `finalize-state.md` and blocks with a Finalize prompt unless max iterations has been reached; final line `STOP` calls `end_loop` with stop state; otherwise state advances to the next round and a next prompt is generated (`hooks/loop-codex-stop-hook.sh:960`, `:967`, `:970`, `:985`, `:1028`, `:1054`, `:1062`, `:1080`).
- gates_or_invariants: Review output markers are exact final non-empty lines, preventing false positives like “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:960`). The templates make deferrals and unfinished work incompatible with `COMPLETE` (`prompt-template/codex/regular-review.md:53`, `:54`, `:55`; `prompt-template/codex/full-alignment-review.md:91`, `:92`, `:93`). Hook gates also require valid state, safe model/effort fields, existing summary file, initialized goal tracker in round 0, Codex availability, non-empty review result, and max-iteration handling (`hooks/loop-codex-stop-hook.sh:88`, `:105`, `:532`, `:566`, `:642`, `:758`, `:883`, `:920`, `:948`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh`, using `hooks/lib/template-loader.sh` for single-pass `{{VAR}}` rendering and fallback behavior (`hooks/lib/template-loader.sh:50`, `:54`, `:71`, `:170`, `:179`, `:189`). Depends on `codex exec`, `jq`, `portable-timeout.sh`, shell utilities, and sibling Claude templates for next-round/finalize prompts (`hooks/loop-codex-stop-hook.sh:798`, `:818`, `:1010`, `:1080`, `:1096`, `:1109`). Sibling/parent coordination is through `prompt-template` as a common template root; `validate_template_dir` expects `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205`, `:216`).
- edge_cases_or_failure_modes: Missing Codex blocks exit with diagnostic JSON (`hooks/loop-codex-stop-hook.sh:758`). Missing/empty result file or failed stdout copy blocks for retry (`hooks/loop-codex-stop-hook.sh:904`, `:920`, `:948`). Missing templates fall back to embedded fallback prompts, so the loop keeps operating with reduced specificity (`hooks/loop-codex-stop-hook.sh:699`, `:710`; `hooks/lib/template-loader.sh:179`). Variable values are single-pass rendered, so `{{...}}` embedded in a summary is not recursively expanded into prompt variables (`hooks/lib/template-loader.sh:54`). `STOP` in a non-alignment round is treated as unusual but still honored (`hooks/loop-codex-stop-hook.sh:1043`).
- validation_or_tests: `tests/test-template-references.sh` checks that common and referenced templates exist, including this codex group by direct hook references and shared template checks (`tests/test-template-references.sh:130`, `:136`, `:149`). `tests/test-templates-comprehensive.sh` renders the real `codex/goal-tracker-update-section.md` and asserts the heading and substituted goal tracker path are present (`tests/test-templates-comprehensive.sh:531`, `:532`, `:535`). The template loader tests cover single-pass rendering behavior and real-template integration (`tests/test-template-loader.sh:147`).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-042 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: True end-to-end executable specification for `_humanize_monitor_codex` graceful shutdown behavior when `.humanize/rlcr` is deleted. It validates the acceptance criteria listed in its header: clean user-facing stop, no shell glob errors, terminal cleanup, and bash/zsh compatibility (`tests/test-monitor-e2e-real.sh:3`, `:5`, `:8`, `:12`).
- algorithmic_behavior: The script creates isolated temp projects, real `.humanize/rlcr/<timestamp>` session directories, `state.md`, `goal-tracker.md`, fake cache log files, and runner scripts that source the real `scripts/humanize.sh` then call `_humanize_monitor_codex` (`tests/test-monitor-e2e-real.sh:48`, `:65`, `:69`, `:79`, `:93`, `:101`, `:105`, `:142`, `:146`). It starts the monitor in the background, waits for startup, deletes `.humanize/rlcr`, waits up to 20 half-second polls, and fails if the process does not exit (`tests/test-monitor-e2e-real.sh:155`, `:160`, `:163`, `:166`, `:173`). It repeats the same deletion test under zsh when zsh is installed (`tests/test-monitor-e2e-real.sh:234`, `:277`, `:303`, `:306`, `:316`, `:323`, `:326`).
- inputs_outputs_state: Inputs are the repository root, fake project directory, fake `HOME`/`XDG_CACHE_HOME`, a timestamped RLCR session, minimal state and goal tracker files, and a fake cached `round-1-codex-run.log` matching the project-path sanitization scheme (`tests/test-monitor-e2e-real.sh:17`, `:18`, `:94`, `:98`, `:99`, `:101`). Outputs are captured monitor logs, PASS/FAIL counters, and final exit code 0/1 from the test harness (`tests/test-monitor-e2e-real.sh:155`, `:183`, `:370`, `:373`, `:381`, `:385`). The target implementation’s state transition is monitor active to graceful stop: `_graceful_stop` calls `_cleanup`, sets `monitor_running=false`, restores terminal, prints `Stopped monitoring.`, then prints `Monitoring stopped: <reason>` and returns 0 when the loop directory disappears (`scripts/humanize.sh:543`, `:559`, `:561`, `:564`, `:572`, `:575`, `:612`, `:615`, `:616`, `:617`).
- gates_or_invariants: The test requires “Monitoring stopped:” and “directory no longer exists,” rejects `no matches found` and `bad pattern`, requires a cleanup message, greps source for scroll-region reset `printf "\033[r"`, and requires `EXIT_CODE:0` (`tests/test-monitor-e2e-real.sh:186`, `:192`, `:199`, `:207`, `:214`, `:221`). The implementation invariant behind this is that `_find_latest_session` and `_find_latest_codex_log` use `find` after checking directory existence rather than unguarded globs, especially for zsh (`scripts/humanize.sh:214`, `:224`, `:231`, `:234`, `:239`, `:251`, `:270`, `:276`, `:310`).
- dependencies_and_callers: Depends on `scripts/humanize.sh`, bash, optional zsh, `sed`, `grep`, `pkill`, `kill`, `tail`/cache log conventions, and terminal commands shimmed by local functions. The monitor implementation depends on top-level helpers `humanize_parse_goal_tracker`, `humanize_parse_git_status`, and `humanize_split_to_array` through wrappers (`scripts/humanize.sh:375`, `:377`, `:378`, `:379`). The public caller is `humanize monitor rlcr-loop`, which dispatches to `_humanize_monitor_codex` (`scripts/humanize.sh:870`, `:876`, `:879`, `:880`).
- edge_cases_or_failure_modes: zsh is skipped if unavailable, so AC-1.4 is conditionally validated (`tests/test-monitor-e2e-real.sh:234`). The runner accepts an `OUTPUT_FILE` argument but capture is actually done by shell redirection outside the runner (`tests/test-monitor-e2e-real.sh:112`, `:156`). The terminal reset is partly source-verified rather than asserted from emitted escape bytes because `tput`/terminal behavior is shimmed (`tests/test-monitor-e2e-real.sh:120`, `:131`, `:213`). The cleanup trap kills lingering processes matching the test basename and removes `/tmp` state, so stale background monitors are treated as test contamination (`tests/test-monitor-e2e-real.sh:51`, `:53`, `:54`).
- validation_or_tests: This file is itself the validation harness. It was inspected directly; no test execution was performed in this read-only research pass. Related monitor implementation points are deletion checks in all monitoring branches (`scripts/humanize.sh:654`, `:656`, `:747`, `:749`) and terminal setup/restore (`scripts/humanize.sh:520`, `:525`, `:530`, `:533`).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-072 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Blocking message template for the PreToolUse Bash validator when a Bash command attempts to modify `round-N-summary.md`. It directs the agent to use Write/Edit at `{{CORRECT_PATH}}` because Bash bypasses validation hooks (`prompt-template/block/summary-bash-write.md:1`, `:3`, `:5`, `:7`).
- algorithmic_behavior: The template is rendered by `summary_bash_blocked_message`, which supplies `CORRECT_PATH` and falls back to an inline equivalent if the template is missing (`hooks/lib/loop-common.sh:385`, `:387`, `:389`, `:393`). The only direct runtime caller is `hooks/loop-bash-validator.sh`: after parsing a Bash hook event and active loop state, it checks whether the lowercased command modifies `round-[0-9]+-summary.md`; if yes, it renders this message to stderr and exits 2 to block the tool (`hooks/loop-bash-validator.sh:22`, `:34`, `:45`, `:52`, `:54`, `:67`, `:72`, `:353`, `:354`, `:355`, `:356`).
- inputs_outputs_state: Inputs are hook JSON, `.tool_input.command`, active loop directory, strict state-derived `CURRENT_ROUND`, and the target command text. Output is a markdown block message containing the current round’s correct summary path, emitted on stderr, plus exit status 2; no repository file state is changed by the validator path (`hooks/loop-bash-validator.sh:24`, `:40`, `:68`, `:353`). The intended state invariant is that summary creation/editing must flow through Write/Edit validators, preserving round/path validation instead of shell side effects.
- gates_or_invariants: The gate only applies to the `Bash` tool and only while an active loop exists (`hooks/loop-bash-validator.sh:36`, `:56`). It fails closed on malformed state before reaching the summary gate (`hooks/loop-bash-validator.sh:67`, `:69`). Detection is provided by `command_modifies_file`, whose patterns cover redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp` destination writes, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` (`hooks/lib/loop-common.sh:772`, `:779`, `:780`, `:782`, `:783`, `:786`, `:787`, `:788`, `:789`, `:790`, `:791`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `jq`, active loop discovery, strict state parsing, and template root initialization. Sibling block templates enforce adjacent invariants for prompt, state, goal tracker, and todos files; this one is the summary-specific Bash bypass message (`hooks/lib/loop-common.sh:347`, `:358`, `:367`, `:396`).
- edge_cases_or_failure_modes: The summary Bash gate is pattern-based and inherits known detection limits: multiple-source `cp file1 file2 goal-tracker.md` is documented as not detected for the generic function, and variable indirection like `echo x > $FILE` is intentionally not detected because the literal target is absent (`tests/test-bash-validator-patterns.sh:165`, `:166`, `:167`, `:170`, `:171`). The direct trigger is `round-N-summary.md`; `finalize-summary.md` is not matched by this specific block, despite the template’s generic “summary files” wording (`hooks/loop-bash-validator.sh:353`). Paths with spaces are also weak for the simple `[^[:space:]]*` patterns in `command_modifies_file` (`hooks/lib/loop-common.sh:780`).
- validation_or_tests: `tests/test-template-references.sh` asserts `block/summary-bash-write.md` exists in the common template set (`tests/test-template-references.sh:149`, `:152`, `:156`, `:161`). `tests/test-bash-validator-patterns.sh` has a summary-specific group that detects `echo x > round-5-summary.md` and `cat data >> round-10-summary.md`, while allowing read-only `cat round-5-summary.md` (`tests/test-bash-validator-patterns.sh:186`, `:192`, `:193`, `:194`). Template rendering behavior is covered by the shared template-loader tests and comprehensive real-template rendering tests (`tests/test-template-loader.sh:147`; `tests/test-templates-comprehensive.sh:500`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-012, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-042, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-072
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`