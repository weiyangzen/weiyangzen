# agent_01 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the full Humanize Claude Code plugin algorithm surface. It defines the RLCR loop: setup creates `.humanize/rlcr/<timestamp>/` state, prompts, plan backup, and goal tracker; Claude works and writes summaries; stop hooks run Codex review; review output either blocks with next-round instructions, enters Finalize Phase, stops by max/circuit breaker, or completes. The public description and command flow are documented in `README.md:7-13`, `README.md:68-84`, and command wrappers in `commands/start-rlcr-loop.md:10-23`, `commands/cancel-rlcr-loop.md:9-36`, `commands/gen-plan.md:19-25`.
- algorithmic_behavior: Root coordination is hook-driven. `.claude-plugin/plugin.json:1-19` and `.claude-plugin/marketplace.json:1-14` package the plugin; `hooks/hooks.json:14-58` wires `Write`, `Edit`, `Read`, `Bash`, `UserPromptSubmit`, and `Stop` events to validator/stop scripts. `scripts/setup-rlcr-loop.sh` validates plan paths, git state, model/effort fields, and Codex availability, then writes `state.md`, `goal-tracker.md`, `plan.md`, and `round-0-prompt.md` under the loop directory (`scripts/setup-rlcr-loop.sh:227-379`, `scripts/setup-rlcr-loop.sh:385-446`, `scripts/setup-rlcr-loop.sh:508-519`, `scripts/setup-rlcr-loop.sh:527-684`). `hooks/loop-codex-stop-hook.sh` is the main transition engine: it finds the active loop (`hooks/loop-codex-stop-hook.sh:59-82`), enforces quick gates, renders Codex review prompts, runs `codex exec`, interprets final markers, and advances state (`hooks/loop-codex-stop-hook.sh:967-1026`, `hooks/loop-codex-stop-hook.sh:1028-1055`, `hooks/loop-codex-stop-hook.sh:1062-1128`).
- inputs_outputs_state: Inputs are slash command arguments, hook JSON, git repository state, plan files, TodoWrite transcript state, generated summaries, goal tracker contents, and Codex review output. Outputs include JSON hook block/allow decisions, Markdown prompts/reviews/summaries, renamed state files, cache logs under XDG cache, and human-visible monitor output. Core states are `state.md` for active loops, `finalize-state.md` after Codex `COMPLETE`, and terminal states such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`; constants and `end_loop` are centralized in `hooks/lib/loop-common.sh:26-40` and `hooks/lib/loop-common.sh:816-849`.
- gates_or_invariants: Setup rejects absolute plan paths, spaces, shell metacharacters, symlinks, paths outside the project, submodule paths, insufficient plan content, unsafe branch/model/effort strings, and invalid git prerequisites (`scripts/setup-rlcr-loop.sh:211-220`, `scripts/setup-rlcr-loop.sh:227-328`, `scripts/setup-rlcr-loop.sh:354-379`, `scripts/setup-rlcr-loop.sh:452-484`). Shared hook invariants include JSON validity, UTF-8/null-byte rejection, max nesting, active-loop discovery, strict state parsing, single-pass template rendering, and safe template fallback (`hooks/lib/loop-common.sh:46-92`, `hooks/lib/loop-common.sh:112-129`, `hooks/lib/loop-common.sh:147-170`, `hooks/lib/loop-common.sh:240-296`, `hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-203`). Stop-hook gates block stale schema/branch changes, plan deletion/modification, incomplete todos, git status failure, large files, uncommitted changes, unpushed commits when required, missing summaries, uninitialized Round 0 goal tracker, and max iterations (`hooks/loop-codex-stop-hook.sh:134-179`, `hooks/loop-codex-stop-hook.sh:185-253`, `hooks/loop-codex-stop-hook.sh:261-300`, `hooks/loop-codex-stop-hook.sh:322-340`, `hooks/loop-codex-stop-hook.sh:402-423`, `hooks/loop-codex-stop-hook.sh:432-518`, `hooks/loop-codex-stop-hook.sh:532-557`, `hooks/loop-codex-stop-hook.sh:565-633`, `hooks/loop-codex-stop-hook.sh:639-660`).
- dependencies_and_callers: Command docs call `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and `scripts/validate-gen-plan-io.sh`. Hook scripts source `hooks/lib/loop-common.sh`, which sources `hooks/lib/template-loader.sh`; git and Codex operations use `scripts/portable-timeout.sh`. `scripts/humanize.sh` provides monitor and public testable helpers (`scripts/humanize.sh:110-203`, `scripts/humanize.sh:213-868`). Templates in `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude` define review contracts and user-facing block reasons. Tests are orchestrated by `tests/run-all-tests.sh:32-63`, with shared counters/temp-dir helpers in `tests/test-helpers.sh:30-89`.
- edge_cases_or_failure_modes: Root handles missing active loop by allowing hook exit or monitor error, malformed state by fail-closed validator behavior, older schema by block, branch changes by block, `git status` failure by stop-hook block, Codex missing/non-zero/empty-output/wrong-output-path by block, Finalize Phase cancellation requiring confirmation, and shell bypass attempts against protected files. Known broad edge surfaces include shell-pattern parsing in `loop-bash-validator.sh`, fallback behavior when templates are missing, and display-only git parsing in `scripts/humanize.sh` that is separate from stop-hook fail-closed git gates.
- validation_or_tests: Validation coverage is broad: `tests/run-all-tests.sh:32-63` includes template loader, bash validator patterns, todo checker, plan-file hooks/validation, finalize phase, cancel signal file, monitor runtime/E2E, zsh monitor safety, gen-plan, and robustness suites. The assigned `tests/robustness/test-git-operations-robustness.sh` covers git status/state helper behavior. CI workflows include shell syntax, template tests, plan-file tests, version bump checks, and Claude review workflows under `.github/workflows`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-031 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor lifecycle edge behavior, especially AC-1.1 and AC-1.3: deleting `.humanize/` during `humanize monitor rlcr-loop` should produce a clean stop message and restore terminal state. The file states this purpose and usage at `tests/manual-monitor-test.sh:3-14`.
- algorithmic_behavior: The script dispatches on one positional command (`tests/manual-monitor-test.sh:23`). `setup` creates a fake active loop at `.humanize/rlcr/2026-01-16_99-99-99`, writes minimal `state.md`, writes minimal `goal-tracker.md`, and prints instructions to run `source scripts/humanize.sh && humanize monitor rlcr-loop` in another terminal (`tests/manual-monitor-test.sh:24-46`). `delete` removes `.humanize` and tells the tester to verify the monitor output and terminal restoration (`tests/manual-monitor-test.sh:47-57`). `cleanup` removes leftover `.humanize` state (`tests/manual-monitor-test.sh:58-61`). Unknown input prints usage and exits non-zero (`tests/manual-monitor-test.sh:63-70`).
- inputs_outputs_state: Input is one CLI argument: `setup`, `delete`, or `cleanup`. Filesystem output is a synthetic `.humanize/rlcr/<timestamp>/` state tree during setup, followed by deletion on `delete`/`cleanup`. User-visible output is instructional stdout. State transition under test is fake active loop present -> monitor running with no log -> `.humanize` removed -> monitor detects missing `.humanize/rlcr` and exits cleanly.
- gates_or_invariants: The script uses `set -euo pipefail` (`tests/manual-monitor-test.sh:17`). It assumes it is run from the repository root by resolving `SCRIPT_DIR` and `PROJECT_ROOT` then `cd`ing there (`tests/manual-monitor-test.sh:19-21`). The expected monitor invariant is explicit: output should contain `Monitoring stopped: .humanize/rlcr directory no longer exists`, terminal scroll region/cursor should be restored, and no shell glob errors should appear (`tests/manual-monitor-test.sh:51-54`). The implementation side is `_graceful_stop` calling `_cleanup`, which calls `_restore_terminal` (`scripts/humanize.sh:531-560`, `scripts/humanize.sh:564-577`), and the monitor loop checks missing `.humanize/rlcr` repeatedly (`scripts/humanize.sh:612-618`, `scripts/humanize.sh:654-657`, `scripts/humanize.sh:747-750`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` monitor command (`scripts/humanize.sh:870-903`) and specifically `_humanize_monitor_codex` (`scripts/humanize.sh:213-868`). Related automated coverage exists in `tests/test-monitor-runtime.sh` for `_graceful_stop`, idempotent cleanup, missing loop detection, and terminal restore source checks, plus `tests/test-zsh-monitor-safety.sh` for zsh glob-safety cases.
- edge_cases_or_failure_modes: This script intentionally uses `rm -rf .humanize`, so it is destructive to local loop state if run in a real active checkout. The synthetic `state.md` lacks YAML frontmatter, but monitor parsing is tolerant and uses direct `grep` fields (`scripts/humanize.sh:357-372`), so that is acceptable for display testing. Because this is manual, terminal restoration is observational rather than automatically asserted here. No log file is created, so the exercised path is the no-log polling branch before deletion.
- validation_or_tests: Manual pass criteria are printed in `delete` output (`tests/manual-monitor-test.sh:51-56`). Automated analogs include runtime tests checking `_cleanup` invokes `_restore_terminal`, `_graceful_stop` includes the reason text, and source contains terminal reset behavior (`tests/test-monitor-runtime.sh`, referenced by grep results around its AC-1.1/AC-1.3 sections).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-061 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Block template for the stop-hook fail-closed git-status gate. It explains that `git status` failed or timed out and prevents the RLCR loop from advancing when repository cleanliness cannot be verified.
- algorithmic_behavior: The template renders a Markdown block headed `Git Status Failed`, interpolates `{{GIT_STATUS_EXIT}}`, lists likely causes, and tells the user to check git status manually (`prompt-template/block/git-status-failed.md:1-10`). It is invoked when `hooks/loop-codex-stop-hook.sh` can confirm a git repo but `run_with_timeout "$GIT_TIMEOUT" git status --porcelain` exits non-zero (`hooks/loop-codex-stop-hook.sh:322-340`). The hook then emits a JSON hook response with `"decision": "block"` and a system message naming the failed exit code (`hooks/loop-codex-stop-hook.sh:337-339`).
- inputs_outputs_state: Input variable is `GIT_STATUS_EXIT`. Output is rendered Markdown assigned to `REASON`, embedded in the hook JSON response. No repository state is modified by this template path. The state transition is attempted Stop -> repository verification failure -> Stop blocked before large-file, clean-tree, summary, or Codex-review transitions run.
- gates_or_invariants: The invariant is fail-closed: git status failure must not be treated as clean. The hook comment states this explicitly at `hooks/loop-codex-stop-hook.sh:317`, captures exit status without `|| echo ""` fail-open behavior (`hooks/loop-codex-stop-hook.sh:324-327`), and blocks on any non-zero status (`hooks/loop-codex-stop-hook.sh:328-340`). Rendering uses `load_and_render_safe`, so if the template is missing or empty, a fallback message with the same exit-code placeholder is rendered (`hooks/loop-codex-stop-hook.sh:330-336`; `hooks/lib/template-loader.sh:170-203`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`. Rendering depends on `hooks/lib/template-loader.sh`, sourced through `hooks/lib/loop-common.sh`. Timeout behavior depends on `scripts/portable-timeout.sh`, which chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`).
- edge_cases_or_failure_modes: If `GIT_STATUS_EXIT` is not supplied, the placeholder remains because rendering intentionally preserves missing variables (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:115-122`). If `git` is not installed or `rev-parse --git-dir` fails, this specific template is not used because `GIT_IS_REPO` remains false and the stop hook skips git cleanliness checks. If timeout tooling is unavailable, `run_with_timeout` warns and runs without timeout (`scripts/portable-timeout.sh:64-68`), so a hang could still affect the gate on minimal systems.
- validation_or_tests: Template existence and placeholder behavior are covered by the repository’s template-focused suites (`tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-template-loader.sh`). The gate behavior is structurally represented in stop-hook tests and robustness timeout tests, though this assigned template itself is a static block with one variable.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-091 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for git-display helper algorithms in `scripts/humanize.sh`: `humanize_parse_git_status` and `humanize_detect_git_state`. It is labeled AC-5 and directly states it tests clean/dirty states, detached HEAD, rebase/merge detection, and non-git handling (`tests/robustness/test-git-operations-robustness.sh:3-10`).
- algorithmic_behavior: The script sources shared helpers, timeout wrapper, and production `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:15-21`). `parse_result` splits the production pipe-delimited status output into named fields (`tests/robustness/test-git-operations-robustness.sh:35-47`). `init_test_repo` creates a temporary git repository with one committed file and local user config (`tests/robustness/test-git-operations-robustness.sh:49-62`). Tests 1-18 exercise `humanize_parse_git_status` across clean, untracked, modified, staged added, insertion/deletion counts, non-git, detached HEAD, simultaneous states, deleted files, empty repo, feature branch, rename-as-modified, many untracked files, filenames with spaces, binary file, and staged+unstaged same-file states (`tests/robustness/test-git-operations-robustness.sh:71-367`). Tests 19-25 exercise `humanize_detect_git_state` for normal, detached, rebase, merge, shallow, non-git, and permission scenarios (`tests/robustness/test-git-operations-robustness.sh:377-500`).
- inputs_outputs_state: Inputs are no external arguments; the suite creates isolated temp directories via `setup_test_dir` and sets an EXIT cleanup trap (`tests/test-helpers.sh:84-89`). It mutates only temp git repositories under `$TEST_DIR`: commits, branches, status changes, synthetic `.git/rebase-merge`, `.git/MERGE_HEAD`, `.git/shallow`, and permission bits. Output is pass/fail text and a final summary; exit code is from `print_test_summary` (`tests/robustness/test-git-operations-robustness.sh:506-507`, `tests/test-helpers.sh:58-77`).
- gates_or_invariants: For `humanize_parse_git_status`, expected output format is `modified|added|deleted|untracked|insertions|deletions`; non-git adds a trailing diagnostic (`scripts/humanize.sh:158-164`, `scripts/humanize.sh:203`). The parser uses `git status --porcelain`, counts `??`, `A`, `D`, `M`, `MM`, and `R` states, and uses `git diff --shortstat HEAD` with fallback to `git diff --shortstat` for line counts (`scripts/humanize.sh:167-203`). For `humanize_detect_git_state`, precedence is not-a-repo/permission, unreadable git dir, rebase, merge, shallow, detached, then normal (`scripts/humanize.sh:110-156`). The tests enforce graceful handling rather than exact strictness in places: empty repos only need non-empty output (`tests/robustness/test-git-operations-robustness.sh:233-245`), permission errors may return `permission_error` or `not_a_repo` (`tests/robustness/test-git-operations-robustness.sh:478-496`), and insertion/deletion counts are lower-bound checks (`tests/robustness/test-git-operations-robustness.sh:145-158`, `tests/robustness/test-git-operations-robustness.sh:349-365`).
- dependencies_and_callers: Depends on local `git`, `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`. The production caller for these helpers is the monitor status bar: `_draw_status_bar` calls `_parse_git_status`, splits fields, and displays clean or dirty counts (`scripts/humanize.sh:375-379`, `scripts/humanize.sh:419-506`). These helpers are display/monitor algorithms, separate from the stop-hook’s fail-closed git gate.
- edge_cases_or_failure_modes: The suite covers many porcelain states but not git command timeout/failure inside `humanize_parse_git_status`; that function is monitor-facing and will effectively display zeros if `git status --porcelain` produces no output after repo detection. Branch cleanup assumes `master` or `main` (`tests/robustness/test-git-operations-robustness.sh:194`, `tests/robustness/test-git-operations-robustness.sh:262`, `tests/robustness/test-git-operations-robustness.sh:428`). Some cleanup uses `git checkout -q file.txt`, which may leave index state in edge cases, but subsequent state-detection tests do not require a clean worktree. Permission behavior is intentionally platform-tolerant.
- validation_or_tests: This file is itself the validation suite and is included in the aggregate runner at `tests/run-all-tests.sh:52-58`. It validates 25 named cases and exits non-zero through shared summary accounting if any `fail` call occurred.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4 evidence sections above; each assigned section header appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`