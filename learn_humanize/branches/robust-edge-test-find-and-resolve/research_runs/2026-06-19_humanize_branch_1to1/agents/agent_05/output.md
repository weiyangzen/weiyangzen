# agent_05 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: `prompt-template/` is the message and prompt contract layer for the RLCR loop. It is loaded by [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/template-loader.sh:24), which resolves the directory as `<plugin root>/prompt-template`, loads markdown templates, performs single-pass `{{VAR}}` substitution, and falls back safely when templates are absent.
- algorithmic_behavior: The directory has three coordinated subtrees. `block/` defines guard/block reasons for hook denials such as protected state files, wrong round files, incomplete todos, large files, dirty git state, unpushed commits, and plan protection. `claude/` defines next-round, finalize, post-alignment, push-note, and goal-tracker request instructions. `codex/` defines regular review, full alignment review, and goal-tracker update responsibility. Runtime call sites include common block-message helpers in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:347), Bash validator blocks in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-bash-validator.sh:82), and stop-hook review prompt generation in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-codex-stop-hook.sh:680).
- inputs_outputs_state: Inputs are template names and variable assignments such as `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `INCOMPLETE_LIST`, `REVIEW_RESULT_FILE`, and path placeholders. Outputs are rendered markdown prompts or block reasons, often embedded into hook JSON as `reason` and `systemMessage`. The template directory itself is stateless; state transitions happen in consumers, for example incomplete todos block exit before Codex review, issue reviews advance to the next prompt, and complete review enters finalize-phase instructions.
- gates_or_invariants: `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/template-loader.sh:205). Placeholder rendering is intentionally single-pass to avoid placeholder injection from user/review content. Templates preserve loop invariants: state files are system-managed, prompt files are read-only instructions, `goal-tracker.md` is Codex-managed after Round 0, native TodoWrite is preferred over `round-*-todos.md`, deferrals remain incomplete, and completion requires all original plan tasks and ACs to be met.
- dependencies_and_callers: Primary dependency is `hooks/lib/template-loader.sh`; `hooks/lib/loop-common.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` consume templates. Validation coverage comes from [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-template-references.sh:51), [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-templates-comprehensive.sh:61), and [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-template-loader.sh:41).
- edge_cases_or_failure_modes: Missing templates degrade through `load_and_render_safe` fallbacks instead of emitting empty block reasons. Syntax risks are malformed placeholders, empty templates, missing subdirectories, and review/summary content containing `{{...}}`; the single-pass renderer and template tests target those cases. Operationally important templates include `block/codex-review-failed.md`, `block/git-status-failed.md`, and `block/schema-outdated.md` for fail-closed paths.
- validation_or_tests: The directory is validated by scanning template references and checking existence in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-template-references.sh:83), loading every markdown template in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-templates-comprehensive.sh:83), checking placeholder syntax in the same test at line 113, fallback behavior at line 441, and real-template rendering at line 493. I inspected recursively; no artificial depth limit was used.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-035 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: This is an executable specification for the `command_modifies_file` detection algorithm used by the RLCR Bash validator to prevent shell-based writes to protected loop files. It sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:1) at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-bash-validator-patterns.sh:13).
- algorithmic_behavior: The test lowercases each candidate command with `to_lower` and asserts whether `command_modifies_file` matches a target regex pattern. The production matcher checks redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` patterns in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:775).
- inputs_outputs_state: Inputs are shell command strings plus an optional file regex, defaulting to `goal-tracker\.md` at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-bash-validator-patterns.sh:39). Outputs are pass/fail counters and process exit status. No persistent repo state is changed by the matcher itself; the test harness only increments `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: The invariant is conservative write detection without blocking normal reads. Positive gates cover output redirection, appending, `tee -a`, in-place editors, destructive file operations, truncation, `dd`, and file descriptor redirection. Negative gates ensure `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo filename`, `ls`, `file`, `stat`, and `diff` do not trigger a write block.
- dependencies_and_callers: The direct dependency is `hooks/lib/loop-common.sh`. Runtime callers are the Bash validator checks for `finalize-state.md`, `state.md`, `.humanize/rlcr/.../plan.md`, `goal-tracker.md`, prompt, summary, and todos files in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-bash-validator.sh:117). The Bash validator adds extra state-file source detection for `mv`/`cp` beyond this helper, so this file tests the shared destination/write pattern subset, not every Bash-validator bypass case.
- edge_cases_or_failure_modes: The test documents a known limitation: multi-source `cp file1 file2 goal-tracker.md` is not detected because the regex expects `cp src dest` format at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-bash-validator-patterns.sh:165). It also verifies case normalization, no-space redirection (`>goal-tracker.md`), target paths with prefixes, variable indirection not matching literal filenames, and alternate file patterns for `state.md` and `round-[0-9]+-summary.md`.
- validation_or_tests: The script is itself the validation surface. It groups cases by command family at lines 69, 82, 95, 109, 123, 135, 154, 174, and 185, then exits successfully only when `TESTS_FAILED` is zero at [tests/test-bash-validator-patterns.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-bash-validator-patterns.sh:206).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-065 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: This template defines the stop-gate message when an agent tries to exit while native todos remain incomplete. It is a small but core review-contract block because it prevents premature Codex review and forces todo closure first.
- algorithmic_behavior: The template emits a titled block, interpolates `{{INCOMPLETE_LIST}}`, and instructs the agent to complete all remaining todos, mark each with TodoWrite, then write the summary and stop. The direct content is at [prompt-template/block/incomplete-todos.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/prompt-template/block/incomplete-todos.md:1).
- inputs_outputs_state: Input is the rendered `INCOMPLETE_LIST` placeholder at line 5. Output is a markdown reason string. State transition is external: [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-codex-stop-hook.sh:287) detects incomplete todos, renders this template at line 297, returns a JSON block decision, and exits before Codex review.
- gates_or_invariants: The invariant is that stop/review cannot proceed until all todos are complete. The template explicitly says not to proceed to Codex review until todos are finished at [prompt-template/block/incomplete-todos.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/prompt-template/block/incomplete-todos.md:12).
- dependencies_and_callers: Depends on `load_and_render_safe` from the template loader. The caller is the Codex stop hook’s todo gate. Related behavior depends on the environment/tooling that supplies `TODO_RESULT`; the hook extracts incomplete entries with `tail -n +2` before rendering.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook has a fallback message, so the block remains fail-closed. If `INCOMPLETE_LIST` is empty despite the todo checker returning incomplete status, the rendered reason would still block but provide less actionable detail. Because rendering is single-pass, todo text containing placeholder-like braces is not recursively substituted.
- validation_or_tests: Covered indirectly by template load/reference tests and runtime stop-hook behavior. It is included in the recursive template load check in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-templates-comprehensive.sh:83) and reference scanning in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-template-references.sh:51).
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-095 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: This is an executable robustness specification for production plan-file validation in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/scripts/setup-rlcr-loop.sh:1), specifically AC-9 edge handling for empty files, large files, mixed line endings, disappearing files, comments, and content-line counting.
- algorithmic_behavior: The helper `test_plan_validation` runs `setup-rlcr-loop.sh` against a candidate plan under a temporary git repo, captures output to a temp file to avoid command-substitution limits, treats known validation errors as rejection, and treats “requires codex”, “must be gitignored”, successful activation text, or loop directory creation as validation success. See [tests/robustness/test-plan-file-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/robustness/test-plan-file-robustness.sh:24).
- inputs_outputs_state: Inputs are generated markdown plan files in `$TEST_DIR`, plus `CLAUDE_PROJECT_DIR="$TEST_DIR"` for production script execution. Outputs are pass/fail counters via [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/test-helpers.sh:30) and final process status from `print_test_summary`. State transitions happen in a disposable temp repo: git init/commit at lines 88-96, `.humanize/rlcr` cleanup before each validation at line 33, and possible loop state creation by production setup.
- gates_or_invariants: Production gates under test include relative path and safety validation, file existence/readability, git tracking policy, at least five physical lines, and at least three nonblank/noncomment content lines. The content-line algorithm treats blank lines, HTML comments, multi-line HTML comment bodies, and `#` lines as non-content in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/scripts/setup-rlcr-loop.sh:395); the test mirrors that with `count_content_lines` at [tests/robustness/test-plan-file-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/robustness/test-plan-file-robustness.sh:103).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `tests/test-helpers.sh`, `git`, `mktemp`, `wc`, `grep`, `chmod`, and shell utilities. The production script depends on `scripts/portable-timeout.sh` and uses git status/ls-files gates before content validation.
- edge_cases_or_failure_modes: Positive cases include normal plans, exactly three content lines, 5KB plans, rich markdown, 1MB-plus plans, and CRLF/LF mixtures. Negative or edge cases include empty files, comments-only files, binary bytes, very long lines, special shell characters in content, whitespace-only files, nested/complex comments, nonexistent files, unreadable files, symlinks, directories instead of files, null bytes, and file disappearance before validation. Not all edge fixtures call production validation; several validate local counting/readability behavior only, so the script mixes production integration tests with helper-level robustness checks.
- validation_or_tests: The script defines 19 named tests from line 152 through line 501 and exits through `print_test_summary` at [tests/robustness/test-plan-file-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/tests/robustness/test-plan-file-robustness.sh:531). I did not execute it because this assignment is read-only research and the test intentionally creates temp files, git repos, chmod changes, and `.humanize/rlcr` directories.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-005, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-035, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-065, ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-095
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`