# agent_06 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is the CLI/control-script layer for the RLCR/humanize loop. It creates loop sessions, validates plan-generation IO, provides a portable timeout abstraction, cancels active sessions through an explicit signal-and-rename protocol, and exposes monitoring helpers for active loop state/logs. Recursive inspection found five files: `scripts/cancel-rlcr-loop.sh`, `scripts/humanize.sh`, `scripts/portable-timeout.sh`, `scripts/setup-rlcr-loop.sh`, and `scripts/validate-gen-plan-io.sh`.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` initializes a loop from a plan file: parses options (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--plan-file`, `--track-plan-file`) at `scripts/setup-rlcr-loop.sh:104`, rejects unsafe plan paths and shell metacharacters at `scripts/setup-rlcr-loop.sh:227`, validates git repository state at `scripts/setup-rlcr-loop.sh:211`, validates tracking/cleanliness mode at `scripts/setup-rlcr-loop.sh:334`, checks plan size/content at `scripts/setup-rlcr-loop.sh:385`, verifies `codex` availability at `scripts/setup-rlcr-loop.sh:440`, validates branch/model/effort YAML safety at `scripts/setup-rlcr-loop.sh:452`, then creates `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` at `scripts/setup-rlcr-loop.sh:490` and `scripts/setup-rlcr-loop.sh:508`. `scripts/cancel-rlcr-loop.sh` locates the newest loop directory, requires `state.md` or `finalize-state.md`, creates `.cancel-requested`, and renames the active state file to `cancel-state.md` at `scripts/cancel-rlcr-loop.sh:69` and `scripts/cancel-rlcr-loop.sh:134`. `scripts/humanize.sh` provides monitoring and parsing helpers: goal-tracker table parsing at `scripts/humanize.sh:24`, git state/status summaries at `scripts/humanize.sh:110` and `scripts/humanize.sh:160`, latest session/log discovery at `scripts/humanize.sh:231` and `scripts/humanize.sh:255`, status-bar rendering at `scripts/humanize.sh:381`, and `humanize monitor rlcr-loop` dispatch at `scripts/humanize.sh:870`. `scripts/portable-timeout.sh` selects `gtimeout`, GNU `timeout`, Python, or no timeout at `scripts/portable-timeout.sh:10` and wraps command execution with GNU-compatible timeout exit `124` at `scripts/portable-timeout.sh:33`. `scripts/validate-gen-plan-io.sh` is a preflight validator for draft-to-plan generation paths, parsing `--input`/`--output` at `scripts/validate-gen-plan-io.sh:29` and enforcing existence, non-empty input, output directory existence, non-existing target, and write permission at `scripts/validate-gen-plan-io.sh:78`.
- inputs_outputs_state: Inputs include CLI arguments, the current git repository, a relative plan file, optional `CLAUDE_PROJECT_DIR`, `codex` availability, git status output, state/frontmatter files, cache/log directories, and optional `XDG_CACHE_HOME`. Primary outputs are loop artifacts under `.humanize/rlcr/<timestamp>/`, including `state.md`, `plan.md`, `goal-tracker.md`, `round-0-prompt.md`, and cancellation state files. `setup-rlcr-loop.sh` writes state frontmatter fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, and `start_branch` at `scripts/setup-rlcr-loop.sh:508`. `cancel-rlcr-loop.sh` transitions `state.md` or `finalize-state.md` to `cancel-state.md` and emits machine-readable statuses such as `NO_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, and `CANCELLED_FINALIZE` at `scripts/cancel-rlcr-loop.sh:75`, `scripts/cancel-rlcr-loop.sh:117`, and `scripts/cancel-rlcr-loop.sh:144`. `humanize.sh` outputs pipe-delimited parser summaries and terminal-monitor display state rather than changing loop state directly.
- gates_or_invariants: Plan paths must be relative, contain no whitespace or shell metacharacters, not be symlinks, stay within the project, and not live under submodules (`scripts/setup-rlcr-loop.sh:227`, `scripts/setup-rlcr-loop.sh:253`, `scripts/setup-rlcr-loop.sh:301`, `scripts/setup-rlcr-loop.sh:315`). Default mode requires the plan file to be gitignored/untracked unless `--track-plan-file` is supplied; tracked mode requires the file to be tracked and clean (`scripts/setup-rlcr-loop.sh:354`). The plan must have at least 5 lines and at least 3 meaningful content lines after blank/comment filtering (`scripts/setup-rlcr-loop.sh:385`, `scripts/setup-rlcr-loop.sh:400`). Branch/model/effort values are restricted to YAML-safe character sets (`scripts/setup-rlcr-loop.sh:458`, `scripts/setup-rlcr-loop.sh:468`). Cancellation in finalize phase is gated by `--force` (`scripts/cancel-rlcr-loop.sh:117`). Timeout use is fail-soft: when no timeout tool is available, the wrapper warns and runs without timeout (`scripts/portable-timeout.sh:64`). `validate-gen-plan-io.sh` uses explicit exit codes 1-6 for distinct IO failure classes (`scripts/validate-gen-plan-io.sh:4`).
- dependencies_and_callers: `setup-rlcr-loop.sh` sources `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:27` and calls `git`, `codex`, `wc`, `grep`, `sed`, and `date`. `humanize.sh` is a shell utility/dispatcher, likely sourced by shell integration; it coordinates with `.humanize/rlcr` and cache logs written by hook code. `cancel-rlcr-loop.sh` coordinates with hook authorization in `hooks/lib/loop-common.sh:is_cancel_authorized` and `hooks/loop-bash-validator.sh`, because cancellation requires both the script-created `.cancel-requested` signal and a permitted state-file rename. `validate-gen-plan-io.sh` is a standalone guard for a `gen-plan` command path. Sibling/parent coordination is with `hooks/lib/loop-common.sh`, especially active loop discovery (`find_active_loop` at `hooks/lib/loop-common.sh:152`), state parsing (`parse_state_file_strict` at `hooks/lib/loop-common.sh:240`), and end-state renaming (`end_loop` at `hooks/lib/loop-common.sh:823`).
- edge_cases_or_failure_modes: Slow or unavailable git commands are guarded by `run_with_timeout`, and timeout exit `124` is treated as failure in setup checks (`scripts/setup-rlcr-loop.sh:334`, `scripts/setup-rlcr-loop.sh:346`). The plan path checks reject absolute paths, path spaces, shell metacharacters, symlinks in the file or parent directories, unresolved directories, submodule paths, too-short plans, comment-only plans, unsafe branch names, and unsafe model/effort strings. `cancel-rlcr-loop.sh` can report a loop directory exists but has no active state file (`scripts/cancel-rlcr-loop.sh:95`), and finalize cancellation requires explicit force. `humanize.sh` handles missing loop dirs, missing sessions, missing logs, session/log deletion, newer sessions, truncated/rotated logs, and terminal cleanup traps (`scripts/humanize.sh:224`, `scripts/humanize.sh:612`, `scripts/humanize.sh:769`, `scripts/humanize.sh:789`). A potential design limitation is that `cancel-rlcr-loop.sh` itself finds the newest directory with `ls|sort|head` regardless of whether that newest directory is active (`scripts/cancel-rlcr-loop.sh:73`), matching the broader newest-only invariant but meaning older active-looking sessions are intentionally ignored.
- validation_or_tests: The directory behavior is covered by focused tests outside `scripts`: `tests/robustness/test-session-robustness.sh` validates active-loop discovery edge cases, while `tests/test-cancel-signal-file.sh` validates the signal-file cancellation authorization and command-blocking surface. Other visible tests in the repository also target setup, monitor runtime, plan validation, state naming, template loading, bash validator patterns, and timeout robustness. The assigned tests below provide executable specs for the most security-sensitive transitions.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-036 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: This is an executable security/regression specification for the cancel signal-file mechanism protecting RLCR loop state files. It verifies that `state.md` or `finalize-state.md` can only be renamed to `cancel-state.md` when a `.cancel-requested` signal exists in the active loop directory, and that other shell mutations of loop state remain blocked.
- algorithmic_behavior: The test creates a temporary `.humanize/rlcr/2024-01-01_12-00-00` loop with `state.md` frontmatter in `setup_test_loop` at `tests/test-cancel-signal-file.sh:42`, exports `CLAUDE_PROJECT_DIR`, and runs Bash hook validation by JSON-encoding commands into `hooks/loop-bash-validator.sh` at `tests/test-cancel-signal-file.sh:61`. Positive cases allow exactly `mv <loop>/state.md <loop>/cancel-state.md` with signal file (`tests/test-cancel-signal-file.sh:78`), a normalized `./state.md` source (`tests/test-cancel-signal-file.sh:98`), quoted paths (`tests/test-cancel-signal-file.sh:363`), and literal `${loop_dir}` syntax after helper normalization (`tests/test-cancel-signal-file.sh:448`, `tests/test-cancel-signal-file.sh:468`). Negative cases exercise missing signal, wrong signal directory, non-rename mutations, wrong destination, arbitrary destination, extra args, wrapper commands, command chaining, hidden variables, command substitutions, background/pipe operators, redirection-prefix variants, unrelated `state.md` paths, and no active-loop behavior.
- inputs_outputs_state: Inputs are synthetic hook JSON with `tool_name: Bash` and `tool_input.command`, temporary loop filesystem state, `.cancel-requested` presence or absence, and direct helper calls to `is_cancel_authorized`. Expected outputs are hook exit codes: mostly `0` for allowed commands and no active loop, `2` for blocked policy violations, and false/nonzero from direct helper checks. The test does not execute the dangerous commands; it validates whether the hook would allow or block them. It increments local pass/fail counters and exits with `TESTS_FAILED` at `tests/test-cancel-signal-file.sh:1347`.
- gates_or_invariants: The central invariant is that the signal file alone is not sufficient: the Bash command must match a narrow two-argument `mv` from the active loop’s `state.md` or `finalize-state.md` to that same loop’s `cancel-state.md`. Tests assert the signal must be in the active session directory (`tests/test-cancel-signal-file.sh:199`), wrong destinations are blocked (`tests/test-cancel-signal-file.sh:179`, `tests/test-cancel-signal-file.sh:281`), source movement to arbitrary paths is blocked (`tests/test-cancel-signal-file.sh:381`), extra args are blocked (`tests/test-cancel-signal-file.sh:424`, `tests/test-cancel-signal-file.sh:511`), and hidden `$` variables after loop-dir normalization are blocked (`tests/test-cancel-signal-file.sh:531`). It also asserts the broader hook invariant that when no active loop exists, commands are allowed (`tests/test-cancel-signal-file.sh:1231`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` at `tests/test-cancel-signal-file.sh:32` and invokes `hooks/loop-bash-validator.sh` at `tests/test-cancel-signal-file.sh:70`. The validator depends on `validate_hook_input`, `find_active_loop`, `parse_state_file_strict`, `command_modifies_file`, and `is_cancel_authorized` from `hooks/lib/loop-common.sh`. The authorization logic being specified is implemented at `hooks/lib/loop-common.sh:455`, while the Bash validator’s state-file blocking path is at `hooks/loop-bash-validator.sh:117` and source-pattern handling begins at `hooks/loop-bash-validator.sh:147`.
- edge_cases_or_failure_modes: The test suite is heavily adversarial. It covers command substitution `$()` and backticks (`tests/test-cancel-signal-file.sh:301`, `tests/test-cancel-signal-file.sh:321`), newline injection through direct helper validation (`tests/test-cancel-signal-file.sh:343`), chained commands using `;`, `&&`, `||`, pipe, `|&`, and `&` (`tests/test-cancel-signal-file.sh:241`, `tests/test-cancel-signal-file.sh:650`, `tests/test-cancel-signal-file.sh:670`, `tests/test-cancel-signal-file.sh:690`, `tests/test-cancel-signal-file.sh:710`, `tests/test-cancel-signal-file.sh:730`), wrapper commands (`tests/test-cancel-signal-file.sh:810`, `tests/test-cancel-signal-file.sh:830`), prefix commands like `sudo`, `env`, and `command --` (`tests/test-cancel-signal-file.sh:550`, `tests/test-cancel-signal-file.sh:590`, `tests/test-cancel-signal-file.sh:630`), and a large family of redirection-prefix bypasses including quoted, escaped-space, and ANSI-C quoted targets (`tests/test-cancel-signal-file.sh:850`, `tests/test-cancel-signal-file.sh:930`, `tests/test-cancel-signal-file.sh:970`, `tests/test-cancel-signal-file.sh:1030`, `tests/test-cancel-signal-file.sh:1110`, `tests/test-cancel-signal-file.sh:1150`). These cases map directly to the validator’s command segmentation and redirection stripping logic at `hooks/loop-bash-validator.sh:147`.
- validation_or_tests: The file is itself a validation harness. It asserts positive/negative cases through `pass` and `fail` helpers at `tests/test-cancel-signal-file.sh:25`, captures hook output and status with `set +e`, and uses direct helper calls for cases hard to represent through JSON such as embedded newline and literal variable normalization. It ends by printing counts and returning a nonzero exit if any expectation failed (`tests/test-cancel-signal-file.sh:1347`). I did not run the test because this worker assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-066 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: This prompt block is a policy/template artifact used when a stop/exit attempt detects files exceeding a configured line limit. It is not executable code, but it defines a gate contract for large-file handling: the loop should not consider work stoppable until oversized files are split and committed.
- algorithmic_behavior: The template accepts placeholders `{{MAX_LINES}}` and `{{LARGE_FILES}}` at `prompt-template/block/large-files.md:3` and `prompt-template/block/large-files.md:4`, explains why large files matter, then branches required actions by file type. For code files, it requires modular splitting below the line limit, strictly unchanged functionality, optional `code-simplifier` review, and clear module boundaries (`prompt-template/block/large-files.md:13`). For documentation files, it requires logical section/chapter splitting, cross-references, narrative continuity, and TOC/navigation updates (`prompt-template/block/large-files.md:19`). The final transition requires committing changes and attempting exit again (`prompt-template/block/large-files.md:25`).
- inputs_outputs_state: Inputs are the renderer’s max-line threshold and list of oversized files. Output is a markdown block presented to the agent/user as a blocking instruction. The implied state transition is from “exit blocked due to large files” back to “continue work/refactor/split”; after remediation and commit, the caller may reattempt stop/exit and rerun the large-file detector.
- gates_or_invariants: The invariant is that each split artifact must be below `{{MAX_LINES}}` lines, with code behavior “strictly unchanged” and documentation cohesion preserved. The template also encodes a commit-before-exit invariant: after splitting, the changes should be committed before trying to exit again (`prompt-template/block/large-files.md:25`). It does not itself specify the detector, file classifier, or commit validation implementation.
- dependencies_and_callers: The template is loaded through the shared template-loader path used by hook/block messages. `hooks/lib/loop-common.sh` initializes `TEMPLATE_DIR` and uses `load_and_render_safe` for related block templates at `hooks/lib/loop-common.sh:136` and throughout message helpers. I did not find a direct reference to `large-files.md` in the assigned files, so its likely caller is a stop/exit hook or large-file checker outside the assigned subset.
- edge_cases_or_failure_modes: Because it is a prompt block, its edge cases are policy ambiguity rather than runtime failures. It distinguishes code and documentation but does not state how mixed files, generated files, vendored files, minified assets, lockfiles, or intentionally monolithic files are exempted. It also recommends an optional plugin if installed but provides no fallback validation beyond preserving behavior and reattempting exit.
- validation_or_tests: Template loading behavior is covered generally by repository tests such as template loader/reference/comprehensive tests listed in the tree, but this specific file is not executable. The meaningful validation is whether a renderer substitutes `MAX_LINES` and `LARGE_FILES`, and whether the caller blocks exit until the split/commit condition is satisfied.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-096 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: This is an executable robustness specification for active RLCR session detection. It validates `find_active_loop` under normal and pathological `.humanize/rlcr` directory states, with emphasis on the “newest session only” invariant.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-session-robustness.sh:14`, creates temporary `rlcr` session directories, and repeatedly calls `find_active_loop "$TEST_DIR/rlcr"`. Positive tests require newest-directory detection with `state.md` (`tests/robustness/test-session-robustness.sh:33`), detection of `finalize-state.md` (`tests/robustness/test-session-robustness.sh:48`), efficient handling of 15 sessions under 1000 ms (`tests/robustness/test-session-robustness.sh:63`), handling both `state.md` and `finalize-state.md` in the same newest session (`tests/robustness/test-session-robustness.sh:105`), non-standard names (`tests/robustness/test-session-robustness.sh:179`), symlinked session entries (`tests/robustness/test-session-robustness.sh:194`), paths with spaces (`tests/robustness/test-session-robustness.sh:210`), and rapid creation (`tests/robustness/test-session-robustness.sh:240`). Negative tests require empty output for no base directory, empty base directory, no subdirectories, sessions without live state files, deeply nested state files, completed-only sessions, cancelled-only sessions, and cases where the newest session is finished even if an older session has `state.md`.
- inputs_outputs_state: Inputs are filesystem layouts under a temporary `rlcr` base: immediate child directories, files named `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`, symlinks, spaces, and nested directories. Output is either the active loop directory path or an empty string. No persistent repository state is modified; all test state lives under `TEST_DIR`. The target implementation returns only the newest immediate directory if it contains `state.md` or `finalize-state.md`, implemented at `hooks/lib/loop-common.sh:152`.
- gates_or_invariants: The key invariant is “newest directory only”: older sessions are ignored even if they contain `state.md`, preventing zombie loop revival. The test asserts this explicitly when the newest directory lacks `state.md` (`tests/robustness/test-session-robustness.sh:89`) and when the newest session is finished but an older one is active-looking (`tests/robustness/test-session-robustness.sh:287`). Another invariant is that only `state.md` and `finalize-state.md` are active indicators; `complete-state.md` and `cancel-state.md` are terminal and ignored (`tests/robustness/test-session-robustness.sh:259`, `tests/robustness/test-session-robustness.sh:273`). The function only checks one immediate directory level, not deep nested paths (`tests/robustness/test-session-robustness.sh:224`).
- dependencies_and_callers: The tested function is `find_active_loop` from `hooks/lib/loop-common.sh`, which checks directory existence, uses `ls -1d "$loop_base_dir"/*/ | sort -r | head -1`, then requires `state.md` or `finalize-state.md` in that newest directory (`hooks/lib/loop-common.sh:155`, `hooks/lib/loop-common.sh:160`, `hooks/lib/loop-common.sh:164`). Callers include `hooks/loop-bash-validator.sh` for deciding whether Bash commands need RLCR state protection (`hooks/loop-bash-validator.sh:52`) and likely read/write/stop validators elsewhere in `hooks`.
- edge_cases_or_failure_modes: The test captures empty or missing directories (`tests/robustness/test-session-robustness.sh:128`, `tests/robustness/test-session-robustness.sh:140`), base directories containing files only (`tests/robustness/test-session-robustness.sh:150`), sessions with unexpected names (`tests/robustness/test-session-robustness.sh:179`), symlinked session directories (`tests/robustness/test-session-robustness.sh:194`), spaces in names (`tests/robustness/test-session-robustness.sh:210`), deep nested state files that must not count (`tests/robustness/test-session-robustness.sh:224`), and Unicode/special-character tolerance by expecting a graceful nonempty result from a normal session (`tests/robustness/test-session-robustness.sh:304`). The symlink case is intentionally weak: it only requires some nonempty result, not a specific symlink-vs-real target.
- validation_or_tests: The file is itself the validation harness and ends with `print_test_summary` from test helpers at `tests/robustness/test-session-robustness.sh:322`. It validates both correctness and a simple performance bound for 15 sessions. I did not run it because the assignment required research notes only, but the expected behavior is directly inspectable from the assertions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence headings, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`