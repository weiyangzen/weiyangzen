# agent_16 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-016 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/gen-plan` command workflow that converts a user draft into a structured implementation plan. It is a command-level algorithm specification rather than executable shell code: it orders validation, repository relevance filtering, draft analysis, user clarification, plan generation, and final write/report behavior.
- algorithmic_behavior: The workflow is explicitly phased. Phase 1 runs `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` with the user arguments before any repository analysis or output write (`commands/gen-plan.md:29-35`). Phase 2 reads the draft and delegates semantic repository relevance to `humanize:draft-relevance-checker` using the haiku model, requiring the verdict format `RELEVANT: <reason>` or `NOT_RELEVANT: <reason>` (`commands/gen-plan.md:48-68`). Phase 3 directs deeper analysis over clarity, consistency, completeness, and functionality, including Explore subagents for affected components, related files, patterns, and integrations (`commands/gen-plan.md:72-104`). Phase 4 uses `AskUserQuestion` until significant issues are resolved or acknowledged (`commands/gen-plan.md:107-116`). Phase 5 emits a constrained markdown plan shape with goal description, AC-format acceptance criteria, path boundaries, feasibility hints, and dependency sequence (`commands/gen-plan.md:120-194`). Phase 6 writes the generated plan and reports path, summary, and AC count (`commands/gen-plan.md:218-224`).
- inputs_outputs_state: Inputs are command arguments matching `--input <path/to/draft.md> --output <path/to/plan.md>` from frontmatter (`commands/gen-plan.md:3`) plus the draft content read from disk. Output is a new plan file at the requested path, never an existing file, and a completion report (`commands/gen-plan.md:220-223`). The command itself carries no persistent loop state; its state transitions are command phases: invalid IO -> stop, not relevant -> stop, unresolved issues -> ask user, resolved/clear draft -> write plan. The IO validator dependency normalizes paths with `realpath -m`, extracts output directory, and emits exit-code-classified validation results (`scripts/validate-gen-plan-io.sh:68-130`).
- gates_or_invariants: The frontmatter limits tools to the IO validation Bash script, Read, Glob, Grep, Task, Write, and AskUserQuestion (`commands/gen-plan.md:4-11`). IO exit codes are part of the command contract: 0 continues, 1-6 stop with specific user-facing messages (`commands/gen-plan.md:37-45`). The relevance gate stops unrelated drafts (`commands/gen-plan.md:63-66`). Generation invariants prohibit time units/estimates, line-number references, and prescriptive feasibility hints, require AC-X or AC-X.Y criteria, require positive and negative tests for every AC, and require affirmative path boundaries (`commands/gen-plan.md:196-215`). Deterministic drafts must narrow path boundaries to the fixed design (`commands/gen-plan.md:170`, `commands/gen-plan.md:214`).
- dependencies_and_callers: Direct dependency: `scripts/validate-gen-plan-io.sh`, which implements the exit-code protocol and checks missing input, empty input, missing output directory, existing output path, directory output path, and output-dir writability (`scripts/validate-gen-plan-io.sh:78-123`). Agent dependency: `agents/draft-relevance-checker.md`, whose frontmatter defines `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep` (`agents/draft-relevance-checker.md:1-6`), matching the command’s relevance-check instruction. Test coverage in `tests/test-gen-plan.sh` asserts command presence/frontmatter, the relevance agent name/model/tools, and validator exit behavior (`tests/test-gen-plan.sh:54-156`, `tests/test-gen-plan.sh:426-541`).
- edge_cases_or_failure_modes: Missing `--input`, missing `--output`, an option used as a value, unknown options, or `--help` are invalid arguments with exit 6 from the validator (`scripts/validate-gen-plan-io.sh:28-66`). Missing input exits 1, empty input exits 2, missing output directory exits 3, existing output file or output path that is a directory exits 4, and non-writable output directory exits 5 (`scripts/validate-gen-plan-io.sh:78-123`). A draft judged unrelated terminates before expensive analysis or write (`commands/gen-plan.md:63-66`). A draft with ambiguity or missing information must route through user clarification; if the plan still cannot be completed, the command explains missing information and suggests draft improvements (`commands/gen-plan.md:227-235`).
- validation_or_tests: `tests/test-gen-plan.sh` validates the command file exists, has description, allowed tools, argument hint, and checks the relevance agent file, name, model, and tools (`tests/test-gen-plan.sh:54-156`). The same test file exercises `validate-gen-plan-io.sh` for no value after `--input`, no value after `--output`, flag-as-value, unknown flag, missing input, empty input, missing output directory, existing output, output directory, valid paths, and `--help` (`tests/test-gen-plan.sh:438-538`). I inspected rather than ran tests because the task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-046 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop state lifecycle naming and active-loop discovery. It verifies that only live state marker files make a loop active, terminal state files do not revive old loops, exit reasons are encoded by renaming `state.md`, and the new `.humanize/rlcr` state directory supersedes the legacy `.humanize-loop.local` path.
- algorithmic_behavior: The test creates an isolated temporary git repository, constructs RLCR loop directories under `.humanize/rlcr/<timestamp>`, sources `hooks/lib/loop-common.sh`, and probes three core helpers: `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` (`tests/test-state-exit-naming.sh:31-67`). Tests 1-6 establish that terminal files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active; only `state.md` is active (`tests/test-state-exit-naming.sh:38-155`). Test 7 asserts the newest timestamp-named directory with `state.md` wins (`tests/test-state-exit-naming.sh:157-176`). Tests 8-10 specify `end_loop` validation and rename behavior (`tests/test-state-exit-naming.sh:178-239`). Tests 11-13 specify new-vs-legacy path detection and search-base separation (`tests/test-state-exit-naming.sh:241-287`).
- inputs_outputs_state: Inputs are synthetic filesystem layouts and state frontmatter files. `find_active_loop "$TEST_DIR/.humanize/rlcr"` outputs either an active loop directory path or an empty string (`tests/test-state-exit-naming.sh:67`, `tests/test-state-exit-naming.sh:86`, `tests/test-state-exit-naming.sh:171`). `end_loop "$loop_dir" "$state_file" "$reason"` consumes a live `state.md` plus a reason and outputs side effects by moving `state.md` to `${reason}-state.md`; valid reasons are `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`tests/test-state-exit-naming.sh:205-223`). The sourced implementation confirms `find_active_loop` checks the newest child directory and treats `state.md` or `finalize-state.md` as active markers (`hooks/lib/loop-common.sh:147-170`), while `end_loop` validates reason and renames the state file (`hooks/lib/loop-common.sh:816-848`).
- gates_or_invariants: Active-loop invariant: completed or terminated state names must not be treated as active, preventing terminal loops from being resumed (`tests/test-state-exit-naming.sh:51-72`, `tests/test-state-exit-naming.sh:93-155`). Newest-loop invariant: older loops are ignored when a newer active directory exists (`tests/test-state-exit-naming.sh:157-176`), consistent with the implementation comment about preventing “zombie” loops (`hooks/lib/loop-common.sh:147-149`). Exit-reason invariant: invalid reasons must fail with “Invalid end_loop reason” and no rename (`tests/test-state-exit-naming.sh:182-200`). Missing-state invariant: `end_loop` fails gracefully when the source state file is absent (`tests/test-state-exit-naming.sh:228-239`). Path invariant: `.humanize/rlcr/` is the recognized loop namespace and `.humanize-loop.local` is deliberately not matched (`tests/test-state-exit-naming.sh:245-287`).
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, sourced at `tests/test-state-exit-naming.sh:65`. The specific helpers under test are implemented at `find_active_loop` (`hooks/lib/loop-common.sh:152-170`), `is_in_humanize_loop_dir` (`hooks/lib/loop-common.sh:616-620`), and `end_loop` (`hooks/lib/loop-common.sh:823-848`). `find_active_loop` is called by read/write/edit/plan/bash/codex-stop hooks and robustness tests, so this test protects hook routing and loop termination semantics across the RLCR system (`rg` showed callers in `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`).
- edge_cases_or_failure_modes: The test intentionally leaves multiple terminal marker files in the same loop directory across Tests 3-6; `find_active_loop` must still return empty when `state.md` is absent. It verifies legacy state directories cannot accidentally reactivate a loop even when they contain `state.md` (`tests/test-state-exit-naming.sh:263-287`). It covers missing `state.md` on loop termination (`tests/test-state-exit-naming.sh:228-239`). A notable coverage boundary: the implementation now also treats `finalize-state.md` as active (`hooks/lib/loop-common.sh:164-165`), but this assigned test’s active-loop cases focus on `state.md` and terminal state names; finalize behavior is covered elsewhere, such as `tests/test-finalize-phase.sh` references to `find_active_loop`.
- validation_or_tests: This file is itself the validation asset. It has 13 numbered tests and exits with the accumulated failure count (`tests/test-state-exit-naming.sh:289-298`). It uses local `pass`, `fail`, and `skip` counters and `set -uo pipefail`, temporarily enabling/disabling `set -e` around expected failures (`tests/test-state-exit-naming.sh:13-29`, `tests/test-state-exit-naming.sh:192-195`, `tests/test-state-exit-naming.sh:231-234`). I inspected rather than ran it because the requested output is research notes only.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-076 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Prompt-block template for RLCR hook denials when a tool targets a valid loop filename from the wrong loop directory. It is part of the validation feedback contract for path-canonicalization gates in read/write workflows.
- algorithmic_behavior: The template renders a short denial message with three substituted variables: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:1-6`). It is loaded through `load_and_render_safe`, which uses the template if available, otherwise renders an inline fallback (`hooks/lib/template-loader.sh:167-203`). Placeholder substitution is single-pass and missing variables remain visible rather than being deleted (`hooks/lib/template-loader.sh:50-132`), which is relevant because this template is only useful when callers provide all three variables.
- inputs_outputs_state: Inputs are action text such as `read` or `write to`, the attempted file path, and the active-loop canonical path. Output is markdown written to stderr by hook validators before they exit with status 2. The read validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and, if the requested path differs, renders this template with `ACTION=read`, the attempted `FILE_PATH`, and computed `CORRECT_PATH` (`hooks/loop-read-validator.sh:150-164`). The write validator does the same with `ACTION=write to` (`hooks/loop-write-validator.sh:220-234`). The template itself has no persisted state; it reflects the hook’s active-loop state and current filename derivation.
- gates_or_invariants: The upstream gate is exact path equality after active loop discovery: same basename is not enough; the full file path must equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` (`hooks/loop-read-validator.sh:154-164`, `hooks/loop-write-validator.sh:224-235`). The gate only executes after earlier filters confirm a relevant round/prompt/summary or protected loop path and after strict state parsing succeeds (`hooks/loop-read-validator.sh:91-96`, `hooks/loop-write-validator.sh:112-117`). Template rendering invariants come from `template-loader.sh`: variable syntax is `{{VARIABLE_NAME}}`, substitution is single-pass, and undefined placeholders are preserved (`hooks/lib/template-loader.sh:7-14`).
- dependencies_and_callers: Direct callers are `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`; `rg` found no `loop-edit-validator.sh` use of this exact template. The template-loader dependency is initialized by `hooks/lib/loop-common.sh`, which sources `template-loader.sh`, derives `TEMPLATE_DIR`, and validates the prompt-template directory (`hooks/lib/loop-common.sh:131-145`). Related sibling templates handle nearby gates: wrong file location, wrong round file/number, wrong summary location, prompt write block, and state modification block.
- edge_cases_or_failure_modes: If the template is missing or empty, both validators have inline fallbacks that still mention the attempted and correct paths (`hooks/loop-read-validator.sh:157-163`, `hooks/loop-write-validator.sh:227-233`). Because missing variables remain unreplaced, a caller bug would surface as literal `{{ACTION}}`, `{{FILE_PATH}}`, or `{{CORRECT_PATH}}` in the user-facing message rather than silently producing incomplete text (`hooks/lib/template-loader.sh:119-122`). Potential contract issue: the final hint in the template says `If you need this file, use: cat {{FILE_PATH}}` even though the denial says the correct path is `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:4-6`). That may intentionally show how to inspect the originally attempted file, but for a “wrong directory” correction it is likely confusing because the actionable path should usually be the computed correct path. I found no direct test asserting this exact template’s rendered output.
- validation_or_tests: General template rendering is tested by `tests/test-template-loader.sh`, including `get_template_dir`, loading existing and missing templates, multiline substitution, special-character values, unreplaced variables, and safe fallback behavior (`tests/test-template-loader.sh:41-220`). Comprehensive template tests validate template directory shape and some real template rendering, but the searched results showed direct real-template assertions for `wrong-round-number.md` and others, not `wrong-directory-path.md` (`tests/test-templates-comprehensive.sh:492-528`). The assigned template is indirectly exercised by read/write validator path branches, but I did not find a direct assertion for the rendered `Wrong Directory Path` block.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`