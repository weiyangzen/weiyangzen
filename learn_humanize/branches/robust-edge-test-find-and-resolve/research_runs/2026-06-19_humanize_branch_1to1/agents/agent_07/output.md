# agent_07 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`
- research_scope: read-only inspection of `/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve`; no files modified.
- validation_note: executable tests were not run because this task requested research notes only in a read-only branch export. A `git status` probe also failed under the managed read-only environment because system git/xcrun tried to create cache files outside the readable export.

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: `tests` is the executable specification surface for the Humanize/RLCR loop. It validates the plugin’s core shell algorithms: template rendering, hook blocking, state-file parsing, plan-file integrity, cancel authorization, git/status parsing, monitor runtime behavior, zsh safety, timeout behavior, and generated command/plugin structure.
- algorithmic_behavior: The directory is recursively organized as a flat top-level shell suite plus `tests/robustness`. `tests/run-all-tests.sh:32` defines the ordered suite list, `tests/run-all-tests.sh:52` starts the robustness block, and `tests/run-all-tests.sh:65` declares zsh-only suites. It runs each script, captures output, strips ANSI color with a portable `sed` expression, extracts `Passed:`/`Failed:` counts, and aggregates suite failures at `tests/run-all-tests.sh:94`-`tests/run-all-tests.sh:127`.
- inputs_outputs_state: Inputs are local repo scripts/hooks/templates plus generated temporary fixtures. Shared helpers in `tests/test-helpers.sh:30`-`tests/test-helpers.sh:52` maintain pass/fail/skip counters, `tests/test-helpers.sh:86` creates temporary test dirs with cleanup traps, and `tests/test-helpers.sh:93` builds mock git repos. Outputs are console summaries and process exit status: runner returns `0` only when all suites pass, otherwise `1` at `tests/run-all-tests.sh:138`-`tests/run-all-tests.sh:149`.
- gates_or_invariants: The suite encodes fail-closed safety invariants: malformed state blocks hooks, `state.md`/`finalize-state.md` are protected, `.humanize` cannot be force-added, plan backups are immutable, active loop detection uses `.humanize/rlcr` rather than legacy paths, todo/prompt files are protected except explicit allowlists, cancel requires a signal file and exact `mv` shape, and zsh glob failures must not break monitor code.
- dependencies_and_callers: The directory coordinates with `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/loop-*-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/check-todos-from-transcript.py`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `commands/gen-plan.md`, and `agents/draft-relevance-checker.md`. `tests/test-template-references.sh:56`-`tests/test-template-references.sh:64` explicitly scans hook scripts for template references.
- edge_cases_or_failure_modes: Recursive coverage includes missing and malformed JSON, null/non-UTF8 input, deeply nested JSON, command injection, shell metacharacters, CRLF/binary files, symlinks, deleted loop dirs, stale/completed state files, detached HEAD/non-git repos, empty zsh glob sets, timeout fallback behavior, large templates/plan files, and plan backup/content mismatch.
- validation_or_tests: Directory contains 31 shell files totaling 13,705 lines by inspection, including runner/helper/manual scripts and 10 robustness suites. Important child roles include `tests/test-template-loader.sh` and `tests/test-error-scenarios.sh` for template behavior, `tests/test-plan-file-hooks.sh` for plan integrity and hook gates, `tests/test-cancel-signal-file.sh` for cancel-state transitions, `tests/test-finalize-phase.sh` for COMPLETE-to-finalize flow, `tests/test-state-exit-naming.sh` for terminal state names, and `tests/robustness/*.sh` for AC-1 through AC-11 edge-case coverage.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-037 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: This file is an executable negative/edge-case specification for `hooks/lib/template-loader.sh`. It verifies template loading and rendering functions degrade safely instead of causing empty or crashing validator messages.
- algorithmic_behavior: It sources `template-loader.sh` at `tests/test-error-scenarios.sh:13`, computes `TEMPLATE_DIR` through `get_template_dir` at `tests/test-error-scenarios.sh:15`, then runs 12 focused checks. The tested algorithms are `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir`.
- inputs_outputs_state: Inputs are template dir paths, template names, raw template strings, fallback strings, and `VAR=value` render bindings. Outputs are local `PASS`/`FAIL` lines and counters initialized at `tests/test-error-scenarios.sh:22`-`tests/test-error-scenarios.sh:24`; `pass()` and `fail()` mutate those counters at `tests/test-error-scenarios.sh:25`-`tests/test-error-scenarios.sh:35`. Final state exits `0` only if `TESTS_FAILED == 0` at `tests/test-error-scenarios.sh:218`-`tests/test-error-scenarios.sh:226`.
- gates_or_invariants: Missing template files and missing template directories must return an empty string with exit `0`, not abort. Empty content must render empty. Safe rendering must fall back when a template is unavailable. Invalid template directories must return nonzero. Empty placeholders and unclosed `{{` sequences must remain literal. The strict-mode subshell at `tests/test-error-scenarios.sh:114`-`tests/test-error-scenarios.sh:126` confirms missing templates do not break `set -euo pipefail`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`. The relevant implementation contracts are: `load_template` returns content or empty plus warning at `hooks/lib/template-loader.sh:36`-`hooks/lib/template-loader.sh:47`; `render_template` performs single-pass `{{VAR}}` replacement at `hooks/lib/template-loader.sh:58`-`hooks/lib/template-loader.sh:131`; `load_and_render_safe` falls back at `hooks/lib/template-loader.sh:170`-`hooks/lib/template-loader.sh:203`; `validate_template_dir` requires `block`, `codex`, and `claude` subdirs at `hooks/lib/template-loader.sh:208`-`hooks/lib/template-loader.sh:222`.
- edge_cases_or_failure_modes: Covers nonexistent files at `tests/test-error-scenarios.sh:45`-`tests/test-error-scenarios.sh:52`, nonexistent dirs at `tests/test-error-scenarios.sh:58`-`tests/test-error-scenarios.sh:65`, missing templates in `load_and_render` at `tests/test-error-scenarios.sh:71`-`tests/test-error-scenarios.sh:78`, special regex characters in values at `tests/test-error-scenarios.sh:97`-`tests/test-error-scenarios.sh:106`, fallback substitution at `tests/test-error-scenarios.sh:150`-`tests/test-error-scenarios.sh:158`, invalid template dir at `tests/test-error-scenarios.sh:175`-`tests/test-error-scenarios.sh:180`, empty placeholder at `tests/test-error-scenarios.sh:186`-`tests/test-error-scenarios.sh:193`, and unclosed placeholder at `tests/test-error-scenarios.sh:199`-`tests/test-error-scenarios.sh:206`.
- validation_or_tests: This file is itself the validation asset. It is also included in the aggregate runner at `tests/run-all-tests.sh:42`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-067 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: This prompt block is the user-facing rejection contract for attempts to modify the RLCR loop’s backup `plan.md`. It explains that the loop-local `plan.md` is a backup and must be read-only to preserve plan integrity.
- algorithmic_behavior: The file has no variables; it is static Markdown. Its text at `prompt-template/block/plan-backup-protected.md:3` states the backup cannot be modified, `prompt-template/block/plan-backup-protected.md:5` defines the integrity rationale, and `prompt-template/block/plan-backup-protected.md:7` instructs users to read rather than modify it. The loader treats it as a template loaded by name and returned unchanged.
- inputs_outputs_state: Input is a validator block event targeting `.humanize/rlcr/.../plan.md` or `.humanize/rlcr/plan.md`. Output is the rendered block message sent to stderr by Write/Edit/Bash validators. No persistent state is changed by the template itself; the surrounding validator state transition is “allow operation” to “block with exit `2`”.
- gates_or_invariants: The invariant is that the copied loop backup `plan.md` is immutable. This supports the stop hook’s later plan-integrity gate: it requires the backup to exist at `hooks/loop-codex-stop-hook.sh:185`-`hooks/loop-codex-stop-hook.sh:199`, checks the original plan exists at `hooks/loop-codex-stop-hook.sh:201`-`hooks/loop-codex-stop-hook.sh:212`, and diffs original versus backup at `hooks/loop-codex-stop-hook.sh:233`-`hooks/loop-codex-stop-hook.sh:250`.
- dependencies_and_callers: Called through `load_and_render_safe`. Write blocks plan backup writes at `hooks/loop-write-validator.sh:147`-`hooks/loop-write-validator.sh:157`; Edit blocks plan backup edits at `hooks/loop-edit-validator.sh:99`-`hooks/loop-edit-validator.sh:110`; Bash blocks shell modifications at `hooks/loop-bash-validator.sh:308`-`hooks/loop-bash-validator.sh:318`. The template loader’s fallback behavior means validators can still block even if this file is missing, but this file provides the intended precise message.
- edge_cases_or_failure_modes: Important bypass cases are covered by tests rather than the template: direct `.humanize/rlcr/plan.md`, nested timestamp dirs, command substitution, glob expansion, brace expansion, pipes, and backticks are tested in `tests/test-plan-file-hooks.sh:254`-`tests/test-plan-file-hooks.sh:335`. The template itself has no placeholders, so rendering cannot fail due to missing variables.
- validation_or_tests: Template existence/reference validation is covered by `tests/test-template-references.sh:83`-`tests/test-template-references.sh:111`. Plan backup hook behavior is validated by `tests/test-plan-file-hooks.sh:194`-`tests/test-plan-file-hooks.sh:263`, which checks Write, Edit, Bash redirection, Bash `rm`, and direct `.humanize/rlcr/plan.md` blocking.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-097 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: This file is the AC-1 robustness specification for RLCR state-file parsing. It verifies tolerant and strict parsing behavior for `state.md` YAML frontmatter, which drives hook round selection, max-iteration control, plan tracking, branch checks, and loop safety gates.
- algorithmic_behavior: It sources `hooks/lib/loop-common.sh` and shared helpers at `tests/robustness/test-state-file-robustness.sh:16`-`tests/robustness/test-state-file-robustness.sh:17`, creates a temp workspace at `tests/robustness/test-state-file-robustness.sh:19`, then writes many synthetic state files and invokes `parse_state_file`, `parse_state_file_strict`, and `get_current_round`. Positive cases are grouped at `tests/robustness/test-state-file-robustness.sh:26`-`tests/robustness/test-state-file-robustness.sh:138`; malformed/edge cases start at `tests/robustness/test-state-file-robustness.sh:139`.
- inputs_outputs_state: Inputs are generated `state*.md` files with YAML frontmatter. Outputs are global parser variables such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH`, plus function return codes. The implementation under test extracts current round at `hooks/lib/loop-common.sh:175`-`hooks/lib/loop-common.sh:184`, parses tolerant state with defaults at `hooks/lib/loop-common.sh:202`-`hooks/lib/loop-common.sh:228`, and parses strict state at `hooks/lib/loop-common.sh:240`-`hooks/lib/loop-common.sh:295`.
- gates_or_invariants: Valid frontmatter must parse required numeric fields; extra fields are ignored; quoted plan/branch values have quotes stripped; missing/nonexistent files fail; strict mode rejects missing separators, missing required fields, and nonnumeric round/max fields. Tolerant mode is intentionally permissive and defaults `current_round` to `0`, `max_iterations` to `10`, and `push_every_round` to `false`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`. Downstream callers include `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`, all of which use strict parsing to fail closed on malformed active loop state.
- edge_cases_or_failure_modes: Covers full valid state at `tests/robustness/test-state-file-robustness.sh:33`-`tests/robustness/test-state-file-robustness.sh:60`, extra fields at `tests/robustness/test-state-file-robustness.sh:72`-`tests/robustness/test-state-file-robustness.sh:98`, quoted values at `tests/robustness/test-state-file-robustness.sh:100`-`tests/robustness/test-state-file-robustness.sh:120`, zero round at `tests/robustness/test-state-file-robustness.sh:122`-`tests/robustness/test-state-file-robustness.sh:137`, missing frontmatter at `tests/robustness/test-state-file-robustness.sh:147`-`tests/robustness/test-state-file-robustness.sh:169`, nonnumeric round at `tests/robustness/test-state-file-robustness.sh:171`-`tests/robustness/test-state-file-robustness.sh:197`, missing fields/defaults at `tests/robustness/test-state-file-robustness.sh:199`-`tests/robustness/test-state-file-robustness.sh:226`, empty/partial/malformed files at `tests/robustness/test-state-file-robustness.sh:228`-`tests/robustness/test-state-file-robustness.sh:273`, large/negative/special values at `tests/robustness/test-state-file-robustness.sh:275`-`tests/robustness/test-state-file-robustness.sh:332`, nonexistent files at `tests/robustness/test-state-file-robustness.sh:354`-`tests/robustness/test-state-file-robustness.sh:360`, binary suffix content at `tests/robustness/test-state-file-robustness.sh:363`-`tests/robustness/test-state-file-robustness.sh:380`, and CRLF at `tests/robustness/test-state-file-robustness.sh:382`-`tests/robustness/test-state-file-robustness.sh:390`.
- validation_or_tests: This file is itself the executable validation. It is included in aggregate execution at `tests/run-all-tests.sh:53`. It finishes through `print_test_summary` and exits with the helper summary status at `tests/robustness/test-state-file-robustness.sh:399`-`tests/robustness/test-state-file-robustness.sh:400`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item evidence sections present in scheduler order`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`