# agent_08 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: High-level behavior contract for the Humanize RLCR workflow. It defines RLCR as an iterative development loop where Claude implements a plan and Codex independently reviews until completion or iteration exhaustion. See `README.md:7-13`, `README.md:15-27`, and the workflow diagram at `README.md:58-66`.
- algorithmic_behavior: The documented state machine is `plan.md -> Claude implements and summarizes -> Codex reviews -> feedback loop -> done`. The terminal conditions are explicitly documented as `COMPLETE` or max iterations at `README.md:60-66`. The README also defines the user-facing command surface: start loop, cancel loop, and plan generation at `README.md:78-85`.
- inputs_outputs_state: Inputs are a structured plan file, command options, and optional draft files for plan generation. `start-rlcr-loop` accepts `<path/to/plan.md>`, `--max`, `--codex-model <MODEL:EFFORT>`, `--codex-timeout`, and `--push-every-round` at `README.md:88-101`. `gen-plan` accepts `--input` and `--output`, then validates, analyzes, engages the user, and emits a structured plan with AC-style acceptance criteria at `README.md:103-120`. Runtime output/state is documented as `.humanize/rlcr/<timestamp>/` at `README.md:75`.
- gates_or_invariants: The README’s core invariant is independent review before completion: Claude implements, Codex reviews, and work continues until acceptance criteria are met. The max-iteration option creates a bounded loop gate, defaulting to 42 at `README.md:93-99`. Codex model/effort and timeout are configurable, defaulting to `gpt-5.2-codex:high` and 5400 seconds at `README.md:95-98`.
- dependencies_and_callers: The README is consumed by users/operators rather than sourced by scripts. It names the plugin commands that are backed by hook and command scripts elsewhere in the repo, especially `/humanize:start-rlcr-loop`, `/humanize:cancel-rlcr-loop`, and `/humanize:gen-plan` at `README.md:70-85`. It depends operationally on the Codex CLI prerequisite at `README.md:52-55`.
- edge_cases_or_failure_modes: The documented failure/stop edges are max iterations, cancellation, and Codex not returning completion. The README does not document the newer Finalize Phase tested elsewhere, so it is a partial behavior contract rather than a complete state-machine spec for this branch.
- validation_or_tests: The README’s flow is exercised more concretely by `tests/test-finalize-phase.sh`, which verifies `COMPLETE`, max-iteration handling, normal rounds, and active-loop states. The README itself has no executable assertions.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-038 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the Finalize Phase state machine and its validator gates. The file enumerates positive and negative cases at `tests/test-finalize-phase.sh:5-19` and exercises both shared helpers and hook scripts.
- algorithmic_behavior: The test constructs an isolated git repo, plan file, loop directory, `state.md`, plan backup, and goal tracker through `setup_test_repo` and `setup_loop_dir` at `tests/test-finalize-phase.sh:87-168`. It models these transitions: normal `state.md` active loop, `COMPLETE` review renaming `state.md` to `finalize-state.md`, Finalize completion renaming to `complete-state.md`, max-iteration completion renaming to `maxiter-state.md`, and normal feedback rounds incrementing `current_round`. The main tested implementation is `hooks/loop-codex-stop-hook.sh`, whose phase detection chooses `finalize-state.md` over `state.md` at `hooks/loop-codex-stop-hook.sh:66-82`.
- inputs_outputs_state: Inputs are synthetic hook JSON payloads for Write/Edit/Bash/Stop hooks, mock Codex executables inserted into `PATH`, transcript JSONL containing TodoWrite state, loop state frontmatter, and git working-tree state. Outputs are validator exit codes, JSON block decisions, marker files proving Codex invocation or non-invocation, review result files, and state-file renames. The test expects Write validator to allow `finalize-summary.md` at `tests/test-finalize-phase.sh:286-296`, block `finalize-state.md` writes/edits/bash mutations at `tests/test-finalize-phase.sh:298-356`, and complete Finalize by creating `complete-state.md` at `tests/test-finalize-phase.sh:417-445`.
- gates_or_invariants: `find_active_loop` must treat `finalize-state.md` as active and `complete-state.md` as inactive, asserted at `tests/test-finalize-phase.sh:231-260` and implemented at `hooks/lib/loop-common.sh:147-170`. Finalize must still require todos complete, a clean git tree, and `finalize-summary.md`, but it must not run Codex again; these gates are asserted at `tests/test-finalize-phase.sh:370-445` and `tests/test-finalize-phase.sh:526-546`. The stop hook implements the ordering: todo check at `hooks/loop-codex-stop-hook.sh:255-310`, git cleanliness at `hooks/loop-codex-stop-hook.sh:464-486`, summary selection at `hooks/loop-codex-stop-hook.sh:521-557`, and Finalize completion without Codex at `hooks/loop-codex-stop-hook.sh:648-660`.
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` at `tests/test-finalize-phase.sh:48-49`. It calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/loop-plan-file-validator.sh` throughout `tests/test-finalize-phase.sh:286-669`. It relies on shared helper functions including `is_finalize_state_file_path`, `is_finalize_summary_path`, `find_active_loop`, and `parse_state_file`, defined in `hooks/lib/loop-common.sh:147-229` and `hooks/lib/loop-common.sh:419-429`.
- edge_cases_or_failure_modes: The test explicitly covers max iterations skipping Finalize at `tests/test-finalize-phase.sh:483-501`; incomplete todos blocking before completion at `tests/test-finalize-phase.sh:520-546`; dirty git blocking at `tests/test-finalize-phase.sh:395-412`; missing summary blocking at `tests/test-finalize-phase.sh:370-385`; `complete-state.md` not reviving an active loop at `tests/test-finalize-phase.sh:243-261`; and normal non-`COMPLETE` review preserving `state.md`, incrementing the round, creating a review result, and returning feedback at `tests/test-finalize-phase.sh:552-619`.
- validation_or_tests: This file is itself the validation suite. It reports pass/fail counters and exits with `TESTS_FAILED` at `tests/test-finalize-phase.sh:672-681`. I did not execute it because this assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-068 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Block-message template for the plan integrity gate. It communicates that the original plan file changed after loop start and instructs the user to cancel, edit, and restart the loop instead of mutating the plan mid-run.
- algorithmic_behavior: The template renders two variables, `{{PLAN_FILE}}` and `{{BACKUP_PATH}}`, and produces a deterministic blocking reason. The content states that modifying plan files is forbidden during an active RLCR loop at `prompt-template/block/plan-file-modified.md:3-12`.
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH`. The stop hook passes these values when `diff -q "$FULL_PLAN_PATH" "$BACKUP_PLAN"` detects divergence, at `hooks/loop-codex-stop-hook.sh:233-250`. Output is a rendered markdown reason embedded into a JSON hook response with `"decision": "block"` at `hooks/loop-codex-stop-hook.sh:250-251`.
- gates_or_invariants: The invariant is plan immutability for an active loop. The stop hook checks that the backup exists, the original plan still exists, tracked plans are clean, and the plan content still matches the backup at `hooks/loop-codex-stop-hook.sh:181-253`. The assigned template is specifically used for the content-diff case, including committed or untracked content drift that status checks alone could miss.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md"` in `hooks/loop-codex-stop-hook.sh:247-249`. Rendering is provided by `hooks/lib/template-loader.sh`, whose syntax and single-pass substitution rules are defined at `hooks/lib/template-loader.sh:7-20` and implemented at `hooks/lib/template-loader.sh:58-132`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to an inline message with the same variables, implemented at `hooks/lib/template-loader.sh:170-203`. The template itself has no conditional logic, so incorrect or empty variables would be surfaced literally as missing or misleading paths. The hook also has earlier distinct failure modes for missing backup, deleted original plan, uncommitted tracked-plan changes, and git tracking status drift at `hooks/loop-codex-stop-hook.sh:188-230`.
- validation_or_tests: Covered indirectly by hook behavior tests and template robustness tests. The branch has template reference and loader tests, and the assigned stress test verifies fallback rendering and variable substitution under load. I did not find a direct assertion for this exact template content in the assigned files, but the call site is concrete.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-098 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for the prompt-template rendering subsystem under AC-8 stress and edge conditions. It validates that the template engine can safely render large values, large templates, many variables, shell/regex special characters, placeholder-looking values, empty values, and boundary placeholders.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` and shared test helpers at `tests/robustness/test-template-stress-robustness.sh:14-18`, creates a temporary test directory at `tests/robustness/test-template-stress-robustness.sh:19`, then runs 18 assertions. It exercises `render_template`, `load_and_render`, and `load_and_render_safe`, which are implemented in `hooks/lib/template-loader.sh:58-147` and `hooks/lib/template-loader.sh:170-203`.
- inputs_outputs_state: Inputs include inline templates, temp template files, `VAR=value` assignments, generated 10KB and 100KB variable values, a generated roughly 100KB template file, and an array of 50 variable assignments. Outputs are rendered strings compared by exact equality, substring checks, length thresholds, elapsed-time observations, and the shared pass/fail counters. The script exits via `print_test_summary` at `tests/robustness/test-template-stress-robustness.sh:285-290`.
- gates_or_invariants: Rendering must be single-pass and non-recursive: a value containing `{{VAR_B}}` must remain literal while the explicit `{{VAR_B}}` placeholder in the original template renders, asserted at `tests/robustness/test-template-stress-robustness.sh:218-228` and specified in `hooks/lib/template-loader.sh:50-57`. Missing templates must use fallback content through `load_and_render_safe`, asserted at `tests/robustness/test-template-stress-robustness.sh:44-52`. Missing variables should remain placeholders per loader comments at `hooks/lib/template-loader.sh:7-14`, though this assigned stress file focuses more on supplied variables.
- dependencies_and_callers: Depends on `awk`, `env`, `grep`, `wc`, `date`, `seq`, and shell array handling. It uses `setup_test_dir`, `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh:30-89`. It validates a dependency used widely by hooks that render block prompts, including the assigned `prompt-template/block/plan-file-modified.md`.
- edge_cases_or_failure_modes: Covered edge cases include regex metacharacters at `tests/robustness/test-template-stress-robustness.sh:161-170`, ampersands at `tests/robustness/test-template-stress-robustness.sh:172-181`, backslashes at `tests/robustness/test-template-stress-robustness.sh:183-193`, multiline values at `tests/robustness/test-template-stress-robustness.sh:195-205`, empty values at `tests/robustness/test-template-stress-robustness.sh:207-216`, dollar signs at `tests/robustness/test-template-stress-robustness.sh:241-250`, repeated variables at `tests/robustness/test-template-stress-robustness.sh:252-261`, boundary variables at `tests/robustness/test-template-stress-robustness.sh:263-272`, and placeholder-only templates at `tests/robustness/test-template-stress-robustness.sh:274-283`. The comments note circular references are not applicable because variables do not reference each other by design at `tests/robustness/test-template-stress-robustness.sh:5-9`.
- validation_or_tests: This file is itself a validation script for the template subsystem. I did not execute it because the scheduler asked for research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 of 4 item sections present, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`