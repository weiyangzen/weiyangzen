# agent_03 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: `commands/` is the slash-command contract layer for the RLCR workflow. It contains three command definitions: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, and `commands/cancel-rlcr-loop.md`. These files do not implement shell logic directly; they constrain allowed tools and define the algorithmic handoff to scripts/hooks.
- algorithmic_behavior: `commands/gen-plan.md` defines a five-phase plan-generation flow: validate IO, check draft relevance, analyze draft quality, resolve issues with the user, and write a structured implementation plan (`commands/gen-plan.md:19-25`, `commands/gen-plan.md:29-45`, `commands/gen-plan.md:48-68`, `commands/gen-plan.md:72-117`, `commands/gen-plan.md:120-224`). `commands/start-rlcr-loop.md` delegates loop setup to `scripts/setup-rlcr-loop.sh`, then describes iterative work, summary writing, Codex review, and completion when Codex outputs `COMPLETE` (`commands/start-rlcr-loop.md:10-23`, `commands/start-rlcr-loop.md:51-55`). `commands/cancel-rlcr-loop.md` delegates cancellation to `scripts/cancel-rlcr-loop.sh`, interprets first-line status tokens, and requires user confirmation before cancelling Finalize Phase unless forced (`commands/cancel-rlcr-loop.md:11-34`).
- inputs_outputs_state: `gen-plan` input is `--input <draft>` and `--output <plan>`; its output is a new plan file with goal, ACs, path boundaries, feasibility hints, and dependencies (`commands/gen-plan.md:3`, `commands/gen-plan.md:126-194`). `start-rlcr-loop` input is a plan path plus optional max/model/timeout/push flags (`commands/start-rlcr-loop.md:3`); its delegated script creates `.humanize/rlcr/<timestamp>/state.md`, copies `plan.md`, initializes `goal-tracker.md`, and writes `round-0-prompt.md`/summary path (`scripts/setup-rlcr-loop.sh:508-520`, `scripts/setup-rlcr-loop.sh:527-618`, `scripts/setup-rlcr-loop.sh:624-684`). `cancel-rlcr-loop` outputs one of `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM`, and state changes are done by renaming `state.md` or `finalize-state.md` to `cancel-state.md` after writing `.cancel-requested` (`commands/cancel-rlcr-loop.md:17-22`, `scripts/cancel-rlcr-loop.sh:85-99`, `scripts/cancel-rlcr-loop.sh:117-138`, `scripts/cancel-rlcr-loop.sh:144-152`).
- gates_or_invariants: `gen-plan` requires `scripts/validate-gen-plan-io.sh` exit code 0 before analysis; exit codes 1-6 map to specific stop reasons (`commands/gen-plan.md:31-45`, `scripts/validate-gen-plan-io.sh:4-11`, `scripts/validate-gen-plan-io.sh:78-123`). It also gates plan structure with AC-X format, positive/negative tests, no time estimates, no line-number references, and affirmative path boundaries (`commands/gen-plan.md:196-214`). `start-rlcr-loop` enforces “no cheating” by forbidding direct state edits/cancel-command bypasses in the command instructions (`commands/start-rlcr-loop.md:43-49`) and relies on setup-script gates for relative plan paths, no spaces/metacharacters, no symlink traversal, git status/tracking policy, sufficient content, Codex availability, YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:227-248`, `scripts/setup-rlcr-loop.sh:253-328`, `scripts/setup-rlcr-loop.sh:334-379`, `scripts/setup-rlcr-loop.sh:385-445`, `scripts/setup-rlcr-loop.sh:458-484`). `cancel-rlcr-loop` invariant: the script is authoritative for cancellation and active loop detection is based on newest loop directory containing `state.md` or `finalize-state.md` (`commands/cancel-rlcr-loop.md:36-37`).
- dependencies_and_callers: Command frontmatter restricts allowed tools: `gen-plan` allows the validation bash script plus Read/Glob/Grep/Task/Write/AskUserQuestion (`commands/gen-plan.md:4-11`), `start` allows only setup script execution (`commands/start-rlcr-loop.md:4`), and `cancel` allows only the cancel script with optional `--force` plus AskUserQuestion (`commands/cancel-rlcr-loop.md:3`). These commands coordinate with sibling `scripts/` and `hooks/`: setup creates loop state consumed by validators, monitor, and stop hooks; cancel signal/state rename is specially authorized by bash validation through `is_cancel_authorized` in `hooks/lib/loop-common.sh:455-614`.
- edge_cases_or_failure_modes: `gen-plan` rejects missing/empty input, missing output directory, pre-existing output path, unwritable output directory, invalid args, and irrelevant drafts (`commands/gen-plan.md:37-45`, `commands/gen-plan.md:63-66`). `start` rejects absolute plan paths, spaces, shell metacharacters, symlinks in file or parent path, submodule paths, tracked plans unless explicitly allowed, and insufficient-content plans (`scripts/setup-rlcr-loop.sh:227-248`, `scripts/setup-rlcr-loop.sh:253-328`, `scripts/setup-rlcr-loop.sh:370-393`). `cancel` protects Finalize Phase by returning `FINALIZE_NEEDS_CONFIRM` unless `--force` is used (`scripts/cancel-rlcr-loop.sh:117-127`).
- validation_or_tests: Related coverage includes plan and path validation tests in `tests/test-gen-plan.sh`, `tests/test-plan-file-validation.sh`, `tests/test-plan-file-hooks.sh`, cancellation tests in `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, and template reference tests. This assigned directory itself is prompt/command contract, not executable validation code.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-033 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the allowlist exception layer in RLCR hook validators. It verifies that a tiny set of loop files can bypass the default “do not access todos/wrong-round summaries” rules while all same-looking paths outside the active loop remain blocked.
- algorithmic_behavior: The test constructs a temporary git repository and active loop directory, writes a minimal `.humanize/rlcr/<timestamp>/state.md` with `current_round: 5`, then tests `is_allowlisted_file` directly and through Write/Edit/Read/Bash validators (`tests/test-allowlist-validators.sh:29-62`, `tests/test-allowlist-validators.sh:64-125`, `tests/test-allowlist-validators.sh:127-183`, `tests/test-allowlist-validators.sh:185-226`, `tests/test-allowlist-validators.sh:228-282`, `tests/test-allowlist-validators.sh:284-381`). The production allowlist is exact path equality for four basenames in the active loop directory: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`hooks/lib/loop-common.sh:327-345`).
- inputs_outputs_state: Inputs are JSON hook payloads on stdin, e.g. `{"tool_name":"Write","tool_input":{"file_path":"<loop>/round-1-todos.md"}}` for file validators and `{"tool_name":"Bash","tool_input":{"command":"echo test > <loop>/round-1-todos.md"}}` for bash validation (`tests/test-allowlist-validators.sh:135-138`, `tests/test-allowlist-validators.sh:290-293`). Outputs are validator exit codes and diagnostic text. Expected pass is exit 0; expected block is exit 2 with text matching “todos” or “round” (`tests/test-allowlist-validators.sh:166-169`, `tests/test-allowlist-validators.sh:278-281`, `tests/test-allowlist-validators.sh:321-324`).
- gates_or_invariants: The invariant is basename plus active-loop absolute path, not basename alone. Direct helper tests require wrong directory rejection (`tests/test-allowlist-validators.sh:118-124`). Bash validator additionally requires the command to mention the full active loop path for `round-[12]-todos.md`; generic relative paths, old loop directories, and same-basename directories under another root are blocked (`tests/test-allowlist-validators.sh:314-381`, `hooks/loop-bash-validator.sh:363-370`). Read/Write/Edit validators only bypass wrong-round/todos blocks if `is_allowlisted_file "$FILE_PATH" "$ACTIVE_LOOP_DIR"` is true (`hooks/loop-read-validator.sh:52-59`, `hooks/loop-read-validator.sh:135-148`, `hooks/loop-write-validator.sh:53-60`, `hooks/loop-write-validator.sh:203-217`, `hooks/loop-edit-validator.sh:36-43`, `hooks/loop-edit-validator.sh:137-151`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` directly (`tests/test-allowlist-validators.sh:15-17`) and invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` with `CLAUDE_PROJECT_DIR` pointing at the temp repo (`tests/test-allowlist-validators.sh:130-137`, `tests/test-allowlist-validators.sh:191-194`, `tests/test-allowlist-validators.sh:234-237`, `tests/test-allowlist-validators.sh:290-293`). The validators depend on `find_active_loop`, round-file parsing, strict state parsing for non-todos paths, and template-backed block messages in `loop-common.sh`.
- edge_cases_or_failure_modes: Covered edge cases include non-allowlisted future/older todo files (`round-3-todos.md`, `round-4-todos.md`), non-allowlisted summary files (`round-2-summary.md`, `round-3-summary.md`), wrong directory, generic relative todo filename, stale older loop directory, and a security case where a different root reuses the same active loop basename (`tests/test-allowlist-validators.sh:102-124`, `tests/test-allowlist-validators.sh:159-183`, `tests/test-allowlist-validators.sh:215-226`, `tests/test-allowlist-validators.sh:258-281`, `tests/test-allowlist-validators.sh:327-381`).
- validation_or_tests: The script reports pass/fail counters and exits with the number of failed tests (`tests/test-allowlist-validators.sh:383-391`). It is itself the validation asset; I did not execute it in this read-only research run.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-063 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: This template is the user-facing enforcement block for the goal-tracker ownership transition after Round 0. It encodes the rule that direct `goal-tracker.md` mutation is blocked and changes must flow through a summary-file request for Codex review.
- algorithmic_behavior: The rendered block names the current round, states that “only Codex can modify the Goal Tracker” after Round 0, forbids Write/Edit/Bash changes to `goal-tracker.md`, and prescribes a `Goal Tracker Update Request` section in `{{SUMMARY_FILE}}` with requested changes and justification (`prompt-template/block/goal-tracker-modification.md:1-23`).
- inputs_outputs_state: Inputs are template variables `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}` (`prompt-template/block/goal-tracker-modification.md:1`, `prompt-template/block/goal-tracker-modification.md:9-10`). Output is a markdown block printed to stderr by validators when a blocked modification is detected. The intended state transition is not direct file mutation; instead, user/Claude writes a request into the current round summary, then Codex applies justified updates later.
- gates_or_invariants: The invariant is strict post-Round-0 ownership: direct `goal-tracker.md` edits are blocked when `CURRENT_ROUND > 0`. `hooks/loop-write-validator.sh` checks `is_goal_tracker_path` plus `CURRENT_ROUND > 0` and emits this block (`hooks/loop-write-validator.sh:160-167`). `hooks/loop-edit-validator.sh` does the same for Edit (`hooks/loop-edit-validator.sh:113-120`). `hooks/loop-bash-validator.sh` blocks shell modification patterns for `goal-tracker.md`; Round 0 gets a Bash-specific “use Write/Edit” block, while later rounds get this template through `goal_tracker_blocked_message` (`hooks/loop-bash-validator.sh:321-335`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` provides `goal_tracker_blocked_message`, renders `block/goal-tracker-modification.md`, and falls back to an inline message if template loading fails (`hooks/lib/loop-common.sh:802-814`). `hooks/loop-codex-stop-hook.sh` references the corresponding Codex responsibility: if the summary includes a Goal Tracker Update Request, Codex should apply justified changes to `goal-tracker.md` (`hooks/loop-codex-stop-hook.sh:680-682`). Related prompt surfaces exist in `prompt-template/codex/goal-tracker-update-section.md` and `prompt-template/claude/goal-tracker-update-request.md`.
- edge_cases_or_failure_modes: If the active loop state is malformed, validators fail closed before reaching normal block rendering (`hooks/loop-write-validator.sh:112-115`, `hooks/loop-edit-validator.sh:77-80`, `hooks/loop-bash-validator.sh:67-70`). If the template directory fails validation, `loop-common.sh` warns and uses inline fallback text (`hooks/lib/loop-common.sh:140-145`, `hooks/lib/loop-common.sh:807-814`). Bash modification detection depends on `command_modifies_file`, so unusual shell constructs outside its patterns are the main residual bypass risk (`hooks/lib/loop-common.sh:772-800`).
- validation_or_tests: Template presence is referenced by `tests/test-template-references.sh` (`tests/test-template-references.sh:158`), and comprehensive template tests inspect goal-tracker request language (`tests/test-templates-comprehensive.sh:535`). Behavioral validation is via write/edit/bash validator tests that block goal-tracker changes after Round 0, including tests outside this assigned set.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-093 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: This file is an executable robustness specification for hook JSON parsing (AC-7), monitor edge handling (AC-6), command modification detection, and production monitor helper integration. It verifies that malformed or hostile hook input is rejected gracefully while benign edge inputs do not crash the system.
- algorithmic_behavior: The first block pipes JSON into production validators and checks valid Read/Write/Bash inputs, invalid JSON, missing `tool_name`, missing `tool_input.file_path`, long commands, shell-special characters, Unicode paths, unknown tool names, deeply nested JSON, invalid UTF-8 bytes, and null-byte handling (`tests/robustness/test-hook-input-robustness.sh:35-235`). The implementation under test is `validate_hook_input`, `require_tool_input_field`, and `is_deeply_nested` in `hooks/lib/loop-common.sh:55-129`, called by Read/Write/Bash validators (`hooks/loop-read-validator.sh:22-43`, `hooks/loop-write-validator.sh:23-43`, `hooks/loop-bash-validator.sh:22-43`). The later blocks exercise terminal/log edge cases, command write-pattern detection, and real monitor helper functions from `scripts/humanize.sh` (`tests/robustness/test-hook-input-robustness.sh:245-370`, `tests/robustness/test-hook-input-robustness.sh:381-664`).
- inputs_outputs_state: Inputs are stdin JSON payloads with `tool_name` and `tool_input` fields, temp log files, temp goal tracker files, fake `.humanize/rlcr/<timestamp>` session directories, and shimmed `HOME`, `XDG_CACHE_HOME`, `tput`, and `clear` for monitor isolation (`tests/robustness/test-hook-input-robustness.sh:37-44`, `tests/robustness/test-hook-input-robustness.sh:455-474`, `tests/robustness/test-hook-input-robustness.sh:514-562`, `tests/robustness/test-hook-input-robustness.sh:605-648`). Outputs are pass/fail counters plus expected validator/monitor exit codes. State is temporary only: `setup_test_dir` creates a temp directory and cleanup trap (`tests/robustness/test-hook-input-robustness.sh:17-20`, `tests/test-helpers.sh:84-89`).
- gates_or_invariants: Valid JSON for matching tools should pass with exit 0 when no protected loop path is involved (`tests/robustness/test-hook-input-robustness.sh:35-69`). Invalid JSON, missing required fields, excessive nesting depth over 30, and invalid UTF-8 must return nonzero below 128, i.e. reject without signal crash (`tests/robustness/test-hook-input-robustness.sh:71-115`, `tests/robustness/test-hook-input-robustness.sh:177-217`). Unknown tool names should pass through (`tests/robustness/test-hook-input-robustness.sh:163-175`). Monitor invariants are “no crash” for missing sessions, narrow terminal, ANSI logs, deleted session/log directories, and cache-backed log lookup (`tests/robustness/test-hook-input-robustness.sh:444-486`, `tests/robustness/test-hook-input-robustness.sh:488-578`, `tests/robustness/test-hook-input-robustness.sh:580-664`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-hook-input-robustness.sh:14-17`). It invokes `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`; it does not exercise malformed-JSON behavior for `loop-edit-validator.sh`, which still parses directly with `jq` rather than shared `validate_hook_input` (`hooks/loop-edit-validator.sh:22-30`). It sources `scripts/humanize.sh` and calls `humanize_parse_goal_tracker`, `humanize_parse_git_status`, `humanize_detect_git_state`, and `_humanize_monitor_codex` (`tests/robustness/test-hook-input-robustness.sh:381-433`, `scripts/humanize.sh:26-105`, `scripts/humanize.sh:110-155`, `scripts/humanize.sh:160-203`, `scripts/humanize.sh:213-868`).
- edge_cases_or_failure_modes: Covered hook edge cases include non-UTF8 binary bytes (`tests/robustness/test-hook-input-robustness.sh:201-217`), null bytes stripped by Bash command substitution but required not to crash (`tests/robustness/test-hook-input-robustness.sh:219-235`), long 10KB commands (`tests/robustness/test-hook-input-robustness.sh:117-133`), Unicode escape paths (`tests/robustness/test-hook-input-robustness.sh:149-160`), and deep nesting DoS prevention (`tests/robustness/test-hook-input-robustness.sh:177-199`). Covered monitor edge cases include terminal width extremes, log deletion, ANSI stripping, binary log content, rapid updates, missing session directory, narrow terminal, and ANSI log display (`tests/robustness/test-hook-input-robustness.sh:245-341`, `tests/robustness/test-hook-input-robustness.sh:444-664`). Residual gap: malformed JSON coverage excludes the Edit validator.
- validation_or_tests: The script is a validation runner and exits through `print_test_summary "Hook Input & Monitor Test Summary"` (`tests/robustness/test-hook-input-robustness.sh:666-671`). Production monitor code under test uses `find` instead of glob for no-match safety, cache-path sanitization for logs, fixed status bar drawing, cleanup traps, and graceful stop when `.humanize/rlcr` disappears (`scripts/humanize.sh:231-313`, `scripts/humanize.sh:381-518`, `scripts/humanize.sh:543-580`, `scripts/humanize.sh:612-864`). I did not execute it in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-003`, `ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-033`, `ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-063`, `ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-093`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`