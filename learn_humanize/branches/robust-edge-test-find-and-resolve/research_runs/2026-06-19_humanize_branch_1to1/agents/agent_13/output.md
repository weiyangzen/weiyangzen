# agent_13 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-013 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Robustness test corpus for the repository’s RLCR/humanize core shell algorithms. It recursively contains ten executable shell specifications covering monitor parsing, active-session discovery, state-file parsing, path/plan validation, git status/state parsing, hook JSON input validation, cancel-command authorization, timeout portability, and template rendering stress behavior.
- algorithmic_behavior: The directory is organized as one shell spec per acceptance area. `tests/robustness/test-goal-tracker-robustness.sh` sources `scripts/humanize.sh` and verifies `humanize_parse_goal_tracker` counts ACs, active/completed/deferred tasks, open issues, and truncates goal summaries using pipe-delimited output; see tests around standard list/table AC formats and completed rows at lines 60-202, and edge formats at lines 237-524. `tests/robustness/test-session-robustness.sh` validates `find_active_loop`, whose production behavior is newest-directory-only active-loop detection for `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh` lines 152-170); the test covers many sessions, missing directories, finished/cancelled sessions, symlink directories, spaces, and deep paths (`test-session-robustness.sh` lines 33-118, 128-317). `tests/robustness/test-state-file-robustness.sh` exercises tolerant and strict parsing: valid YAML frontmatter, defaulting, quoted fields, missing frontmatter, non-numeric values, partial YAML, binary content, CRLF, and missing files (`test-state-file-robustness.sh` lines 33-138, 147-393). The production tolerant parser defaults missing `current_round` to `0`, `max_iterations` to `10`, and `push_every_round` to `false` (`hooks/lib/loop-common.sh` lines 202-228), while strict mode rejects missing separators and required/non-numeric fields (`hooks/lib/loop-common.sh` lines 240-284). `tests/robustness/test-path-validation-robustness.sh` runs `scripts/setup-rlcr-loop.sh` against valid and invalid plan paths, checking relative paths, shell metacharacter rejection, symlink rejection, parent-symlink traversal rejection, deep/long paths, empty/comment-only/short/nonexistent/directory plans (`test-path-validation-robustness.sh` lines 55-89, 98-462). The production gates reject absolute paths, whitespace, shell metacharacters, file symlinks, parent symlinks, missing/unreadable/outside-project/submodule paths, bad tracking state, fewer than 5 lines, and fewer than 3 content lines (`scripts/setup-rlcr-loop.sh` lines 229-440). `tests/robustness/test-git-operations-robustness.sh` validates `humanize_parse_git_status` and `humanize_detect_git_state` for clean, modified, staged, deleted, untracked, renamed, binary, detached, rebase, merge, shallow, non-git, empty repo, and permission states (`test-git-operations-robustness.sh` lines 71-367, 377-500). Production status parsing uses `git status --porcelain` state letters and diff shortstat (`scripts/humanize.sh` lines 160-190); state detection checks `rebase-merge`, `rebase-apply`, `MERGE_HEAD`, `shallow`, symbolic HEAD, and git-dir readability (`scripts/humanize.sh` lines 110-155). `tests/robustness/test-hook-input-robustness.sh` pipes JSON to real hook validators, checking valid Read/Write/Bash input, malformed JSON, missing fields, long commands, special characters, Unicode, unknown tools, nested JSON rejection, non-UTF8 rejection, null-byte graceful handling, monitor terminal/log edge cases, command modification detection, and monitor integration (`test-hook-input-robustness.sh` lines 35-235, 245-665). `tests/robustness/test-cancel-security-robustness.sh` validates `is_cancel_authorized` accepts only signal-file-backed `mv` operations from `state.md` or `finalize-state.md` to `cancel-state.md`, and rejects injection, wrong paths, extra args, mixed quotes, remaining variable expansion, symlinks, and path-name regressions around `finalize` substrings (`test-cancel-security-robustness.sh` lines 39-89, 99-405). The production function requires `.cancel-requested`, rejects command substitution/backticks/newlines/operators/trailing spaces, normalizes `$loop_dir`, rejects residual `$`, and requires an `mv` command (`hooks/lib/loop-common.sh` lines 455-525). `tests/robustness/test-timeout-robustness.sh` exercises timeout implementation selection and `run_with_timeout` command behavior for quick commands, timed-out commands, args, preserved exits, pipelines, zero/short timeouts, large output, rapid cycles, missing commands, special args, signal handlers, subshells, and exported `TIMEOUT_IMPL` (`test-timeout-robustness.sh` lines 31-278). Production timeout selection is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none`, with timeout exit normalized to `124` for Python fallback (`scripts/portable-timeout.sh` lines 10-76). `tests/robustness/test-plan-file-robustness.sh` stress-tests production plan validation for valid/rich/large/mixed-line-ending plans, empty/comment-only/large/binary/long-line/special-character/whitespace/nested-comment/missing/unreadable/symlink/directory/null-byte/disappearing files (`test-plan-file-robustness.sh` lines 22-82, 152-525). `tests/robustness/test-template-stress-robustness.sh` validates template substitution, fallback rendering, large values/templates, many variables, regex/sed-special values, backslashes, newlines, empty values, placeholder injection prevention, repeated variables, and boundary-only placeholders (`test-template-stress-robustness.sh` lines 33-289). Production rendering is single-pass AWK over `{{VAR}}` placeholders with `TMPL_VAR_` environment variables, preserving placeholders in substituted values rather than rescanning them (`hooks/lib/template-loader.sh` lines 49-129).
- inputs_outputs_state: Inputs are temporary files, mock git repositories, synthetic `.humanize/rlcr` session trees, JSON hook payloads on stdin, shell command strings, plan paths, markdown goal/state/plan/template content, and environment variables such as `CLAUDE_PROJECT_DIR`, `HOME`, `XDG_CACHE_HOME`, and terminal shims. Outputs are shell pass/fail counters via `tests/test-helpers.sh` (`pass`, `fail`, `print_test_summary` at lines 30-78), exit status `0` when all assertions pass and `1` on any failed assertion, plus production function outputs such as pipe-delimited summaries, state variables, active-loop paths, hook exit statuses, and timeout command exit codes. State transitions modeled include active loop discovery from no session to `state.md` or `finalize-state.md`, monitor cleanup from running to stopped, cancel from `state.md`/`finalize-state.md` toward `cancel-state.md`, git states from normal to detached/rebase/merge/shallow, and plan validation from raw input path to accepted loop setup preconditions.
- gates_or_invariants: The directory asserts fail-closed behavior around malformed inputs. Key invariants include: only newest RLCR session may be active; finished/cancelled sessions do not reactivate; tolerant state parsing must default safely while strict parsing rejects malformed frontmatter; plan paths must be relative, shell-safe, nonsymlinked, project-contained, and contentful; hook JSON must be valid UTF-8 JSON with required `tool_name` and tool-specific fields, and depth above 30 is rejected (`hooks/lib/loop-common.sh` lines 46-124); cancel operations require a signal file and exact source/destination semantics; command modification detection must catch redirects, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` (`hooks/lib/loop-common.sh` lines 775-800); template substitution is single-pass to prevent placeholder injection.
- dependencies_and_callers: The specs depend on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and the hook entrypoints `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`. They coordinate with parent `tests/` as executable validation assets and with production paths under `hooks/` and `scripts/`. External command dependencies include bash, git, sed, grep, awk, jq for hook validation, iconv where available for encoding checks, timeout/gtimeout or Python fallback, and standard coreutils. Several tests create mock git repos under temporary directories and intentionally set `git config commit.gpgsign false` to keep validation deterministic.
- edge_cases_or_failure_modes: Covered failures include missing files/directories, non-git dirs, empty repos, permission-inaccessible `.git`, detached HEAD, rebase/merge marker files, shallow clones, binary/null content, CRLF/mixed line endings, very large files and values, very long commands/lines, command substitution, shell chaining, residual variable expansion, IFS manipulation, symlink source and parent-directory traversal, paths containing `finalize`, mixed quote styles, multiple trailing spaces, deeply nested JSON, non-UTF8 JSON, deleted logs/session dirs, narrow/wide terminal widths, missing `codex`, and gitignore/tracking mismatches. One notable test design edge: some plan/file tests validate helper behavior locally rather than always invoking production validation for every edge, so they are a mix of production integration checks and lower-level robustness assertions.
- validation_or_tests: This directory itself is validation. It is intended to be run as individual shell specs, for example `bash tests/robustness/test-state-file-robustness.sh`, `bash tests/robustness/test-hook-input-robustness.sh`, or as part of a wider test runner if present. I did not execute the specs in this read-only research pass; I inspected them and their referenced production functions statically.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-043 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime verification spec for monitor shutdown and terminal restoration behavior in `scripts/humanize.sh`, specifically AC-1.1 clean/user-friendly exit when `.humanize` is deleted and AC-1.3 terminal state restoration through cleanup.
- algorithmic_behavior: The script creates a temporary project at `/tmp/test-monitor-runtime-$$`, installs an EXIT trap to delete it, and runs five focused tests (`tests/test-monitor-runtime.sh` lines 45-53, 55-345). Test 1 builds a temporary script that sources `scripts/humanize.sh`, simulates monitor variables, stubs `_restore_terminal`, defines simplified `_cleanup` and `_graceful_stop`, then asserts `_restore_terminal`, `_cleanup`, and the message containing “Monitoring stopped” and the deleted-directory reason are emitted (`tests/test-monitor-runtime.sh` lines 61-134). Test 2 verifies idempotency by calling `_graceful_stop` and `_cleanup` repeatedly and requiring `FINAL_COUNT: 1` (`tests/test-monitor-runtime.sh` lines 136-177). Test 3 models the main loop check: if `.humanize/rlcr` exists it continues, after `rm -rf .humanize/rlcr` it calls `_graceful_stop` and prints `STOPPED_AFTER_DELETE` (`tests/test-monitor-runtime.sh` lines 179-251). Test 4 checks source-level terminal restoration: `_restore_terminal()` is defined, emits the scroll-region reset escape `printf "\033[r"`, and `_cleanup()` calls `_restore_terminal` (`tests/test-monitor-runtime.sh` lines 253-310). Test 5 checks `_graceful_stop()` calls `_cleanup()` (`tests/test-monitor-runtime.sh` lines 312-323). The production behavior under test is in `scripts/humanize.sh`: `_restore_terminal` resets the scroll region and moves the cursor to the bottom (`scripts/humanize.sh` lines 531-536), `_cleanup` is idempotent, resets traps, kills `tail_pid`, restores terminal, and prints “Stopped monitoring” (`scripts/humanize.sh` lines 543-561), and `_graceful_stop` calls `_cleanup` before printing the reason and cancellation/deletion explanation (`scripts/humanize.sh` lines 566-577).
- inputs_outputs_state: Inputs are the project root path, a temporary fake RLCR directory tree, generated helper scripts, and sourced `scripts/humanize.sh`. Outputs are colored `PASS`/`FAIL` lines, accumulated `TESTS_PASSED`/`TESTS_FAILED`, and process exit `0` only when no failures remain (`tests/test-monitor-runtime.sh` lines 22-34, 328-345). State transitions are `cleanup_done=false` to `true`, `monitor_running=true` to `false`, terminal scroll region constrained to restored, and loop-directory-present to loop-directory-deleted causing graceful stop.
- gates_or_invariants: Cleanup must be idempotent and guarded by `cleanup_done`; graceful stop must never bypass cleanup; terminal restoration must reset the scroll region using `\033[r`; deletion of `.humanize/rlcr` must produce a user-facing reason instead of a raw crash; and monitor cleanup must happen before final stop messaging so terminal state is usable for the message.
- dependencies_and_callers: Depends directly on `scripts/humanize.sh` and indirectly on shell builtins plus grep. It does not use `tests/test-helpers.sh`; it defines local `pass`/`fail` counters. It is a sibling of `tests/robustness` and complements `tests/robustness/test-hook-input-robustness.sh`, which invokes `_humanize_monitor_codex` in narrow-terminal and ANSI-log scenarios (`test-hook-input-robustness.sh` lines 444-665).
- edge_cases_or_failure_modes: Covered failures include deleted `.humanize/rlcr`, repeated cleanup calls, cleanup after graceful stop, non-interactive terminal limitations, missing `_restore_terminal` definition, missing scroll reset escape, and missing `_cleanup` call chain. A limitation is that Test 1 redefines simplified `_cleanup` and `_graceful_stop` inside the generated script rather than invoking the nested production functions directly; Tests 4 and 5 compensate with source inspection.
- validation_or_tests: The file is itself the validation command: `bash tests/test-monitor-runtime.sh`. I did not run it in this research pass; static inspection confirmed its expected assertions and production references.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-073 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Prompt/block template that enforces the algorithmic transition away from ad hoc `round-*-todos.md` file access and toward the native TodoWrite tool as the state-tracking surface.
- algorithmic_behavior: The template is a short deny/instruction block: title “Todos File Access Blocked” at line 1, a hard prohibition on creating or accessing `round-*-todos.md` files at line 3, an explicit replacement instruction to use the native TodoWrite tool at line 5, and rationale that native todo tools provide UI-visible state tracking and integration with Claude Code task management at lines 7-8. It is not executable code; it is consumed by the hook/template system as a rendered block message. The template-loader contract supports loading such block files by name and rendering placeholders if present; this file has no placeholders, so it is effectively static content.
- inputs_outputs_state: Input is the attempted operation context from hook/prompt code that selects this block when todo-file access should be blocked. Output is a human/agent-facing denial message. State transition is conceptual but important: task state must remain in the native TodoWrite system rather than being externalized into `round-*-todos.md` files, which preserves UI-visible todo state and prevents hidden markdown todo files from becoming a competing source of truth.
- gates_or_invariants: Invariant: do not create or access files matching `round-*-todos.md`; use native TodoWrite instead. This aligns with hook-level protection in `hooks/loop-bash-validator.sh`, which detects modification attempts matching `round-[0-9]+-todos\.md` (`hooks/loop-bash-validator.sh` line 363), and with command modification detection patterns in `hooks/lib/loop-common.sh` lines 775-800.
- dependencies_and_callers: The block lives under `prompt-template/block/` and is loaded by the template subsystem. `hooks/lib/template-loader.sh` locates the prompt-template directory (`get_template_dir`), loads template files by name (`load_template`), and can safely render fallback/static text (`load_and_render_safe` path implied by loader functions around lines 27-129). It coordinates with hook validators that block protected file reads/writes/bash modifications and then surface block templates as guidance.
- edge_cases_or_failure_modes: Because the template is static and small, rendering failure risk is low. The main failure mode would be a caller not selecting this block for a `round-*-todos.md` access attempt, or a non-file todo workflow bypassing native TodoWrite visibility. There are no variable placeholders, so placeholder injection is not applicable for this file.
- validation_or_tests: Relevant validation is indirect: `tests/robustness/test-hook-input-robustness.sh` validates hook command parsing and modification detection, including redirect and `sed -i` modification patterns (`test-hook-input-robustness.sh` lines 351-370), and the bash validator explicitly checks `round-[0-9]+-todos\.md` modification attempts. I did not execute tests in this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence headings present, one per assigned row, each with cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`