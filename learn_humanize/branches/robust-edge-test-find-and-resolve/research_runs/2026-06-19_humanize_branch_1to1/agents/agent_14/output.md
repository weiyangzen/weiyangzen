# agent_14 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-014 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Specialized Claude subagent prompt used by `gen-plan` as the semantic relevance gate before converting a draft into a repository plan. It is not executable shell/code, but it defines the decision policy for whether a draft is allowed to proceed into plan generation.
- algorithmic_behavior: The frontmatter declares `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-5`. The prompt directs the agent to first inspect repository context, including README/CLAUDE docs and directory structure, then compare draft content against repo concepts, technologies, components, file paths, functions, and features at `agents/draft-relevance-checker.md:14-25`. The output contract is exactly one of `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` at `agents/draft-relevance-checker.md:27-29`.
- inputs_outputs_state: Input is the draft document content plus read-only repository exploration through the allowed tools. Output is a plain-text verdict consumed by the `gen-plan` command. It does not mutate repo state, loop state, git state, or files. It is deliberately semantic rather than syntactic, so the effective decision state is a binary proceed/stop gate for the next `gen-plan` phase.
- gates_or_invariants: The policy is intentionally lenient: drafts that “could reasonably be connected” should pass, informal or multilingual rough notes are acceptable, and uncertainty should lean relevant at `agents/draft-relevance-checker.md:31-36`. The invariant is that unrelated drafts are stopped before plan generation, while borderline drafts should not be rejected merely for rough wording.
- dependencies_and_callers: `commands/gen-plan.md:53-67` instructs Phase 2 to invoke `humanize:draft-relevance-checker` with the haiku model and to stop on `NOT_RELEVANT`. `tests/test-gen-plan.sh:111-155` validates that this agent file exists and has the expected name, model, and tools. `tests/test-gen-plan.sh:396-421` further checks the model value and English-only content policy.
- edge_cases_or_failure_modes: Because the gate is lenient and “if in doubt” passes, false positives are expected for weakly related drafts. The verdict format is unstructured text rather than JSON, so callers must parse prefix strings reliably. Repository docs may be absent, forcing relevance inference from directory/file names. The prompt does not require confidence scores, citations, or exhaustive repo search, so results depend on the subagent’s quick exploration quality.
- validation_or_tests: Static validation exists in `tests/test-gen-plan.sh`: frontmatter presence, exact name `draft-relevance-checker`, model `haiku`, tools declaration, valid model set, and no CJK/emoji content. The task did not require running tests; I inspected the file and its callers read-only.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-044 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable behavioral specification for RLCR plan-file hook safety. It validates that active-loop state parsing, plan-file immutability, branch consistency, Bash bypass protection, Stop-hook integrity checks, and legacy-path handling work together across the hook surface.
- algorithmic_behavior: The test builds an isolated temporary git repo, initializes commits, creates `.humanize/rlcr/2024-01-01_12-00-00`, writes a gitignored `plans/test-plan.md`, copies it to the loop backup `plan.md`, and writes `state.md` with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, and `start_branch` at `tests/test-plan-file-hooks.sh:37-83`. It then mutates state/files/branches and invokes production hooks through stdin JSON, asserting pass or block behavior with `pass`, `fail`, and final `exit $TESTS_FAILED` at `tests/test-plan-file-hooks.sh:26-28` and `tests/test-plan-file-hooks.sh:1057-1065`.
- inputs_outputs_state: Inputs are the repository hook scripts, synthetic Claude hook JSON, `CLAUDE_PROJECT_DIR`, temporary git repos, YAML-like state frontmatter, plan backups, summaries, and goal trackers. Outputs are colored test result lines and a process exit code equal to the failure count. State transitions include initializing repos, committing `.gitignore`, switching branches, modifying/deleting plan files, removing backup files, creating tracked-plan subrepos, committing tracked plan changes, and creating placeholder variants of `goal-tracker.md`.
- gates_or_invariants: UserPromptSubmit validation must pass valid current-loop state and block missing `plan_tracked`, missing `start_branch`, and branch changes at `tests/test-plan-file-hooks.sh:91-183`. Write/Edit validators must block direct writes/edits to loop backup `plan.md` with exit code `2` at `tests/test-plan-file-hooks.sh:194-221`. Bash validation must block backup modification, removal, direct `.humanize/rlcr/plan.md`, command substitution, glob, brace expansion, pipe-to-tee, and backtick bypass attempts at `tests/test-plan-file-hooks.sh:228-334`.
- gates_or_invariants: YAML quote handling is explicitly specified: quoted `plan_file` and quoted `start_branch` must be stripped for comparisons, quoted branch mismatches must still block, and hyphenated plan paths must resolve at `tests/test-plan-file-hooks.sh:341-459`. Stop-hook plan integrity must block modified project plans, deleted project plans, missing backups, tracked uncommitted plan changes, outdated state schema, and tracked committed content drift against the backup at `tests/test-plan-file-hooks.sh:469-729`. Goal-tracker placeholder reporting must be section-specific for Ultimate Goal, Acceptance Criteria, Active Tasks, or all three at `tests/test-plan-file-hooks.sh:737-1009`.
- dependencies_and_callers: Direct dependencies are `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Shared behavior comes from `hooks/lib/loop-common.sh`, especially `find_active_loop`, strict state parsing, path classification under `.humanize/rlcr`, command modification detection, and template rendering. `tests/run-all-tests.sh:41` includes this test in the broader suite. External command dependencies include `bash`, `git`, `grep`, `sed`, `mktemp`, `cp`, `rm`, and hook dependencies such as `jq`.
- dependencies_and_callers: The implementation side matches the spec: `hooks/loop-plan-file-validator.sh:35-70` finds the active loop and parses state strictly, `hooks/loop-plan-file-validator.sh:77-112` blocks branch changes, and `hooks/loop-plan-file-validator.sh:117-226` enforces tracked/gitignored plan-file mode. `hooks/loop-write-validator.sh:118-130` and `hooks/loop-edit-validator.sh:85-96` block backup `plan.md`; `hooks/loop-bash-validator.sh:314-319` blocks commands targeting `.humanize/rlcr(/...)?/plan.md`. Stop-hook backup/content checks are in `hooks/loop-codex-stop-hook.sh:185-257`.
- edge_cases_or_failure_modes: The test accounts for default git branch differences by capturing `DEFAULT_BRANCH` after `git init` at `tests/test-plan-file-hooks.sh:42-48`. It covers double-quoted YAML fields and hyphenated paths, but not single-quoted YAML, spaces in paths, or every possible shell metacharacter bypass. Legacy `.humanize-loop.local` writes are intentionally allowed at `tests/test-plan-file-hooks.sh:1013-1053`, so repositories still using old loop storage would not be protected by these validators. Stop-hook tests must create summary and goal-tracker fixtures to avoid earlier unrelated blocks, which shows gate ordering matters.
- validation_or_tests: This file is itself the validation asset. Assertions require expected hook exit codes and output substrings, including JSON block responses for Stop-hook failures and empty output for successful UserPromptSubmit checks. I did not run the suite because this assignment requested read-only research notes; inspection confirms the test is structured to run production hooks against temporary repositories rather than mocks.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-074 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the `--push-every-round` policy. It converts an ahead-of-remote git state into user-facing instructions that prevent loop exit until local commits are pushed.
- algorithmic_behavior: The template title announces unpushed commits at `prompt-template/block/unpushed-commits.md:1`. It interpolates `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into the block reason at `prompt-template/block/unpushed-commits.md:3`, states that `--push-every-round` requires pushing before exit at `prompt-template/block/unpushed-commits.md:5`, and emits the concrete command `git push origin {{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:7-10`. It ends by allowing retry after all commits are pushed at `prompt-template/block/unpushed-commits.md:12`.
- inputs_outputs_state: Inputs are render variables `AHEAD_COUNT` and `CURRENT_BRANCH`. Output is Markdown used as the `reason` field in a Stop-hook JSON block. The template itself has no state transition, but it participates in the transition from “attempting to stop” to “blocked until push succeeds.”
- gates_or_invariants: The gate only applies when `PUSH_EVERY_ROUND` is true. `hooks/loop-codex-stop-hook.sh:489-512` checks `git status -sb` for `ahead N`, extracts the count and current branch, renders this template, and returns a JSON block with a system message. The invariant is that an RLCR loop configured to push every round cannot exit while local commits remain ahead of the remote.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/unpushed-commits.md"` in `hooks/loop-codex-stop-hook.sh:499-507`. Rendering behavior depends on `hooks/lib/template-loader.sh` via `hooks/lib/loop-common.sh:135-143`. `tests/test-templates-comprehensive.sh:517-527` renders this real template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserts the output contains the title, count, and branch. `tests/test-template-references.sh:83-106` scans hook template references and fails missing templates.
- edge_cases_or_failure_modes: Detection depends on `git status -sb` containing the English phrase `ahead N`; missing upstreams, localized git output, or unusual status formats may bypass this particular check. The template hardcodes `origin`, which may not match a branch’s configured upstream remote. The branch placeholder is inserted into a shell command without shell quoting, so unusual branch names could make the displayed command less robust even though common branch names work.
- validation_or_tests: Covered by template rendering tests in `tests/test-templates-comprehensive.sh:517-527` and existence/reference scanning in `tests/test-template-references.sh`. I inspected the call site and did not execute tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 Item Evidence sections present, one per assigned row, in the same order as the assignment table
- missing_items: none
- duplicate_items: none detected among Item Evidence headings
- final_worker_status: `complete`