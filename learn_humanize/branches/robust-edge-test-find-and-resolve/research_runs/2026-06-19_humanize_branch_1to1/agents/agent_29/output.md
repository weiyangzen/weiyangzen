# agent_29 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-029 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes the RLCR loop state for `/humanize:start-rlcr-loop`. It is the bootstrap script that turns a user-supplied implementation plan into a timestamped `.humanize/rlcr/<timestamp>/` session containing `state.md`, a plan backup, `goal-tracker.md`, and `round-0-prompt.md`. The command wrapper delegates directly to it in `commands/start-rlcr-loop.md:4` and `commands/start-rlcr-loop.md:12-13`.
- algorithmic_behavior: The script is a fail-closed setup pipeline. It parses CLI options in `scripts/setup-rlcr-loop.sh:104-180`, merges `--plan-file` with the positional plan in `scripts/setup-rlcr-loop.sh:188-195`, validates repository and plan prerequisites, then creates initial loop artifacts. Its main phases are: default runtime config in `scripts/setup-rlcr-loop.sh:17-24`; argument parsing and help text in `scripts/setup-rlcr-loop.sh:43-102`; git validation in `scripts/setup-rlcr-loop.sh:211-220`; plan path validation in `scripts/setup-rlcr-loop.sh:227-328`; plan tracking validation in `scripts/setup-rlcr-loop.sh:334-379`; content validation in `scripts/setup-rlcr-loop.sh:385-438`; Codex and YAML-safety validation in `scripts/setup-rlcr-loop.sh:441-484`; state directory creation in `scripts/setup-rlcr-loop.sh:490-499`; state and goal tracker generation in `scripts/setup-rlcr-loop.sh:508-618`; initial prompt generation in `scripts/setup-rlcr-loop.sh:624-684`; and activation output in `scripts/setup-rlcr-loop.sh:702-758`.
- inputs_outputs_state: Inputs are the plan path, optional `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--plan-file`, and `--track-plan-file`; environment input `CLAUDE_PROJECT_DIR` overrides the current directory at `scripts/setup-rlcr-loop.sh:186`; `PATH` must expose `codex` at `scripts/setup-rlcr-loop.sh:441-446`; and git state supplies repo validity, branch, file tracking, and submodule information. Outputs are created under `$PROJECT_ROOT/.humanize/rlcr/<timestamp>`: `plan.md` backup at `scripts/setup-rlcr-loop.sh:498-499`, `state.md` with fields such as `current_round: 0`, model, effort, timeout, plan path, tracking mode, and start branch at `scripts/setup-rlcr-loop.sh:508-520`, `goal-tracker.md` at `scripts/setup-rlcr-loop.sh:527-618`, and `round-0-prompt.md` at `scripts/setup-rlcr-loop.sh:624-684`. The stdout output emits an activation banner, initial prompt, and completion requirements in `scripts/setup-rlcr-loop.sh:702-755`. The initial state transition is from no active session artifact to an active loop session with `current_round: 0`; later transitions are handled by hooks, not this setup script.
- gates_or_invariants: Requires a git repository and at least one commit via timed git calls in `scripts/setup-rlcr-loop.sh:211-220`. The plan path must be relative, contain no whitespace, contain no shell metacharacters, not be a symlink, have existing non-symlink parents, exist, be readable, resolve within the project, and not live in a submodule (`scripts/setup-rlcr-loop.sh:227-328`). Tracking mode is intentionally strict: without `--track-plan-file`, the plan must not be tracked; with `--track-plan-file`, it must be tracked and clean (`scripts/setup-rlcr-loop.sh:354-379`). Content must be at least 5 physical lines and at least 3 nonblank, non-comment content lines (`scripts/setup-rlcr-loop.sh:385-438`). Branch names reject YAML-unsafe characters including colon, hash, quotes, backtick, and newline (`scripts/setup-rlcr-loop.sh:458-466`). Codex model and effort are limited to safe alphanumeric punctuation before being serialized into `state.md` (`scripts/setup-rlcr-loop.sh:468-484`). The script also requires Codex CLI availability before it creates loop artifacts (`scripts/setup-rlcr-loop.sh:441-446`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh`, sourced at `scripts/setup-rlcr-loop.sh:26-28`, whose `run_with_timeout` prefers `gtimeout`, GNU `timeout`, Python, then no timeout fallback (`scripts/portable-timeout.sh:9-71`). Uses `git`, `grep`, `awk`, `sed`, `wc`, `mkdir`, `cp`, `date`, and `codex`. It is called by the slash command definition in `commands/start-rlcr-loop.md:12-13`. The generated state is consumed by stop hooks such as `hooks/loop-codex-stop-hook.sh`, which later reads loop state, summaries, and prompt templates.
- edge_cases_or_failure_modes: Absolute paths, spaces, shell metacharacters, symlinks, parent-directory symlink traversal, missing directories, unreadable files, project escape via `..`, submodule plans, tracked-plan mode mismatches, sparse plans, comment-only plans, unsafe branch names, unsafe model or effort strings, missing Codex, git timeout, and no initial commit all fail early. Content counting intentionally treats lines beginning with `#` as comments rather than Markdown headings, so a plan composed only of headings is considered insufficient (`scripts/setup-rlcr-loop.sh:425-430`). Missing `portable-timeout.sh` would fail immediately because the file is sourced under `set -euo pipefail`. The script creates a new timestamped loop directory every successful run and does not itself check for or lock against an already active RLCR loop. Activation output is protected from downstream broken pipes only after all important files are written via `trap 'exit 0' PIPE` at `scripts/setup-rlcr-loop.sh:698-700`.
- validation_or_tests: `tests/test-plan-file-validation.sh` exercises most setup gates: absolute and nonexistent paths (`tests/test-plan-file-validation.sh:89-126`), spaces and metacharacters (`tests/test-plan-file-validation.sh:128-187`), symlinks and resolution errors (`tests/test-plan-file-validation.sh:189-242`), project escape and non-git/no-commit repos (`tests/test-plan-file-validation.sh:245-323`), tracking-mode behavior (`tests/test-plan-file-validation.sh:330-426`), branch-name safety (`tests/test-plan-file-validation.sh:432-513`), content validation (`tests/test-plan-file-validation.sh:518-661`), CLI option conflicts (`tests/test-plan-file-validation.sh:667-694`), and model/effort validation (`tests/test-plan-file-validation.sh:700-738`). Additional robustness coverage exists in `tests/robustness/test-path-validation-robustness.sh:55-89` and `tests/robustness/test-plan-file-robustness.sh:22-82`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-059 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Defines the blocking prompt shown when an RLCR participant tries to stop while the git worktree has uncommitted changes. It is a control-gate template rather than executable code: its content becomes the hook’s `reason` field and tells the agent that exit is blocked until changes are reviewed, staged, and committed.
- algorithmic_behavior: The template has two variable inputs, `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, at `prompt-template/block/git-not-clean.md:3-4`. It then prescribes required actions: optionally run `code-simplifier`, review untracked files and ignore build artifacts, stage real changes, and commit with a descriptive message (`prompt-template/block/git-not-clean.md:5-9`). It enforces commit-message hygiene by forbidding AI authorship attribution and similar co-author trailers (`prompt-template/block/git-not-clean.md:11-15`). The closing instruction says exit may be attempted again only after committing (`prompt-template/block/git-not-clean.md:16`).
- inputs_outputs_state: Inputs are rendered by `hooks/loop-codex-stop-hook.sh`: `GIT_ISSUES` is set to `uncommitted changes` when cached porcelain status is nonempty (`hooks/loop-codex-stop-hook.sh:437-440`), and `SPECIAL_NOTES` is assembled from untracked `.humanize*` and other-untracked-file notes (`hooks/loop-codex-stop-hook.sh:441-461`). Output is a rendered Markdown reason loaded via `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:473-475`, then emitted in a JSON block decision at `hooks/loop-codex-stop-hook.sh:477-485`. The state transition is deliberately negative: the hook stops before Codex review and keeps the loop active until the worktree is committed.
- gates_or_invariants: The governing invariant is that RLCR stop attempts cannot proceed through review while the repo has staged, unstaged, or untracked changes. The hook performs this quick check before expensive Codex review (`hooks/loop-codex-stop-hook.sh:426-430`). The template reinforces two commit hygiene rules: commit messages must follow project conventions, and AI tools must not be listed as authors (`prompt-template/block/git-not-clean.md:11-14`). A `.humanize*` local-artifact note can be included, but it does not bypass the block.
- dependencies_and_callers: Called only through the template loader path in `hooks/loop-codex-stop-hook.sh:473-475`. Related note templates are loaded at `hooks/loop-codex-stop-hook.sh:446-460`. Rendering depends on `hooks/lib/template-loader.sh`, which documents placeholder syntax and single-pass substitution at `hooks/lib/template-loader.sh:7-13` and implements non-recursive rendering at `hooks/lib/template-loader.sh:50-131`. If the template is missing or empty, the hook falls back to a shorter inline `# Git Not Clean` message at `hooks/loop-codex-stop-hook.sh:467-472`.
- edge_cases_or_failure_modes: Any nonempty porcelain status blocks, including intentionally local `.humanize/` files; the special note explains that case but does not automatically clean or ignore it. The template tells the agent to use `git add -A` only when all files should be tracked, which is important because RLCR loop artifacts should usually stay local. If template rendering failed, the fallback message would still block but would omit several stronger instructions, including the code-simplifier step and AI-authorship ban. Since `{{SPECIAL_NOTES}}` is inserted directly as prose, malformed or unexpectedly long note content can make the block noisy, but the renderer avoids recursive placeholder expansion.
- validation_or_tests: I found no test that asserts this exact template text. Its operational gate is covered by the stop hook path that checks git status and emits a block decision (`hooks/loop-codex-stop-hook.sh:432-486`). Template rendering behavior is covered generically by `tests/test-template-loader.sh`, including placeholder-in-value injection prevention in `tests/test-template-loader.sh:525-540`. Comprehensive template tests cover other real templates, but not this exact file in the inspected ranges.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-089 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Defines the regular Codex review contract for non-full-alignment RLCR rounds. It is an algorithmic control prompt: Codex must read the original plan, compare Claude’s claimed work against the plan, prompt file, goal tracker, and docs, then decide whether to write review feedback or end with the exact `COMPLETE` marker.
- algorithmic_behavior: The template first anchors Codex to the original plan at `@{{PLAN_FILE}}` and requires reading it before review (`prompt-template/codex/regular-review.md:3-9`). It provides Claude’s summary via `{{SUMMARY_CONTENT}}` (`prompt-template/codex/regular-review.md:13-18`). Part 1 demands skeptical implementation review and gap finding, with explicit rules that deferrals and pending work must be turned into concrete implementation instructions (`prompt-template/codex/regular-review.md:20-31`). Part 2 mandates goal alignment against `@{{GOAL_TRACKER_FILE}}`, checking acceptance criteria, forgotten work, deferrals, and plan evolution (`prompt-template/codex/regular-review.md:33-45`). Part 3 is a dynamic injected section from `{{GOAL_TRACKER_UPDATE_SECTION}}` (`prompt-template/codex/regular-review.md:47`). Part 4 defines output semantics: write findings to `@{{REVIEW_RESULT_FILE}}` when reality diverges or work remains, and only put `COMPLETE` on the last line when all original plan tasks and ACs are fully done with no deferrals or pending work (`prompt-template/codex/regular-review.md:49-57`).
- inputs_outputs_state: Inputs are rendered in the stop hook when the current round is not a full-alignment checkpoint. The hook passes `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, loop history values, and `REVIEW_RESULT_FILE` into this template at `hooks/loop-codex-stop-hook.sh:739-751`. Output is `round-N-review-prompt.md`, then Codex is run with that prompt at `hooks/loop-codex-stop-hook.sh:818-845`. Codex should create `round-N-review-result.md`; if it writes to stdout instead, the hook copies stdout into the review result file (`hooks/loop-codex-stop-hook.sh:902-918`). State transitions depend on the last nonempty review line: exact `COMPLETE` moves from `state.md` to `finalize-state.md` unless max iterations has already been reached (`hooks/loop-codex-stop-hook.sh:960-985`); exact `STOP` terminates via the stop path (`hooks/loop-codex-stop-hook.sh:1028-1055`); any other review content increments `current_round` and creates the next round prompt (`hooks/loop-codex-stop-hook.sh:1062-1083`).
- gates_or_invariants: The strict completion invariant is encoded in the template: no deferrals, no unfinished items, all original plan tasks complete, and all ACs met before `COMPLETE` may be the last line (`prompt-template/codex/regular-review.md:53-57`). The hook enforces marker strictness by only considering the last nonempty line and requiring exact marker equality after trimming whitespace (`hooks/loop-codex-stop-hook.sh:960-967`). Regular review is skipped on full-alignment rounds, where `CURRENT_ROUND % 5 == 4` selects `full-alignment-review.md` instead (`hooks/loop-codex-stop-hook.sh:686-690` and `hooks/loop-codex-stop-hook.sh:721-752`). The hook also validates Codex execution before marker interpretation: nonzero exit, missing review file, or empty review file all block with debug details (`hooks/loop-codex-stop-hook.sh:883-955`).
- dependencies_and_callers: Consumed by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe`. It depends on `hooks/lib/template-loader.sh` for safe placeholder rendering, including single-pass protection against prompt corruption when summary or review content contains `{{...}}` syntax (`hooks/lib/template-loader.sh:54-57`). It is coordinated with `prompt-template/codex/goal-tracker-update-section.md`, the generated `goal-tracker.md` from `scripts/setup-rlcr-loop.sh`, the prior round prompt file, and the Codex CLI invocation. The downstream non-complete path feeds the review content into `prompt-template/claude/next-round-prompt.md` at `hooks/loop-codex-stop-hook.sh:1071-1083`.
- edge_cases_or_failure_modes: If Codex writes `CANNOT COMPLETE` or mentions `COMPLETE` anywhere except as the exact final nonempty line, the hook will not complete the loop. If Codex outputs `COMPLETE` at max iterations, finalize is skipped and the loop ends as max-iteration state (`hooks/loop-codex-stop-hook.sh:967-973`). If this template is missing, the fallback regular-review prompt at `hooks/loop-codex-stop-hook.sh:710-719` is much less strict and omits most deferral and goal-alignment wording. Summary content is embedded verbatim between comments, so very large or malformed summaries can make the prompt large, but the renderer avoids recursive placeholder injection. The template’s instruction to write to `@{{REVIEW_RESULT_FILE}}` is backed by stdout fallback, but permission or disk failures copying stdout still block (`hooks/loop-codex-stop-hook.sh:904-916`).
- validation_or_tests: Finalize transition behavior from a `COMPLETE` review is tested in `tests/test-finalize-phase.sh:451-481`. The max-iteration edge case is tested in `tests/test-finalize-phase.sh:483-500`. Normal non-`COMPLETE` feedback preserving `state.md` and continuing the loop is tested in `tests/test-finalize-phase.sh:552-590`. Generic template safety for placeholder injection is tested in `tests/test-template-loader.sh:525-540`. I found no direct assertion over the exact full text of `prompt-template/codex/regular-review.md`, but its rendered role is exercised through the stop-hook review flow.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item evidence sections present, each assigned item heading appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`