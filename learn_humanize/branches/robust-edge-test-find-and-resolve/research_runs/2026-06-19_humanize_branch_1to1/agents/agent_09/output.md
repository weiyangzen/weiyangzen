# agent_09 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer for RLCR loop hooks. It centralizes hook JSON validation, active-loop discovery, state parsing, protected-file classification, block-message rendering, cancel authorization, and shell-command pattern detection. The directory contains `hooks/lib/template-loader.sh` and `hooks/lib/loop-common.sh`.
- algorithmic_behavior: `template-loader.sh` resolves the prompt-template root from the hook library path, loads template files, and renders `{{VAR}}` placeholders in a single pass using `TMPL_VAR_` environment entries and an `awk` scanner; unresolved placeholders are preserved and variable values are not rescanned, which is explicitly meant to prevent placeholder injection. See `hooks/lib/template-loader.sh:26`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:74`, `hooks/lib/template-loader.sh:116`, and `hooks/lib/template-loader.sh:120`. `loop-common.sh` supplies the higher-level hook algorithms: `validate_hook_input` rejects null bytes, invalid UTF-8 when `iconv` exists, invalid JSON, and missing `tool_name`; `is_deeply_nested` gates JSON depth; `find_active_loop` selects only the newest loop directory that has `state.md` or `finalize-state.md`; `parse_state_file` parses tolerant frontmatter defaults; and `parse_state_file_strict` fails closed on malformed state. See `hooks/lib/loop-common.sh:55`, `hooks/lib/loop-common.sh:115`, `hooks/lib/loop-common.sh:152`, `hooks/lib/loop-common.sh:202`, and `hooks/lib/loop-common.sh:240`.
- inputs_outputs_state: Inputs are hook JSON from parent hooks, `.humanize/rlcr/*/state.md` or `finalize-state.md`, path strings, shell command strings, and prompt template files. Outputs are shell return codes, parsed global variables such as `VALIDATED_TOOL_NAME`, `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, rendered block messages, and boolean shell statuses. State transitions are mostly indirect: `end_loop` renames `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` at `hooks/lib/loop-common.sh:823`. `is_cancel_authorized` permits the controlled transition from `state.md` or `finalize-state.md` to `cancel-state.md` only when `.cancel-requested` exists and the command shape is exact; see `hooks/lib/loop-common.sh:455`, `hooks/lib/loop-common.sh:461`, `hooks/lib/loop-common.sh:500`, `hooks/lib/loop-common.sh:585`, and `hooks/lib/loop-common.sh:600`.
- gates_or_invariants: JSON hook inputs must include `tool_name`; deeply nested JSON over depth 30 is blocked by parent hooks through `is_deeply_nested`. State parsing in validators uses strict mode and rejects missing frontmatter separators, missing required `current_round` or `max_iterations`, and non-numeric values with distinct return codes at `hooks/lib/loop-common.sh:248`, `hooks/lib/loop-common.sh:270`, `hooks/lib/loop-common.sh:280`, and `hooks/lib/loop-common.sh:286`. Round-file identification is constrained to `round-[0-9]+-(summary|prompt|todos).md` through `is_round_file_type` and `extract_round_number` at `hooks/lib/loop-common.sh:305` and `hooks/lib/loop-common.sh:315`. Prompt writes, state writes, finalize-state writes, todos access, summary bash writes, goal-tracker edits after round 0, plan backup writes, and `.humanize` git-add attempts all route through shared block-message functions and predicates. See `hooks/lib/loop-common.sh:347`, `hooks/lib/loop-common.sh:358`, `hooks/lib/loop-common.sh:367`, `hooks/lib/loop-common.sh:376`, `hooks/lib/loop-common.sh:385`, `hooks/lib/loop-common.sh:396`, `hooks/lib/loop-common.sh:407`, `hooks/lib/loop-common.sh:413`, `hooks/lib/loop-common.sh:419`, `hooks/lib/loop-common.sh:644`, and `hooks/lib/loop-common.sh:775`.
- dependencies_and_callers: `loop-common.sh` sources `template-loader.sh` and initializes `TEMPLATE_DIR`; if template validation fails it warns and uses inline fallbacks instead of failing immediately at `hooks/lib/loop-common.sh:135` and `hooks/lib/loop-common.sh:142`. Parent callers include `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The read validator uses shared JSON/state/round helpers to block todos, wrong locations, wrong rounds, and wrong active-loop paths; see `hooks/loop-read-validator.sh:25`, `hooks/loop-read-validator.sh:52`, `hooks/loop-read-validator.sh:92`, `hooks/loop-read-validator.sh:119`, and `hooks/loop-read-validator.sh:135`. The write validator uses the same layer to block prompt/state/todos/plan backup/goal tracker/summary misuse; see `hooks/loop-write-validator.sh:26`, `hooks/loop-write-validator.sh:53`, `hooks/loop-write-validator.sh:63`, `hooks/loop-write-validator.sh:113`, `hooks/loop-write-validator.sh:124`, `hooks/loop-write-validator.sh:151`, and `hooks/loop-write-validator.sh:164`. The bash validator depends heavily on `git_adds_humanize`, `command_modifies_file`, and `is_cancel_authorized`; see `hooks/loop-bash-validator.sh:100`, `hooks/loop-bash-validator.sh:117`, `hooks/loop-bash-validator.sh:127`, `hooks/loop-bash-validator.sh:267`, `hooks/loop-bash-validator.sh:314`, `hooks/loop-bash-validator.sh:327`, and `hooks/loop-bash-validator.sh:343`.
- edge_cases_or_failure_modes: `load_template` returns empty content and warns when a template is missing, while `load_and_render_safe` falls back if loading or rendering produces an empty result; see `hooks/lib/template-loader.sh:41`, `hooks/lib/template-loader.sh:177`, and `hooks/lib/template-loader.sh:192`. `validate_hook_input` skips UTF-8 validation if `iconv` is unavailable, then relies on `jq` for JSON syntax. `find_active_loop` intentionally ignores older loop directories even if they contain state to avoid reviving stale sessions. `is_cancel_authorized` guards against command substitution, backticks, newlines, shell operators, unresolved `$`, mixed quote styles, extra args, wrong source/destination, multiple trailing spaces, and symlinked state sources. `git_adds_humanize` lowercases input and accounts for `-A` becoming `-a`, `git -C ... add`, chained commands, direct `.humanize`, force-add broad scopes, and broad add only when `.humanize` exists and is not ignored.
- validation_or_tests: `tests/test-template-loader.sh`, `tests/robustness/test-template-stress-robustness.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh` exercise template loading/rendering and fallback behavior. `tests/robustness/test-state-file-robustness.sh`, `tests/robustness/test-session-robustness.sh`, `tests/robustness/test-cancel-security-robustness.sh`, `tests/robustness/test-hook-input-robustness.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-humanize-escape.sh`, and `tests/test-allowlist-validators.sh` cover the shared validators and predicates. `tests/run-all-tests.sh:34` includes `test-template-loader.sh`, and `tests/run-all-tests.sh:53` through `tests/run-all-tests.sh:62` include the robustness suite.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-039 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `gen-plan` command’s structural contract, frontmatter metadata, agent metadata, version synchronization, invalid-fixture rejection, English-only content gate, and `validate-gen-plan-io.sh` exit-code behavior.
- algorithmic_behavior: The script is a Bash test runner with `set -euo pipefail`, project path discovery, colored `pass`/`fail` counters, and a final exit code based on failures. It runs positive tests PT-1 through PT-9 for expected repository artifacts and negative tests NT-1, NT-2, NT-3, NT-6, plus content validation and IO-validator checks. See `tests/test-gen-plan.sh:9`, `tests/test-gen-plan.sh:11`, `tests/test-gen-plan.sh:22`, `tests/test-gen-plan.sh:26`, `tests/test-gen-plan.sh:31`, `tests/test-gen-plan.sh:47`, `tests/test-gen-plan.sh:182`, `tests/test-gen-plan.sh:425`, and `tests/test-gen-plan.sh:554`.
- inputs_outputs_state: Inputs are `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. It creates temporary invalid fixtures with `mktemp -d` and here-docs, cleaned by `trap`, then emits PASS/FAIL lines and exits `0` only if `TESTS_FAILED` remains zero. See `tests/test-gen-plan.sh:59`, `tests/test-gen-plan.sh:111`, `tests/test-gen-plan.sh:164`, `tests/test-gen-plan.sh:191`, `tests/test-gen-plan.sh:193`, `tests/test-gen-plan.sh:431`, and `tests/test-gen-plan.sh:554`. The referenced command contract declares `description`, `argument-hint`, `allowed-tools`, validation phases, relevance-check agent invocation, and plan output rules in `commands/gen-plan.md:1`, `commands/gen-plan.md:29`, `commands/gen-plan.md:48`, `commands/gen-plan.md:72`, `commands/gen-plan.md:107`, and `commands/gen-plan.md:120`.
- gates_or_invariants: Command metadata must include a non-empty `description`, `allowed-tools`, and `argument-hint`; the relevance agent must exist, be named `draft-relevance-checker`, use `model: haiku`, and declare tools. See `tests/test-gen-plan.sh:71`, `tests/test-gen-plan.sh:85`, `tests/test-gen-plan.sh:98`, `tests/test-gen-plan.sh:111`, `tests/test-gen-plan.sh:123`, `tests/test-gen-plan.sh:137`, and `tests/test-gen-plan.sh:151`. Version strings must match across plugin JSON, marketplace JSON, and README at `tests/test-gen-plan.sh:168` through `tests/test-gen-plan.sh:176`. Names must match `^[a-z][a-z0-9-]*$`; command/agent frontmatter must include required fields; YAML syntax is checked by a simple line-pattern validator; model names must be one of `opus|sonnet|haiku` or start with `claude-`, `gpt-`, `o[0-9]`, or `gemini-`; see `tests/test-gen-plan.sh:196`, `tests/test-gen-plan.sh:201`, `tests/test-gen-plan.sh:305`, and `tests/test-gen-plan.sh:352`. Content must not contain CJK or emoji by `grep -P` where supported at `tests/test-gen-plan.sh:409` and `tests/test-gen-plan.sh:417`.
- dependencies_and_callers: It depends on `scripts/validate-gen-plan-io.sh`, which parses `--input` and `--output`, resolves paths with `realpath -m`, rejects nonexistent or empty inputs, missing output directories, existing output paths, directory output paths, and non-writable output directories with documented exit codes. See `scripts/validate-gen-plan-io.sh:15`, `scripts/validate-gen-plan-io.sh:29`, `scripts/validate-gen-plan-io.sh:68`, `scripts/validate-gen-plan-io.sh:78`, `scripts/validate-gen-plan-io.sh:86`, `scripts/validate-gen-plan-io.sh:94`, `scripts/validate-gen-plan-io.sh:102`, `scripts/validate-gen-plan-io.sh:117`, and `scripts/validate-gen-plan-io.sh:125`. The aggregate runner includes this script at `tests/run-all-tests.sh:51`.
- edge_cases_or_failure_modes: Negative fixtures intentionally validate uppercase names, names with spaces, missing `description`, absent frontmatter, malformed YAML missing a colon, invalid or empty model names, and partial short-model aliases such as `opus-v2`, `haiku123`, and `sonnet-fast`. See `tests/test-gen-plan.sh:220`, `tests/test-gen-plan.sh:228`, `tests/test-gen-plan.sh:253`, `tests/test-gen-plan.sh:268`, `tests/test-gen-plan.sh:321`, `tests/test-gen-plan.sh:360`, `tests/test-gen-plan.sh:366`, and `tests/test-gen-plan.sh:372`. IO-validator edge cases assert exit `6` for missing option values, flag-as-value, unknown option, and help; exit `1` for missing input; exit `2` for empty input; exit `3` for missing output directory; exit `4` for existing output or directory output; and exit `0` for valid paths. See `tests/test-gen-plan.sh:438` through `tests/test-gen-plan.sh:538`.
- validation_or_tests: This file is itself the validation asset. It checks command metadata against `commands/gen-plan.md`, whose phase 1 maps validator exit codes to command behavior at `commands/gen-plan.md:37` through `commands/gen-plan.md:44`, and checks the relevance agent against `agents/draft-relevance-checker.md:1` through `agents/draft-relevance-checker.md:5`. It also confirms the current version family, observed as `1.3.2` in `README.md:3`, `.claude-plugin/plugin.json:4`, and `.claude-plugin/marketplace.json:11`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-069 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that `round-*-prompt.md` files are instruction inputs, not writable work products. It defines the user-facing rejection text for prompt-file write/edit/bash modification attempts.
- algorithmic_behavior: The template is static Markdown. It does not contain variable placeholders or branching. When loaded through `prompt_write_blocked_message`, it replaces the inline fallback message and tells the agent to read the current prompt, execute the described tasks, write results to the summary file, and document prompt errors in the summary rather than editing the prompt. See `prompt-template/block/prompt-file-write.md:1`, `prompt-template/block/prompt-file-write.md:3`, `prompt-template/block/prompt-file-write.md:5`, `prompt-template/block/prompt-file-write.md:7`, and `prompt-template/block/prompt-file-write.md:12`.
- inputs_outputs_state: Input is a blocked write/edit/bash operation targeting a `round-*-prompt.md` path. Output is the rendered Markdown message on stderr from the hook. No repository state is mutated by this template. The shared loader call is `prompt_write_blocked_message`, which invokes `load_and_render_safe "$TEMPLATE_DIR" "block/prompt-file-write.md"` with an inline fallback at `hooks/lib/loop-common.sh:358` through `hooks/lib/loop-common.sh:364`.
- gates_or_invariants: The invariant is that prompt files are generated instructions from Codex to Claude and cannot be modified by the worker. `hooks/loop-write-validator.sh` blocks `Write` to prompt files at `hooks/loop-write-validator.sh:63` through `hooks/loop-write-validator.sh:65`; `hooks/loop-edit-validator.sh` blocks `Edit` at `hooks/loop-edit-validator.sh:46` through `hooks/loop-edit-validator.sh:48`; and `hooks/loop-bash-validator.sh` blocks shell modifications matching `round-[0-9]+-prompt.md` at `hooks/loop-bash-validator.sh:343` through `hooks/loop-bash-validator.sh:345`.
- dependencies_and_callers: Depends on the template loader in `hooks/lib/template-loader.sh` and the shared block-message wrapper in `hooks/lib/loop-common.sh`. `tests/test-template-references.sh` explicitly lists this template among references expected from `loop-common.sh`; the search result showed `tests/test-template-references.sh:154` checking `block/prompt-file-write.md`.
- edge_cases_or_failure_modes: If the file is missing or renders empty, `load_and_render_safe` falls back to the shorter inline message from `hooks/lib/loop-common.sh:360` through `hooks/lib/loop-common.sh:362`. Since this template has no placeholders, placeholder injection and missing-variable behavior are not relevant here. Misclassification risk is low: the file is not executable code, but it is part of the hook enforcement contract for algorithmic state boundaries.
- validation_or_tests: Template existence and reference validity are covered by template reference/comprehensive tests, especially `tests/test-template-references.sh`, and the behavior is indirectly covered by write/edit/bash validator tests that exercise prompt-file blocking. The core validation path is through `prompt_write_blocked_message` and the three hook callers above.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-099 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the portable timeout abstraction used by setup and loop hooks to bound potentially hanging git/Codex commands. It validates implementation detection, timeout semantics, exit-code preservation, argument safety, large output handling, rapid reuse, and fallback-chain consistency.
- algorithmic_behavior: The script sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh`, then runs 18 tests and delegates final status to `print_test_summary`. See `tests/robustness/test-timeout-robustness.sh:12`, `tests/robustness/test-timeout-robustness.sh:16`, `tests/robustness/test-timeout-robustness.sh:17`, and `tests/robustness/test-timeout-robustness.sh:278`. The implementation under test detects `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` in that order at `scripts/portable-timeout.sh:10` through `scripts/portable-timeout.sh:27`, sets `TIMEOUT_IMPL` at `scripts/portable-timeout.sh:29`, and runs commands through the chosen backend at `scripts/portable-timeout.sh:33` through `scripts/portable-timeout.sh:70`.
- inputs_outputs_state: Inputs are a timeout duration and command/args passed to `run_with_timeout`. Outputs are command stdout/stderr and exit status. Timeout backends should return `124` when a command times out; successful commands should preserve their own exit code. `TIMEOUT_IMPL` is exported for sourcing scripts and possible subshell visibility at `scripts/portable-timeout.sh:73` through `scripts/portable-timeout.sh:76`. The test checks quick output, sleep timeouts, formatted args, explicit `exit 42`, pipeline via `sh -c`, large stdout count, nonexistent command status, empty command behavior, special characters, signal handling, and subshell commands.
- gates_or_invariants: `TIMEOUT_IMPL` must be non-empty and one of `gtimeout|timeout|python3|python|none`; see `tests/robustness/test-timeout-robustness.sh:31` through `tests/robustness/test-timeout-robustness.sh:49`. A quick command must complete with exit `0` and expected output at `tests/robustness/test-timeout-robustness.sh:51` through `tests/robustness/test-timeout-robustness.sh:60`. Timed-out sleep must return `124` when an implementation exists at `tests/robustness/test-timeout-robustness.sh:62` through `tests/robustness/test-timeout-robustness.sh:77`. Exit-code preservation is asserted at `tests/robustness/test-timeout-robustness.sh:89` through `tests/robustness/test-timeout-robustness.sh:100`. Very short timeout must finish in under 5 seconds with exit `124` at `tests/robustness/test-timeout-robustness.sh:120` through `tests/robustness/test-timeout-robustness.sh:134`.
- dependencies_and_callers: Depends directly on `scripts/portable-timeout.sh`. Production callers include `scripts/setup-rlcr-loop.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`, which use `run_with_timeout` around git state checks and Codex execution. Search evidence showed calls in `scripts/setup-rlcr-loop.sh:212`, `scripts/setup-rlcr-loop.sh:218`, `scripts/setup-rlcr-loop.sh:318`, `scripts/setup-rlcr-loop.sh:336`, `scripts/setup-rlcr-loop.sh:346`, `scripts/setup-rlcr-loop.sh:452`, `hooks/loop-plan-file-validator.sh:96`, `hooks/loop-plan-file-validator.sh:127`, `hooks/loop-plan-file-validator.sh:152`, `hooks/loop-plan-file-validator.sh:197`, and `hooks/loop-codex-stop-hook.sh:153`, `hooks/loop-codex-stop-hook.sh:219`, `hooks/loop-codex-stop-hook.sh:322`, `hooks/loop-codex-stop-hook.sh:326`, `hooks/loop-codex-stop-hook.sh:494`, `hooks/loop-codex-stop-hook.sh:497`, and `hooks/loop-codex-stop-hook.sh:844`.
- edge_cases_or_failure_modes: If no timeout implementation exists, the wrapper warns and runs commands without bounding them; timeout-specific tests skip the strict `124` checks when `TIMEOUT_IMPL=none`, but still assert the detection value is valid. The Python fallback uses `subprocess.run(sys.argv[1:], timeout=$timeout_secs)` and exits `124` on `TimeoutExpired`, but it does not capture output itself, so child stdout flows normally. Zero timeout is explicitly allowed to either succeed or timeout immediately at `tests/robustness/test-timeout-robustness.sh:139` through `tests/robustness/test-timeout-robustness.sh:152`. Empty command is treated as handled regardless of exit status at `tests/robustness/test-timeout-robustness.sh:201` through `tests/robustness/test-timeout-robustness.sh:209`. Signal-handling commands may return either `124` or `0` at `tests/robustness/test-timeout-robustness.sh:221` through `tests/robustness/test-timeout-robustness.sh:233`.
- validation_or_tests: This file is included in the aggregate suite at `tests/run-all-tests.sh:62`. It validates the underlying timeout helper and the helper’s exported `TIMEOUT_IMPL` behavior at `tests/robustness/test-timeout-robustness.sh:259` through `tests/robustness/test-timeout-robustness.sh:272`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 of 4 assigned item IDs appear once as `## Item Evidence` entry headings.
- missing_items: none
- duplicate_items: none in item evidence headings
- final_worker_status: `complete`