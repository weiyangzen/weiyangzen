# agent_04 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: `hooks` is the runtime guard layer for the Humanize RLCR loop. `hooks/hooks.json:4` wires Claude Code lifecycle events to validators: `UserPromptSubmit` to `loop-plan-file-validator.sh`, `PreToolUse` for `Write`, `Edit`, `Read`, and `Bash`, and `Stop` to `loop-codex-stop-hook.sh` with a 7200s timeout at `hooks/hooks.json:52`.
- algorithmic_behavior: The directory implements a state-machine guard around `.humanize/rlcr/<timestamp>/`. `hooks/lib/loop-common.sh` provides shared primitives for hook input validation, loop discovery, state parsing, path classification, cancel authorization, `.humanize` git-add detection, shell command write detection, and loop termination. `find_active_loop` chooses only the newest session directory containing `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:152`; `parse_state_file_strict` validates YAML frontmatter and numeric `current_round`/`max_iterations` at `hooks/lib/loop-common.sh:240`; `end_loop` renames active state to `<reason>-state.md` at `hooks/lib/loop-common.sh:823`. The Stop hook is the coordinator: it checks plan integrity, todos, git cleanliness, summary presence, Round 0 goal tracker initialization, max iterations, Codex review, finalize phase, and next-round prompt generation in `hooks/loop-codex-stop-hook.sh`.
- inputs_outputs_state: Inputs are Claude hook JSON on stdin, tool names/inputs, transcript JSONL paths, git state, plan files, `.humanize/rlcr` state files, summaries, goal tracker files, templates, and Codex CLI output. Outputs are either exit codes for PreToolUse hooks, JSON block decisions for Stop/UserPromptSubmit hooks, rendered block messages from `prompt-template`, and loop artifacts such as `round-N-review-prompt.md`, `round-N-review-result.md`, `round-N-prompt.md`, `finalize-state.md`, and terminal state files. `check-todos-from-transcript.py` reads `transcript_path` from JSON stdin and returns exit `1` with an `INCOMPLETE_TODOS` marker if the latest TodoWrite contains non-`completed` todos at `hooks/check-todos-from-transcript.py:90`.
- gates_or_invariants: Core invariants include valid UTF-8/JSON input for Read/Write/Bash via `validate_hook_input` at `hooks/lib/loop-common.sh:55`, JSON depth limit via `is_deeply_nested` at `hooks/lib/loop-common.sh:115`, strict active-state parsing, state file immutability through Write/Edit/Bash validators, no prompt-file writes, todos file access routed to TodoWrite, goal-tracker mutable only in Round 0, plan backup protected, no git push unless `push_every_round` is true, and no `.humanize` git-add leakage via `git_adds_humanize` at `hooks/lib/loop-common.sh:644`. Cancel is a special authorized transition only if `.cancel-requested` exists and the command exactly renames active `state.md` or `finalize-state.md` to `cancel-state.md`, rejecting substitution, shell chaining, mixed quote styles, hidden variables, extra args, and symlink sources at `hooks/lib/loop-common.sh:455`.
- dependencies_and_callers: `hooks/hooks.json` is the external hook manifest. Most shell validators source `hooks/lib/loop-common.sh`; that library sources `hooks/lib/template-loader.sh` at `hooks/lib/loop-common.sh:136`. `loop-plan-file-validator.sh` and `loop-codex-stop-hook.sh` source `scripts/portable-timeout.sh` for bounded git/Codex operations at `hooks/loop-plan-file-validator.sh:21` and `hooks/loop-codex-stop-hook.sh:52`. `loop-codex-stop-hook.sh` calls `hooks/check-todos-from-transcript.py` at `hooks/loop-codex-stop-hook.sh:261`. The stop hook depends on Codex CLI and templates under `prompt-template/block`, `prompt-template/claude`, and `prompt-template/codex`.
- edge_cases_or_failure_modes: `loop-edit-validator.sh` is weaker than Read/Write/Bash because it parses with direct `jq` at `hooks/loop-edit-validator.sh:22` and does not call `validate_hook_input` or `is_deeply_nested`, so malformed/deep hook JSON is not covered symmetrically. `is_state_file_path` matches `finalize-state.md`, so several validators explicitly check finalize state first, e.g. `hooks/loop-write-validator.sh:122`. Bash command parsing is intentionally complex and strips leading redirections before matching `mv`/`cp` sources, reducing bypasses such as `2>/tmp/x mv state.md /tmp/foo` at `hooks/loop-bash-validator.sh:147`. Stop-hook malformed state behavior differs by phase: Stop allows exit for safety on malformed state at `hooks/loop-codex-stop-hook.sh:89`, while PreToolUse validators fail closed.
- validation_or_tests: The directory is covered by many test suites under `tests/`, including command-modification patterns, cancel-signal handling, hook-input robustness, state/session robustness, template references, finalize phase, and the assigned ANSI/path tests. `tests/run-all-tests.sh:32` enumerates the suite order and includes robustness tests at `tests/run-all-tests.sh:52`. The assigned `tests/test-ansi-parsing.sh` validates the runner’s output aggregation logic, while `tests/robustness/test-path-validation-robustness.sh` validates setup-time plan path/content gates that feed the same RLCR state lifecycle.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-034 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: This is an executable specification for portable ANSI escape stripping and pass/fail count extraction in the aggregate test runner. It validates the exact sed and grep pipeline used by `tests/run-all-tests.sh:111`.
- algorithmic_behavior: The test constructs colored and plain output strings, strips SGR color sequences with `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"` at `tests/test-ansi-parsing.sh:46`, then extracts numeric counts using `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed` followed by the trailing number. It tests basic color removal, multiple colors, pass extraction, fail extraction, zero values, multiline suite summaries, plain text, and bold-plus-color sequences across `tests/test-ansi-parsing.sh:41` through `tests/test-ansi-parsing.sh:158`.
- inputs_outputs_state: Inputs are local shell literals containing ANSI SGR sequences such as `\033[0;32m`, `\033[0;31m`, and `\033[1;32m`. Outputs are colored PASS/FAIL lines and a final summary with `Passed:` and `Failed:` counters at `tests/test-ansi-parsing.sh:163`. State is limited to `TESTS_PASSED` and `TESTS_FAILED` counters initialized at `tests/test-ansi-parsing.sh:18`.
- gates_or_invariants: The invariant is that test suite aggregation must be independent of colorized output and work on both GNU/Linux and BSD/macOS sed. The test intentionally uses ANSI-C quoting rather than GNU-specific sed syntax, noted at `tests/test-ansi-parsing.sh:38`. The count extractor must choose the last numeric summary in multiline output with `tail -1` at `tests/test-ansi-parsing.sh:124`.
- dependencies_and_callers: It is called by `tests/run-all-tests.sh` as one of the ordered suites at `tests/run-all-tests.sh:43`. Its parsing logic mirrors the runner’s production aggregation code at `tests/run-all-tests.sh:111`, where each suite’s captured output is stripped, counted, and accumulated into `TOTAL_PASSED`/`TOTAL_FAILED`.
- edge_cases_or_failure_modes: It only strips ANSI SGR color codes ending in `m`; non-SGR control sequences such as cursor movement would remain. It expects summary labels exactly matching `Passed:` and `Failed:` with ASCII digits. It also relies on Bash ANSI-C quoting, so despite the portability target being GNU/BSD sed, the script itself must run under Bash.
- validation_or_tests: The file is self-validating: it exits `0` when `TESTS_FAILED` is zero at `tests/test-ansi-parsing.sh:170`, otherwise exits `1`. The aggregate runner treats nonzero exit code or parsed failed count greater than zero as suite failure at `tests/run-all-tests.sh:120`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-064 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: This template is the Stop-hook block message for a Round 0 gate: the RLCR loop may not progress until `goal-tracker.md` has real Ultimate Goal, Acceptance Criteria, and Active Tasks content.
- algorithmic_behavior: The template renders `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}` into a concrete remediation prompt at `prompt-template/block/goal-tracker-not-initialized.md:5`. It instructs the worker to read the goal tracker, replace placeholder text, define 3-7 testable acceptance criteria, populate Active Tasks, and write the updated file at `prompt-template/block/goal-tracker-not-initialized.md:8`.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh:619`: the active `GOAL_TRACKER_FILE` path and computed `MISSING_ITEMS`. Output is a rendered block reason embedded in a JSON hook decision with `systemMessage` set to `Loop: Goal Tracker not initialized in Round 0` at `hooks/loop-codex-stop-hook.sh:623`.
- gates_or_invariants: The invariant is explicit at `prompt-template/block/goal-tracker-not-initialized.md:16`: the immutable section can only be set in Round 0 and becomes read-only later. The Stop hook enforces this only when not in Finalize Phase, current round is zero, and the file exists at `hooks/loop-codex-stop-hook.sh:566`. It detects placeholders by extracting the Ultimate Goal, Acceptance Criteria, and Active Tasks sections and searching for generic `[To be ...]` markers at `hooks/loop-codex-stop-hook.sh:578`, `hooks/loop-codex-stop-hook.sh:585`, and `hooks/loop-codex-stop-hook.sh:593`.
- dependencies_and_callers: Rendered through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:619`, implemented by `hooks/lib/template-loader.sh:170`. The initial `goal-tracker.md` is generated by `scripts/setup-rlcr-loop.sh:527`, with placeholder defaults for missing goal/criteria/tasks at `scripts/setup-rlcr-loop.sh:560`, `scripts/setup-rlcr-loop.sh:580`, and `scripts/setup-rlcr-loop.sh:602`.
- edge_cases_or_failure_modes: The placeholder detector is section-scoped and intentionally generic, so it avoids false positives from placeholder wording elsewhere. However, it only checks for `[To be ` followed by lowercase letters, so uppercase or differently phrased placeholders could bypass the gate. If the goal tracker file is absent, this specific template is not used; the Stop hook only runs the initialization check when `-f "$GOAL_TRACKER_FILE"` is true.
- validation_or_tests: Related behavior is exercised by goal-tracker robustness and template coverage tests referenced in `tests/run-all-tests.sh:40` and `tests/run-all-tests.sh:55`. Static template references are validated by `tests/test-template-references.sh`, which checks that critical render sites use `load_and_render_safe`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-094 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: This is an executable robustness specification for AC-4 path validation in `scripts/setup-rlcr-loop.sh`, the setup entrypoint that creates the RLCR loop state, goal tracker, prompt, and plan backup.
- algorithmic_behavior: The test creates a temporary git repository, commits an initial file, creates valid/invalid plan files, then calls the production setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"` at `tests/robustness/test-path-validation-robustness.sh:64`. The helper classifies explicit path/content validation errors as rejection, while `requires codex` and `must be gitignored` mean path/content validation passed far enough to reach later gates at `tests/robustness/test-path-validation-robustness.sh:67`.
- inputs_outputs_state: Inputs are plan path strings and temporary filesystem/git state. Outputs are pass/fail counters via `tests/test-helpers.sh`, plus process exit code from `setup-rlcr-loop.sh`. It initializes `TEST_DIR`, `git init`, user config, and an initial commit at `tests/robustness/test-path-validation-robustness.sh:16`. It creates valid plan content with at least five lines through `create_valid_plan` at `tests/robustness/test-path-validation-robustness.sh:29`.
- gates_or_invariants: Positive cases require relative paths, root filenames, dashes/underscores, nested directories, dots in filenames, long filenames, and 10-level nesting to pass validation at `tests/robustness/test-path-validation-robustness.sh:98` and `tests/robustness/test-path-validation-robustness.sh:363`. Negative cases require rejection for absolute paths, spaces, shell metacharacters including semicolon, pipe, dollar, backticks, angle brackets, ampersand, glob characters, brackets, hash, exclamation, backslash, and tilde at `tests/robustness/test-path-validation-robustness.sh:155`. Symlink gates cover direct file symlinks, parent-directory symlinks, and symlink chains at `tests/robustness/test-path-validation-robustness.sh:318`. Content gates reject empty files, comment-only files, fewer than five lines, nonexistent paths, and directories at `tests/robustness/test-path-validation-robustness.sh:401`.
- dependencies_and_callers: The test sources `tests/test-helpers.sh` at line `14`; `pass`, `fail`, `setup_test_dir`, and `print_test_summary` live at `tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:86`, and `tests/test-helpers.sh:58`. It exercises `scripts/setup-rlcr-loop.sh`, whose production gates reject absolute paths at `scripts/setup-rlcr-loop.sh:227`, spaces at `scripts/setup-rlcr-loop.sh:233`, shell metacharacters at `scripts/setup-rlcr-loop.sh:241`, file symlinks at `scripts/setup-rlcr-loop.sh:253`, parent-directory symlinks at `scripts/setup-rlcr-loop.sh:266`, missing/unreadable files at `scripts/setup-rlcr-loop.sh:289`, project escape at `scripts/setup-rlcr-loop.sh:301`, git tracking mode at `scripts/setup-rlcr-loop.sh:330`, and content adequacy at `scripts/setup-rlcr-loop.sh:381`.
- edge_cases_or_failure_modes: The test treats `requires codex` as success because Codex availability is intentionally checked after path/content validation in production at `scripts/setup-rlcr-loop.sh:440`. It also treats `must be gitignored` as success because the tested path syntax/content has passed and the later plan-tracking policy is a separate gate. Unicode/CJK/emoji paths are explicitly allowed by policy note at `tests/robustness/test-path-validation-robustness.sh:390`, but the test does not add an executable Unicode case. The production content validator treats Markdown headings starting with `#` as comments at `scripts/setup-rlcr-loop.sh:425`, so plans need non-heading body lines to meet the three-content-line threshold.
- validation_or_tests: The file ends with `print_test_summary` and exits with its status at `tests/robustness/test-path-validation-robustness.sh:462`. It is included in the aggregate runner under robustness tests at `tests/run-all-tests.sh:56`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique Item Evidence sections
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`