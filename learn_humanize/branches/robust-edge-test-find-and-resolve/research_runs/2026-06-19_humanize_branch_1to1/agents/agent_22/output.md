# agent_22 robust-edge-test-find-and-resolve 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `a3112ca4d149f56ced783e805b6dfcf029368dc4`

## Item Evidence

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-022 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Edit` validator for RLCR loop-controlled files. It enforces which `.humanize/rlcr` artifacts Claude may edit directly, preserving state ownership and the round protocol. It is registered for Edit hooks in `hooks/hooks.json:24-30`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name`, exits pass-through for non-`Edit`, then evaluates the edit target path. It blocks `round-*-todos.md` unless the file is on the small active-loop allowlist, blocks all `round-*-prompt.md`, ignores paths outside `.humanize/rlcr`, discovers the active loop, parses either `state.md` or `finalize-state.md`, then blocks protected state, plan backup, and post-round-0 goal tracker edits. For `round-*-summary.md`, it verifies the filename’s round number matches current state unless allowlisted. See [loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-edit-validator.sh:22), [line 36](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-edit-validator.sh:36), [line 46](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-edit-validator.sh:46), [line 55](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-edit-validator.sh:55), [line 78](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-edit-validator.sh:78), and [line 127](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/loop-edit-validator.sh:127).
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path`. Outputs are exit codes and stderr block messages: `0` allows, `2` blocks policy violations, `1` blocks malformed active state. State read is active loop state from `$CLAUDE_PROJECT_DIR/.humanize/rlcr/<newest>/state.md` or `finalize-state.md`; the script does not mutate files.
- gates_or_invariants: Todos files must be handled through native TodoWrite, except exact active-loop allowlist paths from `is_allowlisted_file`; prompt files are read-only; `state.md` and `finalize-state.md` are hook-managed; `plan.md` backups under `.humanize/rlcr` are protected; `goal-tracker.md` may be edited only in round 0; summary edits must target the current round unless allowlisted. The finalize-state check intentionally precedes generic state matching because `finalize-state.md` also matches `state.md`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for path predicates, active-loop discovery, strict state parsing, allowlist checks, and rendered block messages. It depends on `jq`, `sed`, `basename`, and template loading through the common library. Tested by allowlist coverage in `tests/test-allowlist-validators.sh:186-226`, finalize-state protection in `tests/test-finalize-phase.sh:310-319`, plan backup protection in `tests/test-plan-file-hooks.sh:211-222`, and template safety in `tests/test-template-references.sh:176-200`.
- edge_cases_or_failure_modes: If no active loop exists, `.humanize/rlcr` edit checks pass through. If the active state file is malformed, it fails closed with “Malformed state file”. A notable robustness gap versus Read/Write/Bash validators is that this file directly runs `jq` extraction and does not call `validate_hook_input`, `is_deeply_nested`, or `require_tool_input_field`; the sibling validators do, as shown by `rg` hits only in Read/Write/Bash. Under `set -euo pipefail`, invalid JSON may terminate through `jq` rather than the shared validation path.
- validation_or_tests: Direct tests cover allowlisted historical todos/summaries, blocked non-allowlisted todos, finalize-state blocking, and plan backup blocking. Template reference tests ensure this critical validator uses safe rendering for block templates. No direct robustness test was found for malformed JSON/deeply nested Edit hook input.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-052 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR hook algorithm library. It centralizes hook input validation, loop state parsing, active-loop discovery, path classification, protected-file block messages, cancel authorization, git-add protection, bash modification detection, and loop termination state naming.
- algorithmic_behavior: Defines state field constants and exit reason constants, validates JSON and required fields, rejects excessive JSON depth, locates the newest active loop directory only if it has `state.md` or `finalize-state.md`, parses state frontmatter in tolerant and strict modes, normalizes filename/path checks for round files, and renders standard block messages via templates. It also authorizes the one allowed state-file move for cancellation, detects risky `git add` commands that could stage `.humanize`, detects shell commands that modify protected files, and renames active state files to terminal `*-state.md` names. See [loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:55), [line 152](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:152), [line 240](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:240), [line 327](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:327), [line 455](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:455), [line 644](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:644), [line 775](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:775), and [line 823](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/hooks/lib/loop-common.sh:823).
- inputs_outputs_state: Inputs include hook JSON strings, tool field names, loop base paths, active loop paths, state file paths, lowercase command strings, and file patterns. Outputs are shell return codes, exported/global variables such as `VALIDATED_TOOL_NAME`, `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, rendered block text on stdout, active loop paths, or terminal state renames. The state parser reads YAML frontmatter fields including `current_round`, `max_iterations`, `push_every_round`, Codex model/effort/timeout, plan file, branch, and plan tracking.
- gates_or_invariants: Strict state parsing requires at least two `---` separators, nonempty `current_round` and `max_iterations`, and numeric values. Active-loop discovery considers only the newest timestamp-like loop directory and ignores completed/cancelled/failed state files. Allowlisted legacy files are exact active-loop paths only: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`. Cancel authorization requires `.cancel-requested`, a single `mv` from active `state.md` or `finalize-state.md` to `cancel-state.md`, no shell substitution/chaining/extra args, no remaining variables after `$loop_dir` normalization, and no symlink source. `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, or `unexpected`.
- dependencies_and_callers: Sourced by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh`. It sources `hooks/lib/template-loader.sh`, which provides `load_template`, `render_template`, and `load_and_render_safe`. Runtime dependencies include `jq`, `iconv` when available, `sed`, `grep`, `awk`, `sort`, `head`, `tr`, `wc`, and `git check-ignore`.
- edge_cases_or_failure_modes: `validate_hook_input` rejects invalid JSON and invalid UTF-8, but null bytes may be stripped by Bash before detection in some command-substitution paths, which tests document. `parse_state_file` is tolerant and defaults missing values, while `parse_state_file_strict` fails closed. `find_active_loop` intentionally ignores older active-looking directories once a newer directory exists, preventing “zombie” loop revival. `command_modifies_file` has a documented limitation for multi-source `cp file1 file2 goal-tracker.md` forms. `git_adds_humanize` behavior depends on current working directory and whether `.humanize` exists and is ignored for broad non-force adds.
- validation_or_tests: State parsing robustness is covered in `tests/robustness/test-state-file-robustness.sh:33-226`; hook input robustness in `tests/robustness/test-hook-input-robustness.sh:35-235`; cancel hardening in `tests/robustness/test-cancel-security-robustness.sh:39-280`; command modification regexes in `tests/test-bash-validator-patterns.sh:69-195`; active/terminal state naming and `end_loop` in `tests/test-state-exit-naming.sh:38-176` and `tests/test-state-exit-naming.sh:179-238`; template references in `tests/test-template-references.sh:149-167`.
- skip_candidate: `no`

### ROBUST_EDGE_TEST_FIND_AND_RESOLVE-HZ-082 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing prompt template for the RLCR goal-tracker ownership transition. It gives Claude a structured request format when it needs `goal-tracker.md` changes after direct edits are blocked.
- algorithmic_behavior: The template appends a “Goal Tracker Update Request” section skeleton to next-round Claude prompts. It does not run code; its algorithmic role is procedural: convert forbidden direct goal-tracker writes into summary-embedded requested changes with justification, so Codex can review and apply updates. See [goal-tracker-update-request.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/robust-edge-test-find-and-resolve/prompt-template/claude/goal-tracker-update-request.md:2).
- inputs_outputs_state: Input is implicit prompt composition by the stop hook. Output is appended markdown in the next round prompt. It requests “Requested Changes” bullets and “Justification”, including examples for task completion, open issues, plan evolution, and deferral impact. It does not mutate `goal-tracker.md`; it creates evidence for Codex review.
- gates_or_invariants: Pairs with the validators’ invariant that after round 0 only Codex can modify `goal-tracker.md`. The Edit validator blocks post-round-0 goal-tracker edits at `hooks/loop-edit-validator.sh:117-120`; the shared block message tells Claude to put the request in the current summary. Codex-side handling is defined in `prompt-template/codex/goal-tracker-update-section.md:1-17`, which instructs Codex to evaluate requests, update mutable tracker sections if approved, never modify the immutable section, and explain rejections.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh:1108-1113` and appended to `round-${NEXT_ROUND}-prompt.md`; if missing, the stop hook falls back to a one-line instruction. Related enforcement comes from `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:804-814` and the block template `prompt-template/block/goal-tracker-modification.md:1-25`.
- edge_cases_or_failure_modes: If the template is absent or empty, Claude still receives fallback guidance, but loses the richer requested-changes and justification schema. Because the template is free-form markdown, downstream acceptance depends on Claude actually including the section in its summary and Codex judging the request justified. It is a skip candidate only if “core algorithm” is interpreted as executable shell code; otherwise it is central to the post-round-0 goal-tracker transition contract.
- validation_or_tests: `tests/test-template-references.sh:121-140` discovers template files and reports unreferenced ones; direct reference search shows this template is loaded by the stop hook. Comprehensive template tests cover the Codex-side goal-tracker update section at `tests/test-templates-comprehensive.sh:531-539`, but I did not find a direct content test for this Claude-side template.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 section headers for the 3 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`