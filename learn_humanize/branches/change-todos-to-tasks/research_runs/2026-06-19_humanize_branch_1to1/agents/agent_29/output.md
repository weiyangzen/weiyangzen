# agent_29 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Read` guard for the RLCR loop. It prevents agents from reading stale or misplaced loop prompt/summary files and blocks `round-*-todos.md` reads unless the file is explicitly allowlisted in the active loop directory; its stated contract is in `hooks/loop-read-validator.sh:3-12`.
- algorithmic_behavior: The hook reads JSON from stdin, validates it with shared hook parsing, exits early for non-`Read` tools, requires `tool_input.file_path`, lowercases the path for type detection, then applies two layers: a todos-file block at `hooks/loop-read-validator.sh:55-63`, and a prompt/summary round/location validator at `hooks/loop-read-validator.sh:69-170`.
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR` or cwd-derived project root, optional `PROJECT_ROOT`/`LOOP_BASE_DIR`/`LOOP_DIR`, and the newest active `.humanize/rlcr/<session>` state. Outputs are exit `0` allow, exit `1` malformed input/state hard failure, or exit `2` block with a rendered markdown message on stderr. The state transition read is: find active loop, prefer `finalize-state.md` over `state.md`, parse strict, set `CURRENT_ROUND`, then compare requested round/path to that state (`hooks/loop-read-validator.sh:80-99`).
- gates_or_invariants: JSON must be valid, UTF-8, contain `tool_name`, not exceed depth 30, and contain `tool_input.file_path` for `Read` (`hooks/lib/loop-common.sh:59-132`, `hooks/loop-read-validator.sh:25-49`). For prompt/summary reads, the file must be inside `.humanize/rlcr/`, round must equal `current_round` unless allowlisted, and the exact path must be `$ACTIVE_LOOP_DIR/$basename` (`hooks/loop-read-validator.sh:122-168`). Malformed active state fails closed (`hooks/loop-read-validator.sh:94-98`).
- dependencies_and_callers: Registered by `hooks/hooks.json` under PreToolUse matcher `Read` (`hooks/hooks.json:33-39`). It depends on `hooks/lib/loop-common.sh` for active-loop discovery, strict state parsing, round-file regexes, lowercasing, allowlist, and block-message helpers (`hooks/lib/loop-common.sh:156-173`, `255-329`, `395-451`, `713-716`). It depends on `hooks/lib/template-loader.sh` for safe single-pass `{{VAR}}` rendering and fallback behavior (`hooks/lib/template-loader.sh:7-13`, `167-180`).
- edge_cases_or_failure_modes: No active loop allows prompt/summary reads but blocks non-allowlisted todos reads because todos are explicitly redirected to native task tooling (`hooks/loop-read-validator.sh:55-63`, `84-86`). Unknown/non-round filenames pass. Round extraction only matches numeric `round-N-(summary|prompt|todos).md` (`hooks/lib/loop-common.sh:401-418`). The allowlist is hardcoded to exact active-loop paths for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`hooks/lib/loop-common.sh:423-441`). Directory membership is substring-based rather than canonicalized (`hooks/lib/loop-common.sh:713-716`).
- validation_or_tests: Read allowlist behavior is specified in `tests/test-allowlist-validators.sh:234-281`, including allowed `round-1-todos.md` and historical summary, and blocked later todos/summary files. Hook input robustness covers valid JSON, invalid JSON, missing `file_path`, Unicode paths, unknown tools, deep nesting, non-UTF8, and null-byte handling in `tests/robustness/test-hook-input-robustness.sh:35-115` and `150-235`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable regression specification for RLCR plan-file hook behavior. The header names the covered hooks: UserPromptSubmit plan validation, Write/Edit blocking of loop `plan.md`, and Bash modification blocking (`tests/test-plan-file-hooks.sh:3-9`).
- algorithmic_behavior: The test constructs isolated git repositories and active `.humanize/rlcr/2024-01-01_12-00-00` loop state, writes a plan backup, and feeds hook JSON into the actual validators. It increments PASS/FAIL/SKIP counters and exits with the failure count (`tests/test-plan-file-hooks.sh:22-32`, `1108-1117`).
- inputs_outputs_state: Inputs are generated temp repos, `CLAUDE_PROJECT_DIR`, hook JSON, mock `codex`, git branch state, `state.md` frontmatter, plan files, backups, summaries, and goal trackers. Outputs are textual PASS/FAIL evidence and final process exit equal to `TESTS_FAILED`. Its canonical loop state includes `current_round`, `max_iterations`, quoted `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` (`tests/test-plan-file-hooks.sh:63-114`).
- gates_or_invariants: It asserts strict v1.5.0+ state schema and YAML quote stripping (`tests/test-plan-file-hooks.sh:119-189`, `373-500`), branch immutability during a loop (`194-217`), protection of `.humanize/rlcr/.../plan.md` through Write/Edit/Bash (`226-284`), and shell-bypass resistance for direct loop paths, command substitution, globs, brace expansion, pipes, and backticks (`286-367`).
- dependencies_and_callers: Directly executes `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh` throughout the file (`tests/test-plan-file-hooks.sh:125`, `230`, `247`, `264`, `459`). It relies on git, jq-mediated hook behavior, and a local mock `codex` binary to avoid real model calls (`tests/test-plan-file-hooks.sh:38-58`).
- edge_cases_or_failure_modes: Covers missing required fields, missing `start_branch`, branch mismatch, quoted `start_branch`, quoted `plan_file`, hyphenated plan paths, plan modifications, plan deletion, missing backup, tracked-file uncommitted changes, tracked-file committed changes with clean git status, outdated stop-hook schema JSON blocking, section-specific placeholder reporting, and legacy `.humanize-loop.local` paths being allowed (`tests/test-plan-file-hooks.sh:149-217`, `424-500`, `509-775`, `781-1106`).
- validation_or_tests: This file is itself the validation harness. It also verifies stop-hook plan integrity: backup must exist, original plan must exist, tracked plans must be clean, and content must still match backup even if the change was committed (`tests/test-plan-file-hooks.sh:509-775`). Goal tracker placeholder detection must report only the specific incomplete section, or all three when all are placeholders (`781-1062`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-089 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Markdown block template for the Stop hook’s large-file gate. It is rendered when changed code or documentation files exceed the configured line limit and becomes the user-facing reason for blocking loop stop/progress (`prompt-template/block/large-files.md:1-5`).
- algorithmic_behavior: The template parameterizes `{{MAX_LINES}}` and `{{LARGE_FILES}}`, explains why the gate exists, and prescribes separate remediation paths for code files and documentation files (`prompt-template/block/large-files.md:3-25`). The algorithm that feeds it lives in the stop hook: `MAX_LINES=2000`, scan changed tracked/new files, classify by extension, count lines, collect entries over threshold, render this template, emit JSON decision `block`, and exit (`hooks/loop-codex-stop-hook.sh:428-507`).
- inputs_outputs_state: Inputs are the stop hook’s computed max-line threshold and newline list of offending files with line counts and file type. Output is markdown inserted into a JSON block response as `reason`, plus a system message saying large files were detected. There is no persistent state mutation in this template; it defines the remediation contract for the blocked state.
- gates_or_invariants: Code files must be split into modules below `MAX_LINES` while preserving functionality; documentation must be split into logical sections below `MAX_LINES` while preserving cross-references, narrative flow, and navigation (`prompt-template/block/large-files.md:13-24`). The final instruction requires committing the split changes before attempting exit again (`prompt-template/block/large-files.md:25`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` with `MAX_LINES` and `LARGE_FILES` substitutions (`hooks/loop-codex-stop-hook.sh:495-497`). Rendering uses the shared template loader’s single-pass placeholder substitution and fallback semantics (`hooks/lib/template-loader.sh:50-58`, `167-180`).
- edge_cases_or_failure_modes: The stop-hook scanner ignores deleted files, handles rename syntax by using the new path, skips unsupported extensions, ignores nonnumeric `wc -l` results, and only records files strictly greater than the limit (`hooks/loop-codex-stop-hook.sh:445-485`). If the template is missing or empty, the hook has an inline fallback message (`hooks/loop-codex-stop-hook.sh:488-494`).
- validation_or_tests: Template presence and hook references are covered by template-reference tests found by repository search, and the caller path is concrete in `hooks/loop-codex-stop-hook.sh:488-507`. No dedicated assigned test file exercises the exact text here, but the stop-hook gate is wired through the shared template loader.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-119 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Source template for generated plan files. It defines the planning contract consumed upstream of RLCR execution: goal description, deterministic acceptance criteria, path boundaries, feasibility hints, dependency sequencing, and implementation naming rules (`prompt-template/plan/gen-plan-template.md:1-74`).
- algorithmic_behavior: The template forces each acceptance criterion to include positive and negative tests, allows nested subcriteria, then asks for upper/lower implementation bounds and allowed/prohibited choices (`prompt-template/plan/gen-plan-template.md:6-44`). It also separates non-prescriptive feasibility hints from requirements and asks for dependency/sequence relationships rather than time estimates (`prompt-template/plan/gen-plan-template.md:46-68`).
- inputs_outputs_state: Inputs are the static template plus a draft file appended by the generator. Output is a new plan file containing this scaffold, a separator, and the original design draft. There is no runtime state transition inside the template itself; its state impact is creating the plan artifact that later RLCR setup and hooks treat as the immutable plan source.
- gates_or_invariants: Acceptance criteria must be testable with expected pass/fail cases (`prompt-template/plan/gen-plan-template.md:6-23`). Path boundaries must state maximum and minimum acceptable scope, and deterministic designs may collapse upper/lower bounds when choices are fixed (`prompt-template/plan/gen-plan-template.md:25-44`). Implementation code/comments must not contain plan workflow markers such as `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:69-74`).
- dependencies_and_callers: `scripts/validate-gen-plan-io.sh` locates this template via `CLAUDE_PLUGIN_ROOT` or script-relative fallback, verifies it exists, copies it to the requested output file, then appends the original draft (`scripts/validate-gen-plan-io.sh:133-157`). The script also guards input existence, nonempty input, output directory existence, output nonexistence, and output directory writability before copying (`scripts/validate-gen-plan-io.sh:79-124`).
- edge_cases_or_failure_modes: Generator IO validation fails early for missing/empty input, missing output directory, output path already existing or being a directory, no write permission, invalid args, or missing template file; missing template exits as plugin configuration error (`scripts/validate-gen-plan-io.sh:4-13`, `142-146`). The template placeholders are intentionally human/model-fillable and are not dynamically validated by this file.
- validation_or_tests: The generator script validates the template path and materializes it (`scripts/validate-gen-plan-io.sh:126-160`). Repository references also show `commands/gen-plan.md` mirrors this structure, but the authoritative executable copy path for this template is the validation script.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CHANGE_TODOS_TO_TASKS-HZ-029`, `CHANGE_TODOS_TO_TASKS-HZ-059`, `CHANGE_TODOS_TO_TASKS-HZ-089`, `CHANGE_TODOS_TO_TASKS-HZ-119`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`