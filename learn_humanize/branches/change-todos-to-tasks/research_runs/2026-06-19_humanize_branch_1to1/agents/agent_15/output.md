# agent_15 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor utility library for the Humanize CLI monitor surfaces. Recursive inspection found one descendant: `scripts/lib/monitor-common.sh` (498 lines). It is sourced by `scripts/humanize.sh` when present, so monitor commands can reuse session discovery, state parsing, PR-loop phase detection, terminal handling, and goal-tracker parsing logic (`scripts/humanize.sh:6-10`).
- algorithmic_behavior: `monitor-common.sh` is a read-oriented support library. It defines ANSI color helpers (`scripts/lib/monitor-common.sh:14-23`), cross-platform file sizing via GNU/BSD `stat` fallback (`scripts/lib/monitor-common.sh:32-35`), latest session discovery by timestamp-shaped directory names (`scripts/lib/monitor-common.sh:40-63`), terminal scroll-region setup/restore (`scripts/lib/monitor-common.sh:71-92`), status-to-color mapping (`scripts/lib/monitor-common.sh:130-141`), state file detection with active-state priority (`scripts/lib/monitor-common.sh:154-188`), YAML-frontmatter value extraction (`scripts/lib/monitor-common.sh:197-209`), timestamp/string formatting helpers (`scripts/lib/monitor-common.sh:218-252`), PR-loop phase detection (`scripts/lib/monitor-common.sh:267-342`), phase display labels (`scripts/lib/monitor-common.sh:346-381`), RLCR goal tracker parsing (`scripts/lib/monitor-common.sh:390-463`), and PR goal tracker parsing (`scripts/lib/monitor-common.sh:468-498`).
- inputs_outputs_state: Primary inputs are filesystem paths under `.humanize/rlcr` and `.humanize/pr-loop`, markdown state files, goal-tracker markdown, and round-specific PR check files. Outputs are shell strings: latest session path, `state_file|status`, parsed YAML values, phase identifiers such as `approved`, `codex_analyzing`, `waiting_initial_review`, or pipe-delimited tracker stats. `get_pr_loop_phase` also writes a transient `/tmp/humanize-phase-<session>-<round>.size` cache to compare PR-check file growth (`scripts/lib/monitor-common.sh:297-307`).
- gates_or_invariants: Session directory names must match `YYYY-MM-DD_HH-MM-SS` to be considered latest (`scripts/lib/monitor-common.sh:54-60`). Active state wins over terminal state because `state.md` is checked before `*-state.md` (`scripts/lib/monitor-common.sh:162-181`). PR phase final states are prioritized before Codex-growth detection (`scripts/lib/monitor-common.sh:272-276`). `codex_analyzing` requires either a growing `round-*-pr-check.md` size or mtime younger than 10 seconds (`scripts/lib/monitor-common.sh:292-318`). Goal-tracker parsing assumes stable markdown headers/table shapes such as `### Acceptance Criteria`, `#### Active Tasks`, and `### Completed and Verified` (`scripts/lib/monitor-common.sh:397-462`).
- dependencies_and_callers: `scripts/humanize.sh` sources the library at startup and uses `monitor_find_latest_session` in its RLCR monitor flow (`scripts/humanize.sh:237-240`). PR-loop system tests source it directly for phase and state detection coverage (`tests/test-pr-loop-system.sh:256-268`, `tests/test-pr-loop-system.sh:616-635`). The library coordinates with sibling hooks/scripts indirectly by reading artifacts those paths create: loop state files, approval/cancel/max-iteration state files, PR check files, and goal trackers.
- edge_cases_or_failure_modes: `monitor_find_state_file` returns the first `*-state.md` from `find`, so multiple terminal state files can be order-dependent (`scripts/lib/monitor-common.sh:172-181`). `monitor_get_yaml_value` uses a simple grep/sed frontmatter extraction and is best suited to scalar one-line values (`scripts/lib/monitor-common.sh:203-208`). `monitor_truncate_string` assumes `max_len` is large enough to reserve three characters for ellipsis (`scripts/lib/monitor-common.sh:243-250`). `get_pr_loop_phase` uses `/tmp` cache keys based on session basename and round, so same-named sessions in different projects could share cache state (`scripts/lib/monitor-common.sh:297-307`). Goal-tracker counting is header/table-format sensitive and can miscount if headings or table columns drift.
- validation_or_tests: `tests/test-pr-loop-system.sh` exercises phase detection for approved, initial waiting, reviewer waiting, and state-file detection by sourcing this library directly (`tests/test-pr-loop-system.sh:256-308`, `tests/test-pr-loop-system.sh:616-635`). Those tests verify the library’s public functions against synthetic session directories and state files.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Test fixture generator for GitHub CLI behavior. It creates a local executable `gh` shim so PR comment/review fetch and poll scripts can be tested deterministically without network calls (`tests/setup-fixture-mock-gh.sh:3-11`).
- algorithmic_behavior: The script requires `<mock_bin_dir> <fixtures_dir>`, creates the mock bin directory, writes a `gh` shell script, marks it executable, and prints the mock bin path (`tests/setup-fixture-mock-gh.sh:16-24`, `tests/setup-fixture-mock-gh.sh:27-101`). The generated mock dispatches on the first `gh` subcommand: `auth status`, `repo view`, `pr view`, and `api` (`tests/setup-fixture-mock-gh.sh:33-93`). API endpoint matching returns fixture JSON for issue comments, inline PR review comments, and PR reviews (`tests/setup-fixture-mock-gh.sh:71-87`).
- inputs_outputs_state: Inputs are the output directory for the shim and a fixture directory containing `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`. The persistent output is `$MOCK_BIN_DIR/gh`; the immediate stdout output is `$MOCK_BIN_DIR` (`tests/setup-fixture-mock-gh.sh:99-101`). Runtime mock outputs include fixed JSON for user/repo/PR metadata and fixture-backed JSON arrays for GitHub API endpoints (`tests/setup-fixture-mock-gh.sh:42-59`, `tests/setup-fixture-mock-gh.sh:65-90`).
- gates_or_invariants: The setup script runs with `set -euo pipefail` and exits with usage if either argument is missing (`tests/setup-fixture-mock-gh.sh:14-22`). Unknown generated-mock commands fail closed with stderr and exit 1 (`tests/setup-fixture-mock-gh.sh:95-96`). Unmatched `gh api` endpoints intentionally return `[]` with success, letting tests ignore irrelevant API calls (`tests/setup-fixture-mock-gh.sh:89-91`).
- dependencies_and_callers: `tests/test-pr-loop-hooks.sh` wraps this helper in `setup_fixture_mock_gh`, passing `tests/fixtures` as the fixture source and prepending the generated bin dir to `PATH` before invoking `scripts/fetch-pr-comments.sh` (`tests/test-pr-loop-hooks.sh:1231-1251`). The helper’s header states it is also for `poll-pr-reviews.sh` coverage (`tests/setup-fixture-mock-gh.sh:3-4`).
- edge_cases_or_failure_modes: The heredoc expands `FIXTURES_DIR` into the generated script at creation time (`tests/setup-fixture-mock-gh.sh:27-31`), so unusual quote characters in the fixture path could corrupt the generated assignment. The generated mock does not validate fixture file existence before `cat`; because each fixture branch exits explicitly after `cat`, a missing fixture can produce stderr/empty output while still reaching the branch’s forced exit path (`tests/setup-fixture-mock-gh.sh:72-86`). Command matching is substring-based over `$*`, so tests rely on stable `gh` argument shapes rather than full option parsing.
- validation_or_tests: The fixture-backed section of `tests/test-pr-loop-hooks.sh` creates this mock and then verifies `fetch-pr-comments.sh` output against issue comments, review comments, and approval-only review fixtures (`tests/test-pr-loop-hooks.sh:1221-1265` and following fixture-backed assertions).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-075 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking message template for PR-loop startup when the Claude GitHub bot fails to acknowledge a trigger comment with an eyes reaction. It converts a bot-liveness gate failure into operator-facing remediation text (`prompt-template/block/claude-eyes-timeout.md:1-18`).
- algorithmic_behavior: The template interpolates `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}` into a message that names the failed wait budget, lists likely causes, gives required checks, and instructs the operator to post a new trigger comment after configuration is fixed (`prompt-template/block/claude-eyes-timeout.md:3-18`).
- inputs_outputs_state: Inputs are render variables from the PR loop stop hook. In the observed production call, `RETRY_COUNT=3` and `TOTAL_WAIT_SECONDS=15` are passed after `check-bot-reactions.sh claude-eyes ... --retry 3 --delay 5` fails to produce a reaction (`hooks/pr-loop-stop-hook.sh:774-785`). Output is rendered markdown stored in the JSON `reason` field of a blocking hook decision (`hooks/pr-loop-stop-hook.sh:787-789`).
- gates_or_invariants: The gate is active only when Claude is configured and trigger verification is required (`hooks/pr-loop-stop-hook.sh:760-768`). The block fires only when a trigger comment ID exists and the eyes reaction is empty or `null` after retries (`hooks/pr-loop-stop-hook.sh:771-789`). If the trigger timestamp exists but no comment ID is available, the hook warns instead of blocking (`hooks/pr-loop-stop-hook.sh:793-796`). If Claude is configured but trigger verification is not required, the eyes check is skipped (`hooks/pr-loop-stop-hook.sh:797-799`).
- dependencies_and_callers: Rendered through `load_and_render_safe` from `hooks/pr-loop-stop-hook.sh`, with an inline fallback string if the template cannot be loaded (`hooks/pr-loop-stop-hook.sh:780-785`). It depends on `scripts/check-bot-reactions.sh` for the actual reaction polling signal and on the hook framework’s JSON decision contract for blocking behavior.
- edge_cases_or_failure_modes: The template itself is declarative and does not enforce retry timing; incorrect values supplied by the caller would produce misleading text. Bot non-response is treated as configuration or availability failure, not as a recoverable in-loop transition. A missing template falls back to a shorter hardcoded message, preserving the block but losing the fuller remediation instructions (`hooks/pr-loop-stop-hook.sh:780-785`).
- validation_or_tests: No direct template-specific test was found in the inspected references. Runtime coverage is through the PR stop hook path that renders this block when Claude eyes verification fails (`hooks/pr-loop-stop-hook.sh:774-789`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-105 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Hook block template for preventing agents from writing or editing a summary file for the wrong RLCR round. It preserves the loop’s round-number invariant and directs the agent to the current round’s path (`prompt-template/block/wrong-round-number.md:1-7`).
- algorithmic_behavior: The template interpolates `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, and `{{CORRECT_PATH}}` into a concise blocking instruction: the attempted `round-N-type.md` does not match the active round, the correct path is displayed, and the agent is told not to increment the round number manually (`prompt-template/block/wrong-round-number.md:3-7`).
- inputs_outputs_state: Inputs come from validators after extracting a round number from the target filename and reading current loop state. `loop-write-validator.sh` passes `ACTION=write to`, the extracted `CLAUDE_ROUND`, `FILE_TYPE=summary`, `CURRENT_ROUND`, and computed `CORRECT_PATH` (`hooks/loop-write-validator.sh:228-243`). `loop-edit-validator.sh` passes the same shape with `ACTION=edit` (`hooks/loop-edit-validator.sh:155-177`). Output is markdown written to stderr before the validator exits with code 2 (`hooks/loop-write-validator.sh:238-244`, `hooks/loop-edit-validator.sh:172-178`).
- gates_or_invariants: The gate applies to summary files. It triggers when an extracted filename round exists, differs from `CURRENT_ROUND`, and the file is not allowlisted (`hooks/loop-write-validator.sh:228-245`, `hooks/loop-edit-validator.sh:155-180`). The state transition is a blocked write/edit attempt; the current round is not changed, and the agent is redirected to `round-${CURRENT_ROUND}-summary.md`.
- dependencies_and_callers: Called by write and edit validators through `load_and_render_safe`, with inline fallback messages if the template is unavailable (`hooks/loop-write-validator.sh:233-243`, `hooks/loop-edit-validator.sh:167-177`). It depends on shared hook helpers such as `extract_round_number`, `is_round_file_type`, `is_allowlisted_file`, and template loading infrastructure.
- edge_cases_or_failure_modes: If no round number can be extracted from the filename, the wrong-round comparison does not fire. Allowlisted files bypass the block even if the round differs. The template does not validate numeric content itself; it relies entirely on validator-side extraction and current-round parsing. It only names one correct path, so callers must choose the right `FILE_TYPE` and destination.
- validation_or_tests: `tests/test-template-loader.sh` renders this real template and asserts replacement of `round-3-summary.md` and current round `5` (`tests/test-template-loader.sh:150-163`). `tests/test-templates-comprehensive.sh` repeats integration coverage and checks the correct path appears (`tests/test-templates-comprehensive.sh:498-510`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-135 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for plan-file path and content validation in `scripts/setup-rlcr-loop.sh`. It verifies that valid relative plan paths are accepted and risky path/content shapes are rejected before loop setup proceeds (`tests/robustness/test-path-validation-robustness.sh:3-8`).
- algorithmic_behavior: The test initializes an isolated temp directory via `test-helpers.sh`, creates a mock `codex` executable in `$TEST_DIR/bin` to avoid real Codex calls, initializes a git repo with one commit, and defines helpers for valid plan content and validation execution (`tests/robustness/test-path-validation-robustness.sh:12-63`). `test_path_validation` removes any active loop state, runs `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"`, captures output, treats known validation-error messages as rejection, and treats later-stage messages such as missing Codex or gitignore/tracking checks as evidence that path validation passed (`tests/robustness/test-path-validation-robustness.sh:74-111`).
- inputs_outputs_state: Inputs are synthetic plan paths and files created under `$TEST_DIR`. Outputs are pass/fail messages and a final summary/exit status from `print_test_summary` (`tests/robustness/test-path-validation-robustness.sh:65-68`, `tests/robustness/test-path-validation-robustness.sh:480-485`). Test state includes a temporary git repository, mock command directory added to `PATH`, generated plan files, symlinks, nested directories, and `.humanize/rlcr` cleanup before each production-script invocation (`tests/robustness/test-path-validation-robustness.sh:22-45`, `tests/robustness/test-path-validation-robustness.sh:81-87`).
- gates_or_invariants: Positive path cases require acceptance of normal relative paths, root filenames, dash/underscore filenames, nested directories, and dotted filenames (`tests/robustness/test-path-validation-robustness.sh:120-167`). Negative path cases require rejection of absolute paths, spaces, shell metacharacters, backslash, tilde, grouping/glob characters, hash, and exclamation mark (`tests/robustness/test-path-validation-robustness.sh:177-330`). Symlink cases require rejection of the plan file itself as a symlink, a symlinked parent directory, and a file symlink chain (`tests/robustness/test-path-validation-robustness.sh:340-375`). Long-but-valid names and deep paths should be accepted when supported by the filesystem (`tests/robustness/test-path-validation-robustness.sh:385-410`). Content gates reject empty files, comment-only files, files with fewer than five lines, nonexistent files, and directories (`tests/robustness/test-path-validation-robustness.sh:423-478`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp setup and pass/fail summary functions (`tests/robustness/test-path-validation-robustness.sh:12-16`). It directly targets `scripts/setup-rlcr-loop.sh` validation behavior (`tests/robustness/test-path-validation-robustness.sh:84-91`). The corresponding production validator rejects absolute paths, spaces, shell metacharacters, file symlinks, symlinked parents, missing/unreadable files, out-of-project resolved paths, and insufficient content (`scripts/setup-rlcr-loop.sh:320-340`, `scripts/setup-rlcr-loop.sh:346-405`, `scripts/setup-rlcr-loop.sh:483-535`). The robustness suite is listed in `tests/run-all-tests.sh` according to the repository search result at `tests/run-all-tests.sh:59`.
- edge_cases_or_failure_modes: The test’s acceptance/rejection classifier is output-message based, so production error-message drift can change test behavior even if validation semantics are unchanged (`tests/robustness/test-path-validation-robustness.sh:89-107`). The valid plan helper creates markdown headings, but production content validation treats lines starting with `#` as comments; the helper still passes because it includes exactly three non-comment content lines (`tests/robustness/test-path-validation-robustness.sh:51-62`, `scripts/setup-rlcr-loop.sh:493-532`). Unicode/CJK/emoji paths are documented as allowed but not exercised in this file (`tests/robustness/test-path-validation-robustness.sh:412-414`). The backslash test uses a literal backslash sequence, not an actual newline in the filename (`tests/robustness/test-path-validation-robustness.sh:269-276`).
- validation_or_tests: This file is itself the validation asset. It provides 31 numbered cases covering positive paths, injection-oriented path rejection, symlink traversal rejection, path length/depth handling, and plan content sufficiency (`tests/robustness/test-path-validation-robustness.sh:120-478`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: CHANGE_TODOS_TO_TASKS-HZ-015, CHANGE_TODOS_TO_TASKS-HZ-045, CHANGE_TODOS_TO_TASKS-HZ-075, CHANGE_TODOS_TO_TASKS-HZ-105, CHANGE_TODOS_TO_TASKS-HZ-135
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`