# agent_19 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling the active RLCR loop. It delegates all state mutation to `scripts/cancel-rlcr-loop.sh`, keeping the command layer as a routing and confirmation contract rather than an implementation surface. See `commands/cancel-rlcr-loop.md:1-4`, `commands/cancel-rlcr-loop.md:11-15`, and `commands/cancel-rlcr-loop.md:36-38`.
- algorithmic_behavior: The command runs the cancel script once, reads the first output line as a status token, then either reports no active loop, reports cancellation, or enters a confirmation subflow for Finalize Phase. The state-token dispatch is defined at `commands/cancel-rlcr-loop.md:17-22`. If the token is `FINALIZE_NEEDS_CONFIRM`, the command asks the user whether to force cancellation and only then runs the script with `--force`; see `commands/cancel-rlcr-loop.md:23-34`.
- inputs_outputs_state: Input is the active project state under `${CLAUDE_PROJECT_DIR}/.humanize/rlcr`, plus optional user confirmation during Finalize Phase. Output is user-facing cancellation or no-loop messaging. The backing script finds the newest loop dir, detects `state.md` or `finalize-state.md`, writes `.cancel-requested`, and renames the active state file to `cancel-state.md`; see `scripts/cancel-rlcr-loop.sh:69-78`, `scripts/cancel-rlcr-loop.sh:85-99`, and `scripts/cancel-rlcr-loop.sh:134-151`.
- gates_or_invariants: The command’s invariant is that cancellation logic belongs to the script, not the prompt. Finalize Phase requires confirmation unless `--force` is used. A loop is active only when the newest loop directory contains `state.md` or `finalize-state.md`; see `commands/cancel-rlcr-loop.md:36` and `scripts/cancel-rlcr-loop.sh:89-99`.
- dependencies_and_callers: Depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh`; allowed tool metadata restricts Bash calls to normal and forced invocations plus `AskUserQuestion` at `commands/cancel-rlcr-loop.md:3`. The script is also referenced by operator guidance from RLCR hooks, e.g. `hooks/loop-codex-stop-hook.sh:169-170`, `hooks/loop-codex-stop-hook.sh:324-326`.
- edge_cases_or_failure_modes: `NO_LOOP` means no loop directory exists; `NO_ACTIVE_LOOP` means a loop directory exists without active state. Finalize Phase returns exit code `2` and must not be force-cancelled without confirmation. Unknown script options exit `3`; see `scripts/cancel-rlcr-loop.sh:11-15`, `scripts/cancel-rlcr-loop.sh:57-60`, and `scripts/cancel-rlcr-loop.sh:117-127`.
- validation_or_tests: Cancellation behavior is covered by dedicated cancellation/state tests outside this assigned file set, including `tests/test-cancel-signal-file.sh` references to `.cancel-requested` and active loop fixtures. The command file itself is a declarative workflow spec, so validation is primarily that its status-token contract matches `scripts/cancel-rlcr-loop.sh:75-78`, `scripts/cancel-rlcr-loop.sh:96-98`, `scripts/cancel-rlcr-loop.sh:117-127`, and `scripts/cancel-rlcr-loop.sh:144-151`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the core Bash-command write-detection primitive `command_modifies_file` in `hooks/lib/loop-common.sh`. It verifies the regex layer used by `hooks/loop-bash-validator.sh` to block shell-based bypasses of protected RLCR and PR-loop files. See `tests/test-bash-validator-patterns.sh:3-6` and `tests/test-bash-validator-patterns.sh:11-13`.
- algorithmic_behavior: The test lowercases each candidate command with `to_lower`, calls `command_modifies_file "$command_lower" "$pattern"`, and increments pass/fail counters through `assert_modifies` and `assert_not_modifies`; see `tests/test-bash-validator-patterns.sh:36-62`. It covers redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec`, and false-positive read-only commands; see `tests/test-bash-validator-patterns.sh:69-194`.
- inputs_outputs_state: Inputs are literal command strings and file regexes, defaulting to `goal-tracker\.md`; alternate patterns include `state\.md` and `round-[0-9]+-summary\.md` at `tests/test-bash-validator-patterns.sh:181-194`. Outputs are colored PASS/FAIL lines plus final process status `0` only when no failures occurred; see `tests/test-bash-validator-patterns.sh:20-34` and `tests/test-bash-validator-patterns.sh:197-214`. The test mutates only local shell counters.
- gates_or_invariants: The invariant is destination-sensitive write detection with bounded known limitations. The test explicitly accepts that `cp file1.md file2.md goal-tracker.md` is not detected because the implementation expects a two-argument `cp src dest` form; see `tests/test-bash-validator-patterns.sh:165-168`. It also requires read commands such as `cat`, `grep`, `head`, `tail`, `diff`, and variable redirection like `echo x > $FILE` not to match; see `tests/test-bash-validator-patterns.sh:142-153` and `tests/test-bash-validator-patterns.sh:170-172`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:11-13`. The tested helper is implemented at `hooks/lib/loop-common.sh:1071-1096`; it checks regex families for redirection, `tee`, in-place editors, file operations, and descriptor writes. The hook caller uses it to block protected writes in `hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:336-357`, `hooks/loop-bash-validator.sh:365-392`, and `hooks/loop-bash-validator.sh:407-440`.
- edge_cases_or_failure_modes: Case-insensitivity is achieved by pre-lowercasing (`tests/test-bash-validator-patterns.sh:40-43`, `hooks/lib/loop-common.sh:395-397`) and is validated with uppercase input at `tests/test-bash-validator-patterns.sh:80`. The implementation can miss complex quoted filenames with spaces, multi-source `cp`, variable-resolved targets, or shell expansions because it is regex-based rather than an AST parser. Because `state\.md` also matches `finalize-state.md`, the validator hook checks `finalize-state.md` first; see `hooks/loop-bash-validator.sh:131-149`.
- validation_or_tests: This file is itself the validator. It exits nonzero if any expected match or non-match fails; see `tests/test-bash-validator-patterns.sh:206-214`. It provides direct regression coverage for the file-protection engine used in active loop command gating.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the `.humanize/` git-add protection gate. It explains why broad or direct `git add` commands are blocked and gives safe staging alternatives. See `prompt-template/block/git-add-humanize.md:1-7`.
- algorithmic_behavior: This template does not perform detection itself; it is rendered when `git_adds_humanize` returns true. The detection algorithm splits shell command segments, identifies `git add`, normalizes quotes, blocks direct `.humanize` references, force-plus-broad staging, and `git add -A/--all` when `.humanize` exists; see `hooks/lib/loop-common.sh:930-1025`. The block message is loaded by `git_add_humanize_blocked_message` at `hooks/lib/loop-common.sh:1030-1065`, then emitted by the Bash validator at `hooks/loop-bash-validator.sh:109-118`.
- inputs_outputs_state: Inputs are the blocked Bash command and current repo state, especially whether `.humanize` exists and whether `git check-ignore -q .humanize` succeeds. Output is markdown guidance listing allowed specific-path staging (`git add <specific-file>`, `git add src/`, `git add -p`) at `prompt-template/block/git-add-humanize.md:8-15`, and blocked examples at `prompt-template/block/git-add-humanize.md:16-25`. No repository state is mutated by the template.
- gates_or_invariants: The invariant is that local loop state under `.humanize/` must not enter version control. The template reinforces that `.humanize/` is local state and should already be ignored at `prompt-template/block/git-add-humanize.md:3-4`. It warns that the commit message used to add ignore rules must not contain the literal protected string, avoiding re-triggering this protection; see `prompt-template/block/git-add-humanize.md:26-34`.
- dependencies_and_callers: Called via `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` in `hooks/lib/loop-common.sh:1065`. The caller is `hooks/loop-bash-validator.sh`, which runs only for Bash tool input after JSON validation and loop discovery; see `hooks/loop-bash-validator.sh:24-48` and `hooks/loop-bash-validator.sh:54-67`.
- edge_cases_or_failure_modes: The detector blocks direct `.humanize` references regardless of existence, but non-force broad `git add .` is blocked only when `.humanize` exists and is not ignored; see `hooks/lib/loop-common.sh:967-1022`. Force broad adds are blocked even without checking ignore status because force bypasses ignore rules; see `hooks/lib/loop-common.sh:975-1003`. The template’s own “already listed in `.gitignore`” line can be inaccurate for a repo where `.humanize` exists but is not ignored, though the remediation section covers adding `.humanize*`.
- validation_or_tests: Indirectly exercised by validator tests around `.humanize` protection, including references in `tests/test-template-references.sh` and Bash validator coverage. The assigned test `tests/test-bash-validator-patterns.sh` does not cover `git_adds_humanize`; it covers the adjacent `command_modifies_file` primitive.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-109 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt block defining the handoff contract for Claude to request Goal Tracker updates instead of directly mutating protected tracker state. It is a governance template for RLCR goal/task state evolution. See `prompt-template/claude/goal-tracker-update-request.md:2-16`.
- algorithmic_behavior: The template instructs Claude to include a structured `Goal Tracker Update Request` section in its summary when tracker changes are needed. Requested changes can mark tasks complete with evidence, add open issues, record plan evolution, or defer tasks with impact notes; see `prompt-template/claude/goal-tracker-update-request.md:4-13`. The algorithmic transition is request-based: Claude proposes changes, then Codex reviews and updates the Goal Tracker only if justified at `prompt-template/claude/goal-tracker-update-request.md:16`.
- inputs_outputs_state: Input is Claude’s assessment during a round that Goal Tracker state should change. Output is markdown embedded into the round summary, not a direct file mutation. The eventual state target is `.humanize/rlcr/<timestamp>/goal-tracker.md`, whose normal-mode structure includes immutable goal/acceptance criteria and mutable plan/task/open-issue sections; see `scripts/setup-rlcr-loop.sh:765-854`.
- gates_or_invariants: The gate is separation of duties: after initial setup, Claude should not directly change the Goal Tracker through Bash. The Bash validator blocks `goal-tracker.md` modification attempts and either points Round 0 toward proper Write/Edit behavior or later rounds toward summary-based requests; see `hooks/loop-bash-validator.sh:343-357`. The initial Goal Tracker itself declares immutable and mutable sections and requires task states such as Active, Completed, or Deferred in `scripts/setup-rlcr-loop.sh:768-777`.
- dependencies_and_callers: Appended to next-round prompts by `hooks/loop-codex-stop-hook.sh:1552-1557`, with a fallback if the template is missing. It coordinates with `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1098-1105` and the setup-generated tracker at `scripts/setup-rlcr-loop.sh:737-856`.
- edge_cases_or_failure_modes: If the template is missing, the hook falls back to a one-line instruction, reducing structure but preserving the request gate; see `hooks/loop-codex-stop-hook.sh:1553-1556`. If Claude omits justification, Codex has insufficient basis to update the tracker. In `--skip-impl` mode the Goal Tracker is simplified and explicitly not used for acceptance-criteria tracking; see `scripts/setup-rlcr-loop.sh:739-760`.
- validation_or_tests: Template rendering is covered by template reference/comprehensive tests, including `tests/test-templates-comprehensive.sh` references to Goal Tracker templates. Runtime enforcement is covered through the Bash validator’s goal-tracker write block at `hooks/loop-bash-validator.sh:349-357`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-139 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Large executable robustness specification for `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`. It covers argument parsing, plan-file validation, git and YAML safety, mutual exclusion between loop types, symlink protection, timeout handling, full-review cadence, and `--skip-impl` setup behavior. See `tests/robustness/test-setup-scripts-robustness.sh:3-10`.
- algorithmic_behavior: The test constructs isolated temporary repos, initializes git history, creates minimal plans, then invokes setup scripts with `CLAUDE_PROJECT_DIR` pointed at each fixture; see helper definitions at `tests/robustness/test-setup-scripts-robustness.sh:15-20`, `tests/robustness/test-setup-scripts-robustness.sh:31-87`. It asserts specific failure or progression points by matching exit codes and output substrings across 45 scenarios, ending with `print_test_summary` and returning its status at `tests/robustness/test-setup-scripts-robustness.sh:1069-1074`.
- inputs_outputs_state: Inputs are CLI options such as `--max`, `--codex-timeout`, `--codex-model`, `--plan-file`, `--track-plan-file`, `--full-review-round`, and `--skip-impl`; fixture state includes git repos, `.gitignore`, active `.humanize/rlcr` or `.humanize/pr-loop` dirs, symlinks, and mock `codex`, `gh`, `timeout`, and `gtimeout` commands. Outputs are pass/fail counters from `tests/test-helpers.sh:30-77`, setup-script state files under temporary `.humanize` dirs, and process status.
- gates_or_invariants: RLCR parsing rejects missing plan except in `--skip-impl`, duplicate positional plus `--plan-file`, unknown options, nonnumeric or negative numeric parameters, and `--full-review-round` below 2; see tests at `tests/robustness/test-setup-scripts-robustness.sh:96-214` and `tests/robustness/test-setup-scripts-robustness.sh:876-965`, mapped to implementation at `scripts/setup-rlcr-loop.sh:120-224`. Plan files must be relative, no spaces, no shell metacharacters, not symlinks, not through symlink parents, exist, have at least 5 lines, and have at least 3 meaningful non-comment content lines; see tests at `tests/robustness/test-setup-scripts-robustness.sh:224-341` and implementation at `scripts/setup-rlcr-loop.sh:320-385`, `scripts/setup-rlcr-loop.sh:480-536`.
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `scripts/portable-timeout.sh` at `tests/robustness/test-setup-scripts-robustness.sh:15-19`. It directly calls `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh` via wrappers at `tests/robustness/test-setup-scripts-robustness.sh:65-87`. It depends on git and uses mock binaries in fixture-local `bin/` dirs to force later validation stages without requiring real Codex or GitHub auth.
- edge_cases_or_failure_modes: The suite checks Git repo absence and no-commit repos (`tests/robustness/test-setup-scripts-robustness.sh:412-442`), tracked plan rejection without `--track-plan-file` (`tests/robustness/test-setup-scripts-robustness.sh:444-463`), active RLCR/PR mutual exclusion (`tests/robustness/test-setup-scripts-robustness.sh:535-595`), plan symlink and parent symlink rejection (`tests/robustness/test-setup-scripts-robustness.sh:605-658`), timeout exit `124` handling (`tests/robustness/test-setup-scripts-robustness.sh:801-856`), and portable git path assumptions (`tests/robustness/test-setup-scripts-robustness.sh:858-866`). One notable current behavior is that timeout value `0` is accepted because validation uses `^[0-9]+$`; see `tests/robustness/test-setup-scripts-robustness.sh:749-765` and `scripts/setup-rlcr-loop.sh:152-162`.
- validation_or_tests: This file is itself validation. It cross-checks implementation details such as YAML-unsafe branch/model rejection (`tests/robustness/test-setup-scripts-robustness.sh:351-402`, `scripts/setup-rlcr-loop.sh:561-587`), PR-loop bot and GitHub CLI gates (`tests/robustness/test-setup-scripts-robustness.sh:473-525`, `scripts/setup-pr-loop.sh:110-190`, `scripts/setup-pr-loop.sh:243-269`), and `--skip-impl` artifacts including `.review-phase-started`, `review_started: true`, and placeholder-free goal tracker (`tests/robustness/test-setup-scripts-robustness.sh:975-1067`, `scripts/setup-rlcr-loop.sh:673-731`, `scripts/setup-rlcr-loop.sh:739-760`). I did not execute it because this assignment is read-only research and the suite creates temporary repos and loop state.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`