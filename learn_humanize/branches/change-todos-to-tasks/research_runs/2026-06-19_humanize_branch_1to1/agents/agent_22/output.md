# agent_22 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is a thin command wrapper around `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` with a documented algorithm contract for iterative implementation, Codex review, review-phase transition, and finalization. The command metadata limits allowed tooling to that setup script (`commands/start-rlcr-loop.md:1-5`) and executes it with user arguments (`commands/start-rlcr-loop.md:10-14`).
- algorithmic_behavior: The loop is defined as a two-phase state machine: implementation work produces a round summary, Codex reviews the summary, `COMPLETE` transitions into Review Phase, `codex review --base <branch>` checks code quality, issue markers continue the loop, and a clean review enters finalization (`commands/start-rlcr-loop.md:16-25`, `commands/start-rlcr-loop.md:60-67`). It also establishes a Goal Tracker with immutable goal/acceptance criteria and mutable task/evolution/deferral state to prevent drift (`commands/start-rlcr-loop.md:27-44`). The referenced setup script implements argument parsing (`scripts/setup-rlcr-loop.sh:120-224`), mutual exclusion with PR/RLCR loops (`scripts/setup-rlcr-loop.sh:240-261`), validation, state creation, prompt generation, and startup output.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, a plan path or `--plan-file`, loop options such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, and `--skip-impl` (`commands/start-rlcr-loop.md:3`, `scripts/setup-rlcr-loop.sh:48-76`). Setup reads `CLAUDE_PROJECT_DIR` or `pwd` as project root (`scripts/setup-rlcr-loop.sh:230`). Outputs are a timestamped `.humanize/rlcr/<timestamp>` loop directory, plan backup or skip placeholder, `state.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:667-730`, `scripts/setup-rlcr-loop.sh:737-970`). Initial state records `current_round: 0`, max/codex settings, plan tracking, branches, base commit, and `review_started`, which starts true only in skip-implementation mode (`scripts/setup-rlcr-loop.sh:706-725`).
- gates_or_invariants: Only one active RLCR or PR loop may exist (`scripts/setup-rlcr-loop.sh:240-261`). Normal mode requires a valid relative plan path, no spaces or shell metacharacters, no symlink traversal, a readable in-project file, not in a submodule, correct git tracking behavior, at least five lines, and at least three meaningful content lines (`scripts/setup-rlcr-loop.sh:320-536`). Codex CLI is mandatory (`scripts/setup-rlcr-loop.sh:543-549`). Branch/model/effort/base branch values are YAML-safety checked, and the base branch must be local for `codex review` (`scripts/setup-rlcr-loop.sh:555-661`). The command-level stop gates are max iterations, Codex completion plus clean code review, or explicit cancellation (`commands/start-rlcr-loop.md:54-58`).
- dependencies_and_callers: Depends directly on `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, git, and Codex CLI. Runtime continuation is coordinated by stop hooks, especially `hooks/loop-codex-stop-hook.sh`, which enforces git cleanliness and creates next-round prompts. The command is surfaced in docs and referenced by README/help paths, but this file is the command entry definition.
- edge_cases_or_failure_modes: `--skip-impl` allows no plan and creates a placeholder plan plus review-phase marker (`commands/start-rlcr-loop.md:69-86`, `scripts/setup-rlcr-loop.sh:272-291`, `scripts/setup-rlcr-loop.sh:675-731`). `--track-plan-file` is ignored if skip mode has no real plan (`scripts/setup-rlcr-loop.sh:278-282`). Explicit and positional plan files conflict (`scripts/setup-rlcr-loop.sh:263-267`). Remote-only base branches are rejected because `codex review` requires a local branch (`scripts/setup-rlcr-loop.sh:594-610`). Output failure from a closed pipe is trapped late to exit cleanly after setup work is complete (`scripts/setup-rlcr-loop.sh:976-978`).
- validation_or_tests: This file is declarative, but its referenced implementation has extensive validation in `scripts/setup-rlcr-loop.sh`. Related robustness coverage exists in plan-file and loop tests outside the assigned item list. No repository files were modified during inspection.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh` error handling. It verifies that missing templates, missing directories, empty content, malformed placeholders, strict shell mode, safe fallbacks, and template-dir validation do not crash or produce unexpected state (`tests/test-error-scenarios.sh:1-15`).
- algorithmic_behavior: The script sources `template-loader.sh`, computes `TEMPLATE_DIR`, tracks pass/fail counters, and exits nonzero if any assertion fails (`tests/test-error-scenarios.sh:11-24`, `tests/test-error-scenarios.sh:211-225`). It exercises `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir`. The implementation under test returns empty output with warning for missing files (`hooks/lib/template-loader.sh:36-48`), performs single-pass placeholder substitution via an AWK scanner (`hooks/lib/template-loader.sh:58-132`), falls back on missing or empty templates (`hooks/lib/template-loader.sh:170-203`), and validates required template subdirectories (`hooks/lib/template-loader.sh:208-221`).
- inputs_outputs_state: Inputs include real template directory resolution from repo root (`tests/test-error-scenarios.sh:11-15`), non-existing template paths, synthetic templates, fallback strings, and `VAR=value` render assignments. Outputs are terminal PASS/FAIL lines and process exit status. State is limited to `TESTS_PASSED` and `TESTS_FAILED`, mutated only by local `pass` and `fail` helpers (`tests/test-error-scenarios.sh:22-35`).
- gates_or_invariants: Missing template file and missing template directory must return empty string with exit 0 (`tests/test-error-scenarios.sh:42-65`). `load_and_render` with a missing template must return empty (`tests/test-error-scenarios.sh:67-78`). Empty content must render empty (`tests/test-error-scenarios.sh:80-91`). Special regex characters in values must be preserved literally (`tests/test-error-scenarios.sh:93-106`). Strict `set -euo pipefail` callers must continue after missing templates (`tests/test-error-scenarios.sh:108-131`). Safe rendering must use fallbacks and substitute variables in fallback content (`tests/test-error-scenarios.sh:133-158`). Valid template directories are accepted and invalid ones rejected (`tests/test-error-scenarios.sh:160-180`). Empty and unclosed placeholders are expected to remain unchanged (`tests/test-error-scenarios.sh:182-206`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`. The same loader is widely used by RLCR/PR hooks and validators; examples include dirty-git stop blocks, read/write/edit/bash validators, next-round prompts, and PR-loop prompt generation found by repository search.
- edge_cases_or_failure_modes: This test specifically covers missing files/directories, empty input, regex-special replacement values, strict-mode command substitution, fallback substitution, invalid directories, empty placeholder names, and unclosed placeholders. It also indirectly guards against `set -e` surprises because the top-level script uses `set -uo pipefail` rather than `set -e`, while test 6 creates a strict-mode subshell (`tests/test-error-scenarios.sh:9`, `tests/test-error-scenarios.sh:114-126`).
- validation_or_tests: Attempted `bash tests/test-error-scenarios.sh` in the read-only branch export. Result: 8 passed, 4 failed. The failures coincided with `hooks/lib/template-loader.sh: line 129: cannot create temp file for here document: Operation not permitted`, because `render_template` uses a here-string at `hooks/lib/template-loader.sh:129` and the managed read-only sandbox prevented shell temp-file creation. Failed assertions were special-character rendering, fallback substitution, empty placeholder preservation, and unclosed placeholder preservation; the observed empty outputs are consistent with the sandbox temp-file failure rather than normal writable execution.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-082 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Prompt block template defining the RLCR stop-gate response when the user tries to exit while git has uncommitted changes. It turns dirty git state into explicit required actions and commit hygiene constraints (`prompt-template/block/git-not-clean.md:1-16`).
- algorithmic_behavior: The stop hook checks cached git status, builds `GIT_ISSUES` and optional `SPECIAL_NOTES`, renders this template through `load_and_render_safe`, and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:517-570`). The template itself interpolates `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then instructs the worker to review untracked files, stage real changes, commit descriptively, and retry exit (`prompt-template/block/git-not-clean.md:3-10`, `prompt-template/block/git-not-clean.md:16`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`. `SPECIAL_NOTES` can include `.humanize*` local-artifact guidance or generic untracked-file guidance loaded from sibling templates (`hooks/loop-codex-stop-hook.sh:526-546`). Output is markdown text embedded as the `reason` in a hook JSON response with `decision: block` and a concise system message (`hooks/loop-codex-stop-hook.sh:562-569`). State transition is from attempted stop to blocked stop; no loop round advances until the git condition is resolved.
- gates_or_invariants: Git must be clean before expensive Codex review continues (`hooks/loop-codex-stop-hook.sh:512-515`, `hooks/loop-codex-stop-hook.sh:549-570`). The template requires real changes to be staged and committed, build artifacts to be ignored, commit messages to follow project conventions, and no AI authorship trailers such as Claude co-author attribution (`prompt-template/block/git-not-clean.md:5-15`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:558-560`). Rendering depends on `hooks/lib/template-loader.sh`; missing-template resilience is provided by the fallback block at `hooks/loop-codex-stop-hook.sh:552-557`.
- edge_cases_or_failure_modes: If the template is missing, the hook falls back to an inline dirty-git message. If `.humanize*` files are untracked, special local-artifact guidance is included; if other untracked files exist, a separate untracked note is appended (`hooks/loop-codex-stop-hook.sh:529-546`). The gate treats any cached git status as uncommitted changes, covering staged, unstaged, and untracked paths.
- validation_or_tests: Covered indirectly by template loader tests and template reference checks. Assigned test execution showed loader rendering can be disrupted in the current read-only sandbox by shell temp-file restrictions, but the stop-hook fallback path exists for missing templates.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-112 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt template for the post-Full-Goal-Alignment transition. It is appended to the next-round Claude prompt after a full alignment audit finds issues, focusing the next implementation round on forgotten items, unmet acceptance criteria, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2-7`).
- algorithmic_behavior: After Codex review finds issues and the loop advances to the next round, the stop hook updates `current_round`, creates `round-N-prompt.md`, renders the main next-round prompt, then conditionally appends this template when `FULL_ALIGNMENT_CHECK` is true (`hooks/loop-codex-stop-hook.sh:1506-1535`). This file has no placeholders; it is a static behavioral nudge in the loop’s prompt-generation algorithm.
- inputs_outputs_state: Inputs are the boolean full-alignment condition and the existence/content of this template file. Output is appended markdown in the next prompt. State transition happens around it: `state.md` is rewritten from current round to next round before prompt creation (`hooks/loop-codex-stop-hook.sh:1506-1513`). The template then influences the next round’s task prioritization without directly mutating files beyond the prompt append.
- gates_or_invariants: The invariant is that full alignment feedback must be actioned rather than ignored: forgotten tasks are addressed, unmet acceptance criteria are prioritized, and unjustified deferrals are un-deferred (`prompt-template/claude/post-alignment-action-items.md:4-7`). The cadence is configurable through `--full-review-round`, with the command documentation noting a default interval and setup enforcing a minimum of 2 (`commands/start-rlcr-loop.md:37-39`, `scripts/setup-rlcr-loop.sh:188-201`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` using `load_template`, not `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1529-1534`). It coordinates with `prompt-template/claude/next-round-prompt.md`, `next-round-footer.md`, optional push notes, and goal-tracker update request templates in the same prompt-building sequence (`hooks/loop-codex-stop-hook.sh:1515-1557`).
- edge_cases_or_failure_modes: If the template is missing or empty, the hook silently skips appending it because it only appends when `POST_ALIGNMENT` is non-empty (`hooks/loop-codex-stop-hook.sh:1531-1534`). Because it is static, malformed placeholder substitution is not a risk; the main risk is absence causing weaker post-alignment guidance.
- validation_or_tests: Covered indirectly by template reference coverage and by any stop-hook tests that exercise full-alignment prompt generation. No direct dedicated assertion for this exact template was observed in the assigned test files.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-142 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for the template system under broader error and edge conditions than the basic error scenario test. It stress-checks missing templates, malformed placeholders, invalid names, unusual values, filesystem cases, concurrency, and discovery behavior (`tests/robustness/test-template-error-robustness.sh:1-10`).
- algorithmic_behavior: The script runs under strict shell mode, sources `template-loader.sh` and shared test helpers, creates a temporary test directory, executes grouped assertions, prints a helper-managed summary, and exits with the helper return code (`tests/robustness/test-template-error-robustness.sh:13-20`, `tests/robustness/test-template-error-robustness.sh:340-341`). It validates graceful behavior more than exact rendering in several malformed cases, accepting non-empty output or fallback where platform differences are expected.
- inputs_outputs_state: Inputs are generated temporary templates and values in `TEST_DIR`, including empty files, malformed placeholders, nested braces, invalid variable names, template syntax inside values, multiline values, very long names, BOM content, whitespace-only templates, filenames with spaces, permission-denied files, concurrent loads, subdirectories, and symlinks (`tests/robustness/test-template-error-robustness.sh:34-334`). Outputs are PASS/FAIL/SKIP-like helper messages and final exit status. State is in `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, and `TEST_DIR`, provided by `tests/test-helpers.sh:22-89`.
- gates_or_invariants: `load_and_render_safe` must fall back for missing files and directories (`tests/robustness/test-template-error-robustness.sh:34-51`). Empty templates must be handled gracefully (`tests/robustness/test-template-error-robustness.sh:53-63`). Malformed placeholders and invalid variable names must not crash (`tests/robustness/test-template-error-robustness.sh:73-155`). Values containing template syntax must not recursively expand (`tests/robustness/test-template-error-robustness.sh:165-174`). Newlines and long names must be handled (`tests/robustness/test-template-error-robustness.sh:176-198`). Filesystem edge cases must gracefully handle BOM, whitespace, special filenames, permissions, concurrent reads, nested dirs, and symlinks (`tests/robustness/test-template-error-robustness.sh:208-334`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` and `tests/test-helpers.sh`. The helpers create and clean a temp directory with `mktemp -d` and an EXIT trap (`tests/test-helpers.sh:84-89`). Template loader behavior under test is the same core renderer used throughout RLCR/PR hooks and validators.
- edge_cases_or_failure_modes: Permission-denied assertions are conditional on not running as root (`tests/robustness/test-template-error-robustness.sh:245-260`). Symlink and special filename cases degrade to pass-style skips if unsupported (`tests/robustness/test-template-error-robustness.sh:228-243`, `tests/robustness/test-template-error-robustness.sh:320-334`). Concurrent loading starts ten background jobs and requires zero failures plus source-file preservation (`tests/robustness/test-template-error-robustness.sh:270-299`).
- validation_or_tests: Attempted `bash tests/robustness/test-template-error-robustness.sh` in the read-only branch export. It aborted before test execution because `mktemp` could not create a temporary directory under `/var/folders/...`: `Operation not permitted`. This is an environment restriction from the managed read-only sandbox, not evidence about the template-loader algorithm itself.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence sections present above, one per assigned table row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`