# agent_18 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling an active PR loop. It is not the cancellation algorithm itself; it delegates all state mutation to `scripts/cancel-pr-loop.sh` and constrains the assistant-facing workflow.
- algorithmic_behavior: The command metadata allows only two Bash invocations, `cancel-pr-loop.sh` and `cancel-pr-loop.sh --force`, via `allowed-tools` in `commands/cancel-pr-loop.md:3`. The body instructs the agent to run the script at `commands/cancel-pr-loop.md:11-15`, inspect the first output token at `commands/cancel-pr-loop.md:17-20`, and translate script status into user-facing messaging. It explicitly states that active PR-loop detection is based on `state.md` in the newest `.humanize/pr-loop/` directory at `commands/cancel-pr-loop.md:21`.
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT`, the optional `--force` form, and the script output token. The delegated script uses `CLAUDE_PROJECT_DIR` or `pwd` as project root, scans `.humanize/pr-loop/*/`, and requires `state.md` in the newest loop directory (`scripts/cancel-pr-loop.sh:72-98`). Successful output begins with `CANCELLED` and the script renames `state.md` to `cancel-state.md` after creating `.cancel-requested` (`scripts/cancel-pr-loop.sh:118-130`). No-loop outputs are `NO_LOOP` or `NO_ACTIVE_LOOP` (`scripts/cancel-pr-loop.sh:78-97`).
- gates_or_invariants: The command invariant is separation between command orchestration and script-owned mutation: “script handles all cancellation logic” at `commands/cancel-pr-loop.md:21`. Scope is PR-loop-only; RLCR loops are explicitly excluded at `commands/cancel-pr-loop.md:25`. The delegated script only treats a newest PR-loop directory with `state.md` as active; finished/cancelled state files are not active for this command.
- dependencies_and_callers: Depends directly on `scripts/cancel-pr-loop.sh`. Related setup surfaces advertise `/humanize:cancel-pr-loop` as the PR-loop cancellation path (`scripts/setup-pr-loop.sh` references found by search). The PR-loop helper equivalent is `find_active_pr_loop`, which also checks only newest PR-loop directory plus `state.md` (`hooks/lib/loop-common.sh:795-814`), though the script currently implements direct `ls | sort -r | head -1` scanning.
- edge_cases_or_failure_modes: If no `.humanize/pr-loop/*/` directory exists, the delegated script returns `NO_LOOP` and exits 1 (`scripts/cancel-pr-loop.sh:75-81`). If a newest loop directory exists without `state.md`, it returns `NO_ACTIVE_LOOP` and exits 1 (`scripts/cancel-pr-loop.sh:91-98`). `--force` is accepted but has no current additional effect (`scripts/cancel-pr-loop.sh:24-31`, `scripts/cancel-pr-loop.sh:39-41`). Unknown options exit 3 (`scripts/cancel-pr-loop.sh:60-64`). The newest-directory-only rule means an older active-looking `state.md` is ignored if a newer loop directory is not active.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` validates help output, `NO_LOOP` behavior, and active cancellation side effects. The active-loop test creates `.humanize/pr-loop/<timestamp>/state.md`, runs the script, expects `CANCELLED`, and verifies `cancel-state.md` exists while `state.md` is gone (`tests/test-pr-loop-scripts.sh:211-244`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-048 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for ANSI escape stripping and pass/fail count extraction used by the aggregate test runner. It protects cross-platform parsing of colored shell test summaries.
- algorithmic_behavior: The test defines colored local `pass`/`fail` helpers and counters (`tests/test-ansi-parsing.sh:13-31`), then exercises the exact stripping expression used by the runner: `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"` (`tests/test-ansi-parsing.sh:45-48`). It verifies eight cases: basic color stripping, multiple color segments, `Passed:` extraction, `Failed:` extraction, zero failures, multi-line suite output with `tail -1`, plain text without ANSI, and combined bold/color codes (`tests/test-ansi-parsing.sh:42-158`).
- inputs_outputs_state: Inputs are synthetic shell strings containing ANSI SGR sequences and plain summary lines. Outputs are local test pass/fail counters and process exit status. The parser extracts numeric counts with `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'`, analogous logic for `Failed:`, and `tail -1` for multi-line summaries (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`, `tests/test-ansi-parsing.sh:124-125`). Final state is `TESTS_PASSED`/`TESTS_FAILED`, printed with color, then exit 0 only when failures are zero (`tests/test-ansi-parsing.sh:163-178`).
- gates_or_invariants: The main invariant is portable ANSI stripping on GNU/Linux and BSD/macOS sed, stated in `tests/test-ansi-parsing.sh:5-6` and implemented with Bash ANSI-C quoting at `tests/test-ansi-parsing.sh:38-40`. The regex only strips SGR-style `ESC[` sequences ending in `m`; it deliberately validates the project’s summary-count parsing, not arbitrary terminal control parsing.
- dependencies_and_callers: Mirrors `tests/run-all-tests.sh`, which captures each suite’s output, strips ANSI, extracts passed/failed totals, and aggregates them (`tests/run-all-tests.sh:104-128`). The parser depends on `bash`, `sed`, and `grep -E`; comments identify compatibility with both GNU and BSD sed. It is part of the broader tests surface because many project tests print colored `Passed:`/`Failed:` summaries, including shared helpers in `tests/test-helpers.sh:58-78`.
- edge_cases_or_failure_modes: Covered edge cases include multiple color spans in one line (`tests/test-ansi-parsing.sh:55-67`), zero values (`tests/test-ansi-parsing.sh:97-109`), multi-line output where earlier lines could confuse extraction (`tests/test-ansi-parsing.sh:112-130`), no ANSI codes (`tests/test-ansi-parsing.sh:132-144`), and bold+color SGR parameters (`tests/test-ansi-parsing.sh:146-158`). Not covered: non-SGR ANSI sequences, lowercase labels, negative values, thousands separators, and missing summary lines. Because `set -uo pipefail` is used without `set -e` (`tests/test-ansi-parsing.sh:9`), individual failures accumulate rather than aborting immediately.
- validation_or_tests: The file is itself the executable validation asset. Its behavior directly validates `tests/run-all-tests.sh:121-125`, where the same stripping and count extraction logic controls aggregate pass/fail accounting.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Block prompt template for the PR-loop force-push gate. It defines the human/agent-facing transition contract after PR branch history is rewritten non-fast-forward.
- algorithmic_behavior: The template renders a blocking explanation that old and new commit SHAs differ in a non-fast-forward manner (`prompt-template/block/force-push-detected.md:3`), states that force pushes reset review state (`prompt-template/block/force-push-detected.md:5`), and requires a fresh bot-trigger comment before continuing (`prompt-template/block/force-push-detected.md:7-17`). Placeholders are `{{OLD_COMMIT}}`, `{{NEW_COMMIT}}`, and `{{BOT_MENTION_STRING}}`.
- inputs_outputs_state: Inputs come from `hooks/pr-loop-stop-hook.sh` when it detects that stored `latest_commit_sha` is no longer an ancestor of current `HEAD` (`hooks/pr-loop-stop-hook.sh:367-379`). Before rendering, the hook updates `state.md` with the new commit SHA/timestamp and clears `last_trigger_at` plus `trigger_comment_id` (`hooks/pr-loop-stop-hook.sh:395-409`). Output is a rendered markdown reason embedded in a JSON block decision with system message “PR Loop: Force push detected - please re-trigger bots” (`hooks/pr-loop-stop-hook.sh:414-420`).
- gates_or_invariants: The gate invariant is that stale review trigger state cannot survive a force push. The hook records the new commit before blocking, then requires a trigger comment after the rewritten head. The template’s required actions explicitly say tracking has been updated and a new trigger comment must be posted (`prompt-template/block/force-push-detected.md:7-10`).
- dependencies_and_callers: Called by `load_and_render_safe "$TEMPLATE_DIR" "block/force-push-detected.md"` in `hooks/pr-loop-stop-hook.sh:414-416`; if template loading fails, the hook has an inline fallback at `hooks/pr-loop-stop-hook.sh:411-413`. The bot mention string is constructed elsewhere and also appears in no-trigger and PR-loop task templates, but this template’s direct caller is the force-push detection block.
- edge_cases_or_failure_modes: If `gh pr view` cannot provide the new commit timestamp, the hook falls back to current UTC time (`hooks/pr-loop-stop-hook.sh:387-393`). If `CURRENT_HEAD` cannot be read or equals stored SHA, force-push detection does not fire (`hooks/pr-loop-stop-hook.sh:369-374`). If the old commit remains an ancestor, the change is treated as normal new commits rather than force push (`hooks/pr-loop-stop-hook.sh:375-379`). If template rendering fails, the fallback message is shorter and does not include all template details.
- validation_or_tests: I did not find a dedicated test file for this exact template, but the hook implementation provides the primary behavioral contract at `hooks/pr-loop-stop-hook.sh:367-420`. Related PR-loop system tests exercise PR loop state files broadly, but this assigned template is best classified as a transition/gate prompt used by the hook.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template for the RLCR/Codex loop path where code review is skipped. It defines the fallback validation contract before finalizing implementation.
- algorithmic_behavior: The prompt warns that code review was skipped with `{{REVIEW_SKIP_REASON}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:3`), places the agent in Finalize Phase (`prompt-template/claude/finalize-phase-skipped-prompt.md:5`), requires manual verification steps (`prompt-template/claude/finalize-phase-skipped-prompt.md:7-13`), optionally invokes `code-simplifier:code-simplifier` (`prompt-template/claude/finalize-phase-skipped-prompt.md:15-24`), and sets non-negotiable constraints around functionality-equivalent changes and test preservation (`prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`). It also references plan and goal tracker files and requires a finalize summary (`prompt-template/claude/finalize-phase-skipped-prompt.md:35-50`).
- inputs_outputs_state: Inputs are placeholders rendered by `enter_finalize_phase`: `FINALIZE_SUMMARY_FILE`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_SKIP_REASON`, `BASE_BRANCH`, and `START_BRANCH` (`hooks/loop-codex-stop-hook.sh:1070-1076`). The state transition happens before render: `state.md` is renamed to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:1031-1038`). Output is the block prompt returned to the agent for finalization duties, including required summary destination.
- gates_or_invariants: The key invariant is that skipped review does not imply validated completion. The prompt demands manual verification and test execution if possible (`prompt-template/claude/finalize-phase-skipped-prompt.md:9-13`, `prompt-template/claude/finalize-phase-skipped-prompt.md:49-50`). Constraints forbid behavior changes, test failures, new bugs, and non-equivalent refactors (`prompt-template/claude/finalize-phase-skipped-prompt.md:28-34`). The loop’s state-file invariant is that Finalize Phase uses `finalize-state.md`, which active-loop detection recognizes (`hooks/lib/loop-common.sh:151-174`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` inside `enter_finalize_phase` when `skip_reason` is non-empty (`hooks/loop-codex-stop-hook.sh:1043-1076`). The skip path is initialized around implementation completion; one explicit reason is missing `BASE_BRANCH`, which sets `REVIEW_SKIPPED=true` and `REVIEW_SKIP_REASON="No base_branch configured for code review"` (`hooks/loop-codex-stop-hook.sh:1410-1419`). A non-skipped sibling template exists at `prompt-template/claude/finalize-phase-prompt.md`.
- edge_cases_or_failure_modes: If template loading fails, the hook uses an inline fallback with a shorter but equivalent prompt (`hooks/loop-codex-stop-hook.sh:1044-1068`). If `BASE_BRANCH` is empty, review is skipped by design (`hooks/loop-codex-stop-hook.sh:1414-1419`). The prompt asks to commit changes, which is appropriate for normal loop execution but should be considered operational instruction, not a validation proof. A missing or stale `PLAN_FILE`/`GOAL_TRACKER_FILE` reference would reduce context quality but not block render because placeholders are just text.
- validation_or_tests: `tests/test-finalize-phase.sh` is the broader validation suite for Finalize Phase state transitions and validators; search hits show it tests `finalize-state.md` detection, write protection, completion to `complete-state.md`, and skipped/max-iteration behavior. The direct render call is covered by the implementation path in `hooks/loop-codex-stop-hook.sh:1031-1076`; I did not run tests in this read-only research pass.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-138 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR session discovery via `find_active_loop`. It tests how active loop detection behaves under multiple directories, missing state files, unusual names, symlinks, shallow/deep layout, and terminal state files.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared test helpers (`tests/robustness/test-session-robustness.sh:14-18`), creates a temporary test directory (`tests/robustness/test-session-robustness.sh:19`), and runs 18 focused cases. The function under test checks only the newest immediate child directory, returning it only if `state.md` or `finalize-state.md` exists (`hooks/lib/loop-common.sh:151-174`).
- inputs_outputs_state: Inputs are synthetic directory trees under `$TEST_DIR/rlcr`, with files such as `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`. Output is `RESULT=$(find_active_loop "$TEST_DIR/rlcr")`, expected to be either the selected session directory or empty. Test state is maintained through shared `pass`/`fail` counters from `tests/test-helpers.sh:30-44`, summarized by `print_test_summary` (`tests/robustness/test-session-robustness.sh:322-323`).
- gates_or_invariants: The central invariant is newest-directory-only selection: older sessions are ignored even if they contain active state (`tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:287-302`; implementation comment at `hooks/lib/loop-common.sh:151-154`). Active indicators are only `state.md` and `finalize-state.md` (`tests/robustness/test-session-robustness.sh:48-61`, `tests/robustness/test-session-robustness.sh:105-118`; implementation at `hooks/lib/loop-common.sh:168-170`). Finished/cancelled indicators are ignored (`tests/robustness/test-session-robustness.sh:259-285`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop` and `tests/test-helpers.sh` for result accounting and temp-directory cleanup. The tested function is used by RLCR stop hooks and validators, including `hooks/loop-codex-stop-hook.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and setup scripts. It coordinates with the loop state machine because `state.md` means normal active loop, `finalize-state.md` means Finalize Phase, and terminal files like `complete-state.md`/`cancel-state.md` should not revive sessions.
- edge_cases_or_failure_modes: Covered cases include empty base directory (`tests/robustness/test-session-robustness.sh:128-138`), nonexistent base directory (`tests/robustness/test-session-robustness.sh:140-148`), files but no subdirectories (`tests/robustness/test-session-robustness.sh:150-162`), no state files (`tests/robustness/test-session-robustness.sh:164-177`), unexpected session names (`tests/robustness/test-session-robustness.sh:179-192`), symlinked session directories (`tests/robustness/test-session-robustness.sh:194-208`), spaces in names (`tests/robustness/test-session-robustness.sh:210-222`), deep nested paths ignored because only one level is checked (`tests/robustness/test-session-robustness.sh:224-238`), rapid creation (`tests/robustness/test-session-robustness.sh:240-257`), and terminal states ignored (`tests/robustness/test-session-robustness.sh:259-302`). One label says “Unicode” but the fixture uses `session-test`, so it validates graceful handling generally rather than actual Unicode (`tests/robustness/test-session-robustness.sh:304-316`).
- validation_or_tests: The file is itself the validation suite. It also has overlap with `tests/robustness/test-state-transition-robustness.sh`, which further tests active/finalize/cancel transitions and multi-session behavior. I did not execute the test suite; this pass inspected the executable specification and linked implementation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 Item Evidence headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`