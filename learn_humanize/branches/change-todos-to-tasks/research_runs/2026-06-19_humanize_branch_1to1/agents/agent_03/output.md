# agent_03 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command contract layer for the Humanize plugin. The directory does not implement most state mutation itself; it defines command metadata, allowed tool surfaces, user-facing workflows, output interpretation rules, and delegates core behavior to scripts under `scripts/`.
- algorithmic_behavior: `commands/start-pr-loop.md` delegates to `scripts/setup-pr-loop.sh` and defines the PR review loop sequence: detect PR, fetch bot comments, fix issues, push, trigger review, poll, validate, and exit on approval/max/cancel (`commands/start-pr-loop.md:10`, `commands/start-pr-loop.md:16`, `commands/start-pr-loop.md:37`, `commands/start-pr-loop.md:58`). `commands/start-rlcr-loop.md` delegates to `scripts/setup-rlcr-loop.sh`, defining an implementation phase, Codex completion review, code review phase, finalize phase, and skip-implementation path (`commands/start-rlcr-loop.md:16`, `commands/start-rlcr-loop.md:22`, `commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:69`). `commands/cancel-pr-loop.md` and `commands/cancel-rlcr-loop.md` are wrappers around their cancel scripts and specify how first-line status tokens map to assistant behavior (`commands/cancel-pr-loop.md:17`, `commands/cancel-rlcr-loop.md:17`). `commands/gen-plan.md` defines a multi-phase plan-generation algorithm: IO validation, relevance check, draft analysis, user clarification, deterministic plan generation, final read/review, and optional language unification (`commands/gen-plan.md:19`, `commands/gen-plan.md:30`, `commands/gen-plan.md:50`, `commands/gen-plan.md:76`, `commands/gen-plan.md:111`, `commands/gen-plan.md:142`, `commands/gen-plan.md:251`).
- inputs_outputs_state: Inputs are slash-command arguments and environment-provided `CLAUDE_PLUGIN_ROOT`; each command constrains tools through frontmatter. Start commands output prompts/state via setup scripts. Cancel commands interpret script output tokens such as `NO_LOOP`, `NO_ACTIVE_LOOP`, `CANCELLED`, `FINALIZE_NEEDS_CONFIRM`, and `CANCELLED_FINALIZE` (`commands/cancel-rlcr-loop.md:17`). `gen-plan` expects `--input` and `--output`; its validation script pre-creates output from a template and the final command edits that output rather than using Write (`commands/gen-plan.md:32`, `commands/gen-plan.md:253`, `commands/gen-plan.md:257`).
- gates_or_invariants: Commands intentionally centralize mutation in scripts. Cancel PR is scoped only to `.humanize/pr-loop/` and explicitly does not affect `.humanize/rlcr/` (`commands/cancel-pr-loop.md:21`, `commands/cancel-pr-loop.md:25`). Cancel RLCR requires confirmation during Finalize Phase unless forced (`commands/cancel-rlcr-loop.md:21`, `commands/cancel-rlcr-loop.md:23`, `commands/cancel-rlcr-loop.md:31`). Start commands enforce “no cheating” by prohibiting direct state edits or cancel commands to escape loop logic (`commands/start-pr-loop.md:55`, `commands/start-rlcr-loop.md:51`). `gen-plan` gates on validation exit codes 0-7 and requires generated acceptance criteria to use AC-X format with positive and negative tests (`commands/gen-plan.md:38`, `commands/gen-plan.md:235`, `commands/gen-plan.md:239`).
- dependencies_and_callers: Depends on `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, and `scripts/validate-gen-plan-io.sh` via allowed tools (`commands/start-pr-loop.md:4`, `commands/start-rlcr-loop.md:4`, `commands/cancel-pr-loop.md:3`, `commands/cancel-rlcr-loop.md:3`, `commands/gen-plan.md:5`). The command directory coordinates with prompt templates and hooks indirectly through setup scripts and loop validators.
- edge_cases_or_failure_modes: Misuse is mostly handled by delegated scripts. Command specs cover missing active loops, finalize-phase confirmation, missing/invalid gen-plan IO, irrelevant drafts, contradictory drafts, quantitative metrics that need clarification, mixed-language generated plans, and attempts to bypass loop state. `gen-plan` has an explicit failure path for incomplete plan generation (`commands/gen-plan.md:296`).
- validation_or_tests: `tests/run-all-tests.sh` includes PR-loop suites, finalize-phase tests, cancel-signal tests, gen-plan tests, and robustness tests (`tests/run-all-tests.sh:32`). `tests/test-pr-loop.sh` sources `tests/test-pr-loop-scripts.sh` and related hook/stophook modules (`tests/test-pr-loop.sh:5`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`). Command behavior is also constrained by setup/cancel script tests and hook validators referenced in later item sections.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-033 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Implements the RLCR cancellation state transition. It identifies the newest RLCR loop, determines whether it is a normal loop or Finalize Phase, creates the cancellation signal, renames the active state file to `cancel-state.md`, and emits a machine-readable status token.
- algorithmic_behavior: Parses `--force` and help; unknown options exit with code 3 (`scripts/cancel-rlcr-loop.sh:24`, `scripts/cancel-rlcr-loop.sh:26`, `scripts/cancel-rlcr-loop.sh:57`). Resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, scans `.humanize/rlcr/*/`, sorts descending, and selects the newest directory (`scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:73`). Treats `state.md` as normal-loop active state and `finalize-state.md` as finalize active state (`scripts/cancel-rlcr-loop.sh:85`, `scripts/cancel-rlcr-loop.sh:89`, `scripts/cancel-rlcr-loop.sh:92`). If finalize and not forced, returns `FINALIZE_NEEDS_CONFIRM` with loop metadata and exits 2 (`scripts/cancel-rlcr-loop.sh:117`). Otherwise it touches `.cancel-requested` then moves the active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`).
- inputs_outputs_state: Input is optional `--force`; environment input is `CLAUDE_PROJECT_DIR`. Outputs are status lines: `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, or `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:76`, `scripts/cancel-rlcr-loop.sh:96`, `scripts/cancel-rlcr-loop.sh:118`, `scripts/cancel-rlcr-loop.sh:145`, `scripts/cancel-rlcr-loop.sh:149`). State mutation is limited to `.cancel-requested` creation and renaming `state.md` or `finalize-state.md` to `cancel-state.md`.
- gates_or_invariants: Finalize Phase is protected by a confirmation gate unless `--force` is present (`scripts/cancel-rlcr-loop.sh:117`). Missing loop directory and loop directory without active state are distinct failure states (`scripts/cancel-rlcr-loop.sh:75`, `scripts/cancel-rlcr-loop.sh:95`). Round metadata extraction is best-effort from YAML-style fields and defaults to `?` if absent (`scripts/cancel-rlcr-loop.sh:105`, `scripts/cancel-rlcr-loop.sh:110`). The broader hook invariant is that state-file `mv` is only permitted when `.cancel-requested` exists and the command exactly moves `state.md` or `finalize-state.md` to `cancel-state.md`; this is enforced in `hooks/lib/loop-common.sh:533` and `hooks/loop-bash-validator.sh:133`.
- dependencies_and_callers: Called by `commands/cancel-rlcr-loop.md`, which decides whether to ask the user and rerun with `--force` (`commands/cancel-rlcr-loop.md:11`, `commands/cancel-rlcr-loop.md:23`, `commands/cancel-rlcr-loop.md:31`). Works with `hooks/lib/loop-common.sh` cancel authorization, especially signal-file checks, injection rejection, exact source/destination matching, and symlink rejection (`hooks/lib/loop-common.sh:555`, `hooks/lib/loop-common.sh:562`, `hooks/lib/loop-common.sh:681`, `hooks/lib/loop-common.sh:696`). Active-loop detection elsewhere treats `finalize-state.md` as active and `cancel-state.md` as inactive (`tests/test-finalize-phase.sh:293`, `tests/test-state-exit-naming.sh:97` via search context).
- edge_cases_or_failure_modes: If both `state.md` and `finalize-state.md` exist, normal `state.md` wins because it is checked first (`scripts/cancel-rlcr-loop.sh:89`). Existing `cancel-state.md` could be overwritten by `mv` behavior depending on platform; no explicit guard is present. Directory selection by reverse lexical sort assumes timestamp-formatted loop directory names (`scripts/cancel-rlcr-loop.sh:73`). `set -euo pipefail` means failed `touch` or `mv` aborts without custom recovery (`scripts/cancel-rlcr-loop.sh:18`). The script does not validate parsed numeric round fields beyond display.
- validation_or_tests: `tests/test-cancel-signal-file.sh` validates the signal-file mechanism and blocks unauthorized or malicious state-file moves, including missing signal, wrong destination, wrong directory, chained commands, substitutions, extra args, wrappers, redirection prefixes, and wrong source paths (`tests/test-cancel-signal-file.sh:80`, `tests/test-cancel-signal-file.sh:121`, `tests/test-cancel-signal-file.sh:181`, `tests/test-cancel-signal-file.sh:243`, `tests/test-cancel-signal-file.sh:303`, `tests/test-cancel-signal-file.sh:1233`). `tests/robustness/test-cancel-security-robustness.sh` directly tests `is_cancel_authorized` for valid `state.md` and `finalize-state.md` cancels plus bypass rejection (`tests/robustness/test-cancel-security-robustness.sh:39`, `tests/robustness/test-cancel-security-robustness.sh:51`, `tests/robustness/test-cancel-security-robustness.sh:99`). `tests/test-finalize-phase.sh` validates finalize-state active-loop semantics and finalize-state protection (`tests/test-finalize-phase.sh:283`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-063 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop script argument parsing and minimal state mutation. It is a sourced test module, not a standalone runner; `tests/test-pr-loop.sh` sources it and calls `run_script_tests` (`tests/test-pr-loop-scripts.sh:11`, `tests/test-pr-loop-scripts.sh:405`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`).
- algorithmic_behavior: Defines four grouped suites: `run_setup_tests`, `run_cancel_tests`, `run_fetch_tests`, and `run_poll_tests` (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`). Setup tests assert help output, required bot flags, unknown option failure, recognition of `--claude`, `--codex`, combined bot flags, `--max`, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:28`, `tests/test-pr-loop-scripts.sh:39`, `tests/test-pr-loop-scripts.sh:52`, `tests/test-pr-loop-scripts.sh:66`, `tests/test-pr-loop-scripts.sh:104`, `tests/test-pr-loop-scripts.sh:131`). Cancel tests verify help, no-loop `NO_LOOP`, and active-loop cancellation that renames `state.md` to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:181`, `tests/test-pr-loop-scripts.sh:192`, `tests/test-pr-loop-scripts.sh:211`). Fetch and poll tests cover missing/invalid required arguments (`tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`, `tests/test-pr-loop-scripts.sh:352`, `tests/test-pr-loop-scripts.sh:366`, `tests/test-pr-loop-scripts.sh:380`).
- inputs_outputs_state: Inputs are shell scripts resolved from `PROJECT_ROOT`, plus `TEST_DIR` and helper functions from the parent runner. The cancel active-loop test creates `.humanize/pr-loop/<timestamp>/state.md`, exports `CLAUDE_PROJECT_DIR`, runs the cancel script, and asserts the file transition to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:194`, `tests/test-pr-loop-scripts.sh:215`, `tests/test-pr-loop-scripts.sh:218`, `tests/test-pr-loop-scripts.sh:222`, `tests/test-pr-loop-scripts.sh:232`, `tests/test-pr-loop-scripts.sh:237`). Outputs are pass/fail counters through `pass` and `fail` helpers from `tests/test-helpers.sh:30` and `tests/test-helpers.sh:35`.
- gates_or_invariants: The tests encode parsing gates from `scripts/setup-pr-loop.sh`: at least one bot flag is required, unknown options are rejected, numeric options must be positive integers, and known options must not be mistaken for missing arguments (`tests/test-pr-loop-scripts.sh:45`, `tests/test-pr-loop-scripts.sh:59`, `tests/test-pr-loop-scripts.sh:124`). They encode PR cancel semantics from `scripts/cancel-pr-loop.sh`: no loop exits 1 with `NO_LOOP`, active cancellation exits 0 with `CANCELLED`, and `state.md` must disappear while `cancel-state.md` appears (`tests/test-pr-loop-scripts.sh:203`, `tests/test-pr-loop-scripts.sh:236`, `tests/test-pr-loop-scripts.sh:237`).
- dependencies_and_callers: Depends on `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh` (`tests/test-pr-loop-scripts.sh:25`, `tests/test-pr-loop-scripts.sh:179`, `tests/test-pr-loop-scripts.sh:266`, `tests/test-pr-loop-scripts.sh:339`). Called by `tests/test-pr-loop.sh`, which initializes the PR-loop test environment and then runs script, hook, and stop-hook suites (`tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:40`). Uses shared helpers for pass/fail and test directory lifecycle (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:86`).
- edge_cases_or_failure_modes: Several tests intentionally allow downstream failures after option parsing, for example `--claude` may fail outside a git repo but must not report missing bot flag (`tests/test-pr-loop-scripts.sh:68`, `tests/test-pr-loop-scripts.sh:73`). Fetch/poll tests use `/tmp/out.md` for invalid PR validation but do not exercise live GitHub calls. The module assumes parent setup defines `PROJECT_ROOT`, `TEST_DIR`, and helper functions; direct execution without sourcing the parent runner is incomplete.
- validation_or_tests: This file is itself validation. It is included in the full test suite via `tests/test-pr-loop.sh`, which is listed by `tests/run-all-tests.sh` (`tests/run-all-tests.sh:53`). It validates script-level contracts but leaves deeper GitHub API, PR state, hook, and stop-hook behavior to `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-093 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Guard message template for blocking writes/edits to PR-loop generated input files. It supports the algorithmic invariant that PR-loop prompt and PR-comment artifacts are system-owned, read-only inputs.
- algorithmic_behavior: The template tells the agent it cannot write to `round-*-pr-comment.md` or `round-*-prompt.md` inside `.humanize/pr-loop/`, and explains those files are generated by the PR loop system (`prompt-template/block/pr-loop-prompt-write.md:1`, `prompt-template/block/pr-loop-prompt-write.md:3`, `prompt-template/block/pr-loop-prompt-write.md:5`). The message is rendered by `pr_loop_prompt_blocked_message`, with a built-in fallback if the template cannot load (`hooks/lib/loop-common.sh:898`, `hooks/lib/loop-common.sh:905`).
- inputs_outputs_state: Inputs are none beyond template loading. Output is a Markdown error/block reason returned by validators. It does not mutate state; it documents why mutation is denied. The files protected by this message are generated during PR-loop setup, where `setup-pr-loop.sh` writes `round-0-pr-comment.md` from fetched comments and `round-0-prompt.md` from templates plus comments (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:760`, `scripts/setup-pr-loop.sh:764`, `scripts/setup-pr-loop.sh:851`).
- gates_or_invariants: `hooks/loop-write-validator.sh` blocks Write operations to PR-loop read-only files and exits 2 after rendering this message (`hooks/loop-write-validator.sh:74`, `hooks/loop-write-validator.sh:83`, `hooks/loop-write-validator.sh:85`). `hooks/loop-edit-validator.sh` applies the same gate for Edit operations (`hooks/loop-edit-validator.sh:57`, `hooks/loop-edit-validator.sh:66`, `hooks/loop-edit-validator.sh:68`). The file classifier includes more protected PR-loop files than the template names directly mention: `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` (`hooks/lib/loop-common.sh:825`, `hooks/lib/loop-common.sh:830`).
- dependencies_and_callers: Depends on the template loader through `load_and_render_safe` in `hooks/lib/loop-common.sh`. Called indirectly by Write/Edit validators for paths inside `.humanize/pr-loop/`. The broader path matching depends on `is_in_pr_loop_dir`, `is_pr_round_file_type`, and `is_pr_loop_readonly_file` (`hooks/lib/loop-common.sh:783`, `hooks/lib/loop-common.sh:818`, `hooks/lib/loop-common.sh:827`).
- edge_cases_or_failure_modes: Template wording only explicitly lists `round-*-pr-comment.md` and `round-*-prompt.md`, while the shared classifier also blocks `round-*-codex-prompt.md`, `round-*-pr-check.md`, and `round-*-pr-feedback.md`; this is a mild message-coverage mismatch, not a core logic failure. If the template is missing, the fallback message still blocks mutation (`hooks/lib/loop-common.sh:900`). Path detection is substring/regex based on `.humanize/pr-loop/`, so correctness depends on validators receiving normalized enough file paths.
- validation_or_tests: The hook validators exercise PR-loop file protection in the PR-loop test suite. Template existence/reference tests cover common template loading patterns, though `tests/test-template-references.sh` marks only a subset of common templates and treats some templates as dynamically referenced (`tests/test-template-references.sh:130`, `tests/test-template-references.sh:149`). The read-only behavior is anchored in `loop-write-validator.sh` and `loop-edit-validator.sh` lines above.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-123 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR-loop goal tracker template. It seeds `.humanize/pr-loop/<timestamp>/goal-tracker.md` with PR metadata, ultimate goal, issue summary table, aggregate counters, and an initial issue log entry.
- algorithmic_behavior: The template renders placeholders for PR number, branch, start time, active bots, and startup case (`prompt-template/pr-loop/goal-tracker-initial.md:3`, `prompt-template/pr-loop/goal-tracker-initial.md:5`, `prompt-template/pr-loop/goal-tracker-initial.md:9`). It defines the loop objective as getting all monitored bot reviewers to approve the PR (`prompt-template/pr-loop/goal-tracker-initial.md:11`, `prompt-template/pr-loop/goal-tracker-initial.md:13`). It initializes the issue table and counters to zero and records Round 0 as awaiting initial reviews (`prompt-template/pr-loop/goal-tracker-initial.md:15`, `prompt-template/pr-loop/goal-tracker-initial.md:21`, `prompt-template/pr-loop/goal-tracker-initial.md:27`, `prompt-template/pr-loop/goal-tracker-initial.md:29`).
- inputs_outputs_state: Inputs are template variables built in `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `BOT_AC_ROWS`, and `STARTED_AT` (`scripts/setup-pr-loop.sh:658`). The current template consumes all except `BOT_AC_ROWS`. Output is written to `GOAL_TRACKER_FILE="$LOOP_DIR/goal-tracker.md"` (`scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:712`, `scripts/setup-pr-loop.sh:713`). It creates persistent loop state for monitoring and later issue-stat parsing.
- gates_or_invariants: Setup uses `load_and_render_safe` with a much richer fallback tracker if the template fails to load (`scripts/setup-pr-loop.sh:668`, `scripts/setup-pr-loop.sh:712`). The live template’s invariant is issue-centric tracking rather than acceptance-criteria tracking. It starts totals at `0`, so later hooks/monitors must update counts as bot reviews arrive. Goal tracker creation happens after state creation and before the initial prompt is emitted (`scripts/setup-pr-loop.sh:604`, `scripts/setup-pr-loop.sh:637`, `scripts/setup-pr-loop.sh:717`).
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh`. Downstream consumers include PR stop hook goal-tracker update logic (`hooks/pr-loop-stop-hook.sh:1253` from search result) and monitor parsing helpers such as `humanize_parse_pr_goal_tracker` in `scripts/lib/monitor-common.sh` (`scripts/lib/monitor-common.sh:465` from search result). The setup script’s fallback shows an older/alternate acceptance-criteria structure, which means consumers should be robust to both template forms.
- edge_cases_or_failure_modes: The `ACTIVE_BOTS_DISPLAY` construction in setup uses `IFS=', '; echo "${ACTIVE_BOTS_ARRAY[*]}"`, which in Bash uses only the first character of `IFS` for array joining, so display likely becomes comma-separated but not comma-space-separated (`scripts/setup-pr-loop.sh:644`). The template does not include `BOT_AC_ROWS`, so per-bot acceptance rows generated by setup are unused unless fallback is used (`scripts/setup-pr-loop.sh:646`, `scripts/setup-pr-loop.sh:664`). Placeholder rendering must escape or preserve branch/bot strings safely; setup has earlier YAML-safety checks for branch/model/effort before state writing (`scripts/setup-pr-loop.sh:384`, `scripts/setup-pr-loop.sh:393`, `scripts/setup-pr-loop.sh:401`), but template rendering itself depends on the shared loader.
- validation_or_tests: `tests/test-pr-loop-system.sh` has an integration test asserting `setup-pr-loop.sh` creates `goal-tracker.md`, includes the `Issue Summary` section, and includes the mock PR number (`tests/test-pr-loop-system.sh:1453`, `tests/test-pr-loop-system.sh:1492`, `tests/test-pr-loop-system.sh:1505`, `tests/test-pr-loop-system.sh:1517`). The main PR-loop runner includes the system suite via `tests/run-all-tests.sh` (`tests/run-all-tests.sh:53`, `tests/run-all-tests.sh:54`). Template loader comprehensive tests exercise real template rendering patterns, though not this exact PR-loop template in the inspected lines.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 item sections present, one section per assigned row, all unique`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`