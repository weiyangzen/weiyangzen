# agent_14 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR review-loop prompt template set. This directory defines the initial human/agent instructions, goal-tracker seed, critical completion reminders, and Codex goal-tracker update instructions used by the PR-loop state machine.
- algorithmic_behavior: The directory has seven templates:
  - `prompt-template/pr-loop/goal-tracker-initial.md:1` seeds `.humanize/pr-loop/<timestamp>/goal-tracker.md` with PR metadata, ultimate goal, issue summary table, total statistics, and initial Round 0 issue log.
  - `prompt-template/pr-loop/round-0-header.md:1` starts the Round 0 prompt with PR number, branch, active bots, and a review comments section.
  - `prompt-template/pr-loop/round-0-task-no-comments.md:4` handles startup with no fetched comments: instructs the agent not to trigger review comments, write the initial summary, and exit so the stop hook can poll.
  - `prompt-template/pr-loop/round-0-task-has-comments.md:4` handles startup with existing comments: instructs the agent to analyze comments, fix issues, commit, push, trigger bot re-review, and write a resolution summary.
  - `prompt-template/pr-loop/critical-requirements-no-comments.md:6` emits a concise no-comments completion contract.
  - `prompt-template/pr-loop/critical-requirements-has-comments.md:6` emits a stricter existing-comments contract requiring commit, push, PR comment trigger, and summary.
  - `prompt-template/pr-loop/codex-goal-tracker-update.md:1` is injected into local Codex PR review prompts so Codex updates the goal tracker by adding per-round issue-summary rows, totals, and issue-log entries.
- inputs_outputs_state: Inputs are template variables rendered by `hooks/lib/template-loader.sh`: `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are generated prompt/goal files under `.humanize/pr-loop/<timestamp>/`, especially `goal-tracker.md`, `round-0-prompt.md`, and later `round-N-codex-prompt.md`. `scripts/setup-pr-loop.sh:712` renders `goal-tracker-initial.md`; `scripts/setup-pr-loop.sh:758` renders `round-0-header.md`; `scripts/setup-pr-loop.sh:800` and `scripts/setup-pr-loop.sh:847` choose the no-comments vs has-comments task template based on fetched comments; `scripts/setup-pr-loop.sh:914` and `scripts/setup-pr-loop.sh:940` choose the matching critical requirement template. `hooks/pr-loop-stop-hook.sh:1261` renders `codex-goal-tracker-update.md` into the local Codex prompt.
- gates_or_invariants: The templates encode PR-loop invariants rather than executable enforcement. Important gates are: do not edit `.humanize/pr-loop` state files (`round-0-task-has-comments.md:32`, `round-0-task-no-comments.md:21`), push fixes when review comments already exist (`round-0-task-has-comments.md:16`), do not post the first review trigger when no comments exist (`round-0-task-no-comments.md:20`), and keep goal tracker structure intact (`codex-goal-tracker-update.md:59`). These instruction-level gates coordinate with hard validator gates in `hooks/loop-edit-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for loading/rendering and on `scripts/setup-pr-loop.sh` / `hooks/pr-loop-stop-hook.sh` as callers. `scripts/setup-pr-loop.sh:37` sources the template loader and sets `TEMPLATE_DIR` at `scripts/setup-pr-loop.sh:42`. `hooks/pr-loop-stop-hook.sh:51` sources shared loop functions and sets `TEMPLATE_DIR` at `hooks/pr-loop-stop-hook.sh:57`.
- edge_cases_or_failure_modes: If a template is missing or empty, all live call sites use `load_and_render_safe`, so setup/stop-hook behavior degrades to inline fallback text rather than aborting (`scripts/setup-pr-loop.sh:740`, `scripts/setup-pr-loop.sh:769`, `scripts/setup-pr-loop.sh:803`, `scripts/setup-pr-loop.sh:893`, `scripts/setup-pr-loop.sh:916`, `hooks/pr-loop-stop-hook.sh:1258`). The no-comments branch is selected by a sentinel match for `*No comments found.*` at `scripts/setup-pr-loop.sh:723`; any other comment file content is treated as comments present. Since these are prompts, enforcement depends on hooks and stop-hook validations.
- validation_or_tests: Template existence/reference safety is covered by `tests/test-template-references.sh` via extraction of `load_template`, `load_and_render`, and `load_and_render_safe` references, and PR-loop behavior is included in `tests/run-all-tests.sh:53` and `tests/run-all-tests.sh:54` through `test-pr-loop.sh` and `test-pr-loop-system.sh`. `tests/test-pr-loop-system.sh` references the initial goal-tracker template around `tests/test-pr-loop-system.sh:644`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-wide executable test orchestrator. It defines the canonical ordered regression suite for the Humanize plugin, including template, hook, PR-loop, monitor, and robustness suites.
- algorithmic_behavior: The script initializes counters and `FAILED_SUITES` (`tests/run-all-tests.sh:28`), declares ordered `TEST_SUITES` (`tests/run-all-tests.sh:33`), declares `ZSH_TESTS` for interpreter-sensitive tests (`tests/run-all-tests.sh:76`), then iterates suites (`tests/run-all-tests.sh:80`). For each suite, it skips missing files (`tests/run-all-tests.sh:83`), runs zsh-only tests under zsh when available (`tests/run-all-tests.sh:106`), otherwise executes the suite directly (`tests/run-all-tests.sh:116`). It captures output and exit code without immediately aborting (`tests/run-all-tests.sh:105`), strips ANSI escapes (`tests/run-all-tests.sh:121`), extracts last `Passed:` and `Failed:` counts from suite output (`tests/run-all-tests.sh:124`), aggregates totals (`tests/run-all-tests.sh:127`), records failures when the suite exits nonzero or reports failed tests (`tests/run-all-tests.sh:130`), and prints the last 30 lines of failed suite output (`tests/run-all-tests.sh:134`).
- inputs_outputs_state: Input is the local `tests/` tree rooted by `SCRIPT_DIR` (`tests/run-all-tests.sh:14`) plus each suite's executable output contract. Outputs are terminal summary lines and process exit status: `0` only when `FAILED_SUITES` is empty (`tests/run-all-tests.sh:156`), otherwise `1` (`tests/run-all-tests.sh:148`). Internal state is `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES`.
- gates_or_invariants: Missing suite files are warnings/skips, not failures (`tests/run-all-tests.sh:83`). A suite fails if either its process exit code is nonzero or the parsed `Failed:` count is greater than zero (`tests/run-all-tests.sh:130`). This creates an invariant that tests must both exit cleanly and report no failed assertions. ANSI stripping prevents colored helper output from breaking pass/fail parsing (`tests/run-all-tests.sh:121`).
- dependencies_and_callers: Depends on every listed suite having executable permissions or an explicit zsh interpreter path. Depends on each suite following the shared test summary convention from `tests/test-helpers.sh:58`, which prints `Passed:` and `Failed:` lines. It covers the assigned robustness suite at `tests/run-all-tests.sh:69` and template loader suite at `tests/run-all-tests.sh:34`.
- edge_cases_or_failure_modes: The script uses `set -uo pipefail` but not global `set -e` at startup (`tests/run-all-tests.sh:12`); it temporarily disables `errexit` before running suites and enables it afterward (`tests/run-all-tests.sh:105`, `tests/run-all-tests.sh:119`). Because `set -e` is enabled inside the loop after the first suite, later unexpected command failures in the harness itself may abort rather than be recorded as suite failures. Pass/fail count parsing defaults to `0` when no count is found (`tests/run-all-tests.sh:124`), so a suite can still pass by exit code while contributing zero passed tests. Missing zsh causes a skip, not an aggregate failure (`tests/run-all-tests.sh:111`).
- validation_or_tests: This file is itself the top-level validation entrypoint. It explicitly includes core hook and PR-loop suites: `test-template-loader.sh`, `test-todo-checker.sh`, `test-plan-file-hooks.sh`, `test-pr-loop.sh`, `test-pr-loop-system.sh`, and the robustness suites from `tests/run-all-tests.sh:56` through `tests/run-all-tests.sh:72`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-074 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared prompt-template loading and rendering library for RLCR and PR-loop hooks. It centralizes template directory resolution, template file IO, placeholder substitution, fallback rendering, and template directory validation.
- algorithmic_behavior: `get_template_dir` computes the plugin `prompt-template` path from a hooks/lib script directory by walking two levels up (`hooks/lib/template-loader.sh:26`). `load_template` reads a template file or returns empty output with a warning on stderr when missing (`hooks/lib/template-loader.sh:36`). `render_template` accepts template content and `VAR=value` assignments, stores them as environment variables prefixed with `TMPL_VAR_` (`hooks/lib/template-loader.sh:62`), then runs an awk single-pass renderer (`hooks/lib/template-loader.sh:74`). The renderer scans each line for `{{...}}`, replaces placeholders found in the lookup table, preserves unknown placeholders, and does not rescan substituted values (`hooks/lib/template-loader.sh:88`). `load_and_render` composes load plus render without fallback (`hooks/lib/template-loader.sh:136`). `append_template` concatenates a base content string and another template (`hooks/lib/template-loader.sh:151`). `load_and_render_safe` loads a template and falls back to a caller-provided message if the file is missing, empty, or renders empty (`hooks/lib/template-loader.sh:170`). `validate_template_dir` requires the root plus `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:208`).
- inputs_outputs_state: Inputs are a template directory, template name, template content, and zero or more `VAR=value` assignments. Outputs are rendered markdown/text to stdout, warnings to stderr, and return codes from validation. It has no persisted state; it uses process environment variables only for the duration of awk rendering.
- gates_or_invariants: The key invariant is single-pass substitution, documented at `hooks/lib/template-loader.sh:12` and implemented by appending replacement values directly to `result` without re-parsing (`hooks/lib/template-loader.sh:116`). Unknown placeholders remain unchanged (`hooks/lib/template-loader.sh:120`). Missing templates are nonfatal for `load_template` and `load_and_render_safe`; strict existence checks are left to `validate_template_dir`. Placeholder names are not syntactically validated by the renderer despite the comment limiting names to uppercase/numeric/underscore (`hooks/lib/template-loader.sh:9`); whatever appears between `{{` and `}}` becomes a lookup key (`hooks/lib/template-loader.sh:112`).
- dependencies_and_callers: Depends on standard shell, `cat`, `env`, and `awk`. It is sourced by `hooks/lib/loop-common.sh:141`, which sets `TEMPLATE_DIR` and validates it at `hooks/lib/loop-common.sh:144`; by `scripts/setup-pr-loop.sh:39`; and by template-specific tests. Callers include `hooks/loop-read-validator.sh:144`, `hooks/loop-write-validator.sh:207`, `hooks/loop-codex-stop-hook.sh:382`, `hooks/pr-loop-stop-hook.sh:1261`, and many other hook paths using `load_and_render_safe`.
- edge_cases_or_failure_modes: Missing file returns empty plus warning (`hooks/lib/template-loader.sh:41`), so unsafe callers using `load_and_render` can get no content. `load_and_render_safe` suppresses loader stderr and uses fallback when content/result is empty (`hooks/lib/template-loader.sh:177`, `hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:192`). An assignment without `=` becomes a variable whose value equals the whole assignment due to `${var_assignment#*=}` behavior. Values containing newlines are stored in environment variables and rendered by awk; tests cover multiline and special-character values. Because rendering is line-based awk, input line boundaries are preserved with `print result`.
- validation_or_tests: Direct tests are included by `tests/run-all-tests.sh:34`. `tests/test-template-loader.sh` covers directory resolution, missing templates, single/multiple/multiline substitutions, special characters, unreplaced placeholders, `load_and_render_safe`, and directory validation. `tests/test-error-scenarios.sh` covers missing template and invalid directory behavior. `tests/robustness/test-template-stress-robustness.sh` and `tests/robustness/test-template-error-robustness.sh` cover large values, nested placeholder injection, unusual variable names, BOM/whitespace files, permissions, symlinks, and concurrent rendering.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Blocking prompt template for stale RLCR round-file reads. It tells an agent that it attempted to read a prompt/summary for a different round than the active round and points it at current-round files.
- algorithmic_behavior: The template renders a short block message with attempted round/file type, current round, current prompt path, current summary path, and a direct `cat` command for the originally requested file if it is still needed (`prompt-template/block/wrong-round-file.md:1`). It is used as the user-facing reason when `hooks/loop-read-validator.sh` rejects a read of `round-N-prompt.md` or `round-N-summary.md` whose round does not match current state.
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, passed by `hooks/loop-read-validator.sh:144`. Output is rendered markdown to stderr, followed by hook exit code `2` (`hooks/loop-read-validator.sh:150`). It does not mutate state.
- gates_or_invariants: The executable gate is in `hooks/loop-read-validator.sh`: only `Read` tool requests are considered (`hooks/loop-read-validator.sh:39`); only `summary` and `prompt` round files are gated (`hooks/loop-read-validator.sh:69`); malformed active state fails closed (`hooks/loop-read-validator.sh:95`); mismatched rounds are blocked unless the requested file is explicitly allowlisted (`hooks/loop-read-validator.sh:138`). The template supports the invariant that agents must consume current-round prompt/summary files rather than stale loop context.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` via `load_and_render_safe`. The direct caller is `hooks/loop-read-validator.sh:144`. Related helpers are `extract_round_number` in `hooks/lib/loop-common.sh:411`, `is_round_file_type` at `hooks/lib/loop-common.sh:401`, and `is_allowlisted_file` at `hooks/lib/loop-common.sh:423`.
- edge_cases_or_failure_modes: If the template is missing, the read validator fallback still explains the wrong-round condition (`hooks/loop-read-validator.sh:139`). If there is no active loop, the read is allowed (`hooks/loop-read-validator.sh:84`). If the filename does not contain an extractable round number, the hook allows it (`hooks/loop-read-validator.sh:105`). Allowlisted historical files can bypass wrong-round blocking (`hooks/loop-read-validator.sh:138`), notably fixed entries in `hooks/lib/loop-common.sh:427`.
- validation_or_tests: Included in template reference validation via `tests/test-template-references.sh`, because `hooks/loop-read-validator.sh:144` references it through `load_and_render_safe`. Round-file gating is covered by hook tests included in `tests/run-all-tests.sh`, especially `test-plan-file-hooks.sh`, `test-error-scenarios.sh`, and robustness suites around hook input and hook system behavior.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-134 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for the hook system. It verifies that core RLCR/PR-loop validators and stop hooks handle malformed inputs, protected state files, command-injection patterns, concurrent invocations, missing/corrupt state, and large/nested JSON without crashes or unsafe allowance.
- algorithmic_behavior: The script sets strict mode (`tests/robustness/test-hook-system-robustness.sh:18`), resolves project paths, sources `loop-common.sh`, `portable-timeout.sh`, and test helpers (`tests/robustness/test-hook-system-robustness.sh:20`), then creates an isolated temp directory (`tests/robustness/test-hook-system-robustness.sh:26`). It is organized into sections:
  - Edit validator tests: valid Edit JSON allowed (`:40`), direct state edit blocked (`:53`), malformed JSON handled without crash (`:80`), missing `file_path` handled below fatal signal range (`:94`), outside `.humanize` path traversal allowed through to sandbox (`:109`), path traversal resolving to RLCR `state.md` blocked with exit `2` (`:124`).
  - Plan-file validator tests: valid plan edit in a test git repo allowed (`:165`), non-plan file passes through (`:217`), empty JSON does not crash (`:232`).
  - State parsing tests: whitespace around numeric fields parsed (`:253`), quoted/special path values tolerated while reading current round (`:271`), missing closing YAML delimiter handled gracefully as default or parsed value (`:290`).
  - Command-injection prevention tests: Bash append to RLCR `state.md` blocked with exit `2` and block decision (`:317`), Bash append to `goal-tracker.md` after round 0 blocked (`:353`), unrelated dangerous shell command allowed through because sandbox owns non-loop security (`:369`), Edit handles newline strings (`:384`), Write handles binary-looking escaped content (`:399`).
  - Concurrent access test: ten concurrent read-validator invocations should not corrupt state (`:421`).
  - Stop-hook state tests: no active RLCR/PR loop allows exit (`:461`, `:477`), corrupted state does not crash (`:493`), missing critical state fields ends or allows gracefully and may rename to `unexpected-state.md` (`:516`), valid active loop blocks exit with a JSON block decision after mock Codex review (`:559`).
  - JSON edge cases: 100KB Write payload completes under timeout without fatal exit (`:623`), escaped null path handled (`:637`), nested JSON handled below fatal signal range (`:651`).
- inputs_outputs_state: Inputs are synthesized JSON hook payloads piped into hook scripts, generated `.humanize/rlcr` or `.humanize/pr-loop` state fixtures, temporary git repos, and a mock `codex` binary for active loop stop-hook behavior (`tests/robustness/test-hook-system-robustness.sh:592`). Outputs are `pass`/`fail` counter updates from `tests/test-helpers.sh`, stdout section logs, and final exit status from `print_test_summary` (`tests/robustness/test-hook-system-robustness.sh:671`). State mutations are confined to `TEST_DIR`, including intentional `state.md` fixtures and possible `unexpected-state.md` rename by stop-hook under invalid state (`tests/robustness/test-hook-system-robustness.sh:546`).
- gates_or_invariants: The test encodes several core invariants: malformed JSON or missing fields must not terminate via signal-level failure (`exit < 128` checks at `tests/robustness/test-hook-system-robustness.sh:88`, `:103`, `:239`); loop-owned `state.md` modifications must block with exit `2` (`:151`, `:343`); non-loop paths and unrelated shell security are delegated to the external sandbox rather than over-blocked (`:117`, `:377`); active stop hook must emit a JSON `"decision":"block"` while an RLCR loop is live (`:607`); no active loop must allow exit with no block decision (`:470`, `:486`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for parsing helpers such as `get_current_round`; `scripts/portable-timeout.sh` for `run_with_timeout`; `tests/test-helpers.sh` for counters; hook scripts `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`; `jq`, `git`, `bash`, and `python3`-backed timeout fallback. It is included in the top-level orchestrator at `tests/run-all-tests.sh:69`.
- edge_cases_or_failure_modes: The script deliberately accepts multiple graceful outcomes for malformed/incomplete state where behavior may end the loop, block, or allow exit as long as it does not crash (`tests/robustness/test-hook-system-robustness.sh:301`, `:503`, `:540`). The active-loop stop-hook test uses a mock Codex in `PATH` to avoid real API calls (`tests/robustness/test-hook-system-robustness.sh:592`), but still depends on local git initialization and hook logic. Large JSON is bounded by `run_with_timeout 5` (`:628`), so slow hook parsing can fail this robustness gate. Some tests assert pass-through for risky commands outside loop-owned files, which is intentional but means this suite is not a general shell-security validator.
- validation_or_tests: This file is a validation suite. Its own pass/fail summary is produced at `tests/robustness/test-hook-system-robustness.sh:671` and consumed by `tests/run-all-tests.sh`, which parses `Passed:` / `Failed:` lines (`tests/run-all-tests.sh:124`). It complements dedicated suites for hook input, template error robustness, plan-file robustness, state transition robustness, and PR-loop API robustness listed around `tests/run-all-tests.sh:61` through `tests/run-all-tests.sh:72`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 5 assigned item sections are present once, in the same order as the assignment table
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`