# agent_30 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-030 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` gate for RLCR and PR loop state-machine files. It is registered for the `Write` matcher in `hooks/hooks.json:14-21` and enforces which generated loop artifacts Claude may write.
- algorithmic_behavior: Reads hook JSON from stdin, validates structure and nesting, ignores non-`Write` tools, requires `tool_input.file_path`, lowercases for pattern checks, then applies layered policy checks. It blocks RLCR todos except allowlisted active-loop files, blocks RLCR prompt writes, protects PR loop state/read-only generated files, validates PR resolve round, protects RLCR state/finalize-state/plan backup/goal tracker, permits finalize summary in the active finalize loop, validates current summary round, and validates that summary writes land in the active loop directory.
- inputs_outputs_state: Inputs are hook JSON, `CLAUDE_PROJECT_DIR` or cwd, active `.humanize/rlcr/*` loop, active `.humanize/pr-loop/*` loop, `state.md` or `finalize-state.md`, and shared helper/template files. Outputs are exit `0` allow, exit `1` invalid input or malformed state fail-closed, and exit `2` policy block with rendered guidance. It reads `current_round` from strict state parsing at `hooks/loop-write-validator.sh:140-145`.
- gates_or_invariants: Non-`Write` is always allowed at `hooks/loop-write-validator.sh:39-41`. Invalid JSON, missing `file_path`, or overly deep JSON blocks at `hooks/loop-write-validator.sh:25-46`, backed by `validate_hook_input` and `is_deeply_nested` in `hooks/lib/loop-common.sh:59-133`. State files are never writable at `hooks/loop-write-validator.sh:152-160`; `finalize-state.md` is checked first because the generic state matcher also matches it. Goal tracker is writable only in round zero; after that writes are redirected to the current summary at `hooks/loop-write-validator.sh:188-196`. Summary files must use the current round and active directory unless allowlisted at `hooks/loop-write-validator.sh:228-263`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-write-validator.sh:17-19`. Key helpers include active loop discovery `hooks/lib/loop-common.sh:156-174`, strict state parsing `hooks/lib/loop-common.sh:255-330`, round/type helpers `hooks/lib/loop-common.sh:394-418`, hardcoded allowlist `hooks/lib/loop-common.sh:423-441`, PR loop path/read-only helpers `hooks/lib/loop-common.sh:783-887`, and block-message renderers `hooks/lib/loop-common.sh:443-524` plus `hooks/lib/loop-common.sh:889-905`.
- edge_cases_or_failure_modes: If no active RLCR loop exists, RLCR-specific validation falls through allow at `hooks/loop-write-validator.sh:123-130`. If active state is malformed, it blocks safely with exit `1` at `hooks/loop-write-validator.sh:140-144`. Finalize summary is allowed only as exactly `$ACTIVE_LOOP_DIR/finalize-summary.md` at `hooks/loop-write-validator.sh:168-172`. The allowlist intentionally permits only a small set of historical/generated files, such as `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in `hooks/lib/loop-common.sh:427-432`. PR `round-N-pr-resolve.md` is writable only for the current PR round via `hooks/loop-write-validator.sh:89-93` and `hooks/lib/loop-common.sh:843-887`.
- validation_or_tests: Covered by allowlist tests in `tests/test-allowlist-validators.sh:135-184`, plan backup write protection in `tests/test-plan-file-hooks.sh:226-237`, finalize write gates in `tests/test-finalize-phase.sh:345-365`, and PR loop write gates in `tests/test-pr-loop-hooks.sh:85-163`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-060 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable shell specification for `scripts/setup-rlcr-loop.sh` setup-time validation. It asserts the plan file and launch metadata gates that must pass before an RLCR loop can create state, prompts, trackers, or review workflow artifacts.
- algorithmic_behavior: Creates temporary git repositories, mocks `codex`, invokes `scripts/setup-rlcr-loop.sh` with different CLI/path/git/content/model cases, captures stdout/stderr and exit code, and increments pass/fail/skip counters. The suite exits with the number of failed tests at `tests/test-plan-file-validation.sh:742-751`.
- inputs_outputs_state: Inputs are synthetic plan files, git repo state, `.gitignore`, tracked/untracked/dirty plan status, branch names, CLI options, and codex model arguments. Outputs are PASS/FAIL/SKIP lines and final shell exit status. The test explicitly unsets `CLAUDE_PROJECT_DIR` so setup validation uses each temp repo cwd rather than the source checkout at `tests/test-plan-file-validation.sh:19-21`.
- gates_or_invariants: It specifies rejection of absolute paths, missing files/directories, spaces, shell metacharacters, symlinks, inaccessible/failed path resolution, project escapes, non-git repos, repos without commits, tracked plans without `--track-plan-file`, untracked plans with `--track-plan-file`, dirty tracked plans, YAML-unsafe branch names, insufficient content, unsafe codex model/effort, and conflicting CLI forms. These map to implementation gates in `scripts/setup-rlcr-loop.sh:263-341`, `scripts/setup-rlcr-loop.sh:346-472`, `scripts/setup-rlcr-loop.sh:483-535`, and `scripts/setup-rlcr-loop.sh:561-587`.
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh` throughout, for example `tests/test-plan-file-validation.sh:94`, `tests/test-plan-file-validation.sh:106`, `tests/test-plan-file-validation.sh:351`, and `tests/test-plan-file-validation.sh:707-732`. It depends on `git`, `mktemp`, chmod behavior for the permission test, and a test-local codex shim created at `tests/test-plan-file-validation.sh:73-83`. It is part of the aggregate test list at `tests/run-all-tests.sh:32-38`.
- edge_cases_or_failure_modes: Permission-denied path resolution may be skipped if the environment cannot chmod the directory at `tests/test-plan-file-validation.sh:225-241`. Some YAML-unsafe branch tests accept either setup-script rejection or git refusing to create the branch, as seen for colon/hash/quote cases at `tests/test-plan-file-validation.sh:459-510`. The “valid plan” and “valid codex model” tests only assert absence of the targeted validation error because setup may fail later for unrelated environment/base-branch reasons at `tests/test-plan-file-validation.sh:625-633` and `tests/test-plan-file-validation.sh:731-739`.
- validation_or_tests: This file is itself the validation surface. It covers path syntax and containment in `tests/test-plan-file-validation.sh:88-271`, git/tracking state in `tests/test-plan-file-validation.sh:273-425`, branch safety in `tests/test-plan-file-validation.sh:431-511`, content quality in `tests/test-plan-file-validation.sh:517-660`, CLI option handling in `tests/test-plan-file-validation.sh:666-694`, and codex parameter safety in `tests/test-plan-file-validation.sh:700-740`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-090 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: PR loop block-template for the “missing trigger comment” gate. It defines the user-facing reason emitted when the PR loop cannot proceed because an explicit bot mention has not been posted.
- algorithmic_behavior: Template variables `{{STARTUP_CASE}}`, `{{STARTUP_CASE_DESC}}`, `{{CURRENT_ROUND}}`, and `{{BOT_MENTION_STRING}}` are rendered into a block reason. The template states the loop is waiting, explains startup/round context, and gives an example trigger comment using the configured mention string.
- inputs_outputs_state: Inputs are startup case, derived startup case description, current PR round, and bot mention string. Output is markdown reason text returned inside the stop-hook JSON block decision. The consuming hook renders it at `hooks/pr-loop-stop-hook.sh:736-744`.
- gates_or_invariants: The gate fires when `REQUIRE_TRIGGER` is true and `PR_LAST_TRIGGER_AT` is empty at `hooks/pr-loop-stop-hook.sh:727-746`. The hook deliberately validates the trigger before Claude eyes verification to avoid stale-trigger work, documented at `hooks/pr-loop-stop-hook.sh:720-725`.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/pr-loop-stop-hook.sh:739-741`. Startup-case descriptions are assigned immediately before rendering at `hooks/pr-loop-stop-hook.sh:727-734`, with cases 4 and 5 receiving specific descriptions and all other cases treated as subsequent-round trigger requirements.
- edge_cases_or_failure_modes: If the template is missing or cannot render, the stop hook has an inline fallback at `hooks/pr-loop-stop-hook.sh:736-738`. The template does not decide trigger validity itself; it only documents the already-computed block decision. It is a skip candidate only for implementation mechanics, not for algorithm evidence, because it is part of a hard PR-loop transition gate.
- validation_or_tests: Template reference coverage includes `tests/test-template-references.sh` hits for `hooks/pr-loop-stop-hook.sh`; PR-loop stop-hook behavior around trigger gating is exercised in the PR loop test suites referenced by the repository-wide scan.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-120 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: Prompt fragment that instructs Codex how to update the PR loop `goal-tracker.md` after analyzing bot review feedback. It defines the expected state transition for PR review accounting: append per-round reviewer rows, update totals, and add issue-log detail.
- algorithmic_behavior: The template tells Codex to add one Issue Summary row for `{{NEXT_ROUND}}`, increment Total Issues Found/Resolved, recompute Remaining as found minus resolved, and add a `### Round {{NEXT_ROUND}}` Issue Log entry. It constrains status values to “Issues Found”, “All Resolved”, or “Approved” at `prompt-template/pr-loop/codex-goal-tracker-update.md:7-20`.
- inputs_outputs_state: Inputs are `{{GOAL_TRACKER_FILE}}` and `{{NEXT_ROUND}}`, rendered by `hooks/pr-loop-stop-hook.sh:1252-1261`. Output is injected into the generated Codex prompt at `hooks/pr-loop-stop-hook.sh:1263-1270` and later lines, telling Codex to mutate the tracker. The persistent state affected is `.humanize/pr-loop/<timestamp>/goal-tracker.md`.
- gates_or_invariants: The template requires preserving file structure, markdown table formatting, targeted section-only edits, no header changes, additive table behavior, and separate Issue Summary rows per reviewer at `prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`. Those invariants align with the fallback/idempotent updater in `hooks/lib/loop-common.sh:1165-1218`, which checks round-plus-reviewer before adding missing tracker entries.
- dependencies_and_callers: Consumed by `hooks/pr-loop-stop-hook.sh` when building `round-N-codex-prompt.md` after collecting bot reviews. The stop hook sets `GOAL_TRACKER_FILE="$LOOP_DIR/goal-tracker.md"` and passes `NEXT_ROUND` at `hooks/pr-loop-stop-hook.sh:1252-1257`. If Codex misses the update, `update_pr_goal_tracker` can patch consistency using bot-result JSON at `hooks/lib/loop-common.sh:1151-1235`.
- edge_cases_or_failure_modes: The template is advisory prompt content, so enforcement depends on Codex compliance plus the helper fallback. It explicitly supports multi-bot rounds by requiring one row per reviewer, matching the helper’s idempotency check for a specific round/reviewer pair at `hooks/lib/loop-common.sh:1184-1208`. It could become stale if the tracker schema changes because it hardcodes section names and table columns.
- validation_or_tests: Goal tracker behavior is covered broadly by PR loop system tests found in the scan, including stop-hook tracker update tests under `tests/test-pr-loop-system.sh`, and by the production helper comments and idempotency logic in `hooks/lib/loop-common.sh:1151-1218`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: CHANGE_TODOS_TO_TASKS-HZ-030, CHANGE_TODOS_TO_TASKS-HZ-060, CHANGE_TODOS_TO_TASKS-HZ-090, CHANGE_TODOS_TO_TASKS-HZ-120
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`