# agent_28 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit validation gate for an active RLCR loop. It prevents the user/agent from continuing the loop when the state schema is stale, the git branch changed, or the original plan-file tracking contract drifted. Registered as a UserPromptSubmit command in `hooks/hooks.json:4-10`.
- algorithmic_behavior: Reads hook stdin but does not inspect it for decisions (`hooks/loop-plan-file-validator.sh:26-27`). Locates the newest active loop under `.humanize/rlcr` via `find_active_loop` (`hooks/loop-plan-file-validator.sh:29-35`; helper behavior in `hooks/lib/loop-common.sh:151-174`). Prefers `finalize-state.md` over `state.md` during Finalize Phase (`hooks/loop-plan-file-validator.sh:38-42`). Parses state with strict validation and fail-closes on malformed state (`hooks/loop-plan-file-validator.sh:44-48`).
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or current directory, `.humanize/rlcr/*/{state.md,finalize-state.md}`, `plan_file`, `plan_tracked`, `start_branch`, and live git status. Outputs are either no output with exit 0 to allow, JSON `{"decision":"block","reason":...}` with exit 0 for recoverable gates, or stderr plus exit 1 for malformed strict state. It performs no state transition; it only observes and blocks.
- gates_or_invariants: Requires `plan_tracked` and `start_branch` after strict parser succeeds (`hooks/loop-plan-file-validator.sh:55-89`). Requires current git branch to match `start_branch` (`hooks/loop-plan-file-validator.sh:95-115`). If `plan_tracked=true`, the plan must remain tracked and have no porcelain status (`hooks/loop-plan-file-validator.sh:123-192`). If `plan_tracked` is not true, the plan must remain untracked (`hooks/loop-plan-file-validator.sh:193-228`). Git timeout/error cases block rather than allowing continuation (`hooks/loop-plan-file-validator.sh:95-105`, `127-147`, `152-172`, `197-217`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for constants, template loading, active-loop lookup, and strict state parsing (`hooks/lib/loop-common.sh:15-29`, `246-329`). Depends on `scripts/portable-timeout.sh`, whose `run_with_timeout` selects `gtimeout`, GNU `timeout`, Python, or no timeout (`scripts/portable-timeout.sh:9-71`). Uses `git`, `jq`, `sed`, and the `block/schema-outdated.md` template fallback path (`hooks/loop-plan-file-validator.sh:59-77`; template at `prompt-template/block/schema-outdated.md:1-12`).
- edge_cases_or_failure_modes: With no active loop it exits cleanly and silently (`hooks/loop-plan-file-validator.sh:33-36`). If `parse_state_file_strict` rejects missing frontmatter or required v1.5-style fields, the hook emits a generic malformed-state stderr message and exits 1 instead of returning JSON (`hooks/loop-plan-file-validator.sh:44-48`; parser checks at `hooks/lib/loop-common.sh:263-323`). `PLAN_FILE` is joined directly with project root (`hooks/loop-plan-file-validator.sh:121`), so correctness depends on setup-time path validation and state parsing. The "must be gitignored" comment is stronger than the implemented check: the code verifies untracked, not an explicit `.gitignore` match (`hooks/loop-plan-file-validator.sh:193-228`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` directly exercises valid state, YAML-quoted `plan_file`, missing required fields, branch mismatch, and quoted `start_branch` (`tests/test-plan-file-hooks.sh:119-217`, `373-419`). It also covers hyphenated plan paths (`tests/test-plan-file-hooks.sh:480-499`). `tests/test-finalize-phase.sh` includes a plan-file validator finalize-state parsing check per the referenced test block around `tests/test-finalize-phase.sh:853-864`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for monitor shutdown behavior. It verifies that RLCR monitor cleanup restores terminal state, stops idempotently, and handles loop-directory deletion and interrupt signals.
- algorithmic_behavior: Creates an isolated `/tmp/test-monitor-runtime-$$` workspace and removes it on exit (`tests/test-monitor-runtime.sh:45-53`). Generates small test scripts that either source `scripts/humanize.sh` or model its local functions, then checks outputs with `grep`. It reports colored PASS/FAIL counters and exits 0 only when no failures remain (`tests/test-monitor-runtime.sh:22-34`, `492-513`).
- inputs_outputs_state: Inputs are the repository’s `scripts/humanize.sh`, a temporary `.humanize/rlcr` directory, bash signal handling, optional zsh availability, and shell source text. Outputs are PASS/FAIL messages plus process exit status. State under test is local shell state: `cleanup_done=false` becomes true, `monitor_running=true` becomes false, temporary loop directory exists then is deleted, and traps/signals trigger cleanup paths.
- gates_or_invariants: `_graceful_stop` must call `_cleanup`, print a human-readable stop reason, and include the directory-deleted explanation (`tests/test-monitor-runtime.sh:56-135`). Cleanup must be idempotent across repeated `_graceful_stop` and `_cleanup` calls (`tests/test-monitor-runtime.sh:136-177`). Missing `.humanize/rlcr` must trigger graceful stop (`tests/test-monitor-runtime.sh:180-251`). Terminal restoration requires `_restore_terminal`, scroll-region reset `printf "\033[r"`, and `_cleanup -> _restore_terminal` (`tests/test-monitor-runtime.sh:253-311`). Bash traps must cover INT/TERM, zsh must define `TRAPINT` and `TRAPTERM`, and cleanup must reset traps (`tests/test-monitor-runtime.sh:327-423`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` monitor internals: `_restore_terminal` resets the scroll region (`scripts/humanize.sh:522-528`), `_cleanup` guards idempotency, resets traps, kills the tail process, and calls `_restore_terminal` (`scripts/humanize.sh:535-561`), `_graceful_stop` delegates to `_cleanup` and prints the stop message (`scripts/humanize.sh:563-576`), and signal handlers are installed for zsh or bash (`scripts/humanize.sh:578-588`). Included in the aggregate runner at `tests/run-all-tests.sh:50`.
- edge_cases_or_failure_modes: The zsh SIGINT runtime test is skipped when `zsh` is unavailable (`tests/test-monitor-runtime.sh:432-490`). Signal tests use a short asynchronous self-signal window, so slow environments could expose timing sensitivity (`tests/test-monitor-runtime.sh:350-363`, `450-463`). The inner terminal-restore script contains a pipeline from `grep -q` into another `grep`, which cannot produce the intended piped content (`tests/test-monitor-runtime.sh:282-287`), but the outer test uses a separate `grep -A30` assertion that actually gates the cleanup call (`tests/test-monitor-runtime.sh:305-311`).
- validation_or_tests: This file is itself the validation artifact. It is supported by broader monitor E2E coverage for real SIGINT paths in `tests/test-monitor-e2e-real.sh`, but its own scope is focused and partly synthetic: it verifies source-level invariants plus small runtime models rather than running a full RLCR monitor loop for every case.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the "incomplete tasks" gate. It prevents transition to Codex review or loop stop while Task-system work remains unfinished.
- algorithmic_behavior: Renders a markdown block titled "Incomplete Tasks Detected", injects `{{INCOMPLETE_LIST}}`, and instructs the agent to complete all remaining tasks with `TaskUpdate` status `completed` before summarizing and stopping (`prompt-template/block/incomplete-todos.md:1-12`).
- inputs_outputs_state: Input is the rendered incomplete task list from `hooks/check-todos-from-transcript.py`. Output is human-facing markdown embedded as the `reason` field in a JSON block response from the stop hook. It does not mutate state directly, but it forces the task-state transition from pending/in-progress to completed before the RLCR stop/review path can continue.
- gates_or_invariants: The invariant is all native tasks must be completed before stop. The template explicitly says not to proceed to Codex review until all tasks are finished (`prompt-template/block/incomplete-todos.md:7-12`). The caller treats checker exit 1 as a hard block and skips expensive Codex review (`hooks/loop-codex-stop-hook.sh:340-394`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` after `check-todos-from-transcript.py` reports incomplete work (`hooks/loop-codex-stop-hook.sh:346-383`). The stop hook documents compatibility with both legacy TodoWrite and the new Task system (`hooks/loop-codex-stop-hook.sh:340-345`). The checker itself advertises TaskCreate/TaskUpdate support and emits `INCOMPLETE_TODOS` for incomplete state (`hooks/check-todos-from-transcript.py:5`, `67`, `117-118`, `209`).
- edge_cases_or_failure_modes: If the checker has a parse error, this template is bypassed and the stop hook emits a separate parse-error block (`hooks/loop-codex-stop-hook.sh:353-369`). If the checker exits 1 with malformed or empty detail after its first line, `{{INCOMPLETE_LIST}}` may be low-information because the hook blindly uses `tail -n +2` (`hooks/loop-codex-stop-hook.sh:372-383`). The wording names "Codex review", though the same stop-hook gate can also protect late stop/finalization paths.
- validation_or_tests: `tests/test-todo-checker.sh` contains TaskCreate/TaskUpdate cases, including created-but-not-completed, completed update, in-progress update, multiple tasks, and orphan TaskUpdate behavior (`tests/test-todo-checker.sh:297-397`). Stop-hook integration uses this template through the quick incomplete-task check at `hooks/loop-codex-stop-hook.sh:340-394`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-118 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular Codex review contract for an RLCR implementation round. It defines what Codex must inspect and the exact completion semantics that can advance the loop.
- algorithmic_behavior: Requires Codex to read the original plan first (`prompt-template/codex/regular-review.md:3-11`), compare Claude’s claimed work and summary against the plan, prompt, docs, and code (`prompt-template/codex/regular-review.md:20-32`), perform a mandatory goal-alignment check against the goal tracker (`prompt-template/codex/regular-review.md:33-45`), include the injected goal-tracker update section (`prompt-template/codex/regular-review.md:47`), and either write findings to the review result file or end with standalone `COMPLETE` only when fully done (`prompt-template/codex/regular-review.md:49-57`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`. Output is a rendered review prompt saved by the stop hook to `round-N-review-prompt.md`, followed by Codex producing `round-N-review-result.md`. A final non-empty line of `COMPLETE` is the intended completion signal.
- gates_or_invariants: Deferrals and unfinished tasks are explicitly incomplete and must prevent `COMPLETE` (`prompt-template/codex/regular-review.md:24-31`, `53-57`). If deferred or unfinished work exists, the prompt requires Codex to draft a singular, directive implementation plan for Claude to execute (`prompt-template/codex/regular-review.md:24-32`). Goal alignment must report AC coverage, forgotten items, unjustified deferrals, and plan evolution validity (`prompt-template/codex/regular-review.md:33-45`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` for non-full-alignment rounds (`hooks/loop-codex-stop-hook.sh:817-848`). It shares the injected goal tracker update section assembled at `hooks/loop-codex-stop-hook.sh:768-772`. The stop hook later requires the review result file to exist and be non-empty, can copy stdout into the expected file if needed, and only accepts an exact last-line marker (`hooks/loop-codex-stop-hook.sh:1331-1397`). `MARKER_COMPLETE` is defined in `hooks/lib/loop-common.sh:30-32`.
- edge_cases_or_failure_modes: The template depends on Codex obeying the requested file output; the stop hook has fallback copying from stdout but otherwise fails closed if the review file is missing or empty (`hooks/loop-codex-stop-hook.sh:1331-1384`). Completion detection is strict, so phrases like "CANNOT COMPLETE" do not satisfy the gate because only the last non-empty trimmed line is compared (`hooks/loop-codex-stop-hook.sh:1386-1397`). The prompt is intentionally skeptical of Claude’s deferrals, so legitimate scope reductions must be reflected in the plan/goal tracker or they will be treated as blockers.
- validation_or_tests: Stop-hook and finalize-phase tests exercise COMPLETE handling, review-result failures, and review blocking behavior, including cases where COMPLETE triggers the next phase and cases where Codex review output failure blocks exit (`tests/test-finalize-phase.sh:507-653`, `747-785`). The template is also mirrored conceptually by `prompt-template/codex/full-alignment-review.md`, but this item is the regular-round prompt.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 section headers present; each assigned section appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`