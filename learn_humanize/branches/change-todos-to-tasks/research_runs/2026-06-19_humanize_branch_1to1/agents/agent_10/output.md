# agent_10 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Shared Markdown block-template library for hook/loop guard decisions. The directory recursively contains 32 `.md` files and no deeper subdirectories; each child is a standardized block reason/remediation prompt rendered into validator or stop-hook responses.
- algorithmic_behavior: Blocks encode deterministic transitions from an invalid user action or loop precondition to a corrective instruction. Main child groups are path/round guards (`wrong-round-number.md`, `wrong-file-location.md`, `wrong-directory-path.md`), protected-state guards (`state-file-modification.md`, `finalize-state-file-modification.md`, `pr-loop-state-modification.md`), Task/todos migration guards (`todos-file-access.md`, `incomplete-todos.md`), git/commit gates (`git-not-clean.md`, `git-add-humanize.md`, `unpushed-commits.md`), goal-tracker gates, and PR-loop gates (`no-trigger-comment.md`, `force-push-detected.md`, `claude-eyes-timeout.md`, `codex-review-failed.md`).
- inputs_outputs_state: Inputs are uppercase `{{VAR}}` placeholder assignments supplied by hooks, for example `CURRENT_ROUND`, `SUMMARY_FILE`, `AHEAD_COUNT`, `CURRENT_BRANCH`, and `BOT_MENTION_STRING`; outputs are rendered Markdown reason strings passed to hook JSON block responses. The templates themselves do not mutate state; consuming hooks use them when blocking or routing loop state.
- gates_or_invariants: The directory defines user-facing invariants: do not create/access `round-*-todos.md`; use native Task tools instead (`prompt-template/block/todos-file-access.md:3`, `prompt-template/block/todos-file-access.md:5`); incomplete work must be finished with `TaskUpdate` before Codex review (`prompt-template/block/incomplete-todos.md:3`, `prompt-template/block/incomplete-todos.md:9`); loop state files are system-managed (`prompt-template/block/state-file-modification.md:3`); round numbers must not be manually incremented (`prompt-template/block/wrong-round-number.md:7`).
- dependencies_and_callers: `hooks/lib/template-loader.sh` locates `prompt-template` (`hooks/lib/template-loader.sh:24-30`), loads templates (`hooks/lib/template-loader.sh:36-47`), and renders placeholders single-pass (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`). `hooks/lib/loop-common.sh` wraps common block messages for todos/task access, prompt/state protection, summary/goal-tracker Bash writes, PR-loop state/prompt protection, and `.humanize` git-add protection (`hooks/lib/loop-common.sh:443-451`, `hooks/lib/loop-common.sh:454-500`, `hooks/lib/loop-common.sh:889-905`, `hooks/lib/loop-common.sh:1058-1065`, `hooks/lib/loop-common.sh:1098-1109`). Stop hooks render specific blocks for incomplete tasks and unpushed commits (`hooks/loop-codex-stop-hook.sh:340-383`, `hooks/loop-codex-stop-hook.sh:580-591`; `hooks/pr-loop-stop-hook.sh:349-356`).
- edge_cases_or_failure_modes: Missing or empty templates fall back to inline messages through `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`); missing variables remain as placeholders by design (`hooks/lib/template-loader.sh:11-13`); single-pass rendering prevents placeholder injection from values (`hooks/lib/template-loader.sh:54-57`). Some filenames keep legacy `todos` naming while message bodies now steer users to Tasks, so callers must preserve compatibility with old `round-*-todos.md` paths while enforcing Task tool usage.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads every template, validates placeholder syntax, tests fallback behavior, and renders representative block templates, including `block/unpushed-commits.md` (`tests/test-templates-comprehensive.sh:87-110`, `tests/test-templates-comprehensive.sh:118-194`, `tests/test-templates-comprehensive.sh:446-488`, `tests/test-templates-comprehensive.sh:517-525`). `tests/test-template-references.sh` also tracks referenced block templates and safe rendering usage, found by reference search.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime bootstrap for `/humanize:start-pr-loop`; it creates a new `.humanize/pr-loop/<timestamp>` session, initializes PR-loop state, fetches initial review comments, builds the first prompt, and optionally posts/reuses bot trigger comments.
- algorithmic_behavior: Parses `--claude`, `--codex`, `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout` (`scripts/setup-pr-loop.sh:110-173`); requires at least one bot and builds the active bot array (`scripts/setup-pr-loop.sh:175-202`); enforces mutual exclusion with active RLCR/PR loops (`scripts/setup-pr-loop.sh:220-240`); verifies git repo, HEAD, `gh`, `gh auth`, and `codex` (`scripts/setup-pr-loop.sh:243-277`); resolves the PR with fork-aware lookup and upstream fallback (`scripts/setup-pr-loop.sh:283-378`); fetches comments and determines startup case (`scripts/setup-pr-loop.sh:430-447`).
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR` or current directory, git branch/HEAD, GitHub CLI/API data, `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`, and prompt templates. Outputs are `round-0-pr-comment.md`, `state.md`, `goal-tracker.md`, `round-0-prompt.md`, stdout activation instructions, and sometimes a PR trigger comment (`scripts/setup-pr-loop.sh:413-431`, `scripts/setup-pr-loop.sh:615-634`, `scripts/setup-pr-loop.sh:641-713`, `scripts/setup-pr-loop.sh:721-851`, `scripts/setup-pr-loop.sh:860-945`).
- gates_or_invariants: Blocks startup unless exactly the PR-loop preconditions hold: open PR, numeric PR number, no active loop, YAML-safe branch/model/effort (`scripts/setup-pr-loop.sh:351-407`), valid trigger comment for cases 4/5, and Claude eyes reaction when Claude is configured (`scripts/setup-pr-loop.sh:462-601`). It refuses to proceed after failed trigger posting and removes the just-created loop dir to avoid orphaned state (`scripts/setup-pr-loop.sh:525-537`, `scripts/setup-pr-loop.sh:580-598`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `jq`, `gh`, `git`, `codex`, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:33-42`, `scripts/setup-pr-loop.sh:210-215`, `scripts/setup-pr-loop.sh:430-438`, `scripts/setup-pr-loop.sh:585-587`). It coordinates with `hooks/pr-loop-stop-hook.sh`, which later enforces trigger, push, bot-response, and completion gates.
- edge_cases_or_failure_modes: Handles fork PRs by extracting repo from PR URL or querying parent with `FORK_OWNER:BRANCH` (`scripts/setup-pr-loop.sh:308-348`); avoids duplicate bot mentions by finding a post-commit trigger mentioning all active bots after stripping code/quotes (`scripts/setup-pr-loop.sh:467-503`); avoids local clock skew by using GitHub comment timestamps and deliberately not falling back to local time (`scripts/setup-pr-loop.sh:540-561`); fails closed on closed/merged PRs, bad YAML chars, missing tools, API failures, or missing Claude eyes.
- validation_or_tests: PR-loop state shape is covered by concurrent robustness tests for `find_active_pr_loop` and YAML bot lists (`tests/robustness/test-concurrent-state-robustness.sh:401-437`). Broader PR setup behavior is referenced in `tests/test-pr-loop-system.sh` by search results, including verification that `setup-pr-loop.sh` creates `goal-tracker.md`; I inspected callers and tests but did not run them because this branch export is read-only.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template subsystem, including the block templates affected by the todos-to-tasks change.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, resolves `TEMPLATE_DIR`, counts pass/fail/warning state, verifies the required `block`, `codex`, and `claude` subdirectories, loads every Markdown template, validates placeholder syntax, tests malformed placeholder detection, exercises rendering edge cases, validates fallback behavior, runs integration renders on real templates, and stress-renders all templates (`tests/test-templates-comprehensive.sh:17-31`, `tests/test-templates-comprehensive.sh:59-110`, `tests/test-templates-comprehensive.sh:118-194`, `tests/test-templates-comprehensive.sh:198-257`, `tests/test-templates-comprehensive.sh:263-435`, `tests/test-templates-comprehensive.sh:446-605`).
- inputs_outputs_state: Inputs are the real `prompt-template` tree and the loader functions `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Outputs are colored PASS/FAIL/WARN lines and exit status 0 only when `TESTS_FAILED` remains zero (`tests/test-templates-comprehensive.sh:31-57`, `tests/test-templates-comprehensive.sh:611-624`).
- gates_or_invariants: Requires templates to be present and non-empty, placeholders to be uppercase `{{VAR_NAME}}` without extra braces/spaces/lowercase starts, missing templates to use fallback, incomplete template dirs to be rejected, and all placeholders in all templates to render away with dummy values (`tests/test-templates-comprehensive.sh:74-107`, `tests/test-templates-comprehensive.sh:124-191`, `tests/test-templates-comprehensive.sh:446-488`, `tests/test-templates-comprehensive.sh:563-602`).
- dependencies_and_callers: Depends on Bash, `find`, `sort`, `grep`, `sed`, `wc`, `mktemp`, and `hooks/lib/template-loader.sh`; it is likely run from CI or `tests/run-all-tests.sh` as a validation surface for template changes.
- edge_cases_or_failure_modes: Explicitly covers empty content, multiline and empty-line preservation, special regex characters, backslashes, quotes, long values, multiline values, variables at line boundaries, duplicate variables, adjacent variables, missing template files, invalid template directories, and rapid successive rendering (`tests/test-templates-comprehensive.sh:263-435`, `tests/test-templates-comprehensive.sh:446-488`, `tests/test-templates-comprehensive.sh:551-605`).
- validation_or_tests: Self-validating executable; notable assigned-path coverage includes direct render checks for `block/unpushed-commits.md` with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch` (`tests/test-templates-comprehensive.sh:517-525`). It was inspected directly; not executed in this read-only research pass.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the “unpushed commits” gate; it tells the worker that exiting is blocked until local commits are pushed.
- algorithmic_behavior: Renders a heading, interpolates `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, states that `--push-every-round` requires pushing before exit, emits `git push origin {{CURRENT_BRANCH}}`, and tells the user to retry after pushing (`prompt-template/block/unpushed-commits.md:1-12`).
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`; output is Markdown reason text. It does not compute git state itself; consuming stop hooks compute ahead count and choose a block decision.
- gates_or_invariants: Enforces the invariant that local commits cannot remain only local when the loop requires remote review visibility. In RLCR it is used when `--push-every-round` is enabled (`hooks/loop-codex-stop-hook.sh:580-591`); in PR loop it is used whenever PR bot review requires pushed changes (`hooks/pr-loop-stop-hook.sh:349-356`).
- dependencies_and_callers: Rendered through `load_and_render_safe` by `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh`; validated by template loader tests (`tests/test-templates-comprehensive.sh:517-525`).
- edge_cases_or_failure_modes: If branch resolution fails, the RLCR caller falls back to `unknown` for `CURRENT_BRANCH` (`hooks/loop-codex-stop-hook.sh:580-583`). In PR loop, if the PR head is not in local history, the hook treats it as one unpushed commit (`hooks/pr-loop-stop-hook.sh:340-342`). Missing variables would remain visibly unresolved because template rendering preserves unknown placeholders.
- validation_or_tests: Direct render test checks the heading, “3 unpushed” content, and branch interpolation (`tests/test-templates-comprehensive.sh:517-525`). This file is also covered indirectly by all-template load/render stress validation.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-130 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for loop state parsing, active-loop detection, PR-loop state shape, and read consistency during concurrent state updates.
- algorithmic_behavior: Creates synthetic `state.md` files, sources `hooks/lib/loop-common.sh`, then runs 22 tests across parsing, loop discovery, concurrent reads, atomic writes, strict validation, PR-loop state, stale loop handling, and permission behavior (`tests/robustness/test-concurrent-state-robustness.sh:1-19`, `tests/robustness/test-concurrent-state-robustness.sh:30-47`, `tests/robustness/test-concurrent-state-robustness.sh:59-559`).
- inputs_outputs_state: Inputs are temp directories from `setup_test_dir`, generated state files, and loop-common functions such as `get_current_round`, `find_active_loop`, `parse_state_file_strict`, and `find_active_pr_loop`. Outputs are pass/fail records through `test-helpers.sh` and final process status via `print_test_summary` (`tests/robustness/test-concurrent-state-robustness.sh:559-560`).
- gates_or_invariants: Requires tolerant parsing to degrade gracefully for whitespace, CRLF, empty values, comments, Unicode, permissions, large and zero rounds; requires newest-loop-only detection so old state is not revived; requires `finalize-state.md` to count active but `cancel-state.md` not to count active; requires strict parser rejection for malformed YAML and missing frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:61-159`, `tests/robustness/test-concurrent-state-robustness.sh:170-240`, `tests/robustness/test-concurrent-state-robustness.sh:333-387`, `tests/robustness/test-concurrent-state-robustness.sh:456-549`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, standard shell tools, and background process handling. The behavior under test is used by setup scripts and hooks that locate active RLCR/PR sessions before allowing startup or validating tool access.
- edge_cases_or_failure_modes: Simulates 10 parallel state reads and a background atomic writer that writes `state.md.tmp` then `mv`s it into place; all 20 reads during the writer must return numeric values (`tests/robustness/test-concurrent-state-robustness.sh:251-322`). Also checks PR loop YAML list formatting for `active_bots` and `configured_bots` (`tests/robustness/test-concurrent-state-robustness.sh:416-437`).
- validation_or_tests: This file is itself the validation artifact for concurrent state access. I inspected it directly but did not execute it because the scheduler requested research notes only and this workspace is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all five assigned evidence headings are present once in the Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`