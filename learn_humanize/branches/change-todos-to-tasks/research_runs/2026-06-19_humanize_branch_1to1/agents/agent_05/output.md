# agent_05 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Central prompt/template surface for the humanize loop algorithms. It defines the text contracts used by setup scripts, stop hooks, template guards, Codex review phases, Claude work phases, PR-loop startup prompts, and plan generation. It is algorithmic because these templates encode state gates, allowed/blocked actions, reviewer workflow transitions, and completion criteria.
- algorithmic_behavior: The directory is organized into five functional child groups:
  - `prompt-template/block/`: guard/block messages returned by hooks when an operation violates loop invariants. Examples include prompt write protection at `prompt-template/block/prompt-file-write.md:1`, PR-loop state protection at `prompt-template/block/pr-loop-state-modification.md:1`, missing trigger requirements at `prompt-template/block/no-trigger-comment.md:1`, Claude eyes timeout handling at `prompt-template/block/claude-eyes-timeout.md:1`, and `.humanize` git-add protection at `prompt-template/block/git-add-humanize.md:1`.
  - `prompt-template/claude/`: Claude-facing RLCR phase prompts. `next-round-prompt.md` tells Claude to read the original plan, create todos for all discovered issues, consume Codex review content, and treat `goal-tracker.md` as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1`, `:12`, `:22`). `finalize-phase-prompt.md` and `finalize-phase-skipped-prompt.md` define final simplification/verification behavior (`prompt-template/claude/finalize-phase-prompt.md:1`, `prompt-template/claude/finalize-phase-skipped-prompt.md:1`). `review-phase-prompt.md` requires addressing `[P0-9]` findings before proceeding (`prompt-template/claude/review-phase-prompt.md:11`).
  - `prompt-template/codex/`: Codex-facing review/audit prompts. `regular-review.md` requires critical implementation review against the original plan and only permits `COMPLETE` when all work is fully done with no deferrals (`prompt-template/codex/regular-review.md:20`, `:49`). `full-alignment-review.md` adds mandatory acceptance-criteria audit, forgotten-item detection, deferred-item audit, stagnation detection, and `STOP`/`COMPLETE` terminal signals (`prompt-template/codex/full-alignment-review.md:19`, `:59`, `:84`, `:91`). `goal-tracker-update-section.md` makes Codex responsible for approved goal-tracker changes after round 0 (`prompt-template/codex/goal-tracker-update-section.md:1`).
  - `prompt-template/plan/`: `gen-plan-template.md` is the generated plan skeleton. It enforces goal description, acceptance criteria with positive/negative tests, path boundaries, dependency sequencing, and a rule that implementation code must not contain plan workflow markers (`prompt-template/plan/gen-plan-template.md:1`, `:6`, `:25`, `:57`, `:69`).
  - `prompt-template/pr-loop/`: PR-loop startup and review-tracker templates. `round-0-header.md` provides PR metadata and fetched comments (`prompt-template/pr-loop/round-0-header.md:1`). `round-0-task-no-comments.md` encodes the no-comment startup path where bots review automatically and no trigger comment should be posted (`prompt-template/pr-loop/round-0-task-no-comments.md:4`). `round-0-task-has-comments.md` encodes the existing-comment path where issues must be fixed, committed, pushed, and re-triggered (`prompt-template/pr-loop/round-0-task-has-comments.md:4`). `goal-tracker-initial.md` defines PR review tracking schema (`prompt-template/pr-loop/goal-tracker-initial.md:1`). `codex-goal-tracker-update.md` defines how Codex/stop-hook updates issue summary, totals, and log (`prompt-template/pr-loop/codex-goal-tracker-update.md:1`).
- inputs_outputs_state: Inputs are `{{VAR}}` placeholders rendered by `hooks/lib/template-loader.sh`. The loader documents single-pass substitution, uppercase placeholder syntax, missing-variable preservation, and fallback rendering (`hooks/lib/template-loader.sh:7`, `:50`, `:167`). Outputs are rendered prompts or block reasons used by setup scripts and hooks. `scripts/setup-pr-loop.sh` renders PR-loop goal tracker and round-0 prompt templates into `.humanize/pr-loop/<timestamp>/goal-tracker.md` and `round-0-prompt.md` (`scripts/setup-pr-loop.sh:712`, `:757`, `:800`, `:847`). `hooks/pr-loop-stop-hook.sh` renders trigger and Claude-eyes block messages and Codex goal-tracker instructions (`hooks/pr-loop-stop-hook.sh:739`, `:784`, `:1261`). State transitions are indirect: templates instruct agents or block hooks so the runtime can move between active state, next round, approval, cancellation, max-iteration, or review-blocked states.
- gates_or_invariants: Major invariants encoded here include no prompt self-editing (`prompt-template/block/prompt-file-write.md:3`), no state-file modification (`prompt-template/block/state-file-modification.md:3`, `prompt-template/block/pr-loop-state-modification.md:3`), no PR-loop prompt/comment writes (`prompt-template/block/pr-loop-prompt-write.md:3`), no `round-*-todos.md` access in favor of native Task tools (`prompt-template/block/todos-file-access.md:3`), no broad git-add of `.humanize` (`prompt-template/block/git-add-humanize.md:1`), required summaries before exit (`prompt-template/block/work-summary-missing.md:1`), trigger-comment gate for cases requiring re-review (`prompt-template/block/no-trigger-comment.md:1`), and strict `COMPLETE` only when all original requirements are met with no deferrals (`prompt-template/codex/regular-review.md:53`, `prompt-template/codex/full-alignment-review.md:91`).
- dependencies_and_callers: Primary loader is `hooks/lib/template-loader.sh`, initialized by `hooks/lib/loop-common.sh:139` and validated with graceful fallback at `hooks/lib/loop-common.sh:146`. PR-loop setup callers are `scripts/setup-pr-loop.sh:712`, `:758`, `:800`, `:847`, `:914`, and `:940`. PR-loop stop-hook callers include `hooks/pr-loop-stop-hook.sh:739`, `:784`, and `:1261`. Hook guard callers include `hooks/lib/loop-common.sh:451`, `:460`, `:895`, and `:905`. Plan template is used by `scripts/validate-gen-plan-io.sh:136`. Template reference integrity is tested by `tests/test-template-references.sh` and comprehensive rendering behavior by `tests/test-templates-comprehensive.sh`.
- edge_cases_or_failure_modes: Missing templates fall back to inline messages via `load_and_render_safe` rather than crashing (`hooks/lib/template-loader.sh:177`, `:179`, `:192`). Missing placeholders remain unreplaced, which preserves prompt content but can leak raw `{{VAR}}` tokens into prompts (`hooks/lib/template-loader.sh:120`). Template directory validation only requires `block`, `codex`, and `claude` subdirectories, so `pr-loop` and `plan` absence would be discovered at call time by fallback behavior rather than initial validation (`hooks/lib/template-loader.sh:216`). Single-pass rendering prevents injected template syntax inside review content from being expanded (`hooks/lib/template-loader.sh:54`).
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies PR goal-tracker template schema (`tests/test-pr-loop-system.sh:642`), goal-tracker creation via setup (`tests/test-pr-loop-system.sh:1453`), and stop-hook updates (`tests/test-pr-loop-system.sh:1542`). `tests/test-template-references.sh` scans references and includes `block/prompt-file-write.md` as a common template (`tests/test-template-references.sh:154`). `tests/test-templates-comprehensive.sh` covers load/render/fallback behavior. I did not execute tests because this branch export is read-only and these tests create temp repos, fixtures, and loop state.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-035 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop classifier. It determines the PR startup/review state from GitHub comments, review comments, PR review submissions, and latest commit timestamp, producing JSON consumed by setup and stop-hook logic.
- algorithmic_behavior: The script parses `<pr_number> --bots <bot1,bot2>` (`scripts/check-pr-reviewer-status.sh:49`). It maps logical bot names to GitHub authors, with `codex` mapped specially to `chatgpt-codex-connector[bot]` and all other bots to `<bot>[bot]` (`scripts/check-pr-reviewer-status.sh:94`). It resolves the correct PR base repository by trying the current repo and then the parent repo for fork PR support (`scripts/check-pr-reviewer-status.sh:113`, `:126`). It fetches latest head SHA and latest commit date with sorted commit dates (`scripts/check-pr-reviewer-status.sh:142`). It aggregates three review surfaces: issue comments, inline review comments, and PR review submissions (`scripts/check-pr-reviewer-status.sh:153`, `:156`, `:159`, `:164`, `:168`). For each configured bot it determines whether the bot commented, whether the bot is missing, and whether that bot’s newest review is stale relative to the latest commit (`scripts/check-pr-reviewer-status.sh:179`, `:183`, `:190`, `:194`).
- inputs_outputs_state: Inputs are GitHub CLI availability, `jq`, PR number, comma-separated bot list, repository context, and remote PR data. Output is JSON with `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` (`scripts/check-pr-reviewer-status.sh:259`). No local state file is written. Its state effect is by classification: `scripts/setup-pr-loop.sh` stores `startup_case`, latest commit fields, and trigger metadata into PR-loop `state.md` (`scripts/setup-pr-loop.sh:437`, `:629`), while `hooks/pr-loop-stop-hook.sh` re-runs it to update dynamic `startup_case` after later reviews (`hooks/pr-loop-stop-hook.sh:1544`).
- gates_or_invariants: Argument validation is strict: missing PR number, missing `--bots`, empty bot list, unknown option, or multiple PR numbers exit 1 (`scripts/check-pr-reviewer-status.sh:49`, `:75`, `:81`). GitHub operations are wrapped in a portable timeout (`scripts/check-pr-reviewer-status.sh:35`, `:40`). Case classification is:
  - Case 1 when no configured reviewers have commented (`scripts/check-pr-reviewer-status.sh:220`).
  - Case 2 when some reviewers commented, some are missing, and no commented reviewer is stale (`scripts/check-pr-reviewer-status.sh:223`, `:231`).
  - Case 3 when all reviewers commented and none are stale (`scripts/check-pr-reviewer-status.sh:234`, `:242`).
  - Case 4 when all reviewers commented but at least one reviewer is stale (`scripts/check-pr-reviewer-status.sh:237`).
  - Case 5 when some reviewers commented and at least one commented reviewer is stale (`scripts/check-pr-reviewer-status.sh:226`).
  The key invariant is per-bot freshness, not global newest review freshness, so one fresh bot cannot hide another stale bot (`scripts/check-pr-reviewer-status.sh:192`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` for portable `run_with_timeout` (`scripts/portable-timeout.sh:31`) and external `gh`/`jq`. Called during PR-loop setup (`scripts/setup-pr-loop.sh:437`) and after local Codex validation in the stop hook (`hooks/pr-loop-stop-hook.sh:1547`). Tested with `tests/mocks/gh`, which simulates PR view, comments, review comments, PR reviews, repo view, and commit timestamps (`tests/mocks/gh:27`, `:104`, `:154`, `:170`, `:180`, `:197`).
- edge_cases_or_failure_modes: If PR base repo cannot be resolved, it warns and falls back to `CURRENT_REPO`, which may still be empty if `gh repo view` failed (`scripts/check-pr-reviewer-status.sh:137`). Comment API fetch failures degrade to `[]`, which can classify a bot as missing instead of failing hard (`scripts/check-pr-reviewer-status.sh:156`, `:159`, `:164`). Commit-info fetch failure is fatal (`scripts/check-pr-reviewer-status.sh:143`). Timestamp comparison uses lexicographic ISO strings (`scripts/check-pr-reviewer-status.sh:194`), so correctness depends on consistent UTC sortable timestamps. Empty bot tokens caused by malformed comma lists are not explicitly trimmed or rejected.
- validation_or_tests: `tests/test-pr-loop-system.sh` covers case 1 no comments (`:152`), case 2 partial comments (`:173`), PR review submission inclusion (`:236`), case 3 all commented (`:493`), valid JSON for startup case 4/5 detection (`:661`), exact case 4 all commented plus new commits (`:1315`), and exact case 5 partial commented plus new commits (`:1347`). It also validates setup failure when case 4/5 requires a trigger comment ID but the ID lookup fails (`tests/test-pr-loop-system.sh:1384`). I did not execute the test script because it mutates fixtures/temp repos and the export is read-only.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-065 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop system behavior. It covers startup case classification, mutual exclusion between RLCR and PR loops, reaction detection, monitor phase detection/display, goal-tracker schema/update behavior, setup failure gates, and stop-hook integration.
- algorithmic_behavior: The test runner creates an isolated temp git repo per test, injects `tests/mocks` into `PATH`, points `MOCK_GH_FIXTURES_DIR` at fixtures, and runs each test in a subshell (`tests/test-pr-loop-system.sh:51`, `:81`). It defines 42 `run_test` invocations in `main` (`tests/test-pr-loop-system.sh:1761`, `:1775`). It validates both setup-time behavior and stop-hook behavior by creating `.humanize/pr-loop/<timestamp>` or `.humanize/rlcr/<timestamp>` directories and state files.
- inputs_outputs_state: Inputs are fixture JSON files under `tests/fixtures`, the mock `gh` CLI, temporary loop state files, and sourced runtime libraries (`scripts/lib/monitor-common.sh`, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`). Outputs are pass/fail counters and exit code 1 on any failed test (`tests/test-pr-loop-system.sh:1889`, `:1899`). State transitions modeled include `state.md` active, `approve-state.md` approved, `cancel-state.md` cancelled, `maxiter-state.md` max-iteration, updated `goal-tracker.md` rows/totals/log entries, and round advancement through stop-hook logic.
- gates_or_invariants: Key gates encoded by the tests:
  - PR loop and RLCR loop are mutually exclusive (`tests/test-pr-loop-system.sh:110`, `:129`).
  - Reviewer-status cases must classify no comments, partial comments, all comments, stale all-reviewer comments, and stale partial-reviewer comments (`tests/test-pr-loop-system.sh:152`, `:173`, `:493`, `:1315`, `:1347`).
  - PR review submissions count as reviewer comments, not only issue/inline comments (`tests/test-pr-loop-system.sh:236`).
  - Case 1/2/3 at round 0 do not require trigger, but case 4 and later rounds do (`tests/test-pr-loop-system.sh:1112`).
  - Setup for case 4/5 with Claude must fail and clean up if `trigger_comment_id` cannot be retrieved (`tests/test-pr-loop-system.sh:1384`, `:1420`, `:1431`).
  - Goal-tracker updates must be idempotent by round+reviewer and repair partial updates where either the summary row or log entry is missing (`tests/test-pr-loop-system.sh:722`, `:1242`).
  - Monitor phase labels must display approved, waiting, cancelled, and Codex analyzing states (`tests/test-pr-loop-system.sh:947`, `:985`, `:1026`, `:1065`).
- dependencies_and_callers: Calls `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, and `hooks/lib/loop-common.sh` (`tests/test-pr-loop-system.sh:123`, `:142`, `:159`, `:201`, `:226`, `:935`, `:1723`). Uses mock GitHub behavior from `tests/mocks/gh`, including PR state, commits, comments, review comments, PR reviews, and reaction fixtures (`tests/mocks/gh:58`, `:66`, `:154`, `:170`, `:180`). Some tests create local mock `gh`, `git`, and `codex` scripts for stop-hook integration (`tests/test-pr-loop-system.sh:1595`, `:1671`, `:1692`).
- edge_cases_or_failure_modes: This file intentionally mutates fixture JSON and then restores it in several tests, so interrupted execution can leave fixtures changed (`tests/test-pr-loop-system.sh:154`, `:166`, `:1374`). It depends on `timeout`, `git`, `jq`, shell function export support, and date syntax compatible with GNU or BSD via fallback expressions (`tests/test-pr-loop-system.sh:943`, `:1549`). It writes `/tmp/humanize-phase-*.size` cache during phase-detection tests through runtime code (`scripts/lib/monitor-common.sh:297`). Because the test suite creates repos, files, and fixture modifications, it is not safe to run in this read-only branch export.
- validation_or_tests: This is itself the validation surface. Coverage includes reviewer status (`tests/test-pr-loop-system.sh:1779`), reactions (`:1784`), PR reviews (`:1790`), phases (`:1794`, `:1814`, `:1851`), goal tracker parsing and updates (`:1800`, `:1804`, `:1835`, `:1867`, `:1871`), startup cases (`:1841`, `:1875`), setup failure (`:1880`), and setup/stop-hook integration (`:1884`). I inspected directly but did not execute because the workspace is read-only.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-095 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Guard/block template for attempts to write `round-*-prompt.md` files. It protects generated instruction files from being modified by the worker receiving those instructions.
- algorithmic_behavior: The template states that prompt files contain instructions from Codex to Claude and cannot be modified (`prompt-template/block/prompt-file-write.md:3`, `:5`). It redirects behavior into the valid transition: read the current prompt, execute its tasks, write results to the summary file, and document prompt errors in the summary instead of editing the prompt (`prompt-template/block/prompt-file-write.md:7`).
- inputs_outputs_state: Inputs are none beyond rendering selection. It has no placeholders. Output is a block reason from hook code. It does not mutate state; it prevents prompt-file writes so state remains controlled by loop scripts.
- gates_or_invariants: Invariant is prompt immutability: generated round prompts are authoritative instructions, not editable working files. The valid output path for findings is the summary file (`prompt-template/block/prompt-file-write.md:10`). This complements PR-specific prompt/comment protection in `prompt-template/block/pr-loop-prompt-write.md:3`.
- dependencies_and_callers: Rendered by `prompt_write_blocked_message` in `hooks/lib/loop-common.sh:454`, using `load_and_render_safe "$TEMPLATE_DIR" "block/prompt-file-write.md"` at `hooks/lib/loop-common.sh:460`. Template loading is supplied by `hooks/lib/template-loader.sh`. It is included in template-reference tests as a common required template (`tests/test-template-references.sh:154`).
- edge_cases_or_failure_modes: If the template is missing, `prompt_write_blocked_message` falls back to an inline shorter message (`hooks/lib/loop-common.sh:456`). Since this template has no variables, rendering failure risk is limited to file absence or unreadability.
- validation_or_tests: `tests/test-template-references.sh` explicitly checks this common template exists (`tests/test-template-references.sh:154`). `tests/test-templates-comprehensive.sh` and `tests/test-template-loader.sh` cover load/render/fallback behavior generally. I did not run tests due read-only constraints.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-125 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template for the startup path where fetched PR comments already exist. It defines the worker’s initial repair workflow and re-review trigger contract.
- algorithmic_behavior: The task requires the worker to analyze fetched comments with human comments first and bot comments newest first (`prompt-template/pr-loop/round-0-task-has-comments.md:6`). It then requires fixing reviewer issues by reading relevant code, making changes, and adding tests if needed (`prompt-template/pr-loop/round-0-task-has-comments.md:10`). After fixes, it requires commit, push, and PR re-trigger comment using `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review the latest changes"` (`prompt-template/pr-loop/round-0-task-has-comments.md:15`). It then requires a resolution summary at `@{{RESOLVE_PATH}}` listing addressed issues, modified files, and tests (`prompt-template/pr-loop/round-0-task-has-comments.md:23`).
- inputs_outputs_state: Inputs are rendered variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, plus fetched PR comments appended above this template by setup. Output is part of `.humanize/pr-loop/<timestamp>/round-0-prompt.md`; the expected worker output is `round-0-pr-resolve.md` plus commits/pushes and a trigger PR comment. The runtime effect is to move from “existing comments need action” toward stop-hook polling for new bot reviews after exit.
- gates_or_invariants: Enforced behavioral rules are: do not modify `.humanize/pr-loop` state files, always push changes so remote bots can review them, use the correct bot-tagging comment format, and address all valid concerns (`prompt-template/pr-loop/round-0-task-has-comments.md:30`). The stop-hook then polls for new reviews, runs local Codex validation, continues on issues, and ends only when all bots approve (`prompt-template/pr-loop/round-0-task-has-comments.md:39`).
- dependencies_and_callers: Selected by `scripts/setup-pr-loop.sh` when fetched comments are not the no-comments sentinel. The setup script appends fetched comments to `round-0-prompt.md`, chooses this template when `COMMENT_COUNT` is nonzero, renders it through `load_and_render_safe`, and appends it to the prompt (`scripts/setup-pr-loop.sh:723`, `:766`, `:801`, `:847`, `:850`). It pairs with `prompt-template/pr-loop/critical-requirements-has-comments.md`, rendered at setup output time when comments exist (`scripts/setup-pr-loop.sh:915`, `:940`).
- edge_cases_or_failure_modes: The branch between this template and `round-0-task-no-comments.md` depends on `fetch-pr-comments.sh` emitting exactly the sentinel `*No comments found.*`; any other nonempty comment file counts as “has comments” (`scripts/setup-pr-loop.sh:723`). If a worker fixes issues but fails to push or fails to use the exact trigger comment format, the stop hook may block or poll stale/no reviews. If `BOT_MENTION_STRING` or `PR_NUMBER` is unresolved, the rendered command would be invalid; missing placeholders are preserved by the template loader rather than failing (`hooks/lib/template-loader.sh:120`).
- validation_or_tests: `tests/test-pr-loop-system.sh` indirectly validates the corresponding setup path and trigger requirements through case 4/5 trigger tests and setup failure behavior (`tests/test-pr-loop-system.sh:1112`, `:1384`). Template reference search confirms this file is rendered by `scripts/setup-pr-loop.sh:847`. I did not run tests because they write state and fixtures.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 assigned item headers present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`