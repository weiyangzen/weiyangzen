# agent_20 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` command workflow that turns a draft design document into a structured implementation plan. It is a command-level algorithm spec rather than runtime code: it sequences validation, relevance checking, repository analysis, clarification, deterministic plan generation, output editing, review, and final reporting. See the workflow overview in `commands/gen-plan.md:19-26`.
- algorithmic_behavior: The command runs as six phases. Phase 1 delegates input/output validation to `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` and branches on exit codes `0-7` (`commands/gen-plan.md:30-47`). Phase 2 reads the draft and invokes `humanize:draft-relevance-checker` through `Task`, expecting `RELEVANT` or `NOT_RELEVANT` (`commands/gen-plan.md:50-72`). Phase 3 explores affected code using subagents and checks clarity, consistency, completeness, and functionality (`commands/gen-plan.md:76-108`). Phase 4 asks the user to resolve significant issues and explicitly classifies quantitative metrics as hard requirements or optimization directions (`commands/gen-plan.md:111-139`). Phase 5 generates the canonical plan structure with goal, AC-style criteria, path boundaries, hints, dependencies, and code-style notes (`commands/gen-plan.md:142-247`). Phase 6 edits the pre-created output, rereads it completely, optionally unifies language, then reports path, contents, AC count, and language decision (`commands/gen-plan.md:251-292`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` with `--input` and `--output`, the draft file, the current repository structure, user clarifications, and optional language preference (`commands/gen-plan.md:3`, `commands/gen-plan.md:34-36`, `commands/gen-plan.md:56-65`, `commands/gen-plan.md:274-284`). The output is a plan file that contains both the generated structured plan and the preserved original draft at the bottom (`commands/gen-plan.md:253-260`). The IO validator copies `prompt-template/plan/gen-plan-template.md` to the output and appends the original draft, making the output file an initialized state artifact before Phase 6 edits it; this dependency behavior is implemented in `scripts/validate-gen-plan-io.sh:133-160`.
- gates_or_invariants: The command stops on invalid IO, empty or missing input, missing output directory, existing output, unwritable output directory, invalid arguments, or missing plan template (`commands/gen-plan.md:38-47`; implementation detail in `scripts/validate-gen-plan-io.sh:79-145`). It stops when the relevance agent returns `NOT_RELEVANT` (`commands/gen-plan.md:67-72`). It forbids dropping or overriding draft content during clarification and requires the final plan to be a superset of the original draft plus clarifications (`commands/gen-plan.md:111-114`, `commands/gen-plan.md:247`). Generated ACs must use `AC-X` or `AC-X.Y`, include positive and negative tests, avoid time estimates and line-number references, and include a code-style constraint keeping plan terminology out of implementation code (`commands/gen-plan.md:225-247`). Deterministic draft choices must narrow path boundaries rather than invent optionality (`commands/gen-plan.md:192`, `commands/gen-plan.md:243`).
- dependencies_and_callers: Declared allowed tools are the IO validation Bash script, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` (`commands/gen-plan.md:4-11`). The command depends on `scripts/validate-gen-plan-io.sh`, `prompt-template/plan/gen-plan-template.md`, and `agents/draft-relevance-checker.md`; the relevance agent is named in `commands/gen-plan.md:57-65`. README documents the command as `/humanize:gen-plan --input <draft.md> --output <plan.md>` in `README.md:76-78` and `README.md:126-135`.
- edge_cases_or_failure_modes: Edge cases include output path already existing or being a directory, missing template file, invalid flag structure, draft being unrelated, unresolved contradictions, quantitative thresholds being ambiguous, mixed-language output, and deterministic designs with no real implementation choices (`commands/gen-plan.md:38-47`, `commands/gen-plan.md:126-139`, `commands/gen-plan.md:192`, `commands/gen-plan.md:272-284`, `commands/gen-plan.md:296-304`). A notable behavioral invariant is that Phase 6 says to use the Edit tool rather than Write because the validation script has already created the output file (`commands/gen-plan.md:253-260`).
- validation_or_tests: `tests/test-gen-plan.sh` validates the command file structure, YAML frontmatter, naming, required fields, English-only content, and the IO validator’s exit-code behavior. The validator tests cover missing flag values, unknown flags, missing input, empty input, nonexistent output directory, existing output, output as directory, valid paths, and help behavior (`tests/test-gen-plan.sh:426-545`). I inspected these tests but did not execute them because this run is research-only in a read-only branch export.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR cancel safety algorithm. It verifies that the Bash hook permits exactly the intentional loop cancellation transition from an active state file to `cancel-state.md` when a `.cancel-requested` signal exists, while blocking direct or disguised mutation of loop state files.
- algorithmic_behavior: The test creates a temporary active RLCR loop at `.humanize/rlcr/2024-01-01_12-00-00` with `state.md`, exports `CLAUDE_PROJECT_DIR`, and sends synthetic Bash hook JSON to `hooks/loop-bash-validator.sh` (`tests/test-cancel-signal-file.sh:42-73`). Positive cases allow `mv <loop>/state.md <loop>/cancel-state.md` with a signal file, including `./` path normalization, single-quoted paths, and literal `${loop_dir}` forms (`tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-486`). Negative cases ensure the same rename is blocked without the signal, wrong destinations are blocked, non-rename edits are blocked, wrong-directory signals are ignored, and source-file moves/copies to arbitrary targets are rejected (`tests/test-cancel-signal-file.sh:121-237`, `tests/test-cancel-signal-file.sh:283-297`, `tests/test-cancel-signal-file.sh:1208-1227`).
- inputs_outputs_state: Inputs are generated shell command strings, generated hook JSON, the active loop directory, `state.md`, optional `.cancel-requested`, and direct helper calls to `is_cancel_authorized` (`tests/test-cancel-signal-file.sh:63-73`, `tests/test-cancel-signal-file.sh:1251-1343`). Outputs are pass/fail counters and process exit status equal to `TESTS_FAILED` (`tests/test-cancel-signal-file.sh:1345-1357`). The state transition under test is logical authorization, not actual command execution: the validator returns exit `0` for authorized cancellation and exit `2` for blocked state-file mutation. The real cancellation scripts create the signal and perform the rename; for RLCR this is `scripts/cancel-rlcr-loop.sh:85-87` and `scripts/cancel-rlcr-loop.sh:137-151`.
- gates_or_invariants: The allowed transition requires an active loop, `.cancel-requested` in that exact loop directory, command beginning with `mv`, exactly two arguments, source path equal to that loop’s `state.md` or `finalize-state.md`, destination equal to that loop’s `cancel-state.md`, no hidden variables, no command substitution, no newlines, no shell chaining operators, no mixed quoting, no extra arguments, and no symlink source. These invariants are enforced by `is_cancel_authorized` in `hooks/lib/loop-common.sh:551-710`. The Bash validator first strict-parses active loop state, blocks malformed state fail-closed, then applies state-file modification protections with cancellation as the only exception (`hooks/loop-bash-validator.sh:74-85`, `hooks/loop-bash-validator.sh:127-156`). It separately detects `mv` and `cp` with state files as sources, including command segments split around shell operators and redirection prefixes (`hooks/loop-bash-validator.sh:158-304`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` and calls `hooks/loop-bash-validator.sh` through `run_bash_validator` (`tests/test-cancel-signal-file.sh:32-33`, `tests/test-cancel-signal-file.sh:61-73`). It depends on `jq` for safe JSON construction. It exercises `to_lower`, `is_cancel_authorized`, `find_active_loop`, `command_modifies_file`, and the hook’s state protection logic (`hooks/lib/loop-common.sh:156-174`, `hooks/lib/loop-common.sh:394-397`, `hooks/lib/loop-common.sh:551-710`, `hooks/lib/loop-common.sh:1071-1096`).
- edge_cases_or_failure_modes: The test covers injection through `&&`, `;`, newline, pipes, background operators, command substitution, backticks, hidden `${IFS}`, wrapper shells, `sudo`, `env`, `command --`, leading whitespace, extra arguments before or after `state.md`, quoted relative paths, unrelated `/tmp/state.md`, and many redirection-prefix forms including append, stderr, combined stdout/stderr, quoted targets, escaped-space targets, and ANSI-C quoted targets (`tests/test-cancel-signal-file.sh:240-357`, `tests/test-cancel-signal-file.sh:488-846`, `tests/test-cancel-signal-file.sh:848-1206`). It also asserts commands are allowed when no active loop exists, because the hook should not globally block state-like filenames outside active loop context (`tests/test-cancel-signal-file.sh:1229-1249`).
- validation_or_tests: This file is itself the validation suite. It specifies expected hook exit codes: `0` for authorized or irrelevant commands and `2` for blocked state-file mutation. The direct helper tests at the end validate true, no-signal false, wrong-command false, extra-argument false, quoted-path true, extra-before false, and hidden-variable false (`tests/test-cancel-signal-file.sh:1251-1343`). I inspected the executable specification but did not run it in this read-only research pass.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Prompt block used by the RLCR stop hook’s git-clean gate when `.humanize` loop artifacts appear as untracked files. Its role is to steer the user toward ignoring local loop state instead of committing the loop directory.
- algorithmic_behavior: The template emits a special-case note that `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` via `echo '.humanize*' >> .gitignore` followed by `git add .gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). The caller loads this block when cached git status has untracked `.humanize*` paths during the quick clean/pushed check (`hooks/loop-codex-stop-hook.sh:517-535`).
- inputs_outputs_state: Input is the stop hook’s detection of untracked files matching `.humanize` in git status (`hooks/loop-codex-stop-hook.sh:522-531`). Output is human-facing guidance appended to `SPECIAL_NOTES` in the blocked git-clean response (`hooks/loop-codex-stop-hook.sh:519-535`). The intended state transition is from “loop-local artifacts are untracked and blocking clean-git exit” to “`.humanize*` is ignored through tracked `.gitignore` change,” while preserving the invariant that loop runtime files are not committed.
- gates_or_invariants: The gate invariant is that an RLCR round should not proceed as clean while uncommitted changes exist, and `.humanize*` should be treated specially as local loop state rather than implementation output (`hooks/loop-codex-stop-hook.sh:512-550`). The template reinforces “should NOT be committed” (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). It does not itself enforce behavior; enforcement comes from the stop hook and git status check.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` (`hooks/loop-codex-stop-hook.sh:529-535`). It depends on the shared template loader and the stop hook’s cached git status analysis. It sits alongside `prompt-template/block/git-not-clean-untracked.md` for non-`.humanize` untracked files (`hooks/loop-codex-stop-hook.sh:538-545`).
- edge_cases_or_failure_modes: The suggested `echo` command may append duplicate `.humanize*` entries if repeated. The template assumes `.gitignore` is the right ignore surface and that committing `.gitignore` is acceptable for the repository. If the template is missing or empty, the hook falls back to a shorter inline note (`hooks/loop-codex-stop-hook.sh:531-534`).
- validation_or_tests: No dedicated test for this exact template was found. Its behavior is indirectly covered by stop-hook git-clean flows and template loading. The file is small and static; validation is primarily by caller fallback behavior and manual inspection.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Footer template appended to the next-round Claude prompt after Codex review finds issues. It defines the next worker round’s exit contract: do not falsify loop completion or mutate loop state, commit work, and write the required summary file.
- algorithmic_behavior: The template starts with a separator, warns that the worker must not exit by lying, editing loop state files, or running `cancel-rlcr-loop`, then lists post-work requirements: optionally invoke `code-simplifier` if installed, commit changes, and write the work summary into `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:2-9`). The stop hook appends it after incrementing `current_round`, creating `round-N-prompt.md`, and computing `NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:1506-1541`).
- inputs_outputs_state: Input is the rendered template variable `NEXT_SUMMARY_FILE`, set to `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` (`hooks/loop-codex-stop-hook.sh:1511-1513`, `hooks/loop-codex-stop-hook.sh:1537-1541`). Output is appended instruction text in the next round prompt. The state transition it supports is review-loop continuation: the stop hook updates the state file’s `current_round`, writes a new prompt, appends this footer, and expects the next actor to produce code changes plus a summary artifact (`hooks/loop-codex-stop-hook.sh:1506-1541`).
- gates_or_invariants: The footer reinforces anti-bypass invariants: no false completion, no state-file edits, and no self-cancellation (`prompt-template/claude/next-round-footer.md:4`). It also establishes completion evidence requirements for the next round: a commit and a summary file at the rendered path (`prompt-template/claude/next-round-footer.md:6-9`). These instructions pair with hook-level protections that block state-file mutation and summary writes through Bash, forcing summary creation through approved tooling paths (`hooks/loop-bash-validator.sh:127-156`, `hooks/loop-bash-validator.sh:370-379`).
- dependencies_and_callers: Rendered by `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` with fallback text if the template is missing (`hooks/loop-codex-stop-hook.sh:1537-1541`). It depends on the template loader’s placeholder replacement for `NEXT_SUMMARY_FILE`. It is also coupled to optional plugin discovery by instruction only: “If the `code-simplifier` plugin is installed” (`prompt-template/claude/next-round-footer.md:6-7`).
- edge_cases_or_failure_modes: If `NEXT_SUMMARY_FILE` is not rendered, the prompt would contain a stale placeholder and the worker might write to the wrong summary target. If `code-simplifier` is not installed, the instruction is explicitly optional. The template cannot enforce committing or summary writing by itself; it relies on loop stop hooks and later validation to block exit when expected artifacts are missing or dirty state remains.
- validation_or_tests: No dedicated template test was found for this footer. It is indirectly exercised by stop-hook continuation prompt generation in `hooks/loop-codex-stop-hook.sh:1502-1541`. I inspected the caller path and template text directly.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-140 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It defines how tolerant and strict parsers handle valid state, malformed state, partial writes, default values, and optional fields used by loop scheduling.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates a temporary test directory, then generates many state-file variants and checks `parse_state_file`, `get_current_round`, and `parse_state_file_strict` behavior (`tests/robustness/test-state-file-robustness.sh:12-20`). Positive tests cover all common fields, extra unrecognized fields, quoted values, and round zero (`tests/robustness/test-state-file-robustness.sh:33-140`). Negative and robustness tests cover missing frontmatter, nonnumeric fields, missing fields, empty files, partial YAML, malformed YAML, very large and negative rounds, special characters, whitespace, nonexistent files, binary suffix content, CRLF line endings, and `full_review_round` parsing/default/minimum (`tests/robustness/test-state-file-robustness.sh:149-474`).
- inputs_outputs_state: Inputs are synthetic Markdown state files with YAML frontmatter and parser function calls. Outputs are global parser variables such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, and `STATE_FULL_REVIEW_ROUND`, plus test pass/fail counters and final exit status from `print_test_summary` (`tests/robustness/test-state-file-robustness.sh:52-60`, `tests/robustness/test-state-file-robustness.sh:415-448`, `tests/robustness/test-state-file-robustness.sh:476-481`). The parser implementation sets defaults in tolerant mode for current round, max iterations, push behavior, and full-review cadence (`hooks/lib/loop-common.sh:209-243`).
- gates_or_invariants: Tolerant parsing should return defaults where possible and avoid crashing on malformed or partial files; examples include missing frontmatter returning round `0`, missing fields defaulting `current_round=0` and `max_iterations=10`, empty file returning round `0`, and CRLF handling not crashing (`tests/robustness/test-state-file-robustness.sh:163-171`, `tests/robustness/test-state-file-robustness.sh:217-240`, `tests/robustness/test-state-file-robustness.sh:386-397`). Strict parsing should reject missing frontmatter, missing required fields, nonnumeric round or iteration values, invalid boolean `review_started`, and missing `base_branch` or `review_started` (`hooks/lib/loop-common.sh:255-329`). This strict fail-closed behavior is consumed by hooks before allowing protected operations (`hooks/loop-bash-validator.sh:81-85`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` parser functions: `get_current_round` (`hooks/lib/loop-common.sh:179-189`), `parse_state_file` (`hooks/lib/loop-common.sh:209-243`), and `parse_state_file_strict` (`hooks/lib/loop-common.sh:255-329`). Depends on `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/test-helpers.sh:30-89`). Runtime callers of the same parser include the Bash validator and stop/monitor logic, so parser behavior affects loop safety and round progression.
- edge_cases_or_failure_modes: The test intentionally permits tolerant parser ambiguity for partial YAML and nonnumeric current round, checking graceful handling rather than exact values (`tests/robustness/test-state-file-robustness.sh:190-199`, `tests/robustness/test-state-file-robustness.sh:242-257`). Some strict-negative fixtures assert only nonzero failure, so they may not prove the precise intended error class after stricter required fields were added; for example a fixture missing `review_started` and `base_branch` can fail before numeric validation, while still satisfying the test’s broad rejection condition (`tests/robustness/test-state-file-robustness.sh:173-188`; required fields in `hooks/lib/loop-common.sh:289-305`). Binary content is appended after valid frontmatter and expected not to disrupt `get_current_round` (`tests/robustness/test-state-file-robustness.sh:367-384`).
- validation_or_tests: This file is itself a robustness test suite. It finishes through `print_test_summary "State File Robustness Test Summary"` and exits with that result (`tests/robustness/test-state-file-robustness.sh:476-481`). I inspected it directly but did not execute it because this worker run is research-only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 unique Item Evidence headings; matches assigned_item_count`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`