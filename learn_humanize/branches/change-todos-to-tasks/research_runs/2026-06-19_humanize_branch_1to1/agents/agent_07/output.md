# agent_07 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the executable specification suite for the Humanize loop algorithms. It covers RLCR state machines, stop/finalize gates, validator path protections, monitor behavior, PR loop polling/gating, transcript task completion checks, setup validation, and robustness/security edge cases. It is core because production loop behavior is largely shell-hook driven and these tests encode the expected state transitions and guard outcomes.
- algorithmic_behavior: Recursive inspection found 56 files under `tests`, with top-level suites, `tests/robustness`, `tests/fixtures`, and `tests/mocks`. `tests/run-all-tests.sh:32-73` defines the ordered suite list, including core suites such as `test-todo-checker.sh`, `test-state-exit-naming.sh`, `test-finalize-phase.sh`, monitor tests, PR loop suites, and all robustness suites. It also strips ANSI output and aggregates pass/fail counters at `tests/run-all-tests.sh:121-137`, so suite success is derived from both exit code and parsed summary counts.
- algorithmic_behavior: The branch’s todo-to-task transition is directly specified by `tests/test-todo-checker.sh`. That file states support for legacy `TodoWrite` plus new `TaskCreate`/`TaskUpdate` at `tests/test-todo-checker.sh:5-8`, then validates input handling, JSONL transcript variants, legacy latest-`TodoWrite` semantics, and new task-state accumulation. The new task group at `tests/test-todo-checker.sh:296-438` asserts default pending state on `TaskCreate`, completion by `TaskUpdate`, incomplete `in_progress`, multiple-task aggregation, orphan `TaskUpdate` handling, legacy/new mixed mode, output containing `Task #`, and direct `tool_use` format.
- algorithmic_behavior: RLCR state lifecycle is specified by several tests. The assigned `tests/test-state-exit-naming.sh` covers active/final file naming semantics. `tests/test-finalize-phase.sh` covers `finalize-state.md` as an active loop state, COMPLETE entering finalize, max-iteration bypassing finalize, incomplete task/todo blocking before Codex review, review failure preserving state, and validator protection for finalize files. `tests/test-cancel-signal-file.sh` stresses cancel-signal authorization for renaming `state.md` or `finalize-state.md` to `cancel-state.md` and blocks injection, wrong destinations, symlink/source bypasses, extra args, variable expansion tricks, and command chaining.
- algorithmic_behavior: Validator behavior is heavily tested. `tests/test-allowlist-validators.sh` verifies limited allowlist access for `round-1-todos.md`, `round-2-todos.md`, early summaries, and blocks wrong round/directory todo files. `tests/test-plan-file-hooks.sh` and `tests/test-plan-file-validation.sh` validate plan path constraints, tracking requirements, branch/model YAML safety, content sufficiency, and hook decisions. `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and `tests/test-templates-comprehensive.sh` validate prompt template loading, placeholder correctness, and references from hooks into templates.
- algorithmic_behavior: Monitor algorithms are tested by `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, and `tests/manual-monitor-test.sh`. These cover clean terminal restoration, graceful stop when `.humanize/rlcr` is deleted, no-match-safe behavior in zsh, SIGINT cleanup in bash/zsh, real monitor loops, PR monitor behavior, and manually verifiable terminal recovery.
- algorithmic_behavior: PR loop behavior is a large child responsibility. `tests/test-pr-loop.sh:5-8` declares the modular PR loop suites: scripts, hooks, and stop hook. `tests/test-pr-loop-system.sh:80-104` provides a function runner and then tests mutual exclusion, reviewer status cases, bot reaction detection, PR review submissions, phase detection, goal tracker parsing/update/idempotency, force-push/unpushed commits, shared monitor helpers, and monitor output. `tests/test-pr-loop-hooks.sh` covers state/read-only file protection, comment sorting/deduplication, configured/active bot parsing, trigger detection, approval markers, PR API fixture-backed fetch/poll, wrong-round validation, and monitor active-bot display. `tests/test-pr-loop-stophook.sh` covers force-push trigger invalidation, missing trigger blocks, bot timeout handling, approve-state creation, active bot mutation, fork PR base repo resolution, and mixed approval goal tracker updates.
- algorithmic_behavior: `tests/robustness` is a stress layer for production helpers and hooks. It covers git state/status parsing, goal tracker parsing, hook input JSON/UTF-8/depth handling, plan file edge cases, cancel security, timeout behavior, base branch detection, setup scripts, concurrent state access, state file/schema transitions, template errors/stress, session handling, path validation, and PR loop API robustness. Examples: `tests/robustness/test-git-operations-robustness.sh:3-10` enumerates git status/state conditions; `tests/robustness/test-goal-tracker-robustness.sh:3-10` enumerates tracker formats and malformed inputs; `tests/robustness/test-hook-system-robustness.sh:3-15` frames hook system tests around JSON validation, injection prevention, state parsing, and race conditions.
- inputs_outputs_state: Inputs are synthetic `.humanize/rlcr` and `.humanize/pr-loop` directories, `state.md` variants, `goal-tracker.md`, plan files, JSON hook payloads, transcript JSONL, mocked `gh`/`codex` CLIs, fixture JSON, temp git repos, terminal stubs, and environment variables such as `CLAUDE_PROJECT_DIR`, `PATH`, `XDG_CACHE_HOME`, and PR mock variables. Outputs are process exit codes, JSON hook decisions, renamed state files, generated/updated loop files, monitor stdout/stderr, test pass/fail counters, and fixture-backed API responses.
- inputs_outputs_state: `tests/test-helpers.sh:30-52` defines shared `pass`, `fail`, and `skip` counters, while `tests/test-helpers.sh:84-104` creates temp dirs and git repos. The test runner consumes each suite’s `Passed:` and `Failed:` lines after ANSI stripping at `tests/run-all-tests.sh:121-128`.
- inputs_outputs_state: `tests/mocks/gh` and fixture setup scripts isolate PR loop state from real GitHub. `tests/mocks/gh:8-21` documents environment inputs such as `MOCK_GH_FIXTURES_DIR`, `MOCK_GH_PR_STATE`, `MOCK_GH_PR_NUMBER`, and commit timestamp. It routes `gh pr view`, `gh api`, `gh repo view`, and auth calls to fixture data at `tests/mocks/gh:27-246`. `tests/setup-fixture-mock-gh.sh:8-12` creates a smaller fixture-backed mock for fetch/poll scripts.
- gates_or_invariants: Core invariants include: only `state.md` and `finalize-state.md` represent active RLCR loops; terminal state must be restored on monitor exit; `state.md` and `finalize-state.md` are system-managed and blocked from direct edits; direct `round-*-todos.md` access is blocked except narrow migration allowlists; all native tasks must be completed before stopping/finalizing; malformed or suspicious hook input should fail closed; plan files must be repo-relative, tracked/untracked according to flags, contentful, and YAML-safe; PR loops must protect state/read-only files, require valid triggers after startup, and handle active bot state deterministically.
- dependencies_and_callers: Test suites call production code in `hooks/lib/loop-common.sh`, `hooks/check-todos-from-transcript.py`, `hooks/loop-*-validator.sh`, `hooks/loop-codex-stop-hook.sh`, PR loop hooks, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, setup/cancel/fetch/poll scripts, and prompt templates. Fixtures and mocks coordinate with PR scripts through `PATH` and `MOCK_GH_FIXTURES_DIR`.
- edge_cases_or_failure_modes: The directory explicitly targets many failure modes: invalid JSON, empty inputs, missing transcript paths, invalid JSONL lines, orphan task updates, incomplete legacy todos plus incomplete new tasks, corrupted or missing state fields, stale/legacy loop paths, zsh glob failures, terminal width extremes, log deletion/truncation, command substitution/newline/operator injection, symlink cancel sources, branch names with YAML-breaking characters, non-git repos, detached/rebase/merge/shallow git states, huge/binary/CRLF plan files, PR force-pushes, missing trigger comments, pagination, approval-only reviews, and concurrent hook invocations.
- validation_or_tests: The directory is itself validation. The master suite is `tests/run-all-tests.sh`, and individual focused validation is available by running each executable suite. I did not execute the full suite in this read-only research pass; I inspected the files recursively and read representative core files and line ranges.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: `scripts/humanize.sh` is the user-facing shell entry point for `humanize monitor rlcr` and `humanize monitor pr`. It wires monitor CLI dispatch to shared monitor helpers, parses goal/git state for display, tails loop logs, detects session/state transitions, and restores terminal state on stop or signal.
- algorithmic_behavior: The file sources `scripts/lib/monitor-common.sh` when present at `scripts/humanize.sh:6-10`, then defines public helper functions for splitting pipe-delimited values, parsing goal tracker summaries, detecting git state, parsing git status, RLCR monitoring, CLI dispatch, and PR loop monitoring. Function boundaries are at `scripts/humanize.sh:18`, `scripts/humanize.sh:32`, `scripts/humanize.sh:116`, `scripts/humanize.sh:166`, `scripts/humanize.sh:219`, `scripts/humanize.sh:884`, and `scripts/humanize.sh:931`.
- algorithmic_behavior: `humanize_parse_goal_tracker` reads a `goal-tracker.md` and returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` at `scripts/humanize.sh:31-112`. It supports AC rows in table or list form at `scripts/humanize.sh:48-54`, counts active tasks by subtracting completed/deferred rows from the active table at `scripts/humanize.sh:56-84`, counts completed/deferred/open sections at `scripts/humanize.sh:86-102`, and extracts a 60-character goal summary at `scripts/humanize.sh:104-109`.
- algorithmic_behavior: `humanize_detect_git_state` returns normalized repo states at `scripts/humanize.sh:116-162`: `not_a_repo`, `permission_error`, `rebase`, `merge`, `shallow`, `detached`, or `normal`. `humanize_parse_git_status` returns `modified|added|deleted|untracked|insertions|deletions` at `scripts/humanize.sh:166-209`, using porcelain status and shortstat to drive the monitor’s git display.
- algorithmic_behavior: `_humanize_monitor_codex` monitors `.humanize/rlcr` at `scripts/humanize.sh:219-881`. It discovers the latest session using shared `monitor_find_latest_session` at `scripts/humanize.sh:237-240`, finds RLCR Codex run/review logs from `$XDG_CACHE_HOME` or `$HOME/.cache` at `scripts/humanize.sh:242-341`, enforces run/review round consistency at `scripts/humanize.sh:330-337`, reads state through `monitor_find_state_file` at `scripts/humanize.sh:344-347`, parses state fields at `scripts/humanize.sh:349-365`, draws a fixed status bar with session, round, model, progress, git stats, goal, plan, and log at `scripts/humanize.sh:373-510`, then loops over log availability, growth, truncation, deletion, and newer sessions at `scripts/humanize.sh:621-871`.
- algorithmic_behavior: `humanize` dispatches only `monitor rlcr` and `monitor pr` at `scripts/humanize.sh:884-924`, returning usage errors for unknown commands. `_humanize_monitor_pr` monitors `.humanize/pr-loop`, parses `--once`, detects monitorable files, maps `approve-state.md` to `approved` and `maxiter-state.md` to `max-iterations`, parses PR state YAML, draws PR loop status/phase/bot information, supports one-shot status output, and tails live files at `scripts/humanize.sh:931-1364`.
- inputs_outputs_state: Inputs include current working directory, `.humanize/rlcr` sessions, `.humanize/pr-loop` sessions, `state.md` or `*-state.md`, `goal-tracker.md`, cache log files under `$XDG_CACHE_HOME/humanize/<sanitized-project>/<timestamp>/`, git repository metadata, terminal dimensions, and optional `--once`. Outputs are terminal UI lines, tailed log content, status-bar updates, usage messages, and shell return codes.
- inputs_outputs_state: RLCR state transitions are read rather than written. Active/final state is inferred from `monitor_find_state_file` returning `path|status`; the monitor reacts to status changes, no-log status, session deletion, log deletion, file truncation/rotation, and newer timestamped sessions. PR state is similarly read and displayed, with PR-specific status mapping in `_pr_find_state_file` at `scripts/humanize.sh:1011-1028`.
- gates_or_invariants: The monitor requires `.humanize/rlcr` or `.humanize/pr-loop` to exist before live monitoring, otherwise it returns with a user-facing error at `scripts/humanize.sh:230-235` and `scripts/humanize.sh:955-960`. RLCR log ordering has a defensive invariant: review rounds must be greater than run rounds for a session, otherwise `_find_latest_codex_log` reports an inconsistent log state and returns failure at `scripts/humanize.sh:330-337`. The terminal cleanup path is guarded by `cleanup_done` and signal trap reset at `scripts/humanize.sh:535-561` and `scripts/humanize.sh:1174-1200`.
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for session/state/file helpers and PR phase helpers, plus shell utilities `find`, `stat`, `sed`, `grep`, `sort`, `wc`, `tail`, `tput`, and git. Called by user shell after sourcing, by tests such as monitor runtime/e2e suites, and indirectly by PR monitor tests that source the script.
- edge_cases_or_failure_modes: Handles missing loop directory, no session directories, no log file yet, session deletion with fallback to newer sessions, loop root deletion with graceful cleanup, log deletion, log truncation/rotation, zsh array indexing through `ksharrays`, bash/zsh signal differences, missing state fields defaulting to `N/A`, non-git directories, terminal redraw width constraints, PR monitor no-session waiting, and stale background `tail` processes.
- validation_or_tests: Covered by `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, PR monitor portions of `tests/test-pr-loop-system.sh` and `tests/test-pr-loop-hooks.sh`, and robustness tests for goal tracker/git parsing. `tests/run-all-tests.sh:49-52` includes monitor runtime/e2e suites, and `tests/run-all-tests.sh:56-72` includes robustness suites.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: This is an executable specification for RLCR state-file naming and active-loop detection after loop exit. It verifies that active loops are represented by `state.md`, terminal loop outcomes are represented by `<reason>-state.md`, and the new `.humanize/rlcr` path is authoritative over legacy `.humanize-loop.local`.
- algorithmic_behavior: The test initializes a temp git repo and loop directory at `tests/test-state-exit-naming.sh:31-49`, sources `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:66-67`, then runs 13 assertions. Tests 1-7 verify `find_active_loop`: `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active, while `state.md` is active and a newer timestamped directory with `state.md` takes precedence (`tests/test-state-exit-naming.sh:38-180`).
- algorithmic_behavior: Tests 8-10 verify `end_loop` at `tests/test-state-exit-naming.sh:186-243`. Invalid reasons must return nonzero and include `Invalid end_loop reason`; valid reasons `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` must create `${reason}-state.md`; missing `state.md` must fail with a not-found warning.
- algorithmic_behavior: Tests 11-13 verify path detection at `tests/test-state-exit-naming.sh:245-291`. `is_in_humanize_loop_dir` must match `.humanize/rlcr/.../state.md`, must not match legacy `.humanize-loop.local/.../state.md`, and `find_active_loop "$TEST_DIR/.humanize/rlcr"` must not search or revive a legacy directory even when legacy `state.md` exists.
- inputs_outputs_state: Inputs are temporary loop directories, synthetic YAML-ish `state.md` and terminal `*-state.md` files, `CLAUDE_PROJECT_DIR`, a sourced production library, and a temp git repo. Outputs are pass/fail counters and process exit status equal to `TESTS_FAILED` at `tests/test-state-exit-naming.sh:293-302`.
- inputs_outputs_state: The state transition under test is `state.md` to `<reason>-state.md`, implemented by `end_loop` in `hooks/lib/loop-common.sh:1112-1145`. Active-loop discovery is implemented by `find_active_loop` in `hooks/lib/loop-common.sh:151-174`, which checks only the newest timestamped session and treats `state.md` or `finalize-state.md` as active indicators.
- gates_or_invariants: Only active state files should revive a loop. Terminal state files are preserved evidence but must not be detected as active. Valid end-loop reasons are strictly enumerated. Legacy paths are not valid loop paths. Newer active session wins over older terminal sessions.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh` for `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. It is included in the global test order at `tests/run-all-tests.sh:39`.
- edge_cases_or_failure_modes: Covers invalid end reasons, missing state file during end transition, multiple terminal state files in one directory, newer session precedence, stale legacy loop directories, and false positives from `.humanize-loop.local`.
- validation_or_tests: This file is itself validation; it contains 13 explicit tests and exits with the failed count. The production implementation being validated is visible at `hooks/lib/loop-common.sh:156-174`, `hooks/lib/loop-common.sh:713-716`, and `hooks/lib/loop-common.sh:1119-1145`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: This prompt block is the user-facing denial contract for attempts to modify RLCR `state.md`. It is algorithmically relevant because validator hooks load it as the block reason when enforcing that loop state is managed by the loop system, not by the agent.
- algorithmic_behavior: The template is deliberately small: title at `prompt-template/block/state-file-modification.md:1`, prohibition at `prompt-template/block/state-file-modification.md:3`, and a short explanation of protected fields at `prompt-template/block/state-file-modification.md:5-10`. It names current round, max iterations, and Codex configuration as state contents and states that modification would corrupt loop state.
- inputs_outputs_state: Input is a validator decision to block a `state.md` write/edit/bash modification. Output is the rendered markdown message. There are no template variables in this file, so rendering is static.
- gates_or_invariants: Invariant: `state.md` is system-managed. Any direct Write/Edit/Bash modification path that targets active RLCR `state.md` should block with this message rather than allowing mutation.
- dependencies_and_callers: Loaded by `state_file_blocked_message` in `hooks/lib/loop-common.sh:463-470`, via `load_and_render_safe "$TEMPLATE_DIR" "block/state-file-modification.md"`. Write validator calls this path for `state.md` at `hooks/loop-write-validator.sh:157-159`; edit validator does the same pattern after checking `finalize-state.md` first at `hooks/loop-edit-validator.sh:112-120`; bash validator uses shared path-detection/blocking helpers for state file modifications.
- edge_cases_or_failure_modes: If the template is missing or invalid, `state_file_blocked_message` has an inline fallback at `hooks/lib/loop-common.sh:465-467`, so blocking behavior should continue with degraded messaging. The main ambiguity is that `finalize-state.md` also ends with `state.md`; production validators explicitly check finalize first to avoid using this generic state message for finalize-state protection.
- validation_or_tests: Template reference/coverage is tested by `tests/test-template-references.sh`, and state modification blocking is exercised by `tests/test-state-exit-naming.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, `tests/test-cancel-signal-file.sh`, and robustness hook-system tests.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-127 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: `scripts/lib/monitor-common.sh` is the shared monitor utility library for RLCR and PR loop monitors. It centralizes color helpers, cross-platform file sizing, latest-session discovery, state-file detection, YAML value extraction, string/timestamp formatting, PR phase detection, and goal tracker parsing.
- algorithmic_behavior: Color helper functions are defined at `scripts/lib/monitor-common.sh:14-23`. `monitor_get_file_size` is cross-platform, trying GNU `stat -c%s`, BSD/macOS `stat -f%z`, then `0` at `scripts/lib/monitor-common.sh:29-35`. `monitor_find_latest_session` scans immediate children of a loop directory, accepts only timestamp names matching `YYYY-MM-DD_HH-MM-SS`, and returns the lexicographically newest timestamp at `scripts/lib/monitor-common.sh:37-63`.
- algorithmic_behavior: Terminal helpers set and restore scroll regions at `scripts/lib/monitor-common.sh:69-92`, while signal handling is documented as a reference rather than implemented because local variable scope must live in each monitor function (`scripts/lib/monitor-common.sh:98-122`). `monitor_get_status_color` maps normalized statuses to ANSI colors at `scripts/lib/monitor-common.sh:128-141`.
- algorithmic_behavior: `monitor_find_state_file` is the shared state detector at `scripts/lib/monitor-common.sh:147-188`. It returns `state.md|active` first if `state.md` exists, otherwise the first `*-state.md` found with status derived from the filename, otherwise `|unknown`. This supports terminal state display without treating all states as active. `monitor_get_yaml_value` extracts frontmatter keys at `scripts/lib/monitor-common.sh:194-209`.
- algorithmic_behavior: PR loop phase detection is implemented in `get_pr_loop_phase` at `scripts/lib/monitor-common.sh:267-342`. It checks final state files first (`approve-state.md`, `cancel-state.md`, `maxiter-state.md`), detects Codex analysis by latest `round-*-pr-check.md` file growth or modification age using a `/tmp/humanize-phase-<session>-<round>.size` cache, then falls back to `state.md` frontmatter: round 0 plus startup case 1 means `waiting_initial_review`, otherwise `waiting_reviewer`. Human-readable phase strings are produced by `get_pr_loop_phase_display` at `scripts/lib/monitor-common.sh:344-381`.
- algorithmic_behavior: `parse_goal_tracker` mirrors the public parser in `scripts/humanize.sh`, returning `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` at `scripts/lib/monitor-common.sh:387-463`. It counts ACs in list/table formats, computes active rows by excluding completed/deferred active rows, counts completed/deferred/open sections, and truncates the goal summary to 60 characters. `humanize_parse_pr_goal_tracker` extracts PR issue totals and the last reviewer from PR goal tracker markdown at `scripts/lib/monitor-common.sh:465-498`.
- inputs_outputs_state: Inputs include loop directory paths, session directories, file paths, state/frontmatter markdown, PR round files, goal tracker markdown, terminal state, and system time. Outputs are pipe-delimited values, color escape codes, display strings, normalized phase/status names, and state file paths.
- inputs_outputs_state: State transitions are read-only classifications: active `state.md`, terminal `*-state.md`, PR final states, PR active/waiting states, and PR Codex-analyzing state inferred from file growth. The file-growth cache under `/tmp` is the only persistent side effect in this helper (`scripts/lib/monitor-common.sh:297-307`), and failure to write it is ignored.
- gates_or_invariants: Timestamp session names must match the strict pattern to be considered. `state.md` has priority over terminal `*-state.md` in a session. PR final states override live analysis detection. Recent or growing `round-*-pr-check.md` implies `codex_analyzing`. Missing paths return neutral values rather than crashing.
- dependencies_and_callers: Sourced by `scripts/humanize.sh` at startup, and directly by PR loop tests. It depends on shell utilities `find`, `basename`, `stat`, `sed`, `grep`, `sort`, `wc`, `tail`, `date`, `cat`, and `tput` where terminal helpers are used. Its functions are used by `_humanize_monitor_codex`, `_humanize_monitor_pr`, and PR loop system tests.
- edge_cases_or_failure_modes: Handles missing loop dir, empty session list, invalid timestamp directory names, missing state file, unknown terminal state, macOS/GNU stat differences, absent YAML fields, stale `/tmp` phase cache, file modified recently but not growing, no PR check files, no `state.md`, malformed/truncated goal trackers, empty/missing goal trackers, long display strings, and active bot display with empty lists.
- validation_or_tests: Covered by monitor runtime/e2e suites, `tests/test-pr-loop-system.sh` shared monitor tests (`test_shared_monitor_find_latest_session`, `test_shared_monitor_find_state_file`, `test_shared_monitor_get_file_size`, PR phase/output tests), PR hook monitor tests, and robustness tests for goal tracker parsing and hook input/monitor edge cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence sections present: HZ-007 `tests`; HZ-037 `scripts/humanize.sh`; HZ-067 `tests/test-state-exit-naming.sh`; HZ-097 `prompt-template/block/state-file-modification.md`; HZ-127 `scripts/lib/monitor-common.sh`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`