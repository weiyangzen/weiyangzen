# agent_01 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`
- notes: read-only branch export inspected directly; no files modified. Test execution was not attempted because this export is read-only and the shell tests create fixture directories, mock binaries, and loop state.

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin. It coordinates RLCR and PR-loop algorithms through command entrypoints, hook validators, stop hooks, GitHub polling scripts, prompt templates, and shell tests. The directory is core because it contains the stop-hook state machines and their validation/test surfaces.
- algorithmic_behavior: The top-level flow is documented in `README.md:58-72` for RLCR and `README.md:163-171` for the PR loop. Claude runs commands from `commands/`, setup scripts create `.humanize/...` loop state, `hooks/hooks.json:14-64` wires PreToolUse validators and Stop hooks, and helper scripts in `scripts/` fetch GitHub comments/reviews/reactions. The PR loop is initialized by `scripts/setup-pr-loop.sh`: it creates `.humanize/pr-loop/<timestamp>` at `scripts/setup-pr-loop.sh:413-419`, fetches initial comments at `scripts/setup-pr-loop.sh:425-431`, determines startup case at `scripts/setup-pr-loop.sh:437-447`, renders round prompt/task text at `scripts/setup-pr-loop.sh:731-851`, and prints the critical completion contract at `scripts/setup-pr-loop.sh:890-942`.
- inputs_outputs_state: Inputs include user slash commands, hook JSON, plan files, GitHub PR metadata, review comments, bot reactions, Codex review output, and transcript paths. Outputs include `.humanize/rlcr/*` and `.humanize/pr-loop/*` state/prompt/comment/check/feedback files, JSON hook block/allow decisions, monitor status, and PR-loop terminal states such as `approve-state.md`, `merged-state.md`, `closed-state.md`, `maxiter-state.md`, or `usage-limit-state.md`. Shared loop helpers identify round files and allowlists in `hooks/lib/loop-common.sh:399-451`.
- gates_or_invariants: Hook registration enforces Write/Edit/Read/Bash validators before tool use and both RLCR and PR stop hooks on stop (`hooks/hooks.json:14-64`). Validators block `round-*-todos.md` except narrow allowlisted historical files and direct users to native Task tools (`hooks/lib/loop-common.sh:443-451`, `hooks/loop-write-validator.sh:55-62`, `hooks/loop-edit-validator.sh:38-45`, `hooks/loop-bash-validator.sh:381-392`). PR-loop state and generated review artifacts are protected (`hooks/loop-write-validator.sh:74-93`, `hooks/loop-edit-validator.sh:57-76`, `hooks/loop-bash-validator.sh:403-442`). Plan backup integrity is enforced through the assigned block template (`hooks/loop-write-validator.sh:175-185`, `hooks/loop-edit-validator.sh:127-138`, `hooks/loop-bash-validator.sh:330-340`).
- dependencies_and_callers: `commands/start-pr-loop.md` and `commands/start-rlcr-loop.md` are user-facing entrypoints; `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh` are PR-loop data-plane dependencies. `hooks/check-todos-from-transcript.py` supports the branch’s todo-to-task transition by accepting both legacy `TodoWrite` and native `TaskCreate`/`TaskUpdate` transcript events (`hooks/check-todos-from-transcript.py:3-13`, `hooks/check-todos-from-transcript.py:61-68`, `hooks/check-todos-from-transcript.py:97-162`).
- edge_cases_or_failure_modes: The repository handles fork PR base-repo resolution in multiple scripts (`hooks/pr-loop-stop-hook.sh:197-236`, `scripts/poll-pr-reviews.sh:122-150`, `scripts/check-pr-reviewer-status.sh:109-140`), paginated GitHub API responses (`hooks/pr-loop-stop-hook.sh:470-478`), approval-only reviews with empty bodies (`scripts/poll-pr-reviews.sh:276-294`), stale triggers after new commits or force pushes (`hooks/pr-loop-stop-hook.sh:363-423`, `hooks/pr-loop-stop-hook.sh:533-564`), wrong-round writes, uncommitted/unpushed changes, and malformed hook input.
- validation_or_tests: `tests/run-all-tests.sh` lists the suite, while PR-loop algorithm coverage lives in `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-system.sh`, and robustness tests under `tests/robustness/`. Todo/task transcript behavior is covered by `tests/test-todo-checker.sh`; template references by `tests/test-template-references.sh`; validator allowlists by `tests/test-allowlist-validators.sh`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook implementation for the PR-loop state machine. It intercepts Claude exit attempts, validates local readiness, polls remote bot reviews, invokes local Codex to classify bot feedback, updates loop state, and either blocks exit with feedback or moves the loop to a terminal state.
- algorithmic_behavior: The hook discovers the active PR loop at `hooks/pr-loop-stop-hook.sh:48-75`, parses frontmatter state and YAML bot lists at `hooks/pr-loop-stop-hook.sh:87-174`, resolves the PR base repo for fork support at `hooks/pr-loop-stop-hook.sh:197-236`, checks PR terminal state at `hooks/pr-loop-stop-hook.sh:238-255`, requires a current-round resolution file at `hooks/pr-loop-stop-hook.sh:257-276`, enforces clean/pushed git state at `hooks/pr-loop-stop-hook.sh:278-360`, detects force-push/new-commit trigger invalidation at `hooks/pr-loop-stop-hook.sh:363-423` and `hooks/pr-loop-stop-hook.sh:533-564`, determines trigger requirements at `hooks/pr-loop-stop-hook.sh:608-643`, polls bot reviews at `hooks/pr-loop-stop-hook.sh:801-1062`, and classifies Codex results at `hooks/pr-loop-stop-hook.sh:1359-1413`.
- inputs_outputs_state: Inputs are hook stdin, `.humanize/pr-loop/<timestamp>/state.md`, `round-N-pr-resolve.md`, Git state, GitHub PR/reaction/comment APIs, `poll-pr-reviews.sh` JSON, and Codex output. Outputs are JSON block decisions, generated `round-(N+1)-pr-comment.md`, `round-(N+1)-codex-prompt.md`, `round-(N+1)-pr-check.md`, `round-(N+1)-pr-feedback.md`, updated `state.md`, and renamed terminal state files (`merged-state.md`, `closed-state.md`, `approve-state.md`, `maxiter-state.md`, `usage-limit-state.md`).
- gates_or_invariants: `pr_number` and numeric `current_round` are required (`hooks/pr-loop-stop-hook.sh:185-194`). Exit is blocked if the resolution summary is missing (`hooks/pr-loop-stop-hook.sh:261-275`), git status fails or non-humanize tracked files are dirty (`hooks/pr-loop-stop-hook.sh:282-309`), commits are unpushed (`hooks/pr-loop-stop-hook.sh:312-359`), force push requires a fresh trigger (`hooks/pr-loop-stop-hook.sh:395-420`), max iterations are exceeded (`hooks/pr-loop-stop-hook.sh:429-434`), a required trigger comment is absent (`hooks/pr-loop-stop-hook.sh:727-745`), Claude lacks an eyes reaction when required (`hooks/pr-loop-stop-hook.sh:767-789`), Codex is unavailable or fails (`hooks/pr-loop-stop-hook.sh:1304-1357`), or Codex says `WAITING_FOR_BOTS` (`hooks/pr-loop-stop-hook.sh:1381-1401`). Only final-line markers `APPROVE`, `ISSUES_REMAINING`, `WAITING_FOR_BOTS`, and `USAGE_LIMIT_HIT` drive transitions (`hooks/pr-loop-stop-hook.sh:1295-1299`).
- dependencies_and_callers: Registered as a Stop hook in `hooks/hooks.json:60-63`. It sources `hooks/lib/loop-common.sh` and `scripts/portable-timeout.sh` (`hooks/pr-loop-stop-hook.sh:51-58`), calls `scripts/check-bot-reactions.sh` for Codex +1 and Claude eyes (`hooks/pr-loop-stop-hook.sh:675`, `hooks/pr-loop-stop-hook.sh:776`, `hooks/pr-loop-stop-hook.sh:998`), calls `scripts/poll-pr-reviews.sh` (`hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:961`), and refreshes startup case via `scripts/check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:1544-1549`). It loads templates through `load_and_render_safe`, including block messages and `pr-loop/codex-goal-tracker-update.md`.
- edge_cases_or_failure_modes: Supports macOS Bash 3.2 by avoiding associative arrays and using dynamic variable helpers (`hooks/pr-loop-stop-hook.sh:82-85`, `hooks/pr-loop-stop-hook.sh:886-892`). Handles paginated trigger comment lookup (`hooks/pr-loop-stop-hook.sh:470-478`), current-user-only trigger filtering (`hooks/pr-loop-stop-hook.sh:501-515`), startup cases 1-5 (`hooks/pr-loop-stop-hook.sh:612-643`), trigger-anchored per-bot timeouts (`hooks/pr-loop-stop-hook.sh:868-900`), cancel signal during polling (`hooks/pr-loop-stop-hook.sh:951-954`), deduplication by comment id (`hooks/pr-loop-stop-hook.sh:1042-1058`), timeout-based bot removal (`hooks/pr-loop-stop-hook.sh:1068-1143`), re-adding previously approved bots if new issues appear (`hooks/pr-loop-stop-hook.sh:1431-1473`), and approval-only Codex +1 reactions before or during polling (`hooks/pr-loop-stop-hook.sh:661-717`, `hooks/pr-loop-stop-hook.sh:984-1038`).
- validation_or_tests: Directly covered by `tests/test-pr-loop-hooks.sh`: state YAML parsing and validator protections (`tests/test-pr-loop-hooks.sh:26-210`), gate logic (`tests/test-pr-loop-hooks.sh:271-466`), trigger filtering and timeout rules (`tests/test-pr-loop-hooks.sh:538-754`), mocked stop-hook executions (`tests/test-pr-loop-hooks.sh:766-1148`), approval-only review handling (`tests/test-pr-loop-hooks.sh:1160-1219`), fixture-backed fetch/poll coverage (`tests/test-pr-loop-hooks.sh:1242-1419`), and wrong-round validation (`tests/test-pr-loop-hooks.sh:1431-1535`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop hook behavior. It codifies the expected state layout, file-protection gates, trigger detection semantics, mocked stop-hook runtime behavior, fixture-backed GitHub comment/review handling, wrong-round validation, and monitor display requirements.
- algorithmic_behavior: `run_hook_tests` groups tests by concern (`tests/test-pr-loop-hooks.sh:15-23`). It verifies YAML `active_bots` storage (`tests/test-pr-loop-hooks.sh:26-63`), PR-loop write protections for `state.md` and `round-N-pr-comment.md` (`tests/test-pr-loop-hooks.sh:68-130`), allowed current-round `round-N-pr-resolve.md` writes (`tests/test-pr-loop-hooks.sh:135-164`), Bash protection without an RLCR loop (`tests/test-pr-loop-hooks.sh:174-210`), sorting and deduplication logic (`tests/test-pr-loop-hooks.sh:222-286`), bot status parsing and re-add behavior (`tests/test-pr-loop-hooks.sh:288-388`), trigger detection and result markers (`tests/test-pr-loop-hooks.sh:390-457`), and E2E stop-hook runs with mocked `gh`/`git` (`tests/test-pr-loop-hooks.sh:766-1148`).
- inputs_outputs_state: Inputs are temporary `.humanize/pr-loop/<timestamp>/state.md` files, synthetic hook JSON for Write/Edit/Bash validators, mocked `gh` and `git` executables, inline JSON comment/review payloads, fixture-backed mock GitHub API responses, and monitor setup fixtures. Outputs are `pass`/`fail` assertions from the test harness and, during E2E sections, generated or inspected loop files such as `round-0-pr-resolve.md` and updated state.
- gates_or_invariants: The file specifies that PR-loop generated state/comment/check/feedback artifacts are system-managed while `pr-resolve` is the user-writable resolution surface (`tests/test-pr-loop-hooks.sh:68-164`). It specifies current-user trigger filtering (`tests/test-pr-loop-hooks.sh:538-586`), newer trigger refresh (`tests/test-pr-loop-hooks.sh:588-604`), required triggers after round 0 (`tests/test-pr-loop-hooks.sh:606-622`), round-0 preference for `last_trigger_at` over `started_at` (`tests/test-pr-loop-hooks.sh:624-665`), timeout anchoring to the trigger timestamp (`tests/test-pr-loop-hooks.sh:667-696`), configured-vs-active bot separation (`tests/test-pr-loop-hooks.sh:698-724`), and consistent `NEXT_ROUND` output naming (`tests/test-pr-loop-hooks.sh:726-744`).
- dependencies_and_callers: The tests call `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `tests/setup-fixture-mock-gh.sh`, `tests/setup-monitor-test-env.sh`, and `scripts/humanize.sh`. They assume common harness functions like `pass`, `fail`, and `init_test_git_repo` are sourced by the broader test runner.
- edge_cases_or_failure_modes: Covers paginated trigger comments where the trigger is on page 2 (`tests/test-pr-loop-hooks.sh:947-1048`), approval-only PR reviews with null/empty body requiring `[Review state: ...]` placeholders (`tests/test-pr-loop-hooks.sh:1160-1195`), bot regex matching for approval-only reviews (`tests/test-pr-loop-hooks.sh:1198-1215`), timestamp filtering that excludes stale comments (`tests/test-pr-loop-hooks.sh:1287-1319`, `tests/test-pr-loop-hooks.sh:1371-1411`), and wrong-round write/edit attempts (`tests/test-pr-loop-hooks.sh:1431-1535`).
- validation_or_tests: This file is itself validation evidence. It is invoked by the broader suite, but it could not be executed in this read-only export because it creates temp directories, state files, and mock binaries.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR plan-backup invariant. It tells the agent that `.humanize/rlcr/.../plan.md` is a read-only backup of the original plan and must be referenced, not modified.
- algorithmic_behavior: The entire template is a short gate explanation: title at `prompt-template/block/plan-backup-protected.md:1`, prohibition at `prompt-template/block/plan-backup-protected.md:3`, integrity rationale at `prompt-template/block/plan-backup-protected.md:5`, and allowed alternative at `prompt-template/block/plan-backup-protected.md:7`.
- inputs_outputs_state: Input is a validator block event from Write/Edit/Bash when a plan backup path is targeted. Output is human-facing stderr text rendered through `load_and_render_safe`; no state mutation is performed by the template itself.
- gates_or_invariants: Enforces “plan backup is read-only” as a safety invariant. It is invoked for Write at `hooks/loop-write-validator.sh:175-185`, Edit at `hooks/loop-edit-validator.sh:127-138`, and Bash command modifications at `hooks/loop-bash-validator.sh:330-340`.
- dependencies_and_callers: Depends on the template loader and validators. It is part of the broader completion-surface protection system that also blocks state files, goal trackers after round 0, prompt files, and generated todo files.
- edge_cases_or_failure_modes: If the template is missing or fails to render, each caller has a fallback string (`hooks/loop-write-validator.sh:181-182`, `hooks/loop-edit-validator.sh:134-135`, `hooks/loop-bash-validator.sh:337-338`). The template has no variables, reducing rendering failure risk.
- validation_or_tests: Template reference coverage is implied by `tests/test-template-references.sh`; plan-file hook behavior is covered in `tests/test-plan-file-hooks.sh` and robustness coverage in `tests/robustness/test-plan-file-robustness.sh`. Direct callers prove it is live algorithmic text rather than unused documentation.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-121 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop critical completion contract for the startup path where review comments already exist. It defines the mandatory user actions before the stop hook can successfully poll and advance the PR loop.
- algorithmic_behavior: The template requires three ordered actions: commit and push changes (`prompt-template/pr-loop/critical-requirements-has-comments.md:8-10`), comment on the PR with `{{BOT_MENTION_STRING}}` to trigger re-review (`prompt-template/pr-loop/critical-requirements-has-comments.md:12-13`), and write the resolution summary to `{{RESOLVE_PATH}}` with issue/file/test details (`prompt-template/pr-loop/critical-requirements-has-comments.md:15-21`). It then states that the Stop Hook will poll for bot reviews (`prompt-template/pr-loop/critical-requirements-has-comments.md:23`).
- inputs_outputs_state: Inputs are template variables supplied by `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH` (`scripts/setup-pr-loop.sh:731-738`). Output is appended to the startup command response when comments exist, through `load_and_render_safe` at `scripts/setup-pr-loop.sh:915-942`.
- gates_or_invariants: Mirrors hard gates in `hooks/pr-loop-stop-hook.sh`: resolution summary must exist (`hooks/pr-loop-stop-hook.sh:261-275`), git must be clean and pushed (`hooks/pr-loop-stop-hook.sh:282-360`), and required trigger comments must be present before polling (`hooks/pr-loop-stop-hook.sh:727-745`). The template is therefore the user-facing contract for satisfying those gates before attempting to stop.
- dependencies_and_callers: Called only on the “has comments” setup path in `scripts/setup-pr-loop.sh:915-942`; paired with `prompt-template/pr-loop/critical-requirements-no-comments.md` for the no-comments startup path. It coordinates with `round-0-task-has-comments.md` and the stop hook’s polling/validation logic.
- edge_cases_or_failure_modes: If comments are misdetected, setup may render the wrong critical contract; comment existence is inferred by absence of the `*No comments found.*` sentinel in `scripts/setup-pr-loop.sh:723-729`. If rendering fails, setup uses a fallback with the same commit/push/comment/resolve requirements (`scripts/setup-pr-loop.sh:916-940`). Variable substitution errors would degrade the exact `gh pr comment` command or resolution path, but the fallback shares the same placeholders.
- validation_or_tests: Template reference coverage is expected from `tests/test-template-references.sh`; behavior is indirectly validated by PR-loop setup and stop-hook tests that require resolve files, trigger comments, and clean pushed state.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`