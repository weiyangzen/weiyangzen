# agent_09 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell-library core for RLCR/PR loop hook enforcement. The directory contains two files, `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`; together they provide state parsing, active-loop discovery, file/path classifiers, cancellation authorization, protected-file gate messages, shell-command write detection, template loading/rendering, and PR-loop helper functions consumed by the hook validators and loop setup/stop scripts.
- algorithmic_behavior: `loop-common.sh` defines canonical state field names and loop exit reasons at lines 15-44, validates hook JSON with null-byte, UTF-8, jq syntax, `tool_name`, and `tool_input` extraction at lines 59-96, and checks nesting depth with jq at lines 119-133. It sources `template-loader.sh`, derives `TEMPLATE_DIR`, and warns/falls back if the template tree fails validation at lines 139-149. Active-loop discovery uses the newest child directory and requires `state.md` or `finalize-state.md`, deliberately ignoring older loop directories at lines 156-174. State parsing has a tolerant mode at lines 209-244 and strict mode at lines 255-330; strict mode requires YAML frontmatter separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`, validates numeric fields, and validates `review_started` as boolean. Review analysis scans a Codex review log for the first `[P?]` marker in the first 10 characters of a line, extracts from that line onward, writes an audit result file, and returns distinct statuses for issues/no issues/missing log at lines 349-392. File gates classify round files and protected paths at lines 395-525, and `todos_blocked_message` routes `round-*-todos.md` access to the native Task tools through the assigned template at lines 443-452. `is_cancel_authorized` is the cancel-state transition gate: it requires `.cancel-requested`, rejects shell injection and chaining, normalizes `$loop_dir` and `/./`, parses exactly an `mv` with two arguments, requires source `state.md` or `finalize-state.md`, requires destination `cancel-state.md`, and rejects symlink sources at lines 533-710. PR-loop support maps bot names, finds active PR loops, validates PR resolve round numbers, and blocks PR-loop generated/read-only files at lines 722-906. Git and shell bypass guards block adding `.humanize` state to git at lines 908-1066 and detect shell writes to protected files through redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns at lines 1068-1096. `end_loop` performs the loop terminal state transition by renaming `state.md` to `<reason>-state.md` after validating the reason at lines 1112-1145. `update_pr_goal_tracker` idempotently adds per-round/per-reviewer rows, totals, and issue-log entries while avoiding double counting at lines 1168-1315. `template-loader.sh` computes the template root relative to `hooks/lib` at lines 24-31, loads templates at lines 33-48, renders `{{VAR}}` placeholders in a single pass via `awk` and `TMPL_VAR_` environment variables at lines 50-132, supports append/load-and-render helpers at lines 134-161, provides fallback-safe rendering at lines 167-203, and validates required template subdirectories at lines 205-222.
- inputs_outputs_state: Inputs include hook JSON from stdin, `tool_name`, `tool_input.file_path`, `tool_input.command`, `.humanize/rlcr/*/state.md`, `.humanize/rlcr/*/finalize-state.md`, `.humanize/pr-loop/*/state.md`, Codex review logs, prompt templates under `prompt-template`, shell command strings, git ignore state, and caller-provided template variables. Outputs include exported shell variables such as `VALIDATED_TOOL_NAME`, `STATE_CURRENT_ROUND`, `STATE_REVIEW_STARTED`, rendered block messages on stdout/stderr, return codes used by validators, extracted review result files, renamed terminal state files, and updated PR goal tracker files. Most helpers are pure classifiers, but `detect_review_issues`, `end_loop`, and `update_pr_goal_tracker` write files by design.
- gates_or_invariants: Hook inputs must be valid JSON with `tool_name` and bounded nesting before validators proceed. Strict state parsing fails closed on malformed or incomplete state. Only the newest active loop directory is honored, preventing stale/zombie loop revival. `round-*-todos.md` files are blocked unless explicitly allowlisted for the active loop. Prompt/state/finalize-state/goal-tracker/plan backup files are protected by path-specific gates and rendered block messages. Cancel is only authorized by a signal file plus an exact, non-symlink `mv state.md|finalize-state.md cancel-state.md` shape. Template rendering is single-pass so injected `{{OTHER_VAR}}` text inside a variable value is not recursively expanded. `.humanize` git-add protection blocks direct paths, forced broad adds, and broad adds when `.humanize` is not ignored.
- dependencies_and_callers: `hooks/loop-read-validator.sh` sources `loop-common.sh` and uses JSON validation, nesting checks, round-file classification, active-loop discovery, strict state parsing, and `todos_blocked_message` at lines 17-20 and 27-63, then state-based round validation at lines 80-130. `hooks/loop-write-validator.sh` uses the same library for todos/prompt blocks, PR-loop file protection, active loop/state parsing, and state/finalize-state blocks at lines 17-19, 55-67, 74-93, 123-159. `hooks/loop-edit-validator.sh` uses it for edit-time todos/prompt/PR/state/goal-tracker gates at lines 16-18, 38-50, 57-75, 91-149. `hooks/loop-bash-validator.sh` is the heaviest caller: it validates Bash hook input, finds active loops, blocks git push based on parsed state, blocks `.humanize` git adds, authorizes or rejects cancel-state transitions, strips redirections while scanning `mv`/`cp` sources, and blocks Bash writes to protected RLCR/PR loop files at lines 16-18, 24-48, 54-67, 81-117, 139-156, 169-304, 336-393, 403-443. Setup and stop scripts also call these helpers, including `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. Tests covering this directory include template, state, cancel, hook input, Bash pattern, session, PR-loop, finalize, and humanize-escape tests.
- edge_cases_or_failure_modes: `find_active_loop` uses lexicographic sorting and only checks the newest child directory, so a malformed newest loop can mask older valid loops by design. `is_state_file_path` also matches `finalize-state.md`; callers must check `finalize-state.md` first, which the validators explicitly note. `is_cancel_authorized` lowercases the command and expected loop path, so case-sensitive path behavior depends on callers passing already-lowercased commands consistently. It intentionally rejects mixed quote styles and multiple trailing spaces, which is stricter than shell semantics but narrows the authorized cancel grammar. `git_adds_humanize` has cwd-sensitive behavior for broad non-force adds because it checks whether `.humanize` exists and whether git ignores it. Template directory validation only warns in `loop-common.sh`; missing templates are expected to degrade through `load_and_render_safe` fallbacks, and a separate test enforces reference completeness.
- validation_or_tests: `tests/test-template-loader.sh`, `tests/test-error-scenarios.sh`, `tests/robustness/test-template-error-robustness.sh`, and `tests/robustness/test-template-stress-robustness.sh` cover template loading/rendering behavior. `tests/test-template-references.sh` validates referenced templates and safe fallback usage. `tests/robustness/test-cancel-security-robustness.sh` directly targets `is_cancel_authorized`. `tests/test-cancel-signal-file.sh` has broader cancel-signal integration coverage. `tests/robustness/test-state-file-robustness.sh`, `tests/robustness/test-state-transition-robustness.sh`, and `tests/robustness/test-session-robustness.sh` cover state parsing and active-loop discovery. I did not execute tests because this branch export is read-only and the sandbox showed write/cache failures for commands that attempted temp/cache creation; evidence is from direct source inspection.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-039 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Cross-platform timeout abstraction used by setup scripts, hooks, PR review/status scripts, and robustness tests to bound git, gh, Codex, and shell subprocesses on both macOS and Linux.
- algorithmic_behavior: `detect_timeout_impl` chooses an implementation in priority order: Homebrew `gtimeout`, GNU `timeout` only if `timeout --version` succeeds, then `python3`, then `python`, else `none` at lines 9-27. The detected value is cached in global `TIMEOUT_IMPL` at line 29. `run_with_timeout` takes a seconds value plus command/args, stores the command as an array to preserve argument boundaries, and dispatches by `TIMEOUT_IMPL` at lines 31-71. For `gtimeout` and GNU `timeout`, it delegates directly and returns the child status at lines 38-46. For Python fallback, it invokes `subprocess.run(sys.argv[1:], timeout=<seconds>)`, preserves normal return codes, returns `124` on `TimeoutExpired` to match GNU timeout, and prints unexpected exceptions to stderr with exit `1` at lines 47-63. If no timeout mechanism exists, it warns and runs the command unbounded at lines 64-69. The script exports `TIMEOUT_IMPL` for sourcing scripts at lines 73-76.
- inputs_outputs_state: Inputs are available commands on `PATH`, the timeout seconds argument, and the command argv. Outputs are the wrapped command’s stdout/stderr, the command’s exit code when it completes, `124` for timeout in supported implementations, an error message and exit `1` on Python wrapper exceptions, or a warning if no timeout implementation exists. State is limited to the exported `TIMEOUT_IMPL` shell variable; there are no files or persistent state transitions.
- gates_or_invariants: GNU `timeout` is only accepted if `timeout --version` works, avoiding false positives for incompatible implementations. The Python fallback avoids shell evaluation by passing `sys.argv[1:]` directly to `subprocess.run`, preserving literal special characters in arguments. Exit code `124` is the invariant timeout signal for both GNU-style and Python fallback paths. The `none` implementation is an explicit degradation path, not silent success: it warns to stderr before running without enforcement.
- dependencies_and_callers: Sourced by `scripts/setup-rlcr-loop.sh` line 29 and used throughout setup git/base-branch/plan checks; sourced by `scripts/setup-pr-loop.sh` line 35; sourced by `hooks/loop-codex-stop-hook.sh` line 52 for git status, Codex review, and Codex exec timeouts; sourced by `hooks/pr-loop-stop-hook.sh` line 58 for gh/git/Codex PR-loop calls; sourced by `hooks/loop-plan-file-validator.sh` line 21 for bounded git validation. PR helper scripts `scripts/check-bot-reactions.sh` and `scripts/check-pr-reviewer-status.sh` also source it. `tests/robustness/test-timeout-robustness.sh` is the direct executable spec.
- edge_cases_or_failure_modes: If no timeout binary or Python is available, commands run without a timeout, which can hang but is surfaced by the warning. Timeout `0` behavior is implementation-dependent; the robustness test accepts either immediate success or timeout. The Python fallback uses direct subprocess execution, so callers needing pipelines must pass `sh -c`, which the robustness test covers. Nonexistent or empty command inputs return nonzero or implementation-specific errors. Signal behavior can vary for commands that trap or ignore termination; the test accepts either `124` or graceful `0` for a trapped signal case.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` verifies detection, valid implementation names, successful quick commands, timeout exit `124`, argument preservation, exit-code preservation, `sh -c` pipelines, short timeouts, zero timeout behavior, large output, rapid cycles, nonexistent command handling, empty command handling, special-character args, signal handling, fallback-chain validity, subshell commands, and `TIMEOUT_IMPL` export at lines 31-278. I inspected this test but did not execute it because the branch export is read-only and temp/cache writes are restricted.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-069 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that prevents hook validators from referencing missing prompt templates or using unsafe template rendering paths. This directly protects the hook-blocking algorithm from emitting empty or missing guidance when an action is blocked.
- algorithmic_behavior: The script derives `PROJECT_ROOT` and `TEMPLATE_DIR` at lines 14-16, initializes colored counters at lines 18-27, and defines `pass`, `fail`, `warn`, and `section` helpers at lines 29-49. Section 1 hard-codes the shell scripts to inspect, including loop stop/read/write/edit/bash validators and `hooks/lib/loop-common.sh`, at lines 54-64. It scans each existing script line-by-line, skips full-line comments, matches calls to `load_template`, `load_and_render`, or `load_and_render_safe` whose first argument is exactly `"$TEMPLATE_DIR"`, extracts the quoted template path with `sed`, increments `FOUND_REFERENCES`, and records pass/fail based on whether the resolved file exists at lines 74-111. Section 2 enumerates all `*.md` templates with `find`, checks whether each path is directly referenced under `hooks/`, and treats unreferenced templates as warnings rather than failures at lines 119-142. Section 3 checks a curated set of common block templates, including `block/todos-file-access.md`, `block/prompt-file-write.md`, state, summary, and goal-tracker templates at lines 147-167. Section 4 enforces that critical validators use `load_and_render_safe` instead of raw `load_and_render` by counting unsafe calls in read/write/edit validators at lines 172-201. The summary reports pass/fail/warning counts, lists missing templates, exits `0` only when `TESTS_FAILED` is zero, and exits `1` otherwise at lines 206-230.
- inputs_outputs_state: Inputs are the repository file tree, the hard-coded script list, `prompt-template/**/*.md`, and literal template references in hook scripts. Outputs are colored PASS/FAIL/WARN lines, totals, a missing-template list, and process exit code. State is in shell arrays/counters only; it does not modify repository files.
- gates_or_invariants: Every statically detected `"$TEMPLATE_DIR"` template reference in the checked hook scripts must resolve to a real file. The curated common template set must exist regardless of whether static scanning finds references. Critical path validators must render block messages through `load_and_render_safe`, ensuring fallback text exists if templates are absent or rendering fails. Unreferenced templates are warning-only to allow dynamic or future templates without breaking the suite.
- dependencies_and_callers: Depends on the hook scripts and `hooks/lib/loop-common.sh` for static references, on `prompt-template` for actual template files, and on standard shell tools `find`, `grep`, and `sed`. It validates behavior implemented by `hooks/lib/template-loader.sh` and consumed by `loop-common.sh` message helpers, especially the `todos_blocked_message` reference to `block/todos-file-access.md`.
- edge_cases_or_failure_modes: The extractor only recognizes direct quoted calls with `"$TEMPLATE_DIR"` followed by a quoted literal template path, so dynamic template names or alternate variable names are not validated. Full-line comments are skipped, but unusual multiline calls or references inside strings may not be parsed correctly. The script checks only its hard-coded `SCRIPTS_TO_CHECK`, not every shell file in the repo. Unreferenced template files are warnings, so dead templates do not fail the gate. Missing script files are warnings, which avoids false hard failures but could hide renamed-validator coverage gaps.
- validation_or_tests: This file is itself the validation gate. It also indirectly validates the assigned `prompt-template/block/todos-file-access.md` by listing it in `COMMON_TEMPLATES` at line 153. I inspected the executable spec directly and did not run it because the read-only sandbox is not suitable for commands that may require temp/cache writes.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-099 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: User-facing block template that implements the branch’s behavioral transition from file-based `round-*-todos.md` access to native task-tool usage.
- algorithmic_behavior: The template is a short Markdown block titled `Todos File Access Blocked` at line 1. It explicitly says not to create or access `round-*-todos.md` files at line 3. It directs the agent to use `TaskCreate`, `TaskUpdate`, and `TaskList` instead at line 5, then gives the rationale that native task tools provide UI-visible state tracking and Claude Code task-system integration at lines 7-8. It contains no placeholders, conditionals, or dynamic sections.
- inputs_outputs_state: Inputs are only the template-loading call from `todos_blocked_message`; there are no template variables. Output is the rendered Markdown message sent by validators to stderr when blocked. It does not read or write state.
- gates_or_invariants: The invariant is policy-level: `round-*-todos.md` should not be created, read, edited, written, or modified through Bash except for the narrow allowlist handled by `loop-common.sh`. Because there are no placeholders, rendering is deterministic and cannot be affected by caller-supplied variable values.
- dependencies_and_callers: Loaded by `todos_blocked_message` in `hooks/lib/loop-common.sh` line 451. That message helper is called by `hooks/loop-read-validator.sh` lines 55-61, `hooks/loop-write-validator.sh` lines 55-61, `hooks/loop-edit-validator.sh` lines 38-44, and `hooks/loop-bash-validator.sh` lines 385-391. Its existence is validated by `tests/test-template-references.sh` through `COMMON_TEMPLATES` line 153. Related repository text confirms the migration context: `hooks/check-todos-from-transcript.py` supports both legacy `TodoWrite` and the new `TaskCreate`/`TaskUpdate` task system, and prompt templates for finalize/incomplete work tell agents to complete tasks via `TaskUpdate`.
- edge_cases_or_failure_modes: If this file is missing or empty, `todos_blocked_message` falls back to inline text in `loop-common.sh` lines 447-449, so blocking still works but the richer template text may be lost. The template does not include the attempted action (`Read`, `Write`, `Edit`, or `Bash`) even though the caller passes an action string; this keeps output stable but less specific. The template is a policy prompt only; actual enforcement depends on validators recognizing todos file paths and Bash modification patterns.
- validation_or_tests: Covered by `tests/test-template-references.sh`, which checks common template existence and safe render usage. Runtime enforcement is covered indirectly through read/write/edit/bash validator tests and robustness suites that exercise todos-file blocking behavior. I inspected the file directly.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-129 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Security robustness specification for the `is_cancel_authorized` state-transition gate in `hooks/lib/loop-common.sh`. It proves that cancellation can only move loop state to `cancel-state.md` through a tightly constrained, signaled `mv` operation.
- algorithmic_behavior: The script enables strict shell mode, locates `PROJECT_ROOT`, sources `hooks/lib/loop-common.sh` and shared test helpers, creates a temporary loop directory with `state.md`, and then exercises direct calls to `is_cancel_authorized` at lines 13-30. Positive tests verify authorized cancellation when `.cancel-requested` exists and the command moves `state.md` to `cancel-state.md`, when the source is `finalize-state.md`, and when paths use single quotes or no quotes at lines 39-89. Negative tests reject missing signal files, command substitution, backticks, semicolon chaining, `&&`, pipes, wrong destination, wrong source, extra arguments, newline injection, residual variable expansion, non-`mv` commands, `||`, mixed quote styles, IFS manipulation, multiple trailing spaces, non-standard source names, and symlink source files at lines 99-367. One positive edge case accepts `/./` path normalization at lines 265-276. Regression tests create loop paths containing the substring `finalize` and assert that symlink checks still classify source files by exact expected path rather than substring matching at lines 369-405. The script prints a summary and exits with the helper summary status at lines 407-412.
- inputs_outputs_state: Inputs are temporary filesystem fixtures, generated command strings, lowercased command strings, the `.cancel-requested` signal file, and real/symlink `state.md` or `finalize-state.md` files. Outputs are PASS/FAIL lines and process exit code. State changes are confined to a temporary test directory via `mktemp` and the cleanup trap from `tests/test-helpers.sh`.
- gates_or_invariants: The test suite codifies these invariants: no cancel without `.cancel-requested`; only `mv` is authorized; only `state.md` or `finalize-state.md` may be the source; only `cancel-state.md` in the same loop directory may be the destination; exactly two arguments are allowed; shell injection/chaining syntax is rejected; remaining `$` variables after `$loop_dir` normalization are rejected; mixed quote styles and repeated trailing spaces are rejected; symlink sources are rejected; and `/./` normalization is accepted.
- dependencies_and_callers: Depends directly on `is_cancel_authorized` from `hooks/lib/loop-common.sh` and test helper functions `setup_test_dir`, `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh`. The production caller of the tested gate is `hooks/loop-bash-validator.sh`: it calls `is_cancel_authorized` before allowing Bash operations that would otherwise modify `state.md` or `finalize-state.md`, including direct destination matches at lines 139-155, source `mv`/`cp` scans at lines 286-303, and shell-wrapper scans at lines 309-327.
- edge_cases_or_failure_modes: The test lowercases commands before passing them to `is_cancel_authorized`, matching production, but that means it does not independently validate mixed-case path behavior on case-sensitive filesystems. It deliberately treats trailing multiple spaces as a rejection even though shells normally ignore them. It does not cover loop paths containing spaces, escaped spaces, tabs between arguments, or every possible shell quoting mode; those remain residual parser-risk areas. The symlink regression specifically covers loop directory names containing `finalize`, preventing the known substring-misclassification bypass.
- validation_or_tests: This file is an executable test. It complements broader cancel coverage in `tests/test-cancel-signal-file.sh`, while focusing on hostile command shapes and symlink/path bypasses. I inspected the test directly but did not execute it because the current branch export and sandbox are read-only; the test creates temporary files and symlinks.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above; each assigned item id appears once as a section heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`