# agent_26 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Code Stop hook for the RLCR loop. It intercepts exit attempts, checks loop state and repository readiness, runs Codex review, blocks exit with next-round instructions when work is incomplete, and advances the loop through implementation, review, finalize, stop, max-iteration, or completion states. Runtime wiring is through `hooks/hooks.json:52` and `hooks/hooks.json:57`.
- algorithmic_behavior: The hook reads stdin hook JSON into `HOOK_INPUT` at `hooks/loop-codex-stop-hook.sh:29`, resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` at `hooks/loop-codex-stop-hook.sh:43`, locates the active loop with `find_active_loop` at `hooks/loop-codex-stop-hook.sh:59`, and exits cleanly when no loop/state exists at `hooks/loop-codex-stop-hook.sh:61`. It parses state frontmatter at `hooks/loop-codex-stop-hook.sh:90`, maps parsed `STATE_*` values at `hooks/loop-codex-stop-hook.sh:103`, validates model/effort strings at `hooks/loop-codex-stop-hook.sh:120`, requires raw `current_round` and `max_iterations` fields at `hooks/loop-codex-stop-hook.sh:133`, validates newer schema fields at `hooks/loop-codex-stop-hook.sh:163` and `hooks/loop-codex-stop-hook.sh:182`, enforces branch consistency at `hooks/loop-codex-stop-hook.sh:227`, protects the original plan before review phase at `hooks/loop-codex-stop-hook.sh:263`, and checks transcript task completion before expensive review at `hooks/loop-codex-stop-hook.sh:340`.
- inputs_outputs_state: Inputs are hook stdin JSON, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `plan.md` backup, the configured plan file, round summary files, goal tracker, git status, and Codex CLI availability. Outputs are Claude hook JSON objects with `"decision": "block"` plus reason/system messages, review prompts/results under `.humanize/rlcr/<timestamp>/`, debug command/log files under `$XDG_CACHE_HOME` or `$HOME/.cache` at `hooks/loop-codex-stop-hook.sh:878`, next-round prompts, and state-file renames. Main state transitions: no active loop allows exit; `finalize-state.md` switches to Finalize Phase at `hooks/loop-codex-stop-hook.sh:76`; successful finalize renames to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:743`; max iterations call `end_loop(... maxiter)` at `hooks/loop-codex-stop-hook.sh:731`; Codex `COMPLETE` from implementation marks `review_started: true`, creates `.review-phase-started`, then runs code review at `hooks/loop-codex-stop-hook.sh:1395`; no code-review issues enter finalize via `enter_finalize_phase` at `hooks/loop-codex-stop-hook.sh:1025`; code-review issues update `current_round` and emit a review-fix prompt at `hooks/loop-codex-stop-hook.sh:1119`; `STOP` calls `end_loop(... stop)` at `hooks/loop-codex-stop-hook.sh:1472`; ordinary issues increment to the next implementation round at `hooks/loop-codex-stop-hook.sh:1506`.
- gates_or_invariants: Fail-closed gates include YAML safety for Codex config, required state fields, schema version fields, immutable start branch, plan backup/content integrity, all native tasks completed, git status not timing out, no large changed code/docs files over 2000 lines, clean working tree, pushed commits when `push_every_round=true`, summary file presence, initialized round-0 goal tracker placeholders, Codex CLI existence, non-empty Codex review result, review-phase marker integrity, and no `[P0-9]` issues from `codex review`. The full alignment cadence is computed as `current_round % full_review_round == full_review_round - 1` at `hooks/loop-codex-stop-hook.sh:781`, with invalid intervals defaulting to 5 at `hooks/loop-codex-stop-hook.sh:777`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file`, `detect_review_issues`, `to_lower`, and `end_loop`; `hooks/lib/template-loader.sh` for `load_template` and `load_and_render_safe`; `scripts/portable-timeout.sh` for bounded git/Codex execution; `hooks/check-todos-from-transcript.py` for legacy TodoWrite plus TaskCreate/TaskUpdate transcript analysis; and templates under `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude`. External tools: `git`, `jq`, `python3`, `codex`, `sed`, `awk`, `wc`, `diff`, `tail`, `cp`, `mv`, and `mkdir`.
- edge_cases_or_failure_modes: Missing active loop exits without blocking. Missing state in newest loop exits without blocking. Truncated state is not silently defaulted because raw field presence is checked before defaults. Missing `full_review_round` only warns and defaults. Review phase intentionally skips plan integrity because skip-impl mode may not have a real plan. Git failures/timeouts block rather than bypass checks. Codex review failures and empty/missing outputs block with retry instructions. `codex review` stdout/stderr behavior is normalized through cache logs and `detect_review_issues`. Manual `review_started=true` edits are caught by requiring `.review-phase-started`. Non-alignment `STOP` is still honored but logged as unusual. One notable edge in the code path: if `COMPLETE` occurs with empty `BASE_BRANCH`, `REVIEW_SKIPPED` is set at `hooks/loop-codex-stop-hook.sh:1415`, but this branch does not visibly call `enter_finalize_phase` before falling through, so behavior depends on later conditions.
- validation_or_tests: Related tests reference this stop hook directly in `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, and `tests/robustness/test-hook-system-robustness.sh`. The hook’s shared issue detector is defined in `hooks/lib/loop-common.sh:349`; transcript task checker behavior is specified by `tests/test-todo-checker.sh`; template reference coverage includes `tests/test-template-references.sh`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable shell specification for two safety seams in the RLCR tooling: preventing `.humanize` from being accidentally staged by `git add`, and replacing glob-sensitive shell iteration with `find`-based iteration for empty, dotfile-only, and state/session directory discovery.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` at `tests/test-humanize-escape.sh:18`, defines pass/fail counters at `tests/test-humanize-escape.sh:25`, and wraps `git_adds_humanize` through `assert_blocks` and `assert_allows` at `tests/test-humanize-escape.sh:45` and `tests/test-humanize-escape.sh:59`. It lowercases commands via `to_lower`, matching the validator contract documented in `hooks/lib/loop-common.sh:912`. It then asserts that direct `.humanize`, `./.humanize`, nested path, quoted path, forced add, broad force, `-A`, chained command, and `git -C` variants block, while normal files, `.gitkeep`, non-add git commands, patch mode, and similarly named files like `.humanizeconfig` are allowed.
- inputs_outputs_state: Inputs are no CLI args; the script infers `PROJECT_ROOT`, reads shared functions, creates temporary directories under `/tmp`, changes into a temp repo-like directory for the `.humanize` existence-sensitive `-A` cases, and returns to the original directory at `tests/test-humanize-escape.sh:142`. Outputs are colored PASS/FAIL lines, cumulative counters, and exit code 0 only when `TESTS_FAILED` is 0 at `tests/test-humanize-escape.sh:344`.
- gates_or_invariants: `.humanize` path variants must block at `tests/test-humanize-escape.sh:83`; quoted variants must block at `tests/test-humanize-escape.sh:96`; force/broad-scope cases must block at `tests/test-humanize-escape.sh:109`; `git add -A`/`--all` must block when a `.humanize` dir exists at `tests/test-humanize-escape.sh:128`; chained commands and git option prefixes must still be detected at `tests/test-humanize-escape.sh:153` and `tests/test-humanize-escape.sh:164`; safe commands must not over-block at `tests/test-humanize-escape.sh:175`; `.humanize*` lookalike filenames must remain allowed at `tests/test-humanize-escape.sh:189`.
- dependencies_and_callers: Depends on `to_lower` and `git_adds_humanize` from `hooks/lib/loop-common.sh`. The implementation under test splits chained shell operators and normalizes quoted add args in `hooks/lib/loop-common.sh:930`, strips quotes at `hooks/lib/loop-common.sh:962`, blocks exact `.humanize` path boundaries at `hooks/lib/loop-common.sh:967`, detects force/all flags at `hooks/lib/loop-common.sh:975`, and requires actual `.humanize` existence for non-force broad/all blocking at `hooks/lib/loop-common.sh:1005`. Included in the aggregate test runner via `tests/run-all-tests.sh:48`.
- edge_cases_or_failure_modes: The test explicitly covers lowercased `-A` as `-a`, combined flags such as `-fA` and `-Af`, quoted paths, path prefixes, shell command chaining, temp `.humanize` existence sensitivity, avoiding false positives for `.humanizeconfig`/`.humanize-backup`, empty directory iteration, dotfile-only directories, no `*-state.md` files, positive `completed-state.md` detection, and timestamped session directory sorting at `tests/test-humanize-escape.sh:303`.
- validation_or_tests: This file is itself the validation artifact. It uses real shell control flow and `find` process substitution, creates/removes temp files, and exits nonzero on any failed assertion. Zsh/glob-safety scenarios are simulated through `find` loops at `tests/test-humanize-escape.sh:204`, `tests/test-humanize-escape.sh:225`, `tests/test-humanize-escape.sh:248`, `tests/test-humanize-escape.sh:271`, and `tests/test-humanize-escape.sh:303`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Block-message template for the goal-tracker authority gate. It defines the post-round-0 contract that direct edits to `goal-tracker.md` are forbidden and that changes must be requested for Codex review through the summary file.
- algorithmic_behavior: The template renders the current round into the heading at `prompt-template/block/goal-tracker-modification.md:1`, states that after Round 0 only Codex can modify the Goal Tracker at `prompt-template/block/goal-tracker-modification.md:3`, forbids Write/Edit/Bash modification at `prompt-template/block/goal-tracker-modification.md:5`, and prescribes a structured `Goal Tracker Update Request` section in `{{SUMMARY_FILE}}` at `prompt-template/block/goal-tracker-modification.md:9`. The request schema separates requested changes from justification at `prompt-template/block/goal-tracker-modification.md:16` and `prompt-template/block/goal-tracker-modification.md:21`.
- inputs_outputs_state: Inputs are template variables `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`, supplied by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1100`. Output is rendered Markdown used as the hook/validator block reason. It does not mutate state itself; it instructs Claude to place proposed state changes in a summary so Codex can decide whether to update the goal tracker.
- gates_or_invariants: Direct `goal-tracker.md` edits after Round 0 are disallowed regardless of tool surface. Allowed path is indirect: include requested changes plus justification in the current summary. Codex remains the only actor authorized by the template to apply justified tracker updates.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/lib/loop-common.sh:1107`. The template loader performs single-pass `{{VAR}}` substitution as documented in `hooks/lib/template-loader.sh:50`, which avoids recursively expanding placeholders embedded in user-provided values.
- edge_cases_or_failure_modes: This is a policy template, not the enforcing validator. If missing, `goal_tracker_blocked_message` has an inline fallback at `hooks/lib/loop-common.sh:1103`. The examples already use “Task” terminology at `prompt-template/block/goal-tracker-modification.md:17`, aligning with the branch direction away from user-facing “todo” wording.
- validation_or_tests: Template reference coverage includes `tests/test-template-references.sh`, which lists this template. Runtime enforcement depends on validators that call `goal_tracker_blocked_message`; this file supplies the human-readable contract for that gate.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-116 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex prompt template for configurable Full Goal Alignment Check rounds. It defines the comprehensive review contract that audits the immutable plan, goal tracker, implementation reality, historical progress, stagnation, and terminal `COMPLETE`/`STOP` decisions.
- algorithmic_behavior: The stop hook selects this template when the full-alignment cadence is met at `hooks/loop-codex-stop-hook.sh:774` and renders it at `hooks/loop-codex-stop-hook.sh:817`. The template requires Codex to read the original plan first at `prompt-template/codex/full-alignment-review.md:7`, embeds Claude’s round summary at `prompt-template/codex/full-alignment-review.md:13`, mandates a goal tracker audit at `prompt-template/codex/full-alignment-review.md:19`, requires acceptance-criterion-by-acceptance-criterion status with evidence/blockers/deferrals at `prompt-template/codex/full-alignment-review.md:23`, compares plan versus tracker for forgotten items at `prompt-template/codex/full-alignment-review.md:30`, audits deferred items at `prompt-template/codex/full-alignment-review.md:36`, and requires implementation review against claims and docs at `prompt-template/codex/full-alignment-review.md:50`.
- inputs_outputs_state: Inputs are `{{CURRENT_ROUND}}`, `{{PLAN_FILE}}`, `{{SUMMARY_CONTENT}}`, `{{GOAL_TRACKER_FILE}}`, `{{DOCS_PATH}}`, `{{GOAL_TRACKER_UPDATE_SECTION}}`, `{{COMPLETED_ITERATIONS}}`, `{{LOOP_TIMESTAMP}}`, previous-round variables, and `{{REVIEW_RESULT_FILE}}`. Output is Codex-authored review content written to `{{REVIEW_RESULT_FILE}}`; the last non-empty line is consumed by the stop hook as either `COMPLETE`, `STOP`, or ordinary issue feedback. The hook clamps previous-round indices to zero at `hooks/loop-codex-stop-hook.sh:789` to keep historical references valid at low round numbers.
- gates_or_invariants: Full alignment is mandatory at configured intervals. Codex must verify all acceptance criteria, detect forgotten work, review deferrals, assess implementation truth, and inspect recent loop history for stagnation. The template’s completion gate is strict: issues or any unmet acceptance criterion require findings and action items; deferrals are treated as incomplete; `COMPLETE` is allowed only when all original plan tasks and acceptance criteria are fully met with no deferrals at `prompt-template/codex/full-alignment-review.md:86`. Stagnation requires `STOP` as the last line at `prompt-template/codex/full-alignment-review.md:84`.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh`; includes a goal-tracker update section generated separately at `hooks/loop-codex-stop-hook.sh:768`. It coordinates with historical loop artifacts named in the template: `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, and `round-N-review-result.md` at `prompt-template/codex/full-alignment-review.md:63`. Terminal markers are interpreted by the hook’s `LAST_LINE_TRIMMED` logic at `hooks/loop-codex-stop-hook.sh:1389`; `STOP` triggers `end_loop(... stop)` at `hooks/loop-codex-stop-hook.sh:1472`, while `COMPLETE` triggers the implementation-to-review transition at `hooks/loop-codex-stop-hook.sh:1395`.
- edge_cases_or_failure_modes: Repeated issues, lack of acceptance-criteria progress, repeated mistakes, circular discussions, no code changes, or repeated Codex feedback all count as stagnation triggers at `prompt-template/codex/full-alignment-review.md:76`. The hook warns but still honors `STOP` outside alignment rounds at `hooks/loop-codex-stop-hook.sh:1487`. Deferrals cannot be used to finish the loop, which prevents false completion when scope is merely postponed.
- validation_or_tests: Template reference tests cover this file, and the stop hook’s prompt construction writes the rendered prompt to `round-N-review-prompt.md`, making the review contract auditable. Behavioral validation is through hook tests around stop/finalize/review flow plus template-reference checks.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item headings present: HZ-026, HZ-056, HZ-086, HZ-116
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`