# agent_24 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Hook lifecycle registry for the Claude plugin’s RLCR and PR-loop state machines. It is not algorithm code itself, but it wires all runtime validation and stop-phase transition hooks into Claude Code hook events.
- algorithmic_behavior: Declares three hook surfaces. `UserPromptSubmit` always runs `hooks/loop-plan-file-validator.sh` before prompt submission (`hooks/hooks.json:4-13`). `PreToolUse` dispatches by tool matcher: `Write` to `loop-write-validator.sh`, `Edit` to `loop-edit-validator.sh`, `Read` to `loop-read-validator.sh`, and `Bash` to `loop-bash-validator.sh` (`hooks/hooks.json:14-50`). `Stop` runs both loop stop hooks, `loop-codex-stop-hook.sh` then `pr-loop-stop-hook.sh`, each with a 7200 second timeout (`hooks/hooks.json:52-64`).
- inputs_outputs_state: Input is Claude’s hook event stream plus `${CLAUDE_PLUGIN_ROOT}` resolution. Output is side-effect execution of hook commands. The registry itself stores no state, but it activates scripts that inspect `.humanize/rlcr/<timestamp>/state.md`, optional `finalize-state.md`, `.humanize/pr-loop/<timestamp>/state.md`, current git branch/status, current tool JSON, and round files.
- gates_or_invariants: The registry enforces coverage over all major bypass channels: prompt submission, file writes, edits, reads, shell commands, and session stopping. The stop hook ordering means an active RLCR loop is checked first, while an active PR loop can still intercept stops if no RLCR condition exits. The timeout invariant is explicit on stop hooks only; PreToolUse validators rely on fast local checks.
- dependencies_and_callers: Consumed by Claude Code’s hook system. Registered scripts depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `jq`, `git`, and sometimes `gh`/`codex`. The hook scripts use shared strict state parsing and message templates. For example, `loop-plan-file-validator.sh` reads active RLCR state and blocks malformed/outdated schema (`hooks/loop-plan-file-validator.sh:29-49`, `hooks/loop-plan-file-validator.sh:59-89`), while `loop-codex-stop-hook.sh` drives RLCR review/finalize transitions.
- edge_cases_or_failure_modes: Misconfigured `${CLAUDE_PLUGIN_ROOT}` breaks every command path. If a matcher is omitted, that tool becomes a bypass path. The registry has no schema validation beyond being JSON; invalid JSON would prevent hook installation/reading by the host. Running both stop hooks can be intentional but creates ordering sensitivity if both loop directories exist.
- validation_or_tests: Hook-system robustness is covered elsewhere, including references in `tests/robustness/test-hook-system-robustness.sh` and direct stop hook invocations listed by `rg`. `tests/test-template-references.sh` validates template references and safe rendering usage for hook scripts, while `tests/test-plan-file-hooks.sh` exercises `loop-codex-stop-hook.sh`. This assigned file itself is a wiring manifest, not an executable test.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for `/gen-plan` command structure, frontmatter contract, draft relevance agent metadata, content restrictions, and the IO validator’s exit-code semantics.
- algorithmic_behavior: Starts with strict shell settings and derives `PROJECT_ROOT`, `COMMANDS_DIR`, and `AGENTS_DIR` from the test file location (`tests/test-gen-plan.sh:9-14`). It maintains pass/fail counters through local `pass` and `fail` helpers (`tests/test-gen-plan.sh:22-40`). Positive tests PT-1 through PT-9 assert that `commands/gen-plan.md` exists, has description, `allowed-tools`, `argument-hint`, and that `agents/draft-relevance-checker.md` exists with `name: draft-relevance-checker`, `model: haiku`, and tools metadata; it also checks version consistency across plugin, marketplace, and README files (`tests/test-gen-plan.sh:54-180`). Negative tests create invalid fixtures under `mktemp -d` to validate name format, missing frontmatter, malformed YAML-like lines, invalid model aliases, and non-English/emoji/CJK content checks (`tests/test-gen-plan.sh:191-423`). The final section executes `scripts/validate-gen-plan-io.sh` against invalid and valid path combinations and asserts expected exit codes (`tests/test-gen-plan.sh:425-542`).
- inputs_outputs_state: Inputs are repository files: `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. It creates temporary fixture directories and files via `mktemp -d` and `cat >` inside temp locations, then cleans them with `trap`. Output is colored PASS/FAIL lines, a summary, and process exit `0` if `TESTS_FAILED == 0`, otherwise `1` (`tests/test-gen-plan.sh:544-562`).
- gates_or_invariants: Command names must match `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:195-199`). Command frontmatter must start with `---` and include `description`; agent frontmatter must include `name` and `description` (`tests/test-gen-plan.sh:201-214`). Model names must be exact aliases `opus|sonnet|haiku` or full IDs starting `claude-`, `gpt-`, `o[0-9]`, or `gemini-` (`tests/test-gen-plan.sh:350-358`). `gen-plan.md` and the relevance agent must avoid Emoji/CJK content (`tests/test-gen-plan.sh:404-423`). IO validator exit-code contract: missing option values, unknown options, and help return `6`; missing input returns `1`; empty input returns `2`; missing output dir returns `3`; existing output path or directory returns `4`; valid paths return `0` (`tests/test-gen-plan.sh:438-539`).
- dependencies_and_callers: Depends on standard shell tools (`sed`, `grep`, `awk`, `tr`, `mktemp`, `touch`, `mkdir`, `wc`) and the executable validator. It specifies the behavior of `commands/gen-plan.md`, whose workflow starts with running `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` and maps exit codes 0-7 to user-facing stops or continuation (`commands/gen-plan.md:30-47`). It also specifies the relevance agent in `agents/draft-relevance-checker.md`, which returns either `RELEVANT:` or `NOT_RELEVANT:` and is intentionally lenient (`agents/draft-relevance-checker.md:27-36`).
- edge_cases_or_failure_modes: The YAML syntax checker is a lightweight regex, not a real YAML parser; it can accept some invalid YAML or reject complex valid YAML. `grep -P` for Emoji/CJK may be unavailable on some systems; the test redirects errors and treats that as no match. Version extraction assumes exact text patterns. The script sets two `trap "rm -rf ..."` handlers; the later one for `SCRIPT_TEST_DIR` overwrites the earlier `TEST_FIXTURES_DIR` cleanup trap (`tests/test-gen-plan.sh:191-193`, `tests/test-gen-plan.sh:433-436`), leaving a possible temp cleanup gap.
- validation_or_tests: This is itself a validation script. It also verifies `scripts/validate-gen-plan-io.sh`, whose implementation parses `--input`/`--output`, normalizes paths, checks input existence/non-empty, output directory existence, output non-existence, write permission, then copies `prompt-template/plan/gen-plan-template.md` and appends the original draft (`scripts/validate-gen-plan-io.sh:29-160`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when the RLCR stop hook cannot trust `git status`. It defines a fail-closed transition message for repository-state verification failures.
- algorithmic_behavior: The template renders a heading, includes the placeholder `{{GIT_STATUS_EXIT}}`, lists likely causes, and instructs manual `git status` recovery (`prompt-template/block/git-status-failed.md:1-10`). In the RLCR stop hook, git status is cached before large-file and clean-tree checks; if `run_with_timeout 30 git status --porcelain` exits nonzero, the hook renders this template via `load_and_render_safe` and returns JSON with `"decision": "block"` (`hooks/loop-codex-stop-hook.sh:397-425`).
- inputs_outputs_state: Input variable is `GIT_STATUS_EXIT`, passed from the failed `git status` call (`hooks/loop-codex-stop-hook.sh:410-421`). Output is human-readable markdown embedded as the hook block reason. No state file is mutated by this template; it prevents progression by causing the stop hook to block.
- gates_or_invariants: Invariant is fail-closed repository verification: if git status fails or times out, the RLCR loop cannot proceed to large-file checks, clean-tree checks, review transitions, or finalization. The template names three accepted failure classes: git unresponsive/lock contention, invalid repo state, and large repo slowness (`prompt-template/block/git-status-failed.md:5-8`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh`, whose `load_and_render_safe` uses fallback text if the template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`). Direct caller found in `hooks/loop-codex-stop-hook.sh:420`. A similar PR-loop git-status failure path currently uses an inline `# Git Status Failed` reason rather than this file (`hooks/pr-loop-stop-hook.sh:282-292`), so this assigned template is RLCR-facing in the current tree.
- edge_cases_or_failure_modes: If `GIT_STATUS_EXIT` is omitted, the placeholder remains because rendering preserves missing variables (`hooks/lib/template-loader.sh:115-122`). If the template is missing/empty, the hook fallback is shorter and lacks the detailed bullet list. The template cannot distinguish timeout `124` from other git errors except through the exit code.
- validation_or_tests: Template loading behavior is exercised by `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and template robustness tests found by repository search. The concrete gate path is covered indirectly by stop-hook tests that invoke `loop-codex-stop-hook.sh`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-114 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing review-phase prompt template for RLCR. It is emitted after Codex code review finds `[P0-9]` issues and drives the loop from implementation completion into issue-fix iterations.
- algorithmic_behavior: The template states the agent is in Review Phase, injects `{{REVIEW_CONTENT}}`, instructs fixing all `[P0-9]` severity issues, forbids unrelated features, requires committing changes, and requires writing a summary to `{{SUMMARY_FILE}}` (`prompt-template/claude/review-phase-prompt.md:1-15`). It also states that `COMPLETE` has no effect in review phase and that Codex will review again until no `[P0-9]` issues remain (`prompt-template/claude/review-phase-prompt.md:23-28`).
- inputs_outputs_state: Inputs are `REVIEW_CONTENT` from extracted Codex review output and `SUMMARY_FILE` for the next round summary. Output is written to `.humanize/rlcr/<loop>/round-<round>-prompt.md` by `continue_review_loop_with_issues`, then returned as the hook block reason (`hooks/loop-codex-stop-hook.sh:1117-1161`). The same function updates `current_round` in the state file before writing the prompt (`hooks/loop-codex-stop-hook.sh:1123-1129`).
- gates_or_invariants: The main invariant is review-phase completion cannot be self-declared by Claude; `COMPLETE` is ignored when `review_started=true` (`hooks/loop-codex-stop-hook.sh:1395-1400`). The loop continues until Codex review output has no `[P0-9]` markers. A review-phase marker file `.review-phase-started` must exist when `review_started=true`, blocking manual state toggle attacks (`hooks/loop-codex-stop-hook.sh:1447-1463`).
- dependencies_and_callers: Direct caller is `continue_review_loop_with_issues` in `hooks/loop-codex-stop-hook.sh:1149-1151`. Review content originates from `detect_review_issues`, which scans Codex review logs for `[P?]` markers in the first 10 characters of each line and extracts from the first issue to the end (`hooks/lib/loop-common.sh:332-391`). Review execution is performed by `run_codex_code_review`, which writes audit prompt/command/log files and runs `codex review --base <base>` under `run_with_timeout` (`hooks/loop-codex-stop-hook.sh:917-979`). The transition into review phase happens when the implementation-phase last nonblank line is exactly `COMPLETE`; state is changed to `review_started: true`, marker file is touched, and initial code review runs for the next round (`hooks/loop-codex-stop-hook.sh:1395-1435`).
- edge_cases_or_failure_modes: Because rendering is single-pass, any `{{...}}` inside review content is preserved rather than recursively expanded, preventing prompt variable injection (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`). If Codex review fails or produces no usable log, the hook uses a separate hard block path rather than this template. If `SUMMARY_FILE` is missing from render variables, the placeholder remains. The template’s `[P0-9]` language is broad text; actual detection uses `[P<digit>]` near line start, so review output formatting matters.
- validation_or_tests: Review/finalize behavior is covered by `tests/test-finalize-phase.sh`, including negative cases for review failure and empty review output, and by stop-hook tests. Template reference safety is covered by `tests/test-template-references.sh`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-144 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout abstraction used by hooks and loop scripts to bound git, gh, codex, and shell operations.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-17`). It validates timeout implementation detection, accepted implementation names, successful command execution before timeout, timeout exit code `124`, argument preservation, nonzero exit-code preservation, pipeline/subshell execution via `sh -c`, short timeout elapsed behavior, zero-timeout tolerance, large output handling, repeated cycles, nonexistent commands, empty command behavior, special character argument preservation, signal handling, fallback detection, and exported `TIMEOUT_IMPL` visibility (`tests/robustness/test-timeout-robustness.sh:31-272`). It prints a shared summary and exits with the helper’s status (`tests/robustness/test-timeout-robustness.sh:274-279`).
- inputs_outputs_state: Inputs are available system commands: `gtimeout`, `timeout`, `python3`, `python`, `sleep`, `echo`, `printf`, `sh`, `seq`, `wc`, and `date`. It reads `TIMEOUT_IMPL` from the sourced timeout helper. Output is PASS/FAIL lines plus summary; state is only in test counters from `tests/test-helpers.sh`.
- gates_or_invariants: Accepted implementation chain is `gtimeout|timeout|python3|python|none` (`tests/robustness/test-timeout-robustness.sh:42-49`). Timed-out commands must return `124` when a timeout implementation exists (`tests/robustness/test-timeout-robustness.sh:63-77`). Successful commands must preserve stdout and exit code, including exit `42` (`tests/robustness/test-timeout-robustness.sh:89-100`). Very short timeout must complete in under 5 seconds with exit `124` (`tests/robustness/test-timeout-robustness.sh:120-134`). Special characters in arguments must not be shell-expanded (`tests/robustness/test-timeout-robustness.sh:211-219`).
- dependencies_and_callers: Direct dependency is `scripts/portable-timeout.sh`. That helper detects timeout in priority order `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:9-27`), stores `TIMEOUT_IMPL`, and implements `run_with_timeout <seconds> <command> [args...]` (`scripts/portable-timeout.sh:29-71`). Hook callers include `loop-plan-file-validator.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh`, where timeout bounds git/gh/codex operations.
- edge_cases_or_failure_modes: If `TIMEOUT_IMPL=none`, timeout-specific tests are skipped as passes, meaning the suite verifies graceful degradation but not enforcement on such hosts (`tests/robustness/test-timeout-robustness.sh:65-77`, `tests/robustness/test-timeout-robustness.sh:120-155`). The Python fallback uses `subprocess.run(sys.argv[1:], timeout=<seconds>)` without capturing stdout/stderr, so command output passes through but process-tree termination semantics may differ from GNU timeout (`scripts/portable-timeout.sh:47-61`). Zero timeout behavior is intentionally allowed to be either success or immediate timeout (`tests/robustness/test-timeout-robustness.sh:139-155`). The signal-handling test allows either `0` or `124`, accepting implementation variance (`tests/robustness/test-timeout-robustness.sh:221-233`).
- validation_or_tests: This file is the validation. It relies on `tests/test-helpers.sh` for counters and `print_test_summary`, which returns nonzero on any failure (`tests/test-helpers.sh:58-78`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique Item Evidence headings above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`