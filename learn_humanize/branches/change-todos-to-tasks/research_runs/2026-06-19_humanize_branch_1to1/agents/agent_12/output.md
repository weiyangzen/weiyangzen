# agent_12 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side prompt templates for the RLCR review loop. This directory defines the review contracts that decide whether a Claude implementation round continues, transitions to finalize, or stops for stagnation. Its files are rendered by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` at lines 771, 819, 835, and 949.
- algorithmic_behavior: The directory has four files:
  - `prompt-template/codex/regular-review.md`: regular round review prompt. It forces Codex to read `@{{PLAN_FILE}}`, compare Claude’s claimed work against `@{{PROMPT_FILE}}`, inspect `@{{GOAL_TRACKER_FILE}}`, and output `COMPLETE` only when all original plan tasks and acceptance criteria are done with no pending or deferred work (`regular-review.md:3-56`).
  - `prompt-template/codex/full-alignment-review.md`: mandatory interval audit. It adds acceptance-criterion table auditing, forgotten-item detection, deferred-item audit, historical review/summary lookup, and a stagnation circuit breaker. It requires `STOP` as the final line when progress is stalled and allows `COMPLETE` only when every original AC is met with no deferrals (`full-alignment-review.md:19-93`).
  - `prompt-template/codex/goal-tracker-update-section.md`: injected into both regular and full-alignment prompts. It delegates post-Round-0 goal-tracker mutation authority to Codex, allowing movement among Active, Completed, Deferred, Plan Evolution, and Open Issues while forbidding mutation of the immutable Ultimate Goal and Acceptance Criteria (`goal-tracker-update-section.md:1-17`).
  - `prompt-template/codex/code-review-phase.md`: audit documentation for `codex review --base`. It records that `codex review` does not take a prompt, describes severity-marker scanning, and names the generated prompt/result/cmd/log files (`code-review-phase.md:1-36`).
- inputs_outputs_state: Inputs are template variables from `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, historical round indices, `REVIEW_RESULT_FILE`, `BASE_BRANCH`, and `TIMESTAMP` (`hooks/loop-codex-stop-hook.sh:762-847`, `949-955`). Outputs are rendered `round-N-review-prompt.md` files in the loop directory and audit prompt files for code-review phase. State transitions are prompt-mediated: regular/full alignment review can keep the loop active by writing findings, can allow finalize by ending with `COMPLETE`, or can halt as stagnated by ending with `STOP` in full alignment.
- gates_or_invariants: `COMPLETE` is strictly gated on all original work and ACs being fully met with no deferrals (`regular-review.md:53-56`, `full-alignment-review.md:88-93`). Deferred work is explicitly incomplete in both regular and full reviews. Full-alignment rounds add a stagnation invariant: repeated issues, no meaningful AC progress, repeated Claude mistakes, circular discussion, no code changes, or repeated Codex feedback trigger final-line `STOP` (`full-alignment-review.md:76-85`). The goal-tracker template forbids modifying the immutable section (`goal-tracker-update-section.md:10`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which selects full alignment when `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` after validating `FULL_REVIEW_ROUND >= 2` (`hooks/loop-codex-stop-hook.sh:774-784`). Rendering is performed by `hooks/lib/template-loader.sh`, whose placeholder semantics are single-pass and preserve missing placeholders (`hooks/lib/template-loader.sh:7-20`, `50-57`, `71-131`). Template tests cover `goal-tracker-update-section.md` rendering in `tests/test-templates-comprehensive.sh:531-539`.
- edge_cases_or_failure_modes: Missing templates are handled by `load_and_render_safe` fallbacks in the hook (`hooks/loop-codex-stop-hook.sh:768-815`, `937-949`). Invalid or corrupted `FULL_REVIEW_ROUND` defaults to 5 (`hooks/loop-codex-stop-hook.sh:776-780`). Prior-round references are clamped to zero to avoid negative file paths on early full reviews (`hooks/loop-codex-stop-hook.sh:786-792`). `code-review-phase.md` is audit-only because the actual `codex review` command ignores prompt input (`code-review-phase.md:3-4`, `hooks/loop-codex-stop-hook.sh:916-918`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` validates at least the goal-tracker Codex template render path (`tests/test-templates-comprehensive.sh:531-539`). Runtime hook behavior around code review is implemented in `hooks/loop-codex-stop-hook.sh:817-847` and `917-980`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Phase-1 IO gate and materialization step for the `/gen-plan` command. It validates draft input and target plan output before the command performs relevance checking and deeper analysis.
- algorithmic_behavior: The script parses `--input` and `--output`, rejects missing values, unknown flags, and help with usage exit code 6 (`scripts/validate-gen-plan-io.sh:16-67`). It normalizes input/output via `realpath -m` when available (`69-72`), then validates input existence, non-empty input, output directory existence, output path not being a directory or existing file, and write permission (`79-124`). On success, it locates `prompt-template/plan/gen-plan-template.md`, copies it to the output path, appends the original draft between explicit start/end separators, and exits 0 (`126-160`).
- inputs_outputs_state: Inputs are command arguments `--input <draft>` and `--output <plan>`, plus optional `CLAUDE_PLUGIN_ROOT` for template lookup (`135-140`). Outputs are stdout validation messages, error-specific exit codes, and a newly created output plan file containing the plan template plus draft content (`148-157`). It mutates filesystem state only after all IO validations pass and after the plan template is found.
- gates_or_invariants: Exit-code contract is documented at lines 4-12 and mirrored in `commands/gen-plan.md:38-46`: 1 input missing, 2 input empty, 3 output directory missing, 4 output path invalid/existing, 5 no write permission, 6 invalid arguments/help, 7 missing plan template. The output file must be new; existing files and directories both fail before any write (`103-116`). Template absence is treated as plugin configuration failure (`142-146`).
- dependencies_and_callers: Called by `commands/gen-plan.md` Phase 1 using `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` (`commands/gen-plan.md:30-36`). The command’s allowed tools whitelist this script (`commands/gen-plan.md:4-11`) and maps each exit code to a stop/report behavior (`38-47`). Depends on `realpath`, `dirname`, `wc`, `cp`, `cat`, and the plan template under either `$CLAUDE_PLUGIN_ROOT/prompt-template/plan/gen-plan-template.md` or script-relative `../prompt-template/plan/gen-plan-template.md`.
- edge_cases_or_failure_modes: `realpath -m` fallback means non-GNU environments still proceed with the original path string (`69-71`). A path with a missing parent directory fails as output-dir-not-found before output-exists checks (`95-101`). `--help` exits 6, same as invalid arguments, which is intentional per usage and tests (`48-50`). Under `set -e`, failed `cp` or append writes after validation would terminate nonzero, but those post-validation write failures are not assigned a documented custom exit code.
- validation_or_tests: `tests/test-gen-plan.sh:426-541` verifies the script is executable and covers missing `--input`, missing `--output`, flag-as-value, unknown option, missing input, empty input, missing output directory, existing output file, output path as directory, valid path success, and `--help` exit 6.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for zsh compatibility of monitor-related runtime behavior, especially replacing unsafe unmatched globs with `find` loops so empty directories and dotfile-only directories do not crash with zsh `nomatch`.
- algorithmic_behavior: The test must run under zsh (`#!/usr/bin/env zsh`) and uses `set -euo pipefail` (`tests/test-zsh-monitor-safety.sh:1-13`). It creates an isolated `/tmp/test-zsh-humanize-$$` workspace, sets `XDG_CACHE_HOME` under that workspace, and cleans it up on exit (`48-60`). It sources `scripts/humanize.sh` and fails if sourcing emits `no matches found` (`71-77`). It then simulates or mirrors monitor logic for latest session discovery, state-file discovery, cache log discovery, valid timestamp ordering, and zsh shell verification (`90-286`). Test 8 demonstrates why the old `.humanize/rlcr/*` pattern is unsafe and why `find` is safe (`289-321`).
- inputs_outputs_state: Inputs are the project root’s `scripts/humanize.sh`, temporary `.humanize/rlcr` session directories/files, and cache files under `$XDG_CACHE_HOME`. Outputs are PASS/FAIL counters and process exit 0 only if `TESTS_FAILED == 0` (`323-341`). State changes are confined to the temp directory and removed by trap.
- gates_or_invariants: The invariant is “no zsh `no matches found` errors” when loop dirs are empty, contain only dotfiles, contain no `*-state.md`, or cache dirs have no matching Codex logs (`72-77`, `118-124`, `152-157`, `199-205`, `221-235`). Timestamp session names must match `YYYY-MM-DD_HH-MM-SS`, and lexical max timestamp wins (`105-115`, `255-273`). The test also gates that zsh is actually in use via non-empty `$ZSH_VERSION` (`282-286`).
- dependencies_and_callers: Depends on `zsh`, `scripts/humanize.sh`, `find`, `basename`, and temporary filesystem access. Production functions under test are in `scripts/humanize.sh`, which sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6-10`). The shared `monitor_find_latest_session` uses `find` instead of globs (`scripts/lib/monitor-common.sh:40-63`), and `monitor_find_state_file` uses `find "$session_dir" -name '*-state.md'` (`153-188`). `humanize monitor` wraps those helpers and uses `find` for cached `round-*-codex-run.log` / `round-*-codex-review.log` discovery (`scripts/humanize.sh:237-246`, `280-339`, `344-347`).
- edge_cases_or_failure_modes: If run under bash instead of zsh, shell verification fails (`276-286`). If `/tmp` or cache creation is unavailable, setup fails before monitor assertions. Test 2 and Test 3 duplicate the helper pattern locally rather than only calling the production helper, so they specify expected behavior but are not a pure black-box call for every case. Test 8 disables strict nomatch locally for demonstration (`300-321`), so it proves the new `find` path works rather than expecting the old path to actually fail in that subshell.
- validation_or_tests: This file is itself the validation suite. It has eight named tests and exits 1 if any fail (`323-341`). Related broader monitor coverage exists in `tests/test-monitor-runtime.sh` and `tests/test-monitor-e2e-real.sh`, but this assigned file specifically protects zsh glob safety.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Small block-message template used by read/write validators when an agent accesses the right loop filename from the wrong directory path. It participates in path-boundary enforcement for RLCR loop files.
- algorithmic_behavior: The template renders a block reason with `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`, then instructs the agent to use `cat {{FILE_PATH}}` if it needs the file (`prompt-template/block/wrong-directory-path.md:1-6`). It is content-only; validation logic lives in the caller scripts.
- inputs_outputs_state: Inputs are validator-provided template variables. Output is stderr text from `load_and_render_safe` explaining the rejected path and the correct path. It has no persistent state.
- gates_or_invariants: The gate is an exact path equality check in callers. In `hooks/loop-read-validator.sh`, `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and any mismatch renders this template with `ACTION=read`, then exits 2 (`hooks/loop-read-validator.sh:153-168`). In `hooks/loop-write-validator.sh`, the same exact-path rule renders with `ACTION=write to`, then exits 2 (`hooks/loop-write-validator.sh:248-263`).
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh:159-167` and `hooks/loop-write-validator.sh:254-262` through `hooks/lib/template-loader.sh`’s safe loader. It coordinates with sibling block templates for wrong round, wrong location, state-file modification, and other hook blocks under `prompt-template/block`.
- edge_cases_or_failure_modes: The final instruction says `cat {{FILE_PATH}}` even though the validator calculated a separate `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:3-6`). If `FILE_PATH` is the rejected path, this message can be confusing and may be a skip/repair candidate depending on intended UX; however, algorithmically the actual block and correct-path display still come from the caller. If the template is missing, callers have fallback text (`hooks/loop-read-validator.sh:159-163`, `hooks/loop-write-validator.sh:254-258`).
- validation_or_tests: The direct behavior is covered through validator tests in the suite family for hook/path validation, and the template-loader system validates real template rendering patterns. The concrete caller paths and exit 2 behavior are in `hooks/loop-read-validator.sh:159-168` and `hooks/loop-write-validator.sh:254-263`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-132 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test suite for the production `humanize_parse_goal_tracker` parser in `scripts/humanize.sh`. It specifies how monitor/status code summarizes goal-tracker acceptance criteria, task states, deferred work, open issues, and ultimate-goal text.
- algorithmic_behavior: The test sources shared helpers and production `scripts/humanize.sh` (`tests/robustness/test-goal-tracker-robustness.sh:15-24`). It documents the parser output shape as `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`35-36`) and provides `parse_result` to extract fields by pipe position (`38-51`). It then creates synthetic goal-tracker files covering valid formats and malformed/edge formats, calls `humanize_parse_goal_tracker`, and asserts counters/summaries (`60-525`).
- inputs_outputs_state: Inputs are generated markdown files under the helper-created `TEST_DIR`, plus production parser code. Outputs are PASS/FAIL counters through `tests/test-helpers.sh`, summary via `print_test_summary`, and exit status equal to that summary (`tests/robustness/test-goal-tracker-robustness.sh:526-531`; `tests/test-helpers.sh:58-78`). Temporary state is cleaned by `setup_test_dir` trap (`tests/test-helpers.sh:84-89`).
- gates_or_invariants: The parser must count ACs in list and table forms (`60-116`), count active tasks while excluding `completed` and `deferred` statuses in the Active Tasks table (`118-153`), count completed rows and unique completed ACs (`155-202`), extract Ultimate Goal text (`204-227`), and count open issues/deferred tasks (`373-439`). It must return defaults for missing files (`237-244`), zero for empty/header-only/malformed files (`246-256`, `304-326`, `441-475`), handle large AC counts, special characters, truncated content, binary content, mixed bold AC formats, and truncate long goal summaries to 60 characters (`258-371`, `477-525`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/humanize.sh`, and shell utilities `sed`, `grep`, `cut`, `sort`, `wc`, `tr`, `seq`, and `printf`. The production parser is implemented in `scripts/humanize.sh:30-112`: it returns defaults for missing files, counts AC lines with a regex over the `### Acceptance Criteria` to `---` section, counts markdown table rows, subtracts completed/deferred active statuses, counts unique completed AC IDs, and truncates the Ultimate Goal to 60 chars.
- edge_cases_or_failure_modes: The parser is regex/section-boundary based, so section header drift matters. The malformed-file test expects zero when proper `###` headers are absent (`304-326`). The mixed AC test notes `AC-3.1` is not counted by the current regex (`477-499`). Binary-content handling only requires at least one parseable AC, not full recovery after binary bytes (`349-371`). `parse_result` uses pipe delimiters, so a goal summary containing `|` could shift fields; that case is not tested here.
- validation_or_tests: This file is the validation suite with 18 tests and a final summary exit (`526-531`). It is a direct executable specification for `humanize_parse_goal_tracker`, whose parser body is in `scripts/humanize.sh:30-112`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item sections above, one section per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`