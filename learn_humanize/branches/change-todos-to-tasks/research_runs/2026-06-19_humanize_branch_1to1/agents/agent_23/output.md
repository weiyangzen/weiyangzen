# agent_23 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook preflight validator that converts Claude transcript task state into a hard exit gate before RLCR can stop or run expensive Codex review. It is explicitly built for the branch’s todos-to-tasks transition: the module docstring says it supports both legacy `TodoWrite` and the new Task system, `TaskCreate`/`TaskUpdate`, at `hooks/check-todos-from-transcript.py:3-10`.
- algorithmic_behavior: The main algorithm has two layers. `extract_tool_calls_from_entry()` normalizes transcript entries into `(tool_name, tool_input)` pairs from three supported shapes: Claude assistant message blocks, alternative `type: message` blocks, and direct `type: tool_use` entries at `hooks/check-todos-from-transcript.py:21-58`. `find_incomplete_items()` then scans JSONL transcript lines, ignores malformed lines, tracks the latest non-empty legacy `TodoWrite` list, and builds a task map from `TaskCreate`/`TaskUpdate` events at `hooks/check-todos-from-transcript.py:61-164`.
- inputs_outputs_state: Input is hook JSON on stdin with optional `transcript_path`, parsed in `main()` at `hooks/check-todos-from-transcript.py:167-186`. Output is process exit status plus optional stdout/stderr. Exit `0` means no incomplete work or no usable transcript; exit `1` prints `INCOMPLETE_TODOS` plus bullets; exit `2` prints `PARSE_ERROR` to stderr for malformed hook input JSON at `hooks/check-todos-from-transcript.py:176-179` and `hooks/check-todos-from-transcript.py:207-211`. Internal state is transient: `latest_todos`, `tasks`, and `next_task_id` at `hooks/check-todos-from-transcript.py:74-81`.
- gates_or_invariants: Only exact status `completed` is terminal; every other status, including empty, `pending`, or `in_progress`, is incomplete at `hooks/check-todos-from-transcript.py:141-154`. `TaskCreate` always starts as pending and receives synthetic sequential IDs when the transcript does not expose IDs at `hooks/check-todos-from-transcript.py:103-115`. `TaskUpdate` on an unknown task creates a placeholder so tasks created before transcript capture can still be represented at `hooks/check-todos-from-transcript.py:117-135`. The stop hook treats checker exit `1` as a block before Codex review and exit `2` as a parse-error block at `hooks/loop-codex-stop-hook.sh:346-394`.
- dependencies_and_callers: Uses only Python stdlib modules: `json`, `sys`, `pathlib.Path`, and typing imports at `hooks/check-todos-from-transcript.py:15-18`. Primary caller is `hooks/loop-codex-stop-hook.sh`, which invokes `python3 "$TODO_CHECKER"` using the original hook input at `hooks/loop-codex-stop-hook.sh:346-350`. Test coverage is concentrated in `tests/test-todo-checker.sh`, including input handling at `tests/test-todo-checker.sh:52-98`, legacy todo completion at `tests/test-todo-checker.sh:107-120`, and Task system cases at `tests/test-todo-checker.sh:300-435`. `tests/test-finalize-phase.sh` also exercises this gate in finalize mode at `tests/test-finalize-phase.sh:702-741`.
- edge_cases_or_failure_modes: Missing `transcript_path`, empty stdin, or nonexistent transcript path all allow exit `0`, so missing transcript evidence is fail-open by design at `hooks/check-todos-from-transcript.py:171-183` and `hooks/check-todos-from-transcript.py:71-72`. Invalid JSONL lines inside a transcript are silently ignored at `hooks/check-todos-from-transcript.py:89-92`. A later empty `TodoWrite` does not clear previous todos because `latest_todos` is updated only when `todos` is truthy at `hooks/check-todos-from-transcript.py:97-101`. Synthetic Task IDs assume TaskCreate order matches later TaskUpdate IDs; if the real task system emits non-sequential or hidden IDs, updates could attach to placeholders instead of the original task. Status comparison is case-sensitive and has no alias handling.
- validation_or_tests: `tests/test-todo-checker.sh` validates parse errors, empty/missing inputs, nonexistent transcript behavior, completed/incomplete legacy todos, invalid transcript lines, latest-TodoWrite semantics, direct tool-use entries, `TaskCreate`, `TaskUpdate`, orphan updates, mixed legacy/new tasks, and Task ID output. `tests/test-finalize-phase.sh` verifies that incomplete todos/tasks block finalize exit before Codex is invoked at `tests/test-finalize-phase.sh:721-741`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state machine, including transition from normal rounds into finalize, completion out of finalize, validation gates during finalize, and protection of finalize-managed files. The test matrix is declared at `tests/test-finalize-phase.sh:5-19`.
- algorithmic_behavior: The script builds an isolated temp git repo, creates synthetic `.humanize/rlcr/<timestamp>` state, mocks `codex`, and drives hook scripts with JSON inputs. Test helpers and environment setup are at `tests/test-finalize-phase.sh:27-49`; mock Codex implementations are at `tests/test-finalize-phase.sh:51-121`; repo and loop-state setup are at `tests/test-finalize-phase.sh:144-227`. It then runs state detection, validator, stop-hook, review-failure, todo-completion, normal-round, and finalize-state parsing assertions in one end-to-end shell spec.
- inputs_outputs_state: Inputs are generated state files with YAML frontmatter, mock `codex` outputs, temporary summaries, a temporary transcript JSONL, and hook JSON such as `{"stop_hook_active": false, "transcript_path": ...}`. Outputs are pass/fail counters and process exit equal to `TESTS_FAILED` at `tests/test-finalize-phase.sh:867-876`. State transitions under test include `state.md -> finalize-state.md` when Codex says `COMPLETE` before max iterations at `tests/test-finalize-phase.sh:524-540`, `finalize-state.md -> complete-state.md` when all finalize gates pass at `tests/test-finalize-phase.sh:476-496`, and `state.md -> maxiter-state.md` when current round equals max iterations at `tests/test-finalize-phase.sh:542-560`.
- gates_or_invariants: `finalize-state.md` counts as an active loop, while `complete-state.md` does not, tested at `tests/test-finalize-phase.sh:286-320`. `finalize-summary.md` is writable, but `finalize-state.md` is protected from Write, Edit, Bash redirection, `mv`, and `cp` source access at `tests/test-finalize-phase.sh:345-415`. Finalize exit requires a summary, clean git status, and completed todos/tasks before completion at `tests/test-finalize-phase.sh:429-504` and `tests/test-finalize-phase.sh:702-741`. Finalize mode must not invoke Codex at `tests/test-finalize-phase.sh:446-504`. Review failures and empty review output must fail closed, preserve `state.md`, and set or keep `review_started: true` at `tests/test-finalize-phase.sh:562-696`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-finalize-phase.sh:48-49`. Calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. It indirectly depends on `hooks/check-todos-from-transcript.py` for T-NEG-4 and on template loading for block/finalize prompts. It requires shell utilities, `git`, `jq`, and `python3` through the hooks.
- edge_cases_or_failure_modes: The test explicitly guards against a false-success transition when Codex review fails or produces empty output: no `finalize-state.md` or `complete-state.md` may appear in those cases at `tests/test-finalize-phase.sh:588-657`. It verifies normal RLCR behavior remains intact after adding finalize logic: non-COMPLETE review feedback blocks, keeps `state.md`, increments the round, writes `round-3-review-result.md`, and includes review feedback at `tests/test-finalize-phase.sh:747-815`. It also checks validators can parse `finalize-state.md` in place of `state.md`, preventing finalize phase from becoming unreadable to hook validation at `tests/test-finalize-phase.sh:820-865`.
- validation_or_tests: This file is itself the validation asset. It covers helper predicates, active-loop discovery, state-file protection, stop-hook finalize behavior, max-iteration bypass, review-failure fail-closed behavior, incomplete task/todo blocking, normal round continuity, and validator parsing under finalize state. I did not execute it in this read-only research pass; the evidence above comes from direct line-by-line inspection.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Block-message template for the push gate when RLCR is running with `push_every_round: false`. It defines the user-facing reason for rejecting `git push` during a loop and tells the user to start the loop with `--push-every-round` if remote pushes are required.
- algorithmic_behavior: The template itself is static Markdown at `prompt-template/block/git-push.md:1-9`. It has no variables. Runtime behavior is controlled by the bash validator: after parsing active loop state, `hooks/loop-bash-validator.sh` reads `STATE_PUSH_EVERY_ROUND`; if it is not `true` and the command matches a direct `git push`, it renders this template and exits `2` at `hooks/loop-bash-validator.sh:89-105`.
- inputs_outputs_state: Input state is `push_every_round` from loop state frontmatter. `scripts/setup-rlcr-loop.sh` writes this field into `state.md` at `scripts/setup-rlcr-loop.sh:709-717`, and `hooks/lib/loop-common.sh` parses it as `STATE_PUSH_EVERY_ROUND` at `hooks/lib/loop-common.sh:227-239` and strict mode at `hooks/lib/loop-common.sh:282-287`. Output is stderr Markdown headed `Git Push Blocked`, followed by process exit `2` from the bash validator.
- gates_or_invariants: Default RLCR behavior is local commits only. The invariant expressed by this template is: no remote push unless the loop was started with `--push-every-round`, as stated at `prompt-template/block/git-push.md:3-8`. This complements the opposite gate in the stop hook: when `PUSH_EVERY_ROUND` is true, unpushed commits block exit at `hooks/loop-codex-stop-hook.sh:573-603`.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md"` in `hooks/loop-bash-validator.sh:99-104`. Template loader behavior for existing templates is covered by `tests/test-template-loader.sh:57-65` and `tests/test-template-loader.sh:213-222`, and the comprehensive template suite checks that the real template is used instead of fallback at `tests/test-templates-comprehensive.sh:456-463`.
- edge_cases_or_failure_modes: The gate pattern in `hooks/loop-bash-validator.sh:97-99` catches commands beginning with optional whitespace then `git push`; it may not catch semantically equivalent forms such as `git -C path push`, shell aliases, or wrapper scripts unless other validators cover them. Because the template has no variables, rendering failure risk is low; if the file is missing, the validator falls back to an inline `Git Push Blocked` message at `hooks/loop-bash-validator.sh:99-103`.
- validation_or_tests: Template loading is tested in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`. The direct push gate behavior is represented by `hooks/loop-bash-validator.sh:89-105`; this assigned file is the prompt surface for that gate, not the gate implementation itself.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt footer template that flips the push contract when RLCR is configured with `--push-every-round`. It instructs Claude to push commits to remote after each round.
- algorithmic_behavior: The file is a two-line static Markdown note, with the meaningful instruction at `prompt-template/claude/push-every-round-note.md:2`. During next-round prompt generation, `hooks/loop-codex-stop-hook.sh` appends this template only if `PUSH_EVERY_ROUND == true` at `hooks/loop-codex-stop-hook.sh:1543-1550`. If missing, the hook falls back to `Also push your changes after committing.` at `hooks/loop-codex-stop-hook.sh:1545-1548`.
- inputs_outputs_state: Input is the parsed loop state field `push_every_round`, created in initial `state.md` by `scripts/setup-rlcr-loop.sh:709-717`. Output is appended text in the next round prompt file. The same semantic instruction is also inlined into the initial round-0 prompt when `PUSH_EVERY_ROUND` is true at `scripts/setup-rlcr-loop.sh:962-967`.
- gates_or_invariants: This template is the affirmative counterpart to `prompt-template/block/git-push.md`. When push-every-round is true, the stop hook enforces the invariant by checking for unpushed commits and blocking exit if the local branch is ahead of remote at `hooks/loop-codex-stop-hook.sh:573-603`. The prompt note supplies the instruction before that gate is encountered.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh` next-round prompt generation at `hooks/loop-codex-stop-hook.sh:1515-1550`. It depends on `load_template` from `hooks/lib/template-loader.sh`, which returns template contents or empty string at `hooks/lib/template-loader.sh:36-48`. Search also shows `scripts/setup-rlcr-loop.sh` duplicates the same note inline for round 0 rather than loading this template.
- edge_cases_or_failure_modes: The template begins with a blank first line at `prompt-template/claude/push-every-round-note.md:1`; this is harmless for prompt formatting but means exact-output tests need to account for a leading newline. There appears to be an implementation consistency gap: round-0 setup uses inline text instead of this template, so edits to the template affect later rounds but not the initial prompt unless the setup script is also changed. The file has no variable placeholders, so rendering cannot customize branch or remote details.
- validation_or_tests: I found runtime references in `hooks/loop-codex-stop-hook.sh:1543-1550` and the inline round-0 equivalent at `scripts/setup-rlcr-loop.sh:962-967`. I did not find a dedicated test that asserts this exact template file is appended; coverage is mainly indirect through prompt-generation paths and the stop-hook unpushed-commit gate.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-143 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the RLCR prompt template renderer under stress and edge conditions. This matters to core loop behavior because prompt/block templates carry validation instructions, state-transition prompts, and gate messages.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` and `tests/test-helpers.sh`, creates an isolated test directory, then validates `render_template`, `load_and_render`, and `load_and_render_safe` across normal substitution, fallback loading, large values, large files, many variables, special characters, and single-pass injection prevention at `tests/robustness/test-template-stress-robustness.sh:14-19` and `tests/robustness/test-template-stress-robustness.sh:33-289`.
- inputs_outputs_state: Inputs are synthetic template strings, generated template files in `$TEST_DIR`, and `VAR=value` arguments. Outputs are pass/fail helper calls and final `print_test_summary` exit status at `tests/robustness/test-template-stress-robustness.sh:285-290`. State is temporary files such as `standard.md` and `large.md` created at `tests/robustness/test-template-stress-robustness.sh:57-66` and `tests/robustness/test-template-stress-robustness.sh:111-124`.
- gates_or_invariants: Standard `{{VAR}}` placeholders must substitute correctly at `tests/robustness/test-template-stress-robustness.sh:33-42`. Missing templates must use a rendered fallback at `tests/robustness/test-template-stress-robustness.sh:44-52`. Large variable values and templates must still render substitutions at `tests/robustness/test-template-stress-robustness.sh:82-132`. Values containing regex metacharacters, ampersands, backslashes, newlines, dollar signs, and empty strings must remain literal enough to preserve prompt content at `tests/robustness/test-template-stress-robustness.sh:161-250`. Placeholder injection must be prevented: a value containing `{{VAR_B}}` must not be recursively expanded during the same render at `tests/robustness/test-template-stress-robustness.sh:218-228`. This matches the renderer’s documented single-pass invariant in `hooks/lib/template-loader.sh:50-58` and awk implementation at `hooks/lib/template-loader.sh:71-131`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, whose renderer builds `TMPL_VAR_` environment variables and performs a single awk scan at `hooks/lib/template-loader.sh:58-131`. Depends on `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary`. Uses shell utilities `seq`, `printf`, `date`, `wc`, and `grep`.
- edge_cases_or_failure_modes: The test records elapsed time for large substitutions but does not enforce a maximum duration, so it detects functional failure rather than performance regressions. It uses `date +%s%N` at `tests/robustness/test-template-stress-robustness.sh:86-89`, `tests/robustness/test-template-stress-robustness.sh:101-104`, and similar blocks; `%N` is GNU-date-specific, so BSD/macOS environments can make the timing arithmetic brittle even though timing is only used in pass messages. The “Unicode content” test label at `tests/robustness/test-template-stress-robustness.sh:230-239` uses ASCII `Hello World`, so it does not actually stress non-ASCII template rendering. Very large values are generated through Bash brace expansion at `tests/robustness/test-template-stress-robustness.sh:82-103`, so the script is intentionally Bash-specific.
- validation_or_tests: This file is itself the stress validation. It validates the behavior promised by `hooks/lib/template-loader.sh`: single-pass substitution, literal preservation of dangerous replacement characters, missing-template fallback, repeated variables, boundary placeholders, and placeholder-only templates. I did not execute it in this read-only research pass; this evidence is from direct inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CHANGE_TODOS_TO_TASKS-HZ-023`, `CHANGE_TODOS_TO_TASKS-HZ-053`, `CHANGE_TODOS_TO_TASKS-HZ-083`, `CHANGE_TODOS_TO_TASKS-HZ-113`, `CHANGE_TODOS_TO_TASKS-HZ-143`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`