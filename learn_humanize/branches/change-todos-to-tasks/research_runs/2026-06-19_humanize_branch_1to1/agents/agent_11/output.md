# agent_11 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: RLCR loop Claude-facing prompt templates for post-review continuation, review-fix rounds, finalize rounds, full-alignment follow-up, push requirements, and goal-tracker update requests. The directory has eight markdown files and no nested subdirectories: `finalize-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `goal-tracker-update-request.md`, `next-round-footer.md`, `next-round-prompt.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, and `review-phase-prompt.md`.
- algorithmic_behavior: `next-round-prompt.md:1-31` builds the normal “work is not finished” prompt around the original plan, Codex review content, and read-only goal tracker rules after round 0. `next-round-footer.md:4-9` forbids false loop exit/cancel, then requires commit plus summary output. `post-alignment-action-items.md:2-7` is appended after a full goal alignment check to force attention to forgotten items, unmet acceptance criteria, and unjustified deferrals. `push-every-round-note.md:2` conditionally adds remote push requirements. `goal-tracker-update-request.md:2-16` provides the only supported after-round-0 request format for goal tracker changes. `review-phase-prompt.md:1-28` drives the code-review phase by requiring all priority-marked findings to be fixed without unrelated feature work. `finalize-phase-prompt.md:1-51` drives the normal finalize phase after Codex review passes, including code-simplifier invocation and TaskUpdate completion. `finalize-phase-skipped-prompt.md:1-50` is the degraded finalize path when review was skipped, adding manual verification requirements before summary.
- inputs_outputs_state: Inputs are rendered variables from the stop hook: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `NEXT_SUMMARY_FILE`, `SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `REVIEW_SKIP_REASON`, `BASE_BRANCH`, and `START_BRANCH`. Outputs are round prompt files and hook block reasons. The stop hook renders these templates into `round-N-prompt.md` or finalize block output: next-round prompt at `hooks/loop-codex-stop-hook.sh:1524-1527`, post-alignment append at `hooks/loop-codex-stop-hook.sh:1529-1534`, footer at `hooks/loop-codex-stop-hook.sh:1537-1541`, push note at `hooks/loop-codex-stop-hook.sh:1543-1549`, and goal update request at `hooks/loop-codex-stop-hook.sh:1552-1557`. Review and finalize prompts are rendered at `hooks/loop-codex-stop-hook.sh:1149-1151`, `hooks/loop-codex-stop-hook.sh:1070-1076`, and `hooks/loop-codex-stop-hook.sh:1098-1103`.
- gates_or_invariants: The templates enforce phase-specific gates: review phase fixes only and requires a summary (`review-phase-prompt.md:9-15`); finalize phase allows only functionality-equivalent simplification and requires tests not to fail (`finalize-phase-prompt.md:16-24`, `finalize-phase-skipped-prompt.md:26-34`); next-round prompt makes the goal tracker read-only after round 0 and requires update requests instead of direct edits (`next-round-prompt.md:22-31`); footer blocks attempts to exit by editing loop state or canceling the loop (`next-round-footer.md:4`).
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`, using `load_and_render_safe` or `load_template` with fallback text. These templates coordinate with sibling block templates under `prompt-template/block` for hard stops and with setup-generated round-0 prompt state under `.humanize/rlcr/<timestamp>`. The broader loop-common messaging also reinforces the branch transition away from round todo files toward native Task tools at `hooks/lib/loop-common.sh:443-451`.
- edge_cases_or_failure_modes: There is terminology drift: `next-round-prompt.md:12-13` still instructs “create Todos”, while finalize templates require TaskUpdate (`finalize-phase-prompt.md:43`, `finalize-phase-skipped-prompt.md:42`) and loop-common tells agents to use TaskCreate/TaskUpdate/TaskList. This is a likely branch-relevant inconsistency: it may not break execution because the checker supports both legacy TodoWrite and TaskCreate/TaskUpdate, but it can steer agents toward older wording. Review-skipped finalize explicitly lowers confidence and asks for manual verification (`finalize-phase-skipped-prompt.md:7-14`).
- validation_or_tests: Template reference and rendering coverage is implied by `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`; runtime call sites in the stop hook have fallback strings if a template is absent, reducing hard failure risk. The Task-system migration is indirectly validated by `tests/test-todo-checker.sh`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-041 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Entry-point initializer for `/humanize:start-rlcr-loop`. It validates options, validates the implementation plan and repository context, ensures no conflicting loop is active, captures branch/base metadata, creates `.humanize/rlcr/<timestamp>` state, seeds `goal-tracker.md`, and emits the round-0 prompt that starts the RLCR state machine.
- algorithmic_behavior: Argument parsing covers plan location, max iterations, Codex model/effort, Codex timeout, push-every-round, base branch, full-review cadence, and skip-implementation mode (`scripts/setup-rlcr-loop.sh:120-224`). It sources shared helpers (`scripts/setup-rlcr-loop.sh:27-29`, `scripts/setup-rlcr-loop.sh:232-234`), rejects concurrent RLCR/PR loops (`scripts/setup-rlcr-loop.sh:240-261`), normalizes positional versus explicit plan input (`scripts/setup-rlcr-loop.sh:263-270`), and enters a special no-plan skip-impl path when requested (`scripts/setup-rlcr-loop.sh:272-291`). It then validates git repository presence and at least one commit (`scripts/setup-rlcr-loop.sh:293-307`), validates plan path safety (`scripts/setup-rlcr-loop.sh:320-421`), validates plan tracking policy (`scripts/setup-rlcr-loop.sh:423-474`), validates plan content (`scripts/setup-rlcr-loop.sh:476-541`), checks Codex CLI availability (`scripts/setup-rlcr-loop.sh:543-549`), records current branch/model/effort safely (`scripts/setup-rlcr-loop.sh:551-587`), resolves and validates the base branch (`scripts/setup-rlcr-loop.sh:589-660`), creates loop files (`scripts/setup-rlcr-loop.sh:663-731`), builds goal tracker state (`scripts/setup-rlcr-loop.sh:733-856`), builds the initial prompt (`scripts/setup-rlcr-loop.sh:858-970`), then prints activation instructions and the initial prompt (`scripts/setup-rlcr-loop.sh:972-1067`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR` or current working directory, the plan file contents, current git repo, local/remote branch metadata, and Codex availability. Outputs are `LOOP_DIR="$PROJECT_ROOT/.humanize/rlcr/$TIMESTAMP"` (`scripts/setup-rlcr-loop.sh:667-673`), copied or placeholder `plan.md` (`scripts/setup-rlcr-loop.sh:675-697`), `state.md` with `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, and `started_at` (`scripts/setup-rlcr-loop.sh:706-726`), optional `.review-phase-started` in skip-impl mode (`scripts/setup-rlcr-loop.sh:728-731`), `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:737-856`), and `round-0-prompt.md` plus expected summary path (`scripts/setup-rlcr-loop.sh:862-970`). Normal mode moves from no active loop to implementation round 0. Skip-impl starts with `review_started: true` and review marker already present.
- gates_or_invariants: Hard gates include one active loop type only (`scripts/setup-rlcr-loop.sh:240-261`), git repo plus commit required (`scripts/setup-rlcr-loop.sh:297-307`), plan path must be relative, space-free, metacharacter-free, non-symlink, inside project, readable, and not inside a submodule (`scripts/setup-rlcr-loop.sh:320-421`), untracked/gitignored plan unless `--track-plan-file` is used (`scripts/setup-rlcr-loop.sh:447-471`), at least five plan lines and at least three non-comment content lines (`scripts/setup-rlcr-loop.sh:483-536`), Codex CLI required (`scripts/setup-rlcr-loop.sh:543-549`), YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:561-587`, `scripts/setup-rlcr-loop.sh:645-651`), and local base branch availability (`scripts/setup-rlcr-loop.sh:594-642`). Git operations use `run_with_timeout` and fail closed on timeout in several gates (`scripts/setup-rlcr-loop.sh:427-443`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` for `run_with_timeout`, `hooks/lib/loop-common.sh` for `find_active_loop` and `find_active_pr_loop`, git, codex, shell utilities (`grep`, `sed`, `awk`, `wc`, `date`, `cp`, `mkdir`). Called by the slash command represented in the help text (`scripts/setup-rlcr-loop.sh:50-115`) and tested by plan/setup/base-branch robustness tests. Runtime consumers include stop/read/write/bash validators and monitor code that read the generated `.humanize/rlcr/<timestamp>` state.
- edge_cases_or_failure_modes: Skip-impl without a plan creates a placeholder plan and ignores `--track-plan-file` with a warning (`scripts/setup-rlcr-loop.sh:272-282`, `scripts/setup-rlcr-loop.sh:675-695`). Remote-only base branches are rejected because Codex review requires a local ref (`scripts/setup-rlcr-loop.sh:600-605`). Plan markdown headings beginning with `#` are treated as comment lines for content-count purposes (`scripts/setup-rlcr-loop.sh:493-526`), so a heading-only file can fail content validation. Branch/model names with colon, hash, quotes, backticks, or newline are rejected to protect YAML state serialization (`scripts/setup-rlcr-loop.sh:561-587`). Final output installs a `PIPE` trap only after state creation so broken terminal output does not corrupt already-created loop state (`scripts/setup-rlcr-loop.sh:976-978`).
- validation_or_tests: Covered by `tests/test-plan-file-validation.sh`, `tests/robustness/test-path-validation-robustness.sh`, `tests/robustness/test-plan-file-robustness.sh`, `tests/robustness/test-base-branch-detection.sh`, and `tests/robustness/test-setup-scripts-robustness.sh`; those are referenced by file names in repo search and include direct invocations of this script. `tests/run-all-tests.sh:55-67` includes the relevant robustness suite entries.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook preflight that blocks loop exit when the transcript shows unfinished work. This is branch-critical because it verifies compatibility with both legacy `TodoWrite` and the new Task system (`TaskCreate`/`TaskUpdate`).
- algorithmic_behavior: The test creates a temp directory, writes synthetic transcript JSONL files, feeds hook input JSON containing `transcript_path` to the Python checker, and asserts exit codes/output content (`tests/test-todo-checker.sh:37-43`). It validates input handling: invalid hook JSON exits 2, empty input exits 0, missing transcript path exits 0, nonexistent transcript exits 0 (`tests/test-todo-checker.sh:52-98`). It validates legacy TodoWrite interpretation: completed todos pass, pending/in-progress todos fail, incomplete output names the task, latest TodoWrite supersedes earlier TodoWrite state, and supported transcript formats include assistant-message, direct tool_use, and type: message (`tests/test-todo-checker.sh:107-241`). It validates edge cases: missing status is incomplete, empty content still counts, Unicode content passes (`tests/test-todo-checker.sh:250-294`). It then validates TaskCreate/TaskUpdate: created tasks default pending, completed update clears the block, in-progress update remains incomplete, multiple tasks require all completed, orphan completed TaskUpdate is accepted, mixed legacy completed todo plus new pending task fails, output includes `Task #`, and direct tool_use TaskCreate/TaskUpdate works (`tests/test-todo-checker.sh:303-438`).
- inputs_outputs_state: Inputs are stdin hook JSON and JSONL transcript contents. The checker’s modeled state is latest legacy TodoWrite list plus a task map keyed by task ID/order. Outputs are checker exit codes and text. Test script output is colored PASS/FAIL lines and final process exit 0 only if `TESTS_FAILED` is zero (`tests/test-todo-checker.sh:440-458`). The implementation under test documents the same exit-code contract at `hooks/check-todos-from-transcript.py:7-10`, extracts three transcript shapes at `hooks/check-todos-from-transcript.py:21-58`, tracks latest TodoWrite and TaskCreate/TaskUpdate state at `hooks/check-todos-from-transcript.py:61-164`, and prints `INCOMPLETE_TODOS` with optional Task ID details at `hooks/check-todos-from-transcript.py:195-211`.
- gates_or_invariants: The core invariant is “status must equal `completed`”; anything else, including missing status and `in_progress`, is incomplete (`tests/test-todo-checker.sh:145-158`, `tests/test-todo-checker.sh:250-264`, `tests/test-todo-checker.sh:334-348`). Invalid hook input is a parse error and must be distinguishable via exit 2 (`tests/test-todo-checker.sh:52-62`). Missing transcript information is permissive rather than blocking (`tests/test-todo-checker.sh:64-98`). For legacy TodoWrite, only the most recent non-empty TodoWrite list is authoritative (`tests/test-todo-checker.sh:197-211`). For TaskCreate/TaskUpdate, TaskCreate defaults pending until TaskUpdate completes it (`tests/test-todo-checker.sh:303-332`).
- dependencies_and_callers: Depends on `python3`, `hooks/check-todos-from-transcript.py`, `mktemp`, `grep`, and POSIX shell behavior. Runtime caller is the stop hook’s quick pre-Codex check at `hooks/loop-codex-stop-hook.sh:339-350`; incomplete tasks block immediately via `prompt-template/block/incomplete-todos.md` rendering at `hooks/loop-codex-stop-hook.sh:372-390`. It is included in the aggregate test list at `tests/run-all-tests.sh:35-37`.
- edge_cases_or_failure_modes: The tests cover invalid JSONL lines being ignored (`tests/test-todo-checker.sh:180-195`), but they do not cover malformed tool input types such as non-list `todos`, explicit TaskCreate IDs, duplicate TaskUpdate IDs, or task deletion/cancellation semantics. Because the checker auto-assigns TaskCreate IDs by transcript order (`hooks/check-todos-from-transcript.py:80-115`), a real transcript format that emits explicit task IDs on creation would need matching implementation support if order-based IDs differ.
- validation_or_tests: The file is itself the validation harness. It exits successfully only when all 25 scripted assertions pass (`tests/test-todo-checker.sh:440-458`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-101 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR stop hook when the required round or finalize summary file is absent. It is an algorithmic gate because the loop cannot proceed to Codex review/finalize validation without a written work summary artifact.
- algorithmic_behavior: The template states that exit was attempted without a work summary, renders the required `{{SUMMARY_FILE}}`, lists required summary contents, and tells the agent it may try to exit again only after writing the summary (`prompt-template/block/work-summary-missing.md:1-16`). The stop hook selects `finalize-summary.md` in finalize phase or `round-N-summary.md` otherwise (`hooks/loop-codex-stop-hook.sh:610-615`), checks file existence (`hooks/loop-codex-stop-hook.sh:617`), renders this template with `SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:621-625`), and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:633-641`).
- inputs_outputs_state: Input is the resolved summary path. Output is a rendered block reason plus system message. State transition is “attempted exit” back to “blocked, continue current phase” until the summary file exists. The template does not mutate state itself; the stop hook enforces the gate.
- gates_or_invariants: Summary file existence is mandatory before expensive Codex review or finalize checks continue. The summary must include implemented work, files created/modified, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:10-14`). The stop hook varies the system message by normal versus finalize phase (`hooks/loop-codex-stop-hook.sh:627-631`).
- dependencies_and_callers: Called through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`. Sibling templates provide other block states, but this template specifically protects the summary artifact that later review prompts and completion decisions rely on. It coordinates with `scripts/setup-rlcr-loop.sh`, which tells the agent the exact first summary path during setup (`scripts/setup-rlcr-loop.sh:862`, `scripts/setup-rlcr-loop.sh:955-960`, `scripts/setup-rlcr-loop.sh:1054-1063`).
- edge_cases_or_failure_modes: If `SUMMARY_FILE` is computed incorrectly by state metadata, the agent will be told to write the wrong file, so the hook’s phase/current-round state is a dependency. The template checks existence only at this point; content quality is left to later Codex review and phase-specific gates.
- validation_or_tests: Template rendering is covered indirectly by stop-hook tests and template reference tests. The runtime call site includes a fallback if this template is missing (`hooks/loop-codex-stop-hook.sh:621-625`), but the repository has the concrete template present.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-131 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git status/state helpers in `scripts/humanize.sh`. These helpers feed monitor/status surfaces and safety decisions for loop operation by summarizing worktree changes and special git states.
- algorithmic_behavior: The test sources `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`, then creates isolated git repositories under a temp directory (`tests/robustness/test-git-operations-robustness.sh:15-21`, `tests/robustness/test-git-operations-robustness.sh:49-62`). It tests `humanize_parse_git_status`, whose contract is `modified|added|deleted|untracked|insertions|deletions` (`tests/robustness/test-git-operations-robustness.sh:32-46`), across clean state, branch name, untracked, modified, staged added, insertion counts, non-git directories, detached HEAD, simultaneous states, deleted files, empty repo, feature branch, rename, many untracked files, spaces in names, binary files, staged plus unstaged changes on the same file, and deletion counts (`tests/robustness/test-git-operations-robustness.sh:71-367`). It then tests `humanize_detect_git_state` across normal, detached, rebase, merge, shallow, non-git, and permission-error scenarios (`tests/robustness/test-git-operations-robustness.sh:369-500`).
- inputs_outputs_state: Inputs are synthetic repo mutations and simulated git metadata files/directories such as `.git/rebase-merge`, `.git/MERGE_HEAD`, and `.git/shallow`. Outputs are PASS/FAIL assertions and final `print_test_summary` exit status (`tests/robustness/test-git-operations-robustness.sh:502-507`). The production functions under test return special state strings from `humanize_detect_git_state` (`scripts/humanize.sh:114-162`) and pipe-delimited status counts from `humanize_parse_git_status` (`scripts/humanize.sh:164-210`).
- gates_or_invariants: `humanize_parse_git_status` must not crash outside a git repo and should return a recognizable “not a git repo” marker (`tests/robustness/test-git-operations-robustness.sh:168-178`, `scripts/humanize.sh:166-170`). Rename counts as modified by design (`tests/robustness/test-git-operations-robustness.sh:265-277`, `scripts/humanize.sh:187`). Simultaneous staged/unstaged changes must be counted at least once as modified (`tests/robustness/test-git-operations-robustness.sh:331-347`). Special git state detection prioritizes rebase, merge, shallow, then detached after basic repo/permission checks (`scripts/humanize.sh:119-161`).
- dependencies_and_callers: Depends on git, chmod, shell utilities, `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`. The production helpers are used by monitor/status functions inside `scripts/humanize.sh` and are also sanity-checked by hook input robustness tests. The test is included in the aggregate robustness suite at `tests/run-all-tests.sh:55-60`.
- edge_cases_or_failure_modes: Permission behavior can vary by platform, so the test accepts either `permission_error` or `not_a_repo` in the simulated unreadable `.git` case (`tests/robustness/test-git-operations-robustness.sh:478-496`). Empty repositories are only required to produce a non-empty result, not specific counts (`tests/robustness/test-git-operations-robustness.sh:233-245`). The diffstat parsing checks line insertions/deletions from `git diff --shortstat HEAD` with fallback to unstaged diff (`scripts/humanize.sh:197-205`), so unborn HEAD and binary-only diffs can have limited numeric signal.
- validation_or_tests: The file is itself the robustness validation and exits through shared summary logic (`tests/robustness/test-git-operations-robustness.sh:502-507`). It validates the production implementations directly by sourcing `scripts/humanize.sh`, not a local reimplementation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present; item IDs are intentionally not repeated here to preserve the exactly-once section-heading invariant.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`