# agent_16 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive inspection found a flat directory of 17 shell robustness suites, no nested child directories. The directory is the edge-case executable-spec layer for Humanize loop algorithms: state parsing, active session detection, hook validation, setup validation, path/content validation, PR-loop API handling, template rendering, timeout behavior, git status parsing, and cancel/security gates.
- algorithmic_behavior: Each child script creates isolated temp state, invokes production scripts/hooks/functions, asserts pass/fail outcomes through `tests/test-helpers.sh`, and ends with `print_test_summary`. Parent orchestration includes every robustness child in `tests/run-all-tests.sh:55-72`, so this directory is part of the full test frontier rather than an ad hoc/manual-only folder. Child roles: `test-state-file-robustness.sh` validates tolerant/strict YAML frontmatter parsing; `test-session-robustness.sh` validates newest-loop selection; `test-goal-tracker-robustness.sh` validates goal tracker metrics from `scripts/humanize.sh`; `test-path-validation-robustness.sh` and `test-plan-file-robustness.sh` validate setup plan path/content gates; `test-git-operations-robustness.sh` validates git state/status summarizers; `test-hook-input-robustness.sh` and `test-hook-system-robustness.sh` validate JSON hook inputs and write/edit/bash stop-hook behavior; `test-template-stress-robustness.sh` and `test-template-error-robustness.sh` validate template loader stress/error cases; `test-cancel-security-robustness.sh` validates authorized cancel moves and injection rejection; `test-timeout-robustness.sh` validates portable timeout semantics; `test-base-branch-detection.sh` validates base branch fallback order; `test-setup-scripts-robustness.sh` validates setup argument, git, mutual exclusion, symlink, and skip-impl gates; `test-concurrent-state-robustness.sh` validates race/concurrent read behavior; `test-state-transition-robustness.sh` validates loop lifecycle states; `test-pr-loop-api-robustness.sh` validates mocked GitHub API and PR-loop state handling.
- inputs_outputs_state: Inputs are local production paths under `scripts/`, `hooks/`, `prompt-template/`, temp git repos, mocked `codex`/`gh` binaries, JSON hook payloads, and synthetic `.humanize/rlcr` or `.humanize/pr-loop` session trees. Outputs are colored PASS/FAIL lines, aggregate summaries, and process exit status. State is intentionally local to `mktemp` directories, with cleanup traps inherited from helper patterns such as `tests/test-helpers.sh:86-89`.
- gates_or_invariants: The suite encodes fail-closed behavior for malformed state, missing required state fields, symlink traversal, shell metacharacter paths, active-loop mutual exclusion, unauthorized state/finalize/goal-tracker edits, bad JSON, untrusted cancel commands, timeout exit code `124`, and API failure fallback. It also asserts permissive invariants where intended: unknown tools pass through, monitor exits without crashing when sessions disappear, and rich/large valid plan/template inputs remain accepted.
- dependencies_and_callers: Called by `tests/run-all-tests.sh`, which captures each suite output, strips ANSI, extracts Passed/Failed counts, and fails globally if any suite exits nonzero or reports failures (`tests/run-all-tests.sh:80-159`). Child scripts depend on `tests/test-helpers.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, hook validators, PR polling/fetch scripts, and test fixtures/mocks where applicable.
- edge_cases_or_failure_modes: Covered cases include CRLF and binary/null bytes, very large files/templates, very long lines, missing/unreadable/deleted files, symlinks in files and parents, path injection characters, deep paths, rapid create/delete cycles, concurrent reads/writes, malformed/truncated YAML frontmatter, missing `review_started`/`base_branch`, detached/rebase/merge/shallow git states, no-git directories, permission errors, mocked rate limits/network failures, nested JSON, non-UTF8 input, and bot approval/timeout transitions.
- validation_or_tests: The directory itself is validation. It coordinates with sibling tests through the shared runner and helper API; robustness items are listed after standard suites at `tests/run-all-tests.sh:55-72`, and pass/fail accounting is shared via `tests/test-helpers.sh:30-78`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Executable fixture generator for PR-loop monitor state. It creates `.humanize/pr-loop/<timestamp>/state.md` trees with specific YAML-list bot states so monitor parsing/display logic can be tested without going through write validators.
- algorithmic_behavior: Parses `<test_dir>` and optional `<test_name>` (`tests/setup-monitor-test-env.sh:12-13`), requires `TEST_DIR` (`15-18`), dispatches on `yaml_list`, `configured`, or `empty` (`20-88`), writes deterministic frontmatter into a timestamped PR-loop state file, and echoes the test directory on success (`91`). The three fixture modes represent all configured bots active (`21-41`), only `codex` still active after partial approval (`43-62`), and no active bots/all approved (`64-82`).
- inputs_outputs_state: Input 1 is the target test directory; input 2 selects fixture shape. Output is a created `state.md` under `.humanize/pr-loop`, containing `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`. It mutates only the caller-provided test directory.
- gates_or_invariants: Fails with usage when no directory is supplied; fails with an “Unknown test name” message and enumerated valid modes for any unsupported fixture name (`84-87`). YAML list formatting is invariant: bot entries are stored as indented `- claude` / `- codex`, and the empty-active case keeps the `active_bots:` key with no list entries.
- dependencies_and_callers: Called by `tests/test-pr-loop-hooks.sh` monitor tests at `1553`, `1580`, and `1602`. Those tests then source `scripts/humanize.sh` and run `humanize monitor pr --once` to assert YAML-list parsing and configured/active bot display (`tests/test-pr-loop-hooks.sh:1555-1607`). The production parser reads `configured_bots` and `active_bots` lists using `sed` in `scripts/humanize.sh:1038-1059`, then displays configured and active bot status in `scripts/humanize.sh:1131-1136`.
- edge_cases_or_failure_modes: Default `TEST_NAME=default` is intentionally invalid unless the caller passes a real mode, so `test_dir` alone exits as an unknown fixture. The `empty` fixture tests the edge where `active_bots` is present but blank; production display currently maps that to “all approved” rather than a literal bot list (`scripts/humanize.sh:1133-1136`).
- validation_or_tests: Directly supports monitor tests `T-MONITOR-1`, `T-MONITOR-2`, and `T-MONITOR-3` in `tests/test-pr-loop-hooks.sh:1547-1621`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template for a Codex review failure gate. It is intended to tell the user that the Codex review produced no usable output and to surface the command/stdout/stderr debug files.
- algorithmic_behavior: Template content has placeholders for `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5-15`). It is loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:1218-1226` as the reason for a blocking JSON decision in the Codex review failure path (`1228-1236`).
- inputs_outputs_state: Inputs are render variables from the stop hook and stderr tail content from cache/log files. Output is Markdown included in a hook response reason; the hook state transition is “exit blocked, retry required” rather than modifying loop state (`hooks/loop-codex-stop-hook.sh:1172-1178`, `1228-1236`).
- gates_or_invariants: The gate is blocking: Codex review failure returns a JSON decision of `block` and exits hook processing successfully from the hook API perspective (`hooks/loop-codex-stop-hook.sh:1228-1236`). Template rendering is single-pass and preserves missing placeholders by design (`hooks/lib/template-loader.sh:7-13`, `116-121`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` and is called by `hooks/loop-codex-stop-hook.sh`. The same hook also has an implementation-phase inline `codex_failure_exit` path for nonzero exit, missing review result, and empty result (`hooks/loop-codex-stop-hook.sh:1284-1384`), but that inline path does not use this file.
- edge_cases_or_failure_modes: There is a placeholder mismatch in the loaded-template path: the hook passes `EXIT_CODE` and `CODEX_LOG_FILE` (`hooks/loop-codex-stop-hook.sh:1219-1226`), while this template expects `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE`. Because missing variables are preserved, those placeholders can remain literal in the rendered failure message if this path executes.
- validation_or_tests: General template-loader behavior is tested by robustness/template suites, but the inspected references did not show a dedicated assertion for this exact block template. The production behavior is therefore mostly covered indirectly by template rendering semantics and stop-hook failure-path tests.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Hook-block template enforcing the invariant that RLCR summary files must be written inside the active loop directory.
- algorithmic_behavior: Contains a short Markdown block with one render variable, `CORRECT_PATH` (`prompt-template/block/wrong-summary-location.md:1-5`). `hooks/loop-write-validator.sh` renders it when a write targets a summary file outside the `.humanize/rlcr` loop directory (`202-209`) and exits with code `2`, blocking the write.
- inputs_outputs_state: Input is the computed correct summary path, `ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-write-validator.sh:202-208`). Output is a stderr message consumed by the Claude hook protocol; no state file is changed.
- gates_or_invariants: The validator checks this location gate before extracting in-loop path components and before wrong-round/wrong-directory checks (`hooks/loop-write-validator.sh:198-263`). The invariant is narrow: summary writes outside the active loop are blocked and redirected to the current round summary path.
- dependencies_and_callers: Depends on active-loop discovery and strict state parsing earlier in `loop-write-validator.sh`, plus `load_and_render_safe` from the template loader. Related summary protections exist in bash/edit validators, but this specific template is wired to the write validator path shown above.
- edge_cases_or_failure_modes: The template only reports the correct path, not the attempted path or action, so diagnostics are concise but less contextual than other path templates. If `CORRECT_PATH` were not supplied, the loader would leave `{{CORRECT_PATH}}` literal because missing variables are preserved.
- validation_or_tests: Covered indirectly by hook validator suites that exercise write-path blocking behavior and by generic template-loader tests. No direct exact-template assertion for this specific file was found in the inspected test references.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-136 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for `scripts/setup-rlcr-loop.sh` plan file validation, focused on content sufficiency, size/encoding tolerance, file disappearance, and local line-count edge cases.
- algorithmic_behavior: Sets strict shell options, creates a temp test area, isolates `XDG_CACHE_HOME`, installs a mock `codex` in `PATH` (`tests/robustness/test-plan-file-robustness.sh:13-40`), initializes a git repo (`112-120`), and runs production setup through `test_plan_validation` (`48-106`). That helper removes prior `.humanize/rlcr` state, invokes `CLAUDE_PROJECT_DIR="$TEST_DIR" bash scripts/setup-rlcr-loop.sh <plan>`, captures output in a temp file, maps known validation errors to rejection, and treats “requires codex,” “must be gitignored,” successful activation, or created loop state as validation success (`56-96`). A local `count_content_lines` helper mirrors production comment/blank filtering for non-production assertions (`127-167`).
- inputs_outputs_state: Inputs are generated plan files in the temp repo. Outputs are PASS/FAIL counters and final exit status from `print_test_summary` (`555-556`). State transitions are limited to temp files, a mock git repo, and possible `.humanize/rlcr` loop creation that is cleared before each production validation attempt (`56-61`).
- gates_or_invariants: The production gates being specified are visible in `scripts/setup-rlcr-loop.sh`: relative-only paths, no spaces/metacharacters, no plan-file or parent-directory symlinks, file exists/readable, real path remains under project, not in submodule, git tracking policy, at least 5 total lines, and at least 3 nonblank/noncomment content lines (`scripts/setup-rlcr-loop.sh:321-541`). The test’s expected acceptance/rejection patterns align with those messages (`tests/robustness/test-plan-file-robustness.sh:64-83`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for result accounting (`108`), local `git`, mocked `codex`, and the production setup script. Included in the parent suite at `tests/run-all-tests.sh:63`.
- edge_cases_or_failure_modes: Production-backed cases include valid/rich/standard plans, exactly 3 content lines, 1MB+ large plan, mixed CRLF/LF endings, empty plan, comments-only plan, and deleted/missing plan (`176-364`, `525-549`). Local-only checks cover binary bytes, very long lines, special shell characters in content, whitespace-only files, nested/complex HTML comments, non-existent/unreadable files, symlink detection, directory instead of file, and null bytes (`366-523`); those latter checks validate shell/filesystem handling but mostly do not invoke production setup.
- validation_or_tests: The file is itself the test. It verifies both production behavior and helper-level assumptions, then exits with the aggregate summary status. A notable boundary is that symlink/directory/binary/null local checks are not all passed through `test_plan_validation`, so they are weaker as production regression coverage than the production-backed cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 evidence headings present; each assigned item appears once as a heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`