# agent_27 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-027 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Edit` validator for protected RLCR and PR-loop state surfaces. It is registered as the `Edit` hook in `hooks/hooks.json:24-30` and acts as a fail-closed path gate before Claude can mutate loop-managed files.
- algorithmic_behavior: The script reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits immediately for non-`Edit` tools (`hooks/loop-edit-validator.sh:24-32`). It blocks edits to RLCR round todos files unless the file is in a small active-loop allowlist (`hooks/loop-edit-validator.sh:38-45`), blocks all `round-*-prompt.md` writes (`hooks/loop-edit-validator.sh:48-51`), applies PR-loop protections before RLCR protections (`hooks/loop-edit-validator.sh:57-77`), then gates `.humanize/rlcr` files by active loop and current round (`hooks/loop-edit-validator.sh:83-110`). It blocks `finalize-state.md` before generic `state.md` because the generic matcher also matches finalize state (`hooks/loop-edit-validator.sh:117-125`), blocks RLCR `plan.md` backup edits (`hooks/loop-edit-validator.sh:131-138`), blocks `goal-tracker.md` after Round 0 (`hooks/loop-edit-validator.sh:145-149`), and blocks wrong-round summaries except allowlisted historical files (`hooks/loop-edit-validator.sh:155-179`).
- inputs_outputs_state: Inputs are hook JSON, the requested `file_path`, active loop discovery from `${CLAUDE_PROJECT_DIR:-$(pwd)}/.humanize/rlcr`, optional overridden `PROJECT_ROOT`, `LOOP_BASE_DIR`, and `LOOP_DIR`, plus `.humanize/rlcr/<session>/state.md` or `finalize-state.md`. Outputs are process exit codes and stderr messages: `0` means edit allowed, `2` means policy block with a rendered template message, and `1` is used for malformed state safety block (`hooks/loop-edit-validator.sh:105-109`). It does not mutate files; its state transition role is preventing unauthorized transitions of loop-owned artifacts, especially state files and post-Round-0 goal tracker state.
- gates_or_invariants: It preserves these invariants: round todos files are not normally edited directly (`hooks/loop-edit-validator.sh:38-45`); prompt files are read-only (`hooks/loop-edit-validator.sh:48-51`); PR-loop `state.md` and generated review files are read-only (`hooks/loop-edit-validator.sh:59-70`); `round-N-pr-resolve.md` must match current PR round (`hooks/loop-edit-validator.sh:72-76`); RLCR state files and finalize-state files cannot be edited (`hooks/loop-edit-validator.sh:117-125`); RLCR `plan.md` backup cannot be edited (`hooks/loop-edit-validator.sh:131-138`); `goal-tracker.md` is mutable by Edit only in Round 0 (`hooks/loop-edit-validator.sh:145-149`); summaries must target the active loop directory and current round except allowlisted historical files (`hooks/loop-edit-validator.sh:155-179`). The shared allowlist is exactly `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in `hooks/lib/loop-common.sh:423-441`.
- dependencies_and_callers: It sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16-18`) for `to_lower`, `find_active_loop`, file-type predicates, strict state parsing, allowlist checks, PR-loop checks, and block-message rendering. Key dependency implementations include strict required state fields and numeric validation in `hooks/lib/loop-common.sh:255-330`, round-file detection in `hooks/lib/loop-common.sh:399-418`, loop-dir detection in `hooks/lib/loop-common.sh:712-716`, PR-loop file detection/round validation in `hooks/lib/loop-common.sh:783-887`, and goal tracker blocked message rendering in `hooks/lib/loop-common.sh:1098-1110`. It depends on `jq`, `sed`, `grep`, `basename`, and template loading via `hooks/lib/template-loader.sh` through `loop-common.sh`.
- edge_cases_or_failure_modes: Invalid JSON or missing `.tool_input.file_path` is not prevalidated here the way `loop-write-validator.sh` does; because `set -euo pipefail` is active and `jq` is used directly, malformed JSON can abort nonzero before a custom message. Robustness tests explicitly accept graceful non-crash behavior for malformed and missing file paths (`tests/robustness/test-hook-system-robustness.sh:80-107`). If there is no active RLCR loop, most RLCR-specific edits are allowed through (`hooks/loop-edit-validator.sh:91-97`), so enforcement depends on active-loop discovery. Path checks mostly use substring/pattern matching rather than canonical path resolution; a traversal path ending in `state.md` is still blocked by basename/pattern behavior, which is covered by robustness tests (`tests/robustness/test-hook-system-robustness.sh:124-155`). The PR-loop current-round parser in `validate_pr_resolve_round` reads current round only from frontmatter-delimited state (`hooks/lib/loop-common.sh:861-863`), so malformed PR state may default to round `0` for that specific validation.
- validation_or_tests: Direct coverage includes allowlist behavior for Edit on todos and summaries in `tests/test-allowlist-validators.sh:187-228`; finalize-state edit blocking in `tests/test-finalize-phase.sh:369-379`; RLCR `plan.md` backup edit blocking in `tests/test-plan-file-hooks.sh:240-254`; robustness cases for valid Edit JSON, malformed JSON, missing file_path, outside `.humanize` pass-through, and traversal-to-state blocking in `tests/robustness/test-hook-system-robustness.sh:37-155`; PR-loop wrong-round `pr-resolve` edit blocking in `tests/test-pr-loop-hooks.sh:1499-1527`; template reference/fallback hygiene covering the validator in `tests/test-template-references.sh:56-64` and `tests/test-template-references.sh:174-185`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Shell executable specification for real monitor behavior in RLCR and PR-loop sessions. It exercises production `scripts/humanize.sh` monitor functions rather than stubs, with temporary loop directories, fake cache homes, terminal-command shims, deletion events, and SIGINT events.
- algorithmic_behavior: The test creates isolated `/tmp/test-monitor-e2e-real-$$` fixtures, increments pass/fail counters, and deletes the fixture at exit (`tests/test-monitor-e2e-real.sh:25-56`). It runs six end-to-end monitor scenarios. Test 1 starts real `_humanize_monitor_codex` under bash, deletes `.humanize/rlcr`, waits up to 10 seconds, and asserts clean stop messages, no glob errors, terminal cleanup evidence, source scroll-region reset, and `EXIT_CODE:0` (`tests/test-monitor-e2e-real.sh:58-231`). Test 2 repeats RLCR deletion under zsh when available (`tests/test-monitor-e2e-real.sh:233-373`). Test 3 sends SIGINT to the bash RLCR monitor and accepts either direct SIGINT exit or fallback termination while still asserting cleanup/no glob errors (`tests/test-monitor-e2e-real.sh:375-549`). Test 4 repeats SIGINT under zsh (`tests/test-monitor-e2e-real.sh:551-683`). Test 5 runs real PR monitor in `--once` mode while deleting the PR-loop session and checks graceful or clean exit/no glob errors (`tests/test-monitor-e2e-real.sh:685-824`). Test 6 runs continuous PR monitor without `--once`, sends SIGINT, and checks it exits or at least ran with clean stop evidence and no glob errors (`tests/test-monitor-e2e-real.sh:826-990`). Summary exits `0` only when all counted failures are zero (`tests/test-monitor-e2e-real.sh:992-1017`).
- inputs_outputs_state: Inputs are generated test project trees under `/tmp`, `.humanize/rlcr/<timestamp>/state.md`, `.humanize/rlcr/<timestamp>/goal-tracker.md`, `.humanize/pr-loop/<timestamp>/state.md`, `.humanize/pr-loop/<timestamp>/goal-tracker.md`, fake `$HOME`, fake `$XDG_CACHE_HOME`, and cached log files such as `round-1-codex-run.log`. The test runners source `scripts/humanize.sh` and call `_humanize_monitor_codex` or `humanize monitor pr` (`tests/test-monitor-e2e-real.sh:148-155`, `tests/test-monitor-e2e-real.sh:776-795`, `tests/test-monitor-e2e-real.sh:917-925`). Outputs are captured stdout/stderr files, `PASS`/`FAIL` counters, process liveness checks, and final script exit code. State transitions simulated include active monitor -> loop directory deleted -> graceful stop, active monitor -> SIGINT -> cleanup, PR `--once` display -> clean exit, and continuous PR monitor -> interrupted exit.
- gates_or_invariants: The test asserts monitors must not emit zsh/bash glob failures such as `no matches found` or `bad pattern` after directories disappear (`tests/test-monitor-e2e-real.sh:204-209`, `tests/test-monitor-e2e-real.sh:362-365`, `tests/test-monitor-e2e-real.sh:819-824`, `tests/test-monitor-e2e-real.sh:985-990`). RLCR deletion must print `Monitoring stopped:` and mention the directory no longer exists (`tests/test-monitor-e2e-real.sh:191-202`). RLCR cleanup must print `Stopped monitoring` and the source must contain the scroll-region reset escape (`tests/test-monitor-e2e-real.sh:211-224`). Graceful RLCR deletion must exit `0` (`tests/test-monitor-e2e-real.sh:226-231`). SIGINT paths must show cleanup/interrupt/signal/clean-exit evidence and no glob errors (`tests/test-monitor-e2e-real.sh:532-549`, `tests/test-monitor-e2e-real.sh:672-682`). PR monitor paths must cleanly exit or show monitor activity without glob errors (`tests/test-monitor-e2e-real.sh:807-824`, `tests/test-monitor-e2e-real.sh:973-990`).
- dependencies_and_callers: It depends on `scripts/humanize.sh` real monitor functions, especially `_humanize_monitor_codex` starting at `scripts/humanize.sh:219`, `humanize monitor` dispatch at `scripts/humanize.sh:883-924`, and `_humanize_monitor_pr` starting at `scripts/humanize.sh:931`. It also depends on shared monitor helpers in `scripts/lib/monitor-common.sh`: safe `find`-based latest-session discovery avoids zsh glob failures (`scripts/lib/monitor-common.sh:37-63`), and terminal restore resets scroll region (`scripts/lib/monitor-common.sh:84-92`). RLCR monitor deletion handling is implemented in `scripts/humanize.sh:620-626`, `scripts/humanize.sh:662-666`, and `scripts/humanize.sh:755-759`; cleanup restores terminal and prints `Stopped monitoring.` in `scripts/humanize.sh:522-560`, then graceful stop prints the reason in `scripts/humanize.sh:563-576`. PR monitor cleanup is in `scripts/humanize.sh:1174-1213`; PR continuous loop is in `scripts/humanize.sh:1295-1364`.
- edge_cases_or_failure_modes: The test is intentionally race-prone and mitigates with bounded polling loops, sleeps, and force-kill fallbacks. It accepts SIGTERM fallback as pass for bash SIGINT complexity (`tests/test-monitor-e2e-real.sh:505-519`) and for PR monitor no-`--once` SIGINT (`tests/test-monitor-e2e-real.sh:951-965`), so it validates practical cleanup rather than exact signal semantics. zsh-dependent scenarios are skipped if `zsh` is unavailable (`tests/test-monitor-e2e-real.sh:240-242`, `tests/test-monitor-e2e-real.sh:558-560`). Terminal behavior is shimmed, so it verifies command paths and cleanup calls rather than a real terminal. PR `--once` test is weaker than continuous monitor deletion because it starts a deletion subshell and then runs one status iteration (`tests/test-monitor-e2e-real.sh:784-799`); clean exit is accepted even if deletion timing does not produce a specific deletion message.
- validation_or_tests: This file is itself validation. Its assertions map directly to production fixes around `find` instead of shell globs in RLCR log discovery (`scripts/humanize.sh:280-339`) and PR latest-file discovery (`scripts/humanize.sh:975-1008`), terminal scroll-region reset (`scripts/humanize.sh:522-528`), RLCR graceful-stop state transition (`scripts/humanize.sh:563-576`), and PR cleanup traps (`scripts/humanize.sh:1202-1213`). It also validates final aggregate pass/fail accounting and exits nonzero on any failure (`tests/test-monitor-e2e-real.sh:992-1017`).
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-087 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the Round 0 RLCR stop-hook gate when `goal-tracker.md` still contains placeholders in required sections. It converts a structural validation failure into user-facing repair instructions.
- algorithmic_behavior: The template renders a “Goal Tracker Not Initialized” block with two variables: `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}` (`prompt-template/block/goal-tracker-not-initialized.md:1-6`). It then instructs the actor to read the tracker, replace placeholder text with actual Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to ACs, then write the updated goal tracker (`prompt-template/block/goal-tracker-not-initialized.md:8-14`). It closes with the state-machine constraint that the immutable section can only be set in Round 0 and becomes read-only afterward (`prompt-template/block/goal-tracker-not-initialized.md:16-18`).
- inputs_outputs_state: Inputs are rendered values from the stop hook: `GOAL_TRACKER_FILE=$LOOP_DIR/goal-tracker.md` and a multiline `MISSING_ITEMS` list assembled from placeholder checks (`hooks/loop-codex-stop-hook.sh:648-708`). Output is the `reason` field inside a JSON block decision from the stop hook (`hooks/loop-codex-stop-hook.sh:710-718`). The intended state transition is Round 0 blocked exit -> user initializes goal tracker -> user may attempt exit again, after which Round 0 can progress.
- gates_or_invariants: This template enforces the Round 0 initialization invariant for the goal tracker’s immutable content. The stop hook only applies it when not in Finalize Phase, not in review/skip-impl mode, current round is `0`, and the tracker file exists (`hooks/loop-codex-stop-hook.sh:650-653`). The gate checks placeholders in `### Ultimate Goal`, `### Acceptance Criteria`, and `#### Active Tasks` sections using section-specific extraction to avoid false positives elsewhere (`hooks/loop-codex-stop-hook.sh:654-684`). Missing items are reported per section (`hooks/loop-codex-stop-hook.sh:686-699`). The template’s line 16 is the contract that explains why this block is hard: after Round 0, `goal-tracker.md` is protected by write/edit/bash validators.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` with fallback text and variables (`hooks/loop-codex-stop-hook.sh:701-708`). Template discovery/reference integrity is checked by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and verifies referenced template files exist (`tests/test-template-references.sh:56-111`). The later enforcement that the immutable tracker cannot be edited after Round 0 is implemented in `hooks/loop-edit-validator.sh:145-149`, `hooks/loop-write-validator.sh:188-196`, and the bash-validator goal-tracker modification path referenced in `hooks/lib/loop-common.sh:492-506` and `hooks/lib/loop-common.sh:1098-1110`.
- edge_cases_or_failure_modes: The template is only rendered if the file exists; missing `goal-tracker.md` is not handled by this specific block. Placeholder detection looks for the generic pattern `[To be [a-z]` within named sections, so unusual placeholder wording may pass and literal text matching that pattern in required sections may block. If template loading fails, the stop hook has a short fallback at `hooks/loop-codex-stop-hook.sh:701-705`, so the gate remains functional but with less detailed instructions. Since this is a prompt template, it cannot enforce edits by itself; enforcement comes from the stop hook and validators.
- validation_or_tests: Template existence/reference validation is covered by `tests/test-template-references.sh:74-111` and the “all templates referenced” scan in `tests/test-template-references.sh:119-142`. The behavior source is the stop-hook placeholder gate in `hooks/loop-codex-stop-hook.sh:653-719`. Related post-Round-0 immutability is covered by validator tests such as edit/write goal-tracker and state protections in `tests/robustness/test-hook-system-robustness.sh:53-78` and broader hook suites.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-117 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section that assigns responsibility for post-Round-0 goal-tracker mutations to Codex/the reviewer lane rather than Claude. It is part of the RLCR state machine’s mutable-vs-immutable tracker contract.
- algorithmic_behavior: The template tells Codex that Claude cannot directly modify `goal-tracker.md` after Round 0 and that any “Goal Tracker Update Request” in Claude’s summary must be evaluated (`prompt-template/codex/goal-tracker-update-section.md:1-5`). If approved, Codex must update `@{{GOAL_TRACKER_FILE}}` by moving tasks between Active/Completed/Deferred sections, appending Plan Evolution Log entries with round number and justification, adding Open Issues, and never modifying the immutable section (`prompt-template/codex/goal-tracker-update-section.md:6-10`). If rejected, Codex must explain why in review (`prompt-template/codex/goal-tracker-update-section.md:11`). It enumerates common request categories: task completion, new issues, plan changes, and strongly justified deferrals (`prompt-template/codex/goal-tracker-update-section.md:13-17`).
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE`, rendered by the stop hook when composing Codex review prompts (`hooks/loop-codex-stop-hook.sh:768-772`). Output is prompt text injected into regular or full-alignment review templates as `{{GOAL_TRACKER_UPDATE_SECTION}}`, with those review templates referencing the injected section in `prompt-template/codex/regular-review.md:47` and `prompt-template/codex/full-alignment-review.md:57`. The state transition contract is Claude summary request -> Codex evaluates -> approved mutable tracker update or rejected request with explanation. It allows mutable tracker evolution while preserving immutable Ultimate Goal and Acceptance Criteria.
- gates_or_invariants: The key invariant is on line 10: never modify the immutable section. This pairs with the edit/write/bash validators that block Claude from editing `goal-tracker.md` after Round 0, e.g. `hooks/loop-edit-validator.sh:145-149` and `hooks/loop-write-validator.sh:192-196`. The template also gates deferrals by requiring strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`). It preserves separation of duties: Claude may request tracker updates in summaries, while Codex/the stop-hook review path decides and applies them.
- dependencies_and_callers: The main caller is `hooks/loop-codex-stop-hook.sh`, which creates `GOAL_TRACKER_UPDATE_SECTION` via `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:768-772`) and passes it into generated review prompts along with `GOAL_TRACKER_FILE` (`hooks/loop-codex-stop-hook.sh:823-842`). The section is embedded by `prompt-template/codex/regular-review.md` and `prompt-template/codex/full-alignment-review.md`, found by repository search at `prompt-template/codex/regular-review.md:47` and `prompt-template/codex/full-alignment-review.md:57`. It depends on template-loader variable substitution and on the stop hook’s review-prompt generation.
- edge_cases_or_failure_modes: The template is advisory prompt text rather than code; correctness depends on Codex following instructions and the stop hook actually including the section. If a summary contains unsupported or unjustified updates, the expected behavior is rejection with explanation, but enforcement is review-quality based. It assumes the goal tracker has a known section structure with Active, Completed, Deferred, Plan Evolution Log, and Open Issues; malformed trackers can make the requested edits ambiguous. If the template cannot load, the stop hook fallback only says to apply requested changes to the tracker (`hooks/loop-codex-stop-hook.sh:768-771`), losing the more explicit invariant language.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-540` renders this exact template with `GOAL_TRACKER_FILE=.humanize/rlcr/20240101/goal-tracker.md` and asserts the rendered output includes “Goal Tracker Update Requests” and the substituted path. Template reference scanning covers the stop-hook caller in `tests/test-template-references.sh:56-111`. Behavioral coupling is covered by the stop-hook prompt generation around `hooks/loop-codex-stop-hook.sh:768-842` and by validators that prevent Claude-side post-Round-0 tracker edits.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique assigned item sections present above, each with one `cursor: [_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`