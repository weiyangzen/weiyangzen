# agent_25 change-todos-to-tasks 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `0790d28514ab48bec2668f4ec069592872fed586`

## Item Evidence

### CHANGE_TODOS_TO_TASKS-HZ-025 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash hook enforcing RLCR and PR-loop state-machine boundaries. It is registered for Claude Bash tool invocations by `hooks/hooks.json:43-48`, reads hook JSON from stdin, and blocks shell commands that would mutate protected loop artifacts or bypass Write/Edit validators.
- algorithmic_behavior: The script first loads shared hook functions from `hooks/lib/loop-common.sh` at `hooks/loop-bash-validator.sh:16-18`, validates stdin as JSON at `hooks/loop-bash-validator.sh:24-35`, exits early for non-Bash tools at `hooks/loop-bash-validator.sh:36-40`, and requires `tool_input.command` at `hooks/loop-bash-validator.sh:42-48`. It discovers active RLCR and PR loops under `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/...` at `hooks/loop-bash-validator.sh:54-66`; if neither exists, it allows the command. In an active RLCR loop, it selects `state.md` or `finalize-state.md`, parses it strictly, and fails closed on malformed state at `hooks/loop-bash-validator.sh:74-86`. It then applies blocking gates: disallow `git push` unless `push_every_round` is true at `hooks/loop-bash-validator.sh:89-106`; disallow adding `.humanize` to git at `hooks/loop-bash-validator.sh:109-118`; disallow shell writes/moves/removals to state, finalize-state, plan backup, goal tracker, prompt, summary, and todo files at `hooks/loop-bash-validator.sh:121-393`; and apply PR-loop state/read-only file protection at `hooks/loop-bash-validator.sh:397-443`.
- inputs_outputs_state: Input is a Claude hook JSON object with `tool_name` and `tool_input.command`; JSON validation and required-field extraction are delegated to `validate_hook_input` and `require_tool_input_field` in `hooks/lib/loop-common.sh:59-114`. Output is process exit status plus optional stderr block message. Exit `0` means allow; exit `1` means malformed hook/state input; exit `2` means policy-blocked command, usually with a rendered template message. It reads loop state from `.humanize/rlcr/<latest>/state.md` or `finalize-state.md` and `.humanize/pr-loop/<latest>/state.md`, but does not itself mutate loop state. State transitions are guarded indirectly: it permits only a narrowly authorized cancel rename when `.cancel-requested` exists via `is_cancel_authorized`, otherwise blocks state-file movement or mutation.
- gates_or_invariants: Hook input must be valid JSON, UTF-8, non-null-byte, and not deeper than depth 30 (`hooks/loop-bash-validator.sh:26-35`; shared checks in `hooks/lib/loop-common.sh:59-133`). RLCR state must have valid frontmatter and required fields, including numeric rounds and boolean `review_started`, enforced by `parse_state_file_strict` in `hooks/lib/loop-common.sh:255-330`. Protected file detection uses `command_modifies_file`, which matches redirection, `tee`, in-place editors, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns (`hooks/lib/loop-common.sh:1068-1096`). State-file source-copy bypasses get extra handling: the validator splits command chains into segments, preserves redirection constructs with placeholders, strips leading redirections, and detects `mv|cp ... state.md` as a source at `hooks/loop-bash-validator.sh:169-304`. Shell wrapper bypasses using `sh -c` or `bash -c` are blocked when payload contains `mv` or `cp` of state files at `hooks/loop-bash-validator.sh:306-328`.
- dependencies_and_callers: Called by Claude hook configuration at `hooks/hooks.json:43-48`. Depends on `jq`, `sed`, `grep`, `awk` through `loop-common.sh` and local logic. Depends on template rendering through `hooks/lib/template-loader.sh`, especially `load_and_render_safe` fallback behavior at `hooks/lib/template-loader.sh:167-203`. Uses shared message helpers such as `goal_tracker_bash_blocked_message` at `hooks/lib/loop-common.sh:492-501`, PR-loop messages at `hooks/lib/loop-common.sh:889-906`, `.humanize` git-add protection at `hooks/lib/loop-common.sh:930-1066`, active-loop discovery at `hooks/lib/loop-common.sh:156-174` and `hooks/lib/loop-common.sh:795-814`, and cancel authorization at `hooks/lib/loop-common.sh:533-620` plus continuation beyond that range.
- edge_cases_or_failure_modes: Fail-closed behavior on malformed RLCR state can block otherwise benign Bash commands during an active loop (`hooks/loop-bash-validator.sh:81-85`). Command matching is regex-based and lowercases commands, so it catches common bypasses but may be sensitive to shell syntax not represented in the regex set. The state-source guard explicitly handles chained operators, shell wrappers, redirection-before-command cases, quoted/ANSI-C/escaped redirection targets, and `finalize-state.md` before `state.md` to avoid suffix false matches (`hooks/loop-bash-validator.sh:132-178`, `hooks/loop-bash-validator.sh:208-304`). PR loop protection has a broad bare `state.md` block when a PR loop is active (`hooks/loop-bash-validator.sh:411-415`), which protects relative path bypasses but could block unrelated files named `state.md` during an active PR loop. Git-add protection relies partly on current working directory for `.humanize` existence and `git check-ignore` when assessing broad adds (`hooks/lib/loop-common.sh:1005-1022`).
- validation_or_tests: `tests/robustness/test-hook-input-robustness.sh:59-69` exercises valid Bash hook JSON handling, while `tests/robustness/test-hook-input-robustness.sh:71-115` covers invalid JSON and missing fields. `tests/test-bash-validator-patterns.sh:36-62` defines assertions around `command_modifies_file`; cases cover redirection, `tee`, in-place editors, `mv/cp/rm`, `dd`, `truncate`, and false positives at `tests/test-bash-validator-patterns.sh:69-140`. Direct validator invocations appear across `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, `tests/test-cancel-signal-file.sh`, and PR-loop tests per repository search. Template reference validation includes `hooks/loop-bash-validator.sh` in the script scan list at `tests/test-template-references.sh:56-64`.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-055 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared test harness utility for shell-based test scripts. It is not part of RLCR runtime behavior, but it supports executable validation of the hooks and state-machine helpers.
- algorithmic_behavior: Defines color constants at `tests/test-helpers.sh:13-16`, global counters at `tests/test-helpers.sh:22-24`, and three result-recording functions: `pass` increments passed count and prints a green label at `tests/test-helpers.sh:30-33`; `fail` prints expected/got details when supplied and increments failed count at `tests/test-helpers.sh:35-44`; `skip` prints an optional reason and increments skipped count at `tests/test-helpers.sh:46-52`. `print_test_summary` renders aggregate counts, includes skipped count only when nonzero, and returns success only when `TESTS_FAILED` is zero at `tests/test-helpers.sh:58-78`. `setup_test_dir` creates a temporary directory and installs an EXIT trap for cleanup at `tests/test-helpers.sh:84-89`. `init_test_git_repo` creates a minimal git repository, configures identity/GPG behavior, commits an initial file, and restores the previous directory at `tests/test-helpers.sh:91-105`.
- inputs_outputs_state: Inputs are function arguments: message text for `pass/fail/skip`, optional summary title for `print_test_summary`, a target directory for `init_test_git_repo`, and no explicit argument for `setup_test_dir`. Outputs are colored stdout lines, return code from `print_test_summary`, a `TEST_DIR` variable set by `setup_test_dir`, and a throwaway git repo created under the caller-provided path. State consists of process-global counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED`.
- gates_or_invariants: The only hard gate is `print_test_summary`: any failed count makes it return `1`, otherwise it returns `0` (`tests/test-helpers.sh:71-77`). The git helper assumes `git` is available and that `cd "$dir"` plus later `cd -` succeeds. `setup_test_dir` installs a single EXIT trap, so later traps in caller tests can override it unless composed carefully.
- dependencies_and_callers: Used by many robustness and PR-loop tests. Repository search shows sourcing from paths such as `tests/robustness/test-hook-input-robustness.sh:17`, `tests/robustness/test-state-transition-robustness.sh:17`, `tests/robustness/test-cancel-security-robustness.sh:18`, `tests/test-pr-loop.sh:19`, and `tests/test-pr-loop-lib.sh:20`. Depends on shell builtins, `mktemp`, `rm`, and `git`.
- edge_cases_or_failure_modes: Counter variables are global and mutable by caller scripts. The EXIT trap is simple and unquoted in the trap string (`tests/test-helpers.sh:87-88`), though `mktemp -d` normally produces safe paths. `init_test_git_repo` changes the process working directory and relies on `cd -` to restore it; with `set -e`, failures in git setup would abort the caller. ANSI color output uses `echo -e`, which can vary across shells, but the shebang targets Bash.
- validation_or_tests: This file is validation infrastructure rather than a test target. Its behavior is indirectly exercised whenever tests source it and call `print_test_summary` as their final gate. Example callers include robustness tests listed above and `tests/test-pr-loop.sh:19`.
- skip_candidate: `yes: support harness only; it is not a core algorithm implementation, but it is relevant as shared executable-test infrastructure for the hook/state-machine behavior.`

### CHANGE_TODOS_TO_TASKS-HZ-085 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template used when a Bash command attempts to modify `goal-tracker.md` during RLCR round 0. It is part of the guardrail surface that redirects the actor from shell writes to validated Write/Edit tooling.
- algorithmic_behavior: The template content is short and declarative: title at `prompt-template/block/goal-tracker-bash-write.md:1`, prohibition at `prompt-template/block/goal-tracker-bash-write.md:3`, correct-path instruction with `{{CORRECT_PATH}}` placeholder at `prompt-template/block/goal-tracker-bash-write.md:5`, and rationale that Bash bypasses validation hooks at `prompt-template/block/goal-tracker-bash-write.md:7-8`. The runtime caller is `goal_tracker_bash_blocked_message`, which renders this file through `load_and_render_safe` and supplies `CORRECT_PATH` at `hooks/lib/loop-common.sh:492-501`. `loop-bash-validator.sh` invokes that helper only when `command_modifies_file` detects `goal-tracker.md` and `CURRENT_ROUND` equals 0 at `hooks/loop-bash-validator.sh:343-357`.
- inputs_outputs_state: Input is the `CORRECT_PATH` variable from the active loop directory (`hooks/loop-bash-validator.sh:349-353`). Output is rendered markdown to stderr via the validator helper path. It does not read or write state directly; it shapes the human/agent recovery path after the Bash gate blocks a command.
- gates_or_invariants: The template assumes a single placeholder named `CORRECT_PATH`. Rendering uses the single-pass template engine documented at `hooks/lib/template-loader.sh:7-13` and implemented at `hooks/lib/template-loader.sh:50-132`, so placeholder-like text inside variable values is not recursively expanded. `load_and_render_safe` falls back to an inline message if the template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`), preserving the block gate even if the template surface is damaged.
- dependencies_and_callers: Directly referenced by `hooks/lib/loop-common.sh:500`. Its existence is validated in `tests/test-template-references.sh:152-159`, which includes `block/goal-tracker-bash-write.md` among common templates, and by the broader template scan over hook scripts at `tests/test-template-references.sh:56-110`.
- edge_cases_or_failure_modes: If `CORRECT_PATH` is not supplied, the literal placeholder remains because the renderer keeps missing variables unchanged (`hooks/lib/template-loader.sh:119-122`). If the template is missing or empty, callers still emit fallback text due to `load_and_render_safe`. The message is specific to Bash-based edits; non-Bash Write/Edit paths are governed by separate validators.
- validation_or_tests: `tests/test-template-references.sh:152-167` checks this template exists. Bash goal-tracker modification behavior is covered by the validator tests around `command_modifies_file` patterns in `tests/test-bash-validator-patterns.sh:69-140` and by direct `loop-bash-validator.sh` invocations found in hook tests.
- skip_candidate: `no`

### CHANGE_TODOS_TO_TASKS-HZ-115 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit prompt template documenting the Codex review phase of the RLCR loop. It encodes the review gate contract: run automated diff review, scan for severity markers, route issues back for remediation, or transition to Finalize Phase when no issues are detected.
- algorithmic_behavior: The template names the phase and round with `{{REVIEW_ROUND}}` at `prompt-template/codex/code-review-phase.md:1`, records that `codex review` does not consume prompt input at `prompt-template/codex/code-review-phase.md:3-4`, stores review configuration placeholders for base branch, round, and timestamp at `prompt-template/codex/code-review-phase.md:6-10`, and states the transition algorithm at `prompt-template/codex/code-review-phase.md:12-17`: run `codex review --base {{BASE_BRANCH}}`, scan for `[P0-9]` severity markers, return a fix prompt if issues are found, or transition to Finalize Phase otherwise. It also documents expected issue format at `prompt-template/codex/code-review-phase.md:19-28` and generated artifacts at `prompt-template/codex/code-review-phase.md:30-36`.
- inputs_outputs_state: Inputs are template variables supplied by `run_codex_review_phase` in `hooks/loop-codex-stop-hook.sh:949-955`, including `REVIEW_ROUND`, `BASE_BRANCH`, `BASE_COMMIT`, `REVIEW_BASE`, `REVIEW_BASE_TYPE`, and `TIMESTAMP`. The current template uses only `REVIEW_ROUND`, `BASE_BRANCH`, and `TIMESTAMP`; extra supplied variables are harmless. Output is `round-<n>-review-prompt.md` in the active loop directory, an audit artifact created before the actual `codex review` command is written and executed (`hooks/loop-codex-stop-hook.sh:931-979`). It does not itself change loop state, but documents the review gate that controls whether the loop continues or enters finalization.
- gates_or_invariants: The operational gate is implemented around the template by `hooks/loop-codex-stop-hook.sh`: review base prefers captured `BASE_COMMIT` over moving `BASE_BRANCH` at `hooks/loop-codex-stop-hook.sh:922-929`; the command file records `codex review --base $review_base` at `hooks/loop-codex-stop-hook.sh:959-969`; the command runs from `PROJECT_ROOT` with timeout at `hooks/loop-codex-stop-hook.sh:974-979`. Severity detection is implemented in `detect_review_issues`, which scans the review log for `[P<digit>]` markers within the first 10 characters of a line and extracts from the first issue onward (`hooks/lib/loop-common.sh:332-380`). Template rendering uses the same single-pass/fallback-safe loader in `hooks/lib/template-loader.sh:50-203`.
- dependencies_and_callers: Directly loaded by `hooks/loop-codex-stop-hook.sh:949`. Depends on the Codex CLI review command, cache/log paths in the stop hook, and shared `detect_review_issues` logic. Template reference scanning includes `hooks/loop-codex-stop-hook.sh` in `tests/test-template-references.sh:56-64`, and extracts/validates `load_and_render_safe` references at `tests/test-template-references.sh:83-110`.
- edge_cases_or_failure_modes: The template says `--base {{BASE_BRANCH}}`, while the caller may actually use `review_base` derived from `BASE_COMMIT` (`hooks/loop-codex-stop-hook.sh:922-929`, `hooks/loop-codex-stop-hook.sh:968-979`). Because the file is an audit document rather than the executable command source, this mismatch is documentation drift rather than direct runtime failure; the command artifact records the actual base. The template lists expected severity markers, but actual pass/fail behavior depends on log formatting matching `detect_review_issues` line-prefix scanning. If the template is missing, `load_and_render_safe` uses the fallback audit prompt in `hooks/loop-codex-stop-hook.sh:937-948`.
- validation_or_tests: Template existence is covered by the template reference scan in `tests/test-template-references.sh:56-110`. Review issue parsing is implemented and documented in `hooks/lib/loop-common.sh:332-380`; tests for template loader fallback behavior appear in `tests/test-template-loader.sh` and template robustness suites per repository search.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections above; each assigned item is represented once in Item Evidence.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`