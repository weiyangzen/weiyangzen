# agent_26 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-026 `file` `tests/test-plan-file-handling.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the branch’s plan-file protection algorithm in the RLCR loop. It verifies that setup, prompt validation, bash pre-commit validation, stop-hook post-commit validation, templates, README-visible behavior, and shared loop helpers all preserve the intended plan-file state machine. The file explicitly scopes itself to `--commit-plan-file` parsing, backup creation, state fields, git-ignore validation, and git-status filtering at `tests/test-plan-file-handling.sh:3` through `tests/test-plan-file-handling.sh:10`.

- algorithmic_behavior: The script is organized as a linear suite of scenario sections using `pass`, `fail`, and `warn` counters rather than shell `set -e`; failures accumulate until the summary decides the exit code at `tests/test-plan-file-handling.sh:25` through `tests/test-plan-file-handling.sh:42` and `tests/test-plan-file-handling.sh:1022` through `tests/test-plan-file-handling.sh:1036`. Section 1 validates CLI surface area by checking `scripts/setup-rlcr-loop.sh --help` for `--commit-plan-file` and the default “plan file stays uncommitted” contract at `tests/test-plan-file-handling.sh:55` through `tests/test-plan-file-handling.sh:71`. Sections 2 and 3 validate setup-time schema and preconditions: state must include `commit_plan_file`, `start_commit`, and `plan_file_tracked`; setup must create `plan-backup.md`; `PROJECT_ROOT` must be initialized before relative path calculation; commit-plan mode must require tracked, clean, non-ignored, inside-repo plans; and the repo must have at least one commit at `tests/test-plan-file-handling.sh:73` through `tests/test-plan-file-handling.sh:217`. These expectations map to setup implementation in `scripts/setup-rlcr-loop.sh:31` through `scripts/setup-rlcr-loop.sh:37`, `scripts/setup-rlcr-loop.sh:57` through `scripts/setup-rlcr-loop.sh:60`, `scripts/setup-rlcr-loop.sh:199` through `scripts/setup-rlcr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:243` through `scripts/setup-rlcr-loop.sh:303`, and `scripts/setup-rlcr-loop.sh:317` through `scripts/setup-rlcr-loop.sh:338`.

  Section 4 verifies stop-hook behavior after work is done: it reads `commit_plan_file`, `plan_file`, and `start_commit` from state; filters the plan file out of `git status` when the plan should remain uncommitted; preserves similarly named files via an end-of-line anchor; detects accidental plan commits using `git log START_COMMIT..HEAD`; terminates pre-1.1.2 loops; and documents that content validation moved to the prompt validator at `tests/test-plan-file-handling.sh:219` through `tests/test-plan-file-handling.sh:289`. The corresponding stop-hook logic is in `hooks/loop-codex-stop-hook.sh:185` through `hooks/loop-codex-stop-hook.sh:213`, `hooks/loop-codex-stop-hook.sh:215` through `hooks/loop-codex-stop-hook.sh:245`, and `hooks/loop-codex-stop-hook.sh:296` through `hooks/loop-codex-stop-hook.sh:341`.

  Section 4b adds an earlier pre-commit gate: when `commit_plan_file` is false, `hooks/loop-bash-validator.sh` must block `git commit` if the plan file is staged, using `git diff --cached --name-only` and `plan-file-staged.md` at `tests/test-plan-file-handling.sh:295` through `tests/test-plan-file-handling.sh:334`. The implementation reads the same state fields, detects a git commit command, computes a git-root-relative plan path, and uses exact fixed-string matching against staged files at `hooks/loop-bash-validator.sh:99` through `hooks/loop-bash-validator.sh:148`.

  Section 5 treats block templates as part of the algorithm’s output contract. It requires templates for accidental commits, staged plan commits, legacy state files, prompt-time plan changes, uncommitted plans, untracked plans, outside-repo conflicts, and commit-mode plan changes at `tests/test-plan-file-handling.sh:336` through `tests/test-plan-file-handling.sh:415`. The tested templates include required placeholders such as `{{PLAN_FILE}}`, `{{PLAN_FILE_COMMITS}}`, `{{PLAN_FILE_STATUS}}`, `{{PLAN_BACKUP_FILE}}`, and `{{PLAN_FILE_REL}}`, visible in `prompt-template/block/plan-file-committed.md:1` through `prompt-template/block/plan-file-committed.md:33`, `prompt-template/block/plan-file-staged.md:1` through `prompt-template/block/plan-file-staged.md:29`, `prompt-template/block/plan-file-changed-prompt-block.md:1` through `prompt-template/block/plan-file-changed-prompt-block.md:19`, `prompt-template/block/plan-file-uncommitted.md:1` through `prompt-template/block/plan-file-uncommitted.md:11`, `prompt-template/block/plan-file-not-tracked.md:1` through `prompt-template/block/plan-file-not-tracked.md:11`, and `prompt-template/block/plan-file-outside-repo-conflict.md:1` through `prompt-template/block/plan-file-outside-repo-conflict.md:13`.

  Sections 6 and 7 include small behavioral unit tests. Section 6 simulates porcelain status with `docs/plan.md`, `docs/plan.md.bak`, `docs/plan.md~`, `.humanize-loop.local/`, and another file, then verifies that only the exact plan path is filtered by the anchored regex at `tests/test-plan-file-handling.sh:417` through `tests/test-plan-file-handling.sh:466`. Section 7 creates mock git repositories under a temporary directory, commits a plan file after `START_COMMIT`, verifies `git log --follow START_COMMIT..HEAD -- docs/plan.md` detects it, verifies a non-existent file produces no result, checks the empty-repo `git rev-parse HEAD` failure mode, and checks first-commit plan detection after an initial commit exists at `tests/test-plan-file-handling.sh:468` through `tests/test-plan-file-handling.sh:563`.

  Section 8 summarizes the four user-visible plan-file cases using repository fixtures and implementation grep checks. It verifies that without `--commit-plan-file`, a tracked dirty plan is accepted by setup; with `--commit-plan-file`, the plan must be tracked and clean; outside-repo plans are rejected in commit-plan mode; the stop hook avoids invalid `"decision": "allow"` output; and stop-hook comments point prompt-time validation to `UserPromptSubmit` at `tests/test-plan-file-handling.sh:565` through `tests/test-plan-file-handling.sh:686`. This lines up with the README contract, where case behavior is documented at `README.md:211` through `README.md:235`.

  Section 9 verifies the prompt-time algorithm in `hooks/loop-plan-validator.sh`: it must be registered in `hooks/hooks.json`, detect legacy pre-1.1.2 state, compute whether the plan file is inside the git repository, block the invalid `--commit-plan-file` plus outside-repo case, require tracked and clean plans for commit-plan plus inside-repo mode, compare the live plan against `plan-backup.md` with `diff -q`, handle missing plans, output block messages to stderr, and exit 2 to block prompt processing at `tests/test-plan-file-handling.sh:688` through `tests/test-plan-file-handling.sh:836`. Registration is in `hooks/hooks.json:4` through `hooks/hooks.json:12`. The validator’s four-case description is in `hooks/loop-plan-validator.sh:3` through `hooks/loop-plan-validator.sh:22`; legacy state termination is in `hooks/loop-plan-validator.sh:53` through `hooks/loop-plan-validator.sh:78`; inside/outside path detection and ancestry-aware branch-change handling are in `hooks/loop-plan-validator.sh:89` through `hooks/loop-plan-validator.sh:167`; commit-plan invalid configuration and clean/tracked gates are in `hooks/loop-plan-validator.sh:169` through `hooks/loop-plan-validator.sh:251`; and backup-content comparison plus blocking output is in `hooks/loop-plan-validator.sh:253` through `hooks/loop-plan-validator.sh:317`.

  Section 10 validates shared state-transition helpers and ancestry invariants. It requires `stop_loop` to rename `state.md` with reason prefixes instead of deleting it, requires `check_start_commit_ancestry` to use `git merge-base --is-ancestor`, verifies plan and bash validators call that ancestry check, verifies the stop hook uses `stop_loop` with `completed`, `stopped`, and `unexpected` prefixes, verifies cancel command uses `cancelled-state.md`, and includes a mock-repo ancestry unit test where HEAD equal to `start_commit` is valid at `tests/test-plan-file-handling.sh:838` through `tests/test-plan-file-handling.sh:1020`. The helper implementation is in `hooks/lib/loop-common.sh:23` through `hooks/lib/loop-common.sh:61`; cancellation behavior is specified in `commands/cancel-rlcr-loop.md:19` through `commands/cancel-rlcr-loop.md:28`; stop-hook prefix use appears at `hooks/loop-codex-stop-hook.sh:196` through `hooks/loop-codex-stop-hook.sh:213`, `hooks/loop-codex-stop-hook.sh:405` through `hooks/loop-codex-stop-hook.sh:409`, and `hooks/loop-codex-stop-hook.sh:510` through `hooks/loop-codex-stop-hook.sh:515`.

- inputs_outputs_state: Inputs are the repository source files, the setup script help output, mock plan files, mock git repositories, and the shell environment. The test computes `SCRIPT_DIR` and `PROJECT_ROOT` from its own location at `tests/test-plan-file-handling.sh:15` through `tests/test-plan-file-handling.sh:16`, creates a disposable `TEST_DIR` with cleanup trap at `tests/test-plan-file-handling.sh:51` through `tests/test-plan-file-handling.sh:53`, and writes multiple mock plan/repo fixtures inside that temp tree. Output is console PASS/FAIL/WARN reporting plus process exit 0 or 1. Persistent algorithm state being specified is `.humanize-loop.local/<timestamp>/state.md` and `plan-backup.md`; setup writes `commit_plan_file`, `plan_file`, `plan_file_tracked`, and `start_commit` into YAML frontmatter at `scripts/setup-rlcr-loop.sh:326` through `scripts/setup-rlcr-loop.sh:340`, while validators later read those exact fields from state at `hooks/loop-plan-validator.sh:59` through `hooks/loop-plan-validator.sh:83`, `hooks/loop-bash-validator.sh:53` through `hooks/loop-bash-validator.sh:60`, and `hooks/loop-codex-stop-hook.sh:185` through `hooks/loop-codex-stop-hook.sh:189`.

- gates_or_invariants: The major invariant is that the plan file is snapshotted at loop start and cannot silently drift while the loop is active. Setup requires a git repository with at least one commit, a real plan file with at least five lines, and a simple path without whitespace or special regex characters at `scripts/setup-rlcr-loop.sh:176` through `scripts/setup-rlcr-loop.sh:233`. In commit-plan mode, setup and prompt validation require the plan file to be inside the git repo, not git-ignored, tracked by git, and clean at `scripts/setup-rlcr-loop.sh:251` through `scripts/setup-rlcr-loop.sh:303` and `hooks/loop-plan-validator.sh:169` through `hooks/loop-plan-validator.sh:251`. In default mode, the plan may be dirty or untracked, but stop-hook cleanliness checks filter the exact plan path out of `git status` and then separately reject commits containing that path at `hooks/loop-codex-stop-hook.sh:227` through `hooks/loop-codex-stop-hook.sh:245` and `hooks/loop-codex-stop-hook.sh:296` through `hooks/loop-codex-stop-hook.sh:341`. Prompt processing is blocked, not merely warned, when plan content differs from backup or the file is missing, using stderr plus exit 2 at `hooks/loop-plan-validator.sh:258` through `hooks/loop-plan-validator.sh:317`. Branch ancestry is valid only when the loop’s `start_commit` is an ancestor of HEAD, implemented by `git merge-base --is-ancestor` at `hooks/lib/loop-common.sh:40` through `hooks/lib/loop-common.sh:61`; some prompt-time branch divergence only blocks if a tracked default-mode plan changed across the divergence at `hooks/loop-plan-validator.sh:107` through `hooks/loop-plan-validator.sh:167`.

- dependencies_and_callers: The assigned test depends on Bash, Git, `grep`, `sed`, `wc`, temporary directories, and local repository files. It references `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-validator.sh`, `hooks/lib/loop-common.sh`, `hooks/hooks.json`, `commands/cancel-rlcr-loop.md`, multiple `prompt-template/block/*.md` files, and README-documented behavior. Runtime hook coordination is: `hooks/hooks.json` wires `loop-plan-validator.sh` into `UserPromptSubmit`, `loop-bash-validator.sh` into `PreToolUse` for Bash, and `loop-codex-stop-hook.sh` into Stop at `hooks/hooks.json:4` through `hooks/hooks.json:63`. All these hooks source `hooks/lib/loop-common.sh` for active-loop discovery, path calculations, stop-loop renaming, ancestry checks, lowercasing, and template loader setup at `hooks/lib/loop-common.sh:11` through `hooks/lib/loop-common.sh:17`, `hooks/lib/loop-common.sh:63` through `hooks/lib/loop-common.sh:84`, and `hooks/lib/loop-common.sh:143` through `hooks/lib/loop-common.sh:232`.

- edge_cases_or_failure_modes: Covered edge cases include missing `--commit-plan-file` help text, missing state fields, incorrect initialization ordering for `PROJECT_ROOT` before relative-path calculation, git-ignored plans, paths with spaces or regex metacharacters, plan paths outside the git root, empty git repositories without a first commit, tracked dirty plans in default mode, tracked dirty or untracked plans in commit-plan mode, missing or changed plan files versus backup, accidental staging before `git commit`, accidental plan commits after `start_commit`, similarly named files such as `docs/plan.md.bak` and `docs/plan.md~`, `.humanize-loop.local` untracked noise, legacy pre-1.1.2 state files without `start_commit`, branch/ancestry divergence, invalid stop-hook `"decision": "allow"` usage, and stop-loop state deletion versus rename preservation. A test-specific fragility is that several checks are structural greps rather than full runtime assertions, so equivalent refactors can fail the test even if behavior remains correct. Another test-specific fragility is temporary marker files `/tmp/proj_root_line` and `/tmp/plan_rel_line` at `tests/test-plan-file-handling.sh:123` through `tests/test-plan-file-handling.sh:126`, which are outside the suite-specific `TEST_DIR` and could collide across parallel runs.

- validation_or_tests: The assigned file is itself the validation artifact. It combines static implementation checks, template existence/placeholder checks, README contract alignment, and lightweight mock-git behavior checks. I did not execute it in this read-only branch export because the user explicitly requested research notes only and the script creates temporary git repositories; instead I inspected the file and its referenced implementation/template/config paths directly. If run in a writable temporary environment, the intended command would be `bash tests/test-plan-file-handling.sh`, with success determined by the final `FAILED == 0` branch at `tests/test-plan-file-handling.sh:1030` through `tests/test-plan-file-handling.sh:1036`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence heading present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`