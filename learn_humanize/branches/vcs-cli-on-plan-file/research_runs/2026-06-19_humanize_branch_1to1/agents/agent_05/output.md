# agent_05 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-005 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is the executable shell layer for starting and observing the RLCR loop. It contains three files, with no nested child directories: `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`. The core algorithmic child for this branch is `setup-rlcr-loop.sh`, which converts a user-supplied plan file into a timestamped `.humanize-loop.local/<session>/` state machine. `portable-timeout.sh` is a cross-platform subprocess timeout primitive consumed by the Codex stop hook. `humanize.sh` is the interactive monitor and dispatcher for viewing active loop progress.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` parses `/humanize:start-rlcr-loop <plan.md>` arguments, validates exactly one plan file, enforces plan-file policy, initializes loop state, creates a plan backup, seeds goal tracking, emits the round-0 prompt, and prints completion requirements. It defines defaults for Codex model, effort, timeout, and max iterations at lines 21-25, parses options such as `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, and `--commit-plan-file` at lines 102-170, and validates missing or duplicate plan inputs at lines 176-184 and 160-166. It normalizes relative plan paths against `PROJECT_ROOT` at lines 186-191, requires an existing file at lines 193-197, requires a git repository and initial commit at lines 199-210, rejects unsupported plan path characters at lines 215-223, requires at least five plan lines at lines 225-233, and requires `codex` at lines 235-241. For the branch’s plan-file behavior, it computes git-root-relative plan paths at lines 243-249, rejects `--commit-plan-file` for outside-repo or git-ignored plans at lines 251-268, detects whether the plan is tracked at lines 276-283, and when `--commit-plan-file=true`, requires the plan to be tracked and clean at lines 285-303. State initialization creates `.humanize-loop.local/<timestamp>/`, copies `plan-backup.md`, records `START_COMMIT`, and writes YAML-ish state fields including `commit_plan_file`, `plan_file`, `plan_file_tracked`, `codex_timeout`, and `start_commit` at lines 309-340. Goal tracker seeding extracts a goal and acceptance criteria heuristically from `## Goal`, `## Objective`, `## Purpose`, `## Acceptance`, `## Criteria`, or `## Requirements` sections at lines 346-434. The round-0 prompt embeds the plan file content, goal-tracker rules, anti-bypass instruction, commit requirement, and summary path at lines 440-493, conditionally adding push instructions at lines 495-501, then prints activation metadata and work-completion requirements at lines 507-561.
- algorithmic_behavior: `scripts/portable-timeout.sh` detects a timeout implementation in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` at lines 9-27. It stores the selected implementation in `TIMEOUT_IMPL` at line 29. `run_with_timeout` accepts seconds plus a command array, runs through `gtimeout` or GNU `timeout` when present, otherwise uses Python `subprocess.run(..., timeout=...)` and maps `TimeoutExpired` to exit code `124`, then falls back to running without timeout with a stderr warning if no implementation exists at lines 31-71. It exports `TIMEOUT_IMPL` for sourcing scripts at line 76.
- algorithmic_behavior: `scripts/humanize.sh` defines a shell function dispatcher, currently exposing `humanize monitor rlcr-loop` at lines 520-553. The monitor validates `.humanize-loop.local` exists at lines 16-21, finds the newest timestamp-named loop directory at lines 23-41, finds the latest Codex run log in `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/round-*-codex-run.log` by session timestamp and round number at lines 43-96, parses `state.md` fields including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` at lines 98-114, parses `goal-tracker.md` progress counts at lines 116-198, summarizes git status and diff stats at lines 200-246, renders a fixed terminal status bar with session, round, model, progress, git state, goal, plan, and log path at lines 262-381, constrains terminal scroll region below the bar at lines 383-399, and tails the active log incrementally while switching to newer logs when they appear at lines 430-518.
- inputs_outputs_state: `setup-rlcr-loop.sh` inputs are CLI arguments, the plan file, `CLAUDE_PROJECT_DIR` or current directory, git state, `codex` availability, and helper function `get_relative_path` sourced from `hooks/lib/loop-common.sh` at lines 13-15. Its outputs are `.humanize-loop.local/<timestamp>/state.md`, `plan-backup.md`, `goal-tracker.md`, `round-0-prompt.md`, and the activation/prompt text printed to stdout. The main state transition is from no active loop to a new loop session with `current_round: 0`, saved plan backup, start commit, and plan-file policy fields at lines 326-340. `portable-timeout.sh` inputs are a timeout duration and command vector; its output is the wrapped command’s stdout/stderr/exit code, with timeout standardized to exit `124` in the Python fallback. `humanize.sh` inputs are local loop session directories, cached Codex run logs, state and goal-tracker files, terminal dimensions, and git status. Its output is terminal UI state, not repository state; it tracks `current_file`, `current_session_dir`, `last_size`, `tail_pid`, and cleanup flags in process-local shell variables at lines 10-14 and 401-405.
- gates_or_invariants: `setup-rlcr-loop.sh` enforces one and only one plan file argument, positive integer validation for `--max` and `--codex-timeout`, optional `MODEL:EFFORT` parsing for `--codex-model`, git repo plus initial commit, plan file existence, simple plan-file relative path syntax, plan length >=5 lines, and `codex` on PATH. Its key plan-file invariant is that default loops may keep plan files uncommitted, but `--commit-plan-file` requires the plan to be inside the git repository, not ignored, tracked, and clean. It also records `start_commit` so sibling validators can detect incompatible legacy loops and branch/ancestry hazards. `portable-timeout.sh` preserves command argument boundaries by storing the command as an array at lines 34-36 and returning the underlying implementation’s status. `humanize.sh` gates monitoring on `.humanize-loop.local` existence and timestamp-shaped session names. It uses stat fallbacks for Linux/macOS file sizes at lines 447-450 and resets traps/terminal state during cleanup at lines 406-428 and 516-517.
- dependencies_and_callers: `commands/start-rlcr-loop.md` is the slash-command wrapper and only allows Bash execution of `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` at lines 1-14. `setup-rlcr-loop.sh` depends on `hooks/lib/loop-common.sh` for `get_relative_path`; that helper normalizes and computes relative paths portably at `hooks/lib/loop-common.sh` lines 143-220. The state emitted by setup is consumed by `hooks/loop-plan-validator.sh`, which reads `plan_file` and `commit_plan_file` at lines 80-83, handles old state files lacking `start_commit` at lines 59-78, classifies inside/outside repo paths at lines 94-104, blocks `--commit-plan-file` with an outside-repo plan at lines 173-198, requires tracked/clean plan files for committed-plan mode at lines 204-250, and compares the live plan to `plan-backup.md` at lines 253-317. `hooks/loop-bash-validator.sh` reads `commit_plan_file` and `plan_file` at lines 106-107 and blocks `git commit` if the plan is staged while `--commit-plan-file` was not set at lines 109-147. `hooks/loop-codex-stop-hook.sh` reads the setup state at lines 185-188, filters the plan file out of dirty git status when `commit_plan_file` is false at lines 230-245, blocks accidental plan-file commits since `START_COMMIT` at lines 296-340, and sources `scripts/portable-timeout.sh` before running Codex review at lines 652-699. `humanize.sh` coordinates with the stop hook’s cache layout because the monitor looks in `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/`, matching the stop hook’s cache generation at `hooks/loop-codex-stop-hook.sh` lines 638-650.
- edge_cases_or_failure_modes: `setup-rlcr-loop.sh` exits on unknown options, missing option values, non-numeric counts, multiple plan files, missing/too-short plans, non-git repos, repos without commits, missing `codex`, special characters in plan paths, git-ignored plan files under `--commit-plan-file`, outside-repo committed-plan requests, untracked committed-plan requests, and dirty committed-plan requests. A notable design edge case is that plan file paths with spaces or regex metacharacters are rejected because later git-status filtering relies on simple path matching at lines 215-223. For monorepos, setup computes paths relative to git toplevel rather than only `PROJECT_ROOT` at lines 243-249. `humanize.sh` handles no current log by waiting until one appears at lines 434-441, handles missing cache session directories by skipping them at lines 69-73, and handles growing logs by byte offset rather than repeated full tails at lines 483-490. It may repeatedly redraw the status bar using `tput`, so non-interactive or invalid terminal environments are likely fragile. `portable-timeout.sh` degrades to no timeout if no implementation is found, which preserves execution but weakens the timeout guarantee at lines 64-69.
- validation_or_tests: Read-only syntax validation with `bash -n scripts/humanize.sh scripts/portable-timeout.sh scripts/setup-rlcr-loop.sh` completed successfully with no output. A git status sanity check could not complete in this read-only branch export because the directory is not a normal git checkout from the tool’s perspective and macOS developer-tool cache creation was blocked by the sandbox; this did not modify files. Focused tests exist in `tests/test-plan-file-handling.sh`: they verify help text documents `--commit-plan-file` at lines 57-70, state schema fields and plan backup creation at lines 89-119, path/root ordering and tracked/clean checks at lines 121-147, git-ignore and unsupported-character validation at lines 149-186, committed-plan clean gating at lines 605-635, outside-repo committed-plan rejection at lines 637-659, and plan-validator cases for outside/inside repo committed-plan behavior at lines 739-775. The tests are partly structural grep checks rather than full end-to-end execution, apparently because full setup requires Codex availability.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item section present; the single assigned directory item is covered once in `Item Evidence`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`