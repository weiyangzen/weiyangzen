# agent_43 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-043 `file` `prompt-template/block/plan-file-changed-commit-mode.md`
- cursor: `[_]`
- core_role: This file is a Markdown block template for the `--commit-plan-file` failure path where the plan file changed after the loop started. Its user-facing contract is: a changed plan file blocks loop continuation because commit-plan-file mode requires the plan file to be tracked and clean. The template displays the plan file path, an issue detail block, the backup path, and recovery options. See [prompt-template/block/plan-file-changed-commit-mode.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/prompt-template/block/plan-file-changed-commit-mode.md:1).
- algorithmic_behavior: The template itself performs no computation; it defines the rendered message for a guard decision. Its placeholders are `{{PLAN_FILE}}`, `{{ISSUE_DETAILS}}`, and `{{PLAN_BACKUP_FILE}}` at lines 5-9. The intended transition is from active loop execution to blocked/error state when plan-file state has changed unexpectedly under `--commit-plan-file`; the operator is told to commit changes, restore the backup, or restart without `--commit-plan-file` at lines 11-16.
- inputs_outputs_state: Inputs are injected by the template renderer as string variables: `PLAN_FILE`, `ISSUE_DETAILS`, and `PLAN_BACKUP_FILE`. Output is a Markdown error block headed `# Error: Plan File Changed (--commit-plan-file mode)`. State-wise, it represents a guard stop: the loop cannot continue until the plan file state is reconciled. The related live validator reads `plan_file` and `commit_plan_file` from the loop state file in [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:80), computes repository-relative paths at lines 94-103, and checks commit-plan-file case logic at lines 173-250.
- gates_or_invariants: The invariant expressed by the template is stricter than normal plan-file handling: with `--commit-plan-file`, the plan file must be version-controlled and clean. The active validator enforces the adjacent gates directly: outside-repo plus commit-plan-file is blocked at [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:173), untracked plan file is blocked at line 206, and dirty plan file is blocked using `git status --porcelain` at lines 226-249. All cases also compare the current plan file against the loop backup with `diff -q` at lines 258-275, blocking if content changed.
- dependencies_and_callers: Template rendering is provided by `load_and_render_safe`, which loads a template, substitutes `{{VAR}}` placeholders in one pass, and falls back if missing or empty; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:155) and the single-pass placeholder logic at lines 43-116. Direct call-site search found no runtime use of `block/plan-file-changed-commit-mode.md`; the only direct reference is the existence check in [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:394). The active plan-content changed prompt uses sibling template `block/plan-file-changed-prompt-block.md` in [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:311). The active commit-plan-file tracked/clean failures use `plan-file-not-tracked.md` and `plan-file-uncommitted.md` at lines 219 and 244.
- edge_cases_or_failure_modes: If this template were wired into runtime, missing variables would leave unreplaced placeholders because `render_template` preserves unknown `{{VAR}}` tokens at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:101). The command suggestion `cp '{{PLAN_BACKUP_FILE}}' '{{PLAN_FILE}}' then commit` is useful for modified plans but does not separately distinguish deleted, untracked, dirty, or outside-repo cases; the active validator currently handles those with more specific sibling templates. The template also says the plan file “has changed since the loop started,” but no direct caller currently supplies the `ISSUE_DETAILS` aggregation that would explain whether the change is content drift, git dirtiness, missing file, or tracking failure.
- validation_or_tests: The assigned template has an explicit existence test in [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:394). That test only verifies the file exists; placeholder checks immediately afterward are for `plan-file-committed.md`, not this file, because they use `TEMPLATE_FILE` from line 339 rather than `TEMPLATE_FILE_CHANGED` from line 395. Generic template reference tests scan `load_template`, `load_and_render`, and `load_and_render_safe` call sites and warn on unreferenced templates at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-template-references.sh:84) and lines 131-142, which would classify this file as not directly referenced by hooks. I did not execute the test scripts; this was a read-only branch research pass.
- skip_candidate: `yes: the file is a guard-message contract artifact, but direct runtime search found no caller outside an existence test. The currently wired algorithmic behavior is implemented through sibling templates and hook logic, so this specific template appears dormant unless future code renders it dynamically.`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-043`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`