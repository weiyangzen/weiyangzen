# agent_22 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Interactive shell utility for observing an active Humanize/RLCR loop. It defines `_humanize_monitor_codex()` and exposes it through the `humanize monitor rlcr-loop` dispatcher, so its algorithmic role is runtime monitoring, session/log routing, status rendering, and read-only state summarization rather than loop mutation. Entry points are at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:9) and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:521).

- algorithmic_behavior: The monitor starts by requiring a `.humanize-loop.local` directory in the current working directory, failing early if absent at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:16). `_find_latest_session()` scans timestamp-named session directories and chooses the lexicographically newest name matching `YYYY-MM-DD_HH-MM-SS` at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:23). `_find_latest_codex_log()` sanitizes `pwd`, maps it to `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/`, then picks the newest `round-*-codex-run.log` by session timestamp and numeric round at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:43). It parses `state.md`, `goal-tracker.md`, and Git status into pipe-delimited summaries at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:98), [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:116), and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:200). `_draw_status_bar()` renders a fixed top dashboard from those summaries, then the main loop tails the current log, reads appended bytes, refreshes status every 0.5 seconds, and switches when a newer log is detected at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:456).

- inputs_outputs_state: Inputs are the caller’s current directory, `.humanize-loop.local/<timestamp>/state.md`, `.humanize-loop.local/<timestamp>/goal-tracker.md`, the Git worktree status, terminal dimensions from `tput`, and cached Codex stderr logs under `$HOME/.cache/humanize/.../round-N-codex-run.log`. The related setup script creates the session state fields consumed here, including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `plan_file`, and `started_at` at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:326). The stop hook writes the cache layout this monitor expects at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:638). Outputs are terminal-only: usage/help text, a 10-line fixed status bar, tail output in a scroll region, and return status. The script does not write project files. Internal state transitions are `current_file`/`current_session_dir` unset to latest discovered log, `last_size` updated after byte-tail reads, `current_file` switched on newer log detection, and `monitor_running` flipped false during signal cleanup.

- gates_or_invariants: It gates execution on `.humanize-loop.local` existing before any monitoring work. Session discovery ignores non-directory entries and directory names outside the timestamp regex. Log discovery only considers cache directories whose timestamp also exists locally under `.humanize-loop.local`, which prevents monitoring unrelated cached runs. The terminal invariant is a fixed `status_bar_height=10`; `_setup_terminal()` constrains scrolling below that region and `_restore_terminal()` resets it. Signal handling for `INT`/`TERM` calls `_cleanup()`, guarded by `cleanup_done`, to stop monitoring and restore the terminal at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:406). It is otherwise a read-only monitor: there are no commit, validation, or workflow-advancement writes in this file.

- dependencies_and_callers: The documented caller is an interactive shell where users source this script and run `humanize monitor rlcr-loop`; README lines [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/README.md:90) and [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/README.md:100) describe that path. Runtime command dependencies include `basename`, `pwd`, `sed`, `grep`, `sort`, `wc`, `tr`, `git`, `tput`, `clear`, `tail`, `stat`, `seq`, `sleep`, `kill`, and `wait`. It depends structurally on `scripts/setup-rlcr-loop.sh` for session/state creation and `hooks/loop-codex-stop-hook.sh` for cached Codex log production. The dispatcher has only one supported command target; invalid commands print usage and return 1 at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/humanize.sh:525).

- edge_cases_or_failure_modes: If a newer timestamped session exists without a cached log while an older session has logs, `_find_latest_session()` and `_find_latest_codex_log()` can disagree, causing the status bar to read one session’s `state.md` while tailing another session’s log. If a matching log filename has a nonnumeric round segment, the `-gt` comparison can emit an integer-comparison error. If a log file shrinks or rotates in place, `last_size` is not reset, so appended content may be skipped until the file grows past the old byte offset. The parser is intentionally simple: `state.md` parsing is grep/sed key extraction, not YAML parsing, and `goal-tracker.md` counting depends on specific headings and table/list shapes. Very narrow or noninteractive terminals can break `tput`/width arithmetic, and calling `humanize` with no arguments performs `shift` before usage handling, which can emit a shell diagnostic in some shells. `tail_pid` cleanup scaffolding is present, but no background tail process is currently assigned to it.

- validation_or_tests: I inspected the file directly with line numbers and cross-referenced its producers/documentation. `bash -n scripts/humanize.sh` passed. `zsh -n scripts/humanize.sh` also passed in this environment. A repository search found README documentation for the monitor but no dedicated automated test targeting `scripts/humanize.sh`; existing tests mostly cover templates, plan-file handling, validators, and hook behavior.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single Item Evidence section is the assigned file item`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`