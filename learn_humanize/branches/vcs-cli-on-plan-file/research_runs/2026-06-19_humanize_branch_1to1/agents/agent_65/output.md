# agent_65 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-065 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: This template is the Claude continuation prompt for the RLCR loop when Codex reviews the prior round and finds unresolved issues. It is not executable code, but it is core algorithmic control text: it defines the next worker-round contract after a failed/non-complete review, re-binding Claude to the original plan, Codex findings, todo creation, and goal-tracker update rules. The direct prompt sections are `Original Implementation Plan` with `@{{PLAN_FILE}}` at `prompt-template/claude/next-round-prompt.md:3-8`, the embedded Codex review block at `prompt-template/claude/next-round-prompt.md:16-19`, and the read-only goal-tracker reference at `prompt-template/claude/next-round-prompt.md:22-31`.
- algorithmic_behavior: The template turns a review failure into a deterministic next-round instruction set. It begins by declaring the work unfinished and requiring “ultrathink” execution at `prompt-template/claude/next-round-prompt.md:1`. It requires Claude to re-read the original implementation plan before proceeding at `prompt-template/claude/next-round-prompt.md:5-8`. It then imposes exhaustive issue tracking: create todos for all tasks and do not only address the highest-priority issues at `prompt-template/claude/next-round-prompt.md:12-13`. It injects Codex’s review output inside explicit start/end markers at `prompt-template/claude/next-round-prompt.md:16-19`, making the review the actionable delta for the next round. Finally, it forces the next round to treat `goal-tracker.md` as read-only after Round 0 and to request tracker edits through the summary rather than direct modification at `prompt-template/claude/next-round-prompt.md:22-31`.
- inputs_outputs_state: Inputs are three template variables: `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`, all passed by `hooks/loop-codex-stop-hook.sh` when rendering the next prompt at `hooks/loop-codex-stop-hook.sh:883-886`. The primary output is `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md`, established at `hooks/loop-codex-stop-hook.sh:870-886`. The surrounding hook advances loop state by replacing `current_round:` with `NEXT_ROUND` in `state.md` before creating the prompt at `hooks/loop-codex-stop-hook.sh:865-868`. The rendered prompt is then returned as the hook block reason, with system message `Loop: Round $NEXT_ROUND/$MAX_ITERATIONS - Codex found issues to address`, at `hooks/loop-codex-stop-hook.sh:918-929`. The continuation path is reached only after Codex review content is read from `$REVIEW_RESULT_FILE` at `hooks/loop-codex-stop-hook.sh:811-818` and the result is neither terminal `COMPLETE` nor terminal `STOP`; those terminal branches stop the loop at `hooks/loop-codex-stop-hook.sh:821-858`.
- gates_or_invariants: The main gate before this template is max-iteration protection: `NEXT_ROUND` is calculated at `hooks/loop-codex-stop-hook.sh:510`, and the loop stops if it exceeds `MAX_ITERATIONS` at `hooks/loop-codex-stop-hook.sh:512-516`. The semantic gates encoded by the template are: the original plan remains the source of scope, all discovered issues must become todos, the Codex review content must be addressed, and the goal tracker must be read-only after Round 0 except through a summary request. That read-only invariant is reinforced outside the template by validators: edit/write attempts against `goal-tracker.md` after Round 0 are blocked at `hooks/loop-edit-validator.sh:78-82` and `hooks/loop-write-validator.sh:91-96`, while Bash modification attempts are routed through the bash validator at `hooks/loop-bash-validator.sh:166-172`. The corresponding block template states that only Codex can modify the tracker after Round 0 and instructs Claude to use a `Goal Tracker Update Request` in the summary at `prompt-template/block/goal-tracker-modification.md:1-25`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, in the “Review Found Issues - Continue Loop” branch at `hooks/loop-codex-stop-hook.sh:861-886`. Rendering depends on `hooks/lib/template-loader.sh`; `load_and_render_safe` loads the template and falls back if it is missing or renders empty at `hooks/lib/template-loader.sh:155-187`. Placeholder substitution uses the single-pass `render_template` implementation, explicitly designed so variable values containing `{{OTHER_VAR}}` are not rescanned or substituted at `hooks/lib/template-loader.sh:35-42` and implemented through awk at `hooks/lib/template-loader.sh:56-116`. The completed prompt is assembled with sibling Claude templates: optional post-alignment action items at `hooks/loop-codex-stop-hook.sh:888-893`, required footer/summary instructions at `hooks/loop-codex-stop-hook.sh:896-900`, optional push-every-round note at `hooks/loop-codex-stop-hook.sh:902-908`, and the goal-tracker update request appendix at `hooks/loop-codex-stop-hook.sh:911-916`. Upstream, `REVIEW_CONTENT` comes from Codex review execution and result capture at `hooks/loop-codex-stop-hook.sh:672-812`.
- edge_cases_or_failure_modes: If `prompt-template/claude/next-round-prompt.md` is missing or empty, `load_and_render_safe` falls back to the much shorter inline prompt at `hooks/loop-codex-stop-hook.sh:875-886`; that fallback still includes the review and plan/tracker references but loses the stronger todo and read-only goal-tracker wording from the real template. If `REVIEW_CONTENT` contains template-looking text such as `{{PLAN_FILE}}`, the single-pass renderer should preserve it literally rather than injecting more values; this is a deliberate guard against prompt corruption, documented at `hooks/lib/template-loader.sh:39-42`. A very large Codex review could stress the environment-variable substitution approach because values are passed into awk through `TMPL_VAR_*` environment variables at `hooks/lib/template-loader.sh:47-65`. If required caller variables were empty, the template would still render but could produce weak references such as `@` for a missing file path; this template itself has no validation for non-empty inputs. The template also relies on appended sibling templates for commit, summary, push, and formal goal-tracker update request details, so inspecting this file alone would understate the full generated prompt contract.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls and verifies referenced templates exist at `tests/test-template-references.sh:84-107`; this covers the `claude/next-round-prompt.md` reference in the stop hook. `tests/test-templates-comprehensive.sh` loads every markdown template under `prompt-template/` at `tests/test-templates-comprehensive.sh:90-107` and validates placeholder syntax for all templates at `tests/test-templates-comprehensive.sh:118-194`, so this file’s `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{GOAL_TRACKER_FILE}}` syntax is covered generically. `tests/test-template-loader.sh` includes a realistic injection test where `REVIEW_CONTENT` contains `{{GOAL_TRACKER_FILE}}` and `{{PLAN_FILE}}`; the expected result preserves those inner placeholders literally at `tests/test-template-loader.sh:525-540`. I did not execute the tests because this assignment requested read-only research notes; validation evidence here is based on direct inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 sole assigned item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`