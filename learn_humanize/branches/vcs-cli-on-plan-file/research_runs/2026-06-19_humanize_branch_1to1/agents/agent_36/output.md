# agent_36 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-036 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: This is a stop-hook blocking prompt template for the RLCR loop’s “dirty git state” gate. It is not an executable algorithm itself, but it defines the remediation contract shown to the agent/user when exit is attempted while the repository still has uncommitted changes. The hook renders it into the JSON `reason` field used to block the stop event.
- algorithmic_behavior: The template has a fixed title and instructions plus two runtime placeholders. Line 3 injects `{{GIT_ISSUES}}`, usually `uncommitted changes`; line 4 injects `{{SPECIAL_NOTES}}`; lines 5-9 define required cleanup actions: optionally run `code-simplifier`, review untracked files, stage real changes, and commit with a descriptive message. Lines 11-14 define commit metadata rules, especially that AI tools must not be commit authors and must not add AI co-author trailers. Line 16 tells the agent it may attempt exit only after committing all changes.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `GIT_ISSUES` and `SPECIAL_NOTES` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:281). `SPECIAL_NOTES` is built from untracked-file detections, including `.humanize-loop.local` and other untracked artifacts at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:250). Output is rendered Markdown passed as the stop-hook block reason in JSON with `"decision": "block"` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:285). The state transition is: stop attempt -> git status inspected -> non-plan-file dirty status detected -> template rendered -> stop blocked -> user/agent must clean and commit -> next stop attempt may proceed if the dirty gate no longer fires.
- gates_or_invariants: The active gate is in the caller, not the template. The stop hook runs `git status --porcelain`, filters out the plan file when `COMMIT_PLAN_FILE != true`, and blocks if filtered status is non-empty at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:227). The plan-file exemption is exact-path anchored after computing a git-root-relative path at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:230). Template-level invariants are: do not exit with uncommitted real changes, review untracked files before committing, add generated artifacts to `.gitignore`, follow project commit conventions, and avoid AI authorship/co-author attribution.
- dependencies_and_callers: The primary caller is `hooks/loop-codex-stop-hook.sh`, which loads this file through `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:281). Rendering depends on `hooks/lib/template-loader.sh`; `render_template` performs single-pass `{{VAR}}` substitution to avoid placeholder injection at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:35), and `load_and_render_safe` falls back to an inline message if the template is missing or renders empty at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:155). `SPECIAL_NOTES` may include `prompt-template/block/git-not-clean-humanize-local.md` for `.humanize-loop.local` or `prompt-template/block/git-not-clean-untracked.md` for other untracked files.
- edge_cases_or_failure_modes: If `git` is unavailable or the current directory is not a git repo, the dirty-git block is skipped by the caller’s outer condition at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:222). If the template file is missing, the fallback still blocks exit but loses the richer required-actions and AI-attribution rules. If `COMMIT_PLAN_FILE` is false, dirty status for the plan file is intentionally excluded, so the gate can allow exit with a dirty plan file while still blocking on other dirty files. Untracked `.humanize-loop.local` receives special handling instructing `.gitignore` update rather than committing it. The template’s step 0 is conditional on `code-simplifier` being installed; if unavailable, it is advisory rather than enforceable by this file.
- validation_or_tests: I did not find a direct golden-output test for this Markdown template. Related tests verify the caller-side gate that makes this template algorithmically relevant: `tests/test-plan-file-handling.sh` checks that the stop hook contains filtered git status logic at [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:239), simulates exact plan-file path filtering without dropping similarly named files at [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:420), and checks setup behavior around plan-file clean status when `--commit-plan-file` is enabled at [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:608). The template-loader has explicit safe fallback behavior in code, but no specific test evidence was inspected for this file’s exact text.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-036`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`