# agent_17 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook controller for the RLCR loop state machine. It intercepts Claude Code `Stop` events, decides whether the agent may exit, whether to block with more instructions, or whether to terminate the loop as completed/stopped/unexpected. It is registered as the `Stop` hook in `hooks/hooks.json:52-59`, and it relies on active loop state under `.humanize-loop.local/<timestamp>/` as documented in `hooks/loop-codex-stop-hook.sh:8-13`.

- algorithmic_behavior: The hook reads the hook JSON from stdin, resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, sources shared helpers, and selects the newest active loop with `find_active_loop` (`hooks/loop-codex-stop-hook.sh:29-56`, `hooks/lib/loop-common.sh:67-84`). It intentionally ignores `stop_hook_active`, so every blocked continuation can be reviewed again (`hooks/loop-codex-stop-hook.sh:31-37`). Before running Codex, it performs cheap gates: incomplete Claude todos from the transcript (`hooks/loop-codex-stop-hook.sh:67-95`, `hooks/check-todos-from-transcript.py:87-125`), changed code/doc files over 2000 lines (`hooks/loop-codex-stop-hook.sh:104-177`), git cleanliness and plan-file commit policy (`hooks/loop-codex-stop-hook.sh:185-377`), summary-file presence (`hooks/loop-codex-stop-hook.sh:421-442`), and round-0 goal-tracker initialization (`hooks/loop-codex-stop-hook.sh:448-503`). If gates pass, it renders either the regular Codex review prompt or every-fifth-round full-alignment prompt (`hooks/loop-codex-stop-hook.sh:530-608`), runs `codex exec` with configured model/effort/timeout (`hooks/loop-codex-stop-hook.sh:614-699`), validates the review artifact (`hooks/loop-codex-stop-hook.sh:737-809`), and interprets the last non-empty line as the control signal (`hooks/loop-codex-stop-hook.sh:814-859`).

- inputs_outputs_state: Inputs are hook stdin containing at least `transcript_path` when todo checking is possible, environment variables `CLAUDE_PROJECT_DIR` and optionally `CODEX_TIMEOUT`, active loop files `state.md`, `goal-tracker.md`, `round-N-summary.md`, `round-N-prompt.md`, the original `plan_file`, and git repository state. Setup creates the state fields consumed here: `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `commit_plan_file`, `plan_file`, `plan_file_tracked`, and `start_commit` (`scripts/setup-rlcr-loop.sh:326-340`). Outputs are Claude hook JSON blocks with `"decision": "block"` for recoverable gates, stderr progress/debug messages, review prompt/result files in the loop directory, Codex debug files under `$HOME/.cache/humanize/<sanitized-project>/<timestamp>/`, next-round prompt files, and state transitions. Loop termination is modeled by `stop_loop`, which renames `state.md` to `<prefix>-state.md` (`hooks/lib/loop-common.sh:31-38`). `COMPLETE` renames to `completed-state.md`; `STOP` and max-iteration exhaustion rename to `stopped-state.md`; corrupted or legacy state renames to `unexpected-state.md`; normal “issues remain” updates `current_round` in `state.md` and blocks with the next prompt (`hooks/loop-codex-stop-hook.sh:512-515`, `hooks/loop-codex-stop-hook.sh:821-858`, `hooks/loop-codex-stop-hook.sh:865-930`).

- gates_or_invariants: The hook enforces “no exit while TodoWrite has incomplete todos” (`hooks/loop-codex-stop-hook.sh:67-95`), no changed code/doc file above 2000 lines (`hooks/loop-codex-stop-hook.sh:104-177`), no non-plan-file dirty git state before review (`hooks/loop-codex-stop-hook.sh:222-294`), no accidental committed plan file when `commit_plan_file` is false (`hooks/loop-codex-stop-hook.sh:296-341`), and no unpushed commits when `push_every_round` is true (`hooks/loop-codex-stop-hook.sh:344-376`). Legacy pre-1.1.2 loops with `plan_file` but no `start_commit` are terminated and allowed to exit instead of continuing with incompatible plan-file protection (`hooks/loop-codex-stop-hook.sh:190-213`). `current_round` must be numeric or the loop is stopped as unexpected (`hooks/loop-codex-stop-hook.sh:405-410`); `max_iterations` is normalized to `42` if invalid (`hooks/loop-codex-stop-hook.sh:412-415`). Codex completion is deliberately strict: only a last non-empty line exactly equal to `COMPLETE` completes the loop, and only exactly `STOP` triggers the circuit breaker (`hooks/loop-codex-stop-hook.sh:814-859`).

- dependencies_and_callers: Called by Claude Code via `hooks/hooks.json:52-59`. Depends on `hooks/lib/loop-common.sh` for active-loop discovery, path helpers, lowercasing, and `stop_loop`; that file sources `hooks/lib/template-loader.sh` (`hooks/lib/loop-common.sh:11-17`). Template rendering uses `load_and_render_safe`, which falls back when templates are missing or empty (`hooks/lib/template-loader.sh:155-188`). Todo completeness is delegated to `hooks/check-todos-from-transcript.py`, which parses several transcript shapes and treats any status other than `completed` as incomplete (`hooks/check-todos-from-transcript.py:17-84`, `hooks/check-todos-from-transcript.py:109-122`). Codex timeout support comes from `scripts/portable-timeout.sh`, which prefers `gtimeout`, GNU `timeout`, then Python, then no timeout (`scripts/portable-timeout.sh:9-71`). The hook reads templates under `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude`; tests explicitly scan this hook for template references (`tests/test-template-references.sh:56-111`).

- edge_cases_or_failure_modes: No active loop or missing `state.md` allows exit without JSON (`hooks/loop-codex-stop-hook.sh:52-57`, `hooks/loop-codex-stop-hook.sh:383-385`). Missing `codex` blocks exit with installation guidance (`hooks/loop-codex-stop-hook.sh:614-634`). Nonzero Codex exit, absent review result, failed stdout copy, or empty review result all block with debug file paths (`hooks/loop-codex-stop-hook.sh:711-809`). If Codex writes review content to stdout instead of the requested result file, the hook copies stdout into the review result (`hooks/loop-codex-stop-hook.sh:756-772`). Plan-file git-status filtering uses a git-root-relative path, escapes basic grep metacharacters, and anchors the match to avoid suppressing `plan.md.bak` or similar files (`hooks/loop-codex-stop-hook.sh:230-245`; isolated test at `tests/test-plan-file-handling.sh:417-467`). Large-file detection only examines changed tracked/untracked files from porcelain status and code/doc extensions, so binary files and unsupported extensions are ignored (`hooks/loop-codex-stop-hook.sh:109-151`). The `push_every_round` ahead check is based on `git status -sb` text and only catches an explicit `ahead N` marker (`hooks/loop-codex-stop-hook.sh:350-375`). Full-alignment rounds are `current_round % 5 == 4`, so STOP is expected on rounds 4, 9, 14, etc.; STOP outside those rounds is still honored but logged as unusual (`hooks/loop-codex-stop-hook.sh:542-546`, `hooks/loop-codex-stop-hook.sh:831-858`).

- validation_or_tests: I directly inspected the assigned file with line numbers and ran `bash -n hooks/loop-codex-stop-hook.sh`, which passed with no syntax output. Existing tests cover this hook’s state-field reads, filtered git status, post-commit plan-file detection, pre-1.1.2 loop handling, and UserPromptSubmit delegation notes (`tests/test-plan-file-handling.sh:219-289`). The same test file includes an isolated exact-path filtering test and git-log post-commit detection test (`tests/test-plan-file-handling.sh:417-523`). Template coverage is checked by `tests/test-template-references.sh`, which includes `hooks/loop-codex-stop-hook.sh` in the scripts scanned for missing templates and unsafe render calls (`tests/test-template-references.sh:56-111`, `tests/test-template-references.sh:173-231`). A separate `git status --short` sanity probe was interrupted because the sandboxed macOS toolchain attempted forbidden cache writes under `/tmp`; that was not needed for this read-only evidence.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present; the assigned identifier is intentionally not repeated here to preserve exact-once output discipline.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`