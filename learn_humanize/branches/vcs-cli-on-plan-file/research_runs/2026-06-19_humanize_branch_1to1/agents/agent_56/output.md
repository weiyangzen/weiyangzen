# agent_56 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-056 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: This is a stop-block prompt template for the RLCR loop’s push policy. It turns the detected “local branch is ahead of remote” condition into the user-facing reason that prevents loop exit when `--push-every-round` is enabled. The file is small, but it participates in an algorithmic gate: no exit while committed work remains local under the push-every-round contract.
- algorithmic_behavior: The template renders a Markdown block headed “Unpushed Commits Detected” at `prompt-template/block/unpushed-commits.md:1`. It interpolates `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into the reason text at `prompt-template/block/unpushed-commits.md:3`, states that `--push-every-round` requires a push before exit at `prompt-template/block/unpushed-commits.md:5`, and emits the required command `git push origin {{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:7-10`. After the command instruction, it tells the agent/user to retry exit only after pushing at `prompt-template/block/unpushed-commits.md:12`.
- inputs_outputs_state: Inputs are render variables supplied by the stop hook: `AHEAD_COUNT`, derived from the numeric part of `git status -sb` ahead output, and `CURRENT_BRANCH`, derived from `git rev-parse --abbrev-ref HEAD`; see `hooks/loop-codex-stop-hook.sh:352-355`. Output is rendered Markdown assigned to `REASON` by `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:362-364`, then embedded into a JSON block decision as `reason` at `hooks/loop-codex-stop-hook.sh:366-373`. The template itself is stateless; the state transition occurs in the caller, which exits with a block decision at `hooks/loop-codex-stop-hook.sh:374` instead of allowing normal stop completion.
- gates_or_invariants: The gate is conditional on loop state `push_every_round: true`, read from the state file at `hooks/loop-codex-stop-hook.sh:347-350`. That state is set by the setup CLI when `--push-every-round` is parsed at `scripts/setup-rlcr-loop.sh:146-148` and persisted into `state.md` at `scripts/setup-rlcr-loop.sh:326-334`. The unpushed-commit block only activates if the local branch status includes an ahead count at `hooks/loop-codex-stop-hook.sh:351-353`. The invariant expressed by the template is: when push-every-round is enabled and local commits are ahead of remote, the next allowed action is pushing the current branch before attempting exit again.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, specifically the “Check Unpushed Commits” section at `hooks/loop-codex-stop-hook.sh:344-376`. Rendering is handled by `hooks/lib/template-loader.sh`, whose placeholder syntax is documented as `{{VARIABLE_NAME}}` at `hooks/lib/template-loader.sh:5-7`. `render_template` performs single-pass substitution to prevent placeholder injection or recursive replacement at `hooks/lib/template-loader.sh:35-42` and `hooks/lib/template-loader.sh:56-116`. `load_and_render_safe` provides fallback behavior if this template is missing or renders empty at `hooks/lib/template-loader.sh:152-188`. Setup also adds a proactive round prompt note when push-every-round is enabled at `scripts/setup-rlcr-loop.sh:495-500`, so this block is the enforcement-side counterpart to earlier instruction text.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook falls back to an inline shorter unpushed-commits message defined at `hooks/loop-codex-stop-hook.sh:357-361`, so the gate can still block. If `git status -sb` does not include the exact `ahead N` pattern, no block is triggered by this section even if another remote relationship problem exists; the detection depends on Git’s short branch status format at `hooks/loop-codex-stop-hook.sh:352`. If `git rev-parse --abbrev-ref HEAD` fails, the branch placeholder is rendered as `unknown` by the caller at `hooks/loop-codex-stop-hook.sh:355`, which could make the suggested `git push origin unknown` unhelpful. If the current branch has no configured upstream or is detached, the ahead-count path may not represent the intended remote push target. The template always suggests `origin {{CURRENT_BRANCH}}`, so repositories using a non-`origin` remote or a differently named upstream branch are not represented by this prompt.
- validation_or_tests: `tests/test-templates-comprehensive.sh` includes a real-template render test for `block/unpushed-commits.md` at `tests/test-templates-comprehensive.sh:517-528`. The test renders with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch` at `tests/test-templates-comprehensive.sh:518-520`, then validates that the output includes the heading, the rendered count text, and the rendered branch name at `tests/test-templates-comprehensive.sh:522-525`. This validates the template substitution contract, but it does not exercise the full stop-hook Git state detection path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: VCS_CLI_ON_PLAN_FILE-HZ-056
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`