# agent_50 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-050 `file` `prompt-template/block/plan-file-uncommitted.md`
- cursor: `[_]`
- core_role: This is a UserPromptSubmit error-block template for the `--commit-plan-file` plan-file protection algorithm. Its role is not to execute logic directly, but to define the user-facing failure contract when a loop is configured to commit the plan file and the tracked plan file is dirty. The title and blocking condition are defined in `prompt-template/block/plan-file-uncommitted.md:1-3`, with rendered diagnostics for the plan path and git status in `prompt-template/block/plan-file-uncommitted.md:5-6`.
- algorithmic_behavior: The active algorithm lives in `hooks/loop-plan-validator.sh`. During Case 1, `--commit-plan-file + Inside repo`, the validator first requires the plan file to be tracked with `git ls-files --error-unmatch` at `hooks/loop-plan-validator.sh:204-224`, then checks dirtiness with `git status --porcelain "$PLAN_FILE_REL_GIT"` at `hooks/loop-plan-validator.sh:226-229`. If that status is non-empty, it renders this template via `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-uncommitted.md"` at `hooks/loop-plan-validator.sh:244-246`, writes the rendered block to stderr, and exits with code `2` at `hooks/loop-plan-validator.sh:248-249`. The template supplies the remediation branch choices: commit, discard, or restart without `--commit-plan-file` in `prompt-template/block/plan-file-uncommitted.md:8-11`.
- inputs_outputs_state: Inputs are `PLAN_FILE` and `PLAN_FILE_STATUS`, injected into `{{PLAN_FILE}}` and `{{PLAN_FILE_STATUS}}` at `prompt-template/block/plan-file-uncommitted.md:5-6`. The upstream values come from loop state parsing of `plan_file` and `commit_plan_file` at `hooks/loop-plan-validator.sh:80-83`, git-top-level path resolution at `hooks/loop-plan-validator.sh:94-104`, and `git status --porcelain` at `hooks/loop-plan-validator.sh:228`. Output is a rendered Markdown error block on stderr. The template itself makes no persistent state transition, but the caller blocks prompt processing by exiting `2`; unlike some other validator branches, this dirty-plan-file branch does not call `stop_loop`, so the active loop remains recoverable after the plan file is cleaned or the user restarts/cancels.
- gates_or_invariants: The invariant encoded by this file is: when `commit_plan_file=true` and the plan file is inside the repository, the plan file must be tracked and clean before a prompt can be processed. A non-empty porcelain status for the git-relative plan path is the gate failure signal. This mirrors setup-time validation in `scripts/setup-rlcr-loop.sh:285-303`, where `--commit-plan-file` also requires tracked and clean state before the loop starts. The template’s recovery text preserves that invariant by offering only paths that make the plan clean, intentionally discard dirtiness, or change loop mode.
- dependencies_and_callers: Direct caller is `hooks/loop-plan-validator.sh:244-246`. Rendering is handled by `hooks/lib/template-loader.sh`: templates use `{{VARIABLE_NAME}}` placeholders per `hooks/lib/template-loader.sh:5-6`; replacement is single-pass to avoid placeholder injection at `hooks/lib/template-loader.sh:35-43`; `load_and_render_safe` loads the file and falls back if missing or empty at `hooks/lib/template-loader.sh:155-188`. Related sibling templates cover adjacent plan-file gates, including not-tracked, outside-repo conflict, content changed, staged, and committed cases discovered under `prompt-template/block/`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` uses the inline fallback defined at `hooks/loop-plan-validator.sh:230-242` through the fallback path in `hooks/lib/template-loader.sh:164-183`, so the gate still blocks. If `PLAN_FILE_STATUS` contains multi-line porcelain output, it is inserted into a single Markdown code span by the template at `prompt-template/block/plan-file-uncommitted.md:6`, which may be visually imperfect but preserves the diagnostic text. If the plan file is outside the repo, this template is bypassed by the outside-repo conflict branch at `hooks/loop-plan-validator.sh:173-198`. If the plan file is untracked, this template is bypassed by the not-tracked branch at `hooks/loop-plan-validator.sh:204-224`. If `git status` itself errors, the caller uses `|| true` at `hooks/loop-plan-validator.sh:228`; that can convert status-command failure to an empty status and avoid this specific block, relying on earlier path/tracking checks to catch normal invalid states.
- validation_or_tests: `tests/test-plan-file-handling.sh:370-375` asserts that `prompt-template/block/plan-file-uncommitted.md` exists for UserPromptSubmit Case 1. `tests/test-plan-file-handling.sh:760-790` validates the Case 1 path: commit-plan-file inside repo is recognized, tracked status is checked, clean status uses `git status --porcelain`, and the validator references `plan-file-uncommitted.md`. These tests are structural grep-style tests rather than full end-to-end hook execution, so they verify wiring and required checks but not rendered Markdown formatting for concrete dirty-plan statuses.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item heading present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`