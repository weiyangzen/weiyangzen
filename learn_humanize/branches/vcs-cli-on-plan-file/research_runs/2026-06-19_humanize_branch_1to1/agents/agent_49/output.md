# agent_49 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-049 `file` `prompt-template/block/plan-file-staged.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR loop’s pre-commit plan-file protection. It explains why a `git commit` is blocked when the loop was started without `--commit-plan-file` but the configured plan file is staged and would be committed. The template is a policy/error surface, not an executable script.
- algorithmic_behavior: The template has one substitution input, `{{PLAN_FILE}}`, used in the displayed path, the suggested `git reset HEAD {{PLAN_FILE}}` recovery command, and the restart command with `--commit-plan-file`; see `prompt-template/block/plan-file-staged.md:5`, `prompt-template/block/plan-file-staged.md:18`, and `prompt-template/block/plan-file-staged.md:27`. Its message defines the allowed transitions after the gate fires: unstage the plan file and retry, or cancel/restart the RLCR loop in commit-plan-file mode. It also states the stable backup invariant: `.humanize-loop.local/<timestamp>/plan-backup.md` is saved regardless of this mode at `prompt-template/block/plan-file-staged.md:29`.
- inputs_outputs_state: Input is the rendered `PLAN_FILE` value passed by the Bash validator as the git-relative plan path. The direct output is markdown emitted to stderr by the validator through `load_and_render_safe`. The state it reflects is stored in the active loop `state.md`: `commit_plan_file` and `plan_file` are read at `hooks/loop-bash-validator.sh:106-107`. No persistent state is mutated by this template; the enclosing validator exits with status `2` after rendering the block at `hooks/loop-bash-validator.sh:143-145`.
- gates_or_invariants: This block belongs to the “Block Git Commit When Plan File is Staged” gate at `hooks/loop-bash-validator.sh:100-104`. The gate only applies when an active loop exists, `commit_plan_file` is not `true`, a plan file is configured, and the intercepted command lowercases/matches `git commit`; see `hooks/loop-bash-validator.sh:109-111`. The staged-file test uses `git diff --cached --name-only` and `grep -qFx` for literal exact-line matching, preventing partial path or regex matches from triggering incorrectly; see `hooks/loop-bash-validator.sh:120-124`. Policy invariant: without `--commit-plan-file`, plan-file changes may exist as working documents but must not enter the commit.
- dependencies_and_callers: Direct caller is `hooks/loop-bash-validator.sh`, which loads the template using `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-staged.md"` at `hooks/loop-bash-validator.sh:143-144`. Template discovery is provided through `hooks/lib/loop-common.sh`, which sources `template-loader.sh` and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:11-17`. Rendering is single-pass `{{VAR}}` replacement implemented in `hooks/lib/template-loader.sh:43-116`, and safe loading falls back to inline text when the template is missing or empty at `hooks/lib/template-loader.sh:155-188`. Path normalization for the rendered `PLAN_FILE` comes from `get_relative_path`, used by the caller at `hooks/loop-bash-validator.sh:113-118` and defined in `hooks/lib/loop-common.sh:146-220`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the caller’s inline fallback still blocks the commit and displays equivalent recovery instructions through `load_and_render_safe`; see fallback behavior in `hooks/lib/template-loader.sh:164-187`. If the plan path is outside the git top-level or `git rev-parse --show-toplevel` fails, the caller falls back to a project-root-relative path before comparing staged files at `hooks/loop-bash-validator.sh:113-118`; that can avoid hard failure but may not match `git diff --cached --name-only` in unusual repository layouts. Paths with spaces are inserted unquoted in the template’s `git reset HEAD {{PLAN_FILE}}` command at `prompt-template/block/plan-file-staged.md:18`, so the displayed remediation can be unsafe to paste for whitespace-heavy paths, although setup appears to reject complex plan paths separately. The exact staged comparison means staged renames or alternative path spellings only block if the emitted staged path exactly equals `PLAN_FILE_REL_GIT`.
- validation_or_tests: `tests/test-plan-file-handling.sh` explicitly validates this gate’s wiring. Section 4b checks the Bash validator has the staged-plan pre-commit section, detects `git commit`, uses `git diff --cached --name-only`, reads `commit_plan_file`, and references `plan-file-staged.md`; see `tests/test-plan-file-handling.sh:295-334`. Section 5 checks the template file exists at `prompt-template/block/plan-file-staged.md`; see `tests/test-plan-file-handling.sh:346-351`. The template loader’s single-pass placeholder-injection behavior is covered by realistic placeholder tests in `tests/test-template-loader.sh:525-540`, relevant because `PLAN_FILE` is rendered through that loader. I did not run the test suite; the branch export is read-only, and even a `git status` check attempted to create a temp cache under `/tmp` and was blocked by sandbox permissions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-049`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`