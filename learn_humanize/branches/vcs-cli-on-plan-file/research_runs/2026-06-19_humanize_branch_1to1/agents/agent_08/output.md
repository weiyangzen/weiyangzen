# agent_08 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-008 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: `hooks/lib` is the shared algorithm layer for RLCR hook enforcement. It contains exactly two recursive descendants, `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`, totaling 18,816 bytes. `loop-common.sh` centralizes loop-state discovery, round/path classification, git ancestry checks, state-file termination, command write-detection, and standardized blocking messages. `template-loader.sh` centralizes prompt-template path resolution, safe single-pass placeholder rendering, fallback behavior, and template directory validation.
- algorithmic_behavior: `template-loader.sh` resolves the template root by moving from `hooks/lib` to `prompt-template` (`template-loader.sh:11-16`), loads templates by file path with warning-on-miss (`template-loader.sh:21-33`), and renders `{{VAR}}` placeholders in a single awk scan using environment keys prefixed as `TMPL_VAR_` (`template-loader.sh:43-117`). The single-pass scanner replaces known placeholders, preserves unknown placeholders, treats malformed `{{` literally, and avoids re-scanning inserted values (`template-loader.sh:73-114`). `load_and_render_safe` falls back when the template is missing or rendering is empty (`template-loader.sh:155-188`). `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`template-loader.sh:193-207`).
- algorithmic_behavior: `loop-common.sh` sources `template-loader.sh`, initializes `TEMPLATE_DIR`, and warns rather than fails if templates are unavailable (`loop-common.sh:11-21`). It implements loop lifecycle transitions by renaming active `state.md` to `<prefix>-state.md`, with prefixes such as `completed`, `stopped`, `unexpected`, and `cancelled` (`loop-common.sh:23-38`). It verifies HEAD ancestry through `git merge-base --is-ancestor`, but returns success when no `start_commit` exists or git is unavailable (`loop-common.sh:40-61`). It discovers only the newest `.humanize-loop.local/*/` directory with `state.md`, deliberately ignoring older “zombie” loops (`loop-common.sh:63-84`). It extracts `current_round` from YAML-like frontmatter with default `0` (`loop-common.sh:86-98`).
- algorithmic_behavior: The directory’s path and round algorithms classify files by lowercasing (`loop-common.sh:100-103`), normalizing absolute paths (`loop-common.sh:105-141`), computing portable relative paths with GNU `realpath --relative-to`, `grealpath`, or a shell fallback (`loop-common.sh:143-232`), matching `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md` patterns (`loop-common.sh:234-241`), and extracting round numbers with sed (`loop-common.sh:243-253`). It provides reusable blocking-message constructors for todos, prompt writes, state writes, summary Bash writes, goal-tracker Bash writes, and post-round-0 goal tracker edits (`loop-common.sh:255-304`, `loop-common.sh:349-361`). It detects protected paths (`goal-tracker.md`, `state.md`, `.humanize-loop.local/`) and recognizes shell write bypasses via redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv/cp`, and `dd of=` patterns (`loop-common.sh:306-347`).
- inputs_outputs_state: Inputs include hook JSON fields consumed by callers, active loop base paths, `state.md` frontmatter, `start_commit`, git repository state, template names, fallback text, and `VAR=value` substitutions. Outputs are rendered block messages on stdout/stderr, boolean shell return codes, relative paths, parsed round numbers, active loop directory paths, and renamed loop state files. Persistent state mutation is limited to `stop_loop`, which moves/removes `state.md` (`loop-common.sh:31-38`); the rest of the library is read-only and returns derived values. `template-loader.sh` has no persistent state, but uses temporary environment variables with a `TMPL_VAR_` prefix for rendering (`template-loader.sh:47-60`).
- gates_or_invariants: The newest-loop-only invariant prevents old session directories from being revived (`loop-common.sh:63-84`). Round files are valid only when filenames match `round-[0-9]+-(summary|prompt|todos).md` (`loop-common.sh:236-253`). Protected-loop files are guarded consistently across Read, Write, Edit, and Bash validators through the shared matchers and message helpers. Template rendering is intentionally single-pass so substituted content containing `{{OTHER_VAR}}` remains literal (`template-loader.sh:39-42`, `template-loader.sh:101-107`). Missing templates degrade to explicit fallbacks through `load_and_render_safe`, preventing empty block messages (`template-loader.sh:155-188`). Relative path computation must support Linux, macOS with coreutils, and shell fallback paths, including paths outside the base (`loop-common.sh:143-232`).
- dependencies_and_callers: `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `scripts/setup-rlcr-loop.sh` source or depend on `loop-common.sh`. Read validation blocks todos reads, wrong-location prompt/summary reads, wrong round reads, and wrong active-loop directory paths (`loop-read-validator.sh:36-39`, `loop-read-validator.sh:87-132`). Write/Edit validation block todos, prompt, state, and post-round-0 goal-tracker writes and validate summary rounds/locations (`loop-write-validator.sh:37-45`, `loop-write-validator.sh:85-97`, `loop-edit-validator.sh:36-44`, `loop-edit-validator.sh:72-84`). Bash validation uses ancestry checks, `stop_loop`, git-push blocking, plan-file staged detection via `get_relative_path`, and shell write-bypass detection (`loop-bash-validator.sh:53-78`, `loop-bash-validator.sh:87-148`, `loop-bash-validator.sh:155-204`). Plan validation relies on `get_relative_path`, ancestry checks, and `stop_loop` for plan-file state cases (`loop-plan-validator.sh:61-77`, `loop-plan-validator.sh:94-167`, `loop-plan-validator.sh:173-250`, `loop-plan-validator.sh:258-317`). Setup uses `get_relative_path` for plan path validation and git-root relative checks (`scripts/setup-rlcr-loop.sh:13-15`, `scripts/setup-rlcr-loop.sh:212-223`, `scripts/setup-rlcr-loop.sh:243-259`).
- edge_cases_or_failure_modes: `stop_loop` falls back to deleting `state.md` if the rename fails, preserving loop termination but possibly losing the active filename if `mv` errors (`loop-common.sh:34-37`). `check_start_commit_ancestry` silently allows execution when git is unavailable, outside a repo, or `start_commit` is blank (`loop-common.sh:46-53`). `find_active_loop` ignores older directories even if they still contain `state.md`, by design (`loop-common.sh:75-83`). `_normalize_path` removes `..` by truncating the current result and returns `/` for empty output, so purely relative inputs are intentionally outside its normal path (`loop-common.sh:113-140`). `get_relative_path` falls back to `basename` when absolute relative computation is impossible (`loop-common.sh:230-231`). `command_modifies_file` is heuristic and recognizes common write forms, but does not parse shell syntax fully (`loop-common.sh:327-347`). `template-loader.sh` keeps unknown placeholders intact, treats unclosed `{{` literally, warns on missing files, and can fall back to inline messages through safe loaders (`template-loader.sh:26-32`, `template-loader.sh:89-107`, `template-loader.sh:164-185`).
- validation_or_tests: `tests/test-template-loader.sh` covers template directory resolution, existing/missing template loading, single and multiple substitution, multiline rendering, unknown placeholder preservation, safe fallback behavior, and directory validation (`test-template-loader.sh:42-243`). It also covers shell metacharacters, Windows paths, regex/JSON-like values, multiline values, and placeholder injection prevention (`test-template-loader.sh:247-543`). `tests/test-template-references.sh` scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and verifies referenced templates exist; it explicitly includes `hooks/lib/loop-common.sh` in the checked set (`test-template-references.sh:56-65`, `test-template-references.sh:84-111`, `test-template-references.sh:150-160`). `tests/test-plan-file-handling.sh` asserts `setup-rlcr-loop.sh` uses `get_relative_path` after `PROJECT_ROOT` initialization, checks plan-file path validation, verifies exact plan-file filtering preserves similar filenames, and checks `stop_loop` / `check_start_commit_ancestry` presence and callers (`test-plan-file-handling.sh:121-147`, `test-plan-file-handling.sh:424-466`, symbol references at `test-plan-file-handling.sh:842-980`). Additional template behavior is exercised by `tests/test-templates-comprehensive.sh` and `tests/test-error-scenarios.sh`, which call `get_template_dir`, `load_template`, `render_template`, `load_and_render_safe`, and `validate_template_dir`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-008`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`