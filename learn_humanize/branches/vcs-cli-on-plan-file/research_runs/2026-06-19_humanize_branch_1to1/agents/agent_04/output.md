# agent_04 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-004 `directory` `prompt-template`
- cursor: `[_]`
- core_role: `prompt-template/` is a data-driven prompt and block-message catalog for the RLCR loop algorithm. It does not execute logic itself, but it is core because hook logic loads these Markdown templates to render user-facing gate failures, Codex review prompts, Claude next-round prompts, and plan-file policy remediation. Recursive inspection found 39 Markdown files, 674 total lines, and 27,875 bytes across `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude`.
- algorithmic_behavior: Templates are selected by hook state and rendered with `{{VAR}}` placeholders through `hooks/lib/template-loader.sh`. The loader resolves the directory relative to `hooks/lib` via `get_template_dir` (`hooks/lib/template-loader.sh:11-16`), validates that `block`, `codex`, and `claude` exist (`hooks/lib/template-loader.sh:193-206`), and renders placeholders in a single pass to avoid recursive placeholder injection (`hooks/lib/template-loader.sh:39-43`, `hooks/lib/template-loader.sh:56-114`). `block/` templates encode guard outcomes: plan-file commit policy, wrong round/path access, protected state/goal-tracker files, incomplete todos, large files, dirty git state, unpushed commits, and branch/start-commit ancestry failures. `codex/regular-review.md` requires Codex to compare the original plan, prompt, summary, docs, and goal tracker and only emit `COMPLETE` when all work is fully done with no deferrals (`prompt-template/codex/regular-review.md:20-57`). `codex/full-alignment-review.md` is the every-fifth-round audit template, adding acceptance-criteria status, forgotten-item detection, deferred-item audit, stagnation detection, and `STOP` circuit-breaker instructions (`prompt-template/codex/full-alignment-review.md:19-49`, `prompt-template/codex/full-alignment-review.md:59-93`). `codex/goal-tracker-update-section.md` is injected into both review modes and makes Codex the only post-round-0 goal-tracker updater (`prompt-template/codex/goal-tracker-update-section.md:3-17`). `claude/next-round-prompt.md` is rendered when Codex finds issues and tells Claude to read the plan, create todos for all issues, and treat `goal-tracker.md` as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1-31`); footer, push note, post-alignment, and goal-tracker-update-request fragments are appended conditionally (`prompt-template/claude/next-round-footer.md:4-9`, `prompt-template/claude/push-every-round-note.md:1-2`, `prompt-template/claude/post-alignment-action-items.md:2-7`, `prompt-template/claude/goal-tracker-update-request.md:2-16`).
- inputs_outputs_state: Inputs are template name, template directory, runtime variable assignments, hook JSON, loop `state.md`, current round number, plan file path, plan backup, git status/log output, todo transcript state, summary content, and Codex review output. Outputs are rendered Markdown either to stderr for PreToolUse/UserPromptSubmit blocks, as JSON `reason` fields for Stop-hook blocks, or as generated files such as `round-N-review-prompt.md` and `round-N+1-prompt.md`. State transitions are implemented by callers, not templates: `loop-codex-stop-hook.sh` blocks immediately for incomplete todos (`hooks/loop-codex-stop-hook.sh:67-95`), large files (`hooks/loop-codex-stop-hook.sh:104-176`), dirty git state (`hooks/loop-codex-stop-hook.sh:185-294`), accidental plan-file commits (`hooks/loop-codex-stop-hook.sh:301-340`), and unpushed commits when configured (`hooks/loop-codex-stop-hook.sh:344-375`); then it renders Codex review prompts (`hooks/loop-codex-stop-hook.sh:536-607`), runs Codex (`hooks/loop-codex-stop-hook.sh:672-699`), treats the last non-empty Codex line as completion or circuit-breaker control (`hooks/loop-codex-stop-hook.sh:814-858`), otherwise increments `current_round` and writes the next Claude prompt from `claude/` templates (`hooks/loop-codex-stop-hook.sh:865-916`).
- gates_or_invariants: The directory supports four plan-file validation cases documented and enforced by `hooks/loop-plan-validator.sh:5-21`: commit-plan-file plus inside repo requires tracked and clean plan file, no commit-plan-file allows dirty/untracked status but still requires content match, commit-plan-file plus outside repo is a configuration conflict, and outside repo without commit-plan-file still requires backup-content match. Corresponding templates include `plan-file-outside-repo-conflict.md` (`prompt-template/block/plan-file-outside-repo-conflict.md:1-13`), `plan-file-not-tracked.md` (`prompt-template/block/plan-file-not-tracked.md:1-11`), `plan-file-uncommitted.md` (`prompt-template/block/plan-file-uncommitted.md:1-11`), `plan-file-changed-prompt-block.md` (`prompt-template/block/plan-file-changed-prompt-block.md:1-19`), `plan-file-staged.md` (`prompt-template/block/plan-file-staged.md:1-29`), and `plan-file-committed.md` (`prompt-template/block/plan-file-committed.md:1-33`). Other invariants: templates must degrade through fallbacks when missing or empty (`hooks/lib/template-loader.sh:155-188`); todos files are blocked in favor of native TodoWrite (`prompt-template/block/todos-file-access.md:1-8`); prompt and state files are protected (`prompt-template/block/prompt-file-write.md:1-12`, `prompt-template/block/state-file-modification.md:1-10`); goal tracker is editable by Claude only during round 0 and otherwise requires a summary request (`prompt-template/block/goal-tracker-modification.md:1-25`); Codex review completion is strict and deferrals are not accepted as completion (`prompt-template/codex/regular-review.md:53-57`, `prompt-template/codex/full-alignment-review.md:88-93`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` sources the loader, initializes `TEMPLATE_DIR`, and exposes reusable block-message functions for todos, prompt writes, state file writes, summary bash writes, goal-tracker bash writes, and post-round-0 goal-tracker blocks (`hooks/lib/loop-common.sh:11-21`, `hooks/lib/loop-common.sh:255-363`). `hooks/loop-plan-validator.sh` consumes plan-file and branch-change block templates for UserPromptSubmit gating, including legacy state termination (`hooks/loop-plan-validator.sh:61-78`), branch/start-commit divergence when plan changes are involved (`hooks/loop-plan-validator.sh:115-165`), commit-plan-file configuration and git-status checks (`hooks/loop-plan-validator.sh:173-250`), and backup diff blocking (`hooks/loop-plan-validator.sh:258-317`). `hooks/loop-bash-validator.sh` consumes git-push, plan-file-staged, branch-changed, state, goal-tracker, prompt, summary, and todos block templates (`hooks/loop-bash-validator.sh:87-95`, `hooks/loop-bash-validator.sh:109-146`, `hooks/loop-bash-validator.sh:155-205`). `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-edit-validator.sh` consume access/path/round templates to keep agents on current loop files only (`hooks/loop-read-validator.sh:36-39`, `hooks/loop-read-validator.sh:87-132`, `hooks/loop-write-validator.sh:37-45`, `hooks/loop-write-validator.sh:85-164`, `hooks/loop-edit-validator.sh:36-44`, `hooks/loop-edit-validator.sh:72-114`). Parent coordination is through `.humanize-loop.local/<timestamp>/` files generated by setup and stop hooks; sibling coordination is through `hooks/`, `scripts/setup-rlcr-loop.sh`, and tests.
- edge_cases_or_failure_modes: Missing templates are nonfatal when callers use `load_and_render_safe`, but raw `load_template` callers may append a simple fallback or silently omit optional fragments; this is intentional graceful degradation but can hide stale template references. Unknown placeholders remain unchanged (`hooks/lib/template-loader.sh:100-107`), so a caller that forgets a variable can leak `{{VAR}}` into a block or generated prompt. Placeholder values are inserted as Markdown without escaping; this is acceptable for prompt text but means unusual paths with quotes or backticks can make suggested shell snippets inaccurate. The single-pass renderer prevents nested substitution in large injected values such as `SUMMARY_CONTENT` or `REVIEW_CONTENT`, which is important because those fields can contain arbitrary `{{...}}` text. Some child templates appear to be legacy or currently uncalled by hook code found in this tree: `prompt-template/block/plan-file-modified-warning.md`, `prompt-template/block/plan-file-changed-commit-mode.md`, and `prompt-template/block/codex-review-failed.md`; the stop hook currently uses inline Codex failure text instead of loading the Codex failure template (`hooks/loop-codex-stop-hook.sh:709-735`). These are child-level stale-copy risks, not a directory-level skip reason.
- validation_or_tests: Static test coverage exists for loader behavior, directory resolution, rendering, safe fallback, and real templates in `tests/test-template-loader.sh` (`tests/test-template-loader.sh:45-59`, `tests/test-template-loader.sh:151-216`). Comprehensive template tests load every Markdown template and exercise safe rendering and representative real templates (`tests/test-templates-comprehensive.sh:59-107`, `tests/test-templates-comprehensive.sh:446-467`, `tests/test-templates-comprehensive.sh:498-539`). Reference tests scan hook template call sites, require critical block templates, and check validators use safe rendering (`tests/test-template-references.sh:84-101`, `tests/test-template-references.sh:154-200`). Plan-file tests assert the branch-added plan-file templates exist and that validators reference them (`tests/test-plan-file-handling.sh:339-395`, `tests/test-plan-file-handling.sh:726-825`, `tests/test-plan-file-handling.sh:939`). I did not run the test scripts because this branch export is read-only and not a live git checkout; `git status` failed with no `.git` present in the export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 evidence section above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`