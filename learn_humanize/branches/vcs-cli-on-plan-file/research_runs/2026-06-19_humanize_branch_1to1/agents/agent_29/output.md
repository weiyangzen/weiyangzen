# agent_29 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-029 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: This file is an executable CI specification for the prompt-template subsystem. It validates that `prompt-template/` has the required layout, that every Markdown template can be loaded, that placeholders conform to the supported `{{UPPER_CASE}}` syntax, and that `hooks/lib/template-loader.sh` can render realistic hook messages without dropping content. The script is wired into CI by `.github/workflows/template-test.yml`, which chmods and runs it after the basic template-loader test and before template-reference validation.

- algorithmic_behavior: The script starts by resolving `SCRIPT_DIR`, `PROJECT_ROOT`, sourcing `hooks/lib/template-loader.sh`, and deriving `TEMPLATE_DIR` through `get_template_dir` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:15) and [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:59). It maintains three counters, `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS`, through local `pass`, `fail`, and `warn` helpers at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:26). Its main algorithm is sequential: validate the template directory and required subdirectories, enumerate all `*.md` templates with `find ... -print0 | sort -z`, load each file through `load_template`, scan placeholder syntax with regex gates, run known malformed-placeholder detector checks, exercise renderer edge cases, verify fallback behavior, render representative real templates, stress the renderer with 100 calls, then render all discovered templates with generated dummy assignments. The final state transition is controlled only by `TESTS_FAILED`: zero failures exits `0`, otherwise exits `1` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:619).

- inputs_outputs_state: Inputs are the repository-relative `prompt-template/` tree, `hooks/lib/template-loader.sh`, shell utilities (`find`, `sort`, `grep`, `sed`, `tr`, `wc`, `awk` indirectly through the loader, `mktemp`, `rm`), and the specific real templates `block/git-push.md`, `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md`. Outputs are human-readable colored test logs, pass/fail/warning counts, and process exit status. Runtime state is purely in shell variables except for temporary directories created by `mktemp -d` in malformed-placeholder and incomplete-directory tests at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:205) and [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:484), both removed with `rm -rf`. The script does not mutate repository files.

- gates_or_invariants: The first hard gate is that `prompt-template/` must exist; if absent, the script exits immediately at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:66). Required subdirectories are exactly `block`, `codex`, and `claude` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:74). Every Markdown template must load non-empty content through `load_template`; whitespace-only content is a warning, while load failure increments failures at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:90). Placeholder syntax gates reject extra closing braces, extra opening braces, wrong bracket types, likely unclosed uppercase placeholders, spaces inside placeholders, and lowercase placeholders at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:128). Single-brace placeholders are explicitly not enforced because the templates may document `{VAR}` syntax in prose at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:160). Rendering invariants include empty content returns empty, multiline structure and blank lines are preserved, repeated and adjacent placeholders are replaced, missing templates use fallback content, existing templates do not use fallbacks, and all discovered valid placeholders can be substituted with dummy values.

- dependencies_and_callers: The direct dependency is `hooks/lib/template-loader.sh`, especially `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. The loader computes the template root from `hooks/lib` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:11), reads templates with `cat` or returns an empty string plus warning for missing files at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:21), and performs single-pass `awk` replacement from `TMPL_VAR_` environment variables at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:43). The loader’s single-pass invariant is important because it prevents substituted values containing `{{OTHER_VAR}}` from being rescanned, documented at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:39). Consumers include `hooks/lib/loop-common.sh`, which sources the loader and validates the template directory while allowing graceful fallback at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/loop-common.sh:11), plus hook validators and stop hooks that call `load_and_render_safe` for block messages. Related test coverage includes `tests/test-template-loader.sh` for focused loader behavior, including placeholder-injection prevention at [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-template-loader.sh:499), and `tests/test-template-references.sh` for cross-checking that hook scripts reference existing templates and critical validators use safe rendering at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-template-references.sh:54).

- edge_cases_or_failure_modes: The test suite covers malformed placeholders directly with synthetic strings for `{{VAR}}}`, `{{{VAR}}}`, `{{ VAR }}`, lowercase `{{varName}}`, and valid `{{VALID_VAR}}` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:197). Rendering edge cases include empty input, single-line and multiline templates, blank-line preservation, regex-like values, backslashes, quotes, CJK-labelled values, ASCII status markers, Markdown formatting, 1000-character values, multiline values, placeholders at line boundaries, repeated placeholders, and adjacent placeholders at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:261). Some edge checks are intentionally weaker than exact equality: backslashes, quotes, Markdown, CJK, and multiline values mostly assert that rendering does not crash and contains an expected prefix, so byte-exact escaping regressions could slip through. The syntax scanner is regex-based rather than parser-based and can produce false negatives or false positives around complex prose examples; the unclosed-placeholder check even notes a double-check step at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-templates-comprehensive.sh:146). Because the script uses `set -uo pipefail` without `set -e`, most command failures inside command substitutions are manually tolerated with `|| true`; this is deliberate for grep probes, but it means unexpected nonzero commands only fail the suite if their surrounding assertion detects bad output.

- validation_or_tests: This file is itself the comprehensive validator. Its Section 1 validates directory shape, Section 2 validates loadability of every template, Section 3 validates placeholder syntax, Section 3.5 self-tests the malformed-placeholder detector, Section 4 validates rendering edge cases, Section 5 validates fallback and directory validation APIs, Section 6 runs integration checks against real templates, and Section 7 runs stress/all-template rendering checks. The CI entry point is `.github/workflows/template-test.yml`, which runs `./tests/test-templates-comprehensive.sh` after making it executable. I did not execute the suite in this research pass because the assignment requested notes only and the branch export is read-only; direct inspection also showed the script creates temporary directories during normal execution. A `git status --short` probe was attempted only to confirm no edits, but this export has no accessible `.git` metadata and the sandbox blocked macOS tool cache writes, so no repository status result was available.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`