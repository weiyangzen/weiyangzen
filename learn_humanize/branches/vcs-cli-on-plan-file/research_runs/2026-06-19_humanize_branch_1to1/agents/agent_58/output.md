# agent_58 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-058 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: This file is a prompt/block template used when RLCR loop hooks reject access to a round artifact because the requested file path is not the canonical active-loop path. The template itself is not an algorithm implementation; its role is the user-facing block contract for a path-canonicalization gate. It declares the block title, reports the attempted action and file, reports the correct path, and gives a short read command hint at `prompt-template/block/wrong-directory-path.md:1-6`.

- algorithmic_behavior: The template renders three placeholders: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`. It is loaded through `load_and_render_safe`, so normal behavior is: validator detects a wrong directory path, loads this template, substitutes placeholders in one pass, writes the rendered message to stderr, then exits with status `2` to block the tool action. The read validator reaches this template after confirming the tool is `Read`, the target is a current-round prompt or summary file, an active loop exists, the round number matches, and the raw requested path still differs from the canonical active loop path at `hooks/loop-read-validator.sh:25-31`, `hooks/loop-read-validator.sh:45-64`, `hooks/loop-read-validator.sh:103-132`. The write validator reaches it after confirming the tool is `Write`, the target is a summary or protected `.humanize-loop.local` path, an active loop exists, state/goal-tracker/wrong-round checks have passed, and the raw requested path differs from the canonical active loop path at `hooks/loop-write-validator.sh:26-31`, `hooks/loop-write-validator.sh:51-79`, `hooks/loop-write-validator.sh:130-164`.

- inputs_outputs_state: Inputs are supplied by validators as `ACTION=read` or `ACTION=write to`, the original requested `FILE_PATH`, and the computed `CORRECT_PATH`. For reads, `CORRECT_PATH` is built as `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`, where `CLAUDE_FILENAME` comes from `basename "$FILE_PATH"` at `hooks/loop-read-validator.sh:49` and `hooks/loop-read-validator.sh:122`. For writes, `CLAUDE_FILENAME` is extracted from the `.humanize-loop.local` path using `sed`, then `CORRECT_PATH` is built as `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` at `hooks/loop-write-validator.sh:118-124` and `hooks/loop-write-validator.sh:154`. Output is rendered markdown on stderr; the validators then return exit code `2`, which is the state transition from allowed action to blocked pre-tool-use action. The template does not mutate state itself. The active loop state inputs come from `find_active_loop` and `get_current_round`, which select the newest `.humanize-loop.local/*/state.md` directory and parse `current_round` from state frontmatter at `hooks/lib/loop-common.sh:63-98`.

- gates_or_invariants: The central invariant is exact path equality: `FILE_PATH` must equal `CORRECT_PATH`; otherwise the hook blocks. This is deliberately stricter than “same filename somewhere under `.humanize-loop.local`.” The read hook also enforces that round files must be in the active loop directory and match the current round before this directory-path gate runs at `hooks/loop-read-validator.sh:87-115`. The write hook first blocks todos and prompt writes, blocks state writes, blocks post-round-zero goal tracker writes, blocks summaries outside `.humanize-loop.local`, and blocks wrong summary round numbers before this directory-path gate at `hooks/loop-write-validator.sh:37-45`, `hooks/loop-write-validator.sh:85-111`, `hooks/loop-write-validator.sh:130-147`. Template rendering itself preserves the invariant that missing templates still produce feedback because both callers use `load_and_render_safe` with an inline fallback at `hooks/loop-read-validator.sh:125-131` and `hooks/loop-write-validator.sh:157-163`.

- dependencies_and_callers: Direct callers are `hooks/loop-read-validator.sh:128` and `hooks/loop-write-validator.sh:160`. Both source `hooks/lib/loop-common.sh`, which initializes `TEMPLATE_DIR` through `get_template_dir` and validates template directory structure at `hooks/lib/loop-common.sh:11-20`. Rendering depends on `hooks/lib/template-loader.sh`: `load_template` reads the markdown file, `render_template` performs single-pass `{{VAR}}` substitution, and `load_and_render_safe` falls back if the template is missing or renders empty at `hooks/lib/template-loader.sh:21-32`, `hooks/lib/template-loader.sh:43-117`, `hooks/lib/template-loader.sh:155-188`. The template reference suite explicitly scans validators and verifies referenced templates exist, including this one through the read/write validator references, at `tests/test-template-references.sh:56-111`. The comprehensive template suite also loads every markdown template, extracts placeholders, renders dummy values, and warns on unreplaced placeholders at `tests/test-templates-comprehensive.sh:562-605`.

- edge_cases_or_failure_modes: Because the equality check compares the raw requested path string to `CORRECT_PATH`, semantically equivalent paths using symlinks, relative components, or alternate absolute spelling may be blocked even if they resolve to the same file; there is no path normalization in this specific gate. For writes, `CLAUDE_FILENAME` extraction assumes a path shape containing `.humanize-loop.local/`; unusual nesting can produce a multi-segment filename and therefore a computed canonical path under the active loop. If the template is missing or empty, `load_and_render_safe` uses the fallback instead, preventing empty block feedback. One content edge case: line `prompt-template/block/wrong-directory-path.md:6` says `cat {{FILE_PATH}}`, even when the blocked path is not the canonical path and even for `ACTION=write to`; the validators’ fallback messages point at `CORRECT_PATH` instead. That makes the committed template’s final hint potentially misleading for this gate, though the gate itself still blocks correctly.

- validation_or_tests: No test appears to assert this exact rendered template text. Coverage is indirect but relevant: `tests/test-template-references.sh` scans hook references and fails if a referenced template file is missing at `tests/test-template-references.sh:84-111`; it also checks critical validators use `load_and_render_safe` rather than unsafe rendering at `tests/test-template-references.sh:173-202`. `tests/test-templates-comprehensive.sh` verifies safe fallback behavior for missing templates at `tests/test-templates-comprehensive.sh:446-463` and verifies all templates can be loaded and rendered with dummy placeholder values at `tests/test-templates-comprehensive.sh:562-605`. `tests/test-template-loader.sh` covers template substitution, special characters in path-like values, unreplaced placeholders, and safe fallback behavior at `tests/test-template-loader.sh:130-221`. I did not run the test suite because this was a read-only research task and direct inspection was sufficient for the assigned evidence.

- skip_candidate: `yes: The file is classified as a prompt/block template, not a core algorithm implementation. It participates in algorithmic validation behavior only as the rendered feedback surface for the read/write path gate. It should remain in the coverage set if prompt contract correctness is considered part of the hook algorithm, but it is a skip candidate for implementation-level algorithm review.`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-058`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`