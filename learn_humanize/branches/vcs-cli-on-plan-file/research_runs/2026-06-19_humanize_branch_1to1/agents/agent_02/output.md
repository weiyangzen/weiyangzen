# agent_02 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-002 `directory` `commands`
- cursor: `[_]`
- core_role: `commands/` is the Claude Code slash-command surface for the Humanize RLCR loop. Recursive inspection found no subdirectories and exactly two command descriptors, totaling 3,402 bytes: `commands/start-rlcr-loop.md` and `commands/cancel-rlcr-loop.md`. The directory does not hold the shell implementation itself; it is the user-facing dispatch layer that binds slash command metadata, allowed tool boundaries, arguments, and operational instructions to sibling scripts and hooks. `README.md:188-209` documents the same command pair and options, while `README.md:254-280` places `commands/` as the plugin command folder coordinating with `hooks/` and `scripts/`.

- algorithmic_behavior: `commands/start-rlcr-loop.md` declares the loop start command with argument contract `<path/to/plan.md> [--max N] [--codex-model MODEL:EFFORT] [--codex-timeout SECONDS] [--push-every-round]` at `commands/start-rlcr-loop.md:1-5`, then delegates execution to `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at `commands/start-rlcr-loop.md:10-14`. Its algorithmic role is to bootstrap an iterative state machine: Claude implements from a plan, writes a summary, the Stop hook sends the summary to Codex, Codex either returns issues and advances another round or emits `COMPLETE` to finish; this is summarized at `commands/start-rlcr-loop.md:16-23` and `commands/start-rlcr-loop.md:51-55`. The command also introduces the Goal Tracker protocol with immutable goal/acceptance criteria and mutable task/evolution sections at `commands/start-rlcr-loop.md:24-42`. `commands/cancel-rlcr-loop.md` is the cancellation command descriptor: it checks for `.humanize-loop.local/*/state.md`, reports no loop when absent, and when present renames active `state.md` files to `cancelled-state.md` rather than deleting them, preserving artifacts and allowing manual restart; see `commands/cancel-rlcr-loop.md:9-28`.

- inputs_outputs_state: Start inputs are the plan file path and options accepted by the setup script. The command descriptor passes raw slash arguments to `scripts/setup-rlcr-loop.sh`; that script initializes defaults and parses `PLAN_FILE`, `MAX_ITERATIONS`, `CODEX_MODEL`, `CODEX_EFFORT`, `CODEX_TIMEOUT`, `PUSH_EVERY_ROUND`, and `COMMIT_PLAN_FILE` at `scripts/setup-rlcr-loop.sh:31-38` and `scripts/setup-rlcr-loop.sh:102-170`. Start outputs are a new `.humanize-loop.local/<timestamp>/` session containing `state.md`, `plan-backup.md`, `goal-tracker.md`, and `round-0-prompt.md`; the state file schema includes `current_round`, `max_iterations`, Codex config, push/plan-file flags, absolute `plan_file`, `plan_file_tracked`, `start_commit`, and `started_at` at `scripts/setup-rlcr-loop.sh:309-340`. The setup path backs up the plan and records `START_COMMIT` at `scripts/setup-rlcr-loop.sh:317-320`, creates the Goal Tracker from plan heuristics at `scripts/setup-rlcr-loop.sh:346-434`, and emits the initial implementation prompt at `scripts/setup-rlcr-loop.sh:440-493`. Cancel input is the presence of one or more state files; output is state transition from active `state.md` to `cancelled-state.md` via the `mv` loop shown at `commands/cancel-rlcr-loop.md:21-24`. Runtime active-loop discovery in hooks uses only the newest timestamp directory with `state.md`, from `hooks/lib/loop-common.sh:63-84`.

- gates_or_invariants: The command directory itself enforces a narrow tool boundary through command frontmatter: start only allows the setup script (`commands/start-rlcr-loop.md:3-5`), while cancel allows state-file listing/reading and the specific state-file loop command shape (`commands/cancel-rlcr-loop.md:1-4`). Deeper gates are implemented by referenced siblings. Setup requires exactly one plan file, existence, at least five lines, a git repository with a commit, simple path characters, and installed `codex`; see `scripts/setup-rlcr-loop.sh:176-241`. With `--commit-plan-file`, the plan must be inside the git repository, not ignored, tracked, and clean (`scripts/setup-rlcr-loop.sh:251-303`). The UserPromptSubmit hook revalidates plan-file invariants before each prompt: it covers four plan modes in `hooks/loop-plan-validator.sh:5-22`, terminates incompatible legacy state with `unexpected-state.md` at `hooks/loop-plan-validator.sh:59-78`, checks branch ancestry at `hooks/loop-plan-validator.sh:115-167`, rejects `--commit-plan-file` outside-repo conflict at `hooks/loop-plan-validator.sh:173-198`, requires tracked/clean plan in commit mode at `hooks/loop-plan-validator.sh:204-250`, and blocks when the plan differs from `plan-backup.md` at `hooks/loop-plan-validator.sh:258-317`. Stop-loop termination uses prefixed state files via `stop_loop` in `hooks/lib/loop-common.sh:23-38`; prefixes distinguish completed, stopped, unexpected, and cancelled state. The Bash validator blocks push unless configured, staged plan-file commits when `--commit-plan-file` is false, state-file edits, prompt/summary/todos shell writes, and improper Goal Tracker edits at `hooks/loop-bash-validator.sh:80-205`.

- dependencies_and_callers: Parent plugin metadata is `.claude-plugin/plugin.json`, which identifies the Humanize plugin and keywords at `.claude-plugin/plugin.json:1-18`. Claude Code discovers command markdown under `commands/`; `start-rlcr-loop.md` depends on `CLAUDE_PLUGIN_ROOT` and `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:4` and `commands/start-rlcr-loop.md:13`). The setup script sources `hooks/lib/loop-common.sh` for shared path/state helpers at `scripts/setup-rlcr-loop.sh:13-16`. `hooks/hooks.json` registers lifecycle enforcement around the state created by the start command: `UserPromptSubmit` invokes `loop-plan-validator.sh` at `hooks/hooks.json:4-13`, `PreToolUse` routes Write/Edit/Read/Bash validators at `hooks/hooks.json:14-50`, and `Stop` invokes `loop-codex-stop-hook.sh` with a 7200 second timeout at `hooks/hooks.json:52-59`. The Stop hook consumes `state.md`, summaries, prompts, templates, Codex CLI, and cache files: it checks incomplete todos at `hooks/loop-codex-stop-hook.sh:61-96`, large changed files at `hooks/loop-codex-stop-hook.sh:98-177`, clean git/push/plan-file commit conditions at `hooks/loop-codex-stop-hook.sh:179-377`, builds regular or full-alignment Codex prompts at `hooks/loop-codex-stop-hook.sh:527-608`, runs `codex exec` at `hooks/loop-codex-stop-hook.sh:614-699`, and performs complete/stop/continue transitions at `hooks/loop-codex-stop-hook.sh:820-930`.

- edge_cases_or_failure_modes: Start fails before state creation when the plan is missing, too short, has unsupported characters, the workspace is not a git repo or has no commit, `codex` is unavailable, or `--commit-plan-file` conflicts with repo location/tracking/cleanliness (`scripts/setup-rlcr-loop.sh:176-303`). The start command passes `$ARGUMENTS` unquoted at `commands/start-rlcr-loop.md:13`; setup intentionally rejects spaces and shell-sensitive characters later at `scripts/setup-rlcr-loop.sh:215-223`, but shell word splitting can surface as multiple-argument or missing-file failures before that validation is user-friendly. Cancel is instruction-driven: it first lists state files and only then runs the `for` loop (`commands/cancel-rlcr-loop.md:11-24`); if the loop body were run without a matching glob, ordinary shell behavior could attempt to move the literal pattern. Cancel renames all matching `.humanize-loop.local/*/state.md` files, while runtime `find_active_loop` considers only the newest timestamp directory (`hooks/lib/loop-common.sh:63-84`), so cancellation is broader than active-loop discovery. State preservation is normally via rename, but `stop_loop` falls back to `rm -f` if `mv` fails (`hooks/lib/loop-common.sh:34-37`), which is a failure-mode tradeoff outside the command descriptor. Plan-file content drift is blocked even when the plan is intentionally uncommitted, because the active loop compares it to `plan-backup.md` before prompt processing (`hooks/loop-plan-validator.sh:258-317`). Branch/ancestry changes can rename the state to `unexpected-state.md`, ending the loop before further work (`hooks/loop-plan-validator.sh:115-167`, `hooks/loop-bash-validator.sh:49-78`). In this read-only branch export, `git status --short` could not be used as repository validation because the export has no `.git` and the managed sandbox prevented temp-cache creation; evidence here is static source inspection.

- validation_or_tests: Focused coverage exists in `tests/test-plan-file-handling.sh`. It asserts command/setup help exposes `--commit-plan-file`, state schema fields, backup creation, tracked/clean checks, `git ls-files`, git-ignore checks, and path validation at `tests/test-plan-file-handling.sh:57-180`. It asserts the UserPromptSubmit hook is registered, handles pre-1.1.2 state, determines inside/outside repo, covers `--commit-plan-file` outside-repo conflict, checks tracked/clean plan files, diffs the plan against backup, handles missing plans, and exits with blocking semantics at `tests/test-plan-file-handling.sh:688-820`. It asserts `stop_loop`, prefix state renaming, ancestry checks in plan/bash validators, stop hook use of `stop_loop`, and cancel’s `cancelled-state.md` behavior without `rm state.md` at `tests/test-plan-file-handling.sh:840-934`. Static inspection also confirms `commands/cancel-rlcr-loop.md` contains `cancelled-state.md` at `commands/cancel-rlcr-loop.md:21-28`. Tests were not executed in this worker run because the branch export is read-only and the task requested research notes only.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`