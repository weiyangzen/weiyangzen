# agent_46 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-046 `file` `prompt-template/block/plan-file-modified-warning.md`
- cursor: `[_]`
- core_role: This file is a Markdown prompt/block template for warning a user that the RLCR plan file changed after loop start. It is algorithm-adjacent because it describes the recovery contract for the plan-file freshness gate, but it is not itself executable logic. The active gate logic lives in [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:253), which compares the active plan file to the loop-start backup before allowing prompt processing.
- algorithmic_behavior: The template presents a blocking warning headed “Warning: Plan File Modified” and explains that the current plan differs from the loop-start backup. Its recovery choices are: restart the loop with the current plan, overwrite the backup with the current plan and start a new loop, or restore the original plan from backup. The actionable command transitions are embedded in the template at [prompt-template/block/plan-file-modified-warning.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/prompt-template/block/plan-file-modified-warning.md:10). The file’s intended behavior is message rendering, not validation: it assumes some caller has already detected a plan/backup mismatch.
- inputs_outputs_state: Inputs are template variables `{{PLAN_FILE}}`, `{{PLAN_BACKUP_FILE}}`, and `{{TRACKING_STATUS}}` at [prompt-template/block/plan-file-modified-warning.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/prompt-template/block/plan-file-modified-warning.md:5). Output is rendered Markdown suitable for stderr or a prompt-blocking response. State transitions described by the message are manual and file-based: copying `PLAN_FILE` to `PLAN_BACKUP_FILE` accepts the changed plan as the new baseline, while copying `PLAN_BACKUP_FILE` to `PLAN_FILE` restores the original baseline. In the active implementation, the backup is created during loop setup by copying the plan to `.humanize-loop.local/<timestamp>/plan-backup.md` in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:317), and the plan file path plus `commit_plan_file` state are recorded in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:326).
- gates_or_invariants: The relevant invariant is that the plan content used by the loop must match the loop-start backup before prompt processing continues. The active UserPromptSubmit hook locates the plan and backup, checks missing/deleted state, and runs `diff -q "$PLAN_FILE" "$PLAN_BACKUP_FILE"` to decide whether to block at [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:258). If unchanged, the hook exits successfully; if modified or missing, it renders a block message and exits `2` at [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:311). The README documents this invariant as “Plan file content changes are detected before prompt processing” in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/README.md:235).
- dependencies_and_callers: Direct caller evidence was not found for this exact template. A repository search only found `plan-file-modified-warning.md` referenced by itself and its `TRACKING_STATUS` placeholder is not supplied elsewhere. The registered active hook is `loop-plan-validator.sh` under the `UserPromptSubmit` hook in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/hooks.json:4). That hook sources shared template loading via [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/loop-common.sh:11). The active sibling template for this behavior is `prompt-template/block/plan-file-changed-prompt-block.md`, loaded by [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:311). Compared with this assigned file, the active sibling supports `STATUS_TEXT` for both modified and missing/deleted plans and offers restart, restore, or cancel options.
- edge_cases_or_failure_modes: If this assigned template were used directly, unresolved placeholders would be visible unless the renderer supplied all three variables, especially `TRACKING_STATUS`, which currently has no discovered provider. The “continue with the new plan” option copies the changed plan over the backup, which mutates the freshness baseline outside the active validator’s documented restart/restore/cancel flow. Quoting protects paths in the `cp` examples, but the restart command passes `{{PLAN_FILE}}` unquoted, which may be weak for paths with spaces. That said, the README states plan paths must avoid spaces and several special characters, reducing this path-escaping risk in supported usage.
- validation_or_tests: The direct assigned template is not asserted by the current tests. The tests assert the active replacement path instead: `plan-file-changed-prompt-block.md` must exist, the validator must compare plan and backup with `diff`, and the validator must load the changed-plan prompt block at [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:824). The same test section verifies UserPromptSubmit registration, Case 1/3 plan-file gates, missing-plan handling, stderr output, and exit code `2` blocking behavior in [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:688).
- skip_candidate: `yes: this file appears to be a stale or legacy prompt block rather than active core algorithm surface. The current plan-file freshness gate renders the sibling changed-plan template, and no direct caller or test reference was found for this exact filename or its TRACKING_STATUS placeholder.`

## Worker Self-Test
- assigned_items_seen: 1 item evidence heading present; the assigned id is intentionally not repeated here to preserve exactly-once occurrence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`