# agent_14 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-014 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: This is a Stop-hook preflight validator for the RLCR loop. Its job is to read Claude Code’s transcript, locate the most recent native `TodoWrite` call, and fail the stop attempt before expensive Codex review if any current todo is not marked `completed`. It is wired into the RLCR Stop path by `hooks/loop-codex-stop-hook.sh:67-95`, which runs it after confirming an active loop exists and before later git, summary, and Codex-review gates. The Stop hook itself is registered in `hooks/hooks.json:52-58`.
- algorithmic_behavior: The main parser is `find_latest_todos(transcript_path)` at `hooks/check-todos-from-transcript.py:17-84`. It streams the transcript as JSONL, strips blank lines, skips malformed JSON records, and keeps overwriting `latest_todos` with the last non-empty `todos` array found in a `TodoWrite` tool call. It recognizes three transcript shapes: Claude assistant messages with `message.content` tool blocks at `hooks/check-todos-from-transcript.py:48-60`, alternate `type: message` records at `hooks/check-todos-from-transcript.py:62-73`, and direct `type: tool_use` records with either `name` or `tool_name` at `hooks/check-todos-from-transcript.py:75-82`. The executable entrypoint reads hook JSON from stdin, extracts `transcript_path`, expands `~`, and delegates to the parser at `hooks/check-todos-from-transcript.py:87-103`. It then classifies every todo with `status != "completed"` as incomplete at `hooks/check-todos-from-transcript.py:109-116`.
- inputs_outputs_state: Input is a JSON object on stdin containing `transcript_path`; absent or invalid hook input is treated as no todo evidence and exits successfully at `hooks/check-todos-from-transcript.py:89-97`. The transcript input is a filesystem path to JSONL records, not a persistent RLCR state file. Output is process status plus optional stdout: exit `0` means no current todos were found or all found todos are completed; exit `1` means incomplete todos were found and stdout begins with `INCOMPLETE_TODOS`, followed by formatted `  - [status] content` lines at `hooks/check-todos-from-transcript.py:117-122`. The script itself has no durable state transition. The transition is indirect: `hooks/loop-codex-stop-hook.sh:74-95` turns exit `1` into a Claude Code hook JSON block decision using `prompt-template/block/incomplete-todos.md:1-12`, keeping the loop from moving into Codex review or final stop. Exit `0` lets the Stop hook continue to later RLCR gates.
- gates_or_invariants: The core invariant is that a worker/Claude session must clear native `TodoWrite` work before it can leave the RLCR loop and trigger review. The validator is intentionally fail-open for missing hook input, missing `transcript_path`, missing transcript files, no detected todos, blank lines, and malformed transcript lines; see `hooks/check-todos-from-transcript.py:22-36` and `hooks/check-todos-from-transcript.py:91-107`. It is strict only once it has a non-empty latest todo list: every item must have exactly `status == "completed"` at `hooks/check-todos-from-transcript.py:111-115`. The surrounding RLCR system reinforces this by blocking direct file-style todos and instructing agents to use native TodoWrite instead; the shared helper text is in `hooks/lib/loop-common.sh:255-264`, and the incomplete-todos block tells the user to mark each todo completed via TodoWrite at `prompt-template/block/incomplete-todos.md:7-10`.
- dependencies_and_callers: Direct dependencies are only Python standard-library modules: `json`, `sys`, and `pathlib.Path` at `hooks/check-todos-from-transcript.py:12-14`. Runtime dependency is `python3`, invoked by `hooks/loop-codex-stop-hook.sh:71`. The only direct caller found is `hooks/loop-codex-stop-hook.sh`, where `TODO_CHECKER` points at this file and command output is captured with stderr merged into stdout at `hooks/loop-codex-stop-hook.sh:67-72`. The caller then extracts everything after the marker line with `tail -n +2` and renders `block/incomplete-todos.md` at `hooks/loop-codex-stop-hook.sh:74-84`. The hook registration path is `.claude-plugin` to `hooks/hooks.json`, with Stop invoking `loop-codex-stop-hook.sh`. README lists this helper as part of the lifecycle hook set at `README.md:265-273`.
- edge_cases_or_failure_modes: A later `TodoWrite` call with an empty `todos` array is ignored because the parser only updates `latest_todos` when `if todos:` is truthy, so an earlier non-empty list can remain authoritative. A malformed todo object inside the todos list can raise at `todo.get(...)` because item shape is not validated. A transcript permission/read error is not caught. The docstring says incomplete details go to stderr at `hooks/check-todos-from-transcript.py:6-7`, but implementation prints to stdout at `hooks/check-todos-from-transcript.py:118-121`; the caller captures both streams, so behavior still works, but the documentation is stale. If `python3` is unavailable, the caller sees a non-1 exit and does not take the incomplete-todo block path, so the todo gate can be bypassed by runtime failure. The caller also reuses `TODO_EXIT=${TODO_EXIT:-0}` at `hooks/loop-codex-stop-hook.sh:71-72`; in normal hook execution this is fine, but an inherited non-empty `TODO_EXIT` environment value could misclassify a successful checker result. This helper does not validate plan-file checklist cursor states; it is specifically about Claude Code native TodoWrite completion before stop/review.
- validation_or_tests: I found no dedicated repository test that executes `hooks/check-todos-from-transcript.py` directly. Related coverage is indirect: `tests/test-plan-file-handling.sh:661-672` checks the Stop hook follows Claude Code’s allow/block JSON convention, and `tests/test-template-references.sh:153-165` checks common templates but does not include this specific incomplete-todos template in that array. I performed read-only dry checks for invalid/no `transcript_path` input and a definitely missing transcript path; both returned exit `0`, matching `hooks/check-todos-from-transcript.py:91-107`. I did not create fixture files because this branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`