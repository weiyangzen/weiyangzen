# agent_48 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-048 `file` `prompt-template/block/plan-file-outside-repo-conflict.md`
- cursor: `[_]`
- core_role: This is the rendered user-facing block for the plan-file configuration-conflict gate: `--commit-plan-file` is enabled while the configured plan file is outside the git repository. The template is intentionally terminal: it tells the operator the loop cannot continue and offers only restart/configuration fixes. See [plan-file-outside-repo-conflict.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/prompt-template/block/plan-file-outside-repo-conflict.md:1).
- algorithmic_behavior: The file itself contains no executable code, but it defines the review/validation contract emitted by the prompt validator when Case 3 is detected. It presents the error state, interpolates the absolute plan path and computed relative path, states that setup should have caught it, and narrows remediation to either moving the plan file into the repository or restarting without `--commit-plan-file`. The primary hook branch is in [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:169): when `COMMIT_PLAN_FILE == true` and `PLAN_FILE_INSIDE_REPO == false`, it renders this block and exits with code `2`.
- inputs_outputs_state: Inputs are template variables `{{PLAN_FILE}}` and `{{PLAN_FILE_REL}}` at [plan-file-outside-repo-conflict.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/prompt-template/block/plan-file-outside-repo-conflict.md:5). The hook derives `PLAN_FILE` and `COMMIT_PLAN_FILE` from the active loop state file, computes `PLAN_FILE_REL` relative to `PROJECT_ROOT`, computes `PLAN_FILE_REL_GIT` relative to `git rev-parse --show-toplevel`, and chooses `DISPLAY_REL="${PLAN_FILE_REL_GIT:-$PLAN_FILE_REL}"` before rendering. Output is a markdown diagnostic written to stderr; runtime state transition is prompt processing blocked by `exit 2`, with no loop-state mutation in this specific branch.
- gates_or_invariants: The invariant is that commit-managed plan files must be inside the git worktree so git tracking, cleanliness checks, and commit inclusion rules can be enforced. The same invariant is enforced earlier at setup in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:251), which rejects empty git-relative paths or paths beginning with `../` when `COMMIT_PLAN_FILE` is true. The prompt hook keeps a defensive gate for stale/manual state edits, matching the README behavior that this configuration is an early failure and may be surfaced later only if detected from state.
- dependencies_and_callers: Direct caller is `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-outside-repo-conflict.md" ...` in [hooks/loop-plan-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-plan-validator.sh:192). Rendering depends on `load_and_render_safe`, which falls back to inline text if the template is missing or empty, as defined in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/template-loader.sh:152). Path classification depends on `get_relative_path`, sourced through [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/loop-common.sh:11) and implemented portably in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/lib/loop-common.sh:143). Related setup behavior is documented in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/README.md:211).
- edge_cases_or_failure_modes: If setup works normally, this template should be unreachable because setup rejects the invalid configuration before loop state is created. It remains important for corrupted/manual state, old or hand-edited loop state, or a plan path that becomes outside the current git toplevel after environment/checkout changes. If `git rev-parse --show-toplevel` fails, `PLAN_FILE_INSIDE_REPO` remains false, so commit-plan mode blocks rather than allowing unverifiable commit tracking. If the template is missing or renders empty, `load_and_render_safe` emits the hook’s fallback message instead; that fallback includes a slightly richer “Relative to git root” label and cancel-loop command, while the template labels the same value as “Relative path.”
- validation_or_tests: Tests explicitly assert this template exists in [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:386). The setup-side invariant is covered by a grep-based Case 2.2/8.3 test that verifies outside-repo rejection text is present in `scripts/setup-rlcr-loop.sh` at [tests/test-plan-file-handling.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/tests/test-plan-file-handling.sh:637). I did not execute the test suite because this branch export is read-only and the request is research-notes-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-048`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`