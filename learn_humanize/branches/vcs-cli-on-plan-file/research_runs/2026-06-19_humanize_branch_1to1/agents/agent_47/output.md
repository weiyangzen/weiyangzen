# agent_47 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-047 `file` `prompt-template/block/plan-file-not-tracked.md`
- cursor: `[_]`
- core_role: This is a UserPromptSubmit blocking-message template for the RLCR loop plan-file gate. It is rendered when a loop was started with `--commit-plan-file`, the configured plan file is inside the git repository, and git reports that the plan file is not tracked. The template itself is not executable logic, but it is part of the user-facing gate contract for the plan-file algorithm: it tells the user why prompt execution is blocked and presents the two valid transitions, either track and commit the plan file or cancel/restart without commit-plan-file mode. Direct file content is at `prompt-template/block/plan-file-not-tracked.md:1-11`.
- algorithmic_behavior: The template declares the blocking condition at `prompt-template/block/plan-file-not-tracked.md:3`, interpolates the offending plan path via `{{PLAN_FILE}}` at `prompt-template/block/plan-file-not-tracked.md:5`, and provides remediation commands at `prompt-template/block/plan-file-not-tracked.md:7-11`. The active algorithm lives in `hooks/loop-plan-validator.sh`: it reads `plan_file` and `commit_plan_file` from loop state at `hooks/loop-plan-validator.sh:80-82`, computes whether the plan is inside the git root at `hooks/loop-plan-validator.sh:94-103`, enters Case 1 when commit-plan-file is true and the file is inside the repo at `hooks/loop-plan-validator.sh:200-204`, then runs `git ls-files --error-unmatch "$PLAN_FILE_REL_GIT"` at `hooks/loop-plan-validator.sh:205-206`. If that check fails, it renders this template with `PLAN_FILE=$PLAN_FILE`, writes the result to stderr, and exits with code 2 at `hooks/loop-plan-validator.sh:219-223`. This produces a hard prompt-submission stop before further loop work runs.
- inputs_outputs_state: Inputs are the loop state file fields `plan_file` and `commit_plan_file`, the repository root from `git rev-parse --show-toplevel`, the git-relative plan path `PLAN_FILE_REL_GIT`, and the template variable `PLAN_FILE`. The template output is markdown sent to stderr as the block reason by `loop-plan-validator.sh`. The relevant state transition is negative: it does not update files or loop state for this case; it blocks prompt processing by exiting 2. In contrast, other fatal plan-file cases may call `stop_loop`, but the not-tracked case only emits the rendered reason and stops the current prompt. During setup, the same invariant is checked in `scripts/setup-rlcr-loop.sh`: `PLAN_FILE_TRACKED` is computed at `scripts/setup-rlcr-loop.sh:276-283`, and commit-plan-file mode exits with an error if the value is not true at `scripts/setup-rlcr-loop.sh:285-292`.
- gates_or_invariants: The invariant is: commit-plan-file mode requires the plan file to be inside the git repository, not ignored, tracked by git, and clean before loop work proceeds. This specific template covers the tracked-by-git subgate only. The broader setup gate first rejects outside-repo plan files at `scripts/setup-rlcr-loop.sh:251-260`, rejects git-ignored plan files at `scripts/setup-rlcr-loop.sh:263-268`, then rejects untracked files at `scripts/setup-rlcr-loop.sh:285-292`. The prompt-time gate mirrors the tracked/clean requirements after determining the plan file location: the untracked path renders this template at `hooks/loop-plan-validator.sh:207-220`; the dirty tracked path renders the sibling `plan-file-uncommitted.md` template at `hooks/loop-plan-validator.sh:226-249`. The invariant prevents a mode that promises plan-file commits from operating on a plan that cannot be committed.
- dependencies_and_callers: Direct caller is `hooks/loop-plan-validator.sh`, specifically the `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-not-tracked.md"` call at `hooks/loop-plan-validator.sh:219`. Template loading is wired through `hooks/lib/loop-common.sh`, which sources `template-loader.sh` at `hooks/lib/loop-common.sh:11-13`, initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:15-16`, and warns without failing if template validation fails at `hooks/lib/loop-common.sh:18-21`. The rendering helper is `load_and_render_safe`; search results show it falls back to inline fallback rendering when a template is unavailable, with relevant definitions in `hooks/lib/template-loader.sh:153-180`. The functional git dependency is `git ls-files --error-unmatch`, used both in prompt validation at `hooks/loop-plan-validator.sh:205-206` and setup validation at `scripts/setup-rlcr-loop.sh:279`. Sibling templates encode adjacent plan-file states: `plan-file-outside-repo-conflict.md`, `plan-file-uncommitted.md`, `plan-file-changed-prompt-block.md`, and `plan-file-changed-commit-mode.md`.
- edge_cases_or_failure_modes: If the plan file path is outside the git root, this template is bypassed and the outside-repo conflict template is used instead at `hooks/loop-plan-validator.sh:173-197`. If the plan file is tracked but dirty, this template is bypassed and `plan-file-uncommitted.md` is used at `hooks/loop-plan-validator.sh:226-249`. If the template file is missing or the template directory fails validation, the caller still has an inline fallback with equivalent wording at `hooks/loop-plan-validator.sh:207-217`, rendered through `load_and_render_safe`, so the gate can still block. A minor UX edge is that the template uses the absolute or state-derived `PLAN_FILE` value, while setup-time remediation uses `PLAN_FILE_REL`; this is consistent with prompt-time state display but may produce longer shell commands for users. Paths with spaces or shell-sensitive characters are partly mitigated in setup by path validation at `scripts/setup-rlcr-loop.sh:216-221`; prompt-time rendering wraps the plan path in single quotes in the suggested `git add` command at `prompt-template/block/plan-file-not-tracked.md:8`.
- validation_or_tests: `tests/test-plan-file-handling.sh` asserts this template exists for UserPromptSubmit Case 1 at `tests/test-plan-file-handling.sh:378-384`. The same test suite verifies the plan validator handles commit-plan-file plus inside-repo Case 1 at `tests/test-plan-file-handling.sh:760-766`, checks that tracked status is tested with `git ls-files --error-unmatch` at `tests/test-plan-file-handling.sh:768-773`, checks the clean-status gate at `tests/test-plan-file-handling.sh:775-780`, and verifies that `loop-plan-validator.sh` references `plan-file-not-tracked.md` at `tests/test-plan-file-handling.sh:782-786`. Setup-time coverage also checks that the setup script uses `git ls-files` for tracked status and requires clean tracked plan files when commit-plan-file is set, with related assertions around `tests/test-plan-file-handling.sh:136-146` and `tests/test-plan-file-handling.sh:619-632`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: VCS_CLI_ON_PLAN_FILE-HZ-047
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`