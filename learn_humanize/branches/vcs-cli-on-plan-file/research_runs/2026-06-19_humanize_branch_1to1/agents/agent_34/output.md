# agent_34 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-034 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: This is a conditional stop-hook prompt block for the RLCR loop’s git-clean gate. It is not executable code, but it participates in the core stop decision by explaining the special handling for `.humanize-loop.local/` when the repository has uncommitted changes. The block tells the agent that `.humanize-loop.local/` is generated by `humanize:start-rlcr-loop`, must not be committed, and should be ignored via `.gitignore`; see `prompt-template/block/git-not-clean-humanize-local.md:2` through `prompt-template/block/git-not-clean-humanize-local.md:8`.
- algorithmic_behavior: The stop hook computes `GIT_STATUS` with `git status --porcelain`, optionally filters the configured plan file out of that status, and treats any remaining status as a git-not-clean blocker; see `hooks/loop-codex-stop-hook.sh:227` through `hooks/loop-codex-stop-hook.sh:248`. It then extracts untracked entries, detects `.humanize-loop.local` with `grep`, loads this template using `load_template`, and appends it into `SPECIAL_NOTES`; see `hooks/loop-codex-stop-hook.sh:250` through `hooks/loop-codex-stop-hook.sh:260`. The final block reason is rendered from `prompt-template/block/git-not-clean.md`, where `{{SPECIAL_NOTES}}` is inserted after the detected git issue; see `prompt-template/block/git-not-clean.md:1` through `prompt-template/block/git-not-clean.md:5` and `hooks/loop-codex-stop-hook.sh:273` through `hooks/loop-codex-stop-hook.sh:283`. The state transition is therefore: dirty git status detected -> special untracked loop directory classified -> advisory note injected -> stop request blocked until real changes are committed or ignored.
- inputs_outputs_state: Inputs are the filtered porcelain git status, specifically untracked lines containing `.humanize-loop.local`, plus the template directory resolved through the template loader. The block itself has no placeholders, so it is loaded as static markdown. Its output is appended to the `SPECIAL_NOTES` variable and becomes part of the JSON `reason` emitted by the stop hook’s block decision; see `hooks/loop-codex-stop-hook.sh:255` through `hooks/loop-codex-stop-hook.sh:259` and `hooks/loop-codex-stop-hook.sh:285` through `hooks/loop-codex-stop-hook.sh:292`. It also provides concrete follow-up commands: append `.humanize*local*` to `.gitignore` and stage `.gitignore`; see `prompt-template/block/git-not-clean-humanize-local.md:4` through `prompt-template/block/git-not-clean-humanize-local.md:8`.
- gates_or_invariants: The git-clean gate remains blocking whenever filtered git status is non-empty; this template only changes the guidance text, not the block/pass decision. The invariant is that `.humanize-loop.local/` is loop runtime state, not a commit candidate, consistent with README documentation that loop artifacts live under `.humanize-loop.local/<timestamp>/`; see `README.md:75` and `README.md:285` through `README.md:290`. Another invariant is fallback safety: if this template is missing or empty, the stop hook falls back to a short note rather than failing the hook; see `hooks/loop-codex-stop-hook.sh:255` through `hooks/loop-codex-stop-hook.sh:258`. Template loading itself returns file contents when present and an empty string with a warning when absent; see `hooks/lib/template-loader.sh:21` through `hooks/lib/template-loader.sh:32`.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which references `block/git-not-clean-humanize-local.md` in the special-case untracked-file branch; see `hooks/loop-codex-stop-hook.sh:253` through `hooks/loop-codex-stop-hook.sh:260`. It depends on `hooks/lib/template-loader.sh` for loading, especially `load_template`; see `hooks/lib/template-loader.sh:18` through `hooks/lib/template-loader.sh:33`. It composes with sibling template `prompt-template/block/git-not-clean.md`, which owns the surrounding “Git Not Clean” block and required actions, and with `prompt-template/block/git-not-clean-untracked.md`, which is separately appended for other untracked files; see `hooks/loop-codex-stop-hook.sh:262` through `hooks/loop-codex-stop-hook.sh:270`. The broader runtime dependency is the RLCR loop artifact directory created by setup and used by validators/monitoring; examples include `scripts/setup-rlcr-loop.sh:309`, `hooks/loop-read-validator.sh:57`, and `scripts/humanize.sh:10`.
- edge_cases_or_failure_modes: If `.humanize-loop.local` is the only untracked entry, the stop hook still blocks because `FILTERED_GIT_STATUS` is non-empty, but the reason includes this special note telling the agent to ignore it rather than commit it. If other untracked files exist at the same time, both this note and the generic untracked-files note can be appended because `OTHER_UNTRACKED` filters out `.humanize-loop.local` and handles the remainder separately; see `hooks/loop-codex-stop-hook.sh:262` through `hooks/loop-codex-stop-hook.sh:269`. The detection is substring-based on untracked lines, so nested or oddly named paths containing `.humanize-loop.local` may trigger the same note. The recommended ignore pattern `.humanize*local*` is broad and may ignore similarly named local artifacts beyond the exact `.humanize-loop.local/` directory. If the template is missing, the stop hook silently degrades to `Note: .humanize-loop.local/ is intentionally untracked.`; see `hooks/loop-codex-stop-hook.sh:256` through `hooks/loop-codex-stop-hook.sh:258`.
- validation_or_tests: General template coverage loads every markdown template and verifies it is non-empty via `tests/test-templates-comprehensive.sh:90` through `tests/test-templates-comprehensive.sh:107`, so this file is covered by the all-template load path. Template reference validation scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls and checks referenced files exist; see `tests/test-template-references.sh:84` through `tests/test-template-references.sh:111`. The same test also reports whether each template is referenced in hooks; see `tests/test-template-references.sh:131` through `tests/test-template-references.sh:141`. Git-status filtering tests explicitly preserve `.humanize-loop.local/` when filtering out the plan file, which keeps this special-case branch reachable after plan-file filtering; see `tests/test-plan-file-handling.sh:422` through `tests/test-plan-file-handling.sh:428` and `tests/test-plan-file-handling.sh:462` through `tests/test-plan-file-handling.sh:466`. I did not find a dedicated assertion that the rendered stop-hook reason includes this exact block text.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section for the single assigned research row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`