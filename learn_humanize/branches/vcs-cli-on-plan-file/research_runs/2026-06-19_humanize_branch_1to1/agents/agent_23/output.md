# agent_23 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-023 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Provides a sourced Bash timeout abstraction for the RLCR/Codex loop so runtime commands can be bounded on both macOS and Linux. Its direct workflow role is to wrap `codex exec` from `hooks/loop-codex-stop-hook.sh`, preventing an automated review round from hanging indefinitely when a platform-specific timeout binary is missing or named differently.
- algorithmic_behavior: The script detects a timeout backend once at source time via `detect_timeout_impl` in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:10`). GNU `timeout` is accepted only if `timeout --version` succeeds, avoiding non-GNU/BSD-compatible ambiguity (`scripts/portable-timeout.sh:13`). It stores the selected backend in global `TIMEOUT_IMPL` (`scripts/portable-timeout.sh:29`). `run_with_timeout` consumes the first argument as seconds, preserves the remaining command as a Bash array, then dispatches to the selected backend (`scripts/portable-timeout.sh:33`). GNU-style paths call the binary directly (`scripts/portable-timeout.sh:39`, `scripts/portable-timeout.sh:43`). Python fallback uses `subprocess.run(..., timeout=...)` and maps timeout expiration to exit code `124`, matching GNU timeout convention (`scripts/portable-timeout.sh:47`). If no implementation exists, it emits a warning and runs the command unbounded (`scripts/portable-timeout.sh:64`).
- inputs_outputs_state: Inputs are the caller-provided timeout seconds, command argv, and PATH-visible tools at source time. The main runtime input comes from `CODEX_TIMEOUT`, read by the stop hook from loop state frontmatter (`hooks/loop-codex-stop-hook.sh:394`) with priority state file, env var, default (`hooks/loop-codex-stop-hook.sh:402`). The setup script validates `--codex-timeout` as numeric before persisting it into `state.md` (`scripts/setup-rlcr-loop.sh:134`, `scripts/setup-rlcr-loop.sh:332`). Outputs are the wrapped command’s stdout/stderr, the wrapped command’s exit code, exit `124` on Python timeout, stderr warning when no timeout backend exists, and exported `TIMEOUT_IMPL` for sourcing scripts (`scripts/portable-timeout.sh:73`). State is process-local only: backend detection happens once per source operation; no files are written by this helper.
- gates_or_invariants: Requires Bash semantics because it uses arrays and `local` (`scripts/portable-timeout.sh:36`). The backend priority is stable and deterministic. Argument boundaries are preserved for the wrapped command by `cmd=("$@")` and `"${cmd[@]}"` (`scripts/portable-timeout.sh:36`). The helper itself does not validate timeout seconds or require a command after `shift`; it assumes callers pass sane values. If `TIMEOUT_IMPL=none`, the invariant changes from bounded execution to best-effort execution with an explicit warning (`scripts/portable-timeout.sh:65`). For the active Codex workflow, any non-zero result from this wrapper becomes a blocking Codex review failure in the stop hook (`hooks/loop-codex-stop-hook.sh:737`).
- dependencies_and_callers: The main caller is `hooks/loop-codex-stop-hook.sh`, which sources `scripts/portable-timeout.sh` if present (`hooks/loop-codex-stop-hook.sh:652`) and otherwise defines a smaller inline fallback (`hooks/loop-codex-stop-hook.sh:657`). The hook then pipes the review prompt into `run_with_timeout "$CODEX_TIMEOUT" codex exec ...` and captures stdout/stderr into cache files (`hooks/loop-codex-stop-hook.sh:697`). Timeout configuration is user-facing through `/humanize:start-rlcr-loop ... --codex-timeout` documented in README (`README.md:69`, `README.md:204`) and persisted by `scripts/setup-rlcr-loop.sh`. External dependencies are `gtimeout`, GNU `timeout`, `python3`, or `python`; the wrapped command dependency in this branch path is `codex exec`.
- edge_cases_or_failure_modes: If no timeout implementation is available, Codex can run indefinitely despite the configured timeout, with only a stderr warning (`scripts/portable-timeout.sh:66`). The Python fallback interpolates `timeout_secs` directly into Python source (`scripts/portable-timeout.sh:54`); this is safe for setup-created numeric state, but the stop hook does not revalidate `STATE_CODEX_TIMEOUT` or env-provided `CODEX_TIMEOUT`, so a manually corrupted state/env value can cause Python syntax/runtime failure or worse behavior in the fallback path. Setup’s regex accepts `0` even though the error text says positive integer (`scripts/setup-rlcr-loop.sh:139`), and GNU/Python zero-timeout behavior may immediately terminate the command. Backend detection occurs only once when sourced, so PATH changes after sourcing do not affect later calls. Python `subprocess.run` timeout terminates the direct child process, but descendant process cleanup depends on the child process behavior; this helper has no explicit process-group cleanup. If the script is absent, the hook’s inline fallback checks `timeout` before `gtimeout` and lacks Python fallback and GNU-version validation, so behavior differs from this file (`hooks/loop-codex-stop-hook.sh:657`).
- validation_or_tests: No dedicated test file for `scripts/portable-timeout.sh` was found by repository search. Indirect validation exists in the calling flow: setup validates the CLI timeout option before writing loop state (`scripts/setup-rlcr-loop.sh:134`), the hook records the resolved timeout in its Codex command debug file (`hooks/loop-codex-stop-hook.sh:680`), and non-zero wrapper results are surfaced as a blocking JSON decision with debug file references (`hooks/loop-codex-stop-hook.sh:711`). There is no visible automated coverage for backend selection order, Python fallback timeout behavior, `none` fallback warning, invalid timeout values, or parity between this helper and the hook’s inline fallback.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`