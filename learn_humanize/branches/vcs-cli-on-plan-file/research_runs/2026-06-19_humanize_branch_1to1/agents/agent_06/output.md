# agent_06 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the branch’s shell-based validation surface for the `--commit-plan-file` plan-file lifecycle and prompt-template rendering system. It does not contain production runtime algorithms, but it is core validation coverage for runtime code in `scripts/setup-rlcr-loop.sh`, `hooks/loop-plan-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/hooks.json`, and `prompt-template/**`. Recursive inspection found exactly five test scripts: `tests/test-plan-file-handling.sh`, `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh`.

- algorithmic_behavior: `tests/test-plan-file-handling.sh` is the broadest branch-specific regression harness. It validates setup option exposure for `--commit-plan-file`, state schema fields, plan backup creation, git repository prerequisites, unsupported path-character gates, git-ignore checks, tracked/clean requirements, stop-hook filtering, bash-validator pre-commit blocking, template existence, UserPromptSubmit plan validation, ancestry checks, and `stop_loop` state renaming. Static checks dominate: for example help text is queried at `tests/test-plan-file-handling.sh:57`, state-field greps at `tests/test-plan-file-handling.sh:96`, setup path validation at `tests/test-plan-file-handling.sh:166`, stop-hook filtering checks at `tests/test-plan-file-handling.sh:223`, and plan-validator checks at `tests/test-plan-file-handling.sh:688`. It also includes isolated git behavior tests using temporary repos for `git log --follow`, fresh-repo `git rev-parse HEAD`, and `git merge-base --is-ancestor` at `tests/test-plan-file-handling.sh:473`, `tests/test-plan-file-handling.sh:528`, and `tests/test-plan-file-handling.sh:989`.

- algorithmic_behavior: `tests/test-template-loader.sh` directly exercises the template loader API. It sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:12`, then verifies `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Its key algorithmic focus is the single-pass placeholder renderer: simple replacement, multiple placeholders, multiline templates, unused variables, unreplaced variables, fallback rendering, shell metacharacter preservation, and placeholder-injection prevention. The injection tests at `tests/test-template-loader.sh:499`, `tests/test-template-loader.sh:512`, and `tests/test-template-loader.sh:525` specifically assert that a value containing `{{OTHER_VAR}}` is not rescanned and replaced later, matching `hooks/lib/template-loader.sh:39` through `hooks/lib/template-loader.sh:42`.

- algorithmic_behavior: `tests/test-template-references.sh` is a consistency scanner. It enumerates hook scripts at `tests/test-template-references.sh:57`, extracts literal template references from `load_template`, `load_and_render`, and `load_and_render_safe` calls at `tests/test-template-references.sh:88`, verifies referenced files exist under `prompt-template`, then separately scans all template files for direct references at `tests/test-template-references.sh:122`. Its final gate checks that critical read/write/edit validators use `load_and_render_safe` instead of unsafe rendering paths at `tests/test-template-references.sh:173`.

- algorithmic_behavior: `tests/test-templates-comprehensive.sh` provides repository-wide template health checks. It validates the `prompt-template` directory shape at `tests/test-templates-comprehensive.sh:64`, loads every markdown template at `tests/test-templates-comprehensive.sh:90`, checks placeholder syntax with regexes for extra braces, wrong brackets, spaces, and lowercase placeholders at `tests/test-templates-comprehensive.sh:118`, then runs render edge cases, fallback checks, real-template integrations, and a 100-call stress loop. The all-template render pass extracts placeholder names and supplies dummy values at `tests/test-templates-comprehensive.sh:575`, then warns on remaining placeholders at `tests/test-templates-comprehensive.sh:591`.

- algorithmic_behavior: `tests/test-error-scenarios.sh` documents and spot-checks failure behavior in the template loader. It intentionally calls missing-template and missing-directory paths at `tests/test-error-scenarios.sh:24` and `tests/test-error-scenarios.sh:37`, verifies missing `load_and_render` behavior at `tests/test-error-scenarios.sh:50`, exercises empty content and regex-special values at `tests/test-error-scenarios.sh:61` and `tests/test-error-scenarios.sh:74`, and embeds a `set -euo pipefail` subprocess at `tests/test-error-scenarios.sh:89` to confirm missing templates do not crash strict-mode callers. Its summary explicitly records the risk that an empty reason can send empty feedback at `tests/test-error-scenarios.sh:120`.

- inputs_outputs_state: Inputs are shell environment, repository layout, git state, hook/config files, and the `prompt-template` tree. `test-plan-file-handling.sh` creates temporary directories with `mktemp -d` at `tests/test-plan-file-handling.sh:52`, creates mock plan files, initializes git repositories, commits synthetic files, stages/unstages plan file scenarios, and reads implementation files with `grep`, `cat`, and `git` commands. Its output state is internal `PASSED`, `FAILED`, and `WARNINGS` counters initialized at `tests/test-plan-file-handling.sh:25` and summarized at `tests/test-plan-file-handling.sh:1022`; process exit is success only when failures are zero at `tests/test-plan-file-handling.sh:1030`.

- inputs_outputs_state: Template tests source the production renderer from `hooks/lib/template-loader.sh`, whose functions compute the template dir from the hook library path at `hooks/lib/template-loader.sh:11`, return empty content for missing templates at `hooks/lib/template-loader.sh:21`, render using environment-prefixed `TMPL_VAR_` values and awk single-pass scanning at `hooks/lib/template-loader.sh:43`, choose fallbacks in `load_and_render_safe` at `hooks/lib/template-loader.sh:155`, and validate required subdirectories at `hooks/lib/template-loader.sh:193`. The tests output colored pass/fail lines and exit nonzero on failed counters, such as `tests/test-template-loader.sh:555` and `tests/test-templates-comprehensive.sh:619`.

- inputs_outputs_state: The plan-file tests coordinate with setup/runtime state created by `scripts/setup-rlcr-loop.sh`. That script parses `--commit-plan-file` at `scripts/setup-rlcr-loop.sh:150`, requires a git repo and at least one commit at `scripts/setup-rlcr-loop.sh:199`, computes relative paths at `scripts/setup-rlcr-loop.sh:212`, rejects spaces/metacharacters at `scripts/setup-rlcr-loop.sh:215`, validates inside-repo and git-ignore conditions at `scripts/setup-rlcr-loop.sh:251`, records tracking status at `scripts/setup-rlcr-loop.sh:276`, requires tracked and clean plan files only in commit-plan mode at `scripts/setup-rlcr-loop.sh:285`, copies `plan-backup.md` at `scripts/setup-rlcr-loop.sh:317`, and writes state fields including `commit_plan_file`, `plan_file_tracked`, and `start_commit` at `scripts/setup-rlcr-loop.sh:326`.

- gates_or_invariants: Directory-wide gates are counter based: tests increment failure counters rather than relying uniformly on `set -e`, then exit 1 if failures remain. `test-template-loader.sh` uses strict `set -euo pipefail` at `tests/test-template-loader.sh:8`; several others use `set -uo pipefail` to continue after controlled failures. The plan-file invariant matrix is: commit-plan mode plus inside repo requires tracked and clean; commit-plan mode plus outside repo is a configuration conflict; non-commit-plan mode permits dirty/untracked plan file status but content must still match the backup. These gates mirror runtime cases documented in `hooks/loop-plan-validator.sh:5`, with content diffing at `hooks/loop-plan-validator.sh:258`, missing-file detection at `hooks/loop-plan-validator.sh:268`, and prompt blocking via stderr plus exit 2 at `hooks/loop-plan-validator.sh:311`.

- gates_or_invariants: Git invariants under test include exact path matching for plan-file status filtering so `docs/plan.md.bak` and `docs/plan.md~` are preserved while `docs/plan.md` is filtered. The isolated unit uses an end-of-line anchored pattern at `tests/test-plan-file-handling.sh:430`. Runtime stop-hook logic escapes the git-root-relative plan path and filters exact status lines at `hooks/loop-codex-stop-hook.sh:240`. Post-commit detection depends on `git log --oneline --follow "${START_COMMIT}..HEAD"` at `hooks/loop-codex-stop-hook.sh:311`; the test reproduces that command at `tests/test-plan-file-handling.sh:497`.

- gates_or_invariants: Template rendering invariants are single-pass substitution, literal metacharacter preservation, fallback availability for missing templates, and uppercase double-brace placeholder syntax. The renderer builds all replacements before invoking awk at `hooks/lib/template-loader.sh:49`, appends replacement values directly without rescanning at `hooks/lib/template-loader.sh:100`, and leaves unknown placeholders unchanged at `hooks/lib/template-loader.sh:104`. Tests cover ampersands, backslashes, dollar signs, command substitution markers, pipes, semicolons, glob characters, quotes, markdown, JSON, multiline values, and adjacent placeholders across `tests/test-template-loader.sh:257` through `tests/test-template-loader.sh:543` and `tests/test-templates-comprehensive.sh:266` through `tests/test-templates-comprehensive.sh:438`.

- dependencies_and_callers: The tests are standalone shell scripts and no external test runner reference was found outside `tests`; repository-wide search for their names outside the directory returned no matches. They assume execution from any location by deriving `SCRIPT_DIR` and `PROJECT_ROOT` from the script path. Runtime dependencies include `bash`, `grep`, `sed`, `awk`, `find`, `sort`, `wc`, `git`, `jq` indirectly through hooks, `python3` indirectly through the stop hook todo checker, and `codex` only for full setup/runtime behavior. The tests avoid full setup execution where `codex` would be required; for example `tests/test-plan-file-handling.sh:92` says it verifies script contents instead of running full setup.

- dependencies_and_callers: Parent and sibling coordination is validation oriented. `tests/test-plan-file-handling.sh` links setup, plan validator, bash validator, stop hook, shared loop helpers, hooks registration, cancel command, and prompt templates into one behavioral contract. `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` validate the core renderer used by the hooks. `tests/test-template-references.sh` coordinates hook scripts with `prompt-template/**` to prevent missing-template failures. `tests/test-error-scenarios.sh` explains the failure semantics that motivated `load_and_render_safe`, while `test-template-references.sh` enforces safe usage in critical validators.

- edge_cases_or_failure_modes: Important plan-file edge cases covered are missing `start_commit` legacy state files, fresh git repositories without commits, plan file outside git root, git-ignored plan file with commit-plan mode, tracked dirty plan files with and without commit-plan mode, accidentally staged plan files during `git commit`, accidental plan-file commits after loop start, branch ancestry divergence, missing/deleted plan files, unsupported path characters, similar filenames during filtering, missing templates for user-facing block messages, and incorrect Claude hook `"decision": "allow"` usage. Exact references include legacy state handling at `tests/test-plan-file-handling.sh:274`, fresh repo behavior at `tests/test-plan-file-handling.sh:525`, UserPromptSubmit case checks at `tests/test-plan-file-handling.sh:746`, content-diff validation at `tests/test-plan-file-handling.sh:795`, and ancestry logic at `tests/test-plan-file-handling.sh:858`.

- edge_cases_or_failure_modes: Template edge cases include missing files and directories returning empty content, empty template content, fallback rendering, unsafe `load_and_render` calls in critical validators, malformed placeholder syntax, special regex characters, shell metacharacters, multiline values, long values, unreplaced placeholders, repeated placeholders, adjacent placeholders, and placeholder injection. One residual weakness of this test directory is that many plan-file checks are static `grep` assertions rather than full black-box hook executions; they verify that code paths and messages exist but do not always prove full runtime control flow. This is explicit in `tests/test-plan-file-handling.sh:92` and visible in many `grep -q` checks against implementation files.

- validation_or_tests: The directory itself is validation coverage. Running all five scripts would validate the branch’s shell-level contracts, but this research pass did not execute them because the provided branch export is read-only and the test scripts create temporary git repositories and some setup paths may require runtime tooling. Static inspection confirms the test suite’s expected success mechanics: each script prints pass/fail summaries and exits 0 only when its failure counter is zero, with representative exits at `tests/test-plan-file-handling.sh:1030`, `tests/test-template-loader.sh:555`, `tests/test-template-references.sh:222`, and `tests/test-templates-comprehensive.sh:619`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`