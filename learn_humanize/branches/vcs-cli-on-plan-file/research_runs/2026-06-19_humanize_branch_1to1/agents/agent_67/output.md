# agent_67 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-067 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Conditional Claude prompt fragment for the RLCR loop’s `--push-every-round` mode. The file is not an implementation gate by itself; it is a short instruction injected into Claude-facing round prompts so the agent knows that commits must be pushed after each round. The actual template text is one operational sentence at [prompt-template/claude/push-every-round-note.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/prompt-template/claude/push-every-round-note.md:2).
- algorithmic_behavior: When `push_every_round` is enabled in loop state, `hooks/loop-codex-stop-hook.sh` loads this template with `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` and appends it to the next Claude prompt; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:902). If the template is missing or empty, the hook falls back to `Also push your changes after committing.` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:905). Round 0 has a duplicated inline version of the same instruction in `scripts/setup-rlcr-loop.sh`, appended only when `PUSH_EVERY_ROUND` is true; see [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:495).
- inputs_outputs_state: Input is the persisted loop state key `push_every_round`, written from the `--push-every-round` CLI flag. The setup script parses the flag at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:146) and records `push_every_round: $PUSH_EVERY_ROUND` in `state.md` at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/scripts/setup-rlcr-loop.sh:333). Output is appended prompt text in `round-0-prompt.md` during setup and in the next-round prompt during stop-hook generation. The assigned template does not mutate repository state, inspect git state, or perform the push; it only changes prompt content.
- gates_or_invariants: The invariant expressed by the template is: if `--push-every-round` is active, commits should be pushed to remote after each round. Enforcement lives elsewhere. The stop hook reads `push_every_round` and blocks exit when `git status -sb` reports the local branch is ahead of remote; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:345) and [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:352). The inverse gate is in `hooks/loop-bash-validator.sh`: when `push_every_round` is not true, direct `git push` commands are blocked; see [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-bash-validator.sh:81).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, through the shared template loader API. The loader references appear via `load_template` and `load_and_render_safe` in `hooks/lib/template-loader.sh`; the relevant call site for this file is [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/hooks/loop-codex-stop-hook.sh:904). Related user-facing command metadata advertises the flag in [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/commands/start-rlcr-loop.md:3), and README usage documents it as requiring push after each round at [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/vcs-cli-on-plan-file/README.md:206). Related enforcement/block templates are `prompt-template/block/unpushed-commits.md`, which tells the agent to push when exiting with unpushed commits, and `prompt-template/block/git-push.md`, which blocks pushes when the mode is off.
- edge_cases_or_failure_modes: If this template file is empty or missing, the next-round stop hook still emits a fallback push reminder, so prompt generation degrades gracefully. If the template text diverges from the inline Round 0 copy in `scripts/setup-rlcr-loop.sh`, Round 0 and later rounds could give inconsistent instructions. If `state.md` has an unexpected value other than literal `true`, the note is not appended and push enforcement does not activate. If remote tracking is absent or `git status -sb` does not report `ahead N`, the prompt note still asks for push, but the stop-hook gate may not detect unpushed commits reliably.
- validation_or_tests: No direct unit test was found for this tiny template. Behavioral coverage is indirect through shell-hook paths: setup parses and persists `--push-every-round`, the stop hook conditionally appends this template, and `loop-bash-validator.sh` enforces the opposite mode by blocking `git push` when the flag is false. The assigned file itself has no executable validation behavior beyond being loadable as a template.
- skip_candidate: `yes: this file is a prompt note rather than core algorithm implementation. It is still related to algorithmic loop transitions because it participates in the push-every-round contract, but the real gates and state transitions are implemented in setup and hook scripts, not in this markdown fragment.`

## Worker Self-Test
- assigned_items_seen: 1 of 1 item evidence sections present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`