# agent_59 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-059 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: This is a blocking prompt template used by the loop read validator when an agent attempts to read a round prompt or summary file from outside the active loop directory. It is part of the guard surface for preventing stale or misplaced loop-file reads. The file itself is declarative markdown, but it participates in an algorithmic transition because the caller renders it immediately before denying the tool action.
- algorithmic_behavior: The template renders a short diagnostic titled `Wrong File Location` at `prompt-template/block/wrong-file-location.md:1`. It interpolates the attempted path, active loop directory, and current round into a message that says loop files are in the active loop directory at `prompt-template/block/wrong-file-location.md:3`, then lists the canonical current-round prompt and summary paths at `prompt-template/block/wrong-file-location.md:5` through `prompt-template/block/wrong-file-location.md:7`. It ends with an explicit command hint for the originally requested file at `prompt-template/block/wrong-file-location.md:9`.
- inputs_outputs_state: Inputs are render variables supplied by `hooks/loop-read-validator.sh`: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` at `hooks/loop-read-validator.sh:92` through `hooks/loop-read-validator.sh:95`. `FILE_TYPE` is computed by the caller but not consumed by this template. Output is rendered markdown written to stderr by the caller before it exits with status `2` at `hooks/loop-read-validator.sh:92` through `hooks/loop-read-validator.sh:96`. The template itself has no persistent state. State used to decide whether this message is emitted comes from the active loop lookup and current round extraction: `find_active_loop` at `hooks/loop-read-validator.sh:58` and `get_current_round` at `hooks/loop-read-validator.sh:64`.
- gates_or_invariants: The caller only reaches this template for `Read` tool inputs; other tools exit early at `hooks/loop-read-validator.sh:25` through `hooks/loop-read-validator.sh:27`. The file must look like a round prompt or summary, otherwise the validator exits without blocking at `hooks/loop-read-validator.sh:45` through `hooks/loop-read-validator.sh:47`. An active loop must exist, otherwise it exits without blocking at `hooks/loop-read-validator.sh:60` through `hooks/loop-read-validator.sh:62`. The specific invariant enforced by this template is that round prompt and summary reads must be inside `.humanize-loop.local`; if `is_in_humanize_loop_dir` reports false, the template is rendered and the read is blocked at `hooks/loop-read-validator.sh:87` through `hooks/loop-read-validator.sh:96`. A separate later invariant checks exact active-round directory path, using `wrong-directory-path.md`, so this template covers the broader outside-loop case only.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`, which invokes `load_and_render_safe` with `block/wrong-file-location.md` at `hooks/loop-read-validator.sh:92`. Shared dependencies come from `hooks/lib/loop-common.sh`, which sources the template loader at `hooks/lib/loop-common.sh:11` through `hooks/loop-common.sh:16`, validates template directory availability without hard failure at `hooks/loop-common.sh:18` through `hooks/loop-common.sh:21`, identifies round files via `is_round_file_type` at `hooks/lib/loop-common.sh:234` through `hooks/lib/loop-common.sh:241`, and resolves current loop metadata through `find_active_loop` and `get_current_round` at `hooks/lib/loop-common.sh:67` through `hooks/lib/loop-common.sh:98`. Rendering is handled by `hooks/lib/template-loader.sh`, where templates use `{{VARIABLE_NAME}}` placeholders at `hooks/lib/template-loader.sh:5` through `hooks/lib/template-loader.sh:6`, `render_template` performs single-pass substitution at `hooks/lib/template-loader.sh:35` through `hooks/lib/template-loader.sh:117`, and `load_and_render_safe` falls back if the template is missing or empty at `hooks/lib/template-loader.sh:155` through `hooks/lib/template-loader.sh:188`.
- edge_cases_or_failure_modes: If the template file is missing or empty, the caller has an inline fallback message at `hooks/loop-read-validator.sh:89` through `hooks/loop-read-validator.sh:91`, and `load_and_render_safe` uses that fallback at `hooks/lib/template-loader.sh:164` through `hooks/lib/template-loader.sh:183`. If a placeholder is not supplied, the rendering helper leaves it unchanged rather than deleting it, as implemented at `hooks/lib/template-loader.sh:100` through `hooks/lib/template-loader.sh:107`. Placeholder injection is intentionally limited by single-pass rendering, so variable values containing template syntax are not recursively expanded, documented at `hooks/lib/template-loader.sh:39` through `hooks/lib/template-loader.sh:42`. If no active loop directory exists, there is no block and no template output. If the attempted file is inside `.humanize-loop.local` but in the wrong active-loop subdirectory or wrong round, other templates handle those failures later in the validator.
- validation_or_tests: Template reference existence is covered by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls at `tests/test-template-references.sh:84` through `tests/test-template-references.sh:111`; `loop-read-validator.sh` is one of the scripts checked at `tests/test-template-references.sh:57` through `tests/test-template-references.sh:64`. The same test verifies critical validators use safe rendering at `tests/test-template-references.sh:173` through `tests/test-template-references.sh:201`. General template loading and non-empty checks are covered by `tests/test-templates-comprehensive.sh:90` through `tests/test-templates-comprehensive.sh:107`, and safe fallback behavior is covered by `tests/test-template-loader.sh:197` through `tests/test-template-loader.sh:219` plus `tests/test-templates-comprehensive.sh:446` through `tests/test-templates-comprehensive.sh:463`. I did not run tests because this task is research-only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 of 1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`