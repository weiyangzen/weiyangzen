# agent_42 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-042 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: This file is a user-facing stop-hook block template for the RLCR loop’s large-file gate. It does not detect large files itself; it defines the rendered remediation prompt shown when `hooks/loop-codex-stop-hook.sh` refuses loop exit because changed tracked or new files exceed the configured line-count limit. The template’s algorithmic role is therefore a review/transition contract: when the hook transitions to a blocked stop decision, this block explains the invariant violation and the required next actions.
- algorithmic_behavior: The template has two placeholders, `{{MAX_LINES}}` and `{{LARGE_FILES}}`, at `prompt-template/block/large-files.md:3-4`, then branches the remediation text by file category. For code files, it requires splitting into modules below the limit, preserving behavior, optionally invoking `code-simplifier`, and maintaining module boundaries at `prompt-template/block/large-files.md:13-17`. For documentation files, it requires splitting into logical sections, preserving cross-references and narrative flow, and updating navigation at `prompt-template/block/large-files.md:19-23`. The final instruction at `prompt-template/block/large-files.md:25` tells the agent to commit the split and attempt exit again, so the template participates in the loop’s retry cycle after remediation.
- inputs_outputs_state: Inputs are supplied by `hooks/loop-codex-stop-hook.sh`: `MAX_LINES=2000` at `hooks/loop-codex-stop-hook.sh:104`, and `LARGE_FILES`, a newline-prefixed markdown list of offending files with line count and classified type, built at `hooks/loop-codex-stop-hook.sh:149-150`. Rendering is performed through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` with those two values at `hooks/loop-codex-stop-hook.sh:163-165`. Output is a markdown reason string assigned to `REASON`, then emitted in JSON as `"decision": "block"`, `"reason": $reason`, and a short `"systemMessage"` at `hooks/loop-codex-stop-hook.sh:167-174`. State transition is external to the template: if any qualifying changed file has `line_count > MAX_LINES`, the hook blocks exit and exits successfully as a hook decision at `hooks/loop-codex-stop-hook.sh:156-176`; otherwise control continues to later clean/push checks at `hooks/loop-codex-stop-hook.sh:179-224`.
- gates_or_invariants: The gate only runs when `git` is available and the current directory is a git repo, guarded at `hooks/loop-codex-stop-hook.sh:106`. It scans `git status --porcelain` output, so the invariant applies to changed tracked files and new files visible in status, not every file in the repo, as shown at `hooks/loop-codex-stop-hook.sh:152-154`. Deleted files are skipped at `hooks/loop-codex-stop-hook.sh:123-126`. Renames are normalized from `old -> new` to the new path at `hooks/loop-codex-stop-hook.sh:118-121`. Only configured code extensions and documentation extensions are considered; all other extensions continue without evaluation at `hooks/loop-codex-stop-hook.sh:132-143`. The comparison is strict greater-than, so a file exactly at 2000 lines does not trip the gate, while 2001 lines does at `hooks/loop-codex-stop-hook.sh:145-150`. The rendered template’s own invariant is consistent with that gate: split files to each be below `{{MAX_LINES}}` lines at `prompt-template/block/large-files.md:14` and `prompt-template/block/large-files.md:20`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, found by the template reference at `hooks/loop-codex-stop-hook.sh:163`. That hook is registered as a stop hook in `hooks/hooks.json:57`. Template directory resolution comes from `hooks/lib/loop-common.sh`, which sources `template-loader.sh` and initializes `TEMPLATE_DIR` via `get_template_dir` at `hooks/lib/loop-common.sh:11-17`. Rendering semantics come from `hooks/lib/template-loader.sh`: `get_template_dir` maps the plugin root to `prompt-template` at `hooks/lib/template-loader.sh:11-16`; `render_template` uses single-pass `{{VAR}}` replacement at `hooks/lib/template-loader.sh:35-43` and `hooks/lib/template-loader.sh:56-116`; `load_and_render_safe` falls back if a template is missing or renders empty at `hooks/lib/template-loader.sh:152-188`. The fallback string embedded in the stop hook mirrors the same placeholders and is used only if this template is unavailable or empty at `hooks/loop-codex-stop-hook.sh:157-165`.
- edge_cases_or_failure_modes: If `prompt-template/block/large-files.md` is missing or empty, the hook still blocks with the inline fallback because it calls `load_and_render_safe`, not the unsafe loader, at `hooks/loop-codex-stop-hook.sh:163` and `hooks/lib/template-loader.sh:164-180`. If `{{LARGE_FILES}}` contains placeholder-like text from filenames, the template renderer is single-pass and does not rescan replacement values, reducing accidental placeholder injection as described in `hooks/lib/template-loader.sh:39-42` and implemented at `hooks/lib/template-loader.sh:100-110`. Filenames with unusual `git status --porcelain` quoting or paths beginning with spaces may be fragile because the hook slices the first three characters via `${line#???}` at `hooks/loop-codex-stop-hook.sh:115-116`; the template will simply display whatever normalized filename string the hook provides. Binary or unsupported extension files over the line limit are ignored by the gate because only the extension cases at `hooks/loop-codex-stop-hook.sh:133-139` set a file type. Files that cannot be read by `wc -l` are skipped because the count command falls through with `|| continue` at `hooks/loop-codex-stop-hook.sh:145-146`. There is also a policy nuance: the prompt tells the agent to commit after splitting at `prompt-template/block/large-files.md:25`, but actual commit/push gating is handled later by the stop hook, not by this template.
- validation_or_tests: There appears to be no dedicated test that creates an over-2000-line changed file and asserts the exact large-file block decision. Coverage is indirect. `tests/test-template-references.sh` includes `hooks/loop-codex-stop-hook.sh` in the scanned scripts at `tests/test-template-references.sh:57-65`, extracts `load_template`, `load_and_render`, and `load_and_render_safe` references at `tests/test-template-references.sh:84-111`, and fails missing referenced templates at `tests/test-template-references.sh:103-107`; this validates that `block/large-files.md` exists for the stop hook. `tests/test-template-loader.sh` verifies template directory resolution, existing/missing template loading, substitution, multiline replacement, preserved unreplaced variables, and fallback behavior at `tests/test-template-loader.sh:41-78`, `tests/test-template-loader.sh:80-164`, and `tests/test-template-loader.sh:196-222`. `tests/test-templates-comprehensive.sh` loads every markdown template under `prompt-template` at `tests/test-templates-comprehensive.sh:83-107`, validates placeholder syntax including uppercase placeholder names at `tests/test-templates-comprehensive.sh:113-180`, checks fallback behavior at `tests/test-templates-comprehensive.sh:440-463`, and stress-renders all templates with dummy placeholder values at `tests/test-templates-comprehensive.sh:562-605`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`