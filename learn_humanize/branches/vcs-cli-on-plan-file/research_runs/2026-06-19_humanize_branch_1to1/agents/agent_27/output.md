# agent_27 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-027 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: `tests/test-template-loader.sh` is an executable shell specification for `hooks/lib/template-loader.sh`, the shared template loading/rendering layer used by RLCR loop validators and stop hooks. It verifies that prompt templates under `prompt-template/` can be located, loaded, rendered with `{{VAR}}` placeholders, safely fallback when missing, and reject invalid template directories. This is core algorithm evidence because template rendering feeds blocking messages, next-round prompts, review prompts, and validator reasons across `hooks/`.
- algorithmic_behavior: The script bootstraps by resolving `SCRIPT_DIR`, `PROJECT_ROOT`, and sourcing `hooks/lib/template-loader.sh` at lines 10-12. It defines pass/fail counters and assertions at lines 20-34, then runs 36 numbered behavioral tests. The first group verifies path and file loading: `get_template_dir "$PROJECT_ROOT/hooks/lib"` must resolve to `$PROJECT_ROOT/prompt-template` at lines 44-52; `load_template` must read `block/git-push.md` and include `Git Push Blocked` at lines 58-65; a missing template must produce empty stdout at lines 71-78. The renderer contract is tested for single variable replacement, multiple replacement, multiline template preservation, ordinary path-like values, unused substitutions as no-ops, and unreplaced placeholders staying literal at lines 81-193. Integration behavior is tested by loading and rendering `block/wrong-round-number.md` with action, round, file type, current round, and correct path inputs at lines 147-164. Safe rendering is tested for fallback-on-missing and real-template-over-fallback at lines 197-222. Directory validation is tested for valid and invalid roots at lines 225-244. The large regression section at lines 247-487 specifies that shell metacharacters in variable values render literally, including `&`, backslashes, `$`, backticks, pipes, semicolons, globs, parentheses, redirection syntax, quotes, comments, tilde, combined shell syntax, Windows paths, regex fragments, JSON, and multiline content. The final injection section at lines 488-543 specifies single-pass rendering: placeholders introduced through variable values must not be recursively substituted.
- inputs_outputs_state: Inputs are the repository-relative implementation file `hooks/lib/template-loader.sh`, the template root returned by `get_template_dir`, real templates `prompt-template/block/git-push.md` and `prompt-template/block/wrong-round-number.md`, and literal `VAR=value` arguments passed to rendering functions. Outputs are human-readable PASS/FAIL lines, counter state in `TESTS_PASSED` and `TESTS_FAILED`, and a process exit code. The state transition is intentionally simple: each assertion calls `pass` to increment `TESTS_PASSED` at lines 24-27 or `fail` to increment `TESTS_FAILED` at lines 29-34. The terminal gate exits `0` only when `TESTS_FAILED == 0`; otherwise it exits `1` at lines 555-563. The production implementation under test maps `hooks/lib` two levels upward to `prompt-template` in `get_template_dir` at `hooks/lib/template-loader.sh:11-16`, reads files or emits empty output plus a warning in `load_template` at `hooks/lib/template-loader.sh:21-33`, performs single-pass `awk` replacement in `render_template` at `hooks/lib/template-loader.sh:43-117`, composes loading plus rendering in `load_and_render` at `hooks/lib/template-loader.sh:121-132`, falls back in `load_and_render_safe` at `hooks/lib/template-loader.sh:155-188`, and validates required subdirectories in `validate_template_dir` at `hooks/lib/template-loader.sh:193-207`.
- gates_or_invariants: The script enforces that template root derivation is deterministic for a library path, existing templates load non-empty content, missing templates do not crash callers, and safe renderers prefer real templates but use fallback strings for missing or empty outputs. Rendering invariants are: `{{NAME}}` placeholders are replaced only when a matching `NAME=value` argument is supplied; unused arguments do not alter output; absent variables leave the original `{{ID}}` syntax intact; multiline content remains structurally intact; variable values are literal data rather than shell code; and replacement is single-pass, so injected `{{OTHER_VAR}}` text is preserved. Directory validation requires `block`, `codex`, and `claude` subdirectories per `hooks/lib/template-loader.sh:201-204`. Test success is gated by zero failures at lines 555-563.
- dependencies_and_callers: The direct dependency is `hooks/lib/template-loader.sh`, sourced at line 12. The fixture templates are `prompt-template/block/git-push.md`, whose title is `Git Push Blocked` at `prompt-template/block/git-push.md:1`, and `prompt-template/block/wrong-round-number.md`, whose placeholders appear at `prompt-template/block/wrong-round-number.md:1-5`. `hooks/lib/loop-common.sh` sources the template loader and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:11-20`, so most validators inherit this behavior. `rg` shows broad callers: `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-write-validator.sh`, and message helpers in `hooks/lib/loop-common.sh` all call `load_and_render_safe`, `load_template`, or related functions. Sibling validation coverage includes `tests/test-template-references.sh` for referenced template existence, `tests/test-templates-comprehensive.sh` for broader template rendering coverage, `tests/test-error-scenarios.sh` for missing-template/error cases, and `tests/test-plan-file-handling.sh` for plan-file-specific template usage.
- edge_cases_or_failure_modes: Missing template files are expected to produce empty content for `load_template` and fallback content for `load_and_render_safe`, which prevents hook crashes but can degrade to inline messages. Invalid template directories must be rejected. Values with shell-sensitive bytes are treated as literal strings, guarding against accidental command interpretation and an historical `awk gsub`-style `&` replacement bug, especially covered at lines 257-487. Placeholder injection is explicitly blocked by single-pass rendering at lines 499-543 and by the implementation comments at `hooks/lib/template-loader.sh:35-42`; this prevents review content or plan text containing `{{GOAL_TRACKER_FILE}}` from corrupting later prompt variables. A concrete failure mode observed during this read-only worker run: `render_template` uses a here-string into `awk` at `hooks/lib/template-loader.sh:59-114`; in this managed read-only environment Bash reported `cannot create temp file for here document: Operation not permitted`, causing all rendering-dependent tests to fail. That appears environment/sandbox-related, but it also documents a dependency on shell temporary-file capability for here-string processing. A separate implementation edge case surfaced by the run: calling `render_template` with no `VAR=value` arguments, as in the existing-template safe-render case at lines 215-216, can hit `env_vars[@]: unbound variable` under `set -u`, which made `load_and_render_safe` choose fallback in the sandbox run.
- validation_or_tests: The file itself is a validation script with 36 checks and a final zero-failure exit gate. I also ran `bash tests/test-template-loader.sh` read-only from the assigned branch export. Result: exit code `1`; 5 passed and 31 failed. The passed checks were path resolution, existing/missing raw template load, and valid/invalid template directory validation. Rendering checks failed because the sandbox prevented Bash here-document/here-string temp file creation at `hooks/lib/template-loader.sh:114`, and one no-argument render path reported `env_vars[@]: unbound variable`. This means the test was executed, but the result is not a clean product validation in this read-only worker environment.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-027`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`