# agent_57 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-057 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: This is a stop-hook block template for the RLCR loop’s required work-summary gate. When the active loop expects `.humanize-loop.local/<loop>/round-N-summary.md` and the file is absent, the stop hook renders this template as the blocking reason instead of allowing exit. The template’s role is not to implement the file-existence check itself; it defines the user-facing recovery contract for the “summary missing” transition. See `prompt-template/block/work-summary-missing.md:1-16` and caller `hooks/loop-codex-stop-hook.sh:417-442`.
- algorithmic_behavior: The template states that the agent “attempted to exit without writing” the work summary, then requires writing to `{{SUMMARY_FILE}}`. Its required summary payload is four-part: what was implemented, files created/modified, tests added/passed, and remaining items. In the algorithmic flow, `hooks/loop-codex-stop-hook.sh` computes `SUMMARY_FILE="$LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` at `hooks/loop-codex-stop-hook.sh:421`; if `[[ ! -f "$SUMMARY_FILE" ]]`, it renders `block/work-summary-missing.md` with `SUMMARY_FILE=$SUMMARY_FILE` via `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:423-431`, then returns a JSON object with `"decision": "block"` at `hooks/loop-codex-stop-hook.sh:433-440`.
- inputs_outputs_state: Input is the `SUMMARY_FILE` placeholder passed by the stop hook, derived from the active loop directory and current round in `state.md`. The template renderer supports `{{VARIABLE_NAME}}` placeholders and performs single-pass replacement, so `{{SUMMARY_FILE}}` is substituted without recursively expanding placeholder-like content inside the value; see `hooks/lib/template-loader.sh:35-43` and `hooks/lib/template-loader.sh:56-116`. Output is markdown text embedded as the `reason` field in the stop-hook JSON response. The file itself has no persistent state and performs no mutation. The state transition is external: missing summary keeps the loop in a blocked-exit state; once the expected summary file exists, this gate is bypassed and the stop hook proceeds to later checks.
- gates_or_invariants: The key invariant is one summary file per current round at `$LOOP_DIR/round-${CURRENT_ROUND}-summary.md`; the stop hook blocks exit if that exact file is missing. Setup instructions pre-announce this invariant by telling the worker to write the summary into `$SUMMARY_PATH` at `scripts/setup-rlcr-loop.sh:488-493`, and the command docs repeat the rule at `commands/start-rlcr-loop.md:43-49`. The write validator also protects the invariant by blocking summary files written outside `.humanize-loop.local` and by enforcing the current round number; see `hooks/loop-write-validator.sh:100-111` and `hooks/loop-write-validator.sh:127-147`. Template availability is guarded by `load_and_render_safe`, which falls back if the template is missing or renders empty; see `hooks/lib/template-loader.sh:152-188`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, specifically the missing summary branch at `hooks/loop-codex-stop-hook.sh:423-431`. It depends on the shared loader sourced through `hooks/lib/loop-common.sh:11-20`, which resolves `TEMPLATE_DIR` from the plugin root to `prompt-template` using `hooks/lib/template-loader.sh:9-16`. Upstream producers of the expected path include the setup script’s generated round prompt at `scripts/setup-rlcr-loop.sh:488-493` and the Claude footer template at `prompt-template/claude/next-round-footer.md:6-9`, both of which instruct writing a round summary. Sibling validators reinforce related summary-path constraints through `hooks/loop-write-validator.sh`.
- edge_cases_or_failure_modes: If `state.md` is missing, the stop hook exits without this gate at `hooks/loop-codex-stop-hook.sh:383-385`. If `current_round` is corrupt, the hook stops the loop as unexpected before reaching the summary gate at `hooks/loop-codex-stop-hook.sh:405-410`. If the template is missing, empty, or fails to render, the fallback message at `hooks/loop-codex-stop-hook.sh:427-431` still blocks exit and includes the required path. If a summary is written to the wrong directory or wrong round filename, this template is not responsible; `hooks/loop-write-validator.sh` blocks those writes separately. A minimal but semantically poor summary still satisfies this specific file-existence gate; quality review happens later through Codex review after the summary exists.
- validation_or_tests: Template loader behavior is covered by `tests/test-template-loader.sh`, including safe fallback for missing templates and normal use of existing templates at `tests/test-template-loader.sh:197-222`. Broad template coverage exists in `tests/test-templates-comprehensive.sh`, which loads every markdown template at `tests/test-templates-comprehensive.sh:90-107`, validates placeholder syntax beginning at `tests/test-templates-comprehensive.sh:116-130`, tests safe fallback and existing-template rendering at `tests/test-templates-comprehensive.sh:446-463`, and attempts rendering of all templates with dummy placeholder values at `tests/test-templates-comprehensive.sh:562-605`. Template reference validation scans hook calls to ensure referenced templates exist at `tests/test-template-references.sh:84-112`. I did not execute tests because this run is a read-only research pass; evidence is from direct file inspection.
- skip_candidate: `yes: this file is a prompt/block template rather than executable algorithm code. It is still relevant to the core algorithm subset because it defines the recovery message and required output contract for the stop-hook missing-summary gate, which is an algorithmic transition in the RLCR loop.`

## Worker Self-Test
- assigned_items_seen: 1 item, matching the sole Item Evidence heading above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`