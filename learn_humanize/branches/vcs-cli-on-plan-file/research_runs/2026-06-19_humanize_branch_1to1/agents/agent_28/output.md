# agent_28 vcs-cli-on-plan-file 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `4dd1ca2fece39d3c6d7f84965cd71bda02489397`

## Item Evidence

### VCS_CLI_ON_PLAN_FILE-HZ-028 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification for the hook/template integrity contract. The script verifies that shell hook validators never reference missing prompt templates, because missing block templates can otherwise turn validator denials into empty or degraded messages. It is especially relevant to the branch’s plan-file behavior because the hook layer emits plan-file and loop-state blocking messages through `prompt-template/**` files.
- algorithmic_behavior: The test initializes repository-relative paths from its own location, setting `PROJECT_ROOT` and `TEMPLATE_DIR` at `tests/test-template-references.sh:14-16`. It keeps mutable counters for passed checks, failed checks, and warnings at `tests/test-template-references.sh:25-27`, then mutates those counters through `pass`, `fail`, and `warn` helper functions at `tests/test-template-references.sh:29-42`. Section 1 hard-codes the shell scripts that are allowed/expected to reference templates at `tests/test-template-references.sh:57-65`, scans them line by line, ignores comment-only lines at `tests/test-template-references.sh:88-92`, matches calls shaped like `load_template "$TEMPLATE_DIR" "..."`, `load_and_render "$TEMPLATE_DIR" "..."`, or `load_and_render_safe "$TEMPLATE_DIR" "..."` at `tests/test-template-references.sh:94-98`, and checks each extracted template path for file existence at `tests/test-template-references.sh:101-108`. Section 2 enumerates all markdown templates under `prompt-template` with `find ... -print0` at `tests/test-template-references.sh:123-127`, then greps `hooks/` for each quoted relative path at `tests/test-template-references.sh:135-142`; this is informational because unreferenced templates are warnings, not failures. Section 3 checks a fixed cross-reference set for shared `loop-common.sh` message helpers at `tests/test-template-references.sh:153-168`. Section 4 enforces that the three critical read/write/edit validators do not use unsafe `load_and_render` calls where `load_and_render_safe` should provide fallback text at `tests/test-template-references.sh:177-201`. The final state gate exits `0` only when `TESTS_FAILED == 0`; otherwise it exits `1` after listing missing templates at `tests/test-template-references.sh:214-230`.
- inputs_outputs_state: Inputs are the hard-coded hook script paths at `tests/test-template-references.sh:57-65`, the template directory `prompt-template/`, shell source lines containing template loader calls, and the fixed common-template allowlist at `tests/test-template-references.sh:153-160`. Outputs are human-readable colored PASS/FAIL/WARN lines, section headings, a summary count, an optional critical missing-template list, and the process exit code. Internal state consists of `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, `MISSING_TEMPLATES`, `FOUND_REFERENCES`, `TEMPLATE_FILES`, and `UNREFERENCED`; these are monotonically updated during the scan. Validation run in this read-only export: `bash tests/test-template-references.sh` exited `0`, found `39` template references, found `39` template files, reported `84` passed, `0` failed, and `3` warnings.
- gates_or_invariants: Missing hook scripts are warnings, not failures, because `tests/test-template-references.sh:75-79` calls `warn` and continues. Missing templates referenced by loader calls are hard failures because `tests/test-template-references.sh:103-108` calls `fail` and appends to `MISSING_TEMPLATES`. Unreferenced templates are warnings only by design at `tests/test-template-references.sh:137-142`, allowing dynamically used templates. The common shared block-message templates must exist explicitly at `tests/test-template-references.sh:162-168`. Critical validators must use `load_and_render_safe`, not bare `load_and_render`, as enforced by `unsafe_count` at `tests/test-template-references.sh:186-201`. The ultimate invariant is zero failures at `tests/test-template-references.sh:222-230`; warnings do not block success.
- dependencies_and_callers: The script depends on `bash`, `find`, `grep`, `sed`, and standard file tests. It depends on hook/template conventions implemented by `hooks/lib/template-loader.sh`, where `load_template` returns empty output for missing files and warns to stderr at `hooks/lib/template-loader.sh:21-32`, `load_and_render` renders only when content is non-empty at `hooks/lib/template-loader.sh:121-131`, and `load_and_render_safe` falls back when template loading or rendering produces empty content at `hooks/lib/template-loader.sh:155-188`. It scans `hooks/loop-codex-stop-hook.sh`, `hooks/loop-plan-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/lib/loop-common.sh`. The shared-message functions checked in Section 3 correspond to `hooks/lib/loop-common.sh:255-303` and `hooks/lib/loop-common.sh:349-360`. The critical safe-rendering rule is grounded in real validator call sites such as `hooks/loop-read-validator.sh:92`, `hooks/loop-read-validator.sh:109`, `hooks/loop-read-validator.sh:128`, `hooks/loop-write-validator.sh:109`, `hooks/loop-write-validator.sh:140`, `hooks/loop-write-validator.sh:160`, and `hooks/loop-edit-validator.sh:108`. I found no direct runner or documentation reference to `test-template-references.sh`; it appears to be a standalone executable test in `tests/`.
- edge_cases_or_failure_modes: The parser only recognizes single-line calls where the first argument is exactly the quoted literal `"$TEMPLATE_DIR"` and the second quoted argument is on the same line, so dynamically built template names, unquoted variables, reordered arguments, multiline invocations, or alternate template-dir variables can escape Section 1. Comment lines are skipped only when the whole line starts with optional whitespace plus `#`; inline comments after code are still scanned. The script runs with `set -u` and `pipefail` but not `set -e`, so individual `grep` misses inside conditionals are expected, while unset variables outside guarded contexts would still abort. Section 2 greps only under `hooks/`, so templates referenced solely from tests or scripts may be warned as unreferenced. The use of plain `grep -rq "\"$template\""` treats the template path as a basic regex, though current paths are simple enough that this is low risk. If `prompt-template/` is missing, the `find "$TEMPLATE_DIR"` command can emit an error and produce an empty template list; hard failures would still come from Section 1 and Section 3 for referenced/common templates. The current validation warnings are non-blocking unreferenced-template warnings for `block/codex-review-failed.md`, `block/plan-file-modified-warning.md`, and `block/plan-file-changed-commit-mode.md`.
- validation_or_tests: Direct validation was executed with `bash tests/test-template-references.sh` from the assigned branch export. Result: exit code `0`; `Passed: 84`, `Failed: 0`, `Warnings: 3`; final message: all template reference checks passed. Supporting static inspection used `rg` to enumerate template loader call sites in `hooks/` and `tests/`, and `find prompt-template -type f -name '*.md'` to confirm the template inventory contains `39` markdown files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `VCS_CLI_ON_PLAN_FILE-HZ-028`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`