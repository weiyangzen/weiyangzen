# agent_11 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt templates for the RLCR state machine. This directory does not execute code itself; it defines instruction payloads that `hooks/loop-codex-stop-hook.sh` renders when Codex blocks exit, starts a next round, requests finalize work, or asks Claude to report goal-tracker updates.
- algorithmic_behavior: Recursively inspected directory; it contains only six Markdown files and no child directories. `finalize-phase-prompt.md` defines the finalize transition after Codex review passes, requiring code simplification through `code-simplifier:code-simplifier`, no behavior changes, tests, commit, and a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:1`, `:9`, `:16`, `:40`). `next-round-prompt.md` is the corrective-loop prompt: re-read original plan, create todos for all discovered issues, consume Codex review content, and treat `goal-tracker.md` as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1`, `:12`, `:16`, `:22`, `:30`). `next-round-footer.md` adds non-exit constraints, optional code-simplifier invocation, commit, and summary-write requirements (`prompt-template/claude/next-round-footer.md:4`, `:6`). `post-alignment-action-items.md` injects extra priorities after full alignment checks: forgotten work, unmet ACs, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2`). `push-every-round-note.md` adds the remote push requirement when the loop state says push every round (`prompt-template/claude/push-every-round-note.md:2`). `goal-tracker-update-request.md` defines the structured summary section Claude must use when it cannot directly mutate the goal tracker (`prompt-template/claude/goal-tracker-update-request.md:2`).
- inputs_outputs_state: Inputs are template variables supplied by hook code: `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_CONTENT`, `NEXT_SUMMARY_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is rendered Markdown written into round prompt files or returned as a blocking hook reason. State impact is indirect: these prompts instruct Claude to create summaries/commits and request goal-tracker changes, while actual state mutation happens in hooks. `loop-codex-stop-hook.sh` renders the finalize prompt on completion (`hooks/loop-codex-stop-hook.sh:1010`), renders the next-round prompt into `round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:1067`, `:1080`), appends post-alignment text after every fifth-round alignment check (`:1085`), appends footer and optional push note (`:1093`, `:1099`), and appends the goal-tracker update-request template (`:1108`).
- gates_or_invariants: The templates encode non-negotiable workflow gates rather than enforcing them directly. Key invariants: Claude must not directly edit the goal tracker after round 0 (`next-round-prompt.md:30`); Claude must not lie, edit loop state, or run `cancel-rlcr-loop` to exit (`next-round-footer.md:4`); finalize work must be functionality-equivalent and test-preserving (`finalize-phase-prompt.md:16`); all discovered issues must get todos and not only the most important issue (`next-round-prompt.md:12`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, especially single-pass `{{VAR}}` substitution that avoids recursively expanding user/review content (`hooks/lib/template-loader.sh:7`, `:54`, `:71`). Main caller is `hooks/loop-codex-stop-hook.sh` lines `1010`, `1080`, `1087`, `1096`, `1101`, and `1109`. The template directory is resolved by `hooks/lib/loop-common.sh` through `template-loader.sh` (`hooks/lib/loop-common.sh:135`, `:140`).
- edge_cases_or_failure_modes: If a template is missing or renders empty, hook callers use fallback prompt text via `load_and_render_safe` (`hooks/lib/template-loader.sh:167`). Missing optional templates degrade behavior but do not necessarily stop the loop. Template values are substituted single-pass, so review content containing `{{...}}` is preserved instead of corrupting prompts (`hooks/lib/template-loader.sh:54`). If `push-every-round-note.md` is absent, the stop hook falls back to a short push instruction (`hooks/loop-codex-stop-hook.sh:1101`).
- validation_or_tests: Template existence and cross-reference checks live in `tests/test-template-references.sh`, which checks shared template references and `load_and_render_safe` usage (`tests/test-template-references.sh:149`). Comprehensive template rendering coverage exists in `tests/test-templates-comprehensive.sh` per repository search, and `tests/test-finalize-phase.sh` sources `loop-common.sh` to test finalize behavior.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-041 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime preflight validator for `/humanize:gen-plan` IO routing. It prevents the command from analyzing a missing/empty draft or writing into an invalid/unsafe output target before any plan-generation analysis starts.
- algorithmic_behavior: Parses only `--input`, `--output`, and `-h|--help`; any missing value, flag-as-value, unknown option, or help request exits `6` through `usage()` (`scripts/validate-gen-plan-io.sh:15`, `:28`, `:31`, `:39`, `:47`, `:50`). It requires both arguments (`:57`), normalizes paths with `realpath -m` when available (`:68`), computes the output directory (`:71`), then applies five ordered checks: input file exists (`:78`), input file is non-empty (`:86`), output directory exists (`:94`), output path is not a directory and does not already exist (`:102`, `:110`), and output directory is writable (`:117`). On success it prints `VALIDATION_SUCCESS`, input line count, and output target (`:125`).
- inputs_outputs_state: Inputs are CLI args from the gen-plan command, typically passed as `$ARGUMENTS` by `commands/gen-plan.md` (`commands/gen-plan.md:32`). Outputs are stdout/stderr messages and process exit codes only; it does not write files or mutate repository state. It emits machine-readable status tokens such as `VALIDATION_ERROR: INPUT_NOT_FOUND`, `INPUT_EMPTY`, `OUTPUT_DIR_NOT_FOUND`, `OUTPUT_IS_DIRECTORY`, `OUTPUT_EXISTS`, `NO_WRITE_PERMISSION`, and `VALIDATION_SUCCESS` (`scripts/validate-gen-plan-io.sh:80`, `:88`, `:96`, `:104`, `:111`, `:119`, `:127`).
- gates_or_invariants: Output path must be a new file path, not an existing file or directory (`scripts/validate-gen-plan-io.sh:102`). Existing output and output-is-directory both share exit code `4`, matching the gen-plan command’s “output file already exists / choose another path” stop condition (`commands/gen-plan.md:38`). Input must be a regular non-empty file (`scripts/validate-gen-plan-io.sh:79`, `:87`). Output directory must already exist; the validator suggests `mkdir -p` but does not create it (`:95`, `:98`).
- dependencies_and_callers: Direct caller is `commands/gen-plan.md`, whose allowed-tools restrict Bash access to this script and whose Phase 1 runs `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` (`commands/gen-plan.md:5`, `:30`, `:35`). It uses standard shell tools: `realpath`, `dirname`, `wc`, and `tr`; `realpath -m` failure falls back to the original path (`scripts/validate-gen-plan-io.sh:69`).
- edge_cases_or_failure_modes: `usage()` exits `6`, so even `--help` is treated as invalid-argument control flow for the command (`scripts/validate-gen-plan-io.sh:15`, `:22`, `:47`). The script uses `set -e`, but the `realpath` calls are guarded with `|| echo`, so missing `realpath -m` is not fatal (`:69`). It checks `-w` on the output directory, which may not predict all write failures on ACL/network filesystems but catches normal permission errors (`:118`). It does not protect against symlink targets beyond the “path exists” check; an existing symlink would be rejected by `-e`, while a future symlink race is outside this simple validator.
- validation_or_tests: `tests/test-gen-plan.sh` has a dedicated “Script Tests: validate-gen-plan-io.sh” section (`tests/test-gen-plan.sh:425`). It verifies exit `6` for missing values, flag-as-value, unknown option, and help (`:438`, `:447`, `:456`, `:465`, `:532`); exit `1` for missing input (`:474`); exit `2` for empty input (`:483`); exit `3` for missing output directory (`:493`); exit `4` for existing output and output directory (`:503`, `:513`); and exit `0` for valid paths (`:523`). `tests/run-all-tests.sh` includes `test-gen-plan.sh` in the suite (`tests/run-all-tests.sh:51`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-071 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared hook library for the RLCR and PR-loop state machines. It centralizes constants, state parsing, active loop discovery, template-rendered block messages, path classification, protected-file mutation detection, cancel authorization, `.humanize` git-add protection, loop termination, and PR-loop goal-tracker updates.
- algorithmic_behavior: Defines state schema field constants such as `plan_tracked`, `start_branch`, `plan_file`, `current_round`, `max_iterations`, push/model/timeout fields (`hooks/lib/loop-common.sh:15`), Codex markers (`:26`), and exit reasons (`:30`). `validate_hook_input()` rejects null bytes, invalid UTF-8 when `iconv` is available, invalid JSON through `jq`, and missing `tool_name`, while exposing `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` (`:46`, `:58`, `:65`, `:75`, `:82`). `require_tool_input_field()` and `is_deeply_nested()` provide hook input field and DoS-depth gates (`:94`, `:112`). `find_active_loop()` and `find_active_pr_loop()` intentionally choose only the newest timestamp directory with active state, preventing older “zombie” loops from reviving (`:147`, `:160`, `:699`). `parse_state_file()` is tolerant and defaults missing current round/max iterations/push flag (`:187`, `:223`); `parse_state_file_strict()` fails closed on missing file, missing YAML separators, missing required fields, and non-numeric round/max iteration values (`:231`, `:248`, `:270`, `:280`). Path helpers detect round files, state/finalize-state, goal tracker, finalize summary, PR loop readonly files, and loop directory membership (`:303`, `:407`, `:413`, `:419`, `:425`, `:616`, `:687`, `:729`). `is_cancel_authorized()` validates the only permitted state-file move to `cancel-state.md` behind a `.cancel-requested` signal and rejects command substitution, shell chaining, leftover variables, mixed quotes, extra args, wrong source/destination, and symlink sources (`:437`, `:461`, `:466`, `:476`, `:486`, `:538`, `:579`, `:585`, `:600`). `git_adds_humanize()` analyzes git-add command segments and blocks direct `.humanize` paths, force/broad-scope adds, and broad adds when `.humanize` is not ignored (`:812`, `:837`, `:871`, `:879`, `:896`, `:909`). `command_modifies_file()` provides regex-based detection for redirection, tee, in-place editors, mv/cp/rm, dd, truncate, printf redirection, and fd redirection (`:972`). `end_loop()` renames `state.md` to `<reason>-state.md` for terminal states (`:1016`). `update_pr_goal_tracker()` performs idempotent PR-loop tracker updates, checking round+reviewer summary/log entries, updating totals, inserting a table row, and appending an issue log entry (`:1055`, `:1088`, `:1160`, `:1171`, `:1203`).
- inputs_outputs_state: Inputs are hook JSON payloads, filesystem paths under `.humanize/rlcr` and `.humanize/pr-loop`, shell command strings, YAML-frontmatter state files, template files, and optional bot-result JSON. Outputs are global shell variables, return codes, rendered block messages, and in selected helpers actual file mutations: `end_loop()` renames state files (`hooks/lib/loop-common.sh:1041`), and `update_pr_goal_tracker()` rewrites the tracker through temp files (`:1157`, `:1216`). Most helpers are pure predicates/message renderers used by PreToolUse/UserPromptSubmit hooks.
- gates_or_invariants: Hook inputs must be valid JSON with bounded nesting before validators continue (`hooks/loop-read-validator.sh:27`, `:32`; `hooks/loop-bash-validator.sh:26`, `:31`; `hooks/loop-write-validator.sh:27`, `:32`). Strict state parsing fails closed in read/write/bash/edit and plan validators (`hooks/loop-read-validator.sh:94`, `hooks/loop-write-validator.sh:140`, `hooks/loop-bash-validator.sh:81`, `hooks/loop-edit-validator.sh:105`, `hooks/loop-plan-file-validator.sh:44`). State files and finalize-state files are system-owned except for a narrow authorized cancel move (`hooks/loop-bash-validator.sh:139`, `:149`; `hooks/lib/loop-common.sh:437`). Goal tracker mutation is blocked after round 0, with Claude instructed to request updates through the round summary (`hooks/lib/loop-common.sh:1002`; `hooks/loop-write-validator.sh:188`; `hooks/loop-bash-validator.sh:343`). Prompt files are read-only, todos files are blocked except allowlisted early files, and `.humanize` should not be committed (`hooks/lib/loop-common.sh:324`, `:347`, `:358`, `:812`).
- dependencies_and_callers: Sources `hooks/lib/template-loader.sh` and uses `load_and_render_safe` for fallback-safe template messages (`hooks/lib/loop-common.sh:135`, `:140`). Depends on `jq`, `sed`, `grep`, `awk`, `tr`, `ls`, `sort`, `head`, `mv`, and optionally `iconv` and `git check-ignore`. Sourced by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-bash-validator.sh`, `loop-edit-validator.sh`, `loop-plan-file-validator.sh`, `loop-codex-stop-hook.sh`, `pr-loop-stop-hook.sh`, `setup-rlcr-loop.sh`, and `setup-pr-loop.sh` per direct references. Concrete gate call sites include read validation of wrong locations (`hooks/loop-read-validator.sh:122`), write validation of state/goal tracker/summary files (`hooks/loop-write-validator.sh:152`, `:192`, `:202`), bash validation of git push, state moves, goal tracker edits, prompt/summary/todos mutations (`hooks/loop-bash-validator.sh:96`, `:139`, `:349`, `:365`, `:375`, `:385`), and plan-file branch/tracking gates (`hooks/loop-plan-file-validator.sh:79`, `:96`, `:123`).
- edge_cases_or_failure_modes: `is_state_file_path()` matches `finalize-state.md`, so callers must check finalize-state first; several validators explicitly document this ordering (`hooks/loop-write-validator.sh:150`, `hooks/loop-edit-validator.sh:115`, `hooks/loop-bash-validator.sh:132`). `find_active_loop()` ignores older active-looking directories by design, which avoids zombie loops but means a newer malformed/partial directory can mask an older valid one (`hooks/lib/loop-common.sh:147`). `parse_state_file_strict()` accepts negative numeric values because the regex allows `-?[0-9]+`; semantic range enforcement is left to callers (`:280`). `command_modifies_file()` is regex-based and covers many common write forms but not every shell grammar edge; tests note at least one limitation for multi-source `cp` in `tests/test-bash-validator-patterns.sh` search results. Cancel authorization is intentionally strict and may reject benign quoting variants, but that is safer for state-file mutation (`hooks/lib/loop-common.sh:437`). `update_pr_goal_tracker()` mutates files and uses simple parsing, so malformed tracker layout can cause insertion at fallback points or duplicate-ish content if section headers do not match expected forms.
- validation_or_tests: Broadly covered by hook and robustness tests. `tests/robustness/test-hook-input-robustness.sh`, `test-state-file-robustness.sh`, `test-cancel-security-robustness.sh`, `test-session-robustness.sh`, `test-bash-validator-patterns.sh`, `test-allowlist-validators.sh`, `test-finalize-phase.sh`, `test-state-exit-naming.sh`, `test-plan-file-hooks.sh`, and PR-loop tests all source or exercise helpers from this file. Template reference coverage checks common message templates referenced by `loop-common.sh` (`tests/test-template-references.sh:149`). `tests/run-all-tests.sh` includes the relevant normal and robustness suites (`tests/run-all-tests.sh:35`, `:55`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-101 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR read validator when a model tries to read a `round-N-prompt.md` or `round-N-summary.md` file outside the active `.humanize/rlcr/<session>/` directory.
- algorithmic_behavior: The template renders a short corrective message: requested `{{FILE_PATH}}` is wrong because loop files are in `{{ACTIVE_LOOP_DIR}}/`; it lists the current prompt and summary file paths for `{{CURRENT_ROUND}}`; then it says that if the file is truly needed, use `cat {{FILE_PATH}}` (`prompt-template/block/wrong-file-location.md:1`, `:3`, `:5`, `:9`). It is not executable but participates in the gate by providing precise remediation text.
- inputs_outputs_state: Inputs are template variables from `hooks/loop-read-validator.sh`: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` (`hooks/loop-read-validator.sh:127`). Output is stderr text returned with hook exit code `2` to block the attempted Read (`hooks/loop-read-validator.sh:122`, `:127`, `:131`). No state mutation.
- gates_or_invariants: Enforces the invariant that round prompt/summary reads must come from the active loop directory and current round, not similarly named files elsewhere. The caller first determines the active loop, parses current round strictly, and verifies whether the path is in `.humanize/rlcr`; if not, this template is rendered as the block reason (`hooks/loop-read-validator.sh:80`, `:94`, `:122`).
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh` lines `122-131`. Rendering depends on `load_and_render_safe` from `template-loader.sh`, so the caller has an inline fallback if this template is missing (`hooks/loop-read-validator.sh:124`, `:127`; `hooks/lib/template-loader.sh:167`).
- edge_cases_or_failure_modes: The final remediation says `cat {{FILE_PATH}}`, which is unusual because the Read tool is blocked for wrong-location access while Bash `cat` may still be subject to other bash validators. It may be intended as an explicit escape hatch for files that are not loop-controlled or when the user truly needs historical context. If the template is missing, the fallback omits the detailed current-round prompt/summary list (`hooks/loop-read-validator.sh:124`).
- validation_or_tests: Read-validator tests and template-reference tests cover the surrounding mechanism. `tests/test-template-references.sh` checks common template references, while repository search shows `hooks/loop-read-validator.sh` directly references `block/wrong-file-location.md`. The concrete gate path is visible in `hooks/loop-read-validator.sh:118-132`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-131 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout abstraction used by setup scripts, stop hooks, PR-loop hooks, Git/GH checks, and Codex invocation paths. It verifies that timeout behavior is predictable across macOS/Linux fallback implementations.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14`, `:16`, `:17`), then runs 18 tests. Positive coverage verifies timeout implementation detection, valid implementation enum, quick command output/exit, timeout exit code `124`, argument passing, original exit-code preservation, and pipeline support through `sh -c` (`:31`, `:39`, `:51`, `:62`, `:79`, `:89`, `:102`). Negative/edge coverage verifies short timeout elapsed bound, zero timeout permissiveness, large output handling, rapid cycles, nonexistent command failure, empty command handling, special-character arguments, signal handling, fallback-chain detection, subshell command support, and `TIMEOUT_IMPL` export visibility (`:120`, `:139`, `:157`, `:170`, `:188`, `:201`, `:211`, `:221`, `:235`, `:249`, `:259`). It finishes with `print_test_summary` and exits with the failure count status (`:278`).
- inputs_outputs_state: Inputs are environment `PATH` and available timeout tools (`gtimeout`, GNU `timeout`, `python3`, `python`, or none), plus the sourced timeout functions. Outputs are PASS/FAIL lines and final exit status. It creates no explicit temp files itself. Test counters live in `tests/test-helpers.sh` globals (`tests/test-helpers.sh:22`), and final status is computed by `print_test_summary()` returning nonzero if any failures occurred (`tests/test-helpers.sh:58`, `:71`).
- gates_or_invariants: `detect_timeout_impl()` must return one of `gtimeout|timeout|python3|python|none` (`tests/robustness/test-timeout-robustness.sh:42`, `:239`). Any real timeout implementation must return `124` for timed-out commands (`:62`, `:120`). Successful commands must preserve stdout and exit status (`:54`, `:93`). Argument handling must preserve spaces and shell metacharacters when passed as args rather than evaluated by a shell (`:82`, `:214`). The test intentionally tolerates implementation variance for zero timeout and signal-trapping commands (`:147`, `:229`).
- dependencies_and_callers: Depends directly on `scripts/portable-timeout.sh`, where implementation priority is `gtimeout` > GNU `timeout` > `python3` > `python` > no timeout (`scripts/portable-timeout.sh:6`, `:10`), and `run_with_timeout()` dispatches accordingly (`scripts/portable-timeout.sh:33`, `:38`). Timeout is used in core runtime paths such as `hooks/loop-plan-file-validator.sh` for git branch/tracking checks (`hooks/loop-plan-file-validator.sh:19`, `:96`, `:127`, `:152`), `hooks/loop-codex-stop-hook.sh` for git/Codex operations per search results, PR-loop stop hooks, and setup scripts. `tests/run-all-tests.sh` includes this robustness test (`tests/run-all-tests.sh:64`).
- edge_cases_or_failure_modes: If no timeout implementation is available, timeout-specific tests are skipped as passes, while commands run unbounded through the `none` branch in `portable-timeout.sh` (`scripts/portable-timeout.sh:64`; `tests/robustness/test-timeout-robustness.sh:65`, `:122`, `:142`). The Python fallback in `portable-timeout.sh` uses `subprocess.run(sys.argv[1:], timeout=...)` without capturing output, so child stdout/stderr pass through normally and exit code `124` is synthesized on timeout (`scripts/portable-timeout.sh:47`). The test’s large-output assertion uses string comparison for `"$RESULT" -ge "1000"` but in Bash `[[ ... -ge ... ]]` coerces numerically, so it is acceptable if `wc -l` returns whitespace-trimmed or padded numeric text (`tests/robustness/test-timeout-robustness.sh:161`). Empty command behavior is not asserted strictly; it always passes after observing the exit code (`:201`).
- validation_or_tests: This file is itself the validation artifact. It validates the portable timeout layer used by Git/GH and model-runner commands. Shared helper behavior is in `tests/test-helpers.sh:30-78`, and suite inclusion is in `tests/run-all-tests.sh:54-64`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5; each assigned item appears exactly once as an Item Evidence heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`