# agent_21 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-021 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition that exposes the RLCR loop entry point. It is a thin command wrapper around `scripts/setup-rlcr-loop.sh`, with allowed tool scope limited to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` at `commands/start-rlcr-loop.md:4` and invocation at `commands/start-rlcr-loop.md:12-14`.
- algorithmic_behavior: Defines the user-facing lifecycle: implement a plan, write a round summary, exit attempt triggers Codex review, Codex feedback continues the loop, and a terminal marker ends or transitions the loop. The documented review contract is at `commands/start-rlcr-loop.md:16-23`. It also introduces the Goal Tracker model: immutable goal/AC section, mutable task/evolution/deferral state, AC mapping, explicit deferrals, and periodic full alignment checks at `commands/start-rlcr-loop.md:24-42`.
- inputs_outputs_state: Input is `$ARGUMENTS`, accepting a positional plan path or `--plan-file`, max iterations, Codex model/effort, timeout, plan tracking, and push policy per `commands/start-rlcr-loop.md:3`. The wrapper outputs whatever `setup-rlcr-loop.sh` creates and prints. The setup script writes `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md`; state fields include `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, and `start_branch` in `scripts/setup-rlcr-loop.sh:539-552`.
- gates_or_invariants: The command itself states operational invariants: write summaries, maintain `goal-tracker.md`, include implementation/test details, do not edit state or cancel to evade the loop, and trust Codex review at `commands/start-rlcr-loop.md:43-49`. Setup enforces stronger gates: plan must be relative, no spaces/metacharacters, non-symlink, inside project, not in submodule, with enough content; see `scripts/setup-rlcr-loop.sh:258-359` and content checks at `scripts/setup-rlcr-loop.sh:416-469`.
- dependencies_and_callers: Direct dependency is `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`. That script depends on `hooks/lib/loop-common.sh` for active-loop mutual exclusion at `scripts/setup-rlcr-loop.sh:188-199`, Git/Codex availability, the prompt-template tree, and the stop/validator hooks that consume created state. The command text is hidden from slash-command tooling via `hide-from-slash-command-tool` at `commands/start-rlcr-loop.md:5`.
- edge_cases_or_failure_modes: User-facing stop conditions list max iteration, Codex `COMPLETE`, or cancellation at `commands/start-rlcr-loop.md:51-55`. There is a wording drift: this file says Codex `COMPLETE` means the loop ends and says "all ACs met or validly deferred" at `commands/start-rlcr-loop.md:54`, while the current stop hook actually enters a Finalize Phase unless already at max iterations (`hooks/loop-codex-stop-hook.sh:966-1025`) and full alignment forbids deferrals before `COMPLETE` (`prompt-template/codex/full-alignment-review.md:88-93`). This command is therefore slightly stale as workflow documentation.
- validation_or_tests: Coverage is indirect through setup/loop tests. `tests/test-finalize-phase.sh` verifies the current post-`COMPLETE` behavior: `COMPLETE` enters Finalize Phase at `tests/test-finalize-phase.sh:465-481`, max iteration skips Finalize at `tests/test-finalize-phase.sh:483-501`, and normal non-`COMPLETE` rounds remain unaffected at `tests/test-finalize-phase.sh:580-610`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-051 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state machine. It creates an isolated Git repo, mock Codex binaries, loop state, summaries, and transcripts, then asserts hook and validator behavior for normal rounds, `COMPLETE`, Finalize, max-iteration exit, and protected files.
- algorithmic_behavior: The test defines positive and negative requirements in its header at `tests/test-finalize-phase.sh:5-19`. It sources `hooks/lib/loop-common.sh` at `tests/test-finalize-phase.sh:48-49`, creates mock `codex` commands at `tests/test-finalize-phase.sh:51-85`, initializes a minimal Git repo and plan at `tests/test-finalize-phase.sh:87-121`, and creates loop state/goal tracker fixtures at `tests/test-finalize-phase.sh:123-168`.
- inputs_outputs_state: Inputs are generated hook JSON payloads for Write/Edit/Bash/Stop/UserPromptSubmit validators and synthetic transcript JSONL. Outputs are pass/fail counters and hook side effects in `$TEST_DIR/.humanize/rlcr/2024-01-01_12-00-00`. The main state transitions under test are `state.md -> finalize-state.md` on Codex `COMPLETE`, `finalize-state.md -> complete-state.md` after finalize checks pass, and `state.md -> maxiter-state.md` when max iterations are reached.
- gates_or_invariants: The tests assert helper recognizers for `finalize-state.md` and `finalize-summary.md` at `tests/test-finalize-phase.sh:179-220`; active-loop detection includes `finalize-state.md` but excludes `complete-state.md` at `tests/test-finalize-phase.sh:231-260`; write/edit/bash validators block `finalize-state.md` modification but allow the active loop's `finalize-summary.md` at `tests/test-finalize-phase.sh:286-357`; finalize exit still requires summary, clean Git state, and completed todos at `tests/test-finalize-phase.sh:370-445` and `tests/test-finalize-phase.sh:503-546`.
- dependencies_and_callers: Exercises `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and shared functions from `hooks/lib/loop-common.sh`. The implementation backing these expectations detects `finalize-state.md` as active in `hooks/lib/loop-common.sh:147-170`, chooses Finalize phase in the stop hook at `hooks/loop-codex-stop-hook.sh:66-82`, and skips Codex during finalize completion at `hooks/loop-codex-stop-hook.sh:648-660`.
- edge_cases_or_failure_modes: Explicitly covers missing `finalize-summary.md`, dirty Git state, incomplete todos, attempts to write/edit/redirect/move/copy `finalize-state.md`, max-iteration `COMPLETE` behavior, and malformed parser risks in validators. It also asserts Finalize does not invoke Codex by checking a marker file after summary and completion paths at `tests/test-finalize-phase.sh:387-393` and `tests/test-finalize-phase.sh:439-445`.
- validation_or_tests: This file is itself the validation artifact. It exits with `TESTS_FAILED` at `tests/test-finalize-phase.sh:672-681`. It also validates normal RLCR behavior is preserved: a non-`COMPLETE` review blocks exit, keeps `state.md`, increments `current_round` to 4, writes `round-3-review-result.md`, and surfaces Codex feedback at `tests/test-finalize-phase.sh:580-619`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-081 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR push policy gate. It tells the agent that current commits should stay local and points to `--push-every-round` when remote pushes are required.
- algorithmic_behavior: The template has no control flow by itself; it is rendered by `load_and_render_safe` when the Bash validator detects `git push` while `push_every_round` is not true. The Bash validator parses active loop state, reads `STATE_PUSH_EVERY_ROUND`, matches `git push`, then emits this template and exits 2 at `hooks/loop-bash-validator.sh:74-105`.
- inputs_outputs_state: Inputs are the active loop state field `push_every_round` and the attempted Bash command. Output is the blocking text from `prompt-template/block/git-push.md:1-9` to stderr, plus validator exit code 2. State is not mutated.
- gates_or_invariants: Enforces the default invariant that RLCR commits remain local unless the loop was started with `--push-every-round`. The template states that local handling continues until completion at `prompt-template/block/git-push.md:3-4`, and its remediation command is `/humanize:start-rlcr-loop plan.md --push-every-round` at `prompt-template/block/git-push.md:6-9`.
- dependencies_and_callers: Caller is `hooks/loop-bash-validator.sh:96-104`. The state field is written by setup at `scripts/setup-rlcr-loop.sh:539-552`, based on argument parsing at `scripts/setup-rlcr-loop.sh:148-151`. Template loading comes from `hooks/lib/template-loader.sh`, whose safe loader falls back only if the template is missing or renders empty at `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: The gate only matches commands beginning with `git push` after leading whitespace (`hooks/loop-bash-validator.sh:96-99`). Commands such as `command git push`, `env ... git push`, shell-wrapped `sh -c 'git push'`, aliases, or `cd repo && git push` are not covered by this specific regex. If the template is unavailable, the Bash validator uses an inline fallback at `hooks/loop-bash-validator.sh:99-103`.
- validation_or_tests: Template loader tests confirm `block/git-push.md` loads and overrides fallback text at `tests/test-template-loader.sh:55-65` and `tests/test-template-loader.sh:210-222`; comprehensive template tests repeat the safe-loader fallback/real-template behavior at `tests/test-templates-comprehensive.sh:445-463`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-111 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex review template for mandatory full goal-alignment checkpoints. It expands normal implementation review into a comprehensive audit of the original plan, Goal Tracker, deferrals, forgotten items, historical progress, and stagnation.
- algorithmic_behavior: The stop hook selects this template when `CURRENT_ROUND % 5 == 4` at `hooks/loop-codex-stop-hook.sh:686-690`, then renders it into `round-N-review-prompt.md` with variables including plan, summary, goal tracker, docs path, completed iteration count, loop timestamp, previous rounds, and review-result path at `hooks/loop-codex-stop-hook.sh:721-735`. The template itself requires Codex to read the original plan first at `prompt-template/codex/full-alignment-review.md:5-10`, audit every AC at `prompt-template/codex/full-alignment-review.md:19-48`, perform implementation review at `prompt-template/codex/full-alignment-review.md:50-55`, and run stagnation analysis over recent history at `prompt-template/codex/full-alignment-review.md:59-85`.
- inputs_outputs_state: Inputs are rendered placeholders: `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE`. Output is a Codex-written `round-N-review-result.md`, whose last non-empty line drives the stop-hook state machine: `COMPLETE` enters Finalize Phase, `STOP` ends as stop-state, any other content advances to the next round. Marker parsing is strict at `hooks/loop-codex-stop-hook.sh:957-967` and STOP handling is at `hooks/loop-codex-stop-hook.sh:1028-1055`.
- gates_or_invariants: The template is stricter than regular review: deferrals count as incomplete, and `COMPLETE` is allowed only when all original-plan ACs are fully met with no deferrals at `prompt-template/codex/full-alignment-review.md:86-93`. It also introduces a circuit breaker: if development stagnates, Codex must write `STOP` as the last line instead of `COMPLETE` at `prompt-template/codex/full-alignment-review.md:74-85`.
- dependencies_and_callers: Depends on `hooks/loop-codex-stop-hook.sh`, the shared Goal Tracker update section loaded at `hooks/loop-codex-stop-hook.sh:680-684`, and template rendering semantics from `hooks/lib/template-loader.sh:50-132`. It is sibling to `prompt-template/codex/regular-review.md`, which also forbids deferrals before `COMPLETE` but lacks historical stagnation/circuit-breaker analysis; see `prompt-template/codex/regular-review.md:33-57`.
- edge_cases_or_failure_modes: The template says "every 5 rounds" at `prompt-template/codex/full-alignment-review.md:3`, while `commands/start-rlcr-loop.md:36` phrases it as rounds 4, 9, 14 "after every 4 rounds of work"; implementation resolves this as `CURRENT_ROUND % 5 == 4`. Historical paths for previous rounds can produce negative placeholders in very early rounds, but this template is first selected at round 4, so `PREV_ROUND` and `PREV_PREV_ROUND` are valid then. If Codex writes "COMPLETE" anywhere other than the final non-empty line, the stop hook treats it as non-terminal feedback by design.
- validation_or_tests: No direct template-specific test was found for `full-alignment-review.md`; coverage is via the stop hook render path at `hooks/loop-codex-stop-hook.sh:721-735` and marker/state behavior tested in `tests/test-finalize-phase.sh`. Template loader behavior used by this path is covered generally in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections present; each assigned item header appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`