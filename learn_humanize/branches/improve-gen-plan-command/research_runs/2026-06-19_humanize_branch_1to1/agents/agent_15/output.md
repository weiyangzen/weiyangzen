# agent_15 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-015 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Robustness regression harness for the shell-based loop algorithms: RLCR setup validation, state/session discovery, hook input validation, command modification detection, cancel authorization, timeout portability, template rendering, goal-tracker parsing, git status parsing, and monitor survival under terminal/log edge cases.
- algorithmic_behavior: The directory contains 10 executable suites, inspected recursively with no depth limit: `test-goal-tracker-robustness.sh`, `test-cancel-security-robustness.sh`, `test-path-validation-robustness.sh`, `test-timeout-robustness.sh`, `test-git-operations-robustness.sh`, `test-plan-file-robustness.sh`, `test-state-file-robustness.sh`, `test-hook-input-robustness.sh`, `test-template-stress-robustness.sh`, and `test-session-robustness.sh`.
- algorithmic_behavior: Goal tracker tests source `scripts/humanize.sh` and assert `humanize_parse_goal_tracker` returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary`, covering list/table AC formats, active/completed/deferred/open issue counts, truncation, malformed files, binary content, and large counts (`tests/robustness/test-goal-tracker-robustness.sh:21`, `:35`, `:60`, `:155`, `:237`, `:258`, `:349`, `:501`; production parser in `scripts/humanize.sh:30`).
- algorithmic_behavior: Cancel-security tests source `hooks/lib/loop-common.sh` and exercise `is_cancel_authorized`, requiring `.cancel-requested`, exact `mv` from `state.md` or `finalize-state.md` to `cancel-state.md`, and rejection of substitution, shell chaining, wrong paths, extra args, symlinks, and the path-containing-`finalize` regression (`tests/robustness/test-cancel-security-robustness.sh:39`, `:99`, `:109`, `:135`, `:174`, `:317`, `:332`, `:369`; implementation `hooks/lib/loop-common.sh:437`, `:461`, `:466`, `:585`, `:600`).
- algorithmic_behavior: Path and plan-file tests execute `scripts/setup-rlcr-loop.sh` in mock git repos and classify validation outcomes by production error text. They cover relative path acceptance, absolute path rejection, shell metacharacter rejection, symlink and parent-symlink rejection, long/deep paths, empty/comment-only/short/missing/directory plans, large 1MB+ files, CRLF/LF, null/binary content, and disappearing plans (`tests/robustness/test-path-validation-robustness.sh:55`, `:101`, `:158`, `:321`, `:404`; `tests/robustness/test-plan-file-robustness.sh:22`, `:152`, `:274`, `:299`, `:501`; production gates `scripts/setup-rlcr-loop.sh:258`, `:272`, `:284`, `:297`, `:416`, `:426`).
- algorithmic_behavior: Timeout tests source `scripts/portable-timeout.sh` and verify `detect_timeout_impl` fallback order plus `run_with_timeout` behavior for success, timeout exit 124, preserved command exit code, pipelines, zero timeout, high output, rapid cycles, nonexistent commands, special characters, signal traps, and exported `TIMEOUT_IMPL` (`tests/robustness/test-timeout-robustness.sh:31`, `:62`, `:89`, `:120`, `:157`, `:170`, `:235`; implementation `scripts/portable-timeout.sh:9`, `:29`, `:33`, `:47`, `:64`).
- algorithmic_behavior: Git tests source `humanize_parse_git_status` and `humanize_detect_git_state`, validating clean/modified/added/deleted/untracked/renamed/binary/space-file states, insertion/deletion counts, detached HEAD, rebase, merge, shallow clone, non-git dirs, empty repos, and permission simulation (`tests/robustness/test-git-operations-robustness.sh:32`, `:71`, `:168`, `:180`, `:197`, `:369`; implementation `scripts/humanize.sh:114`, `:164`).
- algorithmic_behavior: State/session tests validate `parse_state_file`, `parse_state_file_strict`, `get_current_round`, and `find_active_loop`: tolerant parsing defaults missing fields, strict mode rejects missing YAML separators, missing fields, and non-numeric round/max fields; session detection is newest-directory-only and treats `state.md` or `finalize-state.md` as active while ignoring `complete-state.md` and `cancel-state.md` (`tests/robustness/test-state-file-robustness.sh:33`, `:147`, `:171`, `:199`, `:228`, `:363`; `tests/robustness/test-session-robustness.sh:33`, `:48`, `:89`, `:259`; implementation `hooks/lib/loop-common.sh:147`, `:172`, `:187`, `:231`).
- algorithmic_behavior: Hook-input tests pipe JSON into actual hook validators and require graceful rejection of malformed/missing-field/deeply nested/non-UTF8 input while allowing valid Read/Write/Bash payloads and unknown tools. They also exercise monitor edge cases, command modification detection, and direct monitor helper integration (`tests/robustness/test-hook-input-robustness.sh:35`, `:71`, `:117`, `:177`, `:201`, `:245`, `:351`, `:381`, `:444`; command detection `hooks/lib/loop-common.sh:972`).
- algorithmic_behavior: Template stress tests validate the shared renderer contract: single-pass `{{VAR}}` replacement, fallback rendering, large values/templates, many variables, regex/sed-special characters, backslashes, newlines, empty values, repeated placeholders, and placeholder-in-value injection prevention (`tests/robustness/test-template-stress-robustness.sh:33`, `:44`, `:82`, `:111`, `:134`, `:161`, `:195`, `:218`; implementation `hooks/lib/template-loader.sh:50`, `:71`, `:116`, `:167`).
- inputs_outputs_state: Inputs are generated temp repos, loop directories, YAML-frontmatter state files, plan files, JSON hook payloads, git repositories, shell command strings, template strings, and monitor log/session artifacts. Outputs are pass/fail assertions via `tests/test-helpers.sh`, return codes from production scripts/functions, pipe-delimited parser summaries, filesystem state transitions such as `state.md -> cancel-state.md`, and monitor exit codes.
- gates_or_invariants: Fail closed on unsafe plan paths, unreadable/missing/insufficient plans, symlink traversal, invalid JSON, non-UTF8 hook input, unauthorized state-file moves, shell injection patterns, wrong session state, wrong active loop phase, and malformed strict state files. Invariants include newest-session-only active-loop selection, `finalize-state.md` as active finalize phase marker, `complete-state.md`/`cancel-state.md` as inactive terminal markers, and template single-pass substitution.
- dependencies_and_callers: The suites depend on `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and the real validators under `hooks/`. They coordinate with sibling broader tests such as `tests/test-finalize-phase.sh`, `tests/test-template-loader.sh`, and PR-loop tests by exercising the same helpers under adversarial inputs rather than standard happy paths.
- edge_cases_or_failure_modes: Covered failures include binary/null content, CRLF line endings, very long files/values/paths, many sessions/files, symlinks, permission variance, absent tools, command substitution, shell chaining, mixed quotes, trailing-space strictness, deep JSON, Bash stripping null bytes, terminal width extremes, deleted logs/session dirs, and root/platform-specific permission behavior.
- validation_or_tests: The directory itself is validation evidence. Each suite ends with `print_test_summary ...; exit $?`, making nonzero test failures visible to the test runner. I inspected contents only and did not execute the suites.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-045 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Test fixture generator for PR-loop monitor behavior, especially parsing YAML-list `configured_bots` and `active_bots` states used by `humanize monitor pr`.
- algorithmic_behavior: Accepts `<test_dir> <test_name>`, validates that `TEST_DIR` is present, then dispatches on `TEST_NAME` through `yaml_list`, `configured`, or `empty` cases (`tests/setup-monitor-test-env.sh:12`, `:15`, `:20`). Each case creates `.humanize/pr-loop/<timestamp>/state.md` and writes YAML frontmatter with `current_round`, `max_iterations`, `pr_number`, `start_branch`, bot lists, codex model/effort, and `started_at` (`:23`, `:24`, `:25`).
- inputs_outputs_state: Input `TEST_DIR` determines the project fixture root. Input `TEST_NAME` chooses state shape. Output is the printed `TEST_DIR` path (`:91`) plus a new `.humanize/pr-loop/<timestamp>/state.md`. `yaml_list` sets both bots active (`:31`, `:34`), `configured` keeps both configured but only `codex` active (`:53`, `:56`), and `empty` leaves `active_bots:` empty to model all-approved state (`:74`, `:77`).
- gates_or_invariants: Missing `TEST_DIR` exits 1 with usage (`:15`). Unknown fixture names exit 1 and enumerate valid names (`:84`). Generated timestamps are deterministic per scenario, which lets tests find stable session paths. The state schema preserves YAML-list indentation with `  - bot`, matching monitor parsing in `scripts/humanize.sh:1001`.
- dependencies_and_callers: Called by `tests/test-pr-loop-hooks.sh` monitor tests: `test_monitor_yaml_list_parsing`, `test_monitor_configured_bots`, and `test_monitor_empty_active_bots` (`tests/test-pr-loop-hooks.sh:1548`, `:1553`, `:1575`, `:1580`, `:1597`, `:1602`). The produced state files feed `_humanize_monitor_pr` parsing of `configured_bots` and `active_bots` (`scripts/humanize.sh:989`, `:1001`, `:1012`, `:1091`, `:1215`).
- edge_cases_or_failure_modes: It does not create `goal-tracker.md` or monitor log files, so it is scoped to PR-loop state parsing rather than full monitor rendering. It assumes callers isolate `TEST_DIR`; rerunning the same case overwrites the deterministic state file. The `empty` case intentionally uses a bare `active_bots:` line to test parser defaults.
- validation_or_tests: Its direct consumers assert monitor output for YAML-list parsing, partial approval, and all-approved/empty-active-bots behavior. The helper itself has no internal test runner beyond `set -euo pipefail` and case validation.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-075 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Canonical block message for attempts to modify `finalize-state.md`, the loop-owned state marker used during Finalize Phase.
- algorithmic_behavior: Provides a human-facing denial contract: `finalize-state.md` cannot be modified because it is managed by the loop system during Finalize Phase (`prompt-template/block/finalize-state-file-modification.md:1`, `:3`). It redirects the agent toward the allowed finalize workflow: run code simplifier, commit changes, and write `finalize-summary.md` (`:5`, `:6`, `:7`, `:8`).
- inputs_outputs_state: Input is implicit through `load_and_render_safe` when validators call `finalize_state_file_blocked_message`; this template has no variables. Output is the block reason emitted by read/write/edit/bash validators when a tool targets `finalize-state.md`.
- gates_or_invariants: Invariant is that `finalize-state.md` is system-owned and not an editable task artifact. This prevents agents from bypassing Finalize Phase state transitions while still allowing `finalize-summary.md` as the writable completion artifact.
- dependencies_and_callers: Loaded by `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:376`, with fallback text if the template is unavailable (`hooks/lib/loop-common.sh:377`, `:382`). Used by write/edit/bash validators when `finalize-state.md` is targeted; the validator search shows exact call sites in `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`.
- edge_cases_or_failure_modes: The template is deliberately short and variable-free, so rendering cannot fail due to missing variables. The related implementation must check `finalize-state.md` before generic `state.md` because `state.md` suffix matching also matches `finalize-state.md`; the branch documents that invariant in validators and tests. Cancellation is the controlled exception path: `is_cancel_authorized` allows moving `finalize-state.md` to `cancel-state.md` only with `.cancel-requested` and exact path checks (`hooks/lib/loop-common.sh:451`, `:585`, `:593`).
- validation_or_tests: `tests/test-finalize-phase.sh` validates the function exists, detects finalize-state as active, blocks Write/Edit/Bash modifications, blocks unauthorized `mv`/`cp` from finalize-state, and parses finalize-state through validators. Robustness coverage also checks authorized cancel from finalize-state and symlink rejection (`tests/robustness/test-cancel-security-robustness.sh:51`, `:353`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-105 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize Phase transition prompt emitted after Codex review passes. It defines the last loop phase before completion: functionality-equivalent simplification, validation, commit, and summary.
- algorithmic_behavior: The prompt declares Codex review passed and implementation/acceptance criteria are complete (`prompt-template/claude/finalize-phase-prompt.md:1`, `:3`). It instructs Claude to invoke the `code-simplifier:code-simplifier` subagent via Task (`:7`, `:9`, `:11`, `:13`) and constrains all work to no functionality changes, no test failures, no new bugs, and only equivalent cleanup (`:16`, `:20`, `:21`, `:22`, `:23`).
- inputs_outputs_state: Template inputs are `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` (`:35`, `:37`, `:38`, `:40`, `:44`). Rendered output becomes the hook block reason returned to Claude when the stop hook transitions into Finalize Phase. Required output artifact is `finalize-summary.md` at the rendered `FINALIZE_SUMMARY_FILE`, including simplifications, modified files, test confirmation, and refactoring notes (`:46`, `:47`, `:48`, `:49`, `:50`).
- gates_or_invariants: The prompt gates final completion on a cleanup-only pass plus tests still passing. It requires todos completed, a descriptive commit, and a finalize summary before exiting (`:40`, `:42`, `:43`, `:44`). It explicitly limits focus to code changed in the RLCR session and quality improvements such as removing complexity, duplicate code, dead code, and simplifying flow (`:25`, `:27`, `:29`, `:31`, `:32`, `:33`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` when Codex emits COMPLETE and max-iteration handling does not short-circuit. The hook renames `state.md` to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:984`, `:985`), sets `FINALIZE_SUMMARY_FILE` (`:988`, `:989`), loads this template with all three variables (`:1010`, `:1011`, `:1012`, `:1013`), and returns a JSON block decision containing the rendered prompt (`:1017`, `:1021`, `:1022`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to an inline finalize prompt (`hooks/loop-codex-stop-hook.sh:990`, `:1010`; renderer fallback behavior in `hooks/lib/template-loader.sh:167`). Because the renderer is single-pass, any `{{...}}` sequences inside variable values are not recursively expanded (`hooks/lib/template-loader.sh:50`, `:54`, `:116`). The phase must not be entered on max-iteration failure; tests cover max-iteration skip behavior in finalize-phase tests.
- validation_or_tests: `tests/test-finalize-phase.sh` validates COMPLETE triggers `state.md -> finalize-state.md` and returns a block prompt, validates finalize completion renames to `complete-state.md`, validates max-iterations skip finalize, and validates validators parse `finalize-state.md`. Template rendering behavior is stress-tested in `tests/robustness/test-template-stress-robustness.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item sections present; assigned item IDs appear once each in the Item Evidence headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`