# agent_06 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime shell orchestration layer for Humanize workflows. The directory owns loop setup/cancel, terminal monitoring, PR review polling/comment fetching, bot reaction checks, gen-plan IO validation, and shared timeout/monitor utilities.
- algorithmic_behavior: `setup-rlcr-loop.sh` parses loop options, enforces one active loop, validates plan path/content/tracking, creates `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `plan.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:186`, `scripts/setup-rlcr-loop.sh:258`, `scripts/setup-rlcr-loop.sh:539`, `scripts/setup-rlcr-loop.sh:655`). `setup-pr-loop.sh` performs analogous PR-loop startup with bot selection, PR lookup, and prompt/state creation (`scripts/setup-pr-loop.sh:49`, `scripts/setup-pr-loop.sh:208`, `scripts/setup-pr-loop.sh:341`). Cancel scripts locate newest loop session, create `.cancel-requested`, and rename active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:134`; `scripts/cancel-pr-loop.sh:72`, `scripts/cancel-pr-loop.sh:118`). PR helper scripts resolve fork/base repos, fetch issue/review/review-submission data, filter or format it, and emit JSON/markdown (`scripts/check-pr-reviewer-status.sh:109`, `scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`).
- inputs_outputs_state: Inputs are CLI flags, repository git state, plan/draft paths, GitHub PR/comment identifiers, `.humanize` state files, and cache/log files. Outputs are loop directories and state artifacts, generated prompts/trackers, markdown comment reports, JSON reviewer status/poll results, reaction JSON, validation messages, and monitor terminal output.
- gates_or_invariants: The directory enforces single active loop across RLCR/PR (`scripts/setup-rlcr-loop.sh:196`, `scripts/setup-pr-loop.sh:222`), strict plan path safety (`scripts/setup-rlcr-loop.sh:258`), git repo/commit presence (`scripts/setup-rlcr-loop.sh:242`), tracked-vs-gitignored plan policy (`scripts/setup-rlcr-loop.sh:385`), content sufficiency (`scripts/setup-rlcr-loop.sh:416`), codex availability before activation (`scripts/setup-rlcr-loop.sh:471`), and gen-plan input/output preconditions with distinct exit codes (`scripts/validate-gen-plan-io.sh:4`, `scripts/validate-gen-plan-io.sh:78`).
- dependencies_and_callers: Command markdown calls these scripts, especially `commands/gen-plan.md` invoking `scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:30`). Scripts depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `gh`, `jq`, `git`, `codex`, POSIX shell utilities, and `.humanize/*` runtime state.
- edge_cases_or_failure_modes: Handles fork PRs by resolving base repos; failed GitHub endpoints are retried or downgraded to empty arrays depending on script; missing loops return no-loop states; finalize cancellation requires `--force` in RLCR; plan files reject absolute paths, spaces, metacharacters, symlinks, parent symlink traversal, submodule paths, unreadable/missing files, too-short files, and comment-only files.
- validation_or_tests: Covered by `tests/test-gen-plan.sh`, `tests/test-plan-file-validation.sh`, `tests/robustness/test-path-validation-robustness.sh`, PR-loop suites, monitor suites, and robustness suites. I attempted assigned test execution, but the read-only sandbox blocked temp-file creation: `tests/test-template-loader.sh` failed at Bash here-string temp creation and `tests/robustness/test-path-validation-robustness.sh` failed at `mktemp`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-036 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell function for `humanize monitor rlcr` and `humanize monitor pr`; it renders live fixed-header monitors for loop state, progress, git changes, current files, and latest session activity.
- algorithmic_behavior: Sources `scripts/lib/monitor-common.sh` when present (`scripts/humanize.sh:6`). Public helpers split pipe-delimited values cross-shell (`scripts/humanize.sh:18`), parse goal trackers into counts and summary (`scripts/humanize.sh:32`), detect git special states (`scripts/humanize.sh:116`), and summarize git porcelain/diffstat (`scripts/humanize.sh:166`). `_humanize_monitor_codex` finds latest timestamped RLCR session and cache log, parses state, draws an 11-line status bar, tails logs incrementally, switches to newer sessions/logs, and exits cleanly when loop dirs disappear (`scripts/humanize.sh:219`, `scripts/humanize.sh:245`, `scripts/humanize.sh:333`, `scripts/humanize.sh:579`). `_humanize_monitor_pr` mirrors this for PR loop markdown artifacts with optional `--once` snapshot mode (`scripts/humanize.sh:890`, `scripts/humanize.sh:927`, `scripts/humanize.sh:1174`).
- inputs_outputs_state: Inputs are `.humanize/rlcr` or `.humanize/pr-loop` session directories, state files, `goal-tracker.md`, cached Codex logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, git working tree status, terminal size, and monitor args. Outputs are terminal status bars, recent log/file tails, usage errors, and process cleanup side effects for background tail jobs.
- gates_or_invariants: Requires target loop directory to exist before monitoring (`scripts/humanize.sh:230`, `scripts/humanize.sh:914`). Session names must match timestamp pattern before being considered (`scripts/humanize.sh:269`; shared helper at `scripts/lib/monitor-common.sh:55`). State file priority treats `state.md` as active before stop-state files (`scripts/lib/monitor-common.sh:162`). Trap cleanup is guarded by `cleanup_done` to avoid repeated terminal restoration and stale tail processes (`scripts/humanize.sh:496`, `scripts/humanize.sh:1135`).
- dependencies_and_callers: Called through the shell function dispatcher `humanize monitor <rlcr|pr>` (`scripts/humanize.sh:843`). Delegates latest-session, state-file, PR phase, and PR tracker parsing to `scripts/lib/monitor-common.sh` (`scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:267`, `scripts/lib/monitor-common.sh:468`).
- edge_cases_or_failure_modes: Explicitly handles no loop dir, no session dirs, no log files yet, deleted session dirs, deleted logs, truncated/rotated logs, newer session arrival before log creation, zsh array indexing, zsh/bashing traps, missing state fields, non-git dirs, and terminal restoration after interrupts.
- validation_or_tests: Related coverage appears in `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/manual-monitor-test.sh`, `tests/test-zsh-monitor-safety.sh`, `tests/robustness/test-goal-tracker-robustness.sh`, and PR-loop system tests. I did not run these broader monitor tests because they require writable runtime state and/or interactive terminal behavior.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-066 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, especially template directory discovery, safe loading, single-pass rendering, fallback behavior, and literal handling of dangerous shell-like content.
- algorithmic_behavior: Sources the loader (`tests/test-template-loader.sh:12`), computes the expected `prompt-template` root (`tests/test-template-loader.sh:45`), verifies existing and missing loads (`tests/test-template-loader.sh:59`, `tests/test-template-loader.sh:72`), exercises single/multiple/multiline substitutions (`tests/test-template-loader.sh:85`, `tests/test-template-loader.sh:100`, `tests/test-template-loader.sh:115`), validates `load_and_render` against a real wrong-round template (`tests/test-template-loader.sh:151`), and checks safe fallback rendering (`tests/test-template-loader.sh:201`).
- inputs_outputs_state: Inputs are in-repo templates plus synthetic template strings and `VAR=value` arguments. Outputs are pass/fail counters printed to stdout and exit status 0/1 (`tests/test-template-loader.sh:641`).
- gates_or_invariants: Requires missing placeholders to remain unchanged (`tests/test-template-loader.sh:186`), unused variables to be ignored (`tests/test-template-loader.sh:171`), missing templates to fall back (`tests/test-template-loader.sh:201`), and `validate_template_dir` to reject missing dirs (`tests/test-template-loader.sh:229`, `tests/test-template-loader.sh:240`).
- dependencies_and_callers: Directly specifies `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` in `hooks/lib/template-loader.sh`. That loader uses AWK single-pass replacement with `TMPL_VAR_` env vars (`hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:74`) and is consumed by hook validators and stop hooks.
- edge_cases_or_failure_modes: Tests literal rendering for `&`, backslashes, `$`, backticks, pipes, semicolons, glob characters, parentheses, redirection text, quotes, hash, tilde, regex, JSON, Windows paths, multiline values, empty values, non-ASCII Unicode, underscore-prefixed names, and numbered names (`tests/test-template-loader.sh:257`, `tests/test-template-loader.sh:437`, `tests/test-template-loader.sh:473`, `tests/test-template-loader.sh:555`, `tests/test-template-loader.sh:617`). It also prevents placeholder injection by ensuring placeholders inside inserted values are not rescanned (`tests/test-template-loader.sh:499`).
- validation_or_tests: Attempted `bash tests/test-template-loader.sh`; only loader path/load-dir checks ran, while render tests failed because the read-only sandbox prevented Bash from creating here-string temp files at `hooks/lib/template-loader.sh:129`. This is an environment limitation of the branch export, not evidence that the executable spec is intentionally failing.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-096 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Guard-message template for the rule that RLCR summary files must be written through Write/Edit hooks, not Bash redirection or text-mutating shell commands.
- algorithmic_behavior: Provides a short rendered block with one variable, `{{CORRECT_PATH}}`, telling the agent to use the Write or Edit tool for the current round summary (`prompt-template/block/summary-bash-write.md:1`, `prompt-template/block/summary-bash-write.md:5`). It is loaded through `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:385`) and triggered by Bash validator detection of commands modifying `round-[0-9]+-summary.md` (`hooks/loop-bash-validator.sh:375`).
- inputs_outputs_state: Input is `CORRECT_PATH`, normally `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-bash-validator.sh:376`). Output is a stderr-facing block message used to stop the shell command path with the validator’s block exit.
- gates_or_invariants: Reinforces that Bash commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/summary-bash-write.md:7`). The invariant is that summary round-number/path validation must happen through tool validators, not shell writes.
- dependencies_and_callers: Depends on template-loader single-pass rendering. Caller chain is `loop-bash-validator.sh` → `summary_bash_blocked_message` → `load_and_render_safe` → this template (`hooks/loop-bash-validator.sh:375`, `hooks/lib/loop-common.sh:393`).
- edge_cases_or_failure_modes: If the template is missing or empty, `summary_bash_blocked_message` has a fallback with the same `CORRECT_PATH` variable (`hooks/lib/loop-common.sh:389`). The template itself does not validate paths; correctness depends on caller-provided active-loop/current-round state.
- validation_or_tests: Referenced by `tests/test-template-references.sh` as a required template (`tests/test-template-references.sh:156`) and indirectly by Bash-validator pattern tests for summary write blocking (`tests/test-bash-validator-patterns.sh:192`). Template loading was partially exercised by the attempted template-loader test, but rendering was sandbox-blocked as noted above.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-126 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for production plan path and content validation in `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: Creates a temporary git repo with one commit (`tests/robustness/test-path-validation-robustness.sh:16`), defines a helper that runs `setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR` pointing at the temp repo (`tests/robustness/test-path-validation-robustness.sh:55`), and classifies failures by production validation messages before the later `codex` availability gate (`tests/robustness/test-path-validation-robustness.sh:70`).
- inputs_outputs_state: Inputs are generated plan files, path strings, git repo state, and production script output. Outputs are pass/fail counters and exit status through `print_test_summary` (`tests/robustness/test-path-validation-robustness.sh:465`).
- gates_or_invariants: Positive cases accept normal relative paths, root filenames, dash/underscore, nested paths, dotted filenames, long filenames, and deep nesting (`tests/robustness/test-path-validation-robustness.sh:101`, `tests/robustness/test-path-validation-robustness.sh:381`). Negative cases require rejection of absolute paths, spaces, shell metacharacters, glob-like characters, backslashes, tilde, brackets/braces, hash/exclamation, file symlinks, parent directory symlinks, symlink chains, empty/comment-only/short/missing files, and directory-as-file (`tests/robustness/test-path-validation-robustness.sh:158`, `tests/robustness/test-path-validation-robustness.sh:321`, `tests/robustness/test-path-validation-robustness.sh:404`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp dir and summary helpers (`tests/test-helpers.sh:86`), `git`, and `scripts/setup-rlcr-loop.sh`. The production gates under test are path relativity/metacharacter checks (`scripts/setup-rlcr-loop.sh:258`, `scripts/setup-rlcr-loop.sh:272`), symlink traversal checks (`scripts/setup-rlcr-loop.sh:284`, `scripts/setup-rlcr-loop.sh:297`), in-project resolution (`scripts/setup-rlcr-loop.sh:332`), tracking policy (`scripts/setup-rlcr-loop.sh:385`), and content-line checks (`scripts/setup-rlcr-loop.sh:416`, `scripts/setup-rlcr-loop.sh:426`).
- edge_cases_or_failure_modes: The test intentionally treats “requires codex” and “must be gitignored” as signs that path/content validation already passed (`tests/robustness/test-path-validation-robustness.sh:76`, `tests/robustness/test-path-validation-robustness.sh:81`). Unicode/CJK/emoji paths are explicitly not rejected by this robustness suite (`tests/robustness/test-path-validation-robustness.sh:393`).
- validation_or_tests: Attempted `bash tests/robustness/test-path-validation-robustness.sh`, but the sandbox rejected `mktemp` in the system temp directory before any test logic ran. No repository files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_GEN_PLAN_COMMAND-HZ-006; IMPROVE_GEN_PLAN_COMMAND-HZ-036; IMPROVE_GEN_PLAN_COMMAND-HZ-066; IMPROVE_GEN_PLAN_COMMAND-HZ-096; IMPROVE_GEN_PLAN_COMMAND-HZ-126
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`