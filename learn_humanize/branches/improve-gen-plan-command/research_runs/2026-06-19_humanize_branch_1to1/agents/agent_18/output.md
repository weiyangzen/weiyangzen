# agent_18 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-018 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling an active RLCR loop. The command itself is a thin orchestration layer: its frontmatter restricts tools to the cancel script in normal or forced mode plus `AskUserQuestion`, and hides the command from slash-command discovery tooling (`commands/cancel-rlcr-loop.md:1-5`). It delegates state mutation to `scripts/cancel-rlcr-loop.sh` and tells the agent how to interpret the script’s protocol output (`commands/cancel-rlcr-loop.md:11-22`).
- algorithmic_behavior: The command runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` first, then branches on the first output line. `NO_LOOP` and `NO_ACTIVE_LOOP` produce a no-active-loop response; `CANCELLED` and `CANCELLED_FINALIZE` are terminal success states whose script output should be reported; `FINALIZE_NEEDS_CONFIRM` enters an explicit user-confirmation path (`commands/cancel-rlcr-loop.md:17-22`). In Finalize Phase, it asks the user whether to force cancel or allow normal finalize completion, then either runs the script with `--force` or reports that Finalize Phase will continue (`commands/cancel-rlcr-loop.md:23-34`).
- inputs_outputs_state: Input is the current RLCR loop state under the newest `.humanize/rlcr/<timestamp>/` directory and, conditionally, the user’s confirmation choice. Output is user-facing status text derived from the cancel script. The actual state transition is implemented by the script: it finds `CLAUDE_PROJECT_DIR` or `pwd`, locates the newest loop directory, creates `.cancel-requested`, and renames `state.md` or `finalize-state.md` to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69-99`, `scripts/cancel-rlcr-loop.sh:134-151`). The command documents the same active-loop invariant: an active loop is present when `state.md` or `finalize-state.md` exists in the newest loop directory (`commands/cancel-rlcr-loop.md:36-38`).
- gates_or_invariants: The main gate is Finalize Phase confirmation. Without confirmation, the script returns `FINALIZE_NEEDS_CONFIRM` and exit code 2 (`scripts/cancel-rlcr-loop.sh:117-127`), and the command must ask before using `--force` (`commands/cancel-rlcr-loop.md:23-31`). Another invariant is that cancellation logic lives in the script, not in the prompt (`commands/cancel-rlcr-loop.md:36`). Hook-side protections add a second invariant: state-file moves are normally blocked, but an authorized cancel move is allowed only when the active loop contains `.cancel-requested` and the command is exactly an `mv` from `state.md` or `finalize-state.md` to `cancel-state.md` (`hooks/lib/loop-common.sh:437-455`, `hooks/lib/loop-common.sh:585-613`; `hooks/loop-bash-validator.sh:139-155`).
- dependencies_and_callers: Depends directly on `scripts/cancel-rlcr-loop.sh`, `${CLAUDE_PLUGIN_ROOT}`, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:3`, `commands/cancel-rlcr-loop.md:14`, `commands/cancel-rlcr-loop.md:24`). The script depends on `.humanize/rlcr` directory naming, `state.md`, `finalize-state.md`, and `.cancel-requested` (`scripts/cancel-rlcr-loop.sh:69-87`). The bash validator and shared helper are relevant runtime guards because cancellation writes protected loop state (`hooks/loop-bash-validator.sh:121-155`; `hooks/lib/loop-common.sh:437-613`).
- edge_cases_or_failure_modes: If there is no newest loop directory, the script emits `NO_LOOP` and exits 1 (`scripts/cancel-rlcr-loop.sh:72-79`). If the directory exists but neither active state file exists, it emits `NO_ACTIVE_LOOP` and exits 1 (`scripts/cancel-rlcr-loop.sh:89-99`). In Finalize Phase without `--force`, it refuses direct cancellation and asks for confirmation (`scripts/cancel-rlcr-loop.sh:117-127`). The authorization helper rejects missing signal files, command substitution, newlines, shell chaining, extra arguments, wrong source/destination paths, mixed quote styles, and symlinked source state files (`hooks/lib/loop-common.sh:461-613`).
- validation_or_tests: `tests/test-cancel-signal-file.sh` validates the cancellation safety envelope: allowed `mv` with a signal file, blocked `mv` without a signal file, blocked redirection/sed/rm/arbitrary destinations, wrong-directory signals, injection with `&&`, semicolons, `$()`, backticks, newlines, and quoted path handling (`tests/test-cancel-signal-file.sh:1-10`, `tests/test-cancel-signal-file.sh:78-153`, `tests/test-cancel-signal-file.sh:199-255`, `tests/test-cancel-signal-file.sh:281-378`). It is included in the aggregate suite (`tests/run-all-tests.sh:32-47`). I did not run tests because this worker was instructed to produce read-only research notes only.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-048 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `command_modifies_file`, the core bash-command pattern matcher used by `hooks/loop-bash-validator.sh` to prevent shell writes to RLCR/PR loop managed files. The test sources `hooks/lib/loop-common.sh` directly and asserts expected matcher behavior (`tests/test-bash-validator-patterns.sh:1-13`).
- algorithmic_behavior: The test normalizes each sample command with `to_lower`, then calls `command_modifies_file "$command_lower" "$pattern"` through two assertion helpers: `assert_modifies` expects a match and `assert_not_modifies` expects no match (`tests/test-bash-validator-patterns.sh:36-62`). The tested implementation checks an ordered array of regex patterns for redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and file-descriptor `exec >` (`hooks/lib/loop-common.sh:972-1000`).
- inputs_outputs_state: Inputs are shell command strings plus a target filename regex, defaulting to `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:37-55`). Output is the test process exit code: 0 if all assertions pass, 1 if any assertion fails (`tests/test-bash-validator-patterns.sh:196-214`). The test tracks `TESTS_PASSED` and `TESTS_FAILED` counters as its local state (`tests/test-bash-validator-patterns.sh:20-33`).
- gates_or_invariants: The invariant under test is conservative write detection without blocking read-only access. It must detect common mutation forms such as `>`, `>>`, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv/cp` to target, `rm`, `dd of=`, `truncate`, and `exec 3>` (`tests/test-bash-validator-patterns.sh:69-134`). It must not detect read-only commands like `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, or `diff` (`tests/test-bash-validator-patterns.sh:136-153`). Case-insensitivity is achieved by lowercasing before matching (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:54-57`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `to_lower` and `command_modifies_file` (`tests/test-bash-validator-patterns.sh:11-13`). The matcher is called by `hooks/loop-bash-validator.sh` for protected RLCR files: `finalize-state.md`, `state.md`, `.humanize/rlcr/.../plan.md`, `goal-tracker.md`, `round-*-prompt.md`, `round-*-summary.md`, and `round-*-todos.md` (`hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:330-393`). It is also used for PR-loop protected state and generated prompt/comment/check files (`hooks/loop-bash-validator.sh:403-442`). The suite runner includes this test as an early validation asset (`tests/run-all-tests.sh:32-36`).
- edge_cases_or_failure_modes: The test documents a known limitation: multi-source copy such as `cp file1.md file2.md goal-tracker.md` is not detected because the pattern expects `cp src dest` format (`tests/test-bash-validator-patterns.sh:165-168`). Variable-only redirects like `echo x > $FILE` are intentionally not detected because no literal target pattern is present (`tests/test-bash-validator-patterns.sh:170-172`). It also covers no-space redirects (`>goal-tracker.md`) and bare redirection (`> goal-tracker.md`) (`tests/test-bash-validator-patterns.sh:161-164`).
- validation_or_tests: This file is itself the validation. It covers nine groups: redirection, `tee`, in-place editors, file operations, other modifiers, false positives, edge cases, `state.md`, and `round-N-summary.md` patterns (`tests/test-bash-validator-patterns.sh:69-194`). It exits nonzero if any expected match or non-match fails (`tests/test-bash-validator-patterns.sh:206-214`). I did not execute it because the assignment requested research notes only in a read-only export.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-078 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Block-message subtemplate used when the RLCR stop hook detects untracked `.humanize*` local loop artifacts while the git worktree is not clean. It specializes the generic git-not-clean gate by explaining that `.humanize/` is local loop state and should not be committed (`prompt-template/block/git-not-clean-humanize-local.md:2-8`).
- algorithmic_behavior: The template is loaded as `SPECIAL_NOTES` during the stop hook’s quick git-clean check. If cached git status contains untracked files and those include `.humanize`, `loop-codex-stop-hook.sh` loads this template and appends it to the git-not-clean block reason (`hooks/loop-codex-stop-hook.sh:432-451`). The hook then renders `block/git-not-clean.md` with `GIT_ISSUES=uncommitted changes` and the accumulated `SPECIAL_NOTES`, returns a JSON block decision, and prevents loop exit/review continuation until the worktree is cleaned or committed appropriately (`hooks/loop-codex-stop-hook.sh:464-485`).
- inputs_outputs_state: Input is the hook’s cached `git status --porcelain` output, specifically `??` untracked entries containing `.humanize` (`hooks/loop-codex-stop-hook.sh:437-446`). Output is prompt text embedded into the git-not-clean block reason. The state transition it asks for is a repository hygiene update: append `.humanize*` to `.gitignore` and stage `.gitignore`, not `.humanize` itself (`prompt-template/block/git-not-clean-humanize-local.md:3-8`).
- gates_or_invariants: The invariant is that `.humanize/` is generated by `humanize:start-rlcr-loop` and is local state, not commit content (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). It supports the broader `.humanize` protection model: `git_adds_humanize` blocks direct or broad `git add` forms that would stage `.humanize`, especially force-add or `git add -A` cases when `.humanize` exists (`hooks/lib/loop-common.sh:812-930`). The separate `git-add-humanize` block template instructs adding `.humanize*` to `.gitignore` via targeted `.gitignore` staging, matching this template’s requested action (`prompt-template/block/git-add-humanize.md:26-34`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` (`hooks/loop-codex-stop-hook.sh:444-450`). It is then injected into `prompt-template/block/git-not-clean.md` through `{{SPECIAL_NOTES}}` (`prompt-template/block/git-not-clean.md:1-5`; `hooks/loop-codex-stop-hook.sh:473-475`). Template loading is provided by `hooks/lib/template-loader.sh`, whose rendering model keeps missing placeholders and performs single-pass substitution (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: If the template is missing or empty, the caller falls back to a shorter note: `.humanize* directories are intentionally untracked` (`hooks/loop-codex-stop-hook.sh:446-449`). The caller separately identifies other untracked files and may add another note from `block/git-not-clean-untracked.md`, so `.humanize` is only one branch of the git-clean failure (`hooks/loop-codex-stop-hook.sh:453-460`). The template’s suggested `echo '.humanize*' >> .gitignore` is append-only and assumes `.gitignore` exists or can be created by shell redirection (`prompt-template/block/git-not-clean-humanize-local.md:5-8`).
- validation_or_tests: Covered indirectly by template-reference/comprehensive template tests and by git-operation/plan-file tests that create `.humanize*` ignore entries; direct usage is visible in `loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:444-451`). The template loader behavior that preserves unresolved placeholders and avoids recursive substitution is tested through the template loader suite, and the aggregate suite includes template tests (`tests/run-all-tests.sh:32-41`). I did not run tests because this task is research-only.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-108 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude next-round prompt template for the RLCR loop after Codex finds issues. It is the transition contract from Codex review output to the next Claude implementation round: read the original plan, create comprehensive todos, consume Codex review content, read the goal tracker, and avoid direct goal-tracker writes after Round 0 (`prompt-template/claude/next-round-prompt.md:1-31`).
- algorithmic_behavior: When Codex review does not end with terminal `COMPLETE` or `STOP`, `loop-codex-stop-hook.sh` increments `current_round`, writes `round-${NEXT_ROUND}-prompt.md`, and renders this template with `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` (`hooks/loop-codex-stop-hook.sh:957-965`, `hooks/loop-codex-stop-hook.sh:1058-1083`). The rendered prompt is then extended with post-alignment instructions when applicable, a footer, optional push instructions, and a goal-tracker-update request template before being returned as a blocking JSON decision for the next Claude round (`hooks/loop-codex-stop-hook.sh:1085-1127`).
- inputs_outputs_state: Inputs are the original implementation plan path (`{{PLAN_FILE}}`), Codex review text (`{{REVIEW_CONTENT}}`), and current loop goal tracker path (`{{GOAL_TRACKER_FILE}}`) (`prompt-template/claude/next-round-prompt.md:3-19`, `prompt-template/claude/next-round-prompt.md:22-31`). Output is a prompt file under `.humanize/rlcr/<timestamp>/round-N-prompt.md` plus a JSON block response whose reason is the full prompt (`hooks/loop-codex-stop-hook.sh:1062-1083`, `hooks/loop-codex-stop-hook.sh:1118-1126`). The state transition is `current_round` advancing to `NEXT_ROUND` in `state.md` (`hooks/loop-codex-stop-hook.sh:1062-1065`).
- gates_or_invariants: The template enforces that Claude must review the original plan before continuing and align work with it (`prompt-template/claude/next-round-prompt.md:3-8`). It requires todos for all discovered issues and explicitly prohibits addressing only the most important issues (`prompt-template/claude/next-round-prompt.md:12-14`). It marks the goal tracker read-only after Round 0 and requires update requests in the summary instead of direct edits (`prompt-template/claude/next-round-prompt.md:22-31`). Bash-side enforcement backs that prompt rule: attempts to modify `goal-tracker.md` via Bash are blocked, with stricter post-Round-0 messaging (`hooks/loop-bash-validator.sh:343-358`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-prompt.md"` in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:1080-1083`). It depends on the safe template loader’s single-pass behavior, which is important because `REVIEW_CONTENT` may itself contain `{{...}}`; those placeholders must not be recursively substituted or corrupt the review text (`hooks/lib/template-loader.sh:54-58`, `hooks/lib/template-loader.sh:71-129`). It coordinates with `claude/next-round-footer.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md`, appended after this base prompt (`hooks/loop-codex-stop-hook.sh:1093-1113`).
- edge_cases_or_failure_modes: If this template is missing, the hook falls back to a shorter prompt containing review content and plan/goal references (`hooks/loop-codex-stop-hook.sh:1071-1083`). If Codex’s last non-empty line is exactly `COMPLETE`, this template is bypassed and the loop enters Finalize Phase; if it is exactly `STOP`, the loop terminates through the circuit-breaker path (`hooks/loop-codex-stop-hook.sh:960-1056`). The prompt text shown here ends after the goal-tracker update-request instruction lead-in; the actual generated next-round prompt relies on appended templates to provide summary/commit/update-request details (`prompt-template/claude/next-round-prompt.md:30-31`; `hooks/loop-codex-stop-hook.sh:1093-1113`).
- validation_or_tests: Template rendering is covered by template loader and comprehensive template tests in the aggregate test suite (`tests/run-all-tests.sh:32-41`). Search results show tests exercising `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` substitution, including protection against recursive substitution in review content (`tests/test-template-loader.sh` references from repository search). I did not run tests because this worker assignment requested direct inspection and research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 Item Evidence headings present; item IDs intentionally not repeated here to preserve the exact-once evidence rule`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`