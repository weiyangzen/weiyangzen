# agent_17 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-017 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling PR review loops. It is a thin command surface over `scripts/cancel-pr-loop.sh`, with tool allowlisting in `commands/cancel-pr-loop.md:1-4`, a required script invocation in `commands/cancel-pr-loop.md:11-15`, status interpretation rules in `commands/cancel-pr-loop.md:17-20`, and a PR-vs-RLCR boundary note in `commands/cancel-pr-loop.md:25`.
- algorithmic_behavior: The markdown command does not implement cancellation itself; it delegates all state mutation to `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"`. The script finds the newest `.humanize/pr-loop/*/` directory, requires `state.md` to identify an active loop, creates `.cancel-requested`, and renames `state.md` to `cancel-state.md` (`scripts/cancel-pr-loop.sh:72-98`, `scripts/cancel-pr-loop.sh:118-123`). The command then maps first-line script statuses to user-facing responses: `NO_LOOP` or `NO_ACTIVE_LOOP` means no active PR loop, while `CANCELLED` means report the script’s cancellation message (`commands/cancel-pr-loop.md:17-20`).
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT` for the executable script path and, inside the script, `CLAUDE_PROJECT_DIR` or `pwd` for the project root (`scripts/cancel-pr-loop.sh:72-73`). State input is the newest PR loop directory under `.humanize/pr-loop/`, with active state defined by `state.md` (`commands/cancel-pr-loop.md:21`). Outputs are script status lines and preserved loop artifacts. Successful state transition is `state.md` moved to `cancel-state.md` plus a `.cancel-requested` signal file; comments, resolution summaries, and state information remain in place (`commands/cancel-pr-loop.md:21-23`, `scripts/cancel-pr-loop.sh:118-130`).
- gates_or_invariants: The command’s `allowed-tools` frontmatter restricts execution to the cancel script with or without `--force` (`commands/cancel-pr-loop.md:3`). It explicitly affects only PR loops and must not cancel RLCR loops (`commands/cancel-pr-loop.md:25`). Active PR loop detection is based on `state.md` in the newest PR loop directory (`commands/cancel-pr-loop.md:21`). The script exits `1` for no loop or no active state, `0` for successful cancellation, and `3` for unknown options (`scripts/cancel-pr-loop.sh:11-15`, `scripts/cancel-pr-loop.sh:60-64`, `scripts/cancel-pr-loop.sh:78-98`).
- dependencies_and_callers: Depends directly on `scripts/cancel-pr-loop.sh`. PR loop documentation names this command as one valid stopping path (`commands/start-pr-loop.md:58-62`). Related setup and hook text also refer users to `/humanize:cancel-pr-loop`, while RLCR uses a separate command and script (`commands/cancel-rlcr-loop.md:1-38`), preserving the loop-family boundary.
- edge_cases_or_failure_modes: No PR loop directory yields `NO_LOOP`; a newest directory without `state.md` yields `NO_ACTIVE_LOOP` (`scripts/cancel-pr-loop.sh:76-98`). Missing `current_round`, `max_iterations`, or `pr_number` fields degrade to `?` in the cancellation message rather than blocking (`scripts/cancel-pr-loop.sh:104-112`). `--force` is allowed but currently has no extra effect (`scripts/cancel-pr-loop.sh:39-41`). The newest-directory selection is lexical `sort -r`, so timestamp naming conventions matter (`scripts/cancel-pr-loop.sh:75-76`). Any filesystem failure during `touch` or `mv` exits due to `set -euo pipefail` (`scripts/cancel-pr-loop.sh:18`, `scripts/cancel-pr-loop.sh:118-123`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help output, no-loop handling, and active-loop cancellation with `state.md` renamed to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:181-244`). The command markdown itself is not executable, so validation is through the delegated script and command-reference consistency.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-047 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the aggregate test runner’s ANSI-stripping and pass/fail-count parsing algorithm. It mirrors the parsing logic used by `tests/run-all-tests.sh` to make colored test-suite summaries machine-countable on both GNU/Linux and BSD/macOS sed.
- algorithmic_behavior: The test initializes counters and helper functions (`tests/test-ansi-parsing.sh:18-31`), builds colored and plain sample outputs, strips ANSI SGR color codes with `esc=$'\033'` and `sed "s/${esc}\\[[0-9;]*m//g"` (`tests/test-ansi-parsing.sh:45-48`), then extracts numeric `Passed:` and `Failed:` values using `grep -oE` and trailing-number matching (`tests/test-ansi-parsing.sh:74-90`). For multi-line simulated suite output, it uses `tail -1` to select the last summary count (`tests/test-ansi-parsing.sh:116-129`).
- inputs_outputs_state: Inputs are synthetic shell strings containing ANSI color sequences, bold sequences, plain text, and multi-line suite-like summaries (`tests/test-ansi-parsing.sh:45-48`, `tests/test-ansi-parsing.sh:60-62`, `tests/test-ansi-parsing.sh:116-125`, `tests/test-ansi-parsing.sh:151-153`). Outputs are colored PASS/FAIL diagnostic lines plus a final `Passed:`/`Failed:` summary (`tests/test-ansi-parsing.sh:21-31`, `tests/test-ansi-parsing.sh:163-168`). State is local to the test process via `TESTS_PASSED` and `TESTS_FAILED`, with exit `0` only when failures are zero (`tests/test-ansi-parsing.sh:170-178`).
- gates_or_invariants: The portability invariant is explicit: use ANSI-C quoting `$'\033'` rather than non-portable sed escape forms (`tests/test-ansi-parsing.sh:38-40`). The stripping algorithm only removes SGR-style escape sequences matching ESC, `[`, digits/semicolons, then `m`; all count parsing assumes labels of the form `Passed:` or `Failed:` followed by whitespace and decimal digits. The final process gate is `TESTS_FAILED -eq 0` (`tests/test-ansi-parsing.sh:170-178`).
- dependencies_and_callers: `tests/run-all-tests.sh` includes this suite in its ordered `TEST_SUITES` list (`tests/run-all-tests.sh:32-44`) and uses the same stripping and extraction algorithm for every test-suite output (`tests/run-all-tests.sh:113-117`). Runtime dependencies are `bash`, `sed`, `grep -E`, and `tail`; there are no file writes.
- edge_cases_or_failure_modes: Covered edge cases include multiple ANSI spans in one line (`tests/test-ansi-parsing.sh:55-67`), colored pass count extraction (`tests/test-ansi-parsing.sh:69-81`), colored fail count extraction (`tests/test-ansi-parsing.sh:83-95`), zero failed count (`tests/test-ansi-parsing.sh:97-109`), multi-line suite summaries (`tests/test-ansi-parsing.sh:111-130`), plain text without ANSI codes (`tests/test-ansi-parsing.sh:132-144`), and bold-plus-color combinations (`tests/test-ansi-parsing.sh:146-158`). Not covered: non-SGR ANSI control sequences, lowercase labels, localized labels, or non-decimal count formats.
- validation_or_tests: Ran `bash tests/test-ansi-parsing.sh` in the read-only branch export. Result: 8 passed, 0 failed, exit 0. This directly validates the assigned executable spec. `tests/run-all-tests.sh` also consumes the suite as part of the full test set (`tests/run-all-tests.sh:43`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-077 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the `.humanize/` Git staging protection gate. It defines the review/repair contract shown when the Bash validator detects a `git add` command that could stage local loop state.
- algorithmic_behavior: The template itself is static text, but it is rendered by `git_add_humanize_blocked_message` through `load_and_render_safe` (`hooks/lib/loop-common.sh:932-970`). The actual classifier is `git_adds_humanize`, which splits chained shell commands, finds `git ... add`, normalizes quoted add arguments, blocks direct `.humanize` path references, detects force/all flags, and conditionally blocks broad-scope adds (`hooks/lib/loop-common.sh:812-930`). `hooks/loop-bash-validator.sh` invokes that classifier and exits with a blocking result when it matches (`hooks/loop-bash-validator.sh:109-118`).
- inputs_outputs_state: Input is a Bash tool command string, lowercased before classification (`hooks/loop-bash-validator.sh:47-49`). Additional filesystem input is whether an active RLCR or PR loop exists; if no active loop exists, the Bash validator allows all commands before reaching this gate (`hooks/loop-bash-validator.sh:54-67`). The template output is a stderr block message naming safe alternatives and blocked examples (`prompt-template/block/git-add-humanize.md:8-25`). The protected state is the Git index: this gate prevents `.humanize/` loop-local files from being staged.
- gates_or_invariants: The template declares `.humanize/` local loop state must not be committed and notes it is expected to be ignored (`prompt-template/block/git-add-humanize.md:3-6`). Allowed paths are specific files/directories or patch mode (`prompt-template/block/git-add-humanize.md:8-15`). Blocked examples include direct `.humanize`, `git add -A`, `git add --all`, broad `git add .`, and force-broad `git add -f .` (`prompt-template/block/git-add-humanize.md:16-25`). The classifier has a narrower invariant for broad non-force adds: `git add .` or `git add *` is blocked only when `.humanize` exists and is not ignored (`hooks/lib/loop-common.sh:920-925`), while direct `.humanize` references and force/all combinations are stricter (`hooks/lib/loop-common.sh:871-906`, `hooks/lib/loop-common.sh:914-918`).
- dependencies_and_callers: Depends on the shared template loader via `load_and_render_safe` (`hooks/lib/loop-common.sh:969`) and the Bash pre-tool validator (`hooks/loop-bash-validator.sh:1-18`, `hooks/loop-bash-validator.sh:109-118`). The fallback embedded in `hooks/lib/loop-common.sh` mirrors this template closely, so missing-template behavior still blocks with usable guidance (`hooks/lib/loop-common.sh:935-967`).
- edge_cases_or_failure_modes: The classifier strips simple single and double quotes for `.humanize` path matching (`hooks/lib/loop-common.sh:866-875`) and avoids overblocking `.humanizeconfig`, `.humanize-backup`, or `src/.humanizerc` by requiring `.humanize` as a path segment (`hooks/lib/loop-common.sh:871-876`; allowed examples in `tests/test-humanize-escape.sh:189-193`). It handles command chaining with `;`, `&&`, `||`, and pipe splitting (`hooks/lib/loop-common.sh:837-845`). Potential mismatch: the template’s blocked-command section is intentionally conservative, while the implementation allows broad non-force adds when `.humanize` is already ignored. The commit-message warning is also conservative because the protection may inspect literal `.humanize` in shell commands (`prompt-template/block/git-add-humanize.md:26-34`).
- validation_or_tests: `tests/test-humanize-escape.sh` covers allowed specific paths, patch mode, status/diff/log references, and `.humanize*` lookalikes that should not block (`tests/test-humanize-escape.sh:169-193`), with earlier groups covering chained and `git -C` path variants (`tests/test-humanize-escape.sh:150-166`). Template-loading behavior is covered by `load_and_render_safe` tests for existing-template and fallback paths (`tests/test-template-loader.sh:197-222`, `tests/test-templates-comprehensive.sh:440-463`), and critical validator template rendering is checked for safe loader usage (`tests/test-template-references.sh:172-200`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-107 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Claude/RLCR next-round prompt footer that defines the post-review continuation contract. It is appended when a review found issues and the loop advances to another round, telling the next actor how to proceed and where to write the next summary.
- algorithmic_behavior: The stop hook updates `current_round`, creates `round-N-prompt.md` and `round-N-summary.md`, renders the main next-round prompt, optionally appends post-alignment action items, then appends this footer with `NEXT_SUMMARY_FILE` substituted (`hooks/loop-codex-stop-hook.sh:1062-1097`). The footer requires the agent not to bypass the RLCR loop by lying, editing loop state, or running cancellation (`prompt-template/claude/next-round-footer.md:4`), optionally invokes code simplification when available (`prompt-template/claude/next-round-footer.md:6-7`), and requires commit plus summary output (`prompt-template/claude/next-round-footer.md:8-9`).
- inputs_outputs_state: Input variable is `NEXT_SUMMARY_FILE`, passed as `NEXT_SUMMARY_FILE=$NEXT_SUMMARY_FILE` into `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1093-1097`). Output is appended markdown in the next round prompt file. Related state transition happens just before rendering: `state.md` is rewritten to the next round number and the next prompt/summary paths are computed (`hooks/loop-codex-stop-hook.sh:1062-1069`).
- gates_or_invariants: The key invariant is loop integrity: the agent must not fabricate completion, mutate loop state, or self-cancel to escape feedback (`prompt-template/claude/next-round-footer.md:4`). The summary destination is not optional; it is explicitly bound to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:9`). The footer is always appended before optional push-every-round and goal-tracker follow-up text, so commit/summary obligations are present regardless of push policy (`hooks/loop-codex-stop-hook.sh:1093-1105`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:1093-1097`). It depends on `hooks/lib/template-loader.sh` behavior indirectly through shared rendering. It coordinates with `prompt-template/claude/next-round-prompt.md`, optional `prompt-template/claude/post-alignment-action-items.md`, optional `prompt-template/claude/push-every-round-note.md`, and later goal-tracker update prompt content (`hooks/loop-codex-stop-hook.sh:1071-1110`).
- edge_cases_or_failure_modes: If `NEXT_SUMMARY_FILE` is not substituted, the prompt would retain a raw placeholder and weaken summary routing. If `code-simplifier` is unavailable, the footer’s instruction is conditional and does not block progress (`prompt-template/claude/next-round-footer.md:6-7`). The cancel prohibition is appropriate for autonomous loop escape prevention, but human-initiated cancellation still exists through the separate command surface. This file is RLCR-specific and should not be used as PR-loop cancellation guidance.
- validation_or_tests: No direct unit test targets this exact footer text. Indirect coverage exists through template-reference and safe-rendering tests that verify template references and fallback mechanics (`tests/test-template-references.sh:172-200`, `tests/test-template-loader.sh:197-222`). Related finalize prompts are separately checked for simplification language (`tests/test-finalize-phase.sh:465-478`), but that is not direct coverage for this next-round footer.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4; each assigned evidence heading is present once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`