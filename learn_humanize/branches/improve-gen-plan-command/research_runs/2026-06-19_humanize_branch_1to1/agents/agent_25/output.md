# agent_25 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-025 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: RLCR stop-hook state machine. It intercepts Claude exit attempts, finds the active `.humanize/rlcr/<timestamp>` loop, runs pre-review gates, invokes Codex review when appropriate, and decides whether to allow exit, block with next-round instructions, enter Finalize Phase, or terminate with a reason state. The script explicitly ignores `stop_hook_active` because repeated blocked exits are the intended iteration driver (`hooks/loop-codex-stop-hook.sh:29`, `hooks/loop-codex-stop-hook.sh:31`).
- algorithmic_behavior: Active loop discovery uses `PROJECT_ROOT="${CLAUDE_PROJECT_DIR:-$(pwd)}"` and `find_active_loop "$PROJECT_ROOT/.humanize/rlcr"` from `hooks/lib/loop-common.sh`; no active loop exits cleanly (`hooks/loop-codex-stop-hook.sh:43`, `hooks/loop-codex-stop-hook.sh:59`, `hooks/loop-codex-stop-hook.sh:62`). It detects normal versus finalize phase by preferring `finalize-state.md` over `state.md` (`hooks/loop-codex-stop-hook.sh:72`, `hooks/loop-codex-stop-hook.sh:76`). After state parsing, the hook runs cheap gates first: schema, branch, plan integrity, todos, git state, file-size guard, summary file, goal tracker initialization, and max iterations before spending Codex budget (`hooks/loop-codex-stop-hook.sh:129`, `hooks/loop-codex-stop-hook.sh:148`, `hooks/loop-codex-stop-hook.sh:181`, `hooks/loop-codex-stop-hook.sh:255`, `hooks/loop-codex-stop-hook.sh:312`, `hooks/loop-codex-stop-hook.sh:343`, `hooks/loop-codex-stop-hook.sh:426`, `hooks/loop-codex-stop-hook.sh:521`, `hooks/loop-codex-stop-hook.sh:559`, `hooks/loop-codex-stop-hook.sh:635`).
- inputs_outputs_state: Inputs are hook JSON on stdin, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `plan.md` backup, original plan file, `round-N-summary.md` or `finalize-summary.md`, `goal-tracker.md`, git status, Claude transcript path consumed by `check-todos-from-transcript.py`, templates under `prompt-template`, and Codex CLI output. Outputs are Claude-hook JSON blocks via `jq` with `decision: "block"` and a rendered `reason`, stderr status messages, Codex debug files under `XDG_CACHE_HOME/humanize/<sanitized-project>/<timestamp>/`, review prompt/result files in the loop directory, next-round prompt files, and renamed terminal state files such as `finalize-state.md`, `complete-state.md`, `maxiter-state.md`, or `stop-state.md` (`hooks/loop-codex-stop-hook.sh:143`, `hooks/loop-codex-stop-hook.sh:674`, `hooks/loop-codex-stop-hook.sh:787`, `hooks/loop-codex-stop-hook.sh:794`, `hooks/loop-codex-stop-hook.sh:985`, `hooks/loop-codex-stop-hook.sh:1063`, `hooks/lib/loop-common.sh:1023`).
- gates_or_invariants: Strict state parsing requires YAML frontmatter and numeric `current_round`/`max_iterations`; model and effort are regex-limited for YAML/CLI safety (`hooks/loop-codex-stop-hook.sh:88`, `hooks/loop-codex-stop-hook.sh:107`, `hooks/loop-codex-stop-hook.sh:119`, `hooks/lib/loop-common.sh:240`). Newer schema fields `plan_tracked` and `start_branch` are required or the hook blocks as outdated (`hooks/loop-codex-stop-hook.sh:134`). Branch switching is forbidden (`hooks/loop-codex-stop-hook.sh:169`). The plan file must exist and match the loop backup; tracked plans also must have no uncommitted status (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:217`, `hooks/loop-codex-stop-hook.sh:234`). Incomplete todos block before Codex (`hooks/loop-codex-stop-hook.sh:263`, `hooks/loop-codex-stop-hook.sh:287`). Git status failures fail closed, modified work blocks, and `push_every_round` adds an unpushed-commit gate (`hooks/loop-codex-stop-hook.sh:322`, `hooks/loop-codex-stop-hook.sh:465`, `hooks/loop-codex-stop-hook.sh:492`). Round 0 goal tracker placeholders block exit through the assigned template (`hooks/loop-codex-stop-hook.sh:566`, `hooks/loop-codex-stop-hook.sh:614`). Codex markers are accepted only when the last non-empty trimmed line is exactly `COMPLETE` or `STOP`, preventing false positives such as prose containing “cannot complete” (`hooks/loop-codex-stop-hook.sh:960`, `hooks/loop-codex-stop-hook.sh:967`, `hooks/loop-codex-stop-hook.sh:1029`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for marker constants, active-loop discovery, strict state parsing, template loading, lowercase conversion, and `end_loop` (`hooks/lib/loop-common.sh:26`, `hooks/lib/loop-common.sh:152`, `hooks/lib/loop-common.sh:240`, `hooks/lib/loop-common.sh:1023`). Depends on `scripts/portable-timeout.sh` for bounded git/Codex calls (`hooks/loop-codex-stop-hook.sh:50`). The generated state file originates in `scripts/setup-rlcr-loop.sh`, including `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, and `start_branch` (`scripts/setup-rlcr-loop.sh:539`, `scripts/setup-rlcr-loop.sh:551`). It calls `hooks/check-todos-from-transcript.py`, whose exit codes distinguish all todos complete, incomplete todos, and parse errors (`hooks/check-todos-from-transcript.py:7`, `hooks/check-todos-from-transcript.py:113`).
- edge_cases_or_failure_modes: If no loop or no state file exists, exit is allowed (`hooks/loop-codex-stop-hook.sh:61`, `hooks/loop-codex-stop-hook.sh:79`). A strict parse failure currently prints “allowing exit for safety” and exits 0, while later invalid model/effort/current-round cases terminate as unexpected or block; this is a notable fail-open path despite the nearby “fail closed” comment (`hooks/loop-codex-stop-hook.sh:88`, `hooks/loop-codex-stop-hook.sh:90`, `hooks/loop-codex-stop-hook.sh:107`). If `max_iterations` is malformed it falls back to 42 (`hooks/loop-codex-stop-hook.sh:125`). Large file detection only examines changed files from cached git status and only selected code/doc extensions (`hooks/loop-codex-stop-hook.sh:354`, `hooks/loop-codex-stop-hook.sh:378`). `COMPLETE` at or beyond max iterations becomes `maxiter-state.md` instead of finalize (`hooks/loop-codex-stop-hook.sh:967`, `hooks/loop-codex-stop-hook.sh:970`). Codex success without a review file falls back to stdout copy, then blocks if still missing or empty (`hooks/loop-codex-stop-hook.sh:904`, `hooks/loop-codex-stop-hook.sh:920`, `hooks/loop-codex-stop-hook.sh:948`).
- validation_or_tests: Finalize behavior is covered by `tests/test-finalize-phase.sh`, including active `finalize-state.md` detection, final rename to `complete-state.md`, `COMPLETE` entering Finalize Phase, max-iteration bypass to `maxiter-state.md`, incomplete-todo blocking in finalize, and normal non-complete review feedback (`tests/test-finalize-phase.sh:231`, `tests/test-finalize-phase.sh:429`, `tests/test-finalize-phase.sh:465`, `tests/test-finalize-phase.sh:496`, `tests/test-finalize-phase.sh:507`, `tests/test-finalize-phase.sh:580`). Plan/model validation has coverage in `tests/test-plan-file-validation.sh` for unsafe model strings (`tests/test-plan-file-validation.sh:708`, `tests/test-plan-file-validation.sh:721`). Todo parsing is covered by `tests/test-todo-checker.sh`, while monitor graceful-stop behavior is separately covered by the assigned e2e script.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-055 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for monitor robustness, focused on real monitor functions rather than stubs. It validates that RLCR and PR monitors handle loop/session deletion and SIGINT without shell glob errors or terminal-state damage (`tests/test-monitor-e2e-real.sh:3`, `tests/test-monitor-e2e-real.sh:8`).
- algorithmic_behavior: The script creates throwaway projects under `/tmp/test-monitor-e2e-real-$$`, installs a cleanup trap, writes fake `.humanize/rlcr` and `.humanize/pr-loop` sessions plus cache log files, generates small runner scripts that source `scripts/humanize.sh`, then launches monitors in background or foreground depending on the scenario (`tests/test-monitor-e2e-real.sh:48`, `tests/test-monitor-e2e-real.sh:51`, `tests/test-monitor-e2e-real.sh:64`, `tests/test-monitor-e2e-real.sh:93`, `tests/test-monitor-e2e-real.sh:103`). It covers six scenarios: RLCR deletion in bash, RLCR deletion in zsh, RLCR SIGINT in bash, RLCR SIGINT in zsh, PR monitor deletion in `--once` mode, and PR monitor continuous mode stopped by SIGINT (`tests/test-monitor-e2e-real.sh:59`, `tests/test-monitor-e2e-real.sh:228`, `tests/test-monitor-e2e-real.sh:364`, `tests/test-monitor-e2e-real.sh:534`, `tests/test-monitor-e2e-real.sh:662`, `tests/test-monitor-e2e-real.sh:803`).
- inputs_outputs_state: Inputs are synthetic state files, synthetic goal trackers, fake `HOME`/`XDG_CACHE_HOME`, generated runner scripts, shimmed `tput`/`clear`/sometimes `printf`, real `scripts/humanize.sh`, real monitor helpers, directory deletion events, and POSIX signals. Outputs are captured monitor logs, `PASS`/`FAIL` counters, `EXIT_CODE:` sentinels, and final process exit 0 only if no assertions fail (`tests/test-monitor-e2e-real.sh:68`, `tests/test-monitor-e2e-real.sh:78`, `tests/test-monitor-e2e-real.sh:116`, `tests/test-monitor-e2e-real.sh:120`, `tests/test-monitor-e2e-real.sh:145`, `tests/test-monitor-e2e-real.sh:149`, `tests/test-monitor-e2e-real.sh:978`). The fake RLCR state is intentionally simple key-value text, matching the monitor’s tolerant parser rather than the stop hook’s strict YAML-frontmatter contract (`tests/test-monitor-e2e-real.sh:69`, `scripts/humanize.sh:309`).
- gates_or_invariants: Deletion tests require the monitor to exit within a bounded loop, emit `Monitoring stopped:`, mention that the directory no longer exists, avoid `no matches found`/`bad pattern`, display cleanup text, and return `EXIT_CODE:0` (`tests/test-monitor-e2e-real.sh:162`, `tests/test-monitor-e2e-real.sh:167`, `tests/test-monitor-e2e-real.sh:185`, `tests/test-monitor-e2e-real.sh:198`, `tests/test-monitor-e2e-real.sh:207`, `tests/test-monitor-e2e-real.sh:221`). It also statically verifies the RLCR monitor has a scroll-region reset escape in source (`tests/test-monitor-e2e-real.sh:213`, `scripts/humanize.sh:481`). Zsh scenarios are skipped if `zsh` is unavailable (`tests/test-monitor-e2e-real.sh:234`, `tests/test-monitor-e2e-real.sh:540`). SIGINT scenarios accept either cleanup messages or clean exit-code evidence because signal delivery differs by shell/process group (`tests/test-monitor-e2e-real.sh:474`, `tests/test-monitor-e2e-real.sh:515`, `tests/test-monitor-e2e-real.sh:648`, `tests/test-monitor-e2e-real.sh:914`, `tests/test-monitor-e2e-real.sh:950`).
- dependencies_and_callers: Directly depends on `scripts/humanize.sh`, specifically `_humanize_monitor_codex`, `humanize monitor pr`, and `_humanize_monitor_pr` (`tests/test-monitor-e2e-real.sh:142`, `tests/test-monitor-e2e-real.sh:146`, `tests/test-monitor-e2e-real.sh:752`, `tests/test-monitor-e2e-real.sh:769`). The monitored implementation uses `find` rather than globs for session/log discovery, which is the core protection against zsh glob failures (`scripts/humanize.sh:263`, `scripts/humanize.sh:297`, `scripts/lib/monitor-common.sh:49`, `scripts/lib/monitor-common.sh:60`). The RLCR monitor’s graceful deletion path calls `_cleanup`, restores terminal state, and returns 0 (`scripts/humanize.sh:522`, `scripts/humanize.sh:530`, `scripts/humanize.sh:533`, `scripts/humanize.sh:581`, `scripts/humanize.sh:623`, `scripts/humanize.sh:715`). The PR monitor provides `--once`, cleanup traps, and continuous polling/tailing (`scripts/humanize.sh:890`, `scripts/humanize.sh:901`, `scripts/humanize.sh:1133`, `scripts/humanize.sh:1174`, `scripts/humanize.sh:1262`).
- edge_cases_or_failure_modes: Test cleanup kills lingering processes matching the test id and removes the temp base (`tests/test-monitor-e2e-real.sh:51`). If a monitor fails to exit after deletion or SIGINT, the test escalates to `TERM` or `KILL`; some SIGINT tests still count a forced-termination path as pass if the monitor had started, making those checks more tolerant than strict signal-cleanup assertions (`tests/test-monitor-e2e-real.sh:172`, `tests/test-monitor-e2e-real.sh:487`, `tests/test-monitor-e2e-real.sh:496`, `tests/test-monitor-e2e-real.sh:927`, `tests/test-monitor-e2e-real.sh:936`). The PR deletion test runs `humanize monitor pr --once` while deleting the session in a background subshell, then accepts either graceful text or exit 0, so it is primarily a smoke test for clean one-shot behavior and absence of glob errors rather than a strong deletion-state transition assertion (`tests/test-monitor-e2e-real.sh:760`, `tests/test-monitor-e2e-real.sh:764`, `tests/test-monitor-e2e-real.sh:783`).
- validation_or_tests: The file is itself validation. Its final summary asserts clean exit messaging, no glob errors, terminal restoration, bash/zsh support, SIGINT/Ctrl+C handling, and PR monitor behavior with and without `--once` (`tests/test-monitor-e2e-real.sh:978`, `tests/test-monitor-e2e-real.sh:982`). Related narrower monitor runtime tests exist in `tests/test-monitor-runtime.sh` for the same graceful-stop strings and deletion behavior (`tests/test-monitor-runtime.sh:99`, `tests/test-monitor-runtime.sh:202`, `tests/test-monitor-runtime.sh:241`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-085 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Block-template contract for the RLCR Round 0 goal-tracker initialization gate. It tells Claude exactly why exit is blocked and what must be initialized before the first round can finish (`prompt-template/block/goal-tracker-not-initialized.md:1`).
- algorithmic_behavior: The template renders a `reason` body when `hooks/loop-codex-stop-hook.sh` detects placeholder text in the Round 0 goal tracker. It lists missing sections, requires reading the goal tracker, replacing placeholders, defining a concrete ultimate goal, defining 3-7 testable acceptance criteria, populating active tasks, and retrying exit after writing the updated file (`prompt-template/block/goal-tracker-not-initialized.md:3`, `prompt-template/block/goal-tracker-not-initialized.md:8`, `prompt-template/block/goal-tracker-not-initialized.md:18`).
- inputs_outputs_state: Inputs are `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, passed by the stop hook after section-specific placeholder checks (`hooks/loop-codex-stop-hook.sh:599`, `hooks/loop-codex-stop-hook.sh:619`). Output is markdown embedded in the stop-hook JSON block with system message `Loop: Goal Tracker not initialized in Round 0`; it does not mutate state directly (`hooks/loop-codex-stop-hook.sh:623`, `hooks/loop-codex-stop-hook.sh:625`). The intended state transition is external: Claude updates `goal-tracker.md`, then retries exit so the hook can proceed beyond this gate.
- gates_or_invariants: The invariant is that the IMMUTABLE SECTION can only be established in Round 0 and becomes read-only afterward (`prompt-template/block/goal-tracker-not-initialized.md:16`). The hook only invokes this template when not in Finalize Phase, `CURRENT_ROUND` is 0, and `goal-tracker.md` exists (`hooks/loop-codex-stop-hook.sh:563`, `hooks/loop-codex-stop-hook.sh:566`). It extracts Ultimate Goal, Acceptance Criteria, and Active Tasks sections and flags generic placeholder text matching `[To be ...]` within those sections (`hooks/loop-codex-stop-hook.sh:576`, `hooks/loop-codex-stop-hook.sh:584`, `hooks/loop-codex-stop-hook.sh:591`).
- dependencies_and_callers: Called only from `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` (`hooks/loop-codex-stop-hook.sh:619`). The placeholders it detects originate from `scripts/setup-rlcr-loop.sh`, which seeds the goal tracker with `[To be extracted from plan by Claude in Round 0]`, optional `[To be defined by Claude in Round 0 based on the plan]`, and `[To be populated by Claude based on plan]` (`scripts/setup-rlcr-loop.sh:577`, `scripts/setup-rlcr-loop.sh:591`, `scripts/setup-rlcr-loop.sh:598`, `scripts/setup-rlcr-loop.sh:611`, `scripts/setup-rlcr-loop.sh:629`, `scripts/setup-rlcr-loop.sh:633`).
- edge_cases_or_failure_modes: Because the regex checks for `\[To be [a-z]`, placeholders with different capitalization or wording could evade the gate, while literal placeholder references inside the extracted sections could trigger it (`hooks/loop-codex-stop-hook.sh:580`, `hooks/loop-codex-stop-hook.sh:587`, `hooks/loop-codex-stop-hook.sh:595`). If `goal-tracker.md` is missing, this specific gate is skipped because the condition requires the file to exist; other validators may still constrain loop files (`hooks/loop-codex-stop-hook.sh:566`). The template itself is advisory and relies on the stop hook for enforcement.
- validation_or_tests: No direct template-specific test was found in the inspected assigned paths. The enforcement path is in the stop hook lines above, and setup seeding provides the concrete placeholder values that should trigger it on an uninitialized Round 0 tracker.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-115 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt contract for the “existing comments / issues to address” startup path. It tells the worker what must happen before the PR stop hook should poll for another bot review (`prompt-template/pr-loop/critical-requirements-has-comments.md:2`).
- algorithmic_behavior: The template renders a visible critical-requirements block requiring three actions: commit and push changes, comment on the PR to trigger re-review, and write a resolution summary to the round’s resolve path (`prompt-template/pr-loop/critical-requirements-has-comments.md:6`, `prompt-template/pr-loop/critical-requirements-has-comments.md:8`, `prompt-template/pr-loop/critical-requirements-has-comments.md:12`, `prompt-template/pr-loop/critical-requirements-has-comments.md:15`). It ends by stating that the stop hook will poll for bot reviews (`prompt-template/pr-loop/critical-requirements-has-comments.md:23`).
- inputs_outputs_state: Inputs are `{{PR_NUMBER}}`, `{{BOT_MENTION_STRING}}`, and `{{RESOLVE_PATH}}`. The rendered output is appended by `scripts/setup-pr-loop.sh` when `COMMENT_COUNT` is nonzero, after the main round-0 prompt is printed (`scripts/setup-pr-loop.sh:810`, `scripts/setup-pr-loop.sh:835`, `scripts/setup-pr-loop.sh:860`). The template itself does not mutate state, but its requested artifacts map to stop-hook gates: `round-N-pr-resolve.md`, clean committed work, pushed commits, and a fresh trigger comment.
- gates_or_invariants: The stop hook enforces the resolve summary by blocking if `round-${PR_CURRENT_ROUND}-pr-resolve.md` is absent (`hooks/pr-loop-stop-hook.sh:262`, `hooks/pr-loop-stop-hook.sh:264`). It blocks non-humanize uncommitted changes and unpushed commits before polling (`hooks/pr-loop-stop-hook.sh:283`, `hooks/pr-loop-stop-hook.sh:299`, `hooks/pr-loop-stop-hook.sh:350`). Trigger comment logic searches issue comments from the current user for monitored bot mentions and records `last_trigger_at`/comment id; startup cases with existing comments or new commits require a trigger before polling (`hooks/pr-loop-stop-hook.sh:523`, `hooks/pr-loop-stop-hook.sh:530`, `hooks/pr-loop-stop-hook.sh:678`, `hooks/pr-loop-stop-hook.sh:728`). Polling then fetches bot reviews and runs local Codex validation (`hooks/pr-loop-stop-hook.sh:796`, `hooks/pr-loop-stop-hook.sh:1220`).
- dependencies_and_callers: Rendered only by `scripts/setup-pr-loop.sh` in the comments-present branch; the no-comments branch uses a sibling template that explicitly says not to comment because first reviews are automatic (`scripts/setup-pr-loop.sh:812`, `scripts/setup-pr-loop.sh:834`, `scripts/setup-pr-loop.sh:835`). Runtime enforcement lives in `hooks/pr-loop-stop-hook.sh`, which parses PR loop state and active/configured bot YAML lists, handles merged/closed PR terminal states, updates `active_bots`, and renames state to `approve-state.md` when all bots approve (`hooks/pr-loop-stop-hook.sh:87`, `hooks/pr-loop-stop-hook.sh:110`, `hooks/pr-loop-stop-hook.sh:246`, `hooks/pr-loop-stop-hook.sh:508`, `hooks/pr-loop-stop-hook.sh:1355`, `hooks/pr-loop-stop-hook.sh:1364`).
- edge_cases_or_failure_modes: The template’s exact trigger example uses `please review`, while later stop-hook instructions also suggest `please review the latest changes`; enforcement cares about bot mentions and freshness rather than exact body text (`prompt-template/pr-loop/critical-requirements-has-comments.md:13`, `hooks/pr-loop-stop-hook.sh:549`). If the user writes a resolve summary but leaves uncommitted or unpushed changes, the stop hook blocks before polling, so the three template requirements are conjunctive rather than alternatives (`hooks/pr-loop-stop-hook.sh:264`, `hooks/pr-loop-stop-hook.sh:299`, `hooks/pr-loop-stop-hook.sh:350`). The summary content bullets are not deeply parsed by the observed stop-hook gate; existence is enforced, while quality is left to review context and later Codex/bot feedback (`prompt-template/pr-loop/critical-requirements-has-comments.md:18`, `hooks/pr-loop-stop-hook.sh:264`).
- validation_or_tests: PR-loop tests create `round-N-pr-resolve.md` files and exercise stop-hook polling/approval paths; examples include `tests/test-pr-loop-stophook.sh:52`, `tests/test-pr-loop-stophook.sh:171`, and `tests/test-pr-loop-stophook.sh:276`. The assigned monitor e2e also uses synthetic PR state and validates `humanize monitor pr --once` and continuous monitor signal handling, but it does not validate this prompt template’s commit/push/comment contract.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 of 4 item-evidence headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`