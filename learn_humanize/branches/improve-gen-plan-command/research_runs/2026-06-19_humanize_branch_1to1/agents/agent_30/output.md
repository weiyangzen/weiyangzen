# agent_30 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-030 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Stop hook implementation for `/humanize:start-pr-loop`. It is registered under the `Stop` hook chain in `hooks/hooks.json:52-64` and acts as the PR-loop state machine gate: it decides whether the assistant may exit, whether the loop is complete, or whether another review/fix round must begin.
- algorithmic_behavior: Locates the newest active PR loop under `.humanize/pr-loop` via `find_active_pr_loop` and exits pass-through when none exists (`hooks/pr-loop-stop-hook.sh:48-76`). It parses YAML frontmatter from `state.md`, including round, max iterations, PR number, bot lists, startup case, latest commit metadata, and trigger fields (`hooks/pr-loop-stop-hook.sh:87-174`). It resolves fork/base repository context before all GitHub operations (`hooks/pr-loop-stop-hook.sh:196-237`), checks PR terminal state (`hooks/pr-loop-stop-hook.sh:244-255`), requires the current round resolution summary (`hooks/pr-loop-stop-hook.sh:262-276`), verifies a clean and pushed git state (`hooks/pr-loop-stop-hook.sh:283-361`), detects force-push history rewrites and clears stale trigger state (`hooks/pr-loop-stop-hook.sh:370-421`), enforces max rounds (`hooks/pr-loop-stop-hook.sh:430-435`), polls configured bots, deduplicates comments, and runs local Codex to classify bot status (`hooks/pr-loop-stop-hook.sh:795-1344`).
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/pr-loop/<timestamp>/state.md`, `round-N-pr-resolve.md`, local git state, GitHub PR state/comments/reactions via `gh`, bot polling output, and local Codex analysis. Outputs include JSON block decisions with `decision`, `reason`, and `systemMessage`; generated files `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md`; state mutations to `state.md`; terminal renames to `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, or `usage-limit-state.md` (`hooks/pr-loop-stop-hook.sh:246-255`, `hooks/pr-loop-stop-hook.sh:1355-1399`, `hooks/pr-loop-stop-hook.sh:1545-1573`).
- gates_or_invariants: Required state fields include a PR number and numeric `current_round` (`hooks/pr-loop-stop-hook.sh:185-194`). Exit is blocked until the current round has a resolution file, non-`.humanize` git changes are committed, and local commits are pushed (`hooks/pr-loop-stop-hook.sh:262-361`). Old trigger comments are rejected after force-push or new commit metadata refresh (`hooks/pr-loop-stop-hook.sh:396-421`, `hooks/pr-loop-stop-hook.sh:612-664`). Trigger requirement is conditional: round 0 startup cases 1-3 do not require a new trigger unless new commits are detected, startup cases 4-5 and later rounds do (`hooks/pr-loop-stop-hook.sh:677-735`). If Claude is configured and a trigger is required, the hook blocks unless the trigger comment receives an eyes reaction (`hooks/pr-loop-stop-hook.sh:749-779`). Per-bot timeouts are anchored to the trigger timestamp, not poll start (`hooks/pr-loop-stop-hook.sh:858-891`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for bot-name mapping, YAML list rendering, PR loop discovery, mention-string construction, and goal tracker updates (`hooks/lib/loop-common.sh:630-680`, `hooks/lib/loop-common.sh:699-718`, `hooks/lib/loop-common.sh:1055-1072`). Depends on `scripts/portable-timeout.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `gh`, `git`, `jq`, and `codex`. `README.md:147-155` describes the user-visible PR-loop sequence this hook enforces.
- edge_cases_or_failure_modes: Handles fork PR lookup by trying the current repo then parent repo (`hooks/pr-loop-stop-hook.sh:212-237`). Supports macOS Bash 3.2 by avoiding associative arrays and using dynamic map helpers (`hooks/pr-loop-stop-hook.sh:876-882`). Handles paginated PR comments for trigger detection (`hooks/pr-loop-stop-hook.sh:535-543`), Codex +1 approval without comments for round 0 case 1 (`hooks/pr-loop-stop-hook.sh:442-477`, `hooks/pr-loop-stop-hook.sh:974-1007`), missing `gh` user identity as a warning (`hooks/pr-loop-stop-hook.sh:592-596`), no bot comments/timeouts (`hooks/pr-loop-stop-hook.sh:1055-1165`), missing or failing Codex (`hooks/pr-loop-stop-hook.sh:1291-1344`), usage/rate-limit terminal marker (`hooks/pr-loop-stop-hook.sh:1391-1399`), and re-adding previously approved bots if they later report issues (`hooks/pr-loop-stop-hook.sh:1413-1481`).
- validation_or_tests: Direct stop-hook tests cover stale trigger rejection after force-push, startup case 1 no-trigger behavior, approve-state creation, dynamic startup-case update, unpushed commit blocking, history rewrite detection, missing trigger blocking, bot timeout removal, Codex thumbs-up approval, Claude eyes timeout, fork PR support, and mixed approval goal-tracker behavior in `tests/test-pr-loop-stophook.sh:20-1782`. Broader PR-loop tests are wired through `tests/test-pr-loop.sh:18-53`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-060 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared executable test harness for PR-loop tests. It does not implement the production state machine, but it creates deterministic mocked `gh` and `codex` commands and initializes the test environment consumed by the PR-loop script, hook, and stop-hook test modules.
- algorithmic_behavior: Uses a load guard `TEST_PR_LOOP_LIB_LOADED` to avoid redefinition on repeated sourcing (`tests/test-pr-loop-lib.sh:10-13`). Resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources `test-helpers.sh` when needed (`tests/test-pr-loop-lib.sh:14-21`), defines `create_mock_gh` with canned behavior for `gh auth status`, `gh repo view`, `gh pr view`, and `gh api` (`tests/test-pr-loop-lib.sh:27-91`), defines `create_mock_codex` as a successful placeholder CLI (`tests/test-pr-loop-lib.sh:93-104`), wires mocks into `PATH` via `init_pr_loop_test_env` (`tests/test-pr-loop-lib.sh:110-124`), and reports aggregate pass/fail counts with `print_test_summary` (`tests/test-pr-loop-lib.sh:130-147`).
- inputs_outputs_state: Inputs are the caller-provided mock directory, global test variables from `test-helpers.sh` such as `TEST_DIR`, and global counters `TESTS_PASSED` and `TESTS_FAILED`. Outputs are executable mock binaries under `$TEST_DIR/mock_bin`, an updated `PATH`, exported `MOCK_BIN_DIR`, and a summary return code where any failure count returns nonzero.
- gates_or_invariants: The harness assumes tests should run against mocks before real tools by prepending `MOCK_BIN_DIR` to `PATH` (`tests/test-pr-loop-lib.sh:114-121`). It keeps the mock `gh` response surface deliberately small and deterministic: PR state defaults open, comments default empty, current user is `testuser`, and commits use a fixed timestamp (`tests/test-pr-loop-lib.sh:53-84`). The summary gate returns failure when `TESTS_FAILED` is greater than zero (`tests/test-pr-loop-lib.sh:140-146`).
- dependencies_and_callers: Sourced by the main PR-loop test runner at `tests/test-pr-loop.sh:18-20`; `init_pr_loop_test_env` is called before `test-pr-loop-scripts.sh`, `test-pr-loop-hooks.sh`, and `test-pr-loop-stophook.sh` are sourced and run (`tests/test-pr-loop.sh:26-47`). Depends on `tests/test-helpers.sh` for test directory setup and pass/fail helpers.
- edge_cases_or_failure_modes: The shared mock `gh` only handles a baseline subset; many stop-hook tests override it with scenario-specific mocks for pagination, trigger timing, forks, reactions, and state transitions. The mock `codex` emits `Mock codex output` rather than protocol markers such as approval or issues, so marker-sensitive tests must provide their own Codex mock. Because this file is sourced, accidental global variable collisions are possible, mitigated only by the load guard.
- validation_or_tests: This file is itself test infrastructure. Its behavior is exercised indirectly whenever `tests/test-pr-loop.sh` runs the script, hook, and stop-hook suites (`tests/test-pr-loop.sh:40-53`). Search results show direct usage of `init_pr_loop_test_env`, `create_mock_gh`, `create_mock_codex`, and `print_test_summary` from the PR-loop suite.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-090 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Blocking message template for the RLCR plan immutability gate. It defines the user-facing contract when the source plan differs from the loop backup during an active RLCR loop.
- algorithmic_behavior: The template states that `{{PLAN_FILE}}` was modified, declares plan-file modification forbidden during an active loop, instructs the user to cancel, update the plan, and start a new loop, and exposes `{{BACKUP_PATH}}` for recovery (`prompt-template/block/plan-file-modified.md:1-12`).
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH` substitutions. The output is rendered Markdown used as the `reason` field in a JSON block response. It does not mutate state directly.
- gates_or_invariants: Encodes the invariant that the plan is the stable requirement source for an active RLCR loop. The production gate checks tracked-file status and backup-content diff, then renders this template when the plan content no longer matches the backup (`hooks/loop-codex-stop-hook.sh:214-253`).
- dependencies_and_callers: Rendered through `load_and_render_safe`, whose template syntax uses single-pass `{{VAR}}` substitution and leaves missing variables visible (`hooks/lib/template-loader.sh:7-20`, `hooks/lib/template-loader.sh:50-132`). The concrete caller is `hooks/loop-codex-stop-hook.sh:247-249`.
- edge_cases_or_failure_modes: If the template is missing or empty, the caller has an inline fallback with the same gate semantics (`hooks/loop-codex-stop-hook.sh:235-249`). If a variable is omitted during rendering, the placeholder remains in the message rather than failing, per template-loader behavior (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:116-122`).
- validation_or_tests: Plan-file stop-hook integrity tests exercise modified, deleted, missing-backup, tracked-modification, and committed-change cases in `tests/test-plan-file-hooks.sh:466-729`. Template rendering behavior is covered by `tests/test-template-loader.sh` references.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-120 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop prompt block for startup case with no existing review comments. It establishes the initial human/assistant contract that first bot reviews are automatic and the assistant should not manually trigger them.
- algorithmic_behavior: Instructs the assistant to wait for automatic reviews, write the initial resolution summary to `@{{RESOLVE_PATH}}`, avoid modifying `.humanize/pr-loop` state files, and rely on the Stop hook to poll and validate reviews (`prompt-template/pr-loop/round-0-task-no-comments.md:4-30`). This prompt is selected by `scripts/setup-pr-loop.sh` when fetched comments contain the no-comments sentinel (`scripts/setup-pr-loop.sh:641-720`).
- inputs_outputs_state: Inputs are `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH`, supplied in `TEMPLATE_VARS` during setup (`scripts/setup-pr-loop.sh:651-658`). Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the round header and fetched comments (`scripts/setup-pr-loop.sh:677-684`, `scripts/setup-pr-loop.sh:720`).
- gates_or_invariants: The prompt supports the Stop hook’s round-0 startup-case-1 invariant: no trigger comment is required while waiting for initial automatic reviews (`hooks/pr-loop-stop-hook.sh:677-708`). It also primes the required resolution-file gate by telling the assistant to create `round-0-pr-resolve.md`; the Stop hook blocks exit until that file exists (`hooks/pr-loop-stop-hook.sh:262-276`). The no-manual-trigger instruction prevents creating unnecessary or stale trigger timestamps before automatic review polling.
- dependencies_and_callers: Rendered by `scripts/setup-pr-loop.sh` through `load_and_render_safe` with an inline fallback that mirrors this file (`scripts/setup-pr-loop.sh:686-720`). It coordinates with the PR-loop state fields initialized earlier in setup, including `startup_case`, `active_bots`, `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id` (`scripts/setup-pr-loop.sh:535-555`).
- edge_cases_or_failure_modes: If comments arrive after setup, the Stop hook can dynamically re-evaluate and persist a new startup case (`hooks/pr-loop-stop-hook.sh:1531-1543`). If Codex approves by thumbs-up instead of a comment, the Stop hook removes Codex from active bots or completes the loop (`hooks/pr-loop-stop-hook.sh:442-477`, `hooks/pr-loop-stop-hook.sh:974-1007`). If the template is missing, setup uses its fallback copy (`scripts/setup-pr-loop.sh:689-720`).
- validation_or_tests: Startup case 1 no-trigger behavior and Codex thumbs-up approval are tested in `tests/test-pr-loop-stophook.sh:140-244` and `tests/test-pr-loop-stophook.sh:896-1005`. PR-loop system tests also cover startup-case behavior and monitor display paths referenced by this initial prompt flow.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique assigned item headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`