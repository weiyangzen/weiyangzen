# agent_05 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Prompt contract library for both RLCR and PR-loop control flow. It defines the blocking messages, Claude task prompts, Codex review prompts, and PR-loop round prompts consumed by hooks and setup scripts through `hooks/lib/template-loader.sh`.
- algorithmic_behavior: The directory is split into `block/`, `claude/`, `codex/`, and `pr-loop/`. `block/` templates are gate outputs for protected state, prompt, summary, goal tracker, git, PR-loop, force-push, trigger, and bot timeout conditions. `claude/` templates drive next-round and finalize behavior. `codex/` templates define regular review, full alignment review, and goal-tracker update duties. `pr-loop/` templates initialize PR review state and instruct round-0 behavior depending on whether comments exist.
- inputs_outputs_state: Inputs are `{{UPPER_CASE}}` placeholders rendered by `template-loader.sh`, whose syntax contract is documented in `hooks/lib/template-loader.sh:7-13`. Output is Markdown returned to Claude/Codex or written into generated prompt/goal files. `setup-pr-loop.sh` renders `pr-loop/goal-tracker-initial.md` into `goal-tracker.md` at `scripts/setup-pr-loop.sh:632-633`, then composes round-0 prompt content from `pr-loop/round-0-header.md` and task templates around `scripts/setup-pr-loop.sh:637-675`. Stop hooks render blocking JSON reasons with templates such as `block/unpushed-commits.md`, `block/no-trigger-comment.md`, and `block/claude-eyes-timeout.md` in `hooks/pr-loop-stop-hook.sh:350-359`, `hooks/pr-loop-stop-hook.sh:717-735`, and `hooks/pr-loop-stop-hook.sh:768-779`.
- gates_or_invariants: `block/state-file-modification.md:1-10` forbids changing RLCR `state.md`; `block/pr-loop-state-modification.md:1-12` forbids changing PR-loop `state.md`; `block/prompt-file-write.md:1-12` makes generated prompt files read-only; `block/goal-tracker-modification.md:1-25` says after round 0 only Codex updates `goal-tracker.md`; `block/plan-file-modified.md:1-12` forbids changing the original plan during an active loop; `block/force-push-detected.md:1-17` requires a new trigger after rewritten history; `codex/regular-review.md:49-57` and `codex/full-alignment-review.md:86-93` constrain `COMPLETE` to full completion with no deferrals.
- dependencies_and_callers: `hooks/lib/template-loader.sh:24-31` resolves this directory, `load_template` reads files at `hooks/lib/template-loader.sh:33-48`, `render_template` performs single-pass placeholder substitution at `hooks/lib/template-loader.sh:50-132`, and `load_and_render_safe` supplies fallbacks at `hooks/lib/template-loader.sh:167-203`. `hooks/lib/loop-common.sh` calls block templates through message helpers, including state protection at `hooks/lib/loop-common.sh:367-374`, finalize-state protection at `hooks/lib/loop-common.sh:376-383`, summary bash protection at `hooks/lib/loop-common.sh:385-394`, goal-tracker bash protection at `hooks/lib/loop-common.sh:396-405`, PR-loop state/prompt protection at `hooks/lib/loop-common.sh:793-810`, and git-add protection at `hooks/lib/loop-common.sh:932-970`. `hooks/pr-loop-stop-hook.sh:57` also sets `TEMPLATE_DIR` directly.
- edge_cases_or_failure_modes: Missing templates are not supposed to produce blank blocker messages because callers use `load_and_render_safe`; fallback behavior is in `hooks/lib/template-loader.sh:179-199`. Placeholder substitution is intentionally single pass, so user-supplied values containing `{{OTHER_VAR}}` are not re-expanded (`hooks/lib/template-loader.sh:54-57`). The template inventory has 919 total lines across 45 Markdown files; `pr-loop/` templates are not required by `validate_template_dir`, which only checks `block`, `codex`, and `claude` at `hooks/lib/template-loader.sh:216-219`.
- validation_or_tests: `tests/test-template-loader.sh:41-220` covers path resolution, existing/missing template loads, variable substitution, unreplaced placeholders, and safe fallback. `tests/test-template-references.sh:51-230` scans shell references and checks critical templates exist, including `block/state-file-modification.md` at `tests/test-template-references.sh:152-159`. `tests/test-templates-comprehensive.sh:61-194` loads all templates and validates placeholder syntax, including uppercase-only placeholders and malformed brace detection.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-035 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop ingestion script. It fetches PR issue comments, inline review comments, and PR review summaries from GitHub, normalizes them into one JSON stream, filters/sorts/deduplicates them, and emits Markdown for the PR loop prompt files.
- algorithmic_behavior: Argument parsing accepts `<pr_number> <output_file>`, `--after <timestamp>`, and `--bots <bot1,bot2>` (`scripts/fetch-pr-comments.sh:27-90`). It validates required args and numeric PR number at `scripts/fetch-pr-comments.sh:92-106`, requires `gh` and `jq` at `scripts/fetch-pr-comments.sh:108-120`, resolves the base repository for fork PRs at `scripts/fetch-pr-comments.sh:126-161`, fetches three API endpoints with retry at `scripts/fetch-pr-comments.sh:182-219`, normalizes the three comment types with `jq` at `scripts/fetch-pr-comments.sh:259-319`, deduplicates by ID at `scripts/fetch-pr-comments.sh:321-323`, optionally filters by `created_at > --after` at `scripts/fetch-pr-comments.sh:325-331`, sorts humans before bots and newest first at `scripts/fetch-pr-comments.sh:333-342`, then writes Markdown sections at `scripts/fetch-pr-comments.sh:344-442`.
- inputs_outputs_state: Inputs are GitHub PR number, output path, optional ISO timestamp, and optional comma-separated bot list. External inputs come from GitHub API endpoints: `issues/<pr>/comments`, `pulls/<pr>/comments`, and `pulls/<pr>/reviews` (`scripts/fetch-pr-comments.sh:209-219`). Output is a Markdown file headed with PR number, fetched timestamp, repository, Human Comments, Bot Comments, and warnings. Temporary JSON and JSONL files live under `mktemp -d` and are cleaned by `trap` at `scripts/fetch-pr-comments.sh:167-173`.
- gates_or_invariants: Fails closed for missing PR/output args, unknown options, non-numeric PR number, missing `gh`, missing `jq`, and unresolvable base repository (`scripts/fetch-pr-comments.sh:27-120`, `scripts/fetch-pr-comments.sh:130-161`). It never exits nonzero for per-endpoint API fetch failure after retries; instead it writes `[]`, increments `API_FAILURES`, continues, and appends a warning to output at `scripts/fetch-pr-comments.sh:194-203` and `scripts/fetch-pr-comments.sh:444-449`. PR reviews with empty bodies are preserved using `[Review state: STATE]` at `scripts/fetch-pr-comments.sh:300-312`.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh:411-412` to create `round-0-pr-comment.md` grouped by active bots. Tests and fixtures call it in `tests/test-pr-loop-hooks.sh:1249-1251`, `tests/test-pr-loop-hooks.sh:1294-1296`, and `tests/test-pr-loop-scripts.sh:266-325`. Depends on `gh`, `jq`, `sed`, `date`, `mktemp`, and GitHub API pagination.
- edge_cases_or_failure_modes: Fork PR support tries current repo first and parent repo second (`scripts/fetch-pr-comments.sh:136-153`). `--after` uses lexicographic ISO timestamp comparison, so malformed or non-UTC timestamps can produce unexpected filtering (`scripts/fetch-pr-comments.sh:326-329`). `unique_by(.id)` deduplicates only by numeric ID across all sources; if GitHub ID namespaces ever collided across issue/review/review-comment types, one item could be dropped (`scripts/fetch-pr-comments.sh:321-323`). Sorting filters out null `created_at`, so malformed entries disappear (`scripts/fetch-pr-comments.sh:333-342`). The helper `is_bot` and `format_timestamp` are defined but not used (`scripts/fetch-pr-comments.sh:225-242`), which is harmless but stale.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:268-325` covers help, missing PR number, missing output file, and invalid PR number. Fixture test `tests/test-pr-loop-hooks.sh:1242-1283` verifies all three comment types are included, including approval-only PR reviews with the `[Review state: APPROVED]` placeholder. `tests/test-pr-loop-hooks.sh:1287-1318` verifies `--after` includes later approvals and excludes earlier comments.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-065 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR state-file lifecycle naming and active-loop discovery. It pins the invariant that only `state.md` marks a live loop, while terminal states are reason-prefixed files.
- algorithmic_behavior: Sets up a temporary Git repo and `.humanize/rlcr/<timestamp>` directory (`tests/test-state-exit-naming.sh:31-49`), sources `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:64-65`, and exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. Tests 1-7 validate active-loop sentinel behavior and newest-session precedence (`tests/test-state-exit-naming.sh:38-176`). Tests 8-10 validate `end_loop` reason validation, rename targets, and missing-state handling (`tests/test-state-exit-naming.sh:178-239`). Tests 11-13 validate new `.humanize/rlcr` path matching and legacy `.humanize-loop.local` separation (`tests/test-state-exit-naming.sh:241-287`).
- inputs_outputs_state: Inputs are synthetic state files with YAML frontmatter. State transitions are direct file creates/removes plus `end_loop "$loop_dir" "$state_file" "$reason"`. Expected outputs are empty or specific active-loop path from `find_active_loop`, generated `<reason>-state.md` files from `end_loop`, and pass/fail counters reported at `tests/test-state-exit-naming.sh:289-298`.
- gates_or_invariants: `find_active_loop` should ignore `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` (`tests/test-state-exit-naming.sh:51-155`). `state.md` must be detected as active (`tests/test-state-exit-naming.sh:74-91`). Newer timestamp directories with `state.md` win (`tests/test-state-exit-naming.sh:157-176`). `end_loop` only accepts `complete`, `cancel`, `maxiter`, `stop`, and `unexpected`, mirroring constants and validation in `hooks/lib/loop-common.sh:30-40` and `hooks/lib/loop-common.sh:1023-1049`.
- dependencies_and_callers: Depends on `git`, `mktemp`, shell file operations, and `hooks/lib/loop-common.sh`. Production functions under test are `find_active_loop` at `hooks/lib/loop-common.sh:147-170`, `is_in_humanize_loop_dir` at `hooks/lib/loop-common.sh:617`, and `end_loop` at `hooks/lib/loop-common.sh:1016-1049`.
- edge_cases_or_failure_modes: There is an implementation/test tension: production `find_active_loop` says it checks only the newest directory and ignores older directories (`hooks/lib/loop-common.sh:147-150`), while test 7 expects a newer directory with `state.md` to take precedence. That is aligned for the positive case but does not test the documented negative case where the newest directory lacks `state.md` and an older directory has one. The script uses `set -uo pipefail`, not `set -e`, so individual failures increment counters rather than aborting early (`tests/test-state-exit-naming.sh:13`).
- validation_or_tests: This file is itself validation. It also indirectly validates the prompt-block policy in `prompt-template/block/state-file-modification.md:1-10` by ensuring terminated loop state is preserved under renamed files rather than directly edited while active.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-095 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking message template for attempts to modify RLCR `state.md`. It is a compact operator-facing explanation for a hard guard enforced by write and bash validators.
- algorithmic_behavior: The template contains no placeholders. It emits a fixed Markdown reason: `state.md` is loop-managed, includes current round, max iterations, and Codex configuration, and manual edits would corrupt loop state (`prompt-template/block/state-file-modification.md:1-10`).
- inputs_outputs_state: Input is only the validator’s decision to render this template. Output is Markdown written to stderr or hook block reason. It does not mutate state. The protected state it describes is parsed in production by `parse_state_file_strict` (`hooks/lib/loop-common.sh:231-296`) and transitioned only by controlled operations like `end_loop` (`hooks/lib/loop-common.sh:1016-1049`).
- gates_or_invariants: Write validator blocks direct Write attempts to `state.md` after detecting an active loop and strict-parsing state at `hooks/loop-write-validator.sh:140-159`. Bash validator blocks shell modifications to `state.md` as destination and source, with a narrow authorized-cancel exception, at `hooks/loop-bash-validator.sh:139-156` and `hooks/loop-bash-validator.sh:158-328`. The message helper loads this exact template at `hooks/lib/loop-common.sh:367-374`.
- dependencies_and_callers: Called via `state_file_blocked_message` in `hooks/lib/loop-common.sh:367-374`, then used by `hooks/loop-write-validator.sh:157-159` and `hooks/loop-bash-validator.sh:149-155`, `hooks/loop-bash-validator.sh:296-302`, and `hooks/loop-bash-validator.sh:319-326`. Loaded through `load_and_render_safe`, so fallback text exists if the file is missing.
- edge_cases_or_failure_modes: Because `is_state_file_path` uses `state\.md$` (`hooks/lib/loop-common.sh:413-417`), `finalize-state.md` also matches unless the more specific finalize check runs first. Both write and bash validators explicitly check finalize-state first to avoid the wrong message (`hooks/loop-write-validator.sh:148-154`, `hooks/loop-bash-validator.sh:127-145`). Authorized cancel flow can allow controlled state rename when signal-file validation passes (`hooks/lib/loop-common.sh:437-455`).
- validation_or_tests: `tests/test-template-references.sh:152-159` requires this template to exist as a common template. `tests/test-state-exit-naming.sh:182-226` validates the controlled alternative to manual modification: `end_loop` renames `state.md` to `<reason>-state.md`. `tests/robustness/test-hook-input-robustness.sh:351-370` confirms command modification pattern detection for `sed -i` and redirects, which is the underlying bash guard used to reach this block.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-125 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for hook JSON parsing, shell command modification detection, monitor helpers, and real monitor edge behavior. It verifies failure modes around malformed input and terminal/log anomalies do not crash validators or monitor code.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-hook-input-robustness.sh:14-17`), creates a test directory (`tests/robustness/test-hook-input-robustness.sh:19`), then runs 26 tests. Tests 1-10c pipe JSON into production `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh` (`tests/robustness/test-hook-input-robustness.sh:35-235`). Tests 11-18 simulate terminal/log edge cases (`tests/robustness/test-hook-input-robustness.sh:245-341`). Tests 19-20 call `command_modifies_file` for `sed -i` and redirect detection (`tests/robustness/test-hook-input-robustness.sh:351-370`). Tests 21-23 source `scripts/humanize.sh` and call monitor helper parsers (`tests/robustness/test-hook-input-robustness.sh:381-433`). Tests 24-26 run the real `_humanize_monitor_codex` under missing session, narrow terminal, and ANSI-log conditions (`tests/robustness/test-hook-input-robustness.sh:444-664`).
- inputs_outputs_state: Inputs are synthetic hook JSON payloads, long commands, special characters, Unicode paths, deeply nested JSON, invalid binary bytes, temp log files, temp `.humanize/rlcr` sessions, fake `HOME`/`XDG_CACHE_HOME`, and small runner scripts written under temp directories. Outputs are pass/fail test records and the final summary at `tests/robustness/test-hook-input-robustness.sh:666-671`.
- gates_or_invariants: Valid JSON for non-loop paths exits 0 (`tests/robustness/test-hook-input-robustness.sh:35-69`). Invalid JSON, empty JSON, missing required `file_path`, deeply nested JSON, and non-UTF8 content must exit nonzero without signal crash (`tests/robustness/test-hook-input-robustness.sh:71-217`). Unknown tools pass through (`tests/robustness/test-hook-input-robustness.sh:163-175`). Null bytes are treated as “handled gracefully” because Bash command substitution strips them before the hook can inspect them (`tests/robustness/test-hook-input-robustness.sh:219-235`). Monitor must exit with code below 128 in edge cases (`tests/robustness/test-hook-input-robustness.sh:573-578`, `tests/robustness/test-hook-input-robustness.sh:660-664`).
- dependencies_and_callers: Depends on the production validators `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, shared validation functions in `hooks/lib/loop-common.sh`, and monitor functions in `scripts/humanize.sh`. Validator behavior comes from `validate_hook_input` at `hooks/lib/loop-common.sh:55-92`, `require_tool_input_field` at `hooks/lib/loop-common.sh:94-110`, `is_deeply_nested` at `hooks/lib/loop-common.sh:112-129`, and `command_modifies_file` at `hooks/lib/loop-common.sh:972-1000`.
- edge_cases_or_failure_modes: The test intentionally covers long 10KB commands, shell metacharacters, Unicode JSON escapes, 50-level nested structures, non-UTF8 bytes, Bash-stripped null bytes, binary log content, rapid log growth, missing `.humanize/rlcr`, narrow terminal widths, and ANSI escape sequences. The monitor helper assertion comments have one mismatch: the expected `humanize_parse_goal_tracker` format comment says `pending_tasks|completed_tasks|open_issues|deferred_tasks`, but `scripts/humanize.sh:30-111` returns `active_tasks|completed_tasks|deferred_tasks|open_issues`; the regex only validates shape, not semantic ordering.
- validation_or_tests: This file is itself a robustness test. It directly validates the production hook parsing pipeline in validators, plus production monitor helper APIs `humanize_parse_goal_tracker` (`scripts/humanize.sh:32-111`), `humanize_parse_git_status` (`scripts/humanize.sh:166-210`), `humanize_detect_git_state` (`scripts/humanize.sh:116-162`), and `_humanize_monitor_codex` (`scripts/humanize.sh:219-460` excerpted).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, matching the assigned count
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`