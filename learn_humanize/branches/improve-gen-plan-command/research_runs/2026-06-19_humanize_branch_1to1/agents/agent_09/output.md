# agent_09 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for RLCR and PR-loop hooks. It centralizes hook input validation, loop discovery, state parsing, path classification, guard/block message rendering, cancel authorization, `.humanize` git-add protection, PR bot name mapping, PR-loop round validation, and PR goal-tracker mutation. The directory contains two files: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`.
- algorithmic_behavior: `loop-common.sh` first defines state field constants and exit reason constants at lines 15-40. It validates hook JSON with null-byte rejection, optional UTF-8 validation through `iconv`, `jq` syntax validation, required `tool_name`, and extracted `tool_input` at lines 55-92. It adds a DoS guard with `is_deeply_nested`, which computes maximum JSON path depth using `jq '[paths | length] | max // 0'` and flags input deeper than the configured threshold at lines 115-129.
- algorithmic_behavior: Loop state behavior is split between tolerant and strict parsing. `find_active_loop` selects only the newest timestamp directory under `.humanize/rlcr` and requires `state.md` or `finalize-state.md`, intentionally ignoring older zombie sessions at lines 147-170. `get_current_round` extracts `current_round` from YAML frontmatter and defaults to `0` at lines 175-185. `parse_state_file` tolerates missing fields and applies defaults for `current_round`, `max_iterations`, and `push_every_round` at lines 202-228. `parse_state_file_strict` fails closed when the file is missing, has fewer than two YAML separators, lacks `current_round` or `max_iterations`, or has non-numeric values at lines 240-295.
- algorithmic_behavior: File/path classification helpers drive the validators: RLCR round file matching and round extraction at lines 299-322, allowlisted todo files at lines 327-345, goal/state/finalize path predicates at lines 408-429, and loop-dir predicates for `.humanize/rlcr` and `.humanize/pr-loop` at lines 617-697. PR-loop helpers mirror RLCR discovery and classify PR round files, read-only PR-loop generated files, and resolution-summary round correctness at lines 702-790.
- algorithmic_behavior: Cancel authorization is a security-sensitive parser around a narrowly allowed `mv state.md|finalize-state.md cancel-state.md` transition. `is_cancel_authorized` requires `.cancel-requested`, rejects command substitution, backticks, newlines, shell chaining operators, extra `$` variables after `$loop_dir` normalization, mixed quote styles, extra args, wrong source/destination paths, and symlinked source state files at lines 455-614.
- algorithmic_behavior: Git staging protection is encoded in `git_adds_humanize`, which splits shell command segments and detects `git add` invocations that directly name `.humanize`, force-add broad scopes, `--all`, or broad `.`/`*` when `.humanize` exists and is not ignored at lines 834-930. Shell write detection is generalized by `command_modifies_file`, which matches redirects, `tee`, in-place edits, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >` patterns at lines 975-1000.
- algorithmic_behavior: `end_loop` is the state transition primitive that renames active `state.md` to `<reason>-state.md` for allowed reasons `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` at lines 1023-1048. `update_pr_goal_tracker` updates PR-loop progress tables and log entries idempotently per round plus reviewer: it detects existing summary rows/log entries, repairs partial updates, parses bot result JSON with `jq`, recalculates totals, inserts a table row, and appends a timestamped log entry at lines 1072-1219.
- algorithmic_behavior: `template-loader.sh` resolves `prompt-template` relative to `hooks/lib`, loads templates, renders `{{VAR}}` placeholders in a single pass through `awk`, keeps unresolved placeholders unchanged, exposes safe fallback rendering, and validates required template subdirectories at lines 24-31, 36-48, 58-132, 170-203, and 208-222. Its single-pass design prevents substituted values containing `{{OTHER_VAR}}` from being rescanned and accidentally mutating prompt content.
- inputs_outputs_state: Inputs include Claude hook JSON on stdin, active loop directories under `.humanize/rlcr` and `.humanize/pr-loop`, YAML frontmatter in state files, template files under `prompt-template`, shell command strings, file paths, PR bot result JSON, and Git/GitHub state as observed by callers. Outputs include exit codes, stderr block messages, rendered markdown block templates, global `STATE_*` parser variables, global `VALIDATED_*` hook-input variables, renamed state files, and updated `goal-tracker.md`.
- gates_or_invariants: The directory enforces one newest active loop, strict fail-closed state validation for most validators, max JSON depth, no null/invalid UTF-8 hook input, protected state/finalize-state files, protected generated prompt/todo files, round-correct summaries, no direct `.humanize` staging, authorized cancel only through signal file plus exact move command, PR-loop read-only generated files, and idempotent PR goal-tracker updates.
- dependencies_and_callers: `template-loader.sh` is sourced by `loop-common.sh` at `hooks/lib/loop-common.sh:137` and directly by `scripts/setup-pr-loop.sh:39`. `loop-common.sh` is sourced by read/write/edit/bash/plan validators and stop hooks, including `hooks/loop-read-validator.sh:19`, `hooks/loop-write-validator.sh:19`, `hooks/loop-edit-validator.sh:18`, `hooks/loop-bash-validator.sh:18`, `hooks/loop-plan-file-validator.sh:17`, `hooks/loop-codex-stop-hook.sh:48`, and `hooks/pr-loop-stop-hook.sh:53`. `setup-pr-loop.sh` also sources it for loop discovery and helper functions at `scripts/setup-pr-loop.sh:211`.
- dependencies_and_callers: Concrete call sites show its role as a coordination layer. The Bash validator uses `validate_hook_input`, `is_deeply_nested`, `parse_state_file_strict`, `git_adds_humanize`, and `is_cancel_authorized` at `hooks/loop-bash-validator.sh:27`, `hooks/loop-bash-validator.sh:32`, `hooks/loop-bash-validator.sh:82`, `hooks/loop-bash-validator.sh:115`, and `hooks/loop-bash-validator.sh:141`. The read/write validators use strict state parsing and round/path checks at `hooks/loop-read-validator.sh:55-99` and `hooks/loop-write-validator.sh:55-91`. The PR stop hook calls `update_pr_goal_tracker` for success and issue feedback at `hooks/pr-loop-stop-hook.sh:1361` and `hooks/pr-loop-stop-hook.sh:1518`.
- edge_cases_or_failure_modes: Strict parser accepts negative numeric values because the regex is `^-?[0-9]+$`, so semantic bounds are left to callers. Tolerant parser can return raw non-numeric `current_round` through `get_current_round`, by design. `find_active_loop` only considers newest timestamp directory; an older valid loop is ignored if a newer directory lacks active state, preventing zombie revival but also making recovery depend on timestamp ordering. `git_adds_humanize` lowercases commands before analysis in callers, which is documented for `-A` becoming `-a`. `is_cancel_authorized` lowercases the command path, so case-sensitive filesystem edge cases could matter, but its target scope is normalized loop paths.
- validation_or_tests: Covered broadly by tests referenced in the repo: template references/comprehensive/error/stress tests, cancel-signal and cancel-security tests, humanize escape tests, Bash validator pattern tests, PR-loop system tests, and state robustness tests. Assigned test `tests/robustness/test-state-file-robustness.sh` directly validates `parse_state_file`, `parse_state_file_strict`, and `get_current_round`. The global test runner includes `test-template-loader.sh`, `test-bash-validator-patterns.sh`, `test-template-references.sh`, `test-pr-loop-system.sh`, and robustness suites at `tests/run-all-tests.sh:33-65`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-039 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for `/humanize:start-pr-loop`. It creates a PR-loop session under `.humanize/pr-loop/<timestamp>`, detects the current PR and monitored bots, fetches initial review comments, optionally triggers re-review, verifies Claude reaction when needed, writes loop state and goal tracker files, renders the first prompt, and prints activation instructions.
- algorithmic_behavior: The script runs under `set -euo pipefail`, defines defaults for Codex model/effort/timeout, max iterations, polling interval/timeout, git timeout, and GitHub CLI timeout at lines 11-30. It sources `portable-timeout.sh`, `template-loader.sh`, and later `loop-common.sh` at lines 33-42 and 210-211. Argument parsing accepts `--claude`, `--codex`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and help; invalid or missing option arguments exit with errors at lines 57-173.
- algorithmic_behavior: Bot selection requires at least one monitored bot, then builds `ACTIVE_BOTS_ARRAY` with internal names `claude` and `codex` at lines 175-202. It builds the mention string with shared `build_bot_mention_string` after sourcing `loop-common.sh` at lines 210-214.
- algorithmic_behavior: The setup gate enforces mutual exclusion with active RLCR or PR loops by calling `find_active_loop` and `find_active_pr_loop` and exiting if either returns a directory at lines 217-241. It then validates runtime prerequisites: git repository, at least one commit, installed/authenticated `gh`, and available `codex` at lines 243-277.
- algorithmic_behavior: PR discovery resolves fork context before reading PR data. It gets current branch at lines 283-287, current repo at lines 295-297, parent repo at lines 299-301, tries `gh pr view` in current repo first, then parent repo for forks at lines 303-324, requires numeric PR number at lines 326-338, rejects merged/closed PRs at lines 340-349, and resolves the PR base repository for API calls at lines 351-359.
- algorithmic_behavior: YAML safety gates reject branch names containing `:`, `#`, quotes, backticks, or newline; Codex model must match `[a-zA-Z0-9._-]+`; Codex effort must match `[a-zA-Z0-9_-]+` at lines 361-388. These checks protect the generated `state.md` frontmatter.
- algorithmic_behavior: The script creates a timestamped loop directory, fetches initial comments via `fetch-pr-comments.sh`, asks `check-pr-reviewer-status.sh` for startup case data, falls back to `git rev-parse HEAD` when GitHub lacks a commit SHA, and logs startup case/commit at lines 390-437.
- algorithmic_behavior: For startup cases `4` and `5`, it posts a trigger comment to the PR base repo, aborts and removes the loop directory if posting fails, fetches the current user’s latest comment ID/timestamp through `gh api`, and deliberately avoids local-time fallback to prevent clock-skew filtering bugs at lines 438-483. When `--claude` is active, it requires a retrieved trigger comment ID and verifies Claude’s eyes reaction with `check-bot-reactions.sh`; failures clean up the loop directory and exit at lines 484-521.
- algorithmic_behavior: State creation writes YAML frontmatter to `$LOOP_DIR/state.md` with current round, max iterations, PR number, branch, configured/active bots as YAML lists, Codex settings, polling settings, startup case, latest commit metadata, last trigger timestamp, and trigger comment ID at lines 524-555. Goal tracker creation renders `prompt-template/pr-loop/goal-tracker-initial.md` with a fallback, including per-bot acceptance criteria and initial log row at lines 557-635.
- algorithmic_behavior: Prompt creation writes `round-0-prompt.md`, appends fetched PR comments, detects the exact `*No comments found.*` sentinel, and selects either no-comments or has-comments task template/fallback. The no-comments path instructs summary-only exit and automatic bot review; the comments path instructs issue fixing, commit/push, PR re-review comment, and writing `round-0-pr-resolve.md` at lines 637-772. It then prints activation metadata and critical completion requirements based on comment presence at lines 773-865.
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR` or current directory, git repository state, GitHub CLI authentication/API data, current branch, PR state, bot list, template files, fetched comments, and reviewer status JSON. Outputs are `.humanize/pr-loop/<timestamp>/state.md`, `round-0-pr-comment.md`, `goal-tracker.md`, `round-0-pr-resolve.md` path instruction, `round-0-prompt.md`, optional PR trigger comments, optional Claude reaction verification, and terminal activation instructions.
- gates_or_invariants: Exactly one loop type may be active; at least one bot is required; the project must be a git repo with a commit; `gh` and `codex` must exist; `gh` must be authenticated; a live non-merged/non-closed PR must be found; PR number must be numeric; fork PRs use the base repo for comments; branch/model/effort values must be YAML-safe; case 4/5 trigger-comment failure aborts; Claude reaction verification is mandatory when `--claude` is used after a trigger; generated state is not created silently after failed trigger/reaction verification.
- dependencies_and_callers: The command wrapper `commands/start-pr-loop.md` allows only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)` and invokes the script with `$ARGUMENTS` at lines 1-14. Runtime dependencies include `scripts/portable-timeout.sh`, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `git`, `gh`, `codex`, and `jq`. The PR-loop stop hook later consumes the state, prompt, comments, and resolution summary files created here.
- edge_cases_or_failure_modes: Fork PR lookup falls back from current repo to parent repo; parent repo strings `null/` and `/` are ignored at line 317. Missing reviewer-status JSON defaults to startup case 1 at lines 418-422. Missing GitHub commit SHA falls back to local HEAD at lines 430-433. `COMMENT_COUNT` is binary: any content other than the no-comments sentinel is treated as comments, not a precise count, at lines 643-649. `ACTIVE_BOTS_DISPLAY=$(IFS=', '; echo "${ACTIVE_BOTS_ARRAY[*]}")` uses Bash array joining; because only comma is the first `IFS` char, display may join with commas rather than comma-space. If output consumer closes the pipe, `trap 'exit 0' PIPE` makes final output failure non-fatal after setup work has completed at lines 777-778.
- validation_or_tests: `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-system.sh`, and `tests/test-pr-loop-hooks.sh` cover setup and PR-loop behavior according to references. Specific repo tests mention setup failure when trigger comment ID cannot be retrieved and goal tracker creation integration in `tests/test-pr-loop-system.sh` reference hits around lines 1384-1472 from the search results. The global runner includes `test-pr-loop.sh` and `test-pr-loop-system.sh` at `tests/run-all-tests.sh:51-53`. I did not run tests because this worker was instructed to produce research notes only and not modify files.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-069 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook preflight that blocks loop exit when the latest Claude `TodoWrite` call contains incomplete todos. It defines expected exit-code semantics and transcript parsing behavior.
- algorithmic_behavior: The test locates the project root and todo checker at lines 11-14, tracks pass/fail counters with helper functions at lines 20-34, creates an isolated `mktemp -d` workspace, and deletes it on exit at lines 36-38.
- algorithmic_behavior: Test group 1 validates hook input handling. Invalid JSON and empty stdin must return exit code `2` at lines 51-73. Valid JSON without `transcript_path` exits `0` at lines 75-85. A non-existent transcript path exits `0` at lines 87-97.
- algorithmic_behavior: Test group 2 validates todo completion gates. A transcript whose latest `TodoWrite` has all `status: completed` exits `0` at lines 106-119. A pending todo exits `1` at lines 121-134. The output must include the incomplete task name at lines 136-142. An `in_progress` todo also exits `1` at lines 144-157.
- algorithmic_behavior: Test group 3 validates transcript format tolerance. Empty transcript files exit `0` at lines 166-177. Invalid JSONL lines are ignored while valid later lines are parsed at lines 179-194. Multiple `TodoWrite` calls use the latest call, so an old pending todo followed by a completed todo exits `0` at lines 196-210. Direct `type: tool_use` and alternative `type: message` transcript shapes are accepted at lines 212-240.
- algorithmic_behavior: Test group 4 covers edge cases. Missing `status` is incomplete and exits `1` at lines 249-263. Empty todo content with pending status still exits `1` at lines 265-278. Unicode content in completed todos exits `0` at lines 280-293. The suite exits `0` only when `TESTS_FAILED` is zero at lines 295-313.
- inputs_outputs_state: Inputs are synthetic hook JSON objects sent to `python3 "$TODO_CHECKER"` and synthetic JSONL transcript files under the temp directory. Outputs are observed process exit codes, captured stdout/stderr, pass/fail counters, and human-readable test result lines. Persistent state is limited to temp files removed by the trap.
- gates_or_invariants: The checker contract is: malformed hook input is a parse error exit `2`; missing/nonexistent transcript is non-blocking exit `0`; no todos is non-blocking; latest `TodoWrite` wins; only exact `status == "completed"` is complete; every other status, including missing or empty status, is incomplete exit `1`; incomplete output includes task details.
- dependencies_and_callers: The test directly targets `hooks/check-todos-from-transcript.py`, whose behavior is used by `hooks/loop-codex-stop-hook.sh`. The stop hook calls the checker with full hook input at `hooks/loop-codex-stop-hook.sh:261-266`, blocks parse errors at `hooks/loop-codex-stop-hook.sh:268-285`, and blocks incomplete todos before running expensive Codex review at `hooks/loop-codex-stop-hook.sh:287-309`. The global test runner includes this suite at `tests/run-all-tests.sh:33-37`.
- edge_cases_or_failure_modes: The test intentionally treats malformed transcript lines as ignorable rather than fatal, which means corrupt transcript fragments can be skipped if a later valid `TodoWrite` exists. Nonexistent transcripts permit exit, making transcript availability a soft gate. It does not verify behavior for malformed todo objects that are not dictionaries; the Python checker assumes each todo supports `.get`, so a non-object entry in the latest todos array could raise at runtime. The “Unicode” test content is ASCII text despite the label at lines 280-284.
- validation_or_tests: This file is itself validation. It maps directly to Python checker internals: three transcript formats are implemented at `hooks/check-todos-from-transcript.py:51-85`, latest todos are updated as the file is scanned at lines 28-87, missing transcript returns `[]` at lines 25-26, hook JSON parse errors exit `2` at lines 90-97, missing transcript_path exits `0` at lines 99-101, and incomplete todos emit `INCOMPLETE_TODOS` plus list and exit `1` at lines 113-126.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-099 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop hook when the agent attempts to exit without writing the required round or finalize summary file. It defines the recovery instruction shown to the user/agent and binds the required summary path through `{{SUMMARY_FILE}}`.
- algorithmic_behavior: The template is short markdown: title at line 1, exit-without-summary explanation at line 3, required action and fenced summary path placeholder at lines 5-8, required summary contents at lines 10-14, and retry instruction at line 16. It has no executable logic; its algorithmic effect is through template rendering and stop-hook blocking.
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable supplied by the stop hook to `load_and_render_safe`. Output is a rendered markdown reason string included in the hook’s JSON block response. It does not read or write state itself.
- gates_or_invariants: The invariant encoded by the template is that loop exit is not allowed until a work summary file exists. The summary must include implementation work, created/modified files, tests added or passed, and remaining items. Missing variables remain as placeholders because `template-loader.sh` preserves unresolved `{{VAR}}` tokens at `hooks/lib/template-loader.sh:119-122`.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` constructs `SUMMARY_FILE` as either `finalize-summary.md` during Finalize Phase or `round-${CURRENT_ROUND}-summary.md` otherwise at lines 525-530. If the file does not exist, it renders this template with `SUMMARY_FILE=$SUMMARY_FILE` and returns a JSON block decision at lines 532-556. The rendering path depends on `load_and_render_safe` in `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook has an inline fallback at `hooks/loop-codex-stop-hook.sh:536-540`, so the gate still blocks. If `SUMMARY_FILE` contains unusual characters, it is inserted directly by the single-pass renderer; the hook later passes it to `jq --arg`, which safely JSON-escapes the rendered reason at `hooks/loop-codex-stop-hook.sh:548-555`. The template does not distinguish normal vs finalize summary; that context is supplied by the stop hook’s `systemMessage`.
- validation_or_tests: Referenced by template-reference tests according to `tests/test-template-references.sh` search hits. The concrete caller path is covered by stop-hook/finalize tests; `tests/test-finalize-phase.sh` is included in the global runner at `tests/run-all-tests.sh:45`, and `loop-codex-stop-hook.sh` uses this template directly at lines 536-540. I did not run tests because this was a read-only research pass.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-129 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing in `hooks/lib/loop-common.sh`. It tests tolerant and strict parser behavior under valid frontmatter, missing fields, malformed YAML, unusual values, partial writes, binary suffixes, CRLF endings, and nonexistent files.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared test helpers, creates a temp test directory, and prints suite headers at lines 14-24. Positive tests create valid state files and assert `parse_state_file` extracts `current_round` and `max_iterations`, `get_current_round` returns the round, extra fields are ignored, quoted `plan_file` and `start_branch` values are unquoted correctly, and round `0` is preserved at lines 33-137.
- algorithmic_behavior: Negative/malformed tests distinguish strict vs tolerant behavior. Missing YAML separators must be rejected by `parse_state_file_strict`, while `get_current_round` defaults to `0` at lines 147-169. Non-numeric `current_round` must be rejected by strict parser, while tolerant extraction must not crash at lines 171-197. Missing required fields must be rejected by strict parser, while tolerant parser defaults to `current_round=0` and `max_iterations=10` at lines 199-226.
- algorithmic_behavior: Additional edge tests assert empty state returns default round `0`, partial YAML does not crash and may return `0` or `5`, malformed field syntax returns default `0`, very large numeric values are preserved, negative round values are handled gracefully, string values with spaces parse correctly, trailing whitespace does not alter numeric extraction, nonexistent files return nonzero from tolerant parser, binary content after YAML does not break round extraction, and CRLF line endings do not crash at lines 228-393.
- inputs_outputs_state: Inputs are synthetic `state*.md` files written under the temp test directory. Outputs are pass/fail assertions through `pass`, `fail`, `print_test_summary`, global parser variables such as `STATE_CURRENT_ROUND`, and the script exit code from `print_test_summary` at lines 395-400.
- gates_or_invariants: Strict parser must fail closed for missing frontmatter, missing `current_round`/`max_iterations`, and non-numeric required values. Tolerant parser must preserve backward compatibility: return defaults for missing fields, ignore unrecognized fields, strip quotes for string fields, and avoid crashing on malformed/partial/binary/CRLF input. Nonexistent state files must return nonzero. Binary content after the YAML frontmatter must not affect parsed round.
- dependencies_and_callers: Directly depends on `parse_state_file`, `parse_state_file_strict`, and `get_current_round` from `hooks/lib/loop-common.sh`, sourced at line 16. These functions are used in production by validators and hooks: read/write/edit/bash validators and plan-file validator all call strict parsing before enforcing loop rules, e.g. `hooks/loop-read-validator.sh:94-99`, `hooks/loop-write-validator.sh:140-145`, `hooks/loop-edit-validator.sh:105-110`, `hooks/loop-bash-validator.sh:81-86`, and `hooks/loop-plan-file-validator.sh:44-48`. The global test runner includes this robustness suite at `tests/run-all-tests.sh:55`.
- edge_cases_or_failure_modes: The test documents accepted looseness: tolerant handling may return either `0` or `5` for partial YAML, negative numbers are considered “some value” rather than invalid in `get_current_round`, and CRLF handling only asserts no crash, not a normalized value. Strict parser validates numeric syntax but not semantic bounds, so negative and very large values can pass if explicitly parsed by strict mode. The test appends null/binary bytes after frontmatter, but not inside frontmatter field values.
- validation_or_tests: This file is the validation surface for state parser robustness. It aligns with implementation lines in `loop-common.sh`: tolerant defaults at `hooks/lib/loop-common.sh:223-227`, strict separator count and required-field checks at lines 248-278, numeric checks at lines 280-289, and optional default `push_every_round=false` at lines 292-293. I did not execute the suite in this branch export because the assignment requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`