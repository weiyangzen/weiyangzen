# agent_16 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`
- notes: inspected assigned files directly; followed referenced command, hook, template-loader, and validator dependencies; no repository files modified.

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-016 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Defines the specialized subagent used by `commands/gen-plan.md` during Phase 2 to decide whether a draft document is semantically relevant to the current repository before deeper planning begins. Frontmatter registers `name: draft-relevance-checker`, `model: haiku`, and `tools: Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-5`.
- algorithmic_behavior: The prompt instructs the agent to quickly explore repository documentation and structure, identify technologies/purpose, compare draft content against repository concepts/files/features, then emit one of two machine-readable verdict prefixes: `RELEVANT:` or `NOT_RELEVANT:` at `agents/draft-relevance-checker.md:14-29`. The behavior is intentionally lenient: informal drafts and ambiguous semantic matches should pass, and doubt resolves toward relevance at `agents/draft-relevance-checker.md:31-36`.
- inputs_outputs_state: Input is draft document content supplied by the `gen-plan` command plus repository files reachable through Read/Glob/Grep. Output is a single verdict line with a brief explanation. It does not mutate repository state. In the parent command, `NOT_RELEVANT` stops the workflow; `RELEVANT` advances to draft analysis, as specified in `commands/gen-plan.md:49-72`.
- gates_or_invariants: The verdict string is a control gate for the `gen-plan` workflow. The invariant is loose semantic relevance rather than exact file/path matching. The output prefix must remain parseable by the invoking command because `commands/gen-plan.md:63-71` branches on those prefixes.
- dependencies_and_callers: Called by `commands/gen-plan.md`, which explicitly invokes `humanize:draft-relevance-checker` with haiku and asks it to inspect README/CLAUDE/main files at `commands/gen-plan.md:55-64`. Covered structurally by `tests/test-gen-plan.sh`, which verifies file existence, frontmatter name, haiku model, tools field, naming convention, required fields, valid model, and English-only content at `tests/test-gen-plan.sh:111-155`, `tests/test-gen-plan.sh:241-244`, `tests/test-gen-plan.sh:289-296`, `tests/test-gen-plan.sh:391-421`.
- edge_cases_or_failure_modes: False negatives are intentionally minimized by leniency. A draft in any language or rough format should still be judged semantically. The largest failure mode is an output that omits the exact verdict prefix, because the parent command’s Phase 2 contract depends on `RELEVANT:` or `NOT_RELEVANT:`.
- validation_or_tests: Static tests in `tests/test-gen-plan.sh` validate metadata and content constraints. No runtime task invocation was executed in this read-only research pass.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-046 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR loop validator allowlist. It locks down which historical/current todo and summary files may bypass general protections in Read, Write, Edit, and Bash validators. The test scope is declared at `tests/test-allowlist-validators.sh:3-10`.
- algorithmic_behavior: The script creates an isolated temporary git repo and active loop directory, writes `.humanize/rlcr/<timestamp>/state.md` with `current_round: 5`, `max_iterations: 42`, `plan_file`, `plan_tracked`, and `start_branch`, then exercises helper and hook behavior through JSON hook inputs at `tests/test-allowlist-validators.sh:29-62`. The canonical allowlist behavior comes from `hooks/lib/loop-common.sh:is_allowlisted_file`, which permits exactly `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`, only when the full path equals `$active_loop_dir/$allowed` at `hooks/lib/loop-common.sh:324-345`.
- inputs_outputs_state: Inputs are synthetic hook JSON payloads containing `tool_name` and either `tool_input.file_path` or `tool_input.command`. Outputs are hook exit codes and stderr text: success is exit `0`; blocked policy violations are expected as exit `2`; malformed input/state failures use exit `1` in the underlying validators. Test state lives under `mktemp -d` and is cleaned by trap at `tests/test-allowlist-validators.sh:29-31`.
- gates_or_invariants: The core invariant is path-exact allowlisting, not basename allowlisting. Tests 1-7 prove helper-level behavior: the four allowed filenames pass only in the active loop directory, while `round-3-todos.md`, `round-2-summary.md`, and same filename in a wrong directory fail at `tests/test-allowlist-validators.sh:64-125`. Write/Edit/Read validators must permit only allowlisted files and block non-allowlisted round files with policy messages at `tests/test-allowlist-validators.sh:127-282`. Bash validation is stricter for todos: it only allows full active-loop paths for `round-1-todos.md` and `round-2-todos.md`, and blocks wrong directory, generic relative filename, old loop directory, and same-basename different root at `tests/test-allowlist-validators.sh:284-381`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-allowlist-validators.sh:15-17`. Calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh`. The helper depends on `find_active_loop`, which selects only the newest active loop directory with `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:147-170`, plus round matching/extraction helpers at `hooks/lib/loop-common.sh:303-345`. The suite is included by `tests/run-all-tests.sh:44`.
- edge_cases_or_failure_modes: The Bash validator specifically addresses basename spoofing and stale-loop bypasses by requiring the active loop directory path to appear in the command for allowed todos writes at `hooks/loop-bash-validator.sh:381-393`. The test also catches an important security edge case: `/tmp/.humanize/rlcr/<same basename>/round-1-todos.md` must fail even if the loop directory basename matches at `tests/test-allowlist-validators.sh:368-381`. A notable implementation asymmetry is that `loop-edit-validator.sh` parses JSON directly with `jq` rather than using `validate_hook_input` like Read/Write/Bash; this test only supplies well-formed JSON, so robustness is covered elsewhere.
- validation_or_tests: Attempted to run `bash tests/test-allowlist-validators.sh`; the managed read-only sandbox blocked temporary directory/cache creation before assertions ran: `mktemp: mkdtemp failed ... Operation not permitted`, followed by git cache errors under `/tmp`. No branch files were modified. Direct inspection confirms the script has 25 assertions and exits with `TESTS_FAILED` at `tests/test-allowlist-validators.sh:383-391`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-076 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: User-facing block template for the PR loop force-push gate. It explains that review state is reset when the tracked PR commit SHA changes in a non-fast-forward manner and tells the user how to restart review. Template variables appear at `prompt-template/block/force-push-detected.md:3` and `prompt-template/block/force-push-detected.md:10-14`.
- algorithmic_behavior: When the PR loop detects that the previous tracked commit is no longer reachable from the current PR head, the stop hook updates state to the new commit, clears stale trigger metadata, renders this template, and returns a JSON block decision. The detection and state transition are in `hooks/pr-loop-stop-hook.sh:380-410`; rendering and blocking are in `hooks/pr-loop-stop-hook.sh:412-421`.
- inputs_outputs_state: Inputs are `OLD_COMMIT`, `NEW_COMMIT`, and `BOT_MENTION_STRING` supplied to `load_and_render_safe` at `hooks/pr-loop-stop-hook.sh:415-417`. Output is markdown included in the hook’s JSON `reason`, with `decision: block` and system message `"PR Loop: Force push detected - please re-trigger bots"` at `hooks/pr-loop-stop-hook.sh:419-420`. State mutation occurs before rendering: `latest_commit_sha` and `latest_commit_at` are replaced, while `last_trigger_at` and `trigger_comment_id` are cleared at `hooks/pr-loop-stop-hook.sh:396-404`.
- gates_or_invariants: Force push is a hard review-cycle reset. The invariant is that stale trigger comments cannot continue a rewritten-history review; users must post a new trigger comment after the new commit timestamp. The template’s required actions preserve that gate by saying tracking has moved to the new SHA and a new bot mention is required at `prompt-template/block/force-push-detected.md:5-17`.
- dependencies_and_callers: Loaded through the shared template loader via `load_and_render_safe`, which provides fallback rendering if the template is missing or empty at `hooks/lib/template-loader.sh:167-203`. The template reference is discoverable by `rg` only in `hooks/pr-loop-stop-hook.sh:415`. `tests/test-template-references.sh` scans shell scripts for template references and verifies referenced template files exist at `tests/test-template-references.sh:51-111`.
- edge_cases_or_failure_modes: If GitHub commit timestamp lookup fails, the hook falls back to current UTC time before updating state at `hooks/pr-loop-stop-hook.sh:388-394`. If the template is missing, the fallback still blocks, but with less detailed instructions at `hooks/pr-loop-stop-hook.sh:412-415`. Placeholder rendering is single-pass, so injected `{{...}}` inside variable values is not recursively expanded, per `hooks/lib/template-loader.sh:50-57`.
- validation_or_tests: Template existence is covered by the template reference scanner. Runtime behavior is tied to PR-loop force-push detection paths; no live GitHub PR interaction was run in this read-only pass.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-106 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt contract appended to the next Claude round prompt when Codex finds issues, instructing Claude to request Goal Tracker changes in a structured markdown section instead of editing the tracker directly after Round 0. The section skeleton is defined at `prompt-template/claude/goal-tracker-update-request.md:2-14`.
- algorithmic_behavior: `hooks/loop-codex-stop-hook.sh` loads this template after composing the next-round footer and optional push note, then appends it to `NEXT_PROMPT_FILE` at `hooks/loop-codex-stop-hook.sh:1108-1113`. The hook then blocks exit and sends the full next prompt as the JSON `reason` for the next round at `hooks/loop-codex-stop-hook.sh:1115-1124`.
- inputs_outputs_state: The template itself has no variable placeholders. Its output is a markdown instruction block with `## Goal Tracker Update Request`, `### Requested Changes`, and `### Justification`. State transition is indirect: Claude can include requested goal-tracker changes in its round summary; Codex later reviews and updates the Goal Tracker if justified. This complements validator gates that block direct goal-tracker modification after Round 0 in Write/Edit/Bash paths.
- gates_or_invariants: The invariant is separation of authority: Claude may request tracker updates, while Codex owns tracker mutation after Round 0. Write/Edit validators block `goal-tracker.md` after current round > 0 and point to the summary file at `hooks/loop-write-validator.sh:188-196` and `hooks/loop-edit-validator.sh:141-149`. Bash modifications are blocked and redirected to either Write/Edit in Round 0 or a summary request after Round 0 at `hooks/loop-bash-validator.sh:343-358`.
- dependencies_and_callers: Caller is `hooks/loop-codex-stop-hook.sh:1108-1113`. Loading uses `load_template`, which returns empty string and warns if missing at `hooks/lib/template-loader.sh:33-48`; the caller has a short fallback sentence at `hooks/loop-codex-stop-hook.sh:1110-1112`. The template reference scanner includes `loop-codex-stop-hook.sh` among scripts checked at `tests/test-template-references.sh:56-64`.
- edge_cases_or_failure_modes: Missing template degrades to a terse fallback, losing the structured requested-changes/justification format but preserving the high-level instruction. Because the template contains examples, a model could copy placeholders literally if it does not replace them with actual requests; downstream Codex review is the intended filter. Direct tracker edits after Round 0 are blocked by validators, so the expected path is summary-based request rather than file mutation.
- validation_or_tests: Template presence is covered by the template reference scanner. Goal-tracker blocking behavior is covered in hook validator suites outside this assigned item; no write-path validation was run due read-only constraints.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 Item Evidence headings present; each assigned id appears only in its heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`