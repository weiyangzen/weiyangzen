# agent_13 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-013 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR review-loop prompt template set for Round 0 startup, PR goal-tracker initialization, work completion requirements, and Codex goal-tracker update instructions. This directory is part of the core PR-loop algorithm because `scripts/setup-pr-loop.sh` renders these templates into `.humanize/pr-loop/<timestamp>/goal-tracker.md` and `round-0-prompt.md`, and `hooks/pr-loop-stop-hook.sh` later injects the Codex goal-tracker update template into review validation prompts.
- algorithmic_behavior: The directory contains seven templates:
  - `goal-tracker-initial.md` initializes review-loop state with PR number, branch, start time, monitored bots, startup case, issue summary row for round 0, zeroed totals, and an initial issue log (`prompt-template/pr-loop/goal-tracker-initial.md:1-33`).
  - `round-0-header.md` establishes the Round 0 PR review prompt frame and PR metadata before fetched comments are appended (`prompt-template/pr-loop/round-0-header.md:1-15`).
  - `round-0-task-no-comments.md` defines startup case behavior when no PR comments exist: do not trigger a review comment, write an initial resolution summary, then rely on the Stop Hook to poll automatic reviews (`prompt-template/pr-loop/round-0-task-no-comments.md:4-29`).
  - `round-0-task-has-comments.md` defines startup behavior when comments already exist: analyze human comments first, then newest bot comments; fix issues; commit, push, comment to trigger re-review; write resolution summary (`prompt-template/pr-loop/round-0-task-has-comments.md:4-43`).
  - `critical-requirements-no-comments.md` repeats the no-comments completion gate: write `{{RESOLVE_PATH}}`, exit, and do not comment (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-20`).
  - `critical-requirements-has-comments.md` repeats the has-comments completion gate: commit, push, trigger bot re-review, and write summary (`prompt-template/pr-loop/critical-requirements-has-comments.md:6-23`).
  - `codex-goal-tracker-update.md` specifies how Codex must mutate the goal tracker after review analysis: append issue-summary rows, update total statistics, and add an issue-log entry for `{{NEXT_ROUND}}` without modifying header sections (`prompt-template/pr-loop/codex-goal-tracker-update.md:1-64`).
- inputs_outputs_state: Inputs are template variables supplied by callers: `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. `scripts/setup-pr-loop.sh` builds goal-tracker variables at lines 578-586, renders `goal-tracker-initial.md` at line 632, renders `round-0-header.md` at line 678, appends fetched comments at lines 680-684, selects Round 0 task templates based on `COMMENT_COUNT` at lines 686-768, and prints critical requirement templates at lines 810-862. Outputs are generated prompt/goal-tracker content under `.humanize/pr-loop/<timestamp>/`, plus user-visible command output from `setup-pr-loop.sh`. The key state transition is Round 0 initialization into either “awaiting automatic reviews” when no comments exist or “fix/comment/re-review” when comments exist.
- gates_or_invariants: The templates enforce that `.humanize/pr-loop/` state files are system-managed and must not be modified by the agent (`round-0-task-no-comments.md:20-22`, `round-0-task-has-comments.md:30-35`). The no-comments path forbids manual PR trigger comments (`round-0-task-no-comments.md:20`, `critical-requirements-no-comments.md:15-18`). The comments-present path requires push and bot mention comment before polling (`round-0-task-has-comments.md:15-21`, `critical-requirements-has-comments.md:8-16`). Goal-tracker updates must preserve file structure, modify only Issue Summary, Total Statistics, and Issue Log, and append rather than replace rows (`codex-goal-tracker-update.md:57-64`).
- dependencies_and_callers: Rendering depends on `hooks/lib/template-loader.sh`, whose `{{VAR}}` replacement is single-pass and keeps missing placeholders literal (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-131`). `load_and_render_safe` provides fallback behavior if a template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`). Primary caller is `scripts/setup-pr-loop.sh` for initialization and Round 0 prompt generation (`scripts/setup-pr-loop.sh:557-862`). `hooks/pr-loop-stop-hook.sh` renders `pr-loop/codex-goal-tracker-update.md` into Codex validation prompts (`hooks/pr-loop-stop-hook.sh:1239-1249`). Sibling/parent coordination is with block templates for validator errors and with Stop Hook polling/review validation; these PR-loop templates define the prompt-side contract while hooks enforce runtime transitions.
- edge_cases_or_failure_modes: Missing or empty templates fall back to embedded fallback strings in `setup-pr-loop.sh`, so startup still produces prompts (`scripts/setup-pr-loop.sh:588-632`, `scripts/setup-pr-loop.sh:660-678`, `scripts/setup-pr-loop.sh:689-720`, `scripts/setup-pr-loop.sh:723-768`, `scripts/setup-pr-loop.sh:813-860`). Comment detection uses a sentinel `*No comments found.*`; any non-sentinel content sets `COMMENT_COUNT=1`, so malformed/nonempty comment files drive the “has comments” path (`scripts/setup-pr-loop.sh:643-649`). Missing template variables remain as `{{VAR}}`, which can leak unresolved placeholders into prompts instead of failing fast (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:119-122`). The no-comments templates assume first bot reviews are automatic; if a configured bot requires explicit trigger, later Stop Hook logic must correct/block.
- validation_or_tests: `tests/run-all-tests.sh` includes PR-loop and template validation suites in its ordered test list, including `test-template-loader.sh`, `test-template-references.sh`, `test-templates-comprehensive.sh`, `test-pr-loop.sh`, and `test-pr-loop-system.sh` (`tests/run-all-tests.sh:33-53`). `tests/test-pr-loop.sh` sources PR-loop script, hook, and Stop Hook test modules and runs all three groups (`tests/test-pr-loop.sh:32-47`). `tests/test-pr-loop-system.sh` covers PR-loop system behavior with mocked `gh`, temp repos, mutual exclusion, and reviewer-status cases (`tests/test-pr-loop-system.sh:1-24`, `tests/test-pr-loop-system.sh:50-70`, `tests/test-pr-loop-system.sh:109-180`). `tests/test-template-references.sh` scans template references and validates missing-template/fallback safety (`tests/test-template-references.sh:51-111`, `tests/test-template-references.sh:169-200`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-043 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-wide executable specification and aggregation gate for Humanize plugin behavior, including template loading, PR loop behavior, gen-plan behavior, hooks, monitor runtime, and robustness tests. It is algorithmically relevant because it defines the canonical suite order and failure policy for validating the branch’s core behavior.
- algorithmic_behavior: The script initializes counters and failure collection (`tests/run-all-tests.sh:28-30`), declares an ordered `TEST_SUITES` array (`tests/run-all-tests.sh:32-65`), declares a zsh-only exception list (`tests/run-all-tests.sh:67-70`), then iterates suites. For each suite it resolves `suite_path`, skips missing files, chooses bash execution or zsh execution, captures combined output and exit code, strips ANSI escape codes, extracts the final `Passed:` and `Failed:` counts, accumulates totals, records suite failures, and prints a final summary (`tests/run-all-tests.sh:72-151`).
- inputs_outputs_state: Inputs are executable test scripts under `tests/` and `tests/robustness/`, plus optional availability of `zsh` for `test-zsh-monitor-safety.sh`. Outputs are terminal status lines, aggregate pass/fail counts, failed suite names, and process exit status. State is in local shell variables: `TOTAL_PASSED`, `TOTAL_FAILED`, `FAILED_SUITES`, per-suite `output`, `exit_code`, `passed`, `failed`, and `needs_zsh`.
- gates_or_invariants: Suite order is fixed, starting with template/validator tests and later covering gen-plan, PR-loop, monitor, and robustness suites (`tests/run-all-tests.sh:33-65`). Missing suites are `SKIP`, not failures (`tests/run-all-tests.sh:75-78`). zsh-required suites are run with `zsh` only when available; missing zsh causes skip (`tests/run-all-tests.sh:89-106`). A suite fails if either its exit code is nonzero or its parsed failed count is greater than zero (`tests/run-all-tests.sh:122-129`). The whole runner exits `1` if any suite is recorded failed, otherwise exits `0` (`tests/run-all-tests.sh:140-151`).
- dependencies_and_callers: It depends on each listed test file being executable because non-zsh tests are invoked directly as `"$suite_path"` (`tests/run-all-tests.sh:107-109`). The file mode check showed `tests/run-all-tests.sh` and key listed suites such as `test-gen-plan.sh`, `test-pr-loop.sh`, `test-pr-loop-system.sh`, `test-template-loader.sh`, `test-template-references.sh`, and `test-templates-comprehensive.sh` are executable. It uses standard shell utilities `sed`, `grep`, `tail`, and optionally `zsh` (`tests/run-all-tests.sh:100-117`). `.github/workflows/template-test.yml` separately runs `test-template-loader.sh` and `test-template-references.sh`, but `run-all-tests.sh` is the local aggregate gate for the broader suite.
- edge_cases_or_failure_modes: Because `set -e` is not enabled globally at startup (`set -uo pipefail` only at line 12), the loop can continue across failed suite commands, with explicit `set +e`/`set -e` around suite execution (`tests/run-all-tests.sh:96-111`). Missing test files and missing zsh can hide coverage gaps by skipping rather than failing (`tests/run-all-tests.sh:75-78`, `tests/run-all-tests.sh:100-105`). Pass/fail aggregation depends on test output containing parseable `Passed:` and `Failed:` lines; suites without those strings contribute `0` counts due `|| echo "0"` (`tests/run-all-tests.sh:113-120`). The ANSI stripping regex is tested separately by `tests/test-ansi-parsing.sh` according to repository references.
- validation_or_tests: This file is itself the validation orchestrator. It explicitly includes the branch-relevant `test-gen-plan.sh` and PR-loop suites (`tests/run-all-tests.sh:51-53`), along with template reference and comprehensive template rendering tests (`tests/run-all-tests.sh:34-40`). I did not execute it because the assignment requested research notes only and the branch export is read-only; several suites create temp repos/files and may assume writable test areas.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-073 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking diagnostic template for the PR-loop Stop Hook when Claude is configured and expected to acknowledge a trigger comment with an `eyes` reaction but does not respond within the retry window.
- algorithmic_behavior: The template renders a human-readable block headed “Claude Bot Not Responding,” parameterized by `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}`, then lists possible causes, required actions, configuration guidance, and the instruction to post a new trigger comment after configuration is fixed (`prompt-template/block/claude-eyes-timeout.md:1-18`). The associated transition is from “trigger comment confirmed” to “blocked until Claude bot configuration/permissions/state are fixed.”
- inputs_outputs_state: Inputs are `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`. In `hooks/pr-loop-stop-hook.sh`, these are hardcoded as `3` and `15` after `check-bot-reactions.sh` performs `--retry 3 --delay 5` (`hooks/pr-loop-stop-hook.sh:764-775`). Output is assigned to `REASON` and emitted in a JSON decision object with `"decision": "block"` and system message “PR Loop: Claude bot not responding - check bot configuration” (`hooks/pr-loop-stop-hook.sh:774-779`).
- gates_or_invariants: This gate only runs when Claude is among configured bots and a trigger is required (`hooks/pr-loop-stop-hook.sh:749-757`). It checks the confirmed trigger comment id, avoiding stale trigger ids (`hooks/pr-loop-stop-hook.sh:742-759`). Empty or `null` reaction output is blocking (`hooks/pr-loop-stop-hook.sh:768-779`). If Claude is configured but trigger is not required, the hook explicitly skips eyes verification (`hooks/pr-loop-stop-hook.sh:787-789`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/claude-eyes-timeout.md"` (`hooks/pr-loop-stop-hook.sh:774-775`). It depends on `scripts/check-bot-reactions.sh` returning an eyes reaction value for `claude-eyes` against the trigger comment id (`hooks/pr-loop-stop-hook.sh:764-767`) and on `hooks/lib/template-loader.sh` safe rendering/fallback behavior (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If the trigger exists but no comment id is available, the hook logs a warning instead of rendering this block (`hooks/pr-loop-stop-hook.sh:783-786`). If the template is missing or empty, the fallback message still blocks with a shorter configuration warning (`hooks/pr-loop-stop-hook.sh:770-775`). The rendered template assumes installation/permission/state problems; transient Claude downtime is included as a possible cause (`prompt-template/block/claude-eyes-timeout.md:5-9`). Retry timing is not dynamic beyond the two variables supplied by the caller.
- validation_or_tests: Template reference integrity is covered by repository-wide template reference scanning, which verifies referenced templates exist and critical renderers use safe fallback behavior (`tests/test-template-references.sh:51-111`, `tests/test-template-references.sh:169-200`). The Stop Hook test suite is included by the aggregate runner via `test-pr-loop.sh` and `test-pr-loop-system.sh` (`tests/run-all-tests.sh:52-53`, `tests/test-pr-loop.sh:32-47`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-103 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Hard validation error template used by loop write/edit validators when an agent attempts to write or edit a round-scoped summary file whose embedded round number does not match the active loop round.
- algorithmic_behavior: The template tells the agent it is trying to `{{ACTION}}` `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md` while the current round is `{{CURRENT_ROUND}}`, provides `{{CORRECT_PATH}}`, and explicitly says not to increment the round number manually (`prompt-template/block/wrong-round-number.md:1-7`). The associated algorithmic transition is validator rejection with corrective path guidance.
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. `hooks/loop-write-validator.sh` supplies `ACTION=write to`, `FILE_TYPE=summary`, and exits `2` after rendering on mismatch (`hooks/loop-write-validator.sh:228-244`). `hooks/loop-edit-validator.sh` supplies `ACTION=edit`, `FILE_TYPE=summary`, and also exits `2` on mismatch (`hooks/loop-edit-validator.sh:162-178`). Output is written to stderr as the blocking validator message.
- gates_or_invariants: The core invariant is that summary file round numbers must equal `STATE_CURRENT_ROUND`; agents cannot create/edit future or stale round summaries unless the path is allowlisted (`hooks/loop-write-validator.sh:228-244`, `hooks/loop-edit-validator.sh:162-178`). The validators compute the correct destination as `round-${CURRENT_ROUND}-summary.md` under the active loop directory (`hooks/loop-write-validator.sh:231-233`, `hooks/loop-edit-validator.sh:165-167`). The template reinforces that the loop system, not the agent, advances rounds (`prompt-template/block/wrong-round-number.md:7`).
- dependencies_and_callers: Direct callers are `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` via `load_and_render_safe` (`hooks/loop-write-validator.sh:238-243`, `hooks/loop-edit-validator.sh:172-177`). Both depend on common helpers such as `extract_round_number` and `is_allowlisted_file` from the loop libraries. Rendering behavior depends on `hooks/lib/template-loader.sh` single-pass placeholder substitution and safe fallback (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If no round number can be extracted, the mismatch block is not reached (`hooks/loop-write-validator.sh:228-231`, `hooks/loop-edit-validator.sh:162-165`). Allowlisted files bypass the block even if a round-like filename mismatches (`hooks/loop-write-validator.sh:231`, `hooks/loop-edit-validator.sh:165`). If the template is missing, each validator has an inline fallback with the same essential data (`hooks/loop-write-validator.sh:233-238`, `hooks/loop-edit-validator.sh:167-172`). Missing template variables would remain visible as placeholders due loader behavior, but the current callers pass all required variables.
- validation_or_tests: `tests/test-template-loader.sh` directly renders this real template and asserts the heading, target filename, and current-round text are substituted (`tests/test-template-loader.sh:147-163`). `tests/test-templates-comprehensive.sh` also performs a real-template integration check requiring `Wrong Round Number`, the stale round filename, current round, and correct path (`tests/test-templates-comprehensive.sh:493-510`). `tests/test-template-references.sh` validates template references and safe rendering for critical validators (`tests/test-template-references.sh:51-111`, `tests/test-template-references.sh:169-200`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 evidence sections present above, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`