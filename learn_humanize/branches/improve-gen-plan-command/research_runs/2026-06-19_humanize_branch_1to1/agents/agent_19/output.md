# agent_19 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-019 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` slash-command workflow. It is a prompt-level algorithm for transforming a user draft into a structured implementation plan with goals, AC-format acceptance criteria, path boundaries, feasibility hints, dependencies, and code-style constraints. Frontmatter declares the entry metadata, argument hint, and allowed tools at `commands/gen-plan.md:1-12`.
- algorithmic_behavior: The command is a six-phase pipeline: IO validation, repository relevance check, draft analysis, issue resolution, plan generation, and output write/reporting at `commands/gen-plan.md:19-26`. Phase 1 runs `scripts/validate-gen-plan-io.sh` with raw command arguments and branches on exit codes 0-6 at `commands/gen-plan.md:30-46`. Phase 2 reads the draft and invokes `humanize:draft-relevance-checker` with the `haiku` model, accepting only a `RELEVANT:` verdict and stopping on `NOT_RELEVANT:` at `commands/gen-plan.md:49-72`. Phase 3 asks the agent to explore related code and analyze clarity, consistency, completeness, and functionality at `commands/gen-plan.md:75-107`. Phase 4 requires user clarification without discarding original draft content, and explicitly confirms whether quantitative metrics are hard requirements or optimization directions at `commands/gen-plan.md:110-138`. Phase 5 specifies the generated plan schema and generation rules at `commands/gen-plan.md:141-247`. Phase 6 writes the file and reports path, summary, and AC count at `commands/gen-plan.md:250-255`.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, a required nonempty draft file, a new output path, repository contents for relevance/code exploration, and user answers from `AskUserQuestion`. Output is a new plan file. Intermediate state is mostly conversational: relevance verdict, analysis issues, clarified decisions, and hard-vs-soft metric classifications. The IO validator normalizes input/output paths with `realpath -m`, rejects missing/empty input, nonexistent output directories, existing output paths, and unwritable directories at `scripts/validate-gen-plan-io.sh:68-123`.
- gates_or_invariants: Hard gates are validator exit code handling, relevance verdict, resolved/acknowledged issues, and complete incorporation of draft content. The generated plan must use AC-X or AC-X.Y criteria, positive and negative tests for each AC, milestone/phase/step terminology only, no time estimates, path-only code references, affirmative upper/lower bounds, and a code-style note banning plan-specific progress terminology in implementation code/comments at `commands/gen-plan.md:224-247`.
- dependencies_and_callers: Depends directly on `scripts/validate-gen-plan-io.sh` and `agents/draft-relevance-checker.md`. README documents user-facing usage at `README.md:70-72` and `README.md:110-119`. `tests/test-gen-plan.sh` validates command existence, frontmatter, allowed tools, argument hint, relevance agent metadata/model/tools, content restrictions, and IO-validator exit behavior at `tests/test-gen-plan.sh:55-180` and `tests/test-gen-plan.sh:426-541`.
- edge_cases_or_failure_modes: Stops on missing input, empty input, nonexistent output directory, existing output path, unwritable output directory, invalid arguments, unrelated draft, unresolved missing information, and inability to generate a complete plan. A subtle behavior is that `--help` in `validate-gen-plan-io.sh` exits 6 because `usage()` exits 6 at `scripts/validate-gen-plan-io.sh:15-23`, and tests expect that at `tests/test-gen-plan.sh:536-538`.
- validation_or_tests: `tests/test-gen-plan.sh` is the executable structural/spec test. It also creates invalid fixtures for missing frontmatter and malformed YAML, validates model naming, checks English-only content, and exercises validator positive/negative IO cases at `tests/test-gen-plan.sh:217-541`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-049 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancel-signal state transition. It proves that an active RLCR loop may transition from `state.md` or `finalize-state.md` to `cancel-state.md` only through the authorized cancellation path, and that ordinary Bash-driven state-file tampering remains blocked.
- algorithmic_behavior: The test creates a synthetic `.humanize/rlcr/<timestamp>/state.md`, sets `CLAUDE_PROJECT_DIR`, sends simulated Bash hook JSON into `hooks/loop-bash-validator.sh`, and asserts exit code 0 for authorized cancel commands and exit code 2 for blocked tampering at `tests/test-cancel-signal-file.sh:42-72`. The authorization algorithm under test lives in `is_cancel_authorized`: it requires `.cancel-requested`, rejects command substitution/backticks/newlines/chaining operators, normalizes `$loop_dir`/`${loop_dir}`, rejects remaining `$`, requires the command to start with `mv`, parses exactly two quoted or unquoted args, requires source to be active `state.md` or `finalize-state.md`, requires destination to be active `cancel-state.md`, and rejects symlink sources at `hooks/lib/loop-common.sh:437-614`.
- inputs_outputs_state: Inputs are candidate Bash command strings plus active loop filesystem state. The expected allowed transition is `touch "$LOOP_DIR/.cancel-requested"` followed by `mv <active-state> "$LOOP_DIR/cancel-state.md"`; the real cancel script performs exactly that at `scripts/cancel-rlcr-loop.sh:134-138`. Output is validator exit status and diagnostic text. The hook state remains read-only unless the command is later executed by the tool after validation.
- gates_or_invariants: The key invariant is “signal file plus exact command shape,” not merely signal file presence. Positive coverage allows canonical absolute paths, `/./` path normalization, single-quoted paths, and literal `${loop_dir}` syntax at `tests/test-cancel-signal-file.sh:78-113`, `tests/test-cancel-signal-file.sh:363-378`, and `tests/test-cancel-signal-file.sh:448-484`. Negative coverage blocks missing/wrong-directory signal files, writes via `echo`, `sed -i`, `rm`, wrong destination, arbitrary destination, extra args, options/prefixes, hidden variables, source path outside active loop, and inactive-loop behavior at `tests/test-cancel-signal-file.sh:119-235`, `tests/test-cancel-signal-file.sh:281-355`, and `tests/test-cancel-signal-file.sh:1206-1247`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` directly and exercises `hooks/loop-bash-validator.sh`. The validator first finds active loops, strictly parses state, blocks git push when needed, and then applies RLCR state-file protection with the cancel exception at `hooks/loop-bash-validator.sh:54-85` and `hooks/loop-bash-validator.sh:125-156`. The source-side bypass checks split shell segments and strip redirection prefixes before matching `mv|cp ... state.md` at `hooks/loop-bash-validator.sh:158-304`.
- edge_cases_or_failure_modes: The test is mostly edge cases: `&&`, `;`, `||`, pipes, background `&`, `|&`, `sh -c`/`bash -c`, redirection prefixes including `&>`, `&>>`, quoted redirection targets, escaped spaces, and ANSI-C quoting at `tests/test-cancel-signal-file.sh:241-355` and `tests/test-cancel-signal-file.sh:646-1204`. It also verifies helper false cases for no signal, wrong command, three args, extra arg before source, and hidden variables at `tests/test-cancel-signal-file.sh:1257-1341`. One notable invariant: if no active loop state file exists, the Bash validator allows the command because loop protection is inactive at `tests/test-cancel-signal-file.sh:1227-1247` and `hooks/loop-bash-validator.sh:64-67`.
- validation_or_tests: This file is itself the validation suite. It exits with the number of failed assertions at `tests/test-cancel-signal-file.sh:1347-1355`, so CI can treat any bypass regression as nonzero.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-079 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block template used as an explanatory sub-message when RLCR stop-hook git-clean validation detects untracked non-`.humanize` files. It does not decide the gate itself, but shapes the remediation contract presented to Claude/user when the gate blocks exit.
- algorithmic_behavior: The template tells the agent that untracked files may be artifacts and should usually be ignored rather than committed, listing build outputs, dependencies, IDE files, logs, caches, and temporary files at `prompt-template/block/git-not-clean-untracked.md:2-10`. The stop hook loads it only when `git status` contains `??` entries outside `.humanize*`, appends it to `SPECIAL_NOTES`, and renders the overall `block/git-not-clean.md` reason at `hooks/loop-codex-stop-hook.sh:437-475`.
- inputs_outputs_state: Input is the cached git status classification in `GIT_STATUS_CACHED`; the relevant branch is `OTHER_UNTRACKED=$(echo "$UNTRACKED" | grep -v '\.humanize' || true)` at `hooks/loop-codex-stop-hook.sh:453-456`. Output is extra Markdown embedded inside a blocking JSON response with `"decision": "block"` and a system message requiring commit/cleanup before exit at `hooks/loop-codex-stop-hook.sh:477-484`.
- gates_or_invariants: Gate invariant is that uncommitted changes block the loop before expensive Codex review and before exit. This template adds a secondary invariant: do not blindly commit generated/unwanted files; inspect them and add appropriate `.gitignore` patterns instead. If the template is missing, the hook falls back to “Review untracked files - add to .gitignore or commit them.” at `hooks/loop-codex-stop-hook.sh:456-459`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh`; loaded through `hooks/lib/template-loader.sh` via `load_template`. `tests/test-template-references.sh` scans hook template references and fails missing referenced templates at `tests/test-template-references.sh:83-111`. `tests/test-templates-comprehensive.sh` loads every template and validates syntax at `tests/test-templates-comprehensive.sh:83-194`.
- edge_cases_or_failure_modes: Misclassification risk is simple pattern-based filtering: any untracked path containing `.humanize` is treated as local loop metadata and excluded from this specific note, while all other untracked files trigger it. The template has no placeholders, so rendering cannot fail from missing variables.
- validation_or_tests: Covered by template existence/reference checks and load/syntax checks in `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-109 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt block template appended to Claude’s next-round instructions immediately after a Full Goal Alignment Check. It changes the next implementation round’s priorities toward items discovered by Codex alignment review.
- algorithmic_behavior: The block instructs Claude to pay attention to forgotten items, NOT MET acceptance criteria, and unjustified deferrals at `prompt-template/claude/post-alignment-action-items.md:2-7`. The stop hook appends it to `round-${NEXT_ROUND}-prompt.md` when `CURRENT_ROUND % 5 == 4`, i.e. the next prompt follows the periodic alignment checkpoint at `hooks/loop-codex-stop-hook.sh:1085-1090`. The alignment review prompt itself is selected when `FULL_ALIGNMENT_CHECK` is true at `hooks/loop-codex-stop-hook.sh:721-735`.
- inputs_outputs_state: Input is the loop round number and the presence/content of this template. Output is extra Markdown appended to the next-round prompt after the rendered `claude/next-round-prompt.md` and before footer/push instructions at `hooks/loop-codex-stop-hook.sh:1071-1097`. It does not mutate checklist state directly; it influences the next Claude work cycle after Codex review found issues.
- gates_or_invariants: Invariant is priority continuity after alignment: forgotten work, failed ACs, and unjustified deferrals must be addressed rather than lost when the loop advances. If the template is absent or empty, the hook silently skips the append because it only writes when `POST_ALIGNMENT` is nonempty at `hooks/loop-codex-stop-hook.sh:1087-1090`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` via `load_template`. Template reference validation checks it exists because it is directly referenced at `tests/test-template-references.sh:83-111`; comprehensive template tests load and syntax-check it with the rest of `prompt-template/` at `tests/test-templates-comprehensive.sh:83-194`.
- edge_cases_or_failure_modes: The modulo condition means this block appears only after rounds numbered 4, 9, 14, etc. There is no placeholder rendering, so the only failure mode is missing/empty template content, which results in no post-alignment section.
- validation_or_tests: Covered by template reference and comprehensive template loading/syntax suites. Behavioral coverage is indirect through hook tests that exercise next-round prompt generation rather than this tiny static block specifically.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`