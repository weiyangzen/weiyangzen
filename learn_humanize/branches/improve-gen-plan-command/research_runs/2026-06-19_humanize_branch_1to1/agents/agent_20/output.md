# agent_20 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-020 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the PR review loop. It is a thin command surface that exposes `/humanize:start-pr-loop`, constrains the allowed tool to `scripts/setup-pr-loop.sh`, and delegates all executable behavior to that setup script.
- algorithmic_behavior: The command declares the user-facing argument contract at `commands/start-pr-loop.md:3`, allows only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)` at `commands/start-pr-loop.md:4`, and invokes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh" $ARGUMENTS` at `commands/start-pr-loop.md:12-14`. The described loop algorithm is: detect current branch PR, fetch human/bot review comments, have the agent fix findings, push and trigger bot re-review, let the Stop Hook poll every 30 seconds with a 15-minute timeout, and use local Codex validation for remote review outcomes (`commands/start-pr-loop.md:16-24`). The command also defines prioritization: human comments before bot comments, then newest bot comments first (`commands/start-pr-loop.md:31-35`).
- inputs_outputs_state: Inputs are slash-command arguments `--claude`, `--codex`, `--max N`, `--codex-model MODEL:EFFORT`, and `--codex-timeout SECONDS` (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). Outputs are not direct file writes from this markdown file; the invoked setup script creates `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-pr-resolve.md`, and `round-0-prompt.md` as shown in `scripts/setup-pr-loop.sh:394-412`, `scripts/setup-pr-loop.sh:535-555`, `scripts/setup-pr-loop.sh:561-633`, and `scripts/setup-pr-loop.sh:641-681`. State transitions are delegated to the PR Stop Hook after setup: round initialization, summary requirement, re-review trigger, polling, validation, and either continuation or completion.
- gates_or_invariants: Requires at least one bot flag (`commands/start-pr-loop.md:25-30`; implemented in `scripts/setup-pr-loop.sh:179-190`). The command’s rules enforce write summary, push changes, tag correct bots, do not edit state/cancel files to bypass the loop, and trust Stop Hook orchestration (`commands/start-pr-loop.md:50-57`). Termination gates are maximum iteration count, all monitored bots approve, or explicit `/humanize:cancel-pr-loop` (`commands/start-pr-loop.md:58-62`).
- dependencies_and_callers: Primary dependency is `scripts/setup-pr-loop.sh`, called through `CLAUDE_PLUGIN_ROOT` (`commands/start-pr-loop.md:12-14`). The setup script depends on `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:33-42`, `scripts/setup-pr-loop.sh:210-214`), plus external `git`, `gh`, `codex`, `jq`, and helper scripts such as `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:243-277`, `scripts/setup-pr-loop.sh:411-419`, `scripts/setup-pr-loop.sh:505-508`). The command is documented in `README.md:88` and `README.md:129-132`.
- edge_cases_or_failure_modes: The delegated setup script fails closed for missing bot flags, existing RLCR/PR loops, non-git repos, no commits, missing/unauthenticated GitHub CLI, missing Codex, no PR for the branch, merged/closed PRs, YAML-unsafe branch/model/effort values, inability to post trigger comments, and missing Claude eyes reaction when required (`scripts/setup-pr-loop.sh:179-190`, `scripts/setup-pr-loop.sh:222-277`, `scripts/setup-pr-loop.sh:326-388`, `scripts/setup-pr-loop.sh:443-520`). Fork PRs are explicitly handled by resolving current, parent, lookup, and base repos before comment/review API calls (`scripts/setup-pr-loop.sh:290-359`).
- validation_or_tests: Command behavior is indirectly covered by script tests, including `tests/test-pr-loop-scripts.sh` references to `start-pr-loop`. I did not run PR-loop setup because it requires live `gh` authentication, a PR branch, network/API access, and mutable `.humanize/pr-loop` state.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-050 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable negative-path specification for `hooks/lib/template-loader.sh`. It validates that missing templates/directories, empty content, malformed placeholders, strict shell mode, safe fallback rendering, and template directory validation behave predictably instead of crashing command/Stop Hook flows.
- algorithmic_behavior: The test sources the template loader from the repository root (`tests/test-error-scenarios.sh:11-15`), tracks pass/fail counters (`tests/test-error-scenarios.sh:22-35`), then executes 12 independent scenarios. It checks `load_template` for missing file and missing directory returning empty output with exit 0 (`tests/test-error-scenarios.sh:42-65`); `load_and_render` missing-template behavior (`tests/test-error-scenarios.sh:67-78`); `render_template` empty input, special-character replacement, empty placeholder, and unclosed placeholder behavior (`tests/test-error-scenarios.sh:80-106`, `tests/test-error-scenarios.sh:182-206`); strict-mode survival for missing templates (`tests/test-error-scenarios.sh:108-131`); `load_and_render_safe` fallback and fallback variable substitution (`tests/test-error-scenarios.sh:133-158`); and `validate_template_dir` accept/reject behavior (`tests/test-error-scenarios.sh:160-180`). It exits 0 only when `TESTS_FAILED` is 0 (`tests/test-error-scenarios.sh:218-226`).
- inputs_outputs_state: Inputs are the local project root, resolved template directory, test template strings, fallback strings, and variable assignments such as `PATH=...`, `FILE=test.md`, `VAR=value` (`tests/test-error-scenarios.sh:11-16`, `tests/test-error-scenarios.sh:98-101`, `tests/test-error-scenarios.sh:138-153`). Outputs are terminal PASS/FAIL lines and the process exit code. State is only in shell variables `TESTS_PASSED` and `TESTS_FAILED`; no persistent repo state is intentionally modified.
- gates_or_invariants: The file encodes resilience invariants: missing templates are non-fatal and produce empty output for unsafe loader calls; safe loader calls use fallback content; fallback content still receives variable substitution; strict mode should not abort on missing templates; valid template directories must contain the expected template tree; invalid directories must return nonzero; placeholders without a valid replacement or closing delimiter must stay literal. These invariants correspond to `template-loader.sh` behavior: `load_template` returns empty with warning on missing file (`hooks/lib/template-loader.sh:36-47`), `load_and_render_safe` falls back on missing/empty content (`hooks/lib/template-loader.sh:170-203`), and `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:208-221`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh` (`tests/test-error-scenarios.sh:13`). The tested functions are called throughout command and hook algorithms, including `scripts/setup-pr-loop.sh` for PR-loop prompt generation (`scripts/setup-pr-loop.sh:632`, `scripts/setup-pr-loop.sh:678`, `scripts/setup-pr-loop.sh:720`, `scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:834`, `scripts/setup-pr-loop.sh:860`), `hooks/loop-codex-stop-hook.sh` for Stop Hook blocks and next-round prompts, and PR loop Stop Hook block rendering. Related positive/broader coverage exists in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-template-references.sh`, and `tests/robustness/test-template-stress-robustness.sh`.
- edge_cases_or_failure_modes: The test explicitly covers nonexistent paths, empty template content, shell strict mode, regex-like replacement values, empty placeholder `{{}}`, and unclosed placeholder `{{UNCLOSED`. A notable environmental failure surfaced in this read-only branch export: `render_template` uses a here-string into `awk` at `hooks/lib/template-loader.sh:74-129`, and bash attempted to create a temp file for that here document, which failed with `Operation not permitted`. Because several tests do not check command stderr or distinguish environment failure from empty output, the empty-content case still passed while substitution/literal-preservation cases failed.
- validation_or_tests: Ran `bash tests/test-error-scenarios.sh` in the assigned read-only export. Result: exit 1, 8 passed and 4 failed. Failures were Test 5 special regex characters, Test 8 fallback variable substitution, Test 11 empty placeholder, and Test 12 unclosed placeholder. Each failure was preceded by `hooks/lib/template-loader.sh: line 129: cannot create temp file for here document: Operation not permitted`, indicating the read-only sandbox blocked the loader’s here-string/temp-file mechanism rather than necessarily proving the algorithm is wrong in a writable shell environment.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-080 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop Hook block template for the git-cleanliness gate. It defines the agent-facing transition when a loop tries to stop while the repository has uncommitted tracked or untracked changes.
- algorithmic_behavior: The template renders a blocking prompt headed “Git Not Clean” (`prompt-template/block/git-not-clean.md:1-4`) with two variables: `{{GIT_ISSUES}}` for the detected condition and `{{SPECIAL_NOTES}}` for contextual notes about untracked `.humanize*` or other artifacts. It then enumerates required actions: optionally run `code-simplifier` if installed, review untracked files and ignore build artifacts, stage real changes, and commit with a descriptive message (`prompt-template/block/git-not-clean.md:5-10`). It adds commit authorship constraints forbidding AI attribution trailers (`prompt-template/block/git-not-clean.md:11-15`) and only permits exit retry after all changes are committed (`prompt-template/block/git-not-clean.md:16`).
- inputs_outputs_state: Inputs are injected by `load_and_render_safe`: `GIT_ISSUES` and `SPECIAL_NOTES`. In the RLCR/Codex Stop Hook, `GIT_ISSUES` becomes `uncommitted changes` when cached git status is non-empty; `SPECIAL_NOTES` accumulates optional template content for `.humanize*` and other untracked-file cases (`hooks/loop-codex-stop-hook.sh:432-461`). Output is JSON hook decision content where `decision` is `block`, `reason` is this rendered template, and `systemMessage` summarizes the git issue (`hooks/loop-codex-stop-hook.sh:473-485`). The state transition is “attempted stop” to “blocked, commit required”; no state file is updated by this template itself.
- gates_or_invariants: The invariant is that loop exit is forbidden while git status has uncommitted changes (`hooks/loop-codex-stop-hook.sh:464-486`). It also enforces commit hygiene at the prompt contract level: real changes should be staged, build artifacts should be ignored, commit messages should follow project conventions, and AI authorship must not be included (`prompt-template/block/git-not-clean.md:7-15`). The gate is separate from the unpushed-commits gate that follows when `PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:488-518`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` (`hooks/loop-codex-stop-hook.sh:473-475`). It depends on `hooks/lib/template-loader.sh` for single-pass placeholder rendering (`hooks/lib/template-loader.sh:58-132`) and on earlier Stop Hook git-status caching. It is also discoverable in tests/reference scans as a template used by template-loader examples and reference checks.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the Stop Hook uses an inline fallback message (`hooks/loop-codex-stop-hook.sh:467-472`). If `SPECIAL_NOTES` is empty, the rendered template leaves no extra contextual notes beyond the main required actions. If untracked `.humanize*` files are the only special case, a local note is appended from `block/git-not-clean-humanize-local.md` or fallback text; other untracked files get a separate review note (`hooks/loop-codex-stop-hook.sh:441-461`). The template is not responsible for detecting git state itself; stale or incorrect cached status upstream would determine whether it appears.
- validation_or_tests: Covered indirectly by template reference tests and loader tests. The assigned `tests/test-error-scenarios.sh` validates missing-template and safe fallback behavior for the loader path that would render this template. I did not execute the full Stop Hook because that would require a configured active loop and mutable hook state.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-110 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Minimal next-round prompt fragment that enforces the `push_every_round` contract in RLCR/Codex loop continuation prompts.
- algorithmic_behavior: The template contains a single instruction: when `--push-every-round` is enabled, the agent must push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:1-2`). It is appended to the next-round prompt only if `PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:1099-1106`). This makes push behavior part of the generated task prompt before the later Stop Hook gate checks for unpushed commits.
- inputs_outputs_state: Input is the Stop Hook boolean/config variable `PUSH_EVERY_ROUND`; the template itself has no placeholders. Output is appended text in `round-${NEXT_ROUND}-prompt.md` after the next-round prompt and footer are written (`hooks/loop-codex-stop-hook.sh:1067-1106`). State transition is advisory-to-enforced: the prompt instructs pushing, and the Stop Hook later blocks exit if the branch is ahead of remote when `PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:492-517`).
- gates_or_invariants: The invariant is that every round’s commits must be pushed when the loop is configured for push-every-round. If this template is missing, the hook falls back to “Also push your changes after committing.” (`hooks/loop-codex-stop-hook.sh:1101-1104`). The enforcement gate is not this template but the subsequent git-ahead check that emits a blocking unpushed-commits decision.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which loads it with `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"` and appends it to the next prompt (`hooks/loop-codex-stop-hook.sh:1099-1105`). It depends on the template directory resolution and loader in `hooks/lib/template-loader.sh`, plus the loop’s state/config reader that sets `PUSH_EVERY_ROUND`.
- edge_cases_or_failure_modes: Missing template is non-fatal because the hook has a fallback (`hooks/loop-codex-stop-hook.sh:1101-1104`). The template does not specify remote name, branch, or push command, so the operational details are left to the agent and downstream git gate. If `PUSH_EVERY_ROUND` is false, the note is not appended, and the unpushed-commit gate is skipped in the RLCR Stop Hook path.
- validation_or_tests: Template presence and loader behavior are indirectly covered by template reference tests. The assigned error-scenario test confirms safe/missing-template behavior in the loader family, though it failed in this read-only environment for render cases due shell temp-file restrictions. I did not execute a full loop round because it requires mutable `.humanize` state and active loop context.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`