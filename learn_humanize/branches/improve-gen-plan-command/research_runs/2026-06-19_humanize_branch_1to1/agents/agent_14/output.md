# agent_14 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-014 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell utility directory for monitor/status algorithms used by `scripts/humanize.sh` and PR/RLCR loop tests. Recursive inspection found one descendant: `scripts/lib/monitor-common.sh`.
- algorithmic_behavior: `monitor-common.sh` supplies cross-platform monitor primitives: ANSI color emitters at `scripts/lib/monitor-common.sh:14`, file sizing via Linux/macOS `stat` fallback at `:32`, latest timestamped session selection at `:40`, terminal scroll-region setup/restore at `:71` and `:86`, state-file classification at `:154`, YAML frontmatter value extraction at `:197`, display timestamp/string formatting at `:218` and `:233`, PR-loop phase detection at `:267`, PR-loop phase display text at `:346`, RLCR goal tracker summary parsing at `:390`, and PR goal tracker issue summary parsing at `:468`.
- inputs_outputs_state: Inputs are filesystem paths, markdown state/tracker files, PR check files, and optional status strings. Outputs are pipe-delimited status summaries, raw ANSI escape codes, display labels, session paths, or scalar values. The only persistent state observed is the PR phase file-size cache under `/tmp/humanize-phase-<session>-<round>.size`, written in `get_pr_loop_phase` at `scripts/lib/monitor-common.sh:299-307`.
- gates_or_invariants: Timestamped session directories must match `YYYY-MM-DD_HH-MM-SS` before selection at `scripts/lib/monitor-common.sh:54-60`. Active state wins over terminal `*-state.md` files at `:162-187`. PR loop phase detection prioritizes final states before active-analysis heuristics at `:272-319`. Goal tracker parsing assumes specific markdown section headings and table row formats at `:397-462`.
- dependencies_and_callers: `scripts/humanize.sh` conditionally sources this library at `scripts/humanize.sh:6-10`, wraps `monitor_find_latest_session` at `:237-240`, wraps `monitor_find_state_file` at `:303-306`, parses tracker output for the status bar at `:359-368`, and wraps `monitor_get_file_size` at `:569-572`. Tests source it directly in `tests/test-pr-loop-system.sh:769-895` and elsewhere for PR phase and monitor behavior.
- edge_cases_or_failure_modes: Missing loop/session directories return empty path or `|unknown` rather than failing at `scripts/lib/monitor-common.sh:44-47` and `:157-160`. `monitor_get_file_size` returns `0` for missing files at `:34`. `monitor_get_yaml_value` is simple grep/sed frontmatter extraction and does not parse nested YAML or duplicate keys at `:203-208`. PR phase detection can report `codex_analyzing` on recent mtime even without size growth at `:310-317`; stale `/tmp` cache collisions are possible because cache identity is based on session basename and round at `:297-307`.
- validation_or_tests: Shared monitor tests cover latest-session selection, state-file detection, missing file size fallback, and PR phase transitions in `tests/test-pr-loop-system.sh:769-895`. Additional direct references in `tests/test-pr-loop-system.sh:256-632` validate PR phase display, goal tracker parsing, and PR goal tracker parsing.
- skip_candidate: `yes: this directory is monitor/support infrastructure rather than the improve-gen-plan command’s primary generation algorithm, but it participates in loop state/status algorithms and is covered because it contains an included core descendant.`

### IMPROVE_GEN_PLAN_COMMAND-HZ-044 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Executable test fixture generator that installs a mock `gh` binary for deterministic PR-loop comment/review algorithms.
- algorithmic_behavior: The script requires `<mock_bin_dir> <fixtures_dir>` at `tests/setup-fixture-mock-gh.sh:16-22`, creates the mock binary directory at `:24`, writes a fixture-backed `gh` shell script at `:27-100`, marks it executable at `:102`, and prints the mock directory path at `:104`. The generated mock routes `gh auth status`, `gh repo view`, `gh pr view`, and `gh api` calls to stable JSON responses or fixture files.
- inputs_outputs_state: Inputs are the target mock binary directory and fixture directory. Outputs are an executable `gh` at `$MOCK_BIN_DIR/gh`, stdout containing `$MOCK_BIN_DIR`, and fixture-backed JSON responses during later tests. It reads `issue_comments.json`, `review_comments.json`, and `pr_reviews.json` from the provided fixture directory at `tests/setup-fixture-mock-gh.sh:75-89`.
- gates_or_invariants: Argument presence is mandatory; missing args print usage and exit `1` at `tests/setup-fixture-mock-gh.sh:19-22`. Unknown mock commands print an error and exit `1` at `:98-99`. The mock assumes fixture filenames use underscore names, matching `tests/fixtures/issue_comments.json`, `review_comments.json`, and `pr_reviews.json`.
- dependencies_and_callers: Fixture-backed tests call this setup helper in `tests/test-pr-loop-hooks.sh:1231-1239`, then prepend the returned directory to `PATH` before running `scripts/fetch-pr-comments.sh` at `:1249-1251` and `scripts/poll-pr-reviews.sh` at `:1329-1334`. Those scripts resolve repo/PR base information with `gh repo view` and `gh pr view` before fetching issue comments, review comments, and reviews at `scripts/fetch-pr-comments.sh:130-219` and `scripts/poll-pr-reviews.sh:126-240`.
- edge_cases_or_failure_modes: The mock only implements the subset of `gh` used by fixture tests; additional flags or endpoints fall through to `[]` for unrecognized `gh api` calls at `tests/setup-fixture-mock-gh.sh:92-94` or fail as an unhandled command at `:98-99`. It emits raw JSON and does not implement real `--jq` / `-q` behavior, so tests rely on the consuming scripts tolerating those fixture outputs in their current form.
- validation_or_tests: `tests/test-pr-loop-hooks.sh:1242-1419` validates that the fixture-backed mock lets `fetch-pr-comments.sh` return issue comments, inline review comments, approval-only review placeholders, and `--after` filtering; it also validates that `poll-pr-reviews.sh` returns valid JSON, includes approval-only review placeholders, records responding bots, and filters earlier reviews.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-074 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Prompt/block template for communicating a failed Codex review attempt, including exit code, missing review result path, debug artifact paths, and stderr tail.
- algorithmic_behavior: The template defines a markdown failure contract with placeholders for `{{CODEX_EXIT_CODE}}`, `{{REVIEW_RESULT_FILE}}`, `{{CODEX_CMD_FILE}}`, `{{CODEX_STDOUT_FILE}}`, `{{CODEX_STDERR_FILE}}`, and `{{STDERR_CONTENT}}` at `prompt-template/block/codex-review-failed.md:5-15`. It ends with retry guidance at `:18`.
- inputs_outputs_state: Inputs are template variables supplied by hook code or template tests. Output is rendered markdown used as a blocking reason or operator/debug message. It has no internal state and no executable behavior.
- gates_or_invariants: It assumes the review result file was not created, explicitly labeling `{{REVIEW_RESULT_FILE}}` as “not created” at `prompt-template/block/codex-review-failed.md:6`. It also assumes stderr content is already bounded before substitution; the template text says “last 50 lines” at `:13`.
- dependencies_and_callers: Template rendering is handled by `hooks/lib/template-loader.sh`, where placeholders are single-pass substituted from `VAR=value` inputs at `hooks/lib/template-loader.sh:50-131`, with missing variables preserved at `:119-121`. Search found no direct `load_and_render_safe "$TEMPLATE_DIR" "block/codex-review-failed.md"` call. Related stop hooks currently inline equivalent failure text: RLCR `codex_failure_exit` builds `# Codex Review Failed` at `hooks/loop-codex-stop-hook.sh:855-880` and checks nonzero exit, missing file, and empty file at `:883-955`; PR loop stop hook inlines a simpler Codex failure block at `hooks/pr-loop-stop-hook.sh:1322-1331`.
- edge_cases_or_failure_modes: Because it appears unreferenced by loader calls, changes to this file may not affect runtime unless a caller is added. If rendered without all variables, unresolved placeholders remain by template-loader design. The template’s “last 50 lines” wording differs from current RLCR hook behavior, which tails 30 lines at `hooks/loop-codex-stop-hook.sh:887`, `:924`, and `:929`.
- validation_or_tests: Template infrastructure is covered by `tests/test-template-loader.sh:146-164` and `tests/test-templates-comprehensive.sh:493-514` using other real templates, while reference checks scan template loader usage in `tests/test-template-references.sh` per `rg` results. I found no direct test asserting this specific block renders or is referenced.
- skip_candidate: `yes: it is a failure-contract template, but currently looks inactive/unwired because runtime hooks duplicate the failure text inline instead of loading this file.`

### IMPROVE_GEN_PLAN_COMMAND-HZ-104 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Hook block template that enforces the invariant that RLCR round summary files must be written inside the active loop directory.
- algorithmic_behavior: The template emits a short markdown block with title, invariant statement, and `{{CORRECT_PATH}}` substitution at `prompt-template/block/wrong-summary-location.md:1-5`. `hooks/loop-write-validator.sh` loads it when a write targets a summary file outside `.humanize/rlcr` at `hooks/loop-write-validator.sh:202-209`, then exits `2` to block the tool action.
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` at `hooks/loop-write-validator.sh:203`. Output is rendered stderr content returned to the blocked agent. It carries no persistent state.
- gates_or_invariants: The gate triggers only when `IS_SUMMARY_FILE=true` and `IN_HUMANIZE_LOOP_DIR=false` at `hooks/loop-write-validator.sh:202`. It does not validate the round number itself; that is a later gate using `wrong-round-number.md` at `:228-244`. If the target is inside `.humanize/rlcr` but in the wrong session directory, the validator later uses `wrong-directory-path.md` at `:252-262`.
- dependencies_and_callers: Depends on hook path classification variables from `loop-write-validator.sh` and on `load_and_render_safe` fallback semantics from `hooks/lib/template-loader.sh:170-203`. The template loader preserves missing placeholders rather than failing hard at `hooks/lib/template-loader.sh:119-121`.
- edge_cases_or_failure_modes: If this template is missing or empty, `loop-write-validator.sh` supplies an inline fallback message at `hooks/loop-write-validator.sh:204-208`. If `ACTIVE_LOOP_DIR` or `CURRENT_ROUND` is wrong upstream, the rendered correct path will be wrong as well. Bash/edit validators have separate handling, so this block specifically covers write-tool summary path mistakes.
- validation_or_tests: Generic real-template rendering with `CORRECT_PATH` is covered in `tests/test-template-loader.sh:151-164` and `tests/test-templates-comprehensive.sh:497-514` using adjacent path templates. Template reference tests inspect `load_and_render_safe` usage, and `rg` confirms this block’s direct runtime caller at `hooks/loop-write-validator.sh:207`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique assigned item sections above; item identifiers are present only in their section headings.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`