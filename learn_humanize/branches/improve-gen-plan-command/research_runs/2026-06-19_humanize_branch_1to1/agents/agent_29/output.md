# agent_29 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-029 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` validator for RLCR and PR-loop protected files. It is registered as the `Write` hook in `hooks/hooks.json:14-21` and acts as a write gate for loop-owned state, prompt, todo, summary, plan-backup, and PR-loop files.
- algorithmic_behavior: Reads hook JSON from stdin, validates input through shared helpers, exits early for non-`Write` tools, then classifies `tool_input.file_path` by controlled filename and directory patterns (`hooks/loop-write-validator.sh:25-49`). It blocks `round-N-todos.md` unless the path is explicitly allowlisted in the active RLCR loop, blocks all `round-N-prompt.md` writes, handles PR-loop protections before RLCR state parsing, then enforces RLCR active-loop and current-round rules (`hooks/loop-write-validator.sh:55-94`, `100-126`, `228-263`). It allows ordinary paths outside `.humanize/rlcr/` and non-protected incidental files inside `.humanize/rlcr/` (`hooks/loop-write-validator.sh:104-117`).
- inputs_outputs_state: Inputs are JSON stdin fields `tool_name` and `tool_input.file_path`, environment `CLAUDE_PROJECT_DIR`, active loop directories under `.humanize/rlcr/` and `.humanize/pr-loop/`, and YAML frontmatter in `state.md` or `finalize-state.md`. Outputs are process exit status plus stderr block messages. Exit `0` allows the write, exit `1` rejects malformed input/state, and exit `2` blocks a policy violation. The validator does not mutate loop state; it reads current state and prevents unauthorized state transitions.
- gates_or_invariants: JSON must be valid, UTF-8-safe, include `tool_name`, and must not exceed depth 30 via `validate_hook_input` and `is_deeply_nested` (`hooks/loop-write-validator.sh:27-45`; helpers at `hooks/lib/loop-common.sh:55-129`). `file_path` is required for `Write` (`hooks/loop-write-validator.sh:43-48`). RLCR `state.md` and `finalize-state.md` writes are always blocked once an active loop is found (`hooks/loop-write-validator.sh:152-160`). `finalize-summary.md` is allowed only in the active loop directory (`hooks/loop-write-validator.sh:168-173`). `plan.md` inside `.humanize/rlcr/` is treated as a protected plan backup and blocked (`hooks/loop-write-validator.sh:179-185`). `goal-tracker.md` is blocked after round 0, with the user redirected to the current summary (`hooks/loop-write-validator.sh:192-195`). Summary writes must be in the active loop directory and must match `current_round` unless allowlisted (`hooks/loop-write-validator.sh:202-209`, `228-245`, `252-263`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-write-validator.sh:17-19`), which supplies path classifiers, strict state parsing, active-loop discovery, template-backed block messages, PR-loop classifiers, and PR resolve round validation (`hooks/lib/loop-common.sh:232-345`, `687-810`, `1002-1014`). Uses `load_and_render_safe` through the shared template loader, which preserves fallbacks if templates are missing (`hooks/lib/template-loader.sh:167-203`). Template dependencies include `prompt-template/block/plan-backup-protected.md`, `wrong-summary-location.md`, `wrong-round-number.md`, `wrong-directory-path.md`, PR-loop block templates, and state/goal/todos block templates. Direct tests cover PR-loop write protection in `tests/test-pr-loop-hooks.sh:68-166`, wrong-round `pr-resolve` gates in `tests/test-pr-loop-hooks.sh:1431-1494`, plan backup blocking in `tests/test-plan-file-hooks.sh:194-205`, allowlist behavior in `tests/test-allowlist-validators.sh:133-183`, finalize-phase behavior in `tests/test-finalize-phase.sh:286-307`, and robust JSON handling in `tests/robustness/test-hook-input-robustness.sh:47-57`.
- edge_cases_or_failure_modes: Malformed RLCR state fails closed with “Malformed state file” and exit `1` (`hooks/loop-write-validator.sh:140-144`). No active RLCR loop allows most RLCR-specific checks to pass, but `round-N-todos.md` and `round-N-prompt.md` are still blocked early (`hooks/loop-write-validator.sh:55-68`, `128-130`). PR-loop protection is independent of RLCR active-loop existence, as covered by `tests/test-pr-loop-hooks.sh:174-210`. PR-loop `round-N-pr-resolve.md` is the only writeable PR-loop round file, and only for the active PR round (`hooks/loop-write-validator.sh:89-93`; helper `hooks/lib/loop-common.sh:741-790`). Exact path equality is used for final active-loop directory validation, so normalized or alternate paths that classify as loop files but do not equal the active canonical path are blocked by the wrong-directory gate (`hooks/loop-write-validator.sh:252-263`).
- validation_or_tests: The assigned executable tests exercise state-file blocking, read-only comment blocking, allowed resolve summaries, Bash protection without RLCR, and wrong-round resolve handling (`tests/test-pr-loop-hooks.sh:68-210`, `1431-1535`). Separate validator tests verify allowlisted historical files, finalize summary/state rules, plan backup protection, and input robustness (`tests/test-allowlist-validators.sh:133-183`; `tests/test-finalize-phase.sh:286-307`; `tests/test-plan-file-hooks.sh:194-249`; `tests/robustness/test-hook-input-robustness.sh:47-57`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-059 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop hook behavior. It defines `run_hook_tests()` and is sourced by the main PR-loop test runner after script tests and before stop-hook tests (`tests/test-pr-loop.sh:32-47`). It covers validator policy, comment ordering, gatekeeper parsing, stop-hook integration behavior, fixture-backed fetch/poll scripts, wrong-round checks, and monitor display.
- algorithmic_behavior: The file is organized into sequential test sections inside `run_hook_tests()` (`tests/test-pr-loop-hooks.sh:15-18`, `212-270`, `468-476`, `756-764`, `1150-1158`, `1221-1229`, `1421-1429`, `1537-1545`). Each section creates temporary `.humanize/pr-loop/<timestamp>/state.md` fixtures, mock `gh`/`git` commands, or JSON payloads, invokes production hooks/scripts, and asserts expected exits or parsed outputs through `pass`/`fail`. It is not production code, but it pins core algorithm contracts.
- inputs_outputs_state: Inputs are test globals `SCRIPT_DIR`, `PROJECT_ROOT`, `TEST_DIR`, mock `PATH`, temporary Git repos, mocked GitHub API responses, fixture files, and generated PR-loop `state.md` frontmatter. Outputs are pass/fail test events through the shared helper framework. Test-created loop states include `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, `started_at`, and `last_trigger_at` (`tests/test-pr-loop-hooks.sh:39-55`, `771-789`, `850-868`, `1060-1077`, `1440-1446`). Some E2E tests invoke `pr-loop-stop-hook.sh`, which can update loop state during execution; this file mostly validates observable stderr and generated files rather than mutating state itself.
- gates_or_invariants: PR-loop state is YAML-list based for bots (`tests/test-pr-loop-hooks.sh:26-63`, `288-324`). `state.md` writes are blocked, generated PR comment files are blocked, and current-round `pr-resolve` writes are allowed (`tests/test-pr-loop-hooks.sh:68-166`). Bash protection must work even when no RLCR loop exists (`tests/test-pr-loop-hooks.sh:174-210`). Comments are sorted with human comments before bot comments and timestamps newest first (`tests/test-pr-loop-hooks.sh:222-259`). Gatekeeper parsing deduplicates comments by ID, extracts `ISSUES` rows from Codex output, re-adds previously approved bots when new issues appear, detects trigger comments, and recognizes final markers `APPROVE` and `WAITING_FOR_BOTS` (`tests/test-pr-loop-hooks.sh:271-466`). Stop-hook exit is blocked without a resolve file, trigger timestamps are detected from current-user comments, pagination must find triggers on later pages, and round 0 must prefer `last_trigger_at` over `started_at` when present (`tests/test-pr-loop-hooks.sh:766-1148`). Approval-only reviews must be represented with `[Review state: APPROVED]` placeholders and match bot polling patterns (`tests/test-pr-loop-hooks.sh:1160-1219`). Fetch and poll scripts must honor `--after` filters and include approval-only reviews (`tests/test-pr-loop-hooks.sh:1242-1419`). Wrong-round `pr-resolve` write/edit attempts are blocked while correct-round writes are allowed (`tests/test-pr-loop-hooks.sh:1431-1535`). Monitor output must parse YAML bot lists and display empty active bots as `none` (`tests/test-pr-loop-hooks.sh:1547-1621`).
- dependencies_and_callers: Called by `tests/test-pr-loop.sh`, which sources `tests/test-pr-loop-lib.sh` and this file (`tests/test-pr-loop.sh:18-34`). Depends on `tests/test-helpers.sh` for `pass`, `fail`, and temp setup via the shared lib (`tests/test-pr-loop-lib.sh:18-21`, `110-124`). Invokes production hooks `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/pr-loop-stop-hook.sh` (`tests/test-pr-loop-hooks.sh:90`, `123`, `156`, `198`, `833`, `1520`). Invokes production scripts `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, fixture mock setup, monitor env setup, and `scripts/humanize.sh` (`tests/test-pr-loop-hooks.sh:1231-1252`, `1329-1334`, `1552-1558`). Requires `jq`, shell, mock `gh`, mock `git`, and temporary filesystem state.
- edge_cases_or_failure_modes: The tests encode several edge cases: empty or missing trigger timestamps block later rounds (`tests/test-pr-loop-hooks.sh:606-622`), round 0 falls back to `started_at` only when no trigger exists (`tests/test-pr-loop-hooks.sh:624-665`), per-bot timeout is anchored to the trigger timestamp rather than poll-start time (`tests/test-pr-loop-hooks.sh:667-696`), paginated GitHub API output must be combined (`tests/test-pr-loop-hooks.sh:947-1047`), empty-body PR reviews must not be dropped (`tests/test-pr-loop-hooks.sh:1160-1195`), and older comments before `--after` must be excluded (`tests/test-pr-loop-hooks.sh:1287-1319`, `1371-1412`). The tests also guard historical regression risk where a `pr-resolve` file from an older round would otherwise be accepted (`tests/test-pr-loop-hooks.sh:1431-1527`).
- validation_or_tests: This file is itself validation. It is included in the PR-loop test runner and contributes to the broader suite invoked by `tests/run-all-tests.sh` through `tests/test-pr-loop.sh` (`tests/run-all-tests.sh:52-53`; `tests/test-pr-loop.sh:40-47`). Its direct validation surface includes actual hook invocations, mocked stop-hook execution, fixture-backed script execution, pure `jq` unit checks, and monitor command checks.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-089 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Blocking-message template for attempts to modify `.humanize/rlcr/.../plan.md`, the copied backup of the original plan file. It is part of the RLCR write/edit/Bash gate contract rather than an algorithm executor.
- algorithmic_behavior: Contains static Markdown explaining that the loop-directory `plan.md` is a backup, exists to preserve plan integrity, and should be read rather than modified (`prompt-template/block/plan-backup-protected.md:1-7`). The template has no placeholders, so rendering is deterministic.
- inputs_outputs_state: Inputs are only the template loader call from a validator. Output is Markdown written to stderr by the validator when a plan backup modification is blocked. It does not read or write loop state.
- gates_or_invariants: The invariant is that RLCR’s backup copy of the plan is immutable during the loop. `loop-write-validator.sh` blocks `Write` to `plan.md` under `.humanize/rlcr/` and renders this template with a fallback if loading fails (`hooks/loop-write-validator.sh:111-115`, `179-184`). The same template is referenced by edit and Bash validators for equivalent protection (`hooks/loop-edit-validator.sh:134-135`; `hooks/loop-bash-validator.sh:337-338`).
- dependencies_and_callers: Loaded through `load_and_render_safe`, whose fallback behavior prevents missing templates from disabling the block (`hooks/lib/template-loader.sh:167-203`). Direct callers are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh` as located by template references. The template directory is resolved by `hooks/lib/template-loader.sh:24-31`.
- edge_cases_or_failure_modes: Because the template is static, missing variable substitution cannot corrupt it. If the file is missing or empty, validators still emit their inline fallback and block the operation. The production write validator’s detection relies on basename `plan.md` and path containment `/.humanize/rlcr/`, so the protection is specific to the loop backup path rather than arbitrary repository plan files (`hooks/loop-write-validator.sh:111-115`, `179-185`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` verifies that `Write`, `Edit`, Bash redirection, and Bash `rm` attempts against loop `plan.md` are blocked with plan-related output (`tests/test-plan-file-hooks.sh:194-249`). Template reference coverage also scans `loop-write-validator.sh` as a critical template user (`tests/test-template-references.sh:56-63`, `174-180`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-119 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop round-0 task prompt fragment used when initial PR comments already exist. It instructs the agent how to transition from fetched review feedback into code changes, push, re-review trigger, and resolution-summary production.
- algorithmic_behavior: The template tells the agent to analyze comments with human comments first and bot comments newest first, fix valid reviewer issues, add tests when needed, commit and push, post a bot-triggering PR comment, and write a resolution summary to `@{{RESOLVE_PATH}}` (`prompt-template/pr-loop/round-0-task-has-comments.md:4-27`). It then states loop rules: do not modify `.humanize/pr-loop/` state files, always push, use the configured bot mention format, and be thorough (`prompt-template/pr-loop/round-0-task-has-comments.md:30-35`). The final note defines the Stop Hook continuation contract: poll every 30 seconds up to 15 minutes per bot, let local Codex validate bot review approval, continue on remaining issues, end when all bots approve (`prompt-template/pr-loop/round-0-task-has-comments.md:39-43`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`. `scripts/setup-pr-loop.sh` sets `RESOLVE_PATH="$LOOP_DIR/round-0-pr-resolve.md"` and builds the variable list at `scripts/setup-pr-loop.sh:641-658`. Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the generated header and fetched comments (`scripts/setup-pr-loop.sh:677-684`, `767-771`). The template itself has no direct state mutation, but it instructs the agent to create the `round-0-pr-resolve.md` summary that the stop hook requires before exit.
- gates_or_invariants: Selected only when `fetch-pr-comments.sh` did not produce the sentinel `*No comments found.*`; otherwise the no-comments round-0 task template is used (`scripts/setup-pr-loop.sh:643-649`, `686-720`). The PR re-review command must mention all active/configured bots via `{{BOT_MENTION_STRING}}` and use the current `{{PR_NUMBER}}` (`prompt-template/pr-loop/round-0-task-has-comments.md:18-21`). State files remain system-managed; this aligns with `loop-write-validator.sh` PR-loop state blocking and read-only file blocking (`hooks/loop-write-validator.sh:74-87`). The required output file aligns with the only writeable PR-loop round file type, `round-N-pr-resolve.md`, subject to current-round validation (`hooks/loop-write-validator.sh:89-93`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-task-has-comments.md"` when comments exist (`scripts/setup-pr-loop.sh:721-768`). It is paired with `prompt-template/pr-loop/round-0-header.md` and the fetched comment file to form `round-0-prompt.md` (`scripts/setup-pr-loop.sh:677-684`). Template rendering uses the single-pass loader semantics, leaving missing placeholders intact rather than recursively substituting values (`hooks/lib/template-loader.sh:50-132`). Related prompt variants include `round-0-task-no-comments.md` and `critical-requirements-has-comments.md`.
- edge_cases_or_failure_modes: If the comments file contains anything other than the exact no-comments sentinel, setup treats it as “has comments,” even if content is malformed or only non-review text (`scripts/setup-pr-loop.sh:643-649`). If template loading fails, setup uses an inline fallback with materially the same task text (`scripts/setup-pr-loop.sh:723-767`). If placeholder variables are missing, the template loader preserves unresolved `{{...}}` tokens, which can degrade the generated prompt but avoids accidental placeholder injection (`hooks/lib/template-loader.sh:11-13`, `54-57`, `116-122`).
- validation_or_tests: The PR-loop hook tests validate the downstream contracts this prompt asks the agent to satisfy: resolve summary required before stop-hook exit (`tests/test-pr-loop-hooks.sh:766-840`), trigger comments and timestamp detection after bot mentions (`tests/test-pr-loop-hooks.sh:390-413`, `538-604`, `845-1148`), and current-round `pr-resolve` write allowance/blocking (`tests/test-pr-loop-hooks.sh:135-166`, `1431-1494`). Setup-script tests and template-reference tests cover broader PR-loop setup and template availability, though this exact template is primarily validated through the setup path and downstream hook behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `IMPROVE_GEN_PLAN_COMMAND-HZ-029`, `IMPROVE_GEN_PLAN_COMMAND-HZ-059`, `IMPROVE_GEN_PLAN_COMMAND-HZ-089`, `IMPROVE_GEN_PLAN_COMMAND-HZ-119`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`