# agent_04 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: `hooks/` is the Claude Code enforcement and loop-transition layer for RLCR and PR-review loops. `hooks/hooks.json:1-69` wires validators into `UserPromptSubmit`, `PreToolUse` for `Write`/`Edit`/`Read`/`Bash`, and `Stop` hooks for both RLCR and PR loop stop flows.
- algorithmic_behavior: The directory splits behavior into path/command validators, shared parsing/template utilities, transcript todo checking, and stop-hook state machines. `loop-read-validator.sh:25-170` validates JSON hook input, blocks stale/wrong-location RLCR round reads, and blocks most `round-*-todos.md` reads. `loop-write-validator.sh:25-265` protects todos, prompts, state files, plan backups, post-round-0 goal tracker edits, and PR loop generated files. `loop-edit-validator.sh:24-183` mirrors write protections for edits. `loop-bash-validator.sh:24-445` detects shell-level bypasses such as redirects, `tee`, in-place edits, `mv`/`cp`, shell wrappers, protected `git add`, and PR-loop generated-file modification. `loop-plan-file-validator.sh:29-231` is the prompt-submit guard for schema, branch, and plan-file integrity. `loop-codex-stop-hook.sh:59-1128` is the RLCR stop state machine. `pr-loop-stop-hook.sh:65-1623` is the PR review loop state machine.
- inputs_outputs_state: Inputs are Claude hook JSON on stdin, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `.humanize/pr-loop/<timestamp>/state.md`, Git state, GitHub CLI output, transcripts, prompt templates, and local Codex output. Outputs are hook decisions as JSON blocks, stderr block messages, generated review/prompt/comment/check/feedback files under `.humanize/...`, and state-file renames such as `complete-state.md`, `maxiter-state.md`, `stop-state.md`, `approve-state.md`, `merged-state.md`, `closed-state.md`, and `usage-limit-state.md`. Shared state parsing lives in `hooks/lib/loop-common.sh:202-296` for RLCR and `hooks/pr-loop-stop-hook.sh:87-174` for PR loops.
- gates_or_invariants: Input guards reject invalid JSON, missing required fields, deeply nested JSON, null bytes, and invalid UTF-8 in most shell validators via `hooks/lib/loop-common.sh:55-129`. RLCR validators require current round/file location consistency via `is_round_file_type`, `extract_round_number`, and allowlists at `hooks/lib/loop-common.sh:303-345`. State and finalize-state files are system-managed and blocked for Write/Edit/Bash except an authorized cancel `mv`, whose signal-file, path, quoting, injection, and symlink checks are in `hooks/lib/loop-common.sh:437-614`. `.humanize` is protected from broad/forced `git add` by `hooks/lib/loop-common.sh:812-970`. PR loop generated files are read-only except current `round-N-pr-resolve.md`, validated by `hooks/lib/loop-common.sh:702-790`.
- dependencies_and_callers: Hook registration is in `hooks/hooks.json:4-64`. All shell hooks source `hooks/lib/loop-common.sh`; template rendering is in `hooks/lib/template-loader.sh:24-222`, with single-pass placeholder substitution at `hooks/lib/template-loader.sh:50-132`. Stop hooks depend on `scripts/portable-timeout.sh`, `git`, `jq`, `gh`, `codex`, `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, and `scripts/check-pr-reviewer-status.sh`. The RLCR stop hook calls `hooks/check-todos-from-transcript.py:20-130` before expensive Codex review. PR stop hook calls `scripts/check-pr-reviewer-status.sh` when recomputing startup case at `hooks/pr-loop-stop-hook.sh:1531-1543`.
- edge_cases_or_failure_modes: Fail-closed paths include malformed state, Git failures/timeouts, missing summaries, dirty Git worktree, unpushed commits, missing Codex, Codex failure/empty output, outdated schema, branch switch, plan backup mismatch, large modified files, incomplete TodoWrite tasks, stale PR trigger comments, force-push history rewrite, PR closed/merged, bot polling timeouts, bot usage limits, and stale trigger reaction checks. Notable nuance: `loop-codex-stop-hook.sh:88-91` allows exit on malformed RLCR state “for safety,” while validators like `loop-read-validator.sh:94-98` block operation for malformed state.
- validation_or_tests: Covered by `tests/test-pr-loop.sh:32-47` through PR loop script, hook, and stop-hook modules; direct validator examples appear in `tests/test-pr-loop-hooks.sh:68-210`. Stop-hook integration coverage includes force-push trigger rejection and case-specific trigger behavior in `tests/test-pr-loop-stophook.sh:20-171`. Goal tracker update/parsing behavior is separately stressed by `tests/robustness/test-goal-tracker-robustness.sh`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-034 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop classifier. It determines startup case 1-5 from configured bot reviewers, existing PR comments/reviews, and whether the latest commit is newer than each bot’s latest review.
- algorithmic_behavior: Parses `<pr_number> --bots <bot1,bot2>` at `scripts/check-pr-reviewer-status.sh:49-85`, maps bot names to GitHub authors at `scripts/check-pr-reviewer-status.sh:91-100`, resolves fork/base repo context at `scripts/check-pr-reviewer-status.sh:113-137`, fetches latest commit metadata at `scripts/check-pr-reviewer-status.sh:139-148`, then combines issue comments, inline review comments, and PR review submissions at `scripts/check-pr-reviewer-status.sh:150-165`. It walks each configured bot, records commented/missing/stale arrays, and computes newest review timestamp at `scripts/check-pr-reviewer-status.sh:171-202`.
- inputs_outputs_state: Inputs are CLI args, `gh repo view`, `gh pr view`, `gh api` comment/review endpoints, configured bot list, and `jq`. Output is JSON from `scripts/check-pr-reviewer-status.sh:256-272` with `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews`. It does not persist state directly; callers consume the case and write loop state.
- gates_or_invariants: Requires exactly one PR number and `--bots`; invalid options, missing args, or multiple PR numbers exit nonzero at `scripts/check-pr-reviewer-status.sh:49-85`. Commit info fetch failure is fatal at `scripts/check-pr-reviewer-status.sh:139-145`; comment/review endpoint failures degrade to empty arrays at `scripts/check-pr-reviewer-status.sh:153-162`. Staleness is per-bot, comparing `LATEST_COMMIT_AT > BOT_NEWEST`, not only global newest review time, at `scripts/check-pr-reviewer-status.sh:186-197`.
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` at `scripts/check-pr-reviewer-status.sh:38-40`; depends on `gh` and `jq`. `hooks/pr-loop-stop-hook.sh:1531-1543` calls it after local Codex adjudication to refresh `startup_case` in PR loop state.
- edge_cases_or_failure_modes: Fork PRs are handled by trying current repo, then parent repo, then falling back to current repo with warning at `scripts/check-pr-reviewer-status.sh:113-137`. A bot with no comments is missing; a bot with comments older than the latest commit is stale. Case 5 is used when only some reviewers commented and at least one commenter is stale at `scripts/check-pr-reviewer-status.sh:220-230`; case 4 is all reviewers commented but stale at `scripts/check-pr-reviewer-status.sh:231-241`.
- validation_or_tests: PR stop-hook tests mock this script’s dependencies and explicitly note its transformed `gh --jq` needs in `tests/test-pr-loop-stophook.sh:1192-1290`; `tests/test-pr-loop.sh:32-47` includes the PR loop modules that exercise startup-case flows.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-064 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Top-level executable specification for PR loop behavior. It is not the implementation itself; it orchestrates all PR loop script, hook, and stop-hook test modules.
- algorithmic_behavior: Establishes `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared helpers, initializes mock PR-loop environment, sources module files, runs three module suites, and prints the shared summary. The control flow is linear at `tests/test-pr-loop.sh:15-53`: `init_pr_loop_test_env`, source modules, then `run_script_tests`, `run_hook_tests`, `run_stophook_tests`, `print_test_summary`.
- inputs_outputs_state: Inputs are the repository tree, shell runtime, `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, and the three module scripts. `init_pr_loop_test_env` creates a temp workspace and mock `gh`/`codex` commands in `tests/test-pr-loop-lib.sh:110-124`. Output is pass/fail summary from `tests/test-pr-loop-lib.sh:130-147`; exit status is nonzero if any module increments failures.
- gates_or_invariants: `set -euo pipefail` at `tests/test-pr-loop.sh:13` makes module/source failures fatal. The runner assumes all tests share one temp environment and counters. It requires all three suites to be sourced before invocation, so missing module files fail early.
- dependencies_and_callers: Pulls `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` at `tests/test-pr-loop.sh:32-34`. Script tests cover setup/cancel/fetch/poll argument behavior in `tests/test-pr-loop-scripts.sh:18-410`. Hook tests cover validator protection and fixture behavior in `tests/test-pr-loop-hooks.sh:15-220` and later fixture sections. Stop-hook tests cover force-push, trigger, timeout, state, and dynamic startup-case behavior in `tests/test-pr-loop-stophook.sh:15-180` and later sections.
- edge_cases_or_failure_modes: A failing module stops later commands only where unhandled nonzero exits escape the module; otherwise shared `fail` counters drive summary failure. Because mocks are installed into `PATH`, test behavior is sensitive to mock ordering and `CLAUDE_PROJECT_DIR` cleanup in module tests.
- validation_or_tests: This file is itself validation orchestration; representative covered areas include setup argument parsing at `tests/test-pr-loop-scripts.sh:27-165`, PR state/read-only validator protections at `tests/test-pr-loop-hooks.sh:68-210`, and stop-hook trigger filtering at `tests/test-pr-loop-stophook.sh:20-171`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-094 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: User-facing block template for outdated RLCR state schema. It is a small but core gate contract because validators render it when active loop state lacks required schema fields.
- algorithmic_behavior: Defines the block title, missing-field placeholder, remediation options, and unexpected-termination warning in `prompt-template/block/schema-outdated.md:1-12`. The only variable is `{{FIELD_NAME}}` at line 3, rendered by the template loader’s single-pass substitution rules in `hooks/lib/template-loader.sh:50-132`.
- inputs_outputs_state: Input is `FIELD_NAME` from hook validation. Output is markdown text embedded in a JSON hook block. `hooks/loop-plan-file-validator.sh:59-77` loads this template through `load_and_render_safe`, JSON-escapes it, and returns `{"decision":"block"}` when `plan_tracked` or `start_branch` is missing at `hooks/loop-plan-file-validator.sh:79-88`.
- gates_or_invariants: The template enforces the invariant that v1.1.2+ RLCR loops must have `plan_tracked` and `start_branch`; absence means the loop is not trusted to continue. The prompt-submit validator checks those fields before branch and plan-file integrity checks at `hooks/loop-plan-file-validator.sh:55-89`.
- dependencies_and_callers: Direct caller is `hooks/loop-plan-file-validator.sh:60-65`. A similar inline schema block exists in `hooks/loop-codex-stop-hook.sh:129-146`, but the assigned template is specifically consumed by the UserPromptSubmit validator.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline message at `hooks/loop-plan-file-validator.sh:62-65`. If the placeholder is not provided, the template loader intentionally leaves it unchanged rather than recursively expanding unknown content.
- validation_or_tests: Template presence and references are indirectly covered by template tests listed under `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and comprehensive template tests in the repository listing; the behavioral gate is exercised through `loop-plan-file-validator.sh`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-124 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the production `humanize_parse_goal_tracker` parser used by the humanize loop monitor/status surface.
- algorithmic_behavior: Sources `tests/test-helpers.sh` and production `scripts/humanize.sh`, creates a temp test directory, defines `parse_result`, then writes fixture goal trackers and asserts parser fields. Setup and parser contract are at `tests/robustness/test-goal-tracker-robustness.sh:15-51`; the tested production parser returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` from `scripts/humanize.sh:30-112`.
- inputs_outputs_state: Inputs are generated markdown files under `$TEST_DIR`, including list/table ACs, task tables, completed/deferred/open-issue sections, missing/empty/malformed/truncated/binary files, and long goal summaries. Outputs are pass/fail counters and final exit from `print_test_summary` at `tests/robustness/test-goal-tracker-robustness.sh:526-531`.
- gates_or_invariants: The parser must count list and table AC formats, exclude completed/deferred rows from active tasks, count completed rows and unique completed ACs, count deferred/open issue tables, return defaults for missing files, tolerate empty/malformed/truncated/binary content, and truncate goal summary to at most 60 chars. Those invariants are asserted across tests 1-18 at `tests/robustness/test-goal-tracker-robustness.sh:60-524`.
- dependencies_and_callers: Depends on production `scripts/humanize.sh:32-112`, especially regex sections for AC count at `scripts/humanize.sh:48-54`, active task count at `scripts/humanize.sh:56-84`, completed/deferred/open issue row count at `scripts/humanize.sh:86-103`, and goal summary extraction at `scripts/humanize.sh:104-111`. The parser is consumed by the monitor status bar via `_parse_goal_tracker` and `goal_parts` at `scripts/humanize.sh:326-368`.
- edge_cases_or_failure_modes: The test intentionally covers non-existent file default `No goal tracker`, empty file zeros, 60 AC items, shell-special characters, missing section headings, incomplete markdown, embedded null/binary bytes, header-only sections, mixed bold AC formatting, and long summary truncation. One noted implementation boundary is that decimal AC IDs such as `AC-3.1` are expected not to be counted as normal ACs in `tests/robustness/test-goal-tracker-robustness.sh:477-499`.
- validation_or_tests: This file is a validation entrypoint; it should be run as `tests/robustness/test-goal-tracker-robustness.sh` in a writable test environment. I did not execute it in this read-only branch export because it creates temp fixture files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_GEN_PLAN_COMMAND-HZ-004, IMPROVE_GEN_PLAN_COMMAND-HZ-034, IMPROVE_GEN_PLAN_COMMAND-HZ-064, IMPROVE_GEN_PLAN_COMMAND-HZ-094, IMPROVE_GEN_PLAN_COMMAND-HZ-124
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`