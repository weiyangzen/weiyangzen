# agent_07 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification suite for the Humanize plugin’s loop orchestration, hook validators, PR-review loop, monitor behavior, template loading, plan-file validation, and the branch-relevant `gen-plan` command. The directory is core algorithm evidence because it encodes expected state transitions and safety gates around RLCR/PR loops and verifies the new `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, and `scripts/validate-gen-plan-io.sh` surfaces.
- algorithmic_behavior: `tests/run-all-tests.sh` is the aggregate runner and defines the ordered suite list at lines 32-65, with `test-zsh-monitor-safety.sh` dispatched under zsh at lines 67-110. `tests/test-gen-plan.sh` validates command/agent frontmatter, command structure, model naming, English-only content, and `validate-gen-plan-io.sh` exit-code contracts at lines 55-177 and 425-541. `tests/test-plan-file-validation.sh` drives `scripts/setup-rlcr-loop.sh` through path, git, tracking, content, CLI, and Codex-parameter gates at lines 89-741. `tests/test-pr-loop.sh` composes PR loop modules, while `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-stophook.sh`, and `tests/test-pr-loop-system.sh` validate setup/cancel/fetch/poll scripts, stop-hook state transitions, trigger detection, bot timeout/re-add logic, review sorting/deduplication, and monitor output. `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh` specify template lookup/render/fallback behavior. `tests/test-allowlist-validators.sh`, `test-bash-validator-patterns.sh`, `test-plan-file-hooks.sh`, `test-cancel-signal-file.sh`, `test-finalize-phase.sh`, and `test-state-exit-naming.sh` specify hook protection and loop-state rename behavior. `tests/robustness/*` expands the same production functions across malformed input, timeout, path, state, session, git, cancel-security, and large-file cases.
- inputs_outputs_state: Inputs are shell/JSON fixtures, temporary git repositories, hook JSON payloads, transcript JSONL, PR review/comment/reaction fixture JSON, generated loop directories, and production scripts sourced from `hooks/` and `scripts/`. Outputs are pass/fail counters parsed by runners, validator exit codes, JSON stop-hook decisions, generated loop-state files, mocked `gh` responses, and temporary output files. Shared helpers in `tests/test-helpers.sh` provide pass/fail/skip counters and temp repo setup at lines 30-104. `tests/mocks/gh` routes `gh pr`, `gh api`, `gh repo`, and auth calls to fixture-backed JSON at lines 27-243; `tests/setup-fixture-mock-gh.sh` creates a narrower fixture-backed `gh` for fetch/poll tests at lines 24-104.
- gates_or_invariants: The directory enforces no unsafe writes to loop-managed files; no direct use of stale round todo files outside narrow allowlists; native TodoWrite completion semantics; plan files must be relative, readable, non-symlinked, inside repo, tracked/gitignored according to mode, and contentful; PR loop state files are protected; PR bot review polling must include approval-only reviews; `gen-plan` must keep valid YAML frontmatter and deterministic validation exit codes. `run-all-tests.sh` additionally normalizes ANSI output before aggregating counts at lines 113-128.
- dependencies_and_callers: Depends on bash, zsh for the zsh-specific suite, Python 3 for todo transcript checking, `jq`, `git`, temporary filesystem support, and production files under `hooks/`, `scripts/`, `commands/`, `agents/`, and `prompt-template/`. PR suites depend on mocked GitHub CLI responses from `tests/fixtures/*.json`; fixture shapes include issue comments, inline review comments, PR reviews, reactions, and comment reactions. The directory coordinates with parent paths by treating production scripts/hooks as black-box or sourced units, not reimplementing their logic except for small unit mirrors of jq filters.
- edge_cases_or_failure_modes: Covers invalid JSON, empty and malformed transcript lines, stale or missing state files, symlink plan files, parent symlink traversal, path metacharacters, spaces, nonexistent parent directories, non-git repos, repos without commits, modified tracked plans, YAML-unsafe branches and model strings, missing templates, unsafe template renders, missing `gh`/`jq`, paginated PR API behavior, empty PR review bodies, bot approval via reaction, bot timeout, `.humanize` git-add bypasses, zsh glob errors, terminal cleanup, binary/null/large plan files, mixed line endings, disappearing files, and concurrent session churn.
- validation_or_tests: The directory is itself validation. I did not execute it because this run is read-only research; static inspection found 48 files total, with 29 suites listed in `run-all-tests.sh` and additional manual/helper/mock/fixture files. Key branch-specific coverage: `tests/test-gen-plan.sh` checks `commands/gen-plan.md` and `scripts/validate-gen-plan-io.sh`; `tests/test-template-references.sh` checks `prompt-template/block/todos-file-access.md`; `tests/robustness/test-plan-file-robustness.sh` and `tests/test-plan-file-validation.sh` cover production plan validation.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-037 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop polling script that collects new bot-authored PR feedback after a timestamp and returns normalized JSON for the PR loop stop hook. It is core workflow logic because `hooks/pr-loop-stop-hook.sh` invokes it during bot review polling at lines 801 and 950-955.
- algorithmic_behavior: Parses `<pr_number> --after <timestamp> --bots <bot1,bot2>` with help/error handling at lines 19-102. Requires `gh` and `jq` at lines 108-116. Resolves the base repository, including fork fallback through parent repo, at lines 122-149. Converts logical bot names to GitHub authors, with `codex` mapped to `chatgpt-codex-connector[bot]`, at lines 163-186. Fetches issue comments, inline review comments, and PR review submissions via retrying GitHub API calls at lines 206-293. Normalizes all three streams, including empty PR-review bodies as `[Review state: STATE]`, combines them, filters by timestamp and author regex, and emits final JSON at lines 295-325.
- inputs_outputs_state: Inputs are current GitHub repo context, PR number, ISO timestamp string, comma-separated bot names, and GitHub API results. Outputs a JSON object with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` at lines 316-325. Temporary state is isolated under `mktemp -d` and removed by trap at lines 192-197. It also maintains an `API_FAILURES` counter for diagnostics during retry fallback at lines 199-225, though the final JSON does not expose that counter.
- gates_or_invariants: PR number must be numeric; `--after` and `--bots` are required; `gh` and `jq` must exist; repository owner/name must parse from the base repo. API fetch failures are non-fatal after retries: `fetch_with_retry` logs warnings, returns `[]`, and preserves script success at lines 230-233 so polling loops can keep running through partial outages.
- dependencies_and_callers: Depends on `gh api`, `gh repo view`, `gh pr view`, `jq`, `mktemp`, and shell regex/string processing. Called by `hooks/pr-loop-stop-hook.sh` as `POLL_SCRIPT`, and the hook maps returned author names back to bot names at lines 957-971. Tested with fixture-backed `gh` by `tests/test-pr-loop-hooks.sh` lines 1322-1419 and supported by `tests/setup-fixture-mock-gh.sh` lines 67-90.
- edge_cases_or_failure_modes: Fork PRs require base-repo resolution; parent repo lookup may fail; empty-body approval-only PR reviews are deliberately preserved; GitHub API outages degrade to empty arrays; comma bot lists are space-trimmed; bot patterns are regex-built, so bracket escaping is critical; timestamp filtering is lexicographic ISO comparison and assumes comparable ISO formats; `bots_responded` returns author names, not logical bot names, so callers must map them.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` mirrors the approval-only jq logic at lines 1160-1196, validates bot regex matching at lines 1198-1215, then runs this script against fixtures at lines 1322-1368 and validates `--after` filtering at lines 1371-1411. `tests/setup-fixture-mock-gh.sh` supplies issue comments, inline review comments, and PR reviews at lines 74-89.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-067 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable consistency specification for prompt-template references used by hook validators. It prevents validators from producing empty or missing block messages when enforcing loop safety gates.
- algorithmic_behavior: Defines a fixed script set to scan at lines 56-64, including read/write/edit/bash validators and `hooks/lib/loop-common.sh`. For each non-comment line, it detects `load_template`, `load_and_render`, and `load_and_render_safe` calls against `$TEMPLATE_DIR`, extracts the second quoted argument with sed, and verifies the target markdown file exists at lines 74-111. It then enumerates all markdown templates under `prompt-template/`, warns on unreferenced templates, and separately verifies common block templates at lines 121-167. Finally, it checks critical validators use `load_and_render_safe` rather than unsafe rendering at lines 172-201.
- inputs_outputs_state: Inputs are hook shell scripts and `prompt-template/**/*.md`. Outputs colored PASS/FAIL/WARN lines plus counters and a final exit code. State is in arrays `MISSING_TEMPLATES`, `TEMPLATE_FILES`, `UNREFERENCED`, and scalar counters initialized at lines 25-27 and 71-72.
- gates_or_invariants: Missing directly referenced templates are hard failures; unreferenced templates are warnings; critical read/write/edit validators must use safe rendering for all `load_and_render` calls. The common-template gate explicitly includes `block/todos-file-access.md` at lines 152-159.
- dependencies_and_callers: Depends on `find`, `grep`, `sed`, shell arrays, and the repository’s hook/template layout. It validates `hooks/lib/loop-common.sh`, whose `todos_blocked_message` loads `block/todos-file-access.md` with safe fallback at lines 347-355, plus hook validators that call these common block-message functions.
- edge_cases_or_failure_modes: Only literal quoted template paths after `$TEMPLATE_DIR` are extracted; dynamic template paths may show as unreferenced warnings rather than failures. Missing scripts are warnings at lines 74-77. The unsafe-render check is line-based and targets calls with literal `$TEMPLATE_DIR`, so radically reformatted calls could evade detection.
- validation_or_tests: The script self-validates and is included in the aggregate runner at `tests/run-all-tests.sh` line 38. It directly validates the assigned `prompt-template/block/todos-file-access.md` and other block templates.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-097 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template used when a hook prevents access to `round-*-todos.md` files. It defines the user-facing contract that loop todos should be managed through native TodoWrite, not ad hoc markdown files.
- algorithmic_behavior: The template is static markdown: title at line 1, prohibition at line 3, replacement instruction at line 5, and rationale at lines 7-8. It has no variables, so rendering is deterministic and does not depend on substitution state.
- inputs_outputs_state: Input is a validator block event requesting the standard todos-access message. Output is markdown text loaded through `load_and_render_safe`. No persistent state is read or written by the template itself.
- gates_or_invariants: The message establishes a safety invariant: do not create or access `round-*-todos.md`; use TodoWrite instead. Production loading is in `hooks/lib/loop-common.sh` lines 347-355. Bash validator enforcement blocks generic or wrong-directory todo-file modifications at `hooks/loop-bash-validator.sh` lines 381-392 unless the path is one of the active-loop allowlist cases.
- dependencies_and_callers: Called via `todos_blocked_message` in `hooks/lib/loop-common.sh`; referenced by `tests/test-template-references.sh` as a required common template at lines 152-159; related behavior is tested by `tests/test-allowlist-validators.sh` for read/write/edit/bash todos access and by `tests/test-todo-checker.sh` for native TodoWrite transcript semantics.
- edge_cases_or_failure_modes: Because the template has no variables, missing-file failure is the main risk; `load_and_render_safe` mitigates that with a fallback in `loop-common.sh` lines 351-355. Policy nuance: allowlist behavior still permits `round-1-todos.md` and `round-2-todos.md` in the active loop directory for legacy compatibility, so this template is mainly used for disallowed generic, wrong-round, or wrong-location access.
- validation_or_tests: `tests/test-template-references.sh` checks its existence at lines 152-163. `tests/test-allowlist-validators.sh` checks blocking and allowlisting for `round-*-todos.md` access across Read/Write/Edit/Bash. `tests/test-todo-checker.sh` verifies the replacement TodoWrite pathway by parsing transcript tool calls and failing on incomplete todo statuses.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-127 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for production plan-file validation in `scripts/setup-rlcr-loop.sh`. It focuses on content and file-shape resilience beyond the normal path/tracking tests.
- algorithmic_behavior: Wraps production validation in `test_plan_validation`, which runs `setup-rlcr-loop.sh` inside a temp git repo with `CLAUDE_PROJECT_DIR="$TEST_DIR"`, captures output to a temp file, maps known validation errors to rejection, and treats later failures such as missing Codex or gitignore tracking as evidence that plan validation passed at lines 22-82. It initializes a committed temp git repo at lines 84-97. A local `count_content_lines` helper mirrors production comment/blank-line exclusion at lines 103-143. Positive cases create valid, minimum-content, standard-size, and rich-markdown plans at lines 152-264. Negative and edge cases cover empty/comment-only files, large files, mixed endings, binary bytes, long lines, special characters, whitespace, nested comments, missing/unreadable/symlink/directory paths, null bytes, and disappearance at lines 274-525.
- inputs_outputs_state: Inputs are generated plan files inside `TEST_DIR`, a temporary git repo, `scripts/setup-rlcr-loop.sh`, and helper functions from `tests/test-helpers.sh` sourced at line 84. Outputs are PASS/FAIL counters via `print_test_summary` at lines 531-532. Temporary loop state under `$TEST_DIR/.humanize/rlcr` is removed before each production validation at line 33 to avoid cross-test contamination.
- gates_or_invariants: Production plan validation must require at least five physical lines and at least three content lines, excluding blanks, hash-prefixed comment lines, single-line HTML comments, and multi-line HTML comment blocks. It must reject missing files and insufficient content but accept rich markdown, mixed line endings, 1MB-plus content, and valid minimal content. The production implementation of these gates is in `scripts/setup-rlcr-loop.sh` lines 416-469.
- dependencies_and_callers: Depends on `git`, `wc`, `mktemp`, shell heredocs, `chmod`, symlink support, and the production setup script. It complements `tests/test-plan-file-validation.sh`, which covers path, git, tracking, branch, CLI, and Codex parameter gates at lines 89-741.
- edge_cases_or_failure_modes: The wrapper infers “plan accepted” when validation passes but later setup fails because Codex is unavailable or the plan is not gitignored, so it is intentionally testing only the plan-validation phase. Some checks are local helper checks rather than production calls, including binary, long-line, special-character, whitespace, complex-comment, symlink, directory, and null-byte line-count tests. Running as root can make unreadable-file expectations environment-dependent; the test treats readability as acceptable at lines 451-463.
- validation_or_tests: Included in `tests/run-all-tests.sh` at line 62. Directly traces to `setup-rlcr-loop.sh` validation logic: path/readability checks at lines 320-329 and content checks at lines 416-469. It is itself a spec and was inspected statically, not executed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`