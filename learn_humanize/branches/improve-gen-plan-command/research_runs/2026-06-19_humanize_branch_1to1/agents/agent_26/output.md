# agent_26 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-026 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Edit` validator for the RLCR and PR loop state machines. It protects generated/control files under `.humanize/rlcr/` and `.humanize/pr-loop/` from direct Claude edits, while allowing narrow system-defined edit surfaces such as selected round summaries/todos and current PR resolution files.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name`, exits immediately unless it is `Edit`, then lowercases `.tool_input.file_path` for pattern matching (`hooks/loop-edit-validator.sh:24-32`). It first blocks RLCR `round-*-todos.md` edits unless the file is in the active loop allowlist (`lines 38-45`) and blocks all RLCR prompt file edits (`lines 48-51`). It then applies PR-loop protections: block `state.md`, block generated/read-only PR files, and validate `round-N-pr-resolve.md` against the current PR round (`lines 57-76`). For `.humanize/rlcr/` paths, it finds the active loop, prefers `finalize-state.md` over `state.md` when present, parses state strictly, then blocks state/finalize-state, protected plan backups, post-round-0 goal tracker edits, and wrong-round summary edits (`lines 83-181`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.file_path`. Output is process status plus stderr block guidance. Exit `0` allows the edit, exit `2` blocks an invalid edit with a user-facing reason, and exit `1` fails closed on malformed active RLCR state (`lines 105-109`). State consumed includes `CLAUDE_PROJECT_DIR`, optional `PROJECT_ROOT`, optional `LOOP_BASE_DIR`, optional `LOOP_DIR`, active `.humanize/rlcr/*/state.md` or `finalize-state.md`, and PR-loop state under `.humanize/pr-loop/*/state.md`.
- gates_or_invariants: Non-`Edit` tools are ignored (`lines 27-29`). Todos and prompt files are treated as generated/control surfaces (`lines 38-51`). PR-loop `state.md` and generated PR prompt/comment/check/feedback files are read-only (`lines 59-70`). RLCR state parsing is fail-closed via `parse_state_file_strict` (`lines 105-109`; strict parser requires YAML separators, `current_round`, `max_iterations`, and numeric values in `hooks/lib/loop-common.sh:240-295`). `finalize-state.md` is checked before generic `state.md` because the generic matcher also matches it (`hooks/loop-edit-validator.sh:115-124`). Goal tracker edits are allowed only in round 0 (`lines 145-149`). Summary edits must match `CURRENT_ROUND` unless allowlisted (`lines 155-179`).
- dependencies_and_callers: Registered as the `Edit` PreToolUse hook in `hooks/hooks.json:24-30`. Sources shared helpers from `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16-18`), including active loop discovery (`hooks/lib/loop-common.sh:152-170`), round file matching (`305-310`), active-loop allowlist (`327-345`), path matchers (`408-423`, `617-620`, `687-690`), PR read-only classification (`731-739`), and PR resolve round validation (`747-790`). Template-backed messages come through `load_and_render_safe` in shared helpers, with template reference coverage checked by `tests/test-template-references.sh:57-64` and safe rendering checked at `176-200`.
- edge_cases_or_failure_modes: If no active RLCR loop exists, RLCR internal edits outside earlier todos/prompt checks are allowed by exiting `0` (`hooks/loop-edit-validator.sh:91-97`). Malformed active state blocks safely with exit `1` (`lines 105-109`). Allowlist behavior is exact path based, so an allowlisted filename in the wrong directory is blocked by `is_allowlisted_file` (`hooks/lib/loop-common.sh:327-345`). PR resolve validation returns allow when no active PR loop/state exists, but blocks wrong-round `round-N-pr-resolve.md` when state gives a different `current_round` (`hooks/lib/loop-common.sh:751-790`). A notable classification nuance: `is_in_humanize_loop_dir` and `is_in_pr_loop_dir` are substring/regex path checks rather than canonical path checks (`hooks/lib/loop-common.sh:617-620`, `687-690`), so validation is pattern-oriented, not symlink/canonical-path hardened here.
- validation_or_tests: Allowlist and edit-validator coverage appears in `tests/test-allowlist-validators.sh:189-225`, covering allowed `round-2-todos.md`, allowed historical `round-1-summary.md`, and blocked `round-4-todos.md`. Finalize-state edit blocking is tested in `tests/test-finalize-phase.sh:310-320`. Wrong-round PR resolve edit blocking is tested in `tests/test-pr-loop-hooks.sh:1500-1527`. Template existence and safe fallback rendering for critical validators are checked in `tests/test-template-references.sh:57-64` and `176-200`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-056 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for the RLCR monitor’s graceful stop, cleanup idempotency, terminal restoration, and signal handling behavior. It verifies the user-facing monitor does not leave a damaged terminal state when `.humanize/rlcr` disappears or the monitor receives interrupts.
- algorithmic_behavior: Creates an isolated temp workspace in `/tmp/test-monitor-runtime-$$`, tracks pass/fail counters, and cleans the temp workspace on exit (`tests/test-monitor-runtime.sh:45-53`). The tests generate small shell scripts that source or emulate relevant monitor functions, then assert observable output and source structure. Test 1 verifies `_graceful_stop` calls `_cleanup`, `_cleanup` calls `_restore_terminal`, and the deletion reason is displayed (`lines 61-134`). Test 2 verifies `_cleanup` is idempotent via `cleanup_done` (`lines 143-177`). Test 3 models the main loop directory check and requires graceful stop after `.humanize/rlcr` deletion (`lines 186-250`). Test 4 checks `_restore_terminal` exists, resets the scroll region with `printf "\033[r"`, and is called by `_cleanup` (`lines 263-311`). Test 5 verifies `_graceful_stop` calls `_cleanup` in `scripts/humanize.sh` (`lines 313-324`). Tests 6-9 verify bash and zsh interrupt/trap behavior and trap reset (`lines 326-490`).
- inputs_outputs_state: Inputs are the repository under test, `/tmp`, shell availability, optional `zsh`, and the source file `scripts/humanize.sh`. Outputs are colored PASS/FAIL lines and final exit status: `0` only when `TESTS_FAILED == 0`, otherwise `1` (`lines 492-513`). The test-local state is `TESTS_PASSED`, `TESTS_FAILED`, generated scripts, and temporary `.humanize/rlcr` directories.
- gates_or_invariants: Monitor cleanup must be single-shot (`cleanup_done`) and must restore terminal state before announcing graceful stop (`tests/test-monitor-runtime.sh:94-101`, mirrored in implementation at `scripts/humanize.sh:496-535`). The monitor loop must treat deleted `.humanize/rlcr` as a clean stop, not a crash (`tests/test-monitor-runtime.sh:205-229`; implementation checks at `scripts/humanize.sh:581-585`, `621-625`, `714-718`). Bash must install `trap '_cleanup' INT TERM`, zsh must define `TRAPINT()` and `TRAPTERM()`, and cleanup must reset traps to avoid repeated signal handling (`tests/test-monitor-runtime.sh:390-423`; implementation at `scripts/humanize.sh:502-547`).
- dependencies_and_callers: Directly depends on `scripts/humanize.sh`, especially `_restore_terminal`, `_cleanup`, `_graceful_stop`, and signal setup. The implementation defines scroll-region setup/restoration at `scripts/humanize.sh:471-487`, cleanup at `496-520`, graceful stop at `522-535`, and signal handlers at `537-547`. It also indirectly depends on common monitor helpers sourced by `scripts/humanize.sh:6-10`, though these tests focus on runtime cleanup behavior rather than helper parsing.
- edge_cases_or_failure_modes: The test intentionally avoids relying on actual interactive `tput` effects in non-interactive mode and instead checks function definitions/source strings for terminal restoration (`tests/test-monitor-runtime.sh:260-287`). The zsh runtime test is skipped if `zsh` is unavailable (`lines 432-490`). There is a minor test bug at `lines 282-287`: `grep -q '_restore_terminal' "$2/scripts/humanize.sh" | grep -q '_cleanup'` pipes a quiet grep with no stdout into another grep, so that nested conditional cannot prove call order; the script later performs the real check with `grep -A30 "^    _cleanup()"` at `lines 305-311`. Another edge is that several generated mini-tests emulate copied function bodies instead of invoking the nested monitor functions directly, because those functions live inside `_humanize_monitor_codex`.
- validation_or_tests: This file is itself the validation artifact. It validates against implementation lines in `scripts/humanize.sh`: `_restore_terminal` resets scroll region (`481-487`), `_cleanup` resets traps/kills tail/restores terminal (`496-520`), `_graceful_stop` calls `_cleanup` and prints a deletion/cancel explanation (`522-535`), traps are installed for bash/zsh (`537-547`), and all monitor loops check for deleted `.humanize/rlcr` (`581-585`, `621-625`, `714-718`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-086 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR completion gate that prevents transition from Claude work to Codex review while Claude’s TodoWrite list still has incomplete entries.
- algorithmic_behavior: Provides the rendered block message headed `Incomplete Todos Detected`, interpolates `{{INCOMPLETE_LIST}}`, then instructs the agent to complete all todos, mark each via TodoWrite, and only then write the summary and stop (`prompt-template/block/incomplete-todos.md:1-12`). The hook call site runs the todo checker before expensive Codex review; if incomplete todos are found, it renders this template and returns a hook JSON block decision (`hooks/loop-codex-stop-hook.sh:255-309`).
- inputs_outputs_state: Template input is `INCOMPLETE_LIST`, built from the todo checker result via `tail -n +2` (`hooks/loop-codex-stop-hook.sh:287-298`). The upstream checker reads the transcript path from hook input, finds the latest TodoWrite call in multiple transcript formats, and emits incomplete todos with status/content (`hooks/check-todos-from-transcript.py:20-87`, `90-126`). Rendered output becomes the `reason` field of a JSON response with `"decision": "block"` and system message `"Loop: Blocked - incomplete todos detected, please finish all tasks first"` (`hooks/loop-codex-stop-hook.sh:300-307`).
- gates_or_invariants: This is a pre-review hard gate. Any latest TodoWrite item whose `status != "completed"` is incomplete (`hooks/check-todos-from-transcript.py:113-126`). The template’s invariant is explicit: do not proceed to Codex review until all todos are finished (`prompt-template/block/incomplete-todos.md:7-12`). The gate is intentionally quick and runs before Git status and other expensive stop checks (`hooks/loop-codex-stop-hook.sh:255-259`).
- dependencies_and_callers: Rendered only when `hooks/check-todos-from-transcript.py` exists and exits `1` (`hooks/loop-codex-stop-hook.sh:261-309`). It is loaded with `load_and_render_safe "$TEMPLATE_DIR" "block/incomplete-todos.md"` (`hooks/loop-codex-stop-hook.sh:292-298`), so missing template falls back to the inline `# Incomplete Todos` message. Template reference tests scan scripts and validate referenced templates in `tests/test-template-references.sh:57-110`.
- edge_cases_or_failure_modes: If the todo checker script is missing, this particular gate is skipped (`hooks/loop-codex-stop-hook.sh:261-310`). If hook input cannot be parsed, the stop hook blocks with a parse-error reason instead of this template (`lines 268-285`). If no transcript path or no TodoWrite entries exist, the checker exits `0`, so no incomplete-todo block occurs (`hooks/check-todos-from-transcript.py:99-111`). The checker only evaluates the latest TodoWrite list it finds, so stale earlier incomplete lists are ignored by design (`hooks/check-todos-from-transcript.py:28-87`).
- validation_or_tests: Direct behavior is covered through the stop-hook todo-check path, with template existence/reference checks in `tests/test-template-references.sh:57-110`. The checker itself documents and implements exit codes `0`, `1`, and `2` (`hooks/check-todos-from-transcript.py:7-13`, `90-129`).
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-116 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt footer for the no-comments Round 0 path. It defines the completion contract when a newly opened PR has no existing review comments: write a resolution summary, exit, and let the Stop Hook poll for automatic bot reviews rather than manually commenting.
- algorithmic_behavior: Renders a high-visibility critical section telling the agent to write the resolution summary to `{{RESOLVE_PATH}}`, note that Round 0 is awaiting initial bot reviews and has no issues yet, then try to exit so the Stop Hook can poll (`prompt-template/pr-loop/critical-requirements-no-comments.md:2-20`). `scripts/setup-pr-loop.sh` chooses this template only when the fetched comment file contains the exact no-comments sentinel (`scripts/setup-pr-loop.sh:641-649`, `810-835`).
- inputs_outputs_state: Inputs are template variables assembled during PR-loop setup: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, and `BOT_MENTION_STRING` (`scripts/setup-pr-loop.sh:651-658`). This template directly uses `RESOLVE_PATH` (`prompt-template/pr-loop/critical-requirements-no-comments.md:8-14`). Output is appended to setup command stdout after the initial prompt, making it immediate agent instruction rather than persisted state by itself (`scripts/setup-pr-loop.sh:807-862`).
- gates_or_invariants: In the no-comments state, first reviews are automatic; the agent must not comment on the PR to trigger review (`prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`). The required state transition is: create `round-0-pr-resolve.md` summary at the loop’s resolve path, then Stop Hook polling begins. This is consistent with the no-comments task template, which also says not to comment and to write the initial summary to `@{{RESOLVE_PATH}}` (`prompt-template/pr-loop/round-0-task-no-comments.md:4-30`).
- dependencies_and_callers: Called from `scripts/setup-pr-loop.sh` in the no-comments branch using `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-no-comments.md"` with an inline fallback (`scripts/setup-pr-loop.sh:810-835`). It coordinates with the earlier rendered Round 0 prompt file, comment sentinel detection, and PR-loop stop hook behavior. The complementary comments-present path uses `prompt-template/pr-loop/critical-requirements-has-comments.md` instead (`scripts/setup-pr-loop.sh:835-860`).
- edge_cases_or_failure_modes: The no-comments branch depends on the exact sentinel line `*No comments found.*`; any other comment-file content is treated as comments-present (`scripts/setup-pr-loop.sh:643-649`). If the template is missing or render fails, `load_and_render_safe` uses the inline fallback with the same contract (`scripts/setup-pr-loop.sh:813-834`). The template intentionally omits commit/push/comment instructions because no issues exist yet; using the comments-present contract here would incorrectly trigger bot review comments on a fresh PR.
- validation_or_tests: The call site’s rendered-template fallback path is visible in `scripts/setup-pr-loop.sh:810-862`. Template presence is included in comprehensive template/reference coverage through the project’s template tests, and the adjacent no-comments task template reinforces the same no-comment invariant at `prompt-template/pr-loop/round-0-task-no-comments.md:18-30`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item evidence headings present in assigned order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`