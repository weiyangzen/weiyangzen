# agent_23 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`
- notes: Read-only branch export inspected directly. No files modified. Full test suites were not run; this was static research only. A `git status` probe could not complete because the export lacks a usable `.git` context and the sandbox blocked system cache writes, so source commit is taken from scheduler metadata.

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-023 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Declares the Claude plugin hook graph for the Humanize RLCR and PR-loop state machines. It is not an implementation body itself; it is the dispatch table that attaches validation and stop-transition scripts to hook events. `hooks/hooks.json:2` describes the file as validation and Stop hooks for `/start-rlcr-loop` and `/start-pr-loop`.
- algorithmic_behavior: On `UserPromptSubmit`, it runs `${CLAUDE_PLUGIN_ROOT}/hooks/loop-plan-file-validator.sh` (`hooks/hooks.json:4-12`), which validates active RLCR loop state before accepting a user prompt. On `PreToolUse`, it routes `Write`, `Edit`, `Read`, and `Bash` tool events to dedicated validators (`hooks/hooks.json:14-50`). On `Stop`, it invokes both RLCR/Codex and PR-loop stop hooks with long timeouts (`hooks/hooks.json:52-64`), letting each script detect whether its loop is active.
- inputs_outputs_state: Inputs are Claude hook event payloads plus environment, especially `CLAUDE_PLUGIN_ROOT` and often `CLAUDE_PROJECT_DIR` in the downstream scripts. Outputs are hook decisions expressed by downstream validator exit codes, stderr messages, or JSON block responses. State transitions are delegated: `loop-plan-file-validator.sh` checks active loop state, schema, branch, and tracked plan status; `loop-codex-stop-hook.sh` builds review prompts and advances RLCR rounds/finalize state; `pr-loop-stop-hook.sh` advances PR review loop state.
- gates_or_invariants: The hook graph enforces several invariants by construction: plan validation precedes prompt submission; tool use against protected loop files is intercepted before execution; stop processing has a 7200 second timeout for RLCR and PR-loop transitions. The plan validator fails closed on malformed state (`hooks/loop-plan-file-validator.sh:35-41`) and checks required schema fields plus branch consistency (`hooks/loop-plan-file-validator.sh:62-103`). The PreToolUse hook split keeps file/tool-specific policies separate rather than relying on one broad validator.
- dependencies_and_callers: This file is consumed by Claude’s plugin hook loader. It depends on executable scripts under `hooks/`: `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, `loop-bash-validator.sh`, `loop-codex-stop-hook.sh`, and `pr-loop-stop-hook.sh`. Those scripts in turn depend on shared shell libraries such as `hooks/lib/loop-common.sh`, template loading, `jq`, git commands, and in the stop hook path, the `codex` command.
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is unset or wrong, all hook commands resolve to invalid paths. Since both Stop hooks are always listed, each stop script must safely no-op when its loop type is inactive; if both loop directories were active unexpectedly, both hooks could attempt stop-time logic. A missing or non-executable script turns this dispatch config into a hard hook failure. Long-running Codex/PR stop work is capped at 7200 seconds, which prevents infinite stop-hook hangs but can still block for a long time.
- validation_or_tests: Coverage is indirect through hook and validator tests under `tests/`, including plan-file, PR-loop, finalize, bash-validator, template, and robustness tests. Static references show the hook scripts and templates are exercised broadly, but no direct test output was run for this research pass.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-053 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared Bash test harness for the repository’s executable specifications. It is not production RLCR/PR-loop state-machine code, but it standardizes pass/fail accounting and fixture setup used by tests that validate the core behavior. Usage comments cover sourcing from both `tests/` and `tests/robustness/` (`tests/test-helpers.sh:5-6`).
- algorithmic_behavior: Defines color constants (`tests/test-helpers.sh:13-16`), global counters (`tests/test-helpers.sh:22-24`), result functions `pass`, `fail`, and `skip` that increment counters and emit formatted messages (`tests/test-helpers.sh:30-52`), `print_test_summary` that returns success only when failure count is zero (`tests/test-helpers.sh:58-78`), `setup_test_dir` for temporary directory lifecycle (`tests/test-helpers.sh:86-89`), and `init_test_git_repo` for creating a minimal committed git repository fixture (`tests/test-helpers.sh:93-105`).
- inputs_outputs_state: Inputs are function arguments: labels, expected/actual values, optional skip reasons, summary title, and target directory for git fixture creation. Outputs are terminal messages, updated global counters, return status from `print_test_summary`, exported/global `TEST_DIR`, an installed `EXIT` trap, and a local git repo with configured author and initial commit. Internal state is purely shell-global: `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, and `TEST_DIR`.
- gates_or_invariants: The key validation invariant is that test process success is derived from `TESTS_FAILED == 0` (`tests/test-helpers.sh:71-77`). Failures do not immediately exit by themselves; callers must use `print_test_summary` or their own gate. `init_test_git_repo` disables GPG signing (`tests/test-helpers.sh:100`) so fixture commits are deterministic in CI-like environments.
- dependencies_and_callers: Depends on Bash, `mktemp`, `git`, and basic shell utilities. It is sourced by many tests, including `tests/test-pr-loop.sh:19`, `tests/test-pr-loop-lib.sh:20`, and robustness tests such as `tests/robustness/test-goal-tracker-robustness.sh:19`, `tests/robustness/test-path-validation-robustness.sh:14`, and `tests/robustness/test-hook-input-robustness.sh:17`.
- edge_cases_or_failure_modes: `setup_test_dir` installs `trap "rm -rf $TEST_DIR" EXIT` without quoting the expanded path (`tests/test-helpers.sh:87-88`), so unusual temp paths with whitespace or shell metacharacters would be fragile. It also overwrites any caller’s existing `EXIT` trap. `init_test_git_repo` changes directories and relies on `cd -` to return (`tests/test-helpers.sh:96-104`); failures in between can leave callers in a different directory unless caller strict mode catches them. The helper does not itself set `set -euo pipefail`, so behavior depends on the sourcing test script.
- validation_or_tests: This file is the validation substrate rather than the subject of a dedicated test. Its functions are transitively validated whenever the shell test suite runs and reports through these counters.
- skip_candidate: `yes: shared test harness, not a production hook/validator or direct RLCR state transition implementation`

### IMPROVE_GEN_PLAN_COMMAND-HZ-083 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Bash validator when a Bash command attempts to modify `goal-tracker.md` during Round 0. It is a policy surface for preserving validator visibility: goal tracker edits must go through Write/Edit rather than shell redirection or text utilities.
- algorithmic_behavior: Renders a concise block reason headed `Bash Write Blocked` (`prompt-template/block/goal-tracker-bash-write.md:1`). It forbids Bash modification of `goal-tracker.md` (`prompt-template/block/goal-tracker-bash-write.md:3`), renders the required path via `{{CORRECT_PATH}}` (`prompt-template/block/goal-tracker-bash-write.md:5`), and explains that commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:7-8`).
- inputs_outputs_state: Input is one template variable, `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message`. Output is human-readable markdown sent to stderr by the Bash validator. It does not mutate loop state; instead it blocks a transition path that would bypass the pre-tool validators.
- gates_or_invariants: Enforces the invariant that Round 0 `goal-tracker.md` initialization or correction must use Write/Edit tooling, where path and loop-state validators can inspect the operation. The shared renderer call is in `goal_tracker_bash_blocked_message()` (`hooks/lib/loop-common.sh:396-405`). That function supplies a fallback with the same policy if the template is missing.
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh`, and reached from `hooks/loop-bash-validator.sh` when `command_modifies_file "$COMMAND_LOWER" "goal-tracker\.md"` matches. For later rounds, the Bash validator uses a different goal-tracker block path that redirects changes into the current round summary rather than allowing direct modification.
- edge_cases_or_failure_modes: The template only guides the user; actual blocking depends on the Bash validator’s command-pattern detection. Obfuscated shell commands, generated filenames, or commands that modify through indirect paths could evade simple pattern matching if not covered by `command_modifies_file`. If `CORRECT_PATH` is wrong because active loop discovery is wrong, the message can direct the user to the wrong loop file.
- validation_or_tests: `tests/test-template-references.sh:157` includes `block/goal-tracker-bash-write.md` in common template existence validation. The shared loader path is covered indirectly by template-loader and validator tests; no direct runtime test was executed in this research pass.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-113 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular Codex review prompt template for the RLCR Stop-hook loop. It defines the review contract that decides whether Claude continues iterating or the loop may advance toward completion. Unlike the full-alignment review, this is used for ordinary rounds where `CURRENT_ROUND % 5 != 4`.
- algorithmic_behavior: The template requires Codex to read the original plan first (`prompt-template/codex/regular-review.md:3-11`), compare Claude’s summary against actual implementation (`prompt-template/codex/regular-review.md:20-31`), perform a mandatory goal-alignment check against `goal-tracker.md` (`prompt-template/codex/regular-review.md:33-45`), include the rendered goal-tracker update section (`prompt-template/codex/regular-review.md:47`), and write review comments to `{{REVIEW_RESULT_FILE}}` when work is incomplete or inconsistent (`prompt-template/codex/regular-review.md:49-57`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`. Output is a generated `round-N-review-prompt.md`, consumed by Codex, whose expected product is `round-N-review-result.md`. The downstream state transition is based on the last non-empty line of the review result: exact `COMPLETE` enters Finalize Phase or max-iteration termination; anything else keeps the loop blocked with review feedback.
- gates_or_invariants: The template treats deferred and unfinished work as incomplete (`prompt-template/codex/regular-review.md:24-31`, `prompt-template/codex/regular-review.md:53-56`). It requires a goal alignment summary with AC count, forgotten items, and unjustified deferrals (`prompt-template/codex/regular-review.md:42-45`). The completion invariant is strict: only output `COMPLETE` as the last line when all original plan tasks are fully done, all ACs are met, and there are no deferrals or pending work (`prompt-template/codex/regular-review.md:53-57`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` builds the review prompt. It prepares `REVIEW_RESULT_FILE` and `SUMMARY_CONTENT` (`hooks/loop-codex-stop-hook.sh:676-678`), renders `GOAL_TRACKER_UPDATE_SECTION` (`hooks/loop-codex-stop-hook.sh:680-684`), selects full-alignment every fifth round (`hooks/loop-codex-stop-hook.sh:686-690`), and renders `codex/regular-review.md` with all variables for regular rounds (`hooks/loop-codex-stop-hook.sh:736-751`). The same stop hook later validates that Codex created a nonempty result file (`hooks/loop-codex-stop-hook.sh:904-955`) and checks the final marker strictly (`hooks/loop-codex-stop-hook.sh:960-968`).
- edge_cases_or_failure_modes: If Codex writes to stdout instead of `REVIEW_RESULT_FILE`, the stop hook copies stdout into the expected file (`hooks/loop-codex-stop-hook.sh:904-918`), so prompt noncompliance may still be tolerated. If the result file is missing or empty, the stop hook fails the review path (`hooks/loop-codex-stop-hook.sh:920-955`). The prompt’s completion gate is natural-language, so enforcement of “all ACs met” depends on Codex review quality; the script only enforces the exact final marker. A result ending with text like `CANNOT COMPLETE` does not trigger completion because the hook compares the trimmed last non-empty line exactly.
- validation_or_tests: Final-marker behavior is covered indirectly by finalize-phase tests; search evidence shows `tests/test-finalize-phase.sh` includes COMPLETE handling cases such as “COMPLETE triggers Finalize Phase entry.” Template existence and rendering are covered by the repository’s template tests generally, but this specific prompt’s semantic instructions are primarily enforced through stop-hook behavior and review result parsing.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections present; assigned IDs are used only as the four `## Item Evidence` section headings.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`