# agent_01 improve-gen-plan-command 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `934cf543d66046b72071d121b15583d5e3d6799e`

## Item Evidence

### IMPROVE_GEN_PLAN_COMMAND-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is a Claude Code plugin implementing Humanize workflows: `gen-plan`, RLCR loop, and PR-loop. The root coordinates command markdown under `commands/`, runtime scripts under `scripts/`, hook validators under `hooks/`, prompt/error templates under `prompt-template/`, plugin metadata, and shell tests.
- algorithmic_behavior: Command frontmatter constrains tool access and delegates to scripts, e.g. `commands/gen-plan.md:30-46` validates IO before plan generation, and `commands/start-pr-loop.md:10-24` delegates PR-loop initialization. Runtime scripts create loop sessions, state files, round prompts, GitHub comment snapshots, and cancellation state. Hooks registered in `hooks/hooks.json:14-63` enforce pre-tool validation and stop-hook loop progression. Templates provide deterministic user-facing block messages. Tests and robustness suites encode expected transitions and rejection cases.
- inputs_outputs_state: Inputs include slash-command arguments, draft files, plan files, Git/GitHub state, PR comments/reviews, bot flags, and loop state frontmatter. Outputs include generated plans, `.humanize/rlcr/<timestamp>/` and `.humanize/pr-loop/<timestamp>/` artifacts, round files, goal trackers, monitor summaries, and terminal/JSON responses. State is mostly filesystem-driven: active loops are indicated by `state.md`; terminal states use files such as `cancel-state.md`, `approve-state.md`, and `maxiter-state.md`.
- gates_or_invariants: Hook registration makes Write/Edit/Read/Bash validators mandatory before tool use (`hooks/hooks.json:14-50`). PR-loop protected files are blocked by pattern in `hooks/lib/loop-common.sh:729-739` and surfaced through `pr_loop_prompt_blocked_message` at `hooks/lib/loop-common.sh:802-810`. Only one loop may be active, enforced by setup scripts via `find_active_loop` and `find_active_pr_loop` (`scripts/setup-pr-loop.sh:220-241`). `gen-plan` requires new output paths and non-empty input (`scripts/validate-gen-plan-io.sh:78-123`).
- dependencies_and_callers: README exposes command surface and workflow (`README.md:81-89`, `README.md:110-127`). Plugin metadata identifies this as the `humanize` plugin (`.claude-plugin/plugin.json:1-18`). `scripts/humanize.sh` sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6-10`) and implements monitor commands that consume PR-loop state (`scripts/humanize.sh:989-1119`). Tests are orchestrated by `tests/run-all-tests.sh:32-65`, including gen-plan, PR-loop, monitor, hook, cancellation, and robustness suites.
- edge_cases_or_failure_modes: Git operations may fail in non-repo/read-only exports; workflow scripts handle missing git/gh/codex/jq, missing PRs, fork PR base-repo resolution, unauthenticated `gh`, existing loops, YAML-unsafe branch/model values, and protected artifact modification attempts. PR-loop fetch/poll tolerate partial API failures by emitting warnings and empty arrays rather than crashing in some paths (`scripts/fetch-pr-comments.sh:175-203`, `scripts/poll-pr-reviews.sh:199-233`).
- validation_or_tests: `tests/test-gen-plan.sh` validates command/agent frontmatter plus `validate-gen-plan-io.sh` exit codes (`tests/test-gen-plan.sh:47-180`, `tests/test-gen-plan.sh:426-542`). `tests/test-pr-loop-system.sh` covers phase detection and PR goal tracker parsing (`tests/test-pr-loop-system.sh:251-416`). `tests/test-pr-loop-hooks.sh:102-130` verifies PR comment files are protected. `tests/run-all-tests.sh:32-65` lists the full validation surface. I did not execute tests because the task requested read-only research notes.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-031 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation entry for PR-loop sessions. It converts the newest active PR-loop from active state to cancelled state while preserving the loop directory.
- algorithmic_behavior: Parses `--force` and help (`scripts/cancel-pr-loop.sh:24-66`), resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` (`scripts/cancel-pr-loop.sh:72-73`), selects the newest `.humanize/pr-loop/*/` directory by reverse lexical sort (`scripts/cancel-pr-loop.sh:75-76`), requires `state.md` (`scripts/cancel-pr-loop.sh:88-98`), extracts round/iteration/PR metadata (`scripts/cancel-pr-loop.sh:104-112`), creates `.cancel-requested`, renames `state.md` to `cancel-state.md`, then prints `CANCELLED` (`scripts/cancel-pr-loop.sh:118-130`).
- inputs_outputs_state: Inputs are optional `--force`, environment `CLAUDE_PROJECT_DIR`, current working directory, and active session files. Outputs are stdout status tokens: `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`; filesystem outputs are `.cancel-requested` and `cancel-state.md`. State transition is `state.md` present -> `cancel-state.md` present, with `state.md` removed by `mv`.
- gates_or_invariants: It only operates under `.humanize/pr-loop`, explicitly not RLCR (`scripts/cancel-pr-loop.sh:54-56`). Exit code `1` means no active loop, `3` means invalid option, `0` means cancelled (`scripts/cancel-pr-loop.sh:11-15`, `scripts/cancel-pr-loop.sh:43-47`). The command wrapper states the script owns all cancellation logic and active means `state.md` in newest PR-loop directory (`commands/cancel-pr-loop.md:17-23`).
- dependencies_and_callers: Called by `/humanize:cancel-pr-loop` via `commands/cancel-pr-loop.md:1-15`. Related active-loop discovery helper exists in `hooks/lib/loop-common.sh:699-718`, though this script uses local `ls/sort/head` logic. Bash validators use the `.cancel-requested` convention to authorize state renames in broader cancellation-security tests.
- edge_cases_or_failure_modes: If a newer session directory exists but lacks `state.md`, older active sessions are ignored because only the newest directory is examined (`scripts/cancel-pr-loop.sh:75-98`). `--force` is parsed but currently has no behavioral effect (`scripts/cancel-pr-loop.sh:28-30`, `scripts/cancel-pr-loop.sh:40`). Metadata defaults to `?` if frontmatter keys are missing (`scripts/cancel-pr-loop.sh:109-112`). `ls`-based discovery may be fragile for unusual filenames, though timestamp session naming normally makes reverse sort correct.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:181-252` validates help output, no-loop exit/status, and active-loop cancellation with state rename. `commands/cancel-pr-loop.md:17-19` specifies consumer behavior based on first output line. Cancellation signal mechanics are tested for RLCR in `tests/test-cancel-signal-file.sh:75-255`, establishing the broader invariant that state renames require a local signal and other state edits remain blocked.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-061 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop-adjacent script argument parsing and minimal state behavior across setup, cancel, fetch, and poll scripts.
- algorithmic_behavior: Defines grouped test runners: `run_setup_tests` (`tests/test-pr-loop-scripts.sh:18-166`), `run_cancel_tests` (`tests/test-pr-loop-scripts.sh:172-253`), `run_fetch_tests` (`tests/test-pr-loop-scripts.sh:259-326`), `run_poll_tests` (`tests/test-pr-loop-scripts.sh:332-399`), and `run_script_tests` (`tests/test-pr-loop-scripts.sh:405-410`). Each test invokes the actual script, captures output/exit code, and calls shared `pass`/`fail` helpers expected from the sourced test harness.
- inputs_outputs_state: Inputs are `$PROJECT_ROOT`, `$TEST_DIR`, script files under `scripts/`, and temporary `.humanize/pr-loop/<timestamp>/state.md` fixtures. Outputs are pass/fail records. The main state transition fixture creates `state.md`, runs cancel, and expects `cancel-state.md` to exist while `state.md` no longer exists (`tests/test-pr-loop-scripts.sh:211-244`).
- gates_or_invariants: Setup must require at least one bot flag, reject unknown options, parse `--claude`, `--codex`, combined bot flags, `--max`, `--codex-model`, and `--codex-timeout` (`tests/test-pr-loop-scripts.sh:38-165`). Fetch must reject missing or invalid PR/output arguments (`tests/test-pr-loop-scripts.sh:279-325`). Poll must reject missing PR, `--after`, or `--bots` (`tests/test-pr-loop-scripts.sh:352-398`).
- dependencies_and_callers: Intended to be sourced and run by a broader harness per header (`tests/test-pr-loop-scripts.sh:11`). It assumes `pass` and `fail` functions exist and points to scripts through `PROJECT_ROOT`. It complements deeper PR-loop system/hook suites rather than testing GitHub API behavior directly.
- edge_cases_or_failure_modes: Some positive parse tests intentionally allow downstream failures because the environment may not be a Git repo or have GitHub credentials; they assert only that the parser did not reject the option (`tests/test-pr-loop-scripts.sh:66-90`). `exit_code=${exit_code:-0}` patterns prevent unset exit code when a command succeeds. The test uses `CLAUDE_PROJECT_DIR` to force cancellation lookup into the fixture root (`tests/test-pr-loop-scripts.sh:193-201`, `tests/test-pr-loop-scripts.sh:211-234`).
- validation_or_tests: This file is itself validation. It is included in broader test orchestration indirectly by PR-loop suites; `tests/run-all-tests.sh:51-54` lists gen-plan and PR-loop system suites, while this script exposes `run_script_tests` for harness-driven execution.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-091 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Block-message template enforcing the read-only contract for generated PR-loop prompt/comment files.
- algorithmic_behavior: Provides a deterministic markdown denial response: agents cannot write `round-*-pr-comment.md` or `round-*-prompt.md` under `.humanize/pr-loop/` (`prompt-template/block/pr-loop-prompt-write.md:1-9`). The text explains `pr-comment` files are fetched from GitHub and `prompt` files are generated instructions.
- inputs_outputs_state: Input is a hook/template-loader request for the block template. Output is markdown rendered to stderr/stdout by validators. It changes no state directly; its state effect is indirect by causing validators to exit with a blocking status.
- gates_or_invariants: Protects system-generated PR-loop files from agent writes. The broader invariant is implemented in `hooks/lib/loop-common.sh:729-739` for read-only PR round file patterns and `hooks/lib/loop-common.sh:802-810` for rendering this template. Bash validator applies the message when write-like commands target protected PR-loop artifacts (`hooks/loop-bash-validator.sh:417-442`).
- dependencies_and_callers: Loaded through `load_and_render_safe` by `pr_loop_prompt_blocked_message` (`hooks/lib/loop-common.sh:802-810`). Triggered by Write/Edit/Bash validators registered in `hooks/hooks.json:14-50`. It is sibling to other block templates for state file, prompt file, git status, and plan protections.
- edge_cases_or_failure_modes: The template mentions only `pr-comment` and `prompt`, but caller patterns also protect `codex-prompt`, `pr-check`, and `pr-feedback` (`hooks/loop-bash-validator.sh:417-429`), so the message is slightly narrower than enforcement. If template loading fails, `hooks/lib/loop-common.sh:804-807` contains a fallback message.
- validation_or_tests: `tests/test-pr-loop-hooks.sh:102-130` verifies attempts to Write a `round-0-pr-comment.md` file are blocked with exit code `2`. Repository search confirms this template is referenced from `hooks/lib/loop-common.sh:809`.
- skip_candidate: `no`

### IMPROVE_GEN_PLAN_COMMAND-HZ-121 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for RLCR and PR-loop displays, state detection, phase classification, YAML/frontmatter extraction, and goal-tracker summaries.
- algorithmic_behavior: Defines ANSI helpers (`scripts/lib/monitor-common.sh:12-23`), cross-platform file size and session lookup (`scripts/lib/monitor-common.sh:29-63`), terminal setup/restore (`scripts/lib/monitor-common.sh:69-92`), state-file classification (`scripts/lib/monitor-common.sh:147-188`), YAML value extraction (`scripts/lib/monitor-common.sh:194-209`), string/timestamp helpers (`scripts/lib/monitor-common.sh:215-252`), PR-loop phase detection (`scripts/lib/monitor-common.sh:258-342`), phase display text (`scripts/lib/monitor-common.sh:344-381`), generic goal tracker parsing (`scripts/lib/monitor-common.sh:387-463`), and PR goal tracker parsing (`scripts/lib/monitor-common.sh:465-498`).
- inputs_outputs_state: Inputs are session directories, state files, round output files, and goal tracker markdown. Outputs are strings such as `path|active`, `approved`, `waiting_reviewer`, or pipe-delimited tracker statistics. One small cache side effect writes `/tmp/humanize-phase-<session>-<round>.size` to detect whether latest `round-*-pr-check.md` is growing (`scripts/lib/monitor-common.sh:297-307`).
- gates_or_invariants: Active state takes priority over terminal `*-state.md` files (`scripts/lib/monitor-common.sh:162-181`). PR phase detection prioritizes final state files, then active Codex analysis by file growth/recent mtime, then `state.md` current round/startup case (`scripts/lib/monitor-common.sh:272-341`). Monitor display maps `approve-state.md` to `approved` and `maxiter-state.md` to `max-iterations` in `scripts/humanize.sh:970-987`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh:6-10`; PR monitor consumes `monitor_find_latest_session`, `monitor_find_state_file`, `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` in `scripts/humanize.sh:921-923`, `scripts/humanize.sh:970-987`, `scripts/humanize.sh:1056-1107`, and `scripts/humanize.sh:1194-1200`.
- edge_cases_or_failure_modes: Uses `find` instead of globs to avoid zsh no-match errors (`scripts/lib/monitor-common.sh:49-60`). Cross-platform `stat` supports Linux/macOS. Phase cache writes under `/tmp` can fail harmlessly due `|| true` (`scripts/lib/monitor-common.sh:306`). Numeric comparisons assume parseable `current_round`/`startup_case`; defaults are applied when missing (`scripts/lib/monitor-common.sh:321-338`). `monitor_find_state_file` returns the first `*-state.md` from `find`, so multiple terminal files are not ordered deterministically.
- validation_or_tests: `tests/test-pr-loop-system.sh:255-308` validates approved and waiting phases. `tests/test-pr-loop-system.sh:314-416` validates generic and PR goal tracker parsing. `tests/test-pr-loop-system.sh:422-451` validates active and approve state-file detection. Additional cancelled/maxiter phase tests appear at `tests/test-pr-loop-system.sh:454-486`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item-evidence headings present, one per assigned row, in the requested order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`