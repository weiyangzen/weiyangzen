# agent_28 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-028 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Agent prompt/policy for the `gen-plan` relevance gate. It defines a lightweight haiku-model checker that decides whether a user draft belongs to the current repository before a plan file is created.
- algorithmic_behavior: The agent must inspect repository context first, then compare the draft semantically against repository purpose, technologies, components, file paths, functions, or features. The prompt explicitly biases toward permissive classification: "if in doubt, lean toward marking as relevant" at `agents/draft-relevance-checker.md:31-36`.
- inputs_outputs_state: Input is draft content supplied by `commands/gen-plan.md` plus repository files readable through `Read`, `Glob`, and `Grep` declared in frontmatter at `agents/draft-relevance-checker.md:1-6`. Output is one exact verdict line: `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` at `agents/draft-relevance-checker.md:27-29`. It has no persistent state and performs no writes.
- gates_or_invariants: The invariant is exact-prefix output, because `commands/gen-plan.md:171-176` branches on `NOT_RELEVANT` versus `RELEVANT`. The checker should not require exact syntactic matches; semantic relationship is enough, per `agents/draft-relevance-checker.md:33-36`.
- dependencies_and_callers: Called by `commands/gen-plan.md` Phase 2, which instructs the Task tool to invoke `humanize:draft-relevance-checker` with haiku and repository exploration requirements at `commands/gen-plan.md:154-169`. The downstream transition is: `NOT_RELEVANT` stops plan generation, `RELEVANT` copies the template and appends the original draft at `commands/gen-plan.md:171-180`.
- edge_cases_or_failure_modes: Informal, multilingual, or rough drafts are accepted as valid inputs and should not be rejected for form alone at `agents/draft-relevance-checker.md:33-35`. The primary failure mode is malformed output lacking the expected prefix, because the command contract expects a parseable verdict. False negatives are intentionally minimized by the lenient rule.
- validation_or_tests: `tests/test-gen-plan.sh` checks the file exists, has the expected `name`, uses model `haiku`, declares tools, has required frontmatter, uses a valid model name, and contains no emoji/CJK characters at `tests/test-gen-plan.sh:111-116`, `tests/test-gen-plan.sh:254-287`, `tests/test-gen-plan.sh:421-427`, and `tests/test-gen-plan.sh:523-553`.
- skip_candidate: `no`

### HZ-058 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime setup script for `/humanize:start-rlcr-loop`. It initializes the RLCR loop session, validates user options and repository state, writes `.humanize/rlcr/<timestamp>/state.md`, seeds goal tracker and round contract/prompt files, and prints the initial work prompt.
- algorithmic_behavior: The script parses CLI flags at `scripts/setup-rlcr-loop.sh:188-320`, validates dependencies and active-loop exclusivity at `scripts/setup-rlcr-loop.sh:334-379`, validates plan path/content/tracking rules at `scripts/setup-rlcr-loop.sh:408-676`, captures branch/base commit state at `scripts/setup-rlcr-loop.sh:682-815`, then creates the loop directory and initial artifacts at `scripts/setup-rlcr-loop.sh:821-918`. It has two main modes: normal implementation mode and `--skip-impl` review-only mode. Normal mode embeds the plan into `round-0-prompt.md` and requires Goal Tracker plus Round Contract setup before coding at `scripts/setup-rlcr-loop.sh:1289-1348`. Skip-impl mode writes a review-only contract and prompt at `scripts/setup-rlcr-loop.sh:1202-1285`.
- inputs_outputs_state: Inputs include the plan path or `--plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--agent-teams`, BitLesson policy flags, and privacy mode. It reads project root from `resolve_project_root` supplied by `hooks/lib/loop-common.sh`; Codex defaults also come from that shared library. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `.pending-session-id`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, and `round-0-prompt.md`. State fields include `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `start_branch`, `base_branch`, `base_commit`, `review_started`, `ask_codex_question`, `agent_teams`, `privacy_mode`, BitLesson fields, stall/drift fields, and `started_at` at `scripts/setup-rlcr-loop.sh:880-907`.
- gates_or_invariants: Requires `codex`, `jq`, and `git` before setup at `scripts/setup-rlcr-loop.sh:340-363`. Only one active RLCR loop is allowed at `scripts/setup-rlcr-loop.sh:369-379`. Plan paths must be relative, space-free, shell-metacharacter-free, non-symlink, inside the project, outside submodules, and readable at `scripts/setup-rlcr-loop.sh:455-556`. Unless `--track-plan-file` is used, a real plan must be untracked/gitignored; tracked plan files are rejected at `scripts/setup-rlcr-loop.sh:582-607`. Plan content must have at least five physical lines and at least three non-comment content lines at `scripts/setup-rlcr-loop.sh:618-671`. Working tree must be clean apart from ignored `.humanize` runtime paths at `scripts/setup-rlcr-loop.sh:719-741`. Branch names and model values are YAML-safety checked at `scripts/setup-rlcr-loop.sh:692-716` and `scripts/setup-rlcr-loop.sh:799-805`. `--full-review-round` must be numeric and at least 2 at `scripts/setup-rlcr-loop.sh:256-270`.
- dependencies_and_callers: Invoked by `commands/start-rlcr-loop.md:109-115` after plan compliance and optional quiz checks. It sources `scripts/portable-timeout.sh` for `run_with_timeout` at `scripts/setup-rlcr-loop.sh:25-33` and `hooks/lib/loop-common.sh` for defaults, root resolution, template directory, and active-loop detection. It invokes `scripts/bitlesson-init.sh` and uses `templates/bitlesson.md` at `scripts/setup-rlcr-loop.sh:860-866`. `hooks/loop-post-bash-hook.sh` consumes `.humanize/.pending-session-id` to patch `session_id` into state. Stop/review behavior is enforced later by `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: Handles missing plan only when `--skip-impl` is present, creating an internal placeholder at `scripts/setup-rlcr-loop.sh:408-419` and `scripts/setup-rlcr-loop.sh:829-849`. Remote-only base branches are rejected because `codex review --base` requires a local ref at `scripts/setup-rlcr-loop.sh:748-765`. Git command timeouts fail closed for repo, status, tracking, and branch checks. `--agent-teams` is rejected unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` at `scripts/setup-rlcr-loop.sh:381-397`. Output uses a late `PIPE` trap so broken output pipes do not corrupt earlier validation at `scripts/setup-rlcr-loop.sh:1447-1449`.
- validation_or_tests: Covered by many setup-focused tests: `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, `tests/robustness/test-path-validation-robustness.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, `tests/robustness/test-base-branch-detection.sh`, `tests/test-task-tag-routing.sh`, `tests/test-agent-teams.sh`, `tests/test-session-id.sh`, and `tests/test-unified-codex-config.sh`. I inspected rather than executed tests because this research run is read-only notes only.
- skip_candidate: `no`

### HZ-088 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for the real RLCR monitor path. It validates that `_humanize_monitor_codex` behaves correctly when the runtime directory is deleted and when interrupt signals are sent, in both Bash and zsh.
- algorithmic_behavior: The test creates temporary fake projects under `/tmp/test-monitor-e2e-real-$$`, seeds `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, and matching cache log files, then writes small runner scripts that source `scripts/humanize.sh` and call the real `_humanize_monitor_codex` at `tests/test-monitor-e2e-real.sh:105-152`, `tests/test-monitor-e2e-real.sh:286-320`, `tests/test-monitor-e2e-real.sh:427-472`, and `tests/test-monitor-e2e-real.sh:608-638`. It uses shimmed `tput` and `clear` to make terminal behavior deterministic in noninteractive runs.
- inputs_outputs_state: Inputs are generated state/frontmatter files, goal trackers, fake cache logs under `$XDG_CACHE_HOME/humanize/<sanitized-project>/<timestamp>/`, and process signals or directory deletions. Outputs are captured monitor stdout/stderr files such as `$TEST_BASE/output1.txt`; counters `TESTS_PASSED` and `TESTS_FAILED`; and final exit code 0 when all checks pass. Temporary state is cleaned by `cleanup_test`, which kills related processes and removes `$TEST_BASE` at `tests/test-monitor-e2e-real.sh:43-51`.
- gates_or_invariants: The deletion tests require the monitor to exit within a bounded wait, print `Monitoring stopped:`, mention the directory no longer exists, avoid zsh/bash glob errors such as `no matches found` or `bad pattern`, display cleanup text, and return `EXIT_CODE:0` at `tests/test-monitor-e2e-real.sh:167-227` and `tests/test-monitor-e2e-real.sh:335-370`. SIGINT tests require exit or clean termination indication and no glob errors at `tests/test-monitor-e2e-real.sh:492-549` and `tests/test-monitor-e2e-real.sh:649-684`.
- dependencies_and_callers: Directly depends on `scripts/humanize.sh`, which sources `scripts/lib/monitor-common.sh` at `scripts/humanize.sh:6-10`. Wrapper tests split this file into deletion and SIGINT subsets via `tests/test-monitor-e2e-deletion.sh:1-21` and `tests/test-monitor-e2e-sigint.sh:1-21`.
- edge_cases_or_failure_modes: zsh may be absent, in which case zsh variants print skip messages at `tests/test-monitor-e2e-real.sh:238-240` and `tests/test-monitor-e2e-real.sh:560-562`. Bash SIGINT delivery can be process-group-sensitive; if SIGINT does not end the monitor, the test escalates to TERM/KILL and records a pass for handled SIGTERM because the comment treats delivery as complex at `tests/test-monitor-e2e-real.sh:492-518`. Tests use bounded loops to avoid hanging indefinitely after deletion or signals.
- validation_or_tests: This file is itself the validation artifact. It asserts real monitor behavior, not stubs, and final summary prints verified properties for clean exit, no glob errors, terminal restoration, Bash/zsh operation, and SIGINT handling at `tests/test-monitor-e2e-real.sh:688-724`.
- skip_candidate: `no`

### HZ-118 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block-message template used when the review-phase `codex review` command fails to produce the expected review result. It turns a tool/runtime failure into a user-visible blocked decision with debug pointers.
- algorithmic_behavior: The template renders a failure report with the Codex exit code, missing review result file, debug command/stdout/stderr file paths, and last stderr content. Its operational instruction is that the system will attempt another review when the user exits again at `prompt-template/block/codex-review-failed.md:1-18`.
- inputs_outputs_state: Inputs are template variables `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT`. Output is rendered markdown text returned as the `reason` in a JSON block decision from the stop hook. It does not mutate state by itself.
- gates_or_invariants: The invariant is fail-closed review behavior: if Codex review fails, the loop should block completion rather than letting the review phase be skipped. The template provides the diagnostic payload for that gate. Template substitution is single-pass through `hooks/lib/template-loader.sh:48-56` and `hooks/lib/template-loader.sh:188-210`, so variable values containing placeholder syntax are not recursively expanded.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/codex-review-failed.md"` at `hooks/loop-codex-stop-hook.sh:1639-1648`, then wrapped in JSON with decision `block` at `hooks/loop-codex-stop-hook.sh:1650-1658`. The fallback in the hook is more verbose and includes base branch/round context, but the assigned template is the active externalized message when present.
- edge_cases_or_failure_modes: The hook passes `CODEX_LOG_FILE` rather than the template’s `CODEX_STDOUT_FILE`, so `{{CODEX_STDOUT_FILE}}` may remain unresolved unless another caller supplies it; `{{CODEX_STDERR_FILE}}` also depends on caller variable names. Missing placeholders are intentionally preserved by the renderer per `hooks/lib/template-loader.sh:12-13` and `hooks/lib/template-loader.sh:118-120`, which makes unresolved debug labels visible instead of silently blank. If the template is absent or renders empty, the hook falls back to inline text.
- validation_or_tests: Indirectly exercised by stop-hook tests that cover review failure and template rendering behavior; direct caller line refs are in `hooks/loop-codex-stop-hook.sh:1639-1658`. No dedicated template-only test was found in the assigned inspection.
- skip_candidate: `no`

### HZ-148 `file` `prompt-template/block/wrong-contract-location.md`
- cursor: `[_]`
- core_role: Block-message template for the write validator when a round contract is written outside the active loop directory. It enforces the location contract for `round-N-contract.md`.
- algorithmic_behavior: The template tells the agent that round contract files must be in the active loop directory and renders the required `CORRECT_PATH` at `prompt-template/block/wrong-contract-location.md:1-5`.
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-contract.md` by the write validator. Output is a markdown block reason emitted to stderr with exit code 2. No persistent state changes happen in the template.
- gates_or_invariants: The invariant is that summary/contract files matching the round filename pattern cannot be created elsewhere; contract files must live under the active `.humanize/rlcr/<session>/` directory. This prevents stale or misplaced round contracts from satisfying the loop’s round-gate expectations.
- dependencies_and_callers: Used in `hooks/loop-write-validator.sh` when `IS_CONTRACT_FILE` is true and `IN_HUMANIZE_LOOP_DIR` is false at `hooks/loop-write-validator.sh:278-299`. The validator renders this template via `load_and_render_safe` with a fallback message at `hooks/loop-write-validator.sh:282-289`.
- edge_cases_or_failure_modes: If the template is missing, the fallback still blocks and prints the computed correct path. If the active loop/current round is wrong or stale, the rendered correct path could point to the wrong round; that risk is controlled upstream by active-loop and state parsing in the validator/common library, not by this template.
- validation_or_tests: Covered indirectly by write-validator tests and hook behavior around round file locations. The direct enforcement path is visible at `hooks/loop-write-validator.sh:282-299`.
- skip_candidate: `no`

### HZ-178 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared shell library for monitor implementations. It centralizes terminal color helpers, file/session discovery, state-file classification, YAML frontmatter extraction, timestamp/string formatting, and goal-tracker parsing.
- algorithmic_behavior: `monitor_find_latest_session` scans one directory level with `find`, filters timestamp-shaped session directories, and chooses the lexicographically latest timestamp at `scripts/lib/monitor-common.sh:37-63`. `monitor_find_state_file` classifies a session as `methodology-analysis`, `active`, a terminal stop reason from `*-state.md`, or `unknown` at `scripts/lib/monitor-common.sh:147-192`. `monitor_get_yaml_value` extracts frontmatter between `---` delimiters and greps a key at `scripts/lib/monitor-common.sh:198-213`. `parse_goal_tracker_issue_counts` and `parse_goal_tracker` count markdown table rows and AC/task sections at `scripts/lib/monitor-common.sh:262-383`.
- inputs_outputs_state: Inputs are filesystem paths: loop root, session directory, state markdown, goal tracker markdown, and arbitrary strings/timestamps. Outputs are shell-returned strings: file sizes, session paths, `state_file|status`, YAML values, formatted timestamps, truncated strings, and pipe-delimited parse summaries such as `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary`. It has no persistent state; it is sourced by callers.
- gates_or_invariants: Uses `find` rather than globs to avoid zsh `no matches found` failures at `scripts/lib/monitor-common.sh:49-60`. Missing loop/session/tracker files degrade to empty or zero summaries rather than shell errors at `scripts/lib/monitor-common.sh:44-47`, `scripts/lib/monitor-common.sh:157-160`, and `scripts/lib/monitor-common.sh:300-305`. Active states take precedence over terminal state files in `monitor_find_state_file` at `scripts/lib/monitor-common.sh:162-170`. Table counts subtract two header/separator rows and clamp active task counts to zero at `scripts/lib/monitor-common.sh:307-345`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh:6-10`, whose `_humanize_monitor_codex` uses `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` at `scripts/humanize.sh:279-281`, `scripts/humanize.sh:377-380`, and `scripts/humanize.sh:828-831`. Also referenced by `scripts/lib/monitor-skill.sh` for metadata parsing and timestamp formatting. Related tests include `tests/test-monitor-e2e-real.sh`, `tests/test-skill-monitor.sh`, and robustness tests for goal tracker parsing.
- edge_cases_or_failure_modes: `monitor_find_state_file` uses the first `find` result for `*-state.md`, so multiple terminal state files in one session are not deterministically ordered at `scripts/lib/monitor-common.sh:176-185`. `monitor_get_yaml_value` is simple grep/sed parsing and does not handle nested YAML or duplicate keys. Goal tracker parsing assumes specific heading names and markdown table shape; old-schema `### Open Issues` is treated as blocking-equivalent when no Blocking/Queued side issue rows exist at `scripts/lib/monitor-common.sh:285-292`.
- validation_or_tests: The real monitor e2e test validates the zsh-safe `find` approach, graceful missing-directory behavior, and terminal restoration through the monitor that consumes this library. Goal tracker parsing is also covered by `tests/robustness/test-goal-tracker-robustness.sh`, which sources production monitor parsing paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: HZ-028, HZ-058, HZ-088, HZ-118, HZ-148, HZ-178
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`