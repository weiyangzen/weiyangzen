# agent_17 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-017 `directory` `prompt-template/idea`
- cursor: `[_]`
- core_role: Idea-generation output template directory. It is not part of the RLCR stop/review state machine, but it is part of the broader humanize planning pipeline that turns an initial idea into a structured draft for later planning. The directory has one recursive child: `prompt-template/idea/gen-idea-template.md`.
- algorithmic_behavior: The child template defines the exact Markdown structure produced by `/humanize:gen-idea`: title, original idea, primary direction, rationale, approach summary, evidence, risks, alternatives, and synthesis notes. Placeholders appear in `gen-idea-template.md:1-31`, including `<ORIGINAL_IDEA>`, `<PRIMARY_RATIONALE>`, `<PRIMARY_OBJECTIVE_EVIDENCE>`, and `<SYNTHESIS_NOTES>`.
- inputs_outputs_state: Inputs are placeholder values selected by the command protocol, especially `IDEA_BODY`, primary proposal fields, alternative proposal fields, and generated title. Output is a single draft idea file. The command explicitly says to read `TEMPLATE_FILE` and replace placeholders at `commands/gen-idea.md:162-187`, then write `OUTPUT_FILE` once at `commands/gen-idea.md:189-191`.
- gates_or_invariants: Phase 4 selection gates choose the primary by evidence density, repo-pattern fit, surface area, and confidence at `commands/gen-idea.md:148-156`. The original idea must be preserved byte-identically when replacing `<ORIGINAL_IDEA>` at `commands/gen-idea.md:166-168`. Alternatives are renumbered without gaps after degraded proposals are dropped at `commands/gen-idea.md:142` and `commands/gen-idea.md:174-185`.
- dependencies_and_callers: `scripts/validate-gen-idea-io.sh` resolves the template path and fails with `TEMPLATE_NOT_FOUND` if absent at `scripts/validate-gen-idea-io.sh:176-187`. `commands/gen-idea.md` is the main caller/instruction surface and names `TEMPLATE_FILE` from Phase 1 stdout at `commands/gen-idea.md:164`.
- edge_cases_or_failure_modes: If fewer than two proposals survive parsing, the command stops with `exploration phase degraded; retry.` per `commands/gen-idea.md:135-140`. If the output file already exists, the output directory is missing, or write permission is absent, validation exits before generation at `scripts/validate-gen-idea-io.sh:158-173`. Missing template exits with code 7 at `scripts/validate-gen-idea-io.sh:183-186`.
- validation_or_tests: The validation script itself enforces the template existence contract and prints `TEMPLATE_FILE` on success at `scripts/validate-gen-idea-io.sh:189-197`. No assigned test file directly exercises rendered idea content; validation is mostly preflight IO/schema rather than full template rendering.
- skip_candidate: `yes: directory is algorithm-adjacent planning/template infrastructure, not an RLCR state-machine hook, validator, or executable state-transition specification`

### HZ-047 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse read gate for the RLCR loop. It prevents Claude from reading stale, wrong-session, wrong-round, wrong-location, todo, goal-tracker, finalize-contract, and methodology-analysis-restricted files.
- algorithmic_behavior: The script reads hook JSON from stdin, validates JSON and nesting, exits early unless `tool_name` is `Read`, and requires `tool_input.file_path` at `hooks/loop-read-validator.sh:23-46`. It lowercases the path, extracts `session_id`, and applies special gates before general round-file validation. Todos are blocked unless active-loop allowlisted at `hooks/loop-read-validator.sh:56-64`. Methodology-analysis mode restricts project/loop reads to only methodology artifacts at `hooks/loop-read-validator.sh:67-169`. General RLCR files are then checked against the active loop, current round, correct file location, and canonical directory path at `hooks/loop-read-validator.sh:175-324`.
- inputs_outputs_state: Input is Claude hook JSON. Output is either silent allow with exit 0, hard validation failure with exit 1, or policy block with rendered stderr and exit 2. It does not mutate RLCR state. It reads active state via `resolve_active_state_file`, then parses it with `parse_state_file_strict` to set `STATE_CURRENT_ROUND` at `hooks/loop-read-validator.sh:204-216`.
- gates_or_invariants: Non-Read tools pass through at `hooks/loop-read-validator.sh:37-39`. Invalid JSON, missing `tool_name`, missing `file_path`, or deep JSON fail before path logic through `validate_hook_input`, `require_tool_input_field`, and `is_deeply_nested` at `hooks/lib/loop-common.sh:91-165`. In finalize phase, round contracts are unreadable at `hooks/loop-read-validator.sh:218-221`. Goal tracker reads must target the active loop tracker exactly at `hooks/loop-read-validator.sh:227-244`. Round files must be in `.humanize/rlcr`, match the current round unless allowlisted, and compare equal after prefix canonicalization at `hooks/loop-read-validator.sh:270-321`.
- dependencies_and_callers: Loaded as a PreToolUse hook in `hooks/hooks.json:38`. It sources `hooks/lib/loop-common.sh` at `hooks/loop-read-validator.sh:15-18`, relying on `resolve_project_root`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, `is_round_file_type`, `extract_round_number`, `is_allowlisted_file`, rendered block templates, and `canonicalize_path_prefix`. The canonicalization helper intentionally resolves only parent prefixes at `hooks/lib/project-root.sh:55-84`.
- edge_cases_or_failure_modes: If there is no active loop for a session, restricted checks mostly allow exit 0, avoiding cross-session interference. Methodology-analysis path handling attempts `realpath`, then parent `realpath`; if unresolved paths contain `..` or are symlinks, it blocks fail-closed at `hooks/loop-read-validator.sh:89-116`. If state parsing is malformed, it blocks for safety at `hooks/loop-read-validator.sh:211-215`. Allowlisted historical files are limited to `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop at `hooks/lib/loop-common.sh:819-837`.
- validation_or_tests: `tests/test-allowlist-validators.sh` exercises allowlisted and blocked paths directly at `tests/test-allowlist-validators.sh:72-134`, then the read validator for allowed todos/current contract and blocked stale todos/summary/contract at `tests/test-allowlist-validators.sh:294-370`. Finalize read behavior is tested at `tests/test-finalize-phase.sh:1064-1088`. Hook JSON robustness for valid JSON, invalid JSON, missing fields, Unicode, deep nesting, non-UTF8, and null-byte handling is covered at `tests/robustness/test-hook-input-robustness.sh:35-115` and `tests/robustness/test-hook-input-robustness.sh:149-235`.
- skip_candidate: `no`

### HZ-077 `file` `tests/test-commit-history-section.sh`
- cursor: `[_]`
- core_role: Executable specification for the Integral context block inserted into Codex review prompts. It verifies how stop-hook review context summarizes commits since loop start and recent RLCR round artifacts.
- algorithmic_behavior: The test creates a temporary git repository, computes `COMMIT_HISTORY`, builds `RECENT_ROUND_FILES`, renders `prompt-template/codex/commit-history-section.md` through `load_and_render_safe`, and asserts the rendered section includes the correct commit and round history. This mirrors stop-hook logic found at `hooks/loop-codex-stop-hook.sh:1038-1064`.
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `BASE_COMMIT`, git history, `LOOP_TIMESTAMP`, template content, and fallback text. Output is test pass/fail status via `pass`, `fail`, and `print_test_summary`; it mutates only temporary test repos/files under the test helper’s temp directory.
- gates_or_invariants: Round 0 must show `(no commits yet)` and `(first round, no prior history)` at `tests/test-commit-history-section.sh:33-61`. Round 3 must include all three commits and round 0-2 summary/review-result references at `tests/test-commit-history-section.sh:63-114`. Recent round references are bounded to the previous three rounds by the loop condition at `tests/test-commit-history-section.sh:80-85` and `tests/test-commit-history-section.sh:230-240`.
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/template-loader.sh` at `tests/test-commit-history-section.sh:14-18`. It exercises `prompt-template/codex/commit-history-section.md`, whose rendered contract is “Development History (Integral Context)” plus commit log and recent round files at `prompt-template/codex/commit-history-section.md:1-12`. The production caller is the stop hook’s Codex prompt assembly at `hooks/loop-codex-stop-hook.sh:1055-1119`.
- edge_cases_or_failure_modes: Corrupted or unrelated `BASE_COMMIT` must not crash under `set -e`; the logic falls back to recent branch commits with a “base commit unavailable” annotation at `tests/test-commit-history-section.sh:117-147` and `tests/test-commit-history-section.sh:149-179`. Missing template must still render a full fallback section with commits, recent round files, and the directive to read them at `tests/test-commit-history-section.sh:181-222`. Empty legacy `BASE_COMMIT` uses the same fallback at `tests/test-commit-history-section.sh:247-266`.
- validation_or_tests: This file is itself the validation surface. It contains six named test cases and exits through `print_test_summary "Commit History Section (I Component) Tests"` at `tests/test-commit-history-section.sh:268-271`.
- skip_candidate: `no`

### HZ-107 `file` `hooks/lib/loop-bg-tasks.sh`
- cursor: `[_]`
- core_role: Background-task control helper for the RLCR stop hook. It decides whether the stop hook should short-circuit because Claude Code has asynchronous Agent/Bash work pending, and it manages parked-loop marker behavior across sessions.
- algorithmic_behavior: The file defines path and transcript helpers, pending-task extraction, liveness pruning, count/boolean wrappers, and `handle_bg_task_short_circuit`. It derives loop start timestamps from local loop-dir names into UTC ISO strings at `hooks/lib/loop-bg-tasks.sh:65-95`, derives Claude task-output dirs from transcript paths at `hooks/lib/loop-bg-tasks.sh:97-118`, extracts launched async agent/background shell task IDs and subtracts SDK/legacy completion notifications at `hooks/lib/loop-bg-tasks.sh:156-264`, then applies stop-hook guard ordering at `hooks/lib/loop-bg-tasks.sh:299-435`.
- inputs_outputs_state: Inputs are loop directory, hook JSON, hook session ID, transcript path, optional `since_ts`, Claude transcript JSONL, `/tmp/claude-<uid>/.../tasks` output files, and optional `LSOF_BIN`. Outputs include pending task IDs/counts on stdout for helper functions, JSON `systemMessage` from stop-hook short-circuits, exit 0 when a guard handles the stop, and possible creation/removal of `bg-pending.marker` at `hooks/lib/loop-bg-tasks.sh:401-404` and `hooks/lib/loop-bg-tasks.sh:428-433`.
- gates_or_invariants: Missing transcript, non-file transcript, or missing `jq` causes pending detection to return non-zero so callers do not infer “no pending” from bad evidence at `hooks/lib/loop-bg-tasks.sh:187-207`. Output-file liveness is fail-open unless an output file exists and `lsof` reports no holders at `hooks/lib/loop-bg-tasks.sh:120-141`. The stop-hook guards run in strict order: ambiguous caller with existing marker exits silently, foreign parked session emits parked message, current pending background tasks write marker and exit, same-session verified no-pending removes stale marker at `hooks/lib/loop-bg-tasks.sh:322-435`.
- dependencies_and_callers: Sourced automatically at the end of `hooks/lib/loop-common.sh:1568-1578`. The stop hook calls `handle_bg_task_short_circuit "$LOOP_DIR" "$HOOK_INPUT" "$HOOK_SESSION_ID"` before normal phase/gate logic at `hooks/loop-codex-stop-hook.sh:71-83`. It relies on `FIELD_SESSION_ID`, `resolve_active_state_file`, `find_active_loop` marker fallback semantics, `jq`, `date`, `id`, optional `lsof`, `grep`, `sed`, `sort`, and `comm`.
- edge_cases_or_failure_modes: It handles tilde transcript paths at `hooks/lib/loop-bg-tasks.sh:23-46`, local-time loop directory names compared against UTC transcript timestamps at `hooks/lib/loop-bg-tasks.sh:48-95`, malformed/missing transcript preservation of markers by requiring a successful fresh pending check before cleanup at `hooks/lib/loop-bg-tasks.sh:416-431`, cross-session marker preservation at `hooks/lib/loop-bg-tasks.sh:338-367`, and old transcript launches before loop start via `since_ts` filtering at `hooks/lib/loop-bg-tasks.sh:179-185` and `hooks/lib/loop-bg-tasks.sh:210-216`.
- validation_or_tests: `tests/test-stop-hook-bg-allow.sh` is the main spec. Its acceptance matrix covers no dispatch, pending subagent/shell, completions, missing transcripts, finalize phase, tilde paths, cross-session parking, marker cleanup, SDK and legacy completion formats, malformed transcript fail-closed behavior, timestamp filtering, wrapper behavior, and liveness probes at `tests/test-stop-hook-bg-allow.sh:12-34`. Representative assertions are pending task short-circuit at `tests/test-stop-hook-bg-allow.sh:365-432`, cross-session/marker behavior at `tests/test-stop-hook-bg-allow.sh:610-835`, timestamp conversion and pre-loop filtering at `tests/test-stop-hook-bg-allow.sh:1218-1308`, and liveness probe cases at `tests/test-stop-hook-bg-allow.sh:1410-1445`.
- skip_candidate: `no`

### HZ-137 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Rendered block message for the RLCR plan-file immutability gate. It explains why the stop hook blocks when the original plan diverges from the loop backup.
- algorithmic_behavior: The template substitutes `{{PLAN_FILE}}` and `{{BACKUP_PATH}}` into a Markdown block that says modifying plan files is forbidden during an active session and instructs the user to cancel, update the plan, and restart with the same plan path. Template content is at `prompt-template/block/plan-file-modified.md:1-12`.
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH` passed by the stop hook. Output is a rendered `reason` string embedded into JSON with `decision: block` and `systemMessage: "Loop: Blocked - plan file modified"` at `hooks/loop-codex-stop-hook.sh:377-395`. The template itself has no state mutation.
- gates_or_invariants: The gate fires when `diff -q "$FULL_PLAN_PATH" "$BACKUP_PLAN"` detects content divergence at `hooks/loop-codex-stop-hook.sh:377-378`. For tracked plan files, uncommitted git status is separately blocked earlier at `hooks/loop-codex-stop-hook.sh:361-374`. The invariant is that the plan used to start the loop remains stable during active RLCR execution.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md"` at `hooks/loop-codex-stop-hook.sh:391-393`. Rendering semantics are single-pass variable substitution via `hooks/lib/template-loader.sh:56-135`; missing/empty template falls back to an inline message at `hooks/loop-codex-stop-hook.sh:379-390`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` emits the fallback at `hooks/lib/template-loader.sh:188-210`. The caller has a comment that “plan changes are now allowed” because `plan.md` is a symlink, but the actual code still blocks on diff mismatch at `hooks/loop-codex-stop-hook.sh:377-395`; this is a documentation/comment ambiguity worth noting, not a template behavior.
- validation_or_tests: Template reference coverage includes `tests/test-template-references.sh` search results for `loop-read-validator.sh`, but no assigned test directly validates this specific block. Broader plan-file robustness tests exist elsewhere; this item’s direct validation is primarily the stop-hook caller using safe fallback rendering.
- skip_candidate: `no`

### HZ-167 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude prompt template for RLCR review phase after Codex finds issues. It defines the transition contract from Codex review result to Claude remediation work.
- algorithmic_behavior: The template forces a re-anchor on the original plan, goal tracker, and round contract before code changes at `prompt-template/claude/review-phase-prompt.md:5-12`; injects `{{REVIEW_CONTENT}}` at `prompt-template/claude/review-phase-prompt.md:14-16`; classifies findings as blocking side issues or queued side issues at `prompt-template/claude/review-phase-prompt.md:18-24`; requires `[blocking]` or `[queued]` task lane tags and restricts new `[mainline]` tasks at `prompt-template/claude/review-phase-prompt.md:26-32`; and requires summary output at `{{SUMMARY_FILE}}` at `prompt-template/claude/review-phase-prompt.md:34-50`.
- inputs_outputs_state: Inputs are `REVIEW_CONTENT`, `SUMMARY_FILE`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, and `CURRENT_ROUND` passed by the stop hook. Output is the next round prompt file written by the hook redirection at `hooks/loop-codex-stop-hook.sh:1551-1558`. Subsequent Claude actions update code, commit fixes, and write a summary; the template itself only constrains those actions.
- gates_or_invariants: Review findings do not automatically become the new round objective; the round contract must preserve a single mainline objective at `prompt-template/claude/review-phase-prompt.md:12`. Queued issues may be documented but must not take over the round at `prompt-template/claude/review-phase-prompt.md:24`. The COMPLETE signal is explicitly ignored during review phase at `prompt-template/claude/review-phase-prompt.md:53-58`.
- dependencies_and_callers: Rendered by the local `write_review_phase_prompt` logic in `hooks/loop-codex-stop-hook.sh`, which provides an inline fallback and writes to `round-N-prompt.md` at `hooks/loop-codex-stop-hook.sh:1532-1558`. `load_and_render_safe` ensures fallback rendering if the file is missing or empty at `hooks/lib/template-loader.sh:188-210`. The template may be followed by BitLesson-required appended instructions if `BITLESSON_REQUIRED` is true at `hooks/loop-codex-stop-hook.sh:1559-1565`.
- edge_cases_or_failure_modes: If `REVIEW_CONTENT` contains template-like braces, single-pass rendering prevents placeholder injection or recursive substitution per `hooks/lib/template-loader.sh:52-55` and `hooks/lib/template-loader.sh:69-128`. If the template is unavailable, the fallback still includes re-anchor, contract refresh, blocking-only focus, commit, and summary instructions at `hooks/loop-codex-stop-hook.sh:1534-1549`.
- validation_or_tests: `tests/test-agent-teams.sh:718` references this template behavior indirectly to ensure review-phase prompts do not include agent-teams content. `tests/test-template-loader.sh` has a realistic `REVIEW_CONTENT` template-syntax scenario referenced at `tests/test-template-loader.sh:525-534`, validating the rendering invariant this prompt depends on.
- skip_candidate: `no`

### HZ-197 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It validates tolerant and strict parser behavior used by validators and stop-hook state transitions.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, creates many temporary state files, and exercises `parse_state_file`, `parse_state_file_strict`, and `get_current_round` across valid, malformed, partial, missing, and extended-schema state frontmatter. Setup is at `tests/robustness/test-state-file-robustness.sh:14-23`.
- inputs_outputs_state: Inputs are generated `state.md`-style files with YAML frontmatter fields such as `current_round`, `max_iterations`, `plan_file`, `start_branch`, `review_started`, `base_branch`, `full_review_round`, and drift-tracking fields. Outputs are shell test pass/fail messages and process exit from `print_test_summary` at `tests/robustness/test-state-file-robustness.sh:525-530`. Temporary files are created only under the test directory.
- gates_or_invariants: Tolerant parsing applies defaults for missing `current_round` and `max_iterations`, while strict parsing rejects missing YAML separators, missing required fields, and non-numeric values. The production parser implements defaults at `hooks/lib/loop-common.sh:494-521` and strict validation at `hooks/lib/loop-common.sh:533-599`. The test asserts valid full fields at `tests/robustness/test-state-file-robustness.sh:33-60`, extra fields ignored at `tests/robustness/test-state-file-robustness.sh:72-98`, quoted values at `tests/robustness/test-state-file-robustness.sh:100-122`, and round 0 handling at `tests/robustness/test-state-file-robustness.sh:124-139`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-state-file-robustness.sh:14-18`. The parser is used by read/write/edit/bash validators and the Codex stop hook; for example, `loop-read-validator.sh` uses `parse_state_file_strict` before authorizing current-round reads at `hooks/loop-read-validator.sh:204-216`.
- edge_cases_or_failure_modes: Covers missing frontmatter strict rejection and tolerant defaulting at `tests/robustness/test-state-file-robustness.sh:149-171`, non-numeric `current_round` strict rejection and tolerant non-crash at `tests/robustness/test-state-file-robustness.sh:173-199`, missing required fields at `tests/robustness/test-state-file-robustness.sh:201-228`, empty/partial/malformed files at `tests/robustness/test-state-file-robustness.sh:230-275`, huge and negative numbers at `tests/robustness/test-state-file-robustness.sh:277-312`, binary suffixes and CRLF at `tests/robustness/test-state-file-robustness.sh:367-397`, and schema extensions/defaults for `full_review_round` and drift fields at `tests/robustness/test-state-file-robustness.sh:399-523`.
- validation_or_tests: This file is itself the validation suite for state parsing. It contains 23 named cases and finalizes with `State File Robustness Test Summary` at `tests/robustness/test-state-file-robustness.sh:525-530`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7/7 Item Evidence headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`