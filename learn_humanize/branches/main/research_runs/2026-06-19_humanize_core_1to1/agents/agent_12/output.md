# agent_12 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-012 `file` `README.md`
- cursor: `[_]`
- core_role: Top-level behavior documentation for Humanize and the RLCR workflow. It defines RLCR as “Ralph-Loop with Codex Review” and frames the core loop as Claude implementation plus independent Codex review, with issues feeding back into further implementation rounds; see `README.md:9-27`.
- algorithmic_behavior: Documents the intended workflow sequence: optionally generate an idea draft, generate a plan, refine annotated plans, start the RLCR loop, optionally consult Gemini, and monitor loop progress; see `README.md:42-77`. It establishes that implementation and review are separate phases and that iteration continues until acceptance criteria are met.
- inputs_outputs_state: Inputs are user ideas, draft or plan Markdown files, reviewer comments, a plan path passed to `/humanize:start-rlcr-loop`, and optional Gemini questions. Outputs are generated idea drafts under `.humanize/ideas/`, plan documents, RLCR loop artifacts, Codex review feedback, and monitor views. State is described conceptually rather than structurally: idea → plan → refined plan → implementation/review loop → monitored progress.
- gates_or_invariants: The README states the human must understand the plan before execution, keeping the human as architect; see `README.md:18`. It also defines “One Build + One Review” as an invariant: Claude implements and Codex independently reviews; see `README.md:16`.
- dependencies_and_callers: Depends on Claude Code slash commands, Codex CLI for review, optional Gemini CLI, docs under `docs/usage.md`, install guides, and the monitor script sourced from `scripts/humanize.sh`; see `README.md:40`, `README.md:70-77`, and `README.md:85-92`.
- edge_cases_or_failure_modes: This file does not encode detailed failure handling. It points readers to deeper docs for configuration and install prerequisites. It contains one minor documentation typo in the shell startup file example: `.bashec` appears where `.bashrc` was likely intended at `README.md:72`; not algorithmically material.
- validation_or_tests: Not an executable spec. It is validated indirectly by consistency with commands and docs referenced from `docs/usage.md`, install docs, and command files.
- skip_candidate: `no`

### HZ-042 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash validator for the RLCR state machine. It prevents Bash from bypassing Write/Edit/read-only hook policy, protects RLCR state files, prevents direct hook execution, and enforces phase-specific shell restrictions; see `hooks/loop-bash-validator.sh:1-10`.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON and nesting, exits early for non-Bash tools, then requires `tool_input.command`; see `hooks/loop-bash-validator.sh:22-46`. It resolves the project root, extracts `session_id`, and finds the active loop for that session; see `hooks/loop-bash-validator.sh:52-60`. During methodology analysis, it blocks file-modifying Bash, git write commands, interpreters, shell scripts, build tools, source/dot commands, direct script execution, and unsafe redirection, while allowing a tightly constrained direct `cancel-rlcr-loop.sh` invocation; see `hooks/loop-bash-validator.sh:64-170`. With an active RLCR loop, it blocks direct execution of stop/gate hook scripts even through wrappers such as `env`, `command`, `timeout`, `nice`, `nohup`, tracing tools, or shell launchers; see `hooks/loop-bash-validator.sh:184-201`.
- inputs_outputs_state: Input is hook JSON containing at least `tool_name` and, for Bash, `tool_input.command`; optional `session_id` controls loop filtering. State inputs are `.humanize/rlcr/<loop>/methodology-analysis-state.md`, `finalize-state.md`, or `state.md`, plus loop files such as `goal-tracker.md`, round prompts, summaries, contracts, todos, and plan backups. Outputs are process exits: `0` allow/pass-through, `1` invalid input or malformed state safety failure, and `2` blocked command with a human-facing message on stderr. It does not mutate loop state itself except by allowing a separately authorized cancel path to proceed.
- gates_or_invariants: Enforces fail-closed strict state parsing before loop-specific policy by calling `parse_state_file_strict`; malformed state blocks operation for safety at `hooks/loop-bash-validator.sh:211-217`. Blocks `git push` unless `push_every_round` is true; see `hooks/loop-bash-validator.sh:225-237`. Blocks `git add` attempts that target `.humanize`, including force-add bypasses, through `git_adds_humanize`; see `hooks/loop-bash-validator.sh:241-249` and shared helper behavior at `hooks/lib/loop-common.sh:1270-1325`. Protects `methodology-analysis-state.md`, `finalize-state.md`, and `state.md` both as write destinations and as `mv`/`cp` sources; see `hooks/loop-bash-validator.sh:271-297`, `hooks/loop-bash-validator.sh:310-456`, and shell-wrapper detection at `hooks/loop-bash-validator.sh:458-488`. Blocks Bash modification of plan backup, active goal tracker, round prompts, summaries, round contracts, and most todos paths; see `hooks/loop-bash-validator.sh:490-568`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-bash-validator.sh:14-16`. It relies on shared helpers `validate_hook_input`, `is_deeply_nested`, `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, `command_modifies_file`, `is_cancel_authorized`, `git_adds_humanize`, and template-backed message functions. Relevant shared helper definitions are in `hooks/lib/loop-common.sh:91-165`, `hooks/lib/loop-common.sh:253-280`, `hooks/lib/loop-common.sh:332-360`, `hooks/lib/loop-common.sh:533-570`, `hooks/lib/loop-common.sh:1051-1085`, and `hooks/lib/loop-common.sh:1485-1531`. It also depends on `jq`, `grep`, `sed`, `git`, template loading, and `prompt-template/block/git-push.md`, `prompt-template/block/plan-backup-protected.md`, and `prompt-template/block/round-contract-bash-write.md`.
- edge_cases_or_failure_modes: If the project root cannot be resolved, it exits allow/pass-through at `hooks/loop-bash-validator.sh:52`; this avoids blocking unrelated shell contexts. If no active loop exists, it allows all Bash commands at `hooks/loop-bash-validator.sh:173-175`. Methodology analysis explicitly accepts two limitations: read-only Bash is not blocked, and spawned agents in different sessions are not restricted by these hooks; see `hooks/loop-bash-validator.sh:68-75`. The cancel exception is intentionally narrow and rejects metacharacters, command substitution, redirection, and multi-line payloads; see `hooks/loop-bash-validator.sh:81-105` and `hooks/lib/loop-common.sh:1051-1085`. Command parsing is regex-based, so the implementation adds layered defenses such as segment splitting and redirection stripping for `mv`/`cp` state-file bypasses; see `hooks/loop-bash-validator.sh:316-426`.
- validation_or_tests: Covered directly by robustness tests that create active loop state and assert Bash blocks state modification, goal-tracker modification after round 0, and old-session protections; see `tests/robustness/test-hook-system-robustness.sh:316-416` and `tests/robustness/test-hook-system-robustness.sh:538-565`. Template references used by this validator are scanned by `tests/test-template-references.sh:56-64` and `tests/test-template-references.sh:83-110`.
- skip_candidate: `no`

### HZ-072 `file` `tests/test-bitlesson-select-routing.sh`
- cursor: `[_]`
- core_role: Executable specification for BitLesson selector provider routing. It verifies that `scripts/bitlesson-select.sh` chooses Codex or Claude correctly, preserves stdin prompt transport, handles missing provider binaries, supports codex-only mode, short-circuits empty knowledge bases, and invokes Codex in a hook-safe read-only mode.
- algorithmic_behavior: Builds temporary BitLesson fixtures and mock `codex`/`claude` binaries; see `tests/test-bitlesson-select-routing.sh:18-134`. It then invokes `scripts/bitlesson-select.sh` with `--task`, `--paths`, and `--bitlesson-file`, under controlled `CLAUDE_PROJECT_DIR`, `XDG_CONFIG_HOME`, and `PATH`. Tests assert stable selector output containing `LESSON_IDS:` and `RATIONALE:`. Codex routing for `gpt-*` is tested at `tests/test-bitlesson-select-routing.sh:136-162`; stdin prompt transport with trailing `-` is tested at `tests/test-bitlesson-select-routing.sh:164-197`; Claude routing for haiku, sonnet, and uppercase OPUS is tested at `tests/test-bitlesson-select-routing.sh:199-284`.
- inputs_outputs_state: Inputs include temp `.humanize/config.json` values such as `bitlesson_model`, `codex_model`, and `provider_mode`; BitLesson files either under `.humanize/bitlesson.md` or a direct test path; PATH-scoped mock executables; and CLI task/path arguments. Outputs are test pass/fail counters plus selector stdout/stderr. The tested selector emits exactly two normalized lines after parsing provider output; implementation output enforcement is in `scripts/bitlesson-select.sh:242-267`.
- gates_or_invariants: Unknown model names must fail nonzero with a clear error; see `tests/test-bitlesson-select-routing.sh:286-310` and provider detection in `scripts/lib/model-router.sh:10-30`. A configured Codex model with no `codex` binary must fail nonzero and mention Codex; see `tests/test-bitlesson-select-routing.sh:312-346`. A Claude model with missing `claude` binary falls back to Codex if Codex exists; see `tests/test-bitlesson-select-routing.sh:348-383` and implementation fallback at `scripts/bitlesson-select.sh:126-131`. `provider_mode: codex-only` forces Codex even when the BitLesson model is Claude-like; see `tests/test-bitlesson-select-routing.sh:389-413` and `scripts/bitlesson-select.sh:115-120`. Placeholder BitLesson files with no lesson entries must return `LESSON_IDS: NONE` without invoking a model; see `tests/test-bitlesson-select-routing.sh:416-437` and implementation short-circuit at `scripts/bitlesson-select.sh:105-109`.
- dependencies_and_callers: Depends on `scripts/bitlesson-select.sh`, `tests/test-helpers.sh`, shell tools, `jq`, temp directories, and mocked CLI binaries. `bitlesson-select.sh` sources config and model-router libraries at `scripts/bitlesson-select.sh:9-13`, loads merged config at `scripts/bitlesson-select.sh:20-26`, builds a selector prompt at `scripts/bitlesson-select.sh:148-180`, and runs Codex or Claude at `scripts/bitlesson-select.sh:188-222`.
- edge_cases_or_failure_modes: Uses `SAFE_BASE_PATH` to avoid accidentally finding user-local real Codex/Claude during missing-binary tests; see `tests/test-bitlesson-select-routing.sh:9-11`. The Codex invocation test captures args and requires hook disabling, git repo check skipping, ephemeral mode, read-only sandboxing, and absence of `--full-auto`; see `tests/test-bitlesson-select-routing.sh:440-494`. The implementation probes CLI support before adding optional flags; see `scripts/bitlesson-select.sh:193-211`.
- validation_or_tests: This file is itself the validation asset. It ends with `print_test_summary "Bitlesson Select Routing Test Summary"` at `tests/test-bitlesson-select-routing.sh:496`, which returns failure if any assertion fails via the shared helper in `tests/test-helpers.sh:58-77`.
- skip_candidate: `no`

### HZ-102 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable template integrity validator. It protects hook UX and safety behavior by ensuring hook scripts do not reference missing prompt templates and that critical validators use safe rendering fallbacks.
- algorithmic_behavior: Scans a fixed set of shell scripts that may render templates: stop hook, read/write/edit/bash validators, and `hooks/lib/loop-common.sh`; see `tests/test-template-references.sh:56-64`. It skips comments, matches literal `load_template`, `load_and_render`, and `load_and_render_safe` calls using `"$TEMPLATE_DIR"` as the first argument, extracts the second quoted template path, and checks for file existence under `prompt-template`; see `tests/test-template-references.sh:83-110`. It then enumerates every Markdown template under `prompt-template` and reports whether it is directly referenced from hooks; see `tests/test-template-references.sh:121-142`.
- inputs_outputs_state: Inputs are repository-local hook scripts and `prompt-template/**/*.md`. Outputs are colored PASS/FAIL/WARN lines, missing-template detail, and exit status. State is held in counters `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, plus arrays `MISSING_TEMPLATES`, `TEMPLATE_FILES`, and `UNREFERENCED`; see `tests/test-template-references.sh:25-27`, `tests/test-template-references.sh:71-72`, and `tests/test-template-references.sh:121-132`.
- gates_or_invariants: Any referenced template missing from disk is a failure; see `tests/test-template-references.sh:102-107`. A fixed common-template list must exist, covering todos, prompt write, state modification, summary Bash write, goal tracker Bash write, and goal tracker modification templates; see `tests/test-template-references.sh:149-167`. Critical read/write/edit validators must use `load_and_render_safe` for template rendering rather than unsafe `load_and_render`; see `tests/test-template-references.sh:172-201`. Warnings for unreferenced templates do not fail the test; see `tests/test-template-references.sh:134-141`.
- dependencies_and_callers: Depends on the prompt-template tree, hook scripts, `find`, `grep`, `sed`, and shell arrays. It indirectly validates templates used by `hooks/loop-bash-validator.sh`, including `block/git-push.md`, `block/plan-backup-protected.md`, and `block/round-contract-bash-write.md`.
- edge_cases_or_failure_modes: The scanner recognizes only literal quoted template paths following `"$TEMPLATE_DIR"` on a single line; dynamic template names or nonstandard formatting may be missed. Section 2 warns on templates not directly referenced by hooks even if they are used dynamically or outside hooks; see `tests/test-template-references.sh:130-140`. The script uses `set -uo pipefail` rather than `set -e`, so it can accumulate multiple failures before returning at summary; see `tests/test-template-references.sh:12` and `tests/test-template-references.sh:221-230`.
- validation_or_tests: This file is itself the validation command. It exits success only when `TESTS_FAILED` is zero; see `tests/test-template-references.sh:221-230`.
- skip_candidate: `no`

### HZ-132 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the large-file gate. It defines the user-facing remediation contract when an RLCR stop attempt finds changed code or documentation files exceeding the configured line limit.
- algorithmic_behavior: Receives `{{MAX_LINES}}` and `{{LARGE_FILES}}` placeholders and renders a blocking instruction: split oversized code into smaller modules, preserve behavior strictly, optionally use `code-simplifier`, maintain module boundaries, split documentation into logical sections, keep cross-references/narrative flow, update TOC/navigation, then commit and attempt exit again; see `prompt-template/block/large-files.md:1-25`.
- inputs_outputs_state: Inputs are the maximum allowed line count and a generated bullet list of oversized files with line counts and file type. Output is the rendered `reason` string embedded in a stop-hook JSON block decision. The template itself stores no state.
- gates_or_invariants: The invariant is that files over `{{MAX_LINES}}` lines are not acceptable at loop stop time. For code, functionality must remain strictly unchanged after splitting; see `prompt-template/block/large-files.md:13-17`. For docs, logical structure and cross-references must remain coherent; see `prompt-template/block/large-files.md:19-23`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` after it scans git status entries, resolves paths relative to project root, classifies code/doc extensions, counts lines, and accumulates files exceeding `MAX_LINES=2000`; see `hooks/loop-codex-stop-hook.sh:520-580`. The template is rendered via `load_and_render_safe` with `MAX_LINES` and `LARGE_FILES`, then returned as a JSON block decision; see `hooks/loop-codex-stop-hook.sh:582-603`.
- edge_cases_or_failure_modes: The caller only evaluates recognized code and documentation extensions and skips deleted files; see `hooks/loop-codex-stop-hook.sh:548-568`. Files outside those extensions are ignored by this gate. The caller uses a fallback block if the template cannot be loaded; see `hooks/loop-codex-stop-hook.sh:582-592`.
- validation_or_tests: Template existence is covered by the reference scanner because `block/large-files.md` is a literal stop-hook template reference; see `tests/test-template-references.sh:83-110`.
- skip_candidate: `no`

### HZ-162 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Next-round prompt footer that enforces non-bypass behavior and work handoff duties before the next RLCR round exits.
- algorithmic_behavior: Renders a footer warning that the worker must not exit by lying, editing loop state files, or executing `cancel-rlcr-loop`; then requires optional `code-simplifier` review if installed, a descriptive commit, and a work summary written to `@{{NEXT_SUMMARY_FILE}}`; see `prompt-template/claude/next-round-footer.md:2-9`.
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, computed by the stop hook for the next implementation round. Output is appended text in the generated next-round prompt. The template itself does not mutate state, but it instructs the worker to produce summary evidence in the round summary file.
- gates_or_invariants: The invariant is that Claude should finish the round through the hook-managed lifecycle, not by falsifying completion, editing state, or invoking cancellation directly. It also makes commit plus summary handoff part of the round contract.
- dependencies_and_callers: Appended by `hooks/loop-codex-stop-hook.sh` after updating state fields, creating the next round summary stub if missing, rendering the main next-round or drift-replan prompt, adding optional Agent Teams and open-question notices, and then loading this footer with `NEXT_SUMMARY_FILE`; see `hooks/loop-codex-stop-hook.sh:1990-2024` and `hooks/loop-codex-stop-hook.sh:2160-2165`.
- edge_cases_or_failure_modes: If this template is missing, the stop hook has a fallback footer with less detailed instructions; see `hooks/loop-codex-stop-hook.sh:2160-2164`. The file begins with a blank line before the horizontal rule; this affects prompt formatting only, not state behavior.
- validation_or_tests: Template existence is covered by the template reference scanner’s literal-reference pass; see `tests/test-template-references.sh:83-110`.
- skip_candidate: `no`

### HZ-192 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for the RLCR hook system. It tests edit, plan-file, read, write, Bash, and stop-hook behavior under malformed inputs, state-file edge cases, command-injection attempts, concurrency, and large JSON payloads.
- algorithmic_behavior: Creates temp test roots, sources hook shared libraries and test helpers, then invokes hook scripts with synthetic Claude hook JSON; see `tests/robustness/test-hook-system-robustness.sh:17-25`. Edit validator tests cover valid JSON, blocked `state.md` edits, malformed JSON, missing `file_path`, sandbox-delegated external paths, and path traversal to `state.md`; see `tests/robustness/test-hook-system-robustness.sh:39-155`. Plan-file validator tests create a git repo and loop state, allow plan edits, pass through non-plan edits, and handle empty JSON; see `tests/robustness/test-hook-system-robustness.sh:164-242`. State parsing tests verify whitespace, quoted path values, and malformed frontmatter handling; see `tests/robustness/test-hook-system-robustness.sh:252-306`.
- inputs_outputs_state: Inputs are generated JSON payloads for hook tools, temporary `.humanize/rlcr/<timestamp>/state.md` files, `goal-tracker.md`, plan files, mock git repos, mock Codex binary, and large/nested JSON payloads. Outputs are PASS/FAIL records and final test summary. State transitions are tested through active loop detection, blocked/allowed decisions, immutable-versus-mutable goal tracker behavior, and stop-hook handling of valid, missing, incomplete, and corrupted state.
- gates_or_invariants: Bash must block direct `state.md` modification and goal-tracker Bash writes after round 0; see `tests/robustness/test-hook-system-robustness.sh:316-416`. Write/Edit must allow mutable goal-tracker changes but block immutable-section changes after round 0; see `tests/robustness/test-hook-system-robustness.sh:418-535`. Read validator must block old-session goal-tracker reads; see `tests/robustness/test-hook-system-robustness.sh:538-550`. Unrelated dangerous Bash is intentionally allowed through because sandboxing is delegated outside this hook layer; see `tests/robustness/test-hook-system-robustness.sh:552-565`. Concurrent hook invocations must not corrupt state; see `tests/robustness/test-hook-system-robustness.sh:596-634`. A valid active loop stop attempt must return a block decision, while no-state and malformed-state conditions must fail gracefully rather than crash; see `tests/robustness/test-hook-system-robustness.sh:636-780`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `tests/test-helpers.sh`; see `tests/robustness/test-hook-system-robustness.sh:19-23`. Calls `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Depends on `jq`, `git`, `mktemp`, shell background jobs, and a mocked `codex` in PATH for active stop-hook behavior; see `tests/robustness/test-hook-system-robustness.sh:749-772`.
- edge_cases_or_failure_modes: Uses exit-code thresholds below 128 to assert graceful handling for malformed JSON, missing fields, binary-looking content, large JSON, escaped nulls, and nested JSON; see `tests/robustness/test-hook-system-robustness.sh:79-106`, `tests/robustness/test-hook-system-robustness.sh:567-594`, and `tests/robustness/test-hook-system-robustness.sh:782-832`. The stop-hook test numbering skips from Test 16 to Test 18, but the executable behavior is unaffected. The suite intentionally does not assert sandbox-level security for arbitrary dangerous Bash; it documents that as delegated responsibility.
- validation_or_tests: This file is itself a validation suite and exits through `print_test_summary "Hook System Robustness Test Summary"`; see `tests/robustness/test-hook-system-robustness.sh:834-839`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique assigned item section headers present; each assigned item has cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`