# agent_14 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-014 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Repository-wide library of rendered block reasons for the RLCR hook and stop-gate state machine. The directory has no nested child directories in this checkout; recursive inspection found only Markdown leaf templates under `prompt-template/block`.
- algorithmic_behavior: These files do not execute logic themselves, but they define the human-facing contract emitted when validators block a tool call or when the stop hook blocks a loop transition. Child roles group into: BitLesson delta enforcement (`bitlesson-delta-*.md`), git/worktree hygiene (`git-*.md`, `unpushed-commits.md`), protected RLCR artifacts (`state-file-modification.md`, `finalize-state-file-modification.md`, `plan-backup-protected.md`, `prompt-file-write.md`, `todos-file-access.md`), round/summary path and round-number gates (`wrong-*.md`, `round-contract-*.md`, `work-summary-missing.md`), goal tracker invariants (`goal-tracker-*.md`), review/finalization gates (`codex-review-failed.md`, `mainline-drift-stop.md`, `mainline-verdict-missing.md`), and operational failures (`large-files.md`, `schema-outdated.md`, `claude-eyes-timeout.md`, `git-status-failed.md`).
- inputs_outputs_state: Inputs are template names plus optional variables passed through `hooks/lib/template-loader.sh`; placeholders use `{{VARIABLE}}` syntax and are substituted single-pass, with missing placeholders preserved by design (`hooks/lib/template-loader.sh:48-135`). Outputs are rendered Markdown strings written to stderr by PreToolUse validators or embedded as JSON `reason` fields by the Stop hook. These templates affect state transitions by preventing unsafe progression, for example todo access is redirected to native Task tools (`prompt-template/block/todos-file-access.md:1-8`), plan mutation requires cancel/restart (`prompt-template/block/plan-file-modified.md:1-12`), and mainline drift stops the loop with `drift_status: replan_required` semantics (`prompt-template/block/mainline-drift-stop.md:1-14`).
- gates_or_invariants: The directory encodes invariant messages for “managed files are not agent-editable,” “round files must match the active round,” “state files are hook-owned,” “mainline verdicts are mandatory,” “BitLesson deltas must be explicit,” “broad git add must not capture `.humanize/`,” and “large files must be split before exit.” Example concrete gates include state-file immutability (`prompt-template/block/state-file-modification.md:1-3`), prompt file read-only status (`prompt-template/block/prompt-file-write.md:1-10`), and large file line-limit enforcement (`prompt-template/block/large-files.md:1-25`).
- dependencies_and_callers: Loaded by `load_template`, `load_and_render`, and `load_and_render_safe` in `hooks/lib/template-loader.sh:36-46`, `hooks/lib/template-loader.sh:138-151`, and `hooks/lib/template-loader.sh:188-211`. Callers include shared message helpers in `hooks/lib/loop-common.sh:839-911` and `hooks/lib/loop-common.sh:1012-1019`, Bash/Edit/Read/Write validators, `hooks/loop-codex-stop-hook.sh`, and `scripts/bitlesson-validate-delta.sh`.
- edge_cases_or_failure_modes: Missing or empty templates fall back to hardcoded messages (`hooks/lib/template-loader.sh:185-211`), so missing Markdown does not make a safety gate silently pass. Single-pass rendering prevents substituted review content from recursively expanding `{{...}}` placeholders (`hooks/lib/template-loader.sh:52-55`). Some templates are intentionally generic fragments and depend on callers to provide precise path variables.
- validation_or_tests: Template directory validity is checked by requiring `block`, `codex`, `claude`, and `plan` subdirectories (`hooks/lib/template-loader.sh:213-238`). Most validation is indirect through hook test suites and stop-hook flows; the assigned HZ-044, HZ-074, HZ-104, HZ-134, and HZ-164 items cover several direct consumers.
- skip_candidate: `no`

### HZ-044 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Claude Code `Edit` calls during an RLCR loop. It blocks edits to hook-owned or phase-protected files and enforces active-loop, active-round, and goal-tracker immutability contracts.
- algorithmic_behavior: The script reads hook JSON from stdin, exits immediately for non-`Edit` tools (`hooks/loop-edit-validator.sh:23-28`), extracts `file_path` and `session_id` (`hooks/loop-edit-validator.sh:30-34`), then applies ordered gates. It blocks round todo edits unless allowlisted (`hooks/loop-edit-validator.sh:40-48`), all prompt edits (`hooks/loop-edit-validator.sh:50-53`), methodology-analysis phase edits except `methodology-analysis-report.md` and `methodology-analysis-done.md` (`hooks/loop-edit-validator.sh:71-123`), state-file edits (`hooks/loop-edit-validator.sh:166-179`), finalize contract edits during Finalize Phase (`hooks/loop-edit-validator.sh:181-184`), RLCR plan backup edits (`hooks/loop-edit-validator.sh:190-197`), invalid goal-tracker edits (`hooks/loop-edit-validator.sh:204-239`), and wrong-round summary/contract edits (`hooks/loop-edit-validator.sh:246-271`).
- inputs_outputs_state: Inputs are hook JSON fields `tool_name`, `tool_input.file_path`, optional `session_id`, and for goal-tracker edits after round 0, `old_string`, `new_string`, and `replace_all`. It reads active loop state from `.humanize/rlcr/<session>/state.md`, `finalize-state.md`, or `methodology-analysis-state.md`. Outputs are exit `0` for allow, exit `2` for user-correctable blocks with rendered message on stderr, and exit `1` for malformed state or required Edit fields missing. It does not mutate state; it guards writes before they happen.
- gates_or_invariants: Session-aware loop filtering prevents one session’s methodology-analysis phase from constraining unrelated sessions (`hooks/loop-edit-validator.sh:65-69`). Strict state parsing fails closed on malformed state before round-sensitive checks (`hooks/loop-edit-validator.sh:154-160`; parser in `hooks/lib/loop-common.sh:524-600`). Goal tracker edits after round 0 must use literal Edit fields and preserve the immutable section, checked by rendering the post-edit content then comparing immutable slices (`hooks/loop-edit-validator.sh:214-236`; helpers at `hooks/lib/loop-common.sh:949-991`). Wrong-round summary/contract edits are blocked unless specifically allowlisted (`hooks/loop-edit-validator.sh:246-271`).
- dependencies_and_callers: Wired as the `Edit` PreToolUse hook in `hooks/hooks.json:24-31`. Depends on `jq`, `realpath`, `sed`, `perl` through shared helpers, and `hooks/lib/loop-common.sh`. Template messages come from `prompt-template/block` through `load_and_render_safe`.
- edge_cases_or_failure_modes: In methodology-analysis mode, unresolved paths fall back to parent `realpath`; traversal segments are rejected if canonicalization fails (`hooks/loop-edit-validator.sh:71-87`). Leaf symlinks are rejected when they cannot be resolved, preventing prefix-check bypasses (`hooks/loop-edit-validator.sh:88-97`). Files outside `.humanize/rlcr/` pass unless methodology-analysis mode is active (`hooks/loop-edit-validator.sh:126-132`). Legacy goal trackers without an immutable section allow edits (`hooks/lib/loop-common.sh:960-962`).
- validation_or_tests: This file is an implementation rather than a standalone spec. It is covered indirectly by hook suites that exercise protected plan/state/round file behavior; its sibling Bash validator is exercised extensively by HZ-074, and the shared state/round/path helpers are used across assigned tests.
- skip_candidate: `no`

### HZ-074 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the cancellation authorization sub-state machine. It proves that moving active loop state to `cancel-state.md` is allowed only after an explicit signal file and only for a tightly constrained `mv` form.
- algorithmic_behavior: The test creates a temporary active RLCR loop with `state.md`, exports `CLAUDE_PROJECT_DIR`, and drives `hooks/loop-bash-validator.sh` via JSON hook input (`tests/test-cancel-signal-file.sh:42-73`). Positive cases allow `mv <loop>/state.md <loop>/cancel-state.md` when `<loop>/.cancel-requested` exists, including `./` path normalization and quoted paths (`tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-380`). It also tests literal `${loop_dir}` normalization through both helper and validator (`tests/test-cancel-signal-file.sh:450-486`).
- inputs_outputs_state: Inputs are filesystem fixtures under a temp `.humanize/rlcr/<timestamp>` directory, a signal file named `.cancel-requested`, Bash command strings, and direct helper calls to `is_cancel_authorized`. Outputs are pass/fail counters and script exit equal to failed test count (`tests/test-cancel-signal-file.sh:1421-1433`). The intended state transition is authorization of a later shell move from `state.md` to `cancel-state.md`; the validator itself only allows or blocks the Bash tool invocation.
- gates_or_invariants: The signal must be in the active loop directory, not the parent (`tests/test-cancel-signal-file.sh:197-217`). Without a signal, the cancel move is blocked (`tests/test-cancel-signal-file.sh:117-135`). Other modifications remain blocked even with the signal, including redirects, `sed -i`, `rm`, wrong destinations, arbitrary destinations, and unrelated `/tmp/state.md` paths (`tests/test-cancel-signal-file.sh:137-195`, `tests/test-cancel-signal-file.sh:1212-1227`). The implementation contract is in `is_cancel_authorized`, which requires the signal file, rejects command substitution, newlines, shell chaining, residual variables, mixed quotes, extra args, wrong source/dest, and source symlinks (`hooks/lib/loop-common.sh:1033-1250`).
- dependencies_and_callers: Exercises `hooks/loop-bash-validator.sh`, especially state-file source/destination checks at `hooks/loop-bash-validator.sh:271-297`, shell-segment and redirection normalization at `hooks/loop-bash-validator.sh:310-456`, and shell-wrapper detection at `hooks/loop-bash-validator.sh:458-488`. It sources `hooks/lib/loop-common.sh` directly for helper tests (`tests/test-cancel-signal-file.sh:32-33`).
- edge_cases_or_failure_modes: The spec covers command injection with `&&`, semicolon, newline, `$()`, backticks, pipes, background operators, `sh -c`/`bash -c`, sudo/env/command prefixes, hidden `${IFS}`, extra arguments, and redirection-prefix variants (`tests/test-cancel-signal-file.sh:240-357`, `tests/test-cancel-signal-file.sh:488-1207`). It also regression-tests symlink behavior: symlinked parent prefixes are accepted after canonicalization, but source and destination leaf symlink aliases are rejected (`tests/test-cancel-signal-file.sh:1345-1419`).
- validation_or_tests: The file is itself the validation asset. It was inspected but not executed in this read-only research run.
- skip_candidate: `no`

### HZ-104 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for the stop-gate task completion checker. It verifies that an RLCR loop cannot exit with unresolved blocking/mainline tasks, while queued tasks are allowed to remain.
- algorithmic_behavior: The script drives `hooks/check-todos-from-transcript.py` with JSON stdin. It first validates input handling, then legacy `TodoWrite` transcript parsing, transcript format variations, edge cases, and the newer file-based Task system (`tests/test-todo-checker.sh:46-538`). The checker’s core logic extracts tool calls from assistant/message/direct transcript entries (`hooks/check-todos-from-transcript.py:38-70`), keeps only the most recent `TodoWrite` list (`hooks/check-todos-from-transcript.py:82-103`), and reads current Task files from `~/.claude/tasks/<session_id>/` or a test override (`hooks/check-todos-from-transcript.py:123-170`).
- inputs_outputs_state: Inputs are hook JSON fields `transcript_path`, `session_id`, and `tasks_base_dir`; test fixtures include JSONL transcripts and task JSON files. Outputs are checker exit codes: `0` for no incomplete items or unavailable task source, `1` for incomplete items, and `2` for malformed hook JSON (`hooks/check-todos-from-transcript.py:9-12`, `hooks/check-todos-from-transcript.py:173-221`). On exit `1`, output begins with `INCOMPLETE_TODOS` and lists incomplete items with status, lane marker, and Task ID for file-based tasks (`hooks/check-todos-from-transcript.py:204-220`).
- gates_or_invariants: Statuses other than `completed` are incomplete for legacy todos (`hooks/check-todos-from-transcript.py:104-119`). For file tasks, `completed` and `deleted` are non-blocking; missing status defaults to `pending` (`hooks/check-todos-from-transcript.py:149-165`). Lane tags are recognized only as a prefix matching `mainline`, `blocking`, or `queued`; inline tags do not downgrade a blocking task (`hooks/check-todos-from-transcript.py:24-35`). Queued tasks are skipped in both legacy and file-based modes (`hooks/check-todos-from-transcript.py:110-112`, `hooks/check-todos-from-transcript.py:156-158`).
- dependencies_and_callers: Depends on Python 3 standard library only (`json`, `re`, `pathlib`). The stop hook consumes this checker to block exit when native task state still contains incomplete blocking work, while the shell spec invokes it directly.
- edge_cases_or_failure_modes: The test requires invalid JSON stdin to exit `2`, but empty stdin, missing `transcript_path`, and nonexistent transcript files to exit `0` (`tests/test-todo-checker.sh:52-99`). Invalid JSONL lines are ignored, multiple `TodoWrite` calls use the latest list, and assistant/message/direct tool-use transcript shapes are accepted (`tests/test-todo-checker.sh:210-271`). Empty content, missing status, Unicode, nonexistent task session directories, mixed legacy/file task sources, explicit blocking tags, queued prefixes, and inline queued text are all specified (`tests/test-todo-checker.sh:280-536`).
- validation_or_tests: The file is itself the checker validation suite. It was inspected but not executed in this read-only research run.
- skip_candidate: `no`

### HZ-134 `file` `prompt-template/block/mainline-verdict-missing.md`
- cursor: `[_]`
- core_role: Stop-gate block template for missing implementation-review mainline verdict. It prevents the loop from updating drift state or generating the next-round prompt from ambiguous Codex review output.
- algorithmic_behavior: The template tells the agent that the review output must contain `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED`, explains that Humanize cannot safely update drift or next-round prompting without it, and includes the review result/prompt file references (`prompt-template/block/mainline-verdict-missing.md:1-13`). It is rendered by `block_missing_mainline_verdict`, which returns a JSON block decision with system message “Loop: Blocked - implementation review missing Mainline Progress Verdict” (`hooks/loop-codex-stop-hook.sh:1459-1492`).
- inputs_outputs_state: Inputs are `REVIEW_RESULT_FILE` and `REVIEW_PROMPT_FILE` template variables. Output is a Markdown reason embedded in the Stop hook’s JSON response. The associated state behavior is conservative: no drift counters or next-round state are advanced when verdict extraction returns unknown.
- gates_or_invariants: The verdict extractor accepts only one of the three explicit verdict words after the `Mainline Progress Verdict:` label (`hooks/lib/loop-common.sh:634-661`). It rejects ambiguous template-like lines with multiple verdict words by returning `unknown` (`hooks/lib/loop-common.sh:648-658`). Codex prompt templates state that this line is mandatory (`prompt-template/codex/regular-review.md:56-61`, `prompt-template/codex/full-alignment-review.md:59-66`).
- dependencies_and_callers: Depends on template rendering from `hooks/lib/template-loader.sh` and is called by `hooks/loop-codex-stop-hook.sh`. It coordinates with mainline drift handling in the same hook, where actual stalled/regressed verdicts can update drift fields or stop the loop.
- edge_cases_or_failure_modes: If the Markdown template is missing, the stop hook uses an equivalent fallback (`hooks/loop-codex-stop-hook.sh:1465-1481`). A review that includes the placeholder line with all three verdict options remains invalid because the extractor detects multiple verdict keywords.
- validation_or_tests: No assigned standalone test directly targets this template, but it is part of the stop-hook review contract and is backed by the mandatory verdict instructions in the Codex review prompt templates.
- skip_candidate: `no`

### HZ-164 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Follow-up prompt injection template that forces user clarification when Codex review identifies open questions. It is a review-contract gate rather than an implementation gate.
- algorithmic_behavior: The template is a one-line high-priority notice requiring `AskUserQuestion` before resolving any other Codex findings (`prompt-template/claude/open-question-notice.md:1`). The stop hook scans the review result when `ASK_CODEX_QUESTION` is true; if it finds a short line containing `Open Question`, it injects this notice into the next prompt after the Codex review result boundary (`hooks/loop-codex-stop-hook.sh:2116-2146`).
- inputs_outputs_state: Inputs are the state field `ask_codex_question`, the review result file contents, and the next-round prompt file. Output is injected Markdown in the next prompt; the hook writes through a temporary prompt file and moves it into place. The notice does not directly mutate loop state, but it changes the next-round action ordering by making user clarification the first required step.
- gates_or_invariants: Injection is enabled only when `ASK_CODEX_QUESTION` is true (`hooks/loop-codex-stop-hook.sh:2118`). Detection is intentionally narrow: a line length under 40 characters and case-sensitive substring `Open Question` (`hooks/loop-codex-stop-hook.sh:2116-2125`). Startup records the setting as `ask_codex_question: $ASK_CODEX_QUESTION` in state (`scripts/setup-rlcr-loop.sh:880-899`).
- dependencies_and_callers: Loaded with `load_template`, with a hardcoded fallback if the template is empty or missing (`hooks/loop-codex-stop-hook.sh:2128-2132`). It coordinates with Codex review output, next-round prompt generation, and the setup option that can disable asking Codex questions.
- edge_cases_or_failure_modes: Long open-question headings or differently cased text are not detected by this scan. If the expected review-result end marker/separator shape is absent, the awk insertion point may not be reached even when an open question was detected. Missing template content falls back to the same notice text.
- validation_or_tests: No assigned standalone test directly targets this injection path. The behavior is specified in the stop-hook code and the single-line template.
- skip_candidate: `no`

### HZ-194 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for plan-file validation at RLCR loop startup. It verifies that `scripts/setup-rlcr-loop.sh` accepts valid plan anchors and rejects weak, missing, or unsafe plan inputs before loop state is created.
- algorithmic_behavior: The test creates an isolated temp git repository, installs a mock `codex` executable, then calls production `scripts/setup-rlcr-loop.sh` with different plan paths (`tests/robustness/test-plan-file-robustness.sh:18-40`, `tests/robustness/test-plan-file-robustness.sh:46-106`, `tests/robustness/test-plan-file-robustness.sh:112-123`). Success is inferred from accepted setup output, gitignore/tracking policy messages after validation, or creation of `.humanize/rlcr/<loop>/state.md` (`tests/robustness/test-plan-file-robustness.sh:64-95`).
- inputs_outputs_state: Inputs are generated plan files with different contents, sizes, encodings, file types, and lifecycle behavior. Outputs are test pass/fail counters via `tests/test-helpers.sh` and the production script’s accept/reject outcome. Accepted plans may transition the temp repo into an initialized RLCR state with a loop directory and `state.md`; rejected plans stop before valid loop initialization.
- gates_or_invariants: Production startup rejects absolute paths, spaces, shell metacharacters, symlink plan files, missing parent directories, symlinked parent path segments, nonexistent or unreadable files, paths escaping the project, submodule paths, invalid tracking policy, too few lines, and insufficient non-comment content (`scripts/setup-rlcr-loop.sh:455-671`). The content gate requires at least 5 total lines and at least 3 content lines after skipping blanks, `#` comments, and HTML comments (`scripts/setup-rlcr-loop.sh:618-671`).
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh` as the production function under test (`tests/robustness/test-plan-file-robustness.sh:46-62`), uses `tests/test-helpers.sh` for counters/summary (`tests/robustness/test-plan-file-robustness.sh:108-110`, `tests/test-helpers.sh:58-78`), and depends on git plus the mock codex in the temp PATH.
- edge_cases_or_failure_modes: Positive cases include normal plans, exactly-minimal content, 5KB plans, rich Markdown, large files over 1MB, and mixed CRLF/LF endings (`tests/robustness/test-plan-file-robustness.sh:178-366`). Negative or robustness cases cover empty files, comments-only files, binary bytes, very long lines, shell-special characters in content, whitespace-only content, complex HTML comments, nonexistent files, unreadable permissions, symlink files, directories instead of files, null bytes, and disappearance before validation (`tests/robustness/test-plan-file-robustness.sh:300-551`). The large-file test specifically guards against prior SIGPIPE-style failures in preview/counting pipelines (`tests/robustness/test-plan-file-robustness.sh:325-347`).
- validation_or_tests: The file is itself the robustness validation suite. It was inspected but not executed in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `HZ-014, HZ-044, HZ-074, HZ-104, HZ-134, HZ-164, HZ-194`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`