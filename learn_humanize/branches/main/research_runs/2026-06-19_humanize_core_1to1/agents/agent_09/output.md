# agent_09 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-009 `directory` `skills`
- cursor: `[_]`
- core_role: `skills/` is the agent-facing workflow surface for Humanize. It contains six recursively inspected skill specs: `ask-codex`, `ask-gemini`, `humanize`, `humanize-gen-plan`, `humanize-refine-plan`, and `humanize-rlcr`. These do not implement shell logic directly, but they define the algorithmic contracts that route users/agents into scripts, hooks, plan state, review state, and persisted `.humanize/` artifacts.
- algorithmic_behavior: `skills/humanize/SKILL.md` defines the top-level RLCR algorithm as two phases: implementation summary review until Codex says complete, then code review with severity markers before finalize (`skills/humanize/SKILL.md:30-48`). `skills/humanize-rlcr/SKILL.md` is the Codex-native entrypoint: setup, read generated prompt, implement, commit, write summary, stop normally, and let the native Stop hook drive the next transition (`skills/humanize-rlcr/SKILL.md:24-49`). `skills/humanize-gen-plan/SKILL.md` defines a plan-generation state machine: validate IO, read draft, check repo relevance, analyze issues, resolve user questions, generate structured plan, review/fix inconsistencies, unify language, report success (`skills/humanize-gen-plan/SKILL.md:19-47`). `skills/humanize-refine-plan/SKILL.md` defines annotated-plan refinement: validate, statefully extract comments, classify, process, preserve schema, generate QA, optionally translate variants, write atomically (`skills/humanize-refine-plan/SKILL.md:18-48`). `ask-codex` and `ask-gemini` define one-shot consultation protocols with safe argument handling and persisted output (`skills/ask-codex/SKILL.md:12-36`, `skills/ask-gemini/SKILL.md:15-39`).
- inputs_outputs_state: Inputs are command arguments, plan/draft paths, annotated comments, model/timeout flags, and generated RLCR prompt files. Outputs include `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, round summaries/review results, finalize files, complete state, and `.humanize/skill/<timestamp>/input.md|output.md|metadata.md` (`skills/humanize/SKILL.md:180-203`). Plan generation outputs a structured plan with Goal Description, Acceptance Criteria, Path Boundaries, Dependencies, and Implementation Notes (`skills/humanize-gen-plan/SKILL.md:55-94`). Refine-plan outputs a comment-free plan plus QA ledger rows classified by raw comment block (`skills/humanize-refine-plan/SKILL.md:88-107`). Ask skills output model response to stdout, status to stderr, and persisted `output.md` (`skills/ask-codex/SKILL.md:38-56`, `skills/ask-gemini/SKILL.md:41-61`).
- gates_or_invariants: RLCR enforces summary writing, goal-tracker maintenance, no manual state edits, no ad-hoc review substitutions, git-clean exit, optional push-every-round, plan integrity, max iterations, strict complete/stop marker handling, review transition guard, code-review blocking, and open-question handling (`skills/humanize-rlcr/SKILL.md:50-75`). The Goal Tracker has an immutable Ultimate Goal and Acceptance Criteria section, plus mutable Active/Completed/Deferred/Evolution sections (`skills/humanize/SKILL.md:152-164`). Gen-plan validation exit codes cover missing/empty input, missing output dir, existing output file, write permission, bad args, and missing template (`skills/humanize-gen-plan/SKILL.md:96-107`). Refine-plan requires gen-plan schema preservation, removal of resolved comment blocks, restricted routing tags, and atomic writes (`skills/humanize-refine-plan/SKILL.md:67-87`).
- dependencies_and_callers: The skills point to `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/ask-gemini.sh` through hydrated `{{HUMANIZE_RUNTIME_ROOT}}` or `${CLAUDE_PLUGIN_ROOT}` paths (`skills/humanize/SKILL.md:61-109`). `humanize-rlcr` depends on native Codex Stop hook behavior described in `hooks/loop-codex-stop-hook.sh`. `humanize-refine-plan` depends on `scripts/validate-refine-plan-io.sh` and `prompt-template/plan/refine-plan-qa-template.md` (`skills/humanize-refine-plan/SKILL.md:22`, `skills/humanize-refine-plan/SKILL.md:41`). One-shot skills depend on plugin scripts and external CLIs; Gemini additionally promises Google Search-backed research (`skills/ask-gemini/SKILL.md:10-13`).
- edge_cases_or_failure_modes: Shell metacharacter injection is explicitly called out for one-shot skills: free-form user text must be passed as one quoted argument, never bare argument expansion (`skills/ask-codex/SKILL.md:14-36`, `skills/ask-gemini/SKILL.md:17-39`). RLCR failure modes include missing summaries, skipped hook results, direct state edits, stale/wrong prompt sources, open Codex questions, review failures, and review-only mode (`skills/humanize-rlcr/SKILL.md:69-75`, `skills/humanize-rlcr/SKILL.md:90-97`). Refine-plan failure modes include ambiguous comments, parse errors with location/context, missing schema sections, unwritable QA directory, and unsupported argument combinations (`skills/humanize-refine-plan/SKILL.md:22-40`, `skills/humanize-refine-plan/SKILL.md:62-66`, `skills/humanize-refine-plan/SKILL.md:131-143`).
- validation_or_tests: The skill directory itself is specification, not executable tests. Its behavior is validated indirectly by test coverage in assigned tests and related suites, especially stop-hook, template reference, gen-plan, refine-plan, ask-codex, config, and robustness suites listed in `tests/run-all-tests.sh:59-119`.
- skip_candidate: `no`

### HZ-039 `file` `docs/usage.md`
- cursor: `[_]`
- core_role: This is behavior-defining user documentation for the Humanize workflow. It explains the RLCR algorithm, planning/refinement path, model/config resolution, monitoring, cancellation, and sandbox bypass behavior.
- algorithmic_behavior: The core workflow is a two-phase loop: implementation phase where Claude works and Codex reviews summaries, then review phase where `codex review --base <branch>` checks quality using severity markers (`docs/usage.md:5-12`). A pre-flight Plan Understanding Quiz analyzes a plan and asks two architecture/mechanism questions; wrong answers do not hard-block but introduce explicit friction and a proceed/stop choice (`docs/usage.md:14-35`). The typical flow is generate plan, refine annotated plan if needed, then start RLCR (`docs/usage.md:43-56`). Gen-plan validates paths, checks relevance, analyzes clarity/consistency/completeness/functionality, resolves issues, generates acceptance criteria, and can auto-start RLCR when convergence conditions are met (`docs/usage.md:118-126`).
- inputs_outputs_state: Inputs include `plan.md`, draft markdown, annotated plan markdown, CLI options, config files, and environment variables. Outputs include generated implementation plans, refined plans, QA ledgers, `.humanize/skill/<timestamp>/` consultation artifacts, and `.humanize/rlcr/<timestamp>/` progress state (`docs/usage.md:102-154`, `docs/usage.md:216-254`, `docs/usage.md:310-323`). Refine-plan comment inputs can be `CMT:/ENDCMT`, `<cmt></cmt>`, or `<comment></comment>` blocks, including inline or multiline forms (`docs/usage.md:168-207`).
- gates_or_invariants: Start options define gates and policy knobs: max iterations, plan file tracking, push-every-round, base branch detection priority, full-review cadence, skip-impl, agent teams, yolo, and skip-quiz (`docs/usage.md:70-100`). Refine-plan requires at least one non-empty comment block, ignores comment markers inside fenced code/HTML comments, removes empty blocks without QA rows, preserves gen-plan schema, and supports mixed marker formats (`docs/usage.md:209-215`). Configuration precedence is defaults, user config, project config, then CLI flags (`docs/usage.md:256-263`), while Codex model precedence is CLI, feature defaults, config defaults, hardcoded fallback (`docs/usage.md:299-304`).
- dependencies_and_callers: Commands documented here map to slash commands and underlying scripts/skills: `/humanize:start-rlcr-loop`, `/humanize:gen-plan`, `/humanize:refine-plan`, `/humanize:ask-codex`, and `/humanize:cancel-rlcr-loop` (`docs/usage.md:58-67`). Monitoring depends on sourcing `scripts/humanize.sh` from the installed plugin path and reading `.humanize/rlcr/<timestamp>/` (`docs/usage.md:310-323`). Config keys map to `config/default_config.json`, user config, project config, and CLI flags (`docs/usage.md:256-275`).
- edge_cases_or_failure_modes: The quiz can be skipped with `--skip-quiz`; `--yolo` also lets Claude answer Codex open questions directly, increasing automation risk (`docs/usage.md:37-41`, `docs/usage.md:96-98`). Legacy config keys `loop_reviewer_model` and `loop_reviewer_effort` are ignored, requiring migration to `codex_model` and `codex_effort` (`docs/usage.md:305-307`). `HUMANIZE_CODEX_BYPASS_SANDBOX` is explicitly dangerous: when true/1, Codex uses unrestricted approvals/sandbox bypass and should not be used on shared, untrusted, or production systems (`docs/usage.md:331-364`).
- validation_or_tests: This file is documentation, but it describes testable contracts covered by command, stop-hook, config, gen-plan, refine-plan, monitor, and sandbox-related tests. It is not itself executable. I did not run test suites because this worker was instructed not to modify files and the checkout is read-only.
- skip_candidate: `no`

### HZ-069 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI escape stripping and pass/fail count extraction used by the aggregate test runner.
- algorithmic_behavior: The test defines a portable SGR-stripping algorithm using `esc=$'\033'` and `sed "s/${esc}\\[[0-9;]*m//g"` (`tests/test-ansi-parsing.sh:38-47`). It then extracts counts with `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'` and the analogous Failed expression (`tests/test-ansi-parsing.sh:74-90`). It validates single-line, multi-color, zero count, multiline suite summary, plain text, and bold-plus-color cases (`tests/test-ansi-parsing.sh:42-158`).
- inputs_outputs_state: Inputs are simulated output strings containing ANSI SGR color sequences and suite summary labels. Outputs are stripped strings, numeric `passed`/`failed` variables, and global counters `TESTS_PASSED`/`TESTS_FAILED` (`tests/test-ansi-parsing.sh:18-31`). The terminal state is exit 0 if no assertion failed, exit 1 otherwise (`tests/test-ansi-parsing.sh:160-178`).
- gates_or_invariants: The invariant is that colored and uncolored outputs must parse identically for `Passed:` and `Failed:` labels. The multiline test explicitly uses `tail -1` after count extraction so the final summary value wins when multiple labels exist (`tests/test-ansi-parsing.sh:116-125`). The sed expression is intentionally constrained to numeric/semicolon SGR `m` sequences for GNU/BSD portability.
- dependencies_and_callers: The direct production caller is `tests/run-all-tests.sh`, which launches suites, captures output files, strips ANSI using the same expression, extracts counts, totals them, and treats nonzero exit or nonzero failed count as suite failure (`tests/run-all-tests.sh:229-266`). The runner includes this test as one suite in its suite list (`tests/run-all-tests.sh:59-119`).
- edge_cases_or_failure_modes: Covered edge cases include multiple colors in one line, failed count of zero, repeated summary labels in multiline output, no ANSI codes, and combined bold/color SGR values. Not covered: non-SGR escape sequences, RGB color forms using colon separators, cursor movement/control sequences, localized labels, or labels split across lines.
- validation_or_tests: The file is itself the validation asset. It prints a colored summary and exits by failure count (`tests/test-ansi-parsing.sh:163-178`). I inspected the executable spec but did not run it because this run is research-only in a read-only checkout.
- skip_candidate: `no`

### HZ-099 `file` `tests/test-stop-hook-legacy-compat.sh`
- cursor: `[_]`
- core_role: Executable compatibility specification for `hooks/loop-codex-stop-hook.sh`, focused on legacy local `.humanize-*` runtime directories, `.humanizeconfig` dirty-state behavior, and legacy BitLesson default behavior.
- algorithmic_behavior: The test builds temporary git repos, creates loop fixtures with state frontmatter, goal tracker, plan backup, and round summary, then runs the Stop hook with a mock `codex` binary that writes a marker when invoked (`tests/test-stop-hook-legacy-compat.sh:25-38`, `tests/test-stop-hook-legacy-compat.sh:40-118`). Test 1 verifies untracked `.humanize-old/` does not block git-clean gating and Codex is invoked (`tests/test-stop-hook-legacy-compat.sh:145-160`). Test 1b verifies `.humanizeconfig` remains a normal untracked dirty file and blocks before Codex (`tests/test-stop-hook-legacy-compat.sh:162-183`). Test 2 verifies legacy state without `bitlesson_required` still reaches Codex even when `.humanize/bitlesson.md` exists (`tests/test-stop-hook-legacy-compat.sh:185-197`).
- inputs_outputs_state: Inputs are synthetic repo states, untracked files/directories, `.humanize/bitlesson.md`, and Stop-hook stdin `{}`. Outputs are hook exit code, captured hook output, and presence/absence of `codex-called.marker` (`tests/test-stop-hook-legacy-compat.sh:120-136`). The fixture state includes fields such as `current_round`, `review_started`, `ask_codex_question`, `agent_teams`, `base_branch`, and `base_commit`, while intentionally omitting `bitlesson_required` (`tests/test-stop-hook-legacy-compat.sh:67-85`).
- gates_or_invariants: Production hook gating defaults `BITLESSON_REQUIRED` to false unless the raw frontmatter contains that field (`hooks/loop-codex-stop-hook.sh:144-147`). Dirty-state filtering excludes untracked `.humanize/` and dash-separated `.humanize-*` paths via `^\?\? \.humanize[-/]` (`hooks/loop-codex-stop-hook.sh:692-698`), but other untracked files remain blocking and receive generic untracked-file notes (`hooks/loop-codex-stop-hook.sh:714-723`). Git status failures are fail-closed (`hooks/loop-codex-stop-hook.sh:495-515`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, a real shell/git environment, mock `codex`, and `hooks/loop-codex-stop-hook.sh` (`tests/test-stop-hook-legacy-compat.sh:14-19`). The Stop hook depends on template loading for block messages and cached git status; it also uses the state parser and loop common libraries through its normal startup path.
- edge_cases_or_failure_modes: The critical regression risks are over-filtering `.humanizeconfig` as local runtime state, under-filtering `.humanize-old/` and blocking legacy runtime directories, or treating the mere existence of `.humanize/bitlesson.md` as mandatory BitLesson gating for older loops. The test also accounts for global ignores potentially hiding `.humanize/`, so it uses `.humanize-old/` as a reliable untracked legacy path (`tests/test-stop-hook-legacy-compat.sh:166-170`).
- validation_or_tests: The file has three assertions and exits through `print_test_summary` (`tests/test-stop-hook-legacy-compat.sh:145-200`). I inspected the spec and production hook anchors; I did not run it because it creates temp repos/files and this task requested research notes only.
- skip_candidate: `no`

### HZ-129 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template for a guardrail transition: after Round 0, invalid Goal Tracker modifications are blocked and redirected to safe mutable-section edits or summary-based Codex correction requests.
- algorithmic_behavior: The template emits a block message titled with the current round, states that only the mutable section may be updated after Round 0, identifies the correct tracker path via `{{CORRECT_PATH}}`, and gives rules not to alter the immutable section, not to modify via Bash, and not to write an old session tracker (`prompt-template/block/goal-tracker-modification.md:1-14`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `CORRECT_PATH`. Output is rendered markdown used as the blocking reason for hook/tool validators. It does not mutate state itself; it instructs the agent how to produce a valid future state transition.
- gates_or_invariants: The core invariant is that the immutable Goal Tracker section remains unchanged after Round 0 (`prompt-template/block/goal-tracker-modification.md:3-13`). Related validators enforce this: edit validation checks the active path and previews an edit before allowing it (`hooks/loop-edit-validator.sh:204-236`), write validation checks the full new content against mutable-update rules (`hooks/loop-write-validator.sh:253-275`), and Bash validation blocks direct command-line modifications to `goal-tracker.md` (`hooks/loop-bash-validator.sh:504-517`).
- dependencies_and_callers: Loaded through the hook template loader, whose rendering is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:48-56`) and whose safe loader falls back if a template is missing/empty (`hooks/lib/template-loader.sh:185-210`). Callers are the Goal Tracker guard paths in edit/write/bash validators.
- edge_cases_or_failure_modes: Wrong loop session tracker writes are blocked, as are Bash writes and immutable-section changes. A safe fallback exists if the template cannot be loaded. If an agent cannot safely resolve drift itself, the template tells it to place an optional Goal Tracker Update Request in the summary (`prompt-template/block/goal-tracker-modification.md:14`).
- validation_or_tests: Covered indirectly by validator pattern and plan-file/goal-tracker robustness suites; `tests/test-template-references.sh` includes this template in its reference set. This file is a prompt asset, not an executable test.
- skip_candidate: `no`

### HZ-159 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Prompt template for the finalize-phase transition when code review was skipped. It defines the compensating manual-verification workflow and constrains the agent to non-functional cleanup before exit.
- algorithmic_behavior: The template starts with a review-skipped warning populated by `{{REVIEW_SKIP_REASON}}`, tells the agent the implementation could not be fully validated, and requires manual review, tests, and quality checks before finalizing (`prompt-template/claude/finalize-phase-skipped-prompt.md:1-14`). It optionally suggests simplification via a `code-simplifier` Task invocation focused on the branch delta from `{{BASE_BRANCH}}` to `{{START_BRANCH}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:15-24`). Before exit it requires mainline/blocking tasks complete, queued tasks documented as non-blocking follow-up, a commit, and a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:40-51`).
- inputs_outputs_state: Inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is rendered markdown passed back by the Stop hook as the next prompt/reason. The state transition is handled by the hook renaming the loop state to `finalize-state.md` and expecting `finalize-summary.md` next (`hooks/loop-codex-stop-hook.sh:1319-1364`).
- gates_or_invariants: The non-negotiable constraints are functionality preservation, existing tests must not fail, no new bugs, and only functionality-equivalent changes (`prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`). The skipped-review path is intentionally weaker than full review, so it inserts manual verification obligations rather than declaring final success.
- dependencies_and_callers: `enter_finalize_phase` uses this template only when `skip_reason` is non-empty, otherwise it loads the normal finalize prompt (`hooks/loop-codex-stop-hook.sh:1331-1391`). Rendering goes through `load_and_render_safe`, so missing templates fall back to an inline prompt while preserving the same variables (`hooks/loop-codex-stop-hook.sh:1331-1364`, `hooks/lib/template-loader.sh:185-210`).
- edge_cases_or_failure_modes: If code review is skipped due timeout/tool failure, the template prevents silent acceptance by making manual verification explicit. It also permits queued work only when documented as non-blocking follow-up, reducing the risk that unfinished blocking work gets hidden during finalize (`prompt-template/claude/finalize-phase-skipped-prompt.md:40-44`).
- validation_or_tests: Template behavior is covered indirectly by finalize-phase and template-reference tests. The template is a transition contract, not a standalone executable.
- skip_candidate: `no`

### HZ-189 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git-status and git-state algorithms in `scripts/humanize.sh`.
- algorithmic_behavior: The test sources `scripts/humanize.sh` and validates `humanize_parse_git_status`, whose output contract is `modified|added|deleted|untracked|insertions|deletions` (`tests/robustness/test-git-operations-robustness.sh:1-20`, `tests/robustness/test-git-operations-robustness.sh:28-47`). The production function rejects non-repos with a sentinel string, counts porcelain status codes, treats renames as modified, and computes insertion/deletion counts from `git diff --shortstat HEAD` (`scripts/humanize.sh:206-252`). It also validates `humanize_detect_git_state`, which returns normal, detached, rebase, merge, shallow, permission_error, or not_a_repo based on git-dir probes and sentinel files (`scripts/humanize.sh:156-204`).
- inputs_outputs_state: Inputs are synthetic git repos with clean, modified, staged, deleted, untracked, renamed, binary, detached, branch, rebase, merge, shallow, empty, non-git, and permission-variant states. Outputs are parsed count fields via `parse_result` (`tests/robustness/test-git-operations-robustness.sh:35-47`) and state strings from `humanize_detect_git_state`.
- gates_or_invariants: Clean repo must report zero modified/added/untracked (`tests/robustness/test-git-operations-robustness.sh:71-83`). Staged added files, modified files, deleted files, and untracked files must be counted correctly (`tests/robustness/test-git-operations-robustness.sh:98-157`, `tests/robustness/test-git-operations-robustness.sh:218-230`). Multiple states must coexist without masking each other (`tests/robustness/test-git-operations-robustness.sh:197-216`). State detection must classify normal, detached, rebase, merge, shallow, non-git, and permission scenarios (`tests/robustness/test-git-operations-robustness.sh:369-500`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, `scripts/humanize.sh`, and git (`tests/robustness/test-git-operations-robustness.sh:15-20`). Production callers include the monitor/status logic that displays git summary from `scripts/humanize.sh`, and this robustness suite is included in the aggregate runner (`tests/run-all-tests.sh:102-108`).
- edge_cases_or_failure_modes: Covered cases include non-git directory (`tests/robustness/test-git-operations-robustness.sh:168-177`), detached HEAD (`tests/robustness/test-git-operations-robustness.sh:180-195`), empty repo (`tests/robustness/test-git-operations-robustness.sh:233-245`), feature branch (`tests/robustness/test-git-operations-robustness.sh:248-263`), renamed files (`tests/robustness/test-git-operations-robustness.sh:265-282`), 20 untracked files (`tests/robustness/test-git-operations-robustness.sh:284-299`), filenames with spaces (`tests/robustness/test-git-operations-robustness.sh:301-314`), binary files (`tests/robustness/test-git-operations-robustness.sh:316-329`), staged plus unstaged same-file changes (`tests/robustness/test-git-operations-robustness.sh:331-347`), line deletions (`tests/robustness/test-git-operations-robustness.sh:349-367`), and permission behavior where either permission_error or not_a_repo is accepted (`tests/robustness/test-git-operations-robustness.sh:478-500`).
- validation_or_tests: The file initializes isolated repos, asserts each result, prints a test summary, and exits with the helper status (`tests/robustness/test-git-operations-robustness.sh:49-62`, `tests/robustness/test-git-operations-robustness.sh:502-507`). I inspected rather than executed it because the requested output is research notes only and the checkout is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `7 unique assigned item headings present in Item Evidence; one section per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`