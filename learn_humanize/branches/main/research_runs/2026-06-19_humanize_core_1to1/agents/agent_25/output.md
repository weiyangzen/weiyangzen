# agent_25 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-025 `directory` `skills/humanize-rlcr`
- cursor: `[_]`
- core_role: Codex flow entrypoint for the RLCR loop. Recursive inspection found one child, [skills/humanize-rlcr/SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:1), which defines a non-user-invocable flow skill named `humanize-rlcr`.
- algorithmic_behavior: The flow delegates loop startup to `scripts/setup-rlcr-loop.sh` with pass-through arguments, then requires each round to consume the generated prompt, implement, commit, write a round or finalize summary, and let the native Stop hook drive transition or blocking feedback. The required sequence is explicit at [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:24), with generated prompt and summary paths at [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:40).
- inputs_outputs_state: Inputs are `/flow:humanize-rlcr` arguments, usually a plan file or `--skip-impl`; runtime root is injected as `{{HUMANIZE_RUNTIME_ROOT}}` at [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:16). Outputs/state are `.humanize/rlcr/<timestamp>/round-<N>-prompt.md`, `round-<N>-summary.md`, finalize prompts/summaries, commits, review result files, and hook-managed state files.
- gates_or_invariants: The flow says setup nonzero exits must stop execution, and it forbids manual transition bypass. It lists Stop-hook gates for schema/state, branch consistency, plan-file integrity, incomplete todos, git clean state, push-every-round, summary presence, max iterations, full-alignment rounds, strict `COMPLETE`/`STOP` markers, review-phase transition, code-review `[P0-9]` markers, review failure/empty output, and open-question handling at [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:50).
- dependencies_and_callers: Coordinates with `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, generated `.humanize/rlcr` session files, native Codex Stop hooks, and `codex review` configured as `gpt-5.5:high` per [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:97).
- edge_cases_or_failure_modes: Setup failure is terminal for the flow; hook blocks must be followed exactly; manual edits to `state.md`/`finalize-state.md`, ad-hoc `codex exec`/`codex review`, or treating completion as done without hook-managed transition are explicit violation cases at [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:69).
- validation_or_tests: Validation is mostly delegated to the Stop-hook path and related tests. The skill itself is a contract/entrypoint, not executable test code. Its options define plan tracking, max rounds, model/timeout, base branch, full review interval, push-every-round, agent teams, yolo, and quiz-skipping behavior at [SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-rlcr/SKILL.md:76).
- skip_candidate: `no`

### HZ-055 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell utility and monitor frontend for Humanize loop state. It exposes the `humanize` function and implements `humanize monitor rlcr`, while routing skill/codex/gemini monitors to the sourced skill monitor.
- algorithmic_behavior: The script sources shared monitor utilities and loop constants at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:6), provides parser helpers for goal tracker and git status, then runs a live terminal monitor over `.humanize/rlcr`. It finds the latest session, locates the latest cached Codex run/review log for that session, parses state/frontmatter, draws a fixed status bar, tails log deltas, handles rotation/deletion, and switches sessions/logs when newer ones appear.
- inputs_outputs_state: Inputs include current working directory, `.humanize/rlcr/<timestamp>` session directories, `goal-tracker.md`, `state.md` or terminal `*-state.md`, cached logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, git status, terminal size, and subcommands. Outputs are terminal UI updates, status lines, log tail output, usage text, and exit codes.
- gates_or_invariants: It requires `.humanize/rlcr` to exist before monitoring [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:272). It validates log ordering so implementation `codex-run` rounds must be strictly lower than review `codex-review` rounds when both exist [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:364). It enforces a minimum terminal height before split-view rendering [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:661). It keeps signal cleanup idempotent and restores the terminal scroll region [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:736).
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for session/state/file-size helpers and on `scripts/lib/monitor-skill.sh` for non-RLCR monitors. README usage sources this script and calls `humanize monitor rlcr|skill|codex|gemini`; the CLI dispatcher is at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:1191). Shared helper `monitor_find_latest_session` uses `find` instead of globs to avoid zsh no-match errors [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize/scripts/lib/monitor-common.sh:49).
- edge_cases_or_failure_modes: Handles missing loop dir, no sessions, no log yet, deleted loop/session/log, truncated or rotated log, terminal resize, too-small terminal, SIGINT/SIGTERM, zsh array indexing, missing state file, malformed or absent goal tracker, non-git directories, detached/rebase/merge/shallow/permission git states, and inconsistent run/review log sequencing.
- validation_or_tests: Monitor behavior is covered by real monitor tests referenced in `tests/test-monitor-e2e-real.sh`, runtime/signal checks in `tests/test-monitor-runtime.sh`, zsh safety tests in `tests/test-zsh-monitor-safety.sh`, goal tracker parsing robustness tests, and git status robustness tests. `tests/test-humanize-escape.sh` also mirrors its `find`-based iteration style for empty/dotfile/session directory safety.
- skip_candidate: `no`

### HZ-085 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable regression specification for escape-prevention and shell-safety behavior around Humanize loop state. It tests `hooks/lib/loop-common.sh` behavior directly, especially `git_adds_humanize`.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, lowercases candidate commands with `to_lower`, and asserts whether `git_adds_humanize` blocks or allows each command. Positive cases cover direct `.humanize`, nested RLCR files, quoted paths, forced adds, broad force patterns, `-A/--all`, chained shell commands, and `git -C` / `--git-dir` forms. Negative cases ensure normal files, `.gitignore`, `git status/diff/log`, patch mode, and similarly named non-`.humanize` files are allowed.
- inputs_outputs_state: Inputs are synthetic command strings plus temporary directories under `/tmp` for `.humanize` existence and file-iteration scenarios. Outputs are colored PASS/FAIL lines and final exit `0` when all assertions pass, or exit `1` if any assertion fails [tests/test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize/tests/test-humanize-escape.sh:349).
- gates_or_invariants: The central invariant is that local `.humanize` loop state must not be staged into version control. Direct path detection begins at [tests/test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize/tests/test-humanize-escape.sh:83); force/broad-scope checks begin at [tests/test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize/tests/test-humanize-escape.sh:113); `-A/--all` requires a real `.humanize` directory to trigger at [tests/test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize/tests/test-humanize-escape.sh:133). The tested implementation splits chained commands on shell operators, detects `git ... add`, strips quotes, blocks true `.humanize` path components, blocks forced broad/all adds, and blocks `--all` when `.humanize` exists [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:1280).
- dependencies_and_callers: Depends on `to_lower` [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:790) and `git_adds_humanize` [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:1258). It also models `find`-based traversal used by monitor/session discovery helpers.
- edge_cases_or_failure_modes: Prevents false negatives for `./.humanize`, quoted `.humanize`, `path/to/.humanize`, RLCR subfiles, chained commands, `git -C`, combined flags like `-fA`/`-Af`, and `--all`. Prevents false positives for `.humanizeconfig`, `.humanize-backup`, `src/.humanizerc`, and read-only git commands. Zsh safety cases verify empty directories, dotfiles-only directories, missing `*-state.md`, terminal state file discovery, and timestamped session ordering [tests/test-humanize-escape.sh](/Users/wangweiyang/GitHub/humanize/tests/test-humanize-escape.sh:209).
- validation_or_tests: This file is itself a test. It validates both command classification and shell iteration, and exits nonzero if any assertion fails.
- skip_candidate: `no`

### HZ-115 `file` `prompt-template/block/bitlesson-delta-missing-notes.md`
- cursor: `[_]`
- core_role: Block-message template for BitLesson Delta validation when an `add` or `update` action lacks a meaningful `Notes:` rationale.
- algorithmic_behavior: The template renders `{{ACTION}}` into a concise blocking instruction requiring a `Notes:` field and rejecting empty or placeholder notes [prompt-template/block/bitlesson-delta-missing-notes.md](/Users/wangweiyang/GitHub/humanize/prompt-template/block/bitlesson-delta-missing-notes.md:3). The executable validator extracts `Notes:` from the BitLesson Delta block, trims whitespace, rejects empty values and bracket/angle-bracket placeholders, then loads this template through `load_and_render_safe` [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/scripts/bitlesson-validate-delta.sh:281).
- inputs_outputs_state: Inputs are `ACTION` from the validator and the invalid summary content. Output is a block reason returned to the hook/validator as part of a JSON block decision. It does not mutate state.
- gates_or_invariants: For non-`none` actions, concrete lesson IDs and non-placeholder notes are required before the round summary can pass. The placeholder regex is broad enough to reject values shaped like `[what changed and why]` or `<what changed and why>` [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/scripts/bitlesson-validate-delta.sh:283).
- dependencies_and_callers: Called by `scripts/bitlesson-validate-delta.sh`; related templates cover missing delta, inconsistent delta, invalid IDs, missing IDs, and empty knowledge base. The validator’s caller context is RLCR round-summary validation.
- edge_cases_or_failure_modes: Whitespace-only notes, bracket placeholders, and angle-bracket placeholders block. Valid explanatory text passes. Delta text hidden in fenced code blocks or HTML comments is rejected separately by the same validator.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` asserts whitespace-only notes, bracket placeholders, and angle placeholders block with a Notes error, while valid explanatory notes pass [tests/test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:101).
- skip_candidate: `no`

### HZ-145 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template enforcing the invariant that agents should not create or access `round-*-todos.md` files directly, and should use native task tools instead.
- algorithmic_behavior: The template tells the agent not to create/access `round-*-todos.md` and to use `TaskCreate`, `TaskUpdate`, and `TaskList` [prompt-template/block/todos-file-access.md](/Users/wangweiyang/GitHub/humanize/prompt-template/block/todos-file-access.md:1). `todos_blocked_message` renders this file with fallback text [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:839).
- inputs_outputs_state: Input is the attempted operation context from Read/Write/Edit/Bash validators. Output is the blocking message sent to stderr/validator response. It does not change loop state.
- gates_or_invariants: Direct todos file access is blocked except for a small active-loop allowlist: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` are hardcoded in `is_allowlisted_file` [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:816). Write validator blocks non-allowlisted todos files [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-write-validator.sh:54); Bash validator blocks modifications and only permits allowed active-loop paths [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-bash-validator.sh:558).
- dependencies_and_callers: Used by `hooks/lib/loop-common.sh` message rendering and by Read/Write/Edit/Bash validators. `tests/test-template-references.sh` includes `block/todos-file-access.md` in the expected common template list [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize/tests/test-template-references.sh:149).
- edge_cases_or_failure_modes: Blocks wrong-directory todos files, old loop directory todos files, generic relative `round-1-todos.md`, and non-allowlisted rounds. The main risk is intentional exception drift: the template says “Do NOT create or access” broadly, while validators preserve early-round allowlist compatibility.
- validation_or_tests: `tests/test-allowlist-validators.sh` exercises allowed and blocked todos paths across Read, Write, Edit, and Bash validators; `tests/test-template-references.sh` checks template existence.
- skip_candidate: `no`

### HZ-175 `file` `prompt-template/plan/refine-plan-qa-template.md`
- cursor: `[_]`
- core_role: Required output schema for the refine-plan QA artifact. It records how plan comments were processed, which changes were applied, unresolved decisions, metadata, and convergence status.
- algorithmic_behavior: The template requires sections for Summary, Comment Ledger, Answers, Research Findings, Plan Changes Applied, Remaining Decisions, and Refinement Metadata. It defines the comment ledger columns, per-comment answer/research/change formats, decision records with options/recommendation/status, and metadata fields including input/output/QA paths, counts, modified sections, convergence status, and date [prompt-template/plan/refine-plan-qa-template.md](/Users/wangweiyang/GitHub/humanize/prompt-template/plan/refine-plan-qa-template.md:7).
- inputs_outputs_state: Inputs are extracted plan comments (`CMT-N`), comment classifications, original text, plan locations, research findings, applied edits, unresolved decisions, and output paths. Output is a populated QA document, normally under the refine-plan QA directory. The template itself does not mutate state, but it defines an artifact that must be written as part of an atomic refine-plan transaction.
- gates_or_invariants: `commands/refine-plan.md` requires validation before QA generation: required sections present, no comment markers, all `AC-*` references valid, dependency IDs valid, exactly one routing tag per task row, and pending decisions aligned with convergence status [commands/refine-plan.md](/Users/wangweiyang/GitHub/humanize/commands/refine-plan.md:442). Phase 6 then requires reading and completely populating this template; the QA document is explicitly not optional [commands/refine-plan.md](/Users/wangweiyang/GitHub/humanize/commands/refine-plan.md:457).
- dependencies_and_callers: Referenced by `commands/refine-plan.md` and the `skills/humanize-refine-plan` flow diagram, which routes valid refined plans into QA population [skills/humanize-refine-plan/SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/humanize-refine-plan/SKILL.md:41). The refine-plan validator exposes `--qa-dir`, defaulting to `.humanize/plan_qa` [scripts/validate-refine-plan-io.sh](/Users/wangweiyang/GitHub/humanize/scripts/validate-refine-plan-io.sh:486).
- edge_cases_or_failure_modes: Ambiguous or unresolved comments must be represented as remaining decisions instead of silently dropped. Research requests require scope/findings/impact. Change requests require affected sections and cross-reference updates. The QA document must use the same main language as the refined plan, and translated variants must preserve identifiers when generated.
- validation_or_tests: `tests/test-refine-plan.sh` verifies the QA template includes all required sections, ledger columns, QA document path metadata, and convergence status metadata [tests/test-refine-plan.sh](/Users/wangweiyang/GitHub/humanize/tests/test-refine-plan.sh:803).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 6 assigned section headers are present exactly once, in the assigned table order
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`