# agent_30 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-030 `file` `agents/plan-understanding-quiz.md`
- cursor: `[_]`
- core_role: Defines the `plan-understanding-quiz` subagent prompt used as an advisory comprehension gate before `/humanize:start-rlcr-loop` starts an RLCR run. The agent metadata declares `model: opus` and read-only tools `Read`, `Glob`, `Grep`, so its intended role is analysis and question generation, not repository mutation.
- algorithmic_behavior: The prompt instructs the agent to read the supplied plan, inspect repository context such as `README.md`, `CLAUDE.md`, directory structure, and referenced files, then generate exactly two multiple-choice questions. Question 1 must test changed components and technical mechanism; Question 2 must test integration, existing patterns, or constraints. It also emits a 2-3 sentence technical plan summary. See [agents/plan-understanding-quiz.md](/Users/wangweiyang/GitHub/humanize/agents/plan-understanding-quiz.md:14) and [agents/plan-understanding-quiz.md](/Users/wangweiyang/GitHub/humanize/agents/plan-understanding-quiz.md:31).
- inputs_outputs_state: Input is plan file content supplied by the caller; the agent may read repo context. Output is a strict 13-field flat text contract: `QUESTION_1`, four options, `ANSWER_1`, `QUESTION_2`, four options, `ANSWER_2`, and `PLAN_SUMMARY`. No state files are modified by this prompt. The output contract is specified at [agents/plan-understanding-quiz.md](/Users/wangweiyang/GitHub/humanize/agents/plan-understanding-quiz.md:59).
- gates_or_invariants: Exactly two questions, each exactly four options A-D, exactly one correct answer, all 13 fields present, answer letters restricted to A/B/C/D, correct answer position randomized, and questions generated in the plan language. The prompt explicitly rejects generic, title-level, or exact-line-number trivia. See [agents/plan-understanding-quiz.md](/Users/wangweiyang/GitHub/humanize/agents/plan-understanding-quiz.md:36) and [agents/plan-understanding-quiz.md](/Users/wangweiyang/GitHub/humanize/agents/plan-understanding-quiz.md:77).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md` in the “Plan Understanding Quiz” phase. That command skips the quiz for `--skip-impl`, `--yolo`, `--skip-quiz`, help, or missing plan content; otherwise it invokes the `humanize:plan-understanding-quiz` agent, parses all 13 fields, asks the user both questions, and either proceeds or lets the user stop and review. See [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize/commands/start-rlcr-loop.md:64) and [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize/commands/start-rlcr-loop.md:86).
- edge_cases_or_failure_modes: If the agent output is malformed or any field is missing, the caller warns that the quiz is unavailable and continues setup. If the user answers one or both questions incorrectly, the command shows `PLAN_SUMMARY` and correct answers, then asks whether to proceed anyway or stop. Short or low-detail plans are handled by deriving questions from available hints rather than aborting.
- validation_or_tests: I did not find a dedicated test for this exact agent file. Runtime validation is primarily embedded in `commands/start-rlcr-loop.md`, which specifies parser behavior and the non-blocking/malformed-output fallback. The file is prompt/policy, so its test surface is indirect through startup command behavior.
- skip_candidate: `no`

### HZ-060 `file` `scripts/validate-gen-idea-io.sh`
- cursor: `[_]`
- core_role: Implements the IO and argument validation gate for `/humanize:gen-idea`, before directed-swarm exploration and draft writing. It converts user arguments into a validated machine-readable stdout contract for the command.
- algorithmic_behavior: Parses one positional idea input plus optional `--n` and `--output`; validates `N` as integer 2-10; classifies input as either an existing `.md` file or inline text using a path-looking heuristic; derives a slug; resolves default or user output; checks output existence and writability; verifies the idea template exists; then prints success metadata and, for inline input, the idea body in sentinel markers. See [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize/scripts/validate-gen-idea-io.sh:31), [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize/scripts/validate-gen-idea-io.sh:74), and [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize/scripts/validate-gen-idea-io.sh:189).
- inputs_outputs_state: Inputs are shell arguments and optional `CLAUDE_PLUGIN_ROOT`. Outputs are exit code plus stdout lines: `VALIDATION_SUCCESS`, `INPUT_MODE`, optional `IDEA_BODY_FILE`, `OUTPUT_FILE`, `SLUG`, `TEMPLATE_FILE`, `N`, and inline sentinels `=== IDEA_BODY_BEGIN ===` / `=== IDEA_BODY_END ===`. State transition is intentionally minimal: default output mode may create `.humanize/ideas/` with `mkdir -p`, but it does not create the draft file. User-supplied output directories are not auto-created.
- gates_or_invariants: Exit code map is explicit: `1` missing/empty input, `2` path-like bad input, `3` missing user output parent, `4` output exists, `5` not writable, `6` invalid args, `7` missing template. File inputs must exist, be readable, non-empty, and end in `.md`; inline inputs under 10 chars warn but proceed. Existing output files are always rejected. See [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize/scripts/validate-gen-idea-io.sh:4) and [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize/scripts/validate-gen-idea-io.sh:103).
- dependencies_and_callers: Direct caller is `commands/gen-idea.md`, which allows this script and instructs the command to parse the stdout fields before Phase 2. The script depends on `git rev-parse --show-toplevel` for default output rooting, `realpath`, `date`, `sed`, `tr`, `basename`, `dirname`, and `prompt-template/idea/gen-idea-template.md`. See [commands/gen-idea.md](/Users/wangweiyang/GitHub/humanize/commands/gen-idea.md:46) and [prompt-template/idea/gen-idea-template.md](/Users/wangweiyang/GitHub/humanize/prompt-template/idea/gen-idea-template.md:1).
- edge_cases_or_failure_modes: A nonexistent path containing whitespace is treated as inline text by design, per the script comment. Inline text that looks path-like only if it has no whitespace and ends in `.md` or contains `/` fails as `INPUT_NOT_FOUND`. Slug generation can collapse to empty and falls back to `idea`. `realpath -m` is attempted but falls back to the raw path, so portability depends on downstream handling. The caller currently documents running `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-idea-io.sh" $ARGUMENTS`, so shell splitting remains a caller-side risk for complex inline text.
- validation_or_tests: I found no dedicated test file for this validator. The behavior is specified by the script’s own exit-code map and by `commands/gen-idea.md`, which maps each exit code to user-facing command behavior and states that validation errors stop without partial output. See [commands/gen-idea.md](/Users/wangweiyang/GitHub/humanize/commands/gen-idea.md:53) and [commands/gen-idea.md](/Users/wangweiyang/GitHub/humanize/commands/gen-idea.md:203).
- skip_candidate: `no`

### HZ-090 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR monitor cleanup and graceful-stop runtime behavior in `scripts/humanize.sh`. It verifies terminal state restoration, deletion detection, signal handling, and idempotent cleanup.
- algorithmic_behavior: Creates a temporary `.humanize/rlcr` structure, generates small helper scripts, sources or mirrors relevant monitor functions, runs scenarios, and records pass/fail counters. It exits `0` only if all failures count to zero. The test setup is at [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize/tests/test-monitor-runtime.sh:45); summary and exit behavior are at [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize/tests/test-monitor-runtime.sh:493).
- inputs_outputs_state: Inputs are the repository checkout and host shell environment, with optional `zsh` availability. The test writes temporary files under `/tmp/test-monitor-runtime-$$`, then cleans them on exit. Outputs are human-readable PASS/FAIL lines and process exit status. It does not mutate repository files.
- gates_or_invariants: `_graceful_stop` must call `_cleanup`; `_cleanup` must be idempotent and call `_restore_terminal`; `_restore_terminal` must reset scroll region via `printf "\033[r"`; the monitor loop must stop gracefully if `.humanize/rlcr` disappears; bash must trap `INT TERM`; zsh must define `TRAPINT` and `TRAPTERM`; cleanup must reset traps. See [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize/tests/test-monitor-runtime.sh:56), [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize/tests/test-monitor-runtime.sh:254), and [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize/tests/test-monitor-runtime.sh:384).
- dependencies_and_callers: Targets `scripts/humanize.sh`, especially `_restore_terminal`, `_cleanup`, `_graceful_stop`, and signal trap definitions. The relevant implementation resets the scroll region, prevents double cleanup with `cleanup_done`, kills tail process if live, restores terminal, and checks loop directory existence in several monitor loops. See [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:707), [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:738), and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize/scripts/humanize.sh:840). Included in `tests/run-all-tests.sh` at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize/tests/run-all-tests.sh:80).
- edge_cases_or_failure_modes: The zsh SIGINT runtime test is skipped if `zsh` is unavailable. Some tests use source-text grep instead of fully exercising terminal behavior because `tput`/terminal state is hard to assert non-interactively. The line `if grep -q '_restore_terminal' "$2/scripts/humanize.sh" | grep -q '_cleanup'; then` is logically ineffective because `grep -q` produces no stdout, but the following repository-level check with `grep -A30 "^    _cleanup()"` covers the intended invariant.
- validation_or_tests: This file is itself the validation artifact. It cross-checks both mocked behavior and source implementation, and `tests/run-all-tests.sh` lists it among the main suites.
- skip_candidate: `no`

### HZ-120 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt block used by RLCR validators when a user/tool attempts to modify `finalize-state.md`, the state file for the Finalize Phase.
- algorithmic_behavior: Emits a blocking message telling the actor that `finalize-state.md` is loop-managed and redirects work toward Finalize Phase tasks: run the code-simplifier agent, commit changes, and write `finalize-summary.md`. The template itself has no variables and is loaded through `finalize_state_file_blocked_message`.
- inputs_outputs_state: Input is the validator’s decision that a path or bash command targets `finalize-state.md`. Output is a rendered block message to stderr plus validator exit code `2` at the caller level. It does not change state; it prevents user/tool changes to the loop-managed state file.
- gates_or_invariants: `finalize-state.md` must not be written, edited, redirected to, moved from, or copied from except through authorized cancel handling. Specificity matters because generic `state.md` matching also matches `finalize-state.md`; validators check finalize first. See [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-write-validator.sh:198), [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-edit-validator.sh:161), and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-bash-validator.sh:280).
- dependencies_and_callers: Loaded by `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh`, which uses `load_and_render_safe` with an inline fallback. The template loader provides fallback behavior and single-pass rendering. See [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:868) and [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/template-loader.sh:188).
- edge_cases_or_failure_modes: If the template is missing or renders empty, callers still emit the fallback message. Bash validator also catches source-side `mv`/`cp` and `sh -c`/`bash -c` wrapper bypass attempts for `finalize-state.md`. Authorized cancellation can bypass the block when `is_cancel_authorized` approves the state transition to `cancel-state.md`.
- validation_or_tests: `tests/test-finalize-phase.sh` verifies helper existence, path matching, and that Write/Edit/Bash validators block `finalize-state.md` with exit `2` and a finalize-related message. It also tests `mv FROM` and `cp FROM` protections. See [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize/tests/test-finalize-phase.sh:316), [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize/tests/test-finalize-phase.sh:398), and [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize/tests/test-finalize-phase.sh:458).
- skip_candidate: `no`

### HZ-150 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Prompt block used by read validators when an actor tries to read a loop file from the wrong location instead of the active loop directory.
- algorithmic_behavior: Renders `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND` into guidance that loop files are in the active directory and lists the current round prompt and summary paths. It also says to use `cat {{FILE_PATH}}` if that file is really needed. See [prompt-template/block/wrong-file-location.md](/Users/wangweiyang/GitHub/humanize/prompt-template/block/wrong-file-location.md:3).
- inputs_outputs_state: Inputs are validator-supplied template variables from `hooks/loop-read-validator.sh`. Output is a blocking stderr message and caller exit `2`; no repository state changes. The template is rendered via `load_and_render_safe`.
- gates_or_invariants: Read access to round files outside the active `.humanize/rlcr/<session>/` directory is blocked; current-round file references are normalized to active loop paths. Goal tracker reads inside the loop are also checked against the active goal tracker path before this block is rendered. See [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-read-validator.sh:224) and [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-read-validator.sh:267).
- dependencies_and_callers: Called from `hooks/loop-read-validator.sh` for wrong goal-tracker path and wrong round-file location. It depends on active loop discovery, strict state parsing, current round extraction, and the shared template loader. Active loop and state resolution come from `hooks/lib/loop-common.sh`; rendering comes from `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If no active loop exists, the read validator exits `0` and this block is not used. If state parsing fails, the validator blocks with a malformed-state error before reaching this template. If the template is unavailable, the read validator’s fallback text is rendered instead. The wording “If you need this file, use: `cat {{FILE_PATH}}`” could be confusing because the validator just blocked a read; algorithmically it is guidance text, not an allow rule.
- validation_or_tests: Template loading/rendering behavior is covered by `tests/test-template-loader.sh`, including fallback behavior and literal rendering of shell metacharacters. `tests/test-template-references.sh` requires critical validators, including read/write/edit validators, to use `load_and_render_safe`. See [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize/tests/test-template-loader.sh:197), [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize/tests/test-template-loader.sh:250), and [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize/tests/test-template-references.sh:172).
- skip_candidate: `no`

### HZ-180 `file` `skills/ask-codex/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction for one-shot Codex consultation through `scripts/ask-codex.sh`. It defines safe invocation patterns, output interpretation, and error handling for `/humanize:ask-codex`.
- algorithmic_behavior: If the user supplies only free-form text, the skill says to call `"${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh" "$ARGUMENTS"`. If flags are present, it instructs the caller to reconstruct shell arguments so flags stay separate and the remaining question is one quoted final argument. It explicitly forbids bare `$ARGUMENTS` expansion because shell reparsing can break or misinterpret user text. See [skills/ask-codex/SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/ask-codex/SKILL.md:14) and [skills/ask-codex/SKILL.md](/Users/wangweiyang/GitHub/humanize/skills/ask-codex/SKILL.md:30).
- inputs_outputs_state: Input is user question/task plus optional `--codex-model MODEL:EFFORT` and `--codex-timeout SECONDS`. The skill delegates to `scripts/ask-codex.sh`, whose stdout is Codex’s response and stderr is status/debug information. The script saves input/output/metadata under `.humanize/skill/<unique-id>/` and debug files under cache. See [scripts/ask-codex.sh](/Users/wangweiyang/GitHub/humanize/scripts/ask-codex.sh:202) and [scripts/ask-codex.sh](/Users/wangweiyang/GitHub/humanize/scripts/ask-codex.sh:391).
- gates_or_invariants: Non-zero script exit must be reported. Exit `1` means validation error, `124` means timeout and should suggest increasing `--codex-timeout`, other non-zero values are Codex process errors. The underlying script validates Codex availability, non-empty question, safe model/effort characters, timeout numericity, project root, non-empty Codex stdout, and writes metadata statuses `success`, `error`, `timeout`, or `empty_response`.
- dependencies_and_callers: Depends on `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh`, Codex CLI, `portable-timeout.sh`, `hooks/lib/loop-common.sh`, and project-root/config loading for default model/effort. It is referenced by docs, `commands/gen-plan.md`, `commands/start-rlcr-loop.md` for `analyze` task routing, setup prompt generation, and monitor skill tooling. Search results show task-tag tests assert prompts include `/humanize:ask-codex`.
- edge_cases_or_failure_modes: Primary skill-level risk is unsafe shell expansion; the skill directly mitigates it. Script-level failures include missing Codex CLI, empty question, invalid model/effort characters, empty Codex response, timeout, non-writable home cache fallback, and non-zero Codex exit. Concurrent calls are expected to create unique project-local and cache directories using timestamp, PID, and random suffix.
- validation_or_tests: `tests/test-ask-codex.sh` mocks Codex and verifies validation errors, successful stdout propagation, saved files, metadata statuses, non-zero exit propagation, timeouts, unique directories, argument parsing, cache debug files, and that the skill warns against bare `$ARGUMENTS` while documenting quoted invocation. `tests/test-unified-codex-config.sh` verifies config-backed defaults and CLI overrides are reported. See [tests/test-ask-codex.sh](/Users/wangweiyang/GitHub/humanize/tests/test-ask-codex.sh:415) and [tests/test-unified-codex-config.sh](/Users/wangweiyang/GitHub/humanize/tests/test-unified-codex-config.sh:677).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6 item sections present; all assigned IDs covered once in `## Item Evidence`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`