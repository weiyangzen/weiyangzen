# agent_15 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt templates for the RLCR loop’s round transitions: initial/continued implementation, review correction, drift recovery, finalize, methodology analysis, goal-tracker fallback, open-question handling, push policy, post-alignment action routing, and Agent Teams coordination.
- algorithmic_behavior: The directory is flat and contains 14 templates. `next-round-prompt.md` requires re-reading the plan/tracker/reviews and writing a round contract before code, then classifies work into `[mainline]`, `[blocking]`, and `[queued]` lanes (`prompt-template/claude/next-round-prompt.md:12`, `:20`, `:31`). `drift-replan-prompt.md` switches the next round into recovery mode when prior rounds stall/regress, requiring a recovered mainline objective and success criteria that would restore mainline progress (`prompt-template/claude/drift-replan-prompt.md:3`, `:25`, `:31`). `review-phase-prompt.md` keeps review findings from replacing the round objective and permits only blocking/queued review lanes unless the original mainline is incomplete (`prompt-template/claude/review-phase-prompt.md:5`, `:20`, `:32`). `finalize-phase-prompt.md` and `finalize-phase-skipped-prompt.md` gate the final phase into functionality-preserving simplification plus summary output, with the skipped variant warning that code review was not fully available (`prompt-template/claude/finalize-phase-prompt.md:3`, `:16`, `:41`; `prompt-template/claude/finalize-phase-skipped-prompt.md:3`, `:9`, `:40`). `methodology-analysis-prompt.md` defines the post-exit methodology improvement analysis and sanitization contract (`prompt-template/claude/methodology-analysis-prompt.md:8`, `:22`, `:43`, `:71`).
- inputs_outputs_state: Inputs are template variables rendered by `hooks/lib/template-loader.sh`, whose syntax and single-pass substitution are defined at `hooks/lib/template-loader.sh:7` and `:52`; missing templates use `load_and_render_safe` fallback behavior at `hooks/lib/template-loader.sh:188`. The Stop hook writes next-round/finalize prompt files by rendering these templates: finalize templates at `hooks/loop-codex-stop-hook.sh:1358` and `:1386`, next-round or drift-replan at `hooks/loop-codex-stop-hook.sh:2055` and `:2066`, then appends footer/push/goal-tracker/agent-team blocks at `:2160`, `:2168`, `:2177`, and `:2185`. The setup script injects initial Agent Teams templates into `round-0-prompt.md` when enabled (`scripts/setup-rlcr-loop.sh:1370`).
- gates_or_invariants: Templates enforce “round contract exists before implementation” (`next-round-prompt.md:27`; `drift-replan-prompt.md:33`), a single stable mainline objective (`next-round-prompt.md:20`; `review-phase-prompt.md:12`), immutable goal-tracker preservation after Round 0 (`next-round-prompt.md:63`; `drift-replan-prompt.md:57`), no queued work takeover (`next-round-prompt.md:39`; `drift-replan-prompt.md:65`), and functionality-preserving finalize-only changes (`finalize-phase-prompt.md:18`). `open-question-notice.md` forces user clarification before resolving other review findings when Codex emits open questions (`prompt-template/claude/open-question-notice.md:1`). `next-round-footer.md` forbids exiting by lying, editing loop state, or running cancellation as a workaround (`prompt-template/claude/next-round-footer.md:4`).
- dependencies_and_callers: Main callers are `hooks/loop-codex-stop-hook.sh` and `scripts/setup-rlcr-loop.sh`. `post-alignment-action-items.md` is appended only when `FULL_ALIGNMENT_CHECK` is true (`hooks/loop-codex-stop-hook.sh:2152`). Agent Teams continuation is appended only in implementation phase, not review phase (`hooks/loop-codex-stop-hook.sh:2183`). Tests assert Agent Teams templates exist and contain required structure (`tests/test-agent-teams.sh:331`, `:350`, `:372`) and that implementation/drift prompts include Agent Teams continuation while review prompts do not (`tests/test-agent-teams.sh:614`, `:632`, `:704`).
- edge_cases_or_failure_modes: If a template is missing or renders empty, callers rely on fallback content through `load_and_render_safe` (`hooks/lib/template-loader.sh:197`, `:205`). Drift prompts receive a BitLesson block if required and absent (`hooks/loop-codex-stop-hook.sh:2077`). Agent Teams fallback text is used when the continuation/core templates cannot be loaded (`hooks/loop-codex-stop-hook.sh:2188`; fallback begins after `:2193`). The directory is a core prompt-contract surface, not executable code, so failures manifest as incorrect next-round instructions rather than shell exit codes.
- validation_or_tests: `tests/test-agent-teams.sh` validates template existence/content and phase-specific prompt inclusion. Template-loading behavior is broadly covered by template-loader tests referenced from `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` in the repository search results. Stop-hook tests also exercise prompt generation indirectly through finalize/review/next-round flows.
- skip_candidate: `no`

### HZ-045 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: `UserPromptSubmit` hook that protects RLCR loop invariants before accepting a user prompt: active-loop detection, strict state schema parsing, branch consistency, and plan-file tracking-mode consistency.
- algorithmic_behavior: The hook reads JSON input from stdin (`hooks/loop-plan-file-validator.sh:27`), extracts `session_id` (`:30`), locates the active loop under `.humanize/rlcr` with session filtering (`:33`), resolves the active state file (`:42`), and fail-closes on malformed state (`:45`). It maps parsed state into `PLAN_TRACKED`, `PLAN_FILE`, and `START_BRANCH` (`:51`). It then validates required legacy guard fields `plan_tracked` and `start_branch` (`:80`), verifies the current git branch equals the loop’s starting branch (`:96`, `:108`), and enforces plan-file tracking mode (`:124`, `:194`).
- inputs_outputs_state: Inputs are hook JSON, project root from `resolve_project_root`, active loop state frontmatter, and git status/branch outputs. Output is either silent success with exit 0, a JSON object with `"decision": "block"` and reason on user-action blocks, or exit 1 only for strict malformed state (`hooks/loop-plan-file-validator.sh:46`). It does not mutate files. State consumed comes from `hooks/lib/loop-common.sh`, including field constants for `plan_tracked` and `start_branch` (`hooks/lib/loop-common.sh:24`) and active-state resolution order including methodology/finalize/state files (`hooks/lib/loop-common.sh:268`).
- gates_or_invariants: The hook blocks if schema-critical fields are missing, using `block/schema-outdated.md` via `load_and_render_safe` (`hooks/loop-plan-file-validator.sh:60`, `:66`). It blocks branch switching during an active loop (`:108`). If `plan_tracked=true`, the plan must still be tracked and clean (`:124`, `:175`, `:185`). If `plan_tracked=false`, the plan must not become tracked (`:194`, `:221`). Git operations are wrapped in a 30-second timeout and fail closed on timeout/unexpected errors (`:24`, `:130`, `:155`, `:200`, `:209`).
- dependencies_and_callers: Installed as a `UserPromptSubmit` hook in `hooks/hooks.json:4` through `:10`. Depends on `hooks/lib/loop-common.sh` for root/session/state parsing (`hooks/loop-plan-file-validator.sh:15`) and `scripts/portable-timeout.sh` for git timeouts (`:20`). `find_active_loop` deliberately checks only the newest active session when no session filter is present to prevent stale loop revival (`hooks/lib/loop-common.sh:342`), while session-filtered mode scans newest-to-oldest with exact session isolation (`hooks/lib/loop-common.sh:359`).
- edge_cases_or_failure_modes: No active loop exits silently (`hooks/loop-plan-file-validator.sh:37`). Malformed state exits with an stderr error and code 1 (`:46`). Git branch read failures, plan tracking checks, and status checks return JSON blocks rather than shell failure (`:99`, `:130`, `:155`, `:209`). `parse_state_file_strict` also requires `review_started` and `base_branch` before this script’s own `plan_tracked/start_branch` field loop can run (`hooks/lib/loop-common.sh:562`, `:566`), so older state files can be rejected as malformed first. Quoted `start_branch` and `plan_file` are stripped in `_parse_state_fields` (`hooks/lib/loop-common.sh:449`, `:453`).
- validation_or_tests: `tests/test-plan-file-hooks.sh` directly exercises valid pass-through (`:145`), quoted plan parsing (`:160`), malformed state blocking (`:175`, `:196`), branch drift blocking (`:220`), quoted branch parsing and mismatch detection (`:399`, `:425`), and tracked-plan protection in Stop hook companion logic (`:680`). `tests/robustness/test-hook-system-robustness.sh` verifies the plan-file validator tolerates edit/non-plan JSON and empty JSON without crashing (`:198`, `:216`, `:231`). `tests/robustness/test-state-file-robustness.sh` validates quoted state parsing (`:100`).
- skip_candidate: `no`

### HZ-075 `file` `tests/test-codex-hook-install.sh`
- cursor: `[_]`
- core_role: Executable specification for Codex-native installation: skill bundle sync, native `hooks.json` merge/idempotence, `codex_hooks` feature enablement, Codex-only user config seeding, and `bitlesson-selector` shim routing.
- algorithmic_behavior: The test builds an isolated fake Codex environment (`tests/test-codex-hook-install.sh:29`), creates a fake `codex` binary supporting `features list`, `features enable codex_hooks`, and `exec` (`:40`), seeds an existing `hooks.json` with unrelated SessionStart/Stop hooks and stale Humanize stop hook (`:71`), then runs `scripts/install-skill.sh --target codex` with isolated Codex/config/bin dirs (`:110`). It validates installation artifacts, parses `hooks.json` with Python to inspect commands (`:168`), invokes the generated `bitlesson-selector` shim (`:235`), reruns install for idempotence (`:251`), and verifies unsupported Codex builds fail (`:290`).
- inputs_outputs_state: Inputs are fake CLI outputs, an existing Codex `hooks.json`, install paths, and a test `bitlesson.md`. Outputs are installed `skills/humanize` and `skills/humanize-rlcr`, Codex `hooks.json`, `.codex-hooks-enabled` marker, Humanize user config JSON, executable command shim, install logs, and feature-enable log entries (`tests/test-codex-hook-install.sh:118`, `:130`, `:136`, `:142`, `:148`). The installed user config must contain `bitlesson_model: gpt-5.5` and `provider_mode: codex-only` (`:154`, `:161`).
- gates_or_invariants: Managed Stop hooks must be added exactly once, stale Humanize hook paths removed, and unrelated hooks preserved (`tests/test-codex-hook-install.sh:198`, `:204`, `:210`, `:216`, `:222`). Reinstall must remain idempotent (`:278`). Feature enablement is expected on each install/update (`:284`). Unsupported Codex without `codex_hooks` must fail nonzero and explain missing support (`:310`, `:326`).
- dependencies_and_callers: Tests `scripts/install-skill.sh`, which syncs skills/runtime components (`scripts/install-skill.sh:155`, `:162`), calls `install_codex_user_config` (`scripts/install-skill.sh:269`) and `install_codex_native_hooks` (`:253`). Native hook merging is delegated to `scripts/install-codex-hooks.sh`, which requires the `codex_hooks` feature (`scripts/install-codex-hooks.sh:75`), filters stale managed Stop hooks via regex (`:138`), appends the template-managed Stop groups (`:163`), writes `hooks.json` (`:170`), and enables the feature (`:175`). The shim implementation dispatches to installed `scripts/bitlesson-select.sh` candidates (`scripts/install-skill.sh:350`, `:368`).
- edge_cases_or_failure_modes: Existing `hooks.json` may contain stale Humanize hooks from old runtime roots; merge removes only managed Stop hook commands and leaves unrelated hooks/groups alone (`scripts/install-codex-hooks.sh:140`). Runtime paths are JSON-escaped and shell-quoted to survive spaces/metacharacters (`scripts/install-codex-hooks.sh:105`, `:112`). Existing malformed Humanize user config causes install failure (`scripts/install-skill.sh:301`, `:325`). Missing Codex feature causes hard failure (`scripts/install-codex-hooks.sh:80`).
- validation_or_tests: This file is itself the spec; it concludes through `print_test_summary "Codex Hook Install Tests"` (`tests/test-codex-hook-install.sh:334`). It also indirectly validates `config/codex-hooks.json`, `scripts/install-codex-hooks.sh`, `scripts/install-skill.sh`, the installed runtime bundle, and the `bitlesson-selector` shim dispatch path.
- skip_candidate: `no`

### HZ-105 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: Executable specification for the unified Codex configuration model across defaults, config merge hierarchy, loop state parsing, stop hook execution, setup script state generation, and `ask-codex`.
- algorithmic_behavior: The test asserts `config/default_config.json` uses `codex_model` and `codex_effort`, not stale reviewer keys (`tests/test-unified-codex-config.sh:57`, `:68`, `:75`). It sources `scripts/lib/config-loader.sh` and verifies default-only and project override merge behavior (`:90`, `:101`, `:115`). It sources `hooks/lib/loop-common.sh` to validate `DEFAULT_CODEX_MODEL/DEFAULT_CODEX_EFFORT` from config, caller preset precedence, invalid config fallback/warnings, and unsupported model prefix warnings (`:132`, `:167`, `:180`, `:196`, `:223`). It checks stop-hook fallback from `STATE_CODEX_*` to defaults (`:253`), setup script template fields (`:377`), stale reviewer keys ignored (`:391`), invalid stop-hook effort blocks Codex invocation (`:472`), setup-loop runtime state generation (`:547`), regex validation (`:636`), and `ask-codex` config-backed defaults/override behavior (`:676`, `:708`).
- inputs_outputs_state: Inputs include default config JSON, user/project `.humanize/config.json`, synthetic state files, setup script flags, mock Codex binaries, and test git repos. Outputs are merged config JSON values, shell defaults after sourcing `loop-common`, parsed `STATE_CODEX_*` variables, stderr warnings, generated RLCR `state.md`, `round-0-summary.md`, and `ask-codex` stderr model/effort reports (`tests/test-unified-codex-config.sh:599`, `:615`, `:740`, `:753`).
- gates_or_invariants: Default values must be `gpt-5.5` and `high` (`config/default_config.json:2`). Config precedence is default < user < project through `load_merged_config` slurped layers (`scripts/lib/config-loader.sh:113`). Caller-pre-set defaults outrank config (`hooks/lib/loop-common.sh:222`, `:230`). Configured Codex model must be shell-safe and start with `gpt-` or `oN`; effort must be one of `xhigh|high|medium|low` (`hooks/lib/loop-common.sh:211`, `:216`, `:224`). Stop hook re-validates state-sourced model/effort and ends unexpectedly on invalid effort without invoking Codex (`hooks/loop-codex-stop-hook.sh:172`, `:179`).
- dependencies_and_callers: Depends on `config/default_config.json`, `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, and `scripts/ask-codex.sh`. `ask-codex.sh` sources `loop-common` for defaults (`scripts/ask-codex.sh:31`), assigns runtime model/effort from those defaults (`:41`), validates model/effort syntax (`:172`, `:180`), and builds Codex exec args using `model_reasoning_effort` (`:244`). Setup execution verifies `--codex-model gpt-5.3:xhigh` writes split `codex_model`/`codex_effort` fields (`tests/test-unified-codex-config.sh:585`, `:615`).
- edge_cases_or_failure_modes: Missing `jq`, `codex`, or referenced scripts cause skips for affected blocks (`tests/test-unified-codex-config.sh:65`, `:553`, `:555`). Malformed optional configs are ignored with warnings by config loader, while required default config malformation is fatal (`scripts/lib/config-loader.sh:52`). Stale `loop_reviewer_*` config/state keys must have no effect (`tests/test-unified-codex-config.sh:400`, `:419`). Invalid effort in loop state is a hard stop-hook error and must not invoke Codex (`tests/test-unified-codex-config.sh:531`, `:537`).
- validation_or_tests: This file is the executable validator and ends with `print_test_summary "Unified Codex Config Test Summary"` (`tests/test-unified-codex-config.sh:779`). Its coverage spans both static grep assertions and runtime behavior with isolated repos and mock Codex commands.
- skip_candidate: `no`

### HZ-135 `file` `prompt-template/block/methodology-analysis-state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking message template for attempts to modify `methodology-analysis-state.md`, a loop-managed state file in the Methodology Analysis Phase.
- algorithmic_behavior: The template tells the agent it cannot modify `methodology-analysis-state.md` because the loop system manages it (`prompt-template/block/methodology-analysis-state-file-modification.md:1`, `:3`). It redirects allowed phase work to spawning the Opus analysis agent, reviewing the sanitized report, optionally helping file a GitHub issue, and writing the completion marker `methodology-analysis-done.md` (`:5`, `:9`).
- inputs_outputs_state: It has no variables; it is loaded through `methodology_analysis_state_file_blocked_message` in `hooks/lib/loop-common.sh:1012`. Output is the text used by write/edit/bash validators when a path targets `methodology-analysis-state.md`; path recognition is handled by `is_methodology_analysis_state_path` at `hooks/lib/loop-common.sh:1008`.
- gates_or_invariants: The state file is system-owned and must not be user/agent-modified. The only allowed completion signal for this phase is writing meaningful content to `methodology-analysis-done.md`, matching the methodology prompt’s completion-marker contract (`prompt-template/claude/methodology-analysis-prompt.md:71`).
- dependencies_and_callers: Called by shared validator logic through `hooks/lib/loop-common.sh`, with template fallback if missing (`hooks/lib/loop-common.sh:1014`, `:1018`). Coordinates with `prompt-template/claude/methodology-analysis-prompt.md`, which creates the analysis workflow and requires the done marker (`prompt-template/claude/methodology-analysis-prompt.md:43`, `:73`).
- edge_cases_or_failure_modes: If the template is missing or empty, the fallback in `methodology_analysis_state_file_blocked_message` still blocks with a shorter message (`hooks/lib/loop-common.sh:1014`). Since this is a static block, its only algorithmic risk is stale or incomplete instructions causing an agent to mutate loop state or fail to create the done marker.
- validation_or_tests: Template loading and reference coverage is exercised by template tests (`tests/test-template-references.sh` includes block templates), and validators using shared block-message functions cover the path-blocking behavior indirectly.
- skip_candidate: `no`

### HZ-165 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Small prompt appendage for the round immediately after a Full Goal Alignment Check, steering the next implementation round toward alignment findings without letting queued work take over.
- algorithmic_behavior: The template instructs the agent to address forgotten items, prioritize unmet acceptance criteria, un-defer unjustified deferrals, and keep queued issues queued unless they block mainline progress (`prompt-template/claude/post-alignment-action-items.md:4`, `:8`). This is a transition modifier rather than a standalone phase prompt.
- inputs_outputs_state: It has no variables. `hooks/loop-codex-stop-hook.sh` appends it to the next prompt only when `FULL_ALIGNMENT_CHECK` is true (`hooks/loop-codex-stop-hook.sh:2152`, `:2156`). The output is extra prompt content in the generated next-round prompt file.
- gates_or_invariants: Maintains the mainline/AC alignment invariant after periodic full review: forgotten tasks and unmet ACs become high priority, unjustified deferrals are reversed, and queued issues remain non-blocking unless their status changes (`post-alignment-action-items.md:5`, `:7`, `:8`).
- dependencies_and_callers: Depends on Stop hook’s full-alignment detection and template loader. It coordinates with `next-round-prompt.md` lane rules (`prompt-template/claude/next-round-prompt.md:31`) and goal-tracker update expectations (`prompt-template/claude/next-round-prompt.md:63`).
- edge_cases_or_failure_modes: If the template is absent, Stop hook simply does not append it because it checks for non-empty content (`hooks/loop-codex-stop-hook.sh:2154`, `:2155`). The file is core-adjacent and small, but not a skip candidate because it directly changes post-alignment transition behavior.
- validation_or_tests: Covered indirectly through Stop hook prompt generation and template reference tests. No dedicated unit test was found for this exact appendage in the assigned inspection, so residual risk is untested conditional placement rather than template parsing.
- skip_candidate: `no`

### HZ-195 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for active RLCR session discovery, especially zombie-loop prevention, active/finalize state recognition, and tolerance of unusual session directory layouts.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` (`tests/robustness/test-session-robustness.sh:16`) and exercises `find_active_loop` over synthetic `.humanize/rlcr` trees. Positive cases verify newest active `state.md` detection (`:33`), `finalize-state.md` detection (`:48`), efficient many-session scanning (`:63`), both state files present (`:105`), and rapid session creation (`:240`). Negative/edge cases verify empty/nonexistent bases (`:128`, `:140`), no subdirectories (`:150`), no state files (`:164`), finished/cancelled-only loops ignored (`:259`, `:273`), and newest terminal/empty directories preventing older stale sessions from being revived (`:89`, `:287`).
- inputs_outputs_state: Inputs are temporary directories and marker files named `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`. Outputs are `find_active_loop` return strings compared against expected directory paths or empty results. It also measures elapsed time for 15 sessions and requires under 1000 ms (`tests/robustness/test-session-robustness.sh:74`, `:80`).
- gates_or_invariants: `find_active_loop` without session filter checks only the lexicographically newest immediate child directory and returns it only if it has an active state (`hooks/lib/loop-common.sh:342`, `:349`). Active states are `methodology-analysis-state.md`, `finalize-state.md`, then `state.md` in that priority order (`hooks/lib/loop-common.sh:271`). The function intentionally ignores deep nested paths, as asserted by Test 13 (`tests/robustness/test-session-robustness.sh:224`, `:232`). Finished or cancelled newest loops must not allow older loops to resurrect (`tests/robustness/test-session-robustness.sh:296`).
- dependencies_and_callers: Direct dependency is `find_active_loop` and `resolve_active_state_file` in `hooks/lib/loop-common.sh`. These functions are used by validators and Stop hook; `loop-plan-file-validator.sh` calls `find_active_loop "$LOOP_BASE_DIR" "$HOOK_SESSION_ID"` for session-aware loop filtering (`hooks/loop-plan-file-validator.sh:30`, `:35`).
- edge_cases_or_failure_modes: Symlinked session directories may return a symlink or real session but must not fail empty (`tests/robustness/test-session-robustness.sh:194`). Names with spaces and non-standard names must work (`:179`, `:210`). Unicode handling test is conservative and only asserts graceful non-empty behavior after creating a normal `session-test` directory (`:304`). The test does not cover session-filtered exact-match behavior or background marker fallback; those are implemented in `find_active_loop` session-filter branch (`hooks/lib/loop-common.sh:359`) but outside this file’s scope.
- validation_or_tests: The file itself is the validation and ends with `print_test_summary "Session Robustness Test Summary"` (`tests/robustness/test-session-robustness.sh:322`). It is intentionally read-only to the source checkout, using temp dirs via `setup_test_dir`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`