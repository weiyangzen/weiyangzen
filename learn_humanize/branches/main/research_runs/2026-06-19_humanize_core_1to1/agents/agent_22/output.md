# agent_22 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-022 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: This directory is the packaged Humanize skill contract. Recursive inspection found one child, `skills/humanize/SKILL.md`, which defines the RLCR workflow, commands, plan shape, state artifacts, monitoring path, and public exit-code contract. It is algorithmically important as the user-facing operating manual for the loop, not as executable code.
- algorithmic_behavior: The skill defines a two-phase RLCR algorithm: Phase 1 has an AI implement, write a summary, then Codex reviews until the final line is `COMPLETE`; Phase 2 runs `codex review --base <branch>`, loops on `[P0-9]` findings, and enters Finalize when clean (`skills/humanize/SKILL.md:32-48`). It also defines plan generation from drafts into structured acceptance criteria, bounds, dependencies, and milestones (`skills/humanize/SKILL.md:50-58`, `111-150`).
- inputs_outputs_state: Inputs are a plan file or `--skip-impl`, plus options such as max iterations, Codex model/effort, timeout, base branch, full-review interval, plan immutability, push-per-round, agent teams, quiz controls, and privacy (`skills/humanize/SKILL.md:61-88`). Outputs live under `.humanize/rlcr/<timestamp>/`, including `state.md`, `goal-tracker.md`, round summaries/reviews, `finalize-state.md`, `finalize-summary.md`, methodology-analysis files, and `complete-state.md` (`skills/humanize/SKILL.md:180-203`).
- gates_or_invariants: The contract requires summaries before exit, goal-tracker updates, detailed changed-file/test evidence, no state-file cheating, native Codex Stop hook use, and trust in external review (`skills/humanize/SKILL.md:166-173`). The Goal Tracker invariant splits immutable Ultimate Goal/Acceptance Criteria from mutable Active/Completed/Deferred/Evolution sections (`skills/humanize/SKILL.md:152-164`).
- dependencies_and_callers: The documented entrypoints are `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh` monitor usage (`skills/humanize/SKILL.md:61-109`, `205-212`). Runtime depends on the `codex` CLI for review (`skills/humanize/SKILL.md:175-178`).
- edge_cases_or_failure_modes: Review-only mode is supported via `--skip-impl`; max iterations can stop the loop; tracked plan immutability and push-every-round are opt-in gates; missing Codex is a prerequisite failure. The directory itself has no hidden recursive descendants beyond `SKILL.md`, so executable behavior is delegated to sibling `scripts/` and `hooks/`.
- validation_or_tests: The skill references exit-code contracts for `ask-codex.sh` and `validate-gen-plan-io.sh` (`skills/humanize/SKILL.md:214-229`). Concrete behavior is validated elsewhere in the repository by shell tests such as finalize, stop-hook, config, and plan-validation specs.
- skip_candidate: `no`

### HZ-052 `file` `scripts/bitlesson-select.sh`
- cursor: `[_]`
- core_role: Runtime selector for the BitLesson memory algorithm. It receives a task, related paths, and a BitLesson knowledge-base file, then routes to Codex or Claude to choose directly relevant lesson IDs.
- algorithmic_behavior: The script loads merged config, resolves `bitlesson_model`, `codex_model`, and `provider_mode`, validates CLI args, reads the BitLesson file, short-circuits to `LESSON_IDS: NONE` when the file contains no concrete `## Lesson:` entries, then constructs a strict two-line selector prompt (`scripts/bitlesson-select.sh:9-29`, `50-109`, `148-180`). Decision rules force precise matching, `NONE` for no relevance, and no external tool use inside the selector prompt (`scripts/bitlesson-select.sh:166-178`).
- inputs_outputs_state: Inputs are `--task`, `--paths`, and `--bitlesson-file` (`scripts/bitlesson-select.sh:35-48`). Output is exactly `LESSON_IDS: <ids|NONE>` and `RATIONALE: <sentence>` on stdout (`scripts/bitlesson-select.sh:40-42`, `266-267`). It reads config from project/user/default layers through `load_merged_config`, and detects project root from the BitLesson file directory for Codex `-C` (`scripts/bitlesson-select.sh:20-27`, `137-142`).
- gates_or_invariants: Empty task/paths or missing BitLesson file exit nonzero (`scripts/bitlesson-select.sh:76-97`). Whitespace-only BitLesson files are hard errors (`scripts/bitlesson-select.sh:99-103`). Selector output is parsed and rejected unless both stable lines exist (`scripts/bitlesson-select.sh:242-264`). Codex helper is run with low effort, read-only sandbox, project root, stdin prompt, and optional `--disable codex_hooks`, `--skip-git-repo-check`, `--ephemeral` when supported (`scripts/bitlesson-select.sh:192-211`).
- dependencies_and_callers: Depends on `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `hooks/lib/project-root.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh` (`scripts/bitlesson-select.sh:9-33`). Installed as a `bitlesson-selector` shim by `scripts/install-skill.sh` and referenced in RLCR prompts/hook-generated next-round instructions. Provider routing uses `detect_provider` and `check_provider_dependency`; Claude models can fall back to Codex if Claude is missing (`scripts/bitlesson-select.sh:115-131`).
- edge_cases_or_failure_modes: `provider_mode: codex-only` converts a Claude provider to the Codex fallback model (`scripts/bitlesson-select.sh:117-120`). Unsupported providers produce an explicit error (`scripts/bitlesson-select.sh:220-221`). Timeout exits `124`; other model-runner failures print captured raw output and preserve exit code (`scripts/bitlesson-select.sh:224-236`). A placeholder BitLesson file avoids model invocation and returns `NONE`, which prevents unnecessary external calls (`scripts/bitlesson-select.sh:105-109`).
- validation_or_tests: `tests/test-bitlesson-select-routing.sh` validates Codex routing for `gpt-*`, Claude routing for `haiku`/`sonnet`/`opus`, unknown-model failure, missing-binary behavior, Claude-to-Codex fallback, codex-only routing, placeholder short-circuit, and Codex helper flags/no full-auto (`tests/test-bitlesson-select-routing.sh:142-190`, `210-260`, `290-345`, `355-435`, `445-490`).
- skip_candidate: `no`

### HZ-082 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase and adjacent review/drift state machine. It is a test file, but it directly encodes core algorithm behavior and failure gates for `loop-codex-stop-hook.sh` and validators.
- algorithmic_behavior: The test creates an isolated git repo, mock Codex binaries, loop state, plan backup, goal tracker, and round contract (`tests/test-finalize-phase.sh:40-80`, `172-268`). It verifies active-loop detection, transition into Finalize on implementation `COMPLETE`, Finalize completion to `complete-state.md`, max-iteration bypass to `maxiter-state.md`, and normal non-complete review continuation (`tests/test-finalize-phase.sh:327-376`, `541-627`, `818-887`).
- inputs_outputs_state: Inputs are synthetic hook JSON payloads, mock transcript JSONL, mock Codex output, and state files with frontmatter fields including `current_round`, `max_iterations`, `review_started`, `base_branch`, `mainline_stall_count`, `last_mainline_verdict`, and `drift_status` (`tests/test-finalize-phase.sh:218-235`, `499`, `787-793`). Expected outputs are hook block/allow decisions, state renames (`state.md`, `finalize-state.md`, `complete-state.md`, `maxiter-state.md`, `stop-state.md`), review result files, review logs, and next prompt files.
- gates_or_invariants: Finalize Phase requires `finalize-summary.md`, clean git state, completed todos, and must not invoke Codex (`tests/test-finalize-phase.sh:494-568`, `770-812`). It protects `finalize-state.md` against Write/Edit/Bash modification and source-side `mv`/`cp`, while allowing `finalize-summary.md` (`tests/test-finalize-phase.sh:386-480`). During Finalize, round contracts are blocked for write/edit/read because no active round contract remains (`tests/test-finalize-phase.sh:410-444`, `1078-1088`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and invokes `hooks/loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-read-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh` (`tests/test-finalize-phase.sh:48-49`, `389-449`, `501-548`, `1053-1095`). The implementation under test includes `resolve_active_state_file`, `find_active_loop`, `parse_state_file`, finalize message helpers, and Codex review handling.
- edge_cases_or_failure_modes: Review failure or empty review stdout blocks instead of entering Finalize, preserves `state.md`, and sets `review_started: true` (`tests/test-finalize-phase.sh:630-688`, `690-767`). Missing `Mainline Progress Verdict` blocks and preserves prior drift state (`tests/test-finalize-phase.sh:948-994`). Two stalled rounds trigger drift recovery prompt; three stalled/regressed rounds trip a circuit breaker and write `stop-state.md` (`tests/test-finalize-phase.sh:890-947`, `996-1039`).
- validation_or_tests: This file is itself the validation harness and exits with `TESTS_FAILED` (`tests/test-finalize-phase.sh:1104-1113`). It maps to hook behavior where Finalize detection uses `finalize-state.md`, Finalize summary replaces round summary, max-iteration checks skip Finalize, Finalize completion skips Codex and renames to `complete-state.md`, and successful code review enters Finalize (`hooks/loop-codex-stop-hook.sh:85-99`, `784-822`, `956-990`, `1288-1316`, `1321-1385`).
- skip_candidate: `no`

### HZ-112 `file` `prompt-template/block/bitlesson-delta-empty-kb.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the BitLesson delta gate when the knowledge base has no concrete lessons and the round reports `Action: none`.
- algorithmic_behavior: The template tells the agent that `Action: none` is disallowed because `.humanize/bitlesson.md` still lacks concrete lesson entries, and instructs adding/updating a reusable lesson when prior-round issues were resolved (`prompt-template/block/bitlesson-delta-empty-kb.md:1-5`).
- inputs_outputs_state: Inputs are supplied by `scripts/bitlesson-validate-delta.sh` through `load_and_render_safe`, with fallback variables for current round and BitLesson path. Output is a human-readable block reason consumed by the stop hook’s blocking JSON.
- gates_or_invariants: The gate is triggered only after `## BitLesson Delta` parsing finds exactly one action and that action is `none`; if the concrete lesson count is zero and empty-none is not allowed, this template blocks exit (`scripts/bitlesson-validate-delta.sh:202-217`, `227-267`). The invariant is that non-zero learning rounds cannot keep an empty KB while claiming no BitLesson change.
- dependencies_and_callers: Direct caller is `scripts/bitlesson-validate-delta.sh`, which is part of the RLCR stop-gate path for validating summaries and BitLesson discipline. Related templates handle invalid, inconsistent, missing, and missing-notes BitLesson deltas.
- edge_cases_or_failure_modes: If the BitLesson file has placeholders only, concrete lesson count remains zero because placeholder IDs like `<BL-YYYYMMDD-short-name>` are ignored (`scripts/bitlesson-validate-delta.sh:227-239`). The gate can be bypassed only by `BITLESSON_ALLOW_EMPTY_NONE=true`; otherwise it blocks `Action: none` and forces `Action: add` or `Action: update`.
- validation_or_tests: Covered by BitLesson delta validation tests and by the script branch that renders `block/bitlesson-delta-empty-kb.md` (`scripts/bitlesson-validate-delta.sh:254-267`). `rg` also shows this template is the specific render target for the empty-KB condition.
- skip_candidate: `no`

### HZ-142 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template for attempts to modify `state.md`, the main RLCR loop state file.
- algorithmic_behavior: It communicates that `state.md` is loop-managed and that manual modification would corrupt state; if the work is done, the agent should stop so review is triggered automatically (`prompt-template/block/state-file-modification.md:1-3`).
- inputs_outputs_state: Input is a validator-detected state-file write/edit/bash operation. Output is stderr/block reason text through `state_file_blocked_message` (`hooks/lib/loop-common.sh:859-866`).
- gates_or_invariants: The invariant is that the loop system, not the implementing agent, owns `state.md`. Write validator checks state paths after resolving the active loop and strict-parsing the state file, then blocks `state.md`, `finalize-state.md`, and methodology-analysis state writes, with the specific `state.md` branch loading this template (`hooks/loop-write-validator.sh:183-215`). Bash validator similarly blocks commands that modify or move/copy from state files (`hooks/loop-bash-validator.sh:257-300`).
- dependencies_and_callers: Called from `hooks/lib/loop-common.sh` via `load_and_render_safe`; used by write/edit/bash validators. `tests/test-template-references.sh` includes this template in the common template existence set (`tests/test-template-references.sh:149-166`).
- edge_cases_or_failure_modes: `is_state_file_path` matches any path ending in `state.md`, so validators must check more specific `finalize-state.md` and methodology-analysis paths first; the write validator documents and implements this ordering (`hooks/loop-write-validator.sh:197-215`). Authorized cancel paths are the narrow exception in bash validation, not normal worker state edits.
- validation_or_tests: Finalize tests cover the related state-protection family, including function existence and blocking for `finalize-state.md` (`tests/test-finalize-phase.sh:316-320`, `398-480`). State-file robustness and template-reference tests broaden coverage for regular `state.md`.
- skip_candidate: `no`

### HZ-172 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex implementation-review prompt template for normal RLCR rounds. It defines the review contract whose output drives continuation, completion, goal alignment, and drift-state transitions.
- algorithmic_behavior: The prompt requires Codex to read the original plan first, compare Claude’s summary against the plan and prompt, identify gaps, reject deferrals as incomplete, and produce actionable implementation plans for unfinished work (`prompt-template/codex/regular-review.md:3-33`). It mandates a Goal Alignment Check against `goal-tracker.md` and a compact AC/forgotten/deferral summary (`prompt-template/codex/regular-review.md:35-48`).
- inputs_outputs_state: Render inputs include current round, plan file, prompt file, summary content, goal tracker, docs path, commit history, goal-tracker update section, and review result file; `loop-codex-stop-hook.sh` writes the rendered prompt to `round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:1080-1124`). Expected output is either review comments written to the review result file, or `COMPLETE` as the final line only if all original tasks/ACs are done with no deferrals or pending work (`prompt-template/codex/regular-review.md:67-76`).
- gates_or_invariants: The prompt requires finding classification into Mainline Gaps, Blocking Side Issues, and Queued Side Issues, plus a mandatory `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` line (`prompt-template/codex/regular-review.md:49-63`). The stop hook blocks if this verdict is missing for non-`COMPLETE` implementation reviews (`hooks/loop-codex-stop-hook.sh:1460-1490`, `1815-1829`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, choosing this template when the round is not a full alignment check (`hooks/loop-codex-stop-hook.sh:1093-1124`). It depends on `prompt-template/codex/goal-tracker-update-section.md` and `commit-history-section.md` content being injected into placeholders.
- edge_cases_or_failure_modes: If Claude defers work, the prompt explicitly forbids accepting the deferral and instructs Codex to force completion now (`prompt-template/codex/regular-review.md:26-33`). If Claude mainly works on side issues while failing the mainline, Codex must say so (`prompt-template/codex/regular-review.md:63`). A final `COMPLETE` is valid only under the strict all-done condition; deferred or unfinished tasks prevent completion (`prompt-template/codex/regular-review.md:72-76`).
- validation_or_tests: Finalize-phase tests verify the verdict line’s downstream effect: missing verdict blocks and preserves state, stalled/regressed verdicts increment drift counters, two stalls generate replan, three trigger stop (`tests/test-finalize-phase.sh:890-994`, `996-1039`). The hook implements extraction and normalization of these verdicts (`hooks/lib/loop-common.sh:602-660`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 6/6 item evidence headings present once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`