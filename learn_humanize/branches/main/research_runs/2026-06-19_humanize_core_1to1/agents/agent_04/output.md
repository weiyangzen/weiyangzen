# agent_04 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-004 `directory` `config`
- cursor: `[_]`
- core_role: Repository-default configuration surface. Recursive inspection found exactly `config/default_config.json` and `config/codex-hooks.json`; no deeper children.
- algorithmic_behavior: `default_config.json:2-7` seeds RLCR/refine/gen defaults: `codex_model=gpt-5.5`, `codex_effort=high`, `bitlesson_model=haiku`, `agent_teams=false`, no alternative plan language, and `gen_plan_mode=discussion`. `codex-hooks.json:1-17` defines the native Codex `Stop` hook that dispatches to `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` with a 7200 second timeout and status message.
- inputs_outputs_state: Inputs are the runtime root placeholder, optional user config, optional project config, and Codex hook install target. Outputs are merged config values consumed by loop scripts and installed Codex hook JSON. `scripts/lib/config-loader.sh:63-137` merges default, user, and project layers; `scripts/install-codex-hooks.sh:85-172` substitutes and merges the hook template into `hooks.json`.
- gates_or_invariants: Default config is required and malformed required JSON is fatal (`scripts/lib/config-loader.sh:40-56`, `77-111`). Optional malformed user/project JSON is warned and ignored (`scripts/lib/config-loader.sh:52-59`). Config merging strips nulls before deep merge (`scripts/lib/config-loader.sh:113-132`). Hook install requires Codex hook support and validates existing hook object shape (`scripts/install-codex-hooks.sh:75-83`, `120-136`).
- dependencies_and_callers: `hooks/lib/loop-common.sh:204-235` reads merged config for BitLesson, Codex model/effort, and `agent_teams`; invalid model/effort values are ignored with warnings (`loop-common.sh:211-230`). `commands/refine-plan.md:90-116` and `commands/gen-plan.md` depend on the same merge semantics. `scripts/install-skill.sh:269-310` uses `default_config.json` to seed Codex-friendly user config.
- edge_cases_or_failure_modes: Missing `jq` blocks config loading (`config-loader.sh:17-21`). Unsupported `codex_model` prefixes and invalid effort values fall back to caller preset or hardcoded defaults (`loop-common.sh:211-230`). Runtime roots with spaces/metacharacters are JSON-escaped and shell-quoted during hook install (`install-codex-hooks.sh:105-119`). Missing hook template is fatal (`install-codex-hooks.sh:71`).
- validation_or_tests: Covered indirectly by `tests/test-config-merge.sh`, `tests/test-config-error-handling.sh`, `tests/test-codex-hook-install.sh`, and hook install tests referenced by repository file list; direct behavior is implemented in `config-loader.sh` and `install-codex-hooks.sh`.
- skip_candidate: `no`

### HZ-034 `file` `commands/refine-plan.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for refining annotated implementation plans into a comment-free `gen-plan` plan plus a QA ledger. It is planning-only and explicitly forbids repository implementation changes (`commands/refine-plan.md:19-28`).
- algorithmic_behavior: Defines a strict sequential pipeline: parse mode/paths, load config, validate IO, extract comments, classify comments, process them, refine the plan, generate QA, and atomically write outputs (`commands/refine-plan.md:30-43`). The comment scanner must be stateful, tracking code fences, HTML comments, active comment blocks, and nearest heading (`commands/refine-plan.md:184-229`). Classification produces exactly one primary type per raw block, with deterministic sub-items for mixed intent (`commands/refine-plan.md:276-329`).
- inputs_outputs_state: Inputs are `--input`, optional `--output`, optional `--qa-dir`, optional `--alt-language`, mutually exclusive `--discussion`/`--direct`, merged config, the annotated plan, and the QA template (`commands/refine-plan.md:46-83`, `86-153`, `457-505`). Outputs are the refined plan, QA document, optional translated variants, and a final report (`commands/refine-plan.md:508-585`). State transitions include comment dispositions (`answered`, `applied`, `researched`, `deferred`, `resolved`) and convergence status (`converged` or `partially_converged`) (`commands/refine-plan.md:378-391`).
- gates_or_invariants: Must preserve required `gen-plan` sections and optional known sections (`commands/refine-plan.md:399-418`), remove all comment markers, keep AC/task references valid, and restrict task routing tags to `coding` or `analyze` (`commands/refine-plan.md:442-453`). `--alt-language` must not be passed to the validator (`commands/refine-plan.md:72-77`). Atomic writes prepare temp files first and replace the primary in-place plan last (`commands/refine-plan.md:546-566`).
- dependencies_and_callers: Calls `${CLAUDE_PLUGIN_ROOT}/scripts/validate-refine-plan-io.sh` (`commands/refine-plan.md:157-175`), follows `load_merged_config` semantics from `scripts/lib/config-loader.sh` (`commands/refine-plan.md:86-99`), and populates `prompt-template/plan/refine-plan-qa-template.md` (`commands/refine-plan.md:457-459`). The validator implements comment scanning and section scanning in `scripts/validate-refine-plan-io.sh:16-478`, with final IO gates at `560-692`.
- edge_cases_or_failure_modes: Fatal parse cases are nested starts, stray/wrong end markers, and EOF inside a block; errors must include line, column, nearest heading, and context (`commands/refine-plan.md:244-264`). Empty blocks are removed but do not consume IDs (`commands/refine-plan.md:227-228`). Ambiguous classification asks the user in discussion mode or records a safe assumption in direct mode (`commands/refine-plan.md:306-312`, `598-607`). Unsupported CLI language stops; unsupported config language only warns and disables variants (`commands/refine-plan.md:140-147`).
- validation_or_tests: Validator exit codes are specified in `commands/refine-plan.md:165-175` and implemented in `scripts/validate-refine-plan-io.sh:573-692`: input missing, empty, invalid/no comments, missing sections, output/input dir not writable, QA dir not writable, invalid args. QA structure is specified by `prompt-template/plan/refine-plan-qa-template.md:1-119`. Repository tests include `tests/test-refine-plan.sh` for malformed comments, section detection, directory writability, and valid in-place/new-file modes.
- skip_candidate: `no`

### HZ-064 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor-loop graceful shutdown when `.humanize/` disappears during an active RLCR monitor session.
- algorithmic_behavior: Provides three modes: `setup` creates a fake RLCR session under `.humanize/rlcr/2026-01-16_99-99-99`, `delete` removes `.humanize`, and `cleanup` removes leftovers (`tests/manual-monitor-test.sh:23-62`). The prescribed observer command is `source scripts/humanize.sh && humanize monitor rlcr` in a second terminal (`tests/manual-monitor-test.sh:9-14`, `43-45`).
- inputs_outputs_state: Input is one positional command: `setup`, `delete`, or `cleanup`. `setup` writes `state.md` with `current_round`, `max_iterations`, model/effort, timestamp, and plan file, plus a minimal `goal-tracker.md` (`tests/manual-monitor-test.sh:26-38`). `delete` and `cleanup` remove `.humanize` (`tests/manual-monitor-test.sh:47-61`). Expected monitor output is a clean stop message and restored terminal state (`tests/manual-monitor-test.sh:51-56`).
- gates_or_invariants: Uses `set -euo pipefail` and runs from project root (`tests/manual-monitor-test.sh:17-21`). The monitor must exit without shell glob errors and must restore scroll region/cursor after the RLCR directory is deleted (`tests/manual-monitor-test.sh:51-54`).
- dependencies_and_callers: Depends on the monitor command exposed by `scripts/humanize.sh` and monitor runtime libraries under `scripts/lib/monitor-*`. It models `.humanize/rlcr/<timestamp>/state.md` and `goal-tracker.md` as the monitor’s core state inputs.
- edge_cases_or_failure_modes: This is manual and intentionally destructive to `.humanize`, so it should not be run in a real active loop unless that is the test target. It has no automated assertions; the human validates terminal restoration and absence of `zsh/bash` “no matches found” errors (`tests/manual-monitor-test.sh:51-56`). Unknown commands print usage and exit nonzero (`tests/manual-monitor-test.sh:63-70`).
- validation_or_tests: The file itself is a manual validation protocol. Related automated coverage exists in monitor tests such as `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-monitor-e2e-deletion.sh`, and `tests/test-zsh-monitor-safety.sh`.
- skip_candidate: `no`

### HZ-094 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable specification for session-aware RLCR loop ownership, hook isolation, cancellation behavior, and stale/zombie-loop prevention.
- algorithmic_behavior: Creates temporary git repos and loop directories, then verifies setup creates `session_id:` in `state.md` and `.humanize/.pending-session-id` (`tests/test-session-id.sh:31-107`); PostToolUse captures `session_id` into state and consumes the signal (`tests/test-session-id.sh:109-165`); `find_active_loop` filters by session and active/terminal state (`tests/test-session-id.sh:202-346`, `576-784`); cancel ignores session filtering by design (`tests/test-session-id.sh:348-437`); command-signature tests prevent false signal consumption (`tests/test-session-id.sh:786-1053`).
- inputs_outputs_state: Inputs include fake plans, mocked hook JSON with `tool_name`, `tool_input.command`, and `session_id`, synthetic `state.md`/`finalize-state.md`/`complete-state.md`/`cancel-state.md`, and a mock setup path (`tests/test-session-id.sh:27-29`, `118-146`, `208-218`). Outputs are pass/fail assertions, mutated `session_id` fields, consumed or preserved signal files, `cancel-state.md`, and active-loop lookup results.
- gates_or_invariants: Empty stored `session_id` matches any filter for backward compatibility (`tests/test-session-id.sh:238-261`), no filter checks only the newest directory to avoid stale revival (`tests/test-session-id.sh:264-286`, `677-684`), newest terminal state blocks older loops for the same session (`tests/test-session-id.sh:633-684`, `747-784`), and different sessions can coexist (`tests/test-session-id.sh:686-745`). PostToolUse must reject non-setup commands, echo/path substrings, basename-only strings, and quoted-prefix concatenation while accepting quoted/unquoted space or tab-delimited setup invocations (`tests/test-session-id.sh:463-529`, `825-1053`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` (`tests/test-session-id.sh:15-20`). It exercises `scripts/setup-rlcr-loop.sh`, `hooks/loop-post-bash-hook.sh`, and `scripts/cancel-rlcr-loop.sh` (`tests/test-session-id.sh:55-57`, `143-146`, `368-370`). Implementation references: setup writes empty `session_id` and two-line signal (`scripts/setup-rlcr-loop.sh:880-918`); PostToolUse boundary-matches setup command, safely patches `session_id`, and removes signal (`hooks/loop-post-bash-hook.sh:1-140`); active-loop lookup semantics live in `hooks/lib/loop-common.sh:308-425`.
- edge_cases_or_failure_modes: Tests special characters including `/` and `&` in `session_id`, requiring safe `awk` replacement rather than fragile `sed` (`tests/test-session-id.sh:530-574`, `hooks/loop-post-bash-hook.sh:124-134`). It checks command race/false-positive cases where an unrelated Bash command could otherwise steal a pending session signal (`tests/test-session-id.sh:463-529`, `825-925`). Cancel has no session ID input and deliberately cancels the newest active project loop, not the caller’s session (`scripts/cancel-rlcr-loop.sh:80-91`).
- validation_or_tests: This is a full automated shell test suite ending with `print_test_summary` (`tests/test-session-id.sh:1055-1059`). It is not safe to run in this research-only checkout because it creates/removes test repos and `.humanize` state, but its assertions define expected behavior precisely.
- skip_candidate: `no`

### HZ-124 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook blocking template for the RLCR git-clean gate. It tells the agent why exit is blocked and what must be committed before the loop may proceed.
- algorithmic_behavior: Renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then mandates review of untracked files, specific-path staging, and a descriptive commit (`prompt-template/block/git-not-clean.md:1-10`). It also conditionally asks the agent to use `code-simplifier` before commit if installed (`prompt-template/block/git-not-clean.md:5-6`).
- inputs_outputs_state: Inputs are template variables from the stop hook. Output is the Markdown `reason` field inside a hook JSON block decision. `hooks/loop-codex-stop-hook.sh:642-653` uses it after methodology analysis; `hooks/loop-codex-stop-hook.sh:725-748` uses it for normal uncommitted changes.
- gates_or_invariants: Forbids broad staging commands: `git add -A`, `git add --all`, and `git add .` (`prompt-template/block/git-not-clean.md:11-13`). Forbids staging `.humanize/` or legacy `.humanize-*` loop artifacts (`prompt-template/block/git-not-clean.md:13`). Requires project commit conventions and forbids AI authorship/co-author trailers (`prompt-template/block/git-not-clean.md:14-16`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`, with fallbacks if the template cannot be loaded (`hooks/loop-codex-stop-hook.sh:634-644`, `730-738`). Special notes may be assembled from sibling templates such as `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md` (`hooks/loop-codex-stop-hook.sh:702-721`).
- edge_cases_or_failure_modes: `.humanize/` untracked local state is excluded from dirty-block detection, but tracked Humanize state is a separate hard block (`hooks/loop-codex-stop-hook.sh:677-699`). Other untracked files add special-note guidance (`hooks/loop-codex-stop-hook.sh:714-721`). If this template is missing, inline fallback text still blocks exit, but loses the stricter detailed rules.
- validation_or_tests: Covered by stop-hook/template tests such as `tests/test-stop-gate.sh`, `tests/test-template-loader.sh`, and template reference checks. Caller behavior is directly visible in `hooks/loop-codex-stop-hook.sh:620-748`.
- skip_candidate: `no`

### HZ-154 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: Prompt transition template for continuing RLCR in Agent Teams mode after a Codex review round.
- algorithmic_behavior: Reasserts the current agent as Team Leader, states the previous team no longer exists, requires review of Codex feedback before spawning members, prevents redoing already-correct work, and requires cold-start context for new members (`prompt-template/claude/agent-teams-continue.md:1-12`).
- inputs_outputs_state: Inputs are prior Codex review feedback, goal tracker, previous summaries, current file state, and remaining review findings. Output is appended next-round prompt guidance for team allocation. State transition is from prior-round team context to a new per-round team with no access to old conversation history (`prompt-template/claude/agent-teams-continue.md:5-10`).
- gates_or_invariants: Must not message or rely on old teammates (`prompt-template/claude/agent-teams-continue.md:7`). Must inspect current state before assigning file ownership (`prompt-template/claude/agent-teams-continue.md:12`). Must prioritize critical remaining review issues when work exceeds one round (`prompt-template/claude/agent-teams-continue.md:11`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh:2183-2193` appends this template and `agent-teams-core.md` only when `AGENT_TEAMS=true` and not in review phase. `agent_teams` defaults to false in `config/default_config.json:5`, is loaded by `hooks/lib/loop-common.sh:232-235`, and is written into loop state by `scripts/setup-rlcr-loop.sh:896-898`.
- edge_cases_or_failure_modes: If either continuation or core template is missing, the stop hook writes a shorter fallback (`hooks/loop-codex-stop-hook.sh:2193-2201`). That fallback preserves delegation guidance but lacks the stricter cold-start, no-redo, and state-verification details from this file.
- validation_or_tests: Agent team behavior is covered by `tests/test-agent-teams.sh` and stop-hook prompt generation paths. Direct caller path is `hooks/loop-codex-stop-hook.sh:2176-2203`.
- skip_candidate: `no`

### HZ-184 `file` `skills/humanize-rlcr/SKILL.md`
- cursor: `[_]`
- core_role: Codex flow entrypoint for RLCR, delegating all state transitions to setup scripts and native Stop hooks.
- algorithmic_behavior: Defines required sequence: run `setup-rlcr-loop.sh`, read generated round/finalize prompts, implement, commit, write the required summary, stop/exit, and let the native Stop hook drive the next transition (`skills/humanize-rlcr/SKILL.md:24-49`). The skill is non-invocation flow metadata with a hydrated runtime root placeholder (`skills/humanize-rlcr/SKILL.md:1-23`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` and options passed to setup, including plan path, max iterations, model/effort, base branch, review cadence, push policy, agent teams, yolo/quiz behavior, and skip-implementation mode (`skills/humanize-rlcr/SKILL.md:76-97`). Outputs are `.humanize/rlcr/<timestamp>/round-<N>-prompt.md`, review/finalize prompts, summaries, commits, and hook-managed state files (`skills/humanize-rlcr/SKILL.md:38-48`).
- gates_or_invariants: The Stop hook enforces schema/state validation, branch consistency, plan integrity, incomplete Task/Todo blocking, git clean, push-every-round, summary presence, max iteration handling, full-alignment rounds, strict `COMPLETE`/`STOP` markers, review-phase marker guard, P0-9 review gating, review failure/empty-output blocking, and open-question handling (`skills/humanize-rlcr/SKILL.md:50-68`).
- dependencies_and_callers: Calls `{{HUMANIZE_RUNTIME_ROOT}}/scripts/setup-rlcr-loop.sh` and `cancel-rlcr-loop.sh` (`skills/humanize-rlcr/SKILL.md:28-34`, `109-113`). Depends on native Codex hooks from `config/codex-hooks.json`, state/schema helpers in `hooks/lib/loop-common.sh`, and the Codex Stop hook `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: Setup nonzero means stop and report (`skills/humanize-rlcr/SKILL.md:34`). Critical rules forbid manual edits to `state.md`/`finalize-state.md`, manual completion bypass after hook blocks, ad-hoc `codex exec`/`codex review` in place of hook-managed transitions, and using anything other than generated loop files as source of truth (`skills/humanize-rlcr/SKILL.md:69-75`).
- validation_or_tests: Installation coverage includes `tests/test-codex-hook-install.sh`, which checks the skill exists for Codex installs. Runtime behavior is covered by stop-hook, session-id, monitor, finalize, and hook validator tests throughout `tests/`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 evidence sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`