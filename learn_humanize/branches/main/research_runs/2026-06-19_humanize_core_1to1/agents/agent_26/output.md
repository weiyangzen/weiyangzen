# agent_26 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-026 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Regression and robustness specification suite for the RLCR/humanize core shell algorithms. The directory has no nested subdirectories; recursive inspection found 16 shell tests under `tests/robustness/*.sh`. Parent runner `tests/run-all-tests.sh:103-118` includes every file in this directory as part of the full test sweep.
- algorithmic_behavior: The directory exercises production runtime behavior rather than implementing one runtime path itself. Child roles are one-to-one with core surfaces: base branch auto-detection from setup logic in `test-base-branch-detection.sh:5-11`; cancel authorization and command/path bypass rejection in `test-cancel-security-robustness.sh:5-10`; concurrent state parsing and active loop discovery in `test-concurrent-state-robustness.sh:5-10`; git status/state parsing in `test-git-operations-robustness.sh:5-10`; goal tracker metric extraction in `test-goal-tracker-robustness.sh:5-10`; hook JSON/monitor behavior in `test-hook-input-robustness.sh:5-9`; edit/plan/stop hook protection in `test-hook-system-robustness.sh:5-14`; setup plan path validation in `test-path-validation-robustness.sh:5-7`; production plan content validation in `test-plan-file-robustness.sh:5-10`; session discovery in `test-session-robustness.sh:5-9`; setup script argument/git/symlink/skip-impl/dependency behavior in `test-setup-scripts-robustness.sh:5-10`; tolerant and strict state parsing in `test-state-file-robustness.sh:5-9`; state/finalize/cancel transition behavior in `test-state-transition-robustness.sh:5-9`; template error handling in `test-template-error-robustness.sh:5-10`; template stress rendering in `test-template-stress-robustness.sh:5-9`; timeout fallback and exit semantics in `test-timeout-robustness.sh:5-9`.
- inputs_outputs_state: Inputs are synthetic project roots, temp git repos, hook JSON payloads, mocked `codex`/runtime binaries, plan files, goal trackers, state files, loop directories, env vars such as `CLAUDE_PROJECT_DIR`, `PATH`, `XDG_CACHE_HOME`, and command-line flags passed to `scripts/setup-rlcr-loop.sh`. Outputs are PASS/FAIL counters via `tests/test-helpers.sh:30-77`, process exit status from each final `print_test_summary`, hook JSON block/allow decisions, and generated temp loop artifacts. The tests simulate state transitions such as `state.md` to `finalize-state.md` or `cancel-state.md`, dirty/clean git transitions, setup-created `.humanize/rlcr/<timestamp>` sessions, and hook allow/block decisions.
- gates_or_invariants: The suite encodes fail-closed invariants for malformed hook JSON and malformed state; state/finalize files must not be written or edited by normal tools; only the newest active session counts; completed/cancelled sessions are ignored; plan paths must be relative, non-symlinked, and free of shell metacharacters; setup must reject dirty git states and missing dependencies; timeout should return `124` on real timeouts; template rendering must not re-expand injected placeholders; goal tracker counts must tolerate malformed, binary, and mixed markdown. Concrete sections: path gate tests in `test-path-validation-robustness.sh:176-486`, setup dependency gates in `test-setup-scripts-robustness.sh:1056-1191`, hook block gates in `test-hook-system-robustness.sh:313-838`, strict state schema gates in `test-state-file-robustness.sh:146-529`.
- dependencies_and_callers: Tests source `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, and `scripts/portable-timeout.sh`, and invoke production hooks under `hooks/loop-*-validator.sh` plus `scripts/setup-rlcr-loop.sh`. The production surfaces under test are discoverable at `hooks/lib/loop-common.sh:332` (`find_active_loop`), `hooks/lib/loop-common.sh:430` (`get_current_round`), `hooks/lib/loop-common.sh:494`/`:533` (`parse_state_file`/strict), `hooks/lib/loop-common.sh:1051` (`is_cancel_authorized`), `hooks/lib/template-loader.sh:56`/`:140`/`:188`, and `scripts/humanize.sh:38`, `:71`, `:158`, `:208`.
- edge_cases_or_failure_modes: The directory deliberately probes command injection (`test-cancel-security-robustness.sh:109-317`), symlink bypass (`test-cancel-security-robustness.sh:332-403`, `test-path-validation-robustness.sh:339-374`), deep or binary JSON (`test-hook-input-robustness.sh:177-239`), concurrent reads and atomic writes (`test-concurrent-state-robustness.sh:247-329`), binary/CRLF/missing state (`test-state-file-robustness.sh:367-398`), huge templates and values (`test-template-stress-robustness.sh:79-157`), null bytes and disappearing plan files (`test-plan-file-robustness.sh:514-557`), detached/rebase/merge/shallow git states (`test-git-operations-robustness.sh:374-506`), and no-timeout fallback (`test-timeout-robustness.sh:235-278`).
- validation_or_tests: This directory is itself validation. I did not execute the tests during this research pass because the checkout is read-only and git already emitted sandbox cache warnings while reading repository state; evidence is based on direct source inspection. Each script ends in `print_test_summary`, for example `test-timeout-robustness.sh:278`, `test-setup-scripts-robustness.sh:1191`, and `test-hook-system-robustness.sh:838`.
- skip_candidate: `no`

### HZ-056 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Cross-platform timeout abstraction used by setup, hooks, assistant-wrapper scripts, bitlesson selection, and robustness tests so core workflows do not depend on GNU `timeout` existing on macOS.
- algorithmic_behavior: At source time, `detect_timeout_impl` chooses the first available implementation in priority order: Homebrew `gtimeout`, GNU `timeout` verified by `timeout --version`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:10-27`). `TIMEOUT_IMPL` is assigned once (`:29`). `run_with_timeout <seconds> <command> [args...]` dispatches to the selected implementation, preserving command arguments as a bash array for native timeout paths (`:33-46`) or using Python `subprocess.run(..., timeout=...)` with GNU-compatible timeout exit `124` (`:47-63`). If no implementation exists it warns and runs unbounded (`:64-69`).
- inputs_outputs_state: Inputs are the timeout seconds, command argv, current `PATH`, and availability of timeout/Python binaries. Outputs are the wrapped command stdout/stderr and exit code; Python timeout returns `124`, generic Python exceptions return `1`. The only exported state is `TIMEOUT_IMPL` (`:73-76`), cached when the file is sourced.
- gates_or_invariants: GNU `timeout` is accepted only if `timeout --version` succeeds (`:13-19`), avoiding BSD/non-GNU ambiguity. The wrapper preserves command exit status on success (`:39-45`, `:55`) and makes timeout semantics explicit with exit `124` (`:56-57`). It intentionally degrades to a warning rather than hard failure when no timeout tool exists (`:64-67`).
- dependencies_and_callers: Depends on bash arrays, `command -v`, optional `gtimeout`/GNU `timeout`, and optional Python. Callers include `hooks/loop-plan-file-validator.sh:22`, `hooks/loop-codex-stop-hook.sh:51`, `scripts/setup-rlcr-loop.sh:27`, `scripts/ask-codex.sh:29`, `scripts/ask-gemini.sh:30`, `scripts/bitlesson-select.sh:29`, and robustness tests such as `tests/robustness/test-timeout-robustness.sh:16`.
- edge_cases_or_failure_modes: `TIMEOUT_IMPL` is not recomputed if `PATH` changes after sourcing. Non-numeric timeout values fall through to the selected implementation and may fail there. `none` mode can hang because it deliberately runs without a watchdog. Python mode uses `$timeout_secs` inside an inline Python program, so invalid shell input can become invalid Python code; normal callers pass numeric values. Empty command and nonexistent command behavior is delegated to the selected implementation.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` covers detection (`:31-47`), quick success (`:51-59`), timeout exit `124` (`:62-76`), argument preservation (`:79-87`), exit-code preservation (`:89-99`), pipelines via `sh -c` (`:102-110`), zero/short timeouts (`:120-154`), large output and rapid cycles (`:157-185`), nonexistent/empty command (`:188-209`), special characters and signal handling (`:211-232`), fallback detection (`:235-245`), subshells and export behavior (`:249-271`).
- skip_candidate: `no`

### HZ-086 `file` `tests/test-model-router.sh`
- cursor: `[_]`
- core_role: Executable specification for model-provider routing, CLI dependency detection, and reasoning-effort normalization in `scripts/lib/model-router.sh`.
- algorithmic_behavior: The test sources shared helpers and the router implementation (`tests/test-model-router.sh:5-8`), creates temporary mock provider binaries (`:17-27`), then validates three public functions. It asserts Codex routing for `gpt-*` and `oN*` models (`:29-111`), Claude routing for `haiku`, `sonnet`, `opus`, and `claude-*` names including mixed case (`:113-196`), hard failure for unknown and empty model names (`:198-230`), provider binary checks for `codex` and `claude` using controlled `PATH` (`:232-298`), and effort mapping/pass-through/failure behavior (`:300-420`). The underlying router patterns are in `scripts/lib/model-router.sh:10-30`, dependency checks in `:32-60`, and effort mapping in `:62-91`.
- inputs_outputs_state: Inputs are model strings, provider names, effort strings, and scoped `PATH` values. Outputs are provider names (`codex`/`claude`), mapped effort strings, stderr diagnostics, pass/fail counters, and script exit status through `print_test_summary` (`tests/test-model-router.sh:422-426`). Temporary state is isolated via `setup_test_dir` and mock binaries under `$TEST_DIR/bin`.
- gates_or_invariants: Empty model must fail with a non-empty-model error (`:216-230`); unknown model must fail with an error (`:198-213`); missing provider binary must fail and mention the missing provider (`:249-263`, `:283-298`); `xhigh` must be downgraded to `high` only for Claude and must log the mapping (`:300-318`); `xhigh` remains valid for Codex (`:337-352`); unknown efforts fail for both providers (`:388-420`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for counters and temp dirs, `scripts/lib/model-router.sh` for functions under test, and shell `PATH` lookup. The router is a shared abstraction for code that must select Codex vs Claude behavior based on requested model/provider semantics.
- edge_cases_or_failure_modes: The test intentionally uses a safe base `PATH` (`:10`) to avoid accidental real provider binaries. Case-insensitive Claude matching is covered by `claude-3-OPUS-20240229` (`:181-196`). Numbering comments duplicate “Test 10” around dependency tests (`:216`, `:232`), but behavior coverage is unambiguous. The test writes mock binaries if executed; this research pass only inspected it.
- validation_or_tests: This file is the validation surface. It terminates through `print_test_summary "Model Router Test Summary"` at `tests/test-model-router.sh:426`, which returns non-zero on any failed assertion via `tests/test-helpers.sh:58-77`.
- skip_candidate: `no`

### HZ-116 `file` `prompt-template/block/bitlesson-delta-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when a round summary lacks the required `## BitLesson Delta` contract. It converts a validator failure into an actionable correction prompt.
- algorithmic_behavior: The template emits a fixed title and message (`prompt-template/block/bitlesson-delta-missing.md:1-3`) followed by the minimal required markdown schema (`:5-12`): `Action: none|add|update`, lesson IDs or `NONE`, and notes. It has no placeholders; it is a static gate explanation.
- inputs_outputs_state: Inputs are only the template file content and fallback selection by the caller. Output is markdown text rendered into a hook/block reason. It does not mutate state itself; the state transition is external: a stop/validation attempt is blocked until the summary includes a valid BitLesson Delta section.
- gates_or_invariants: The invariant is that every relevant round summary must expose a real `## BitLesson Delta` section with an action, lesson IDs, and notes. The actual detection and block are in `scripts/bitlesson-validate-delta.sh:183-197`, which loads this template at `:196` when the section is missing.
- dependencies_and_callers: Loaded through `load_and_render_safe` from the template loader. Related callers and surfaces include the stop hook’s BitLesson Delta check at `hooks/loop-codex-stop-hook.sh:858`, setup scaffolding that creates a default BitLesson Delta section at `scripts/setup-rlcr-loop.sh:1183`, and user-facing start-loop instructions at `commands/start-rlcr-loop.md:177`.
- edge_cases_or_failure_modes: Because the template has no variables, rendering is insensitive to placeholder escaping and missing inputs. It does not validate invalid actions or inconsistent lesson IDs; sibling templates and validator branches handle those cases (`prompt-template/block/bitlesson-delta-invalid.md`, `bitlesson-delta-inconsistent.md`, and `bitlesson-delta-missing-notes.md`).
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` covers missing, fenced, commented, and valid BitLesson Delta cases, and `tests/test-unified-codex-config.sh:601-606` checks setup-created summaries include the scaffold. Robustness setup tests also assert skip-impl scaffolding includes this section at `tests/robustness/test-setup-scripts-robustness.sh:943-952`.
- skip_candidate: `no`

### HZ-146 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the `--push-every-round` invariant: if the branch is ahead of remote when stopping, the loop tells the agent to push before exit.
- algorithmic_behavior: The template interpolates `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into a stop-block message (`prompt-template/block/unpushed-commits.md:1-5`) and emits the required command `git push origin {{CURRENT_BRANCH}}` (`:7-10`). It closes by telling the user/agent to retry exit after all commits are pushed (`:12`).
- inputs_outputs_state: Inputs are ahead count and current branch from the stop hook. Output is markdown used as a block reason. External state transition is “unpushed commits present” to “remote pushed / no ahead count”; the template itself only explains the gate.
- gates_or_invariants: The gate is active only when `push_every_round` is true and git reports the branch is ahead. `hooks/loop-codex-stop-hook.sh:757-773` extracts `ahead N`, resolves the current branch, renders this template with `AHEAD_COUNT` and `CURRENT_BRANCH`, and returns a JSON block decision. Setup also tells agents about the requirement when `--push-every-round` is enabled at `scripts/setup-rlcr-loop.sh:1437`.
- dependencies_and_callers: Depends on `load_and_render_safe` and the template-loader substitution semantics. It is called by `hooks/loop-codex-stop-hook.sh:767-769`; related user-facing note is `prompt-template/claude/push-every-round-note.md`.
- edge_cases_or_failure_modes: The template assumes the branch name supplied by git is safe to display and place in a shell command. It does not itself verify the push result, remote name, upstream configuration, or branch existence; the stop hook re-checks git ahead status on the next exit attempt. If branch names contain unusual shell metacharacters, the rendered command is advisory text rather than an escaped shell script.
- validation_or_tests: `tests/test-templates-comprehensive.sh:517-527` renders this real template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`. Stop-hook behavior around push blocking is exercised through the stop-hook test family and the direct call site at `hooks/loop-codex-stop-hook.sh:757-773`.
- skip_candidate: `no`

### HZ-176 `file` `scripts/lib/config-loader.sh`
- cursor: `[_]`
- core_role: Shared configuration loader implementing deterministic four-layer JSON config resolution for commands, hooks, and runtime scripts.
- algorithmic_behavior: The file is source-guarded (`scripts/lib/config-loader.sh:2-4`) and requires strict shell mode (`:6`). `_config_loader_require_jq` makes `jq` mandatory (`:17-22`). `_config_loader_prepare_layer` normalizes each config layer to a JSON object, writing `{}` for absent optional layers, warning and ignoring malformed optional layers, and failing for missing/malformed required layers (`:24-61`). `load_merged_config <plugin_root> <project_root>` resolves default config at `$plugin_root/config/default_config.json`, user config via `XDG_CONFIG_HOME` or `$HOME/.config/humanize/config.json`, and project config via `HUMANIZE_CONFIG` or `$project_root/.humanize/config.json` (`:63-89`). It creates a temp dir, prepares four layer files, recursively strips nulls, and merges layers with later layers overriding earlier ones (`:90-136`). `get_config_value` reads a top-level key and returns strings directly, non-strings via `tostring`, and empty output for null/missing keys (`:139-159`).
- inputs_outputs_state: Inputs are `plugin_root`, `project_root`, environment variables `XDG_CONFIG_HOME`, `HOME`, and `HUMANIZE_CONFIG`, and JSON object files. Outputs are merged JSON on stdout, warnings/errors on stderr, and scalar config values from `get_config_value`. State is temporary and local to the subshell: `mktemp -d` plus cleanup trap (`:100-101`); no repository file is modified by the loader.
- gates_or_invariants: Required default config must exist and be a JSON object (`:109` with required true, enforced in `:40-56`); optional user/project configs may be missing or malformed without aborting (`:48-59`, `:110-111`); null values are stripped before merge so higher-level nulls cannot erase lower-level defaults (`:119-131`); missing function arguments are usage errors (`:70-73`, `:143-146`); `jq` absence is fatal (`:17-22`).
- dependencies_and_callers: Depends on `jq`, `mktemp`, and bash. Defaults are defined in `config/default_config.json:1-8`. Callers include command specs `commands/gen-plan.md:64-78` and `commands/refine-plan.md:88-92`, hook common config initialization at `hooks/lib/loop-common.sh:183-234`, and runtime selection in `scripts/bitlesson-select.sh:10-25`.
- edge_cases_or_failure_modes: If `HOME` is unset and `XDG_CONFIG_HOME` is unset, the computed user config path starts at `/.config/humanize/config.json`; because it is optional, absence is harmless. Arrays are recursively stripped of null entries before merge. `get_config_value` only supports top-level keys, not dotted paths. Fatal errors inside `_config_loader_prepare_layer` use `exit 1` in the subshell to avoid bash `set -e` propagation surprises (`:43-46`).
- validation_or_tests: `tests/test-config-merge.sh:1-200` validates default-only behavior, project override, project-over-user priority, additive merge, null stripping, `HUMANIZE_CONFIG`, and all-layer merge. `tests/test-config-error-handling.sh:1-174` validates missing default fatality, malformed optional warnings and fallback, empty project config, and missing optional config behavior. `tests/test-unified-codex-config.sh` also checks unified codex model/effort config behavior against this loader.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6 item sections above; each assigned checklist row has one corresponding Item Evidence section`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`