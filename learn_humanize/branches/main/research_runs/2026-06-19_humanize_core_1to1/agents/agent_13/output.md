# agent_13 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-013 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared RLCR hook library layer. It centralizes state schema, active-loop discovery, path canonicalization, template rendering, background-task parking, cancel authorization, git-state protections, review parsing, and methodology-analysis transitions. The directory contains five files: `loop-common.sh`, `loop-bg-tasks.sh`, `project-root.sh`, `template-loader.sh`, and `methodology-analysis.sh`.
- algorithmic_behavior: `loop-common.sh` defines canonical state fields and enums for normal/review/finalize/methodology/terminal phases at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:24), resolves active state files in priority order at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:268), selects active loop directories with session filtering and zombie-loop protection at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:332), parses state files at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:494), extracts Codex mainline verdicts at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:637), extracts `[P?]` review findings from recent Codex output at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:740), and terminates loops by renaming state files at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:1540). `loop-bg-tasks.sh` parses Claude transcripts for async Agent/Bash task launches and terminal notifications, prunes orphaned tasks by task-output liveness, and runs the stop-hook short-circuit guards at [loop-bg-tasks.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-bg-tasks.sh:311). `methodology-analysis.sh` inserts an optional exit analysis phase by renaming active state to `methodology-analysis-state.md`, then later renames it to the original terminal state after completion artifacts are present at [methodology-analysis.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/methodology-analysis.sh:38) and [methodology-analysis.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/methodology-analysis.sh:114).
- inputs_outputs_state: Inputs include hook JSON, `session_id`, transcript path, `.humanize/rlcr/<timestamp>/*-state.md`, review logs, template files, git status, and environment/config values. Outputs include shell return codes, rendered prompts/block reasons, JSON hook decisions through callers, state-file mutations such as frontmatter upserts, `bg-pending.marker`, terminal state renames, and methodology report/done gating. `project-root.sh` resolves `CLAUDE_PROJECT_DIR` or git top-level and canonicalizes paths at [project-root.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/project-root.sh:41); `template-loader.sh` emits rendered templates or fallbacks via `load_and_render_safe` at [template-loader.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/template-loader.sh:188).
- gates_or_invariants: Active loop selection never scans older unfiltered loops if the newest dir is terminal, preventing stale revival. With session filtering, the newest terminal loop for that session blocks fallback to older active loops; marker-based foreign-loop adoption is opt-in for the stop hook only. Template rendering is single-pass so injected `{{VAR}}` inside variable values is not expanded, implemented at [template-loader.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/template-loader.sh:56). Cancel authorization requires `.cancel-requested`, rejects command substitution, shell chaining, extra args, mixed quote styles, wrong source/destination, and symlink leaf attacks at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:1051). `git_adds_humanize` and `git_has_tracked_humanize_state` prevent local `.humanize/` runtime state from entering commits at [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:1280) and [loop-common.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-common.sh:1428).
- dependencies_and_callers: The stop hook sources `loop-common.sh` and `methodology-analysis.sh` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:44) and [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:54). Read/write/edit/bash/plan validators also source `loop-common.sh`, while `setup-rlcr-loop.sh`, `cancel-rlcr-loop.sh`, `ask-codex.sh`, `bitlesson-select.sh`, statusline, and monitor scripts reuse these helpers. External dependencies include `jq`, `git`, `awk`, `sed`, `grep`, `realpath` or `python3`, `date`, `lsof` for background liveness, and Codex/config-loader paths.
- edge_cases_or_failure_modes: Background task liveness intentionally fails open when task output is absent, `lsof` is unavailable, or lsof errors, avoiding premature Codex review while async work may still be running at [loop-bg-tasks.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-bg-tasks.sh:120). Transcript parsing fails closed for cleanup: stale markers are removed only if pending-task enumeration succeeds and returns empty at [loop-bg-tasks.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/loop-bg-tasks.sh:428). `canonicalize_path` is explicitly unsafe for user authorization because it dereferences leaf symlinks; authorization code must use `canonicalize_path_prefix`, documented at [project-root.sh](/Users/wangweiyang/GitHub/humanize/hooks/lib/project-root.sh:98). State parsing has tolerant and strict variants, so callers choose between migration tolerance and hard schema rejection.
- validation_or_tests: Covered by assigned template and path robustness tests, plus related suites referenced in the tree such as `tests/test-stop-hook-bg-allow.sh` for background-task/session behavior, `tests/test-session-id.sh` for active-loop filtering, `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh` for cancel authorization, `tests/test-codex-review-merge.sh` for review issue extraction, and `tests/test-template-loader.sh` for rendering. I inspected, but did not execute, tests in this research-only read-only pass.
- skip_candidate: `no`

### HZ-043 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Main RLCR stop-hook state machine. It intercepts Claude stop attempts, determines whether the loop should pause, continue, enter code-review, enter finalize, enter methodology analysis, or end in a terminal state. It is the central coordinator for gating implementation progress with Codex, git cleanliness, summaries, BitLesson deltas, round contracts, and anti-drift state.
- algorithmic_behavior: The hook reads JSON from stdin at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:28), resolves project root and active loop at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:46), delegates background-task short-circuiting before all other checks at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:83), parses state and validates critical fields at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:107), enforces branch/plan/todo/git/summary/contract/BitLesson gates, builds Codex prompts, runs `codex exec` for implementation summary review, and runs `codex review` for review-phase acceptance. A summary review ending with `COMPLETE` does not directly complete the loop; it sets `review_started=true`, writes `.review-phase-started`, then requires `codex review` to produce no `[P0-9]` findings before finalize at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1882) and [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1904).
- inputs_outputs_state: Inputs include hook JSON fields such as `session_id` and `transcript_path`, state frontmatter, `round-N-summary.md`, `round-N-contract.md`, `goal-tracker.md`, `.humanize/bitlesson.md`, git branch/status/log output, Codex CLI output, and templates. Outputs are hook JSON decisions with `decision: block` or natural `exit 0`, review prompt/result files, debug files under cache, updated round numbers, updated drift fields, `.review-phase-started`, `finalize-state.md`, terminal `complete-state.md`/`stop-state.md`/`maxiter-state.md`/`unexpected-state.md`, and methodology-analysis artifacts.
- gates_or_invariants: State schema requires `current_round`, `max_iterations`, `review_started`, and `base_branch`; missing legacy fields block or terminate rather than silently defaulting at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:186). Branch must remain equal to `start_branch` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:306). Plan integrity is skipped only in review phase; otherwise the backup and original must exist and match at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:325). Dirty git state blocks exit except untracked local `.humanize/` runtime state; tracked `.humanize/` always blocks at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:677). Round contracts are enforced when anti-drift state exists at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:827). Implementation reviews must include a non-ambiguous `Mainline Progress Verdict` unless they end with `STOP`; missing verdict blocks at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1823).
- dependencies_and_callers: Claude Code calls this as a Stop hook. It depends on `hooks/lib/loop-common.sh`, `hooks/lib/loop-bg-tasks.sh` via common, `hooks/lib/methodology-analysis.sh`, `scripts/portable-timeout.sh`, `hooks/check-todos-from-transcript.py`, `scripts/bitlesson-validate-delta.sh`, prompt templates under `prompt-template`, `jq`, `git`, and the Codex CLI. It also calls helper functions such as `detect_review_issues`, `upsert_state_fields`, `extract_mainline_progress_verdict`, `end_loop`, and `load_and_render_safe`.
- edge_cases_or_failure_modes: No active loop or no state file exits naturally. Background tasks park the loop before expensive gates and emit a stop-allowed message. Git status failures fail closed and attempt stale `index.lock` cleanup. Missing Codex CLI blocks with an install message. Codex non-zero exit, missing review result file, empty review result, or missing review stdout in review phase all block and require retry at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1708) and [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1589). Review phase cannot be entered by manually setting `review_started=true`; `.review-phase-started` must exist at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1922).
- validation_or_tests: Behavior is covered across targeted executable specs: BitLesson delta validation is delegated at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:861) and specified by `tests/test-bitlesson-validate-delta.sh`; template rendering/fallbacks by `tests/test-templates-comprehensive.sh`; background task parking by `tests/test-stop-hook-bg-allow.sh`; review issue parsing by `tests/test-codex-review-merge.sh`; finalize/state transitions by `tests/test-finalize-phase.sh`, `tests/test-state-exit-naming.sh`, and robustness state suites. Not executed in this research pass.
- skip_candidate: `no`

### HZ-073 `file` `tests/test-bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/bitlesson-validate-delta.sh`, which is invoked by the stop hook before Codex review when `bitlesson_required=true`. It verifies that round summaries contain a real, parseable `## BitLesson Delta` section and that declared lesson changes are actionable.
- algorithmic_behavior: The test creates a temporary BitLesson knowledge base with one concrete lesson at [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:21), writes synthetic round summaries with configurable action and notes at [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:36), runs the production validator with `--allow-empty-none false` at [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:51), and asserts either JSON `decision == "block"` or empty output. The production validator extracts only non-comment, non-fenced delta sections at [bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/scripts/bitlesson-validate-delta.sh:92) and checks action/IDs/notes consistency.
- inputs_outputs_state: Inputs are temporary summary files, a temporary `.humanize/bitlesson.md`, the real `prompt-template` directory, current round `1`, and `jq` for JSON assertions. Outputs are test `pass`/`fail` counters via `test-helpers.sh`; the validator returns block JSON on validation failure or no stdout on pass. It does not mutate RLCR state directly.
- gates_or_invariants: `Action: add` and `Action: update` require non-empty `Notes:` that are not placeholder text. A delta section inside a fenced code block or HTML comment must not satisfy the requirement. A real text delta with a concrete lesson ID and explanatory notes passes. These assertions appear at [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:101), [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:111), [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:116), [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:130), and [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:144).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `jq`, `scripts/bitlesson-validate-delta.sh`, and templates. The stop hook is the runtime caller of the validator; the test simulates that command line directly.
- edge_cases_or_failure_modes: The test guards against false positives from quoted template examples in fenced markdown and HTML comments. It also guards against hollow compliance where `Notes:` is whitespace or a placeholder such as `[what changed and why]` or `<what changed and why>`. It does not cover malformed IDs, missing knowledge-base entries, missing BitLesson files, duplicate actions, or `Action: none`; those are implemented in the validator but outside this specific test file.
- validation_or_tests: This file is itself validation. It ends with `print_test_summary "BitLesson Delta Validator Test Summary"` at [test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize/tests/test-bitlesson-validate-delta.sh:154). Not executed in this research pass.
- skip_candidate: `no`

### HZ-103 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Comprehensive executable spec for the prompt-template subsystem used by hook block messages, Codex prompts, Claude next-round prompts, finalize prompts, and methodology prompts. It is core support rather than state-machine transition code, because failed or malformed templates directly alter stop-hook gate instructions.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:17), resolves the template directory, verifies expected subdirectories, loads every `*.md` file, validates placeholder syntax, checks malformed placeholder detection regexes, exercises `render_template` with edge cases, tests fallback behavior, renders representative real templates, then stress-renders all templates with dummy placeholder values. The summary exits `0` only when no failures occurred at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:619).
- inputs_outputs_state: Inputs are all files under `prompt-template`, inline synthetic template strings, temporary incomplete template directories, and placeholder assignment arrays. Outputs are colored pass/fail/warn lines and process exit status. No RLCR state files are modified; temporary directories are created and removed for malformed/incomplete directory checks.
- gates_or_invariants: Required subdirectories are `block`, `codex`, and `claude` in this test at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:74). All markdown templates must load and be non-empty or produce warnings. Valid placeholders are uppercase double-brace names like `{{VAR_NAME}}`; extra braces, wrong bracket forms, spaces inside placeholders, and lowercase placeholders are failures at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:122). Rendering must preserve empty lines, substitute adjacent and repeated variables, tolerate regex characters, quotes, markdown, long values, multiline values, and leave no placeholders unreplaced when dummy values are supplied at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:262) and [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:562).
- dependencies_and_callers: Depends directly on `template-loader.sh`, `find`, `grep`, `sed`, `sort`, `mktemp`, `awk` through the renderer, and the current prompt-template tree. Runtime callers include `loop-common.sh` message helpers, the stop hook’s review/finalize/next-round prompt generation, BitLesson validator block messages, setup prompts, and methodology-analysis prompts.
- edge_cases_or_failure_modes: The test intentionally treats unreplaced placeholders during all-template render as warnings rather than failures, which catches visibility issues without breaking on intentionally deferred placeholders. CJK/emoji handling is conservative: the CJK branch uses ASCII sample text in this file, so it validates no-crash behavior rather than true non-ASCII fidelity at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:347). `validate_template_dir` in the library requires `block`, `codex`, `claude`, and `plan`, while this test’s directory-structure section checks only the first three; the later direct `validate_template_dir` call covers the library rule.
- validation_or_tests: This is a validation suite for `template-loader.sh` and all prompt templates. It includes integration checks for real templates such as `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at [test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize/tests/test-templates-comprehensive.sh:492). Not executed in this research pass.
- skip_candidate: `no`

### HZ-133 `file` `prompt-template/block/mainline-drift-stop.md`
- cursor: `[_]`
- core_role: User-facing block template for the anti-drift circuit breaker. It supplies the reason text when the stop hook determines that implementation has failed to advance the mainline for too many consecutive rounds.
- algorithmic_behavior: The template renders a concise stop explanation, interpolating `{{STALL_COUNT}}`, `{{LAST_VERDICT}}`, and `{{PLAN_FILE}}` at [mainline-drift-stop.md](/Users/wangweiyang/GitHub/humanize/prompt-template/block/mainline-drift-stop.md:3). The stop hook loads it inside `stop_for_mainline_drift` at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1441), after upserting `mainline_stall_count`, `last_mainline_verdict`, and `drift_status: replan_required`, then calls `end_loop` with `EXIT_STOP`.
- inputs_outputs_state: Inputs are rendered variables from stop-hook state: consecutive stall/regression count, last normalized verdict, and plan path. Output is markdown inserted into JSON `reason` with system message `"Loop: Stopped - mainline drift circuit breaker triggered"`. State mutation happens in the caller, not the template: `drift_status` becomes `replan_required` and active state is renamed to `stop-state.md`.
- gates_or_invariants: The message states the loop “should not continue automatically” and directs the next action to re-read the plan, diagnose stalling/regression, and start a fresh loop with a narrower recovered objective at [mainline-drift-stop.md](/Users/wangweiyang/GitHub/humanize/prompt-template/block/mainline-drift-stop.md:9). This aligns with the hook invariant that three consecutive stalled/regressed mainline verdicts stop the loop, while two set replan-required for the next prompt.
- dependencies_and_callers: Depends on `template-loader.sh` substitution semantics. Called only by `hooks/loop-codex-stop-hook.sh` in the inspected code path. It is part of the `prompt-template/block` family validated by the comprehensive template test.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook has an inline fallback with the same semantic fields at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1432). Because it is a static template, malformed placeholders would degrade the block reason but not the underlying state transition; template tests are the safety net.
- validation_or_tests: Covered by all-template loading/rendering and placeholder syntax checks in `tests/test-templates-comprehensive.sh`. The transition that uses it is governed by mainline verdict extraction and drift state updates in the stop hook; I did not find a dedicated assigned test file solely for this template.
- skip_candidate: `no`

### HZ-163 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude instruction template for a normal next implementation round after Codex found issues but before drift recovery is required. It defines the algorithmic contract for re-anchoring, writing a round contract, tagging tasks, preserving mainline focus, applying BitLesson selection, and updating the goal tracker.
- algorithmic_behavior: The stop hook increments `current_round`, writes or preserves `round-N-summary.md`, sets `round-N-contract.md`, and renders this template when `DRIFT_REPLAN_REQUIRED` is false at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:1990) and [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:2066). The template first requires reading the original plan, goal tracker, recent summaries/reviews, and writing the round contract before code at [next-round-prompt.md](/Users/wangweiyang/GitHub/humanize/prompt-template/claude/next-round-prompt.md:12). It embeds Codex review content between explicit markers at [next-round-prompt.md](/Users/wangweiyang/GitHub/humanize/prompt-template/claude/next-round-prompt.md:48), then reiterates goal tracker and mainline guardrails.
- inputs_outputs_state: Inputs are `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `BITLESSON_FILE`, `ROUND_CONTRACT_FILE`, and round/drift variables passed by the stop hook. Output is `round-N-prompt.md`, later returned as JSON block reason. The template itself does not mutate state, but it instructs Claude to create/update `round-N-contract.md`, maintain `goal-tracker.md` mutable section, and select BitLesson IDs before each task.
- gates_or_invariants: The prompt enforces exactly one mainline objective, 1-2 target ACs, explicit blocking versus queued issue classification, concrete success criteria, and “Do not start implementation until the round contract exists” at [next-round-prompt.md](/Users/wangweiyang/GitHub/humanize/prompt-template/claude/next-round-prompt.md:20). Task tags must be `[mainline]`, `[blocking]`, or `[queued]`, with queued work prevented from replacing the round objective at [next-round-prompt.md](/Users/wangweiyang/GitHub/humanize/prompt-template/claude/next-round-prompt.md:29). Mainline guardrails prohibit queued issues from taking over at [next-round-prompt.md](/Users/wangweiyang/GitHub/humanize/prompt-template/claude/next-round-prompt.md:67).
- dependencies_and_callers: Depends on `template-loader.sh` and stop-hook rendering. The stop hook may append footer, task-tag routing reminder, push-every-round note, goal tracker update request, open-question notice, post-alignment instructions, and agent-teams continuation after rendering. If drift recovery is required, the hook chooses `claude/drift-replan-prompt.md` instead of this template at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:2055).
- edge_cases_or_failure_modes: This template assumes normal continuation; if mainline stall count reaches the replan threshold, it is bypassed. If BitLesson is disabled or the rendered template already includes selector instructions, the hook avoids duplicate insertion except in drift path. If Codex review includes “Open Question”, the hook may inject an AskUserQuestion notice into the rendered prompt at [loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize/hooks/loop-codex-stop-hook.sh:2116).
- validation_or_tests: Covered by all-template loading/rendering and placeholder syntax checks in `tests/test-templates-comprehensive.sh`. Behavior is also indirectly exercised by stop-hook tests that inspect next-round prompt construction, though no assigned file is solely dedicated to this prompt.
- skip_candidate: `no`

### HZ-193 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for `scripts/setup-rlcr-loop.sh` plan-file path and content validation. It protects the RLCR bootstrap boundary: the plan must be a safe, project-relative, readable, meaningful file and must not cross symlinks or shell-injection paths before loop state is created.
- algorithmic_behavior: The test creates a temporary git repo, installs a mock `codex`, writes valid/invalid plan files, then invokes the real setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"` at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:76). It classifies output containing plan validation errors as rejection; output reaching later Codex/gitignore checks is treated as path-validation acceptance at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:91).
- inputs_outputs_state: Inputs are synthetic plan paths and files inside the temp repo, a clean initial git commit, `.gitignore`, mock Codex on `PATH`, and the production `setup-rlcr-loop.sh`. Outputs are pass/fail summary lines. The setup script may create `.humanize/rlcr` inside the temp repo during accepted cases; the helper removes prior loop state before each run at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:83).
- gates_or_invariants: Positive cases accept normal relative paths, root filenames, dash/underscore, nested directories, dotted filenames, long filenames, and 10-level deep paths at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:122) and [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:402). Negative cases reject absolute paths, spaces, shell metacharacters, `$`, backticks, glob characters, brackets, hash, tilde, backslash, and punctuation used for command injection at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:179). Symlink file, parent-dir symlink, and symlink-chain cases are rejected at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:342). Content gates reject empty files, comment-only files, files under five lines, non-existent paths, and directories at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:425).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `git`, shell utilities, and the production setup script. The production validation block rejects absolute paths at [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:455), spaces at [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:461), shell metacharacters at [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:469), symlink leaves and parents at [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:481) and [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:494), out-of-project real paths at [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:529), and content insufficiency at [setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize/scripts/setup-rlcr-loop.sh:618).
- edge_cases_or_failure_modes: The test notes Unicode/CJK/emoji path characters are allowed, but it does not actively create such a path at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:414). Its classification depends on matching current error-message text, so message wording changes could cause false test failures even if validation behavior remains sound. It treats gitignore/tracking errors as evidence that path validation passed, which is correct for isolating the path-validation layer but not a full setup success assertion.
- validation_or_tests: This file is itself a robustness validation suite and ends through `print_test_summary "Path Validation Robustness Test Summary"` at [test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize/tests/robustness/test-path-validation-robustness.sh:486). Not executed in this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7, represented by the seven item evidence section headers above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`