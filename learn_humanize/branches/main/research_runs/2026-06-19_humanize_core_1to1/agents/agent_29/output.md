# agent_29 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 6
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-029 `file` `agents/plan-compliance-checker.md`
- cursor: `[_]`
- core_role: Specialized agent prompt for the `/humanize:start-rlcr-loop` preflight. It decides whether a user implementation plan is related to the current repository and whether it contains branch-switching instructions that would violate RLCR’s constant-branch invariant.
- algorithmic_behavior: The agent is instructed to perform two checks. Check A explores repository documentation and structure, then leniently rejects only plans clearly unrelated to the repo (`agents/plan-compliance-checker.md:16-30`). Check B scans the full plan for required branch switches, including natural-language instructions and commands such as `git switch`, `git branch`, `git checkout -b`, `gh pr checkout`, or worktree creation (`agents/plan-compliance-checker.md:31-39`). It explicitly suppresses false positives for file restore commands, negated “do not switch” language, descriptive branch references, and `--base-branch` review parameters (`agents/plan-compliance-checker.md:40-45`).
- inputs_outputs_state: Input is the plan file content plus repository access via `Read`, `Glob`, and `Grep` (`agents/plan-compliance-checker.md:5`, `agents/plan-compliance-checker.md:14-15`). Output must be exactly one verdict line: `PASS: ...`, `FAIL_RELEVANCE: ...`, or `FAIL_BRANCH_SWITCH: ...` (`agents/plan-compliance-checker.md:48-55`). It has no persistent state; it is a stateless classifier prompt.
- gates_or_invariants: The critical invariant is that an RLCR loop keeps the same working branch for all rounds (`agents/plan-compliance-checker.md:46`). The command that invokes it treats missing or malformed verdicts as fail-closed: unexpected output stops the command (`commands/start-rlcr-loop.md:52-56`).
- dependencies_and_callers: The caller is `commands/start-rlcr-loop.md`, which directs Claude to invoke `humanize:plan-compliance-checker` using the Task tool with the `sonnet` model (`commands/start-rlcr-loop.md:41-49`). The command continues only on `PASS`, stops on either failure verdict, and reports user-facing explanations (`commands/start-rlcr-loop.md:52-56`).
- edge_cases_or_failure_modes: Relevance is deliberately lenient, so ambiguous plans pass rather than blocking (`agents/plan-compliance-checker.md:29`, `agents/plan-compliance-checker.md:59`). Branch-switch detection is also conservative and passes on uncertainty to avoid false positives (`agents/plan-compliance-checker.md:60`). Because the command parser looks for verdict substrings, malformed multi-line or mixed verdict output can trigger fail-closed behavior.
- validation_or_tests: I found no direct shell test for this agent file. Its behavior is specified by the agent prompt itself and by `commands/start-rlcr-loop.md:41-56`, which documents the caller contract and failure handling.
- skip_candidate: `no`

### HZ-059 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Standalone Claude Code status line renderer that adds an RLCR status field for the Humanize plugin while also showing generic session, cost, context, branch, fast-mode, and line-change information. The file declares that only the final RLCR field is Humanize-specific (`scripts/statusline.sh:7-14`).
- algorithmic_behavior: It reads a JSON payload from stdin (`scripts/statusline.sh:19`) and extracts values through `jq` via `get_value` (`scripts/statusline.sh:21-24`). It determines RLCR display status by inspecting `$CWD/.humanize/rlcr` and optional Claude `session_id` (`scripts/statusline.sh:257-262`). `_resolve_rlcr_display` maps state files to display labels: `methodology-analysis-state.md` to `Analyzing`, `finalize-state.md` to `Finalizing`, `state.md` to `Active`, terminal `*-state.md` names to capitalized stop reasons, and no state to `Off` (`scripts/statusline.sh:36-63`). `get_rlcr_status` scans loop directories newest-first and uses `session_id` frontmatter when available (`scripts/statusline.sh:67-155`).
- inputs_outputs_state: Inputs are stdin JSON fields `.model.display_name`, `.cwd`, `.session_id`, `.transcript_path`, `.cost.*`, and `.context_window.used_percentage` (`scripts/statusline.sh:169-174`, `scripts/statusline.sh:242-245`, `scripts/statusline.sh:326`). Additional filesystem inputs include `$CWD/.humanize/rlcr`, `$HOME/.claude/settings.json`, transcript JSONL files, `sessions-index.json`, and Git branch state (`scripts/statusline.sh:178-239`). Output is one or more ANSI-colored terminal lines assembled from eight fields and greedily wrapped to 75% of terminal width (`scripts/statusline.sh:376-403`).
- gates_or_invariants: The RLCR lookup mirrors `find_active_loop` intent but is display-only. It uses zombie-loop protection when no session id is present by checking only the newest directory unless session-aware files exist (`scripts/statusline.sh:76-93`). With a session id, it skips session-unaware entries when any session-aware state exists and returns only a matching stored session (`scripts/statusline.sh:121-155`). Color mapping encodes state severity: active/finalizing green, complete cyan, pause/cancel/stop yellow, failed/timeout/maxiter red, off dim (`scripts/statusline.sh:157-167`).
- dependencies_and_callers: Depends on `jq`, `git`, `grep`, `awk`, `sed`, `cut`, `tr`, `ls`, `sort`, `head`, `stty`, and `tput`. It is self-contained; repository search found no in-repo caller other than comments in this file. Its RLCR lookup is related to `hooks/lib/loop-common.sh:308-425`, especially the session-aware `find_active_loop` behavior and active-state priority.
- edge_cases_or_failure_modes: If `jq` is missing or JSON fields are absent, values default to empty and later to `?`, `0`, or `Off`. If `/dev/tty` is unavailable, terminal width falls back through `tput cols` and then `80` (`scripts/statusline.sh:379-382`). The session-aware search uses `grep -rqE "$rlcr_dir"/*/*.md`; with no matches it suppresses errors (`scripts/statusline.sh:76-80`). It does not implement `find_active_loop`’s marker fallback (`hooks/lib/loop-common.sh:399-424`), which is acceptable for display but a divergence from stop-hook adoption semantics.
- validation_or_tests: I found no direct tests or installation reference for `scripts/statusline.sh`. Related RLCR state semantics are tested elsewhere through `find_active_loop`, for example finalize-state detection in `tests/test-finalize-phase.sh:331-371` and session isolation in `tests/test-stop-hook-bg-allow.sh` references found by search.
- skip_candidate: `yes: included as a community/shared status-line reference where only the RLCR display field is repository-specific; it is not a core mutating algorithm or scheduler path`

### HZ-089 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: Thin executable test shard for end-to-end monitor SIGINT handling. It selects the SIGINT subset from the larger real monitor E2E suite.
- algorithmic_behavior: The script enables strict shell error handling (`tests/test-monitor-e2e-sigint.sh:3`), resolves its directory, sources `tests/test-monitor-e2e-real.sh`, prints a suite banner, runs `monitor_test_bash_sigint` and `monitor_test_zsh_sigint`, then exits based on the shared `TESTS_FAILED` counter (`tests/test-monitor-e2e-sigint.sh:4-21`).
- inputs_outputs_state: Inputs are the sourced test harness and the local shell environment. Outputs are human-readable test progress and a process exit code: `0` when `TESTS_FAILED` is zero, `1` otherwise (`tests/test-monitor-e2e-sigint.sh:15-21`). State is inherited from the sourced harness, particularly `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: The invariant under test is that the real RLCR monitor can receive SIGINT/Ctrl+C, clean up terminal/tail state, and avoid shell glob errors. The bash test creates a fake project loop and cache log, runs `_humanize_monitor_codex`, sends SIGINT to the process group, waits, and accepts either clean SIGINT exit or forced SIGTERM fallback when SIGINT delivery is difficult (`tests/test-monitor-e2e-real.sh:377-548`). The zsh test requires `zsh`, sends SIGINT to the process, and requires exit or cleanup indication without glob errors (`tests/test-monitor-e2e-real.sh:555-684`).
- dependencies_and_callers: Sourced dependency is `tests/test-monitor-e2e-real.sh` (`tests/test-monitor-e2e-sigint.sh:5`). The suite is included in `tests/run-all-tests.sh:82`. Runtime dependencies include `bash`, optional `zsh`, process signaling via `kill`, temporary fake `.humanize/rlcr` directories, fake cache logs, and `scripts/humanize.sh` sourced by generated runner scripts (`tests/test-monitor-e2e-real.sh:464-468`, `tests/test-monitor-e2e-real.sh:632-635`).
- edge_cases_or_failure_modes: The bash test explicitly tolerates force termination after SIGTERM as a pass when SIGINT delivery does not end the monitor (`tests/test-monitor-e2e-real.sh:505-518`). It can skip zsh behavior when `zsh` is not installed (`tests/test-monitor-e2e-real.sh:560-562`). It fails if the monitor exits too early, no cleanup signal/exit evidence appears, or shell glob errors such as `no matches found` / `bad pattern` appear (`tests/test-monitor-e2e-real.sh:520-548`, `tests/test-monitor-e2e-real.sh:669-683`).
- validation_or_tests: This file is itself the validation entrypoint for SIGINT E2E behavior. It complements lower-level signal checks in `tests/test-monitor-runtime.sh:327-423`, which verify bash trap setup, zsh `TRAPINT`/`TRAPTERM`, and cleanup trap reset.
- skip_candidate: `no`

### HZ-119 `file` `prompt-template/block/finalize-contract-access.md`
- cursor: `[_]`
- core_role: Rendered prompt block used by validators to block access to historical `round-N-contract.md` files during the Finalize Phase, when no active round contract should be read, edited, or written.
- algorithmic_behavior: The template emits a fixed heading and explanation, substitutes `{{ACTION}}` into the prohibition, and redirects the agent to `finalize-summary.md` for finalize-only notes or `goal-tracker.md` for current mainline/backlog state (`prompt-template/block/finalize-contract-access.md:1-7`).
- inputs_outputs_state: Input is the template variable `ACTION`, supplied by `finalize_contract_blocked_message` (`hooks/lib/loop-common.sh:877-890`). Output is a markdown block printed to stderr by validators. It has no persistent state.
- gates_or_invariants: The invariant is phase-specific: in Finalize Phase there is no active round contract, so historical contract files are blocked rather than treated as current writable/readable state. Read validator blocks contract reads in Finalize Phase (`hooks/loop-read-validator.sh:218-220`); write validator blocks contract writes (`hooks/loop-write-validator.sh:230-233`); edit validator also calls the same helper (`hooks/loop-edit-validator.sh:182`, found by reference search).
- dependencies_and_callers: Loaded via `load_and_render_safe` from `hooks/lib/loop-common.sh:888-889`, with an inline fallback if the template cannot be loaded (`hooks/lib/loop-common.sh:881-886`). Caller validators depend on state parsing to know `IS_FINALIZE_PHASE` before invoking the block.
- edge_cases_or_failure_modes: If the template is missing or template loading fails, the fallback message in `finalize_contract_blocked_message` preserves the same core prohibition (`hooks/lib/loop-common.sh:881-886`). If `ACTION` is awkwardly phrased by a caller, the rendered sentence can be grammatically rough, but the enforcement still exits with block code in validators.
- validation_or_tests: Direct references show integration through read/write/edit validators. Broader Finalize Phase tests verify finalize-state detection and validator blocking behavior, including write/edit/bash blocks for finalize state and finalize phase transitions in `tests/test-finalize-phase.sh:331-405` and related sections. I found no template-specific test that renders this exact file by name.
- skip_candidate: `no`

### HZ-149 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Rendered prompt block for read/write validators when the requested round file name is correct but located under the wrong loop directory path. It enforces that operations target the active loop directory’s canonical path.
- algorithmic_behavior: The template reports the attempted action and file path, prints the computed correct path, and suggests `cat {{FILE_PATH}}` if the user needs to read that exact file (`prompt-template/block/wrong-directory-path.md:1-6`). It is used after validators derive `CLAUDE_FILENAME` and compare canonicalized requested path against `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`.
- inputs_outputs_state: Inputs are `ACTION`, `FILE_PATH`, and `CORRECT_PATH`, supplied by validators through `load_and_render_safe` (`hooks/loop-read-validator.sh:314-320`, `hooks/loop-write-validator.sh:354-360`). Output is a markdown block on stderr followed by validator exit code `2` in both read and write paths (`hooks/loop-read-validator.sh:317-321`, `hooks/loop-write-validator.sh:357-361`). No persistent state is modified.
- gates_or_invariants: The path invariant is active-loop locality: even if the file name and round number look valid, the operation must address the active loop directory, not another `.humanize/rlcr` directory. Both read and write validators compare prefix-canonical forms to handle equivalent ancestor paths while avoiding dereferencing a malicious leaf symlink (`hooks/loop-read-validator.sh:305-321`, `hooks/loop-write-validator.sh:342-361`).
- dependencies_and_callers: Used by `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`. It depends indirectly on `canonicalize_path_prefix`, `ACTIVE_LOOP_DIR`, `CLAUDE_FILENAME`, and template loading from `loop-common.sh`. The same template is not used by the edit validator in the searched references.
- edge_cases_or_failure_modes: The read/write validators use inline fallbacks with the same variables if the template cannot load (`hooks/loop-read-validator.sh:314-317`, `hooks/loop-write-validator.sh:354-357`). The line suggesting `cat {{FILE_PATH}}` is read-oriented and can be odd for write actions, but the block still communicates the correct path and exits with a block code.
- validation_or_tests: Template loader tests cover rendering mechanics using a neighboring real template, not this exact file (`tests/test-template-loader.sh:147-164`, `tests/test-templates-comprehensive.sh:497-510`). Validator tests elsewhere exercise path and phase restrictions; direct references confirm this block is the concrete message for directory mismatch in read/write validators.
- skip_candidate: `no`

### HZ-179 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Runtime monitor for Humanize one-shot skill invocations under `.humanize/skill`, covering `ask-codex` and `ask-gemini`. It provides `_humanize_monitor_skill`, which powers `humanize monitor skill`, `humanize monitor codex`, and `humanize monitor gemini`.
- algorithmic_behavior: The function parses `--once` and `--tool-filter <codex|gemini>` (`scripts/lib/monitor-skill.sh:31-41`), requires `.humanize/skill` to exist (`scripts/lib/monitor-skill.sh:43-48`), classifies invocation tool from `metadata.md` or `input.md` (`scripts/lib/monitor-skill.sh:50-64`), filters legacy unknown tool entries as Codex when requested (`scripts/lib/monitor-skill.sh:66-75`), lists timestamp-named invocation dirs newest-first (`scripts/lib/monitor-skill.sh:77-90`), chooses the newest invocation with watchable non-empty content (`scripts/lib/monitor-skill.sh:97-117`), counts statuses (`scripts/lib/monitor-skill.sh:119-143`), extracts first-line question text (`scripts/lib/monitor-skill.sh:145-152`), resolves global cache path (`scripts/lib/monitor-skill.sh:154-163`), and selects the best monitored file from output, cache logs, cache stdout, local cache, or input fallback (`scripts/lib/monitor-skill.sh:165-217`).
- inputs_outputs_state: Inputs are `.humanize/skill/<timestamp...>/input.md`, optional `metadata.md`, optional `output.md`, local `cache/`, global `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/skill-<id>/`, terminal dimensions, and optional filter arguments. `ask-codex.sh` creates the expected `input.md`, `metadata.md`, `output.md`, and `codex-run.*` files (`scripts/ask-codex.sh:202-238`, `scripts/ask-codex.sh:262-276`, `scripts/ask-codex.sh:391-406`); `ask-gemini.sh` creates analogous Gemini files (`scripts/ask-gemini.sh:184-218`, `scripts/ask-gemini.sh:247-261`, `scripts/ask-gemini.sh:368-380`). Outputs are either a one-shot textual report including watched output and recent invocations (`scripts/lib/monitor-skill.sh:339-425`) or an interactive terminal monitor with a fixed status bar and live `tail -f` output (`scripts/lib/monitor-skill.sh:428-519`). State is local shell state plus a background `tail` PID.
- gates_or_invariants: Valid invocation directories must start with `YYYY-MM-DD_HH-MM-SS` (`scripts/lib/monitor-skill.sh:77-90`). Missing `.humanize/skill` is a hard user error (`scripts/lib/monitor-skill.sh:43-48`). `--once` returns nonzero when no matching invocation exists (`scripts/lib/monitor-skill.sh:339-347`). Running invocations are identified by absence of `metadata.md` and prefer live `.log` files over `.out` files (`scripts/lib/monitor-skill.sh:173-208`). Completed invocations prefer `output.md`, then stdout/log cache fallbacks (`scripts/lib/monitor-skill.sh:209-215`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` if present (`scripts/humanize.sh:1243-1245`). The public dispatcher maps `humanize monitor skill` to all invocations and `humanize monitor codex/gemini` to filtered modes (`scripts/humanize.sh:1195-1211`). It depends on `monitor_get_yaml_value` and `monitor_format_timestamp` from `scripts/lib/monitor-common.sh` (`scripts/lib/monitor-common.sh:201-213`, `scripts/lib/monitor-common.sh:219-222`) and `humanize_split_to_array` from `scripts/humanize.sh:22-34`, plus standard shell tools `find`, `sort`, `head`, `grep`, `sed`, `git`, `tput`, `tail`, `kill`, `wait`, and `disown`.
- edge_cases_or_failure_modes: If metadata lacks a `tool`, the monitor reports `unknown`; legacy unknowns pass the Codex filter (`scripts/lib/monitor-skill.sh:70-74`). If a running invocation has no cache logs, it falls back to `input.md`, so the monitor can show something before model output exists (`scripts/lib/monitor-skill.sh:202-208`). If a completed invocation has no output or cache content, one-shot mode prints “No output available” unless status is running (`scripts/lib/monitor-skill.sh:394-400`). Interactive cleanup is guarded by `cleanup_done`, kills/disowns the tail process, resets scroll region, restores cursor/screen, and prints “Monitor stopped.” (`scripts/lib/monitor-skill.sh:438-461`). Bash uses traps for `EXIT INT TERM`; zsh defines `TRAPINT`/`TRAPTERM` plus an EXIT trap (`scripts/lib/monitor-skill.sh:463-470`). Potential fragility: helper functions are nested and share names inside the monitor scope; `tput cols` is assumed to succeed in status drawing.
- validation_or_tests: `tests/test-skill-monitor.sh` exercises `--once` behavior: missing directory, empty directory, single success, mixed statuses, running invocation, recent list, first-line question extraction, empty response, and ignoring non-timestamp directories (`tests/test-skill-monitor.sh:110-389`). Interactive mode is explicitly not covered there (`tests/test-skill-monitor.sh:5-6`). Related signal behavior for the RLCR monitor is covered by `tests/test-monitor-e2e-sigint.sh`, but that suite targets `_humanize_monitor_codex`, not this skill monitor.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `6/6 Item Evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`