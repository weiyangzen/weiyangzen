# agent_06 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the executable specification suite for the RLCR/Humanize loop algorithm. It is a flat directory of shell test suites plus an aggregate runner. It verifies setup-time plan validation, hook-time read/write/edit/bash gates, template loading/rendering, state-file lifecycle transitions, finalize-phase behavior, cancel authorization, and regression hardening around path and command-injection edge cases. The directory is therefore core validation evidence for the branch algorithm, not merely peripheral tests.
- algorithmic_behavior: `tests/run-all-tests.sh` enumerates the suite order and treats each child script as a standalone executable validator, capturing output, stripping ANSI escape codes, extracting `Passed:`/`Failed:` counts, and failing if any suite exits nonzero or reports failures (`tests/run-all-tests.sh:32`, `tests/run-all-tests.sh:61`, `tests/run-all-tests.sh:66`, `tests/run-all-tests.sh:75`). The child tests coordinate around `.humanize/rlcr/<timestamp>/` loop directories and temporary Git repositories. They synthesize `state.md`, `finalize-state.md`, `complete-state.md`, `cancel-state.md`, `plan.md`, `goal-tracker.md`, `round-N-summary.md`, `round-N-todos.md`, and transcript JSONL inputs, then invoke production hooks and helpers under `hooks/` and `scripts/`.
- algorithmic_behavior: Recursive child roles are:
  - `test-template-loader.sh` validates `hooks/lib/template-loader.sh`: locating `prompt-template/`, loading existing/missing templates, single-pass `{{VAR}}` rendering, safe fallback rendering, strict-mode behavior, shell metacharacters, placeholder-injection prevention, empty/unclosed placeholders, multiline values, and non-ASCII handling (`tests/test-template-loader.sh:41`, `tests/test-template-loader.sh:147`, `tests/test-template-loader.sh:197`, `tests/test-template-loader.sh:247`, `tests/test-template-loader.sh:488`).
  - `test-template-references.sh` statically scans the hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and ensures referenced templates exist (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:93`, `tests/test-template-references.sh:102`). This assigned file is detailed separately below.
  - `test-templates-comprehensive.sh` loads every `prompt-template/**/*.md`, checks template directory structure, validates placeholder syntax, exercises malformed placeholder detection, renders dummy values through all templates, and stress-tests repeated rendering (`tests/test-templates-comprehensive.sh:61`, `tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:118`, `tests/test-templates-comprehensive.sh:197`, `tests/test-templates-comprehensive.sh:542`).
  - `test-error-scenarios.sh` hardens template-loader failure behavior: missing templates/directories return empty without crashing, `load_and_render_safe` uses fallbacks, strict-mode scripts continue, malformed placeholders remain literal (`tests/test-error-scenarios.sh:42`, `tests/test-error-scenarios.sh:109`, `tests/test-error-scenarios.sh:134`, `tests/test-error-scenarios.sh:182`).
  - `test-bash-validator-patterns.sh` unit-tests `command_modifies_file` patterns for redirection, `tee`, in-place editors, `mv/cp/rm`, `dd`, `truncate`, `exec`, false positives, and round summary/state patterns (`tests/test-bash-validator-patterns.sh:36`, `tests/test-bash-validator-patterns.sh:70`, `tests/test-bash-validator-patterns.sh:96`, `tests/test-bash-validator-patterns.sh:110`, `tests/test-bash-validator-patterns.sh:175`, `tests/test-bash-validator-patterns.sh:186`).
  - `test-todo-checker.sh` validates `hooks/check-todos-from-transcript.py` input parsing and the stop-hook todo gate. It expects malformed hook input to exit `2`, missing/nonexistent transcript paths to exit `0`, completed todos to exit `0`, incomplete or missing-status todos to exit `1`, invalid JSONL lines to be ignored, and the latest `TodoWrite` call to win (`tests/test-todo-checker.sh:45`, `tests/test-todo-checker.sh:100`, `tests/test-todo-checker.sh:159`, `tests/test-todo-checker.sh:242`).
  - `test-plan-file-validation.sh` exercises `scripts/setup-rlcr-loop.sh` setup gates: absolute path rejection, missing file/parent directory errors, spaces/metacharacter rejection, symlink rejection, project-root escape checks, Git repo and commit requirements, tracked/untracked plan-file mode consistency, branch/model YAML-safety checks, minimum meaningful plan content, CLI argument exclusivity, and codex model/effort validation (`tests/test-plan-file-validation.sh:3`, `tests/test-plan-file-validation.sh:89`, `tests/test-plan-file-validation.sh:169`, `tests/test-plan-file-validation.sh:274`, `tests/test-plan-file-validation.sh:329`, `tests/test-plan-file-validation.sh:432`, `tests/test-plan-file-validation.sh:515`, `tests/test-plan-file-validation.sh:663`, `tests/test-plan-file-validation.sh:697`).
  - `test-plan-file-hooks.sh` verifies runtime plan integrity across hooks: `loop-plan-file-validator.sh` requires `plan_tracked` and `start_branch`, detects branch changes and YAML-quoted fields, blocks writes/edits/bash modifications to loop `plan.md`, rejects command-substitution/glob/brace/piped/backtick bypasses, blocks modified/deleted project plans and missing backups in the stop hook, detects tracked-plan races and committed changes by content diff, reports goal-tracker placeholders section-specifically, and intentionally allows legacy `.humanize-loop.local` paths (`tests/test-plan-file-hooks.sh:88`, `tests/test-plan-file-hooks.sh:121`, `tests/test-plan-file-hooks.sh:164`, `tests/test-plan-file-hooks.sh:194`, `tests/test-plan-file-hooks.sh:268`, `tests/test-plan-file-hooks.sh:388`, `tests/test-plan-file-hooks.sh:466`, `tests/test-plan-file-hooks.sh:631`, `tests/test-plan-file-hooks.sh:733`, `tests/test-plan-file-hooks.sh:1012`).
  - `test-allowlist-validators.sh` validates the active-loop allowlist for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`, then confirms Read/Write/Edit/Bash validators allow only those exact files in the active loop directory and reject same basenames in wrong/old/different-root directories (`tests/test-allowlist-validators.sh:64`, `tests/test-allowlist-validators.sh:127`, `tests/test-allowlist-validators.sh:186`, `tests/test-allowlist-validators.sh:229`, `tests/test-allowlist-validators.sh:284`).
  - `test-state-exit-naming.sh` validates state lifecycle semantics: only `state.md` and, in branch-specific finalize logic elsewhere, `finalize-state.md` indicate active loops; `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are terminal states; `end_loop` accepts only valid reasons and renames to `<reason>-state.md`; new `.humanize/rlcr` paths are recognized and legacy `.humanize-loop.local` is ignored (`tests/test-state-exit-naming.sh:38`, `tests/test-state-exit-naming.sh:93`, `tests/test-state-exit-naming.sh:178`, `tests/test-state-exit-naming.sh:241`).
  - `test-finalize-phase.sh` is the branch-heavy suite for the Finalize Phase: it validates `is_finalize_state_file_path`, `is_finalize_summary_path`, active-loop detection with `finalize-state.md`, write/edit/bash protection for `finalize-state.md`, write allowance for `finalize-summary.md`, finalization gates for summary existence, Git cleanliness, completed todos, no Codex invocation during finalize, transition from `state.md` to `finalize-state.md` on `COMPLETE`, max-iteration bypass to `maxiter-state.md`, successful rename to `complete-state.md`, and unchanged normal RLCR rounds (`tests/test-finalize-phase.sh:174`, `tests/test-finalize-phase.sh:196`, `tests/test-finalize-phase.sh:219`, `tests/test-finalize-phase.sh:274`, `tests/test-finalize-phase.sh:354`, `tests/test-finalize-phase.sh:442`, `tests/test-finalize-phase.sh:498`, `tests/test-finalize-phase.sh:617`).
  - `test-cancel-signal-file.sh` is the branch-heavy cancel suite: it requires `.cancel-requested` in the active loop dir before allowing `mv state.md cancel-state.md`, rejects the same operation without the signal or with the signal in the wrong directory, blocks all other state modifications, and tests command-injection and argument-shape bypasses including `&&`, `;`, `$()`, backticks, newlines, extra args, hidden variables, `sudo`, `env`, options, quoted paths, and literal `${loop_dir}` documented syntax (`tests/test-cancel-signal-file.sh:3`, `tests/test-cancel-signal-file.sh:75`, `tests/test-cancel-signal-file.sh:116`, `tests/test-cancel-signal-file.sh:136`, `tests/test-cancel-signal-file.sh:196`, `tests/test-cancel-signal-file.sh:238`, `tests/test-cancel-signal-file.sh:357`, `tests/test-cancel-signal-file.sh:440`, `tests/test-cancel-signal-file.sh:486`).
  - `test-ansi-parsing.sh` verifies that the aggregate runner’s BSD/GNU-portable ANSI stripping preserves pass/fail count extraction from colored output (`tests/test-ansi-parsing.sh:38`, `tests/test-ansi-parsing.sh:70`, `tests/test-ansi-parsing.sh:112`).
- inputs_outputs_state: Inputs are temporary Git repos, loop directories, synthetic YAML frontmatter, mock `codex` executables, hook JSON on stdin, transcript JSONL files, shell commands to validate, and prompt/template files. Outputs are suite pass/fail counters, process exit codes, hook block JSON or stderr messages, created/renamed state files, created summaries/review-result files, and temporary marker files such as `codex_called.marker`. State transitions under test include normal `state.md` active-loop progression, `state.md -> finalize-state.md` after Codex `COMPLETE`, finalize success `finalize-state.md -> complete-state.md`, max-iteration `state.md -> maxiter-state.md`, cancel authorization `state.md` or `finalize-state.md -> cancel-state.md`, and invalid/unexpected terminal states via `end_loop`.
- gates_or_invariants: Core invariants include: loop state files are system-owned and protected; cancel requires `.cancel-requested` and an exact two-argument `mv` to `cancel-state.md`; finalize-state is active but complete/cancel/stop/maxiter/unexpected states are terminal; finalize requires `finalize-summary.md`, clean Git, completed todos, and no Codex review; normal rounds require round-specific summaries and todo completion before stop-hook progress; plan files must be relative, inside the repo, non-symlink, Git-status-consistent, contentful, and branch/schema-safe; protected loop files cannot be modified through Bash bypasses; old `.humanize-loop.local` is no longer an active loop root; templates must exist or have safe fallbacks; placeholder rendering is single-pass and injection-resistant.
- dependencies_and_callers: `tests/run-all-tests.sh` is the parent harness for all child suites (`tests/run-all-tests.sh:32`). Most suites call production scripts and hooks: `scripts/setup-rlcr-loop.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/check-todos-from-transcript.py`, `hooks/lib/loop-common.sh`, and `hooks/lib/template-loader.sh`. The tests require standard shell tools plus `git`, `jq`, `python3`, `sed`, `grep`, `awk`, `mktemp`, and executable permissions on the test scripts. Production helper relationships visible in the code include `find_active_loop`, `parse_state_file`, `is_allowlisted_file`, `is_finalize_summary_path`, `is_cancel_authorized`, `command_modifies_file`, and `end_loop` in `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: Edge coverage is extensive: missing templates, missing template directories, empty template content, special regex and shell metacharacters, placeholder-in-placeholder injection, invalid JSON input, malformed JSONL transcript lines, missing todo status, stale/old loop directories, legacy path false positives, path spaces, shell metacharacters in plan paths, inaccessible plan directories, branch names unsafe for YAML, committed-but-changed tracked plans, active-loop same-basename path spoofing, state-file `mv/cp` from-source bypasses, shell wrapper bypasses, redirections before commands, command chaining, command substitution, hidden `${IFS}` variables, extra `mv` args, wrong cancel signal location, and finalize not invoking Codex.
- validation_or_tests: The directory is itself validation. The aggregate runner lists all suites in order and aggregates counts (`tests/run-all-tests.sh:32`, `tests/run-all-tests.sh:86`). I inspected the directory recursively with no artificial depth limit; it contains only the flat `tests/` directory and the 14 executable shell files shown in the runner. I did not execute the suites because the assignment requested read-only research notes, and some tests intentionally create temp Git repos and hook fixtures.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-036 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: This file is an executable static analysis gate for template reference integrity. Its role is to prevent hook validators from emitting empty or missing block messages when they reject a user action. It is core because many algorithmic gates rely on `prompt-template/block/*.md` text being present and rendered safely.
- algorithmic_behavior: The script initializes project paths from its own location, sets `TEMPLATE_DIR="$PROJECT_ROOT/prompt-template"`, and maintains pass/fail/warning counters (`tests/test-template-references.sh:14`, `tests/test-template-references.sh:25`). Section 1 defines the production shell scripts to scan: `loop-codex-stop-hook.sh`, read/write/edit/bash validators, and `hooks/lib/loop-common.sh` (`tests/test-template-references.sh:56`). For each file, it skips comment lines, matches calls to `load_template`, `load_and_render`, or `load_and_render_safe` whose first argument is exactly `"$TEMPLATE_DIR"`, extracts the second quoted argument with `sed`, and asserts that `prompt-template/<path>` exists (`tests/test-template-references.sh:87`, `tests/test-template-references.sh:93`, `tests/test-template-references.sh:96`, `tests/test-template-references.sh:102`). Section 2 enumerates all `*.md` templates and warns when a template is not directly quoted in hooks; warnings do not fail the suite because dynamic usage may be valid (`tests/test-template-references.sh:121`, `tests/test-template-references.sh:134`, `tests/test-template-references.sh:139`). Section 3 hard-codes common block templates used by message helper functions in `loop-common.sh` and asserts those files exist (`tests/test-template-references.sh:149`, `tests/test-template-references.sh:152`). Section 4 checks critical path validators, specifically Read/Write/Edit, and fails if they call plain `load_and_render` instead of `load_and_render_safe`, enforcing fallback messages for blocking paths (`tests/test-template-references.sh:172`, `tests/test-template-references.sh:176`, `tests/test-template-references.sh:185`, `tests/test-template-references.sh:196`).
- inputs_outputs_state: Inputs are hook shell scripts, `hooks/lib/loop-common.sh`, and the `prompt-template/**/*.md` tree. The output is colored PASS/FAIL/WARN text plus summary counts. State is limited to in-process arrays and counters: `MISSING_TEMPLATES`, `UNREFERENCED`, `FOUND_REFERENCES`, `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` (`tests/test-template-references.sh:71`, `tests/test-template-references.sh:122`). The final transition is binary: exit `0` if `TESTS_FAILED == 0`, otherwise print missing template details and exit `1` (`tests/test-template-references.sh:213`, `tests/test-template-references.sh:221`, `tests/test-template-references.sh:225`).
- gates_or_invariants: Every statically quoted template reference in the scanned hook files must resolve to an existing file. The common message templates for todos, prompt writes, state modifications, summary Bash writes, goal-tracker Bash writes, and goal-tracker modification must exist (`tests/test-template-references.sh:152`). Critical validators must use `load_and_render_safe` for all rendering so missing templates degrade to fallback messages instead of empty blocks (`tests/test-template-references.sh:174`, `tests/test-template-references.sh:196`). Unreferenced templates are informational warnings, not failures (`tests/test-template-references.sh:130`).
- dependencies_and_callers: The script depends on `find`, `grep`, `sed`, Bash arrays, and readable project files. It is called by the aggregate runner as the fifth suite (`tests/run-all-tests.sh:32`, `tests/run-all-tests.sh:38`). It validates production usage of `hooks/lib/template-loader.sh`, whose `load_template` returns empty on missing files, while `load_and_render_safe` substitutes a fallback (`hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:170`). It specifically covers the hook/template call surface across stop/read/write/edit/bash validators and common message helpers.
- edge_cases_or_failure_modes: The extraction logic only recognizes direct quoted calls using literal `"$TEMPLATE_DIR"` and a quoted second argument; templates built dynamically or called through other variable names become warnings or are invisible to Section 1. Comment lines are skipped to avoid false positives (`tests/test-template-references.sh:87`). Missing scanned hook scripts produce warnings rather than immediate failures (`tests/test-template-references.sh:75`). Missing templates fail, and unsafe non-safe rendering in Read/Write/Edit fails. Unreferenced templates warn because dynamic or future-use templates may be legitimate (`tests/test-template-references.sh:139`).
- validation_or_tests: This file is directly listed in `run-all-tests.sh` (`tests/run-all-tests.sh:38`). Its own validation behavior is its pass/fail counter and exit code. It also indirectly protects `prompt-template/block/wrong-summary-location.md`, because `hooks/loop-write-validator.sh` references that template with `load_and_render_safe` when blocking summary writes outside `.humanize/rlcr` (`hooks/loop-write-validator.sh:155`, `hooks/loop-write-validator.sh:160`).
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-066 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: This is a block-message template used by the Write validator when a round summary filename is written outside the active loop directory. It is small but core to the human-facing enforcement path because it tells the agent the required target for summary output.
- algorithmic_behavior: The template contains a title, a short invariant statement, and one substitution variable: `{{CORRECT_PATH}}` (`prompt-template/block/wrong-summary-location.md:1`, `prompt-template/block/wrong-summary-location.md:3`, `prompt-template/block/wrong-summary-location.md:5`). It does not execute logic itself; its algorithmic behavior is supplied by `load_and_render_safe`, which replaces `{{CORRECT_PATH}}` with the active loop’s current-round summary path and preserves fallback behavior if the template cannot be loaded (`hooks/lib/template-loader.sh:170`, `hooks/lib/template-loader.sh:189`). The production gate is in `hooks/loop-write-validator.sh`: if `FILE_PATH` is a `round-N-summary.md` but is not inside `.humanize/rlcr`, it computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"`, renders this template, writes it to stderr, and exits `2` (`hooks/loop-write-validator.sh:155`, `hooks/loop-write-validator.sh:156`, `hooks/loop-write-validator.sh:160`, `hooks/loop-write-validator.sh:162`).
- inputs_outputs_state: Input is a single render variable, `CORRECT_PATH`, supplied by the Write validator. Output is Markdown block text beginning with `# Wrong Summary Location` and including the correct path line. The template has no persistent state. Runtime state comes from the active loop discovery and parsed current round: `ACTIVE_LOOP_DIR` is found via `find_active_loop`, `CURRENT_ROUND` comes from `parse_state_file`, and the rendered message is emitted only during a hook block (`hooks/loop-write-validator.sh:79`, `hooks/loop-write-validator.sh:82`, `hooks/loop-write-validator.sh:96`, `hooks/loop-write-validator.sh:97`).
- gates_or_invariants: The invariant is that summary files must live in the loop directory, specifically the active loop directory for the current round. This gate applies to Write-tool attempts for round summary files outside `.humanize/rlcr`; it is distinct from the wrong-round gate and wrong-directory gate that run after a file is inside the loop path (`hooks/loop-write-validator.sh:151`, `hooks/loop-write-validator.sh:177`, `hooks/loop-write-validator.sh:201`). The template’s placeholder must remain uppercase and well-formed so comprehensive template syntax validation passes (`tests/test-templates-comprehensive.sh:122`, `tests/test-templates-comprehensive.sh:165`).
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh` through `load_and_render_safe` (`hooks/loop-write-validator.sh:160`). Rendering depends on `hooks/lib/template-loader.sh`. The template is covered by the template inventory and syntax suites: `test-template-references.sh` scans direct hook references and fails missing templates (`tests/test-template-references.sh:93`, `tests/test-template-references.sh:102`), while `test-templates-comprehensive.sh` loads all templates and validates placeholder syntax (`tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:118`). The broader summary-file behavior is also covered by validator allowlist and Bash-pattern suites for summary file names (`tests/test-allowlist-validators.sh:172`, `tests/test-allowlist-validators.sh:271`, `tests/test-bash-validator-patterns.sh:186`).
- edge_cases_or_failure_modes: If this file is missing or empty, `load_and_render_safe` falls back to an inline message in the Write validator, so the hook still blocks but the canonical template wording is lost (`hooks/loop-write-validator.sh:157`, `hooks/lib/template-loader.sh:179`). If `CORRECT_PATH` is not supplied, the literal placeholder would remain because rendering preserves missing variables (`hooks/lib/template-loader.sh:119`). If a summary path is inside `.humanize/rlcr` but in the wrong timestamped loop or wrong round, this template is not used; later wrong-round or wrong-directory templates handle those cases (`hooks/loop-write-validator.sh:181`, `hooks/loop-write-validator.sh:207`).
- validation_or_tests: Template existence is validated by `tests/test-template-references.sh` through the direct `load_and_render_safe` reference in `loop-write-validator.sh`. Template loadability and syntax are validated by `tests/test-templates-comprehensive.sh`, which loads all Markdown templates and checks uppercase placeholder syntax. Specific summary-file path behavior is exercised indirectly by write/read/edit allowlist tests and Bash summary modification tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 Item Evidence headings matched the 3 assigned rows; each assigned row is represented once.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`