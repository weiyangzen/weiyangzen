# agent_25 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-025 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable test aggregator for the Humanize/RLCR hook system. It is not the implementation algorithm itself, but it is the top-level validation harness that defines which behavior suites must pass for this branch, including the branch-specific finalize and cancel-signal coverage. The file is executable and declares usage plus success/failure exit semantics at `tests/run-all-tests.sh:5` and `tests/run-all-tests.sh:7`.
- algorithmic_behavior: Initializes counters and failed-suite storage, iterates over an ordered `TEST_SUITES` array, executes each existing suite from the local `tests/` directory, captures its combined stdout/stderr, strips ANSI color escapes, extracts the final `Passed:` and `Failed:` numeric counts, aggregates totals, records failed suites, and exits nonzero if any suite failed. The ordered suite list is at `tests/run-all-tests.sh:32`, and this branch’s relevant additions are `test-finalize-phase.sh` and `test-cancel-signal-file.sh` at `tests/run-all-tests.sh:45` and `tests/run-all-tests.sh:46`.
- inputs_outputs_state: Input is the repository checkout and the executable test scripts named in `TEST_SUITES`; the script derives `SCRIPT_DIR` from its own location at `tests/run-all-tests.sh:14`, so callers can run it from any working directory. Output is human-readable colored progress plus a final total summary. State is held in process-local shell variables: `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` at `tests/run-all-tests.sh:28`. Exit state is `0` only when `FAILED_SUITES` remains empty; otherwise it exits `1` at `tests/run-all-tests.sh:93` through `tests/run-all-tests.sh:103`.
- gates_or_invariants: Missing suites are explicitly skipped instead of failing the whole run (`tests/run-all-tests.sh:52` through `tests/run-all-tests.sh:55`), which makes suite absence nonfatal. Suite failure is determined by either a nonzero suite exit code or parsed `Failed:` count greater than zero (`tests/run-all-tests.sh:75`). ANSI stripping must be portable across GNU/BSD sed; the script uses an ANSI-C `esc=$'\033'` and sed expression at `tests/run-all-tests.sh:67` through `tests/run-all-tests.sh:68`, and `tests/test-ansi-parsing.sh:38` through `tests/test-ansi-parsing.sh:47` specifically validates that parsing strategy.
- dependencies_and_callers: Depends on Bash, `sed`, `grep`, `tail`, executable child test scripts, and each suite’s own dependencies such as `git`, `jq`, and the hook scripts. The runner directly coordinates all first-level test suites in `tests/`: template loading, validator command-pattern checks, todo checking, plan-file validation, template reference validation, comprehensive template validation, hook behavior, error scenarios, ANSI parsing, allowlist validators, finalize phase, and cancel-signal authorization. The prompt-template reference test checks that `block/prompt-file-write.md` exists as a common template at `tests/test-template-references.sh:152` through `tests/test-template-references.sh:164`.
- edge_cases_or_failure_modes: Because `set -uo pipefail` is used but not global `set -e` at startup (`tests/run-all-tests.sh:12`), the runner can continue after controlled failures; it enables `set +e` around suite execution and then runs `set -e` after capturing the exit code (`tests/run-all-tests.sh:61` through `tests/run-all-tests.sh:64`). A suite that prints no parseable `Passed:` or `Failed:` lines contributes zero counts because the parse pipelines fall back to `echo "0"` (`tests/run-all-tests.sh:69` and `tests/run-all-tests.sh:70`). If a suite exits `0` but prints a nonzero failed count, the runner still fails it; if a suite exits nonzero but reports failed count `0`, the runner still fails it. On failure, only the last 30 output lines are shown (`tests/run-all-tests.sh:78` through `tests/run-all-tests.sh:79`), so earlier diagnostics can be hidden.
- validation_or_tests: The file is itself the validation entrypoint and includes explicit suites for this branch’s core state-machine behavior. `tests/test-finalize-phase.sh` validates `COMPLETE -> finalize-state.md`, finalize completion to `complete-state.md`, finalize-state detection, finalize summary write allowance, finalize state protection, no Codex invocation during finalize, max-iteration bypass, git-clean and todo-complete gates, and normal-round non-regression. `tests/test-cancel-signal-file.sh` validates the `.cancel-requested` authorization gate for moving active state to `cancel-state.md`, including many bypass and command-injection forms. I inspected the runner and referenced suites; I did not execute the full runner during this research pass.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-055 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the invariant that round prompt files are read-only instruction inputs. It is part of the hook-enforced algorithmic contract separating Codex-authored instructions from Claude-authored execution summaries. The core prohibition is stated at `prompt-template/block/prompt-file-write.md:3`.
- algorithmic_behavior: When a validator detects an attempted write/edit/bash modification to `round-N-prompt.md`, this template supplies the block reason. The message tells the agent that prompt files contain instructions from Codex to Claude, cannot be modified by the recipient, and that the correct workflow is to read the current prompt, execute it, and write results to the summary file (`prompt-template/block/prompt-file-write.md:5` through `prompt-template/block/prompt-file-write.md:10`). It also defines the error-reporting transition: if the prompt is wrong, document that in the summary rather than changing the prompt (`prompt-template/block/prompt-file-write.md:12`).
- inputs_outputs_state: Input is the validator’s decision that a target path matches the protected prompt-file pattern. The template has no dynamic placeholders, so rendering is static. Output is a Markdown block reason. It does not mutate state; it constrains allowed state transitions by preventing agent-authored edits to `round-*-prompt.md` and redirecting any correction request into `round-N-summary.md`.
- gates_or_invariants: The invariant is “prompt files are instructions, not work output.” Hook code enforces it in two layers: `prompt_write_blocked_message()` loads this template with an inline fallback at `hooks/lib/loop-common.sh:201` through `hooks/lib/loop-common.sh:208`, and the Bash validator blocks commands matching `round-[0-9]+-prompt\.md` at `hooks/loop-bash-validator.sh:308` through `hooks/loop-bash-validator.sh:316`. The same shared message function is referenced by write/edit validators as shown by `hooks/loop-write-validator.sh:48` and `hooks/loop-edit-validator.sh:47`.
- dependencies_and_callers: Depends on `load_and_render_safe` through `prompt_write_blocked_message()`. Its direct caller is the common helper in `hooks/lib/loop-common.sh`; validator callers include Bash, Write, and Edit pre-tool hooks. Template existence is part of the template-reference validation list at `tests/test-template-references.sh:152` through `tests/test-template-references.sh:164`, ensuring a missing file does not produce an empty block message.
- edge_cases_or_failure_modes: The template itself is intentionally static, so malformed placeholder substitution is not a concern. If the template is missing or unreadable, the common helper falls back to an inline message with the same essential rule (`hooks/lib/loop-common.sh:203` through `hooks/lib/loop-common.sh:205`). A notable contract edge is that the block only forbids `round-*-prompt.md` writes; the correct writable sink for findings is the summary file, which other validators may independently gate by round and tool path.
- validation_or_tests: `tests/test-template-references.sh` checks that `block/prompt-file-write.md` exists in the common template set. `tests/run-all-tests.sh` includes that reference suite at `tests/run-all-tests.sh:38`, plus validator suites that exercise protected file modification detection. The Bash validator path has a concrete prompt-file modification gate at `hooks/loop-bash-validator.sh:313` through `hooks/loop-bash-validator.sh:315`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2; each assigned item appears once as an Item Evidence heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`