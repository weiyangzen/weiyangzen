# agent_22 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell utility for observing an RLCR loop. The file defines `_humanize_monitor_codex` and exposes it through `humanize monitor rlcr-loop`; it is meant to be sourced from a shell profile, not executed as the loop scheduler. README documents this exact source-and-run flow at `README.md:90` and `README.md:103`.
- algorithmic_behavior: The monitor treats `.humanize/rlcr` as the local session index (`scripts/humanize.sh:10`). It selects the latest timestamp-like session directory with lexicographic comparison against names matching `YYYY-MM-DD_HH-MM-SS` (`scripts/humanize.sh:23`, `scripts/humanize.sh:34`). It then maps the current project path to a sanitized cache key and searches `$HOME/.cache/humanize/<sanitized-project-path>/<session>/round-*-codex-run.log` for the highest session/round log (`scripts/humanize.sh:43`, `scripts/humanize.sh:49`, `scripts/humanize.sh:53`, `scripts/humanize.sh:75`, `scripts/humanize.sh:84`). The display loop renders a fixed top status area, tails the current log, polls every 0.5 seconds for appended bytes, and switches to newer logs or sessions when they appear (`scripts/humanize.sh:519`, `scripts/humanize.sh:600`, `scripts/humanize.sh:607`, `scripts/humanize.sh:621`, `scripts/humanize.sh:631`, `scripts/humanize.sh:638`, `scripts/humanize.sh:661`).
- inputs_outputs_state: Inputs are the current working directory, `.humanize/rlcr/<timestamp>/` session folders, active or terminal state files, optional `goal-tracker.md`, git status from the current repo, terminal dimensions, and cached Codex run logs. State parsing recognizes `state.md` as active and `<stop_reason>-state.md` as terminal, returning a pipe-delimited `state_file|loop_status` (`scripts/humanize.sh:98`, `scripts/humanize.sh:110`, `scripts/humanize.sh:116`, `scripts/humanize.sh:131`). Parsed state fields are `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` (`scripts/humanize.sh:138`, `scripts/humanize.sh:146`). Goal tracker parsing produces counts for ACs, active/completed/deferred tasks, open issues, and a goal summary (`scripts/humanize.sh:156`, `scripts/humanize.sh:177`, `scripts/humanize.sh:182`, `scripts/humanize.sh:212`, `scripts/humanize.sh:222`, `scripts/humanize.sh:226`, `scripts/humanize.sh:230`). Git parsing emits modified/added/deleted/untracked counts plus insertion/deletion counts (`scripts/humanize.sh:240`, `scripts/humanize.sh:249`, `scripts/humanize.sh:273`). Outputs are terminal-only: ANSI dashboard lines, tail content, switch notices, usage text, and return codes for invalid usage or missing sessions. The script does not write RLCR state.
- gates_or_invariants: Hard precondition: `.humanize/rlcr` must exist or the monitor returns `1` (`scripts/humanize.sh:16`). A valid monitor session must have at least one timestamp-shaped session directory or it returns `1` (`scripts/humanize.sh:493`). Session selection is constrained to timestamp directory names (`scripts/humanize.sh:34`, `scripts/humanize.sh:65`). Active state has priority over terminal state files (`scripts/humanize.sh:110`). Terminal status color mapping recognizes `completed`, `failed`, `error`, `timeout`, and `cancelled`; the branch under research is relevant because a renamed `cancel-state.md` becomes status `cancel` from filename extraction, while `cancelled` is the color case currently listed (`scripts/humanize.sh:116`, `scripts/humanize.sh:125`, `scripts/humanize.sh:395`). The public dispatcher only accepts `humanize monitor rlcr-loop` (`scripts/humanize.sh:681`, `scripts/humanize.sh:686`, `scripts/humanize.sh:690`, `scripts/humanize.sh:706`).
- dependencies_and_callers: Depends on shell builtins plus `basename`, `sed`, `grep`, `sort`, `wc`, `git`, `tput`, `stat`, `tail`, `seq`, and terminal ANSI behavior. It intentionally mirrors cache-path sanitization from `hooks/loop-codex-stop-hook.sh`, noted in comments (`scripts/humanize.sh:51`). README instructs users to source `scripts/humanize.sh` and run `humanize monitor rlcr-loop` (`README.md:90`, `README.md:103`). Cancel semantics come from `commands/cancel-rlcr-loop.md`, which renames `state.md` or `finalize-state.md` to `cancel-state.md` after writing `.cancel-requested` (`commands/cancel-rlcr-loop.md:28`, `commands/cancel-rlcr-loop.md:34`, `commands/cancel-rlcr-loop.md:43`). README lists terminal state files including `cancel-state.md` (`README.md:255`).
- edge_cases_or_failure_modes: Missing `.humanize/rlcr` or no timestamp session produces an immediate user-facing error (`scripts/humanize.sh:16`, `scripts/humanize.sh:493`). Missing state file returns `unknown` fields/status rather than failing (`scripts/humanize.sh:103`, `scripts/humanize.sh:141`). Missing goal tracker reports zero counts and "No goal tracker" (`scripts/humanize.sh:160`). Non-git directories are allowed and displayed as zero git changes (`scripts/humanize.sh:243`). Log files are selected from the cache only if their session timestamp also exists locally, so orphan cache logs are ignored (`scripts/humanize.sh:57`, `scripts/humanize.sh:69`). A current log truncation or rotation is not explicitly handled: if file size drops below `last_size`, the incremental branch never resets until a newer log/session appears (`scripts/humanize.sh:621`). The monitor defines a `tail_pid` cleanup path but uses foreground `tail` calls rather than a persistent background tail in this version (`scripts/humanize.sh:461`, `scripts/humanize.sh:475`, `scripts/humanize.sh:605`, `scripts/humanize.sh:626`). Terminal cleanup is trap-driven for `INT`/`TERM`, but normal fallthrough from the main loop does not call `_cleanup`; the loop is effectively intended to end by signal (`scripts/humanize.sh:465`, `scripts/humanize.sh:486`, `scripts/humanize.sh:677`).
- validation_or_tests: No direct unit test targets `scripts/humanize.sh` or `_humanize_monitor_codex` by name in the inspected test set. Related validation exists around state naming and cancel/finalize semantics: `tests/run-all-tests.sh` includes `test-state-exit-naming.sh`, `test-finalize-phase.sh`, and `test-cancel-signal-file.sh` (`tests/run-all-tests.sh:39`, `tests/run-all-tests.sh:45`, `tests/run-all-tests.sh:46`). Those suites validate the state-file lifecycle that this monitor reads, but not terminal rendering, cache log selection, session switching, or status color mapping.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-052 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Prompt/block template used by the RLCR stop hook when an attempted loop stop is blocked because changed code or documentation files exceed the configured line-count threshold. It is core to exit-gate messaging rather than state mutation.
- algorithmic_behavior: The template itself is declarative Markdown with two placeholders: `{{MAX_LINES}}` and `{{LARGE_FILES}}` (`prompt-template/block/large-files.md:3`, `prompt-template/block/large-files.md:4`). It tells the agent that stopping is blocked, explains maintainability risk, then gives separate remediation contracts for code files and documentation files (`prompt-template/block/large-files.md:6`, `prompt-template/block/large-files.md:13`, `prompt-template/block/large-files.md:19`). The executable gate lives in `hooks/loop-codex-stop-hook.sh`: it sets `MAX_LINES=2000`, walks cached `git status --porcelain` output, extracts changed filenames, normalizes rename targets, skips deleted/nonexistent files, classifies extensions as code or documentation, counts lines with `wc -l`, and appends any over-limit file to `LARGE_FILES` (`hooks/loop-codex-stop-hook.sh:346`, `hooks/loop-codex-stop-hook.sh:351`, `hooks/loop-codex-stop-hook.sh:357`, `hooks/loop-codex-stop-hook.sh:360`, `hooks/loop-codex-stop-hook.sh:365`, `hooks/loop-codex-stop-hook.sh:375`, `hooks/loop-codex-stop-hook.sh:387`, `hooks/loop-codex-stop-hook.sh:393`). If any large files are found, the hook renders this template and emits a JSON block decision (`hooks/loop-codex-stop-hook.sh:399`, `hooks/loop-codex-stop-hook.sh:407`, `hooks/loop-codex-stop-hook.sh:411`).
- inputs_outputs_state: Template inputs are `MAX_LINES`, currently `2000`, and `LARGE_FILES`, a newline-prefixed bullet list of paths, line counts, and file type labels generated by the stop hook (`hooks/loop-codex-stop-hook.sh:346`, `hooks/loop-codex-stop-hook.sh:394`). Rendering is performed through `load_and_render_safe`, whose single-pass template engine replaces supplied placeholders and leaves missing variables intact (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`, `hooks/lib/template-loader.sh:120`, `hooks/lib/template-loader.sh:170`). Output is the `reason` field in a Claude hook JSON response with `"decision": "block"` and a concise `systemMessage` (`hooks/loop-codex-stop-hook.sh:411`, `hooks/loop-codex-stop-hook.sh:415`). No persistent state is updated by the template itself.
- gates_or_invariants: The large-file gate only runs when the project is recognized as a git repo and cached git status was obtained successfully (`hooks/loop-codex-stop-hook.sh:320`, `hooks/loop-codex-stop-hook.sh:348`). It evaluates only changed tracked/new files from `git status --porcelain`, not the entire repository (`hooks/loop-codex-stop-hook.sh:323`, `hooks/loop-codex-stop-hook.sh:351`). It only considers known code extensions and documentation extensions, skipping other file types (`hooks/loop-codex-stop-hook.sh:375`, `hooks/loop-codex-stop-hook.sh:379`, `hooks/loop-codex-stop-hook.sh:382`). The invariant enforced by the prompt is "each split file below `MAX_LINES` while preserving behavior or narrative continuity" (`prompt-template/block/large-files.md:14`, `prompt-template/block/large-files.md:15`, `prompt-template/block/large-files.md:20`, `prompt-template/block/large-files.md:21`). If the template is missing or empty, the hook has an inline fallback so the gate still blocks with actionable text (`hooks/loop-codex-stop-hook.sh:400`, `hooks/lib/template-loader.sh:179`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` (`hooks/loop-codex-stop-hook.sh:407`). The template loader resolves and renders files from `prompt-template/` (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:170`). Template-reference tests scan `loop-codex-stop-hook.sh` and verify referenced templates exist (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:83`, `tests/test-template-references.sh:102`). Comprehensive template tests load every Markdown template and validate placeholder syntax (`tests/test-templates-comprehensive.sh:83`, `tests/test-templates-comprehensive.sh:118`).
- edge_cases_or_failure_modes: Because the file list comes from `git status --porcelain`, unchanged pre-existing large files are not blocked by this gate. Filenames with porcelain quoting, unusual whitespace, or newlines may be parsed imperfectly because extraction uses `${line#???}` and simple rename string handling (`hooks/loop-codex-stop-hook.sh:357`, `hooks/loop-codex-stop-hook.sh:360`). Extensionless code, generated files, notebooks, JSON/YAML, and other large structured assets are skipped by extension classification (`hooks/loop-codex-stop-hook.sh:382`). The wording says "trying to stop," so if the hook blocks during a finalize or cancel-adjacent stop attempt, the template still presents this as an exit-stop remediation rather than a scheduler-level validation failure. The optional `code-simplifier` instruction is advisory and not validated by the hook (`prompt-template/block/large-files.md:16`).
- validation_or_tests: Template existence and references are covered by `tests/test-template-references.sh`, which scans hook calls and fails on missing referenced templates (`tests/test-template-references.sh:83`, `tests/test-template-references.sh:102`, `tests/test-template-references.sh:221`). Generic template loading, fallback, and rendering behavior is covered by `tests/test-template-loader.sh` (`tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:197`, `tests/test-template-loader.sh:211`). Syntax/load coverage for all templates is covered by `tests/test-templates-comprehensive.sh` (`tests/test-templates-comprehensive.sh:87`, `tests/test-templates-comprehensive.sh:118`). I did not find a dedicated test that constructs an over-2000-line changed file and asserts this specific block JSON path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 section headings present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`