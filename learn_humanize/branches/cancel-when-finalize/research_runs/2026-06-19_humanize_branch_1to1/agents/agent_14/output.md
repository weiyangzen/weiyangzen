# agent_14 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-014 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that short-circuits the RLCR/Codex review path when Claude still has unfinished TodoWrite items. It is invoked by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:258) before git-clean checks and before expensive Codex review.
- algorithmic_behavior: Reads hook input JSON from stdin, extracts `transcript_path`, scans the transcript JSONL in order, remembers only the most recent non-empty TodoWrite todo list, then rejects exit when any latest todo status is not exactly `completed`. The transcript scanner supports three observed shapes: Claude assistant message content blocks, alternative `type: message` content blocks, and direct `type: tool_use` entries in [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/check-todos-from-transcript.py:51), [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/check-todos-from-transcript.py:65), and [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/check-todos-from-transcript.py:78). Invalid JSONL transcript rows are ignored, but invalid hook input JSON is fatal to this helper.
- inputs_outputs_state: Input is a stdin JSON object with optional `transcript_path`; path is expanded with `~` at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/check-todos-from-transcript.py:103). Internal state is `latest_todos`, overwritten only when a TodoWrite call has a non-empty `todos` list. Output is exit code plus optional text: `0` means no blocking todos, no transcript, no path, or all latest todos completed; `1` prints `INCOMPLETE_TODOS` plus `  - [status] content` lines; `2` prints `PARSE_ERROR` to stderr for malformed hook input.
- gates_or_invariants: Gate invariant is strict completion: every latest todo must have `status == "completed"` at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/check-todos-from-transcript.py:113). The stop hook treats exit `1` as a block and renders `block/incomplete-todos.md` or fallback with the incomplete list at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:284). Exit `2` is also a block, surfaced as a todo-checker parse error at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:265). Missing checker file disables this quick check because invocation is guarded by `[[ -f "$TODO_CHECKER" ]]`.
- dependencies_and_callers: Depends only on Python stdlib `json`, `sys`, and `pathlib.Path`. Called by `hooks/loop-codex-stop-hook.sh`, which passes the original hook input through `python3 "$TODO_CHECKER"` and captures stdout/stderr together at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:262). The helper output contract is consumed by `tail -n +2`, so the first stdout line must remain the marker when incomplete todos exist.
- edge_cases_or_failure_modes: Nonexistent transcript returns no todos and allows exit, so a bad but syntactically valid transcript path is fail-open. Empty latest TodoWrite `todos: []` does not overwrite prior latest state, so an earlier non-empty TodoWrite remains authoritative. Missing status is incomplete because default status is `""`. Unknown status values, `pending`, and `in_progress` all block. Malformed transcript lines are skipped silently. If transcript content is not a list, it is ignored. If a TodoWrite input has missing or non-list `todos`, the code assumes the object interface exists enough for `.get`; unusual non-dict `input` values could raise an exception because only JSON decode errors are caught inside transcript scanning.
- validation_or_tests: [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-todo-checker.sh:51) covers invalid stdin JSON, empty stdin, missing `transcript_path`, nonexistent transcript, completed vs incomplete todos, `in_progress`, empty transcripts, invalid JSONL rows, latest TodoWrite selection, direct tool-use format, alternative message format, missing status, empty content, and unicode content. The stop-hook integration path also references this checker directly.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-044 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block template appended to the git-not-clean stop-hook block when untracked files other than `.humanize*` are present. It shapes the human/agent remediation contract for a dirty git worktree gate.
- algorithmic_behavior: The template itself is static Markdown, but it participates in an algorithmic transition in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:450): git status porcelain lines beginning with `??` are split into `.humanize*` local state versus other untracked paths. If `OTHER_UNTRACKED` is non-empty, this template is loaded and concatenated into `SPECIAL_NOTES`, which is then rendered into the broader `block/git-not-clean.md` reason at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:470).
- inputs_outputs_state: Input is not passed through template variables; the triggering input is cached `git status --porcelain` output. Output is guidance text advising that untracked build artifacts, dependency directories, IDE files, logs, caches, and temporary files usually belong in `.gitignore` rather than commits. It does not mutate state directly; it affects the stop-hook JSON `reason` text shown while the dirty-worktree gate blocks exit.
- gates_or_invariants: Supports the invariant that the RLCR loop should not exit/review while the repository has uncommitted changes. The untracked note refines the gate by distinguishing files that should probably be ignored from files that should be committed. If this template is missing or empty, the stop hook falls back to `Review untracked files - add to .gitignore or commit them.` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:454), so the block remains fail-closed.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` from the Codex stop hook. It depends on the git-clean gate having already cached status and filtered untracked files. It coordinates with sibling templates `block/git-not-clean-humanize-local.md` and `block/git-not-clean.md`: this file is only a special note, not the main blocking message.
- edge_cases_or_failure_modes: Since no actual untracked filenames are interpolated here, the user must rely on the main git status context or rerun git status to see exact paths. The heuristic treats any non-`.humanize` untracked path as possibly needing ignore-or-commit review, including source files intentionally created but not staged. Matching `.humanize` is broad and can classify any untracked path containing that substring as local humanize state. If the template is present but misleading for a legitimate new source file, the hard gate still correctly requires the user/agent to either commit it or ignore it.
- validation_or_tests: Reference discovery tests scan template loader calls, but the visible `COMMON_TEMPLATES` list in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-template-references.sh:152) does not explicitly list this block. The stop-hook has a fallback branch for missing templates, which is indirectly protected by template-loader fallback tests rather than a dedicated assertion for this exact file.
- skip_candidate: `yes: static prompt fragment, but still algorithmically relevant because it is part of the git-clean exit gate remediation surface`

### CANCEL_WHEN_FINALIZE-HZ-074 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section that transfers responsibility for post-Round-0 `goal-tracker.md` updates from Claude to Codex/the reviewer lane. It defines the state-transition contract for mutable goal tracker sections while preserving immutable ultimate-goal and acceptance-criteria content.
- algorithmic_behavior: The template instructs Codex to inspect Claude’s summary for a "Goal Tracker Update Request", evaluate whether the requested change is justified and serves the Ultimate Goal, then either update `@{{GOAL_TRACKER_FILE}}` or reject the request with reasoning. Approved state transitions include moving tasks among Active, Completed/Verified, Deferred, adding Plan Evolution Log entries, and adding Open Issues. It explicitly forbids modifying the immutable section at [prompt-template/codex/goal-tracker-update-section.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/prompt-template/codex/goal-tracker-update-section.md:10).
- inputs_outputs_state: Input variable is `GOAL_TRACKER_FILE`, rendered by the stop hook at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:680). Additional semantic input is Claude’s round summary, especially any update-request section. Output is not a machine-readable artifact from this template alone; it becomes instructions embedded in generated Codex review prompts. The resulting state change, if approved, is a direct update to `goal-tracker.md` by the reviewer lane rather than by Claude after Round 0.
- gates_or_invariants: Enforces several invariants: Claude cannot directly modify `goal-tracker.md` after Round 0; Codex must evaluate rather than blindly accept update requests; immutable Ultimate Goal and Acceptance Criteria cannot be changed; deferrals need strong justification. The parent review prompts reinforce completion gates by requiring no unjustified deferrals or pending work before `COMPLETE`, for example [prompt-template/codex/regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/prompt-template/codex/regular-review.md:47) and [prompt-template/codex/full-alignment-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/prompt-template/codex/full-alignment-review.md:57).
- dependencies_and_callers: Loaded through `load_and_render_safe` in the Codex stop hook with a fallback `## Goal Tracker Updates` section. The rendered section is inserted into both full-alignment and regular review prompt paths at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:720) and [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-codex-stop-hook.sh:736). It coordinates with write/bash validators that block Claude-side goal tracker modifications after Round 0, making this section the sanctioned update route.
- edge_cases_or_failure_modes: The contract is prompt-enforced, not parsed or structurally validated by this file. If Claude omits a formal update request, Codex may not update stale tracker state unless other review instructions catch it. If Codex updates the wrong section, the template itself cannot prevent it beyond explicit instruction. `GOAL_TRACKER_FILE` rendering must be correct; the fallback still points to the same variable but is less detailed and omits the immutable-section warning. Deferrals are subjective because "strong justification" is not machine-checkable here.
- validation_or_tests: [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-templates-comprehensive.sh:531) renders this template with `.humanize/rlcr/20240101/goal-tracker.md` and asserts the output contains both `Goal Tracker Update Requests` and the rendered path. Parent prompt templates include `{{GOAL_TRACKER_UPDATE_SECTION}}` placeholders in regular and full alignment review templates.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 section headings present, one per assigned row; no extra assigned item sections`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`