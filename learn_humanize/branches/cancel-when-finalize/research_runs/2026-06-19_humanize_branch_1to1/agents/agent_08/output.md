# agent_08 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-008 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared hook library layer for the RLCR loop. The directory contains `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`, which provide state parsing, active-loop discovery, path classification, guarded cancel authorization, loop termination naming, and template rendering used by the validator and stop-hook scripts.
- algorithmic_behavior: `loop-common.sh` defines loop state constants and exit reason constants at `hooks/lib/loop-common.sh:15` through `hooks/lib/loop-common.sh:40`, loads `template-loader.sh` at `hooks/lib/loop-common.sh:46` through `hooks/lib/loop-common.sh:51`, and warns if the template tree is invalid at `hooks/lib/loop-common.sh:53` through `hooks/lib/loop-common.sh:56`. `find_active_loop` selects only the newest loop directory and treats `state.md` or `finalize-state.md` as active indicators, preventing older completed loop directories from being revived (`hooks/lib/loop-common.sh:58` through `hooks/lib/loop-common.sh:81`). `parse_state_file` reads YAML-like frontmatter into `STATE_*` globals and defaults round/max/push values (`hooks/lib/loop-common.sh:98` through `hooks/lib/loop-common.sh:139`). Path and file classifiers identify round files, state/finalize files, summaries, goal tracker paths, and loop directory membership (`hooks/lib/loop-common.sh:141` through `hooks/lib/loop-common.sh:188`, `hooks/lib/loop-common.sh:250` through `hooks/lib/loop-common.sh:272`, `hooks/lib/loop-common.sh:415` through `hooks/lib/loop-common.sh:419`). `is_cancel_authorized` is a command validator for the cancel transition and requires a `.cancel-requested` signal plus an exact `mv state.md|finalize-state.md cancel-state.md` shape after normalization (`hooks/lib/loop-common.sh:274` through `hooks/lib/loop-common.sh:413`). `command_modifies_file` detects shell write patterns such as redirection, `tee`, in-place editors, `mv`, `cp`, `rm`, `dd`, `truncate`, and `exec` redirection (`hooks/lib/loop-common.sh:421` through `hooks/lib/loop-common.sh:449`). `end_loop` validates allowed terminal reasons and renames `state.md` to a reason-specific state file (`hooks/lib/loop-common.sh:465` through `hooks/lib/loop-common.sh:498`).
- inputs_outputs_state: Inputs are hook JSON interpreted by caller scripts, loop directories under `.humanize/rlcr`, frontmatter fields in `state.md` or `finalize-state.md`, file paths, shell command strings, and template variables. Outputs are boolean shell return codes, parsed globals such as `STATE_CURRENT_ROUND`, rendered block messages, active-loop path strings, and renamed terminal state files like `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`. `template-loader.sh` receives a template root, template name, fallback text, and `VAR=value` pairs, then outputs rendered markdown or an empty string for missing non-safe loads (`hooks/lib/template-loader.sh:24` through `hooks/lib/template-loader.sh:48`, `hooks/lib/template-loader.sh:134` through `hooks/lib/template-loader.sh:203`).
- gates_or_invariants: The active-loop invariant is newest directory only, with active marker restricted to `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:71` through `hooks/lib/loop-common.sh:80`). Cancel is gated by `.cancel-requested`, rejects command substitution/backticks, newlines, shell operators, leftover `$`, non-`mv` commands, extra args, and wrong source/destination paths (`hooks/lib/loop-common.sh:288` through `hooks/lib/loop-common.sh:410`). Template rendering is single-pass to prevent variable-value placeholder injection (`hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:132`). `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205` through `hooks/lib/template-loader.sh:222`). `end_loop` accepts only the five known reasons and fails on missing state file (`hooks/lib/loop-common.sh:472` through `hooks/lib/loop-common.sh:497`).
- dependencies_and_callers: Registered hooks in `hooks/hooks.json` call `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, `loop-bash-validator.sh`, and `loop-codex-stop-hook.sh` (`hooks/hooks.json:4` through `hooks/hooks.json:63`). Those scripts source `hooks/lib/loop-common.sh`; examples include read validation at `hooks/loop-read-validator.sh:14` through `hooks/loop-read-validator.sh:17`, write validation at `hooks/loop-write-validator.sh:15` through `hooks/loop-write-validator.sh:17`, edit validation at `hooks/loop-edit-validator.sh:14` through `hooks/loop-edit-validator.sh:16`, bash validation at `hooks/loop-bash-validator.sh:14` through `hooks/loop-bash-validator.sh:16`, and plan validation at `hooks/loop-plan-file-validator.sh:16` through `hooks/loop-plan-file-validator.sh:21`. Stop-hook use includes todo checking, prompt rendering, finalize-phase prompting, next-round prompt construction, and `end_loop` on stop/max iteration paths (`hooks/loop-codex-stop-hook.sh:252` through `hooks/loop-codex-stop-hook.sh:307`, `hooks/loop-codex-stop-hook.sh:677` through `hooks/loop-codex-stop-hook.sh:681`, `hooks/loop-codex-stop-hook.sh:1005` through `hooks/loop-codex-stop-hook.sh:1020`, `hooks/loop-codex-stop-hook.sh:1049`, `hooks/loop-codex-stop-hook.sh:1075` through `hooks/loop-codex-stop-hook.sh:1108`).
- edge_cases_or_failure_modes: `find_active_loop` intentionally ignores older loop directories even if they contain active markers, so a newer malformed or completed directory can shadow older state. State parsing is line-oriented and frontmatter-only; missing fields become empty except selected defaults (`hooks/lib/loop-common.sh:119` through `hooks/lib/loop-common.sh:137`). `command_modifies_file` has a known limitation in tests for multi-source `cp` patterns (`tests/test-bash-validator-patterns.sh:165` through `tests/test-bash-validator-patterns.sh:168`). `is_cancel_authorized` normalizes `./` and repeated slashes but lowercases the command before comparison through callers, so case-sensitive filesystem paths could be affected if mixed case is used in loop paths. `load_template` prints a warning for missing templates, while safe loading suppresses that and falls back (`hooks/lib/template-loader.sh:36` through `hooks/lib/template-loader.sh:48`, `hooks/lib/template-loader.sh:170` through `hooks/lib/template-loader.sh:203`).
- validation_or_tests: Direct validation surfaces include `tests/test-template-loader.sh` for `get_template_dir`, `load_template`, rendering, safe fallback, and directory validation (`tests/test-template-loader.sh:41` through `tests/test-template-loader.sh:240`); `tests/test-bash-validator-patterns.sh` for `command_modifies_file` positive/negative cases (`tests/test-bash-validator-patterns.sh:36` through `tests/test-bash-validator-patterns.sh:194`); `tests/test-state-exit-naming.sh` for active loop detection and `end_loop` behavior (`tests/test-state-exit-naming.sh:64` through `tests/test-state-exit-naming.sh:239`); `tests/test-cancel-signal-file.sh` for `is_cancel_authorized` helper behavior (`tests/test-cancel-signal-file.sh:1249` through `tests/test-cancel-signal-file.sh:1340`); and `tests/test-template-references.sh` for referenced message templates and safe rendering usage (`tests/test-template-references.sh:149` through `tests/test-template-references.sh:200`). I did not execute tests because this assignment requested read-only research notes only.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-038 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook preflight that blocks loop exit/final review when the latest Claude `TodoWrite` state still has unfinished todos.
- algorithmic_behavior: The test sets strict shell mode, locates the project root and Python checker (`tests/test-todo-checker.sh:9` through `tests/test-todo-checker.sh:13`), creates an isolated temp directory with cleanup (`tests/test-todo-checker.sh:36` through `tests/test-todo-checker.sh:38`), and exercises the checker by piping hook JSON into `python3 "$TODO_CHECKER"`. Test Group 1 validates hook-input parsing behavior: invalid JSON and empty input must exit 2, missing `transcript_path` exits 0, and nonexistent transcript path exits 0 (`tests/test-todo-checker.sh:45` through `tests/test-todo-checker.sh:97`). Test Group 2 specifies todo status semantics: all `completed` exits 0, `pending` exits 1, output includes incomplete task content, and `in_progress` exits 1 (`tests/test-todo-checker.sh:99` through `tests/test-todo-checker.sh:157`). Test Group 3 specifies transcript parsing variants: empty transcript exits 0, invalid JSONL lines are ignored if a valid todo exists, multiple `TodoWrite` calls use the latest one, direct `tool_use` format is accepted, and alternative `type: message` format is accepted (`tests/test-todo-checker.sh:159` through `tests/test-todo-checker.sh:240`). Test Group 4 covers missing status as incomplete, empty content as still incomplete, and unicode content with completed status as allowed (`tests/test-todo-checker.sh:242` through `tests/test-todo-checker.sh:293`). Summary logic exits 0 only when no failures were recorded (`tests/test-todo-checker.sh:295` through `tests/test-todo-checker.sh:313`).
- inputs_outputs_state: Inputs are generated JSONL transcript fixture files and hook JSON of the form `{"transcript_path": "..."}`. Outputs are checker exit codes and stdout/stderr captured into `RESULT`; the test harness translates assertions into pass/fail counters. The checked Python behavior is defined in `hooks/check-todos-from-transcript.py`: `find_latest_todos` returns the most recent non-empty `todos` list from recognized transcript patterns (`hooks/check-todos-from-transcript.py:20` through `hooks/check-todos-from-transcript.py:87`), `main` exits 2 on hook JSON parse error, exits 0 when no transcript path or no todos exist, exits 1 with an `INCOMPLETE_TODOS` marker plus item details when any latest todo status is not `completed`, and exits 0 when all latest todos are completed (`hooks/check-todos-from-transcript.py:90` through `hooks/check-todos-from-transcript.py:129`).
- gates_or_invariants: The core invariant is latest `TodoWrite` wins, not any earlier todo state (`tests/test-todo-checker.sh:196` through `tests/test-todo-checker.sh:210`). A todo is complete only when `status` is exactly `completed`; missing, `pending`, and `in_progress` are incomplete (`tests/test-todo-checker.sh:121` through `tests/test-todo-checker.sh:157`, `tests/test-todo-checker.sh:249` through `tests/test-todo-checker.sh:263`). Invalid transcript lines are non-fatal, but invalid hook input is fatal to the checker with exit 2 (`tests/test-todo-checker.sh:51` through `tests/test-todo-checker.sh:73`, `tests/test-todo-checker.sh:179` through `tests/test-todo-checker.sh:194`).
- dependencies_and_callers: Depends on `python3`, `mktemp`, shell builtins, and `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:11` through `tests/test-todo-checker.sh:13`). The production caller is `hooks/loop-codex-stop-hook.sh`, which runs the checker before expensive Codex review, blocks on parse error, and blocks on incomplete todos by rendering `prompt-template/block/incomplete-todos.md` through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:252` through `hooks/loop-codex-stop-hook.sh:307`). The suite is included in aggregate test ordering in `tests/run-all-tests.sh:32` through `tests/run-all-tests.sh:47`.
- edge_cases_or_failure_modes: The checker ignores invalid JSONL transcript lines, so corrupted transcript fragments do not fail closed if a valid latest todo record is found (`tests/test-todo-checker.sh:179` through `tests/test-todo-checker.sh:194`). Nonexistent transcript paths and missing transcript paths exit 0, which avoids blocking when transcript data is unavailable but may allow exit without todo evidence (`tests/test-todo-checker.sh:75` through `tests/test-todo-checker.sh:97`). Empty `todos` lists are treated as no todos because `find_latest_todos` only updates latest when `todos` is truthy (`hooks/check-todos-from-transcript.py:61` through `hooks/check-todos-from-transcript.py:63`, `hooks/check-todos-from-transcript.py:73` through `hooks/check-todos-from-transcript.py:76`, `hooks/check-todos-from-transcript.py:83` through `hooks/check-todos-from-transcript.py:85`). Empty content is still reported incomplete because completion depends on status, not content (`tests/test-todo-checker.sh:265` through `tests/test-todo-checker.sh:278`).
- validation_or_tests: This file is itself the validation spec and would produce a pass/fail summary with exit 0 only if all 16 checks pass (`tests/test-todo-checker.sh:295` through `tests/test-todo-checker.sh:313`). I inspected it directly but did not run it because the task explicitly requested research notes only and no modifications.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-068 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing prompt template that defines the controlled request path for Goal Tracker changes after direct edits are blocked. It is a small policy/contract template rather than executable code, but it participates in the loop’s state-transition protocol for goal tracking.
- algorithmic_behavior: The template tells Claude to include a `Goal Tracker Update Request` section in its summary only if the Goal Tracker needs updates (`prompt-template/claude/goal-tracker-update-request.md:2` through `prompt-template/claude/goal-tracker-update-request.md:4`). It structures requested changes as bullet examples for marking tasks complete with evidence, adding open issues, documenting plan evolution, or deferring work with impact notes (`prompt-template/claude/goal-tracker-update-request.md:6` through `prompt-template/claude/goal-tracker-update-request.md:10`). It requires a justification explaining why changes serve the Ultimate Goal (`prompt-template/claude/goal-tracker-update-request.md:12` through `prompt-template/claude/goal-tracker-update-request.md:13`). It ends by assigning review authority to Codex, which updates the Goal Tracker only if justified (`prompt-template/claude/goal-tracker-update-request.md:16`).
- inputs_outputs_state: Input is the current round context and Claude’s decision that goal-tracker changes are needed; the template itself has no variables. Output is appended markdown in the next-round prompt and, later, an optional summary section authored by Claude. State transition is indirect: Claude requests Goal Tracker changes in a summary; Codex reviews the request; Codex may update `goal-tracker.md` if justified. This complements the write/edit gate that blocks direct Goal Tracker changes after round 0 (`hooks/loop-write-validator.sh:141` through `hooks/loop-write-validator.sh:148`, `hooks/loop-edit-validator.sh:110` through `hooks/loop-edit-validator.sh:117`).
- gates_or_invariants: Direct Goal Tracker modification is not the normal path after round 0; the allowed path is a request with evidence and justification. The template’s examples encode evidence requirements for completion, issue tracking, plan evolution, and deferrals (`prompt-template/claude/goal-tracker-update-request.md:6` through `prompt-template/claude/goal-tracker-update-request.md:13`). Codex remains the review gate (`prompt-template/claude/goal-tracker-update-request.md:16`).
- dependencies_and_callers: The template is loaded in `hooks/loop-codex-stop-hook.sh` when building the next-round prompt: `load_template "$TEMPLATE_DIR" "claude/goal-tracker-update-request.md"` at `hooks/loop-codex-stop-hook.sh:1103` through `hooks/loop-codex-stop-hook.sh:1108`. The same stop hook builds Codex review prompts with a separate Codex-side goal tracker update section, allowing Codex to apply justified requests (`hooks/loop-codex-stop-hook.sh:677` through `hooks/loop-codex-stop-hook.sh:681`). Template discovery and rendering depend on `hooks/lib/template-loader.sh`, whose `get_template_dir` resolves `prompt-template` relative to `hooks/lib` (`hooks/lib/template-loader.sh:24` through `hooks/lib/template-loader.sh:31`).
- edge_cases_or_failure_modes: If this template is missing, the stop hook falls back to a one-line instruction: `Include a Goal Tracker Update Request section in your summary if needed.` (`hooks/loop-codex-stop-hook.sh:1103` through `hooks/loop-codex-stop-hook.sh:1107`). Because the template has no placeholders, rendering errors are unlikely; the main risk is policy erosion if the template is absent or if Claude omits evidence/justification. The template does not itself validate requests; enforcement is in downstream Codex review and Goal Tracker write gates.
- validation_or_tests: Template reference tests scan hook/template references generally; `tests/test-template-references.sh` checks common templates and safe rendering usage for critical validators, though this exact Claude template is more directly exercised by `hooks/loop-codex-stop-hook.sh` prompt assembly (`tests/test-template-references.sh:149` through `tests/test-template-references.sh:200`, `hooks/loop-codex-stop-hook.sh:1103` through `hooks/loop-codex-stop-hook.sh:1108`). Comprehensive template tests also exercise template loading and rendering APIs used by this path (`tests/test-template-loader.sh:55` through `tests/test-template-loader.sh:78`, `tests/test-template-loader.sh:197` through `tests/test-template-loader.sh:222`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique Item Evidence headings, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`