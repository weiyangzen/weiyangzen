# agent_17 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook controller for the RLCR loop. It intercepts Claude Code Stop events, checks whether an RLCR loop is active, enforces pre-review safety gates, runs Codex review during normal rounds, and either blocks exit with next instructions or advances terminal loop state. It is registered as the Claude Stop hook in `hooks/hooks.json:52-58` with a 7200-second timeout. The file is core algorithm, not a wrapper, because it owns the normal-round to finalize-phase transition and the finalize-phase to complete-state transition.
- algorithmic_behavior: Reads the hook JSON payload from stdin into `HOOK_INPUT` at `hooks/loop-codex-stop-hook.sh:29`. It intentionally ignores `stop_hook_active`, documented at `hooks/loop-codex-stop-hook.sh:31-37`, so repeated blocked-stop iterations still run review. It locates the active loop under `${CLAUDE_PROJECT_DIR:-$(pwd)}/.humanize/rlcr` at `hooks/loop-codex-stop-hook.sh:43-59` using `find_active_loop`, which only treats the newest timestamp directory with `state.md` or `finalize-state.md` as active (`hooks/lib/loop-common.sh:58-81`). If no active loop exists, it exits 0 and allows Stop (`hooks/loop-codex-stop-hook.sh:61-64`).
  
  The hook detects normal versus Finalize Phase at `hooks/loop-codex-stop-hook.sh:66-82`: `state.md` means normal loop; `finalize-state.md` means post-Codex completion cleanup. It parses frontmatter through `parse_state_file` (`hooks/loop-codex-stop-hook.sh:88-100`; parser in `hooks/lib/loop-common.sh:98-139`) and maps `plan_tracked`, `start_branch`, `plan_file`, `current_round`, `max_iterations`, `push_every_round`, and Codex runtime settings into local variables.
  
  The pre-review gate chain is ordered and mostly fail-closed: model/effort YAML safety regexes at `hooks/loop-codex-stop-hook.sh:102-113`; numeric `current_round` and `max_iterations` handling at `hooks/loop-codex-stop-hook.sh:115-124`; required schema fields at `hooks/loop-codex-stop-hook.sh:127-143`; branch consistency through `git -C "$PROJECT_ROOT" rev-parse --abbrev-ref HEAD` at `hooks/loop-codex-stop-hook.sh:145-176`; plan backup/existence/integrity checks at `hooks/loop-codex-stop-hook.sh:178-250`; TodoWrite transcript completion check at `hooks/loop-codex-stop-hook.sh:252-307`; cached full-repo `git status --porcelain` at `hooks/loop-codex-stop-hook.sh:309-338`; large changed-file detection at `hooks/loop-codex-stop-hook.sh:340-421`; clean worktree and optional push gate at `hooks/loop-codex-stop-hook.sh:423-516`; summary-file existence at `hooks/loop-codex-stop-hook.sh:518-554`; Round 0 goal-tracker placeholder detection at `hooks/loop-codex-stop-hook.sh:556-630`; and max-iteration cutoff at `hooks/loop-codex-stop-hook.sh:632-643`.
  
  In Finalize Phase, after all pre-review gates pass, the hook skips Codex entirely and renames `finalize-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:645-657`). This is the branch’s key “cancel/complete when finalize” behavior: `finalize-state.md` remains an active loop state until the final summary, todo, git, branch, plan, and large-file gates pass.
  
  In normal phase, it builds a Codex review prompt at `hooks/loop-codex-stop-hook.sh:659-749`, choosing full-alignment review every fifth round (`CURRENT_ROUND % 5 == 4`) at `hooks/loop-codex-stop-hook.sh:683-687`. It then requires the `codex` command (`hooks/loop-codex-stop-hook.sh:755-775`), writes debug command/stdout/stderr paths under `$HOME/.cache/humanize/...` (`hooks/loop-codex-stop-hook.sh:779-833`), and runs `codex exec` with `--full-auto -C "$PROJECT_ROOT"` and configured model/effort (`hooks/loop-codex-stop-hook.sh:813-840`). Review output is expected at `round-N-review-result.md`; stdout is copied there as a fallback (`hooks/loop-codex-stop-hook.sh:897-913`), and missing/empty/nonzero Codex results block exit with debug file paths (`hooks/loop-codex-stop-hook.sh:850-950`).
  
  The Codex result parser reads the last non-empty line only and requires exact `COMPLETE` or `STOP` markers (`hooks/loop-codex-stop-hook.sh:952-960`). `COMPLETE` normally renames `state.md` to `finalize-state.md` and blocks with the finalize prompt (`hooks/loop-codex-stop-hook.sh:961-1021`). If `CURRENT_ROUND >= MAX_ITERATIONS`, `COMPLETE` is downgraded to max-iteration termination instead of entering Finalize Phase (`hooks/loop-codex-stop-hook.sh:963-968`). `STOP` invokes `end_loop ... stop` and exits (`hooks/loop-codex-stop-hook.sh:1023-1050`). Any other review content increments `current_round`, writes next-round prompt files, and blocks with Codex feedback (`hooks/loop-codex-stop-hook.sh:1053-1123`).
- inputs_outputs_state: Inputs are stdin hook JSON, optional `transcript_path` inside that JSON, environment `CLAUDE_PROJECT_DIR`, active loop files under `.humanize/rlcr/<timestamp>/`, current git branch/status, plan file and loop backup, Codex config fields, prompt templates, and the Codex CLI. Important loop-state inputs are `state.md` and `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:72-82`). The hook writes/renames state and prompt artifacts: `round-N-review-prompt.md`, `round-N-review-result.md`, `round-(N+1)-prompt.md`, `finalize-summary.md` expectation, `finalize-state.md`, `complete-state.md`, and terminal `*-state.md` files through `end_loop` (`hooks/lib/loop-common.sh:465-498`). It outputs Claude hook JSON blocks with `decision: block`, `reason`, and usually `systemMessage` for all recoverable gates, while successful no-block paths exit 0. State transitions are:
  - no active loop -> no state change, exit 0 (`hooks/loop-codex-stop-hook.sh:61-64`);
  - invalid model/effort/current round -> `unexpected-state.md` through `end_loop` (`hooks/loop-codex-stop-hook.sh:104-119`, helper at `hooks/lib/loop-common.sh:472-490`);
  - normal round, Codex review not complete -> `current_round` increments in `state.md` and Stop is blocked (`hooks/loop-codex-stop-hook.sh:1057-1060`, `hooks/loop-codex-stop-hook.sh:1113-1123`);
  - normal round, Codex `COMPLETE`, not at max -> `state.md` becomes `finalize-state.md`, Stop remains blocked with finalization instructions (`hooks/loop-codex-stop-hook.sh:971-1020`);
  - normal round, Codex `COMPLETE` at max -> `maxiter-state.md` (`hooks/loop-codex-stop-hook.sh:963-968`);
  - normal round max cutoff before Codex -> `maxiter-state.md` (`hooks/loop-codex-stop-hook.sh:636-643`);
  - Codex `STOP` -> `stop-state.md` (`hooks/loop-codex-stop-hook.sh:1023-1050`);
  - finalize phase with all gates passed -> `finalize-state.md` becomes `complete-state.md` (`hooks/loop-codex-stop-hook.sh:651-656`).
- gates_or_invariants: The hook enforces that branch switching is forbidden during a loop (`hooks/loop-codex-stop-hook.sh:166-175`), plan files must remain unchanged relative to the loop backup (`hooks/loop-codex-stop-hook.sh:182-250`), incomplete native todos block exit before Codex review (`hooks/loop-codex-stop-hook.sh:252-307`; parser in `hooks/check-todos-from-transcript.py:90-129`), full-repo `git status` failures block rather than skip repository-state checks (`hooks/loop-codex-stop-hook.sh:319-337`), changed code/docs files over 2000 lines block (`hooks/loop-codex-stop-hook.sh:346-420`), uncommitted changes block (`hooks/loop-codex-stop-hook.sh:430-483`), optional `push_every_round` blocks local-ahead branches (`hooks/loop-codex-stop-hook.sh:489-514`), and a round/finalize summary must exist before review or completion (`hooks/loop-codex-stop-hook.sh:522-554`). The Finalize Phase invariant is especially important: it is active state, but Codex is no longer run; completion requires all stop-hook gates to pass and then renames to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:645-657`). The exact terminal markers are strict last-line tokens, avoiding accidental matches such as “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:955-960`).
- dependencies_and_callers: Called by Claude Code Stop hook configuration in `hooks/hooks.json:52-58`. Depends on `hooks/lib/loop-common.sh` for active-loop discovery, state parsing, constants `MARKER_COMPLETE`/`MARKER_STOP`, template helpers, and `end_loop` (`hooks/loop-codex-stop-hook.sh:46-49`; constants at `hooks/lib/loop-common.sh:26-40`; `end_loop` at `hooks/lib/loop-common.sh:465-498`). Depends on `hooks/lib/template-loader.sh` transitively for template resolution and single-pass rendering (`hooks/lib/template-loader.sh:24-31`, `hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:167-203`). Depends on `scripts/portable-timeout.sh` for portable timeout execution (`hooks/loop-codex-stop-hook.sh:50-55`; implementation at `scripts/portable-timeout.sh:9-71`). Depends on `hooks/check-todos-from-transcript.py` for TodoWrite transcript parsing (`hooks/loop-codex-stop-hook.sh:258-263`; parser behavior at `hooks/check-todos-from-transcript.py:20-87`). Depends on many prompt templates, including `prompt-template/block/git-status-failed.md` at `hooks/loop-codex-stop-hook.sh:327-333`. External command dependencies include `jq`, `git`, `python3`, `diff`, `wc`, `grep`, `awk`, `sed`, `codex`, `mkdir`, `cp`, `mv`, `tail`, and timeout providers.
  
  Sibling/parent coordination: write/read/bash/edit/plan validators also parse `finalize-state.md` as active state, so the stop hook’s finalize transition remains protected by tool validators (`hooks/loop-plan-file-validator.sh:38-45`; references in `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh` from the search evidence). The cancel command explicitly supports both normal and finalize cancellation by touching `.cancel-requested` and renaming `state.md` or `finalize-state.md` to `cancel-state.md` (`commands/cancel-rlcr-loop.md:20-49`), while this stop hook completes finalize state only after gates pass.
- edge_cases_or_failure_modes: Schema validation comments say outdated state should terminate as unexpected, but the implementation returns a JSON block without calling `end_loop` at `hooks/loop-codex-stop-hook.sh:127-143`; invalid model/effort/current_round do call `end_loop ... unexpected` (`hooks/loop-codex-stop-hook.sh:102-119`). `MAX_ITERATIONS` defaults to 42 if the parsed value is nonnumeric (`hooks/loop-codex-stop-hook.sh:122-124`), while `parse_state_file` itself defaults to 10 (`hooks/lib/loop-common.sh:133-136`). The full-repo cached git status runs without `git -C "$PROJECT_ROOT"` at `hooks/loop-codex-stop-hook.sh:319-323`, unlike branch and plan checks; if the hook process cwd differs from `PROJECT_ROOT`, git cleanliness/large-file checks may observe a different cwd. Plan-specific tracked status at `hooks/loop-codex-stop-hook.sh:216` uses `|| echo ""`, so that particular check can lose a nonzero exit, although the later full-repo status gate is fail-closed. If `hooks/check-todos-from-transcript.py` is absent, the todo gate is skipped (`hooks/loop-codex-stop-hook.sh:258-307`). `git status --porcelain` parsing is line-based and non-`-z`, so exotic filenames may be imperfectly parsed in large-file detection (`hooks/loop-codex-stop-hook.sh:351-397`). If Codex writes nothing, writes to the wrong path, exits nonzero, or produces an empty review file, the hook blocks with debug paths rather than advancing state (`hooks/loop-codex-stop-hook.sh:878-950`). A non-alignment-round `STOP` is unusual but still honored (`hooks/loop-codex-stop-hook.sh:1023-1050`). I attempted a git metadata sanity check in the read-only export, but it failed due the sandbox lacking writable macOS cache/temp access and then reported no usable `.git`; I did not rely on that command for evidence.
- validation_or_tests: Static template-reference validation includes `hooks/loop-codex-stop-hook.sh` in its scan list (`tests/test-template-references.sh:56-64`) and verifies referenced templates exist (`tests/test-template-references.sh:83-110`), with a summary failure path for missing templates (`tests/test-template-references.sh:213-229`). Finalize-phase tests exercise missing finalize summary blocking, dirty git blocking, finalize completion renaming to `complete-state.md`, Codex not being invoked during finalize, `COMPLETE` entering Finalize Phase, max-iteration behavior, incomplete todo blocking, normal-round non-COMPLETE feedback, and state parsing by validators (`tests/test-finalize-phase.sh:354-665`). Plan-file hook tests exercise stop-hook plan modification/deletion/backup-missing/tracked-modification/schema paths (`tests/test-plan-file-hooks.sh:466-653`). State naming tests cover terminal state files not being treated as active loops, with README documenting `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` meanings (`README.md:256-260`). I did not run tests because this task is research-only in a read-only branch export.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-047 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: User-facing block template for the stop hook’s fail-closed git-status gate. Although it is a small static Markdown file, it is core algorithm support because it defines the reason surfaced when the hook cannot verify repository cleanliness and therefore refuses to let the RLCR loop exit.
- algorithmic_behavior: The template contains a heading, interpolates `{{GIT_STATUS_EXIT}}`, lists likely causes, and instructs the operator to check `git status` manually (`prompt-template/block/git-status-failed.md:1-10`). It is consumed at `hooks/loop-codex-stop-hook.sh:327-333` after `git status --porcelain` fails or times out. The hook renders it with `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md" ... "GIT_STATUS_EXIT=$GIT_STATUS_EXIT"` and then emits hook JSON with `decision: block` plus system message `Loop: Blocked - git status failed (exit $GIT_STATUS_EXIT)` at `hooks/loop-codex-stop-hook.sh:332-335`.
- inputs_outputs_state: Input is one template variable, `GIT_STATUS_EXIT`, provided by the stop hook from the exit code captured at `hooks/loop-codex-stop-hook.sh:322-325`. Output is Markdown text used as the JSON `reason` field in the Claude hook response. The template does not mutate files or state directly; its state effect is indirect: it keeps the loop in its current active state by causing the stop hook to block exit and avoid advancing to review, finalize completion, or terminal state.
- gates_or_invariants: Enforces the invariant that repository cleanliness checks cannot be bypassed when `git status --porcelain` cannot be trusted. The stop hook’s comment makes this explicit: “fail-closed on git failures” at `hooks/loop-codex-stop-hook.sh:314`. The template’s message says repository state cannot be verified (`prompt-template/block/git-status-failed.md:5`) and provides possible lock/contention/invalid-state/large-repo causes (`prompt-template/block/git-status-failed.md:6-8`). Because template rendering uses the shared single-pass renderer, placeholder values are not recursively expanded (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-132`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:327-335`. Rendering path is provided by `load_and_render_safe` in `hooks/lib/template-loader.sh:167-203`, with template directory resolution through `get_template_dir` (`hooks/lib/template-loader.sh:24-31`) and sourced via `hooks/lib/loop-common.sh:46-55`. The template is validated structurally by `tests/test-template-references.sh`, which scans `hooks/loop-codex-stop-hook.sh` for `load_template`, `load_and_render`, and `load_and_render_safe` calls and checks that referenced template files exist (`tests/test-template-references.sh:56-110`).
- edge_cases_or_failure_modes: If `GIT_STATUS_EXIT` is missing from the render call, the placeholder would remain literal because missing variables are preserved by the renderer (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:119-122`). If the template file is missing or renders empty, the hook has an inline fallback at `hooks/loop-codex-stop-hook.sh:327-331`; that fallback is shorter and omits the detailed likely-cause list from `prompt-template/block/git-status-failed.md:5-9`. The template does not include stderr or the exact git command, so troubleshooting information is intentionally generic. It also only applies to the full-repo cached status gate; other git failures, such as branch consistency, use inline messages rather than this template (`hooks/loop-codex-stop-hook.sh:149-164`).
- validation_or_tests: `rg` evidence found the template and its direct hook reference only in `prompt-template/block/git-status-failed.md:1-10` and `hooks/loop-codex-stop-hook.sh:322-335`, plus the template-reference test machinery. `tests/test-template-references.sh:83-110` should catch a missing `block/git-status-failed.md` because it scans the stop hook for template path references. I found no dedicated behavioral test that forces `git status --porcelain` to timeout or return nonzero and asserts this exact template body; existing finalize tests cover dirty git blocking (`tests/test-finalize-phase.sh:390-407`) but not the git-status-failure path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CANCEL_WHEN_FINALIZE-HZ-017`, `CANCEL_WHEN_FINALIZE-HZ-047`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`