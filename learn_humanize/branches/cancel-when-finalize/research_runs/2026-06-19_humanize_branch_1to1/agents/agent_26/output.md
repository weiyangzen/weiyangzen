# agent_26 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-026 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR hook allowlist behavior. It verifies the narrow exception set that lets validators access only specific generated loop artifacts while preserving the default block posture for todo files, stale summaries, wrong directories, and Bash bypass attempts.
- algorithmic_behavior: The test builds a temporary Git repository and active loop directory under `.humanize/rlcr/2024-01-01_12-00-00`, writes `state.md` with `current_round: 5`, `max_iterations: 42`, `plan_file`, `plan_tracked`, and `start_branch`, then exercises shared allowlist logic and four hook validators via JSON hook inputs. The allowlist expected by the test is exactly `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop directory. This mirrors `hooks/lib/loop-common.sh:170`, where `is_allowlisted_file` compares full path equality against those four filenames under `active_loop_dir`.
- inputs_outputs_state: Inputs are synthetic hook payloads such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}`, active-loop state from `state.md`, `CLAUDE_PROJECT_DIR`, and Bash command strings. Outputs are process exit codes and stderr/stdout messages: allowed operations must exit `0`; blocked operations must exit `2` and include expected diagnostic text such as `todos` or `round`. Test state is local to `TEST_DIR`, with cleanup via trap at `tests/test-allowlist-validators.sh:30`.
- gates_or_invariants: The exact-path invariant is central: basename alone is insufficient. `round-1-todos.md` in `/other/path` is blocked by the direct function test at `tests/test-allowlist-validators.sh:118`, and Bash attempts without the full active-loop path are blocked at `tests/test-allowlist-validators.sh:340`. The newest active loop directory controls authorization via `find_active_loop` in `hooks/lib/loop-common.sh:63`; old-loop and same-basename-different-root Bash cases are explicitly blocked at `tests/test-allowlist-validators.sh:353` and `tests/test-allowlist-validators.sh:368`. Bash allows only full-path `round-[12]-todos.md` in the active loop dir, implemented by escaped active-loop path matching in `hooks/loop-bash-validator.sh:333`.
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` at `tests/test-allowlist-validators.sh:17`, then calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh`. Those validators depend on `jq`, `git`, shared helpers such as `is_round_file_type`, `extract_round_number`, `is_allowlisted_file`, `find_active_loop`, and template-backed block messages through `hooks/lib/template-loader.sh`. Write/Edit/Read validators parse `tool_name` and `file_path`; Bash parses `tool_input.command`.
- edge_cases_or_failure_modes: Covered edge cases include non-current but allowlisted historical summaries (`round-0-summary.md`, `round-1-summary.md`), non-allowlisted future/stale todos and summaries, wrong directory, relative/generic Bash output path, stale loop directory, and different project root with matching loop basename. Notably, direct Write/Edit/Read allowlisted summaries bypass the current-round check because the shared allowlist exception is consulted before wrong-round blocking in `hooks/loop-write-validator.sh:181`, `hooks/loop-edit-validator.sh:124`, and `hooks/loop-read-validator.sh:116`. Bash is stricter for summaries: `hooks/loop-bash-validator.sh:323` blocks all `round-*-summary.md` modifications through shell commands, so the allowlist exception applies to Bash todos only in these tests.
- validation_or_tests: The file contains 25 assertions: direct function checks at `tests/test-allowlist-validators.sh:64`, Write validator checks at `tests/test-allowlist-validators.sh:126`, Edit checks at `tests/test-allowlist-validators.sh:185`, Read checks at `tests/test-allowlist-validators.sh:228`, and Bash path-restriction checks at `tests/test-allowlist-validators.sh:284`. The script exits with `TESTS_FAILED`, so any failed assertion makes the test process nonzero.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-056 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for the state-schema compatibility gate. It defines the user-facing failure contract when an active RLCR state file lacks required fields introduced in humanize `1.1.2+`.
- algorithmic_behavior: The template has one placeholder, `{{FIELD_NAME}}`, used to name the missing required state field. It explains that the loop was started with an older plugin version, gives three recovery options, and states that the loop will be terminated as `unexpected` to preserve state information. The rendered message is loaded through `load_and_render_safe` in `hooks/loop-plan-file-validator.sh:62`.
- inputs_outputs_state: Input is `FIELD_NAME`, passed by `schema_validation_error` in `hooks/loop-plan-file-validator.sh:57`. Output is Markdown text embedded as JSON string content in a hook response with `"decision": "block"` at `hooks/loop-plan-file-validator.sh:68`. The template itself does not mutate state; it communicates a validation result. Related state fields are parsed by `parse_state_file` in `hooks/lib/loop-common.sh:112`, with required values checked as `plan_tracked` and `start_branch` in `hooks/loop-plan-file-validator.sh:76`.
- gates_or_invariants: The gate fails closed when either required field is empty. The validator exits `0` after printing a blocking JSON decision, matching hook semantics where a successful hook process can still block the user action through its JSON payload. The template syntax follows the repository’s placeholder contract: uppercase `{{FIELD_NAME}}`, single-pass rendering, and unreplaced placeholders preserved if no variable is supplied, per `hooks/lib/template-loader.sh:7` and `hooks/lib/template-loader.sh:50`.
- dependencies_and_callers: Primary caller is `hooks/loop-plan-file-validator.sh`, a `UserPromptSubmit` hook that first finds the active loop, parses `state.md` or `finalize-state.md`, then checks schema, branch consistency, and plan tracking. Template resolution depends on `hooks/lib/template-loader.sh`, sourced through `hooks/lib/loop-common.sh`. Related but separate schema handling exists in `hooks/loop-codex-stop-hook.sh:127`, which emits an inline outdated-schema block when both fields are missing or empty.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to an inline message at `hooks/loop-plan-file-validator.sh:59`, so the block remains explainable. If only one field is missing, the plan-file validator reports the specific missing field because it iterates `REQUIRED_FIELDS` and calls `schema_validation_error` with that field name. If a malformed template placeholder were introduced, comprehensive template syntax validation is intended to catch it through `tests/test-templates-comprehensive.sh:114`.
- validation_or_tests: Template existence and reference integrity are checked by `tests/test-template-references.sh`, which scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails on missing templates. Broader template loading, placeholder syntax, fallback, and rendering behavior are covered by `tests/test-templates-comprehensive.sh:83`, `tests/test-templates-comprehensive.sh:114`, and `tests/test-templates-comprehensive.sh:441`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence headings present; each assigned item appears once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`