# agent_09 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-009 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: `prompt-template/block` is the reusable block-message surface for RLCR guard failures. The files are not executable, but they are algorithmically significant because hook validators render them when blocking unsafe loop actions. The directory contains 26 markdown fragments for state protection, round/path correctness, goal tracker rules, todo access, git cleanliness, large-file gates, schema drift, Codex-review failure, and finalize-phase state protection.
- algorithmic_behavior: Templates are loaded through `hooks/lib/template-loader.sh`, where `load_and_render_safe` renders a block file or falls back to inline text if the template is missing or empty (`hooks/lib/template-loader.sh:170`). Rendering is single-pass and leaves unknown placeholders intact (`hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:116`). The directory’s children map to concrete guard decisions: read/write/edit/bash validators block wrong round files, prompt writes, state/finalize-state writes, todo files, plan backup edits, and goal tracker writes (`hooks/loop-read-validator.sh:100`, `hooks/loop-write-validator.sh:105`, `hooks/loop-edit-validator.sh:86`, `hooks/loop-bash-validator.sh:87`). The stop hook uses the same block directory for exit gates: incomplete todos, git status failure, large files, dirty git state, unpushed commits, missing summaries, plan modifications, and uninitialized goal tracker (`hooks/loop-codex-stop-hook.sh:244`, `hooks/loop-codex-stop-hook.sh:294`, `hooks/loop-codex-stop-hook.sh:332`, `hooks/loop-codex-stop-hook.sh:407`, `hooks/loop-codex-stop-hook.sh:470`, `hooks/loop-codex-stop-hook.sh:501`, `hooks/loop-codex-stop-hook.sh:536`, `hooks/loop-codex-stop-hook.sh:616`).
- inputs_outputs_state: Inputs are template variables such as `CURRENT_ROUND`, `FILE_PATH`, `ACTIVE_LOOP_DIR`, `CORRECT_PATH`, `PLAN_FILE`, `BACKUP_PATH`, `GIT_ISSUES`, `INCOMPLETE_LIST`, `AHEAD_COUNT`, and `SUMMARY_FILE`. Outputs are human-readable markdown reasons emitted to stderr by PreToolUse validators with exit code `2`, or JSON `decision: block` reasons from the stop hook. The templates do not mutate state directly; they explain state-machine restrictions while hooks perform the block or state transition.
- gates_or_invariants: The directory encodes guard contracts: `state.md` and `finalize-state.md` are system-managed (`prompt-template/block/state-file-modification.md:3`, `prompt-template/block/finalize-state-file-modification.md:3`); prompt files are read-only instructions from Codex (`prompt-template/block/prompt-file-write.md:3`); summary files must be in the active loop directory and current round unless allowlisted (`prompt-template/block/wrong-summary-location.md:3`, `prompt-template/block/wrong-round-number.md:3`); todos files should use native todo tooling (`prompt-template/block/todos-file-access.md:3`); plan backups are protected (`prompt-template/block/plan-backup-protected.md:3`); after Round 0 only Codex may modify `goal-tracker.md` and Claude must request changes through the summary (`prompt-template/block/goal-tracker-modification.md:3`, `prompt-template/block/goal-tracker-modification.md:9`); dirty/unpushed git and oversized files block loop exit (`prompt-template/block/git-not-clean.md:3`, `prompt-template/block/unpushed-commits.md:3`, `prompt-template/block/large-files.md:3`).
- dependencies_and_callers: Direct callers are `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`, all via `load_and_render_safe` or `load_template`. Shared message functions in `hooks/lib/loop-common.sh` wrap common block templates for todos, prompt writes, state-file writes, finalize-state writes, summary bash writes, goal tracker bash writes, and post-Round-0 goal tracker modification (`hooks/lib/loop-common.sh:192`, `hooks/lib/loop-common.sh:202`, `hooks/lib/loop-common.sh:211`, `hooks/lib/loop-common.sh:220`, `hooks/lib/loop-common.sh:230`, `hooks/lib/loop-common.sh:241`, `hooks/lib/loop-common.sh:453`).
- edge_cases_or_failure_modes: Missing or empty templates degrade to hardcoded fallback text rather than allowing the operation (`hooks/lib/template-loader.sh:179`, `hooks/lib/template-loader.sh:192`). Unresolved placeholders remain visible instead of being recursively expanded (`hooks/lib/template-loader.sh:120`), preventing prompt/content injection through nested `{{...}}` values. Some files are composite notes rather than standalone decisions: `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md` are inserted into `git-not-clean.md` through `SPECIAL_NOTES` (`hooks/loop-codex-stop-hook.sh:441`, `hooks/loop-codex-stop-hook.sh:453`). Allowlist exceptions exist for early todo/summary files (`hooks/lib/loop-common.sh:174`) and are tested separately.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for template references and fails if referenced block templates are missing (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:102`, `tests/test-template-references.sh:221`). `tests/test-template-loader.sh` verifies loading, missing-template fallback, variable rendering, safe rendering of shell metacharacters, and valid template directory detection (`tests/test-template-loader.sh:59`, `tests/test-template-loader.sh:151`, `tests/test-template-loader.sh:200`, `tests/test-template-loader.sh:224`, `tests/test-template-loader.sh:257`). `tests/test-allowlist-validators.sh` exercises allowlisted and non-allowlisted todo/summary paths (`tests/test-allowlist-validators.sh:70`, `tests/test-allowlist-validators.sh:159`). `tests/test-finalize-phase.sh` verifies finalize-state block messaging and finalize summary allowance (`tests/test-finalize-phase.sh:281`, `tests/test-finalize-phase.sh:293`, `tests/test-finalize-phase.sh:317`).
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-039 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: `hooks/lib/loop-common.sh` is the shared shell library for RLCR hook validators. It defines field names, review markers, exit-reason constants, active-loop discovery, state frontmatter parsing, round-file parsing, path predicates, safe cancel authorization, bash modification detection, block-message renderers, and state termination by renaming.
- algorithmic_behavior: `find_active_loop` selects only the newest timestamped loop directory containing `state.md` or `finalize-state.md`, which prevents older loop directories from reviving after abnormal exits (`hooks/lib/loop-common.sh:63`, `hooks/lib/loop-common.sh:71`, `hooks/lib/loop-common.sh:76`). `parse_state_file` extracts YAML frontmatter into `STATE_*` variables, strips legacy quotes from branch and plan path fields, and defaults `current_round`, `max_iterations`, and `push_every_round` (`hooks/lib/loop-common.sh:112`, `hooks/lib/loop-common.sh:119`, `hooks/lib/loop-common.sh:133`). Round-file helpers identify `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md` (`hooks/lib/loop-common.sh:148`, `hooks/lib/loop-common.sh:158`). Message helpers render block templates through the template loader (`hooks/lib/loop-common.sh:192`, `hooks/lib/loop-common.sh:202`, `hooks/lib/loop-common.sh:211`, `hooks/lib/loop-common.sh:220`). `end_loop` validates the reason and renames the active state file to `<reason>-state.md` (`hooks/lib/loop-common.sh:472`, `hooks/lib/loop-common.sh:477`, `hooks/lib/loop-common.sh:487`).
- inputs_outputs_state: Inputs are a loop base directory, state-file path, file paths or lowercased shell commands from hook JSON, and template variables. Outputs include active-loop directory paths, parsed `STATE_*` shell globals, boolean shell return codes, rendered markdown block messages, and renamed state files. State transitions supported by constants are `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`hooks/lib/loop-common.sh:36`). The library treats `COMPLETE` and `STOP` as strict Codex review markers (`hooks/lib/loop-common.sh:27`), which the stop hook later reads from the final non-empty review line (`hooks/loop-codex-stop-hook.sh:955`).
- gates_or_invariants: The most branch-relevant invariant is that canceling by moving `state.md` or `finalize-state.md` to `cancel-state.md` is allowed only when `.cancel-requested` exists in the active loop directory and the command is exactly a two-argument `mv` from the active state path to the active cancel path (`hooks/lib/loop-common.sh:288`, `hooks/lib/loop-common.sh:290`, `hooks/lib/loop-common.sh:328`, `hooks/lib/loop-common.sh:378`, `hooks/lib/loop-common.sh:396`, `hooks/lib/loop-common.sh:405`). It rejects command substitution, backticks, newlines, chaining operators, pipes, leftover `$` variables, extra args, and wrong directories (`hooks/lib/loop-common.sh:295`, `hooks/lib/loop-common.sh:300`, `hooks/lib/loop-common.sh:305`, `hooks/lib/loop-common.sh:323`, `hooks/lib/loop-common.sh:383`). The path predicate order matters because `finalize-state.md` also matches the broader `state.md` suffix; callers check finalize-state first (`hooks/loop-bash-validator.sh:80`, `hooks/loop-write-validator.sh:103`, `hooks/loop-edit-validator.sh:84`).
- dependencies_and_callers: Sourced by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh` (`hooks/loop-read-validator.sh:16`, `hooks/loop-write-validator.sh:17`, `hooks/loop-edit-validator.sh:16`, `hooks/loop-bash-validator.sh:16`, `hooks/loop-plan-file-validator.sh:17`, `hooks/loop-codex-stop-hook.sh:48`). It depends on `hooks/lib/template-loader.sh`, loaded at startup (`hooks/lib/loop-common.sh:47`). The cancel command specification in `commands/cancel-rlcr-loop.md` deliberately creates `.cancel-requested` before moving either state file, matching `is_cancel_authorized` (`commands/cancel-rlcr-loop.md:30`, `commands/cancel-rlcr-loop.md:43`).
- edge_cases_or_failure_modes: `find_active_loop` only checks the newest directory; if the newest has no active state file, older active-looking directories are ignored by design (`hooks/lib/loop-common.sh:58`). State parsing is grep/sed based, so duplicate frontmatter keys would concatenate through command substitution rather than structured YAML parsing; required-field validation in stop and plan-file hooks catches missing newer schema fields (`hooks/loop-codex-stop-hook.sh:131`, `hooks/loop-plan-file-validator.sh:76`). `command_modifies_file` is regex-based and recognizes common writes, redirects, in-place edits, file moves/copies/removals, `dd`, `truncate`, and file descriptor redirects (`hooks/lib/loop-common.sh:428`), but it is intentionally a pattern guard rather than a shell parser. The bash validator adds extra handling for state/finalize-state as a source path in `mv`/`cp`, command chaining, leading redirections, and `sh -c` wrappers (`hooks/loop-bash-validator.sh:117`, `hooks/loop-bash-validator.sh:156`, `hooks/loop-bash-validator.sh:254`).
- validation_or_tests: `tests/test-cancel-signal-file.sh` directly tests `is_cancel_authorized` positive and negative paths, including signal presence, wrong command, extra args, quoted paths, and hidden variables (`tests/test-cancel-signal-file.sh:1258`, `tests/test-cancel-signal-file.sh:1270`, `tests/test-cancel-signal-file.sh:1282`, `tests/test-cancel-signal-file.sh:1294`, `tests/test-cancel-signal-file.sh:1306`, `tests/test-cancel-signal-file.sh:1330`). `tests/test-finalize-phase.sh` tests finalize-state helpers, active-loop detection, finalize-summary allowance, and state-file block behavior (`tests/test-finalize-phase.sh:174`, `tests/test-finalize-phase.sh:229`, `tests/test-finalize-phase.sh:281`, `tests/test-finalize-phase.sh:293`). `tests/test-state-exit-naming.sh` tests active-state discovery and `end_loop` reason handling (`tests/test-state-exit-naming.sh:179`). `tests/test-bash-validator-patterns.sh` exercises `command_modifies_file` across redirects, `tee`, in-place editors, and file operations (`tests/test-bash-validator-patterns.sh:36`, `tests/test-bash-validator-patterns.sh:75`, `tests/test-bash-validator-patterns.sh:89`, `tests/test-bash-validator-patterns.sh:102`, `tests/test-bash-validator-patterns.sh:116`).
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-069 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: `prompt-template/claude/next-round-footer.md` is the footer appended to a generated next-round Claude prompt after Codex review found issues. It defines the worker-facing exit contract for the next iteration: do not fake loop exit, do not edit loop state, do not execute `cancel-rlcr-loop`, optionally run `code-simplifier`, commit changes, and write the next round summary.
- algorithmic_behavior: The stop hook increments `current_round`, writes the next prompt from `claude/next-round-prompt.md`, optionally appends post-alignment instructions, then appends this footer using `load_and_render_safe` with `NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:1057`, `hooks/loop-codex-stop-hook.sh:1063`, `hooks/loop-codex-stop-hook.sh:1075`, `hooks/loop-codex-stop-hook.sh:1088`, `hooks/loop-codex-stop-hook.sh:1091`). The footer itself contains a hard behavioral prohibition against lying, state-file editing, or running cancellation (`prompt-template/claude/next-round-footer.md:4`) and completion instructions for commit and summary (`prompt-template/claude/next-round-footer.md:6`, `prompt-template/claude/next-round-footer.md:8`, `prompt-template/claude/next-round-footer.md:9`).
- inputs_outputs_state: The only dynamic input is `NEXT_SUMMARY_FILE`, rendered into `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:9`). Output is appended markdown in `round-${NEXT_ROUND}-prompt.md`, not a state file. State mutation is performed before the footer is appended: `current_round` in `state.md` is updated via sed and the temp file is moved into place (`hooks/loop-codex-stop-hook.sh:1058`, `hooks/loop-codex-stop-hook.sh:1060`).
- gates_or_invariants: The footer reinforces a core loop invariant that only the loop machinery should decide termination; Claude must continue through the stop hook by committing and writing the expected summary. It complements validator enforcement: direct state/finalize-state modification is blocked by write/edit/bash validators (`hooks/loop-write-validator.sh:105`, `hooks/loop-edit-validator.sh:86`, `hooks/loop-bash-validator.sh:87`), and unauthorized cancel moves are blocked unless `.cancel-requested` exists and the command matches the strict `mv` contract (`hooks/lib/loop-common.sh:284`, `hooks/lib/loop-common.sh:396`, `hooks/lib/loop-common.sh:405`). In finalize phase, this footer is bypassed; the separate finalize prompt is used instead (`hooks/loop-codex-stop-hook.sh:983`, `prompt-template/claude/finalize-phase-prompt.md:1`).
- dependencies_and_callers: Sole direct caller found is `hooks/loop-codex-stop-hook.sh:1091`. It depends on the template-loader single-pass renderer. It coordinates with `claude/next-round-prompt.md`, which contains the main Codex feedback and goal tracker reference (`prompt-template/claude/next-round-prompt.md:16`, `prompt-template/claude/next-round-prompt.md:22`), and with optional push and goal-tracker request appendices added after the footer (`hooks/loop-codex-stop-hook.sh:1094`, `hooks/loop-codex-stop-hook.sh:1103`).
- edge_cases_or_failure_modes: If this file is missing or renders empty, the stop hook appends a fallback with only commit and summary instructions, losing the explicit anti-cancel/anti-state-edit wording (`hooks/loop-codex-stop-hook.sh:1088`, `hooks/lib/template-loader.sh:179`). If `NEXT_SUMMARY_FILE` is not supplied, the placeholder remains as-is due to the renderer’s unknown-variable behavior (`hooks/lib/template-loader.sh:120`). Because the footer tells the agent not to execute `cancel-rlcr-loop`, there is an intentional distinction between normal next-round work and the separate user-invoked cancellation command, which is documented in `commands/cancel-rlcr-loop.md`.
- validation_or_tests: Template existence and direct references are covered by `tests/test-template-references.sh`, which scans hook references and validates referenced template files (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:102`). Rendering behavior for real templates and fallback paths is covered by `tests/test-template-loader.sh` (`tests/test-template-loader.sh:151`, `tests/test-template-loader.sh:200`, `tests/test-template-loader.sh:214`). I did not find a dedicated unit test asserting the exact footer text or its append position in `round-N-prompt.md`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item headings present; HZ-009, HZ-039, and HZ-069 each appear once as an item section.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`