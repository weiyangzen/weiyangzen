# agent_28 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-028 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash write-detection algorithm used by RLCR loop hooks. The file tests `command_modifies_file` from `hooks/lib/loop-common.sh`, which is the shared predicate used by `hooks/loop-bash-validator.sh` to block shell-based writes to protected loop artifacts such as `state.md`, `goal-tracker.md`, `round-*-summary.md`, `round-*-prompt.md`, and `round-*-todos.md`.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:11-13`, then wraps `command_modifies_file` with two assertions: `assert_modifies` expects the command to match a protected write pattern, while `assert_not_modifies` expects it to be ignored as read-only or unrelated. Both helpers lowercase commands through `to_lower` before calling the predicate at `tests/test-bash-validator-patterns.sh:37-47` and `tests/test-bash-validator-patterns.sh:51-61`, mirroring production behavior in `hooks/loop-bash-validator.sh:29-30`.
- inputs_outputs_state: Inputs are literal Bash command strings plus an optional file regex pattern. The default pattern is `goal-tracker\.md` at `tests/test-bash-validator-patterns.sh:39` and `tests/test-bash-validator-patterns.sh:53`; later groups override with `state\.md` and `round-[0-9]+-summary\.md`. Outputs are test pass/fail messages and an aggregate exit code: `0` only when `TESTS_FAILED` remains zero, otherwise `1` at `tests/test-bash-validator-patterns.sh:206-214`. State is in-memory counters `TESTS_PASSED` and `TESTS_FAILED` initialized at `tests/test-bash-validator-patterns.sh:20-21` and mutated by `pass`/`fail`.
- gates_or_invariants: The main invariant is that commands capable of writing to the target file pattern must be detected, while normal read-only commands must not trigger false positives. The suite covers redirection `>`/`>>` at `tests/test-bash-validator-patterns.sh:75-80`, `tee` at `tests/test-bash-validator-patterns.sh:89-93`, in-place `sed`/`awk`/`perl` at `tests/test-bash-validator-patterns.sh:102-107`, `mv`/`cp`/`rm` at `tests/test-bash-validator-patterns.sh:116-121`, and `dd`/`truncate`/`exec`/`printf` at `tests/test-bash-validator-patterns.sh:130-133`. It also asserts common readers such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` are not blocked at `tests/test-bash-validator-patterns.sh:142-152`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, especially `to_lower` at `hooks/lib/loop-common.sh:141-144` and `command_modifies_file` at `hooks/lib/loop-common.sh:421-449`. The production caller is `hooks/loop-bash-validator.sh`, which invokes the same predicate for finalize/state files at `hooks/loop-bash-validator.sh:87-103`, plan backups at `hooks/loop-bash-validator.sh:284-288`, goal tracker at `hooks/loop-bash-validator.sh:297-305`, prompt files at `hooks/loop-bash-validator.sh:313-315`, summary files at `hooks/loop-bash-validator.sh:323-326`, and todos files at `hooks/loop-bash-validator.sh:333-340`. The all-suite runner includes this test at `tests/run-all-tests.sh:32-47`.
- edge_cases_or_failure_modes: The suite explicitly documents a known limitation: `cp file1.md file2.md goal-tracker.md` is not tested as a required positive because the implementation expects a simple `cp src dest` shape at `tests/test-bash-validator-patterns.sh:165-168`. Variable indirection such as `echo x > $FILE` is expected not to match because the predicate only sees literal file patterns at `tests/test-bash-validator-patterns.sh:170-172`. Edge cases include no-command redirection `> goal-tracker.md`, tight redirection `>goal-tracker.md`, uppercase commands after lowercasing, absolute/path-prefixed filenames, alternate target patterns for `state.md`, and round-summary regexes.
- validation_or_tests: This file is itself a validator test. It validates the concrete regex array in `command_modifies_file`, which includes redirection, `tee`, in-place editors, `mv|cp`, `rm`, `dd of=`, `truncate`, `printf` redirection, and `exec fd>` patterns at `hooks/lib/loop-common.sh:428-440`. It is part of the repository-wide test list in `tests/run-all-tests.sh:35`. I inspected the file directly but did not execute it because this branch export is read-only and the task requested research notes only.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-058 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the summary-file Bash write gate. It defines the message shown when `loop-bash-validator.sh` detects a Bash command attempting to modify `round-*-summary.md`, directing the agent to use Write/Edit tooling so summary-file round validation hooks are not bypassed.
- algorithmic_behavior: The template is short but participates in a state transition: a Bash command that matches `round-[0-9]+-summary\.md` is rejected with exit code `2` by `hooks/loop-bash-validator.sh:323-326`; during that rejection, `summary_bash_blocked_message` renders this template with `CORRECT_PATH` set to the active loop’s expected summary path. The template title and instructions are at `prompt-template/block/summary-bash-write.md:1-8`, including the placeholder `{{CORRECT_PATH}}` at line 5.
- inputs_outputs_state: Input is the render variable `CORRECT_PATH`, supplied by `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:230-237`. Production computes that value as `"$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` at `hooks/loop-bash-validator.sh:323-325`, where `CURRENT_ROUND` comes from parsed loop state at `hooks/loop-bash-validator.sh:51-53`. Output is rendered Markdown sent to stderr by the Bash validator before it exits `2`. The template itself has no mutable state; it is loaded from `prompt-template/block/summary-bash-write.md` via the template loader.
- gates_or_invariants: The invariant enforced by this block is that summary files must not be modified through Bash because shell writes bypass validation hooks. The message specifically tells the agent not to use Bash for summary files at `prompt-template/block/summary-bash-write.md:3`, points to the correct summary path at line 5, and explains that shell commands such as `cat`, `echo`, `sed`, and `awk` bypass validation at lines 7-8. The rendering path uses `load_and_render_safe`, so a missing template falls back to an inline message rather than producing an empty block, as implemented at `hooks/lib/template-loader.sh:170-203`.
- dependencies_and_callers: Direct caller is `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:230-237`. That function is called from the summary-file branch of `hooks/loop-bash-validator.sh:323-326`. The template loader resolves the repository template directory through `get_template_dir` at `hooks/lib/template-loader.sh:24-31` and performs single-pass `{{VAR}}` substitution in `render_template` at `hooks/lib/template-loader.sh:50-132`. `tests/test-template-references.sh` lists `block/summary-bash-write.md` as a required common template at `tests/test-template-references.sh:152-158` and checks existence at `tests/test-template-references.sh:161-167`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` uses the fallback string embedded in `summary_bash_blocked_message` at `hooks/lib/loop-common.sh:232-236`. If `CORRECT_PATH` is not supplied, the placeholder would remain literal because the renderer preserves missing variables rather than substituting empty strings, as documented and implemented at `hooks/lib/template-loader.sh:7-13` and `hooks/lib/template-loader.sh:115-122`. A stale or incorrect `CURRENT_ROUND` in state would cause the rendered path to point to the wrong round summary, but the block’s role remains to gate Bash writes, not to validate state parsing itself.
- validation_or_tests: Existence and reference coverage are validated by `tests/test-template-references.sh`, which scans shell scripts for template references at `tests/test-template-references.sh:74-111` and explicitly requires this common template at `tests/test-template-references.sh:152-167`. Runtime behavior is indirectly covered by Bash-validator tests that expect summary writes to be detected using `round-[0-9]+-summary\.md` patterns at `tests/test-bash-validator-patterns.sh:186-194`, and by production hook routing at `hooks/loop-bash-validator.sh:323-326`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 Item Evidence sections present; each assigned item id appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`