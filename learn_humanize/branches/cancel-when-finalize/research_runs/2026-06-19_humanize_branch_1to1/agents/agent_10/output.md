# agent_10 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-010 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing RLCR continuation/finalization prompt templates. This directory is not executable code, but it defines the instructions emitted by the RLCR stop hook when the state machine either continues to another implementation round or enters the new Finalize Phase.
- algorithmic_behavior: The directory contains six markdown fragments, all inspected recursively:
  - `prompt-template/claude/next-round-prompt.md:1` starts the continuation prompt when Codex found issues.
  - `prompt-template/claude/finalize-phase-prompt.md:1` defines the Finalize Phase prompt after Codex returns `COMPLETE`.
  - `prompt-template/claude/next-round-footer.md:4` forbids escaping the RLCR loop by lying, editing loop state, or running `cancel-rlcr-loop`, then requires commit and summary output.
  - `prompt-template/claude/post-alignment-action-items.md:2` adds extra action items after a full alignment check.
  - `prompt-template/claude/push-every-round-note.md:2` adds the remote push instruction when `push_every_round` is enabled.
  - `prompt-template/claude/goal-tracker-update-request.md:2` provides the summary-section contract for requesting Codex-owned goal tracker changes.
- inputs_outputs_state: Inputs are supplied by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe`/`load_template`: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `NEXT_SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, plus state flags like `PUSH_EVERY_ROUND`, `CURRENT_ROUND`, and `MAX_ITERATIONS`. Outputs are assembled markdown prompts written to `round-${NEXT_ROUND}-prompt.md` or returned directly in a JSON block reason. The main continuation transition is in `hooks/loop-codex-stop-hook.sh:1057`, where `current_round` is incremented, then `round-${NEXT_ROUND}-prompt.md` is created at `hooks/loop-codex-stop-hook.sh:1062`. The Finalize transition is in `hooks/loop-codex-stop-hook.sh:961`: on `COMPLETE`, unless max iterations is reached, `state.md` is renamed to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:980`, and the finalize prompt is returned as a blocking decision at `hooks/loop-codex-stop-hook.sh:1012`.
- gates_or_invariants: The templates encode prompt-level invariants: read the original plan before proceeding (`next-round-prompt.md:5`), create Todos for all discovered issues rather than only top-priority issues (`next-round-prompt.md:12`), treat `goal-tracker.md` as read-only after Round 0 (`next-round-prompt.md:30`), use a Goal Tracker Update Request instead of direct edits (`goal-tracker-update-request.md:2`), preserve functionality during Finalize (`finalize-phase-prompt.md:20`), run tests before Finalize exit (`finalize-phase-prompt.md:21`), commit and write the final summary (`finalize-phase-prompt.md:42`). Template directory shape is itself a gate: `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories in `hooks/lib/template-loader.sh:216`.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`. It renders `claude/finalize-phase-prompt.md` at `hooks/loop-codex-stop-hook.sh:1005`, renders `claude/next-round-prompt.md` at `hooks/loop-codex-stop-hook.sh:1075`, appends post-alignment items at `hooks/loop-codex-stop-hook.sh:1081`, appends the footer at `hooks/loop-codex-stop-hook.sh:1091`, conditionally appends push instructions at `hooks/loop-codex-stop-hook.sh:1095`, and appends the goal tracker update request at `hooks/loop-codex-stop-hook.sh:1104`. The loader is sourced indirectly through `hooks/lib/loop-common.sh:48`, which sets `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:51`.
- edge_cases_or_failure_modes: Missing render-critical templates use inline fallbacks through `load_and_render_safe`, so a missing base/finalize/footer template should still produce a blocking prompt. Optional fragments loaded with `load_template` have local fallbacks or are skipped: missing post-alignment content is skipped at `hooks/loop-codex-stop-hook.sh:1082`, missing push note falls back at `hooks/loop-codex-stop-hook.sh:1097`, and missing goal update request falls back at `hooks/loop-codex-stop-hook.sh:1105`. The max-iteration edge case bypasses Finalize entirely: `hooks/loop-codex-stop-hook.sh:963` terminates as `MAXITER` instead of creating `finalize-state.md`. Placeholder injection in `REVIEW_CONTENT` is mitigated by the single-pass renderer in `hooks/lib/template-loader.sh:54`.
- validation_or_tests: Template reference coverage is checked by `tests/test-template-references.sh:57`, which scans hook scripts and verifies referenced templates exist at `tests/test-template-references.sh:93`. Comprehensive template loading and syntax are checked in `tests/test-templates-comprehensive.sh:61`, `tests/test-templates-comprehensive.sh:90`, and `tests/test-templates-comprehensive.sh:118`. Finalize behavior, including `COMPLETE` entering Finalize and max iterations skipping it, is tested in `tests/test-finalize-phase.sh:460` and `tests/test-finalize-phase.sh:478`. I inspected these tests but did not run them in this read-only research pass.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-040 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared template loading/rendering library for the RLCR hook system. It is part of the core algorithm because hook decisions and blocking messages depend on reliable prompt/template rendering, including the Claude continuation/finalize prompts and validator block messages.
- algorithmic_behavior: Key functions:
  - `get_template_dir` at `hooks/lib/template-loader.sh:26` derives the template root by walking two levels up from `hooks/lib` and appending `prompt-template`.
  - `load_template` at `hooks/lib/template-loader.sh:36` reads a template by relative name and emits its content, or warns and emits empty output when missing.
  - `render_template` at `hooks/lib/template-loader.sh:58` performs single-pass `{{VAR}}` replacement using environment variables prefixed as `TMPL_VAR_`.
  - `load_and_render` at `hooks/lib/template-loader.sh:136` composes direct load plus render and returns empty output for missing templates.
  - `append_template` at `hooks/lib/template-loader.sh:151` concatenates a base string with another template’s content.
  - `load_and_render_safe` at `hooks/lib/template-loader.sh:170` adds fallback behavior for missing or empty templates.
  - `validate_template_dir` at `hooks/lib/template-loader.sh:208` validates the required template directory shape.
- inputs_outputs_state: Inputs are a template directory, template name, template content, and zero or more `VAR=value` assignments. Outputs are rendered markdown/text on stdout and warnings/errors on stderr. It does not directly mutate RLCR state, but it enables state transitions by producing the prompt payloads and validator block reasons consumed by hook JSON responses. `hooks/lib/loop-common.sh:48` sources this file, initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:51`, and warns rather than hard-failing if validation fails at `hooks/lib/loop-common.sh:54`.
- gates_or_invariants: The renderer intentionally uses a single-pass algorithm; comments at `hooks/lib/template-loader.sh:54` state that placeholders inside values are not recursively expanded. Missing variables are preserved as literal placeholders by the awk branch at `hooks/lib/template-loader.sh:119`. Missing safe templates are routed to fallback content at `hooks/lib/template-loader.sh:179`. Directory validation requires the template root and the `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:211` and `hooks/lib/template-loader.sh:216`.
- dependencies_and_callers: Dependencies are standard shell utilities: `cd`, `pwd`, `cat`, `env`, and `awk`. Callers include validators and the stop hook. `rg` showed direct uses in `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/lib/loop-common.sh`. The Claude templates assigned in this research are assembled by `hooks/loop-codex-stop-hook.sh:1005`, `hooks/loop-codex-stop-hook.sh:1075`, `hooks/loop-codex-stop-hook.sh:1091`, and related `load_template` calls.
- edge_cases_or_failure_modes: Missing template files produce empty output with a warning in `load_template`; callers that use plain `load_and_render` can therefore silently get empty content, while callers using `load_and_render_safe` get fallback text. Shell command substitution strips trailing newlines from rendered content. The comments describe uppercase placeholder names, but the awk implementation extracts any text between `{{` and `}}` and only replaces it when an exact environment key exists. Unclosed `{{` is treated literally at `hooks/lib/template-loader.sh:104`. Large or multiline values are passed through environment variables, so they can be constrained by process environment limits. Template names are concatenated with `"$template_dir/$template_name"` at `hooks/lib/template-loader.sh:39`; there is no path traversal guard inside this helper, so safety depends on trusted callers using fixed template paths.
- validation_or_tests: `tests/test-template-loader.sh:42` verifies `get_template_dir`; `tests/test-template-loader.sh:55` and `tests/test-template-loader.sh:68` cover existing and missing template loading; `tests/test-template-loader.sh:81` through `tests/test-template-loader.sh:224` cover rendering and safe fallbacks; `tests/test-template-loader.sh:257` through `tests/test-template-loader.sh:486` cover shell metacharacters and multiline values; `tests/test-template-loader.sh:499` through `tests/test-template-loader.sh:543` cover placeholder injection prevention. `tests/test-templates-comprehensive.sh:443` through `tests/test-templates-comprehensive.sh:490` covers fallback and directory validation; `tests/test-template-references.sh:172` through `tests/test-template-references.sh:200` checks critical validators use safe rendering. I inspected these tests but did not run them.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-070 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Base Claude prompt for the “continue loop” transition after Codex review finds issues. It defines the next-round work contract: read the plan, create complete Todos, use Codex feedback as input, read the goal tracker, and avoid direct goal tracker mutation after Round 0.
- algorithmic_behavior: The template starts with an explicit continuation signal at `prompt-template/claude/next-round-prompt.md:1`. It injects the original plan path through `{{PLAN_FILE}}` at `prompt-template/claude/next-round-prompt.md:6`, injects Codex’s review through `{{REVIEW_CONTENT}}` at `prompt-template/claude/next-round-prompt.md:18`, and injects the goal tracker path through `{{GOAL_TRACKER_FILE}}` at `prompt-template/claude/next-round-prompt.md:24`. It is rendered by `hooks/loop-codex-stop-hook.sh:1075` into `round-${NEXT_ROUND}-prompt.md`, after `current_round` is incremented in `state.md` at `hooks/loop-codex-stop-hook.sh:1057`.
- inputs_outputs_state: Inputs are `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`. Output is the first section of the next round prompt file. Additional output sections are appended later: post-alignment actions conditionally at `hooks/loop-codex-stop-hook.sh:1081`, the footer at `hooks/loop-codex-stop-hook.sh:1091`, optional push instructions at `hooks/loop-codex-stop-hook.sh:1095`, and goal tracker update request text at `hooks/loop-codex-stop-hook.sh:1104`. The surrounding state transition is “review found issues”: not `COMPLETE`, not `STOP`, so the hook updates `current_round`, writes the prompt, and returns JSON `"decision": "block"` at `hooks/loop-codex-stop-hook.sh:1114`.
- gates_or_invariants: The file encodes several prompt gates. It requires reviewing the original plan before proceeding at `prompt-template/claude/next-round-prompt.md:5`. It requires creating Todos for all discovered issues and explicitly forbids only addressing the most important issues at `prompt-template/claude/next-round-prompt.md:12`. It makes `goal-tracker.md` read-only after Round 0 at `prompt-template/claude/next-round-prompt.md:30`, with updates routed through a summary request at `prompt-template/claude/next-round-prompt.md:31`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for substitution and on `hooks/loop-codex-stop-hook.sh` for lifecycle placement. The stop hook provides an inline fallback at `hooks/loop-codex-stop-hook.sh:1067`, so a missing `next-round-prompt.md` still produces a basic continuation prompt, though with weaker instructions than this template. Goal tracker read-only behavior is backed by validators and messages in `hooks/lib/loop-common.sh`, including `goal_tracker_blocked_message` at `hooks/lib/loop-common.sh:453`.
- edge_cases_or_failure_modes: If `REVIEW_CONTENT` contains literal `{{PLAN_FILE}}` or other template-looking tokens, `render_template` does not recursively substitute them because it is single-pass; this is tested as a realistic injection scenario in `tests/test-template-loader.sh:525`. If a variable is missing, the placeholder remains visible in the generated prompt rather than failing closed. If this template is missing, the fallback prompt in `hooks/loop-codex-stop-hook.sh:1067` does not include the full Todo/goal-tracker contract from this file.
- validation_or_tests: Template loading and rendering for all templates are covered by `tests/test-templates-comprehensive.sh:90` and `tests/test-templates-comprehensive.sh:565`. Reference existence is covered by `tests/test-template-references.sh:93`. The rendering behavior that protects `REVIEW_CONTENT` from placeholder injection is covered by `tests/test-template-loader.sh:525`. I inspected these tests but did not run them.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 Item Evidence sections present, one per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`