# agent_01 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`
- note: read-only branch export inspected recursively; no files modified. Tests were inspected as executable specs, not run.

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the Humanize Claude Code plugin implementing the RLCR loop: setup creates `.humanize/rlcr/<timestamp>/`, hooks enforce loop invariants, Codex reviews round summaries, and a post-COMPLETE Finalize Phase can transition `state.md -> finalize-state.md -> complete-state.md`. The root coordinates plugin metadata, slash commands, scripts, hooks, templates, tests, CI, and docs.
- algorithmic_behavior: `commands/start-rlcr-loop.md:1-14` exposes setup through `scripts/setup-rlcr-loop.sh`; setup validates git/plan/model inputs and writes `state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:211-498`, `600-653`). `hooks/hooks.json:4-63` registers UserPromptSubmit, PreToolUse validators, and the Stop hook. `hooks/loop-codex-stop-hook.sh` is the main state machine: finds active loop (`59-82`), validates schema/branch/plan/todos/git/summary (`126-630`), completes finalize without Codex (`646-657`), runs Codex in normal phase (`751-951`), handles `COMPLETE` by entering Finalize unless at max iterations (`961-1021`), handles `STOP` (`1023-1051`), and otherwise increments the round and writes the next prompt (`1057-1123`).
- inputs_outputs_state: Inputs are slash command arguments, hook JSON from Claude Code, transcript JSONL, plan file content, git state, Codex output, and templates. Outputs are JSON hook blocks/allows, loop artifact files, review prompts/results, cache logs under `$HOME/.cache/humanize`, and renamed terminal state files. Important states are `state.md` active, `finalize-state.md` active-finalize, and inactive terminal states such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, `unexpected-state.md` (`README.md:242-260`; `hooks/lib/loop-common.sh:30-40`, `465-498`).
- gates_or_invariants: `hooks/lib/loop-common.sh:63-81` only treats newest timestamp dir with `state.md` or `finalize-state.md` as active. Validators block direct state mutation, wrong round/location reads/writes, prompt/todos misuse, plan backup modification, goal tracker mutation after round 0, and bash bypasses (`hooks/loop-write-validator.sh:37-218`; `hooks/loop-edit-validator.sh:36-152`; `hooks/loop-read-validator.sh:36-148`; `hooks/loop-bash-validator.sh:63-343`). Stop hook fails closed on git status errors, dirty git, unpushed commits when configured, missing summaries, incomplete todos, outdated schema, branch drift, plan drift, and large changed files (`hooks/loop-codex-stop-hook.sh:252-553`).
- dependencies_and_callers: `hooks/lib/template-loader.sh:24-222` provides single-pass template rendering and safe fallbacks; `hooks/lib/loop-common.sh` provides constants, parsing, path classifiers, cancel authorization, and command mutation detection. `hooks/check-todos-from-transcript.py:20-129` supplies the todo-completion gate. `scripts/portable-timeout.sh:9-76` abstracts timeout behavior. `prompt-template/block`, `prompt-template/claude`, and `prompt-template/codex` provide user-facing gates and review/finalize prompts. `.claude-plugin/plugin.json:1-19`, `.claude-plugin/marketplace.json:1-14`, and `.claude/CLAUDE.md:1-7` define plugin/project metadata and version policy.
- edge_cases_or_failure_modes: Branch export has no `.git`, so git status cannot be used as repository evidence here. Algorithm edge cases covered in code/tests include `finalize-state.md` matching generic `state.md` suffix and therefore requiring specific-first checks, `complete-state.md` not being active, max-iteration COMPLETE producing `maxiter-state.md`, finalize skipping Codex, malformed todo hook JSON, template absence fallback, shell wrapper/redirection bypass attempts, cancel requiring `.cancel-requested`, and plan files that are absolute, symlinked, tracked unexpectedly, modified, too short, or YAML unsafe.
- validation_or_tests: `tests/run-all-tests.sh:32-47` enumerates the suite. Relevant suites include finalize behavior (`tests/test-finalize-phase.sh`), state exit naming, cancel signal authorization, plan file validation/hooks, template loader/reference/comprehensive checks, bash validator patterns, allowlist validators, todo checker, ANSI parsing, and error scenarios. CI workflows run shell syntax, template tests, plan/state/cancel tests, and version bump checks (`.github/workflows/shell-syntax-check.yml:19-61`, `template-test.yml:26-65`, `plan-file-test.yml:42-60`, `version-bump-check.yml:16-143`).
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-031 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the Finalize Phase algorithm introduced after Codex emits `COMPLETE`. It verifies both positive transitions and negative guards around `finalize-state.md`, `finalize-summary.md`, no-Codex finalize completion, and normal round preservation.
- algorithmic_behavior: The test builds a disposable git repo and loop directory, sources `hooks/lib/loop-common.sh`, and injects mock `codex` binaries (`tests/test-finalize-phase.sh:40-81`). It defines reusable repo/loop fixtures (`83-163`), then tests helper classifiers (`174-216`), active-loop detection for finalize/complete/normal states (`219-271`), Write/Edit/Bash validator protections (`274-351`), finalize Stop-hook behavior (`354-440`), normal COMPLETE handling and max-iteration handling (`443-496`), incomplete todo gating (`499-541`), normal non-COMPLETE round behavior (`544-615`), and validator parsing of `finalize-state.md` (`617-665`).
- inputs_outputs_state: Inputs are mocked Codex output, hook JSON payloads for Write/Edit/Bash/Read/Stop/UserPromptSubmit, a transcript JSONL containing TodoWrite calls, and fixture loop state frontmatter. Outputs are PASS/FAIL counters and shell exit status, plus state-file assertions inside the temp repo. Key asserted transitions are `state.md -> finalize-state.md` on COMPLETE (`460-475`), `finalize-state.md -> complete-state.md` after finalize checks pass (`412-431`), and `state.md -> maxiter-state.md` when COMPLETE arrives at max iterations (`478-496`).
- gates_or_invariants: The file asserts `finalize-state.md` is active but `complete-state.md` is not (`229-255`), `finalize-summary.md` is writable only through the Write validator path (`281-290`), `finalize-state.md` cannot be written/edited or moved/copied from Bash (`293-351`), finalize still requires summary, clean git, and completed todos (`365-407`, `521-540`), and finalize must not invoke Codex (`383-388`, `434-440`, `535-540`).
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-plan-file-validator.sh`. It is included in the aggregate runner at `tests/run-all-tests.sh:45`; CI plan-file workflow paths include hook/test surfaces that interact with this behavior (`.github/workflows/plan-file-test.yml:5-29`).
- edge_cases_or_failure_modes: Covers suffix ambiguity where `finalize-state.md` also matches a generic `state.md` regex, source-protection bypass attempts via `mv`/`cp`, missing summary before finalize exit, dirty worktree after summary exists, incomplete TodoWrite transcript, accidental Codex invocation during finalize, normal non-COMPLETE rounds being unaffected, and validators parsing finalize state when `state.md` is absent.
- validation_or_tests: This file is itself the validation asset. It checks expected validator exit codes (`0` allow, `2` block), JSON Stop-hook block decisions, file existence/nonexistence after state transitions, round increment to 4 on normal feedback (`591-597`), review-result creation (`599-605`), and final summary of pass/fail counts with `exit $TESTS_FAILED` (`667-676`).
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-061 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Block-message template for the Stop-hook summary gate. It tells the worker that exiting is blocked until the required work summary file exists.
- algorithmic_behavior: The template renders a markdown block with one variable, `{{SUMMARY_FILE}}`, and required summary contents: implementation, files changed, tests, and remaining items (`prompt-template/block/work-summary-missing.md:1-16`). It has no executable logic but directly shapes the algorithm’s retry instruction when the Stop hook blocks.
- inputs_outputs_state: Input is `SUMMARY_FILE`, supplied by `hooks/loop-codex-stop-hook.sh:533-537`. Output is rendered markdown embedded as the Stop-hook JSON `reason`. It does not mutate state; it preserves the active loop by blocking exit until the expected summary path exists.
- gates_or_invariants: The Stop hook chooses `finalize-summary.md` during Finalize Phase and `round-N-summary.md` during normal phase (`hooks/loop-codex-stop-hook.sh:522-527`). If the selected file is absent, it renders this template and returns `decision: block` with phase-specific system messages (`529-553`). The summary gate runs after todo, git, and plan integrity checks, so those earlier failures can block before this message.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh:170-203`, called by `hooks/loop-codex-stop-hook.sh:533-537`. Covered specifically by finalize missing-summary assertions in `tests/test-finalize-phase.sh:365-380` and generally by template reference/comprehensive tests.
- edge_cases_or_failure_modes: If the template is missing or empty, the Stop hook fallback still asks for `{{SUMMARY_FILE}}` (`hooks/loop-codex-stop-hook.sh:533-537`). If `SUMMARY_FILE` were not passed, the single-pass renderer would leave the placeholder intact rather than inventing a path. In finalize, this blocks before final `complete-state.md` rename; in normal rounds, it blocks before Codex review can run.
- validation_or_tests: `tests/test-finalize-phase.sh:365-380` verifies finalize exit is blocked when `finalize-summary.md` is absent and that output mentions a summary. Template loading behavior is validated by `tests/test-template-loader.sh` and reference/comprehensive validation by `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 Item Evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`