# agent_03 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-003 `directory` `hooks`
- cursor: `[_]`
- core_role: `hooks/` is the RLCR loop enforcement layer. `hooks/hooks.json:4-63` wires Claude hook events to validators: `UserPromptSubmit` to `loop-plan-file-validator.sh`, `PreToolUse` to Read/Write/Edit/Bash validators, and `Stop` to `loop-codex-stop-hook.sh` with a long timeout. The directory owns active-loop detection, state-file protection, plan integrity gates, cancel authorization, finalize-phase transitions, Codex review orchestration, and template-rendered block messages.
- algorithmic_behavior: Shared state logic lives in `hooks/lib/loop-common.sh`: constants for state fields and markers at `15-40`, newest active loop detection at `63-81`, frontmatter parsing at `112-139`, round-file helpers at `146-188`, state/finalize/goal path detectors at `250-272`, cancel authorization at `274-413`, shell modification pattern detection at `421-449`, and terminal state renaming via `end_loop` at `465-498`. Template loading is centralized in `hooks/lib/template-loader.sh`: template root resolution at `24-31`, template load at `36-48`, single-pass placeholder rendering at `58-132`, fallback rendering at `170-203`, and template directory validation at `205-222`. `hooks/check-todos-from-transcript.py:20-129` scans JSONL transcripts for the most recent `TodoWrite` call and blocks unfinished todos.
- inputs_outputs_state: Inputs are hook JSON from stdin, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `plan.md` backup, original plan path, git status, Claude transcript JSONL, tool file paths/commands, and prompt templates. Outputs are hook decisions: validators mostly return exit `0` to allow or exit `2` with markdown on stderr to block; UserPromptSubmit and Stop hooks emit JSON block decisions on stdout. State transitions include `state.md` to `finalize-state.md` on Codex `COMPLETE` in `hooks/loop-codex-stop-hook.sh:961-1021`, `finalize-state.md` to `complete-state.md` at `651-657`, and `state.md` to reason-specific terminal state through `end_loop`.
- gates_or_invariants: Read validation blocks todos access unless allowlisted and forces current-round prompt/summary reads from the active loop dir (`hooks/loop-read-validator.sh:36-44`, `100-146`). Write validation blocks todos, prompts, `state.md`, `finalize-state.md`, loop `plan.md`, and post-round-0 goal tracker writes, while explicitly allowing active-loop `finalize-summary.md` (`hooks/loop-write-validator.sh:37-50`, `105-149`, `121-126`, `181-216`). Edit validation applies the same protection to edits (`hooks/loop-edit-validator.sh:36-49`, `86-117`, `124-150`). Bash validation prevents bypasses through redirection, `tee`, in-place edits, `mv`/`cp`, wrapped shell commands, and unauthorized state moves (`hooks/loop-bash-validator.sh:63-73`, `87-104`, `117-276`, `284-341`). UserPromptSubmit blocks outdated schema, branch switching, and plan tracking drift (`hooks/loop-plan-file-validator.sh:56-86`, `92-112`, `120-226`). Stop hook blocks if schema, branch, plan integrity, todos, git cleanliness, summary, goal-tracker, Codex execution, or review-result gates fail (`hooks/loop-codex-stop-hook.sh:102-250`, `258-307`, `319-516`, `522-657`, `755-950`).
- dependencies_and_callers: `hooks/hooks.json` is the caller registry. Shell hooks depend on `jq`, `git`, `python3`, `codex`, `scripts/portable-timeout.sh`, `prompt-template/block/**`, `prompt-template/claude/**`, and `prompt-template/codex/**`. Cancel flow is specified by `commands/cancel-rlcr-loop.md:28-45`, which requires creating `.cancel-requested` before renaming `state.md` or `finalize-state.md` to `cancel-state.md`; the Bash validator authorizes only that exact signal-gated move via `is_cancel_authorized`.
- edge_cases_or_failure_modes: `find_active_loop` intentionally inspects only the newest timestamped loop dir, so ended newer loops prevent older loops from reviving (`hooks/lib/loop-common.sh:58-81`). `finalize-state.md` must be checked before generic `state.md` matching because `finalize-state.md` also contains `state.md` as a suffix (`hooks/loop-write-validator.sh:101-113`, `hooks/loop-edit-validator.sh:81-94`, `hooks/loop-bash-validator.sh:76-104`). Cancel authorization rejects command substitution, backticks, newlines, shell operators, extra args, hidden variables, wrong source/destination, and wrong signal location (`hooks/lib/loop-common.sh:290-410`). Codex failures, missing/empty review files, dirty git state, unpushed commits when configured, plan deletion/modification, incomplete todos, and large changed files all block rather than allowing exit.
- validation_or_tests: Directory behavior is exercised by the aggregate suite list in `tests/run-all-tests.sh:32-47`, including plan validation, state naming, plan hooks, finalize phase, and cancel-signal tests. Finalize behavior is covered by `tests/test-finalize-phase.sh:174-216`, `221-351`, `365-440`, and `460-496`; cancel signal behavior begins in `tests/test-cancel-signal-file.sh:75-215`; plan hook behavior is covered in `tests/test-plan-file-hooks.sh:91-260`. No tests were executed during this read-only research pass.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-033 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for `scripts/setup-rlcr-loop.sh` setup-time validation. It defines the accepted and rejected input space for starting an RLCR loop with a plan file, including path safety, git repository state, plan tracking policy, branch/model YAML safety, content sufficiency, and CLI option parsing.
- algorithmic_behavior: The test creates isolated temporary git repos under `TEST_DIR`, unsets `CLAUDE_PROJECT_DIR` so the setup script uses the temp cwd, mocks `codex` if unavailable, invokes `scripts/setup-rlcr-loop.sh` with specific arguments, and checks exit codes plus diagnostic substrings. Helpers and environment setup are at `tests/test-plan-file-validation.sh:14-84`. Path validation cases run at `89-272`, git repo validity at `274-323`, tracking policy at `329-426`, branch safety at `432-512`, content validation at `518-660`, CLI parsing at `667-694`, and Codex parameter validation at `700-738`.
- inputs_outputs_state: Inputs are plan-file paths, file contents, git tracking state, branch names, chmod permissions, CLI flags like `--plan-file`, `--track-plan-file`, and `--codex-model`, plus the presence of a `codex` command. Outputs are PASS/FAIL/SKIP lines and a final exit code equal to `TESTS_FAILED` (`tests/test-plan-file-validation.sh:740-749`). The tests create and discard temporary repos, plan files, `.gitignore`, branches, and mock `codex` binaries.
- gates_or_invariants: The tested setup invariants map directly to `scripts/setup-rlcr-loop.sh`: project must be a git repo with at least one commit (`211-220`); plan path must be relative, no spaces, no shell metacharacters, not a symlink, existing/readable, resolvable inside project, and not inside a submodule (`227-305`); plan tracking must match `--track-plan-file` and be clean when tracked (`311-356`); content must have at least five lines and at least three meaningful non-comment lines (`362-415`); branch/model/effort values must be YAML-safe (`435-460`). CLI mutually excludes `--plan-file` and a positional path (`scripts/setup-rlcr-loop.sh:188-195`).
- dependencies_and_callers: Depends on `git`, `chmod`, `grep`, `mktemp`, the repository script `scripts/setup-rlcr-loop.sh`, and optionally a mocked `codex` command (`tests/test-plan-file-validation.sh:73-84`). The file is part of the overall test runner via `tests/run-all-tests.sh:32-47`.
- edge_cases_or_failure_modes: The test accounts for platform/environment variance: permission-denial testing is skipped if `chmod 000` fails (`tests/test-plan-file-validation.sh:226-242`), and unsafe branch-name tests pass if git itself rejects the branch before the setup script runs (`461-475`, `481-493`, `499-511`). Acceptance tests for valid content and valid Codex model assert only that the targeted validation error is absent, allowing later setup gates to fail for unrelated reasons (`626-634`, `729-738`). The metacharacter loop records a failure on first unexpected diagnostic and then still reaches the summary counter (`179-187`).
- validation_or_tests: This file is itself the validator. It covers setup behavior for absolute paths, missing files/directories, spaces, shell metacharacters, symlinks, unreadable dirs, project escapes, non-git repos, no-commit repos, tracked/untracked/modified plan mismatch, YAML-unsafe branch names, blank/sparse/comment-only plans, single-line HTML comment handling, `--plan-file` parsing, conflicting path inputs, and Codex model/effort character validation. No execution was performed during this research pass.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-063 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: This is a block-message template used when a user tries to read a round prompt or summary from outside the active `.humanize/rlcr/<timestamp>/` loop directory. It contributes to the read validator’s stale/wrong-location file-access gate.
- algorithmic_behavior: The template renders `{{FILE_PATH}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{CURRENT_ROUND}}` into a markdown block titled “Wrong File Location” (`prompt-template/block/wrong-file-location.md:1-9`). It tells the user where the current round prompt and summary live and provides a direct `cat` command for the attempted file.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-read-validator.sh:100-109` after it detects a prompt/summary read outside `.humanize/rlcr`. Output is markdown written to stderr through `load_and_render_safe`; there is no persistent state transition in this template itself.
- gates_or_invariants: The associated invariant is that `round-N-prompt.md` and `round-N-summary.md` reads must target the active loop dir and current round unless explicitly allowlisted. The validator computes `CORRECT_PATH` and blocks with this template when `IN_HUMANIZE_LOOP_DIR` is false (`hooks/loop-read-validator.sh:96-110`), then separately enforces current round and exact active-directory path (`116-146`).
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh` through `hooks/lib/template-loader.sh`. Rendering is single-pass, so variable values containing `{{...}}` are not recursively expanded (`hooks/lib/template-loader.sh:54-132`).
- edge_cases_or_failure_modes: If this file is missing or renders empty, the read validator falls back to an inline “Wrong File Location” message (`hooks/loop-read-validator.sh:102-105`). The final line recommends `cat {{FILE_PATH}}`; that is a deliberate escape hatch for reading the exact attempted file, but it means the UX message can point from a blocked Read action to a Bash read command. Bash write protections still apply separately.
- validation_or_tests: Template reference and rendering behavior are indirectly covered by template-loader/template-reference suites in `tests/run-all-tests.sh:32-47`; read-path behavior is also exercised by finalize/read validator coverage in `tests/test-finalize-phase.sh:639-650`. No test execution was performed during this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique assigned item sections present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`