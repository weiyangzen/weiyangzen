# agent_13 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-013 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command entrypoint for starting the RLCR iterative development/review loop. It is a thin command definition that exposes `/humanize:start-rlcr-loop`, constrains the allowed tool to `scripts/setup-rlcr-loop.sh`, and delegates all setup/state-machine initialization to that script (`commands/start-rlcr-loop.md:1-14`).
- algorithmic_behavior: The command describes the loop as: implementation from a plan, summary emission, Codex review on attempted exit, continuation when Codex finds issues, and termination when Codex outputs `COMPLETE` (`commands/start-rlcr-loop.md:16-23`). It also defines the Goal Tracker protocol: immutable goal/acceptance criteria initialized in Round 0, mutable task/defer/evolution tracking, and periodic full alignment audits at rounds 4, 9, 14, etc. (`commands/start-rlcr-loop.md:24-41`). The delegated setup script parses options, validates prerequisites, creates `.humanize/rlcr/<timestamp>/`, copies the plan, writes `state.md`, creates `goal-tracker.md`, and emits `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:104-180`, `scripts/setup-rlcr-loop.sh:467-507`, `scripts/setup-rlcr-loop.sh:600-653`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` passed to `setup-rlcr-loop.sh`, with plan path and optional `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, `--plan-file`, and `--track-plan-file` (`commands/start-rlcr-loop.md:3`, `scripts/setup-rlcr-loop.sh:43-64`). Outputs are local loop artifacts under `.humanize/rlcr/<timestamp>/`: `state.md` with `current_round`, max iteration, Codex config, plan path/tracking, branch, and timestamp (`scripts/setup-rlcr-loop.sh:485-498`); `goal-tracker.md` with immutable and mutable sections (`scripts/setup-rlcr-loop.sh:504-594`); and `round-0-prompt.md` plus the initial prompt printed to the caller (`scripts/setup-rlcr-loop.sh:600-653`, `scripts/setup-rlcr-loop.sh:667-720`). State transitions after setup are handled by the stop hook: normal feedback increments `current_round` and writes the next prompt (`hooks/loop-codex-stop-hook.sh:1057-1123`); `COMPLETE` renames `state.md` to `finalize-state.md` and blocks with a finalization prompt (`hooks/loop-codex-stop-hook.sh:961-1020`); finalization later renames to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:651-656`); `STOP` terminates as stop state (`hooks/loop-codex-stop-hook.sh:1023-1050`).
- gates_or_invariants: The command itself says users must write summaries, maintain `goal-tracker.md`, include implementation details/tests, and not cheat by editing state or running cancel commands (`commands/start-rlcr-loop.md:43-49`). The setup script enforces: git repository with at least one commit (`scripts/setup-rlcr-loop.sh:211-221`); relative plan path only, no spaces, no shell metacharacters, no symlink, inside project, not in submodule (`scripts/setup-rlcr-loop.sh:227-305`); plan tracking policy depending on `--track-plan-file` (`scripts/setup-rlcr-loop.sh:311-356`); minimum plan size and content (`scripts/setup-rlcr-loop.sh:362-415`); Codex availability (`scripts/setup-rlcr-loop.sh:417-423`); YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:429-461`). The command help has a notable contract mismatch: it says `COMPLETE` may mean all ACs met or validly deferred (`commands/start-rlcr-loop.md:53-55`), while Codex review templates require no deferrals for `COMPLETE` (`prompt-template/codex/regular-review.md:53-56`, `prompt-template/codex/full-alignment-review.md:88-93`).
- dependencies_and_callers: Direct dependency is `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:12-14`). Runtime coordination depends on hooks under `hooks/`, especially `loop-codex-stop-hook.sh`, and template loading via `hooks/lib/template-loader.sh` with single-pass `{{VAR}}` rendering (`hooks/lib/template-loader.sh:7-20`, `hooks/lib/template-loader.sh:50-132`). Related user escape hatch is `/humanize:cancel-rlcr-loop` (`commands/start-rlcr-loop.md:55`).
- edge_cases_or_failure_modes: Empty or missing plan errors; multiple plan inputs; absolute/escaping/symlink/submodule paths; spaces and shell metacharacters; tracked plan without `--track-plan-file`; untracked or dirty plan with `--track-plan-file`; insufficient content after ignoring comments; missing `codex`; unsafe branch/model/effort characters; max iteration handling can override a successful `COMPLETE` by ending as max-iteration instead of entering finalize (`hooks/loop-codex-stop-hook.sh:961-969`).
- validation_or_tests: Plan validation is covered by `tests/test-plan-file-validation.sh`, including rejection of absolute paths, paths/filenames with spaces, shell metacharacters, tracked/untracked policy violations, insufficient content, and Codex model validation (`tests/test-plan-file-validation.sh:93-187`, `tests/test-plan-file-validation.sh:329-423`, `tests/test-plan-file-validation.sh:540-660`, `tests/test-plan-file-validation.sh:706-730`). Finalize transition behavior for `COMPLETE` is covered in `tests/test-finalize-phase.sh:443-496`, including `state.md -> finalize-state.md` and max-iteration skip.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-043 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Specialized prompt block inserted into the git-clean exit gate when `.humanize*` local loop artifacts are detected as untracked. It prevents RLCR runtime state from accidentally entering commits and tells the worker to ignore these artifacts via `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`).
- algorithmic_behavior: The block is loaded when `loop-codex-stop-hook.sh` sees untracked paths matching `.humanize` in cached git status (`hooks/loop-codex-stop-hook.sh:430-448`). It is appended to `SPECIAL_NOTES`, then rendered into the broader `block/git-not-clean.md` template, which blocks exit until uncommitted changes are committed or ignored (`hooks/loop-codex-stop-hook.sh:461-482`, `prompt-template/block/git-not-clean.md:1-16`). The suggested remediation is `echo '.humanize*' >> .gitignore` and staging `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:3-8`).
- inputs_outputs_state: Input is the stop hook’s cached `git status --porcelain` result, specifically `??` untracked lines containing `.humanize` (`hooks/loop-codex-stop-hook.sh:434-443`). Output is not a file mutation by the template itself; it is a Markdown note embedded in a JSON hook response with `"decision": "block"` and a system message telling the agent to commit first (`hooks/loop-codex-stop-hook.sh:474-481`). State remains active; the loop does not advance to Codex review while the git-clean gate is failing.
- gates_or_invariants: Invariant: `.humanize/` is RLCR-local loop state and should not be committed (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). The broader git-clean gate requires all uncommitted changes to be handled before exit/review (`hooks/loop-codex-stop-hook.sh:461-482`). This block distinguishes sanctioned local loop artifacts from other untracked files, but it still participates in a blocking dirty-worktree decision.
- dependencies_and_callers: Called only through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` in `hooks/loop-codex-stop-hook.sh:441-447`. It is composed with `prompt-template/block/git-not-clean.md`, whose `{{SPECIAL_NOTES}}` placeholder receives this note (`prompt-template/block/git-not-clean.md:3-4`). Template loading behavior is supplied by `hooks/lib/template-loader.sh:33-48`.
- edge_cases_or_failure_modes: Detection is broad: any untracked path containing `.humanize` triggers this note, including legacy `.humanize-*` names per hook comment (`hooks/loop-codex-stop-hook.sh:441`). If the template is missing or empty, the hook falls back to `Note: .humanize* directories are intentionally untracked.` (`hooks/loop-codex-stop-hook.sh:443-446`). The template suggests appending `.humanize*` every time; repeated use could duplicate `.gitignore` lines unless the agent checks first. The hook also separately flags other untracked files via `git-not-clean-untracked.md` (`hooks/loop-codex-stop-hook.sh:450-458`).
- validation_or_tests: No direct test was found that asserts this exact template content. Related tests establish `.humanize*` ignore behavior in plan-file hook scenarios (`tests/test-plan-file-hooks.sh:748`, `tests/test-plan-file-hooks.sh:819`, `tests/test-plan-file-hooks.sh:888`, `tests/test-plan-file-hooks.sh:955`) and finalize-phase setup includes `.humanize*` in `.gitignore` (`tests/test-finalize-phase.sh:109`). Template loading itself is covered by `tests/test-templates-comprehensive.sh`, including real-template rendering and directory validation (`tests/test-templates-comprehensive.sh:492-540`).
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-073 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex review prompt for mandatory full goal-alignment checkpoints. It upgrades a normal round review into a comprehensive audit of original plan coverage, goal-tracker correctness, deferred work, implementation truthfulness, and stagnation/circuit-breaker conditions (`prompt-template/codex/full-alignment-review.md:1-4`, `prompt-template/codex/full-alignment-review.md:19-93`).
- algorithmic_behavior: Rendered every fifth round using zero-based round math: when `CURRENT_ROUND % 5 == 4`, the stop hook chooses this template rather than `regular-review.md` (`hooks/loop-codex-stop-hook.sh:683-687`, `hooks/loop-codex-stop-hook.sh:718-731`). The template forces Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), audit each immutable acceptance criterion (`prompt-template/codex/full-alignment-review.md:19-29`), compare the plan to the goal tracker for forgotten or falsely completed work (`prompt-template/codex/full-alignment-review.md:30-35`), reevaluate deferrals (`prompt-template/codex/full-alignment-review.md:36-41`), review implementation claims against code/design docs (`prompt-template/codex/full-alignment-review.md:50-56`), process goal-tracker update instructions (`prompt-template/codex/full-alignment-review.md:57`), then inspect historical round summaries/reviews for stagnation (`prompt-template/codex/full-alignment-review.md:59-85`). Output semantics are strict: findings go to `@{{REVIEW_RESULT_FILE}}`; `STOP` as final line means stagnation; `COMPLETE` as final line is allowed only when all original ACs are fully met with no deferrals (`prompt-template/codex/full-alignment-review.md:86-93`).
- inputs_outputs_state: Placeholders are supplied by `loop-codex-stop-hook.sh`: current round, plan file, summary content, goal tracker file, docs path, goal tracker update section, completed iteration count, loop timestamp, previous rounds, and review result path (`hooks/loop-codex-stop-hook.sh:718-731`). Inputs also include historical files in `.humanize/rlcr/{{LOOP_TIMESTAMP}}/round-N-*` referenced by the template (`prompt-template/codex/full-alignment-review.md:61-74`). The stop hook writes the rendered prompt to `round-${CURRENT_ROUND}-review-prompt.md`, runs `codex exec`, expects or copies output into `round-${CURRENT_ROUND}-review-result.md`, then reads the last non-empty line to decide state transition (`hooks/loop-codex-stop-hook.sh:671-675`, `hooks/loop-codex-stop-hook.sh:813-840`, `hooks/loop-codex-stop-hook.sh:897-960`).
- gates_or_invariants: Mandatory plan-first read; acceptance criteria in the immutable section are source-of-truth; deferred ACs remain incomplete; stale or invalid deferrals must not be treated as success; `COMPLETE` must be the last line and is valid only with all ACs fully met and no deferrals (`prompt-template/codex/full-alignment-review.md:7-10`, `prompt-template/codex/full-alignment-review.md:23-48`, `prompt-template/codex/full-alignment-review.md:88-93`). `STOP` is reserved for stagnation and must be final-line exactness from Codex output (`prompt-template/codex/full-alignment-review.md:84-90`, `hooks/loop-codex-stop-hook.sh:955-960`). The stop hook enforces exact last-line matching for both `COMPLETE` and `STOP`, avoiding false positives such as “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:955-960`).
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`, which selects this template on full-alignment rounds and passes values through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:677-731`). Rendering uses `hooks/lib/template-loader.sh`, whose single-pass substitution prevents nested placeholders inside variable values from being expanded (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-132`). It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, inserted into Part 3 (`hooks/loop-codex-stop-hook.sh:677-681`, `prompt-template/codex/full-alignment-review.md:57`), and with regular review behavior for non-alignment rounds (`prompt-template/codex/regular-review.md:1-57`).
- edge_cases_or_failure_modes: `PREV_PREV_ROUND` can be negative only for early rounds, but the template is selected at rounds 4, 9, 14, so normal use has valid prior rounds (`hooks/loop-codex-stop-hook.sh:683-693`). If Codex exits nonzero, fails to create the result file, or writes an empty result, the hook blocks and surfaces debug files (`hooks/loop-codex-stop-hook.sh:850-950`). If Codex writes `COMPLETE` at max iteration, the hook ends as max-iteration rather than finalize (`hooks/loop-codex-stop-hook.sh:961-969`). If `STOP` appears on a non-alignment round, the hook still honors it but reports it as unusual; for this template’s normal path, it is treated as circuit breaker (`hooks/loop-codex-stop-hook.sh:1023-1050`). There is a wording mismatch with `commands/start-rlcr-loop.md`, whose stopping summary allows “validly deferred” ACs, while this template forbids all deferrals for `COMPLETE`.
- validation_or_tests: Template rendering infrastructure is tested in `tests/test-templates-comprehensive.sh`, including real template rendering and fallback/validation behavior (`tests/test-templates-comprehensive.sh:492-540`). Final-line `COMPLETE` handling and transition to `finalize-state.md` are tested in `tests/test-finalize-phase.sh:443-496`; normal non-`COMPLETE` review keeps `state.md` and increments the round (`tests/test-finalize-phase.sh:547-596`). I did not find a dedicated test asserting that this exact full-alignment template includes the stagnation checklist or no-deferrals completion rule.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item evidence sections present, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`