# agent_29 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-029 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the cancel signal-file exception in the RLCR loop Bash hook. The file verifies that normal state-file mutation remains blocked, while the deliberate cancel flow can rename the active loop `state.md` or `finalize-state.md` into `cancel-state.md` only after a colocated `.cancel-requested` file exists. The intent is stated in the file header: allowed rename with signal, blocked rename without signal, blocked non-cancel state modifications, and blocked wrong-directory signal use at `tests/test-cancel-signal-file.sh:3`.
- algorithmic_behavior: The test creates an isolated fake project in `mktemp -d`, builds `.humanize/rlcr/2024-01-01_12-00-00/state.md`, sets `CLAUDE_PROJECT_DIR`, and sources the shared hook library at `tests/test-cancel-signal-file.sh:28` and `tests/test-cancel-signal-file.sh:42`. It simulates Claude hook input by JSON-encoding a Bash command with `jq`, piping it to `hooks/loop-bash-validator.sh`, and checking the validator exit code at `tests/test-cancel-signal-file.sh:59`. The specification exercises the actual hook path, where `loop-bash-validator.sh` parses `.tool_name`, `.tool_input.command`, finds the active loop, parses state, and calls `is_cancel_authorized` before allowing otherwise-blocked state-file commands at `hooks/loop-bash-validator.sh:22`, `hooks/loop-bash-validator.sh:36`, and `hooks/loop-bash-validator.sh:87`.
- inputs_outputs_state: Inputs are shell command strings embedded as hook JSON, the active loop directory under `CLAUDE_PROJECT_DIR`, the presence or absence of `$LOOP_DIR/.cancel-requested`, and the loop state file frontmatter. Expected outputs are exit `0` for authorized/pass-through commands and exit `2` for blocked hook commands. The test state transition under scrutiny is not the actual rename execution but the pre-tool authorization transition: a Bash `mv <active-loop>/state.md <active-loop>/cancel-state.md` command changes from blocked to allowed only when `.cancel-requested` exists in that same active loop directory, as shown by positive tests at `tests/test-cancel-signal-file.sh:78`, `tests/test-cancel-signal-file.sh:98`, `tests/test-cancel-signal-file.sh:363`, and `tests/test-cancel-signal-file.sh:468`.
- gates_or_invariants: Key invariant: the signal file is an authorization token for one exact operation, not a general unlock. The validator still blocks `echo > state.md`, `sed -i`, wrong destination, `rm`, arbitrary destination, `cp`, extra arguments, unrelated `/tmp/state.md`, and command wrappers/chaining even when `.cancel-requested` exists, covered at `tests/test-cancel-signal-file.sh:139`, `tests/test-cancel-signal-file.sh:159`, `tests/test-cancel-signal-file.sh:179`, `tests/test-cancel-signal-file.sh:221`, `tests/test-cancel-signal-file.sh:281`, `tests/test-cancel-signal-file.sh:404`, `tests/test-cancel-signal-file.sh:424`, and `tests/test-cancel-signal-file.sh:1210`. The underlying invariant is implemented in `is_cancel_authorized`: signal file must exist, command substitution/backticks/newlines/chaining are rejected, only `$loop_dir`/`${loop_dir}` is normalized, remaining `$` variables are rejected, command must start with `mv`, exactly two args must remain, source must be active-loop `state.md` or `finalize-state.md`, and destination must be active-loop `cancel-state.md` at `hooks/lib/loop-common.sh:288`, `hooks/lib/loop-common.sh:295`, `hooks/lib/loop-common.sh:300`, `hooks/lib/loop-common.sh:305`, `hooks/lib/loop-common.sh:310`, `hooks/lib/loop-common.sh:323`, `hooks/lib/loop-common.sh:328`, `hooks/lib/loop-common.sh:378`, `hooks/lib/loop-common.sh:396`, and `hooks/lib/loop-common.sh:405`.
- dependencies_and_callers: Direct dependencies are Bash, `jq`, `mktemp`, `sed`, `grep`, `tr`, `hooks/lib/loop-common.sh`, and `hooks/loop-bash-validator.sh`. `loop-bash-validator.sh` depends on `find_active_loop`, `parse_state_file`, `command_modifies_file`, state/finalize block messages, and `is_cancel_authorized` from `hooks/lib/loop-common.sh`. The related user-facing cancel command explicitly instructs the two-step flow: create `.cancel-requested`, then rename `state.md` or `finalize-state.md` to `cancel-state.md` at `commands/cancel-rlcr-loop.md:30` and `commands/cancel-rlcr-loop.md:43`. The validator’s state-file protection is broader than cancel: it also blocks plan backup, goal tracker, prompt, summary, and todos Bash modifications after the cancel/state checks at `hooks/loop-bash-validator.sh:278`, `hooks/loop-bash-validator.sh:291`, `hooks/loop-bash-validator.sh:308`, `hooks/loop-bash-validator.sh:318`, and `hooks/loop-bash-validator.sh:329`.
- edge_cases_or_failure_modes: The test is security-heavy. It covers wrong signal directory at `tests/test-cancel-signal-file.sh:199`, command chaining with `&&`, `;`, `||`, pipe, `|&`, background `&` at `tests/test-cancel-signal-file.sh:241`, `tests/test-cancel-signal-file.sh:261`, `tests/test-cancel-signal-file.sh:670`, `tests/test-cancel-signal-file.sh:690`, `tests/test-cancel-signal-file.sh:710`, `tests/test-cancel-signal-file.sh:790`, and `tests/test-cancel-signal-file.sh:730`; command substitution and backticks at `tests/test-cancel-signal-file.sh:301` and `tests/test-cancel-signal-file.sh:321`; newline injection via direct helper test at `tests/test-cancel-signal-file.sh:343`; options/prefixes such as `mv --`, `sudo`, `env`, `command --`, and `sh -c`/`bash -c` at `tests/test-cancel-signal-file.sh:384`, `tests/test-cancel-signal-file.sh:550`, `tests/test-cancel-signal-file.sh:590`, `tests/test-cancel-signal-file.sh:630`, `tests/test-cancel-signal-file.sh:810`, and `tests/test-cancel-signal-file.sh:830`; redirection-prefixed bypasses with quoted, escaped-space, and ANSI-C targets at `tests/test-cancel-signal-file.sh:850`, `tests/test-cancel-signal-file.sh:930`, `tests/test-cancel-signal-file.sh:1030`, `tests/test-cancel-signal-file.sh:1110`, and `tests/test-cancel-signal-file.sh:1150`. It also confirms the validator allows commands when no active `state.md` exists at `tests/test-cancel-signal-file.sh:1231`.
- validation_or_tests: This file is itself the validation. It uses `pass`/`fail` counters at `tests/test-cancel-signal-file.sh:25`, runs all cases sequentially, prints aggregate results at `tests/test-cancel-signal-file.sh:1347`, and exits with `TESTS_FAILED` at `tests/test-cancel-signal-file.sh:1355`. It includes both end-to-end validator tests and direct helper tests for `is_cancel_authorized`, beginning at `tests/test-cancel-signal-file.sh:1250`. I inspected the file and related implementation paths only; I did not execute the test because this task requested read-only research notes.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-059 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR hook policy that forbids direct file access to `round-*-todos.md` and redirects agents to native TodoWrite state tracking. The full template is eight lines and contains the blocking title, the prohibited file pattern, the required alternate tool, and rationale at `prompt-template/block/todos-file-access.md:1`.
- algorithmic_behavior: The template has no executable logic itself; its algorithmic role is declarative policy text rendered by hook validators when a todos-file access gate fails. `todos_blocked_message` in `hooks/lib/loop-common.sh` loads this template through `load_and_render_safe`, falling back to equivalent inline text if the template cannot be loaded at `hooks/lib/loop-common.sh:190`. That makes the template part of the error-output contract for read/write/edit/bash validators.
- inputs_outputs_state: Input is the validator decision that a tool action targets a `round-*-todos.md` file outside the narrow allowlist. Output is a Markdown block telling the agent: “Do NOT create or access `round-*-todos.md` files” and “Use the native TodoWrite tool instead” at `prompt-template/block/todos-file-access.md:3` and `prompt-template/block/todos-file-access.md:5`. No persistent state is mutated by the template; state tracking is intentionally delegated to TodoWrite, described at `prompt-template/block/todos-file-access.md:7`.
- gates_or_invariants: The invariant is that round todos files are not the authoritative task state for agents during RLCR interaction; native TodoWrite is. The read validator blocks `Read` on todos files unless there is no active-loop issue or the file is allowlisted at `hooks/loop-read-validator.sh:32`; the write validator blocks `Write` to todos files under the same helper path at `hooks/loop-write-validator.sh:33`; the bash validator blocks shell modifications to `round-[0-9]+-todos.md` unless the command targets an allowlisted active-loop path at `hooks/loop-bash-validator.sh:329`. The common allowlist currently includes `round-1-todos.md` and `round-2-todos.md` plus selected summaries at `hooks/lib/loop-common.sh:167`.
- dependencies_and_callers: Direct caller is `todos_blocked_message` in `hooks/lib/loop-common.sh`, which is used by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`. Search references also show `tests/test-template-references.sh` expects `block/todos-file-access.md` to exist at `tests/test-template-references.sh:153`, so the template participates in template coverage. The policy coordinates with `hooks/check-todos-from-transcript.py`, which reads transcript JSONL and finds the most recent TodoWrite tool call, giving the system a native todo-state path rather than filesystem round todos.
- edge_cases_or_failure_modes: If this template is missing or malformed, validators still have a fallback message in `todos_blocked_message`, but the user-facing wording may drift from the prompt-template contract. If the allowlist is too broad, agents could access stale or wrong todos files despite this policy; if too narrow, permitted bootstrap or compatibility paths could be blocked. The Bash gate includes an additional active-loop full-path check to avoid same-basename bypasses from other roots at `hooks/loop-bash-validator.sh:333`.
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh` via the listed template reference. Functional coverage is distributed across hook validator behavior: read blocks todos access through `loop-read-validator.sh`, write/edit through their validators, and Bash through `command_modifies_file` plus `todos_blocked_message`. I inspected the template and its callers only; I did not run the test suite.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item sections
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`