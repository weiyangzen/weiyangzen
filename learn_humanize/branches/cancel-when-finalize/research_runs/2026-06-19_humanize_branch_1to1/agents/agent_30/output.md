# agent_30 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-030 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable error-path specification for the prompt-template loading/rendering algorithm in `hooks/lib/template-loader.sh`. It verifies that missing templates, invalid template directories, malformed placeholders, strict-mode callers, and fallback rendering degrade predictably instead of crashing hook execution.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` at `tests/test-error-scenarios.sh:13`, resolves `TEMPLATE_DIR` through `get_template_dir` at `tests/test-error-scenarios.sh:15`, then runs 12 sequential assertions. The test harness increments `TESTS_PASSED`/`TESTS_FAILED` through `pass` and `fail` helpers at `tests/test-error-scenarios.sh:25-35`, reports a summary at `tests/test-error-scenarios.sh:211-216`, and exits success only when `TESTS_FAILED` is zero at `tests/test-error-scenarios.sh:218-225`.
- inputs_outputs_state: Inputs are the project root, `prompt-template` directory, template names, inline template strings, and variable assignments such as `PATH=...` and `FILE=test.md`. Outputs are colored PASS/FAIL lines, a final pass/fail count, and process exit code `0` or `1`. Internal state is limited to shell variables and counters; the file performs no repository writes.
- gates_or_invariants: Missing template files and directories must return empty output with exit code `0` for `load_template` and `load_and_render` (`tests/test-error-scenarios.sh:42-78`). `render_template` must return empty output for empty content (`tests/test-error-scenarios.sh:80-91`). Special regex-like characters must render literally (`tests/test-error-scenarios.sh:93-106`). A strict `set -euo pipefail` subshell must continue after a missing template (`tests/test-error-scenarios.sh:108-131`). `load_and_render_safe` must use fallback text, including variable substitution inside fallback text (`tests/test-error-scenarios.sh:133-158`). `validate_template_dir` must accept a valid template tree and reject a missing directory (`tests/test-error-scenarios.sh:160-180`). Empty and unclosed placeholders must remain unchanged (`tests/test-error-scenarios.sh:182-206`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, especially `get_template_dir` (`hooks/lib/template-loader.sh:26-31`), `load_template` (`hooks/lib/template-loader.sh:36-48`), `render_template` (`hooks/lib/template-loader.sh:58-132`), `load_and_render` (`hooks/lib/template-loader.sh:136-147`), `load_and_render_safe` (`hooks/lib/template-loader.sh:170-203`), and `validate_template_dir` (`hooks/lib/template-loader.sh:208-222`). The suite is included in the aggregate runner at `tests/run-all-tests.sh:32-47`, specifically `tests/run-all-tests.sh:42`.
- edge_cases_or_failure_modes: Covered edge cases are missing files, missing directories, empty content, regex metacharacters in replacement values, strict shell mode, fallback paths, invalid template roots, empty placeholders, and unterminated placeholders. A local execution attempt in this read-only branch export failed 4 render-related assertions because Bash could not create the temporary file backing the here-string used at `hooks/lib/template-loader.sh:129` (`Operation not permitted`). That is an environment restriction observed during validation, not a file modification.
- validation_or_tests: Ran `bash tests/test-error-scenarios.sh` from the assigned branch export. Result: 8 passed, 4 failed. Failures were `Special regex characters`, fallback variable substitution, empty placeholder preservation, and unclosed placeholder preservation, all correlated with the sandbox temp-file error from `render_template` before expected output could be produced. The script itself correctly aggregated and returned exit `1`.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-060 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: User-facing block template for the stop/finalize gate that prevents loop exit when `--push-every-round` is enabled and the local branch has commits ahead of its remote. Although it is Markdown, it defines an algorithmic transition contract: unpushed commits move an attempted stop into a blocked decision with a required push action.
- algorithmic_behavior: The template has two placeholders: `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:3` and `prompt-template/block/unpushed-commits.md:9`. When rendered, it tells the agent that stop was attempted with unpushed commits, explains that `--push-every-round` requires pushing first, and emits the concrete command `git push origin {{CURRENT_BRANCH}}`.
- inputs_outputs_state: Inputs are `AHEAD_COUNT`, derived from `git status -sb` ahead metadata in `hooks/loop-codex-stop-hook.sh:491-493`, and `CURRENT_BRANCH`, derived from `git rev-parse --abbrev-ref HEAD` in `hooks/loop-codex-stop-hook.sh:494`. Output is rendered Markdown assigned to the stop-hook JSON `reason` field at `hooks/loop-codex-stop-hook.sh:501-511`. The hook exits after emitting a block decision at `hooks/loop-codex-stop-hook.sh:513`, so the state transition is “exit attempt blocked until push is completed,” not “loop completed.”
- gates_or_invariants: The gate only runs when parsed loop state has `PUSH_EVERY_ROUND=true` (`hooks/loop-codex-stop-hook.sh:489`). The `push_every_round` state field is created by setup at `scripts/setup-rlcr-loop.sh:485-493`, set by the `--push-every-round` argument at `scripts/setup-rlcr-loop.sh:148-150`, parsed from state frontmatter at `hooks/lib/loop-common.sh:121-136`, and defaults false at `hooks/lib/loop-common.sh:136`. If the branch is not ahead, the block is not emitted. The companion bash validator enforces the inverse policy: when `push_every_round` is not true, `git push` commands are blocked at `hooks/loop-bash-validator.sh:63-71`.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh:501`, via `load_and_render_safe "$TEMPLATE_DIR" "block/unpushed-commits.md" ...`. Rendering semantics come from `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}` syntax documented at `hooks/lib/template-loader.sh:7-13`, and `load_and_render_safe` provides a fallback if the template is missing or renders empty at `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: If `git status -sb` fails, times out, or does not include an `ahead N` token, `GIT_AHEAD` is empty and the block is skipped because the command is wrapped with stderr suppression and `|| true` at `hooks/loop-codex-stop-hook.sh:491`. If branch detection fails, the hook uses `unknown` as fallback branch text at `hooks/loop-codex-stop-hook.sh:494`. The rendered command is unquoted, so unusual branch names with shell-significant characters would be displayed directly. If the template file is missing, the inline fallback at `hooks/loop-codex-stop-hook.sh:496-500` preserves the block but is less explicit than the template because it does not include the exact `git push origin ...` command block.
- validation_or_tests: Template rendering is covered by `tests/test-templates-comprehensive.sh:517-528`, which renders the template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then checks for the title, the count phrase, and branch name. Template reference discovery also sees this call through the loader-reference scan in `tests/test-template-references.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 Item Evidence sections present; each assigned item id is used once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`