# agent_15 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-015 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude plugin hook dispatch table for the RLCR loop. It does not implement validation logic itself; it binds Claude lifecycle events to the shell validators that enforce the loop contract. The README lists this file under lifecycle hooks, alongside the referenced validator scripts (`README.md:224`).
- algorithmic_behavior: Registers one `UserPromptSubmit` hook for plan/branch/plan-file consistency (`hooks/hooks.json:4`), four `PreToolUse` validators keyed by tool matcher (`Write`, `Edit`, `Read`, `Bash`) (`hooks/hooks.json:14`), and one long-running `Stop` hook that runs Codex review/finalize logic (`hooks/hooks.json:52`). The command paths use `${CLAUDE_PLUGIN_ROOT}` so runtime resolution depends on the plugin install environment (`hooks/hooks.json:9`, `hooks/hooks.json:20`, `hooks/hooks.json:29`, `hooks/hooks.json:38`, `hooks/hooks.json:47`, `hooks/hooks.json:57`).
- inputs_outputs_state: Inputs are Claude Code hook events. Outputs are the subprocess results from the referenced hook scripts: `decision:block` JSON for hook-level blocks in some cases, stderr plus exit `2` for PreToolUse blocks, or exit `0` to allow flow. The JSON file itself has no persisted state; state is read/written by scripts against `.humanize/rlcr/<timestamp>/state.md`, `finalize-state.md`, and related round files.
- gates_or_invariants: The hook graph enforces the main invariant that Claude cannot bypass RLCR state-machine constraints through normal tool use: prompt/todos/state/goal-tracker/round-file access is guarded before tool execution, and stop attempts are intercepted for git cleanliness, review, and finalize completion. The `Stop` hook timeout is `7200`, which permits expensive Codex review while still bounding execution (`hooks/hooks.json:57`).
- dependencies_and_callers: Depends on Claude Code’s hook engine honoring event names and matchers. The referenced scripts share `hooks/lib/loop-common.sh`; the plan validator checks schema, branch, and plan tracking (`hooks/loop-plan-file-validator.sh:52`); Write/Edit/Read/Bash validators inspect tool JSON via `jq` (`hooks/loop-write-validator.sh:23`, `hooks/loop-edit-validator.sh:22`, `hooks/loop-read-validator.sh:22`, `hooks/loop-bash-validator.sh:22`); the stop hook performs Codex review and finalize transitions (`hooks/loop-codex-stop-hook.sh:1`).
- edge_cases_or_failure_modes: If `${CLAUDE_PLUGIN_ROOT}` is unset or wrong, none of the scripts can be found. If Claude hook matchers change names, the PreToolUse validators silently stop covering those tools. The JSON has no fallback path or conditional logic; missing hook scripts become installation/configuration failures, not recoverable state-machine decisions.
- validation_or_tests: `tests/test-template-references.sh` scans scripts that load templates and includes all validator scripts referenced by this hook graph (`tests/test-template-references.sh:56`). `tests/test-plan-file-hooks.sh` exercises plan-file validator and stop-hook behavior (`tests/test-plan-file-hooks.sh:453`, `tests/test-plan-file-hooks.sh:499`). `tests/test-finalize-phase.sh` covers stop-hook finalize behavior (`tests/test-finalize-phase.sh:372`). I inspected these tests but did not run them.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-045 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-time block template rendered when the RLCR loop detects a dirty git worktree before allowing review/finalization to proceed. It is part prompt contract, part remediation instruction, and is directly used by the stop hook’s git-clean gate.
- algorithmic_behavior: The template interpolates `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` (`prompt-template/block/git-not-clean.md:3`). It tells the agent that stopping is blocked, then requires review of untracked files, staging real changes, and committing with project conventions (`prompt-template/block/git-not-clean.md:5`). It also bans AI authorship/co-author attribution in commit messages (`prompt-template/block/git-not-clean.md:11`).
- inputs_outputs_state: Inputs are generated by `hooks/loop-codex-stop-hook.sh`: `GIT_ISSUES` is set to `uncommitted changes` when cached `git status --porcelain` is non-empty (`hooks/loop-codex-stop-hook.sh:431`, `hooks/loop-codex-stop-hook.sh:435`), and `SPECIAL_NOTES` is assembled from special untracked-file templates for `.humanize*` and other untracked paths (`hooks/loop-codex-stop-hook.sh:441`, `hooks/loop-codex-stop-hook.sh:450`). Output is rendered Markdown placed into a JSON block decision as `reason` with system message `Loop: Blocked - ...` (`hooks/loop-codex-stop-hook.sh:470`, `hooks/loop-codex-stop-hook.sh:474`).
- gates_or_invariants: Enforces “do not stop with uncommitted changes” before expensive Codex review and before finalize completion. In finalize phase, this gate still runs after finalize-summary checks, and tests expect dirty git to block exit (`tests/test-finalize-phase.sh:390`). The invariant is that loop progress/final completion requires committed work; untracked artifacts must be intentionally ignored or tracked.
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:470`). Rendering is provided by `hooks/lib/template-loader.sh`, which uses single-pass `{{VAR}}` substitution and leaves unknown placeholders intact (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:50`). Missing or empty template content falls back to an inline minimal message (`hooks/loop-codex-stop-hook.sh:464`, `hooks/lib/template-loader.sh:170`).
- edge_cases_or_failure_modes: The template assumes committing is the correct remediation; in contexts where dirty files are generated artifacts, the special notes must steer `.gitignore` behavior. If `code-simplifier` is not installed, step `0` is conditional, so absence is non-fatal (`prompt-template/block/git-not-clean.md:6`). If `SPECIAL_NOTES` concatenates multiple notes without spacing, readability depends on the referenced note templates including their own line breaks.
- validation_or_tests: `tests/test-finalize-phase.sh` validates dirty git blocking in finalize phase (`tests/test-finalize-phase.sh:397`). `tests/test-template-references.sh` verifies referenced templates exist and critical validators use safe rendering (`tests/test-template-references.sh:172`). Template loader tests cover real-template rendering patterns, though not this exact template (`tests/test-templates-comprehensive.sh:493`). I inspected these tests but did not run them.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-075 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex reviewer prompt for non-full-alignment RLCR rounds. It defines the review contract that decides whether the loop proceeds to finalize (`COMPLETE`) or feeds issues back into the next Claude iteration.
- algorithmic_behavior: Requires Codex to read the original implementation plan first (`prompt-template/codex/regular-review.md:5`), compare the plan and current round prompt against Claude’s summary (`prompt-template/codex/regular-review.md:11`), conduct a skeptical implementation review (`prompt-template/codex/regular-review.md:20`), check goal-tracker alignment (`prompt-template/codex/regular-review.md:33`), and write findings to `{{REVIEW_RESULT_FILE}}` when work is incomplete (`prompt-template/codex/regular-review.md:49`). Its terminal marker rule is strict: only output `COMPLETE` as the last line when all original plan tasks and ACs are fully complete with no deferrals or pending work (`prompt-template/codex/regular-review.md:53`).
- inputs_outputs_state: Render inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, supplied by the stop hook when not on a full-alignment round (`hooks/loop-codex-stop-hook.sh:736`). Output is the review prompt file at `.humanize/rlcr/<loop>/round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:671`). Codex must create or stdout-provide `.humanize/rlcr/<loop>/round-N-review-result.md`; the stop hook copies stdout into the expected file if necessary (`hooks/loop-codex-stop-hook.sh:899`).
- gates_or_invariants: The stop hook reads the last non-empty line and matches it exactly against `COMPLETE` or `STOP` after trimming whitespace (`hooks/loop-codex-stop-hook.sh:955`). `COMPLETE` enters Finalize Phase by renaming `state.md` to `finalize-state.md` and blocks exit with a finalize prompt (`hooks/loop-codex-stop-hook.sh:971`, `hooks/loop-codex-stop-hook.sh:980`). Any normal review result without `COMPLETE` advances `current_round`, creates the next prompt with Codex feedback, and keeps the loop active (`hooks/loop-codex-stop-hook.sh:1057`, `hooks/loop-codex-stop-hook.sh:1063`). Deferrals are explicitly treated as incomplete by the template (`prompt-template/codex/regular-review.md:24`, `prompt-template/codex/regular-review.md:54`).
- dependencies_and_callers: Used by `hooks/loop-codex-stop-hook.sh` for rounds where `CURRENT_ROUND % 5 != 4`; every fifth round uses `prompt-template/codex/full-alignment-review.md` instead (`hooks/loop-codex-stop-hook.sh:683`, `hooks/loop-codex-stop-hook.sh:718`). Depends on the template loader’s single-pass rendering (`hooks/lib/template-loader.sh:54`), on `codex exec` availability (`hooks/loop-codex-stop-hook.sh:756`), and on Codex being run in the project root with the rendered prompt as stdin (`hooks/loop-codex-stop-hook.sh:813`, `hooks/loop-codex-stop-hook.sh:839`).
- edge_cases_or_failure_modes: If Codex is missing, exits nonzero, fails to create a review file, or creates an empty file, the stop hook blocks with debug paths instead of advancing the loop (`hooks/loop-codex-stop-hook.sh:756`, `hooks/loop-codex-stop-hook.sh:878`, `hooks/loop-codex-stop-hook.sh:915`, `hooks/loop-codex-stop-hook.sh:943`). If `COMPLETE` appears anywhere except the final non-empty line, it does not finalize. If Codex emits `STOP` on a regular review, the hook treats it as unusual but honors termination (`hooks/loop-codex-stop-hook.sh:1038`). At max iterations, even a passing review terminates as max-iteration rather than entering finalize (`hooks/loop-codex-stop-hook.sh:961`).
- validation_or_tests: `tests/test-template-references.sh` includes the stop hook in template-reference scanning (`tests/test-template-references.sh:58`). `tests/test-plan-file-hooks.sh` covers stop-hook plan integrity gates before review generation (`tests/test-plan-file-hooks.sh:469`). `tests/test-finalize-phase.sh` validates the post-`COMPLETE` finalize-phase behavior and confirms finalize phase does not invoke Codex again (`tests/test-finalize-phase.sh:382`). I inspected these tests but did not run them.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item sections present; each assigned item appears once as a section heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`