# agent_27 cancel-when-finalize 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `17b776ba6dc09b935afde8f448716ab5bae73b49`

## Item Evidence

### CANCEL_WHEN_FINALIZE-HZ-027 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the aggregate test runner’s ANSI-stripping and pass/fail count extraction behavior. The file explicitly targets the portable parsing used by [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/run-all-tests.sh:66), where suite output is captured, stripped of color codes, and reduced to numeric `Passed:` / `Failed:` totals.
- algorithmic_behavior: Defines local `pass()` and `fail()` counters, then runs eight table-like shell cases against the same parsing primitives used by the runner: `esc=$'\033'`, `sed "s/${esc}\\[[0-9;]*m//g"`, and `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'` or the equivalent `Failed:` expression. The cases cover basic color stripping at [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-ansi-parsing.sh:42), multiple color spans at line 56, colored `Passed` extraction at line 70, colored `Failed` extraction at line 84, zero-count extraction at line 98, multi-line suite-summary parsing with `tail -1` at line 112, plain text without ANSI at line 133, and combined bold/color escape sequences at line 147.
- inputs_outputs_state: Inputs are synthetic shell strings containing ANSI SGR escape sequences such as `\033[0;32m`, `\033[1m`, `\033[0;31m`, and plain uncolored summaries. Outputs are printed PASS/FAIL lines plus final colored summary counts at [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-ansi-parsing.sh:163). Internal mutable state is limited to `TESTS_PASSED` and `TESTS_FAILED`; no project files are read or written by the test itself.
- gates_or_invariants: The invariant under test is that SGR ANSI sequences matching `ESC[` plus zero or more digits/semicolons plus `m` must be removed without changing adjacent payload text. Numeric extraction must tolerate `[[:space:]]*`, return the trailing integer, preserve `0` as a valid count, and choose the last summary count in multi-line output. The script exits `0` only if `TESTS_FAILED == 0`; otherwise it exits `1` at [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-ansi-parsing.sh:170).
- dependencies_and_callers: Depends on Bash ANSI-C quoting, `sed`, `grep -E`, `tail`, and normal shell arithmetic. It is included in the aggregate suite list as `test-ansi-parsing.sh` at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/run-all-tests.sh:43). Its behavior mirrors the aggregate runner’s capture and parse block at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/run-all-tests.sh:60), especially ANSI stripping at line 68 and `Passed` / `Failed` extraction at lines 69-70.
- edge_cases_or_failure_modes: It validates the intended GNU/BSD portable path by avoiding non-portable sed escape notation and using `$'\033'`. It covers multiple SGR codes in one line, bold plus color, no ANSI codes, zero failures, and multi-line output. It does not cover non-SGR ANSI sequences, color codes using colon notation, erase-line/cursor movement sequences, lowercase summary labels, negative or decimal counts, or malformed escape sequences. Because `set -e` is not enabled, failed assertions accumulate rather than aborting early, which is intentional for a test summary.
- validation_or_tests: The file is itself a validation suite. The aggregate runner relies on the same parsing logic and sums extracted values into `TOTAL_PASSED` / `TOTAL_FAILED` at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/run-all-tests.sh:72). I inspected the file directly with line numbering. I did not execute it because this branch export is read-only and the assignment requested research notes only.
- skip_candidate: `no`

### CANCEL_WHEN_FINALIZE-HZ-057 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template emitted when a validator blocks attempts to modify `state.md`, the normal-loop control file. It defines the user-facing enforcement contract for the invariant that loop state is system-managed and must not be edited by the agent or user tool calls.
- algorithmic_behavior: The template is static Markdown with a title and short rationale. It states that `state.md` cannot be modified because it is managed by the loop system, and identifies the state fields: current round number, max iterations, and Codex configuration at [prompt-template/block/state-file-modification.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/prompt-template/block/state-file-modification.md:5). The algorithmic action happens in callers: `state_file_blocked_message()` loads this template via `load_and_render_safe "$TEMPLATE_DIR" "block/state-file-modification.md"` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/lib/loop-common.sh:211).
- inputs_outputs_state: The template has no placeholders, so it takes no render variables. Its output is the rendered Markdown warning written to stderr by validators before they exit with a blocking code. It protects `state.md`, whose parsed frontmatter feeds fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, and `codex_timeout` as defined in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/lib/loop-common.sh:15). The template itself does not mutate state; it documents why mutation is rejected.
- gates_or_invariants: The central invariant is that normal-loop `state.md` is not a user-editable work product. Write validator blocks direct writes to any path ending in `state.md` after checking the more specific `finalize-state.md` first at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-write-validator.sh:101). Edit validator applies the same ordering and exits `2` at [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-edit-validator.sh:81). Bash validator blocks commands that modify `state.md` directly or move/copy it from source position, with a narrow cancellation exception, at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/loop-bash-validator.sh:75).
- dependencies_and_callers: Direct loader dependency is `hooks/lib/template-loader.sh`, sourced by `hooks/lib/loop-common.sh` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/lib/loop-common.sh:46). Callers include `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh` through `state_file_blocked_message()`. Path classification uses `is_state_file_path()`, which matches `state\.md$` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/hooks/lib/loop-common.sh:256); comments note that `finalize-state.md` must be checked first because it also matches that suffix. The template is also tracked by `tests/test-template-references.sh` as a common template at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-template-references.sh:152).
- edge_cases_or_failure_modes: Since `state.md` suffix matching also matches `finalize-state.md`, callers must preserve the current more-specific-first order or finalize-phase edits would receive the wrong block message. The fallback in `state_file_blocked_message()` is shorter than the template and omits the detailed field list, so missing template files degrade gracefully but with less guidance. The Bash validator includes a cancellation exception where `is_cancel_authorized` can allow a constrained `mv state.md cancel-state.md` only when a signal file exists; other `state.md` writes remain blocked.
- validation_or_tests: Template existence is validated as part of common template reference checks at [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-template-references.sh:161). Comprehensive template loading walks every Markdown template and ensures it can be loaded as non-empty content at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/cancel-when-finalize/tests/test-templates-comprehensive.sh:90). Related behavioral coverage appears in state/finalize/cancel validator tests, but this assigned file’s direct contract is the static block content and its reference from common hook message functions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `CANCEL_WHEN_FINALIZE-HZ-027`, `CANCEL_WHEN_FINALIZE-HZ-057`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`