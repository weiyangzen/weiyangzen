# agent_27 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-027 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse guard for Claude `Edit` calls during RLCR and PR loop execution. It protects generated/stateful loop artifacts from direct edits and enforces current-round write discipline before Claude can mutate files. It is registered as the `Edit` matcher in `hooks/hooks.json:24-30`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, exits immediately unless the tool is `Edit` (`hooks/loop-edit-validator.sh:24-32`). It lowercases the path for pattern checks, blocks round todo files unless allowlisted, blocks round prompt files, applies PR-loop protections, then applies RLCR-loop protections only when the target is under `.humanize/rlcr/` (`hooks/loop-edit-validator.sh:38-85`). For RLCR files, it resolves the active loop directory, selects `finalize-state.md` over `state.md` when present, strictly parses state, and uses `current_round` as the transition authority (`hooks/loop-edit-validator.sh:91-110`).
- inputs_outputs_state: Input is Claude hook JSON on stdin plus environment/context variables `CLAUDE_PROJECT_DIR`, `PROJECT_ROOT`, `LOOP_BASE_DIR`, and optionally `LOOP_DIR` (`hooks/loop-edit-validator.sh:39-42`, `91-93`). Output is no stdout on allow, stderr block text on denial, and process exit codes: `0` allow/no-op, `2` intentional tool block, `1` malformed state safety failure (`hooks/loop-edit-validator.sh:27-29`, `43-50`, `106-109`). It does not mutate state; it only reads loop files and emits a tool decision through process status.
- gates_or_invariants: Prompt files are read-only for Edit (`hooks/loop-edit-validator.sh:48-51`). PR `state.md` and generated PR artifacts are blocked; only `round-N-pr-resolve.md` is eligible, and its round must match active PR state (`hooks/loop-edit-validator.sh:57-76`). RLCR `state.md` and `finalize-state.md` are blocked, with finalize checked first because `state.md` pattern also matches `finalize-state.md` (`hooks/loop-edit-validator.sh:112-125`). RLCR `plan.md` backups are blocked inside `.humanize/rlcr/` (`hooks/loop-edit-validator.sh:131-138`). `goal-tracker.md` is editable only in round 0; after that, Claude must request updates via the summary file (`hooks/loop-edit-validator.sh:141-149`). Summary edits must target the current round unless the path is in the explicit allowlist (`hooks/loop-edit-validator.sh:155-179`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for path classifiers, active-loop lookup, strict state parsing, allowlist checks, and rendered block messages (`hooks/loop-edit-validator.sh:16-18`). Important delegated helpers include `find_active_loop`, which only considers the newest timestamped loop with `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151-174`); `parse_state_file_strict`, which requires YAML frontmatter plus fields including `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:247-330`); and PR round validation (`hooks/lib/loop-common.sh:843-887`). Message rendering flows through `load_and_render_safe`, which falls back when templates are missing (`hooks/lib/template-loader.sh:168-190`).
- edge_cases_or_failure_modes: Non-Edit events are intentionally ignored, so this hook only protects the `Edit` tool surface (`hooks/loop-edit-validator.sh:27-29`). If `.humanize/rlcr` is absent, or no active loop is found, RLCR-specific checks are bypassed (`hooks/loop-edit-validator.sh:83-97`). If the active RLCR state file is malformed, the hook fails closed with exit `1`, blocking operation for safety (`hooks/loop-edit-validator.sh:105-109`). Todo and summary allowlisting can permit historical migration/special files even if they do not match the current round (`hooks/lib/loop-common.sh:423-435`). The hook checks only direct file paths from hook JSON; broader shell mutations are handled by sibling Bash/Write validators.
- validation_or_tests: Targeted tests cover allowlisted Edit access to `round-2-todos.md` and `round-1-summary.md`, and blocking of `round-4-todos.md` (`tests/test-allowlist-validators.sh:188-228`). Plan backup protection for Edit is tested at `tests/test-plan-file-hooks.sh:240-254`, with a legacy `.humanize-loop.local` path allowed at `tests/test-plan-file-hooks.sh:1095-1106`. PR wrong-round `pr-resolve` Edit blocking is tested at `tests/test-pr-loop-hooks.sh:1515-1527`. Robustness tests also invoke this hook for malformed/missing hook input surfaces (`tests/robustness/test-hook-system-robustness.sh`, discovered by reference).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable end-to-end specification for the real monitor control loop, not a stubbed unit test. It verifies monitor behavior when RLCR or PR loop directories disappear and when monitors receive interrupts, across bash and zsh where available (`tests/test-monitor-e2e-real.sh:3-13`).
- algorithmic_behavior: Creates isolated temporary projects under `/tmp/test-monitor-e2e-real-$$`, synthesizes `.humanize/rlcr` and `.humanize/pr-loop` session directories, writes representative `state.md` and `goal-tracker.md` files, creates matching fake cache log directories, then launches scripts that source `scripts/humanize.sh` and call the real monitor functions (`tests/test-monitor-e2e-real.sh:48-56`, `64-163`, `243-329`, `692-805`). The tests then delete loop directories or send `SIGINT`, wait in bounded loops, force-kill only after timeout, and inspect captured output for clean stop signals, absence of glob errors, and expected exit codes.
- inputs_outputs_state: Inputs are generated test fixtures: RLCR state fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `plan_file`, `base_branch`, and `review_started` (`tests/test-monitor-e2e-real.sh:69-82`), goal tracker sections (`tests/test-monitor-e2e-real.sh:84-97`), fake `HOME`/`XDG_CACHE_HOME` cache paths (`tests/test-monitor-e2e-real.sh:99-107`), and terminal shim functions (`tests/test-monitor-e2e-real.sh:126-146`). Outputs are captured monitor stdout/stderr files or shell variables plus pass/fail counters (`tests/test-monitor-e2e-real.sh:25-37`, `188-231`). Test state is cleaned by trap via `cleanup_test`, including process cleanup and temp directory removal (`tests/test-monitor-e2e-real.sh:51-56`).
- gates_or_invariants: The RLCR monitor must exit with `EXIT_CODE:0` after `.humanize/rlcr` deletion, print `Monitoring stopped:` and the deletion reason, print cleanup text `Stopped monitoring`, avoid zsh/bash glob errors, and include source support for scroll-region reset via `printf "\033[r"` (`tests/test-monitor-e2e-real.sh:191-231`). Bash and zsh variants must avoid `no matches found` or `bad pattern` (`tests/test-monitor-e2e-real.sh:204-209`, `362-366`, `544-549`, `678-682`). Signal tests require either cleanup wording or a clean exit marker, with timeout fallback for delivery differences (`tests/test-monitor-e2e-real.sh:492-548`, `647-682`). PR monitor tests require graceful/clean exit for `humanize monitor pr --once` and no glob errors (`tests/test-monitor-e2e-real.sh:807-824`), plus continuous PR monitor interruption behavior without `--once` (`tests/test-monitor-e2e-real.sh:938-990`).
- dependencies_and_callers: Depends directly on `scripts/humanize.sh`, specifically `_humanize_monitor_codex` and `_humanize_monitor_pr` (`tests/test-monitor-e2e-real.sh:148-153`, `314-321`, `464-471`, `776-795`, `917-925`). The monitored implementation uses `find` instead of globbing for RLCR logs to avoid zsh unmatched-glob failures (`scripts/humanize.sh:280-339`), checks `.humanize/rlcr` existence in main and nested polling loops (`scripts/humanize.sh:620-625`, `657-665`, `751-758`), restores terminal scroll region in `_restore_terminal` (`scripts/humanize.sh:522-528`), and prints the graceful-stop message after cleanup (`scripts/humanize.sh:563-576`). PR monitor dependencies include `humanize monitor pr` dispatch (`scripts/humanize.sh:889-898`), PR once-mode rendering (`scripts/humanize.sh:1215-1293`), and continuous monitor cleanup/traps (`scripts/humanize.sh:1174-1213`, `1303-1355`).
- edge_cases_or_failure_modes: zsh may be unavailable; those tests skip rather than fail (`tests/test-monitor-e2e-real.sh:240-242`, `558-560`). SIGINT delivery to process groups is acknowledged as complex, so bash tests may count a SIGTERM cleanup path as pass if the monitor was running but did not exit promptly (`tests/test-monitor-e2e-real.sh:505-518`, `951-964`). The PR `--once` deletion test schedules deletion in a background subshell, but once-mode may complete before deletion; the assertion accepts any graceful/clean zero exit (`tests/test-monitor-e2e-real.sh:784-817`). Terminal behavior is validated with shims, so this proves cleanup code paths and visible messages more than actual interactive terminal rendering.
- validation_or_tests: The file is itself the validation asset. It summarizes success criteria at the end: clean user-friendly exit, no glob errors, terminal state restored, bash/zsh operation, real SIGINT handling, and PR monitor behavior with and without `--once` (`tests/test-monitor-e2e-real.sh:992-1017`). I did not execute it during this read-only research pass; evidence is from direct inspection and referenced implementation lines.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-087 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the Round 0 goal-tracker initialization gate. It defines the remediation contract when `goal-tracker.md` still contains placeholders and prevents transition out of Round 0 until the immutable goal and acceptance criteria are established.
- algorithmic_behavior: The template renders a block titled `Goal Tracker Not Initialized`, includes the target `{{GOAL_TRACKER_FILE}}`, lists `{{MISSING_ITEMS}}`, and instructs Claude to read the tracker, replace placeholder content, define 3-7 testable acceptance criteria, populate Active Tasks, and write the updated tracker (`prompt-template/block/goal-tracker-not-initialized.md:1-15`). It explicitly states the immutable section can only be set in Round 0 and becomes read-only after that (`prompt-template/block/goal-tracker-not-initialized.md:16-18`).
- inputs_outputs_state: Inputs are template variables from `hooks/loop-codex-stop-hook.sh`: `GOAL_TRACKER_FILE` and computed `MISSING_ITEMS` (`hooks/loop-codex-stop-hook.sh:685-707`). Output is a rendered markdown reason embedded in a JSON block decision returned by the Stop hook (`hooks/loop-codex-stop-hook.sh:709-717`). The template does not mutate state directly, but its rendered instructions route Claude to update `goal-tracker.md` while Round 0 edits are still allowed.
- gates_or_invariants: The corresponding Stop-hook gate runs only when not in finalize phase, not already in review phase, `review_started` is not true, current round is `0`, and the goal tracker file exists (`hooks/loop-codex-stop-hook.sh:649-652`). The gate extracts Ultimate Goal, Acceptance Criteria, and Active Tasks sections and flags generic `[To be ...]` placeholder text in those sections (`hooks/loop-codex-stop-hook.sh:658-683`). Any missing item causes a block decision instead of round progression (`hooks/loop-codex-stop-hook.sh:700-718`). Invariant: the immutable section must be initialized in Round 0 because later direct goal-tracker edits are blocked by edit/write/bash validators.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` in `hooks/loop-codex-stop-hook.sh:705-707`, with inline fallback if the template is missing (`hooks/loop-codex-stop-hook.sh:701-704`). The template depends on the shared template loader’s `{{VAR}}` substitution behavior (`hooks/lib/template-loader.sh:168-190`) and on setup output that initially creates placeholder goal tracker sections in `scripts/setup-rlcr-loop.sh` (references discovered at `scripts/setup-rlcr-loop.sh:737-819`).
- edge_cases_or_failure_modes: If `goal-tracker.md` does not exist, this particular gate does not run because the condition requires `-f "$GOAL_TRACKER_FILE"` (`hooks/loop-codex-stop-hook.sh:652`). Placeholder detection is section-scoped and generic, so unusual placeholder wording not matching `[To be [a-z]` may pass; conversely, a legitimate bracketed phrase matching that pattern inside a target section would block. Finalize/review/skip-impl flows bypass this template by design (`hooks/loop-codex-stop-hook.sh:649-652`).
- validation_or_tests: No direct template-specific test was found for this exact file, but it is referenced by template-reference tests through the stop hook and by the stop-hook implementation search. Its runtime validation is the Stop-hook Round 0 initialization check at `hooks/loop-codex-stop-hook.sh:644-719`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-117 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review-prompt section that transfers post-Round-0 goal-tracker mutation authority from Claude to Codex. It defines how Codex should evaluate Claude’s requested tracker changes during review without allowing changes to the immutable goal/acceptance criteria.
- algorithmic_behavior: The template tells Codex that Claude cannot directly modify `goal-tracker.md` after Round 0 and that any Claude summary section named `Goal Tracker Update Request` must be evaluated by Codex (`prompt-template/codex/goal-tracker-update-section.md:1-5`). If approved, Codex updates `@{{GOAL_TRACKER_FILE}}`, moving tasks among Active/Completed/Deferred sections, adding Plan Evolution Log entries, and adding Open Issues (`prompt-template/codex/goal-tracker-update-section.md:6-9`). If rejected, Codex must explain the rejection in review (`prompt-template/codex/goal-tracker-update-section.md:11`). It enumerates common request classes: task completion, new issues, plan changes, and justified deferrals (`prompt-template/codex/goal-tracker-update-section.md:13-17`).
- inputs_outputs_state: Input variable is `GOAL_TRACKER_FILE`, rendered into the Codex review prompt (`prompt-template/codex/goal-tracker-update-section.md:6`). Output is not a direct shell result; it becomes part of `round-N-review-prompt.md` generated by the Stop hook (`hooks/loop-codex-stop-hook.sh:761-771`, `816-845`). The induced state transition is mediated by Codex: approved requests mutate mutable goal-tracker sections, while rejected requests remain review feedback.
- gates_or_invariants: Hard invariant in the template: never modify the immutable section containing Ultimate Goal and Acceptance Criteria (`prompt-template/codex/goal-tracker-update-section.md:10`). Requests must be evaluated for justification and alignment with the Ultimate Goal before update (`prompt-template/codex/goal-tracker-update-section.md:5`). Deferrals require strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`). This works with the validator invariant that Claude’s direct `goal-tracker.md` edits after Round 0 are blocked (`hooks/loop-edit-validator.sh:145-149`).
- dependencies_and_callers: Rendered in `hooks/loop-codex-stop-hook.sh` as `GOAL_TRACKER_UPDATE_SECTION` with fallback text, then injected into both full-alignment and regular Codex review prompts (`hooks/loop-codex-stop-hook.sh:767-771`, `812-845`). The enclosing prompt templates that receive it include `prompt-template/codex/full-alignment-review.md` and `prompt-template/codex/regular-review.md`, referenced by the same Stop-hook generation path (`hooks/loop-codex-stop-hook.sh:816-845`).
- edge_cases_or_failure_modes: The template relies on Claude putting changes in a recognizable `Goal Tracker Update Request` section; unstructured summary text may not trigger Codex action. It gives Codex discretion to approve/reject, so weak or ambiguous justifications can be rejected but require review explanation. It instructs Codex to update mutable sections but does not itself enforce file write success; downstream validation depends on hook execution and review behavior.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` renders this template with a sample `.humanize/rlcr/.../goal-tracker.md` path and asserts both the heading and rendered path appear. Runtime use is validated through the Stop-hook prompt construction path at `hooks/loop-codex-stop-hook.sh:767-845`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 section headers present; exact full item ids are used only as item section headers
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`