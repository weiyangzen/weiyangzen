# agent_04 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role:
  The `hooks` directory is the runtime enforcement and state-machine layer for Humanize RLCR and PR loops. It registers Claude Code hook entrypoints in `hooks/hooks.json`, guards tool access before reads/writes/edits/bash, and drives loop continuation/completion through Stop hooks. The directory is core algorithmic infrastructure, not passive support code.
- algorithmic_behavior:
  `hooks/hooks.json` wires `UserPromptSubmit` to `loop-plan-file-validator.sh`, `PreToolUse` to write/edit/read/bash validators, and `Stop` to both `loop-codex-stop-hook.sh` and `pr-loop-stop-hook.sh` with 7200-second timeouts (`hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`).
  `hooks/lib/loop-common.sh` centralizes shared constants, JSON validation, state parsing, path classification, PR bot mapping, cancel authorization, state-ending, and PR goal-tracker updates. It validates hook JSON and rejects null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, and excessive nesting (`hooks/lib/loop-common.sh:59`, `hooks/lib/loop-common.sh:80`, `hooks/lib/loop-common.sh:119`). It parses RLCR state in tolerant and strict modes, with strict mode requiring YAML frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, plus numeric/boolean validation (`hooks/lib/loop-common.sh:246`, `hooks/lib/loop-common.sh:289`, `hooks/lib/loop-common.sh:307`). It detects active RLCR/PR loop directories by newest timestamp directory with `state.md` or finalize state (`hooks/lib/loop-common.sh:156`, `hooks/lib/loop-common.sh:798`).
  `loop-codex-stop-hook.sh` is the RLCR loop transition engine. It validates schema/branch/plan integrity, checks incomplete todos from transcript, enforces git cleanliness and large-file limits, builds Codex review prompts, invokes Codex, interprets strict terminal markers `COMPLETE` and `STOP`, enters review/finalize phases, increments rounds, and writes next-round prompts (`hooks/loop-codex-stop-hook.sh:61`, `hooks/loop-codex-stop-hook.sh:131`, `hooks/loop-codex-stop-hook.sh:340`, `hooks/loop-codex-stop-hook.sh:891`, `hooks/loop-codex-stop-hook.sh:1248`, `hooks/loop-codex-stop-hook.sh:1397`, `hooks/loop-codex-stop-hook.sh:1514`).
  `pr-loop-stop-hook.sh` is the PR review-loop transition engine. It parses PR loop state, resolves fork/base PR repository, blocks closed/merged PRs, requires a resolution file, enforces clean/pushed git state, detects force pushes, validates trigger comments, checks Codex `+1` and Claude `eyes` reactions, polls all configured bots, runs local Codex feedback analysis, updates `active_bots`, updates goal tracker, creates feedback for the next round, or moves `state.md` to terminal state files (`hooks/pr-loop-stop-hook.sh:87`, `hooks/pr-loop-stop-hook.sh:204`, `hooks/pr-loop-stop-hook.sh:238`, `hooks/pr-loop-stop-hook.sh:257`, `hooks/pr-loop-stop-hook.sh:278`, `hooks/pr-loop-stop-hook.sh:363`, `hooks/pr-loop-stop-hook.sh:450`, `hooks/pr-loop-stop-hook.sh:646`, `hooks/pr-loop-stop-hook.sh:748`, `hooks/pr-loop-stop-hook.sh:801`, `hooks/pr-loop-stop-hook.sh:1232`, `hooks/pr-loop-stop-hook.sh:1368`, `hooks/pr-loop-stop-hook.sh:1505`, `hooks/pr-loop-stop-hook.sh:1605`).
  Branch-specific sandbox behavior exists in both Stop hooks: Codex exec defaults to `--full-auto`, but if `HUMANIZE_CODEX_BYPASS_SANDBOX` is `true` or `1`, it uses `--dangerously-bypass-approvals-and-sandbox` (`hooks/loop-codex-stop-hook.sh:898`, `hooks/loop-codex-stop-hook.sh:901`, `hooks/pr-loop-stop-hook.sh:1328`, `hooks/pr-loop-stop-hook.sh:1331`).
  Pre-tool validators enforce the state machines’ invariants before Claude can mutate or read managed surfaces. `loop-write-validator.sh` blocks writes to todos, prompts, RLCR state, finalize state, plan backups, PR loop `state.md`, PR generated files, wrong-round summaries, and wrong-directory summaries (`hooks/loop-write-validator.sh:55`, `hooks/loop-write-validator.sh:65`, `hooks/loop-write-validator.sh:76`, `hooks/loop-write-validator.sh:140`, `hooks/loop-write-validator.sh:152`, `hooks/loop-write-validator.sh:179`, `hooks/loop-write-validator.sh:192`, `hooks/loop-write-validator.sh:202`, `hooks/loop-write-validator.sh:228`, `hooks/loop-write-validator.sh:254`). `loop-edit-validator.sh` mirrors edit protections for the same managed surfaces (`hooks/loop-edit-validator.sh:38`, `hooks/loop-edit-validator.sh:59`, `hooks/loop-edit-validator.sh:105`, `hooks/loop-edit-validator.sh:117`, `hooks/loop-edit-validator.sh:132`, `hooks/loop-edit-validator.sh:145`, `hooks/loop-edit-validator.sh:155`). `loop-read-validator.sh` blocks stale/wrong-location RLCR prompt/summary/todos reads while generally allowing PR loop reads (`hooks/loop-read-validator.sh:55`, `hooks/loop-read-validator.sh:69`, `hooks/loop-read-validator.sh:94`, `hooks/loop-read-validator.sh:122`, `hooks/loop-read-validator.sh:138`, `hooks/loop-read-validator.sh:159`). `loop-bash-validator.sh` blocks shell-mediated bypasses, including redirection, `tee`, `sed -i`, broad `git add` of `.humanize`, forbidden `git push` when `push_every_round` is false, state-file move/copy/source/destination attacks, shell-wrapper bypasses, and PR generated-file mutations (`hooks/loop-bash-validator.sh:96`, `hooks/loop-bash-validator.sh:115`, `hooks/loop-bash-validator.sh:139`, `hooks/loop-bash-validator.sh:171`, `hooks/loop-bash-validator.sh:306`, `hooks/loop-bash-validator.sh:336`, `hooks/loop-bash-validator.sh:349`, `hooks/loop-bash-validator.sh:365`, `hooks/loop-bash-validator.sh:375`, `hooks/loop-bash-validator.sh:403`, `hooks/loop-bash-validator.sh:423`).
- inputs_outputs_state:
  Inputs include Claude hook JSON on stdin, current project root from `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/<timestamp>/state.md`, `.humanize/pr-loop/<timestamp>/state.md`, transcript paths, git/gh/Codex command outputs, prompt templates, and bot comments/reactions. Outputs are Claude hook JSON block/allow decisions, stderr operator messages, generated loop files such as `round-N-pr-comment.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, RLCR review/debug files, and terminal state renames such as `approve-state.md`, `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `usage-limit-state.md`, `complete-state.md`, or `cancel-state.md` (`hooks/lib/loop-common.sh:1112`, `hooks/pr-loop-stop-hook.sh:245`, `hooks/pr-loop-stop-hook.sh:251`, `hooks/pr-loop-stop-hook.sh:431`, `hooks/pr-loop-stop-hook.sh:1386`, `hooks/loop-codex-stop-hook.sh:744`).
- gates_or_invariants:
  Managed state files are hook-owned and blocked from normal tool access. RLCR strict state requires valid schema and current branch consistency. PR loop requires the PR to be open, clean working tree, pushed commits, valid trigger freshness after latest commit, and per-bot response/timeout handling. `active_bots` is the PR loop continuation cursor: empty means approval/terminal state; non-empty means feedback loop continues. Codex output markers are strict line markers: RLCR uses `COMPLETE`/`STOP`; PR loop uses `APPROVE`, `ISSUES_REMAINING`, `WAITING_FOR_BOTS`, and `USAGE_LIMIT_HIT` (`hooks/loop-codex-stop-hook.sh:1397`, `hooks/pr-loop-stop-hook.sh:1376`, `hooks/pr-loop-stop-hook.sh:1390`, `hooks/pr-loop-stop-hook.sh:1413`).
- dependencies_and_callers:
  Called by Claude Code via `hooks/hooks.json`. Depends on `jq`, `git`, `gh`, `codex`, `timeout` abstraction from `scripts/portable-timeout.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, and prompt templates under `prompt-template`. Shared template loading is through `hooks/lib/template-loader.sh`, whose rendering is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:71`).
- edge_cases_or_failure_modes:
  Fail-closed cases include malformed hook JSON, too-deep JSON, malformed state frontmatter, missing required state fields, non-numeric rounds, git failures, branch mismatch, dirty/unpushed work, missing resolution/summary files, force pushes, missing or stale trigger comments, Claude eyes timeout, missing Codex command, Codex nonzero exit or empty output, PR closed/merged, max iterations exceeded, no active loop, stale old loop directories, shell wrapper bypass attempts, and fork PR comments living on the base repo rather than the fork.
- validation_or_tests:
  Covered by multiple tests visible in the repo, especially `tests/test-pr-loop-stophook.sh` for PR Stop hook behavior, `tests/test-pr-loop-hooks.sh` for validator behavior, `tests/test-todo-checker.sh` for transcript todo parsing, `tests/test-template-loader.sh` for template rendering, and robustness tests under `tests/robustness`. The assigned stop-hook test explicitly exercises force push, trigger validation, timeout, Codex `+1`, Claude `eyes`, dynamic startup cases, fork PR support, and mixed approval.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-034 `file` `scripts/check-bot-reactions.sh`
- cursor: `[_]`
- core_role:
  Runtime helper used by PR loop setup and Stop hook to translate GitHub reactions into loop gates. It detects Codex approval via `+1` on the PR issue and Claude bot receipt via `eyes` on a trigger comment (`scripts/check-bot-reactions.sh:3`, `scripts/check-bot-reactions.sh:5`, `scripts/check-bot-reactions.sh:10`).
- algorithmic_behavior:
  The script exposes two commands. `codex-thumbsup <pr_number> [--after <timestamp>]` resolves the PR base repository, fetches all PR issue reactions with pagination, filters for user `chatgpt-codex-connector[bot]` and content `+1`, optionally rejects reactions older than `--after`, and prints the reaction JSON on success (`scripts/check-bot-reactions.sh:86`, `scripts/check-bot-reactions.sh:119`, `scripts/check-bot-reactions.sh:152`, `scripts/check-bot-reactions.sh:166`, `scripts/check-bot-reactions.sh:177`, `scripts/check-bot-reactions.sh:185`). `claude-eyes <comment_id> [--retry N] [--delay S] [--pr PR]` resolves the base repository when a PR number is provided, then sleeps and retries reaction fetches until it finds `claude[bot]` with content `eyes`, or exhausts attempts (`scripts/check-bot-reactions.sh:190`, `scripts/check-bot-reactions.sh:232`, `scripts/check-bot-reactions.sh:264`, `scripts/check-bot-reactions.sh:273`, `scripts/check-bot-reactions.sh:282`, `scripts/check-bot-reactions.sh:298`).
- inputs_outputs_state:
  Inputs are command-line mode, PR number or comment ID, optional `--after`, `--pr`, retry count, retry delay, GitHub CLI auth/context, and current/fork repo metadata. Output is reaction JSON on stdout when found; status-only failure otherwise. It has no persistent local state.
- gates_or_invariants:
  Exit code `0` means reaction found, `1` means reaction not found or retry timeout, and `2` means argument/API error (`scripts/check-bot-reactions.sh:13`). It paginates and merges per-page `--jq` arrays with `jq -s 'add // []'` so large PR reaction sets are not truncated (`scripts/check-bot-reactions.sh:156`, `scripts/check-bot-reactions.sh:272`). Timestamp comparison is lexicographic ISO-8601 string comparison, so it assumes GitHub UTC timestamps are consistently formatted (`scripts/check-bot-reactions.sh:177`).
- dependencies_and_callers:
  Depends on `gh`, `jq`, `sleep`, and `run_with_timeout` from `scripts/portable-timeout.sh` (`scripts/check-bot-reactions.sh:31`). Called by `hooks/pr-loop-stop-hook.sh` for pre-poll and in-poll Codex `+1` approval and Claude eyes verification (`hooks/pr-loop-stop-hook.sh:675`, `hooks/pr-loop-stop-hook.sh:776`, `hooks/pr-loop-stop-hook.sh:998`), and by `scripts/setup-pr-loop.sh` to verify Claude reaction after initial trigger setup (`scripts/setup-pr-loop.sh:587`).
- edge_cases_or_failure_modes:
  Handles missing PR/comment arguments as exit `2`; unknown options/commands exit `2`; unresolved fork/base repo falls back to current repo, which may still fail API calls; failed PR reaction fetch exits `2`; failed comment reaction fetch during Claude eyes continues to retry; no reaction exits `1`. For fork PRs it deliberately uses the base repo because comments/reactions are stored there (`scripts/check-bot-reactions.sh:119`, `scripts/check-bot-reactions.sh:232`).
- validation_or_tests:
  `tests/test-pr-loop-system.sh` invokes `codex-thumbsup`, `codex-thumbsup --after`, and `claude-eyes --retry 1 --delay 0` (`tests/test-pr-loop-system.sh:201`, `tests/test-pr-loop-system.sh:211`, `tests/test-pr-loop-system.sh:226`). The assigned stop-hook tests mock this behavior indirectly through `pr-loop-stop-hook.sh`, including Codex `+1` approval and Claude eyes timeout paths (`tests/test-pr-loop-stophook.sh:896`, `tests/test-pr-loop-stophook.sh:1011`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-064 `file` `tests/test-pr-loop-stophook.sh`
- cursor: `[_]`
- core_role:
  Executable specification for the PR loop Stop hook state machine. It constructs synthetic `.humanize/pr-loop/<timestamp>` state directories, mocks `gh`, `git`, and sometimes `codex`, runs `hooks/pr-loop-stop-hook.sh`, and asserts state transitions or block messages (`tests/test-pr-loop-stophook.sh:1`, `tests/test-pr-loop-stophook.sh:12`, `tests/test-pr-loop-stophook.sh:1768`).
- algorithmic_behavior:
  The file defines `run_stophook_tests`, then nested tests that model key PR-loop cases. It verifies old trigger comments are rejected when `latest_commit_at` is newer (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:25`, `tests/test-pr-loop-stophook.sh:129`). It verifies round 0 startup case 1 does not require a trigger (`tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:231`). It verifies empty `active_bots` creates or signals `approve-state.md` (`tests/test-pr-loop-stophook.sh:246`, `tests/test-pr-loop-stophook.sh:312`). It verifies dynamic `startup_case` updates when comments arrive (`tests/test-pr-loop-stophook.sh:327`, `tests/test-pr-loop-stophook.sh:436`). It verifies unpushed commits block exit (`tests/test-pr-loop-stophook.sh:452`, `tests/test-pr-loop-stophook.sh:528`). It verifies force-push detection when old commit is not an ancestor (`tests/test-pr-loop-stophook.sh:538`, `tests/test-pr-loop-stophook.sh:596`, `tests/test-pr-loop-stophook.sh:632`). It verifies missing trigger for cases 4/5 blocks (`tests/test-pr-loop-stophook.sh:642`, `tests/test-pr-loop-stophook.sh:744`). It verifies bot timeouts remove bots and can complete the loop with empty `active_bots` (`tests/test-pr-loop-stophook.sh:754`, `tests/test-pr-loop-stophook.sh:850`, `tests/test-pr-loop-stophook.sh:874`). It verifies Codex `+1` removes Codex and approves when it is the only active bot (`tests/test-pr-loop-stophook.sh:896`, `tests/test-pr-loop-stophook.sh:999`). It verifies Claude eyes timeout blocks exit (`tests/test-pr-loop-stophook.sh:1011`, `tests/test-pr-loop-stophook.sh:1131`). It verifies fork PR support by requiring fallback from fork repo to parent repo (`tests/test-pr-loop-stophook.sh:1390`, `tests/test-pr-loop-stophook.sh:1424`, `tests/test-pr-loop-stophook.sh:1501`). It verifies mixed approval keeps issue-reporting bots active and removes approving bots without premature completion (`tests/test-pr-loop-stophook.sh:1519`, `tests/test-pr-loop-stophook.sh:1678`, `tests/test-pr-loop-stophook.sh:1740`, `tests/test-pr-loop-stophook.sh:1752`, `tests/test-pr-loop-stophook.sh:1758`).
- inputs_outputs_state:
  Inputs are generated state frontmatter fields such as `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, `poll_timeout`, `started_at`, `last_trigger_at`, `trigger_comment_id`, `startup_case`, `latest_commit_sha`, and `latest_commit_at`. Mocks intercept `gh`, `git`, and `codex` through `PATH`, and the project root is injected with `CLAUDE_PROJECT_DIR`. Outputs are pass/fail assertions, created/renamed state files, generated check/feedback files, and hook JSON/block text captured from stdout/stderr.
- gates_or_invariants:
  The specification encodes that the loop must not accept stale triggers after commits/force pushes, must not proceed with unpushed commits, must require triggers for later rounds or startup cases 4/5, must treat Codex `+1` as approval only after the valid timestamp gate, must require Claude eyes only when trigger is required, must anchor bot timeout windows correctly, and must leave `active_bots` as the continuation cursor. The mixed-approval test specifically guards against inflating resolved issue counts when one bot approves but another still reports issues (`tests/test-pr-loop-stophook.sh:1711`, `tests/test-pr-loop-stophook.sh:1734`).
- dependencies_and_callers:
  Intended to be sourced by the broader test harness, which supplies `PROJECT_ROOT`, `TEST_DIR`, `pass`, and `fail`. It depends on shell, temporary mock executables, `timeout`, `grep`, `sed`, and date variants compatible with GNU/BSD (`tests/test-pr-loop-stophook.sh:1150`). It is a caller of `hooks/pr-loop-stop-hook.sh` in every scenario.
- edge_cases_or_failure_modes:
  Tests use real time in some paths to avoid immediate timeout, support both `date -d` and BSD `date -v`, mock `gh --jq` output differences carefully, and limit hook invocations with `timeout` where polling could otherwise hang (`tests/test-pr-loop-stophook.sh:433`, `tests/test-pr-loop-stophook.sh:852`, `tests/test-pr-loop-stophook.sh:1133`, `tests/test-pr-loop-stophook.sh:1339`, `tests/test-pr-loop-stophook.sh:1709`). Some assertions accept alternate success signals, such as output message or file creation, to tolerate implementation output variations.
- validation_or_tests:
  This file is itself the validation artifact. It runs twelve stop-hook integration tests in order at the end (`tests/test-pr-loop-stophook.sh:1768`). It complements `tests/test-pr-loop-system.sh`, which tests `check-bot-reactions.sh` directly.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-094 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role:
  Block-message template for attempts to modify `.humanize/pr-loop/**/state.md`. It is a small but core guard contract because validators use it to communicate an invariant: PR loop state is system-managed and not user/Claude-managed.
- algorithmic_behavior:
  The template states that `state.md` in `.humanize/pr-loop/` cannot be modified, lists the managed fields, and explains that manual modification would corrupt PR loop state (`prompt-template/block/pr-loop-state-modification.md:1`, `prompt-template/block/pr-loop-state-modification.md:3`, `prompt-template/block/pr-loop-state-modification.md:5`, `prompt-template/block/pr-loop-state-modification.md:12`). It is loaded through `pr_loop_state_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:889`, `hooks/lib/loop-common.sh:895`).
- inputs_outputs_state:
  No dynamic placeholders are required. Input is template lookup by name; output is markdown sent to stderr by pre-tool validators when a blocked operation targets PR loop state.
- gates_or_invariants:
  Protects state fields including current round, PR number/branch, active bots, Codex config, and polling settings (`prompt-template/block/pr-loop-state-modification.md:5`). This backs write/edit/bash blocks for PR loop `state.md` (`hooks/loop-write-validator.sh:76`, `hooks/loop-edit-validator.sh:59`, `hooks/loop-bash-validator.sh:403`).
- dependencies_and_callers:
  Depends on `hooks/lib/template-loader.sh` via `load_and_render_safe`. Called indirectly from `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh` through `pr_loop_state_blocked_message`.
- edge_cases_or_failure_modes:
  If the template is missing or empty, `load_and_render_safe` falls back to an inline message (`hooks/lib/loop-common.sh:891`). The message is intentionally generic and does not include runtime path variables, reducing rendering failure surface.
- validation_or_tests:
  Template reference and validator tests likely cover availability; direct line reference shows it is included in `rg` hits from the validators. It is also part of the comprehensive template suite listed in repo files.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-124 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role:
  Prompt header template for PR loop round 0. It seeds Claude’s initial PR review-loop context before fetched comments are appended, making it part of the PR loop’s prompt-generation contract.
- algorithmic_behavior:
  The template instructs the agent to execute “with ultrathink,” identifies the prompt as `PR Review Loop (Round 0)`, states that the loop monitors remote review bots, and renders PR number, branch, and active bots through `{{PR_NUMBER}}`, `{{START_BRANCH}}`, and `{{ACTIVE_BOTS_DISPLAY}}` placeholders (`prompt-template/pr-loop/round-0-header.md:1`, `prompt-template/pr-loop/round-0-header.md:3`, `prompt-template/pr-loop/round-0-header.md:5`, `prompt-template/pr-loop/round-0-header.md:7`, `prompt-template/pr-loop/round-0-header.md:12`).
- inputs_outputs_state:
  Inputs are template variables supplied by setup code: PR number, starting branch, and active bot display. Output is markdown that becomes the beginning of the round-0 PR loop prompt/comment review context.
- gates_or_invariants:
  It defines the first-round framing and ensures the prompt carries the same `active_bots` state that the PR loop state machine uses for polling and completion. Because it only headers the review section and does not include transition logic itself, its invariant role is prompt consistency rather than state mutation.
- dependencies_and_callers:
  Loaded by `scripts/setup-pr-loop.sh` via `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-header.md"` (`scripts/setup-pr-loop.sh:758`). Rendering depends on the single-pass template loader, which avoids recursive placeholder substitution (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:54`).
- edge_cases_or_failure_modes:
  Missing variables remain as literal placeholders by design (`hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:116`). If the template is missing, setup uses a fallback header at the call site. The template ends after introducing “Review Comments,” so callers must append fetched PR comments; by itself it is incomplete as a standalone prompt.
- validation_or_tests:
  Template reference is discoverable in `scripts/setup-pr-loop.sh`; broader template tests in the repo cover template references. Its role is also exercised indirectly by PR loop setup/system tests that create round-0 prompts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above; each assigned item id is used once as its section heading
- missing_items: none
- duplicate_items: none detected in item section headings
- final_worker_status: `complete`