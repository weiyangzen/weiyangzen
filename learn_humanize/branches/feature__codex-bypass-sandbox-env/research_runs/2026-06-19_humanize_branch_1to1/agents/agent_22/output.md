# agent_22 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is the user-facing command surface that delegates all executable setup to `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The command exposes an iterative two-phase development algorithm: implementation rounds reviewed by Codex summary review, followed by a Review Phase using `codex review --base <branch>` once Codex says `COMPLETE`. The stop condition requires both implementation completion and code-review cleanliness, with `[P0-9]` markers treated as review issues that keep the loop active. See `commands/start-rlcr-loop.md:16-25` and `commands/start-rlcr-loop.md:60-67`.
- inputs_outputs_state: Inputs are command arguments: plan file or `--plan-file`, iteration cap, Codex model and effort, timeout, plan tracking mode, push requirement, base branch, full alignment interval, and `--skip-impl`, declared at `commands/start-rlcr-loop.md:3`. The command output is not a computed artifact itself; it invokes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at `commands/start-rlcr-loop.md:12-14`, which creates `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, plan backup, and round prompt files. In the setup script, state fields include current round, max iterations, Codex model/effort/timeout, push policy, full-review interval, plan metadata, start/base branches, captured base commit, and `review_started` at `scripts/setup-rlcr-loop.sh:709-725`.
- gates_or_invariants: The wrapper restricts allowed execution to the setup script through `allowed-tools` at `commands/start-rlcr-loop.md:4`. The documented loop invariants are: write a summary before exit, maintain `goal-tracker.md`, include files/tests/details in summaries, do not bypass by editing state or canceling, and trust Codex feedback, at `commands/start-rlcr-loop.md:46-52`. The delegated setup script enforces one active loop at a time (`scripts/setup-rlcr-loop.sh:240-261`), requires a git repo with at least one commit (`scripts/setup-rlcr-loop.sh:297-307`), rejects unsafe plan paths including absolute paths, spaces, shell metacharacters, symlink traversal, missing/unreadable files, paths outside the project, and submodule paths (`scripts/setup-rlcr-loop.sh:320-421`), enforces plan tracking policy (`scripts/setup-rlcr-loop.sh:427-472`), requires meaningful plan content unless `--skip-impl` without a plan (`scripts/setup-rlcr-loop.sh:480-541`), requires `codex` on `PATH` (`scripts/setup-rlcr-loop.sh:543-549`), and validates YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:561-587`, `scripts/setup-rlcr-loop.sh:645-651`).
- dependencies_and_callers: This command depends directly on `scripts/setup-rlcr-loop.sh` and transitively on `scripts/portable-timeout.sh` (`scripts/setup-rlcr-loop.sh:27-29`) and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:232-234`). Setup relies on git, Codex CLI, local branch refs, and the `.humanize/rlcr` state directory. The command documentation also depends on stop-hook behavior: Codex summary review triggers when the user exits, and code review begins after `COMPLETE`, matching `hooks/loop-codex-stop-hook.sh` behavior around prompt generation and round advancement.
- edge_cases_or_failure_modes: `--skip-impl` makes the plan optional, creates an internal placeholder plan, starts directly in review mode, sets `review_started` true, and touches `.review-phase-started` (`scripts/setup-rlcr-loop.sh:272-290`, `scripts/setup-rlcr-loop.sh:675-731`). Explicit `--base-branch` must exist locally; if it exists only on remote, setup fails with instructions to fetch a local branch (`scripts/setup-rlcr-loop.sh:594-611`). Auto-detection can fail when no local remote default/main/master branch exists (`scripts/setup-rlcr-loop.sh:613-642`). Path validation intentionally rejects common awkward cases like spaces and symlinks to keep serialized state and shell usage predictable.
- validation_or_tests: The command itself is declarative, but its behavior is validated through the setup script’s guard logic and related robustness tests in the repository. The setup script emits the initial prompt and completion requirements after successful state creation (`scripts/setup-rlcr-loop.sh:980-1064`). `--skip-impl` behavior is documented in the command file at `commands/start-rlcr-loop.md:69-86` and implemented in setup state/prompt creation at `scripts/setup-rlcr-loop.sh:864-898`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for graceful error behavior in `hooks/lib/template-loader.sh`, especially missing templates, malformed placeholders, strict-shell compatibility, fallback rendering, and directory validation.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh`, derives `TEMPLATE_DIR` via `get_template_dir`, then runs 12 direct assertions using local `pass`/`fail` counters. It treats template-system failures as controlled outputs, not shell crashes. The harness setup is at `tests/test-error-scenarios.sh:9-23`, and result aggregation exits 0 only when `TESTS_FAILED` is zero at `tests/test-error-scenarios.sh:211-226`.
- inputs_outputs_state: Inputs are template directory paths, template names, raw template strings, and `VAR=value` substitution pairs. Outputs are captured command substitutions from `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir`, plus process exit codes. Test state is maintained in `TESTS_PASSED` and `TESTS_FAILED` counters (`tests/test-error-scenarios.sh:22-35`).
- gates_or_invariants: Missing template file and missing template directory must return empty content with exit 0 for `load_template` (`tests/test-error-scenarios.sh:42-65`). `load_and_render` with a missing template must also return empty and exit 0 (`tests/test-error-scenarios.sh:67-78`). Rendering empty content must return empty without error (`tests/test-error-scenarios.sh:80-91`). Special regex-like characters in values must be inserted literally (`tests/test-error-scenarios.sh:93-106`). Strict mode must not abort when a missing template is loaded inside `set -euo pipefail` (`tests/test-error-scenarios.sh:108-131`). `load_and_render_safe` must use fallback text and substitute fallback variables (`tests/test-error-scenarios.sh:133-158`). `validate_template_dir` must accept the real template dir and reject a nonexistent one (`tests/test-error-scenarios.sh:160-180`). Empty placeholder `{{}}` and unclosed `{{` must remain stable in the checked scenarios (`tests/test-error-scenarios.sh:182-206`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh` from `tests/test-error-scenarios.sh:13`. The implementation under test defines `get_template_dir` (`hooks/lib/template-loader.sh:26-31`), `load_template` returning empty with a warning on missing files (`hooks/lib/template-loader.sh:36-48`), `render_template` as a single-pass awk renderer (`hooks/lib/template-loader.sh:58-132`), `load_and_render` (`hooks/lib/template-loader.sh:136-147`), `load_and_render_safe` with fallback behavior (`hooks/lib/template-loader.sh:170-203`), and `validate_template_dir` requiring `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:208-222`). This test is included in the aggregate runner list at `tests/run-all-tests.sh:42`.
- edge_cases_or_failure_modes: The test intentionally covers absent files, absent directories, strict mode with command substitution, fallback substitution, special regex characters that would break sed-style renderers, empty placeholder names, and unclosed placeholders. One subtle invariant is that `load_template` reports missing files to stderr but still prints an empty string and exits successfully, so callers under strict mode can continue. Another is that `render_template` is single-pass, so substituted values are not recursively interpreted.
- validation_or_tests: This file is itself a validator. It uses explicit expected/got comparisons and exits 1 if any test failed. It overlaps with but is narrower than `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`, both of which also exercise `template-loader.sh` paths shown by repository search.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-082 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when the RLCR stop hook detects uncommitted git changes. It defines the user-facing remediation contract for the “git not clean” gate.
- algorithmic_behavior: The template renders a stop-block reason with `{{GIT_ISSUES}}` and optional `{{SPECIAL_NOTES}}`, then instructs the agent to review untracked files, stage real changes, commit with a project-conventional message, avoid AI authorship metadata, and retry exit only after committing. The template body is at `prompt-template/block/git-not-clean.md:1-16`.
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES` placeholders (`prompt-template/block/git-not-clean.md:3-4`). Output is markdown text passed as the `reason` field in a JSON block decision from `hooks/loop-codex-stop-hook.sh`. At the call site, `GIT_ISSUES` is set to `uncommitted changes` when cached git status is nonempty, and `SPECIAL_NOTES` is assembled from `.humanize*` and other untracked-file notes (`hooks/loop-codex-stop-hook.sh:516-545`).
- gates_or_invariants: This template is part of the pre-Codex-review fast gate. If `GIT_ISSUES` is nonempty, the stop hook does not run expensive review; it returns a JSON decision of `block` and a system message telling the agent to commit first (`hooks/loop-codex-stop-hook.sh:548-569`). The remediation invariant is commit-before-exit, with no AI co-author trailers (`prompt-template/block/git-not-clean.md:11-16`). It also explicitly suggests using `code-simplifier` before committing when available (`prompt-template/block/git-not-clean.md:5-6`).
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`, which invokes `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` with a fallback message and the two variables (`hooks/loop-codex-stop-hook.sh:551-559`). Rendering depends on `hooks/lib/template-loader.sh`; fallback semantics matter because the stop hook must still block even if the template is missing. `SPECIAL_NOTES` may depend on sibling templates `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md` (`hooks/loop-codex-stop-hook.sh:528-544`).
- edge_cases_or_failure_modes: If this template is missing or empty, the hook uses the inline fallback beginning `# Git Not Clean` (`hooks/loop-codex-stop-hook.sh:551-556`). If `.humanize*` paths are the only untracked files, the special note clarifies they may be intentionally untracked, but the gate still blocks because cached git status was nonempty. If other untracked files exist, the hook adds an artifact-review note. Because rendering is single-pass, any placeholder-looking text inside `SPECIAL_NOTES` should not trigger recursive expansion.
- validation_or_tests: The template path is exercised indirectly by template tests. `tests/test-templates-comprehensive.sh` checks template discovery and renderability across prompt templates, while `tests/test-error-scenarios.sh` and `tests/robustness/test-template-error-robustness.sh` validate fallback behavior used by this hook if the template is unavailable. Repository search shows `git-not-clean.md` is a direct `load_and_render_safe` call in `hooks/loop-codex-stop-hook.sh:557`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-112 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Next-round instruction fragment appended after a Full Goal Alignment Check. It turns Codex alignment findings into prioritized action categories for the next RLCR implementation round.
- algorithmic_behavior: The template has no variables. It appends three concrete review-followup priorities: address forgotten items, prioritize any unmet acceptance criteria, and un-defer unjustified deferrals. The content is at `prompt-template/claude/post-alignment-action-items.md:2-7`.
- inputs_outputs_state: Input is the boolean state `FULL_ALIGNMENT_CHECK` in `hooks/loop-codex-stop-hook.sh`. When true, the hook loads this template and appends it to `round-${NEXT_ROUND}-prompt.md`; no substitution is performed (`hooks/loop-codex-stop-hook.sh:1537-1543`). Output is additional markdown in the next-round prompt after the normal Codex feedback prompt and before footer/push/goal-tracker update instructions (`hooks/loop-codex-stop-hook.sh:1520-1565`).
- gates_or_invariants: This fragment reinforces the Goal Tracker invariant described by the command: acceptance criteria and deferred work cannot silently drift. It only appears after full alignment checks, which the command describes as configurable, default every five rounds, at `commands/start-rlcr-loop.md:35-40`. The setup script stores `full_review_round` in loop state (`scripts/setup-rlcr-loop.sh:716-717`) and advertises the interval in setup output (`scripts/setup-rlcr-loop.sh:1017`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:1539`, using `load_template` rather than `load_and_render_safe`. It depends on template discovery through `TEMPLATE_DIR`, which is set via `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. It is coordinated with `claude/next-round-prompt.md`, `claude/next-round-footer.md`, optional `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md` in the same next-round prompt build path (`hooks/loop-codex-stop-hook.sh:1523-1565`).
- edge_cases_or_failure_modes: Because the hook uses `load_template` and checks `[[ -n "$POST_ALIGNMENT" ]]`, a missing or empty template simply omits this extra instruction block while the next-round prompt still exists (`hooks/loop-codex-stop-hook.sh:1537-1543`). Since there are no placeholders, malformed variable input is not relevant. The main behavioral risk is silent loss of post-alignment emphasis if the template is absent.
- validation_or_tests: Template reference tests and comprehensive template rendering tests cover existence/renderability patterns for prompt templates. The `load_template` missing-file behavior is validated by `tests/test-error-scenarios.sh:42-65`, which is relevant because this caller intentionally tolerates missing templates.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-142 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: Broad robustness suite for template-system error handling under malformed template syntax, invalid variable names, filesystem anomalies, concurrent loads, and discovery edge cases.
- algorithmic_behavior: The script runs under `set -euo pipefail`, sources `template-loader.sh` and shared `test-helpers.sh`, creates a temporary test directory with cleanup, then executes 20 tests grouped by missing templates, malformed templates, invalid variable names, variable-value edge cases, filesystem edge cases, concurrent loading, and discovery. Setup is at `tests/robustness/test-template-error-robustness.sh:13-20`; final summary delegates exit status to `print_test_summary` at `tests/robustness/test-template-error-robustness.sh:340-341`.
- inputs_outputs_state: Inputs are temporary template files under `$TEST_DIR`, synthetic template strings, template names, fallback strings, and variable assignments. Outputs are rendered strings and pass/fail counters owned by `tests/test-helpers.sh`, where `pass`, `fail`, and `print_test_summary` maintain counters and return nonzero on failures (`tests/test-helpers.sh:22-77`). Test directory state is created with `mktemp -d` and removed by an EXIT trap (`tests/test-helpers.sh:84-89`).
- gates_or_invariants: Missing templates and missing directories must return fallback text through `load_and_render_safe` (`tests/robustness/test-template-error-robustness.sh:34-51`). Empty templates may produce empty content or fallback, but must not fail unexpectedly (`tests/robustness/test-template-error-robustness.sh:53-63`). Malformed placeholders must not crash and should produce nonempty output for unclosed, nested, opening-only, and empty-name forms (`tests/robustness/test-template-error-robustness.sh:73-115`). Invalid variable names with spaces, special characters, or digits must not crash (`tests/robustness/test-template-error-robustness.sh:125-155`). Values containing template syntax must not recursively expand (`tests/robustness/test-template-error-robustness.sh:165-174`). Newlines in values must survive enough to include `line1` (`tests/robustness/test-template-error-robustness.sh:176-186`). BOM, whitespace-only files, filenames with spaces, permission denial, concurrent reads, subdirectories, and symlinks are all expected to behave gracefully (`tests/robustness/test-template-error-robustness.sh:208-334`).
- dependencies_and_callers: Direct dependencies are `hooks/lib/template-loader.sh` and `tests/test-helpers.sh` (`tests/robustness/test-template-error-robustness.sh:17-18`). The implementation under test uses awk-based single-pass substitution (`hooks/lib/template-loader.sh:71-129`) and fallback rendering (`hooks/lib/template-loader.sh:170-203`). This robustness suite is included in the aggregate test runner at `tests/run-all-tests.sh:70`, so it participates in repository-level validation.
- edge_cases_or_failure_modes: This suite deliberately tolerates platform variance for permission tests and symlink tests: permission handling may return fallback or another graceful result depending on privileges, and symlink tests skip/pass if symlinks are unsupported (`tests/robustness/test-template-error-robustness.sh:245-260`, `tests/robustness/test-template-error-robustness.sh:320-334`). Concurrent loading starts 10 background jobs and fails if any render incorrectly or if the source file disappears (`tests/robustness/test-template-error-robustness.sh:270-299`). A notable edge distinction from `tests/test-error-scenarios.sh` is that empty placeholder with an explicit empty variable assignment only needs nonempty output here, not preservation.
- validation_or_tests: This file is itself a validation artifact. It complements narrower unit tests by stressing filesystem and concurrency behavior while keeping assertions broad enough for cross-platform shells. It verifies the loader’s safety contract more than exact formatting in malformed cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`