# agent_12 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-facing RLCR prompt template directory. It defines review, full-alignment, goal-tracker-update, and code-review-audit contracts used by `hooks/loop-codex-stop-hook.sh` to drive loop transitions after Claude writes a round summary.
- algorithmic_behavior: Directory is flat with four files: `code-review-phase.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`. `regular-review.md` requires Codex to read `@{{PLAN_FILE}}`, compare Claude’s summary to the implementation, audit goal alignment, and only end with `COMPLETE` when all original tasks and ACs are fully complete with no deferrals (`prompt-template/codex/regular-review.md:3`, `:20`, `:33`, `:49`). `full-alignment-review.md` adds a mandatory periodic audit, acceptance-criteria table, forgotten/deferred item checks, historical stagnation review, and `STOP` circuit-breaker output for stalled work (`prompt-template/codex/full-alignment-review.md:19`, `:59`, `:76`, `:84`, `:91`). `goal-tracker-update-section.md` delegates post-round tracker mutation responsibility to Codex, while forbidding changes to the immutable goal/AC section (`prompt-template/codex/goal-tracker-update-section.md:3`, `:6`, `:10`). `code-review-phase.md` is an audit template for `codex review --base`, documenting that `codex review` does not accept prompt input and that severity markers `[P0-9]` determine whether to remediate or finalize (`prompt-template/codex/code-review-phase.md:3`, `:14`, `:15`, `:17`).
- inputs_outputs_state: Inputs are template variables such as `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `REVIEW_RESULT_FILE`, `FULL_REVIEW_ROUND`-derived values, and review base metadata. Outputs are rendered prompt/audit files under `.humanize/rlcr/<timestamp>/round-N-review-prompt.md` and review results expected at `round-N-review-result.md`. The hook renders the shared goal-tracker block first (`hooks/loop-codex-stop-hook.sh:767`) and injects it into either full-alignment or regular review (`hooks/loop-codex-stop-hook.sh:816`, `:834`). Code-review audit output is written before `codex review` execution (`hooks/loop-codex-stop-hook.sh:939`, `:957`).
- gates_or_invariants: The templates encode strict completion semantics: deferrals and pending work are incomplete, and `COMPLETE` must be the last line only when all original scope is done (`prompt-template/codex/regular-review.md:53`; `prompt-template/codex/full-alignment-review.md:91`). `STOP` is reserved for stagnation detection in full-alignment rounds (`prompt-template/codex/full-alignment-review.md:84`). The hook enforces exact last non-empty line matching to avoid false positives like “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:1397`). `COMPLETE` in implementation phase starts review/finalize flow, while `STOP` ends the loop via `stop-state.md` (`hooks/loop-codex-stop-hook.sh:1403`, `:1480`; `hooks/lib/loop-common.sh:1112`). Goal-tracker updates must not modify the immutable section (`prompt-template/codex/goal-tracker-update-section.md:10`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, using `load_and_render_safe` from `hooks/lib/template-loader.sh`. The loader performs single-pass `{{VAR}}` substitution to prevent placeholder injection and falls back when templates are missing or render empty (`hooks/lib/template-loader.sh:54`, `:170`). `hooks/loop-codex-stop-hook.sh` computes full-alignment cadence with `FULL_REVIEW_ROUND`, defaulting invalid values to `5` (`hooks/loop-codex-stop-hook.sh:773`). Review issue detection later delegates to `detect_review_issues`, which scans the combined codex review log for `[P0-9]` markers in the first 10 characters of a line (`hooks/lib/loop-common.sh:332`). This directory also sits next to the branch’s sandbox-bypass behavior: Codex execution arguments are built in the same hook, where `HUMANIZE_CODEX_BYPASS_SANDBOX=true|1` changes `--full-auto` to `--dangerously-bypass-approvals-and-sandbox` (`hooks/loop-codex-stop-hook.sh:891`, `:899`; `README.md:60`).
- edge_cases_or_failure_modes: Missing templates fall back to embedded fallback prompts, so the loop can continue with degraded prompt detail (`hooks/lib/template-loader.sh:179`). Invalid `FULL_REVIEW_ROUND` values are corrected to `5` (`hooks/loop-codex-stop-hook.sh:776`). If Codex does not create a nonempty review-result file, hook logic blocks exit and reports failure (`hooks/loop-codex-stop-hook.sh:1339`, `:1357`, `:1385`). In review phase, a `COMPLETE` marker in review content is ignored; only absence of `[P0-9]` issues allows finalize (`hooks/loop-codex-stop-hook.sh:1405`, `:1452`). A `STOP` on non-alignment rounds is unusual but honored (`hooks/loop-codex-stop-hook.sh:1495`).
- validation_or_tests: `tests/test-templates-comprehensive.sh` explicitly renders `codex/goal-tracker-update-section.md` and asserts that both the section title and substituted tracker path appear (`tests/test-templates-comprehensive.sh:531`). Finalize/review semantics are covered in `tests/test-finalize-phase.sh`, including `COMPLETE` triggering finalize entry and codex-review failure blocking exit as found by search hits. I inspected, but did not execute, tests in this read-only research pass.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime validation and initialization script for `/humanize:gen-plan`. It gates draft and output path correctness before the command proceeds, then creates the initial structured plan file by combining the plan template with the original draft.
- algorithmic_behavior: Parses only `--input`, `--output`, `-h`, and `--help`; missing values, flag-as-value, unknown options, or absent required args call `usage` and exit `6` (`scripts/validate-gen-plan-io.sh:16`, `:30`, `:58`). Converts paths through `realpath -m` if available, derives `OUTPUT_DIR`, and prints a validation header (`scripts/validate-gen-plan-io.sh:69`). Sequential checks exit early for missing input `1`, empty input `2`, missing output directory `3`, output path as directory `4`, existing output path `4`, and non-writable output dir `5` (`scripts/validate-gen-plan-io.sh:79`, `:87`, `:95`, `:103`, `:111`, `:118`). After successful IO checks, it locates `prompt-template/plan/gen-plan-template.md` via `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`scripts/validate-gen-plan-io.sh:133`). Missing template exits `7`; success copies the template to output and appends a delimited copy of the draft (`scripts/validate-gen-plan-io.sh:142`, `:148`).
- inputs_outputs_state: Inputs are CLI args `--input <draft>` and `--output <plan>`, current filesystem state, optional `CLAUDE_PLUGIN_ROOT`, and `prompt-template/plan/gen-plan-template.md`. Output on success is a newly-created plan file at the requested path, containing the template followed by `--- Original Design Draft Start ---`, draft content, and `--- Original Design Draft End ---` (`scripts/validate-gen-plan-io.sh:149`, `:153`, `:155`, `:157`). It emits diagnostic strings such as `VALIDATION_ERROR: INPUT_NOT_FOUND` and `VALIDATION_SUCCESS` to stdout/stderr-equivalent shell output.
- gates_or_invariants: Output target must be new and must not already exist, whether file or directory (`scripts/validate-gen-plan-io.sh:103`). Output directory must already exist and be writable; the script does not create it (`scripts/validate-gen-plan-io.sh:95`, `:118`). The input draft must exist and be nonempty before template lookup or output write (`scripts/validate-gen-plan-io.sh:79`, `:87`). Exit-code vocabulary is part of the command contract and is documented both in the script header and `commands/gen-plan.md` (`scripts/validate-gen-plan-io.sh:4`; `commands/gen-plan.md:38`).
- dependencies_and_callers: `commands/gen-plan.md` allows Bash execution of this script and instructs the command to run `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` as Phase 1 (`commands/gen-plan.md:4`, `:30`, `:35`). The command maps each exit code to a user-facing stop reason (`commands/gen-plan.md:38`). The script depends on standard shell utilities `realpath`, `dirname`, `wc`, `tr`, `cp`, and `cat`, with a fallback if `realpath -m` fails (`scripts/validate-gen-plan-io.sh:70`).
- edge_cases_or_failure_modes: `--help` exits `6`, same as invalid args, by design (`scripts/validate-gen-plan-io.sh:48`, `:23`). `set -e` means unexpected utility failures can abort. If `realpath -m` is unavailable, path strings remain as provided, which may affect diagnostic exactness but not validation order (`scripts/validate-gen-plan-io.sh:70`). The script checks write permission before template copy, but actual `cp`/append can still fail due to races, disk space, or permission changes after validation. Because the output file is written after validation, a concurrent creator between checks and `cp` could be overwritten by `cp`; the script does not use atomic exclusive creation.
- validation_or_tests: `tests/test-gen-plan.sh` verifies the script is executable, then asserts exit codes for missing values, flag-as-value, unknown option, missing input, empty input, missing output directory, existing output file, output-is-directory, valid paths, and help (`tests/test-gen-plan.sh:426`, `:433`, `:438`, `:474`, `:483`, `:493`, `:503`, `:513`, `:523`, `:532`). README documents `/humanize:gen-plan --input ... --output ...` usage (`README.md:115`, `:165`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for monitor shell compatibility under zsh. It protects the RLCR monitor from zsh `nomatch`/glob failures when loop, cache, or state directories are empty or contain only nonmatching entries.
- algorithmic_behavior: Requires zsh via shebang and verifies `ZSH_VERSION` later (`tests/test-zsh-monitor-safety.sh:1`, `:276`). Sets `set -euo pipefail`, creates an isolated `/tmp/test-zsh-humanize-$$`, exports `XDG_CACHE_HOME` to that temporary tree, and traps cleanup (`tests/test-zsh-monitor-safety.sh:12`, `:48`, `:52`, `:56`). Test 1 sources `scripts/humanize.sh` and fails if output contains `no matches found` (`tests/test-zsh-monitor-safety.sh:63`). Tests 2 and 3 exercise latest-session discovery using `find` with empty and dotfile-only `.humanize/rlcr` directories (`tests/test-zsh-monitor-safety.sh:80`, `:127`). Test 4 checks state-file fallback when no `*-state.md` exists (`tests/test-zsh-monitor-safety.sh:160`). Test 5 checks empty cache log discovery (`tests/test-zsh-monitor-safety.sh:208`). Test 6 validates timestamp-named session ordering (`tests/test-zsh-monitor-safety.sh:240`). Test 8 demonstrates why the old glob pattern was unsafe and that the new `find` pattern works (`tests/test-zsh-monitor-safety.sh:288`).
- inputs_outputs_state: Inputs are the project’s `scripts/humanize.sh`, synthetic `.humanize/rlcr` directories, synthetic cache directories under `XDG_CACHE_HOME`, and zsh runtime options. Outputs are PASS/FAIL counters and exit `0` only when `TESTS_FAILED` is zero (`tests/test-zsh-monitor-safety.sh:24`, `:333`). It mutates only temporary test directories outside the repository and removes them on exit (`tests/test-zsh-monitor-safety.sh:56`).
- gates_or_invariants: No monitored function or equivalent logic may emit zsh “no matches found” on empty/nonmatching directories. Session names must match `YYYY-MM-DD_HH-MM-SS` before being considered, and latest selection is lexicographic on that timestamp name (`tests/test-zsh-monitor-safety.sh:109`, `:110`, `:259`). Missing state returns `|unknown`, while `state.md` would indicate active and `<reason>-state.md` produces the reason (`tests/test-zsh-monitor-safety.sh:172`). The production helper being protected uses `find "$loop_dir" -mindepth 1 -maxdepth 1 -type d` specifically to avoid zsh glob errors (`scripts/lib/monitor-common.sh:49`, `:60`).
- dependencies_and_callers: Sources `scripts/humanize.sh`, which in turn sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6`). The relevant production helpers are `monitor_find_latest_session` and `monitor_find_state_file` (`scripts/lib/monitor-common.sh:37`, `:147`), plus `_find_latest_codex_log` inside `_humanize_monitor_codex` (`scripts/humanize.sh:242`). These helpers are used by monitor display paths to locate current sessions, current log files, and state status (`scripts/humanize.sh:591`, `:603`).
- edge_cases_or_failure_modes: The test intentionally covers missing loop dirs, empty loop dirs, dotfile-only loop dirs, no `*-state.md`, empty cache dirs, and mixed valid sessions. It also checks that old `for x in .humanize/rlcr/*` behavior would be unsafe under strict zsh (`tests/test-zsh-monitor-safety.sh:303`). One limitation is that several tests duplicate helper logic locally rather than directly invoking private nested functions; production coverage relies on matching the patterns from `humanize.sh` and `monitor-common.sh`.
- validation_or_tests: This file is itself the validation. Related broader coverage exists in `tests/test-monitor-e2e-real.sh`, which also greps monitor output for `no matches found|bad pattern` in multiple scenarios, as seen in search results. I inspected but did not execute it in this research pass.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Small block template for read/write validator denials when Claude attempts to access the correct round file name from the wrong loop directory. It provides corrective path feedback to keep agents within the active RLCR loop directory.
- algorithmic_behavior: Template renders a title, the attempted action/path, the correct path, and a terse command hint (`prompt-template/block/wrong-directory-path.md:1`, `:3`, `:4`, `:6`). It is selected after round-number validation succeeds but the full requested `FILE_PATH` differs from `ACTIVE_LOOP_DIR/$CLAUDE_FILENAME`.
- inputs_outputs_state: Inputs are `ACTION`, `FILE_PATH`, and `CORRECT_PATH` placeholders. Output is a markdown denial message written to stderr by validators; validators then exit `2`, which blocks the attempted read/write (`hooks/loop-read-validator.sh:159`, `:163`, `:167`; `hooks/loop-write-validator.sh:254`, `:258`, `:262`). It does not alter repository state.
- gates_or_invariants: The invariant is exact path equality against the active loop directory, not just matching filename or round number. `loop-read-validator.sh` computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and blocks if `FILE_PATH != CORRECT_PATH` (`hooks/loop-read-validator.sh:157`, `:159`). `loop-write-validator.sh` applies the same gate for summary writes (`hooks/loop-write-validator.sh:252`, `:254`). This prevents cross-session or stale-directory access to round files.
- dependencies_and_callers: Called through `load_and_render_safe` from `hooks/lib/template-loader.sh`, so missing-template fallback still blocks with a simpler message (`hooks/loop-read-validator.sh:160`; `hooks/loop-write-validator.sh:255`). It coordinates with sibling block templates such as wrong-round-file and wrong-round-number templates: those validate filename/round first, then this template handles directory mismatch (`hooks/loop-read-validator.sh:138`; `hooks/loop-write-validator.sh:235`).
- edge_cases_or_failure_modes: The final hint says `cat {{FILE_PATH}}`, but because `FILE_PATH` is the attempted wrong path while line 4 shows `CORRECT_PATH`, the hint may be misleading; for write attempts it is especially odd because the validator passes `ACTION=write to` but the hint still says `cat` (`prompt-template/block/wrong-directory-path.md:6`; `hooks/loop-write-validator.sh:259`). If the action/path contains unusual characters, rendering is plain substitution with no shell quoting. The gate itself remains in the caller, not the template.
- validation_or_tests: Template rendering behavior is indirectly covered by template-loader and validator tests in `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and allowlist/validator tests by path search. I inspected direct callers rather than executing tests.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-132 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `humanize_parse_goal_tracker`, the production parser that feeds monitor/status summaries from `.humanize/rlcr/<session>/goal-tracker.md`.
- algorithmic_behavior: Sources shared test helpers and production `scripts/humanize.sh`, creates a temporary test directory, and defines `parse_result` to split the parser’s seven-field pipe output (`tests/robustness/test-goal-tracker-robustness.sh:18`, `:21`, `:24`, `:39`). The function under test returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`tests/robustness/test-goal-tracker-robustness.sh:35`). Positive tests cover list-format ACs, table-format ACs, active task exclusion of completed/deferred rows, completed task count, unique completed AC count, and ultimate-goal extraction (`tests/robustness/test-goal-tracker-robustness.sh:60`, `:89`, `:118`, `:155`, `:194`, `:204`). Negative/edge tests cover nonexistent files, empty files, 60 ACs, special characters, malformed files, truncated files, binary content, open issue count, deferred task count, headers-only files, mixed bold/subnumber formats, and long-goal truncation (`tests/robustness/test-goal-tracker-robustness.sh:237`, `:246`, `:258`, `:281`, `:304`, `:328`, `:349`, `:373`, `:411`, `:441`, `:477`, `:501`).
- inputs_outputs_state: Inputs are synthetic goal-tracker markdown files written under `TEST_DIR`. Output from the parser is a pipe-delimited status string. Test pass/fail state is tracked through `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh`; `setup_test_dir` creates a temp dir and traps cleanup (`tests/test-helpers.sh:30`, `:35`, `:58`, `:86`). The test exits with the summary status (`tests/robustness/test-goal-tracker-robustness.sh:530`).
- gates_or_invariants: Parser must return `0|0|0|0|0|0|No goal tracker` for missing files (`tests/robustness/test-goal-tracker-robustness.sh:237`). AC counting must support `AC-1`, `AC1`, list rows, table rows, and bold `**AC-2**`, while decimal subcriteria are not counted by the documented regex expectation (`tests/robustness/test-goal-tracker-robustness.sh:477`). Active tasks equal active-section data rows minus rows whose status column is completed or deferred (`tests/robustness/test-goal-tracker-robustness.sh:146`). Goal summary must be first nonempty content under `### Ultimate Goal` and no longer than 60 chars (`tests/robustness/test-goal-tracker-robustness.sh:501`). Malformed files without proper `###` headers should fail gracefully with zero ACs (`tests/robustness/test-goal-tracker-robustness.sh:304`).
- dependencies_and_callers: Production parser is `humanize_parse_goal_tracker` in `scripts/humanize.sh`, which counts ACs with `sed` over `### Acceptance Criteria` to `---`, counts table rows in named sections, counts unique completed ACs, and truncates goal summary to 60 chars (`scripts/humanize.sh:30`, `:39`, `:48`, `:56`, `:86`, `:90`, `:96`, `:100`, `:104`). Monitor code wraps it as `_parse_goal_tracker` and uses it while drawing monitor status (`scripts/humanize.sh:367`). The parser depends on `sed`, `grep`, `sort`, `wc`, `tr`, `head`, and `cut`.
- edge_cases_or_failure_modes: Section detection is markdown-heading dependent; missing `### Acceptance Criteria` or `#### Active Tasks` causes zero counts even if content mentions ACs or tasks (`scripts/humanize.sh:52`, `:64`). Table row counting subtracts two header rows, so non-table prose in those sections is ignored (`scripts/humanize.sh:41`). Binary content is expected to be tolerated as long as at least parseable AC content remains (`tests/robustness/test-goal-tracker-robustness.sh:349`). The parser’s pipe-delimited output can be fragile if `goal_summary` contains `|`; this test suite does not cover pipe characters in the goal summary. It also accepts `TOTAL_ACS >= 1` for truncated/binary cases rather than exact counts, intentionally specifying graceful degradation.
- validation_or_tests: This file is itself the validation suite and ends with `print_test_summary "Goal Tracker Robustness Test Summary"` (`tests/robustness/test-goal-tracker-robustness.sh:530`). Related hook robustness also calls `humanize_parse_goal_tracker` in `tests/robustness/test-hook-input-robustness.sh` by search result. I inspected but did not execute tests in this read-only branch research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present; each assigned item is represented once as a section heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`