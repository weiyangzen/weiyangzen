# agent_30 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`
- scope_note: research-only pass; no files modified and no scheduler checkout touched.

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-030 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` gate for RLCR and PR-loop state surfaces. It is registered for the `Write` matcher in `hooks/hooks.json:14-21` and prevents Claude from writing generated prompts, state files, protected todos, wrong-round summaries, PR-loop generated files, and post-round-zero goal tracker state.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON/UTF-8/tool shape, rejects deep nesting, and ignores non-`Write` tools at `hooks/loop-write-validator.sh:25-45`. It blocks RLCR `round-*-todos.md` unless allowlisted, blocks `round-*-prompt.md`, protects `.humanize/pr-loop/` state/read-only files, validates `round-N-pr-resolve.md` round alignment, then separately validates RLCR summary/finalize/goal-tracker/plan-backup paths at `hooks/loop-write-validator.sh:55-117`. It locates the active loop, chooses `state.md` or `finalize-state.md`, strictly parses current round, and then enforces state-file, finalize-summary, goal-tracker, summary-location, summary-round, and active-directory path rules at `hooks/loop-write-validator.sh:123-263`.
- inputs_outputs_state: Inputs are hook JSON with `tool_name` and `tool_input.file_path`, `CLAUDE_PROJECT_DIR` or `pwd`, active `.humanize/rlcr/*` and `.humanize/pr-loop/*` directories, YAML frontmatter state files, and rendered block templates. Outputs are exit `0` allow, exit `1` malformed/safety failure, or exit `2` policy block with a human-readable reason on stderr; the hook does not mutate loop state.
- gates_or_invariants: Generated loop state is write-protected, summaries must be in the active RLCR loop directory and current round, `goal-tracker.md` is blocked after round `0`, `finalize-summary.md` is allowed only in the active finalize loop, PR-loop generated review/comment/prompt/check/feedback files are read-only, and PR resolve writes must target the current PR round. The strict parser fails closed on malformed state at `hooks/loop-write-validator.sh:140-145`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, active-loop detection, strict state parsing, path predicates, allowlists, and rendered block messages; key helper definitions are at `hooks/lib/loop-common.sh:59-133`, `hooks/lib/loop-common.sh:156-174`, `hooks/lib/loop-common.sh:255-330`, `hooks/lib/loop-common.sh:401-525`, and `hooks/lib/loop-common.sh:783-905`. Runtime dependencies include `jq`, `sed`, `grep`, `basename`, and `template-loader.sh`.
- edge_cases_or_failure_modes: If no active RLCR loop exists, protected RLCR summary/state checks fall through to allow at `hooks/loop-write-validator.sh:128-130`. `finalize-state.md` must be checked before generic `state.md` because the generic predicate also matches it, documented at `hooks/loop-write-validator.sh:150-154`. The hardcoded allowlist in `hooks/lib/loop-common.sh:423-441` permits specific early todos/summaries. Path equality checks use the provided path string, so noncanonical but equivalent paths are a sensitivity to watch.
- validation_or_tests: Covered by PR-loop write validator tests that block PR state/comment and allow current-round resolve writes at `tests/test-pr-loop-hooks.sh:85-163`, finalize-phase tests that allow `finalize-summary.md` and block `finalize-state.md` at `tests/test-finalize-phase.sh:341-367`, plus allowlist/template-reference/robustness test suites found by repository references.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-060 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for setup-time RLCR plan-file, branch-name, content, CLI, and Codex-parameter validation in `scripts/setup-rlcr-loop.sh`. It protects the algorithm’s bootstrap boundary: only a safe, meaningful, correctly tracked plan file can seed loop state.
- algorithmic_behavior: Creates isolated temp git repositories, unsets `CLAUDE_PROJECT_DIR`, mocks `codex`, invokes `scripts/setup-rlcr-loop.sh`, captures exit/output, and records PASS/FAIL/SKIP counters at `tests/test-plan-file-validation.sh:16-38` and `tests/test-plan-file-validation.sh:73-83`. It exercises path rejection for absolute paths, missing files/dirs, spaces, metacharacters, symlinks, permission/path-resolution failures, project escapes, non-git repos, and repos without commits at `tests/test-plan-file-validation.sh:88-322`. It then verifies tracking policy, YAML-unsafe branch names, content sufficiency, CLI option exclusivity, and Codex model/effort validation at `tests/test-plan-file-validation.sh:328-751`.
- inputs_outputs_state: Inputs are generated temp plan files, git repos, command-line options such as `--plan-file`, `--track-plan-file`, and `--codex-model`, plus mocked PATH entries. Outputs are console test results and final exit code equal to `TESTS_FAILED` at `tests/test-plan-file-validation.sh:742-751`; all test repo writes are confined to temp directories cleaned by trap at `tests/test-plan-file-validation.sh:36-38`.
- gates_or_invariants: The tested setup script must require a git repo with at least one commit, relative plan paths only, no spaces or shell metacharacters, no file or parent-directory symlinks, readable file inside project root, no submodule plan location, clean/tracked behavior only when `--track-plan-file` is set, gitignored/untracked behavior otherwise, at least five physical lines, at least three nonblank noncomment content lines, YAML-safe branch/model/effort values, and mutual exclusion between `--plan-file` and positional plan args. Implementation anchors are `scripts/setup-rlcr-loop.sh:263-307`, `scripts/setup-rlcr-loop.sh:320-474`, `scripts/setup-rlcr-loop.sh:483-541`, and `scripts/setup-rlcr-loop.sh:555-587`.
- dependencies_and_callers: Depends on `git`, shell utilities, the real `scripts/setup-rlcr-loop.sh`, and a mocked `codex` executable. CI runs it in `.github/workflows/plan-file-test.yml:42-45`, triggered by setup, hook, common-lib, and plan-file test changes at `.github/workflows/plan-file-test.yml:3-29`.
- edge_cases_or_failure_modes: Permission-denial coverage is skipped if the test environment cannot `chmod 000` at `tests/test-plan-file-validation.sh:225-241`. Unsafe branch-name tests accept either git rejecting the branch or the setup script rejecting it at `tests/test-plan-file-validation.sh:431-510`. Some positive tests assert absence of a specific validation error rather than complete setup success, because later setup stages may still fail for unrelated environment reasons.
- validation_or_tests: This file is itself the validation asset and is part of the plan-file CI workflow. It also complements robustness suites referenced in the repo, including production path and plan-file robustness tests.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-090 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: PR-loop block template for the “missing trigger comment” transition gate. It tells the operator the loop cannot proceed until a PR comment mentioning the configured bot string is posted.
- algorithmic_behavior: The template renders startup case, startup-case description, current round, and bot mention string into a Markdown block at `prompt-template/block/no-trigger-comment.md:1-17`. `hooks/pr-loop-stop-hook.sh` loads it when `REQUIRE_TRIGGER` is true and `PR_LAST_TRIGGER_AT` is empty, then returns a JSON stop-hook block decision at `hooks/pr-loop-stop-hook.sh:727-745`.
- inputs_outputs_state: Inputs are `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING`. Output is a rendered reason embedded in `{"decision":"block"}` from the stop hook; no loop files are changed on this path, and polling does not start.
- gates_or_invariants: A trigger must exist before bot polling and before Claude eyes verification. The stop hook intentionally validates this first so stale or missing trigger IDs do not cause wasted eyes-reaction checks, as documented at `hooks/pr-loop-stop-hook.sh:724-725` and enforced before `hooks/pr-loop-stop-hook.sh:748-799`.
- dependencies_and_callers: Called through `load_and_render_safe` in `hooks/pr-loop-stop-hook.sh:739-741`, using fallback text at `hooks/pr-loop-stop-hook.sh:736-738` if template rendering fails. The mention string is built from configured bots via shared PR-loop helpers, and the trigger requirement is derived by the PR-loop stop-hook state machine.
- edge_cases_or_failure_modes: Startup cases `4` and `5` get specific descriptions for new commits after previous bot review states, while all other required-trigger cases use “Subsequent round requires trigger” at `hooks/pr-loop-stop-hook.sh:728-734`. If the hook reaches a later no-trigger defensive path, it still blocks before polling at `hooks/pr-loop-stop-hook.sh:847-865`.
- validation_or_tests: Missing-trigger behavior is covered by a logic-level hook test at `tests/test-pr-loop-hooks.sh:606-621` and an end-to-end stop-hook case for startup case `4` with no trigger comments at `tests/test-pr-loop-stophook.sh:642-700`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-120 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop Codex prompt block defining the goal-tracker update contract after bot-review analysis. It tells Codex how to advance the PR goal tracker’s issue summary, totals, and issue log for the next review round.
- algorithmic_behavior: The template requires adding one Issue Summary row for `{{NEXT_ROUND}}`, updating total found/resolved/remaining counts, and appending a round log entry at `prompt-template/pr-loop/codex-goal-tracker-update.md:3-20`. It provides an example at `prompt-template/pr-loop/codex-goal-tracker-update.md:22-55` and rules to preserve structure, update only named sections, append rather than replace, and use one row per reviewer at `prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`.
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE`, `NEXT_ROUND`, and Codex’s per-bot review findings. Output is intended edits to `.humanize/pr-loop/<loop>/goal-tracker.md`, specifically Issue Summary rows, Total Statistics counters, and Issue Log entries; the surrounding stop hook also writes Codex’s analysis to `round-N-pr-check.md`.
- gates_or_invariants: The status vocabulary for tracker rows is constrained to “Issues Found”, “All Resolved”, or “Approved”; totals must satisfy Remaining = Total Found - Total Resolved; header sections such as PR Information and Ultimate Goal must not be modified; existing tables are extended instead of replaced. The generated Codex prompt also requires a final recommendation marker such as `APPROVE`, `ISSUES_REMAINING`, `WAITING_FOR_BOTS`, or `USAGE_LIMIT_HIT` before including this tracker block at `hooks/pr-loop-stop-hook.sh:1274-1302`.
- dependencies_and_callers: Loaded into the Codex prompt by `hooks/pr-loop-stop-hook.sh:1252-1262`, then appended to `CODEX_PROMPT_FILE` at `hooks/pr-loop-stop-hook.sh:1301`. The Codex run path supports sandbox bypass selection via `HUMANIZE_CODEX_BYPASS_SANDBOX` at `hooks/pr-loop-stop-hook.sh:1328-1336`. The shared fallback updater `update_pr_goal_tracker` documents the same responsibilities and acts as an idempotent repair path at `hooks/lib/loop-common.sh:1151-1167`, with duplicate/partial update handling at `hooks/lib/loop-common.sh:1184-1218`.
- edge_cases_or_failure_modes: If Codex does not update the tracker, the stop hook derives issue counts from `round-N-pr-check.md` and calls `update_pr_goal_tracker` at `hooks/pr-loop-stop-hook.sh:1511-1540`. The fallback updates totals only when adding a new summary row to avoid double-counting, then inserts a row and appends an Issue Log entry at `hooks/lib/loop-common.sh:1256-1313`. Partial tracker updates are completed rather than duplicated.
- validation_or_tests: Stop-hook goal-tracker updating is covered by `tests/test-pr-loop-system.sh:1541-1754`, which mocks `gh`, `git`, and `codex`, runs the stop hook, and verifies a round row plus codex entry were written.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`