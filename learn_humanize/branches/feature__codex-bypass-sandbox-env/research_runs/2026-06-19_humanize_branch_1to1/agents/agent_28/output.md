# agent_28 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit gate for active RLCR loops. It prevents continuing a loop when the state file is malformed, the working branch has changed, or the plan file’s git-tracking status no longer matches the mode recorded at loop start. It is registered as a `UserPromptSubmit` command in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/hooks.json:4).
- algorithmic_behavior: The hook reads stdin to satisfy the hook contract, finds the newest active RLCR session under `.humanize/rlcr`, chooses `finalize-state.md` over `state.md` when finalize phase is active, then strict-parses YAML frontmatter through `parse_state_file_strict` from [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/lib/loop-common.sh:255). If there is no active loop it exits silently. If state parsing fails, it fails closed with stderr and exit `1` at [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-plan-file-validator.sh:45). It then checks schema fields `plan_tracked` and `start_branch`, verifies current git branch against the recorded start branch, and checks plan-file tracking or cleanliness according to `plan_tracked`.
- inputs_outputs_state: Inputs are hook stdin, `CLAUDE_PROJECT_DIR` or current working directory, `.humanize/rlcr/<session>/state.md` or `finalize-state.md`, git branch state, and `PLAN_FILE` from state frontmatter. Outputs are either no output with exit `0` for allow, JSON with `"decision": "block"` and a human-readable `reason` for recoverable policy blocks, or exit `1` for malformed state. State read includes `STATE_PLAN_TRACKED`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH`; no repository or loop files are modified by this script.
- gates_or_invariants: Requires active-loop state to contain strict parser fields such as `current_round`, `max_iterations`, `review_started`, and `base_branch` via the common parser, then separately requires `plan_tracked` and `start_branch` at [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-plan-file-validator.sh:79). Branch invariant: current `git rev-parse --abbrev-ref HEAD` must equal `start_branch` or the hook blocks at line 107. If `plan_tracked=true`, the plan file must remain tracked and have empty `git status --porcelain`; if `plan_tracked=false`, the plan file must remain untracked/gitignored.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, state constants, strict parsing, and template loader initialization; depends on `scripts/portable-timeout.sh` for `run_with_timeout`; depends on `git` and `jq`. `find_active_loop` only considers the newest timestamp-like child with `state.md` or `finalize-state.md` ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/lib/loop-common.sh:151)). Schema block text uses `prompt-template/block/schema-outdated.md` through `load_and_render_safe`, falling back inline if template loading fails.
- edge_cases_or_failure_modes: No active loop is a silent allow. Malformed or missing strict frontmatter is a hard safety failure. Git command timeout uses exit code `124` and blocks with a timeout reason. Unexpected git errors also block. YAML quote stripping is delegated to common parsing for `plan_file` and `start_branch`; tests cover quoted branch and hyphenated plan paths. A subtle shell edge is that `GIT_EXIT_CODE`, `LS_FILES_EXIT`, and `STATUS_EXIT` are not locally reset before every command; in the current script each variable is initialized by the first command path before use, but future refactors should avoid stale exit-code reuse.
- validation_or_tests: Covered by [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-plan-file-hooks.sh:119) for valid state, missing schema fields, branch changes, quoted YAML fields, and hyphenated plan paths. Finalize-state parsing is covered at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-finalize-phase.sh:853). Broader hook robustness includes empty JSON handling and non-plan file pass-through at [tests/robustness/test-hook-system-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/robustness/test-hook-system-robustness.sh:232).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for RLCR monitor shutdown behavior in `scripts/humanize.sh`. It validates that monitoring exits cleanly when `.humanize/rlcr` disappears and that terminal/trap cleanup is idempotent and signal-safe.
- algorithmic_behavior: The test creates a temp project under `/tmp/test-monitor-runtime-$$`, sources or mirrors monitor functions, and runs nine groups: graceful stop message and cleanup, idempotent cleanup, loop-directory deletion detection, terminal restoration, `_graceful_stop` calling `_cleanup`, bash SIGINT handling, bash/zsh trap definitions, trap reset inside cleanup, and a real zsh SIGINT check when `zsh` exists. Pass/fail counters are maintained with shell functions at lines 25 and 30, and the script exits nonzero if any required check fails.
- inputs_outputs_state: Inputs are the repository’s `scripts/humanize.sh`, a temporary `.humanize/rlcr/<timestamp>/state.md`, shell signal delivery, optional `zsh`, and grep-based source inspections. Outputs are TAP-like colored `PASS`/`FAIL` lines and final exit `0` or `1`. Runtime state is limited to the temp directory and child test scripts; cleanup removes the temp base via trap at [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-monitor-runtime.sh:49).
- gates_or_invariants: `_cleanup` must run only once even under repeated `_graceful_stop` and direct cleanup calls. `_restore_terminal` must reset the scroll region using `printf "\033[r"` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/scripts/humanize.sh:523). `_cleanup` must reset traps, stop monitor state, kill tail process if present, call `_restore_terminal`, and print a stop message ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/scripts/humanize.sh:537)). `_graceful_stop` must call `_cleanup` before printing its reason ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/scripts/humanize.sh:565)). The main loop must check `.humanize/rlcr` before normal iteration and inside both no-log and incremental monitoring loops ([scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/scripts/humanize.sh:620), lines 662 and 755).
- dependencies_and_callers: Depends on `bash`, `grep`, `kill`, `sleep`, temp filesystem access, repository `scripts/humanize.sh`, and optionally `zsh`. It is included in the aggregate test order in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/run-all-tests.sh:50).
- edge_cases_or_failure_modes: The test intentionally cannot fully exercise real terminal `tput` effects in non-interactive mode, so it combines runtime stubs with source greps. One local snippet at [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-monitor-runtime.sh:282) pipes `grep -q` into another `grep -q`; because the first `grep -q` emits no stdout, that inner conditional is ineffective, but the actual assertion for `_cleanup -> _restore_terminal` is repeated correctly at lines 305-311. The zsh runtime test is conditional and skipped if `zsh` is unavailable, so it is not a hard portability gate on minimal systems.
- validation_or_tests: This file is itself the validation asset. It directly encodes expected behavior for monitor cleanup and signal paths, and it cross-checks the real `scripts/humanize.sh` function definitions and traps. No tests were executed during this research pass because the instruction was to produce research notes only and not modify files.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template used as a pre-review gate when Claude tries to stop an RLCR round while TodoWrite still contains unfinished tasks. It prevents expensive Codex review from starting before the local work queue is complete.
- algorithmic_behavior: The template renders a short block message titled “Incomplete Todos Detected”, injects `{{INCOMPLETE_LIST}}`, and gives required action: finish all todos, mark them completed via TodoWrite, then proceed to summary and stop. It explicitly says not to proceed to Codex review until all todos are finished.
- inputs_outputs_state: Input variable is `INCOMPLETE_LIST` from `hooks/check-todos-from-transcript.py`. The rendered output becomes the `reason` field in a JSON block response from `hooks/loop-codex-stop-hook.sh` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-codex-stop-hook.sh:381). It does not mutate state itself; it controls the conversational transition by blocking Stop.
- gates_or_invariants: The gate is driven by todo checker exit code `1`. The checker reads the most recent TodoWrite call from transcript JSONL and treats every status other than `completed` as incomplete, formatting each as `- [status] content` ([hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/check-todos-from-transcript.py:118)). Parse errors exit `2` and produce a different block path; empty input, missing transcript, no todos, or all-completed todos allow proceeding.
- dependencies_and_callers: Called only through the RLCR Codex stop hook’s quick todo check ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-codex-stop-hook.sh:340)). Depends on `load_and_render_safe` single-pass template substitution in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/lib/template-loader.sh:170), so missing templates degrade to the inline fallback at stop-hook lines 376-380.
- edge_cases_or_failure_modes: If `check-todos-from-transcript.py` is absent, the stop hook skips this todo gate entirely because the block runs only when the file exists. If transcript JSONL contains malformed lines, they are ignored by the checker; malformed hook stdin is a parse-error block instead. The checker uses only the latest non-empty TodoWrite list, so an older incomplete todo list is superseded by a later completed list. The template itself relies on caller-provided escaping via `jq -n --arg`, so injected todo content is treated as JSON string data.
- validation_or_tests: Todo parsing behavior is covered in [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-todo-checker.sh:51), including invalid JSON, empty input, nonexistent transcript, incomplete statuses, in-progress statuses, invalid JSONL lines, and multiple TodoWrite calls. The template reference is discoverable through repo-wide template reference checks, though the critical safe-render check shown in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-template-references.sh:172) focuses on the read/write/edit validators rather than this stop hook.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-118 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular RLCR implementation-review prompt for Codex during non-full-alignment rounds. It defines the review contract that decides whether Claude may move from implementation into review/finalize flow or must continue another round.
- algorithmic_behavior: The template instructs Codex to read the original plan file first, compare Claude’s claimed work and summary against both the plan and current prompt, perform a skeptical implementation review, check goal alignment, handle pending/deferred work as incomplete, optionally draft a directive implementation plan for unfinished work, and write review comments to `{{REVIEW_RESULT_FILE}}`. It makes `COMPLETE` the terminal marker only when all original tasks and acceptance criteria are fully satisfied with no deferred or pending work.
- inputs_outputs_state: Render inputs include `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, derived round values, and `REVIEW_RESULT_FILE`. The rendered prompt is written to `.humanize/rlcr/<session>/round-<n>-review-prompt.md` by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-codex-stop-hook.sh:834). In implementation phase, that rendered prompt is fed to `codex exec`; Codex is expected to create or stdout review content, which the hook copies to `round-<n>-review-result.md` if necessary ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-codex-stop-hook.sh:1264), lines 1281 and 1341). The hook then reads the last non-empty line and interprets exact `COMPLETE` as implementation completion.
- gates_or_invariants: The prompt forbids accepting deferrals or pending work as complete. It requires a Goal Alignment Summary with acceptance-criteria count, forgotten-item count, and unjustified-deferral count. The stop hook independently requires Codex output to exist and be non-empty before interpreting it, and only an exact last-line `COMPLETE` has terminal effect; phrases such as “CANNOT COMPLETE” do not match because the hook trims and compares the whole last line ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-codex-stop-hook.sh:1397)).
- dependencies_and_callers: Used by `hooks/loop-codex-stop-hook.sh` on rounds that are not full-alignment rounds; full alignment uses a sibling template instead. It embeds `prompt-template/codex/goal-tracker-update-section.md` via a pre-rendered variable from stop-hook lines 767-771. It depends on `load_and_render_safe`, Codex CLI availability, state-derived model/effort/timeout fields, and the stop hook’s review-phase state machine. Once implementation review returns `COMPLETE`, the hook changes `review_started` to `true`, creates `.review-phase-started`, and runs `codex review --base` as a separate review phase ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/hooks/loop-codex-stop-hook.sh:1428)).
- edge_cases_or_failure_modes: The template tells Codex to write a file, but the hook tolerates stdout-only output by copying stdout to the expected file. If Codex exits nonzero, creates no result, or creates an empty result, the hook blocks and asks for retry. In review phase, the regular-review prompt is no longer the governing reviewer; `codex review --base` runs without accepting this prompt, and issue detection is based on severity markers in review output. Full-alignment cadence bypasses this template, so its contract applies only to regular rounds.
- validation_or_tests: Template rendering safety and `load_and_render_safe` behavior are tested in template-loader suites; stop-hook behavior around quoted state fields and mocked Codex is exercised in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/feature__codex-bypass-sandbox-env/tests/test-plan-file-hooks.sh:424). Review-phase command construction and failure handling are anchored in `hooks/loop-codex-stop-hook.sh`; no live Codex run was performed in this research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-028, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-058, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-088, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-118
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`