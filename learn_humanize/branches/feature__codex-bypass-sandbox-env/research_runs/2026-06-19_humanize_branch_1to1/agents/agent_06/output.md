# agent_06 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts` is the repository’s runtime command layer. It starts and cancels RLCR/PR review loops, fetches and polls GitHub review state, verifies bot reactions, validates gen-plan IO, and exposes terminal monitors. The directory is not a single algorithm but a coordinated shell workflow surface around `.humanize/rlcr` and `.humanize/pr-loop` state directories.
- algorithmic_behavior: The directory splits responsibilities into setup, state transition, external API fetch/poll, safety wrappers, and monitors. `setup-rlcr-loop.sh` validates a plan, git state, base branch, Codex settings, and creates `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:263`, `scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:667`, `scripts/setup-rlcr-loop.sh:709`). `setup-pr-loop.sh` resolves a PR, creates `.humanize/pr-loop/<timestamp>`, fetches initial comments, computes startup case, optionally posts re-review triggers, verifies Claude eyes reaction, then renders round-0 prompt/critical templates (`scripts/setup-pr-loop.sh:208`, `scripts/setup-pr-loop.sh:283`, `scripts/setup-pr-loop.sh:430`, `scripts/setup-pr-loop.sh:437`, `scripts/setup-pr-loop.sh:462`, `scripts/setup-pr-loop.sh:615`, `scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:800`). `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh` form the PR-loop GitHub observation layer.
- inputs_outputs_state: Primary inputs are command arguments, `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_ROOT`, git repository state, GitHub CLI output, `jq` JSON transforms, Codex CLI availability, and prompt templates. Outputs are markdown prompts/comments/state under `.humanize/**`, JSON hook decisions from associated hooks, terminal monitor output, and command stdout/status codes. `cancel-pr-loop.sh` and `cancel-rlcr-loop.sh` transition active `state.md` or `finalize-state.md` into `cancel-state.md` and create `.cancel-requested` (`scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`, `scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`).
- gates_or_invariants: Only one RLCR or PR loop may be active; both setup scripts call shared loop discovery before starting (`scripts/setup-rlcr-loop.sh:240`, `scripts/setup-pr-loop.sh:220`). PR setup requires at least one bot flag and validates `--max`, Codex model/effort, branch YAML safety, PR openness, GitHub auth, and Codex availability (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:384`, `scripts/setup-pr-loop.sh:393`). RLCR setup forbids absolute plan paths, spaces, shell metacharacters, symlink traversal, submodule plans, tracked plans unless opted in, and insufficient plan content (`scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:326`, `scripts/setup-rlcr-loop.sh:334`, `scripts/setup-rlcr-loop.sh:346`, `scripts/setup-rlcr-loop.sh:408`, `scripts/setup-rlcr-loop.sh:447`, `scripts/setup-rlcr-loop.sh:483`). `portable-timeout.sh` standardizes bounded execution with `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:33`).
- dependencies_and_callers: Command wrappers under `commands/` invoke scripts directly: `start-pr-loop.md` calls `scripts/setup-pr-loop.sh`, `start-rlcr-loop.md` calls `scripts/setup-rlcr-loop.sh`, cancel commands call corresponding cancel scripts, and `gen-plan.md` calls `scripts/validate-gen-plan-io.sh` (`commands/start-pr-loop.md:13`, `commands/start-rlcr-loop.md:13`, `commands/cancel-pr-loop.md:14`, `commands/gen-plan.md:35`). Hook integration calls script utilities: `hooks/pr-loop-stop-hook.sh` calls `check-bot-reactions.sh`, `poll-pr-reviews.sh`, and `check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:675`, `hooks/pr-loop-stop-hook.sh:776`, `hooks/pr-loop-stop-hook.sh:811`, `hooks/pr-loop-stop-hook.sh:1556`). `humanize.sh` sources `scripts/lib/monitor-common.sh` and dispatches `humanize monitor rlcr|pr` (`scripts/humanize.sh:6`, `scripts/humanize.sh:883`).
- edge_cases_or_failure_modes: Fork PRs are explicitly handled by resolving current vs parent/base repositories across fetch, poll, reviewer status, reactions, and setup paths (`scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`, `scripts/check-pr-reviewer-status.sh:109`, `scripts/check-bot-reactions.sh:119`, `scripts/setup-pr-loop.sh:290`). API failure handling differs by script: fetch/poll retry three times and continue with empty arrays plus warnings (`scripts/fetch-pr-comments.sh:176`, `scripts/fetch-pr-comments.sh:190`, `scripts/fetch-pr-comments.sh:445`; `scripts/poll-pr-reviews.sh:200`, `scripts/poll-pr-reviews.sh:215`, `scripts/poll-pr-reviews.sh:225`). PR setup aborts and deletes the loop directory if a required re-review trigger or Claude eyes reaction cannot be confirmed (`scripts/setup-pr-loop.sh:528`, `scripts/setup-pr-loop.sh:536`, `scripts/setup-pr-loop.sh:568`, `scripts/setup-pr-loop.sh:587`). Monitors handle missing/deleted sessions, log rotation/truncation, zsh array differences, and terminal cleanup (`scripts/humanize.sh:220`, `scripts/humanize.sh:777`, `scripts/humanize.sh:797`, `scripts/humanize.sh:930`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`).
- validation_or_tests: `tests/test-pr-loop.sh` delegates PR-loop script, hook, and stop-hook validation (`tests/test-pr-loop.sh:32`). Script tests cover setup/cancel/fetch/poll argument validation (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`). Fixture-backed tests verify `fetch-pr-comments.sh` and `poll-pr-reviews.sh` include issue comments, inline review comments, approval-only reviews, and timestamp filtering (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1287`, `tests/test-pr-loop-hooks.sh:1322`, `tests/test-pr-loop-hooks.sh:1371`). Additional system/robustness tests reference setup, reviewer status, reactions, monitor, gen-plan IO, and path validation.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-036 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: `scripts/fetch-pr-comments.sh` is the PR-loop snapshot generator. It normalizes three GitHub review surfaces into a single markdown artifact consumed by PR-loop setup and user prompts: issue comments, inline PR review comments, and PR review submissions.
- algorithmic_behavior: It parses `<pr_number> <output_file>`, optional `--after`, and optional `--bots`, validates required inputs and numeric PR number (`scripts/fetch-pr-comments.sh:27`, `scripts/fetch-pr-comments.sh:92`, `scripts/fetch-pr-comments.sh:103`). It resolves the correct PR base repository by trying the current repo first and parent repo second for forks (`scripts/fetch-pr-comments.sh:130`, `scripts/fetch-pr-comments.sh:139`, `scripts/fetch-pr-comments.sh:143`, `scripts/fetch-pr-comments.sh:151`). It fetches three endpoints with retry and pagination, transforms each shape into common JSON fields, deduplicates by `id`, filters by timestamp if provided, sorts humans before bots and newest first, then renders markdown sections (`scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:212`, `scripts/fetch-pr-comments.sh:216`, `scripts/fetch-pr-comments.sh:220`, `scripts/fetch-pr-comments.sh:261`, `scripts/fetch-pr-comments.sh:281`, `scripts/fetch-pr-comments.sh:303`, `scripts/fetch-pr-comments.sh:323`, `scripts/fetch-pr-comments.sh:327`, `scripts/fetch-pr-comments.sh:337`, `scripts/fetch-pr-comments.sh:358`, `scripts/fetch-pr-comments.sh:381`).
- inputs_outputs_state: Inputs are GitHub CLI auth/context, `jq`, PR number, output path, optional ISO timestamp, and optional comma-separated active bot list. Output is a markdown file beginning with PR number, fetch timestamp, repository, then grouped comment sections and final end marker (`scripts/fetch-pr-comments.sh:245`, `scripts/fetch-pr-comments.sh:247`, `scripts/fetch-pr-comments.sh:440`). It uses a `mktemp` work directory cleaned by trap and does not persist internal JSON state (`scripts/fetch-pr-comments.sh:168`, `scripts/fetch-pr-comments.sh:170`).
- gates_or_invariants: Hard prerequisites are `gh` and `jq` (`scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:117`). It refuses missing `--after`/`--bots` arguments, unknown options, unexpected extra arguments, missing PR/output, and non-numeric PR number (`scripts/fetch-pr-comments.sh:29`, `scripts/fetch-pr-comments.sh:37`, `scripts/fetch-pr-comments.sh:74`, `scripts/fetch-pr-comments.sh:84`). For PR reviews with empty bodies, it emits a state placeholder so approval-only reviews are not lost (`scripts/fetch-pr-comments.sh:301`, `scripts/fetch-pr-comments.sh:312`).
- dependencies_and_callers: `setup-pr-loop.sh` calls it during round-0 setup to write `round-0-pr-comment.md`, passing active bots for grouping (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:428`, `scripts/setup-pr-loop.sh:431`). It relies on GitHub REST endpoints via `gh api`, `jq` for JSON transforms, and shell utilities `sed`, `tr`, `cat`, `date`, `mktemp`.
- edge_cases_or_failure_modes: If a GitHub endpoint fails after retries, the script writes `[]` for that endpoint, increments `API_FAILURES`, continues, exits 0, and appends a warning to the markdown (`scripts/fetch-pr-comments.sh:199`, `scripts/fetch-pr-comments.sh:200`, `scripts/fetch-pr-comments.sh:201`, `scripts/fetch-pr-comments.sh:445`). If no comments remain after filtering, it writes `*No comments found.*`, which `setup-pr-loop.sh` uses as the sentinel for the no-comments startup prompt (`scripts/fetch-pr-comments.sh:348`, `scripts/fetch-pr-comments.sh:350`, `scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:725`). Deduplication by raw `id` can merge objects from different endpoint namespaces if GitHub IDs collide across comment/review types, although GitHub IDs are generally globally distinct enough for this use (`scripts/fetch-pr-comments.sh:322`).
- validation_or_tests: Direct script tests cover help, missing PR number, missing output, and invalid PR number (`tests/test-pr-loop-scripts.sh:268`, `tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`). Fixture tests run the script with mocked `gh` and assert human issue comments, inline review comments, approval-only review placeholders, and `--after` filtering (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1251`, `tests/test-pr-loop-hooks.sh:1277`, `tests/test-pr-loop-hooks.sh:1287`). Robustness tests also exercise empty arrays, endpoint API behaviors, bot comments, Unicode, and long bodies (`tests/robustness/test-pr-loop-api-robustness.sh:348`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-066 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: `tests/test-pr-loop.sh` is the umbrella executable specification for PR-loop behavior. It does not contain the detailed assertions itself; it initializes a mocked test environment, sources focused modules, runs them in order, and returns a summary status.
- algorithmic_behavior: The runner computes `SCRIPT_DIR` and `PROJECT_ROOT`, sources common test helpers and PR-loop test library, initializes a temporary environment with mock `gh` and mock `codex`, sources script/hook/stophook modules, then invokes `run_script_tests`, `run_hook_tests`, `run_stophook_tests`, and `print_test_summary` (`tests/test-pr-loop.sh:15`, `tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:20`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`, `tests/test-pr-loop.sh:44`, `tests/test-pr-loop.sh:47`, `tests/test-pr-loop.sh:53`).
- inputs_outputs_state: Input is the repository checkout plus shell environment. `init_pr_loop_test_env` creates `TEST_DIR`, prepends a mock bin directory to `PATH`, and installs mock `gh`/`codex` commands (`tests/test-pr-loop-lib.sh:108`, `tests/test-pr-loop-lib.sh:112`, `tests/test-pr-loop-lib.sh:117`). Output is TAP-like colored pass/fail text and a nonzero return from `print_test_summary` if failures occurred (`tests/test-pr-loop-lib.sh:128`, `tests/test-pr-loop-lib.sh:137`).
- gates_or_invariants: The runner uses `set -euo pipefail` (`tests/test-pr-loop.sh:13`) and relies on module functions to enforce expected exit codes, blocked hook decisions, state file renames, mock API semantics, JSON parsing, and PR-loop state transitions. It assumes `jq`, `bash`, and standard Unix tools are available because module tests use them directly.
- dependencies_and_callers: It depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:20`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:33`, `tests/test-pr-loop.sh:34`). `tests/run-all-tests.sh` includes it in the broader suite (`tests/run-all-tests.sh:53`).
- edge_cases_or_failure_modes: Because `setup_test_dir` installs a single `trap "rm -rf $TEST_DIR" EXIT`, later nested tests that modify traps or `PATH` may leak process-level state if a sourced module exits unexpectedly (`tests/test-helpers.sh:86`, `tests/test-pr-loop-lib.sh:111`). The basic mock `gh` in `test-pr-loop-lib.sh` is intentionally limited and returns empty arrays for most APIs; deeper fixture and stop-hook modules create more specialized mocks for realistic behavior (`tests/test-pr-loop-lib.sh:27`, `tests/test-pr-loop-lib.sh:72`, `tests/test-pr-loop-hooks.sh:478`, `tests/test-pr-loop-stophook.sh:57`).
- validation_or_tests: The runner covers script argument validation for `setup-pr-loop.sh`, `cancel-pr-loop.sh`, `fetch-pr-comments.sh`, and `poll-pr-reviews.sh` (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`). It covers hook protections for state/comment writes, resolve writes, Bash state protection, sorting, deduplication, bot re-add, trigger timestamp behavior, and markers (`tests/test-pr-loop-hooks.sh:26`, `tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`, `tests/test-pr-loop-hooks.sh:271`, `tests/test-pr-loop-hooks.sh:326`, `tests/test-pr-loop-hooks.sh:415`). It covers stop-hook behavior including force-push trigger rejection, case-1 no-trigger exception, approve-state creation, dynamic startup case, unpushed commits, missing trigger, timeout auto-removal, Codex +1, and Claude eyes timeout (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:246`, `tests/test-pr-loop-stophook.sh:327`, `tests/test-pr-loop-stophook.sh:452`, `tests/test-pr-loop-stophook.sh:642`, `tests/test-pr-loop-stophook.sh:754`, `tests/test-pr-loop-stophook.sh:896`, `tests/test-pr-loop-stophook.sh:1011`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-096 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: This template is a schema-migration block message. It tells an agent/user that loop state is missing a required field and that continuing would risk corrupting or misinterpreting state.
- algorithmic_behavior: The template has one variable, `{{FIELD_NAME}}`, rendered into “State file is missing required field” (`prompt-template/block/schema-outdated.md:1`, `prompt-template/block/schema-outdated.md:3`). It prescribes cancel/update/restart options and states that the session will terminate as `unexpected` to preserve state (`prompt-template/block/schema-outdated.md:7`, `prompt-template/block/schema-outdated.md:12`).
- inputs_outputs_state: Input is `FIELD_NAME` from a caller. Output is a markdown block returned in a hook decision or terminal message via `load_and_render_safe`. It does not update files itself.
- gates_or_invariants: The invariant is fail-closed state-schema compatibility. The caller must not silently default a missing critical state field if that field changes loop semantics. The generic block is especially relevant to fields such as `review_started` and `base_branch`, which gate RLCR implementation vs review phase.
- dependencies_and_callers: `hooks/loop-plan-file-validator.sh` renders this template through `load_and_render_safe` when `block_schema_outdated` is called (`hooks/loop-plan-file-validator.sh:62`, `hooks/loop-plan-file-validator.sh:65`). The template loader performs single-pass placeholder substitution and falls back if missing (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:170`). Related schema checks are in `hooks/lib/loop-common.sh`, which treats missing `review_started` and `base_branch` as strict parser errors (`hooks/lib/loop-common.sh:289`, `hooks/lib/loop-common.sh:298`, `hooks/lib/loop-common.sh:302`), and in `hooks/loop-codex-stop-hook.sh`, which blocks outdated state for `plan_tracked`, `start_branch`, `review_started`, and `base_branch` (`hooks/loop-codex-stop-hook.sh:163`, `hooks/loop-codex-stop-hook.sh:182`, `hooks/loop-codex-stop-hook.sh:196`).
- edge_cases_or_failure_modes: The block’s text says update to version `1.1.2+` (`prompt-template/block/schema-outdated.md:9`), while some active schema gates mention pre-`1.5.0` fields in code-specific fallback text (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:199`). If reused for newer fields without overriding fallback copy, the version hint may be stale. Since missing variables are preserved by the template loader, an unprovided `FIELD_NAME` would leave `{{FIELD_NAME}}` visible rather than failing render (`hooks/lib/template-loader.sh:119`).
- validation_or_tests: Template existence and rendering are covered indirectly by template-loader tests and plan-file hook tests. `rg` found `hooks/loop-plan-file-validator.sh` as the direct caller and multiple tests asserting newer state fields such as `review_started` are required (`tests/test-plan-file-hooks.sh:163`, `tests/test-plan-file-hooks.sh:184`, `tests/robustness/test-state-file-robustness.sh:7`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-126 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: This is the round-0 PR-loop task template for the startup path where `fetch-pr-comments.sh` found no existing PR review comments. It defines the agent’s first action: write an initial summary and let the Stop Hook wait for automatic bot reviews rather than manually triggering review.
- algorithmic_behavior: The rendered task says the PR has no review comments yet, monitored bots will automatically review, and the agent should simply write a summary and try to exit (`prompt-template/pr-loop/round-0-task-no-comments.md:4`, `prompt-template/pr-loop/round-0-task-no-comments.md:6`, `prompt-template/pr-loop/round-0-task-no-comments.md:8`, `prompt-template/pr-loop/round-0-task-no-comments.md:12`). It forbids commenting to trigger the first review, forbids modifying `.humanize/pr-loop/` state files, and delegates polling/Codex validation to the Stop Hook (`prompt-template/pr-loop/round-0-task-no-comments.md:18`, `prompt-template/pr-loop/round-0-task-no-comments.md:20`, `prompt-template/pr-loop/round-0-task-no-comments.md:21`, `prompt-template/pr-loop/round-0-task-no-comments.md:26`).
- inputs_outputs_state: Inputs are `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH`, rendered by `setup-pr-loop.sh` via the template loader (`scripts/setup-pr-loop.sh:731`, `scripts/setup-pr-loop.sh:735`, `scripts/setup-pr-loop.sh:736`, `scripts/setup-pr-loop.sh:800`). Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the header and fetched comment file (`scripts/setup-pr-loop.sh:761`, `scripts/setup-pr-loop.sh:764`, `scripts/setup-pr-loop.sh:850`).
- gates_or_invariants: Its main invariant is that Case 1/no-comments round 0 must not post a trigger comment. This matches Stop Hook trigger logic: round 0 startup cases `1|2|3` do not require a trigger, while cases `4|5` and later rounds do (`hooks/pr-loop-stop-hook.sh:612`, `hooks/pr-loop-stop-hook.sh:627`, `hooks/pr-loop-stop-hook.sh:629`, `hooks/pr-loop-stop-hook.sh:634`). It also instructs users not to modify system-managed state files, which hook validators enforce for PR-loop state and comment files while allowing `round-N-pr-resolve.md` (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`).
- dependencies_and_callers: `setup-pr-loop.sh` selects this template only when `round-0-pr-comment.md` contains the exact `*No comments found.*` sentinel from `fetch-pr-comments.sh` (`scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:725`, `scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:800`). A companion `critical-requirements-no-comments.md` is rendered for the same branch after the initial prompt (`scripts/setup-pr-loop.sh:890`, `scripts/setup-pr-loop.sh:914`).
- edge_cases_or_failure_modes: The no-comments detection is sentinel-based rather than counting parsed comments; if `fetch-pr-comments.sh` emits warnings plus no comments, the sentinel still appears, but any nonstandard output that omits the exact line would route to the has-comments template (`scripts/setup-pr-loop.sh:723`, `scripts/fetch-pr-comments.sh:348`). The template assumes “first reviews are automatic”; if a repository requires explicit bot mentions even for initial review, the prompt would direct the agent to wait until Stop Hook times out or other logic intervenes.
- validation_or_tests: `tests/test-pr-loop-stophook.sh` explicitly verifies the Case 1 exception where no trigger is required for startup_case `1`, round `0` (`tests/test-pr-loop-stophook.sh:140`, `tests/test-pr-loop-stophook.sh:231`). Hook tests also verify round-0 timestamp fallback and trigger priority behavior (`tests/test-pr-loop-hooks.sh:624`, `tests/test-pr-loop-hooks.sh:646`). `setup-pr-loop.sh` includes an inline fallback with equivalent content if this template is missing (`scripts/setup-pr-loop.sh:768`, `scripts/setup-pr-loop.sh:800`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 evidence sections, one per assigned checklist row: HZ-006, HZ-036, HZ-066, HZ-096, HZ-126
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`