# agent_09 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared hook algorithm library for RLCR/PR loop enforcement. The directory contains exactly two shell libraries: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. `loop-common.sh` centralizes hook input validation, loop state parsing, round-file policy, cancel authorization, `.humanize` git-add protection, PR-loop helpers, exit-state transitions, and goal-tracker reconciliation. `template-loader.sh` is the prompt/block rendering subsystem used by hook error gates.
- algorithmic_behavior: `loop-common.sh` validates JSON hook input with null-byte, UTF-8, `jq`, and required `tool_name` checks at lines 59-96, then exposes field gating via `require_tool_input_field` at lines 101-114 and depth DoS protection via `is_deeply_nested` at lines 119-133. It sources `template-loader.sh`, resolves `TEMPLATE_DIR`, and warns/falls back if templates are unavailable at lines 139-149. State discovery selects only the newest active loop directory with `state.md` or `finalize-state.md` at lines 156-174, preventing older session revival. State parsing has tolerant and strict modes at lines 209-330; strict mode requires YAML separators, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric rounds/iterations, and boolean `review_started`. Round-file helpers classify `round-N-summary/prompt/todos.md`, extract round numbers, and allowlist only specific historical bootstrap files at lines 399-441. Cancel authorization is a security parser at lines 551-710: requires `.cancel-requested`, rejects substitution/chaining/remaining vars, parses exactly `mv <state|finalize-state> <cancel-state>`, normalizes `/./`, rejects symlink sources, and uses exact source matching to avoid `finalize` substring bypasses. `git_adds_humanize` blocks direct/broad/forced staging of `.humanize` at lines 930-1026. `command_modifies_file` detects shell write primitives at lines 1071-1096. `end_loop` renames state files to reason-specific terminal names at lines 1119-1145. `update_pr_goal_tracker` idempotently updates PR tracker summary rows, totals, and log entries at lines 1168-1315.
- inputs_outputs_state: Inputs are Claude hook JSON from `Read`, `Write`, `Edit`, and `Bash` hooks; loop filesystem state under `.humanize/rlcr` and `.humanize/pr-loop`; prompt templates under `prompt-template`; Git status/ignore state for `.humanize`; Codex review logs; and PR goal tracker JSON. Outputs include global shell variables such as `VALIDATED_TOOL_NAME`, `STATE_CURRENT_ROUND`, rendered block messages, exit status conventions, renamed state files such as `cancel-state.md` or `complete-state.md`, review-result markdown, and goal-tracker updates. State transitions are file-based: active loop detection, strict parse before protected actions, cancel transition only through authorized `mv`, and terminal loop transition through `end_loop`.
- gates_or_invariants: Fail closed on malformed hook JSON, missing required fields, deep JSON, malformed loop state, wrong round files, protected prompt/state/todo files, unsafe Bash writes, unauthorized cancel attempts, `.humanize` git staging, and missing/invalid template directories only as warning plus fallback. `template-loader.sh` preserves single-pass rendering: variable values containing `{{OTHER}}` are not rescanned, reducing prompt-placeholder injection risk at lines 54-58 and 71-129. `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories at lines 208-221.
- dependencies_and_callers: Direct shell dependencies include `bash`, `jq`, `iconv` when available, `sed`, `awk`, `grep`, `tr`, `wc`, `git check-ignore`, filesystem tests, and `date`. Main callers are `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and setup scripts. The read/write/edit validators apply todos, prompt, state, round, PR-loop, and goal-tracker gates; the bash validator applies command-pattern bypass protection and calls `is_cancel_authorized`.
- edge_cases_or_failure_modes: The state parser is line-oriented YAML frontmatter parsing, so complex YAML structures are not supported. `is_cancel_authorized` expects the caller to provide a lowercased command and compares against a lowercased loop path; this matches the bash validator but can be sensitive to case-sensitive path semantics. `git_adds_humanize` uses heuristic command splitting and depends on current working directory for `.humanize` existence and `git check-ignore`. `command_modifies_file` detects common write forms but is pattern-based rather than a shell parser. `template-loader.sh` returns empty for missing templates, so callers must use `load_and_render_safe` for critical blocks.
- validation_or_tests: Covered by `tests/robustness/test-cancel-security-robustness.sh` for cancel parsing, `tests/test-template-references.sh` for template reference presence and safe rendering usage, `tests/test-template-loader.sh` for rendering/fallback semantics, `tests/test-allowlist-validators.sh` for todo/summary allowlist behavior, `tests/test-bash-validator-patterns.sh` and hook robustness tests for Bash write detection, plus state robustness tests for `parse_state_file*`. Research inspection only; no tests were executed.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-039 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Runtime compatibility wrapper that gives Git, GitHub CLI, Codex, and setup/monitor scripts a single `run_with_timeout <seconds> <command> [args...]` API across macOS and Linux.
- algorithmic_behavior: `detect_timeout_impl` selects the first available timeout backend in priority order: Homebrew `gtimeout`, GNU `timeout` verified by `timeout --version`, `python3`, `python`, then `none` at lines 10-27. `TIMEOUT_IMPL` is computed once when the file is sourced at line 29. `run_with_timeout` stores command arguments in a Bash array at lines 33-37, dispatches to `gtimeout`/`timeout` at lines 39-45, or runs an inline Python `subprocess.run(sys.argv[1:], timeout=<seconds>)` wrapper at lines 47-63. If no backend exists, it warns and runs the command without any timeout at lines 64-69.
- inputs_outputs_state: Inputs are timeout seconds, command path/name, command arguments, and PATH/backend availability at source time. Outputs are the wrapped command’s stdout/stderr and exit code. Timeout returns backend-specific behavior, with the Python fallback intentionally returning `124` on `subprocess.TimeoutExpired` to match GNU timeout at lines 56-57. State is the exported `TIMEOUT_IMPL` variable at lines 73-76.
- gates_or_invariants: GNU `timeout` is accepted only when `timeout --version` succeeds, avoiding BSD/non-GNU ambiguity at lines 13-19. Arguments are forwarded as an array for shell-safe separation. The wrapper preserves child exit codes for non-timeout failures in all real backends.
- dependencies_and_callers: Used by `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and `hooks/loop-plan-file-validator.sh` for bounded Git, `gh`, and Codex operations. Tests source it directly in timeout, Git-operation, setup-script, base-branch, and hook-system robustness suites.
- edge_cases_or_failure_modes: If no timeout backend exists, long-running commands are not killed; the script only emits a warning. The Python fallback interpolates `timeout_secs` directly into Python source, so invalid/non-numeric timeout values can fail before running the command. Timeout behavior for process groups and descendants depends on the selected backend; the wrapper does not add its own process-group cleanup. Backend detection is fixed at source time, so later PATH changes do not update `TIMEOUT_IMPL` unless re-sourced.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` validates backend detection, quick success, timeout exit `124`, argument forwarding, exit-code preservation, pipelines through `sh -c`, short/zero timeouts, large output, rapid cycles, nonexistent/empty commands, special characters, signal-handling variance, fallback-chain values, subshell commands, and `TIMEOUT_IMPL` export behavior.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-069 `file` `tests/test-template-references.sh`
- cursor: `[_]`
- core_role: Executable specification that prevents hook block-message regressions by verifying that template paths referenced by shell hook code resolve to real files and that critical validators use fallback-safe rendering.
- algorithmic_behavior: The script derives `PROJECT_ROOT` and `TEMPLATE_DIR` at lines 14-16, maintains pass/fail/warn counters at lines 25-42, and scans a fixed `SCRIPTS_TO_CHECK` list at lines 57-64. For each script, it skips comments, finds same-line `load_template`, `load_and_render`, or `load_and_render_safe` calls whose first argument is `"$TEMPLATE_DIR"`, extracts the quoted template path with `sed`, and fails if the referenced file is absent at lines 74-110. Section 2 enumerates all markdown templates under `prompt-template` and warns, but does not fail, when a template is not directly referenced under `hooks/` at lines 121-142. Section 3 asserts a curated `COMMON_TEMPLATES` list exists at lines 152-167. Section 4 checks `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-edit-validator.sh` for unsafe `load_and_render "$TEMPLATE_DIR"` calls that are not `load_and_render_safe` at lines 176-201. It exits `0` only when `TESTS_FAILED == 0` at lines 221-230.
- inputs_outputs_state: Inputs are repository file contents under `hooks/` and `prompt-template/`. Outputs are colored PASS/WARN/FAIL lines, counts, a critical missing-template list, and process exit `0` or `1`. State is local arrays `MISSING_TEMPLATES`, `TEMPLATE_FILES`, `UNREFERENCED`, and counters.
- gates_or_invariants: Missing directly referenced templates are hard failures. Missing scripts are warnings, allowing partial environments. Unreferenced templates are warnings because dynamic usage may be valid. Critical validators must use `load_and_render_safe`, preserving non-empty fallback messages when templates are missing.
- dependencies_and_callers: Depends on Bash, `find -print0`, `grep`, `sed`, `basename`, and the repository’s template directory. It validates references from hook scripts and `hooks/lib/loop-common.sh`; it is itself likely run by `tests/run-all-tests.sh` as part of the shell test suite.
- edge_cases_or_failure_modes: The reference extractor is intentionally simple: it only catches single-line calls containing `"$TEMPLATE_DIR"` followed by a quoted path. Dynamic template names, different variable names, or multiline argument layouts can be missed. Section 2 searches only under `hooks/`, so templates used only by `scripts/setup-pr-loop.sh` or other scripts can be reported as unreferenced warnings. `COMMON_TEMPLATES` is curated and not exhaustive; for example PR-loop and git-add block templates are validated by Section 1 when referenced, not by the common list.
- validation_or_tests: This file is itself the validation gate. It complements `tests/test-template-loader.sh`, which validates `load_template`, `render_template`, `load_and_render_safe`, and `validate_template_dir` semantics.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-099 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Static policy template for the todos-file access gate. It defines the user-facing block text shown when hooks reject direct access to `round-*-todos.md` files.
- algorithmic_behavior: The template contains no variables or control flow. It declares the block title, forbids creating or accessing `round-*-todos.md`, instructs use of the native `TodoWrite` tool, and explains that native todo tooling provides UI-visible state tracking and Claude Code task integration at lines 1-8.
- inputs_outputs_state: Input is only the template path requested through `todos_blocked_message`. Output is rendered markdown, normally sent to stderr by read/write/edit/bash validators. There is no local mutable state.
- gates_or_invariants: The invariant is behavioral rather than computational: direct round todo files are not the authoritative task interface for the agent; `TodoWrite` is. `hooks/lib/loop-common.sh` loads this file through `todos_blocked_message` at line 451, with an inline fallback if the template is absent.
- dependencies_and_callers: Called through `todos_blocked_message` from `hooks/loop-read-validator.sh` lines 55-61, `hooks/loop-write-validator.sh` lines 55-61, `hooks/loop-edit-validator.sh` lines 38-44, and `hooks/loop-bash-validator.sh` lines 385-391. The template is included in `tests/test-template-references.sh` `COMMON_TEMPLATES` at line 153.
- edge_cases_or_failure_modes: The template says “Do NOT create or access” broadly, while `loop-common.sh` has a narrow allowlist for specific active-loop bootstrap files (`round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, `round-1-summary.md`) at lines 427-441. That means the message expresses the default policy, not every implementation exception. Because it has no placeholders, the `action` argument passed to `todos_blocked_message` is unused.
- validation_or_tests: Presence is checked by `tests/test-template-references.sh`. Enforcement behavior and active-loop/path-restricted exceptions are tested by `tests/test-allowlist-validators.sh`, including read/write/edit/bash blocking for non-allowlisted or wrong-root todo files.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-129 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable security specification for `is_cancel_authorized`, the function that allows the otherwise forbidden state-file move only when a cancellation has been explicitly requested.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at lines 15-18, creates a temporary loop directory with `state.md` at lines 20-30, then exercises 26 cancel authorization scenarios. Positive cases require `.cancel-requested` and accept exact moves from `state.md` or `finalize-state.md` to `cancel-state.md`, with double quotes, single quotes, or unquoted paths at lines 39-89. Negative cases reject missing signal file, command substitution, backticks, semicolon/`&&`/pipe/`||` chaining, wrong source/destination, extra args, newline injection, remaining variables such as `${HOME}` and `${IFS}`, non-`mv` commands, mixed quote styles, multiple trailing spaces, non-standard source filenames, and symlinked state sources at lines 99-367. Regression cases ensure paths containing the string `finalize` do not bypass symlink checks for either `state.md` or `finalize-state.md` at lines 369-405. Final result is delegated to `print_test_summary` at lines 407-412.
- inputs_outputs_state: Inputs are constructed shell command strings, lowercased with `tr`, plus filesystem state in `TEST_DIR` such as `.cancel-requested`, real state files, symlinks, and tricky directory names. Outputs are PASS/FAIL lines and a final exit status from `print_test_summary`. State transitions inside the test repeatedly create/remove `.cancel-requested`, create/remove `finalize-state.md`, replace `state.md` with symlinks, and restore normal files.
- gates_or_invariants: A cancel is authorized only if the signal file exists and the command is structurally a single `mv` with exactly two paths: source must be the active loop’s `state.md` or `finalize-state.md`, destination must be the same loop’s `cancel-state.md`, and the source must not be a symlink. The spec also asserts that normalization allows `/./` but not arbitrary variable expansion or shell metacharacter bypasses.
- dependencies_and_callers: Depends directly on `is_cancel_authorized` in `hooks/lib/loop-common.sh` lines 551-710 and test helpers in `tests/test-helpers.sh`. This behavior is consumed by `hooks/loop-bash-validator.sh`, which otherwise blocks `state.md` and `finalize-state.md` modifications but exits `0` for authorized cancellation paths.
- edge_cases_or_failure_modes: The tests mirror actual hook behavior by lowercasing command strings before calling `is_cancel_authorized`; this can hide case-sensitive path concerns outside macOS-style assumptions. The symlink tests are filesystem-dependent but tolerate `ln` setup failures only in the non-standard filename case. The script is security-focused and does not execute the `mv`; it validates authorization decisions only.
- validation_or_tests: This file is the direct robustness validator for cancellation security. It also indirectly validates the regression fix documented in `loop-common.sh` lines 696-704: source-file selection is based on exact path comparison rather than substring matching on paths containing `finalize`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5` item evidence sections present; item IDs are listed only in the five section headers above
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`