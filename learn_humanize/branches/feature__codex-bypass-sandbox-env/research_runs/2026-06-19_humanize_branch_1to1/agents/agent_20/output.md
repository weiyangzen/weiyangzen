# agent_20 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for turning a user draft into a structured implementation plan. It is not executable code itself, but it is an algorithmic command contract: validate IO, check repository relevance, analyze draft quality, resolve ambiguities with the user, generate a deterministic plan format, write the plan, and report completion.
- algorithmic_behavior: The workflow is phase-based. Phase 1 invokes `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` with `$ARGUMENTS` and maps exit codes `0..7` to stop/continue outcomes (`commands/gen-plan.md:30-47`). Phase 2 reads the draft and delegates a lightweight repository relevance decision to `humanize:draft-relevance-checker`, accepting only `RELEVANT` and stopping on `NOT_RELEVANT` (`commands/gen-plan.md:50-72`). Phase 3 requires deeper codebase exploration for clarity, consistency, completeness, and functionality (`commands/gen-plan.md:76-108`). Phase 4 requires user clarification while preserving all original draft content and explicitly classifying quantitative metrics as hard requirements or optimization directions (`commands/gen-plan.md:111-139`). Phase 5 defines the generated plan schema and generation rules, including AC numbering, TDD-style positive and negative tests, path boundaries, feasibility hints, dependency sequencing, and implementation-note constraints (`commands/gen-plan.md:142-248`). Phase 6 requires updating the already-created output file with Edit, reviewing the whole plan, optionally unifying mixed languages, then reporting path and AC count (`commands/gen-plan.md:251-303`).
- inputs_outputs_state: Inputs are `--input <draft>` and `--output <plan>`, repository context, Task/Explore findings, and user clarification answers. The validation script canonicalizes paths, rejects invalid states, copies `prompt-template/plan/gen-plan-template.md`, then appends the original draft between stable sentinels (`scripts/validate-gen-plan-io.sh:69-160`). Output is a plan file containing both structured generated content and the preserved draft; the command also outputs a user-facing summary. State transition is draft file plus empty output path -> template-plus-draft seed -> edited complete plan -> reported result.
- gates_or_invariants: IO gates include input exists, nonempty input, output directory exists, output path absent, output dir writable, and template file present (`scripts/validate-gen-plan-io.sh:79-145`). Relevance gate stops unrelated drafts. Draft preservation is a hard invariant: final plan must be a superset of the original draft plus clarifications (`commands/gen-plan.md:113-114`, `commands/gen-plan.md:247`). Plan-format invariants include AC-X / AC-X.Y, no line-number references, no time estimates, affirmative path boundaries, TDD-style positive and negative tests for each AC, and no plan progress terminology in implementation code/comments (`commands/gen-plan.md:225-245`). Language unification is gated by explicit user choice (`commands/gen-plan.md:272-285`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, `prompt-template/plan/gen-plan-template.md`, the Task tool with `humanize:draft-relevance-checker`, Explore subagents, Read/Glob/Grep, AskUserQuestion, and file editing. The template mirrors the schema in the command, including `Goal Description`, `Acceptance Criteria`, `Path Boundaries`, `Dependencies and Sequence`, and code-style requirements (`prompt-template/plan/gen-plan-template.md:1-74`). This command feeds downstream RLCR/plan algorithms by producing the plan.md surface that other scripts count and validate for AC entries.
- edge_cases_or_failure_modes: Invalid or missing CLI args exit `6`; nonexistent input exits `1`; empty input exits `2`; missing output dir exits `3`; existing output or output directory exits `4`; unwritable output dir exits `5`; missing template exits `7` (`scripts/validate-gen-plan-io.sh:4-12`, `scripts/validate-gen-plan-io.sh:79-145`). The command can stop on irrelevant drafts, unresolved analysis issues, ambiguous quantitative metrics, or inability to generate a complete plan (`commands/gen-plan.md:67-70`, `commands/gen-plan.md:296-303`). A subtle workflow constraint is that allowed tools include Write, but Phase 6 explicitly says to use Edit because the IO validator has already created the file (`commands/gen-plan.md:4-11`, `commands/gen-plan.md:251-260`).
- validation_or_tests: Direct validation is delegated to `scripts/validate-gen-plan-io.sh`; template presence and loadability are covered elsewhere by template reference/comprehensive tests, but this assigned command file itself is a prompt/workflow spec rather than a shell test. The strongest executable evidence for its IO behavior is the validation script exit-code contract and template-copy behavior.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancel-signal mechanism. It verifies that loop state file protection cannot be bypassed through Bash except for one narrow, signal-authorized cancellation transition.
- algorithmic_behavior: The test builds a temporary active RLCR loop at `.humanize/rlcr/2024-01-01_12-00-00`, writes a representative `state.md`, and sets `CLAUDE_PROJECT_DIR` to the temp project (`tests/test-cancel-signal-file.sh:42-59`). Commands are JSON-encoded with `jq` and piped into `hooks/loop-bash-validator.sh`, so the test exercises the real PreToolUse Bash validator path without executing the candidate shell commands (`tests/test-cancel-signal-file.sh:61-74`). Positive cases allow only `mv <loop>/state.md <loop>/cancel-state.md` when `.cancel-requested` exists, including normalized `./` paths and quoted paths (`tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-379`). It also tests literal `${loop_dir}` syntax through the helper and full validator (`tests/test-cancel-signal-file.sh:450-485`).
- inputs_outputs_state: Inputs are candidate Bash command strings, active-loop files, optional `.cancel-requested`, and hook JSON. Outputs are validator exit codes and pass/fail counters. The intended state transition is active `state.md` plus in-loop `.cancel-requested` plus exact safe `mv` command -> authorized transition to `cancel-state.md`; all other state-file modifications remain blocked. The test exits with the number of failed assertions (`tests/test-cancel-signal-file.sh:1349-1357`).
- gates_or_invariants: Authorization requires the signal file in the active loop directory, not a parent or unrelated directory (`tests/test-cancel-signal-file.sh:121-135`, `tests/test-cancel-signal-file.sh:201-217`). The helper contract in `hooks/lib/loop-common.sh` requires no command substitution, no newlines, no shell chaining operators, no remaining `$` after loop-dir normalization, exact `mv` structure, exactly two args, source equal to active `state.md` or `finalize-state.md`, destination equal to active `cancel-state.md`, and no symlink source (`hooks/lib/loop-common.sh:551-710`). The Bash validator also fails closed on malformed active state via `parse_state_file_strict` before applying state protections (`hooks/loop-bash-validator.sh:74-85`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `to_lower` and `is_cancel_authorized` (`tests/test-cancel-signal-file.sh:32-33`), `hooks/loop-bash-validator.sh` for real hook behavior, `jq` for safe JSON construction, and POSIX shell utilities. The validator under test protects both destination-style writes and source-style `mv`/`cp` bypasses, with dedicated source-pattern handling for prefixes, wrappers, shell operators, and redirections (`hooks/loop-bash-validator.sh:139-208` and following redirection-stripping logic).
- edge_cases_or_failure_modes: Negative coverage blocks missing signal, wrong destination, wrong signal directory, `echo > state.md`, `sed -i`, `rm`, arbitrary destination, command injection with `&&`, semicolon, `$()`, backticks, newlines, `mv --`, `cp -f`, extra args before/after state path, hidden `${IFS}`, leading whitespace, `sudo`, `env`, `command --`, chained shell segments, pipes, background operators, `sh -c` / `bash -c`, many redirection-prefix variants, unrelated `/tmp/state.md`, and active-loop absence (`tests/test-cancel-signal-file.sh:121-357`, `tests/test-cancel-signal-file.sh:386-440`, `tests/test-cancel-signal-file.sh:520-1249`). The no-active-loop case intentionally allows the command because no managed loop state exists (`tests/test-cancel-signal-file.sh:1230-1249`). One implementation edge not directly asserted here is symlink-source rejection, though the helper supports return `6` for that case (`hooks/lib/loop-common.sh:696-707`).
- validation_or_tests: This file is itself the validation suite. It combines full-hook tests and direct helper tests, including helper true/false cases for signal presence, wrong commands, extra args, quoted paths, and hidden variables (`tests/test-cancel-signal-file.sh:1251-1343`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Prompt block used inside the RLCR stop-hook clean-git gate when `.humanize/` artifacts are detected as untracked local files. It defines the user-facing transition guidance for keeping loop-private state out of commits.
- algorithmic_behavior: The template emits a special-case note saying `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` with `echo '.humanize*' >> .gitignore` followed by `git add .gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). It is loaded when cached `git status` contains untracked paths matching `.humanize`; the note is concatenated into `SPECIAL_NOTES` used by the broader `git-not-clean` block (`hooks/loop-codex-stop-hook.sh:521-560`).
- inputs_outputs_state: Input is not a runtime placeholder but a triggering condition: untracked `.humanize*` appears in git status. Output is markdown guidance embedded in the stop-hook block reason. The state transition it encourages is untracked loop-private artifact -> `.gitignore` updated/tracked -> git clean enough for loop exit/next phase.
- gates_or_invariants: Invariant: `.humanize/` is loop system state and must not be committed. Gate: if uncommitted changes exist, the stop hook blocks exit and requires commit hygiene before continuing; this template specializes the remediation when the dirty status is caused by `.humanize` (`hooks/loop-codex-stop-hook.sh:548-560`). It avoids telling the worker to add `.humanize/` itself.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` (`hooks/loop-codex-stop-hook.sh:528-534`). Depends on `hooks/lib/template-loader.sh` indirectly through loop-common template initialization.
- edge_cases_or_failure_modes: If the template is missing or empty, the caller falls back to a shorter note: `Note: .humanize* directories are intentionally untracked.` (`hooks/loop-codex-stop-hook.sh:530-533`). The suggested `echo` command can append a duplicate `.humanize*` entry if one already exists; the template does not include duplicate detection because it is prompt guidance, not executable logic.
- validation_or_tests: Template existence/loadability is covered by the repository’s template reference/comprehensive tests. This specific file’s behavioral validation is indirect through the stop-hook dirty-git path that loads it when `.humanize` appears untracked.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer appended to generated next-round Claude instructions after Codex review finds issues. It is a behavioral gate contract for the next worker round: do not bypass loop state, commit changes, and write the required summary.
- algorithmic_behavior: The template warns that the worker must not exit by lying, editing loop state files, or running `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:4`). It then orders post-work actions: optionally run `code-simplifier` if installed, commit changes with a descriptive message, and write the work summary to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:6-9`). The stop hook appends this template after rendering the next-round prompt and before optional push and goal-tracker update notes (`hooks/loop-codex-stop-hook.sh:1523-1565`).
- inputs_outputs_state: Input is the `NEXT_SUMMARY_FILE` placeholder supplied by `loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:1520-1549`). Output is appended markdown in `round-N-prompt.md`. State transition is Codex review issues -> generated next-round prompt -> worker receives required end-of-round commit/summary obligations.
- gates_or_invariants: Invariants are explicit anti-bypass rules: no false completion, no manual loop-state edits, no cancellation command as an escape route (`prompt-template/claude/next-round-footer.md:4`). Completion-surface invariant: after work, a descriptive commit and summary file must exist (`prompt-template/claude/next-round-footer.md:6-9`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-footer.md"` with `NEXT_SUMMARY_FILE=$NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:1545-1549`). Depends on template rendering for placeholder replacement. It coordinates with `claude/next-round-prompt.md`, optional `claude/post-alignment-action-items.md`, `claude/push-every-round-note.md`, and `claude/goal-tracker-update-request.md` in the same prompt assembly sequence (`hooks/loop-codex-stop-hook.sh:1532-1565`).
- edge_cases_or_failure_modes: If this template is missing, the fallback only says to commit changes and write the summary, losing the stronger anti-bypass warning and optional code-simplifier instruction (`hooks/loop-codex-stop-hook.sh:1545-1549`). The `code-simplifier` line is conditional on plugin availability and lists multiple invocation forms, so it is guidance rather than a hard executable check.
- validation_or_tests: Template load/render behavior is validated indirectly by template-loader and template-reference tests; runtime behavior is validated through next-round prompt generation in stop-hook tests where review feedback blocks exit and writes a next prompt.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-140 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing. It defines tolerated and rejected shapes of `state.md` frontmatter for both tolerant parser reads and strict safety gates.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and shared test helpers, creates a temp test directory, then writes many synthetic state files and asserts parser behavior (`tests/robustness/test-state-file-robustness.sh:14-20`). Positive tests verify `parse_state_file` extracts `current_round`, `max_iterations`, quoted string fields, extra-field tolerance, zero rounds, large numbers, special characters, whitespace, `full_review_round`, and default `full_review_round=5` (`tests/robustness/test-state-file-robustness.sh:33-140`, `tests/robustness/test-state-file-robustness.sh:277-474`). Negative/robustness tests verify strict parser rejection for missing frontmatter, nonnumeric rounds, missing required fields, plus tolerant handling of empty, partial, malformed, binary, CRLF, and nonexistent files (`tests/robustness/test-state-file-robustness.sh:149-275`, `tests/robustness/test-state-file-robustness.sh:358-397`).
- inputs_outputs_state: Inputs are generated markdown state files with YAML-like frontmatter. Outputs are global `STATE_*` variables from `parse_state_file`, scalar output from `get_current_round`, pass/fail test results, and final summary exit code (`tests/robustness/test-state-file-robustness.sh:480-481`). The tested state transition is raw state file content -> parsed frontmatter variables/defaults or strict rejection.
- gates_or_invariants: Tolerant parsing defaults missing `current_round` to `0`, `max_iterations` to `10`, `push_every_round` to `false`, and `full_review_round` to `5` (`hooks/lib/loop-common.sh:209-243`). Strict parsing requires at least two `---` separators, required fields, numeric `current_round` and `max_iterations`, boolean `review_started`, and optional defaults after validation (`hooks/lib/loop-common.sh:255-329`). `get_current_round` extracts only `current_round` from frontmatter and defaults to `0` (`hooks/lib/loop-common.sh:179-189`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for parser functions and `tests/test-helpers.sh` for `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/robustness/test-state-file-robustness.sh:14-19`). The strict parser is a safety dependency for hooks like `hooks/loop-bash-validator.sh`, which blocks Bash operations when active loop state is malformed (`hooks/loop-bash-validator.sh:81-85`).
- edge_cases_or_failure_modes: The suite intentionally accepts broad tolerant behavior for partial YAML (`0` or `5`), nonnumeric `current_round` in tolerant mode, negative numbers, and CRLF because the key invariant is “does not crash” rather than exact normalization (`tests/robustness/test-state-file-robustness.sh:190-199`, `tests/robustness/test-state-file-robustness.sh:242-257`, `tests/robustness/test-state-file-robustness.sh:297-312`, `tests/robustness/test-state-file-robustness.sh:386-397`). A notable nuance is that some strict rejection tests assert only nonzero return, not the exact failure reason; with the current strict parser requiring `review_started` and `base_branch`, a malformed fixture can pass the test by failing earlier than the label implies (`hooks/lib/loop-common.sh:289-323`).
- validation_or_tests: This file is itself the validation suite and ends with `print_test_summary "State File Robustness Test Summary"` (`tests/robustness/test-state-file-robustness.sh:476-481`). It directly validates tolerant and strict state parsing under malformed, missing, partial, oversized numeric, binary-tail, CRLF, and optional-field scenarios.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence headings present; each assigned item_id appears once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`