# agent_01 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin. It contains the RLCR and PR-loop state machines, hook registration, setup/cancel commands, prompt templates, GitHub polling utilities, monitor support, and regression/robustness tests. The branch-relevant core algorithm is the Codex execution mode selection controlled by `HUMANIZE_CODEX_BYPASS_SANDBOX`, documented in `README.md:60` and implemented in both the RLCR stop hook and PR-loop stop hook.
- algorithmic_behavior: `hooks/hooks.json:14` registers read/write/edit/bash validators as `PreToolUse` gates, and `hooks/hooks.json:52` registers both `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh` as Stop hooks. `commands/start-pr-loop.md:12` delegates PR-loop initialization to `scripts/setup-pr-loop.sh`; that setup script detects the current PR, validates `gh`/`codex`, creates `.humanize/pr-loop/<timestamp>/state.md`, fetches initial comments, computes startup cases, and renders prompts. `commands/start-rlcr-loop.md` and `scripts/setup-rlcr-loop.sh` provide the sibling RLCR flow. The root coordinates via `.humanize/rlcr` and `.humanize/pr-loop` session directories rather than central mutable database state.
- inputs_outputs_state: Inputs include Claude hook JSON, slash-command arguments, git status/branch/commit data, GitHub PR metadata/comments/reviews/reactions, prompt templates, and environment variables. Outputs include Claude hook JSON block decisions, loop state file transitions, round prompt/comment/check/feedback/summary files, goal tracker updates, and monitor output. README documents the branch env var: default unset uses `--full-auto`; values `true` or `1` use `--dangerously-bypass-approvals-and-sandbox` (`README.md:64`, `README.md:67`).
- gates_or_invariants: The repository enforces one active loop at a time in PR setup (`scripts/setup-pr-loop.sh:220`), hook-validated loop artifact ownership (`hooks/loop-write-validator.sh:77`, `hooks/loop-write-validator.sh:175`), stop-hook completion only after required summaries and review checks, clean/pushed git state in PR loop (`hooks/pr-loop-stop-hook.sh:282`, `hooks/pr-loop-stop-hook.sh:349`), and branch/review state sanity. The bypass flag does not remove repository-level validators; it only changes the local `codex exec` automation flag used inside stop hooks.
- dependencies_and_callers: `.claude-plugin/plugin.json` and `hooks/hooks.json` make the repo a plugin surface. `hooks/lib/loop-common.sh` supplies common parsing, bot mapping, YAML list generation, active loop discovery, and goal tracker update routines. `hooks/lib/template-loader.sh` loads prompt/block templates. `scripts/portable-timeout.sh` wraps long-running `gh`, `git`, and `codex` calls. `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, and `scripts/check-pr-reviewer-status.sh` implement GitHub data acquisition used by PR-loop setup and stop hooks.
- edge_cases_or_failure_modes: Root-level behavior accounts for fork PRs, missing GitHub auth, missing Codex CLI, closed/merged PRs, dirty worktrees, unpushed commits, force pushes, missing trigger comments, stale trigger timestamps, bot nonresponse, usage/rate limits, missing templates with fallbacks, older loop directories that should not revive, and macOS Bash 3.2 limitations. Git metadata was unavailable in this read-only branch export because no `.git` directory is present, but file inspection did not require it.
- validation_or_tests: `tests/run-all-tests.sh:33` runs the aggregate test list, including `tests/test-pr-loop.sh`, `tests/test-pr-loop-system.sh`, and robustness suites. `tests/test-pr-loop.sh:33` sources `tests/test-pr-loop-hooks.sh` and calls its `run_hook_tests` at `tests/test-pr-loop.sh:44`. Template validation, hook input robustness, setup robustness, PR API robustness, and stop-hook behavior have dedicated suites under `tests/` and `tests/robustness/`. I did not run tests because the assignment requested read-only research notes only.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop hook for the PR review loop. It intercepts attempted Claude exit during an active `.humanize/pr-loop/<timestamp>` session, checks local and remote readiness, polls configured PR review bots, invokes local Codex to classify bot feedback, and advances or terminates PR-loop state.
- algorithmic_behavior: The hook finds the newest active PR-loop state via `find_active_pr_loop` (`hooks/pr-loop-stop-hook.sh:64`), parses YAML frontmatter and YAML-list `configured_bots`/`active_bots` (`hooks/pr-loop-stop-hook.sh:87`, `hooks/pr-loop-stop-hook.sh:120`), resolves the PR base repository for fork support (`hooks/pr-loop-stop-hook.sh:197`), blocks until a current-round resolve file exists (`hooks/pr-loop-stop-hook.sh:258`), enforces clean/pushed git status (`hooks/pr-loop-stop-hook.sh:279`), detects force-push history rewrites and clears stale trigger state (`hooks/pr-loop-stop-hook.sh:364`), calculates whether a trigger comment is required (`hooks/pr-loop-stop-hook.sh:609`), handles Codex thumbs-up reaction shortcuts (`hooks/pr-loop-stop-hook.sh:646`), verifies Claude eyes reaction when required (`hooks/pr-loop-stop-hook.sh:749`), polls all configured bots rather than only active bots (`hooks/pr-loop-stop-hook.sh:801`), deduplicates comments by id (`hooks/pr-loop-stop-hook.sh:1042`), writes round comment/check/feedback artifacts, and updates `active_bots` based on Codex’s per-bot classification (`hooks/pr-loop-stop-hook.sh:1425`).
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/pr-loop/<timestamp>/state.md`, `round-N-pr-resolve.md`, `gh` PR APIs, `git` status/commit ancestry, bot reactions, fetched comments/reviews, prompt templates, `codex`, and `HUMANIZE_CODEX_BYPASS_SANDBOX`. Outputs are either no-op exit, loop terminal state renames such as `approve-state.md`, `merged-state.md`, `closed-state.md`, `usage-limit-state.md`, or JSON hook blocks with a reason/systemMessage. It writes `round-(N+1)-pr-comment.md`, `round-(N+1)-codex-prompt.md`, `round-(N+1)-pr-check.md`, and `round-(N+1)-pr-feedback.md`; it also rewrites `state.md` when trigger fields, active bots, round number, startup case, and latest commit fields change.
- gates_or_invariants: Required state fields include a numeric `current_round` and `pr_number` (`hooks/pr-loop-stop-hook.sh:185`). The hook blocks if the resolution summary is missing (`hooks/pr-loop-stop-hook.sh:263`), non-humanize files are dirty (`hooks/pr-loop-stop-hook.sh:298`), commits are unpushed (`hooks/pr-loop-stop-hook.sh:349`), max iterations are exceeded (`hooks/pr-loop-stop-hook.sh:431`), a required trigger is missing (`hooks/pr-loop-stop-hook.sh:727`), Claude does not add eyes after retries (`hooks/pr-loop-stop-hook.sh:778`), bot responses are still absent (`hooks/pr-loop-stop-hook.sh:1390`), Codex is missing (`hooks/pr-loop-stop-hook.sh:1304`), Codex fails (`hooks/pr-loop-stop-hook.sh:1344`), or Codex output is empty (`hooks/pr-loop-stop-hook.sh:1356`). The branch-specific invariant is at `hooks/pr-loop-stop-hook.sh:1328`: default local Codex review uses sandboxed `--full-auto`; only `HUMANIZE_CODEX_BYPASS_SANDBOX=true` or `1` changes `CODEX_AUTO_FLAG` to `--dangerously-bypass-approvals-and-sandbox` (`hooks/pr-loop-stop-hook.sh:1331`).
- dependencies_and_callers: Registered by `hooks/hooks.json:60`. Uses `hooks/lib/loop-common.sh` for active loop discovery, bot-author mapping, YAML list generation, mention strings, template loading, and goal tracker updates. Uses `scripts/portable-timeout.sh` for bounded operations, `scripts/check-bot-reactions.sh` for Codex thumbs-up and Claude eyes reactions, `scripts/poll-pr-reviews.sh` for comment/review polling, and `scripts/check-pr-reviewer-status.sh` for startup-case refresh. Called after `/humanize:start-pr-loop`, whose setup writes the expected state and initial round files in `scripts/setup-pr-loop.sh:615`.
- edge_cases_or_failure_modes: Handles no active loop, missing state file, invalid round, current PR not found in fork/current repo, closed/merged PRs, missing resolve file, git status timeout/failure, dirty non-humanize files, missing upstream branch, PR head mismatch, force-push rewrite with old commit no longer ancestor, stale trigger after new commits, old trigger comments before `latest_commit_at`, startup cases 1-5, Codex thumbs-up instead of comments, Claude eyes retry timeout, per-bot timeout anchored to trigger time, cancel signal during polling, duplicate comments across poll pages, empty comment body PR reviews converted upstream into placeholders, Codex rate/usage-limit marker, and macOS Bash lacking associative arrays.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` covers YAML state format, write protections, sorting, deduplication, trigger detection, missing-trigger blocking, trigger timestamp priority, timeout anchoring, round file naming, mocked end-to-end trigger detection, pagination, approval-only reviews, fixture-backed fetch/poll behavior, wrong-round validation, and monitor display. `tests/test-pr-loop-stophook.sh` adds deeper stop-hook scenarios for force-push trigger rejection, startup-case exceptions, timeout/state transitions, and dynamic startup cases. Robustness coverage appears in `tests/robustness/test-hook-system-robustness.sh:461` and following stop-hook cases.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification bundle for PR-loop hook behavior. It is sourced by `tests/test-pr-loop.sh:33` and executed via `run_hook_tests` (`tests/test-pr-loop.sh:44`), relying on helpers such as `pass`, `fail`, and `init_test_git_repo` from `tests/test-helpers.sh`.
- algorithmic_behavior: Defines `run_hook_tests` at `tests/test-pr-loop-hooks.sh:15`, then runs grouped tests inline. The first group validates PR-loop file protections: YAML-list `active_bots` format (`tests/test-pr-loop-hooks.sh:26`), `state.md` write blocking (`tests/test-pr-loop-hooks.sh:68`), `pr-comment` write blocking (`tests/test-pr-loop-hooks.sh:102`), allowed `pr-resolve` writes (`tests/test-pr-loop-hooks.sh:135`), and Bash redirection protection without an RLCR loop (`tests/test-pr-loop-hooks.sh:174`). Later groups encode sorting, gate logic, mocked stop-hook execution, approval-only PR review handling, fixture-backed fetch/poll behavior, wrong-round write/edit validation, and monitor formatting.
- inputs_outputs_state: Inputs are temporary test repositories/directories, mock `gh` and `git` binaries, fixture JSON files under `tests/fixtures`, real project scripts/hooks, and helper-created `.humanize/pr-loop/<timestamp>/state.md` files. Outputs are test pass/fail counters and temporary generated files such as mock loop state, PR comments, and fetched markdown. The tests model state fields including `configured_bots`, `active_bots`, `last_trigger_at`, `startup_case`, `latest_commit_sha`, and `latest_commit_at`.
- gates_or_invariants: The tests assert that bot lists are YAML lists (`tests/test-pr-loop-hooks.sh:45`), PR-loop managed files cannot be forged through Write/Edit/Bash validators, current round controls which `round-N-pr-resolve.md` path is writable (`tests/test-pr-loop-hooks.sh:1431`, `tests/test-pr-loop-hooks.sh:1465`, `tests/test-pr-loop-hooks.sh:1499`), human comments sort ahead of bot comments (`tests/test-pr-loop-hooks.sh:239`), comments dedupe by id (`tests/test-pr-loop-hooks.sh:271`), configured bots stay separate from active bots (`tests/test-pr-loop-hooks.sh:288`, `tests/test-pr-loop-hooks.sh:698`), previously approved bots can be re-added when new issues appear (`tests/test-pr-loop-hooks.sh:353`), final Codex markers are parsed from the last nonblank line (`tests/test-pr-loop-hooks.sh:415`, `tests/test-pr-loop-hooks.sh:437`), and `last_trigger_at` beats `started_at` even in round 0 (`tests/test-pr-loop-hooks.sh:624`, `tests/test-pr-loop-hooks.sh:1052`).
- dependencies_and_callers: Depends on project hooks `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `pr-loop-stop-hook.sh`; scripts `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `humanize.sh`, `setup-fixture-mock-gh.sh`, and `setup-monitor-test-env.sh`; command-line tools `jq`, `grep`, `sed`, `git`, and shell. Called indirectly by `tests/test-pr-loop.sh`, which is included in aggregate suite `tests/run-all-tests.sh:53`.
- edge_cases_or_failure_modes: Captures malformed or malicious loop-file writes, PR loop behavior when no RLCR loop exists, jq timestamp ordering, bot status extraction from markdown tables, old versus new trigger timestamps, missing trigger for round greater than zero, per-bot timeout anchored to trigger rather than poll start, pagination where trigger appears after first page, empty-body approval reviews, `--after` timestamp filtering, wrong-round writes and edits, and display of empty `active_bots` as none. One notable portability issue: `test_bot_readd_logic` uses associative arrays (`declare -A`) at `tests/test-pr-loop-hooks.sh:360`, while the production hook avoids associative arrays for macOS Bash 3.2 compatibility; this test may require a newer Bash than the production algorithm.
- validation_or_tests: This file is itself the validation surface. It contains unit-style simulations for jq/shell parsing and integration-style executions with mocked binaries. It does not appear to include a direct assertion for `HUMANIZE_CODEX_BYPASS_SANDBOX`; that env-var behavior is covered by inspection of `hooks/pr-loop-stop-hook.sh` and sibling RLCR hook logic rather than this assigned test file.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR plan-backup integrity gate. It is not PR-loop-specific, but it is core hook-contract text because validators render it when a user attempts to modify `.humanize/rlcr/<timestamp>/plan.md`, which is the loop’s backup copy of the original plan.
- algorithmic_behavior: The template states that `plan.md` in the loop directory is a backup and cannot be modified (`prompt-template/block/plan-backup-protected.md:3`), explains that the backup preserves plan integrity (`prompt-template/block/plan-backup-protected.md:5`), and instructs users to read the plan rather than modify it (`prompt-template/block/plan-backup-protected.md:7`). It has no variables and no branching logic; its behavior is determined by callers.
- inputs_outputs_state: Input is only the template loader request. Output is static markdown used as a validator block reason. It does not mutate state, but it protects the invariant that the copied plan backup remains a stable reference while an RLCR loop is active.
- gates_or_invariants: The invariant is enforced in multiple validators: `hooks/loop-write-validator.sh:179` blocks Write to `.humanize/rlcr/**/plan.md` and renders this template at `hooks/loop-write-validator.sh:182`; `hooks/loop-edit-validator.sh:135` and `hooks/loop-bash-validator.sh:338` also reference it. The template is a user-facing contract for rejecting both direct tool writes and shell attempts that would bypass tool-specific validation.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/lib/template-loader.sh` when the validators detect the plan-backup path. Related enforcement logic includes common path helpers in `hooks/lib/loop-common.sh` and command validators registered in `hooks/hooks.json:14`.
- edge_cases_or_failure_modes: The file itself is a static message, so failure modes are caller-side: missing template falls back to inline text in validators, path normalization errors could fail to identify the backup, and Bash redirection detection must catch shell-level edits. Because it is RLCR backup protection and not Codex sandbox selection, it is a partial skip candidate for the branch title, but not for the assigned core algorithm subset because it defines a loop integrity gate.
- validation_or_tests: `tests/test-plan-file-hooks.sh`, `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, and hook-system robustness suites cover plan file and backup protection behavior. Template reference coverage is also checked by `tests/test-template-references.sh` and template loading by `tests/test-template-loader.sh`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-121 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt contract used when the loop starts with existing comments. It defines the mandatory user/agent completion actions that make the stop hook’s later gates satisfiable: commit and push changes, comment on the PR to trigger re-review, and write the resolution summary file.
- algorithmic_behavior: The template emits a prominent “Work Completion Requirements” block (`prompt-template/pr-loop/critical-requirements-has-comments.md:3`). It requires commit and push (`prompt-template/pr-loop/critical-requirements-has-comments.md:8`), a `gh pr comment` command with `{{PR_NUMBER}}` and `{{BOT_MENTION_STRING}}` (`prompt-template/pr-loop/critical-requirements-has-comments.md:12`), and a resolution summary at `{{RESOLVE_PATH}}` (`prompt-template/pr-loop/critical-requirements-has-comments.md:15`). It specifies resolution summary contents: issues addressed, files modified, tests added if any (`prompt-template/pr-loop/critical-requirements-has-comments.md:18`), and states that the Stop Hook will poll bot reviews afterward (`prompt-template/pr-loop/critical-requirements-has-comments.md:23`).
- inputs_outputs_state: Inputs are template variables supplied by PR-loop setup: `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`. Output is prompt markdown inserted into the initial PR-loop task. It indirectly drives state by instructing creation of the current-round `round-0-pr-resolve.md` and by requiring a PR trigger comment that later becomes `last_trigger_at` and `trigger_comment_id`.
- gates_or_invariants: This template mirrors hard gates in `hooks/pr-loop-stop-hook.sh`: unpushed commits block exit (`hooks/pr-loop-stop-hook.sh:349`), missing trigger comments block where required (`hooks/pr-loop-stop-hook.sh:727`), and missing resolve files block before polling (`hooks/pr-loop-stop-hook.sh:263`). The template prevents an agent from thinking local edits alone complete the PR-loop; remote bot review requires pushed commits and a PR mention.
- dependencies_and_callers: `scripts/setup-pr-loop.sh` renders this template when initial PR comments exist, using `load_and_render_safe` at `scripts/setup-pr-loop.sh:940` after constructing `TEMPLATE_VARS` that include `PR_NUMBER`, `RESOLVE_PATH`, and `BOT_MENTION_STRING` (`scripts/setup-pr-loop.sh:733`). The no-comments sibling template is rendered from `scripts/setup-pr-loop.sh:914`, but this assigned file is specifically for the has-comments path.
- edge_cases_or_failure_modes: If variables are missing or incorrectly rendered, the actor may comment on the wrong PR, mention the wrong bots, or write the wrong resolve file, causing stop-hook blocks. If the prompt is ignored, the hook catches missing summary, unpushed commits, or missing trigger. The template itself does not distinguish startup cases; setup chooses whether this has-comments contract applies.
- validation_or_tests: Template presence and references are covered by template tests. Behavioral correspondence is exercised by `tests/test-pr-loop-hooks.sh`: missing resolve blocks (`tests/test-pr-loop-hooks.sh:766`), missing trigger blocks (`tests/test-pr-loop-hooks.sh:606`), trigger detection (`tests/test-pr-loop-hooks.sh:845`), and wrong-round resolve validation (`tests/test-pr-loop-hooks.sh:1431`). Setup-script robustness tests validate PR-loop argument and state initialization paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence sections present, one for each assigned row
- missing_items: none
- duplicate_items: none detected in Item Evidence headings
- final_worker_status: `complete`