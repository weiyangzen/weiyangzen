# agent_03 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command contract directory for the plugin’s algorithmic entry points. Recursive inspection found five command specs: `commands/gen-plan.md`, `commands/start-pr-loop.md`, `commands/cancel-pr-loop.md`, `commands/start-rlcr-loop.md`, and `commands/cancel-rlcr-loop.md`. The directory does not implement shell logic directly; it defines allowed tool boundaries, argument surfaces, delegation targets, sentinel-output handling, and user-facing loop rules.
- algorithmic_behavior: `gen-plan.md` defines a six-phase draft-to-plan algorithm: validate IO through `scripts/validate-gen-plan-io.sh`, check repo relevance, analyze clarity/consistency/completeness/functionality, ask for clarifications, generate AC-style plan content, then edit and verify the output plan (`commands/gen-plan.md:30`, `commands/gen-plan.md:50`, `commands/gen-plan.md:76`, `commands/gen-plan.md:111`, `commands/gen-plan.md:142`, `commands/gen-plan.md:251`). `start-pr-loop.md` delegates to `scripts/setup-pr-loop.sh`, then describes the PR review loop: detect PR, fetch comments, fix issues, push, tag bots, Stop Hook polls, local Codex validates remote bot concerns (`commands/start-pr-loop.md:10`, `commands/start-pr-loop.md:16`). `start-rlcr-loop.md` delegates to `scripts/setup-rlcr-loop.sh`, with implementation and review phases plus goal-tracker discipline (`commands/start-rlcr-loop.md:10`, `commands/start-rlcr-loop.md:16`, `commands/start-rlcr-loop.md:27`, `commands/start-rlcr-loop.md:60`). Cancel specs delegate to cancellation scripts and interpret first-line sentinels (`commands/cancel-pr-loop.md:17`, `commands/cancel-rlcr-loop.md:17`).
- inputs_outputs_state: Inputs are slash-command arguments: plan IO paths for gen-plan; PR loop bot flags and Codex options; RLCR plan/max/model/timeout/tracking/base-review options; cancel optional `--force`. Outputs are generated plan files, `.humanize/pr-loop/<timestamp>/` session files, `.humanize/rlcr/<timestamp>/` session files, and human-readable activation/cancellation instructions. State transitions are delegated: PR cancel treats active state as newest `.humanize/pr-loop/*/state.md` and preserves the loop directory (`commands/cancel-pr-loop.md:21`); RLCR cancel treats newest `state.md` or `finalize-state.md` as active and may require confirmation in finalize phase (`commands/cancel-rlcr-loop.md:21`, `commands/cancel-rlcr-loop.md:36`).
- gates_or_invariants: Command specs restrict allowed tools to known scripts for start/cancel, preventing arbitrary command expansion from the slash command surface (`commands/start-pr-loop.md:4`, `commands/cancel-pr-loop.md:3`, `commands/start-rlcr-loop.md:4`). PR loop requires at least one bot flag (`commands/start-pr-loop.md:25`). Loop anti-cheat invariants are explicit: write summaries, push changes, tag configured bots, do not edit state or run cancel from inside the loop (`commands/start-pr-loop.md:50`, `commands/start-rlcr-loop.md:46`). RLCR finalize cancellation requires AskUserQuestion confirmation unless forced (`commands/cancel-rlcr-loop.md:23`).
- dependencies_and_callers: `commands` coordinates with sibling `scripts/` for setup/cancel/validation, `hooks/` for Stop Hook interception and file-protection behavior, and `prompt-template/` for generated round prompts and block messages. `gen-plan.md` depends on `scripts/validate-gen-plan-io.sh` and the plan template contract (`commands/gen-plan.md:32`, `commands/gen-plan.md:253`). PR loop start/cancel map to `scripts/setup-pr-loop.sh` and `scripts/cancel-pr-loop.sh`; RLCR start/cancel map to `scripts/setup-rlcr-loop.sh` and `scripts/cancel-rlcr-loop.sh`.
- edge_cases_or_failure_modes: Gen-plan has explicit exit-code mapping for missing/empty input, missing output dir, existing output, permissions, invalid args, and missing template (`commands/gen-plan.md:38`). PR loop command ends on max iterations, all bots approved, or user cancellation (`commands/start-pr-loop.md:58`). RLCR supports `--skip-impl` for direct code review without plan/goal-tracker init (`commands/start-rlcr-loop.md:69`). Finalize cancellation is a special edge case: plain cancel returns `FINALIZE_NEEDS_CONFIRM`, while forced cancel returns `CANCELLED_FINALIZE` through the script contract (`commands/cancel-rlcr-loop.md:20`, `commands/cancel-rlcr-loop.md:31`).
- validation_or_tests: The command contracts are covered indirectly by script tests: `tests/test-pr-loop-scripts.sh` asserts setup/cancel/fetch/poll argument behavior, sentinel outputs, and state rename effects (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`). No tests were run for this research pass because the task requested read-only notes.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-033 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation state-transition script for active RLCR loops. It creates a cancellation signal and renames the active loop state file to `cancel-state.md`, preserving session evidence while making the loop inactive.
- algorithmic_behavior: Parses only `--force` and help; unknown options exit as other error (`scripts/cancel-rlcr-loop.sh:24`, `scripts/cancel-rlcr-loop.sh:26`, `scripts/cancel-rlcr-loop.sh:57`). Resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, then selects the newest subdirectory under `.humanize/rlcr/` via reverse sorted directory names (`scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:73`). It classifies the loop as `NORMAL_LOOP` if `state.md` exists, `FINALIZE_PHASE` if `finalize-state.md` exists, otherwise `NO_ACTIVE_LOOP` (`scripts/cancel-rlcr-loop.sh:85`, `scripts/cancel-rlcr-loop.sh:89`). It extracts `current_round` and `max_iterations` for reporting (`scripts/cancel-rlcr-loop.sh:105`) and, after gates, touches `.cancel-requested` then moves the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`).
- inputs_outputs_state: Inputs are filesystem state under `$PROJECT_ROOT/.humanize/rlcr`, optional `--force`, and YAML-like fields in `state.md` or `finalize-state.md`. Outputs are sentinel first lines: `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, or `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:75`, `scripts/cancel-rlcr-loop.sh:96`, `scripts/cancel-rlcr-loop.sh:117`, `scripts/cancel-rlcr-loop.sh:144`). State transition is `state.md -> cancel-state.md` for normal loops and `finalize-state.md -> cancel-state.md` for forced finalize cancellation.
- gates_or_invariants: Finalize phase has a confirmation gate: without `--force`, the script exits code 2 and does not touch state (`scripts/cancel-rlcr-loop.sh:117`). The `.cancel-requested` file is a required authorization signal for hook validators that allow the otherwise protected `mv` operation; shared validation requires the signal, rejects command substitution/chaining/newlines/extra args/wrong paths, and rejects symlink sources (`hooks/lib/loop-common.sh:533`, `hooks/lib/loop-common.sh:557`, `hooks/lib/loop-common.sh:562`, `hooks/lib/loop-common.sh:681`, `hooks/lib/loop-common.sh:696`).
- dependencies_and_callers: Called by `commands/cancel-rlcr-loop.md`, which handles finalize confirmation using AskUserQuestion and reruns with `--force` only when confirmed (`commands/cancel-rlcr-loop.md:23`). Loop monitors classify `cancel-state.md` as cancelled (`scripts/lib/monitor-common.sh:272`). Hook validators and robustness tests depend on the `.cancel-requested` plus rename convention.
- edge_cases_or_failure_modes: Directory selection uses lexicographic newest timestamp directory; malformed/non-timestamp names could affect “newest” if present. Missing active state in an existing latest directory returns `NO_ACTIVE_LOOP`, even if an older directory is active (`scripts/cancel-rlcr-loop.sh:73`, `scripts/cancel-rlcr-loop.sh:95`). Missing round fields are tolerated for reporting with `?` defaults (`scripts/cancel-rlcr-loop.sh:109`). `set -euo pipefail` means failed `touch` or `mv` exits nonzero before success sentinel, so callers should trust the sentinel, not just partial output.
- validation_or_tests: Finalize behavior is specified by `commands/cancel-rlcr-loop.md` sentinel handling (`commands/cancel-rlcr-loop.md:17`). Security and transition coverage exists in `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, which exercise authorized and malicious `mv state.md/finalize-state.md cancel-state.md` forms. `tests/test-finalize-phase.sh` covers finalize-state active-loop semantics and protection of finalize state (`tests/test-finalize-phase.sh:283`, `tests/test-finalize-phase.sh:338`). No tests were run in this read-only pass.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-063 `file` `tests/test-pr-loop-scripts.sh`
- cursor: `[_]`
- core_role: Executable specification for PR-loop shell script API behavior. It does not implement the PR loop itself; it asserts script-level parsing, required arguments, sentinel outputs, and the critical cancel state transition for the four PR-loop scripts.
- algorithmic_behavior: Defines four grouped test runners and one aggregate `run_script_tests` (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:259`, `tests/test-pr-loop-scripts.sh:332`, `tests/test-pr-loop-scripts.sh:405`). Setup tests verify help text, missing bot flag rejection, unknown option rejection, acceptance of `--claude`, `--codex`, both bot flags, `--max`, `--codex-model`, and `--codex-timeout`, plus invalid `--max` rejection (`tests/test-pr-loop-scripts.sh:27`, `tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:52`, `tests/test-pr-loop-scripts.sh:66`, `tests/test-pr-loop-scripts.sh:117`). Cancel tests verify help, `NO_LOOP` when no active loop exists, and active-loop cancellation by renaming `state.md` to `cancel-state.md` (`tests/test-pr-loop-scripts.sh:181`, `tests/test-pr-loop-scripts.sh:192`, `tests/test-pr-loop-scripts.sh:211`). Fetch/poll tests verify required PR/output/after/bot arguments and invalid PR number handling (`tests/test-pr-loop-scripts.sh:279`, `tests/test-pr-loop-scripts.sh:293`, `tests/test-pr-loop-scripts.sh:307`, `tests/test-pr-loop-scripts.sh:352`).
- inputs_outputs_state: Inputs are environment variables from the broader test harness: `PROJECT_ROOT`, `TEST_DIR`, and `pass`/`fail` functions. It executes real scripts under `$PROJECT_ROOT/scripts`. For cancel state, it creates `.humanize/pr-loop/<timestamp>/state.md` with `current_round`, `max_iterations`, and `pr_number`, exports `CLAUDE_PROJECT_DIR` so the script targets the test dir, and expects `cancel-state.md` to exist while `state.md` is absent after cancellation (`tests/test-pr-loop-scripts.sh:194`, `tests/test-pr-loop-scripts.sh:215`, `tests/test-pr-loop-scripts.sh:222`, `tests/test-pr-loop-scripts.sh:236`).
- gates_or_invariants: The spec encodes CLI invariants: PR setup cannot start without at least one bot; numeric options must be numeric; fetch requires numeric PR and output path; poll requires numeric PR plus `--after` and `--bots`. It also encodes the sentinel protocol used by command prompts: no loop must return `NO_LOOP` with exit code 1, while successful cancel must return `CANCELLED` with exit code 0 (`tests/test-pr-loop-scripts.sh:203`, `tests/test-pr-loop-scripts.sh:236`).
- dependencies_and_callers: Intended to be sourced by a test harness, not run standalone, because it assumes `PROJECT_ROOT`, `TEST_DIR`, `pass`, and `fail` exist (`tests/test-pr-loop-scripts.sh:11`). It covers `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`. Those scripts in turn depend on git/gh/jq/codex for full runtime behavior; several tests intentionally only assert early parsing before those deeper prerequisites are reached (`tests/test-pr-loop-scripts.sh:68`).
- edge_cases_or_failure_modes: Positive parse tests often assert absence of a specific parse error rather than full success, so they can pass even when later runtime prerequisites fail. The cancel test creates one timestamped directory and does not cover multiple directories, `NO_ACTIVE_LOOP`, or PR-loop finalize-like states, which PR loops do not have. Fetch/poll tests cover required arguments but not GitHub API retry, fork repo resolution, dedupe, timestamp filtering, or approval-only review handling; those are covered elsewhere, for example fixture tests in `tests/test-pr-loop-hooks.sh`.
- validation_or_tests: This file itself is a validation surface. It aligns with setup parsing in `scripts/setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:110`, `scripts/setup-pr-loop.sh:179`), cancel implementation in `scripts/cancel-pr-loop.sh` (`scripts/cancel-pr-loop.sh:78`, `scripts/cancel-pr-loop.sh:118`), fetch validation in `scripts/fetch-pr-comments.sh` (`scripts/fetch-pr-comments.sh:92`), and poll validation in `scripts/poll-pr-reviews.sh` (`scripts/poll-pr-reviews.sh:83`). No tests were executed in this read-only research pass.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-093 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: Block-message template used by validators when a user/tool attempts to write generated PR-loop prompt or comment files. It is a guardrail template, not executable logic, but it participates directly in the algorithm by explaining and enforcing read-only PR-loop generated artifacts.
- algorithmic_behavior: The template declares that `round-*-pr-comment.md` and `round-*-prompt.md` under `.humanize/pr-loop/` cannot be written because they are generated by the PR loop system (`prompt-template/block/pr-loop-prompt-write.md:1`, `prompt-template/block/pr-loop-prompt-write.md:3`). It describes the generated roles: PR comments fetched from GitHub and current-round instructions (`prompt-template/block/pr-loop-prompt-write.md:5`).
- inputs_outputs_state: Inputs are the validator context that calls `pr_loop_prompt_blocked_message`; the template itself has no variables. Output is a rendered Markdown denial message. It does not mutate loop state; it prevents mutation by being returned on hook exit code 2 from Write/Edit/Bash validators.
- gates_or_invariants: Invariant: generated PR-loop inputs and analysis artifacts are system-managed read-only files. The actual validator scope is broader than the template wording: Bash protection blocks `round-N-pr-comment.md`, `round-N-prompt.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md` with both full-path and bare-filename checks (`hooks/loop-bash-validator.sh:417`, `hooks/loop-bash-validator.sh:423`, `hooks/loop-bash-validator.sh:431`). Write/Edit validators use `is_pr_loop_readonly_file` and this same block message for PR-loop read-only files (`hooks/loop-write-validator.sh:83`, `hooks/loop-edit-validator.sh:66`, `hooks/lib/loop-common.sh:827`).
- dependencies_and_callers: Loaded by `hooks/lib/loop-common.sh` in `pr_loop_prompt_blocked_message`, with a fallback message if template loading fails (`hooks/lib/loop-common.sh:898`). Called by Bash, Write, and Edit validators when protected PR-loop files are targeted (`hooks/loop-bash-validator.sh:433`, `hooks/loop-write-validator.sh:84`, `hooks/loop-edit-validator.sh:67`). The protected files are created by `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh`, for example round-0 prompt/comment and later Codex prompt/check/feedback files (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:761`, `hooks/pr-loop-stop-hook.sh:1237`, `hooks/pr-loop-stop-hook.sh:1243`).
- edge_cases_or_failure_modes: Template text explicitly names only comment and prompt files, while code also protects Codex prompt/check/feedback files, so the user-facing message is slightly narrower than enforcement. If template loading fails, the fallback preserves the same core denial (`hooks/lib/loop-common.sh:900`). The guard depends on path-pattern detection; validators include bare filename checks to reduce relative-path bypass risk (`hooks/loop-bash-validator.sh:432`).
- validation_or_tests: Pattern protection is covered by PR-loop hook and bash-validator tests, including generated-file protection and PR-loop read-only behavior. The template’s existence and rendering route are also indirectly covered by comprehensive template tests. No tests were executed in this read-only pass.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-123 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR-loop goal-tracker template. It defines the durable schema that the PR-loop setup script writes to `.humanize/pr-loop/<timestamp>/goal-tracker.md` and that monitor/update helpers later parse and mutate.
- algorithmic_behavior: The template records PR metadata with placeholders for PR number, branch, start timestamp, active bots, and startup case (`prompt-template/pr-loop/goal-tracker-initial.md:3`). It sets the ultimate goal as obtaining approval from all monitored bot reviewers (`prompt-template/pr-loop/goal-tracker-initial.md:11`). It initializes an Issue Summary table with round 0, total counters at zero, and an Issue Log saying round 0 is awaiting initial reviews (`prompt-template/pr-loop/goal-tracker-initial.md:15`, `prompt-template/pr-loop/goal-tracker-initial.md:21`, `prompt-template/pr-loop/goal-tracker-initial.md:27`).
- inputs_outputs_state: Inputs are template variables supplied by `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `BOT_AC_ROWS`, and `STARTED_AT` (`scripts/setup-pr-loop.sh:658`). Output is `goal-tracker.md` in the PR-loop session dir (`scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:712`). Subsequent state transitions add Issue Summary rows, update totals, and append Issue Log entries via `update_pr_goal_tracker` after Codex/bot analysis (`hooks/lib/loop-common.sh:1151`, `hooks/lib/loop-common.sh:1220`, `hooks/lib/loop-common.sh:1256`, `hooks/lib/loop-common.sh:1267`, `hooks/lib/loop-common.sh:1299`).
- gates_or_invariants: Required schema sections are `Issue Summary`, `Total Statistics`, and `Issue Log`; tests assert those headings and counter fields exist (`tests/test-pr-loop-system.sh:642`, `tests/test-pr-loop-system.sh:647`). Idempotency invariant for updates is round-plus-reviewer based, allowing multiple bots per round without double-counting the same reviewer (`hooks/lib/loop-common.sh:1184`). Monitor parsing depends on exact counter labels `Total Issues Found`, `Total Issues Resolved`, and `Remaining` (`scripts/lib/monitor-common.sh:475`).
- dependencies_and_callers: Rendered by `scripts/setup-pr-loop.sh` after PR state detection and before round-0 prompt emission (`scripts/setup-pr-loop.sh:637`, `scripts/setup-pr-loop.sh:717`). Read by monitor helpers through `humanize_parse_pr_goal_tracker` (`scripts/lib/monitor-common.sh:465`). Used in stop-hook/Codex flow when later analysis prompts instruct goal-tracker updates (`hooks/pr-loop-stop-hook.sh:1252`). It coordinates with `prompt-template/pr-loop/codex-goal-tracker-update.md` and with state fields written to `state.md` at setup (`scripts/setup-pr-loop.sh:615`).
- edge_cases_or_failure_modes: The setup script builds a richer fallback tracker with an Acceptance Criteria table, but the primary template does not currently render `BOT_AC_ROWS`; if the template loads normally, those bot-specific AC rows are unused despite being supplied (`scripts/setup-pr-loop.sh:646`, `scripts/setup-pr-loop.sh:668`, `scripts/setup-pr-loop.sh:712`). Monitor parsing comments mention a different table shape for last reviewer, so the tracker still works for totals but last-reviewer extraction may be schema-sensitive (`scripts/lib/monitor-common.sh:489`). If template loading fails, fallback content has different headings but preserves goal, status, and issue-tracking intent.
- validation_or_tests: `tests/test-pr-loop-system.sh` validates the template schema and an integration path where `setup-pr-loop.sh --codex` creates `goal-tracker.md` containing the expected structure and PR number (`tests/test-pr-loop-system.sh:642`, `tests/test-pr-loop-system.sh:1453`, `tests/test-pr-loop-system.sh:1492`, `tests/test-pr-loop-system.sh:1505`, `tests/test-pr-loop-system.sh:1517`). No tests were executed in this read-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-003; FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-033; FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-063; FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-093; FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-123
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`