# agent_23 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that enforces the “all Claude todos complete before loop exit/finalize” gate. It is called by `hooks/loop-codex-stop-hook.sh` before expensive Codex review or final completion, so incomplete local task state blocks the RLCR state machine from advancing.
- algorithmic_behavior: `find_latest_todos(transcript_path)` scans a Claude transcript JSONL file line-by-line and keeps only the most recent non-empty `TodoWrite` todo list. It supports three transcript shapes: Claude assistant-message tool use, alternate `type: message` tool use, and direct `type: tool_use` entries (`hooks/check-todos-from-transcript.py:20`, `hooks/check-todos-from-transcript.py:51`, `hooks/check-todos-from-transcript.py:65`, `hooks/check-todos-from-transcript.py:78`). `main()` parses hook JSON from stdin, extracts `transcript_path`, finds latest todos, and treats any status other than exact `completed` as incomplete (`hooks/check-todos-from-transcript.py:90`, `hooks/check-todos-from-transcript.py:118`).
- inputs_outputs_state: Input is hook JSON on stdin containing optional `transcript_path`; transcript input is UTF-8 JSONL. Output is process status plus stdout/stderr diagnostics. Exit `0` means no blocking todo evidence: empty stdin, missing transcript path, nonexistent transcript file, no todos, or latest todos all completed. Exit `1` emits `INCOMPLETE_TODOS` plus `  - [status] content` lines on stdout. Exit `2` emits `PARSE_ERROR` on stderr for malformed hook JSON (`hooks/check-todos-from-transcript.py:7`, `hooks/check-todos-from-transcript.py:93`, `hooks/check-todos-from-transcript.py:126`).
- gates_or_invariants: The gate is fail-open for unavailable transcript evidence but fail-closed for malformed hook input. Invalid JSONL transcript lines are ignored, not fatal (`hooks/check-todos-from-transcript.py:36`). Only the latest TodoWrite call matters; older pending todos are superseded when a later TodoWrite list exists (`hooks/check-todos-from-transcript.py:62`, `hooks/check-todos-from-transcript.py:75`, `hooks/check-todos-from-transcript.py:84`). Missing todo `status` becomes `""`, therefore incomplete (`hooks/check-todos-from-transcript.py:121`).
- dependencies_and_callers: Uses Python stdlib `json`, `sys`, and `pathlib.Path` only (`hooks/check-todos-from-transcript.py:15`). The stop hook invokes it via `TODO_CHECKER="$SCRIPT_DIR/check-todos-from-transcript.py"` and blocks on exit `1` or parse exit `2` (`hooks/loop-codex-stop-hook.sh:345`, `hooks/loop-codex-stop-hook.sh:347`, `hooks/loop-codex-stop-hook.sh:352`, `hooks/loop-codex-stop-hook.sh:371`). On incomplete todos, the stop hook renders `prompt-template/block/incomplete-todos.md` or fallback and returns a JSON `decision: block` without Codex review (`hooks/loop-codex-stop-hook.sh:376`, `hooks/loop-codex-stop-hook.sh:381`, `hooks/loop-codex-stop-hook.sh:388`).
- edge_cases_or_failure_modes: If transcript path is absent or nonexistent, loop exit is allowed; this is intentionally permissive when transcript evidence is unavailable (`hooks/check-todos-from-transcript.py:25`, `hooks/check-todos-from-transcript.py:104`). A malformed transcript line is silently skipped, so a later valid TodoWrite can still govern. If a TodoWrite call has an empty `todos` array, it does not replace the prior latest list because replacement is guarded by `if todos:`; this preserves the last non-empty todo state. A todo with empty content still blocks with an empty-content bullet.
- validation_or_tests: `tests/test-todo-checker.sh` directly specifies parse handling, empty input, missing transcript path, nonexistent transcript, all-complete vs pending/in-progress statuses, invalid JSONL tolerance, latest TodoWrite precedence, direct `tool_use`, alternate `message`, missing status, empty content, and content encoding cases (`tests/test-todo-checker.sh:51`, `tests/test-todo-checker.sh:63`, `tests/test-todo-checker.sh:106`, `tests/test-todo-checker.sh:121`, `tests/test-todo-checker.sh:144`, `tests/test-todo-checker.sh:179`, `tests/test-todo-checker.sh:196`, `tests/test-todo-checker.sh:212`, `tests/test-todo-checker.sh:227`, `tests/test-todo-checker.sh:249`). `tests/test-finalize-phase.sh` also verifies finalize exit blocks on incomplete transcript todos and does not invoke Codex during that check (`tests/test-finalize-phase.sh:715`, `tests/test-finalize-phase.sh:721`, `tests/test-finalize-phase.sh:735`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state transition: normal `state.md` plus Codex `COMPLETE` enters `finalize-state.md`; finalization requires summary, clean git, and completed todos; successful finalization renames to `complete-state.md` without another Codex review.
- algorithmic_behavior: The test builds an isolated git repo and loop directory under a temp path, sources `hooks/lib/loop-common.sh`, creates mock `codex` commands, and drives real hook scripts through JSON stdin (`tests/test-finalize-phase.sh:40`, `tests/test-finalize-phase.sh:49`, `tests/test-finalize-phase.sh:53`, `tests/test-finalize-phase.sh:144`, `tests/test-finalize-phase.sh:180`). It specifies helper behavior for finalize path detection, active loop detection, write/edit/bash validator protections, stop-hook finalization, COMPLETE handling, review failure blocking, empty review blocking, todo blocking, and normal-round continuation.
- inputs_outputs_state: Inputs are synthetic loop state frontmatter, mock Codex stdout/stderr, hook JSON such as `{"stop_hook_active": false, "transcript_path": ...}`, and filesystem/git state in the temp repo. Expected outputs are hook exit codes, JSON block decisions, state-file renames, generated review files, and preserved state fields. The loop state fixture includes `current_round`, `max_iterations`, Codex config, `push_every_round`, plan metadata, branches, and `review_started` (`tests/test-finalize-phase.sh:190`). Test result state is accumulated in `TESTS_PASSED`/`TESTS_FAILED` and exits with the failure count (`tests/test-finalize-phase.sh:36`, `tests/test-finalize-phase.sh:876`).
- gates_or_invariants: `find_active_loop` must detect `finalize-state.md` as active but ignore `complete-state.md` (`tests/test-finalize-phase.sh:286`, `tests/test-finalize-phase.sh:305`). `finalize-state.md` is protected from Write, Edit, Bash redirection, Bash `mv` source, and Bash `cp` source, while `finalize-summary.md` is writable in the active finalize loop (`tests/test-finalize-phase.sh:345`, `tests/test-finalize-phase.sh:357`, `tests/test-finalize-phase.sh:369`, `tests/test-finalize-phase.sh:381`, `tests/test-finalize-phase.sh:393`, `tests/test-finalize-phase.sh:405`). Finalize completion requires `finalize-summary.md`, a clean git state, and completed todos, then renames `finalize-state.md` to `complete-state.md` and skips Codex (`tests/test-finalize-phase.sh:429`, `tests/test-finalize-phase.sh:454`, `tests/test-finalize-phase.sh:476`, `tests/test-finalize-phase.sh:498`, `tests/test-finalize-phase.sh:698`).
- dependencies_and_callers: Exercises `hooks/lib/loop-common.sh` helpers (`is_finalize_state_file_path`, `is_finalize_summary_path`, `finalize_state_file_blocked_message`, `find_active_loop`), `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh` (`tests/test-finalize-phase.sh:238`, `tests/test-finalize-phase.sh:260`, `tests/test-finalize-phase.sh:275`, `tests/test-finalize-phase.sh:348`, `tests/test-finalize-phase.sh:372`, `tests/test-finalize-phase.sh:384`, `tests/test-finalize-phase.sh:436`, `tests/test-finalize-phase.sh:843`, `tests/test-finalize-phase.sh:857`). The implementation side detects finalize phase in the stop hook by selecting `finalize-state.md` as `STATE_FILE` when present (`hooks/loop-codex-stop-hook.sh:72`, `hooks/loop-codex-stop-hook.sh:76`).
- edge_cases_or_failure_modes: Max-iteration termination bypasses finalize and creates `maxiter-state.md` (`tests/test-finalize-phase.sh:542`). Codex review command failure must block rather than advancing to finalize, preserving `state.md` with `review_started: true` (`tests/test-finalize-phase.sh:566`, `tests/test-finalize-phase.sh:609`). Empty Codex review stdout is a hard block, with an empty review log expected at the cache path (`tests/test-finalize-phase.sh:625`, `tests/test-finalize-phase.sh:659`). Normal non-COMPLETE review must block with feedback, preserve `state.md`, increment `current_round`, and write `round-N-review-result.md` (`tests/test-finalize-phase.sh:775`, `tests/test-finalize-phase.sh:791`, `tests/test-finalize-phase.sh:799`).
- validation_or_tests: This file is itself a validation suite. It also documents the corresponding implementation invariants: `find_active_loop` checks only newest loop dir with `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151`), finalize path helpers match filename suffixes (`hooks/lib/loop-common.sh:515`, `hooks/lib/loop-common.sh:521`), the stop hook expects `finalize-summary.md` in finalize phase (`hooks/loop-codex-stop-hook.sh:609`), and finalize completion skips Codex review and moves state to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:737`, `hooks/loop-codex-stop-hook.sh:742`). I inspected but did not execute the test because the assignment requested research notes only and the workspace is read-only.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR push policy gate. It tells the agent that pushes are disallowed unless the loop was started with `--push-every-round`.
- algorithmic_behavior: Static Markdown rendered by `load_and_render_safe` when `hooks/loop-bash-validator.sh` sees a `git push` command and parsed state has `push_every_round` not equal to `true`. The validator exits `2` after rendering this block (`hooks/loop-bash-validator.sh:89`, `hooks/loop-bash-validator.sh:96`, `hooks/loop-bash-validator.sh:98`, `hooks/loop-bash-validator.sh:103`). Template text states local commits should remain local and gives the restart command form `/humanize:start-rlcr-loop plan.md --push-every-round` (`prompt-template/block/git-push.md:1`, `prompt-template/block/git-push.md:6`).
- inputs_outputs_state: Input state is `STATE_PUSH_EVERY_ROUND` from `parse_state_file_strict`; command input is Bash hook JSON parsed by the Bash validator. Output is a stderr block plus exit `2`. It does not mutate loop state; it prevents a Bash action from running.
- gates_or_invariants: Invariant is asymmetric: default `push_every_round: false` means `git push` is blocked; only explicit `true` allows push. The template is part of the user-facing contract for that invariant, while setup and stop hook have corresponding positive instructions when push-every-round is true (`scripts/setup-rlcr-loop.sh:1036`, `scripts/setup-rlcr-loop.sh:1044`, `hooks/loop-codex-stop-hook.sh:1551`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh` functions. The template loader resolves `prompt-template` from the plugin root and loads static content by relative name (`hooks/lib/template-loader.sh:26`, `hooks/lib/template-loader.sh:36`). Direct caller is `hooks/loop-bash-validator.sh`; tests also load it as a canonical existing template (`tests/test-template-loader.sh:59`, `tests/test-template-loader.sh:216`).
- edge_cases_or_failure_modes: If the template is missing, the Bash validator uses an inline fallback with the same policy shape (`hooks/loop-bash-validator.sh:99`). The validator pattern only checks commands starting with `git push` after leading whitespace, so aliases/wrappers may be handled elsewhere only if other validation layers catch them. Because it is static and has no variables, render failure mainly degrades to fallback messaging.
- validation_or_tests: `tests/test-template-loader.sh` asserts `block/git-push.md` loads and that `load_and_render_safe` uses the template rather than fallback (`tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:211`). Comprehensive template tests also reference it through safe rendering (`tests/test-templates-comprehensive.sh:456`, from search evidence). The Bash validator integration is covered by push policy state parsing in validator tests outside this assigned file set.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Positive prompt-contract counterpart to the git-push block. When `--push-every-round` is enabled, this note tells Claude that each round must push commits after committing.
- algorithmic_behavior: Static one-line Markdown note loaded or appended only when `PUSH_EVERY_ROUND == true`. Initial loop setup appends equivalent inline text to `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:962`). Later stop-hook next-round prompt generation loads this exact template and appends it to `NEXT_PROMPT_FILE`; if the template is absent, it falls back to “Also push your changes after committing.” (`hooks/loop-codex-stop-hook.sh:1551`, `hooks/loop-codex-stop-hook.sh:1553`, `hooks/loop-codex-stop-hook.sh:1555`). The template content itself is the behavioral instruction (`prompt-template/claude/push-every-round-note.md:2`).
- inputs_outputs_state: Input state is `push_every_round: true` from loop state. Output is additional prompt content in round prompts; no direct process exit or file gate. It affects agent behavior by making remote push part of round completion expectations.
- gates_or_invariants: Invariant is that push policy is explicit and round-scoped. If enabled, the agent must push after each round; if disabled, the Bash validator blocks push using `block/git-push.md`. The stop hook also enforces unpushed-commit checks only when `PUSH_EVERY_ROUND == true` (`hooks/loop-codex-stop-hook.sh:573`, `hooks/loop-codex-stop-hook.sh:576`), which makes this prompt note more than advisory.
- dependencies_and_callers: Depends on `load_template` from `hooks/lib/template-loader.sh` for direct static loading (`hooks/lib/template-loader.sh:36`). Called by `hooks/loop-codex-stop-hook.sh` during next prompt generation, and mirrored inline by `scripts/setup-rlcr-loop.sh` during initial setup. Related setup output changes the displayed completion requirements depending on the same flag (`scripts/setup-rlcr-loop.sh:1044`).
- edge_cases_or_failure_modes: The file begins with a blank line, so append behavior inserts a leading blank before the note; harmless for Markdown but observable. If the template is missing or empty, the stop hook falls back to a shorter push instruction (`hooks/loop-codex-stop-hook.sh:1554`). Since the setup script currently appends inline text rather than loading this template for round 0, edits to this template affect later rounds but not necessarily the initial prompt unless setup is updated in parallel.
- validation_or_tests: No dedicated direct unit assertion for this exact template appeared in the inspected references. Its behavior is indirectly covered by prompt-generation code paths and by push-every-round state handling in setup/stop-hook logic. I did not classify it as a skip candidate because it defines a transition contract for the push gate.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-143 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for the prompt-template rendering algorithm used by RLCR hooks and validators. It stress-tests `hooks/lib/template-loader.sh`, especially single-pass variable substitution, safe fallback rendering, and large/special-character inputs.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` and shared test helpers, creates a temp dir, then runs positive, stress, and edge-case checks against `render_template`, `load_and_render`, and `load_and_render_safe` (`tests/robustness/test-template-stress-robustness.sh:14`, `tests/robustness/test-template-stress-robustness.sh:16`, `tests/robustness/test-template-stress-robustness.sh:19`). It verifies standard substitution, missing-template fallback, 1KB and 100KB templates, 10KB and 100KB variable values, 50 substitutions, regex metacharacters, ampersands, backslashes, newlines, empty values, placeholder-injection prevention, dollar signs, repeated variables, boundary variables, and placeholder-only templates (`tests/robustness/test-template-stress-robustness.sh:33`, `tests/robustness/test-template-stress-robustness.sh:44`, `tests/robustness/test-template-stress-robustness.sh:82`, `tests/robustness/test-template-stress-robustness.sh:111`, `tests/robustness/test-template-stress-robustness.sh:134`, `tests/robustness/test-template-stress-robustness.sh:161`, `tests/robustness/test-template-stress-robustness.sh:218`, `tests/robustness/test-template-stress-robustness.sh:252`).
- inputs_outputs_state: Inputs are inline template strings, generated template files in `TEST_DIR`, and `VAR=value` assignments. Outputs are rendered strings and pass/fail counters from `tests/test-helpers.sh`. The script exits with `print_test_summary`’s status (`tests/robustness/test-template-stress-robustness.sh:289`). It mutates only its temp test directory when executed.
- gates_or_invariants: Core invariant is single-pass substitution: variable values containing `{{VAR_B}}` must not be rescanned and expanded, preventing prompt content from corrupting later placeholders (`tests/robustness/test-template-stress-robustness.sh:218`). Missing variables remain unchanged and missing templates can use rendered fallback, matching loader comments (`hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:167`). Special replacement characters must be literal because the renderer uses AWK environment lookup rather than sed replacement (`hooks/lib/template-loader.sh:71`, `hooks/lib/template-loader.sh:74`, `hooks/lib/template-loader.sh:116`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`; test helper dependency is `tests/test-helpers.sh` (`tests/robustness/test-template-stress-robustness.sh:16`, `tests/robustness/test-template-stress-robustness.sh:17`). The template loader is widely used by validators and stop hooks for block/review/next-round prompt rendering, including the assigned git-push template and finalize-state block (`hooks/lib/loop-common.sh:478`, `hooks/loop-bash-validator.sh:103`, `hooks/loop-codex-stop-hook.sh:381`, `hooks/loop-codex-stop-hook.sh:1532`).
- edge_cases_or_failure_modes: Large inputs are checked by length thresholds rather than exact byte-for-byte full output, so performance and truncation regressions are caught only coarsely. The multiline-value test checks presence of first and last line, not exact formatting (`tests/robustness/test-template-stress-robustness.sh:195`). The “Unicode” test uses ASCII “Hello World” despite the label (`tests/robustness/test-template-stress-robustness.sh:230`), so it is a weak Unicode coverage point. The AWK renderer prints each input record with `print`, so embedded newlines in environment values can expand into multi-line output; this is accepted by the test.
- validation_or_tests: This file is the validation suite for stress behavior. Related baseline tests in `tests/test-template-loader.sh` verify existing template loading, unused variables, unreplaced placeholders, and fallback behavior (`tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:147`, `tests/test-template-loader.sh:166`, `tests/test-template-loader.sh:181`, `tests/test-template-loader.sh:197`). I inspected but did not execute the test because the run requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections present, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`