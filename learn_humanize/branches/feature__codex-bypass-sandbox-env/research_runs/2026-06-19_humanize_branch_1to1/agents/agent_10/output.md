# agent_10 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Markdown block templates used by hook validators and stop hooks to turn detected rule violations into consistent user-facing block reasons. The directory is part of the loop control surface: it does not execute logic itself, but it defines the contracts shown when runtime gates deny reads, writes, Bash commands, exits, pushes, or PR-loop progress.
- algorithmic_behavior: Recursively inspected all files under `prompt-template/block`. Child roles group into access/path guards (`wrong-file-location.md`, `wrong-round-file.md`, `wrong-round-number.md`, `wrong-directory-path.md`, `wrong-summary-location.md`, `todos-file-access.md`), protected-state guards (`state-file-modification.md`, `pr-loop-state-modification.md`, `finalize-state-file-modification.md`, `plan-backup-protected.md`, `plan-file-modified.md`, `schema-outdated.md`), workflow completion guards (`incomplete-todos.md`, `work-summary-missing.md`, `goal-tracker-not-initialized.md`, `goal-tracker-modification.md`), Git/repository gates (`git-not-clean.md`, `git-status-failed.md`, `git-add-humanize.md`, `git-push.md`, `unpushed-commits.md`, `force-push-detected.md`, `large-files.md`), and PR review gates (`no-trigger-comment.md`, `claude-eyes-timeout.md`, `codex-review-failed.md`).
- inputs_outputs_state: Inputs are uppercase `{{PLACEHOLDER}}` values supplied by callers through `load_and_render_safe`, implemented in `hooks/lib/template-loader.sh:170`. Outputs are rendered Markdown reasons, commonly embedded in JSON hook decisions or written to stderr. The templates do not mutate state; they instruct the agent/user which state transition is required, such as writing a summary, pushing commits, using the correct round file, or leaving managed files untouched.
- gates_or_invariants: The directory enforces invariant vocabulary for loop safety: managed `.humanize` state files are read-only, generated prompt/comment files are read-only, round file numbers must match current round, broad `git add` must not stage `.humanize`, summary/goal-tracker writes must use validated tools, and exit is blocked while required work remains. Template syntax is constrained to uppercase placeholders by `tests/test-templates-comprehensive.sh:122` and malformed-placeholder checks at `tests/test-templates-comprehensive.sh:208`.
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh:36`, `render_template` at `hooks/lib/template-loader.sh:58`, and `load_and_render_safe` at `hooks/lib/template-loader.sh:170`. Major callers include `hooks/lib/loop-common.sh:445` for standard block messages, `hooks/loop-read-validator.sh:127`, `hooks/loop-write-validator.sh:182`, `hooks/loop-bash-validator.sh:103`, `hooks/loop-codex-stop-hook.sh:329`, and `hooks/pr-loop-stop-hook.sh:355`.
- edge_cases_or_failure_modes: Missing or empty templates degrade to caller-provided fallbacks via `load_and_render_safe`; unreplaced placeholders can survive if callers omit variables, which the comprehensive test warns about at `tests/test-templates-comprehensive.sh:592`. Some templates are intentionally advisory fragments, for example `git-not-clean-untracked.md:1`, and are loaded as supplemental notes rather than full block decisions.
- validation_or_tests: `tests/test-templates-comprehensive.sh` validates directory existence and required subdirectories `block`, `codex`, and `claude` at lines `66-80`; loads every Markdown template at lines `90-107`; validates placeholder syntax at lines `118-194`; and renders all templates with dummy substitutions at lines `565-602`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime setup script for `/humanize:start-pr-loop`; it creates an active PR-loop session rooted at `.humanize/pr-loop/<timestamp>` and seeds the state, goal tracker, fetched comments, and first-round prompt that later hooks consume.
- algorithmic_behavior: Parses bot and runtime options (`--claude`, `--codex`, `--max`, `--codex-model`, `--codex-timeout`) at `scripts/setup-pr-loop.sh:110`. It requires at least one bot at line `179`, builds active bot names at lines `196-202`, checks mutual exclusion against active RLCR or PR loops at lines `222-240`, validates git/GitHub/Codex prerequisites at lines `243-277`, resolves current branch and PR context including forks at lines `283-378`, validates YAML-safe values at lines `384-407`, creates a timestamped loop directory at lines `413-419`, fetches initial PR comments at lines `425-431`, determines startup case from reviewer status at lines `437-447`, handles re-review trigger comments for startup cases 4 and 5 at lines `462-562`, optionally verifies Claude eyes reaction at lines `564-600`, writes `state.md` at lines `615-635`, writes `goal-tracker.md` at lines `641-713`, builds `round-0-prompt.md` at lines `721-851`, and prints activation instructions plus critical completion requirements at lines `860-942`.
- inputs_outputs_state: Inputs include CLI flags, `CLAUDE_PROJECT_DIR`, `TEMPLATE_DIR`, local git state, GitHub CLI auth, PR metadata, bot comment state, and helper script results. Outputs include `.humanize/pr-loop/<timestamp>/state.md`, `round-0-pr-comment.md`, `goal-tracker.md`, `round-0-prompt.md`, and a referenced `round-0-pr-resolve.md` path. State transition is from no active loop to active PR loop with `current_round: 0`, `configured_bots`, `active_bots`, PR number, branch, Codex settings, polling settings, startup case, latest commit metadata, and trigger metadata recorded in frontmatter at lines `616-634`.
- gates_or_invariants: Fails closed when another loop is active, the repo has no commit, `gh` is missing or unauthenticated, `codex` is unavailable, no PR is found, PR is merged/closed, branch/model/effort values are YAML unsafe, trigger comment posting fails, or Claude eyes verification fails. For re-review triggers it avoids duplicate mentions by checking comments after the latest commit, stripping code blocks, inline code, indented code, and quoted lines before testing bot mentions at lines `481-499`.
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` at line `35`, `hooks/lib/template-loader.sh` at line `39`, and `hooks/lib/loop-common.sh` at line `211`. Calls `scripts/fetch-pr-comments.sh` at line `431`, `scripts/check-pr-reviewer-status.sh` at line `438`, and `scripts/check-bot-reactions.sh` at line `587`. External tools are `git`, `gh`, `codex`, `jq`, `date`, `grep`, and shell utilities.
- edge_cases_or_failure_modes: Fork PRs are handled by deriving the PR lookup repo from `gh pr view` URL or trying the parent repo with `FORK_OWNER:BRANCH` at lines `308-347`. Reviewer-status failure defaults to startup case 1 at lines `438-441`. Missing latest commit SHA falls back to local `git rev-parse HEAD` at lines `449-452`. If a trigger is posted but its GitHub timestamp cannot be retrieved, the script intentionally avoids local-time fallback to prevent clock-skew filtering at lines `558-561`. Failed trigger or eyes checks remove the newly created loop directory at lines `536` and `581/597`.
- validation_or_tests: Template rendering used by this script is covered by `tests/test-templates-comprehensive.sh`; PR-loop stop behavior and template references appear in related tests from the repository search, but this research pass did not execute the setup script because it would require GitHub/authenticated runtime state and would create loop files.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable CI-style specification for the prompt-template system and `hooks/lib/template-loader.sh`. It verifies that the template corpus is loadable, syntactically constrained, renderable under edge cases, and protected by fallback behavior.
- algorithmic_behavior: Initializes project/template paths and counters at `tests/test-templates-comprehensive.sh:15-28`; validates template directory and required subdirectories at lines `66-80`; loads every Markdown template at lines `90-107`; scans every template for malformed placeholders at lines `118-194`; separately proves malformed patterns are detectable at lines `204-259`; exercises `render_template` edge cases at lines `266-438`; checks fallback behavior and template directory validation at lines `445-490`; renders real templates including `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at lines `497-540`; stress-renders 100 calls and all templates at lines `547-606`; exits based on failure count at lines `619-625`.
- inputs_outputs_state: Inputs are `prompt-template/**/*.md`, `hooks/lib/template-loader.sh`, and temporary test directories/files. Outputs are colored PASS/FAIL/WARN messages plus process exit code. State is limited to counters `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, and temporary directories.
- gates_or_invariants: Requires `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude`; templates must load non-empty or warn/fail depending on content; placeholders must use uppercase `{{VAR_NAME}}` shape without extra braces, spaces, or lowercase names; missing templates must use safe fallback when requested; valid directories must pass `validate_template_dir`, invalid or incomplete directories must fail.
- dependencies_and_callers: Sources `hooks/lib/template-loader.sh` at line `17`, exercising `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. It depends on `find`, `grep`, `sed`, `sort`, `mktemp`, and shell pattern matching.
- edge_cases_or_failure_modes: Rendering tests include empty content, multiline templates, blank line preservation, regex-special values, backslashes, quotes, CJK-like content, markdown formatting, long values, multiline values, variables at line boundaries, repeated variables, and adjacent variables. Some assertions are intentionally permissive for hard shell cases, for example backslashes and multiline values are checked for “no crash” rather than byte-for-byte equality at lines `329-345` and `393-402`.
- validation_or_tests: This file is itself the validation artifact. It also directly validates the assigned `prompt-template/block/unpushed-commits.md` by rendering `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch` and checking the expected title/count/branch at lines `517-525`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Stop-gate block template shown when local commits must be pushed before the loop can exit, especially when push-every-round or PR-loop review visibility requires the remote branch to contain the latest work.
- algorithmic_behavior: The template title is `Unpushed Commits Detected` at `prompt-template/block/unpushed-commits.md:1`. It renders the ahead count and current branch at line `3`, explains the `--push-every-round` requirement at line `5`, provides the required `git push origin {{CURRENT_BRANCH}}` command at lines `7-10`, and allows exit retry after all commits are pushed at line `12`.
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`. Output is Markdown used as the hook block reason. The template does not mutate state; the enforced transition is external: local branch ahead of remote becomes pushed/synchronized, after which the stop hook can continue.
- gates_or_invariants: The invariant is that exit is blocked while the configured workflow requires pushed commits and the current branch is ahead. It assumes `origin` is the push remote and that the rendered branch name is safe to show in a shell command.
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh:576-590` after `git status -sb` reports ahead commits when `PUSH_EVERY_ROUND` is true; the hook emits JSON decision `block` at lines `592-600`. Also called from `hooks/pr-loop-stop-hook.sh:349-359`, where PR-loop logic computes `AHEAD_COUNT` from upstream or PR head fallback before blocking.
- edge_cases_or_failure_modes: If caller cannot determine branch, `loop-codex-stop-hook.sh` falls back to `unknown` at line `581`. PR-loop caller treats missing PR head or missing local history conservatively as unpushed at `hooks/pr-loop-stop-hook.sh:333-343`. The template has no remote-name placeholder, so non-`origin` setups depend on caller fallback or user correction.
- validation_or_tests: Rendered in `tests/test-templates-comprehensive.sh:517-525`; included in all-template load and syntax validation at `tests/test-templates-comprehensive.sh:90-194`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-130 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for loop state parsing and active-loop discovery under malformed state files, stale directories, concurrent reads, and atomic writes.
- algorithmic_behavior: Creates temporary test state files with `create_state_file` at `tests/robustness/test-concurrent-state-robustness.sh:30-50`. It tests `get_current_round` against trailing whitespace, leading whitespace, CRLF endings, empty values, inline comments, unicode content, very large rounds, zero round, and permission-denied behavior at lines `59-160`, `374-388`, and `495-553`. It tests `find_active_loop` newest-directory semantics, `finalize-state.md` detection, non-existent/empty directories, stale-loop ignoring, and cancellation-state exclusion at lines `170-241` and `453-485`. It tests concurrency with 10 parallel reads at lines `251-278` and read-during-write using temp-file plus `mv` atomic replacement at lines `280-323`. It tests strict parser rejection for malformed or missing frontmatter at lines `333-372`, and PR-loop state detection/list format at lines `398-440`.
- inputs_outputs_state: Inputs are temporary `.md` state files and `hooks/lib/loop-common.sh` functions. Outputs are PASS/FAIL messages and final summary exit code from `print_test_summary` at lines `559-560`. State is confined to `TEST_DIR`, created by `tests/test-helpers.sh:86`, with cleanup registered by trap.
- gates_or_invariants: `get_current_round` must return the numeric value or gracefully default rather than crash. `find_active_loop` must only consider the newest timestamp directory and must require `state.md` or `finalize-state.md`, matching implementation comments in `hooks/lib/loop-common.sh:151-170`. Atomic writes must produce valid numeric reads every time. `parse_state_file_strict` must reject missing frontmatter or malformed required fields rather than silently accepting bad state.
- dependencies_and_callers: Sources `tests/test-helpers.sh` at line `17` and `hooks/lib/loop-common.sh` at line `72`. The implemented functions under test are `find_active_loop` at `hooks/lib/loop-common.sh:156`, `get_current_round` at `hooks/lib/loop-common.sh:179`, `parse_state_file_strict` at `hooks/lib/loop-common.sh:255`, and `find_active_pr_loop` at `hooks/lib/loop-common.sh:798`.
- edge_cases_or_failure_modes: Leading whitespace before `current_round` may default to 0 because parsing uses anchored grep, acknowledged at lines `91-95`. CRLF may default because frontmatter separators require `^---$`, acknowledged at lines `113-118`. Older active-looking directories are intentionally ignored when a newer directory lacks active state, preventing stale loop revival. Permission test is skipped when running as root at lines `532-553`. Non-atomic partial writes are not tested; the intended safe write pattern is write-temp then `mv`.
- validation_or_tests: This file is the validation artifact for concurrent state behavior. It was inspected, not executed in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections present above; item IDs are listed only in their section headings to preserve the exact-once evidence constraint.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`