# agent_16 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Robustness validation suite for the Humanize/RLCR/PR-loop core algorithms. The directory contains 17 executable shell test suites and is wired into the default aggregate test runner at `tests/run-all-tests.sh:55-72`, so it acts as a first-class regression gate for setup validation, state parsing, hook safety, template rendering, PR-loop API handling, Git status parsing, timeout behavior, cancel security, and loop/session discovery.
- algorithmic_behavior: The directory is organized as edge-case executable specifications. Each script creates isolated temp state, sources production libraries or invokes production commands, drives specific inputs through the algorithm, and records `pass`/`fail` counters through `tests/test-helpers.sh`. The shared helper contract is simple: `pass`, `fail`, `skip`, `print_test_summary`, and `setup_test_dir` define output and exit behavior at `tests/test-helpers.sh:30-89`; `run-all-tests.sh` then strips ANSI, aggregates `Passed:`/`Failed:` counts, and fails if any suite exits nonzero or reports failed tests at `tests/run-all-tests.sh:121-158`.
- inputs_outputs_state: Inputs are synthetic filesystem fixtures, mocked binaries, JSON hook payloads, Git repositories, generated `.humanize/rlcr/*` and `.humanize/pr-loop/*` session trees, state/frontmatter files, PR API mock responses, plan files, and template files. Outputs are test summaries, exit codes, and sometimes generated loop artifacts used as success indicators. State transitions tested include active `state.md`, `finalize-state.md`, `cancel-state.md`, PR approval/rejection states, setup-created loop directories, and Git dirty/clean/rebase/merge states.
- gates_or_invariants: The suite enforces that malformed or dangerous inputs fail closed, while tolerated oddities degrade gracefully. Key invariants include newest-loop-only discovery and active indicators in `hooks/lib/loop-common.sh:151-174`; strict state parser error modes in `hooks/lib/loop-common.sh:246-260`; JSON/null/UTF-8/depth validation in `hooks/lib/loop-common.sh:59-133`; template single-pass substitution and fallback rendering in `hooks/lib/template-loader.sh:50-132` and `hooks/lib/template-loader.sh:167-203`; and plan path/content checks in `scripts/setup-rlcr-loop.sh:320-536`.
- dependencies_and_callers: The directory depends on production code under `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/portable-timeout.sh`, and hook validators such as `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and stop hooks. It is called by `tests/run-all-tests.sh` after the standard tests, which lists all 17 robustness suites explicitly at `tests/run-all-tests.sh:56-72`.
- edge_cases_or_failure_modes: Child roles by file:
  - `tests/robustness/test-state-file-robustness.sh`: state parser handling for valid frontmatter, extra fields, quoted values, missing delimiters, nonnumeric/negative/large rounds, binary and CRLF content, and `full_review_round` defaults/minimums.
  - `tests/robustness/test-session-robustness.sh`: session discovery across multiple timestamped dirs, finalized sessions, many sessions, empty/nonexistent bases, symlink/special/unicode names, complete/cancel state exclusion, and rapid creation.
  - `tests/robustness/test-goal-tracker-robustness.sh`: `humanize_parse_goal_tracker` over list/table AC formats, active/completed/deferred/open issue counts, malformed/truncated/binary files, large AC counts, special chars, and goal summary truncation.
  - `tests/robustness/test-path-validation-robustness.sh`: production `setup-rlcr-loop.sh` path validation with normal nested paths, metacharacter rejection, symlink file/parent rejection, long/deep paths, empty/comment/short/nonexistent/directory plan failures.
  - `tests/robustness/test-git-operations-robustness.sh`: `humanize_parse_git_status` and Git state detection for clean/dirty/staged/deleted/renamed/binary/space-name files, detached HEAD, rebase/merge, shallow clone, non-git, and permission-like failures.
  - `tests/robustness/test-hook-input-robustness.sh`: hook JSON parsing, malformed/missing fields, long/special/unicode/binary/null/deep JSON, monitor terminal/log edge cases, and production parser availability.
  - `tests/robustness/test-template-stress-robustness.sh`: template substitution under 10KB/100KB values, large template files, many variables, regex/sed-special chars, newlines, Unicode, repeated variables, and placeholder injection prevention.
  - `tests/robustness/test-plan-file-robustness.sh`: assigned separately below; production plan content/path validation for normal, rich, sparse, huge, mixed-ending, binary, null, symlink, directory, and disappearing plan files.
  - `tests/robustness/test-cancel-security-robustness.sh`: cancel authorization and exact `mv` command validation, rejecting command substitution, backticks, shell chaining, wrong source/destination, extra args, newline/variable expansion, symlink sources, and path-normalization bypasses.
  - `tests/robustness/test-timeout-robustness.sh`: portable timeout implementation detection, exit code preservation, timeout code 124, missing/empty commands, large output, special args, signal handling, fallback chain, and export behavior.
  - `tests/robustness/test-base-branch-detection.sh`: setup base branch auto-detection using explicit base branch, local `main`, local `master`, no fallback, and simulated `origin` default branch.
  - `tests/robustness/test-setup-scripts-robustness.sh`: setup argument parsing, plan validation, Git repo constraints, YAML-safe branch/model validation, active loop blocking, symlink rejection, valid setup progression, timeout simulation, full-review-round bounds, and `--skip-impl`.
  - `tests/robustness/test-concurrent-state-robustness.sh`: state parsing with whitespace/CRLF/comments, active loop discovery, concurrent reads, atomic write simulation, strict malformed/missing-frontmatter rejection, PR-loop YAML lists, stale loop exclusion, and permissions.
  - `tests/robustness/test-hook-system-robustness.sh`: edit/write/bash/plan validators and stop hooks for valid and malformed JSON, state modification blocking, path traversal handling, goal-tracker blocking after round 0, concurrent hook calls, corrupted/missing state handling, large/null/deep JSON.
  - `tests/robustness/test-template-error-robustness.sh`: missing template fallback, empty/malformed/unclosed/nested placeholders, invalid variable names, BOM/whitespace templates, permission handling, concurrent loads, subdirectories, and symlinks.
  - `tests/robustness/test-state-transition-robustness.sh`: valid round progressions, max round, finalize/cancel precedence, negative/over-max/nonnumeric rounds, missing required fields, newest directory selection, lexicographic ordering, and wrong-location state exclusion.
  - `tests/robustness/test-pr-loop-api-robustness.sh`: mocked `gh` API failures, rate limits, network errors, bot response parsing, unicode/long comments, PR stop hook state errors, approval-state directory, and poll timeout/failure JSON behavior.
- validation_or_tests: The directory is itself validation. It is invoked by `tests/run-all-tests.sh`, which executes each file, captures output, parses pass/fail counts, and exits 1 if any suite fails. I did not execute the robustness suite because the task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Fixture generator for PR-loop monitor tests. It creates minimal `.humanize/pr-loop/<timestamp>/state.md` directories with controlled YAML frontmatter so tests can verify monitor parsing of `configured_bots` and `active_bots`.
- algorithmic_behavior: The script accepts `TEST_DIR` and optional `TEST_NAME` at `tests/setup-monitor-test-env.sh:12-13`, rejects missing `TEST_DIR` with usage and exit 1 at `tests/setup-monitor-test-env.sh:15-18`, then dispatches by `case "$TEST_NAME"` at `tests/setup-monitor-test-env.sh:20-89`. Each branch sets a deterministic timestamp, creates the PR-loop session directory, and writes a `state.md` frontmatter fixture.
- inputs_outputs_state: Input 1 is the root temp directory to populate. Input 2 is one of `yaml_list`, `configured`, or `empty`. Output is the path echoed at `tests/setup-monitor-test-env.sh:91` plus filesystem state under `$TEST_DIR/.humanize/pr-loop/<timestamp>/state.md`. The generated states contain `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- gates_or_invariants: `yaml_list` creates both `configured_bots` and `active_bots` as YAML lists containing `claude` and `codex` at `tests/setup-monitor-test-env.sh:31-37`. `configured` simulates partial approval by keeping both configured bots but only `codex` active at `tests/setup-monitor-test-env.sh:53-58`. `empty` simulates all-approved/no-active-bot state by writing `active_bots:` with no list entries at `tests/setup-monitor-test-env.sh:74-78`. Unknown fixture names fail closed with the available names at `tests/setup-monitor-test-env.sh:84-87`.
- dependencies_and_callers: Referenced by `tests/test-pr-loop-hooks.sh` for monitor-related fixture setup: `yaml_list` at `tests/test-pr-loop-hooks.sh:1553`, `configured` at `tests/test-pr-loop-hooks.sh:1580`, and `empty` at `tests/test-pr-loop-hooks.sh:1602`. It depends only on Bash, `mkdir`, heredocs, and the caller-provided temp path.
- edge_cases_or_failure_modes: It does not sanitize `TEST_DIR`; tests are expected to pass controlled temp dirs. Unknown names and missing directory argument are explicit failures. Because timestamps are fixed per fixture, repeated calls with the same temp dir/test name overwrite the same state path, which is acceptable for deterministic tests but not suitable as a general-purpose fixture factory.
- validation_or_tests: The behavior is indirectly validated by PR-loop hook/monitor tests that call it and then inspect monitor output against active/configured/all-approved bot states. The file itself is a test helper, not a production runtime path.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt/message template for Codex review execution failure. It defines the user-facing reason shown when the RLCR stop hook cannot obtain a valid Codex review result, forcing retry instead of allowing the loop to exit without review.
- algorithmic_behavior: The template has fixed title/body text and placeholders for `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` at `prompt-template/block/codex-review-failed.md:1-18`. It is rendered through `load_and_render_safe`, so the hook uses this file when present and falls back to inline text if missing or empty.
- inputs_outputs_state: Inputs are the template variables supplied by the stop hook. Output is Markdown embedded in a JSON hook response with `"decision": "block"` and a retry-oriented system message. The block is generated in `codex_review_failure` at `hooks/loop-codex-stop-hook.sh:1180-1244`, where stderr is tailed, variables are passed to the template at `hooks/loop-codex-stop-hook.sh:1226-1234`, and JSON is emitted at `hooks/loop-codex-stop-hook.sh:1236-1243`.
- gates_or_invariants: The invariant is that review failure blocks exit. The surrounding hook runs Codex, saves debug command/stdout/stderr files at `hooks/loop-codex-stop-hook.sh:1257-1286`, blocks nonzero exit codes at `hooks/loop-codex-stop-hook.sh:1320-1337`, tries to copy stdout into the review result file if Codex wrote there instead at `hooks/loop-codex-stop-hook.sh:1339-1355`, blocks missing result files at `hooks/loop-codex-stop-hook.sh:1357-1383`, and blocks empty result files at `hooks/loop-codex-stop-hook.sh:1385-1392`.
- dependencies_and_callers: Depends on the template loader contract in `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE}}` syntax, missing variables remain literal, and substitution is single-pass to prevent placeholder injection at `hooks/lib/template-loader.sh:7-14` and `hooks/lib/template-loader.sh:54-58`. Caller is `hooks/loop-codex-stop-hook.sh`; `rg` also shows an inline PR-loop stop-hook failure message with similar title, but this exact template is loaded by the RLCR stop hook.
- edge_cases_or_failure_modes: There is a variable-name mismatch risk: the template expects `{{CODEX_EXIT_CODE}}`, but the `codex_review_failure` call site supplies `EXIT_CODE=$exit_code` rather than `CODEX_EXIT_CODE` at `hooks/loop-codex-stop-hook.sh:1226-1231`. Under the template loader, missing variables remain unreplaced, so this template may display literal `{{CODEX_EXIT_CODE}}` in that call path unless a different caller supplies it. The call site also supplies `CODEX_LOG_FILE`, while the template asks for `CODEX_STDERR_FILE`; that means the debug stderr line can remain literal in this path. Separate inline `codex_failure_exit` paths at `hooks/loop-codex-stop-hook.sh:1292-1318` do not use this template.
- validation_or_tests: Template loading/rendering behavior is covered generally by `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness template suites. Template reference existence is checked by `tests/test-template-references.sh`. I did not find a targeted assertion for this template’s specific placeholder names, so the variable mismatch above is a residual test gap.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Blocking prompt/message template for write attempts that place a round summary outside the active RLCR loop directory. It is part of the hook-enforced file-location contract for summary artifacts.
- algorithmic_behavior: The file is a tiny Markdown template: title, rule statement, and `{{CORRECT_PATH}}` placeholder at `prompt-template/block/wrong-summary-location.md:1-5`. It is rendered when the write validator detects a summary file path that is not inside `.humanize/rlcr/<active-session>/`.
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` at `hooks/loop-write-validator.sh:202-204`. Output is a stderr block followed by hook exit code 2, which blocks the tool action under Claude Code hook semantics at `hooks/loop-write-validator.sh:207-209`.
- gates_or_invariants: The gate condition is `IS_SUMMARY_FILE == true` and `IN_HUMANIZE_LOOP_DIR == false` at `hooks/loop-write-validator.sh:202`. If triggered, the hook renders `block/wrong-summary-location.md` and exits before later round-number or directory-path validation. Follow-on summary invariants include correct round number at `hooks/loop-write-validator.sh:228-245` and exact loop directory path at `hooks/loop-write-validator.sh:252-263`.
- dependencies_and_callers: Depends on `load_and_render_safe` from `hooks/lib/template-loader.sh`, with fallback inline text at `hooks/loop-write-validator.sh:204-208`. The template loader’s safe mode means missing/empty template files degrade to the fallback while still substituting `CORRECT_PATH` at `hooks/lib/template-loader.sh:170-203`.
- edge_cases_or_failure_modes: This template only covers writes outside the loop dir. If the path is inside `.humanize/rlcr` but wrong round or wrong subdirectory, different templates handle it. Because it only receives `CORRECT_PATH`, it deliberately omits the attempted path; richer diagnostics are left to `wrong-directory-path.md` and `wrong-round-number.md`.
- validation_or_tests: General template rendering and fallback behavior is covered by `tests/test-template-loader.sh` and comprehensive template tests. Hook path behavior is exercised through write/plan-file hook tests and robustness hook-system tests, especially cases around summary files, wrong locations, and state-protected paths.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-136 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production plan-file validation in `scripts/setup-rlcr-loop.sh`. It focuses on file content and filesystem edge cases that could otherwise let weak, unsafe, or unstable plan inputs seed an RLCR loop.
- algorithmic_behavior: The script sets strict Bash mode at `tests/robustness/test-plan-file-robustness.sh:13`, resolves `SCRIPT_DIR`/`PROJECT_ROOT` at `tests/robustness/test-plan-file-robustness.sh:15-16`, creates a temp workspace at `tests/robustness/test-plan-file-robustness.sh:19-24`, installs a mock `codex` binary at `tests/robustness/test-plan-file-robustness.sh:27-40`, and defines `test_plan_validation` as the production call adapter at `tests/robustness/test-plan-file-robustness.sh:46-106`. That adapter runs `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"` at `tests/robustness/test-plan-file-robustness.sh:61`, then classifies output as rejected if it contains plan-validation errors at `tests/robustness/test-plan-file-robustness.sh:64-68` or accepted if it reaches later setup/codex/gitignore/loop-created states at `tests/robustness/test-plan-file-robustness.sh:70-95`.
- inputs_outputs_state: Inputs are generated plan files under a temporary Git repo initialized at `tests/robustness/test-plan-file-robustness.sh:112-120`. Outputs are pass/fail counters via `tests/test-helpers.sh`, plus transient `.humanize/rlcr` loop directories created by production setup if validation succeeds. Local helper `count_content_lines` mirrors production content-line semantics, skipping blanks, single-line HTML comments, multiline HTML comments, and `#` comment lines at `tests/robustness/test-plan-file-robustness.sh:127-167`.
- gates_or_invariants: The script exercises the production invariants implemented in `scripts/setup-rlcr-loop.sh`: relative path only, no spaces, no shell metacharacters, no symlink plan file or symlink parent segments, file exists/readable, resolved path stays under project root, Git tracking policy, at least 5 total lines, and at least 3 meaningful content lines. Those production checks are at `scripts/setup-rlcr-loop.sh:320-536`, with the line-count gate at `scripts/setup-rlcr-loop.sh:483-491` and content-line gate at `scripts/setup-rlcr-loop.sh:493-536`.
- dependencies_and_callers: Depends on Bash, Git, `wc`, `grep`, `mktemp`, `chmod`, `ln`, `seq`, `date`, the test helpers, production `scripts/setup-rlcr-loop.sh`, and the mock `codex` inserted at the front of `PATH`. It is invoked by the default test runner at `tests/run-all-tests.sh:63`.
- edge_cases_or_failure_modes: Positive tests cover a valid plan, minimum three content lines after comments, standard-size file, and rich Markdown at `tests/robustness/test-plan-file-robustness.sh:176-288`. Negative/edge tests cover empty file and comments-only rejection at `tests/robustness/test-plan-file-robustness.sh:298-321`; 1MB+ file acceptance and SIGPIPE regression at `tests/robustness/test-plan-file-robustness.sh:323-351`; mixed CRLF/LF at `tests/robustness/test-plan-file-robustness.sh:353-364`; binary and null bytes at `tests/robustness/test-plan-file-robustness.sh:366-386` and `tests/robustness/test-plan-file-robustness.sh:512-523`; very long lines at `tests/robustness/test-plan-file-robustness.sh:388-405`; shell-special text content at `tests/robustness/test-plan-file-robustness.sh:407-428`; whitespace-only and complex/nested HTML comments at `tests/robustness/test-plan-file-robustness.sh:430-464`; missing/unreadable/symlink/directory inputs at `tests/robustness/test-plan-file-robustness.sh:466-510`; and disappearance before validation at `tests/robustness/test-plan-file-robustness.sh:525-549`.
- validation_or_tests: The file is an executable test suite and ends with `print_test_summary "Plan File Robustness Test Summary"` plus `exit $?` at `tests/robustness/test-plan-file-robustness.sh:551-556`. I did not run it because the instruction was research-only and the export is read-only. Static inspection shows it does call production setup rather than only duplicating validation locally, which makes it a meaningful regression gate.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, one per assigned row, with no merged topics and no repeated item headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`