# agent_26 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook implementation for the RLCR loop. It intercepts Claude exit attempts, validates loop state and work hygiene, runs Codex review, and either allows final completion by moving into finalize/completed state or blocks exit with the next prompt/review feedback. The branch-relevant algorithm is the Codex execution mode switch at `hooks/loop-codex-stop-hook.sh:891` through `hooks/loop-codex-stop-hook.sh:906`.
- algorithmic_behavior: The hook reads JSON hook input from stdin, locates the active loop under `.humanize/rlcr`, detects normal versus finalize phase, parses YAML frontmatter state, then runs a sequence of fail-closed gates before invoking Codex. It validates required state fields and numeric values at `hooks/loop-codex-stop-hook.sh:88` through `hooks/loop-codex-stop-hook.sh:156`, blocks outdated state schemas at `hooks/loop-codex-stop-hook.sh:163` through `hooks/loop-codex-stop-hook.sh:208`, and warns/defaults missing `full_review_round` at `hooks/loop-codex-stop-hook.sh:216` through `hooks/loop-codex-stop-hook.sh:220`. It enforces branch consistency at `hooks/loop-codex-stop-hook.sh:227` through `hooks/loop-codex-stop-hook.sh:254`, plan-file immutability at `hooks/loop-codex-stop-hook.sh:263` through `hooks/loop-codex-stop-hook.sh:337`, incomplete todo blocking at `hooks/loop-codex-stop-hook.sh:345` through `hooks/loop-codex-stop-hook.sh:394`, Git status/large file/clean state gates at `hooks/loop-codex-stop-hook.sh:403` through `hooks/loop-codex-stop-hook.sh:603`, summary-file presence at `hooks/loop-codex-stop-hook.sh:609` through `hooks/loop-codex-stop-hook.sh:641`, and round-0 goal tracker initialization at `hooks/loop-codex-stop-hook.sh:647` through `hooks/loop-codex-stop-hook.sh:719`. After gates pass, it builds either the full-alignment or regular review prompt at `hooks/loop-codex-stop-hook.sh:761` through `hooks/loop-codex-stop-hook.sh:847`. For implementation-phase review, it runs `codex exec` at `hooks/loop-codex-stop-hook.sh:1281` through `hooks/loop-codex-stop-hook.sh:1282`; for review phase, it runs `codex review --base` through `run_codex_code_review` at `hooks/loop-codex-stop-hook.sh:925` through `hooks/loop-codex-stop-hook.sh:993`.
- inputs_outputs_state: Inputs include `HOOK_INPUT` from stdin, `CLAUDE_PROJECT_DIR` or current directory as `PROJECT_ROOT`, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `round-N-summary.md` or `finalize-summary.md`, the plan backup, Git repository status, prompt templates, Codex CLI availability, `CODEX_MODEL`, `CODEX_EFFORT`, `CODEX_TIMEOUT`, and the branch-specific environment variable `HUMANIZE_CODEX_BYPASS_SANDBOX`. Outputs are Claude hook JSON decisions with `"decision": "block"` for retry/next-action states, generated prompt/review files under `.humanize/rlcr/<timestamp>/`, audit/debug command and log files under `XDG_CACHE_HOME/humanize/...` at `hooks/loop-codex-stop-hook.sh:877` through `hooks/loop-codex-stop-hook.sh:887`, and renamed state files such as `finalize-state.md`, `complete-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`. State transitions include normal `state.md` to `finalize-state.md` in `enter_finalize_phase` at `hooks/loop-codex-stop-hook.sh:1041` through `hooks/loop-codex-stop-hook.sh:1123`, finalize to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:742` through `hooks/loop-codex-stop-hook.sh:747`, implementation COMPLETE to review phase by setting `review_started: true` and touching `.review-phase-started` at `hooks/loop-codex-stop-hook.sh:1428` through `hooks/loop-codex-stop-hook.sh:1437`, review issues to next round at `hooks/loop-codex-stop-hook.sh:1127` through `hooks/loop-codex-stop-hook.sh:1169`, and implementation issues to next round at `hooks/loop-codex-stop-hook.sh:1514` through `hooks/loop-codex-stop-hook.sh:1579`.
- gates_or_invariants: The hook is intentionally fail-closed on malformed state, branch mismatch, plan-file mutation, incomplete todos, failed Git status, large changed source/docs files over 2000 lines, dirty worktree, missing summary, uninitialized goal tracker, missing Codex CLI, failed Codex execution, missing/empty Codex review result, failed code review command, missing review phase marker, and max iteration exhaustion. The branch-specific invariant is: default Codex execution remains sandboxed via `--full-auto`, and only exact env values `true` or `1` switch to `--dangerously-bypass-approvals-and-sandbox` at `hooks/loop-codex-stop-hook.sh:901` through `hooks/loop-codex-stop-hook.sh:903`; that flag is appended to `CODEX_EXEC_ARGS` before `-C "$PROJECT_ROOT"` at `hooks/loop-codex-stop-hook.sh:906`. `codex review` does not receive this automation flag and intentionally uses a different arg shape at `hooks/loop-codex-stop-hook.sh:908` through `hooks/loop-codex-stop-hook.sh:914`.
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh` for markers, active loop discovery, state parsing, template loader setup, review issue detection, and `end_loop`; `scripts/portable-timeout.sh` for `run_with_timeout`; `hooks/check-todos-from-transcript.py` for todo completion gating; `prompt-template/codex/full-alignment-review.md`, `prompt-template/codex/regular-review.md`, `prompt-template/codex/goal-tracker-update-section.md`, `prompt-template/codex/code-review-phase.md`, `prompt-template/claude/*`, and `prompt-template/block/*`; shell tools `jq`, `git`, `diff`, `awk`, `sed`, `grep`, `wc`; and Codex CLI. Related helper evidence: `detect_review_issues` scans the combined Codex review log for `[P0-9]` markers near line starts in `hooks/lib/loop-common.sh:332` through `hooks/lib/loop-common.sh:390`; `end_loop` renames state files according to terminal reason in `hooks/lib/loop-common.sh:1112` through `hooks/lib/loop-common.sh:1145`; `run_with_timeout` selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback in `scripts/portable-timeout.sh:9` through `scripts/portable-timeout.sh:71`.
- edge_cases_or_failure_modes: Active loop discovery can return no loop and allow exit, so no state means no RLCR enforcement at `hooks/loop-codex-stop-hook.sh:59` through `hooks/loop-codex-stop-hook.sh:64`. A missing state file after loop discovery also allows exit at `hooks/loop-codex-stop-hook.sh:76` through `hooks/loop-codex-stop-hook.sh:82`. Missing `current_round` or `max_iterations` is treated as possible truncation and terminates unexpectedly, while invalid `max_iterations` falls back to 42. `full_review_round` below 2 or nonnumeric defaults to 5 at `hooks/loop-codex-stop-hook.sh:775` through `hooks/loop-codex-stop-hook.sh:779`. Review phase is protected against manual state edits by requiring `.review-phase-started` at `hooks/loop-codex-stop-hook.sh:1455` through `hooks/loop-codex-stop-hook.sh:1471`. Codex output written to stdout instead of the review result file is copied into place at `hooks/loop-codex-stop-hook.sh:1339` through `hooks/loop-codex-stop-hook.sh:1355`; if no result exists or it is empty, exit is blocked. STOP is honored as a circuit breaker, expected on full-alignment rounds but still terminating on non-alignment rounds at `hooks/loop-codex-stop-hook.sh:1480` through `hooks/loop-codex-stop-hook.sh:1507`.
- validation_or_tests: I did not execute tests because this assignment requested research notes only in a read-only branch export. Static validation evidence includes direct README contract lines for `HUMANIZE_CODEX_BYPASS_SANDBOX` at `README.md:60` through `README.md:93`, which match the hook implementation at `hooks/loop-codex-stop-hook.sh:899` through `hooks/loop-codex-stop-hook.sh:906`. No assigned test directly exercises the bypass env var; the assigned executable spec covers `.humanize` escape protections used by sibling hook validators.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable regression specification for RLCR escape-prevention behavior around `.humanize` staging and shell-safe directory scanning. It validates `git_adds_humanize` from `hooks/lib/loop-common.sh`, plus find-based iteration patterns that avoid zsh glob failures.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` at `tests/test-humanize-escape.sh:16` through `tests/test-humanize-escape.sh:18`, defines pass/fail counters, and uses `assert_blocks`/`assert_allows` to lower-case commands and call `git_adds_humanize` at `tests/test-humanize-escape.sh:44` through `tests/test-humanize-escape.sh:70`. It asserts blocking for direct `.humanize` paths, path prefixes, nested paths, quoted paths, force flags, force-plus-broad-scope commands, `-A`/`--all` when `.humanize` exists, chained commands, and `git -C`/`--git-dir` forms at `tests/test-humanize-escape.sh:83` through `tests/test-humanize-escape.sh:166`. It asserts allowed commands for ordinary files, non-add Git operations touching `.humanize`, patch mode, and lookalike names such as `.humanizeconfig` at `tests/test-humanize-escape.sh:175` through `tests/test-humanize-escape.sh:192`. It then validates shell-safe directory scans using `find` instead of fragile globs for empty directories, dotfile-only directories, state-file matching, and timestamped session directory detection at `tests/test-humanize-escape.sh:204` through `tests/test-humanize-escape.sh:332`.
- inputs_outputs_state: Inputs are hardcoded shell command strings, temporary directories under `/tmp/test-humanize-*`, the sourced `git_adds_humanize` function, and the current working directory for tests that require `.humanize` existence. Outputs are colored PASS/FAIL lines and a final process exit code: zero if `TESTS_FAILED` is zero, one otherwise at `tests/test-humanize-escape.sh:337` through `tests/test-humanize-escape.sh:352`. The only transient state is `/tmp` test folders and counter variables `TESTS_PASSED`/`TESTS_FAILED`; each test cleans up its temp directory.
- gates_or_invariants: The main invariant is that local `.humanize` loop state must not be added to version control by direct path, quote/path variants, force-add, all-add, chained command, or Git option variants. The paired invariant is that unrelated paths, patch mode, and `.humanize` lookalike filenames must not be over-blocked. The zsh-safety invariant is that empty or dotfile-only directories should iterate as zero results without shell glob expansion errors, and active session discovery should consider only timestamp-shaped directories.
- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh`. The behavior under test is implemented in `git_adds_humanize` at `hooks/lib/loop-common.sh:908` through `hooks/lib/loop-common.sh:1026`, which is called by `hooks/loop-bash-validator.sh:115` through `hooks/loop-bash-validator.sh:118` to block Bash `git add` commands that would stage `.humanize`. The helper splits chained shell segments, recognizes `git ... add`, strips quotes, detects `.humanize` path boundaries, handles `--force`/`-f`, lowercased `-A` as `-a`, broad `.`/`*` scopes, and checks `git check-ignore` for non-force broad adds.
- edge_cases_or_failure_modes: The test intentionally covers `.humanize` as `./.humanize`, nested `path/to/.humanize`, parent-relative paths, quoted paths, force flags combined with broad scope, combined flags like `-fA`/`-Af`, lowercased all flags, command chains separated by `&&` or `;`, and `git -C` or `--git-dir` options. It also guards false positives for `.humanizeconfig`, `.humanize-backup`, and `.humanizerc`. A notable edge is that `git_adds_humanize` blocks `-A`/`--all` only when a `.humanize` directory exists, so the test creates such a directory at `tests/test-humanize-escape.sh:128` through `tests/test-humanize-escape.sh:144`.
- validation_or_tests: This file is itself the validator. It was inspected but not run in this read-only research pass. Its expected validation result is the final "All tests passed!" path when no fail counter increments at `tests/test-humanize-escape.sh:344` through `tests/test-humanize-escape.sh:348`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the goal-tracker protection gate after round 0. It defines the review contract that Claude cannot mutate `goal-tracker.md` directly and must instead submit a structured update request in the round summary for Codex to evaluate.
- algorithmic_behavior: The template renders a round-specific block title with `{{CURRENT_ROUND}}`, states that after round 0 only Codex can modify the Goal Tracker, forbids Write/Edit/Bash mutation of `goal-tracker.md`, and provides the required "Goal Tracker Update Request" format in `{{SUMMARY_FILE}}` at `prompt-template/block/goal-tracker-modification.md:1` through `prompt-template/block/goal-tracker-modification.md:25`.
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `SUMMARY_FILE`, supplied by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1100` through `hooks/lib/loop-common.sh:1109`. Output is a rendered Markdown block returned to validators and emitted to stderr with exit code 2 when direct modification is blocked. It does not mutate state itself; it redirects user behavior into summary-file evidence that Codex can later review and apply.
- gates_or_invariants: The invariant is custody of `goal-tracker.md`: round 0 initialization can happen through normal guarded workflows, but after round 0 Claude must not directly edit the goal tracker. The requested-change format requires both a concrete list of requested changes and a justification, which supports auditability and prevents silent goal drift.
- dependencies_and_callers: Rendered through `load_and_render_safe` via `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1107` through `hooks/lib/loop-common.sh:1109`. Called by Write validation at `hooks/loop-write-validator.sh:192` through `hooks/loop-write-validator.sh:195`, Edit validation at `hooks/loop-edit-validator.sh:145` through `hooks/loop-edit-validator.sh:148`, and Bash validation at `hooks/loop-bash-validator.sh:349` through `hooks/loop-bash-validator.sh:357`. Template existence is cross-checked in `tests/test-template-references.sh:152` through `tests/test-template-references.sh:159`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to a shorter inline message in `hooks/lib/loop-common.sh:1103` through `hooks/lib/loop-common.sh:1105`. Bash detection depends on `command_modifies_file`, which covers redirection, tee, in-place sed/awk/perl, mv/cp, rm, dd, truncate, printf redirection, and exec redirection patterns at `hooks/lib/loop-common.sh:1068` through `hooks/lib/loop-common.sh:1096`. The template assumes the caller passes the current round summary path; wrong/missing summary path would make the instructions less actionable but still preserve the direct-edit prohibition.
- validation_or_tests: Static reference validation includes `tests/test-template-references.sh:152` through `tests/test-template-references.sh:159`. Runtime enforcement is covered by the three validators listed above, each blocking after `CURRENT_ROUND` is greater than zero or for Bash command modification patterns.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-116 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Full-alignment Codex review template used at configurable RLCR checkpoint intervals. It defines the high-level audit algorithm for checking implementation progress against the original plan, goal tracker, acceptance criteria, historical round evidence, and stagnation circuit breaker.
- algorithmic_behavior: The template instructs Codex to read the original plan first at `prompt-template/codex/full-alignment-review.md:5` through `prompt-template/codex/full-alignment-review.md:10`, inspect Claude's summary at `prompt-template/codex/full-alignment-review.md:13` through `prompt-template/codex/full-alignment-review.md:16`, audit the goal tracker and acceptance criteria at `prompt-template/codex/full-alignment-review.md:19` through `prompt-template/codex/full-alignment-review.md:48`, review implementation reality against claims at `prompt-template/codex/full-alignment-review.md:50` through `prompt-template/codex/full-alignment-review.md:55`, include the reusable goal-tracker update section at `prompt-template/codex/full-alignment-review.md:57`, check historical progress/stagnation at `prompt-template/codex/full-alignment-review.md:59` through `prompt-template/codex/full-alignment-review.md:84`, and enforce output requirements at `prompt-template/codex/full-alignment-review.md:86` through `prompt-template/codex/full-alignment-review.md:93`.
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE`. These are passed by the stop hook when `FULL_ALIGNMENT_CHECK` is true at `hooks/loop-codex-stop-hook.sh:816` through `hooks/loop-codex-stop-hook.sh:829`. Output is a rendered review prompt saved as `round-N-review-prompt.md`; Codex is expected to write findings to `round-N-review-result.md`, ending with STOP for stagnation or COMPLETE only when all original plan acceptance criteria are fully met with no deferrals.
- gates_or_invariants: The template makes full alignment stricter than regular review: every acceptance criterion in the immutable section must be audited; forgotten tasks, unverified summary claims, and deferred items must be surfaced; deferred acceptance criteria are incomplete; STOP is the stagnation circuit breaker; COMPLETE is valid only when all original plan tasks and acceptance criteria are complete with no deferrals. The hook schedules this template every `FULL_REVIEW_ROUND` interval using modulo logic at `hooks/loop-codex-stop-hook.sh:773` through `hooks/loop-codex-stop-hook.sh:783`.
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe`. It embeds `GOAL_TRACKER_UPDATE_SECTION`, which is built from `prompt-template/codex/goal-tracker-update-section.md` or fallback at `hooks/loop-codex-stop-hook.sh:767` through `hooks/loop-codex-stop-hook.sh:771`. It depends on template rendering semantics in `hooks/lib/template-loader.sh`, especially single-pass substitution to avoid accidental placeholder injection at `hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:132`. It coordinates with stop-hook handling of markers: COMPLETE transitions implementation to review/finalize flow at `hooks/loop-codex-stop-hook.sh:1403` through `hooks/loop-codex-stop-hook.sh:1444`; STOP triggers loop termination at `hooks/loop-codex-stop-hook.sh:1480` through `hooks/loop-codex-stop-hook.sh:1507`.
- edge_cases_or_failure_modes: If `FULL_REVIEW_ROUND` is missing, the stop hook defaults to 5 with a warning; if invalid or less than 2, it is reset to 5. Previous-round references are clamped to zero to avoid negative file names at `hooks/loop-codex-stop-hook.sh:785` through `hooks/loop-codex-stop-hook.sh:791`. The template explicitly tells Codex to use STOP only as the last line for stagnation; a STOP on a non-alignment round is still honored by the hook, but reported as unexpected. A too-permissive Codex response ending in COMPLETE despite deferred criteria would violate the template contract; the hook only detects marker text, so the template is the primary policy guard for semantic completeness.
- validation_or_tests: Static behavior is validated by template rendering through `load_and_render_safe` and by the hook’s full-alignment scheduling path. The assigned file itself is a prompt contract rather than executable code; I inspected it directly and correlated it with its hook caller.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item evidence sections above
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`