# agent_13 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`
- note: research only; no files modified. I did not run the mutating/manual tests.

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Static plan-generation template directory. Recursive inspection found one child: `prompt-template/plan/gen-plan-template.md`. It defines the canonical output shape for generated implementation plans.
- algorithmic_behavior: The template forces generated plans into: goal description (`gen-plan-template.md:3`), TDD-style acceptance criteria with positive and negative tests (`gen-plan-template.md:6-23`), upper/lower path boundaries and allowed choices (`gen-plan-template.md:25-44`), feasibility hints/references (`gen-plan-template.md:46-55`), dependency sequencing (`gen-plan-template.md:57-67`), and implementation code-style constraints (`gen-plan-template.md:69-74`).
- inputs_outputs_state: Input is an original design draft handled by the gen-plan validator/copy script, not by the template itself. `scripts/validate-gen-plan-io.sh` validates input/output arguments and permissions (`scripts/validate-gen-plan-io.sh:1-123`), locates this template via `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`scripts/validate-gen-plan-io.sh:133-140`), copies it to the requested output (`scripts/validate-gen-plan-io.sh:148-149`), then appends the original draft (`scripts/validate-gen-plan-io.sh:151-157`). The template has no persistent runtime state.
- gates_or_invariants: Generated criteria must include deterministic positive and negative tests (`gen-plan-template.md:8-22`). Path boundaries must be affirmative and may narrow to a fixed design when the draft is deterministic (`gen-plan-template.md:27-44`). Implementation code/comments must not contain plan-progress terminology such as `AC-`, `Milestone`, `Step`, or `Phase` (`gen-plan-template.md:71-74`).
- dependencies_and_callers: `scripts/validate-gen-plan-io.sh` is the concrete caller that copies this template (`scripts/validate-gen-plan-io.sh:136-149`). `commands/gen-plan.md` embeds the same command-facing schema and generation rules (`commands/gen-plan.md:142-245`). The directory coordinates with sibling `prompt-template/*` directories by serving plan creation only; block/codex/claude/pr-loop siblings handle runtime hook and loop prompts.
- edge_cases_or_failure_modes: Missing template is treated as plugin configuration failure with exit `7` (`scripts/validate-gen-plan-io.sh:142-145`). Empty input, existing output, missing output directory, and unwritable output directory fail before template copy (`scripts/validate-gen-plan-io.sh:79-123`). Deterministic designs are an explicit edge case where upper and lower bounds may converge (`gen-plan-template.md:44`).
- validation_or_tests: Gen-plan IO validation script enforces filesystem preconditions and emits structured validation messages before copying the template (`scripts/validate-gen-plan-io.sh:74-131`). `tests/run-all-tests.sh` includes `test-gen-plan.sh` in the suite (`tests/run-all-tests.sh:52`), though that specific file was outside the assigned item set.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-043 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor shutdown behavior when `.humanize/` is deleted during monitoring.
- algorithmic_behavior: Provides three commands. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99/` with a minimal `state.md` and `goal-tracker.md` (`tests/manual-monitor-test.sh:23-46`). `delete` removes `.humanize` and tells the operator what the monitor should show (`tests/manual-monitor-test.sh:47-57`). `cleanup` removes leftover `.humanize` state (`tests/manual-monitor-test.sh:58-62`). Invalid invocation prints usage and exits `1` (`tests/manual-monitor-test.sh:63-71`).
- inputs_outputs_state: Input is a positional action: `setup`, `delete`, or `cleanup` (`tests/manual-monitor-test.sh:23`). Outputs are local loop-state files under `.humanize/rlcr/...` for setup, deletion of `.humanize` for delete/cleanup, and human-readable expected-result text. The state transition under test is active loop directory present -> monitor running -> `.humanize` removed -> monitor exits cleanly.
- gates_or_invariants: The script runs with `set -euo pipefail` (`tests/manual-monitor-test.sh:17`) and always relocates to project root (`tests/manual-monitor-test.sh:19-21`). Expected monitor gates are a clean stop message, restored scroll region/cursor, and no shell glob errors (`tests/manual-monitor-test.sh:51-54`).
- dependencies_and_callers: The manual instructions require a second terminal running `source scripts/humanize.sh && humanize monitor rlcr` (`tests/manual-monitor-test.sh:10-14`, `tests/manual-monitor-test.sh:43-45`). Production monitor behavior is in `_humanize_monitor_codex`: it refuses to start without `.humanize/rlcr` (`scripts/humanize.sh:230-235`), sets/restores terminal scroll region (`scripts/humanize.sh:512-528`), cleans up tail processes and terminal state (`scripts/humanize.sh:535-560`), and prints `Monitoring stopped: ...` on graceful deletion (`scripts/humanize.sh:563-575`).
- edge_cases_or_failure_modes: It is intentionally manual, so assertions depend on operator observation. The hardcoded timestamp contains `99-99-99`; that is not a valid clock time but is lexicographically latest-looking and sufficient for session discovery. `delete` and `cleanup` use `rm -rf .humanize`, so they are destructive to local loop state by design.
- validation_or_tests: This script is itself a manual validation artifact. Automated coverage for the same monitor deletion behavior exists in `tests/robustness/test-hook-input-robustness.sh` monitor integration tests (`tests/robustness/test-hook-input-robustness.sh:444-664`) and broader real monitor e2e tests referenced by `tests/run-all-tests.sh:51`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-073 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Central shared algorithm library for RLCR and PR-loop hook validation/state management. It is sourced by read/write/edit/bash validators, setup scripts, stop hooks, and tests.
- algorithmic_behavior: Major sections: constants for state fields and exit reasons (`hooks/lib/loop-common.sh:15-44`); JSON hook-input validation, required field checks, UTF-8/null-byte rejection, and max JSON-depth guard (`hooks/lib/loop-common.sh:50-133`); template-loader initialization (`hooks/lib/loop-common.sh:139-149`); active loop discovery and state parsing (`hooks/lib/loop-common.sh:151-330`); Codex review issue extraction from logs using `[P?]` markers (`hooks/lib/loop-common.sh:332-392`); round-file/path predicates and block-message renderers (`hooks/lib/loop-common.sh:394-530`); cancel authorization parser and security gate (`hooks/lib/loop-common.sh:533-710`); PR-loop path, round, and bot helpers (`hooks/lib/loop-common.sh:712-905`); `.humanize` git-add protection (`hooks/lib/loop-common.sh:908-1066`); generic Bash file-modification detection (`hooks/lib/loop-common.sh:1068-1096`); RLCR loop termination by renaming state files (`hooks/lib/loop-common.sh:1112-1145`); PR goal-tracker update/idempotency logic (`hooks/lib/loop-common.sh:1147-1315`).
- inputs_outputs_state: Inputs include raw hook JSON, file paths, shell commands, loop base directories, `state.md`/`finalize-state.md`, Codex log paths, PR bot JSON, and tracker files. Outputs include global parsed variables such as `VALIDATED_TOOL_NAME`, `STATE_CURRENT_ROUND`, and `STATE_BASE_BRANCH`; rendered block messages; boolean shell return codes; generated review result files (`hooks/lib/loop-common.sh:381-386`); renamed terminal state files like `complete-state.md` or `cancel-state.md` (`hooks/lib/loop-common.sh:1119-1144`); and updated PR goal trackers (`hooks/lib/loop-common.sh:1253-1314`).
- gates_or_invariants: Hook JSON must be valid UTF-8 JSON with `tool_name` (`hooks/lib/loop-common.sh:59-95`), required tool-input fields must exist (`hooks/lib/loop-common.sh:101-113`), and JSON depth over 30 is rejected by callers (`hooks/lib/loop-common.sh:119-132`). Strict state files must have frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, with numeric/boolean validation (`hooks/lib/loop-common.sh:255-329`). `find_active_loop` only considers the newest directory and only if it has `state.md` or `finalize-state.md`, preventing older zombie loops (`hooks/lib/loop-common.sh:151-174`). Cancel authorization requires `.cancel-requested`, rejects command substitution, shell chaining, hidden variables, mixed quote styles, extra args, wrong source/destination, and symlink source files (`hooks/lib/loop-common.sh:551-710`). `git_adds_humanize` blocks direct, broad, all, and force-add variants that would stage `.humanize` (`hooks/lib/loop-common.sh:930-1026`).
- dependencies_and_callers: Sourced by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-edit-validator.sh`, plan validator, Codex stop hook, PR stop hook, setup scripts, and many tests. Read/write/bash validators call `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, `find_active_loop`, and `parse_state_file_strict` (`hooks/loop-read-validator.sh:27-99`, `hooks/loop-write-validator.sh:27-145`, `hooks/loop-bash-validator.sh:24-86`). Bash validator additionally relies on `git_adds_humanize`, `is_cancel_authorized`, and `command_modifies_file` (`hooks/loop-bash-validator.sh:115-393`). `hooks/loop-codex-stop-hook.sh` calls `end_loop` and `detect_review_issues`; `hooks/pr-loop-stop-hook.sh` calls `update_pr_goal_tracker`.
- edge_cases_or_failure_modes: Non-UTF8 is rejected when `iconv` is available; minimal systems without `iconv` skip that check but still rely on `jq` (`hooks/lib/loop-common.sh:69-83`). Null-byte detection is attempted but Bash command substitution can strip nulls before functions see them, which tests account for. `find_active_loop` uses lexicographic newest directory, so malformed timestamp names can influence active-loop selection if they sort latest. Strict state parsing allows negative numeric values syntactically (`hooks/lib/loop-common.sh:307-317`), leaving semantic bounds to callers. `command_modifies_file` is regex-based and broad by design; it covers common write forms but is not a full shell parser (`hooks/lib/loop-common.sh:1071-1096`).
- validation_or_tests: `tests/robustness/test-hook-input-robustness.sh` directly exercises hook-input parsing, depth/UTF-8 behavior, and `command_modifies_file` (`tests/robustness/test-hook-input-robustness.sh:35-236`, `tests/robustness/test-hook-input-robustness.sh:351-369`). Additional referenced suites cover state parsing, session discovery, cancel authorization, command patterns, git-add protection, Codex review issue extraction, and PR goal tracker updates.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-103 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-response template for the read validator when an agent tries to read RLCR round prompt/summary files from outside the active loop directory.
- algorithmic_behavior: Renders a short correction: the requested `{{FILE_PATH}}` is outside the loop location, active loop files are under `{{ACTIVE_LOOP_DIR}}/`, and the current prompt/summary files are `round-{{CURRENT_ROUND}}-prompt.md` and `round-{{CURRENT_ROUND}}-summary.md` (`prompt-template/block/wrong-file-location.md:1-9`).
- inputs_outputs_state: Inputs are template variables `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Output is Markdown emitted to stderr by hook validators through `load_and_render_safe`. It has no persistent state.
- gates_or_invariants: The invariant is location correctness: round prompt/summary reads must target the active `.humanize/rlcr/<session>/` directory, not an arbitrary same-basename file. It reinforces current-round access by displaying only the current prompt and summary paths (`prompt-template/block/wrong-file-location.md:5-7`).
- dependencies_and_callers: `hooks/loop-read-validator.sh` uses this template when a `Read` target is a round summary/prompt file and `is_in_humanize_loop_dir` is false (`hooks/loop-read-validator.sh:118-131`). Rendering is provided by `hooks/lib/template-loader.sh`, whose single-pass substitution preserves unresolved placeholders and avoids recursive placeholder injection (`hooks/lib/template-loader.sh:50-131`).
- edge_cases_or_failure_modes: The final line says `If you need this file, use: cat {{FILE_PATH}}` (`prompt-template/block/wrong-file-location.md:9`). That gives an escape hatch for reading the exact requested file via shell, while the hook’s algorithm still blocks the structured `Read` tool for stale/wrong-location round files. If a variable is missing, the template loader leaves the placeholder unchanged by design (`hooks/lib/template-loader.sh:11-13`, `hooks/lib/template-loader.sh:116-122`).
- validation_or_tests: Comprehensive template tests are referenced in the suite, including `tests/test-templates-comprehensive.sh` and template-loader tests. Read-validator behavior around wrong locations is covered by path/allowlist validator suites referenced by searches, especially `tests/test-allowlist-validators.sh` and robustness hook-system tests.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-133 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Automated robustness specification for hook JSON parsing, Bash command-detection helpers, and monitor edge cases.
- algorithmic_behavior: Sources production `hooks/lib/loop-common.sh` and shared test helpers (`tests/robustness/test-hook-input-robustness.sh:14-19`), then runs grouped tests. Hook input tests pipe JSON into actual read/write/bash validators (`tests/robustness/test-hook-input-robustness.sh:35-236`). Monitor edge tests exercise terminal width, log growth/deletion, ANSI stripping, binary logs, narrow/wide layouts, and rapid updates (`tests/robustness/test-hook-input-robustness.sh:245-341`). Command pattern tests call `command_modifies_file` for `sed -i` and redirection detection (`tests/robustness/test-hook-input-robustness.sh:351-369`). Monitor helper/integration tests source `scripts/humanize.sh`, call parser functions, and invoke real `_humanize_monitor_codex` under fake terminal/cache conditions (`tests/robustness/test-hook-input-robustness.sh:381-664`).
- inputs_outputs_state: Inputs are synthetic JSON hook payloads, temporary loop directories, fake HOME/XDG cache directories, mock logs, and shimmed `tput`/`clear` functions. Outputs are pass/fail counters via `tests/test-helpers.sh`, subprocess exit codes, monitor output strings, temporary logs, and a final summary/exit status (`tests/robustness/test-hook-input-robustness.sh:670-671`).
- gates_or_invariants: Valid JSON for matching tools should pass for non-loop paths (`tests/robustness/test-hook-input-robustness.sh:35-69`). Invalid JSON, missing `tool_name`, missing required fields, deep nesting, and non-UTF8 binary input must reject without signal crashes (`tests/robustness/test-hook-input-robustness.sh:71-217`). Unknown tool names should pass through (`tests/robustness/test-hook-input-robustness.sh:163-175`). Null-byte cases must not crash because Bash may strip nulls before validators see them (`tests/robustness/test-hook-input-robustness.sh:219-235`). Monitor integration gates accept exit codes below `128`, proving no signal crash in no-session, narrow-terminal, and ANSI-log scenarios (`tests/robustness/test-hook-input-robustness.sh:476-486`, `tests/robustness/test-hook-input-robustness.sh:564-578`, `tests/robustness/test-hook-input-robustness.sh:651-664`).
- dependencies_and_callers: Depends on `jq`, production validators `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`, and `tests/test-helpers.sh`. It is part of the aggregate test list in `tests/run-all-tests.sh:55-62`.
- edge_cases_or_failure_modes: Long 10KB command payloads are expected to pass (`tests/robustness/test-hook-input-robustness.sh:117-133`). Unicode paths are expected to pass (`tests/robustness/test-hook-input-robustness.sh:149-161`). Deep nesting over max depth is expected to reject (`tests/robustness/test-hook-input-robustness.sh:177-199`). Binary log files and ANSI logs are treated as display robustness cases rather than semantic parser failures (`tests/robustness/test-hook-input-robustness.sh:280-303`). Monitor tests simulate deletion of `.humanize/rlcr` after startup to force graceful exit (`tests/robustness/test-hook-input-robustness.sh:531-560`, `tests/robustness/test-hook-input-robustness.sh:621-647`).
- validation_or_tests: This file is itself a validation suite. I inspected it but did not execute it because the task requested research notes only and the environment is a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence headings present; item IDs are not repeated here to preserve the exactly-once output constraint`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`