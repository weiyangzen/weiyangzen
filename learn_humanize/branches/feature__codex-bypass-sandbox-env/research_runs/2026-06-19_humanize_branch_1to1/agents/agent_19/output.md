# agent_19 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow wrapper for canceling an active RLCR loop. It delegates mutation to `scripts/cancel-rlcr-loop.sh` and defines the user-facing decision protocol for normal vs Finalize Phase cancellation.
- algorithmic_behavior: The command first runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` (`commands/cancel-rlcr-loop.md:11-15`), then dispatches on the first output line: `NO_LOOP`/`NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` (`commands/cancel-rlcr-loop.md:17-22`). For `FINALIZE_NEEDS_CONFIRM`, it asks the user whether to force cancel; yes runs the same script with `--force`, no leaves Finalize Phase running (`commands/cancel-rlcr-loop.md:23-34`).
- inputs_outputs_state: Inputs are the current RLCR state in `.humanize/rlcr/<newest-loop>/state.md` or `finalize-state.md`, plus an optional user confirmation. Outputs are a human-readable report and, through the script, `.cancel-requested` plus `cancel-state.md`. The referenced script locates the newest loop directory, distinguishes `state.md` from `finalize-state.md`, emits `FINALIZE_NEEDS_CONFIRM` without `--force`, and renames the active state file to `cancel-state.md` after touching `.cancel-requested` (`scripts/cancel-rlcr-loop.sh:69-78`, `scripts/cancel-rlcr-loop.sh:89-99`, `scripts/cancel-rlcr-loop.sh:117-138`).
- gates_or_invariants: The command’s allowed tools are narrowly limited to the cancel script, the forced variant, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-4`). Its key invariant is that the script owns all cancellation logic; an active loop is defined only by `state.md` or `finalize-state.md` in the newest loop directory (`commands/cancel-rlcr-loop.md:36`). Finalize Phase has an explicit confirmation gate before destructive state rename (`commands/cancel-rlcr-loop.md:21-31`).
- dependencies_and_callers: Depends directly on `scripts/cancel-rlcr-loop.sh`. That script’s status tokens are the protocol consumed by this command (`scripts/cancel-rlcr-loop.sh:75-78`, `scripts/cancel-rlcr-loop.sh:96-98`, `scripts/cancel-rlcr-loop.sh:117-127`, `scripts/cancel-rlcr-loop.sh:144-151`). Related setup and hook text point users to `/humanize:cancel-rlcr-loop` when loop state is invalid or active (`scripts/setup-rlcr-loop.sh:103`, `hooks/loop-plan-file-validator.sh:62`, `hooks/loop-codex-stop-hook.sh:1216`).
- edge_cases_or_failure_modes: If no loop base exists, the script returns `NO_LOOP`; if a newest loop directory exists without `state.md` or `finalize-state.md`, it returns `NO_ACTIVE_LOOP` (`scripts/cancel-rlcr-loop.sh:72-79`, `scripts/cancel-rlcr-loop.sh:89-99`). Finalize Phase without force exits with code 2 and does not rename state (`scripts/cancel-rlcr-loop.sh:117-127`). Unknown script flags exit as an error (`scripts/cancel-rlcr-loop.sh:57-60`). The command does not parse multi-line output beyond first-token branching, so the script’s first line is a stable API.
- validation_or_tests: No direct test file was assigned for this command, but the behavior is indirectly supported by hook/common logic that treats `finalize-state.md` as an active loop marker (`hooks/lib/loop-common.sh:154-169` from search results) and by bash-validator cancel authorization rules that protect manual state moves except authorized cancel operations (`hooks/lib/loop-common.sh:533-710`, `hooks/loop-bash-validator.sh:131-154` from search results).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for `command_modifies_file` in `hooks/lib/loop-common.sh`; it defines which Bash command forms count as protected file writes and which read-only forms must not be blocked.
- algorithmic_behavior: The test lowercases each command via `to_lower`, calls `command_modifies_file "$command_lower" "$pattern"`, and increments pass/fail counters (`tests/test-bash-validator-patterns.sh:36-62`). It covers redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec`, false positives, and alternate target regexes for `state.md` and `round-N-summary.md` (`tests/test-bash-validator-patterns.sh:69-194`).
- inputs_outputs_state: Inputs are command strings plus a regex target pattern, defaulting to `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:37-43`, `tests/test-bash-validator-patterns.sh:50-58`). Outputs are console PASS/FAIL lines and process exit 0 only when `TESTS_FAILED` is zero (`tests/test-bash-validator-patterns.sh:197-214`). It mutates only in-memory counters.
- gates_or_invariants: The central invariant is balanced detection: write-like commands targeting protected file names must match, while plain reads such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` must not (`tests/test-bash-validator-patterns.sh:75-133`, `tests/test-bash-validator-patterns.sh:142-152`). Case-insensitivity is enforced by lowercasing, including `ECHO X > GOAL-TRACKER.MD` (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:80`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`tests/test-bash-validator-patterns.sh:11-13`). The tested function contains the actual regex list for redirection, `tee`, in-place edits, move/copy/remove, `dd`, `truncate`, `printf`, and `exec` (`hooks/lib/loop-common.sh:1068-1096`). Callers include `hooks/loop-bash-validator.sh`, which uses the helper for `finalize-state.md`, `state.md`, RLCR `plan.md`, `goal-tracker.md`, prompts, summaries, todos, and PR-loop protected files (`hooks/loop-bash-validator.sh:139-154`, `hooks/loop-bash-validator.sh:334-385`, `hooks/loop-bash-validator.sh:407-438` from search results).
- edge_cases_or_failure_modes: Known limitation is documented: multi-source `cp file1 file2 goal-tracker.md` is not detected because the regex expects `cp src dest` shape (`tests/test-bash-validator-patterns.sh:165-168`). Variable targets such as `echo x > $FILE` are intentionally not matched because the literal protected filename is absent (`tests/test-bash-validator-patterns.sh:170-172`). The function is regex-based and does not shell-parse quotes or expansions, so its safety depends on conservative coverage in the bash validator around it.
- validation_or_tests: This file is itself the validation. It fails closed via nonzero exit if any expectation fails (`tests/test-bash-validator-patterns.sh:206-214`). It also validates alternate target patterns for loop state and generated summary files (`tests/test-bash-validator-patterns.sh:175-194`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Block-message template for the `.humanize/` commit-protection gate. It explains why broad or direct `git add` commands are rejected when they could stage local loop state.
- algorithmic_behavior: The template presents allowed narrow staging commands and blocked broad/direct staging forms (`prompt-template/block/git-add-humanize.md:8-25`). It is loaded by `git_add_humanize_blocked_message`, with an embedded fallback mirroring the same content (`hooks/lib/loop-common.sh:1028-1065`).
- inputs_outputs_state: Inputs are implicit: a blocked Bash command detected by `git_adds_humanize`. Output is the rendered markdown warning printed by `hooks/loop-bash-validator.sh` when `git_adds_humanize "$COMMAND_LOWER"` returns true (`hooks/loop-bash-validator.sh:115-116` from search results). No state is mutated by the template itself.
- gates_or_invariants: The enforcement invariant is that `.humanize/` contains local loop state and should not be committed (`prompt-template/block/git-add-humanize.md:3-6`). Allowed commands require specific paths or patch mode; blocked examples include direct `.humanize`, `git add -A`, `git add --all`, `git add .`, and forced broad add (`prompt-template/block/git-add-humanize.md:8-25`). A special invariant warns that a commit message adding ignore rules must not contain the literal `.humanize`, to avoid triggering protection (`prompt-template/block/git-add-humanize.md:26-34`).
- dependencies_and_callers: The actual detector is `git_adds_humanize` in `hooks/lib/loop-common.sh`, which splits chained commands, recognizes `git ... add`, strips quote characters for path matching, detects direct `.humanize`, force/all/broad flags, checks whether `.humanize` exists, and consults `git check-ignore` for broad non-force adds (`hooks/lib/loop-common.sh:908-1026`). The bash validator invokes the detector before other protected-file checks (`hooks/loop-bash-validator.sh:115-116` from search results).
- edge_cases_or_failure_modes: Since the detector lowercases input, it explicitly treats `-A` as `-a` (`hooks/lib/loop-common.sh:912-914`, `hooks/lib/loop-common.sh:983-990`). Broad `git add .` or `git add *` is only blocked when `.humanize` exists and is not ignored, while force plus broad/all is always blocked because force bypasses ignore rules (`hooks/lib/loop-common.sh:998-1022`). Direct `.humanize` paths are blocked regardless of flags, but the regex avoids over-blocking names like `.humanizeconfig` (`hooks/lib/loop-common.sh:967-972`).
- validation_or_tests: No assigned test directly targets this template, but the implementation has detailed path/flag logic in `git_adds_humanize` and is integrated into `loop-bash-validator`. This file is a user-facing contract for that gate, not the gate itself.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-109 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt fragment defining how Claude requests Goal Tracker changes when direct modification is not allowed after Round 0.
- algorithmic_behavior: The fragment instructs Claude to include a `## Goal Tracker Update Request` section only when updates are needed, with requested changes and justification (`prompt-template/claude/goal-tracker-update-request.md:2-14`). It converts attempted stateful Goal Tracker mutation into an explicit review/update request for Codex.
- inputs_outputs_state: Input is Claude’s assessment that Goal Tracker changes are needed. Output is a markdown section in Claude’s summary, not a direct write to `goal-tracker.md`. Codex later reviews and applies justified changes (`prompt-template/claude/goal-tracker-update-request.md:16`).
- gates_or_invariants: The core invariant is separation of authority: after Round 0, Claude does not directly update the Goal Tracker; it asks Codex. The related block template says to include a “Goal Tracker Update Request” in the summary instead of modifying `goal-tracker.md` (`prompt-template/block/goal-tracker-modification.md:9-21` from search results).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` when generating next-round Claude prompts (`hooks/loop-codex-stop-hook.sh:1561-1563` from search results). Related Codex-side prompt content declares that Codex is responsible for processing Claude’s request and updating the tracker if justified (`prompt-template/codex/goal-tracker-update-section.md:1-12` from search results). Bash protection also blocks direct `goal-tracker.md` modification via `command_modifies_file` (`hooks/loop-bash-validator.sh:349-352` from search results).
- edge_cases_or_failure_modes: The template is conditional text; it cannot enforce anything alone. Enforcement depends on write/edit/bash validators and Codex prompt compliance. If Claude omits this section when a tracker change is needed, the loop may lose planning state until Codex infers or requests repair. If Claude includes unjustified or stale requests, Codex is expected to reject or adjust them.
- validation_or_tests: Indirectly covered by bash-validator behavior around `goal-tracker.md` writes and by prompt integration in `loop-codex-stop-hook`. No assigned executable test targets this exact template.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-139 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Large executable robustness specification for `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`. It validates setup-time gates for argument parsing, plan-file safety, git repository state, YAML-safe persisted state, mutual exclusion, symlink defense, dependency handoff, timeout handling, full-review-round, and skip-implementation mode.
- algorithmic_behavior: The harness creates isolated temporary repos, minimal plans, and mock executables, then runs setup scripts with `CLAUDE_PROJECT_DIR` scoped to each test repo (`tests/robustness/test-setup-scripts-robustness.sh:15-20`, `tests/robustness/test-setup-scripts-robustness.sh:31-87`). It uses output/exit-code assertions for negative gates and success-path progression checks (`tests/robustness/test-setup-scripts-robustness.sh:96-214`, `tests/robustness/test-setup-scripts-robustness.sh:668-739`).
- inputs_outputs_state: Inputs are generated git repos, plan files, flags, fake active loop state files, fake `codex`, fake `gh`, and fake `timeout`/`gtimeout`. Outputs are PASS/FAIL assertions through `test-helpers.sh`, plus temporary `.humanize/rlcr/<timestamp>` loop directories in positive skip-impl tests. The final state is summarized with `print_test_summary` and returned as the script exit code (`tests/robustness/test-setup-scripts-robustness.sh:1069-1074`).
- gates_or_invariants: RLCR argument gates reject missing plan without `--skip-impl`, nonnumeric/negative/empty `--max`, bad `--codex-timeout`, missing `--codex-model`, unknown options, and duplicate positional/`--plan-file` plan specification (`tests/robustness/test-setup-scripts-robustness.sh:96-214`). Plan validation gates reject comment-only plans, plans under five lines, spaces, shell metacharacters, absolute paths, tracked plans without `--track-plan-file`, symlink files, and symlink parent directories (`tests/robustness/test-setup-scripts-robustness.sh:224-341`, `tests/robustness/test-setup-scripts-robustness.sh:444-463`, `tests/robustness/test-setup-scripts-robustness.sh:597-658`). Setup implementation enforces these through explicit parser and path/content checks (`scripts/setup-rlcr-loop.sh:120-224`, `scripts/setup-rlcr-loop.sh:263-541`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `scripts/portable-timeout.sh` (`tests/robustness/test-setup-scripts-robustness.sh:15-19`). Calls `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh` through wrappers (`tests/robustness/test-setup-scripts-robustness.sh:65-87`). The PR setup assertions map to parser and mutual-exclusion logic in `setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:123-165`, `scripts/setup-pr-loop.sh:220-240`). RLCR active-loop checks depend on `find_active_loop`/`find_active_pr_loop` from `hooks/lib/loop-common.sh` as sourced by setup scripts (`scripts/setup-rlcr-loop.sh:232-258`, `scripts/setup-pr-loop.sh:220-240`).
- edge_cases_or_failure_modes: The tests intentionally cover git repos without commits, non-git directories, YAML-unsafe branch/model strings, active RLCR blocking RLCR setup, active PR loop blocking RLCR setup, zero timeout accepted despite “positive integer” wording, very large timeout accepted, and timeout-command failure surfaces (`tests/robustness/test-setup-scripts-robustness.sh:351-463`, `tests/robustness/test-setup-scripts-robustness.sh:527-595`, `tests/robustness/test-setup-scripts-robustness.sh:749-866`). Skip-impl mode is a key state-transition edge case: no plan is required, `.review-phase-started` is created, `review_started: true` is written to `state.md`, and the generated goal tracker must not contain placeholder text (`tests/robustness/test-setup-scripts-robustness.sh:975-1067`; implementation at `scripts/setup-rlcr-loop.sh:675-760`).
- validation_or_tests: This file is itself a validation suite with 45 named tests. It checks negative rejection, positive progression to dependency/auth checks, and concrete filesystem artifacts for skip-impl mode (`tests/robustness/test-setup-scripts-robustness.sh:96-1074`). It is a strong core-algorithm candidate because it specifies setup preconditions and state initialization for both RLCR and PR loops.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-019`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-049`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-079`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-109`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-139`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`