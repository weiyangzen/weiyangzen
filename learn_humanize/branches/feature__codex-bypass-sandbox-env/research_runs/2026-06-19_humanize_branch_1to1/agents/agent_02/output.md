# agent_02 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: The `agents` directory is a Claude Code subagent prompt surface for repository-specific delegated analysis. In this branch export it contains exactly one child, `agents/draft-relevance-checker.md`, which supports the `gen-plan` command’s relevance gate before deeper plan generation.
- algorithmic_behavior: Recursive inspection found only `agents/draft-relevance-checker.md`. Its frontmatter declares `name: draft-relevance-checker`, `description: Checks if a draft document is relevant to the current repository`, `model: haiku`, and `tools: Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-6`. The prompt instructs the subagent to inspect repository docs and structure, compare draft content against repository concepts/files/features, then return exactly one verdict shape: `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` at `agents/draft-relevance-checker.md:14-29`.
- inputs_outputs_state: Input is draft-document content supplied by the caller plus read-only repository exploration via `Read`, `Glob`, and `Grep`. Output is a single semantic classification line. It has no persistent state of its own; it influences command control flow by allowing or stopping `gen-plan` phase progression.
- gates_or_invariants: The key invariant is lenient semantic relevance: the agent says to mark relevant when the draft could reasonably connect to the repository and to lean relevant on doubt at `agents/draft-relevance-checker.md:31-36`. This intentionally biases against false negatives. The command-level gate is described in `commands/gen-plan.md:50-72`: after IO validation, invoke `humanize:draft-relevance-checker`; if `NOT_RELEVANT`, report the reason and stop; if `RELEVANT`, continue to Phase 3.
- dependencies_and_callers: `commands/gen-plan.md:56-65` is the direct caller contract, including model `"haiku"` and required output forms. `tests/test-gen-plan.sh:111-155` validates the agent file exists, has name `draft-relevance-checker`, uses model `haiku`, and declares tools. Additional validation checks naming/frontmatter at `tests/test-gen-plan.sh:241-295`, model validity at `tests/test-gen-plan.sh:391-399`, and English-only/no emoji/CJK content at `tests/test-gen-plan.sh:417-422`.
- edge_cases_or_failure_modes: The prompt is deliberately tolerant of informal drafts and any language at `agents/draft-relevance-checker.md:33-34`. A potential failure mode is returning text outside the two expected prefixes, which would leave the caller to interpret an invalid or ambiguous verdict. Another is shallow repository exploration causing a false `NOT_RELEVANT`, partially mitigated by the “if in doubt” rule.
- validation_or_tests: Static tests in `tests/test-gen-plan.sh` validate existence, frontmatter fields, model, tool declaration, naming convention, and content character restrictions. There is no direct behavioral test that runs the agent and verifies verdict parsing.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation script for active PR review loops. It is the authoritative implementation behind `/humanize:cancel-pr-loop`, transitioning a PR-loop session from active `state.md` to terminal `cancel-state.md` while placing a `.cancel-requested` signal for stop-hook/polling logic.
- algorithmic_behavior: The script parses only `--force`, `-h`, and `--help`; unknown options exit `3` at `scripts/cancel-pr-loop.sh:24-66`. `--force` currently only sets `FORCE=true` and has no behavior change, matching the help text at `scripts/cancel-pr-loop.sh:39-41`. It sets `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, scopes cancellation to `.humanize/pr-loop`, finds the newest timestamp-like child directory using `ls -1d "$LOOP_BASE_DIR"/*/ | sort -r | head -1`, and treats it as the candidate loop at `scripts/cancel-pr-loop.sh:72-77`. If no directory exists, it prints `NO_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:78-82`. If the newest directory lacks `state.md`, it prints `NO_ACTIVE_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:88-98`. On active state, it extracts `current_round`, `max_iterations`, and `pr_number` from frontmatter-style lines, defaulting missing values to `?`, then `touch`es `.cancel-requested` and `mv`s `state.md` to `cancel-state.md` at `scripts/cancel-pr-loop.sh:104-122`. Success prints `CANCELLED`, a PR/round summary, and “State preserved as cancel-state.md” at `scripts/cancel-pr-loop.sh:128-130`.
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR`, current filesystem state under `.humanize/pr-loop`, and metadata lines in `state.md`. Outputs are stdout sentinel lines consumed by the slash command, the new `.cancel-requested` file, and a renamed `cancel-state.md`. State transition is active `state.md` to canceled `cancel-state.md`; no rollback is implemented if `touch` succeeds and `mv` fails under `set -e`.
- gates_or_invariants: The slash-command wrapper allows only this script and its `--force` variant at `commands/cancel-pr-loop.md:1-4`. The command contract says the script handles all cancellation logic and a PR loop is active if `state.md` exists in the newest `.humanize/pr-loop/` directory at `commands/cancel-pr-loop.md:17-25`. The script intentionally does not touch RLCR loops, documented in both its help at `scripts/cancel-pr-loop.sh:54-56` and the command note at `commands/cancel-pr-loop.md:25`. State-file mutation is protected elsewhere by bash validation: `hooks/loop-bash-validator.sh:133-153` permits state-file rename only when `is_cancel_authorized` approves the cancel signal and command structure.
- dependencies_and_callers: Direct caller is `commands/cancel-pr-loop.md:11-19`, which runs `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh` and interprets first-line sentinels `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`. PR loop setup tells users to cancel with `/humanize:cancel-pr-loop` when a PR loop is already active at `scripts/setup-pr-loop.sh:234-240` and in activation output at `scripts/setup-pr-loop.sh:875-881`. `hooks/pr-loop-stop-hook.sh:951-955` checks `.cancel-requested` during polling and exits cleanly when present. `hooks/lib/loop-common.sh:533-710` defines `is_cancel_authorized`, requiring the signal file, rejecting command injection/chaining/substitution, requiring exactly `mv state.md|finalize-state.md cancel-state.md`, normalizing paths, and rejecting symlink sources.
- edge_cases_or_failure_modes: The “newest loop” selection is lexical reverse sort over child directory paths; malformed or non-timestamp names can win if lexically later. If the newest directory is canceled or otherwise lacks `state.md` while an older active loop exists, the script returns `NO_ACTIVE_LOOP` rather than searching older directories. It does not handle existing `cancel-state.md` explicitly; `mv` may overwrite depending on platform semantics or fail in unusual permission cases. `grep` extraction assumes simple `key: value` lines and strips spaces, so values containing spaces are collapsed. `--force` is a no-op, which is explicit but can surprise callers expecting confirmation bypass or stronger cleanup.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:179-189` verifies help output includes `cancel-pr-loop`. `tests/test-pr-loop-scripts.sh:192-207` verifies no loop returns exit `1` and `NO_LOOP`, using `CLAUDE_PROJECT_DIR` to isolate the test directory. `tests/test-pr-loop-scripts.sh:211-244` creates `.humanize/pr-loop/2026-01-18_12-00-00/state.md`, runs the script, expects exit `0` plus `CANCELLED`, and asserts `cancel-state.md` exists while `state.md` no longer exists. Broader cancel-signal validation is covered by `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, referenced by numerous cases around `.cancel-requested`, injection rejection, path validation, and symlink handling.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-062 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared executable test support library for PR-loop tests. It creates deterministic `gh` and `codex` mocks, initializes isolated test environment state, and provides a PR-loop-specific summary function for the main PR-loop test runner.
- algorithmic_behavior: The library is guarded by `TEST_PR_LOOP_LIB_LOADED` so sourcing is idempotent at `tests/test-pr-loop-lib.sh:10-13`. It computes `SCRIPT_DIR` and `PROJECT_ROOT` if absent and sources `tests/test-helpers.sh` only if `setup_test_dir` is not already declared at `tests/test-pr-loop-lib.sh:14-21`. `create_mock_gh` writes an executable mock `gh` into a supplied directory at `tests/test-pr-loop-lib.sh:27-88`. That mock handles `gh auth status`, `gh repo view --json owner|name`, `gh pr view` for commits, number, and state, and `gh api user`/generic API responses, returning stable JSON or empty arrays at `tests/test-pr-loop-lib.sh:36-81`. `create_mock_codex` writes a mock `codex` command that prints “Mock codex output” and exits `0` at `tests/test-pr-loop-lib.sh:90-101`. `init_pr_loop_test_env` calls `setup_test_dir`, creates `$TEST_DIR/mock_bin`, prepends it to `PATH`, creates both mocks, and exports `MOCK_BIN_DIR` at `tests/test-pr-loop-lib.sh:107-121`. `print_test_summary` prints pass/fail totals and returns `1` when failures are nonzero at `tests/test-pr-loop-lib.sh:127-144`.
- inputs_outputs_state: Inputs are sourced shell variables (`SCRIPT_DIR`, `PROJECT_ROOT`, counters from `test-helpers.sh`) and a caller-supplied mock directory for mock creation functions. Outputs include temporary directories, executable mock binaries, modified `PATH`, exported `MOCK_BIN_DIR`, and summary process exit status. It mutates shell state intentionally for downstream test modules.
- gates_or_invariants: Idempotent loading prevents double definition/reinitialization. The mock `gh` creates stable GitHub responses expected by setup, fetch, poll, hook, and stop-hook tests. Unhandled `gh` commands fail with exit `1` at `tests/test-pr-loop-lib.sh:84-85`, which is an invariant that tests must explicitly mock every GitHub CLI behavior they rely on. The summary gate treats any `TESTS_FAILED > 0` as test failure.
- dependencies_and_callers: Direct main caller is `tests/test-pr-loop.sh:18-27`, which sources `test-helpers.sh` and this library, then calls `init_pr_loop_test_env`. That runner then sources `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh`, runs their test functions, and calls `print_test_summary` at `tests/test-pr-loop.sh:32-53`. The library depends on `tests/test-helpers.sh` for counters, `pass`, `fail`, and `setup_test_dir`; the helper implementation initializes counters and `setup_test_dir` with cleanup at `tests/test-helpers.sh:22-89`.
- edge_cases_or_failure_modes: Since the library prepends mock bin to the existing `PATH`, tests can still accidentally hit real commands if a mock subcommand is incomplete and a script calls a different binary not mocked. The mock `gh pr view` pattern checks are simplistic and argument-order dependent in places, so production command changes can break tests or, worse, stop exercising the intended branch. `print_test_summary` here shadows the more general helper summary; both use `TESTS_FAILED`, but this PR-loop version omits skipped count handling from `tests/test-helpers.sh:58-78`.
- validation_or_tests: This file is itself test infrastructure rather than a production behavior test. Its behavior is indirectly exercised by `tests/test-pr-loop.sh:26-47`, which initializes the mock environment and runs the script, hook, and stop-hook suites. Search results show other robustness tests often define their own `create_mock_gh`, so this library is scoped to the main PR-loop suite, not every PR-loop-related test.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-092 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR plan-integrity gate. It is rendered when a loop detects that the source plan file differs from the backup captured at session start, and it blocks continuation until the user cancels and restarts around the modified plan.
- algorithmic_behavior: The template declares a “Plan File Modified” block and interpolates `{{PLAN_FILE}}` and `{{BACKUP_PATH}}` at `prompt-template/block/plan-file-modified.md:1-12`. It states that modifying plan files is forbidden during an active session and gives a three-step transition: cancel current session, update the plan file, start a new session with the same plan path. This makes the plan file immutable within an active RLCR loop and directs the user to restart so state/backup/instructions realign.
- inputs_outputs_state: Inputs are template variables `PLAN_FILE` and `BACKUP_PATH`. Output is a rendered human-facing block used as the `reason` in a JSON block decision. The template itself does not mutate state; it explains the required state transition from active loop to canceled loop to new loop.
- gates_or_invariants: The invariant is “plan file content must match the loop backup while implementation/review has not made the plan irrelevant.” `hooks/loop-codex-stop-hook.sh:267-335` checks `BACKUP_PLAN="$LOOP_DIR/plan.md"`, ensures the backup and original exist, checks tracked status for uncommitted changes, then runs `diff -q "$FULL_PLAN_PATH" "$BACKUP_PLAN"`. On mismatch it renders this template with `load_and_render_safe` and returns `{"decision":"block"}` at `hooks/loop-codex-stop-hook.sh:316-334`. A related validator blocks tracked plan-file modifications or tracking-state drift at `hooks/loop-plan-file-validator.sh:150-191`.
- dependencies_and_callers: Rendered through `hooks/lib/template-loader.sh`, whose contract uses `{{VARIABLE_NAME}}` placeholders and single-pass substitution at `hooks/lib/template-loader.sh:7-20` and `hooks/lib/template-loader.sh:50-131`. The stop hook fallback text at `hooks/loop-codex-stop-hook.sh:317-328` mirrors this template if loading fails. Search also shows `prompt-template/block/schema-outdated.md` and command docs use similar cancel/restart language, but this specific file’s direct production caller is the codex stop hook.
- edge_cases_or_failure_modes: The template wording says “active session” while the fallback says “active RLCR loop”; this is not behaviorally wrong but slightly less specific. If `PLAN_FILE` or `BACKUP_PATH` contains literal `{{...}}`, the single-pass renderer prevents recursive substitution, reducing placeholder injection risk. If the template is missing, the fallback still blocks with equivalent instructions. It applies only before review phase: `hooks/loop-codex-stop-hook.sh:260-265` skips plan integrity checks once review has started.
- validation_or_tests: Template rendering mechanics are covered by `tests/test-template-loader.sh` cases referenced in search results, including placeholder replacement. Plan-file integrity behavior is covered indirectly by plan-file robustness and validator tests such as `tests/robustness/test-plan-file-robustness.sh` and `hooks/loop-plan-file-validator.sh` behavior. No direct test line was found that asserts this exact template text, but the stop hook includes a fallback with matching semantic content.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-122 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 prompt template for the “no existing comments” startup path. It is a critical completion contract that tells the worker to write the initial resolution summary, avoid PR comments, and exit so the Stop Hook can poll for automatic bot reviews.
- algorithmic_behavior: The template renders a prominent “CRITICAL - Work Completion Requirements” block at `prompt-template/pr-loop/critical-requirements-no-comments.md:2-4`. It requires the worker to write a resolution summary to `{{RESOLVE_PATH}}`, specifically noting Round 0 is awaiting initial bot reviews and that there are no issues yet at `prompt-template/pr-loop/critical-requirements-no-comments.md:6-14`. It then directs the worker to try to exit so the Stop Hook will poll, and explicitly forbids commenting on the PR to trigger review because new-PR bot review is expected to happen automatically at `prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`.
- inputs_outputs_state: Input is `RESOLVE_PATH`, set by setup to `$LOOP_DIR/round-0-pr-resolve.md` at `scripts/setup-pr-loop.sh:721`. Output is appended human/agent instruction text in the setup command output. The intended downstream state is creation of `round-0-pr-resolve.md` by the worker, followed by attempted session exit, after which the stop hook begins polling.
- gates_or_invariants: This template is selected only when setup detects no comments by finding the exact sentinel `*No comments found.*` in the fetched comment file at `scripts/setup-pr-loop.sh:723-728`. If `COMMENT_COUNT=0`, setup renders this no-comments critical template at `scripts/setup-pr-loop.sh:890-915`; otherwise it renders the has-comments critical template. The core invariant is that no PR trigger comment should be posted during initial no-comments Round 0; review bots are expected to review automatically. This avoids redundant or premature bot-trigger comments.
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh`, which builds `TEMPLATE_VARS` including `RESOLVE_PATH` at `scripts/setup-pr-loop.sh:731-738`, outputs the initial prompt at `scripts/setup-pr-loop.sh:887-888`, and then appends this critical requirements block for `COMMENT_COUNT=0` via `load_and_render_safe` at `scripts/setup-pr-loop.sh:890-915`. Related no-comments task prompt is `prompt-template/pr-loop/round-0-task-no-comments.md`, referenced by setup at `scripts/setup-pr-loop.sh:766-800`. Stop-hook polling behavior is implemented in `hooks/pr-loop-stop-hook.sh`, including cancel-signal polling exit at `hooks/pr-loop-stop-hook.sh:951-955`.
- edge_cases_or_failure_modes: Comment detection is sentinel-based: any non-sentinel content, including malformed fetch output, counts as comments (`COMMENT_COUNT=1`) at `scripts/setup-pr-loop.sh:723-729`, so this template will not be used. If the bots do not auto-review, the user may wait until stop-hook timeout rather than triggering review manually, but the template is intentionally strict for the no-comments path. If `RESOLVE_PATH` is not rendered, the worker would receive a literal placeholder; `load_and_render_safe` fallback reduces missing-template risk, and the template loader preserves unknown placeholders rather than failing.
- validation_or_tests: The setup script contains an inline fallback with equivalent text at `scripts/setup-pr-loop.sh:893-913`, so behavior survives missing template files. Search shows `prompt-template/pr-loop/critical-requirements-no-comments.md` is directly referenced at `scripts/setup-pr-loop.sh:914`. PR-loop tests exercise setup and prompt-generation paths through the main runner `tests/test-pr-loop.sh:40-47`, though no direct assertion line was identified for this exact template content.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-002`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-032`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-062`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-092`, `FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-122`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`