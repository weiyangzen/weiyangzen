# agent_18 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow contract for cancelling active PR review loops. It is not the cancellation implementation itself; it constrains the agent to call `scripts/cancel-pr-loop.sh` and interpret that script’s first output token. Frontmatter limits allowed tools to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh)` and the same command with `--force` at `commands/cancel-pr-loop.md:1-4`.
- algorithmic_behavior: The command delegates all cancellation logic to the script, then maps script status tokens to user-facing responses. The workflow says to run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"` at `commands/cancel-pr-loop.md:11-15`, inspect line one at `commands/cancel-pr-loop.md:17-20`, and report either no active loop or the cancellation message. The command states the active-loop predicate: `state.md` exists in the newest `.humanize/pr-loop/` directory at `commands/cancel-pr-loop.md:21`.
- inputs_outputs_state: Inputs are the plugin root environment variable and optional `--force` allowed by command metadata. Output is a textual response derived from script tokens `NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED` at `commands/cancel-pr-loop.md:17-20`. Runtime state mutation is delegated to `scripts/cancel-pr-loop.sh`, which resolves `PROJECT_ROOT="${CLAUDE_PROJECT_DIR:-$(pwd)}"`, scans `.humanize/pr-loop`, touches `.cancel-requested`, and renames `state.md` to `cancel-state.md` at `scripts/cancel-pr-loop.sh:72-89` and `scripts/cancel-pr-loop.sh:118-122`.
- gates_or_invariants: The command explicitly gates scope to PR loops only and excludes RLCR loops at `commands/cancel-pr-loop.md:25`. The implementation confirms the PR-only base path `.humanize/pr-loop` at `scripts/cancel-pr-loop.sh:72-76`. The cancel script requires a newest loop directory and an active `state.md`; otherwise it emits `NO_LOOP` or `NO_ACTIVE_LOOP` and exits 1 at `scripts/cancel-pr-loop.sh:78-98`. The PR stop hook observes `.cancel-requested` to exit polling at `hooks/pr-loop-stop-hook.sh:951-955`.
- dependencies_and_callers: Primary dependency is `scripts/cancel-pr-loop.sh`. The README exposes `/cancel-pr-loop` at `README.md:138`. `commands/start-pr-loop.md:62` and PR loop prompts mention cancellation as an escape path. Tests for the script live in `tests/test-pr-loop-scripts.sh:169-253`, covering help, no-loop, and active-loop rename behavior.
- edge_cases_or_failure_modes: If no PR loop directory exists, the script prints `NO_LOOP` and exits 1 at `scripts/cancel-pr-loop.sh:78-82`. If a newest loop directory exists without `state.md`, it prints `NO_ACTIVE_LOOP` and exits 1 at `scripts/cancel-pr-loop.sh:91-98`. `--force` currently has no additional effect per help text at `scripts/cancel-pr-loop.sh:39-41`. The command does not cover finalize-state cancellation for PR loops; it only keys on `state.md`.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:181-190` verifies help output, `tests/test-pr-loop-scripts.sh:192-208` verifies `NO_LOOP`, and `tests/test-pr-loop-scripts.sh:211-247` verifies `CANCELLED` plus `state.md` to `cancel-state.md` rename. Broader cancel-signal security tests focus on hook allowance for moving state files only when `.cancel-requested` exists, referenced across `tests/test-cancel-signal-file.sh`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-048 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the portable ANSI-stripping and pass/fail count extraction used by the aggregate test runner. The test file documents that it targets the implementation in `tests/run-all-tests.sh` and specifically BSD/macOS plus GNU/Linux `sed` compatibility at `tests/test-ansi-parsing.sh:3-6`.
- algorithmic_behavior: The test defines `esc=$'\033'`, strips SGR color sequences with `sed "s/${esc}\\[[0-9;]*m//g"`, then extracts summary counts with `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'`. This mirrors `tests/run-all-tests.sh:121-125`. Eight scenarios exercise basic color stripping, multiple colors, passed extraction, failed extraction, zero failed count, multiline suite output, plain text, and bold+color combined at `tests/test-ansi-parsing.sh:41-158`.
- inputs_outputs_state: Inputs are synthetic strings containing ANSI SGR sequences and plain-text summaries. Outputs are local `result`, `passed`, and `failed` variables compared against expected literals. Test state is only in counters `TESTS_PASSED` and `TESTS_FAILED` initialized at `tests/test-ansi-parsing.sh:18-19` and updated by `pass()` / `fail()` at `tests/test-ansi-parsing.sh:21-31`. The script exits 0 when `TESTS_FAILED` is zero, otherwise exits 1 at `tests/test-ansi-parsing.sh:170-178`.
- gates_or_invariants: The portable escape literal must be ANSI-C quoted `$'\033'`, not a nonportable sed escape, per `tests/test-ansi-parsing.sh:38-40`. The stripping regex only removes CSI SGR `m` sequences with numeric or semicolon parameters. Count extraction requires labels `Passed:` and `Failed:` followed by whitespace and decimal digits. In multiline output, the runner must use the last matching count via `tail -1`, as tested at `tests/test-ansi-parsing.sh:124-125` and implemented at `tests/run-all-tests.sh:124-125`.
- dependencies_and_callers: The aggregate runner includes this suite in its ordered `TEST_SUITES` list at `tests/run-all-tests.sh:32-44`. The suite depends only on Bash, `sed`, `grep`, and shell arithmetic. It intentionally mirrors the runner’s parse block at `tests/run-all-tests.sh:121-128`, where parsed counts are accumulated into `TOTAL_PASSED` and `TOTAL_FAILED`.
- edge_cases_or_failure_modes: Unsupported ANSI escape families, such as cursor movement or OSC sequences, are not stripped because the regex is limited to SGR color/style codes. Count labels with different spelling or counts not at the end of the extracted match would parse as zero or wrong value. The test covers zero counts at `tests/test-ansi-parsing.sh:97-109`, no ANSI codes at `tests/test-ansi-parsing.sh:132-144`, and combined style/color like `1;32m` at `tests/test-ansi-parsing.sh:146-158`.
- validation_or_tests: The file is itself a validation suite and is run by `tests/run-all-tests.sh` at `tests/run-all-tests.sh:43`. Its own summary emits colored `Passed:` / `Failed:` lines at `tests/test-ansi-parsing.sh:163-168`, making it a self-referential fixture for the aggregate parser. I did not run it because the branch export is read-only and the assignment requested research notes only.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Block prompt template for a PR loop state transition when the stored PR commit SHA is no longer an ancestor of current `HEAD`, indicating a non-fast-forward history rewrite. The template is short but algorithmically important because it defines the operator contract after force-push detection: tracking is updated, review state is reset, and a new trigger comment is required.
- algorithmic_behavior: The rendered message announces old and new commit SHAs with `{{OLD_COMMIT}}` and `{{NEW_COMMIT}}` at `prompt-template/block/force-push-detected.md:3`, explains that force pushes reset review state at `prompt-template/block/force-push-detected.md:5`, then lists required actions at `prompt-template/block/force-push-detected.md:7-10`. The required transition is not “continue automatically”; the user must post a fresh comment containing `{{BOT_MENTION_STRING}}`.
- inputs_outputs_state: Inputs are template variables `OLD_COMMIT`, `NEW_COMMIT`, and `BOT_MENTION_STRING`; `PR_NUMBER` is also passed by the caller though unused by this template. Output is Markdown embedded as the hook block `reason`. Runtime state is updated before rendering: `hooks/pr-loop-stop-hook.sh` rewrites `latest_commit_sha`, `latest_commit_at`, clears `last_trigger_at`, and clears `trigger_comment_id` at `hooks/pr-loop-stop-hook.sh:395-403`, then updates in-memory variables at `hooks/pr-loop-stop-hook.sh:405-409`.
- gates_or_invariants: The force-push gate triggers only when a stored `PR_LATEST_COMMIT_SHA` exists, current `HEAD` differs, and `git merge-base --is-ancestor "$PR_LATEST_COMMIT_SHA" "$CURRENT_HEAD"` returns false. That gate is implemented at `hooks/pr-loop-stop-hook.sh:369-379`. After the gate fires, stale trigger data must be cleared to prevent old comments from satisfying the new review cycle, enforced at `hooks/pr-loop-stop-hook.sh:396-401`.
- dependencies_and_callers: Rendered via `load_and_render_safe "$TEMPLATE_DIR" "block/force-push-detected.md"` in `hooks/pr-loop-stop-hook.sh:411-419`. Rendering uses the shared template loader, whose placeholder replacement is single-pass and preserves unknown placeholders at `hooks/lib/template-loader.sh:50-58` and `hooks/lib/template-loader.sh:111-122`. Related setup writes `latest_commit_sha`, `latest_commit_at`, `last_trigger_at`, and `trigger_comment_id` fields at `scripts/setup-pr-loop.sh:630-633`.
- edge_cases_or_failure_modes: If the new head timestamp cannot be read from GitHub, the hook falls back to current UTC time at `hooks/pr-loop-stop-hook.sh:384-393`. If the template is missing or renders empty, `load_and_render_safe` uses the inline fallback message at `hooks/pr-loop-stop-hook.sh:411-415` and `hooks/lib/template-loader.sh:170-203`. A fast-forward update does not render this template because the old SHA remains an ancestor. The template does not itself enforce the state update; it only communicates the already-applied reset.
- validation_or_tests: `tests/test-pr-loop-stophook.sh:538-640` simulates a history rewrite with mocked `git rev-parse HEAD` returning `newsha456` and `git merge-base --is-ancestor` failing, then asserts the hook output mentions force push/history rewrite/re-trigger at `tests/test-pr-loop-stophook.sh:632-637`. Template existence is indirectly covered by template-reference scanning patterns in `tests/test-template-references.sh:83-110`, though that scanner’s explicit script list focuses on RLCR hook files and may not fully cover `hooks/pr-loop-stop-hook.sh`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt variant for RLCR sessions when the code review phase is skipped. It defines the manual-verification contract and finalization deliverables for a degraded validation path.
- algorithmic_behavior: The prompt begins with a warning using `{{REVIEW_SKIP_REASON}}` at `prompt-template/claude/finalize-phase-skipped-prompt.md:1-5`, instructs manual verification steps at `prompt-template/claude/finalize-phase-skipped-prompt.md:7-13`, allows optional simplification via `code-simplifier:code-simplifier` at `prompt-template/claude/finalize-phase-skipped-prompt.md:15-24`, and enforces non-negotiable constraints against functional changes or test regressions at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`.
- inputs_outputs_state: Inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a Markdown prompt returned in a JSON block decision by `enter_finalize_phase()`. The state transition is performed by moving `state.md` to `finalize-state.md` before rendering at `hooks/loop-codex-stop-hook.sh:1041-1048`, then returning `{"decision":"block","reason":finalize_prompt,"systemMessage":...}` at `hooks/loop-codex-stop-hook.sh:1114-1122`.
- gates_or_invariants: This template is selected only when `enter_finalize_phase` receives a nonempty `skip_reason` at `hooks/loop-codex-stop-hook.sh:1051-1084`. One concrete skip path is missing `base_branch`: the stop hook sets `REVIEW_SKIPPED=true` and `REVIEW_SKIP_REASON="No base_branch configured for code review"` at `hooks/loop-codex-stop-hook.sh:1418-1427`. The prompt requires all todos completed, a commit, and writing `finalize-summary.md` at `prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-skipped-prompt.md"` at `hooks/loop-codex-stop-hook.sh:1078-1084`. Rendering depends on `hooks/lib/template-loader.sh`, especially the single-pass placeholder replacement behavior at `hooks/lib/template-loader.sh:54-58` and fallback behavior at `hooks/lib/template-loader.sh:170-203`. It is paired with the non-skipped `prompt-template/claude/finalize-phase-prompt.md`, selected in the `else` branch at `hooks/loop-codex-stop-hook.sh:1085-1111`.
- edge_cases_or_failure_modes: Because missing variables remain as placeholders, an unset `PLAN_FILE` or `FINALIZE_SUMMARY_FILE` would leak literal `{{...}}` into the prompt rather than fail rendering, per `hooks/lib/template-loader.sh:119-122`. If the template is missing, the hook uses an inline fallback containing the same major instructions at `hooks/loop-codex-stop-hook.sh:1052-1077`. The skipped-review path reduces validation confidence, so the prompt explicitly shifts burden to manual checks and available tests at `prompt-template/claude/finalize-phase-skipped-prompt.md:7-13`.
- validation_or_tests: Finalize-phase transition behavior is covered around `tests/test-finalize-phase.sh:524-540`, verifying `COMPLETE` causes a block decision and `state.md` to `finalize-state.md`. The same suite verifies review failure does not enter finalize at `tests/test-finalize-phase.sh:566-619`, and normal non-complete review keeps `state.md` intact at `tests/test-finalize-phase.sh:775-797`. Direct skipped-review template rendering is indirectly supported by template loader/reference tests, but I did not find a narrow test that asserts this exact prompt’s `REVIEW_SKIP_REASON` content.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-138 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for RLCR session discovery through `find_active_loop`. It verifies how hooks and setup scripts determine whether an RLCR loop is active under concurrent or malformed session-directory conditions.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-session-robustness.sh:14-17`, creates a temp directory at `tests/robustness/test-session-robustness.sh:19`, then repeatedly constructs `.rlcr`-like session trees and calls `find_active_loop "$TEST_DIR/rlcr"`. The implementation under test checks only the lexicographically newest immediate child directory, returning it only if it contains `state.md` or `finalize-state.md`, at `hooks/lib/loop-common.sh:151-174`.
- inputs_outputs_state: Inputs are filesystem layouts under `$TEST_DIR/rlcr`, including timestamped directories, arbitrary directory names, symlinks, files, deep nested paths, and terminal state files. Output is the `RESULT` string from `find_active_loop`, compared to an expected path or empty string. Test state is the shared pass/fail counters from `tests/test-helpers.sh:22-24`, summarized via `print_test_summary` at `tests/robustness/test-session-robustness.sh:318-323`.
- gates_or_invariants: Active indicators are exactly `state.md` and `finalize-state.md`, as stated in `hooks/lib/loop-common.sh:151-155` and implemented at `hooks/lib/loop-common.sh:168-173`. Finished or cancelled sessions are not active: `complete-state.md` is ignored by test 15 at `tests/robustness/test-session-robustness.sh:259-271`, and `cancel-state.md` is ignored by test 16 at `tests/robustness/test-session-robustness.sh:273-285`. The newest-only invariant is explicit in test 4 at `tests/robustness/test-session-robustness.sh:89-103` and mixed finished/active test 17 at `tests/robustness/test-session-robustness.sh:287-302`.
- dependencies_and_callers: Depends on `find_active_loop` in `hooks/lib/loop-common.sh:156-174`, and on helper functions `pass`, `fail`, `setup_test_dir`, and `print_test_summary` from `tests/test-helpers.sh:30-89`. `find_active_loop` is called by validators and setup scripts, including `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-pr-loop.sh`, and `scripts/setup-rlcr-loop.sh` as found by repository references.
- edge_cases_or_failure_modes: The test locks in several non-obvious behaviors: older active sessions are ignored if the newest directory lacks active state at `tests/robustness/test-session-robustness.sh:91-103`; non-existent and empty base directories return empty at `tests/robustness/test-session-robustness.sh:128-148`; files without subdirectories return empty at `tests/robustness/test-session-robustness.sh:150-162`; unexpected names and spaces can work at `tests/robustness/test-session-robustness.sh:179-222`; deep nested active state is ignored because only one level is scanned at `tests/robustness/test-session-robustness.sh:224-238`. The “Unicode” test actually creates `session-test`, so it verifies graceful handling generally rather than Unicode-specific bytes at `tests/robustness/test-session-robustness.sh:304-316`.
- validation_or_tests: The file itself defines 18 session robustness checks and exits through the shared summary at `tests/robustness/test-session-robustness.sh:322-323`. It is included in the aggregate runner’s robustness suite list at `tests/run-all-tests.sh:55-58`. Related coverage appears in `tests/test-finalize-phase.sh:286-330`, which verifies `finalize-state.md` is active and `complete-state.md` is not, and in `tests/robustness/test-state-transition-robustness.sh`, which exercises state transitions around finalize/cancel/complete.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-018, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-048, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-078, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-108, FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-138
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`