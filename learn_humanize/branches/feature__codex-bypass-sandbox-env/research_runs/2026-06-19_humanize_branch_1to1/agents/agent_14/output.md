# agent_14 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: Prompt-template bundle for the PR review loop’s round-zero startup, work-completion instructions, initial goal tracker, and later Codex goal-tracker update instructions. The directory is not executable code, but it defines behavior contracts consumed by `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh`.
- algorithmic_behavior: `round-0-header.md` renders the initial PR metadata and comment preface with `PR_NUMBER`, `START_BRANCH`, and `ACTIVE_BOTS_DISPLAY` placeholders at `prompt-template/pr-loop/round-0-header.md:1-15`. `round-0-task-no-comments.md` tells the worker to wait for automatic bot reviews and write `round-0-pr-resolve.md`, explicitly forbidding a trigger comment for fresh PRs at lines `4-14` and `18-23`. `round-0-task-has-comments.md` switches the state contract when comments exist: prioritize human comments, fix issues, commit/push, comment on the PR, and write the resolution summary at lines `4-27`. The two critical-requirements templates repeat the terminal gate in terse form: no-comments mode requires only a summary and exit attempt at `critical-requirements-no-comments.md:6-20`; has-comments mode requires commit, push, PR comment, and summary at `critical-requirements-has-comments.md:6-23`. `goal-tracker-initial.md` seeds the PR loop tracker with PR info, ultimate goal, zeroed issue summary/statistics, and an initial log entry at `goal-tracker-initial.md:1-33`. `codex-goal-tracker-update.md` instructs Codex to append per-round issue-summary rows, update totals, and add issue-log entries without changing tracker headers at lines `3-20` and `57-64`.
- inputs_outputs_state: Inputs are template variables rendered by `load_and_render_safe`: setup builds `GOAL_TRACKER_VARS` with `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `BOT_AC_ROWS`, and `STARTED_AT` at `scripts/setup-pr-loop.sh:658-666`, then writes rendered output to `.humanize/pr-loop/<timestamp>/goal-tracker.md` at `scripts/setup-pr-loop.sh:712-713`. The round-zero prompt uses `TEMPLATE_VARS` including `RESOLVE_PATH` and `BOT_MENTION_STRING` at `scripts/setup-pr-loop.sh:731-738`, writes the header to `round-0-prompt.md`, appends fetched comments, then appends the selected task template at `scripts/setup-pr-loop.sh:757-851`. On exit, the PR stop hook injects `codex-goal-tracker-update.md` into the local Codex prompt using `GOAL_TRACKER_FILE` and `NEXT_ROUND` at `hooks/pr-loop-stop-hook.sh:1252-1262`; that prompt asks Codex to emit per-bot status and recommendation into `round-N-pr-check.md` at `hooks/pr-loop-stop-hook.sh:1282-1302`.
- gates_or_invariants: Round-zero behavior is selected by the comment sentinel `*No comments found.*` at `scripts/setup-pr-loop.sh:723-729`. No-comments mode forbids trigger comments because first bot reviews are expected automatically, while has-comments mode requires a bot mention and push before polling can be useful. The templates repeatedly declare `.humanize/pr-loop/` state files system-managed and not user-modifiable, matching hook protections in `hooks/loop-edit-validator.sh:57-77` and shared PR-loop messages in `hooks/lib/loop-common.sh:889-905`. Goal-tracker update rules preserve header sections and append rows rather than replacing tables at `codex-goal-tracker-update.md:57-64`.
- dependencies_and_callers: Main callers are `scripts/setup-pr-loop.sh`, which renders all round-zero templates and prints the critical requirements at `scripts/setup-pr-loop.sh:890-942`, and `hooks/pr-loop-stop-hook.sh`, which renders the Codex tracker update fragment at `hooks/pr-loop-stop-hook.sh:1261`. Rendering goes through `hooks/lib/template-loader.sh`; missing templates fall back to inline strings via `load_and_render_safe`, so runtime should still produce actionable instructions if the template file is absent.
- edge_cases_or_failure_modes: A missing template causes fallback text to be used, but degraded text may diverge from the maintained template contract. Incorrect comment sentinel detection changes the algorithmic branch between “wait for automatic bot reviews” and “fix/comment/push.” Template variable values are single-pass rendered, so injected placeholder syntax in PR comments or bot names should not be recursively expanded by the loader. The directory is a core prompt-contract directory, not a state machine implementation itself.
- validation_or_tests: `tests/test-template-references.sh` scans shell template references and fails on missing referenced templates at lines `51-110`, checks template inventory at `121-142`, and verifies critical validators use safe rendering at `172-200`. `tests/test-template-loader.sh` covers real `load_and_render` integration at lines `147-164`, fallback behavior at `196-222`, shell metacharacters at `257-487`, and placeholder-injection prevention at `488-529`. PR-loop behavior is included in the aggregate runner via `tests/run-all-tests.sh:53-54` and robustness PR-loop API tests via `tests/run-all-tests.sh:72`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Aggregate executable specification for the Humanize plugin. It defines the canonical ordered list of unit, integration, and robustness suites that validate RLCR hooks, PR-loop hooks, template loading, state transitions, monitor behavior, and branch/path validators.
- algorithmic_behavior: Initializes counters and failed-suite list at `tests/run-all-tests.sh:28-30`, declares ordered test suites at lines `32-73`, and declares zsh-only tests at `75-78`. For each suite, it resolves the path under the tests directory, skips missing files at `80-86`, chooses `zsh` only for listed suites at `88-114`, otherwise executes the script directly at `115-118`. It captures output, strips ANSI escape codes, extracts final `Passed:` and `Failed:` counts, accumulates totals, and records failures when either the suite exit code is nonzero or extracted failed count is greater than zero at `121-137`. Final process status is success only when no failed suites are recorded at `148-158`.
- inputs_outputs_state: Inputs are executable test scripts located relative to `SCRIPT_DIR`, plus availability of `zsh` for `test-zsh-monitor-safety.sh`. Outputs are terminal summary lines, `TOTAL_PASSED`, `TOTAL_FAILED`, `FAILED_SUITES`, and process exit code. State is in-memory only; the runner does not mutate repository files.
- gates_or_invariants: Suite order is fixed and includes core validators before robustness tests. Missing suite files are skipped rather than failed at `83-85`, so repository completeness is not enforced by this runner alone. ANSI stripping uses a portable `sed` pattern with an explicit escape character at `121-123`. A suite with zero failed count but nonzero exit code still fails the aggregate gate at `130-132`; a suite with exit zero but positive failed count also fails.
- dependencies_and_callers: Depends on each listed test script being executable, on standard shell tools `sed`, `grep`, and `tail`, and optionally on `zsh`. It includes the assigned `tests/robustness/test-hook-system-robustness.sh` at `tests/run-all-tests.sh:69`, template tests at `34` and `38-40`, PR-loop suites at `53-54`, and PR-loop API robustness at `72`.
- edge_cases_or_failure_modes: Because `set -e` is enabled only after the command-capture block via `set -e` at `119`, the script starts with `set -uo pipefail` at line `12` and intentionally avoids aborting while capturing a failing suite. Count extraction defaults to `0` when output does not contain the expected summary format at `124-125`, which means a malformed suite can still be caught by nonzero exit but not necessarily by counters. Missing `zsh` causes a skip at `108-113`, not a failure. Missing test files also skip, so this runner is an availability-sensitive aggregate rather than a manifest-enforcing test.
- validation_or_tests: `tests/test-ansi-parsing.sh` specifically validates the ANSI stripping/count extraction approach used here, with cases for colored `Passed:` and `Failed:` lines and multiple summary lines. The runner itself is the top-level validation command for all listed behavior.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-074 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared template loading and rendering library for hook block messages and PR/RLCR prompts. It is central to the hook state machine because validators and stop hooks rely on it to generate actionable block reasons and prompt fragments without inline string duplication.
- algorithmic_behavior: `get_template_dir` derives the repository `prompt-template` path from the caller’s `hooks/lib` directory by walking two levels up at `hooks/lib/template-loader.sh:26-31`. `load_template` concatenates a template path, prints file content when present, and warns plus returns empty output when absent at `36-47`. `render_template` accepts `VAR=value` assignments, prefixes them as `TMPL_VAR_*` environment variables, then uses a single-pass `awk` scanner to replace `{{VAR}}` placeholders while preserving unknown placeholders at `58-132`. `load_and_render` composes load and render at `136-147`. `append_template` prints base content followed by another loaded template at `151-161`. `load_and_render_safe` falls back to caller-provided text when loading or rendering produces empty output at `170-203`. `validate_template_dir` checks that the template root and `block`, `codex`, and `claude` subdirectories exist at `208-221`.
- inputs_outputs_state: Inputs are a template directory, a relative template name, optional fallback text, and variable assignments. Outputs are rendered markdown to stdout and warnings/errors to stderr. It has no persistent state, but it temporarily supplies variables via environment to the `awk` process at `62-74`.
- gates_or_invariants: Placeholder syntax is documented as `{{VARIABLE_NAME}}` at `7-13`, but the implementation replaces any text between `{{` and `}}` when that exact key exists in `vars`; unknown placeholders are kept literal at `115-122`. The key invariant is single-pass substitution: replacement values are appended to output and not rescanned, preventing prompt corruption from values containing `{{OTHER_VAR}}` as documented at `54-57` and implemented by the character scanner at `88-126`. Missing templates should not produce empty user-facing blocks when callers use `load_and_render_safe`.
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh` at `hooks/lib/loop-common.sh:139-144`, which initializes `TEMPLATE_DIR` and warns rather than fails if validation fails at `146-149`. It is directly sourced by `scripts/setup-pr-loop.sh:39-42`. Callers include read/write/edit/bash validators for block messages, RLCR stop hook review-prompt fragments, and PR-loop setup/stop hook templates. It depends on `awk`, `env`, `cat`, and shell array handling.
- edge_cases_or_failure_modes: `load_template` returns empty for a missing file, so any caller using unsafe `load_and_render` can emit no block reason; this risk is why `tests/test-template-references.sh` checks safe usage for critical validators. Variable assignments with names unsuitable for environment variables can make `env` fail or create unreachable keys; robustness tests cover malformed names but the production function does not reject them itself. A malformed placeholder without closing braces is treated as literal `{{` and scanning continues at `103-109`. Multiline values flow through environment variables and are expected to render literally; tests exercise this.
- validation_or_tests: `tests/test-template-loader.sh` validates directory resolution, existing/missing loads, variable substitution, unreplaced placeholders, fallbacks, shell metacharacter literal rendering, placeholder-injection prevention, empty values, Unicode, underscores, and numbered variable names. `tests/test-template-references.sh` detects missing referenced templates and unsafe critical-validator calls. Robustness suites `tests/robustness/test-template-stress-robustness.sh` and `tests/robustness/test-template-error-robustness.sh` are included in the aggregate runner at `tests/run-all-tests.sh:62` and `70`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR read validator when an agent tries to read a round prompt or summary that does not match the active RLCR round. It is a small prompt fragment, but it encodes an important state-machine guard: active-round isolation.
- algorithmic_behavior: The template tells the user they are trying to read `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, states the current round, lists the current prompt and summary paths, and gives an explicit `cat {{FILE_PATH}}` command if the blocked historical file is still intentionally needed at `prompt-template/block/wrong-round-file.md:1-9`.
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, supplied by `hooks/loop-read-validator.sh:144-149`. Output is markdown written to stderr before the validator exits with code `2` at `hooks/loop-read-validator.sh:138-150`. It does not mutate loop state.
- gates_or_invariants: The gate triggers only for RLCR `round-N-summary.md` or `round-N-prompt.md` reads, after the active loop directory is found and the state file parses strictly. PR-loop files are explicitly allowed for reads in the read-validator comments at `hooks/loop-read-validator.sh:11-12`. The read is blocked when the requested round differs from `CURRENT_ROUND` and the file is not allowlisted at `hooks/loop-read-validator.sh:138`; allowlisted exceptions are defined in `hooks/lib/loop-common.sh:423-441`.
- dependencies_and_callers: Sole direct caller found is `hooks/loop-read-validator.sh`, which reaches this template after JSON validation, deep-nesting check, active-loop lookup, strict state parsing, file-type extraction, and location validation at `hooks/loop-read-validator.sh:25-150`. Rendering uses `load_and_render_safe`, so the inline fallback at `hooks/loop-read-validator.sh:139-143` covers missing-template failure.
- edge_cases_or_failure_modes: If no active loop exists, reads pass through at `hooks/loop-read-validator.sh:84-86`. If state parsing fails, the operation fails closed with a malformed-state error before this template is rendered at `94-98`. If the path is outside `.humanize/rlcr`, a different template, `wrong-file-location.md`, is used first at `122-131`. If the path uses the right filename but wrong directory, `wrong-directory-path.md` is used at `157-167`. The explicit `cat {{FILE_PATH}}` escape hatch is advisory text, not a validator bypass; Bash validators may still apply depending on command and loop state.
- validation_or_tests: `tests/test-template-loader.sh` validates the same rendering pathway using a related wrong-round template at `151-164`. `tests/test-allowlist-validators.sh` covers historical allowlisted reads/writes for summary files, and `tests/test-template-references.sh` would detect missing referenced block templates. The assigned robustness suite also verifies malformed-state and validator edge behavior around active loops.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-134 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for hook-system behavior across edit, plan-file, bash, read/write, stop-hook, JSON, and concurrency edge cases. It asserts the defensive behavior of the RLCR and PR-loop validators under malformed inputs, protected-file attempts, state corruption, and large/deep payloads.
- algorithmic_behavior: Initializes project paths, sources `loop-common.sh`, `portable-timeout.sh`, and shared test helpers, then creates an isolated temp directory at `tests/robustness/test-hook-system-robustness.sh:20-26`. Edit-validator tests verify valid Edit JSON passes, direct and traversal-based `state.md` edits are blocked, malformed JSON exits non-crash, missing `file_path` is handled, and unrelated outside paths pass through at `40-155`. Plan-file validator tests create a git repo, state file, and plan, then verify plan edits can pass when valid, non-plan files pass through, and empty JSON does not crash at `165-243`. State parser tests verify whitespace, quoted/special path values, and missing closing delimiter behavior at `253-307`. Bash validator tests assert `state.md` modification and post-round-zero `goal-tracker.md` writes are blocked, while unrelated dangerous commands are delegated to the external sandbox at `317-382`. Additional Edit/Write payload tests cover embedded newlines and binary-looking content at `384-411`. Concurrent read-validator invocations assert state remains parseable at `421-451`. Stop-hook tests cover missing state for RLCR and PR loops, corrupted/incomplete state, active-loop blocking behavior with mocked Codex, large JSON, escaped nulls, and deeply nested JSON at `461-665`.
- inputs_outputs_state: Inputs are synthetic Claude hook JSON payloads piped to hook scripts with `CLAUDE_PROJECT_DIR` pointing at temp projects. It creates temporary `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `plan.md`, and git repositories as fixtures. Outputs are PASS/FAIL counters via `pass`, `fail`, and `print_test_summary`, with process exit from `print_test_summary` at `671-672`.
- gates_or_invariants: Protected state files must not be editable through Edit or Bash; expected blocking is usually exit `2` for validator blocks at `73-78`, `150-155`, and `342-350`. Validators should handle malformed or unusual input with exit codes below crash range rather than shell termination at `88-92`, `102-107`, `393-411`, and `631-665`. Active RLCR stop hook with complete state must block exit by returning JSON containing `"decision": "block"` at `603-613`. Missing or invalid state is allowed to end or pass gracefully rather than trap the user in a broken loop at `461-557`.
- dependencies_and_callers: Calls `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. It depends on `jq`, `git`, shell utilities, sourced helper functions, and a mock `codex` executable inserted into `PATH` for the active-loop stop-hook test at `592-604`.
- edge_cases_or_failure_modes: This suite intentionally documents security boundaries: unrelated dangerous shell commands pass through because sandboxing is delegated outside this plugin at `369-382`, and paths outside `.humanize` pass through for Edit at `109-122`. Large JSON is bounded by `run_with_timeout 5` at `623-629`. The active-loop stop-hook test uses mock Codex output without completion markers, so expected behavior is continuation via block. Because it creates temp git repos and commits, failures can come from unavailable git identity defaults, missing `jq`, or environment-specific shell behavior; the script configures local git identity where needed at `170-177` and `582-589`.
- validation_or_tests: This file is itself validation. It is included in the full aggregate runner at `tests/run-all-tests.sh:69`. It complements more focused robustness suites for hook input, state file, state transition, path validation, timeout, and PR-loop API behavior listed in `tests/run-all-tests.sh:56-72`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item sections present exactly once above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`