# agent_24 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Hook registration surface for the Humanize plugin. It wires Claude hook events to the RLCR and PR-loop validators/stop hooks rather than implementing validation directly. The file declares itself as "Validation hooks and Stop hooks for /start-rlcr-loop and /start-pr-loop" at `hooks/hooks.json:2`.
- algorithmic_behavior: On `UserPromptSubmit`, it runs `${CLAUDE_PLUGIN_ROOT}/hooks/loop-plan-file-validator.sh` to validate active RLCR state before accepting more user input (`hooks/hooks.json:4-12`). On `PreToolUse`, it routes `Write`, `Edit`, `Read`, and `Bash` tool calls to dedicated validators (`hooks/hooks.json:14-50`). On `Stop`, it runs both `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` and `${CLAUDE_PLUGIN_ROOT}/hooks/pr-loop-stop-hook.sh` with 7200 second timeouts (`hooks/hooks.json:52-64`).
- inputs_outputs_state: Input is Claude Code's hook event stream and the matching tool/event names. Outputs are the referenced hook command results, typically JSON decisions such as allow/block from validators or stop-hook phase prompts. This config itself has no mutable state; state is held under active `.humanize/rlcr` and `.humanize/pr-loop` directories by the hook scripts.
- gates_or_invariants: The routing invariant is event/tool-specific separation: prompt submission validates plan/branch/tracking state, mutating tools validate file/path permissions, shell commands are scanned for bypasses, and stop events run loop transition logic. The stop hooks are explicitly long-running tolerant via `timeout: 7200`.
- dependencies_and_callers: Consumed by Claude Code's plugin hook loader. It depends on `CLAUDE_PLUGIN_ROOT` resolving to the plugin root and on executable scripts at `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. Directly inspected related behavior: plan validator fail-closes malformed state and git timeout/errors (`hooks/loop-plan-file-validator.sh:44-48`, `hooks/loop-plan-file-validator.sh:95-115`); write/edit/bash validators protect state, prompt, todos, summaries, goal tracker, plan backup, and PR-loop read-only files (`hooks/loop-write-validator.sh:51-68`, `hooks/loop-write-validator.sh:147-195`, `hooks/loop-edit-validator.sh:112-149`, `hooks/loop-bash-validator.sh:88-118`, `hooks/loop-bash-validator.sh:127-395`); stop hook performs git-clean/review/finalize transitions (`hooks/loop-codex-stop-hook.sh:396-424`, `hooks/loop-codex-stop-hook.sh:997-1036`).
- edge_cases_or_failure_modes: Misconfigured `CLAUDE_PLUGIN_ROOT` makes every hook command path invalid. Missing or non-executable scripts leave the registry present but ineffective. Since both RLCR and PR stop hooks run on `Stop`, they must independently no-op when their loop state is absent. Hook order matters for `Stop`: RLCR stop hook is listed before PR stop hook (`hooks/hooks.json:55-63`).
- validation_or_tests: Hook registry references are covered indirectly by `tests/test-template-references.sh` and hook behavior by `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, and robustness hook tests found by repository search. This item is a configuration dispatch point, not an executable test itself.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:gen-plan` command contract. It validates command metadata, supporting agent metadata, version consistency, content constraints, and the IO validator script's exit-code semantics.
- algorithmic_behavior: Initializes project paths to `commands`, `agents`, and repository root (`tests/test-gen-plan.sh:11-15`), maintains pass/fail counters (`tests/test-gen-plan.sh:22-40`), then runs positive tests PT-1 through PT-9 and negative/script tests. Positive tests require `commands/gen-plan.md` to exist, expose YAML `description`, `allowed-tools`, and `argument-hint`, and require `agents/draft-relevance-checker.md` to exist with `name: draft-relevance-checker`, `model: haiku`, and `tools` metadata (`tests/test-gen-plan.sh:54-157`). Version consistency is checked across `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md` (`tests/test-gen-plan.sh:160-180`).
- inputs_outputs_state: Inputs are repository files plus temporary invalid fixtures under `mktemp -d` (`tests/test-gen-plan.sh:191-193`). Outputs are colored PASS/FAIL lines, summary counts, and process exit `0` only when `TESTS_FAILED == 0` (`tests/test-gen-plan.sh:544-562`). State is local shell counters plus temporary fixture directories cleaned by `trap`.
- gates_or_invariants: Naming must match `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:195-199`). Command frontmatter must start with `---` and include `description` (`tests/test-gen-plan.sh:201-206`); agent frontmatter must include `name` and `description` (`tests/test-gen-plan.sh:208-214`). YAML syntax check rejects malformed non-key/list/quoted lines (`tests/test-gen-plan.sh:304-319`). Model names must be exact aliases `opus|sonnet|haiku` or full IDs prefixed by `claude-`, `gpt-`, `o[0-9]`, or `gemini-`; partial aliases like `opus-v2` are rejected (`tests/test-gen-plan.sh:350-390`). Command and agent content must avoid Emoji/CJK characters (`tests/test-gen-plan.sh:403-423`).
- dependencies_and_callers: Depends on `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. The command contract it tests says IO validation must execute `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` and map exit codes 0-7 to stop/continue behavior (`commands/gen-plan.md:30-47`). The relevance checker agent is required to return `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:27-36`).
- edge_cases_or_failure_modes: The YAML syntax validator is intentionally lightweight and may not validate full YAML semantics; it checks enough structure for this plugin contract. `grep -P` content checks degrade silently if PCRE support is unavailable because stderr is redirected and the condition simply fails. Two `trap "rm -rf ..."` assignments are used, and the second trap for `SCRIPT_TEST_DIR` overwrites the first fixture cleanup trap (`tests/test-gen-plan.sh:191-193`, `tests/test-gen-plan.sh:433-436`), leaving a possible temp cleanup gap if both dirs exist.
- validation_or_tests: Script tests `scripts/validate-gen-plan-io.sh` for missing flag values, unknown/help options, missing input, empty input, missing output directory, existing output, output path as directory, valid paths, and expected exit codes (`tests/test-gen-plan.sh:431-541`). The validator itself documents exit codes 0-7 (`scripts/validate-gen-plan-io.sh:4-12`), normalizes paths, validates input/output conditions, locates `prompt-template/plan/gen-plan-template.md`, copies it, and appends original draft content (`scripts/validate-gen-plan-io.sh:69-160`).
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt template for an RLCR stop-hook safety gate when `git status --porcelain` cannot be trusted. It is a fail-closed message, not executable logic.
- algorithmic_behavior: Renders a fixed heading and substitutes `{{GIT_STATUS_EXIT}}` into the failure sentence (`prompt-template/block/git-status-failed.md:1-4`). It explains likely causes: git lock contention, invalid repository state, or a large/slow repository (`prompt-template/block/git-status-failed.md:5-8`), then instructs manual git status verification (`prompt-template/block/git-status-failed.md:10`).
- inputs_outputs_state: Input is `GIT_STATUS_EXIT`, passed by `hooks/loop-codex-stop-hook.sh`. Output is Markdown rendered into the stop-hook JSON `reason`. No state mutation occurs in the template.
- gates_or_invariants: The invariant is that inability to verify repository cleanliness blocks loop progress. The stop hook caches git status once and explicitly treats non-zero `git status` exit as blocking rather than defaulting to clean (`hooks/loop-codex-stop-hook.sh:396-424`).
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` in `hooks/loop-codex-stop-hook.sh:419-420`. Template rendering is single-pass and preserves missing placeholders, preventing injected `{{...}}` content from being recursively expanded (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-131`). The same stop-hook block emits `{"decision":"block", "systemMessage":"Loop: Blocked - git status failed ..."}` (`hooks/loop-codex-stop-hook.sh:421-423`).
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook uses an inline fallback with the same variable (`hooks/loop-codex-stop-hook.sh:414-420`). If `GIT_STATUS_EXIT` is unset upstream, the placeholder would remain or render empty depending on caller, but inspected caller initializes it to `0` before capture (`hooks/loop-codex-stop-hook.sh:409-410`).
- validation_or_tests: Template reference coverage exists via repository tests found for template references. Behavioral coverage is tied to stop-hook tests that simulate git/status failures and assert blocking behavior in RLCR flows.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-114 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Review-phase remediation prompt template for the RLCR state machine. It instructs Claude to fix Codex review findings and keeps the loop in review until no `[P0-9]` severity markers remain.
- algorithmic_behavior: Inserts review output at `{{REVIEW_CONTENT}}` (`prompt-template/claude/review-phase-prompt.md:5-8`) and the required summary path at `{{SUMMARY_FILE}}` (`prompt-template/claude/review-phase-prompt.md:11-15`). It instructs the worker to address all `[P0-9]` issues, make fixes only, commit changes, and write a summary (`prompt-template/claude/review-phase-prompt.md:9-22`). It states the COMPLETE signal is ignored during review and the loop continues until no `[P0-9]` issues are found (`prompt-template/claude/review-phase-prompt.md:23-28`).
- inputs_outputs_state: Inputs are `REVIEW_CONTENT` from Codex review output and `SUMMARY_FILE` pointing to the next round summary. Output is a generated `round-N-prompt.md` file and a blocking hook response containing that prompt. State transition: the stop hook updates `current_round` in `state.md`, writes the review prompt for the new round, and returns a block decision to continue remediation (`hooks/loop-codex-stop-hook.sh:1125-1169`).
- gates_or_invariants: The review gate is severity-marker based. `run_and_handle_code_review` runs `codex review`, treats review command failure or missing stdout as hard blocking errors, calls review remediation when markers are detected, and enters finalize only when no issues are found (`hooks/loop-codex-stop-hook.sh:997-1036`). A separate invariant prevents manual toggling into review phase: `review_started=true` requires `.review-phase-started` marker (`hooks/loop-codex-stop-hook.sh:1455-1471`).
- dependencies_and_callers: Loaded by `continue_review_loop_with_issues` through `load_and_render_safe "$TEMPLATE_DIR" "claude/review-phase-prompt.md"` (`hooks/loop-codex-stop-hook.sh:1157-1159`). The review phase begins after implementation `COMPLETE` if a `BASE_BRANCH` is available; otherwise review can be skipped into finalize with a warning (`hooks/loop-codex-stop-hook.sh:1403-1443`). Template rendering uses the single-pass loader, important because review content may itself contain template-looking braces (`hooks/lib/template-loader.sh:54-57`).
- edge_cases_or_failure_modes: The prompt explicitly neutralizes `COMPLETE` as an exit signal in review, so only review output can end the loop. If Codex review fails, the loop blocks instead of finalizing (`hooks/loop-codex-stop-hook.sh:1014-1018`). If the template is missing, an inline fallback prompt with the same core instructions is used (`hooks/loop-codex-stop-hook.sh:1142-1159`).
- validation_or_tests: The review/finalize flow is exercised by `tests/test-finalize-phase.sh` references found in repository search, including calls into `loop-codex-stop-hook.sh`. Template loader tests include placeholder behavior with `REVIEW_CONTENT`, protecting the review prompt from accidental recursive substitution.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-144 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for `scripts/portable-timeout.sh`, which is a shared dependency for git, GitHub CLI, Codex, setup, RLCR, and PR-loop operations. It verifies timeout implementation detection, exit-code normalization, argument handling, and edge-case behavior.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-18`), then runs 18 tests. Positive tests assert detection, valid implementation name, quick command success, timeout exit `124`, argument preservation, command exit-code preservation, and pipeline execution through `sh -c` (`tests/robustness/test-timeout-robustness.sh:31-110`). Negative/edge tests cover short timeout elapsed time, zero timeout, large output, rapid cycles, nonexistent command, empty command, special characters, signal handling, fallback detection, subshells, and `TIMEOUT_IMPL` export (`tests/robustness/test-timeout-robustness.sh:120-272`).
- inputs_outputs_state: Inputs are local system commands (`echo`, `sleep`, `printf`, `sh`, `seq`, `wc`) and whichever timeout implementation is installed. Outputs are PASS/FAIL lines and final exit status from `print_test_summary` (`tests/robustness/test-timeout-robustness.sh:274-279`). State is limited to helper counters, `TIMEOUT_IMPL`, elapsed timestamps, and local loop variables.
- gates_or_invariants: Accepted timeout implementation names are exactly `gtimeout`, `timeout`, `python3`, `python`, or `none` (`tests/robustness/test-timeout-robustness.sh:41-49`, `tests/robustness/test-timeout-robustness.sh:235-247`). Timeout must return `124` when implementation exists and a command exceeds the deadline (`tests/robustness/test-timeout-robustness.sh:62-77`). Successful commands must preserve stdout and their real exit codes (`tests/robustness/test-timeout-robustness.sh:51-60`, `tests/robustness/test-timeout-robustness.sh:89-100`).
- dependencies_and_callers: Depends directly on `scripts/portable-timeout.sh`, whose detection priority is `gtimeout` > GNU `timeout` > `python3` > `python` > `none` (`scripts/portable-timeout.sh:6-29`). `run_with_timeout` delegates to the selected command, uses Python `subprocess.run(..., timeout=...)` for Python fallback, normalizes timeout expiration to `124`, and runs without timeout when no implementation exists (`scripts/portable-timeout.sh:31-71`). The helper is widely used by setup scripts, hook validators, RLCR/PR stop hooks, and GitHub API scripts; repository search found calls in `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and PR reviewer/bot scripts.
- edge_cases_or_failure_modes: Tests accept implementation-dependent behavior for zero timeout as either success or `124` (`tests/robustness/test-timeout-robustness.sh:139-155`) and signal-handling behavior as either `0` or `124` (`tests/robustness/test-timeout-robustness.sh:221-233`). The Python fallback does not capture child stdout/stderr itself, so output streams pass through the subprocess. If no timeout implementation exists, timeout-specific assertions are skipped by marking them pass as "Skipped", so the suite confirms graceful degradation rather than enforcing hard timeout support (`tests/robustness/test-timeout-robustness.sh:65-77`, `scripts/portable-timeout.sh:64-69`).
- validation_or_tests: This file is itself the validation. It uses `tests/test-helpers.sh` counters and `print_test_summary`, which returns `0` only when there are no failures (`tests/test-helpers.sh:58-77`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item evidence sections present, one for each assigned row
- missing_items: none
- duplicate_items: none detected in item evidence headings
- final_worker_status: `complete`