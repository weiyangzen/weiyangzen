# agent_08 feature/codex-bypass-sandbox-env 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `f0f1ad947157c3d1e9d0bdd58bf36aae92075cc6`

## Item Evidence

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing behavior contract for the RLCR and PR-loop workflows. It defines the two-phase Claude/Codex loop, CLI commands, state directory expectations, PR review loop semantics, and the new `HUMANIZE_CODEX_BYPASS_SANDBOX` runtime switch.
- algorithmic_behavior: Documents RLCR as an iterative state machine: plan input -> Claude implementation summary -> Codex summary review -> feedback loop -> `COMPLETE` -> `codex review --base <branch>` -> fix or done. See `README.md:99-112`. PR loop behavior is similarly documented as PR detection -> bot comment fetch -> Claude fixes -> push/re-trigger -> stop hook polling -> local Codex validation -> repeat until approvals or max iterations, `README.md:202-210`.
- inputs_outputs_state: Inputs include plan files for `/humanize:start-rlcr-loop`, draft/output files for `/humanize:gen-plan`, PR bot flags for `/humanize:start-pr-loop`, max iteration limits, Codex model/effort, timeouts, tracking/push flags, and base branch override, `README.md:144-162`, `README.md:167-181`, `README.md:187-199`. Outputs/state are `.humanize/rlcr/<timestamp>/` monitorable loop artifacts and PR-loop monitor state, `README.md:123-128`, `README.md:217-220`.
- gates_or_invariants: The docs define several workflow gates: Codex review must emit `COMPLETE` before code review phase; code review uses `[P0-9]` severity markers; PR loop requires GitHub CLI, Codex CLI, and an open associated PR, `README.md:109-115`, `README.md:212-215`. Base branch auto-detection priority is remote default -> `main` -> `master`, `README.md:156-157`.
- dependencies_and_callers: The documented sandbox bypass variable is implemented in both RLCR Codex execution and PR-loop local Codex validation. `hooks/loop-codex-stop-hook.sh:898-906` maps unset/non-true to `--full-auto` and `true`/`1` to `--dangerously-bypass-approvals-and-sandbox`. `hooks/pr-loop-stop-hook.sh:1328-1336` applies the same rule for PR-loop Codex execution. README command docs correspond to `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: The bypass option is explicitly dangerous: unrestricted filesystem access and arbitrary command execution without approval prompts, `README.md:60-84`. Intended use cases are trusted Linux/CI/development environments where sandboxing fails or is controlled, `README.md:70-73`; public/shared/untrusted/production contexts are rejected by documentation, `README.md:75-79`.
- validation_or_tests: No direct README test, but implementation references show the env-var behavior is wired in both Codex command builders. Base-branch behavior documented in README is executable-tested by `tests/robustness/test-base-branch-detection.sh`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-038 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime polling script for PR-loop state transitions. It fetches GitHub issue comments, inline review comments, and PR review summaries after a timestamp, filters them to watched bots, and emits machine-readable JSON consumed by the PR stop hook.
- algorithmic_behavior: Parses positional PR number plus required `--after` and `--bots` options, validates numeric PR number, requires `gh` and `jq`, resolves the correct base repository for fork PRs, maps logical bot names to GitHub authors, fetches three comment streams, normalizes them into a common schema, filters by timestamp and bot regex, then emits `{comments,bots_responded,has_new_comments,comment_count}` JSON, `scripts/poll-pr-reviews.sh:23-102`, `108-187`, `193-327`.
- inputs_outputs_state: Inputs are `<pr_number>`, ISO-8601 `--after`, comma-separated bot aliases, current GitHub repo context, and GitHub API responses. Output is JSON on stdout with normalized comment objects and booleans, `scripts/poll-pr-reviews.sh:316-326`. It uses a temp directory for intermediate JSON and removes it on exit, `scripts/poll-pr-reviews.sh:193-199`.
- gates_or_invariants: Required args and prerequisites are fail-fast gates, `scripts/poll-pr-reviews.sh:83-116`. PR repo resolution must find the PR in the current repo or parent repo, which protects fork PR polling, `scripts/poll-pr-reviews.sh:122-150`. `codex` maps specially to `chatgpt-codex-connector[bot]`; other bot aliases map to `<bot>[bot]`, `scripts/poll-pr-reviews.sh:164-172`.
- dependencies_and_callers: Called by `hooks/pr-loop-stop-hook.sh` through `POLL_SCRIPT="$PLUGIN_ROOT/scripts/poll-pr-reviews.sh"`, then inside the polling loop with waiting bots, `hooks/pr-loop-stop-hook.sh:801-811`, `960-970`. The stop hook maps returned authors back to bot names and updates per-bot response state, `hooks/pr-loop-stop-hook.sh:967-982`.
- edge_cases_or_failure_modes: API fetches retry up to 3 times, warn on stderr, increment an internal failure count, then return `[]` with status 0 so transient/partial API failure does not crash the PR loop, `scripts/poll-pr-reviews.sh:200-235`. Empty-body PR reviews are preserved using `[Review state: <state>]`, so approval-only reviews are not dropped, `scripts/poll-pr-reviews.sh:276-290`. Timestamp comparison is lexicographic ISO string comparison, so callers must provide comparable UTC-style timestamps.
- validation_or_tests: Fixture-backed tests assert approval-only PR reviews are represented with placeholders and match bot filters, `tests/test-pr-loop-hooks.sh:1160-1219`. Additional fixture tests run this script with mock `gh`, validate JSON, require approval-only reviews in output, and verify `--after` excludes earlier comments, `tests/test-pr-loop-hooks.sh:1322-1419`. Robustness tests cover help, missing args, mocked JSON shape, slow API behavior, and API failure returning valid JSON with boolean `has_new_comments:false`, `tests/robustness/test-pr-loop-api-robustness.sh:619-843`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-068 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for prompt-template loading and single-pass rendering semantics. It defines the contract for `hooks/lib/template-loader.sh`, which is used by RLCR/PR hooks to generate blocking messages and prompts.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, builds a project-root-relative template dir, then runs assertions for `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`, `tests/test-template-loader.sh:10-13`, `41-243`. The tested implementation computes `prompt-template` from `hooks/lib`, loads files or returns empty with warning, and uses an awk single-pass scanner over `{{VAR}}` placeholders, `hooks/lib/template-loader.sh:24-48`, `50-132`.
- inputs_outputs_state: Test inputs are inline template strings, real template files such as `block/git-push.md` and `block/wrong-round-number.md`, fallback strings, and `VAR=value` bindings. Outputs are rendered strings compared exactly against expected values, plus pass/fail counters and exit 0/1 summary, `tests/test-template-loader.sh:20-34`, `641-659`.
- gates_or_invariants: The core invariant is single-pass substitution: provided variables replace matching placeholders once, missing variables stay literal, unused variables are ignored, and placeholders embedded in variable values are not expanded later, `tests/test-template-loader.sh:167-194`, `488-543`. `load_and_render_safe` must fall back on missing/empty templates, while `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories, `tests/test-template-loader.sh:196-243`; implementation lines are `hooks/lib/template-loader.sh:170-222`.
- dependencies_and_callers: The test covers a shared hook library used across loop validators and stop hooks. Examples include `hooks/lib/loop-common.sh:144-147` initializing `TEMPLATE_DIR`, `hooks/loop-codex-stop-hook.sh` rendering review prompts, `hooks/pr-loop-stop-hook.sh` rendering PR-loop blocks, and Bash/Write validators rendering safety messages.
- edge_cases_or_failure_modes: Tests 15-33 require shell metacharacters to render literally: `&`, backslashes, `$`, backticks, pipes, semicolons, globs, parentheses, redirection syntax, quotes, comments, tilde, regex-like strings, JSON, and multiline values, `tests/test-template-loader.sh:247-486`. Tests 34-36 prevent placeholder injection from review content, `tests/test-template-loader.sh:488-543`. Tests 37-41 cover empty variables, non-ASCII bytes, underscore-prefixed variables, and numeric variable names, `tests/test-template-loader.sh:545-639`.
- validation_or_tests: This file is itself the validation asset. Related broader coverage exists in `tests/test-templates-comprehensive.sh`, `tests/test-error-scenarios.sh`, and robustness template stress/error tests, but this assigned file is the concise canonical loader contract.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-098 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR Bash-tool guard. It tells Claude that Bash-based writes to round summary files are forbidden and directs use of Write/Edit so round/path validation hooks run.
- algorithmic_behavior: The template has one variable, `{{CORRECT_PATH}}`, rendered into the required destination path, `prompt-template/block/summary-bash-write.md:1-8`. It is loaded by `summary_bash_blocked_message` through `load_and_render_safe`, which supplies a fallback if the template is missing, `hooks/lib/loop-common.sh:481-490`.
- inputs_outputs_state: Input is the current round’s correct summary path, normally `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`. Output is a human-readable block message on stderr when Bash attempts to modify `round-N-summary.md`.
- gates_or_invariants: The invariant is that summary files must be modified through Write/Edit, not shell redirection or shell editing tools, because Bash commands bypass validation hooks. The enforcement gate is in `hooks/loop-bash-validator.sh`: after validating hook JSON, active loop state, and current round, it detects Bash commands that modify `round-[0-9]+-summary.md`, renders this message, and exits 2 to block, `hooks/loop-bash-validator.sh:24-49`, `74-87`, `370-378`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for safe rendering and `hooks/lib/loop-common.sh` for wrapper message construction. It coordinates with Write/Edit validators that validate summary path, round number, and active loop directory when proper tools are used, e.g. `hooks/loop-write-validator.sh:198-263`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to a hardcoded Bash Write Blocked message, `hooks/lib/loop-common.sh:485-489`, `hooks/lib/template-loader.sh:170-202`. Detection quality depends on `command_modifies_file` recognizing the Bash command’s write target; the broader Bash validator also blocks state, prompt, plan backup, and goal-tracker bypasses before summary modification handling, `hooks/loop-bash-validator.sh:121-379`.
- validation_or_tests: Template reference coverage includes `tests/test-template-references.sh` listing `block/summary-bash-write.md` as a referenced template. The rendering mechanism and fallback behavior are covered by `tests/test-template-loader.sh`, especially `load_and_render_safe` tests at `tests/test-template-loader.sh:196-222`.
- skip_candidate: `no`

### FEATURE__CODEX_BYPASS_SANDBOX_ENV-HZ-128 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Executable robustness spec for RLCR base-branch auto-detection. It isolates and tests the branch-selection algorithm used by `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: Defines `detect_base_branch(project_root, git_timeout)` mirroring setup logic: ask `git remote show origin` for `HEAD branch`, accept it if not empty/unknown, otherwise fall back to local `main`, then local `master`, otherwise return failure, `tests/robustness/test-base-branch-detection.sh:35-64`. It uses `run_with_timeout` for portable macOS/Linux command timeouts, `tests/robustness/test-base-branch-detection.sh:19`, `scripts/portable-timeout.sh:31-71`.
- inputs_outputs_state: Inputs are temporary Git repositories with different branch/remote layouts. Output is the detected branch name on stdout or nonzero failure. Test state is isolated with `setup_test_dir`, temp repos, local commits, and pass/fail counters from `tests/test-helpers.sh`, `tests/robustness/test-base-branch-detection.sh:24-29`, `tests/test-helpers.sh:84-89`.
- gates_or_invariants: The production algorithm priority is user-specified branch first, then remote default if present locally, then local `main`, then local `master`, then hard error, `scripts/setup-rlcr-loop.sh:589-643`. Production additionally validates user-specified base branches exist locally, detects remote-only branch errors, rejects YAML-unsafe branch names, and captures the base commit SHA so later diffs do not collapse when the base ref moves, `scripts/setup-rlcr-loop.sh:594-610`, `645-661`.
- dependencies_and_callers: Base branch state is written during RLCR setup and later consumed by Codex review phase. `hooks/loop-codex-stop-hook.sh` requires `STATE_BASE_BRANCH`, prefers captured `BASE_COMMIT` as review base, and uses the base branch in review prompts and final review commands, as shown by references around `hooks/loop-codex-stop-hook.sh:930-959` and README command docs at `README.md:156-157`.
- edge_cases_or_failure_modes: Tests cover no origin with local `main`, no origin with local `master`, no origin and neither branch causing failure, and a simulated origin remote, `tests/robustness/test-base-branch-detection.sh:66-191`. The `remote show origin ... || true` guard is important under `set -euo pipefail`; missing origin must not terminate before local fallback, `tests/robustness/test-base-branch-detection.sh:41-44`.
- validation_or_tests: The file is included in the aggregate runner via `tests/run-all-tests.sh:66`. Its own summary returns success only when no failures are recorded, `tests/robustness/test-base-branch-detection.sh:193-198`. I inspected tests only and did not execute them, preserving the requested read-only research scope.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present above, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`