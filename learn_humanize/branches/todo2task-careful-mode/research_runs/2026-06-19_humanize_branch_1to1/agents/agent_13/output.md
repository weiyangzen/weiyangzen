# agent_13 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Plan-generation prompt template directory. Recursive inspection found one descendant: `prompt-template/plan/gen-plan-template.md`. This directory defines the structured plan contract that later RLCR loop setup consumes as the human-authored goal source.
- algorithmic_behavior: `gen-plan-template.md` is a skeleton for transforming a free-form draft into a deterministic implementation plan. It requires `Goal Description`, TDD-style `Acceptance Criteria`, `Path Boundaries`, feasibility hints, dependency sequencing, and code-style constraints. The acceptance criteria section mandates positive and negative tests per AC at `prompt-template/plan/gen-plan-template.md:6-23`. Path boundary semantics are defined at `prompt-template/plan/gen-plan-template.md:25-44`. Dependency sequencing and implementation terminology constraints are at `prompt-template/plan/gen-plan-template.md:57-74`.
- inputs_outputs_state: Input is a draft document passed to the `gen-plan` command. `scripts/validate-gen-plan-io.sh` validates `--input` and `--output`, copies this template to the output, appends the original draft, then exits success at `scripts/validate-gen-plan-io.sh:126-160`. The final output is a plan file containing both structured plan content and the preserved original draft, per `commands/gen-plan.md:251-260`.
- gates_or_invariants: IO validation gates reject missing input, empty input, missing output directory, existing output path, missing write permission, invalid args, or missing template with explicit exit codes at `scripts/validate-gen-plan-io.sh:4-12` and checks at `scripts/validate-gen-plan-io.sh:79-146`. Generation rules require AC-X/AC-X.Y format, no time estimates, no line-number references, TDD-style tests, affirmative path boundaries, and complete preservation of all draft information at `commands/gen-plan.md:225-247`.
- dependencies_and_callers: Called by `commands/gen-plan.md`, whose allowed tools include `scripts/validate-gen-plan-io.sh`, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` at `commands/gen-plan.md:1-12`. The command workflow validates IO, checks repository relevance, analyzes clarity/consistency/completeness/functionality, resolves issues with the user, generates plan content, reviews the full plan, optionally unifies language, and reports the result at `commands/gen-plan.md:19-47`, `commands/gen-plan.md:50-72`, `commands/gen-plan.md:76-138`, and `commands/gen-plan.md:262-304`.
- edge_cases_or_failure_modes: Template absence is treated as plugin configuration error exit 7. Existing output files are rejected to avoid overwriting. Mixed-language output triggers an explicit user question rather than automatic translation. Highly deterministic drafts are handled by allowing upper/lower path bounds to converge, documented at `prompt-template/plan/gen-plan-template.md:44`.
- validation_or_tests: `scripts/validate-gen-plan-io.sh` is the direct executable validator for this directory. It verifies paths and then materializes the template plus original draft. No direct unit test was assigned for this directory, but template references are also covered by repository-wide template reference tests found via `tests/test-template-references.sh`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-043 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor deletion behavior and terminal restoration during an RLCR session. It is not the implementation, but it concretely specifies a user-visible state transition: active monitor to graceful stop when `.humanize/` is removed.
- algorithmic_behavior: The script has three modes. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99/`, writes a minimal `state.md`, and writes a minimal `goal-tracker.md` at `tests/manual-monitor-test.sh:23-46`. `delete` removes `.humanize` and tells the tester to observe clean shutdown behavior at `tests/manual-monitor-test.sh:47-57`. `cleanup` removes leftovers at `tests/manual-monitor-test.sh:58-62`.
- inputs_outputs_state: Input is the first CLI argument: `setup`, `delete`, or `cleanup`. Output is terminal guidance plus filesystem side effects in `.humanize/`. The seeded `state.md` includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` at `tests/manual-monitor-test.sh:26-32`; the seeded goal tracker has an immutable goal/AC scaffold at `tests/manual-monitor-test.sh:33-38`.
- gates_or_invariants: The usage gate rejects any other argument with exit 1 at `tests/manual-monitor-test.sh:63-71`. The manual expected invariant is that `humanize monitor rlcr` exits with `Monitoring stopped: .humanize/rlcr directory no longer exists`, restores terminal scroll/cursor state, and does not emit shell glob errors, documented at `tests/manual-monitor-test.sh:51-56`.
- dependencies_and_callers: The user is instructed to run `source scripts/humanize.sh && humanize monitor rlcr` in another terminal at `tests/manual-monitor-test.sh:10-14` and `tests/manual-monitor-test.sh:43-45`. The implementation path is `scripts/humanize.sh`: `_graceful_stop` calls `_cleanup`, restores terminal state, and prints the stop reason at `scripts/humanize.sh:580-593`. The monitor loop checks for missing `.humanize/rlcr` and returns 0 at `scripts/humanize.sh:637-643`, `scripts/humanize.sh:679-683`, and `scripts/humanize.sh:772-775`.
- edge_cases_or_failure_modes: This is intentionally manual because it verifies terminal rendering and cleanup across two terminals. It uses `rm -rf .humanize`, so it is destructive to local loop state and should only run in a disposable/test project. The seeded state lacks YAML frontmatter used by stricter hook parsers, but monitor deletion behavior only needs a session directory and minimal files.
- validation_or_tests: Complements automated monitor tests such as `tests/test-monitor-runtime.sh` and `tests/test-monitor-e2e-real.sh`, found by reference search. Its acceptance criteria are observational: clean message, restored terminal, no shell errors.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-073 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared hook/validator library for RLCR and PR-loop state-machine enforcement. It centralizes constants, JSON input validation, loop discovery, strict state parsing, review issue detection, file/path classification, cancellation authorization, git protection, shell mutation detection, loop termination, and PR goal-tracker updates.
- algorithmic_behavior: Hook input validation rejects null bytes, invalid UTF-8, invalid JSON, and missing `tool_name`, setting `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` at `hooks/lib/loop-common.sh:51-97`; required `tool_input` fields are enforced at `hooks/lib/loop-common.sh:99-115`; deep JSON is rejected at `hooks/lib/loop-common.sh:117-134`. Active loop discovery selects only the newest directory with `state.md` or `finalize-state.md`, preventing older sessions from reviving, at `hooks/lib/loop-common.sh:152-175`. State parsing has tolerant mode at `hooks/lib/loop-common.sh:192-248` and strict fail-closed mode at `hooks/lib/loop-common.sh:250-336`. Review issue detection scans Codex logs for `[P?]` markers near line starts and writes `round-N-review-result.md` at `hooks/lib/loop-common.sh:338-398`.
- inputs_outputs_state: Inputs include hook JSON, loop directory paths, state files, command strings, bot result JSON, and tracker files. Outputs are exported/shell variables, stdout path/status values, stderr block messages, rendered templates, return codes, renamed state files, and updated PR goal trackers. `end_loop` converts active `state.md` to `{complete|cancel|maxiter|stop|unexpected}-state.md` at `hooks/lib/loop-common.sh:1118-1151`, which is a direct RLCR state transition.
- gates_or_invariants: Strict state parsing requires frontmatter separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`; numeric fields and boolean `review_started` are validated at `hooks/lib/loop-common.sh:267-336`. File gates classify round files, goal tracker, state/finalize-state, summary, RLCR loop paths, and PR-loop paths at `hooks/lib/loop-common.sh:405-447`, `hooks/lib/loop-common.sh:509-536`, and `hooks/lib/loop-common.sh:718-840`. Cancellation is heavily constrained: signal file required, no command substitution, no newlines/chaining/operators, normalized `$loop_dir`, exact `mv state.md|finalize-state.md cancel-state.md`, no symlink source at `hooks/lib/loop-common.sh:539-716`. Git-add protection blocks direct `.humanize`, force/broad adds, and `git add -A` while `.humanize` exists at `hooks/lib/loop-common.sh:914-1032`. Bash mutation detection catches redirects, `tee`, in-place editors, `mv/cp/rm`, `dd`, `truncate`, `printf >`, and `exec >` at `hooks/lib/loop-common.sh:1074-1102`.
- dependencies_and_callers: Sourced by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, setup scripts, and many tests. Read validation uses its JSON/state/path helpers to block todos, wrong locations, wrong rounds, and wrong active directory at `hooks/loop-read-validator.sh:25-170`. Write validation blocks todos, prompts, PR read-only files, state files, plan backups, post-round-0 goal tracker writes, wrong summary locations/rounds/directories at `hooks/loop-write-validator.sh:55-263`. Bash validation uses `command_modifies_file`, `git_adds_humanize`, and cancel authorization to block shell bypasses at `hooks/loop-bash-validator.sh:74-445`. Plan-file validation uses strict state constants to enforce schema, branch consistency, and plan tracking status at `hooks/loop-plan-file-validator.sh:44-229`.
- edge_cases_or_failure_modes: `find_active_loop` ignores older active-looking directories by design. `is_state_file_path` can match `finalize-state.md`, so callers must check finalize first; the bash/write validators do so. `git_adds_humanize` receives lowercased input, so `-A` is handled as `-a`. Broad `git add .` is blocked only if `.humanize` exists and is not ignored, while force broad adds are blocked regardless. `command_modifies_file` is pattern-based and documented by tests as not detecting every uncommon form, such as some multi-source `cp` cases. Template loading degrades to fallbacks if templates are missing, because `loop-common.sh` sources `template-loader.sh` and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:140-149`.
- validation_or_tests: Referenced tests include `tests/test-allowlist-validators.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-cancel-signal-file.sh`, `tests/test-codex-review-merge.sh`, `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-pr-loop-system.sh`, and robustness suites for hook input, hook system, cancel security, state files, state transitions, sessions, PR-loop API, setup scripts, and concurrent state. Template reference validation checks loop-common message templates in `tests/test-template-references.sh`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-103 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: User-facing block template for the read-validator transition where a round prompt/summary file is requested outside the active RLCR loop directory.
- algorithmic_behavior: The template tells the agent it is trying to read `{{FILE_PATH}}`, states that loop files are in `{{ACTIVE_LOOP_DIR}}/`, lists current round prompt and summary paths using `{{CURRENT_ROUND}}`, and gives a direct `cat {{FILE_PATH}}` fallback if the user truly needs that file at `prompt-template/block/wrong-file-location.md:1-9`.
- inputs_outputs_state: Inputs are template variables `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Output is a rendered block message to stderr. Rendering is single-pass via `hooks/lib/template-loader.sh`, which keeps missing placeholders literal and prevents recursive placeholder injection at `hooks/lib/template-loader.sh:50-132`.
- gates_or_invariants: It is emitted only after the read validator has found an active loop, parsed strict state, recognized a round prompt/summary file, and determined the requested file is not inside `.humanize/rlcr/` at `hooks/loop-read-validator.sh:69-131`. The validator exits 2 after rendering, so this template is part of the blocking gate.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`, which uses `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-file-location.md"` with an inline fallback at `hooks/loop-read-validator.sh:122-131`. The loader comes through `loop-common.sh` sourcing `hooks/lib/template-loader.sh` at `hooks/lib/loop-common.sh:140-145`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the validator uses its fallback message. The template’s final line says `cat {{FILE_PATH}}`, which is advisory and may appear surprising because the operation was blocked for a Read tool; algorithmically the actual allowed path for current round files is the active loop path.
- validation_or_tests: `tests/test-template-references.sh` explicitly scans `hooks/lib/loop-common.sh` and hook message templates; `hooks/loop-read-validator.sh` is the behavior-level validator for this block. Related wrong-round and wrong-directory templates cover adjacent branches.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-133 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the production goal-tracker parser used by `humanize monitor rlcr`. It verifies that goal/AC/task counts are stable across valid, malformed, large, special-character, and partially corrupted tracker files.
- algorithmic_behavior: The test sources shared helpers and `scripts/humanize.sh`, then calls `humanize_parse_goal_tracker` directly at `tests/robustness/test-goal-tracker-robustness.sh:18-24`. It parses pipe-delimited parser output using `parse_result` for `total_acs`, `completed_acs`, `active_tasks`, `completed_tasks`, `deferred_tasks`, `open_issues`, and `goal_summary` at `tests/robustness/test-goal-tracker-robustness.sh:35-51`. The production parser returns the same fields, defaults missing files to `0|0|0|0|0|0|No goal tracker`, counts ACs in list/table forms, subtracts completed/deferred rows from active tasks, counts unique completed ACs, and truncates goal summary to 60 chars at `scripts/humanize.sh:30-112`.
- inputs_outputs_state: Inputs are temporary `goal-tracker*.md` files created under `TEST_DIR`. Outputs are PASS/FAIL counters from `tests/test-helpers.sh`, printed summary, and process exit status from `print_test_summary` at `tests/robustness/test-goal-tracker-robustness.sh:526-531`. The test mutates only its temp directory, because `setup_test_dir` uses `mktemp -d` and traps cleanup at `tests/test-helpers.sh:84-89`.
- gates_or_invariants: Parser invariants under test include: standard AC list count equals 3 at `tests/robustness/test-goal-tracker-robustness.sh:60-87`; table AC count equals 3 at `tests/robustness/test-goal-tracker-robustness.sh:89-116`; active tasks exclude completed/deferred rows at `tests/robustness/test-goal-tracker-robustness.sh:118-153`; completed task rows and unique completed ACs are separately counted at `tests/robustness/test-goal-tracker-robustness.sh:155-202`; missing files return exact defaults at `tests/robustness/test-goal-tracker-robustness.sh:237-244`; malformed files without proper headers produce zero ACs at `tests/robustness/test-goal-tracker-robustness.sh:304-326`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for `pass`, `fail`, `setup_test_dir`, and `print_test_summary`. Depends on `scripts/humanize.sh`, which itself sources `scripts/lib/monitor-common.sh` if present. `humanize monitor rlcr` uses `humanize_parse_goal_tracker` via its internal `_parse_goal_tracker` bridge and status drawing path, referenced at `scripts/humanize.sh:371`, `scripts/humanize.sh:382`, and `scripts/humanize.sh:404-406`.
- edge_cases_or_failure_modes: Covers empty files, 60 AC items, shell-special characters like `$PATH` and backticks, truncated markdown, binary bytes mixed into content, open issues, deferred tasks, headers-only files, bold/mixed AC formats, sub-numbering that may not count as a normal AC, and long goal summary truncation at `tests/robustness/test-goal-tracker-robustness.sh:246-524`. The production parser in `scripts/humanize.sh` stops AC scanning at `---`, while `scripts/lib/monitor-common.sh` has a related `parse_goal_tracker` that stops at `^##`; this test specifically targets the public `humanize_parse_goal_tracker`.
- validation_or_tests: This file is itself the validation asset. It ends with `print_test_summary "Goal Tracker Robustness Test Summary"` and exits with the helper’s status, so any failed assertion makes the script fail at `tests/robustness/test-goal-tracker-robustness.sh:526-531`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5; each assigned item is represented once as an Item Evidence heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`