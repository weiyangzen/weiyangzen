# agent_30 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-030 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse write gate for RLCR and PR-loop managed files. It protects loop state, prompts, todos, summaries, goal tracking, PR-loop state, and PR-loop generated files from writes that would corrupt the loop state machine.
- algorithmic_behavior: Reads hook JSON from stdin, validates it, exits immediately for non-`Write` tools, then classifies the target `tool_input.file_path`. JSON parsing and required-field validation happen at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-write-validator.sh:25), using shared helpers from `hooks/lib/loop-common.sh`. It blocks `round-*-todos.md` unless the file is one of the explicit active-loop allowlist entries, blocks any `round-*-prompt.md`, applies PR-loop protections, then applies RLCR protections only for summaries, finalize summaries, state files, goal tracker, and `plan.md` backups.
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.file_path`. Output is process exit status plus stderr block text rendered through `load_and_render_safe`. Exit `0` allows the write, exit `1` indicates malformed/unsafe hook or loop state, and exit `2` blocks policy violations. State is read from the newest active `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`; `find_active_loop` only selects the newest timestamped directory with `state.md` or `finalize-state.md` in `hooks/lib/loop-common.sh` lines 157-175.
- gates_or_invariants: Hook input must be valid JSON, UTF-8, no null bytes, and no nesting deeper than 30 via `validate_hook_input` and `is_deeply_nested` in `hooks/lib/loop-common.sh` lines 60-134. Writes to `state.md` and `finalize-state.md` are always blocked after active-loop detection, with finalize checked first because `state.md` matching also catches `finalize-state.md` ([hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-write-validator.sh:140)). Goal tracker writes are blocked after round 0 ([hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-write-validator.sh:192)). Summary writes must target the active loop directory and current round unless allowlisted ([hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-write-validator.sh:228)).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for JSON validation, active-loop lookup, path classification, strict state parsing, allowlist, PR-loop helpers, and block messages. It depends on `jq`, shell utilities, and prompt templates under `prompt-template/block/`, including `wrong-summary-location.md`, `wrong-round-number.md`, `wrong-directory-path.md`, and `plan-backup-protected.md`. Sibling validators use the same helper vocabulary: `hooks/loop-read-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`.
- edge_cases_or_failure_modes: No active loop means most RLCR managed-path writes are allowed after classification ([hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-write-validator.sh:128)), so protection is session-scoped rather than global. Malformed active state fails closed with “Malformed state file” and exit `1`. `finalize-summary.md` is allowed only when it is exactly `$ACTIVE_LOOP_DIR/finalize-summary.md`; the code does not explicitly require `IS_FINALIZE_PHASE=true` despite the comment saying “In Finalize Phase” ([hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-write-validator.sh:168)). PR-loop `round-N-pr-resolve.md` writes are allowed only for the current PR round; generated PR comment, prompt, codex-prompt, check, feedback, and state files are blocked.
- validation_or_tests: Behavior is covered indirectly by hook/path tests such as `tests/test-allowlist-validators.sh`, `tests/test-finalize-phase.sh`, `tests/test-state-exit-naming.sh`, PR-loop hook tests, and robustness tests for hook input and state parsing. The shared helpers include explicit robustness coverage for active-loop selection and malformed state parsing in `tests/robustness/test-concurrent-state-robustness.sh`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-060 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for setup-time plan file validation in `scripts/setup-rlcr-loop.sh`. It defines the accepted and rejected inputs before an RLCR loop can start from a plan file.
- algorithmic_behavior: Creates temporary git repositories, mocks `codex` to avoid real model calls, invokes `scripts/setup-rlcr-loop.sh`, captures exit codes/output, and asserts expected gate messages. Setup uses `mktemp -d`, initializes git, commits baseline content, creates plan files, and adds `plans/` to `.gitignore` in [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-validation.sh:36).
- inputs_outputs_state: Inputs are CLI arguments to `setup-rlcr-loop.sh`, current working directory, git repo state, plan file path/content, branch name, and codex model option. Outputs are PASS/FAIL/SKIP counters and final exit status equal to `$TESTS_FAILED` ([tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-validation.sh:742)). Temporary repo state is intentionally varied between tests: non-git, no-commit git repo, tracked plan, ignored/untracked plan, modified tracked plan, branch with unsafe characters, and content-only test repos.
- gates_or_invariants: The test asserts these invariants from `setup-rlcr-loop.sh`: plan path must be relative, existing, readable, inside project, not symlinked, not contain spaces or shell metacharacters, and not be in a missing parent directory; repository must be git with at least one commit; tracked plan files require `--track-plan-file` and must be clean; untracked/ignored plans are rejected when `--track-plan-file` is used; branch, codex model, and codex effort must be YAML-safe. The implementation gates are in `scripts/setup-rlcr-loop.sh` lines 327-481 for path/tracking and 490-543 for content.
- dependencies_and_callers: Depends on `git`, `chmod`, temp directories, POSIX shell tools, and the repo’s `scripts/setup-rlcr-loop.sh`. The test unsets `CLAUDE_PROJECT_DIR` so setup uses the temp repo rather than the plugin repo ([tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-validation.sh:19)). It injects a mock `codex` into `PATH` at lines 73-83.
- edge_cases_or_failure_modes: The header lists “Submodule rejection,” but this assigned file contains no concrete submodule test; the setup script implements submodule rejection when `.gitmodules` exists in `scripts/setup-rlcr-loop.sh` lines 415-428. Permission-denial handling is conditional: if `chmod 000 plans` is unavailable, the path-resolution test is skipped ([tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-validation.sh:225)). Branch-name tests allow git itself to reject unsafe names before the script runs. The content validator treats Markdown headings beginning with `#` as comments, so valid content tests use plain text headings ([tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-validation.sh:609)).
- validation_or_tests: This file is itself the validation surface. It covers absolute paths, missing files/dirs, spaces, shell metacharacters, file symlink, path-resolution errors, `../` escapes, non-git repos, repos without commits, tracking mode polarity, modified tracked files, YAML-unsafe branch names, blank/sparse/comment-only plans, single-line HTML comment regression, CLI `--plan-file` parsing, conflicting positional/explicit plan paths, and codex model/effort character validation.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-090 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Block-message template for PR-loop trigger gating. It defines the human-facing reason when the PR loop cannot proceed because no required bot-mention trigger comment exists.
- algorithmic_behavior: Static Markdown template with placeholders for `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING`. It tells the operator that the PR loop is waiting for a trigger comment and provides a concrete `@bot` mention example ([prompt-template/block/no-trigger-comment.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/block/no-trigger-comment.md:1)).
- inputs_outputs_state: Inputs are template variables passed by `hooks/pr-loop-stop-hook.sh`. Output is rendered block text embedded into a JSON decision: `{"decision":"block","reason":...,"systemMessage":...}` when `REQUIRE_TRIGGER=true` and `PR_LAST_TRIGGER_AT` is empty ([hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/pr-loop-stop-hook.sh:727)).
- gates_or_invariants: This template represents the invariant that certain PR-loop states require explicit user trigger before review polling or Claude-eyes checks proceed. The stop hook computes this gate from startup case and round: round > 0 always requires trigger, startup cases 4/5 require trigger at round 0, while startup cases 1/2/3 at round 0 may proceed without one.
- dependencies_and_callers: Called via `load_and_render_safe "$TEMPLATE_DIR" "block/no-trigger-comment.md"` in `hooks/pr-loop-stop-hook.sh` lines 739-741. The stop hook derives `BOT_MENTION_STRING` through `build_bot_mention_string` in `hooks/lib/loop-common.sh` lines 770-783 and sets case descriptions locally in `hooks/pr-loop-stop-hook.sh` lines 728-734.
- edge_cases_or_failure_modes: If the template is missing, the stop hook falls back to an inline “Missing Trigger Comment” message, so the algorithm still blocks. The template is informational only; correctness depends on upstream trigger detection and state fields such as `last_trigger_at`, `startup_case`, and `current_round`, not on the template itself.
- validation_or_tests: `tests/test-pr-loop-stophook.sh` builds a case-4 PR-loop state with empty `last_trigger_at`, mocks `gh` to return no comments, runs the stop hook, and expects trigger/mention/comment block output at lines 642-749. `tests/test-pr-loop-hooks.sh` also models the round > 0 missing-trigger invariant at lines 606-621.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-120 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Canonical skeleton for generated implementation plans. It constrains plan output shape, acceptance-criteria format, path boundaries, dependency sequencing, and implementation-note hygiene before a draft becomes an RLCR-ready plan.
- algorithmic_behavior: Static Markdown template copied by `scripts/validate-gen-plan-io.sh` after IO validation succeeds. The script appends the original draft between `--- Original Design Draft Start ---` and `--- Original Design Draft End ---` ([scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/scripts/validate-gen-plan-io.sh:148)). The slash command then asks the agent to generate plan content following the same structure embedded in `commands/gen-plan.md` lines 142-245.
- inputs_outputs_state: Inputs are the input draft file and output target passed through `--input`/`--output`; output is a newly created plan file containing this template plus the draft. The template itself defines sections: title, goal description, acceptance criteria with positive/negative tests, path boundaries, feasibility hints, dependencies/sequence, and implementation notes.
- gates_or_invariants: Acceptance criteria must use AC-style identifiers and include positive and negative deterministic tests ([prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/plan/gen-plan-template.md:6)). Path boundaries must describe upper/lower acceptable scope and allowed choices, with deterministic designs narrowing those choices ([prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/plan/gen-plan-template.md:25)). Implementation code/comments must not contain plan workflow markers such as `AC-`, `Milestone`, `Step`, or `Phase` ([prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/plan/gen-plan-template.md:69)).
- dependencies_and_callers: Used directly by `scripts/validate-gen-plan-io.sh`, resolved from `$CLAUDE_PLUGIN_ROOT/prompt-template/plan/gen-plan-template.md` or script-relative fallback at lines 133-146. The command surface is `commands/gen-plan.md`, which allows the validation script and the `Write`, `Read`, `Glob`, `Grep`, `Task`, and `AskUserQuestion` tools at lines 1-12.
- edge_cases_or_failure_modes: If the template file is missing, `validate-gen-plan-io.sh` exits `7` as a plugin configuration error. The IO script rejects missing input, empty input, missing output directory, existing output path, directory-as-output, and unwritable output directory before copying the template ([scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/scripts/validate-gen-plan-io.sh:79)). The template is a scaffold, so placeholders must be replaced by the command’s generation phase; it is not itself a runtime validator for final plan quality.
- validation_or_tests: `tests/test-gen-plan.sh` validates `validate-gen-plan-io.sh` exit codes for invalid args, missing input, empty input, missing output dir, existing output, output directory, valid paths, and help behavior at lines 426-542.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item sections present in the Item Evidence section; one section per assigned row, in assignment order`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`