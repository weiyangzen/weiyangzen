# agent_27 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-027 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse gate for Claude `Edit` operations against RLCR and PR-loop controlled files. It is a state-machine guard that keeps generated prompts, state files, todos, historical round artifacts, plan backups, and post-Round-0 goal tracker content from being edited through the wrong path.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits immediately unless the tool is `Edit` (`hooks/loop-edit-validator.sh:24-32`). It lowercases the path for pattern checks, blocks `round-N-todos.md` unless explicitly allowlisted (`hooks/loop-edit-validator.sh:38-45`), blocks `round-N-prompt.md` always (`hooks/loop-edit-validator.sh:48-51`), applies PR-loop protections before RLCR protections (`hooks/loop-edit-validator.sh:57-77`), then only continues into RLCR validation if the file is under `.humanize/rlcr` (`hooks/loop-edit-validator.sh:83-85`). For active RLCR loops, it resolves the newest active loop, prefers `finalize-state.md` over `state.md`, parses state strictly, then uses `CURRENT_ROUND` to block state edits, plan backup edits, goal tracker edits after Round 0, and wrong-round summary edits (`hooks/loop-edit-validator.sh:91-183`).
- inputs_outputs_state: Input is a Claude hook payload on stdin plus environment context such as `CLAUDE_PROJECT_DIR`, `PROJECT_ROOT`, `LOOP_BASE_DIR`, and optional `LOOP_DIR`. Output is only process status and stderr block text: exit `0` allows the edit, exit `2` blocks with a user-facing policy message, exit `1` blocks malformed state as a safety failure (`hooks/loop-edit-validator.sh:105-109`). State read includes `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, and `.humanize/pr-loop/<timestamp>/state.md` for PR resolve round validation. It does not mutate state.
- gates_or_invariants: Non-`Edit` tools are outside this validator and pass through (`hooks/loop-edit-validator.sh:27-29`). Prompt files are immutable to Claude edits (`hooks/loop-edit-validator.sh:48-51`). RLCR `state.md` and `finalize-state.md` are system-owned and blocked, with finalize checked first because the generic state matcher also matches it (`hooks/loop-edit-validator.sh:113-125`). `plan.md` backups inside `.humanize/rlcr` are protected (`hooks/loop-edit-validator.sh:131-138`). `goal-tracker.md` becomes read-only to Claude after Round 0; the intended path is a Goal Tracker Update Request in the current summary (`hooks/loop-edit-validator.sh:145-148`, `hooks/lib/loop-common.sh:1104-1116`). Wrong-round summary edits are blocked unless the file is in the small allowlist (`hooks/loop-edit-validator.sh:155-179`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for path classifiers, active-loop discovery, strict state parsing, allowlist checks, PR-loop helpers, and message renderers (`hooks/loop-edit-validator.sh:16-18`). `find_active_loop` selects only the lexicographically newest loop directory and requires `state.md` or `finalize-state.md`, intentionally avoiding older zombie loops (`hooks/lib/loop-common.sh:152-175`). `parse_state_file_strict` requires YAML frontmatter, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric rounds, and boolean `review_started` (`hooks/lib/loop-common.sh:250-336`). Template messages use `load_and_render_safe`, which falls back if templates are missing or render empty (`hooks/lib/template-loader.sh:167-203`). PR-loop checks depend on `is_in_pr_loop_dir`, `is_pr_loop_readonly_file`, and `validate_pr_resolve_round` (`hooks/lib/loop-common.sh:789-912`).
- edge_cases_or_failure_modes: Malformed or incomplete RLCR state fails closed with “Malformed state file” and exit `1` (`hooks/loop-edit-validator.sh:105-109`). If no active RLCR loop exists, edits under `.humanize/rlcr` are allowed because there is no current round to enforce (`hooks/loop-edit-validator.sh:93-97`). Todos and summaries have a hardcoded allowlist limited to `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop only (`hooks/lib/loop-common.sh:426-447`), so later historical edits are blocked unless the allowlist changes. PR `state.md` edits are blocked, PR read-only files are blocked, and `round-N-pr-resolve.md` is allowed only for the current PR loop round (`hooks/loop-edit-validator.sh:59-76`). JSON parsing here uses direct `jq` extraction rather than the library’s stronger `validate_hook_input`, so malformed JSON will fail via `set -euo pipefail` rather than a tailored error path.
- validation_or_tests: `tests/test-allowlist-validators.sh` directly covers the allowlist and edit validator behavior for allowed `round-2-todos.md`, allowed historical `round-1-summary.md`, and blocked `round-4-todos.md` (`tests/test-allowlist-validators.sh:188-227`). `tests/test-plan-file-hooks.sh` verifies the edit validator blocks `plan.md` backups and permits legacy `.humanize-loop.local` paths (`tests/test-plan-file-hooks.sh:240-253`, `tests/test-plan-file-hooks.sh:1095-1105`). `tests/test-template-references.sh` includes `loop-edit-validator.sh` in template-reference and safe-render checks (`tests/test-template-references.sh:60-64`, `tests/test-template-references.sh:176-200`). State parsing and active-loop edge behavior are covered in robustness tests for strict parsing, finalize state, cancel state, and newer active loops (`tests/robustness/test-state-transition-robustness.sh:98-207`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Real executable specification for monitor lifecycle behavior across RLCR and PR-loop monitors. It validates that monitor loops observe filesystem state changes and signals safely while preserving terminal state and avoiding shell glob failures.
- algorithmic_behavior: Creates isolated `/tmp/test-monitor-e2e-real-$$` projects, synthetic `.humanize/rlcr` and `.humanize/pr-loop` session directories, state files, goal trackers, and cache log files, then sources the production `scripts/humanize.sh` from generated runner scripts (`tests/test-monitor-e2e-real.sh:48-56`, `tests/test-monitor-e2e-real.sh:64-158`). It runs the real `_humanize_monitor_codex` under bash and zsh, deletes `.humanize/rlcr`, waits boundedly for exit, and asserts graceful message, deletion reason, no glob errors, cleanup message, scroll-region reset source, and exit code `0` (`tests/test-monitor-e2e-real.sh:160-231`, `tests/test-monitor-e2e-real.sh:326-372`). It repeats the real RLCR monitor flow for SIGINT/Ctrl+C in bash and zsh (`tests/test-monitor-e2e-real.sh:376-550`, `tests/test-monitor-e2e-real.sh:552-683`). It then exercises `humanize monitor pr --once` during directory deletion and continuous `humanize monitor pr` interrupted by SIGINT (`tests/test-monitor-e2e-real.sh:686-824`, `tests/test-monitor-e2e-real.sh:827-990`).
- inputs_outputs_state: Inputs are generated loop directories, `state.md` frontmatter, `goal-tracker.md`, fake cache logs under `XDG_CACHE_HOME`, shimmed terminal commands, optional `zsh`, and OS process signals. Outputs are test pass/fail counters and process output assertions, with final exit `0` only when `TESTS_FAILED` remains zero (`tests/test-monitor-e2e-real.sh:25-37`, `tests/test-monitor-e2e-real.sh:992-1017`). Runtime state transitions include active RLCR session to deleted loop root, active monitor to cleanup on SIGINT, PR monitor once-mode status render to clean exit, and continuous PR monitor to cleanup on SIGINT.
- gates_or_invariants: RLCR monitor must stop cleanly when `.humanize/rlcr` disappears and must say both “Monitoring stopped:” and “directory no longer exists” (`tests/test-monitor-e2e-real.sh:191-202`). Output must not contain zsh/bash glob errors such as “no matches found” or “bad pattern” (`tests/test-monitor-e2e-real.sh:204-209`, `tests/test-monitor-e2e-real.sh:362-365`, `tests/test-monitor-e2e-real.sh:544-549`, `tests/test-monitor-e2e-real.sh:819-824`, `tests/test-monitor-e2e-real.sh:985-990`). Terminal cleanup is asserted through “Stopped monitoring” output and the source-level presence of `printf "\033[r"` in `scripts/humanize.sh` (`tests/test-monitor-e2e-real.sh:211-224`). Graceful RLCR deletion must produce `EXIT_CODE:0` (`tests/test-monitor-e2e-real.sh:226-231`, `tests/test-monitor-e2e-real.sh:368-372`). PR monitor deletion/SIGINT gates are looser: clean exit indicators or evidence that the monitor ran before interruption are accepted (`tests/test-monitor-e2e-real.sh:807-817`, `tests/test-monitor-e2e-real.sh:973-983`).
- dependencies_and_callers: Depends on production `_humanize_monitor_codex` and `_humanize_monitor_pr` from `scripts/humanize.sh` (`tests/test-monitor-e2e-real.sh:148-153`, `tests/test-monitor-e2e-real.sh:776-793`, `tests/test-monitor-e2e-real.sh:917-925`). RLCR monitor depends on shared helpers from `scripts/lib/monitor-common.sh`, including latest-session discovery via `find` to avoid zsh glob errors (`scripts/humanize.sh:237-240`, `scripts/lib/monitor-common.sh:37-63`). `_humanize_monitor_codex` finds project-specific cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<timestamp>` and uses `find` for session/log iteration (`scripts/humanize.sh:242-341`). It parses goal tracker and git status for its status bar (`scripts/humanize.sh:369-424`). PR monitor uses `monitor_find_latest_session`, state-file mapping, latest PR activity file discovery, phase detection, and optional `--once` mode (`scripts/humanize.sh:948-1306`).
- edge_cases_or_failure_modes: Tests cover missing zsh by skipping zsh-only sections (`tests/test-monitor-e2e-real.sh:240-242`, `tests/test-monitor-e2e-real.sh:558-560`). SIGINT delivery to background bash process groups is treated as environment-sensitive; force-kill after timeout can still count as pass if the monitor ran and needed SIGTERM fallback (`tests/test-monitor-e2e-real.sh:492-519`, `tests/test-monitor-e2e-real.sh:938-965`). RLCR monitor itself has deletion checks in the outer loop, no-log polling loop, and incremental log loop (`scripts/humanize.sh:637-643`, `scripts/humanize.sh:674-683`, `scripts/humanize.sh:768-776`), plus session/log deletion switching behavior (`scripts/humanize.sh:710-756`, `scripts/humanize.sh:814-849`). PR continuous monitor lacks an explicit deleted-root graceful-stop check in the same style as RLCR; the test primarily validates no glob errors and interrupt cleanup for continuous PR monitoring (`scripts/humanize.sh:1316-1368`, `tests/test-monitor-e2e-real.sh:827-990`). The test stubs `tput`, `clear`, and sometimes `printf`, so it validates control-flow cleanup more than real terminal rendering fidelity.
- validation_or_tests: This file is itself validation. It verifies production monitor behavior end-to-end rather than stubs, as stated in its header (`tests/test-monitor-e2e-real.sh:3-13`). Additional monitor helper behavior is covered in robustness tests referenced by the repository, including real `_humanize_monitor_codex` invocation for monitor edge cases in `tests/robustness/test-hook-input-robustness.sh` (`tests/robustness/test-hook-input-robustness.sh:438-626` from search evidence) and shared monitor helpers in `scripts/lib/monitor-common.sh`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-087 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the Round 0 goal-tracker initialization gate. It defines what the agent must do when the RLCR stop hook detects placeholder content in the goal tracker before the first implementation round can exit.
- algorithmic_behavior: The template renders a block headed “Goal Tracker Not Initialized,” tells the agent it is in Round 0, lists missing items via `{{MISSING_ITEMS}}`, and instructs the agent to read `{{GOAL_TRACKER_FILE}}`, replace placeholder text with a real Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to ACs, then write the updated `goal-tracker.md` (`prompt-template/block/goal-tracker-not-initialized.md:1-18`). It encodes the state transition from uninitialized Round 0 goal tracker to initialized Round 0 goal tracker as a mandatory retry-before-exit contract.
- inputs_outputs_state: Inputs are template variables `GOAL_TRACKER_FILE` and `MISSING_ITEMS`. The stop hook computes those by reading `.humanize/rlcr/<timestamp>/goal-tracker.md` during Round 0, extracting the Ultimate Goal, Acceptance Criteria, and Active Tasks sections, and checking each section for `[To be ...]` placeholders (`hooks/loop-codex-stop-hook.sh:649-685`). Output is rendered JSON block reason content from `load_and_render_safe`, with `decision: block` and system message “Loop: Goal Tracker not initialized in Round 0” (`hooks/loop-codex-stop-hook.sh:702-719`).
- gates_or_invariants: The gate applies only when not in Finalize Phase, not in Review Phase, `review_started` is not true, `CURRENT_ROUND` is `0`, and the goal tracker file exists (`hooks/loop-codex-stop-hook.sh:651-654`). The immutable goal-tracker section can only be set in Round 0; after Round 0 it is read-only to Claude, enforced separately by edit/write/bash validators (`prompt-template/block/goal-tracker-not-initialized.md:16`, `hooks/loop-edit-validator.sh:142-149`, `hooks/lib/loop-common.sh:1104-1116`). The missing-item list is generated only for sections that still contain placeholder text, so the template can point to partial initialization failures (`hooks/loop-codex-stop-hook.sh:687-700`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` (`hooks/loop-codex-stop-hook.sh:703-709`). Rendering depends on the template loader’s single-pass placeholder substitution, preserving undefined placeholders rather than expanding recursively (`hooks/lib/template-loader.sh:50-132`). The initial goal tracker skeleton is created by `scripts/setup-rlcr-loop.sh`, which seeds Round 0 placeholders for Ultimate Goal and Acceptance Criteria when plan extraction is incomplete (`scripts/setup-rlcr-loop.sh:742-862` from search evidence).
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook uses a shorter fallback that still includes `GOAL_TRACKER_FILE` and `MISSING_ITEMS` (`hooks/loop-codex-stop-hook.sh:702-709`, `hooks/lib/template-loader.sh:170-203`). If `goal-tracker.md` is absent, this specific gate does not fire because the condition requires the file to exist (`hooks/loop-codex-stop-hook.sh:654`); other setup/monitor behavior may then show zero tracker data. Placeholder detection is generic and section-scoped, checking `\[To be [a-z]`, which avoids coupling to exact placeholder wording but may miss differently formatted placeholders or uppercase variants (`hooks/loop-codex-stop-hook.sh:655-685`). Skip-implementation/review-started paths bypass this initialization gate by design (`hooks/loop-codex-stop-hook.sh:651-654`).
- validation_or_tests: Template existence and safe rendering are indirectly checked by `tests/test-template-references.sh`, which scans hook references and requires critical validators to use safe template rendering (`tests/test-template-references.sh:83-110`, `tests/test-template-references.sh:172-200`). Template-loader fallback behavior is covered by `tests/test-template-loader.sh` and robustness tests referenced in search evidence. Goal tracker parsing and monitor display behavior are covered by `tests/robustness/test-goal-tracker-robustness.sh` and `scripts/humanize.sh` parser logic (`scripts/humanize.sh:30-112`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-117 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory periodic Codex review contract for full goal alignment. It defines the review-phase algorithm that audits Claude’s work against the original plan, goal tracker, implementation reality, historical progress, and stagnation circuit breaker.
- algorithmic_behavior: The prompt requires Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), consume Claude’s summary (`prompt-template/codex/full-alignment-review.md:13-17`), audit each immutable Acceptance Criterion with status/evidence/blocker/deferral justification (`prompt-template/codex/full-alignment-review.md:19-29`), detect forgotten plan items and unverified summary claims (`prompt-template/codex/full-alignment-review.md:30-35`), audit deferred items for validity and contradictions (`prompt-template/codex/full-alignment-review.md:36-41`), produce a completion summary (`prompt-template/codex/full-alignment-review.md:42-48`), perform implementation review against reality and docs (`prompt-template/codex/full-alignment-review.md:50-56`), process the injected goal-tracker update section (`prompt-template/codex/full-alignment-review.md:57`), check historical stagnation (`prompt-template/codex/full-alignment-review.md:59-85`), and write the review result with strict terminal marker semantics (`prompt-template/codex/full-alignment-review.md:86-93`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE`, all passed by `hooks/loop-codex-stop-hook.sh` when a full alignment round is selected (`hooks/loop-codex-stop-hook.sh:818-831`). Output must be written to `round-N-review-result.md` and end with either action findings, `STOP` for stagnation, or `COMPLETE` only when all original plan tasks and all ACs are fully met with no deferrals (`prompt-template/codex/full-alignment-review.md:84-93`). It drives later stop-hook transitions: review result existence and content are checked after Codex review, with missing/empty result treated as failure (`hooks/loop-codex-stop-hook.sh:1343-1397` from search evidence).
- gates_or_invariants: Full alignment is selected every `FULL_REVIEW_ROUND` rounds at rounds `N-1`, `2N-1`, etc.; invalid intervals below `2` default to `5` (`hooks/loop-codex-stop-hook.sh:775-785`). Previous-round references are clamped to `0` to avoid negative paths (`hooks/loop-codex-stop-hook.sh:787-793`). Deferrals are explicitly incomplete, and `COMPLETE` is allowed only when all ACs from the original plan are fully met and no deferrals remain (`prompt-template/codex/full-alignment-review.md:88-93`). Stagnation forces `STOP` as a single final line instead of `COMPLETE` (`prompt-template/codex/full-alignment-review.md:76-85`). Goal tracker updates after Round 0 are Codex’s responsibility through the injected update section, aligning with validators that block Claude from editing the tracker directly after Round 0 (`hooks/loop-codex-stop-hook.sh:769-773`, `hooks/loop-edit-validator.sh:142-149`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` when `FULL_ALIGNMENT_CHECK=true` and rendered to `round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:763-831`). Depends on `prompt-template/codex/goal-tracker-update-section.md`, rendered first and injected as `GOAL_TRACKER_UPDATE_SECTION` (`hooks/loop-codex-stop-hook.sh:769-773`). Rendering uses `load_and_render_safe`, so a fallback prompt exists if this template is absent (`hooks/loop-codex-stop-hook.sh:795-805`, `hooks/loop-codex-stop-hook.sh:818-831`). The generated prompt references `.humanize/rlcr/{{LOOP_TIMESTAMP}}/round-N-*` history files and the configured docs path, defaulting to `docs` (`prompt-template/codex/full-alignment-review.md:61-73`, `hooks/loop-codex-stop-hook.sh:752-758`).
- edge_cases_or_failure_modes: If `FULL_REVIEW_ROUND` is corrupt, the hook warns and reverts to `5`, which changes when this template is used (`hooks/loop-codex-stop-hook.sh:775-781`). If earlier history files are missing, the prompt still asks Codex to read them; the template does not define fallback behavior for absent history, so review quality depends on Codex handling missing context explicitly (`prompt-template/codex/full-alignment-review.md:63-74`). If Codex writes `COMPLETE` despite deferrals or unmet ACs, the prompt contract is violated and downstream stop-hook logic must catch incomplete reality through review/result checks. Because template substitution is single-pass, summary content containing `{{...}}` is preserved rather than accidentally replaced, which avoids prompt corruption (`hooks/lib/template-loader.sh:50-132`).
- validation_or_tests: Template reference coverage is provided by `tests/test-template-references.sh`, which scans `loop-codex-stop-hook.sh` and other scripts for referenced templates and verifies their existence (`tests/test-template-references.sh:74-110`). Template rendering mechanics and real template checks are covered by `tests/test-templates-comprehensive.sh`, including related Codex goal-tracker update section rendering (`tests/test-templates-comprehensive.sh:530-539`). The stop-hook path around review prompt generation is visible at `hooks/loop-codex-stop-hook.sh:760-849`; later review-result validation is referenced in search evidence at `hooks/loop-codex-stop-hook.sh:1343-1397`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4 section headers present; 4 assigned paths inspected directly`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`