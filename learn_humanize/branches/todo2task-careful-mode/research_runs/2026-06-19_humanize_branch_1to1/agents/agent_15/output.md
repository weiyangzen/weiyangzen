# agent_15 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor algorithm library for `scripts/humanize.sh`. The directory recursively contains one core file, `scripts/lib/monitor-common.sh`, sourced at `scripts/humanize.sh:6-10` to provide cross-loop display, session discovery, state-file discovery, PR-loop phase detection, and goal-tracker parsing.
- algorithmic_behavior: `monitor-common.sh` provides ANSI status color helpers (`scripts/lib/monitor-common.sh:14-23`, `130-141`), portable file-size lookup using GNU/BSD `stat` fallback (`32-35`), timestamp-named session selection (`40-63`), terminal scroll-region setup/restoration (`71-92`), active or terminal state-file detection (`154-188`), YAML-frontmatter key extraction (`197-209`), timestamp/string formatting (`218-252`), PR-loop phase detection (`267-342`), PR-loop phase display mapping (`346-381`), RLCR goal-tracker summary parsing (`390-463`), and PR goal-tracker issue-stat parsing (`468-498`).
- inputs_outputs_state: Inputs are filesystem paths, session directories, state files, Markdown goal trackers, terminal dimensions, and cached PR-check file size. Outputs are shell strings consumed by monitor UI code, commonly pipe-delimited tuples such as `state_file|status` (`154-188`), `total_acs|completed_acs|active_tasks|...` (`390-463`), and PR issue stats (`468-498`). `get_pr_loop_phase` also writes transient size cache files under `/tmp/humanize-phase-<session>-<round>.size` to infer whether Codex is still writing `round-N-pr-check.md` (`297-317`).
- gates_or_invariants: Session names must match `YYYY-MM-DD_HH-MM-SS` before being considered (`54-59`). `monitor_find_state_file` gives active `state.md` priority over terminal `*-state.md` files (`162-181`). PR-loop terminal states are detected before live phase inference (`272-275`). `get_pr_loop_phase` treats a growing or recently modified PR check file as `codex_analyzing` (`292-318`), otherwise falls back to `state.md` fields: round `0` plus startup case `1` means `waiting_initial_review`, otherwise `waiting_reviewer` (`321-338`). Goal tracker parsing assumes specific Markdown section headings and table structures (`397-462`, `475-497`).
- dependencies_and_callers: `scripts/humanize.sh` wraps `monitor_find_latest_session` for RLCR monitoring at `scripts/humanize.sh:237-240` and PR monitoring at `979-982`; wraps `monitor_find_state_file` at `344-347` and `1028-1045`; uses `monitor_get_file_size` for incremental log tailing at `627-630`; uses `get_pr_loop_phase` and `get_pr_loop_phase_display` in PR monitor rendering at `1110-1116` and once-mode output at `1248-1254`; and uses `humanize_parse_pr_goal_tracker` for issue counters at `1152-1160`. Tests directly source this library in `tests/test-pr-loop-system.sh` for shared monitor behavior, as shown by search hits around `tests/test-pr-loop-system.sh:257-317`, `770-850`, and `872-891`.
- edge_cases_or_failure_modes: Missing loop/session dirs return empty or `|unknown` rather than crashing (`44-47`, `157-160`, `183-187`). `monitor_get_file_size` returns `0` when both stat variants fail (`32-35`). PR phase cache writes are best-effort and ignore failures (`306`). The active/growing heuristic can briefly classify a file modified in the last 10 seconds as `codex_analyzing` even if no process is active (`308-317`). Markdown parsers are regex/section-heading based and can miscount if headings or table columns drift (`397-462`, `493-497`).
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh` shared monitor tests for latest-session, state-file, file-size, PR phase, and tracker parsing; robustness overlap appears in `tests/robustness/test-goal-tracker-robustness.sh` for `humanize_parse_goal_tracker`. I did not run the tests in this read-only branch export because several test paths create temp directories and git repositories.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Fixture-backed `gh` command generator for PR-loop fetch/poll algorithm tests. It replaces GitHub CLI network/API calls with deterministic fixture JSON so `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` can be tested end-to-end.
- algorithmic_behavior: The script requires `<mock_bin_dir> <fixtures_dir>` (`tests/setup-fixture-mock-gh.sh:16-22`), creates the mock bin directory (`24`), writes an executable `gh` shim (`27-97`), and prints the mock bin path for callers to prepend to `PATH` (`99-101`). The shim dispatches on the first CLI argument: `auth status` succeeds (`33-39`), `repo view` emits owner/name or parent JSON variants (`40-53`), `pr view` emits PR number/state JSON (`54-63`), and `api` maps GitHub endpoints to fixture files (`64-92`).
- inputs_outputs_state: Inputs are the destination mock binary directory and fixture directory. Outputs are an executable `gh` file and stdout containing `MOCK_BIN_DIR`. The generated mock reads `issue_comments.json`, `review_comments.json`, and `pr_reviews.json` from the supplied fixture directory for `/issues/*/comments`, `/pulls/*/comments`, and `/pulls/*/reviews` respectively (`71-87`). Unknown `gh api` endpoints return `[]` (`89-91`); unhandled non-API commands exit `1` with a diagnostic (`95-96`).
- gates_or_invariants: Fails fast on missing args (`19-22`). The mock assumes fixture filenames with underscores, not hyphenated duplicates (`73`, `79`, `85`). Endpoint matching order checks issue comments before pull review comments and PR reviews, which matches the endpoint shapes used by the production scripts.
- dependencies_and_callers: `tests/test-pr-loop-hooks.sh` calls this setup helper in `setup_fixture_mock_gh` (`tests/test-pr-loop-hooks.sh:1231-1240`), then runs `fetch-pr-comments.sh` and `poll-pr-reviews.sh` with the mock on `PATH` (`1250-1251`, `1294-1296`, `1329-1334`, `1378-1383`). Production fetch code requests the same three endpoint classes at `scripts/fetch-pr-comments.sh:210-220`; production poll code requests them at `scripts/poll-pr-reviews.sh:240-280`.
- edge_cases_or_failure_modes: The shim is intentionally minimal; query flags such as `--paginate`, `--jq`, and `-q` are mostly ignored except by substring checks. If fixture files are missing, `cat` fails inside the mock. For `repo view`, a request containing both `owner` and `name` returns combined owner/name JSON (`42-44`), while narrower requests return partial JSON (`46-50`).
- validation_or_tests: Fixture tests verify all three comment classes, approval-only PR reviews with placeholder bodies, and `--after` filtering. In `tests/test-pr-loop-hooks.sh`, fetch output is expected to contain a human issue comment, an inline review comment, and `[Review state: APPROVED]` (`1265-1283`), while poll output must be valid JSON and include approval-only review placeholders (`1343-1367`). Fixture data includes bot issue comments in `tests/fixtures/issue_comments.json:12-19`, inline Codex comments in `tests/fixtures/review_comments.json:2-12`, and approval-only PR reviews in `tests/fixtures/pr_reviews.json:2-21`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-075 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the PR-loop gate that verifies Claude acknowledged a trigger comment with an `eyes` reaction before the loop proceeds to poll reviews.
- algorithmic_behavior: The template renders a human-facing block titled `Claude Bot Not Responding` (`prompt-template/block/claude-eyes-timeout.md:1`) and interpolates retry/time variables into the failure sentence (`3`). It then lists likely configuration/permission causes (`5-8`), required operator actions (`10-13`), and a restart instruction after configuration is fixed (`15-18`).
- inputs_outputs_state: Inputs are template variables `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`. Output is Markdown used as the `reason` in a hook block JSON. It does not mutate repo state directly; it defines the text payload for a state transition that keeps the PR-loop hook blocked.
- gates_or_invariants: This template is selected only after trigger validation has succeeded and Claude is configured. In `hooks/pr-loop-stop-hook.sh`, trigger presence is validated first (`724-746`), Claude configuration is detected from configured bots (`759-765`), and eyes verification runs only when Claude is configured and a trigger is required (`767-777`). If the check returns empty or `null`, the hook emits a blocking JSON decision using this template (`778-789`).
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/claude-eyes-timeout.md"` in `hooks/pr-loop-stop-hook.sh:780-785`, with fallback text embedded beside the call. The actual reaction probe is delegated to `scripts/check-bot-reactions.sh` via `claude-eyes`, trigger comment ID, PR number, `--retry 3`, and `--delay 5` (`774-777`).
- edge_cases_or_failure_modes: If there is no trigger comment ID despite a trigger timestamp, the hook warns but does not use this block (`793-796`). If Claude is configured but no trigger is required, the eyes verification is skipped (`797-799`). Template failure is covered by the caller’s inline fallback (`780-785`).
- validation_or_tests: Indirectly covered by PR-loop stop-hook tests and template-loader/comprehensive template tests. Search references show this exact block is called from `hooks/pr-loop-stop-hook.sh:784` and the placeholder contract is captured by `RETRY_COUNT` and `TOTAL_WAIT_SECONDS` in `prompt-template/block/claude-eyes-timeout.md:3`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-105 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Blocking prompt template for RLCR write/edit attempts that target a `round-N-summary.md` file whose round number does not match the active loop round.
- algorithmic_behavior: The template renders the attempted action, target `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, current round, correct path, and the invariant that the agent must not increment the round number itself (`prompt-template/block/wrong-round-number.md:1-7`).
- inputs_outputs_state: Inputs are `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Output is Markdown written to stderr by validators before they exit with a blocking status. It does not update state itself; it enforces that writes/edits stay aligned to `STATE_CURRENT_ROUND`.
- gates_or_invariants: The invariant is active-loop ownership of round files. `hooks/loop-edit-validator.sh` parses the active loop state strictly (`105-110`), detects summary files (`155-163`), compares extracted `CLAUDE_ROUND` with `CURRENT_ROUND`, allows a small explicit allowlist, and otherwise renders this block then exits `2` (`165-179`). `hooks/loop-write-validator.sh` applies the same round check for writes (`228-245`) after separately blocking state files, plan backups, goal-tracker updates after round 0, and summary files outside `.humanize/rlcr` (`140-210`).
- dependencies_and_callers: Called by `hooks/loop-edit-validator.sh:172-177` with `ACTION=edit` and by `hooks/loop-write-validator.sh:238-243` with `ACTION=write to`. Round extraction comes from `extract_round_number` in `hooks/lib/loop-common.sh:414-424`; the active loop/current round comes from `find_active_loop` and `parse_state_file_strict` in `hooks/lib/loop-common.sh:157-175` and `250-336`.
- edge_cases_or_failure_modes: If the target path is not a recognized summary file, this template is not used. If the file is allowlisted (`round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, `round-1-summary.md`), the wrong-round block is bypassed (`hooks/lib/loop-common.sh:426-447`). If state parsing fails, validators fail closed before reaching this block (`hooks/loop-edit-validator.sh:105-108`, `hooks/loop-write-validator.sh:140-143`).
- validation_or_tests: Template rendering is explicitly exercised in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` search hits, and validator behavior is covered by hook robustness tests plus wrong-round validation sections in PR-loop hook tests. The template’s exact placeholder surface is confirmed in `prompt-template/block/wrong-round-number.md:3-5`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-135 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the hook subsystem: edit/write/read/bash validators, plan-file validator, RLCR stop hook, PR-loop stop hook, JSON edge handling, malformed state handling, and concurrent hook access.
- algorithmic_behavior: The test script sources core hook utilities and timeout/test helpers (`tests/robustness/test-hook-system-robustness.sh:20-26`), creates a temp workspace, then runs 21 numbered tests. It invokes validators by piping synthetic hook JSON with `CLAUDE_PROJECT_DIR` set to fixture repos/directories, captures stdout/stderr and exit code, and classifies outcomes with `pass`/`fail`.
- inputs_outputs_state: Inputs are synthetic Claude hook JSON payloads for `Edit`, `Write`, `Read`, and `Bash`; temporary `.humanize/rlcr/<timestamp>/state.md` fixtures; temporary git repos; and mock `codex` binaries. Outputs are test pass/fail counters via `tests/test-helpers.sh:30-78`, plus temporary state transitions such as a possible `unexpected-state.md` when the stop hook ends on incomplete state (`tests/robustness/test-hook-system-robustness.sh:536-556`). The final process exit is the helper summary status (`671-672`).
- gates_or_invariants: Edit validator must allow ordinary valid edits (`40-51`), block direct `state.md` edits (`53-78`), handle malformed/missing fields without crashing (`80-107`), allow paths outside `.humanize` because sandboxing owns that security boundary (`109-122`), and block path traversal back to active `state.md` (`124-155`). Plan-file validator must allow valid plan edits in a matching repo/branch (`165-215`), pass through non-plan files (`217-230`), and tolerate empty JSON without signal-level crash (`232-243`). State parsing must tolerate whitespace/special values/missing delimiters (`253-307`). Bash validator must block `state.md` mutation and post-round-0 `goal-tracker.md` mutation (`317-367`), but pass unrelated dangerous shell text to the sandbox layer (`369-382`). Stop hooks must allow no-state exits (`461-491`), handle corrupted/incomplete state gracefully (`493-557`), and block active-loop exits with a JSON block decision (`559-613`). Large, null-byte-escaped, and nested JSON must exit below signal/crash range (`623-665`).
- dependencies_and_callers: Directly exercises `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. It depends on `hooks/lib/loop-common.sh` for `get_current_round` and state parsing (`22`, `264`, `283`, `301`, `443`), `scripts/portable-timeout.sh` for `run_with_timeout` around large JSON (`23`, `628`), and `tests/test-helpers.sh` for temp dirs and reporting (`24-26`, `671-672`). The stop-hook active-loop test creates a mock `codex` on `PATH` to avoid real API calls (`592-604`).
- edge_cases_or_failure_modes: The test deliberately accepts some flexible outcomes where the invariant is “does not crash”: missing `file_path` may pass or fail gracefully (`102-107`), malformed state may return `0` or parsed value (`301-307`), corrupted stop-hook state may block or allow if no active loop is detected as long as it exits cleanly (`503-514`), and missing required fields may either rename to `unexpected-state.md` or exit `0` with a diagnostic (`540-556`). It also documents intentional non-goals: generic path traversal outside `.humanize` and unrelated dangerous shell commands are not hook responsibilities (`109-122`, `369-382`).
- validation_or_tests: This file is itself validation evidence. I inspected it directly rather than executing it because the read-only export and restricted environment are not suitable for tests that create temp directories, initialize git repos, write mock binaries, and mutate test state (`168-180`, `423-439`, `592-604`). Its coverage maps to production gates in `hooks/lib/loop-common.sh`: strict state parsing (`250-336`), JSON validation/depth checks (`60-134`), state/goal/path helpers (`509-537`), command modification detection (`1074-1102`), and loop-ending state rename (`1118-1151`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: TODO2TASK_CAREFUL_MODE-HZ-015, TODO2TASK_CAREFUL_MODE-HZ-045, TODO2TASK_CAREFUL_MODE-HZ-075, TODO2TASK_CAREFUL_MODE-HZ-105, TODO2TASK_CAREFUL_MODE-HZ-135
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`