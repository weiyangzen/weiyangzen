# agent_12 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex review prompt control surface for the RLCR loop. This directory does not implement shell logic directly, but it defines the review-phase contracts that decide whether the loop continues, feeds remediation back to Claude, updates goal-tracker state, enters full alignment review, or finalizes.
- algorithmic_behavior: The directory contains four templates. `regular-review.md` requires Codex to read the original plan, compare Claude’s summary against implementation reality, reject deferrals, inspect goal tracker state, and only terminate with `COMPLETE` when all original work is done (`prompt-template/codex/regular-review.md:3`, `:20`, `:24`, `:49`). `full-alignment-review.md` is the stricter periodic audit: it checks every immutable acceptance criterion, finds forgotten/deferred work, reviews historical rounds for stagnation, and can end with `STOP` on stagnation (`prompt-template/codex/full-alignment-review.md:19`, `:30`, `:59`, `:76`, `:84`). `goal-tracker-update-section.md` delegates goal tracker mutations to Codex review, while forbidding changes to the immutable goal/AC section (`prompt-template/codex/goal-tracker-update-section.md:1`, `:3`, `:6`, `:10`). `code-review-phase.md` is an audit template for `codex review`, documenting the diff-review invocation and severity-marker scan (`prompt-template/codex/code-review-phase.md:3`, `:14`, `:15`, `:30`).
- inputs_outputs_state: Inputs are rendered variables such as `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `CURRENT_ROUND`, historical round numbers, and `REVIEW_RESULT_FILE`. Outputs are rendered review prompt files under the active loop directory, especially `round-N-review-prompt.md`, and review decisions written to `round-N-review-result.md`. State transitions are textual but authoritative for the hook: issues continue the loop, `COMPLETE` enables finalize, and `STOP` trips stagnation handling.
- gates_or_invariants: Strong invariant: no deferrals or pending work may be treated as completion (`regular-review.md:53`, `:54`; `full-alignment-review.md:91`, `:92`). The immutable goal and acceptance criteria must not be modified (`goal-tracker-update-section.md:10`). Full alignment must read the original plan before reviewing (`full-alignment-review.md:7`, `:10`) and must audit historical files for repeated failures (`:63`, `:69`, `:74`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh`: shared goal tracker section is loaded at `hooks/loop-codex-stop-hook.sh:769`, full alignment template at `:820`, regular review at `:836`, and code review audit prompt at `:959`. Rendering uses `hooks/lib/template-loader.sh` via `load_and_render_safe` per search results. These templates coordinate with loop state files, round summaries, cache logs, and `codex review` output.
- edge_cases_or_failure_modes: If `FULL_REVIEW_ROUND` is invalid, the hook defaults to 5 before choosing the full-alignment template (`hooks/loop-codex-stop-hook.sh:777`, `:779`). Full alignment clamps previous-round indexes to avoid negative historical references (`hooks/loop-codex-stop-hook.sh:790`). A weak or missing template falls back to inline fallback prompts (`hooks/loop-codex-stop-hook.sh:796`, `:807`). A review can falsely finish if the final-line protocol is ignored, so the templates repeatedly constrain `COMPLETE`.
- validation_or_tests: Template coverage appears in `tests/test-templates-comprehensive.sh` for `goal-tracker-update-section.md` based on references. Runtime use is indirectly exercised by hook tests and loop tests. The directory is algorithmic because these templates encode termination, stagnation, and goal-tracker update gates.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: `/humanize:gen-plan` preflight and materialization gate. It validates draft input/output paths, locates the plan template, then writes the initial combined plan file before downstream relevance and analysis phases.
- algorithmic_behavior: Parses only `--input`, `--output`, and help flags (`scripts/validate-gen-plan-io.sh:29`). It normalizes paths with `realpath -m` where available (`:69`), checks input existence and non-empty content (`:79`, `:87`), checks output directory existence (`:95`), rejects output directories and existing paths (`:103`, `:111`), verifies output directory write permission (`:118`), locates `prompt-template/plan/gen-plan-template.md` from `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`:133`, `:135`, `:138`), copies the template, and appends the original draft between explicit separators (`:148`, `:153`, `:155`, `:157`).
- inputs_outputs_state: Inputs are CLI args and optional `CLAUDE_PLUGIN_ROOT`. Output is the requested new plan file containing template content plus original draft. It also emits structured status lines such as `VALIDATION_ERROR:*`, `VALIDATION_SUCCESS`, line counts, and final target path. Exit codes are part of the interface: 0 success; 1 missing input; 2 empty input; 3 missing output dir; 4 output exists/is directory; 5 no write permission; 6 invalid args/help; 7 missing template (`:4`-`:12`).
- gates_or_invariants: The output file must not preexist (`:111`) and must not be a directory (`:104`), so gen-plan is create-only and avoids accidental overwrite. The output parent must already exist (`:96`), meaning directory creation is explicit user responsibility. Template absence is treated as plugin configuration failure (`:142`).
- dependencies_and_callers: Called by `commands/gen-plan.md`, which restricts allowed Bash use to this script (`commands/gen-plan.md:4`) and defines Phase 1 as executing it with user arguments (`commands/gen-plan.md:30`, `:35`). The command maps every exit code to stop/continue behavior (`commands/gen-plan.md:38`-`:46`), then proceeds to relevance checking and draft analysis only after exit 0.
- edge_cases_or_failure_modes: `realpath -m` is GNU-specific, but the script falls back to the raw path when unavailable (`scripts/validate-gen-plan-io.sh:70`). `--help` exits 6 via `usage`, matching command handling but semantically unusual (`:16`, `:23`, `:48`). `set -e` means copy/append failures after validation can abort without a custom exit code. The script writes after printing `VALIDATION_SUCCESS`, so callers should still honor final process status.
- validation_or_tests: `tests/test-gen-plan.sh` directly tests missing option values, unknown option, input missing, empty input, missing output dir, existing output, output as directory, valid path success, and help behavior (`tests/test-gen-plan.sh:438`-`:538`). The command file also tests gen-plan structure and frontmatter earlier in that same test.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable safety specification for zsh monitor behavior, focused on eliminating zsh `nomatch` glob failures in empty or sparse RLCR/cache directories.
- algorithmic_behavior: Runs under zsh (`tests/test-zsh-monitor-safety.sh:1`) with strict error flags (`:13`), creates an isolated `/tmp/test-zsh-humanize-$$` workspace (`:48`), redirects cache state via `XDG_CACHE_HOME` (`:52`), and sources `scripts/humanize.sh` to catch load-time glob errors (`:72`). It then exercises find-based session/state/log discovery patterns against empty `.humanize/rlcr`, dotfile-only directories, no `*-state.md`, empty cache log dirs, valid timestamp sessions, and a comparison with the old glob pattern (`:80`, `:127`, `:160`, `:208`, `:240`, `:289`).
- inputs_outputs_state: Inputs are the repository scripts plus synthetic `.humanize/rlcr` and cache directories. Outputs are PASS/FAIL lines and final exit 0 only if all checks pass (`:333`-`:341`). Test state is isolated and removed by `trap cleanup EXIT` (`:56`, `:60`).
- gates_or_invariants: No output from sourcing or helper patterns may contain `no matches found` (`:73`, `:119`, `:152`, `:200`). Timestamp session selection must ignore nonmatching files and choose the lexicographically latest valid directory (`:259`, `:267`). New monitor file enumeration must use `find` rather than unguarded globbing (`:314`-`:320`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` and mirrors production helpers. Production `monitor_find_latest_session` explicitly uses `find` to avoid zsh glob errors (`scripts/lib/monitor-common.sh:49`, `:60`). Production `monitor_find_state_file` uses `find` for `*-state.md` (`scripts/lib/monitor-common.sh:168`, `:181`). Production `_find_latest_codex_log` searches cache logs with `find` over both run and review log names (`scripts/humanize.sh:280`, `:328`).
- edge_cases_or_failure_modes: The test guards empty directories, dotfile-only directories, missing state files, absent cache logs, invalid session names, and zsh-specific strict glob behavior. It also confirms the suite is truly running under zsh (`tests/test-zsh-monitor-safety.sh:276`, `:282`). A limitation is that several helper implementations are copied into the test instead of invoking all production internals directly, so drift between copied logic and production helpers is possible.
- validation_or_tests: This file is itself the validation. Related broader monitor validation appears in `tests/test-monitor-e2e-real.sh` and `tests/test-pr-loop-system.sh` based on reference search. The direct production hooks are in `scripts/humanize.sh` monitor functions and `scripts/lib/monitor-common.sh`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Small block template used by loop read/write validators to stop agents from reading or writing the right round file name in the wrong loop directory.
- algorithmic_behavior: Renders a blocking message with attempted action, attempted path, correct path, and a suggested `cat` command (`prompt-template/block/wrong-directory-path.md:1`-`:6`). The shell validators decide when to render it; the template is the user-facing transition payload for the directory-path gate.
- inputs_outputs_state: Inputs are `ACTION`, `FILE_PATH`, and `CORRECT_PATH`. Output is a rendered Markdown error sent to stderr by validators before exiting with code 2. It does not mutate state.
- gates_or_invariants: The invariant is exact path equality with the active loop directory path. `loop-read-validator.sh` computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and blocks when `FILE_PATH` differs (`hooks/loop-read-validator.sh:157`, `:159`, `:163`, `:167`). `loop-write-validator.sh` applies the same check for writes (`hooks/loop-write-validator.sh:252`, `:254`, `:258`, `:262`).
- dependencies_and_callers: Called by both read and write validators through `load_and_render_safe` (`hooks/loop-read-validator.sh:163`; `hooks/loop-write-validator.sh:258`). It coordinates with related wrong-round templates and the active loop directory/round metadata.
- edge_cases_or_failure_modes: The rendered suggestion says `cat {{FILE_PATH}}` even though the displayed `Correct path` may be different (`prompt-template/block/wrong-directory-path.md:4`, `:6`); if the attempted path is wrong, that hint may be misleading and is a potential template bug. The fallback strings in both validators are clearer because they describe the correct path directly (`hooks/loop-read-validator.sh:160`-`:162`; `hooks/loop-write-validator.sh:255`-`:257`).
- validation_or_tests: Covered by hook validator behavior and template-loading tests; direct references show both validators use this template. This is algorithmic because it enforces the active-loop path boundary.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-132 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable specification for robust Git status and repository-state detection used by monitor/status surfaces in `scripts/humanize.sh`.
- algorithmic_behavior: Sources test helpers, `portable-timeout.sh`, and production `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:15`-`:19`), creates isolated git repos (`:49`-`:62`), then validates `humanize_parse_git_status` counts clean, untracked, modified, staged-added, deleted, insertion/deletion, renamed, binary, spaces-in-name, many-file, same-file staged+unstaged, detached HEAD, empty repo, and non-git cases (`:71`-`:367`). It then validates `humanize_detect_git_state` for normal, detached, rebase, merge, shallow, non-git, and permission-error scenarios (`:377`-`:500`).
- inputs_outputs_state: Inputs are synthetic git repositories and direct filesystem manipulations under `TEST_DIR`. Outputs are PASS/FAIL records through sourced test helpers, with final summary and process exit status from `print_test_summary` (`:506`, `:507`). It mutates only temporary test repositories.
- gates_or_invariants: `humanize_parse_git_status` must return pipe-delimited fields `modified|added|deleted|untracked|insertions|deletions` as documented in the test (`:32`-`:46`). Non-git directories must not crash and must include `not a git repo` (`:168`-`:176`). Git special states must be classified deterministically, with rebase and merge detected from git-dir markers (`:403`-`:447`) and shallow clones from `.git/shallow` (`:450`-`:463`).
- dependencies_and_callers: Exercises production `humanize_detect_git_state` and `humanize_parse_git_status`. The implementation returns `permission_error`, `not_a_repo`, `rebase`, `merge`, `shallow`, `detached`, or `normal` based on git-dir checks and HEAD symbolic-ref (`scripts/humanize.sh:116`-`:162`). Status parsing uses `git status --porcelain`, status-code counting, and `git diff --shortstat` (`scripts/humanize.sh:166`-`:209`).
- edge_cases_or_failure_modes: The test simulates rebase/merge/shallow by creating marker files/directories rather than running full operations, so it validates detector logic rather than Git’s full command behavior. Permission-error behavior is allowed to return either `permission_error` or `not_a_repo` on some systems (`tests/robustness/test-git-operations-robustness.sh:487`-`:494`). Branch-name check accepts either `master` or `main` (`:90`-`:94`). The exported branch workspace here is not a git checkout, so these tests were inspected but not executed in this read-only run.
- validation_or_tests: This file is itself the robustness validation. It is supported by production code in `scripts/humanize.sh` and by shared test helpers. It complements hook input robustness tests that also call the same Git functions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 headings present; each assigned evidence item appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`