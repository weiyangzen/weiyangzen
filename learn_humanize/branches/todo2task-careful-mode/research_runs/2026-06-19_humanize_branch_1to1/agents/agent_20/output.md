# agent_20 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` command workflow. It transforms a user draft into a structured implementation plan with goal description, AC-formatted acceptance criteria, path boundaries, feasibility hints, dependency sequencing, and code-style constraints. This is a planning front door for later RLCR-style implementation loops because it turns informal human input into deterministic implementation criteria.
- algorithmic_behavior: The command is a six-phase prompt algorithm. Phase 1 delegates path validation and template seeding to `scripts/validate-gen-plan-io.sh` via `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` at `commands/gen-plan.md:30-36`. Phase 2 performs a lenient repository relevance gate using the `humanize:draft-relevance-checker` haiku agent at `commands/gen-plan.md:50-72`. Phase 3 asks Explore agents to inspect affected code and analyze clarity, consistency, completeness, and functionality at `commands/gen-plan.md:76-109`. Phase 4 requires user clarification for discovered issues and explicit confirmation of quantitative metrics as hard requirements or optimization direction at `commands/gen-plan.md:111-139`. Phase 5 generates the plan in a fixed markdown schema at `commands/gen-plan.md:142-248`. Phase 6 edits the preseeded output file, rereads it, optionally unifies language, and reports output metadata at `commands/gen-plan.md:251-293`.
- inputs_outputs_state: Inputs are `--input <draft.md>` and `--output <plan.md>` from the frontmatter argument hint at `commands/gen-plan.md:3`; the command also depends on repository contents explored through Read/Glob/Grep/Task. The output is a new plan file seeded by `scripts/validate-gen-plan-io.sh`, then completed by Edit rather than Write per `commands/gen-plan.md:253-259`. The validation script copies `prompt-template/plan/gen-plan-template.md` and appends the original draft between “Original Design Draft” markers at `scripts/validate-gen-plan-io.sh:148-157`, so the state transition is draft file plus empty target path to generated plan file containing both structured plan and preserved draft. The command itself has no persistent loop state file, but it produces the plan artifact consumed by start-loop workflows.
- gates_or_invariants: IO exit codes are contractually mapped at `commands/gen-plan.md:38-47`: missing input, empty input, nonexistent output directory, preexisting output, no write permission, invalid args, and missing template all stop the command. Relevance is a soft but real gate: `NOT_RELEVANT` stops at `commands/gen-plan.md:67-70`. Draft preservation is a strong invariant: original draft content must never be discarded or overridden at `commands/gen-plan.md:111-114` and the final plan must be a superset at `commands/gen-plan.md:247`. Quantitative metrics require user confirmation before encoding acceptance criteria at `commands/gen-plan.md:126-138`. Generation rules forbid time estimates and line-number references, require AC-X/AC-X.Y criteria, positive and negative tests for every criterion, affirmative path boundaries, and a code-style note excluding plan progress terminology from implementation code at `commands/gen-plan.md:225-247`.
- dependencies_and_callers: Direct dependencies are the validation script declared in allowed tools at `commands/gen-plan.md:4-11`, the `draft-relevance-checker` agent (`agents/draft-relevance-checker.md:1-6`), the plan template (`prompt-template/plan/gen-plan-template.md:1-74`), and the Claude tools Read, Glob, Grep, Task, Write, AskUserQuestion. The validation script resolves template location from `CLAUDE_PLUGIN_ROOT` with script-relative fallback at `scripts/validate-gen-plan-io.sh:133-140`. README documents `/humanize:gen-plan --input ... --output ...` as the user-facing entrypoint, and `tests/test-gen-plan.sh` validates command/frontmatter and script behavior, especially `tests/test-gen-plan.sh:426-542`.
- edge_cases_or_failure_modes: The workflow stops if the draft is wholly unrelated, if any path validation exit code is nonzero, if user clarification is needed but unresolved, or if a complete plan cannot be generated. Mixed-language output triggers a user decision gate instead of automatic translation at `commands/gen-plan.md:272-285`. A deterministic draft with no meaningful implementation choices narrows path boundaries so upper and lower bounds may converge at `commands/gen-plan.md:192` and `commands/gen-plan.md:243`. The validation script treats `--help` as invalid-argument exit code 6, refuses existing output paths and directories, requires write permission, and fails with exit 7 if the plan template is missing (`scripts/validate-gen-plan-io.sh:79-145`).
- validation_or_tests: `tests/test-gen-plan.sh` covers command presence, frontmatter, agent definition, English-only content checks, and `validate-gen-plan-io.sh` exit-code behavior. The script tests assert missing `--input` and `--output` values exit 6, unknown options exit 6, missing input exits 1, empty input exits 2, nonexistent output directory exits 3, existing output path or directory exits 4, valid paths exit 0, and `--help` exits 6 at `tests/test-gen-plan.sh:438-538`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable security specification for the RLCR cancellation exception. It proves that direct state-file mutation is blocked except for one tightly authorized transition: moving active loop `state.md` or `finalize-state.md` to `cancel-state.md` when `.cancel-requested` exists in the same active loop directory.
- algorithmic_behavior: The test builds isolated fake RLCR loops under a temp `.humanize/rlcr/<timestamp>` directory, writes a minimal `state.md`, sets `CLAUDE_PROJECT_DIR`, and feeds synthetic Bash hook JSON into `hooks/loop-bash-validator.sh` through `run_bash_validator` at `tests/test-cancel-signal-file.sh:42-74`. It exercises the real helper `is_cancel_authorized` from `hooks/lib/loop-common.sh`, whose algorithm requires a same-directory `.cancel-requested` signal, rejects substitutions/operators/newlines, normalizes `$loop_dir` and `${loop_dir}`, parses exactly two `mv` arguments, accepts only active-loop `state.md` or `finalize-state.md` as source, accepts only active-loop `cancel-state.md` as destination, and rejects symlink sources (`hooks/lib/loop-common.sh:539-716`).
- inputs_outputs_state: Inputs are constructed Bash command strings embedded in hook JSON. State inputs are the active loop directory, `state.md`, optional `finalize-state.md` by implication through the helper, and optional `.cancel-requested`. Expected outputs are hook exit code 0 for authorized cancel moves and exit code 2 for blocked state-file modification attempts; direct helper tests expect shell true/false return status. The intended state transition is not performed by this test, but the authorized command shape is `mv <active-loop>/state.md <active-loop>/cancel-state.md`, meaning active loop state becomes cancellation state only after the external cancel script has created the signal file.
- gates_or_invariants: The main invariant is that `.cancel-requested` authorizes only the exact cancel rename, not arbitrary state-file edits. Positive cases include normal absolute paths, `/./` path normalization, single-quoted paths, literal `${loop_dir}` syntax, and full validator flow with literal loop-dir syntax at `tests/test-cancel-signal-file.sh:80-115`, `tests/test-cancel-signal-file.sh:365-486`. Negative gates cover missing signal, wrong destination, signal in parent directory, direct redirects, `sed -i`, `rm`, arbitrary destination, command substitution, backticks, newlines, hidden variables, extra args, prefix wrappers, chained commands, pipes, background operators, redirection prefixes, unrelated state paths, and shell wrappers across `tests/test-cancel-signal-file.sh:121-357` and `tests/test-cancel-signal-file.sh:493-1227`. The no-active-loop case is intentionally allowed at `tests/test-cancel-signal-file.sh:1230-1249`, preserving hook noninterference outside active RLCR.
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` at `tests/test-cancel-signal-file.sh:32-33`, uses `jq` to build validator input at `tests/test-cancel-signal-file.sh:63-71`, and calls `hooks/loop-bash-validator.sh` at `tests/test-cancel-signal-file.sh:72`. The validator finds active loops from `CLAUDE_PROJECT_DIR`, parses state strictly, and checks state-file mutation before other protected RLCR files (`hooks/loop-bash-validator.sh:54-86`, `hooks/loop-bash-validator.sh:125-156`). The source-side bypass logic for `mv`/`cp` from `state.md` is in `hooks/loop-bash-validator.sh:158-304`; shell wrapper bypass checks are at `hooks/loop-bash-validator.sh:306-328`. User-facing cancellation command documentation is in `commands/cancel-rlcr-loop.md`, and the stop hook also treats cancel as a terminal state transition.
- edge_cases_or_failure_modes: The test intentionally covers attacker-style inputs rather than only normal operation. Important edge cases include command injection with `&&` and `;` (`tests/test-cancel-signal-file.sh:243-277`), `$()` and backticks (`tests/test-cancel-signal-file.sh:300-337`), literal newline injection directly against the helper (`tests/test-cancel-signal-file.sh:340-357`), `mv --` and `cp -f` option bypass attempts (`tests/test-cancel-signal-file.sh:383-420`), sudo/env/command prefixes (`tests/test-cancel-signal-file.sh:549-646`), shell wrappers (`tests/test-cancel-signal-file.sh:808-846`), and redirection prefix variants including quoted, escaped-space, and ANSI-C quote targets (`tests/test-cancel-signal-file.sh:848-1206`). One explicit bypass fix is the arbitrary destination test at `tests/test-cancel-signal-file.sh:279-297`.
- validation_or_tests: This file is itself the validation asset. It increments pass/fail counters and exits with `TESTS_FAILED` at `tests/test-cancel-signal-file.sh:1345-1357`, making it suitable for CI. Its coverage is stronger than a unit test of the helper alone because it validates both the public Bash hook path and direct helper semantics in the same run.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Small prompt block used by the RLCR stop hook when git is dirty specifically because `.humanize` local loop artifacts are untracked. It defines the remediation contract that `.humanize/` is loop-local state and should be ignored rather than committed.
- algorithmic_behavior: The template emits a special-case note telling the agent that `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore` with `echo '.humanize*' >> .gitignore` followed by `git add .gitignore` at `prompt-template/block/git-not-clean-humanize-local.md:2-8`. The stop hook appends this note to the broader Git-not-clean block when cached git status contains untracked `.humanize*` paths.
- inputs_outputs_state: Input is implicit template loading by `hooks/loop-codex-stop-hook.sh`; the condition is `GIT_STATUS_CACHED` containing untracked paths whose names match `.humanize` (`hooks/loop-codex-stop-hook.sh:523-536`). Output is prompt text included in the hook’s block reason. The intended state transition is not direct file mutation by the template; it instructs the agent to transition repository ignore configuration so future local loop directories no longer appear as untracked git dirt.
- gates_or_invariants: Invariant: `.humanize/` is operational loop state, not commit content. The surrounding gate is the quick Git clean check before running expensive Codex review; uncommitted changes block exit and require commit/ignore cleanup (`hooks/loop-codex-stop-hook.sh:512-555`). The specific note only appears for `.humanize*` untracked files; other untracked files load a different note at `hooks/loop-codex-stop-hook.sh:539-547`.
- dependencies_and_callers: The only observed caller is `hooks/loop-codex-stop-hook.sh`, which loads `block/git-not-clean-humanize-local.md` through `load_template` at `hooks/loop-codex-stop-hook.sh:531-536`. It depends on template lookup through the repo’s template loader and on git status classification. It coordinates with sibling prompt blocks such as `git-not-clean-untracked.md` and the fallback Git Not Clean message assembled immediately after the special notes.
- edge_cases_or_failure_modes: If the template cannot be loaded, the stop hook falls back to `Note: .humanize* directories are intentionally untracked.` at `hooks/loop-codex-stop-hook.sh:532-535`, losing the concrete `.gitignore` remediation. The template advises `git add .gitignore` but not a commit by itself; the surrounding hook separately requires committed work before loop exit. Because it appends `.humanize*`, it covers both `.humanize/` and legacy similarly prefixed paths.
- validation_or_tests: No direct unit test was found for this specific markdown block, but its caller path is exercised by Git-clean stop-hook behavior. The template’s existence is referenced by `rg` only from `hooks/loop-codex-stop-hook.sh:532`, making it a narrow prompt dependency rather than a standalone algorithm.
- skip_candidate: `yes: prompt text only, but it participates in the Git-clean gate by defining required remediation for local RLCR artifacts`

### TODO2TASK_CAREFUL_MODE-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Prompt footer appended to each generated next-round RLCR prompt. It enforces end-of-round behavioral contracts: do not fake loop exit, do not edit loop state, do not execute `cancel-rlcr-loop`, commit changes, and write the next summary file.
- algorithmic_behavior: The footer is loaded after the next-round prompt body, optional Open Question notice, and optional post-alignment instructions. The stop hook renders `{{NEXT_SUMMARY_FILE}}` with the next round summary path and appends it to `round-<next>-prompt.md` at `hooks/loop-codex-stop-hook.sh:1521-1587`. The footer’s first line is a guard against invalid loop escape behavior; lines 6-9 define required completion actions for the next worker turn.
- inputs_outputs_state: Input placeholder is `NEXT_SUMMARY_FILE`, computed as `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` at `hooks/loop-codex-stop-hook.sh:1521-1523`. Output is appended markdown in the next prompt file. The surrounding state transition increments `current_round` in the active state file using a temp file and `mv` at `hooks/loop-codex-stop-hook.sh:1516-1519`, then creates `round-${NEXT_ROUND}-prompt.md` and instructs the agent to produce `round-${NEXT_ROUND}-summary.md`.
- gates_or_invariants: Invariant: the agent must not exit by lying, editing loop state files, or invoking cancellation (`prompt-template/claude/next-round-footer.md:4`). The footer also makes a commit and summary file mandatory before completion (`prompt-template/claude/next-round-footer.md:6-9`). It conditionally recommends the `code-simplifier` plugin if installed at `prompt-template/claude/next-round-footer.md:7`; this is advisory optimization, while commit and summary are operational requirements enforced by later stop-hook checks.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:1583-1587`, with fallback text if the template is missing. It depends on `load_and_render_safe` and template placeholder substitution. It coordinates with `prompt-template/claude/next-round-prompt.md`, `prompt-template/claude/open-question-notice.md`, `prompt-template/claude/post-alignment-action-items.md`, and optional `push-every-round-note.md` appended afterward when `push_every_round` is true (`hooks/loop-codex-stop-hook.sh:1589-1595`).
- edge_cases_or_failure_modes: If this template is missing, fallback only says to commit and write a summary (`hooks/loop-codex-stop-hook.sh:1583-1587`), which omits the stronger anti-bypass warning and code-simplifier advisory. If `NEXT_SUMMARY_FILE` substitution fails, the prompt would contain an unresolved placeholder, weakening the explicit file target. The footer also forbids `cancel-rlcr-loop` even though user-initiated cancellation exists elsewhere, preventing the worker from self-terminating the control loop.
- validation_or_tests: No direct test for this exact footer was found. It is indirectly part of stop-hook prompt generation: the hook writes next-round prompts after issueful Codex review and appends the footer as the final required-action block. Template reference is visible at `hooks/loop-codex-stop-hook.sh:1586`.
- skip_candidate: `yes: prompt footer only, but it defines loop-transition behavior and worker obligations for the next round`

### TODO2TASK_CAREFUL_MODE-HZ-140 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR and PR setup scripts. It validates argument parsing, plan-file safety, git prerequisites, YAML-safe state serialization, mutual exclusion, symlink rejection, timeout handling, review-round controls, and skip-implementation mode.
- algorithmic_behavior: The test creates isolated git repositories with helper functions, writes minimal plan files, mocks `codex`, `gh`, `timeout`, and `gtimeout` as needed, then runs `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh` with `CLAUDE_PROJECT_DIR` pointed at each temp repo (`tests/robustness/test-setup-scripts-robustness.sh:31-87`). It checks outputs and exit codes for expected accept/reject behavior rather than relying on a single golden output. This maps setup algorithms from user command arguments to validated `.humanize` loop state directories.
- inputs_outputs_state: Inputs are command-line flags and repo state. RLCR inputs include positional plan file or `--plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--full-review-round`, `--skip-impl`, tracked versus ignored plan state, current branch, base branch detection, and availability of `codex`. PR inputs include bot flags, `--max`, `--codex-model`, `--codex-timeout`, git repo status, `gh` auth, and PR lookup. Outputs are exit status, diagnostics, and on success `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `.review-phase-started`, and `goal-tracker.md` for RLCR skip-impl cases. The RLCR setup state schema is written at `scripts/setup-rlcr-loop.sh:716-734`; skip-impl marker creation is at `scripts/setup-rlcr-loop.sh:736-739`.
- gates_or_invariants: Argument gates reject missing plan unless `--skip-impl`, duplicate positional plus `--plan-file`, unknown options, nonnumeric numeric parameters, `--full-review-round` below 2, and missing values (`tests/robustness/test-setup-scripts-robustness.sh:96-215`, `tests/robustness/test-setup-scripts-robustness.sh:876-965`). Plan gates reject comment-only plans, fewer than five lines, spaces in path, shell metacharacters, absolute paths, symlink files, symlink parent directories, unreadable or missing files, tracked plan files without `--track-plan-file`, and insufficient content (`tests/robustness/test-setup-scripts-robustness.sh:224-341`, `tests/robustness/test-setup-scripts-robustness.sh:605-658`). Git gates reject nongit dirs and repos without commits (`tests/robustness/test-setup-scripts-robustness.sh:412-442`). Mutual exclusion blocks starting RLCR when RLCR or PR loop state already exists (`tests/robustness/test-setup-scripts-robustness.sh:535-595`). YAML safety gates reject unsafe branch names and invalid Codex model characters (`tests/robustness/test-setup-scripts-robustness.sh:351-402`).
- dependencies_and_callers: The test sources `tests/test-helpers.sh` and `scripts/portable-timeout.sh` at `tests/robustness/test-setup-scripts-robustness.sh:17-18`. It directly exercises `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh` through wrappers at `tests/robustness/test-setup-scripts-robustness.sh:65-87`. RLCR setup sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:27-30`, `scripts/setup-rlcr-loop.sh:237-241`). PR setup sources `portable-timeout.sh`, template loader, and `loop-common.sh` (`scripts/setup-pr-loop.sh:33-42`, `scripts/setup-pr-loop.sh:208-214`). Both depend on git command timeout protection via `run_with_timeout`, whose implementation selection is `gtimeout`, GNU `timeout`, Python, then no-timeout fallback at `scripts/portable-timeout.sh:9-71`.
- edge_cases_or_failure_modes: The suite highlights several intentional behaviors that could be surprising. `--codex-timeout 0` is accepted because validation uses `^[0-9]+$`, despite error text saying positive integer (`tests/robustness/test-setup-scripts-robustness.sh:749-765`). Very large timeout values are accepted (`tests/robustness/test-setup-scripts-robustness.sh:781-799`). Timeout failure is simulated by a mock timeout returning 124 for `git rev-parse`, and any nonzero timeout-related failure is accepted (`tests/robustness/test-setup-scripts-robustness.sh:801-856`). Branch names containing colon may be impossible on some git versions, so that test passes as environment-variable behavior if creation fails (`tests/robustness/test-setup-scripts-robustness.sh:364-380`). `--skip-impl` intentionally makes plan file optional, creates a review-phase marker, sets `review_started: true`, and requires goal tracker output without placeholder text (`tests/robustness/test-setup-scripts-robustness.sh:975-1039`).
- validation_or_tests: This file is itself the validation suite and exits via `print_test_summary` at `tests/robustness/test-setup-scripts-robustness.sh:1069-1074`. It gives broad executable coverage of setup algorithms and their safety invariants. It also validates positive paths that proceed beyond argument parsing to later dependency checks, such as valid RLCR setup reaching Codex availability checks (`tests/robustness/test-setup-scripts-robustness.sh:668-692`) and valid PR loop setup reaching GitHub auth checks (`tests/robustness/test-setup-scripts-robustness.sh:714-739`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique item evidence sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`