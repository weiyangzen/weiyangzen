# agent_22 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. The file is a thin command wrapper with frontmatter metadata and delegates execution to `scripts/setup-rlcr-loop.sh` via the only allowed tool path, `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` at `commands/start-rlcr-loop.md:1-14`.
- algorithmic_behavior: Defines the high-level two-phase algorithm: implementation work is summarized, Codex reviews the summary, `COMPLETE` moves the loop into Review Phase, `codex review --base <branch>` checks code quality, severity markers keep the loop active, and a clean review moves to Finalize Phase at `commands/start-rlcr-loop.md:16-25` and `commands/start-rlcr-loop.md:60-67`. It also defines the Goal Tracker as a drift-prevention mechanism with immutable goal/acceptance criteria and mutable tasks/evolution/deferrals at `commands/start-rlcr-loop.md:27-45`.
- inputs_outputs_state: Inputs are the command arguments in `commands/start-rlcr-loop.md:3`: plan path or `--plan-file`, max iterations, Codex model and timeout, plan tracking, push-every-round, base branch, full review interval, skip-implementation mode, and open-question mode. The setup script parses these options at `scripts/setup-rlcr-loop.sh:123-231`. Outputs are a new `.humanize/rlcr/<timestamp>/` loop directory, a copied or placeholder `plan.md`, `state.md`, `goal-tracker.md`, `round-0-prompt.md`, and summary path. The state file records `current_round`, `max_iterations`, Codex settings, branch metadata, base commit, `review_started`, and `ask_codex_question` at `scripts/setup-rlcr-loop.sh:716-734`.
- gates_or_invariants: The command can only launch the setup script. The setup script enforces mutual exclusion against active RLCR and PR loops at `scripts/setup-rlcr-loop.sh:247-268`, requires a Git repository with at least one commit at `scripts/setup-rlcr-loop.sh:300-314`, validates plan path locality, no absolute paths, no spaces, no shell metacharacters, no symlink traversal, and no submodule placement at `scripts/setup-rlcr-loop.sh:316-429`. It also enforces plan tracking policy at `scripts/setup-rlcr-loop.sh:430-479`, minimum plan content at `scripts/setup-rlcr-loop.sh:483-548`, Codex CLI availability at `scripts/setup-rlcr-loop.sh:550-556`, YAML-safe branch/model/effort values at `scripts/setup-rlcr-loop.sh:568-594`, and a local base branch plus captured base commit at `scripts/setup-rlcr-loop.sh:596-668`.
- dependencies_and_callers: Depends on the Claude slash-command runner, `CLAUDE_PLUGIN_ROOT`, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, Git, and the Codex CLI. The setup script relies on `find_active_loop` and `find_active_pr_loop` from `loop-common.sh`, sourced at `scripts/setup-rlcr-loop.sh:237-241`.
- edge_cases_or_failure_modes: `--skip-impl` makes the plan optional and starts review mode immediately, with no full goal-tracker initialization required, as documented at `commands/start-rlcr-loop.md:69-86` and implemented at `scripts/setup-rlcr-loop.sh:279-289`. Multiple plan inputs, missing option values, invalid numeric options, and invalid full-review intervals are rejected at `scripts/setup-rlcr-loop.sh:123-231`. Base-branch autodetection fails closed if no usable local branch exists at `scripts/setup-rlcr-loop.sh:620-649`. The command itself is not the state-machine implementation; it is the public workflow contract and launcher.
- validation_or_tests: Behavior is covered indirectly by RLCR setup and hook tests, especially the state-transition robustness tests and setup-script tests under `tests/`. I did not execute tests in this read-only research pass; inspection was limited to source and executable specs.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for template-loader failure tolerance. It verifies prompt-template loading and rendering degrade safely under missing files, missing directories, strict shell mode, malformed placeholders, and special-character values.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, computes `TEMPLATE_DIR`, then runs twelve independent assertions with local pass/fail counters at `tests/test-error-scenarios.sh:11-24`. The script checks that `load_template` returns empty content and exit zero for a missing file and missing directory at `tests/test-error-scenarios.sh:42-65`, that `load_and_render` and `render_template` return empty content without failure for empty or missing inputs at `tests/test-error-scenarios.sh:67-91`, and that substitution preserves regex-like characters at `tests/test-error-scenarios.sh:93-106`.
- inputs_outputs_state: Inputs are the real template directory, synthetic template strings, fallback strings, variable assignments such as `PATH=...`, and intentionally missing paths. Outputs are colored `PASS` or `FAIL` lines, counters `TESTS_PASSED` and `TESTS_FAILED`, and process exit zero only if no failures are recorded at `tests/test-error-scenarios.sh:208-226`.
- gates_or_invariants: Missing templates must not crash callers. `load_and_render_safe` must use and render fallbacks for missing templates at `tests/test-error-scenarios.sh:133-158`. `validate_template_dir` must accept the real template directory and reject nonexistent directories at `tests/test-error-scenarios.sh:160-180`. Empty placeholder `{{}}` and unclosed `{{` are expected to remain literal at `tests/test-error-scenarios.sh:182-206`.
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`. The tested implementation resolves `prompt-template` from `hooks/lib` at `hooks/lib/template-loader.sh:24-31`, returns empty output plus a warning for missing files at `hooks/lib/template-loader.sh:33-48`, performs single-pass `{{VAR}}` replacement through environment-backed `awk` at `hooks/lib/template-loader.sh:50-132`, provides fallback rendering at `hooks/lib/template-loader.sh:167-203`, and validates required template subdirectories at `hooks/lib/template-loader.sh:205-222`. These loader functions are used by stop hooks, validators, and prompt builders throughout `hooks/`.
- edge_cases_or_failure_modes: Test 6 explicitly runs a subshell with `set -euo pipefail` and a missing template, then requires `SCRIPT_COMPLETED` to prove the missing-template path does not abort strict-mode callers at `tests/test-error-scenarios.sh:108-131`. The script itself uses `set -uo pipefail` rather than global `set -e`, allowing all failure cases to be reported before final exit. It does not cover every invalid variable name; broader cases are handled by other template tests.
- validation_or_tests: This file is itself the validation asset. It complements `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness tests for template stress/error behavior found by repository search.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-082 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Blocking prompt fragment for the RLCR stop hook when the user tries to exit while the Git worktree has uncommitted changes.
- algorithmic_behavior: The template renders a stop message with `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then gives required remediation: optionally run `code-simplifier`, review untracked files, stage real changes, and commit with a descriptive project-convention message at `prompt-template/block/git-not-clean.md:1-10`.
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`. The caller builds these from cached Git status: nonempty status sets `GIT_ISSUES="uncommitted changes"` at `hooks/loop-codex-stop-hook.sh:518-526`; untracked `.humanize*` files and other untracked files append special-note templates or fallbacks at `hooks/loop-codex-stop-hook.sh:527-547`. Output is a rendered Markdown reason embedded in the stop-hook JSON block response at `hooks/loop-codex-stop-hook.sh:550-570`.
- gates_or_invariants: This is a pre-Codex-review gate. If dirty Git state exists, the hook blocks exit before running expensive review work, requiring committed changes first at `hooks/loop-codex-stop-hook.sh:512-517` and `hooks/loop-codex-stop-hook.sh:550-570`. The template also enforces commit authorship hygiene: no AI authorship and no `Co-Authored-By` AI attribution at `prompt-template/block/git-not-clean.md:11-16`.
- dependencies_and_callers: Loaded by `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` in `hooks/loop-codex-stop-hook.sh:559-561`. It depends on the template-loader single-pass renderer and fallback path, so a missing template still blocks with an inline fallback at `hooks/loop-codex-stop-hook.sh:553-558`.
- edge_cases_or_failure_modes: Special notes can be empty, `.humanize`-specific, untracked-artifact-specific, or both. The template itself does not inspect Git; it only defines the human-facing transition contract. If template loading fails, the caller still blocks with fallback text, preserving the invariant that dirty Git cannot exit the loop.
- validation_or_tests: Template loading and fallback behavior is validated by `tests/test-error-scenarios.sh` and template-loader suites. The specific caller path is covered by stop-hook tests elsewhere in `tests/`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-112 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: Prompt fragment that enforces user-clarification ordering when Codex feedback contains open questions and the RLCR loop was started with open-question mode enabled.
- algorithmic_behavior: The template is a single notice requiring the agent to use `AskUserQuestion` to clarify Codex open questions before resolving other findings at `prompt-template/claude/open-question-notice.md:1`.
- inputs_outputs_state: It has no placeholders. Input state comes from the `--ask-codex-question` flag parsed at `scripts/setup-rlcr-loop.sh:211-213` and persisted as `ask_codex_question` in `state.md` at `scripts/setup-rlcr-loop.sh:731`. Output is inserted into the next-round prompt file when the stop hook detects an open-question heading in Codex review output.
- gates_or_invariants: The gate activates only when `ASK_CODEX_QUESTION` is true. Detection scans `REVIEW_RESULT_FILE` for a line shorter than 40 characters containing the case-sensitive substring `Open Question` at `hooks/loop-codex-stop-hook.sh:1539-1548`. If detected, the notice is inserted after the Codex review end marker and separator before subsequent goal-tracker context at `hooks/loop-codex-stop-hook.sh:1550-1571`.
- dependencies_and_callers: Loaded by `load_template "$TEMPLATE_DIR" "claude/open-question-notice.md"` at `hooks/loop-codex-stop-hook.sh:1552`. If the file is missing, the hook uses an equivalent inline fallback at `hooks/loop-codex-stop-hook.sh:1553-1555`.
- edge_cases_or_failure_modes: Detection is intentionally narrow: long headings, lowercase variants, or phrasing without the exact substring will not trigger. The inserted notice changes prompt ordering but does not itself enforce tool execution; enforcement depends on the next agent round following the prompt and on any external tool policy for `AskUserQuestion`.
- validation_or_tests: Covered by template-loader fallback tests and by stop-hook behavior around next-round prompt generation. No direct standalone test for this exact file was found in the assigned set.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-142 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR state-file parsing, active-loop discovery, finalize/cancel state handling, and schema rejection. It anchors the loop state machine behavior implemented in `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The test creates synthetic `.humanize/rlcr/<timestamp>/` directories and state files using helper constructors for normal `state.md`, `finalize-state.md`, and `cancel-state.md` at `tests/robustness/test-state-transition-robustness.sh:30-89`. It then exercises valid progression, finalize state, cancel state, invalid schema, and discovery ordering at `tests/robustness/test-state-transition-robustness.sh:91-423`.
- inputs_outputs_state: Inputs are generated YAML-frontmatter state files with fields such as `current_round`, `max_iterations`, `base_branch`, and `review_started`. Outputs are pass/fail assertions from `tests/test-helpers.sh`, whose counters and summary determine exit status at `tests/test-helpers.sh:30-78`. Temporary test state lives under `TEST_DIR`, created and cleaned by `setup_test_dir` at `tests/test-helpers.sh:84-89`.
- gates_or_invariants: Valid state includes round zero, higher rounds, and a round equal to max iterations at `tests/robustness/test-state-transition-robustness.sh:98-138`. `find_active_loop` must treat `finalize-state.md` as active at `tests/robustness/test-state-transition-robustness.sh:148-158`, must not treat `cancel-state.md` as active at `tests/robustness/test-state-transition-robustness.sh:182-192`, and must prefer a newer active directory after an older cancel directory at `tests/robustness/test-state-transition-robustness.sh:194-207`. Strict parsing must reject nonnumeric rounds and missing required fields at `tests/robustness/test-state-transition-robustness.sh:259-361`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-state-transition-robustness.sh:14-18`. The implementation under test discovers only the lexicographically newest loop directory and considers it active only if it contains `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:152-175`. `get_current_round` extracts `current_round` from YAML frontmatter and defaults to zero at `hooks/lib/loop-common.sh:177-190`. `parse_state_file_strict` requires frontmatter separators, required fields, numeric `current_round` and `max_iterations`, and boolean `review_started` at `hooks/lib/loop-common.sh:250-336`.
- edge_cases_or_failure_modes: Negative `current_round` is accepted by the regex and parsed as `-1`, explicitly noted as potentially suspicious but not rejected at `tests/robustness/test-state-transition-robustness.sh:217-236`. Rounds above `max_iterations` are parsed but not enforced here, with enforcement expected elsewhere at `tests/robustness/test-state-transition-robustness.sh:238-257`. Directory selection is lexicographic, not timestamp-validated, as asserted at `tests/robustness/test-state-transition-robustness.sh:371-402`. Nested state files in the wrong location are ignored at `tests/robustness/test-state-transition-robustness.sh:404-417`. Test 5 claims finalize precedence but only verifies coexistence of `finalize-state.md` with `state.md`, so it is weaker than a behavioral precedence assertion at `tests/robustness/test-state-transition-robustness.sh:160-172`.
- validation_or_tests: This file is a direct robustness test. It validates state-machine contracts used by RLCR read/write/bash validators and `loop-codex-stop-hook.sh`, which selects `finalize-state.md` over `state.md` when present at `hooks/loop-codex-stop-hook.sh:66-82`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all five assigned item section headers are present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`