# agent_07 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification suite for the Humanize RLCR and PR-loop algorithms. The directory covers hook guard behavior, template rendering, loop state naming, PR review polling, monitor runtime behavior, path validation, git/status parsing, cancel authorization, setup scripts, and robustness edge cases.
- algorithmic_behavior: `tests/run-all-tests.sh:32-73` defines the top-level ordered suite, including template tests, hook tests, finalize/state tests, monitor tests, PR-loop tests, and `tests/robustness/*`. It detects the one zsh-required suite at `tests/run-all-tests.sh:75-78`, strips ANSI output for portable result parsing at `tests/run-all-tests.sh:121-125`, aggregates pass/fail counts at `tests/run-all-tests.sh:127-137`, and exits nonzero on any failed suite at `tests/run-all-tests.sh:148-158`.
- inputs_outputs_state: Most tests create temporary repositories and loop state under `mktemp` directories, then execute production scripts/hooks against those synthetic states. Shared helper setup is in `tests/test-helpers.sh:84-104`. The fixtures subtree supplies static GitHub-style JSON inputs: comments/reactions/reviews in `tests/fixtures/*.json`, including bot comments in `tests/fixtures/issue_comments.json:11-19`, PR review states in `tests/fixtures/pr_reviews.json:1-42`, and inline review comments in `tests/fixtures/review_comments.json:1-24`. Outputs are shell pass/fail counters and process exit codes.
- gates_or_invariants: The suite encodes core invariants: state files are protected and terminal end states are not active loops (`tests/test-state-exit-naming.sh:51-159`), active loop discovery only accepts current `state.md`/`finalize-state.md` in the newest session (`tests/robustness/test-session-robustness.sh:89-103`, `tests/robustness/test-session-robustness.sh:259-302`), cancel renames require a signal file and exact command shape (`tests/test-cancel-signal-file.sh:3-10`), PR loop files and comments are guarded (`tests/test-pr-loop-hooks.sh:3-12`), and template references must exist (`tests/test-template-references.sh:149-167`).
- dependencies_and_callers: The tests coordinate with sibling runtime paths: `hooks/lib/loop-common.sh` for loop detection, template loading, cancel authorization, and `end_loop`; `hooks/*validator.sh` for read/write/edit/bash gates; `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, cancel scripts, `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, and `scripts/humanize.sh`. The mock GitHub CLI at `tests/mocks/gh:27-246` intercepts `gh pr`, `gh api`, `gh repo`, and `gh auth` calls and routes to fixture data or environment-controlled responses. `tests/setup-fixture-mock-gh.sh:26-99` builds a fixture-backed mock CLI for fetch/poll tests. `tests/setup-monitor-test-env.sh:20-89` creates PR-loop state variants for monitor parsing.
- edge_cases_or_failure_modes: The robustness subtree expands failure coverage: base branch fallback (`tests/robustness/test-base-branch-detection.sh:3-12`), cancel injection/path bypass/symlink rejection (`tests/robustness/test-cancel-security-robustness.sh:3-11`), concurrent state reads and race conditions (`tests/robustness/test-concurrent-state-robustness.sh:3-10`), git dirty/detached/rebase/merge/non-repo behavior (`tests/robustness/test-git-operations-robustness.sh:3-10`), malformed hook JSON and monitor edge cases (`tests/robustness/test-hook-input-robustness.sh:3-10`), path validation including symlink/submodule/absolute paths (`tests/test-plan-file-validation.sh:3-12`), setup-script argument validation (`tests/robustness/test-setup-scripts-robustness.sh`), API error scenarios (`tests/robustness/test-pr-loop-api-robustness.sh`), timeout behavior (`tests/robustness/test-timeout-robustness.sh`), and template stress/error handling (`tests/robustness/test-template-stress-robustness.sh`, `tests/robustness/test-template-error-robustness.sh`).
- validation_or_tests: This directory is the validation surface itself. Important child roles include `tests/test-template-loader.sh:55-130` for template load/render behavior; `tests/test-templates-comprehensive.sh:82-130` for loading every markdown template and checking placeholder syntax; `tests/test-pr-loop.sh:3-10` as the PR-loop module runner; `tests/test-pr-loop-system.sh:1761` as the full PR-loop system runner; `tests/test-monitor-e2e-real.sh:3-12` for real monitor runtime behavior; and `tests/test-zsh-monitor-safety.sh:1-10` for zsh-specific glob/runtime safety.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell entrypoint for monitoring RLCR and PR-loop sessions. It does not implement checklist scheduling, but it is core runtime observability: it parses loop state, goal trackers, git status, logs, PR phases, and renders status bars while tails/logs are changing.
- algorithmic_behavior: The script sources shared monitor helpers from `scripts/lib/monitor-common.sh` at `scripts/humanize.sh:6-10`. Public parsers include `humanize_split_to_array` for bash/zsh pipe-delimited fields at `scripts/humanize.sh:18-28`, `humanize_parse_goal_tracker` for acceptance criteria/tasks/issues at `scripts/humanize.sh:32-112`, `humanize_detect_git_state` for repository states at `scripts/humanize.sh:116-162`, and `humanize_parse_git_status` for modified/added/deleted/untracked/diff counts at `scripts/humanize.sh:166-210`. `_humanize_monitor_codex` implements RLCR monitoring from `.humanize/rlcr` at `scripts/humanize.sh:219-898`. `humanize` dispatches `humanize monitor rlcr` and `humanize monitor pr` at `scripts/humanize.sh:901-941`. `_humanize_monitor_pr` implements PR-loop monitoring at `scripts/humanize.sh:948-1377`.
- inputs_outputs_state: Inputs are loop directories (`.humanize/rlcr`, `.humanize/pr-loop`), timestamped session directories, active or terminal state files, cached Codex logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, PR-loop round files, `goal-tracker.md`, and current git status. Outputs are terminal status bars, tail output, phase summaries, and exit status; the file itself does not mutate loop state. It reads state values such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, `plan_file`, and `review_started` at `scripts/humanize.sh:357-366`, and PR fields such as `pr_number`, `configured_bots`, and `active_bots` at `scripts/humanize.sh:1052-1077`.
- gates_or_invariants: RLCR log selection rejects inconsistent run/review ordering: if a `codex-run` round is not strictly earlier than a `codex-review` round it emits an error and returns failure (`scripts/humanize.sh:330-337`). State-file discovery is delegated to `monitor_find_state_file`, where `state.md` means active and `*-state.md` means terminal/status (`scripts/lib/monitor-common.sh:147-188`). The monitor guards missing loop directories (`scripts/humanize.sh:230-235`, `scripts/humanize.sh:972-977`), no sessions (`scripts/humanize.sh:611-615`, `scripts/humanize.sh:1230-1233`), deleted loop/session/log directories (`scripts/humanize.sh:639-642`, `scripts/humanize.sh:814-849`), and log truncation/rotation (`scripts/humanize.sh:794-805`).
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh`, `git`, `find`, `stat`, `sed`, `grep`, `tail`, `tput`, and shell-specific trap behavior. It is documented for users in `README.md:125-126` and `README.md:219`, and exercised by tests including `tests/robustness/test-goal-tracker-robustness.sh:21-36`, `tests/robustness/test-git-operations-robustness.sh:17-33`, `tests/test-monitor-e2e-real.sh:3-12`, `tests/test-monitor-runtime.sh:3-10`, and `tests/test-zsh-monitor-safety.sh:1-10`.
- edge_cases_or_failure_modes: Handles zsh array/glob compatibility using `setopt localoptions ksharrays` (`scripts/humanize.sh:220-223`, `scripts/humanize.sh:949-950`), empty/missing dirs via `find` instead of shell globs (`scripts/humanize.sh:280-339`), cross-platform `stat` through the shared helper (`scripts/lib/monitor-common.sh:29-35`), narrow terminal truncation (`scripts/humanize.sh:432-444`), repeated cleanup/trap calls (`scripts/humanize.sh:552-578`, `scripts/humanize.sh:1182-1213`), no PR monitorable files (`scripts/humanize.sh:1361-1365`), and one-shot PR output mode for tests/automation (`scripts/humanize.sh:1228-1305`).
- validation_or_tests: Covered directly by monitor runtime/e2e tests, zsh safety tests, goal-tracker parser robustness, git parser robustness, PR-loop system monitor tests, and hook input monitor edge cases. The test directory references real `_humanize_monitor_codex` and `_humanize_monitor_pr` invocations in `tests/test-monitor-e2e-real.sh:59-922`, and monitor output phase checks in `tests/test-pr-loop-system.sh:947-1065`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for loop terminal-state naming and active-loop detection. It verifies the contract that only `state.md` is an active normal loop state, while renamed terminal files preserve state history without being treated as live sessions.
- algorithmic_behavior: The script creates a temporary git repo and `.humanize/rlcr/<timestamp>` session at `tests/test-state-exit-naming.sh:31-49`, sources production `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:66-67`, and runs 13 assertions. Tests 1-6 verify `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active, while `state.md` is active (`tests/test-state-exit-naming.sh:51-159`). Test 7 verifies the newest timestamped directory with `state.md` takes precedence (`tests/test-state-exit-naming.sh:161-180`). Tests 8-10 verify `end_loop` rejects invalid reasons, creates reason-specific files, and fails gracefully if the state file is missing (`tests/test-state-exit-naming.sh:186-243`). Tests 11-13 verify `.humanize/rlcr` path detection and legacy `.humanize-loop.local` separation (`tests/test-state-exit-naming.sh:249-291`).
- inputs_outputs_state: Inputs are synthetic state frontmatter files, session directories, reason strings passed to `end_loop`, and path strings passed to `is_in_humanize_loop_dir`. State transitions under test are `state.md` to `${reason}-state.md` for `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`tests/test-state-exit-naming.sh:206-230`). Output is pass/fail counters and shell exit code equal to `TESTS_FAILED` at `tests/test-state-exit-naming.sh:293-302`.
- gates_or_invariants: Enforces reason allowlist in production `end_loop` (`hooks/lib/loop-common.sh:1125-1138`), target filename `${reason}-state.md` (`hooks/lib/loop-common.sh:1140-1145`), missing-state failure (`hooks/lib/loop-common.sh:1142-1150`), active-loop detection limited to `state.md` or `finalize-state.md` in newest session (`hooks/lib/loop-common.sh:152-175`), and new path detection only for `.humanize/rlcr/` (`hooks/lib/loop-common.sh:718-722`).
- dependencies_and_callers: Depends on `git`, `mktemp`, and `hooks/lib/loop-common.sh`. It is included in the global suite at `tests/run-all-tests.sh:32-40`. It overlaps with broader state/session robustness suites such as `tests/robustness/test-session-robustness.sh:259-302` and finalize-phase tests that assert `complete-state.md` is not active.
- edge_cases_or_failure_modes: Covers invalid reason, missing state file, multiple terminal files coexisting, newest-session precedence, terminal files not reviving loops, legacy path non-detection, and accidental search isolation when legacy state exists but `.humanize/rlcr` has no active state (`tests/test-state-exit-naming.sh:267-291`).
- validation_or_tests: This file is itself a focused validator. It also validates production helpers used by read/write/edit/bash hooks and stop hooks: `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when an agent attempts to modify `state.md`. It is a small but core guard-surface artifact: the hook system uses it to explain why loop state is system-managed and must not be edited by agents.
- algorithmic_behavior: The template emits a title and denial message at `prompt-template/block/state-file-modification.md:1-3`, lists the protected state contents at `prompt-template/block/state-file-modification.md:5-9`, and states that modification would corrupt loop state at `prompt-template/block/state-file-modification.md:10`.
- inputs_outputs_state: Inputs are none; no placeholders are present. Output is static markdown returned to a blocked tool invocation. The protected state named by the message includes current round number, max iterations, and Codex configuration (`prompt-template/block/state-file-modification.md:5-9`).
- gates_or_invariants: Supports the invariant that agents cannot write/edit/bash-modify `state.md`; state transitions must go through loop hooks/scripts. Production loader `state_file_blocked_message` uses this template with an inline fallback at `hooks/lib/loop-common.sh:469-476`.
- dependencies_and_callers: Loaded via `load_and_render_safe` from `hooks/lib/loop-common.sh:469-476`; consumed by validators that call the common blocked-message function. Template existence is explicitly checked as a common template in `tests/test-template-references.sh:152-167`, and all templates are loaded/syntax-checked in `tests/test-templates-comprehensive.sh:87-130`.
- edge_cases_or_failure_modes: If missing or empty, validators still have a fallback message (`hooks/lib/loop-common.sh:471-475`), but the tests treat missing common templates as failures. Because the template has no variables, rendering risk is low; the primary failure mode is stale wording or missing file.
- validation_or_tests: Covered by `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and state-file protection suites such as `tests/test-finalize-phase.sh` and PR/RLCR hook tests.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-127 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop Round 0 task template for the no-review-comments startup case. It defines the agent’s first-round behavior when a PR has no fetched comments yet: do not trigger reviews manually, write an initial summary, and rely on the Stop Hook to poll bots.
- algorithmic_behavior: The template declares the no-comment condition with `{{ACTIVE_BOTS_DISPLAY}}` at `prompt-template/pr-loop/round-0-task-no-comments.md:4-7`, instructs the agent to wait for automatic bot reviews and exit after writing a summary at `prompt-template/pr-loop/round-0-task-no-comments.md:8-15`, states important rules at `prompt-template/pr-loop/round-0-task-no-comments.md:18-22`, and describes the Stop Hook polling/validation loop at `prompt-template/pr-loop/round-0-task-no-comments.md:26-30`.
- inputs_outputs_state: Inputs are template variables `ACTIVE_BOTS_DISPLAY` and `RESOLVE_PATH`. Output is appended task text in `.humanize/pr-loop/<timestamp>/round-0-prompt.md`. In `scripts/setup-pr-loop.sh`, when `COMMENT_COUNT` is zero, this template is selected and rendered at `scripts/setup-pr-loop.sh:766-800`; the generated prompt is built by writing the header, appending fetched comments, then appending this task content (`scripts/setup-pr-loop.sh:760-800`).
- gates_or_invariants: Enforces three Round 0 invariants: first reviews are automatic, the agent must not comment solely to trigger review, and `.humanize/pr-loop/` state files are managed by the system (`prompt-template/pr-loop/round-0-task-no-comments.md:18-22`). It also establishes that Stop Hook owns polling cadence and terminal approval/feedback handling (`prompt-template/pr-loop/round-0-task-no-comments.md:26-30`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh:766-800` through `load_and_render_safe`. It pairs with `prompt-template/pr-loop/round-0-header.md:1-15`, which supplies PR metadata and fetched-comment header, and contrasts with `prompt-template/pr-loop/round-0-task-has-comments.md:4-27`, which instructs fixing/commenting when comments exist.
- edge_cases_or_failure_modes: Main edge case is a fresh PR with zero comments; the template avoids accidental bot-trigger comments that would conflict with automatic first review behavior. If missing, setup has an inline fallback containing the same operational instructions at `scripts/setup-pr-loop.sh:769-799`. Incorrect variable rendering would affect bot display or summary target path, but not state mutation directly.
- validation_or_tests: Covered by setup/PR-loop tests that inspect generated Round 0 behavior, including `tests/test-pr-loop-hooks.sh` Round 0 trigger/timeout cases and `tests/robustness/test-setup-scripts-robustness.sh`; template loading/syntax is covered by `tests/test-templates-comprehensive.sh:87-130`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: TODO2TASK_CAREFUL_MODE-HZ-007, TODO2TASK_CAREFUL_MODE-HZ-037, TODO2TASK_CAREFUL_MODE-HZ-067, TODO2TASK_CAREFUL_MODE-HZ-097, TODO2TASK_CAREFUL_MODE-HZ-127
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`