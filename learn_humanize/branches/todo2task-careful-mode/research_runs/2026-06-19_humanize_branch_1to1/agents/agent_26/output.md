# agent_26 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Claude Code Stop hook for the RLCR loop. It intercepts exit attempts, validates loop state and repository hygiene, invokes Codex for implementation review, then transitions the loop through normal rounds, review phase, finalize phase, or terminal state files. The file header states the state surface: `.humanize/rlcr/<timestamp>/state.md`, per-round summaries/prompts/results, and Codex review artifacts at `hooks/loop-codex-stop-hook.sh:3-12`.

- algorithmic_behavior: The hook reads JSON hook input from stdin, locates the active RLCR loop under `.humanize/rlcr`, and exits with success if no active state exists (`hooks/loop-codex-stop-hook.sh:29-63`). It prefers `finalize-state.md` over `state.md` when present (`hooks/loop-codex-stop-hook.sh:72-80`). It parses YAML frontmatter through `parse_state_file`, maps state fields into local variables, and validates model/effort safety plus required raw fields before continuing (`hooks/loop-codex-stop-hook.sh:88-157`). After gates pass, implementation phase builds either a regular Codex prompt or periodic full-alignment prompt (`hooks/loop-codex-stop-hook.sh:763-849`), runs `codex exec`, requires a nonempty review result, and interprets the last nonempty line strictly as `COMPLETE` or `STOP` only when it is the whole line (`hooks/loop-codex-stop-hook.sh:1254-1407`). Non-complete feedback increments `current_round`, writes the next prompt, and blocks exit with Codex feedback (`hooks/loop-codex-stop-hook.sh:1516-1618`).

- inputs_outputs_state: Inputs are hook stdin, `CLAUDE_PROJECT_DIR` or current directory, `.humanize/rlcr` state files, per-round summary files, git status, Todo/Task transcript data, template files, Codex CLI, and state frontmatter fields such as `current_round`, `max_iterations`, `base_branch`, `base_commit`, `review_started`, `full_review_round`, and `push_every_round` (`hooks/loop-codex-stop-hook.sh:43-117`). Outputs are JSON hook decisions, stderr diagnostics, round prompt/result files, cache command/stdout/stderr logs, and state-file renames. Important state transitions include `state.md` to `finalize-state.md` on successful implementation plus clean code review (`hooks/loop-codex-stop-hook.sh:1043-1048`), `finalize-state.md` to `complete-state.md` after final checks (`hooks/loop-codex-stop-hook.sh:744-749`), `state.md` to `maxiter-state.md` on max iterations (`hooks/loop-codex-stop-hook.sh:727-735` and `hooks/lib/loop-common.sh:1118-1150`), and `state.md` to `stop-state.md` when Codex emits STOP (`hooks/loop-codex-stop-hook.sh:1482-1509`).

- gates_or_invariants: The hook is fail-closed on schema and integrity errors. It blocks outdated state missing `plan_tracked` or `start_branch` (`hooks/loop-codex-stop-hook.sh:164-176`), missing/invalid `review_started` or missing `base_branch` (`hooks/loop-codex-stop-hook.sh:183-209`), branch drift from the start branch (`hooks/loop-codex-stop-hook.sh:228-255`), plan backup deletion or mutation outside review phase (`hooks/loop-codex-stop-hook.sh:264-338`), incomplete Todo/Task state from transcript parsing (`hooks/loop-codex-stop-hook.sh:347-395`), git status failure or dirty working tree (`hooks/loop-codex-stop-hook.sh:405-427` and `hooks/loop-codex-stop-hook.sh:519-572`), unpushed commits when `push_every_round=true` (`hooks/loop-codex-stop-hook.sh:574-604`), missing summary (`hooks/loop-codex-stop-hook.sh:611-643`), uninitialized round-0 goal tracker sections (`hooks/loop-codex-stop-hook.sh:649-720`), and files over 2000 lines among changed code/docs (`hooks/loop-codex-stop-hook.sh:430-510`). Review phase has a tamper guard: `review_started=true` requires `.review-phase-started`, preventing manual state toggles (`hooks/loop-codex-stop-hook.sh:1457-1473`).

- dependencies_and_callers: Called by Claude Code as a Stop hook. It sources `hooks/lib/loop-common.sh` and `scripts/portable-timeout.sh` (`hooks/loop-codex-stop-hook.sh:46-55`). It depends on `find_active_loop`, `parse_state_file`, `detect_review_issues`, `end_loop`, marker constants, and template rendering from `loop-common`; the relevant common constants and active-loop/state helpers are in `hooks/lib/loop-common.sh:31-45`, `hooks/lib/loop-common.sh:152-175`, `hooks/lib/loop-common.sh:192-248`, `hooks/lib/loop-common.sh:338-390`, and `hooks/lib/loop-common.sh:1118-1150`. It calls `hooks/check-todos-from-transcript.py` when present (`hooks/loop-codex-stop-hook.sh:347-395`), uses `jq`, `git`, `diff`, `awk`, `sed`, `wc`, `codex exec`, and `codex review`, and loads templates under `prompt-template/block`, `prompt-template/claude`, and `prompt-template/codex`.

- edge_cases_or_failure_modes: Missing active loop allows exit instead of blocking (`hooks/loop-codex-stop-hook.sh:59-63`). Missing `current_round` or `max_iterations` is treated as corrupted state and ends unexpectedly rather than accepting parser defaults (`hooks/loop-codex-stop-hook.sh:132-145`). Invalid `full_review_round` defaults to 5 with warning (`hooks/loop-codex-stop-hook.sh:775-785`). `COMPLETE` at max iterations terminates as maxiter rather than entering finalize (`hooks/loop-codex-stop-hook.sh:1412-1418`). Code review failure, missing log output, or empty review output blocks and requires retry, not review bypass (`hooks/loop-codex-stop-hook.sh:1016-1038` and `hooks/loop-codex-stop-hook.sh:1174-1246`). If Codex writes review content to stdout instead of the expected result file, the hook copies stdout into the result file, but blocks if copying fails (`hooks/loop-codex-stop-hook.sh:1341-1357`). STOP outside a full-alignment round is still honored but reported as unexpected (`hooks/loop-codex-stop-hook.sh:1482-1509`).

- validation_or_tests: Relevant executable coverage includes finalize and review-phase behavior in `tests/test-finalize-phase.sh`, whose header enumerates COMPLETE-to-finalize, finalization, finalize-state detection, git/todo/summary gates, protected finalize-state, and normal-round preservation (`tests/test-finalize-phase.sh:3-20`). That test mocks `codex exec` and `codex review` (`tests/test-finalize-phase.sh:51-121`), verifies finalize-state active detection and complete-state inactive behavior (`tests/test-finalize-phase.sh:286-320`), checks normal non-COMPLETE feedback keeps `state.md` and increments the round (`tests/test-finalize-phase.sh:775-797`), and validates review output propagation (`tests/test-finalize-phase.sh:799-814`). Review issue extraction is specified in `tests/test-codex-review-merge.sh:3-15` and tested across marker placement, missing/empty logs, full-file scans, result-file creation, and multi-issue extraction (`tests/test-codex-review-merge.sh:57-354`). This research pass inspected tests only; it did not execute them.

- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for guard behavior that prevents local `.humanize` loop state from being committed and validates shell-safe loop/session discovery patterns. It is core-adjacent to the RLCR state machine because `.humanize/rlcr` is the state directory protected by validators, and active-loop discovery controls whether hooks engage.

- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and uses `git_adds_humanize` through helper assertions that lowercase commands before evaluation, matching validator call behavior (`tests/test-humanize-escape.sh:16-19` and `tests/test-humanize-escape.sh:44-69`). It defines block/allow cases for `.humanize` path variants, quoted variants, force/all variants, chained commands, `git -C` forms, and safe non-add commands (`tests/test-humanize-escape.sh:77-192`). It also simulates find-based iteration for empty directories, dotfiles-only directories, no `*-state.md` files, completed-state detection, and newest session directory selection (`tests/test-humanize-escape.sh:194-332`). It exits 0 when all assertions pass, otherwise exits 1 after printing pass/fail totals (`tests/test-humanize-escape.sh:334-352`).

- inputs_outputs_state: Inputs are synthetic git command strings, temporary directories under `/tmp`, a temporary `.humanize` directory for `git add -A` tests, and filesystem fixtures for state/session detection. Outputs are colored PASS/FAIL lines and an aggregate exit code. The test mutates only temp paths and returns to the original directory after `git add -A` coverage (`tests/test-humanize-escape.sh:128-144`).

- gates_or_invariants: Expected blocking includes direct `.humanize`, `./.humanize`, nested path-to-`.humanize`, and quoted forms (`tests/test-humanize-escape.sh:83-100`); force plus broad scope or all flags, including combined flags such as `-fA` and `-Af` (`tests/test-humanize-escape.sh:109-119`); all-mode adds when a `.humanize` directory exists (`tests/test-humanize-escape.sh:128-140`); chained shell segments (`tests/test-humanize-escape.sh:153-155`); and `git -C` or `--git-dir` forms (`tests/test-humanize-escape.sh:164-166`). Expected allowed behavior includes specific source files, `.gitignore`, non-add commands targeting `.humanize`, patch mode, and similarly named files that are not the `.humanize` directory (`tests/test-humanize-escape.sh:175-192`). These assertions map to `git_adds_humanize`, which detects git-add segments, strips quotes for path matching, blocks exact `.humanize` path components, detects force/all/broad-scope flags, and consults `.humanize` existence plus gitignore for broad non-force adds (`hooks/lib/loop-common.sh:914-1031`).

- dependencies_and_callers: Depends directly on `hooks/lib/loop-common.sh` for `to_lower` and `git_adds_humanize` (`tests/test-humanize-escape.sh:18`). The production caller for `git_adds_humanize` is the bash validator, which blocks matching commands before RLCR state-file checks (`hooks/loop-bash-validator.sh:109-118`). The zsh-safety/session sections are model tests for the repository’s find-based active-loop/session iteration style; production `find_active_loop` uses newest one-level loop directory and only treats `state.md` or `finalize-state.md` as active (`hooks/lib/loop-common.sh:152-175`).

- edge_cases_or_failure_modes: The test deliberately covers dotfile and empty-directory scenarios to avoid shell glob failures such as “no matches found” (`tests/test-humanize-escape.sh:201-246`). It verifies that `completed-state.md` can be found by a generic `*-state.md` scan in monitor-style code, but this is not the same as active-loop detection; production `find_active_loop` only treats `state.md` and `finalize-state.md` as active (`hooks/lib/loop-common.sh:169-174`). It also asserts that `git add -A src/` blocks whenever `.humanize` exists because the all flag can include broader tracked/untracked changes despite a path argument (`tests/test-humanize-escape.sh:135-140`). The test name mentions zsh safety, but the script itself runs under bash and validates find-based patterns rather than executing zsh.

- validation_or_tests: This file is itself the validation artifact. It cross-checks `.humanize` add-block policy and shell-safe directory traversal against production helpers. It is supplemented by broader session/state tests elsewhere, such as active-loop behavior for finalize and finished state files in `tests/test-finalize-phase.sh:286-320`.

- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Blocking prompt template that defines the post-round-0 ownership boundary for `goal-tracker.md`: Claude cannot directly edit it; Codex owns modifications after reviewing explicit update requests. This is a small prompt file, but it defines a real gate contract in the RLCR loop.

- algorithmic_behavior: The rendered message is titled with `{{CURRENT_ROUND}}`, states that after Round 0 only Codex can modify the Goal Tracker, forbids direct `goal-tracker.md` modification via Write, Edit, or Bash, and instructs Claude to put a `Goal Tracker Update Request` section in `{{SUMMARY_FILE}}` (`prompt-template/block/goal-tracker-modification.md:1-10`). It specifies the request schema: requested changes plus justification, then says Codex will apply justified changes (`prompt-template/block/goal-tracker-modification.md:12-25`).

- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `SUMMARY_FILE`, passed by `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:1104-1115`). Output is denial/instruction text emitted to stderr by validators or embedded in hook blocking responses. It does not mutate state directly; it redirects the desired state change into the next summary, where Codex can evaluate it during review.

- gates_or_invariants: The invariant is that after initial goal-tracker setup, direct modification is disallowed and update intent must be mediated through the summary file. Write validator enforces this when `is_goal_tracker_path` and `CURRENT_ROUND > 0` (`hooks/loop-write-validator.sh:188-196`). Edit validator enforces the same condition (`hooks/loop-edit-validator.sh:145-149`). Bash validator blocks shell-level goal-tracker modification in all rounds, using a different round-0 message and this template for later rounds (`hooks/loop-bash-validator.sh:343-358`). The stop hook then includes goal-tracker update instructions in Codex review prompts so the request can be evaluated (`hooks/loop-codex-stop-hook.sh:769-773` and `hooks/loop-codex-stop-hook.sh:800-816`).

- dependencies_and_callers: Called through `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1104-1115`. Production callers are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh` as cited above. It depends on the template loader’s safe render path and fallback behavior; if the template is unavailable, `loop-common` has an inline fallback (`hooks/lib/loop-common.sh:1109-1115`).

- edge_cases_or_failure_modes: The template is advisory text; enforcement depends on path detection in validators. It only governs direct tool/bash modifications, not Codex’s own reviewed update path. If a summary omits a structured update request, Codex has no template-directed change request to apply. If `CURRENT_ROUND` parsing is wrong, validators could select the wrong policy, which is why state parsing and strict validation are separately enforced in `loop-common` and hooks (`hooks/lib/loop-common.sh:250-336`).

- validation_or_tests: Template existence is covered as a common validator template in `tests/test-template-references.sh:149-167`. Production enforcement is exercised indirectly by validator suites and goal-tracker robustness tests; the direct reference path is confirmed in `hooks/lib/loop-common.sh:1104-1115`.

- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-116 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit prompt template for the RLCR Code Review Phase. It documents a `codex review` invocation and the transition rule from review findings to remediation or finalization. It is not fed to `codex review`; it is persisted so the loop directory has an auditable record of the review phase.

- algorithmic_behavior: The template records `{{BASE_BRANCH}}`, `{{REVIEW_ROUND}}`, and `{{TIMESTAMP}}`, then states that the phase runs `codex review --base {{BASE_BRANCH}}`, scans output for `[P0-9]` severity markers, returns a fix prompt if issues are found, and transitions to Finalize Phase if no issues are found (`prompt-template/codex/code-review-phase.md:1-18`). It documents the expected issue format and generated artifact files: review prompt/audit file, review result, command file, stdout capture, and stderr/log capture (`prompt-template/codex/code-review-phase.md:19-36`).

- inputs_outputs_state: Inputs are render variables from `run_codex_code_review`, primarily review round, base branch, and timestamp. The hook also passes base commit/review-base variables, but this template currently does not display them, while the inline fallback does (`hooks/loop-codex-stop-hook.sh:947-965`). Output is `round-{{REVIEW_ROUND}}-review-prompt.md` in the loop directory for audit purposes; real `codex review` output goes to the cache log and extracted result file (`hooks/loop-codex-stop-hook.sh:941-989`).

- gates_or_invariants: The template declares the review-phase gate: only absence of `[P0-9]` issues allows transition to finalize; any issue markers send Claude back into a review-fix round. The implementation enforces this in `run_and_handle_code_review`, where Codex review command failure blocks, `detect_review_issues` exit 2 blocks, detected issues call `continue_review_loop_with_issues`, and no detected issues calls `enter_finalize_phase` (`hooks/loop-codex-stop-hook.sh:999-1038`). `detect_review_issues` scans the entire log for severity markers in the first 10 characters of a line and writes extracted issues to the review result file (`hooks/lib/loop-common.sh:338-390`).

- dependencies_and_callers: Directly loaded in `run_codex_code_review` via `load_and_render_safe "$TEMPLATE_DIR" "codex/code-review-phase.md"` (`hooks/loop-codex-stop-hook.sh:927-965`). The same function selects `BASE_COMMIT` over `BASE_BRANCH` as the actual review base when available, preventing branch-ref drift from hiding changes (`hooks/loop-codex-stop-hook.sh:932-939`). It depends on Codex CLI, cache directory setup, template rendering, `detect_review_issues`, and the review-phase marker created during the COMPLETE transition (`hooks/loop-codex-stop-hook.sh:1424-1444` and `hooks/loop-codex-stop-hook.sh:1457-1480`).

- edge_cases_or_failure_modes: The template’s text says `codex review --base {{BASE_BRANCH}}`, but implementation may use `BASE_COMMIT` as the real `review_base`; the audit file could therefore under-report the exact base unless the template is expanded to include the extra variables already passed by the hook (`hooks/loop-codex-stop-hook.sh:932-965`). Because `codex review` does not accept prompt input, editing this template affects audit clarity, not the model’s review behavior (`prompt-template/codex/code-review-phase.md:3-4`). Missing or empty review logs are hard failures in code, not “no issues” (`hooks/lib/loop-common.sh:360-364`).

- validation_or_tests: The issue-detection contract stated by this template is tested in `tests/test-codex-review-merge.sh`, whose header repeats the algorithm and expected return behavior (`tests/test-codex-review-merge.sh:3-15`). The test suite covers issue markers near the start of a line, ignored markers later in a line, no-issue output, missing and empty logs, long logs, extraction from first issue to end, and result-file creation (`tests/test-codex-review-merge.sh:57-354`). Finalize-phase tests also mock `codex review` to validate that clean review output enters finalize while failures or empty output block (`tests/test-finalize-phase.sh:51-121` and `tests/test-finalize-phase.sh:566-690`).

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item evidence sections present; each assigned heading appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`