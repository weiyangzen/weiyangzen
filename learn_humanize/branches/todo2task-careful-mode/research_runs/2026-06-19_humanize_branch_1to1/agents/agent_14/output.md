# agent_14 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: Prompt-contract directory for the PR review loop. It is not executable code, but it defines the instructions, completion gates, and goal-tracker schema that `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh` render into live `.humanize/pr-loop/<timestamp>/` state. The directory contains seven templates totaling 6761 bytes: `round-0-header.md`, two round-0 task variants, two critical-requirements variants, `goal-tracker-initial.md`, and `codex-goal-tracker-update.md`.
- algorithmic_behavior: `scripts/setup-pr-loop.sh` renders `goal-tracker-initial.md` into `goal-tracker.md` using PR number, branch, start time, active bots, and startup case variables, then creates `round-0-prompt.md` from `round-0-header.md`, fetched PR comments, and either `round-0-task-no-comments.md` or `round-0-task-has-comments.md` depending on whether the comment file contains the no-comments sentinel. See `scripts/setup-pr-loop.sh:712`, `scripts/setup-pr-loop.sh:758`, `scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:800`, and `scripts/setup-pr-loop.sh:847`. The setup script also emits one of the critical requirements templates at startup, selecting no-comment versus has-comment behavior at `scripts/setup-pr-loop.sh:890` through `scripts/setup-pr-loop.sh:942`.
- inputs_outputs_state: Inputs are render variables such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `STARTED_AT`, and `STARTUP_CASE`. Outputs are rendered prompt text, live goal-tracker text, and startup instructions. `goal-tracker-initial.md:5` through `goal-tracker-initial.md:9` records PR number, branch, start time, monitored bots, and startup case; `goal-tracker-initial.md:17` through `goal-tracker-initial.md:25` initializes issue counts to zero; `goal-tracker-initial.md:29` through `goal-tracker-initial.md:33` creates the Round 0 log. `codex-goal-tracker-update.md:7` through `codex-goal-tracker-update.md:20` tells the local Codex review phase how to append per-reviewer rows, update totals, and add a round log entry.
- gates_or_invariants: The templates encode two startup branches. With existing comments, `round-0-task-has-comments.md:6` through `round-0-task-has-comments.md:21` requires analyzing comments, fixing issues, committing, pushing, and posting a trigger comment. With no comments, `round-0-task-no-comments.md:6` through `round-0-task-no-comments.md:15` requires only writing an initial summary and exiting so the stop hook can poll. Both task variants forbid modifying `.humanize/pr-loop/` state files at `round-0-task-has-comments.md:30` through `round-0-task-has-comments.md:35` and `round-0-task-no-comments.md:18` through `round-0-task-no-comments.md:23`. The critical requirements templates repeat the completion gate: has-comments requires commit, push, trigger comment, and summary at `critical-requirements-has-comments.md:6` through `critical-requirements-has-comments.md:23`; no-comments forbids a trigger comment and asks for the initial summary at `critical-requirements-no-comments.md:6` through `critical-requirements-no-comments.md:20`.
- dependencies_and_callers: Direct callers are `scripts/setup-pr-loop.sh` for all initial prompt and critical requirement templates and `hooks/pr-loop-stop-hook.sh` for `codex-goal-tracker-update.md`. The stop hook injects the goal-tracker update instructions into a local Codex validation prompt at `hooks/pr-loop-stop-hook.sh:1252` through `hooks/pr-loop-stop-hook.sh:1263`. Rendering depends on `hooks/lib/template-loader.sh` through `load_and_render_safe`, which supplies fallbacks if a template is missing. State coordination depends on `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-prompt.md`, and `round-0-pr-resolve.md`.
- edge_cases_or_failure_modes: Missing templates do not hard-fail setup because every call uses `load_and_render_safe` with inline fallback text. The no-comment branch is keyed to the exact sentinel `*No comments found.*` in `scripts/setup-pr-loop.sh:723` through `scripts/setup-pr-loop.sh:729`; malformed or unexpected comment output will be treated as “comments exist” and route to the has-comments branch. The goal-tracker update prompt is advisory to Codex, but `update_pr_goal_tracker` in `hooks/lib/loop-common.sh:1171` through `hooks/lib/loop-common.sh:1173` exists as consistency backstop when Codex does not update correctly.
- validation_or_tests: `tests/test-pr-loop-system.sh:642` through `tests/test-pr-loop-system.sh:655` validates required sections in `goal-tracker-initial.md`, including Issue Summary, Total Statistics, Issue Log, and issue total fields. `tests/test-template-references.sh:52` through `tests/test-template-references.sh:111` scans template references from hook scripts and verifies the files exist. `tests/run-all-tests.sh:53` through `tests/run-all-tests.sh:54` includes PR loop suites, and `tests/run-all-tests.sh:72` includes PR loop API robustness.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable test orchestrator. It is not part of runtime hooks, but it is the canonical aggregate specification for core algorithm behavior across template loading, RLCR hook validation, PR-loop state handling, monitor behavior, and robustness suites.
- algorithmic_behavior: Defines an ordered `TEST_SUITES` array at `tests/run-all-tests.sh:32` through `tests/run-all-tests.sh:73`, then iterates over each suite at `tests/run-all-tests.sh:80` through `tests/run-all-tests.sh:139`. For each suite it skips missing files, detects whether the suite must run under zsh, captures stdout/stderr, records the exit code, strips ANSI color codes, extracts the latest `Passed:` and `Failed:` counters, accumulates totals, and records failed suite names.
- inputs_outputs_state: Input is the repository’s `tests/` tree and executable test files. It derives `SCRIPT_DIR` from the script path at `tests/run-all-tests.sh:14`. Output is a terminal report with per-suite `PASSED`, `FAILED`, or `SKIP` lines, plus a final summary at `tests/run-all-tests.sh:141` through `tests/run-all-tests.sh:158`. Internal state is `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` initialized at `tests/run-all-tests.sh:28` through `tests/run-all-tests.sh:30`.
- gates_or_invariants: A missing suite is skipped rather than failed at `tests/run-all-tests.sh:83` through `tests/run-all-tests.sh:86`, so the orchestrator assumes optional or platform-dependent test files may be absent. The only suite explicitly forced to zsh is `test-zsh-monitor-safety.sh` via `ZSH_TESTS` at `tests/run-all-tests.sh:75` through `tests/run-all-tests.sh:78`; if zsh is unavailable, it skips that suite at `tests/run-all-tests.sh:106` through `tests/run-all-tests.sh:114`. A suite fails the aggregate if its process exit is nonzero or if parsed `Failed:` count is greater than zero at `tests/run-all-tests.sh:130` through `tests/run-all-tests.sh:137`.
- dependencies_and_callers: Depends on each listed suite being executable or zsh-runnable. The assigned hook-input robustness script is included at `tests/run-all-tests.sh:61`. Template loader, PR loop, monitor, plan-file, stop-hook, and robustness behavior are indirectly covered by the named suite scripts. It uses standard shell tools: `sed`, `grep`, `tail`, and optionally `zsh`.
- edge_cases_or_failure_modes: The script intentionally uses `set -uo pipefail` rather than `set -e` globally at `tests/run-all-tests.sh:12`, then wraps each suite execution in `set +e` and `set -e` at `tests/run-all-tests.sh:105` through `tests/run-all-tests.sh:119` so one failing suite does not stop the aggregate. Counter extraction is text-based: suites must emit `Passed:` and `Failed:` lines, otherwise they count as zero even if they passed behaviorally. Failed suite output is truncated to the last 30 lines at `tests/run-all-tests.sh:133` through `tests/run-all-tests.sh:134`, which keeps logs readable but can hide the first failing assertion.
- validation_or_tests: This script validates by executing the suite list itself. It exits `1` if any suite failed at `tests/run-all-tests.sh:148` through `tests/run-all-tests.sh:155`; otherwise it exits `0` at `tests/run-all-tests.sh:156` through `tests/run-all-tests.sh:158`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-074 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared template loading and rendering library for RLCR and PR-loop hook messages. It is a key adapter between hook state transitions and human-readable prompt/block templates. Runtime validators source it indirectly through `hooks/lib/loop-common.sh`, which sources this file at `hooks/lib/loop-common.sh:140` through `hooks/lib/loop-common.sh:145`.
- algorithmic_behavior: `get_template_dir` computes the template root by going two levels up from `hooks/lib` and appending `prompt-template` at `hooks/lib/template-loader.sh:26` through `hooks/lib/template-loader.sh:31`. `load_template` reads a template if present and returns empty content plus a warning if missing at `hooks/lib/template-loader.sh:36` through `hooks/lib/template-loader.sh:48`. `render_template` accepts `VAR=value` pairs, exports them with `TMPL_VAR_` prefixes, and runs an awk single-pass renderer at `hooks/lib/template-loader.sh:58` through `hooks/lib/template-loader.sh:132`. `load_and_render` combines load plus render at `hooks/lib/template-loader.sh:136` through `hooks/lib/template-loader.sh:147`. `append_template` concatenates base and additional template content at `hooks/lib/template-loader.sh:151` through `hooks/lib/template-loader.sh:161`. `load_and_render_safe` uses caller-provided fallback text if the template is missing or render output is empty at `hooks/lib/template-loader.sh:170` through `hooks/lib/template-loader.sh:203`. `validate_template_dir` checks that the template root contains `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:208` through `hooks/lib/template-loader.sh:222`.
- inputs_outputs_state: Inputs are a template directory, template name, template content, fallback text, and substitution assignments. Outputs are rendered markdown or block text on stdout; warnings/errors go to stderr. The renderer’s temporary state is an awk `vars` map built from `TMPL_VAR_` environment variables at `hooks/lib/template-loader.sh:74` through `hooks/lib/template-loader.sh:83`.
- gates_or_invariants: Rendering is explicitly single-pass to prevent placeholder injection: comments at `hooks/lib/template-loader.sh:54` through `hooks/lib/template-loader.sh:57` state that placeholder-looking text inside a value is not recursively expanded. Unknown placeholders are kept as original `{{NAME}}` tokens at `hooks/lib/template-loader.sh:115` through `hooks/lib/template-loader.sh:122`. Unclosed `{{` sequences are treated literally at `hooks/lib/template-loader.sh:102` through `hooks/lib/template-loader.sh:109`. The loader fails soft for missing templates; critical callers are expected to use `load_and_render_safe`.
- dependencies_and_callers: Direct callers are runtime validators and stop hooks through `loop-common.sh`. `hooks/loop-read-validator.sh:127`, `hooks/loop-read-validator.sh:144`, and `hooks/loop-read-validator.sh:163` render block messages through the safe path. `hooks/loop-write-validator.sh:207`, `hooks/loop-write-validator.sh:238`, and `hooks/loop-write-validator.sh:258` do the same for write gates. `hooks/pr-loop-stop-hook.sh:1261` uses it to render PR-loop goal-tracker update instructions. Tests source it directly in `tests/test-template-loader.sh:12`, `tests/robustness/test-template-stress-robustness.sh:16`, and `tests/robustness/test-template-error-robustness.sh:17`.
- edge_cases_or_failure_modes: The documented variable syntax says uppercase letters, numbers, and underscores, but the awk renderer itself does not enforce that; it extracts whatever appears between `{{` and `}}`. Values containing shell metacharacters are passed through environment variables and printed literally rather than evaluated. Missing directories, empty templates, permission failures, BOMs, whitespace-only files, special filenames, symlinks, and concurrent template loads are covered by robustness tests. A subtle portability point: `validate_template_dir` requires `block`, `codex`, and `claude`; it does not require `pr-loop`, so PR-loop templates can be absent without directory validation failing, relying on per-call fallback behavior instead.
- validation_or_tests: `tests/test-template-loader.sh:41` through `tests/test-template-loader.sh:244` covers template directory discovery, existing/missing template loads, basic substitutions, unreplaced placeholders, safe fallback behavior, and directory validation. `tests/test-template-loader.sh:247` through `tests/test-template-loader.sh:486` verifies literal handling for shell metacharacters, paths, regex-like text, JSON-like text, and multiline values. `tests/test-template-loader.sh:488` through `tests/test-template-loader.sh:543` verifies placeholder injection prevention. `tests/robustness/test-template-stress-robustness.sh:82` through `tests/robustness/test-template-stress-robustness.sh:151` covers 10KB and 100KB values/templates and many substitutions. `tests/robustness/test-template-error-robustness.sh:34` through `tests/robustness/test-template-error-robustness.sh:63` covers missing and empty templates; `tests/robustness/test-template-error-robustness.sh:208` through `tests/robustness/test-template-error-robustness.sh:334` covers filesystem and concurrency edge cases.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR read validator when an agent attempts to read a `round-N-prompt.md` or `round-N-summary.md` file from a non-current round. It defines a safety gate against stale prompt/summary context.
- algorithmic_behavior: The template declares the attempted round/file type and current round, then points to the current round’s prompt and summary paths and says how to read the requested file if needed. The template body is at `prompt-template/block/wrong-round-file.md:1` through `prompt-template/block/wrong-round-file.md:9`. It is rendered by `hooks/loop-read-validator.sh` when `CLAUDE_ROUND` differs from `CURRENT_ROUND` and the path is not allowlisted.
- inputs_outputs_state: Inputs are `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`, passed from `hooks/loop-read-validator.sh:144` through `hooks/loop-read-validator.sh:149`. Output is stderr block text and exit code `2` from the read validator at `hooks/loop-read-validator.sh:150`.
- gates_or_invariants: The gate applies only after the read validator confirms the tool is `Read`, validates JSON input, requires `tool_input.file_path`, detects a round prompt or summary path, finds the active loop, and parses the current round from strict state at `hooks/loop-read-validator.sh:25` through `hooks/loop-read-validator.sh:100`. It does not block allowlisted historical files; `hooks/loop-read-validator.sh:138` checks `! is_allowlisted_file`. The allowlist includes selected todo and summary files at `hooks/lib/loop-common.sh:429` through `hooks/lib/loop-common.sh:447`.
- dependencies_and_callers: Depends on `load_and_render_safe` from the template loader and shared path/round helpers from `loop-common.sh`: `find_active_loop`, `parse_state_file_strict`, `extract_round_number`, `is_round_file_type`, and `is_allowlisted_file`. The caller section is `hooks/loop-read-validator.sh:135` through `hooks/loop-read-validator.sh:151`. Related sibling templates handle wrong file location and wrong directory path at `hooks/loop-read-validator.sh:122` through `hooks/loop-read-validator.sh:131` and `hooks/loop-read-validator.sh:157` through `hooks/loop-read-validator.sh:167`.
- edge_cases_or_failure_modes: If there is no active RLCR loop, the read validator exits `0` before this gate at `hooks/loop-read-validator.sh:84` through `hooks/loop-read-validator.sh:86`. If the active state file is malformed, the validator fails closed with exit `1` before rendering this template at `hooks/loop-read-validator.sh:94` through `hooks/loop-read-validator.sh:98`. If the template is missing or empty, the inline fallback in `hooks/loop-read-validator.sh:139` through `hooks/loop-read-validator.sh:143` is used. PR-loop files are not governed by this template; `hooks/loop-read-validator.sh:11` through `hooks/loop-read-validator.sh:12` explicitly says PR-loop files are generally readable.
- validation_or_tests: `tests/test-template-references.sh:52` through `tests/test-template-references.sh:111` verifies referenced templates exist for loader calls. `tests/test-template-loader.sh:147` through `tests/test-template-loader.sh:164` exercises a sibling wrong-round template through `load_and_render`. PR-loop hook tests include wrong round write/edit cases, but direct coverage for this exact read block is through the read-validator template reference and the shared loader/validator behavior.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-134 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for hook JSON input parsing, guard exit semantics, command-modification detection, and monitor edge-case behavior. It pipes synthetic JSON into real production validators and invokes real monitor/parser functions rather than mocking those surfaces.
- algorithmic_behavior: The script initializes `PROJECT_ROOT`, sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, creates a temp test directory, then runs 26 numbered tests before exiting through `print_test_summary` at `tests/robustness/test-hook-input-robustness.sh:14` through `tests/robustness/test-hook-input-robustness.sh:20` and `tests/robustness/test-hook-input-robustness.sh:666` through `tests/robustness/test-hook-input-robustness.sh:671`. Tests 1-3 pipe valid Read, Write, and Bash JSON into `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh` and expect exit `0` at `tests/robustness/test-hook-input-robustness.sh:35` through `tests/robustness/test-hook-input-robustness.sh:69`. Tests 4-6 verify malformed JSON, empty JSON, and missing `tool_input.file_path` are rejected with nonzero non-signal exits at `tests/robustness/test-hook-input-robustness.sh:71` through `tests/robustness/test-hook-input-robustness.sh:115`. Tests 7-10c exercise long command input, special shell characters, Unicode, unknown tool passthrough, deep JSON rejection, invalid UTF-8 rejection, and null-byte graceful handling at `tests/robustness/test-hook-input-robustness.sh:117` through `tests/robustness/test-hook-input-robustness.sh:235`.
- inputs_outputs_state: Inputs are synthetic hook JSON strings and generated temporary files/logs under `TEST_DIR`. Validator invocations set `CLAUDE_PROJECT_DIR="$TEST_DIR"` so hooks resolve loop state against the test sandbox. Output is pass/fail records through `pass`/`fail` from `tests/test-helpers.sh:30` through `tests/test-helpers.sh:44`, and the final process exit comes from `print_test_summary` at `tests/test-helpers.sh:58` through `tests/test-helpers.sh:78`.
- gates_or_invariants: The script encodes exit-code invariants: valid non-loop tool operations should pass with `0`; malformed or missing required hook input should reject with nonzero below 128; invalid UTF-8 for Bash input must reject specifically with exit `1`; null bytes may be stripped by Bash command substitution, so the invariant is only “no signal crash” with exit below 128. These match `validate_hook_input` in `hooks/lib/loop-common.sh:60` through `hooks/lib/loop-common.sh:97`, `require_tool_input_field` in `hooks/lib/loop-common.sh:102` through `hooks/lib/loop-common.sh:115`, and `is_deeply_nested` in `hooks/lib/loop-common.sh:120` through `hooks/lib/loop-common.sh:134`. The assigned test covers Read, Write, and Bash validators; it does not exercise `loop-edit-validator.sh`, which still parses `jq` directly at `hooks/loop-edit-validator.sh:24` through `hooks/loop-edit-validator.sh:32`.
- dependencies_and_callers: Direct dependencies are `jq`, `bash`, core shell tools, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`, and test helpers. Test 21 sources `scripts/humanize.sh` and calls `humanize_parse_goal_tracker` at `tests/robustness/test-hook-input-robustness.sh:381` through `tests/robustness/test-hook-input-robustness.sh:408`; the parser is defined at `scripts/humanize.sh:32` through `scripts/humanize.sh:112`. Test 22 calls `humanize_parse_git_status` at `tests/robustness/test-hook-input-robustness.sh:410` through `tests/robustness/test-hook-input-robustness.sh:422`; the function is defined at `scripts/humanize.sh:166` through `scripts/humanize.sh:210`. Test 23 calls `humanize_detect_git_state` at `tests/robustness/test-hook-input-robustness.sh:424` through `tests/robustness/test-hook-input-robustness.sh:433`; the function is defined at `scripts/humanize.sh:116` through `scripts/humanize.sh:162`.
- edge_cases_or_failure_modes: Monitor tests assert graceful behavior around missing session directories, narrow terminals, ANSI-coded logs, session deletion during monitoring, and wrapper exit extraction. Test 24 creates a project with no `.humanize` directory and accepts monitor exit `0` or `1` at `tests/robustness/test-hook-input-robustness.sh:444` through `tests/robustness/test-hook-input-robustness.sh:486`. Test 25 builds a fake `.humanize/rlcr` session, cache log, 40-column terminal shim, deletes the session while monitor runs, and requires exit below 128 at `tests/robustness/test-hook-input-robustness.sh:488` through `tests/robustness/test-hook-input-robustness.sh:578`. Test 26 repeats this with ANSI-coded logs at `tests/robustness/test-hook-input-robustness.sh:580` through `tests/robustness/test-hook-input-robustness.sh:664`. Command pattern tests 19-20 directly check `command_modifies_file` for `sed -i` and redirect writes at `tests/robustness/test-hook-input-robustness.sh:351` through `tests/robustness/test-hook-input-robustness.sh:370`; that helper is defined at `hooks/lib/loop-common.sh:1074` through `hooks/lib/loop-common.sh:1102`.
- validation_or_tests: This file is itself a validation suite and is included in the aggregate runner at `tests/run-all-tests.sh:61`. It validates production hook input behavior against real validators: Read validator starts with strict JSON/depth/field checks at `hooks/loop-read-validator.sh:25` through `hooks/loop-read-validator.sh:48`, Write validator mirrors that at `hooks/loop-write-validator.sh:25` through `hooks/loop-write-validator.sh:48`, and Bash validator mirrors that at `hooks/loop-bash-validator.sh:24` through `hooks/loop-bash-validator.sh:48`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item headings above; each assigned item is represented once as a `###` evidence section, with IDs intentionally not repeated here to keep the output single-occurrence by heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`