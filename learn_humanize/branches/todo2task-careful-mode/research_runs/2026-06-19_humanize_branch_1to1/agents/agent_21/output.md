# agent_21 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-021 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow entrypoint for the PR review loop. It is a thin command definition that delegates execution to `scripts/setup-pr-loop.sh` through the allowed Bash tool declaration at `commands/start-pr-loop.md:1-5` and command body at `commands/start-pr-loop.md:10-14`.
- algorithmic_behavior: Defines the user-facing PR loop algorithm: detect the PR for the current branch, fetch selected bot review comments, have Claude fix and push changes, tag the configured bots, let the Stop Hook poll every 30 seconds for up to 15 minutes, and then use local Codex validation to decide whether remote concerns remain or approval has been reached (`commands/start-pr-loop.md:16-24`). It also defines comment priority: human comments take precedence over bot comments, and bot comments are processed newest-first (`commands/start-pr-loop.md:31-36`).
- inputs_outputs_state: Inputs are slash-command arguments passed as `$ARGUMENTS` into `setup-pr-loop.sh`, with required bot flags `--claude` or `--codex` and optional `--max`, `--codex-model`, and `--codex-timeout` (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25-30`). The delegated setup script creates `.humanize/pr-loop/<timestamp>/state.md` with `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, Codex settings, poll timings, startup case, latest commit metadata, and trigger comment metadata (`scripts/setup-pr-loop.sh:615-635`). It also creates `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-prompt.md`, and `round-0-pr-resolve.md` references (`scripts/setup-pr-loop.sh:425-431`, `scripts/setup-pr-loop.sh:641-715`, `scripts/setup-pr-loop.sh:721-851`).
- gates_or_invariants: Requires at least one bot flag (`commands/start-pr-loop.md:25-30`; implemented in `scripts/setup-pr-loop.sh:179-190`). The workflow requires summaries before exit, pushed fixes for bot review, correct bot mentions, and forbids exiting by editing state or canceling indirectly (`commands/start-pr-loop.md:50-56`). The setup script enforces mutual exclusion with active RLCR or PR loops (`scripts/setup-pr-loop.sh:220-241`), git repository and HEAD availability (`scripts/setup-pr-loop.sh:243-253`), authenticated GitHub CLI (`scripts/setup-pr-loop.sh:255-269`), Codex CLI presence (`scripts/setup-pr-loop.sh:271-277`), PR existence and open state (`scripts/setup-pr-loop.sh:351-374`), and YAML-safe branch/model/effort values (`scripts/setup-pr-loop.sh:384-407`).
- dependencies_and_callers: The command depends directly on `scripts/setup-pr-loop.sh` (`commands/start-pr-loop.md:12-14`). That script depends on `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, and `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:33-42`, `scripts/setup-pr-loop.sh:210-214`). It shells out to `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`, `gh`, `git`, and `codex` (`scripts/setup-pr-loop.sh:430-438`, `scripts/setup-pr-loop.sh:527-587`). Runtime continuation is handled by `hooks/pr-loop-stop-hook.sh`, which parses PR loop state, polls bots, runs Codex validation, updates `active_bots`, and renames state files for terminal conditions (`hooks/pr-loop-stop-hook.sh:1-19`, `hooks/pr-loop-stop-hook.sh:87-174`, `hooks/pr-loop-stop-hook.sh:1232-1302`, `hooks/pr-loop-stop-hook.sh:1368-1387`, `hooks/pr-loop-stop-hook.sh:1567-1595`).
- edge_cases_or_failure_modes: Startup fails when no bot flag is supplied, an RLCR/PR loop is already active, the repo lacks git history, `gh` is missing or unauthenticated, Codex is missing, no PR is found, PR is closed or merged, branch/model values are YAML-unsafe, or Claude eyes verification cannot be completed for required Claude trigger comments (`scripts/setup-pr-loop.sh:179-190`, `scripts/setup-pr-loop.sh:225-241`, `scripts/setup-pr-loop.sh:243-277`, `scripts/setup-pr-loop.sh:351-390`, `scripts/setup-pr-loop.sh:564-599`). Runtime edge cases include merged/closed PRs becoming `merged-state.md` or `closed-state.md`, max-iteration exit to `maxiter-state.md`, missing trigger comments blocking exit, waiting bots blocking without round advancement, usage-limit terminal state, and all-bots-approved completion (`hooks/pr-loop-stop-hook.sh:239-253`, `hooks/pr-loop-stop-hook.sh:429-445`, `hooks/pr-loop-stop-hook.sh:724-746`, `hooks/pr-loop-stop-hook.sh:1390-1410`, `hooks/pr-loop-stop-hook.sh:1418`, `hooks/pr-loop-stop-hook.sh:1591-1595`).
- validation_or_tests: Covered by PR loop tests such as `tests/test-pr-loop-scripts.sh` for command/script behavior and `tests/test-pr-loop-system.sh` for mutual exclusion, setup integration, trigger comment handling, and goal tracker creation. The command’s own documentation also states terminal conditions: max iteration count, all monitored bots approve, or `/humanize:cancel-pr-loop` (`commands/start-pr-loop.md:58-62`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-051 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for the Codex review log issue-detection algorithm used by the RLCR stop hook to decide whether to continue the loop with feedback or enter finalize. The test sources `hooks/lib/loop-common.sh`, specifically `detect_review_issues`, at `tests/test-codex-review-merge.sh:41-42`.
- algorithmic_behavior: The header states the required algorithm exactly: scan the entire log from line 1, find the first line where `[P?]` with a digit appears within the first 10 characters, extract from that line through EOF, output it, and return no-issue status when absent (`tests/test-codex-review-merge.sh:5-15`). The implementation under test matches this: `detect_review_issues` constructs `round-N-codex-review.log` and `round-N-review-result.md`, rejects missing or empty logs with return `2`, uses `awk` on `substr($0, 1, 10) ~ /\[P[0-9]\]/`, writes extracted content to the result file, returns `0` with content, and returns `1` when no issue marker is found (`hooks/lib/loop-common.sh:355-398`).
- inputs_outputs_state: Inputs are `round` argument, global `CACHE_DIR`, and global `LOOP_DIR` (`hooks/lib/loop-common.sh:344-358`). Test setup creates an isolated `XDG_CACHE_HOME`, a loop directory, and per-round log files (`tests/test-codex-review-merge.sh:33-54`). Outputs are function stdout containing `## Codex Review Issues` plus extracted review content, side-effect file `$LOOP_DIR/round-N-review-result.md`, stderr diagnostics, and exit codes `0`, `1`, or `2` (`hooks/lib/loop-common.sh:338-343`, `hooks/lib/loop-common.sh:387-397`).
- gates_or_invariants: Marker matching is deliberately position-gated: `[P0-9]` must appear in the first 10 characters of a line, so prose mentions later in a line do not count (`tests/test-codex-review-merge.sh:83-107`). The scan must cover the whole file rather than the last N lines, tested with markers late in long logs and early in long logs (`tests/test-codex-review-merge.sh:171-227`). The first matching marker establishes the extraction boundary, and later markers are included only because they are after the first match (`tests/test-codex-review-merge.sh:229-255`). Missing or empty log files are hard errors (`tests/test-codex-review-merge.sh:133-169`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` (`tests/test-codex-review-merge.sh:41-42`). Runtime caller is `run_and_handle_code_review` in `hooks/loop-codex-stop-hook.sh`, which treats return `2` as blocking review failure, return `0` as “continue loop with issues,” and return `1` as permission to enter finalize phase (`hooks/loop-codex-stop-hook.sh:1010-1038`). It uses the cache log created by `run_codex_code_review` and stores extracted review content in loop state files consumed by next-round prompt generation.
- edge_cases_or_failure_modes: Tests cover marker at position 0, dash-prefixed common review format, exactly 50-line logs, markers after line 50, markers before line 50 in long logs, absent markers, markers outside the first 10 characters, multiple markers, missing logs, and empty logs (`tests/test-codex-review-merge.sh:57-354`). A subtle accepted behavior is that any `[Pdigit]` in the first 10 characters counts, regardless of priority digit value, so `[P3]` is valid (`tests/test-codex-review-merge.sh:257-278`).
- validation_or_tests: This file is itself the validation surface: 12 tests, with pass/fail counters and nonzero exit when any test fails (`tests/test-codex-review-merge.sh:23-31`, `tests/test-codex-review-merge.sh:356-368`). It validates both return-code semantics and result file creation (`tests/test-codex-review-merge.sh:305-330`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-081 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block used by the RLCR stop hook when git is not clean and the dirty state includes untracked files other than `.humanize*`. It supplies operator guidance that untracked files may be generated artifacts and should often be ignored instead of committed (`prompt-template/block/git-not-clean-untracked.md:2-10`).
- algorithmic_behavior: This template does not implement shell logic, but it participates in the git-cleanliness gate by adding a specialized note to a blocking prompt. The stop hook computes cached git status, extracts untracked entries with `grep '^??'`, excludes `.humanize`, and loads this template when other untracked files remain (`hooks/loop-codex-stop-hook.sh:523-547`). The content enumerates artifact classes: build outputs, dependencies, IDE/editor files, logs, caches, and temporary files (`prompt-template/block/git-not-clean-untracked.md:3-8`).
- inputs_outputs_state: Inputs are implicit: the stop hook’s `GIT_STATUS_CACHED` and derived `OTHER_UNTRACKED` decide whether the block is loaded (`hooks/loop-codex-stop-hook.sh:523-542`). Output is markdown text concatenated into `SPECIAL_NOTES`, then embedded into the larger “Git Not Clean” block that prevents loop progress until changes are committed, ignored, or otherwise handled (`hooks/loop-codex-stop-hook.sh:542-553`).
- gates_or_invariants: The invariant is that RLCR should not proceed through stop-hook review/finalize while the repository has uncommitted changes. Untracked non-humanize files trigger an explicit `.gitignore` review reminder, while `.humanize*` untracked files use a separate local-state note (`hooks/loop-codex-stop-hook.sh:530-547`). The template’s guidance reinforces the policy distinction between source changes that should be committed and generated artifacts that should be ignored (`prompt-template/block/git-not-clean-untracked.md:2-10`).
- dependencies_and_callers: Loaded by `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` in `hooks/loop-codex-stop-hook.sh:542`. Template loading is provided by `hooks/lib/template-loader.sh`, sourced through `hooks/lib/loop-common.sh` and stop-hook setup (`hooks/lib/loop-common.sh:140-145`). It is sibling to `prompt-template/block/git-not-clean-humanize-local.md`, which handles `.humanize*` local loop directories separately (`hooks/loop-codex-stop-hook.sh:530-536`).
- edge_cases_or_failure_modes: If the template cannot be loaded, the stop hook falls back to a single-line message: “Review untracked files - add to .gitignore or commit them” (`hooks/loop-codex-stop-hook.sh:542-545`). The gate distinguishes `.humanize` from other untracked paths using grep patterns, so unusual filenames containing `.humanize` may be grouped into the local-note branch (`hooks/loop-codex-stop-hook.sh:530-540`).
- validation_or_tests: Indirectly validated by git-cleanliness and hook tests covering stop-hook behavior and `.humanize` protection. The template itself is static markdown; its algorithmic value is in the stop hook branch that loads it (`hooks/loop-codex-stop-hook.sh:523-547`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-111 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Main Claude prompt template for the next RLCR implementation round after Codex finds review issues. It converts Codex review output into required Claude actions while preserving read-only access to the original plan and goal tracker after Round 0.
- algorithmic_behavior: The template instructs Claude that the work is not finished, requires rereading the original implementation plan via `@{{PLAN_FILE}}`, requires creating Todos for all discovered issues instead of only the highest-priority ones, injects Codex review content between explicit markers, and requires reading the goal tracker before work (`prompt-template/claude/next-round-prompt.md:1-31`). It establishes an important state-transition contract: after Round 0, `goal-tracker.md` is read-only to Claude, and changes must be requested via a summary section rather than direct edits (`prompt-template/claude/next-round-prompt.md:22-31`).
- inputs_outputs_state: Inputs are template variables `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` (`prompt-template/claude/next-round-prompt.md:5-6`, `prompt-template/claude/next-round-prompt.md:16-19`, `prompt-template/claude/next-round-prompt.md:22-31`). The stop hook writes rendered output to `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md` after incrementing `current_round` in `state.md` (`hooks/loop-codex-stop-hook.sh:1518-1537`). Downstream output expected from Claude is implementation work plus a round summary and, when needed, a “Goal Tracker Update Request” rather than direct goal tracker modification (`prompt-template/claude/next-round-prompt.md:30-31`).
- gates_or_invariants: The prompt’s core invariants are full-scope issue handling, alignment with the original plan, and protection of the goal tracker after Round 0. Its “ALL discovered issues” instruction is a behavioral gate against resolving only the highest-severity Codex findings (`prompt-template/claude/next-round-prompt.md:12-14`). The read-only goal tracker rule is enforced separately by validators and hooks that block post-Round-0 goal tracker modification (`hooks/lib/loop-common.sh:1104-1116`).
- dependencies_and_callers: Rendered by `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-prompt.md"` in `hooks/loop-codex-stop-hook.sh:1534-1537`. The review content fed into it originates from `detect_review_issues` and `continue_review_loop_with_issues` after Codex review detects `[Pdigit]` findings (`hooks/lib/loop-common.sh:355-398`, `hooks/loop-codex-stop-hook.sh:1022-1033`). Template safety depends on `hooks/lib/template-loader.sh`; grep results show tests around safe replacement of `REVIEW_CONTENT` containing template-like syntax in `tests/test-template-loader.sh`.
- edge_cases_or_failure_modes: If template rendering fails, `loop-codex-stop-hook.sh` has a fallback next-round prompt containing review content and plan/goal references (`hooks/loop-codex-stop-hook.sh:1525-1537`). If `REVIEW_CONTENT` contains its own `{{...}}` text, the safe template loader is intended to prevent recursive substitution; this is covered by template-loader tests referenced in the repository search results. The prompt itself ends at the read-only goal-tracker warning in the inspected file, so any additional operational requirements are expected to come from sibling footers or hook-generated instructions rather than this file.
- validation_or_tests: Covered indirectly by comprehensive template tests and stop-hook next-round generation. The exact caller path is visible at `hooks/loop-codex-stop-hook.sh:1521-1537`, and the static placeholders are traceable in `prompt-template/claude/next-round-prompt.md:5-31`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-141 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state-file parsing, including tolerant parsing, strict validation, defaulting, malformed YAML handling, and the newer `full_review_round` field. It sources `hooks/lib/loop-common.sh` and shared test helpers (`tests/robustness/test-state-file-robustness.sh:14-19`).
- algorithmic_behavior: Exercises three state readers: `get_current_round`, `parse_state_file`, and `parse_state_file_strict`. `get_current_round` extracts `current_round` from YAML frontmatter and defaults to `0` (`hooks/lib/loop-common.sh:177-190`). `parse_state_file` is tolerant: it parses known fields, strips quotes for selected string fields, ignores extras, defaults `current_round` to `0`, `max_iterations` to `10`, `push_every_round` to `false`, `full_review_round` to `5`, and leaves `review_started` unset for schema validation (`hooks/lib/loop-common.sh:192-248`). `parse_state_file_strict` requires frontmatter separators, required fields, numeric round/max values, boolean `review_started`, and `base_branch`, while still defaulting optional fields (`hooks/lib/loop-common.sh:250-336`).
- inputs_outputs_state: Inputs are generated temporary `state.md` variants under `$TEST_DIR`; no repository files are modified. Outputs are exported/global shell variables like `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_START_BRANCH`, and `STATE_FULL_REVIEW_ROUND`, plus return codes from parser functions (`tests/robustness/test-state-file-robustness.sh:33-60`, `tests/robustness/test-state-file-robustness.sh:100-122`, `tests/robustness/test-state-file-robustness.sh:399-474`). State transition behavior is read-side only: it validates whether state files can be safely consumed by hooks before the loop advances.
- gates_or_invariants: Strict parser must reject missing YAML frontmatter, missing required fields, non-numeric `current_round`, and malformed required schema (`tests/robustness/test-state-file-robustness.sh:149-215`). Tolerant parser must remain resilient and provide defaults for missing or malformed cases where safe (`tests/robustness/test-state-file-robustness.sh:163-228`). Existing invariants include preserving zero round values, handling quoted paths/branches, ignoring unknown metadata, and using default `full_review_round=5` when missing (`tests/robustness/test-state-file-robustness.sh:62-139`, `tests/robustness/test-state-file-robustness.sh:425-448`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` parser functions and `tests/test-helpers.sh` test lifecycle functions (`tests/robustness/test-state-file-robustness.sh:14-19`). Runtime callers include RLCR validators (`hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`) and the Codex stop hook (`hooks/loop-codex-stop-hook.sh:95-112`). Setup writes `full_review_round` to state in `scripts/setup-rlcr-loop.sh:724`, which this test verifies can be parsed.
- edge_cases_or_failure_modes: Covers extra fields, quoted values, round `0`, missing frontmatter, non-numeric current round, missing required fields, empty files, partial YAML, malformed YAML, very large values, negative values, special characters and spaces in string values, trailing whitespace, nonexistent files, binary content after YAML, CRLF line endings, explicit `full_review_round`, missing `full_review_round`, and minimum valid `full_review_round=2` (`tests/robustness/test-state-file-robustness.sh:72-474`). Some tolerant-mode assertions intentionally accept “does not crash” behavior rather than a single canonical value, such as CRLF and non-numeric tolerant cases (`tests/robustness/test-state-file-robustness.sh:190-199`, `tests/robustness/test-state-file-robustness.sh:386-397`).
- validation_or_tests: This file contains 21 tests and exits through `print_test_summary`, returning the aggregate test status (`tests/robustness/test-state-file-robustness.sh:476-481`). It directly validates the parser behavior relied on by stop hooks and validators before they allow RLCR state transitions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item evidence sections present; item IDs are used only as their section headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`