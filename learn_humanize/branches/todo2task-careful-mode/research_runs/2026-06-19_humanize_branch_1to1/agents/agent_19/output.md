# agent_19 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow contract for cancelling an active RLCR loop. It is a thin operator-facing wrapper around `scripts/cancel-rlcr-loop.sh`, with explicit handling for normal loop cancellation, finalize-phase confirmation, and no-loop cases.
- algorithmic_behavior: The command first runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` and branches entirely on the first output line: `NO_LOOP` / `NO_ACTIVE_LOOP`, `CANCELLED`, `CANCELLED_FINALIZE`, or `FINALIZE_NEEDS_CONFIRM` at `commands/cancel-rlcr-loop.md:17`. If finalize confirmation is required, it asks the user whether to force cancellation or let finalization continue at `commands/cancel-rlcr-loop.md:23`.
- inputs_outputs_state: Inputs are the plugin root, the script’s first-line status token, and possibly the user’s confirmation answer. Output is a user-facing status report. The actual filesystem state transition is implemented by `scripts/cancel-rlcr-loop.sh`: it finds `$PROJECT_ROOT/.humanize/rlcr`, chooses the newest loop directory via `ls | sort -r | head -1`, detects `state.md` or `finalize-state.md`, writes `.cancel-requested`, then renames the active state file to `cancel-state.md` at `scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:85`, and `scripts/cancel-rlcr-loop.sh:134`.
- gates_or_invariants: The command’s allowed tools are restricted to the cancel script, its `--force` form, and `AskUserQuestion` at `commands/cancel-rlcr-loop.md:3`. Finalize-phase cancellation is gated by confirmation unless `--force` is used, matching `scripts/cancel-rlcr-loop.sh:117`. The command explicitly states the script owns all cancellation logic and that only `state.md` or `finalize-state.md` in the newest loop directory makes a loop active at `commands/cancel-rlcr-loop.md:36`.
- dependencies_and_callers: Depends directly on `scripts/cancel-rlcr-loop.sh`. That script’s `.cancel-requested` signal coordinates with Bash validation: `hooks/loop-bash-validator.sh` allows otherwise-blocked state-file `mv` operations only when `is_cancel_authorized` passes at `hooks/loop-bash-validator.sh:139` and `hooks/loop-bash-validator.sh:149`. The authorization routine checks the signal file and validates the exact `mv state/finalize-state -> cancel-state` shape in `hooks/lib/loop-common.sh:539`.
- edge_cases_or_failure_modes: No loop directory yields `NO_LOOP`; a newest loop directory without an active state file yields `NO_ACTIVE_LOOP`; finalize phase without force yields `FINALIZE_NEEDS_CONFIRM`. If both `state.md` and `finalize-state.md` exist, the script checks `state.md` first at `scripts/cancel-rlcr-loop.sh:89`. Older active directories are ignored because newest-directory selection is the only search path.
- validation_or_tests: Active-loop discovery behavior relevant to cancellation is specified in `tests/robustness/test-session-robustness.sh`: finalize-state detection at lines 48-61, completed sessions ignored at lines 259-271, cancelled sessions ignored at lines 273-285, and older active sessions ignored when the newest is finished at lines 287-302. I did not run tests; this task requested read-only research notes.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the `command_modifies_file` detection algorithm used by RLCR Bash validators to block file mutation through shell commands.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, lowercases each sample command with `to_lower`, and asserts whether `command_modifies_file "$command_lower" "$pattern"` should match at `tests/test-bash-validator-patterns.sh:11`, `tests/test-bash-validator-patterns.sh:37`, and `tests/test-bash-validator-patterns.sh:51`. It covers write redirection, `tee`, in-place editors, file operations, `dd`, `truncate`, file descriptor redirection, safe read-only commands, and alternate filename patterns.
- inputs_outputs_state: Inputs are literal Bash command strings and regex file patterns, defaulting to `goal-tracker\.md` at `tests/test-bash-validator-patterns.sh:39`. Outputs are PASS/FAIL lines and process status. State is limited to `TESTS_PASSED` and `TESTS_FAILED`, with exit success only when no failures occurred at `tests/test-bash-validator-patterns.sh:206`.
- gates_or_invariants: Commands that write to matching files must be detected, while read-only commands such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` must not match at `tests/test-bash-validator-patterns.sh:142`. The test also establishes case-insensitive handling by lowercasing commands before matching and includes uppercase input at `tests/test-bash-validator-patterns.sh:80`.
- dependencies_and_callers: Directly depends on `to_lower` and `command_modifies_file` in `hooks/lib/loop-common.sh`. `to_lower` is a `tr` wrapper at `hooks/lib/loop-common.sh:400`; `command_modifies_file` iterates regexes for redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`/`cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec N>` at `hooks/lib/loop-common.sh:1074`. The production Bash validator uses the same helper for plan backup protection, goal tracker protection, prompt-file protection, and summary-file protection at `hooks/loop-bash-validator.sh:336`, `hooks/loop-bash-validator.sh:349`, `hooks/loop-bash-validator.sh:365`, and `hooks/loop-bash-validator.sh:375`.
- edge_cases_or_failure_modes: The test records a known limitation: multi-source `cp file1 file2 goal-tracker.md` is not detected because the regex expects a two-operand `cp src dest` shape at `tests/test-bash-validator-patterns.sh:165`. Variable-target writes such as `echo x > $FILE` intentionally do not match at `tests/test-bash-validator-patterns.sh:170`. The implementation is regex-based, so quoted paths with spaces, complex shell expansions, and some chained command forms are outside this file’s explicit coverage.
- validation_or_tests: This file is itself the validation. It ends with a hard failure gate when any assertion fails at `tests/test-bash-validator-patterns.sh:206`. I did not execute it because the run instruction was research-only.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Blocking-message template for the `.humanize` version-control protection gate. It explains why certain `git add` commands are denied and gives allowed alternatives.
- algorithmic_behavior: The template renders a deterministic Markdown block: `.humanize/` contains local loop state, broad or forced adds are blocked, and users should stage explicit paths, directories, or patch hunks instead. The key instruction appears at `prompt-template/block/git-add-humanize.md:3`; allowed commands are listed at lines 8-14; blocked command classes are listed at lines 16-24.
- inputs_outputs_state: No dynamic placeholders are used. Input is the fact that the Bash validator has classified a `git add` command as unsafe. Output is the rendered block, emitted through `git_add_humanize_blocked_message`. It does not mutate state; its algorithmic purpose is to preserve the invariant that `.humanize` loop state remains local and uncommitted.
- gates_or_invariants: The invariant is reinforced both by `.gitignore:5`, which contains `.humanize*`, and by the hook-side guard. The template specifically calls out direct `.humanize` staging, broad staging (`git add -A`, `git add --all`, `git add .`), and forced staging (`git add -f .`) at `prompt-template/block/git-add-humanize.md:20`. The `.gitignore` repair path requires staging only `.gitignore` and using a commit message without the literal protected string at `prompt-template/block/git-add-humanize.md:26`.
- dependencies_and_callers: Loaded by `git_add_humanize_blocked_message()` through `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` at `hooks/lib/loop-common.sh:1034`. The Bash validator invokes that message and exits with a blocking status when `command_would_add_humanize` matches at `hooks/loop-bash-validator.sh:110`. The underlying detector handles direct references, forced adds, broad adds when `.humanize` exists, and broad non-force adds when `.humanize` is not ignored, as described around `hooks/lib/loop-common.sh:914`.
- edge_cases_or_failure_modes: The template is intentionally conservative, but its own text says `git add .` “may include .humanize if not gitignored”; implementation narrows that case by checking whether `.humanize` exists and whether `git check-ignore -q .humanize` protects it. The warning about commit messages matters because the protection can key off the literal protected string; the suggested message avoids that literal.
- validation_or_tests: The direct template reference is covered by template-loader paths rather than this file containing test logic. Related test inventory found `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`; I did not run them. The primary behavioral validation lives in the Bash validator and `command_would_add_humanize` path.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-109 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Prompt contract that lets Claude request Goal Tracker changes indirectly after direct modification is restricted. It defines the required summary section that Codex can review and apply.
- algorithmic_behavior: The template instructs Claude to include a `Goal Tracker Update Request` section only when updates are needed, with `Requested Changes` bullets and a `Justification` paragraph at `prompt-template/claude/goal-tracker-update-request.md:2`. Example request categories include marking a task complete with evidence, adding open issues, recording plan evolution, and deferring work with acceptance-criteria impact at lines 6-10.
- inputs_outputs_state: Inputs are Claude’s observed need for tracker updates and its evidence/justification. Output is structured Markdown in Claude’s round summary. There is no direct file mutation from this template; the state transition is deferred to Codex review, as stated at `prompt-template/claude/goal-tracker-update-request.md:16`.
- gates_or_invariants: The gate is “if needed” plus justification. This supports the RLCR invariant that after early setup, Claude should not directly mutate `goal-tracker.md`; it should request changes for Codex to adjudicate. The Bash validator enforces that direct Bash writes to `goal-tracker.md` are blocked and points to summary-based requests after round zero at `hooks/loop-bash-validator.sh:349`.
- dependencies_and_callers: Appended to next-round prompts by `hooks/loop-codex-stop-hook.sh`: it loads `claude/goal-tracker-update-request.md`, falls back to a one-line generic instruction if missing, then appends the result to `NEXT_PROMPT_FILE` at `hooks/loop-codex-stop-hook.sh:1598`. The post-round block message path also references Goal Tracker Update Requests through `goal_tracker_blocked_message` at `hooks/lib/loop-common.sh:1104`.
- edge_cases_or_failure_modes: Missing template degrades to a minimal fallback line rather than failing prompt generation at `hooks/loop-codex-stop-hook.sh:1600`. Poorly justified or overbroad requests may be ignored by Codex because the template explicitly says Codex reviews the request “if justified.” Requests that defer work should include acceptance-criteria impact to avoid hiding incomplete goal state.
- validation_or_tests: Related template-loading coverage exists in the broader template test suite, and `hooks/loop-codex-stop-hook.sh` has a fallback path for missing template content. I did not execute tests. The file is a prompt contract, not an executable validator.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-139 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR session discovery, centered on `find_active_loop`.
- algorithmic_behavior: The test creates temporary `.humanize/rlcr`-style session directories, places marker files, calls `find_active_loop "$TEST_DIR/rlcr"`, and compares the result against expected active-loop behavior. It covers positive cases at `tests/robustness/test-session-robustness.sh:33` and negative/edge cases starting at line 120.
- inputs_outputs_state: Inputs are synthetic session directory trees under `TEST_DIR`. Outputs are PASS/FAIL lines and a final summary via `print_test_summary` at `tests/robustness/test-session-robustness.sh:322`. State is temporary only: `setup_test_dir` creates a temp directory and installs cleanup at `tests/test-helpers.sh:86`.
- gates_or_invariants: The main invariant is newest-only session selection: older directories are ignored even if they contain `state.md`. Test 4 asserts empty output when the newest directory lacks state despite an older active state at `tests/robustness/test-session-robustness.sh:89`; test 17 asserts the same when the newest session is finished at lines 287-302. Active markers are only `state.md` and `finalize-state.md`; `complete-state.md` and `cancel-state.md` are ignored at lines 259-285. The implementation confirms this at `hooks/lib/loop-common.sh:152`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-session-robustness.sh:16`. `find_active_loop` checks whether the base directory exists, selects `ls -1d "$loop_base_dir"/*/ | sort -r | head -1`, and returns the newest directory only if it contains `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:157`.
- edge_cases_or_failure_modes: The test explicitly covers empty base dirs, nonexistent dirs, files without subdirectories, session dirs without state files, nonstandard names, symlinked session dirs, spaces in names, deep nested paths, rapid creation, completed/cancelled sessions, and a nominal Unicode case. Test 13 establishes that `find_active_loop` only checks immediate children, not nested session trees, at `tests/robustness/test-session-robustness.sh:224`. The header mentions permission changes, but this file does not appear to implement a permission-change case. Test 18’s label says Unicode, but the created directory is `session-test`, so it is more of a graceful-handling smoke test than actual Unicode coverage.
- validation_or_tests: This file is the validation for session discovery. It also imposes a performance gate: 15 sessions must resolve in under 1000 ms at `tests/robustness/test-session-robustness.sh:63`. I did not run it because the requested deliverable is research evidence only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 Item Evidence headings present; exact item identifiers are intentionally not repeated here to preserve the exactly-once identifier constraint.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`