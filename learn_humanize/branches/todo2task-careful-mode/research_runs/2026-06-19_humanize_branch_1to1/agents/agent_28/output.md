# agent_28 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit safety gate for active RLCR loops. It prevents prompt submission when the loop state schema is stale/malformed, the user has switched branches, or the configured plan-file tracking mode has drifted. Registered in `hooks/hooks.json:4-10`.
- algorithmic_behavior: Reads hook stdin only to satisfy hook protocol (`hooks/loop-plan-file-validator.sh:26-27`), locates the active loop under `.humanize/rlcr` with `find_active_loop` (`lines 29-35`), chooses `finalize-state.md` over `state.md` during Finalize Phase (`lines 38-42`), strictly parses state (`lines 44-48`), then performs three ordered gates: required schema fields (`lines 59-89`), branch identity (`lines 95-115`), and plan-file tracking/cleanliness (`lines 121-229`).
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or current directory as project root (`line 14`), active loop state file (`lines 30-42`), state fields `plan_tracked`, `plan_file`, `start_branch` (`lines 50-53`), and git status. Output is either silence plus exit `0` for allow, or JSON `{ "decision": "block", "reason": ... }` plus exit `0` for user-fixable gates. Malformed strict state is different: it writes a plain stderr error and exits `1` (`lines 44-48`). It does not mutate state.
- gates_or_invariants: `plan_tracked` and `start_branch` must exist (`lines 79-89`), current branch must match loop start branch (`lines 95-115`), `plan_tracked=true` requires the plan file to be git-tracked and clean (`lines 123-192`), and `plan_tracked=false` requires the plan file to remain untracked/gitignored (`lines 193-228`). Git timeouts or unexpected git errors fail closed (`lines 129-147`, `154-171`, `199-216`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, field constants, and strict state parsing (`hooks/loop-plan-file-validator.sh:16-17`; `hooks/lib/loop-common.sh:15-20`, `157-175`, `259-336`), `hooks/lib/template-loader.sh` for fallback-safe rendering (`hooks/loop-plan-file-validator.sh:65`; `hooks/lib/template-loader.sh:167-203`), `scripts/portable-timeout.sh` for portable 30-second git command bounds (`hooks/loop-plan-file-validator.sh:19-24`; `scripts/portable-timeout.sh:31-71`), `jq` for JSON escaping (`hooks/loop-plan-file-validator.sh:67-75`), and `git`.
- edge_cases_or_failure_modes: No active loop exits silently (`lines 33-36`). `finalize-state.md` changes the state source (`lines 38-42`). Missing newer strict fields like `review_started` or `base_branch` are caught by strict parser before this script’s own schema fallback (`hooks/lib/loop-common.sh:294-310`), so some old-schema states get the generic malformed-state block rather than the templated missing-field message. `LS_FILES_EXIT` and `STATUS_EXIT` are not reset before each git command, so a prior nonzero value can theoretically leak within the same shell invocation if later command substitution succeeds without reassigning; the current command sequence mostly avoids this by separate branches, but it is a fragile pattern.
- validation_or_tests: Covered by `tests/test-plan-file-hooks.sh`: valid state passes (`lines 119-132`), quoted `plan_file` passes (`134-147`), malformed/missing fields block (`149-189`), branch switching blocks (`194-217`), quoted `start_branch` is stripped (`373-397`), quoted branch mismatch blocks (`399-422`), and hyphenated plan-file paths pass (`469-500`). Robustness tests also call this hook in `tests/robustness/test-hook-system-robustness.sh` per search results.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for monitor shutdown and terminal cleanup invariants around RLCR loop monitoring. It does not implement the RLCR state machine, but it verifies behavior that prevents a cancelled/deleted loop from leaving terminal state or monitor processes inconsistent.
- algorithmic_behavior: Creates an isolated `/tmp/test-monitor-runtime-$$` workspace (`tests/test-monitor-runtime.sh:45-53`), generates small runtime scripts, executes them, and accumulates pass/fail counters (`lines 22-34`, `493-512`). The test suite validates graceful stop output (`lines 61-135`), cleanup idempotency (`143-177`), loop-directory deletion detection (`186-251`), terminal restore source wiring (`263-311`), `_graceful_stop -> _cleanup` call chain (`313-324`), bash SIGINT cleanup (`333-381`), source-level signal handler declarations (`383-423`), and optional zsh SIGINT handling (`426-490`).
- inputs_outputs_state: Inputs are the repo’s `scripts/humanize.sh` source and shell availability, especially `zsh` for test 9. It writes temporary scripts and temporary `.humanize/rlcr/.../state.md` under `/tmp`, then deletes the temp base on exit (`lines 45-53`). Output is human-readable PASS/FAIL lines and process exit `0` only when `TESTS_FAILED == 0` (`lines 493-512`).
- gates_or_invariants: `_cleanup` must be idempotent via `cleanup_done` (`lines 143-177`), `_graceful_stop` must call cleanup before printing the cancellation/deletion explanation (`lines 94-105`, `313-324`), monitor loop must stop when `.humanize/rlcr` disappears (`lines 205-229`), `_restore_terminal` must reset scroll region (`lines 274-303`), `_cleanup` must call `_restore_terminal` (`lines 305-311`), bash must trap INT/TERM (`lines 347-381`, `390-395`), zsh must define `TRAPINT`/`TRAPTERM` (`lines 397-409`), and cleanup must reset traps (`lines 411-423`).
- dependencies_and_callers: Reads and partially sources `scripts/humanize.sh`; the asserted implementation is around `_restore_terminal`, `_cleanup`, `_graceful_stop`, and trap setup at `scripts/humanize.sh:539-605`, with loop deletion checks at `scripts/humanize.sh:637-643` and `679-682`. Uses standard shell tools: `grep`, `chmod`, `kill`, `sleep`, optional `zsh`.
- edge_cases_or_failure_modes: Several generated scripts duplicate simplified versions of the functions rather than invoking the exact production functions, so they verify contract shape and signal semantics more than full integrated behavior. Test 4 contains a dead/ineffective pipeline check at `tests/test-monitor-runtime.sh:282` because `grep -q ... | grep -q ...` cannot inspect the first grep’s matched text; the later `grep -A30` check is the effective assertion (`lines 305-311`). The zsh test is skipped if zsh is unavailable (`lines 432-490`).
- validation_or_tests: This file is itself the validation asset. It complements deeper monitor E2E coverage in `tests/test-monitor-e2e-real.sh`, which exercises real monitor SIGINT behavior according to search results. Production source confirms the expected behavior: `_restore_terminal` resets scroll region (`scripts/humanize.sh:539-545`), `_cleanup` resets traps, kills tail process, restores terminal, and prints stop (`554-578`), `_graceful_stop` calls `_cleanup` then prints the specific reason (`580-593`), and bash/zsh signal handlers are installed (`595-605`).
- skip_candidate: `yes: support-runtime monitor specification, not direct TODO-to-Task transition or plan-file validation algorithm`

### TODO2TASK_CAREFUL_MODE-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template that enforces the “no review/stop while work items remain incomplete” gate for both legacy TodoWrite items and newer Task-system items.
- algorithmic_behavior: Renders an incomplete item list into a blocking instruction page via `{{INCOMPLETE_LIST}}` (`prompt-template/block/incomplete-todos.md:3-12`). It explicitly routes `(Task #...)` entries to `TaskUpdate` and non-task entries to `TodoWrite` (`lines 9-11`), then forbids proceeding to Codex review until all tasks are finished (`line 14`).
- inputs_outputs_state: Input is the rendered `INCOMPLETE_LIST` generated by `hooks/check-todos-from-transcript.py` and passed by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:373-384`). Output is markdown text inserted into a JSON block response as `reason` (`hooks/loop-codex-stop-hook.sh:386-393`). It does not mutate state directly; it directs the agent to mutate task/todo state through the appropriate tool.
- gates_or_invariants: This template is reached only when the checker exits `1` for incomplete work (`hooks/loop-codex-stop-hook.sh:373-395`). Parse errors exit through a different block path (`lines 354-370`). The invariant is strict: all incomplete Task/Todo items must be completed before review and stop are allowed (`prompt-template/block/incomplete-todos.md:7-14`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` (`lines 378-384`). Depends on `hooks/check-todos-from-transcript.py`, which reads task files from `~/.claude/tasks/<session_id>/` and treats statuses other than `completed` or `deleted` as incomplete (`hooks/check-todos-from-transcript.py:104-147`), and parses the latest legacy `TodoWrite` transcript list for non-`completed` statuses (`58-101`). Template loading uses single-pass placeholder substitution in `hooks/lib/template-loader.sh:50-132`.
- edge_cases_or_failure_modes: If the template is missing or empty, caller falls back to a shorter inline “Incomplete Tasks” message (`hooks/loop-codex-stop-hook.sh:378-384`; `hooks/lib/template-loader.sh:167-203`). The checker exits `0` on empty stdin or missing transcript/tasks directory, so the template is not shown when no authoritative task data is available (`hooks/check-todos-from-transcript.py:150-179`). Malformed/unreadable individual task files are skipped (`hooks/check-todos-from-transcript.py:143-145`), which can hide an incomplete task if its JSON file is corrupt.
- validation_or_tests: The caller path is exercised by todo-checker tests in `tests/test-todo-checker.sh` per search results, including legacy TodoWrite, latest TodoWrite wins, Task files, and mixed TodoWrite/Task cases. `tests/test-finalize-phase.sh` includes stop-hook transcript cases with incomplete and completed TodoWrite entries according to search results.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-118 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review-prompt section that makes Codex responsible for applying justified “Goal Tracker Update Request” changes after Round 0, while preserving immutable goal/acceptance criteria. It is a prompt-level transition contract between Claude summaries, Codex review, and `goal-tracker.md` state.
- algorithmic_behavior: Instructs Codex to inspect Claude’s summary for a “Goal Tracker Update Request,” evaluate whether it serves the Ultimate Goal, then either update `@{{GOAL_TRACKER_FILE}}` or reject with rationale (`prompt-template/codex/goal-tracker-update-section.md:1-12`). Approved updates can move tasks between Active/Completed/Deferred, append Plan Evolution Log entries, and add Open Issues (`lines 6-10`). Common request classes are task completion, new issues, plan changes, and deferrals with strong justification (`lines 13-17`).
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE` supplied by the stop hook (`hooks/loop-codex-stop-hook.sh:769-773`) plus Claude’s summary embedded into the review prompt (`lines 763-768`, `795-845`). Output is prompt text inserted into regular and full-alignment review templates via `{{GOAL_TRACKER_UPDATE_SECTION}}` (`prompt-template/codex/regular-review.md:47`; `prompt-template/codex/full-alignment-review.md:57`). The intended state output is direct Codex edits to `goal-tracker.md` when approved.
- gates_or_invariants: Hard invariant: never modify the immutable section containing Ultimate Goal and Acceptance Criteria (`prompt-template/codex/goal-tracker-update-section.md:10`). Deferrals require strong justification (`line 17`). The stop hook separately blocks Round 0 if goal tracker placeholders remain uninitialized before prompt generation (`hooks/loop-codex-stop-hook.sh:690-720`).
- dependencies_and_callers: Called once while building the Codex review prompt (`hooks/loop-codex-stop-hook.sh:769-773`), then embedded in either full alignment prompt (`lines 818-831`) or regular review prompt (`833-845`). The surrounding review templates require Codex to read the original plan and goal tracker, reject unjustified deferrals, and output `COMPLETE` only when all original tasks/ACs are fully met with no deferrals or pending work (`prompt-template/codex/regular-review.md:20-57`; `prompt-template/codex/full-alignment-review.md:19-92`).
- edge_cases_or_failure_modes: If this template is unavailable, caller falls back to a much weaker two-line instruction that only says to apply requested changes (`hooks/loop-codex-stop-hook.sh:769-773`), losing the explicit immutable-section and rejection rules. The section relies on Codex judgment; there is no direct shell validator here that prevents edits to the immutable section after prompt generation.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` renders this real template and asserts the title plus substituted goal-tracker path appear. Broader prompt-template reference tests check safe template rendering and use of `load_and_render_safe` according to search results.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TODO2TASK_CAREFUL_MODE-HZ-028`, `TODO2TASK_CAREFUL_MODE-HZ-058`, `TODO2TASK_CAREFUL_MODE-HZ-088`, `TODO2TASK_CAREFUL_MODE-HZ-118`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`