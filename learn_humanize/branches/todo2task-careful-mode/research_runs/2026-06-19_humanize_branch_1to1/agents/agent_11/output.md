# agent_11 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt template set for the RLCR loop. It defines the instructions sent back to Claude when the stop hook advances from implementation review to another round, code-review remediation, skipped-review finalization, normal finalization, open-question handling, push reminders, and goal-tracker update requests.
- algorithmic_behavior: The directory contains nine templates: `next-round-prompt.md`, `next-round-footer.md`, `review-phase-prompt.md`, `finalize-phase-prompt.md`, `finalize-phase-skipped-prompt.md`, `open-question-notice.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, and `goal-tracker-update-request.md`. These are loaded through `load_template` / `load_and_render_safe`, whose template root resolves to `prompt-template` and requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:24-31`, `hooks/lib/template-loader.sh:205-221`).
- inputs_outputs_state: Inputs are rendered variables such as `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_CONTENT`, `SUMMARY_FILE`, `NEXT_SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, and `REVIEW_SKIP_REASON`. Outputs are generated prompt files under `.humanize/rlcr/<timestamp>/round-N-prompt.md` or a hook block response body (`hooks/loop-codex-stop-hook.sh:1140-1161`, `hooks/loop-codex-stop-hook.sh:1521-1603`, `hooks/loop-codex-stop-hook.sh:1080-1113`).
- gates_or_invariants: `review-phase-prompt.md` says review phase ignores the `COMPLETE` signal and must address severity markers before proceeding (`prompt-template/claude/review-phase-prompt.md:23-28`). `next-round-footer.md` forbids lying, direct loop-state edits, or running cancellation as an exit path, then requires commit and summary (`prompt-template/claude/next-round-footer.md:4-9`). Finalize templates require functionality-equivalent changes only and passing tests (`prompt-template/claude/finalize-phase-prompt.md:16-24`, `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`).
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`, which renders `review-phase-prompt.md` for code-review issues (`hooks/loop-codex-stop-hook.sh:1159-1161`), `next-round-prompt.md` for implementation feedback (`hooks/loop-codex-stop-hook.sh:1534-1537`), inserts `open-question-notice.md` when configured and detected (`hooks/loop-codex-stop-hook.sh:1539-1572`), appends `post-alignment-action-items.md`, footer, push note, and goal-tracker request (`hooks/loop-codex-stop-hook.sh:1575-1603`), and renders finalize prompts (`hooks/loop-codex-stop-hook.sh:1080-1113`).
- edge_cases_or_failure_modes: Missing templates fall back to inline strings via `load_and_render_safe`, so prompt generation is fail-soft (`hooks/lib/template-loader.sh:167-203`). Open-question insertion relies on a short line containing `Open Question`, so verbose headings may not trigger the notice (`hooks/loop-codex-stop-hook.sh:1539-1548`). Placeholder substitution is single-pass to prevent review content containing template syntax from mutating later variables (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-129`).
- validation_or_tests: Template existence and rendering are covered by `tests/test-template-references.sh` and comprehensive template loader tests discovered in `tests/run-all-tests.sh:32-73`; direct render behavior is exercised through `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` references in the suite list (`tests/run-all-tests.sh:34-40`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-041 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for `/humanize:start-rlcr-loop`. It creates the RLCR loop state directory, validates the implementation plan and repo preconditions, captures branch/review metadata, seeds the goal tracker, and emits the first Claude prompt.
- algorithmic_behavior: Parses options for plan selection, max iterations, Codex model/effort/timeout, push policy, base branch, full-review cadence, skip-implementation mode, and open-question policy (`scripts/setup-rlcr-loop.sh:123-231`). It blocks concurrent RLCR or PR loops using `find_active_loop` and `find_active_pr_loop` (`scripts/setup-rlcr-loop.sh:237-268`), validates plan path safety and plan content (`scripts/setup-rlcr-loop.sh:320-548`), verifies Codex is installed (`scripts/setup-rlcr-loop.sh:550-556`), determines start/base branches, captures base commit, then writes `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `plan.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:562-734`, `scripts/setup-rlcr-loop.sh:741-978`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR`, current git repo, optional plan file, local branch refs, and Codex availability. Outputs are a timestamped `.humanize/rlcr` session directory, backed-up plan or skip-impl placeholder, YAML frontmatter state with `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, `review_started`, and `ask_codex_question` (`scripts/setup-rlcr-loop.sh:716-734`).
- gates_or_invariants: Requires git repo and at least one commit (`scripts/setup-rlcr-loop.sh:304-314`). Rejects absolute plan paths, spaces, shell metacharacters, symlink file or symlink parents, missing/unreadable files, paths escaping the project, and submodule-contained plans (`scripts/setup-rlcr-loop.sh:327-428`). Enforces plan tracking mode: tracked and clean when `--track-plan-file`, otherwise gitignored/untracked (`scripts/setup-rlcr-loop.sh:434-479`). Validates branch/model/effort strings for YAML safety (`scripts/setup-rlcr-loop.sh:568-594`, `scripts/setup-rlcr-loop.sh:652-658`).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md`, whose allowed tool is this script and whose command passes user arguments (`commands/start-rlcr-loop.md:3-13`). Depends on `scripts/portable-timeout.sh` (`scripts/setup-rlcr-loop.sh:27-29`) and `hooks/lib/loop-common.sh` for active loop detection (`scripts/setup-rlcr-loop.sh:239-241`).
- edge_cases_or_failure_modes: `--skip-impl` allows no plan and creates an internal placeholder (`scripts/setup-rlcr-loop.sh:279-289`, `scripts/setup-rlcr-loop.sh:682-704`). Remote-only base branch is rejected because Codex review needs a local ref (`scripts/setup-rlcr-loop.sh:601-618`). If remote default is unavailable, local `main`, then `master` are tried (`scripts/setup-rlcr-loop.sh:620-649`). A SIGPIPE trap is installed only after setup is done so truncated terminal output does not convert a successful setup into failure (`scripts/setup-rlcr-loop.sh:980-986`).
- validation_or_tests: Setup behavior is covered by `tests/robustness/test-setup-scripts-robustness.sh` in the all-tests suite (`tests/run-all-tests.sh:67-68`). Related plan-file runtime gates are tested by `tests/test-plan-file-validation.sh` and `tests/test-plan-file-hooks.sh` in the same suite (`tests/run-all-tests.sh:37-41`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook gate that prevents loop exit while Claude TodoWrite items or file-based Task records remain incomplete.
- algorithmic_behavior: The test feeds JSON hook input into the checker and asserts exit codes: parse errors return 2, absent input/transcript/session returns 0, incomplete tasks return 1, and completed-only state returns 0 (`tests/test-todo-checker.sh:52-98`, `tests/test-todo-checker.sh:107-158`, `tests/test-todo-checker.sh:309-455`). It verifies legacy transcript parsing and the new authoritative task-directory path.
- inputs_outputs_state: Inputs are stdin JSON containing `transcript_path`, `session_id`, and test-only `tasks_base_dir`. The test creates JSONL transcripts and task JSON files under a temp directory (`tests/test-todo-checker.sh:37-39`, `tests/test-todo-checker.sh:109-151`, `tests/test-todo-checker.sh:305-428`). Outputs are checker exit codes and stdout containing `INCOMPLETE_TODOS`, item status, content, and Task ID marker when appropriate.
- gates_or_invariants: Latest TodoWrite wins for transcript state (`tests/test-todo-checker.sh:197-210`). `pending`, `in_progress`, missing status, and empty-content todos are incomplete (`tests/test-todo-checker.sh:122-158`, `tests/test-todo-checker.sh:250-279`). Task JSON defaults missing status to pending; `deleted` is ignored; any non-completed/non-deleted task blocks (`tests/test-todo-checker.sh:309-414`). Mixed legacy and Task sources must still detect incomplete file-based tasks (`tests/test-todo-checker.sh:417-443`).
- dependencies_and_callers: Direct dependency is `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:12-15`). The stop hook calls this checker before summary/review processing and blocks on incomplete results (`hooks/loop-codex-stop-hook.sh:341-384`). The checker itself reads Task JSON from `~/.claude/tasks/<session_id>` unless overridden and parses transcript TodoWrite calls as a fallback/legacy source (`hooks/check-todos-from-transcript.py:104-147`, `hooks/check-todos-from-transcript.py:150-196`).
- edge_cases_or_failure_modes: Invalid hook JSON is a hard parse error (`tests/test-todo-checker.sh:52-62`). Invalid JSONL transcript lines are ignored if a valid TodoWrite exists (`tests/test-todo-checker.sh:181-195`). Empty input, no transcript, missing transcript file, empty transcript, and nonexistent session directory allow proceeding (`tests/test-todo-checker.sh:64-98`, `tests/test-todo-checker.sh:167-178`, `tests/test-todo-checker.sh:445-455`). Malformed task files are skipped by checker implementation (`hooks/check-todos-from-transcript.py:143-145`).
- validation_or_tests: This file is itself the validation harness and is listed in the global suite (`tests/run-all-tests.sh:32-37`). It declares pass/fail counters and exits nonzero if any assertion fails (`tests/test-todo-checker.sh:21-35`, `tests/test-todo-checker.sh:457-475`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-101 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the required work-summary gate. It tells Claude that an exit attempt is rejected until the expected summary file exists.
- algorithmic_behavior: The template renders `{{SUMMARY_FILE}}` and lists required summary contents: implemented work, files created/modified, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:1-16`). It is used as the stop-hook block reason when the summary file is absent.
- inputs_outputs_state: Input is the expected summary path. In normal rounds the stop hook expects `round-N-summary.md`; in finalize phase it expects `finalize-summary.md` (`hooks/loop-codex-stop-hook.sh:611-616`). Output is JSON hook response with decision `block`, rendered reason, and phase-specific system message (`hooks/loop-codex-stop-hook.sh:618-642`).
- gates_or_invariants: Summary existence is checked after task-completion and repository cleanliness gates, but before goal-tracker initialization review and Codex review. Without this file, the loop cannot advance because Codex has no work summary to review (`hooks/loop-codex-stop-hook.sh:607-643`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` with `load_and_render_safe`; fallback text exists if the template is missing (`hooks/loop-codex-stop-hook.sh:622-626`). Rendering semantics come from `hooks/lib/template-loader.sh`, which keeps unresolved placeholders as-is and supports fallback rendering (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, the inline fallback still blocks with the correct summary path (`hooks/loop-codex-stop-hook.sh:622-626`, `hooks/lib/template-loader.sh:179-199`). Finalize phase changes only the expected filename and system message, not the gate semantics (`hooks/loop-codex-stop-hook.sh:611-631`).
- validation_or_tests: Template reference presence is covered by the template-reference suite, and the template is part of the comprehensive template load/render checks (`tests/run-all-tests.sh:38-40`). Stop-hook summary-path behavior is also indirectly covered by finalize and hook-system suites in `tests/run-all-tests.sh:45-46`, `tests/run-all-tests.sh:69-71`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-131 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for concurrent and malformed access to RLCR/PR loop state files. It defines expected behavior for tolerant parsing, strict parsing, active-loop discovery, atomic state updates, and stale/cancelled loop exclusion.
- algorithmic_behavior: Sources test helpers and `hooks/lib/loop-common.sh`, creates synthetic state files, then exercises `get_current_round`, `find_active_loop`, `find_active_pr_loop`, and `parse_state_file_strict` under edge and concurrent conditions (`tests/robustness/test-concurrent-state-robustness.sh:13-19`, `tests/robustness/test-concurrent-state-robustness.sh:30-50`, `tests/robustness/test-concurrent-state-robustness.sh:71-73`).
- inputs_outputs_state: Inputs are temp `.humanize`-style directories and YAML frontmatter files. Outputs are pass/fail counters through `tests/test-helpers.sh` and final test summary (`tests/test-helpers.sh:30-78`, `tests/robustness/test-concurrent-state-robustness.sh:555-560`).
- gates_or_invariants: Tolerant round parsing should return the numeric round or degrade to 0 for leading whitespace, CRLF, empty values, permission issues, or parse failures (`tests/robustness/test-concurrent-state-robustness.sh:59-160`, `tests/robustness/test-concurrent-state-robustness.sh:530-553`). Active loop detection only considers the newest timestamped directory and only `state.md` or `finalize-state.md`; older directories and `cancel-state.md` are not active (`tests/robustness/test-concurrent-state-robustness.sh:170-230`, `tests/robustness/test-concurrent-state-robustness.sh:453-485`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, whose `find_active_loop` intentionally checks only the newest directory and detects normal/finalize state (`hooks/lib/loop-common.sh:152-175`), whose `get_current_round` extracts YAML frontmatter by `---` separators (`hooks/lib/loop-common.sh:177-190`), and whose strict parser validates required fields and numeric values (`hooks/lib/loop-common.sh:250-335`).
- edge_cases_or_failure_modes: Concurrent read test requires all ten parallel reads to return the actual nonzero round, not a default (`tests/robustness/test-concurrent-state-robustness.sh:251-278`). Simulated writer uses temp file plus `mv`, and all 20 reads must remain numeric (`tests/robustness/test-concurrent-state-robustness.sh:280-323`). Strict parser must reject malformed YAML-like values and missing frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:333-371`). Unicode content and large/zero round values must parse (`tests/robustness/test-concurrent-state-robustness.sh:374-388`, `tests/robustness/test-concurrent-state-robustness.sh:495-528`).
- validation_or_tests: This file is in the global test suite (`tests/run-all-tests.sh:55-69`). It is a read/write temp-dir test and was inspected but not run in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TODO2TASK_CAREFUL_MODE-HZ-011`, `TODO2TASK_CAREFUL_MODE-HZ-041`, `TODO2TASK_CAREFUL_MODE-HZ-071`, `TODO2TASK_CAREFUL_MODE-HZ-101`, `TODO2TASK_CAREFUL_MODE-HZ-131`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`