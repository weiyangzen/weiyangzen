# agent_29 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Read` guard for RLCR loop files. It prevents Claude from reading stale or wrong-location `round-N-summary.md`, `round-N-prompt.md`, and most `round-N-todos.md` files, while generally allowing unrelated reads. It is registered for the `Read` matcher in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/hooks.json:33).
- algorithmic_behavior: Reads hook JSON from stdin, validates structure, rejects deeply nested JSON above depth 30, exits immediately for non-`Read` tools, then requires `tool_input.file_path` ([hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-read-validator.sh:25)). For `round-N-todos.md`, it finds the active RLCR loop and blocks unless the exact file is allowlisted in that active loop ([hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-read-validator.sh:55)). For summary/prompt files, it resolves the active loop, prefers `finalize-state.md` over `state.md`, strictly parses state, extracts the current round, and then enforces location, round, and exact active-loop path checks ([hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-read-validator.sh:80)).
- inputs_outputs_state: Inputs are the Claude hook JSON, `CLAUDE_PROJECT_DIR` or current working directory, optional overrides such as `PROJECT_ROOT`, `LOOP_BASE_DIR`, `LOOP_DIR`, and the active loop state file. Outputs are process status plus stderr block text rendered through templates: exit `0` allows, exit `1` rejects invalid input or malformed state, exit `2` blocks the tool action. The hook does not mutate loop state.
- gates_or_invariants: Valid hook input is mandatory; missing `file_path` fails closed. The active loop is the newest timestamped child under `.humanize/rlcr` that has `state.md` or `finalize-state.md`, as defined by `find_active_loop` ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/lib/loop-common.sh:157)). Strict state parsing requires frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, with numeric and boolean validation ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/lib/loop-common.sh:259)). Summary/prompt reads must come from `.humanize/rlcr`, match the current round unless allowlisted, and match `$ACTIVE_LOOP_DIR/$basename` exactly ([hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-read-validator.sh:122)). The allowlist is intentionally narrow: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`, only at the exact active loop path ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/lib/loop-common.sh:429)).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, which brings in the template loader and helper functions such as `validate_hook_input`, `is_deeply_nested`, `require_tool_input_field`, `to_lower`, `is_round_file_type`, `find_active_loop`, `is_allowlisted_file`, `todos_blocked_message`, `is_in_humanize_loop_dir`, `parse_state_file_strict`, and `extract_round_number`. Uses `jq`, `basename`, `grep`, `sed`, and rendered block templates such as `wrong-file-location.md`, `wrong-round-file.md`, and `wrong-directory-path.md`.
- edge_cases_or_failure_modes: If no active loop exists, summary/prompt reads are allowed, but todos reads are blocked because no active-loop allowlist can be established. During finalize phase, `finalize-state.md` supplies current round. Malformed state blocks for safety. File type detection is case-insensitive because the path is lowercased before matching. The final exact-path comparison is string-based, not `realpath`-normalized, so relative or symlinked aliases to the active loop can be treated as wrong-directory reads.
- validation_or_tests: `tests/test-allowlist-validators.sh` directly verifies the allowlist function and read-hook behavior: allowlisted `round-1-todos.md` and `round-0-summary.md` pass, while `round-3-todos.md` and `round-3-summary.md` block ([tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-allowlist-validators.sh:234)). Robustness tests also reference malformed input, nested JSON, concurrent hooks, active-loop discovery, and strict state parsing.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file protection across the UserPromptSubmit validator, Write/Edit/Bash validators, and Stop hook. It defines the expected behavior for plan immutability, state schema strictness, branch consistency, goal-tracker initialization, and historical path handling.
- algorithmic_behavior: Creates an isolated temp workspace, configures an isolated cache, and installs a mock `codex` executable that returns `COMPLETE` for `codex exec` so stop-hook paths do not call the real model ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:30)). `setup_test_loop` initializes git, records the default branch, creates `.humanize/rlcr/2024-01-01_12-00-00`, writes a gitignored source plan, copies it to loop `plan.md`, and writes v1.5-style state fields including `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:63)).
- inputs_outputs_state: Inputs are the repository hook scripts plus synthetic hook JSON payloads. The script exports `CLAUDE_PROJECT_DIR`, mutates only its temp git repos, and fabricates state, plan, summary, and goal-tracker files. Outputs are colored PASS/FAIL/SKIP lines and a final exit code equal to the failure count ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:1108)).
- gates_or_invariants: The UserPromptSubmit hook must pass valid state and quoted YAML fields, but block malformed or outdated state schemas and branch drift ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:119)). Write, Edit, and Bash validators must block direct changes to loop `plan.md` ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:226)). Bash validation must also catch command-substitution, glob, brace expansion, pipe/`tee`, and backtick bypass attempts against `.humanize/rlcr/.../plan.md` ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:286)). The Stop hook must detect modified, deleted, missing-backup, tracked-uncommitted, outdated-schema, and tracked-committed plan changes ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:509)).
- dependencies_and_callers: Calls `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. It relies on git, bash, `mktemp`, `grep`, and the hook dependencies such as `jq`. The spec corresponds to plan-file and branch checks in [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-plan-file-validator.sh:44), plan backup Bash blocking in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-bash-validator.sh:336), and stop-hook plan diff checks in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:300).
- edge_cases_or_failure_modes: Tests are branch-name portable for `main` versus `master`. They explicitly cover quoted YAML fields and hyphenated `plan_file` paths ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:373)). Goal-tracker placeholder tests are section-specific, proving the Stop hook reports only the uninitialized section or all three sections when all remain placeholders ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:781)). Legacy `.humanize-loop.local` paths are negative tests: Bash, Write, and Edit validators must allow them because they are no longer active loop directories ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:1064)).
- validation_or_tests: This file is itself validation. It checks the RLCR state-machine guard surface from initial prompt submission through implementation-stop review gates, including JSON block responses for schema failures and plan integrity violations.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-089 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR large-file gate. It provides the user-facing remediation contract when changed code or documentation files exceed the line-count limit.
- algorithmic_behavior: Accepts `{{MAX_LINES}}` and `{{LARGE_FILES}}` placeholders, explains the maintenance risk, then prescribes separate remediation paths for code and documentation files ([prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/block/large-files.md:3)). Code files must be split into modules below the limit with behavior unchanged and may be reviewed with the `code-simplifier` plugin ([prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/block/large-files.md:13)). Documentation files must be split into logical sections with cross-references, narrative flow, and navigation updates ([prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/block/large-files.md:19)).
- inputs_outputs_state: Inputs are rendered by `loop-codex-stop-hook.sh`: `MAX_LINES=2000` and a bullet list of changed oversized files. Output is Markdown embedded as the `reason` of a JSON block decision; it does not mutate state by itself.
- gates_or_invariants: The caller scans git-changed files when in a git repo, skips deleted files, handles rename syntax by using the new path, classifies code and documentation by extension, counts lines with `wc -l`, and collects files whose line count is greater than `MAX_LINES` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:435)). If any are found, the Stop hook renders this template and returns a block decision with a system message that large files must be split ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:488)).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` in the Stop hook ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:496)). Depends on the shared template loader for placeholder substitution and fallback rendering.
- edge_cases_or_failure_modes: The gate applies only when `GIT_IS_REPO` is true and only to files present in the hook’s cached git-status set. Unchanged large files, unsupported extensions, files whose line count cannot be read, and nonnumeric `wc` output are skipped by the caller. The template says to commit the split changes before attempting exit again, so it coordinates with later clean-git and stop-review gates rather than replacing them.
- validation_or_tests: Template existence and renderability are covered by the repository’s template validation tests; the runtime behavior is implemented in the Stop hook large-file scan. No assigned test file contains a dedicated large-file scenario.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-119 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex regular-review prompt for non-full-alignment RLCR review rounds. It defines the review contract that decides whether Claude’s claimed implementation is accepted, sent back with findings, or allowed to progress toward review/finalization.
- algorithmic_behavior: Requires Codex to read the original plan first via `@{{PLAN_FILE}}`, then compare the original plan and `@{{PROMPT_FILE}}` against Claude’s work summary ([prompt-template/codex/regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/codex/regular-review.md:3)). Part 1 instructs a skeptical implementation review for plan-versus-implementation gaps and treats deferrals or admitted pending work as incomplete; when such work exists, Codex must include a singular, directive, executable implementation plan ([prompt-template/codex/regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/codex/regular-review.md:20)). Part 2 requires a goal-alignment check against `@{{GOAL_TRACKER_FILE}}` for acceptance-criteria progress, forgotten items, deferrals, and plan evolution, including a summary line of addressed ACs and issue counts ([prompt-template/codex/regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/codex/regular-review.md:33)). Part 4 requires review comments in `@{{REVIEW_RESULT_FILE}}` when reality does not match claims, and permits `COMPLETE` only as the last line when all original tasks and ACs are fully done with no deferrals or pending work ([prompt-template/codex/regular-review.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/prompt-template/codex/regular-review.md:49)).
- inputs_outputs_state: Inputs are template variables from the Stop hook: current round, plan file, prompt file, summary content, goal tracker file, docs path, injected goal-tracker update section, derived round metadata, and review result file. Output is `round-N-review-prompt.md`, which Codex consumes; Codex is expected to write `round-N-review-result.md`.
- gates_or_invariants: Deferrals and pending work are explicitly non-complete states. The Stop hook renders this template on regular review rounds when the full-alignment interval does not apply ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:833)). After Codex runs, the Stop hook requires a review result file or copies stdout into that file, rejects missing or empty output, and treats only the last non-empty line exactly equal to `COMPLETE` or `STOP` as a marker ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:1341)). During implementation phase, `COMPLETE` transitions into review phase by setting `review_started: true`, creating `.review-phase-started`, and running code review; in review phase, a `COMPLETE` marker from this path is ignored because review logic governs finalization ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:1405)).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` with `load_and_render_safe`, alongside the periodic `full-alignment-review.md` template and the shared `codex/goal-tracker-update-section.md` injection ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/hooks/loop-codex-stop-hook.sh:769)). Depends on loop files created earlier in the RLCR process: plan backup/source plan, round prompt, Claude summary, and goal tracker.
- edge_cases_or_failure_modes: The template’s semantic requirements depend on Codex following instructions; the Stop hook enforces file presence and marker syntax but cannot fully prove that every plan item was reviewed correctly. A mention of `COMPLETE` inside prose is insufficient because the hook checks only the final standalone marker. If Codex writes to stdout instead of the requested file, the Stop hook attempts to copy stdout into the review result file before failing.
- validation_or_tests: `tests/test-plan-file-hooks.sh` mocks `codex exec` to return `COMPLETE` and verifies Stop-hook parsing paths around quoted YAML and required loop files ([tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/todo2task-careful-mode/tests/test-plan-file-hooks.sh:38)). The repository also includes comprehensive template tests for renderability; this assigned test file does not deeply validate the review template’s semantic review instructions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique assigned item sections are present above, one section per assigned row.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`