# agent_01 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin. The root coordinates two iterative state machines: RLCR, which loops Claude implementation through Codex review, and PR Loop, which loops local fixes against remote PR bot feedback. README describes RLCR as an iterative feedback loop and PR Loop as a bot-review automation flow (`README.md:7-13`, `README.md:202-210`).
- algorithmic_behavior: The root is organized as command entrypoints, setup scripts, stop hooks, validators, templates, and tests. Slash-command specs in `commands/` restrict tools and dispatch to setup scripts, e.g. `commands/start-pr-loop.md:1-14` and `commands/start-rlcr-loop.md:1-14`. Setup scripts create `.humanize/.../<timestamp>/` state directories and round files; stop hooks advance or terminate the loop; PreToolUse validators guard generated state and round artifacts through `hooks/hooks.json:14-64`. `prompt-template/` supplies rendered block messages and round prompts. `tests/` and `tests/robustness/` encode expected transitions, parser behavior, and security gates.
- inputs_outputs_state: Inputs are slash command arguments, hook JSON payloads, local git state, GitHub PR/API data, Codex CLI output, and user-authored summaries. Outputs are `.humanize/rlcr/<timestamp>/...` or `.humanize/pr-loop/<timestamp>/...` files, JSON hook decisions, generated prompts, feedback files, terminal state renames such as `approve-state.md`, `merged-state.md`, `closed-state.md`, and rendered block messages. The root contains 149 files in top-level roles: `.claude`, `.claude-plugin`, `.github`, `agents`, `commands`, `hooks`, `prompt-template`, `scripts`, and `tests`.
- gates_or_invariants: Only one active RLCR or PR loop is allowed during PR setup (`scripts/setup-pr-loop.sh:220-240`). `hooks/hooks.json` wires Write/Edit/Read/Bash validators plus both Stop hooks (`hooks/hooks.json:14-64`). PR loop state is YAML-frontmatter-based and stores `configured_bots` separately from mutable `active_bots` (`scripts/setup-pr-loop.sh:615-634`). Generated PR-loop files are read-only except the current `round-N-pr-resolve.md` (`hooks/loop-write-validator.sh:71-93`). RLCR plan backups are protected (`hooks/loop-write-validator.sh:175-185`).
- dependencies_and_callers: Command specs call `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/validate-gen-plan-io.sh`. Stop hooks depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh`. External dependencies include `git`, `gh`, `jq`, and `codex`. CI workflows under `.github/workflows/` run syntax/template/version/plan checks.
- edge_cases_or_failure_modes: Fork PR lookup is handled by setup, fetch, poll, and stop-hook paths (`scripts/setup-pr-loop.sh:290-378`, `scripts/fetch-pr-comments.sh:126-154`, `scripts/poll-pr-reviews.sh:122-150`, `hooks/pr-loop-stop-hook.sh:197-236`). Empty approval reviews are preserved with a placeholder (`scripts/fetch-pr-comments.sh:300-313`, `scripts/poll-pr-reviews.sh:276-289`). GitHub API failures retry and degrade to empty arrays in fetch/poll scripts (`scripts/fetch-pr-comments.sh:183-205`, `scripts/poll-pr-reviews.sh:207-235`). Startup cases distinguish initial reviews, existing reviews, and new commits after reviews (`scripts/setup-pr-loop.sh:433-463`, `hooks/pr-loop-stop-hook.sh:612-642`).
- validation_or_tests: Core behavior is covered by `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-stophook.sh`, validator tests, and robustness tests for hook input/state/path/session/security. I did not run tests because this task requested research notes only and the branch export is read-only/non-git; source inspection was used as evidence.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: Main PR Loop Stop hook. It intercepts Claude exit attempts, verifies local and remote prerequisites, polls GitHub bot feedback, asks local Codex to adjudicate each configured bot’s status, and either blocks with next-round feedback or renames `state.md` to a terminal state. Header states the core features: poll all active bots, detect closed/merged PRs, use Codex `APPROVE`, and update `active_bots` (`hooks/pr-loop-stop-hook.sh:3-19`).
- algorithmic_behavior: The hook reads hook stdin, locates the newest active PR loop via `find_active_pr_loop`, parses frontmatter, resolves the correct repository for fork PRs, checks PR state, requires a `round-N-pr-resolve.md`, verifies git cleanliness and pushed commits, detects force pushes, validates trigger comments and Claude eyes reaction, polls all configured bots, deduplicates comments, writes `round-(N+1)-pr-comment.md`, builds a local Codex prompt, parses Codex’s final marker, updates `active_bots` and goal tracker, and writes next feedback (`hooks/pr-loop-stop-hook.sh:42-76`, `87-176`, `197-276`, `282-360`, `367-423`, `429-447`, `465-525`, `907-1062`, `1185-1302`, `1372-1645`).
- inputs_outputs_state: Inputs include `.humanize/pr-loop/<timestamp>/state.md`, the current `round-N-pr-resolve.md`, git status/HEAD/upstream data, GitHub `gh pr view`, issue comments, bot reactions, `poll-pr-reviews.sh` JSON, and Codex CLI output. State fields parsed include `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, Codex config, polling config, timestamps, startup case, latest commit SHA/date, and trigger ID (`hooks/pr-loop-stop-hook.sh:90-108`, `120-174`). Outputs include hook JSON block decisions, updated `state.md`, terminal state files such as `approve-state.md`, `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `usage-limit-state.md`, generated `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md`.
- gates_or_invariants: Required field and type gates: missing PR number or non-numeric round exits safely (`hooks/pr-loop-stop-hook.sh:185-194`). Terminal PR states rename state and allow exit (`hooks/pr-loop-stop-hook.sh:243-255`). Missing resolution summary blocks (`hooks/pr-loop-stop-hook.sh:261-276`). Non-`.humanize` dirty git blocks; unpushed commits block because PR Loop always requires pushed changes (`hooks/pr-loop-stop-hook.sh:282-360`). Force-push detection clears stale trigger fields and requires re-trigger (`hooks/pr-loop-stop-hook.sh:367-420`). `NEXT_ROUND > max_iterations` renames to `maxiter-state.md` (`hooks/pr-loop-stop-hook.sh:429-435`). Empty `active_bots` completes (`hooks/pr-loop-stop-hook.sh:443-446`). Trigger requirements vary by round/startup case/new commits (`hooks/pr-loop-stop-hook.sh:612-642`), and Claude eyes verification is blocking when Claude is configured and a trigger is required (`hooks/pr-loop-stop-hook.sh:748-799`).
- dependencies_and_callers: Called as a Claude Stop hook through `hooks/hooks.json:52-64`. Depends on `hooks/lib/loop-common.sh` for active-loop lookup, bot mapping, YAML list rendering, mention strings, and goal tracker helpers (`hooks/pr-loop-stop-hook.sh:51-58`; `hooks/lib/loop-common.sh:733-820`, `1174-1248`). Uses `scripts/portable-timeout.sh`, `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, rendered templates from `prompt-template/block/*.md` and `prompt-template/pr-loop/codex-goal-tracker-update.md`, plus `git`, `gh`, `jq`, `date`, and `codex`.
- edge_cases_or_failure_modes: Fork PR base repo lookup is explicit and falls back to current repo with warning (`hooks/pr-loop-stop-hook.sh:204-236`). Force-push/history rewrite clears `last_trigger_at` and `trigger_comment_id` to prevent stale approvals (`hooks/pr-loop-stop-hook.sh:395-409`). New non-force commits refresh `latest_commit_at` and clear stale triggers (`hooks/pr-loop-stop-hook.sh:540-564`). Trigger detection is paginated, current-user filtered, and optionally constrained after latest commit time (`hooks/pr-loop-stop-hook.sh:470-516`). Codex can approve via PR `+1` reaction before or during polling (`hooks/pr-loop-stop-hook.sh:661-718`, `984-1040`). Per-bot timeouts are anchored to trigger time except all-comments startup cases (`hooks/pr-loop-stop-hook.sh:868-900`, `920-948`). Timed-out active bots may be removed and can complete the loop if no active bots remain (`hooks/pr-loop-stop-hook.sh:1070-1125`). Codex failure, empty output, `WAITING_FOR_BOTS`, and `USAGE_LIMIT_HIT` have separate terminal/blocking paths (`hooks/pr-loop-stop-hook.sh:1304-1366`, `1390-1422`).
- validation_or_tests: `tests/test-pr-loop-hooks.sh` directly tests YAML bot parsing, state/comment write protection, resolve-file allowance, comment sorting/deduplication, bot status extraction, re-add logic, trigger detection, approve/waiting markers, mocked stop-hook runtime, pagination, trigger priority, approval-only reviews, fixture-backed fetch/poll behavior, wrong-round resolve gating, and monitor display (`tests/test-pr-loop-hooks.sh:26-210`, `222-467`, `766-1148`, `1160-1419`, `1431-1621`). I did not execute it in this read-only research run.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for PR Loop hook and validator behavior. It defines `run_hook_tests()` and groups tests for validators, sorting, gatekeeper logic, stop-hook mocked execution, approval-only reviews, fixture-backed fetch/poll scripts, wrong-round validation, and monitor output (`tests/test-pr-loop-hooks.sh:1-15`).
- algorithmic_behavior: The file constructs disposable loop directories and mock `state.md` files, invokes validators with synthetic Claude hook JSON, mocks `gh` and `git` for stop-hook runtime cases, runs helper scripts against fixtures, and asserts outputs via `pass`/`fail`. It encodes algorithm expectations rather than being production logic: e.g. YAML list state shape (`tests/test-pr-loop-hooks.sh:26-63`), protected PR-loop files (`tests/test-pr-loop-hooks.sh:68-172`), Bash protection without active RLCR (`tests/test-pr-loop-hooks.sh:174-210`), sorting and deduplication (`tests/test-pr-loop-hooks.sh:222-286`), and Codex status-marker parsing (`tests/test-pr-loop-hooks.sh:326-457`).
- inputs_outputs_state: Inputs are `$TEST_DIR`, `$PROJECT_ROOT`, helper functions from the broader test harness, mocked `gh`/`git` executables, fixture JSON under `tests/fixtures`, and real scripts under `hooks/` and `scripts/`. Outputs are pass/fail records through the test harness plus temporary files such as `state.md`, `round-N-pr-resolve.md`, and generated fetch/poll outputs. Test state includes PR loop frontmatter with `configured_bots`, `active_bots`, `current_round`, timestamps, and trigger fields (`tests/test-pr-loop-hooks.sh:771-790`, `850-868`, `953-970`, `1060-1077`).
- gates_or_invariants: It asserts PR `state.md` and generated `pr-comment` files are write-blocked, while current-round `pr-resolve` is allowed (`tests/test-pr-loop-hooks.sh:68-172`). It asserts missing trigger blocks rounds greater than zero (`tests/test-pr-loop-hooks.sh:606-622`), round 0 prefers `last_trigger_at` over `started_at` when present (`tests/test-pr-loop-hooks.sh:624-665`), per-bot timeout is trigger-anchored (`tests/test-pr-loop-hooks.sh:667-696`), round files use `NEXT_ROUND` consistently (`tests/test-pr-loop-hooks.sh:726-744`), and wrong-round `pr-resolve` writes/edits are blocked while correct-round writes pass (`tests/test-pr-loop-hooks.sh:1431-1535`).
- dependencies_and_callers: Intended to be sourced by a larger test runner (`tests/test-pr-loop-hooks.sh:12-15`), likely `tests/run-all-tests.sh` or related harness. Depends on `tests/test-helpers.sh` style helpers (`pass`, `fail`, `init_test_git_repo`), `tests/setup-fixture-mock-gh.sh`, `tests/setup-monitor-test-env.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/humanize.sh`.
- edge_cases_or_failure_modes: Covers paginated trigger detection where the relevant comment appears on page 2 (`tests/test-pr-loop-hooks.sh:947-1048`), current-user filtering to avoid accepting other users’ @bot comments (`tests/test-pr-loop-hooks.sh:538-586`), empty-body approval reviews receiving `[Review state: ...]` placeholders (`tests/test-pr-loop-hooks.sh:1160-1196`), fixture-based after-filter behavior excluding earlier comments (`tests/test-pr-loop-hooks.sh:1287-1319`, `1371-1412`), bot re-add when a previously approved bot reports new issues (`tests/test-pr-loop-hooks.sh:353-388`), and monitor display when `active_bots` is empty (`tests/test-pr-loop-hooks.sh:1596-1621`).
- validation_or_tests: This file is itself validation. It was inspected but not executed because the task forbids modification and this export is read-only/non-git; many tests create temp repos/files and mock binaries and also depend on harness setup.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Small RLCR block template used when a write attempts to modify the loop-local `plan.md` backup. It defines the human-facing invariant that the backup is read-only and exists to preserve plan integrity (`prompt-template/block/plan-backup-protected.md:1-7`).
- algorithmic_behavior: The template is loaded by `hooks/loop-write-validator.sh` when the target filename is `plan.md` inside `.humanize/rlcr/`; the validator renders this template or a fallback, writes the message to stderr, and exits 2 to block the operation (`hooks/loop-write-validator.sh:175-185`).
- inputs_outputs_state: Input is a Write hook request whose `file_path` points at `.humanize/rlcr/.../plan.md`. Output is a blocking message explaining that the file is a backup and should be read, not modified. It does not mutate state.
- gates_or_invariants: Invariant: RLCR loop plan backups are immutable during the session. This prevents the active algorithm from changing the original acceptance plan via the loop-local backup, preserving the plan baseline for review and drift detection.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/plan-backup-protected.md"` in `hooks/loop-write-validator.sh:181-183`. Uses template-loader fallback behavior indirectly through `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If the template is missing or fails to render, the caller uses fallback text (`hooks/loop-write-validator.sh:181-183`). The gate only applies to `plan.md` under `.humanize/rlcr/`, not arbitrary repository plan files (`hooks/loop-write-validator.sh:179-185`).
- validation_or_tests: Plan-file and write-validator behavior is covered by tests such as `tests/test-plan-file-hooks.sh`, `tests/test-plan-file-validation.sh`, `tests/test-allowlist-validators.sh`, and robustness tests for plan files and hooks. This exact template is also discoverable from the write-validator reference at `hooks/loop-write-validator.sh:181-183`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-121 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: Prompt fragment injected into the PR Loop local Codex adjudication prompt. It instructs Codex to update the PR loop `goal-tracker.md` after analyzing bot feedback (`prompt-template/pr-loop/codex-goal-tracker-update.md:1-4`).
- algorithmic_behavior: The stop hook renders it with `GOAL_TRACKER_FILE` and `NEXT_ROUND`, appends it to the generated `round-(N+1)-codex-prompt.md`, then runs Codex. The template requires adding an Issue Summary row, updating totals, and adding an Issue Log entry (`prompt-template/pr-loop/codex-goal-tracker-update.md:5-20`; caller at `hooks/pr-loop-stop-hook.sh:1252-1263` and injection at `hooks/pr-loop-stop-hook.sh:1295-1302`).
- inputs_outputs_state: Inputs are the rendered `{{GOAL_TRACKER_FILE}}` and `{{NEXT_ROUND}}`, plus Codex’s interpretation of bot review content. Expected output is a modified goal tracker with per-reviewer issue rows, total issues found/resolved, remaining count, and a round log entry. The stop hook also has a fallback/idempotent helper, `update_pr_goal_tracker`, which repairs or supplements the tracker if Codex does not update it correctly (`hooks/lib/loop-common.sh:1174-1248`; stop-hook calls at `hooks/pr-loop-stop-hook.sh:1380-1384`, `1535-1541`).
- gates_or_invariants: The template preserves tracker structure: only Issue Summary, Total Statistics, and Issue Log may be modified; header sections such as PR Information and Ultimate Goal must not be changed (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`). Each reviewer gets a separate row, so multi-bot rounds do not collapse distinct review outcomes (`prompt-template/pr-loop/codex-goal-tracker-update.md:63-64`). The helper enforces idempotency by checking round+reviewer combinations before adding rows/log entries (`hooks/lib/loop-common.sh:1184-1214`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh`, which provides `GOAL_TRACKER_TEMPLATE_VARS`, fallback content, and the Codex prompt (`hooks/pr-loop-stop-hook.sh:1252-1302`). It depends on `hooks/lib/template-loader.sh` placeholder rendering and on the goal tracker initialized by `scripts/setup-pr-loop.sh:638-713`.
- edge_cases_or_failure_modes: If the template is unavailable, the stop hook falls back to a shorter goal-tracker update instruction (`hooks/pr-loop-stop-hook.sh:1258-1261`). If Codex partially updates the tracker, `update_pr_goal_tracker` detects missing summary/log pieces and completes them (`hooks/lib/loop-common.sh:1216-1224`). The non-approval path records issues found but keeps resolved count at zero until all bots approve, avoiding inflated resolution totals (`hooks/pr-loop-stop-hook.sh:1519-1533`).
- validation_or_tests: Goal-tracker update behavior is indirectly tested through PR-loop stop-hook tests and robustness tests around goal tracker/template handling. The executable spec asserts tracker-related state shape and stop-hook prompt/adjudication flow around `tests/test-pr-loop-hooks.sh:326-467` and mocked stop-hook sections at `tests/test-pr-loop-hooks.sh:766-1148`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TODO2TASK_CAREFUL_MODE-HZ-001`, `TODO2TASK_CAREFUL_MODE-HZ-031`, `TODO2TASK_CAREFUL_MODE-HZ-061`, `TODO2TASK_CAREFUL_MODE-HZ-091`, `TODO2TASK_CAREFUL_MODE-HZ-121`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`