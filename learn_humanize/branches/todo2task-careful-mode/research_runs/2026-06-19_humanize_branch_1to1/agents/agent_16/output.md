# agent_16 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness specification suite for the Humanize loop algorithms. The directory is flat, containing 17 shell test suites and no subdirectories. It stress-tests path validation, state machines, hook input validation, template rendering, PR-loop API handling, setup argument parsing, timeout behavior, git status parsing, session discovery, and cancel authorization.
- algorithmic_behavior: Each suite creates isolated temporary fixtures via `tests/test-helpers.sh` (`setup_test_dir`, `pass`, `fail`, `print_test_summary` at `tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:58`, `tests/test-helpers.sh:86`) and invokes production scripts/hooks rather than pure mocks where possible. `tests/run-all-tests.sh:55-72` includes all robustness suites in the broader ordered test run.
- inputs_outputs_state: Inputs are temporary git repositories, loop state files under `.humanize/rlcr` and `.humanize/pr-loop`, JSON hook payloads, mock `gh`/`codex`/timeout commands, plan files, template files, and command strings. Outputs are shell exit status plus pass/fail counters. State transitions exercised include active RLCR loops, finalize/cancel/complete states, PR-loop approval states, current round progression, review phase markers, and monitor cache/log state.
- gates_or_invariants: The directory encodes fail-closed gates: malformed hook JSON must reject or degrade safely; state parsing must distinguish strict and tolerant behavior; summary/state/goal-tracker paths must be constrained; plan paths must be relative, in-project, non-symlink, and contentful; setup scripts must reject invalid args and active-loop conflicts; timeout wrappers must preserve exit semantics.
- dependencies_and_callers: Key production dependencies are `hooks/lib/loop-common.sh` functions such as `validate_hook_input`, `find_active_loop`, `get_current_round`, `parse_state_file_strict`, and `find_active_pr_loop` (`hooks/lib/loop-common.sh:60`, `hooks/lib/loop-common.sh:157`, `hooks/lib/loop-common.sh:180`, `hooks/lib/loop-common.sh:259`, `hooks/lib/loop-common.sh:804`); monitor helpers in `scripts/humanize.sh` (`humanize_parse_goal_tracker`, `humanize_detect_git_state`, `humanize_parse_git_status`, `_humanize_monitor_codex` at `scripts/humanize.sh:32`, `scripts/humanize.sh:116`, `scripts/humanize.sh:166`, `scripts/humanize.sh:219`); timeout support in `scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:29`, `scripts/portable-timeout.sh:33`; setup scripts `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`; PR scripts `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh`.
- edge_cases_or_failure_modes: Child suites cover: base branch fallback and missing-origin failures (`test-base-branch-detection.sh:3-11`); cancel command injection, path normalization, and symlink bypasses (`test-cancel-security-robustness.sh:3-10`, `test-cancel-security-robustness.sh:334-392`); concurrent/partial state reads (`test-concurrent-state-robustness.sh:3-10`); dirty git states, detached HEAD, merge/rebase/shallow/non-repo cases (`test-git-operations-robustness.sh:3-10`); malformed/binary/large goal trackers (`test-goal-tracker-robustness.sh:3-10`); hook JSON, non-UTF8, null, terminal/log monitor cases (`test-hook-input-robustness.sh:3-9`); hook system blockers and stop-hook parsing (`test-hook-system-robustness.sh:3-15`); plan path and content validation (`test-path-validation-robustness.sh:3-7`); large/mixed/binary plan files (`test-plan-file-robustness.sh:3-10`); PR-loop API/rate/network/bot JSON behavior (`test-pr-loop-api-robustness.sh:3-11`); session discovery races and unusual names (`test-session-robustness.sh:3-9`); malformed YAML and full-review-round state fields (`test-state-file-robustness.sh:3-9`); valid/invalid state transitions (`test-state-transition-robustness.sh:3-9`); template error and stress cases (`test-template-error-robustness.sh:3-10`, `test-template-stress-robustness.sh:3-9`); timeout fallback and signal handling (`test-timeout-robustness.sh:3-9`); setup script argument/YAML/git/symlink/skip-impl behavior (`test-setup-scripts-robustness.sh:3-10`).
- validation_or_tests: The directory is itself validation evidence. I inspected it recursively with no artificial depth limit; I did not execute the suites because this worker task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Fixture generator for monitor PR-loop tests. It synthesizes `.humanize/pr-loop/<timestamp>/state.md` variants so monitor/status code can be tested against YAML-list bot fields and approval states.
- algorithmic_behavior: Requires `<test_dir>` and optional `<test_name>` (`tests/setup-monitor-test-env.sh:7`, `tests/setup-monitor-test-env.sh:12-18`). Dispatches by `TEST_NAME` with three cases: `yaml_list`, `configured`, and `empty` (`tests/setup-monitor-test-env.sh:20-83`). Each case creates a timestamped PR-loop directory and writes frontmatter-only `state.md` with `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`.
- inputs_outputs_state: Input 1 is target test directory; input 2 selects fixture shape. Output is the created state tree plus stdout echo of `TEST_DIR` (`tests/setup-monitor-test-env.sh:91`). State shapes model all bots active (`active_bots: claude,codex` at `tests/setup-monitor-test-env.sh:34-36`), partial approval (`active_bots: codex` at `tests/setup-monitor-test-env.sh:56-58`), and full approval/empty active set (`active_bots:` blank at `tests/setup-monitor-test-env.sh:77-78`).
- gates_or_invariants: Fails closed when `TEST_DIR` is missing (`tests/setup-monitor-test-env.sh:15-18`) or an unknown fixture is requested (`tests/setup-monitor-test-env.sh:84-88`). All generated fixtures use deterministic timestamps, making newest-loop selection predictable.
- dependencies_and_callers: Uses only POSIX-ish shell utilities plus `mkdir` and heredocs. It is related to PR-loop monitor tests such as `tests/manual-monitor-test.sh` and monitor/status parsing in `scripts/humanize.sh`, though direct callers were not found in the inspected grep output.
- edge_cases_or_failure_modes: Unknown fixture name exits 1 with available names. The `empty` case intentionally leaves `active_bots` without YAML list entries, exercising parsers that must distinguish no active bots from malformed state.
- validation_or_tests: This script is a fixture specification rather than a test runner. It supports monitor behavior that interprets configured-vs-active bot state.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when Codex review fails to produce required review output. It is part of the review gate contract: failed or missing Codex review output blocks loop exit and requests retry.
- algorithmic_behavior: Renders a concise Markdown block with placeholders for exit code, missing review result path, debug command/stdout/stderr files, and last stderr content (`prompt-template/block/codex-review-failed.md:1-18`). The main RLCR stop hook loads it through `load_and_render_safe` in `codex_failure_exit` (`hooks/loop-codex-stop-hook.sh:1227-1236`) after collecting stderr tail (`hooks/loop-codex-stop-hook.sh:1184-1188`).
- inputs_outputs_state: Inputs are template variables: `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, `STDERR_CONTENT`. The caller also passes `FAILURE_REASON`, `ROUND_NUMBER`, `BASE_BRANCH`, and `EXIT_CODE`, but this template does not consume all of those names. Output is a rendered Markdown reason string embedded into a JSON block decision (`hooks/loop-codex-stop-hook.sh:1238-1240`).
- gates_or_invariants: The invariant is that absence of a created review result is not silently accepted. The hook checks for missing result, tries to copy nonempty stdout into the result file, and if still missing blocks through `codex_failure_exit` (`hooks/loop-codex-stop-hook.sh:1341-1372`). It also blocks empty review-result files (`hooks/loop-codex-stop-hook.sh:1387-1394`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` via `load_and_render_safe`; the direct RLCR caller is `hooks/loop-codex-stop-hook.sh:1228`. `rg` also found related inline PR-loop failure messaging in `hooks/pr-loop-stop-hook.sh:1344-1353`, but that call site does not load this template.
- edge_cases_or_failure_modes: If the template is missing or rendering fails, `load_and_render_safe` falls back to an inline, more detailed failure block (`hooks/loop-codex-stop-hook.sh:1190-1225`). A contract mismatch exists: the template references `CODEX_STDERR_FILE`, while the RLCR caller passes `CODEX_LOG_FILE` at `hooks/loop-codex-stop-hook.sh:1236`; unless another caller supplies `CODEX_STDERR_FILE`, that placeholder can remain unresolved.
- validation_or_tests: Template loading/rendering is covered generally by template suites under `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and robustness suites for template errors/stress. The failure path is behaviorally covered by stop-hook/review tests, with this template acting as the rendered reason surface.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Blocking prompt template for enforcing that round summary files are written inside the active loop directory, not elsewhere.
- algorithmic_behavior: Renders a minimal Markdown message with a single `CORRECT_PATH` placeholder (`prompt-template/block/wrong-summary-location.md:1-5`). `hooks/loop-write-validator.sh` invokes it when a summary file write is detected outside `.humanize/rlcr` (`hooks/loop-write-validator.sh:198-209`), then exits with code 2 to block the write.
- inputs_outputs_state: Input is `CORRECT_PATH`, computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-write-validator.sh:202-208`). Output is stderr text explaining the correct path, followed by validator exit 2. The state read upstream is the active loop directory and current round.
- gates_or_invariants: Summary files must live in the loop directory. This gate triggers before round-number validation inside the loop directory, so wrong location is classified separately from wrong round (`hooks/loop-write-validator.sh:198-245`).
- dependencies_and_callers: Depends on `load_and_render_safe` from the template loader and active-loop/path classification in `loop-write-validator.sh`. Related same-domain blockers include wrong round and wrong directory templates, but this file only handles summary-location errors.
- edge_cases_or_failure_modes: If the template cannot load, `loop-write-validator.sh` provides an inline fallback with the same `CORRECT_PATH` variable (`hooks/loop-write-validator.sh:204-208`). If `ACTIVE_LOOP_DIR` or `CURRENT_ROUND` is wrong upstream, the rendered correction path will also be wrong; this template does no validation itself.
- validation_or_tests: Hook system robustness covers summary/state path blocking broadly; template reference/comprehensive tests cover that referenced templates load and placeholders render.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-136 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable spec for `scripts/setup-rlcr-loop.sh` plan path and content validation. It verifies that the setup algorithm accepts safe relative plan paths and rejects path traversal/injection/symlink/content failures before loop setup proceeds.
- algorithmic_behavior: Sets up a temporary git repository with one commit (`tests/robustness/test-path-validation-robustness.sh:37-45`), injects a mock `codex` into `PATH` (`tests/robustness/test-path-validation-robustness.sh:18-35`), creates valid plan content (`tests/robustness/test-path-validation-robustness.sh:47-63`), then calls production `scripts/setup-rlcr-loop.sh` through `test_path_validation` (`tests/robustness/test-path-validation-robustness.sh:74-111`). The helper classifies path/content validation errors by matching production error text (`tests/robustness/test-path-validation-robustness.sh:89-93`) and treats later `requires codex` or `must be gitignored` failures as evidence that path validation passed (`tests/robustness/test-path-validation-robustness.sh:95-103`).
- inputs_outputs_state: Inputs are candidate plan paths and generated files/directories under `TEST_DIR`. Output is pass/fail accounting via `print_test_summary` and process exit status (`tests/robustness/test-path-validation-robustness.sh:480-485`). The suite repeatedly deletes `.humanize/rlcr` before each production invocation to avoid false active-loop failures (`tests/robustness/test-path-validation-robustness.sh:81-86`).
- gates_or_invariants: Positive path invariants: normal relative paths, root filenames, dash/underscore names, nested directories, and dotted filenames are accepted (`tests/robustness/test-path-validation-robustness.sh:117-167`). Negative gates reject absolute paths, whitespace, shell metacharacters including semicolon, pipe, `$`, backticks, angle brackets, ampersand, glob chars, backslash, tilde, parentheses, braces, brackets, hash, and exclamation (`tests/robustness/test-path-validation-robustness.sh:177-330`). Symlink gates reject the plan file itself, parent directory symlinks, and symlink chains (`tests/robustness/test-path-validation-robustness.sh:340-374`). Long safe paths are accepted where the filesystem supports them (`tests/robustness/test-path-validation-robustness.sh:385-410`). Content gates reject empty, comments-only, fewer-than-5-line, nonexistent, and directory inputs (`tests/robustness/test-path-validation-robustness.sh:423-478`).
- dependencies_and_callers: Directly depends on `tests/test-helpers.sh` (`tests/robustness/test-path-validation-robustness.sh:14`) and production `scripts/setup-rlcr-loop.sh` (`tests/robustness/test-path-validation-robustness.sh:86`). The production validation block rejects absolute paths, whitespace, shell metacharacters, file symlinks, missing parent dirs, parent symlinks, missing/unreadable/non-file paths, out-of-project resolution, and submodule paths (`scripts/setup-rlcr-loop.sh:327-428`), then enforces tracking/gitignore policy (`scripts/setup-rlcr-loop.sh:430-479`) and content thresholds (`scripts/setup-rlcr-loop.sh:483-540`).
- edge_cases_or_failure_modes: The test’s classifier is string-pattern based, so production error message drift can make tests misclassify outcomes even if behavior is still correct. It intentionally accepts some later setup failures as “validation passed,” because the suite’s scope ends at path/content validation. Unicode/CJK/emoji paths are explicitly noted as allowed and not tested as invalid (`tests/robustness/test-path-validation-robustness.sh:412-414`).
- validation_or_tests: This file is both assigned item and validation artifact. It is included in the aggregate runner at `tests/run-all-tests.sh:59`. I inspected it only; I did not execute it in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 5 assigned item sections are present exactly once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`