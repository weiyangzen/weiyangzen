# agent_25 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-025 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash validator for the RLCR and PR-loop state machines. It prevents shell-command writes from bypassing Write/Edit validators and protects loop-owned state, prompts, summaries, todos, goal trackers, and local `.humanize` metadata.
- algorithmic_behavior: The hook reads one JSON hook payload from stdin, validates it, ignores non-Bash tools, extracts `tool_input.command`, lowercases the command, discovers the newest active RLCR loop under `.humanize/rlcr` and newest active PR loop under `.humanize/pr-loop`, and then applies loop-specific blocking rules. See `hooks/loop-bash-validator.sh:24-48` for input parsing and `hooks/loop-bash-validator.sh:54-67` for active loop discovery and the no-loop allow path. For RLCR, it selects `finalize-state.md` over `state.md` when present and fail-closes if strict state parsing fails, at `hooks/loop-bash-validator.sh:74-86`.
- inputs_outputs_state: Inputs are JSON fields `tool_name` and `tool_input.command`, plus `CLAUDE_PROJECT_DIR` or `pwd` as project root. It depends on RLCR state frontmatter fields parsed into `STATE_CURRENT_ROUND` and `STATE_PUSH_EVERY_ROUND`. Outputs are process exit decisions: `0` allows, `1` rejects malformed hook/state input, and `2` blocks a Bash command with a rendered message. It does not directly advance loop state; it enforces who may mutate loop state. It delegates state parsing and message rendering to `hooks/lib/loop-common.sh`.
- gates_or_invariants: It blocks `git push` during RLCR unless `push_every_round` is true, at `hooks/loop-bash-validator.sh:93-105`. It blocks `git add` operations that would add `.humanize`, at `hooks/loop-bash-validator.sh:115-118` using `git_adds_humanize` from `hooks/lib/loop-common.sh:936-1032`. It blocks direct and indirect modification of `state.md` and `finalize-state.md`, including destination writes, `mv`/`cp` from those files, shell-wrapper payloads, command chaining, and leading redirection tricks, at `hooks/loop-bash-validator.sh:139-328`. Authorized cancel is the only state-file exception, via `is_cancel_authorized`, which requires `.cancel-requested`, rejects command substitution, shell operators, extra args, mixed quote styles, symlink sources, and requires exactly moving the active state file to `cancel-state.md`; see `hooks/lib/loop-common.sh:557-716`. It blocks RLCR plan backup writes, goal-tracker Bash writes, round prompt writes, summary Bash writes, and restricted todos writes at `hooks/loop-bash-validator.sh:330-393`. For PR loops, it blocks `state.md` and generated read-only round files at `hooks/loop-bash-validator.sh:403-442`.
- dependencies_and_callers: Registered as the Bash matcher command in `hooks/hooks.json:43-48`. It sources `hooks/lib/loop-common.sh` at `hooks/loop-bash-validator.sh:16-18`. Key helper dependencies are `validate_hook_input`, `require_tool_input_field`, `is_deeply_nested`, `find_active_loop`, `find_active_pr_loop`, `parse_state_file_strict`, `command_modifies_file`, `git_adds_humanize`, and block-message renderers. It requires `jq`, `sed`, `grep`, `tr`, and normal shell utilities. It uses templates such as `block/git-push.md`, `block/goal-tracker-bash-write.md`, `block/goal-tracker-modification.md`, PR-loop block templates, and inline fallbacks through `load_and_render_safe`.
- edge_cases_or_failure_modes: If no active RLCR or PR loop exists, all Bash commands are allowed, including otherwise dangerous commands; `tests/robustness/test-hook-system-robustness.sh:369-382` asserts that broader command safety is delegated to the sandbox. Malformed active RLCR state blocks all Bash for safety. Deeply nested JSON over depth 30 is rejected before command evaluation. Lowercasing the entire command makes matching simpler but can blur case-sensitive paths. `command_modifies_file` is regex-based rather than a full shell parser, so it handles common redirection/edit forms but remains pattern-driven; see `hooks/lib/loop-common.sh:1077-1102`. The todos gate has a narrow active-loop path exception for `round-[12]-todos.md` at `hooks/loop-bash-validator.sh:385-390`, which is unusual for a section titled “Block Todos File Modifications” and could need careful interpretation. While a PR loop is active, the bare `state.md` check can intentionally overblock relative-path attempts at `hooks/loop-bash-validator.sh:411-414`.
- validation_or_tests: Related tests pipe real hook JSON into this script. `tests/robustness/test-hook-system-robustness.sh:317-367` verifies Bash blocks state and goal-tracker modification. `tests/robustness/test-hook-input-robustness.sh:59-69` verifies valid Bash JSON passes, `tests/robustness/test-hook-input-robustness.sh:117-147` covers long and special-character commands, and `tests/robustness/test-hook-input-robustness.sh:177-217` covers deep nesting and non-UTF8 rejection. `tests/test-pr-loop-hooks.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-cancel-signal-file.sh`, and `tests/test-allowlist-validators.sh` also call the hook according to repository references.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-055 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared executable-test harness for core algorithm tests. It is not production loop logic, but it defines the pass/fail accounting, temporary workspace setup, and mock git repository initialization used by tests that specify hook and state-machine behavior.
- algorithmic_behavior: Provides `pass`, `fail`, and `skip` functions that print colored status lines and increment global counters at `tests/test-helpers.sh:30-52`. `print_test_summary` reports totals and returns success only when `TESTS_FAILED` is zero at `tests/test-helpers.sh:58-78`. `setup_test_dir` creates a temporary test directory and registers cleanup at `tests/test-helpers.sh:84-89`. `init_test_git_repo` creates a minimal git repository with configured identity and one initial commit at `tests/test-helpers.sh:91-105`.
- inputs_outputs_state: Inputs are sourced test scripts, optional summary title, target directory for git repo initialization, and the host shell/git environment. Outputs are colored terminal messages, updated global counters `TESTS_PASSED`, `TESTS_FAILED`, `TESTS_SKIPPED`, the `TEST_DIR` variable, an EXIT trap, and a mock git repository with `file.txt` committed.
- gates_or_invariants: The key invariant is that any failed assertion increments `TESTS_FAILED`, and `print_test_summary` converts that accumulated state into the test script’s final exit status. Tests that use this helper can accumulate multiple failures before exiting, rather than aborting at the first assertion.
- dependencies_and_callers: Sourced broadly by robustness and integration tests. Direct references include `tests/robustness/test-timeout-robustness.sh:17`, `tests/robustness/test-hook-input-robustness.sh:17`, `tests/robustness/test-hook-system-robustness.sh:24`, `tests/test-pr-loop.sh:19`, and many sibling robustness scripts found by repository search. It depends on Bash arithmetic, `mktemp`, `trap`, `git`, and `echo -e`.
- edge_cases_or_failure_modes: `setup_test_dir` installs a single EXIT trap, so repeated calls overwrite prior cleanup behavior. The trap string is unquoted, which is usually fine for `mktemp` paths but fragile for paths with whitespace. `init_test_git_repo` changes directories and then returns with `cd -`; failures in `git init`, config, add, or commit can leave the caller in an unexpected location if the surrounding test does not fail fast. The helper assumes git is available and that commits are permitted in the temporary directory.
- validation_or_tests: This file is itself the shared validation scaffold rather than a tested production unit. Its behavior is indirectly validated whenever sourced tests rely on its counters and summary exit code. In the assigned set, `tests/robustness/test-timeout-robustness.sh:17` uses it and exits through `print_test_summary` at `tests/robustness/test-timeout-robustness.sh:278-279`.
- skip_candidate: `yes: support harness rather than core state-machine implementation; still relevant because it defines how executable specs report pass/fail and create isolated state.`

### TODO2TASK_CAREFUL_MODE-HZ-085 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Round 0 goal-tracker gate. It tells the agent that Bash writes to `goal-tracker.md` are forbidden and directs it to use Write/Edit on the canonical path.
- algorithmic_behavior: The template renders a short Markdown reason headed “Bash Write Blocked: Use Write or Edit Tool” and interpolates `{{CORRECT_PATH}}` into the allowed tool path at `prompt-template/block/goal-tracker-bash-write.md:1-7`. It encodes the algorithmic distinction between shell-based mutation, which bypasses validation hooks, and Write/Edit-based mutation, which remains hook-validated.
- inputs_outputs_state: Input is the template variable `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message`. Output is the blocking reason shown to the agent. It has no local state and performs no transition itself; it is a policy surface used by the Bash validator.
- gates_or_invariants: The invariant is that `goal-tracker.md` must not be modified by `cat`, `echo`, `sed`, `awk`, or other Bash write/edit commands. During RLCR Round 0, the acceptable transition is to use Write/Edit on the active loop’s goal tracker path. The caller-side gate is at `hooks/loop-bash-validator.sh:349-357`: when `CURRENT_ROUND` is `0`, this template is used; when the round is greater than zero, the validator instead directs the user to request goal-tracker updates in the round summary.
- dependencies_and_callers: Rendered by `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh:498-507`, which uses `load_and_render_safe` and falls back to an inline message if the template is unavailable. Called by `hooks/loop-bash-validator.sh:349-353` when `command_modifies_file` detects a Bash command targeting `goal-tracker.md`.
- edge_cases_or_failure_modes: If `CORRECT_PATH` is missing or the template loader fails to substitute it, the message can be less actionable; the inline fallback in `hooks/lib/loop-common.sh:502-504` preserves the same placeholder structure. This template only covers Round 0 Bash writes. After Round 0, the active policy is a different template, `block/goal-tracker-modification.md`, via `goal_tracker_blocked_message` at `hooks/lib/loop-common.sh:1106-1115`.
- validation_or_tests: `tests/test-template-references.sh:152-159` includes `block/goal-tracker-bash-write.md` in the expected common template list. Hook-level coverage comes through the goal-tracker Bash block path in `tests/robustness/test-hook-system-robustness.sh:353-367`, though that specific test exercises the post-Round-0 branch.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-115 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Review-phase prompt template for the RLCR loop. It defines the agent contract after Codex review finds severity-marked issues and keeps the loop in review/fix mode until those issues are resolved.
- algorithmic_behavior: The template inserts `{{REVIEW_CONTENT}}` under “Review Results” and instructs the agent to address all `[P0-9]` severity issues, make fixes only, commit changes, and write a summary to `{{SUMMARY_FILE}}`; see `prompt-template/claude/review-phase-prompt.md:1-28`. It explicitly says the COMPLETE signal has no effect during review and that Codex will review again after the fix summary.
- inputs_outputs_state: Inputs are `REVIEW_CONTENT` and `SUMMARY_FILE`. Output is the next round prompt file content. In the caller, `handle_review_issues` increments or sets the review round in `state.md`, writes `round-${round}-prompt.md`, points the agent to `round-${round}-summary.md`, and returns a JSON block decision containing the rendered prompt; see `hooks/loop-codex-stop-hook.sh:1130-1171`.
- gates_or_invariants: The review phase cannot be exited by a generic completion signal. The agent must resolve severity-marked findings, avoid unrelated feature work, commit the fixes, and produce the summary file before Codex reviews again. The upstream detection gate is `detect_review_issues`, which scans Codex review logs for `[P?]` markers in the first 10 characters of a line and extracts from the first issue onward; see `hooks/lib/loop-common.sh:338-397`.
- dependencies_and_callers: Called by `load_and_render_safe "$TEMPLATE_DIR" "claude/review-phase-prompt.md"` in `hooks/loop-codex-stop-hook.sh:1159-1161`. It depends on the stop hook’s review log analysis, loop directory variables, state file path, and template loader. It also depends semantically on Codex review output using priority markers in the expected form.
- edge_cases_or_failure_modes: If review findings do not include a marker in the expected early-line position, the review-phase prompt may not be generated because upstream detection treats the log as issue-free. If the review log is missing or empty, the stop hook has a separate hard-error path rather than this template. If `REVIEW_CONTENT` is large, the rendered prompt can become large because the template embeds the findings directly. The “fixes only” instruction is policy, not mechanically enforced by this file alone; enforcement depends on later review and hook gates.
- validation_or_tests: The caller path is visible in `hooks/loop-codex-stop-hook.sh:1140-1171`. Template presence is indirectly checked by template-reference coverage, and review-loop behavior is tied to `detect_review_issues` in `hooks/lib/loop-common.sh:355-397`. I did not execute tests in this read-only research pass.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-145 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for the repository’s portable timeout abstraction, which is used by setup scripts and hooks to prevent hanging git, gh, hook, and Codex subprocesses.
- algorithmic_behavior: The script sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh`, then runs 18 assertions against `detect_timeout_impl`, `TIMEOUT_IMPL`, and `run_with_timeout`; see `tests/robustness/test-timeout-robustness.sh:14-18` and `tests/robustness/test-timeout-robustness.sh:31-272`. It verifies implementation detection, accepted implementation names, quick command completion, timeout exit code `124`, argument preservation, exit-code preservation, pipeline/subshell behavior through `sh -c`, short elapsed time, zero-timeout behavior, large output handling, repeated cycles, nonexistent command handling, empty command behavior, special characters, signal-handling tolerance, fallback-chain validity, and export visibility.
- inputs_outputs_state: Inputs are the host commands available on PATH: `gtimeout`, GNU `timeout`, `python3`, `python`, or none; plus shell utilities such as `sleep`, `date`, `seq`, `wc`, `printf`, and `sh`. It consumes the `TIMEOUT_IMPL` value assigned by `scripts/portable-timeout.sh:29`. Outputs are PASS/FAIL lines and a final exit status from `print_test_summary` at `tests/robustness/test-timeout-robustness.sh:278-279`.
- gates_or_invariants: The implementation name must be one of `gtimeout`, `timeout`, `python3`, `python`, or `none` at `tests/robustness/test-timeout-robustness.sh:39-49`. When a timeout implementation exists, a timed-out command should return `124`, as checked at `tests/robustness/test-timeout-robustness.sh:63-77` and `tests/robustness/test-timeout-robustness.sh:120-137`. Successful commands must preserve stdout, arguments, and nonzero exit codes, at `tests/robustness/test-timeout-robustness.sh:51-110`. The fallback chain must return a valid implementation at `tests/robustness/test-timeout-robustness.sh:235-247`.
- dependencies_and_callers: Directly depends on `scripts/portable-timeout.sh`, whose detection priority is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none`; see `scripts/portable-timeout.sh:9-27`. `run_with_timeout` dispatches to the selected implementation at `scripts/portable-timeout.sh:33-71` and exports `TIMEOUT_IMPL` at `scripts/portable-timeout.sh:73-76`. Repository search shows the timeout abstraction is used by `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: If `TIMEOUT_IMPL` is `none`, timeout-specific tests are treated as passed skips in tests 4, 8, and 9, so the suite can pass on a platform without real timeout enforcement. Test 13 accepts any empty-command result and only records that it was handled, so it is a crash-safety check rather than a semantic assertion. Signal handling accepts either `124` or `0`, acknowledging implementation differences at `tests/robustness/test-timeout-robustness.sh:221-233`. The Python fallback in `scripts/portable-timeout.sh:47-62` uses `subprocess.run` without shell expansion, which is appropriate for argv preservation but means callers needing pipelines must explicitly use `sh -c`, as the tests do.
- validation_or_tests: This file is itself the validation script. It ends by calling `print_test_summary "Timeout Robustness Test Summary"` and exiting with that result at `tests/robustness/test-timeout-robustness.sh:274-279`. I inspected it directly and did not execute it because the assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 unique Item Evidence sections; each assigned row represented once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`