# agent_06 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime command layer for the humanize loop system. The directory owns shell entrypoints for starting, cancelling, polling, fetching, monitoring, timeout-wrapping, and validating loop workflows. It coordinates with `hooks/` for enforcement, `prompt-template/` for rendered prompts/block reasons, `.humanize/rlcr/` and `.humanize/pr-loop/` for state, and GitHub/Codex CLIs for review-loop transitions.
- algorithmic_behavior: The directory has three main algorithm families. PR-loop scripts create `.humanize/pr-loop/<timestamp>` sessions, discover the active PR, fetch and classify reviewer comments, determine startup cases, render round-0 prompts, poll remote bots, and cancel sessions. RLCR scripts create `.humanize/rlcr/<timestamp>` sessions, validate plan paths/content/git state, capture base branch/commit, write state and prompts, and cancel normal/finalize phases. Shared utilities provide portable timeouts and monitor parsing/display.
- inputs_outputs_state: `setup-pr-loop.sh` accepts bot flags and Codex settings, validates active loop exclusivity, git repo, `gh`, `codex`, PR state, branch/model YAML safety, writes `state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-prompt.md`, and optionally posts trigger comments for stale reviews (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:220`, `scripts/setup-pr-loop.sh:430`, `scripts/setup-pr-loop.sh:615`). `setup-rlcr-loop.sh` accepts plan/base/model options, validates path/tracking/content, captures `base_commit`, writes RLCR state/prompt/goal tracker, and sets `review_started` according to `--skip-impl` (`scripts/setup-rlcr-loop.sh:327`, `scripts/setup-rlcr-loop.sh:454`, `scripts/setup-rlcr-loop.sh:660`, `scripts/setup-rlcr-loop.sh:716`). `fetch-pr-comments.sh` writes formatted markdown; `poll-pr-reviews.sh` writes JSON; cancel scripts rename active state to `cancel-state.md`.
- gates_or_invariants: Only one RLCR or PR loop may be active (`scripts/setup-pr-loop.sh:220`, `scripts/setup-rlcr-loop.sh:247`). State files are frontmatter-driven and guarded by schema/path/branch invariants. PR branch, Codex model, and Codex effort are constrained to YAML-safe characters (`scripts/setup-pr-loop.sh:384`, `scripts/setup-pr-loop.sh:393`). RLCR plan files must be relative, no spaces/metacharacters, not symlink-traversing, inside project, not submodule-contained, and either clean tracked with `--track-plan-file` or untracked/gitignored otherwise (`scripts/setup-rlcr-loop.sh:327`, `scripts/setup-rlcr-loop.sh:353`, `scripts/setup-rlcr-loop.sh:401`, `scripts/setup-rlcr-loop.sh:454`). Git/GitHub commands are timeout-wrapped through `scripts/portable-timeout.sh`.
- dependencies_and_callers: `scripts/setup-pr-loop.sh` sources `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, and `scripts/portable-timeout.sh`; calls `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:35`, `scripts/setup-pr-loop.sh:39`, `scripts/setup-pr-loop.sh:431`, `scripts/setup-pr-loop.sh:438`, `scripts/setup-pr-loop.sh:587`). `scripts/humanize.sh` sources `scripts/lib/monitor-common.sh` and exposes `humanize monitor pr|rlcr` (`scripts/humanize.sh:6`, `scripts/humanize.sh:900`). Stop hooks call these scripts indirectly for PR polling and state transitions, especially `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: Fork PRs are explicitly handled by resolving current repo then parent/base repo in PR scripts (`scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`, `scripts/check-pr-reviewer-status.sh:109`). API failures in fetch/poll retry three times and degrade to empty arrays plus warnings rather than aborting all processing (`scripts/fetch-pr-comments.sh:183`, `scripts/poll-pr-reviews.sh:207`). `setup-pr-loop.sh` avoids duplicate stale-review trigger comments by stripping code blocks/quotes before mention detection and requires Claude eyes reaction when Claude is active (`scripts/setup-pr-loop.sh:467`, `scripts/setup-pr-loop.sh:564`). Cancel scripts fail when no newest active state exists and preserve state by rename (`scripts/cancel-pr-loop.sh:75`, `scripts/cancel-pr-loop.sh:121`; `scripts/cancel-rlcr-loop.sh:117`).
- validation_or_tests: `tests/test-pr-loop.sh` delegates to script, hook, and stop-hook suites (`tests/test-pr-loop.sh:32`). `tests/test-pr-loop-scripts.sh` validates setup/cancel/fetch/poll argument and state behaviors (`tests/test-pr-loop-scripts.sh:405`). Fixture-backed tests verify comment fetch/poll behavior with approval-only PR reviews and `--after` filtering (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1287`, `tests/test-pr-loop-hooks.sh:1322`, `tests/test-pr-loop-hooks.sh:1371`). Robustness and monitor tests cover schema, state, timeout, path, hook, and monitor behavior.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-036 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: PR-loop comment ingestion and markdown normalization script. It fetches all GitHub PR feedback surfaces and transforms them into the review prompt input consumed by `setup-pr-loop.sh`.
- algorithmic_behavior: Parses `<pr_number> <output_file>` plus `--after` and `--bots`, validates numeric PR and prerequisites (`gh`, `jq`), resolves the correct PR base repository for forks, fetches issue comments, inline review comments, and PR review submissions, normalizes them into a common JSON shape, deduplicates by `id`, filters by timestamp, sorts humans before bots and newest first, then emits markdown sections for humans and bots. Bot grouping maps `codex` to `chatgpt-codex-connector[bot]` and other bots to `<bot>[bot]` (`scripts/fetch-pr-comments.sh:27`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:130`, `scripts/fetch-pr-comments.sh:260`, `scripts/fetch-pr-comments.sh:322`, `scripts/fetch-pr-comments.sh:337`, `scripts/fetch-pr-comments.sh:381`).
- inputs_outputs_state: Inputs are GitHub PR number, output markdown path, optional ISO timestamp and active bot list. Outputs a markdown file beginning with PR number, fetched timestamp, and repository, followed by human and bot comment sections, ending with `*End of comments*` (`scripts/fetch-pr-comments.sh:245`, `scripts/fetch-pr-comments.sh:357`, `scripts/fetch-pr-comments.sh:440`). It uses a temporary directory cleaned by `trap` and does not modify loop state directly (`scripts/fetch-pr-comments.sh:168`).
- gates_or_invariants: Requires numeric PR number and output file; exits on unknown options or missing option args (`scripts/fetch-pr-comments.sh:29`, `scripts/fetch-pr-comments.sh:74`, `scripts/fetch-pr-comments.sh:92`). Requires `gh` and `jq` (`scripts/fetch-pr-comments.sh:112`). PR base repo must resolve from current or parent repo; otherwise it exits (`scripts/fetch-pr-comments.sh:139`, `scripts/fetch-pr-comments.sh:151`).
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` to create `round-0-pr-comment.md` before rendering `round-0-prompt.md` (`scripts/setup-pr-loop.sh:425`, `scripts/setup-pr-loop.sh:430`). Uses GitHub REST endpoints `issues/<pr>/comments`, `pulls/<pr>/comments`, and `pulls/<pr>/reviews` (`scripts/fetch-pr-comments.sh:212`, `scripts/fetch-pr-comments.sh:216`, `scripts/fetch-pr-comments.sh:220`). Tests mock `gh` through `tests/setup-fixture-mock-gh.sh`.
- edge_cases_or_failure_modes: API fetch failure is retried three times; final failure writes `[]`, increments `API_FAILURES`, and continues so partial outages produce incomplete output with warnings rather than hard failure (`scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:199`, `scripts/fetch-pr-comments.sh:445`). Approval-only reviews with empty body are preserved using a review-state placeholder (`scripts/fetch-pr-comments.sh:300`). Entries with null `created_at` are filtered before ISO parsing to avoid `fromdateiso8601` errors (`scripts/fetch-pr-comments.sh:334`). A real edge concern: deduplication uses only `.id` across issue comments, review comments, and reviews; GitHub IDs are typically globally unique enough, but if endpoint-local collisions occurred, one item would be dropped (`scripts/fetch-pr-comments.sh:322`).
- validation_or_tests: Direct script argument tests cover help, missing PR, missing output, and invalid PR (`tests/test-pr-loop-scripts.sh:256`). Fixture tests verify all comment types, approval-only placeholder preservation, and `--after` filtering (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1287`). Polling counterpart tests verify similar JSON behavior for PR reviews (`tests/test-pr-loop-hooks.sh:1322`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-066 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Top-level executable specification and test runner for PR-loop behavior. It is not itself algorithm implementation, but it defines the validation surface for the PR-loop command, hook, and stop-hook algorithm.
- algorithmic_behavior: Establishes test context by resolving `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared test helpers and PR-loop test library, initializes a PR-loop test environment, sources three test modules, then runs script tests, hook tests, stop-hook tests, and prints a summary (`tests/test-pr-loop.sh:15`, `tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`).
- inputs_outputs_state: Input is no CLI args; it relies on repository-local test helpers and creates/manipulates test fixtures through delegated modules. Output is test pass/fail reporting via `print_test_summary` (`tests/test-pr-loop.sh:53`). State is initialized by `init_pr_loop_test_env`, then individual modules create temporary `.humanize/pr-loop` state and mock command fixtures.
- gates_or_invariants: `set -euo pipefail` makes sourced module failures fail fast unless modules deliberately capture exits (`tests/test-pr-loop.sh:13`). The test runner preserves separation of concerns: script argument validation in `test-pr-loop-scripts.sh`, hook access control in `test-pr-loop-hooks.sh`, and stop-hook transitions in `test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:5`).
- dependencies_and_callers: Sourced dependencies are `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:32`). Included in aggregate test execution via `tests/run-all-tests.sh` (`tests/run-all-tests.sh:53`).
- edge_cases_or_failure_modes: Because modules are sourced, global variables/functions can leak across modules; the suite mitigates through common setup helpers and per-test temp dirs. The runner itself performs no discovery or filtering, so a missing sourced module fails before any partial test result is meaningful.
- validation_or_tests: Delegated tests cover setup/cancel/fetch/poll argument validation (`tests/test-pr-loop-scripts.sh:405`), state/comment protection and resolve-file allowance (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`), fixture-backed comment ingestion (`tests/test-pr-loop-hooks.sh:1242`), and stop-hook cases such as stale trigger rejection and startup-case-1 trigger exemption (`tests/test-pr-loop-stophook.sh:20`, `tests/test-pr-loop-stophook.sh:140`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-096 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for state schema migration failures. It is a guard contract used when an active loop state file is missing a required field.
- algorithmic_behavior: Renders `{{FIELD_NAME}}` into a short user-facing block reason, explains that the session was started with an older humanize version, gives recovery options, and states that the session will terminate as `unexpected` to preserve state information (`prompt-template/block/schema-outdated.md:3`, `prompt-template/block/schema-outdated.md:7`, `prompt-template/block/schema-outdated.md:12`).
- inputs_outputs_state: Input variable is `FIELD_NAME`. Output is markdown reason text, normally JSON-escaped by the caller before being returned to a hook consumer. It does not mutate state, but it represents a state-transition decision: malformed/outdated schema should block/terminate rather than continuing with ambiguous state.
- gates_or_invariants: The template backs strict schema invariants. `parse_state_file_strict` rejects missing frontmatter and required fields including `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:267`, `hooks/lib/loop-common.sh:294`). `loop-plan-file-validator.sh` separately checks required `plan_tracked` and `start_branch` and renders this template when missing (`hooks/loop-plan-file-validator.sh:59`, `hooks/loop-plan-file-validator.sh:79`).
- dependencies_and_callers: Directly rendered by `hooks/loop-plan-file-validator.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/schema-outdated.md"` with a fallback message (`hooks/loop-plan-file-validator.sh:65`). Rendering uses the single-pass template loader, which avoids placeholder re-expansion in variable values (`hooks/lib/template-loader.sh:54`).
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to inline text (`hooks/lib/template-loader.sh:170`). The template says version `1.1.2+`, while nearby tests/comments refer to newer schema fields such as v1.5.0 required fields; that makes it a possible wording drift, not a behavior bug (`tests/test-plan-file-hooks.sh:149`).
- validation_or_tests: Plan-file hook tests assert malformed schema blocks (`tests/test-plan-file-hooks.sh:149`) and stop-hook schema errors return a JSON block mentioning schema/missing field/`plan_tracked` (`tests/test-plan-file-hooks.sh:673`). Robustness tests exercise strict parser missing-field rejection.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-126 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task contract used when fetched PR comments already exist. It instructs the agent how to process feedback, modify code, push, trigger review, and write the resolution summary.
- algorithmic_behavior: After `setup-pr-loop.sh` writes a header and appends fetched comments, it selects this template when `round-0-pr-comment.md` does not contain the no-comments sentinel (`scripts/setup-pr-loop.sh:723`, `scripts/setup-pr-loop.sh:766`, `scripts/setup-pr-loop.sh:847`). The template defines the transition from initial comment ingestion to implementation work: prioritize human comments, then bot comments; fix issues; commit and push; run `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review the latest changes"`; write summary to `@{{RESOLVE_PATH}}`; then let the stop hook poll and validate (`prompt-template/pr-loop/round-0-task-has-comments.md:4`, `prompt-template/pr-loop/round-0-task-has-comments.md:15`, `prompt-template/pr-loop/round-0-task-has-comments.md:23`, `prompt-template/pr-loop/round-0-task-has-comments.md:39`).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, plus the already-appended comment file content. Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` (`scripts/setup-pr-loop.sh:850`). State is not modified directly by the template, but the required summary file becomes the stop-hook readiness signal (`hooks/pr-loop-stop-hook.sh:16`, `hooks/pr-loop-stop-hook.sh:258`).
- gates_or_invariants: User-facing rules prohibit state-file modification, require pushing changes before bot review, require correct bot mention format, and require addressing valid reviewer concerns (`prompt-template/pr-loop/round-0-task-has-comments.md:30`). These align with hook protections that block writes to `.humanize/pr-loop/state.md` and fetched comment/prompt files while allowing `round-N-pr-resolve.md` writes (`tests/test-pr-loop-hooks.sh:68`, `tests/test-pr-loop-hooks.sh:102`, `tests/test-pr-loop-hooks.sh:135`).
- dependencies_and_callers: Rendered only by `scripts/setup-pr-loop.sh` through `load_and_render_safe`; fallback content in the script mirrors this template (`scripts/setup-pr-loop.sh:803`, `scripts/setup-pr-loop.sh:847`). It depends on `fetch-pr-comments.sh` output to populate the preceding comments and on `hooks/pr-loop-stop-hook.sh` to perform post-exit polling/local Codex validation (`hooks/pr-loop-stop-hook.sh:5`).
- edge_cases_or_failure_modes: If comments file contains any content other than the exact `*No comments found.*` sentinel, setup treats it as comments-present, even if the file only contains warnings or metadata (`scripts/setup-pr-loop.sh:723`). If template loading fails, fallback content preserves the same required behavior (`scripts/setup-pr-loop.sh:803`). The embedded `gh pr comment` command omits `--repo`; that is acceptable as a prompt for users in normal contexts, but fork PR support in scripts themselves uses explicit base repo resolution.
- validation_or_tests: Template reference tests search for safe rendering use, and PR-loop hook tests validate that resolve summaries are allowed while state/comment files remain protected. End-to-end behavior is covered by `tests/test-pr-loop.sh`, which runs PR-loop scripts/hooks/stop-hook modules (`tests/test-pr-loop.sh:40`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 item sections present; each assigned item appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`