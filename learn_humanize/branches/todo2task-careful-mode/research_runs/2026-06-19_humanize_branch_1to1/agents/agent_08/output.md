# agent_08 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: Public behavior contract for the Humanize/RLCR plugin. It defines the intended two-phase RLCR state machine and the PR review loop at `README.md:97-112`, command surfaces at `README.md:130-139`, and operational prerequisites at `README.md:212-215`.
- algorithmic_behavior: The RLCR loop is documented as Claude implementation followed by Codex summary review feedback until completion, then a `codex review --base <branch>` phase with severity markers (`README.md:99-112`). The PR loop is documented as PR detection, bot review collection, Claude fixes, push and re-trigger, stop-hook polling, local Codex validation, and repeat until approval or max iterations (`README.md:202-210`).
- inputs_outputs_state: Inputs include plan files or drafts (`README.md:115-122`), command options such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, and `--skip-impl` (`README.md:144-163`), plus PR bot flags `--claude|--codex` (`README.md:187-199`). Outputs/state are loop artifacts under `.humanize/rlcr/<timestamp>/` and monitorable PR/RLCR sessions (`README.md:123-128`, `README.md:217-220`).
- gates_or_invariants: Requires Codex CLI for review (`README.md:52-55`), GitHub CLI/auth and an open PR for PR loop (`README.md:212-215`), and warns that `HUMANIZE_CODEX_BYPASS_SANDBOX=true|1` switches Codex from sandboxed mode to unrestricted execution (`README.md:60-93`). Base branch auto-detection priority is remote default, then `main`, then `master` (`README.md:155-157`).
- dependencies_and_callers: This is documentation rather than called code; it maps to command markdown and scripts such as `commands/start-pr-loop.md`, `commands/cancel-pr-loop.md`, `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `hooks/pr-loop-stop-hook.sh`, and `scripts/humanize.sh` found by repository reference search. It also documents the monitor command path sourced from plugin cache at `README.md:123-127`.
- edge_cases_or_failure_modes: Security-sensitive sandbox bypass is explicitly called out as dangerous and unsuitable for shared/untrusted environments (`README.md:60-85`). `--skip-impl` allows the implementation phase to be skipped and makes the plan file optional (`README.md:160-161`). PR loop operation fails its prerequisites if `gh`, Codex, or an associated open PR are unavailable (`README.md:212-215`).
- validation_or_tests: README behavior is covered indirectly by PR loop and template tests: `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-system.sh`, `tests/test-pr-loop-hooks.sh`, and template tests exercise command/help, monitor state display, polling, and validation templates. No tests were executed in this read-only research pass.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-038 `file` `scripts/poll-pr-reviews.sh`
- cursor: `[_]`
- core_role: Runtime PR-loop polling adapter. It normalizes GitHub issue comments, inline review comments, and PR review submissions into one JSON stream for configured review bots (`scripts/poll-pr-reviews.sh:240-297`).
- algorithmic_behavior: Parses positional PR number plus `--after` and `--bots` (`scripts/poll-pr-reviews.sh:23-81`), validates required/numeric inputs (`scripts/poll-pr-reviews.sh:83-102`), resolves the correct base repository for fork PRs by trying current repo then parent repo (`scripts/poll-pr-reviews.sh:122-150`), maps friendly bot names to GitHub authors with Codex special-cased to `chatgpt-codex-connector[bot]` (`scripts/poll-pr-reviews.sh:164-187`), fetches three GitHub API endpoints, filters by timestamp and author regex, then emits structured JSON (`scripts/poll-pr-reviews.sh:299-326`).
- inputs_outputs_state: Inputs are `<pr_number>`, ISO-ish `--after` timestamp, comma-separated bot names, current GitHub repository context, `gh`, and `jq`. Output is a JSON object with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:317-326`). Temporary state is isolated under `mktemp -d` and cleaned by trap (`scripts/poll-pr-reviews.sh:193-199`).
- gates_or_invariants: Hard-fails if arguments are missing, PR number is not numeric, `gh` is absent, `jq` is absent, current repo cannot be resolved, PR cannot be found in current or parent repo, or owner/name parsing fails (`scripts/poll-pr-reviews.sh:83-158`). API fetches are retry-tolerant: `fetch_with_retry` tries three times, warns, then returns `[]` with success so the outer polling loop can continue (`scripts/poll-pr-reviews.sh:200-235`).
- dependencies_and_callers: `hooks/pr-loop-stop-hook.sh` assigns `POLL_SCRIPT` to this path (`hooks/pr-loop-stop-hook.sh:811`) and repeatedly invokes it with currently waiting bots (`hooks/pr-loop-stop-hook.sh:960-965`). The stop hook maps returned author names back to bot names, tracks per-bot responses, deduplicates comments by id, and accumulates results (`hooks/pr-loop-stop-hook.sh:967-1058`).
- edge_cases_or_failure_modes: Empty-body PR reviews are preserved by replacing empty/null body with a state placeholder while keeping `.state` (`scripts/poll-pr-reviews.sh:276-294`). Partial API outages yield empty arrays, not process failure (`scripts/poll-pr-reviews.sh:231-234`). Timestamp filtering is string comparison on `.created_at >= $after` (`scripts/poll-pr-reviews.sh:299-305`), so callers should pass normalized timestamps. Bot author regex only escapes square brackets, so unexpected regex metacharacters in bot-derived author names could overmatch.
- validation_or_tests: Help and missing argument behavior are checked in `tests/test-pr-loop-scripts.sh:341-390`. Fixture-backed tests validate approval-only review placeholders and `--after` filtering in `tests/test-pr-loop-hooks.sh:1322-1411`. Robustness tests require valid JSON with boolean `has_new_comments`, slow API tolerance, and API failure returning valid JSON with `has_new_comments:false` at `tests/robustness/test-pr-loop-api-robustness.sh:619-840`.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-068 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`; it defines expected template lookup, rendering, fallback, validation, literal escaping, and injection-prevention behavior.
- algorithmic_behavior: Sources the production loader (`tests/test-template-loader.sh:10-12`), tracks pass/fail counters (`tests/test-template-loader.sh:20-34`), then runs 41 checks. The first group verifies template dir discovery, existing/missing template load, single/multiple/multiline rendering, real template integration, unused variables, unreplaced placeholders, safe fallback behavior, and template directory validation (`tests/test-template-loader.sh:41-244`).
- inputs_outputs_state: Inputs are repository root, `hooks/lib/template-loader.sh`, `prompt-template` contents, inline template strings, and `VAR=value` arguments. Outputs are colored PASS/FAIL lines, pass/fail counts, and process exit `0` if no failures or `1` otherwise (`tests/test-template-loader.sh:641-659`). State is limited to shell variables `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: Defines the invariant that rendering is single-pass and literal: shell metacharacters such as `&`, backslashes, `$`, backticks, pipes, semicolons, globs, redirection, quotes, comments, tildes, regex, JSON, and multiline values must not be interpreted or corrupted (`tests/test-template-loader.sh:247-487`). Values containing `{{VAR}}` syntax must not trigger secondary replacement (`tests/test-template-loader.sh:488-543`).
- dependencies_and_callers: Depends directly on loader functions `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` from `hooks/lib/template-loader.sh`. It uses real templates such as `block/git-push.md` and `block/wrong-round-number.md` (`tests/test-template-loader.sh:59-64`, `tests/test-template-loader.sh:151-164`). `tests/run-all-tests.sh` includes this script in the broader test suite, per repository search.
- edge_cases_or_failure_modes: Missing templates must return empty or fallback depending on API (`tests/test-template-loader.sh:68-78`, `tests/test-template-loader.sh:197-222`). Empty variable values are legal (`tests/test-template-loader.sh:555-565`). Non-ASCII Unicode is intentionally represented through shell byte escapes in template and value tests (`tests/test-template-loader.sh:579-615`). Variable names with underscore prefix and numbers are accepted (`tests/test-template-loader.sh:617-639`).
- validation_or_tests: This file is itself validation. It is complemented by `tests/test-error-scenarios.sh`, `tests/test-templates-comprehensive.sh`, and robustness/stress template tests found by repository reference search. It was inspected but not executed in this read-only research pass.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-098 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Prompt block used as a validation error contract when a Bash command attempts to modify RLCR summary files.
- algorithmic_behavior: Renders a fixed markdown block: “Bash Write Blocked,” instructs the agent not to use Bash for summary files, points to `{{CORRECT_PATH}}`, and explains that `cat`, `echo`, `sed`, `awk`, etc. bypass validation hooks (`prompt-template/block/summary-bash-write.md:1-8`).
- inputs_outputs_state: Input is the template variable `CORRECT_PATH` (`prompt-template/block/summary-bash-write.md:5`). Output is the rendered block emitted to stderr by validators; it causes the attempted Bash write path to be blocked rather than mutating loop state.
- gates_or_invariants: Enforces that summary files are written through Write/Edit tooling so round-number and path validation hooks remain in force. This is part of the invariant that `round-*-summary.md` updates must target the current round summary file, not arbitrary paths.
- dependencies_and_callers: `hooks/lib/loop-common.sh` loads this template in `summary_bash_blocked_message` with a fallback and `CORRECT_PATH` substitution (`hooks/lib/loop-common.sh:487-496`). `hooks/loop-bash-validator.sh` calls that helper when `command_modifies_file` detects a shell modification to `round-[0-9]+-summary.md`, computes the current summary path, emits the block, and exits with code `2` (`hooks/loop-bash-validator.sh:370-378`).
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to an inline message (`hooks/lib/loop-common.sh:491-495`). Correct-path values containing shell-sensitive characters rely on `render_template` literal substitution, whose metacharacter and injection properties are specified in `tests/test-template-loader.sh:247-543`.
- validation_or_tests: Template reference coverage includes this file in `tests/test-template-references.sh` per repository search. Rendering and fallback behavior are covered by `tests/test-template-loader.sh`, especially the real-template integration and literal substitution cases.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-128 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for Humanize’s RLCR and PR loop terminal monitors. It is sourced by `scripts/humanize.sh` if present (`scripts/humanize.sh:6-10`) and supplies session discovery, state detection, PR phase detection, formatting, and tracker parsing.
- algorithmic_behavior: Provides cross-platform file sizing (`scripts/lib/monitor-common.sh:29-35`), latest timestamped session selection (`scripts/lib/monitor-common.sh:37-63`), terminal scroll-region setup/restore (`scripts/lib/monitor-common.sh:69-92`), status coloring (`scripts/lib/monitor-common.sh:128-141`), state file detection with active `state.md` priority over final `*-state.md` files (`scripts/lib/monitor-common.sh:147-188`), frontmatter value extraction (`scripts/lib/monitor-common.sh:194-209`), timestamp/string formatting (`scripts/lib/monitor-common.sh:215-252`), PR phase detection/display (`scripts/lib/monitor-common.sh:258-381`), RLCR-style goal tracker parsing (`scripts/lib/monitor-common.sh:387-463`), and PR goal tracker statistics (`scripts/lib/monitor-common.sh:465-498`).
- inputs_outputs_state: Inputs are `.humanize/...` loop directories, timestamp-named session directories, `state.md` or final state files, `round-*-pr-check.md`, and `goal-tracker.md`. Outputs are plain strings, pipe-delimited stats, ANSI color escape strings, and monitor phase labels. The only persistent side effect is phase-size cache writes under `/tmp/humanize-phase-<session>-<round>.size` for growth detection (`scripts/lib/monitor-common.sh:297-307`).
- gates_or_invariants: Latest-session discovery only accepts names matching `YYYY-MM-DD_HH-MM-SS` (`scripts/lib/monitor-common.sh:54-60`). PR final phase detection recognizes `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` before checking active analysis (`scripts/lib/monitor-common.sh:272-276`). Active Codex analysis is inferred from the highest round PR check file growing or being modified in the last 10 seconds (`scripts/lib/monitor-common.sh:277-318`). Round `0` plus `startup_case: 1` means waiting for initial review; otherwise active state means waiting for reviewer (`scripts/lib/monitor-common.sh:321-341`).
- dependencies_and_callers: `scripts/humanize.sh` wraps `monitor_find_latest_session` for RLCR monitor session selection (`scripts/humanize.sh:237-240`) and `monitor_find_state_file` for RLCR status bars (`scripts/humanize.sh:344-347`). The PR monitor uses `monitor_find_latest_session`, maps state names for display, calls `get_pr_loop_phase`/`get_pr_loop_phase_display`, and parses PR tracker stats via `humanize_parse_pr_goal_tracker` (`scripts/humanize.sh:979-1045`, `scripts/humanize.sh:1110-1160`, `scripts/humanize.sh:1248-1254`).
- edge_cases_or_failure_modes: Missing loop dirs or files return empty/unknown defaults rather than hard failure (`scripts/lib/monitor-common.sh:44-47`, `scripts/lib/monitor-common.sh:157-160`, `scripts/lib/monitor-common.sh:392-394`, `scripts/lib/monitor-common.sh:470-472`). Multiple final state files are resolved by first `find` result, not explicit priority beyond active `state.md`. `/tmp` cache keys use only session basename and round, so identical session names across projects can collide. YAML/frontmatter and markdown table parsing are regex-based and assume conventional headings/table shapes.
- validation_or_tests: Shared monitor tests cover phase detection, goal tracker parsing, PR tracker parsing, state file detection, latest session selection, file sizing, and Codex analyzing detection in `tests/test-pr-loop-system.sh:256-309`, `tests/test-pr-loop-system.sh:315-410`, `tests/test-pr-loop-system.sh:423-487`, and `tests/test-pr-loop-system.sh:770-895`. No tests were executed in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 Item Evidence headings present; each assigned item heading is unique.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`