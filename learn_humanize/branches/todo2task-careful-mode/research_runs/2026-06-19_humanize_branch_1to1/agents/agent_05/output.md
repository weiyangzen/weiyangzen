# agent_05 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: The `prompt-template/` tree is the prompt/state contract layer for the loop algorithms. It does not execute shell logic itself, but it defines the text emitted by setup scripts, stop hooks, validators, Codex review prompts, Claude continuation prompts, PR-loop startup prompts, goal-tracker schemas, and blocking messages. It is core because the shell algorithms delegate many transition instructions and guard messages to these templates.
- algorithmic_behavior: The directory is partitioned by workflow role. `prompt-template/block/` contains hook-block messages for invalid write/read/exit behavior, including state-file protection, prompt-file protection, wrong round/path protection, unpushed commits, missing work summary, large files, no trigger comment, stale schema, and bot timeout/failure messages. Examples: `prompt-template/block/pr-loop-state-modification.md:3` says `.humanize/pr-loop/state.md` is system-managed, and `prompt-template/block/wrong-round-number.md:3` explains the current-round invariant. `prompt-template/pr-loop/` defines PR review-loop startup and goal tracking: `goal-tracker-initial.md:15` starts the Issue Summary table, `round-0-header.md:3` declares Round 0, `round-0-task-no-comments.md:6` encodes the automatic first-review branch, and `round-0-task-has-comments.md:6` encodes the review-fix-push-retrigger branch. `prompt-template/claude/` defines Claude-side state transitions for implementation continuation, review remediation, finalize, skipped-review finalize, goal-tracker update requests, and push-every-round notices. `prompt-template/codex/` defines Codex-side review contracts: regular review requires full plan verification and only emits `COMPLETE` when all original tasks are done (`prompt-template/codex/regular-review.md:53`), full alignment review adds stagnation detection and `STOP` (`prompt-template/codex/full-alignment-review.md:84`), and the goal-tracker update section makes Codex responsible for approved tracker mutations (`prompt-template/codex/goal-tracker-update-section.md:3`). `prompt-template/plan/gen-plan-template.md` defines the plan-generation schema with acceptance criteria, path boundaries, sequencing, and code-style constraints.
- inputs_outputs_state: Inputs are `{{...}}` template variables supplied by shell scripts and hooks. The template loader documents uppercase placeholder syntax and single-pass substitution at `hooks/lib/template-loader.sh:7` and `hooks/lib/template-loader.sh:54`. Outputs are rendered Markdown prompts, state-blocking messages, PR-loop goal trackers, and audit prompt files. `scripts/setup-pr-loop.sh` renders `pr-loop/goal-tracker-initial.md` into `goal-tracker.md` at `scripts/setup-pr-loop.sh:712`, renders `pr-loop/round-0-header.md` into `round-0-prompt.md` at `scripts/setup-pr-loop.sh:758`, appends fetched comments at `scripts/setup-pr-loop.sh:764`, selects the no-comment or has-comment task template at `scripts/setup-pr-loop.sh:767`, and emits critical requirements at `scripts/setup-pr-loop.sh:914` or `scripts/setup-pr-loop.sh:940`.
- gates_or_invariants: The block templates encode invariants enforced by hooks: prompt files are instructions and cannot be modified (`prompt-template/block/prompt-file-write.md:3`), PR-loop generated prompt/comment files are read-only (`prompt-template/block/pr-loop-prompt-write.md:3`), loop state files are system-managed (`prompt-template/block/state-file-modification.md:3`), summaries must be in the loop directory (`prompt-template/block/wrong-summary-location.md:3`), users must not increment the round number (`prompt-template/block/wrong-round-number.md:7`), and goal-tracker mutation after Round 0 belongs to Codex (`prompt-template/block/goal-tracker-modification.md:3`). The Codex templates enforce no deferred/pending work before completion (`prompt-template/codex/regular-review.md:53` and `prompt-template/codex/full-alignment-review.md:91`). The PR-loop templates enforce branch-specific behavior: no trigger comment for fresh PRs (`prompt-template/pr-loop/critical-requirements-no-comments.md:17`) versus commit, push, and bot mention when comments already exist (`prompt-template/pr-loop/critical-requirements-has-comments.md:8`).
- dependencies_and_callers: `hooks/lib/template-loader.sh` is the rendering engine. `scripts/setup-pr-loop.sh` directly consumes the PR-loop templates and provides fallbacks if missing (`scripts/setup-pr-loop.sh:668`, `scripts/setup-pr-loop.sh:740`, `scripts/setup-pr-loop.sh:769`, `scripts/setup-pr-loop.sh:803`). Hook libraries consume block templates: `hooks/lib/loop-common.sh` references `block/prompt-file-write.md`, `block/state-file-modification.md`, `block/finalize-state-file-modification.md`, and `block/pr-loop-prompt-write.md`; validators reference `block/wrong-round-number.md`. `tests/test-template-references.sh` scans shell callers for template references and missing files (`tests/test-template-references.sh:83`) and separately checks common block templates (`tests/test-template-references.sh:152`). `tests/test-templates-comprehensive.sh` and `tests/test-template-loader.sh` validate loading/rendering behavior across the tree.
- edge_cases_or_failure_modes: Missing or empty templates are not fatal when callers use `load_and_render_safe`; fallback messages are rendered instead (`hooks/lib/template-loader.sh:170`). Single-pass substitution prevents injected `{{VAR}}` inside values from being expanded (`hooks/lib/template-loader.sh:54`). Directory validation only requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:216`), so `pr-loop` and `plan` completeness is validated by reference/comprehensive tests rather than that minimal directory check. Several block templates are policy messages only; their correctness depends on caller hooks selecting them at the correct state transition.
- validation_or_tests: `tests/test-template-references.sh` verifies referenced templates exist and critical validators use safe rendering (`tests/test-template-references.sh:172`). `tests/test-templates-comprehensive.sh` loads and renders all templates, including real examples such as `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md`. `tests/test-pr-loop-system.sh` validates `pr-loop/goal-tracker-initial.md` required sections at `tests/test-pr-loop-system.sh:642` and setup integration at `tests/test-pr-loop-system.sh:1453`. I inspected these tests but did not execute them because this run is research-only in a read-only branch export.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-035 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: This script is the PR-loop startup-case classifier. It reads GitHub PR commit/review/comment state and emits JSON that drives `setup-pr-loop.sh` and stop-hook trigger requirements.
- algorithmic_behavior: The script parses `<pr_number> --bots <bot1,bot2>` (`scripts/check-pr-reviewer-status.sh:49`), maps friendly bot names to GitHub authors (`codex` to `chatgpt-codex-connector[bot]`, others to `<bot>[bot]`) at `scripts/check-pr-reviewer-status.sh:94`, resolves the PR base repository for fork support by trying current repo then parent repo (`scripts/check-pr-reviewer-status.sh:113` to `scripts/check-pr-reviewer-status.sh:140`), fetches latest head commit SHA/date (`scripts/check-pr-reviewer-status.sh:142`), fetches issue comments, review comments, and PR review submissions (`scripts/check-pr-reviewer-status.sh:153` to `scripts/check-pr-reviewer-status.sh:165`), combines all comment sources (`scripts/check-pr-reviewer-status.sh:168`), classifies each configured bot as commented/missing/stale (`scripts/check-pr-reviewer-status.sh:179` to `scripts/check-pr-reviewer-status.sh:205`), and assigns startup case at `scripts/check-pr-reviewer-status.sh:220`.
- inputs_outputs_state: Inputs are CLI args, `gh` API responses, `jq`, and the configured bot list. Output is a JSON object with `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews` (`scripts/check-pr-reviewer-status.sh:259` to `scripts/check-pr-reviewer-status.sh:275`). It does not persist local state. Downstream, `scripts/setup-pr-loop.sh` invokes it at `scripts/setup-pr-loop.sh:438`, parses `STARTUP_CASE`, commit SHA/date, and `has_commits_after_reviews` at `scripts/setup-pr-loop.sh:444`, then writes those fields into `state.md` at `scripts/setup-pr-loop.sh:629` to `scripts/setup-pr-loop.sh:633`.
- gates_or_invariants: Argument validation requires one PR number and `--bots` (`scripts/check-pr-reviewer-status.sh:75` and `scripts/check-pr-reviewer-status.sh:81`). Each `gh` operation is bounded by `GH_TIMEOUT=60` via `portable-timeout.sh` (`scripts/check-pr-reviewer-status.sh:35` and `scripts/check-pr-reviewer-status.sh:40`). Case selection is per-bot stale aware, not only based on the newest global review: a bot is stale if latest commit time is lexically greater than that bot’s newest review time (`scripts/check-pr-reviewer-status.sh:194`). Cases: no comments -> 1, partial fresh -> 2, all fresh -> 3, all with any stale -> 4, partial with any stale -> 5 (`scripts/check-pr-reviewer-status.sh:220` to `scripts/check-pr-reviewer-status.sh:244`).
- dependencies_and_callers: Depends on `gh`, `jq`, Bash arrays, and `scripts/portable-timeout.sh`. `portable-timeout.sh` selects `gtimeout`, GNU `timeout`, Python, or no timeout (`scripts/portable-timeout.sh:6`). Primary caller is `scripts/setup-pr-loop.sh`; `hooks/pr-loop-stop-hook.sh` also re-evaluates startup case dynamically via this script near `hooks/pr-loop-stop-hook.sh:1556`. System tests call it directly across startup-case scenarios.
- edge_cases_or_failure_modes: If `gh repo view` cannot resolve the base repo, the script warns and falls back to current repo (`scripts/check-pr-reviewer-status.sh:137`). If commit info cannot be fetched, it exits nonzero (`scripts/check-pr-reviewer-status.sh:143` to `scripts/check-pr-reviewer-status.sh:148`). Comment API failures degrade to empty arrays (`scripts/check-pr-reviewer-status.sh:156`, `scripts/check-pr-reviewer-status.sh:159`, `scripts/check-pr-reviewer-status.sh:164`), which can bias toward case 1/2 rather than failing. Timestamp comparison is string comparison and assumes normalized ISO timestamps. Empty bot names from malformed comma lists are not explicitly filtered. PR review submissions are normalized with `submitted_at` as `created_at` (`scripts/check-pr-reviewer-status.sh:164`), so approval-only reviews count as bot feedback.
- validation_or_tests: `tests/test-pr-loop-system.sh` covers no comments -> case 1 (`tests/test-pr-loop-system.sh:152`), partial comments -> case 2 (`tests/test-pr-loop-system.sh:173`), PR review submissions counting as comments (`tests/test-pr-loop-system.sh:236`), all commented -> case 3 (`tests/test-pr-loop-system.sh:493`), all commented with newer commit -> case 4 (`tests/test-pr-loop-system.sh:1315`), and partial with newer commit -> case 5 (`tests/test-pr-loop-system.sh:1347`). `test_setup_case45_missing_trigger_comment_id` verifies that case 4/5 setup fails safely if the trigger comment id cannot be retrieved (`tests/test-pr-loop-system.sh:1384`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-065 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: This is the executable system specification for the PR-loop algorithm. It defines expected behavior for setup mutual exclusion, reviewer-status classification, bot reaction detection, monitor phase detection, goal-tracker parsing/update, startup-case trigger rules, setup failure cleanup, and stop-hook goal-tracker integration.
- algorithmic_behavior: The test runner creates isolated temp repos, prepends `tests/mocks` to `PATH`, points mocked `gh` at `tests/fixtures`, and resets state per test (`tests/test-pr-loop-system.sh:51` to `tests/test-pr-loop-system.sh:103`). It then executes named test groups through `run_test` and optional filter dispatch in `main` (`tests/test-pr-loop-system.sh:1761`). The file encodes the loop’s expected algorithmic transitions: RLCR and PR loops are mutually exclusive (`tests/test-pr-loop-system.sh:110` and `tests/test-pr-loop-system.sh:128`), reviewer status cases are derived from comment/review fixtures (`tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:173`, `tests/test-pr-loop-system.sh:493`, `tests/test-pr-loop-system.sh:1315`, `tests/test-pr-loop-system.sh:1347`), monitor phases derive from state files and file-growth signals (`tests/test-pr-loop-system.sh:256`, `tests/test-pr-loop-system.sh:271`, `tests/test-pr-loop-system.sh:291`, `tests/test-pr-loop-system.sh:833`), and goal tracker updates must be idempotent, table-correct, and able to repair partial updates (`tests/test-pr-loop-system.sh:681`, `tests/test-pr-loop-system.sh:718`, `tests/test-pr-loop-system.sh:1179`, `tests/test-pr-loop-system.sh:1236`).
- inputs_outputs_state: Inputs are test fixtures under `tests/fixtures`, mocked CLIs under `tests/mocks`, temporary `.humanize/pr-loop` and `.humanize/rlcr` session directories, generated `state.md`/terminal state files, and environment variables such as `MOCK_GH_LATEST_COMMIT_AT`, `MOCK_GH_PR_NUMBER`, and `CLAUDE_PROJECT_DIR`. Outputs are test pass/fail counters and mutation of temp files only. Some tests temporarily overwrite shared fixture JSON and then restore them, for example reviewer-status tests restore `issue-comments.json` and `pr-reviews.json` after assertions (`tests/test-pr-loop-system.sh:166` and `tests/test-pr-loop-system.sh:187`).
- gates_or_invariants: The test file asserts that state-file detection prioritizes active `state.md` and terminal `*-state.md` files (`tests/test-pr-loop-system.sh:431` and `tests/test-pr-loop-system.sh:775`), approved/cancelled/maxiter phases map to explicit phase labels, and `codex_analyzing` is detected by recent or growing `round-N-pr-check.md` files (`tests/test-pr-loop-system.sh:833`). It encodes trigger requirements: round 0 case 1/2/3 requires no trigger, case 4/5 requires one, and later rounds require one (`tests/test-pr-loop-system.sh:1113` to `tests/test-pr-loop-system.sh:1173`). It also gates setup failure: case 4/5 with Claude configured must fail when trigger comment id lookup fails and must clean up loop state (`tests/test-pr-loop-system.sh:1384` to `tests/test-pr-loop-system.sh:1448`).
- dependencies_and_callers: Depends on `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/lib/monitor-common.sh`, `scripts/humanize.sh`, `hooks/lib/loop-common.sh`, and `hooks/pr-loop-stop-hook.sh`. It also relies on `jq`, `git`, `timeout`, `date`, and platform-compatible `stat`/`touch` calls. The main test dispatch maps filters to groups at `tests/test-pr-loop-system.sh:1774` through `tests/test-pr-loop-system.sh:1886`.
- edge_cases_or_failure_modes: The tests explicitly cover missing PR review sources, PR review submissions without inline comments, stale reviews after new commits, partial reviewer coverage, setup cleanup on failed trigger-comment-id lookup, monitor output in noninteractive terminals, temporary cache cleanup for phase-size tracking, goal-tracker duplicate update prevention, and partial goal-tracker repair where either the summary row or log entry already exists. It also contains platform-aware date/stat fallbacks in the stop-hook integration test (`tests/test-pr-loop-system.sh:1549`).
- validation_or_tests: This file is itself validation. It reports aggregate counts and exits nonzero on any failed test (`tests/test-pr-loop-system.sh:1895` to `tests/test-pr-loop-system.sh:1901`). I inspected it directly but did not execute it because the assigned task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-095 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: This block template is a guard message for preventing an agent from modifying `round-*-prompt.md`, which are system-generated instruction files.
- algorithmic_behavior: The template states the blocked action (`prompt-template/block/prompt-file-write.md:3`), defines prompt files as instructions from Codex to Claude (`prompt-template/block/prompt-file-write.md:5`), and redirects the agent to the valid loop flow: read prompt, execute tasks, write results to summary (`prompt-template/block/prompt-file-write.md:7` to `prompt-template/block/prompt-file-write.md:10`). If the prompt is wrong, it requires documenting the issue in the summary rather than editing the prompt (`prompt-template/block/prompt-file-write.md:12`).
- inputs_outputs_state: Input is no dynamic variable in this template; callers render it directly or through safe fallback. Output is a Markdown denial/repair instruction shown by hook validation. It does not mutate state but protects state integrity by keeping generated prompt files immutable to the worker.
- gates_or_invariants: The invariant is that prompt files are read-only instructions and the only valid writable completion surface is the summary file. This prevents prompt tampering, round redefinition, and self-modification of instructions.
- dependencies_and_callers: `hooks/lib/loop-common.sh` loads it in the prompt-file block helper (`hooks/lib/loop-common.sh:466`). `tests/test-template-references.sh` treats it as a required common template (`tests/test-template-references.sh:154`). It is rendered by the shared template loader, whose safe loader fallback behavior is at `hooks/lib/template-loader.sh:170`.
- edge_cases_or_failure_modes: Because the template has no placeholders, rendering failures are limited to missing/empty file or template directory issues; safe callers fall back. It applies to generic RLCR `round-*-prompt.md`; PR-loop has a more specific sibling template, `prompt-template/block/pr-loop-prompt-write.md`, that also protects `round-*-pr-comment.md`.
- validation_or_tests: Covered by template existence/reference tests (`tests/test-template-references.sh:152`) and comprehensive load/render tests that load all templates. The system depends on validator tests for the specific blocked write path rather than this template alone.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-125 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: This is the fixed header for the initial PR review-loop prompt. It anchors Round 0 context before fetched PR comments and task-specific instructions are appended.
- algorithmic_behavior: The template instructs the agent to execute with high reasoning (`prompt-template/pr-loop/round-0-header.md:1`), declares the workflow as `PR Review Loop (Round 0)` (`prompt-template/pr-loop/round-0-header.md:3`), tells the agent it is monitoring remote review-bot feedback (`prompt-template/pr-loop/round-0-header.md:5`), injects PR number, branch, and active bots (`prompt-template/pr-loop/round-0-header.md:7` to `prompt-template/pr-loop/round-0-header.md:10`), and creates the `Review Comments` section that receives fetched PR feedback (`prompt-template/pr-loop/round-0-header.md:12` to `prompt-template/pr-loop/round-0-header.md:15`).
- inputs_outputs_state: Inputs are `PR_NUMBER`, `START_BRANCH`, and `ACTIVE_BOTS_DISPLAY`. `scripts/setup-pr-loop.sh` constructs `TEMPLATE_VARS` with those values plus downstream task variables at `scripts/setup-pr-loop.sh:731`, renders this template at `scripts/setup-pr-loop.sh:758`, writes the output to `round-0-prompt.md` at `scripts/setup-pr-loop.sh:761`, and appends fetched comments immediately after it at `scripts/setup-pr-loop.sh:764`.
- gates_or_invariants: The header must be generated before the comments and task body so the agent always sees PR identity, branch, and active bot scope before acting. It does not decide whether comments exist; setup performs that branch at `scripts/setup-pr-loop.sh:767` and appends either `round-0-task-no-comments.md` or `round-0-task-has-comments.md`.
- dependencies_and_callers: Primary caller is `scripts/setup-pr-loop.sh`. It is rendered via `load_and_render_safe`, so `scripts/setup-pr-loop.sh` has an inline fallback header at `scripts/setup-pr-loop.sh:740`. It coordinates with `fetch-pr-comments.sh` output, the PR-loop task templates, and the critical-requirements templates emitted after setup.
- edge_cases_or_failure_modes: Missing template falls back to the inline header. Missing variables remain as placeholders because template rendering is single-pass and leaves unresolved variables intact, making bad setup state visible in the generated prompt rather than silently blanking it.
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies setup integration creates a loop directory and `goal-tracker.md`; prompt header rendering is indirectly exercised by `setup-pr-loop.sh` integration. Template reference and comprehensive template tests cover existence/load/render of this template.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 assigned headings present once each
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`