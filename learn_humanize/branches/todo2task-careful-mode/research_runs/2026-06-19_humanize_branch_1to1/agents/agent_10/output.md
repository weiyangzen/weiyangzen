# agent_10 todo2task-careful-mode 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `7d9dd4fbb5c376ae0a72b7caf81c50909ff14c37`
- note: read-only research pass only; no repository files were modified. A `git status` probe was not usable in this sandbox because the export is not exposed as a normal writable git checkout, so evidence below is from direct file inspection.

## Item Evidence

### TODO2TASK_CAREFUL_MODE-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Runtime block-message template directory for RLCR and PR-loop gates. It is a flat directory of 32 markdown fragments totaling 14,184 bytes, used to turn detected invalid states into concrete instructions for the agent/user. Examples include git cleanliness, unpushed commits, round-file misuse, state-file protection, prompt-file protection, PR trigger/force-push handling, Claude eyes timeout, and goal-tracker protections.
- algorithmic_behavior: The directory itself is data, but it drives control flow through `hooks/lib/template-loader.sh`: `load_template` reads fragments, `render_template` performs single-pass `{{VAR}}` substitution, and `load_and_render_safe` falls back to inline messages when a fragment is missing or empty (`hooks/lib/template-loader.sh:36`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:170`). Hook call sites render block templates when deciding to block a tool/action, for example state/prompt/todos messages in `hooks/lib/loop-common.sh:449-507`, PR-loop state/prompt protections in `hooks/lib/loop-common.sh:895-912`, and `.humanize` git-add protection in `hooks/lib/loop-common.sh:1071`.
- inputs_outputs_state: Inputs are template variables supplied by validators and stop hooks, such as `CURRENT_ROUND`, `CORRECT_PATH`, `AHEAD_COUNT`, `CURRENT_BRANCH`, `BOT_MENTION_STRING`, `OLD_COMMIT`, and `NEW_COMMIT`. Outputs are markdown reason strings, usually embedded into hook JSON responses or printed to stderr/stdout by gate scripts. The templates do not mutate state directly; they explain and enforce state transitions by blocking stop/exit, file writes, reads, bash commands, pushes, or PR-loop progression until the caller’s gate condition is satisfied.
- gates_or_invariants: Template placeholders are expected to be uppercase `{{VAR_NAME}}` form. The comprehensive template test scans for malformed extra braces, wrong bracket types, spaces inside placeholders, and lowercase placeholders (`tests/test-templates-comprehensive.sh:118-194`). `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:208-221`). Missing block templates should not produce empty messages because critical render paths use `load_and_render_safe`, a property also checked by `tests/test-template-references.sh:172-200`.
- dependencies_and_callers: Primary dependency is `hooks/lib/template-loader.sh`. Runtime callers include `hooks/loop-codex-stop-hook.sh` for git/summary/todo/large-file/review failure blocks, `hooks/pr-loop-stop-hook.sh` for PR unpushed/force-push/no-trigger/Claude-eyes gates, read/write/edit/bash validators for round and path protections, and `hooks/lib/loop-common.sh` for shared message helpers. `tests/test-template-references.sh:56-64` scans key hook scripts for template references, and `tests/test-templates-comprehensive.sh:90-107` loads every `.md` template.
- edge_cases_or_failure_modes: Missing or empty templates fall back only when the caller uses `load_and_render_safe`; direct `load_template` uses can return empty content. Missing variables remain unreplaced by design (`hooks/lib/template-loader.sh:120-121`), so a caller that forgets a variable can leak `{{VAR}}` into the block message. The stress test warns, rather than fails, on unreplaced placeholders during all-template rendering (`tests/test-templates-comprehensive.sh:590-597`). Notes templates such as `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md` are loaded as supplemental fragments, not standalone blockers.
- validation_or_tests: `tests/test-templates-comprehensive.sh` validates directory structure, loadability, syntax, fallback rendering, real block rendering, and stress rendering. `tests/test-template-references.sh` verifies that referenced templates exist and critical validators use safe rendering. The assigned `unpushed-commits.md` template is explicitly rendered in the comprehensive test (`tests/test-templates-comprehensive.sh:517-527`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap/runtime initializer for `/humanize:start-pr-loop`. It creates the `.humanize/pr-loop/<timestamp>/` state surface for a PR review loop, discovers the PR context, configures monitored bots, fetches initial comments, optionally triggers bot re-review, verifies Claude acknowledgement, writes state, goal tracker, and round-0 prompt files, then prints the initial agent instructions.
- algorithmic_behavior: Defaults are defined for Codex model/effort/timeout, max iterations, polling, git timeout, and gh timeout (`scripts/setup-pr-loop.sh:17-31`). Argument parsing accepts `--claude`, `--codex`, `--max`, `--codex-model`, and `--codex-timeout`, rejecting unknown or malformed values (`scripts/setup-pr-loop.sh:110-173`). At least one bot must be selected, then the script builds `ACTIVE_BOTS_ARRAY` (`scripts/setup-pr-loop.sh:179-202`). It sources timeout, template, and loop-common helpers (`scripts/setup-pr-loop.sh:33-42`, `scripts/setup-pr-loop.sh:208-214`).
- inputs_outputs_state: Inputs include CLI flags, `CLAUDE_PROJECT_DIR`, optional `TEMPLATE_DIR`, current git branch, `gh` authentication/API data, `jq`, `codex`, and helper scripts. Outputs include `.humanize/pr-loop/<timestamp>/state.md`, `round-0-pr-comment.md`, `goal-tracker.md`, `round-0-prompt.md`, and a planned `round-0-pr-resolve.md` path (`scripts/setup-pr-loop.sh:413-431`, `scripts/setup-pr-loop.sh:615-635`, `scripts/setup-pr-loop.sh:641-713`, `scripts/setup-pr-loop.sh:721-851`). It may also post a PR trigger comment for startup cases 4 or 5 (`scripts/setup-pr-loop.sh:525-538`).
- gates_or_invariants: The script enforces single active loop by checking both RLCR and PR-loop state directories (`scripts/setup-pr-loop.sh:220-241`). It requires a git repo with at least one commit, installed/authenticated `gh`, and installed `codex` (`scripts/setup-pr-loop.sh:243-277`). It validates PR discovery, numeric PR number, and non-merged/non-closed PR state (`scripts/setup-pr-loop.sh:351-378`). It rejects YAML-unsafe branch/model/effort values before writing `state.md` (`scripts/setup-pr-loop.sh:384-407`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh`. The state it writes is consumed later by PR-loop hooks and cancellation tooling. Template rendering uses `pr-loop/goal-tracker-initial.md`, `pr-loop/round-0-header.md`, `pr-loop/round-0-task-no-comments.md`, `pr-loop/round-0-task-has-comments.md`, and critical-requirement templates, each with inline fallbacks.
- edge_cases_or_failure_modes: Fork PRs are handled by resolving the PR lookup repo from the PR URL and falling back to the parent repo with `owner:branch` syntax (`scripts/setup-pr-loop.sh:290-348`). Startup cases 4/5 avoid duplicate bot mentions by searching comments after the latest commit, stripping code blocks, inline code, indented code, and quotes before mention matching (`scripts/setup-pr-loop.sh:462-503`). Trigger post failure, missing trigger comment ID, or failed Claude eyes verification cleans up the loop dir before exit (`scripts/setup-pr-loop.sh:525-538`, `scripts/setup-pr-loop.sh:568-582`, `scripts/setup-pr-loop.sh:585-598`). It intentionally avoids local-time fallback for trigger timestamps to prevent clock-skew filtering (`scripts/setup-pr-loop.sh:558-561`). Output SIGPIPE is trapped after all important writes (`scripts/setup-pr-loop.sh:857-858`).
- validation_or_tests: The script contains its own validation gates for arguments, prerequisites, PR state, YAML safety, trigger reuse, and Claude eyes confirmation. Template dependencies used by this setup flow are covered by the template loader/comprehensive tests. I did not execute this script because it requires live git/GitHub/Codex side effects and this task requested research only.
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template subsystem and `prompt-template/` tree. It verifies that templates exist, load, use valid placeholder syntax, render safely across edge cases, fall back correctly, and survive repeated rendering.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh`, computes `TEMPLATE_DIR`, and keeps explicit pass/fail/warning counters (`tests/test-templates-comprehensive.sh:15-59`). Section 1 requires the template root plus `block`, `codex`, and `claude` subdirectories (`tests/test-templates-comprehensive.sh:61-80`). Section 2 loads every markdown file under the template tree and fails empty load results (`tests/test-templates-comprehensive.sh:82-111`). Section 3 scans every template for malformed placeholder syntax (`tests/test-templates-comprehensive.sh:113-194`).
- inputs_outputs_state: Inputs are the repository’s `prompt-template/` directory and template-loader functions. Outputs are colored test logs, accumulated pass/fail/warning counts, and exit code 0 if no failures or 1 if any failure remains (`tests/test-templates-comprehensive.sh:608-625`). Temporary directories are created for malformed placeholder and incomplete directory checks and removed during normal flow (`tests/test-templates-comprehensive.sh:204-259`, `tests/test-templates-comprehensive.sh:481-490`).
- gates_or_invariants: Valid placeholders are uppercase double-brace tokens. The test rejects extra closing braces, extra opening braces, wrong bracket types, likely unclosed placeholders, spaces inside placeholders, and lowercase placeholders (`tests/test-templates-comprehensive.sh:128-175`). Rendering is specified as single-pass via the library, with tests for empty content, multiline preservation, regex-sensitive characters, quotes, markdown values, long values, duplicate variables, and adjacent variables (`tests/test-templates-comprehensive.sh:266-438`).
- dependencies_and_callers: Depends directly on `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` from `hooks/lib/template-loader.sh`. It also depends on POSIX shell tools such as `find`, `grep`, `sed`, `sort`, `wc`, and `mktemp`. It is intended for CI/CD according to its header (`tests/test-templates-comprehensive.sh:3-10`).
- edge_cases_or_failure_modes: The script uses `set -uo pipefail` rather than `set -e`, so individual failures accumulate instead of aborting early (`tests/test-templates-comprehensive.sh:13`). Some edge tests intentionally assert “no crash” rather than byte-perfect rendering, especially backslashes, quotes, CJK, markdown, and multiline values (`tests/test-templates-comprehensive.sh:329-402`). Unreplaced placeholders during the all-template stress pass are warnings, not failures (`tests/test-templates-comprehensive.sh:590-597`), so missing variable coverage can escape as a warning.
- validation_or_tests: Integration checks render real templates: `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` (`tests/test-templates-comprehensive.sh:493-540`). Stress checks include 100 rapid `render_template` calls and rendering every template with generated dummy placeholder values (`tests/test-templates-comprehensive.sh:542-606`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Block template used when the loop cannot exit because local commits have not been pushed to the remote branch. It is a small but algorithmically active transition gate: it tells the agent that stop/exit is blocked until `git push origin {{CURRENT_BRANCH}}` is completed.
- algorithmic_behavior: The template renders `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into a markdown block headed “Unpushed Commits Detected” (`prompt-template/block/unpushed-commits.md:1-5`) and emits the exact push command in a fenced bash block (`prompt-template/block/unpushed-commits.md:7-10`). It closes by allowing another exit attempt only after all commits are pushed (`prompt-template/block/unpushed-commits.md:12`).
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH` from the caller. Output is a markdown reason string embedded into hook JSON. It does not mutate files itself; the state transition is external: local branch ahead of remote blocks exit, pushing commits resolves the gate.
- gates_or_invariants: In RLCR stop handling, this template is used only when `PUSH_EVERY_ROUND` is true and `git status -sb` reports an ahead count (`hooks/loop-codex-stop-hook.sh:574-603`). In PR-loop stop handling, pushing is always required so bots can review the latest commits; the hook computes ahead count through upstream, `origin/<branch>`, or PR head SHA fallback before rendering this template (`hooks/pr-loop-stop-hook.sh:312-360`).
- dependencies_and_callers: Rendered through `load_and_render_safe` from both `hooks/loop-codex-stop-hook.sh:590-592` and `hooks/pr-loop-stop-hook.sh:355-356`. It depends on template-loader single-pass substitution and on caller-provided branch/ahead values. The PR-loop caller has additional safety fallback when no upstream exists or PR head cannot be fetched (`hooks/pr-loop-stop-hook.sh:322-344`).
- edge_cases_or_failure_modes: If `CURRENT_BRANCH` is missing, caller fallbacks can render `unknown` in RLCR or `main` in PR-loop contexts. If variables are omitted, template-loader leaves placeholders intact by design. Branch names are interpolated into a displayed shell command, so branch-name safety depends on upstream validation and caller context rather than this template.
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this exact template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserts that the title, ahead count, and branch appear (`tests/test-templates-comprehensive.sh:517-527`).
- skip_candidate: `no`

### TODO2TASK_CAREFUL_MODE-HZ-130 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable security specification for cancel authorization. It verifies that cancellation is only authorized for a narrowly defined move from active loop state to `cancel-state.md`, guarded by a `.cancel-requested` signal file, and resistant to command/path bypasses.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` and test helpers, creates a temporary loop directory with `state.md`, then calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` across positive and negative command strings (`tests/robustness/test-cancel-security-robustness.sh:15-30`). Positive cases allow valid cancellation with the signal file, `finalize-state.md` source, single quotes, and unquoted paths (`tests/robustness/test-cancel-security-robustness.sh:39-89`).
- inputs_outputs_state: Inputs are synthetic command strings and temporary filesystem fixtures under `TEST_DIR`. Outputs are pass/fail counters and a final summary via `print_test_summary`, with the script exiting to that status (`tests/robustness/test-cancel-security-robustness.sh:407-412`). State changes are temporary: `.cancel-requested`, state/finalize files, symlinks, and regression directories are created and removed during the test.
- gates_or_invariants: Negative cases reject missing signal file, command substitution, backticks, semicolons, `&&`, pipes, wrong destination, wrong source, extra args, newline injection, variable expansion, non-`mv`, `||`, mixed quotes, `${IFS}`, multiple trailing spaces, nonstandard source file names, and symlink sources (`tests/robustness/test-cancel-security-robustness.sh:99-367`). A normalized `/./` path is accepted (`tests/robustness/test-cancel-security-robustness.sh:265-276`).
- dependencies_and_callers: The tested function is `is_cancel_authorized` in `hooks/lib/loop-common.sh:557-715`. That function requires `.cancel-requested`, rejects shell operators and remaining `$`, parses exactly two `mv` args, normalizes paths, validates source as `state.md` or `finalize-state.md`, validates destination as `cancel-state.md`, and rejects symlink sources. The operational producer of the legitimate cancel pattern is `scripts/cancel-pr-loop.sh`, which creates `.cancel-requested` and moves `state.md` to `cancel-state.md` (`scripts/cancel-pr-loop.sh:118-122`).
- edge_cases_or_failure_modes: The regression cases ensure a loop directory containing the substring `finalize` cannot trick symlink classification; exact path matching is required (`tests/robustness/test-cancel-security-robustness.sh:369-405`, `hooks/lib/loop-common.sh:702-710`). The test validates parser authorization only; it does not execute the command. It does not cover every possible shell quoting or path-with-space form, and command strings are lowercased before validation, matching the hook-side contract.
- validation_or_tests: This file is itself the robustness test. It uses `setup_test_dir`, which creates a `mktemp` directory and registers cleanup with a trap (`tests/test-helpers.sh:84-89`). I inspected it directly rather than executing it, because the assigned task requested research notes only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 item sections present once each
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`