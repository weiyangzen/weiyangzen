# agent_25 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-025 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable test aggregator for the Humanize plugin. It is a top-level validation harness that serially runs the branch’s shell test suites and converts each suite’s own `Passed:` / `Failed:` summary into a combined pass/fail result. It is core algorithm evidence because the branch behavior is specified through validator and hook tests, including the cancel-state transition suite at `tests/test-cancel-signal-file.sh`.
- algorithmic_behavior: The script initializes aggregate counters and an ordered `TEST_SUITES` array at `tests/run-all-tests.sh:28-46`, then iterates the suites in order at `tests/run-all-tests.sh:48-83`. For each existing suite, it runs the executable shell script, captures combined stdout/stderr, captures the suite exit code, strips ANSI color escapes, extracts the last `Passed:` and `Failed:` numeric values, adds them to global totals, and records any failed suite. Missing suite files are skipped rather than treated as failures at `tests/run-all-tests.sh:51-54`.
- inputs_outputs_state: Inputs are the fixed suite list in `TEST_SUITES`, the filesystem under `tests/`, and each suite’s stdout/stderr and exit code. Outputs are human-readable console sections, per-suite `PASSED` / `FAILED` / `SKIP` status lines, a final total summary, and process exit code `0` only when no suite is recorded as failed. Internal state is held in `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` at `tests/run-all-tests.sh:28-30`, then updated after each suite’s parsed counters at `tests/run-all-tests.sh:65-76`.
- gates_or_invariants: The success gate is strict at the suite level: a suite fails the aggregate if its process exit code is nonzero or its parsed `Failed:` count is greater than zero, even if other suites pass (`tests/run-all-tests.sh:74-79`). The final gate exits `1` when any failed suite exists and exits `0` only when `FAILED_SUITES` is empty (`tests/run-all-tests.sh:92-103`). The runner assumes every suite prints parsable `Passed:` and `Failed:` lines; if parsing finds no count, it defaults to `0` through `|| echo "0"` (`tests/run-all-tests.sh:68-69`).
- dependencies_and_callers: Depends on Bash, executable suite files, `sed`, `grep -E`, `tail`, ANSI-compatible terminal output, and the suite-local counting convention. The ANSI parsing contract is itself tested in `tests/test-ansi-parsing.sh`, which mirrors the same escape-stripping pattern at `tests/test-ansi-parsing.sh:38-48` and count extraction at `tests/test-ansi-parsing.sh:123-125`. The branch-specific cancel-state validator coverage is included explicitly as `test-cancel-signal-file.sh` in the suite list at `tests/run-all-tests.sh:45`; that suite tests the state-file `mv state.md cancel-state.md` allowance and blocking behavior.
- edge_cases_or_failure_modes: Missing test scripts are skipped, so a deleted suite can reduce coverage without failing the aggregate (`tests/run-all-tests.sh:51-54`). If a suite exits `0` but omits `Failed:`, the aggregate treats failures as `0`; if it omits `Passed:`, the suite can still pass with `0` counted tests. The runner only tails the last 30 lines of failed suite output, which can hide the original failing assertion in very long outputs (`tests/run-all-tests.sh:77-78`). It does not use `set -e` globally at startup, but temporarily enables `set -e` after each suite capture (`tests/run-all-tests.sh:60-63`); this means later unexpected command failures may terminate the runner even though initial mode is `set -uo pipefail`.
- validation_or_tests: The runner validates all listed suites, including template loading, bash validator patterns, todo checking, plan-file validation, template references, state exit naming, comprehensive template checks, hook behavior, error scenarios, ANSI parsing, allowlist validators, and cancel signal file behavior (`tests/run-all-tests.sh:33-46`). The cancel suite creates active loop directories with `state.md`, exercises the Bash validator, and checks that only an authorized cancel move is allowed when `.cancel-requested` exists, while other `state.md` writes remain blocked (`tests/test-cancel-signal-file.sh:42-57`, `tests/test-cancel-signal-file.sh:78-153`). I did not run the aggregate because this assignment is research-only in a read-only branch export.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-055 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Blocking message template used when hook validators reject direct modification of `state.md`. It is part of the algorithm’s guard surface: the hook logic decides whether a state-file operation is allowed, and this template provides the standardized rejection contract explaining that `state.md` is loop-managed.
- algorithmic_behavior: The template is static Markdown. It declares the block reason at `prompt-template/block/state-file-modification.md:1`, states that `state.md` cannot be modified because it is managed by the loop system at `prompt-template/block/state-file-modification.md:3`, lists the state fields protected by this gate at `prompt-template/block/state-file-modification.md:5-8`, and gives the invariant rationale at `prompt-template/block/state-file-modification.md:10`. The content has no placeholders, so rendering is effectively a direct load.
- inputs_outputs_state: Input is the template path `block/state-file-modification.md` passed through `load_and_render_safe`. Output is the rendered Markdown written to stderr by validators when blocking. It does not mutate state itself; it communicates why state mutation is forbidden. The protected loop state it references includes current round, max iterations, and Codex configuration (`prompt-template/block/state-file-modification.md:5-8`).
- gates_or_invariants: The invariant expressed by the template is that direct edits to `state.md` would corrupt loop state (`prompt-template/block/state-file-modification.md:10`). The behavioral gate is implemented in callers: `state_file_blocked_message()` loads this template with a fallback at `hooks/lib/loop-common.sh:208-215`; Edit blocks state file edits with exit `2` at `hooks/loop-edit-validator.sh:79-81`; Write blocks state file writes with exit `2` at `hooks/loop-write-validator.sh:95-97`; Bash blocks detected state modifications unless the cancel authorization exception passes at `hooks/loop-bash-validator.sh:81-88`.
- dependencies_and_callers: The template depends on the shared template loader. `get_template_dir()` resolves `prompt-template` from the hooks library path at `hooks/lib/template-loader.sh:24-31`, while `load_and_render_safe()` falls back if the template is missing or renders empty at `hooks/lib/template-loader.sh:167-203`. `hooks/lib/loop-common.sh` is the direct caller through `state_file_blocked_message()` at `hooks/lib/loop-common.sh:208-215`. Template presence is cross-checked by `tests/test-template-references.sh`, which includes `block/state-file-modification.md` in `COMMON_TEMPLATES` at `tests/test-template-references.sh:152-156`.
- edge_cases_or_failure_modes: If this file is missing or empty, `load_and_render_safe()` suppresses the load error and emits the inline fallback instead (`hooks/lib/template-loader.sh:176-186`, `hooks/lib/loop-common.sh:210-214`), so the guard can still block state modifications but with a shorter message. Since the template has no variables, placeholder injection is not relevant here. The Bash path has a deliberate exception: an authorized cancel command may move the active loop’s exact `state.md` to exact `cancel-state.md` only when `.cancel-requested` exists and the command passes the strict argument and injection checks in `is_cancel_authorized()` (`hooks/lib/loop-common.sh:261-388`). All other state-file modification attempts still use this block template.
- validation_or_tests: The template-loader behavior is covered by `tests/test-template-loader.sh`, including existing template loading at `tests/test-template-loader.sh:55-65` and missing-template empty behavior at `tests/test-template-loader.sh:68-78`. Reference existence for this exact block template is checked in `tests/test-template-references.sh:149-166`. The state-file blocking and cancel exception are tested through `tests/test-cancel-signal-file.sh`, including allowed cancel move with signal (`tests/test-cancel-signal-file.sh:78-91`), blocked move without signal (`tests/test-cancel-signal-file.sh:119-132`), blocked non-cancel writes even with signal (`tests/test-cancel-signal-file.sh:139-153`), and helper-level rejection of wrong commands, extra arguments, hidden variables, and missing signal (`tests/test-cancel-signal-file.sh:1257-1340`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence headings present; each assigned item appears once as a heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`