# agent_10 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-010 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing continuation prompt fragments for the RLCR loop. This directory defines what Claude receives after Codex finds issues and the stop hook blocks exit for the next round. It is a flat directory with five templates: `next-round-prompt.md`, `next-round-footer.md`, `post-alignment-action-items.md`, `push-every-round-note.md`, and `goal-tracker-update-request.md`.
- algorithmic_behavior: The main assembly happens in `hooks/loop-codex-stop-hook.sh:967-1033`. After Codex review output is not terminal, the hook increments `current_round` via temp-file plus `mv` at `hooks/loop-codex-stop-hook.sh:967-970`, creates `round-N-prompt.md` and `round-N-summary.md` paths at `hooks/loop-codex-stop-hook.sh:972-974`, then renders and appends these Claude templates. `next-round-prompt.md` is the base prompt: it tells Claude the work is unfinished, requires reading the original plan, requires todos for all discovered issues, embeds Codex review content, and makes the goal tracker read-only after Round 0 (`prompt-template/claude/next-round-prompt.md:1-31`). `next-round-footer.md` adds loop-integrity and completion instructions: do not exit by lying, editing state, or executing `cancel-rlcr-loop`; optionally use `code-simplifier`; commit; write the summary file (`prompt-template/claude/next-round-footer.md:4-9`). `post-alignment-action-items.md` is appended only after a full alignment check round, focusing the next round on forgotten items, failed acceptance criteria, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2-7`; caller condition at `hooks/loop-codex-stop-hook.sh:990-996`). `push-every-round-note.md` is appended only when `PUSH_EVERY_ROUND=true` and requires pushing commits to remote (`prompt-template/claude/push-every-round-note.md:2`; caller at `hooks/loop-codex-stop-hook.sh:1004-1010`). `goal-tracker-update-request.md` gives Claude a structured summary section to request goal-tracker changes, which Codex later evaluates (`prompt-template/claude/goal-tracker-update-request.md:2-16`; caller at `hooks/loop-codex-stop-hook.sh:1013-1018`).
- inputs_outputs_state: Inputs are template variables supplied by `load_and_render_safe`: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, and `NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:985-1002`). Conditional inputs/state include `CURRENT_ROUND % 5 == 4` for post-alignment prompt injection and `PUSH_EVERY_ROUND` for push instructions. Output is the rendered next-round prompt file at `.humanize/rlcr/<timestamp>/round-<NEXT_ROUND>-prompt.md`, which is returned to Claude as a blocking hook reason through JSON at `hooks/loop-codex-stop-hook.sh:1023-1031`. State transition is nonterminal review feedback: current round advances to next round, exit remains blocked, and Claude receives instructions for another implementation iteration.
- gates_or_invariants: The directory encodes several loop gates. Claude must read the original plan before continuing (`prompt-template/claude/next-round-prompt.md:3-8`). Claude must create todos for all discovered issues and is prohibited from only addressing the most important issues (`prompt-template/claude/next-round-prompt.md:12-14`). The goal tracker is read-only after Round 0, and updates must be requested rather than directly edited (`prompt-template/claude/next-round-prompt.md:22-31`; `prompt-template/claude/goal-tracker-update-request.md:16`). The footer’s strongest invariant is that Claude must not try to exit the loop by deception, state-file edits, or `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:4`). That line is relevant to this branch’s state-transition boundary because it treats state mutation and cancellation as guarded control paths, not ordinary implementation actions.
- dependencies_and_callers: Runtime dependency is `hooks/lib/template-loader.sh`, especially `load_template`, `render_template`, and `load_and_render_safe`. Rendering is single-pass and intentionally preserves unresolved placeholders to prevent placeholder injection from embedded review content (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:116-122`). Direct callers are in `hooks/loop-codex-stop-hook.sh:985-1018`. The goal-tracker request template coordinates with the Codex-side sibling `prompt-template/codex/goal-tracker-update-section.md:1-17`, which tells Codex to evaluate, approve or reject, and apply requested tracker updates while never modifying the immutable section.
- edge_cases_or_failure_modes: Missing or empty base/footer templates fall back through `load_and_render_safe`, so the loop can still produce a prompt (`hooks/loop-codex-stop-hook.sh:977-988`, `hooks/loop-codex-stop-hook.sh:998-1002`; fallback behavior at `hooks/lib/template-loader.sh:170-203`). Optional append templates use `load_template`; if `post-alignment-action-items.md` is missing, no post-alignment section is appended, and if `push-every-round-note.md` is missing, a short fallback push sentence is used (`hooks/loop-codex-stop-hook.sh:990-1010`). The footer’s blanket prohibition on `cancel-rlcr-loop` can conflict with other hook failure messages that present cancellation as an escape hatch, so the intended policy likely depends on context: ordinary next-round work should not self-cancel, while explicit human/admin cancellation may still be allowed elsewhere.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for template references and validates referenced template files exist (`tests/test-template-references.sh:51-110`). It also checks template directory completeness and warns for unreferenced templates (`tests/test-template-references.sh:117-142`). The test specifically covers safe rendering expectations for critical validators (`tests/test-template-references.sh:170-200`), while these Claude templates are covered by existence/reference scanning through the stop-hook references.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-040 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Intended block-message template for Codex review execution failure, especially the case where Codex does not produce the expected review result file. It would communicate exit code, missing result path, debug artifact paths, and stderr tail back to Claude or the operator (`prompt-template/block/codex-review-failed.md:1-18`).
- algorithmic_behavior: The template itself has no executable behavior, but it models a failed review gate: Codex review did not produce usable output, so the loop should block exit and attempt another review when Claude exits again (`prompt-template/block/codex-review-failed.md:3`, `prompt-template/block/codex-review-failed.md:18`). It expects a rendered block reason containing `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` (`prompt-template/block/codex-review-failed.md:5-15`).
- inputs_outputs_state: Inputs are placeholder values from the Codex invocation path: exit code, review result file path, debug command/stdout/stderr paths, and stderr tail. Intended output is a markdown failure report. The corresponding runtime state transition is nonterminal failure: do not mark the RLCR loop complete; block exit and let a later exit retry Codex review. The current runtime implements that transition inline in `hooks/loop-codex-stop-hook.sh` through `codex_failure_exit`, which emits a JSON block decision and exits successfully from the hook protocol (`hooks/loop-codex-stop-hook.sh:811-837`).
- gates_or_invariants: The gate is “Codex review must create a nonempty review result file.” Runtime checks include nonzero Codex exit code (`hooks/loop-codex-stop-hook.sh:839-856`), missing review file with optional stdout copy fallback (`hooks/loop-codex-stop-hook.sh:858-874`), still-missing review file (`hooks/loop-codex-stop-hook.sh:876-902`), and empty review file (`hooks/loop-codex-stop-hook.sh:904-910`). The template’s invariant is narrower than runtime: line 6 explicitly says the review result file was “not created,” while runtime distinguishes not created, copy failure, empty file, and nonzero exit.
- dependencies_and_callers: I found no direct caller for `prompt-template/block/codex-review-failed.md`. Repository search for its placeholder names and related text only found this template plus inline runtime variables/failure handling in `hooks/loop-codex-stop-hook.sh:750-910`. The active implementation builds the failure text inline at `hooks/loop-codex-stop-hook.sh:817-828`, rather than using `load_template` or `load_and_render_safe` for this file.
- edge_cases_or_failure_modes: Because it appears unreferenced, edits to this template would not currently affect the Codex failure path. It also says “Stderr (last 50 lines)” at `prompt-template/block/codex-review-failed.md:13`, while the active hook tails 30 lines for stderr/stdout in failure cases (`hooks/loop-codex-stop-hook.sh:843`, `hooks/loop-codex-stop-hook.sh:880`, `hooks/loop-codex-stop-hook.sh:885`). If this template were wired back in, that mismatch should be resolved. It also only represents a missing-result case and would need either generic wording or separate variants for nonzero exit, copy failure, and empty result.
- validation_or_tests: Template reference validation can detect missing referenced templates, but because this template is not directly referenced by a hook call, it would likely be reported only as “not directly referenced” by the completeness scan (`tests/test-template-references.sh:130-140`). There is no direct behavioral test proving this template is rendered in a Codex failure path.
- skip_candidate: `yes: current runtime appears to use inline Codex failure messages instead of this template, so the file is likely stale or reserved unless the branch intends to wire it into the stop hook`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-070 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex-facing mandatory full goal alignment checkpoint prompt. It is used every fifth implementation round to audit the original plan, goal tracker, Claude’s summary, implementation reality, deferred work, and stagnation risk before allowing the RLCR loop to either continue, stop by circuit breaker, or complete (`prompt-template/codex/full-alignment-review.md:1-93`).
- algorithmic_behavior: The stop hook determines full alignment rounds with `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:644-648`). On such rounds, it renders this template into `round-N-review-prompt.md` with plan, summary, tracker, docs path, round history, and output path variables (`hooks/loop-codex-stop-hook.sh:679-692`). The template forces Codex to read the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), audit each acceptance criterion in the immutable section (`prompt-template/codex/full-alignment-review.md:19-29`), detect forgotten plan items (`prompt-template/codex/full-alignment-review.md:30-34`), reassess deferred items (`prompt-template/codex/full-alignment-review.md:36-40`), summarize completion status (`prompt-template/codex/full-alignment-review.md:42-48`), conduct implementation review (`prompt-template/codex/full-alignment-review.md:50-55`), handle goal tracker updates through an injected section (`prompt-template/codex/full-alignment-review.md:57`), and inspect recent history for stagnation (`prompt-template/codex/full-alignment-review.md:59-85`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE` from `hooks/loop-codex-stop-hook.sh:681-692`. Output must be written to `@{{REVIEW_RESULT_FILE}}` (`prompt-template/codex/full-alignment-review.md:84`, `prompt-template/codex/full-alignment-review.md:88`). Runtime then reads that file and interprets the last nonempty line: `COMPLETE` ends the loop successfully, `STOP` triggers the stagnation circuit breaker, and any other review content causes the hook to create the next Claude round prompt (`hooks/loop-codex-stop-hook.sh:913-960`, `hooks/loop-codex-stop-hook.sh:963-1033`).
- gates_or_invariants: This is the strictest review gate in the prompt set. It says the checkpoint is mandatory every five rounds (`prompt-template/codex/full-alignment-review.md:3`). It requires all acceptance criteria from the original plan to be fully met before completion (`prompt-template/codex/full-alignment-review.md:91-93`). Deferred items are explicitly incomplete and must prevent `COMPLETE` (`prompt-template/codex/full-alignment-review.md:90-93`). Stagnation signs include repeated issues, no meaningful AC progress, repeated Claude mistakes, circular discussions, no new code changes, and repeated Codex feedback (`prompt-template/codex/full-alignment-review.md:76-83`). If stagnation is found, the last line must be `STOP`, not `COMPLETE` (`prompt-template/codex/full-alignment-review.md:84-85`, `prompt-template/codex/full-alignment-review.md:90`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:681`. It depends on `hooks/lib/template-loader.sh` for single-pass placeholder rendering, which prevents embedded content such as Claude’s summary from causing accidental second-pass placeholder substitution (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:71-129`). It also depends on the injected Codex-side goal tracker update section from `prompt-template/codex/goal-tracker-update-section.md:1-17`, constructed in `hooks/loop-codex-stop-hook.sh:638-642`. Runtime terminal behavior depends on marker parsing in `hooks/loop-codex-stop-hook.sh:916-960`.
- edge_cases_or_failure_modes: The first full alignment check occurs at zero-based round 4 because the hook uses `CURRENT_ROUND % 5 == 4`; that matches “every 5 rounds” but can be surprising if someone expects round 5 by label. Historical references use `PREV_ROUND` and `PREV_PREV_ROUND` (`prompt-template/codex/full-alignment-review.md:69-72`); because the template is first used at round 4, those values should be valid in normal operation. If Codex finds issues but forgets a terminal marker, runtime still continues the loop because only exact last-line `COMPLETE` or `STOP` has terminal meaning. If Codex writes `STOP` during a nonalignment round, the hook still honors it as an “unexpected circuit breaker,” but this template is only selected for full alignment rounds (`hooks/loop-codex-stop-hook.sh:933-960`).
- validation_or_tests: Template existence and references are covered by `tests/test-template-references.sh`, which scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls and fails when referenced templates are missing (`tests/test-template-references.sh:51-110`). Safe fallback behavior exists in `load_and_render_safe`, but this file is present and directly referenced. Runtime validation of review output is in the stop hook: nonzero Codex exit, missing result, empty result, and final marker parsing (`hooks/loop-codex-stop-hook.sh:839-920`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 Item Evidence sections present; assigned item ids are used only as section headings`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`