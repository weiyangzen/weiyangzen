# agent_06 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification for the RLCR loop control plane. The directory verifies state-file lifecycle, cancel authorization, hook access gates, template rendering, plan-file immutability, TodoWrite completion enforcement, and aggregate test reporting. `tests/run-all-tests.sh:32-46` is the suite coordinator and defines the intended execution order.
- algorithmic_behavior: The directory is organized as black-box and helper-level tests for sibling implementation paths under `hooks/`, `hooks/lib/`, `scripts/`, `prompt-template/`, and `commands/`. `tests/test-cancel-signal-file.sh:75-113` proves the positive cancel path: with `.cancel-requested`, `mv <loop>/state.md <loop>/cancel-state.md` is allowed. Its negative cases prove the branch invariant that every other `state.md` mutation remains blocked, including no signal, wrong destination, wrong signal directory, shell wrappers, chained commands, redirection disguise, unrelated `/tmp/state.md`, and extra-argument cases (`tests/test-cancel-signal-file.sh:115-215`, `tests/test-cancel-signal-file.sh:1206-1247`, `tests/test-cancel-signal-file.sh:1250-1341`). `tests/test-state-exit-naming.sh:38-76` and `tests/test-state-exit-naming.sh:93-155` define active-loop detection: only `state.md` marks active, while `cancel-state.md`, `complete-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` are terminal states. `tests/test-state-exit-naming.sh:202-225` verifies `end_loop` creates the valid terminal names.
- inputs_outputs_state: Test inputs are synthetic Claude hook JSON, temporary `.humanize/rlcr/<timestamp>/` loop directories, mock `state.md` frontmatter, temporary Git repos, transcripts, templates, and shell commands. Outputs are pass/fail counters and process exit codes; the aggregate runner strips ANSI and accumulates counts from child output (`tests/run-all-tests.sh:65-80`). State transitions under test include active `state.md` to terminal `*-state.md`, current-round gating, plan backup preservation, and TodoWrite completion blocking.
- gates_or_invariants: The directory collectively encodes these gates: only newest `.humanize/rlcr/*/state.md` is active (`tests/test-state-exit-naming.sh:157-175`); legacy `.humanize-loop.local` is ignored (`tests/test-state-exit-naming.sh:245-287`); shell writes to protected files are detected (`tests/test-bash-validator-patterns.sh:69-183`); known limitation is documented for multi-source `cp` detection (`tests/test-bash-validator-patterns.sh:165-168`); allowlisted historical files are narrowly scoped and path-restricted (`tests/test-allowlist-validators.sh:64-126`, `tests/test-allowlist-validators.sh:285-379`); plan files must be relative, inside project, non-symlink, sufficiently substantive, YAML-safe, and tracked/ignored according to startup mode (`tests/test-plan-file-validation.sh:89-202`, `tests/test-plan-file-validation.sh:326-422`, `tests/test-plan-file-validation.sh:429-740`); stop hooks must reject stale schema, branch changes, plan mutation/deletion, and placeholder goal-tracker sections (`tests/test-plan-file-hooks.sh:88-186`, `tests/test-plan-file-hooks.sh:466-717`, `tests/test-plan-file-hooks.sh:734-1000`).
- dependencies_and_callers: `tests/run-all-tests.sh` calls child suites directly. Suites source `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`, execute `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/check-todos-from-transcript.py`, and `scripts/setup-rlcr-loop.sh`. The core implementation being protected includes `is_cancel_authorized` (`hooks/lib/loop-common.sh:251-389`), `command_modifies_file` (`hooks/lib/loop-common.sh:397-425`), `end_loop` (`hooks/lib/loop-common.sh:441-473`), and bash-validator state mutation checks (`hooks/loop-bash-validator.sh:81-241`).
- edge_cases_or_failure_modes: The directory explicitly covers malformed JSON and missing transcripts (`tests/test-todo-checker.sh:45-97`), latest TodoWrite wins and missing status is incomplete (`tests/test-todo-checker.sh:196-263`), ANSI stripping and count extraction (`tests/test-ansi-parsing.sh:38-154`), missing templates and fallback behavior under strict shell mode (`tests/test-error-scenarios.sh:42-158`), empty/unclosed placeholders (`tests/test-error-scenarios.sh:182-205`), shell metacharacters and placeholder injection in template values (`tests/test-template-loader.sh:247-543`), and bypass attempts using command substitution, globbing, braces, pipes, and backticks against plan backup protection (`tests/test-plan-file-hooks.sh:269-331`).
- validation_or_tests: Child roles inspected recursively: `test-template-loader.sh` validates loader primitives and safe single-pass substitution; `test-templates-comprehensive.sh` validates all template files and rendering stress behavior; `test-template-references.sh` scans hook scripts for referenced template existence and safe fallback usage (`tests/test-template-references.sh:51-111`, `tests/test-template-references.sh:172-201`); `test-bash-validator-patterns.sh` validates modification-detection regexes; `test-cancel-signal-file.sh` is the focused branch regression suite; `test-state-exit-naming.sh` validates lifecycle filenames; `test-allowlist-validators.sh` validates read/write/edit/bash allowlists; `test-plan-file-validation.sh` validates setup input gates; `test-plan-file-hooks.sh` validates runtime plan and state hooks; `test-todo-checker.sh` validates transcript TodoWrite parsing; `test-error-scenarios.sh` validates graceful template failures; `test-ansi-parsing.sh` validates runner parsing. I did not execute the suite in this research pass; evidence is from direct file inspection.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-036 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: CI-style executable specification for the prompt-template subsystem. It verifies that every Markdown template under `prompt-template/` exists, loads, has acceptable placeholder syntax, renders safely, and can be stress-rendered with dummy placeholder values.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` (`tests/test-templates-comprehensive.sh:15-17`) and derives the template root with `get_template_dir` (`tests/test-templates-comprehensive.sh:59`). It validates required subdirectories `block`, `codex`, and `claude` (`tests/test-templates-comprehensive.sh:64-80`), loads every `*.md` file using `find ... -print0 | sort -z` (`tests/test-templates-comprehensive.sh:90-107`), and then scans every template for malformed placeholder patterns: extra closing braces, extra opening braces, wrong bracket types, unclosed placeholders, spaces inside placeholders, and lowercase placeholder names (`tests/test-templates-comprehensive.sh:118-194`).
- inputs_outputs_state: Inputs are the repo’s real `prompt-template/**/*.md` files plus synthetic template strings used for edge cases. Outputs are colored pass/fail/warn lines, aggregate counters `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, and final exit code `0` when no failures occur or `1` otherwise (`tests/test-templates-comprehensive.sh:608-625`). Temporary state is limited to `mktemp -d` for malformed-placeholder checks (`tests/test-templates-comprehensive.sh:204-259`) and temporary incomplete-directory validation (`tests/test-templates-comprehensive.sh:481-490`).
- gates_or_invariants: Template directory must exist or the suite aborts (`tests/test-templates-comprehensive.sh:66-72`). All templates must load as non-empty content or fail/warn depending on empty-vs-whitespace behavior (`tests/test-templates-comprehensive.sh:94-106`). Placeholder syntax must use uppercase-style `{{VAR_NAME}}` forms (`tests/test-templates-comprehensive.sh:122-175`). Rendering must preserve empty lines, multiline content, long values, adjacent placeholders, repeated placeholders, and variables at line boundaries (`tests/test-templates-comprehensive.sh:266-439`). Safe fallback behavior is required for missing templates and incomplete template directories (`tests/test-templates-comprehensive.sh:440-490`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, especially `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` (`hooks/lib/template-loader.sh:33-48`, `hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:167-222`). It is invoked by the aggregate runner as the seventh suite (`tests/run-all-tests.sh:32-46`). It also indirectly protects `prompt-template/claude/next-round-footer.md` because it loads and renders all template files (`tests/test-templates-comprehensive.sh:563-606`).
- edge_cases_or_failure_modes: Covers malformed placeholders with direct regex probes (`tests/test-templates-comprehensive.sh:199-259`), special regex characters and shell-like content in values (`tests/test-templates-comprehensive.sh:318-345`), long values over 1000 characters (`tests/test-templates-comprehensive.sh:382-390`), multiline values without crashing (`tests/test-templates-comprehensive.sh:392-402`), and unreplaced placeholders after dummy rendering (`tests/test-templates-comprehensive.sh:575-597`). One residual weakness: the “CJK characters” block mostly checks ASCII text and the `CJK:` label rather than asserting a specific non-ASCII payload (`tests/test-templates-comprehensive.sh:347-360`).
- validation_or_tests: Real-template integration checks render `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` with expected substitutions (`tests/test-templates-comprehensive.sh:493-540`). Stress coverage runs 100 rapid render calls and then attempts to load/render every template with dummy values for every detected placeholder (`tests/test-templates-comprehensive.sh:543-606`).
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-066 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Claude-facing transition footer appended to the next-round prompt when Codex blocks loop exit and sends Claude into another RLCR round. It is small, but algorithmically relevant because it carries the prompt-level invariant that Claude must not self-terminate the loop by deception, direct state edits, or running the cancel command.
- algorithmic_behavior: The template begins with a Markdown separator and then states the central gate: Claude must not try to exit by lying, editing the loop state file, or executing `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:2-4`). It then instructs post-work actions: optionally run the installed `code-simplifier`, commit changes, and write the work summary to `@{{NEXT_SUMMARY_FILE}}` (`prompt-template/claude/next-round-footer.md:6-9`). The placeholder is rendered by `load_and_render_safe` when `hooks/loop-codex-stop-hook.sh` creates the next prompt (`hooks/loop-codex-stop-hook.sh:998-1002`).
- inputs_outputs_state: Input is `NEXT_SUMMARY_FILE`, computed as `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` (`hooks/loop-codex-stop-hook.sh:972-974`). Output is appended Markdown inside `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md`. The surrounding state transition increments `current_round` in `state.md` and writes it via temp file then `mv` (`hooks/loop-codex-stop-hook.sh:967-970`), then creates the next prompt (`hooks/loop-codex-stop-hook.sh:972-989`), appends this footer (`hooks/loop-codex-stop-hook.sh:998-1002`), optionally appends push instructions (`hooks/loop-codex-stop-hook.sh:1004-1010`), then returns a JSON block decision containing the prompt (`hooks/loop-codex-stop-hook.sh:1020-1030`).
- gates_or_invariants: This template is the soft prompt-layer counterpart to hard hook gates. Hard enforcement lives in `loop-bash-validator.sh`, which blocks `state.md` mutation except when `is_cancel_authorized` approves the exact signal-file-backed cancel move (`hooks/loop-bash-validator.sh:81-89`, `hooks/loop-bash-validator.sh:91-241`). The authorized cancel flow is deliberately outside normal Claude work: `/cancel-rlcr-loop` first touches `.cancel-requested`, then renames `state.md` to `cancel-state.md` (`commands/cancel-rlcr-loop.md:28-32`). The footer tells Claude not to invoke that path as a way to escape work.
- dependencies_and_callers: Called only through the stop hook’s next-round prompt construction (`hooks/loop-codex-stop-hook.sh:998-1002`). Rendering depends on the single-pass template loader behavior in `hooks/lib/template-loader.sh:50-132`; missing-template fallback is handled by `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`). Its placeholder syntax is compatible with the uppercase placeholder convention.
- edge_cases_or_failure_modes: If the file is missing or renders empty, the stop hook falls back to a shorter message that only says to commit and write the summary (`hooks/loop-codex-stop-hook.sh:998-1002`), losing the explicit anti-cancel and anti-state-edit wording. The template includes an optional external instruction for `code-simplifier`; no hook enforces that action. The summary target is a rendered path, so an incorrect `NEXT_SUMMARY_FILE` value from the caller would be faithfully embedded.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads all templates (`tests/test-templates-comprehensive.sh:90-107`), validates syntax for all placeholders (`tests/test-templates-comprehensive.sh:118-194`), and renders all templates with dummy placeholder assignments (`tests/test-templates-comprehensive.sh:563-606`). `tests/test-template-references.sh` scans hook references and verifies referenced templates exist (`tests/test-template-references.sh:51-111`). There is no dedicated semantic assertion that line 4’s anti-cancel wording remains present, so the current protection is partly by general template existence/render tests and partly by the hard bash-validator cancel-state gates.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 item headings present, one per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`