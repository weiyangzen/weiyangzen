# agent_13 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-013 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting an RLCR loop. The file is a thin command wrapper: its front matter exposes `/humanize:start-rlcr-loop`, restricts execution to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)`, and hides the command from the slash-command tool surface (`commands/start-rlcr-loop.md:1-5`). Its operational action is to invoke `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` (`commands/start-rlcr-loop.md:10-14`).
- algorithmic_behavior: Defines the high-level loop algorithm: implement the supplied plan, write a round summary, attempt to stop, let Codex review the summary, continue on issues, and end only when Codex emits `COMPLETE` (`commands/start-rlcr-loop.md:16-22`). It also establishes the Goal Tracker algorithm: immutable goal and acceptance criteria, mutable task/completion/deferred/evolution sections, round-by-round updates, and periodic full alignment checks at rounds 4, 9, 14, etc. (`commands/start-rlcr-loop.md:24-42`). The actual initialization algorithm lives in `scripts/setup-rlcr-loop.sh`: parse options, validate repository and plan, create `.humanize/rlcr/<timestamp>/`, write state, copy the plan backup, create `goal-tracker.md`, create `round-0-prompt.md`, and emit that prompt (`scripts/setup-rlcr-loop.sh:104-180`, `scripts/setup-rlcr-loop.sh:467-653`, `scripts/setup-rlcr-loop.sh:667-691`).
- inputs_outputs_state: Inputs are `$ARGUMENTS` containing a relative markdown plan path plus options such as `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--plan-file`, and `--track-plan-file` (`commands/start-rlcr-loop.md:3`, `scripts/setup-rlcr-loop.sh:43-100`, `scripts/setup-rlcr-loop.sh:104-180`). Environment inputs include `CLAUDE_PLUGIN_ROOT` for script resolution and `CLAUDE_PROJECT_DIR` or `pwd` for project root (`commands/start-rlcr-loop.md:4`, `scripts/setup-rlcr-loop.sh:186`). Outputs are the setup message and initial prompt, plus persistent loop artifacts under `.humanize/rlcr/<timestamp>/`: `state.md`, `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:467-477`, `scripts/setup-rlcr-loop.sh:485-498`, `scripts/setup-rlcr-loop.sh:504-653`). Initial state transition is from no active loop to an active loop represented by `state.md` with `current_round: 0`, `max_iterations`, Codex settings, plan metadata, start branch, and timestamp (`scripts/setup-rlcr-loop.sh:485-498`). Documented terminal transitions are max iteration, Codex `COMPLETE`, or user cancellation (`commands/start-rlcr-loop.md:51-55`); the related cancel workflow treats the newest `.humanize/rlcr/*/` as current and ends activity by creating `.cancel-requested` and moving `state.md` to `cancel-state.md` (`commands/cancel-rlcr-loop.md:28-34`).
- gates_or_invariants: The command-level invariant is that the command may only launch the setup script, not arbitrary tools (`commands/start-rlcr-loop.md:4`). The user-facing loop invariants are: always write summaries, maintain `goal-tracker.md`, include implementation/files/tests details, do not bypass by editing state files or running cancel commands, and trust Codex review (`commands/start-rlcr-loop.md:43-50`). Setup enforces a git repository with at least one commit (`scripts/setup-rlcr-loop.sh:211-220`), a provided single plan file (`scripts/setup-rlcr-loop.sh:188-205`), relative plan paths with no spaces or shell metacharacters (`scripts/setup-rlcr-loop.sh:227-248`), no symlink plan files (`scripts/setup-rlcr-loop.sh:253-257`), existing/readable plan files inside the project (`scripts/setup-rlcr-loop.sh:259-290`), no submodule plans (`scripts/setup-rlcr-loop.sh:292-305`), tracked-plan cleanliness only when `--track-plan-file` is used and gitignored-plan behavior otherwise (`scripts/setup-rlcr-loop.sh:311-355`), enough line/content density (`scripts/setup-rlcr-loop.sh:362-415`), Codex CLI availability (`scripts/setup-rlcr-loop.sh:417-423`), YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:429-461`), and optional push requirement when `--push-every-round` is set (`scripts/setup-rlcr-loop.sh:655-660`, `scripts/setup-rlcr-loop.sh:700-708`).
- dependencies_and_callers: Direct dependency is `scripts/setup-rlcr-loop.sh`, invoked through `CLAUDE_PLUGIN_ROOT` (`commands/start-rlcr-loop.md:10-14`). The setup script depends on `scripts/portable-timeout.sh` (`scripts/setup-rlcr-loop.sh:26-28`), `git`, `codex`, shell utilities such as `wc`, `grep`, and `sed`, and the hook system that later handles stop attempts and reviews. Runtime coordination dependencies include stop hooks and validators under `hooks/`, especially `hooks/loop-codex-stop-hook.sh` for exit gating and review, plus `commands/cancel-rlcr-loop.md` for the authorized cancel transition. User-facing docs also describe the command and artifact layout in `README.md`, but this assigned command file is the slash-command entry point.
- edge_cases_or_failure_modes: Explicit failures include no plan, conflicting positional and `--plan-file`, multiple plans, unknown options, nonnumeric `--max` or timeout, absolute paths, whitespace paths, shell-metacharacter paths, symlink plans, missing/unreadable plans, paths escaping the project, plans in submodules, tracked plans without `--track-plan-file`, untracked plans with `--track-plan-file`, dirty tracked plans, too few lines, comment-only or sparse plans, missing Codex CLI, unsafe branch names, unsafe Codex model names, and unsafe effort values (`scripts/setup-rlcr-loop.sh:109-180`, `scripts/setup-rlcr-loop.sh:188-423`, `scripts/setup-rlcr-loop.sh:429-461`). A behavioral edge case is that the markdown command states "No cheating" and says not to run cancel commands (`commands/start-rlcr-loop.md:48`), while the stopping section lists user cancellation as valid (`commands/start-rlcr-loop.md:51-55`); the distinction is that normal loop exit should not self-bypass, while explicit user cancellation is the sanctioned state-moving path.
- validation_or_tests: The setup script has targeted shell-test coverage in `tests/test-plan-file-validation.sh` for plan path and validation failures such as absolute paths, nonexistent files, missing directories, spaces, metacharacters, symlinks, project escape, tracked/untracked plan rules, modified tracked plans, blank/sparse/comment plans, good plan acceptance, and invalid Codex model/effort inputs (examples surfaced at `tests/test-plan-file-validation.sh:95`, `tests/test-plan-file-validation.sh:107`, `tests/test-plan-file-validation.sh:140`, `tests/test-plan-file-validation.sh:181`, `tests/test-plan-file-validation.sh:193`, `tests/test-plan-file-validation.sh:265`, `tests/test-plan-file-validation.sh:352`, `tests/test-plan-file-validation.sh:386`, `tests/test-plan-file-validation.sh:419`, `tests/test-plan-file-validation.sh:537`, `tests/test-plan-file-validation.sh:627`, `tests/test-plan-file-validation.sh:706`, `tests/test-plan-file-validation.sh:718`). Related lifecycle/state tests exist for cancellation and active-loop naming in `tests/test-cancel-signal-file.sh` and `tests/test-state-exit-naming.sh`. I did not run tests because this task asked for read-only research notes.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-043 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Prompt/block template used when an RLCR stop attempt is blocked because the git working tree is not clean. The template is not executable by itself; it defines the human/agent-facing recovery contract rendered into the stop-hook block reason (`prompt-template/block/git-not-clean.md:1-16`).
- algorithmic_behavior: The template turns a detected dirty-git state into required remediation steps: acknowledge `{{GIT_ISSUES}}`, include contextual `{{SPECIAL_NOTES}}`, optionally run `code-simplifier`, review untracked files and ignore artifacts, stage real changes, commit with a project-conventional message, avoid AI authorship metadata, and retry exit only after committing (`prompt-template/block/git-not-clean.md:3-16`). In the stop hook, this occurs before expensive Codex review: if cached git status is nonempty, the hook sets `GIT_ISSUES="uncommitted changes"`, derives special notes for `.humanize*` and other untracked files, renders this template, returns a JSON block decision, and exits without advancing review (`hooks/loop-codex-stop-hook.sh:411-471`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`, supplied by `hooks/loop-codex-stop-hook.sh` to `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:419-460`). `GIT_ISSUES` is derived from nonempty cached `git status` output (`hooks/loop-codex-stop-hook.sh:422-425`). `SPECIAL_NOTES` is assembled from sibling templates when untracked `.humanize*` paths or other untracked files are present (`hooks/loop-codex-stop-hook.sh:426-445`). Output is rendered markdown embedded as the JSON `reason`, with `systemMessage` set to "Loop: Blocked - uncommitted changes detected, please commit first" (`hooks/loop-codex-stop-hook.sh:462-469`). State transition is a guarded non-transition: the loop remains active and the stop attempt is denied until git is clean. The template itself does not mutate git state; it instructs the worker to perform the cleanup and commit.
- gates_or_invariants: Gate condition is `GIT_STATUS_CACHED` nonempty while inside a git repo (`hooks/loop-codex-stop-hook.sh:418-424`, `hooks/loop-codex-stop-hook.sh:449-471`). Invariants expressed by the template are: only real changes should be staged, build artifacts should go to `.gitignore`, commit messages must follow project conventions, and AI tools must not appear as commit authors or co-authors (`prompt-template/block/git-not-clean.md:7-14`). The sibling `.humanize` note adds an invariant that `.humanize/` is generated by `humanize:start-rlcr-loop` and should not be committed, recommending `.humanize*` in `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). Other untracked files are classified as likely artifacts, dependencies, editor files, logs, cache, or temp files until reviewed (`prompt-template/block/git-not-clean-untracked.md:2-10`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, which loads this template through `hooks/lib/template-loader.sh` (`hooks/loop-codex-stop-hook.sh:458-460`). The template loader implements `{{VAR}}` replacement as a single-pass substitution and keeps missing placeholders literal (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-129`). Safe loading provides fallback text when the template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`), and the stop hook also embeds a fallback dirty-git message (`hooks/loop-codex-stop-hook.sh:452-457`). Sibling dependencies are `prompt-template/block/git-not-clean-humanize-local.md` and `prompt-template/block/git-not-clean-untracked.md`, conditionally appended into `SPECIAL_NOTES` (`hooks/loop-codex-stop-hook.sh:430-445`).
- edge_cases_or_failure_modes: If this template is missing, the stop hook falls back to a minimal "Detected: {{GIT_ISSUES}}" message and `load_and_render_safe` renders that fallback (`hooks/loop-codex-stop-hook.sh:452-458`, `hooks/lib/template-loader.sh:167-203`). If variables are absent, the loader preserves placeholders rather than expanding recursively, preventing injected template syntax from being reinterpreted (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:116-123`). If both `.humanize*` and other untracked files exist, notes are concatenated; the sibling templates start with blank lines, so rendered text remains separated (`prompt-template/block/git-not-clean-humanize-local.md:1-8`, `prompt-template/block/git-not-clean-untracked.md:1-10`). The instruction to use `git add -A` can conflict with generated `.humanize/` artifacts unless the special note is present and followed; the stop hook mitigates this by detecting `.humanize` in untracked files (`hooks/loop-codex-stop-hook.sh:429-435`). Push enforcement is deliberately outside this template and handled later only when `push_every_round` is true (`hooks/loop-codex-stop-hook.sh:473-475`).
- validation_or_tests: I found indirect template-system validation in `tests/test-templates-comprehensive.sh`, including real-template rendering checks and template-directory validation (`tests/test-templates-comprehensive.sh:480-540`). The same test file stress-tests repeated `render_template` calls (`tests/test-templates-comprehensive.sh:547-560`). A targeted search found no direct test named for `git-not-clean.md`; coverage appears to rely on the shared template loader plus stop-hook dirty-git behavior. I did not run tests because this task asked for read-only research notes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 Item Evidence headings present; each corresponds to one assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`