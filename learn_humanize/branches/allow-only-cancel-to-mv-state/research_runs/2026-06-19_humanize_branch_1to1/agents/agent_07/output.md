# agent_07 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-007 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing behavioral specification for the RLCR loop: plan-driven Claude implementation, Codex review, iterative continuation, loop state files, cancellation semantics, and stop/completion states. The core loop is summarized as `Plan -> Claude -> Codex -> feedback loop -> done` in `README.md:31-39`.
- algorithmic_behavior: The documented algorithm starts from a markdown plan with acceptance criteria (`README.md:41-55`), invokes `/humanize:start-rlcr-loop` with optional iteration/model/timeout settings (`README.md:57-71`, `README.md:190-200`), persists all round artifacts under `.humanize/rlcr/<timestamp>/` (`README.md:73-86`, `README.md:242-267`), and uses Codex feedback to continue until `COMPLETE`, maximum iteration, cancellation, `STOP`, or abnormal termination. It also defines goal tracking as a drift-control mechanism with immutable ultimate goal / acceptance criteria and mutable active/completed/deferred/evolution sections (`README.md:131-143`).
- inputs_outputs_state: Inputs are a relative plan file, optional max iterations, Codex model/effort, Codex timeout, and optional push policy (`README.md:59-70`, `README.md:190-199`). Outputs are loop-local files: `state.md`, copied `plan.md`, `goal-tracker.md`, per-round prompt, summary, Codex review prompt, and review result (`README.md:244-253`), plus Codex run cache files under `$HOME/.cache/humanize/...` (`README.md:262-267`). State is represented by filenames: active loop means `state.md` exists (`README.md:112-119`, `README.md:247`); terminal state is encoded by renaming to `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md` (`README.md:255-260`).
- gates_or_invariants: The key invariant for this branch is that cancellation is a controlled `state.md -> cancel-state.md` transition, not arbitrary state-file mutation. README documents the manual cancel path as `mv "${LOOP_DIR}state.md" "${LOOP_DIR}cancel-state.md"` (`README.md:120-127`). The command spec tightens this by requiring `.cancel-requested` before the move (`commands/cancel-rlcr-loop.md:28-32`), while the bash validator blocks general state-file writes and allows only authorized cancel moves (`hooks/loop-bash-validator.sh:71-89`). The shared authorization routine requires a signal file, rejects command substitution, newlines, shell chaining, leftover variables, wrong source/destination, and extra args (`hooks/lib/loop-common.sh:251-388`). Other invariants: plan has at least five lines (`README.md:51-55`), active loop is the newest timestamp with `state.md` (`README.md:116-118`, `commands/cancel-rlcr-loop.md:34`), Goal Tracker protects acceptance criteria from drift (`README.md:131-143`), and full alignment checks may emit `STOP` on stagnation (`README.md:145-152`).
- dependencies_and_callers: README names `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, hook scripts, `hooks/check-todos-from-transcript.py`, `hooks/lib/loop-common.sh`, and setup/monitor scripts as the system surface (`README.md:213-239`). `commands/start-rlcr-loop.md:10-23` delegates to `scripts/setup-rlcr-loop.sh`; `commands/cancel-rlcr-loop.md:11-34` implements documented cancellation. `hooks/lib/loop-common.sh:441-474` implements terminal state renames for `complete|cancel|maxiter|stop|unexpected`.
- edge_cases_or_failure_modes: No `.humanize/rlcr` directory means no active loop (`commands/cancel-rlcr-loop.md:18`). A newest loop directory without `state.md` is already ended or malformed (`commands/cancel-rlcr-loop.md:20-27`). `STOP` is normal during full alignment but treated as unusual when emitted in a non-alignment round (`hooks/loop-codex-stop-hook.sh:933-960`). Schema/integrity issues terminate as `unexpected-state.md` per README (`README.md:260`). Manual or malicious `state.md` mutation is blocked except the exact authorized cancel move described above.
- validation_or_tests: README’s state semantics are backed by state and cancel tests visible in the tree: `tests/test-state-exit-naming.sh` covers active-vs-terminal state filenames, and `tests/test-cancel-signal-file.sh` covers the authorized cancel move and bypass cases. Related validators are `hooks/loop-write-validator.sh:92-98` for direct writes and `hooks/loop-bash-validator.sh:71-241` for shell bypasses.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-037 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook gate that blocks RLCR loop exit when Claude’s latest `TodoWrite` list still has incomplete tasks.
- algorithmic_behavior: The script runs isolated cases against `hooks/check-todos-from-transcript.py` using JSON hook input on stdin (`tests/test-todo-checker.sh:11-14`). It creates temporary JSONL transcript fixtures, executes the Python checker, captures output and exit code, and increments pass/fail counters through `pass()` / `fail()` helpers (`tests/test-todo-checker.sh:20-38`). The checker implementation reads hook input JSON, finds the latest `TodoWrite`, treats any status other than `completed` as incomplete, emits `INCOMPLETE_TODOS`, and exits `1` (`hooks/check-todos-from-transcript.py:90-129`).
- inputs_outputs_state: Inputs are synthetic hook JSON objects such as `{"transcript_path": ...}` and transcript JSONL entries in three accepted shapes: Claude assistant message content, alternative `type: message`, and direct `type: tool_use` (`tests/test-todo-checker.sh:106-240`; implementation at `hooks/check-todos-from-transcript.py:51-85`). Outputs are process exit codes and stdout/stderr. Expected codes are `0` for all-complete/no-todos/no-file, `1` for incomplete todos, and `2` for hook input parse errors (`tests/test-todo-checker.sh:51-97`, `hooks/check-todos-from-transcript.py:7-10`).
- gates_or_invariants: The latest `TodoWrite` call wins when multiple entries exist (`tests/test-todo-checker.sh:196-210`; implementation overwrites `latest_todos` at `hooks/check-todos-from-transcript.py:62-85`). Only exact `status == "completed"` passes; `pending`, `in_progress`, missing status, and empty-content pending todos all fail (`tests/test-todo-checker.sh:121-157`, `tests/test-todo-checker.sh:249-278`; implementation at `hooks/check-todos-from-transcript.py:113-126`). Invalid JSONL transcript lines are ignored, while invalid hook input JSON is a hard parse error (`tests/test-todo-checker.sh:51-73`, `tests/test-todo-checker.sh:179-194`).
- dependencies_and_callers: Requires bash, `python3`, `mktemp`, `grep`, and the Python checker at `hooks/check-todos-from-transcript.py`. The production caller is `hooks/loop-codex-stop-hook.sh`, which runs the checker before Codex review and blocks immediately on exit `1` with an incomplete-todos block template (`hooks/loop-codex-stop-hook.sh:246-292`). The same stop hook blocks parse errors from checker exit `2` (`hooks/loop-codex-stop-hook.sh:253-270`).
- edge_cases_or_failure_modes: Nonexistent transcript files and missing `transcript_path` are permissive exits (`tests/test-todo-checker.sh:75-97`, `hooks/check-todos-from-transcript.py:99-111`). Empty transcripts pass (`tests/test-todo-checker.sh:166-177`). The test labelled “Unicode in todo content” uses ASCII text `"Task with unicode"` rather than a non-ASCII fixture (`tests/test-todo-checker.sh:280-293`), so the label overstates actual Unicode coverage. The harness depends on `mktemp -d`; in this read-only branch export, direct execution failed before assertions with `mktemp: mkdtemp failed ... Operation not permitted`, so the script could not be fully run in this sandbox.
- validation_or_tests: This file contains 16 assertions grouped into input handling, todo detection, transcript format variations, and edge cases (`tests/test-todo-checker.sh:45-313`). Attempted validation command: `bash tests/test-todo-checker.sh`; result: failed at temporary directory creation due sandbox permission, not due checker assertion failure.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-067 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Prompt template defining the “continue loop after Codex found issues” transition from one RLCR round to the next. It is behavior-defining because it tells Claude what inputs to re-read, how to translate Codex review into work, and how to treat the goal tracker after Round 0.
- algorithmic_behavior: The template opens with “Your work is not finished” (`prompt-template/claude/next-round-prompt.md:1`), forces review of the original implementation plan through `@{{PLAN_FILE}}` (`prompt-template/claude/next-round-prompt.md:3-8`), requires Todos for all discovered issues and forbids addressing only the most important subset (`prompt-template/claude/next-round-prompt.md:12-13`), embeds Codex review content between explicit markers (`prompt-template/claude/next-round-prompt.md:16-19`), and requires reading the Goal Tracker before starting work (`prompt-template/claude/next-round-prompt.md:22-29`).
- inputs_outputs_state: Inputs are template variables `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` (`prompt-template/claude/next-round-prompt.md:6`, `prompt-template/claude/next-round-prompt.md:18`, `prompt-template/claude/next-round-prompt.md:24`). Output is a generated `round-N-prompt.md` written by `hooks/loop-codex-stop-hook.sh` when Codex does not return `COMPLETE` or `STOP` (`hooks/loop-codex-stop-hook.sh:963-988`). The same hook increments `current_round` in `state.md` before creating this next prompt (`hooks/loop-codex-stop-hook.sh:967-970`) and appends footer instructions with `NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:998-1002`).
- gates_or_invariants: It preserves plan alignment by requiring the original plan before implementation (`prompt-template/claude/next-round-prompt.md:3-8`). It turns Codex findings into a complete TodoWrite work queue, not selective issue handling (`prompt-template/claude/next-round-prompt.md:12-13`). It enforces Goal Tracker read-only behavior after Round 0 and routes desired changes into a summary section instead of direct edits (`prompt-template/claude/next-round-prompt.md:22-31`). Direct enforcement exists in `hooks/loop-write-validator.sh:114-120`, which blocks `goal-tracker.md` writes after Round 0.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh:985-988` via `load_and_render_safe`. Template rendering is single-pass, so placeholders embedded inside `REVIEW_CONTENT` are not recursively expanded or corrupted (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-120`). It coordinates with `prompt-template/claude/next-round-footer.md`, which adds no-cheating, commit, and summary-write instructions (`prompt-template/claude/next-round-footer.md:4-9`), and `prompt-template/claude/goal-tracker-update-request.md`, which defines the requested update section (`prompt-template/claude/goal-tracker-update-request.md:2-14`).
- edge_cases_or_failure_modes: If this template is rendered without the hook-appended footer, it does not itself instruct where to write the next summary or commit changes; the complete round prompt depends on footer append logic. If variables are missing, the template loader preserves unresolved placeholders rather than failing (`hooks/lib/template-loader.sh:12-13`). If `REVIEW_CONTENT` contains `{{...}}`, single-pass substitution intentionally prevents accidental second-pass replacement (`hooks/lib/template-loader.sh:54-57`).
- validation_or_tests: Template reference coverage appears in `tests/test-template-references.sh` and comprehensive rendering tests in `tests/test-templates-comprehensive.sh`; template-loader injection behavior is covered by `tests/test-template-loader.sh` cases around `REVIEW_CONTENT` placeholder preservation. Production validation is the stop hook path that renders this prompt only after non-terminal Codex review (`hooks/loop-codex-stop-hook.sh:914-988`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 Item Evidence headings present, each with cursor `[_]`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`