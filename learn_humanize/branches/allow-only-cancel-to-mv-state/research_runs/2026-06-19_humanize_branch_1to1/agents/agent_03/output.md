# agent_03 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-003 `directory` `hooks`
- cursor: `[_]`
- core_role: The `hooks` directory is the Claude Code hook enforcement layer for the RLCR loop. It coordinates tool-time validation, loop-state protection, active-loop detection, summary/prompt/todo access rules, and stop-time Codex review. The branch-relevant core behavior is that `state.md` is the sole active-loop marker, direct user/tool modification of it is blocked, and the only user-initiated shell move allowed is the exact cancel transition `state.md -> cancel-state.md` after a `.cancel-requested` signal exists.
- algorithmic_behavior: `hooks/hooks.json:4-63` registers the full algorithm surface: `loop-plan-file-validator.sh` on `UserPromptSubmit`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, and `loop-bash-validator.sh` on `PreToolUse`, and `loop-codex-stop-hook.sh` on `Stop` with a 7200 second timeout. Shared state logic lives in `hooks/lib/loop-common.sh`. `find_active_loop` checks only the newest `.humanize/rlcr/<timestamp>/` directory and treats it as active only when `state.md` exists (`hooks/lib/loop-common.sh:58-79`), preventing older or renamed state files from reviving a completed/cancelled loop. `parse_state_file` extracts frontmatter fields including `current_round`, `max_iterations`, `push_every_round`, plan metadata, and Codex configuration (`hooks/lib/loop-common.sh:96-137`). Round-file helpers recognize `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md`, extract `N`, and enforce a small allowlist for bootstrapping files (`hooks/lib/loop-common.sh:144-186`). The stop hook runs pre-review gates, invokes Codex, interprets terminal markers, and either ends the loop or advances `current_round` and writes the next prompt (`hooks/loop-codex-stop-hook.sh:608-1033`).
- inputs_outputs_state: Inputs are Claude hook JSON from stdin, filesystem state under `$CLAUDE_PROJECT_DIR/.humanize/rlcr`, Git state, transcript JSONL path for todo checks, prompt templates under `prompt-template`, and Codex CLI output. Outputs are hook exit status, stderr block messages for `PreToolUse` hooks, JSON block/allow decisions for prompt/stop hooks, generated review prompt/result files, next-round prompts, cache logs under `$HOME/.cache/humanize`, and renamed terminal state files. Core state transitions are: `state.md` present means active (`hooks/lib/loop-common.sh:74-75`); `end_loop "$loop_dir" "$state_file" reason` renames it to `${reason}-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`hooks/lib/loop-common.sh:441-474`); the stop hook calls `end_loop` for invalid Codex config or corrupted round as `unexpected` (`hooks/loop-codex-stop-hook.sh:90-107`), max iterations as `maxiter` (`hooks/loop-codex-stop-hook.sh:612-617`), Codex `COMPLETE` as `complete` (`hooks/loop-codex-stop-hook.sh:922-930`), and Codex `STOP` as `stop` (`hooks/loop-codex-stop-hook.sh:933-960`). Nonterminal review feedback advances `current_round` by writing a temp file and moving it back to `state.md` (`hooks/loop-codex-stop-hook.sh:967-970`).
- gates_or_invariants: The branch’s central invariant is enforced in `is_cancel_authorized`: a signal file must exist at `$active_loop_dir/.cancel-requested` (`hooks/lib/loop-common.sh:265-270`); commands containing `$(`, backticks, newlines, shell chaining operators, pipes, or remaining `$` after `$loop_dir` normalization are rejected (`hooks/lib/loop-common.sh:272-303`); command must begin with `mv`, parse as exactly two arguments, and normalize to exactly `<active_loop_dir>/state.md` and `<active_loop_dir>/cancel-state.md` (`hooks/lib/loop-common.sh:305-388`). `loop-bash-validator.sh` blocks all state-file writes by destination pattern unless this cancel authorization succeeds (`hooks/loop-bash-validator.sh:81-89`), blocks `mv`/`cp` from `state.md` to arbitrary destinations using segment splitting and redirection-prefix stripping (`hooks/loop-bash-validator.sh:91-226`), and blocks shell-wrapper attempts such as `sh -c 'mv state.md ...'` unless the exact cancel gate passes (`hooks/loop-bash-validator.sh:228-240`). `loop-write-validator.sh` and `loop-edit-validator.sh` independently block direct Write/Edit access to `state.md` (`hooks/loop-write-validator.sh:91-98`, `hooks/loop-edit-validator.sh:75-82`). Other invariants include: no Git push unless `push_every_round: true` (`hooks/loop-bash-validator.sh:51-69`); prompt files are read-only (`hooks/loop-write-validator.sh:47-50`, `hooks/loop-edit-validator.sh:46-49`, `hooks/loop-bash-validator.sh:273-281`); summary writes must target the current active loop and current round (`hooks/loop-write-validator.sh:153-188`, `hooks/loop-edit-validator.sh:112-138`); goal tracker is directly editable only in round 0 and via non-Bash tools (`hooks/loop-bash-validator.sh:256-271`); plan backup in `.humanize/rlcr` is protected (`hooks/loop-write-validator.sh:100-111`, `hooks/loop-edit-validator.sh:84-96`, `hooks/loop-bash-validator.sh:243-254`).
- dependencies_and_callers: Hook registration depends on Claude Code hook names and `${CLAUDE_PLUGIN_ROOT}` (`hooks/hooks.json:4-58`). Shell validators depend on `jq`, `sed`, `grep`, `awk`, Git, and shared helpers from `hooks/lib/loop-common.sh`. Template rendering depends on `hooks/lib/template-loader.sh`, whose `get_template_dir` resolves `prompt-template` from the plugin root (`hooks/lib/template-loader.sh:24-31`) and whose single-pass `render_template` prevents placeholder injection from variable values (`hooks/lib/template-loader.sh:50-132`). `loop-plan-file-validator.sh` and `loop-codex-stop-hook.sh` also source `scripts/portable-timeout.sh` for bounded Git/Codex calls (`hooks/loop-plan-file-validator.sh:19-24`, `hooks/loop-codex-stop-hook.sh:50-55`). The documented user cancel caller is `commands/cancel-rlcr-loop.md`, which allows only listing loop dirs, touching `.cancel-requested`, moving `state.md` to `cancel-state.md`, reading state, and Read (`commands/cancel-rlcr-loop.md:1-4`), then explicitly instructs touch-before-mv (`commands/cancel-rlcr-loop.md:28-32`). External status parsing consumes renamed `*-state.md` files via `scripts/humanize.sh`, which prioritizes `state.md|active`, then returns `<reason>` from `*-state.md` (`scripts/humanize.sh:98-136`). Setup creates initial `state.md` with all fields the hooks later parse (`scripts/setup-rlcr-loop.sh:485-498`).
- edge_cases_or_failure_modes: `find_active_loop` intentionally ignores older directories even if they contain `state.md`; only the newest timestamp can be active (`hooks/lib/loop-common.sh:58-79`). If no active loop exists, validators allow normally (`hooks/loop-bash-validator.sh:40-43`, `hooks/loop-write-validator.sh:83-85`, `hooks/loop-edit-validator.sh:67-69`, `hooks/loop-read-validator.sh:65-67`). `is_cancel_authorized` lowercases commands before comparison through caller flow, so case-sensitive filesystem paths containing uppercase could be fragile, although tests appear to use lowercase temporary paths. Its parser accepts simple quoted/unquoted two-argument `mv` only; options such as `mv -f` are rejected for the authorized cancel operation, while the broader Bash validator still detects option/prefix bypasses for blocking. `command_modifies_file` is regex-based and intentionally conservative for common shell write forms (`hooks/lib/loop-common.sh:397-425`). Stop-hook failure modes fail closed: invalid `codex_model`/`codex_effort` or nonnumeric `current_round` end as `unexpected`; Git failures, plan integrity violations, incomplete todos, large files, dirty Git state, missing summary, missing Codex, nonzero Codex exit, missing/empty review result all block exit rather than silently completing (`hooks/loop-codex-stop-hook.sh:90-237`, `hooks/loop-codex-stop-hook.sh:246-530`, `hooks/loop-codex-stop-hook.sh:716-911`).
- validation_or_tests: Direct validation coverage includes `tests/test-state-exit-naming.sh`, which proves active state naming and `end_loop` behavior, and `tests/test-cancel-signal-file.sh`, which specifically validates the cancel signal mechanism. The cancel tests cover positive authorization with `.cancel-requested` and exact `mv state.md cancel-state.md` (`tests/test-cancel-signal-file.sh:74-113`), block without signal (`tests/test-cancel-signal-file.sh:115-133`), block writes/sed/rm/wrong destination/wrong signal dir/injection/arbitrary destination (`tests/test-cancel-signal-file.sh:135-295`), accept documented literal `${loop_dir}` syntax (`tests/test-cancel-signal-file.sh:440-484`), reject extra args/hidden vars/prefix bypasses (`tests/test-cancel-signal-file.sh:420-560`), block unrelated state paths and allow no-active-loop commands (`tests/test-cancel-signal-file.sh:1206-1247`), and directly unit-test `is_cancel_authorized` helper outcomes (`tests/test-cancel-signal-file.sh:1249-1341`). Template and allowlist tests reference hook/template behavior (`tests/test-template-loader.sh:151-164`, `tests/test-templates-comprehensive.sh:498-512`). I did not execute tests because this assignment says research notes only and the branch export is read-only.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-033 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the loop-state lifecycle naming contract. It proves that only `state.md` denotes an active loop, and terminal states are persisted by renaming `state.md` to `<reason>-state.md` rather than editing a status field inside the file.
- algorithmic_behavior: The test creates an isolated Git repository in a temp dir, exports `CLAUDE_PROJECT_DIR`, sources `hooks/lib/loop-common.sh`, and exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` (`tests/test-state-exit-naming.sh:31-67`). Tests 1-6 create `complete-state.md`, `state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` in the same loop directory and assert only `state.md` is detected active (`tests/test-state-exit-naming.sh:38-155`). Test 7 adds a newer loop directory with `state.md` and asserts newest-active precedence (`tests/test-state-exit-naming.sh:157-176`). Tests 8-10 exercise `end_loop`: invalid reason is rejected, all valid reasons produce `${reason}-state.md`, and missing source state fails gracefully (`tests/test-state-exit-naming.sh:178-239`). Tests 11-13 assert that only the new `.humanize/rlcr/` path is considered loop state, and legacy `.humanize-loop.local` is not matched or searched (`tests/test-state-exit-naming.sh:241-287`).
- inputs_outputs_state: Inputs are a temporary loop filesystem, synthetic YAML frontmatter state files, the sourced common hook library, and environment variable `CLAUDE_PROJECT_DIR`. Outputs are PASS/FAIL/SKIP counters and process exit code equal to `TESTS_FAILED` (`tests/test-state-exit-naming.sh:289-298`). State transitions under test are direct file presence transitions: `state.md` present means active, removing it and leaving any terminal `*-state.md` means inactive, and `end_loop` converts active `state.md` into `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`.
- gates_or_invariants: Valid `end_loop` reasons are exactly `complete`, `cancel`, `maxiter`, `stop`, and `unexpected`, matching the common library case statement (`hooks/lib/loop-common.sh:453-461`) and asserted by looped test data (`tests/test-state-exit-naming.sh:202-226`). Active-loop discovery must not scan legacy `.humanize-loop.local` (`tests/test-state-exit-naming.sh:254-287`). Newest directory selection is an invariant, so stale older sessions cannot become active if a newer active session exists (`tests/test-state-exit-naming.sh:157-176`). A missing `state.md` must be treated as no active loop, not as an error requiring recovery (`tests/test-state-exit-naming.sh:93-155`, `tests/test-state-exit-naming.sh:228-239`).
- dependencies_and_callers: The test depends directly on `hooks/lib/loop-common.sh` (`tests/test-state-exit-naming.sh:64-65`) and thereby on `hooks/lib/template-loader.sh` as sourced by the common library (`hooks/lib/loop-common.sh:46-55`). It validates behavior consumed by `loop-bash-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, and `loop-codex-stop-hook.sh`, all of which locate the active loop with `find_active_loop` before enforcing or transitioning state. It also aligns with `scripts/humanize.sh` status parsing, which treats `state.md` as active and `*-state.md` as terminal (`scripts/humanize.sh:98-136`).
- edge_cases_or_failure_modes: The script uses `set -uo pipefail`, not global `set -e`, and temporarily toggles `set +e` around negative command tests to inspect exit codes (`tests/test-state-exit-naming.sh:192-199`, `tests/test-state-exit-naming.sh:212-220`, `tests/test-state-exit-naming.sh:231-238`). Because it sources real hook code, failures in template directory validation only warn via common-library setup and should not prevent state tests. It checks legacy path rejection explicitly to avoid accidental compatibility with `.humanize-loop.local`, which would be a state discovery regression. It does not cover the `.cancel-requested` authorization gate; that is covered in the related `tests/test-cancel-signal-file.sh`.
- validation_or_tests: This is itself a validation asset. It verifies five terminal state names from the branch contract, invalid/missing-state error behavior, newest loop precedence, and new-path-only detection. Expected successful completion is `exit 0`; any failed assertion increments `TESTS_FAILED` and becomes the script exit code (`tests/test-state-exit-naming.sh:27-29`, `tests/test-state-exit-naming.sh:289-298`). I inspected but did not run it due to the read-only research-only assignment.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-063 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: This is a blocking prompt template for the round-number invariant on summary-file write/edit attempts. It tells the agent which attempted round file is invalid, states the current round, gives the correct path, and prevents manual round increments.
- algorithmic_behavior: The template has placeholders for `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH` (`prompt-template/block/wrong-round-number.md:1-7`). `loop-write-validator.sh` renders it when a Write target is a summary file whose extracted round number does not equal `STATE_CURRENT_ROUND` and the file is not allowlisted (`hooks/loop-write-validator.sh:153-169`). `loop-edit-validator.sh` renders the same template for Edit operations under the same wrong-round condition (`hooks/loop-edit-validator.sh:112-135`). Rendering is single-pass via `render_template`, so placeholder-like content inside values is not recursively expanded (`hooks/lib/template-loader.sh:50-132`).
- inputs_outputs_state: Inputs are validator-derived values: attempted action (`write to` or `edit`), attempted round from `extract_round_number`, file type `summary`, current round parsed from active `state.md`, and the computed correct active-loop summary path. Output is a Markdown block message on stderr, and the hook exits `2` to block the tool call (`hooks/loop-write-validator.sh:163-170`, `hooks/loop-edit-validator.sh:129-136`). It does not mutate state; it preserves the current-round state machine by stopping writes/edits to future or stale `round-N-summary.md` files.
- gates_or_invariants: The invariant is that agents must write/edit only `round-${CURRENT_ROUND}-summary.md` in the active loop directory, except for the explicit allowlisted files in `is_allowlisted_file` (`hooks/lib/loop-common.sh:165-186`). The final instruction, “Do NOT increment the round number yourself,” is the human-facing enforcement message (`prompt-template/block/wrong-round-number.md:7`); the actual state transition to the next round is owned by the stop hook after Codex review feedback (`hooks/loop-codex-stop-hook.sh:967-988`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for loading/rendering and `hooks/lib/loop-common.sh` for current-round parsing and filename recognition. Direct callers are `loop-write-validator.sh` and `loop-edit-validator.sh`; `loop-read-validator.sh` uses a separate `wrong-round-file.md` template for Read operations (`hooks/loop-read-validator.sh:110-122`). Template tests load this exact file with sample substitutions in `tests/test-template-loader.sh:151-164` and `tests/test-templates-comprehensive.sh:498-512`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, validators use inline fallback strings through `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`, `hooks/loop-write-validator.sh:158-168`, `hooks/loop-edit-validator.sh:124-134`). Missing variables remain as placeholders by renderer design (`hooks/lib/template-loader.sh:11-13`, `hooks/lib/template-loader.sh:119-122`), which is safer than silently deleting diagnostic context but could expose unresolved `{{...}}` if a caller omits a variable. The template itself is path-agnostic and relies on callers to compute `CORRECT_PATH`; wrong active-loop discovery would produce a wrong correction.
- validation_or_tests: `tests/test-template-loader.sh` checks that the real template renders the title, attempted file `round-3-summary.md`, and current round `5` (`tests/test-template-loader.sh:151-164`). `tests/test-templates-comprehensive.sh` additionally checks that the rendered correct path appears (`tests/test-templates-comprehensive.sh:498-512`). The validators’ wrong-round branches are covered indirectly by hook tests that exercise summary access controls. I inspected but did not run tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item headings above; exact IDs intentionally not repeated here to preserve the “appears exactly once” output constraint
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`