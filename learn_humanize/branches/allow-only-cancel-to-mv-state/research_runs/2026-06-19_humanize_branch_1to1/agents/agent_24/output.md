# agent_24 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-024 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap entry script for `/humanize:start-rlcr-loop`. It creates the active RLCR loop workspace under `.humanize/rlcr/<timestamp>/`, validates the user-supplied plan file and runtime settings, writes the initial `state.md`, backs up the plan, creates `goal-tracker.md`, and emits the round-0 prompt that starts the loop. The slash-command wrapper allows this script directly via `commands/start-rlcr-loop.md:4` and invokes it at `commands/start-rlcr-loop.md:12-14`.
- algorithmic_behavior: Argument parsing at `scripts/setup-rlcr-loop.sh:104-180` accepts one plan source, or `--plan-file`, plus `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--push-every-round`, and `--track-plan-file`. It merges explicit and positional plan inputs at `scripts/setup-rlcr-loop.sh:188-195` and rejects missing or duplicate plan sources at `scripts/setup-rlcr-loop.sh:197-205` and `scripts/setup-rlcr-loop.sh:189-191`. It validates the repo has a Git directory and at least one commit at `scripts/setup-rlcr-loop.sh:211-220`. The plan path gate rejects absolute paths, spaces, shell metacharacters, symlinks, missing parents, missing/unreadable files, path escape outside the project, and submodule-contained plans at `scripts/setup-rlcr-loop.sh:227-305`. Git tracking policy is bifurcated: with `--track-plan-file`, the plan must be tracked and clean; without it, the plan must not be tracked, effectively requiring a gitignored/local plan at `scripts/setup-rlcr-loop.sh:311-356`. Content validation requires at least 5 physical lines and at least 3 meaningful content lines after skipping blanks, shell/YAML-style comments, and HTML comments at `scripts/setup-rlcr-loop.sh:362-415`. It requires `codex` on PATH at `scripts/setup-rlcr-loop.sh:417-423`, records the start branch at `scripts/setup-rlcr-loop.sh:429-443`, and validates model/effort strings for YAML-safe state serialization at `scripts/setup-rlcr-loop.sh:445-460`.
- inputs_outputs_state: Inputs are CLI arguments, `CLAUDE_PROJECT_DIR` or current working directory for `PROJECT_ROOT` at `scripts/setup-rlcr-loop.sh:186`, the plan file content, Git metadata, and the `codex` executable. It sources `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:26-28`; that helper selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at `scripts/portable-timeout.sh:9-71`. Outputs are `.humanize/rlcr/<timestamp>/plan.md` as an immutable backup copy at `scripts/setup-rlcr-loop.sh:467-477`, `.humanize/rlcr/<timestamp>/state.md` with frontmatter fields `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` at `scripts/setup-rlcr-loop.sh:485-498`, `.humanize/rlcr/<timestamp>/goal-tracker.md` at `scripts/setup-rlcr-loop.sh:504-594`, and `.humanize/rlcr/<timestamp>/round-0-prompt.md` plus expected `round-0-summary.md` path at `scripts/setup-rlcr-loop.sh:600-653`. The state transition performed by this file is initialization only: no active state is advanced or terminated here. Downstream, `hooks/loop-codex-stop-hook.sh` increments `current_round` at `hooks/loop-codex-stop-hook.sh:967-970`, and `hooks/lib/loop-common.sh` renames `state.md` into terminal reason files via `end_loop` at `hooks/lib/loop-common.sh:441-474`.
- gates_or_invariants: Exactly one plan file is allowed, and it must be a relative, shell-safe, in-project, non-symlink, readable file outside submodules. The plan tracking invariant is encoded into `plan_tracked`: tracked plans require explicit opt-in and a clean Git status; default plans must remain untracked. The plan is copied into the loop directory so later hooks can detect modifications by diffing the live plan against the backup; the stop hook performs that at `hooks/loop-codex-stop-hook.sh:170-238`. The branch invariant is captured as `start_branch`; downstream branch consistency is enforced by `hooks/loop-codex-stop-hook.sh:137-164` and `hooks/loop-plan-file-validator.sh:84-108`. The state schema fields `plan_tracked` and `start_branch` are required by later validators; shared constants and parsing live at `hooks/lib/loop-common.sh:15-24` and `hooks/lib/loop-common.sh:96-136`. The generated prompt explicitly tells the worker not to edit loop state or invoke cancellation as an escape path at `scripts/setup-rlcr-loop.sh:646`, while the user-facing output separately says cancellation should use `/humanize:cancel-rlcr-loop` at `scripts/setup-rlcr-loop.sh:684`.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md:12-14`. Runtime dependencies include Bash, Git, `codex`, `wc`, `grep`, `sed`, `awk`, `dirname`, `basename`, `date`, `mkdir`, `cp`, and the sourced timeout wrapper. Its state output is consumed by `hooks/lib/loop-common.sh:117-129`, `hooks/loop-codex-stop-hook.sh:76-89`, and `hooks/loop-plan-file-validator.sh:40-47`. The goal tracker produced here is later checked for Round 0 initialization by `hooks/loop-codex-stop-hook.sh:537-605`. Codex review settings written here are later used to invoke `codex exec` at `hooks/loop-codex-stop-hook.sh:774-801`.
- edge_cases_or_failure_modes: The integer checks for `--max` and `--codex-timeout` accept `0` even though help text says positive; this can create immediate max-iteration behavior or a zero-second review timeout. Markdown headings beginning with `#` are treated as comments for content-count purposes by design at `scripts/setup-rlcr-loop.sh:372-405`, so a plan made mostly of headings can fail the “content lines” gate. The path metacharacter denylist blocks brackets and glob characters at `scripts/setup-rlcr-loop.sh:241-248`, which is conservative but can reject otherwise valid filenames. The branch/model/effort YAML-safety gates protect state serialization but also reject branches containing colon, hash, quotes, backticks, or newlines at `scripts/setup-rlcr-loop.sh:435-460`. `PLAN_CONTENT=$(cat "$FULL_PLAN_PATH")` at `scripts/setup-rlcr-loop.sh:505` appears unused. Starting a new loop does not itself check for an already active loop; active loop discovery later only considers the newest timestamped directory with `state.md` per `hooks/lib/loop-common.sh:58-79`.
- validation_or_tests: I inspected but did not run tests, per research-only scope. Relevant coverage exists in `tests/test-plan-file-validation.sh`, whose header lists path, symlink, submodule, Git repo, and tracking validation targets at `tests/test-plan-file-validation.sh:3-12`; concrete cases cover absolute paths at `tests/test-plan-file-validation.sh:89-102`, missing files and parent dirs at `tests/test-plan-file-validation.sh:104-126`, paths with spaces at `tests/test-plan-file-validation.sh:128-167`, shell metacharacters at `tests/test-plan-file-validation.sh:169-187`, symlinks at `tests/test-plan-file-validation.sh:189-200`, and path escape at `tests/test-plan-file-validation.sh:245-265`. State lifecycle expectations are covered by `tests/test-state-exit-naming.sh:38-176` and valid terminal rename reasons by `tests/test-state-exit-naming.sh:202-225`.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-054 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Small but core prompt/block template for the RLCR state-schema gate. It defines the user-facing reason when an active loop’s `state.md` lacks a required field, currently rendered for missing `plan_tracked` or `start_branch` by `hooks/loop-plan-file-validator.sh`.
- algorithmic_behavior: The template contains a single placeholder, `{{FIELD_NAME}}`, at `prompt-template/block/schema-outdated.md:3`, followed by remediation options to cancel, update the plugin, and restart the loop at `prompt-template/block/schema-outdated.md:7-10`. It is loaded through `load_and_render_safe` in `hooks/loop-plan-file-validator.sh:52-59`; that caller wraps the rendered Markdown into a JSON block decision at `hooks/loop-plan-file-validator.sh:60-69`. The missing-field loop checks required entries at `hooks/loop-plan-file-validator.sh:72-82`, using shared field constants from `hooks/lib/loop-common.sh:15-24`.
- inputs_outputs_state: Input is the symbolic field name supplied as `FIELD_NAME`, derived from internal constants rather than raw user input. Output is a Markdown block used as the hook `reason`. This template does not mutate state directly. The hook that renders it exits with a block decision after emitting JSON, so its immediate state effect is preventative: the prompt submission does not proceed while schema-critical fields are absent.
- gates_or_invariants: The invariant is that active RLCR state must contain the new schema fields required by v1.1.2-era plan tracking and branch consistency checks. Missing `plan_tracked` or `start_branch` blocks the loop-facing prompt path before branch or plan-file tracking logic runs. Rendering itself is single-pass via `hooks/lib/template-loader.sh:50-132`, which avoids recursive placeholder substitution and leaves unknown placeholders intact.
- dependencies_and_callers: Direct caller is `hooks/loop-plan-file-validator.sh:52-59`. Supporting loader behavior is in `hooks/lib/template-loader.sh`: template path discovery at `hooks/lib/template-loader.sh:24-31`, file loading at `hooks/lib/template-loader.sh:33-48`, and safe fallback handling at `hooks/lib/template-loader.sh:167-203`. The stop hook performs a related outdated-schema check with inline text, not this template, at `hooks/loop-codex-stop-hook.sh:114-131`.
- edge_cases_or_failure_modes: If both required fields are missing, `hooks/loop-plan-file-validator.sh:72-82` reports the first missing field encountered and exits, so the user may see one field at a time. If the template is missing or empty, the caller’s fallback text at `hooks/loop-plan-file-validator.sh:55-58` preserves the same basic contract. The template’s final line says the loop “will be terminated as unexpected” at `prompt-template/block/schema-outdated.md:12`, but the prompt-submit validator that renders it only blocks and does not rename `state.md`; the stop hook also blocks for missing schema fields at `hooks/loop-codex-stop-hook.sh:119-130` without calling `end_loop`. That wording is therefore stronger than the observed caller behavior.
- validation_or_tests: I inspected but did not run tests. Related schema-block coverage appears in `tests/test-plan-file-hooks.sh:631-653`, where an old state file missing `plan_tracked` is expected to produce a JSON block mentioning schema or missing field information. Shared state parsing and exit-state conventions are covered by `tests/test-state-exit-naming.sh:38-176` and `tests/test-state-exit-naming.sh:202-239`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-024`, `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-054`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`