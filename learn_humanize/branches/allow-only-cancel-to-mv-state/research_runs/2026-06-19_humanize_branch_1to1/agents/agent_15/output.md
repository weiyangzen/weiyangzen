# agent_15 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-015 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Hook manifest for the Humanize RLCR loop. It is not the validator implementation itself; it is the dispatch table that wires Claude lifecycle events to the shell validators and stop hook. Its description scopes it to “Validation hooks and Stop hooks for /start-rlcr-loop” at `hooks/hooks.json:2`.
- algorithmic_behavior: Defines three event groups. `UserPromptSubmit` runs `loop-plan-file-validator.sh` before user prompts continue, making plan/state consistency a pre-turn gate (`hooks/hooks.json:4`, `hooks/hooks.json:9`). `PreToolUse` dispatches per-tool validators for `Write`, `Edit`, `Read`, and `Bash` (`hooks/hooks.json:14`, `hooks/hooks.json:16`, `hooks/hooks.json:25`, `hooks/hooks.json:34`, `hooks/hooks.json:43`). `Stop` runs `loop-codex-stop-hook.sh` with a 7200-second timeout, making loop exit subject to review and repository gates (`hooks/hooks.json:52`, `hooks/hooks.json:57`, `hooks/hooks.json:58`).
- inputs_outputs_state: Input is Claude hook JSON from the runtime, routed by event type and matcher. The manifest’s output is indirect: each command emits either allow/no output with exit `0`, or a blocking message/JSON and exit convention from the target script. State is read from the active `.humanize/rlcr/<timestamp>/state.md` selected by `find_active_loop`, which only treats the newest directory containing `state.md` as active (`hooks/lib/loop-common.sh:62`, `hooks/lib/loop-common.sh:74`). The manifest itself performs no state transitions, but it delegates to code that protects `state.md`, and the stop hook can end loops by renaming `state.md` through `end_loop` to `<reason>-state.md` (`hooks/lib/loop-common.sh:441`, `hooks/lib/loop-common.sh:463`, `hooks/lib/loop-common.sh:466`).
- gates_or_invariants: The major invariant relevant to this branch is enforced under the `Bash` matcher: state-file mutation is blocked unless it is the authorized cancel move. `loop-bash-validator.sh` blocks destination writes to `state.md` (`hooks/loop-bash-validator.sh:81`, `hooks/loop-bash-validator.sh:87`) and separately catches `mv`/`cp` where `state.md` is the source (`hooks/loop-bash-validator.sh:91`, `hooks/loop-bash-validator.sh:104`, `hooks/loop-bash-validator.sh:218`). The only cancel exception calls `is_cancel_authorized` (`hooks/loop-bash-validator.sh:83`, `hooks/loop-bash-validator.sh:219`, `hooks/loop-bash-validator.sh:234`). That helper requires `.cancel-requested` in the active loop directory (`hooks/lib/loop-common.sh:265`, `hooks/lib/loop-common.sh:268`), rejects command substitution, newlines, and shell chaining (`hooks/lib/loop-common.sh:272`, `hooks/lib/loop-common.sh:277`, `hooks/lib/loop-common.sh:282`), requires plain `mv` (`hooks/lib/loop-common.sh:305`), enforces exactly two arguments (`hooks/lib/loop-common.sh:355`, `hooks/lib/loop-common.sh:361`), and requires source exactly `<active_loop>/state.md` plus destination exactly `<active_loop>/cancel-state.md` after normalization (`hooks/lib/loop-common.sh:373`, `hooks/lib/loop-common.sh:377`, `hooks/lib/loop-common.sh:381`, `hooks/lib/loop-common.sh:384`). The slash command metadata aligns with this by allowlisting `touch .humanize/rlcr/*/.cancel-requested` and `mv .humanize/rlcr/*/state.md .humanize/rlcr/*/cancel-state.md` (`commands/cancel-rlcr-loop.md:3`, `commands/cancel-rlcr-loop.md:30`, `commands/cancel-rlcr-loop.md:31`).
- dependencies_and_callers: The manifest depends on `CLAUDE_PLUGIN_ROOT` resolving to the plugin root because all command paths are `${CLAUDE_PLUGIN_ROOT}/hooks/...` (`hooks/hooks.json:9`, `hooks/hooks.json:20`, `hooks/hooks.json:29`, `hooks/hooks.json:38`, `hooks/hooks.json:47`, `hooks/hooks.json:57`). The individual validators source `hooks/lib/loop-common.sh`, for example the Bash validator at `hooks/loop-bash-validator.sh:15` and `hooks/loop-bash-validator.sh:16`. The README documents `hooks/hooks.json` as the lifecycle hook entrypoint under `hooks/` (`README.md:224`, `README.md:225`). `.claude-plugin/plugin.json` provides plugin metadata but not hook registration content (`.claude-plugin/plugin.json:1`, `.claude-plugin/plugin.json:3`).
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is unset or wrong, the manifest commands cannot resolve. If no active loop exists, validators generally allow actions because `find_active_loop` returns empty, as seen in `loop-bash-validator.sh:40` and `loop-bash-validator.sh:42`. If the newest loop directory lacks `state.md`, older directories are intentionally ignored, preventing ended loops from reviving (`hooks/lib/loop-common.sh:58`, `hooks/lib/loop-common.sh:74`). The Bash validator has explicit defenses for bypass variants: `mv state.md` to arbitrary destinations, `cp`, shell wrappers, chained commands, leading redirections, quoted relative paths, command substitution, hidden variables, and wrong-directory signal files (`hooks/loop-bash-validator.sh:91`, `hooks/loop-bash-validator.sh:228`, `hooks/lib/loop-common.sh:272`, `hooks/lib/loop-common.sh:300`). A remaining structural risk is that `hooks/hooks.json` is declarative only; correctness depends on Claude actually loading the hook manifest and honoring matchers.
- validation_or_tests: Direct coverage exists in `tests/test-cancel-signal-file.sh`, which states its positive and negative scope for cancel-state behavior (`tests/test-cancel-signal-file.sh:3`, `tests/test-cancel-signal-file.sh:6`, `tests/test-cancel-signal-file.sh:8`). It constructs hook JSON and invokes `loop-bash-validator.sh` (`tests/test-cancel-signal-file.sh:61`, `tests/test-cancel-signal-file.sh:70`), expects the authorized cancel move to exit `0` with a signal file (`tests/test-cancel-signal-file.sh:78`, `tests/test-cancel-signal-file.sh:80`, `tests/test-cancel-signal-file.sh:88`), and expects block exit `2` without the signal or for other state modifications (`tests/test-cancel-signal-file.sh:119`, `tests/test-cancel-signal-file.sh:129`, `tests/test-cancel-signal-file.sh:139`, `tests/test-cancel-signal-file.sh:149`, `tests/test-cancel-signal-file.sh:179`, `tests/test-cancel-signal-file.sh:189`). `tests/test-state-exit-naming.sh` also validates that only `state.md` represents active state and reason-named files such as `cancel-state.md` do not. `tests/run-all-tests.sh` includes `test-bash-validator-patterns.sh`, `test-template-references.sh`, `test-state-exit-naming.sh`, and `test-cancel-signal-file.sh` in the suite (`tests/run-all-tests.sh` references found at lines 35, 38, 39, and 45 by search). I did not run tests because this task requested research notes only in a read-only branch export.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-045 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR stop-hook git-status gate. It is not state-transition logic, but it is part of the algorithm’s fail-closed validation surface: when repository state cannot be verified, the stop hook blocks exit and renders this message.
- algorithmic_behavior: The template has a heading and one variable interpolation, `{{GIT_STATUS_EXIT}}`, used to report the failed or timed-out git status exit code (`prompt-template/block/git-status-failed.md:1`, `prompt-template/block/git-status-failed.md:3`). It tells the agent why verification failed and lists likely causes: lock contention, invalid repository state, or slow large repository (`prompt-template/block/git-status-failed.md:5`, `prompt-template/block/git-status-failed.md:6`, `prompt-template/block/git-status-failed.md:7`, `prompt-template/block/git-status-failed.md:8`). It ends with a manual remediation instruction (`prompt-template/block/git-status-failed.md:10`).
- inputs_outputs_state: Input is the `GIT_STATUS_EXIT` value passed by `loop-codex-stop-hook.sh` when cached `git status --porcelain` fails or times out (`hooks/loop-codex-stop-hook.sh:310`, `hooks/loop-codex-stop-hook.sh:311`, `hooks/loop-codex-stop-hook.sh:313`). Output is rendered Markdown inserted into a JSON hook response as the `reason`, with `systemMessage` set to `Loop: Blocked - git status failed (exit ...)` (`hooks/loop-codex-stop-hook.sh:320`, `hooks/loop-codex-stop-hook.sh:322`, `hooks/loop-codex-stop-hook.sh:323`). It does not modify files or loop state directly; it prevents the Stop event from proceeding by returning a block decision and `exit 0` from the hook script (`hooks/loop-codex-stop-hook.sh:323`, `hooks/loop-codex-stop-hook.sh:324`).
- gates_or_invariants: The invariant is fail-closed repository verification. The stop hook explicitly says cached git status is used by later large-file and clean-worktree checks and must not fail open (`hooks/loop-codex-stop-hook.sh:297`, `hooks/loop-codex-stop-hook.sh:301`, `hooks/loop-codex-stop-hook.sh:302`). If `git status --porcelain` cannot complete successfully, the algorithm does not proceed to large-file, clean-tree, summary, or Codex review gates (`hooks/loop-codex-stop-hook.sh:313`, `hooks/loop-codex-stop-hook.sh:314`). This avoids allowing loop exit when the repository may be locked, corrupted, or too slow to inspect reliably.
- dependencies_and_callers: Sole direct caller found is `hooks/loop-codex-stop-hook.sh`, which loads the template via `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` (`hooks/loop-codex-stop-hook.sh:320`). The template path is validated by the template reference test, which scans shell scripts using `load_template`, `load_and_render`, and `load_and_render_safe` and checks the referenced files exist (`tests/test-template-references.sh:5`, `tests/test-template-references.sh:8`, `tests/test-template-references.sh:57`, `tests/test-template-references.sh:93`, `tests/test-template-references.sh:102`). The loader dependency comes through `hooks/lib/loop-common.sh`, which sources `template-loader.sh` and initializes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:46`, `hooks/lib/loop-common.sh:48`, `hooks/lib/loop-common.sh:51`).
- edge_cases_or_failure_modes: If `git` is not installed or the directory is not a git repository, this specific template is not used because the stop hook only enters the cached-status block after `command -v git` and `git rev-parse --git-dir` succeed (`hooks/loop-codex-stop-hook.sh:307`). If `git rev-parse` succeeds but `git status --porcelain` exits nonzero, times out through `run_with_timeout`, or fails due to lock contention, this template blocks exit (`hooks/loop-codex-stop-hook.sh:311`, `hooks/loop-codex-stop-hook.sh:313`). If the template is missing, `load_and_render_safe` uses the inline fallback in the caller, so blocking still happens but with less specific copy (`hooks/loop-codex-stop-hook.sh:315`, `hooks/loop-codex-stop-hook.sh:320`). The template itself does not distinguish timeout from other nonzero exit codes beyond reporting the numeric code.
- validation_or_tests: `tests/test-template-references.sh` is the direct safety net for this template class; it enumerates the stop hook and validators as scripts to scan (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:58`, `tests/test-template-references.sh:63`) and fails missing template references (`tests/test-template-references.sh:102`, `tests/test-template-references.sh:105`). Stop-hook plan tests cover many pre-review block paths, but I did not find a dedicated test that simulates `git status --porcelain` failure and asserts this exact rendered template. I did not run tests because this task requested read-only research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 Item Evidence sections present, each with one cursor
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`