# agent_27 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-027 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the test harness aggregation algorithm that strips ANSI color codes before extracting pass/fail counts. The file states it covers the portable ANSI stripping used by [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/run-all-tests.sh:65), especially GNU/Linux and BSD/macOS `sed` compatibility.
- algorithmic_behavior: The core transform is `sed "s/${esc}\\[[0-9;]*m//g"` with `esc=$'\033'`, first exercised in [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:45). After stripping, counts are extracted with `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'`, mirrored by the aggregate runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/run-all-tests.sh:67). The test cases cover basic color removal, multiple color spans, count extraction, zero counts, multi-line suite output, plain text, and combined bold/color sequences.
- inputs_outputs_state: Inputs are hardcoded terminal-output strings containing SGR ANSI sequences such as `\033[0;32m`, `\033[0;31m`, `\033[1m`, and `\033[1;32m`. Outputs are colored `PASS`/`FAIL` lines plus a final colored summary at [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:163). Internal state is limited to `TESTS_PASSED` and `TESTS_FAILED`, mutated only by `pass()` and `fail()` in [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:21). Exit state is binary: zero failures exits `0`; any failure exits `1` at [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:170).
- gates_or_invariants: The invariant is that ANSI-decorated summaries must parse identically to plain summaries. Multi-line parsing deliberately uses `tail -1` for passed/failed counts so the final matching summary wins when earlier lines also contain counters, shown in [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:124). The script uses `set -uo pipefail` but not `set -e`, so failed assertions accumulate instead of aborting early.
- dependencies_and_callers: It is listed as an aggregate suite in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/run-all-tests.sh:43). The aggregate runner uses the same ANSI stripping and counter extraction algorithm at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/run-all-tests.sh:65), making this file a regression guard for the runner’s own parsing. Runtime dependencies are Bash, `sed`, `grep -E/-o`, `tail`, and terminal-safe ANSI-C quoting.
- edge_cases_or_failure_modes: Covered edge cases include zero failed counts, no ANSI codes, multiple ANSI spans in one line, bold plus color, and complex multi-line output. Known limits: the regex strips CSI SGR sequences ending in `m` with digits and semicolons only; it does not remove non-SGR terminal controls such as cursor movement, erase-line codes, OSC sequences, or SGR forms using parameters outside `[0-9;]`. If a suite emits malformed summary labels or non-numeric counts, extraction falls back to `0` because of the `|| echo "0"` pattern.
- validation_or_tests: The file is itself the validation artifact. Its expected behavior is checked through eight explicit test sections from [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:41) through [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-ansi-parsing.sh:158). I did not execute it because this branch export was provided as read-only research context.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-057 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Hook-block template for the policy that agents should not use file-based `round-*-todos.md` workflows and should use native `TodoWrite` state instead. The template’s substantive rule is in [prompt-template/block/todos-file-access.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/prompt-template/block/todos-file-access.md:3), with the replacement mechanism stated at line 5.
- algorithmic_behavior: The file has no placeholders or branching logic; it is rendered as a static Markdown block by `todos_blocked_message()` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:190). That helper calls `load_and_render_safe "$TEMPLATE_DIR" "block/todos-file-access.md"`, so the template participates in validator decisions as the user-facing block reason rather than as the detector itself.
- inputs_outputs_state: Inputs are validator detections of paths or shell commands targeting todo files. Read, Write, and Edit validators lower-case the path for pattern detection, call `is_round_file_type "$FILE_PATH_LOWER" "todos"`, locate the active loop, then block unless the path is allowlisted. Bash validation uses command-pattern detection for modifications to `round-[0-9]+-todos.md`. Output is the rendered Markdown message to stderr and hook exit code `2` when blocked. The template itself mutates no repository state; the intended state transition is behavioral: move task tracking from file access to native `TodoWrite`, which keeps task state visible in the UI.
- gates_or_invariants: The base filename detector is `round-[0-9]+-todos\.md$` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:146). This branch has an explicit active-loop allowlist for `round-1-todos.md` and `round-2-todos.md` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:172), so the template is not emitted for those exact paths in the active loop. It remains the block contract for no active loop, wrong directory, old loop directory, generic relative todo filenames, and todo rounds outside the allowlist.
- dependencies_and_callers: Direct caller is `todos_blocked_message()` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:190). That helper is used by [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-read-validator.sh:36), [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-write-validator.sh:37), [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-edit-validator.sh:36), and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:298). Rendering depends on [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/template-loader.sh:170), which falls back to an inline message if the template is missing or empty.
- edge_cases_or_failure_modes: Because the template is static, the `action` argument passed to `todos_blocked_message "Read|Write|Bash"` is currently unused; all blocked actions receive the same wording. The message says not to create or access `round-*-todos.md`, while enforcement now permits exact active-loop `round-1` and `round-2` todo paths; this is not user-visible for those permitted cases, but the wording is broader than the validator’s current allowlist semantics. Bash enforcement is path-sensitive: it permits active-loop `round-[12]-todos.md` only when the full active-loop path is present, and blocks same-basename files in `/tmp`, old loop dirs, or relative paths.
- validation_or_tests: Template existence is checked in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-template-references.sh:152). Allowlist and blocking behavior for Read/Write/Edit/Bash validators is covered by [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-allowlist-validators.sh:70), including active-loop allow cases, blocked higher-round todos, wrong-directory todos, old-loop todos, and same-basename different-root attempts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`