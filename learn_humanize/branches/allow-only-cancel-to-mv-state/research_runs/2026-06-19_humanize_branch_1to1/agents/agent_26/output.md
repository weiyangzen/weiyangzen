# agent_26 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-026 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR hook allowlist exception path. It verifies that only a narrow set of generated loop files can bypass normal read/write/edit/todos blocking: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`, and only when they live under the active loop directory.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` at `tests/test-allowlist-validators.sh:15-17`, builds an isolated temporary git repository, creates `.humanize/rlcr/2024-01-01_12-00-00/state.md`, and sets `current_round: 5` in the state frontmatter at `tests/test-allowlist-validators.sh:29-62`. It then drives the same JSON hook inputs that the runtime validators receive. The first section directly asserts `is_allowlisted_file` accepts only the four exact active-loop paths and rejects `round-3-todos.md`, `round-2-summary.md`, and a matching filename in `/other/path` at `tests/test-allowlist-validators.sh:64-124`. Subsequent sections feed hook JSON to `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, and `loop-bash-validator.sh`, checking exit `0` for allowed cases and exit `2` with an error message mentioning todos or round for blocked cases.
- inputs_outputs_state: Inputs are shell hook payloads such as `{"tool_name":"Write","tool_input":{"file_path":".../round-1-todos.md"}}` and Bash commands like `echo test > <loop>/round-1-todos.md`. Outputs are validator process exit codes and stderr/stdout text captured into `RESULT`; `pass` and `fail` counters are updated through helper functions at `tests/test-allowlist-validators.sh:19-27`. The test state is an active loop discovered from the temp repo through `CLAUDE_PROJECT_DIR`, with `LOOP_DIR` reset by `setup_test_loop`. The script exits with `TESTS_FAILED`, so any failed invariant becomes a nonzero suite result at `tests/test-allowlist-validators.sh:383-391`.
- gates_or_invariants: The central invariant is exact path identity, not basename identity. The implementation contract is visible in `hooks/lib/loop-common.sh:168-186`: `is_allowlisted_file` loops over the four fixed filenames and returns success only when `file_path == active_loop_dir/allowed`. Write, edit, and read validators use this helper as an exception to their normal block on todos files or wrong-round summary files: write at `hooks/loop-write-validator.sh:37-44` and `hooks/loop-write-validator.sh:153-170`, edit at `hooks/loop-edit-validator.sh:36-43` and `hooks/loop-edit-validator.sh:112-137`, read at `hooks/loop-read-validator.sh:36-43` and `hooks/loop-read-validator.sh:110-123`. The Bash validator is stricter for todos: once `command_modifies_file` detects a modification to `round-[0-9]+-todos.md`, it requires the lowercased command string to contain the fully escaped active loop dir followed by `round-[12]-todos.md` at `hooks/loop-bash-validator.sh:298-305`.
- dependencies_and_callers: Direct dependencies include `git`, `jq`, `mktemp`, Bash, and the repo hooks under `hooks/`. The tests depend on `find_active_loop`, `parse_state_file`, `is_round_file_type`, `is_allowlisted_file`, and `command_modifies_file` from `hooks/lib/loop-common.sh`. Hook dispatch is configured by `hooks/hooks.json`, and runtime callers are Claude/Codex pre-tool validators for Read, Write, Edit, and Bash tool use. The Bash hook depends on `command_modifies_file` patterns for redirects, append redirects, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >` at `hooks/lib/loop-common.sh:397-425`.
- edge_cases_or_failure_modes: The suite explicitly covers wrong round, wrong directory, stale loop directory, relative basename-only paths, and same timestamp basename under a different root. Bash tests 21-25 block `/tmp/round-1-todos.md`, active-loop `round-3-todos.md`, relative `round-1-todos.md`, an older `.humanize/rlcr/2023...` loop, and `/tmp/.humanize/rlcr/<same-basename>/round-1-todos.md` at `tests/test-allowlist-validators.sh:314-381`. An important asymmetry is that Bash modifications to summary files are never allowlisted; the Bash hook blocks any detected summary modification and routes the user to Write/Edit at `hooks/loop-bash-validator.sh:283-292`, while Write/Edit/Read permit the two historical summary files through the allowlist. A missing active loop causes these validators to allow or skip some checks, so this suite creates a valid newest loop with `state.md` to test the guarded path.
- validation_or_tests: This file is itself the validation artifact. It contains 25 numbered tests: 7 direct helper tests, 4 Write validator tests, 3 Edit validator tests, 4 Read validator tests, and 7 Bash validator path-restriction tests. The positive cases validate allowed reads/writes/edits for active-loop `round-1` or `round-2` todos and historical `round-0` or `round-1` summaries. The negative cases validate exit `2` and message content for disallowed todos or wrong-round summaries. I did not execute the suite because the branch export is read-only and the test creates a temporary git repository plus commits; research requirements only required direct inspection.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-056 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: User-facing block template for the Bash validator’s summary-file write gate. It is not an algorithm implementation by itself, but it defines the review/transition contract presented when a user attempts to modify `round-N-summary.md` through Bash instead of validated Write/Edit tooling.
- algorithmic_behavior: The template has a title, a short prohibition, the corrected path placeholder, and the rationale that Bash commands bypass validation hooks at `prompt-template/block/summary-bash-write.md:1-8`. Its only dynamic field is `{{CORRECT_PATH}}` at line 5. The shared message function `summary_bash_blocked_message` renders this file through `load_and_render_safe` with `CORRECT_PATH=$correct_path` at `hooks/lib/loop-common.sh:217-226`. The Bash validator calls that function after `command_modifies_file "$COMMAND_LOWER" "round-[0-9]+-summary\.md"` detects a summary modification, then exits `2` at `hooks/loop-bash-validator.sh:283-292`.
- inputs_outputs_state: Input is a single template variable, `CORRECT_PATH`, normally set to `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` by the Bash validator at `hooks/loop-bash-validator.sh:288-290`. Output is rendered Markdown written to stderr through `summary_bash_blocked_message`. It does not mutate loop state; it supports the validator’s state transition from “Bash command requested” to “blocked with corrective path and tool guidance.”
- gates_or_invariants: The invariant enforced by this template is tool-channel separation: summary files must be modified through Write or Edit so round-number and directory validation still run. The template states that Bash commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks at `prompt-template/block/summary-bash-write.md:7`. The loader contract is single-pass placeholder substitution with unresolved placeholders preserved, described in `hooks/lib/template-loader.sh:50-132`; safe rendering falls back if the file is missing or empty at `hooks/lib/template-loader.sh:167-203`.
- dependencies_and_callers: Direct caller is `summary_bash_blocked_message` in `hooks/lib/loop-common.sh`. Runtime caller is `hooks/loop-bash-validator.sh` when it detects summary-file modification. The template is also covered structurally by `tests/test-template-references.sh`, which lists `block/summary-bash-write.md` among required common templates at `tests/test-template-references.sh:149-167`. Related rendering behavior is exercised by template-loader integration tests around real template substitution at `tests/test-template-loader.sh:146-164` and `tests/test-templates-comprehensive.sh:494-514`, although those examples use another block template.
- edge_cases_or_failure_modes: If `CORRECT_PATH` is missing, the placeholder remains visible because `render_template` preserves missing variables at `hooks/lib/template-loader.sh:119-122`; that would make the corrective instruction less useful but still blocks the unsafe action. If the template is missing or empty, `load_and_render_safe` uses the fallback text embedded in `summary_bash_blocked_message` at `hooks/lib/loop-common.sh:221-225`. The template is intentionally Bash-specific; Write/Edit wrong-round or wrong-directory guidance comes from separate templates and validators, so this file should not be used to authorize any Bash summary write.
- validation_or_tests: Indirect validation comes from the Bash validator tests in `tests/test-allowlist-validators.sh`: summary Bash modification is not among allowed Bash cases, and the tested allowlist only permits Bash writes to active-loop `round-1-todos.md` and `round-2-todos.md` at `tests/test-allowlist-validators.sh:288-312`. Template presence is checked by `tests/test-template-references.sh:149-167`. The loader’s substitution/fallback behavior is defined in `hooks/lib/template-loader.sh:167-203`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2, each represented once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`