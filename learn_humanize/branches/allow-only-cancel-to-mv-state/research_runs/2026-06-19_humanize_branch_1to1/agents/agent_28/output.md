# agent_28 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-028 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash command write-detection algorithm used by RLCR loop hooks. The file directly tests `command_modifies_file` from `hooks/lib/loop-common.sh`, which is the shared predicate used by `hooks/loop-bash-validator.sh` to block shell-based edits to protected loop files.
- algorithmic_behavior: The script normalizes every candidate command with `to_lower` before validation, then calls `command_modifies_file "$command_lower" "$pattern"` through two assertion helpers: `assert_modifies` and `assert_not_modifies` at `tests/test-bash-validator-patterns.sh:36` and `tests/test-bash-validator-patterns.sh:50`. It verifies detection for redirection operators, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`/`rm`, `dd`, `truncate`, `exec` redirection, and `printf` redirection across test groups at `tests/test-bash-validator-patterns.sh:69`, `tests/test-bash-validator-patterns.sh:83`, `tests/test-bash-validator-patterns.sh:96`, `tests/test-bash-validator-patterns.sh:110`, and `tests/test-bash-validator-patterns.sh:124`. It also verifies false-positive avoidance for read-only commands such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `stat`, and `diff` at `tests/test-bash-validator-patterns.sh:136`.
- inputs_outputs_state: Inputs are shell command strings and a target file regex, defaulting to `goal-tracker\.md` in `assert_modifies` and `assert_not_modifies` at `tests/test-bash-validator-patterns.sh:39` and `tests/test-bash-validator-patterns.sh:53`. Outputs are pass/fail counters, colored console messages, and process exit status: success exits `0` only when `TESTS_FAILED` is zero at `tests/test-bash-validator-patterns.sh:206`; failure exits `1` at `tests/test-bash-validator-patterns.sh:210`. The script does not mutate repository state; its only internal state is `TESTS_PASSED` and `TESTS_FAILED` at `tests/test-bash-validator-patterns.sh:20`.
- gates_or_invariants: The key invariant is that write-like Bash forms targeting protected filenames must be detected, while read-only inspection forms must not be detected. The state-file-specific checks at `tests/test-bash-validator-patterns.sh:175` require writes to `state.md` to match and `cat state.md` to remain allowed. The summary-file regex checks at `tests/test-bash-validator-patterns.sh:186` require numbered `round-N-summary.md` write detection. The test explicitly documents a known limitation: multi-source `cp file1 file2 goal-tracker.md` is not covered because the underlying pattern expects a two-argument source/destination shape at `tests/test-bash-validator-patterns.sh:165`.
- dependencies_and_callers: The test depends on `hooks/lib/loop-common.sh`, sourced at `tests/test-bash-validator-patterns.sh:13`. The tested implementation is `command_modifies_file` in `hooks/lib/loop-common.sh:400`, whose regex table covers redirection, `tee`, in-place editors, `mv`/`cp`, `rm`, `dd`, `truncate`, `printf`, and `exec` at `hooks/lib/loop-common.sh:404`. The production caller is `hooks/loop-bash-validator.sh`, which applies the predicate to protected files including `state.md` at `hooks/loop-bash-validator.sh:81`, plan backups at `hooks/loop-bash-validator.sh:249`, goal tracker files at `hooks/loop-bash-validator.sh:262`, and prompt files at `hooks/loop-bash-validator.sh:278`. For this branch’s cancel-state behavior, the generic detection gate is paired with `is_cancel_authorized` checks in `hooks/loop-bash-validator.sh:82`, `hooks/loop-bash-validator.sh:218`, and `hooks/loop-bash-validator.sh:231`.
- edge_cases_or_failure_modes: The tests cover case-insensitive detection through lowercasing with `ECHO X > GOAL-TRACKER.MD` at `tests/test-bash-validator-patterns.sh:80`, compact redirection with `>goal-tracker.md` at `tests/test-bash-validator-patterns.sh:163`, and variable targets that should not match literal protected filenames at `tests/test-bash-validator-patterns.sh:170`. The implementation’s simple regex approach means quoted paths with spaces, multi-source copies, or more complex shell parsing can be outside this specific test’s scope. The branch-specific cancel exception is not part of this assigned test file itself; it is enforced by the production validator using `is_cancel_authorized`, which requires a `.cancel-requested` signal and an exact `mv state.md cancel-state.md` shape in `hooks/lib/loop-common.sh:251`.
- validation_or_tests: This file is itself a validation script with grouped executable assertions and a hard failing exit status. I inspected it directly rather than modifying or running repository code, consistent with the read-only research request. Related broader validation exists in `tests/test-cancel-signal-file.sh`, which exercises the cancel-signal authorization path referenced by the branch name, but that file was not one of this worker’s assigned one-to-one items.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-058 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Prompt block template used by the Codex stop hook to block loop exit when `--push-every-round` is enabled and the current branch has local commits ahead of its remote. It defines the user-facing transition from “attempting to stop” to “blocked until push completes.”
- algorithmic_behavior: The template renders a Markdown block headed `Unpushed Commits Detected` at `prompt-template/block/unpushed-commits.md:1`. It interpolates `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}` into the blocking explanation at `prompt-template/block/unpushed-commits.md:3`, then emits a required `git push origin {{CURRENT_BRANCH}}` action at `prompt-template/block/unpushed-commits.md:7`. In production, `hooks/loop-codex-stop-hook.sh` computes ahead status only when `PUSH_EVERY_ROUND` is true at `hooks/loop-codex-stop-hook.sh:477`, extracts the ahead count from `git status -sb` at `hooks/loop-codex-stop-hook.sh:479`, resolves the current branch with `git rev-parse --abbrev-ref HEAD` at `hooks/loop-codex-stop-hook.sh:482`, and renders this template through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:489`.
- inputs_outputs_state: Template inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`. Its rendered output is assigned to `REASON` in the stop hook and placed into the JSON block response as `reason`, while `systemMessage` summarizes the count in `hooks/loop-codex-stop-hook.sh:493`. The template itself has no persistent state. The relevant state gate is the loop state field `push_every_round`, exposed in the stop hook as `PUSH_EVERY_ROUND`; if not true, this specific unpushed-commit block is bypassed.
- gates_or_invariants: The gate invariant is: when push-every-round mode is active, the loop must not exit while the local branch is ahead of remote. The required action is specifically `git push origin {{CURRENT_BRANCH}}`, so the rendered block ties the unblock operation to the detected branch rather than a generic push. The template assumes the hook has already established that there is a non-empty ahead marker; it does not encode fallback behavior itself.
- dependencies_and_callers: Direct production caller: `hooks/loop-codex-stop-hook.sh:489`. Rendering depends on the template loader’s safe substitution path via `load_and_render_safe`. The template is covered by `tests/test-templates-comprehensive.sh:517`, which renders it with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserts the output contains the heading, count text, and branch name at `tests/test-templates-comprehensive.sh:522`. It belongs to the `prompt-template/block/` family of stop/block messages, alongside other templates for schema, plan-file, and git-state gates.
- edge_cases_or_failure_modes: If branch detection fails, the caller supplies `unknown` as the branch name through `git rev-parse ... || echo "unknown"` at `hooks/loop-codex-stop-hook.sh:482`, which would render a potentially non-actionable `git push origin unknown`. Ahead detection depends on parsing `git status -sb` for `ahead N`; unusual localized git output or detached HEAD states may bypass or degrade the message. The template also assumes `origin` is the correct remote, because the required command hardcodes `origin` at `prompt-template/block/unpushed-commits.md:9`.
- validation_or_tests: The assigned template has explicit coverage in `tests/test-templates-comprehensive.sh:517`, validating successful substitution for count and branch. The stop-hook path additionally wraps the rendered template into a JSON block decision at `hooks/loop-codex-stop-hook.sh:493`, so validation should include both template rendering and hook behavior when push-every-round mode is enabled.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-028`, `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-058`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`