# agent_01 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the full Humanize Claude Code plugin algorithm surface. Recursive inspection found the plugin metadata, slash-command docs, setup/monitor scripts, hook registrations, validation hooks, template system, block/review templates, and shell/Python test suites. `hooks/hooks.json:4` wires `UserPromptSubmit`, `PreToolUse` for `Write`/`Edit`/`Read`/`Bash`, and `Stop` to the core hook scripts.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` starts an RLCR loop by validating one plan file, creating `.humanize/rlcr/<timestamp>/`, copying `plan.md`, writing `state.md`, seeding `goal-tracker.md`, and emitting `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:227`, `:485`, `:507`, `:602`). Validators guard reads/writes/bash operations while `hooks/loop-codex-stop-hook.sh` reviews attempted exits, blocks on unmet gates, advances `current_round`, or ends the loop by renaming state.
- inputs_outputs_state: Inputs are Claude hook JSON, slash-command arguments, repo git state, plan files, active loop files, transcript todo state, templates, and Codex review output. Outputs are JSON block decisions, exit-code blocks for PreToolUse, generated loop files, review prompts/results, cache logs, and terminal state files. Active state is the newest `.humanize/rlcr/*/state.md`; `find_active_loop` ignores ended `*-state.md` files (`hooks/lib/loop-common.sh:62`).
- gates_or_invariants: State schema requires `plan_tracked` and `start_branch`; branch must not change; plan file path must be relative, in-project, non-symlink, no spaces/metacharacters, and tracked/ignored according to setup mode (`scripts/setup-rlcr-loop.sh:227`, `:331`). `state.md` is immutable except the cancel path: `is_cancel_authorized` requires `.cancel-requested` and exactly `mv <active>/state.md <active>/cancel-state.md`, rejecting substitutions, extra args, chaining, pipes, hidden variables, and wrong destinations (`hooks/lib/loop-common.sh:261`). Bash validator enforces that exception while blocking normal `state.md` modification (`hooks/loop-bash-validator.sh:81`, `:102`).
- dependencies_and_callers: Shared `hooks/lib/loop-common.sh` parses state, detects loop files, renders common block messages, authorizes cancel, and performs `end_loop` renames (`:110`, `:448`). `hooks/lib/template-loader.sh` provides single-pass template rendering and fallbacks. `scripts/portable-timeout.sh` wraps git/Codex calls. `commands/start-rlcr-loop.md` calls setup; `commands/cancel-rlcr-loop.md:30` documents the signal-file-then-rename sequence. CI workflows run shell syntax, template validation, and plan/cancel suites.
- edge_cases_or_failure_modes: Git timeouts fail closed; missing/outdated state fields block; plan backup missing/deleted/modified blocks; uncommitted changes, large files, incomplete todos, missing summaries, unpushed commits when configured, and uninitialized goal tracker block stop. Bash detection covers redirection, `tee`, in-place editors, `mv`/`cp`/`rm`, shell wrappers, command chaining, glob/brace/substitution bypasses, and direct `.humanize/rlcr/plan.md` paths. Legacy `.humanize-loop.local` paths are intentionally not protected by current loop validators.
- validation_or_tests: Test coverage is broad: `tests/run-all-tests.sh:32` orders suites for template loader, bash patterns, todo checker, plan validation, template references, state naming, comprehensive templates, plan hooks, error scenarios, ANSI parsing, allowlist validators, and cancel signal. `plan-file-test.yml:42` runs plan-file validation, hook, state-exit, and cancel-signal suites. I did not execute tests because this assignment is read-only research notes only.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-031 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for plan-file and loop-file hook behavior. It models realistic RLCR sessions in temporary git repos, then asserts hook decisions, exit codes, and block messages across user prompt, PreToolUse, Bash, and Stop hooks.
- algorithmic_behavior: `setup_test_loop` creates a git repo, ignored plan file, loop directory, plan backup, and `state.md` with `current_round`, `max_iterations`, quoted `plan_file`, `plan_tracked`, and `start_branch` (`tests/test-plan-file-hooks.sh:37`). Tests 1-4 verify `loop-plan-file-validator.sh` allows valid state, strips YAML quotes, blocks missing schema fields, and blocks branch changes (`:91`, `:106`, `:121`, `:141`, `:164`). Tests 5-8.5 validate Write/Edit/Bash protection of plan backups and bypass attempts (`:194`, `:211`, `:228`, `:241`, `:254`, `:272`). Tests 9-14 validate Stop-hook plan integrity for modified, deleted, missing-backup, tracked dirty, outdated schema, and committed-but-different plan content (`:469`, `:509`, `:543`, `:562`, `:631`, `:655`). Tests 14.1-14.4 verify section-specific goal-tracker placeholder reporting (`:737`, `:809`, `:878`, `:945`). Tests 15-17 assert legacy path allowance (`:1016`).
- inputs_outputs_state: Inputs are generated hook JSON such as `{"tool_name":"Write","tool_input":{"file_path":...}}`, Bash command strings, `{}` Stop/UserPromptSubmit payloads, repo files, `CLAUDE_PROJECT_DIR`, and git state. Outputs are `PASS`/`FAIL` counters and process exits; expected hook outputs are empty success, exit `2` PreToolUse blocks, or JSON block payloads with reason text.
- gates_or_invariants: Plan backup under `.humanize/rlcr/.../plan.md` must not be written, edited, removed, or overwritten through Bash. `plan_file` and `start_branch` quote stripping must preserve semantics. Tracked plans must remain tracked and clean; all plans must match loop backup content. `goal-tracker.md` Round 0 initialization must report only sections still containing placeholders. Current loop path is `.humanize/rlcr/`, not legacy `.humanize-loop.local`.
- dependencies_and_callers: Directly invokes `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Those depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `jq`, `git`, `diff`, and in Stop-hook paths sometimes `python3` todo parsing and `codex`.
- edge_cases_or_failure_modes: Covers default branch name variation (`main`/`master`), YAML quotes, hyphenated plan paths, direct `.humanize/rlcr/plan.md` without timestamp segment, command substitution `$()` and backticks, glob and brace expansion, piped `tee`, tracked plan race conditions, committed plan modifications with clean git status, schema downgrade, and placeholder false positives scoped to individual goal-tracker sections.
- validation_or_tests: This file is itself the validation asset. CI runs it from `.github/workflows/plan-file-test.yml:47`, and aggregate test runner includes it at `tests/run-all-tests.sh:41`. It exits with `TESTS_FAILED`, so any failed assertion fails the suite (`tests/test-plan-file-hooks.sh:1056`).
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-061 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Markdown block template used when Claude attempts to read a round prompt/summary file from outside the active `.humanize/rlcr/<timestamp>/` loop directory. It is part of the validator-to-user feedback contract.
- algorithmic_behavior: Template renders a title, identifies the attempted `FILE_PATH`, points to `ACTIVE_LOOP_DIR`, lists current-round prompt and summary paths using `CURRENT_ROUND`, and provides a command-style hint (`prompt-template/block/wrong-file-location.md:1`, `:3`, `:5`, `:9`).
- inputs_outputs_state: Inputs are template variables `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Output is a Markdown reason sent to stderr by `hooks/loop-read-validator.sh` when a blocked read occurs. It does not mutate state.
- gates_or_invariants: The caller gate is in `hooks/loop-read-validator.sh:94`: if the requested round prompt/summary is not inside `.humanize/rlcr`, read access is blocked and this template is rendered with active-loop context (`:99`). The invariant is that prompt/summary reads must target the active loop directory and current round unless explicitly allowlisted.
- dependencies_and_callers: Called via `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-file-location.md"` in `hooks/loop-read-validator.sh:99`. Rendering depends on `hooks/lib/template-loader.sh`, whose substitution is single-pass and leaves missing variables unchanged (`hooks/lib/template-loader.sh:50`, `:71`) with fallback handling at `:170`.
- edge_cases_or_failure_modes: If no active loop exists, the read validator allows the read before this template is used. If the file is in the active loop directory but wrong round, `wrong-round-file.md` is used instead. If variables are missing, placeholders remain visible. The final hint uses the attempted `FILE_PATH`; for a blocked wrong-location read this can be ambiguous because the actionable active paths are listed above it.
- validation_or_tests: Template existence is checked by `tests/test-template-references.sh:56`; load/render behavior is covered by `tests/test-template-loader.sh:41` and comprehensive template loading by `tests/test-templates-comprehensive.sh:87`. The read-validator behavior using this template is part of the broader validator suites.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present; each assigned row handled separately`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`