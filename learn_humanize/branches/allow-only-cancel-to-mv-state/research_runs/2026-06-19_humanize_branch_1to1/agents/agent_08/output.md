# agent_08 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`
- note: inspected the assigned read-only branch export only; no files were modified. I did not execute the shell test suite because this run requested research notes only and the sandbox is read-only.

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-008 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: `hooks/lib` is the shared library layer for the RLCR hook state machine. It contains exactly two recursive descendants: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. The directory is not an executable hook itself; it supplies common state parsing, active-loop discovery, file/command classification, cancellation authorization, end-state renaming, and template-backed block-message rendering to sibling hook entrypoints.
- algorithmic_behavior: `loop-common.sh` defines the RLCR state field names, exit reason names, completion markers, active loop lookup, frontmatter parsing, round-file classifiers, allowlist checks, state-file protection helpers, cancel authorization, shell command mutation detection, and `end_loop` state renaming. `template-loader.sh` resolves the plugin `prompt-template` directory, loads markdown templates, performs single-pass placeholder replacement, provides fallback-safe rendering, and validates the template directory shape. `loop-common.sh` sources `template-loader.sh` at `hooks/lib/loop-common.sh:46-51`, so every hook sourcing `loop-common.sh` inherits the same template rendering behavior.
- inputs_outputs_state: Inputs are hook-provided file paths and Bash command strings, `.humanize/rlcr/<timestamp>/state.md`, `.humanize/rlcr/<timestamp>/.cancel-requested`, and templates under `prompt-template/{block,codex,claude}`. Outputs are function return codes, rendered block messages to stderr/stdout, parsed `STATE_*` shell variables, active loop paths, and final state files such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`. The shared state convention is that only a file named `state.md` makes a loop active; renamed state files are terminal archives.
- gates_or_invariants: `find_active_loop` inspects only the newest timestamp directory and only reports it active when `state.md` exists, preventing older loop directories from being revived (`hooks/lib/loop-common.sh:58-79`). `template-loader.sh` requires `block`, `codex`, and `claude` template subdirectories for a valid template root (`hooks/lib/template-loader.sh:205-222`). `loop-common.sh` centralizes the invariant that `state.md` is system-managed: general Write/Edit/Bash modification attempts are blocked by sibling validators, with a narrow cancel exception implemented through `is_cancel_authorized`.
- dependencies_and_callers: Sibling hook entrypoints source `loop-common.sh`: `hooks/loop-read-validator.sh:14-16`, `hooks/loop-write-validator.sh:15-17`, `hooks/loop-edit-validator.sh:14-16`, `hooks/loop-bash-validator.sh:14-16`, `hooks/loop-plan-file-validator.sh:16-17`, and `hooks/loop-codex-stop-hook.sh:46-48`. `loop-common.sh` depends on `template-loader.sh`, plus standard shell tools such as `sed`, `grep`, `tr`, `ls`, `sort`, `head`, and `mv`. `template-loader.sh` depends on `awk`, `env`, and `cat`. Parent coordination is with `prompt-template` for rendered user-facing hook messages and with `.humanize/rlcr` state directories created by setup scripts.
- edge_cases_or_failure_modes: `find_active_loop` intentionally ignores older active-looking directories when the newest directory lacks `state.md`; this can hide an older loop after a partial newer loop directory is created. `parse_state_file` is a regex/frontmatter parser, not YAML parsing, so duplicate or malformed fields can produce surprising values. Numeric validation is not uniform across all callers; the stop hook validates corrupted `current_round`, but validators using arithmetic comparisons can still fail if the state was manually corrupted. `template-loader.sh` preserves unresolved placeholders and does not re-scan replacement values, which prevents placeholder injection but also leaves missing variables visible. `is_in_humanize_loop_dir` is substring-based and does not canonicalize paths. The directory’s safety posture relies on the sibling validators consistently sourcing this library.
- validation_or_tests: Relevant read-only evidence includes `tests/test-template-loader.sh:42-240` for template directory resolution, loading, rendering, fallback behavior, and validation; `tests/test-template-references.sh:56-200` for existence of referenced templates and safe rendering usage in critical validators; `tests/test-state-exit-naming.sh:38-239` for active-loop detection and `end_loop` terminal filenames; and `tests/test-allowlist-validators.sh:64-170` for shared allowlist behavior. These were inspected, not executed.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-038 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: `hooks/lib/loop-common.sh` is the central shared algorithm file for the RLCR loop hook state machine. It provides the constants and functions that determine which loop is active, how `state.md` is parsed, which round files are protected, when state-file mutation is blocked, when cancel-driven movement of `state.md` is authorized, and how terminal loop states are encoded by renaming `state.md`.
- algorithmic_behavior: The file defines state field constants and exit reasons at `hooks/lib/loop-common.sh:15-40`, loads the template library at `hooks/lib/loop-common.sh:46-56`, and locates the active loop by choosing the newest `.humanize/rlcr/*/` directory only if it contains `state.md` at `hooks/lib/loop-common.sh:62-79`. It parses state frontmatter into `STATE_*` globals at `hooks/lib/loop-common.sh:110-137`, classifies round files with `is_round_file_type` and `extract_round_number` at `hooks/lib/loop-common.sh:144-163`, allows only a small historical bootstrap set of round files at `hooks/lib/loop-common.sh:168-186`, renders standard block messages at `hooks/lib/loop-common.sh:188-237` and `hooks/lib/loop-common.sh:427-439`, identifies protected paths at `hooks/lib/loop-common.sh:239-249`, detects shell commands that modify protected files at `hooks/lib/loop-common.sh:397-425`, and ends loops by moving `state.md` to `<reason>-state.md` at `hooks/lib/loop-common.sh:441-474`.
- inputs_outputs_state: Primary inputs are `state.md`, an active loop directory, hook file paths, lowercased shell command text, and optional template variables. `parse_state_file` reads YAML-like frontmatter and sets `STATE_PLAN_TRACKED`, `STATE_START_BRANCH`, `STATE_PLAN_FILE`, `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PUSH_EVERY_ROUND`, `STATE_CODEX_MODEL`, `STATE_CODEX_EFFORT`, and `STATE_CODEX_TIMEOUT`; it defaults current round to `0`, max iterations to `10`, and push-every-round to `false` at `hooks/lib/loop-common.sh:131-135`. `is_cancel_authorized` returns success or failure only; it does not move files itself. `end_loop` performs the actual terminal state transition by renaming `state.md` when called by the stop hook or other loop manager code.
- gates_or_invariants: The branch-relevant gate is `is_cancel_authorized` at `hooks/lib/loop-common.sh:251-389`. It requires `.cancel-requested` in the active loop directory, rejects command substitution/backticks, rejects newlines, rejects shell chaining operators, normalizes literal `$loop_dir` or `${loop_dir}` to the active path, rejects any remaining dollar sign, requires the command to start with bare `mv`, parses exactly two arguments, rejects extra arguments, normalizes `/./` and repeated slashes, and requires the source to be exactly `<active_loop_dir>/state.md` and the destination exactly `<active_loop_dir>/cancel-state.md`. This creates the “only cancel may move state” exception consumed by `hooks/loop-bash-validator.sh`: destination/source state-file checks call `is_cancel_authorized` before otherwise blocking at `hooks/loop-bash-validator.sh:81-89`, `hooks/loop-bash-validator.sh:218-225`, and `hooks/loop-bash-validator.sh:231-240`.
- dependencies_and_callers: `loop-common.sh` depends directly on `hooks/lib/template-loader.sh` and the repository’s `prompt-template` directory. It is called by the read/write/edit/bash validators and by the plan-file and Codex stop hooks. The stop hook uses its parsed state fields at `hooks/loop-codex-stop-hook.sh:76-88`, validates model/effort/current round before continuing at `hooks/loop-codex-stop-hook.sh:90-107`, and invokes `end_loop` for unexpected, max-iteration, complete, and stop outcomes at `hooks/loop-codex-stop-hook.sh:93-106`, `hooks/loop-codex-stop-hook.sh:616`, `hooks/loop-codex-stop-hook.sh:929`, and `hooks/loop-codex-stop-hook.sh:959`. The user-facing cancel command creates the signal then runs the permitted move at `commands/cancel-rlcr-loop.md:28-32`.
- edge_cases_or_failure_modes: `is_cancel_authorized` intentionally refuses common but non-canonical cancel forms such as `mv -f`, `mv --`, leading whitespace before `mv`, chained commands, wrappers, or remaining shell variables. Quoted two-argument forms are supported, but escaped-space argument parsing is not generally supported for the authorized move. Path normalization handles `/./` and repeated slashes but not `..` components or symlink resolution. Because the hook lowercases the command before passing it in, case-sensitive filesystems with uppercase path components are a residual risk: the authorization comparison is case-normalized while the shell executes the original command. `end_loop` will overwrite an existing `<reason>-state.md` if `mv` semantics allow it, and there is no preflight for that. `command_modifies_file` is regex-based and may miss complex shell syntax outside the additional state-file source checks in the Bash validator.
- validation_or_tests: `tests/test-cancel-signal-file.sh` directly targets this file’s cancel gate. It covers allowed cancel with signal and exact move (`tests/test-cancel-signal-file.sh:78-113`), blocked missing/wrong signal and non-cancel writes (`tests/test-cancel-signal-file.sh:119-235`), injection via operators/substitution/newline (`tests/test-cancel-signal-file.sh:241-355`), quoted paths and literal `${loop_dir}` normalization (`tests/test-cancel-signal-file.sh:363-483`), extra-argument and hidden-variable rejection (`tests/test-cancel-signal-file.sh:424-543`), prefix/wrapper/chaining/redirection bypass attempts (`tests/test-cancel-signal-file.sh:550-1204`), wrong-directory `state.md` movement (`tests/test-cancel-signal-file.sh:1210-1225`), no-active-loop behavior (`tests/test-cancel-signal-file.sh:1231-1247`), and direct helper tests (`tests/test-cancel-signal-file.sh:1257-1341`). `tests/test-state-exit-naming.sh:38-239` validates active state naming and `end_loop` reason outputs.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-068 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: This is a small Claude prompt fragment that encodes the behavioral contract immediately after a Codex Full Goal Alignment Check. It is core-adjacent rather than executable: it influences the next RLCR round’s work priorities after an alignment review finds unresolved issues.
- algorithmic_behavior: The template has no variables and is loaded as literal markdown. Its content tells the next worker round to pay attention to forgotten items, unmet acceptance criteria, and unjustified deferrals at `prompt-template/claude/post-alignment-action-items.md:2-7`. The stop hook appends it only after Full Alignment Check rounds, computed as `CURRENT_ROUND % 5 == 4` at `hooks/loop-codex-stop-hook.sh:644-648`, and only on the “continue loop” path after Codex did not terminate the loop. The append happens after the next-round prompt is rendered and before the footer/push/goal-tracker request sections at `hooks/loop-codex-stop-hook.sh:972-1002`.
- inputs_outputs_state: Inputs are the stop hook’s current round number and the existence/content of this template file. There are no template variables. Output is appended markdown in `round-<NEXT_ROUND>-prompt.md`. It does not mutate `state.md` directly; the related state transition is performed earlier on the continue path when `current_round` is advanced with a temp file and `mv` at `hooks/loop-codex-stop-hook.sh:967-970`.
- gates_or_invariants: The gate is purely round-based and template-presence-based. The hook loads this file with `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"` and appends it only if the loaded content is non-empty at `hooks/loop-codex-stop-hook.sh:990-995`. Since this uses `load_template` rather than `load_and_render_safe`, a missing or empty file silently removes the special post-alignment instructions from future prompts.
- dependencies_and_callers: The sole caller found is `hooks/loop-codex-stop-hook.sh:990-995`. The template depends on the template loader path established by `loop-common.sh` and `template-loader.sh`. It coordinates with Codex full-alignment review prompts, particularly the full alignment review path selected at `hooks/loop-codex-stop-hook.sh:679-692`.
- edge_cases_or_failure_modes: The template is generic and does not include the actual Codex findings, so it relies on `{{REVIEW_CONTENT}}` already being included in the main next-round prompt. If the file is missing, empty, or not discoverable through `TEMPLATE_DIR`, the next prompt still exists but loses these targeted post-alignment reminders. Because the hook appends only when the prior round number satisfies the full-alignment condition, completion or STOP outcomes do not receive this block, which matches the continue-only use case.
- validation_or_tests: `tests/test-template-references.sh:56-111` scans template references from shell scripts, including the stop hook’s `load_template` call, and verifies referenced files exist. There does not appear to be a dedicated semantic test asserting this exact block is appended after alignment rounds; the behavior is evidenced by the stop hook code path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-008`, `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-038`, `ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-068`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`