# agent_29 allow-only-cancel-to-mv-state 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `3c39192e9b738743cdc5305a83a797a5ca66889b`

## Item Evidence

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-029 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable regression specification for the RLCR cancellation/state-file protection algorithm. It verifies the only permitted mutation of an active loop `state.md` via Bash is the documented cancellation transition: create `.cancel-requested`, then `mv state.md cancel-state.md`. The test directly exercises [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:81) and `is_cancel_authorized` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:251).
- algorithmic_behavior: The file builds a temporary active loop directory with `.humanize/rlcr/<timestamp>/state.md`, exports `CLAUDE_PROJECT_DIR`, wraps candidate Bash commands into Claude hook JSON using `jq`, then pipes them through `hooks/loop-bash-validator.sh` and checks exit codes. Test setup is defined at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:42), and hook simulation is at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:61). The expected transition is positive only when `.cancel-requested` exists and the command is exactly `mv <loop>/state.md <loop>/cancel-state.md`, including normalized `/./` paths and quoted paths. That positive behavior is covered at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:78), [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:98), and [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:363).
- inputs_outputs_state: Inputs are candidate Bash command strings, a synthetic active loop directory, `state.md`, optional `.cancel-requested`, and the shared hook library. Outputs are pass/fail counters plus process exit status equal to the number of failed assertions. The validator output contract under test is exit `0` for allowed commands and exit `2` for blocked state-file mutations. State transition under test is conceptual, not performed by this test: active loop state is represented by `state.md`; authorized cancellation requires signal file `.cancel-requested` and renames to `cancel-state.md`. The command contract mirrors [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/commands/cancel-rlcr-loop.md:28), which instructs the agent to touch the cancel signal first, then rename the state file.
- gates_or_invariants: The key invariant is that active loop lifecycle state is controlled by state-file presence and name. `state.md` means active; `cancel-state.md` means cancelled. This file asserts that the cancellation exception is narrow: no signal means blocked, wrong signal directory means blocked, wrong destination means blocked, unrelated `state.md` paths are blocked, extra arguments are blocked, hidden variables are blocked, and general edits/removals/copies are blocked. The implementation’s explicit security notes are at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:255): normalize `$loop_dir`, reject command substitution/backticks, reject remaining `$`, enforce exactly two args, and reject chaining. The validator applies three layers: destination `state.md` modification detection, source `state.md` `mv|cp` detection after segment splitting, and shell-wrapper detection for `sh -c`/`bash -c` payloads. Those gates are at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:81), [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:91), and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:228).
- dependencies_and_callers: The test depends on `bash`, `jq`, `mktemp`, `sed`, `grep`, the shared `to_lower`, `find_active_loop`, `command_modifies_file`, and `is_cancel_authorized` helpers from [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:58) and [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/loop-common.sh:397). It calls the real pre-tool hook script [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:22), which expects JSON fields `tool_name` and `tool_input.command`. It also directly calls `is_cancel_authorized` for cases that are awkward to encode through JSON, such as embedded newlines and literal `$loop_dir` syntax.
- edge_cases_or_failure_modes: The coverage is intentionally adversarial. It checks no-signal denial, signal in parent directory denial, arbitrary destination denial, command injection through `&&`, `;`, `$()`, backticks, and newline, option-prefixed `mv --`, `cp -f`, extra args before and after the source, literal `$loop_dir` normalization, `${IFS}` hidden-argument injection, `sudo`, `env`, `command --`, chained operators `;`, `&&`, `||`, `|`, `|&`, background `&` variants, shell wrappers, redirection-prefixed commands, quoted redirection targets, escaped-space targets, ANSI-C quoted targets, wrong active directory, and no-active-loop behavior. The no-active-loop case intentionally allows commands because the hook exits early when no newest active `state.md` exists; see [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:1231) and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-bash-validator.sh:40). One notable implementation boundary is that `is_cancel_authorized` is a shell-pattern parser, not a full shell parser; this test encodes many known bypass forms to pin the supported grammar.
- validation_or_tests: This file is itself the validation artifact. It contains 5 positive validator/helper scenarios, 53 negative validator scenarios, and 7 direct helper assertions, ending with `exit $TESTS_FAILED` at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-cancel-signal-file.sh:1347). I did not execute it because this worker run requested research notes only and the branch export is read-only; the inspection confirms the file is an executable shell test with temporary-directory setup and no intended repository writes.
- skip_candidate: `no`

### ALLOW_ONLY_CANCEL_TO_MV_STATE-HZ-059 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Prompt/block template for a loop stop gate: it defines the user-facing block reason when an agent attempts to exit an RLCR round without writing the required work summary. Although not part of the cancellation `mv state.md` path, it is core loop-control surface because it blocks state advancement until summary evidence exists.
- algorithmic_behavior: The template is a short Markdown message with one placeholder, `{{SUMMARY_FILE}}`, at [prompt-template/block/work-summary-missing.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/prompt-template/block/work-summary-missing.md:5). It tells the agent the attempted exit is blocked, provides the exact required summary path, lists required summary contents, and permits exit retry after the summary is written. It is rendered by `load_and_render_safe` in the Codex stop hook when the current round summary file is absent; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-codex-stop-hook.sh:510).
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable supplied by the stop hook as `$LOOP_DIR/round-${CURRENT_ROUND}-summary.md`. The rendered output becomes the JSON `reason` field in a hook response with `"decision": "block"` and system message `"Loop: Summary file missing for round $CURRENT_ROUND"` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-codex-stop-hook.sh:522). State does not mutate directly from this template. Instead, it prevents the loop from advancing to review/next-round/end-loop behavior until the missing summary file exists.
- gates_or_invariants: The invariant is that every RLCR round must leave a written `round-N-summary.md` before exit can proceed. The stop hook checks file existence with `[[ ! -f "$SUMMARY_FILE" ]]`; if missing, it returns a block decision and exits without continuing to goal-tracker checks or review generation. Template rendering uses the shared single-pass renderer, so values containing `{{OTHER_VAR}}` are not recursively expanded; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/template-loader.sh:50). `load_and_render_safe` also supplies fallback behavior if the template is absent or renders empty, preserving the gate even if template files are damaged; see [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/template-loader.sh:170).
- dependencies_and_callers: Direct caller is [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/loop-codex-stop-hook.sh:519). Rendering depends on [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/hooks/lib/template-loader.sh:58), which discovers templates under `prompt-template` via `get_template_dir`. The broader summary-writing contract is reinforced by next-round prompt text in [prompt-template/claude/next-round-footer.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/prompt-template/claude/next-round-footer.md:9) and by write/bash validators that restrict summary-file writes to the correct location and discourage Bash edits.
- edge_cases_or_failure_modes: If the template is missing, the stop hook fallback still blocks but with a shorter message. If `SUMMARY_FILE` contains unusual content, the template loader substitutes it single-pass and does not re-scan placeholders, reducing prompt-corruption risk. If the file exists but is empty or incomplete, this specific gate passes because it only checks existence; later review quality gates may still evaluate content, but this template’s responsibility is only the missing-file condition. If the active loop state is absent, the stop hook exits earlier and this template is not involved.
- validation_or_tests: Template references are covered by the repository’s template-reference tests, which scan `load_and_render_safe` calls and verify critical validators use safe rendering; see [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-template-references.sh:172). Template loader behavior, including fallback handling, is covered by [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-template-loader.sh:197) and [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state/tests/test-error-scenarios.sh:134). I did not execute tests because this worker task is research-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 unique item evidence sections`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`