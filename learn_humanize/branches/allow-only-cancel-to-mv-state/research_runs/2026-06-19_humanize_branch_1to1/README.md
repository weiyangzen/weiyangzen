# 2026-06-19_humanize_branch_1to1 - `allow-only-cancel-to-mv-state`

This run uses 30 tmux windows with Codex `gpt-5.5` and reasoning effort `xhigh`.

```text
codex -a never exec -m gpt-5.5 -c model_reasoning_effort=xhigh -C /Users/wangweiyang/GitHub/humanize_branch_worktrees/allow-only-cancel-to-mv-state -s read-only --ephemeral -o <agent>/output.md -
```
