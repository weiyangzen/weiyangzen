# agent_09 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for loop hooks, validators, PR-loop setup, state parsing, template rendering, protected-file messaging, PR-loop path classification, bot-name mapping, and local-loop lifecycle transitions. The directory recursively contains `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`.
- algorithmic_behavior: `template-loader.sh` resolves the repository prompt template root from the library path, loads templates, performs single-pass `{{VAR}}` substitution through prefixed environment variables, preserves unknown placeholders, and falls back safely when templates are missing or render empty (`template-loader.sh:24`, `template-loader.sh:36`, `template-loader.sh:58`, `template-loader.sh:170`). `loop-common.sh` validates hook JSON input, rejects null bytes/invalid UTF-8 where possible, requires `tool_name`, checks JSON nesting depth, sources the template loader, and warns rather than hard-failing if templates are unavailable (`loop-common.sh:55`, `loop-common.sh:97`, `loop-common.sh:115`, `loop-common.sh:135`). It finds active RLCR/PR loop directories by selecting the newest timestamp directory with live state files, intentionally avoiding older zombie loops (`loop-common.sh:152`, `loop-common.sh:702`). It provides tolerant and strict YAML-frontmatter state parsers: tolerant parsing defaults missing `current_round` to `0`, `max_iterations` to `10`, and `push_every_round` to `false`; strict parsing rejects missing separators, missing required fields, and non-numeric round/iteration values (`loop-common.sh:175`, `loop-common.sh:202`, `loop-common.sh:240`). It classifies round files, extracts round numbers, blocks protected state/prompt/todo/summary/goal-tracker operations by rendering standardized templates, validates PR-loop `round-N-pr-resolve.md` writes against the current state round, and detects unsafe `git add` commands that would stage `.humanize` (`loop-common.sh:305`, `loop-common.sh:315`, `loop-common.sh:347`, `loop-common.sh:747`, `loop-common.sh:834`). It also handles cancellation authorization through a constrained `mv state.md|finalize-state.md cancel-state.md` command form with injection, quote, symlink, and path checks (`loop-common.sh:455`).
- inputs_outputs_state: Inputs include hook JSON payloads, `.humanize/rlcr/*/state.md`, `.humanize/rlcr/*/finalize-state.md`, `.humanize/pr-loop/*/state.md`, command strings lowercased by validators, paths to round files, bot names/authors, template variables, and optional JSON bot-result summaries. Outputs are shell return codes, global parsed variables such as `STATE_CURRENT_ROUND`, rendered block messages, resolved active-loop paths, YAML list strings, bot mention strings, and mutated goal tracker files via `update_pr_goal_tracker` (`loop-common.sh:1055`). State transitions include `end_loop`, which renames `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`loop-common.sh:1016`), and goal-tracker updates that insert summary/log entries idempotently by round plus reviewer (`loop-common.sh:1088`, `loop-common.sh:1160`, `loop-common.sh:1203`).
- gates_or_invariants: JSON hook input must be syntactically valid and contain `tool_name`; null bytes and invalid UTF-8 are rejected where detectable (`loop-common.sh:58`, `loop-common.sh:68`, `loop-common.sh:75`, `loop-common.sh:82`). Strict state parsing fail-closes validators on malformed state (`loop-read-validator.sh:94`, `loop-write-validator.sh:140`, `loop-bash-validator.sh:81`). Template rendering is intentionally single-pass to prevent placeholder injection from user/review content (`template-loader.sh:54`). PR-loop generated files are read-only except `round-N-pr-resolve.md`, which must match the current loop round (`loop-common.sh:731`, `loop-common.sh:747`). `.humanize` must not be accidentally committed through direct, broad, or forced `git add` patterns (`loop-common.sh:834`). Cancellation is only authorized when a `.cancel-requested` signal file exists and the command structure exactly matches the expected move (`loop-common.sh:459`, `loop-common.sh:500`, `loop-common.sh:585`).
- dependencies_and_callers: All major hook validators source `loop-common.sh`: read, write, edit, bash, plan-file, Codex stop hook, and PR-loop stop hook (`loop-read-validator.sh:19`, `loop-write-validator.sh:19`, `loop-edit-validator.sh:18`, `loop-bash-validator.sh:18`, `loop-plan-file-validator.sh:17`, `loop-codex-stop-hook.sh:48`, `pr-loop-stop-hook.sh:53`). `scripts/setup-pr-loop.sh` sources `template-loader.sh` directly for initial rendering and `loop-common.sh` for mutual exclusion, bot mentions, and YAML list building (`scripts/setup-pr-loop.sh:37`, `scripts/setup-pr-loop.sh:210`). `scripts/setup-rlcr-loop.sh` also relies on active-loop discovery for cross-loop exclusion. Tests directly source this directory in state, cancel, hook-input, path, session, finalization, PR-loop, template, and `.humanize` git-add suites.
- edge_cases_or_failure_modes: The tolerant parser can return raw non-numeric/negative values from `get_current_round`, while strict parser rejects them only when validators opt into strict mode (`loop-common.sh:175`, `loop-common.sh:280`). CRLF frontmatter separators may not match `^---$`, so robustness tests only assert graceful behavior, not semantic success. `git_adds_humanize` checks whether `.humanize` exists and whether it is ignored, so broad `git add .` behavior depends on the current working directory and gitignore state (`loop-common.sh:909`). Template directory validation is non-fatal in `loop-common.sh`, so missing templates degrade to inline fallback messages rather than blocking hook startup (`loop-common.sh:142`). The command-modification detector is regex based and covers common redirection/editing commands but is not a shell parser (`loop-common.sh:972`).
- validation_or_tests: Direct tests include `tests/robustness/test-state-file-robustness.sh` for parsers, `tests/robustness/test-cancel-security-robustness.sh` for cancel authorization, `tests/test-template-loader.sh` and `tests/robustness/test-template-stress-robustness.sh` for rendering, `tests/test-humanize-escape.sh` for `.humanize` add protection, and PR-loop/system tests that source `loop-common.sh`. I did not execute tests because this assignment requested read-only research notes and the branch export is read-only.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-039 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime bootstrap script for `/humanize:start-pr-loop`; it initializes a timestamped `.humanize/pr-loop` session, discovers the current PR, fetches current review context, chooses the startup case, optionally triggers re-review, writes loop state/goal-tracker/prompt artifacts, and emits the initial operator prompt.
- algorithmic_behavior: The script sets strict shell mode and defaults for Codex model/effort/timeout, max iterations, polling, git timeout, and GitHub timeout (`scripts/setup-pr-loop.sh:11`, `scripts/setup-pr-loop.sh:17`). It parses `--claude`, `--codex`, `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout`, rejecting missing/invalid values and unknown arguments (`scripts/setup-pr-loop.sh:110`). It requires at least one bot flag and builds `ACTIVE_BOTS_ARRAY` using canonical internal bot names (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:196`). After sourcing `loop-common.sh`, it rejects concurrent RLCR or PR loops by checking active loop directories (`scripts/setup-pr-loop.sh:222`). It validates git repo presence, at least one commit, `gh` installation/authentication, and `codex` availability (`scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:271`). PR resolution first tries the current repo, then parent repo for fork PRs, validates numeric PR number, rejects merged/closed PRs, and resolves the PR base repo for API/comment operations (`scripts/setup-pr-loop.sh:295`, `scripts/setup-pr-loop.sh:316`, `scripts/setup-pr-loop.sh:334`, `scripts/setup-pr-loop.sh:340`, `scripts/setup-pr-loop.sh:351`). It validates YAML safety for branch/model/effort before writing state (`scripts/setup-pr-loop.sh:365`). It creates `.humanize/pr-loop/<timestamp>`, fetches initial comments, calls `check-pr-reviewer-status.sh`, parses startup case and latest commit metadata, and for cases 4/5 posts a bot trigger comment for re-review (`scripts/setup-pr-loop.sh:394`, `scripts/setup-pr-loop.sh:411`, `scripts/setup-pr-loop.sh:418`, `scripts/setup-pr-loop.sh:443`). For Claude re-review triggers, it requires retrieving the trigger comment ID and confirming an eyes reaction within retry limits before proceeding (`scripts/setup-pr-loop.sh:484`). It writes `state.md`, `goal-tracker.md`, and `round-0-prompt.md`, choosing different task/critical requirement templates depending on whether comments exist (`scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:632`, `scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:687`, `scripts/setup-pr-loop.sh:721`, `scripts/setup-pr-loop.sh:810`).
- inputs_outputs_state: Inputs are CLI flags, current working directory or `CLAUDE_PROJECT_DIR`, git branch/HEAD, GitHub CLI auth/repo/PR API results, active bot selection, fetched PR comments, reviewer-status JSON, GitHub user/comment timestamps, and prompt templates under `prompt-template/pr-loop`. Outputs are a loop directory under `.humanize/pr-loop`, `round-0-pr-comment.md`, `state.md` with YAML fields including `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, Codex settings, poll settings, `startup_case`, latest commit metadata, and trigger metadata (`scripts/setup-pr-loop.sh:535`), a rendered `goal-tracker.md`, a rendered `round-0-prompt.md`, and stdout activation instructions plus the initial prompt (`scripts/setup-pr-loop.sh:780`, `scripts/setup-pr-loop.sh:807`).
- gates_or_invariants: Only one loop may be active across RLCR and PR-loop surfaces (`scripts/setup-pr-loop.sh:220`). The target must be a git repo with a PR that is open and numerically identified (`scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:326`, `scripts/setup-pr-loop.sh:334`, `scripts/setup-pr-loop.sh:342`). `gh` must be installed/authenticated and `codex` must be available before state creation (`scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`). YAML-unsafe branch/model/effort values are rejected before writing `state.md` (`scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:374`, `scripts/setup-pr-loop.sh:382`). For case 4/5, failure to post a trigger comment aborts and removes the loop directory to avoid orphaned state (`scripts/setup-pr-loop.sh:446`, `scripts/setup-pr-loop.sh:457`). Claude eyes verification is mandatory when `--claude` is selected for re-review triggers; failure also cleans up and exits (`scripts/setup-pr-loop.sh:484`, `scripts/setup-pr-loop.sh:507`, `scripts/setup-pr-loop.sh:517`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` for `run_with_timeout` (`scripts/setup-pr-loop.sh:33`), `hooks/lib/template-loader.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-pr-loop.sh:37`, `scripts/setup-pr-loop.sh:210`), `scripts/fetch-pr-comments.sh` for markdown comment snapshots (`scripts/setup-pr-loop.sh:411`; helper contract at `scripts/fetch-pr-comments.sh:3`), `scripts/check-pr-reviewer-status.sh` for startup-case JSON (`scripts/setup-pr-loop.sh:418`; helper contract at `scripts/check-pr-reviewer-status.sh:11`), `scripts/check-bot-reactions.sh` for Claude eyes confirmation (`scripts/setup-pr-loop.sh:507`; helper contract at `scripts/check-bot-reactions.sh:9`), `git`, `gh`, `jq`, `codex`, and PR-loop templates. It is the setup side for runtime stop hooks and validators that consume the generated state/prompt/summary files.
- edge_cases_or_failure_modes: Fork PRs are explicitly handled by trying current repo first and parent/base repo fallback for PR lookup, comments, and reactions (`scripts/setup-pr-loop.sh:292`, `scripts/setup-pr-loop.sh:351`). If reviewer-status lookup fails, it defaults to startup case 1 with empty review metadata, but if latest commit SHA is absent it falls back to local `git rev-parse HEAD` (`scripts/setup-pr-loop.sh:419`, `scripts/setup-pr-loop.sh:430`). It deliberately avoids falling back to local time when trigger comment timestamp lookup fails, because clock skew could filter out future comments (`scripts/setup-pr-loop.sh:479`). `COMMENT_COUNT` is booleanized to `0` or `1`, not the actual count, by detecting the `*No comments found.*` sentinel (`scripts/setup-pr-loop.sh:643`). It uses `cat >` writes and `rm -rf "$LOOP_DIR"` cleanup internally; those are runtime behaviors, not performed in this research run. Output has a `PIPE` trap so closed stdout does not turn completed setup into failure (`scripts/setup-pr-loop.sh:777`).
- validation_or_tests: Covered by script-focused tests such as `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop.sh`, `tests/test-pr-loop-system.sh`, monitor/runtime tests, and helper-specific mock `gh` fixtures under `tests/fixtures` and `tests/mocks/gh`. Related helper contracts are exercised through fetch/reviewer/reaction tests. I did not execute tests due the read-only research instruction.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-069 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the stop-hook preflight gate that blocks loop exit when the latest TodoWrite list still contains incomplete tasks.
- algorithmic_behavior: The test script creates a temp directory, feeds JSON hook input to the Python checker over stdin, captures exit codes/output, and counts pass/fail outcomes (`tests/test-todo-checker.sh:20`, `tests/test-todo-checker.sh:37`). It specifies input handling: invalid JSON and empty stdin exit `2`, missing `transcript_path` exits `0`, and nonexistent transcript file exits `0` (`tests/test-todo-checker.sh:51`, `tests/test-todo-checker.sh:63`, `tests/test-todo-checker.sh:75`, `tests/test-todo-checker.sh:87`). It specifies todo detection: all `completed` todos exit `0`; any `pending`, `in_progress`, missing status, or empty-content pending todo exits `1`; incomplete output includes the task name (`tests/test-todo-checker.sh:106`, `tests/test-todo-checker.sh:121`, `tests/test-todo-checker.sh:136`, `tests/test-todo-checker.sh:144`, `tests/test-todo-checker.sh:249`, `tests/test-todo-checker.sh:265`). It specifies transcript parsing variants: empty transcript exits `0`, invalid JSONL lines are ignored, multiple TodoWrite calls use the latest one, direct `tool_use` entries are accepted, and alternative `type: message` format is accepted (`tests/test-todo-checker.sh:166`, `tests/test-todo-checker.sh:179`, `tests/test-todo-checker.sh:196`, `tests/test-todo-checker.sh:212`, `tests/test-todo-checker.sh:227`). It also verifies Unicode content does not break parsing (`tests/test-todo-checker.sh:280`).
- inputs_outputs_state: Inputs are synthetic JSON hook payloads containing `transcript_path` and generated JSONL transcript files in `mktemp` space. Outputs are process exit codes from the checker and stdout/stderr text, especially the incomplete-todo detail used by the stop hook. The test state is local counters `TESTS_PASSED`/`TESTS_FAILED` and temporary transcript files cleaned by trap (`tests/test-todo-checker.sh:20`, `tests/test-todo-checker.sh:37`). The production checker parses stdin JSON, expands `~`, scans the latest TodoWrite list, prints `INCOMPLETE_TODOS` plus `- [status] content`, and exits `1` for incomplete todos (`hooks/check-todos-from-transcript.py:90`, `hooks/check-todos-from-transcript.py:103`, `hooks/check-todos-from-transcript.py:113`, `hooks/check-todos-from-transcript.py:121`).
- gates_or_invariants: Only exact status `completed` is accepted as complete; every other status, including missing/empty status, is incomplete (`hooks/check-todos-from-transcript.py:115`). Malformed hook input is a parse error with exit `2`, but malformed transcript JSONL lines are ignored to tolerate partial/noisy logs (`hooks/check-todos-from-transcript.py:36`, `hooks/check-todos-from-transcript.py:92`). The latest non-empty TodoWrite payload is authoritative; earlier incomplete todos do not matter if superseded by a later all-complete TodoWrite (`hooks/check-todos-from-transcript.py:28`, `tests/test-todo-checker.sh:196`). In runtime, `loop-codex-stop-hook.sh` blocks immediately on checker exit `1` or `2` before expensive Codex review (`hooks/loop-codex-stop-hook.sh:255`, `hooks/loop-codex-stop-hook.sh:287`).
- dependencies_and_callers: Directly depends on `python3` and `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:13`). Production caller is `hooks/loop-codex-stop-hook.sh`, which pipes the stop-hook input into the checker, surfaces parse errors as a blocking decision, and renders `block/incomplete-todos.md` when incomplete tasks remain (`hooks/loop-codex-stop-hook.sh:261`, `hooks/loop-codex-stop-hook.sh:268`, `hooks/loop-codex-stop-hook.sh:297`).
- edge_cases_or_failure_modes: The checker intentionally returns success when no transcript path, nonexistent file, empty transcript, or no TodoWrite exists; this avoids false blocks but can allow exit if transcript capture is missing (`tests/test-todo-checker.sh:75`, `tests/test-todo-checker.sh:87`, `tests/test-todo-checker.sh:166`). It ignores invalid JSONL transcript lines, which is robust against log noise but can hide a corrupted latest TodoWrite if that line is unparsable (`tests/test-todo-checker.sh:179`). It only updates `latest_todos` when the todos list is truthy, so an explicit latest TodoWrite with an empty list does not clear an earlier non-empty todo list in the implementation (`hooks/check-todos-from-transcript.py:61`, `hooks/check-todos-from-transcript.py:62`); the assigned test suite does not cover that specific case.
- validation_or_tests: This file is itself the validation script and exits `0` only when `TESTS_FAILED` is zero (`tests/test-todo-checker.sh:295`, `tests/test-todo-checker.sh:305`). I inspected it directly but did not run it because the assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-099 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template used by the Codex stop hook when the loop participant attempts to exit without writing the required round or finalize summary file.
- algorithmic_behavior: The template renders a concise error titled “Work Summary Missing,” tells the actor they attempted to exit without writing the work summary, substitutes `{{SUMMARY_FILE}}` into a fenced path block, lists required summary contents, and allows another exit attempt only after the summary is written (`prompt-template/block/work-summary-missing.md:1`). It is loaded through `load_and_render_safe`, so the runtime can substitute the specific round/finalize summary path while retaining a fallback if the template is absent (`hooks/loop-codex-stop-hook.sh:536`, `hooks/loop-codex-stop-hook.sh:539`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` template variable supplied by `hooks/loop-codex-stop-hook.sh`; for normal rounds this is `.humanize/rlcr/<session>/round-N-summary.md`, and in finalize phase it is `.humanize/rlcr/<session>/finalize-summary.md` (`hooks/loop-codex-stop-hook.sh:525`, `hooks/loop-codex-stop-hook.sh:529`). Output is a rendered markdown block assigned to `REASON`, then wrapped into a JSON hook response with decision `block` and system message identifying the missing summary condition (`hooks/loop-codex-stop-hook.sh:548`). The state transition is negative: no loop progress occurs until the required summary file exists.
- gates_or_invariants: The invariant is that each stop attempt must have a corresponding work summary file for the current round or finalize phase before downstream review/finalization proceeds (`hooks/loop-codex-stop-hook.sh:532`). The template’s required summary contents are implementation summary, created/modified files, tests added/passed, and remaining items (`prompt-template/block/work-summary-missing.md:10`). The stop hook chooses a distinct system message for finalize-phase missing summaries versus normal round summaries (`hooks/loop-codex-stop-hook.sh:542`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/work-summary-missing.md"` (`hooks/loop-codex-stop-hook.sh:539`). It depends on `hooks/lib/template-loader.sh` for placeholder rendering. Related tests likely include comprehensive template reference/coverage suites (`tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`) and stop-hook/finalize tests that assert missing-summary blocking behavior.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook fallback only says “Please write your work summary to: {{SUMMARY_FILE}},” losing the richer checklist but preserving the hard block (`hooks/loop-codex-stop-hook.sh:536`). The template does not encode different content requirements for finalize versus normal rounds; differentiation is handled by caller-selected `SUMMARY_FILE` and system message. Because unknown placeholders are preserved by the renderer, a missing `SUMMARY_FILE` variable would render literally rather than disappear (`template-loader.sh:119`).
- validation_or_tests: Template existence and safe rendering are covered by template-reference/comprehensive tests, and the runtime gate is in `loop-codex-stop-hook.sh` lines 521-555. I did not execute tests in this read-only research run.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-129 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for state-file parsing helpers in `hooks/lib/loop-common.sh`, especially tolerant `parse_state_file`, strict `parse_state_file_strict`, and `get_current_round`.
- algorithmic_behavior: The script sources `loop-common.sh` and shared test helpers, creates a temporary test directory, then generates many synthetic `state*.md` files to exercise parser behavior (`tests/robustness/test-state-file-robustness.sh:14`, `tests/robustness/test-state-file-robustness.sh:16`, `tests/robustness/test-state-file-robustness.sh:19`). Positive tests assert valid full frontmatter parses current/max rounds, `get_current_round` extracts the current round, extra unknown fields are ignored, quoted `plan_file`/`start_branch` are unquoted, and round zero is preserved (`tests/robustness/test-state-file-robustness.sh:33`, `tests/robustness/test-state-file-robustness.sh:62`, `tests/robustness/test-state-file-robustness.sh:72`, `tests/robustness/test-state-file-robustness.sh:100`, `tests/robustness/test-state-file-robustness.sh:122`). Negative/edge tests assert strict parser rejection for missing frontmatter, non-numeric `current_round`, and missing required fields, while tolerant parsing/default extraction remains graceful (`tests/robustness/test-state-file-robustness.sh:147`, `tests/robustness/test-state-file-robustness.sh:171`, `tests/robustness/test-state-file-robustness.sh:199`, `tests/robustness/test-state-file-robustness.sh:215`). Additional cases cover empty files, partial YAML, malformed YAML, very large numbers, negative round numbers, spaces in quoted values, trailing whitespace, nonexistent files, binary data after YAML, and CRLF line endings (`tests/robustness/test-state-file-robustness.sh:228`, `tests/robustness/test-state-file-robustness.sh:240`, `tests/robustness/test-state-file-robustness.sh:257`, `tests/robustness/test-state-file-robustness.sh:275`, `tests/robustness/test-state-file-robustness.sh:295`, `tests/robustness/test-state-file-robustness.sh:312`, `tests/robustness/test-state-file-robustness.sh:334`, `tests/robustness/test-state-file-robustness.sh:354`, `tests/robustness/test-state-file-robustness.sh:363`, `tests/robustness/test-state-file-robustness.sh:382`).
- inputs_outputs_state: Inputs are generated markdown state files with YAML frontmatter variants. Outputs are pass/fail assertions through `pass`, `fail`, and `print_test_summary`; parser side effects are global variables set by `loop-common.sh`, including `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH` (`hooks/lib/loop-common.sh:202`, `hooks/lib/loop-common.sh:240`). The test exits with the shared summary return code (`tests/robustness/test-state-file-robustness.sh:399`).
- gates_or_invariants: Strict parser must fail closed on missing state file, missing two `---` separators, missing required `current_round`/`max_iterations`, and non-numeric current/max values (`hooks/lib/loop-common.sh:243`, `hooks/lib/loop-common.sh:248`, `hooks/lib/loop-common.sh:270`, `hooks/lib/loop-common.sh:280`, `hooks/lib/loop-common.sh:286`). Tolerant parser must remain backward-compatible by returning defaults for missing fields and handling extra fields/quoted strings (`hooks/lib/loop-common.sh:211`, `hooks/lib/loop-common.sh:223`). `get_current_round` defaults to `0` when extraction fails (`hooks/lib/loop-common.sh:175`, `hooks/lib/loop-common.sh:184`). These invariants support hook fail-closed behavior in validators while allowing older utility callers to avoid crashing.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-state-file-robustness.sh:16`, `tests/robustness/test-state-file-robustness.sh:17`). Runtime callers of the strict parser include read/write/edit/bash/plan-file validators and stop hooks; examples include `loop-read-validator.sh:94`, `loop-write-validator.sh:140`, and `loop-bash-validator.sh:81`. Tolerant parsing is used in tests and some legacy/finalize paths where defaults are acceptable.
- edge_cases_or_failure_modes: The suite deliberately allows ambiguous graceful results for partial YAML, non-numeric tolerant extraction, negative values, and CRLF line endings rather than requiring canonical normalization (`tests/robustness/test-state-file-robustness.sh:188`, `tests/robustness/test-state-file-robustness.sh:240`, `tests/robustness/test-state-file-robustness.sh:295`, `tests/robustness/test-state-file-robustness.sh:382`). Strict parser accepts negative integers because the regex is `^-?[0-9]+$`, so semantic bounds like nonnegative rounds are not enforced there (`hooks/lib/loop-common.sh:280`). Binary content after the closing YAML separator is expected not to affect `get_current_round` (`tests/robustness/test-state-file-robustness.sh:363`).
- validation_or_tests: This file is itself the validation suite for state-file robustness and ends with `print_test_summary "State File Robustness Test Summary"` (`tests/robustness/test-state-file-robustness.sh:399`). I inspected the test and implementation directly but did not run it because the current task asked for research notes only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`