# agent_19 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-019 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` command workflow that converts a rough draft into a structured implementation plan. It is a command-level algorithm spec: validate IO, reject unrelated drafts, analyze requirements, resolve ambiguity, then emit a deterministic `plan.md` shape with acceptance criteria and path boundaries.
- algorithmic_behavior: The workflow is explicitly phased at `commands/gen-plan.md:19-26`. Phase 1 runs `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh` with `$ARGUMENTS` before any plan analysis (`commands/gen-plan.md:30-36`). Phase 2 delegates relevance classification to `humanize:draft-relevance-checker` using a lenient rule that only clearly unrelated drafts fail (`commands/gen-plan.md:49-71`). Phase 3 asks Explore agents to inspect affected components, related files, patterns, dependencies, and integrations (`commands/gen-plan.md:75-107`). Phase 4 forces user clarification for analysis issues and separately confirms whether quantitative metrics are hard gates or optimization directions (`commands/gen-plan.md:110-136`). Phase 5 emits a fixed markdown structure with goal, ACs, path boundaries, feasibility hints, and dependency sequence (`commands/gen-plan.md:139-213`). Phase 6 writes the output and reports generated path, summary, and AC count (`commands/gen-plan.md:237-243`).
- inputs_outputs_state: Inputs are command arguments `--input <draft.md> --output <plan.md>` from frontmatter (`commands/gen-plan.md:2-3`), the input draft content (`commands/gen-plan.md:55`), repository context from README/CLAUDE/main files via the relevance agent (`commands/gen-plan.md:56-64`), Explore-agent findings (`commands/gen-plan.md:100-107`), and user answers from `AskUserQuestion` (`commands/gen-plan.md:114-135`). Output is a newly written plan file at the requested output path containing the required sections and AC numbering (`commands/gen-plan.md:145-213`). Command state transitions are: invalid IO -> stop with mapped error; unrelated draft -> stop; unresolved issues -> ask user until resolved or acknowledged; confirmed metrics -> generation; write success -> report result. The command does not maintain persistent loop state itself; it creates a plan artifact used later by RLCR.
- gates_or_invariants: IO validation is a hard precondition. Exit codes 1-6 map to specific stops: missing input, empty input, missing output dir, existing output, no write permission, invalid args (`commands/gen-plan.md:38-45`). Plan-generation invariants include AC IDs in `AC-X` or `AC-X.Y` format (`commands/gen-plan.md:225`), positive and negative tests for every AC (`commands/gen-plan.md:153-168`, `commands/gen-plan.md:229`), no line-number references (`commands/gen-plan.md:219`), no time estimates (`commands/gen-plan.md:217-221`), dependency ordering by relationship rather than schedule (`commands/gen-plan.md:202-212`, `commands/gen-plan.md:227`), affirmative upper/lower path boundaries (`commands/gen-plan.md:231`), and narrowed boundaries when the draft is deterministic (`commands/gen-plan.md:189`, `commands/gen-plan.md:233`).
- dependencies_and_callers: Frontmatter allows only the IO validator Bash command plus `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` (`commands/gen-plan.md:4-12`). The IO validator is `scripts/validate-gen-plan-io.sh`, whose exit-code contract matches the command: success 0, missing input 1, empty input 2, missing output dir 3, output exists or is dir 4, no write permission 5, invalid args 6 (`scripts/validate-gen-plan-io.sh:4-11`, `scripts/validate-gen-plan-io.sh:78-130`). The relevance subagent is `agents/draft-relevance-checker.md`, with `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep` (`agents/draft-relevance-checker.md:1-6`); it returns `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:27-30`). README documents `/humanize:gen-plan` as the path from draft to plan before `/humanize:start-rlcr-loop` (`README.md:70-77`, `README.md:110-125`). Structural and IO behavior is covered by `tests/test-gen-plan.sh`, including command frontmatter and validator exit cases (`tests/test-gen-plan.sh:426-541`).
- edge_cases_or_failure_modes: Empty or missing inputs stop before analysis; output must be a new file in an existing writable directory, so accidental overwrite is blocked. Drafts deemed completely unrelated stop at Phase 2 (`commands/gen-plan.md:66-69`). Ambiguous requirements are not silently guessed: the command asks the user and documents decisions (`commands/gen-plan.md:114-121`, `commands/gen-plan.md:246-254`). Quantitative metrics are an explicit edge case because hard requirements and optimization targets produce different ACs (`commands/gen-plan.md:123-136`). Deterministic drafts are another edge case: upper/lower bounds may converge, and allowed choices should state the fixed choice (`commands/gen-plan.md:189`).
- validation_or_tests: `tests/test-gen-plan.sh` validates that `gen-plan.md` exists, has frontmatter fields, allowed tools, argument hint, valid YAML, English-only content, and that `draft-relevance-checker` exists with the expected name/model/tools; it also directly validates `scripts/validate-gen-plan-io.sh` exit paths for invalid args, missing/empty input, missing output dir, existing output, output dir, valid paths, and help (`tests/test-gen-plan.sh:59-155`, `tests/test-gen-plan.sh:280-340`, `tests/test-gen-plan.sh:426-541`). I did not execute tests in this read-only research run.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-049 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR cancel-signal authorization algorithm. It defines the security contract for allowing exactly one otherwise blocked state transition: `state.md` or `finalize-state.md` may be moved to `cancel-state.md` only when the active loop directory contains `.cancel-requested`.
- algorithmic_behavior: The test creates a synthetic active RLCR loop under a temporary `.humanize/rlcr/<timestamp>/` directory with `state.md`, sets `CLAUDE_PROJECT_DIR`, and invokes `hooks/loop-bash-validator.sh` through JSON hook input (`tests/test-cancel-signal-file.sh:42-71`). It verifies the Bash validator allows the exact cancel move when the signal exists (`tests/test-cancel-signal-file.sh:75-92`) and a normalized `./state.md` source path (`tests/test-cancel-signal-file.sh:95-113`). It then asserts block behavior for the same move without a signal (`tests/test-cancel-signal-file.sh:116-133`), all other state mutations even with a signal (`tests/test-cancel-signal-file.sh:136-235`), wrong destination or unrelated directory moves (`tests/test-cancel-signal-file.sh:176-215`, `tests/test-cancel-signal-file.sh:1207-1225`), and a no-active-loop case where commands are allowed because no `state.md` exists (`tests/test-cancel-signal-file.sh:1228-1247`). It also directly probes `is_cancel_authorized` helper behavior for canonical allow/deny paths (`tests/test-cancel-signal-file.sh:1250-1341`).
- inputs_outputs_state: Inputs are constructed Bash command strings, the presence or absence of `$LOOP_DIR/.cancel-requested`, the test loop state file, and JSON hook envelopes with `tool_name: Bash` and `tool_input.command` (`tests/test-cancel-signal-file.sh:61-70`). Expected outputs are validator exit code `0` for allowed commands and `2` for blocked command attempts, plus helper return code success/failure. State transitions under test are not the actual file move result; the primary transition is validator authorization state: active loop + signal + exact `mv state.md cancel-state.md` -> allow; active loop + no signal or malformed command -> block; no active loop -> allow all (`tests/test-cancel-signal-file.sh:64-71`, `tests/test-cancel-signal-file.sh:88-91`, `tests/test-cancel-signal-file.sh:129-132`, `tests/test-cancel-signal-file.sh:1243-1247`).
- gates_or_invariants: The core invariant is narrow authorization. `is_cancel_authorized` requires `.cancel-requested`, rejects command substitution, backticks, newlines, shell chaining, leftover `$` variables after `$loop_dir` normalization, mixed quote styles, extra args, wrong source/destination, and symlink sources (`hooks/lib/loop-common.sh:438-455`, `hooks/lib/loop-common.sh:461-614`). The Bash validator finds active RLCR loops, parses state strictly, blocks state-file modifications, and delegates only the cancel exception to `is_cancel_authorized` (`hooks/loop-bash-validator.sh:54-85`, `hooks/loop-bash-validator.sh:125-156`). It also catches `mv` or `cp` where `state.md` is a source rather than a destination, including options, prefixes, chained segments, wrappers, and redirection-prefixed forms (`hooks/loop-bash-validator.sh:158-328`). The tests encode those invariants across positive, negative, and helper groups.
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` to call helpers directly (`tests/test-cancel-signal-file.sh:32-33`) and invokes `hooks/loop-bash-validator.sh` for end-to-end hook behavior (`tests/test-cancel-signal-file.sh:70`). That validator depends on `find_active_loop`, `parse_state_file_strict`, `command_modifies_file`, and `is_cancel_authorized` from the shared library (`hooks/loop-bash-validator.sh:16-18`, `hooks/loop-bash-validator.sh:57-63`, `hooks/loop-bash-validator.sh:81-85`, `hooks/loop-bash-validator.sh:139-156`). The cancel signal itself is produced by `scripts/cancel-rlcr-loop.sh`, which documents cancellation by creating `.cancel-requested` (`scripts/cancel-rlcr-loop.sh:5`, `scripts/cancel-rlcr-loop.sh:52`, `scripts/cancel-rlcr-loop.sh:87`, `scripts/cancel-rlcr-loop.sh:134`). `tests/run-all-tests.sh` includes `test-cancel-signal-file.sh` in the main test list (`tests/run-all-tests.sh:46` from search output).
- edge_cases_or_failure_modes: The test covers command-injection operators `&&`, `;`, `||`, pipes, background `&`, and `|&` (`tests/test-cancel-signal-file.sh:238-335`, `tests/test-cancel-signal-file.sh:647-804`); command substitution using `$()` and backticks (`tests/test-cancel-signal-file.sh:298-335`); literal newline injection (`tests/test-cancel-signal-file.sh:338-355`); option and argument bypasses such as `mv --`, `cp -f`, three args, extra args before `state.md`, and quoted relative paths (`tests/test-cancel-signal-file.sh:381-438`, `tests/test-cancel-signal-file.sh:487-525`); hidden variable injection via `${IFS}` (`tests/test-cancel-signal-file.sh:528-544`); command prefixes `sudo`, `env`, `command --`, and shell wrappers `sh -c` / `bash -c` (`tests/test-cancel-signal-file.sh:547-644`, `tests/test-cancel-signal-file.sh:807-844`); many redirection-prefix bypass forms including spaced, append, fd, `&>`, `&>>`, quoted targets, escaped-space targets, and ANSI-C quoting (`tests/test-cancel-signal-file.sh:847-1204`); wrong-directory signal files (`tests/test-cancel-signal-file.sh:196-215`); and no-active-loop permissiveness (`tests/test-cancel-signal-file.sh:1228-1247`). The helper implementation also has symlink-source rejection, although this file’s direct helper group focuses on signal, wrong command, extra args, quoted paths, and hidden variables (`hooks/lib/loop-common.sh:600-610`, `tests/test-cancel-signal-file.sh:1258-1341`).
- validation_or_tests: This file is itself the validation artifact. It initializes pass/fail counters, prints each case, and exits with `$TESTS_FAILED` (`tests/test-cancel-signal-file.sh:17-30`, `tests/test-cancel-signal-file.sh:1343-1355`). It defines 5 positive end-to-end/helper-normalization cases, 53 negative/end-to-end cases, and 7 direct helper tests by label. I inspected it directly but did not execute it because the worker run is research-only and the branch export is read-only.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-079 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt block used by the RLCR stop hook when git is not clean and untracked non-humanize files are present. Its algorithmic role is advisory gating text: it distinguishes likely artifacts from files that should be committed, nudging the agent toward `.gitignore` updates instead of blindly staging untracked outputs.
- algorithmic_behavior: The template content is a short markdown note titled “Note on Untracked Files” (`prompt-template/block/git-not-clean-untracked.md:2`). It classifies untracked files as possible build artifacts, test outputs, or runtime-generated files (`prompt-template/block/git-not-clean-untracked.md:3`) and lists categories usually ignored: build outputs, dependencies, editor files, logs, caches, and temp files (`prompt-template/block/git-not-clean-untracked.md:4-8`). It ends with the transition instruction to review untracked files and add appropriate patterns to `.gitignore` (`prompt-template/block/git-not-clean-untracked.md:10`).
- inputs_outputs_state: Input is the stop hook’s git status classification, specifically non-empty untracked files outside `.humanize*`. Output is appended prompt text inside the git-not-clean blocking message. The state transition is from “git issue detected” to “blocked with special notes”; it does not mutate git state itself.
- gates_or_invariants: The gate is in `hooks/loop-codex-stop-hook.sh`: it extracts untracked lines from cached status, treats `.humanize*` separately, and loads this template only for other untracked files (`hooks/loop-codex-stop-hook.sh:441-460`). If the template is missing or empty, the hook falls back to “Review untracked files - add to .gitignore or commit them” (`hooks/loop-codex-stop-hook.sh:456-459`). The invariant encoded by the template is that generated artifacts should generally be ignored rather than committed.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, via `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` (`hooks/loop-codex-stop-hook.sh:453-460`). It is coordinated with sibling prompt `block/git-not-clean-humanize-local.md` for `.humanize*` untracked dirs (`hooks/loop-codex-stop-hook.sh:444-450`) and with the broader git-not-clean block assembled when `GIT_ISSUES` is non-empty (`hooks/loop-codex-stop-hook.sh:464-470`).
- edge_cases_or_failure_modes: If both `.humanize*` and other untracked files exist, both special notes can be appended because the hook builds `SPECIAL_NOTES` incrementally (`hooks/loop-codex-stop-hook.sh:441-460`). If the template is missing, the fallback preserves the algorithm’s blocking guidance. A possible skip-candidate argument is that the file is not executable logic, but it participates in the git-clean gate’s decision feedback and therefore is part of the prompt-level transition contract.
- validation_or_tests: `rg` shows this template is referenced directly by `hooks/loop-codex-stop-hook.sh`; `tests/test-template-references.sh` includes hook/template reference validation in this area from the repository’s test suite. I did not execute tests in this read-only research run.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-109 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt block injected after a Full Goal Alignment Check round to bias the next Claude implementation round toward unresolved alignment findings. It is a lightweight algorithmic transition template from review/alignment output to next-round priorities.
- algorithmic_behavior: The template declares “Post-Alignment Check Action Items” (`prompt-template/claude/post-alignment-action-items.md:2`) and directs the next round to focus on three categories: forgotten items identified by Codex, acceptance criteria marked `NOT MET`, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:4-7`). This converts alignment review state into prioritized implementation work without adding new parser logic.
- inputs_outputs_state: Input is the current RLCR round number and the existence/content of this template. Output is appended text in the next Claude prompt file. The state transition is periodic: after specific alignment-check cadence rounds, the next prompt includes extra action-item constraints.
- gates_or_invariants: The injection gate is in `hooks/loop-codex-stop-hook.sh`: after rendering `claude/next-round-prompt.md`, it checks `CURRENT_ROUND % 5 == 4`, loads this template, and appends it only when non-empty (`hooks/loop-codex-stop-hook.sh:1078-1090`). The invariant is that rounds following every fifth-cycle alignment checkpoint must not ignore alignment findings, especially `NOT MET` ACs and unjustified deferrals.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"` (`hooks/loop-codex-stop-hook.sh:1085-1090`). It depends on the surrounding next-round prompt generation, which passes plan, review content, and goal tracker references into `claude/next-round-prompt.md` before this block is appended (`hooks/loop-codex-stop-hook.sh:1078-1083`). It coordinates with the RLCR review cadence and goal-alignment mechanism rather than with standalone shell scripts.
- edge_cases_or_failure_modes: If the template is missing or empty, no post-alignment note is appended because the hook checks `[[ -n "$POST_ALIGNMENT" ]]` (`hooks/loop-codex-stop-hook.sh:1087-1090`). Since the trigger is modular arithmetic on `CURRENT_ROUND`, off-by-one behavior matters: the block appears when the just-completed/current round number leaves remainder 4 modulo 5, i.e. the round after every fifth-cycle alignment checkpoint per the in-code comment (`hooks/loop-codex-stop-hook.sh:1085-1086`). It is not executable logic by itself, but it defines a prompt-level priority contract for alignment remediation.
- validation_or_tests: Direct reference validation is implied by the hook/template reference tests in the suite, and `rg` confirms the only direct runtime load is from `hooks/loop-codex-stop-hook.sh`. I did not execute tests in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique evidence sections above, one per assigned checklist row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`