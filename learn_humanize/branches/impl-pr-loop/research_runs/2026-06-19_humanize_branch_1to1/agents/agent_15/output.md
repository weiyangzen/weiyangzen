# agent_15 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-015 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Robustness regression suite for the loop algorithms’ shell-facing contracts. The directory has no nested subdirectories and contains ten executable shell specs, all registered in the aggregate runner at `tests/run-all-tests.sh:55-64`. It covers state/session discovery, goal tracker parsing, plan path/content validation, git state summarization, hook JSON/input validation, template rendering, cancel authorization, and timeout behavior.
- algorithmic_behavior: Each script creates an isolated temp workspace via `tests/test-helpers.sh:86-89`, sources the production function or invokes the production command, then asserts expected accept/reject behavior. Child roles: `test-state-file-robustness.sh` validates tolerant and strict YAML frontmatter parsing against valid, missing, malformed, numeric, binary, and CRLF cases; `test-session-robustness.sh` validates newest active session selection, including `finalize-state.md`; `test-goal-tracker-robustness.sh` validates `humanize_parse_goal_tracker` counts and summary extraction; `test-path-validation-robustness.sh` invokes `scripts/setup-rlcr-loop.sh` for plan path acceptance/rejection; `test-plan-file-robustness.sh` invokes the same setup script for plan content/size/readability cases; `test-git-operations-robustness.sh` validates git status and special git state detection; `test-hook-input-robustness.sh` pipes JSON into read/write/bash hooks and exercises monitor edge cases; `test-template-stress-robustness.sh` validates single-pass prompt template rendering; `test-cancel-security-robustness.sh` validates cancel command authorization; `test-timeout-robustness.sh` validates portable timeout fallback and exit code semantics.
- inputs_outputs_state: Inputs are generated temp files, temp git repos, hook JSON payloads, loop state files, plan file paths, command strings, and terminal/log fixtures. Outputs are shell exit statuses plus PASS/FAIL counters from `tests/test-helpers.sh:30-77`. State transitions under test include active `state.md` to `finalize-state.md` detection, PR/RLCR session selection, active bot YAML parsing, plan setup rejection before loop creation, cancel signal authorization, and timeout exit `124`.
- gates_or_invariants: The suite asserts fail-closed behavior for malformed state (`hooks/lib/loop-common.sh:240-295`), only-newest active loop detection (`hooks/lib/loop-common.sh:147-170`), restricted cancel shape with `.cancel-requested` and no symlink source (`hooks/lib/loop-common.sh:437-614`), plan path metacharacter and symlink rejection (`scripts/setup-rlcr-loop.sh:258-344`), minimum plan size/content (`scripts/setup-rlcr-loop.sh:416-469`), JSON validation plus nesting limits in hooks (`hooks/loop-read-validator.sh:25-35`), and template single-pass substitution (`hooks/lib/template-loader.sh:50-132`).
- dependencies_and_callers: Central caller is `tests/run-all-tests.sh:55-64`. Common test helpers are in `tests/test-helpers.sh:30-89`. Production dependencies include `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/portable-timeout.sh`, `scripts/setup-rlcr-loop.sh`, and hook validators under `hooks/`. Several specs call production commands rather than mocked helpers, notably path/plan validation via `CLAUDE_PROJECT_DIR="$TEST_DIR" bash "$PROJECT_ROOT/scripts/setup-rlcr-loop.sh" ...` in `tests/robustness/test-path-validation-robustness.sh:57-92` and `tests/robustness/test-plan-file-robustness.sh:24-82`.
- edge_cases_or_failure_modes: Covered cases include symlink loops and misleading path substrings such as loop dirs containing `finalize` (`test-cancel-security-robustness.sh:369-401`), absolute/metacharacter/space/glob plan paths (`test-path-validation-robustness.sh:158-307`), disappearing or unreadable plan files (`test-plan-file-robustness.sh:442-520`), binary/null content in state, tracker, logs, and plans, non-git/empty/detached/rebase/merge/shallow git states, missing `.humanize` monitor directories, terminal width extremes, and commands with special characters or ignored SIGTERM.
- validation_or_tests: This directory is validation. I inspected recursively and did not execute it because the task requested research notes only. The scripts are executable specs with explicit runner inclusion at `tests/run-all-tests.sh:55-64`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-045 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Test fixture generator for PR loop monitor state parsing. It creates `.humanize/pr-loop/<timestamp>/state.md` fixtures with YAML-list `configured_bots` and `active_bots` variants used by monitor tests.
- algorithmic_behavior: The script requires a target test directory and optional fixture name (`tests/setup-monitor-test-env.sh:12-18`). It dispatches on `yaml_list`, `configured`, or `empty` (`tests/setup-monitor-test-env.sh:20-89`), creates a timestamped PR loop directory, writes YAML frontmatter state, and prints the target directory (`tests/setup-monitor-test-env.sh:91`).
- inputs_outputs_state: Input 1 is `TEST_DIR`; input 2 is `TEST_NAME` defaulting to `default` but only three named cases are accepted. Outputs are fixture directories under `$TEST_DIR/.humanize/pr-loop/` and `state.md` files containing `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`. The `yaml_list` case leaves both bots active (`tests/setup-monitor-test-env.sh:20-42`), `configured` leaves only `codex` active (`tests/setup-monitor-test-env.sh:43-63`), and `empty` leaves `active_bots:` empty (`tests/setup-monitor-test-env.sh:64-83`).
- gates_or_invariants: It fails on missing `TEST_DIR` with usage (`tests/setup-monitor-test-env.sh:15-18`) and fails closed on unknown fixture names with the allowed list (`tests/setup-monitor-test-env.sh:84-88`). The fixture invariant is YAML list syntax with indented `- bot` items, matching monitor parsing in `scripts/humanize.sh:997-1018`.
- dependencies_and_callers: Called by `tests/test-pr-loop-hooks.sh:1552-1602` for `T-MONITOR-1`, `T-MONITOR-2`, and `T-MONITOR-3`. Those tests run `humanize monitor pr --once` and assert display of YAML-list `active_bots`, separate `configured_bots`, and empty-active behavior (`tests/test-pr-loop-hooks.sh:1547-1621`). Runtime parser/display behavior is in `scripts/humanize.sh:990-1018` and `scripts/humanize.sh:1174-1222`.
- edge_cases_or_failure_modes: It does not create logs or goal trackers; it only targets one-shot monitor status parsing. It intentionally writes `active_bots:` with no list items for the empty case, which production parsing defaults to `none` (`scripts/humanize.sh:1012-1013`) and the one-shot output prints as `Active Bots:     ${active_bots:-none}` (`scripts/humanize.sh:1215-1216`). A small mismatch exists between the interactive status bar wording, which renders empty active bots as `all approved` (`scripts/humanize.sh:1091-1095`), and the caller expecting `none` in one-shot mode.
- validation_or_tests: Validated by the PR monitor tests at `tests/test-pr-loop-hooks.sh:1547-1621`; not part of the `tests/robustness` runner.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-075 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for write/edit/bash attempts against `finalize-state.md`, enforcing that Finalize Phase state is system-managed and not agent-managed.
- algorithmic_behavior: The template tells the agent it cannot modify `finalize-state.md` and redirects behavior to the allowed finalization flow: run code simplifier, commit changes, and write `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1-8`).
- inputs_outputs_state: It has no placeholders and produces static text. The rendered output is emitted by `finalize_state_file_blocked_message`, which loads this template with fallback text (`hooks/lib/loop-common.sh:376-383`). It preserves state by preventing direct changes to `finalize-state.md`.
- gates_or_invariants: The invariant is that `finalize-state.md` must only be moved by loop control paths, not edited by tools. Write validator checks `finalize-state.md` before generic `state.md` because the latter regex also matches the former (`hooks/loop-write-validator.sh:148-159`). Bash validator has the same ordering and allows only authorized cancel moves through `is_cancel_authorized` (`hooks/loop-bash-validator.sh:139-155`, `hooks/loop-bash-validator.sh:286-327`).
- dependencies_and_callers: Loaded through `hooks/lib/loop-common.sh:376-383`. Used by `hooks/loop-write-validator.sh:152-154`, `hooks/loop-edit-validator.sh` via the same common function, and `hooks/loop-bash-validator.sh:139-145` plus source-protection checks at `hooks/loop-bash-validator.sh:286-317`. Related finalize tests assert validators block writes/edits/bash modification and source moves/copies of `finalize-state.md` in `tests/test-finalize-phase.sh:298-355`.
- edge_cases_or_failure_modes: The main risk is specificity: `state.md` matching also catches `finalize-state.md`, so validator ordering is required. Authorized cancellation is the exception, but only if `.cancel-requested` exists and command shape, paths, quoting, and symlink checks pass (`hooks/lib/loop-common.sh:455-614`). Template fallback exists if the file is missing, so a missing template weakens wording but not the gate.
- validation_or_tests: Covered by finalize phase validator tests (`tests/test-finalize-phase.sh:298-355`) and cancel robustness symlink/source tests (`tests/robustness/test-cancel-security-robustness.sh:51-63`, `tests/robustness/test-cancel-security-robustness.sh:353-401`).
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-105 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize Phase transition prompt shown after Codex marks the loop complete but before the loop is allowed to terminate. It defines the allowed post-review algorithm: functionality-equivalent simplification, verification, commit, todos complete, and final summary.
- algorithmic_behavior: The prompt announces Codex review success and instructs Claude to invoke `code-simplifier:code-simplifier` via Task (`prompt-template/claude/finalize-phase-prompt.md:1-14`). It imposes non-negotiable constraints against functionality changes, test failures, new bugs, and non-equivalent refactoring (`prompt-template/claude/finalize-phase-prompt.md:16-24`). It scopes work to RLCR-session changes and simplification targets (`prompt-template/claude/finalize-phase-prompt.md:25-34`), includes plan and goal tracker placeholders (`prompt-template/claude/finalize-phase-prompt.md:35-39`), and requires todos complete, commit, and `{{FINALIZE_SUMMARY_FILE}}` write before exit (`prompt-template/claude/finalize-phase-prompt.md:40-50`).
- inputs_outputs_state: Inputs are template variables `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. The stop hook supplies them when rendering (`hooks/loop-codex-stop-hook.sh:1010-1013`). Output is a JSON block decision reason passed to Claude as the Finalize Phase prompt (`hooks/loop-codex-stop-hook.sh:1015-1020`). State transition is `state.md` renamed to `finalize-state.md` before rendering (`hooks/loop-codex-stop-hook.sh:984-986`).
- gates_or_invariants: Trigger requires last non-empty Codex line exactly `COMPLETE` (`hooks/loop-codex-stop-hook.sh:960-967`). If current round is at or beyond max iterations, Finalize Phase is skipped and the loop ends as max-iteration (`hooks/loop-codex-stop-hook.sh:968-974`). Otherwise `COMPLETE` is converted into a blocked continuation rather than final completion, requiring the finalize summary and normal safety checks before eventual completion.
- dependencies_and_callers: Loaded by `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-prompt.md"` in `hooks/loop-codex-stop-hook.sh:1010-1013`; rendering semantics come from single-pass `render_template` in `hooks/lib/template-loader.sh:50-132`. Related file protection for the new `finalize-state.md` is supplied by the block template in IMPL_PR_LOOP-HZ-075.
- edge_cases_or_failure_modes: If the template is absent or renders empty, the hook falls back to an inline prompt (`hooks/loop-codex-stop-hook.sh:990-1008`). If placeholders are malformed or omitted, single-pass rendering leaves unknown placeholders literal rather than recursively expanding them (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:116-122`). The prompt instructs a commit, so environments where no code changed after simplification can need a no-op or documented decision, but the prompt itself does not define that branch.
- validation_or_tests: Finalize transition behavior is covered by `tests/test-finalize-phase.sh`, including `COMPLETE` entering Finalize Phase with `state.md -> finalize-state.md` and prompt content mentioning code simplifier (`tests/test-finalize-phase.sh:465-480`), max-iteration skip (`tests/test-finalize-phase.sh:484-500`), and finalize completion requiring todos/summary checks (`tests/test-finalize-phase.sh:504-588`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`