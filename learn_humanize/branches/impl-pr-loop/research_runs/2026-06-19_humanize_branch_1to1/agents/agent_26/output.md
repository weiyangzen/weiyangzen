# agent_26 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-026 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Edit` validator for RLCR and PR loop control surfaces. It prevents Claude edit operations from mutating system-managed files under `.humanize/rlcr/` and `.humanize/pr-loop/`, while allowing the intended writable surfaces such as the current round summary or PR resolve file. It is registered for the `Edit` matcher in `hooks/hooks.json:24-30`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits immediately unless `tool_name == "Edit"` (`hooks/loop-edit-validator.sh:24-31`). It lowercases the file path for suffix/type checks (`:31-32`). It blocks round todo edits unless the path is allowlisted for the active RLCR loop (`:38-45`), blocks all `round-*-prompt.md` writes (`:48-51`), then applies PR-loop-specific protections before RLCR protections (`:57-77`). For RLCR files, it finds the active loop, prefers `finalize-state.md` over `state.md` during finalize, strictly parses state frontmatter, and uses `current_round` to validate editable round files (`:91-110`, `:155-180`).
- inputs_outputs_state: Inputs are hook stdin JSON, `CLAUDE_PROJECT_DIR`/`PROJECT_ROOT`, optional `LOOP_BASE_DIR`/`LOOP_DIR`, active `.humanize/rlcr/*/{state.md,finalize-state.md}`, active `.humanize/pr-loop/*/state.md`, and templates loaded through `hooks/lib/loop-common.sh`. Outputs are process exit codes and stderr block messages. Exit `0` allows the edit, exit `2` blocks with a user-facing reason, and malformed RLCR state exits `1` for fail-closed safety (`hooks/loop-edit-validator.sh:105-108`). The hook does not mutate state; it enforces which state transitions are allowed by blocking edits to protected files.
- gates_or_invariants: Only native TodoWrite should manage todos; prompt files are read-only generated inputs (`hooks/loop-edit-validator.sh:35-51`). PR-loop `state.md` and generated `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` files are read-only (`:59-70`; helper definition in `hooks/lib/loop-common.sh:729-739`). PR `round-N-pr-resolve.md` is writable only for the active PR round (`hooks/loop-edit-validator.sh:72-75`; `hooks/lib/loop-common.sh:747-790`). RLCR `state.md` and `finalize-state.md` are blocked, with finalize checked first because generic `state.md` matching would also match it (`hooks/loop-edit-validator.sh:113-125`). `plan.md` backup edits inside `.humanize/rlcr/` are blocked (`:131-138`). `goal-tracker.md` is blocked after round 0, forcing update requests through the current round summary (`:145-148`). Summary filenames must match the current round unless allowlisted (`:155-180`).
- dependencies_and_callers: Depends on `jq`, Bash, `hooks/lib/loop-common.sh`, and the template loader sourced by that library (`hooks/loop-edit-validator.sh:16-18`; `hooks/lib/loop-common.sh:135-140`). Key helper dependencies include `find_active_loop`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, `is_in_pr_loop_dir`, `is_pr_loop_readonly_file`, `validate_pr_resolve_round`, and block-message renderers. Sibling validators implement similar contracts for Read/Write/Bash. Tests cover related behavior in `tests/test-pr-loop-hooks.sh:1499-1527` for wrong-round PR resolve edits, `tests/test-finalize-phase.sh:310-319` for finalize-state edit blocking, and template reference safety in `tests/test-template-references.sh:56-64`.
- edge_cases_or_failure_modes: Invalid JSON or missing `jq` will fail through command substitution under `set -euo pipefail`, which is effectively fail-closed but not formatted as a friendly block response. If no active RLCR loop exists, RLCR-path edits are allowed after the active-loop lookup (`hooks/loop-edit-validator.sh:95-97`), while todo files with no active loop are blocked earlier (`:38-45`). PR resolve validation allows edits when no active PR loop or state file is found (`hooks/lib/loop-common.sh:756-763`). File type checks use lowercased paths, but directory containment checks use the original case, so unusual case variants of `.humanize` would not match the loop directory guards. The hook only handles `Edit`; other edit-like tools would need their own matcher/validator.
- validation_or_tests: Direct executable tests exist for PR round validation and finalize-state protection as noted above. The hook’s template references are included in the template-reference test set. I inspected the file and dependencies read-only; I did not execute tests or modify files.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-056 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for monitor cleanup behavior in `scripts/humanize.sh`, focused on graceful shutdown when `.humanize/rlcr` disappears, terminal restoration, idempotent cleanup, and signal handling. It validates behavior around the RLCR monitor’s runtime state machine rather than implementation work-item selection.
- algorithmic_behavior: Creates an isolated `/tmp/test-monitor-runtime-$$` workspace and removes it on exit (`tests/test-monitor-runtime.sh:45-53`). Test 1 builds a temporary script that sources `scripts/humanize.sh`, stubs `_restore_terminal`, defines simplified `_cleanup` and `_graceful_stop`, calls `_graceful_stop ".humanize/rlcr directory no longer exists"`, and checks for restore, cleanup, and user-friendly stop messages (`:61-135`). Test 2 verifies `cleanup_done` makes cleanup idempotent across repeated `_graceful_stop` and `_cleanup` calls (`:143-177`). Test 3 simulates the monitor-loop directory-existence check and verifies deletion triggers graceful stop (`:186-251`). Tests 4-5 grep `scripts/humanize.sh` for `_restore_terminal`, scroll-region reset, `_cleanup -> _restore_terminal`, and `_graceful_stop -> _cleanup` (`:263-323`). Tests 6-9 verify bash and zsh signal cleanup contracts and trap installation/reset behavior (`:327-490`).
- inputs_outputs_state: Inputs are `scripts/humanize.sh`, shell availability, optional `zsh`, and temporary `.humanize/rlcr` directories created under `/tmp`. Outputs are colored PASS/FAIL lines, aggregate pass/fail counts, and final exit `0` only when `TESTS_FAILED == 0` (`tests/test-monitor-runtime.sh:492-513`). Runtime state under test includes `cleanup_done=false -> true`, `monitor_running=true -> false`, terminal scroll region reset through `printf "\033[r"` in `scripts/humanize.sh:481-487`, and signal trap cleanup in `scripts/humanize.sh:537-547`.
- gates_or_invariants: `_cleanup` must run once only and must restore terminal state (`scripts/humanize.sh:494-520`). `_graceful_stop` must call `_cleanup` before printing the stop reason and cancellation/deletion explanation (`scripts/humanize.sh:522-535`). The monitor loop must return success when `.humanize/rlcr` disappears, treating deletion/cancellation as graceful stop rather than a crash (`scripts/humanize.sh:579-585`). Bash must install `trap '_cleanup' INT TERM`; zsh must define `TRAPINT` and `TRAPTERM`; `_cleanup` must reset traps to avoid repeated handling (`scripts/humanize.sh:502-504`, `:540-547`).
- dependencies_and_callers: Depends on Bash, `grep`, `chmod`, `kill`, `sleep`, `/tmp`, and `scripts/humanize.sh`. `scripts/humanize.sh` optionally sources `scripts/lib/monitor-common.sh` at load time (`scripts/humanize.sh:6-10`) and defines the monitor function `_humanize_monitor_codex` starting at `scripts/humanize.sh:219`. The tested monitor loop uses shared helpers such as `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` via wrappers (`scripts/humanize.sh:237-306`, `:569-572`).
- edge_cases_or_failure_modes: Several tests are structural or replica-based rather than a full live monitor integration. Test 1 defines local copies of `_cleanup` and `_graceful_stop` after sourcing `scripts/humanize.sh`, so it validates the intended contract and output pattern more than the actual nested function object. Test 4 includes a pipeline at `tests/test-monitor-runtime.sh:282` where `grep -q` output is piped into another `grep`; that branch is effectively inert, but the real assertion follows at `:307-310`. The zsh runtime test is skipped if `zsh` is unavailable (`:432-490`). The test writes only to `/tmp`, but it is not purely read-only when executed.
- validation_or_tests: This file is itself the validation asset. It should be run as `bash tests/test-monitor-runtime.sh` in a writable environment where `/tmp` and signal delivery are available. I inspected it read-only and did not execute it because this worker was asked to produce research notes only.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-086 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR loop’s “all todos complete before review/stop” gate. It is a prompt/message contract, not executable logic, but it defines the user-facing transition blocker when unfinished TodoWrite items remain.
- algorithmic_behavior: The template renders an incomplete todo list into a stop-blocking message. It states that the agent is trying to stop while incomplete todos remain (`prompt-template/block/incomplete-todos.md:1-5`), then requires completing all remaining todos, marking each with TodoWrite, and only afterward writing the summary and stopping (`:7-10`). It explicitly forbids proceeding to Codex review while todos remain (`:12`).
- inputs_outputs_state: The sole template input is `{{INCOMPLETE_LIST}}` (`prompt-template/block/incomplete-todos.md:5`). The runtime source is the stop hook’s transcript check: `hooks/loop-codex-stop-hook.sh` invokes `hooks/check-todos-from-transcript.py`, treats exit `1` as incomplete todos, strips the marker line, renders this template, and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:261-308`). The Python helper reads the latest TodoWrite call from a transcript and marks any todo whose status is not `completed` as incomplete (`hooks/check-todos-from-transcript.py:20-87`, `:113-126`). The state transition prevented is “Claude tries to stop -> Codex review/final summary proceeds”; the hook keeps the loop in the current work state until TodoWrite state is complete.
- gates_or_invariants: The invariant is that the latest TodoWrite list must have every todo at status `completed` before the expensive Codex review or stop path is allowed. Missing transcript path or no TodoWrite entries allows the stop path (`hooks/check-todos-from-transcript.py:99-111`), while hook input JSON parse errors are blocked separately as a checker parse error (`hooks/loop-codex-stop-hook.sh:268-285`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` with fallback text (`hooks/loop-codex-stop-hook.sh:292-298`). Depends on `hooks/check-todos-from-transcript.py` to derive `INCOMPLETE_LIST`. Related tests for todo transcript parsing live in `tests/test-todo-checker.sh`, including completed, pending, in-progress, multiple TodoWrite calls, malformed/missing fields, and unicode cases.
- edge_cases_or_failure_modes: The template itself cannot distinguish stale or semantically obsolete todos; it trusts the most recent TodoWrite payload. The checker ignores malformed JSONL transcript lines but fails on malformed hook input JSON. Todo entries missing status/content can produce sparse list text because defaults are empty strings. This block uses Claude TodoWrite statuses, not the execution-cron dual cursor checkbox states `[ ]`, `[_]`, `[x]`.
- validation_or_tests: Validated indirectly through the stop-hook path and the todo checker tests. I inspected the template and its hook caller read-only; I did not run tests.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-116 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop startup prompt block for the Round 0 “no comments yet” case. It defines the completion contract for a fresh PR where bots are expected to review automatically, so Claude should write a resolve summary and exit rather than commenting on the PR.
- algorithmic_behavior: The template tells the worker that on completion it must write a resolution summary to `{{RESOLVE_PATH}}`, note that Round 0 is awaiting initial bot reviews, note that there are no issues yet, then try to exit so the Stop Hook can poll (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-16`). It explicitly forbids commenting on the PR to trigger review because the first reviews are automatic (`:17-18`) and repeats that the Stop Hook owns polling (`:20`).
- inputs_outputs_state: Input is `{{RESOLVE_PATH}}`, prepared as `$LOOP_DIR/round-0-pr-resolve.md` in `scripts/setup-pr-loop.sh:641` and passed through `TEMPLATE_VARS` at `:651-658`. Output is appended to the startup response after the initial prompt when `COMMENT_COUNT == 0` (`scripts/setup-pr-loop.sh:810-834`). The template does not mutate state directly; it guides the user to create the PR resolve file, which the PR stop hook later requires at `hooks/pr-loop-stop-hook.sh:258-277`.
- gates_or_invariants: This block encodes the no-comments branch invariant: do not comment to trigger first review; the stop hook polls for initial bot reviews. This differs from the has-comments critical block, which requires commit/push and `gh pr comment ... please review` (`prompt-template/pr-loop/critical-requirements-has-comments.md:6-23`). The downstream stop hook still enforces a resolve file, a clean non-`.humanize` git status, and no unpushed commits before continuing (`hooks/pr-loop-stop-hook.sh:262-330`).
- dependencies_and_callers: Rendered by `scripts/setup-pr-loop.sh` after comments are fetched and the no-comments sentinel `*No comments found.*` is detected (`scripts/setup-pr-loop.sh:643-649`, `:812-834`). It coordinates with `prompt-template/pr-loop/round-0-task-no-comments.md`, which gives the same no-comment rule and stop-hook polling details in the main prompt body (`prompt-template/pr-loop/round-0-task-no-comments.md:6-30`). The PR stop hook consumes the resulting `round-0-pr-resolve.md` and handles polling/review validation.
- edge_cases_or_failure_modes: If `fetch-pr-comments.sh` output does not exactly match the no-comments sentinel, setup will choose the has-comments path instead. If bots do not actually review automatically, this prompt can lead to stop-hook polling timeouts rather than a manual trigger. The block is only appropriate for Round 0 with zero fetched comments; it intentionally omits commit/push and PR-comment instructions.
- validation_or_tests: Covered indirectly by PR-loop setup and stop-hook tests. `tests/test-pr-loop-hooks.sh` exercises stop-hook missing resolve behavior and PR-loop round/resolve protections; template reference tests scan shell render references. I inspected the template and caller read-only; I did not run tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item sections present above, one section per assigned row; item identifiers are intentionally present only in section headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`