# agent_28 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-028 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse read gate for RLCR loop artifacts. It prevents Claude from reading stale or misplaced RLCR round prompts/summaries and most todos files while allowing unrelated reads and generally leaving PR-loop files outside this validator’s RLCR path rules. Registered for `Read` in `hooks/hooks.json:33-39`.
- algorithmic_behavior: Reads hook JSON from stdin, validates syntax/shape, rejects excessive JSON depth, exits early unless `tool_name` is `Read`, then requires `tool_input.file_path` (`hooks/loop-read-validator.sh:25-48`). It lowercases the path for pattern matching (`:49`), blocks non-allowlisted `round-*-todos.md` reads (`:55-62`), and only continues round validation for `round-*-summary.md` or `round-*-prompt.md` (`:69-71`). For summary/prompt files, it finds the active RLCR loop, picks `finalize-state.md` over `state.md` during Finalize Phase, parses strict state, extracts `current_round`, then validates location, round number, and exact active-loop directory path (`:80-168`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and `tool_input.file_path`. Outputs are exit status plus stderr block templates on denial: exit `0` allow/pass-through, exit `1` malformed input or malformed state, exit `2` intentional block with rendered guidance. State inputs are `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`; it does not mutate state. Current round comes from `STATE_CURRENT_ROUND` after `parse_state_file_strict` (`:89-99`).
- gates_or_invariants: Only active newest RLCR loop is authoritative via `find_active_loop`; old session dirs are not valid. Round file reads must be inside `.humanize/rlcr/` (`hooks/lib/loop-common.sh:617-620`), match active loop exact path, and match `round-${CURRENT_ROUND}-${type}.md` unless allowlisted (`hooks/loop-read-validator.sh:122-168`). Todos are blocked unless allowlisted. Strict state parse fail-closes on missing/malformed frontmatter or nonnumeric `current_round`/`max_iterations` (`hooks/lib/loop-common.sh:240-295`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-read-validator.sh:17-19`), which provides JSON validation, path/type helpers, allowlist, state parsing, and template rendering. Uses `jq`, `grep`, `sed`, `tr`, `basename`, and `ls/sort/head` through shared helpers. Denial messages load `prompt-template/block/wrong-file-location.md`, `wrong-round-file.md`, `wrong-directory-path.md`, and common `todos-file-access.md`, with inline fallbacks (`hooks/loop-read-validator.sh:124-167`, `hooks/lib/loop-common.sh:349-356`).
- edge_cases_or_failure_modes: Non-Read tools pass through (`hooks/loop-read-validator.sh:39-41`). No active RLCR loop allows summary/prompt reads (`:84-86`). Non-round filenames pass through after `extract_round_number` returns empty (`:105-108`). Deeply nested JSON is rejected at depth over 30 (`:32-35`). Malformed state blocks for safety (`:94-98`). Allowlist is hard-coded to early historical files only: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, `round-1-summary.md` (`hooks/lib/loop-common.sh:331-336`).
- validation_or_tests: `tests/test-allowlist-validators.sh:232-281` executes read-validator allowlist cases: allows `round-1-todos.md` and `round-0-summary.md`, blocks later todos and stale summaries. `tests/robustness/test-hook-input-robustness.sh:35-115` checks valid JSON, invalid JSON, missing `tool_name`, missing `file_path`; `:177-217` checks deep nesting and non-UTF8 rejection. `tests/test-template-references.sh:56-64` scans this validator for template references and `:176-200` asserts critical validators use safe template rendering.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-058 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file validation before an RLCR state machine can start. It defines the accepted/rejected boundary for plan path safety, git ownership, tracking policy, content sufficiency, branch YAML safety, CLI parsing, and Codex model parameter safety.
- algorithmic_behavior: Creates isolated temp git repos, mocks `codex` if missing, invokes `scripts/setup-rlcr-loop.sh` with controlled plan paths/options, captures exit code and stderr/stdout, and records pass/fail counters (`tests/test-plan-file-validation.sh:36-84`). It deliberately unsets `CLAUDE_PROJECT_DIR` so setup uses the temp repo as project root (`:19-21`).
- inputs_outputs_state: Inputs are generated plan files, git repository states, branches, subdirectories, symlinks, permissions, and setup CLI flags. Output is TAP-like colored pass/fail text plus final process exit equal to `TESTS_FAILED` (`:743-752`). It mutates only its temp directory and removes it via trap (`:36-38`); it does not alter repo state.
- gates_or_invariants: Tests assert plan path must be relative, existent, parent directory existent, no spaces, no shell metacharacters, no symlink file or symlink parent traversal, resolvable/readable, inside project, outside submodules, and inside a git repo with at least one commit (`tests/test-plan-file-validation.sh:89-323`). These map to `setup-rlcr-loop.sh:258-359`. Tracking policy is exclusive: without `--track-plan-file`, tracked plans are rejected; with `--track-plan-file`, the plan must be tracked and clean (`tests/test-plan-file-validation.sh:329-426`, `scripts/setup-rlcr-loop.sh:365-410`).
- dependencies_and_callers: Depends on `git`, `mktemp`, `chmod`, shell heredocs, and `scripts/setup-rlcr-loop.sh`. The target script in turn uses `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, git commands, `codex`, and writes `.humanize/rlcr/<timestamp>/state.md` only after all validation passes (`scripts/setup-rlcr-loop.sh:186-190`, `:471-552`).
- edge_cases_or_failure_modes: Covers nonexistent file vs nonexistent parent directory with distinct messages (`tests/test-plan-file-validation.sh:104-126`), inaccessible directory resolution (`:202-243`), path escape via `..` (`:245-272`), non-git and no-commit repos (`:274-323`), branch names with YAML-unsafe colon/hash/quotes while tolerating git rejecting them first (`:432-512`), blank/sparse/comment-only plans (`:518-608`), single-line HTML comment regression so valid content after comments is accepted (`:637-661`), CLI ambiguity between `--plan-file` and positional (`:667-695`), and Codex model/effort character validation (`:701-741`).
- validation_or_tests: This file is itself the validation harness. It verifies implementation branches in `setup-rlcr-loop.sh`: absolute/space/metacharacter checks (`scripts/setup-rlcr-loop.sh:258-279`), symlink/parent/realpath/project/submodule checks (`:281-359`), git tracking cleanliness (`:365-410`), content-line counting excluding blank, hash comments, and HTML comments (`:416-469`), branch YAML safety (`:483-497`), and Codex model/effort regexes (`:499-515`). It is included in `tests/run-all-tests.sh:37`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-088 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: PR-loop blocking template for the “trigger comment required but missing” transition. It converts a stop-hook gate failure into actionable user-facing instructions without mutating state.
- algorithmic_behavior: Template renders a markdown block titled “No Trigger Comment Found,” explains that the PR loop is waiting for a trigger comment, includes startup case, current round, bot mention string, and an example comment (`prompt-template/block/no-trigger-comment.md:1-17`). It is rendered by `hooks/pr-loop-stop-hook.sh` when `REQUIRE_TRIGGER=true` and no `PR_LAST_TRIGGER_AT` exists (`hooks/pr-loop-stop-hook.sh:717-735`).
- inputs_outputs_state: Inputs are `STARTUP_CASE`, `STARTUP_CASE_DESC`, `CURRENT_ROUND`, and `BOT_MENTION_STRING` passed through `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:729-731`). Output is embedded in JSON as `decision: block`, `reason: <rendered template>`, and `systemMessage: "PR Loop: Missing trigger comment - please @mention bots first"` (`:733-735`). It does not read or write files at runtime beyond template loading.
- gates_or_invariants: The upstream invariant is trigger requirement logic: round greater than 0 always requires trigger; new commits detected require trigger; round 0 startup cases 4/5 require trigger; cases 1/2/3 do not, unless overridden by new commits (`hooks/pr-loop-stop-hook.sh:677-708`). This template is only used after trigger validation and before Claude eyes verification, preventing eyes checks against stale or absent trigger ids (`:711-743`).
- dependencies_and_callers: Caller is `hooks/pr-loop-stop-hook.sh`; state variables originate from PR state frontmatter parsed at `hooks/pr-loop-stop-hook.sh:87-174`, including `current_round`, `last_trigger_at`, `startup_case`, and `trigger_comment_id`. Mention string is built from configured bots via `build_bot_mention_string` (`hooks/pr-loop-stop-hook.sh:178-183`). Rendering uses `hooks/lib/template-loader.sh`, whose renderer is single-pass and preserves unknown placeholders (`hooks/lib/template-loader.sh:50-132`).
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` falls back to a compact message telling the user to run `gh pr comment ...` (`hooks/pr-loop-stop-hook.sh:726-731`; safe fallback behavior in `hooks/lib/template-loader.sh:167-203`). Unknown startup case defaults to no trigger upstream (`hooks/pr-loop-stop-hook.sh:703-706`), so this block is not reached for that case unless another rule requires it. Startup case descriptions are specialized for cases 4 and 5; all other required-trigger paths use “Subsequent round requires trigger” (`:719-724`).
- validation_or_tests: PR-loop tests exercise the trigger gate. `tests/test-pr-loop-system.sh:1112-1166` models startup case trigger requirements, including cases 1/2/3 false, case 4 true, and later rounds true. `tests/test-pr-loop-stophook.sh:647-666` sets state with `startup_case: 4`, round 0, and empty trigger fields for the missing-trigger path. Template existence and safe rendering are covered generically by `tests/test-template-references.sh`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-118 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: Initial PR-loop prompt header template. It frames round 0 for Claude by naming the PR, source branch, active bots, and introducing fetched review comments.
- algorithmic_behavior: Static markdown with placeholders for `PR_NUMBER`, `START_BRANCH`, and `ACTIVE_BOTS_DISPLAY` (`prompt-template/pr-loop/round-0-header.md:1-15`). During PR loop setup, `scripts/setup-pr-loop.sh` renders it into `.humanize/pr-loop/<timestamp>/round-0-prompt.md`, then appends fetched comment content and a task template selected by whether comments exist (`scripts/setup-pr-loop.sh:651-684`, `:686-771`).
- inputs_outputs_state: Inputs come from setup-time PR detection and state initialization: PR number, current git branch, and active bot list. `TEMPLATE_VARS` also includes `RESOLVE_PATH` and `BOT_MENTION_STRING`, though this header uses only the first three (`scripts/setup-pr-loop.sh:651-658`). Output is the first section of `round-0-prompt.md` (`:677-684`) that is printed to the user at setup completion (`:808`).
- gates_or_invariants: Setup requires at least one bot flag, no active RLCR/PR loop, a git repo with a commit, GitHub CLI, PR metadata, and reviewer status before this header is generated (`scripts/setup-pr-loop.sh:175-260`, `:425-455`). The header is always round 0 specific; subsequent rounds are generated later by the stop hook rather than this setup template.
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh`. It uses `hooks/lib/template-loader.sh` with safe fallback (`scripts/setup-pr-loop.sh:37-43`, `:660-678`). The PR state file created immediately before prompt generation stores `current_round: 0`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `startup_case`, commit metadata, and trigger fields (`scripts/setup-pr-loop.sh:535-555`), keeping the rendered prompt aligned with loop state.
- edge_cases_or_failure_modes: If the template cannot load, setup uses an inline fallback header with the same placeholders and structure (`scripts/setup-pr-loop.sh:660-678`). If fetched comments contain the sentinel `*No comments found.*`, setup appends a no-comments task telling Claude to wait for automatic bot reviews; otherwise it appends comment-processing/review tasks (`:643-649`, `:686-710`). Startup cases 4/5 may post a trigger comment before state and prompt creation; failure to post or retrieve needed Claude trigger reaction aborts and removes the loop dir (`:443-521`).
- validation_or_tests: Template rendering safety is covered by `hooks/lib/template-loader.sh:167-203`. PR setup/system tests cover startup cases and state/prompt flow broadly, including startup case 4/5 detection and failure path (`tests/test-pr-loop-system.sh:660`, `:1380-1421`). Template reference scanning currently checks hook/common template references rather than this setup script specifically, so direct coverage for this exact header path is weaker than the stop-hook block templates.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item evidence sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`