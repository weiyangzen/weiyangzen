# agent_29 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-029 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` validator for both RLCR and PR loop managed files. It is registered for `Write` in `hooks/hooks.json:14-21` and blocks writes that would corrupt loop state, generated prompts/comments, wrong-round summaries, or protected plan backups.
- algorithmic_behavior: Reads hook JSON from stdin, validates JSON/tool shape, exits early unless `tool_name == "Write"` (`hooks/loop-write-validator.sh:25-45`). It normalizes `file_path` for pattern checks (`:48-49`), blocks RLCR `round-*-todos.md` unless allowlisted (`:55-63`), blocks `round-*-prompt.md` (`:65-68`), then performs PR-loop-specific gating before RLCR gating (`:70-94`). For RLCR paths, it classifies summary/finalize/humanize-loop files (`:100-117`), locates the active loop (`:123-130`), parses `state.md` or `finalize-state.md` strictly (`:132-145`), and validates the target path against the active loop and current round (`:228-263`).
- inputs_outputs_state: Inputs are stdin JSON with `.tool_name` and `.tool_input.file_path`, `CLAUDE_PROJECT_DIR` or cwd, active `.humanize/rlcr/*` and `.humanize/pr-loop/*` directories, and state frontmatter. Outputs are exit `0` allow, exit `1` invalid/malformed input or malformed RLCR state, and exit `2` blocked write with a rendered markdown reason on stderr. It does not mutate state; it enforces allowed state transitions by allowing only the current PR `round-N-pr-resolve.md`, current RLCR `round-N-summary.md`, and active-loop finalize summary.
- gates_or_invariants: Invalid JSON, missing `tool_name`, missing `tool_input.file_path`, null bytes, invalid UTF-8, and JSON depth over 30 fail before path logic via shared helpers (`hooks/lib/loop-common.sh:55-129`). PR loop `state.md` is blocked (`hooks/loop-write-validator.sh:76-81`); generated PR loop files `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` are blocked (`:83-87`; helper definitions at `hooks/lib/loop-common.sh:720-739`). PR resolve writes must match active PR round (`hooks/loop-write-validator.sh:89-93`; `hooks/lib/loop-common.sh:747-790`). RLCR `state.md` and `finalize-state.md` are blocked, with finalize checked first because `state.md` matching is broad (`hooks/loop-write-validator.sh:152-160`). Active-loop `finalize-summary.md` is allowed (`:168-173`). `.humanize/rlcr/.../plan.md` is always blocked as a plan backup (`:179-185`). `goal-tracker.md` is blocked after round 0 (`:192-195`). Summary files outside `.humanize/rlcr` or in the wrong round/directory are blocked (`:202-210`, `:228-263`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, loop discovery, strict state parsing, path classifiers, PR round validation, allowlist, and block messages (`hooks/loop-write-validator.sh:17-20`). Depends on `hooks/lib/template-loader.sh` for `load_and_render_safe`; template directory resolution is defined at `hooks/lib/template-loader.sh:24-31`. Uses `prompt-template/block/plan-backup-protected.md` at `hooks/loop-write-validator.sh:182`. Sibling validators mirror major invariants for other tools: Edit plan protection at `hooks/loop-edit-validator.sh:131-137`, Bash plan protection at `hooks/loop-bash-validator.sh:336-340`. Called by Claude hook config in `hooks/hooks.json:14-21`.
- edge_cases_or_failure_modes: Non-`Write` tools return allow immediately (`hooks/loop-write-validator.sh:39-41`). If no active RLCR loop exists, RLCR summary/state-style checks fall through to allow after prompt/todos/PR checks (`:128-130`). Malformed active RLCR state fails closed (`:140-144`). PR resolve validation uses the newest active PR loop found by `find_active_pr_loop` and checks round number, not full directory identity (`hooks/lib/loop-common.sh:702-718`, `:747-790`). The allowlist permits specific legacy RLCR files only for todos/summary handling (`hooks/lib/loop-common.sh:327-345`). Exact path equality checks use original-case `FILE_PATH`, while pattern checks use lowercase, which matters on case-sensitive filesystems.
- validation_or_tests: Direct PR-loop tests cover state write blocking, PR comment write blocking, PR resolve allow, and wrong-round PR resolve blocking at `tests/test-pr-loop-hooks.sh:68-163` and `:1431-1494`. Bash no-RLCR PR state protection is tested at `tests/test-pr-loop-hooks.sh:174-210`. RLCR plan backup write blocking is covered at `tests/test-plan-file-hooks.sh:194-204`, and legacy `.humanize-loop.local` plan writes are allowed at `tests/test-plan-file-hooks.sh:1030-1040`. I inspected tests only; I did not execute them in this read-only research run.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-059 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification module for PR-loop hook behavior. It defines `run_hook_tests()` (`tests/test-pr-loop-hooks.sh:15`) and is sourced by the main PR-loop runner at `tests/test-pr-loop.sh:32-44`, alongside script and stop-hook modules.
- algorithmic_behavior: Exercises PR-loop validator contracts, comment ordering, gatekeeper parsing, stop-hook trigger logic, mocked end-to-end stop-hook flows, approval-only review handling, fixture-backed fetch/poll behavior, wrong-round protection, and monitor display. It uses shell test helpers `pass`/`fail` from `tests/test-helpers.sh:30-44` and temp/mock environments created under `TEST_DIR`.
- inputs_outputs_state: Inputs include synthetic `.humanize/pr-loop/<timestamp>/state.md` files, hook JSON payloads, mocked `gh`/`git` binaries, fixture-backed GitHub API responses, and PR-loop state fields such as `current_round`, `configured_bots`, `active_bots`, `started_at`, and `last_trigger_at`. Outputs are PASS/FAIL lines and global test counters. The tests model state transitions such as `active_bots` shrinking on approvals/timeouts, previously approved bots being re-added when new issues appear, `last_trigger_at` taking precedence over `started_at`, and next-round file naming.
- gates_or_invariants: Validator invariants include YAML-list `active_bots` storage (`tests/test-pr-loop-hooks.sh:26-64`), PR-loop `state.md` write protection (`:68-97`), generated PR comment write protection (`:102-130`), current-round `pr-resolve` writes allowed (`:135-163`), and Bash state protection even when no RLCR loop exists (`:174-210`). Comment/gate invariants include newest-first timestamp sorting (`:222-237`), human comments before bot comments (`:239-255`), comment ID deduplication (`:271-286`), YAML `configured_bots` parsing (`:288-324`), bot status extraction from Codex output (`:326-351`), re-adding a bot with new issues (`:353-388`), trigger mention detection (`:390-413`), and final marker detection for `APPROVE` and `WAITING_FOR_BOTS` (`:415-457`).
- dependencies_and_callers: Called from `tests/test-pr-loop.sh:32-44`. Calls `hooks/loop-write-validator.sh` in validator tests (`tests/test-pr-loop-hooks.sh:90`, `:123`, `:156`, `:1453`, `:1487`), `hooks/loop-bash-validator.sh` at `:198`, `hooks/loop-edit-validator.sh` at `:1520`, and `hooks/pr-loop-stop-hook.sh` in E2E tests at `:833`, `:934`, `:1040`, and `:1128`. Uses `scripts/fetch-pr-comments.sh` at `:1251` and `scripts/poll-pr-reviews.sh` at `:1332` and `:1381`. Uses fixture mock setup at `:1231-1238` and monitor setup helpers at `:1552`, `:1579`, and `:1601`.
- edge_cases_or_failure_modes: Some sections are pure algorithm simulations rather than executing production code, for example timestamp selection and timeout anchoring at `tests/test-pr-loop-hooks.sh:606-696`. The stop-hook E2E tests rely on mocked `gh` output shape, including paginated `--jq` object streams (`:947-1049`). Approval-only reviews with empty bodies must be preserved via `[Review state: ...]` placeholders (`:1160-1195`). `test_state_has_configured_bots` counts all YAML list items in the state file (`:715-720`), so it proves list entries exist but is not a precise parser assertion for only `configured_bots`. The module is not standalone; it expects sourced helpers and initialized PR-loop test env from `tests/test-pr-loop.sh`.
- validation_or_tests: This file is itself the validation surface. Its E2E assertions cover missing resolve blocking (`tests/test-pr-loop-hooks.sh:766-840`), trigger detection and persistence/logging (`:845-944`), pagination across API pages (`:947-1049`), round-0 trigger timestamp priority (`:1052-1148`), fixture-backed comment fetch and filtering (`:1242-1319`), fixture-backed review polling and filtering (`:1322-1419`), wrong-round write/edit blocking (`:1431-1535`), and monitor active/configured bot display (`:1547-1621`). I inspected the file and related call sites; I did not execute the suite.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-089 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR plan-backup invariant. It tells the agent that `.humanize/rlcr/.../plan.md` is a backup of the original plan and must not be modified (`prompt-template/block/plan-backup-protected.md:1-7`).
- algorithmic_behavior: The template has no placeholders or branching. Validators render it when a tool attempts to modify `plan.md` inside an RLCR loop directory, replacing inline fallback text with a stable, user-facing protection reason.
- inputs_outputs_state: Input is only the template file selected by `load_and_render_safe`; there are no template variables. Output is markdown text written to stderr by validators. It does not mutate state, but it preserves the RLCR source-plan snapshot so later loop rounds compare against an immutable backup.
- gates_or_invariants: Establishes the invariant that the loop-local `plan.md` is read-only and should be read for reference instead of edited (`prompt-template/block/plan-backup-protected.md:3-7`). This supports plan integrity throughout the RLCR loop (`:5`).
- dependencies_and_callers: Rendered by `hooks/loop-write-validator.sh:179-185`, `hooks/loop-edit-validator.sh:131-137`, and `hooks/loop-bash-validator.sh:336-340`. Loaded through `hooks/lib/template-loader.sh`, whose safe loader falls back if the file is missing.
- edge_cases_or_failure_modes: Because the template has no variables, it cannot include the attempted path or action-specific wording; Write/Edit/Bash fallbacks are more specific, but the loaded template is generic. Protection is scoped by caller logic to `/.humanize/rlcr/.../plan.md`; legacy `.humanize-loop.local` plan paths are intentionally allowed by tests.
- validation_or_tests: Write protection is validated at `tests/test-plan-file-hooks.sh:194-204`; legacy non-RLCR plan path allow behavior is validated at `tests/test-plan-file-hooks.sh:1030-1040`. Sibling Edit/Bash protections are visible in their validators, but I did not execute tests.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-119 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template used when fetched PR comments already exist. It defines the agent’s required behavior for resolving existing review feedback before the stop hook polls for fresh bot reviews.
- algorithmic_behavior: The template instructs the agent to analyze comments with human comments taking precedence and bot comments newest first (`prompt-template/pr-loop/round-0-task-has-comments.md:6-9`), fix issues and add tests if needed (`:10-13`), commit/push changes, post a bot-triggering PR comment (`:15-21`), and write a resolution summary to `@{{RESOLVE_PATH}}` (`:23-27`). It then states the stop-hook loop: poll for reviews, use local Codex validation, continue on issues, end on approval (`:39-43`).
- inputs_outputs_state: Inputs are rendered placeholders `{{PR_NUMBER}}`, `{{BOT_MENTION_STRING}}`, and `{{RESOLVE_PATH}}`. `scripts/setup-pr-loop.sh` sets `RESOLVE_PATH` to `round-0-pr-resolve.md` and builds the template variable array at `scripts/setup-pr-loop.sh:641-658`. Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` after the fetched comment file (`scripts/setup-pr-loop.sh:681-684`, `:770-771`). The expected produced artifact is the current-round resolution summary, which the stop hook checks before polling.
- gates_or_invariants: Enforces “do not modify state files” for `.humanize/pr-loop` (`prompt-template/pr-loop/round-0-task-has-comments.md:30-33`), requires fixes to be pushed so remote bots can review them (`:32-34`), requires the correct bot mention trigger format (`:18-21`, `:33-34`), and requires a thorough resolution summary (`:23-27`, `:35`). The template relies on validators to enforce the state-file and generated-file write protections mechanically.
- dependencies_and_callers: Selected by `scripts/setup-pr-loop.sh` only when the fetched comment file does not contain the exact no-comments sentinel (`scripts/setup-pr-loop.sh:643-649`, `:686-768`). It depends on `scripts/fetch-pr-comments.sh` for the preceding comment content and on `hooks/pr-loop-stop-hook.sh` for the post-summary poll/validation behavior, including missing-trigger and review polling gates around `hooks/pr-loop-stop-hook.sh:710-868`. `hooks/loop-write-validator.sh` allows only current-round `pr-resolve` writes and blocks generated prompt/comment files.
- edge_cases_or_failure_modes: Sentinel-based comment detection means any fetch output other than exactly `*No comments found.*` selects this “has comments” flow (`scripts/setup-pr-loop.sh:643-649`). The template always says to commit, push, and trigger review after fixes; if comments are informational or invalid, the agent still needs to summarize what was addressed and may need to explain no code changes. The trigger command depends on the rendered bot mention string; if active bot state is stale, the stop hook’s configured/active bot logic governs actual polling.
- validation_or_tests: The fallback content in `scripts/setup-pr-loop.sh:723-767` mirrors this template, giving a built-in degraded path if template loading fails. Stop-hook behavior promised by the template is covered by `tests/test-pr-loop-hooks.sh` E2E and gate tests for missing resolve, trigger detection, pagination, timestamp priority, and review polling (`tests/test-pr-loop-hooks.sh:766-1148`, `:1242-1419`). I inspected the references only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`