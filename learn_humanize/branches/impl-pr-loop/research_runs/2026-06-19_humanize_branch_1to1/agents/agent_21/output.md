# agent_21 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-021 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR algorithm. It is a thin command wrapper whose only allowed tool is `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` (`commands/start-rlcr-loop.md:3-4`).
- algorithmic_behavior: Runs `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` (`commands/start-rlcr-loop.md:10-14`), then defines the loop contract: implement the plan, write a round summary, stop-hook triggers Codex review, Codex feedback continues the loop, and final `COMPLETE` exits (`commands/start-rlcr-loop.md:16-23`). It also defines the Goal Tracker as drift control with immutable Ultimate Goal/Acceptance Criteria and mutable tasks, completed items, deferrals, and plan evolution (`commands/start-rlcr-loop.md:24-41`).
- inputs_outputs_state: Inputs are the command arguments shown in the hint: plan path or `--plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, and `--push-every-round` (`commands/start-rlcr-loop.md:3`). The delegated setup script validates those inputs, rejects active RLCR/PR loops, validates plan path/tracking/content, requires `codex`, creates `.humanize/rlcr/<timestamp>/state.md`, copies `plan.md`, creates `goal-tracker.md`, and emits `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:104-180`, `scripts/setup-rlcr-loop.sh:196-217`, `scripts/setup-rlcr-loop.sh:258-345`, `scripts/setup-rlcr-loop.sh:385-424`, `scripts/setup-rlcr-loop.sh:471-530`, `scripts/setup-rlcr-loop.sh:539-650`, `scripts/setup-rlcr-loop.sh:655-757`).
- gates_or_invariants: Important command-level invariants are: always write summaries, keep `goal-tracker.md` current, include implemented files/tests, do not edit loop state or run cancel commands to escape, and trust Codex feedback (`commands/start-rlcr-loop.md:43-49`). The setup script enforces one active loop only, relative non-symlink plan paths inside the project, no shell metacharacters/spaces in plan paths, valid YAML-safe branch/model/effort values, and plan tracking cleanliness (`scripts/setup-rlcr-loop.sh:196-217`, `scripts/setup-rlcr-loop.sh:258-318`, `scripts/setup-rlcr-loop.sh:385-410`, `scripts/setup-rlcr-loop.sh:489-515`).
- dependencies_and_callers: Depends directly on `scripts/setup-rlcr-loop.sh`. Subsequent behavior is enforced by hooks, especially `hooks/loop-codex-stop-hook.sh`, which finds the active loop, parses `state.md`/`finalize-state.md`, checks summaries/todos/git state, builds Codex review prompts, and transitions state (`hooks/loop-codex-stop-hook.sh:59-90`, `hooks/loop-codex-stop-hook.sh:520-660`, `hooks/loop-codex-stop-hook.sh:672-760`, `hooks/loop-codex-stop-hook.sh:960-1026`).
- edge_cases_or_failure_modes: Stops by max iteration, Codex `COMPLETE`, or `/humanize:cancel-rlcr-loop` (`commands/start-rlcr-loop.md:51-55`). A small wording mismatch exists: this command says full alignment at rounds 4, 9, 14 “after every 4 rounds of work” (`commands/start-rlcr-loop.md:36`), while the stop hook implements `CURRENT_ROUND % 5 == 4` and the full-alignment template says every 5 rounds (`hooks/loop-codex-stop-hook.sh:686-690`, `prompt-template/codex/full-alignment-review.md:3`).
- validation_or_tests: Covered indirectly by setup robustness/path/plan tests and by `tests/test-finalize-phase.sh` for downstream COMPLETE/finalize transitions. `tests/run-all-tests.sh` includes `test-finalize-phase.sh` in the ordered suite (`tests/run-all-tests.sh:32-46`).
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-051 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the Finalize Phase algorithm introduced between Codex `COMPLETE` and terminal loop completion. It enumerates positive and negative requirements for state transitions, hook gates, validator behavior, and Codex bypass (`tests/test-finalize-phase.sh:3-20`).
- algorithmic_behavior: Builds an isolated temp git repo, writes a minimal plan and loop state, creates mock `codex` binaries, and drives production hooks with JSON hook input (`tests/test-finalize-phase.sh:40-66`, `tests/test-finalize-phase.sh:87-168`). It verifies helper predicates, active-loop detection, validator permissions, stop-hook finalize completion, COMPLETE handling, max-iteration behavior, todo blocking, normal-round behavior, and finalize-state parsing across validators (`tests/test-finalize-phase.sh:179-670`).
- inputs_outputs_state: Inputs include synthetic `.humanize/rlcr/2024-01-01_12-00-00/state.md`, `finalize-state.md`, `complete-state.md`, summaries, transcript JSONL, and mock Codex outputs (`tests/test-finalize-phase.sh:123-168`, `tests/test-finalize-phase.sh:231-248`, `tests/test-finalize-phase.sh:366-375`, `tests/test-finalize-phase.sh:451-463`, `tests/test-finalize-phase.sh:520-528`). Outputs asserted are hook exit codes, block JSON containing `"decision": "block"`, marker files proving Codex invocation or non-invocation, review-result files, and state renames to `finalize-state.md`, `complete-state.md`, or `maxiter-state.md`.
- gates_or_invariants: Key invariants are: `finalize-state.md` is active but `complete-state.md` is not (`tests/test-finalize-phase.sh:231-260`); `finalize-summary.md` is writable only in the active finalize loop (`tests/test-finalize-phase.sh:282-295`); `finalize-state.md` cannot be written, edited, moved, or copied by normal tools (`tests/test-finalize-phase.sh:298-355`); finalize exit still requires summary, clean git state, and completed todos (`tests/test-finalize-phase.sh:370-412`, `tests/test-finalize-phase.sh:504-546`); finalize completion must not run Codex (`tests/test-finalize-phase.sh:387-393`, `tests/test-finalize-phase.sh:439-445`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for helpers (`tests/test-finalize-phase.sh:48-49`). Exercises `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-plan-file-validator.sh` (`tests/test-finalize-phase.sh:287-325`, `tests/test-finalize-phase.sh:377-405`, `tests/test-finalize-phase.sh:468-493`, `tests/test-finalize-phase.sh:530-583`, `tests/test-finalize-phase.sh:633-662`). The shared helper `find_active_loop` treats `state.md` and `finalize-state.md` as active only in the newest loop dir (`hooks/lib/loop-common.sh:147-170`).
- edge_cases_or_failure_modes: Covers max iteration skipping Finalize and creating `maxiter-state.md` (`tests/test-finalize-phase.sh:483-501`), incomplete todos blocking before Codex (`tests/test-finalize-phase.sh:520-546`), normal non-COMPLETE review preserving `state.md` and incrementing `current_round` (`tests/test-finalize-phase.sh:580-607`), and validator parsing when only `finalize-state.md` exists (`tests/test-finalize-phase.sh:621-670`). Production stop hook confirms finalize summary selection and Codex bypass on finalize completion (`hooks/loop-codex-stop-hook.sh:525-557`, `hooks/loop-codex-stop-hook.sh:649-660`).
- validation_or_tests: This is itself validation and is registered in `tests/run-all-tests.sh` (`tests/run-all-tests.sh:32-46`). I did not execute it because the branch export is read-only/managed; the test creates temp repos and invokes git/mock binaries, so direct inspection is the appropriate evidence mode here.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-081 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Blocking-message template for the RLCR “local commits by default” gate. It explains that ordinary `git push` is not needed and points users to `--push-every-round` if they want push enforcement (`prompt-template/block/git-push.md:1-9`).
- algorithmic_behavior: The template does not execute logic itself; it is loaded by the Bash validator when an active RLCR loop has `push_every_round` not equal to `true` and the attempted Bash command starts with `git push` (`hooks/loop-bash-validator.sh:88-105`). The rendered output becomes the stderr reason before the validator exits `2`, blocking the tool call.
- inputs_outputs_state: Input state comes from parsed loop metadata field `push_every_round` in `state.md` (`scripts/setup-rlcr-loop.sh:539-551`, `hooks/loop-bash-validator.sh:81-96`). Input command is normalized as `COMMAND_LOWER` by the Bash validator. Output is the markdown block beginning `# Git Push Blocked` and containing the suggested `/humanize:start-rlcr-loop plan.md --push-every-round` invocation (`prompt-template/block/git-push.md:1-9`).
- gates_or_invariants: Invariant: during default RLCR mode, commits stay local and `git push` via Bash is blocked; when `--push-every-round` is enabled, this particular block is bypassed and the stop hook instead requires unpushed commits to be pushed before exit (`hooks/loop-bash-validator.sh:96-105`, `hooks/loop-codex-stop-hook.sh:488-518`).
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md" "$FALLBACK"` (`hooks/loop-bash-validator.sh:99-104`). Template loading is provided by `hooks/lib/template-loader.sh`, whose safe loader falls back if a template is missing or renders empty (`hooks/lib/template-loader.sh:167-195`).
- edge_cases_or_failure_modes: The validator pattern only matches commands beginning with whitespace then `git push` (`hooks/loop-bash-validator.sh:96-99`), so aliased/wrapped push forms are not part of this template’s direct gate. If the template is missing, the fallback message still blocks (`hooks/loop-bash-validator.sh:99-104`, `hooks/lib/template-loader.sh:176-186`).
- validation_or_tests: `tests/test-template-loader.sh` verifies `block/git-push.md` loads and that `load_and_render_safe` uses the real template instead of fallback (`tests/test-template-loader.sh:55-65`, `tests/test-template-loader.sh:197-222`). `tests/test-templates-comprehensive.sh` also checks safe loading of this template (`tests/test-templates-comprehensive.sh:456-462` from search evidence).
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-111 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Codex review prompt template for mandatory full goal-alignment checkpoints. It governs the review contract at alignment rounds: audit the original plan, Goal Tracker, implementation claims, historical progress, stagnation, and final marker semantics (`prompt-template/codex/full-alignment-review.md:1-93`).
- algorithmic_behavior: The stop hook selects this template when `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:686-690`). It renders the template with current round, plan path, summary content, goal tracker path, docs path, derived iteration/history fields, previous round numbers, and review-result path (`hooks/loop-codex-stop-hook.sh:692-734`). Codex is expected to write findings to `round-N-review-result.md`; the stop hook then reads the last non-empty line and interprets exact `COMPLETE` or `STOP` markers (`hooks/loop-codex-stop-hook.sh:902-965`).
- inputs_outputs_state: Inputs are `@{{PLAN_FILE}}`, `{{SUMMARY_CONTENT}}`, `@{{GOAL_TRACKER_FILE}}`, `@{{DOCS_PATH}}`, `{{COMPLETED_ITERATIONS}}`, `{{LOOP_TIMESTAMP}}`, `{{PREV_ROUND}}`, `{{PREV_PREV_ROUND}}`, and `@{{REVIEW_RESULT_FILE}}` (`prompt-template/codex/full-alignment-review.md:5-17`, `prompt-template/codex/full-alignment-review.md:19-57`, `prompt-template/codex/full-alignment-review.md:59-85`, `prompt-template/codex/full-alignment-review.md:86-93`). Output is a Codex review file whose final line drives state: `COMPLETE` enters Finalize Phase unless at max iterations, while `STOP` terminates via circuit breaker (`hooks/loop-codex-stop-hook.sh:966-1026`, `hooks/loop-codex-stop-hook.sh:1028-1055`).
- gates_or_invariants: Strong completion invariant: Codex may write `COMPLETE` only when all original plan tasks are done, all ACs are fully met, and no deferrals remain (`prompt-template/codex/full-alignment-review.md:88-93`). Deferred ACs count as incomplete (`prompt-template/codex/full-alignment-review.md:91-93`). Stagnation invariant: if repeated issues/no progress/circular feedback are found in the last 5 rounds, Codex must end with `STOP` instead of `COMPLETE` (`prompt-template/codex/full-alignment-review.md:74-85`).
- dependencies_and_callers: Depends on `prompt-template/codex/goal-tracker-update-section.md` being embedded into `{{GOAL_TRACKER_UPDATE_SECTION}}` by the stop hook (`hooks/loop-codex-stop-hook.sh:680-684`, `hooks/loop-codex-stop-hook.sh:723-734`). Rendered by `load_and_render_safe`, so missing template falls back to a shorter full-alignment prompt rather than breaking the loop (`hooks/loop-codex-stop-hook.sh:698-708`, `hooks/loop-codex-stop-hook.sh:721-734`).
- edge_cases_or_failure_modes: A `STOP` marker on a full-alignment round is treated as a circuit breaker with `stop-state` via `end_loop`; a `STOP` on a non-alignment round is unusual but still honored (`hooks/loop-codex-stop-hook.sh:1028-1055`). A `COMPLETE` marker at or beyond max iterations skips Finalize and exits as max iteration (`hooks/loop-codex-stop-hook.sh:966-973`). Any non-marker final line continues the loop, increments `current_round`, and creates the next prompt (`hooks/loop-codex-stop-hook.sh:1058-1120`).
- validation_or_tests: Template existence/loading is covered by template reference/comprehensive tests (`tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh` from search evidence). The finalize-phase test verifies downstream marker handling for `COMPLETE`, including transition to `finalize-state.md`, max-iteration bypass, normal review continuation, and review-result creation (`tests/test-finalize-phase.sh:451-501`, `tests/test-finalize-phase.sh:552-619`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `IMPL_PR_LOOP-HZ-021`, `IMPL_PR_LOOP-HZ-051`, `IMPL_PR_LOOP-HZ-081`, `IMPL_PR_LOOP-HZ-111`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`