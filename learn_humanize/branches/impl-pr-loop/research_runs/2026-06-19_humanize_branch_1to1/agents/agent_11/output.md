# agent_11 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt fragments that drive RLCR loop transitions after Codex review. The directory is not executable code, but it defines the worker contract for next-round remediation, finalize-phase refactoring, goal-tracker update requests, post-alignment priorities, summary writing, commits, and push-every-round behavior.
- algorithmic_behavior: Recursively contains six templates. `next-round-prompt.md:1-31` starts a non-final round, injects `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{GOAL_TRACKER_FILE}}`, requires todos for all discovered issues, and makes the goal tracker read-only after Round 0. `next-round-footer.md:4-9` forbids false loop exit, direct loop-state edits, and `cancel-rlcr-loop`, then requires optional simplification, commit, and summary to `{{NEXT_SUMMARY_FILE}}`. `goal-tracker-update-request.md:2-16` standardizes a summary section for proposed tracker mutations that Codex may accept. `post-alignment-action-items.md:2-7` augments a round after full alignment checks with forgotten-item, unmet-AC, and unjustified-deferral priorities. `push-every-round-note.md:2` adds the remote-push requirement when that mode is enabled. `finalize-phase-prompt.md:1-50` transitions to finalize after Codex passes review and requires functionality-equivalent simplification, passing tests, commit, and `{{FINALIZE_SUMMARY_FILE}}`.
- inputs_outputs_state: Inputs are rendered template variables such as `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_CONTENT`, `NEXT_SUMMARY_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is prompt text consumed by Claude. State-wise, these templates describe transitions from “review found work” to next round, from “Codex review passed” to Finalize Phase, and from “post-alignment check ran” to a prioritized remediation round.
- gates_or_invariants: Invariants are prompt-level rather than shell-enforced: preserve original implementation plan scope, create todos for all issues, do not directly modify `goal-tracker.md` after Round 0, do not exit by lying or editing loop state, commit changes, write the expected summary, push only when configured, and in Finalize Phase only perform functionality-equivalent simplification while keeping tests passing.
- dependencies_and_callers: Templates are loaded through the shared template loader, whose single-pass substitution and fallback behavior are defined in `hooks/lib/template-loader.sh:50-57` and `hooks/lib/template-loader.sh:170-203`. `hooks/loop-codex-stop-hook.sh` references `claude/finalize-phase-prompt.md` around its finalize handoff and `claude/next-round-prompt.md` around next-round prompt construction; template reference coverage also includes `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: Missing variables remain as `{{VAR}}` placeholders by design in `render_template`, so a bad caller can leak unresolved placeholders into a prompt. Missing template files fall back only when callers use `load_and_render_safe`; otherwise content can be empty. These templates cannot enforce behavior alone; enforcement depends on hook validators and Codex stop-hook state transitions.
- validation_or_tests: Template existence/reference integrity is covered by `tests/test-template-references.sh`. Finalize behavior is also exercised by `tests/test-finalize-phase.sh`, while the runtime composition path depends on `hooks/loop-codex-stop-hook.sh`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-041 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime IO gate for the `gen-plan` workflow. It prevents plan generation from starting unless the draft input is readable and non-empty and the output target is a new file in an existing writable directory.
- algorithmic_behavior: Parses only `--input`, `--output`, `-h`, and `--help` in `scripts/validate-gen-plan-io.sh:28-55`; missing values, flag-as-value, unknown options, missing required arguments, and help all route through `usage()` and exit `6` (`scripts/validate-gen-plan-io.sh:15-23`, `57-66`). It normalizes paths through `realpath -m` with fallback at `scripts/validate-gen-plan-io.sh:68-71`, then runs ordered checks: input exists (`78-84`), input non-empty (`86-92`), output directory exists (`94-100`), output is not a directory (`102-108`), output does not already exist (`110-115`), and output directory is writable (`117-123`). Success emits `VALIDATION_SUCCESS`, input line count, output target, and exits `0` at `scripts/validate-gen-plan-io.sh:125-131`.
- inputs_outputs_state: Inputs are command-line arguments and filesystem state for the input path, output path, output directory, and permissions. Outputs are stdout diagnostics with `VALIDATION_ERROR:*` or `VALIDATION_SUCCESS` plus distinct exit codes `0-6`. It does not mutate repo state, create directories, or create the output file.
- gates_or_invariants: Exit code contract is part of the command algorithm: `1` input missing, `2` input empty, `3` output directory missing, `4` output path exists or is a directory, `5` no write permission, `6` invalid arguments. The output plan must be a new file path, not an overwrite or directory.
- dependencies_and_callers: `commands/gen-plan.md:30-45` defines this as Phase 1 and maps each exit code to stop/continue behavior; `commands/gen-plan.md:4-11` grants the command permission to invoke this exact script. Dependencies are standard shell tools: `realpath`, `dirname`, `wc`, `tr`, and filesystem test operators.
- edge_cases_or_failure_modes: `realpath -m` is GNU-specific; the script falls back to the original path if unavailable, so canonicalization may be weaker on some systems. `-w` permission checks can vary under unusual users or ACLs. A directory output and an existing file share exit code `4`, so callers must inspect text if they need the distinction. The script intentionally refuses to create missing output directories.
- validation_or_tests: `tests/test-gen-plan.sh:426-541` covers missing option values, flag-as-value, unknown options, missing input, empty input, missing output directory, existing output, output-is-directory, valid paths, and help exit behavior.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-071 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared policy and state-machine library for RLCR and PR-loop hooks. It centralizes hook JSON validation, loop discovery, state parsing, protected-file decisions, cancellation authorization, `.humanize` git-add protection, PR-loop reviewer mapping, PR resolve round validation, loop termination naming, and PR goal tracker updates.
- algorithmic_behavior: Defines state field constants and exit reasons at `hooks/lib/loop-common.sh:15-40`. Hook input validation rejects null bytes, invalid UTF-8 when `iconv` exists, invalid JSON, and missing `tool_name` at `55-91`; required tool fields are checked at `97-110`; excessive JSON depth is detected via `jq` paths at `115-128`. Active RLCR loop discovery checks only the newest session directory and accepts `state.md` or `finalize-state.md` at `152-170`; tolerant and strict state parsers are at `202-229` and `240-296`. Round/path helpers, hardcoded allowlist, and block-message renderers span `298-405`. Cancel authorization requires `.cancel-requested`, rejects command substitution, shell chaining, extra args, hidden variables, wrong source/destination, and symlinked sources before allowing `mv state.md|finalize-state.md cancel-state.md` at `455-614`. PR-loop helpers map bot names (`631-653`), find active PR loops (`702-718`), identify read-only PR loop round files (`722-739`), and enforce current-round `pr-resolve` writes (`747-790`). `git_adds_humanize` blocks direct, forced, all, and broad-scope adds that can include `.humanize` at `834-930`. `command_modifies_file` recognizes redirection, `tee`, in-place edits, moves/copies/removes, `dd`, `truncate`, `printf >`, and `exec >` at `975-1000`. `end_loop` validates reasons and renames `state.md` to `{reason}-state.md` at `1023-1049`. `update_pr_goal_tracker` performs idempotent round+reviewer updates, totals recalculation, table-row insertion, partial repair, and issue-log append at `1072-1219`.
- inputs_outputs_state: Inputs are hook JSON payloads, tool names and tool inputs, filesystem loop directories, `state.md`/`finalize-state.md` frontmatter, Bash command strings, path strings, `.cancel-requested`, `.humanize`, gitignore state, PR loop tracker files, and optional bot-results JSON. Outputs include shell return codes, exported/global variables such as `VALIDATED_TOOL_NAME`, `VALIDATED_TOOL_INPUT`, and `STATE_*`, rendered stderr block messages, renamed loop state files through `end_loop`, and in-place tracker updates through temp-file replacement.
- gates_or_invariants: Validators fail closed on malformed strict state files, deeply nested JSON, or protected state paths. Only the newest loop directory is active, preventing older “zombie” sessions from reviving. `state.md` and `finalize-state.md` are system-owned except the narrowly authorized cancel move. `.humanize` must not be staged through broad or forced `git add`. Goal-tracker writes after Round 0 must route through summary requests. PR loop generated files are read-only, while `round-N-pr-resolve.md` must match the active PR-loop round. Tracker updates are idempotent by round plus reviewer and avoid double-counting totals when a row already exists.
- dependencies_and_callers: Sources `hooks/lib/template-loader.sh` at `hooks/lib/loop-common.sh:135-140`; relies on `jq`, `sed`, `awk`, `grep`, `tr`, `sort`, `head`, `ls`, `git check-ignore`, and optionally `iconv`. It is sourced by `hooks/loop-read-validator.sh:17-19`, `hooks/loop-write-validator.sh:17-19`, `hooks/loop-bash-validator.sh:16-18`, `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, and setup scripts. Concrete call paths include read validation of wrong loop files in `hooks/loop-read-validator.sh:122-131`, write protection in `hooks/loop-write-validator.sh:55-67` and `152-195`, Bash protection in `hooks/loop-bash-validator.sh:24-45`, `115-156`, and PR tracker updates in `hooks/pr-loop-stop-hook.sh`.
- edge_cases_or_failure_modes: `find_active_loop` intentionally ignores older sessions even if they contain `state.md`; tests call this out as “newest-only.” Strict parsing accepts negative numeric values because it matches `^-?[0-9]+$`, so semantic non-negativity is left to callers. `is_state_file_path` can also match `finalize-state.md`, so callers must check finalize first, as noted in validators. `git_adds_humanize` depends on current working directory for `.humanize` and `git check-ignore` behavior. `command_modifies_file` is regex-based shell detection, so it is broad but not a complete shell parser. `update_pr_goal_tracker` assumes recognizable markdown sections and uses `jq` for bot result fields.
- validation_or_tests: `tests/robustness/test-session-robustness.sh:33-175` specifies active-loop detection, finalize detection, newest-only behavior, empty/nonexistent directory handling, and performance with many sessions. `tests/robustness/test-state-file-robustness.sh:52-220` covers tolerant and strict state parsing, quoted values, missing frontmatter, non-numeric values, and defaulting. `tests/robustness/test-cancel-security-robustness.sh:39-224` covers valid cancel forms and injection/bypass rejections. `tests/test-humanize-escape.sh:41-150` covers `.humanize` git-add variants. `tests/test-pr-loop-system.sh` covers PR tracker idempotency, table insertion, and partial repair.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-101 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Hook block-message template used when a tool attempts to read an RLCR round prompt or summary file from the wrong location. It is a guardrail prompt, not executable logic, but it defines the remediation text for a location gate.
- algorithmic_behavior: The template title and message at `prompt-template/block/wrong-file-location.md:1-4` explain that the requested `{{FILE_PATH}}` is outside the active loop file location. Lines `5-7` render the current prompt and summary paths under `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-*`. Line `9` gives a direct `cat {{FILE_PATH}}` fallback if the user truly needs that specific file.
- inputs_outputs_state: Inputs are `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`, supplied by the read validator. Output is rendered markdown sent to stderr as the block reason. It does not mutate state; the associated hook exits with a block status.
- gates_or_invariants: The invariant is that current RLCR prompt/summary reads must use the active loop directory, not stale copies or round files elsewhere. The actual gate is enforced before rendering by `hooks/loop-read-validator.sh:122-131`.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-read-validator.sh:127-130` after active loop discovery and strict state parsing. Rendering semantics come from `hooks/lib/template-loader.sh:50-57`, which performs single-pass placeholder replacement and leaves missing placeholders intact.
- edge_cases_or_failure_modes: If variables are missing, unresolved placeholders remain. The text’s final “use: cat {{FILE_PATH}}” is advisory and could be confusing because the hook already blocked a Read-tool access; the actual ability to use Bash depends on the Bash validator and whether the target is protected.
- validation_or_tests: Template reference integrity is checked by `tests/test-template-references.sh`; runtime behavior is exercised through `hooks/loop-read-validator.sh` paths that block wrong-location round file reads.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-131 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout abstraction used by setup scripts, PR-loop scripts, and stop hooks. It validates behavior of `detect_timeout_impl`, `TIMEOUT_IMPL`, and `run_with_timeout` across GNU, Homebrew, Python fallback, and no-timeout environments.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh` at `tests/robustness/test-timeout-robustness.sh:14-17`. Positive tests validate non-empty implementation detection, allowed implementation names, quick command completion, timeout exit `124`, argument preservation, successful command exit-code preservation, and pipeline execution via `sh -c` at `31-110`. Edge/negative tests cover short timeouts with elapsed-time bound, zero timeout variability, large output, five rapid cycles, nonexistent commands, empty command handling, special-character arguments, signal-trapping commands, fallback-chain detection, subshell commands, and `TIMEOUT_IMPL` visibility in a subshell at `120-272`. It exits through `print_test_summary` at `278-279`.
- inputs_outputs_state: Inputs are the host’s available timeout tools and shell commands executed through `run_with_timeout`. Outputs are test pass/fail records and a final exit status from the helper summary. It creates no durable project state.
- gates_or_invariants: The allowed implementation set is exactly `gtimeout|timeout|python3|python|none` (`tests/robustness/test-timeout-robustness.sh:42-49`). Timed-out commands should return `124` when a timeout implementation exists (`62-77`). Normal commands must preserve stdout and exit status (`51-60`, `89-100`). Fallback detection must remain inside the allowed set (`235-247`).
- dependencies_and_callers: The target implementation is `scripts/portable-timeout.sh`, where detection priority is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` at `scripts/portable-timeout.sh:9-27`; `TIMEOUT_IMPL` is set at `29`, `run_with_timeout` dispatches at `33-71`, and `TIMEOUT_IMPL` is exported at `73-76`. Runtime callers include setup scripts, PR status scripts, and stop hooks that wrap git, gh, and Codex calls with `run_with_timeout`.
- edge_cases_or_failure_modes: Tests intentionally accept variable behavior for zero timeout (`0` or `124`) and signal-trapping commands (`0` or `124`). If no timeout implementation exists, timeout-specific checks are skipped but detection must still return `none`. The empty-command test treats any exit as handled, so it is a smoke check rather than a strict behavioral contract. Elapsed-time validation uses second granularity and requires a one-second timeout to finish under five seconds.
- validation_or_tests: This file is itself the validation artifact. It directly exercises the timeout library’s fallback chain, command dispatch, exit semantics, output handling, argument quoting, and repeated-use behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 assigned items are represented exactly once as Item Evidence sections above.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`