# agent_14 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-014 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor algorithm library for humanize loop status displays. The directory currently contains exactly one file, `scripts/lib/monitor-common.sh` at 17,436 bytes. It is sourced by `scripts/humanize.sh` when present at startup, making these helpers part of the RLCR and PR-loop monitor read path rather than standalone executables; see `scripts/humanize.sh:6-10`.
- algorithmic_behavior: `monitor-common.sh` centralizes cross-loop discovery, parsing, display, and phase-classification logic. It provides color emitters (`scripts/lib/monitor-common.sh:14-23`), cross-platform file sizing (`32-35`), latest timestamped session selection (`40-63`), terminal scroll-region setup and restore (`71-92`), status-to-color mapping (`130-141`), active/final state-file detection (`154-188`), YAML frontmatter lookup (`197-209`), timestamp/string formatting (`218-252`), PR-loop phase detection (`267-342`), PR phase display labels (`346-381`), RLCR-style goal tracker metrics (`390-463`), and PR goal tracker metrics (`468-498`). The latest-session algorithm filters children to `YYYY-MM-DD_HH-MM-SS` names and picks the lexicographically greatest timestamp, which is valid for that timestamp format (`54-60`). State detection gives `state.md` priority as active, then returns the first `*-state.md` stop file discovered by `find` (`162-184`). PR phase detection first checks terminal state files, then infers `codex_analyzing` from the highest `round-*-pr-check.md` file growing or having recent mtime, with a per-session `/tmp/humanize-phase-<session>-<round>.size` cache (`272-319`), then falls back to `state.md` round/startup case (`321-341`).
- inputs_outputs_state: Inputs are filesystem paths under `.humanize/rlcr` or `.humanize/pr-loop`, markdown state/goal files, terminal dimensions, and timestamps/strings. Outputs are shell-returned strings such as `path|active`, `approved`, `waiting_reviewer`, `total|resolved|remaining|reviewer`, ANSI color codes, and terminal control sequences. Persistent/transient state is minimal: `get_pr_loop_phase` writes a size cache under `/tmp` (`297-307`); terminal helpers mutate the current terminal scroll region (`77-91`); parser functions only read files. `scripts/humanize.sh` wraps `monitor_find_latest_session` for RLCR at `scripts/humanize.sh:237-240`, wraps `monitor_find_state_file` at `303-306`, uses `monitor_get_file_size` for log-tail monitoring at the caller shown by search (`scripts/humanize.sh:571`), and uses the same session/state helpers for PR monitoring at `scripts/humanize.sh:921-987`. The PR status bar calls `get_pr_loop_phase` and `get_pr_loop_phase_display` at `scripts/humanize.sh:1056-1062`, then uses `humanize_parse_pr_goal_tracker` at `1100-1101`.
- gates_or_invariants: Session directory names must match the strict timestamp regex or they are ignored (`scripts/lib/monitor-common.sh:54-55`). Missing loop/session dirs return empty or `unknown` rather than failing (`44-47`, `157-160`, `270`). `state.md` always overrides terminal state files (`162-166`). PR final state files short-circuit phase detection before file-growth heuristics (`272-275`). Goal tracker parsing assumes stable markdown headings and table formats; active-task counts subtract completed/deferred statuses and clamp negative counts to zero (`419-435`). The template-like pipe output contract is an invariant for callers that split on `|`; goal summaries containing `|` would corrupt field splitting.
- dependencies_and_callers: Direct caller is `scripts/humanize.sh`, which sources the library conditionally (`scripts/humanize.sh:6-10`) and uses helpers in both `_humanize_monitor_codex` and `_humanize_monitor_pr`. Tests source the library directly in `tests/test-pr-loop-system.sh`, including phase detection (`255-307`, `458-485`, `848-898`), goal tracker parsing (`314-370`), PR goal tracker parsing (`376-416`), state detection (`422-451`, `793-819`), latest-session selection (`769-787`), and file-size behavior (`825-842`). External command dependencies include `find`, `basename`, `stat` with GNU/BSD alternatives, `sed`, `grep`, `sort`, `wc`, `tr`, `date`, `tput`, `clear`, and `/tmp` writeability.
- edge_cases_or_failure_modes: `monitor_find_state_file` takes the first `*-state.md` from `find`, so multiple terminal state files are nondeterministic after active `state.md` is absent (`172-181`). `get_pr_loop_phase` cache names use only session basename plus round, so two projects with the same session timestamp can collide in `/tmp` (`298-306`). `touch -d` in tests is GNU-specific and may differ on macOS, though production code uses GNU/BSD `stat` fallbacks. `monitor_truncate_string` can compute negative substring lengths when `max_len` is under 3 (`243-250`). `monitor_get_yaml_value` greps unsanitized keys into a regex (`207-208`). `parse_goal_tracker` has two similarly named implementations: `scripts/lib/monitor-common.sh` exports `parse_goal_tracker`, while `scripts/humanize.sh` still defines `humanize_parse_goal_tracker` separately at `scripts/humanize.sh:30-112`, creating possible drift in RLCR parsing behavior.
- validation_or_tests: `tests/test-pr-loop-system.sh` covers key shared monitor helpers: latest session (`769-787`), state-file detection (`793-819`), file sizing including missing-file return zero (`825-842`), PR phase detection for approved/waiting/cancelled/maxiter/codex-analyzing (`255-307`, `458-485`, `848-898`), generic goal tracker parsing (`314-370`), and PR issue statistics (`376-416`). Robustness tests primarily exercise `humanize_parse_goal_tracker` from `scripts/humanize.sh`, not the library’s `parse_goal_tracker`, as noted in `tests/robustness/test-goal-tracker-robustness.sh` search results.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-044 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Executable test fixture generator for PR-loop GitHub API behavior. It creates a temporary `gh` CLI shim used by fixture-backed tests for `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh`; see the file purpose comments at `tests/setup-fixture-mock-gh.sh:3-11`.
- algorithmic_behavior: The script requires two positional inputs, `mock_bin_dir` and `fixtures_dir`, validates both are non-empty (`16-22`), creates the mock bin directory (`24`), writes an executable shell script named `gh` (`27-100`), chmods it executable (`102`), and prints the mock bin directory path (`104`). The generated `gh` handles `auth status` with a success message (`33-39`), `repo view` JSON queries for owner/name/parent (`40-53`), `pr view` queries for base repository, number, and state (`54-66`), `gh api user` (`67-72`), and API paths for issue comments, review comments, and PR reviews by returning fixture files (`74-90`). Unknown `gh api` endpoints return `[]` (`92-94`), while unknown top-level commands fail with stderr and exit 1 (`98-99`).
- inputs_outputs_state: Inputs are the target mock-bin directory and fixture directory. It writes one output executable at `$MOCK_BIN_DIR/gh` and embeds the fixture directory path into that generated file at line `31`. Runtime outputs from the generated `gh` are JSON objects/arrays that mimic `gh repo view`, `gh pr view`, and `gh api`. Required fixture filenames are underscore variants: `issue_comments.json`, `review_comments.json`, and `pr_reviews.json` (`76`, `82`, `88`). Tests also contain hyphenated fixture files, but this generator specifically consumes the underscore names; the inventory shows both naming families under `tests/fixtures`.
- gates_or_invariants: The setup script hard-fails on missing arguments (`19-22`) and assumes the fixture directory exists and contains readable underscore-named files. The generated mock matches API routes by substring rather than full argument parsing (`75`, `81`, `87`), which is sufficient for current callers because `fetch-pr-comments.sh` and `poll-pr-reviews.sh` call `gh api "repos/.../issues/.../comments"` and `gh api "repos/.../pulls/.../comments|reviews"` with `--paginate`; see `scripts/fetch-pr-comments.sh:209-219` and `scripts/poll-pr-reviews.sh:239-279`. Endpoint match order is important: issue comments are checked before pull comments/reviews, and pull comments before pull reviews (`74-89`).
- dependencies_and_callers: Called by `tests/test-pr-loop-hooks.sh` in `setup_fixture_mock_gh`, which creates `$TEST_DIR/mock_bin`, points at `$SCRIPT_DIR/fixtures`, and prepends the mock directory to `PATH` for real script invocations (`tests/test-pr-loop-hooks.sh:1231-1251`, `1322-1334`). The generated mock supports the repository resolution path in both fetch/poll scripts, where they call `gh repo view --json owner,name`, then `gh pr view ... --json baseRepository` (`scripts/fetch-pr-comments.sh:130-156`; `scripts/poll-pr-reviews.sh:126-156`). It also supplies comment payloads for the three fetch categories that both scripts process.
- edge_cases_or_failure_modes: Because query handling is based on `$*` substring tests, unexpected argument combinations can produce simplified or wrong JSON. For `repo view --json parent`, the mock returns `{"parent": null}` only when `parent` appears and no earlier owner/name branch matches (`tests/setup-fixture-mock-gh.sh:42-50`); this is acceptable for current happy-path base-repo resolution. Missing fixture files cause `cat` to fail and the generated mock to exit nonzero because it is a shell script without explicit fallback around those `cat` calls. Unknown API endpoints return `[]`, which can mask missing mock coverage for tests that expect failures. The setup script writes the generated mock with an unquoted here-doc delimiter expansion, so the fixture path is fixed at generation time; spaces in `FIXTURES_DIR` are preserved inside quotes in the generated script.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` has four fixture-backed integration tests. It verifies `fetch-pr-comments.sh` includes issue comments, inline review comments, and approval-only PR reviews with `[Review state: APPROVED]` placeholders (`1242-1283`), verifies `--after` filtering excludes early comments while retaining late approvals (`1287-1318`), verifies `poll-pr-reviews.sh` emits valid JSON with approval placeholders and bot responses (`1322-1367`), and verifies poll timestamp filtering excludes older change-request content (`1371-1411`). These tests execute the production scripts with `PATH="$mock_bin_dir:$PATH"` (`1251`, `1332`).
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-074 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Template asset intended to describe a failed Codex review execution, with placeholders for exit code, missing review-result path, debug files, and stderr excerpt. It defines the user-facing failure contract for a review retry gate, but appears disconnected from the current RLCR and PR-loop stop-hook implementations.
- algorithmic_behavior: The template is static markdown. It renders a `# Codex Review Failed` title (`prompt-template/block/codex-review-failed.md:1`), explains that no output was produced (`3`), interpolates `{{CODEX_EXIT_CODE}}` and `{{REVIEW_RESULT_FILE}}` (`5-6`), lists debug files via `{{CODEX_CMD_FILE}}`, `{{CODEX_STDOUT_FILE}}`, and `{{CODEX_STDERR_FILE}}` (`8-11`), includes `{{STDERR_CONTENT}}` in a fenced block (`13-16`), then instructs retry after exit (`18`). Rendering, if used, would be through `hooks/lib/template-loader.sh`, whose substitution syntax and single-pass behavior are documented at `hooks/lib/template-loader.sh:7-13` and implemented at `58-132`.
- inputs_outputs_state: Inputs are placeholder assignments for exit code, review result file, debug command/stdout/stderr files, and stderr content. Output is rendered markdown suitable for a hook block reason. It has no state transitions itself. In the current stop hook, equivalent runtime state is built inline: `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE` are assigned in `hooks/loop-codex-stop-hook.sh:674-676` and `794-797`; Codex exit code is captured at `843-847`; stderr/stdout tails are captured for failure cases at `884-899` and `920-945`.
- gates_or_invariants: If this template were wired through `load_and_render_safe`, missing variables would remain as raw `{{VAR}}` placeholders because the renderer preserves unknown variables (`hooks/lib/template-loader.sh:115-122`). The current live failure gate does not depend on this file: `hooks/loop-codex-stop-hook.sh` defines `codex_failure_exit` inline, emits a JSON hook decision with `"decision": "block"`, and exits 0 for retry semantics (`855-881`). It gates nonzero Codex exit (`883-900`), missing review result with stdout copy fallback (`902-946`), empty review result (`948-955`), then parses review content (`957-960`).
- dependencies_and_callers: Repository search found only the template itself for `prompt-template/block/codex-review-failed.md`, and live `# Codex Review Failed` strings in `hooks/pr-loop-stop-hook.sh:1323` and `hooks/loop-codex-stop-hook.sh:861`. No current caller loads `block/codex-review-failed.md` by filename. Therefore its dependency is conceptual: it matches the template-loader placeholder system but is not currently in the live hook path.
- edge_cases_or_failure_modes: The template says stderr last 50 lines (`prompt-template/block/codex-review-failed.md:13`), while the live RLCR stop hook tails 30 lines (`hooks/loop-codex-stop-hook.sh:887`, `924`, `929`), so wiring this template without aligning the tail count would change or misdescribe diagnostics. The template says the review result file was “not created” (`prompt-template/block/codex-review-failed.md:6`), which only matches one failure mode; live code also handles nonzero exit, stdout-copy failure, and empty file (`883-955`). Because it is unused, edits here would not affect current behavior unless a hook is later changed to load it.
- validation_or_tests: Template-loader behavior is covered generally by `tests/test-template-loader.sh`, including existing-file loading (`55-65`), missing-template empty behavior (`68-78`), variable replacement (`81-164`), unreplaced placeholders (`181-194`), and safe fallback rendering (`197-208`). No inspected test directly renders `block/codex-review-failed.md`. The closest live validation is in stop-hook failure code inspection, not a dedicated test for this template.
- skip_candidate: `yes: currently an unused template asset; it documents an intended Codex failure block but is not loaded by the active hook code paths found in this branch`

### IMPL_PR_LOOP-HZ-104 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Active hook block template for enforcing the RLCR invariant that round summary files must be written inside the active loop directory. It participates directly in the write-validator gate that blocks summary writes outside `.humanize/rlcr`.
- algorithmic_behavior: The template emits a short markdown block with title `# Wrong Summary Location`, a mandatory-location statement, and a rendered `{{CORRECT_PATH}}` line (`prompt-template/block/wrong-summary-location.md:1-5`). It is loaded by `hooks/loop-write-validator.sh` when a write targets a summary filename but is not inside a humanize loop directory: `IS_SUMMARY_FILE == true` and `IN_HUMANIZE_LOOP_DIR == false` (`hooks/loop-write-validator.sh:198-209`). The validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` (`202-203`), calls `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-summary-location.md"` with a fallback (`204-208`), writes the rendered block to stderr, and exits 2 to block the tool action (`209`).
- inputs_outputs_state: Inputs are the active loop directory, current round, the attempted file path classification flags, and `CORRECT_PATH`. Output is a rendered block message on stderr plus process exit code 2 from the validator. No files are modified by the template. The active state is read earlier by the validator through shared hook logic; this block is one transition in the write-validation decision tree before round-number and directory-path validation (`hooks/loop-write-validator.sh:212-263`).
- gates_or_invariants: The gate enforces “summary file writes must target the active loop dir” before extracting per-loop path components. It only triggers for summary-like paths outside the loop; summary writes inside the loop continue to later gates that validate round number and exact directory (`hooks/loop-write-validator.sh:216-263`). The fallback message has the same `{{CORRECT_PATH}}` variable and is used if the template is missing or renders empty, per `load_and_render_safe` behavior (`hooks/lib/template-loader.sh:167-203`). Placeholder substitution is single-pass and unknown variables remain literal (`hooks/lib/template-loader.sh:54-57`, `115-122`).
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh:202-209`. It depends on `TEMPLATE_DIR`, `load_and_render_safe`, and path helper classifications from `hooks/lib/loop-common.sh`, including round-file recognition and round extraction (`hooks/lib/loop-common.sh:303-322`) and allowlisted historical files (`327-345`) used by later gates. Search found this template filename only in the write validator plus the file itself, so the write-validator path is its active behavioral surface.
- edge_cases_or_failure_modes: If `ACTIVE_LOOP_DIR` or `CURRENT_ROUND` is wrong, the template faithfully renders a wrong correction path; it does not validate those values. If an attempted summary path is inside `.humanize/rlcr` but in the wrong session directory, this template is bypassed and the broader wrong-directory template handles the block (`hooks/loop-write-validator.sh:252-262`). If the write-validator cannot classify the attempted path as a summary file, this gate does not trigger. Missing template is nonfatal because `load_and_render_safe` renders the fallback (`hooks/lib/template-loader.sh:179-186`).
- validation_or_tests: General template rendering is covered by `tests/test-template-loader.sh` (`81-164`, `197-208`) and comprehensive real-template rendering exists for sibling block templates in `tests/test-templates-comprehensive.sh:493-520`, but no inspected test directly renders `wrong-summary-location.md`. Behavioral coverage is likely through hook-validator tests that exercise summary path validation; search results show allowlist and validator tests in `tests/test-allowlist-validators.sh`, though the assigned template’s exact string was not directly asserted in the snippets inspected.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item evidence headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`