# agent_27 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-027 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit safety gate for an active RLCR loop. It prevents the loop from continuing when the state schema is stale, the git branch has drifted, or the original plan-file tracking contract has changed.
- algorithmic_behavior: Initializes `SCRIPT_DIR`, `PROJECT_ROOT`, sources shared loop functions and portable timeout support at `hooks/loop-plan-file-validator.sh:13-24`; reads hook stdin at `:26-27` but does not use its content for decisions; locates the newest active `.humanize/rlcr/*` loop with `find_active_loop` at `:29-36`; switches from `state.md` to `finalize-state.md` during Finalize Phase at `:38-42`; strict-parses state and fails closed on malformed state at `:44-48`; maps `STATE_PLAN_TRACKED`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH` into local names at `:50-53`.
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or current directory, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, git branch/tracking/status, `jq`, and hook stdin. Outputs are either no output with exit 0 for allow, a JSON `{"decision":"block","reason":...}` with exit 0 for user-correctable gate failures, or stderr plus exit 1 for malformed state. It does not mutate files or state; it enforces a state-machine invariant before user prompts proceed.
- gates_or_invariants: Requires v1.1.2+ fields `plan_tracked` and `start_branch` at `:79-89`; branch must still equal `start_branch` at `:95-115`; if `plan_tracked=true`, `PLAN_FILE` must remain git-tracked and clean at `:123-192`; if `plan_tracked` is anything else, `PLAN_FILE` must not be tracked at `:193-228`. Git failures and timeouts block rather than allow at `:98-105`, `:129-147`, `:154-171`, and `:199-216`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for constants, `find_active_loop`, `parse_state_file_strict`, and template loading; strict parser requires YAML frontmatter plus numeric `current_round` and `max_iterations` in `hooks/lib/loop-common.sh:240-295`. Depends on `scripts/portable-timeout.sh:33-71` for `run_with_timeout`, preserving GNU timeout exit code 124. Uses `prompt-template/block/schema-outdated.md` via `load_and_render_safe` at `hooks/loop-plan-file-validator.sh:60-77`.
- edge_cases_or_failure_modes: No active loop exits allow at `:33-36`, so stale non-newest loop directories are ignored by shared `find_active_loop` semantics. Malformed state exits 1 rather than JSON block at `:45-48`. Missing `plan_file` is not explicitly schema-gated in this script; downstream `git ls-files/status` checks are the practical failure path. `FULL_PLAN_PATH` is computed at `:121` but unused here; content drift against backup is handled by the stop hook, not this prompt validator.
- validation_or_tests: Covered by `tests/test-plan-file-hooks.sh`: valid state and YAML-quoted `plan_file` at `tests/test-plan-file-hooks.sh:91-119`; missing `plan_tracked` and `start_branch` at `:121-159`; branch drift at `:164-185`; quoted `start_branch` and mismatch at `:341-386`; hyphenated plan path at `:431-460`. Stop-hook schema coverage for outdated state appears at `:631-653`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-057 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR plan-file protection surface. It verifies the prompt validator plus Write/Edit/Bash/Stop hooks preserve plan integrity, state schema compatibility, branch consistency, and goal-tracker initialization rules.
- algorithmic_behavior: Creates an isolated temporary git repo at `tests/test-plan-file-hooks.sh:30-36`; `setup_test_loop` initializes git, captures default branch, creates `.humanize/rlcr/<timestamp>`, creates a gitignored project plan, copies the loop backup `plan.md`, and writes state with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, and `start_branch` at `:37-86`. It then invokes hooks by piping JSON hook payloads and compares exit codes/output with grep-based assertions.
- inputs_outputs_state: Inputs are the repository hooks under `$PROJECT_ROOT/hooks`, temporary git fixtures, hook JSON for `UserPromptSubmit`, `Write`, `Edit`, and `Bash`, and fabricated loop files. Outputs are colored PASS/FAIL/SKIP lines, counters, and process exit equal to `TESTS_FAILED` at `:1056-1065`. State transitions are fixture-only: valid loop, stale schema, changed branch, modified/deleted plan, missing backup, tracked clean/dirty plan, and placeholder goal-tracker variants.
- gates_or_invariants: UserPromptSubmit must allow valid state and block missing required fields or branch changes at `:91-185`. Write/Edit/Bash must block direct modifications to loop `plan.md` backup at `:194-266`. Bash validator must catch redirection/removal and bypass patterns using command substitution, globs, braces, pipes, and backticks at `:228-335`. Stop hook must block plan drift, deletion, missing backup, outdated schema, uncommitted tracked changes, and committed tracked changes at `:469-731`. Legacy `.humanize-loop.local` paths are intentionally allowed at `:1012-1054`.
- dependencies_and_callers: Calls `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. It relies on git, shell, `grep`, `mktemp`, and hook dependencies such as `jq`. Its stop-hook tests depend on blocking early enough to avoid requiring a successful Codex review, for example test 8.8 only asserts absence of YAML parsing errors at `:388-429`.
- edge_cases_or_failure_modes: The harness begins with `set -uo pipefail`, then toggles `set +e` around expected hook failures and later enables `set -e`, so fixture setup after that point must remain successful. `setup_test_loop` reuses the same temp repo for several cases and appends `plans/` to `.gitignore` on each call, which creates commit changes but means tests are not fully independent until later cases create subrepos. The tests intentionally mutate temporary repos and should not be run in a read-only research pass. Some assertions match broad text such as `branch`, `plan.*modif`, or `uncommitted`, so they specify behavior class more than exact message text.
- validation_or_tests: This file is itself the validation artifact. It covers YAML quote parsing at `:106-119` and `:341-429`, path regex bypass hardening at `:254-335`, tracked-plan race/content checks at `:562-731`, section-specific goal tracker placeholder reporting at `:737-1010`, and legacy-path negative tests at `:1016-1054`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-087 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR large-file gate. It turns detected oversized changed files into actionable instructions that prevent loop exit until maintainability limits are restored.
- algorithmic_behavior: Defines a rendered message headed “Large Files Detected” at `prompt-template/block/large-files.md:1`; interpolates `{{MAX_LINES}}` and `{{LARGE_FILES}}` at `:3-4`; explains review/maintenance risk at `:6-9`; splits remediation by code files and documentation files at `:11-24`; requires committing the split before attempting exit again at `:25`.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `MAX_LINES=2000` and a newline list of offending files. Output is markdown text embedded as the `reason` in a JSON block response. The template itself has no persistent state, but its rendered result enforces the stop-hook transition from “exit attempt” to “blocked until large files are split.”
- gates_or_invariants: Code files over the limit must be split into modules below `MAX_LINES` while preserving behavior at `prompt-template/block/large-files.md:13-17`. Documentation over the limit must be split into coherent sections with cross-references/navigation maintained at `:19-23`. The hook gate only fires after `git status` is available and at least one changed recognized code/doc file exceeds the threshold.
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh:343-423`. The caller caches `git status --porcelain`, recognizes code extensions such as `py`, `js`, `ts`, `go`, `rs`, `sh` and docs such as `md`, `rst`, `txt`, counts lines with `wc -l`, builds `LARGE_FILES`, then renders this template via `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:410-412`. Rendering uses single-pass placeholder substitution in `hooks/lib/template-loader.sh:58-132`.
- edge_cases_or_failure_modes: Because the caller iterates `GIT_STATUS_CACHED`, clean but historically large tracked files are not considered by this gate. Deleted files are skipped, unsupported extensions are skipped, and only files visible in porcelain status are checked. If the template is missing, the stop hook has an inline fallback at `hooks/loop-codex-stop-hook.sh:402-409`; if a variable is missing, template-loader leaves the placeholder intact by design at `hooks/lib/template-loader.sh:119-122`.
- validation_or_tests: Template reference safety is covered by template tests that scan `load_and_render_safe` references, notably `tests/test-template-references.sh` reference extraction around `tests/test-template-references.sh:83-100` and safe-render checks around `:172-199`. Functional gate behavior is implemented in the stop hook at `hooks/loop-codex-stop-hook.sh:343-423`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-117 `file` `prompt-template/pr-loop/goal-tracker-initial.md`
- cursor: `[_]`
- core_role: Initial PR review loop goal-tracker template. It seeds the PR loop’s review-contract document so later rounds can track bot reviewers, issue counts, resolution counts, and per-round logs.
- algorithmic_behavior: Renders PR metadata fields `PR_NUMBER`, `START_BRANCH`, `STARTED_AT`, `ACTIVE_BOTS_DISPLAY`, and `STARTUP_CASE` at `prompt-template/pr-loop/goal-tracker-initial.md:3-10`; defines the ultimate goal as approval from all monitored bots at `:11-14`; initializes an Issue Summary table with Round 0 at `:15-20`; initializes Total Statistics to zero at `:21-25`; initializes Issue Log Round 0 with startup context at `:27-33`.
- inputs_outputs_state: Inputs come from `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `START_BRANCH`, active bot display string, startup case, `BOT_AC_ROWS`, and `STARTED_AT` are assembled at `scripts/setup-pr-loop.sh:563-586`. Output is rendered content written to `.humanize/pr-loop/<timestamp>/goal-tracker.md` at `scripts/setup-pr-loop.sh:632-633`. The state transition is PR loop initialization: the tracker begins in “awaiting initial reviews” with zero found/resolved/remaining issues.
- gates_or_invariants: The tracker must contain `## Issue Summary`, `## Total Statistics`, and `## Issue Log`, plus the three statistics fields, because downstream update logic inserts rows and recalculates totals against those headings. This invariant is directly tested at `tests/test-pr-loop-system.sh:641-653`. The downstream update contract in `prompt-template/pr-loop/codex-goal-tracker-update.md:7-20` assumes this schema for adding per-round reviewer rows, updating totals, and appending issue-log entries.
- dependencies_and_callers: Rendered by `scripts/setup-pr-loop.sh:632` through `load_and_render_safe`. If the template is absent, the setup script fallback at `scripts/setup-pr-loop.sh:588-630` creates a broader tracker with Acceptance Criteria and Current Status sections; the live template is narrower and does not consume the prepared `BOT_AC_ROWS` variable. Runtime mutation is supported by `update_pr_goal_tracker` in `hooks/lib/loop-common.sh:1055-1215`, which repairs or appends Issue Summary rows, totals, and Issue Log entries idempotently by round/reviewer.
- edge_cases_or_failure_modes: Missing template variables remain as `{{...}}` placeholders because rendering is single-pass and non-strict. Empty `ACTIVE_BOTS_DISPLAY` would render an empty monitored-bots goal. The live template omits the fallback’s Acceptance Criteria section, so generic goal-tracker parsers looking for `### Acceptance Criteria` will not get PR-specific progress; PR-specific parsing instead reads Total Statistics in `scripts/lib/monitor-common.sh:465-497`.
- validation_or_tests: Template schema is tested in `tests/test-pr-loop-system.sh:641-653`. Setup integration verifies that `setup-pr-loop.sh` creates `goal-tracker.md` and that it contains Issue Summary at `tests/test-pr-loop-system.sh:1452-1506`. The update prompt that consumes the schema is `prompt-template/pr-loop/codex-goal-tracker-update.md:1-64`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item evidence sections, matching the 4 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`