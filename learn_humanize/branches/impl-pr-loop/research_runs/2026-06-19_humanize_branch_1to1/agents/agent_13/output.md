# agent_13 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-013 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: PR-loop prompt-template bundle for round-0 startup, worker task selection, completion instructions, goal-tracker initialization, and later Codex goal-tracker update instructions. The directory is flat, with seven `.md` templates: `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, `critical-requirements-no-comments.md`, `critical-requirements-has-comments.md`, `goal-tracker-initial.md`, and `codex-goal-tracker-update.md`.
- algorithmic_behavior: `scripts/setup-pr-loop.sh` renders these templates through `load_and_render_safe` after creating `.humanize/pr-loop/<timestamp>` state. It fetches PR comments into `round-0-pr-comment.md`, detects whether that file contains the no-comment sentinel, writes `round-0-prompt.md`, appends fetched comments, then appends either the no-comments or has-comments task template. See `scripts/setup-pr-loop.sh:406`, `scripts/setup-pr-loop.sh:632`, `scripts/setup-pr-loop.sh:641`, `scripts/setup-pr-loop.sh:643`, `scripts/setup-pr-loop.sh:677`, `scripts/setup-pr-loop.sh:681`, `scripts/setup-pr-loop.sh:684`, `scripts/setup-pr-loop.sh:687`, `scripts/setup-pr-loop.sh:720`, `scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:771`. The critical-requirements templates are printed after the prompt to force the correct exit protocol; see `scripts/setup-pr-loop.sh:810`, `scripts/setup-pr-loop.sh:834`, `scripts/setup-pr-loop.sh:860`.
- inputs_outputs_state: Inputs are template variables produced by setup and stop-hook code: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `STARTED_AT`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Outputs are rendered runtime files or messages under `.humanize/pr-loop/<timestamp>`: `goal-tracker.md`, `round-0-prompt.md`, and printed critical requirements. `goal-tracker-initial.md:5-9` seeds PR metadata, `goal-tracker-initial.md:17-25` seeds issue counts at zero, and `goal-tracker-initial.md:29-33` records round-0 startup state. `round-0-header.md:7-15` supplies PR identity and fetched-comment framing. `codex-goal-tracker-update.md:3-20` tells the stop-hook Codex validation pass how to update the tracker after per-bot review analysis.
- gates_or_invariants: Startup branches on comment presence. If `round-0-pr-comment.md` exactly contains `*No comments found.*`, the no-comments path tells the worker not to trigger review and to write the initial summary only (`round-0-task-no-comments.md:6-14`, `critical-requirements-no-comments.md:6-20`). Otherwise the has-comments path requires analysis, fixes, commit, push, PR comment, and summary (`round-0-task-has-comments.md:6-27`, `critical-requirements-has-comments.md:6-23`). Both task templates prohibit direct state-file edits (`round-0-task-no-comments.md:20-22`, `round-0-task-has-comments.md:30-35`). `codex-goal-tracker-update.md:57-64` preserves tracker structure and requires separate rows per reviewer.
- dependencies_and_callers: Rendered by `scripts/setup-pr-loop.sh`, which sources `hooks/lib/template-loader.sh` and sets `TEMPLATE_DIR` via `get_template_dir` (`scripts/setup-pr-loop.sh:37-43`). The loader uses `{{VAR}}` single-pass substitution and keeps unknown placeholders literal, preventing placeholder injection from substituted values (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:71-129`). `hooks/pr-loop-stop-hook.sh` later renders `pr-loop/codex-goal-tracker-update.md` into a Codex validation prompt (`hooks/pr-loop-stop-hook.sh:1239-1249`). Template existence is covered by `tests/test-template-references.sh`, which scans loader calls and all template files (`tests/test-template-references.sh:51-111`, `tests/test-template-references.sh:121-142`).
- edge_cases_or_failure_modes: Missing templates do not hard-fail setup because all these call sites use `load_and_render_safe` with embedded fallback strings; that preserves runtime behavior but may drift from canonical prompt wording (`hooks/lib/template-loader.sh:167-203`). Comment detection is sentinel-based, so malformed fetch output that omits the exact no-comment line is treated as comments-present (`scripts/setup-pr-loop.sh:643-649`). The fallback goal tracker in `setup-pr-loop.sh` includes an Acceptance Criteria shape not present in the canonical `goal-tracker-initial.md`, so missing template fallback changes tracker schema (`scripts/setup-pr-loop.sh:588-630` versus `prompt-template/pr-loop/goal-tracker-initial.md:1-33`). `BOT_AC_ROWS` is prepared but unused by the canonical template, making it a stale or fallback-only variable (`scripts/setup-pr-loop.sh:566-585`).
- validation_or_tests: `tests/test-pr-loop-system.sh:1452-1527` verifies `setup-pr-loop.sh --codex` creates `goal-tracker.md`, includes an Issue Summary section, and includes the PR number. `tests/test-template-references.sh:74-111` verifies referenced templates exist, while `tests/test-template-references.sh:121-142` reports unreferenced templates. `tests/run-all-tests.sh:32-65` includes `test-template-references.sh`, `test-templates-comprehensive.sh`, `test-pr-loop.sh`, and `test-pr-loop-system.sh` in the full suite. I did not run the full suite because this branch export is read-only and the tests create/remove fixture state; I did run `bash -n tests/run-all-tests.sh` successfully.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-043 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level executable validation aggregator for the plugin’s algorithmic behavior. It is the suite orchestrator that encodes which lower-level tests constitute the project’s validation contract and in what order they run.
- algorithmic_behavior: The script initializes counters and a failed-suite array (`tests/run-all-tests.sh:28-30`), defines an ordered `TEST_SUITES` list (`tests/run-all-tests.sh:32-65`), defines tests requiring zsh (`tests/run-all-tests.sh:67-70`), then iterates through each suite (`tests/run-all-tests.sh:72-131`). For each suite, it skips missing files, determines whether zsh is needed, executes the test while capturing stdout/stderr, strips ANSI escape codes, extracts the final `Passed:` and `Failed:` counters, aggregates totals, records failed suites, and prints the tail of failed output (`tests/run-all-tests.sh:75-129`). It exits nonzero if any suite failed (`tests/run-all-tests.sh:140-151`).
- inputs_outputs_state: Inputs are executable test files under `tests/` and `tests/robustness/`, plus optional `zsh` availability for `test-zsh-monitor-safety.sh`. Outputs are terminal summary lines, aggregated pass/fail counts, a failed-suite list, and process exit status. Internal state is `TOTAL_PASSED`, `TOTAL_FAILED`, `FAILED_SUITES`, `output`, `exit_code`, `passed`, and `failed`.
- gates_or_invariants: The script treats either a nonzero suite exit or a parsed failed count greater than zero as a failed suite (`tests/run-all-tests.sh:122-124`). Missing suite files are skipped rather than fatal (`tests/run-all-tests.sh:75-78`). Missing `zsh` skips zsh-only tests rather than failing (`tests/run-all-tests.sh:98-106`). It uses `set -uo pipefail`, then disables immediate failure around individual suite execution with `set +e` so it can aggregate all results (`tests/run-all-tests.sh:12`, `tests/run-all-tests.sh:97-111`).
- dependencies_and_callers: Depends on each suite file being executable or runnable as a script through its shebang for non-zsh tests (`tests/run-all-tests.sh:108`). Depends on `sed`, `grep`, `tail`, and optionally `zsh` (`tests/run-all-tests.sh:100-117`, `tests/run-all-tests.sh:126`). It is likely called manually or by CI as the full local validation entrypoint; CI workflows exist under `.github/workflows`, but this specific script was inspected as an executable specification rather than traced to a workflow caller.
- edge_cases_or_failure_modes: If a suite prints no `Passed:` or `Failed:` line, the parser defaults to zero and may report a zero-test pass when exit code is zero (`tests/run-all-tests.sh:116-117`, `tests/run-all-tests.sh:127-129`). Skipping missing suites and missing zsh means the final success result may not represent total coverage on a minimal environment (`tests/run-all-tests.sh:75-78`, `tests/run-all-tests.sh:103-105`). Non-zsh suites are invoked directly, so executable bits and valid shebangs matter (`tests/run-all-tests.sh:108`). The script sets `set -e` after the captured command block even though it did not start with `-e`; this makes later aggregation stricter than setup (`tests/run-all-tests.sh:111`).
- validation_or_tests: The file itself passed shell syntax validation with `bash -n tests/run-all-tests.sh`. The full runner was not executed because the research branch export is read-only and many suites create, mutate, or remove temporary `.humanize` and fixture state.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-073 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking message template for the PR-loop stop hook when Claude is configured for review, a trigger comment is required and confirmed, but the Claude bot fails to add an eyes reaction to the trigger comment within the retry window.
- algorithmic_behavior: The template renders a human-facing block explaining that Claude did not respond after `{{RETRY_COUNT}}` retries and `{{TOTAL_WAIT_SECONDS}}` seconds (`prompt-template/block/claude-eyes-timeout.md:1-4`), lists likely causes (`prompt-template/block/claude-eyes-timeout.md:5-8`), required checks (`prompt-template/block/claude-eyes-timeout.md:10-13`), and a remediation path to configure the GitHub App and post a fresh trigger comment (`prompt-template/block/claude-eyes-timeout.md:15-18`). The actual gate lives in `hooks/pr-loop-stop-hook.sh:757-779`, which calls `scripts/check-bot-reactions.sh claude-eyes` with `--retry 3 --delay 5`, renders this template with `RETRY_COUNT=3` and `TOTAL_WAIT_SECONDS=15`, then returns a JSON block decision.
- inputs_outputs_state: Inputs are `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, supplied by the stop hook (`hooks/pr-loop-stop-hook.sh:774-775`). Runtime preconditions include `CLAUDE_CONFIGURED=true`, `REQUIRE_TRIGGER=true`, and a nonempty `PR_TRIGGER_COMMENT_ID` (`hooks/pr-loop-stop-hook.sh:749-761`). Output is a rendered Markdown reason embedded in `{"decision":"block","reason":...,"systemMessage":...}` (`hooks/pr-loop-stop-hook.sh:777-779`). It does not mutate state; it prevents the loop from polling stale or unacknowledged Claude review triggers.
- gates_or_invariants: Eyes verification occurs only after trigger validation, explicitly to avoid checking reactions on stale trigger IDs (`hooks/pr-loop-stop-hook.sh:738-747`). The hook blocks if `EYES_REACTION` is empty or `null` (`hooks/pr-loop-stop-hook.sh:768-779`). If Claude is configured but no trigger is required, the hook skips eyes verification (`hooks/pr-loop-stop-hook.sh:787-789`). If a trigger timestamp exists but no comment ID exists, it warns rather than blocks (`hooks/pr-loop-stop-hook.sh:783-786`).
- dependencies_and_callers: Called only by `hooks/pr-loop-stop-hook.sh` through `load_and_render_safe`, with fallback text if the template is missing (`hooks/pr-loop-stop-hook.sh:770-775`). Depends on `scripts/check-bot-reactions.sh` to detect reactions and on the template loader’s single-pass substitution behavior (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: The rendered message says “post a new trigger comment” after configuration (`prompt-template/block/claude-eyes-timeout.md:18`), but the stop hook’s gate only knows that the specific checked comment lacked an eyes reaction; the root cause may also be API latency, permissions, or reaction lookup failure. The fallback says “within 15 seconds,” while the template says “after retries” and “seconds total”; both are consistent for current hardcoded values but could diverge if hook retry values change without updating variable assignments (`hooks/pr-loop-stop-hook.sh:764-775`). If `check-bot-reactions.sh` fails transiently and returns empty, the loop blocks as if Claude did not respond.
- validation_or_tests: This template is referenced in the project-wide reference scan output by `tests/test-template-references.sh` logic, which scans `load_and_render_safe` template calls (`tests/test-template-references.sh:83-111`). PR-loop stop-hook behavior is broadly included in the full suite through `tests/run-all-tests.sh:52-53` and related robustness tests in `tests/run-all-tests.sh:55-64`. I did not run the full suite in the read-only export; `bash -n tests/run-all-tests.sh` passed.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-103 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Generic wrong-round blocking message template for loop validators. It tells the agent that it attempted to write or edit a round-scoped file for a different round than the active state permits, and points to the correct path.
- algorithmic_behavior: The template renders `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH` into a concise denial message (`prompt-template/block/wrong-round-number.md:1-7`). It is used by RLCR write and edit validators for `round-N-summary.md` mismatches: `hooks/loop-write-validator.sh:228-244` and `hooks/loop-edit-validator.sh:155-178`. Those validators extract the attempted round number, compare it to `CURRENT_ROUND`, check allowlist exemptions, then render this block and exit with code 2 on mismatch.
- inputs_outputs_state: Inputs are the attempted file path, active loop state, action verb, file type, and correct summary path. `extract_round_number` parses `round-N-summary.md`, `round-N-prompt.md`, or `round-N-todos.md` shapes (`hooks/lib/loop-common.sh:312-322`). Output is stderr Markdown and validator exit code 2, blocking the attempted tool action. It does not mutate state; it preserves the invariant that agents write only the current round’s summary file unless the target file is allowlisted.
- gates_or_invariants: The active round is authoritative; agents are explicitly told not to increment the round themselves (`prompt-template/block/wrong-round-number.md:7`). The gate ignores files without an extractable round and allows files on the allowlist (`hooks/loop-write-validator.sh:228-245`, `hooks/loop-edit-validator.sh:162-180`). The allowlist includes selected historical todos and summary files (`hooks/lib/loop-common.sh:324-345`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` helpers for round file detection, round extraction, and allowlist checks (`hooks/lib/loop-common.sh:303-345`). Rendered through `load_and_render_safe`, so fallback messages are available if the template is missing (`hooks/loop-write-validator.sh:233-243`, `hooks/loop-edit-validator.sh:167-177`). Important nuance: PR-loop `round-N-pr-resolve.md` wrong-round checks currently use an inline message in `validate_pr_resolve_round`, not this template (`hooks/lib/loop-common.sh:741-790`), although PR-loop tests verify equivalent blocking behavior (`tests/test-pr-loop-hooks.sh:1431-1460`, `tests/test-pr-loop-hooks.sh:1499-1527`).
- edge_cases_or_failure_modes: Because this template text says `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, it fits summary/prompt/todos shapes but not PR-loop `pr-resolve` without a different `FILE_TYPE` value. Current PR-loop resolve validation avoids that mismatch by printing its own message. If `CURRENT_ROUND` parsing is wrong upstream, the template faithfully renders the wrong authoritative value. If the target path is allowlisted, the wrong-round block is bypassed by design.
- validation_or_tests: `tests/test-template-loader.sh:150-164` renders this real template and checks for substituted title and round values. `tests/test-templates-comprehensive.sh:498-514` also verifies full real-template rendering with `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. `tests/test-pr-loop-hooks.sh:1431-1460` and `tests/test-pr-loop-hooks.sh:1499-1527` validate the related PR-loop wrong-round resolve gate, even though that gate uses an inline message. I did not run the full suite; `bash -n tests/run-all-tests.sh` passed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `IMPL_PR_LOOP-HZ-013`, `IMPL_PR_LOOP-HZ-043`, `IMPL_PR_LOOP-HZ-073`, `IMPL_PR_LOOP-HZ-103`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`