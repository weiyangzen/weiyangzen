# agent_17 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-017 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow contract for cancelling an active PR loop. It is a thin command definition that delegates all mutation and loop-discovery behavior to `scripts/cancel-pr-loop.sh`, while constraining allowed tool execution through frontmatter at `commands/cancel-pr-loop.md:1-5`.
- algorithmic_behavior: The command instructs the agent to run `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh"` and classify the result by the script’s first output line: `NO_LOOP` or `NO_ACTIVE_LOOP` maps to “No active PR loop found,” while `CANCELLED` maps to reporting the script’s cancellation message at `commands/cancel-pr-loop.md:11-20`. The actual cancellation algorithm in `scripts/cancel-pr-loop.sh` resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, selects the newest `.humanize/pr-loop/*/` directory, requires `state.md`, touches `.cancel-requested`, and renames `state.md` to `cancel-state.md` at `scripts/cancel-pr-loop.sh:72-123`.
- inputs_outputs_state: Inputs are the plugin root env var for script path, optional `--force` allowed by frontmatter, current project directory, and `.humanize/pr-loop/<timestamp>/state.md`. Outputs are command-facing status tokens and human-readable messages. State transition is active PR loop state file present to cancelled PR loop state file preserved as `cancel-state.md`, with `.cancel-requested` created; comments, summaries, and loop directory contents are intentionally preserved per `commands/cancel-pr-loop.md:21-23`.
- gates_or_invariants: The command’s key invariant is that cancellation logic must remain in the script, not in the prompt workflow, stated at `commands/cancel-pr-loop.md:21`. The command is scoped only to PR loops and must not affect `.humanize/rlcr/`, with explicit handoff to `/humanize:cancel-rlcr-loop` at `commands/cancel-pr-loop.md:25`. Allowed tools restrict execution to the cancel script and its force variant at `commands/cancel-pr-loop.md:3`.
- dependencies_and_callers: Depends directly on `scripts/cancel-pr-loop.sh`. Related setup and stop surfaces reference `/humanize:cancel-pr-loop`, including `commands/start-pr-loop.md:62`, `scripts/setup-pr-loop.sh:801`, and PR stop-hook messages in `hooks/pr-loop-stop-hook.sh`. The command is separate from `commands/cancel-rlcr-loop.md`, which has its own force/confirmation behavior.
- edge_cases_or_failure_modes: If no PR loop directory exists, the script emits `NO_LOOP` and exits 1 at `scripts/cancel-pr-loop.sh:76-82`. If a loop directory exists but lacks active `state.md`, it emits `NO_ACTIVE_LOOP` and exits 1 at `scripts/cancel-pr-loop.sh:91-98`. Unknown script options exit 3 at `scripts/cancel-pr-loop.sh:60-64`. Missing YAML fields default to `?` for round, max iterations, or PR number at `scripts/cancel-pr-loop.sh:104-112`. The documented `--force` option currently has no additional effect at `scripts/cancel-pr-loop.sh:39-41`.
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help output, no-loop behavior, and active-loop cancellation. The active-loop test creates a mock `.humanize/pr-loop/<timestamp>/state.md`, runs the script, expects `CANCELLED`, and asserts `cancel-state.md` exists while `state.md` no longer exists at `tests/test-pr-loop-scripts.sh:211-244`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-047 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the test-runner aggregation algorithm that strips ANSI color sequences before extracting pass/fail counts from individual test-suite output. It protects the portability of `tests/run-all-tests.sh` across GNU/Linux and BSD/macOS sed behavior.
- algorithmic_behavior: The core algorithm under test is: define `esc=$'\033'`, strip SGR ANSI color codes with `sed "s/${esc}\\[[0-9;]*m//g"`, then extract counters using `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `grep -oE 'Failed:[[:space:]]*[0-9]+'`, followed by `grep -oE '[0-9]+$'`. This mirrors `tests/run-all-tests.sh:113-117`, where suite output is normalized before totals are accumulated at `tests/run-all-tests.sh:119-120`.
- inputs_outputs_state: Inputs are simulated colored or plain text test summary lines, including single-line, multi-line, zero-count, bold, and color-combined forms. Outputs are normalized strings and extracted numeric `passed` or `failed` counts. Internal state is limited to `TESTS_PASSED` and `TESTS_FAILED`, incremented by `pass()` and `fail()` at `tests/test-ansi-parsing.sh:18-31`, then emitted in a colored summary at `tests/test-ansi-parsing.sh:163-168`.
- gates_or_invariants: The invariant is that ANSI SGR sequences of the form ESC `[` digits/semicolons `m` must be removed without corrupting surrounding text. The test explicitly uses ANSI-C quoting for the escape byte because it is portable for the sed invocation used by the runner, documented at `tests/test-ansi-parsing.sh:38-40`. The final gate exits 0 only when `TESTS_FAILED` is zero; otherwise it exits 1 at `tests/test-ansi-parsing.sh:170-178`.
- dependencies_and_callers: Depends on Bash, sed, grep with extended regex, and common POSIX shell utilities. It is included in the aggregate runner’s ordered `TEST_SUITES` list at `tests/run-all-tests.sh:32-44`. Its behavior is a direct regression guard for the parser block in `tests/run-all-tests.sh:113-117`.
- edge_cases_or_failure_modes: Covered cases include basic color stripping at `tests/test-ansi-parsing.sh:44-53`, multiple color spans at `tests/test-ansi-parsing.sh:59-67`, colored pass extraction at `tests/test-ansi-parsing.sh:73-81`, colored fail extraction at `tests/test-ansi-parsing.sh:87-95`, zero fail counts at `tests/test-ansi-parsing.sh:101-109`, multi-line summaries with `tail -1` semantics at `tests/test-ansi-parsing.sh:115-130`, plain text without ANSI at `tests/test-ansi-parsing.sh:136-144`, and combined bold/color escapes at `tests/test-ansi-parsing.sh:150-158`. The strip pattern is intentionally limited to SGR `m` sequences, so non-SGR control sequences are outside this spec.
- validation_or_tests: This file is itself an executable validation asset. Running `tests/test-ansi-parsing.sh` validates the parser in isolation; running `tests/run-all-tests.sh` includes it in the full suite and then uses the same parsing algorithm to aggregate all suites.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-077 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the `.humanize/` git-add protection gate. It explains why a shell command was rejected and gives safe staging alternatives when a command would add local Humanize loop state to version control.
- algorithmic_behavior: The template itself is declarative, but it is selected by `git_add_humanize_blocked_message()` in `hooks/lib/loop-common.sh:932-970`. The decision algorithm lives in `git_adds_humanize()` at `hooks/lib/loop-common.sh:834-930`: split commands on shell operators, detect `git ... add`, extract and normalize add arguments, block direct `.humanize` references, block forced broad-scope adds, block `-A` or `--all` when `.humanize` exists, and block broad `.` or `*` when `.humanize` is not ignored. When blocked, `hooks/loop-bash-validator.sh:109-118` emits this template and exits 2.
- inputs_outputs_state: Inputs are shell command text already normalized to lowercase by the validator path, the existence of `.humanize`, and `git check-ignore -q .humanize` status. Output is a Markdown explanation headed “Git Add Blocked: .humanize Protection” at `prompt-template/block/git-add-humanize.md:1`, with safe commands at `prompt-template/block/git-add-humanize.md:8-14`, blocked examples at `prompt-template/block/git-add-humanize.md:16-24`, and `.gitignore` remediation steps at `prompt-template/block/git-add-humanize.md:26-34`.
- gates_or_invariants: The main invariant is that `.humanize/` contains local loop state and should not be committed, stated at `prompt-template/block/git-add-humanize.md:3-6`. The validator treats direct `.humanize` references as blocked regardless of flags at `hooks/lib/loop-common.sh:871-877`. Force plus broad scope is blocked because force bypasses gitignore at `hooks/lib/loop-common.sh:902-907`. Non-force broad scope is blocked only if `.humanize` exists and is not gitignored at `hooks/lib/loop-common.sh:909-925`.
- dependencies_and_callers: Called through `hooks/loop-bash-validator.sh` after `hooks/lib/loop-common.sh` is sourced. Template loading uses `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` with an embedded fallback copy at `hooks/lib/loop-common.sh:935-969`, so the gate can still explain itself if the template is missing. Template reference coverage scans `hooks/lib/loop-common.sh` and related hooks in `tests/test-template-references.sh:56-110`.
- edge_cases_or_failure_modes: The detector handles chained commands split by `&&`, `||`, pipe, and semicolon at `hooks/lib/loop-common.sh:837-845`; `git -C` or other git options before `add` at `hooks/lib/loop-common.sh:850-859`; quoted `.humanize` by stripping quotes at `hooks/lib/loop-common.sh:866-875`; combined force/all short flags at `hooks/lib/loop-common.sh:879-894`; and avoids over-blocking names like `.humanizeconfig` via the direct-reference regex noted at `hooks/lib/loop-common.sh:871-875`. The template’s final warning says the commit message for adding ignore rules must not contain the literal string `.humanize`, to avoid triggering the protection at `prompt-template/block/git-add-humanize.md:34`.
- validation_or_tests: `tests/test-humanize-escape.sh` directly tests path variants, quoted paths, force/broad-scope combinations, `-A`/`--all`, chained commands, and `git -C` forms for `git_adds_humanize()` at `tests/test-humanize-escape.sh:41-160`. `tests/test-template-references.sh` verifies referenced templates exist, and `tests/test-templates-comprehensive.sh` loads every Markdown template and checks placeholder syntax at `tests/test-templates-comprehensive.sh:83-140`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-107 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Footer template appended to generated next-round RLCR prompts. It establishes non-bypass loop behavior and the required end-of-round actions for the Claude-side repair loop: optional simplifier review, commit, and summary write.
- algorithmic_behavior: The template is rendered by `hooks/loop-codex-stop-hook.sh` when Codex review blocks exit and the system creates the next round prompt. The hook increments `current_round`, writes `round-<NEXT_ROUND>-prompt.md`, defines `NEXT_SUMMARY_FILE`, renders the main next-round prompt, optionally appends post-alignment instructions, then appends this footer through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-footer.md"` with `NEXT_SUMMARY_FILE` substitution at `hooks/loop-codex-stop-hook.sh:1062-1097`.
- inputs_outputs_state: Input is the placeholder value `NEXT_SUMMARY_FILE`, set to `$LOOP_DIR/round-${NEXT_ROUND}-summary.md` at `hooks/loop-codex-stop-hook.sh:1068-1070`. Output is appended Markdown containing a hard instruction not to exit by lying, editing loop state, or executing `cancel-rlcr-loop` at `prompt-template/claude/next-round-footer.md:4`, followed by numbered completion actions at `prompt-template/claude/next-round-footer.md:6-9`. State transition is from current review-blocked round to next prompt file and updated state file, with the final `jq` response blocking exit and returning the prompt as the reason at `hooks/loop-codex-stop-hook.sh:1115-1125`.
- gates_or_invariants: The footer enforces that loop state is controlled by the loop system, not by the agent, and that cancellation is not an acceptable escape path for normal work completion. It also requires a commit and summary write before exiting at `prompt-template/claude/next-round-footer.md:8-9`. The placeholder must be rendered because the summary path is the concrete artifact the next round depends on.
- dependencies_and_callers: Depends on the template loader and stop-hook context variables. Primary caller is `hooks/loop-codex-stop-hook.sh:1093-1097`. If the template is unavailable, the hook falls back to a shorter “Before Exiting” message at `hooks/loop-codex-stop-hook.sh:1093-1095`. It can be followed by `push-every-round-note.md` when `PUSH_EVERY_ROUND` is true at `hooks/loop-codex-stop-hook.sh:1099-1106`, and by the goal tracker update request at `hooks/loop-codex-stop-hook.sh:1108-1113`.
- edge_cases_or_failure_modes: If `code-simplifier` is not installed, the first completion action is conditional and should be skipped without blocking the rest of the footer at `prompt-template/claude/next-round-footer.md:7`. If `NEXT_SUMMARY_FILE` substitution fails or is missing, the agent receives an unresolved `@{{NEXT_SUMMARY_FILE}}` target, making summary placement ambiguous. If the template is missing, the fallback loses the explicit anti-bypass and simplifier instructions but still preserves commit and summary requirements.
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`, which scans `hooks/loop-codex-stop-hook.sh` and verifies `claude/next-round-footer.md` exists through the generic template-reference extractor at `tests/test-template-references.sh:56-110`. Template loadability and placeholder syntax are covered by `tests/test-templates-comprehensive.sh:83-140`. Related finalize-phase simplifier expectations are tested separately in `tests/test-finalize-phase.sh`, but this footer itself is primarily validated structurally rather than by a dedicated behavioral fixture.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`