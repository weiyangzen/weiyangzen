# agent_20 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-020 `file` `commands/start-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow entrypoint for PR-loop activation. It is a thin command definition that delegates the algorithm to `scripts/setup-pr-loop.sh` via the only allowed tool in frontmatter, `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-pr-loop.sh:*)`, at `commands/start-pr-loop.md:1-5` and `commands/start-pr-loop.md:10-14`.
- algorithmic_behavior: Defines the user-facing PR review loop contract: detect the current branch PR, fetch monitored bot comments, fix issues, push changes, trigger re-review with the bot mentions, let the Stop Hook poll for new reviews, and use local Codex validation before loop exit. The ordered behavior is described at `commands/start-pr-loop.md:16-24` and expanded as the main loop at `commands/start-pr-loop.md:37-45`.
- inputs_outputs_state: Inputs are command arguments `$ARGUMENTS` plus required bot flags `--claude` and/or `--codex` (`commands/start-pr-loop.md:25-30`). Options include max iteration count and Codex model/timeout in the frontmatter hint (`commands/start-pr-loop.md:3`). Output is not direct code mutation from this file; it runs setup, which creates `.humanize/pr-loop/<timestamp>/state.md`, round prompt/comment/resolve files, and goal tracker as seen in `scripts/setup-pr-loop.sh:394-412`, `scripts/setup-pr-loop.sh:535-555`, and `scripts/setup-pr-loop.sh:561-684`.
- gates_or_invariants: Requires at least one bot flag (`commands/start-pr-loop.md:25-30`), requires summaries before exit (`commands/start-pr-loop.md:50-53`), requires pushed fixes and correct bot tags for remote review (`commands/start-pr-loop.md:52-55`), forbids state-file/cancel-command cheating (`commands/start-pr-loop.md:54-56`), and ends only on max iterations, all monitored bots approving, or explicit cancel (`commands/start-pr-loop.md:58-62`).
- dependencies_and_callers: Depends directly on `scripts/setup-pr-loop.sh`. That script enforces mutual exclusion with active RLCR/PR loops (`scripts/setup-pr-loop.sh:220-240`), git/gh/codex prerequisites (`scripts/setup-pr-loop.sh:243-277`), PR lookup including fork-parent fallback (`scripts/setup-pr-loop.sh:295-359`), YAML-safe branch/model/effort fields (`scripts/setup-pr-loop.sh:365-388`), and state/prompt generation (`scripts/setup-pr-loop.sh:535-684`). Runtime continuation depends on `hooks/pr-loop-stop-hook.sh`, whose header describes polling, PR state checks, Codex approval validation, and active bot updates (`hooks/pr-loop-stop-hook.sh:3-19`).
- edge_cases_or_failure_modes: Setup aborts when another loop is active, outside git, no commits exist, `gh` is missing or unauthenticated, `codex` is missing, no PR is found, PR is merged/closed, branch/model fields are YAML-unsafe, trigger comment posting fails, trigger comment ID cannot be found for Claude eyes verification, or Claude fails the eyes reaction check (`scripts/setup-pr-loop.sh:225-277`, `scripts/setup-pr-loop.sh:326-348`, `scripts/setup-pr-loop.sh:365-388`, `scripts/setup-pr-loop.sh:443-519`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers setup-script help, missing/invalid bot flags, `--claude`, `--codex`, both bots, `--max`, `--codex-model`, and `--codex-timeout` parsing (`tests/test-pr-loop-scripts.sh:18-166`). `tests/run-all-tests.sh` includes PR-loop suites in the global suite list (`tests/run-all-tests.sh:52-53`).
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-050 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for template-loader error semantics. It validates that prompt-template loading and rendering degrade safely instead of crashing hooks that gate RLCR/PR-loop transitions.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, derives the template root via `get_template_dir`, then runs 12 assertions with local pass/fail counters (`tests/test-error-scenarios.sh:11-24`, `tests/test-error-scenarios.sh:25-35`). It verifies missing templates/directories return empty with exit 0, missing `load_and_render` returns empty, empty render returns empty, special characters render literally, strict-mode callers continue, safe fallback works and renders variables, valid/invalid template directories are accepted/rejected, and malformed placeholders remain unchanged (`tests/test-error-scenarios.sh:42-206`).
- inputs_outputs_state: Inputs are the repository root, template directory, synthetic template strings, fallback strings, and variable assignments (`tests/test-error-scenarios.sh:11-15`, `tests/test-error-scenarios.sh:98-101`, `tests/test-error-scenarios.sh:138-153`). Output is console PASS/FAIL lines and process exit 0 only if `TESTS_FAILED` remains zero (`tests/test-error-scenarios.sh:211-226`). State is local shell counters; no repository state is intentionally modified.
- gates_or_invariants: The key invariant is graceful degradation under `set -uo pipefail`: missing templates must not make callers abort, because hooks use templates to generate block reasons. `load_template` is expected to return empty for missing files (`hooks/lib/template-loader.sh:33-48`), `load_and_render` emits only when content is non-empty (`hooks/lib/template-loader.sh:134-147`), `load_and_render_safe` must fall back on missing/empty render results (`hooks/lib/template-loader.sh:170-203`), and `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:208-222`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`. The loader is used broadly by PR and RLCR hooks, including PR stop-hook blocks (`hooks/pr-loop-stop-hook.sh:356`, `hooks/pr-loop-stop-hook.sh:415`, `hooks/pr-loop-stop-hook.sh:729`), Codex/RLCR stop-hook blocks (`hooks/loop-codex-stop-hook.sh:473`, `hooks/loop-codex-stop-hook.sh:1101`), validators, and setup scripts. `tests/test-template-references.sh` separately enforces template reference existence and safe rendering usage.
- edge_cases_or_failure_modes: Covers absent template file, absent template directory, empty template content, special regex-like characters in values, strict mode interaction, fallback variable substitution, invalid directory, empty placeholder `{{}}`, and unclosed `{{` placeholder (`tests/test-error-scenarios.sh:42-206`). The loader itself uses a single-pass `awk` renderer to avoid placeholder injection and preserve missing placeholders (`hooks/lib/template-loader.sh:50-132`).
- validation_or_tests: I ran `bash tests/test-error-scenarios.sh` in this read-only branch export. Result: 8 passed, 4 failed. Failures were caused by sandbox filesystem restrictions when Bash attempted the here-string feeding `awk` in `render_template` (`hooks/lib/template-loader.sh:129`): `cannot create temp file for here document: Operation not permitted`. The affected assertions were special-character render, fallback substitution, empty placeholder preservation, and unclosed placeholder preservation. This is validation evidence for the assigned script, but the failure is environment-induced by the read-only sandbox rather than an edited-code result.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-080 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the “dirty git state” gate. It tells the agent that it is trying to stop while the worktree has unresolved git issues, and gives the commit/staging actions required before exit can proceed.
- algorithmic_behavior: Renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` into a blocking reason (`prompt-template/block/git-not-clean.md:1-5`). The Codex/RLCR stop-hook computes `GIT_ISSUES="uncommitted changes"` from cached git status, adds notes for `.humanize*` and other untracked files, renders this template through `load_and_render_safe`, emits JSON with `"decision": "block"`, and exits successfully to block the stop attempt (`hooks/loop-codex-stop-hook.sh:432-486`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES` template variables. `SPECIAL_NOTES` may include text from `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md` when untracked files match those categories (`hooks/loop-codex-stop-hook.sh:441-461`). Output is a rendered human-readable block reason embedded in hook JSON (`hooks/loop-codex-stop-hook.sh:477-484`). The state transition is “attempted stop” to “blocked until git clean”; no loop state file is advanced here.
- gates_or_invariants: Requires review of untracked files, staging real changes, and committing with a descriptive project-convention message (`prompt-template/block/git-not-clean.md:5-10`). It also enforces commit authorship hygiene: AI tools must not be authors and must not add AI co-author trailers (`prompt-template/block/git-not-clean.md:11-16`). The stop hook treats any non-empty cached git status as a hard block before later push checks or next-round generation (`hooks/loop-codex-stop-hook.sh:437-486`).
- dependencies_and_callers: Consumed by `hooks/loop-codex-stop-hook.sh` via `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:473-475`). The rendering path depends on `hooks/lib/template-loader.sh`, specifically safe fallback behavior (`hooks/lib/template-loader.sh:170-203`). This template is not referenced by `hooks/pr-loop-stop-hook.sh` in the current search results, so its direct caller is the RLCR/Codex stop-hook path, though the template-loader contract is shared with PR-loop code.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the caller falls back to an inline `# Git Not Clean` message (`hooks/loop-codex-stop-hook.sh:467-475`). If `.humanize*` directories are untracked, the hook adds a special note rather than treating them exactly like other artifacts (`hooks/loop-codex-stop-hook.sh:444-451`). Other untracked files get an artifact-review note (`hooks/loop-codex-stop-hook.sh:453-460`).
- validation_or_tests: `tests/test-finalize-phase.sh` includes a git-clean blocking assertion by checking for a block decision containing “uncommitted”, “git”, or “clean” (`tests/test-finalize-phase.sh:408`). The assigned `tests/test-error-scenarios.sh` also validates the fallback/rendering machinery this template depends on.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-110 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Conditional prompt-footer note for loops configured to require remote pushes after each round.
- algorithmic_behavior: The template is a static note: when `--push-every-round` is enabled, the agent must push commits to remote after each round (`prompt-template/claude/push-every-round-note.md:1-2`). In `hooks/loop-codex-stop-hook.sh`, the next-round prompt appends this template only when `PUSH_EVERY_ROUND == "true"`; missing template falls back to `Also push your changes after committing.` (`hooks/loop-codex-stop-hook.sh:1099-1105`).
- inputs_outputs_state: Input is the parsed loop-state boolean `push_every_round`. `loop-common.sh` defines and parses this field, defaulting it to `false` (`hooks/lib/loop-common.sh:21`, `hooks/lib/loop-common.sh:218-227`). Output is appended text in `NEXT_PROMPT_FILE`, so the next round’s agent receives a push requirement. It does not itself mutate git or state.
- gates_or_invariants: The invariant is instructional consistency with the push gate. When push-every-round is true, the stop hook also blocks local branches that are ahead of remote before allowing exit (`hooks/loop-codex-stop-hook.sh:489-517`). For PR loops, pushing is core workflow even without this exact template, because PR-loop startup and critical requirements tell the agent to commit, push, and tag bots for re-review (`commands/start-pr-loop.md:52-55`, `scripts/setup-pr-loop.sh:737-756`, `scripts/setup-pr-loop.sh:841-858`).
- dependencies_and_callers: Directly loaded by `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:1101-1105`). Related setup for RLCR stores `push_every_round` in state (`scripts/setup-rlcr-loop.sh:539-547`) and writes a similar inline initial-round note when enabled (`scripts/setup-rlcr-loop.sh:717-723`). Search found no direct use by `scripts/setup-pr-loop.sh`; PR-loop pushing is enforced through PR-specific prompt text and stop-hook unpushed-commit checks rather than this static note.
- edge_cases_or_failure_modes: If the template is missing, next-round prompt generation still proceeds with an inline fallback (`hooks/loop-codex-stop-hook.sh:1101-1104`). If `push_every_round` is absent or malformed, shared parsing defaults it to `false`, so this note is not appended (`hooks/lib/loop-common.sh:223-227`).
- validation_or_tests: `tests/test-finalize-phase.sh` contains fixture state with `push_every_round: false` (`tests/test-finalize-phase.sh:140`), and the stop-hook unpushed-commit gate is covered indirectly by tests that look for block decisions in finalize/loop scenarios. The template itself is also discoverable by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls for valid template references (`tests/test-template-references.sh:67-94`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 of 4 assigned IDs appear once, as Item Evidence headings only
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`