# agent_25 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-025 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: RLCR stop-hook state-machine gate. It intercepts Claude stop attempts, finds the active `.humanize/rlcr/<timestamp>` loop, validates loop/session invariants, runs Codex review when cheap gates pass, and either blocks exit with next-round/finalize instructions or terminates the loop state. See [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:3).
- algorithmic_behavior: Reads hook JSON from stdin into `HOOK_INPUT`, intentionally ignores `stop_hook_active`, resolves `PROJECT_ROOT`, sources `loop-common.sh` and `portable-timeout.sh`, and calls `find_active_loop` to select the newest active loop. It detects normal `state.md` versus post-Codex `finalize-state.md`; parses state via `parse_state_file_strict`; maps `STATE_*` variables into local loop variables; validates model/effort and numeric round fields; then executes a sequence of fail-closed gates before Codex review. The review phase builds either a regular review or full-alignment prompt every fifth round where `CURRENT_ROUND % 5 == 4`, runs `codex exec`, validates the review result file, and dispatches on the last non-empty line: `COMPLETE`, `STOP`, or ordinary issue feedback. Key transition lines: active loop and phase detection at [line 59](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:59), state parsing at [line 89](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:89), review prompt selection at [line 686](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:686), Codex invocation at [line 843](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:843), and marker dispatch at [line 960](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:960).
- inputs_outputs_state: Inputs are stdin hook payload, `CLAUDE_PROJECT_DIR` or cwd, `.humanize/rlcr/*/{state.md,finalize-state.md,plan.md,goal-tracker.md,round-N-summary.md}`, git status/branch data, templates from `prompt-template`, `check-todos-from-transcript.py`, and Codex CLI output. Outputs are Claude hook JSON blocks with `"decision": "block"` for actionable failures, next-round prompts, `round-N-review-prompt.md`, `round-N-review-result.md`, cache/debug files under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<timestamp>/`, and state-file renames such as `state.md -> finalize-state.md`, `state.md -> maxiter-state.md`, or `state.md -> stop-state.md`. If no active loop or no state file exists, it exits 0 and allows stop.
- gates_or_invariants: Enforces state schema fields `plan_tracked` and `start_branch`; branch consistency against `start_branch`; plan backup existence and exact content match; no incomplete transcript todos; git status success; no dirty worktree; no unpushed commits when `push_every_round=true`; no changed code/docs files over 2000 lines; required summary file; initialized Round 0 goal tracker; max-iteration ceiling; installed `codex`; non-empty review result; strict marker matching only when the last non-empty line is exactly `COMPLETE` or `STOP`. The goal-tracker gate extracts only the `Ultimate Goal`, `Acceptance Criteria`, and `Active Tasks` sections and detects generic `[To be ...]` placeholders, preventing stray placeholder mentions elsewhere from blocking.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file_strict`, marker constants, `to_lower`, and `end_loop`; `hooks/lib/template-loader.sh` via `loop-common.sh`; `scripts/portable-timeout.sh`; `hooks/check-todos-from-transcript.py`; `jq`, `git`, `diff`, `awk`, `grep`, `sed`, `wc`, and `codex`. It renders many prompt blocks, including `block/goal-tracker-not-initialized.md`, `codex/regular-review.md`, `codex/full-alignment-review.md`, `claude/finalize-phase-prompt.md`, and `claude/next-round-prompt.md`. It is the stop hook called by Claude Code when an RLCR loop is active.
- edge_cases_or_failure_modes: Malformed state parsing currently prints “allowing exit for safety” and exits 0, while invalid model/effort/current_round calls `end_loop ... unexpected`; non-numeric `max_iterations` falls back to `42`. `parse_state_file_strict` accepts negative numeric strings, but this hook re-validates `CURRENT_ROUND` with non-negative digits only; `MAX_ITERATIONS` negative is silently replaced. Git status failures block rather than fail open, but `git rev-parse --git-dir` is run without `-C "$PROJECT_ROOT"` in the cached-status section, so correctness depends on current working directory matching project root. If Codex outputs `COMPLETE` at or past max iterations, the hook records `maxiter` instead of entering finalize. If Codex writes to stdout instead of the requested review file, stdout is copied into the review result.
- validation_or_tests: Syntax checked with `bash -n hooks/loop-codex-stop-hook.sh` successfully. Related tests found by inspection include finalize/state naming tests and monitor tests, but this research task did not execute mutating e2e tests. Shared helper behavior is covered by robustness tests referencing `find_active_loop`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-055 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: Executable e2e specification for monitor behavior, focused on real monitor functions rather than stubs. It validates that RLCR and PR monitor loops exit cleanly when state directories disappear or when interrupted, without zsh/bash glob errors and with terminal cleanup. Header scope is stated at [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/tests/test-monitor-e2e-real.sh:3).
- algorithmic_behavior: Creates isolated temporary projects under `/tmp/test-monitor-e2e-real-$$`, seeds minimal `.humanize/rlcr` or `.humanize/pr-loop` session directories, writes state and goal-tracker fixtures, points `HOME`/`XDG_CACHE_HOME` to fake cache roots, shims terminal commands (`tput`, `clear`, sometimes `printf`), sources `scripts/humanize.sh`, and invokes real `_humanize_monitor_codex`, `_humanize_monitor_pr`, or `humanize monitor pr`. Each scenario starts a monitor, deletes a loop directory or sends SIGINT, waits in a bounded polling loop, force-kills if necessary, then asserts output patterns and exit-code markers. Test setup and cleanup are at [line 48](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/tests/test-monitor-e2e-real.sh:48); bash RLCR deletion scenario starts at [line 59](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/tests/test-monitor-e2e-real.sh:59).
- inputs_outputs_state: Inputs are repo `scripts/humanize.sh`, fake RLCR state files containing `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, `plan_file`, fake PR loop state with `pr_number`, bots, poll intervals, and cache log files named like `round-1-codex-run.log`. Outputs are PASS/FAIL counters, captured monitor output files in the temp tree, and final process exit 0 only when `TESTS_FAILED == 0`. It modifies only its temp test tree and fake cache during normal execution.
- gates_or_invariants: Asserts monitor processes exit within 20 half-second waits after deletion/SIGINT; output contains “Monitoring stopped:” and deletion reason for RLCR deletion; output does not contain `no matches found` or `bad pattern`; RLCR source contains a scroll-region reset escape; graceful exits include `EXIT_CODE:0`; zsh tests are skipped when `zsh` is unavailable; PR monitor tests accept graceful messages or clean `EXIT_CODE:0`/`EXIT_CODE:[01]` depending on signal timing. Test 5 exercises `humanize monitor pr --once`; Test 6 exercises continuous `humanize monitor pr`.
- dependencies_and_callers: Depends on bash, optional zsh, `scripts/humanize.sh`, process control commands (`kill`, `pkill`, `wait`, `sleep`), `/tmp`, and fake XDG cache layout matching monitor expectations. It targets `_humanize_monitor_codex` defined in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/scripts/humanize.sh:219), command dispatch for `humanize monitor pr` at [line 843](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/scripts/humanize.sh:843), and `_humanize_monitor_pr` at [line 890](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/scripts/humanize.sh:890). It implicitly depends on monitor code using `find` instead of globs, which is called out in `scripts/humanize.sh`.
- edge_cases_or_failure_modes: Signal delivery is treated as nondeterministic: bash SIGINT and PR no-`--once` tests may pass after SIGTERM fallback if the monitor had been running. zsh coverage is conditional. The PR `--once` deletion test deletes a specific session directory after 0.5s, so timing matters. Because the file creates/deletes `/tmp` trees and generated runner scripts, it is not safe to run under this “do not modify files” research constraint.
- validation_or_tests: Syntax checked with `bash -n tests/test-monitor-e2e-real.sh` successfully. Full e2e execution was intentionally not run because it would create and delete temp files/scripts under `/tmp`, contrary to the scheduler instruction to produce research notes only and not modify files.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-085 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Round 0 goal-tracker initialization gate. It turns the hook’s detected missing placeholder sections into instructions that keep Claude inside the loop until the immutable goal-tracker content is filled.
- algorithmic_behavior: Contains placeholders `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, rendered by `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh` when the Round 0 goal tracker contains placeholder text. The hook populates missing entries for Ultimate Goal, Acceptance Criteria, and Active Tasks at [hooks/loop-codex-stop-hook.sh:599](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:599), then renders this template at [line 619](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/hooks/loop-codex-stop-hook.sh:619).
- inputs_outputs_state: Inputs are the path to `goal-tracker.md` and the generated missing-items bullet list. Output is hook block text telling the agent to read the goal tracker, replace placeholder content, define 3-7 testable acceptance criteria, populate Active Tasks mapped to ACs, and retry exit. The template itself does not mutate state; it drives the next Claude action through hook JSON.
- gates_or_invariants: Reinforces the invariant that the IMMUTABLE SECTION “can only be set in Round 0” and becomes read-only afterward. This is the human-readable contract paired with the hook’s Round 0-only placeholder detector.
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh`; rendered through `hooks/lib/template-loader.sh` using `TEMPLATE_DIR` from `hooks/lib/loop-common.sh`. It also coordinates with write/edit/bash validators that protect goal-tracker immutability after Round 0, although those validators are outside this assigned item.
- edge_cases_or_failure_modes: If the template is missing, the hook has an inline fallback with the same placeholders. If the hook’s section extraction misses a malformed heading, this template will not be rendered. If placeholder text appears in sections outside the three inspected sections, the hook intentionally ignores it.
- validation_or_tests: Direct inspection of [prompt-template/block/goal-tracker-not-initialized.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/prompt-template/block/goal-tracker-not-initialized.md:1). Its rendering path is covered by the hook syntax check and by the hook code path at lines 614-631.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-115 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt block that defines the required completion protocol when existing review comments/issues are present. It ensures an agent cannot treat fixes as complete until changes are committed, pushed, re-review is triggered, and a resolution summary is written.
- algorithmic_behavior: Emits a critical requirements section with three mandatory actions: commit and push, comment on the PR with `gh pr comment {{PR_NUMBER}} --body "{{BOT_MENTION_STRING}} please review"`, and write a resolution summary to `{{RESOLVE_PATH}}` including issues addressed, files modified, and tests added. This template is selected by `scripts/setup-pr-loop.sh` when `COMMENT_COUNT != 0`; the no-comment branch uses a separate template that explicitly avoids PR comments. Selection is at [scripts/setup-pr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/scripts/setup-pr-loop.sh:810).
- inputs_outputs_state: Inputs are template variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, passed in the setup script’s `TEMPLATE_VARS`. Output is prompt text appended during PR loop setup/startup, guiding the worker toward `round-N-pr-resolve.md` and the external PR re-review trigger. It does not directly poll or mutate state; it establishes the completion contract consumed later by stop hooks and monitor flow.
- gates_or_invariants: Encodes the invariant that with existing review comments, local work is not complete until remote PR review has been re-triggered after a pushed commit and a resolution summary exists. This prevents the PR loop from stalling on unpushed or unreviewed local fixes.
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-has-comments.md"` in `scripts/setup-pr-loop.sh` at [line 860](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/scripts/setup-pr-loop.sh:860). It coordinates with `hooks/pr-loop-stop-hook.sh`, which later polls bot reviews, and with `humanize monitor pr`, which displays PR loop progress.
- edge_cases_or_failure_modes: Shell quoting in the displayed `gh pr comment` command depends on `BOT_MENTION_STRING` being safe for double-quoted shell use; if bot mention text contains quotes or shell metacharacters, the prompt command may need manual adjustment. If `COMMENT_COUNT` is wrong during setup, the system may choose the no-comments requirements and fail to ask for a re-review trigger.
- validation_or_tests: Direct inspection of [prompt-template/pr-loop/critical-requirements-has-comments.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-pr-loop/prompt-template/pr-loop/critical-requirements-has-comments.md:1). Caller branch inspected in `scripts/setup-pr-loop.sh`; syntax checks for assigned shell files passed, but no mutating PR-loop e2e was run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique item sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`