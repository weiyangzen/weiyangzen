# agent_06 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime command layer for the Humanize loops. The directory contains setup, cancellation, GitHub PR polling/comment/reaction ingestion, monitoring, timeout portability, and gen-plan I/O validation scripts. Recursive contents inspected: `scripts/cancel-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/fetch-pr-comments.sh`, `scripts/humanize.sh`, `scripts/poll-pr-reviews.sh`, `scripts/portable-timeout.sh`, `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, and `scripts/lib/monitor-common.sh`.
- algorithmic_behavior: The directory coordinates two loop families. RLCR setup validates a plan file, creates `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `plan.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:196`, `scripts/setup-rlcr-loop.sh:258`, `scripts/setup-rlcr-loop.sh:539`, `scripts/setup-rlcr-loop.sh:560`, `scripts/setup-rlcr-loop.sh:658`). PR setup validates bot flags, resolves PR/fork/base repo context, fetches comments, computes startup case, optionally posts trigger comments, verifies Claude eyes reactions, and writes PR loop state/prompt/tracker files (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:295`, `scripts/setup-pr-loop.sh:411`, `scripts/setup-pr-loop.sh:418`, `scripts/setup-pr-loop.sh:443`, `scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:632`, `scripts/setup-pr-loop.sh:678`). Monitor utilities find latest timestamped sessions and state files, classify PR phases, parse trackers, and render terminal status (`scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:267`, `scripts/lib/monitor-common.sh:390`).
- inputs_outputs_state: Inputs include CLI flags, `CLAUDE_PROJECT_DIR`, plan files, git state, GitHub CLI API output, active loop directories, and prompt templates. Outputs include state files, prompt files, goal trackers, formatted PR comment markdown, JSON reviewer/poll results, cancellation marker files, terminal monitor output, and validation status messages. State transitions include active `state.md` to `cancel-state.md` for cancellation (`scripts/cancel-rlcr-loop.sh:85`, `scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`; `scripts/cancel-pr-loop.sh:88`, `scripts/cancel-pr-loop.sh:118`, `scripts/cancel-pr-loop.sh:122`) and monitor phase detection from files such as `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` (`scripts/lib/monitor-common.sh:272`).
- gates_or_invariants: Only one loop type may be active at a time (`scripts/setup-rlcr-loop.sh:196`, `scripts/setup-pr-loop.sh:221`). RLCR plan paths must be relative, no spaces, no shell metacharacters, no symlink file or symlink parent, inside project, readable, non-submodule, and contentful (`scripts/setup-rlcr-loop.sh:258`, `scripts/setup-rlcr-loop.sh:264`, `scripts/setup-rlcr-loop.sh:274`, `scripts/setup-rlcr-loop.sh:285`, `scripts/setup-rlcr-loop.sh:297`, `scripts/setup-rlcr-loop.sh:341`, `scripts/setup-rlcr-loop.sh:416`, `scripts/setup-rlcr-loop.sh:464`). YAML-sensitive values reject unsafe branch/model/effort characters (`scripts/setup-rlcr-loop.sh:489`, `scripts/setup-pr-loop.sh:365`). PR scripts require numeric PR numbers, `gh`, `jq`, authentication where needed, open PR state, and base-repo resolution for fork PRs (`scripts/fetch-pr-comments.sh:103`, `scripts/fetch-pr-comments.sh:112`, `scripts/poll-pr-reviews.sh:99`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:340`).
- dependencies_and_callers: Setup scripts source `scripts/portable-timeout.sh` for cross-platform command timeouts (`scripts/setup-rlcr-loop.sh:27`, `scripts/setup-pr-loop.sh:33`) and `hooks/lib/loop-common.sh`/`hooks/lib/template-loader.sh` for active-loop detection, YAML/list helpers, and template rendering (`scripts/setup-rlcr-loop.sh:188`, `scripts/setup-pr-loop.sh:37`, `scripts/setup-pr-loop.sh:210`). GitHub scripts depend on `gh` and `jq` (`scripts/fetch-pr-comments.sh:112`, `scripts/poll-pr-reviews.sh:108`). `scripts/humanize.sh` sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:7`). PR setup calls sibling scripts `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh` (`scripts/setup-pr-loop.sh:411`, `scripts/setup-pr-loop.sh:418`, `scripts/setup-pr-loop.sh:507`).
- edge_cases_or_failure_modes: Handles missing loop dirs, missing state files, malformed/unsafe paths, git timeouts, no commits, untracked/tracked plan policy mismatches, absent Codex/gh/jq, fork PR API lookup, GitHub API retry failure, empty review bodies, clock skew avoiding local timestamp fallback, missing trigger comment ID, absent Claude eyes reaction, log deletion/truncation/rotation, and terminal cleanup on signals (`scripts/setup-rlcr-loop.sh:242`, `scripts/setup-rlcr-loop.sh:365`, `scripts/fetch-pr-comments.sh:182`, `scripts/setup-pr-loop.sh:479`, `scripts/setup-pr-loop.sh:488`, `scripts/humanize.sh:736`, `scripts/humanize.sh:756`, `scripts/humanize.sh:496`).
- validation_or_tests: Covered by path robustness tests against `setup-rlcr-loop.sh`, PR loop script tests, monitor runtime tests, template loader/reference tests, and command/path validator tests under `tests/`. The assigned robustness test explicitly drives production `scripts/setup-rlcr-loop.sh` (`tests/robustness/test-path-validation-robustness.sh:55`). `tests/run-all-tests.sh` includes `test-template-loader.sh`, PR loop tests, and robustness suites.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-036 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Interactive monitor and parser entrypoint for Humanize shell users. It exposes `humanize monitor rlcr` and `humanize monitor pr`, plus reusable parsing helpers for goal trackers and git status.
- algorithmic_behavior: Public helpers split pipe-delimited data compatibly across bash/zsh (`scripts/humanize.sh:18`), parse RLCR goal tracker counts and goal text (`scripts/humanize.sh:32`), detect git repository states (`scripts/humanize.sh:116`), and summarize porcelain/diff status (`scripts/humanize.sh:166`). `_humanize_monitor_codex` finds the latest RLCR session and Codex log, reads state/goal/git data, draws a fixed status bar, tails log bytes incrementally, switches to newer sessions/logs, and handles missing/truncated/deleted logs (`scripts/humanize.sh:219`, `scripts/humanize.sh:238`, `scripts/humanize.sh:245`, `scripts/humanize.sh:333`, `scripts/humanize.sh:580`, `scripts/humanize.sh:731`, `scripts/humanize.sh:793`). `_humanize_monitor_pr` similarly monitors PR loop files and supports `--once` scripting output (`scripts/humanize.sh:890`, `scripts/humanize.sh:901`, `scripts/humanize.sh:927`, `scripts/humanize.sh:1174`).
- inputs_outputs_state: Inputs are `.humanize/rlcr` or `.humanize/pr-loop` session directories, `state.md` or `*-state.md`, `goal-tracker.md`, cached Codex run logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, git status, terminal size, and optional `--once`. Outputs are terminal status bars, tailed log/activity text, one-shot PR status reports, and exit codes for missing dirs/sessions. Internal state includes current session, current file, last file size, cleanup flags, monitor_running flags, and PR tail PID (`scripts/humanize.sh:224`, `scripts/humanize.sh:574`, `scripts/humanize.sh:899`, `scripts/humanize.sh:1128`).
- gates_or_invariants: Requires loop directories before monitoring (`scripts/humanize.sh:230`, `scripts/humanize.sh:914`). Uses timestamped session-name regex for latest-session/log selection (`scripts/humanize.sh:270`). Uses `find` rather than shell globs for zsh-safe empty-directory behavior (`scripts/humanize.sh:263`, `scripts/humanize.sh:280`). Enables zsh `ksharrays` to keep 0-index array behavior (`scripts/humanize.sh:222`, `scripts/humanize.sh:892`). Restores traps and terminal regions on cleanup (`scripts/humanize.sh:496`, `scripts/humanize.sh:833`, `scripts/humanize.sh:1135`, `scripts/humanize.sh:1316`).
- dependencies_and_callers: Sources `scripts/lib/monitor-common.sh` when present (`scripts/humanize.sh:7`) and delegates session/state/file-size/phase/tracker behavior to monitor helpers (`scripts/humanize.sh:238`, `scripts/humanize.sh:303`, `scripts/humanize.sh:570`, `scripts/humanize.sh:1059`). The public `humanize()` dispatcher maps user commands to RLCR/PR monitor functions (`scripts/humanize.sh:843`).
- edge_cases_or_failure_modes: Missing loop directory, no session directories, no log file yet, loop directory deletion, current session/log deletion, newer session without log, log rotation/truncation, non-git directories, detached/rebase/merge/shallow/permission git states, malformed/missing PR state frontmatter, terminal dimensions constraining display length, and duplicate cleanup invocations (`scripts/humanize.sh:34`, `scripts/humanize.sh:120`, `scripts/humanize.sh:554`, `scripts/humanize.sh:600`, `scripts/humanize.sh:653`, `scripts/humanize.sh:736`, `scripts/humanize.sh:992`).
- validation_or_tests: Monitor behavior is covered by monitor-focused tests such as `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/manual-monitor-test.sh`, and zsh safety tests. Parser helpers are also testable directly because they are top-level functions (`scripts/humanize.sh:13`).
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-066 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, especially template directory resolution, load/render behavior, missing-template fallback, literal handling of shell metacharacters, and placeholder-injection prevention.
- algorithmic_behavior: Sources the production loader (`tests/test-template-loader.sh:12`), then runs 41 numbered checks. It verifies `get_template_dir` resolves `prompt-template` (`tests/test-template-loader.sh:44`), `load_template` loads existing files and returns empty for missing files (`tests/test-template-loader.sh:58`, `tests/test-template-loader.sh:71`), `render_template` replaces single/multiple/multiline variables (`tests/test-template-loader.sh:84`, `tests/test-template-loader.sh:99`, `tests/test-template-loader.sh:114`), and `load_and_render_safe` uses fallback only when needed (`tests/test-template-loader.sh:200`, `tests/test-template-loader.sh:214`).
- inputs_outputs_state: Inputs are real templates from `prompt-template`, inline template strings, and variable assignments passed as `VAR=value`. Outputs are pass/fail lines and final process exit `0` when `TESTS_FAILED == 0`, else `1` (`tests/test-template-loader.sh:648`, `tests/test-template-loader.sh:651`). Test state is `TESTS_PASSED`/`TESTS_FAILED` counters updated by helper functions (`tests/test-template-loader.sh:20`, `tests/test-template-loader.sh:24`, `tests/test-template-loader.sh:29`).
- gates_or_invariants: Rendering must be single-pass: placeholders inside injected values must not be expanded later (`tests/test-template-loader.sh:489`, `tests/test-template-loader.sh:504`, `tests/test-template-loader.sh:517`, `tests/test-template-loader.sh:532`). Missing variables stay as literal `{{ID}}` (`tests/test-template-loader.sh:185`). Unused variables are ignored (`tests/test-template-loader.sh:170`). Values containing `&`, backslashes, `$`, backticks, pipes, semicolons, globs, parentheses, redirections, quotes, hashes, tildes, JSON, regex, Windows paths, and multiline content must render literally (`tests/test-template-loader.sh:257`, `tests/test-template-loader.sh:473`). Empty values, non-ASCII text, underscore-prefixed names, and numeric variable names are accepted (`tests/test-template-loader.sh:555`, `tests/test-template-loader.sh:579`, `tests/test-template-loader.sh:604`, `tests/test-template-loader.sh:617`, `tests/test-template-loader.sh:629`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, whose implementation builds `TMPL_VAR_` environment variables and performs a single awk scan (`hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:74`). This loader is used by setup scripts and hook validators to render block, PR-loop, Codex, and Claude prompt templates.
- edge_cases_or_failure_modes: Catches the common awk/gsub replacement bug where `&` can duplicate match text, command-substitution-like values, escaped path values, placeholder injection from review content, missing template directories, empty substitution, and Unicode preservation. It does not mock external hooks; it directly calls the template library.
- validation_or_tests: The file is itself a test and is included by the aggregate runner (`tests/run-all-tests.sh`). It also overlaps with `tests/test-templates-comprehensive.sh`, `tests/test-template-references.sh`, and `tests/robustness/test-template-stress-robustness.sh`.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-096 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Hook block message template for the RLCR Bash validator when a Bash command attempts to modify `round-N-summary.md`. It enforces the transition from unsafe shell write paths to validated Write/Edit tool paths.
- algorithmic_behavior: Static markdown with one placeholder, `{{CORRECT_PATH}}`, rendered into a blocking reason. It tells the agent not to use Bash to modify summary files and points to the active round summary path (`prompt-template/block/summary-bash-write.md:1`, `prompt-template/block/summary-bash-write.md:5`). The rendered message is emitted by `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:387`) when `loop-bash-validator` detects a command modifying `round-[0-9]+-summary.md` (`hooks/loop-bash-validator.sh:375`).
- inputs_outputs_state: Input is `CORRECT_PATH` from the active loop directory/current round (`hooks/loop-bash-validator.sh:376`). Output is a markdown block sent to stderr with validator exit code `2` (`hooks/loop-bash-validator.sh:377`). It does not mutate state itself.
- gates_or_invariants: Preserves summary validation by forcing writes through Write/Edit hooks, because Bash commands like `cat`, `echo`, `sed`, and `awk` bypass round/path validators (`prompt-template/block/summary-bash-write.md:7`). The invariant is that summary files must be written via tool paths that can validate current round and active loop directory.
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/summary-bash-write.md"` (`hooks/lib/loop-common.sh:393`) using the template loader. Called only after command modification detection in `loop-bash-validator`; related Write/Edit validators separately handle wrong summary location and round number (`hooks/loop-write-validator.sh:202`, `hooks/loop-edit-validator.sh:155`).
- edge_cases_or_failure_modes: If the template is missing or empty, `summary_bash_blocked_message` has a fallback with the same `CORRECT_PATH` substitution (`hooks/lib/loop-common.sh:389`). The template depends on correct active-loop/current-round computation; if state parsing fails, the bash validator blocks earlier for safety (`hooks/loop-bash-validator.sh:81`).
- validation_or_tests: Template existence is checked by template reference tests, where `block/summary-bash-write.md` is listed as a common template (`tests/test-template-references.sh:149`). Rendering safety is indirectly covered by `tests/test-template-loader.sh`; command blocking behavior is covered by bash validator and PR/RLCR hook tests.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-126 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for production RLCR plan-file validation in `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: Creates a temporary git repo, commits an initial file, builds plan-file variants, then invokes the production setup script with `CLAUDE_PROJECT_DIR="$TEST_DIR"` (`tests/robustness/test-path-validation-robustness.sh:16`, `tests/robustness/test-path-validation-robustness.sh:19`, `tests/robustness/test-path-validation-robustness.sh:67`). The helper classifies known path/content validation messages as rejection, while treating later “requires codex” or plan tracking errors as evidence that path validation passed (`tests/robustness/test-path-validation-robustness.sh:70`, `tests/robustness/test-path-validation-robustness.sh:76`, `tests/robustness/test-path-validation-robustness.sh:81`).
- inputs_outputs_state: Inputs are generated plan paths and contents. Outputs are pass/fail test lines and `print_test_summary` result (`tests/robustness/test-path-validation-robustness.sh:465`). Test state is the temp repo and `.humanize/rlcr` cleanup before each validation to avoid false “loop already active” failures (`tests/robustness/test-path-validation-robustness.sh:62`).
- gates_or_invariants: Positive cases require normal relative paths, root files, dash/underscore, nested dirs, dotted filenames, long filenames, and deep nested paths to pass path validation (`tests/robustness/test-path-validation-robustness.sh:101`, `tests/robustness/test-path-validation-robustness.sh:120`, `tests/robustness/test-path-validation-robustness.sh:130`, `tests/robustness/test-path-validation-robustness.sh:366`, `tests/robustness/test-path-validation-robustness.sh:381`). Negative cases require rejection of absolute paths, spaces, shell metacharacters, symlink files, symlink parent directories, empty files, comments-only files, short files, nonexistent files, and directories (`tests/robustness/test-path-validation-robustness.sh:158`, `tests/robustness/test-path-validation-robustness.sh:167`, `tests/robustness/test-path-validation-robustness.sh:178`, `tests/robustness/test-path-validation-robustness.sh:321`, `tests/robustness/test-path-validation-robustness.sh:331`, `tests/robustness/test-path-validation-robustness.sh:404`, `tests/robustness/test-path-validation-robustness.sh:413`, `tests/robustness/test-path-validation-robustness.sh:429`, `tests/robustness/test-path-validation-robustness.sh:442`, `tests/robustness/test-path-validation-robustness.sh:451`). Unicode paths are explicitly allowed by policy comment (`tests/robustness/test-path-validation-robustness.sh:393`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for temp dirs, pass/fail counters, and summary output (`tests/test-helpers.sh:86`, `tests/test-helpers.sh:30`, `tests/test-helpers.sh:58`). Tests production code in `scripts/setup-rlcr-loop.sh`, especially path checks at `scripts/setup-rlcr-loop.sh:258`, `scripts/setup-rlcr-loop.sh:264`, `scripts/setup-rlcr-loop.sh:274`, `scripts/setup-rlcr-loop.sh:285`, `scripts/setup-rlcr-loop.sh:297`, and content checks at `scripts/setup-rlcr-loop.sh:416` and `scripts/setup-rlcr-loop.sh:464`.
- edge_cases_or_failure_modes: The harness intentionally tolerates absence of Codex after validation, because reaching the Codex prerequisite means path/content gates already passed (`tests/robustness/test-path-validation-robustness.sh:65`, `tests/robustness/test-path-validation-robustness.sh:76`). It also tolerates filesystem refusal to create the 250-character filename by marking that scenario as skipped/pass-like (`tests/robustness/test-path-validation-robustness.sh:371`). A limitation is that it classifies by matching error text, so production message wording is part of the executable contract.
- validation_or_tests: This file is executable validation and is included in the robustness suite. It directly verifies the core path validation contract against production code rather than a duplicate helper.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 item sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`