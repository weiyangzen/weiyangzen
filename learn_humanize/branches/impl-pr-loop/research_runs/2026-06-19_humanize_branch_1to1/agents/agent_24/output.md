# agent_24 impl-pr-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `96455ba5aff935988d78439ca55427c603b1adcd`

## Item Evidence

### IMPL_PR_LOOP-HZ-024 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash validator for RLCR and PR-loop state-machine safety. It blocks shell-command bypasses of Write/Edit protections for loop-owned state, prompts, summaries, goal tracker files, PR-loop generated files, and `.humanize` git staging. It is registered for the Bash matcher in `hooks/hooks.json:42-48`.
- algorithmic_behavior: Reads hook JSON from stdin, validates it through shared helpers, exits early for non-Bash tools, extracts `.tool_input.command`, lowercases it, discovers active RLCR and PR loops, and applies layered gates. The top-level parse path is `hooks/loop-bash-validator.sh:24-48`; active loop discovery is `hooks/loop-bash-validator.sh:54-67`.
- inputs_outputs_state: Input is a Claude hook JSON payload with `tool_name` and `tool_input.command`. Output is primarily the hook exit code: `0` allows, `1` rejects malformed hook/state input, and `2` blocks a valid Bash command with a human-facing message. It does not mutate loop state. It reads active state from `.humanize/rlcr/<session>/state.md` or `finalize-state.md` and `.humanize/pr-loop/<session>/state.md`; RLCR state parsing sets `CURRENT_ROUND` and `PUSH_EVERY_ROUND` via `parse_state_file_strict` at `hooks/loop-bash-validator.sh:74-96`.
- gates_or_invariants: JSON must be syntactically valid, include `tool_name`, and not exceed depth 30 before any command logic runs (`hooks/loop-bash-validator.sh:24-35`, backed by `hooks/lib/loop-common.sh:55-129`). Bash commands must include `tool_input.command` (`hooks/loop-bash-validator.sh:42-47`). If neither active loop exists, all Bash commands pass (`hooks/loop-bash-validator.sh:64-67`). RLCR malformed state fails closed (`hooks/loop-bash-validator.sh:81-85`; strict parser requires YAML separators and numeric `current_round`/`max_iterations` in `hooks/lib/loop-common.sh:240-295`). `git push` is blocked unless `push_every_round: true` (`hooks/loop-bash-validator.sh:93-106`). Any command that would stage `.humanize` is blocked (`hooks/loop-bash-validator.sh:109-118`). RLCR state files, plan backups, goal tracker, prompt, summary, and todos files have separate modification gates (`hooks/loop-bash-validator.sh:125-395`). PR-loop `state.md` and generated read-only round files are blocked both by full path and bare filename to catch relative-path bypasses (`hooks/loop-bash-validator.sh:403-442`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, loop discovery, strict state parsing, command/file detection, cancel authorization, template-backed block messages, and `.humanize` git-add detection. Depends on `jq`, `find`, `sed`, `grep`, `sort`, `head`, and `git check-ignore` indirectly. Template messages come from `prompt-template/block/*.md` via `load_and_render_safe`. `hooks/lib/loop-common.sh:1004-1014` renders `prompt-template/block/goal-tracker-modification.md`. Tests call this validator directly in several suites, including PR-loop hooks, finalize phase, plan hooks, allowlist validators, cancel-signal tests, and robustness tests; direct references were found in `tests/test-pr-loop-hooks.sh:198`, `tests/test-finalize-phase.sh:325`, and `tests/robustness/test-hook-input-robustness.sh:63`.
- edge_cases_or_failure_modes: The validator distinguishes `finalize-state.md` before `state.md` because the latter pattern can otherwise match the former (`hooks/loop-bash-validator.sh:131-146`). It has a special authorized cancellation path: `mv state.md cancel-state.md` or `mv finalize-state.md cancel-state.md` can pass only when `.cancel-requested` exists and the command has no shell substitutions, newlines, chaining, mixed quotes, extra args, bad source/dest, or symlink source (`hooks/loop-bash-validator.sh:139-156`; helper in `hooks/lib/loop-common.sh:455-613`). It splits `mv`/`cp` source checks on shell operators while preserving redirection operators with placeholders (`hooks/loop-bash-validator.sh:169-304`) and separately detects shell-wrapper bypasses such as `sh -c 'mv state.md /tmp/foo'` (`hooks/loop-bash-validator.sh:306-328`). The generic `command_modifies_file` matcher covers redirection, append, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` (`hooks/lib/loop-common.sh:975-1000`). A notable invariant is fail-closed behavior for malformed RLCR state, but PR-loop state modification protection does not parse PR-loop state before blocking writes; it only requires an active PR-loop dir found by `find_active_pr_loop`.
- validation_or_tests: Template reference coverage includes this script in `tests/test-template-references.sh:57-64`. `tests/run-all-tests.sh:35-48` includes related validator and humanize-escape suites. The assigned `tests/test-humanize-escape.sh` directly exercises the shared `.humanize` git-add detection used by this validator. I did not execute tests because this assignment requested research notes only and explicitly said not to modify files.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-054 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for two safety regressions around `.humanize` escape prevention and shell-safe directory discovery. It verifies that shared validator logic blocks attempts to add local loop state to git and that loop monitor/discovery patterns avoid zsh/bash glob failures on empty or dotfile-only directories.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, defines `pass`, `fail`, `assert_blocks`, and `assert_allows`, then runs eight grouped scenarios. The core predicate under test is `git_adds_humanize`, always called with lowercased command text through `to_lower` (`tests/test-humanize-escape.sh:16-19`, `44-70`). The test exits `0` only when `TESTS_FAILED` remains zero and exits `1` otherwise (`tests/test-humanize-escape.sh:337-352`).
- inputs_outputs_state: Inputs are hardcoded shell command strings and temporary filesystem fixtures under `/tmp`. Outputs are colored PASS/FAIL lines, pass/fail counters, and the final process exit code. Temporary state includes `/tmp/test-humanize-git-add-$$` containing `.humanize` for `git add -A` cases (`tests/test-humanize-escape.sh:128-145`) and several `/tmp/test-humanize-*` directories for find-pattern tests. It cleans up each temp directory before assertion completion.
- gates_or_invariants: Direct `.humanize` path variants must block: `./.humanize`, trailing slash, child paths, nested paths, and parent-relative paths (`tests/test-humanize-escape.sh:83-87`). Quoted `.humanize` variants must block (`tests/test-humanize-escape.sh:96-100`). Forced broad adds must block, including `-f .`, `--force .`, `-f *`, and combined flags like `-fA`/`-Af` (`tests/test-humanize-escape.sh:109-119`). `git add -A`/`--all` must block when `.humanize` exists (`tests/test-humanize-escape.sh:128-140`). Chained commands and `git -C` or `--git-dir` forms must still block (`tests/test-humanize-escape.sh:153-166`). Specific non-loop files, non-add git commands, patch mode, and lookalike names such as `.humanizeconfig` must pass (`tests/test-humanize-escape.sh:175-192`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `to_lower` and `git_adds_humanize`. The function under test handles command splitting, git-option forms, quote stripping, force/all/broad-scope flags, and `.humanize` existence/gitignore checks in `hooks/lib/loop-common.sh:834-930`. The suite is part of the aggregate runner at `tests/run-all-tests.sh:47`.
- edge_cases_or_failure_modes: The `.humanize` tests intentionally distinguish `.humanize` as a directory/path component from names that merely start with the same bytes (`tests/test-humanize-escape.sh:189-192`). The `-A`/`--all` tests depend on current working directory, so the test changes into a temp directory containing `.humanize` and then restores `ORIGINAL_DIR` (`tests/test-humanize-escape.sh:130-144`). The zsh safety checks model find-based iteration over empty dirs, dotfiles-only dirs, no `*-state.md` matches, one `*-state.md` match, and timestamp-like session directory selection (`tests/test-humanize-escape.sh:204-332`). These edge cases protect against shell glob expansion errors like “no matches found” by using `find ... 2>/dev/null` and read loops instead of raw globs.
- validation_or_tests: This file is itself validation. It maps directly to the shared behavior consumed by `hooks/loop-bash-validator.sh:115-118`. I inspected it but did not run it, because the worker instruction was research-only and “Do not modify files”; while the test mostly uses `/tmp`, it is still an executable test run rather than evidence extraction.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-084 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Block-message template that enforces RLCR goal-tracker ownership after Round 0. It is an algorithmic contract for state transition authority: after initial setup, non-Codex actors cannot directly mutate `goal-tracker.md`; they must request changes through the round summary.
- algorithmic_behavior: Renders a Markdown denial headed with `{{CURRENT_ROUND}}`, states that only Codex can modify the Goal Tracker after Round 0, forbids direct `Write`, `Edit`, or `Bash` changes to `goal-tracker.md`, and prescribes a structured “Goal Tracker Update Request” section in `{{SUMMARY_FILE}}` (`prompt-template/block/goal-tracker-modification.md:1-23`). The validator invokes this when `command_modifies_file` detects a Bash write to `goal-tracker.md` and `CURRENT_ROUND` is greater than zero (`hooks/loop-bash-validator.sh:349-357`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `SUMMARY_FILE`, supplied by `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:1004-1014`). Output is rendered Markdown sent to stderr by the Bash validator. It does not modify state directly; instead it redirects requested state changes into the current round summary, preserving Codex as the only goal-tracker writer after Round 0.
- gates_or_invariants: Invariant: `goal-tracker.md` cannot be directly modified after Round 0 by Write/Edit/Bash-style actions. In this assigned Bash path, Round 0 gets a different message that directs the user to Write/Edit for the correct file (`hooks/loop-bash-validator.sh:349-353`; fallback helper in `hooks/lib/loop-common.sh:398-405`), while Round > 0 gets this template and must use summary-mediated requests (`hooks/loop-bash-validator.sh:353-356`). The requested format requires both a bullet list of requested changes and a justification (`prompt-template/block/goal-tracker-modification.md:12-23`).
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh:1004-1014`; used by `hooks/loop-bash-validator.sh:349-357`. Template existence is cross-checked in `tests/test-template-references.sh:152-159`, and that script includes `loop-bash-validator.sh` and `loop-common.sh` in its scan set (`tests/test-template-references.sh:57-64`).
- edge_cases_or_failure_modes: If the template is missing or invalid, `load_and_render_safe` falls back to an inline denial in `hooks/lib/loop-common.sh:1007-1009`, so the gate remains functional though less detailed. Since this template is message-only, enforcement depends on callers correctly detecting `goal-tracker.md` modification attempts. The Bash validator uses lowercased command text and a regex file-modification detector, so the template does not itself resolve path aliases or command bypasses.
- validation_or_tests: `tests/test-template-references.sh:152-159` explicitly expects `block/goal-tracker-modification.md` to exist as a common template. Broader behavior is indirectly validated by Bash validator tests that exercise goal-tracker modification blocking. No tests were run in this research-only pass.
- skip_candidate: `no`

### IMPL_PR_LOOP-HZ-114 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop Codex prompt block that defines how Codex must reconcile review results into `goal-tracker.md`. It is part of the PR-loop review state update contract, not executable shell logic.
- algorithmic_behavior: Instructs Codex, after analysis, to update the goal tracker at `{{GOAL_TRACKER_FILE}}` (`prompt-template/pr-loop/codex-goal-tracker-update.md:1-4`). Required updates are: add one Issue Summary table row for `{{NEXT_ROUND}}`, update aggregate totals, and add an Issue Log entry for that round (`prompt-template/pr-loop/codex-goal-tracker-update.md:5-21`). It includes an example showing separate reviewer rows and aggregate counts (`prompt-template/pr-loop/codex-goal-tracker-update.md:22-55`) and restricts edits to specific sections (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`).
- inputs_outputs_state: Inputs are `GOAL_TRACKER_FILE` and `NEXT_ROUND`. `hooks/pr-loop-stop-hook.sh` sets `GOAL_TRACKER_FILE="$LOOP_DIR/goal-tracker.md"` and passes both variables to `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:1239-1248`). Output is rendered Markdown embedded into `round-${NEXT_ROUND}-codex-prompt.md`, immediately after the per-bot final recommendation contract (`hooks/pr-loop-stop-hook.sh:1250-1289`). Codex then executes with that prompt and writes its analysis to `round-${NEXT_ROUND}-pr-check.md` (`hooks/pr-loop-stop-hook.sh:1316-1320`), while the prompt authorizes/requests goal-tracker updates.
- gates_or_invariants: Each reviewer gets a separate Issue Summary row (`prompt-template/pr-loop/codex-goal-tracker-update.md:7-10`, `64`). Totals must obey `Remaining = Total Found - Total Resolved` (`prompt-template/pr-loop/codex-goal-tracker-update.md:12-16`). The Issue Log must include the round heading and details (`prompt-template/pr-loop/codex-goal-tracker-update.md:17-21`). Structural invariants: keep file structure intact, preserve PR Information and Ultimate Goal headers, update only Issue Summary, Total Statistics, and Issue Log, and append to existing tables rather than replacing them (`prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`).
- dependencies_and_callers: The direct caller is `hooks/pr-loop-stop-hook.sh:1239-1248`. It depends on the PR-loop stop hook’s bot review aggregation, expected-bot list, generated comment file, Codex prompt file, and Codex execution path (`hooks/pr-loop-stop-hook.sh:1223-1320`). The template works alongside PR-loop read-only protections: generated files such as `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md` are later protected from Bash modification by `hooks/loop-bash-validator.sh:417-442`.
- edge_cases_or_failure_modes: The template’s example uses the literal reviewer names and expected statuses but the actual issue counts depend on Codex’s preceding per-bot analysis. Incorrect arithmetic or table replacement would violate the prompt contract but is not enforced in this template itself. If the template cannot be loaded, the stop hook falls back to a shorter “Goal Tracker Update” instruction (`hooks/pr-loop-stop-hook.sh:1245-1248`), so the PR loop still prompts for tracker updates but loses detailed section-level constraints. If Codex is unavailable, fails, or produces empty output, the stop hook blocks with JSON decisions rather than completing the PR-loop transition (`hooks/pr-loop-stop-hook.sh:1291-1344`).
- validation_or_tests: Direct template existence was found through repository references; `hooks/pr-loop-stop-hook.sh:1248` renders it into the Codex prompt. The general template-reference test scans hook scripts for template references (`tests/test-template-references.sh:52-75`), though its common-template fixed list shown at `tests/test-template-references.sh:152-159` focuses on shared block templates. No tests were run in this research-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 unique assigned item sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`