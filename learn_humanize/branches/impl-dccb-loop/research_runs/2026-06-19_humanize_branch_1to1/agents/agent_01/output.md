# agent_01 impl-dccb-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `0fabd14f224c998e6dedd7cddaf57c479524700c`

## Item Evidence

### IMPL_DCCB_LOOP-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role:
  - The repository root is the complete Humanize Claude Code plugin export. It defines two iterative Claude/Codex loops: RLCR, a documentation-to-implementation loop, and DCCB, an implementation-to-documentation loop. The assigned branch is `impl-dccb-loop`, and the core algorithmic path for that branch is the DCCB lifecycle: slash command -> setup script -> loop state/tracker/prompt creation -> Claude documentation work -> Stop hook validation -> Codex reconstruction-readiness review -> complete, stop, or next-round feedback.
  - Manifest/config surfaces register the plugin and hooks: `.claude-plugin/plugin.json:1`, `.claude-plugin/marketplace.json:1`, `hooks/hooks.json:1`. User-facing behavior and conceptual goals are documented in `README.md:9`, `README.md:26`, `README.md:165`, and `commands/start-dccb-loop.md:8`.
- algorithmic_behavior:
  - DCCB starts through `commands/start-dccb-loop.md:12`, which invokes `scripts/setup-dccb-loop.sh` with optional `--max`, `--codex-model`, `--codex-timeout`, and `--output-dir`.
  - `scripts/setup-dccb-loop.sh` parses and validates arguments at `scripts/setup-dccb-loop.sh:107`, rejects positional arguments because DCCB analyzes the current codebase at `scripts/setup-dccb-loop.sh:164`, requires `codex` at `scripts/setup-dccb-loop.sh:176`, creates `.humanize-dccb.local/<timestamp>/` plus the output documentation directory at `scripts/setup-dccb-loop.sh:188`, writes YAML state at `scripts/setup-dccb-loop.sh:205`, writes the blueprint tracker template at `scripts/setup-dccb-loop.sh:223`, and writes the Round 0 prompt at `scripts/setup-dccb-loop.sh:303`.
  - The Round 0 DCCB prompt encodes the core distillation algorithm: quantitative codebase analysis, structural/module analysis, conceptual analysis, interface analysis, dynamic documentation structure proposal, self-contained conceptual documentation writing, tracker update, and summary writing. See `scripts/setup-dccb-loop.sh:333`, `scripts/setup-dccb-loop.sh:361`, `scripts/setup-dccb-loop.sh:404`, and `scripts/setup-dccb-loop.sh:449`.
  - Stop-time orchestration lives in `hooks/loop-dccb-stop-hook.sh`. It locates the newest active DCCB state directory at `hooks/loop-dccb-stop-hook.sh:36`, checks incomplete TodoWrite state before expensive review at `hooks/loop-dccb-stop-hook.sh:66`, blocks overlarge markdown docs over 1800 lines at `hooks/loop-dccb-stop-hook.sh:104`, parses state frontmatter at `hooks/loop-dccb-stop-hook.sh:165`, requires a round summary at `hooks/loop-dccb-stop-hook.sh:198`, requires documentation directory and at least one markdown file at `hooks/loop-dccb-stop-hook.sh:233` and `hooks/loop-dccb-stop-hook.sh:260`, requires Round 0 tracker initialization at `hooks/loop-dccb-stop-hook.sh:294`, checks max iterations at `hooks/loop-dccb-stop-hook.sh:353`, builds a regular or full review prompt at `hooks/loop-dccb-stop-hook.sh:381`, runs Codex with timeout at `hooks/loop-dccb-stop-hook.sh:721`, and branches on the review’s last non-empty line at `hooks/loop-dccb-stop-hook.sh:782`.
  - State transition rules are direct: `COMPLETE` removes `dccb-state.md` and ends the loop at `hooks/loop-dccb-stop-hook.sh:787`; `STOP` removes state as a circuit breaker at `hooks/loop-dccb-stop-hook.sh:797`; any other review content increments `current_round` and writes the next prompt at `hooks/loop-dccb-stop-hook.sh:825`.
  - Full DCCB reviews occur when `CURRENT_ROUND % 5 == 4`, per `hooks/loop-dccb-stop-hook.sh:375`. Those prompts include deeper structure, self-containment, completeness, de-specialization, consistency, reconstruction-test, gap-list, and stagnation checks at `hooks/loop-dccb-stop-hook.sh:413`, `hooks/loop-dccb-stop-hook.sh:435`, `hooks/loop-dccb-stop-hook.sh:465`, `hooks/loop-dccb-stop-hook.sh:489`, and `hooks/loop-dccb-stop-hook.sh:526`.
  - The monitor in `scripts/humanize.sh` is a runtime observability algorithm. DCCB-specific monitoring starts at `scripts/humanize.sh:520`, scans `.humanize-dccb.local`, maps project paths into `$HOME/.cache/humanize-dccb`, parses DCCB state/tracker at `scripts/humanize.sh:589` and `scripts/humanize.sh:604`, renders status at `scripts/humanize.sh:618`, and streams latest Codex logs at `scripts/humanize.sh:749`.
- inputs_outputs_state:
  - Inputs: DCCB slash command arguments (`commands/start-dccb-loop.md:3`), current project root from `CLAUDE_PROJECT_DIR` or `pwd` (`scripts/setup-dccb-loop.sh:188`, `hooks/loop-dccb-stop-hook.sh:36`), Claude Stop hook JSON from stdin (`hooks/loop-dccb-stop-hook.sh:30`), TodoWrite transcript path consumed by `hooks/check-todos-from-transcript.py:87`, and generated summary/tracker/doc markdown files.
  - Outputs: `.humanize-dccb.local/<timestamp>/dccb-state.md`, `dccb-tracker.md`, `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, plus documentation under `dccb-doc/` or the configured output directory. These surfaces are described in `README.md:429` and created by `scripts/setup-dccb-loop.sh:195`, `scripts/setup-dccb-loop.sh:198`, `scripts/setup-dccb-loop.sh:205`, `scripts/setup-dccb-loop.sh:223`, and `scripts/setup-dccb-loop.sh:303`.
  - Persistent state is file-based. `dccb-state.md` carries `current_round`, `max_iterations`, Codex model/effort/timeout, output dir, and start time (`scripts/setup-dccb-loop.sh:205`). The tracker records codebase metrics, proposed structure, reconstruction criteria, doc progress, coverage gaps, and review history (`scripts/setup-dccb-loop.sh:237`, `scripts/setup-dccb-loop.sh:248`, `scripts/setup-dccb-loop.sh:263`, `scripts/setup-dccb-loop.sh:280`, `scripts/setup-dccb-loop.sh:286`, `scripts/setup-dccb-loop.sh:291`).
  - Codex debug artifacts are written outside the project tree in `$HOME/.cache/humanize-dccb/<sanitized-project-path>/<timestamp>/`, with command/stdout/stderr files at `hooks/loop-dccb-stop-hook.sh:669`.
- gates_or_invariants:
  - DCCB documentation must be self-contained, de-specialized, reconstruction-oriented, proportional to codebase size, and not contain line-number or original file-path references. This invariant is stated in `README.md:36`, `commands/start-dccb-loop.md:81`, and encoded into setup/review prompts at `scripts/setup-dccb-loop.sh:410`, `scripts/setup-dccb-loop.sh:416`, and `hooks/loop-dccb-stop-hook.sh:604`.
  - Stop hook gates block exit when incomplete TodoWrite items exist (`hooks/loop-dccb-stop-hook.sh:72`), docs exceed 1800 lines (`hooks/loop-dccb-stop-hook.sh:117`), summary is missing (`hooks/loop-dccb-stop-hook.sh:200`), output directory is missing (`hooks/loop-dccb-stop-hook.sh:235`), no markdown docs exist (`hooks/loop-dccb-stop-hook.sh:263`), or Round 0 tracker placeholders remain (`hooks/loop-dccb-stop-hook.sh:301`).
  - Completion is strict on the final review line: only an exact last non-empty `COMPLETE` ends successfully (`hooks/loop-dccb-stop-hook.sh:782`). `STOP` is a circuit breaker, not success (`hooks/loop-dccb-stop-hook.sh:797`).
  - `hooks/hooks.json:42` registers both RLCR and DCCB Stop hooks. This means both hooks run on Stop, but each exits if its own loop state is absent.
  - Important asymmetry: pre-tool validators are RLCR-specific. `hooks/lib/loop-common.sh:172` only recognizes `.humanize-rlcr.local/`; write/edit/read/bash validators refer to RLCR state and tracker paths at `hooks/loop-write-validator.sh:78`, `hooks/loop-edit-validator.sh:58`, `hooks/loop-read-validator.sh:67`, and `hooks/loop-bash-validator.sh:36`. DCCB therefore has robust Stop-hook gates but does not appear to have matching pre-tool write protections for `.humanize-dccb.local` prompt/state/tracker files.
- dependencies_and_callers:
  - Claude Code calls command markdown entrypoints under `commands/`; those entrypoints call setup scripts under `scripts/`.
  - Claude Code hook registration in `hooks/hooks.json` calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/loop-dccb-stop-hook.sh`.
  - `hooks/loop-dccb-stop-hook.sh` depends on `jq`, `python3`, `codex`, `find`, `grep`, `sed`, `wc`, cache directory creation, and `scripts/portable-timeout.sh` via `PLUGIN_ROOT/scripts/portable-timeout.sh` at `hooks/loop-dccb-stop-hook.sh:679`.
  - `hooks/check-todos-from-transcript.py` accepts Stop hook JSON, reads `transcript_path`, scans JSONL entries, and extracts the latest TodoWrite todos across three transcript shapes at `hooks/check-todos-from-transcript.py:17`, `hooks/check-todos-from-transcript.py:48`, `hooks/check-todos-from-transcript.py:62`, and `hooks/check-todos-from-transcript.py:75`.
  - `scripts/portable-timeout.sh` abstracts macOS/Linux timeout behavior by preferring `gtimeout`, GNU `timeout`, Python, then no timeout at `scripts/portable-timeout.sh:10` and `scripts/portable-timeout.sh:33`.
  - RLCR is a sibling algorithm, not the branch focus, but shares the same hook registration and utility patterns. Its setup/stop lifecycle is in `scripts/setup-rlcr-loop.sh:223` and `hooks/loop-codex-stop-hook.sh:50`.
- edge_cases_or_failure_modes:
  - This branch export is not a Git working tree in the sandbox; `git rev-parse` failed with “not a git repository.” That does not block directory research, but it means source commit/tree metadata was treated as scheduler-provided.
  - If `codex` is not installed, DCCB setup fails immediately (`scripts/setup-dccb-loop.sh:176`). If Codex review does not create the review result file, the Stop hook copies stdout if available, otherwise blocks with debug paths (`hooks/loop-dccb-stop-hook.sh:729`, `hooks/loop-dccb-stop-hook.sh:741`).
  - Corrupt non-numeric `current_round` removes the state file and stops the loop (`hooks/loop-dccb-stop-hook.sh:183`). Invalid `max_iterations` falls back to 42 (`hooks/loop-dccb-stop-hook.sh:190`).
  - `--max 0` is accepted by the numeric regex in setup, then the first Stop check sees `NEXT_ROUND=1 > 0` and ends without review (`scripts/setup-dccb-loop.sh:117`, `hooks/loop-dccb-stop-hook.sh:355`).
  - Active-loop selection differs: DCCB stop hook uses `ls -1dt` newest state-bearing directory (`hooks/loop-dccb-stop-hook.sh:47`), while shared RLCR helper only checks newest timestamp and ignores older active loops (`hooks/lib/loop-common.sh:23`). This is not directly wrong for DCCB because it does not call `find_active_loop`, but it is a sibling behavior difference.
  - The DCCB monitor’s tracker parser expects checkbox list lines (`scripts/humanize.sh:610`), while the setup tracker template uses markdown tables (`scripts/setup-dccb-loop.sh:280`). Unless Claude rewrites tracker progress into checklist form, monitor doc counts may show `0`.
  - Pre-tool validators do not protect DCCB state/prompt files, so a Claude session could potentially edit `dccb-state.md` or `round-N-prompt.md` directly; the setup prompt instructs not to do so (`scripts/setup-dccb-loop.sh:478`), but enforcement appears absent outside the Stop hook.
- validation_or_tests:
  - No dedicated automated test suite is present in the export. Validation is implemented as runtime gates and Codex review prompts.
  - Recursive file coverage completed across all files reported by `rg --files -uu`: README, plugin manifests, four command docs, eight hook/helper files, and four scripts. Line count total was 5,479 lines across inspected files.
  - Stop-hook validation behavior is concrete and enforced through shell exit/block JSON decisions: incomplete todos, missing summary/docs, oversized docs, uninitialized tracker, Codex failure, max iterations, `COMPLETE`, and `STOP`.
  - Research command validation was read-only. No files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `IMPL_DCCB_LOOP-HZ-001` appears as one item evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`