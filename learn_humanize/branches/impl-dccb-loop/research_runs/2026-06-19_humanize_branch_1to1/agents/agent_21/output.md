# agent_21 impl-dccb-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `0fabd14f224c998e6dedd7cddaf57c479524700c`

## Item Evidence

### IMPL_DCCB_LOOP-HZ-021 `file` `scripts/setup-dccb-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script for the DCCB loop, where DCCB means “Distill Code to Conceptual Blueprint.” It initializes a timestamped loop session, writes the first state/tracker/prompt artifacts, creates the requested documentation output directory, and emits the first Claude-facing work prompt. It is not the iterative reviewer itself; the actual stop-time review and round advancement are handled by `hooks/loop-dccb-stop-hook.sh`.

- algorithmic_behavior: The script is a linear bootstrap algorithm with strict shell failure mode enabled by `set -euo pipefail` at `scripts/setup-dccb-loop.sh:13`. It starts from default configuration at `scripts/setup-dccb-loop.sh:19` through `scripts/setup-dccb-loop.sh:24`, then parses options in a `while`/`case` loop at `scripts/setup-dccb-loop.sh:107` through `scripts/setup-dccb-loop.sh:170`. Supported inputs are `--max`, `--codex-model`, `--codex-timeout`, `--output-dir`, and help. After parsing, it gates on `codex` availability at `scripts/setup-dccb-loop.sh:176` through `scripts/setup-dccb-loop.sh:182`. It then resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, builds `.humanize-dccb.local/<timestamp>/`, and creates both that state directory and the user-facing docs output directory at `scripts/setup-dccb-loop.sh:188` through `scripts/setup-dccb-loop.sh:199`.

  The artifact generation sequence is fixed. First, it writes `dccb-state.md` with YAML frontmatter containing `current_round: 0`, max iteration count, Codex model/effort/timeout, output directory, and UTC start time at `scripts/setup-dccb-loop.sh:205` through `scripts/setup-dccb-loop.sh:215`. Second, it writes `dccb-tracker.md` at `scripts/setup-dccb-loop.sh:221` through `scripts/setup-dccb-loop.sh:295`; this tracker establishes round-zero analysis placeholders, reconstruction criteria, documentation progress, coverage gaps, and review feedback history. Third, it writes `round-0-prompt.md` at `scripts/setup-dccb-loop.sh:301` through `scripts/setup-dccb-loop.sh:479`; this prompt instructs Claude to analyze the current codebase, choose a proportional documentation structure, write conceptual reconstruction documentation, update the tracker, and write a round-zero summary. Finally, it prints activation metadata and replays the initial prompt to stdout at `scripts/setup-dccb-loop.sh:485` through `scripts/setup-dccb-loop.sh:541`.

- inputs_outputs_state: Primary inputs are command-line arguments, `codex` presence in `PATH`, `CLAUDE_PROJECT_DIR` if set, current working directory otherwise, wall-clock timestamp from `date`, and the project tree that the generated prompt instructs Claude to analyze. The `--codex-model` argument accepts either `MODEL:EFFORT` or just `MODEL`; the split is implemented with shell parameter expansion at `scripts/setup-dccb-loop.sh:129` through `scripts/setup-dccb-loop.sh:136`. The script outputs a session directory under `.humanize-dccb.local`, a documentation output directory such as `dccb-doc/`, a state file, a tracker file, and the first prompt/summary path contract. The important state transition is from “no DCCB session for this timestamp” to “active DCCB session,” where activity is represented by the presence of `.humanize-dccb.local/<timestamp>/dccb-state.md`. The downstream hook discovers the newest active loop by scanning timestamped directories for that file in `hooks/loop-dccb-stop-hook.sh:43` through `hooks/loop-dccb-stop-hook.sh:58`.

  The state file initialized here is later parsed by the stop hook for `current_round`, `max_iterations`, Codex settings, timeout, and output directory in `hooks/loop-dccb-stop-hook.sh:165` through `hooks/loop-dccb-stop-hook.sh:181`. On incomplete review, that hook increments `current_round` by rewriting the state file in `hooks/loop-dccb-stop-hook.sh:825` through `hooks/loop-dccb-stop-hook.sh:828`. On completion, cancellation, stop, or max-iteration exit, the state file is removed by consumers rather than by this setup script. The cancel command’s documented state-removal contract is in `commands/cancel-dccb-loop.md:19` through `commands/cancel-dccb-loop.md:22`.

- gates_or_invariants: The setup gate requires syntactically valid options and a visible Codex CLI. Numeric validation for `--max` and `--codex-timeout` uses `^[0-9]+$` at `scripts/setup-dccb-loop.sh:117` and `scripts/setup-dccb-loop.sh:144`; despite error messages saying “positive,” zero is accepted by the regex. The script rejects positional arguments at `scripts/setup-dccb-loop.sh:164` through `scripts/setup-dccb-loop.sh:168`. It also rejects missing option values for each option branch. The tracker/prompt invariants are documentation-specific: the generated tracker says the documentation structure is dynamic and locked after round zero at `scripts/setup-dccb-loop.sh:230` through `scripts/setup-dccb-loop.sh:234`, and the prompt requires proportional structure, self-contained conceptual documentation, no code snippets, no original file path references, and files under 1800 lines at `scripts/setup-dccb-loop.sh:318` through `scripts/setup-dccb-loop.sh:329` and `scripts/setup-dccb-loop.sh:408` through `scripts/setup-dccb-loop.sh:445`.

  Important gates are intentionally deferred to the stop hook. The hook blocks exit if transcript todos remain incomplete in `hooks/loop-dccb-stop-hook.sh:62` through `hooks/loop-dccb-stop-hook.sh:98`; blocks markdown docs over 1800 lines in `hooks/loop-dccb-stop-hook.sh:100` through `hooks/loop-dccb-stop-hook.sh:155`; requires the current round summary in `hooks/loop-dccb-stop-hook.sh:194` through `hooks/loop-dccb-stop-hook.sh:227`; requires a documentation directory and at least one markdown file in `hooks/loop-dccb-stop-hook.sh:229` through `hooks/loop-dccb-stop-hook.sh:286`; and checks that round-zero tracker placeholders have been replaced in `hooks/loop-dccb-stop-hook.sh:288` through `hooks/loop-dccb-stop-hook.sh:347`. This means setup establishes the state and prompt contract, while enforcement happens when Claude attempts to stop.

- dependencies_and_callers: The slash command entry point is `commands/start-dccb-loop.md`, which allows and invokes only `${CLAUDE_PLUGIN_ROOT}/scripts/setup-dccb-loop.sh` at `commands/start-dccb-loop.md:1` through `commands/start-dccb-loop.md:14`. Stop hook registration is in `hooks/hooks.json:42` through `hooks/hooks.json:54`, where `hooks/loop-dccb-stop-hook.sh` is registered alongside the RLCR stop hook. The generated state is monitored by `scripts/humanize.sh`; the DCCB monitor searches `.humanize-dccb.local`, parses `dccb-state.md`, parses `dccb-tracker.md`, and displays round/model/output status in `scripts/humanize.sh:520` through `scripts/humanize.sh:689`. Repository documentation describes the same lifecycle and artifact layout in `README.md:177` through `README.md:207` and `README.md:431` through `README.md:449`.

  Runtime dependencies directly used by this setup script are Bash, `codex`, `date`, `mkdir`, `cat`, `pwd`, and basic shell utilities. Downstream dependencies include `jq`, `python3`, `find`, `wc`, `sed`, `grep`, and Codex execution from the stop hook. The stop hook optionally sources `scripts/portable-timeout.sh` and otherwise falls back to `timeout`/`gtimeout` if available at `hooks/loop-dccb-stop-hook.sh:679` through `hooks/loop-dccb-stop-hook.sh:695`. The setup script’s generated prompt uses Claude-specific file mention syntax such as `@$BLUEPRINT_TRACKER_FILE` and `@$SUMMARY_PATH`, so the bootstrap is coupled to the Claude command/hook environment even though Codex reviews the output.

- edge_cases_or_failure_modes: Multiple active sessions can coexist because setup never checks for an existing `.humanize-dccb.local/*/dccb-state.md`; the stop hook chooses the newest active state file. Timestamp precision is one second at `scripts/setup-dccb-loop.sh:192`, so repeated launches within the same second can reuse and overwrite the same session files. `--max 0` and `--codex-timeout 0` pass validation even though the messages claim positive integers. `--codex-model :high` or `--codex-model model:` can produce an empty model or empty effort because the split only checks for the presence of a colon. `--output-dir` is joined directly under `PROJECT_ROOT` at `scripts/setup-dccb-loop.sh:198`; values containing traversal or absolute-path-like text are not normalized or constrained by this script. If `CLAUDE_PROJECT_DIR` is wrong, all session and output artifacts are created relative to that environment value instead of the actual shell directory. The script requires `codex` before any state is created, so environments that want only to prepare DCCB artifacts without immediate review support cannot bootstrap.

  The prompt prohibits lying, editing loop state, or invoking cancellation to escape the loop at `scripts/setup-dccb-loop.sh:478`, but this is behavioral instruction rather than a setup-time technical guard. The actual state file can still be removed by the documented cancel command. Another important boundary is that setup creates the output directory immediately at `scripts/setup-dccb-loop.sh:197` through `scripts/setup-dccb-loop.sh:199`, so the downstream “documentation directory missing” stop-hook gate only catches missing or removed directories, not a round-zero run that created an empty docs directory; empty markdown content is caught later by the markdown-file count gate.

- validation_or_tests: No dedicated automated tests for `scripts/setup-dccb-loop.sh` were found in the branch export. I inspected the direct command wrapper, cancel wrapper, hook registration, DCCB stop hook, monitor code, and README references to validate how the setup artifacts are consumed. The script’s own validation is limited to option parsing and Codex availability. The stronger runtime validation belongs to `hooks/loop-dccb-stop-hook.sh`: state parsing and numeric recovery, summary enforcement, doc existence checks, tracker initialization checks, full review cadence every fifth round, Codex execution with timeout/debug logs, and completion detection from the last non-empty review line. I also attempted repository metadata checks, but this read-only branch export does not appear to include a usable `.git` directory, and `git status` failed with “not a git repository”; source commit identity is therefore taken from the scheduler-provided run metadata.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence section`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`