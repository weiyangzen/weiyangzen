# agent_15 impl-dccb-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `0fabd14f224c998e6dedd7cddaf57c479524700c`

## Item Evidence

### IMPL_DCCB_LOOP-HZ-015 `file` `hooks/loop-dccb-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook controller for the DCCB loop, the code-to-documentation counterpart of the RLCR review loop. It intercepts Claude Code Stop events, checks whether the DCCB session is allowed to end, invokes Codex as a reconstruction-readiness reviewer, and either terminates loop state or blocks Stop with the next-round prompt. It is registered as a Stop hook in `hooks/hooks.json:42-54`.
- algorithmic_behavior: The hook reads hook JSON from stdin into `HOOK_INPUT` at `hooks/loop-dccb-stop-hook.sh:30`, resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` at `hooks/loop-dccb-stop-hook.sh:36`, then selects the newest `.humanize-dccb.local/*/` directory containing `dccb-state.md` at `hooks/loop-dccb-stop-hook.sh:43-58`. If no active loop exists, it exits successfully and does not interfere. For an active loop, it runs layered gates in order: incomplete TodoWrite detection via `check-todos-from-transcript.py` at `hooks/loop-dccb-stop-hook.sh:66-98`; markdown file line-limit enforcement at `hooks/loop-dccb-stop-hook.sh:104-155`; frontmatter parsing and defaults at `hooks/loop-dccb-stop-hook.sh:165-181`; current-round numeric validation at `hooks/loop-dccb-stop-hook.sh:183-188`; required summary file check at `hooks/loop-dccb-stop-hook.sh:198-227`; documentation directory and markdown presence checks at `hooks/loop-dccb-stop-hook.sh:233-286`; Round 0 tracker placeholder detection at `hooks/loop-dccb-stop-hook.sh:292-347`; max-iteration stop at `hooks/loop-dccb-stop-hook.sh:353-359`. If gates pass, it builds either a regular review prompt or a full reconstruction-readiness review prompt every fifth round, specifically rounds where `CURRENT_ROUND % 5 == 4`, at `hooks/loop-dccb-stop-hook.sh:375-383`. It then runs `codex exec`, captures stdout/stderr/debug files, reads the review result, and branches on the last non-empty line: exact `COMPLETE` removes `dccb-state.md` and allows Stop; exact `STOP` removes state as a circuit breaker; anything else increments `current_round`, writes the next prompt, and returns a JSON `decision: block` response to keep Claude in the loop.
- inputs_outputs_state: Inputs are hook stdin JSON, especially `transcript_path` consumed by `hooks/check-todos-from-transcript.py:87-103`; environment variables `CLAUDE_PROJECT_DIR`, `CODEX_TIMEOUT`, and the user shell path; DCCB state frontmatter keys `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, and `output_dir` parsed at `hooks/loop-dccb-stop-hook.sh:166-181`; the current round summary file `round-N-summary.md`; generated documentation under `$PROJECT_ROOT/$OUTPUT_DIR`; and `dccb-tracker.md`. Outputs are Claude hook JSON blocks with `decision: block`, `reason`, and `systemMessage` on blocked paths, for example `hooks/loop-dccb-stop-hook.sh:88-96`, `hooks/loop-dccb-stop-hook.sh:218-226`, and `hooks/loop-dccb-stop-hook.sh:917-925`; stderr diagnostics around Codex execution at `hooks/loop-dccb-stop-hook.sh:665-727`; review prompt/result files under the loop directory at `hooks/loop-dccb-stop-hook.sh:365-367`; Codex command/stdout/stderr debug artifacts under `$HOME/.cache/humanize-dccb/...` at `hooks/loop-dccb-stop-hook.sh:669-678`; and state mutations by removing `dccb-state.md` on completion, stop, corrupted current round, or max-iteration exhaustion at `hooks/loop-dccb-stop-hook.sh:186-187`, `hooks/loop-dccb-stop-hook.sh:357-358`, `hooks/loop-dccb-stop-hook.sh:793-794`, and `hooks/loop-dccb-stop-hook.sh:817-818`. The primary continuing transition updates `current_round` with `sed` into a temp file and `mv` at `hooks/loop-dccb-stop-hook.sh:825-828`, then writes `round-N-prompt.md` for the next Claude iteration at `hooks/loop-dccb-stop-hook.sh:837-911`.
- gates_or_invariants: Active-loop detection is presence-based: a session is active only when a newest `.humanize-dccb.local/<timestamp>/dccb-state.md` exists. Claude cannot stop with incomplete native todos: the checker reports any latest TodoWrite item whose status is not `completed` at `hooks/check-todos-from-transcript.py:109-122`, and the hook blocks at `hooks/loop-dccb-stop-hook.sh:72-97`. Documentation markdown files must stay at or below 1800 lines, enforced by `MAX_LINES=1800` and `find "$DOC_DIR" -name "*.md"` at `hooks/loop-dccb-stop-hook.sh:104-123`. A round summary must exist before review, but the hook does not require it to be nonempty. Output documentation must have at least one markdown file at `hooks/loop-dccb-stop-hook.sh:260-286`. Round 0 requires tracker initialization only when `dccb-tracker.md` exists; it blocks if key `[pending]` placeholders remain at `hooks/loop-dccb-stop-hook.sh:294-346`. Codex completion is intentionally conservative: prompt text tells Codex to emit `COMPLETE` only as the final line when documentation is self-contained, complete, de-specialized, consistent, and reconstruction-ready; the shell implementation requires the final non-empty line after trimming to be exactly `COMPLETE` at `hooks/loop-dccb-stop-hook.sh:782-794`. Full-review prompts add explicit stagnation detection and allow a final `STOP` circuit breaker at `hooks/loop-dccb-stop-hook.sh:526-558` and `hooks/loop-dccb-stop-hook.sh:797-819`.
- dependencies_and_callers: Called by Claude Code’s Stop hook chain through `hooks/hooks.json:42-54`; initialized by `/humanize:start-dccb-loop`, whose command delegates to `scripts/setup-dccb-loop.sh` from `commands/start-dccb-loop.md:8-13`. The setup script creates the DCCB loop directory, documentation output directory, state frontmatter, tracker, and initial prompt at `scripts/setup-dccb-loop.sh:188-215`, `scripts/setup-dccb-loop.sh:221-295`, and `scripts/setup-dccb-loop.sh:301-340`. The hook sources `hooks/lib/loop-common.sh` at `hooks/loop-dccb-stop-hook.sh:39-41`, although the shared file is mostly RLCR-oriented and this script performs its own DCCB loop discovery. It invokes `hooks/check-todos-from-transcript.py` at `hooks/loop-dccb-stop-hook.sh:66-70`; that helper parses Claude transcript JSONL formats and extracts the most recent TodoWrite list at `hooks/check-todos-from-transcript.py:17-84`. It uses `scripts/portable-timeout.sh` when available at `hooks/loop-dccb-stop-hook.sh:679-683`, whose `run_with_timeout` chooses `gtimeout`, GNU `timeout`, Python subprocess timeout, or no timeout at `scripts/portable-timeout.sh:10-71`. External runtime dependencies are `bash`, `jq`, `python3`, `codex`, core Unix tools (`find`, `wc`, `grep`, `sed`, `tail`, `sort`, `mkdir`, `cp`, `mv`), and writable loop/cache paths. README lines `README.md:165-275` describe the user-level DCCB lifecycle and reconstruction criteria that this hook enforces.
- edge_cases_or_failure_modes: If `hooks/lib/loop-common.sh` is missing, the script exits because `set -euo pipefail` is active and `source` is unconditional at `hooks/loop-dccb-stop-hook.sh:16` and `hooks/loop-dccb-stop-hook.sh:41`. The active-loop scan uses `for dir in $(ls -1dt ...)` at `hooks/loop-dccb-stop-hook.sh:47`, so paths containing whitespace would split incorrectly. The todo checker only blocks on exit code `1`; parser errors or unexpected helper failures do not block Codex review because only `TODO_EXIT == 1` is handled at `hooks/loop-dccb-stop-hook.sh:72`. `current_round` corruption deletes the state file and silently ends the loop after a warning at `hooks/loop-dccb-stop-hook.sh:183-188`; invalid `max_iterations` is reset to 42, but `codex_timeout` is not numeric-validated before `run_with_timeout`. If `dccb-tracker.md` is missing in Round 0, the placeholder gate is skipped because the condition requires the file to exist at `hooks/loop-dccb-stop-hook.sh:294`, yet later prompts still reference it. If Codex exits nonzero but writes or stdout-produces a review result file, the hook ignores the nonzero exit code and trusts the review content; the nonzero path only blocks when no result file exists at `hooks/loop-dccb-stop-hook.sh:729-777`. Passing the full prompt as a single CLI argument to `codex exec` at `hooks/loop-dccb-stop-hook.sh:721-723` may hit shell or OS argument-size limits for very large documentation trees or summaries. Large-file scanning only checks markdown under the configured output directory, not the tracker or round artifacts. Max-iteration exhaustion removes state and permits exit even if reconstruction-readiness was never confirmed at `hooks/loop-dccb-stop-hook.sh:355-358`.
- validation_or_tests: No dedicated automated tests for this hook were found in the inspected repository surface. Its built-in validation behavior is runtime gating plus Codex review, not a separate test suite. I did not execute the hook because it writes loop state/cache files and may invoke Codex; this branch export is read-only and the task requested research notes only. Static validation was performed by direct line-number inspection of `hooks/loop-dccb-stop-hook.sh`, caller registration in `hooks/hooks.json`, setup behavior in `scripts/setup-dccb-loop.sh`, todo parsing in `hooks/check-todos-from-transcript.py`, and timeout behavior in `scripts/portable-timeout.sh`.
- skip_candidate: `no`; despite the inclusion reason using RLCR wording, this file is a core DCCB loop state-machine hook for the `impl-dccb-loop` branch.

## Worker Self-Test
- assigned_items_seen: 1 of 1; the assigned item id appears only in its Item Evidence heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`