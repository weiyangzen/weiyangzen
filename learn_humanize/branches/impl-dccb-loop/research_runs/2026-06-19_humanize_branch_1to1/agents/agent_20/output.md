# agent_20 impl-dccb-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `0fabd14f224c998e6dedd7cddaf57c479524700c`

## Item Evidence

### IMPL_DCCB_LOOP-HZ-020 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Provides a Bash-sourced portability layer for running subprocesses with a timeout across macOS and Linux. It is not a standalone scheduler or monitor, but it is part of the loop runtime because stop hooks use it to bound Codex review execution. The file advertises its use as `source portable-timeout.sh; run_with_timeout <seconds> <command> [args...]` in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:4).

- algorithmic_behavior: The script first detects the timeout backend once at source time through `detect_timeout_impl` in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:10). Detection priority is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none`, matching the comment in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:6). GNU `timeout` is accepted only if `timeout --version` succeeds, preventing accidental use of a non-GNU implementation in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:13). `run_with_timeout` then consumes the first argument as timeout seconds, shifts it away, stores the remaining command as an array, and dispatches by `TIMEOUT_IMPL` in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:33). Native implementations invoke `gtimeout "$timeout_secs" "${cmd[@]}"` or `timeout "$timeout_secs" "${cmd[@]}"` in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:39). The Python fallback uses `subprocess.run(sys.argv[1:], timeout=$timeout_secs)` and exits with the child return code, or `124` on `TimeoutExpired`, in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:47).

- inputs_outputs_state: Inputs are the sourced shell environment, the available commands on `PATH`, and the `run_with_timeout` arguments: timeout seconds plus command vector. The script emits no normal stdout except the backend name internally captured into `TIMEOUT_IMPL`; it emits a warning to stderr only when no timeout implementation exists, from [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:64). Runtime output and exit status are otherwise those of the wrapped command, except Python timeout maps to exit `124`, matching GNU timeout behavior in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:56). Persistent state is limited to the shell variable `TIMEOUT_IMPL`, assigned at source/load time in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:29) and exported for sourcing scripts in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:76). There are no file writes, locks, ledgers, checkpoints, or checklist cursor updates in this file.

- gates_or_invariants: The main invariant is backend selection order: Homebrew `gtimeout` wins on macOS, GNU `timeout` wins on Linux, Python is fallback, and `none` preserves operability without enforcement. The command vector is stored as a Bash array before dispatch, preserving argument boundaries for native timeout calls in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/portable-timeout.sh:36). The Python implementation preserves the wrapped process return code and standardizes timeout failure to `124`, so callers can treat Python fallback similarly to GNU timeout. There is no validation inside this file that timeout seconds are numeric or positive; that gate is performed by setup scripts when accepting `--codex-timeout`, for example [scripts/setup-dccb-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/setup-dccb-loop.sh:139) and [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/setup-rlcr-loop.sh:125). The stop hooks read the configured timeout from loop state with defaults in [hooks/loop-dccb-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/hooks/loop-dccb-stop-hook.sh:172) and [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/hooks/loop-codex-stop-hook.sh:333).

- dependencies_and_callers: Direct dependencies are Bash, `command -v`, optional `gtimeout`, optional GNU `timeout`, optional `python3` or `python`, and Python `subprocess`/`sys` for fallback. The script is sourced by the RLCR stop hook at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/hooks/loop-codex-stop-hook.sh:704) and by the DCCB stop hook at [hooks/loop-dccb-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/hooks/loop-dccb-stop-hook.sh:679). If the script is missing, both hooks define a smaller inline fallback `run_with_timeout`; this means the helper is preferred but not a hard startup dependency. Actual runtime use wraps `codex exec` with `CODEX_TIMEOUT` and redirects stdout/stderr to cache files in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/hooks/loop-codex-stop-hook.sh:750) and [hooks/loop-dccb-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/hooks/loop-dccb-stop-hook.sh:722). README lists it as part of the `scripts/` setup utilities in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/README.md:400).

- edge_cases_or_failure_modes: If no backend exists, timeout enforcement silently degrades to “run without timeout” after a stderr warning, so a hung `codex exec` can hang the loop despite configuration. If `run_with_timeout` is called without a timeout or command, there is no local argument validation; behavior falls through to the selected backend or Python error handling. If the caller mutates `PATH` after sourcing, backend selection does not refresh because `TIMEOUT_IMPL` is computed once. If `TIMEOUT_IMPL=python3|python`, `$timeout_secs` is interpolated directly into the Python snippet; the setup scripts validate CLI-provided timeout values, but direct callers could pass malformed values and cause Python syntax/runtime failure. Native timeout backends may terminate process groups differently than Python `subprocess.run(timeout=...)`; the Python fallback kills only the direct child process managed by `subprocess.run`, which can leave grandchildren depending on the wrapped command’s behavior. The Bash array command preservation is good for native calls, but the Python backend passes arguments through `sys.argv[1:]`, so shell features such as pipelines still require an explicit shell wrapper by the caller.

- validation_or_tests: No dedicated test, spec, Bats, shellcheck config, or unit harness was found in the repository for this script. Behavioral validation is indirect through the two stop hooks: setup writes `codex_timeout` into state in [scripts/setup-dccb-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/setup-dccb-loop.sh:205) and [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/impl-dccb-loop/scripts/setup-rlcr-loop.sh:223), hooks parse that value, then call `run_with_timeout` around `codex exec`. The script file itself is executable Bash/ASCII and present at 2116 bytes.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 / 1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`