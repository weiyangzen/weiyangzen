# agent_04 impl-dccb-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `0fabd14f224c998e6dedd7cddaf57c479524700c`

## Item Evidence

### IMPL_DCCB_LOOP-HZ-004 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts` is a flat four-file directory, not a nested tree: `scripts/setup-dccb-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/humanize.sh`, and `scripts/portable-timeout.sh`. Its core role is to initialize and observe the Humanize feedback loops. In this branch, the DCCB-relevant core algorithm is the code-to-blueprint loop initializer in `scripts/setup-dccb-loop.sh`, with `scripts/humanize.sh` providing live monitoring, `scripts/portable-timeout.sh` providing bounded Codex execution support for sibling hooks, and `scripts/setup-rlcr-loop.sh` preserving the older reverse loop pattern for documentation-to-implementation.
- algorithmic_behavior: `scripts/setup-dccb-loop.sh` parses `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--output-dir`, rejects positional arguments, requires `codex`, creates `.humanize-dccb.local/<timestamp>/`, creates the requested documentation output directory, writes `dccb-state.md`, seeds `dccb-tracker.md`, writes `round-0-prompt.md`, and prints that initial prompt to the caller (`scripts/setup-dccb-loop.sh:107`, `scripts/setup-dccb-loop.sh:176`, `scripts/setup-dccb-loop.sh:188`, `scripts/setup-dccb-loop.sh:205`, `scripts/setup-dccb-loop.sh:221`, `scripts/setup-dccb-loop.sh:303`, `scripts/setup-dccb-loop.sh:511`). The DCCB prompt algorithm forces a staged workflow: analyze codebase, choose a proportional documentation structure, write self-contained reconstruction docs, update tracker state, and write a round summary (`scripts/setup-dccb-loop.sh:333`, `scripts/setup-dccb-loop.sh:361`, `scripts/setup-dccb-loop.sh:404`, `scripts/setup-dccb-loop.sh:449`, `scripts/setup-dccb-loop.sh:458`). `scripts/setup-rlcr-loop.sh` is the paired setup algorithm for implementation work from a plan file: parse options, validate a plan path and minimum line count, require `codex`, create `.humanize-rlcr.local/<timestamp>/`, write state/tracker files, derive initial goal/acceptance hints from headings when possible, and emit a round-0 implementation prompt (`scripts/setup-rlcr-loop.sh:93`, `scripts/setup-rlcr-loop.sh:163`, `scripts/setup-rlcr-loop.sh:185`, `scripts/setup-rlcr-loop.sh:195`, `scripts/setup-rlcr-loop.sh:207`, `scripts/setup-rlcr-loop.sh:223`, `scripts/setup-rlcr-loop.sh:240`, `scripts/setup-rlcr-loop.sh:336`). `scripts/humanize.sh` defines `humanize monitor rlcr-loop` and `humanize monitor dccb-loop`; each monitor locates the newest loop session, locates the newest cached Codex log under `$HOME/.cache/humanize*`, parses state/tracker/git status, draws a fixed terminal status bar with `tput`, tails the latest log, and switches when a newer round log appears (`scripts/humanize.sh:9`, `scripts/humanize.sh:43`, `scripts/humanize.sh:98`, `scripts/humanize.sh:116`, `scripts/humanize.sh:200`, `scripts/humanize.sh:262`, `scripts/humanize.sh:456`, `scripts/humanize.sh:522`, `scripts/humanize.sh:550`, `scripts/humanize.sh:589`, `scripts/humanize.sh:604`, `scripts/humanize.sh:618`, `scripts/humanize.sh:789`). `scripts/portable-timeout.sh` selects `gtimeout`, GNU `timeout`, `python3`, `python`, or no timeout, then exposes `run_with_timeout <seconds> <command...>` and preserves the wrapped command exit code, using 124 for Python timeout expiry (`scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:29`, `scripts/portable-timeout.sh:33`, `scripts/portable-timeout.sh:38`, `scripts/portable-timeout.sh:47`, `scripts/portable-timeout.sh:64`).
- inputs_outputs_state: Inputs are slash-command arguments, environment variables such as `CLAUDE_PROJECT_DIR` and `HOME`, the current working tree, the `codex` executable, and for RLCR a markdown plan file. DCCB outputs `.humanize-dccb.local/<timestamp>/dccb-state.md`, `dccb-tracker.md`, `round-0-prompt.md`, `round-N-summary.md` targets, `round-N-review-*` files consumed later by hooks, and an in-project documentation directory defaulting to `dccb-doc/` (`scripts/setup-dccb-loop.sh:188`, `scripts/setup-dccb-loop.sh:197`, `scripts/setup-dccb-loop.sh:205`, `scripts/setup-dccb-loop.sh:221`, `scripts/setup-dccb-loop.sh:301`). RLCR outputs the analogous `.humanize-rlcr.local/<timestamp>/` state and prompt files (`scripts/setup-rlcr-loop.sh:207`, `scripts/setup-rlcr-loop.sh:223`, `scripts/setup-rlcr-loop.sh:240`, `scripts/setup-rlcr-loop.sh:334`). State begins at `current_round: 0`; the script layer initializes state only, while stop hooks later advance `current_round`, remove the state file on completion/stop/max-iteration, and create next-round prompts (`hooks/loop-dccb-stop-hook.sh:825`, `hooks/loop-dccb-stop-hook.sh:837`, `hooks/loop-dccb-stop-hook.sh:787`, `hooks/loop-dccb-stop-hook.sh:797`). Monitor state is read-only and terminal-local: it tracks current log path, current session dir, last byte offset, and a cleanup flag (`scripts/humanize.sh:401`, `scripts/humanize.sh:452`, `scripts/humanize.sh:707`, `scripts/humanize.sh:746`).
- gates_or_invariants: All shell scripts use `set -euo pipefail` except the sourced monitor/timeout utility files that are intended to define functions (`scripts/setup-dccb-loop.sh:13`, `scripts/setup-rlcr-loop.sh:11`). Setup gates reject missing option values, non-numeric iteration/timeout values, unknown options, and missing `codex` (`scripts/setup-dccb-loop.sh:112`, `scripts/setup-dccb-loop.sh:124`, `scripts/setup-dccb-loop.sh:139`, `scripts/setup-dccb-loop.sh:159`, `scripts/setup-dccb-loop.sh:176`; `scripts/setup-rlcr-loop.sh:98`, `scripts/setup-rlcr-loop.sh:125`, `scripts/setup-rlcr-loop.sh:141`, `scripts/setup-rlcr-loop.sh:195`). RLCR additionally requires exactly one plan file, an existing file, and at least five lines (`scripts/setup-rlcr-loop.sh:147`, `scripts/setup-rlcr-loop.sh:180`, `scripts/setup-rlcr-loop.sh:185`). DCCB intentionally has no plan input and analyzes the current codebase (`scripts/setup-dccb-loop.sh:164`). DCCB documentation invariants are seeded in the prompt and later enforced by the stop hook: dynamic proportional structure, self-contained docs, de-specialized conceptual content, no original-code line/path references, and files under 1800 lines (`scripts/setup-dccb-loop.sh:318`, `scripts/setup-dccb-loop.sh:410`, `scripts/setup-dccb-loop.sh:416`, `scripts/setup-dccb-loop.sh:438`; `hooks/loop-dccb-stop-hook.sh:104`, `hooks/loop-dccb-stop-hook.sh:123`). Stop-hook gates coordinated with the scripts block on incomplete todos, missing summary, missing documentation directory, zero markdown files, uninitialized tracker placeholders, Codex failure, and non-terminal review output (`hooks/loop-dccb-stop-hook.sh:66`, `hooks/loop-dccb-stop-hook.sh:198`, `hooks/loop-dccb-stop-hook.sh:233`, `hooks/loop-dccb-stop-hook.sh:260`, `hooks/loop-dccb-stop-hook.sh:292`, `hooks/loop-dccb-stop-hook.sh:741`, `hooks/loop-dccb-stop-hook.sh:782`).
- dependencies_and_callers: `commands/start-dccb-loop.md` delegates directly to `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-dccb-loop.sh" $ARGUMENTS` (`commands/start-dccb-loop.md:12`). `commands/start-rlcr-loop.md` does the same for `setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:12`). `hooks/hooks.json` registers pre-tool validators and both stop hooks; the DCCB and RLCR stop hooks consume the state files created by the setup scripts (`hooks/hooks.json:4`, `hooks/hooks.json:42`). `hooks/loop-dccb-stop-hook.sh` sources shared hook helpers, finds `.humanize-dccb.local`, reads `dccb-state.md`, creates Codex review prompts, and sources `scripts/portable-timeout.sh` before invoking `codex exec` (`hooks/loop-dccb-stop-hook.sh:36`, `hooks/loop-dccb-stop-hook.sh:40`, `hooks/loop-dccb-stop-hook.sh:60`, `hooks/loop-dccb-stop-hook.sh:365`, `hooks/loop-dccb-stop-hook.sh:679`, `hooks/loop-dccb-stop-hook.sh:721`). `hooks/loop-codex-stop-hook.sh` similarly sources `scripts/portable-timeout.sh` and uses the RLCR state/tracker files generated by `setup-rlcr-loop.sh` (`hooks/loop-codex-stop-hook.sh:43`, `hooks/loop-codex-stop-hook.sh:50`, `hooks/loop-codex-stop-hook.sh:704`, `hooks/loop-codex-stop-hook.sh:750`). README documents the same directory contract and state/cache surfaces (`README.md:379`, `README.md:411`, `README.md:429`), and `.gitignore` excludes both local loop state directories (`.gitignore:3`).
- edge_cases_or_failure_modes: In this read-only branch export, the setup scripts cannot be executed end-to-end because they create directories and files; even `--help` here-doc output failed under the sandbox due temporary-file creation, despite syntax passing. `scripts/setup-dccb-loop.sh` accepts `--output-dir` without normalization, so `../` components can escape the project-root intent, and absolute-looking values become prefixed under `PROJECT_ROOT` rather than treated as true absolute paths (`scripts/setup-dccb-loop.sh:151`, `scripts/setup-dccb-loop.sh:198`). The DCCB monitor has a concrete helper-scope issue: `_split_to_array` is defined inside the RLCR monitor function (`scripts/humanize.sh:248`), but the DCCB status renderer calls it (`scripts/humanize.sh:626`, `scripts/humanize.sh:635`); after merely sourcing `scripts/humanize.sh`, `type _split_to_array` returned not found, so `humanize monitor dccb-loop` can fail in a fresh shell once it reaches status rendering. The DCCB tracker parser counts bullet checkbox lines (`scripts/humanize.sh:610`), but the seeded DCCB tracker uses a markdown table for documentation progress (`scripts/setup-dccb-loop.sh:280`), so initial monitor doc counts may underreport until the tracker is rewritten in the expected shape. `portable-timeout.sh` falls back to running without any timeout if no implementation is available (`scripts/portable-timeout.sh:64`), which preserves functionality but weakens stop-hook bounding. Monitor behavior also assumes an interactive terminal with `tput`, readable cache dirs, and usable `git`; this export is not a git checkout, so git-backed monitor fields degrade.
- validation_or_tests: Recursion over `scripts` found exactly four files and no subdirectories beyond `scripts` itself. `bash -n scripts/humanize.sh`, `bash -n scripts/portable-timeout.sh`, `bash -n scripts/setup-dccb-loop.sh`, and `bash -n scripts/setup-rlcr-loop.sh` all completed successfully. A read-only runtime probe of `scripts/portable-timeout.sh` successfully sourced the file, detected `python3` as the available timeout backend in this sandbox, and `run_with_timeout 2 true` returned exit code 0, though macOS developer-tool cache warnings appeared because `/tmp` writes are blocked. I did not run either setup script for real because the user explicitly requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; exactly one Item Evidence heading is present for the assigned directory item`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`