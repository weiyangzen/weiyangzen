# agent_16 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: This directory is the shell-level robustness suite for the RLCR and PR-loop control algorithms. It exercises parser tolerance, strict validation, path and content gates, hook JSON validation, protected loop-file write gates, cancellation authorization, monitor resilience, git status/state parsing, template substitution, timeout portability, and setup-time branch detection. It is core algorithm evidence because these tests define how the loop should behave under malformed state, hostile input, missing files, race-like disappearance, terminal/log edge cases, and platform differences.
- algorithmic_behavior: The directory contains 11 executable shell tests with no nested descendants beyond the files themselves. Child roles:
  - `test-state-file-robustness.sh` validates tolerant and strict state frontmatter parsing through `get_current_round`, `parse_state_file`, and `parse_state_file_strict`; see `tests/robustness/test-state-file-robustness.sh:14-17`, `:33-139`, `:149-397`.
  - `test-session-robustness.sh` validates active RLCR session selection using newest-directory-only semantics and active markers `state.md` or `finalize-state.md`; see `tests/robustness/test-session-robustness.sh:33-118`, `:128-192`, `:224-302`.
  - `test-goal-tracker-robustness.sh` validates monitor-facing goal tracker parsing for AC counts, completed/deferred/open issue sections, goal summary truncation, malformed files, binary content, and large counts; see `tests/robustness/test-goal-tracker-robustness.sh:32-50`, `:60-227`, `:237-524`.
  - `test-path-validation-robustness.sh` drives `scripts/setup-rlcr-loop.sh` as the production gate for plan-file path safety; it accepts normal relative paths and rejects absolute paths, spaces, shell metacharacters, file and parent-directory symlinks, empty/short/missing/directory plan inputs; see `tests/robustness/test-path-validation-robustness.sh:55-92`, `:101-148`, `:158-312`, `:321-356`, `:404-459`.
  - `test-git-operations-robustness.sh` validates `humanize_parse_git_status` and `humanize_detect_git_state` across clean/dirty/staged/untracked/deleted/renamed/binary paths plus detached, rebase, merge, shallow, non-repo, and permission-like states; see `tests/robustness/test-git-operations-robustness.sh:71-158`, `:168-246`, `:377-500`.
  - `test-hook-input-robustness.sh` validates actual hook validators with JSON on stdin, deep nesting rejection, invalid UTF-8 rejection, monitor terminal/log resilience, command modification pattern detection, and monitor helper availability; see `tests/robustness/test-hook-input-robustness.sh:35-235`, `:245-341`, `:351-370`, `:381-433`, `:444-664`.
  - `test-template-stress-robustness.sh` validates `render_template`, `load_and_render`, and safe fallback rendering under large values, large template files, many substitutions, shell/regex metacharacters, newlines, empty values, repeated variables, and placeholder-in-value injection prevention; see `tests/robustness/test-template-stress-robustness.sh:33-72`, `:82-151`, `:161-283`.
  - `test-plan-file-robustness.sh` drives production plan validation and separately mirrors content-line counting rules for blank/comment filtering, large files, CRLF/LF, binary/null content, disappearing files, symlinks, unreadable files, and directories; see `tests/robustness/test-plan-file-robustness.sh:24-82`, `:103-143`, `:152-264`, `:274-327`, `:329-525`.
  - `test-cancel-security-robustness.sh` validates `is_cancel_authorized` for signal-file gating, exact `mv` command shape, quote handling, path normalization, shell injection rejection, wrong source/destination rejection, symlink rejection, and the “finalize” substring regression; see `tests/robustness/test-cancel-security-robustness.sh:39-89`, `:99-263`, `:265-315`, `:332-405`.
  - `test-timeout-robustness.sh` validates portable timeout detection and `run_with_timeout` behavior for success, timeout exit `124`, argument preservation, pipelines, rapid cycles, nonexistent/empty commands, signal handling, fallback detection, subshells, and exported `TIMEOUT_IMPL`; see `tests/robustness/test-timeout-robustness.sh:31-110`, `:120-233`, `:235-272`.
  - `test-base-branch-detection.sh` replicates setup-time base branch detection logic using `run_with_timeout`, checking remote default, local `main`, local `master`, and failure when none exists; see `tests/robustness/test-base-branch-detection.sh:35-64`, `:73-149`, `:156-190`.
- inputs_outputs_state: Inputs are temporary repositories, temporary `.humanize/rlcr` or `.humanize/pr-loop` directories, generated `state.md`, plan files, goal trackers, JSON hook payloads, shell command strings, template strings/files, git repositories, simulated git metadata, and log files. Outputs are pass/fail counters through `tests/test-helpers.sh:30-77`, temp dirs from `tests/test-helpers.sh:86-89`, and process exit status via each script’s `print_test_summary`. Tests mutate only temporary directories created by the scripts. State transitions covered include active loop detection from newest session, `state.md` to terminal marker eligibility, `active_bots` shrinking in PR-loop assumptions, plan validation progressing to the “codex required” post-validation gate, and cancellation eligibility from `.cancel-requested` plus exact `state.md` or `finalize-state.md` move.
- gates_or_invariants: The suite codifies these invariants: malformed hook JSON must fail without crashing; non-UTF8 hook input must be rejected; deeply nested JSON is a denial-of-service guard; plan paths must be relative, shell-safe, non-symlinked, inside the project, and contentful; active session lookup checks only the newest session to avoid reviving stale loops; cancel requires a signal file and exact path/command shape; state parsing has tolerant defaults but strict schema rejection; template rendering is single-pass; timeout wrappers must preserve exit codes and return `124` on timeout where supported; git status parsers must handle non-standard repo states gracefully.
- dependencies_and_callers: Shared dependencies are `tests/test-helpers.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, and `scripts/portable-timeout.sh`. Production functions under test include `find_active_loop` at `hooks/lib/loop-common.sh:155-173`, state parsers at `hooks/lib/loop-common.sh:178-239` and `:251-323`, `is_cancel_authorized` at `hooks/lib/loop-common.sh:545-704`, command modification detection at `hooks/lib/loop-common.sh:1065-1090`, goal/git parsers at `scripts/humanize.sh:32-111`, `:116-162`, `:166-210`, plan/path validation gates at `scripts/setup-rlcr-loop.sh:273-492`, template rendering at `hooks/lib/template-loader.sh:58-132`, and timeout wrapping at `scripts/portable-timeout.sh:10-76`. Parent test orchestration includes most robustness scripts in `tests/run-all-tests.sh:56-65`.
- edge_cases_or_failure_modes: Key edge cases include missing YAML frontmatter, non-numeric state, empty state, partial YAML, CRLF, binary tails, very large round numbers, negative rounds, newest session lacking state while older session has one, symlinked sessions, deep paths, malformed goal trackers, binary goal trackers, path shell metacharacters, parent symlink traversal, large plan files, disappearing plans, JSON invalid syntax, missing required hook fields, deep JSON, non-UTF8 bytes, monitor terminal width extremes, ANSI/binary logs, cancellation injection strings, symlink source files, timeout fallback absence, and non-git or transient git states. Coverage gap: `test-base-branch-detection.sh` is present in the directory but is not listed in `tests/run-all-tests.sh:56-65`, so full-suite execution may omit that robustness surface unless another runner includes it.
- validation_or_tests: These files are themselves validation assets. I inspected them recursively and did not execute them because the assignment requested research notes only and the branch export is read-only. The suite’s executable summaries are `print_test_summary` calls in each script, for example `tests/robustness/test-state-file-robustness.sh:403-404`, `test-session-robustness.sh:322-323`, `test-goal-tracker-robustness.sh:530-531`, `test-path-validation-robustness.sh:465-466`, `test-git-operations-robustness.sh:506-507`, `test-hook-input-robustness.sh:670-671`, `test-template-stress-robustness.sh:289-290`, `test-plan-file-robustness.sh:531-532`, `test-cancel-security-robustness.sh:411-412`, and `test-timeout-robustness.sh:278-279`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Test fixture generator for PR-loop monitor behavior. It creates canonical `.humanize/pr-loop/<timestamp>/state.md` structures that exercise monitor parsing of `configured_bots` and `active_bots` as YAML lists.
- algorithmic_behavior: The script requires a test directory and optional scenario name, then dispatches by `TEST_NAME`; see `tests/setup-monitor-test-env.sh:12-20`. It writes one of three PR-loop state files: `yaml_list` with both `claude` and `codex` configured and active at `:21-41`; `configured` with both configured but only `codex` active at `:43-62`; and `empty` with both configured and no active bot list entries at `:64-82`. Unknown scenario names print supported values and exit nonzero at `:84-88`. The script echoes the test directory at `:91`.
- inputs_outputs_state: Inputs are `$1` as target test directory and `$2` as scenario name. Output is a created `.humanize/pr-loop/<fixed timestamp>/state.md` with YAML frontmatter fields including `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at`. State modeled: pending PR-loop review with both bots active, partial approval where one bot remains active, and all-approved/empty-active state.
- gates_or_invariants: It enforces a required target directory argument at `:15-18` and restricts fixture variants to the known scenario names at `:20-88`. The fixtures preserve the YAML-list shape that monitor and stop-hook code expect rather than comma strings. The empty scenario intentionally leaves `active_bots:` with no list entries at `:77-78`, which tests that empty active bots displays as none rather than causing a parse error.
- dependencies_and_callers: It is used by PR-loop hook monitor tests: `tests/test-pr-loop-hooks.sh:1553` for YAML list parsing, `:1580` for configured-vs-active display, and `:1602` for empty active bots. The behavior lines up with monitor parsing in `scripts/humanize.sh:1042-1059`, setup generation in `scripts/setup-pr-loop.sh:528-542`, and PR-loop stop-hook state transitions that remove or rebuild `active_bots`, for example `hooks/pr-loop-stop-hook.sh:485-491`, `:1003-1015`, and `:1483-1553`.
- edge_cases_or_failure_modes: Unsupported scenario is rejected; missing test dir is rejected. It does not create a goal tracker or logs, so tests using it must supply any additional monitor artifacts they need. Timestamps are fixed, which is deterministic but can collide if a caller reuses a test directory across scenarios without cleanup.
- validation_or_tests: The script is a helper, not a standalone assertion suite. Its generated files are validated by `tests/test-pr-loop-hooks.sh` monitor cases around `:1547-1621`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Blocking prompt/template emitted when the RLCR Codex review phase fails to produce a review result file. It defines the user-facing contract that review failure is not success and exit will be retried.
- algorithmic_behavior: The template declares a failure title and summary at `prompt-template/block/codex-review-failed.md:1-3`, exposes exit code and missing review result path at `:5-6`, lists debug file locations at `:8-11`, includes the last stderr lines at `:13-16`, and tells the user another review will be attempted on exit at `:18`.
- inputs_outputs_state: Inputs are template variables for exit code, result file path, command/stdout/stderr file paths, and stderr content. Output is Markdown text used as a blocking reason. The state transition is not directly in the template; its caller blocks the stop-hook decision and keeps the loop in implementation/retry flow rather than finalizing.
- gates_or_invariants: The invariant is that a missing review result is a hard gate: the system must not treat an absent Codex review as approval. The caller constructs a block decision through `jq` at `hooks/loop-codex-stop-hook.sh:1193-1201` after rendering this template at `:1183-1191`.
- dependencies_and_callers: The only discovered caller is `hooks/loop-codex-stop-hook.sh:1183-1191`, using `load_and_render_safe` from `hooks/lib/template-loader.sh`. The template loader’s single-pass substitution keeps missing placeholders unchanged by design; see `hooks/lib/template-loader.sh:12-13` and `:116-121`.
- edge_cases_or_failure_modes: Placeholder naming appears inconsistent with the caller. The template expects `{{CODEX_EXIT_CODE}}`, `{{CODEX_STDOUT_FILE}}`, and `{{CODEX_STDERR_FILE}}` at `prompt-template/block/codex-review-failed.md:5-11`, while the caller passes `EXIT_CODE`, `CODEX_LOG_FILE`, and does not pass stdout/stderr file variables at `hooks/loop-codex-stop-hook.sh:1183-1191`. Because missing placeholders are preserved, the rendered block can expose unresolved placeholders. Also, the caller passes `CODEX_LOG_FILE` but the template does not display it.
- validation_or_tests: No direct test for this exact template was found. Template substitution stress behavior is covered generically in `tests/robustness/test-template-stress-robustness.sh`, especially missing/fallback and placeholder-injection behavior at `:44-52` and `:218-228`, but this specific call-site variable contract lacks an explicit assertion.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Write-validator block template that enforces the RLCR invariant that round summary files must be written inside the active loop directory.
- algorithmic_behavior: The template emits a heading, states that summary files must be in the loop directory, and renders the exact expected path via `{{CORRECT_PATH}}`; see `prompt-template/block/wrong-summary-location.md:1-5`. It is intentionally short because the validator computes the authoritative path.
- inputs_outputs_state: Input is `CORRECT_PATH`, supplied by the write validator as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`; see `hooks/loop-write-validator.sh:202-208`. Output is a Markdown error message to stderr followed by validator exit code `2` at `hooks/loop-write-validator.sh:207-209`. The state transition is preventative: an attempted write outside `.humanize/rlcr` is blocked, so no summary state is accepted at the wrong location.
- gates_or_invariants: The gate fires only for summary files outside the loop directory at `hooks/loop-write-validator.sh:202-209`. Finalize summary inside the active loop is allowed earlier at `hooks/loop-write-validator.sh:168-172`, so this block does not overrule the finalize-specific valid path. The invariant is location correctness, not content validation.
- dependencies_and_callers: It depends on `load_and_render_safe` and the template directory initialized through `hooks/lib/loop-common.sh:138-147`. Its discovered caller is `hooks/loop-write-validator.sh:207`. It coordinates with summary file round validation that follows path extraction at `hooks/loop-write-validator.sh:213-225`.
- edge_cases_or_failure_modes: If `ACTIVE_LOOP_DIR` or `CURRENT_ROUND` is stale or unparsable, the rendered correct path can be misleading. If `CORRECT_PATH` were not supplied, template-loader behavior would preserve the placeholder rather than failing hard. The template itself does not mention why the current path was wrong; the gate relies on the caller’s context.
- validation_or_tests: Hook input tests validate write validator JSON parsing generally at `tests/robustness/test-hook-input-robustness.sh:47-57`, but no direct assertion for this exact message was found in the assigned robustness directory. Related write-location behavior is exercised elsewhere through loop write-validator tests referenced by the caller surface.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-136 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable specification for state-file parser robustness. It defines how RLCR state frontmatter should be parsed in tolerant monitor/control paths and rejected in strict validation paths.
- algorithmic_behavior: The script sources production `hooks/lib/loop-common.sh` and test helpers at `tests/robustness/test-state-file-robustness.sh:14-17`, creates a temp workspace at `:19`, then runs 18 cases. Positive cases validate full tolerant parsing, `get_current_round`, ignored extra fields, quoted strings, and round zero at `:33-139`. Negative/edge cases validate strict rejection of missing frontmatter, nonnumeric `current_round`, missing required fields, plus tolerant defaults and graceful handling for empty files, partial YAML, malformed YAML, very large and negative values, special characters, trailing whitespace, missing files, binary tails, and CRLF at `:149-397`. It exits with the helper summary at `:403-404`.
- inputs_outputs_state: Inputs are generated `state*.md` files in `$TEST_DIR`, each with YAML-like frontmatter. Outputs are global parser variables such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH`, plus stdout pass/fail lines and final exit code. State transitions under test are parser-level: missing or malformed state either becomes tolerant defaults (`current_round=0`, `max_iterations=10`) or strict nonzero rejection.
- gates_or_invariants: Tolerant parsing must not crash and must default missing `current_round` to `0` and `max_iterations` to `10`; see assertions at `:217-228` and production defaults at `hooks/lib/loop-common.sh:231-239`. Strict parsing must reject missing YAML separators, missing schema fields, and nonnumeric numeric fields; production strict checks are at `hooks/lib/loop-common.sh:259-323`. `get_current_round` must return a numeric string or default `0` for absent/unparseable frontmatter; production logic is `hooks/lib/loop-common.sh:178-187`.
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`. The production functions are `get_current_round` at `hooks/lib/loop-common.sh:178-187`, `parse_state_file` at `:207-239`, and `parse_state_file_strict` at `:251-323`. These parser outputs are consumed by loop validators, stop hooks, and monitor paths that need current round, plan metadata, base branch, review state, and max iteration data.
- edge_cases_or_failure_modes: The script covers no frontmatter, nonnumeric round, missing fields, empty files, partial frontmatter, malformed YAML, huge integers, negative rounds, spaces/special characters, nonexistent files, binary suffixes, and CRLF. One notable coverage gap: production strict parsing now requires `review_started` and `base_branch` at `hooks/lib/loop-common.sh:293-299`, but this test only asserts strict failure cases and does not include a strict positive fixture containing all current required fields.
- validation_or_tests: This is the validation file. It is also included in the aggregate runner at `tests/run-all-tests.sh:56`. I inspected it directly and did not execute it in this research-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item evidence sections present; each assigned item header appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`