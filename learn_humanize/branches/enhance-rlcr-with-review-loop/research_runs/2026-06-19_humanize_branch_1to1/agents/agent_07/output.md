# agent_07 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the executable specification surface for the Humanize RLCR, finalize/review loop, PR-loop, monitor, validator, template, path-safety, and robustness behavior. It contains 42 recursively inspected files: 30 top-level files, 11 under `tests/robustness`, 8 JSON fixtures, and one `tests/mocks/gh` mock. The central aggregate runner is `tests/run-all-tests.sh`, whose ordered suite list includes the assigned `test-state-exit-naming.sh`, review-loop specs, monitor specs, template specs, validator specs, and robustness suites at `tests/run-all-tests.sh:32-66`.
- algorithmic_behavior: The directory acts as a behavior contract around the runtime scripts and hooks rather than as production logic. Top-level tests validate normal loop behavior: state exit naming, finalize phase, Codex review issue detection, template reference completeness, monitor runtime/e2e behavior, plan-file handling, cancel signaling, allowlists, and PR-loop orchestration. Robustness tests stress malformed state files, path traversal, session selection, hook JSON inputs, git status parsing, template rendering, timeout fallback, and cancel command injection attempts. `tests/test-codex-review-merge.sh` documents and checks the review issue algorithm: scan the full review log, find the first `[P?]` marker in the first 10 characters of a line, extract from that line to EOF, return no-issue or hard-error exit codes (`tests/test-codex-review-merge.sh:3-16`, `tests/test-codex-review-merge.sh:57-80`, `tests/test-codex-review-merge.sh:133-169`, `tests/test-codex-review-merge.sh:171-227`, `tests/test-codex-review-merge.sh:306-330`). `tests/test-finalize-phase.sh` specifies the new review/finalize transitions: COMPLETE enters finalize only after review, review failure or empty output blocks exit and preserves `state.md`, finalization renames `finalize-state.md` to `complete-state.md`, max iterations skip finalize, and validators protect `finalize-state.md` (`tests/test-finalize-phase.sh:3-20`, `tests/test-finalize-phase.sh:51-142`, `tests/test-finalize-phase.sh:180-205`, `tests/test-finalize-phase.sh:483-492`, `tests/test-finalize-phase.sh:525-539`, `tests/test-finalize-phase.sh:566-618`, `tests/test-finalize-phase.sh:625-690`, `tests/test-finalize-phase.sh:820-864`).
- inputs_outputs_state: Inputs are shell scripts, hook JSON payloads, synthetic `.humanize/rlcr` and `.humanize/pr-loop` session directories, temporary git repos, mock Codex executables, mock GitHub CLI fixtures, plan files, state files, goal trackers, transcript files, and cache logs. Outputs are pass/fail counters, generated temp files inside test temp dirs, mocked review logs/results, state-file rename assertions, and process exit codes. State coverage includes active `state.md`, `finalize-state.md`, terminal state files like `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, `unexpected-state.md`, `.review-phase-started`, `.cancel-requested`, goal tracker sections, PR loop state, and monitor cache files. Shared `tests/test-helpers.sh` provides reusable counters and temp repo setup (`tests/test-helpers.sh:22-44`, `tests/test-helpers.sh:58-78`, `tests/test-helpers.sh:84-105`).
- gates_or_invariants: `run-all-tests.sh` treats a suite as failed if its exit code is nonzero or parsed failed count is positive, strips ANSI for portable result parsing, and has a special zsh runner for `test-zsh-monitor-safety.sh` (`tests/run-all-tests.sh:68-112`, `tests/run-all-tests.sh:114-130`, `tests/run-all-tests.sh:141-152`). Template reference tests require all hook-referenced templates to exist and require critical validators to use safe rendering with fallbacks (`tests/test-template-references.sh:52-111`, `tests/test-template-references.sh:149-167`, `tests/test-template-references.sh:172-200`). Cancel tests require `.cancel-requested` for the only authorized state-to-cancel rename and block arbitrary moves, redirections, command substitution, chaining, quoting bypasses, symlinks, and wrong directories (`tests/test-cancel-signal-file.sh:1-9`; robustness coverage at `tests/robustness/test-cancel-security-robustness.sh:3-10`). Robust state tests assert strict parsing rejects missing YAML separators, missing fields, nonnumeric values, and partial/corrupt state while tolerant helpers avoid crashing (`tests/robustness/test-state-file-robustness.sh:3-10`, `tests/robustness/test-state-file-robustness.sh:149-215`). Session tests assert `find_active_loop` uses newest active session semantics and includes finalize-state sessions (`tests/robustness/test-session-robustness.sh:3-10`, `tests/robustness/test-session-robustness.sh:40-55`).
- dependencies_and_callers: Tests directly source or execute production surfaces: `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/portable-timeout.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/fetch-pr-comments.sh`, and `scripts/poll-pr-reviews.sh`. PR-loop tests use `tests/fixtures/*.json`, `tests/setup-fixture-mock-gh.sh`, and `tests/mocks/gh` to simulate GitHub API responses (`tests/setup-fixture-mock-gh.sh:1-12`). Monitor tests source `scripts/humanize.sh` and `scripts/lib/monitor-common.sh` (`tests/test-monitor-runtime.sh:64-109`, `tests/test-pr-loop-system.sh:257-316`, `tests/test-pr-loop-system.sh:793-816`).
- edge_cases_or_failure_modes: The directory explicitly targets failure modes that can corrupt loop state or hide review failures: missing/empty Codex review logs are hard errors (`tests/test-codex-review-merge.sh:133-169`); review markers outside the first 10 characters are ignored (`tests/test-codex-review-merge.sh:83-107`); monitor deletion must restore terminal state and stop cleanly (`tests/test-monitor-runtime.sh:55-135`, `tests/test-monitor-runtime.sh:179-220`); zsh glob/no-match behavior must be safe (`tests/test-zsh-monitor-safety.sh:1-10`); plan files reject absolute paths, symlinks, submodules, sparse/blank files, and injection-like model options (`tests/test-plan-file-validation.sh:1-11`); hook inputs reject malformed/deep JSON and command parsing bypasses (`tests/robustness/test-hook-input-robustness.sh:3-10`).
- validation_or_tests: This item itself is test coverage. I did not execute the full suite because the branch export is read-only/restricted and an attempted `git status` showed this sandbox cannot create required developer-tool cache files and the export is not a normal `.git` checkout. Evidence was gathered by recursive file inspection, suite inventory, and representative executable spec reads.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-037 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: `scripts/humanize.sh` is the user-facing shell entrypoint for Humanize monitoring. It defines public helpers for goal tracker parsing, git state/status parsing, the `humanize` command dispatcher, and two real-time monitors: RLCR via `humanize monitor rlcr` and PR loop via `humanize monitor pr`. It sources shared monitor utilities from `scripts/lib/monitor-common.sh` when present (`scripts/humanize.sh:6-10`) and dispatches monitor subcommands at `scripts/humanize.sh:883-924`.
- algorithmic_behavior: The RLCR monitor `_humanize_monitor_codex` watches `.humanize/rlcr`, selects the newest timestamped session, locates corresponding implementation and review logs in `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>`, draws a fixed status bar, tails logs incrementally, switches to newer sessions/logs, handles missing/truncated/deleted logs, and exits cleanly if the loop directory disappears (`scripts/humanize.sh:219-240`, `scripts/humanize.sh:242-342`, `scripts/humanize.sh:373-510`, `scripts/humanize.sh:620-740`, `scripts/humanize.sh:750-870`). A key review-loop invariant is encoded in `_find_latest_codex_log`: if both run and review logs exist, `max_run_round` must be strictly less than `min_review_round`; otherwise it prints an inconsistent log-state error and returns failure (`scripts/humanize.sh:297-338`). The PR monitor `_humanize_monitor_pr` similarly watches `.humanize/pr-loop`, supports `--once`, picks the newest monitorable PR artifact by mtime, maps state stop reasons for display, parses PR state frontmatter, draws phase/bot/review status, and tails the latest PR loop file (`scripts/humanize.sh:930-1028`, `scripts/humanize.sh:1030-1167`, `scripts/humanize.sh:1215-1293`, `scripts/humanize.sh:1303-1355`).
- inputs_outputs_state: Inputs include `.humanize/rlcr` or `.humanize/pr-loop` directories, timestamped session directories, session `state.md` or terminal `*-state.md`, `goal-tracker.md`, git repository state, cached `round-*-codex-run.log` and `round-*-codex-review.log`, PR loop artifacts (`round-*-pr-check.md`, `round-*-pr-feedback.md`, `round-*-pr-comment.md`), terminal dimensions, shell flavor, and `XDG_CACHE_HOME`. Outputs are terminal UI lines, real-time tailed log content, human-readable status, nonzero returns for missing loop/session, and cleanup messages. Internal state includes `current_session_dir`, `current_file`, `last_size`, `monitor_running`, cleanup guards, and tail PIDs (`scripts/humanize.sh:224-229`, `scripts/humanize.sh:530-561`, `scripts/humanize.sh:615-618`, `scripts/humanize.sh:1169-1200`).
- gates_or_invariants: The script avoids zsh glob errors by using `find` rather than direct glob expansion for sessions/logs (`scripts/humanize.sh:280-339`). It treats `state.md` as active and terminal `*-state.md` as completed/failed/cancelled style statuses through `monitor_find_state_file` (`scripts/humanize.sh:344-347`, `scripts/humanize.sh:383-389`). It validates monitor prerequisites: missing `.humanize/rlcr` or `.humanize/pr-loop` returns an error (`scripts/humanize.sh:230-235`, `scripts/humanize.sh:955-960`), no session returns an RLCR error (`scripts/humanize.sh:590-599`), and PR `--once` returns an error if no session or state file exists (`scripts/humanize.sh:1215-1230`). Cleanup is guarded to run once and restore the terminal/kill tail processes (`scripts/humanize.sh:535-561`, `scripts/humanize.sh:1174-1200`).
- dependencies_and_callers: Depends on `scripts/lib/monitor-common.sh` for file size, latest session, state file, PR phase, and PR goal tracker parsing helpers. Uses system tools `find`, `stat`, `sed`, `grep`, `sort`, `wc`, `tail`, `tput`, `clear`, `basename`, `date`, and `git`. It is called interactively through the `humanize` shell function and by tests such as `tests/robustness/test-goal-tracker-robustness.sh`, `tests/robustness/test-git-operations-robustness.sh`, `tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/test-zsh-monitor-safety.sh`, and PR-loop monitor tests.
- edge_cases_or_failure_modes: Handles missing loop dir, missing session dirs, no log files yet, no log after terminal state, current session deletion, log deletion, newer session without a log, newer log in same session, file truncation/rotation, zsh trap differences, very narrow terminal truncation, absent git repo, detached/rebase/merge/shallow git states, and ANSI terminal cleanup. One notable limitation is that parsing is mostly grep/sed-based and not a full YAML parser; malformed state frontmatter can yield `N/A` in monitor UI rather than hard failure (`scripts/humanize.sh:349-365`, `scripts/humanize.sh:1030-1059`).
- validation_or_tests: Covered by `tests/test-monitor-runtime.sh` for cleanup/graceful-stop behavior, `tests/test-monitor-e2e-real.sh` for real monitor execution and terminal restoration, `tests/test-zsh-monitor-safety.sh` for zsh no-match safety, `tests/robustness/test-goal-tracker-robustness.sh` for `humanize_parse_goal_tracker`, `tests/robustness/test-git-operations-robustness.sh` for git parsing/detection, and `tests/test-pr-loop-system.sh`/`tests/test-pr-loop-hooks.sh` for PR monitor and shared monitor helper integration. Not run in this read-only research pass.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-067 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: This file is an executable spec for RLCR state-file lifecycle naming. It verifies which state filenames represent active loops and which represent terminal outcomes, and it checks the `end_loop` helper’s allowed reason set and rename behavior.
- algorithmic_behavior: The test creates a temp git repo and `.humanize/rlcr/<timestamp>` session, sources `hooks/lib/loop-common.sh`, then asserts `find_active_loop` ignores terminal files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` but detects active `state.md` (`tests/test-state-exit-naming.sh:38-95`, `tests/test-state-exit-naming.sh:97-159`). It then verifies the newest timestamped directory with `state.md` takes precedence (`tests/test-state-exit-naming.sh:161-180`). For termination, it calls `end_loop` directly, expects invalid reasons to fail with an error, expects each valid reason (`complete`, `cancel`, `maxiter`, `stop`, `unexpected`) to produce `<reason>-state.md`, and expects missing state file to fail gracefully (`tests/test-state-exit-naming.sh:182-243`). Finally it verifies path recognition has moved to `.humanize/rlcr` and does not accidentally treat legacy `.humanize-loop.local` as a loop dir (`tests/test-state-exit-naming.sh:245-291`).
- inputs_outputs_state: Inputs are synthetic state files with YAML frontmatter fields such as `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` (`tests/test-state-exit-naming.sh:51-62`, `tests/test-state-exit-naming.sh:78-88`, `tests/test-state-exit-naming.sh:165-173`). Outputs are PASS/FAIL counters, `ACTIVE_LOOP` path strings, exit codes from `end_loop`, and renamed state files like `complete-state.md` (`tests/test-state-exit-naming.sh:27-33`, `tests/test-state-exit-naming.sh:196-230`, `tests/test-state-exit-naming.sh:293-302`).
- gates_or_invariants: Only `state.md` in `.humanize/rlcr` marks normal active loop in this specific test; terminal names do not reactivate a loop. The newest timestamped active session wins. `end_loop` has a closed set of valid reasons and must not silently accept arbitrary names. Missing state file is a failure rather than a successful no-op. Legacy `.humanize-loop.local` is intentionally excluded from `is_in_humanize_loop_dir` and from active-loop searches.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` (`tests/test-state-exit-naming.sh:66-68`). The helper behavior under test is implemented in `hooks/lib/loop-common.sh`: active loop detection checks only the newest timestamp directory and accepts `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:150-173`), while `end_loop` validates reasons and renames state to `<reason>-state.md` (`hooks/lib/loop-common.sh:1106-1139`). The file is included in the normal suite at `tests/run-all-tests.sh:32-40`.
- edge_cases_or_failure_modes: Covers stale terminal sessions, invalid exit reason injection, missing state file, multiple session precedence, legacy path false positives, and ensuring old loop directories do not become active simply because they contain terminal state files. It does not test `finalize-state.md`; that is covered in `tests/test-finalize-phase.sh` and robustness session tests.
- validation_or_tests: The file is itself validation. It exits with `TESTS_FAILED` (`tests/test-state-exit-naming.sh:293-302`). I did not execute it in this read-only pass because it creates temp git repos and commits, and the sandbox already showed git/developer-tool cache restrictions.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-097 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: This prompt-template block is the user-facing denial message for attempts to modify RLCR `state.md`. It enforces the contract that loop state is system-managed, not agent-editable.
- algorithmic_behavior: The template is static markdown. It tells the agent it cannot modify `state.md`, says the file is managed by the loop system, lists the state contents, and states that modification would corrupt loop state (`prompt-template/block/state-file-modification.md:1-10`). It is rendered by `state_file_blocked_message` in `hooks/lib/loop-common.sh`, which uses `load_and_render_safe` with an inline fallback (`hooks/lib/loop-common.sh:457-464`).
- inputs_outputs_state: Inputs are none at template level; there are no placeholders. At runtime, validator code selects this template when a Write/Edit/Bash operation targets RLCR `state.md`. Output is the block reason text emitted to stderr or returned through hook blocking JSON, depending on caller.
- gates_or_invariants: The invariant is that `state.md` must not be modified by the agent. Direct Write/Edit blocks are enforced in `hooks/loop-write-validator.sh`, which checks state paths and exits 2 after rendering the state message (`hooks/loop-write-validator.sh:147-160`). Bash-command modification attempts are blocked in `hooks/loop-bash-validator.sh`, including redirection, `tee`, `sed -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, and source-side move/copy patterns (`hooks/loop-bash-validator.sh:127-156`, `hooks/loop-bash-validator.sh:158-190`, `hooks/lib/loop-common.sh:1062-1090`). Cancel has a narrowly authorized exception via `.cancel-requested` and exact `mv state.md cancel-state.md` validation (`hooks/lib/loop-common.sh:527-704`).
- dependencies_and_callers: Called indirectly by RLCR validators through `hooks/lib/loop-common.sh`. Template existence is explicitly validated by `tests/test-template-references.sh`, which lists `block/state-file-modification.md` as a common template (`tests/test-template-references.sh:149-167`). It is part of the broader prompt-template system loaded by `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: Because it has no placeholders, rendering cannot fail due to missing variables. The main failure mode would be missing template file; `state_file_blocked_message` has a fallback (`hooks/lib/loop-common.sh:457-464`), and `tests/test-template-references.sh` catches missing references before runtime (`tests/test-template-references.sh:52-111`). The template only names `state.md`; finalize has a separate template, `prompt-template/block/finalize-state-file-modification.md`, so this file should not be reused for finalize-state messaging.
- validation_or_tests: Covered by template reference validation and by validator suites that expect state-file modification blocks: `tests/test-template-references.sh`, `tests/test-cancel-signal-file.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-finalize-phase.sh` for adjacent finalize protection, and robustness cancel/security tests. Not executed in this read-only pass.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-127 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: `scripts/lib/monitor-common.sh` is the shared monitor helper library for `scripts/humanize.sh`. It centralizes ANSI color helpers, file/session/state utilities, terminal setup/restore helpers, loop status coloring, YAML frontmatter extraction, string/timestamp formatting, PR-loop phase detection, goal tracker parsing, and PR goal tracker stats parsing.
- algorithmic_behavior: The library provides cross-platform file size lookup via GNU/BSD `stat` (`scripts/lib/monitor-common.sh:29-35`), timestamped session selection with `find` and lexical timestamp comparison (`scripts/lib/monitor-common.sh:37-63`), terminal scroll-region setup and restoration (`scripts/lib/monitor-common.sh:69-92`), status color mapping (`scripts/lib/monitor-common.sh:128-141`), state-file discovery that prioritizes active `state.md` then falls back to first `*-state.md` terminal state (`scripts/lib/monitor-common.sh:147-188`), simple YAML frontmatter key extraction (`scripts/lib/monitor-common.sh:190-209`), display formatting (`scripts/lib/monitor-common.sh:215-252`), PR-loop phase inference (`scripts/lib/monitor-common.sh:255-342`), phase display text (`scripts/lib/monitor-common.sh:344-381`), generic goal tracker summary parsing (`scripts/lib/monitor-common.sh:383-463`), and PR issue-stat parsing (`scripts/lib/monitor-common.sh:465-498`).
- inputs_outputs_state: Inputs are session directories, state files, PR check files, goal trackers, terminal dimensions, and optional cache files under `/tmp/humanize-phase-<session>-<round>.size`. Outputs are pipe-delimited values such as `state_file|status`, `total_acs|completed_acs|...`, human-readable phase strings, ANSI escape codes, formatted timestamps, and truncated strings. PR phase detection writes/updates a small `/tmp` size cache to infer whether a latest `round-*-pr-check.md` file is growing (`scripts/lib/monitor-common.sh:292-317`).
- gates_or_invariants: Session directories must match `YYYY-MM-DD_HH-MM-SS` in callers that use `monitor_find_latest_session`; this helper itself picks lexically latest timestamp names and ignores missing loop dirs (`scripts/lib/monitor-common.sh:44-63`). `monitor_find_state_file` treats `state.md` as active before any terminal file (`scripts/lib/monitor-common.sh:162-187`). PR phase final states take precedence over activity detection (`scripts/lib/monitor-common.sh:272-276`). `codex_analyzing` is inferred from file growth or mtime younger than 10 seconds (`scripts/lib/monitor-common.sh:292-317`), otherwise state frontmatter selects `waiting_initial_review` for round 0/startup case 1 or `waiting_reviewer` (`scripts/lib/monitor-common.sh:321-340`).
- dependencies_and_callers: Sourced by `scripts/humanize.sh` (`scripts/humanize.sh:6-10`) and directly by PR-loop tests. Depends on shell utilities `stat`, `find`, `basename`, `sed`, `grep`, `wc`, `sort`, `date`, `tput`, and `/tmp` writeability for PR phase size cache. `scripts/humanize.sh` wraps or calls these helpers for RLCR state/log display (`scripts/humanize.sh:237-240`, `scripts/humanize.sh:344-347`, `scripts/humanize.sh:610-613`) and PR monitor display (`scripts/humanize.sh:962-965`, `scripts/humanize.sh:1011-1028`, `scripts/humanize.sh:1097-1103`).
- edge_cases_or_failure_modes: Handles absent directories by returning empty/unknown, absent state file by returning `|unknown`, absent YAML key by returning empty, `N/A` timestamps by passing through, and overlong strings by truncating start or end. Potential edge cases: `monitor_find_state_file` uses the first `find` result for `*-state.md`, so multiple terminal state files are not ordered deterministically (`scripts/lib/monitor-common.sh:168-184`); PR phase writes cache files under `/tmp`, which can fail silently due `|| true` (`scripts/lib/monitor-common.sh:297-307`); parsing is grep/sed-based and can misread complex YAML. In this sandbox, `/tmp` cache writes were generally restricted for some tools, so tests that rely on cache updates may be environment-sensitive.
- validation_or_tests: Covered by `tests/test-pr-loop-system.sh`, which sources this file for PR phase and state-file helpers (`tests/test-pr-loop-system.sh:257-316`, `tests/test-pr-loop-system.sh:793-816`), by monitor tests via `scripts/humanize.sh`, and by PR monitor `--once` tests in `tests/test-pr-loop-hooks.sh`. Not executed in this read-only pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-007, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-037, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-067, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-097, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-127
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`