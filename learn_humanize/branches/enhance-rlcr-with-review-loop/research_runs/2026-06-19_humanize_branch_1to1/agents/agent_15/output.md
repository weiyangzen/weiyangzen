# agent_15 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared monitor support library directory. Recursive inspection found one child, `scripts/lib/monitor-common.sh`, a 498-line shell library for RLCR and PR-loop terminal monitoring. It is sourced opportunistically by `scripts/humanize.sh` at lines 6-10, so monitor behavior degrades only if the file is missing.
- algorithmic_behavior: `monitor-common.sh` supplies cross-platform file sizing (`monitor_get_file_size`, lines 29-35), timestamp-pattern latest-session selection (`monitor_find_latest_session`, lines 37-63), terminal scroll-region setup and restore (lines 69-92), loop status coloring (lines 128-141), state-file classification (`monitor_find_state_file`, lines 147-188), frontmatter value extraction (lines 194-209), display formatting/truncation (lines 215-252), PR-loop phase detection (`get_pr_loop_phase`, lines 267-342), PR phase display text (lines 344-381), generic goal-tracker statistics (`parse_goal_tracker`, lines 387-463), and PR goal-tracker issue statistics (`humanize_parse_pr_goal_tracker`, lines 465-498).
- inputs_outputs_state: Inputs are filesystem paths for loop/session directories, state files, goal trackers, terminal dimensions, and file paths. Outputs are shell strings such as latest session path, `state_file|status`, pipe-delimited tracker stats, ANSI escape codes, or human-readable phase labels. The only persistent state observed is the PR phase file-size cache under `/tmp/humanize-phase-${session_name}-${highest_round}.size` written by `get_pr_loop_phase` at lines 297-306.
- gates_or_invariants: Latest-session detection accepts only names matching `YYYY-MM-DD_HH-MM-SS` in `monitor_find_latest_session` lines 54-59. State classification prioritizes `state.md` as active before any `*-state.md` file at lines 162-181. PR phase detection prioritizes final markers `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` before growth detection at lines 272-275. A PR check file is considered active if size increased or its mtime is younger than 10 seconds at lines 308-317. Missing files/directories return neutral defaults rather than failing, for example line 34 returns `0`, lines 44-47 return empty, and lines 392-394 return zero tracker counts.
- dependencies_and_callers: `scripts/humanize.sh` wraps `monitor_find_latest_session` for RLCR monitor selection at lines 237-240, `monitor_get_file_size` for incremental log reading at lines 610-613, `monitor_find_state_file` for PR monitor state mapping at lines 1011-1028, `get_pr_loop_phase` and `get_pr_loop_phase_display` for PR status rendering at lines 1097-1103, and `humanize_parse_pr_goal_tracker` for PR issue counters at lines 1139-1146. `tests/test-pr-loop-system.sh` directly exercises the shared helpers, with rg hits for latest-session, state-file, file-size, goal-tracker, and PR tracker parser tests.
- edge_cases_or_failure_modes: `monitor_find_latest_session` ignores non-timestamp session names, unlike `hooks/lib/loop-common.sh::find_active_loop`; this is appropriate for display of generated sessions but may skip ad-hoc folders. `monitor_find_state_file` uses `find` and takes the first `*-state.md` encountered, so multiple terminal marker files have filesystem-order ambiguity after `state.md` is absent. The `/tmp` phase cache keys only by session basename and round, so two projects with same session basename could share phase cache state. YAML/frontmatter parsing is grep/sed based and intentionally simple; duplicate keys or complex YAML are not structurally parsed.
- validation_or_tests: Direct tests are in `tests/test-pr-loop-system.sh`: parse-goal-tracker hit at line 359, PR goal-tracker parser at line 404, state-file tests around lines 432/448/632/793-816, latest-session at lines 769-781, and file-size at lines 825-840. Robustness coverage also exists for the production `scripts/humanize.sh` parser in `tests/robustness/test-goal-tracker-robustness.sh`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: Test fixture generator for GitHub CLI-dependent PR review/comment algorithms. It creates a temporary `gh` executable that makes `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` deterministic under fixture data.
- algorithmic_behavior: The script validates two required args, `<mock_bin_dir>` and `<fixtures_dir>`, at lines 16-22, creates the mock binary directory at line 24, writes a shell script to `$MOCK_BIN_DIR/gh` at lines 27-100, marks it executable at line 102, and echoes the mock bin directory at line 104. The generated mock dispatches on first-level `gh` commands: `auth status` returns logged-in success lines 33-39; `repo view` returns owner/name/parent JSON variants lines 40-53; `pr view` returns baseRepository, number, or state JSON lines 54-66; `api user` returns `testuser` lines 67-72; issue comments, review comments, and PR reviews endpoints return fixture JSON files lines 74-90; other API calls return an empty array lines 92-94.
- inputs_outputs_state: Inputs are a writable mock-bin path and fixture directory containing `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`. Output is an executable `gh` shim plus stdout containing the mock-bin path. State is filesystem-local to the test temp directory; the mock closes over the fixture directory path via generated assignment at line 31.
- gates_or_invariants: It requires both arguments and exits with usage otherwise lines 19-22. Endpoint matching order matters: issue comments are matched before PR review comments, and PR review comments before PR reviews lines 74-90. Unhandled non-API commands fail with an explicit stderr message and exit 1 lines 98-99, while unknown API endpoints return `[]`, which lets consumers test empty-result paths without crashing.
- dependencies_and_callers: The direct caller is `tests/test-pr-loop-hooks.sh::setup_fixture_mock_gh`, which invokes this script at lines 1231-1239 and prepends the returned directory to `PATH` for fixture-backed tests. Those tests run `scripts/fetch-pr-comments.sh` at lines 1249-1251 and 1294-1296, and `scripts/poll-pr-reviews.sh` at lines 1329-1334 and 1381-1383.
- edge_cases_or_failure_modes: Fixture files are assumed present; missing JSON files will make the generated mock fail at `cat`. The mock uses substring checks against `$*`, so it is tolerant of argument order but not a full `gh` parser. `repo view` field matching uses broad substring conditions at lines 42-49, which can satisfy simplified tests but may not model every `gh --json` query precisely.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` verifies fetch includes human issue comments, inline review comments, and approval-only review placeholders lines 1265-1283; verifies `--after` filtering lines 1294-1318; verifies poll JSON validity, placeholder reviews, and responded bots lines 1329-1367; and verifies poll timestamp filtering lines 1378-1411.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-075 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the PR-loop Claude acknowledgement gate. It is shown when Claude is configured and a trigger comment exists, but the Claude bot does not add an `eyes` reaction within the retry window.
- algorithmic_behavior: The template renders a failure title line 1, interpolates `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}` in the timeout sentence line 3, lists likely causes lines 5-8, gives required operator actions lines 10-13, and instructs posting a new trigger comment after bot configuration is fixed line 18.
- inputs_outputs_state: Inputs are template variables supplied by the caller, specifically `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`. Output is Markdown used as a JSON block reason. It has no local state; rendering is handled by `hooks/lib/template-loader.sh`, whose `render_template` performs single-pass `{{VAR}}` replacement and keeps missing placeholders unchanged lines 50-131, while `load_and_render_safe` falls back to inline text if the template is missing or renders empty lines 167-203.
- gates_or_invariants: The transition gate is in `hooks/pr-loop-stop-hook.sh`: Claude must be configured and a trigger must be required lines 749-757; a confirmed trigger comment ID must exist lines 757-766; `scripts/check-bot-reactions.sh claude-eyes ... --retry 3 --delay 5` must return a nonempty, non-null reaction lines 764-768. Failure renders this template with `RETRY_COUNT=3` and `TOTAL_WAIT_SECONDS=15` at lines 774-775 and returns a JSON `decision: block` at lines 777-779.
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh` lines 742-779. The reaction check depends on `scripts/check-bot-reactions.sh`, whose mode description and retry behavior are referenced by rg hits around its lines 187-294. `scripts/setup-pr-loop.sh` has a related initial setup eyes-verification gate at lines 484-520 but uses its own stderr messages rather than this template.
- edge_cases_or_failure_modes: If the trigger comment ID is absent, the hook warns and skips this exact template path at lines 785-788, so this message only covers confirmed trigger IDs. Missing template variables would remain as raw placeholders because rendering is single-pass and non-failing. A temporary GitHub API issue or delayed bot response is treated the same as bot misconfiguration after the fixed 15-second retry budget.
- validation_or_tests: `tests/test-pr-loop-stophook.sh` includes `test_stophook_claude_eyes_timeout` beginning at line 1012, mocks no eyes reaction around lines 1046-1064, and expects output mentioning eyes/timeout/bot configuration around lines 1131-1139. `tests/test-pr-loop-system.sh` separately covers successful Claude eyes detection around lines 222-228.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-105 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Blocking prompt template for stale round-number write/edit attempts against RLCR round artifacts. It prevents agents from manually advancing or targeting obsolete `round-N-summary.md` files.
- algorithmic_behavior: The template renders a title line 1, interpolates `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, and `{{CURRENT_ROUND}}` into the violation sentence line 3, emits the authoritative `{{CORRECT_PATH}}` line 5, and states the invariant: do not increment the round number yourself line 7.
- inputs_outputs_state: Inputs are action text (`write to` or `edit`), detected file round, file type, current round parsed from state, and the correct active-loop path. Output is stderr Markdown from validators. Rendering uses `load_and_render_safe` in `hooks/lib/template-loader.sh`, with missing-template fallback and single-pass substitution lines 167-203.
- gates_or_invariants: `hooks/loop-write-validator.sh` applies this to summary-file writes: it extracts the requested round at lines 228-230, compares it to `CURRENT_ROUND`, checks allowlist status, computes `round-${CURRENT_ROUND}-summary.md`, renders the template, and exits 2 at lines 231-245. `hooks/loop-edit-validator.sh` applies the same rule to edit paths at lines 162-179. The explicit invariant is that agents must target the current round and must not self-increment line 7.
- dependencies_and_callers: Direct callers are `hooks/loop-write-validator.sh` lines 228-245 and `hooks/loop-edit-validator.sh` lines 162-179. The read validator uses a neighboring template, `block/wrong-round-file.md`, for stale read attempts at `hooks/loop-read-validator.sh` lines 138-150. State values come from `hooks/lib/loop-common.sh` parsing, including `current_round` extraction/defaults around lines 175-188 and broader state parsing around rg hits lines 198-304.
- edge_cases_or_failure_modes: If `extract_round_number` returns empty, the write validator does not block on round mismatch line 231, so non-round filenames are handled elsewhere. Allowlisted files bypass this block. The write validator only invokes this template when `IS_SUMMARY_FILE` is true lines 224-228, so prompt-file read/write location issues use other templates. Incorrect or manually edited `current_round` in state can cause a valid path to be rejected or stale path to be accepted, though state parsing has separate robustness checks.
- validation_or_tests: `tests/test-template-loader.sh` renders this template with sample values around lines 151-155. `tests/test-templates-comprehensive.sh` has a real-template render check around lines 498-503. `tests/test-pr-loop-hooks.sh` enters wrong-round validation tests after line 1422, and broader hook tests exercise validator exits.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-135 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR active-session discovery. It defines expected behavior for `hooks/lib/loop-common.sh::find_active_loop`, especially newest-session gating and active/finalize state detection.
- algorithmic_behavior: The script enables strict shell mode line 12, sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` lines 14-17, creates an isolated temp directory via `setup_test_dir` line 19, then runs 18 scenario tests. Each scenario mutates `$TEST_DIR/rlcr`, calls `find_active_loop`, compares stdout to the expected active directory or empty string, records pass/fail, and exits through `print_test_summary` lines 318-322.
- inputs_outputs_state: Inputs are temporary session directory layouts and marker files: `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`. Output is console pass/fail text and an exit code from the shared test summary. Test state is isolated under `mktemp -d` from `tests/test-helpers.sh` lines 84-89.
- gates_or_invariants: The production helper under test checks only the newest immediate child directory by reverse lexical sort lines 163-165 of `hooks/lib/loop-common.sh`, then returns it only if it contains `state.md` or `finalize-state.md` lines 167-172. Tests encode that invariant: newest active state wins lines 33-46; finalize is active lines 48-61; many sessions stay under 1000ms lines 63-87; older `state.md` is ignored when newest lacks active state lines 89-103; finished/cancelled markers alone are ignored lines 259-285; newest finished session suppresses older active session lines 287-302.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, which also sources `hooks/lib/template-loader.sh` during library setup lines 138-148. Uses test helper functions `pass`, `fail`, `print_test_summary`, and `setup_test_dir` from `tests/test-helpers.sh` lines 30-89. The helper behavior is consumed by validators and setup scripts, including rg hits in `hooks/loop-edit-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `scripts/setup-rlcr-loop.sh`, and `scripts/setup-pr-loop.sh`.
- edge_cases_or_failure_modes: The test covers non-existent and empty bases lines 128-148, files but no subdirectories lines 150-162, no state files lines 164-177, non-standard names lines 179-192, symlinked session directories lines 194-208, spaces in names lines 210-222, nested paths ignored because only one level is checked lines 224-238, rapid creation lines 240-257, and graceful handling of a simple session name in the “Unicode” test lines 304-316. A notable contract tension is that comments say timestamp/newest, but implementation uses lexical newest over any immediate directory; this is why non-standard names and symlinks can be returned.
- validation_or_tests: This file is itself the validation spec. It has positive active-session cases tests 1-5 lines 33-118 and negative/edge cases tests 6-18 lines 128-316. The expected behavior is tightly matched to `find_active_loop` lines 150-173 in `hooks/lib/loop-common.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 item headings above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`