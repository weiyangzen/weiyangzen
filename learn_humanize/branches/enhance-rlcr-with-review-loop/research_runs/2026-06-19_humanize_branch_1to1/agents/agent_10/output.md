# agent_10 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`
- note: read-only research pass only; no files were modified and workflow/test scripts were not executed because several assigned scripts interact with GitHub, git state, or local loop state.

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: This directory is the block-message contract for RLCR and PR-loop guard decisions. It does not perform the checks itself; it defines the operator-facing Markdown payloads used when hooks block exit, edits, writes, bash commands, pushes, stale schemas, bad file locations, incomplete todos, failed Codex review, PR trigger gaps, force-push detection, and bot reaction timeouts.
- algorithmic_behavior: Recursively inspected with no depth limit; the directory is flat and contains 31 Markdown templates. The child roles group into guard families: git/push cleanliness (`git-not-clean.md`, `git-status-failed.md`, `git-push.md`, `git-add-humanize.md`, `unpushed-commits.md`, `force-push-detected.md`), loop-owned file protection (`state-file-modification.md`, `finalize-state-file-modification.md`, `prompt-file-write.md`, `pr-loop-state-modification.md`, `pr-loop-prompt-write.md`, `plan-backup-protected.md`), round/path correctness (`wrong-round-number.md`, `wrong-round-file.md`, `wrong-directory-path.md`, `wrong-file-location.md`, `wrong-summary-location.md`), task/summary gates (`work-summary-missing.md`, `summary-bash-write.md`, `todos-file-access.md`, `incomplete-todos.md`, `large-files.md`), goal-tracker gates (`goal-tracker-not-initialized.md`, `goal-tracker-modification.md`, `goal-tracker-bash-write.md`), and PR review-loop gates (`no-trigger-comment.md`, `claude-eyes-timeout.md`, `codex-review-failed.md`).
- inputs_outputs_state: Inputs are template variables in `{{UPPER_CASE}}` form, rendered by `hooks/lib/template-loader.sh`. The loader resolves the template directory from `hooks/lib` to `prompt-template` at `hooks/lib/template-loader.sh:24-31`, loads files at `hooks/lib/template-loader.sh:36-47`, and renders placeholders with a single-pass awk substitution at `hooks/lib/template-loader.sh:58-132`. Outputs are Markdown strings returned to hook callers, usually embedded in JSON `reason` fields or written to stderr by validators. The templates themselves do not mutate loop state; consumers use them to block or guide the next required user action.
- gates_or_invariants: Placeholder syntax is part of the invariant: uppercase letters, numbers, and underscores only are documented at `hooks/lib/template-loader.sh:7-13`. Missing variables are intentionally left as placeholders, and value contents are not rescanned, preventing injected `{{OTHER_VAR}}` from being expanded. `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:205-221`. `tests/test-templates-comprehensive.sh:118-194` validates malformed placeholder patterns across every Markdown template, including extra braces, wrong bracket types, spacing, and lowercase placeholders.
- dependencies_and_callers: `hooks/lib/loop-common.sh` wraps many directory templates in reusable message functions: todo/prompt/state/summary/goal-tracker messages at `hooks/lib/loop-common.sh:437-495`, PR-loop state and prompt blocks at `hooks/lib/loop-common.sh:883-899`, `.humanize` git-add protection at `hooks/lib/loop-common.sh:1022-1059`, and post-round goal-tracker protection at `hooks/lib/loop-common.sh:1092-1103`. Stop hooks consume specific templates directly, for example PR-loop unpushed commits and force-push blocks at `hooks/pr-loop-stop-hook.sh:350-420`, PR trigger/Claude-eyes blocks at `hooks/pr-loop-stop-hook.sh:717-779`, and RLCR git/summary blocks at `hooks/loop-codex-stop-hook.sh:524-600`.
- edge_cases_or_failure_modes: Missing or empty templates fall back to inline messages through `load_and_render_safe` at `hooks/lib/template-loader.sh:170-203`, so a missing file should not crash a hook but may degrade message specificity. Unreplaced placeholders are warnings in the comprehensive stress pass, not immediate failures, at `tests/test-templates-comprehensive.sh:590-597`. Template content can influence user recovery instructions but not the actual guard predicate, so stale wording is a correctness risk for operator action even if the shell check is correct.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads every template, validates syntax, renders dummy values, and spot-checks real templates including `block/unpushed-commits.md` at `tests/test-templates-comprehensive.sh:517-525`. It is included in the aggregate suite at `tests/run-all-tests.sh:33-40`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for `/humanize:start-pr-loop`. It creates the active `.humanize/pr-loop/<timestamp>/` session, detects the current PR and monitored bots, fetches initial review comments, classifies startup state, writes loop state, builds the goal tracker, and emits the initial Claude-facing prompt.
- algorithmic_behavior: The script parses `--claude`, `--codex`, `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout` at `scripts/setup-pr-loop.sh:48-173`. It builds the active bot list and mention string at `scripts/setup-pr-loop.sh:192-214`, enforces that no RLCR or PR loop is already active at `scripts/setup-pr-loop.sh:220-241`, validates git/GitHub/Codex prerequisites at `scripts/setup-pr-loop.sh:243-277`, resolves the PR across current and parent repos for fork support at `scripts/setup-pr-loop.sh:283-359`, validates YAML-safe branch/model/effort fields at `scripts/setup-pr-loop.sh:361-388`, and creates the timestamped state directory at `scripts/setup-pr-loop.sh:390-400`.
- inputs_outputs_state: Inputs include CLI flags, `CLAUDE_PROJECT_DIR` or `pwd` as project root, current git branch, GitHub CLI PR data, configured bot set, and templates. Outputs are `round-0-pr-comment.md`, `state.md`, `goal-tracker.md`, and `round-0-prompt.md` under `.humanize/pr-loop/<timestamp>/`. The state file frontmatter records `current_round: 0`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, Codex model/effort/timeout, polling settings, startup case, latest commit data, and optional trigger metadata at `scripts/setup-pr-loop.sh:535-555`.
- gates_or_invariants: At least one bot flag is mandatory at `scripts/setup-pr-loop.sh:179-190`. A PR must exist and must not be merged or closed at `scripts/setup-pr-loop.sh:326-349`. Startup cases 4 and 5 require posting a new bot trigger comment before loop activation at `scripts/setup-pr-loop.sh:443-459`; when Claude is monitored, the script treats the Claude eyes reaction as mandatory and aborts with cleanup if the trigger ID cannot be retrieved or the reaction is absent at `scripts/setup-pr-loop.sh:484-520`. It deliberately avoids falling back to local time for `last_trigger_at` to avoid clock-skew filtering bugs at `scripts/setup-pr-loop.sh:479-482`.
- dependencies_and_callers: `commands/start-pr-loop.md` allows and invokes this script. The script sources `scripts/portable-timeout.sh` and `hooks/lib/template-loader.sh` at `scripts/setup-pr-loop.sh:33-42`, then `hooks/lib/loop-common.sh` at `scripts/setup-pr-loop.sh:210-214`. It depends on `gh`, `git`, `codex`, `jq`, `fetch-pr-comments.sh`, `check-pr-reviewer-status.sh`, and `check-bot-reactions.sh`. It renders PR-loop prompt templates with inline fallbacks at `scripts/setup-pr-loop.sh:632`, `scripts/setup-pr-loop.sh:678`, `scripts/setup-pr-loop.sh:720`, `scripts/setup-pr-loop.sh:767`, `scripts/setup-pr-loop.sh:834`, and `scripts/setup-pr-loop.sh:860`.
- edge_cases_or_failure_modes: Fork PRs are handled by trying the current repo first, then the parent repo, and later using the base repo for comments at `scripts/setup-pr-loop.sh:295-359`. If reviewer status lookup fails, it defaults to startup case 1 at `scripts/setup-pr-loop.sh:418-422`. If no comments are found, it selects the no-comments prompt flow and tells the user not to trigger first review manually at `scripts/setup-pr-loop.sh:643-720`; otherwise it selects the issue-fix/push/retrigger flow at `scripts/setup-pr-loop.sh:721-768`. SIGPIPE during final output is converted to a clean exit after all important state has been written at `scripts/setup-pr-loop.sh:777-778`.
- validation_or_tests: PR-loop system tests reference setup behavior, including no active bot failure, missing trigger ID failure, and goal-tracker creation integration in `tests/test-pr-loop-system.sh` per repository search hits around lines 122, 1384-1403, and 1453-1482. The script is also part of the broader PR-loop test surface listed in `tests/run-all-tests.sh:53-54`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template system, including this branch’s block templates. It verifies that prompt templates are discoverable, syntactically valid, safely renderable, and resilient to fallback and edge input cases.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` at `tests/test-templates-comprehensive.sh:15-17`, resolves `TEMPLATE_DIR` at `tests/test-templates-comprehensive.sh:59`, then runs sections for directory structure, loading all Markdown files, syntax validation, malformed-placeholder detection, render edge cases, fallback behavior, real-template integration checks, stress calls, and final summary. It exits `0` only when `TESTS_FAILED` is zero at `tests/test-templates-comprehensive.sh:619-625`.
- inputs_outputs_state: Inputs are all `*.md` files below `prompt-template`, template-loader functions, and synthetic variable assignments. Outputs are pass/fail/warn console lines and a process exit code. State is limited to local counters plus temporary directories created for malformed-placeholder probes at `tests/test-templates-comprehensive.sh:204-259` and incomplete-directory validation at `tests/test-templates-comprehensive.sh:482-490`.
- gates_or_invariants: The template directory must exist and contain `block`, `codex`, and `claude` subdirectories at `tests/test-templates-comprehensive.sh:61-80`. Every Markdown template must load and be non-empty or whitespace warning at `tests/test-templates-comprehensive.sh:87-111`. Placeholder syntax rejects extra closing braces, extra opening braces, wrong bracket types, likely unclosed placeholders, spaces inside placeholders, and lowercase placeholder names at `tests/test-templates-comprehensive.sh:122-177`. Real-template checks require expected substitutions for `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at `tests/test-templates-comprehensive.sh:492-540`.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, `find`, `grep`, `sed`, `sort`, `mktemp`, shell arrays, and standard Unix text tools. It is included in the aggregate test runner at `tests/run-all-tests.sh:33-40`, making it part of normal repository validation rather than only ad hoc coverage.
- edge_cases_or_failure_modes: The render edge cases cover empty content, single/multiline templates, empty line preservation, regex-special characters, backslashes, quotes, CJK-labeled content, ASCII status patterns, Markdown formatting, long values, multiline values, variables at line boundaries, repeated variables, and adjacent variables at `tests/test-templates-comprehensive.sh:261-438`. The stress section verifies 100 rapid render calls and attempts to render every template with dummy values at `tests/test-templates-comprehensive.sh:542-606`. Unreplaced placeholders in rendered all-template stress are warnings, not hard failures, at `tests/test-templates-comprehensive.sh:590-597`.
- validation_or_tests: This file is itself the validation surface for the template subsystem. It specifically validates the assigned `prompt-template/block/unpushed-commits.md` template with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch` at `tests/test-templates-comprehensive.sh:517-525`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Block template for the “local commits must be pushed before exit” gate. It is used when the loop requires remote-visible changes so bot reviewers or subsequent automation can inspect the latest commit set.
- algorithmic_behavior: The template renders a short Markdown block headed “Unpushed Commits Detected” and interpolates `{{AHEAD_COUNT}}` plus `{{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:1-4`. It states that `--push-every-round` is enabled and gives the required command `git push origin {{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:5-10`.
- inputs_outputs_state: Inputs are `AHEAD_COUNT` and `CURRENT_BRANCH`. Output is a Markdown reason string used by a hook block response. The template does not read git state and does not transition state itself; callers decide that the branch is ahead and emit the rendered message.
- gates_or_invariants: The invariant is that loop exit is blocked while commits are ahead of remote under push-required conditions. In RLCR, this gate is conditional on `PUSH_EVERY_ROUND == true` at `hooks/loop-codex-stop-hook.sh:548-578`. In PR-loop, pushing is always required before bots can review, so the gate checks ahead commits after clean working-tree validation at `hooks/pr-loop-stop-hook.sh:313-361`.
- dependencies_and_callers: Rendered by `load_and_render_safe` through the template loader. PR-loop caller computes ahead count using `git status -sb`, upstream comparison, `origin/<branch>`, or PR head SHA fallback for missing upstream/fork cases at `hooks/pr-loop-stop-hook.sh:318-347`, then renders this template at `hooks/pr-loop-stop-hook.sh:350-357`. RLCR caller uses `git status -sb` when push-every-round is true and renders the same template at `hooks/loop-codex-stop-hook.sh:552-566`.
- edge_cases_or_failure_modes: If the upstream is missing, PR-loop tries `origin/$CURRENT_BRANCH`; if that is also missing, it compares to the PR `headRefOid`, and if that cannot be fetched it fails closed by assuming one unpushed commit at `hooks/pr-loop-stop-hook.sh:323-344`. If `CURRENT_BRANCH` is unknown, callers may render a fallback branch value, so the template relies on caller hygiene. The command instruction assumes `origin` is the push remote, which matches the existing caller fallback logic but may be imprecise for unusual remotes.
- validation_or_tests: `tests/test-templates-comprehensive.sh` renders this exact file with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserts the heading, count, and branch appear at `tests/test-templates-comprehensive.sh:517-525`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-130 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git-state helpers used by the Humanize monitor and guard surfaces. It validates that git cleanliness/status summaries and special repository-state detection behave across normal, dirty, and abnormal repository states.
- algorithmic_behavior: The test sources shared helpers, `scripts/portable-timeout.sh`, and production `scripts/humanize.sh` at `tests/robustness/test-git-operations-robustness.sh:15-21`. It tests `humanize_parse_git_status`, whose contract is `modified|added|deleted|untracked|insertions|deletions` per `tests/robustness/test-git-operations-robustness.sh:28-46`, and `humanize_detect_git_state` in the later state-detection section at `tests/robustness/test-git-operations-robustness.sh:369-500`.
- inputs_outputs_state: Inputs are temporary git repositories created by `init_test_repo` at `tests/robustness/test-git-operations-robustness.sh:49-62`, working-tree mutations, simulated `.git` marker files/directories, and production helper output. Outputs are pass/fail records via `tests/test-helpers.sh` and final exit code from `print_test_summary` at `tests/robustness/test-git-operations-robustness.sh:502-507`. State is confined to the temporary test directory created by `setup_test_dir`, which is cleaned by trap in `tests/test-helpers.sh:84-89`.
- gates_or_invariants: Production `humanize_detect_git_state` must classify inaccessible/non-git repos, rebase, merge, shallow clone, detached HEAD, and normal state as implemented at `scripts/humanize.sh:114-162`. Production `humanize_parse_git_status` must reject non-git dirs with a sentinel, count porcelain states, treat renames as modified, and count insertions/deletions using diff shortstat at `scripts/humanize.sh:164-210`. The test asserts clean repo zeros, untracked/modified/added/deleted counts, insertion/deletion counts, and correct special-state names.
- dependencies_and_callers: Depends on `git`, shell temp directories, `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`. It is part of the aggregate test runner at `tests/run-all-tests.sh:55-65`, specifically listed at `tests/run-all-tests.sh:60`.
- edge_cases_or_failure_modes: Covered edge cases include non-git directories at `tests/robustness/test-git-operations-robustness.sh:168-178`, detached HEAD at `tests/robustness/test-git-operations-robustness.sh:181-195`, simultaneous dirty states at `tests/robustness/test-git-operations-robustness.sh:197-216`, empty repositories at `tests/robustness/test-git-operations-robustness.sh:233-246`, feature branches at `tests/robustness/test-git-operations-robustness.sh:248-263`, renames at `tests/robustness/test-git-operations-robustness.sh:265-282`, many untracked files at `tests/robustness/test-git-operations-robustness.sh:284-299`, filenames with spaces at `tests/robustness/test-git-operations-robustness.sh:301-314`, binary files at `tests/robustness/test-git-operations-robustness.sh:316-329`, staged and unstaged changes on the same file at `tests/robustness/test-git-operations-robustness.sh:331-347`, simulated rebase/merge/shallow states at `tests/robustness/test-git-operations-robustness.sh:403-464`, and permission-error tolerance at `tests/robustness/test-git-operations-robustness.sh:478-500`.
- validation_or_tests: This file is itself the validation artifact for git operation robustness. Related production callers include monitor/status code in `scripts/humanize.sh:411-413` and stop hooks that depend on git status and ahead detection, such as `hooks/loop-codex-stop-hook.sh:375-397` and `hooks/pr-loop-stop-hook.sh:283-361`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 unique item section headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`