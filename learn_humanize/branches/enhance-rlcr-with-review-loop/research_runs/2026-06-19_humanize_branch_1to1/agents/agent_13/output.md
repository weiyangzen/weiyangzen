# agent_13 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: This directory contains the plan-generation schema used before an RLCR loop starts. It has one recursive child, `prompt-template/plan/gen-plan-template.md`, which defines the canonical plan skeleton that `/humanize:gen-plan` copies into new plan files before the model replaces placeholders.
- algorithmic_behavior: `gen-plan-template.md:1-74` structures a plan into goal description, TDD-style acceptance criteria, path boundaries, feasibility hints, dependencies/sequence, and implementation notes. The acceptance criteria section requires positive and negative tests per criterion (`gen-plan-template.md:6-23`), which later gives RLCR setup and review a deterministic target surface. The path boundary section (`gen-plan-template.md:25-44`) constrains implementation scope without turning feasibility hints into requirements. The implementation notes section (`gen-plan-template.md:69-74`) prevents plan workflow markers such as `AC-`, `Milestone`, `Step`, and `Phase` from leaking into implementation code/comments.
- inputs_outputs_state: The directory itself has no runtime state. Its direct input is the static template file. Its output is a generated plan file produced by `scripts/validate-gen-plan-io.sh`, which locates `prompt-template/plan/gen-plan-template.md` (`scripts/validate-gen-plan-io.sh:133-145`), copies it to the requested output path (`scripts/validate-gen-plan-io.sh:148-149`), then appends the original draft under explicit delimiters (`scripts/validate-gen-plan-io.sh:151-157`). The command wrapper then instructs the agent to edit the generated file, preserve the original draft, and verify consistency (`commands/gen-plan.md:251-270`).
- gates_or_invariants: `commands/gen-plan.md:142-247` mirrors the same plan structure and adds generation invariants: no line-number references, no time estimates, AC format, clear dependencies, positive/negative tests, affirmative path boundaries, and complete preservation of all draft information. `.claude/CLAUDE.md:8-9` declares that this template and `commands/gen-plan.md` must stay synchronized.
- dependencies_and_callers: Primary caller is `/humanize:gen-plan`, whose frontmatter allows `scripts/validate-gen-plan-io.sh` and write/edit/read tooling (`commands/gen-plan.md:1-12`). The validation script enforces input/output existence, non-empty input, output nonexistence, output directory writeability, and template existence before copying (`scripts/validate-gen-plan-io.sh:79-146`). `tests/test-gen-plan.sh:55-180` validates command structure, allowed tools, argument hint, relevance-check agent, and version consistency around the command surface.
- edge_cases_or_failure_modes: Missing template is treated as plugin configuration error exit code 7 (`scripts/validate-gen-plan-io.sh:142-145`, `commands/gen-plan.md:38-47`). Existing output path or directory output path are rejected (`scripts/validate-gen-plan-io.sh:103-116`). A drift risk exists because the same schema is duplicated in the static template and `commands/gen-plan.md`; project rules explicitly require synchronized edits.
- validation_or_tests: `tests/test-gen-plan.sh` validates the command and agent scaffolding but does not deeply diff the template body against `commands/gen-plan.md`. The strongest enforcement is the project rule in `.claude/CLAUDE.md:8-9` plus the IO validator’s template existence/copy behavior.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-043 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor lifecycle behavior when the `.humanize` loop directory disappears during `humanize monitor rlcr`. It is not an implementation module; it creates a controlled fake RLCR state and asks a human to observe monitor shutdown.
- algorithmic_behavior: The `setup` branch creates `.humanize/rlcr/2026-01-16_99-99-99`, writes a minimal `state.md`, and writes a minimal `goal-tracker.md` (`tests/manual-monitor-test.sh:23-45`). The `delete` branch removes `.humanize` completely and prints expected monitor behavior (`tests/manual-monitor-test.sh:47-57`). The `cleanup` branch removes leftovers (`tests/manual-monitor-test.sh:58-61`). Invalid usage exits 1 with command help (`tests/manual-monitor-test.sh:63-70`).
- inputs_outputs_state: Input is one positional command: `setup`, `delete`, or `cleanup` (`tests/manual-monitor-test.sh:23`). Outputs are filesystem mutations under `.humanize/` and user-facing instructions. The state transition exercised is fake active RLCR session -> missing `.humanize` root. The expected monitor output is the clean stop message `Monitoring stopped: .humanize/rlcr directory no longer exists` (`tests/manual-monitor-test.sh:51-53`).
- gates_or_invariants: The manual pass criteria are: clean exit message, terminal scroll/cursor restoration, and no shell glob errors (`tests/manual-monitor-test.sh:51-54`). The fixture state includes `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` fields (`tests/manual-monitor-test.sh:27-32`), and a goal tracker with immutable goal and acceptance criteria headings (`tests/manual-monitor-test.sh:33-38`).
- dependencies_and_callers: The instructions depend on `source scripts/humanize.sh && humanize monitor rlcr` in a second terminal (`tests/manual-monitor-test.sh:10-13`, `tests/manual-monitor-test.sh:43-45`). It coordinates indirectly with whatever monitor implementation watches `.humanize/rlcr`, but this script does not source or call that monitor code.
- edge_cases_or_failure_modes: This script deliberately uses `rm -rf .humanize` (`tests/manual-monitor-test.sh:49`, `tests/manual-monitor-test.sh:60`), so it is unsafe outside disposable/manual test contexts. The fake `state.md` lacks YAML frontmatter separators, meaning it is targeted at monitor robustness rather than the stricter hook parsers. It does not assert automatically; success depends on human observation.
- validation_or_tests: It is itself the validation artifact. No automated assertion is present, and I did not run it because this branch export is read-only research and the script intentionally mutates/removes `.humanize`.
- skip_candidate: `yes: manual monitor harness only; it specifies lifecycle robustness but does not implement the RLCR review-loop state machine`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-073 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared hook library for RLCR and PR-loop state-machine validation. It centralizes state field constants, exit reasons, hook JSON validation, active loop discovery, state parsing, review issue detection, protected-file gates, cancellation authorization, `.humanize` git-add protection, command write detection, loop termination, and PR goal-tracker updates.
- algorithmic_behavior: Hook input is validated by `validate_hook_input`, which rejects null bytes, invalid UTF-8 when `iconv` is available, invalid JSON, and missing `tool_name` (`hooks/lib/loop-common.sh:58-95`). `is_deeply_nested` rejects JSON structures beyond a default depth of 30 via `jq` (`hooks/lib/loop-common.sh:118-132`). `find_active_loop` selects only the newest loop directory containing `state.md` or `finalize-state.md`, intentionally ignoring older zombie sessions (`hooks/lib/loop-common.sh:155-173`). `parse_state_file` is tolerant and applies defaults for some fields (`hooks/lib/loop-common.sh:207-240`), while `parse_state_file_strict` requires YAML separators, `current_round`, `max_iterations`, `review_started`, `base_branch`, numeric round/iteration fields, and boolean `review_started` (`hooks/lib/loop-common.sh:251-324`).
- inputs_outputs_state: Inputs include hook JSON, loop paths, state files, Codex review logs, Bash command strings, PR bot result JSON, and active loop directories. Outputs include exported shell variables such as `STATE_CURRENT_ROUND`, rendered block messages, review result files, renamed terminal state files, and updated PR goal trackers. `detect_review_issues` reads `$CACHE_DIR/round-N-codex-review.log`, returns 2 for missing/empty log, returns 0 and writes `$LOOP_DIR/round-N-review-result.md` when it finds a `[P0-9]` marker in the first 10 characters of a line, and returns 1 when no issue marker is found (`hooks/lib/loop-common.sh:343-385`). `end_loop` renames `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`hooks/lib/loop-common.sh:1113-1139`).
- gates_or_invariants: Protected RLCR files are identified by round-file, goal-tracker, state-file, finalize-state, and finalize-summary helpers (`hooks/lib/loop-common.sh:395-419`, `hooks/lib/loop-common.sh:498-519`). Message functions render templates with fallbacks through `load_and_render_safe` (`hooks/lib/loop-common.sh:439-495`, `hooks/lib/template-loader.sh:170-203`). Cancellation is gated by `.cancel-requested`, exact `mv state.md|finalize-state.md cancel-state.md` shape, no command substitution/backticks, no newlines, no shell chaining, no hidden variables after `$loop_dir` normalization, exactly two args, consistent quote style, exact source/destination paths, and non-symlink source (`hooks/lib/loop-common.sh:545-704`). `git_adds_humanize` blocks direct `.humanize` adds, forced broad adds, and all-scope adds when `.humanize` exists (`hooks/lib/loop-common.sh:924-1020`). `command_modifies_file` detects redirection, tee, in-place edit, mv/cp, rm, dd, truncate, printf redirection, and exec redirection patterns (`hooks/lib/loop-common.sh:1065-1090`).
- dependencies_and_callers: Sourced by RLCR validators: read (`hooks/loop-read-validator.sh:17-19`), write (`hooks/loop-write-validator.sh:17-19`), edit (`hooks/loop-edit-validator.sh:16-18`), bash (`hooks/loop-bash-validator.sh:16-18`), plan-file validator (`hooks/loop-plan-file-validator.sh:16-21`), and Codex stop hook (`hooks/loop-codex-stop-hook.sh:46-52`). It also serves PR-loop code via `hooks/pr-loop-stop-hook.sh` and setup scripts via `scripts/setup-rlcr-loop.sh:203-205` and `scripts/setup-pr-loop.sh`. External command dependencies include `jq`, `sed`, `awk`, `grep`, `iconv` when present, `git`, and the template loader.
- edge_cases_or_failure_modes: `find_active_loop` only considers the newest timestamp directory, so older valid state files are intentionally ignored (`hooks/lib/loop-common.sh:150-153`). `is_state_file_path` matches any path ending in `state.md`, so callers must check `finalize-state.md` first; several callers document that ordering. Path membership helpers use substring grep for `.humanize/rlcr/` and `.humanize/pr-loop/` (`hooks/lib/loop-common.sh:707-710`, `hooks/lib/loop-common.sh:777-786`), so they are lightweight validators rather than canonical path resolvers. `detect_review_issues` ignores markers not in the first 10 characters, by design (`hooks/lib/loop-common.sh:335-339`). Cancellation parses a lowercased command string, which simplifies matching but can be sensitive on case-sensitive filesystems.
- validation_or_tests: `tests/test-codex-review-merge.sh:5-16` documents and tests the review-log algorithm, including full-file scan, first marker extraction, and exit codes. `tests/test-state-exit-naming.sh:186-243` tests `end_loop` invalid reason, all valid reasons, and missing state file. `tests/test-humanize-escape.sh:41-145` tests `.humanize` git-add blocking variants. `tests/robustness/test-cancel-security-robustness.sh` and `tests/test-cancel-signal-file.sh` exercise `is_cancel_authorized` injection, signal, quoting, and symlink behavior. `tests/robustness/test-state-file-robustness.sh` targets strict state parsing. I did not run tests in this read-only research pass.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-103 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: User-facing block template for the read validator when an agent tries to read an RLCR round prompt/summary from the wrong location. It is a prompt contract that redirects the agent to the active loop directory.
- algorithmic_behavior: The template renders `{{FILE_PATH}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{CURRENT_ROUND}}` (`prompt-template/block/wrong-file-location.md:1-9`). It tells the agent that loop files live under the active loop directory and lists the current round prompt and summary paths (`prompt-template/block/wrong-file-location.md:5-7`).
- inputs_outputs_state: Inputs are provided by `hooks/loop-read-validator.sh` when it detects a round prompt/summary filename outside `.humanize/rlcr/`: attempted `FILE_PATH`, active loop directory, and current round (`hooks/loop-read-validator.sh:122-130`). Output is rendered text on stderr, followed by hook exit code 2 (`hooks/loop-read-validator.sh:127-131`). No persistent state is mutated by this template.
- gates_or_invariants: The gate fires only after a strict state parse succeeds and only for `round-N-summary.md` or `round-N-prompt.md` reads (`hooks/loop-read-validator.sh:69-99`). It enforces the invariant that current loop files must be read from the active session directory, not from copied paths, stale session directories, or arbitrary workspace locations. A later directory check enforces exact active-loop path equality (`hooks/loop-read-validator.sh:157-168`).
- dependencies_and_callers: Rendered through `load_and_render_safe` from `hooks/lib/template-loader.sh`; if missing, the read validator has an inline fallback (`hooks/loop-read-validator.sh:124-130`). The template is one of several block templates surfaced by `loop-common.sh` message helpers and validators.
- edge_cases_or_failure_modes: The final line says `If you need this file, use: cat {{FILE_PATH}}` (`prompt-template/block/wrong-file-location.md:9`). That is potentially confusing because the Read tool was blocked for that same path; it may be intended as a Bash read hint, but it weakens the clarity of the “wrong location” remediation compared with the listed active-loop paths.
- validation_or_tests: `tests/test-template-references.sh` is referenced by repository search as checking message-function template references, and `hooks/loop-read-validator.sh:122-131` is the direct runtime validation path. No separate behavior test for this exact template text was identified in the assigned pass.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-133 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Automated robustness specification for `scripts/setup-rlcr-loop.sh` plan-path and plan-content validation. It tests what plan paths may enter the RLCR setup state machine before any loop directory is created.
- algorithmic_behavior: The script creates an isolated temporary git repository with one commit (`tests/robustness/test-path-validation-robustness.sh:16-27`), defines `create_valid_plan` with at least five lines and meaningful content (`tests/robustness/test-path-validation-robustness.sh:28-44`), and runs production setup with `CLAUDE_PROJECT_DIR="$TEST_DIR"` (`tests/robustness/test-path-validation-robustness.sh:55-68`). It interprets setup errors matching path/content validation patterns as rejection, while later `requires codex` or `must be gitignored` messages mean path validation already passed (`tests/robustness/test-path-validation-robustness.sh:70-84`).
- inputs_outputs_state: Inputs are synthetic plan path strings. Outputs are pass/fail counters via `tests/test-helpers.sh`, production setup stdout/stderr classification, and temporary files under `TEST_DIR`. State is reset before each setup invocation by removing `$TEST_DIR/.humanize/rlcr` (`tests/robustness/test-path-validation-robustness.sh:62-63`), preventing “loop already active” from masking path validation.
- gates_or_invariants: Positive path cases include normal relative paths, root filenames, dash/underscore names, nested directories, dotted filenames, long filenames, and deep nested paths (`tests/robustness/test-path-validation-robustness.sh:101-148`, `tests/robustness/test-path-validation-robustness.sh:366-391`). Negative path cases reject absolute paths, spaces, shell metacharacters such as semicolon/pipe/dollar/backticks/angle brackets/ampersand/globs/question/backslash/tilde/parentheses/braces/brackets/hash/exclamation (`tests/robustness/test-path-validation-robustness.sh:158-312`). Symlink cases reject symlink plan files, symlink parent directories, and simple symlink chains (`tests/robustness/test-path-validation-robustness.sh:321-356`). Content cases reject empty files, comments-only files, fewer than five lines, nonexistent paths, and directories used as files (`tests/robustness/test-path-validation-robustness.sh:404-459`).
- dependencies_and_callers: Direct target is `scripts/setup-rlcr-loop.sh`, whose path validator rejects absolute paths (`scripts/setup-rlcr-loop.sh:273-277`), spaces (`scripts/setup-rlcr-loop.sh:279-285`), shell metacharacters (`scripts/setup-rlcr-loop.sh:287-294`), plan-file symlinks (`scripts/setup-rlcr-loop.sh:299-303`), missing parent directories (`scripts/setup-rlcr-loop.sh:305-310`), parent-directory symlinks (`scripts/setup-rlcr-loop.sh:312-333`), missing/unreadable files (`scripts/setup-rlcr-loop.sh:335-345`), paths outside the project after resolution (`scripts/setup-rlcr-loop.sh:347-359`), submodule paths (`scripts/setup-rlcr-loop.sh:361-374`), bad tracking state (`scripts/setup-rlcr-loop.sh:380-425`), low line count (`scripts/setup-rlcr-loop.sh:431-439`), and insufficient non-comment content (`scripts/setup-rlcr-loop.sh:441-484`). It depends on `tests/test-helpers.sh` for counters and summary (`tests/test-helpers.sh:30-78`).
- edge_cases_or_failure_modes: The test intentionally treats downstream setup failures as success for path validation, so it validates the front gate rather than end-to-end loop creation. It notes Unicode/CJK/Emoji paths are now allowed (`tests/robustness/test-path-validation-robustness.sh:393-394`) but does not include an explicit Unicode path case. It does not explicitly test `../` traversal strings, though production setup checks resolved plan path stays under `PROJECT_ROOT` (`scripts/setup-rlcr-loop.sh:347-359`). Positive tests may stop at gitignore validation rather than Codex availability depending on fixture tracking state.
- validation_or_tests: The script ends with `print_test_summary` and exits with its status (`tests/robustness/test-path-validation-robustness.sh:461-466`). I did not run it because this task requested research notes only and the workspace is read-only, while the script creates and mutates temporary git repositories.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5/5 evidence sections present; each assigned section heading appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`