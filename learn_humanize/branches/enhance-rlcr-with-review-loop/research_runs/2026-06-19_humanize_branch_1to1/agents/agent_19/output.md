# agent_19 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-019 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow contract for cancelling an active RLCR loop. It is a thin command definition that delegates all mutation to `scripts/cancel-rlcr-loop.sh`, then maps machine-readable first-line statuses into user-facing behavior.
- algorithmic_behavior: The command first runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` as its only normal action, per `commands/cancel-rlcr-loop.md:11-15`. It branches only on the script’s first output line: `NO_LOOP` / `NO_ACTIVE_LOOP` means report no loop, `CANCELLED` and `CANCELLED_FINALIZE` mean report the script’s cancellation message, and `FINALIZE_NEEDS_CONFIRM` opens an explicit user confirmation path, per `commands/cancel-rlcr-loop.md:17-22`.
- inputs_outputs_state: Input is the active project context plus optional user confirmation if Finalize Phase is detected. Output is a status report to the user. State transition is performed by the script, not the command: `scripts/cancel-rlcr-loop.sh` finds the newest `.humanize/rlcr/*/` directory, detects `state.md` or `finalize-state.md`, creates `.cancel-requested`, then renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69-99`, `scripts/cancel-rlcr-loop.sh:134-139`). Normal loop output is `CANCELLED`; forced Finalize cancellation output is `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:144-152`).
- gates_or_invariants: The command’s allowed tools are tightly scoped to the cancel script, the same script with `--force`, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:3`). Finalize Phase is protected by a confirmation gate: without `--force`, the script emits `FINALIZE_NEEDS_CONFIRM` and exits with code 2 (`scripts/cancel-rlcr-loop.sh:117-128`); the command must ask the user before force-cancelling (`commands/cancel-rlcr-loop.md:23-34`). The command states the key invariant that a loop is active only when `state.md` or `finalize-state.md` exists in the newest loop directory (`commands/cancel-rlcr-loop.md:36`).
- dependencies_and_callers: Depends directly on `scripts/cancel-rlcr-loop.sh`. Related setup and guidance references appear in `scripts/setup-rlcr-loop.sh`, which advertises `/humanize:cancel-rlcr-loop` and points users to it when another loop is active. Hook safety depends on the cancel signal: `hooks/loop-bash-validator.sh` allows authorized state-file renames only when `is_cancel_authorized` approves the command (`hooks/loop-bash-validator.sh:139-150`), and `tests/test-cancel-signal-file.sh` verifies the signal-file mechanism.
- edge_cases_or_failure_modes: If no loop directory exists, the script emits `NO_LOOP` and exits 1 (`scripts/cancel-rlcr-loop.sh:72-79`). If the newest directory exists but has no active `state.md` or `finalize-state.md`, it emits `NO_ACTIVE_LOOP` and exits 1 (`scripts/cancel-rlcr-loop.sh:85-99`). If Finalize Phase is active and the command is not forced, cancellation is intentionally delayed for confirmation. Unknown script options exit 3 (`scripts/cancel-rlcr-loop.sh:57-60`). The script uses newest directory by reverse sort of path names (`scripts/cancel-rlcr-loop.sh:72-73`), so timestamp naming order is an implicit assumption.
- validation_or_tests: Direct cancellation-signal behavior is covered by `tests/test-cancel-signal-file.sh`: it verifies `mv state.md cancel-state.md` is allowed when `.cancel-requested` is in the active loop dir (`tests/test-cancel-signal-file.sh:76-94`), a wrong-directory signal does not authorize cancellation (`tests/test-cancel-signal-file.sh:197-217`), and non-cancel mutations like `rm state.md` remain blocked even with the signal (`tests/test-cancel-signal-file.sh:219-230`). I did not execute tests because this run is read-only research notes only.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash command mutation detector `command_modifies_file` in `hooks/lib/loop-common.sh`. This is core guard behavior because `hooks/loop-bash-validator.sh` uses that helper to block unauthorized Bash writes to RLCR state, plan, goal-tracker, prompt, summary, and todos files.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh` (`tests/test-bash-validator-patterns.sh:11-13`), lowercases each command with `to_lower`, then calls `command_modifies_file "$command_lower" "$pattern"` through positive and negative assertion helpers (`tests/test-bash-validator-patterns.sh:36-62`). The tested helper itself evaluates a fixed list of regex families: redirection, `tee`, in-place `sed` / `awk` / `perl`, `mv` / `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` (`hooks/lib/loop-common.sh:1062-1090`).
- inputs_outputs_state: Inputs are shell command strings and a file regex pattern, defaulting to `goal-tracker\.md` (`tests/test-bash-validator-patterns.sh:37-55`). Outputs are process-level pass/fail results and final exit code: the script accumulates `TESTS_PASSED` / `TESTS_FAILED` and exits 0 only when failures are zero (`tests/test-bash-validator-patterns.sh:20-34`, `tests/test-bash-validator-patterns.sh:197-214`). It does not mutate repository state; it validates detector semantics.
- gates_or_invariants: Core invariant is that write-like Bash commands targeting protected loop files must match, while read-only commands must not. Positive gates cover `>` / `>>` redirects (`tests/test-bash-validator-patterns.sh:69-80`), `tee` (`tests/test-bash-validator-patterns.sh:82-93`), in-place editors (`tests/test-bash-validator-patterns.sh:95-107`), file operations (`tests/test-bash-validator-patterns.sh:109-121`), and lower-level modifiers (`tests/test-bash-validator-patterns.sh:123-133`). False-positive protection covers `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, and `diff` (`tests/test-bash-validator-patterns.sh:135-153`).
- dependencies_and_callers: The tested helper is consumed by `hooks/loop-bash-validator.sh`. That hook blocks `finalize-state.md` and `state.md` mutations, with cancel-signal exceptions (`hooks/loop-bash-validator.sh:139-150`), blocks `.humanize/rlcr/.../plan.md` writes (`hooks/loop-bash-validator.sh:330-340`), blocks Bash writes to `goal-tracker.md` (`hooks/loop-bash-validator.sh:343-358`), and blocks prompt, summary, and todos file writes (`hooks/loop-bash-validator.sh:360-393`). The test also depends on `to_lower`, which normalizes case before matching (`tests/test-bash-validator-patterns.sh:40-43`, `tests/test-bash-validator-patterns.sh:52-57`).
- edge_cases_or_failure_modes: The test explicitly records a known limitation: `cp file1.md file2.md goal-tracker.md` is not detected because the regex expects a two-operand `cp src dest` shape (`tests/test-bash-validator-patterns.sh:165-168`). Variable indirection is not detected, e.g. `echo x > $FILE` is expected not to match (`tests/test-bash-validator-patterns.sh:170-172`). Edge coverage includes bare redirection `> goal-tracker.md`, no-space redirection `>goal-tracker.md`, custom `state\.md` patterns, and `round-[0-9]+-summary\.md` patterns (`tests/test-bash-validator-patterns.sh:155-194`).
- validation_or_tests: This file is itself the validator test. It provides focused regression coverage for the regex detector used by the Bash hook. I inspected it directly but did not execute it due to the research-only, read-only instruction.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the `.humanize/` commit-surface guard. It explains why certain `git add` commands are blocked and supplies allowed alternatives, preserving the invariant that local RLCR/PR loop state must not enter version control.
- algorithmic_behavior: The template is loaded by `git_add_humanize_blocked_message` in `hooks/lib/loop-common.sh`, which uses `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` with an inline fallback (`hooks/lib/loop-common.sh:1022-1060`). The detector `git_adds_humanize` scans shell command segments split on operators, finds `git ... add`, normalizes quoted args, and returns true for direct `.humanize` references, forced broad adds, all-scope adds when `.humanize` exists, or broad-scope adds when `.humanize` is not gitignored (`hooks/lib/loop-common.sh:902-1020`).
- inputs_outputs_state: Input is a blocked Bash command that would add `.humanize` state. The template output is a Markdown block beginning `# Git Add Blocked: .humanize Protection` and then lists allowed specific-path staging commands (`prompt-template/block/git-add-humanize.md:1-15`) and blocked broad/direct patterns (`prompt-template/block/git-add-humanize.md:16-25`). It does not change state; it is emitted to stderr by `hooks/loop-bash-validator.sh` before exit code 2 (`hooks/loop-bash-validator.sh:109-118`).
- gates_or_invariants: The invariant is that `.humanize/` is local loop state and should not be committed (`prompt-template/block/git-add-humanize.md:3-6`). The implementation blocks direct `.humanize` path references regardless of flags (`hooks/lib/loop-common.sh:961-967`), detects force flags including combined forms (`hooks/lib/loop-common.sh:969-975`), detects all flags including lowercased `-A` as `-a` (`hooks/lib/loop-common.sh:977-984`), and blocks `git add -A` / `--all` whenever `.humanize` exists (`hooks/lib/loop-common.sh:999-1008`). It only blocks broad non-force `git add .` / `*` when `.humanize` exists and is not protected by `git check-ignore` (`hooks/lib/loop-common.sh:1010-1015`).
- dependencies_and_callers: Called by `hooks/loop-bash-validator.sh` early in Bash validation (`hooks/loop-bash-validator.sh:109-118`). The detector and message live in `hooks/lib/loop-common.sh`. Regression references in `tests/test-humanize-escape.sh` cover path variants, force-add variants, `git add -A` / `--all`, and non-overblocking of `.humanizeconfig` / `.humanize-backup`.
- edge_cases_or_failure_modes: The template’s instructions say `.humanize/` is already listed in `.gitignore` (`prompt-template/block/git-add-humanize.md:3-4`), but the implementation still accounts for repos where it is not ignored and for force adds that bypass ignore rules. The commit-message instruction has a subtle self-trigger concern: it tells users to commit `.gitignore` with message `Add humanize local folder into gitignore` and warns not to include the literal `.humanize` string (`prompt-template/block/git-add-humanize.md:26-34`). Detector logic intentionally avoids over-blocking similarly named files such as `.humanizeconfig` by requiring `.humanize` as a path segment (`hooks/lib/loop-common.sh:961-965`).
- validation_or_tests: `tests/test-humanize-escape.sh` is the direct regression suite for this area. Search results show it tests direct `.humanize` path variants, `git add -f .`, `git add -f *`, combined `-fA` / `-Af`, `git add -A`, `git add --all`, and allows `.humanizeconfig` / `.humanize-backup`. I did not execute the suite.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-109 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-side prompt fragment that defines the indirect update contract for `goal-tracker.md` after Round 0. It tells Claude to request changes in its summary instead of modifying the tracker directly, preserving Codex as the reviewer/updater.
- algorithmic_behavior: The template emits a required Markdown section shape only when Goal Tracker changes are needed: `## Goal Tracker Update Request`, `### Requested Changes`, and `### Justification` (`prompt-template/claude/goal-tracker-update-request.md:2-14`). The requested changes examples include marking completion with evidence, adding open issues, recording plan evolution, and deferring tasks with acceptance-criteria impact (`prompt-template/claude/goal-tracker-update-request.md:6-10`). It is inserted into Claude’s next-round prompt by `hooks/loop-codex-stop-hook.sh`, which loads `claude/goal-tracker-update-request.md` and falls back to a short one-line instruction if missing (`hooks/loop-codex-stop-hook.sh:1518-1520`).
- inputs_outputs_state: Input is Claude’s need to update the Goal Tracker after performing work. Output is not a direct file mutation; it is structured request text inside Claude’s summary. Codex later evaluates and applies justified changes. The paired Codex template makes that reviewer responsibility explicit: if Claude’s summary contains a request, Codex must evaluate justification, update `@{{GOAL_TRACKER_FILE}}` if approved, never modify the immutable section, or explain rejection (`prompt-template/codex/goal-tracker-update-section.md:1-17`).
- gates_or_invariants: This template enforces the Round > 0 write gate: Claude cannot directly modify `goal-tracker.md` after Round 0. The next-round prompt states that directly (`prompt-template/claude/next-round-prompt.md:30-31`). The Bash validator backs it with enforcement: Bash commands modifying `goal-tracker.md` are blocked; in Round 0 the message redirects to Write/Edit, while later rounds instruct the summary request path (`hooks/loop-bash-validator.sh:343-358`). The common blocked-message fallback also states that after Round 0 only Codex can modify the tracker (`hooks/lib/loop-common.sh:1092-1103`).
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` for Claude prompt generation (`hooks/loop-codex-stop-hook.sh:1518-1520`). It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, which defines Codex’s update responsibilities, and with `prompt-template/block/goal-tracker-modification.md`, which is used when Claude attempts direct modification through disallowed channels.
- edge_cases_or_failure_modes: The section is conditional: if no update is needed, Claude should omit it (`prompt-template/claude/goal-tracker-update-request.md:2`). The request can be rejected by Codex if unjustified, which is part of the paired Codex review contract (`prompt-template/codex/goal-tracker-update-section.md:11`). The template relies on Claude following a precise heading; if the heading is malformed, downstream Codex review may not recognize it reliably. The immutable Ultimate Goal / Acceptance Criteria are protected by Codex-side instructions, not by this Claude fragment itself (`prompt-template/codex/goal-tracker-update-section.md:10`).
- validation_or_tests: Template loading is indirectly covered in `tests/test-templates-comprehensive.sh` for the paired Codex goal-tracker update section, checking that the rendered output contains “Goal Tracker Update Requests” and the target tracker path (`tests/test-templates-comprehensive.sh:530-540`). The assigned Claude fragment itself is referenced by `hooks/loop-codex-stop-hook.sh`; I found no direct dedicated test for its exact heading text.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4 item evidence sections above; each assigned heading is unique`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`