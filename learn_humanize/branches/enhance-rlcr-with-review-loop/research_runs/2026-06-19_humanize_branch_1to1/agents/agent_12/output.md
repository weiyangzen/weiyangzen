# agent_12 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-012 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side review-loop prompt contract directory. These templates define how the RLCR stop hook asks Codex to audit Claude’s implementation, update goal-tracker state, decide whether work is complete, trigger stagnation stop, and document the later `codex review --base` phase.
- algorithmic_behavior: Recursively inspected files: `prompt-template/codex/code-review-phase.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`. `regular-review.md:5-11` forces Codex to read the original plan before judging Claude’s summary; `regular-review.md:20-31` requires skeptical implementation review and immediate implementation plans for deferred or unfinished work; `regular-review.md:33-47` adds goal-alignment checks; `regular-review.md:49-57` defines the `COMPLETE` terminal condition. `full-alignment-review.md:1-4` defines the mandatory full alignment checkpoint; `full-alignment-review.md:19-48` audits ACs, forgotten work, deferrals, and remaining blockers; `full-alignment-review.md:59-84` adds a stagnation circuit-breaker audit over the last five rounds; `full-alignment-review.md:86-93` defines `STOP` and `COMPLETE` output semantics. `goal-tracker-update-section.md:1-17` delegates post-round goal-tracker mutations to Codex, while forbidding immutable-section edits at line 10. `code-review-phase.md:12-18` documents the later automated code-review phase: run `codex review --base`, scan for `[P0-9]`, remediate if found, finalize if clean.
- inputs_outputs_state: Inputs are template variables rendered by `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `REVIEW_RESULT_FILE`, historical round values, and review/base metadata (`hooks/loop-codex-stop-hook.sh:735-812`, `896-920`). Outputs are rendered review/audit prompt files under `.humanize/rlcr/<session>/round-N-review-prompt.md` and expected review result files under `round-N-review-result.md`. State transitions are textual but binding: the stop hook reads the last non-empty Codex result line at `hooks/loop-codex-stop-hook.sh:1354-1358`; `COMPLETE` enters review/finalize flow at `1360-1400`; `STOP` ends via stop-state at `1437-1464`.
- gates_or_invariants: `COMPLETE` is legal only when all original tasks and ACs are fully met with no deferrals or pending work (`regular-review.md:53-56`, `full-alignment-review.md:91-93`). Full alignment is selected every fifth round by `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:747-751`). In review phase, a textual `COMPLETE` is ignored; absence of `[P0-9]` in `codex review` output is the real finalize gate (`hooks/loop-codex-stop-hook.sh:1361-1365`, `1409-1434`). Goal-tracker changes must not touch the immutable section (`goal-tracker-update-section.md:10`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe` for goal-tracker section (`744-745`), full alignment prompt (`784-795`), regular review prompt (`800-812`), and code-review audit prompt (`914-920`). Rendering uses `hooks/lib/template-loader.sh:50-131`, which performs single-pass `{{VAR}}` substitution and intentionally leaves missing placeholders unchanged. Review-phase issue detection depends on `hooks/lib/loop-common.sh:326-386`, where `[P0-9]` markers in the first 10 characters of a line keep the loop open.
- edge_cases_or_failure_modes: Missing templates fall back to inline fallback prompts in `hooks/loop-codex-stop-hook.sh:760-780` and `902-913`, so behavior degrades but does not hard-fail. `STOP` outside full-alignment rounds is treated as unusual but still honored (`hooks/loop-codex-stop-hook.sh:1452-1464`). `code-review-phase.md` documents `BASE_BRANCH`, but the hook also computes `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE` at `887-895`; those extra values are passed but not currently displayed by the template, so the audit file can omit the fixed commit base even when the command uses it.
- validation_or_tests: The runtime validation path is in the stop hook itself: prompt generation at `hooks/loop-codex-stop-hook.sh:782-813`, Codex exec result checks at `1277-1349`, last-line marker parsing at `1354-1365`, and review-output issue detection through `run_and_handle_code_review` at `965-993`. Template loading is protected by fallback behavior in `hooks/lib/template-loader.sh:170-203`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Phase-1 gate and initializer for `/humanize:gen-plan`. It validates user-supplied draft/output paths before the command proceeds, then seeds the output plan file with the plan template plus the original draft content.
- algorithmic_behavior: Argument parsing accepts only `--input`, `--output`, and help flags (`scripts/validate-gen-plan-io.sh:30-56`). Required argument checks run at `58-67`; paths are normalized with `realpath -m` when available at `69-72`. The validation sequence is deterministic: input must exist (`79-85`), input must be non-empty (`87-93`), output directory must exist (`95-101`), output path must not be a directory or existing file (`103-116`), and output directory must be writable (`118-124`). After validation, it locates `prompt-template/plan/gen-plan-template.md` via `CLAUDE_PLUGIN_ROOT` or script-relative fallback (`133-140`), errors if missing (`142-146`), copies the template to the target (`148-149`), and appends the original draft between start/end markers (`151-157`).
- inputs_outputs_state: Inputs are `--input <draft>` and `--output <plan>`, plus optional environment variable `CLAUDE_PLUGIN_ROOT`. Outputs are stdout/stderr diagnostic messages, exit codes `0-7` documented at `4-12`, and on success a newly created output plan file. Persistent state is limited to that output file; no loop state is changed.
- gates_or_invariants: The output file must be new; existing files and directory paths both exit `4` (`103-116`). The script does not create missing output directories, even though it prints a suggested `mkdir -p` command (`95-100`). Missing or malformed arguments always route through `usage`, which exits `6` (`16-24`, `58-67`). Template existence is a plugin-configuration hard gate with exit `7` (`142-146`).
- dependencies_and_callers: `commands/gen-plan.md:30-47` calls `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` and maps each exit code to command-level stop behavior. The script depends on common shell tools: `realpath`, `dirname`, `wc`, `cp`, and `cat`; the `realpath` failure path preserves the original path string (`69-71`). It depends on `prompt-template/plan/gen-plan-template.md`, whose required plan sections start at `prompt-template/plan/gen-plan-template.md:1-74`.
- edge_cases_or_failure_modes: There is a time-of-check/time-of-use race between the nonexistence checks and `cp` to `OUTPUT_FILE`; a concurrent creator could still interfere after `111-116`. If `realpath -m` is unavailable, relative paths remain unnormalized, which can make diagnostics less canonical but not skip validation. The help path exits with `6`, the same as invalid arguments (`48-50`), which is intentional per command documentation but unusual for CLI help. Write-permission validation may be platform/user dependent, especially under elevated privileges.
- validation_or_tests: `tests/test-gen-plan.sh:426-542` provides direct executable coverage for missing option values, flag-as-value, unknown option, missing input, empty input, missing output directory, existing output file, output directory path, valid success, and `--help`. Gaps visible from that block: no direct test for exit `5` no-write-permission and no direct test for exit `7` missing plan template.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for zsh runtime safety of the humanize monitor, especially avoiding zsh `nomatch` failures when loop/cache directories are empty, missing, or contain only nonmatching files.
- algorithmic_behavior: The test must run under zsh (`tests/test-zsh-monitor-safety.sh:1-9`, `276-286`) and uses `set -euo pipefail` (`12-13`). It creates an isolated temp workspace and cache (`48-55`) and cleans up on exit (`56-60`). Test 1 sources `scripts/humanize.sh` and fails if output contains `no matches found` (`63-77`). Tests 2-3 model latest-session discovery using `find` over empty and dotfile-only `.humanize/rlcr` directories (`80-157`). Test 4 models state-file discovery with no `*-state.md` matches (`160-205`). Test 5 models Codex log discovery with an empty cache directory (`208-237`). Test 6 verifies timestamped session ordering (`240-273`). Test 8 demonstrates that the old glob pattern would be unsafe while the `find` pattern is safe (`288-321`).
- inputs_outputs_state: Inputs are the checked-out `scripts/humanize.sh`, a synthetic `.humanize/rlcr` tree, and synthetic `$XDG_CACHE_HOME/humanize/...` cache directories. Outputs are PASS/FAIL counters and process exit `0` only when `TESTS_FAILED == 0` (`323-341`). State is isolated under `/tmp/test-zsh-humanize-$$` and removed by trap.
- gates_or_invariants: Monitor directory scanning must use `find` instead of bare globs, so empty dirs do not trigger zsh `no matches found`. Session directories must match `YYYY-MM-DD_HH-MM-SS` exactly (`109`, `145`, `259`). Missing state/log files must produce empty or unknown results, not shell errors. The script also gates that the test is actually running in zsh via `$ZSH_VERSION` (`282-286`).
- dependencies_and_callers: It directly sources `scripts/humanize.sh` (`72`, `92`) and mirrors production helper logic from `scripts/lib/monitor-common.sh:37-63` and `147-188`. The production monitor’s log search also uses `find` for both run and review logs in `scripts/humanize.sh:280-339`. It requires `zsh`, `find`, standard POSIX utilities, and a writable `/tmp`.
- edge_cases_or_failure_modes: Several tests reimplement production helper patterns locally instead of invoking only exported production functions, so they strongly specify the intended algorithm but can drift if helper internals evolve. Test 8 disables strict nomatch locally (`300-302`), so it demonstrates the historical failure mode rather than asserting the old pattern actually fails under the current shell options. It does not exercise the full terminal UI loop; that deeper monitor behavior is covered by the robustness test item below.
- validation_or_tests: This file is itself the validation artifact. It specifically validates no zsh glob errors when sourcing `humanize.sh`, scanning empty loop directories, scanning dotfile-only directories, scanning no matching state/log files, selecting the latest timestamped session, and using `find` instead of glob expansion.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Blocking-message template for hook-enforced canonical loop-file location. It tells the agent that a round file path is in the wrong directory and gives the active-loop path that must be used instead.
- algorithmic_behavior: The template has three substitutions: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:3-4`). It is rendered when a read or write targets a round file name that belongs to the active loop but the full path does not exactly equal the active loop directory plus filename. `hooks/loop-read-validator.sh:157-167` constructs `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and exits `2` after rendering the block. `hooks/loop-write-validator.sh:252-262` uses the same invariant for writes.
- inputs_outputs_state: Inputs are the attempted action, attempted path, and computed correct active-loop path. Output is a rendered Markdown block sent to stderr by the validator, followed by hook exit `2`. It does not mutate repository state; it blocks the tool invocation.
- gates_or_invariants: The invariant is exact string equality between the attempted file path and `ACTIVE_LOOP_DIR/<round-file-basename>` (`hooks/loop-read-validator.sh:159`, `hooks/loop-write-validator.sh:254`). It complements earlier gates for wrong location and wrong round number in `loop-read-validator.sh:122-151` and `loop-write-validator.sh:202-245`.
- dependencies_and_callers: Rendered through `load_and_render_safe` in the read and write validators; the renderer is `hooks/lib/template-loader.sh:170-203`. The active loop and current round come from `find_active_loop` and strict state parsing in `hooks/lib/loop-common.sh:150-173` and `242-324`.
- edge_cases_or_failure_modes: Line 6 says `If you need this file, use: cat {{FILE_PATH}}`; because `FILE_PATH` is the rejected path, this may be a prompt-copy bug and likely should reference `{{CORRECT_PATH}}` instead. The exact string comparison can block semantically equivalent paths involving symlinks, relative path segments, or alternate normalization. The template is not used by `loop-edit-validator.sh`, which validates some path/round invariants but has no equivalent wrong-directory final check.
- validation_or_tests: The template reference is exercised indirectly by validators. The assigned robustness tests verify validator JSON parsing and hook failure behavior around production validators, while template existence is covered by the hook rendering path. Direct line-level caller references are `hooks/loop-read-validator.sh:163-166` and `hooks/loop-write-validator.sh:258-261`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-132 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Broad executable robustness specification for hook input parsing, shell-command modification detection, and monitor edge cases. It pipes JSON into production validators and invokes production monitor/parser helpers.
- algorithmic_behavior: Setup sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, then creates an isolated test dir (`tests/robustness/test-hook-input-robustness.sh:14-19`). Hook parsing tests pipe valid Read, Write, and Bash JSON to real validators and expect pass-through (`35-69`). They then require graceful rejection for malformed JSON (`71-85`), missing `tool_name` (`87-100`), missing Read `tool_input.file_path` (`102-115`), deeply nested JSON over depth 30 (`177-199`), and non-UTF8 binary content (`201-217`). They require acceptance for a 10KB Bash command (`117-133`), special shell characters (`135-147`), Unicode path content (`149-161`), and unknown tool names (`163-175`). Null-byte behavior is explicitly documented as “bash strips them,” so the invariant is graceful no-signal handling rather than mandatory rejection (`219-235`). Monitor edge tests cover terminal width, log growth/deletion, ANSI stripping, binary logs, narrow/wide formatting, and rapid writes (`245-341`). Command pattern tests assert `command_modifies_file` detects `sed -i` and redirection (`351-369`). Production monitor integration tests source `scripts/humanize.sh`, call `humanize_parse_goal_tracker`, `humanize_parse_git_status`, and `humanize_detect_git_state` (`381-433`), then run `_humanize_monitor_codex` through wrapper scripts for no-session, narrow-terminal, and ANSI-log cases (`444-664`).
- inputs_outputs_state: Inputs are generated JSON payloads, synthetic goal-tracker files, synthetic `.humanize/rlcr` sessions, fake `$HOME`/`$XDG_CACHE_HOME`, and fake terminal functions. The validators consume JSON on stdin with `CLAUDE_PROJECT_DIR="$TEST_DIR"`. Outputs are PASS/FAIL lines and `print_test_summary` exit status at `670-671`. State is temporary and test-local; wrapper scripts create and delete fake loop directories to force monitor transitions.
- gates_or_invariants: Production validators must fail closed on invalid JSON, missing required fields, excessive JSON depth, and invalid UTF-8; they must pass through unrecognized tools and valid non-target paths. These gates map to `validate_hook_input`, `require_tool_input_field`, and `is_deeply_nested` in `hooks/lib/loop-common.sh:49-132`. Monitor behavior must exit gracefully with codes below 128 when sessions are missing or deleted and when logs include ANSI escapes. Command mutation detection must recognize in-place edits and output redirection through `command_modifies_file` (`hooks/lib/loop-common.sh:1062-1090`).
- dependencies_and_callers: Direct dependencies are `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/lib/loop-common.sh`, `scripts/humanize.sh`, and `tests/test-helpers.sh`. The tested validators themselves depend on `jq`; UTF-8 rejection uses `iconv` when available (`hooks/lib/loop-common.sh:68-76`). Monitor wrappers depend on bash, `mktemp`, `sed`, terminal shims, and fake cache paths matching `scripts/humanize.sh:250-256`.
- edge_cases_or_failure_modes: The test intentionally accepts stripped-null-byte behavior because Bash command substitution cannot preserve null bytes (`tests/robustness/test-hook-input-robustness.sh:219-235`), leaving a known detection limitation outside the hook’s visibility. The first few validator tests reuse `EXIT_CODE` with `${EXIT_CODE:-0}`; after an early failure, a later successful command could inherit a stale nonzero value, causing cascading harness failures. The test creates wrapper scripts with `cat >` inside temp dirs; that is fine for execution but means the spec assumes writable temp storage. It checks monitor crash-safety, not visual correctness of the rendered monitor screen.
- validation_or_tests: This file is itself the validation artifact and runs production paths rather than mocks for the main hook validators. It validates the assigned hook-input behavior against `hooks/lib/loop-common.sh:49-132`, mutation pattern behavior against `hooks/lib/loop-common.sh:1062-1090`, goal/git parser behavior against `scripts/humanize.sh:30-210`, and monitor graceful-exit behavior against `_humanize_monitor_codex` in `scripts/humanize.sh:219-830`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique Item Evidence headings present; item_id occurrences are limited to those headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`