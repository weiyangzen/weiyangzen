# agent_11 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`
- notes: Research-only pass. I inspected the assigned files/directories directly and followed the relevant runtime references, especially `hooks/loop-codex-stop-hook.sh`, `hooks/check-todos-from-transcript.py`, `scripts/humanize.sh`, and `hooks/lib/loop-common.sh`.

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Runtime prompt-template directory for the Claude side of the RLCR state machine. These templates shape what Claude is told after Codex review, during post-alignment remediation, during code-review repair, and during finalization. The directory is algorithmic because the hook renders these templates into loop prompts and JSON block reasons that control whether the loop continues, transitions phase, or finalizes.
- algorithmic_behavior: The directory contains eight templates. `next-round-prompt.md` tells Claude to re-read the original plan, create todos for all discovered issues, consume Codex review content, and treat `goal-tracker.md` as read-only after round 0 (`prompt-template/claude/next-round-prompt.md:1`, `prompt-template/claude/next-round-prompt.md:12`, `prompt-template/claude/next-round-prompt.md:16`, `prompt-template/claude/next-round-prompt.md:22`). `next-round-footer.md` adds invariant exit instructions: do not falsify loop exit, edit state, or cancel; commit; write the next summary (`prompt-template/claude/next-round-footer.md:4`, `prompt-template/claude/next-round-footer.md:6`). `push-every-round-note.md` is conditionally appended when push-every-round mode is active (`prompt-template/claude/push-every-round-note.md:2`). `goal-tracker-update-request.md` defines the required summary block for proposed goal-tracker updates, with Codex as the approving updater (`prompt-template/claude/goal-tracker-update-request.md:2`, `prompt-template/claude/goal-tracker-update-request.md:16`). `post-alignment-action-items.md` is injected after a full alignment check to force attention to forgotten items, unmet acceptance criteria, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2`). `review-phase-prompt.md` is the code-review repair prompt: fix severity-marked review issues only, commit, summarize, and continue until code review passes (`prompt-template/claude/review-phase-prompt.md:1`, `prompt-template/claude/review-phase-prompt.md:11`, `prompt-template/claude/review-phase-prompt.md:25`). `finalize-phase-prompt.md` requires a final simplification/refactor pass through the code-simplifier agent after Codex review passes, under no-behavior-change and tests-still-pass constraints (`prompt-template/claude/finalize-phase-prompt.md:1`, `prompt-template/claude/finalize-phase-prompt.md:9`, `prompt-template/claude/finalize-phase-prompt.md:18`, `prompt-template/claude/finalize-phase-prompt.md:41`). `finalize-phase-skipped-prompt.md` handles degraded finalization when review was skipped, requiring manual verification and preserving the same no-behavior-change constraints (`prompt-template/claude/finalize-phase-skipped-prompt.md:1`, `prompt-template/claude/finalize-phase-skipped-prompt.md:3`, `prompt-template/claude/finalize-phase-skipped-prompt.md:9`, `prompt-template/claude/finalize-phase-skipped-prompt.md:26`).
- inputs_outputs_state: Inputs are hook-provided template variables: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `NEXT_SUMMARY_FILE`, `SUMMARY_FILE`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, and `REVIEW_SKIP_REASON`. Outputs are rendered prompt files such as `.humanize/rlcr/<timestamp>/round-N-prompt.md`, rendered JSON block reasons, and finalize prompts. The stop hook renders finalize templates at `hooks/loop-codex-stop-hook.sh:1035` and `hooks/loop-codex-stop-hook.sh:1063`, renders the review repair prompt at `hooks/loop-codex-stop-hook.sh:1114`, and assembles next-round prompts at `hooks/loop-codex-stop-hook.sh:1489`, `hooks/loop-codex-stop-hook.sh:1505`, `hooks/loop-codex-stop-hook.sh:1510`, and `hooks/loop-codex-stop-hook.sh:1518`.
- gates_or_invariants: The templates enforce: summary-before-exit; no state-file tampering; no loop cancellation as a fake success path; all discovered tasks must become todos; goal-tracker direct edits stop after round 0; review-phase work must be fixes only; finalization must preserve behavior and tests. They also coordinate with the hook’s state transitions: next round increments at `hooks/loop-codex-stop-hook.sh:1471`, review phase starts after implementation completion at `hooks/loop-codex-stop-hook.sh:1387`, and finalize phase starts after code review passes at `hooks/loop-codex-stop-hook.sh:991`.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`. Template loading depends on shared template functions from `hooks/lib/loop-common.sh`; missing templates are handled through `load_and_render_safe` fallbacks or explicit inline fallbacks in the hook. These Claude templates coordinate with sibling Codex templates under `prompt-template/codex`, especially regular review, full-alignment review, and goal-tracker update instructions, plus block templates under `prompt-template/block`.
- edge_cases_or_failure_modes: Missing optional snippets do not break prompt generation: the hook substitutes fallback text for post-alignment, push-every-round, and goal-tracker-update snippets (`hooks/loop-codex-stop-hook.sh:1511`, `hooks/loop-codex-stop-hook.sh:1519`). The skipped-finalize template remains present for defensive compatibility even though setup now tries to require a valid local base branch. Prompt correctness relies on placeholder rendering; malformed or missing template variables could produce weak instructions rather than shell failures. The templates themselves do not validate commits, pushes, todos, or test results; they instruct Claude, while hooks enforce selected gates.
- validation_or_tests: Indirect coverage comes from hook/finalize/template tests found by reference scan, especially `tests/test-finalize-phase.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-template-references.sh`. The assigned directory has no standalone executable test, but its runtime call sites are explicit in `hooks/loop-codex-stop-hook.sh`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-041 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrapper for `/humanize:start-rlcr-loop`. It validates the input plan and repository, prevents conflicting loops, captures initial branch/base state, creates `.humanize/rlcr/<timestamp>/` state, seeds `goal-tracker.md`, backs up the plan, and emits the round-0 prompt. This is the entry point that turns a plan file into an active RLCR state machine.
- algorithmic_behavior: Argument parsing accepts one plan file via positional or `--plan-file`, plus `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--track-plan-file`, and `--base-branch` (`scripts/setup-rlcr-loop.sh:111`, `scripts/setup-rlcr-loop.sh:116`, `scripts/setup-rlcr-loop.sh:128`, `scripts/setup-rlcr-loop.sh:143`, `scripts/setup-rlcr-loop.sh:155`, `scripts/setup-rlcr-loop.sh:159`, `scripts/setup-rlcr-loop.sh:167`, `scripts/setup-rlcr-loop.sh:171`). It rejects concurrent RLCR or PR loops using `find_active_loop` and `find_active_pr_loop` (`scripts/setup-rlcr-loop.sh:203`, `scripts/setup-rlcr-loop.sh:213`). It validates the repo, plan path, plan tracking policy, plan content, Codex availability, branch/model/effort safety, and base branch. It then creates `LOOP_DIR`, copies the plan, writes `state.md`, writes a structured goal tracker, writes `round-0-prompt.md`, conditionally appends push instructions, and prints activation text plus the initial prompt (`scripts/setup-rlcr-loop.sh:610`, `scripts/setup-rlcr-loop.sh:616`, `scripts/setup-rlcr-loop.sh:619`, `scripts/setup-rlcr-loop.sh:628`, `scripts/setup-rlcr-loop.sh:650`, `scripts/setup-rlcr-loop.sh:750`, `scripts/setup-rlcr-loop.sh:809`, `scripts/setup-rlcr-loop.sh:825`).
- inputs_outputs_state: Inputs are CLI options, `CLAUDE_PROJECT_DIR` or current directory, the plan file, git repository state, `.gitmodules`, local and remote branch refs, and availability of `codex`. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `.humanize/rlcr/<timestamp>/state.md`, `.humanize/rlcr/<timestamp>/goal-tracker.md`, `.humanize/rlcr/<timestamp>/round-0-prompt.md`, and stdout activation instructions. `state.md` records `current_round: 0`, max iterations, Codex model/effort/timeout, push mode, plan path/tracking, start/base branch, captured base commit, `review_started: false`, and UTC start time (`scripts/setup-rlcr-loop.sh:628`, `scripts/setup-rlcr-loop.sh:630`, `scripts/setup-rlcr-loop.sh:636`, `scripts/setup-rlcr-loop.sh:638`, `scripts/setup-rlcr-loop.sh:640`, `scripts/setup-rlcr-loop.sh:641`). The goal tracker has immutable goal/acceptance sections and mutable plan/task/verification/deferred/open-issue sections (`scripts/setup-rlcr-loop.sh:652`, `scripts/setup-rlcr-loop.sh:666`, `scripts/setup-rlcr-loop.sh:710`).
- gates_or_invariants: It enforces one active loop at a time (`scripts/setup-rlcr-loop.sh:211`), exactly one plan source (`scripts/setup-rlcr-loop.sh:234`), git repo with at least one commit (`scripts/setup-rlcr-loop.sh:257`), relative plan paths only (`scripts/setup-rlcr-loop.sh:273`), no spaces (`scripts/setup-rlcr-loop.sh:279`), no shell metacharacters (`scripts/setup-rlcr-loop.sh:287`), no symlinked plan or symlinked parent path (`scripts/setup-rlcr-loop.sh:299`, `scripts/setup-rlcr-loop.sh:312`), existing/readable/in-project plan file (`scripts/setup-rlcr-loop.sh:335`, `scripts/setup-rlcr-loop.sh:341`, `scripts/setup-rlcr-loop.sh:347`), no submodule plan path (`scripts/setup-rlcr-loop.sh:361`), strict tracked-vs-gitignored plan policy (`scripts/setup-rlcr-loop.sh:400`, `scripts/setup-rlcr-loop.sh:416`), at least five physical lines and at least three non-comment content lines (`scripts/setup-rlcr-loop.sh:431`, `scripts/setup-rlcr-loop.sh:441`, `scripts/setup-rlcr-loop.sh:479`), Codex CLI availability (`scripts/setup-rlcr-loop.sh:486`), YAML-safe branch/model/effort strings (`scripts/setup-rlcr-loop.sh:504`, `scripts/setup-rlcr-loop.sh:514`, `scripts/setup-rlcr-loop.sh:523`, `scripts/setup-rlcr-loop.sh:588`), and a local base branch or explicit failure (`scripts/setup-rlcr-loop.sh:537`, `scripts/setup-rlcr-loop.sh:556`, `scripts/setup-rlcr-loop.sh:576`).
- dependencies_and_callers: Called by the command surface in `commands/start-rlcr-loop.md`, which allows the setup script as a Bash tool and invokes it with user arguments (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:13`). It sources `scripts/portable-timeout.sh` for bounded git/Codex-related checks (`scripts/setup-rlcr-loop.sh:26`, `scripts/portable-timeout.sh:31`) and `hooks/lib/loop-common.sh` for active-loop detection (`scripts/setup-rlcr-loop.sh:203`). Its state is consumed by `hooks/loop-codex-stop-hook.sh`, whose schema validation expects fields such as `review_started`, `base_branch`, and `max_iterations` (`hooks/lib/loop-common.sh:242`, `hooks/lib/loop-common.sh:293`, `hooks/lib/loop-common.sh:297`).
- edge_cases_or_failure_modes: The content validator treats markdown heading lines beginning with `#` as comments, so a heading-heavy but otherwise sparse plan can fail content validation (`scripts/setup-rlcr-loop.sh:441`, `scripts/setup-rlcr-loop.sh:471`). Remote-only base branches are rejected with a fetch instruction because later `codex review --base` needs a local ref (`scripts/setup-rlcr-loop.sh:543`). If no remote default/local `main`/local `master` can be used, setup fails closed (`scripts/setup-rlcr-loop.sh:576`). Plan path `..` escape attempts are caught by real-path containment after directory resolution (`scripts/setup-rlcr-loop.sh:350`, `scripts/setup-rlcr-loop.sh:356`). Git timeouts fail closed in plan status/tracking checks (`scripts/setup-rlcr-loop.sh:380`, `scripts/setup-rlcr-loop.sh:389`). The script captures a fixed `base_commit`, preventing later self-diff collapse when working on the base branch itself (`scripts/setup-rlcr-loop.sh:596`). A detached start state is not explicitly rejected if `git rev-parse --abbrev-ref HEAD` returns a YAML-safe string.
- validation_or_tests: Referenced validation includes `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, `tests/robustness/test-path-validation-robustness.sh`, and `tests/robustness/test-base-branch-detection.sh`. Runtime follow-on behavior is covered by stop-hook/finalize tests. I did not execute tests because this assignment requested research notes only.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for the todo-completion gate that runs before expensive Codex review. It verifies `hooks/check-todos-from-transcript.py`, which reads Claude transcript JSONL and blocks loop exit when the latest TodoWrite state has incomplete items.
- algorithmic_behavior: The test script creates temporary transcript files, pipes hook-input JSON into the Python checker, captures exit codes/output, and increments pass/fail counters (`tests/test-todo-checker.sh:36`, `tests/test-todo-checker.sh:51`, `tests/test-todo-checker.sh:305`). It validates four behavior groups. Input handling: invalid JSON and empty stdin must exit 2, missing `transcript_path` and nonexistent transcript must exit 0 (`tests/test-todo-checker.sh:51`, `tests/test-todo-checker.sh:63`, `tests/test-todo-checker.sh:75`, `tests/test-todo-checker.sh:87`). Todo detection: all completed exits 0; pending exits 1; output includes the incomplete task; `in_progress` is incomplete (`tests/test-todo-checker.sh:106`, `tests/test-todo-checker.sh:121`, `tests/test-todo-checker.sh:136`, `tests/test-todo-checker.sh:144`). Transcript variants: empty transcript exits 0, invalid JSONL lines are ignored, multiple TodoWrite calls use the latest, direct `tool_use` format works, and `type: message` format works (`tests/test-todo-checker.sh:166`, `tests/test-todo-checker.sh:179`, `tests/test-todo-checker.sh:196`, `tests/test-todo-checker.sh:212`, `tests/test-todo-checker.sh:227`). Edge cases: missing status is incomplete, empty content is still incomplete, and a completed unicode-named test case exits 0 (`tests/test-todo-checker.sh:249`, `tests/test-todo-checker.sh:265`, `tests/test-todo-checker.sh:280`).
- inputs_outputs_state: Inputs are synthetic JSONL transcript files under a temp dir and stdin JSON containing `transcript_path`. Output is the checker exit code plus stdout/stderr, interpreted by the shell test into pass/fail counters. The production checker returns exit 0 for all complete or no todos, exit 1 for incomplete todos, and exit 2 for hook-input parse errors (`hooks/check-todos-from-transcript.py:7`). It prints `INCOMPLETE_TODOS` and the incomplete list before exit 1 (`hooks/check-todos-from-transcript.py:121`). State is transient in `TESTS_PASSED`, `TESTS_FAILED`, and files under `mktemp -d`.
- gates_or_invariants: The underlying runtime gate is in `hooks/loop-codex-stop-hook.sh`: it runs the checker before Codex review, blocks on parse error, and blocks immediately when incomplete todos are found (`hooks/loop-codex-stop-hook.sh:315`, `hooks/loop-codex-stop-hook.sh:321`, `hooks/loop-codex-stop-hook.sh:328`, `hooks/loop-codex-stop-hook.sh:347`). The core invariant is that every todo in the most recent TodoWrite call must have `status == "completed"`; any other status, including missing status, blocks (`hooks/check-todos-from-transcript.py:113`, `hooks/check-todos-from-transcript.py:118`).
- dependencies_and_callers: Depends directly on `hooks/check-todos-from-transcript.py` (`tests/test-todo-checker.sh:13`). Production parser supports Claude assistant-message format, alternative message format, and direct tool_use entries (`hooks/check-todos-from-transcript.py:51`, `hooks/check-todos-from-transcript.py:65`, `hooks/check-todos-from-transcript.py:78`). Runtime caller is `hooks/loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: Invalid JSONL transcript lines are intentionally ignored rather than failing (`hooks/check-todos-from-transcript.py:36`). The parser only replaces `latest_todos` when a TodoWrite call has a non-empty `todos` array (`hooks/check-todos-from-transcript.py:62`, `hooks/check-todos-from-transcript.py:75`, `hooks/check-todos-from-transcript.py:84`), so an empty later TodoWrite call would not clear a previous non-empty set; this edge is not covered by the test script. The unicode test name does not actually include non-ASCII todo text in the fixture (`tests/test-todo-checker.sh:283`), so true Unicode payload handling is only indirectly implied by Python’s UTF-8 read path (`hooks/check-todos-from-transcript.py:30`).
- validation_or_tests: This file is itself the validation harness. It exits 0 only when `TESTS_FAILED` is zero and exits 1 otherwise (`tests/test-todo-checker.sh:305`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-101 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Exit-block template for the RLCR summary gate. It tells Claude that an exit attempt is blocked because the required round or finalize summary file does not exist.
- algorithmic_behavior: The template renders a short corrective prompt: attempted exit without work summary, required action is to write `{{SUMMARY_FILE}}`, and the summary should include implementation, changed files, tests, and remaining items (`prompt-template/block/work-summary-missing.md:1`, `prompt-template/block/work-summary-missing.md:3`, `prompt-template/block/work-summary-missing.md:5`, `prompt-template/block/work-summary-missing.md:10`). The stop hook selects the expected summary path based on phase, checks file existence, renders this template, and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:585`, `hooks/loop-codex-stop-hook.sh:592`, `hooks/loop-codex-stop-hook.sh:599`, `hooks/loop-codex-stop-hook.sh:608`).
- inputs_outputs_state: Input is the `SUMMARY_FILE` placeholder, supplied as either `.humanize/rlcr/<timestamp>/round-N-summary.md` or `.humanize/rlcr/<timestamp>/finalize-summary.md` depending on whether finalize phase is active (`hooks/loop-codex-stop-hook.sh:585`). Output is rendered Markdown embedded in the hook JSON `reason`, with a phase-specific `systemMessage` (`hooks/loop-codex-stop-hook.sh:602`, `hooks/loop-codex-stop-hook.sh:608`). The template itself has no persistent state, but its gate prevents transition to Codex review or loop completion until the required summary path exists.
- gates_or_invariants: The invariant is file existence, not content quality. If the summary file is absent, the hook blocks before goal-tracker initialization checks, max-iteration handling, Codex review, review-phase code review, or finalize completion can proceed (`hooks/loop-codex-stop-hook.sh:592`, `hooks/loop-codex-stop-hook.sh:616`). Once the file exists, this specific block no longer applies; downstream gates decide whether the content is acceptable.
- dependencies_and_callers: Single primary caller is `hooks/loop-codex-stop-hook.sh`, via `load_and_render_safe` with an inline fallback (`hooks/loop-codex-stop-hook.sh:596`, `hooks/loop-codex-stop-hook.sh:599`). It coordinates with `prompt-template/claude/next-round-footer.md`, `prompt-template/claude/review-phase-prompt.md`, and finalize templates, all of which instruct Claude to create the summary file that this block enforces.
- edge_cases_or_failure_modes: An empty summary file satisfies this particular existence check, so semantic summary quality must be enforced by Codex review or later logic rather than by this template. If the template is missing, the hook fallback still blocks with a shorter message (`hooks/loop-codex-stop-hook.sh:596`). A wrong path in state variables would cause the block to request the wrong summary file, because the template trusts the hook-supplied `SUMMARY_FILE`.
- validation_or_tests: Template reference/comprehensive tests were found by scan, and stop-hook behavior exercises this path. The assigned file is a template, not a standalone test.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-131 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for goal-tracker parsing. It validates the production `humanize_parse_goal_tracker` function from `scripts/humanize.sh`, which summarizes RLCR progress for monitor/status surfaces and helps detect drift in goal/acceptance/task accounting.
- algorithmic_behavior: The test sources helpers and production `scripts/humanize.sh`, creates temporary `goal-tracker.md` variants, calls `humanize_parse_goal_tracker`, and extracts pipe-delimited fields with `parse_result` (`tests/robustness/test-goal-tracker-robustness.sh:18`, `tests/robustness/test-goal-tracker-robustness.sh:21`, `tests/robustness/test-goal-tracker-robustness.sh:24`, `tests/robustness/test-goal-tracker-robustness.sh:39`). It documents the parser contract as `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` (`tests/robustness/test-goal-tracker-robustness.sh:35`). Positive cases cover list-format acceptance criteria, table-format acceptance criteria, active task counting excluding completed/deferred rows, completed task count, unique completed acceptance criteria, and goal summary extraction (`tests/robustness/test-goal-tracker-robustness.sh:60`, `tests/robustness/test-goal-tracker-robustness.sh:89`, `tests/robustness/test-goal-tracker-robustness.sh:118`, `tests/robustness/test-goal-tracker-robustness.sh:155`, `tests/robustness/test-goal-tracker-robustness.sh:194`, `tests/robustness/test-goal-tracker-robustness.sh:204`). Negative/edge cases cover missing files, empty files, 60 criteria, special characters, malformed headers, truncated files, embedded binary bytes, open issues, deferred tasks, header-only files, mixed AC formats, and long-goal truncation (`tests/robustness/test-goal-tracker-robustness.sh:237`, `tests/robustness/test-goal-tracker-robustness.sh:246`, `tests/robustness/test-goal-tracker-robustness.sh:258`, `tests/robustness/test-goal-tracker-robustness.sh:281`, `tests/robustness/test-goal-tracker-robustness.sh:304`, `tests/robustness/test-goal-tracker-robustness.sh:328`, `tests/robustness/test-goal-tracker-robustness.sh:349`, `tests/robustness/test-goal-tracker-robustness.sh:373`, `tests/robustness/test-goal-tracker-robustness.sh:411`, `tests/robustness/test-goal-tracker-robustness.sh:441`, `tests/robustness/test-goal-tracker-robustness.sh:477`, `tests/robustness/test-goal-tracker-robustness.sh:501`).
- inputs_outputs_state: Inputs are generated markdown goal tracker files in `TEST_DIR`. Output is the parser’s pipe-delimited summary, then pass/fail output through helper functions. Production parser returns default `0|0|0|0|0|0|No goal tracker` for a missing file (`scripts/humanize.sh:32`, `scripts/humanize.sh:34`). It counts total acceptance criteria from the `### Acceptance Criteria` section using grep patterns (`scripts/humanize.sh:48`, `scripts/humanize.sh:52`), computes active tasks as active-section rows minus completed/deferred statuses (`scripts/humanize.sh:56`, `scripts/humanize.sh:64`, `scripts/humanize.sh:70`, `scripts/humanize.sh:76`, `scripts/humanize.sh:82`), counts completed/deferred/open-issue table rows (`scripts/humanize.sh:86`, `scripts/humanize.sh:96`, `scripts/humanize.sh:100`), and truncates the first meaningful ultimate-goal line to 60 characters (`scripts/humanize.sh:104`, `scripts/humanize.sh:106`).
- gates_or_invariants: The parser assumes exact section headings such as `### Acceptance Criteria`, `#### Active Tasks`, `### Completed and Verified`, `### Explicitly Deferred`, and `### Open Issues`. The tests encode graceful failure for malformed files by expecting zero counts rather than crashes (`tests/robustness/test-goal-tracker-robustness.sh:304`). They also require large-count support, special-character tolerance, and partial recovery from truncated or binary-containing input. The final invariant is no failed assertions: `print_test_summary` determines process exit (`tests/robustness/test-goal-tracker-robustness.sh:530`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` and production `scripts/humanize.sh` (`tests/robustness/test-goal-tracker-robustness.sh:18`, `tests/robustness/test-goal-tracker-robustness.sh:21`). The production parser is used by monitor/status functions in `scripts/humanize.sh` and related monitor utilities; the setup script creates the goal tracker shape this parser expects (`scripts/setup-rlcr-loop.sh:652`, `scripts/setup-rlcr-loop.sh:690`, `scripts/setup-rlcr-loop.sh:721`, `scripts/setup-rlcr-loop.sh:727`, `scripts/setup-rlcr-loop.sh:732`, `scripts/setup-rlcr-loop.sh:737`).
- edge_cases_or_failure_modes: The production output is pipe-delimited, so a goal summary containing `|` would confuse `parse_result` and any similar consumers; the tests do not cover that. The mixed-format test comments say decimal AC sub-numbering is not counted, but the assertion only requires at least two matches (`tests/robustness/test-goal-tracker-robustness.sh:493`), and the production regex may match the integer prefix of a decimal AC line because it is not anchored after the digits (`scripts/humanize.sh:53`). Active task counting is table-row based, so placeholder rows like `(none)` can be counted unless section content is carefully shaped. Exact heading dependence is intentional but brittle for renamed sections.
- validation_or_tests: This file is the validation harness. It is included in the full suite by `tests/run-all-tests.sh` according to the repository scan, and it exits through `print_test_summary`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `5 of 5 item headings present; each assigned item is represented once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none detected`
- final_worker_status: `complete`