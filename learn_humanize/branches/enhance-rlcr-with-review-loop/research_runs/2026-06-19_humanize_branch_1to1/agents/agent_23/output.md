# agent_23 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-023 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that converts Claude transcript TodoWrite state into a hard exit gate before RLCR review/finalize can proceed. It is explicitly wired into [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:321), before Codex review and before finalize completion checks.
- algorithmic_behavior: [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/check-todos-from-transcript.py:20) scans a JSONL transcript line by line, ignores blank and malformed JSONL rows, and keeps the latest nonempty TodoWrite payload. It recognizes three transcript shapes: Claude assistant message content blocks at lines 51-64, an alternate `type: message` shape at lines 65-76, and direct `type: tool_use` rows at lines 78-85. In `main`, it reads hook input JSON from stdin, extracts `transcript_path`, expands `~`, fetches latest todos, and treats every status other than `completed` as incomplete at lines 90-126.
- inputs_outputs_state: Input is stdin JSON containing `transcript_path`; missing path exits successfully at lines 99-101. Secondary input is the transcript JSONL file. Output is process exit status plus optional text: `0` when no todos exist or all latest todos are completed, `1` with `INCOMPLETE_TODOS` and per-task details when any latest todo is incomplete, and `2` with `PARSE_ERROR` on hook-input JSON parse failure. The script is stateless; the transcript is the only state source.
- gates_or_invariants: The only accepted terminal todo status is the exact string `completed` at lines 115-119. A nonexistent transcript is permissive and returns no todos at lines 25-26. Invalid JSONL transcript rows are ignored rather than fatal at lines 36-39. The latest nonempty TodoWrite list wins, so an earlier incomplete list can be superseded by a later completed list.
- dependencies_and_callers: Uses only Python stdlib modules `json`, `sys`, and `pathlib` at lines 15-17. The stop hook calls it with the full hook input and maps exit `2` to a blocking parse-error JSON response, exit `1` to an incomplete-todos block, and exit `0` to continued gate evaluation in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:323).
- edge_cases_or_failure_modes: If the transcript path is absent, nonexistent, empty, or has no TodoWrite entries, the gate allows progress. If the latest TodoWrite call has an empty `todos` array, the implementation does not replace the previous nonempty list, which can preserve stale todo state. If `todos` is malformed as a non-list truthy value, `main` may fail while iterating because it assumes each entry has `.get`. File read errors after `exists()` are not caught.
- validation_or_tests: Direct coverage exists in [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-todo-checker.sh:51): invalid and empty stdin JSON exit `2`; missing or nonexistent transcript exits `0`; incomplete, pending, in-progress, missing-status, and empty-content todos exit `1`; invalid JSONL transcript rows are ignored; multiple TodoWrite calls use the latest nonempty one; direct and alternate message formats are accepted. Integration coverage appears in [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-finalize-phase.sh:698), where finalize phase blocks on incomplete todos and verifies Codex is not invoked.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state machine. It defines the expected transitions among implementation, review, finalize, max-iteration, and completed terminal states, and verifies the hooks preserve those boundaries.
- algorithmic_behavior: The test builds an isolated temporary git repository, loop directory, state file, plan backup, and goal tracker in setup helpers at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-finalize-phase.sh:40). It then replaces the real `codex` executable with mocks for normal output, tracked invocation, review failure, and empty review output at lines 51-141. The assertions cover helper predicates, active-loop discovery, tool validators, stop-hook finalize gates, COMPLETE handling, review failure handling, todo completion gating, normal round continuity, and validator parsing when only `finalize-state.md` exists.
- inputs_outputs_state: Inputs are the repository hooks under test, a synthetic `state.md` frontmatter file, generated summaries, optional transcript JSONL, mocked `codex exec` and `codex review` output, git cleanliness, and `CLAUDE_PROJECT_DIR`. Output is TAP-like pass/fail text and the script exits with `TESTS_FAILED` at line 876. State transitions asserted include `state.md` to `finalize-state.md` after `COMPLETE` plus clean review, `finalize-state.md` to `complete-state.md` when final checks pass, `state.md` to `maxiter-state.md` at max iterations, and preserving `state.md` with `review_started: true` when code review fails or emits empty output.
- gates_or_invariants: Finalize state is active loop state, while complete state is not, at lines 283-320. `finalize-state.md` is protected from Write, Edit, Bash modification, and Bash `mv` or `cp` source use at lines 337-415. Finalize requires `finalize-summary.md`, a clean git tree, and completed todos before allowing exit at lines 429-504 and 698-741. Finalize must not invoke Codex at lines 446-451 and 498-504. Max iterations skip finalize and create `maxiter-state.md` at lines 542-560. Review command failure and empty review output block transition to finalize and keep review state for retry at lines 562-696.
- dependencies_and_callers: Sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/lib/loop-common.sh:1) at line 49. Calls `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-read-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh`. Depends on `git`, shell, mocked `codex`, the hook template system, and the todo checker through the stop hook. It also relies on cache path logic matching the stop hook when checking empty review logs at lines 659-668.
- edge_cases_or_failure_modes: The script intentionally exercises review failure, empty review stdout, dirty git, missing finalize summary, incomplete todos, max-iteration completion, complete-state inactivity, and validators parsing `finalize-state.md`. It also verifies normal non-COMPLETE review feedback still increments `current_round`, creates a review result file, and keeps `state.md` rather than entering finalize at lines 743-814. A practical test fragility is that it toggles `set -e` inside cases even though the file starts with only `set -uo pipefail`; unexpected unguarded command failure later in the run could abort instead of recording a structured failure.
- validation_or_tests: This file is itself the validator for the feature. Key assertion blocks are helper function checks at lines 238-280, active-loop detection at lines 283-335, file validator checks at lines 337-415, finalize stop-hook behavior at lines 417-504, COMPLETE/max-iteration behavior at lines 507-560, review-failure blocks at lines 562-696, incomplete todo blocking at lines 698-741, normal round preservation at lines 743-814, and finalize-state parser coverage across validators at lines 816-865. I did not execute it because this branch export is read-only and the assignment asks for research notes only.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR “no remote push by default” policy. It is part of the Bash validator gate that prevents `git push` during active RLCR loops unless the loop was started with push-every-round enabled.
- algorithmic_behavior: [prompt-template/block/git-push.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/prompt-template/block/git-push.md:1) is static markdown. It tells the agent commits should stay local and points to `/humanize:start-rlcr-loop plan.md --push-every-round` as the mode switch. No variables are rendered.
- inputs_outputs_state: Input is only template loading through the shared loader. Output is markdown emitted to stderr by the Bash validator. It has no state transitions itself; it communicates the policy that the loop will handle local commits until completion.
- gates_or_invariants: The enforcement gate lives in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-bash-validator.sh:96): when `STATE_PUSH_EVERY_ROUND` is not exactly `true`, a command matching a leading `git push` is blocked, this template is loaded, and the validator exits `2`. The invariant is that push behavior must be chosen at loop start, not ad hoc during a round.
- dependencies_and_callers: Loaded by `load_and_render_safe` at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-bash-validator.sh:99). The template loader resolves templates from `prompt-template` via [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/lib/template-loader.sh:24), and falls back to inline text if a template is absent at lines 167-203.
- edge_cases_or_failure_modes: Because the template has no placeholders, rendering cannot corrupt dynamic content. Enforcement depends on the Bash validator command recognizer; the cited gate catches commands beginning with `git push` after lowercasing, but policy bypass risk belongs to command parsing rather than this template. If the file is missing, the validator still blocks with its fallback message.
- validation_or_tests: Template loading is directly covered in [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-template-loader.sh:59), which expects content containing “Git Push Blocked”, and by safe-render fallback coverage at lines 215-221. Comprehensive template coverage repeats the real-template assertion in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-templates-comprehensive.sh:455).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Prompt-footer template that turns the `push_every_round` state flag into an explicit per-round agent instruction. It complements the stop-hook gate that blocks unpushed commits when that mode is enabled.
- algorithmic_behavior: [prompt-template/claude/push-every-round-note.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/prompt-template/claude/push-every-round-note.md:2) is a static note: when `--push-every-round` is enabled, the agent must push commits to remote after each round. No variables are accepted or transformed.
- inputs_outputs_state: Input is the parsed state value `PUSH_EVERY_ROUND`. Output is appended text in the next-round prompt file. The template does not mutate state; it reinforces the round contract after the stop hook has advanced into another blocked continuation prompt.
- gates_or_invariants: The prompt inclusion gate is at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:1508): only exact `PUSH_EVERY_ROUND == true` appends this note. The corresponding enforcement gate is earlier in the same hook at lines 552-578, where local commits ahead of remote block exit when push-every-round is enabled.
- dependencies_and_callers: Loaded by `load_template` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:1510) after the next-round footer is rendered. If missing, the hook appends the inline fallback “Also push your changes after committing” at lines 1511-1514.
- edge_cases_or_failure_modes: The note is appended only to next-round prompts generated after review feedback; it is not a validator by itself and cannot enforce remote synchronization without the stop-hook unpushed-commit check. If the state field is missing or not exactly `true`, the note is skipped because parsed defaults treat push-every-round as false.
- validation_or_tests: Indirectly covered by finalize/normal-round tests because test state includes `push_every_round: false` at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-finalize-phase.sh:197), keeping push behavior out of normal finalize paths. The loader behavior used for this template is covered generically by [tests/test-template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-template-loader.sh:55). No focused test was found that asserts this exact note is appended when the flag is true.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence sections, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`