# agent_24 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-024 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude Code hook registry for the Humanize RLCR and PR loop state machines. It binds user-prompt, pre-tool, and stop events to the actual validator/transition scripts, so this file is the dispatch table that makes the loop enforcement active.
- algorithmic_behavior: `UserPromptSubmit` runs `loop-plan-file-validator.sh`, which checks active RLCR loop state before each prompt. `PreToolUse` dispatches by matcher: `Write` to `loop-write-validator.sh`, `Edit` to `loop-edit-validator.sh`, `Read` to `loop-read-validator.sh`, and `Bash` to `loop-bash-validator.sh` ([hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/hooks.json:4), [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/hooks.json:14)). `Stop` runs both RLCR and PR loop stop hooks with long 7200s timeouts ([hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/hooks.json:52)).
- inputs_outputs_state: Input is Claude Code hook event JSON and environment, especially `CLAUDE_PLUGIN_ROOT`; output is whichever child hook emits, usually exit `0` to allow, exit `2` from pre-tool validators to block a tool, or a JSON `{"decision":"block"}` response from prompt/stop hooks. State lives outside this JSON in `.humanize/rlcr/<timestamp>/state.md`, `.humanize/rlcr/<timestamp>/finalize-state.md`, and `.humanize/pr-loop/<timestamp>/state.md`, parsed by the scripts it references.
- gates_or_invariants: The registry enforces coverage across the main mutation/read channels: direct `Write`/`Edit`, indirect `Bash`, stale/wrong `Read`, prompt-submission state checks, and exit attempts. Important invariant: generated loop files, state files, prompts, todos, summaries, goal trackers, branch consistency, plan-file tracking, and review/finalize transitions are guarded by scripts rather than by the prompt alone.
- dependencies_and_callers: Consumed by Claude Code’s plugin hook system; depends on `${CLAUDE_PLUGIN_ROOT}` resolving to the plugin root. Child scripts share `hooks/lib/loop-common.sh` and templates via `hooks/lib/template-loader.sh`. Tests reference the registered hook scripts directly, for example `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: If `CLAUDE_PLUGIN_ROOT` is unset or wrong, every registered command path breaks. If a tool/matcher name changes upstream, the relevant guard silently stops covering that tool class. The stop hook runs RLCR then PR hook in the same `Stop` event; safety relies on each hook exiting early when its own loop is inactive.
- validation_or_tests: `tests/test-template-references.sh` scans shell scripts for referenced templates and checks critical validators use safe template rendering. Hook behavior is covered across `tests/test-plan-file-hooks.sh`, `tests/test-finalize-phase.sh`, `tests/test-pr-loop-hooks.sh`, `tests/test-pr-loop-stophook.sh`, `tests/test-allowlist-validators.sh`, and robustness tests for hook JSON input. I did not execute tests because this worker was instructed not to modify files, and several tests create temporary fixtures/state.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-054 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for `/humanize:gen-plan` command packaging and IO validation. It is adjacent to the RLCR loop because gen-plan produces implementation plans that can feed `/start-rlcr-loop`, but it is not itself the RLCR review-loop transition engine.
- algorithmic_behavior: Positive tests assert `commands/gen-plan.md` exists and has YAML fields `description`, `allowed-tools`, and `argument-hint`; `agents/draft-relevance-checker.md` exists with `name: draft-relevance-checker`, `model: haiku`, and a `tools:` field; plugin, marketplace, and README versions match ([tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:54), [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:107), [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:160)). Negative tests create invalid fixtures to verify name regex, missing frontmatter, malformed YAML, invalid model names, and non-English/emoji detection ([tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:191)).
- inputs_outputs_state: Inputs are repository files under `commands/`, `agents/`, `.claude-plugin/`, `README.md`, and `scripts/validate-gen-plan-io.sh`. It creates temp fixture directories with `mktemp -d` and cleans them by `trap`. Output is colored PASS/FAIL lines plus final exit `0` only if `TESTS_FAILED == 0`, otherwise exit `1` ([tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:547)).
- gates_or_invariants: Command/agent names must match `^[a-z][a-z0-9-]*$`; command and agent markdown must have required YAML frontmatter; accepted model names are exact short aliases `opus|sonnet|haiku` or full IDs prefixed by `claude-`, `gpt-`, `o[0-9]`, or `gemini-`; `gen-plan.md` and relevance agent content must avoid CJK and emoji; `validate-gen-plan-io.sh` must return documented exit codes for invalid/missing args and path failures ([tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:195), [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:350), [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:431)).
- dependencies_and_callers: Depends on `commands/gen-plan.md`, whose workflow validates IO, invokes the `draft-relevance-checker` agent, analyzes the draft, asks clarification questions, then edits the generated plan ([commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/commands/gen-plan.md:19)). Depends on `scripts/validate-gen-plan-io.sh`, which validates `--input/--output`, rejects existing output, copies `prompt-template/plan/gen-plan-template.md`, and appends the original draft ([scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/scripts/validate-gen-plan-io.sh:79), [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/scripts/validate-gen-plan-io.sh:148)).
- edge_cases_or_failure_modes: The test’s YAML checks are intentionally shallow shell regexes, not a full YAML parser. `grep -P` content checks are skipped only by command failure behavior; on systems without PCRE support, the condition simply does not fail. The test treats `--help` returning exit `6` as expected, matching the validator’s usage-path convention rather than standard CLI success. `realpath -m` in the validator may be GNU-specific, though the script falls back to the original path if unavailable.
- validation_or_tests: This file is the validation artifact. It also exercises `validate-gen-plan-io.sh` exit codes: missing flag values and unknown/help options exit `6`, missing input exits `1`, empty input exits `2`, missing output dir exits `3`, existing output or directory output exits `4`, and valid paths exit `0` ([tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/tests/test-gen-plan.sh:438)). Not executed in this research run because it creates temp files and invokes a script that writes an output plan during valid-path validation.
- skip_candidate: `yes: it is an executable spec for gen-plan packaging/IO, not a direct RLCR state-machine hook or review transition; it remains relevant because gen-plan supplies structured plans consumed by RLCR.`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Blocking message template for the RLCR stop hook’s fail-closed repository-state gate. It defines the user-facing reason when `git status --porcelain` cannot be verified.
- algorithmic_behavior: The template renders `{{GIT_STATUS_EXIT}}` into a block explaining that git status failed or timed out and lists likely causes: lock contention, invalid repository state, or large repository slowness ([prompt-template/block/git-status-failed.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/prompt-template/block/git-status-failed.md:1)). In `loop-codex-stop-hook.sh`, after checking that the project is a git repository, the hook caches `git status --porcelain`; any nonzero status renders this template and emits `{"decision":"block"}` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:372), [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:388)).
- inputs_outputs_state: Input variable is `GIT_STATUS_EXIT`; output is markdown embedded as the `reason` field in a JSON block decision, with system message `Loop: Blocked - git status failed (exit N)` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:395)). It does not mutate state; it prevents the stop hook from advancing to large-file checks, clean-tree checks, Codex review, review phase, or finalize phase while repository state is unknown.
- gates_or_invariants: Fail-closed invariant: if repository status cannot be determined, the loop must not allow exit or completion. This protects later gates that depend on reliable dirty-file detection, large-file scanning, commit cleanliness, and review-base correctness.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh`, which comes from `hooks/lib/template-loader.sh`. The template loader performs single-pass substitution and falls back to inline text if the template is missing ([hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/lib/template-loader.sh:54), [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/lib/template-loader.sh:170)).
- edge_cases_or_failure_modes: If the template is absent, the inline fallback is shorter and omits the detailed cause list. If `GIT_STATUS_EXIT` is `124`, the message covers timeout via the same path. If `git rev-parse --git-dir` itself fails, the hook skips git status caching rather than using this template, so this template specifically covers status failure after repo detection.
- validation_or_tests: `tests/test-template-references.sh` verifies referenced templates exist and safe rendering is used. Stop-hook behavior around git failure is in the broader stop-hook/finalize test family; this template’s direct existence is validated by template reference scanning.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-114 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Prompt template for the RLCR Review Phase repair loop. It is generated after `codex review --base <branch>` finds `[P0-9]` issues, instructing Claude to fix review findings and write a round summary.
- algorithmic_behavior: The template inserts `{{REVIEW_CONTENT}}` and `{{SUMMARY_FILE}}`, then directs Claude to address all `[P0-9]` severity issues, make fixes only, commit changes, and summarize what was fixed or unresolved ([prompt-template/claude/review-phase-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/prompt-template/claude/review-phase-prompt.md:5)). The stop hook calls `continue_review_loop_with_issues`, increments/persists the review round in `state.md`, writes `round-N-prompt.md` from this template, and blocks exit with that prompt as the next instruction ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:1082)).
- inputs_outputs_state: Inputs are extracted review findings and the target summary path. Output is a new `.humanize/rlcr/<loop>/round-N-prompt.md` and a JSON stop-hook block decision with system message `Loop: Review Phase Round N - Fix code review issues` ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:1095), [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:1118)).
- gates_or_invariants: Review phase ignores `COMPLETE`; completion requires `codex review` to produce no `[P0-9]` markers. The paired detector scans the combined review log for `[Pdigit]` markers in the first 10 characters of a line, extracts from the first such line through EOF, and treats missing/empty logs as a hard error ([hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/lib/loop-common.sh:326)). No-issue review output advances to finalize; issue output loops back through this template ([hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/hooks/loop-codex-stop-hook.sh:965)).
- dependencies_and_callers: Called only from `hooks/loop-codex-stop-hook.sh` after `run_and_handle_code_review` invokes `codex review --base`. The two-phase behavior is documented in `commands/start-rlcr-loop.md`, where Review Phase begins after Codex outputs `COMPLETE` and continues until no `[P0-9]` issues are found ([commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/enhance-rlcr-with-review-loop/commands/start-rlcr-loop.md:16)).
- edge_cases_or_failure_modes: Review findings that contain literal `{{...}}` are protected from recursive substitution by the template loader’s single-pass rendering. Findings not formatted with `[P0-9]` near the start of a line are not considered blocking by `detect_review_issues`. If Codex review fails or emits no usable log, the loop uses `block/codex-review-failed.md` instead of this prompt and requires retry.
- validation_or_tests: `tests/test-finalize-phase.sh` includes review-phase assertions that failed or empty Codex review preserves `state.md` with `review_started: true`, and broader stop-hook tests exercise review/finalize transitions. `tests/test-template-references.sh` validates this template is present because it is referenced by the stop hook.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item sections present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`